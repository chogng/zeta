{
  "name": "zeta",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build:desktop": "corepack pnpm --dir desktop build",
    "dev:desktop": "corepack pnpm --dir desktop dev",
    "fetch:pdfium": "node scripts/fetch-pdfium.mjs",
    "generate:protocol": "cargo run --quiet --manifest-path zeta-rs/Cargo.toml -p zeta-app-server-protocol --bin write_schema_fixtures && node desktop/scripts/sync-app-server-protocol.mjs",
    "verify:protocol": "cargo test --manifest-path zeta-rs/Cargo.toml -p zeta-app-server-protocol"
  }
}
