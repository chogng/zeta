name: Promote Zeta Code stable release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Semantic version to promote without the v prefix
        required: true
        type: string

permissions:
  contents: write

jobs:
  promote:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      GH_TOKEN: ${{ github.token }}
      ZETA_UPDATE_PUBLIC_KEY: ${{ vars.ZETA_UPDATE_PUBLIC_KEY }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Build update signer
        run: python3 -B scripts/cargo.py build --package zeta-update-sign --bin zeta-update-sign

      - name: Sign stable release pointers
        env:
          ZETA_UPDATE_SIGNING_KEY: ${{ secrets.ZETA_UPDATE_SIGNING_KEY }}
        run: |
          version="${{ inputs.version }}"
          case "$version" in
            *[!0-9.]*|.*|*.) exit 2 ;;
          esac
          release_tag="v${version}"
          output=".build/stable"
          mkdir -p "$output"
          for target in \
            aarch64-apple-darwin \
            x86_64-apple-darwin \
            aarch64-unknown-linux-gnu \
            x86_64-unknown-linux-gnu \
            aarch64-pc-windows-msvc \
            x86_64-pc-windows-msvc
          do
            case "$target" in
              *-apple-darwin) archive="zeta-code-${target}.zip"; format="zip" ;;
              *) archive="zeta-code-${target}.tar.gz"; format="tar-gz" ;;
            esac
            gh release download "$release_tag" --pattern "$archive" --dir "$output"
            .build/cargo/debug/zeta-update-sign \
              --repository chogng/zeta \
              --product zeta-code \
              --channel stable \
              --version "$version" \
              --release-tag "$release_tag" \
              --target "$target" \
              --format "$format" \
              --archive "$output/$archive" \
              --output "$output/zeta-code-stable-${target}.update.json" \
              --public-key "$ZETA_UPDATE_PUBLIC_KEY"
          done

      - name: Publish stable release pointers
        run: |
          gh release view zeta-code-stable >/dev/null 2>&1 || \
            gh release create zeta-code-stable \
              --target "$GITHUB_SHA" \
              --title "Zeta Code stable channel" \
              --notes "Signed pointers managed by the stable promotion workflow." \
              --prerelease \
              --latest=false
          gh release upload zeta-code-stable .build/stable/*.update.json --clobber
