name: 💻 CLI Bug
description: Report an issue with the Zeta CLI
labels:
- cli
body:
  - type: markdown
    attributes:
      value: |
        Before submitting a new issue, please search for existing issues to see if your issue has already been reported.
        If it has, please add a 👍 reaction (no need to leave a comment) to the existing issue instead of creating a new one.

        Do not report security vulnerabilities in a public issue. Follow the repository Security Policy instead.

        Make sure you are running the [latest](https://npmjs.com/package/@chogng/zeta) version of Zeta CLI. The bug you are experiencing may already have been fixed.

        If your version supports it, please run `zeta doctor --json` and paste the output in the "Zeta doctor report" field below. This helps us diagnose install, config, auth, terminal, MCP, network, and local state issues.
  - type: input
    id: version
    attributes:
      label: What version of the Zeta CLI are you using?
      description: Use `zeta --version`
    validations:
      required: true
  - type: input
    id: model
    attributes:
      label: Which model were you using?
      description: Like `gpt-5.6-sol`, `claude-fable-5`, etc.
  - type: input
    id: platform
    attributes:
      label: What platform is your computer?
      description: |
        For macOS and Linux: copy the output of `uname -mprs`
        For Windows: copy the output of `"$([Environment]::OSVersion | ForEach-Object VersionString) $(if ([Environment]::Is64BitOperatingSystem) { "x64" } else { "x86" })"` in the PowerShell console
  - type: input
    id: terminal
    attributes:
      label: What terminal emulator and version are you using (if applicable)?
      description: |
        Also note any multiplexer in use (screen / tmux / zellij).
        E.g., VS Code, Terminal.app, iTerm2, Ghostty, Windows Terminal (WSL / PowerShell)
  - type: input
    id: multiplexer
    attributes:
      label: Are you using a terminal multiplexer?
      description: 'Examples: tmux, zellij, screen. Write `none` if not applicable.'
      placeholder: none / tmux 3.x / zellij ...
  - type: textarea
    id: doctor
    attributes:
      label: zeta doctor report
      description: |
        If available, run `zeta doctor --json` and paste the full output here.

        The report is designed to redact secrets, but please review it before submitting.
        If your Zeta version does not support `doctor`, write `not available`.
      render: json
  - type: textarea
    id: actual
    attributes:
      label: What issue are you seeing?
      description: Please include the full error messages and prompts with PII redacted. If possible, please provide text instead of a screenshot.
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: What steps can reproduce the bug?
      description: Explain the bug and provide a code snippet that can reproduce it. Please include thread id if applicable.
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: What is the expected behavior?
      description: If possible, please provide text instead of a screenshot.
  - type: textarea
    id: notes
    attributes:
      label: Additional information
      description: TERM/COLORTERM values, logs, screenshots, related issues, or recent regressions.
