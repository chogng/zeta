# 行为规格

本次实现遵守[设计及 AC-1 至 AC-6](../../memory-diagnostics.md)。

开始诊断必须创建独立的连接所属资源，重复开始不重复建立采样工作。读取和导出必须校验 owner；停止幂等。失效连接的证据不能进入新诊断。关闭展示不停止采集，连接关闭必须释放采集。

正常负载增长、证据缺失与确定的对象保留分开报告。没有运行时证据时只能提供增长线索。三端入口必须可用键盘操作；产品错误不能被静默吞掉。
