identifier!(
    /// Stable identity for one coordinated development goal.
    WorkRunId,
    "work run ID"
);

identifier!(
    /// Stable identity shared by every immutable version of one work contract.
    WorkContractId,
    "work contract ID"
);

identifier!(
    /// Stable identity for one Agent execution against an exact contract version.
    WorkAttemptId,
    "work attempt ID"
);

identifier!(
    /// Stable identity for one explicit relationship between work attempts.
    WorkRelationId,
    "work relation ID"
);

identifier!(
    /// Stable identity for one accepted work decision.
    WorkDecisionId,
    "work decision ID"
);

identifier!(
    /// Stable identity for one process generation executed by a work attempt.
    WorkExecutionId,
    "work execution ID"
);

identifier!(
    /// Stable identity for one recorded coordination conflict.
    WorkConflictId,
    "work conflict ID"
);
