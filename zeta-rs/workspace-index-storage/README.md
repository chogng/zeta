# Workspace Index Storage

- Owns the profile-local layout for rebuildable Workspace indexes.
- Coordinates index use and explicit deletion across processes.
- Keeps lock files outside deletable index directories.
