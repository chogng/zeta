load("//:defs.bzl", "zeta_rust_crate")

zeta_rust_crate(
    name = "pty",
    crate_name = "zeta_utils_pty",
    # Real terminal scenarios use the host Python, shell, and Windows console commands.
    test_env_inherit = ["PATH"] + select({
        "@platforms//os:windows": ["COMSPEC", "PATHEXT", "SystemRoot"],
        "//conditions:default": [],
    }),
)
