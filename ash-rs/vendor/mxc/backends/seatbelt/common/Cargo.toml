[package]
name = "seatbelt_common"
version = "0.8.0"
edition = "2021"
license = "MIT"

[dependencies]
wxc_common = { "path" = "../../../core/wxc_common" }

[target."cfg(target_os = \"macos\")".dependencies]
libc = { "version" = "0.2" }
