# 实现计划

1. 已实现：发现 Codex 安装，有 Codex 时只读，无 Codex 时由 Ash 维护认证；测试显式隔离管理模式。
2. 已实现：按 Codex 的五分钟/八天规则刷新，保留省略的 token 字段及其他元数据，更新 last_refresh，缓存永久失败并允许重新登录。
3. 已实现：进程间锁、重读与提交前校验；复用模式仅断开，自管模式登出清除认证。
4. 已实现：普通/流式请求读取当前凭据；仅明确 HTTP 401 且未交付输出时恢复一次，不跨账户、不重放已接受流。
5. 已完成：认证与恢复回归、Codex CLI 兼容读取、正常后端构建、Luna / low 真实只读调用。结果见 verification.md。

此次维护修正未改变账户 RPC 类型或前端布局；沿用已验证的 connected/deviceCode/browser 结果与界面。
