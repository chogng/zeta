interface:
  display_name: "App Server API Integration"
  short_description: "Connect desktop domains to a Rust app-server"
  default_prompt: "Use $app-server-api-integration to replace the TypeScript backend runtime with a Rust app-server while preserving frontend domain services, per-renderer connections, exact file ownership, and conflict gating."
