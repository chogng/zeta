interface:
  display_name: "App Server API Integration"
  short_description: "Replace VS Code TS backends with a Rust app-server"
  default_prompt: "Use $app-server-api-integration to replace a VS Code TypeScript or Node backend capability with a Codex-style Rust app-server while preserving the VS Code frontend contract."
