interface:
  display_name: "App Server API Integration"
  short_description: "Make app-server the product business backend"
  default_prompt: "Use $app-server-api-integration to expand a Rust app-server into the product business backend while preserving the TypeScript editor platform, with domain-owned crates, renderer-owned protocol clients, a transparent Main relay, exact ownership, and conflict gating."
