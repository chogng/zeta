interface:
  display_name: "App Server API Integration"
  short_description: "Design the TypeScript desktop frontend to Rust app-server boundary"
  default_prompt: "Use $app-server-api-integration to design or implement the final TypeScript desktop frontend to Rust app-server boundary, including session ownership, bidirectional protocol semantics, lifecycle, and exact file placement."
