interface:
  display_name: "VS Code Change Review"
  short_description: "Explicit-only VS Code commit review"
  default_prompt: "Use $vscode-change-review to inspect VS Code commits since the recorded checkpoint and report which changes may need Zeta work without modifying product source."

policy:
  allow_implicit_invocation: false
