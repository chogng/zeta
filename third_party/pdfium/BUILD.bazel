package(default_visibility = ["//visibility:public"])

# Release CI materializes the verified archive under third_party/.cache. Keeping
# this target empty in a source checkout follows the same archive-only pattern
# used by Codex's PowerShell runtime target.
filegroup(
    name = "runtime",
    srcs = glob([".cache/**"], allow_empty = True),
    tags = ["manual"],
)
