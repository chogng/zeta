# WezTerm PTY provenance

Ash does not bundle the WezTerm terminal emulator. Its adapted Codex PTY
integration uses WezTerm-derived Windows ConPTY files; retain this MIT license
in release notices. See `ash-rs/utils/pty/NOTICE` for complete provenance.
