package(default_visibility = ["//visibility:public"])

exports_files(["pwsh.exe"])

# PowerShell is an optional Windows release runtime. The archive is materialized
# only in signed release staging; source checkouts intentionally have no binary.
filegroup(
    name = "runtime",
    srcs = glob([".cache/**"], allow_empty = True),
    tags = ["manual"],
)
