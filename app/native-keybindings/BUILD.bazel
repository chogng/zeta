load("//app:defs.bzl", "app_rust_library")

app_rust_library(
    name = "native-keybindings",
    crate_name = "zeta_keybindings_host",
    package_name = "app/native-keybindings",
    unit_tests = True,
)
