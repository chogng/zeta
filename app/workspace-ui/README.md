# zeta-workspace-ui

Workspace Files and Changes panes, their retained state, layout, and typed user actions.

This crate owns feature behavior and UI dependencies. The app host supplies filesystem and
source-control snapshots and executes actions such as opening a file, loading a directory, and
refreshing repository data.
