load("//app:defs.bzl", "app_rust_library")

app_rust_library(
    name = "workbench",
    crate_name = "zeta_workbench",
    crate_root = "lib.rs",
    package_name = "app/workbench",
    source_files = ["README.md"],
    source_globs = ["**/*.rs"],
    unit_tests = True,
)
