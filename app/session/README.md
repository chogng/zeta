# zeta-session

1. Owns Session Thread state, incremental updates, search, and retained interaction state.
2. Owns Session canvas, timeline, context menu, and Workbench input description.
3. Accepts transport results from the host without owning windows or App Server transport.
