{
  "formatVersion": 1,
  "signatureRecord": "app-signature.json",
  "releaseRequired": true,
  "authenticatedResources": {
    "remoteRuntimeCatalog": "HTTPS URL when network-backed and SHA-256 compiled into the signed binary",
    "remoteRuntimeArtifacts": "SHA-256 records in the authenticated catalog"
  },
  "platforms": {
    "darwin": {
      "tool": "codesign",
      "signatureMode": "embedded",
      "identityEnvironment": "ZETA_MACOS_SIGNING_IDENTITY",
      "verification": ["codesign", "--verify", "--strict", "--verbose=2"],
      "notarizationProfileEnvironment": "ZETA_MACOS_NOTARY_PROFILE"
    },
    "linux": {
      "tool": "cosign",
      "signatureMode": "detached",
      "signatureFile": "app-signature.sig",
      "identityEnvironment": "APP_COSIGN_IDENTITY",
      "verification": ["cosign", "verify-blob"]
    },
    "windows": {
      "tool": "signtool",
      "signatureMode": "embedded",
      "identityEnvironment": "ZETA_WINDOWS_SIGNING_THUMBPRINT",
      "timestampUrlEnvironment": "ZETA_WINDOWS_TIMESTAMP_URL",
      "verification": ["signtool", "verify", "/pa", "/all", "/v"]
    }
  }
}
