# 实现计划

1. 已实现：拆开字段选择和编辑状态，更新 Enter/Esc 和草稿测试（AC-1～3）。
2. 已实现并通过后端定向测试：接完已有结构化模型发现改动，验证成功、空目录、失败、重试、只读请求（AC-4）。
3. 已实现并通过通用组件测试：通用 TabList 读取启用状态，集中处理跳过、替换、全部禁用及样式（AC-5）。
4. 已完成自动化验收：更新受影响字符快照、协议产物和长期规格，执行包级测试与普通构建检查（AC-6）。

验证过程与剩余项见 [verification.md](verification.md)。

5. 已完成：按用户补充要求抽出通用 TextField，移除表单的文字输入模式与逐字段取消逻辑，补控件测试并重跑受影响 TUI 行为及普通构建（AC-7）。
