# 本次验收要求

- AC-1：Config 提供本地化「状态栏风格」，默认「简洁」，可切换「生动」，使用现有按键、保存与错误处理。
- AC-2：风格存入 `[tui].statusLineStyle`，只接受 compact / rich；重载后生效，保留条目顺序及其他设置，拒绝无效值。
- AC-3：生动模式为已选条目加入 Emoji，并为 Plan、可选上下文条目显示进度条和数值；简洁模式保持文字数值。上下文使用当前 Thread 最近一轮数据与匹配模型容量，未知不伪造，估计值明确标记。
- AC-4：窄屏压缩进度条并保留数值，不拆分组合字形，不改变底部两行和权限信息布局；无数据、零容量和切换 Thread 不显示错误进度。
- AC-5：配置键盘交互、持久化、字符渲染、真实 PTY 与所属包构建通过。
