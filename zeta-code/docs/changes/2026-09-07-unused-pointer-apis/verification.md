# 清理无入口的鼠标接口

本次修复 TUI 普通构建报告的 `dead_code` 警告，不改变现行交互规则。

- 对应要求：[终端鼠标规则](../../spec/terminal.md#鼠标规则)；设计：[TUI](../../design/tui.md)。
- AC-1：删除固定面板、正文和会话管理中不再被事件循环调用的鼠标命中、激活接口，以及列表专用鼠标滚动状态；普通 `zeta-tui` 构建不再报告这些警告。
- AC-2：保留键盘导航、动作映射、审批防重复提交、提问翻页和覆盖浮层鼠标行为。把旧接口测试改用键盘入口；删除只验证已退场鼠标入口的测试，保留布局和字符输出断言。

## 2026-09-07 验证

基线：`e5f56394908b744eaf7313cd92dbfdb56df2f752`；开始时工作区干净。候选包括本次 `zeta-code/tui/src/` 差异；`git diff -- zeta-code/tui/src` 的 SHA-256 为 `16ea138334d257dbfbc3a89091209466eef3d51a4edfa6d15ea3bc046b3330f2`。验证期间用户另有 Issue 模板修改，不属于本次范围。

| 验证 | 结果 |
| --- | --- |
| `just check zeta-tui` | 最终代码通过，无警告（3m 40s） |
| `just test zeta-tui --lib` | 首轮 699 通过、2 失败、1 忽略；两项失败已修正运行方式或预期并定向重跑通过 |
| PTY 定向重跑下列两项 | 2 通过、0 失败，无编译警告 |
| `git diff --check` 与文档相对链接 | 通过 |

本次是不可达代码清理，复用事件循环对固定区域忽略鼠标、浮层鼠标交互的现有回归测试；未新增产品功能或扩大鼠标捕获范围。

首轮失败中，动作映射测试仍断言鼠标点击后的旧选中位置，已改为验证键盘选择第二项后保持第二项；终端历史协议测试因普通管道没有终端设备而失败，改在 80×24 PTY 中运行并响应光标位置查询。最初的普通检查在系统加载依赖期间停止，最终普通检查已完成；未调整增量编译模式或系统设置。

定向重跑命令（80×24 PTY）：

```sh
just test zeta-tui --lib -- --exact app::state::tests::keyboard_activation_uses_the_feature_action_mapping terminal::session::tests::transcript_output_protocol_keeps_the_main_buffer_and_existing_history
```

按首轮与失败项重跑合计：701 项通过，1 项原有测试忽略。没有重复运行全部测试；最后删除空条件只清理无操作语句，普通构建再次验证最终代码。

AC-1、AC-2 已通过本次自动化验收；本次仅自查，未进行独立审查。
