# 实现与验证

1. 已完成：复用账户协议，为 Rust 客户端补齐账户调用。
2. 已完成：在终端配置功能中持有订阅操作状态，复用列表组件，接通完成通知。
3. 已完成离线验证：入口、登录和取消、失败重试、旧通知、退出、返回和重新进入。
4. 已完成：受影响 package 定向测试、正常构建与真实终端入口验证，结果见 verification.md。真实 OpenAI 设备登录未验证。

后续认证扩展和只读复用验证见[认证兼容计划](../../../../zeta-rs/docs/changes/chatgpt-auth/plan.md)。
