// This file is generated by scripts/generate-docs.mjs. Do not edit by hand.
import type { DocGroup, ZetaDoc } from "@/lib/types";

export const docs: ZetaDoc[] = [
  {
    "slug": "README",
    "sourcePath": "docs/README.md",
    "title": "Zeta 文档导航",
    "description": "这份索引只负责跨产品、跨 crate 的系统文档；zeterm 的产品宿主、渲染、终端和输入文档集中在 zeterm/docs/README.md。按下面的边界选择入口：",
    "group": "开始了解",
    "markdown": "> 本文件是跨产品 `docs/` 的索引。新增跨产品文档必须同时加入本索引和\n> [`docs-site/scripts/generate-docs.mjs`](../docs-site/scripts/generate-docs.mjs) 的分组。\n> `zeterm` 专属系统文档由 [`zeterm/docs/README.md`](../zeterm/docs/README.md) 索引，避免产品宿主\n> 文档和共享系统文档混在同一目录。\n> 系统性的阅读方法（先系统后 crate、两层文档职责）见\n> [`documentation-guidelines.md`](documentation-guidelines.md)。\n\n## 快速理解\n\n这份索引只负责跨产品、跨 crate 的系统文档；`zeterm` 的产品宿主、渲染、终端和输入文档集中在\n[`zeterm/docs/README.md`](../zeterm/docs/README.md)。按下面的边界选择入口：\n\n| 目标 | 入口 |\n| --- | --- |\n| 理解共享 backend、协议、执行和存储 | 本目录对应的系统文档与 `zeta-rs/*/README.md` |\n| 理解 `zeterm` 产品行为 | [`zeterm/docs/README.md`](../zeterm/docs/README.md) |\n| 理解某个 crate 的实现契约 | 对应 crate 的 `README.md` |\n| 查看 docs-site 如何收录文档 | [`docs-site/scripts/generate-docs.mjs`](../docs-site/scripts/generate-docs.mjs) |\n\n## 1. 我该看哪份？\n\n按你要做的事查表，**按列出顺序读**：\n\n| 你要做什么 | 看这些（按序） |\n| --- | --- |\n| 新人理解 Zeta 全貌 | [`architecture.md`](architecture.md) → 感兴趣系统的领域文档 |\n| **搭 / 改 agent harness**（提示词、工具、循环） | [`agent-harness-implementation-plan.md`](agent-harness-implementation-plan.md)（照着做）→ [`agent-tools-spec.md`](agent-tools-spec.md)（照着抄）→ [`agent-harness-design.md`](agent-harness-design.md)（查原因） |\n| 改执行内核（Turn/Thread/恢复/取消） | [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) → [`core.md`](core.md) |\n| 做上下文预算 / 压缩 | [`core-context.md`](core-context.md) + [`agent-harness-design.md`](agent-harness-design.md) §9–§11 |\n| 做多 Agent | [`core-multi-agent.md`](core-multi-agent.md)（gate 条件见其状态头） |\n| 开发代码知识、Workspace Symbol 或跨语言导航 | [`code-intelligence.md`](code-intelligence.md) → [`syntax-analysis.md`](syntax-analysis.md) → [`lsp.md`](lsp.md) → [`code-index.md`](code-index.md) |\n| 开发工作区代码索引 / RAG | [`code-intelligence.md`](code-intelligence.md) → [`code-index.md`](code-index.md) → [`zeta-code-index` README](../zeta-rs/code-index/README.md) → [`zeta-code-index-semantic` README](../zeta-rs/code-index-semantic/README.md) → [`zeta-code-retrieval` README](../zeta-rs/code-retrieval/README.md) → [`zeta-code-index-cloud` README](../zeta-rs/code-index-cloud/README.md) |\n| 设计 Instructions / Skills / Agents 或外部导入 | [`agent-customizations.md`](agent-customizations.md) → 对应 authority 文档 |\n| 加 / 改一个工具 | [`agent-tools-spec.md`](agent-tools-spec.md) → [`tools.md`](tools.md)（契约层） |\n| 改协议 / 加 App Server 方法 | [`protocol.md`](protocol.md) → [`zeta-app-server-api.md`](zeta-app-server-api.md) → [`app-server-client.md`](app-server-client.md) |\n| 权限 / 审批 / 沙箱 | [`permissions.md`](permissions.md) → [`auto-review.md`](auto-review.md) → [`sandboxing.md`](sandboxing.md) |\n| 接 / 改模型供应商 | [`model-provider.md`](model-provider.md) → [`model-provider-config.md`](model-provider-config.md) → [`models-manager.md`](models-manager.md) |\n| 改 Desktop UI | [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) → [`ui-styling-ownership.md`](ui-styling-ownership.md) |\n| 改 `zeterm` 产品行为 | [`zeterm/docs/README.md`](../zeterm/docs/README.md) → 对应产品文档 → [`zeterm/README.md`](../zeterm/README.md) |\n| 写 / 改文档本身 | [`documentation-guidelines.md`](documentation-guidelines.md) |\n\n## 2. 文档类型\n\n每份文档属于四类之一，读法不同：\n\n| 类型 | 含义 | 过期方式 |\n| --- | --- | --- |\n| 设计 | 回答\"为什么这样设计、边界在哪\"；组件挂状态标记（已实现/部分/仅设计/推迟） | 长期维护，重审时修订 |\n| 规格 | 逐字照抄的实现规格（schema、文案、字段） | 随实现同步，改规格必须跑对应评测 |\n| 计划 | 阶段性工作计划 | **完成即过期**——做完后并入设计文档的状态标记，本体标记 Done 或删除 |\n| 参考 | 运行手册、模板、外部快照 | 按需更新 |\n\n## 3. 分类清单\n\n### Agent 与运行时（当前主战场）\n\n| 文档 | 类型 | 一句话 |\n| --- | --- | --- |\n| [`agent-harness-implementation-plan.md`](agent-harness-implementation-plan.md) | 计划 | coding 最小闭环（WI1–WI6）的文件级步骤 |\n| [`agent-tools-spec.md`](agent-tools-spec.md) | 规格 | 逐工具 schema / 描述正文 / 错误文案 + 系统提示词扩写 |\n| [`agent-harness-design.md`](agent-harness-design.md) | 设计 | harness 行为策略：提示词、循环、失败、裁剪、压缩、缓存 |\n| [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) | 设计 | 执行内核总体设计、组件状态总账、阶段 A–E |\n| [`core.md`](core.md) | 设计 | zeta-core 的 ownership、组件、端口、提交顺序 |\n| [`core-context.md`](core-context.md) | 设计 | ContextPlan / Manager / checkpoint / compaction 机制 |\n| [`core-multi-agent.md`](core-multi-agent.md) | 设计 | delegation / Fresh spawn / delivery / 隔离（部分实现，缺口见状态头） |\n| [`agent-customizations.md`](agent-customizations.md) | 设计 | Instructions / Skills / Agents、`.zeta` 与外部导入边界 |\n| [`tools.md`](tools.md) | 设计 | 工具三层契约、registry snapshot |\n| [`exec.md`](exec.md) | 设计 | 进程执行 |\n| [`plugins.md`](plugins.md) / [`connectors.md`](connectors.md) / [`skills.md`](skills.md) | 设计 | Plugin 扩展分发、Connector 外部账号连接与 Skill 指令运行时边界 |\n| [`editor-extensions.md`](editor-extensions.md) | 设计 | 声明式扩展与 Zeta 原生可执行 Host v1 的双轨边界、信任、生命周期和产品接入状态 |\n| [`mcp.md`](mcp.md) / [`mcp-server.md`](mcp-server.md) | 设计 | MCP 协议会话与能力调用、Connector ready binding，以及 Zeta 作为 MCP server |\n| [`slash-commands.md`](slash-commands.md) | 设计 | Slash Command 与统一斜杠启动面板边界 |\n\n### 协议与 API\n\n[`protocol.md`](protocol.md)（canonical 产品契约）、\n[`zeta-app-server-api.md`](zeta-app-server-api.md)、[`app-server-client.md`](app-server-client.md)、\n[`zeta-api.md`](zeta-api.md)、[`zeta-api-interface-requirements.md`](zeta-api-interface-requirements.md)、\n[`zeta-api-interface-template.md`](zeta-api-interface-template.md)（模板）、\n[`zeta-client.md`](zeta-client.md)、[`codex-app-server.md`](codex-app-server.md)（参考）\n\n### 模型与配置\n\n[`model-provider.md`](model-provider.md)、[`model-provider-config.md`](model-provider-config.md)、\n[`models-manager.md`](models-manager.md)、[`config.md`](config.md)、[`login.md`](login.md)、\n[`secrets.md`](secrets.md)\n\n### 安全与权限\n\n[`permissions.md`](permissions.md)、[`auto-review.md`](auto-review.md)、\n[`sandboxing.md`](sandboxing.md)、[`workspace-security.md`](workspace-security.md)、\n[`windows-sandbox-acceptance-runbook.md`](windows-sandbox-acceptance-runbook.md)（参考/手册）\n\n### 界面与体验\n\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md)、\n[`ui-styling-ownership.md`](ui-styling-ownership.md)、\n[`editor-architecture.md`](editor-architecture.md)、[`editor-core.md`](editor-core.md)、\n[`workbench-pane-composite-design.md`](workbench-pane-composite-design.md)、\n[`design-tokens.md`](design-tokens.md)、[`theme-authoring-template.md`](theme-authoring-template.md)（模板）、\n[`menu-system.md`](menu-system.md)、[`icons.md`](icons.md)、[`search.md`](search.md)、\n[`code-intelligence.md`](code-intelligence.md)、\n[`code-index.md`](code-index.md)、\n[`syntax-analysis.md`](syntax-analysis.md)、[`lsp.md`](lsp.md)、\n[`editor-extensions.md`](editor-extensions.md)、\n[`tui.md`](tui.md)、\n[`chat-session-inspector.md`](chat-session-inspector.md)、[`pdf.md`](pdf.md)、[`typst.md`](typst.md)\n\n### zeterm 产品\n\n[`zeterm/docs/README.md`](../zeterm/docs/README.md) 是产品文档入口，包含 Agent Console、Terminal\nSurface、Native Text Input、Rendering、UI 迁移、Native 弃用、应用迁移和发布图。\n\n### 平台与产品\n\n[`architecture.md`](architecture.md)（总入口）、[`zeta-rs-architecture.md`](zeta-rs-architecture.md)、\n[`zeta-cli-architecture.md`](zeta-cli-architecture.md)、[`product-editions.md`](product-editions.md)、\n[`product-lines.md`](product-lines.md)、[`git.md`](git.md)、\n[`documentation-guidelines.md`](documentation-guidelines.md)\n\n### 计划与迁移（会过期）\n\n| 文档 | 状态 |\n| --- | --- |\n| [`zeta-code-architecture-codex-style-v2.md`](zeta-code-architecture-codex-style-v2.md) | 长期演进方向 |\n| [`zeterm/docs/native-deprecation-plan.md`](../zeterm/docs/native-deprecation-plan.md) | Native 弃用迁移 |\n| [`zeterm/docs/ui-component-migration-plan.md`](../zeterm/docs/ui-component-migration-plan.md) | UI 组件迁移 |\n| [`zeterm/docs/zeterm-app-migration-plan.md`](../zeterm/docs/zeterm-app-migration-plan.md) | Zeterm 迁移 |\n| [`zeterm/docs/zeterm-release-graph.md`](../zeterm/docs/zeterm-release-graph.md) | Zeterm 发布依赖 |\n\n## 4. 维护规则\n\n1. 新文档：入本索引 + docs-site 分组，声明类型（设计/规格/计划/参考）；\n2. 计划类文档完成后：结论并入对应设计文档的状态标记，计划本体标 Done 或删除——\n   不允许长期留着已完成的计划冒充现状；\n3. 设计文档描述未实现组件必须挂状态标记（已实现/部分/仅设计/推迟），词表定义见\n   [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md)；\n4. 同一主题冲突时的权威关系在两份文档开头互相声明（见\n   [`documentation-guidelines.md`](documentation-guidelines.md)）。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-我该看哪份",
        "title": "1. 我该看哪份？"
      },
      {
        "depth": 2,
        "id": "2-文档类型",
        "title": "2. 文档类型"
      },
      {
        "depth": 2,
        "id": "3-分类清单",
        "title": "3. 分类清单"
      },
      {
        "depth": 3,
        "id": "agent-与运行时当前主战场",
        "title": "Agent 与运行时（当前主战场）"
      },
      {
        "depth": 3,
        "id": "协议与-api",
        "title": "协议与 API"
      },
      {
        "depth": 3,
        "id": "模型与配置",
        "title": "模型与配置"
      },
      {
        "depth": 3,
        "id": "安全与权限",
        "title": "安全与权限"
      },
      {
        "depth": 3,
        "id": "界面与体验",
        "title": "界面与体验"
      },
      {
        "depth": 3,
        "id": "zeterm-产品",
        "title": "zeterm 产品"
      },
      {
        "depth": 3,
        "id": "平台与产品",
        "title": "平台与产品"
      },
      {
        "depth": 3,
        "id": "计划与迁移会过期",
        "title": "计划与迁移（会过期）"
      },
      {
        "depth": 2,
        "id": "4-维护规则",
        "title": "4. 维护规则"
      }
    ],
    "searchText": " 本文件是跨产品 docs/ 的索引。新增跨产品文档必须同时加入本索引和 docs-site/scripts/generate-docs.mjs 的分组。 zeterm 专属系统文档由 zeterm/docs/README.md 索引，避免产品宿主 文档和共享系统文档混在同一目录。 系统性的阅读方法（先系统后 crate、两层文档职责）见 documentation-guidelines.md。 快速理解 这份索引只负责跨产品、跨 crate 的系统文档；zeterm 的产品宿主、渲染、终端和输入文档集中在 zeterm/docs/README.md。按下面的边界选择入口： 目标 入口 --- --- 理解共享 backend、协议、执行和存储 本目录对应的系统文档与 zeta-rs//README.md 理解 zeterm 产品行为 zeterm/docs/README.md 理解某个 crate 的实现契约 对应 crate 的 README.md 查看 docs-site 如何收录文档 docs-site/scripts/generate-docs.mjs 1. 我该看哪份？ 按你要做的事查表，按列出顺序读： 你要做什么 看这些（按序） --- --- 新人理解 Zeta 全貌 architecture.md → 感兴趣系统的领域文档 搭 / 改 agent harness（提示词、工具、循环） agent-harness-implementation-plan.md（照着做）→ agent-tools-spec.md（照着抄）→ agent-harness-design.md（查原因） 改执行内核（Turn/Thread/恢复/取消） zeta-agent-runtime-architecture.md → core.md 做上下文预算 / 压缩 core-context.md + agent-harness-design.md §9–§11 做多 Agent core-multi-agent.md（gate 条件见其状态头） 开发代码知识、Workspace Symbol 或跨语言导航 code-intelligence.md → syntax-analysis.md → lsp.md → code-index.md 开发工作区代码索引 / RAG code-intelligence.md → code-index.md → zeta-code-index README → zeta-code-index-semantic README → zeta-code-retrieval README → zeta-code-index-cloud README 设计 Instructions / Skills / Agents 或外部导入 agent-customizations.md → 对应 authority 文档 加 / 改一个工具 agent-tools-spec.md → tools.md（契约层） 改协议 / 加 App Server 方法 protocol.md → zeta-app-server-api.md → app-server-client.md 权限 / 审批 / 沙箱 permissions.md → auto-review.md → sandboxing.md 接 / 改模型供应商 model-provider.md → model-provider-config.md → models-manager.md 改 Desktop UI zeta-desktop-architecture.md → ui-styling-ownership.md 改 zeterm 产品行为 zeterm/docs/README.md → 对应产品文档 → zeterm/README.md 写 / 改文档本身 documentation-guidelines.md 2. 文档类型 每份文档属于四类之一，读法不同： 类型 含义 过期方式 --- --- --- 设计 回答\"为什么这样设计、边界在哪\"；组件挂状态标记（已实现/部分/仅设计/推迟） 长期维护，重审时修订 规格 逐字照抄的实现规格（schema、文案、字段） 随实现同步，改规格必须跑对应评测 计划 阶段性工作计划 完成即过期——做完后并入设计文档的状态标记，本体标记 Done 或删除 参考 运行手册、模板、外部快照 按需更新 3. 分类清单 Agent 与运行时（当前主战场） 文档 类型 一句话 --- --- --- agent-harness-implementation-plan.md 计划 coding 最小闭环（WI1–WI6）的文件级步骤 agent-tools-spec.md 规格 逐工具 schema / 描述正文 / 错误文案 + 系统提示词扩写 agent-harness-design.md 设计 harness 行为策略：提示词、循环、失败、裁剪、压缩、缓存 zeta-agent-runtime-architecture.md 设计 执行内核总体设计、组件状态总账、阶段 A–E core.md 设计 zeta-core 的 ownership、组件、端口、提交顺序 core-context.md 设计 ContextPlan / Manager / checkpoint / compaction 机制 core-multi-agent.md 设计 delegation / Fresh spawn / delivery / 隔离（部分实现，缺口见状态头） agent-customizations.md 设计 Instructions / Skills / Agents、.zeta 与外部导入边界 tools.md 设计 工具三层契约、registry snapshot exec.md 设计 进程执行 plugins.md / connectors.md / skills.md 设计 Plugin 扩展分发、Connector 外部账号连接与 Skill 指令运行时边界 editor-extensions.md 设计 声明式扩展与 Zeta 原生可执行 Host v1 的双轨边界、信任、生命周期和产品接入状态 mcp.md / mcp-server.md 设计 MCP 协议会话与能力调用、Connector ready binding，以及 Zeta 作为 MCP server slash-commands.md 设计 Slash Command 与统一斜杠启动面板边界 协议与 API protocol.md（canonical 产品契约）、 zeta-app-server-api.md、app-server-client.md、 zeta-api.md、zeta-api-interface-requirements.md、 zeta-api-interface-template.md（模板）、 zeta-client.md、codex-app-server.md（参考） 模型与配置 model-provider.md、model-provider-config.md、 models-manager.md、config.md、login.md、 secrets.md 安全与权限 permissions.md、auto-review.md、 sandboxing.md、workspace-security.md、 windows-sandbox-acceptance-runbook.md（参考/手册） 界面与体验 zeta-desktop-architecture.md、 ui-styling-ownership.md、 editor-architecture.md、editor-core.md、 workbench-pane-composite-design.md、 design-tokens.md、theme-authoring-template.md（模板）、 menu-system.md、icons.md、search.md、 code-intelligence.md、 code-index.md、 syntax-analysis.md、lsp.md、 editor-extensions.md、 tui.md、 chat-session-inspector.md、pdf.md、typst.md zeterm 产品 zeterm/docs/README.md 是产品文档入口，包含 Agent Console、Terminal Surface、Native Text Input、Rendering、UI 迁移、Native 弃用、应用迁移和发布图。 平台与产品 architecture.md（总入口）、zeta-rs-architecture.md、 zeta-cli-architecture.md、product-editions.md、 product-lines.md、git.md、 documentation-guidelines.md 计划与迁移（会过期） 文档 状态 --- --- zeta-code-architecture-codex-style-v2.md 长期演进方向 zeterm/docs/native-deprecation-plan.md Native 弃用迁移 zeterm/docs/ui-component-migration-plan.md UI 组件迁移 zeterm/docs/zeterm-app-migration-plan.md Zeterm 迁移 zeterm/docs/zeterm-release-graph.md Zeterm 发布依赖 4. 维护规则 1. 新文档：入本索引 + docs-site 分组，声明类型（设计/规格/计划/参考）； 2. 计划类文档完成后：结论并入对应设计文档的状态标记，计划本体标 Done 或删除—— 不允许长期留着已完成的计划冒充现状； 3. 设计文档描述未实现组件必须挂状态标记（已实现/部分/仅设计/推迟），词表定义见 zeta-agent-runtime-architecture.md； 4. 同一主题冲突时的权威关系在两份文档开头互相声明（见 documentation-guidelines.md）。"
  },
  {
    "slug": "architecture",
    "sourcePath": "docs/architecture.md",
    "title": "Zeta 系统地图",
    "description": "Zeta 的架构应从用户请求如何进入系统、经过哪些决定、最终如何执行和保存来理解，而不是从 crate 名称开始倒推。本文是面向开发者的总入口：先建立产品的整体心智模型，再把每个系统映射 到权威文档和实现。",
    "group": "开始了解",
    "markdown": "Zeta 的架构应从用户请求如何进入系统、经过哪些决定、最终如何执行和保存来理解，而不是从\ncrate 名称开始倒推。本文是面向开发者的总入口：先建立产品的整体心智模型，再把每个系统映射\n到权威文档和实现。\n\n> 本文描述用于审计架构的系统边界，不预设现有边界已经正确。当前实现、计划演进和潜在方向\n> 必须在各系统文档中分别说明；crate README 只负责提供实现证据，不能反过来定义产品语义。\n\n## 快速理解\n\n| 你正在解决的问题 | 首先从哪里看 | 接下来验证什么 |\n| --- | --- | --- |\n| 用户为什么会看到某种行为 | 对应的系统文档 | 行为表、规则和例外 |\n| 两个组件为什么都在做相似决定 | 系统边界与端到端流程 | 谁决定、谁执行、谁保存 |\n| 一次请求为什么在某个阶段失败 | 请求执行流程 | 输入、状态、失败语义和恢复点 |\n| 修改一个 crate 会影响哪些地方 | crate README | 它所属的系统、调用方和权威契约 |\n| 一个计划功能是否已经可用 | 系统文档的当前状态 | 代码、测试和对外接口证据 |\n\n阅读顺序应当始终是“用户问题 → 系统行为 → 责任边界 → 执行流程 → 实现符号”。如果必须先理解\n大量 crate、类型和函数名才能知道系统在做什么，说明文档的信息顺序需要调整。\n\n对于 `Session`、`Thread`、`Turn`、`ThreadItem` 产品能力，App Server 是唯一的外部入口和出口。\nDesktop、CLI、TUI 以及其他客户端只能通过版本化 App Server 契约读写产品状态和订阅更新；Core、\nStore、Provider 与私有运行时接口不对客户端开放。进程内嵌只改变传输成本，不改变这条门禁规则。\n\n## 2. 一次请求如何穿过 Zeta\n\n下面是用于理解和审计的产品级流程，不是某个进程内部的函数调用图：\n\n```mermaid\nflowchart TD\n    user[\"用户通过 Desktop、CLI、TUI 或其他客户端提出请求\"] --> gate[\"App Server<br/>唯一外部门禁\"]\n    gate --> session[\"会话系统<br/>识别 Session、Thread 与 Turn\"]\n    session --> context[\"上下文系统<br/>选择、组织和压缩模型输入\"]\n    context --> model[\"模型系统<br/>解析供应商、模型与调用配置\"]\n    model --> agent[\"Agent 运行时<br/>消费模型输出并推进 Turn\"]\n    agent --> tools[\"工具系统<br/>发现、绑定和调度工具\"]\n    tools --> permission{\"权限系统<br/>这次动作能否执行？\"}\n    permission -- \"无需批准或已有授权\" --> boundary[\"执行边界<br/>按批准能力选择执行方式\"]\n    permission -- \"需要用户确认\" --> approval[\"用户批准或拒绝\"]\n    approval -- \"批准\" --> boundary\n    approval -- \"拒绝\" --> stopped[\"阻止动作并记录结果\"]\n    permission -- \"策略阻止\" --> stopped\n    boundary --> isolation{\"需要受限沙箱？\"}\n    isolation -- \"是\" --> sandbox[\"沙箱系统<br/>限制文件、网络与进程能力\"]\n    isolation -- \"否，完全访问\" --> executor[\"平台执行器\"]\n    sandbox --> executor\n    executor --> result[\"工具结果\"]\n    result --> session\n\n    config[\"配置系统<br/>产生各领域的有效快照\"] -.-> context\n    config -.-> model\n    config -.-> tools\n    config -.-> permission\n    extension[\"扩展系统<br/>Plugin、Skill 与 MCP\"] -.-> context\n    extension -.-> tools\n```\n\n这张图表达三个不同层次：\n\n- **产品流程**：用户请求从哪里进入，最终在哪里形成结果；\n- **决定链**：模型选择、工具选择和动作授权分别由哪个系统决定；\n- **执行链**：授权结果如何变成可被操作系统强制执行的能力边界。\n\n其中任意一个箭头如果无法说明输入、输出、失败语义和责任归属，就是需要优先审计的架构接缝。\n\n## 3. 系统边界\n\n### 3.1 对话、上下文与运行时\n\n| 系统 | 回答的核心问题 | 应当拥有 | 重点审计边界 | 权威文档 |\n| --- | --- | --- | --- | --- |\n| 会话系统 | 一次工作如何被识别、恢复和持续保存？ | Session、Thread、Turn、事件顺序与持久化事务 | Session、Thread、Store 与 rollout 是否存在重复权威 | [`core.md`](core.md)、[`protocol.md`](protocol.md) |\n| 上下文系统 | 当前模型究竟能看到什么？ | 上下文选择、预算、压缩、恢复和每个 Thread 的上下文状态 | 持久事实、模型输入和 UI 展示状态是否混为一体 | [`core-context.md`](core-context.md) |\n| Agent 运行时 | 模型输出如何推进一次 Turn？ | Agent 生命周期、模型回合、工具回合、取消与多 Agent 协调 | 调度、上下文、工具和持久化是否有唯一协调者 | [`agent-harness-design.md`](agent-harness-design.md)、[`core-multi-agent.md`](core-multi-agent.md)、[`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) |\n| Agent 自定义系统 | Agent 长期遵循什么、如何复用工作方法、使用哪种执行配置？ | Instructions、Skills、Agents、`.zeta` 原生命名空间与外部导入边界 | Prompt/Task/Slash Command 是否被误建模为 artifact，外部格式是否污染原生 authority | [`agent-customizations.md`](agent-customizations.md) |\n\n### 3.2 能力、决策与执行\n\n| 系统 | 回答的核心问题 | 应当拥有 | 重点审计边界 | 权威文档 |\n| --- | --- | --- | --- | --- |\n| 模型系统 | 最终使用哪个供应商、模型和调用配置？ | 模型目录、能力、配置解析和运行时选择 | 目录、配置、凭据、供应商适配、传输和重试是否分层 | [`models-manager.md`](models-manager.md)、[`model-provider.md`](model-provider.md)、[`model-provider-config.md`](model-provider-config.md) |\n| 工具系统 | Agent 能看到和调用哪些能力？ | 工具定义、发现、绑定、参数验证、调用和结果契约 | 工具定义、调度、授权、执行与结果持久化是否互相越界 | [`tools.md`](tools.md) |\n| 权限系统 | 某个具体动作能否执行？ | 授权规则、批准范围、批准有效期与最终授权决定 | 权限、Auto Review、工具调度和沙箱是否都在做最终决定 | [`permissions.md`](permissions.md)、[`auto-review.md`](auto-review.md) |\n| 沙箱系统 | 已获准动作实际能触及什么？ | 文件、网络、进程能力和平台强制执行 | 策略选择、用户批准与操作系统强制执行是否分开 | [`sandboxing.md`](sandboxing.md) |\n| 配置系统 | 当前作用域下哪个值最终生效？ | 配置来源、优先级、作用域、合并和不可变领域快照 | 通用合并与各领域验证是否有清楚交接 | [`config.md`](config.md) |\n| 身份与秘密系统 | 用户如何登录，敏感凭据保存在哪里？ | 登录流程、账户状态和秘密的安全存取 | 身份、账户展示、供应商凭据和网络调用是否解耦 | [`login.md`](login.md)、[`secrets.md`](secrets.md)、[`codex-app-server.md`](codex-app-server.md) |\n\n### 3.3 扩展、接口与产品入口\n\n| 系统 | 回答的核心问题 | 应当拥有 | 重点审计边界 | 权威文档 |\n| --- | --- | --- | --- | --- |\n| 扩展系统 | 外部能力如何被发现、激活和撤销？ | Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Skill 管指令加载 | 声明控制面、连接状态面、协议运行时和 Agent 消费是否分层 | [`plugins.md`](plugins.md)、[`connectors.md`](connectors.md)、[`skills.md`](skills.md)、[`mcp.md`](mcp.md) |\n| App Server 与协议 | 产品入口如何调用同一套权威能力？ | 唯一外部进入/输出边界、对外方法、DTO、事件、订阅、版本和客户端契约 | 客户端是否绕过门禁，或协议层是否偷偷拥有产品决定或持久化规则 | [`zeta-app-server-api.md`](zeta-app-server-api.md)、[`app-server-client.md`](app-server-client.md)、[`protocol.md`](protocol.md) |\n| 产品界面 | 用户如何观察和控制这些系统？ | Desktop、CLI、TUI 的交互、呈现和平台适配 | 界面是否复制 Core 状态或在本地发明业务规则 | [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md)、[`zeta-cli-architecture.md`](zeta-cli-architecture.md)、[`tui.md`](tui.md) |\n\n系统名称不是按照 crate 数量划分的。一个系统可以由多个 crate 实现，一个 crate 也可能只是某个\n系统的适配器。真正的边界由权威状态、最终决定、执行责任和失败语义决定。\n\n## 4. 怎样判断边界是否清楚\n\n审计每个系统时，必须能够连续回答下面的问题：\n\n| 审计问题 | 清楚边界应当给出的答案 |\n| --- | --- |\n| 它为谁解决什么问题？ | 一个用户或调用方能够识别的问题 |\n| 它接收什么？ | 明确输入、信任级别和前置条件 |\n| 谁拥有权威状态？ | 唯一来源、作用范围和生命周期 |\n| 谁作最终决定？ | 一个可以命名的 owner，而不是“多个组件共同决定” |\n| 谁执行或强制落实？ | 与作决定者区分开的执行责任 |\n| 谁保存结果？ | 持久化位置、恢复方式和失效条件 |\n| 失败意味着什么？ | 失败分类、是否重试、是否回滚和用户能看到什么 |\n| 它明确不负责什么？ | 相邻系统拥有的责任和禁止依赖 |\n\n如果“作决定”“执行”和“保存”三个答案落在多个位置，且没有明确的协调关系，通常不是文档写得\n不够详细，而是架构本身存在重复权威或责任泄漏。\n\n## 5. 优先审计的三条流程\n\n### 5.1 用户请求到工具结果\n\n```text\n请求 → Session/Thread → 上下文 → 模型 → Agent → 工具 → 权限 → 执行边界 → 结果 → 持久化\n```\n\n这条流程优先检查：\n\n- Turn 的唯一协调者在哪里；\n- 模型输出什么时候成为工具调用；\n- 权限决定和风险建议是否分开；\n- 沙箱是否只执行批准后的能力；\n- 工具结果由谁排序、关联并持久化。\n\n### 5.2 配置变化到运行时生效\n\n```text\n配置来源 → 作用域解析 → 合并 → 领域验证 → 不可变快照 → 运行时消费者\n```\n\n这条流程优先检查：\n\n- 通用配置层是否知道了过多领域规则；\n- 模型、工具、权限和扩展是否各自重新解析配置；\n- 配置变化在什么安全点生效；\n- 失败的配置是否会污染当前可用快照。\n\n### 5.3 扩展安装到能力可用\n\n```text\n发现 → 安装 → 信任判断 → 激活 → 能力贡献 → Agent 可见 → 调用 → 撤销\n```\n\n这条流程优先检查：\n\n- Plugin、Skill 和 MCP 是否共享清楚的身份与来源模型；\n- 安装权限和运行时工具权限是否被混为一谈；\n- 能力在配置变化或扩展移除后如何失效；\n- 外部能力是否能绕过工具、权限或上下文边界。\n\n## 6. 当前实现基础\n\n当前已经具备用于继续审计和演进的基础，但“存在实现”不等于“边界已经验证清楚”。Agent 执行\n面的逐组件状态总账（已实现 / 部分 / 仅设计 / 推迟）与分阶段实施计划由\n[`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md#2-组件状态总账)\n权威维护：\n\n- **对话与持久化**：Session/Thread 归约器、Store、逻辑序列、写入租约、恢复事务和 rollout；\n- **协议与接口**：Rust 权威类型、JSON Schema、TypeScript、模式哈希、App Server 分发与订阅；\n- **工具与 MCP**：外部工具客户端、MCP Server、批准和持久化结果的纵向切片；\n- **产品入口**：CLI/TUI 进程内客户端、Desktop JSONL 客户端和 Electron 可信 IPC；\n- **产品资源**：统一图标来源、Desktop 注册表和 Rust 类型化目录；\n- **领域基础**：配置、供应商注册表、秘密存储、资源发现和跨平台沙箱。\n\n计划文档不能覆盖当前 API 或已经实现的领域边界。当前行为与计划发生冲突时，应先修正权威契约\n和调用方，再更新文档；不能仅通过修改说明文字掩盖实现分歧。\n\n## 7. 从系统进入实现\n\n需要理解产品行为时，从本文和对应系统文档开始；需要修改代码时，再进入 crate README：\n\n1. 在系统文档中确认用户行为、owner、状态和执行流程；\n2. 沿文档链接找到承载责任的 crate；\n3. 在 crate README 中确认公共接口、关键私有符号、失败语义和测试；\n4. 用真实调用关系检查实现是否仍符合系统边界；\n5. 如果实现无法映射回唯一系统，先记录并解决架构问题，不为现状补一个模糊名称。\n\n完整的文档分层、语言和图表规则见\n[`documentation-guidelines.md`](documentation-guidelines.md)。长期目标与 staged evolution 见\n[`zeta-code-architecture-codex-style-v2.md`](zeta-code-architecture-codex-style-v2.md)，\nRust 产品内核和对外层的总体边界见\n[`zeta-rs-architecture.md`](zeta-rs-architecture.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-一次请求如何穿过-zeta",
        "title": "2. 一次请求如何穿过 Zeta"
      },
      {
        "depth": 2,
        "id": "3-系统边界",
        "title": "3. 系统边界"
      },
      {
        "depth": 3,
        "id": "31-对话上下文与运行时",
        "title": "3.1 对话、上下文与运行时"
      },
      {
        "depth": 3,
        "id": "32-能力决策与执行",
        "title": "3.2 能力、决策与执行"
      },
      {
        "depth": 3,
        "id": "33-扩展接口与产品入口",
        "title": "3.3 扩展、接口与产品入口"
      },
      {
        "depth": 2,
        "id": "4-怎样判断边界是否清楚",
        "title": "4. 怎样判断边界是否清楚"
      },
      {
        "depth": 2,
        "id": "5-优先审计的三条流程",
        "title": "5. 优先审计的三条流程"
      },
      {
        "depth": 3,
        "id": "51-用户请求到工具结果",
        "title": "5.1 用户请求到工具结果"
      },
      {
        "depth": 3,
        "id": "52-配置变化到运行时生效",
        "title": "5.2 配置变化到运行时生效"
      },
      {
        "depth": 3,
        "id": "53-扩展安装到能力可用",
        "title": "5.3 扩展安装到能力可用"
      },
      {
        "depth": 2,
        "id": "6-当前实现基础",
        "title": "6. 当前实现基础"
      },
      {
        "depth": 2,
        "id": "7-从系统进入实现",
        "title": "7. 从系统进入实现"
      }
    ],
    "searchText": "Zeta 的架构应从用户请求如何进入系统、经过哪些决定、最终如何执行和保存来理解，而不是从 crate 名称开始倒推。本文是面向开发者的总入口：先建立产品的整体心智模型，再把每个系统映射 到权威文档和实现。 本文描述用于审计架构的系统边界，不预设现有边界已经正确。当前实现、计划演进和潜在方向 必须在各系统文档中分别说明；crate README 只负责提供实现证据，不能反过来定义产品语义。 快速理解 你正在解决的问题 首先从哪里看 接下来验证什么 --- --- --- 用户为什么会看到某种行为 对应的系统文档 行为表、规则和例外 两个组件为什么都在做相似决定 系统边界与端到端流程 谁决定、谁执行、谁保存 一次请求为什么在某个阶段失败 请求执行流程 输入、状态、失败语义和恢复点 修改一个 crate 会影响哪些地方 crate README 它所属的系统、调用方和权威契约 一个计划功能是否已经可用 系统文档的当前状态 代码、测试和对外接口证据 阅读顺序应当始终是“用户问题 → 系统行为 → 责任边界 → 执行流程 → 实现符号”。如果必须先理解 大量 crate、类型和函数名才能知道系统在做什么，说明文档的信息顺序需要调整。 对于 Session、Thread、Turn、ThreadItem 产品能力，App Server 是唯一的外部入口和出口。 Desktop、CLI、TUI 以及其他客户端只能通过版本化 App Server 契约读写产品状态和订阅更新；Core、 Store、Provider 与私有运行时接口不对客户端开放。进程内嵌只改变传输成本，不改变这条门禁规则。 2. 一次请求如何穿过 Zeta 下面是用于理解和审计的产品级流程，不是某个进程内部的函数调用图： ``mermaid flowchart TD user \"用户通过 Desktop、CLI、TUI 或其他客户端提出请求\" -- gate \"App Server<br/ 唯一外部门禁\" gate -- session \"会话系统<br/ 识别 Session、Thread 与 Turn\" session -- context \"上下文系统<br/ 选择、组织和压缩模型输入\" context -- model \"模型系统<br/ 解析供应商、模型与调用配置\" model -- agent \"Agent 运行时<br/ 消费模型输出并推进 Turn\" agent -- tools \"工具系统<br/ 发现、绑定和调度工具\" tools -- permission \"权限系统<br/ 这次动作能否执行？\" permission -- \"无需批准或已有授权\" -- boundary \"执行边界<br/ 按批准能力选择执行方式\" permission -- \"需要用户确认\" -- approval \"用户批准或拒绝\" approval -- \"批准\" -- boundary approval -- \"拒绝\" -- stopped \"阻止动作并记录结果\" permission -- \"策略阻止\" -- stopped boundary -- isolation \"需要受限沙箱？\" isolation -- \"是\" -- sandbox \"沙箱系统<br/ 限制文件、网络与进程能力\" isolation -- \"否，完全访问\" -- executor \"平台执行器\" sandbox -- executor executor -- result \"工具结果\" result -- session config \"配置系统<br/ 产生各领域的有效快照\" -.- context config -.- model config -.- tools config -.- permission extension \"扩展系统<br/ Plugin、Skill 与 MCP\" -.- context extension -.- tools ` 这张图表达三个不同层次： - 产品流程：用户请求从哪里进入，最终在哪里形成结果； - 决定链：模型选择、工具选择和动作授权分别由哪个系统决定； - 执行链：授权结果如何变成可被操作系统强制执行的能力边界。 其中任意一个箭头如果无法说明输入、输出、失败语义和责任归属，就是需要优先审计的架构接缝。 3. 系统边界 3.1 对话、上下文与运行时 系统 回答的核心问题 应当拥有 重点审计边界 权威文档 --- --- --- --- --- 会话系统 一次工作如何被识别、恢复和持续保存？ Session、Thread、Turn、事件顺序与持久化事务 Session、Thread、Store 与 rollout 是否存在重复权威 core.md、protocol.md 上下文系统 当前模型究竟能看到什么？ 上下文选择、预算、压缩、恢复和每个 Thread 的上下文状态 持久事实、模型输入和 UI 展示状态是否混为一体 core-context.md Agent 运行时 模型输出如何推进一次 Turn？ Agent 生命周期、模型回合、工具回合、取消与多 Agent 协调 调度、上下文、工具和持久化是否有唯一协调者 agent-harness-design.md、core-multi-agent.md、zeta-agent-runtime-architecture.md Agent 自定义系统 Agent 长期遵循什么、如何复用工作方法、使用哪种执行配置？ Instructions、Skills、Agents、.zeta 原生命名空间与外部导入边界 Prompt/Task/Slash Command 是否被误建模为 artifact，外部格式是否污染原生 authority agent-customizations.md 3.2 能力、决策与执行 系统 回答的核心问题 应当拥有 重点审计边界 权威文档 --- --- --- --- --- 模型系统 最终使用哪个供应商、模型和调用配置？ 模型目录、能力、配置解析和运行时选择 目录、配置、凭据、供应商适配、传输和重试是否分层 models-manager.md、model-provider.md、model-provider-config.md 工具系统 Agent 能看到和调用哪些能力？ 工具定义、发现、绑定、参数验证、调用和结果契约 工具定义、调度、授权、执行与结果持久化是否互相越界 tools.md 权限系统 某个具体动作能否执行？ 授权规则、批准范围、批准有效期与最终授权决定 权限、Auto Review、工具调度和沙箱是否都在做最终决定 permissions.md、auto-review.md 沙箱系统 已获准动作实际能触及什么？ 文件、网络、进程能力和平台强制执行 策略选择、用户批准与操作系统强制执行是否分开 sandboxing.md 配置系统 当前作用域下哪个值最终生效？ 配置来源、优先级、作用域、合并和不可变领域快照 通用合并与各领域验证是否有清楚交接 config.md 身份与秘密系统 用户如何登录，敏感凭据保存在哪里？ 登录流程、账户状态和秘密的安全存取 身份、账户展示、供应商凭据和网络调用是否解耦 login.md、secrets.md、codex-app-server.md 3.3 扩展、接口与产品入口 系统 回答的核心问题 应当拥有 重点审计边界 权威文档 --- --- --- --- --- 扩展系统 外部能力如何被发现、激活和撤销？ Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Skill 管指令加载 声明控制面、连接状态面、协议运行时和 Agent 消费是否分层 plugins.md、connectors.md、skills.md、mcp.md App Server 与协议 产品入口如何调用同一套权威能力？ 唯一外部进入/输出边界、对外方法、DTO、事件、订阅、版本和客户端契约 客户端是否绕过门禁，或协议层是否偷偷拥有产品决定或持久化规则 zeta-app-server-api.md、app-server-client.md、protocol.md 产品界面 用户如何观察和控制这些系统？ Desktop、CLI、TUI 的交互、呈现和平台适配 界面是否复制 Core 状态或在本地发明业务规则 zeta-desktop-architecture.md、zeta-cli-architecture.md、tui.md 系统名称不是按照 crate 数量划分的。一个系统可以由多个 crate 实现，一个 crate 也可能只是某个 系统的适配器。真正的边界由权威状态、最终决定、执行责任和失败语义决定。 4. 怎样判断边界是否清楚 审计每个系统时，必须能够连续回答下面的问题： 审计问题 清楚边界应当给出的答案 --- --- 它为谁解决什么问题？ 一个用户或调用方能够识别的问题 它接收什么？ 明确输入、信任级别和前置条件 谁拥有权威状态？ 唯一来源、作用范围和生命周期 谁作最终决定？ 一个可以命名的 owner，而不是“多个组件共同决定” 谁执行或强制落实？ 与作决定者区分开的执行责任 谁保存结果？ 持久化位置、恢复方式和失效条件 失败意味着什么？ 失败分类、是否重试、是否回滚和用户能看到什么 它明确不负责什么？ 相邻系统拥有的责任和禁止依赖 如果“作决定”“执行”和“保存”三个答案落在多个位置，且没有明确的协调关系，通常不是文档写得 不够详细，而是架构本身存在重复权威或责任泄漏。 5. 优先审计的三条流程 5.1 用户请求到工具结果 `text 请求 → Session/Thread → 上下文 → 模型 → Agent → 工具 → 权限 → 执行边界 → 结果 → 持久化 ` 这条流程优先检查： - Turn 的唯一协调者在哪里； - 模型输出什么时候成为工具调用； - 权限决定和风险建议是否分开； - 沙箱是否只执行批准后的能力； - 工具结果由谁排序、关联并持久化。 5.2 配置变化到运行时生效 `text 配置来源 → 作用域解析 → 合并 → 领域验证 → 不可变快照 → 运行时消费者 ` 这条流程优先检查： - 通用配置层是否知道了过多领域规则； - 模型、工具、权限和扩展是否各自重新解析配置； - 配置变化在什么安全点生效； - 失败的配置是否会污染当前可用快照。 5.3 扩展安装到能力可用 `text 发现 → 安装 → 信任判断 → 激活 → 能力贡献 → Agent 可见 → 调用 → 撤销 ` 这条流程优先检查： - Plugin、Skill 和 MCP 是否共享清楚的身份与来源模型； - 安装权限和运行时工具权限是否被混为一谈； - 能力在配置变化或扩展移除后如何失效； - 外部能力是否能绕过工具、权限或上下文边界。 6. 当前实现基础 当前已经具备用于继续审计和演进的基础，但“存在实现”不等于“边界已经验证清楚”。Agent 执行 面的逐组件状态总账（已实现 / 部分 / 仅设计 / 推迟）与分阶段实施计划由 zeta-agent-runtime-architecture.md 权威维护： - 对话与持久化：Session/Thread 归约器、Store、逻辑序列、写入租约、恢复事务和 rollout； - 协议与接口：Rust 权威类型、JSON Schema、TypeScript、模式哈希、App Server 分发与订阅； - 工具与 MCP：外部工具客户端、MCP Server、批准和持久化结果的纵向切片； - 产品入口：CLI/TUI 进程内客户端、Desktop JSONL 客户端和 Electron 可信 IPC； - 产品资源：统一图标来源、Desktop 注册表和 Rust 类型化目录； - 领域基础：配置、供应商注册表、秘密存储、资源发现和跨平台沙箱。 计划文档不能覆盖当前 API 或已经实现的领域边界。当前行为与计划发生冲突时，应先修正权威契约 和调用方，再更新文档；不能仅通过修改说明文字掩盖实现分歧。 7. 从系统进入实现 需要理解产品行为时，从本文和对应系统文档开始；需要修改代码时，再进入 crate README： 1. 在系统文档中确认用户行为、owner、状态和执行流程； 2. 沿文档链接找到承载责任的 crate； 3. 在 crate README 中确认公共接口、关键私有符号、失败语义和测试； 4. 用真实调用关系检查实现是否仍符合系统边界； 5. 如果实现无法映射回唯一系统，先记录并解决架构问题，不为现状补一个模糊名称。 完整的文档分层、语言和图表规则见 documentation-guidelines.md。长期目标与 staged evolution 见 zeta-code-architecture-codex-style-v2.md， Rust 产品内核和对外层的总体边界见 zeta-rs-architecture.md`。"
  },
  {
    "slug": "product-editions",
    "sourcePath": "docs/product-editions.md",
    "title": "zeta Electron Desktop 产品版本、Sessions 与构建边界",
    "description": "两个产品位于同一仓库、共享 Workbench 基础设施和 Rust App Server，但各自拥有 applicationId、userDataFolderName、renderer storage namespace 与 App Server profile root。Electron Main 在持久化服务启动前设置这些路径，因此两者能够同机安装和同时运行。",
    "group": "开始了解",
    "markdown": "> 本文是 Electron Desktop 内部 `code` 与 `academic` 构建变体的 canonical 说明。\n> 它们不等同于公开产品线；公开宿主边界见 [`product-lines.md`](product-lines.md)。\n\n## 当前构建变体\n\n| Electron 构建变体 | `ZETA_PRODUCT` | Workbench 入口 | Dedicated Sessions | Renderer 输出 |\n| --- | --- | --- | --- | --- |\n| Zeta Code | `code` | `workbench-code` | `sessions-code` | `desktop/dist/renderer/code` |\n| Zeta Academic | `academic` | `workbench-academic` | ❌ 未提供 | `desktop/dist/renderer/academic` |\n\n两个产品位于同一仓库、共享 Workbench 基础设施和 Rust App Server，但各自拥有\n`applicationId`、`userDataFolderName`、renderer storage namespace 与 App Server profile\nroot。Electron Main 在持久化服务启动前设置这些路径，因此两者能够同机安装和同时运行。\n同一 workspace 仍是共享文件场景，需要文件级协调；它不是产品状态共享。\n\n## Dedicated Sessions\n\nCode Sessions 是独立页面，不是给 `workbench/browser/layout*` 增加产品分支。Code 的普通\nWorkbench 入口只注册一个 Titlebar action，Browser 页面导航到 sibling Sessions HTML；\nElectron Main 创建独立 Sessions 窗口，并把对应 HTML 加入 trusted IPC allowlist。\n\n```text\nregular Workbench titlebar\n        │ Open <Product> Sessions\n        ▼\nsessions/<product> page\n        │ Return to Workbench\n        ▼\nregular Workbench page\n```\n\n`sessions/` 可以依赖 `workbench/` 的可复用 Chat、Markdown 和 renderer capability；\n`workbench/` 不得反向导入 `sessions/`。这是避免专用工作台污染默认 Workbench 布局的硬边界。\n\n| Sessions workbench | 固定布局 | 当前能力 |\n| --- | --- | --- |\n| Code | Titlebar / Sessions Sidebar / 多 Session Grid / Auxiliary Bar | 持久 Session/Thread、完整 ChatPane、草稿延迟持久化、活动叶节点与 Back/Forward、返回普通 Workbench |\n| Academic | ❌ 未实现 | Academic 当前只提供普通 Workbench；不得把未来研究工作台描述为现有能力 |\n\nCode Sessions 的 Renderer 实现、状态 owner、执行路径、失败语义和扩展点见\n[`desktop/src/zeta/sessions/README.md`](../desktop/src/zeta/sessions/README.md)。Academic 若未来\n增加专用研究工作台，必须先新增明确的 product capability 与独立 renderer 入口；PDF、文献库、\nZotero 同步和引用索引等领域能力不得提前放进 Workbench layout 或 generic Session storage。\n\n## 编辑器与默认 Workbench\n\n普通 Workbench 仍使用已有的 immutable `WorkbenchSession` 初始 profile：Code 使用 Aster，\nAcademic 使用 Aster。它们只决定正常工作台启动时的默认区域和 editor bundle；Dedicated\nSessions 自己构造固定页面，不读取或更改 `WorkbenchLayout`。\n\n产品入口分别静态加载 `editor.code.all.ts` 与 `editor.academic.all.ts`。两个 bundle 都来自同一个扁平的 `src/zeta/editor` 模块；Aster 是统一内核品牌，`TextModel` 与 `DocumentModel` 分别拥有 Text Engine 和 Document Engine 的同步语义。\n\n## 构建与验证\n\n从仓库根目录执行：\n\n```bash\ncorepack pnpm --dir desktop build:code\ncorepack pnpm --dir desktop build:academic\n```\n\n每次构建必须产生该产品的 Browser 与 Electron Workbench HTML；只有声明\n`dedicatedSessions` 的 Code 还必须产生 Browser 与 Electron Sessions HTML。发布包只收录一个\n产品 renderer 目录；Main 会校验该产品声明的完整入口集合，避免安装身份依赖用户可控的环境变量。",
    "headings": [
      {
        "depth": 2,
        "id": "当前构建变体",
        "title": "当前构建变体"
      },
      {
        "depth": 2,
        "id": "dedicated-sessions",
        "title": "Dedicated Sessions"
      },
      {
        "depth": 2,
        "id": "编辑器与默认-workbench",
        "title": "编辑器与默认 Workbench"
      },
      {
        "depth": 2,
        "id": "构建与验证",
        "title": "构建与验证"
      }
    ],
    "searchText": " 本文是 Electron Desktop 内部 code 与 academic 构建变体的 canonical 说明。 它们不等同于公开产品线；公开宿主边界见 product-lines.md。 当前构建变体 Electron 构建变体 ZETAPRODUCT Workbench 入口 Dedicated Sessions Renderer 输出 --- --- --- --- --- Zeta Code code workbench-code sessions-code desktop/dist/renderer/code Zeta Academic academic workbench-academic ❌ 未提供 desktop/dist/renderer/academic 两个产品位于同一仓库、共享 Workbench 基础设施和 Rust App Server，但各自拥有 applicationId、userDataFolderName、renderer storage namespace 与 App Server profile root。Electron Main 在持久化服务启动前设置这些路径，因此两者能够同机安装和同时运行。 同一 workspace 仍是共享文件场景，需要文件级协调；它不是产品状态共享。 Dedicated Sessions Code Sessions 是独立页面，不是给 workbench/browser/layout 增加产品分支。Code 的普通 Workbench 入口只注册一个 Titlebar action，Browser 页面导航到 sibling Sessions HTML； Electron Main 创建独立 Sessions 窗口，并把对应 HTML 加入 trusted IPC allowlist。 ``text regular Workbench titlebar │ Open <Product Sessions ▼ sessions/<product page │ Return to Workbench ▼ regular Workbench page ` sessions/ 可以依赖 workbench/ 的可复用 Chat、Markdown 和 renderer capability； workbench/ 不得反向导入 sessions/。这是避免专用工作台污染默认 Workbench 布局的硬边界。 Sessions workbench 固定布局 当前能力 --- --- --- Code Titlebar / Sessions Sidebar / 多 Session Grid / Auxiliary Bar 持久 Session/Thread、完整 ChatPane、草稿延迟持久化、活动叶节点与 Back/Forward、返回普通 Workbench Academic ❌ 未实现 Academic 当前只提供普通 Workbench；不得把未来研究工作台描述为现有能力 Code Sessions 的 Renderer 实现、状态 owner、执行路径、失败语义和扩展点见 desktop/src/zeta/sessions/README.md。Academic 若未来 增加专用研究工作台，必须先新增明确的 product capability 与独立 renderer 入口；PDF、文献库、 Zotero 同步和引用索引等领域能力不得提前放进 Workbench layout 或 generic Session storage。 编辑器与默认 Workbench 普通 Workbench 仍使用已有的 immutable WorkbenchSession 初始 profile：Code 使用 Aster， Academic 使用 Aster。它们只决定正常工作台启动时的默认区域和 editor bundle；Dedicated Sessions 自己构造固定页面，不读取或更改 WorkbenchLayout。 产品入口分别静态加载 editor.code.all.ts 与 editor.academic.all.ts。两个 bundle 都来自同一个扁平的 src/zeta/editor 模块；Aster 是统一内核品牌，TextModel 与 DocumentModel 分别拥有 Text Engine 和 Document Engine 的同步语义。 构建与验证 从仓库根目录执行： `bash corepack pnpm --dir desktop build:code corepack pnpm --dir desktop build:academic ` 每次构建必须产生该产品的 Browser 与 Electron Workbench HTML；只有声明 dedicatedSessions` 的 Code 还必须产生 Browser 与 Electron Sessions HTML。发布包只收录一个 产品 renderer 目录；Main 会校验该产品声明的完整入口集合，避免安装身份依赖用户可控的环境变量。"
  },
  {
    "slug": "product-lines",
    "sourcePath": "docs/product-lines.md",
    "title": "Zeta 产品线与宿主边界",
    "description": "Zeta 不是一个 UI 宿主的三种包装，而是三条产品线共享 Rust 后端契约、各自拥有 UI 宿主。凡是 Session、Thread、Turn、ThreadItem Agent 产品能力，都必须经过 App Server；zeterm 当前直接组合的路径只属于终端/PTY 宿主，不是 Agent API 的例外。",
    "group": "开始了解",
    "markdown": "> 状态：Current product model。本文是三条公开产品线的 canonical 说明。\n> Electron Desktop 的构建变体与静态入口见 [`product-editions.md`](product-editions.md)；\n> 具体实现分别见 [`zeta-cli-architecture.md`](zeta-cli-architecture.md)、\n> [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md)、[`tui.md`](tui.md) 和\n> [`zeterm/docs/native-terminal-ui.md`](../zeterm/docs/native-terminal-ui.md)。\n\n## 快速理解\n\nZeta 不是一个 UI 宿主的三种包装，而是三条产品线共享 Rust 后端契约、各自拥有 UI 宿主。凡是\n`Session`、`Thread`、`Turn`、`ThreadItem` Agent 产品能力，都必须经过 App Server；`zeterm`\n当前直接组合的路径只属于终端/PTY 宿主，不是 Agent API 的例外。\n\n| 产品线 | 产品形态 | 当前 UI/宿主 | 前后端接线 | 终端实现边界 |\n| --- | --- | --- | --- | --- |\n| `zeta code` | TUI 产品 | `zeta-code/cli` + `zeta-code/tui` | `zeta-app-server-client` 连接 App Server | TUI 管理自己的 `crossterm`/`ratatui` 宿主终端；不直接拥有子 PTY |\n| `zeta` | Electron Desktop | Renderer + Preload + Electron Main | Electron Main 启动并桥接 Rust App Server | 当前 Renderer 用 xterm；Rust/App Server 管理 `zeta-utils-pty` |\n| `zeterm` | 纯 Rust Desktop | `zeterm/` 原生窗口与 UI | Rust 进程内直接组合 | `zeta-terminal` 负责终端语义，`zeta-utils-pty` 负责 PTY/进程 |\n\n产品线与 Electron 的内部构建变体不是同一个维度。当前 Desktop 保留 `code`、`academic`\n两个源码和构建标识；它们是 `zeta` Electron Desktop 的构建变体，不代表\n`zeta code` TUI，也不构成额外的公开产品线。具体说明见\n[`product-editions.md`](product-editions.md)。\n\n## 当前调用关系\n\n```mermaid\nflowchart LR\n    Code[\"zeta code\\nTUI\"] --> Client[\"zeta-app-server-client\"]\n    Desktop[\"zeta\\nElectron Renderer\"] --> Main[\"Electron Main\"]\n    Main --> Server[\"Rust App Server\"]\n    Client --> Server\n    Server --> Pty[\"zeta-utils-pty\"]\n    Term[\"zeterm\\nRust Desktop\"] --> Core[\"zeta-terminal\"]\n    Term --> Pty\n    Term -. \"Agent capability only\" .-> Server\n```\n\n`Electron Main` 只属于 `zeta` 产品线。它负责 Electron 生命周期、App Server 子进程监督、\n可信 IPC 和 Renderer adapter；它不是三条产品线共享的通用后端层。\n\n`zeta code` 的 TUI 宿主终端和“运行一个子 Shell 的终端能力”必须区分：前者属于 TUI 的\n`crossterm`/`ratatui` 事件循环，后者如果产品需要，应通过 App Server 的 typed contract 使用\n后端 PTY。TUI 不因为运行在终端里，就自动成为 PTY owner。\n\n## 终端分层\n\n| 层 | 当前 owner | 负责什么 | 不负责什么 |\n| --- | --- | --- | --- |\n| PTY/进程层 | `zeta-utils-pty` | spawn、读写、resize、signal、exit | ANSI/VT 解析、网格、scrollback 语义 |\n| 终端语义层 | `zeta-terminal` | ANSI/VT parser、cell/grid、cursor、mode、scrollback 等 | 创建 Shell 进程、Electron IPC、产品窗口 |\n| 产品后端层 | Rust App Server | 连接级 Terminal session、授权、生命周期和 protocol DTO | Renderer DOM 或 TUI 绘制 |\n| Electron 桥接层 | `zeta` 的 Electron Main | 进程监督、trusted IPC、Renderer adapter | 复制 Rust 终端状态机 |\n| TUI 宿主层 | `zeta code` 的 `zeta-tui` | raw mode、alternate screen、输入事件和 Ratatui frame | 第二套 Agent runtime 或 PTY authority |\n| Native 宿主层 | `zeterm` 的 `zeterm/` | 原生窗口、GPU/UI、终端输入输出组合 | Electron Main、Renderer bridge |\n\n因此，`zeta-terminal` 不能被 `zeta-utils-pty` 替代。两者在 `zeterm` 中已经是上下层组合；\n在 `zeta` 中是否由 App Server 进一步组合 `zeta-terminal`，取决于是否把终端语义状态从\nRenderer 的 xterm 投影迁移为 Rust authoritative state，这属于独立的终端演进，不影响三条\n产品线的宿主划分。\n\n## 代码入口对照\n\n| 公开产品线 | 当前代码入口 | 当前状态 |\n| --- | --- | --- |\n| `zeta code` | `zeta-code/cli` 的 `zeta` binary → `zeta-tui` | TUI 产品路径已存在；TUI 通过 App Server Client 工作 |\n| `zeta` | `desktop` Electron client | Electron Desktop 已存在；默认 Renderer 仍使用 `code` 这一内部构建 ID |\n| `zeterm` | `zeterm/` 的 `zeterm` binary | 终端宿主已存在，并直接组合 `zeta-terminal` 与 `zeta-utils-pty`；Agent 能力尚未作为 Native 旁路提供 |\n\n## Canonical `just` 命令\n\n| 命令 | 产品线 | 运行方式 |\n| --- | --- | --- |\n| `just zeta` | `zeta code` | 从 Rust workspace 启动 TUI，并接收 CLI 参数 |\n| `just zeta-dev` | `zeta code` | 监听 Rust/TUI 源码并重启 TUI |\n| `just zeta-desktop` | `zeta` | 启动 Electron Desktop 开发环境 |\n| `just zeterm` | `zeterm` | 启动纯 Rust Desktop |\n| `just zeterm-dev` | `zeterm` | 监听 Rust/shader 源码并重启 Native Desktop |\n\n产品线命令是唯一的公开 `just` 命令面；不要重新添加以 `tui` 或 `native` 为名的实现层别名。\n\n修改产品归属、前后端接线或终端 owner 时，先以本文的产品线定义为准，再分别更新对应\n宿主文档和实现 README；不要用 `code`、`zeta-tui` 或旧的 Native 迁移标识反推公开\n产品线名称。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "当前调用关系",
        "title": "当前调用关系"
      },
      {
        "depth": 2,
        "id": "终端分层",
        "title": "终端分层"
      },
      {
        "depth": 2,
        "id": "代码入口对照",
        "title": "代码入口对照"
      },
      {
        "depth": 2,
        "id": "canonical-just-命令",
        "title": "Canonical just 命令"
      }
    ],
    "searchText": " 状态：Current product model。本文是三条公开产品线的 canonical 说明。 Electron Desktop 的构建变体与静态入口见 product-editions.md； 具体实现分别见 zeta-cli-architecture.md、 zeta-desktop-architecture.md、tui.md 和 zeterm/docs/native-terminal-ui.md。 快速理解 Zeta 不是一个 UI 宿主的三种包装，而是三条产品线共享 Rust 后端契约、各自拥有 UI 宿主。凡是 Session、Thread、Turn、ThreadItem Agent 产品能力，都必须经过 App Server；zeterm 当前直接组合的路径只属于终端/PTY 宿主，不是 Agent API 的例外。 产品线 产品形态 当前 UI/宿主 前后端接线 终端实现边界 --- --- --- --- --- zeta code TUI 产品 zeta-code/cli + zeta-code/tui zeta-app-server-client 连接 App Server TUI 管理自己的 crossterm/ratatui 宿主终端；不直接拥有子 PTY zeta Electron Desktop Renderer + Preload + Electron Main Electron Main 启动并桥接 Rust App Server 当前 Renderer 用 xterm；Rust/App Server 管理 zeta-utils-pty zeterm 纯 Rust Desktop zeterm/ 原生窗口与 UI Rust 进程内直接组合 zeta-terminal 负责终端语义，zeta-utils-pty 负责 PTY/进程 产品线与 Electron 的内部构建变体不是同一个维度。当前 Desktop 保留 code、academic 两个源码和构建标识；它们是 zeta Electron Desktop 的构建变体，不代表 zeta code TUI，也不构成额外的公开产品线。具体说明见 product-editions.md。 当前调用关系 ``mermaid flowchart LR Code \"zeta code\\nTUI\" -- Client \"zeta-app-server-client\" Desktop \"zeta\\nElectron Renderer\" -- Main \"Electron Main\" Main -- Server \"Rust App Server\" Client -- Server Server -- Pty \"zeta-utils-pty\" Term \"zeterm\\nRust Desktop\" -- Core \"zeta-terminal\" Term -- Pty Term -. \"Agent capability only\" .- Server ` Electron Main 只属于 zeta 产品线。它负责 Electron 生命周期、App Server 子进程监督、 可信 IPC 和 Renderer adapter；它不是三条产品线共享的通用后端层。 zeta code 的 TUI 宿主终端和“运行一个子 Shell 的终端能力”必须区分：前者属于 TUI 的 crossterm/ratatui 事件循环，后者如果产品需要，应通过 App Server 的 typed contract 使用 后端 PTY。TUI 不因为运行在终端里，就自动成为 PTY owner。 终端分层 层 当前 owner 负责什么 不负责什么 --- --- --- --- PTY/进程层 zeta-utils-pty spawn、读写、resize、signal、exit ANSI/VT 解析、网格、scrollback 语义 终端语义层 zeta-terminal ANSI/VT parser、cell/grid、cursor、mode、scrollback 等 创建 Shell 进程、Electron IPC、产品窗口 产品后端层 Rust App Server 连接级 Terminal session、授权、生命周期和 protocol DTO Renderer DOM 或 TUI 绘制 Electron 桥接层 zeta 的 Electron Main 进程监督、trusted IPC、Renderer adapter 复制 Rust 终端状态机 TUI 宿主层 zeta code 的 zeta-tui raw mode、alternate screen、输入事件和 Ratatui frame 第二套 Agent runtime 或 PTY authority Native 宿主层 zeterm 的 zeterm/ 原生窗口、GPU/UI、终端输入输出组合 Electron Main、Renderer bridge 因此，zeta-terminal 不能被 zeta-utils-pty 替代。两者在 zeterm 中已经是上下层组合； 在 zeta 中是否由 App Server 进一步组合 zeta-terminal，取决于是否把终端语义状态从 Renderer 的 xterm 投影迁移为 Rust authoritative state，这属于独立的终端演进，不影响三条 产品线的宿主划分。 代码入口对照 公开产品线 当前代码入口 当前状态 --- --- --- zeta code zeta-code/cli 的 zeta binary → zeta-tui TUI 产品路径已存在；TUI 通过 App Server Client 工作 zeta desktop Electron client Electron Desktop 已存在；默认 Renderer 仍使用 code 这一内部构建 ID zeterm zeterm/ 的 zeterm binary 终端宿主已存在，并直接组合 zeta-terminal 与 zeta-utils-pty；Agent 能力尚未作为 Native 旁路提供 Canonical just 命令 命令 产品线 运行方式 --- --- --- just zeta zeta code 从 Rust workspace 启动 TUI，并接收 CLI 参数 just zeta-dev zeta code 监听 Rust/TUI 源码并重启 TUI just zeta-desktop zeta 启动 Electron Desktop 开发环境 just zeterm zeterm 启动纯 Rust Desktop just zeterm-dev zeterm 监听 Rust/shader 源码并重启 Native Desktop 产品线命令是唯一的公开 just 命令面；不要重新添加以 tui 或 native 为名的实现层别名。 修改产品归属、前后端接线或终端 owner 时，先以本文的产品线定义为准，再分别更新对应 宿主文档和实现 README；不要用 code、zeta-tui` 或旧的 Native 迁移标识反推公开 产品线名称。"
  },
  {
    "slug": "zeta-rs-architecture",
    "sourcePath": "docs/zeta-rs-architecture.md",
    "title": "zeta-rs 共享后端与统一对外层",
    "description": "zeta-rs 是多个产品共享的 Rust 后端；Core 只是其中的执行控制面。协议、存储、配置、模型、工具和 产品接口各有独立边界，不能都塞回 Core，产品宿主也不应反向放回这里。",
    "group": "开始了解",
    "markdown": "> 当前基线：[Zeta 长期架构](zeta-code-architecture-codex-style-v2.md) 与\n> [App Server API](zeta-app-server-api.md)。\n> Agent 自定义对象与外部生态反腐化层见\n> [`agent-customizations.md`](agent-customizations.md)。\n> 各 crate 的当前 public/private interface、调用图与修改路径以对应 README 为准。\n\n## 快速理解\n\n`zeta-rs` 是多个产品共享的 Rust 后端；Core 只是其中的执行控制面。协议、存储、配置、模型、工具和\n产品接口各有独立边界，不能都塞回 Core，产品宿主也不应反向放回这里。\n\n| 想知道什么 | 直接答案 | 从哪里继续 |\n| --- | --- | --- |\n| 哪些状态由 Rust 权威拥有？ | Session、Thread、Turn、工具生命周期、配置与持久化事实 | [核心](#4-核心) |\n| Desktop、`zeta-code` 和其他 Agent 客户端如何调用？ | 统一经过 App Server API，不链接 Core、Store 或 Provider | [对外接口](#8-app-server) |\n| protocol、history、Core 和 storage 有什么区别？ | 分别拥有共享事实、持久化记录形状、状态协调和物理读写 | [Protocol 边界](#3-protocol-边界)、[存储](#5-存储端口与物理存储) |\n| 为什么有这么多 crate？ | 按可独立验证的责任拆分，不按功能名称堆成通用 service | [Workspace 边界](#2-workspace-边界) |\n| 具体函数和修改路径在哪里？ | 进入对应 crate README，系统文档不复制私有实现 | [文档规范](documentation-guidelines.md) |\n\n## 1. Workspace 职责\n\n`zeta-rs/` 是共享 Rust 后端，`zeta-rs/core/` 是领域运行时。它负责：\n\n- Session、Thread、Turn、ThreadItem 的 reducer、命令与恢复；\n- Agent/model/tool 执行编排；\n- persisted Thread history contract、typed SessionStore/ThreadStore 与 SQLite adapter；\n- Config、Credential、sandbox 与 resource；\n- App Server、client、transport；\n- Rust、TypeScript 与 JSON Schema contract tests。\n\nDesktop UI、Electron IPC、`zeta-code` 的 TUI 宿主和第三方网页 UI 不属于共享后端。\n\n## 2. Workspace 边界\n\n```text\nzeta-rs/\n├── protocol/             # canonical shared domain contract\n├── agent-import/         # external Agent ecosystem discovery + metadata-only import plan\n├── tools/                # target host-side tool types, interfaces and pure adapters\n├── shell-command/        # concrete approved-process executor\n├── file-system/          # concrete read-only filesystem executor\n├── file-search/          # workspace path fuzzy search + CLI\n├── code-index/           # workspace-side chunk identity、local SQLite/FTS 与 revision-bound retrieval\n├── code-index-cloud/     # explicit egress grants、cloud publication/deletion lifecycle 与 provider port\n├── code-index-semantic/  # local embedding cache、vector recall、rerank 与来源内 final ranking\n├── code-retrieval/       # lexical/semantic/optional remote fusion、dedupe、fallback、verification 与 budget\n├── slash-commands/       # headless catalog, input grammar and interaction state\n├── slash-launcher/       # product-selected list composition, slash query and selection state\n├── file-watcher/         # shared filesystem invalidation hints\n├── git/                  # bounded Git repository operations and structured parsing\n├── diff/                 # shared bounded line/inline diff mapping for Native and TUI\n├── syntax/               # bounded incremental tree-sitter analysis；不拥有文件、索引或 presentation\n├── terminal-detection/   # host terminal identity/color capability 与 background fallback interpretation\n├── theme/                # shared manifest/user-theme resolver 与 device-local bounded loader\n├── editor-core/          # 纯 Rust text transaction / selection / history；不拥有 presentation 或 transport\n├── text-file/            # UTF-8 文件保存基线、磁盘版本与外部变化冲突；不拥有 editor 或 I/O\n├── markdown/             # shared bounded CommonMark/GFM parsing；presentation 位于 zeterm/\n├── lsp/                  # LSP stdio lifecycle、request pairing、document sync 与 server events\n├── language-server-catalog/ # 内置 server、可信 executable resolution 与 availability；不启动进程\n├── language-server-distribution/ # verified package staging 与 side-by-side install storage\n├── language-service/     # 产品级 LSP 启停、文档路由、请求 facade 与 stale-result gate\n├── install-context/      # runtime install method, package layout and resource candidates\n├── apply-patch/          # concrete validated write executor\n├── session-store/        # Session persistence port + envelope\n├── history/              # model-history + persisted Thread record domain types\n├── thread-store/         # Thread persistence port + append/page validation\n├── context-engine/       # provider-neutral context budget、token measurement 与边界判定\n├── core/                 # reducers, coordinators, execution policy and recovery\n├── storage/              # SQLite Session/Thread authority adapters\n├── rollout/              # local state repository + recovery composition（crate 名待清理）\n├── rollout-trace/        # read-only export, diagnostics and evaluation artifact\n├── app-server-protocol/  # external RPC wire contract + generators\n├── app-server-transport/\n├── app-server-client/\n├── app-server/\n├── mcp-server/           # current stdio/HTTP Agent-as-tool App Server adapter\n├── config/\n├── secrets/              # provider-neutral secret persistence primitives\n├── login/                # target interactive account-login control plane\n├── codex-app-server/     # target adapter for the external Codex App Server\n├── model-provider-config/\n├── model-provider/\n├── zeta-api/\n├── http-client/           # shared proxy/TLS/unary HTTP substrate；stream/WebSocket 尚未实现\n├── zeta-client/           # API operation retry 与 SSE framing layer\n├── exec/                  # target headless Agent runner\n├── tool-executor/         # target local process execution boundary\n```\n\n产品宿主不属于共享后端：`zeterm` 的 `zui`、`zeta-ui`、renderer、`wgpu` 和 `winit` 位于\n`zeterm/` 的直接子 crate；`zeta-code` 的 `zeta-cli` 与 `zeta-tui` 位于 `zeta-code/`。它们仍加入同一个\n根 Cargo workspace，但 ownership 由物理目录和依赖方向表达。\n\n当前 `exec/` 仍实现 process `ToolExecutor`。它迁移为 `tool-executor/` 后，`exec/` 名称用于\n[`exec.md`](exec.md) 定义的 headless Agent runner；迁移完成前不能把目标目录注释理解为现状。\n\n不建立职责含糊的 `common`、`service` 或总括式执行 crate。Agent loop 先在 Core 内按模块\n分层；只有具备第二个真实消费者、独立 typed port 与测试 vertical slice 时才提取 crate。\n\n`zeta-git` 当前拥有系统 Git 进程身份、仓库发现、porcelain-v2 快照、分支/远端/历史查询、\n类型化暂存/取消暂存/丢弃/提交/获取/拉取/推送以及补丁检查与应用；不拥有实时仓库注册表、\n监听状态、App Server 线协议或 Desktop SCM。\nGit 状态/修改适配器、工作区范围运行时、监听失效/版本通知与 SCM View 已在各自层实现；\n它们不改变 `zeta-git` 的 crate 所有权。当前 API 和失败语义见\n[`git/README.md`](../zeta-rs/git/README.md)，跨层状态见 [`git.md`](git.md)。\n\n`zeta-diff` 当前拥有 Native 与 TUI 共享的 bounded text diff：精确 line ending、Myers 行映射、\nUnicode 字素级内联范围、Git-style hunk、比较策略、取消和资源上限；不读取文件、不调用 Git，\n也不拥有 Editor/TUI presentation。当前 API、失败和修改影响见\n[`diff/README.md`](../zeta-rs/diff/README.md)。\n\n`zeta-slash-commands` 当前拥有 App Server、TUI 与 Native 共享的无渲染命令 catalog 校验、\nlocal/server origin、输入 grammar、匹配、选择、dismiss、滚动、补全与 submission parsing；它不执行\n命令、不读取 config，也不依赖任何 renderer。Desktop 通过 generated protocol types 与共享\nconformance fixture 保持语义一致。跨产品边界见 [`slash-commands.md`](slash-commands.md)，当前 API\n和失败语义见 [`slash-commands/README.md`](../zeta-rs/slash-commands/README.md)。\n\n`zeta-slash-launcher` 当前拥有产品选择列表的无渲染组合、首个 `/query` token、跨列表匹配、选择和\ndismiss 状态。它不认识 Slash Command、Skill、业务 target、handler、protocol 或 renderer；产品把\n各领域对象投影成列表，并用返回的 `(list_id, item_id)` 解析自己的 typed binding。当前三个产品尚未\n迁移到该 crate。跨产品分层见 [`slash-commands.md`](slash-commands.md)，实现契约见\n[`slash-launcher/README.md`](../zeta-rs/slash-launcher/README.md)。\n\n`zeta-syntax` 当前拥有 Rust、JSON、JSONC 与 Shell 文档的有界增量 tree-sitter parse、revision binding，以及\nsyntax token、folding range、document symbol 和 parse diagnostic snapshot。它不读取文件、\n不监听 workspace、不保存符号索引，也不依赖 legacy editor runtime、`zeta-editor` 或 `zeta-lsp`。它是\nRust `zeta-editor` 内部组合的底层分析 crate，不是 App Server 产品 API。跨编辑器\n所有权与演进阶段见 [`syntax-analysis.md`](syntax-analysis.md)，当前 API 和修改路径见\n[`syntax/README.md`](../zeta-rs/syntax/README.md)。\n\n`zeta-code-index` 当前拥有授权 `WorkspaceRoot` 内的 ignore-aware scan、syntax-assisted/fallback\nchunking、stable revision/chunk identity、local SQLite generation 与 FTS5 retrieval。它消费\n`zeta-syntax` declaration facts，但不把 workspace lifecycle 下沉给 parser；App Server 拥有 watcher、\nprofile placement 和 RPC state。跨层隐私与云端边界见 [`code-index.md`](code-index.md)，实现契约见\n[`code-index/README.md`](../zeta-rs/code-index/README.md)。\n\n`zeta-code-index-cloud` 当前拥有 chunk-only 云投影的 root-bound consent、精确 preview、\nbyte/path scope、provider capability、durable publication/deletion lifecycle。它只消费\n`zeta-code-index` 复核后的 exact chunks，不允许 provider 读取完整 source 后重新切块，也不实现 credential/network/provider 本身；默认\nlocal composition 的 registry 为空。provider query 契约要求云端 CodeIndex 完成 embedding/vector\nrecall/rerank/过滤/截断并返回 final relevance order。具体 contract 见\n[`code-index-cloud/README.md`](../zeta-rs/code-index-cloud/README.md)。\n\n`zeta-code-index-semantic` 当前拥有本地语义 projection：只接收 Workspace 复核后的\n`MaterializedChunk`，准备 embedding/rerank 输入，在本地 SQLite 复用/持久化 vectors，执行\nexact-generation vector recall，并解释 rerank 分数形成来源内 final order。它没有 filesystem、scan、\nignore、chunker 或远端服务 authority；模型网络适配继续属于 `zeta-model-provider`。具体 contract 见\n[`code-index-semantic/README.md`](../zeta-rs/code-index-semantic/README.md)。\n\n`zeta-code-retrieval` 当前拥有 lexical/local-semantic/optional-remote candidate fan-out、deterministic\nRRF、revision-bound identity dedupe、可选来源 failure fallback、current-source excerpt verification 与\ncontent byte budget。它保留每个来源给出的排序，不执行 embedding/rerank、不拥有 grant/network 或\nAgent conversation state；App Server 按请求组合并通过\n`workspace/codeIndex/retrieve` 暴露结果。具体 contract 见\n[`code-retrieval/README.md`](../zeta-rs/code-retrieval/README.md)。\n\n`zeta-theme` 当前嵌入 Desktop registry 生成的语言中立 manifest，严格解析同一用户主题 JSON，\n解析 alias/transform/default graph，并从 device-local `configuration.json` 与 `themes/*.json`\n选择 Graphical/Terminal snapshot。它不依赖 renderer、不拥有组件 geometry，也不进入 Agent\nconfig、Session/Thread store。`zeterm/zeterm` 消费完整相关 palette，TUI 只消费明确子集；当前 API、\n失败语义和 conformance contract 见 [`theme/README.md`](../zeta-rs/theme/README.md)。\n\n`zeta-editor` 当前拥有 `zeterm/zeterm` 使用的多行编辑、caret/selection、undo/redo、IME、language-aware\nsyntax lifecycle/projection、普通文档结构折叠、viewport soft wrap 与 source/visual row 映射、代码视口绘制、retained `DiffEditorDocument`、复用两个 CodeEditor pane 的 side-by-side DiffEditor，以及纵向组合\n多个文件 section 的 MultiDiffEditor；它依赖\n`zeta-ui`、`zeta-diff` 和 `zeta-syntax`，但不依赖 `zeterm/zeterm`，也不拥有文件 Tab、平台事件、EditorHost 或\nTUI presentation。当前 API、接入义务和限制见\n[`editor/README.md`](../zeterm/editor/README.md)。\n\n`zeta-editor-core` 当前拥有不依赖 renderer 或 transport 的纯 Rust document transaction vertical\nslice：UTF-16 selection、revision-bound atomic multi-edit、bounded undo/redo 和 snapshot。`zeterm/zeterm`\n的 `CodeEditorDocument` 是当前真实消费者，以 persistent core 持有 committed text/history/revision，zeterm text\nprojection 仅供行索引、syntax、folding 与绘制使用。Zeta Aster 是独立的 TypeScript Browser editor，拥有自己的\nPieceTree、transaction、history、selection 和 tracked ranges；它只异步消费 Rust file/language/workspace service，\n不通过 WASM 或 App Server shadow document 调用 `zeta-editor-core`。跨运行时边界见\n[`editor-core.md`](editor-core.md)。\n\n`zeta-text-file` 当前拥有与编辑器实现无关的 UTF-8 文件生命周期：保存文本基线、磁盘版本、\n只读状态、dirty/reload/conflict 分类、乐观保存 payload 与待处理外部 snapshot。它不读取或写入\n文件、不拥有 mutable editor text、Tab、关闭确认或 presentation。`zeterm/zeterm` 把 active\n`CodeEditorDocument` 的当前文本交给该领域模型，并通过 App Server 的独立文件能力执行 I/O；\nNative 拥有关闭确认和 reload/conflict 操作条，而显式覆盖请求仍使用待处理外部 snapshot 的版本\n执行乐观 preflight，磁盘再次变化时不会无条件写入；\n当前 API、失败语义和接入义务见\n[`text-file/README.md`](../zeta-rs/text-file/README.md)。\n\n`zeta-markdown` 当前拥有有资源上限的 CommonMark/GFM parsing、只读文档 snapshot、富文本与\nblock layout 和 zeterm presentation，并消费 `zeta-ui::ScrollState`；它依赖 `zeta-ui`，但不依赖\n`zeterm/zeterm`，也不拥有消息 identity、网络图片、链接激活、平台输入或持久化。当前 API、\n信任边界和限制见 [`markdown/README.md`](../zeterm/markdown/README.md)。\n\n`zeta-lsp` 当前拥有单语言服务器的 stdio/async transport、initialize gate、typed request\npairing、deadline cancellation、文档同步版本、push diagnostics 与规范关闭；宿主路由层另外\n把一个 language ID 绑定到一个 initialized client，保存 editor revision / server incarnation /\ndocument version，并在显式 replacement 时重放当前全文。它不依赖 `zeta-editor` 或 zeterm host，也不拥有\nserver discovery、安装、workspace 配置、restart policy 或 UI projection。\n它会上报规范关闭之外的 transport-close 事实；`zeta-language-service` 用 generation/server epoch\n隔离旧实例，拥有断连 route retirement、有限指数退避、crash-loop gate 和 authoritative snapshot\n重放。跨层所有权与当前阶段见 [`lsp.md`](lsp.md)，当前 API 和修改路径见\n[`lsp/README.md`](../zeta-rs/lsp/README.md)。\n\n`zeta-language-service` 把 fresh diagnostics 转换为 product-neutral UTF-8 document ranges；zeterm\nadapter 只负责转换到 `CodeEditorDiagnostic` 并按精确 editor revision 选择缓存，CodeEditor 自己\n完成跨行、soft-wrap 波浪线与命中，Native 在命中后绘制 hover detail。任何 LSP 类型进入 editor\ncrate，或 zeterm 重新计算波浪线 geometry，都表示该边界发生漂移。\n\n`zeta-language-server-catalog` 当前拥有内置 Rust、JSON/JSONC、Shell server identity、\nAutomatic/Enabled/Disabled preference、execution policy gate、冻结 PATH candidate 校验、canonical\nexecutable 和 availability；App Server Config authority 分别持久化 mode/path，Native Settings UI 为三个\nserver 保留独立 draft，并只提交 revision-safe typed command，\n再把权威 snapshot 映射进 catalog，并将 definition 交给 language-service。它不启动进程、不决定 workspace trust，也不\n读取编辑器文档。crate contract 见\n[`language-server-catalog/README.md`](../zeta-rs/language-server-catalog/README.md)。\n\n`zeta-language-server-distribution` 拥有 application-managed package 的 traversal validation、\nprovider-supplied SHA-256 verification、private staging、原子 publish、version receipt 与 side-by-side\nupdate storage。它不查询网络、不选择 release channel、不操作全局包管理器，也不激活版本；可信 release\nprovider 与 Config/Native 激活 UI 是独立产品责任。\n\n`zeta-language-service` 当前位于产品宿主与 `zeta-lsp` 之间，拥有显式 enablement、resolved\ndefinition 消费、非阻塞文档/request API、editor revision / LSP version freshness、位置编码转换、\nserver generation 和 supervisor thread 生命周期。`zeterm/zeterm` 已把文件 open/change/save/close、workspace\nreplacement、hover/completion/definition 和事件循环接到该层；PATH 中存在有效内置 server 时启用\n对应 route；config generation 变化时 zeterm 重建服务并重放全部打开文档。它不读取文件、不依赖\n`zeta-editor`、不发现 executable，也不绘制 UI。crate contract 见\n[`language-service/README.md`](../zeta-rs/language-service/README.md)。\n\n`zeta-ui::ScrollState`、`ScrollMetrics`、`ScrollView` 与 `ScrollbarController` 提供\ndomain-agnostic logical-pixel offset、clamp、viewport clip、内容坐标、同源 scrollbar\npaint/hit/track-page/thumb-drag geometry，以及 hover/active/fade deadline。MultiDiffEditor\n复用这套基座；平台 wheel normalization、pointer capture，以及 Terminal scrollback 的距底部\n行偏移、输出增长锚定和 alternate-screen 分流仍由 `zeterm/zeterm` 拥有，不能迁入通用 ScrollView。\n\n组件到 GPU 的依赖方向固定为 `zeta-ui Component → zui::UiScene → Renderer → concrete backend`；\n`UiScene` 通过 `SceneBatch` 保留跨 primitive 的真实绘制顺序。`zui` 不依赖组件 crate、窗口系统或\nwgpu，`zeta-ui` 只向下依赖 `zui`；`zeterm/zeterm` 只保存\n`dyn Renderer`，当前具体类型只在 composition-root adapter 中选择。Native 的 interaction 与\naccessibility frame 不进入 renderer。完整所有权、后端替换路径和架构约束见\n[`zeterm/docs/rendering-architecture.md`](../zeterm/docs/rendering-architecture.md)。\n\nDirect-provider credential ownership 由 [`model-provider.md`](model-provider.md) 维护；通用 secret\npersistence 由 [`secrets.md`](secrets.md) 维护；interactive login control plane 由\n[`login.md`](login.md) 维护。Workspace 不创建统一 credential/OAuth crate，也不让 Core、API 或\nnetwork client 读取 secret store。\n\nChatGPT/Codex subscription 通过 [`codex-app-server.md`](codex-app-server.md) 接入：\n`zeta-codex-app-server` 启动并适配上游 `codex app-server`，由上游拥有 PKCE、callback、token\npersistence/refresh 和 Codex backend compatibility。Zeta App Server 只组合其 redacted login/account\n状态；完整 Codex Agent loop 通过 Core `TurnExecutionBackend` 委托，不进入 `model-provider`。产品\ncomposition 使用 `openai-chatgpt` provider 的 exact ModelRef 显式选择哪些 Turn 使用该 backend，\n不会由“已登录”隐式切换。Zeta 不复制 OAuth 实现，也不读取 `~/.codex/auth.json`。\n\n## 3. Protocol 边界\n\nCanonical 产品模型、command/event/update/request 的分类、ID/cursor 语义、当前缺口和后续迁移\n统一由 [`protocol.md`](protocol.md) 维护。本文件只规定 workspace 依赖关系：\n`zeta-protocol` 是纯共享值层，Core、store、provider adapter 与 App Server wire 可以依赖它，\n它不能反向依赖这些执行或 I/O crate。\n\n工具 host contract、registry/binding、executor interface、MCP/dynamic conversion、tool search、\nPlugin discovery、code mode 与图片精度由 [`tools.md`](tools.md) 维护。`zeta-tools` 复用 protocol\nidentity/content，不拥有 Core 调度、MCP session、Plugin authority 或 provider wire。\n\n## 4. 核心\n\nCore 的完整 ownership、执行组件、ports、并发与恢复规则统一由\n[`core.md`](core.md) 维护。本节只保留 workspace 级约束。\n\nSessionCoordinator 与 ThreadController 都通过纯 reducer 维护可重建 projection：\n\n```text\nstored event + previous snapshot → next snapshot\n```\n\nlive commit 与 recovery 必须调用同一 reducer。副作用顺序固定为：\n\n```text\nvalidate command\n→ build typed events\n→ append atomic batch\n→ update in-memory projection\n→ publish update\n```\n\nappend 失败时不能暴露未提交状态。\n\nSessionCoordinator 只序列化 membership、lineage 与 lifecycle。ThreadController 只序列化一个\nThread 的执行历史。不同 Thread 可并行，不受 Session sequence 阻塞。\n\n## 5. 存储端口与物理存储\n\n`zeta-history` 拥有模型历史中已经落地的 persisted Thread record：`StoredEvent`、exact\n`ThreadCommandReceipt`、event ID、时间戳和 schema version。它是纯数据 contract，不提供 Store，\n也不建立第二份 history authority。具体契约见\n[`history/README.md`](../zeta-rs/history/README.md)。\n\n`zeta-session-store` 仍拥有 Session envelope；`zeta-thread-store` 拥有 storage-neutral Thread\nStore trait、完整恢复/追加请求、atomic batch validator 与错误。Core 依赖 history 类型和这些 port，\n不依赖本地文件实现。Store 细节分别见\n[`session-store/README.md`](../zeta-rs/session-store/README.md) 与\n[`thread-store/README.md`](../zeta-rs/thread-store/README.md)。\n\n`zeta-storage` 当前提供 `SqliteSessionStore` 与 `SqliteThreadStore`。两者打开同一\n`state.sqlite3`，并统一负责：\n\n- `BEGIN IMMEDIATE` 下的 sequence compare-and-set；\n- batch/event identity 唯一性；\n- typed envelope JSON 与可查询 identity/sequence 列的原子提交；\n- foreign key、WAL、`synchronous=FULL` 和 bounded busy timeout；\n- component-scoped schema version gate。\n\nSQLite 是 Session/Thread 的物理 authority，不是 JSONL 的 projection。旧 JSONL stream、\ntail-recovery 和 rollout adapter 已退出执行路径；开发期不保留双写或自动 import。\n\n`zeta-rollout` 中公开的 `LocalStateRepository` 组合 SQLite stores 与 writer lease，负责从一个\nprofile root 恢复可运行的 SessionCoordinator；它先恢复 Thread，再恢复 Session 以便继续\ncreate/fork saga。crate 名仍是历史命名，不能据此重新引入 rollout 文件格式。App Server 的本地\ncomposition root 只依赖该 repository，不重复恢复流程。\n具体打开与恢复顺序见 [`rollout/README.md`](../zeta-rs/rollout/README.md)。\n\n`zeta-rollout-trace` 以两个 store port 为输入，生成只读、可序列化的 Session trace。它适合\n诊断、导出和评测，但不是 authority，也不能成为执行输入。它保留独立 Session/Thread\nsequence，而不把并发 aggregate 拼成伪全局顺序。trace 可能携带用户输入、工具参数和结果，因此\ncrate 不提供默认文件写入；持久化或上传必须由调用方显式施加脱敏、访问控制和保留期策略。\n实现与 privacy obligation 见\n[`rollout-trace/README.md`](../zeta-rs/rollout-trace/README.md)。\n\n当前开发期只读当前 Session/Thread SQLite schema。旧 JSONL、implicit Session、kind/payload\nupcast 和 sidecar ledger 不进入执行路径。旧 Config DB 正文只在 component v1→v2 时一次性迁出\n为 TOML，迁移后不再读取。\n\nConfig transaction metadata 也在同一 profile 数据库中，通过独立 component migration 与表维护\nrevision/generation/receipt contract；desired document 的唯一 authority 是 profile\n`config.toml`，SQLite 只保存 digest 与事务元数据。Workspace 的 `.zeta/config.toml` 是严格、\n只读的 scoped intent，不是用户级数据库的替代 writer。\n\n## 6. Sequence 与并发\n\nSequence、cursor、ID 和 optimistic concurrency 的领域语义统一见\n[`protocol.md`](protocol.md#5-sequencecursor-与-id)。Workspace 实现必须为每个 aggregate\n提供独立 writer lease，使不同 Thread 可以并发且不会占用 Session revision。\n\nFork 在 Session lineage 中保存 `parentThreadId + parentSequence`。该 parent sequence 是一个\n不可变历史锚点，不是另一套物理日志计数。\n\n创建/fork Thread 使用可恢复 saga：\n\n```text\nSession plan(creating)\n→ Thread create\n→ Session attach(active)\n```\n\n## 7. 类型化命令重放\n\nCommand identity、receipt 和 replay 规则统一见\n[`protocol.md`](protocol.md#41-command请求改变状态)。在 Workspace 内，store adapter 负责把\ntyped receipt 与首个业务 event 原子提交，reducer recovery 恢复稳定结果；Config authority\n沿用相同模式。Config、Plugin、MCP 与 Skill 的 authority 分布、snapshot reconcile 和 safe-point\n组合由 [`config.md`](config.md) 统一规定。\n\n## 8. App Server\n\n`zeta-app-server-protocol` 直接引用语义完全一致的 canonical Session/Thread/Turn/ThreadItem、\nevents 和 updates，只为真正 wire-specific 的 params/result/error 定义 DTO。\nRegistry/generator 实现见\n[`app-server-protocol/README.md`](../zeta-rs/app-server-protocol/README.md)。\n\n`zeta-app-server` 负责：\n\n- initialize 与 schema hash gate；\n- method dispatch；registry 已声明 Session/Thread serialization scope，但当前同步 server 尚未\n  接入 per-scope scheduler；\n- connection subscription cursor；\n- `session/update` / `session/thread/update`；\n- Resource ownership；\n- Core error 到 stable RPC error 的映射。\n\n它不重建旧事件、不运行 reducer、不推断领域状态，也不拥有持久化模型。\n当前 dispatch、broker、resource 与 local composition 见\n[`app-server/README.md`](../zeta-rs/app-server/README.md)。\n\n## 9. 客户端与传输\n\n```text\nzeta-exec / TUI\n  → app-server-client::AppServerSession\n  → request handle + event stream\n  → App Server dispatcher\n\nDesktop\n  → generated TypeScript contract\n  → JSONL / stdio\n  → App Server dispatcher\n```\n\nRust `app-server-client` 是本地 App Server 的共享宿主层：负责启动、initialize、请求/事件\nchannel wiring 和显式 shutdown，详细边界见\n[`app-server-client.md`](app-server-client.md)。进程内 typed channel 是性能优化，不是语义\n捷径；Rust 本地路径与 Desktop JSONL 路径都必须经过 typed request/response、initialize、\ndispatcher 和 notification contract。对于 `Session`、`Thread`、`Turn`、`ThreadItem` 产品能力，\nApp Server 是唯一外部进入/输出边界；长期可增加相同契约的远程 App Server 后端。\n\n`zeterm` 当前的 Rust 进程内直接组合只覆盖终端/PTY 宿主。它不能把该宿主路径扩展成 Agent\n产品的 Core 旁路；Native Agent 能力必须复用同一个 App Server 契约和分发器。\n\n`zeta-mcp-server` 当前通过该 client 将 stdio/Streamable HTTP MCP `zeta` / `zeta-reply` tool\ncall 映射到 canonical Session/Thread/Turn，并提供 bounded progress、approval/user-input form\ninteraction 与 principal-scoped durable invocation recovery。它是外层 adapter，不直接依赖\nCore 或 stores；HTTP security、SSE recovery、remote App Server 和 remote Agent bridge 边界见\n[`mcp-server.md`](mcp-server.md)。\n\n产品 Session、App Server connection session 与 terminal session 是三种不同生命周期，命名\n时必须带领域限定。\n\n## 10. 无界面 exec 与远程执行\n\n`zeta-exec` 是无交互 Agent runner，负责 run-once、机器输出、terminal outcome 和未来 scheduler\nworker adapter。它只通过 App Server Client 工作，不依赖 Core、store、provider、sandbox 或\nprocess executor。完整架构见 [`exec.md`](exec.md)。\n\n底层 process execution 已从原同名 crate 迁移为 `zeta-tool-executor`。未来\n`zeta-exec-server` 只负责远程 process/PTY/filesystem execution，不能接收 Agent\n`session/request`。Remote Agent scheduling 与 remote process execution 使用不同 protocol、\nidentity、lease 和 disconnect 语义。\n\n## 11. 验证\n\n```bash\ncargo fmt --check\ncargo clippy --workspace --all-targets -- -D warnings\ncargo test --workspace\ncorepack pnpm --dir desktop test:main\ncorepack pnpm --dir desktop typecheck:renderer\n```\n\n协议变更还必须重新生成并提交 JSON Schema、TypeScript 与 Desktop 同步产物。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-workspace-职责",
        "title": "1. Workspace 职责"
      },
      {
        "depth": 2,
        "id": "2-workspace-边界",
        "title": "2. Workspace 边界"
      },
      {
        "depth": 2,
        "id": "3-protocol-边界",
        "title": "3. Protocol 边界"
      },
      {
        "depth": 2,
        "id": "4-核心",
        "title": "4. 核心"
      },
      {
        "depth": 2,
        "id": "5-存储端口与物理存储",
        "title": "5. 存储端口与物理存储"
      },
      {
        "depth": 2,
        "id": "6-sequence-与并发",
        "title": "6. Sequence 与并发"
      },
      {
        "depth": 2,
        "id": "7-类型化命令重放",
        "title": "7. 类型化命令重放"
      },
      {
        "depth": 2,
        "id": "8-app-server",
        "title": "8. App Server"
      },
      {
        "depth": 2,
        "id": "9-客户端与传输",
        "title": "9. 客户端与传输"
      },
      {
        "depth": 2,
        "id": "10-无界面-exec-与远程执行",
        "title": "10. 无界面 exec 与远程执行"
      },
      {
        "depth": 2,
        "id": "11-验证",
        "title": "11. 验证"
      }
    ],
    "searchText": " 当前基线：Zeta 长期架构 与 App Server API。 Agent 自定义对象与外部生态反腐化层见 agent-customizations.md。 各 crate 的当前 public/private interface、调用图与修改路径以对应 README 为准。 快速理解 zeta-rs 是多个产品共享的 Rust 后端；Core 只是其中的执行控制面。协议、存储、配置、模型、工具和 产品接口各有独立边界，不能都塞回 Core，产品宿主也不应反向放回这里。 想知道什么 直接答案 从哪里继续 --- --- --- 哪些状态由 Rust 权威拥有？ Session、Thread、Turn、工具生命周期、配置与持久化事实 核心 Desktop、zeta-code 和其他 Agent 客户端如何调用？ 统一经过 App Server API，不链接 Core、Store 或 Provider 对外接口 protocol、history、Core 和 storage 有什么区别？ 分别拥有共享事实、持久化记录形状、状态协调和物理读写 Protocol 边界、存储 为什么有这么多 crate？ 按可独立验证的责任拆分，不按功能名称堆成通用 service Workspace 边界 具体函数和修改路径在哪里？ 进入对应 crate README，系统文档不复制私有实现 文档规范 1. Workspace 职责 zeta-rs/ 是共享 Rust 后端，zeta-rs/core/ 是领域运行时。它负责： - Session、Thread、Turn、ThreadItem 的 reducer、命令与恢复； - Agent/model/tool 执行编排； - persisted Thread history contract、typed SessionStore/ThreadStore 与 SQLite adapter； - Config、Credential、sandbox 与 resource； - App Server、client、transport； - Rust、TypeScript 与 JSON Schema contract tests。 Desktop UI、Electron IPC、zeta-code 的 TUI 宿主和第三方网页 UI 不属于共享后端。 2. Workspace 边界 ``text zeta-rs/ ├── protocol/ canonical shared domain contract ├── agent-import/ external Agent ecosystem discovery + metadata-only import plan ├── tools/ target host-side tool types, interfaces and pure adapters ├── shell-command/ concrete approved-process executor ├── file-system/ concrete read-only filesystem executor ├── file-search/ workspace path fuzzy search + CLI ├── code-index/ workspace-side chunk identity、local SQLite/FTS 与 revision-bound retrieval ├── code-index-cloud/ explicit egress grants、cloud publication/deletion lifecycle 与 provider port ├── code-index-semantic/ local embedding cache、vector recall、rerank 与来源内 final ranking ├── code-retrieval/ lexical/semantic/optional remote fusion、dedupe、fallback、verification 与 budget ├── slash-commands/ headless catalog, input grammar and interaction state ├── slash-launcher/ product-selected list composition, slash query and selection state ├── file-watcher/ shared filesystem invalidation hints ├── git/ bounded Git repository operations and structured parsing ├── diff/ shared bounded line/inline diff mapping for Native and TUI ├── syntax/ bounded incremental tree-sitter analysis；不拥有文件、索引或 presentation ├── terminal-detection/ host terminal identity/color capability 与 background fallback interpretation ├── theme/ shared manifest/user-theme resolver 与 device-local bounded loader ├── editor-core/ 纯 Rust text transaction / selection / history；不拥有 presentation 或 transport ├── text-file/ UTF-8 文件保存基线、磁盘版本与外部变化冲突；不拥有 editor 或 I/O ├── markdown/ shared bounded CommonMark/GFM parsing；presentation 位于 zeterm/ ├── lsp/ LSP stdio lifecycle、request pairing、document sync 与 server events ├── language-server-catalog/ 内置 server、可信 executable resolution 与 availability；不启动进程 ├── language-server-distribution/ verified package staging 与 side-by-side install storage ├── language-service/ 产品级 LSP 启停、文档路由、请求 facade 与 stale-result gate ├── install-context/ runtime install method, package layout and resource candidates ├── apply-patch/ concrete validated write executor ├── session-store/ Session persistence port + envelope ├── history/ model-history + persisted Thread record domain types ├── thread-store/ Thread persistence port + append/page validation ├── context-engine/ provider-neutral context budget、token measurement 与边界判定 ├── core/ reducers, coordinators, execution policy and recovery ├── storage/ SQLite Session/Thread authority adapters ├── rollout/ local state repository + recovery composition（crate 名待清理） ├── rollout-trace/ read-only export, diagnostics and evaluation artifact ├── app-server-protocol/ external RPC wire contract + generators ├── app-server-transport/ ├── app-server-client/ ├── app-server/ ├── mcp-server/ current stdio/HTTP Agent-as-tool App Server adapter ├── config/ ├── secrets/ provider-neutral secret persistence primitives ├── login/ target interactive account-login control plane ├── codex-app-server/ target adapter for the external Codex App Server ├── model-provider-config/ ├── model-provider/ ├── zeta-api/ ├── http-client/ shared proxy/TLS/unary HTTP substrate；stream/WebSocket 尚未实现 ├── zeta-client/ API operation retry 与 SSE framing layer ├── exec/ target headless Agent runner ├── tool-executor/ target local process execution boundary ` 产品宿主不属于共享后端：zeterm 的 zui、zeta-ui、renderer、wgpu 和 winit 位于 zeterm/ 的直接子 crate；zeta-code 的 zeta-cli 与 zeta-tui 位于 zeta-code/。它们仍加入同一个 根 Cargo workspace，但 ownership 由物理目录和依赖方向表达。 当前 exec/ 仍实现 process ToolExecutor。它迁移为 tool-executor/ 后，exec/ 名称用于 exec.md 定义的 headless Agent runner；迁移完成前不能把目标目录注释理解为现状。 不建立职责含糊的 common、service 或总括式执行 crate。Agent loop 先在 Core 内按模块 分层；只有具备第二个真实消费者、独立 typed port 与测试 vertical slice 时才提取 crate。 zeta-git 当前拥有系统 Git 进程身份、仓库发现、porcelain-v2 快照、分支/远端/历史查询、 类型化暂存/取消暂存/丢弃/提交/获取/拉取/推送以及补丁检查与应用；不拥有实时仓库注册表、 监听状态、App Server 线协议或 Desktop SCM。 Git 状态/修改适配器、工作区范围运行时、监听失效/版本通知与 SCM View 已在各自层实现； 它们不改变 zeta-git 的 crate 所有权。当前 API 和失败语义见 git/README.md，跨层状态见 git.md。 zeta-diff 当前拥有 Native 与 TUI 共享的 bounded text diff：精确 line ending、Myers 行映射、 Unicode 字素级内联范围、Git-style hunk、比较策略、取消和资源上限；不读取文件、不调用 Git， 也不拥有 Editor/TUI presentation。当前 API、失败和修改影响见 diff/README.md。 zeta-slash-commands 当前拥有 App Server、TUI 与 Native 共享的无渲染命令 catalog 校验、 local/server origin、输入 grammar、匹配、选择、dismiss、滚动、补全与 submission parsing；它不执行 命令、不读取 config，也不依赖任何 renderer。Desktop 通过 generated protocol types 与共享 conformance fixture 保持语义一致。跨产品边界见 slash-commands.md，当前 API 和失败语义见 slash-commands/README.md。 zeta-slash-launcher 当前拥有产品选择列表的无渲染组合、首个 /query token、跨列表匹配、选择和 dismiss 状态。它不认识 Slash Command、Skill、业务 target、handler、protocol 或 renderer；产品把 各领域对象投影成列表，并用返回的 listid, itemid 解析自己的 typed binding。当前三个产品尚未 迁移到该 crate。跨产品分层见 slash-commands.md，实现契约见 slash-launcher/README.md。 zeta-syntax 当前拥有 Rust、JSON、JSONC 与 Shell 文档的有界增量 tree-sitter parse、revision binding，以及 syntax token、folding range、document symbol 和 parse diagnostic snapshot。它不读取文件、 不监听 workspace、不保存符号索引，也不依赖 legacy editor runtime、zeta-editor 或 zeta-lsp。它是 Rust zeta-editor 内部组合的底层分析 crate，不是 App Server 产品 API。跨编辑器 所有权与演进阶段见 syntax-analysis.md，当前 API 和修改路径见 syntax/README.md。 zeta-code-index 当前拥有授权 WorkspaceRoot 内的 ignore-aware scan、syntax-assisted/fallback chunking、stable revision/chunk identity、local SQLite generation 与 FTS5 retrieval。它消费 zeta-syntax declaration facts，但不把 workspace lifecycle 下沉给 parser；App Server 拥有 watcher、 profile placement 和 RPC state。跨层隐私与云端边界见 code-index.md，实现契约见 code-index/README.md。 zeta-code-index-cloud 当前拥有 chunk-only 云投影的 root-bound consent、精确 preview、 byte/path scope、provider capability、durable publication/deletion lifecycle。它只消费 zeta-code-index 复核后的 exact chunks，不允许 provider 读取完整 source 后重新切块，也不实现 credential/network/provider 本身；默认 local composition 的 registry 为空。provider query 契约要求云端 CodeIndex 完成 embedding/vector recall/rerank/过滤/截断并返回 final relevance order。具体 contract 见 code-index-cloud/README.md。 zeta-code-index-semantic 当前拥有本地语义 projection：只接收 Workspace 复核后的 MaterializedChunk，准备 embedding/rerank 输入，在本地 SQLite 复用/持久化 vectors，执行 exact-generation vector recall，并解释 rerank 分数形成来源内 final order。它没有 filesystem、scan、 ignore、chunker 或远端服务 authority；模型网络适配继续属于 zeta-model-provider。具体 contract 见 code-index-semantic/README.md。 zeta-code-retrieval 当前拥有 lexical/local-semantic/optional-remote candidate fan-out、deterministic RRF、revision-bound identity dedupe、可选来源 failure fallback、current-source excerpt verification 与 content byte budget。它保留每个来源给出的排序，不执行 embedding/rerank、不拥有 grant/network 或 Agent conversation state；App Server 按请求组合并通过 workspace/codeIndex/retrieve 暴露结果。具体 contract 见 code-retrieval/README.md。 zeta-theme 当前嵌入 Desktop registry 生成的语言中立 manifest，严格解析同一用户主题 JSON， 解析 alias/transform/default graph，并从 device-local configuration.json 与 themes/.json 选择 Graphical/Terminal snapshot。它不依赖 renderer、不拥有组件 geometry，也不进入 Agent config、Session/Thread store。zeterm/zeterm 消费完整相关 palette，TUI 只消费明确子集；当前 API、 失败语义和 conformance contract 见 theme/README.md。 zeta-editor 当前拥有 zeterm/zeterm 使用的多行编辑、caret/selection、undo/redo、IME、language-aware syntax lifecycle/projection、普通文档结构折叠、viewport soft wrap 与 source/visual row 映射、代码视口绘制、retained DiffEditorDocument、复用两个 CodeEditor pane 的 side-by-side DiffEditor，以及纵向组合 多个文件 section 的 MultiDiffEditor；它依赖 zeta-ui、zeta-diff 和 zeta-syntax，但不依赖 zeterm/zeterm，也不拥有文件 Tab、平台事件、EditorHost 或 TUI presentation。当前 API、接入义务和限制见 editor/README.md。 zeta-editor-core 当前拥有不依赖 renderer 或 transport 的纯 Rust document transaction vertical slice：UTF-16 selection、revision-bound atomic multi-edit、bounded undo/redo 和 snapshot。zeterm/zeterm 的 CodeEditorDocument 是当前真实消费者，以 persistent core 持有 committed text/history/revision，zeterm text projection 仅供行索引、syntax、folding 与绘制使用。Zeta Aster 是独立的 TypeScript Browser editor，拥有自己的 PieceTree、transaction、history、selection 和 tracked ranges；它只异步消费 Rust file/language/workspace service， 不通过 WASM 或 App Server shadow document 调用 zeta-editor-core。跨运行时边界见 editor-core.md。 zeta-text-file 当前拥有与编辑器实现无关的 UTF-8 文件生命周期：保存文本基线、磁盘版本、 只读状态、dirty/reload/conflict 分类、乐观保存 payload 与待处理外部 snapshot。它不读取或写入 文件、不拥有 mutable editor text、Tab、关闭确认或 presentation。zeterm/zeterm 把 active CodeEditorDocument 的当前文本交给该领域模型，并通过 App Server 的独立文件能力执行 I/O； Native 拥有关闭确认和 reload/conflict 操作条，而显式覆盖请求仍使用待处理外部 snapshot 的版本 执行乐观 preflight，磁盘再次变化时不会无条件写入； 当前 API、失败语义和接入义务见 text-file/README.md。 zeta-markdown 当前拥有有资源上限的 CommonMark/GFM parsing、只读文档 snapshot、富文本与 block layout 和 zeterm presentation，并消费 zeta-ui::ScrollState；它依赖 zeta-ui，但不依赖 zeterm/zeterm，也不拥有消息 identity、网络图片、链接激活、平台输入或持久化。当前 API、 信任边界和限制见 markdown/README.md。 zeta-lsp 当前拥有单语言服务器的 stdio/async transport、initialize gate、typed request pairing、deadline cancellation、文档同步版本、push diagnostics 与规范关闭；宿主路由层另外 把一个 language ID 绑定到一个 initialized client，保存 editor revision / server incarnation / document version，并在显式 replacement 时重放当前全文。它不依赖 zeta-editor 或 zeterm host，也不拥有 server discovery、安装、workspace 配置、restart policy 或 UI projection。 它会上报规范关闭之外的 transport-close 事实；zeta-language-service 用 generation/server epoch 隔离旧实例，拥有断连 route retirement、有限指数退避、crash-loop gate 和 authoritative snapshot 重放。跨层所有权与当前阶段见 lsp.md，当前 API 和修改路径见 lsp/README.md。 zeta-language-service 把 fresh diagnostics 转换为 product-neutral UTF-8 document ranges；zeterm adapter 只负责转换到 CodeEditorDiagnostic 并按精确 editor revision 选择缓存，CodeEditor 自己 完成跨行、soft-wrap 波浪线与命中，Native 在命中后绘制 hover detail。任何 LSP 类型进入 editor crate，或 zeterm 重新计算波浪线 geometry，都表示该边界发生漂移。 zeta-language-server-catalog 当前拥有内置 Rust、JSON/JSONC、Shell server identity、 Automatic/Enabled/Disabled preference、execution policy gate、冻结 PATH candidate 校验、canonical executable 和 availability；App Server Config authority 分别持久化 mode/path，Native Settings UI 为三个 server 保留独立 draft，并只提交 revision-safe typed command， 再把权威 snapshot 映射进 catalog，并将 definition 交给 language-service。它不启动进程、不决定 workspace trust，也不 读取编辑器文档。crate contract 见 language-server-catalog/README.md。 zeta-language-server-distribution 拥有 application-managed package 的 traversal validation、 provider-supplied SHA-256 verification、private staging、原子 publish、version receipt 与 side-by-side update storage。它不查询网络、不选择 release channel、不操作全局包管理器，也不激活版本；可信 release provider 与 Config/Native 激活 UI 是独立产品责任。 zeta-language-service 当前位于产品宿主与 zeta-lsp 之间，拥有显式 enablement、resolved definition 消费、非阻塞文档/request API、editor revision / LSP version freshness、位置编码转换、 server generation 和 supervisor thread 生命周期。zeterm/zeterm 已把文件 open/change/save/close、workspace replacement、hover/completion/definition 和事件循环接到该层；PATH 中存在有效内置 server 时启用 对应 route；config generation 变化时 zeterm 重建服务并重放全部打开文档。它不读取文件、不依赖 zeta-editor、不发现 executable，也不绘制 UI。crate contract 见 language-service/README.md。 zeta-ui::ScrollState、ScrollMetrics、ScrollView 与 ScrollbarController 提供 domain-agnostic logical-pixel offset、clamp、viewport clip、内容坐标、同源 scrollbar paint/hit/track-page/thumb-drag geometry，以及 hover/active/fade deadline。MultiDiffEditor 复用这套基座；平台 wheel normalization、pointer capture，以及 Terminal scrollback 的距底部 行偏移、输出增长锚定和 alternate-screen 分流仍由 zeterm/zeterm 拥有，不能迁入通用 ScrollView。 组件到 GPU 的依赖方向固定为 zeta-ui Component → zui::UiScene → Renderer "
  },
  {
    "slug": "zeta-desktop-architecture",
    "sourcePath": "docs/zeta-desktop-architecture.md",
    "title": "zeta Electron Desktop 架构与协作边界",
    "description": "zeta 是 Zeta 的 Electron 产品界面和平台宿主：它负责窗口、交互和系统能力，只投影后端状态， 不复制 Agent、权限或持久化规则。",
    "group": "开始了解",
    "markdown": "> 负责人：Desktop 开发者\n> Rust 对接负责人：zeta-rs 开发者\n> 当前开发基线：[`zeta-app-server-api.md`](zeta-app-server-api.md)\n> 产品装配与构建版本：[`product-editions.md`](product-editions.md)\n> Renderer 控件、Workbench Part 与 CSS 状态所有权：[`ui-styling-ownership.md`](ui-styling-ownership.md)\n> Pane-like Part 的标题槽位、CompositeBar、命名与生命周期：[`workbench-pane-composite-design.md`](workbench-pane-composite-design.md)\n> Renderer Command、MenuId 与 UI Action 组合系统：[`menu-system.md`](menu-system.md)\n> Chat 内 Session Inspector 的信息架构与 Plan 演进：[`chat-session-inspector.md`](chat-session-inspector.md)\n> 外部 Agent Skill 来源与加载边界：[`skills.md`](skills.md)\n> Agent 自定义对象、`.zeta` 与外部导入边界：[`agent-customizations.md`](agent-customizations.md)\n> TUI 明确不提供外部 Agent 导入入口：[`tui.md`](tui.md)\n> 三条公开产品线与宿主边界：[`product-lines.md`](product-lines.md)\n\n## 快速理解\n\n`zeta` 是 Zeta 的 Electron 产品界面和平台宿主：它负责窗口、交互和系统能力，只投影后端状态，\n不复制 Agent、权限或持久化规则。\n\n| 用户或开发者需求 | Desktop 负责 | 必须交给后端 |\n| --- | --- | --- |\n| 显示对话、工具和批准状态 | Renderer 组件、交互状态和可访问性 | Session、Thread、Turn 的权威状态 |\n| 启动桌面应用 | Electron Main、Preload、窗口和 App Server 监督 | Agent 生命周期与恢复 |\n| 调用本地产品能力 | 通过类型化 Preload API 和 App Server 客户端 | 领域校验、授权和持久化 |\n| 使用浏览器、终端或系统 UI | 平台桥接、用户可见控制和能力请求 | 是否允许执行的最终决定 |\n| 增加新产品功能 | 界面拥有呈现，App Server 提供类型化能力 | 禁止在 Renderer 中补一套业务状态机 |\n| 断线或后端重启 | 显示连接状态并重新取得快照 | 不根据旧 UI 状态猜测服务端事实 |\n\n## 1. 目标\n\n`zeta` 是 Zeta 的 Electron 富客户端，负责窗口、浏览器、系统能力和 UI，不拥有\nSession、Thread、Turn、ThreadItem、审批策略或持久化状态机。\n\nDesktop 只能通过版本化 App Server API 使用 zeta-rs：\n\n```text\nRenderer\n  → typed Preload API\n  → Electron Main\n  → JSON-RPC / JSONL / stdio\n  → zeta app-server\n```\n\nDesktop 禁止执行 `zeta ask ...` 后解析终端输出，也禁止直接链接 `zeta-core`。\n\n跨客户端的唯一外部门禁、进程内嵌规则和 Core 旁路禁止项以\n[`zeta-app-server-api.md#唯一外部门禁`](zeta-app-server-api.md#唯一外部门禁) 为准；本文件只补充\nElectron 的 Renderer、Preload、Main 和可信 IPC 适配细节。\n\n## 2. Desktop 所有权\n\nDesktop 负责：\n\n- Electron Main、Preload、Renderer；\n- App Server 进程启动、初始化、监督、重启和关闭；\n- 窗口、菜单、快捷键、命令面板；\n- Browser View、Tab、BrowserSession、CDP 和下载；\n- Renderer 纯 UI 状态与服务端状态投影；\n- 宿主权限、导航策略、origin 策略；\n- Desktop 端集成测试。\n\nDesktop 不负责：\n\n- Session、Thread、Turn、ThreadItem、Tool Call 的权威状态；\n- Agent 规划和工具循环；\n- 是否需要审批的业务策略；\n- rollout、SQLite 投影和 Thread writer lease；\n- 模型供应商与长期凭据持久化；\n- Rust 协议 DTO 的定义。\n\n### 2.1 新功能归属判断\n\n不能因为功能从 UI 进入，就把整项功能都归给 Renderer。设计新功能时，按下面的顺序判断并\n拆分职责：\n\n1. 没有 Desktop 时，CLI、TUI 或远程客户端是否仍需要相同语义？如果需要，权威行为和共享\n   contract 属于 Rust，并通过 App Server 暴露。\n2. 功能是否修改权威状态、访问磁盘或网络、执行进程，或者承担权限与安全校验？如果是，它\n   不能只在 Renderer 实现。跨客户端的产品语义归 Rust；Desktop 独有的宿主能力归 Electron\n   Main，并通过窄的 typed Preload API 暴露。\n3. 功能是否只决定如何显示、如何交互，或维护可丢弃且可重建的视图状态？如果是，它属于\n   Renderer。\n4. 如果以上答案跨越多层，就把它实现为纵向功能，不把后端语义复制到前端，也不把 UI 状态\n   塞进 Rust。\n\n前端可以为了即时反馈重复一部分格式校验，但这不替代权威 owner 在可信边界内重新校验。\nRenderer 不能因为已经校验过输入，就获得直接使用 `fs`、网络或任意 IPC/RPC 的权限。\n\n`Files` 按下表拆分所有权；“当前状态”用于区分本阶段实现和后续能力：\nRust primitive 与 model adapter 的实现细节分别见\n[`zeta-rs/file-system/README.md`](../zeta-rs/file-system/README.md) 和\n[`zeta-rs/file-system-tool/README.md`](../zeta-rs/file-system-tool/README.md)。跨平台 Rust\n`file:` URI 的 canonical implementation contract 见\n[`zeta-rs/utils/path-uri/README.md`](../zeta-rs/utils/path-uri/README.md)；该 crate 当前尚未接入\nproduction consumer，因此下表的共享 URI 状态仍为“部分具备”。\n\n| 能力 | Owner | 当前状态 |\n| --- | --- | --- |\n| 文件树渲染、展开、加载态 | Renderer | ✅ 单目录 Explorer 与 Seti 文件图标 |\n| 选中、快捷键、文件打开与编辑 | Renderer | 部分具备：点击 UTF-8 文件进入编辑器；保存与键盘选择尚未完成 |\n| 系统目录选择器 | Electron Main / Preload | ✅ Empty Explorer 选择单目录并重启绑定 workspace |\n| 在原生文件管理器中显示 | Electron Main / Preload | 尚未完成 |\n| 目录枚举、metadata、文件读写与 workspace 边界校验 | Rust / App Server | ✅ `fs/readDirectory`、`fs/getMetadata`、`fs/readFile`、`fs/writeFile` |\n| 重命名、删除 | Rust / App Server | 尚未完成 |\n| workspace 内容搜索执行、取消与结果限额 | Rust / App Server | ✅ connection-owned pull job |\n| 搜索表单、增量结果分组与高亮 | Renderer | ✅ Search contrib |\n| 搜索结果打开文件 | Files / Editor vertical | 尚未完成 |\n| Explorer watcher invalidation 与文件树自动刷新 | Rust authority + Renderer projection | 部分具备：App Server 已发布 root-relative `fs/changed`；Renderer 尚未消费 |\n| 文件位置 identity | 共享 URI contract；Renderer 只维护其视图投影 | 部分具备：单根 URI 映射 |\n| 跨重启的领域 `FileId` 或 `DocumentId` | 拥有该生命周期的 Rust 领域模型 | 尚未完成 |\n| Tab、Pane 等纯 UI 实例 ID | Renderer | 已有 Workbench 基础设施 |\n\n集成终端同样按 UI 与进程 authority 拆分：\n\n| 能力 | Owner | 当前状态 |\n| --- | --- | --- |\n| 每实例 xterm、Tab、输入、焦点和 panel actions | Renderer | ✅ `TerminalViewPane` / `TerminalInstanceWidget` |\n| 实例列表、active instance、输入 batching 与 resize coalescing | Renderer `ITerminalService` | ✅ |\n| Typed IPC 与 App Server DTO adapter | Preload / Electron Main | ✅ exact-shape validation |\n| Terminal ID、workspace binding、输出 ring 与 connection cleanup | Rust / App Server | ✅ connection-owned |\n| PTY/ConPTY spawn、raw bytes、resize 与进程终止 | `zeta-utils-pty` | ✅ |\n| 可信 Shell Profile discovery 与 ID 解析 | Rust / App Server | ✅ 不暴露 executable |\n| 宿主终端环境继承 | Electron Main + Rust / App Server | ✅ 双层 allowlist，凭据变量不进入 App Server 或 PTY |\n| 任意 executable/environment 选择 | 无 | ❌ 当前客户端不能提交 |\n\n文件图标的跨客户端数据契约由\n[`zeta-file-icons`](../zeta-rs/file-icons/README.md) 拥有：crate 内保存 Seti manifest、WOFF\n并提供 Rust resolver。Desktop 在构建前同步运行时资源、直接从 JSON 推导 TypeScript 所需\n结构并负责 DOM glyph 渲染；App Server 不参与图标解析。Rust TUI 当前没有 terminal\nSeti codepoint adapter；`zeta code` 的当前产品要求也不包含该呈现，因此不构成 TUI backlog。\n\n因此，一项完整功能可以具有一条跨层执行路径：\n\n```text\nRenderer component\n  → UI command\n  → typed Preload API\n  → Electron Main\n  → typed App Server method\n  → Rust authority\n```\n\n### 2.2 外部 Agent 配置导入（仅限 Desktop）\n\n外部 Agent 配置导入是 Desktop 专属的用户工作流。当前\n[`zeta-agent-import`](../zeta-rs/agent-import/README.md) 已实现 Codex/Claude 已知路径的\nmetadata-only 检查、canonical containment、symlink 拒绝、确定性 `AgentPathInspection` 和安全诊断；\n它不读取候选正文。Desktop 的目录选择、内容预览、冲突确认、导入进度和撤销入口，以及 App\nServer 的 apply orchestration 仍是计划设计。TUI 不提供对应命令、目录选择器或配置界面。\n\n底层解析、来源身份、安全校验和持久化仍由各 Rust 领域 authority 与 App Server typed\ncontract 拥有，Renderer 不能直接扫描用户主目录或自行解释外部配置。\nZeta 原生 Instructions/Skills/Agents、`.zeta` 命名空间以及 Import 与 source registration 的区别\n由 [`agent-customizations.md`](agent-customizations.md) 统一定义。\n\n| 外部内容 | Desktop 导入行为 | 权威 owner 与安全边界 |\n| --- | --- | --- |\n| Codex 的 `~/.agents/skills` 与 Claude 的 `~/.claude/skills` | 用户明确选择后注册为窄的只读外部来源 | Config authority 保存来源；Skill manager 校验 containment、格式、摘要和来源身份 |\n| 规则或 instruction 文件 | 预览并按明确映射导入；没有 canonical contract 时不可导入 | 对应 instruction/config 领域定义优先级，外部内容不能覆盖系统、开发者或产品策略 |\n| Agent 定义 | 仅在 Agent definition authority 提供 typed import contract 后开放 | Agent/Multi-Agent authority 校验角色、工具请求和生命周期；Desktop 只呈现映射与诊断 |\n| MCP 声明 | 单独展示并要求用户确认，不因导入自动连接或获得凭据 | MCP/config authority 保存声明；连接、网络和凭据继续走各自授权 |\n| 认证文件、密钥、日志和历史记录 | ❌ 不导入 | Desktop 不读取 `~/.codex/auth.json`，也不把整个 `~/.codex` 或 `~/.claude` 注册为可浏览根 |\n\n导入操作只授予已选择且经过规范化的内容根只读访问，并且必须可查询、禁用和移除。它不是\n“以后同类工具都允许”的长期执行批准；导入 Skill 附带的脚本仍通过普通工具、权限与沙箱流程。\n具体来源和激活语义由 [`skills.md`](skills.md) 定义，批准语义由\n[`permissions.md`](permissions.md) 定义。\n\n该功能即使首版很小，也不能整体放入 `zeta-rs/utils`。外部目录识别、格式映射、敏感内容排除和\n配置 mutation 都属于产品领域语义；`zeta-agent-import` 拥有只读发现与计划模型，Desktop 只\n拥有交互，App Server 负责协调，各目标领域负责校验和落库。只有不理解 Codex、Claude、Skill、\nMCP 或 Agent definition 的路径规范化、目录 containment 和文件 identity 原语可以复用\n`zeta-rs/utils/path-utils`、`zeta-rs/utils/path-uri` 等基础 crate。\n\n## 3. 目录边界\n\n```text\ndesktop/\n├── src/\n│   ├── main.ts\n│   ├── bootstrap.ts\n│   └── zeta/\n│       ├── base/\n│       ├── code/\n│       ├── editor/\n│       ├── platform/\n│       ├── product/\n│       └── workbench/\n├── generated/\n│   └── app-server/\n├── package.json\n├── tsconfig.main.json\n├── tsconfig.preload.json\n└── tsconfig.renderer.json\n```\n\n`src/` 根目录属于宿主进程启动侧。`bootstrap.ts` 只配置必须在 Electron `ready`\n之前生效的进程级策略，`main.ts` 在 bootstrap 完成后加载 Zeta 应用入口。\n`src/zeta/` 是产品源码命名空间；其中 `code/electron-main/main.ts` 选择产品并创建\n`ZetaApplication`，`code/electron-main/app.ts` 持有服务、窗口、IPC 与退出生命周期。\n产品功能不得反向进入根 bootstrap。\n\n产品主进程入口同步注册 Electron `ready` 监听器；异步启动链只能从该监听器触发，\n不得在 ESM 顶层等待一个内部再调用 `app.whenReady()` 的 Promise。\n`ZetaApplication.startupAfterReady()` 断言 Ready 前置条件，并在不创建业务窗口的状态下\n完成 App Server gate。gate 成功后才创建 Workbench；主窗口在 `ready-to-show` 前保持隐藏，\n启动过程不创建额外的 splash 窗口。gate 失败时，原生 Retry/Quit 对话框允许 supervisor\n回到 stopped 后重新初始化，或按正常退出生命周期关闭应用。\n\n`desktop/generated/` 由 zeta-rs 协议生成命令更新，不手写 wire DTO。\n生成的 `APP_SERVER_SCHEMA_HASH` 是 bundled Desktop 的 exact-schema 基线；Electron Main\n必须比较 initialize response，hash 不一致时不得创建业务窗口或进入 Ready。\n\n开发态与发布态共享 canonical Zeta package contract。Node 开发组装器\n`desktop/scripts/prepare-dev-package.mjs` 在 `desktop/.tmp/zeta-package` 生成 debug\npackage；它读取 production builder 使用的同一份 runtime lock、校验 archive digest，并且\n只有新 package 完整构建并通过 layout validation 后才替换上一代。它不安装或调用 Python；\nPython builder 只属于显式 release packaging。`appServerExecutablePath()` 在开发态选择该\npackage root，在发布态选择 Electron `resourcesPath`，两者都只启动\n`<package>/bin/zeta[.exe]`。因此 ripgrep、sandbox helper 与 built-in Skills 不依赖开发机\n`PATH`，缺失或 digest 不匹配会在 package preparation 阶段失败，而不是推迟到 App Server\ninitialize gate。\n\n## 4. 主进程\n\nMain 必须：\n\n1. 从应用包内确定的绝对路径启动 `zeta app-server --listen stdio://`；\n2. 使用 `shell: false`，只传递环境变量 allowlist；\n3. 在创建业务 UI 前完成 `initialize`；\n4. 校验 protocol version、schema hash 和 server build；\n5. 将 stdout 仅交给 JSONL 协议解析器；\n6. 对 stderr 做大小限制和 secret 脱敏；\n7. 为启动、初始化、请求和关闭设置 deadline；\n8. 采用有上限的指数退避处理崩溃重启；\n9. 校验每个 Renderer IPC 的 sender、frame URL、origin 和参数；\n10. 持有 Browser Target 与 Resource 的宿主侧所有权。\n\nMain 不把 `ipcRenderer`、`fs`、`child_process`、`webContents` 或任意 JSON-RPC method\n直接暴露给 Renderer。\n\n当前 `ChildProcessJsonlTransport` 将子进程 stream lifecycle 与 JSON-RPC pairing 分开。它在\n积累无限 buffer 前按原始 byte 拒绝超过 1 MiB 的 frame，只接受严格 LF 和有效 UTF-8；\noutbound write 同时等待 callback 与 drain，并限制 pending write 数。child/stdio 任一错误\n都会关闭 transport；stderr 只保留 64 KiB ring，诊断读取时脱敏 credential。`close()` 异步、\n幂等，并在 graceful deadline 后强制终止。`npm run test:main` 覆盖分片 UTF-8、超限 frame、\n非法 framing、backpressure、stderr 和 close。\n\n`JsonRpcPeer` 在 transport 之上负责双向 JSON-RPC envelope、request ID pairing、remote\nerror、timeout/abort、late/unknown/duplicate response、入站 handler cancellation、pending\n上限和 listener 隔离。协议生成器输出 `APP_SERVER_METHODS` 与\n`APP_SERVER_NOTIFICATIONS` typed definitions，Electron Main 通过 `AppServerClient` 使用；\n产品代码不能传任意 method string 或手写 result 泛型。\n\n`AppServerSession` 独占一个 peer，只有 initialize response 同时通过 server identity、\nprotocol version 和 schema hash gate 后才进入 Ready，并保存协商后的 server\ninfo/capabilities。\n\n`AppServerSession` 是 connection lifecycle。它不是 canonical 产品 `Session`，不得保存\n产品 Session membership、lineage 或权威业务状态；Renderer 只维护可丢弃并可 resync 的\nSession/Thread projection。\n`AppServerSupervisor` 只接受绝对 executable、显式 child environment allowlist，并管理\nStopped/Starting/Initializing/Ready/Stopping/Crashed/Restarting 状态、initialize deadline、\n有界指数退避和 crash budget。崩溃会拒绝旧 Session 的 pending request；新 Session 不自动\n重放结果未知的副作用操作。\n\n结构化 IPC router 集中注册有限 channel，并在调用 validator/handler 前同时验证目标\nwebContents、main frame identity 和确切入口 URL。各能力在自己的\n`platform/<capability>/electron-main/*IpcRoutes.ts` 中拥有 channel、exact-shape validator 与\nApp Server method 映射；`platform/app-server/electron-main` 只拥有连接状态、通用 Resource\nroute、Supervisor、Session 与 JSON-RPC transport。通用可信 router 和 exact-shape validation\nprimitive 位于 `platform/ipc/electron-main`，不反向依赖任何产品能力。`code/electron-main/app.ts`\n是这些 route factory 的 composition root。unknown field、错误 enum、空 ID 或畸形 Turn input均\n不会到达 App Server。协议生成 runtime validator 后，应替换这些同形显式 validator 的来源而\n不改变 router 边界。\n\n### 4.1 Workspace 身份与窗口策略\n\n当前实现明确区分两个所有权边界：\n\n- `platform/workspace`（单数）定义一个窗口当前工作区的模型、结构化标识、\n  `WorkbenchState` 和 `IWorkspaceContextService`；\n- `platform/workspaces`（复数）负责解析、识别和管理工作区。当前已实现启动目标解析，\n  最近项目、运行时切换和 Untitled Workspace 尚未实现。\n\nDesktop 在创建窗口前由 `WorkspacesMainService.resolveStartupWorkspace()` 解析一次启动参数，\n并产生不可变的 `IAnyWorkspaceIdentifier`：\n\n- 无项目参数为 `Empty`；\n- 目录参数或 `--folder <path>` 为 `Folder`；\n- `.zeta-workspace` 文件或 `--workspace <path>` 为 `Workspace`。\n\n`resolveWorkspaceOpenTarget()` 只在 Node/Electron Main 中规范化路径、判断文件类型并为\nFolder/Workspace 产生稳定 ID。标识采用 `{ id }`、`{ id, uri }` 或\n`{ id, configPath }` 的结构，不存储重复的 `WorkbenchState` 判别字段。窗口状态策略从标识\n推导状态：`EMPTY` 映射到 `1200 × 800` 默认窗口，`FOLDER` 和 `WORKSPACE` 映射到\n`1440 × 900` 默认窗口。`WindowsStateHandler` 在单个 `windowsState` 记录中持有\n`lastActiveWindow` 和 `openedWindows`；每个窗口使用 `workspaceIdentifier`、`folder` 或\n`backupPath` 绑定其 UI state。恢复时先匹配具体 Workspace/Folder/空窗口备份，再回退到\nlast-active state，最后才使用默认尺寸。旧的 `windowState` 与 `windowState.empty` 键不会迁移\n或读取。\n\nRenderer 通过受信 IPC route 和 `workspace.getWorkspace()` 读取该身份，并在\n`parseWorkspaceIdentifier()` 校验和恢复 URI。`WorkspaceContextService` 根据该标识构造当前\n`IWorkspace`，并从 `configuration` 或单根 `folders` 推导 `WorkbenchState`。Workbench\ncontribution 不得通过该服务直接访问文件系统。单根 Folder 启动时，Electron Main 将该根\n配置给 App Server；Renderer 的 `BrowserFileService` 只把 workspace URI 映射成根相对路径，\n目录枚举、metadata、有界原子写入、filesystem invalidation 与最终边界授权由 Rust / App Server\n完成。Workspace 内容搜索通过独立的\n`workspace/search/start|read|cancel` contract 接入；其 ownership 与限制见\n[`search.md`](search.md)。Desktop 的保存命令、dirty state、watcher 消费、多根 Workspace 与\n搜索结果打开仍未实现。\n\n当前限制：\n\n- Workspace 身份只在启动时确定，尚无运行时打开、关闭或切换项目流程；\n- `.zeta-workspace` 当前只作为窗口身份，尚未定义或解析其内容；\n- 普通单文件参数仍属于空窗口，文件编辑器尚未实现；\n- Explorer 当前仅支持单根 Folder 的按需读取；后端已有 `fs/writeFile` 与 `fs/changed`，但\n  Renderer 尚未接入保存、自动刷新、选择模型或键盘导航。Search contrib 已能展示单根\n  workspace 内容结果，但尚不能打开文件；\n- 当前 `WorkspacesMainService` 只负责启动目标解析，最近项目、多窗口创建和 workspace\n  配置管理尚未实现；`windowsState` 已保留多窗口恢复数据形状，但当前只写入单个主窗口；\n- 空窗口 backup service 尚未实现，因此当前启动路径没有可传给 `WindowsStateHandler` 的\n  `backupPath`，无备份的空窗口只能使用 last-active fallback；\n- 启动目标无效时记录错误并安全回退到空窗口。\n\n## 5. 沙箱桥接与 Renderer API\n\nElectron sandbox 边界分为两层。`ISandboxGlobals` 是 preload 唯一暴露到主世界的底层桥接：\n它只包含只读进程元数据，以及受 `zeta:` 频道前缀约束的 `invoke` / `on`。preload 必须保持\n自包含，运行时除 `electron` 外不得加载任何模块，也不得把 Electron event 对象传给 Renderer。\n构建后的 preload 由 `verify-sandbox-preload.mjs` 检查这一约束。\n\n`createElectronRendererApi()` 是该桥接的唯一产品适配器。它在普通 Renderer bundle 中引用频道\n常量，并组装领域化、强类型、可枚举的 `ZetaElectronRendererApi`。跨宿主领域能力由其父接口\n`IRendererHost` 定义，Electron 专属能力保持以下精确形状：\n\n```ts\ninterface ZetaElectronRendererApi extends IRendererHost {\n  readonly environment: IRuntimeEnvironment;\n  readonly browserView: IBrowserViewApi;\n  readonly configuration: IConfigurationApi;\n  readonly keybindings: IKeybindingsResourceApi;\n  readonly nativeContextMenu: INativeContextMenuApi;\n  readonly nativeMenubar: INativeMenubarApi;\n  readonly workspace: IWorkspaceContextApi;\n}\n```\n\nWorkbench composition root 是聚合 `IRendererHost` 的唯一产品消费者：它把每个 transport\ncapability 注入对应的领域 Service。Contribution 只能依赖 `IChatService`、`IGitService`、\n`IWorkspaceSearchService`、`ITerminalService` 等前端契约和前端自有领域类型，不能取得整个\nRenderer Host，也不能导入生成 DTO。所有产品代码禁止直接导入 sandbox globals，并禁止提供\n绕过领域 capability 的通用 App Server 调用：\n\n```ts\nexecute(method: string, params?: unknown): Promise<unknown>\n```\n\n## 6. Renderer\n\nRenderer 负责 Command Registry、路由、组件、输入框、虚拟列表和状态投影。\n\n```text\nbutton / menu / shortcut\n  → UI Command\n  → typed renderer API\n  → sandbox IPC bridge\n  → trusted IPC route\n  → domain RPC\n```\n\nRenderer 不复制 Rust 状态机。遇到 durable `sequence` 或 `streamCursor` 空洞时，停止合并\n当前实体，并通过 `session/subscribe` 或带 Session scope 的 `session/thread/subscribe` 获取权威 snapshot + gap。\n\n### 6.1 Editor 宿主\n\n`EditorPart` 是 Workbench 中央编辑区域的唯一宿主。`EditorInput` 表示待打开资源；\n`IEditorPane` 定义编辑器真正共享的创建、输入、取消、布局、可见性、聚焦与释放语义；\n`EditorPaneRegistry` 负责默认匹配、候选枚举和显式编辑器选择。具体产品装配规则由\n[`product-editions.md`](product-editions.md) 负责。\n\n打开新输入时，旧 pane 保持可见，直到新 pane 的异步 `setInput()` 成功。失败不会破坏当前\n编辑器；被后续打开或普通内容替代时，宿主中止 `AbortSignal` 并释放候选 pane。成功切换后由\n宿主隐藏、清空并释放旧 pane。当前只实现单活动 pane，尚无 tab、文档模型、脏状态、保存、\n备份或恢复协议。\n\n### 6.2 iframe Webview\n\n当前 `WebviewElement` 是 Renderer 内用于受控 HTML 的可释放组件，并暴露可由宿主挂载的\niframe 元素。它适合 Markdown Preview、产品内 HTML 面板和后续自定义编辑器，不负责完整\n网页浏览、导航历史、Cookie、CDP 或 Agent Browser Target；后者属于第 7 节的\n`WebContentsView` 能力。\n\n`WebviewElement` 创建 `srcdoc` iframe，并固定以下边界：\n\n```text\nsandbox: allow-scripts\n无 allow-same-origin / forms / popups / downloads / top-navigation\nopaque origin + credentialless\n固定 iframe CSP 与 document CSP\n无 connect / nested frame / object / form action\n无 Electron preload、Zeta renderer API 或 Node capability\n```\n\n内容通过 `acquireZetaWebviewApi().postMessage()` 发送 structured-clone 数据。宿主只接收\n`event.source === iframe.contentWindow` 且实例 channel 匹配的 envelope；宿主向 iframe\n发送消息时因为 opaque origin 必须使用 `targetOrigin: \"*\"`，iframe 内容因此有义务检查\n`event.source === parent`。\n\n当前实现只拥有 DOM sandbox、HTML replacement、focus、双向 message 与 deterministic\ndisposal。扩展宿主、独立 origin endpoint、远程/本地资源映射、端口映射、find widget、\nstate persistence 和权限扩展均尚未实现。引入这些能力时必须保留独立 origin，不能通过加入\n`allow-same-origin` 来绕过资源加载问题。当前也尚未接管 iframe 自身的页面跳转；在加入链接\n打开策略前，调用方只应提供产品控制的 HTML。\n\n### 6.3 Markdown\n\n当前 Renderer 有两条 Markdown 渲染路径，但共享同一个最终安全边界：\n\n```text\nWorkbench 短内容\n  → marked\n  → DOMPurify allowlist\n  → MarkdownElement（普通 DOM）\n\n完整文档预览\n  → markdown-it\n  → DOMPurify allowlist\n  → MarkdownPreview\n  → WebviewElement（opaque-origin sandbox iframe）\n```\n\n`base/browser/domSanitize.ts` 是 DOMPurify 的唯一直接适配器，为目标 document 创建隔离的\nsanitizer 实例，防止 hook 跨窗口或跨消费者泄漏。`base/browser/markdownRenderer.ts` 拥有\n普通 Markdown 组件、Markdown 标签/属性 allowlist 和 URL policy。\n`platform/markdown/browser/markdownPreview.ts` 负责完整文档解析、预览样式及 iframe 链接\n消息桥接。`workbench/contrib/markdown/browser/markdownDocumentRenderer.ts` 再将平台预览\n适配为 Editor Part 可持有的 `MarkdownDocumentView`，并拥有产品级链接打开回调。\n\n`workbench/contrib/markdown/browser/markdown.contribution.ts` 是 Workbench 功能入口，由\n`workbench.contribution.ts` 静态加载；该层只接入产品视图和样式，不重复解析器或 sanitizer。\n解析器返回的 HTML 从不视为可信内容，也不得绕过 DOMPurify 直接写入 DOM 或\n`WebviewElement.setHtml()`。\n\n当前 allowlist 覆盖标题、段落、列表、表格、代码块、引用和任务复选框等标准 Markdown\n结构，拒绝脚本、事件属性、内联样式、SVG/MathML 与未知元素。链接只保留 `http:`、\n`https:` 和页内 fragment，并由宿主接管点击；图片只保留 base64 PNG、JPEG、GIF 和 WebP，\n不会直接读取本地文件或请求远程资源。预览消息仍需通过 `WebviewElement` 的 source/channel\n校验，并在 `MarkdownPreview` 中再次做 exact-shape validation。\n\n当前没有语法高亮、Markdown 扩展插件、Mermaid、KaTeX、工作区相对资源 URI 映射、滚动同步\n或预览状态持久化。这些属于后续能力，加入时必须继续保持“解析后统一 sanitize，再进入隔离\n容器”的顺序。\n\n### 6.4 Workbench 布局\n\n`base/browser/ui/grid/GridView` 是不感知 Workbench 语义的索引路径布局引擎：它用\nbranch/leaf descriptor 创建嵌套 `SplitView`，拥有运行时拓扑、尺寸、显隐和隐藏 leaf\n的 cached visible size，并通过 `GridLocation` 执行 add/remove/move。`Grid` 在其上提供\n以 View identity 为参数的常用 API，Workbench 调用方不持有索引路径。`SerializableGrid`\n通过底层 `GridView` 的 `toJSON()` 与显式 deserializer 生成和恢复完整拓扑快照；这些\nbase 能力不引用 Part、ViewContainer 或其他 Workbench domain。\n\n`workbench/services/layout/browser/layoutService.ts` 拥有面向 contribution 的窗口级布局\n契约、Part identity 和 service identifier。`workbench/browser/layout.ts` 是具体实现，\n拥有合法的 Workbench 拓扑和初始化策略；产品入口通过\n`workbench/browser/workbenchSession.ts` 提供初始 Sidebar、Auxiliary Bar、Agent Sidebar 和\nPanel profile，Workbench 不反向导入产品 contribution。窗口变化由高优先级 Editor 区域吸收，\nPart 即使隐藏也保持挂载，尺寸查询返回其可恢复尺寸。\n\n当前可变尺寸和显隐快照是具体 `WorkbenchLayout` 的私有实现关注点，不是 Layout Service\n契约，也不存在独立的 `layoutState` service。状态流为：\n\n```text\nProduct session profile defaults\n  → initialization state\n  → Profile/Workspace scoped stored values\n  → SerializableGrid runtime\n  → resize / visibility event\n  → onWillSaveState\n  → scoped Storage Service\n```\n\n`platform/storage/common/storage.ts` 定义 Renderer 通用存储契约，包括 Application、\nProfile、Workspace scope，User/Machine target，值变更事件和 will-save lifecycle。\n`workbench/services/storage/browser/storageService.ts` 是浏览器适配器：以产品、profile 和\nworkspace identity 隔离 versioned `localStorage` 文档，周期 flush，并在 `pagehide` 和\nWorkbench 释放前发布 shutdown flush；存储不可用或文档损坏时回退到内存 projection。\n\n具体 Layout 内的私有 `WorkbenchLayoutStateModel` 负责把 domain state 映射为存储 key：\nSidebar、Auxiliary Bar 和 Panel 的尺寸使用 Profile/Machine，显隐使用\nWorkspace/Machine。Layout Service 契约和通用 Storage Service 都不包含这组 key 或状态\nschema。Panel 换边、Sidebar 换边、任意 Part 移动和多窗口拓扑尚未实现，出现真实产品需求\n时应扩展具体 Layout，而不是让 contribution 直接操作 Grid。\n\nRenderer Part 的视觉所有权仍以\n[`ui-styling-ownership.md`](ui-styling-ownership.md) 为准；Grid 只拥有几何和 sash，\n不拥有 Part 内部样式。\n\n### 6.5 Workbench View 与 Chat\n\nWorkbench 使用 `ViewContainerLocation` 区分 Sidebar、Auxiliary Bar、Agent Sidebar 和 Panel。\n这些 pane-like Part 均由 `PaneCompositePart` 持有统一的标题槽位、`CompositeBar`、可选标题\ntoolbar 与 retained `PaneComposite` 生命周期；Editor 保留其专用 editor-group 架构。容器贡献只\n负责声明位置、顺序和默认项，不直接操作 Workbench 布局。`IViewsService` 根据 view ID 解析所属\ncontainer，再委托对应 Part 显示并激活该 composite。Primary Sidebar 和 Panel 显示标准\n`CompositeBar`；Auxiliary Bar 隐藏固定 Chat container 的冗余 bar 并投影 Chat 自有标题；Agent\nSidebar 保留统一标题和 CompositeBar host，但过滤唯一冗余 container item，并在标题右侧\n`titleActions` 槽位投影收起动作。四者仍使用相同的 Composite 生命周期。该层级、命名和槽位契约以\n[`workbench-pane-composite-design.md`](workbench-pane-composite-design.md) 为准。\n\nChat 是独立 contrib，而不是 Auxiliary Bar 的内建内容：\n\n```text\nOpen Chat / New Chat\n  → IViewsService\n  → AuxiliarybarPart\n  → Chat ViewContainer\n  → ChatViewPane\n\nIWorkbenchSessionService\n  → 当前 session / root thread\n  → IChatService\n  → ChatService\n  → thread.subscribe + thread/stream 事件\n  → ChatPaneModel\n  → ChatViewPane\n```\n\n`IWorkbenchSessionService` 负责恢复和切换当前 session/thread；`IChatService` 隔离 model、\nthread、turn 和 App Server lifecycle transport，并把生成 DTO 映射为前端领域类型；`ChatPaneModel` 负责单个活动\nthread 的可释放订阅、已提交 transcript 与临时 stream projection。重新连接或 stream 序号\n不连续时，以 `thread.read` 返回的权威状态重建展示。`ChatViewPane` 当前支持文本发送、中断、\n审批、用户输入请求和经过统一 sanitizer 的 Markdown 展示。\n\n当前尚未实现 session/thread picker、附件和图片输入、fork/history 导航、动态工具执行器。\n由于 session 列表当前没有最近活动时间，启动时只能按服务端顺序选择首个活动 thread；\nBrowser 入口没有 App Server 连接时会明确显示不可用状态。`dev:web` 是不构建 Rust 的独立\n前端开发入口，使用同一 disconnected API 保持 UI 可检查，但不声称拥有后端能力。当前本地\n`dev:web:full` 是已实现的开发例外：Vite host 为每个 HMR WebSocket client 启动一个 stdio\nApp Server，并把 transport-neutral JSON-RPC 帧投影成同一份 `IRendererHost`。该 bridge 只监听\nloopback、复用 Vite WebSocket\ntoken 与同源检查，client 断开后回收其子进程；它不构成生产远程 Web transport。静态 Browser\n构建仍使用 disconnected API，生产 HTTP/WebSocket listener、认证、origin policy 和部署服务\n仍是当前限制。\n\n### 6.6 集成终端\n\nTerminal contribution 只依赖 Workbench service layer 的 `ITerminalService`。实例管理、输入\nbatching、resize coalescing 和 polling 由 `TerminalService` 负责；process contract 位于\nplatform layer 的 `ITerminalProcessService`。`IRendererHost` 直接提供该领域契约；Electron、\nVite development 和 disconnected runtime 分别实现它，wire DTO 只出现在对应 runtime\nimplementation 内。Contribution 和 xterm view 都不直接调用 `IRendererHost`：\n\n```text\nTerminalViewPane / xterm\n  → ITerminalService\n  → TerminalService (Renderer)\n  → ITerminalProcessService\n  → ElectronTerminalProcessService + IAppServerApi\n  → trusted Electron IPC\n  → platform/terminal/electron-main/terminalIpcRoutes\n  → terminal/* App Server methods\n  → TerminalService (Rust)\n  → zeta-utils-pty\n```\n\nSCM 同样通过 `IGitService → GitService → IGitApi` 访问仓库，并由 Service 把 status notification\n和 reconnect lifecycle 投影成前端事件；Search 通过\n`IWorkspaceSearchService → BrowserWorkspaceSearchService → IWorkspaceSearchApi` 消费有界批次。\n两者的 contrib 都不接触 App Server notification union 或生成 DTO。\n\nTerminal title actions 通过 `MenuId.TerminalTitle`、Context Key 与\n`MenuWorkbenchToolBar` 接入 MenuService；profile selector 仍由 Terminal 自定义 action view\nitem 呈现。Command/Menu/Toolbar 的分层以 [`menu-system.md`](menu-system.md) 为准。\n\n当前输出采用 `terminal/read` bounded polling，而不是 `terminal/output` notification。这是现有\n同步 JSONL loop 的明确限制；前端 service 将 pull 转成 `onDidWriteData` 事件，因此 future\ntransport 支持主动、有背压的 stream 后，Workbench caller 不需要改变。Renderer 对输入做 8 ms\nbatch，对 resize 做 microtask coalescing；Rust 仍重新校验输入 byte limit、rows/cols、owner 和\noutput cursor。\n\nTerminal 当前只在单根 workspace composition 中可用；空窗口会显示 terminal service\nunavailable。PTY\n不跨 App Server crash 恢复。每个实例拥有独立 xterm widget，Tab 切换或 Panel 隐藏不会丢失\n窗口生命周期内的 scrollback 与 ANSI parser 状态；Profile picker 只提交 App Server 已列出的\n稳定 ID。Supervisor 离开 ready 后，运行实例进入 `disconnected`；恢复 ready 后用户可以显式\nRelaunch，新 PTY 使用原 Profile，但不会重放未确认输入或冒充旧进程。当前尚无 shell\nintegration、跨进程 reconnection attach 或跨应用重启的持久 scrollback。\n\n## 7. 浏览器能力\n\nElectron Main 是 Browser Target 的唯一权威持有者。\n\n### 7.1 当前实现\n\n`BrowserViewMainService` 为每个目标创建一个 Electron `WebContentsView`，将其挂载到所属\n`BrowserWindow.contentView`，并在目标关闭或窗口释放时移除并关闭 `webContents`。新目标默认\n隐藏；Renderer 必须先通过 `browserView.layout()` 提交窗口内容坐标，再通过\n`browserView.setVisibility()` 显示。\n\n浏览器目标当前同时服务于界面操作和 Agent 语义工具，但两条入口共享同一个 Electron Main\n目标权威源：\n\n| 场景 | 入口 | 执行路径 | 权威边界 |\n| --- | --- | --- | --- |\n| Workbench 创建、布局和导航嵌入页面 | Renderer 的 `browserView` API | 可信 IPC → `BrowserViewMainService` | Electron Main 持有目标和原生对象 |\n| Agent 观察页面或执行输入 | Rust 内置浏览器工具 | `BrowserHost` → 反向 JSON-RPC → `BrowserAutomationMainService` | Rust 决定批准；Electron Main 执行语义动作 |\n| 截取页面 | `browser_screenshot` 或观察选项 | Electron `capturePage()` → App Server `ResourceStore` | 图片按连接隔离，响应只返回资源引用 |\n| App Server 断开或重启 | Supervisor 状态迁移 | `BrowserAutomationMainService.reset()` | 只关闭该宿主能力创建的目标 |\n\n```mermaid\nflowchart LR\n    Tool[Agent browser Tool] --> Policy[Rust Tool policy]\n    Policy --> Host[BrowserHost]\n    Host -->|browser/create, observe, perform, close| Peer[Desktop JSON-RPC peer]\n    Peer --> Automation[BrowserAutomationMainService]\n    Automation --> Registry[BrowserTargetRegistry]\n    Registry --> View[BrowserViewMainService]\n    View --> WebContents[WebContentsView]\n    Automation -->|bounded semantic CDP| WebContents\n    Automation -->|PNG response| Peer\n    Host -->|validated PNG| Resource[connection-owned ResourceStore]\n```\n\n`platform/browser/common/browserView.ts` 拥有可序列化 DTO、频道和输入 validator。\n`browserViewIpcRoutes()` 只做受信 IPC 绑定，`BrowserViewMainService` 拥有 target map、原生 view、\nsession 安全策略、导航历史和事件投影。`WebContentsView`、`WebContents`、Electron event 与\nsession 对象均不得跨越 IPC。`BrowserTargetRegistry` 只在 Electron Main 中把精确\n`targetId` 映射到仍存活的原生目标；它不选择活动标签，也不向 Renderer 暴露 CDP。\n\n当前 URL policy 允许 HTTPS、loopback HTTP 与精确的 `about:blank`，拒绝 URL credentials、\n`file:`、`javascript:` 和其他特权 scheme。每个目标使用独立临时 partition，并固定：\n\n```text\nnodeIntegration: false\ncontextIsolation: true\nsandbox: true\nwebviewTag: false\n无远程页面 preload\n默认拒绝 permission / device permission / download / popup\n```\n\npopup 请求只以 `openRequested` 事件返回已验证 URL，不会由远程页面直接创建窗口。Renderer\n可收到目标 state、加载失败、popup 请求、renderer 崩溃和关闭事件，但不能获得底层 Electron\n对象。\n\nDesktop 在 `initialize` 中声明浏览器宿主能力版本 1，并注册四个 Server → Client 请求：\n\n- `browser/create` 创建隔离且默认隐藏的目标；\n- `browser/observe` 返回 URL、标题、加载状态，以及可选的 accessibility tree、DOM snapshot 和\n  PNG 截图；\n- `browser/perform` 只接受导航、按后端 DOM node ID 点击/输入、滚动、后退和刷新；\n- `browser/close` 只关闭请求中的精确目标。\n\nElectron Main 直接使用 Electron 自带的 Node runtime 和 `webContents.debugger`；没有 Node sidecar，\n也没有 localhost remote-debugging port。自动化层只发出固定的 Accessibility、DOM、Runtime 和\nInput CDP 命令，不接受任意 CDP method。accessibility tree 与 DOM snapshot 各限制为 8 MiB，PNG\n限制为 16 MiB；Debugger 操作按目标串行，并在每个可安全停止的边界观察取消。\n\nRust `BrowserHost` 使用独立的字符串 request ID 复用现有 JSONL 连接，把新目标绑定到实际响应的\nDesktop connection。后续观察、动作和关闭只路由给这一 owner；非 owner 响应、目标身份变化和\n重复响应均失败。请求在 30 秒后超时，取消或超时会发送 `$/cancelRequest`，安全忽略已放弃请求的\n晚到终态。截图经 Base64 长度、PNG MIME/signature 校验后进入 5 分钟 TTL 的 connection-owned\n`ResourceStore`。\n\n内置工具为 `browser_open`、`browser_observe`、`browser_navigate`、`browser_click`、\n`browser_type`、`browser_scroll`、`browser_back`、`browser_reload`、`browser_screenshot` 和\n`browser_close`。Rust 重新执行 URL、目标与 node ID 校验，并把每次动作建模为\n`BrowserInteraction` + `UserInterface` capability；当前策略要求一次性用户批准，Electron Main\n不能自行放宽。完整浏览器工具面只有在工作区可信且至少一个 version 1 connection 同时声明\n`observe + input` 时才进入当前 Tool generation；工作区进入 Restricted 状态、信任被撤销或最后\n一个完整宿主断开时会原子移除。\n反向 RPC handler 可以继续注册，但 Agent 无法从没有上述双重授权的工作区发起动作。\n\n### 7.2 当前限制与计划演进\n\n当前限制：\n\n- 尚无浏览器编辑器、地址栏、标签页或 DOM 容器自动布局绑定；\n- 尚未实现持久 BrowserSession、下载 UI、权限提示、证书信任或 PDF 导出；\n- 当前观察结果是有界的原始 CDP JSON，还没有 Playwright locator、ARIA snapshot、trace、console\n  或 network inspection；\n- Electron Debugger 的单条在途命令不能被 Chromium 抢占；取消会阻止后续步骤并使 Rust 调用\n  终止，但底层命令仍可能在目标关闭前完成；\n- 目标只存在于当前 Desktop 窗口和 App Server 连接生命周期，不跨应用重启恢复。\n\n下一阶段可以在 Electron Main 内增加 `CDPBrowserProxy`，通过 Playwright\n`connectOverCDP` 的进程内 transport 驱动现有 `WebContentsView`。这一阶段仍不得新增 Node\nsidecar、调试端口或把任意 CDP 暴露给 Rust。PDF、下载、trace、network、console 和高级 locator\n属于后续独立契约，当前不能描述为已完成能力。\n\n## 8. Desktop 提交 App Server 能力需求\n\nDesktop 开发者在实现前提交一份符合\n[`zeta-api-interface-requirements.md`](zeta-api-interface-requirements.md) 的产品接口需求。\nDesktop 是需求提出方；zeta-rs 是已接受 App Server 契约的 owner。接口必须同时评估 CLI、\ndaemon 和远程客户端影响，不能定义为 Desktop 私有业务 API。\n\n文档必须覆盖：\n\n- Client → Server 方法；\n- Server → Client 请求；\n- Server → Client 通知；\n- Resource RPC；\n- Browser Target 生命周期；\n- 错误码、超时、取消、幂等和顺序；\n- 每个请求、成功响应和错误响应的 JSON fixture。\n\nzeta-rs 开发者根据该文档实现 Rust DTO、dispatcher、typed client、handler、schema 和\nTypeScript 生成。进程内 CLI client 与 Desktop stdio client 必须经过同一个 dispatcher。\n\n当前已接受的方法、通知、错误码和前端可开发范围以\n[`zeta-app-server-api.md`](zeta-app-server-api.md) 为准。\n\n## 9. Rust 交付给 Desktop 的产物\n\n每次协议交付至少包含：\n\n- 可运行的 `zeta` 二进制；\n- `zeta app-server --listen stdio://`；\n- `zeta-rs/app-server-protocol/schema/types.ts`；\n- `zeta-rs/app-server-protocol/schema/schema.json`；\n- schema hash；\n- 当前 schema fixtures；\n- Rust contract tests；\n- API 变更说明。\n\n## 10. Desktop 验收\n\nDesktop 完成的最低证据：\n\n- TypeScript strict build 通过；\n- initialize 成功并校验 schema hash；\n- Session 创建、Thread 创建/fork、订阅恢复和 Turn 中断端到端通过；\n- 通知能从 App Server 到 Renderer；\n- 未生成或参数错误的 IPC 被拒绝；\n- 不可信网页无法访问应用 IPC；\n- Browser Target 关闭后不会操作其他 Tab；\n- App Server 崩溃、重启和 graceful shutdown 有测试。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-目标",
        "title": "1. 目标"
      },
      {
        "depth": 2,
        "id": "2-desktop-所有权",
        "title": "2. Desktop 所有权"
      },
      {
        "depth": 3,
        "id": "21-新功能归属判断",
        "title": "2.1 新功能归属判断"
      },
      {
        "depth": 3,
        "id": "22-外部-agent-配置导入仅限-desktop",
        "title": "2.2 外部 Agent 配置导入（仅限 Desktop）"
      },
      {
        "depth": 2,
        "id": "3-目录边界",
        "title": "3. 目录边界"
      },
      {
        "depth": 2,
        "id": "4-主进程",
        "title": "4. 主进程"
      },
      {
        "depth": 3,
        "id": "41-workspace-身份与窗口策略",
        "title": "4.1 Workspace 身份与窗口策略"
      },
      {
        "depth": 2,
        "id": "5-沙箱桥接与-renderer-api",
        "title": "5. 沙箱桥接与 Renderer API"
      },
      {
        "depth": 2,
        "id": "6-renderer",
        "title": "6. Renderer"
      },
      {
        "depth": 3,
        "id": "61-editor-宿主",
        "title": "6.1 Editor 宿主"
      },
      {
        "depth": 3,
        "id": "62-iframe-webview",
        "title": "6.2 iframe Webview"
      },
      {
        "depth": 3,
        "id": "63-markdown",
        "title": "6.3 Markdown"
      },
      {
        "depth": 3,
        "id": "64-workbench-布局",
        "title": "6.4 Workbench 布局"
      },
      {
        "depth": 3,
        "id": "65-workbench-view-与-chat",
        "title": "6.5 Workbench View 与 Chat"
      },
      {
        "depth": 3,
        "id": "66-集成终端",
        "title": "6.6 集成终端"
      },
      {
        "depth": 2,
        "id": "7-浏览器能力",
        "title": "7. 浏览器能力"
      },
      {
        "depth": 3,
        "id": "71-当前实现",
        "title": "7.1 当前实现"
      },
      {
        "depth": 3,
        "id": "72-当前限制与计划演进",
        "title": "7.2 当前限制与计划演进"
      },
      {
        "depth": 2,
        "id": "8-desktop-提交-app-server-能力需求",
        "title": "8. Desktop 提交 App Server 能力需求"
      },
      {
        "depth": 2,
        "id": "9-rust-交付给-desktop-的产物",
        "title": "9. Rust 交付给 Desktop 的产物"
      },
      {
        "depth": 2,
        "id": "10-desktop-验收",
        "title": "10. Desktop 验收"
      }
    ],
    "searchText": " 负责人：Desktop 开发者 Rust 对接负责人：zeta-rs 开发者 当前开发基线：zeta-app-server-api.md 产品装配与构建版本：product-editions.md Renderer 控件、Workbench Part 与 CSS 状态所有权：ui-styling-ownership.md Pane-like Part 的标题槽位、CompositeBar、命名与生命周期：workbench-pane-composite-design.md Renderer Command、MenuId 与 UI Action 组合系统：menu-system.md Chat 内 Session Inspector 的信息架构与 Plan 演进：chat-session-inspector.md 外部 Agent Skill 来源与加载边界：skills.md Agent 自定义对象、.zeta 与外部导入边界：agent-customizations.md TUI 明确不提供外部 Agent 导入入口：tui.md 三条公开产品线与宿主边界：product-lines.md 快速理解 zeta 是 Zeta 的 Electron 产品界面和平台宿主：它负责窗口、交互和系统能力，只投影后端状态， 不复制 Agent、权限或持久化规则。 用户或开发者需求 Desktop 负责 必须交给后端 --- --- --- 显示对话、工具和批准状态 Renderer 组件、交互状态和可访问性 Session、Thread、Turn 的权威状态 启动桌面应用 Electron Main、Preload、窗口和 App Server 监督 Agent 生命周期与恢复 调用本地产品能力 通过类型化 Preload API 和 App Server 客户端 领域校验、授权和持久化 使用浏览器、终端或系统 UI 平台桥接、用户可见控制和能力请求 是否允许执行的最终决定 增加新产品功能 界面拥有呈现，App Server 提供类型化能力 禁止在 Renderer 中补一套业务状态机 断线或后端重启 显示连接状态并重新取得快照 不根据旧 UI 状态猜测服务端事实 1. 目标 zeta 是 Zeta 的 Electron 富客户端，负责窗口、浏览器、系统能力和 UI，不拥有 Session、Thread、Turn、ThreadItem、审批策略或持久化状态机。 Desktop 只能通过版本化 App Server API 使用 zeta-rs： ``text Renderer → typed Preload API → Electron Main → JSON-RPC / JSONL / stdio → zeta app-server ` Desktop 禁止执行 zeta ask ... 后解析终端输出，也禁止直接链接 zeta-core。 跨客户端的唯一外部门禁、进程内嵌规则和 Core 旁路禁止项以 zeta-app-server-api.md 唯一外部门禁 为准；本文件只补充 Electron 的 Renderer、Preload、Main 和可信 IPC 适配细节。 2. Desktop 所有权 Desktop 负责： - Electron Main、Preload、Renderer； - App Server 进程启动、初始化、监督、重启和关闭； - 窗口、菜单、快捷键、命令面板； - Browser View、Tab、BrowserSession、CDP 和下载； - Renderer 纯 UI 状态与服务端状态投影； - 宿主权限、导航策略、origin 策略； - Desktop 端集成测试。 Desktop 不负责： - Session、Thread、Turn、ThreadItem、Tool Call 的权威状态； - Agent 规划和工具循环； - 是否需要审批的业务策略； - rollout、SQLite 投影和 Thread writer lease； - 模型供应商与长期凭据持久化； - Rust 协议 DTO 的定义。 2.1 新功能归属判断 不能因为功能从 UI 进入，就把整项功能都归给 Renderer。设计新功能时，按下面的顺序判断并 拆分职责： 1. 没有 Desktop 时，CLI、TUI 或远程客户端是否仍需要相同语义？如果需要，权威行为和共享 contract 属于 Rust，并通过 App Server 暴露。 2. 功能是否修改权威状态、访问磁盘或网络、执行进程，或者承担权限与安全校验？如果是，它 不能只在 Renderer 实现。跨客户端的产品语义归 Rust；Desktop 独有的宿主能力归 Electron Main，并通过窄的 typed Preload API 暴露。 3. 功能是否只决定如何显示、如何交互，或维护可丢弃且可重建的视图状态？如果是，它属于 Renderer。 4. 如果以上答案跨越多层，就把它实现为纵向功能，不把后端语义复制到前端，也不把 UI 状态 塞进 Rust。 前端可以为了即时反馈重复一部分格式校验，但这不替代权威 owner 在可信边界内重新校验。 Renderer 不能因为已经校验过输入，就获得直接使用 fs、网络或任意 IPC/RPC 的权限。 Files 按下表拆分所有权；“当前状态”用于区分本阶段实现和后续能力： Rust primitive 与 model adapter 的实现细节分别见 zeta-rs/file-system/README.md 和 zeta-rs/file-system-tool/README.md。跨平台 Rust file: URI 的 canonical implementation contract 见 zeta-rs/utils/path-uri/README.md；该 crate 当前尚未接入 production consumer，因此下表的共享 URI 状态仍为“部分具备”。 能力 Owner 当前状态 --- --- --- 文件树渲染、展开、加载态 Renderer ✅ 单目录 Explorer 与 Seti 文件图标 选中、快捷键、文件打开与编辑 Renderer 部分具备：点击 UTF-8 文件进入编辑器；保存与键盘选择尚未完成 系统目录选择器 Electron Main / Preload ✅ Empty Explorer 选择单目录并重启绑定 workspace 在原生文件管理器中显示 Electron Main / Preload 尚未完成 目录枚举、metadata、文件读写与 workspace 边界校验 Rust / App Server ✅ fs/readDirectory、fs/getMetadata、fs/readFile、fs/writeFile 重命名、删除 Rust / App Server 尚未完成 workspace 内容搜索执行、取消与结果限额 Rust / App Server ✅ connection-owned pull job 搜索表单、增量结果分组与高亮 Renderer ✅ Search contrib 搜索结果打开文件 Files / Editor vertical 尚未完成 Explorer watcher invalidation 与文件树自动刷新 Rust authority + Renderer projection 部分具备：App Server 已发布 root-relative fs/changed；Renderer 尚未消费 文件位置 identity 共享 URI contract；Renderer 只维护其视图投影 部分具备：单根 URI 映射 跨重启的领域 FileId 或 DocumentId 拥有该生命周期的 Rust 领域模型 尚未完成 Tab、Pane 等纯 UI 实例 ID Renderer 已有 Workbench 基础设施 集成终端同样按 UI 与进程 authority 拆分： 能力 Owner 当前状态 --- --- --- 每实例 xterm、Tab、输入、焦点和 panel actions Renderer ✅ TerminalViewPane / TerminalInstanceWidget 实例列表、active instance、输入 batching 与 resize coalescing Renderer ITerminalService ✅ Typed IPC 与 App Server DTO adapter Preload / Electron Main ✅ exact-shape validation Terminal ID、workspace binding、输出 ring 与 connection cleanup Rust / App Server ✅ connection-owned PTY/ConPTY spawn、raw bytes、resize 与进程终止 zeta-utils-pty ✅ 可信 Shell Profile discovery 与 ID 解析 Rust / App Server ✅ 不暴露 executable 宿主终端环境继承 Electron Main + Rust / App Server ✅ 双层 allowlist，凭据变量不进入 App Server 或 PTY 任意 executable/environment 选择 无 ❌ 当前客户端不能提交 文件图标的跨客户端数据契约由 zeta-file-icons 拥有：crate 内保存 Seti manifest、WOFF 并提供 Rust resolver。Desktop 在构建前同步运行时资源、直接从 JSON 推导 TypeScript 所需 结构并负责 DOM glyph 渲染；App Server 不参与图标解析。Rust TUI 当前没有 terminal Seti codepoint adapter；zeta code 的当前产品要求也不包含该呈现，因此不构成 TUI backlog。 因此，一项完整功能可以具有一条跨层执行路径： `text Renderer component → UI command → typed Preload API → Electron Main → typed App Server method → Rust authority ` 2.2 外部 Agent 配置导入（仅限 Desktop） 外部 Agent 配置导入是 Desktop 专属的用户工作流。当前 zeta-agent-import 已实现 Codex/Claude 已知路径的 metadata-only 检查、canonical containment、symlink 拒绝、确定性 AgentPathInspection 和安全诊断； 它不读取候选正文。Desktop 的目录选择、内容预览、冲突确认、导入进度和撤销入口，以及 App Server 的 apply orchestration 仍是计划设计。TUI 不提供对应命令、目录选择器或配置界面。 底层解析、来源身份、安全校验和持久化仍由各 Rust 领域 authority 与 App Server typed contract 拥有，Renderer 不能直接扫描用户主目录或自行解释外部配置。 Zeta 原生 Instructions/Skills/Agents、.zeta 命名空间以及 Import 与 source registration 的区别 由 agent-customizations.md 统一定义。 外部内容 Desktop 导入行为 权威 owner 与安全边界 --- --- --- Codex 的 /.agents/skills 与 Claude 的 /.claude/skills 用户明确选择后注册为窄的只读外部来源 Config authority 保存来源；Skill manager 校验 containment、格式、摘要和来源身份 规则或 instruction 文件 预览并按明确映射导入；没有 canonical contract 时不可导入 对应 instruction/config 领域定义优先级，外部内容不能覆盖系统、开发者或产品策略 Agent 定义 仅在 Agent definition authority 提供 typed import contract 后开放 Agent/Multi-Agent authority 校验角色、工具请求和生命周期；Desktop 只呈现映射与诊断 MCP 声明 单独展示并要求用户确认，不因导入自动连接或获得凭据 MCP/config authority 保存声明；连接、网络和凭据继续走各自授权 认证文件、密钥、日志和历史记录 ❌ 不导入 Desktop 不读取 /.codex/auth.json，也不把整个 /.codex 或 /.claude 注册为可浏览根 导入操作只授予已选择且经过规范化的内容根只读访问，并且必须可查询、禁用和移除。它不是 “以后同类工具都允许”的长期执行批准；导入 Skill 附带的脚本仍通过普通工具、权限与沙箱流程。 具体来源和激活语义由 skills.md 定义，批准语义由 permissions.md 定义。 该功能即使首版很小，也不能整体放入 zeta-rs/utils。外部目录识别、格式映射、敏感内容排除和 配置 mutation 都属于产品领域语义；zeta-agent-import 拥有只读发现与计划模型，Desktop 只 拥有交互，App Server 负责协调，各目标领域负责校验和落库。只有不理解 Codex、Claude、Skill、 MCP 或 Agent definition 的路径规范化、目录 containment 和文件 identity 原语可以复用 zeta-rs/utils/path-utils、zeta-rs/utils/path-uri 等基础 crate。 3. 目录边界 `text desktop/ ├── src/ │ ├── main.ts │ ├── bootstrap.ts │ └── zeta/ │ ├── base/ │ ├── code/ │ ├── editor/ │ ├── platform/ │ ├── product/ │ └── workbench/ ├── generated/ │ └── app-server/ ├── package.json ├── tsconfig.main.json ├── tsconfig.preload.json └── tsconfig.renderer.json ` src/ 根目录属于宿主进程启动侧。bootstrap.ts 只配置必须在 Electron ready 之前生效的进程级策略，main.ts 在 bootstrap 完成后加载 Zeta 应用入口。 src/zeta/ 是产品源码命名空间；其中 code/electron-main/main.ts 选择产品并创建 ZetaApplication，code/electron-main/app.ts 持有服务、窗口、IPC 与退出生命周期。 产品功能不得反向进入根 bootstrap。 产品主进程入口同步注册 Electron ready 监听器；异步启动链只能从该监听器触发， 不得在 ESM 顶层等待一个内部再调用 app.whenReady 的 Promise。 ZetaApplication.startupAfterReady 断言 Ready 前置条件，并在不创建业务窗口的状态下 完成 App Server gate。gate 成功后才创建 Workbench；主窗口在 ready-to-show 前保持隐藏， 启动过程不创建额外的 splash 窗口。gate 失败时，原生 Retry/Quit 对话框允许 supervisor 回到 stopped 后重新初始化，或按正常退出生命周期关闭应用。 desktop/generated/ 由 zeta-rs 协议生成命令更新，不手写 wire DTO。 生成的 APPSERVERSCHEMAHASH 是 bundled Desktop 的 exact-schema 基线；Electron Main 必须比较 initialize response，hash 不一致时不得创建业务窗口或进入 Ready。 开发态与发布态共享 canonical Zeta package contract。Node 开发组装器 desktop/scripts/prepare-dev-package.mjs 在 desktop/.tmp/zeta-package 生成 debug package；它读取 production builder 使用的同一份 runtime lock、校验 archive digest，并且 只有新 package 完整构建并通过 layout validation 后才替换上一代。它不安装或调用 Python； Python builder 只属于显式 release packaging。appServerExecutablePath 在开发态选择该 package root，在发布态选择 Electron resourcesPath，两者都只启动 <package /bin/zeta .exe 。因此 ripgrep、sandbox helper 与 built-in Skills 不依赖开发机 PATH，缺失或 digest 不匹配会在 package preparation 阶段失败，而不是推迟到 App Server initialize gate。 4. 主进程 Main 必须： 1. 从应用包内确定的绝对路径启动 zeta app-server --listen stdio://； 2. 使用 shell: false，只传递环境变量 allowlist； 3. 在创建业务 UI 前完成 initialize； 4. 校验 protocol version、schema hash 和 server build； 5. 将 stdout 仅交给 JSONL 协议解析器； 6. 对 stderr 做大小限制和 secret 脱敏； 7. 为启动、初始化、请求和关闭设置 deadline； 8. 采用有上限的指数退避处理崩溃重启； 9. 校验每个 Renderer IPC 的 sender、frame URL、origin 和参数； 10. 持有 Browser Target 与 Resource 的宿主侧所有权。 Main 不把 ipcRenderer、fs、childprocess、webContents 或任意 JSON-RPC method 直接暴露给 Renderer。 当前 ChildProcessJsonlTransport 将子进程 stream lifecycle 与 JSON-RPC pairing 分开。它在 积累无限 buffer 前按原始 byte 拒绝超过 1 MiB 的 frame，只接受严格 LF 和有效 UTF-8； outbound write 同时等待 callback 与 drain，并限制 pending write 数。child/stdio 任一错误 都会关闭 transport；stderr 只保留 64 KiB ring，诊断读取时脱敏 credential。close 异步、 幂等，并在 graceful deadline 后强制终止。npm run test:main 覆盖分片 UTF-8、超限 frame、 非法 framing、backpressure、stderr 和 close。 JsonRpcPeer 在 transport 之上负责双向 JSON-RPC envelope、request ID pairing、remote error、timeout/abort、late/unknown/duplicate response、入站 handler cancellation、pending 上限和 listener 隔离。协议生成器输出 APPSERVERMETHODS 与 APPSERVERNOTIFICATIONS typed definitions，Electron Main 通过 AppServerClient 使用； 产品代码不能传任意 method string 或手写 result 泛型。 AppServerSession 独占一个 peer，只有 initialize response 同时通过 server identity、 protocol version 和 schema hash gate 后才进入 Ready，并保存协商后的 server info/capabilities。 AppServerSession 是 connection lifecycle。它不是 canonical 产品 Session，不得保存 产品 Session membership、lineage 或权威业务状态；Renderer 只维护可丢弃并可 resync 的 Session/Thread projection。 AppServerSupervisor 只接受绝对 executable、显式 child environment allowlist，并管理 Stopped/Starting/Initializing/Ready/Stopping/Crashed/Restarting 状态、initialize deadline、 有界指数退避和 crash budget。崩溃会拒绝旧 Session 的 pending request；新 Session 不自动 重放结果未知的副作用操作。 结构化 IPC router 集中注册有限 channel，并在调用 validator/handler 前同时验证目标 webContents、main frame identity 和确切入口 URL。各能力在自己的 platform/<capability /electron-main/IpcRoutes.ts 中拥有 channel、exact-shape validator 与 App Server method 映射；platform/app-server/electron-main 只拥有连接状态、通用 Resource route、Supervisor、Session 与 JSON-RPC transport。通用可信 router 和 exact-shape validation primitive 位于 platform/ipc/electron-main，不反向依赖任何产品能力。code/electron-main/app.ts 是这些 route factory 的 composition root。unknown field、错误 enum、空 ID 或畸形 Turn input均 不会到达 App Server。协议生成 runtime validator 后，应替换这些同形显式 validator 的来源而 不改变 router 边界。 4.1 Workspace 身份与窗口策略 当前实现明确区分两个所有权边界： - platform/workspace（单数）定义一个窗口当前工作区的模型、结构化标识、 WorkbenchState 和 IWorkspaceContextService； - platform/workspaces（复数）负责解析、识别和管理工作区。当前已实现启动目标解析， 最近项目、运行时切换和 Untitled Workspace 尚未实现。 Desktop 在创建窗口前由 WorkspacesMainService.resolveStartupWorkspace 解析一次启动参数， 并产生不可变的 IAnyWorkspaceIdentifier： - 无项目参数为 Empty； - 目录参数或 --folder <path 为 Folder； - .zeta-workspace 文件或 --workspace <path 为 Workspace。 resolveWorkspaceOpenTarget 只在 Node/Electron Main 中规范化路径、判断文件类型并为 Folder/Workspace 产生稳定 ID。标识采用 id 、 id, uri 或 id, configPath 的结构，不存储重复的 WorkbenchState 判别字段。窗口状态策略从标识 推导状态：EMPTY 映射到 1200 × 800 默认窗口，FOLDER 和 WORKSPACE 映射到 1440 × 900 默认窗口。WindowsStateHandler 在单个 windowsState 记录中持有 lastActiveWindow 和 openedWindows；每个窗口使用 workspaceIdentifier、folder 或 backupPath 绑定其 UI state。恢复时先匹配具体 Workspace/Folder/空窗口备份，再回退到 last-active state，最后才使用默认尺寸。旧的 windowState 与 windowState.empty 键不会迁移 或读取。 Renderer 通过受信 IPC route 和 workspace.getWorkspace 读取该身份，并在 parseWorkspaceIdentifier 校验和恢复 URI。WorkspaceContextService 根据该标识构造当前 IWorkspace，并从 configuration 或单根 folders 推导 WorkbenchState。Workbench contribution 不得通过该服务直接访问文件系统。单根 Folder 启动时，Electron Main 将该根 配置给 App Server；Renderer 的 BrowserFileService 只把 workspace URI 映射成根相对路径， 目录枚举、metadata、有界原子写入、filesystem invalidation 与最终边界授权由 Rust / App Server 完成。Workspace 内容搜索通过独立的 workspace/search/start read cancel contract 接入；其 ownership 与限制见 search.md。Desktop 的保存命令、dirty state、watcher 消费、多根 Workspace 与 搜索结果打开仍未实现。 当前限制： - Workspace 身份只在启动时确定，尚无运行时打开、关闭或切换项目流程； - .zeta-workspace 当前只作为窗口身份，尚未定义或解析其内容； - 普通单文件参数仍属于空窗口，文件编辑器尚未实现； - Explorer 当前仅支持单根 Folder 的按需读取；后端已有 fs/writeFile 与 fs/changed，但 Renderer 尚未接入保存、自动刷新、选择模型或键盘导航。Search contrib 已能展示单根 workspace 内容结果，但尚不能打开文件； - 当前 WorkspacesMainService 只负责启动目标解析，最近项目、多窗口创建和 workspace 配置管理尚未实现；windowsState 已保留多窗口恢复数据形状，但当前只写入单个主窗口； - 空窗口 backup service 尚未实现，因此当前启动路径没有可传给 WindowsStateHandler 的 backupPath，无备份的空窗口只能使用 last-active fallback； - 启动目标无效时记录错误并安全回退到空窗口。 5. 沙箱桥接与 Renderer API Electron sandbox 边界分为两层。ISandboxGlobals 是 preload 唯一暴露到主世界的底层桥接： 它只包含只读进程元数据，以及受 zeta: 频道前缀约束的 invoke / on。preload 必须保持 自包含，运行时除 electron 外不得加载任何模块，也不得把"
  },
  {
    "slug": "zeta-cli-architecture",
    "sourcePath": "docs/zeta-cli-architecture.md",
    "title": "Zeta CLI 架构与协作边界",
    "description": "zeta code 是 Zeta 的 TUI 产品线；zeta-cli 是它的命令入口和宿主启动器，zeta-tui 负责 交互呈现。CLI 负责命令、输入输出和退出码，但所有 Agent 工作都通过统一的 Session-first App Server 契约执行。",
    "group": "开始了解",
    "markdown": "> 负责人：CLI 开发者  \n> App Server 与 Rust 对接负责人：zeta-rs 开发者  \n> CLI 与 Desktop 共用唯一的 Zeta App Server 产品契约。\n> 当前开发基线：[`zeta-app-server-api.md`](zeta-app-server-api.md)\n> 本地启动与连接基线：[`app-server-client.md`](app-server-client.md)\n> 无交互执行基线：[`exec.md`](exec.md)\n> MCP Agent server：[`mcp-server.md`](mcp-server.md)\n> 三条公开产品线与宿主边界：[`product-lines.md`](product-lines.md)\n\n## 快速理解\n\n`zeta code` 是 Zeta 的 TUI 产品线；`zeta-cli` 是它的命令入口和宿主启动器，`zeta-tui` 负责\n交互呈现。CLI 负责命令、输入输出和退出码，但所有 Agent 工作都通过统一的 Session-first\nApp Server 契约执行。\n\n| 用户命令 | CLI 负责 | 后端负责 |\n| --- | --- | --- |\n| `zeta` | 启动交互式终端体验 | Session、Thread 和 Agent 状态 |\n| `zeta ask` | 解析 prompt 并输出最终 Agent message | 模型、工具、权限和持久化 |\n| `zeta exec` | 提供 new/resume/fork、Human/JSONL 与稳定退出码 | 执行同一 Agent/Tool 路径，不绕过工具系统 |\n| `zeta login` / `zeta config` | 收集用户意图并调用类型化方法 | 登录、秘密和配置权威 |\n| `zeta app-server` / `zeta mcp-server` | 选择进程入口和监听方式 | 对应服务的协议与生命周期 |\n| 连接中断或失败 | 输出稳定错误、诊断和退出码 | 决定领域失败和恢复语义 |\n\n## 1. 目标\n\nCLI 是正式产品客户端。它负责终端体验，不拥有 Agent 状态机，也不建立第二套 Rust 业务\nfacade。\n\n第一版命令：\n\n```bash\nzeta\nzeta ask \"解释当前仓库\"\nzeta exec \"检查测试失败\"\nzeta exec --jsonl --auto-review \"检查测试失败\"\nzeta exec --resume SESSION_ID THREAD_ID \"继续处理\"\nzeta exec --fork SESSION_ID PARENT_THREAD_ID --title \"替代方案\" \"尝试另一种修复\"\nzeta login\nzeta config\nzeta app-server --listen stdio://\nzeta mcp-server\nzeta mcp-server --listen http://127.0.0.1:8787/mcp\n```\n\n`ask` 和 `exec` 当前通过 Session-first API 工作：先创建 Session，再在其中创建 Thread，\n最后启动 Turn 并消费 canonical update。`exec` 是\n非交互 Agent 入口，不是绕过 Agent Tool Executor 的任意 shell 执行器。\n\n## 2. 物理位置与所有权\n\nCLI 保留在同一个根 Rust workspace，但物理上属于 `zeta-code` 产品：\n\n```text\nzeta-code/\n├── cli/                 # zeta binary and product command host\n└── tui/                 # Ratatui presentation shell\n```\n\n其中 App Server、client、protocol、exec 等共享 crate 仍位于 `zeta-rs/`；`zeta-code/` 只拥有\nTUI 产品入口和它的命令宿主。\n\nCLI 开发者负责：\n\n- 参数解析、子命令和帮助；\n- Human、JSON 和 JSONL 输出；\n- TTY 检测、颜色、进度、退出码；\n- TUI 交互和键盘事件；\n- shell completion、安装体验和 CLI 集成测试。\n\nCLI 开发者不负责：\n\n- Session、Thread、Turn、ThreadItem 和 Tool Call 状态机；\n- rollout、SQLite、writer lease；\n- sandbox、审批和工具执行策略；\n- 模型供应商内部实现；\n- App Server wire DTO 的权威定义；\n- Desktop 或 Browser View。\n\n## 3. 唯一产品接口\n\n普通 CLI 路径分为：\n\n```text\nzeta-cli → zeta-tui  → zeta-app-server-client\n         └→ zeta-exec → zeta-app-server-client\n                                ↓\n                       zeta-app-server-protocol\n```\n\nCLI 可以依赖产品层 `zeta-exec` 运行无交互 Agent，但不依赖目标\n`zeta-tool-executor`。CLI/TUI/exec 都不直接调用 `zeta-core`、`zeta-storage`、\n`zeta-rollout`、`zeta-rollout-trace`、`zeta-sandboxing` 或 Model Provider。\n\n`zeta app-server` 子命令可以作为明确的宿主入口依赖 `zeta-app-server`，但不能绕过\ndispatcher 直接调用 Core 用例。\n\n跨客户端的唯一外部门禁和进程内嵌规则以\n[`zeta-app-server-api.md#唯一外部门禁`](zeta-app-server-api.md#唯一外部门禁) 为准；本文件只描述\nCLI、TUI 和 exec 如何接入同一 App Server 契约。\n\n`zeta mcp-server` 是 `zeta-mcp-server` binary 的 CLI 入口。该 crate 依赖\n`zeta-app-server-client`，将外部 MCP tool call 映射到同一 Session/Thread/Turn dispatcher；\nCLI 不复制 MCP framing、Agent polling、interaction 或 invocation identity 逻辑。CLI 只解析\n`stdio://` 或 `http://IP:PORT/PATH` listener；HTTP bearer 由\n`ZETA_MCP_BEARER_TOKEN` 提供，可选 exact Origin 由 `ZETA_MCP_ALLOWED_ORIGIN` 提供。非 loopback\n远程部署必须在 TLS/auth reverse proxy 后运行，CLI 本身不拥有 OAuth 或 tenant provisioning。\n\n禁止新增名为 `runtime`、`service`、`common` 或 `platform` 的泛化 CLI facade 来聚合内部\n能力。这类层会重复 App Server 契约并逐渐成为职责不清的杂物桶。\n\n## 4. 传输模式\n\n所有模式使用相同的 Params、Result、Notification 和 Error DTO：\n\n```text\n默认 CLI/TUI\n  → AppServerSession\n  → request handle + event stream\n  → typed in-process App Server dispatcher\n\nDesktop\n  → JSONL / stdio\n  → 独立 App Server\n\n未来 daemon/remote App Server\n  → App Server Client remote backend\n  → 相同 App Server protocol\n\n未来 remote scheduler\n  → zeta-exec worker adapter\n  → App Server Client\n```\n\n进程内模式可以避免子进程和 JSON 编解码，但仍必须通过同一个 typed client 和 dispatcher，\n不能提供只在进程内可用的隐藏业务方法。当前 `zeta-app-server-client` 的首要职责是为\n`zeta-exec` 和 TUI 启动本地 App Server、完成 initialize、连接 request/event channel 并正确\n关闭。后续 `zeta-exec` 作为远程调度的 headless execution entry；scheduler adapter 仍通过\n这一 owned session 工作，不能建立第二套 Core 或 App Server 私有调用路径。完整 Job/Attempt、\nlease、event cursor 与 remote execution plane 边界见 [`exec.md`](exec.md)。\n\n## 5. 请求与事件\n\nCLI 需要的能力必须先进入 App Server API 文档，再由 zeta-rs 实现：\n\n- `initialize`\n- Session create/read/list/subscribe/lifecycle\n- Session-owned Thread create/fork/archive\n- Thread read/subscribe/unsubscribe\n- Turn start/interrupt\n- SessionUpdate / ThreadUpdate\n- Tool Call proposed/running/completed\n- Approval request/response\n- 文件变更\n- warning\n- Turn completed/failed/interrupted\n\nCLI 不解析日志、stderr 或人类文本来判断状态。\n\n## 6. 输出契约\n\nHuman 输出可以演进；JSON/JSONL 是稳定 CLI 契约，但其事件必须由 App Server typed\nnotification 显式映射。具体 mapping、stdout/stderr 和 scheduler event 规则由\n[`exec.md`](exec.md#7-输出契约) 维护。\n\n```json\n{\n  \"schemaVersion\": 1,\n  \"runId\": \"run-123\",\n  \"event\": {\n    \"type\": \"turnStarted\",\n    \"sessionId\": \"session_123\",\n    \"threadId\": \"thread_123\",\n    \"turnId\": \"turn_456\"\n  }\n}\n```\n\n当前 `--jsonl` 每行输出一个完整 `ExecEvent` 并立即 flush；stderr 只用于诊断。Human 模式只把\n最终 Agent message 写入 stdout。\n\n当前退出码：\n\n```text\n0  成功\n1  一般运行失败\n2  参数错误，或无界面运行需要交互\n75 Turn 已启动但无法确认 canonical terminal outcome\n130 用户中断或 Turn 被中断\n```\n\n## 7. 审批与安全\n\nCLI/TUI 可以展示审批 UI；headless exec 当前提供 deny、automatic review 和显式 bypass 三种模式。\n默认 deny 不会等待不存在的 UI；automatic review 仍由 App Server/Core policy 决定，CLI 不拥有批准\n结论。远程 delegated reviewer 仍是 Proposed。\n\n审批响应必须绑定：\n\n- `approvalRequestId`\n- `threadId`\n- `turnId`\n- `toolCallId`\n- `actionDigest`\n- decision、scope 和 expiry\n\nCLI 不能直接执行 Agent 请求的命令、写文件或网络操作；这些动作必须经过 Rust Tool\nExecutor 和 host policy。目标底层边界名为 `zeta-tool-executor`，不能与产品层\n`zeta-exec` 混用。\n\n## 8. 协作交接\n\nzeta-rs 开发者交付：\n\n- 版本化 `zeta-app-server-protocol`；\n- `zeta-app-server-client` 的本地 App Server owned session、request handle 与 event stream；\n- typed request、notification 和 error；\n- mock transport、fixtures 和 contract tests；\n- error → exit code 建议映射。\n\n`zeta-exec` 当前已交付：\n\n- run-once、new/resume/fork、interrupt 与 terminal outcome；\n- human/JSONL output contract；\n- headless approval handling；\n\n后续由 `zeta-exec` 开发者交付 remote worker 的 Job/Attempt/lease/cursor adapter。\n\nCLI 开发者交付：\n\n- 命令、参数和输出格式文档；\n- 所需 App Server method/notification 清单；\n- 每个命令的输入/输出 fixture；\n- TTY 与非 TTY 行为；\n- approval 交互流程；\n- CLI 集成测试。\n\nCLI 新需求不得通过直接依赖内部 crate 临时解决；先补产品 API 契约。\n\n当前可以实现的 method、notification 和限制以\n[`zeta-app-server-api.md`](zeta-app-server-api.md) 为准。\n\n## 9. 验收\n\n- `ask`、`exec`、`login`、`config` 通过 App Server Client 工作；\n- 默认进程内模式通过 typed channel 经过相同 App Server method dispatcher；\n- `zeta-exec` 与 TUI 共用 App Server 启动、initialize、channel wiring 和 shutdown 实现；\n- 远程调度通过 `zeta-exec` 映射为相同 typed request 与 canonical update；\n- CLI 和 Desktop 对相同请求得到相同 DTO 与错误语义；\n- CLI crate 不直接依赖 Core、Storage、Tool Executor、Sandbox 或 Model Provider；\n- JSON/JSONL stdout 无日志污染；\n- Ctrl-C 发出 `session/request` `InterruptTurn` 并等待终态；\n- TTY、pipe、重定向和非交互环境均有测试。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-目标",
        "title": "1. 目标"
      },
      {
        "depth": 2,
        "id": "2-物理位置与所有权",
        "title": "2. 物理位置与所有权"
      },
      {
        "depth": 2,
        "id": "3-唯一产品接口",
        "title": "3. 唯一产品接口"
      },
      {
        "depth": 2,
        "id": "4-传输模式",
        "title": "4. 传输模式"
      },
      {
        "depth": 2,
        "id": "5-请求与事件",
        "title": "5. 请求与事件"
      },
      {
        "depth": 2,
        "id": "6-输出契约",
        "title": "6. 输出契约"
      },
      {
        "depth": 2,
        "id": "7-审批与安全",
        "title": "7. 审批与安全"
      },
      {
        "depth": 2,
        "id": "8-协作交接",
        "title": "8. 协作交接"
      },
      {
        "depth": 2,
        "id": "9-验收",
        "title": "9. 验收"
      }
    ],
    "searchText": " 负责人：CLI 开发者 App Server 与 Rust 对接负责人：zeta-rs 开发者 CLI 与 Desktop 共用唯一的 Zeta App Server 产品契约。 当前开发基线：zeta-app-server-api.md 本地启动与连接基线：app-server-client.md 无交互执行基线：exec.md MCP Agent server：mcp-server.md 三条公开产品线与宿主边界：product-lines.md 快速理解 zeta code 是 Zeta 的 TUI 产品线；zeta-cli 是它的命令入口和宿主启动器，zeta-tui 负责 交互呈现。CLI 负责命令、输入输出和退出码，但所有 Agent 工作都通过统一的 Session-first App Server 契约执行。 用户命令 CLI 负责 后端负责 --- --- --- zeta 启动交互式终端体验 Session、Thread 和 Agent 状态 zeta ask 解析 prompt 并输出最终 Agent message 模型、工具、权限和持久化 zeta exec 提供 new/resume/fork、Human/JSONL 与稳定退出码 执行同一 Agent/Tool 路径，不绕过工具系统 zeta login / zeta config 收集用户意图并调用类型化方法 登录、秘密和配置权威 zeta app-server / zeta mcp-server 选择进程入口和监听方式 对应服务的协议与生命周期 连接中断或失败 输出稳定错误、诊断和退出码 决定领域失败和恢复语义 1. 目标 CLI 是正式产品客户端。它负责终端体验，不拥有 Agent 状态机，也不建立第二套 Rust 业务 facade。 第一版命令： ``bash zeta zeta ask \"解释当前仓库\" zeta exec \"检查测试失败\" zeta exec --jsonl --auto-review \"检查测试失败\" zeta exec --resume SESSIONID THREADID \"继续处理\" zeta exec --fork SESSIONID PARENTTHREADID --title \"替代方案\" \"尝试另一种修复\" zeta login zeta config zeta app-server --listen stdio:// zeta mcp-server zeta mcp-server --listen http://127.0.0.1:8787/mcp ` ask 和 exec 当前通过 Session-first API 工作：先创建 Session，再在其中创建 Thread， 最后启动 Turn 并消费 canonical update。exec 是 非交互 Agent 入口，不是绕过 Agent Tool Executor 的任意 shell 执行器。 2. 物理位置与所有权 CLI 保留在同一个根 Rust workspace，但物理上属于 zeta-code 产品： `text zeta-code/ ├── cli/ zeta binary and product command host └── tui/ Ratatui presentation shell ` 其中 App Server、client、protocol、exec 等共享 crate 仍位于 zeta-rs/；zeta-code/ 只拥有 TUI 产品入口和它的命令宿主。 CLI 开发者负责： - 参数解析、子命令和帮助； - Human、JSON 和 JSONL 输出； - TTY 检测、颜色、进度、退出码； - TUI 交互和键盘事件； - shell completion、安装体验和 CLI 集成测试。 CLI 开发者不负责： - Session、Thread、Turn、ThreadItem 和 Tool Call 状态机； - rollout、SQLite、writer lease； - sandbox、审批和工具执行策略； - 模型供应商内部实现； - App Server wire DTO 的权威定义； - Desktop 或 Browser View。 3. 唯一产品接口 普通 CLI 路径分为： `text zeta-cli → zeta-tui → zeta-app-server-client └→ zeta-exec → zeta-app-server-client ↓ zeta-app-server-protocol ` CLI 可以依赖产品层 zeta-exec 运行无交互 Agent，但不依赖目标 zeta-tool-executor。CLI/TUI/exec 都不直接调用 zeta-core、zeta-storage、 zeta-rollout、zeta-rollout-trace、zeta-sandboxing 或 Model Provider。 zeta app-server 子命令可以作为明确的宿主入口依赖 zeta-app-server，但不能绕过 dispatcher 直接调用 Core 用例。 跨客户端的唯一外部门禁和进程内嵌规则以 zeta-app-server-api.md 唯一外部门禁 为准；本文件只描述 CLI、TUI 和 exec 如何接入同一 App Server 契约。 zeta mcp-server 是 zeta-mcp-server binary 的 CLI 入口。该 crate 依赖 zeta-app-server-client，将外部 MCP tool call 映射到同一 Session/Thread/Turn dispatcher； CLI 不复制 MCP framing、Agent polling、interaction 或 invocation identity 逻辑。CLI 只解析 stdio:// 或 http://IP:PORT/PATH listener；HTTP bearer 由 ZETAMCPBEARERTOKEN 提供，可选 exact Origin 由 ZETAMCPALLOWEDORIGIN 提供。非 loopback 远程部署必须在 TLS/auth reverse proxy 后运行，CLI 本身不拥有 OAuth 或 tenant provisioning。 禁止新增名为 runtime、service、common 或 platform 的泛化 CLI facade 来聚合内部 能力。这类层会重复 App Server 契约并逐渐成为职责不清的杂物桶。 4. 传输模式 所有模式使用相同的 Params、Result、Notification 和 Error DTO： `text 默认 CLI/TUI → AppServerSession → request handle + event stream → typed in-process App Server dispatcher Desktop → JSONL / stdio → 独立 App Server 未来 daemon/remote App Server → App Server Client remote backend → 相同 App Server protocol 未来 remote scheduler → zeta-exec worker adapter → App Server Client ` 进程内模式可以避免子进程和 JSON 编解码，但仍必须通过同一个 typed client 和 dispatcher， 不能提供只在进程内可用的隐藏业务方法。当前 zeta-app-server-client 的首要职责是为 zeta-exec 和 TUI 启动本地 App Server、完成 initialize、连接 request/event channel 并正确 关闭。后续 zeta-exec 作为远程调度的 headless execution entry；scheduler adapter 仍通过 这一 owned session 工作，不能建立第二套 Core 或 App Server 私有调用路径。完整 Job/Attempt、 lease、event cursor 与 remote execution plane 边界见 exec.md。 5. 请求与事件 CLI 需要的能力必须先进入 App Server API 文档，再由 zeta-rs 实现： - initialize - Session create/read/list/subscribe/lifecycle - Session-owned Thread create/fork/archive - Thread read/subscribe/unsubscribe - Turn start/interrupt - SessionUpdate / ThreadUpdate - Tool Call proposed/running/completed - Approval request/response - 文件变更 - warning - Turn completed/failed/interrupted CLI 不解析日志、stderr 或人类文本来判断状态。 6. 输出契约 Human 输出可以演进；JSON/JSONL 是稳定 CLI 契约，但其事件必须由 App Server typed notification 显式映射。具体 mapping、stdout/stderr 和 scheduler event 规则由 exec.md 维护。 `json \"schemaVersion\": 1, \"runId\": \"run-123\", \"event\": \"type\": \"turnStarted\", \"sessionId\": \"session123\", \"threadId\": \"thread123\", \"turnId\": \"turn456\" ` 当前 --jsonl 每行输出一个完整 ExecEvent 并立即 flush；stderr 只用于诊断。Human 模式只把 最终 Agent message 写入 stdout。 当前退出码： `text 0 成功 1 一般运行失败 2 参数错误，或无界面运行需要交互 75 Turn 已启动但无法确认 canonical terminal outcome 130 用户中断或 Turn 被中断 ` 7. 审批与安全 CLI/TUI 可以展示审批 UI；headless exec 当前提供 deny、automatic review 和显式 bypass 三种模式。 默认 deny 不会等待不存在的 UI；automatic review 仍由 App Server/Core policy 决定，CLI 不拥有批准 结论。远程 delegated reviewer 仍是 Proposed。 审批响应必须绑定： - approvalRequestId - threadId - turnId - toolCallId - actionDigest - decision、scope 和 expiry CLI 不能直接执行 Agent 请求的命令、写文件或网络操作；这些动作必须经过 Rust Tool Executor 和 host policy。目标底层边界名为 zeta-tool-executor，不能与产品层 zeta-exec 混用。 8. 协作交接 zeta-rs 开发者交付： - 版本化 zeta-app-server-protocol； - zeta-app-server-client 的本地 App Server owned session、request handle 与 event stream； - typed request、notification 和 error； - mock transport、fixtures 和 contract tests； - error → exit code 建议映射。 zeta-exec 当前已交付： - run-once、new/resume/fork、interrupt 与 terminal outcome； - human/JSONL output contract； - headless approval handling； 后续由 zeta-exec 开发者交付 remote worker 的 Job/Attempt/lease/cursor adapter。 CLI 开发者交付： - 命令、参数和输出格式文档； - 所需 App Server method/notification 清单； - 每个命令的输入/输出 fixture； - TTY 与非 TTY 行为； - approval 交互流程； - CLI 集成测试。 CLI 新需求不得通过直接依赖内部 crate 临时解决；先补产品 API 契约。 当前可以实现的 method、notification 和限制以 zeta-app-server-api.md 为准。 9. 验收 - ask、exec、login、config 通过 App Server Client 工作； - 默认进程内模式通过 typed channel 经过相同 App Server method dispatcher； - zeta-exec 与 TUI 共用 App Server 启动、initialize、channel wiring 和 shutdown 实现； - 远程调度通过 zeta-exec 映射为相同 typed request 与 canonical update； - CLI 和 Desktop 对相同请求得到相同 DTO 与错误语义； - CLI crate 不直接依赖 Core、Storage、Tool Executor、Sandbox 或 Model Provider； - JSON/JSONL stdout 无日志污染； - Ctrl-C 发出 session/request InterruptTurn` 并等待终态； - TTY、pipe、重定向和非交互环境均有测试。"
  },
  {
    "slug": "zeta-agent-runtime-architecture",
    "sourcePath": "docs/zeta-agent-runtime-architecture.md",
    "title": "Zeta Agent 执行架构总体设计",
    "description": "本文是 Agent 执行架构的总入口和跨层演进的权威文档。Core 的 ownership 与 crate 内部分层以 core.md 为准；Context 以 core-context.md 为准；多 Agent 以 core-multi-agent.md 为准；canonical 产品契约以 protocol.md 为准；harness 产品策略（提示词组织、工具面选…",
    "group": "开始了解",
    "markdown": "> 状态：Accepted（2026-08-03 整体重审修订，替代此前 Proposed 版本）\n> 审查基线：`0df46ca9ff870489b58ebe6a3cbd0b1b8192928a` 加当前工作区改动\n> 最后状态核对：2026-08-12\n> 适用范围：Session、Thread 执行控制、Agent loop、工具执行、上下文、流式、Provider\n> 切换和多 Agent 演进\n>\n> Cancellation tree 的当前实现与 race semantics 见\n> [`zeta-async-utils` README](../zeta-rs/async-utils/README.md)。\n\n本文是 Agent 执行架构的总入口和跨层演进的权威文档。Core 的 ownership 与 crate 内部分层以\n[`core.md`](core.md) 为准；Context 以 [`core-context.md`](core-context.md) 为准；多 Agent 以\n[`core-multi-agent.md`](core-multi-agent.md) 为准；canonical 产品契约以\n[`protocol.md`](protocol.md) 为准；**harness 产品策略**（提示词组织、工具面选择与注册时机、\n上下文裁剪/压缩策略、prompt cache）以\n[`agent-harness-design.md`](agent-harness-design.md) 为准。本文拥有三件事：**跨层分层与依赖\n规则、重审后的关键决策、分阶段实施计划**。\n\n组件状态使用四个显式标记，本文和四篇领域文档共用同一词表：\n\n- `已实现`：有代码与测试证据；\n- `部分`：纵向切片存在，声明的完整语义未达成；\n- `仅设计`：文档中有设计，代码中零引用；\n- `推迟`：明确不做，直到写明的前置条件出现。\n\n## 快速理解\n\n| 审计问题 | 当前结论 | 深入阅读 |\n| --- | --- | --- |\n| 哪些已经落地，哪些还是纸面设计？ | 单 Agent 顺序执行、durable policy binding 与上下文纵向切片已实现；真实流式、多 Agent 和完整运行时快照仍为部分/仅设计 | [组件状态总账](#2-组件状态总账) |\n| Session 和 Thread 谁是执行边界？ | Session 聚合任务；每个 Thread 独立排序、执行、恢复和持久化 | [分层与执行链](#3-分层与执行链) |\n| 执行内核会异步化（tokio）吗？ | 不承诺；保留同步端口 + per-Thread OS 线程，流式经 sink 达成 | [R2](#42-r2同步执行内核流式经-sink) |\n| Turn 中途策略会漂移吗？ | 模型选择与 policy revision 都在 `TurnAccepted` 冻结；恢复遇到 revision 变化会 fail closed | [R1](#41-r1策略冻结-durable-化) |\n| 上下文溢出怎么办？ | 已由纯 planner 返回显式 overflow/compaction outcome；checkpoint durable commit 后才重规划 | [R3](#43-r3上下文系统裁剪落地) |\n| 多 Agent 什么时候做？ | 阶段 D 契约和阶段 E 的 Fresh spawn/delivery 纵向切片已落地；join、取消树和 UI projection 继续按 gate 演进 | [R4](#44-r4多-agent-契约冻结先行) |\n\n## 1. 重审结论\n\n### 1.1 经验证保留的决策\n\n以下决策经代码与测试证据验证，不再重开：\n\n- protocol → core → store 分层；App Server 是产品能力唯一外部门禁；Core 不依赖 provider\n  wire 类型、storage 实现或 transport；\n- Session（membership / lineage / defaults）与 Thread（history / 上下文 / 执行 / 恢复边界）\n  的聚合拆分；同 Session 下 Thread 可并行；\n- 每 Thread 逻辑单写者 + typed event log + 纯 reducer + command receipt 幂等回放；\n- 副作用前 durable（`ToolExecutionStarted`）、unknown outcome 不自动重放、escalation marker\n  防止恢复后静默重试；\n- explicit incarnation 拒绝迟到/重复/旧实例 completion；空闲回收后从 durable store 重建；\n- 模型和工具 I/O 不占用状态提交临界区；通知不早于 durable append；\n- 拒绝的抽象继续拒绝：与 rollout 并列的 `CanonicalHistory` store、常驻\n  `ProviderLaneRegistry`、把 summary 固化为 `ProviderHandoff` 协议、未定义授权/删除/隔离/\n  评测契约的跨 Thread 长期记忆。\n\n### 1.2 修订的决策\n\n本次重审修订四项设计决策，编号 R1–R4；文档形态修订为 R5。详细设计见\n[第 4 节](#4-修订决策详细设计)。\n\n| # | 原设计 | 修订立场 | 核心理由 |\n| --- | --- | --- | --- |\n| R1 | `TurnPolicySnapshot` 为进程内不可变结构 | policy 冻结改为 **durable fact**：Turn 接受时持久化 policy revision，恢复时据此重建 | 进程内快照不能跨 crash-resume 存活；恢复后的 Turn 会从当前配置重建 policy，违反\"不得静默放宽\"的验收标准。模型选择已经这样做（`TurnAccepted` 携带 model），policy 应对齐 |\n| R2 | 端口演进为 async streaming（隐含运行时异步化） | **不承诺 tokio 迁移**：保留同步端口 + per-Thread OS 线程邮箱；真实流式经 wire-level SSE decoder → `ModelStreamSink`；App Server 补独立 outbound writer 线程 | 桌面级并发上限是几十个 Thread；同步代码对 durability 不变量更易验证；cancellation 已闭环；sink 契约已为流式预留。异步化收益不成比例 |\n| R3 | ContextManager 完整形态（cache / baseline / estimate）一步到位 | **裁剪落地**：纯函数 planner + `ContextPlan` 先行；ContextManager 只做薄协调（无 cache）；compaction checkpoint 的 durable schema 提前进 protocol | 纯函数可独立验证 precedence / budget / 配对 / 确定性；cache 失效是最难验对的部分，推迟到有真实性能证据 |\n| R4 | 多 Agent 按十步顺序整体落地 | **契约冻结与运行时分离**：先只冻结身份语义进 protocol（阶段 D）；coordinator 运行时 gate 在上下文系统完成之后（阶段 E） | context isolation 与 seed 依赖 `ContextPlan`；先冻结契约避免后续 protocol 破坏性变更 |\n| R5 | 文档以现在时描述未实现组件，差异只在\"当前状态\"小节标注 | 每个组件在其权威文档中挂**显式状态标记**（已实现/部分/仅设计/推迟），本文维护跨层状态总账 | 重审当时 `ContextManager`、`MultiAgentCoordinator`、两级快照在代码中零引用，但组件章节读起来像现状；后续以状态总账消除这一风险 |\n\n### 1.3 明确推迟的决策\n\n| 决策 | 重新评审的触发条件 |\n| --- | --- |\n| 执行内核 tokio / async 化 | 出现需要数百以上并发 Thread 的真实宿主，或仅支持 async 的必需 transport |\n| 提取独立 `zeta-agent` crate | 至少两个真实执行宿主，且 Agent loop 不依赖 Thread projection、store、receipt 或 App Server |\n| `ProviderHandoff` 协议 | [第 6 节](#6-history上下文与供应商切换)的 continuity 评测持续失败 |\n| 跨 Thread 长期记忆 | 单独 RFC 接受 consent、scope、删除、保留期与评测契约 |\n| ContextManager cache / reference baseline | 阶段 B 完成后有真实性能证据表明重复组装是瓶颈 |\n| 与 Thread 一一对应的 `AgentId` aggregate | 出现一个 Agent 身份跨多个 Thread 延续的真实需求 |\n\n## 2. 组件状态总账\n\n跨层组件的当前状态。各领域文档不再重复此表，只在组件章节挂对应标记。\n\n### 2.1 执行面（已验证部分）\n\n| 组件 | 状态 | 代码证据 |\n| --- | --- | --- |\n| SessionCoordinator + create/fork/rewind saga | 已实现 | `core/src/session_coordinator.rs` |\n| ThreadController 单写者 / receipt / replay / conflict | 已实现 | `core/src/thread_controller.rs` |\n| per-Thread loaded projection + FIFO mutation gate + incarnation + idle eviction | 已实现 | `core/src/thread_controller/loaded_thread.rs` |\n| 有界执行邮箱（OS 线程 lane，容量 8，30s 空闲回收） | 已实现 | `core/src/thread_controller/mailbox.rs` |\n| TurnExecutor 顺序 model → tool → model 循环 | 已实现 | `core/src/turn/executor.rs` |\n| ToolScheduler：durable one-time approval、sandbox escalation、rejection circuit breaker | 已实现 | `core/src/turn/tool_scheduler.rs` |\n| Tool unknown-outcome 基线（start marker / escalation marker，不自动重放） | 已实现 | `core/src/turn/tool_scheduler.rs`、`thread_reducer.rs` |\n| 模型选择冻结（`TurnAccepted` 携带 model） | 已实现 | `core/src/thread_controller.rs` |\n| `ContextAssembler`（`ContextPlan` → `ModelRequest`） | 已实现 | `core/src/context/assembler.rs` |\n| `ModelService` / `ModelStreamSink` 契约 | 已实现（同步桥接：默认 stream 只回放 final response） | `core/src/services.rs` |\n| 取消链路 session/request InterruptTurn → mailbox cancel → token → model/tool | 已实现 | [`core.md`](core.md) §7.3 |\n| App Server 可唤醒 outbound 通知源（`ConnectionNotifications`） | 部分 | `app-server/src/server.rs`；stdio 主循环仍在每个请求处理后才 drain |\n\n### 2.2 设计面（本计划的工作对象）\n\n| 组件 | 状态 | 归属阶段 | 权威文档 |\n| --- | --- | --- | --- |\n| policy 冻结（durable policy revision binding） | 已实现 | A | `TurnAccepted.policy_revision`、`ToolScheduler` recovery checks |\n| `ModelInvocationSnapshot` | 部分 | B | selected model、`ContextPlan` 与 tools 已冻结；独立 provider/config/catalog revision 集合尚未建模 |\n| `ContextInput` / `ContextPlan` / 纯内容选择 planner | 已实现 | B | [`core-context.md`](core-context.md) |\n| 通用 context budget / token measurement 判定 | 已实现 | B | [`zeta-context-engine`](../zeta-rs/context-engine/README.md)；OpenAI exact，Anthropic/Google/Kimi/Z.AI estimated remote preflight 已接入，local tokenizer 尚未接入 |\n| `ContextManager`（薄协调，无 cache） | 已实现 | B | `core/src/context_manager.rs`、`loaded_thread.rs` |\n| compaction checkpoint schema + 压缩流程 | 已实现 | B | [`core-context.md`](core-context.md) §8 |\n| `ContextCompactionService` / `Clock` / `IdGenerator` / `CapabilityBroker` 端口 | 部分 | B / 按需 | model-backed compaction 已实现；其余仍按需设计 |\n| provider wire-level SSE streaming | 仅设计 | C | 本文 §4.2 |\n| App Server 独立 outbound writer 线程 | 仅设计 | C | 本文 §4.2 |\n| Desktop projection gap/resync | 仅设计 | C | [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) |\n| `DelegationId` / `AgentMessageId` / `ThreadOrigin::AgentSpawn` / seed schema | 已实现 | D | [`core-multi-agent.md`](core-multi-agent.md) |\n| `MultiAgentCoordinator`、Fresh spawn saga、delivery、结构性 tree budget | 已实现 | E | [`core-multi-agent.md`](core-multi-agent.md) |\n| Selected/ForkedPrefix inheritance、durable join、cancellation tree、Agent-tree UI projection | 已实现 | E | [`core-multi-agent.md`](core-multi-agent.md) |\n| 并行工具计划、通用 deadline、声明式 retry、reconciliation | 仅设计 | E 之后按需 | [`core.md`](core.md) §11 |\n| 跨 Thread 长期记忆 | 推迟 | 单独 RFC | 本文 §1.3 |\n\n## 3. 分层与执行链\n\n```text\nCLI / Desktop / future daemon\n             │\n             ▼\nVersioned App Server\n  connection gate / dispatcher / subscriptions / outbound queue\n             │\n             ▼\nSessionCoordinator (zeta-core)\n  membership / lineage / defaults / Session durable commit\n             │ ThreadHandle registry\n             ▼\nThreadController (zeta-core, one logical writer per loaded Thread)\n  durable commit / recovery / incarnation / execution mailbox\n             │\n             ▼\nTurnExecutor (zeta-core private module)\n  context → model → tool calls → tool results → next model request\n       │                              │\n       ▼                              ▼\n  ModelService                   ToolService + ActionPolicyService\n       │                              │\nmodel-provider              shell-command / file-system / apply-patch / MCP adapters\n\nSessionCoordinator ── append ──► SessionStore\nThreadController  ─── append ──► ThreadStore ──► rollout\n      │ committed events and transient deltas\n      └─────────────────────────► ThreadUpdateSink ──► subscription hub\n```\n\n依赖规则（禁止项详见 [`core.md`](core.md) §6）：\n\n- `zeta-core` 不依赖 JSON-RPC DTO、provider HTTP wire 类型、concrete adapter、storage 或\n  rollout；\n- composition root（App Server）构造 adapter 并注入 Core 端口，依赖不反向；\n- `app-server-protocol` 只复用经过审核的 canonical public view；Core-private aggregate、\n  loaded state、mailbox、incarnation 永不进入 wire；\n- Tool adapter 不直接修改 Thread projection。\n\nSession 的三种语义必须区分（详见 [`protocol.md`](protocol.md)）：产品 `Session` 根 aggregate、\n`AppServerConnection`（一条 RPC 连接的资源 owner）、`BrowserSession` / `TerminalSession`\n（capability 生命周期）。三者不得混用命名或状态。\n\n提交顺序、安全点与取消语义由 [`core.md`](core.md) §7 权威定义，本文不重复。工具执行生命\n周期、approval 与 escalation 由 [`core.md`](core.md) §11 权威定义。\n\n## 4. 修订决策详细设计\n\n### 4.1 R1：策略冻结 durable 化\n\n**原问题。** 原设计的 `TurnPolicySnapshot` 是进程内不可变结构，\"整个 Turn 生命周期有效\"。但\nTurn 可以跨进程重启恢复（waiting approval、resumable tool continuation），进程内快照在恢复\n后不存在；当前实现在每次 pending call 审查时从 live `ActionPolicyService` 读取最新策略。结果是：\n配置在 Turn 中途放宽后，恢复的 Turn 会在更宽的策略下继续——这违反\"安全策略不能在 Turn\n中途静默放宽\"的固定决策。已有的缓解只覆盖局部：one-time approval 与 escalation 绑定了\n`action_digest + policy_revision`，但未绑定 Turn 级策略环境。\n\n**当前实现。** 下列 durable revision binding 已接入 protocol、reducer、projection、scheduler\n和 recovery；更宽的 execution limits/agent role snapshot 仍不是当前公共结构。\n\n- `TurnAccepted` 事件增加 `policy_revision` 字段（protocol 变更，进入\n  [`protocol.md`](protocol.md) 阶段 P2/P3 的同一批 schema 同步）；\n- `ThreadSnapshot` 的 Turn 投影暴露冻结 revision；`ToolScheduler` 构造\n  `ActionReviewRequest` 时同时携带冻结 revision 与当前 revision；\n- `ActionPolicyService` 端口新增 host obligation（doc comment 契约）：当当前 revision 不等于冻结\n  revision 时，实现只允许返回**不宽于**冻结 revision 的决定；无法判定时必须返回 `AskUser`\n  或 `Block`，不得静默采用更宽策略；\n- 恢复路径（`resume_recovered_tool_continuations` 与 approval resume）从 durable 冻结\n  revision 重建策略环境，而不是从当前配置；\n- 显式收紧仍然即时生效（当前 revision 更严时按当前执行），与原设计的\"单调收紧\"一致。\n\n进程内的 `TurnPolicySnapshot` 结构仍可作为实现细节存在，但它是冻结 fact 的派生视图，不是\nauthority。\n\n### 4.2 R2：同步执行内核，流式经 sink\n\n**立场。** 执行内核保持同步：per-Thread OS 线程邮箱（`ThreadExecutionMailboxes`）、同步\n`ModelService` / `ToolService` / `ActionPolicyService` 端口、`CancellationToken` 协作取消。不进行\ntokio / async 迁移；重新评审触发条件见 [§1.3](#13-明确推迟的决策)。\n\n**真实流式不需要异步运行时。** `ModelService::stream` 契约已经存在；当前默认桥接在\n`invoke` 返回后一次性回放 final response。阶段 C 的工作是：\n\n- provider adapter 实现 wire-level SSE decoder，在同步读循环中逐 chunk 解码并调用\n  `sink.emit(...)`；每个 chunk 边界观察 cancellation；socket 层仍由 `zeta-http-client` 的\n  bounded transport timeout 收束；\n- `InvocationStream`（Core 侧 sink 实现）已具备 stream incarnation + cursor 发布路径，无需\n  改动契约；\n- transient delta 经有界 channel 发布，饱和时允许合并或丢弃；durable completion 不依赖\n  transient stream。\n\n**App Server outbound topology。** 当前 `serve_jsonl` 是同步 read → dispatch → write →\ndrain 循环：通知在下一个请求处理完成后才被取走。可唤醒的 `ConnectionNotifications`\n（wait / drain / close）已经存在，缺的是独立消费者。目标 topology：\n\n```text\nper-connection reader thread        per-connection writer thread\n  read_message                        wait on ConnectionNotifications\n  → dispatch                          → drain\n  → enqueue response                  → write_message*\n      （response 与 notification 进入同一有界 outbound 队列，\n        由唯一 writer 线程串行写出，保序且互不阻塞）\n```\n\n需要区分的四种终止语义保持原设计：`$/cancelRequest`（取消一个 RPC handler 等待）、\n`session/request` 的 `InterruptTurn`（durable Turn 推向 Interrupted）、connection close（清理 connection-owned\n资源并唤醒 writer 退出）、server shutdown（带 deadline 的全局 graceful stop）。\n\nDesktop Renderer 不持有 raw peer；由单一 projection service 消费 notification，检测 durable\nsequence / stream cursor gap，并通过 `session/subscribe` / `session/thread/subscribe` 的\nsnapshot + gap 重建。\n\n### 4.3 R3：上下文系统裁剪落地\n\n领域权威是 [`core-context.md`](core-context.md)；本节只固定裁剪范围与顺序。\n\n**阶段 B 核心纵向切片已落地：**\n\n1. `ContextInput` / `ContextPlan` 不可变类型 + 纯 `ContextPlanner`（precedence、完整语义单元选择、\n   Tool Call/Result 原子配对、checkpoint selection、显式 overflow outcome），模型无关预算和计量\n   判定由 `zeta-context-engine` 提供；\n2. `ContextAssembler` 从 `ThreadSnapshot → ModelRequest` 过渡 API 改为\n   `ContextPlan → ModelRequest` 纯组装；\n3. 薄 `ContextManager`：per-loaded-Thread、由 `LoadedThreadState` 持有、只做 sequence 校验\n   与 prepare 协调，**无 cache / baseline / token estimate**；\n4. compaction checkpoint durable schema 进入 protocol 与 store envelope（与 R1 的 protocol\n   变更同批规划，避免两次 schema bump）；\n5. compaction 流程：`NeedsCompaction` → `ContextCompactionService` 端口 → 验证 provenance/digest →\n   durable commit checkpoint → 失效重建。Summary model I/O 不持有 Thread writer。\n\n**明确未做（推迟）：** `cached_plan`、`reference_baseline`、`TokenEstimate` 缓存。触发条件\n见 §1.3。\n\n`ModelInvocationSnapshot` 当前随 `ContextPlan` 一起冻结 selected model、上下文计划与工具；\n输出/推理设置和独立 provider/config/catalog revision 集合尚未建模。它是可从 durable fact 与\n安全点快照重建的进程内值，无需自身持久化——这与 R1 的 policy 冻结不同。\n\n### 4.4 R4：多 Agent 契约冻结先行\n\n领域权威是 [`core-multi-agent.md`](core-multi-agent.md)；本节只固定门槛。\n\n**阶段 D（契约冻结，已完成）：** `DelegationId`、`AgentMessageId`、\n`ThreadOrigin::AgentSpawn`、`AgentContextSeed` schema、delegation requested/started/terminal\nfacts、`DelegationResult` Item 进入 canonical protocol，同步 Rust types / JSON Schema /\ngenerated TS / fixtures。目的：后续运行时开发不再产生破坏性 protocol 变更。\n\n**阶段 E 的 gate 条件：**\n\n- 阶段 B 完成（seed 的 `Fresh / Selected / ForkedPrefix` 语义依赖 `ContextPlan` 与\n  checkpoint）；\n- 阶段 D 契约测试通过；\n- spawn saga 的 fault injection 框架就绪（复用 Session create/fork saga 的既有测试基建）。\n\n## 5. 工具执行与恢复（保留设计，状态标注）\n\n工具生命周期、取消、unknown outcome 语义由 [`core.md`](core.md) §11 权威定义，均为已实现。\n以下仍为仅设计，归属阶段 E 之后按需评审：\n\n- 并行 Tool 计划（policy、Tool definition、resource conflict 三重检查后启用；完成顺序不\n  决定 transcript 顺序）；\n- 声明式 retry policy（`Never` / `SafeRead` / `IdempotentWrite(operation key)` /\n  `ReconcileBeforeRetry`；参数错误、permission denial、unknown outcome 默认不自动 retry）；\n- 通用 deadline 与 tool-specific reconciliation。\n\n## 6. History、上下文与供应商切换\n\nContext authority 见 [`core-context.md`](core-context.md)。Provider 切换保持原设计：它是\n\"未来调用配置变更\"，不是新的事实存储层——排入 ThreadController，在 model safe point 从\ndurable history 重新构造 context 并创建新的 invocation snapshot；运行中的请求默认沿用旧\n快照或由用户显式 interrupt。Provider-specific response ID / cache key 可作为 adapter 优化\n暂存，不能成为恢复正确性的前提。\n\n只有以下 continuity 评测持续失败时，才重新评审 `ProviderHandoff`：约束保留、已完成工作\n识别、决策一致性、Tool Result 引用准确性、切换后继续执行成功率。\n\n供应商配置两层边界（`model-provider-config` 声明层 / `model-provider` 运行时层）保持原\n设计，权威见 [`model-provider-config.md`](model-provider-config.md) 与\n[`model-provider.md`](model-provider.md)。\n\n## 7. 分阶段实施计划\n\n替代此前的阶段 0–8。原阶段 0–5 中标注\"完成 / 基础完成\"的内容已并入\n[§2.1 状态总账](#21-执行面已验证部分)，不再作为计划项。\n\n### 阶段 A｜地基修整\n\n范围：\n\n- 按 [`core.md`](core.md) §13 目标目录拆分三个越线文件（`thread_controller.rs` 1189 LoC、\n  `session_coordinator.rs` 998 LoC、`thread_reducer.rs` 947 LoC）；纯迁移，不改语义，测试\n  随实现同步迁移；\n- R1：`TurnAccepted` 增加 `policy_revision`，reducer / projection / scheduler / recovery 接\n  线，protocol schema + TS + fixtures 同步。\n\n完成条件：\n\n- 拆分后 implementation module 低于 500 LoC，全部现有测试通过；\n- 新测试：策略放宽后恢复的 Turn 不以更宽策略执行；冻结 revision 在 replay / recovery 中\n  保持；\n- schema 哈希与 fixtures 更新完整（`pnpm verify:protocol`）。\n\n### 阶段 B｜上下文系统\n\n范围：[§4.3](#43-r3上下文系统裁剪落地) 的五项 + `ModelInvocationSnapshot`。\n\n完成条件：\n\n- 相同 `ContextInput` 产生字节级等价 `ContextPlan`；\n- overflow 产生显式 outcome，当前输入 / 权限约束 / 未完成 Tool continuation 永不被静默\n  删除；\n- Tool Call/Result 原子保留；delegation group 在阶段 D protocol 类型落地后启用同一规则；\n- compaction checkpoint 前后 crash 注入：原始 event log 完整；corrupt checkpoint replay\n  fail closed，且不删除可供显式修复的原始 history；\n- ContextManager 丢弃后从 durable facts 重建等价 plan。\n\n### 阶段 C｜真实流式\n\n范围：[§4.2](#42-r2同步执行内核流式经-sink) 的 provider SSE decoder、App Server\nreader/writer 拆分、Desktop gap/resync。\n\n完成条件：\n\n- 增量 delta 从 provider wire 到客户端全链路可见，不再等待 final response；\n- 断连后 snapshot + gap 重建一致；\n- 流中取消 race：迟到 delta 丢弃、durable completion 不受 transient channel 饱和阻塞；\n- response 与 notification 保序，slow client 不阻塞 durable commit。\n\n阶段 B 与 C 无强依赖，可由不同人并行；合入顺序建议 B 先行，避免流式测试对上下文管线改动\n重跑两轮。\n\n### 阶段 D｜多 Agent 契约冻结（已完成）\n\n范围：[§4.4](#44-r4多-agent-契约冻结先行) 的 protocol 变更。\n\n完成条件：Rust / schema / TS / fixtures 四处一致；contract test 覆盖新类型的\nserialize / deserialize / 拒绝非法值；不引入任何 Core 运行时依赖。\n\n### 阶段 E｜MultiAgentCoordinator（核心纵向切片已完成）\n\n当前已完成 Fresh/Selected/ForkedPrefix spawn saga、outbox/inbox delivery、durable join、向下\ncancellation tree、结构性 Agent tree budget、恢复、Desktop tree projection 与可信 Workspace\n的模型工具接线。后续范围是 Agent definition 自动选择、跨产品 projection 与更完整的故障注入\n矩阵；gate 条件见 §4.4。\n\n完成条件：[`core-multi-agent.md`](core-multi-agent.md) §17 验证矩阵全量通过，其中 spawn\n的每个 durable boundary crash、duplicate delegation 拒绝、parent/child/sibling 隔离为必过\n项。\n\n### 贯穿项\n\n- 每个新增 durable boundary 在其落地阶段内补 fault injection，不集中后补；\n- 文档随阶段收口更新状态标记（R5），不允许\"实现先行、文档滞后超过一个阶段\"。\n\n## 8. 验证门\n\nRust：\n\n```bash\ncargo fmt --manifest-path zeta-rs/Cargo.toml --all -- --check\ncargo clippy --manifest-path zeta-rs/Cargo.toml --workspace --all-targets -- -D warnings\ncargo test --manifest-path zeta-rs/Cargo.toml --workspace\n```\n\nDesktop 与协议：\n\n```bash\npnpm verify:protocol\npnpm --dir desktop run build:host\npnpm --dir desktop run typecheck:renderer\npnpm --dir desktop run test:main\n```\n\n跨阶段必须持续覆盖（已实现部分回归 + 新增项）：\n\n- 同 Thread FIFO 与不同 Thread 并发；Session 结构提交不阻塞子 Thread 执行；\n- acceptance response 与 notification 顺序；\n- provider/tool cancellation race；completion 迟到、重复和旧 incarnation；\n- mailbox、stream 和 outbound queue saturation；\n- partial durable batch、crash recovery 和 idempotency replay；\n- Tool unknown outcome 不自动重放；\n- 冻结 policy revision 下的恢复语义（阶段 A 起）；\n- Context determinism、overflow、checkpoint crash（阶段 B 起）；\n- Renderer gap/resync（阶段 C 起）。\n\n## 9. 验收标准\n\n- rollout 是唯一权威历史，任何投影都能重建；\n- Session 是 Thread membership/lineage 的权威边界，Thread 是独立执行与恢复边界；\n- 长模型或工具调用期间仍可 interrupt 并服务其他 Thread；\n- 同 Thread 只有一个逻辑状态提交者；\n- 所有用户可见 final Item 在通知前 durable；\n- Turn 级策略环境冻结为 durable fact，恢复不产生静默放宽；\n- 上下文溢出产生显式 outcome，当前输入与安全约束永不被静默删除；\n- provider/tool/execution 失败都有明确 terminal path；断连、取消、超时和 shutdown 不留下\n  永久 pending；\n- Provider 切换不依赖旧 provider cache；Tool 副作用在 unknown outcome 下不被静默重放；\n- connection/capability Session 不得与产品 Session 混用；\n- 每个组件的文档状态标记与代码证据一致；\n- 长期 Memory 不在没有独立 consent、scope、删除和评测契约时进入核心模型。\n\n## 10. 参考\n\n- [Zeta 架构索引](architecture.md)\n- [会话与执行系统（Core）](core.md)\n- [上下文系统](core-context.md)\n- [多 Agent 协作系统](core-multi-agent.md)\n- [产品协议](protocol.md)\n- [zeta-rs 产品内核与统一对外层](zeta-rs-architecture.md)\n- [Zeta App Server API](zeta-app-server-api.md)\n- [OpenAI Codex App Server snapshot](https://github.com/openai/codex/blob/322d5b96cfa5c8fd52bd83ecfdb79cd9b330205f/codex-rs/app-server/README.md)\n- [Pi session format snapshot](https://github.com/earendil-works/pi/blob/5bc1c2c0a6f07e00e8c240304182f213ab8d311f/packages/coding-agent/docs/session-format.md)",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-重审结论",
        "title": "1. 重审结论"
      },
      {
        "depth": 3,
        "id": "11-经验证保留的决策",
        "title": "1.1 经验证保留的决策"
      },
      {
        "depth": 3,
        "id": "12-修订的决策",
        "title": "1.2 修订的决策"
      },
      {
        "depth": 3,
        "id": "13-明确推迟的决策",
        "title": "1.3 明确推迟的决策"
      },
      {
        "depth": 2,
        "id": "2-组件状态总账",
        "title": "2. 组件状态总账"
      },
      {
        "depth": 3,
        "id": "21-执行面已验证部分",
        "title": "2.1 执行面（已验证部分）"
      },
      {
        "depth": 3,
        "id": "22-设计面本计划的工作对象",
        "title": "2.2 设计面（本计划的工作对象）"
      },
      {
        "depth": 2,
        "id": "3-分层与执行链",
        "title": "3. 分层与执行链"
      },
      {
        "depth": 2,
        "id": "4-修订决策详细设计",
        "title": "4. 修订决策详细设计"
      },
      {
        "depth": 3,
        "id": "41-r1策略冻结-durable-化",
        "title": "4.1 R1：策略冻结 durable 化"
      },
      {
        "depth": 3,
        "id": "42-r2同步执行内核流式经-sink",
        "title": "4.2 R2：同步执行内核，流式经 sink"
      },
      {
        "depth": 3,
        "id": "43-r3上下文系统裁剪落地",
        "title": "4.3 R3：上下文系统裁剪落地"
      },
      {
        "depth": 3,
        "id": "44-r4多-agent-契约冻结先行",
        "title": "4.4 R4：多 Agent 契约冻结先行"
      },
      {
        "depth": 2,
        "id": "5-工具执行与恢复保留设计状态标注",
        "title": "5. 工具执行与恢复（保留设计，状态标注）"
      },
      {
        "depth": 2,
        "id": "6-history上下文与供应商切换",
        "title": "6. History、上下文与供应商切换"
      },
      {
        "depth": 2,
        "id": "7-分阶段实施计划",
        "title": "7. 分阶段实施计划"
      },
      {
        "depth": 3,
        "id": "阶段-a地基修整",
        "title": "阶段 A｜地基修整"
      },
      {
        "depth": 3,
        "id": "阶段-b上下文系统",
        "title": "阶段 B｜上下文系统"
      },
      {
        "depth": 3,
        "id": "阶段-c真实流式",
        "title": "阶段 C｜真实流式"
      },
      {
        "depth": 3,
        "id": "阶段-d多-agent-契约冻结已完成",
        "title": "阶段 D｜多 Agent 契约冻结（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段-emultiagentcoordinator核心纵向切片已完成",
        "title": "阶段 E｜MultiAgentCoordinator（核心纵向切片已完成）"
      },
      {
        "depth": 3,
        "id": "贯穿项",
        "title": "贯穿项"
      },
      {
        "depth": 2,
        "id": "8-验证门",
        "title": "8. 验证门"
      },
      {
        "depth": 2,
        "id": "9-验收标准",
        "title": "9. 验收标准"
      },
      {
        "depth": 2,
        "id": "10-参考",
        "title": "10. 参考"
      }
    ],
    "searchText": " 状态：Accepted（2026-08-03 整体重审修订，替代此前 Proposed 版本） 审查基线：0df46ca9ff870489b58ebe6a3cbd0b1b8192928a 加当前工作区改动 最后状态核对：2026-08-12 适用范围：Session、Thread 执行控制、Agent loop、工具执行、上下文、流式、Provider 切换和多 Agent 演进 Cancellation tree 的当前实现与 race semantics 见 zeta-async-utils README。 本文是 Agent 执行架构的总入口和跨层演进的权威文档。Core 的 ownership 与 crate 内部分层以 core.md 为准；Context 以 core-context.md 为准；多 Agent 以 core-multi-agent.md 为准；canonical 产品契约以 protocol.md 为准；harness 产品策略（提示词组织、工具面选择与注册时机、 上下文裁剪/压缩策略、prompt cache）以 agent-harness-design.md 为准。本文拥有三件事：跨层分层与依赖 规则、重审后的关键决策、分阶段实施计划。 组件状态使用四个显式标记，本文和四篇领域文档共用同一词表： - 已实现：有代码与测试证据； - 部分：纵向切片存在，声明的完整语义未达成； - 仅设计：文档中有设计，代码中零引用； - 推迟：明确不做，直到写明的前置条件出现。 快速理解 审计问题 当前结论 深入阅读 --- --- --- 哪些已经落地，哪些还是纸面设计？ 单 Agent 顺序执行、durable policy binding 与上下文纵向切片已实现；真实流式、多 Agent 和完整运行时快照仍为部分/仅设计 组件状态总账 Session 和 Thread 谁是执行边界？ Session 聚合任务；每个 Thread 独立排序、执行、恢复和持久化 分层与执行链 执行内核会异步化（tokio）吗？ 不承诺；保留同步端口 + per-Thread OS 线程，流式经 sink 达成 R2 Turn 中途策略会漂移吗？ 模型选择与 policy revision 都在 TurnAccepted 冻结；恢复遇到 revision 变化会 fail closed R1 上下文溢出怎么办？ 已由纯 planner 返回显式 overflow/compaction outcome；checkpoint durable commit 后才重规划 R3 多 Agent 什么时候做？ 阶段 D 契约和阶段 E 的 Fresh spawn/delivery 纵向切片已落地；join、取消树和 UI projection 继续按 gate 演进 R4 1. 重审结论 1.1 经验证保留的决策 以下决策经代码与测试证据验证，不再重开： - protocol → core → store 分层；App Server 是产品能力唯一外部门禁；Core 不依赖 provider wire 类型、storage 实现或 transport； - Session（membership / lineage / defaults）与 Thread（history / 上下文 / 执行 / 恢复边界） 的聚合拆分；同 Session 下 Thread 可并行； - 每 Thread 逻辑单写者 + typed event log + 纯 reducer + command receipt 幂等回放； - 副作用前 durable（ToolExecutionStarted）、unknown outcome 不自动重放、escalation marker 防止恢复后静默重试； - explicit incarnation 拒绝迟到/重复/旧实例 completion；空闲回收后从 durable store 重建； - 模型和工具 I/O 不占用状态提交临界区；通知不早于 durable append； - 拒绝的抽象继续拒绝：与 rollout 并列的 CanonicalHistory store、常驻 ProviderLaneRegistry、把 summary 固化为 ProviderHandoff 协议、未定义授权/删除/隔离/ 评测契约的跨 Thread 长期记忆。 1.2 修订的决策 本次重审修订四项设计决策，编号 R1–R4；文档形态修订为 R5。详细设计见 第 4 节。 原设计 修订立场 核心理由 --- --- --- --- R1 TurnPolicySnapshot 为进程内不可变结构 policy 冻结改为 durable fact：Turn 接受时持久化 policy revision，恢复时据此重建 进程内快照不能跨 crash-resume 存活；恢复后的 Turn 会从当前配置重建 policy，违反\"不得静默放宽\"的验收标准。模型选择已经这样做（TurnAccepted 携带 model），policy 应对齐 R2 端口演进为 async streaming（隐含运行时异步化） 不承诺 tokio 迁移：保留同步端口 + per-Thread OS 线程邮箱；真实流式经 wire-level SSE decoder → ModelStreamSink；App Server 补独立 outbound writer 线程 桌面级并发上限是几十个 Thread；同步代码对 durability 不变量更易验证；cancellation 已闭环；sink 契约已为流式预留。异步化收益不成比例 R3 ContextManager 完整形态（cache / baseline / estimate）一步到位 裁剪落地：纯函数 planner + ContextPlan 先行；ContextManager 只做薄协调（无 cache）；compaction checkpoint 的 durable schema 提前进 protocol 纯函数可独立验证 precedence / budget / 配对 / 确定性；cache 失效是最难验对的部分，推迟到有真实性能证据 R4 多 Agent 按十步顺序整体落地 契约冻结与运行时分离：先只冻结身份语义进 protocol（阶段 D）；coordinator 运行时 gate 在上下文系统完成之后（阶段 E） context isolation 与 seed 依赖 ContextPlan；先冻结契约避免后续 protocol 破坏性变更 R5 文档以现在时描述未实现组件，差异只在\"当前状态\"小节标注 每个组件在其权威文档中挂显式状态标记（已实现/部分/仅设计/推迟），本文维护跨层状态总账 重审当时 ContextManager、MultiAgentCoordinator、两级快照在代码中零引用，但组件章节读起来像现状；后续以状态总账消除这一风险 1.3 明确推迟的决策 决策 重新评审的触发条件 --- --- 执行内核 tokio / async 化 出现需要数百以上并发 Thread 的真实宿主，或仅支持 async 的必需 transport 提取独立 zeta-agent crate 至少两个真实执行宿主，且 Agent loop 不依赖 Thread projection、store、receipt 或 App Server ProviderHandoff 协议 第 6 节的 continuity 评测持续失败 跨 Thread 长期记忆 单独 RFC 接受 consent、scope、删除、保留期与评测契约 ContextManager cache / reference baseline 阶段 B 完成后有真实性能证据表明重复组装是瓶颈 与 Thread 一一对应的 AgentId aggregate 出现一个 Agent 身份跨多个 Thread 延续的真实需求 2. 组件状态总账 跨层组件的当前状态。各领域文档不再重复此表，只在组件章节挂对应标记。 2.1 执行面（已验证部分） 组件 状态 代码证据 --- --- --- SessionCoordinator + create/fork/rewind saga 已实现 core/src/sessioncoordinator.rs ThreadController 单写者 / receipt / replay / conflict 已实现 core/src/threadcontroller.rs per-Thread loaded projection + FIFO mutation gate + incarnation + idle eviction 已实现 core/src/threadcontroller/loadedthread.rs 有界执行邮箱（OS 线程 lane，容量 8，30s 空闲回收） 已实现 core/src/threadcontroller/mailbox.rs TurnExecutor 顺序 model → tool → model 循环 已实现 core/src/turn/executor.rs ToolScheduler：durable one-time approval、sandbox escalation、rejection circuit breaker 已实现 core/src/turn/toolscheduler.rs Tool unknown-outcome 基线（start marker / escalation marker，不自动重放） 已实现 core/src/turn/toolscheduler.rs、threadreducer.rs 模型选择冻结（TurnAccepted 携带 model） 已实现 core/src/threadcontroller.rs ContextAssembler（ContextPlan → ModelRequest） 已实现 core/src/context/assembler.rs ModelService / ModelStreamSink 契约 已实现（同步桥接：默认 stream 只回放 final response） core/src/services.rs 取消链路 session/request InterruptTurn → mailbox cancel → token → model/tool 已实现 core.md §7.3 App Server 可唤醒 outbound 通知源（ConnectionNotifications） 部分 app-server/src/server.rs；stdio 主循环仍在每个请求处理后才 drain 2.2 设计面（本计划的工作对象） 组件 状态 归属阶段 权威文档 --- --- --- --- policy 冻结（durable policy revision binding） 已实现 A TurnAccepted.policyrevision、ToolScheduler recovery checks ModelInvocationSnapshot 部分 B selected model、ContextPlan 与 tools 已冻结；独立 provider/config/catalog revision 集合尚未建模 ContextInput / ContextPlan / 纯内容选择 planner 已实现 B core-context.md 通用 context budget / token measurement 判定 已实现 B zeta-context-engine；OpenAI exact，Anthropic/Google/Kimi/Z.AI estimated remote preflight 已接入，local tokenizer 尚未接入 ContextManager（薄协调，无 cache） 已实现 B core/src/contextmanager.rs、loadedthread.rs compaction checkpoint schema + 压缩流程 已实现 B core-context.md §8 ContextCompactionService / Clock / IdGenerator / CapabilityBroker 端口 部分 B / 按需 model-backed compaction 已实现；其余仍按需设计 provider wire-level SSE streaming 仅设计 C 本文 §4.2 App Server 独立 outbound writer 线程 仅设计 C 本文 §4.2 Desktop projection gap/resync 仅设计 C zeta-desktop-architecture.md DelegationId / AgentMessageId / ThreadOrigin::AgentSpawn / seed schema 已实现 D core-multi-agent.md MultiAgentCoordinator、Fresh spawn saga、delivery、结构性 tree budget 已实现 E core-multi-agent.md Selected/ForkedPrefix inheritance、durable join、cancellation tree、Agent-tree UI projection 已实现 E core-multi-agent.md 并行工具计划、通用 deadline、声明式 retry、reconciliation 仅设计 E 之后按需 core.md §11 跨 Thread 长期记忆 推迟 单独 RFC 本文 §1.3 3. 分层与执行链 ``text CLI / Desktop / future daemon │ ▼ Versioned App Server connection gate / dispatcher / subscriptions / outbound queue │ ▼ SessionCoordinator zeta-core membership / lineage / defaults / Session durable commit │ ThreadHandle registry ▼ ThreadController zeta-core, one logical writer per loaded Thread durable commit / recovery / incarnation / execution mailbox │ ▼ TurnExecutor zeta-core private module context → model → tool calls → tool results → next model request │ │ ▼ ▼ ModelService ToolService + ActionPolicyService │ │ model-provider shell-command / file-system / apply-patch / MCP adapters SessionCoordinator ── append ──► SessionStore ThreadController ─── append ──► ThreadStore ──► rollout │ committed events and transient deltas └─────────────────────────► ThreadUpdateSink ──► subscription hub ` 依赖规则（禁止项详见 core.md §6）： - zeta-core 不依赖 JSON-RPC DTO、provider HTTP wire 类型、concrete adapter、storage 或 rollout； - composition root（App Server）构造 adapter 并注入 Core 端口，依赖不反向； - app-server-protocol 只复用经过审核的 canonical public view；Core-private aggregate、 loaded state、mailbox、incarnation 永不进入 wire； - Tool adapter 不直接修改 Thread projection。 Session 的三种语义必须区分（详见 protocol.md）：产品 Session 根 aggregate、 AppServerConnection（一条 RPC 连接的资源 owner）、BrowserSession / TerminalSession （capability 生命周期）。三者不得混用命名或状态。 提交顺序、安全点与取消语义由 core.md §7 权威定义，本文不重复。工具执行生命 周期、approval 与 escalation 由 core.md §11 权威定义。 4. 修订决策详细设计 4.1 R1：策略冻结 durable 化 原问题。 原设计的 TurnPolicySnapshot 是进程内不可变结构，\"整个 Turn 生命周期有效\"。但 Turn 可以跨进程重启恢复（waiting approval、resumable tool continuation），进程内快照在恢复 后不存在；当前实现在每次 pending call 审查时从 live ActionPolicyService 读取最新策略。结果是： 配置在 Turn 中途放宽后，恢复的 Turn 会在更宽的策略下继续——这违反\"安全策略不能在 Turn 中途静默放宽\"的固定决策。已有的缓解只覆盖局部：one-time approval 与 escalation 绑定了 actiondigest + policyrevision，但未绑定 Turn 级策略环境。 当前实现。 下列 durable revision binding 已接入 protocol、reducer、projection、scheduler 和 recovery；更宽的 execution limits/agent role snapshot 仍不是当前公共结构。 - TurnAccepted 事件增加 policyrevision 字段（protocol 变更，进入 protocol.md 阶段 P2/P3 的同一批 schema 同步）； - ThreadSnapshot 的 Turn 投影暴露冻结 revision；ToolScheduler 构造 ActionReviewRequest 时同时携带冻结 revision 与当前 revision； - ActionPolicyService 端口新增 host obligation（doc comment 契约）：当当前 revision 不等于冻结 revision 时，实现只允许返回不宽于冻结 revision 的决定；无法判定时必须返回 AskUser 或 Block，不得静默采用更宽策略； - 恢复路径（resumerecoveredtoolcontinuations 与 approval resume）从 durable 冻结 revision 重建策略环境，而不是从当前配置； - 显式收紧仍然即时生效（当前 revision 更严时按当前执行），与原设计的\"单调收紧\"一致。 进程内的 TurnPolicySnapshot 结构仍可作为实现细节存在，但它是冻结 fact 的派生视图，不是 authority。 4.2 R2：同步执行内核，流式经 sink 立场。 执行内核保持同步：per-Thread OS 线程邮箱（ThreadExecutionMailboxes）、同步 ModelService / ToolService / ActionPolicyService 端口、CancellationToken 协作取消。不进行 tokio / async 迁移；重新评审触发条件见 §1.3。 真实流式不需要异步运行时。 ModelService::stream 契约已经存在；当前默认桥接在 invoke 返回后一次性回放 final response。阶段 C 的工作是： - provider adapter 实现 wire-level SSE decoder，在同步读循环中逐 chunk 解码并调用 sink.emit ... ；每个 chunk 边界观察 cancellation；socket 层仍由 zeta-http-client 的 bounded transport timeout 收束； - InvocationStream（Core 侧 sink 实现）已具备 stream incarnation + cursor 发布路径，无需 改动契约； - transient delta 经有界 channel 发布，饱和时允许合并或丢弃；durable completion 不依赖 transient stream。 App Server outbound topology。 当前 servejsonl 是同步 read → dispatch → write → drain 循环：通知在下一个请求处理完成后才被取走。可唤醒的 ConnectionNotifications （wait / drain / close）已经存在，缺的是独立消费者。目标 topology： `text per-connection reader thread per-connection writer thread readmessage wait on ConnectionNotifications → dispatch → drain → enqueue response → writemessage （response 与 notification 进入同一有界 outbound 队列， 由唯一 writer 线程串行写出，保序且互不阻塞） ` 需要区分的四种终止语义保持原设计：$/cancelRequest（取消一个 RPC handler 等待）、 session/request 的 InterruptTurn（durable Turn 推向 Interrupted）、connection close（清理 connection-owned 资源并唤醒 writer 退出）、server shutdown（带 deadline 的全局 graceful stop）。 Desktop Renderer 不持有 raw peer；由单一 projection service 消费 notification，检测 durable sequence / stream cursor gap，并通过 session/subscribe / session/thread/subscribe 的 snapshot + gap 重建。 4.3 R3：上下文系统裁剪落地 领域权威是 core-context.md；本节只固定裁剪范围与顺序。 阶段 B 核心纵向切片已落地： 1. ContextInput / ContextPlan 不可变类型 + 纯 ContextPlanner（precedence、完整语义单元选择、 Tool Call/Result 原子配对、checkpoint selection、显式 overflow outcome），模型无关预算和计量 判定由 zeta-context-engine 提供； 2. ContextAssembler 从 ThreadSnapshot → ModelRequest 过渡 API 改为 ContextPlan → ModelRequest 纯组装； 3. 薄 ContextManager：per-loaded-Thread、由 LoadedThreadState 持有、只做 sequence 校验 与 prepare 协调，无 cache / baseline / token estimate； 4. compaction checkpoint durable schema 进入 protocol 与 store envelope（与 R1 的 protocol 变更同批规划，避免两次 schema bump）； 5. compaction 流程：NeedsCompaction → ContextCompactionService 端口 → 验证 provenance/digest → durable commit checkpoint → 失效重建。Summary model I/O 不持有 Thread writer。 明确未做（推迟）： cachedplan、referencebaseline、TokenEstimate 缓存。触发条件 见 §1.3。 ModelInvocationSnapshot 当前随 ContextPlan 一起冻结 selected model、上下文计划与工具； 输出/推理设置和独立 provider/config/catalog revision 集合尚未建模。它是可从 durable fact 与 安全点快照重建的进程内值，无需自身持久化——这与 R1 的 policy 冻结不同。 4.4 R4：多 Agent 契约冻结先行 领域权威是 core-multi-agent.md；本节只固定门槛。 阶段 D（契约冻结，已完成）： DelegationId、AgentMessageId、 ThreadOrigin::AgentSpawn、AgentContextSeed schema、delegation requested/started/terminal facts、DelegationResult Item 进入 canonical protocol，同步 Rust types / JSON Schema / generated TS / fixtures。目的：后续运行时开发不再产生破坏性 protocol 变更。 阶段 E 的 gate 条件： - 阶段 B 完成（seed 的 Fresh / Selected / ForkedPrefix 语义依赖 ContextPlan 与 checkpoint）； - 阶段 D 契约测试通过； - spawn saga 的 fault injection 框架就绪（复用 Session create/fork saga 的既有测试基建）。 5. 工具执行与恢复（保留设计，状态标注） 工具生命周期、取消、unknown outcome 语义由 core.md §11 权威定义，均为已实现。 以下仍为仅设计，归属阶段 E 之后按需评审： - 并行 Tool 计划（policy、Tool definition、resource conflict 三重检查后启用；完成顺序不 决定 transcript 顺序）； - 声明式 retry policy（Never / SafeRead / IdempotentWrite operation key / ReconcileBeforeRetry；参数错误、permission denial、unknown outcome 默认不自动 retry）； - 通用 deadline 与 tool-specific reconciliation。 6. History、上下文与供应商切换 Context authority 见 core-context.md。Provider 切换保持原设计：它是 \"未来调用配置变更\"，不是新的事实存储层——排入 ThreadController，在 model safe point 从 durable history 重新构造 context 并创建新的 invocation snapshot；运行中的请求默认沿用旧 快照或由用户显式 interrupt。Provider-specific response ID / cache key 可作为 adapter 优化 暂存，不能成为恢复正确性的前提。 只有以下 continuity 评测持"
  },
  {
    "slug": "ui-styling-ownership",
    "sourcePath": "docs/ui-styling-ownership.md",
    "title": "Renderer UI 样式所有权规范",
    "description": "每一种视觉规则只能有一个 owner。调用方选择组件、传入语义状态或选择公开的 presentation variant；调用方不得通过深层 selector 重写被调用组件的内部 item、hover、active、focus 或 disabled 样式。",
    "group": "界面与体验",
    "markdown": "> 本文是 Zeta Desktop Renderer 中组件、组合控件与 Workbench Part 样式边界的 canonical 文档。\n> 主题值与 token 注册仍以 [`design-tokens.md`](design-tokens.md) 为准；Desktop 的跨进程和产品所有权仍以 [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) 为准。\n> Pane-like Part 的标题层级、槽位、命名和 Composite 生命周期以 [`workbench-pane-composite-design.md`](workbench-pane-composite-design.md) 为准。\n> Command、MenuId、Context Key 与菜单型 Toolbar 的组合语义以 [`menu-system.md`](menu-system.md) 为准；本文只拥有它们最终投影到控件后的视觉边界。\n\n## 快速理解\n\n每一种视觉规则只能有一个 owner。调用方选择组件、传入语义状态或选择公开的 presentation variant；调用方不得通过深层 selector 重写被调用组件的内部 item、hover、active、focus 或 disabled 样式。\n\n判断 owner 时使用下面的顺序：\n\n1. 状态由哪个控件定义，状态视觉就由哪个控件拥有。\n2. DOM 由哪个控件创建，内部结构样式就由哪个控件拥有。\n3. Part 只拥有区域布局、边界、背景和直接子组件的外部尺寸。\n4. 颜色和尺寸值来自 design token；token 不拥有 selector、状态机或 DOM 结构。\n5. 同一组件需要不同外观时，增加有名字的 presentation variant，不在 host CSS 中穿透覆盖。\n\n| 想修改什么 | 样式所有者 | 正确做法 |\n| --- | --- | --- |\n| 组件内部间距和交互状态 | 创建该 DOM 和状态的组件 | 修改组件自己的样式 |\n| Workbench 区域布局和边界 | 对应 Part | 只修改直接托管区域的外部盒子 |\n| 同一组件的另一种正式外观 | 组件公开的展示变体 | 增加有名字的变体 |\n| 主题颜色或标准尺寸 | Design Token | 修改语义 token，不修改 selector |\n| 某个业务动作的显隐 | Command/Menu/Context Key | 不用 CSS 猜业务状态 |\n\n## 分层所有权\n\n| 层级 | 当前代表 | 必须负责 | 禁止负责 |\n| --- | --- | --- | --- |\n| Primitive | `Button`、`ActionBar`、`ToolBar`、`TabList`、`PaneView` | 通用 DOM、键盘行为、ARIA、基础布局和 primitive 自身状态 | Workbench 区域语义、Panel/Sidebar 特例 |\n| Composed control | `CompositeBar`、Editor/Chat tabs control、`MenubarControl` | 领域内 item 几何、presentation variant、hover/active/selected 视觉 | Part 的位置、区域背景和网格尺寸 |\n| Part | `TitlebarPart`、`PanelPart`、`SidebarPart` | Part 根节点、标题区/内容区布局、边框、背景、直接子组件占位 | 深入修改组件内部 `.zeta-action-bar`、`.zeta-tab`、`.zeta-button` 状态 |\n| Contribution/View | `TerminalViewPane`、Explorer、Search、Chat | 自有内容、命令、View 内部交互和私有子控件 | Workbench Part 的全局布局或其他 View 的皮肤 |\n| Theme | color/size registries 与 CSS custom properties | 视觉值、别名、主题覆盖和快照投影 | selector、DOM、active/hover 判定 |\n\n文件位置不是所有权的唯一证据，root class 和构造者才是。Part 私有子控件可以与 Part CSS 共置，但 selector 必须以该私有子控件的 root class 开始，不能借 Part root 任意穿透共享组件。\n\n## 当前控件的准确职责\n\n| 控件 | 当前职责 | 状态视觉 owner |\n| --- | --- | --- |\n| `ActionBar` | action 排列、方向键导航、roving tabindex、item shell；可显式启用 toggled 高亮上下文 | 不自动决定业务 selected/checked 视觉；Workbench 全局皮肤统一提供横向 action hover |\n| `Button` | button DOM、focus-visible、disabled，以及 `.checked` 与 `aria-pressed` 的并行状态投影 | 不默认提供 hover 背景；hover 与 checked 的具体皮肤由 presentation context 决定 |\n| `Switch` | track/thumb 结构、on/off、hover、focus、pressed、disabled 的内部 presentation | 宿主只提供状态与命名变体，不穿透覆盖内部 track/thumb |\n| `ContextView` | 浮层挂载、锚点定位、视口内翻转和裁剪，以及通用浮层外壳 | 下拉框、提示、选择器和菜单各自的内容结构与交互状态 |\n| `ToolBar` | primary/secondary action 编排、22px icon action 尺寸、More Actions、可选 toggled 高亮 | Button 负责状态投影；所在 presentation context 决定 hover 皮肤 |\n| `DropdownWithPrimaryActionViewItem` | primary 与 dropdown 的组合 DOM、连续几何、内部键盘导航 | Workbench 全局 ActionBar 皮肤为整个 split action 提供 hover；产品只提供 actions 和 menu 数据 |\n| `WorkbenchToolBar` | 把 platform action representation 适配到 base `ToolBar`；actions 仍由调用方提供 | 横向 action hover 归 Workbench 全局皮肤，其余视觉仍归 base `ToolBar`/`Button` |\n| `MenuWorkbenchToolBar` | 从 `MenuId` 解析并刷新 `WorkbenchToolBar` actions | Menu 来源不改变 toolbar ARIA 与视觉 owner |\n| `TabList` | `tablist/tab` ARIA、`aria-selected`、激活回调、`.checked` 状态投影，以及标准 tab 的选中背景 | `tablist.css` |\n| `CompositeBar` | View Container 切换，以及 `icon`/`label` 两种 presentation | presentation 专属的几何与前景色；不重定义标准 tab 选中背景 |\n| `PaneComposite` | `tabpanel`、pane 生命周期、pane header presentation | `views.css` |\n| `PaneCompositePart` | 32px title control、CompositeBar/自有标题与 title actions 的左右槽位 | `paneCompositePart.css` 只拥有外层布局 |\n| `TitlebarPart` | 左区、应用菜单、标题和右区的窗口级布局 | 通用 toolbar/button 状态不归 Titlebar |\n\n`PaneCompositePart` 的标题高度为 `32px`；`titleContentElement` 占据左侧弹性空间，`titleActionsSlotElement` 固定在右侧并提供 `4px` 的双侧 inset。当前 View 的 `partTitleProjection.actions` 与 Part 级 menu toolbar 使用独立子槽并列；Part CSS 只拥有这些直接槽位的外框与排列，不得穿透修改其中的 toolbar/button 内部状态。\n\n`CompositeBar` 的 icon presentation item 状态盒为 `24px × 24px`，hover 与 `.checked` 使用同一外框。`CompositeBar` 拥有该 item 几何及 presentation 专属前景色；`TabList` 提供标准 tab 的稳定状态 class、选中背景，以及由 `tabList.itemContentInset` 定义的标准内容左右 inset；非选中 tab 保持透明；`ActionBar` 只在显式启用 toggled 高亮时提供 action 的背景。\n\n## 状态归属矩阵\n\n| 状态 | 判定 owner | 样式 owner | Host 可以做什么 |\n| --- | --- | --- | --- |\n| `hover` | 原生 pointer 状态 | presentation context；Workbench 横向 ActionBar 由全局 Workbench 皮肤统一处理 | 选择 presentation；不得在具体 Part 或 contribution 中重写内部 action |\n| `focus-visible` / `disabled` | 原生交互 primitive | primitive CSS | 选择 primitive；不得重写内部状态 |\n| menu 当前项 | `Menu` 统一投射鼠标与键盘焦点为 `.focused` | `menu.css` | 提供 actions；不得用调用方 selector 重建菜单焦点态 |\n| tab `selected` / `active` | `TabList` 投影状态 | `TabList` 默认皮肤；有独立语义的组合控件通过自己的 token 覆盖 | 选择 presentation variant，不能复用 ActionBar token |\n| toggle `checked` / `pressed` | command/action model | 控件并行投影 `.checked` 与 ARIA；具体 composed control CSS 决定皮肤 | 提供 checked 值或显式启用 toggled 高亮；不得在 Part 中猜状态 |\n| Part visible/hidden | Workbench layout | Part/layout CSS | contribution 只能请求命令 |\n| theme light/dark/high contrast | Theme service | token snapshot | 组件消费 token，不硬编码主题分支 |\n\n`ActionBar` 是行为与排列基座，不因为它包裹了 item 就自动拥有业务 selected 皮肤。需要通用 toggled 背景时，由调用方显式启用 `highlightToggledItems`，ActionBar 再通过 `.highlight-toggled .checked` selector 应用自己的公开高亮 presentation。\n\n### ActionBar 与 TabList 的选中态边界\n\n`ActionBar` 的 `actionBar.toggledBackground` 只表达“一个 action 在显式 toggled 上下文中被选中”。`TabList` 的 `tabList.activeBackground` 只表达“一个 tab 被选中”；非选中 tab 不设置背景。两者即使当前主题值相同，也必须保持独立 token 和独立 CSS selector；这保证日后主题或交互语义变化时不会产生隐式耦合。\n\n标准 tab 由 `tablist.css` 消费 `tabList.activeBackground` token，并保持非选中项透明。`CompositeBar` 只能追加 `icon`/`label` presentation 的几何、间距和前景色，不能覆盖标准 tab 的选中背景。Chat 这类会话 tab 可以通过 `chat.tabBackground` 定义非选中背景，但必须继承 `TabList` 的选中背景；不得借用 ActionBar token 或通过 Part 深层 selector 改写 TabList。\n\n和 VS Code 一致，ARIA attribute 用于无障碍语义，稳定 class 用于视觉 selector。不要使用 `[aria-pressed=\"true\"]` 或 `[aria-selected=\"true\"]` 作为皮肤 selector。\n\nMenu 的 pointer hover 与键盘导航必须汇入同一个 `focusedEntry`，并在直接 action item 上投射 `.focused`。`menu.css` 只能通过该 class 设置当前项背景；不得同时用 `:hover` 与 `:focus-visible` 驱动两套高亮。`:focus-visible` 只适合表达额外的键盘焦点轮廓。\n\n原生 DOM attribute 同样不作为组件视觉状态 API。组件需要覆盖 author CSS 时，应并行保留原生语义并投影稳定 class，例如 `MenuWorkbenchToolBar` 同时设置 `hidden` 和 `.empty`，CSS 只选择 `.empty`。\n\n需要让选中状态覆盖 hover 时，先写一般 `:hover` 规则，再写同等或更高 specificity 的 `.checked` 规则；不要使用 `:not(.checked):hover` 表达状态优先级。\n\n## 行为身份与视觉身份\n\n行为身份和视觉身份必须使用不同契约：\n\n| 身份 | 表达方式 | 允许用途 | 禁止用途 |\n| --- | --- | --- | --- |\n| 行为身份 | `IAction.id`、`data-action-id` | TypeScript 路由、动作查找、诊断和测试 | CSS selector、颜色、显隐和布局 |\n| 视觉身份 | 组件拥有的稳定 class，例如 `.zeta-tab-close-action` | 组件 CSS、交互状态和 presentation | 命令分派、持久化身份和业务查找 |\n\n组合控件定义某个 item 的语义时，也负责把该语义投影为稳定的视觉 class。底层控件可以继续保留行为 ID，但 CSS 不得把 ID 当作公开样式 API：\n\n```css\n/* 禁止：行为身份泄漏为视觉契约 */\n.zeta-tab-actions [data-action-id=\"zeta.tab.close\"] {\n  visibility: hidden;\n}\n\n/* 允许：TabList 投影并拥有视觉身份 */\n.zeta-tab-close-action {\n  visibility: hidden;\n}\n```\n\n添加视觉 class 不等于需要新的 representation。只有 DOM 结构、交互行为、ARIA 语义或生命周期确实不同，才新增 `ActionViewItem` 等专用表示。仅有颜色、显隐、间距或状态皮肤差异时，由语义 owner 在现有直接托管 shell 上投影 class。只有多个真实消费者需要同一项通用能力时，才扩展 `ActionBar`、`Button` 等基座接口；不要为了单个样式差异增加转发层、模糊选项或具体实现继承。\n\n## Selector 规则\n\nWorkbench 的固定横向几何使用物理方向 CSS。仅需左右 inset 时，写作 `padding: 0 <value>`；不要使用 `padding-inline`、`margin-inline` 或其他 `*-inline` logical property。token 仍表达数值，例如 `padding: 0 var(--zeta-tab-list-item-content-inset)`。\n\n允许组件修改自己的内部结构：\n\n```css\n.zeta-composite-bar-label .zeta-tab {\n  padding: 0 5px;\n}\n```\n\n允许 Part 修改直接托管组件 root 的外部尺寸或位置：\n\n```css\n.zeta-panel-title-control > .zeta-composite-bar {\n  flex: 1 1 auto;\n  min-width: 0;\n}\n```\n\n允许 Part 修改自己的直接内容区：\n\n```css\n.zeta-workbench-panel > .zeta-composite-content {\n  overflow: hidden;\n}\n```\n\n禁止 Part 穿透共享组件内部结构：\n\n```css\n/* 禁止 */\n.zeta-panel-title-control\n  .zeta-tab-list-scroll-content\n  > .zeta-action-bar\n  > .zeta-tab.checked {\n  background: var(--some-color);\n}\n```\n\n如果 host 需要改变 item 的 padding、radius、最小尺寸、hover 或 active 视觉，应回到该组件实现，增加语义明确的 variant。variant 使用枚举或字符串联合表达，不使用含义模糊的布尔参数。\n\n当前示例：\n\n```ts\nexport type CompositeBarPresentation = \"icon\" | \"label\";\n```\n\nSidebar 使用默认 `icon` presentation；Panel 显式选择 `label` presentation。两种 item 的几何及 presentation 专属前景色由 `CompositeBar` 拥有；标准 tab 的选中背景仍归 `TabList`，非选中项透明。\n\n## Panel 规范\n\nPanel 顶部的 Problems、Output、Terminal、Ports 是同一 View Container location 中的互斥目的地，因此语义是 `tablist`，实现为 `CompositeBar`。右侧是当前选中 Panel View 的上下文命令，因此语义是 `toolbar`，实现为 `ToolBar`。\n\n| 区域 | 语义/实现 | 样式 owner |\n| --- | --- | --- |\n| Problems / Output / Terminal / Ports | `TabList` + `CompositeBar` | 标准 tab 的选中背景归 `tablist.css`；label presentation 归 `compositebar.css` |\n| 当前 View 命令 | `WorkbenchToolBar` 或 `MenuWorkbenchToolBar` | action 编排归 toolbar；button 状态归 button |\n| 未显示的 Panel 目的地 | `CompositeBar` 自有 overflow button 与菜单 | `CompositeBar` |\n| title control 左右布局 | `PanelPart` | `panelpart.css` |\n| terminal 实例列表 | Terminal View 内第二级 `TabList` | Terminal contribution/view |\n| pane header 是否显示 | `PaneComposite` presentation | `views.css` |\n\nPanel 不拥有 tab 的 active 下划线，也不拥有 active/hover 背景。选中态应填满 `CompositeBar` item 的命中区域；标准背景由 `TabList` 的 token 与 CSS 决定。\n\n当 Panel 目的地超出 `CompositeBar` 可用宽度时，`CompositeBar` 必须在自己的 action row 中、最后一个可见 tab 之后创建独立的 More button，并将未显示的目的地放入该 button 的菜单。不得把这些目的地注入当前 View 的 `WorkbenchToolBar` 或 `MenuWorkbenchToolBar`；View title toolbar 的 More 只承载该 View 自有 Menu 的 secondary actions。\n\n## Titlebar 规范\n\nTitlebar 是 Workbench Part，不是一个巨型 toolbar。它负责窗口拖拽区、左中右区域编排、应用菜单与窗口控件的占位；其中嵌入的 Button、ToolBar、ActionBar 仍保留各自状态所有权，横向 ActionBar 的 hover 由 Workbench 全局皮肤统一提供。\n\n| Titlebar 内容 | Owner | Titlebar 可以负责 | Titlebar 不负责 |\n| --- | --- | --- | --- |\n| 窗口级网格、拖拽区、左右区域 | `TitlebarPart` | 高度、排列、间距、背景、边界 | 子控件 active/hover 状态 |\n| 应用菜单 | `MenubarControl` | Titlebar 只放置其 root | 菜单 item 内部状态 |\n| 通用图标命令 | `ToolBar` + `Button` + Workbench 全局 ActionBar 皮肤 | toolbar root 的位置 | action hover、button focus/disabled |\n| 窗口控制按钮 | Electron/native integration | 预留布局与主题投影 | 模拟通用 Workbench action 状态 |\n\nTitlebar 可以给直接托管的公共组件 root 设置适配当前背景所需的继承色，但不应改变组件内部状态规则。如果某种前景色必须在不同背景下变化，优先注册语义 token 或公开 presentation，而不是新增深层 selector。\n\n## Token 与 CSS 的边界\n\nDesign token 回答“值是什么”，组件 CSS 回答“何时使用这个值”。\n\n```text\nTheme registry\n  → --zeta-tab-list-active-background\n  → tablist.css 的 .zeta-tab.checked\n```\n\nTheme 不判断某个 tab 是否 active，Part 也不选择 active token。`TabList` 在自己的状态 selector 中消费相应 token；`CompositeBar` 只消费其 presentation 所需的 token。\n\n## 字体层级\n\n字体角色由字号与强调程度两个正交 token 组合，不增加 `body1Strong`、`label1Bold` 之类的复合 token。`fontSize.*` 回答文本处于哪个阅读层级；`fontWeight.*` 回答它是否需要强调。这样同一强调语义能在不同字号间保持一致，也避免把 600 或 400 重新写成局部魔法数。\n\n| 文本语义 | 字号 token | 字重 token |\n| --- | --- | --- |\n| 常规正文 | `fontSize.body1` | `fontWeight.regular` |\n| 常规标签 / 次级标题 | `fontSize.label1` | `fontWeight.regular` |\n| 元数据 | `fontSize.label2` | `fontWeight.regular` |\n| 强调正文 / Pane tab | `fontSize.body1` | `fontWeight.semiBold` |\n| 强调标题 | 对应 `fontSize.heading*` 或 `fontSize.label1` | `fontWeight.semiBold` |\n\n`TabList` 基座拥有 tab label 的统一强调字重 `fontWeight.semiBold` 与标准高度 `tab.height`（24px）；`CompositeBar`、Chat tabs、Terminal tabs 等组合控件通过 presentation 决定字号、行高和内部间距，并只能调整直接托管的 TabList root 以完成对齐。Part 不得用深层 selector 改写这些字体规则。系统仅提供 regular（400）与 strong（600）两级；不引入 700 的第三层级。\n\n新增视觉规则时：\n\n1. 先确认已有语义 token 是否准确。\n2. 没有准确 token 时，在实际消费语义的 domain 注册 token。\n3. 在拥有状态的组件 CSS 中消费 token。\n4. 不为一次 host 覆盖创建位置型 token，例如 `panel-tab-hover-background`；只有产品语义确实不同才拆分。\n\n## 当前实现状态\n\n| 项目 | 状态 |\n| --- | --- |\n| Panel 一级目的地使用 `CompositeBar`/`TabList` | ✅ |\n| Panel 右侧上下文命令使用 `ToolBar` | ✅ |\n| `CompositeBar` 提供 `icon` / `label` presentation | ✅ |\n| Panel tab 几何与状态已从 `panelpart.css` 移回 `compositebar.css` | ✅ |\n| Panel pane header 显示策略由 `PaneComposite` 拥有 | ✅ |\n| tab 与 tabpanel 使用 `aria-controls` / `aria-labelledby` 配对 | ✅ |\n| Workbench Part CSS 共享交互控件 selector 门禁 | ✅ |\n| CSS 禁止使用 ARIA state attribute 作为视觉 selector 的门禁 | ✅ |\n| CSS 禁止使用 `data-action-id` 作为视觉 selector 的门禁 | ✅ |\n| CSS 禁止通过 `:not(.checked)` 等否定投影状态表达优先级 | ✅ |\n| 全仓所有组合控件的深层 selector 自动判定 | 尚未完成 |\n| Button 并行投影 `.checked` 与 `aria-pressed`，CSS 不依赖 ARIA selector | ✅ |\n| ActionBar/ToolBar 通过 `highlightToggledItems` 选择通用 checked 背景 | ✅ |\n| `WorkbenchToolBar` 与 `MenuWorkbenchToolBar` 不改变 base toolbar 样式所有权 | ✅ |\n| `MenuWorkbenchToolBar` 以 `.empty` 投影空状态，CSS 不选择 `hidden` attribute | ✅ |\n\n历史 CSS 中可能仍有不符合本规范的穿透 selector。它们是待迁移实现，不构成新的先例；修改相关区域时应就地迁移到 owner 或公开 variant。\n\n## 审查清单\n\n- 这个 selector 的 root class 是否由当前模块创建？\n- 这个状态是否由当前模块定义？\n- Part 是否只触及子组件 root 的外部尺寸和位置？\n- 相同组件的差异是否通过有名字的 presentation 表达？\n- 是否错误地把 `ActionBar` 当成所有 action 状态的 owner？\n- 是否使用语义 token，而不是硬编码颜色或位置型 token？\n- ARIA 状态、逻辑状态和视觉状态是否由同一个控件链路投影？\n- selector 是否把 `data-action-id` 等行为身份误当成视觉身份？\n- 新增专用 `ActionViewItem` 是否真的改变了 DOM、行为、ARIA 或生命周期，而不只是添加样式 class？\n- 对基座的扩展是否已有多个真实消费者，而不是为单个 host 的皮肤差异预留？\n- 文档描述的是当前行为，还是尚未实现的计划？",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "分层所有权",
        "title": "分层所有权"
      },
      {
        "depth": 2,
        "id": "当前控件的准确职责",
        "title": "当前控件的准确职责"
      },
      {
        "depth": 2,
        "id": "状态归属矩阵",
        "title": "状态归属矩阵"
      },
      {
        "depth": 3,
        "id": "actionbar-与-tablist-的选中态边界",
        "title": "ActionBar 与 TabList 的选中态边界"
      },
      {
        "depth": 2,
        "id": "行为身份与视觉身份",
        "title": "行为身份与视觉身份"
      },
      {
        "depth": 2,
        "id": "selector-规则",
        "title": "Selector 规则"
      },
      {
        "depth": 2,
        "id": "panel-规范",
        "title": "Panel 规范"
      },
      {
        "depth": 2,
        "id": "titlebar-规范",
        "title": "Titlebar 规范"
      },
      {
        "depth": 2,
        "id": "token-与-css-的边界",
        "title": "Token 与 CSS 的边界"
      },
      {
        "depth": 2,
        "id": "字体层级",
        "title": "字体层级"
      },
      {
        "depth": 2,
        "id": "当前实现状态",
        "title": "当前实现状态"
      },
      {
        "depth": 2,
        "id": "审查清单",
        "title": "审查清单"
      }
    ],
    "searchText": " 本文是 Zeta Desktop Renderer 中组件、组合控件与 Workbench Part 样式边界的 canonical 文档。 主题值与 token 注册仍以 design-tokens.md 为准；Desktop 的跨进程和产品所有权仍以 zeta-desktop-architecture.md 为准。 Pane-like Part 的标题层级、槽位、命名和 Composite 生命周期以 workbench-pane-composite-design.md 为准。 Command、MenuId、Context Key 与菜单型 Toolbar 的组合语义以 menu-system.md 为准；本文只拥有它们最终投影到控件后的视觉边界。 快速理解 每一种视觉规则只能有一个 owner。调用方选择组件、传入语义状态或选择公开的 presentation variant；调用方不得通过深层 selector 重写被调用组件的内部 item、hover、active、focus 或 disabled 样式。 判断 owner 时使用下面的顺序： 1. 状态由哪个控件定义，状态视觉就由哪个控件拥有。 2. DOM 由哪个控件创建，内部结构样式就由哪个控件拥有。 3. Part 只拥有区域布局、边界、背景和直接子组件的外部尺寸。 4. 颜色和尺寸值来自 design token；token 不拥有 selector、状态机或 DOM 结构。 5. 同一组件需要不同外观时，增加有名字的 presentation variant，不在 host CSS 中穿透覆盖。 想修改什么 样式所有者 正确做法 --- --- --- 组件内部间距和交互状态 创建该 DOM 和状态的组件 修改组件自己的样式 Workbench 区域布局和边界 对应 Part 只修改直接托管区域的外部盒子 同一组件的另一种正式外观 组件公开的展示变体 增加有名字的变体 主题颜色或标准尺寸 Design Token 修改语义 token，不修改 selector 某个业务动作的显隐 Command/Menu/Context Key 不用 CSS 猜业务状态 分层所有权 层级 当前代表 必须负责 禁止负责 --- --- --- --- Primitive Button、ActionBar、ToolBar、TabList、PaneView 通用 DOM、键盘行为、ARIA、基础布局和 primitive 自身状态 Workbench 区域语义、Panel/Sidebar 特例 Composed control CompositeBar、Editor/Chat tabs control、MenubarControl 领域内 item 几何、presentation variant、hover/active/selected 视觉 Part 的位置、区域背景和网格尺寸 Part TitlebarPart、PanelPart、SidebarPart Part 根节点、标题区/内容区布局、边框、背景、直接子组件占位 深入修改组件内部 .zeta-action-bar、.zeta-tab、.zeta-button 状态 Contribution/View TerminalViewPane、Explorer、Search、Chat 自有内容、命令、View 内部交互和私有子控件 Workbench Part 的全局布局或其他 View 的皮肤 Theme color/size registries 与 CSS custom properties 视觉值、别名、主题覆盖和快照投影 selector、DOM、active/hover 判定 文件位置不是所有权的唯一证据，root class 和构造者才是。Part 私有子控件可以与 Part CSS 共置，但 selector 必须以该私有子控件的 root class 开始，不能借 Part root 任意穿透共享组件。 当前控件的准确职责 控件 当前职责 状态视觉 owner --- --- --- ActionBar action 排列、方向键导航、roving tabindex、item shell；可显式启用 toggled 高亮上下文 不自动决定业务 selected/checked 视觉；Workbench 全局皮肤统一提供横向 action hover Button button DOM、focus-visible、disabled，以及 .checked 与 aria-pressed 的并行状态投影 不默认提供 hover 背景；hover 与 checked 的具体皮肤由 presentation context 决定 Switch track/thumb 结构、on/off、hover、focus、pressed、disabled 的内部 presentation 宿主只提供状态与命名变体，不穿透覆盖内部 track/thumb ContextView 浮层挂载、锚点定位、视口内翻转和裁剪，以及通用浮层外壳 下拉框、提示、选择器和菜单各自的内容结构与交互状态 ToolBar primary/secondary action 编排、22px icon action 尺寸、More Actions、可选 toggled 高亮 Button 负责状态投影；所在 presentation context 决定 hover 皮肤 DropdownWithPrimaryActionViewItem primary 与 dropdown 的组合 DOM、连续几何、内部键盘导航 Workbench 全局 ActionBar 皮肤为整个 split action 提供 hover；产品只提供 actions 和 menu 数据 WorkbenchToolBar 把 platform action representation 适配到 base ToolBar；actions 仍由调用方提供 横向 action hover 归 Workbench 全局皮肤，其余视觉仍归 base ToolBar/Button MenuWorkbenchToolBar 从 MenuId 解析并刷新 WorkbenchToolBar actions Menu 来源不改变 toolbar ARIA 与视觉 owner TabList tablist/tab ARIA、aria-selected、激活回调、.checked 状态投影，以及标准 tab 的选中背景 tablist.css CompositeBar View Container 切换，以及 icon/label 两种 presentation presentation 专属的几何与前景色；不重定义标准 tab 选中背景 PaneComposite tabpanel、pane 生命周期、pane header presentation views.css PaneCompositePart 32px title control、CompositeBar/自有标题与 title actions 的左右槽位 paneCompositePart.css 只拥有外层布局 TitlebarPart 左区、应用菜单、标题和右区的窗口级布局 通用 toolbar/button 状态不归 Titlebar PaneCompositePart 的标题高度为 32px；titleContentElement 占据左侧弹性空间，titleActionsSlotElement 固定在右侧并提供 4px 的双侧 inset。当前 View 的 partTitleProjection.actions 与 Part 级 menu toolbar 使用独立子槽并列；Part CSS 只拥有这些直接槽位的外框与排列，不得穿透修改其中的 toolbar/button 内部状态。 CompositeBar 的 icon presentation item 状态盒为 24px × 24px，hover 与 .checked 使用同一外框。CompositeBar 拥有该 item 几何及 presentation 专属前景色；TabList 提供标准 tab 的稳定状态 class、选中背景，以及由 tabList.itemContentInset 定义的标准内容左右 inset；非选中 tab 保持透明；ActionBar 只在显式启用 toggled 高亮时提供 action 的背景。 状态归属矩阵 状态 判定 owner 样式 owner Host 可以做什么 --- --- --- --- hover 原生 pointer 状态 presentation context；Workbench 横向 ActionBar 由全局 Workbench 皮肤统一处理 选择 presentation；不得在具体 Part 或 contribution 中重写内部 action focus-visible / disabled 原生交互 primitive primitive CSS 选择 primitive；不得重写内部状态 menu 当前项 Menu 统一投射鼠标与键盘焦点为 .focused menu.css 提供 actions；不得用调用方 selector 重建菜单焦点态 tab selected / active TabList 投影状态 TabList 默认皮肤；有独立语义的组合控件通过自己的 token 覆盖 选择 presentation variant，不能复用 ActionBar token toggle checked / pressed command/action model 控件并行投影 .checked 与 ARIA；具体 composed control CSS 决定皮肤 提供 checked 值或显式启用 toggled 高亮；不得在 Part 中猜状态 Part visible/hidden Workbench layout Part/layout CSS contribution 只能请求命令 theme light/dark/high contrast Theme service token snapshot 组件消费 token，不硬编码主题分支 ActionBar 是行为与排列基座，不因为它包裹了 item 就自动拥有业务 selected 皮肤。需要通用 toggled 背景时，由调用方显式启用 highlightToggledItems，ActionBar 再通过 .highlight-toggled .checked selector 应用自己的公开高亮 presentation。 ActionBar 与 TabList 的选中态边界 ActionBar 的 actionBar.toggledBackground 只表达“一个 action 在显式 toggled 上下文中被选中”。TabList 的 tabList.activeBackground 只表达“一个 tab 被选中”；非选中 tab 不设置背景。两者即使当前主题值相同，也必须保持独立 token 和独立 CSS selector；这保证日后主题或交互语义变化时不会产生隐式耦合。 标准 tab 由 tablist.css 消费 tabList.activeBackground token，并保持非选中项透明。CompositeBar 只能追加 icon/label presentation 的几何、间距和前景色，不能覆盖标准 tab 的选中背景。Chat 这类会话 tab 可以通过 chat.tabBackground 定义非选中背景，但必须继承 TabList 的选中背景；不得借用 ActionBar token 或通过 Part 深层 selector 改写 TabList。 和 VS Code 一致，ARIA attribute 用于无障碍语义，稳定 class 用于视觉 selector。不要使用 aria-pressed=\"true\" 或 aria-selected=\"true\" 作为皮肤 selector。 Menu 的 pointer hover 与键盘导航必须汇入同一个 focusedEntry，并在直接 action item 上投射 .focused。menu.css 只能通过该 class 设置当前项背景；不得同时用 :hover 与 :focus-visible 驱动两套高亮。:focus-visible 只适合表达额外的键盘焦点轮廓。 原生 DOM attribute 同样不作为组件视觉状态 API。组件需要覆盖 author CSS 时，应并行保留原生语义并投影稳定 class，例如 MenuWorkbenchToolBar 同时设置 hidden 和 .empty，CSS 只选择 .empty。 需要让选中状态覆盖 hover 时，先写一般 :hover 规则，再写同等或更高 specificity 的 .checked 规则；不要使用 :not .checked :hover 表达状态优先级。 行为身份与视觉身份 行为身份和视觉身份必须使用不同契约： 身份 表达方式 允许用途 禁止用途 --- --- --- --- 行为身份 IAction.id、data-action-id TypeScript 路由、动作查找、诊断和测试 CSS selector、颜色、显隐和布局 视觉身份 组件拥有的稳定 class，例如 .zeta-tab-close-action 组件 CSS、交互状态和 presentation 命令分派、持久化身份和业务查找 组合控件定义某个 item 的语义时，也负责把该语义投影为稳定的视觉 class。底层控件可以继续保留行为 ID，但 CSS 不得把 ID 当作公开样式 API： ``css / 禁止：行为身份泄漏为视觉契约 / .zeta-tab-actions data-action-id=\"zeta.tab.close\" visibility: hidden; / 允许：TabList 投影并拥有视觉身份 / .zeta-tab-close-action visibility: hidden; ` 添加视觉 class 不等于需要新的 representation。只有 DOM 结构、交互行为、ARIA 语义或生命周期确实不同，才新增 ActionViewItem 等专用表示。仅有颜色、显隐、间距或状态皮肤差异时，由语义 owner 在现有直接托管 shell 上投影 class。只有多个真实消费者需要同一项通用能力时，才扩展 ActionBar、Button 等基座接口；不要为了单个样式差异增加转发层、模糊选项或具体实现继承。 Selector 规则 Workbench 的固定横向几何使用物理方向 CSS。仅需左右 inset 时，写作 padding: 0 <value ；不要使用 padding-inline、margin-inline 或其他 -inline logical property。token 仍表达数值，例如 padding: 0 var --zeta-tab-list-item-content-inset 。 允许组件修改自己的内部结构： `css .zeta-composite-bar-label .zeta-tab padding: 0 5px; ` 允许 Part 修改直接托管组件 root 的外部尺寸或位置： `css .zeta-panel-title-control .zeta-composite-bar flex: 1 1 auto; min-width: 0; ` 允许 Part 修改自己的直接内容区： `css .zeta-workbench-panel .zeta-composite-content overflow: hidden; ` 禁止 Part 穿透共享组件内部结构： `css / 禁止 / .zeta-panel-title-control .zeta-tab-list-scroll-content .zeta-action-bar .zeta-tab.checked background: var --some-color ; ` 如果 host 需要改变 item 的 padding、radius、最小尺寸、hover 或 active 视觉，应回到该组件实现，增加语义明确的 variant。variant 使用枚举或字符串联合表达，不使用含义模糊的布尔参数。 当前示例： `ts export type CompositeBarPresentation = \"icon\" \"label\"; ` Sidebar 使用默认 icon presentation；Panel 显式选择 label presentation。两种 item 的几何及 presentation 专属前景色由 CompositeBar 拥有；标准 tab 的选中背景仍归 TabList，非选中项透明。 Panel 规范 Panel 顶部的 Problems、Output、Terminal、Ports 是同一 View Container location 中的互斥目的地，因此语义是 tablist，实现为 CompositeBar。右侧是当前选中 Panel View 的上下文命令，因此语义是 toolbar，实现为 ToolBar。 区域 语义/实现 样式 owner --- --- --- Problems / Output / Terminal / Ports TabList + CompositeBar 标准 tab 的选中背景归 tablist.css；label presentation 归 compositebar.css 当前 View 命令 WorkbenchToolBar 或 MenuWorkbenchToolBar action 编排归 toolbar；button 状态归 button 未显示的 Panel 目的地 CompositeBar 自有 overflow button 与菜单 CompositeBar title control 左右布局 PanelPart panelpart.css terminal 实例列表 Terminal View 内第二级 TabList Terminal contribution/view pane header 是否显示 PaneComposite presentation views.css Panel 不拥有 tab 的 active 下划线，也不拥有 active/hover 背景。选中态应填满 CompositeBar item 的命中区域；标准背景由 TabList 的 token 与 CSS 决定。 当 Panel 目的地超出 CompositeBar 可用宽度时，CompositeBar 必须在自己的 action row 中、最后一个可见 tab 之后创建独立的 More button，并将未显示的目的地放入该 button 的菜单。不得把这些目的地注入当前 View 的 WorkbenchToolBar 或 MenuWorkbenchToolBar；View title toolbar 的 More 只承载该 View 自有 Menu 的 secondary actions。 Titlebar 规范 Titlebar 是 Workbench Part，不是一个巨型 toolbar。它负责窗口拖拽区、左中右区域编排、应用菜单与窗口控件的占位；其中嵌入的 Button、ToolBar、ActionBar 仍保留各自状态所有权，横向 ActionBar 的 hover 由 Workbench 全局皮肤统一提供。 Titlebar 内容 Owner Titlebar 可以负责 Titlebar 不负责 --- --- --- --- 窗口级网格、拖拽区、左右区域 TitlebarPart 高度、排列、间距、背景、边界 子控件 active/hover 状态 应用菜单 MenubarControl Titlebar 只放置其 root 菜单 item 内部状态 通用图标命令 ToolBar + Button + Workbench 全局 ActionBar 皮肤 toolbar root 的位置 action hover、button focus/disabled 窗口控制按钮 Electron/native integration 预留布局与主题投影 模拟通用 Workbench action 状态 Titlebar 可以给直接托管的公共组件 root 设置适配当前背景所需的继承色，但不应改变组件内部状态规则。如果某种前景色必须在不同背景下变化，优先注册语义 token 或公开 presentation，而不是新增深层 selector。 Token 与 CSS 的边界 Design token 回答“值是什么”，组件 CSS 回答“何时使用这个值”。 `text Theme registry → --zeta-tab-list-active-background → tablist.css 的 .zeta-tab.checked ` Theme 不判断某个 tab 是否 active，Part 也不选择 active token。TabList 在自己的状态 selector 中消费相应 token；CompositeBar 只消费其 presentation 所需的 token。 字体层级 字体角色由字号与强调程度两个正交 token 组合，不增加 body1Strong、label1Bold 之类的复合 token。fontSize. 回答文本处于哪个阅读层级；fontWeight. 回答它是否需要强调。这样同一强调语义能在不同字号间保持一致，也避免把 600 或 400 重新写成局部魔法数。 文本语义 字号 token 字重 token --- --- --- 常规正文 fontSize.body1 fontWeight.regular 常规标签 / 次级标题 fontSize.label1 fontWeight.regular 元数据 fontSize.label2 fontWeight.regular 强调正文 / Pane tab fontSize.body1 fontWeight.semiBold 强调标题 对应 fontSize.heading 或 fontSize.label1 fontWeight.semiBold TabList 基座拥有 tab label 的统一强调字重 fontWeight.semiBold 与标准高度 tab.height（24px）；CompositeBar、Chat tabs、Terminal tabs 等组合控件通过 presentation 决定字号、行高和内部间距，并只能调整直接托管的 TabList root 以完成对齐。Part 不得用深层 selector 改写这些字体规则。系统仅提供 regular（400）与 strong（600）两级；不引入 700 的第三层级。 新增视觉规则时： 1. 先确认已有语义 token 是否准确。 2. 没有准确 token 时，在实际消费语义的 domain 注册 token。 3. 在拥有状态的组件 CSS 中消费 token。 4. 不为一次 host 覆盖创建位置型 token，例如 panel-tab-hover-background；只有产品语义确实不同才拆分。 当前实现状态 项目 状态 --- --- Panel 一级目的地使用 CompositeBar/TabList ✅ Panel 右侧上下文命令使用 ToolBar ✅ CompositeBar 提供 icon / label presentation ✅ Panel tab 几何与状态已从 panelpart.css 移回 compositebar.css ✅ Panel pane header 显示策略由 PaneComposite 拥有 ✅ tab 与 tabpanel 使用 aria-controls / aria-labelledby 配对 ✅ Workbench Part CSS 共享交互控件 selector 门禁 ✅ CSS 禁止使用 ARIA state attribute 作为视觉 selector 的门禁 ✅ CSS 禁止使用 data-action-id 作为视觉 selector 的门禁 ✅ CSS 禁止通过 :not .checked 等否定投影状态表达优先级 ✅ 全仓所有组合控件的深层 selector 自动判定 尚未完成 Button 并行投影 .checked 与 aria-pressed，CSS 不依赖 ARIA selector ✅ ActionBar/ToolBar 通过 highlightToggledItems 选择通用 checked 背景 ✅ WorkbenchToolBar 与 MenuWorkbenchToolBar 不改变 base toolbar 样式所有权 ✅ MenuWorkbenchToolBar 以 .empty 投影空状态，CSS 不选择 hidden attribute ✅ 历史 CSS 中可能仍有不符合本规范的穿透 selector。它们是待迁移实现，不构成新的先例；修改相关区域时应就地迁移到 owner 或公开 variant。 审查清单 - 这个 selector 的 root class 是否由当前模块创建？ - 这个状态是否由当前模块定义？ - Part 是否只触及子组件 root 的外部尺寸和位置？ - 相同组件的差异是否通过有名字的 presentation 表达？ - 是否错误地把 ActionBar 当成所有 action 状态的 owner？ - 是否使用语义 token，而不是硬编码颜色或位置型 token？ - ARIA 状态、逻辑状态和视觉状态是否由同一个控件链路投影？ - selector 是否把 data-action-id 等行为身份误当成视觉身份？ - 新增专用 ActionViewItem` 是否真的改变了 DOM、行为、ARIA 或生命周期，而不只是添加样式 class？ - 对基座的扩展是否已有多个真实消费者，而不是为单个 host 的皮肤差异预留？ - 文档描述的是当前行为，还是尚未实现的计划？"
  },
  {
    "slug": "workbench-pane-composite-design",
    "sourcePath": "docs/workbench-pane-composite-design.md",
    "title": "Workbench Pane Composite 设计规范",
    "description": "除 Editor 外，承载 View Container 的 Workbench 区域统一使用 PaneCompositePart 架构。统一的是标题槽位、CompositeBar、标题动作槽位和 retained PaneComposite 生命周期；各 Part 只提供区域约束和明确的展示选择。",
    "group": "界面与体验",
    "markdown": "> 本文是 Zeta Desktop Browser Workbench 中 pane-like Part 的结构、槽位、命名和状态投影的 canonical 文档。\n> Renderer 控件与 CSS 状态所有权以 [`ui-styling-ownership.md`](ui-styling-ownership.md) 为准；Command、MenuId 与 Context Key 组合以 [`menu-system.md`](menu-system.md) 为准；Workbench 整体拓扑和持久化边界以 [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) 为准。\n\n## 快速理解\n\n除 Editor 外，承载 View Container 的 Workbench 区域统一使用 `PaneCompositePart` 架构。统一的是标题槽位、`CompositeBar`、标题动作槽位和 retained `PaneComposite` 生命周期；各 Part 只提供区域约束和明确的展示选择。\n\n| 需求 | Owner | 正确入口 |\n| --- | --- | --- |\n| Part 在 Workbench 中的位置、尺寸和显隐 | `WorkbenchLayout` | 布局状态与 Part 可见性 API |\n| 标题左侧的 View Container 选择 | `PaneCompositePart` 托管的 `CompositeBar` | `compositeBarPresentation`、`compositeBarVisible`、`compositeBarContainerFilter` |\n| 标题右侧的菜单动作 | `PaneCompositePart` 标题动作槽位 | `titleActions` + `MenuId` |\n| 当前 View 的内容与标题投影 | retained `PaneComposite` / View | `contentElement`、原子 `partTitleProjection` |\n| action 的业务显隐和 checked 状态 | Command、Menu、Context Key | 条件菜单项与稳定 `.checked` 状态投影 |\n| hover、focus、选中态和内部 item 几何 | 创建对应 DOM 的控件 | 控件 CSS 或公开 presentation variant |\n\n关键约束：一个选项必须以它配置的 owner 和语义槽位命名。Part 标题右侧的 toolbar 必须叫 `titleActions`，不能因为它与 `CompositeBar` 相邻就叫 `compositeBarActions`。\n\n## 1. 适用范围\n\n本文适用于 `SidebarPart`、`AuxiliarybarPart`、`Agent Sidebar` 和 `PanelPart`。它们都是某个 `ViewContainerLocation` 的 pane-like host。Editor 使用 editor-group 专用架构，不套用本文的 Composite 生命周期。\n\n本文不重新定义：\n\n- CSS selector、交互状态与 design token 的所有权；这些由 `ui-styling-ownership.md` 负责。\n- Command、Menu、Context Key 的注册与求值；这些由 `menu-system.md` 负责。\n- Grid、sash、窗口边缘留白和布局持久化；这些由 `WorkbenchLayout` 及 Desktop 架构负责。\n\n## 2. Canonical 层级\n\n```text\nPaneCompositePart\n├─ titleElement (.zeta-pane-composite-title)\n│  ├─ titleContentElement\n│  │  └─ CompositeBar 或 partTitleProjection.content\n│  └─ titleActionsSlotElement\n│     ├─ viewTitleActionsElement ← partTitleProjection.actions\n│     └─ partTitleActionsElement ← Part 级 MenuWorkbenchToolBar\n└─ contentElement\n   └─ retained PaneComposite\n      └─ ViewPane\n```\n\n`PaneCompositePart` 创建标题的左右槽位并持有 Composite 生命周期。`CompositeBar` 只负责把可用 View Container 投影为可切换 item。`MenuWorkbenchToolBar` 只负责把指定 `MenuId` 投影到标题右侧。`PaneComposite` 负责当前 container 的 pane、内容和 view-owned title control。\n\n| 层级 | 拥有 | 不拥有 |\n| --- | --- | --- |\n| `WorkbenchLayout` | Part 拓扑、尺寸、可见性、sash、持久化 | Part 内部标题和 action item |\n| `PaneCompositePart` | 标题左右槽位、Composite 激活与 retained 生命周期 | 业务 action 的可见条件、控件内部 hover 状态 |\n| `CompositeBar` | container item、激活、overflow、`icon`/`label` presentation | 标题右侧菜单动作、Part 边框和背景 |\n| `MenuWorkbenchToolBar` | 从 `MenuId` 解析 action 并投影到 toolbar | action 业务状态的权威来源、Part 几何 |\n| `PaneComposite` / View | 当前 View 内容、pane 生命周期、私有标题内容 | Workbench 全局布局、其他 Part 的样式 |\n| Contribution | container、view、command 和 menu 声明 | 直接移动 Part DOM 或操作 Grid |\n\n## 3. 命名契约\n\n命名必须表达“谁拥有这个输入”和“它改变哪个语义”，不能描述偶然的视觉邻接关系。\n\n| 名称 | 准确语义 |\n| --- | --- |\n| `titleActions` | 配置 Part 标题右侧的 menu-backed toolbar |\n| `titleActionsSlotElement` | `PaneCompositePart` 创建的标题右侧动作宿主槽位 |\n| `compositeBarPresentation` | 选择 `CompositeBar` 的公开展示变体 |\n| `compositeBarVisible` | 控制整个 `CompositeBar` 是否参与标题布局 |\n| `compositeBarContainerFilter` | Part 对其子 `CompositeBar.containerFilter` 的显式配置 |\n| `containerFilter` | `CompositeBar` 内部选择哪些 container 生成 item |\n| `partTitleProjection` | 当前 View/Composite 原子提供的 `{ content, actions }` 标题投影 |\n\n以下命名和调用方式禁止新增：\n\n- ❌ 用 `compositeBarActions` 表示标题右侧 toolbar。它不是 `CompositeBar` 的子结构。\n- ❌ 用 `sidebarActions`、`agentActions` 等 host 名称掩盖实际槽位语义。\n- ❌ 用 `showHeader` 之类含糊布尔值同时控制 title、bar、item 或 pane header。\n- ❌ 让 contribution 根据 DOM 层级查找并迁移 action item。\n\n当父层只是把配置转发给明确的子组件时，父层选项应保留子组件命名空间，例如 `compositeBarContainerFilter`；进入 `CompositeBarOptions` 后收敛为 `containerFilter`。这样调用点和实现点都能看出配置边界。\n\n## 4. 状态与显隐契约\n\n下面的状态彼此独立，不能由一个布尔值或 CSS selector 代替：\n\n| 状态 | 判定 owner | DOM/CSS 投影 |\n| --- | --- | --- |\n| Part 是否可见 | `WorkbenchLayout` | Part 进入或退出布局 |\n| 标题是否存在 | `PaneCompositePart` | 标题在有 CompositeBar、自有标题内容或标题动作时保留 |\n| `CompositeBar` 是否可见 | `PaneCompositePart` | `compositeBarVisible` 投影到 bar root 的 `hidden` |\n| container 是否生成 item | `CompositeBar` | `containerFilter` 参与 items 计算 |\n| 哪个 Composite 激活 | `PaneCompositePart` / `IViewsService` | `CompositeBar` active item 与 retained content 同步 |\n| menu action 是否可见 | `MenuService` + Context Key | toolbar 刷新对应 action |\n| action 是否 checked | command/action model | `.checked` 与 ARIA 并行投影 |\n\n同一 toggle action 需要在 Part 收起和展开时出现于不同标题区域时，优先注册两个固定 menu 槽位，并以互斥 Context Key 控制显隐。槽位位置保持稳定，action 不在 DOM host 之间迁移，因此不会引入 presentation drift。\n\n## 5. 标题槽位\n\n`titleContentElement` 与 `titleActionsSlotElement` 是 `PaneCompositePart` 创建的同级槽位。当前 View 通过一次 `partTitleProjection` 同时提供左侧 `content` 和右侧 `actions`，两者必定来自同一个 View；前者替换 `CompositeBar` 的视觉，后者进入 `viewTitleActionsElement`。Part 级 `titleActions` 独立进入 `partTitleActionsElement`，与 View actions 并列且不互相覆盖。当前 View 的上下文动作必须进入其所属的固定标题槽位，不得在 View、CompositeBar 或窗口标题栏之间迁移 DOM。\n\n一个 `PaneComposite` 至多允许一个可见 View 提供 `partTitleProjection`；若多个 View 同时提供，运行时立即拒绝该歧义配置，而不会拼接或按顺序挑选不同 View 的标题片段。\n\n标题、toolbar 和 `CompositeBar` 的视觉尺寸、状态 class 与 CSS selector 规则以 [`ui-styling-ownership.md`](ui-styling-ownership.md) 为准。\n\n## 6. 当前 Part 的正式变体\n\n| Part | 标题左槽 | 标题右槽 | 内容生命周期 |\n| --- | --- | --- | --- |\n| Primary Sidebar | 可见的 icon `CompositeBar` | 按需提供 | retained `PaneComposite` |\n| Panel | 可见的 label `CompositeBar` | 当前 Composite 的上下文 actions | retained `PaneComposite` |\n| Auxiliary Bar | 隐藏冗余 `CompositeBar`，投影 Chat `partTitleProjection.content` | 投影 Chat `partTitleProjection.actions` | retained `PaneComposite` |\n| Agent Sidebar | 保留统一标题/CompositeBar host，但过滤唯一冗余 container item | `MenuId.AgentSidebarTitle` | retained `PaneComposite` |\n\nAgent Sidebar 的空 `CompositeBar` root 仍是统一标题结构的一部分，但不创建 `Agent` action item；“Hide Agent Sidebar” 属于标题右侧 `titleActions`。因此它和 Auxiliary Bar 的 title toolbar 使用相同的 Part 标题槽位。\n\n## 7. 禁止模式\n\n- ❌ 为了让一个 action 看起来对齐，把它注册到窗口 `TitlebarPart`。\n- ❌ 在展开/收起时把同一个 action DOM 从 Chat title 搬到 Agent Sidebar title。\n- ❌ 在仍需多个 container item 的 Part 中，隐藏整个 `CompositeBar` 来删除其中一个冗余 item。\n- ❌ 保留空 toolbar 的布局占位，再由 Part CSS 猜测是否应隐藏。\n- ❌ 在 Part CSS 中穿透 `.zeta-action-bar`、`.zeta-button` 或 `.zeta-tab` 调整 hover/checked 视觉。\n- ❌ 让 contribution 直接增删 Grid pane 或读写布局尺寸。\n\n## 8. 修改流程\n\n修改 pane-like Part 时按以下顺序检查：\n\n1. 这是 Workbench 拓扑、Part 外框、标题槽位、CompositeBar、toolbar，还是 View 内容的责任？\n2. 新输入是否以 owner 和语义槽位命名？\n3. 显隐是否拆分为 Part、标题、bar、item、menu action 五类独立状态？\n4. 是否能通过 `MenuId` + Context Key 保持固定槽位，而不是迁移 DOM？\n5. 是否复用了 `PaneCompositePart` 的 retained Composite 生命周期？\n6. CSS 是否停在直接托管子控件的外部盒子，没有穿透共享组件内部？\n7. 是否同步更新本规范、Desktop 架构和相关测试？\n\n当前实现的关键入口是：\n\n- `desktop/src/zeta/workbench/browser/parts/paneCompositePart.ts`\n- `desktop/src/zeta/workbench/browser/parts/sidebar/sidebarPart.ts`\n- `desktop/src/zeta/workbench/browser/parts/auxiliarybar/auxiliarybarPart.ts`\n- `desktop/src/zeta/workbench/browser/parts/panel/panelPart.ts`\n- `desktop/src/zeta/workbench/browser/parts/compositebar/compositeBar.ts`\n- `desktop/src/zeta/workbench/browser/workbench.ts`\n\n相关改动至少运行 Desktop TypeScript 编译，以及 Workbench layout、Chat view、toolbar/action view item 和 UI styling ownership 测试。视觉改动还需要在 Browser Workbench 中验证收起、展开、hover、拖拽与窄宽度 overflow。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-适用范围",
        "title": "1. 适用范围"
      },
      {
        "depth": 2,
        "id": "2-canonical-层级",
        "title": "2. Canonical 层级"
      },
      {
        "depth": 2,
        "id": "3-命名契约",
        "title": "3. 命名契约"
      },
      {
        "depth": 2,
        "id": "4-状态与显隐契约",
        "title": "4. 状态与显隐契约"
      },
      {
        "depth": 2,
        "id": "5-标题槽位",
        "title": "5. 标题槽位"
      },
      {
        "depth": 2,
        "id": "6-当前-part-的正式变体",
        "title": "6. 当前 Part 的正式变体"
      },
      {
        "depth": 2,
        "id": "7-禁止模式",
        "title": "7. 禁止模式"
      },
      {
        "depth": 2,
        "id": "8-修改流程",
        "title": "8. 修改流程"
      }
    ],
    "searchText": " 本文是 Zeta Desktop Browser Workbench 中 pane-like Part 的结构、槽位、命名和状态投影的 canonical 文档。 Renderer 控件与 CSS 状态所有权以 ui-styling-ownership.md 为准；Command、MenuId 与 Context Key 组合以 menu-system.md 为准；Workbench 整体拓扑和持久化边界以 zeta-desktop-architecture.md 为准。 快速理解 除 Editor 外，承载 View Container 的 Workbench 区域统一使用 PaneCompositePart 架构。统一的是标题槽位、CompositeBar、标题动作槽位和 retained PaneComposite 生命周期；各 Part 只提供区域约束和明确的展示选择。 需求 Owner 正确入口 --- --- --- Part 在 Workbench 中的位置、尺寸和显隐 WorkbenchLayout 布局状态与 Part 可见性 API 标题左侧的 View Container 选择 PaneCompositePart 托管的 CompositeBar compositeBarPresentation、compositeBarVisible、compositeBarContainerFilter 标题右侧的菜单动作 PaneCompositePart 标题动作槽位 titleActions + MenuId 当前 View 的内容与标题投影 retained PaneComposite / View contentElement、原子 partTitleProjection action 的业务显隐和 checked 状态 Command、Menu、Context Key 条件菜单项与稳定 .checked 状态投影 hover、focus、选中态和内部 item 几何 创建对应 DOM 的控件 控件 CSS 或公开 presentation variant 关键约束：一个选项必须以它配置的 owner 和语义槽位命名。Part 标题右侧的 toolbar 必须叫 titleActions，不能因为它与 CompositeBar 相邻就叫 compositeBarActions。 1. 适用范围 本文适用于 SidebarPart、AuxiliarybarPart、Agent Sidebar 和 PanelPart。它们都是某个 ViewContainerLocation 的 pane-like host。Editor 使用 editor-group 专用架构，不套用本文的 Composite 生命周期。 本文不重新定义： - CSS selector、交互状态与 design token 的所有权；这些由 ui-styling-ownership.md 负责。 - Command、Menu、Context Key 的注册与求值；这些由 menu-system.md 负责。 - Grid、sash、窗口边缘留白和布局持久化；这些由 WorkbenchLayout 及 Desktop 架构负责。 2. Canonical 层级 ``text PaneCompositePart ├─ titleElement .zeta-pane-composite-title │ ├─ titleContentElement │ │ └─ CompositeBar 或 partTitleProjection.content │ └─ titleActionsSlotElement │ ├─ viewTitleActionsElement ← partTitleProjection.actions │ └─ partTitleActionsElement ← Part 级 MenuWorkbenchToolBar └─ contentElement └─ retained PaneComposite └─ ViewPane ` PaneCompositePart 创建标题的左右槽位并持有 Composite 生命周期。CompositeBar 只负责把可用 View Container 投影为可切换 item。MenuWorkbenchToolBar 只负责把指定 MenuId 投影到标题右侧。PaneComposite 负责当前 container 的 pane、内容和 view-owned title control。 层级 拥有 不拥有 --- --- --- WorkbenchLayout Part 拓扑、尺寸、可见性、sash、持久化 Part 内部标题和 action item PaneCompositePart 标题左右槽位、Composite 激活与 retained 生命周期 业务 action 的可见条件、控件内部 hover 状态 CompositeBar container item、激活、overflow、icon/label presentation 标题右侧菜单动作、Part 边框和背景 MenuWorkbenchToolBar 从 MenuId 解析 action 并投影到 toolbar action 业务状态的权威来源、Part 几何 PaneComposite / View 当前 View 内容、pane 生命周期、私有标题内容 Workbench 全局布局、其他 Part 的样式 Contribution container、view、command 和 menu 声明 直接移动 Part DOM 或操作 Grid 3. 命名契约 命名必须表达“谁拥有这个输入”和“它改变哪个语义”，不能描述偶然的视觉邻接关系。 名称 准确语义 --- --- titleActions 配置 Part 标题右侧的 menu-backed toolbar titleActionsSlotElement PaneCompositePart 创建的标题右侧动作宿主槽位 compositeBarPresentation 选择 CompositeBar 的公开展示变体 compositeBarVisible 控制整个 CompositeBar 是否参与标题布局 compositeBarContainerFilter Part 对其子 CompositeBar.containerFilter 的显式配置 containerFilter CompositeBar 内部选择哪些 container 生成 item partTitleProjection 当前 View/Composite 原子提供的 content, actions 标题投影 以下命名和调用方式禁止新增： - ❌ 用 compositeBarActions 表示标题右侧 toolbar。它不是 CompositeBar 的子结构。 - ❌ 用 sidebarActions、agentActions 等 host 名称掩盖实际槽位语义。 - ❌ 用 showHeader 之类含糊布尔值同时控制 title、bar、item 或 pane header。 - ❌ 让 contribution 根据 DOM 层级查找并迁移 action item。 当父层只是把配置转发给明确的子组件时，父层选项应保留子组件命名空间，例如 compositeBarContainerFilter；进入 CompositeBarOptions 后收敛为 containerFilter。这样调用点和实现点都能看出配置边界。 4. 状态与显隐契约 下面的状态彼此独立，不能由一个布尔值或 CSS selector 代替： 状态 判定 owner DOM/CSS 投影 --- --- --- Part 是否可见 WorkbenchLayout Part 进入或退出布局 标题是否存在 PaneCompositePart 标题在有 CompositeBar、自有标题内容或标题动作时保留 CompositeBar 是否可见 PaneCompositePart compositeBarVisible 投影到 bar root 的 hidden container 是否生成 item CompositeBar containerFilter 参与 items 计算 哪个 Composite 激活 PaneCompositePart / IViewsService CompositeBar active item 与 retained content 同步 menu action 是否可见 MenuService + Context Key toolbar 刷新对应 action action 是否 checked command/action model .checked 与 ARIA 并行投影 同一 toggle action 需要在 Part 收起和展开时出现于不同标题区域时，优先注册两个固定 menu 槽位，并以互斥 Context Key 控制显隐。槽位位置保持稳定，action 不在 DOM host 之间迁移，因此不会引入 presentation drift。 5. 标题槽位 titleContentElement 与 titleActionsSlotElement 是 PaneCompositePart 创建的同级槽位。当前 View 通过一次 partTitleProjection 同时提供左侧 content 和右侧 actions，两者必定来自同一个 View；前者替换 CompositeBar 的视觉，后者进入 viewTitleActionsElement。Part 级 titleActions 独立进入 partTitleActionsElement，与 View actions 并列且不互相覆盖。当前 View 的上下文动作必须进入其所属的固定标题槽位，不得在 View、CompositeBar 或窗口标题栏之间迁移 DOM。 一个 PaneComposite 至多允许一个可见 View 提供 partTitleProjection；若多个 View 同时提供，运行时立即拒绝该歧义配置，而不会拼接或按顺序挑选不同 View 的标题片段。 标题、toolbar 和 CompositeBar 的视觉尺寸、状态 class 与 CSS selector 规则以 ui-styling-ownership.md 为准。 6. 当前 Part 的正式变体 Part 标题左槽 标题右槽 内容生命周期 --- --- --- --- Primary Sidebar 可见的 icon CompositeBar 按需提供 retained PaneComposite Panel 可见的 label CompositeBar 当前 Composite 的上下文 actions retained PaneComposite Auxiliary Bar 隐藏冗余 CompositeBar，投影 Chat partTitleProjection.content 投影 Chat partTitleProjection.actions retained PaneComposite Agent Sidebar 保留统一标题/CompositeBar host，但过滤唯一冗余 container item MenuId.AgentSidebarTitle retained PaneComposite Agent Sidebar 的空 CompositeBar root 仍是统一标题结构的一部分，但不创建 Agent action item；“Hide Agent Sidebar” 属于标题右侧 titleActions。因此它和 Auxiliary Bar 的 title toolbar 使用相同的 Part 标题槽位。 7. 禁止模式 - ❌ 为了让一个 action 看起来对齐，把它注册到窗口 TitlebarPart。 - ❌ 在展开/收起时把同一个 action DOM 从 Chat title 搬到 Agent Sidebar title。 - ❌ 在仍需多个 container item 的 Part 中，隐藏整个 CompositeBar 来删除其中一个冗余 item。 - ❌ 保留空 toolbar 的布局占位，再由 Part CSS 猜测是否应隐藏。 - ❌ 在 Part CSS 中穿透 .zeta-action-bar、.zeta-button 或 .zeta-tab 调整 hover/checked 视觉。 - ❌ 让 contribution 直接增删 Grid pane 或读写布局尺寸。 8. 修改流程 修改 pane-like Part 时按以下顺序检查： 1. 这是 Workbench 拓扑、Part 外框、标题槽位、CompositeBar、toolbar，还是 View 内容的责任？ 2. 新输入是否以 owner 和语义槽位命名？ 3. 显隐是否拆分为 Part、标题、bar、item、menu action 五类独立状态？ 4. 是否能通过 MenuId + Context Key 保持固定槽位，而不是迁移 DOM？ 5. 是否复用了 PaneCompositePart 的 retained Composite 生命周期？ 6. CSS 是否停在直接托管子控件的外部盒子，没有穿透共享组件内部？ 7. 是否同步更新本规范、Desktop 架构和相关测试？ 当前实现的关键入口是： - desktop/src/zeta/workbench/browser/parts/paneCompositePart.ts - desktop/src/zeta/workbench/browser/parts/sidebar/sidebarPart.ts - desktop/src/zeta/workbench/browser/parts/auxiliarybar/auxiliarybarPart.ts - desktop/src/zeta/workbench/browser/parts/panel/panelPart.ts - desktop/src/zeta/workbench/browser/parts/compositebar/compositeBar.ts - desktop/src/zeta/workbench/browser/workbench.ts` 相关改动至少运行 Desktop TypeScript 编译，以及 Workbench layout、Chat view、toolbar/action view item 和 UI styling ownership 测试。视觉改动还需要在 Browser Workbench 中验证收起、展开、hover、拖拽与窄宽度 overflow。"
  },
  {
    "slug": "design-tokens",
    "sourcePath": "docs/design-tokens.md",
    "title": "Design Token：主题系统边界与演进",
    "description": "Zeta 采用“单一声明目录、共享版本化 contract、各宿主独立投影”的模型。Desktop TypeScript registry 是 token 声明的 authoring authority；构建器把保留 alias/default graph 的 manifest、Schema 和模板生成到 resources/design-tokens/。Desktop …",
    "group": "界面与体验",
    "markdown": "> 本文是 Zeta 主题与 design token 跨模块架构的 canonical 文档。完整 token 清单由构建器生成在 [`resources/design-tokens/design-tokens.md`](../resources/design-tokens/design-tokens.md)。\n> 新增具体主题请使用 [`theme-authoring-template.md`](theme-authoring-template.md)。\n> selector、交互状态与组件/Part CSS 的 canonical 所有权见 [`ui-styling-ownership.md`](ui-styling-ownership.md)。\n\n## 快速理解\n\nZeta 采用“单一声明目录、共享版本化 contract、各宿主独立投影”的模型。Desktop TypeScript registry 是 token 声明的 authoring authority；构建器把保留 alias/default graph 的 manifest、Schema 和模板生成到 `resources/design-tokens/`。Desktop resolver 与 Rust `zeta-theme` 都读取这份 contract，分别产生不可变快照，再投影给 CSS、Aster CodeEditor、Native UI 或 Ratatui。`system` 只选择具体明暗方案，不是第四套主题数据。\n\n| 想改变什么 | 应该修改哪里 | 不应该怎么做 |\n| --- | --- | --- |\n| 一个组件的语义颜色 | 修改或新增该组件所有者注册的 token | 在组件 CSS 中复制十六进制颜色 |\n| 整套主题外观 | 切换主题入口或覆盖公开 token | 重写组件 selector |\n| Native/CodeEditor 语法颜色 | 覆盖 `editor.token.*Foreground` | 让 parser 或 CodeEditor token 携带固定 RGB |\n| TUI 外观 | 覆盖 TUI 消费的共享子集；必要时设置 `tui.colorTheme` | 要求终端实现全部 Desktop 视觉能力 |\n| 跟随操作系统明暗模式 | 选择 `system` | 维护第四套 system 主题值 |\n| 增加新的视觉值类型 | 有真实跨组件消费者后增加独立注册表 | 把阴影、字体或动效伪装成颜色 |\n\n## 所有权\n\n| 边界 | Owner | 职责 |\n| --- | --- | --- |\n| 通用 RGBA 运算 | `src/zeta/base/common/color.ts` | 解析、混合、透明度和字符串化；不感知主题或 workbench |\n| token 定义与依赖解析 | `platform/theme/common` | 注册颜色/尺寸、拒绝重复 ID、解析别名与变换、检测循环和无效引用 |\n| token domain | `platform/theme/common/colors`、`sizes` | 按消费语义声明 token、默认值、owner 和说明 |\n| 语言中立 contract | `resources/design-tokens/` | 保存版本化 manifest、主题入口、用户主题 Schema/模板和跨运行时 conformance fixture |\n| Desktop 快照 | `colorTheme.ts` | 将 scheme、覆盖值和注册目录编译为只读颜色/尺寸表 |\n| Rust 快照与加载 | `zeta-rs/theme` | 嵌入同一 manifest，严格解析用户 JSON，选择 Graphical/Terminal 偏好并产生 RGBA snapshot |\n| 宿主投影 | Desktop theme binding、Native `shell_style`、TUI `ui/theme` | 把 snapshot 转成宿主组件公开的 palette/style；不注册新的产品语义 |\n| device-local 偏好 | `configuration.json` 与 `themes/*.json` | `workbench.colorTheme` 供图形宿主，`tui.colorTheme` 可选且回退到前者；不进入 Agent config/store |\n| 离线治理 | `tokenCompiler.ts` 与 `scripts/compile-design-tokens.mjs` | 校验所有 scheme，生成 manifest、Schema、模板和目录 |\n\n`src/zeta/base` 不引用主题平台或 workbench。颜色数学保持通用，注册、用户偏好和产品语义都位于更高层。\n\n## 端到端模型\n\n```mermaid\nflowchart LR\n  A[\"Domain registerColor / registerSize\"] --> B[\"TypeScript registries\"]\n  B --> C[\"Build-time compiler\"]\n  C --> D[\"Versioned manifest + Schema\"]\n  B --> E[\"Desktop resolver\"]\n  D --> F[\"zeta-theme resolver\"]\n  G[\"Device preference + user JSON\"] --> E\n  G --> F\n  K[\"Built-in theme entry\"] --> F\n  E --> H[\"CSS / Aster / xterm\"]\n  F --> I[\"Native component palettes\"]\n  F --> J[\"TUI token subset + capability downgrade\"]\n```\n\n注册表保留声明顺序，因此生成物与 CSS 投影稳定。颜色引用可以指向另一颜色 token，也可以使用透明、明暗、混合和不透明化变换。解析采用带路径的深度优先遍历；未知引用、未知覆盖、重复 ID、循环依赖或透明度契约不满足都会失败，而不是静默回退。\n\n## 调用方契约\n\n- 新语义必须注册新 ID；组件不能通过复制某个十六进制值表达“看起来一样”。\n- 一个 token 只有一个 owner。owner 负责默认值、描述、弃用路径和视觉回归。\n- ID 采用点分语义命名，CSS 名称由系统稳定生成，例如 `button.primaryBackground` 对应 `--zeta-button-primary-background`。\n- CSS 消费颜色与标准标量尺寸，包括布局尺寸、字体大小和字重；需要颜色值的 JavaScript 消费者使用 `IColorTheme.getColor()` 或 `getColorCss()`。\n- 主题是已解析快照。消费层不能修改快照，也不能自行解释别名或变换。\n- parser/highlighter 只发布 `CodeEditorTokenRole`；颜色在绘制时由当前 `CodeEditorStyle` 解析，因此换主题不要求重新分析文本。\n- 新注册或默认值变更后运行 `pnpm tokens:generate`，提交 `resources/design-tokens/` 生成物。\n\n## 当前状态/已实现\n\n- `light`、`dark` 与跟随操作系统的 `system` 偏好。\n- 语言中立的 `theme-entries.json` 为 Rust `ThemeLoader` 提供 `zeta`（Electron Desktop）、\n  `zeta-code`（TUI）与 `zeterm`（纯 Rust Desktop）默认入口；入口只覆盖统一 token，不创建产品\n  token 或组件分支。Zeta Code 另有 `zeta-code-colorblind` 与 `zeta-code-ansi` 入口；前者把 diff\n  成功/失败从红绿对改成蓝橙对，后者由 TUI 强制投影为 ANSI 16 色。标准与 colorblind syntax/diff\n  角色取值跟随 [GitHub VS Code theme](https://github.com/primer/github-vscode-theme) 和\n  [Primer functional color](https://www.primer.style/product/primitives/color/) 的经典语义，但值已固化在\n  Zeta 自己的 token entry 中，不形成运行时主题依赖。`zeta-code` 保留蓝紫 highlight；TUI 的\n  `system` 默认必须通过 `with_default_entry(\"zeta-code\")` 选择它。\n- Desktop、Native 和 TUI 使用同一 device root 下的 `configuration.json` 与 `themes/*.json`；每个错误文件独立隔离，内置主题始终可回退。Native `zeterm` 在没有显式用户主题时选择 `zeterm` 入口。\n- 127 个语义颜色 token 与 23 个标准标量尺寸 token；四种 `ColorScheme` 均在编译期解析，高对比度当前继承对应明暗默认值。\n- 不可变颜色对象、注册贡献、主题快照和生成产物。\n- Aster/Native CodeEditor 使用 source-neutral `editor.token.*` 角色；旧 `editor.semanticToken.*` 仅作为兼容覆盖入口。\n- Native shell、composer、terminal ANSI、scrollbar 和 multi-diff editor 已由共享 snapshot 构造组件 palette；没有宿主 selector 或 parser 固定色。\n- TUI chrome 只消费 accent、chrome、error/success/warning、muted 和 highlight；Theme Pane preview\n  额外消费有限的 syntax/diff 子集，并按 TrueColor、ANSI-256、ANSI-16、Monochrome 确定性降级。\n- Desktop Terminal 使用完整 terminal 前景、背景、光标和 ANSI 16 色 token；legacy editor runtime 仅保留迁移期兼容同步，Aster Text Engine 是默认文本编辑器。\n- Electron renderer 通过受校验的 window-theme IPC 将标题栏背景和按钮颜色投影到主进程；Terminal canvas 使用当前编辑器背景，不依赖 xterm 黑色回退。\n- Desktop 与 Rust resolver 共同执行 `theme-conformance.json`，防止 alias、变换、量化或兼容映射发生跨语言漂移。\n\n## 当前状态 limitation /当前限制\n\n- 高对比度使用明暗默认值回退，尚未提供独立视觉设计；类型、解析路径和 manifest 已保留独立 scheme。\n- Desktop 的保存/预览可即时更新；Native 与 TUI 当前在进程启动时加载一次，外部文件修改需要重启对应宿主。\n  这是当前宿主生命周期边界；`zeta code` 产品文档未要求外部主题热重载，因此不构成 TUI backlog。\n- `tui.colorTheme` 当前有 typed configuration contract，但尚无 Settings UI；TUI 可用 `/theme` 打开\n  不带搜索的八项 Zeta Code Theme Pane，用 Enter 原子保存、即时切换并返回主界面，也可用 `/theme <id>` 快速\n  切换；Theme Pane 的 syntax/diff preview 读取同一 resolved snapshot，切换不追加 transcript notice；\n  缺失时回退到 `workbench.colorTheme`。\n- 字体族和行高仍由平台或组件 CSS 管理；字体大小与字重作为 `fontSize.*`、`fontWeight.*` 标量尺寸注册。\n- 颜色和标量尺寸是当前已实现 token kind；阴影和动效应在出现真实跨组件消费者后增加独立注册表，不能伪装成颜色或尺寸。\n- 用户主题只能覆盖已封存 catalog 中的 token，不能在 JSON 中注册新的产品语义。运行期动态插件如果需要新增 token，应先引入显式 catalog revision 与快照重编译，不应直接让旧快照变为可变对象。\n\n## 长期不变量\n\n- base 层保持领域无关且不存在反向依赖。\n- token ID、CSS 投影名称和 owner 变更属于兼容性变更。\n- 同一宿主内所有组件必须消费同一主题快照；不同语言 runtime 必须消费同一版本化 manifest 并通过共享 fixture 验证一致性。\n- TS registry 是声明 authority，生成 manifest 是跨语言 runtime contract；Rust 不维护第二份默认颜色表，宿主中的常量只能作为 manifest 无法加载时的安全 fallback。\n- TUI 部分接入是明确 contract，不是未完成的 Desktop 复制：新增 TUI token 消费必须有真实终端语义并定义能力降级。\n- 未知、循环或不完整数据必须快速失败；不存在“缺失时猜一个颜色”的恢复语义。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "端到端模型",
        "title": "端到端模型"
      },
      {
        "depth": 2,
        "id": "调用方契约",
        "title": "调用方契约"
      },
      {
        "depth": 2,
        "id": "当前状态已实现",
        "title": "当前状态/已实现"
      },
      {
        "depth": 2,
        "id": "当前状态-limitation-当前限制",
        "title": "当前状态 limitation /当前限制"
      },
      {
        "depth": 2,
        "id": "长期不变量",
        "title": "长期不变量"
      }
    ],
    "searchText": " 本文是 Zeta 主题与 design token 跨模块架构的 canonical 文档。完整 token 清单由构建器生成在 resources/design-tokens/design-tokens.md。 新增具体主题请使用 theme-authoring-template.md。 selector、交互状态与组件/Part CSS 的 canonical 所有权见 ui-styling-ownership.md。 快速理解 Zeta 采用“单一声明目录、共享版本化 contract、各宿主独立投影”的模型。Desktop TypeScript registry 是 token 声明的 authoring authority；构建器把保留 alias/default graph 的 manifest、Schema 和模板生成到 resources/design-tokens/。Desktop resolver 与 Rust zeta-theme 都读取这份 contract，分别产生不可变快照，再投影给 CSS、Aster CodeEditor、Native UI 或 Ratatui。system 只选择具体明暗方案，不是第四套主题数据。 想改变什么 应该修改哪里 不应该怎么做 --- --- --- 一个组件的语义颜色 修改或新增该组件所有者注册的 token 在组件 CSS 中复制十六进制颜色 整套主题外观 切换主题入口或覆盖公开 token 重写组件 selector Native/CodeEditor 语法颜色 覆盖 editor.token.Foreground 让 parser 或 CodeEditor token 携带固定 RGB TUI 外观 覆盖 TUI 消费的共享子集；必要时设置 tui.colorTheme 要求终端实现全部 Desktop 视觉能力 跟随操作系统明暗模式 选择 system 维护第四套 system 主题值 增加新的视觉值类型 有真实跨组件消费者后增加独立注册表 把阴影、字体或动效伪装成颜色 所有权 边界 Owner 职责 --- --- --- 通用 RGBA 运算 src/zeta/base/common/color.ts 解析、混合、透明度和字符串化；不感知主题或 workbench token 定义与依赖解析 platform/theme/common 注册颜色/尺寸、拒绝重复 ID、解析别名与变换、检测循环和无效引用 token domain platform/theme/common/colors、sizes 按消费语义声明 token、默认值、owner 和说明 语言中立 contract resources/design-tokens/ 保存版本化 manifest、主题入口、用户主题 Schema/模板和跨运行时 conformance fixture Desktop 快照 colorTheme.ts 将 scheme、覆盖值和注册目录编译为只读颜色/尺寸表 Rust 快照与加载 zeta-rs/theme 嵌入同一 manifest，严格解析用户 JSON，选择 Graphical/Terminal 偏好并产生 RGBA snapshot 宿主投影 Desktop theme binding、Native shellstyle、TUI ui/theme 把 snapshot 转成宿主组件公开的 palette/style；不注册新的产品语义 device-local 偏好 configuration.json 与 themes/.json workbench.colorTheme 供图形宿主，tui.colorTheme 可选且回退到前者；不进入 Agent config/store 离线治理 tokenCompiler.ts 与 scripts/compile-design-tokens.mjs 校验所有 scheme，生成 manifest、Schema、模板和目录 src/zeta/base 不引用主题平台或 workbench。颜色数学保持通用，注册、用户偏好和产品语义都位于更高层。 端到端模型 ``mermaid flowchart LR A \"Domain registerColor / registerSize\" -- B \"TypeScript registries\" B -- C \"Build-time compiler\" C -- D \"Versioned manifest + Schema\" B -- E \"Desktop resolver\" D -- F \"zeta-theme resolver\" G \"Device preference + user JSON\" -- E G -- F K \"Built-in theme entry\" -- F E -- H \"CSS / Aster / xterm\" F -- I \"Native component palettes\" F -- J \"TUI token subset + capability downgrade\" ` 注册表保留声明顺序，因此生成物与 CSS 投影稳定。颜色引用可以指向另一颜色 token，也可以使用透明、明暗、混合和不透明化变换。解析采用带路径的深度优先遍历；未知引用、未知覆盖、重复 ID、循环依赖或透明度契约不满足都会失败，而不是静默回退。 调用方契约 - 新语义必须注册新 ID；组件不能通过复制某个十六进制值表达“看起来一样”。 - 一个 token 只有一个 owner。owner 负责默认值、描述、弃用路径和视觉回归。 - ID 采用点分语义命名，CSS 名称由系统稳定生成，例如 button.primaryBackground 对应 --zeta-button-primary-background。 - CSS 消费颜色与标准标量尺寸，包括布局尺寸、字体大小和字重；需要颜色值的 JavaScript 消费者使用 IColorTheme.getColor 或 getColorCss 。 - 主题是已解析快照。消费层不能修改快照，也不能自行解释别名或变换。 - parser/highlighter 只发布 CodeEditorTokenRole；颜色在绘制时由当前 CodeEditorStyle 解析，因此换主题不要求重新分析文本。 - 新注册或默认值变更后运行 pnpm tokens:generate，提交 resources/design-tokens/ 生成物。 当前状态/已实现 - light、dark 与跟随操作系统的 system 偏好。 - 语言中立的 theme-entries.json 为 Rust ThemeLoader 提供 zeta（Electron Desktop）、 zeta-code（TUI）与 zeterm（纯 Rust Desktop）默认入口；入口只覆盖统一 token，不创建产品 token 或组件分支。Zeta Code 另有 zeta-code-colorblind 与 zeta-code-ansi 入口；前者把 diff 成功/失败从红绿对改成蓝橙对，后者由 TUI 强制投影为 ANSI 16 色。标准与 colorblind syntax/diff 角色取值跟随 GitHub VS Code theme 和 Primer functional color 的经典语义，但值已固化在 Zeta 自己的 token entry 中，不形成运行时主题依赖。zeta-code 保留蓝紫 highlight；TUI 的 system 默认必须通过 withdefaultentry \"zeta-code\" 选择它。 - Desktop、Native 和 TUI 使用同一 device root 下的 configuration.json 与 themes/.json；每个错误文件独立隔离，内置主题始终可回退。Native zeterm 在没有显式用户主题时选择 zeterm 入口。 - 127 个语义颜色 token 与 23 个标准标量尺寸 token；四种 ColorScheme 均在编译期解析，高对比度当前继承对应明暗默认值。 - 不可变颜色对象、注册贡献、主题快照和生成产物。 - Aster/Native CodeEditor 使用 source-neutral editor.token. 角色；旧 editor.semanticToken. 仅作为兼容覆盖入口。 - Native shell、composer、terminal ANSI、scrollbar 和 multi-diff editor 已由共享 snapshot 构造组件 palette；没有宿主 selector 或 parser 固定色。 - TUI chrome 只消费 accent、chrome、error/success/warning、muted 和 highlight；Theme Pane preview 额外消费有限的 syntax/diff 子集，并按 TrueColor、ANSI-256、ANSI-16、Monochrome 确定性降级。 - Desktop Terminal 使用完整 terminal 前景、背景、光标和 ANSI 16 色 token；legacy editor runtime 仅保留迁移期兼容同步，Aster Text Engine 是默认文本编辑器。 - Electron renderer 通过受校验的 window-theme IPC 将标题栏背景和按钮颜色投影到主进程；Terminal canvas 使用当前编辑器背景，不依赖 xterm 黑色回退。 - Desktop 与 Rust resolver 共同执行 theme-conformance.json，防止 alias、变换、量化或兼容映射发生跨语言漂移。 当前状态 limitation /当前限制 - 高对比度使用明暗默认值回退，尚未提供独立视觉设计；类型、解析路径和 manifest 已保留独立 scheme。 - Desktop 的保存/预览可即时更新；Native 与 TUI 当前在进程启动时加载一次，外部文件修改需要重启对应宿主。 这是当前宿主生命周期边界；zeta code 产品文档未要求外部主题热重载，因此不构成 TUI backlog。 - tui.colorTheme 当前有 typed configuration contract，但尚无 Settings UI；TUI 可用 /theme 打开 不带搜索的八项 Zeta Code Theme Pane，用 Enter 原子保存、即时切换并返回主界面，也可用 /theme <id 快速 切换；Theme Pane 的 syntax/diff preview 读取同一 resolved snapshot，切换不追加 transcript notice； 缺失时回退到 workbench.colorTheme。 - 字体族和行高仍由平台或组件 CSS 管理；字体大小与字重作为 fontSize.、fontWeight.` 标量尺寸注册。 - 颜色和标量尺寸是当前已实现 token kind；阴影和动效应在出现真实跨组件消费者后增加独立注册表，不能伪装成颜色或尺寸。 - 用户主题只能覆盖已封存 catalog 中的 token，不能在 JSON 中注册新的产品语义。运行期动态插件如果需要新增 token，应先引入显式 catalog revision 与快照重编译，不应直接让旧快照变为可变对象。 长期不变量 - base 层保持领域无关且不存在反向依赖。 - token ID、CSS 投影名称和 owner 变更属于兼容性变更。 - 同一宿主内所有组件必须消费同一主题快照；不同语言 runtime 必须消费同一版本化 manifest 并通过共享 fixture 验证一致性。 - TS registry 是声明 authority，生成 manifest 是跨语言 runtime contract；Rust 不维护第二份默认颜色表，宿主中的常量只能作为 manifest 无法加载时的安全 fallback。 - TUI 部分接入是明确 contract，不是未完成的 Desktop 复制：新增 TUI token 消费必须有真实终端语义并定义能力降级。 - 未知、循环或不完整数据必须快速失败；不存在“缺失时猜一个颜色”的恢复语义。"
  },
  {
    "slug": "theme-authoring-template",
    "sourcePath": "docs/theme-authoring-template.md",
    "title": "用户主题 JSON 模板",
    "description": "创建主题最简单的方法是在 Settings → Appearance 中从当前主题另存一份，再只修改需要变化的 语义颜色。主题文件不需要复制完整颜色表；未覆盖的颜色会继续使用所选明暗方案的默认值。",
    "group": "界面与体验",
    "markdown": "> 本文是可安装用户主题的 canonical 说明。可以直接复制并修改 [`color-theme.template.json`](../resources/design-tokens/color-theme.template.json)；架构与可靠性边界见 [`design-tokens.md`](design-tokens.md)，可用 token 见[生成目录](../resources/design-tokens/design-tokens.md)，格式 Schema 见 [`color-theme.schema.json`](../resources/design-tokens/color-theme.schema.json)。\n\n## 快速理解\n\n创建主题最简单的方法是在 Settings → Appearance 中从当前主题另存一份，再只修改需要变化的\n语义颜色。主题文件不需要复制完整颜色表；未覆盖的颜色会继续使用所选明暗方案的默认值。\n\n| 想做什么 | 推荐方式 | 生效方式 |\n| --- | --- | --- |\n| 从当前外观开始修改 | 使用“Create from current theme” | 保存后立即预览和切换 |\n| 安装别人提供的主题 | 把 JSON 放入用户主题目录 | 完全重启后加载 |\n| 更新已有主题 | 在设置中保存，或替换同名文件 | 设置内保存立即生效；外部替换需重启 |\n| 删除主题 | 在设置中删除，或移除对应文件 | 自动回到内置明暗主题 |\n| 修复加载失败 | 根据 Appearance 中的错误修改 JSON | 其他有效主题不受影响 |\n\n## 从当前主题创建\n\n推荐在 Settings → Appearance 中操作：\n\n- 当前选择 Light 时，“Create from current theme”会生成完整的 Light JSON；Dark 同理。\n- System 会采用当前操作系统实际生效的 Light 或 Dark。\n- 有效 JSON 会即时预览；取消或关闭 Settings 会恢复编辑前的主题。\n- 内置 Light/Dark 只能使用“Save As”创建新主题，不会被覆盖。\n- 用户主题可以直接“Save”，也可以修改 `id` 和 `label` 后“Save As”。\n- 用户主题可以在 JSON 编辑器中“Delete”；确认后删除文件并按原主题明暗类型切回 Zeta Light 或 Zeta Dark。\n- 保存成功后 JSON 会立即注册、切换并写入用户主题目录，不需要重启。\n\n## 文件安装与卸载\n\nZeta 宿主读取 device root 的 `themes` 目录中的常规 `*.json` 文件。Desktop 的 device root 是 Electron `userData`；Native/TUI 使用相同平台目录（macOS 为 `~/Library/Application Support/Zeta`，Windows 为 `%APPDATA%/Zeta`，Linux 为 `$XDG_CONFIG_HOME/zeta` 或 `~/.config/zeta`）。测试和开发可用 `ZETA_DEVICE_ROOT` 显式覆盖。Desktop 中的实际绝对路径会显示在 Settings → Appearance 底部。\n\n- 外部安装：把 [`color-theme.template.json`](../resources/design-tokens/color-theme.template.json) 复制到该目录，修改 `id`、`label` 和颜色后保存，完全重启 Zeta。\n- 外部更新：替换同名文件，完全重启 Zeta。\n- 卸载：删除对应文件，完全重启 Zeta。\n- 恢复：如果已选择的主题不存在或加载失败，配置验证会回退到 System，内置 Light/Dark 始终可用。\n\n每个文件独立加载。一个损坏主题不会阻止其他主题或 App 启动；错误文件和原因会显示在 Appearance 页面。目录只读取非递归的常规 JSON 文件，最多 128 个，每个最大 1 MiB，不跟随目录或符号链接。\n\n## 可复制模板\n\n保存为 `aurora.json`：\n\n```json\n{\n  \"$schema\": \"https://zeta.dev/schemas/color-theme.schema.json\",\n  \"version\": 1,\n  \"id\": \"zeta-aurora\",\n  \"label\": \"Zeta Aurora\",\n  \"colorScheme\": \"dark\",\n  \"colors\": {\n    \"workbench.background\": \"#0b1020\",\n    \"editor.background\": \"#0b1020\",\n    \"editor.foreground\": \"#dbe7ff\",\n    \"sideBar.background\": \"#10172a\",\n    \"auxiliaryBar.background\": \"sideBar.background\",\n    \"panel.background\": \"sideBar.background\",\n    \"titleBar.background\": \"#080d18\",\n    \"titleBar.foreground\": \"#edf4ff\",\n    \"titleBar.actionForeground\": \"#dbe7ff\",\n    \"input.background\": \"#18223a\",\n    \"input.border\": \"#31446d\",\n    \"focusBorder\": \"#7aa2f7\",\n    \"accent.foreground\": \"#89b4fa\",\n    \"button.primaryBackground\": \"#4169a8\",\n    \"button.primaryHoverBackground\": \"#527bbd\",\n    \"selection.background\": \"#29466f\",\n    \"list.activeSelectionBackground\": \"#29466f\",\n    \"toolbar.hoverBackground\": {\n      \"op\": \"transparent\",\n      \"value\": \"#ffffff\",\n      \"factor\": 0.2\n    }\n  }\n}\n```\n\n只覆盖与基础方案不同的语义 token，不要复制整张颜色表。未覆盖 token 会继承 `colorScheme` 对应的注册表默认值，因此产品新增 token 后，用户主题仍可形成完整快照。\n\n## 字段契约\n\n| 字段 | 约束 |\n| --- | --- |\n| `$schema` | 可省略；存在时必须使用模板中的 Schema URL |\n| `version` | 当前固定为 `1` |\n| `id` | 稳定、唯一、小写 kebab-case；不能与内置或其他用户主题重复 |\n| `label` | Settings 中显示的名称，1–80 个已去除首尾空格的字符 |\n| `colorScheme` | `light`、`dark`、`high-contrast-light` 或 `high-contrast-dark` |\n| `colors` | 已注册颜色 token 到颜色值的映射，最多 512 项 |\n\n普通颜色值可以是：\n\n- 十六进制颜色：`#rgb`、`#rgba`、`#rrggbb`、`#rrggbbaa`；\n- 另一个 token ID，例如 `\"panel.background\": \"sideBar.background\"`；\n- 下述受支持的颜色变换对象。\n\n## 颜色变换\n\n透明度：\n\n```json\n{\n  \"op\": \"transparent\",\n  \"value\": \"foreground\",\n  \"factor\": 0.5\n}\n```\n\n变亮或变暗：\n\n```json\n{\n  \"op\": \"lighten\",\n  \"value\": \"editor.background\",\n  \"factor\": 0.12\n}\n```\n\n`op` 也可以是 `darken`。`factor` 必须在 0 到 1 之间。\n\n混合：\n\n```json\n{\n  \"op\": \"mix\",\n  \"value\": \"editor.background\",\n  \"other\": \"accent.foreground\",\n  \"factor\": 0.2\n}\n```\n\n合成到不透明背景：\n\n```json\n{\n  \"op\": \"opaque\",\n  \"value\": {\n    \"op\": \"transparent\",\n    \"value\": \"#ffffff\",\n    \"factor\": 0.15\n  },\n  \"background\": \"editor.background\"\n}\n```\n\n变换最多嵌套 8 层。未知字段、未知 token、循环引用、非法颜色或不满足透明度契约都会让该主题文件加载失败。\n\n## 主题生效范围\n\n一个成功注册的用户主题会自动出现在 Settings → Appearance，并使用实际快照生成预览。选择后以下消费者使用同一快照：\n\n- Workbench CSS custom properties 与 Aster editor token 颜色；\n- Native shell、composer CodeEditor、multi-diff editor、terminal ANSI palette 与 scrollbar；\n- Desktop Terminal 前景、背景、光标、选择色和完整 ANSI palette；\n- Windows/Linux 原生标题栏按钮区域；\n- 状态栏、菜单、输入框、列表和其他语义组件。\n\nTUI 有意只消费共享 token 子集，并根据终端能力降级到 TrueColor、ANSI-256、ANSI-16 或 Monochrome；因此一个合法主题不保证终端能复现 Desktop 的全部颜色细节。\n\n选择值保存在 device-local `configuration.json`，而不是 Agent config、Session store 或 TOML：\n\n```json\n{\n  \"version\": 1,\n  \"values\": {\n    \"workbench.colorTheme\": \"zeta-aurora\",\n    \"tui.colorTheme\": \"zeta-aurora\"\n  }\n}\n```\n\n`tui.colorTheme` 可省略；省略时跟随 `workbench.colorTheme`。`system` 仍表示跟随操作系统并在内置 Light/Dark 之间切换。Desktop 保存/预览会即时更新；TUI 可用 `/theme` 打开不带搜索的 Zeta Code Theme Pane，通过 Enter 原子保存、即时切换并返回主界面，并在 transcript 中以状态圆点、`/theme <id>` 与下一行通过 `└─` 归属且对齐的 `Theme set to …` 记录结果；也可用 `/theme <id>` 直接选择。Custom color theme 行进入有效用户主题列表；外部修改主题文件后，Native 与 TUI 仍需重启才能重新读取文件内容。\n\n## 开发与验证\n\n修改 Loader、Schema 或 token 后，在 `desktop` 目录运行：\n\n```text\npnpm tokens:generate\npnpm tokens:check\npnpm test:main\npnpm typecheck:renderer\npnpm build\n```\n\n只修改用户主题 JSON 不需要重新构建 App。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "从当前主题创建",
        "title": "从当前主题创建"
      },
      {
        "depth": 2,
        "id": "文件安装与卸载",
        "title": "文件安装与卸载"
      },
      {
        "depth": 2,
        "id": "可复制模板",
        "title": "可复制模板"
      },
      {
        "depth": 2,
        "id": "字段契约",
        "title": "字段契约"
      },
      {
        "depth": 2,
        "id": "颜色变换",
        "title": "颜色变换"
      },
      {
        "depth": 2,
        "id": "主题生效范围",
        "title": "主题生效范围"
      },
      {
        "depth": 2,
        "id": "开发与验证",
        "title": "开发与验证"
      }
    ],
    "searchText": " 本文是可安装用户主题的 canonical 说明。可以直接复制并修改 color-theme.template.json；架构与可靠性边界见 design-tokens.md，可用 token 见生成目录，格式 Schema 见 color-theme.schema.json。 快速理解 创建主题最简单的方法是在 Settings → Appearance 中从当前主题另存一份，再只修改需要变化的 语义颜色。主题文件不需要复制完整颜色表；未覆盖的颜色会继续使用所选明暗方案的默认值。 想做什么 推荐方式 生效方式 --- --- --- 从当前外观开始修改 使用“Create from current theme” 保存后立即预览和切换 安装别人提供的主题 把 JSON 放入用户主题目录 完全重启后加载 更新已有主题 在设置中保存，或替换同名文件 设置内保存立即生效；外部替换需重启 删除主题 在设置中删除，或移除对应文件 自动回到内置明暗主题 修复加载失败 根据 Appearance 中的错误修改 JSON 其他有效主题不受影响 从当前主题创建 推荐在 Settings → Appearance 中操作： - 当前选择 Light 时，“Create from current theme”会生成完整的 Light JSON；Dark 同理。 - System 会采用当前操作系统实际生效的 Light 或 Dark。 - 有效 JSON 会即时预览；取消或关闭 Settings 会恢复编辑前的主题。 - 内置 Light/Dark 只能使用“Save As”创建新主题，不会被覆盖。 - 用户主题可以直接“Save”，也可以修改 id 和 label 后“Save As”。 - 用户主题可以在 JSON 编辑器中“Delete”；确认后删除文件并按原主题明暗类型切回 Zeta Light 或 Zeta Dark。 - 保存成功后 JSON 会立即注册、切换并写入用户主题目录，不需要重启。 文件安装与卸载 Zeta 宿主读取 device root 的 themes 目录中的常规 .json 文件。Desktop 的 device root 是 Electron userData；Native/TUI 使用相同平台目录（macOS 为 /Library/Application Support/Zeta，Windows 为 %APPDATA%/Zeta，Linux 为 $XDGCONFIGHOME/zeta 或 /.config/zeta）。测试和开发可用 ZETADEVICEROOT 显式覆盖。Desktop 中的实际绝对路径会显示在 Settings → Appearance 底部。 - 外部安装：把 color-theme.template.json 复制到该目录，修改 id、label 和颜色后保存，完全重启 Zeta。 - 外部更新：替换同名文件，完全重启 Zeta。 - 卸载：删除对应文件，完全重启 Zeta。 - 恢复：如果已选择的主题不存在或加载失败，配置验证会回退到 System，内置 Light/Dark 始终可用。 每个文件独立加载。一个损坏主题不会阻止其他主题或 App 启动；错误文件和原因会显示在 Appearance 页面。目录只读取非递归的常规 JSON 文件，最多 128 个，每个最大 1 MiB，不跟随目录或符号链接。 可复制模板 保存为 aurora.json： ``json \"$schema\": \"https://zeta.dev/schemas/color-theme.schema.json\", \"version\": 1, \"id\": \"zeta-aurora\", \"label\": \"Zeta Aurora\", \"colorScheme\": \"dark\", \"colors\": \"workbench.background\": \" 0b1020\", \"editor.background\": \" 0b1020\", \"editor.foreground\": \" dbe7ff\", \"sideBar.background\": \" 10172a\", \"auxiliaryBar.background\": \"sideBar.background\", \"panel.background\": \"sideBar.background\", \"titleBar.background\": \" 080d18\", \"titleBar.foreground\": \" edf4ff\", \"titleBar.actionForeground\": \" dbe7ff\", \"input.background\": \" 18223a\", \"input.border\": \" 31446d\", \"focusBorder\": \" 7aa2f7\", \"accent.foreground\": \" 89b4fa\", \"button.primaryBackground\": \" 4169a8\", \"button.primaryHoverBackground\": \" 527bbd\", \"selection.background\": \" 29466f\", \"list.activeSelectionBackground\": \" 29466f\", \"toolbar.hoverBackground\": \"op\": \"transparent\", \"value\": \" ffffff\", \"factor\": 0.2 ` 只覆盖与基础方案不同的语义 token，不要复制整张颜色表。未覆盖 token 会继承 colorScheme 对应的注册表默认值，因此产品新增 token 后，用户主题仍可形成完整快照。 字段契约 字段 约束 --- --- $schema 可省略；存在时必须使用模板中的 Schema URL version 当前固定为 1 id 稳定、唯一、小写 kebab-case；不能与内置或其他用户主题重复 label Settings 中显示的名称，1–80 个已去除首尾空格的字符 colorScheme light、dark、high-contrast-light 或 high-contrast-dark colors 已注册颜色 token 到颜色值的映射，最多 512 项 普通颜色值可以是： - 十六进制颜色： rgb、 rgba、 rrggbb、 rrggbbaa； - 另一个 token ID，例如 \"panel.background\": \"sideBar.background\"； - 下述受支持的颜色变换对象。 颜色变换 透明度： `json \"op\": \"transparent\", \"value\": \"foreground\", \"factor\": 0.5 ` 变亮或变暗： `json \"op\": \"lighten\", \"value\": \"editor.background\", \"factor\": 0.12 ` op 也可以是 darken。factor 必须在 0 到 1 之间。 混合： `json \"op\": \"mix\", \"value\": \"editor.background\", \"other\": \"accent.foreground\", \"factor\": 0.2 ` 合成到不透明背景： `json \"op\": \"opaque\", \"value\": \"op\": \"transparent\", \"value\": \" ffffff\", \"factor\": 0.15 , \"background\": \"editor.background\" ` 变换最多嵌套 8 层。未知字段、未知 token、循环引用、非法颜色或不满足透明度契约都会让该主题文件加载失败。 主题生效范围 一个成功注册的用户主题会自动出现在 Settings → Appearance，并使用实际快照生成预览。选择后以下消费者使用同一快照： - Workbench CSS custom properties 与 Aster editor token 颜色； - Native shell、composer CodeEditor、multi-diff editor、terminal ANSI palette 与 scrollbar； - Desktop Terminal 前景、背景、光标、选择色和完整 ANSI palette； - Windows/Linux 原生标题栏按钮区域； - 状态栏、菜单、输入框、列表和其他语义组件。 TUI 有意只消费共享 token 子集，并根据终端能力降级到 TrueColor、ANSI-256、ANSI-16 或 Monochrome；因此一个合法主题不保证终端能复现 Desktop 的全部颜色细节。 选择值保存在 device-local configuration.json，而不是 Agent config、Session store 或 TOML： `json \"version\": 1, \"values\": \"workbench.colorTheme\": \"zeta-aurora\", \"tui.colorTheme\": \"zeta-aurora\" ` tui.colorTheme 可省略；省略时跟随 workbench.colorTheme。system 仍表示跟随操作系统并在内置 Light/Dark 之间切换。Desktop 保存/预览会即时更新；TUI 可用 /theme 打开不带搜索的 Zeta Code Theme Pane，通过 Enter 原子保存、即时切换并返回主界面，并在 transcript 中以状态圆点、/theme <id 与下一行通过 └─ 归属且对齐的 Theme set to … 记录结果；也可用 /theme <id 直接选择。Custom color theme 行进入有效用户主题列表；外部修改主题文件后，Native 与 TUI 仍需重启才能重新读取文件内容。 开发与验证 修改 Loader、Schema 或 token 后，在 desktop 目录运行： `text pnpm tokens:generate pnpm tokens:check pnpm test:main pnpm typecheck:renderer pnpm build `` 只修改用户主题 JSON 不需要重新构建 App。"
  },
  {
    "slug": "menu-system",
    "sourcePath": "docs/menu-system.md",
    "title": "MenuId 与 UI Action 组合系统",
    "description": "MenuId 是一个稳定的 UI action 贡献位置标识。功能模块注册 Command，并声明该 Command 要出现在哪些 MenuId 中；Titlebar、Editor、Chat 或 Menubar 等 host 只消费自己 拥有的 MenuId，不直接依赖提供 action 的功能模块。",
    "group": "界面与体验",
    "markdown": "> 本文是 Zeta Desktop Renderer 中 Command、MenuId、Context Key、MenuService 与菜单型\n> Toolbar 组合关系的 canonical 文档。\n> Renderer 组件和 CSS 状态所有权以 [`ui-styling-ownership.md`](ui-styling-ownership.md)\n> 为准；Desktop 的进程与产品边界以\n> [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) 为准。\n\n## 快速理解\n\n`MenuId` 是一个稳定的 **UI action 贡献位置标识**。功能模块注册 Command，并声明该\nCommand 要出现在哪些 `MenuId` 中；Titlebar、Editor、Chat 或 Menubar 等 host 只消费自己\n拥有的 `MenuId`，不直接依赖提供 action 的功能模块。\n\n```text\nAction2 / registerAction2\n  ├─→ CommandsRegistry：注册可执行行为\n  ├─→ MenusRegistry(MenuId)：注册 UI 位置\n  └─→ KeybindingsRegistry：注册可选快捷键\n\nMenusRegistry + ContextKeyService\n  → MenuService\n  → 分组后的 MenuItemAction / SubmenuItemAction\n  → MenuWorkbenchToolBar\n  → WorkbenchToolBar\n  → ToolBar / Menu\n  → CommandService.executeCommand()\n```\n\nMenu 系统解决的是 **action 的跨模块发现、条件投影、排序和呈现组合**，不是任意模块之间\n相互调用的总线。真正执行功能的是 Command；真正决定 CSS 视觉的是控件及其 presentation。\n\n| 读者的问题 | 应该查看的概念 | 它负责什么 |\n| --- | --- | --- |\n| 点击后做什么 | Command | 执行业务行为 |\n| 动作出现在哪里 | `MenuId` | 标识稳定贡献位置 |\n| 什么时候显示或可用 | Context Key | 投影当前上下文条件 |\n| 如何分组和排序 | Menu contribution | 声明组合关系 |\n| 最终长什么样 | Toolbar、Menu 和组件样式 | 渲染与交互视觉 |\n\n## 核心概念与所有权\n\n| 层 | 当前实现 | 负责 | 不负责 |\n| --- | --- | --- | --- |\n| Command | `CommandsRegistry`、`ICommandService` | 命令 ID、执行入口、依赖服务访问 | UI 出现位置、排序和样式 |\n| Action 声明 | `Action2`、`registerAction2()` | 把一个内建 action 的 command、menu、keybinding、F1 声明集中注册 | host 布局和 DOM |\n| 位置标识 | `MenuId` | 标识一个稳定的 action 贡献槽位 | 菜单实例、DOM ID、业务服务定位 |\n| 静态注册表 | `MenusRegistry` | 保存 `MenuId → contribution[]`，提供注册和释放事件 | 判断当前 context、执行命令 |\n| 条件状态 | `IContextKeyService` | 提供 visibility、enablement、checked 等规则的输入 | 业务权限和最终执行校验 |\n| 解析层 | `MenuService`、`IMenu` | 按 context 过滤、排序、分组、解析 submenu，并生成 runtime action | 控件布局和视觉样式 |\n| Runtime action | `MenuItemAction`、`SubmenuItemAction` | 暴露 label、icon、enabled、checked，并桥接到 `CommandService` | Command 的业务实现 |\n| Base 呈现 | `ToolBar` | primary/secondary 排列、More Actions、键盘与 DOM | platform action 类型、MenuId |\n| Workbench 适配 | `WorkbenchToolBar` | 接受调用方提供的 actions，并把 platform menu action 适配为 base action view item | 自动查询 MenuId |\n| Menu 驱动 | `MenuWorkbenchToolBar` | 消费一个 `MenuId`、监听 Menu/Context 变化并更新 `WorkbenchToolBar` | Command 业务实现、host 布局 |\n| 菜单呈现 | Menubar control、popup `Menu` | 把 action 呈现为 `menubar/menu/menuitem` | Toolbar 语义 |\n| Host | Titlebar、Editor、Chat 等 | 选择要消费的 `MenuId`，拥有区域布局 | 直接 import 其他功能模块来收集 action |\n\n一句话记忆：\n\n| 问题 | 去哪里找 |\n| --- | --- |\n| 点击后做什么？ | Command |\n| 出现在哪里？ | MenuId placement |\n| 何时出现、可用或选中？ | Context Key expression |\n| 如何排序和组成 submenu？ | Menu contribution + MenuService |\n| 渲染成什么控件？ | Toolbar / Menubar host |\n| hover、checked、focus 长什么样？ | 对应控件 CSS 与 presentation |\n\n## MenuId 到底是什么\n\n`MenuId` 的值只表示一个逻辑位置。例如：\n\n- `MenuId.TitleBarLeft`：Titlebar 左侧 action 槽位。\n- `MenuId.TitleBar`：Titlebar 右侧 action 槽位。\n- `MenuId.EditorTitle`：Editor title action 槽位。\n- `MenuId.ChatTitle`：Chat title action 槽位。\n- `MenuId.TerminalTitle`：Terminal View title action 槽位。\n- `MenuId.MenubarViewMenu`：应用菜单 View 下的 contribution 槽位。\n- `MenuId.CommandPalette`：Command Palette 的 action 集合。\n\n同一个 Command 可以贡献到多个 `MenuId`。例如 Toggle Panel 同时贡献到\n`MenuId.TitleBar` 与 `MenuId.MenubarViewMenu`，并通过 `f1: true` 进入 Command Palette。\n这些 UI 入口最终执行同一个 Command，不复制业务实现。\n\n`MenuId` 不是：\n\n- ❌ 一个已经创建好的菜单或 Toolbar。\n- ❌ DOM element ID 或 CSS selector。\n- ❌ 服务定位器、模块 RPC 或通用 event bus。\n- ❌ 权限边界；隐藏或禁用 action 不能替代 Command 执行时的业务校验。\n- ❌ action 的选中态样式 owner。\n\n内建的公共槽位应优先使用 `MenuId` 上已有的静态实例。需要新增共享槽位时，定义一次明确\n导出的 canonical 实例；跨模块按字符串取得同一槽位时使用 `MenuId.for(identifier)`。\n不要在多个模块里重复 `new MenuId(\"same.id\")`：构造器会拒绝重复 ID。\n\n## 注册操作（Action）\n\n内建、静态加载的功能优先使用 `Action2`。一个声明可以原子地注册 Command、一个或多个\nMenu placement、快捷键和 Command Palette 入口：\n\n```ts\nimport { Action2, MenuId, registerAction2 } from \"../../../../platform/actions/common/actions.js\";\nimport type { ServicesAccessor } from \"../../../../platform/instantiation/common/instantiation.js\";\nimport { PanelVisibleContext } from \"../../../common/contextkeys.js\";\nimport { IWorkbenchLayoutService } from \"../../../services/layout/browser/layoutService.js\";\n\nregisterAction2(class TogglePanelAction extends Action2 {\n  constructor() {\n    super({\n      id: \"workbench.action.togglePanel\",\n      title: \"Show Panel\",\n      toggled: {\n        condition: PanelVisibleContext.isEqualTo(true),\n        title: \"Hide Panel\",\n      },\n      menu: [\n        { id: MenuId.TitleBar, group: \"navigation\", order: 9 },\n        { id: MenuId.MenubarViewMenu, group: \"2_appearance\", order: 11 },\n      ],\n      f1: true,\n    });\n  }\n\n  override run(accessor: ServicesAccessor): void {\n    const layout = accessor.get(IWorkbenchLayoutService);\n    if (layout.isPartVisible(\"panel\")) layout.hidePart(\"panel\");\n    else layout.showPart(\"panel\");\n  }\n});\n```\n\n静态 contribution module 可以让该注册随当前 JavaScript realm 存活。动态功能必须保存并\n释放 `registerAction2()` 返回的 `IDisposable`。如果任一步注册失败，\n`registerAction2()` 会释放此前已完成的注册，不留下半注册状态。\n\n### 动态添加位置\n\nCommand 已经存在，只需要动态改变 UI 位置时，可以直接使用 `MenusRegistry`：\n\n```ts\nconst registration = MenusRegistry.appendMenuItem(MenuId.EditorTitle, {\n  command: {\n    id: \"editor.action.preview\",\n    title: \"Open Preview\",\n  },\n  when: EditorHasPreviewContext,\n  group: \"navigation\",\n  order: 20,\n});\n\n// contribution 生命周期结束时\nregistration.dispose();\n```\n\n这段代码只注册 placement；`editor.action.preview` 仍必须由 Command 系统注册执行入口。\n\n### 声明子菜单（Submenu）\n\nSubmenu 自己也是一个 `MenuId`，父菜单只贡献一个指向它的入口：\n\n```ts\nconst RefactorMenu = MenuId.for(\"EditorRefactor\");\n\nconst submenuRegistration = MenusRegistry.appendMenuItem(MenuId.EditorTitle, {\n  title: \"Refactor\",\n  submenu: RefactorMenu,\n  group: \"1_modification\",\n  order: 10,\n});\n\nconst itemRegistration = MenusRegistry.appendMenuItem(RefactorMenu, {\n  command: {\n    id: \"editor.action.rename\",\n    title: \"Rename Symbol\",\n  },\n  order: 10,\n});\n```\n\n默认情况下，解析后为空的 submenu 不会出现；调用方显式传入\n`preserveEmptySubmenus: true` 时才保留。循环引用会在解析时抛出错误。\n\n## 条件语义\n\n下面几个字段看起来相似，但 owner 和效果不同：\n\n| 字段 | 判断对象 | 当前效果 |\n| --- | --- | --- |\n| placement `when` | 这个位置 | 条件不满足时，该 action 不出现在这个 `MenuId` 中 |\n| command `precondition` | 这个 Command action | action 仍可出现，但 `enabled` 为 `false` |\n| command `toggled` | 这个 Command action | 解析为 `checked`，并可替换 checked 时的 title、tooltip、icon |\n| keybinding `when` | 这个快捷键入口 | 与 `precondition` 合并后决定快捷键是否匹配 |\n| `f1: true` | Command Palette placement | 自动贡献到 `MenuId.CommandPalette`；当前以 `precondition` 作为该 placement 的 `when` |\n\n`when` 和 `precondition` 都只是 UI/输入层规则。Command 不能假设所有调用都来自该 Menu，\n仍需在执行路径中维护业务不变量和权限边界。\n\n`toggled` 只生成语义状态。控件会并行投影 `.checked` 与 ARIA 状态；是否高亮以及具体皮肤由\nToolbar/Button 等呈现层负责，详见\n[`ui-styling-ownership.md`](ui-styling-ownership.md)。\n\n## 分组与排序\n\n`MenuService` 当前按以下顺序解析 contribution：\n\n1. 用 placement `when` 过滤。\n2. 先按 `group` 排序：`navigation` 最前，无 group 最后，其余按名称排序。\n3. 同一 group 内按 `order` 升序。\n4. `order` 相同时按 title 排序。\n5. 递归解析 submenu。\n\n`MenuWorkbenchToolBar` 对 group 还有一层明确的呈现规则：\n\n| Group | 当前 Toolbar 呈现 |\n| --- | --- |\n| `navigation` | primary action，直接显示 |\n| 其他 group 或无 group | secondary action，以 separator 分组后进入 More Actions |\n\n因此 `group` 既参与稳定排序，也会影响菜单型 Toolbar 的 primary/secondary 布局。不要仅为\n得到某种 CSS 外观随意使用 `navigation`；它表达的是主要、直接可达的 action。\n\n## 宿主如何消费 MenuId\n\nHost 只选择槽位，不收集功能模块：\n\n```ts\nconst toolbar = new MenuWorkbenchToolBar(\n  menuService,\n  contextMenuService,\n  MenuId.TitleBar,\n  ownerDocument,\n  { presentation: \"inherit-foreground\" },\n);\n```\n\n`MenuWorkbenchToolBar` 会：\n\n1. 通过 `menuService.createMenu(menuId)` 创建当前 context 下的可观察视图。\n2. 调用 `menu.getActions()` 获得分组后的 runtime actions。\n3. 将 `navigation` 投影到 primary，其余 group 投影到 secondary。\n4. 在 Menu Registry 或 Context Key 变化时重新解析和渲染。\n5. 点击 `MenuItemAction` 时通过 `CommandService.executeCommand()` 执行同 ID Command。\n\nTitlebar 因而只依赖 `MenuId.TitleBar`，不需要知道 Toggle Panel 是哪个模块注册的；Panel\n模块也不需要取得 Titlebar 实例。MenuId 是二者之间稳定、低耦合的组合边界。\n\n## Toolbar 三层边界\n\n三层按 action 来源和依赖方向拆分，不按视觉拆分；它们最终都保持 `role=\"toolbar\"`：\n\n```text\nToolBar\n  ↑ 提供 platform action view item 适配\nWorkbenchToolBar\n  ↑ 提供 MenuId 自动解析与刷新\nMenuWorkbenchToolBar\n```\n\n| 层 | 调用方式 | 典型用途 |\n| --- | --- | --- |\n| `ToolBar` | `setActions(primary, secondary)` | base 控件和不应依赖 platform actions 的底层调用方 |\n| `WorkbenchToolBar` | `setActions(primary, secondary)` | 调用方已经拥有 actions，但其中可能包含 platform menu actions |\n| `MenuWorkbenchToolBar` | 构造时传入 `MenuId` | Host 只拥有贡献槽位，actions 由 MenuService 提供 |\n\n`WorkbenchToolBar` 当前的真实职责是 action representation 适配：调用方可以手工传入\n`MenuItemAction`、`SubmenuItemAction` 或普通 `IAction`，它会选择正确的 toolbar view item。\n它不自动读取 `MenuId`，也没有复制 VS Code 的 action 隐藏、遥测、配置快捷键或复杂 overflow\n策略。以后只有这些能力形成真实共享需求时，才继续加入这一层。\n\n`MenuWorkbenchToolBar` 在此基础上拥有 `IMenu` 生命周期、group 到\nprimary/secondary 的投影和刷新。它的 `Menu` 表示 action 来源，不表示 ARIA role；常驻\n按钮区域仍是 toolbar，只有 More Actions 弹层及应用菜单使用 menu/menuitem。\n\n## 什么时候使用哪一层\n\n| 场景 | 推荐 |\n| --- | --- |\n| 多个模块会向同一区域贡献 action | 使用 MenuId-backed Toolbar/Menu |\n| action 需要随 Context Key 动态出现、禁用或 checked | 使用 MenuId + MenuService |\n| 同一 Command 要进入 Titlebar、Menubar、Command Palette 等多个入口 | 使用 `Action2` 的多个 placement |\n| Workbench 调用方已经拥有完整 action 列表 | 使用 `WorkbenchToolBar` |\n| base 控件内部一次性构造固定 action，且不应依赖 platform actions | 使用 `ToolBar` |\n| 只需要程序调用或快捷键，不需要 UI 入口 | 只注册 Command/Keybinding |\n\n选择 Toolbar 时按以下顺序机械判断，不根据外观或所在 Part 猜测：\n\n| 判断 | 是 | 否 |\n| --- | --- | --- |\n| action 是否来自 `MenuId` / `MenuService`？ | 使用 `MenuWorkbenchToolBar` | 继续判断 |\n| 调用方是否已经拥有完整的 primary/secondary action 列表？ | Workbench 产品代码使用 `WorkbenchToolBar` | 先明确 action 的 owner 和来源 |\n| 是否是 `src/zeta/base` 内部的领域无关控件？ | 可以使用 `ToolBar` | 不得直接构造 base `ToolBar` |\n\n只有 base UI 和 `platform/actions` 中实现标准适配层的代码可以直接依赖 base `ToolBar`。\nWorkbench Part、View、Contribution 等产品代码必须在 `WorkbenchToolBar` 与\n`MenuWorkbenchToolBar` 之间选择。`MenuWorkbenchToolBar` 自己拥有 action 列表；调用方\n通过注册 Command、Menu contribution 和更新 Context Key 改变内容，不调用\n`setActions()`。\n\nTitlebar、Editor title、Chat title 与 Terminal title 当前都使用\n`MenuWorkbenchToolBar`。Terminal 拥有 `MenuId.TerminalTitle`，其 profile、New、Relaunch\n与 Kill action 注册为 Command + Menu contribution；profile 使用自定义 action view item\n保留原生选择控件，显隐与 enablement 由 Terminal Context Key 投影。\n\n## 生命周期与失败语义\n\n- `MenusRegistry` 是当前 JavaScript realm 内的共享注册表。\n- `appendMenuItem()` 与 `registerAction2()` 都返回可释放注册；动态调用方必须与自己的\n  生命周期绑定。\n- `MenuService.getMenuActions()` 是一次性解析；`createMenu()` 返回可观察、可释放的 `IMenu`。\n- 当前 `IMenu.onDidChange` 会响应任意 Menu Registry 变化和任意 Context Key 变化，调用方\n  应假设事件表示“可能需要重算”，不能把它当成某个字段的精确变更通知。\n- `getMenuItems()` 返回数组副本，调用方不能借此修改 Registry。\n- Submenu 循环会报错；空 submenu 默认丢弃。\n- Command 的执行错误由 `CommandService.executeCommand()` 返回的 Promise 向调用方传播。\n\n## 禁止的耦合方式\n\n- ❌ Titlebar 为了显示 Panel action 而直接 import `TogglePanelAction` 并手工构造按钮。\n- ❌ 功能模块直接查询 Titlebar DOM 后插入 action。\n- ❌ 只注册 Menu placement，却没有注册相同 ID 的 Command 执行入口。\n- ❌ 把 `when` 或 `precondition` 当成安全校验。\n- ❌ 用 `MenuId.id` 拼 CSS selector 或作为视觉 variant。\n- ❌ 在 host CSS 中根据某个 Command ID 重写通用 Toolbar/Button 的内部 checked 样式。\n- ❌ 在多个模块中重复构造同名 `MenuId`。\n- ❌ Workbench Part、View 或 Contribution 直接构造 base `ToolBar`，绕过 platform action\n  适配。\n- ❌ 调用方对 `MenuWorkbenchToolBar` 调用 `setActions()`；它的 actions 只能来自构造时\n  指定的 `MenuId`。\n- ❌ 因为类名包含 `Menu` 就把常驻 Toolbar 设置为 `role=\"menu\"`；只有弹出的菜单容器和\n  菜单项使用 `menu` / `menuitem`。\n\n## 当前实现状态\n\n| 能力 | 状态 |\n| --- | --- |\n| 一个 Action 原子注册 Command、Menu、Keybinding 和 F1 | ✅ |\n| 一个 Command 贡献到多个 MenuId | ✅ |\n| Context 驱动 visibility、enablement、checked | ✅ |\n| Group/order 排序与递归 Submenu | ✅ |\n| Registry 与 Context 变化后刷新 Menu-backed Toolbar | ✅ |\n| Submenu 循环检测和空 Submenu 策略 | ✅ |\n| `ToolBar → WorkbenchToolBar → MenuWorkbenchToolBar` 分层 | ✅ |\n| Terminal title actions 接入 `MenuId.TerminalTitle` | ✅ |\n| 只通知受影响 MenuId 和相关 Context Key | 尚未完成 |\n| 每个 MenuId 的显式 owner catalog | 尚未完成 |\n| 每个 Toolbar 自定义 group 到 primary/secondary 的策略 | 尚未完成 |\n\n### 当前限制\n\n1. `Menu` 当前订阅所有 Menu Registry 与 Context Key 变化，还没有按自身 `MenuId` 和表达式\n   依赖做精确失效。\n2. `MenuWorkbenchToolBar` 当前固定把 `navigation` 作为 primary，把所有其他 group 作为\n   secondary；host 不能声明更丰富的分组呈现策略。\n3. `MenuId` 的 owner 目前主要依赖静态定义和命名，没有独立 catalog。新增槽位时必须由\n   实际消费它的 host/domain 拥有并导出。\n4. `WorkbenchToolBar` 当前只拥有 platform action representation 适配；VS Code 中的 action\n   隐藏、配置快捷键、遥测和复杂 overflow 策略尚未实现，不能描述为当前能力。\n\n## 扩展原则\n\n后续扩展 Menu 系统时保持以下不变量：\n\n1. Command 是可执行行为的唯一 canonical ID；Menu placement 不复制实现。\n2. MenuId 由消费该位置的 host/domain 拥有，不由某个偶然的首个 contributor 拥有。\n3. Base UI 不得反向依赖 Workbench Menu 系统；`platform/actions` 负责把 Workbench\n   contribution 适配为 base `IAction`。\n4. Context Key 负责 UI 策略投影，不替代业务校验。\n5. Group 表达 action 组织语义，不表达 CSS 皮肤。\n6. 视觉差异通过控件 presentation 和 token 解决，不污染 MenuId 或 Command。\n7. 新增能力必须区分当前实现、已决定但未实现的 proposed work 与潜在方向。\n8. `WorkbenchToolBar` 只接收调用方 actions；MenuId 监听与刷新只能由\n   `MenuWorkbenchToolBar` 拥有。\n\n## 审查清单\n\n- 这是一个可执行行为，还是一个 UI 位置？\n- Command 是否只注册一次，并由所有 UI 入口复用？\n- MenuId 是否由消费该位置的 host/domain 拥有？\n- Toolbar 的 action 来源是 `MenuId`，还是调用方拥有的 action 列表？\n- Workbench 产品代码是否直接构造了 base `ToolBar`？\n- 是否错误地对 `MenuWorkbenchToolBar` 调用了 `setActions()`？\n- 常驻 action 区域是否保持 `role=\"toolbar\"`，仅弹出菜单使用 `menu` / `menuitem`？\n- `when`、`precondition` 与 `toggled` 是否使用了正确语义？\n- group 是否表达真实组织关系，`order` 是否只在同组内比较？\n- 动态注册是否随 owner 释放？\n- Command 执行路径是否仍维护业务不变量和权限校验？\n- 样式是否留在对应控件，而不是写进 Menu/Command 层？\n- 文档与代码是否明确区分当前能力和后续方向？",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "核心概念与所有权",
        "title": "核心概念与所有权"
      },
      {
        "depth": 2,
        "id": "menuid-到底是什么",
        "title": "MenuId 到底是什么"
      },
      {
        "depth": 2,
        "id": "注册操作action",
        "title": "注册操作（Action）"
      },
      {
        "depth": 3,
        "id": "动态添加位置",
        "title": "动态添加位置"
      },
      {
        "depth": 3,
        "id": "声明子菜单submenu",
        "title": "声明子菜单（Submenu）"
      },
      {
        "depth": 2,
        "id": "条件语义",
        "title": "条件语义"
      },
      {
        "depth": 2,
        "id": "分组与排序",
        "title": "分组与排序"
      },
      {
        "depth": 2,
        "id": "宿主如何消费-menuid",
        "title": "宿主如何消费 MenuId"
      },
      {
        "depth": 2,
        "id": "toolbar-三层边界",
        "title": "Toolbar 三层边界"
      },
      {
        "depth": 2,
        "id": "什么时候使用哪一层",
        "title": "什么时候使用哪一层"
      },
      {
        "depth": 2,
        "id": "生命周期与失败语义",
        "title": "生命周期与失败语义"
      },
      {
        "depth": 2,
        "id": "禁止的耦合方式",
        "title": "禁止的耦合方式"
      },
      {
        "depth": 2,
        "id": "当前实现状态",
        "title": "当前实现状态"
      },
      {
        "depth": 3,
        "id": "当前限制",
        "title": "当前限制"
      },
      {
        "depth": 2,
        "id": "扩展原则",
        "title": "扩展原则"
      },
      {
        "depth": 2,
        "id": "审查清单",
        "title": "审查清单"
      }
    ],
    "searchText": " 本文是 Zeta Desktop Renderer 中 Command、MenuId、Context Key、MenuService 与菜单型 Toolbar 组合关系的 canonical 文档。 Renderer 组件和 CSS 状态所有权以 ui-styling-ownership.md 为准；Desktop 的进程与产品边界以 zeta-desktop-architecture.md 为准。 快速理解 MenuId 是一个稳定的 UI action 贡献位置标识。功能模块注册 Command，并声明该 Command 要出现在哪些 MenuId 中；Titlebar、Editor、Chat 或 Menubar 等 host 只消费自己 拥有的 MenuId，不直接依赖提供 action 的功能模块。 ``text Action2 / registerAction2 ├─→ CommandsRegistry：注册可执行行为 ├─→ MenusRegistry MenuId ：注册 UI 位置 └─→ KeybindingsRegistry：注册可选快捷键 MenusRegistry + ContextKeyService → MenuService → 分组后的 MenuItemAction / SubmenuItemAction → MenuWorkbenchToolBar → WorkbenchToolBar → ToolBar / Menu → CommandService.executeCommand ` Menu 系统解决的是 action 的跨模块发现、条件投影、排序和呈现组合，不是任意模块之间 相互调用的总线。真正执行功能的是 Command；真正决定 CSS 视觉的是控件及其 presentation。 读者的问题 应该查看的概念 它负责什么 --- --- --- 点击后做什么 Command 执行业务行为 动作出现在哪里 MenuId 标识稳定贡献位置 什么时候显示或可用 Context Key 投影当前上下文条件 如何分组和排序 Menu contribution 声明组合关系 最终长什么样 Toolbar、Menu 和组件样式 渲染与交互视觉 核心概念与所有权 层 当前实现 负责 不负责 --- --- --- --- Command CommandsRegistry、ICommandService 命令 ID、执行入口、依赖服务访问 UI 出现位置、排序和样式 Action 声明 Action2、registerAction2 把一个内建 action 的 command、menu、keybinding、F1 声明集中注册 host 布局和 DOM 位置标识 MenuId 标识一个稳定的 action 贡献槽位 菜单实例、DOM ID、业务服务定位 静态注册表 MenusRegistry 保存 MenuId → contribution ，提供注册和释放事件 判断当前 context、执行命令 条件状态 IContextKeyService 提供 visibility、enablement、checked 等规则的输入 业务权限和最终执行校验 解析层 MenuService、IMenu 按 context 过滤、排序、分组、解析 submenu，并生成 runtime action 控件布局和视觉样式 Runtime action MenuItemAction、SubmenuItemAction 暴露 label、icon、enabled、checked，并桥接到 CommandService Command 的业务实现 Base 呈现 ToolBar primary/secondary 排列、More Actions、键盘与 DOM platform action 类型、MenuId Workbench 适配 WorkbenchToolBar 接受调用方提供的 actions，并把 platform menu action 适配为 base action view item 自动查询 MenuId Menu 驱动 MenuWorkbenchToolBar 消费一个 MenuId、监听 Menu/Context 变化并更新 WorkbenchToolBar Command 业务实现、host 布局 菜单呈现 Menubar control、popup Menu 把 action 呈现为 menubar/menu/menuitem Toolbar 语义 Host Titlebar、Editor、Chat 等 选择要消费的 MenuId，拥有区域布局 直接 import 其他功能模块来收集 action 一句话记忆： 问题 去哪里找 --- --- 点击后做什么？ Command 出现在哪里？ MenuId placement 何时出现、可用或选中？ Context Key expression 如何排序和组成 submenu？ Menu contribution + MenuService 渲染成什么控件？ Toolbar / Menubar host hover、checked、focus 长什么样？ 对应控件 CSS 与 presentation MenuId 到底是什么 MenuId 的值只表示一个逻辑位置。例如： - MenuId.TitleBarLeft：Titlebar 左侧 action 槽位。 - MenuId.TitleBar：Titlebar 右侧 action 槽位。 - MenuId.EditorTitle：Editor title action 槽位。 - MenuId.ChatTitle：Chat title action 槽位。 - MenuId.TerminalTitle：Terminal View title action 槽位。 - MenuId.MenubarViewMenu：应用菜单 View 下的 contribution 槽位。 - MenuId.CommandPalette：Command Palette 的 action 集合。 同一个 Command 可以贡献到多个 MenuId。例如 Toggle Panel 同时贡献到 MenuId.TitleBar 与 MenuId.MenubarViewMenu，并通过 f1: true 进入 Command Palette。 这些 UI 入口最终执行同一个 Command，不复制业务实现。 MenuId 不是： - ❌ 一个已经创建好的菜单或 Toolbar。 - ❌ DOM element ID 或 CSS selector。 - ❌ 服务定位器、模块 RPC 或通用 event bus。 - ❌ 权限边界；隐藏或禁用 action 不能替代 Command 执行时的业务校验。 - ❌ action 的选中态样式 owner。 内建的公共槽位应优先使用 MenuId 上已有的静态实例。需要新增共享槽位时，定义一次明确 导出的 canonical 实例；跨模块按字符串取得同一槽位时使用 MenuId.for identifier 。 不要在多个模块里重复 new MenuId \"same.id\" ：构造器会拒绝重复 ID。 注册操作（Action） 内建、静态加载的功能优先使用 Action2。一个声明可以原子地注册 Command、一个或多个 Menu placement、快捷键和 Command Palette 入口： `ts import Action2, MenuId, registerAction2 from \"../../../../platform/actions/common/actions.js\"; import type ServicesAccessor from \"../../../../platform/instantiation/common/instantiation.js\"; import PanelVisibleContext from \"../../../common/contextkeys.js\"; import IWorkbenchLayoutService from \"../../../services/layout/browser/layoutService.js\"; registerAction2 class TogglePanelAction extends Action2 constructor super id: \"workbench.action.togglePanel\", title: \"Show Panel\", toggled: condition: PanelVisibleContext.isEqualTo true , title: \"Hide Panel\", , menu: id: MenuId.TitleBar, group: \"navigation\", order: 9 , id: MenuId.MenubarViewMenu, group: \"2appearance\", order: 11 , , f1: true, ; override run accessor: ServicesAccessor : void const layout = accessor.get IWorkbenchLayoutService ; if layout.isPartVisible \"panel\" layout.hidePart \"panel\" ; else layout.showPart \"panel\" ; ; ` 静态 contribution module 可以让该注册随当前 JavaScript realm 存活。动态功能必须保存并 释放 registerAction2 返回的 IDisposable。如果任一步注册失败， registerAction2 会释放此前已完成的注册，不留下半注册状态。 动态添加位置 Command 已经存在，只需要动态改变 UI 位置时，可以直接使用 MenusRegistry： `ts const registration = MenusRegistry.appendMenuItem MenuId.EditorTitle, command: id: \"editor.action.preview\", title: \"Open Preview\", , when: EditorHasPreviewContext, group: \"navigation\", order: 20, ; // contribution 生命周期结束时 registration.dispose ; ` 这段代码只注册 placement；editor.action.preview 仍必须由 Command 系统注册执行入口。 声明子菜单（Submenu） Submenu 自己也是一个 MenuId，父菜单只贡献一个指向它的入口： `ts const RefactorMenu = MenuId.for \"EditorRefactor\" ; const submenuRegistration = MenusRegistry.appendMenuItem MenuId.EditorTitle, title: \"Refactor\", submenu: RefactorMenu, group: \"1modification\", order: 10, ; const itemRegistration = MenusRegistry.appendMenuItem RefactorMenu, command: id: \"editor.action.rename\", title: \"Rename Symbol\", , order: 10, ; ` 默认情况下，解析后为空的 submenu 不会出现；调用方显式传入 preserveEmptySubmenus: true 时才保留。循环引用会在解析时抛出错误。 条件语义 下面几个字段看起来相似，但 owner 和效果不同： 字段 判断对象 当前效果 --- --- --- placement when 这个位置 条件不满足时，该 action 不出现在这个 MenuId 中 command precondition 这个 Command action action 仍可出现，但 enabled 为 false command toggled 这个 Command action 解析为 checked，并可替换 checked 时的 title、tooltip、icon keybinding when 这个快捷键入口 与 precondition 合并后决定快捷键是否匹配 f1: true Command Palette placement 自动贡献到 MenuId.CommandPalette；当前以 precondition 作为该 placement 的 when when 和 precondition 都只是 UI/输入层规则。Command 不能假设所有调用都来自该 Menu， 仍需在执行路径中维护业务不变量和权限边界。 toggled 只生成语义状态。控件会并行投影 .checked 与 ARIA 状态；是否高亮以及具体皮肤由 Toolbar/Button 等呈现层负责，详见 ui-styling-ownership.md。 分组与排序 MenuService 当前按以下顺序解析 contribution： 1. 用 placement when 过滤。 2. 先按 group 排序：navigation 最前，无 group 最后，其余按名称排序。 3. 同一 group 内按 order 升序。 4. order 相同时按 title 排序。 5. 递归解析 submenu。 MenuWorkbenchToolBar 对 group 还有一层明确的呈现规则： Group 当前 Toolbar 呈现 --- --- navigation primary action，直接显示 其他 group 或无 group secondary action，以 separator 分组后进入 More Actions 因此 group 既参与稳定排序，也会影响菜单型 Toolbar 的 primary/secondary 布局。不要仅为 得到某种 CSS 外观随意使用 navigation；它表达的是主要、直接可达的 action。 宿主如何消费 MenuId Host 只选择槽位，不收集功能模块： `ts const toolbar = new MenuWorkbenchToolBar menuService, contextMenuService, MenuId.TitleBar, ownerDocument, presentation: \"inherit-foreground\" , ; ` MenuWorkbenchToolBar 会： 1. 通过 menuService.createMenu menuId 创建当前 context 下的可观察视图。 2. 调用 menu.getActions 获得分组后的 runtime actions。 3. 将 navigation 投影到 primary，其余 group 投影到 secondary。 4. 在 Menu Registry 或 Context Key 变化时重新解析和渲染。 5. 点击 MenuItemAction 时通过 CommandService.executeCommand 执行同 ID Command。 Titlebar 因而只依赖 MenuId.TitleBar，不需要知道 Toggle Panel 是哪个模块注册的；Panel 模块也不需要取得 Titlebar 实例。MenuId 是二者之间稳定、低耦合的组合边界。 Toolbar 三层边界 三层按 action 来源和依赖方向拆分，不按视觉拆分；它们最终都保持 role=\"toolbar\"： `text ToolBar ↑ 提供 platform action view item 适配 WorkbenchToolBar ↑ 提供 MenuId 自动解析与刷新 MenuWorkbenchToolBar ` 层 调用方式 典型用途 --- --- --- ToolBar setActions primary, secondary base 控件和不应依赖 platform actions 的底层调用方 WorkbenchToolBar setActions primary, secondary 调用方已经拥有 actions，但其中可能包含 platform menu actions MenuWorkbenchToolBar 构造时传入 MenuId Host 只拥有贡献槽位，actions 由 MenuService 提供 WorkbenchToolBar 当前的真实职责是 action representation 适配：调用方可以手工传入 MenuItemAction、SubmenuItemAction 或普通 IAction，它会选择正确的 toolbar view item。 它不自动读取 MenuId，也没有复制 VS Code 的 action 隐藏、遥测、配置快捷键或复杂 overflow 策略。以后只有这些能力形成真实共享需求时，才继续加入这一层。 MenuWorkbenchToolBar 在此基础上拥有 IMenu 生命周期、group 到 primary/secondary 的投影和刷新。它的 Menu 表示 action 来源，不表示 ARIA role；常驻 按钮区域仍是 toolbar，只有 More Actions 弹层及应用菜单使用 menu/menuitem。 什么时候使用哪一层 场景 推荐 --- --- 多个模块会向同一区域贡献 action 使用 MenuId-backed Toolbar/Menu action 需要随 Context Key 动态出现、禁用或 checked 使用 MenuId + MenuService 同一 Command 要进入 Titlebar、Menubar、Command Palette 等多个入口 使用 Action2 的多个 placement Workbench 调用方已经拥有完整 action 列表 使用 WorkbenchToolBar base 控件内部一次性构造固定 action，且不应依赖 platform actions 使用 ToolBar 只需要程序调用或快捷键，不需要 UI 入口 只注册 Command/Keybinding 选择 Toolbar 时按以下顺序机械判断，不根据外观或所在 Part 猜测： 判断 是 否 --- --- --- action 是否来自 MenuId / MenuService？ 使用 MenuWorkbenchToolBar 继续判断 调用方是否已经拥有完整的 primary/secondary action 列表？ Workbench 产品代码使用 WorkbenchToolBar 先明确 action 的 owner 和来源 是否是 src/zeta/base 内部的领域无关控件？ 可以使用 ToolBar 不得直接构造 base ToolBar 只有 base UI 和 platform/actions 中实现标准适配层的代码可以直接依赖 base ToolBar。 Workbench Part、View、Contribution 等产品代码必须在 WorkbenchToolBar 与 MenuWorkbenchToolBar 之间选择。MenuWorkbenchToolBar 自己拥有 action 列表；调用方 通过注册 Command、Menu contribution 和更新 Context Key 改变内容，不调用 setActions 。 Titlebar、Editor title、Chat title 与 Terminal title 当前都使用 MenuWorkbenchToolBar。Terminal 拥有 MenuId.TerminalTitle，其 profile、New、Relaunch 与 Kill action 注册为 Command + Menu contribution；profile 使用自定义 action view item 保留原生选择控件，显隐与 enablement 由 Terminal Context Key 投影。 生命周期与失败语义 - MenusRegistry 是当前 JavaScript realm 内的共享注册表。 - appendMenuItem 与 registerAction2 都返回可释放注册；动态调用方必须与自己的 生命周期绑定。 - MenuService.getMenuActions 是一次性解析；createMenu 返回可观察、可释放的 IMenu。 - 当前 IMenu.onDidChange 会响应任意 Menu Registry 变化和任意 Context Key 变化，调用方 应假设事件表示“可能需要重算”，不能把它当成某个字段的精确变更通知。 - getMenuItems 返回数组副本，调用方不能借此修改 Registry。 - Submenu 循环会报错；空 submenu 默认丢弃。 - Command 的执行错误由 CommandService.executeCommand 返回的 Promise 向调用方传播。 禁止的耦合方式 - ❌ Titlebar 为了显示 Panel action 而直接 import TogglePanelAction 并手工构造按钮。 - ❌ 功能模块直接查询 Titlebar DOM 后插入 action。 - ❌ 只注册 Menu placement，却没有注册相同 ID 的 Command 执行入口。 - ❌ 把 when 或 precondition 当成安全校验。 - ❌ 用 MenuId.id 拼 CSS selector 或作为视觉 variant。 - ❌ 在 host CSS 中根据某个 Command ID 重写通用 Toolbar/Button 的内部 checked 样式。 - ❌ 在多个模块中重复构造同名 MenuId。 - ❌ Workbench Part、View 或 Contribution 直接构造 base ToolBar，绕过 platform action 适配。 - ❌ 调用方对 MenuWorkbenchToolBar 调用 setActions ；它的 actions 只能来自构造时 指定的 MenuId。 - ❌ 因为类名包含 Menu 就把常驻 Toolbar 设置为 role=\"menu\"；只有弹出的菜单容器和 菜单项使用 menu / menuitem。 当前实现状态 能力 状态 --- --- 一个 Action 原子注册 Command、Menu、Keybinding 和 F1 ✅ 一个 Command 贡献到多个 MenuId ✅ Context 驱动 visibility、enablement、checked ✅ Group/order 排序与递归 Submenu ✅ Registry 与 Context 变化后刷新 Menu-backed Toolbar ✅ Submenu 循环检测和空 Submenu 策略 ✅ ToolBar → WorkbenchToolBar → MenuWorkbenchToolBar 分层 ✅ Terminal title actions 接入 MenuId.TerminalTitle ✅ 只通知受影响 MenuId 和相关 Context Key 尚未完成 每个 MenuId 的显式 owner catalog 尚未完成 每个 Toolbar 自定义 group 到 primary/secondary 的策略 尚未完成 当前限制 1. Menu 当前订阅所有 Menu Registry 与 Context Key 变化，还没有按自身 MenuId 和表达式 依赖做精确失效。 2. MenuWorkbenchToolBar 当前固定把 navigation 作为 primary，把所有其他 group 作为 secondary；host 不能声明更丰富的分组呈现策略。 3. MenuId 的 owner 目前主要依赖静态定义和命名，没有独立 catalog。新增槽位时必须由 实际消费它的 host/domain 拥有并导出。 4. WorkbenchToolBar 当前只拥有 platform action representation 适配；VS Code 中的 action 隐藏、配置快捷键、遥测和复杂 overflow 策略尚未实现，不能描述为当前能力。 扩展原则 后续扩展 Menu 系统时保持以下不变量： 1. Command 是可执行行为的唯一 canonical ID；Menu placement 不复制实现。 2. MenuId 由消费该位置的 host/domain 拥有，不由某个偶然的首个 contributor 拥有。 3. Base UI 不得反向依赖 Workbench Menu 系统；platform/actions 负责把 Workbench contribution 适配为 base IAction。 4. Context Key 负责 UI 策略投影，不替代业务校验。 5. Group 表达 action 组织语义，不表达 CSS 皮肤。 6. 视觉差异通过控件 presentation 和 token 解决，不污染 MenuId 或 Command。 7. 新增能力必须区分当前实现、已决定但未实现的 proposed work 与潜在方向。 8. WorkbenchToolBar 只接收调用方 actions；MenuId 监听与刷新只能由 MenuWorkbenchToolBar 拥有。 审查清单 - 这是一个可执行行为，还是一个 UI 位置？ - Command 是否只注册一次，并由所有 UI 入口复用？ - MenuId 是否由消费该位置的 host/domain 拥有？ - Toolbar 的 action 来源是 MenuId，还是调用方拥有的 action 列表？ - Workbench 产品代码是否直接构造了 base ToolBar？ - 是否错误地对 MenuWorkbenchToolBar 调用了 setActions ？ - 常驻 action 区域是否保持 role=\"toolbar\"，仅弹出菜单使用 menu / menuitem？ - when、precondition 与 toggled 是否使用了正确语义？ - group 是否表达真实组织关系，order` 是否只在同组内比较？ - 动态注册是否随 owner 释放？ - Command 执行路径是否仍维护业务不变量和权限校验？ - 样式是否留在对应控件，而不是写进 Menu/Command 层？ - 文档与代码是否明确区分当前能力和后续方向？"
  },
  {
    "slug": "icons",
    "sourcePath": "docs/icons.md",
    "title": "Product Icon System：资源、语义与渲染边界",
    "description": "Product icon 是 renderer-independent semantic identity，不是某个 component 或 GPU backend 的资源私有类型：",
    "group": "界面与体验",
    "markdown": "> 状态：Current。\n> 本文拥有跨 Desktop、Rust native 与 renderer 的 product-icon ownership。Canonical SVG\n> 文件操作见 [`resources/README.md`](../resources/README.md)，Rust API 与生成路径见\n> [`zeta-icons`](../zeterm/icons/README.md)。\n\n## 快速理解\n\nProduct icon 是 renderer-independent semantic identity，不是某个 component 或 GPU backend\n的资源私有类型：\n\n```text\nresources/icons/*.svg\n  ├─ Desktop generator → private SVG factories → semantic registry → browser SVG renderer\n  └─ Rust generator → private artwork → explicit semantic library\n                                     → zeta-icons\n                       → zui PaintIcon → zeta-ui IconLabel / Button\n                       → native product host\n```\n\n| 想做什么 | 使用的契约 | 不应该传递什么 |\n| --- | --- | --- |\n| 在产品界面使用已有图标 | 稳定语义图标 ID | 文件名或原始 SVG |\n| 更换图稿但保留含义 | 更新语义 ID 对应的资源 | 要求所有调用方改名 |\n| 增加新的产品动作图标 | 显式注册新的语义图标 | 让资源目录自动扩张公共 API |\n| 在不同渲染器显示图标 | 各渲染器消费同一语义定义 | 把 GPU 或组件类型放入资源 crate |\n\n## 2. 所有权\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| Canonical first-party SVG artwork | `resources/icons` | ✅ |\n| Desktop generated SVG factories | `desktop/generated/product-icons.ts` | ✅ |\n| Desktop semantic registration与resolution | `base/common/icon.ts` / `lxiconsLibrary.ts` | ✅ |\n| Rust semantic identity、definition 与 rendering mode | `zeta-icons` | ✅ |\n| Rust logical placement、tint 与 clip scene contract | `zui::PaintIcon` | ✅ |\n| Rust icon+text component geometry | `zeta-ui::IconLabel` | ✅ |\n| Product command 与 icon selection | 各 product host | ✅ |\n| Seti file-extension/theme resolution | `zeta-file-icons` | ✅，独立系统 |\n| Native symbolic mask、fixed-color atlas 与 render path | `zeta-wgpu` | ✅ |\n\n`zeta-icons` 不依赖 `zui` 或 `zeta-ui`。`PaintIcon`、`IconLabel`、`Button` 和 `InputBox` 可以依赖 icon identity，但\n资源 crate 不得包含 component、font、layout、theme color、GPU 或 input routing。\n\n## 3. 身份与图稿\n\n- 产品接口传递 `Icon` / `IconId`，不传 filename 或 raw SVG；\n- public semantic library 由产品显式登记，不能由 resource filename 自动扩张；\n- semantic ID 与 artwork 是多对一关系，允许稳定 alias 和无调用方迁移的 artwork 替换；\n- checked-in generated binding 保证 Cargo/Bazel compile action 不运行 generator；\n- `IconRendering::Symbolic` 表示整个图标由 caller tint；\n- `IconRendering::Multicolor` 表示固定颜色必须保留，同时黑色 symbolic region 可以跟随 caller\n  tint；\n- renderer 不支持某种 mode 时必须显式失败，不能静默降级为错误颜色；\n- resource filename 是 artwork generation input，不是 component API。\n\n## 4. 当前实现\n\nRust generator 扫描全部 canonical SVG，生成 164 个 crate-private `IconDefinition` binding；\n`library.rs` 显式登记与 Desktop `lxiconsLibrary` 对齐的公共 semantic constants、排序后的\n`ALL_ICONS` 和 `icon_by_id` lookup。`history → refresh.svg`、`dropdown-indicator →\nchevron-down.svg` 等映射证明 semantic identity 不依赖 filename。`Button` 的 icon+text paint\npath 复用 `IconLabel`。\n`zeta-wgpu` 使用共享区域分配的 R8 symbolic-mask atlas 与 sRGB RGBA fixed-color atlas。Symbolic\nartwork 只写 mask；multicolor artwork 经 `resvg` 栅格化后，把纯黑 coverage 写入 mask、其余\n颜色写入 fixed-color atlas，shader 再把 caller tint 与固定色合成为一个 icon draw。\n\nNative shell 从 `zeta-icons::icons` 选择语义 icon，再交给 component；titlebar sidebar toggle\n已同时消费 symbolic 与 multicolor artwork。\n\n## 5. 修改路径\n\n```bash\nnode scripts/sync-rust-icons.mjs\nnode scripts/sync-rust-icons.mjs --check\ncargo test --manifest-path Cargo.toml -p zeta-icons -p zeta-ui\n```\n\nDesktop 继续运行自己的 generate/check/optimize workflow。新增 SVG 必须同时更新两个 checked-in\ngenerated output；新增公共语义还必须显式更新两个客户端的 library。未来可以把两个 generator\n的 discovery/validation 合并为 repository-level tool，但不能让 Rust 或 Desktop build 在编译\n阶段隐式改写源码。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 2,
        "id": "3-身份与图稿",
        "title": "3. 身份与图稿"
      },
      {
        "depth": 2,
        "id": "4-当前实现",
        "title": "4. 当前实现"
      },
      {
        "depth": 2,
        "id": "5-修改路径",
        "title": "5. 修改路径"
      }
    ],
    "searchText": " 状态：Current。 本文拥有跨 Desktop、Rust native 与 renderer 的 product-icon ownership。Canonical SVG 文件操作见 resources/README.md，Rust API 与生成路径见 zeta-icons。 快速理解 Product icon 是 renderer-independent semantic identity，不是某个 component 或 GPU backend 的资源私有类型： ``text resources/icons/.svg ├─ Desktop generator → private SVG factories → semantic registry → browser SVG renderer └─ Rust generator → private artwork → explicit semantic library → zeta-icons → zui PaintIcon → zeta-ui IconLabel / Button → native product host ` 想做什么 使用的契约 不应该传递什么 --- --- --- 在产品界面使用已有图标 稳定语义图标 ID 文件名或原始 SVG 更换图稿但保留含义 更新语义 ID 对应的资源 要求所有调用方改名 增加新的产品动作图标 显式注册新的语义图标 让资源目录自动扩张公共 API 在不同渲染器显示图标 各渲染器消费同一语义定义 把 GPU 或组件类型放入资源 crate 2. 所有权 能力 当前 owner 状态 --- --- --- Canonical first-party SVG artwork resources/icons ✅ Desktop generated SVG factories desktop/generated/product-icons.ts ✅ Desktop semantic registration与resolution base/common/icon.ts / lxiconsLibrary.ts ✅ Rust semantic identity、definition 与 rendering mode zeta-icons ✅ Rust logical placement、tint 与 clip scene contract zui::PaintIcon ✅ Rust icon+text component geometry zeta-ui::IconLabel ✅ Product command 与 icon selection 各 product host ✅ Seti file-extension/theme resolution zeta-file-icons ✅，独立系统 Native symbolic mask、fixed-color atlas 与 render path zeta-wgpu ✅ zeta-icons 不依赖 zui 或 zeta-ui。PaintIcon、IconLabel、Button 和 InputBox 可以依赖 icon identity，但 资源 crate 不得包含 component、font、layout、theme color、GPU 或 input routing。 3. 身份与图稿 - 产品接口传递 Icon / IconId，不传 filename 或 raw SVG； - public semantic library 由产品显式登记，不能由 resource filename 自动扩张； - semantic ID 与 artwork 是多对一关系，允许稳定 alias 和无调用方迁移的 artwork 替换； - checked-in generated binding 保证 Cargo/Bazel compile action 不运行 generator； - IconRendering::Symbolic 表示整个图标由 caller tint； - IconRendering::Multicolor 表示固定颜色必须保留，同时黑色 symbolic region 可以跟随 caller tint； - renderer 不支持某种 mode 时必须显式失败，不能静默降级为错误颜色； - resource filename 是 artwork generation input，不是 component API。 4. 当前实现 Rust generator 扫描全部 canonical SVG，生成 164 个 crate-private IconDefinition binding； library.rs 显式登记与 Desktop lxiconsLibrary 对齐的公共 semantic constants、排序后的 ALLICONS 和 iconbyid lookup。history → refresh.svg、dropdown-indicator → chevron-down.svg 等映射证明 semantic identity 不依赖 filename。Button 的 icon+text paint path 复用 IconLabel。 zeta-wgpu 使用共享区域分配的 R8 symbolic-mask atlas 与 sRGB RGBA fixed-color atlas。Symbolic artwork 只写 mask；multicolor artwork 经 resvg 栅格化后，把纯黑 coverage 写入 mask、其余 颜色写入 fixed-color atlas，shader 再把 caller tint 与固定色合成为一个 icon draw。 Native shell 从 zeta-icons::icons 选择语义 icon，再交给 component；titlebar sidebar toggle 已同时消费 symbolic 与 multicolor artwork。 5. 修改路径 `bash node scripts/sync-rust-icons.mjs node scripts/sync-rust-icons.mjs --check cargo test --manifest-path Cargo.toml -p zeta-icons -p zeta-ui `` Desktop 继续运行自己的 generate/check/optimize workflow。新增 SVG 必须同时更新两个 checked-in generated output；新增公共语义还必须显式更新两个客户端的 library。未来可以把两个 generator 的 discovery/validation 合并为 repository-level tool，但不能让 Rust 或 Desktop build 在编译 阶段隐式改写源码。"
  },
  {
    "slug": "search",
    "sourcePath": "docs/search.md",
    "title": "搜索：Rust 权威执行与桌面端投影",
    "description": "zeta-search 承担跨文件内容检索，默认作用域是当前主工作目录；App Server 只把 RPC 映射为 它的领域请求与结果； Desktop Search contrib 只拥有查询表单、取消时机、增量结果投影和可丢弃的视图状态。当前实现使用 workspace/search/start、workspace/search/read、workspace/sear…",
    "group": "界面与体验",
    "markdown": "> 本文是 workspace 内容搜索的跨进程 ownership、产品语义和演进边界的 canonical 文档。\n> 跨文件内容搜索的实现细节见\n> [`zeta-rs/search/README.md`](../zeta-rs/search/README.md)，App Server 的\n> RPC 适配见 [`zeta-rs/app-server/README.md`](../zeta-rs/app-server/README.md)，wire DTO 与生成流程见\n> [`zeta-rs/app-server-protocol/README.md`](../zeta-rs/app-server-protocol/README.md)。\n\n## 快速理解\n\n`zeta-search` 承担跨文件内容检索，默认作用域是当前主工作目录；App Server 只把 RPC 映射为\n它的领域请求与结果；\nDesktop Search contrib 只拥有查询表单、取消时机、增量结果投影和可丢弃的视图状态。当前实现使用\n`workspace/search/start`、`workspace/search/read`、`workspace/search/cancel` 三个 pull RPC，\n而不是把 `rg` 进程、路径授权或无界结果流放进 Renderer。\n\n这套 RPC 是产品搜索能力，不是模型 Tool。模型是否可以调用搜索、如何审批以及如何向模型压缩\n结果属于另一条 Tool/Policy contract；两者不能共享隐式权限或生命周期。\n\n| 用户操作 | 当前行为 | 当前限制 |\n| --- | --- | --- |\n| 搜索主工作目录文字 | 分批返回匹配项并持续显示进度 | 最多 5,000 条结果 |\n| 搜索附加目录 | 目标语义是与主目录一起搜索 | `add-dir` 尚未实现 |\n| 修改查询 | 取消旧任务并启动新任务 | 旧任务结果不会混入新查询 |\n| 使用正则或文件过滤 | 在 Rust 边界重新校验输入 | 只能使用工作区相对 glob |\n| 点击结果打开文件 | 尚未接通编辑器 | 结果当前只用于查看 |\n| 让模型调用搜索 | 走独立工具与权限契约 | 不复用界面搜索权限 |\n\n## 所有权\n\n| 能力 | Owner | 当前状态 |\n| --- | --- | --- |\n| query、大小写、正则和 include/exclude 输入 | Renderer | ✅ |\n| 结果分组、高亮、状态和重新搜索取消 | Renderer | ✅ |\n| IPC sender、exact shape 与输入上限的快速校验 | Electron Main | ✅ |\n| workspace root 授权与 `rg` executable 冻结 | Rust / App Server composition | ✅ |\n| 主工作目录与附加目录的 Search scope | `zeta-add-dir` + App Server | 领域模型已实现，runtime 尚未接入 |\n| 查询校验、`rg` 进程、结果解析、分页与取消 | `zeta-search` | ✅ |\n| connection ID → `SearchOwner`、DTO 转换与稳定 RPC error | App Server | ✅ |\n| wire DTO、method registry、schema 与 TypeScript bindings | `zeta-app-server-protocol` | ✅ |\n| 文件路径 fuzzy match | `zeta-file-search` | ✅，与内容搜索无依赖 |\n| 点击结果后读取文件并打开编辑器 | Files / Editor vertical | 尚未完成 |\n| 独立 workspace code index | `zeta-code-index` + App Server | ✅ 本地 lexical chunk retrieval；不作为当前 SearchView backend |\n| replace、multi-root 和 watcher 驱动的产品搜索失效 | 未确定 | 尚未完成 |\n\n## 端到端流程\n\n```text\nSearchViewPane\n  → IWorkspaceSearchService.search(query, signal, onProgress)\n  → trusted zeta:workspace-search:* IPC\n  → AppServerClient\n  → workspace/search/start\n  → App Server maps DTO + connection to SearchQuery + SearchOwner\n  → zeta-search::SearchService job\n  → frozen RipgrepExecutable under WorkspaceRoot\n  → workspace/search/read batches\n  → renderer groups and highlights matches\n  → workspace/search/cancel releases the job\n```\n\n`start` 冻结查询参数并返回 opaque `searchId`。App Server 把 connection ID 映射成不含传输语义的\n`SearchOwner`；搜索 crate 只比较 owner，不依赖 JSON-RPC。`read` 使用 `afterMatch` cursor 读取最多 200 条；\n没有新结果且作业仍在运行时可以返回空 batch。Renderer 只在结果非空时推进 cursor。\n完成、取消或 Renderer 异常退出当前搜索流程时都会调用 `cancel`；完成作业也会在服务端延迟清理，\n因此 cleanup RPC 失败不改变已返回结果。\n\n## 当前语义与边界\n\n- 查询不能为空，UTF-8 最多 16 KiB；单次搜索最多返回 5,000 条，Desktop 默认 2,000 条。\n- include/exclude 各最多 64 个 workspace-relative glob，每项最多 1 KiB；绝对路径、`..`、\n  前导 `!` 和 NUL 被拒绝。\n- `zeta-search` 使用 host discovery 后冻结的 `rg` executable，使用 argument vector 和\n  `shell: false` 等价的进程 API，不做 shell 拼接。\n- `rg` 未安装时 stdio App Server 仍可启动，但 `workspaceSearch` capability 为 `false`，\n  Search 调用返回 `SearchUnavailable`；Desktop 会把显式 `ZETA_RG_PATH` 透传给可信子进程。\n- 搜索 cwd 固定为受信 `WorkspaceRoot`；当前沿用 ripgrep 默认 ignore/hidden 行为。\n- preview 单行最多由 `--max-columns=1000 --max-columns-preview` 约束，单文件最大 16 MiB。\n- match range 在 Rust 中从 ripgrep byte offset 转换为 UTF-16 offset，Renderer 可直接用于\n  JavaScript 字符串切片。\n- job 绑定创建它的 App Server connection。其他 connection 的 read/cancel 返回\n  `SearchNotOwner`；未知或已释放 ID 返回 `SearchNotFound`。\n- 同一 server 最多保留 32 个 job；超限返回 `SearchBusy`。已完成 job 最长保留约 5 分钟。\n- 进程或解析失败通过 terminal `read.error` 返回稳定、已脱敏的说明；不会把任意 stderr 暴露给\n  Renderer。\n\n当前 SearchViewPane 只显示根相对路径、行号、preview 与高亮。它不尝试用现有目录枚举 API\n拼出文件内容，也不伪造 editor input；在 Files 具备受约束的 read-file contract、Editor\n具备稳定打开路径后，再把结果激活接到该 vertical。\n\n## 取舍\n\n| 方案 | 判断 | 原因 |\n| --- | --- | --- |\n| Renderer 直接运行 `rg` | ❌ | 绕过 sandbox、workspace authority 和跨客户端产品语义 |\n| 单个同步 RPC 返回全部结果 | ❌ | 受 1 MiB JSONL frame 限制，取消与首批结果延迟也更差 |\n| JSON-RPC notification 推送每条结果 | 暂不采用 | 当前 notification queue 无 backpressure，慢消费者可放大内存 |\n| connection-owned pull job | ✅ | bounded batch、显式取消，并复用现有同步 JSONL transport |\n| 复活旧模型 Search Tool 作为 UI backend | ❌ | 产品 UI 和模型 Tool 的调用者、权限及结果预算不同 |\n\n## 演进\n\n近期只在现有 contract 内完善可用性：空结果/错误呈现、查询历史和搜索中再次提交。结果点击必须\n等待受信 file-content API 与 editor opening contract，不由 Search 绕过。\n\n当前只有一个主工作目录；`zeta-add-dir` 已实现纯 scope contract，但 App Server/Search runtime\n尚未接入。未来 Search scope 由主工作目录和全部具备文件读取权限的附加目录组成，不把附加目录\n提升为独立项目，也不触发项目配置加载。无论目录来自启动参数、会话命令还是持久\n`additionalDirectories`，只要其 file-access grant 有效，Search 都可以消费它；Agent Import 的\n一次性来源则不能自动进入 Search。\n\n多 root Search 不能继续只返回裸 relative path。协议必须增加稳定 root identity 或 root alias，\n使 `src/lib.rs` 能明确归属于主目录或某个附加目录，并避免不同 root 的同名 path 碰撞。Glob、\nignore 与 containment 也必须逐 root 计算，不能先把多个绝对目录拼成一个伪 Workspace。\n\n当前已经存在独立的 [`zeta-code-index`](../zeta-rs/code-index/README.md)，它在 workspace side\n完成 ignore-aware chunking、持久化 generation 与 FTS5 retrieval；跨系统边界见\n[`code-index.md`](code-index.md)。它服务 revision-bound chunk retrieval，不替换本页的逐行文字/\n正则产品搜索。是否把 SearchView 迁移到索引 backend 必须先证明 regex、glob、UTF-16 range、\nconnection-owned cancel 和结果完整性语义等价；当前 `searchId` 不承诺 backend 类型。\n\n只有 transport 获得有界 backpressure 后，才考虑用 notification 替代 pull。\n\n长期不变项是：Renderer 不获得任意进程或磁盘权限；workspace 授权在 Rust 可信边界重复校验；\n结果传输有明确上限；job 不跨 connection 泄漏；产品搜索与模型 Tool 保持独立 contract。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "端到端流程",
        "title": "端到端流程"
      },
      {
        "depth": 2,
        "id": "当前语义与边界",
        "title": "当前语义与边界"
      },
      {
        "depth": 2,
        "id": "取舍",
        "title": "取舍"
      },
      {
        "depth": 2,
        "id": "演进",
        "title": "演进"
      }
    ],
    "searchText": " 本文是 workspace 内容搜索的跨进程 ownership、产品语义和演进边界的 canonical 文档。 跨文件内容搜索的实现细节见 zeta-rs/search/README.md，App Server 的 RPC 适配见 zeta-rs/app-server/README.md，wire DTO 与生成流程见 zeta-rs/app-server-protocol/README.md。 快速理解 zeta-search 承担跨文件内容检索，默认作用域是当前主工作目录；App Server 只把 RPC 映射为 它的领域请求与结果； Desktop Search contrib 只拥有查询表单、取消时机、增量结果投影和可丢弃的视图状态。当前实现使用 workspace/search/start、workspace/search/read、workspace/search/cancel 三个 pull RPC， 而不是把 rg 进程、路径授权或无界结果流放进 Renderer。 这套 RPC 是产品搜索能力，不是模型 Tool。模型是否可以调用搜索、如何审批以及如何向模型压缩 结果属于另一条 Tool/Policy contract；两者不能共享隐式权限或生命周期。 用户操作 当前行为 当前限制 --- --- --- 搜索主工作目录文字 分批返回匹配项并持续显示进度 最多 5,000 条结果 搜索附加目录 目标语义是与主目录一起搜索 add-dir 尚未实现 修改查询 取消旧任务并启动新任务 旧任务结果不会混入新查询 使用正则或文件过滤 在 Rust 边界重新校验输入 只能使用工作区相对 glob 点击结果打开文件 尚未接通编辑器 结果当前只用于查看 让模型调用搜索 走独立工具与权限契约 不复用界面搜索权限 所有权 能力 Owner 当前状态 --- --- --- query、大小写、正则和 include/exclude 输入 Renderer ✅ 结果分组、高亮、状态和重新搜索取消 Renderer ✅ IPC sender、exact shape 与输入上限的快速校验 Electron Main ✅ workspace root 授权与 rg executable 冻结 Rust / App Server composition ✅ 主工作目录与附加目录的 Search scope zeta-add-dir + App Server 领域模型已实现，runtime 尚未接入 查询校验、rg 进程、结果解析、分页与取消 zeta-search ✅ connection ID → SearchOwner、DTO 转换与稳定 RPC error App Server ✅ wire DTO、method registry、schema 与 TypeScript bindings zeta-app-server-protocol ✅ 文件路径 fuzzy match zeta-file-search ✅，与内容搜索无依赖 点击结果后读取文件并打开编辑器 Files / Editor vertical 尚未完成 独立 workspace code index zeta-code-index + App Server ✅ 本地 lexical chunk retrieval；不作为当前 SearchView backend replace、multi-root 和 watcher 驱动的产品搜索失效 未确定 尚未完成 端到端流程 ``text SearchViewPane → IWorkspaceSearchService.search query, signal, onProgress → trusted zeta:workspace-search: IPC → AppServerClient → workspace/search/start → App Server maps DTO + connection to SearchQuery + SearchOwner → zeta-search::SearchService job → frozen RipgrepExecutable under WorkspaceRoot → workspace/search/read batches → renderer groups and highlights matches → workspace/search/cancel releases the job ` start 冻结查询参数并返回 opaque searchId。App Server 把 connection ID 映射成不含传输语义的 SearchOwner；搜索 crate 只比较 owner，不依赖 JSON-RPC。read 使用 afterMatch cursor 读取最多 200 条； 没有新结果且作业仍在运行时可以返回空 batch。Renderer 只在结果非空时推进 cursor。 完成、取消或 Renderer 异常退出当前搜索流程时都会调用 cancel；完成作业也会在服务端延迟清理， 因此 cleanup RPC 失败不改变已返回结果。 当前语义与边界 - 查询不能为空，UTF-8 最多 16 KiB；单次搜索最多返回 5,000 条，Desktop 默认 2,000 条。 - include/exclude 各最多 64 个 workspace-relative glob，每项最多 1 KiB；绝对路径、..、 前导 ! 和 NUL 被拒绝。 - zeta-search 使用 host discovery 后冻结的 rg executable，使用 argument vector 和 shell: false 等价的进程 API，不做 shell 拼接。 - rg 未安装时 stdio App Server 仍可启动，但 workspaceSearch capability 为 false， Search 调用返回 SearchUnavailable；Desktop 会把显式 ZETARGPATH 透传给可信子进程。 - 搜索 cwd 固定为受信 WorkspaceRoot；当前沿用 ripgrep 默认 ignore/hidden 行为。 - preview 单行最多由 --max-columns=1000 --max-columns-preview 约束，单文件最大 16 MiB。 - match range 在 Rust 中从 ripgrep byte offset 转换为 UTF-16 offset，Renderer 可直接用于 JavaScript 字符串切片。 - job 绑定创建它的 App Server connection。其他 connection 的 read/cancel 返回 SearchNotOwner；未知或已释放 ID 返回 SearchNotFound。 - 同一 server 最多保留 32 个 job；超限返回 SearchBusy。已完成 job 最长保留约 5 分钟。 - 进程或解析失败通过 terminal read.error 返回稳定、已脱敏的说明；不会把任意 stderr 暴露给 Renderer。 当前 SearchViewPane 只显示根相对路径、行号、preview 与高亮。它不尝试用现有目录枚举 API 拼出文件内容，也不伪造 editor input；在 Files 具备受约束的 read-file contract、Editor 具备稳定打开路径后，再把结果激活接到该 vertical。 取舍 方案 判断 原因 --- --- --- Renderer 直接运行 rg ❌ 绕过 sandbox、workspace authority 和跨客户端产品语义 单个同步 RPC 返回全部结果 ❌ 受 1 MiB JSONL frame 限制，取消与首批结果延迟也更差 JSON-RPC notification 推送每条结果 暂不采用 当前 notification queue 无 backpressure，慢消费者可放大内存 connection-owned pull job ✅ bounded batch、显式取消，并复用现有同步 JSONL transport 复活旧模型 Search Tool 作为 UI backend ❌ 产品 UI 和模型 Tool 的调用者、权限及结果预算不同 演进 近期只在现有 contract 内完善可用性：空结果/错误呈现、查询历史和搜索中再次提交。结果点击必须 等待受信 file-content API 与 editor opening contract，不由 Search 绕过。 当前只有一个主工作目录；zeta-add-dir 已实现纯 scope contract，但 App Server/Search runtime 尚未接入。未来 Search scope 由主工作目录和全部具备文件读取权限的附加目录组成，不把附加目录 提升为独立项目，也不触发项目配置加载。无论目录来自启动参数、会话命令还是持久 additionalDirectories，只要其 file-access grant 有效，Search 都可以消费它；Agent Import 的 一次性来源则不能自动进入 Search。 多 root Search 不能继续只返回裸 relative path。协议必须增加稳定 root identity 或 root alias， 使 src/lib.rs 能明确归属于主目录或某个附加目录，并避免不同 root 的同名 path 碰撞。Glob、 ignore 与 containment 也必须逐 root 计算，不能先把多个绝对目录拼成一个伪 Workspace。 当前已经存在独立的 zeta-code-index，它在 workspace side 完成 ignore-aware chunking、持久化 generation 与 FTS5 retrieval；跨系统边界见 code-index.md。它服务 revision-bound chunk retrieval，不替换本页的逐行文字/ 正则产品搜索。是否把 SearchView 迁移到索引 backend 必须先证明 regex、glob、UTF-16 range、 connection-owned cancel 和结果完整性语义等价；当前 searchId` 不承诺 backend 类型。 只有 transport 获得有界 backpressure 后，才考虑用 notification 替代 pull。 长期不变项是：Renderer 不获得任意进程或磁盘权限；workspace 授权在 Rust 可信边界重复校验； 结果传输有明确上限；job 不跨 connection 泄漏；产品搜索与模型 Tool 保持独立 contract。"
  },
  {
    "slug": "code-intelligence",
    "sourcePath": "docs/code-intelligence.md",
    "title": "代码知识系统：边界、现状与演进",
    "description": "Zeta 不用一个巨型数据库同时冒充语法树、语言服务器、搜索索引和 AI 上下文。当前系统已经具备 增量语法事实、完整 LSP 请求链路、本地持久化符号索引、未保存 Buffer 覆盖层、本地优先的代码 检索，以及符号感知的精确声明候选。它们共享 Workspace source identity，但分别拥有 freshness、 失败与排序语义。跨语言代码图和 navi…",
    "group": "界面与体验",
    "markdown": "> 类型：设计。状态：Current implementation + gated roadmap。本文 canonical 拥有编辑器语法事实、\n> Language Server、工作区符号索引、代码检索与未来代码图之间的跨系统边界，以及已完成纵向链路\n> 和仍需证据才能启动的阶段。\n> 当前检索实现见 [`code-index.md`](code-index.md)，编辑器语法能力见\n> [`syntax-analysis.md`](syntax-analysis.md)，精确语言语义见 [`lsp.md`](lsp.md)。各 crate 的内部接口\n> 继续由对应 README 拥有；本文不复制其实现细节。\n\n## 快速理解\n\nZeta 不用一个巨型数据库同时冒充语法树、语言服务器、搜索索引和 AI 上下文。当前系统已经具备\n增量语法事实、完整 LSP 请求链路、本地持久化符号索引、未保存 Buffer 覆盖层、本地优先的代码\n检索，以及符号感知的精确声明候选。它们共享 Workspace source identity，但分别拥有 freshness、\n失败与排序语义。跨语言代码图和 navigation cache 仍未建立：前者缺少 compiler/SCIP/resolver\n事实来源，后者要先由已安装的请求指标证明收益。\n\n| 用户场景 | 当前行为 | 目标行为 | 状态 |\n| --- | --- | --- | --- |\n| 在工作区按名称找符号 | 本地索引先返回；Language Server 并发补充；结果分阶段、确定性融合 | 维持并测量现有行为 | Current |\n| Language Server 未启动时找符号 | 支持 grammar 的语言仍可搜索语法声明 | 扩展语言只需增加可信 syntax facts | Current |\n| AI 搜索相关代码 | 本地 symbol + FTS + 可选 vector/remote，融合后由 Workspace 复核正文 | 有强证据图来源后再增加有界 graph expansion | Current + gated |\n| 搜索未保存代码 | 同一路径的磁盘 symbol、lexical、semantic、cloud 候选被 overlay 抑制 | 维持 save handoff 与 current-text invariant | Current |\n| 查定义、引用和层级 | revision-bound LSP 请求，记录冷暖延迟、结果数、取消与 stale outcome | 指标证明重复成本后才评审会话缓存 | Current + gated |\n| 跨语言导航 | 没有统一语义图或 resolver | 先接入有精确证据的 schema/generated-code 边，再评审启发式边 | Potential |\n| 结构化编辑 | Smart Select 按当前 revision/selection 请求有限 parser scopes，并保留 lexical fallback/shrink history | 继续增加 select declaration，之后才评审原子 mutation plan | 部分 Current |\n\n继续阅读：[当前基线](#1-当前基线)、[目标架构](#2-目标架构)、[数据与失效](#4-数据身份与失效)、\n[阶段计划](#8-阶段计划)、[验收门](#9-测试性能与验收门)。\n\n## 1. 当前基线\n\n截至本文建立时，仓库中的真实能力如下。未来阶段不得写成当前事实。\n\n| 能力 | 当前 owner | 当前状态 |\n| --- | --- | --- |\n| 单文档增量 parse、token、fold、document symbol、parse diagnostic | `zeta-syntax` | Current |\n| 打开文档的文本、selection、undo/redo 与 Editor revision | Aster / `zeta-editor` | Current |\n| completion、definition、references、hierarchy、workspace symbol 等精确语义 | `zeta-language-service` + `zeta-lsp` | Current |\n| Workspace scan、ignore、结构辅助切块、source/chunk identity、SQLite FTS | `zeta-code-index` | Current |\n| embedding cache、vector recall、optional rerank | `zeta-code-index-semantic` | Current |\n| lexical/semantic/optional remote 融合、源码复核与 byte budget | `zeta-code-retrieval` | Current |\n| `search_code` 与可选 first-invocation evidence | App Server + Core | Current |\n| 持久化本地 symbol projection、overlay symbol 与 fuzzy matcher | `zeta-symbol-index` | Current |\n| CodeIndex 未保存 Buffer overlay、dirty suppression 与 save handoff | `zeta-code-index` + App Server | Current |\n| Workspace Symbol staged aggregation、取消、dedupe 与 accept-time hash verification | Desktop | Current |\n| 语言请求取消与隐私安全指标 | `zeta-language-service` + App Server sink | Current |\n| references/navigation semantic cache | 无 | 未安装；等待指标门禁 |\n| SCIP、occurrence/edge graph 与跨语言 resolver | 无 | 尚未完成 |\n| revision-bound structural selection scopes + Smart Select | `zeta-syntax` + Aster | Current；mutation 尚未完成 |\n\n当前 `zeta-code-index` 的 `ChunkReference` 仍只表达 root、path、source revision、chunk key、content\nhash 和范围；`zeta-symbol-index` 单独保存 name、kind 与声明/选择范围。两者通过 source identity 与\nverified excerpt 交汇，而不是共享存储。当前系统仍不保存 occurrence 或 edge，因此“代码索引/RAG”、\n“符号索引”和“代码图”是相邻能力，不是一个数据库的不同表名。\n\n### 1.1 与统一 Code Intelligence Store / FileShard 方案的差异\n\n外部方案对能力分类是对的，但其“一份 `FileShard` 写入一个 Store，再由所有功能读取”的实现不适合\n当前 Zeta。这里采用共享 authority/identity、分离 projection/runtime：\n\n| 设计点 | 统一 Store / FileShard 方案 | Zeta 当前选择 | 结论 |\n| --- | --- | --- | --- |\n| 持久化 | files/symbols/occurrences/edges/chunks/cache 共用一个 SQLite | lexical、symbol、semantic、cloud control 各自持有可删除 projection | failure、schema、retention 与权限不同，不合库 |\n| 文件事实 | parser/LSP/SCIP 合成一个 immutable shard | `CodeIndexManifest` 只发布 Workspace-authorized sources/chunks；LSP 结果仍由 service incarnation 管理 | 不把不同 freshness 压成一个 revision |\n| dirty Buffer | overlay 覆盖单一 persistent shard | CodeIndex 拥有 canonical text/chunks；SymbolIndex 投影 declarations；retrieval 统一抑制同路径持久候选 | 共享当前文本 authority，不共享存储 |\n| symbol identity | structural fingerprint 尝试跨 edit 稳定 | `SymbolReference` 明确绑定 source revision；暂无 stable semantic `SymbolId` | 没有 compiler identity 时不作虚假稳定承诺 |\n| occurrence/edge | Tree-sitter 先写 unresolved occurrences，后由 LSP/SCIP 增强 | 当前不持久化 occurrence/edge；只有真实 compiler/SCIP/resolver consumer 后才建 graph | syntax-only 同名关系不能成为精确导航 |\n| references/navigation cache | 作为第一阶段热点能力 | 先安装 content-free cold/warm/cancel/stale metrics；收益显著后才设计 session cache | 避免复制 LSP 内部索引和 stale complexity |\n| 结构化编辑 API | 暴露 node/parent/sibling/field primitives 与长期 anchor | 按真实 command 暴露 revision-bound ranges/plans；第一条为按需 selection scopes | 不把 Tree-sitter node model 泄漏给 Editor |\n| 坐标转换 | 全系统共用一个 LineIndex | 每个 authority 内部使用 canonical coordinate，协议边界显式转换并验证 Unicode | 避免把 Editor/LSP/source revision 生命周期绑成共享 mutable service |\n| AI retrieval | graph/semantic/context store 统一读取 | retrieval 独立融合 symbol/FTS/vector/cloud，并在发给模型前由 CodeIndex 复核 exact excerpt | candidate provider 不拥有最终上下文 |\n\n实现顺序也不同。Zeta 在本计划前已经具备 LSP、SQLite lexical、semantic vector/rerank 和 Agent\nretrieval，因此没有按“先 FileShard、再 LSP、最后 semantic search”重建已有系统；本轮补的是实际缺口：\n本轮落地顺序是 symbol index → dirty overlay → symbol-aware retrieval → metrics/cancellation → on-demand Smart Select。Graph、\nresolver 和 cache 继续由证据门控制。\n\n## 2. 目标架构\n\n```mermaid\nflowchart TD\n    Disk[\"Workspace 磁盘源码\"] --> Index[\"zeta-code-index<br/>scan / ignore / revision / chunks\"]\n    Index --> Sources[\"verified manifest<br/>+ materialized sources\"]\n    Sources --> Symbols[\"zeta-symbol-index<br/>symbol projection + fuzzy search\"]\n    Sources --> Semantic[\"zeta-code-index-semantic<br/>embedding / vector / rerank\"]\n\n    Buffer[\"Editor 未保存 Buffer\"] --> Overlay[\"App Server<br/>ephemeral document overlay\"]\n    Overlay --> Index\n    Overlay --> Symbols\n\n    Lsp[\"zeta-language-service<br/>精确语言语义\"] --> WorkspaceSymbols[\"Workspace Symbol aggregator\"]\n    Symbols --> WorkspaceSymbols\n    WorkspaceSymbols --> UI[\"Ctrl/Cmd+T\"]\n\n    Index --> Retrieval[\"zeta-code-retrieval\"]\n    Semantic --> Retrieval\n    Symbols --> Retrieval\n    Graph[\"Potential zeta-code-graph\"] --> Retrieval\n    Retrieval --> Agent[\"search_code / automatic evidence\"]\n```\n\n目标系统有四类知识平面：\n\n- **编辑器语法平面**：服务当前打开文档的输入热路径，随 Editor revision 更新。\n- **语言语义平面**：Language Server 或编译器对 definition、reference、type 和 hierarchy 给出精确事实。\n- **工作区检索平面**：CodeIndex 对授权源码维护可重建的 lexical/semantic chunk projection。\n- **符号与图平面**：本地声明索引提供低延迟候选；未来 semantic facts 和 resolver 形成有证据的边。\n\n这些平面可以共享 source identity 和查询编排，但不能共享一个模糊的“最新 revision”。\n\n## 3. 所有权与依赖方向\n\n| 组件 | 拥有 | 明确不拥有 |\n| --- | --- | --- |\n| `zeta-syntax` | grammar/query、增量 tree、revision-bound syntax facts | 文件扫描、SQLite、LSP semantic identity |\n| `zeta-code-index` | Workspace scan、ignore、读取、source/chunk identity、磁盘与未来 overlay chunk authority | symbol graph、模型选择、最终 AI 排名 |\n| `zeta-symbol-index` | verified source 与 dirty overlay 的声明 projection、持久化复用、本地 exact/fuzzy symbol search | 自主扫描文件、LSP request、跨语言猜测 |\n| `zeta-language-service` | server route/incarnation、document freshness、精确 LSP 请求 | 本地 symbol database、AI retrieval |\n| `zeta-code-index-semantic` | 模型输入、embedding persistence、vector recall、rerank 与来源内排序 | scan、chunk、跨来源融合 |\n| `zeta-code-retrieval` | 多来源候选融合、identity dedupe、current-source verification 与内容预算 | 模型 transport、Workspace grant、Editor state |\n| Potential `zeta-code-graph` | semantic symbol、occurrence、typed edge、evidence/confidence | filesystem authority、UI、模型调用 |\n| App Server | trust、watcher、projection 调度、ephemeral overlay、RPC、metrics sink 与 fallback composition | parser、fuzzy 算法、Renderer 展示 |\n| Desktop | frontend service、provider 聚合、取消、渐进结果和导航展示 | 索引存储、semantic cache authority |\n\n固定依赖方向：\n\n```text\nzeta-symbol-index → zeta-code-index + zeta-syntax\nzeta-code-retrieval → zeta-code-index + optional semantic/symbol/graph sources\nApp Server → 上述 backend-neutral crates\nDesktop → generated protocol + frontend-owned service contracts\n```\n\n禁止 `zeta-symbol-index` 直接遍历 Workspace。它必须比较 `CodeIndexManifest.sources` 与自身 projection，\n并通过 `CodeIndex::materialize_sources` 只读取经过当前 source revision 复核的文件。这样 scan、ignore、\n权限、symlink 和资源限制不会出现第二套实现。\n\n## 4. 数据、身份与失效\n\n### 4.1 当前符号契约\n\n当前 public contract 把每个语法声明绑定到一个精确 Workspace source revision：\n\n```rust\npub struct SymbolReference {\n    pub root_id: IndexRootId,\n    pub relative_path: PathBuf,\n    pub source_revision: SourceRevision,\n    pub language: IndexedLanguage,\n    pub source_bytes: usize,\n    pub ordinal: usize,\n    pub declaration_range: SymbolRange,\n    pub selection_range: SymbolRange,\n}\n\npub struct IndexedSymbol {\n    pub reference: SymbolReference,\n    pub name: String,\n    pub kind: SymbolKind,\n    pub container_name: Option<String>,\n}\n\npub struct SymbolSearchHit {\n    pub symbol: IndexedSymbol,\n    pub score: u32,\n    pub matched_indices: Vec<u32>,\n}\n```\n\n第一版不定义跨 revision 稳定的 `SymbolId`。仅凭路径、位置、名称或 structural fingerprint 无法可靠\n表达 rename/move 后仍是同一语义对象。当前引用只保证绑定到一个精确 source revision；真正稳定的\nsemantic key 必须等 SCIP/compiler/LSP facts 出现真实消费者后再定义。\n\n### 4.2 投影代次\n\n符号 projection 保存自己的 generation，并记录对应的 CodeIndex source generation：\n\n```text\nSymbolIndexSnapshot\n  generation\n  source_generation\n  source_count\n  symbol_count\n  limit flags\n```\n\n一次 reconcile：\n\n1. 读取当前 `CodeIndexManifest`。\n2. 按 `relative_path + source_revision + language` 复用未变化文件的 symbol rows。\n3. 批量 materialize 新增或变化 sources。\n4. 用 `zeta-syntax` 提取有界 document symbols。\n5. 删除 manifest 已不存在的文件。\n6. 在一个 SQLite transaction 中发布新 generation。\n7. 由新 generation 构造只读内存 fuzzy candidate snapshot。\n\n`zeta-syntax` 必须提供能使持久化消费者失效的 syntax-facts identity。grammar 或 tags query 改变但\nidentity 不变会静默复用不兼容 projection，属于 correctness bug。\n\n### 4.3 不同 revision 不得混用\n\n| 事实 | freshness identity |\n| --- | --- |\n| Editor syntax | document identity + Editor revision |\n| 磁盘 symbol/chunk | root + path + source revision + projection generation |\n| LSP result | service generation + server incarnation + Editor revision |\n| semantic vector | source/chunk identity + chunker identity + model identity |\n| remote candidate | durable grant + published chunk identity + provider generation |\n| future graph edge | source fact identities + resolver/indexer identity |\n\n静态优先级如“SCIP > LSP > Tree-sitter”不是正确合并规则。来源、事实种类和 freshness 必须一起判断；\n例如 Language Server 对未保存 Buffer 的结果通常比磁盘 SCIP index 更新。\n\n## 5. Workspace Symbol 用户语义\n\n本地 symbol provider 与 Language Server provider 是互补关系：\n\n| 来源 | 优势 | 限制 | UI 语义 |\n| --- | --- | --- | --- |\n| 本地 syntax symbol index | 启动快、无需 server、跨已支持 grammar 统一搜索 | 只有声明与语法 container，没有类型解析 | 可直接打开声明位置，不宣称 semantic reference |\n| Language Server `workspace/symbol` | 语言/项目语义精确，可包含 server 独有符号 | 受启动、索引、配置和语言覆盖影响 | 异步补充或替换相同 location |\n| Future extension provider | 扩展语言或专用来源 | 受 extension trust/lifecycle 约束 | 通过统一 provider contract 合并 |\n\n一次 `Ctrl/Cmd+T` 查询应执行：\n\n1. query revision 增加并取消上一轮请求。\n2. 同时请求本地 symbol index 和全部适用 provider。\n3. 任一来源返回后发布阶段性结果，不等待最慢 provider 才首次展示。\n4. 按来源内 rank 做 deterministic fusion，并对 exact/prefix/camel/subsequence match 加权。\n5. 按 `resource + name + selection start` 去重。\n6. 只接受当前 query revision 的更新。\n7. 接受结果时由当前文件内容验证 selection text；失配则触发 document-symbol reanchor 或刷新，而不盲跳旧 range。\n\n本地 fuzzy matcher 第一版直接使用已有 workspace dependency `nucleo`，但不把文件搜索 crate 扩成\n泛化 symbol owner。共享抽象必须等第二个相同领域 consumer 出现后再提取。\n\n## 6. 未保存 Buffer 浮层\n\n当前 Editor 与 LSP 已有打开文档 revision，App Server 同时持有 Workspace-scoped 临时 projection：\n\n```text\nWorkspaceTrustId\n+ relative path\n+ editor revision\n+ language\n+ content hash\n+ full text\n```\n\n规则：\n\n- Editor 仍是未保存文本 authority；App Server 只持有最新 immutable snapshot。\n- 同一 open/change/close 事实由 host 分别投影给 LSP 和 code-intelligence overlay，两者不互相依赖。\n- overlay 不写入 SQLite，不随进程恢复，不自动外发。\n- symbol query 中 overlay 完全抑制同路径磁盘 symbols。\n- code retrieval 中 dirty path 的磁盘 lexical、semantic 和 cloud candidates 全部被抑制。\n- 第一版 dirty buffer 只建立内存 lexical/symbol projection；不在每次按键后发送 embedding。\n- 保存后保留 overlay，直到磁盘 CodeIndex 出现相同 content hash，再无缝交回持久化 generation。\n- close/discard、Workspace replacement 或 App Server host teardown 时清理对应 overlay。\n\nAI consumer 读取正文时必须先查询 overlay，缺失时才读取磁盘。不能先取磁盘 candidate，再把其旧\nrange 用到新 Buffer 上。\n\n## 7. 可靠性、安全与隐私\n\n| 条件 | 必须发生的结果 |\n| --- | --- |\n| 本地 symbol index 未 ready/失败 | LSP workspace symbol 保持可用；状态不伪装 ready |\n| Language Server 未安装或 crash | 本地 syntax symbol search 保持可用 |\n| watcher event 合并或 overflow | CodeIndex 先发布 canonical generation，再调度下游 reconcile |\n| symbol projection schema/query identity 变化 | 丢弃可重建 projection，不静默复用 |\n| query 被取消 | worker 在可用 checkpoint 停止；旧 query revision 不发布 UI |\n| unsupported language | CodeIndex 仍可按 plaintext chunk；symbol projection 跳过该文件 |\n| syntax 含 recoverable error | 返回有界可证明的 partial declarations，不提升为 semantic fact |\n| Workspace restricted | 本地只读 projection 可用；不启动 executable，不增加网络外发 |\n| remote model configured | 沿用 CodeIndex 明示 consent；symbol index 本身不获得网络能力 |\n\n本地 symbol database 与 lexical/semantic CodeIndex 一样只是可重建 projection，不提升为源码 authority。\nUnix persistent file 使用普通文件和 `0600`。路径、错误和日志不得输出源码正文或 embedding payload。\n\n## 8. 阶段计划\n\n### P0：冻结文档与基线（已完成）\n\n- 建立本文并加入文档导航与文档站。\n- 从 `code-index.md`、`syntax-analysis.md` 和 `lsp.md` 链回本文。\n- 固定当前状态、owner、non-goal、测试和性能验收口径。\n\n完成状态：已建立 canonical 边界、文档导航和 owner/non-goal；产品 Workbench 只安装 contribution。\n\n### P1：本地 Symbol 索引纵向链路（已完成）\n\n- 新增 `zeta-symbol-index` crate、README、Cargo/Bazel target。\n- 定义 revision-bound symbol types、limits、error 和 storage。\n- 实现 manifest reconcile、syntax extraction、SQLite publication、persistent reuse。\n- 实现内存 Nucleo matcher、exact/fuzzy Top K 和 query cancellation。\n- 在 App Server 中随 CodeIndex generation 调度 symbol reconcile。\n- 增加 `workspace/symbolIndex/status` 与 `workspace/symbolIndex/search`。\n- Desktop 增加 `ISymbolIndexService`、App Server implementation 和本地 workspace-symbol provider。\n- Workspace Symbol service 支持并发 provider 和阶段性结果。\n\n完成状态：`zeta-symbol-index`、App Server status/search RPC、Desktop service/provider 和 staged\nWorkspace Symbol fusion 已接通。Language Server 缺席或单个 provider 失败时本地结果仍可用；旧 query\ngeneration 不会发布，接受结果前以 SHA-256 对当前文件进行复核。\n\n### P2：未保存 Buffer 浮层（已完成）\n\n- 建立 App Server ephemeral `WorkspaceDocumentOverlay`。\n- host 把 open/change/close snapshot 同步投影给 overlay 与 LSP。\n- `zeta-symbol-index` 查询合并 overlay symbols 并抑制同路径磁盘 rows。\n- `zeta-code-index` 增加 canonical in-memory chunk overlay。\n- `zeta-code-retrieval` 对 dirty paths 抑制全部持久化/remote candidates，并从 overlay 复核正文。\n- 实现 save handoff：content hash 相同的磁盘 generation ready 后才删除 overlay。\n\n完成状态：Editor snapshot 通过 `workspace/codeIntelligence/document/synchronize|close` 同步；CodeIndex\n拥有 canonical text overlay，SymbolIndex 投影其声明；retrieval 对 dirty path 抑制所有磁盘与远端候选，\n只物化当前 overlay。相同 content hash 的磁盘 generation 发布后才 handoff。\n\n### P3：Symbol-aware AI retrieval（已完成）\n\n- 为 retrieval 增加 exact path/name 和 symbol fuzzy candidate source。\n- 将 declaration span 投影为可由 Workspace authority 复核的 excerpt reference。\n- 对 symbol、FTS、vector 和 remote 同范围命中执行 identity dedupe。\n- 在 Core evidence 中保留 source/path/range/revision/provenance，不泄漏内部 store ID。\n- 明确当前选择、显式文件/符号线索与普通 semantic match 的预算优先级。\n\n完成状态：local symbol 已成为独立 retrieval origin；声明范围通过\n`CodeIndex::materialize_verified_excerpt` 变成 content-addressed `SourceExcerptReference` 后参与融合。\n所有模型输入仍经过 overlay/disk current-source verification。\n\n### P4：导航指标与可选会话缓存（指标已完成；缓存 gated）\n\n- 记录 request kind、server incarnation、cold/warm latency、结果数、取消率与配置 generation。\n- 只有指标证明重复请求成本显著时，增加 `zeta-language-service` 内存 cache。\n- cache key 至少绑定 server incarnation、semantic config generation、request kind、source revision、\n  position 和请求选项。\n- server replacement、配置变化或相关 document revision 变化时失效。\n\n当前状态：已记录 request kind、server incarnation、configuration/service generation、cold/warm、elapsed、\nresult count，以及 delivered/empty/failed/cancelled/stale-discarded/rejected outcome；指标不含路径、query、\n位置和文本。显式取消、server disable 与 stale task 都会中止 in-flight request。\n\n缓存结论：当前不安装 cache。先收集重复请求与冷暖延迟；只有收益显著且 key/失效测试可证明不会跨\nserver incarnation、configuration generation 或 document revision 时，才进入独立设计评审。\n\n### P5：Semantic graph 与 SCIP（gated）\n\n- 在第一个真实 indexer/resolver 与消费场景同时出现时建立 `zeta-code-graph`。\n- 定义 semantic symbol、occurrence、typed edge、origin、evidence、confidence 和 freshness identity。\n- 第一批只接入 compiler/SCIP、generator source map、Protobuf/OpenAPI/GraphQL 等强证据关系。\n- 同名和路径启发式只产生候选，不产生精确跳转。\n\n仓库审计结论：当前没有 SCIP/compiler occurrence 数据源，也没有能提供 current fact identity 的 graph\nconsumer。因此本阶段不创建空壳 crate，不用 Tree-sitter 同名关系伪造语义图。启动门仍是：真实\nindexer/resolver、真实 consumer、可追溯 freshness 和 failure tests 同时存在。\n\n### P6：跨语言解析器与图感知上下文（gated）\n\n- 按真实项目需求逐个增加 schema/generated-code、FFI、route 或 ORM resolver。\n- 为 traversal 设置 hop、edge-per-node、confidence、文件和内容预算。\n- retrieval 消费 graph candidates 并继续拥有跨来源融合、去重、复核和最终预算。\n- Context evidence 增加 `DefinitionOf`、`CallerOf`、`GeneratedFrom` 等可解释 reason。\n\n仓库审计结论：未发现当前 Protobuf/OpenAPI/GraphQL/source-map 等 concrete resolver consumer。待具体\n项目关系进入产品需求后逐个接入；没有图时现有 symbol/lexical/semantic retrieval 保持完整。\n\n### P7：Syntax-aware editing（第一条只读纵向链路已完成）\n\n- 不暴露原始 Tree-sitter `Node`，先按真实命令增加 revision-bound operation。\n- 已实现 `syntax/selectionRanges`：只按当前最多 1,024 个 selection 请求，每个 selection 最多返回\n  64 个 innermost-first named parser scopes；普通 `syntax/analyze` 不批量携带整棵树节点。\n- Aster Smart Select 已按 snapshot revision 和选区绑定异步结果，支持取消与 stale gate；parser 失败时\n  回退 word/pair/line/document，shrink 使用 Editor-owned history。\n- 后续顺序实现 select declaration、argument/expression/statement selection，之后\n  才实现 delete/move/wrap 等编辑。\n- `zeta-syntax` 返回 range 或 `StructuralEditPlan`；Editor 拥有 undo transaction、edit application、\n  reparse validation 和 formatting。\n- 连续输入热路径不触发结构范围请求；当前 App Server round trip 只发生在离散 Smart Select command。\n  若未来操作进入 typing hot path，必须迁移到 editor-owned worker/in-process parser。\n\n当前完成条件：expand/shrink 在同 revision 应用，stale 结果无副作用。未来 mutation 操作仍必须在同\nrevision 原子执行、进入一个 undo transaction，并在 invalid syntax plan 时无副作用失败。\n\n## 9. 测试、性能与验收门\n\n### Rust 单元与集成测试\n\n- 所有支持语言的 symbol extraction、嵌套声明、Unicode byte range 与 recoverable parse error。\n- SQLite 首次构建、增量 add/change/delete、reopen、schema/query identity reset。\n- exact、prefix、camel-case、subsequence、container/path boost 与 bounded Top K。\n- stale manifest/source revision、transaction publication、取消和资源限制。\n- overlay dirty override、save handoff、close/discard、Workspace replacement。\n- symbol/FTS/vector/remote dedupe、current-source verification 与 byte budget。\n\n新 Rust test module 必须使用 sibling `*_tests.rs` 和显式 `#[path = \"...\"]`，新增 trait 必须有 doc\ncomment，public module 保持 private implementation + named exports。\n\n### App Server 与协议测试\n\n- watcher generation 只对真实 source generation 变化调度一次 reconcile。\n- local-only、LSP-only、combined 和各来源独立失败。\n- initial indexing、last-ready/stale、rebuild failure 与 persistent reopen。\n- Workspace replacement 释放旧 runtime；App Server host teardown 不持久化 overlay。\n- App Server schema、fixtures 与 generated TypeScript 同步。\n\n### Desktop 测试\n\n- query revision、AbortSignal 与 staged provider updates。\n- deterministic fusion、dedupe 和 local/LSP failure fallback。\n- Quick Pick 打开 selection range，并处理接受时内容失配。\n- contribution 只在选择它的产品 bundle 安装；Workbench bootstrap 不包含索引逻辑。\n\n### 性能基线\n\n对 10 万、50 万和 100 万 symbol 记录：\n\n- 首次构建时间与峰值内存；\n- 单文件增量 refresh；\n- persistent reopen/hydrate；\n- 空 query、exact、camel-case 和一般 fuzzy query 的 p50/p95；\n- 快速连续输入时的取消收敛；\n- watcher refresh 对 Renderer 和 App Server request latency 的影响。\n\n在建立基线前不承诺未经测量的绝对毫秒数。交互查询必须满足三个结构性门：不重新扫描 Workspace、\n不完整排序全部候选、不会因最慢 Language Server 阻止本地首批结果。\n\n## 10. 实现落点与修改影响\n\n| 变更 | 主要落点 | 必须同步检查 |\n| --- | --- | --- |\n| 新 symbol kind/language | `zeta-syntax` + `zeta-symbol-index` mapping | extractor identity、fixtures、DTO mapping |\n| symbol identity/range | `zeta-symbol-index` | SQLite schema、dedupe、accept-time validation、AI excerpt |\n| CodeIndex generation/materialization | `zeta-code-index` | semantic、symbol、cloud consumers 与 stale tests |\n| watcher scheduling | App Server refresh worker | semantic/symbol jobs、coalescing、shutdown |\n| Workspace Symbol aggregation | Desktop language service | extension/LSP/local providers、query cancellation、Quick Pick |\n| overlay lifecycle | Editor host + App Server + CodeIndex/SymbolIndex | save、close、host teardown、Workspace replacement |\n| new retrieval source | `zeta-code-retrieval` | RRF、origin/degradation、materialization、Core evidence |\n| semantic edge | future graph owner | source freshness、resolver identity、navigation/AI consumers |\n\n`desktop/src/zeta/code/*/workbench/workbench-code.ts` 只允许导入产品 contribution；\n`workbenchServiceContributions.ts` 只拥有静态安装机制；`workbench.ts` 只组合所有产品共同的服务。\n`extensionHost.contribution.ts` 在 Extension Host 明确支持 workspace-symbol operation 前不参与内置\nsymbol index。`zeta-tools` 保持通用 Tool contract，不拥有 `search_code` 的候选或排名。\n\n## 11. 拒绝的替代方案\n\n| 方案 | 判断 | 原因 |\n| --- | --- | --- |\n| 把 symbol、occurrence、edge、vector 和 LSP cache 全放入现有 CodeIndex SQLite | ❌ | authority、freshness、失败和资源生命周期不同 |\n| Symbol Index 再做一遍 filesystem scan/ignore | ❌ | 复制 Workspace authority，产生隐私和 stale 分叉 |\n| 每次 fuzzy query 调用全部 Language Server 后才展示 | ❌ | 本地低延迟能力无法改善首批结果 |\n| Tree-sitter 同名 reference 作为精确 references | ❌ | 无法可靠解析 import、overload、scope 和类型 |\n| 第一版就设计跨 edit 稳定 SymbolId | ❌ | 没有 compiler/indexer identity，稳定性是虚假承诺 |\n| 每次 dirty buffer 变化都远程 embedding | ❌ | 延迟、成本和源码外发面不可接受 |\n| 先实现持久化 navigation cache | ❌ | LSP 已有内部索引，缺乏稳定 key 和收益证据 |\n| 在 Native 或 Workbench bootstrap 中实现索引 | ❌ | 违反 backend-neutral owner 与产品组合边界 |\n\n## 12. 长期不变量\n\n- Workspace authority 决定哪些源码可见、如何 ignore、如何读取、如何切块以及 revision/chunk identity。\n- Editor authority 决定未保存文本；磁盘 projection 不得覆盖同路径 dirty Buffer。\n- `zeta-syntax` 提供 syntax facts，不声称 compiler/LSP semantic truth。\n- Language Server restart、配置变化和 document revision 必须使旧精确语义结果失效。\n- model provider 只执行 embedding/rerank 调用，不决定 candidates、graph traversal 或最终排序。\n- 云端只能消费 Workspace 已授权并复核的 exact chunks，不能读取整文件后重新切块。\n- 跨来源融合属于 retrieval 或明确的产品 aggregator，不属于任一事实 provider。\n- 任何 exact navigation/AI evidence 都必须能追溯到 source revision 和事实来源。\n- 新 graph/resolver/cache 只有在真实 consumer、failure semantics 和测试门同时存在时才建立。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-当前基线",
        "title": "1. 当前基线"
      },
      {
        "depth": 3,
        "id": "11-与统一-code-intelligence-store-fileshard-方案的差异",
        "title": "1.1 与统一 Code Intelligence Store / FileShard 方案的差异"
      },
      {
        "depth": 2,
        "id": "2-目标架构",
        "title": "2. 目标架构"
      },
      {
        "depth": 2,
        "id": "3-所有权与依赖方向",
        "title": "3. 所有权与依赖方向"
      },
      {
        "depth": 2,
        "id": "4-数据身份与失效",
        "title": "4. 数据、身份与失效"
      },
      {
        "depth": 3,
        "id": "41-当前符号契约",
        "title": "4.1 当前符号契约"
      },
      {
        "depth": 3,
        "id": "42-投影代次",
        "title": "4.2 投影代次"
      },
      {
        "depth": 3,
        "id": "43-不同-revision-不得混用",
        "title": "4.3 不同 revision 不得混用"
      },
      {
        "depth": 2,
        "id": "5-workspace-symbol-用户语义",
        "title": "5. Workspace Symbol 用户语义"
      },
      {
        "depth": 2,
        "id": "6-未保存-buffer-浮层",
        "title": "6. 未保存 Buffer 浮层"
      },
      {
        "depth": 2,
        "id": "7-可靠性安全与隐私",
        "title": "7. 可靠性、安全与隐私"
      },
      {
        "depth": 2,
        "id": "8-阶段计划",
        "title": "8. 阶段计划"
      },
      {
        "depth": 3,
        "id": "p0冻结文档与基线已完成",
        "title": "P0：冻结文档与基线（已完成）"
      },
      {
        "depth": 3,
        "id": "p1本地-symbol-索引纵向链路已完成",
        "title": "P1：本地 Symbol 索引纵向链路（已完成）"
      },
      {
        "depth": 3,
        "id": "p2未保存-buffer-浮层已完成",
        "title": "P2：未保存 Buffer 浮层（已完成）"
      },
      {
        "depth": 3,
        "id": "p3symbol-aware-ai-retrieval已完成",
        "title": "P3：Symbol-aware AI retrieval（已完成）"
      },
      {
        "depth": 3,
        "id": "p4导航指标与可选会话缓存指标已完成缓存-gated",
        "title": "P4：导航指标与可选会话缓存（指标已完成；缓存 gated）"
      },
      {
        "depth": 3,
        "id": "p5semantic-graph-与-scipgated",
        "title": "P5：Semantic graph 与 SCIP（gated）"
      },
      {
        "depth": 3,
        "id": "p6跨语言解析器与图感知上下文gated",
        "title": "P6：跨语言解析器与图感知上下文（gated）"
      },
      {
        "depth": 3,
        "id": "p7syntax-aware-editing第一条只读纵向链路已完成",
        "title": "P7：Syntax-aware editing（第一条只读纵向链路已完成）"
      },
      {
        "depth": 2,
        "id": "9-测试性能与验收门",
        "title": "9. 测试、性能与验收门"
      },
      {
        "depth": 3,
        "id": "rust-单元与集成测试",
        "title": "Rust 单元与集成测试"
      },
      {
        "depth": 3,
        "id": "app-server-与协议测试",
        "title": "App Server 与协议测试"
      },
      {
        "depth": 3,
        "id": "desktop-测试",
        "title": "Desktop 测试"
      },
      {
        "depth": 3,
        "id": "性能基线",
        "title": "性能基线"
      },
      {
        "depth": 2,
        "id": "10-实现落点与修改影响",
        "title": "10. 实现落点与修改影响"
      },
      {
        "depth": 2,
        "id": "11-拒绝的替代方案",
        "title": "11. 拒绝的替代方案"
      },
      {
        "depth": 2,
        "id": "12-长期不变量",
        "title": "12. 长期不变量"
      }
    ],
    "searchText": " 类型：设计。状态：Current implementation + gated roadmap。本文 canonical 拥有编辑器语法事实、 Language Server、工作区符号索引、代码检索与未来代码图之间的跨系统边界，以及已完成纵向链路 和仍需证据才能启动的阶段。 当前检索实现见 code-index.md，编辑器语法能力见 syntax-analysis.md，精确语言语义见 lsp.md。各 crate 的内部接口 继续由对应 README 拥有；本文不复制其实现细节。 快速理解 Zeta 不用一个巨型数据库同时冒充语法树、语言服务器、搜索索引和 AI 上下文。当前系统已经具备 增量语法事实、完整 LSP 请求链路、本地持久化符号索引、未保存 Buffer 覆盖层、本地优先的代码 检索，以及符号感知的精确声明候选。它们共享 Workspace source identity，但分别拥有 freshness、 失败与排序语义。跨语言代码图和 navigation cache 仍未建立：前者缺少 compiler/SCIP/resolver 事实来源，后者要先由已安装的请求指标证明收益。 用户场景 当前行为 目标行为 状态 --- --- --- --- 在工作区按名称找符号 本地索引先返回；Language Server 并发补充；结果分阶段、确定性融合 维持并测量现有行为 Current Language Server 未启动时找符号 支持 grammar 的语言仍可搜索语法声明 扩展语言只需增加可信 syntax facts Current AI 搜索相关代码 本地 symbol + FTS + 可选 vector/remote，融合后由 Workspace 复核正文 有强证据图来源后再增加有界 graph expansion Current + gated 搜索未保存代码 同一路径的磁盘 symbol、lexical、semantic、cloud 候选被 overlay 抑制 维持 save handoff 与 current-text invariant Current 查定义、引用和层级 revision-bound LSP 请求，记录冷暖延迟、结果数、取消与 stale outcome 指标证明重复成本后才评审会话缓存 Current + gated 跨语言导航 没有统一语义图或 resolver 先接入有精确证据的 schema/generated-code 边，再评审启发式边 Potential 结构化编辑 Smart Select 按当前 revision/selection 请求有限 parser scopes，并保留 lexical fallback/shrink history 继续增加 select declaration，之后才评审原子 mutation plan 部分 Current 继续阅读：当前基线、目标架构、数据与失效、 阶段计划、验收门。 1. 当前基线 截至本文建立时，仓库中的真实能力如下。未来阶段不得写成当前事实。 能力 当前 owner 当前状态 --- --- --- 单文档增量 parse、token、fold、document symbol、parse diagnostic zeta-syntax Current 打开文档的文本、selection、undo/redo 与 Editor revision Aster / zeta-editor Current completion、definition、references、hierarchy、workspace symbol 等精确语义 zeta-language-service + zeta-lsp Current Workspace scan、ignore、结构辅助切块、source/chunk identity、SQLite FTS zeta-code-index Current embedding cache、vector recall、optional rerank zeta-code-index-semantic Current lexical/semantic/optional remote 融合、源码复核与 byte budget zeta-code-retrieval Current searchcode 与可选 first-invocation evidence App Server + Core Current 持久化本地 symbol projection、overlay symbol 与 fuzzy matcher zeta-symbol-index Current CodeIndex 未保存 Buffer overlay、dirty suppression 与 save handoff zeta-code-index + App Server Current Workspace Symbol staged aggregation、取消、dedupe 与 accept-time hash verification Desktop Current 语言请求取消与隐私安全指标 zeta-language-service + App Server sink Current references/navigation semantic cache 无 未安装；等待指标门禁 SCIP、occurrence/edge graph 与跨语言 resolver 无 尚未完成 revision-bound structural selection scopes + Smart Select zeta-syntax + Aster Current；mutation 尚未完成 当前 zeta-code-index 的 ChunkReference 仍只表达 root、path、source revision、chunk key、content hash 和范围；zeta-symbol-index 单独保存 name、kind 与声明/选择范围。两者通过 source identity 与 verified excerpt 交汇，而不是共享存储。当前系统仍不保存 occurrence 或 edge，因此“代码索引/RAG”、 “符号索引”和“代码图”是相邻能力，不是一个数据库的不同表名。 1.1 与统一 Code Intelligence Store / FileShard 方案的差异 外部方案对能力分类是对的，但其“一份 FileShard 写入一个 Store，再由所有功能读取”的实现不适合 当前 Zeta。这里采用共享 authority/identity、分离 projection/runtime： 设计点 统一 Store / FileShard 方案 Zeta 当前选择 结论 --- --- --- --- 持久化 files/symbols/occurrences/edges/chunks/cache 共用一个 SQLite lexical、symbol、semantic、cloud control 各自持有可删除 projection failure、schema、retention 与权限不同，不合库 文件事实 parser/LSP/SCIP 合成一个 immutable shard CodeIndexManifest 只发布 Workspace-authorized sources/chunks；LSP 结果仍由 service incarnation 管理 不把不同 freshness 压成一个 revision dirty Buffer overlay 覆盖单一 persistent shard CodeIndex 拥有 canonical text/chunks；SymbolIndex 投影 declarations；retrieval 统一抑制同路径持久候选 共享当前文本 authority，不共享存储 symbol identity structural fingerprint 尝试跨 edit 稳定 SymbolReference 明确绑定 source revision；暂无 stable semantic SymbolId 没有 compiler identity 时不作虚假稳定承诺 occurrence/edge Tree-sitter 先写 unresolved occurrences，后由 LSP/SCIP 增强 当前不持久化 occurrence/edge；只有真实 compiler/SCIP/resolver consumer 后才建 graph syntax-only 同名关系不能成为精确导航 references/navigation cache 作为第一阶段热点能力 先安装 content-free cold/warm/cancel/stale metrics；收益显著后才设计 session cache 避免复制 LSP 内部索引和 stale complexity 结构化编辑 API 暴露 node/parent/sibling/field primitives 与长期 anchor 按真实 command 暴露 revision-bound ranges/plans；第一条为按需 selection scopes 不把 Tree-sitter node model 泄漏给 Editor 坐标转换 全系统共用一个 LineIndex 每个 authority 内部使用 canonical coordinate，协议边界显式转换并验证 Unicode 避免把 Editor/LSP/source revision 生命周期绑成共享 mutable service AI retrieval graph/semantic/context store 统一读取 retrieval 独立融合 symbol/FTS/vector/cloud，并在发给模型前由 CodeIndex 复核 exact excerpt candidate provider 不拥有最终上下文 实现顺序也不同。Zeta 在本计划前已经具备 LSP、SQLite lexical、semantic vector/rerank 和 Agent retrieval，因此没有按“先 FileShard、再 LSP、最后 semantic search”重建已有系统；本轮补的是实际缺口： 本轮落地顺序是 symbol index → dirty overlay → symbol-aware retrieval → metrics/cancellation → on-demand Smart Select。Graph、 resolver 和 cache 继续由证据门控制。 2. 目标架构 ``mermaid flowchart TD Disk \"Workspace 磁盘源码\" -- Index \"zeta-code-index<br/ scan / ignore / revision / chunks\" Index -- Sources \"verified manifest<br/ + materialized sources\" Sources -- Symbols \"zeta-symbol-index<br/ symbol projection + fuzzy search\" Sources -- Semantic \"zeta-code-index-semantic<br/ embedding / vector / rerank\" Buffer \"Editor 未保存 Buffer\" -- Overlay \"App Server<br/ ephemeral document overlay\" Overlay -- Index Overlay -- Symbols Lsp \"zeta-language-service<br/ 精确语言语义\" -- WorkspaceSymbols \"Workspace Symbol aggregator\" Symbols -- WorkspaceSymbols WorkspaceSymbols -- UI \"Ctrl/Cmd+T\" Index -- Retrieval \"zeta-code-retrieval\" Semantic -- Retrieval Symbols -- Retrieval Graph \"Potential zeta-code-graph\" -- Retrieval Retrieval -- Agent \"searchcode / automatic evidence\" ` 目标系统有四类知识平面： - 编辑器语法平面：服务当前打开文档的输入热路径，随 Editor revision 更新。 - 语言语义平面：Language Server 或编译器对 definition、reference、type 和 hierarchy 给出精确事实。 - 工作区检索平面：CodeIndex 对授权源码维护可重建的 lexical/semantic chunk projection。 - 符号与图平面：本地声明索引提供低延迟候选；未来 semantic facts 和 resolver 形成有证据的边。 这些平面可以共享 source identity 和查询编排，但不能共享一个模糊的“最新 revision”。 3. 所有权与依赖方向 组件 拥有 明确不拥有 --- --- --- zeta-syntax grammar/query、增量 tree、revision-bound syntax facts 文件扫描、SQLite、LSP semantic identity zeta-code-index Workspace scan、ignore、读取、source/chunk identity、磁盘与未来 overlay chunk authority symbol graph、模型选择、最终 AI 排名 zeta-symbol-index verified source 与 dirty overlay 的声明 projection、持久化复用、本地 exact/fuzzy symbol search 自主扫描文件、LSP request、跨语言猜测 zeta-language-service server route/incarnation、document freshness、精确 LSP 请求 本地 symbol database、AI retrieval zeta-code-index-semantic 模型输入、embedding persistence、vector recall、rerank 与来源内排序 scan、chunk、跨来源融合 zeta-code-retrieval 多来源候选融合、identity dedupe、current-source verification 与内容预算 模型 transport、Workspace grant、Editor state Potential zeta-code-graph semantic symbol、occurrence、typed edge、evidence/confidence filesystem authority、UI、模型调用 App Server trust、watcher、projection 调度、ephemeral overlay、RPC、metrics sink 与 fallback composition parser、fuzzy 算法、Renderer 展示 Desktop frontend service、provider 聚合、取消、渐进结果和导航展示 索引存储、semantic cache authority 固定依赖方向： `text zeta-symbol-index → zeta-code-index + zeta-syntax zeta-code-retrieval → zeta-code-index + optional semantic/symbol/graph sources App Server → 上述 backend-neutral crates Desktop → generated protocol + frontend-owned service contracts ` 禁止 zeta-symbol-index 直接遍历 Workspace。它必须比较 CodeIndexManifest.sources 与自身 projection， 并通过 CodeIndex::materializesources 只读取经过当前 source revision 复核的文件。这样 scan、ignore、 权限、symlink 和资源限制不会出现第二套实现。 4. 数据、身份与失效 4.1 当前符号契约 当前 public contract 把每个语法声明绑定到一个精确 Workspace source revision： `rust pub struct SymbolReference pub rootid: IndexRootId, pub relativepath: PathBuf, pub sourcerevision: SourceRevision, pub language: IndexedLanguage, pub sourcebytes: usize, pub ordinal: usize, pub declarationrange: SymbolRange, pub selectionrange: SymbolRange, pub struct IndexedSymbol pub reference: SymbolReference, pub name: String, pub kind: SymbolKind, pub containername: Option<String , pub struct SymbolSearchHit pub symbol: IndexedSymbol, pub score: u32, pub matchedindices: Vec<u32 , ` 第一版不定义跨 revision 稳定的 SymbolId。仅凭路径、位置、名称或 structural fingerprint 无法可靠 表达 rename/move 后仍是同一语义对象。当前引用只保证绑定到一个精确 source revision；真正稳定的 semantic key 必须等 SCIP/compiler/LSP facts 出现真实消费者后再定义。 4.2 投影代次 符号 projection 保存自己的 generation，并记录对应的 CodeIndex source generation： `text SymbolIndexSnapshot generation sourcegeneration sourcecount symbolcount limit flags ` 一次 reconcile： 1. 读取当前 CodeIndexManifest。 2. 按 relativepath + sourcerevision + language 复用未变化文件的 symbol rows。 3. 批量 materialize 新增或变化 sources。 4. 用 zeta-syntax 提取有界 document symbols。 5. 删除 manifest 已不存在的文件。 6. 在一个 SQLite transaction 中发布新 generation。 7. 由新 generation 构造只读内存 fuzzy candidate snapshot。 zeta-syntax 必须提供能使持久化消费者失效的 syntax-facts identity。grammar 或 tags query 改变但 identity 不变会静默复用不兼容 projection，属于 correctness bug。 4.3 不同 revision 不得混用 事实 freshness identity --- --- Editor syntax document identity + Editor revision 磁盘 symbol/chunk root + path + source revision + projection generation LSP result service generation + server incarnation + Editor revision semantic vector source/chunk identity + chunker identity + model identity remote candidate durable grant + published chunk identity + provider generation future graph edge source fact identities + resolver/indexer identity 静态优先级如“SCIP LSP Tree-sitter”不是正确合并规则。来源、事实种类和 freshness 必须一起判断； 例如 Language Server 对未保存 Buffer 的结果通常比磁盘 SCIP index 更新。 5. Workspace Symbol 用户语义 本地 symbol provider 与 Language Server provider 是互补关系： 来源 优势 限制 UI 语义 --- --- --- --- 本地 syntax symbol index 启动快、无需 server、跨已支持 grammar 统一搜索 只有声明与语法 container，没有类型解析 可直接打开声明位置，不宣称 semantic reference Language Server workspace/symbol 语言/项目语义精确，可包含 server 独有符号 受启动、索引、配置和语言覆盖影响 异步补充或替换相同 location Future extension provider 扩展语言或专用来源 受 extension trust/lifecycle 约束 通过统一 provider contract 合并 一次 Ctrl/Cmd+T 查询应执行： 1. query revision 增加并取消上一轮请求。 2. 同时请求本地 symbol index 和全部适用 provider。 3. 任一来源返回后发布阶段性结果，不等待最慢 provider 才首次展示。 4. 按来源内 rank 做 deterministic fusion，并对 exact/prefix/camel/subsequence match 加权。 5. 按 resource + name + selection start 去重。 6. 只接受当前 query revision 的更新。 7. 接受结果时由当前文件内容验证 selection text；失配则触发 document-symbol reanchor 或刷新，而不盲跳旧 range。 本地 fuzzy matcher 第一版直接使用已有 workspace dependency nucleo，但不把文件搜索 crate 扩成 泛化 symbol owner。共享抽象必须等第二个相同领域 consumer 出现后再提取。 6. 未保存 Buffer 浮层 当前 Editor 与 LSP 已有打开文档 revision，App Server 同时持有 Workspace-scoped 临时 projection： `text WorkspaceTrustId + relative path + editor revision + language + content hash + full text ` 规则： - Editor 仍是未保存文本 authority；App Server 只持有最新 immutable snapshot。 - 同一 open/change/close 事实由 host 分别投影给 LSP 和 code-intelligence overlay，两者不互相依赖。 - overlay 不写入 SQLite，不随进程恢复，不自动外发。 - symbol query 中 overlay 完全抑制同路径磁盘 symbols。 - code retrieval 中 dirty path 的磁盘 lexical、semantic 和 cloud candidates 全部被抑制。 - 第一版 dirty buffer 只建立内存 lexical/symbol projection；不在每次按键后发送 embedding。 - 保存后保留 overlay，直到磁盘 CodeIndex 出现相同 content hash，再无缝交回持久化 generation。 - close/discard、Workspace replacement 或 App Server host teardown 时清理对应 overlay。 AI consumer 读取正文时必须先查询 overlay，缺失时才读取磁盘。不能先取磁盘 candidate，再把其旧 range 用到新 Buffer 上。 7. 可靠性、安全与隐私 条件 必须发生的结果 --- --- 本地 symbol index 未 ready/失败 LSP workspace symbol 保持可用；状态不伪装 ready Language Server 未安装或 crash 本地 syntax symbol search 保持可用 watcher event 合并或 overflow CodeIndex 先发布 canonical generation，再调度下游 reconcile symbol projection schema/query identity 变化 丢弃可重建 projection，不静默复用 query 被取消 worker 在可用 checkpoint 停止；旧 query revision 不发布 UI unsupported language CodeIndex 仍可按 plaintext chunk；symbol projection 跳过该文件 syntax 含 recoverable error 返回有界可证明的 partial declarations，不提升为 semantic fact Workspace restricted 本地只读 projection 可用；不启动 executable，不增加网络外发 remote model configured 沿用 CodeIndex 明示 consent；symbol index 本身不获得网络能力 本地 symbol database 与 lexical/semantic CodeIndex 一样只是可重建 projection，不提升为源码 authority。 Unix persistent file 使用普通文件和 0600。路径、错误和日志不得输出源码正文或 embedding payload。 8. 阶段计划 P0：冻结文档与基线（已完成） - 建立本文并加入文档导航与文档站。 - 从 code-index.md、syntax-analysis.md 和 lsp.md 链回本文。 - 固定当前状态、owner、non-goal、测试和性能验收口径。 完成状态：已建立 canonical 边界、文档导航和 owner/non-goal；产品 Workbench 只安装 contribution。 P1：本地 Symbol 索引纵向链路（已完成） - 新增 zeta-symbol-index crate、README、Cargo/Bazel target。 - 定义 revision-bound symbol types、limits、error 和 storage。 - 实现 manifest reconcile、syntax extraction、SQLite publication、persis"
  },
  {
    "slug": "code-index",
    "sourcePath": "docs/code-index.md",
    "title": "工作区代码索引",
    "description": "Zeta 采用 local-first：本地扫描与切块、本地 SQLite/FTS、本地向量存储、本地 recall/fusion/final ordering。zeta-model-provider 只接入 embedding/rerank 模型；它不决定 chunks、候选、排序或存储。",
    "group": "界面与体验",
    "markdown": "> 状态：Current。本文 canonical 拥有 CodeIndex 的跨 crate 架构、数据位置、隐私边界与实施状态。\n> 具体实现见 [`zeta-code-index`](../zeta-rs/code-index/README.md)、\n> [`zeta-code-index-semantic`](../zeta-rs/code-index-semantic/README.md)、\n> [`zeta-code-retrieval`](../zeta-rs/code-retrieval/README.md)；可选远端 provider 控制面见\n> [`zeta-code-index-cloud`](../zeta-rs/code-index-cloud/README.md)。编辑器语法、Language Server、本地符号\n> 索引与未来代码图的跨系统边界和开发顺序由 [`code-intelligence.md`](code-intelligence.md) canonical\n> 维护。\n\n## 快速理解\n\nZeta 采用 local-first：本地扫描与切块、本地 SQLite/FTS、本地向量存储、本地 recall/fusion/final\nordering。`zeta-model-provider` 只接入 embedding/rerank 模型；它不决定 chunks、候选、排序或存储。\n\n| 环节 | 执行位置 | 当前状态 |\n| --- | --- | --- |\n| scan、ignore、chunk、revision identity | Workspace 本地 | ✅ |\n| lexical SQLite/FTS | 本地 | ✅ |\n| syntax symbol projection + fuzzy search | 本地 `zeta-symbol-index` | ✅；持久磁盘 projection + ephemeral dirty overlay |\n| embedding/rerank API 调用 | model adapter；模型可本地或远程 | ✅ OpenAI-compatible embedding/rerank；OpenAI/Ollama embedding |\n| embedding persistence、vector recall、rerank 分数解释与来源内排序 | 本地 `zeta-code-index-semantic` | ✅；自适应 exact/SimHash ANN |\n| symbol/lexical/semantic/optional remote fusion、复核与预算 | 本地 `zeta-code-retrieval` | ✅ |\n| 远端托管 code-index | 独立 provider | 可选 contract ✅；concrete provider 尚未完成 |\n| Agent 检索消费 | App Server + Core consumer | ✅ `search_code`；可选 first-invocation 自动 evidence |\n\n因此，Zeta 当前不是“把源码上传给自建服务器再让服务器完成整个 RAG”。默认 semantic selection 为\ndisabled，只运行 local symbol + lexical。用户配置 exact provider/model 并授权当前 Workspace 后，Zeta 才把模型当作\n计算 API 调用；向量数据库和检索编排仍在本地。若 endpoint 位于远端，相应 chunk/query/candidate 文本\n会外发给模型。\n\n## 为什么这样分\n\n| 方案 | 优点 | 代价 | Zeta 选择 |\n| --- | --- | --- | --- |\n| 全本地模型 + 本地检索 | 最强隐私、离线 | 模型质量和硬件受限 | ✅ Ollama embedding；rerank 需兼容 endpoint |\n| 远程模型 + 本地检索 | 模型效果好；索引、召回与排序规则仍可控 | embedding/rerank 文本会外发 | ✅ 主要目标形态，需 consent |\n| 托管远端 code-index | 多设备复用、服务端可上 ANN | 需上传 chunks、租户与删除控制面 | 可选 provider，不是默认 |\n| 整文件上传后云端重新切块 | 服务端自主 | 外发面大，复制 Workspace authority，stale 难验证 | ❌ |\n| PostgreSQL 作为本地统一数据库 | 与服务器技术栈统一 | 要求 daemon/账号/端口，桌面生命周期复杂 | ❌；本地用 SQLite |\n\nSQLite 是嵌入式本地 projection；PostgreSQL/pgvector 只可能属于未来独立远端服务仓库。Zeta 仓库保留\ntyped provider port、consent 和 exact chunk identity，不拥有某个云部署的数据库、migration 或运维。\n\n## 本地端到端路径\n\n```mermaid\nflowchart TD\n    Root[\"Trusted WorkspaceRoot\"] --> Watch[\"filesystem watcher\"]\n    Watch --> Scan[\"bounded ignore-aware scan\"]\n    Scan --> Chunk[\"local structural/line chunking\"]\n    Chunk --> Lexical[\"SQLite generation + FTS5\"]\n    Chunk --> Symbols[\"SQLite symbol generation + Nucleo\"]\n    Buffer[\"Editor dirty snapshot\"] --> Overlay[\"canonical in-memory overlay\"]\n    Overlay --> Lexical\n    Overlay --> Symbols\n    Lexical --> Verify[\"materialize exact current chunks\"]\n    Verify --> Reuse[\"reuse vectors by root/model/path/language/chunk key\"]\n    Reuse --> Embed[\"EmbeddingInvoker for missing chunks\"]\n    Embed --> Dense[\"local SQLite embeddings\"]\n    Query[\"workspace/codeIndex/retrieve\"] --> FTS[\"local lexical recall\"]\n    Query --> SymbolQuery[\"local symbol recall\"]\n    Query --> QueryEmbed[\"query embedding\"]\n    QueryEmbed --> Vector[\"local cosine recall\"]\n    Vector --> Rerank[\"optional RerankInvoker\"]\n    Rerank --> Fuse[\"local RRF + identity dedupe\"]\n    FTS --> Fuse\n    SymbolQuery --> SymbolExcerpt[\"verified declaration excerpt\"]\n    SymbolExcerpt --> Fuse\n    Fuse --> Current[\"reread + verify current source\"]\n    Current --> Budget[\"item/total byte budget\"]\n```\n\nApp Server 先注册 watcher，再把 scan/reconcile 投递给独立 refresh worker。lexical generation 成功更新后，\nworker 同步 symbol 与 semantic projection。新 generation 采用 transaction 发布；同步完成前查询不会\n把旧 semantic generation 冒充当前结果，而是明确降级为 symbol/lexical。Editor dirty snapshot 由同一\nCodeIndex overlay 作为 current text authority；同路径磁盘 symbol/chunk 及 semantic/cloud candidate\n全部被抑制，直到磁盘 content hash 对齐后 handoff。\n\n重建并不等于重新调用所有 embedding。semantic store 使用 root、model ID、relative path、language 与\n稳定 chunk key 匹配可复用向量；只计算新增或变化的 chunks，再原子替换当前 generation。模型 ID 变化会\n使全部旧向量失效。\n\n## 职责边界\n\n| 能力 | Owner | 判断 |\n| --- | --- | --- |\n| filesystem scan、ignore、chunk、source revision 与 lexical FTS | `zeta-code-index` | ✅ 本地 authority |\n| verified declaration projection 与 fuzzy symbol ranking | `zeta-symbol-index` | ✅；不自主扫描 Workspace |\n| embedding/rerank 请求与响应 transport | `zeta-model-provider` | ✅ adapter contract；不排序 |\n| 模型输入准备、embedding cache、vector recall、rerank 时机/分数解释/排序 | `zeta-code-index-semantic` | ✅ 本地 orchestration |\n| 多来源 RRF、dedupe、current-source verification、content budget | `zeta-code-retrieval` | ✅ |\n| root trust、watcher、model/provider injection、profile paths、RPC | App Server | ✅ |\n| remote publication grant/query/delete lifecycle | `zeta-code-index-cloud` | 可选 |\n| remote vector DB、tenant auth、endpoint 与 retention | 独立服务/provider 项目 | ❌ 不属于 Zeta |\n| settings endpoint/model 与 Workspace consent/revoke | 产品 UI + App Server policy | ✅；含本地 progress/cancel/retry；远端 retention audit 仍属 cloud provider |\n\n关键不变量：model provider 只“调用模型并返回有序向量/分数”。把文本变成哪些 model documents、向量\n如何持久化、候选如何召回、rerank 分数如何解释以及最终如何排序，都由 code-index domain owner 决定。\n\n## 数据与 consent\n\n| 模式 | 可能离开设备的数据 | 必须满足 |\n| --- | --- | --- |\n| lexical only | 无 | 默认可用 |\n| local model semantic | 无 | host 安装本地 invoker |\n| remote embedding | exact chunk 文本、query 文本 | 明示源码外发 consent、destination/model、scope、删除/retention 说明 |\n| remote rerank | query + recalled candidate 文本 | 同上；UI 需单独说明 rerank 外发 |\n| remote code-index provider | grant scope 内的 verified chunks | durable `CloudCodeIndexGrant`、byte ceiling、幂等 grant deletion |\n\nDesktop Settings 可以保存 Ollama 或 unauthenticated OpenAI-compatible endpoint、选择 embedding/optional\nrerank 模型，并对 active Workspace 单独授权或撤销。授权精确绑定 Workspace、模型和对应 provider\n配置；模型或 base URL 变化后旧授权立即失效，语义 runtime 被卸载。普通聊天模型配置不会自动授权\n后台索引源码。当前 UI 尚未提供 chunk/byte 预览、同步进度或远端 retention 证明。\n\n远端托管 code-index 使用另一套 durable grant：`provider/tenant/collection`、path selection、最大 source\nbytes 与稳定 grant ID。`authorize` 只持久化 permission；`sync` 才 materialize 并发送。`revoke` 先保存\n`Revoking`，provider 完成 grant 级幂等删除后才清除本地 pending state。该 provider 的实际数据库与\nendpoint 不在本仓库。\n\n## App Server 与持久化\n\ncanonical 本地协议：\n\n- `workspace/codeIndex/status {}`；\n- `workspace/codeIndex/search { query, maxResults }`：纯 lexical 诊断；\n- `workspace/codeIndex/retrieve { query, maxResults }`：symbol + lexical + 可用 semantic/remote sources；\n- `workspace/codeIndex/rebuild {}`；\n- `workspace/symbolIndex/status {}` 与 `workspace/symbolIndex/search { query, maxResults }`；\n- `workspace/codeIntelligence/document/synchronize|close`：同步/释放 ephemeral dirty overlay。\n\n`retrieve` origin 明确区分 `localSymbol`、`localLexical`、`localSemantic` 和 `cloudSemantic`。symbol、\nlocal semantic 或 remote source 失败不会吞掉 FTS，而会返回独立的\n`localSymbolQueryFailed`、`localSemanticQueryFailed` 或 `cloudQueryFailed` degradation。\n\n| projection | 默认路径 | 内容 |\n| --- | --- | --- |\n| lexical | `<profile>/code-index/<root-digest>.sqlite3` | chunks、generation、FTS |\n| symbols | `<profile>/symbol-index/<root-digest>.sqlite3` | source revisions、syntax declarations、generation；dirty overlay 不持久化 |\n| local semantic | `<profile>/code-index-semantic/<root-digest>.sqlite3` | chunks、model ID、embeddings |\n| remote control state | `<profile>/code-index-cloud/<root-digest>.sqlite3` | grant/phase/generation metadata；不保存源码/secret |\n\nUnix persistent files 固定为普通文件和 `0600`。lexical/semantic 都是可重建 projection，不提升为源码\nauthority。restricted Workspace 可继续 lexical；当前 semantic invoker 和 remote controller 只在 Trusted\nWorkspace 安装。\n\n## 一致性与安全\n\n- SQLite publication 使用 transaction；读者只看到完整 generation。\n- symbol、lexical、semantic 和 remote candidates 都只是 references；返回前必须从 Workspace 重读并验证\n  root、revision、chunk key、range、line span 与 content hash。\n- dirty overlay 是 Editor-authorized current source projection；同 revision 不同内容会被拒绝，close、\n  Workspace replacement 或 content-hash handoff 会清理它，且它不会触发每次按键 remote embedding。\n- full scan 默认排除 hidden files，读取 Git ignore，并硬排除 `.git`、`.zeta`、`node_modules`、`target`。\n- 文件读取权限不等于网络 egress consent；默认 provider/model registry 为空。\n- OpenAI API-key semantic 调用要求 host 注入 `SecretStore`；Desktop 尚无持久化 API-key 管理 UI，因此\n  当前产品闭环面向 Ollama 或 unauthenticated OpenAI-compatible endpoint。\n- semantic model 或 chunker 语义变化必须换 model/chunker identity，使不兼容 cache 显式失效。\n\n## 当前状态与下一步\n\n| 阶段 | 状态 | 内容 |\n| --- | --- | --- |\n| 本地 lexical | ✅ Current | scan、chunk、stable identity、SQLite generation、FTS5、watcher |\n| 本地 semantic domain | ✅ Current | SQLite vectors、增量复用、embedding batches、cosine recall、optional rerank |\n| local symbol index | ✅ Current | syntax extraction、SQLite reuse、Nucleo fuzzy、App Server RPC、Desktop staged provider |\n| retrieval RPC | ✅ Current | 四来源 origin、RRF、dedupe、fallback、verification、byte budget |\n| App Server composition | ✅ Current | durable selection/consent、trusted-only runtime、config rebind、background sync |\n| concrete embedding/rerank adapter | 部分具备 | OpenAI-compatible embedding/rerank、OpenAI/Ollama embedding；调用支持 cancellation/retry；持久化 credential UI 仍待补 |\n| semantic consent UI | 部分具备 | endpoint/model disclosure、exact Workspace scope、authorize/revoke、progress/cancel/retry；远端 retention audit 仍待 cloud provider |\n| scalable local ANN | ✅ Current | ≥2,048 chunks 使用 rebuildable SimHash candidates + exact cosine；异常自动 brute-force |\n| optional hosted provider | 尚未完成 | concrete endpoint、tenant auth、server DB、retention/deletion receipt |\n| Agent consumer | ✅ Current | 显式只读 `search_code`；可选 first-invocation 自动 evidence，默认关闭、预算受限且标记 untrusted |\n| Editor unsaved overlay | ✅ Current | canonical in-memory lexical/symbol projection、dirty suppression、save handoff |\n\n接下来的优先级是 secure credential 产品接入、ANN 召回/延迟实测与远端 retention audit。远端托管 provider\n与 PostgreSQL/pgvector 不应阻塞本地主路径。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "为什么这样分",
        "title": "为什么这样分"
      },
      {
        "depth": 2,
        "id": "本地端到端路径",
        "title": "本地端到端路径"
      },
      {
        "depth": 2,
        "id": "职责边界",
        "title": "职责边界"
      },
      {
        "depth": 2,
        "id": "数据与-consent",
        "title": "数据与 consent"
      },
      {
        "depth": 2,
        "id": "app-server-与持久化",
        "title": "App Server 与持久化"
      },
      {
        "depth": 2,
        "id": "一致性与安全",
        "title": "一致性与安全"
      },
      {
        "depth": 2,
        "id": "当前状态与下一步",
        "title": "当前状态与下一步"
      }
    ],
    "searchText": " 状态：Current。本文 canonical 拥有 CodeIndex 的跨 crate 架构、数据位置、隐私边界与实施状态。 具体实现见 zeta-code-index、 zeta-code-index-semantic、 zeta-code-retrieval；可选远端 provider 控制面见 zeta-code-index-cloud。编辑器语法、Language Server、本地符号 索引与未来代码图的跨系统边界和开发顺序由 code-intelligence.md canonical 维护。 快速理解 Zeta 采用 local-first：本地扫描与切块、本地 SQLite/FTS、本地向量存储、本地 recall/fusion/final ordering。zeta-model-provider 只接入 embedding/rerank 模型；它不决定 chunks、候选、排序或存储。 环节 执行位置 当前状态 --- --- --- scan、ignore、chunk、revision identity Workspace 本地 ✅ lexical SQLite/FTS 本地 ✅ syntax symbol projection + fuzzy search 本地 zeta-symbol-index ✅；持久磁盘 projection + ephemeral dirty overlay embedding/rerank API 调用 model adapter；模型可本地或远程 ✅ OpenAI-compatible embedding/rerank；OpenAI/Ollama embedding embedding persistence、vector recall、rerank 分数解释与来源内排序 本地 zeta-code-index-semantic ✅；自适应 exact/SimHash ANN symbol/lexical/semantic/optional remote fusion、复核与预算 本地 zeta-code-retrieval ✅ 远端托管 code-index 独立 provider 可选 contract ✅；concrete provider 尚未完成 Agent 检索消费 App Server + Core consumer ✅ searchcode；可选 first-invocation 自动 evidence 因此，Zeta 当前不是“把源码上传给自建服务器再让服务器完成整个 RAG”。默认 semantic selection 为 disabled，只运行 local symbol + lexical。用户配置 exact provider/model 并授权当前 Workspace 后，Zeta 才把模型当作 计算 API 调用；向量数据库和检索编排仍在本地。若 endpoint 位于远端，相应 chunk/query/candidate 文本 会外发给模型。 为什么这样分 方案 优点 代价 Zeta 选择 --- --- --- --- 全本地模型 + 本地检索 最强隐私、离线 模型质量和硬件受限 ✅ Ollama embedding；rerank 需兼容 endpoint 远程模型 + 本地检索 模型效果好；索引、召回与排序规则仍可控 embedding/rerank 文本会外发 ✅ 主要目标形态，需 consent 托管远端 code-index 多设备复用、服务端可上 ANN 需上传 chunks、租户与删除控制面 可选 provider，不是默认 整文件上传后云端重新切块 服务端自主 外发面大，复制 Workspace authority，stale 难验证 ❌ PostgreSQL 作为本地统一数据库 与服务器技术栈统一 要求 daemon/账号/端口，桌面生命周期复杂 ❌；本地用 SQLite SQLite 是嵌入式本地 projection；PostgreSQL/pgvector 只可能属于未来独立远端服务仓库。Zeta 仓库保留 typed provider port、consent 和 exact chunk identity，不拥有某个云部署的数据库、migration 或运维。 本地端到端路径 ``mermaid flowchart TD Root \"Trusted WorkspaceRoot\" -- Watch \"filesystem watcher\" Watch -- Scan \"bounded ignore-aware scan\" Scan -- Chunk \"local structural/line chunking\" Chunk -- Lexical \"SQLite generation + FTS5\" Chunk -- Symbols \"SQLite symbol generation + Nucleo\" Buffer \"Editor dirty snapshot\" -- Overlay \"canonical in-memory overlay\" Overlay -- Lexical Overlay -- Symbols Lexical -- Verify \"materialize exact current chunks\" Verify -- Reuse \"reuse vectors by root/model/path/language/chunk key\" Reuse -- Embed \"EmbeddingInvoker for missing chunks\" Embed -- Dense \"local SQLite embeddings\" Query \"workspace/codeIndex/retrieve\" -- FTS \"local lexical recall\" Query -- SymbolQuery \"local symbol recall\" Query -- QueryEmbed \"query embedding\" QueryEmbed -- Vector \"local cosine recall\" Vector -- Rerank \"optional RerankInvoker\" Rerank -- Fuse \"local RRF + identity dedupe\" FTS -- Fuse SymbolQuery -- SymbolExcerpt \"verified declaration excerpt\" SymbolExcerpt -- Fuse Fuse -- Current \"reread + verify current source\" Current -- Budget \"item/total byte budget\" ` App Server 先注册 watcher，再把 scan/reconcile 投递给独立 refresh worker。lexical generation 成功更新后， worker 同步 symbol 与 semantic projection。新 generation 采用 transaction 发布；同步完成前查询不会 把旧 semantic generation 冒充当前结果，而是明确降级为 symbol/lexical。Editor dirty snapshot 由同一 CodeIndex overlay 作为 current text authority；同路径磁盘 symbol/chunk 及 semantic/cloud candidate 全部被抑制，直到磁盘 content hash 对齐后 handoff。 重建并不等于重新调用所有 embedding。semantic store 使用 root、model ID、relative path、language 与 稳定 chunk key 匹配可复用向量；只计算新增或变化的 chunks，再原子替换当前 generation。模型 ID 变化会 使全部旧向量失效。 职责边界 能力 Owner 判断 --- --- --- filesystem scan、ignore、chunk、source revision 与 lexical FTS zeta-code-index ✅ 本地 authority verified declaration projection 与 fuzzy symbol ranking zeta-symbol-index ✅；不自主扫描 Workspace embedding/rerank 请求与响应 transport zeta-model-provider ✅ adapter contract；不排序 模型输入准备、embedding cache、vector recall、rerank 时机/分数解释/排序 zeta-code-index-semantic ✅ 本地 orchestration 多来源 RRF、dedupe、current-source verification、content budget zeta-code-retrieval ✅ root trust、watcher、model/provider injection、profile paths、RPC App Server ✅ remote publication grant/query/delete lifecycle zeta-code-index-cloud 可选 remote vector DB、tenant auth、endpoint 与 retention 独立服务/provider 项目 ❌ 不属于 Zeta settings endpoint/model 与 Workspace consent/revoke 产品 UI + App Server policy ✅；含本地 progress/cancel/retry；远端 retention audit 仍属 cloud provider 关键不变量：model provider 只“调用模型并返回有序向量/分数”。把文本变成哪些 model documents、向量 如何持久化、候选如何召回、rerank 分数如何解释以及最终如何排序，都由 code-index domain owner 决定。 数据与 consent 模式 可能离开设备的数据 必须满足 --- --- --- lexical only 无 默认可用 local model semantic 无 host 安装本地 invoker remote embedding exact chunk 文本、query 文本 明示源码外发 consent、destination/model、scope、删除/retention 说明 remote rerank query + recalled candidate 文本 同上；UI 需单独说明 rerank 外发 remote code-index provider grant scope 内的 verified chunks durable CloudCodeIndexGrant、byte ceiling、幂等 grant deletion Desktop Settings 可以保存 Ollama 或 unauthenticated OpenAI-compatible endpoint、选择 embedding/optional rerank 模型，并对 active Workspace 单独授权或撤销。授权精确绑定 Workspace、模型和对应 provider 配置；模型或 base URL 变化后旧授权立即失效，语义 runtime 被卸载。普通聊天模型配置不会自动授权 后台索引源码。当前 UI 尚未提供 chunk/byte 预览、同步进度或远端 retention 证明。 远端托管 code-index 使用另一套 durable grant：provider/tenant/collection、path selection、最大 source bytes 与稳定 grant ID。authorize 只持久化 permission；sync 才 materialize 并发送。revoke 先保存 Revoking，provider 完成 grant 级幂等删除后才清除本地 pending state。该 provider 的实际数据库与 endpoint 不在本仓库。 App Server 与持久化 canonical 本地协议： - workspace/codeIndex/status ； - workspace/codeIndex/search query, maxResults ：纯 lexical 诊断； - workspace/codeIndex/retrieve query, maxResults ：symbol + lexical + 可用 semantic/remote sources； - workspace/codeIndex/rebuild ； - workspace/symbolIndex/status 与 workspace/symbolIndex/search query, maxResults ； - workspace/codeIntelligence/document/synchronize close：同步/释放 ephemeral dirty overlay。 retrieve origin 明确区分 localSymbol、localLexical、localSemantic 和 cloudSemantic。symbol、 local semantic 或 remote source 失败不会吞掉 FTS，而会返回独立的 localSymbolQueryFailed、localSemanticQueryFailed 或 cloudQueryFailed degradation。 projection 默认路径 内容 --- --- --- lexical <profile /code-index/<root-digest .sqlite3 chunks、generation、FTS symbols <profile /symbol-index/<root-digest .sqlite3 source revisions、syntax declarations、generation；dirty overlay 不持久化 local semantic <profile /code-index-semantic/<root-digest .sqlite3 chunks、model ID、embeddings remote control state <profile /code-index-cloud/<root-digest .sqlite3 grant/phase/generation metadata；不保存源码/secret Unix persistent files 固定为普通文件和 0600。lexical/semantic 都是可重建 projection，不提升为源码 authority。restricted Workspace 可继续 lexical；当前 semantic invoker 和 remote controller 只在 Trusted Workspace 安装。 一致性与安全 - SQLite publication 使用 transaction；读者只看到完整 generation。 - symbol、lexical、semantic 和 remote candidates 都只是 references；返回前必须从 Workspace 重读并验证 root、revision、chunk key、range、line span 与 content hash。 - dirty overlay 是 Editor-authorized current source projection；同 revision 不同内容会被拒绝，close、 Workspace replacement 或 content-hash handoff 会清理它，且它不会触发每次按键 remote embedding。 - full scan 默认排除 hidden files，读取 Git ignore，并硬排除 .git、.zeta、nodemodules、target。 - 文件读取权限不等于网络 egress consent；默认 provider/model registry 为空。 - OpenAI API-key semantic 调用要求 host 注入 SecretStore；Desktop 尚无持久化 API-key 管理 UI，因此 当前产品闭环面向 Ollama 或 unauthenticated OpenAI-compatible endpoint。 - semantic model 或 chunker 语义变化必须换 model/chunker identity，使不兼容 cache 显式失效。 当前状态与下一步 阶段 状态 内容 --- --- --- 本地 lexical ✅ Current scan、chunk、stable identity、SQLite generation、FTS5、watcher 本地 semantic domain ✅ Current SQLite vectors、增量复用、embedding batches、cosine recall、optional rerank local symbol index ✅ Current syntax extraction、SQLite reuse、Nucleo fuzzy、App Server RPC、Desktop staged provider retrieval RPC ✅ Current 四来源 origin、RRF、dedupe、fallback、verification、byte budget App Server composition ✅ Current durable selection/consent、trusted-only runtime、config rebind、background sync concrete embedding/rerank adapter 部分具备 OpenAI-compatible embedding/rerank、OpenAI/Ollama embedding；调用支持 cancellation/retry；持久化 credential UI 仍待补 semantic consent UI 部分具备 endpoint/model disclosure、exact Workspace scope、authorize/revoke、progress/cancel/retry；远端 retention audit 仍待 cloud provider scalable local ANN ✅ Current ≥2,048 chunks 使用 rebuildable SimHash candidates + exact cosine；异常自动 brute-force optional hosted provider 尚未完成 concrete endpoint、tenant auth、server DB、retention/deletion receipt Agent consumer ✅ Current 显式只读 searchcode`；可选 first-invocation 自动 evidence，默认关闭、预算受限且标记 untrusted Editor unsaved overlay ✅ Current canonical in-memory lexical/symbol projection、dirty suppression、save handoff 接下来的优先级是 secure credential 产品接入、ANN 召回/延迟实测与远端 retention audit。远端托管 provider 与 PostgreSQL/pgvector 不应阻塞本地主路径。"
  },
  {
    "slug": "syntax-analysis",
    "sourcePath": "docs/syntax-analysis.md",
    "title": "编辑器语法能力",
    "description": "语法高亮、折叠、outline 和 parse diagnostic 都是编辑器能力。产品层对外组合 CodeEditor 或 DiffEditor，只提供文档、语言、主题和平台输入；它不获得独立 syntax service， 也不通过 App Server 同步编辑器内部 revision。",
    "group": "界面与体验",
    "markdown": "> 状态：Rust `CodeEditor` 已内部接入 Rust、JSON、JSONC 与 Shell 的增量 tree-sitter\n> 分析；Rust `DiffEditor` 已通过 retained `DiffEditorDocument` 内部维护两侧语法状态；Desktop\n> Aster Text Engine 通过 TextMate 与有界 Rust syntax facts 提供语法能力。本文拥有跨编辑器的语法能力边界；底层解析契约见\n> [`zeta-syntax` README](../zeta-rs/syntax/README.md)，符号索引、Language Server、代码检索与未来代码图\n> 的跨系统演进见 [`code-intelligence.md`](code-intelligence.md)，Native 编辑器 API 见\n> [`zeta-editor` README](../zeterm/editor/README.md)。\n\n## 快速理解\n\n语法高亮、折叠、outline 和 parse diagnostic 都是编辑器能力。产品层对外组合\n`CodeEditor` 或 `DiffEditor`，只提供文档、语言、主题和平台输入；它不获得独立 syntax service，\n也不通过 App Server 同步编辑器内部 revision。\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| Rust/JSON/JSONC/Shell grammar、query 与增量 tree | `zeta-syntax`，由 Rust `CodeEditor` 私有组合 | ✅ |\n| 文本、selection、undo/redo、语言切换和 syntax token 生命周期 | `zeta-editor::CodeEditorDocument` | ✅ |\n| Native 普通代码结构折叠、visible-row mapping 与 gutter control | `zeta-editor::CodeEditorDocument` / `CodeEditor` | ✅；宿主只转交点击 |\n| Native Composer 的 Shell 高亮 | `CodeEditorDocument::from_text_with_language` / `set_language` | ✅ |\n| Native 文件 document lifecycle | `zeta-text-file` + `FileEditorHost` / `FileEditorPane` | ✅；独立 crate 拥有 baseline/version/dirty/conflict，Native 已接通 Tab、Explorer load、save、关闭确认、外部重载/显式乐观覆盖、中心 Editor Surface 以及 keyboard/IME/pointer/clipboard/viewport 输入 |\n| Native `DiffEditor` 两侧 syntax token 投影 | `zeta-editor::DiffEditorDocument` / `DiffEditor` | ✅；宿主只提交 diff 与 language |\n| Aster bundled-language token | TextMate worker + lexical fallback | ✅ |\n| Aster parser facts | `zeta-rs/syntax` via bounded `platform/syntax` adapter | ✅ JavaScript/JSX、TypeScript/TSX、JSON/JSONC、Rust、Shell token/diagnostic/symbol/folding |\n| App Server syntax RPC | bounded stateless analysis | ✅；仅异步 facts，不拥有 editor revision 或输入热路径 |\n| Aster Smart Select | Aster selection/history + `zeta-syntax` on-demand scopes | ✅ expand/shrink；revision-bound、可取消、stale-safe，parser 失败时 lexical fallback |\n| completion、type、definition/reference、rename | `zeta-lsp` + language server，经编辑器 language feature 接入 | ✅ Code 主路径；语言覆盖由 catalog 决定 |\n| workspace code chunk index | `zeta-code-index` 消费 `zeta-syntax` declaration facts | ✅ 本地 lexical retrieval；不是统一 semantic symbol graph |\n| workspace symbol/reference capability | `zeta-lsp` + App Server language runtime | ✅ workspace symbols 与按需 references 已接通；持久化全局 semantic graph 仍属于可选 index 演进，不属于单个 editor document |\n\n## 一次 CodeEditor 编辑\n\n```text\nNative host\n  └─ CodeEditorDocument::apply / apply_composition / replace_text\n       ├─ 修改 authoritative editor text 与 history\n       ├─ CodeEditorAnalysis::synchronize\n       │  └─ zeta-syntax::SyntaxDocument::apply_edit\n       └─ 同 revision snapshot\n          ├─ token → line-relative CodeEditorSyntaxToken\n          └─ folding range → editor-owned visible-row projection\n             └─ CodeEditor::paint / fold_control_at\n```\n\n1. 宿主创建 `CodeEditorDocument` 时选择 `CodeEditorLanguage`，或在 mode 改变时调用\n   `set_language`。\n2. `CodeEditorDocument` 执行 Unicode-safe 编辑、IME commit、undo/redo 或全文替换。\n3. 私有 `CodeEditorAnalysis` 计算单个 UTF-8 replacement，推进内部 revision，并让\n   `SyntaxDocument` 复用旧 tree；解析失败时丢弃旧 analyzer，并从当前 authoritative text 重建。\n4. 同 revision snapshot 被投影为逐行 token 与 source-row folding range；每个\n   `CodeEditorDocument` 独立保存 collapsed state，并把 source row 映射为 visual row。\n5. 绘制只读取 document 已确认的 token/fold projection，并从当前 `CodeEditorStyle` 解析颜色；\n   主题变化不触发重解析。宿主把 `fold_control_at` 的结果原样交给 `toggle_fold_control`，不计算\n   fold 或 hidden row。\n\n调用方看不到 parser、tree、revision、edit batch 或 syntax transport。这些细节随编辑器文档一起\n创建、编辑和释放，避免 model ID、连接所有权、重连恢复和 stale-result gate 泄漏到 Workbench。\n\n## 所有权边界\n\n| 能力 | `zeta-syntax` | `CodeEditor` / Aster | 产品宿主 | App Server | `zeta-lsp` |\n| --- | --- | --- | --- | --- | --- |\n| grammar、query、tree-sitter tree | ✅ | 组合/消费 | ❌ | ❌ | ❌ |\n| editor text、selection、language 与本地 revision | ❌ | ✅ | 选择初始资源与语言 | ❌ | 消费同步 |\n| syntax token、parse facts 与 selection scopes | 计算 | ✅ 生命周期、selection history 与展示 | ❌ | 只投影有界 stateless request | ❌ |\n| theme color、DOM/native geometry、fold UI state | ❌ | ✅ | 注入主题/布局 | ❌ | ❌ |\n| 文件 dirty/save/conflict | ❌ | ❌ | 组合 `zeta-text-file` | 可提供独立文件 I/O capability | ❌ |\n| type、completion、definition/reference、rename | ❌ | 交互入口 | 协调 | 可承载独立 LSP runtime | ✅ |\n| workspace 扫描、watch 与 symbol index | ❌ | ❌ | 协调 | ✅ 承载 LSP workspace symbols/references；持久化 graph 可独立演进 | 消费/提供事实 |\n\n`zeta-syntax` 仍保持独立 crate，因为 grammar、query、资源上限和增量 tree 算法需要可测试的领域\n边界；但它不是产品对外服务。`zeta-editor` 是当前唯一产品 consumer，Native 不直接依赖它。\ntree-sitter 的输出是 concrete syntax facts，不是统一 AST，也不是 compiler/LSP semantic facts。\n\n## Desktop Aster\n\nAster 只对外暴露编辑器与 versioned language-provider contract。声明式 grammar 在专用 TextMate\nWorker 中运行；受支持语言还可通过 `RustSyntaxFactsService` 请求 App Server 的有界、无状态 syntax\nfacts。该 adapter 不建立远端 shadow document，不参与键盘、IME、selection 或同步 transaction；\n返回结果仍经过 Aster 的 model-version gate 后进入 token、diagnostic、symbol 与 folding owner。\n\nSmart Select 是一条按需路径：快捷键捕获当前 snapshot 与所有 selection，调用\n`syntax/selectionRanges`，每个 selection 只沿 parser named ancestors 返回默认最多 64 层；普通\n`syntax/analyze` 不序列化全树 selection nodes。结果只有在 request 未取消、model revision 与 selection\nset 都仍一致时才应用。首次 caret expansion 仍选择 word，后续优先最小 parser scope，再回退到\npair、line 和 document；shrink 只使用 Editor-owned history。该离散 command 可以承受 App Server\nround trip；任何进入连续 typing hot path 的未来 structural operation 必须迁到 editor worker/in-process\nparser。\n\n## DiffEditor 与后续演进\n\n`DiffEditorDocument` 接收已经计算完成的 `DiffDocument` 和一个 `CodeEditorLanguage`，内部重建\noriginal/modified 精确文本，并各自持有 `CodeEditorDocument`。双栏和 unified projection 根据\nsource line number 读取同一份 editor-owned token；Changes Pane 不知道 parser、revision 或 token。\n语言切换只调用 `DiffEditorDocument::set_language`，不会产生 App Server 请求。\n\n近期工作按以下顺序推进：\n\n1. 按真实产品需要扩展 parser-grade 语言覆盖，不复制已有 editor language contract。\n2. 当前 `zeta-code-index` 已作为独立 workspace capability 消费 declaration ranges；后续 semantic\n   symbol/reference graph 继续由 LSP/index owner 演进，不能回填到 editor-local parser lifecycle。\n3. 先增加 select declaration/argument/expression/statement 等只读 operation；delete/move/wrap 等 mutation\n   必须返回 revision-bound plan，由 Editor 在一个 undo transaction 中应用和复核。\n\n长期不变量是：产品层暴露编辑器，不暴露 parser RPC；编辑器拥有文档内语言能力，\n`zeta-syntax` 拥有底层 syntax 算法，LSP/compiler 拥有跨文件语义事实。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "一次-codeeditor-编辑",
        "title": "一次 CodeEditor 编辑"
      },
      {
        "depth": 2,
        "id": "所有权边界",
        "title": "所有权边界"
      },
      {
        "depth": 2,
        "id": "desktop-aster",
        "title": "Desktop Aster"
      },
      {
        "depth": 2,
        "id": "diffeditor-与后续演进",
        "title": "DiffEditor 与后续演进"
      }
    ],
    "searchText": " 状态：Rust CodeEditor 已内部接入 Rust、JSON、JSONC 与 Shell 的增量 tree-sitter 分析；Rust DiffEditor 已通过 retained DiffEditorDocument 内部维护两侧语法状态；Desktop Aster Text Engine 通过 TextMate 与有界 Rust syntax facts 提供语法能力。本文拥有跨编辑器的语法能力边界；底层解析契约见 zeta-syntax README，符号索引、Language Server、代码检索与未来代码图 的跨系统演进见 code-intelligence.md，Native 编辑器 API 见 zeta-editor README。 快速理解 语法高亮、折叠、outline 和 parse diagnostic 都是编辑器能力。产品层对外组合 CodeEditor 或 DiffEditor，只提供文档、语言、主题和平台输入；它不获得独立 syntax service， 也不通过 App Server 同步编辑器内部 revision。 能力 当前 owner 状态 --- --- --- Rust/JSON/JSONC/Shell grammar、query 与增量 tree zeta-syntax，由 Rust CodeEditor 私有组合 ✅ 文本、selection、undo/redo、语言切换和 syntax token 生命周期 zeta-editor::CodeEditorDocument ✅ Native 普通代码结构折叠、visible-row mapping 与 gutter control zeta-editor::CodeEditorDocument / CodeEditor ✅；宿主只转交点击 Native Composer 的 Shell 高亮 CodeEditorDocument::fromtextwithlanguage / setlanguage ✅ Native 文件 document lifecycle zeta-text-file + FileEditorHost / FileEditorPane ✅；独立 crate 拥有 baseline/version/dirty/conflict，Native 已接通 Tab、Explorer load、save、关闭确认、外部重载/显式乐观覆盖、中心 Editor Surface 以及 keyboard/IME/pointer/clipboard/viewport 输入 Native DiffEditor 两侧 syntax token 投影 zeta-editor::DiffEditorDocument / DiffEditor ✅；宿主只提交 diff 与 language Aster bundled-language token TextMate worker + lexical fallback ✅ Aster parser facts zeta-rs/syntax via bounded platform/syntax adapter ✅ JavaScript/JSX、TypeScript/TSX、JSON/JSONC、Rust、Shell token/diagnostic/symbol/folding App Server syntax RPC bounded stateless analysis ✅；仅异步 facts，不拥有 editor revision 或输入热路径 Aster Smart Select Aster selection/history + zeta-syntax on-demand scopes ✅ expand/shrink；revision-bound、可取消、stale-safe，parser 失败时 lexical fallback completion、type、definition/reference、rename zeta-lsp + language server，经编辑器 language feature 接入 ✅ Code 主路径；语言覆盖由 catalog 决定 workspace code chunk index zeta-code-index 消费 zeta-syntax declaration facts ✅ 本地 lexical retrieval；不是统一 semantic symbol graph workspace symbol/reference capability zeta-lsp + App Server language runtime ✅ workspace symbols 与按需 references 已接通；持久化全局 semantic graph 仍属于可选 index 演进，不属于单个 editor document 一次 CodeEditor 编辑 ``text Native host └─ CodeEditorDocument::apply / applycomposition / replacetext ├─ 修改 authoritative editor text 与 history ├─ CodeEditorAnalysis::synchronize │ └─ zeta-syntax::SyntaxDocument::applyedit └─ 同 revision snapshot ├─ token → line-relative CodeEditorSyntaxToken └─ folding range → editor-owned visible-row projection └─ CodeEditor::paint / foldcontrolat ` 1. 宿主创建 CodeEditorDocument 时选择 CodeEditorLanguage，或在 mode 改变时调用 setlanguage。 2. CodeEditorDocument 执行 Unicode-safe 编辑、IME commit、undo/redo 或全文替换。 3. 私有 CodeEditorAnalysis 计算单个 UTF-8 replacement，推进内部 revision，并让 SyntaxDocument 复用旧 tree；解析失败时丢弃旧 analyzer，并从当前 authoritative text 重建。 4. 同 revision snapshot 被投影为逐行 token 与 source-row folding range；每个 CodeEditorDocument 独立保存 collapsed state，并把 source row 映射为 visual row。 5. 绘制只读取 document 已确认的 token/fold projection，并从当前 CodeEditorStyle 解析颜色； 主题变化不触发重解析。宿主把 foldcontrolat 的结果原样交给 togglefoldcontrol，不计算 fold 或 hidden row。 调用方看不到 parser、tree、revision、edit batch 或 syntax transport。这些细节随编辑器文档一起 创建、编辑和释放，避免 model ID、连接所有权、重连恢复和 stale-result gate 泄漏到 Workbench。 所有权边界 能力 zeta-syntax CodeEditor / Aster 产品宿主 App Server zeta-lsp --- --- --- --- --- --- grammar、query、tree-sitter tree ✅ 组合/消费 ❌ ❌ ❌ editor text、selection、language 与本地 revision ❌ ✅ 选择初始资源与语言 ❌ 消费同步 syntax token、parse facts 与 selection scopes 计算 ✅ 生命周期、selection history 与展示 ❌ 只投影有界 stateless request ❌ theme color、DOM/native geometry、fold UI state ❌ ✅ 注入主题/布局 ❌ ❌ 文件 dirty/save/conflict ❌ ❌ 组合 zeta-text-file 可提供独立文件 I/O capability ❌ type、completion、definition/reference、rename ❌ 交互入口 协调 可承载独立 LSP runtime ✅ workspace 扫描、watch 与 symbol index ❌ ❌ 协调 ✅ 承载 LSP workspace symbols/references；持久化 graph 可独立演进 消费/提供事实 zeta-syntax 仍保持独立 crate，因为 grammar、query、资源上限和增量 tree 算法需要可测试的领域 边界；但它不是产品对外服务。zeta-editor 是当前唯一产品 consumer，Native 不直接依赖它。 tree-sitter 的输出是 concrete syntax facts，不是统一 AST，也不是 compiler/LSP semantic facts。 Desktop Aster Aster 只对外暴露编辑器与 versioned language-provider contract。声明式 grammar 在专用 TextMate Worker 中运行；受支持语言还可通过 RustSyntaxFactsService 请求 App Server 的有界、无状态 syntax facts。该 adapter 不建立远端 shadow document，不参与键盘、IME、selection 或同步 transaction； 返回结果仍经过 Aster 的 model-version gate 后进入 token、diagnostic、symbol 与 folding owner。 Smart Select 是一条按需路径：快捷键捕获当前 snapshot 与所有 selection，调用 syntax/selectionRanges，每个 selection 只沿 parser named ancestors 返回默认最多 64 层；普通 syntax/analyze 不序列化全树 selection nodes。结果只有在 request 未取消、model revision 与 selection set 都仍一致时才应用。首次 caret expansion 仍选择 word，后续优先最小 parser scope，再回退到 pair、line 和 document；shrink 只使用 Editor-owned history。该离散 command 可以承受 App Server round trip；任何进入连续 typing hot path 的未来 structural operation 必须迁到 editor worker/in-process parser。 DiffEditor 与后续演进 DiffEditorDocument 接收已经计算完成的 DiffDocument 和一个 CodeEditorLanguage，内部重建 original/modified 精确文本，并各自持有 CodeEditorDocument。双栏和 unified projection 根据 source line number 读取同一份 editor-owned token；Changes Pane 不知道 parser、revision 或 token。 语言切换只调用 DiffEditorDocument::setlanguage，不会产生 App Server 请求。 近期工作按以下顺序推进： 1. 按真实产品需要扩展 parser-grade 语言覆盖，不复制已有 editor language contract。 2. 当前 zeta-code-index 已作为独立 workspace capability 消费 declaration ranges；后续 semantic symbol/reference graph 继续由 LSP/index owner 演进，不能回填到 editor-local parser lifecycle。 3. 先增加 select declaration/argument/expression/statement 等只读 operation；delete/move/wrap 等 mutation 必须返回 revision-bound plan，由 Editor 在一个 undo transaction 中应用和复核。 长期不变量是：产品层暴露编辑器，不暴露 parser RPC；编辑器拥有文档内语言能力， zeta-syntax` 拥有底层 syntax 算法，LSP/compiler 拥有跨文件语义事实。"
  },
  {
    "slug": "lsp",
    "sourcePath": "docs/lsp.md",
    "title": "语言服务器系统",
    "description": "Zeta 通过独立的 catalog、语言服务协调层和 LSP 运行时连接现有语言服务器，而不是把语言分析逻辑 写进编辑器。Native 已接入文档生命周期和事件循环；App Server Config authority 持久化 三个内置服务器的 Disabled/Automatic/Enabled 与可选绝对 executable path。Settings 中每个 …",
    "group": "界面与体验",
    "markdown": "> 状态：catalog/provider、托管 Node、低层 LSP runtime 与产品级 language-service supervisor 已实现；Desktop/App Server\n> 已接入 revision-bound 语言请求与编辑器 provider，Native 仍保留既有 Rust、JSON/JSONC、Shell\n> 设置和 diagnostics presentation。\n> 本文拥有跨 crate 的语言能力语义、所有权和演进阶段；当前实现接口与修改路径由\n> [`zeta-lsp` README](../zeta-rs/lsp/README.md) 和\n> [`zeta-language-server-catalog` README](../zeta-rs/language-server-catalog/README.md)、\n> [`zeta-language-service` README](../zeta-rs/language-service/README.md) 分别拥有。本地符号索引与\n> Language Server 结果如何组合、以及未来代码图的边界见\n> [`code-intelligence.md`](code-intelligence.md)。\n\n## 快速理解\n\nZeta 通过独立的 catalog、语言服务协调层和 LSP 运行时连接现有语言服务器，而不是把语言分析逻辑\n写进编辑器。Native 已接入文档生命周期和事件循环；App Server Config authority 持久化\n三个内置服务器的 Disabled/Automatic/Enabled 与可选绝对 executable path。Settings 中每个 server\n都有独立的 Enable switch，默认关闭；开启时默认选择 Automatic，仍可用 Launch mode 选择\nAutomatic 或 Enabled。未配置的 PATH server 默认 Disabled，不会自动拉起；用户显式开启后才从进程\n启动时冻结的 PATH 解析。Marketplace package 的安装确认同时写入 activation receipt，因此其 packaged\nroute 默认启用；显式 Config `Disabled` 仍可关闭。缺失或不可执行时保持无语言服务器。\n\n| 使用场景 | 当前结果 | 谁负责下一步 |\n| --- | --- | --- |\n| 启动一个已解析的语言服务器命令 | ✅ 完成 initialize/initialized，冻结能力和位置编码 | `zeta-lsp` |\n| 打开、修改、保存和关闭文档 | ✅ Native 发送 editor revision/full text；协调层绑定 LSP version | `zeta-language-service` + `zeta-lsp` |\n| hover、completion、definition 等请求 | ✅ 产品 facade、capability/freshness gate、Desktop 编辑器 provider；completion resolve/command 和多目标 Peek 已接通 | `zeta-language-service` + Desktop |\n| 请求取消与冷暖延迟观测 | ✅ in-flight task cancellation；按 request kind/server incarnation/config/service generation 记录 content-free outcome | `zeta-language-service` + App Server metrics sink |\n| Semantic Tokens 与文档特性 | ✅ Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors、Folding 已适配现有 Editor contract | `zeta-language-service` + Desktop |\n| push / document pull diagnostics | ✅ 共用 freshness 校验和 Problems 数据入口；pull full report 替换当前快照，unchanged report 保留已有结果 | `zeta-language-service` + App Server + Desktop |\n| workspace diagnostics | ✅ 调用 `workspace/diagnostic`，App Server 通过 Workspace authority 读取未打开文件并转换范围，Desktop 将完整结果写入同一 Problems repository | `zeta-language-service` + App Server + Desktop |\n| 日志、show message 与 work-done progress | ✅ Desktop 将日志投影到 Output/Language Servers，showMessage 使用 Workbench Dialog，活动进度显示在状态栏与 Output | App Server + Desktop Workbench |\n| 显式替换服务器并恢复文档 | ✅ 新实例重放成功后切换 route/incarnation | 宿主需暂存 replacement 早期事件 |\n| 配置与发现 Rust/JSON/Shell server | ✅ 独立 Settings draft、revision-safe mode/path、catalog 校验与热重配 | 扩展安装 provider/UI |\n| 用共享 Node-compatible runtime 运行已验证 CSS package | ✅ Desktop 复用 Electron run-as-Node；Rust 同步 TUF catalog、校验兼容性，并从 activation receipt 自动重建 provider | 用户在 Settings/Languages 确认 exact signed package |\n| 意外退出、退避重启和 crash-loop | ✅ 断连 retirement、有限指数退避、状态展示和全文重放 | `zeta-language-service` + Native |\n| 安装、更新和选择其他 server | 部分具备；CSS 的 TUF download、确认、activation 与热 registry 已完成；其他 server 仍需 catalog adapter | Marketplace / catalog |\n| 动态注册与 work-done progress | ✅ 按 server incarnation 隔离，静态与动态 capability 共同参与请求 gate | `zeta-lsp` + `zeta-language-service` |\n| workspace edit、LSP 3.18 新能力 | 尚未完成 | 后续按真实消费者逐项加入 |\n\n继续阅读：[一次操作](#1-一次操作)、[所有权](#2-所有权边界)、\n[失败语义](#3-可靠性与失败语义)、[当前状态](#4-当前实现与演进)。\n\n```mermaid\nflowchart LR\n    Host[\"Native / Desktop host\"] --> Catalog[\"zeta-language-server-catalog\"]\n    Pack[\"Verified language pack\"] --> Provider[\"LanguageServerProvider\"]\n    Node[\"Managed JS runtime<br/>Electron or packaged Node\"] --> Provider\n    Provider --> Catalog\n    Catalog --> Service[\"zeta-language-service\"]\n    Service --> Runtime[\"zeta-lsp runtime\"]\n    Runtime --> Server[\"Language server process\"]\n    Server --> Runtime\n    Runtime --> Service\n    Service --> Host\n    Host --> Editor[\"zeta-editor / legacy editor runtime presentation\"]\n```\n\n## 1. 一次操作\n\n1. Native 通过 `config/read` 获得持久化 server preference；Settings UI 通过 revision-safe typed\n   command 更新，`config/changed` 只作为重新读取权威 snapshot 的失效提示。\n2. 产品宿主把 preference、冻结的 executable candidates 和 execution policy 交给 catalog；catalog 只在候选\n   可 canonicalize、为普通可执行文件时产生 resolved definition。对 package-backed CSS，宿主先把\n   distribution activation authority 返回的 `InstalledLanguageServer` 和宿主选择的共享 Node-compatible runtime 注入\n   `CssLanguageServerProvider`；provider 产生同样的 definition，不直接启动 child。\n3. 产品宿主把 definitions 交给 `zeta-language-service`；无 definition 时禁用，不启动进程。\n4. 协调层把 resolved command 委托给 `zeta-lsp`；后者启动或接入 transport、发送 initialize，\n   并校验 server 选定的位置编码。\n5. 运行时发送 initialized，只有此后才向调用方返回 ready client。\n6. 宿主发送带 editor revision 的 full snapshot；协调层拒绝 stale revision，运行时从版本 1 开始，\n   按协商策略发送后续 change/save/close。\n7. hover、completion 或 definition 先经过 routed revision 与 capability gate，再转换协商位置编码并\n   发送 typed request。结果必须再次通过 service generation、server epoch 和 editor revision；Native\n   用鼠标请求 hover、用 Ctrl/Cmd+Space 打开 completion popup，只接受可安全投影为精确 UTF-8\n   `textEdit` 的候选，F12 definition 通过 App Server 文件 authority 打开目标。\n   请求 task 绑定 service generation、server incarnation 与 editor revision；显式取消、disable、重配或\n   shutdown 都会中止 task。完成后只记录 kind、代次、cold/warm、elapsed、result count 和 terminal\n   outcome，不记录路径、文本、query 或 position。\n8. Server push diagnostics 时，协调层按 URI、document version 和当前 position encoding 拒绝\n   stale result、转换为 UTF-8 byte range，再交给产品 event loop；Native adapter 转换为\n   `CodeEditorDiagnostic`，CodeEditor 按 source row/soft-wrap geometry 绘制并执行 hover hit-test。\n9. 关闭时依次执行 shutdown request、exit notification、driver stop 和 child reap。\n\n文档内容仍由 EditorHost 拥有。LSP version 只是 open document 的协议顺序，不是磁盘 revision、\nGit object identity 或 durable product sequence。\n\n## 2. 所有权边界\n\n| 能力 | `zeta-lsp` | Catalog / Provider | Language Service | Distribution | Native / Editor | App Server |\n| --- | --- | --- | --- | --- | --- | --- |\n| framing、initialize、request pairing、shutdown | ✅ | ❌ | 委托 | ❌ | ❌ | ❌ |\n| PATH discovery、canonical executable、server identity | ❌ | ✅ | ❌ | ❌ | 提供候选/policy | ❌ |\n| managed Node 与 package/native 启动命令 | 只执行冻结 command | ✅ | ❌ | 只返回入口路径 | 注入 package/provider | 组合 registry |\n| process enablement、language route、generation | ❌ | 提供 definition | ✅ | ❌ | 组合 | 配置与组合 |\n| restart backoff、预算和 crash-loop | 只上报断连事实 | ❌ | ✅ | ❌ | 展示运行态 | 事件转发 |\n| server 包 TUF/digest 校验与 side-by-side 安装 | ❌ | 只消费已安装 receipt | ❌ | ✅ 安装/activation | 展示确认 | ✅ 组合 Marketplace consumer |\n| 当前 document text / editor revision | ❌ | ❌ | 借用 snapshot | ❌ | ✅ | 文件 I/O authority |\n| diagnostics freshness 与 position conversion | 提供协议事实 | ❌ | ✅ | ❌ | ✅ Editor 绘制，Native hover | 协议投影 |\n| mode/path durable preference | ❌ | 只消费 preference | ❌ | ❌ | Settings UI / adapter | ✅ authority |\n| workspace trust / executable policy | ❌ | 只消费结果 | 只消费 definition | ❌ | 协调 | authority |\n\n`zeta-editor` 保持纯 presentation，不依赖 catalog、`zeta-lsp` 或协调层。Native host 组合 catalog、\neditor 和 `zeta-language-service`，只在 adapter 中转换文档与事件；\nDesktop 的 legacy editor runtime host 可以消费相同系统语义，但不需要复用 Native paint types。\nApp Server 已经是 Desktop 的 workspace/document authority 与 LSP IPC boundary；它只组合 provider、配置、\n`zeta-language-service` 和协议 DTO，不复制 LSP framing、process supervisor 或 Editor presentation。\n\n## 3. 可靠性与失败语义\n\n- **有界 transport**：header 上限为 16 KiB，单消息上限为 4 MiB；非法 framing 或 envelope\n  明确终止 connection。\n- **初始化 gate**：initialize 失败、超时或选择不支持的位置编码时，不返回部分 ready client。\n- **请求隔离**：每个 request 使用唯一整数 ID 和独立 completion；普通请求超时后发送协议取消。\n- **文档顺序**：同一 URI 的 open/change/save/close 在 document lock 下排序；change 版本只在\n  notification 成功写入 transport 后提交。\n- **宿主回调**：事件 callback 必须快速返回；阻塞 callback 会阻塞该 server 的协议进展。\n- **进程回收**：规范关闭失败仍继续 exit 和 cleanup；直接 drop 只作为 fail-safe kill。\n- **替换恢复**：新服务器先按 URI 排序重放当前全文；任一 open 失败就清理 replacement 并保留\n  原 route。成功后 server incarnation 递增、LSP document version 从 1 重新开始。\n- **请求观测**：`Delivered`、`Empty`、`Failed`、`Cancelled`、`StaleDiscarded` 与 `Rejected` 显式\n  区分；cold/warm 以 `(server, incarnation, request kind)` 计算，避免把重启后的首次请求算作 warm。\n\n意外 EOF、framing failure 或 transport channel 关闭会产生 `TransportClosed`。Supervisor 只接受\n当前 service generation 和 server epoch 的断连事件：Starting 阶段直接进入失败决策，Ready 阶段\n先清空该 route 的 diagnostics、标记文档 unrouted、退休旧 route/bindings，再按有限指数退避启动\n新进程。默认最多重启五次，延迟从 250 ms 增长到 4 s 上限；Ready 持续 60 s 后连续失败预算重置，\n预算耗尽则进入可见 `CrashLoop`。禁用或热重配会使旧 launch、timer 和协议事件失效。新实例 Ready\n后从协调层保留的 authoritative editor snapshots 重放，不从已退休 router 复用旧文本。\n\n## 4. 当前实现与演进\n\n### 当前状态\n\n- 独立 `zeta-lsp` crate、Cargo/Bazel target 和 typed `lsp-types` public surface；\n- stdio child 与 caller-provided async transport；\n- initialize/initialized、workspace configuration、动态 capability registration、work-done progress、push/document-pull diagnostics、日志和消息事件；\n- full/incremental document synchronization、save policy 和单调 version；\n- generic typed requests、deadline cancellation、shutdown/exit；\n- 唯一 language route、EditorHost revision binding、显式 server replacement 和全文 replay；\n- 独立 `zeta-language-server-catalog`、Rust built-in identity、frozen PATH resolution 和 policy gate；\n- `LanguageServerProviderRegistry`、`ManagedNodeRuntime` 和 verified CSS package provider；\n- Desktop 通过 allowlisted `ZETA_ELECTRON_RUN_AS_NODE_PATH` 复用 exact Electron executable，且只在\n  language-server child 的 clean environment 中设置 `ELECTRON_RUN_AS_NODE=1`；\n- standalone/headless package 保留锁定 Node.js runtime 与 license 作为非 Electron 回退；\n- App Server 将 activation-confirmed provider definition 与显式启用的 built-in catalog definition 合并后交给同一 supervisor；\n- 独立 `zeta-language-service` supervisor、显式 enablement、resolved definition 校验、generation gate；\n- Native 文档 open/change/save/close、workspace replacement 与 event-loop adapter；\n- `zeta-config` mode/path schema、App Server typed mutation/config notification，以及三个内置 server 的\n  Native Settings selector 与独立 draft；\n- config generation gate、server replacement 与全部打开文档 replay；\n- diagnostics freshness 校验、UTF-8/UTF-16 position conversion 和 product-neutral event；\n- Native revision gate、CodeEditor severity squiggle/soft-wrap projection 与 hover detail；\n- transport-close detection、断连 route retirement、server epoch gate、有限指数退避与 crash-loop state；\n- Native Settings 的 Starting/Ready/BackingOff/CrashLoop/Failed/Stopped runtime projection；\n- hover/completion/resolve/command/navigation/hierarchy/rename/code-action/formatting/signature-help/inlay-hints/linked-editing product facade、\n  capability gate、三重 freshness gate、Renderer latest-request gate 与可见交互；\n- 语言请求 in-flight task registry、显式 cancellation，以及 content-free cold/warm latency/result-count/\n  terminal-outcome metrics sink；\n- Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors、Folding 的 revision-bound facade 与 Desktop Editor provider；\n- document/workspace pull diagnostics 的 capability gate、report projection、App Server route 与 Desktop Problems 数据接入；\n- 多 definition target 使用现有 Peek 列表选择，单目标直接导航；\n- Rust、JSON/JSONC、Shell 三项 built-in catalog definitions；\n- verified package staging、SHA-256、side-by-side update 与 receipt；\n- 通用 Language Marketplace TUF consumer、delegated publisher 校验、有界 ZIP extraction、Marketplace\n  tree digest、consumer SemVer compatibility 与 stable error mapping；\n- Desktop Settings/Languages exact package confirmation，App Server `language/marketplace/list` / `install`\n  RPC，以及 profile activation receipt 启动重建和安装后的热 registry replacement；\n- in-memory protocol vertical tests。\n\n### 计划\n\n1. 把 workspace trust 的明确结果映射为 catalog execution policy。\n2. 增加 pull diagnostics 的 result-id cache、partial-result progress 与 `workspace/diagnostic/refresh`。\n3. 为其他 language-server family 增加 schema 2 runtime metadata 和 catalog provider adapter，并为\n   未激活 version 增加独立 garbage collection policy。\n4. 先评估已记录的重复请求、冷暖延迟和取消率；只有收益显著时才设计 session navigation cache。\n   cache 必须绑定 server incarnation、configuration/service generation、request shape 和 document\n   revision；当前没有安装或持久化 navigation cache。\n\n### 潜在方向\n\n远程 workspace 或共享 daemon 出现后，可以把 server execution 放到 App Server 后方；前置条件是\n定义 workspace authority、document content transport、cancellation、incarnation、privacy 和\ndisconnect recovery。当前本地 editor request 不承担这套远程成本。\n\n长期不变量是：协议运行时不拥有编辑器文档，编辑器组件不解析 LSP，宿主不把旧文档版本的结果\n应用到当前 revision，未实现的 server capability 不对外宣称。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-一次操作",
        "title": "1. 一次操作"
      },
      {
        "depth": 2,
        "id": "2-所有权边界",
        "title": "2. 所有权边界"
      },
      {
        "depth": 2,
        "id": "3-可靠性与失败语义",
        "title": "3. 可靠性与失败语义"
      },
      {
        "depth": 2,
        "id": "4-当前实现与演进",
        "title": "4. 当前实现与演进"
      },
      {
        "depth": 3,
        "id": "当前状态",
        "title": "当前状态"
      },
      {
        "depth": 3,
        "id": "计划",
        "title": "计划"
      },
      {
        "depth": 3,
        "id": "潜在方向",
        "title": "潜在方向"
      }
    ],
    "searchText": " 状态：catalog/provider、托管 Node、低层 LSP runtime 与产品级 language-service supervisor 已实现；Desktop/App Server 已接入 revision-bound 语言请求与编辑器 provider，Native 仍保留既有 Rust、JSON/JSONC、Shell 设置和 diagnostics presentation。 本文拥有跨 crate 的语言能力语义、所有权和演进阶段；当前实现接口与修改路径由 zeta-lsp README 和 zeta-language-server-catalog README、 zeta-language-service README 分别拥有。本地符号索引与 Language Server 结果如何组合、以及未来代码图的边界见 code-intelligence.md。 快速理解 Zeta 通过独立的 catalog、语言服务协调层和 LSP 运行时连接现有语言服务器，而不是把语言分析逻辑 写进编辑器。Native 已接入文档生命周期和事件循环；App Server Config authority 持久化 三个内置服务器的 Disabled/Automatic/Enabled 与可选绝对 executable path。Settings 中每个 server 都有独立的 Enable switch，默认关闭；开启时默认选择 Automatic，仍可用 Launch mode 选择 Automatic 或 Enabled。未配置的 PATH server 默认 Disabled，不会自动拉起；用户显式开启后才从进程 启动时冻结的 PATH 解析。Marketplace package 的安装确认同时写入 activation receipt，因此其 packaged route 默认启用；显式 Config Disabled 仍可关闭。缺失或不可执行时保持无语言服务器。 使用场景 当前结果 谁负责下一步 --- --- --- 启动一个已解析的语言服务器命令 ✅ 完成 initialize/initialized，冻结能力和位置编码 zeta-lsp 打开、修改、保存和关闭文档 ✅ Native 发送 editor revision/full text；协调层绑定 LSP version zeta-language-service + zeta-lsp hover、completion、definition 等请求 ✅ 产品 facade、capability/freshness gate、Desktop 编辑器 provider；completion resolve/command 和多目标 Peek 已接通 zeta-language-service + Desktop 请求取消与冷暖延迟观测 ✅ in-flight task cancellation；按 request kind/server incarnation/config/service generation 记录 content-free outcome zeta-language-service + App Server metrics sink Semantic Tokens 与文档特性 ✅ Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors、Folding 已适配现有 Editor contract zeta-language-service + Desktop push / document pull diagnostics ✅ 共用 freshness 校验和 Problems 数据入口；pull full report 替换当前快照，unchanged report 保留已有结果 zeta-language-service + App Server + Desktop workspace diagnostics ✅ 调用 workspace/diagnostic，App Server 通过 Workspace authority 读取未打开文件并转换范围，Desktop 将完整结果写入同一 Problems repository zeta-language-service + App Server + Desktop 日志、show message 与 work-done progress ✅ Desktop 将日志投影到 Output/Language Servers，showMessage 使用 Workbench Dialog，活动进度显示在状态栏与 Output App Server + Desktop Workbench 显式替换服务器并恢复文档 ✅ 新实例重放成功后切换 route/incarnation 宿主需暂存 replacement 早期事件 配置与发现 Rust/JSON/Shell server ✅ 独立 Settings draft、revision-safe mode/path、catalog 校验与热重配 扩展安装 provider/UI 用共享 Node-compatible runtime 运行已验证 CSS package ✅ Desktop 复用 Electron run-as-Node；Rust 同步 TUF catalog、校验兼容性，并从 activation receipt 自动重建 provider 用户在 Settings/Languages 确认 exact signed package 意外退出、退避重启和 crash-loop ✅ 断连 retirement、有限指数退避、状态展示和全文重放 zeta-language-service + Native 安装、更新和选择其他 server 部分具备；CSS 的 TUF download、确认、activation 与热 registry 已完成；其他 server 仍需 catalog adapter Marketplace / catalog 动态注册与 work-done progress ✅ 按 server incarnation 隔离，静态与动态 capability 共同参与请求 gate zeta-lsp + zeta-language-service workspace edit、LSP 3.18 新能力 尚未完成 后续按真实消费者逐项加入 继续阅读：一次操作、所有权、 失败语义、当前状态。 ``mermaid flowchart LR Host \"Native / Desktop host\" -- Catalog \"zeta-language-server-catalog\" Pack \"Verified language pack\" -- Provider \"LanguageServerProvider\" Node \"Managed JS runtime<br/ Electron or packaged Node\" -- Provider Provider -- Catalog Catalog -- Service \"zeta-language-service\" Service -- Runtime \"zeta-lsp runtime\" Runtime -- Server \"Language server process\" Server -- Runtime Runtime -- Service Service -- Host Host -- Editor \"zeta-editor / legacy editor runtime presentation\" ` 1. 一次操作 1. Native 通过 config/read 获得持久化 server preference；Settings UI 通过 revision-safe typed command 更新，config/changed 只作为重新读取权威 snapshot 的失效提示。 2. 产品宿主把 preference、冻结的 executable candidates 和 execution policy 交给 catalog；catalog 只在候选 可 canonicalize、为普通可执行文件时产生 resolved definition。对 package-backed CSS，宿主先把 distribution activation authority 返回的 InstalledLanguageServer 和宿主选择的共享 Node-compatible runtime 注入 CssLanguageServerProvider；provider 产生同样的 definition，不直接启动 child。 3. 产品宿主把 definitions 交给 zeta-language-service；无 definition 时禁用，不启动进程。 4. 协调层把 resolved command 委托给 zeta-lsp；后者启动或接入 transport、发送 initialize， 并校验 server 选定的位置编码。 5. 运行时发送 initialized，只有此后才向调用方返回 ready client。 6. 宿主发送带 editor revision 的 full snapshot；协调层拒绝 stale revision，运行时从版本 1 开始， 按协商策略发送后续 change/save/close。 7. hover、completion 或 definition 先经过 routed revision 与 capability gate，再转换协商位置编码并 发送 typed request。结果必须再次通过 service generation、server epoch 和 editor revision；Native 用鼠标请求 hover、用 Ctrl/Cmd+Space 打开 completion popup，只接受可安全投影为精确 UTF-8 textEdit 的候选，F12 definition 通过 App Server 文件 authority 打开目标。 请求 task 绑定 service generation、server incarnation 与 editor revision；显式取消、disable、重配或 shutdown 都会中止 task。完成后只记录 kind、代次、cold/warm、elapsed、result count 和 terminal outcome，不记录路径、文本、query 或 position。 8. Server push diagnostics 时，协调层按 URI、document version 和当前 position encoding 拒绝 stale result、转换为 UTF-8 byte range，再交给产品 event loop；Native adapter 转换为 CodeEditorDiagnostic，CodeEditor 按 source row/soft-wrap geometry 绘制并执行 hover hit-test。 9. 关闭时依次执行 shutdown request、exit notification、driver stop 和 child reap。 文档内容仍由 EditorHost 拥有。LSP version 只是 open document 的协议顺序，不是磁盘 revision、 Git object identity 或 durable product sequence。 2. 所有权边界 能力 zeta-lsp Catalog / Provider Language Service Distribution Native / Editor App Server --- --- --- --- --- --- --- framing、initialize、request pairing、shutdown ✅ ❌ 委托 ❌ ❌ ❌ PATH discovery、canonical executable、server identity ❌ ✅ ❌ ❌ 提供候选/policy ❌ managed Node 与 package/native 启动命令 只执行冻结 command ✅ ❌ 只返回入口路径 注入 package/provider 组合 registry process enablement、language route、generation ❌ 提供 definition ✅ ❌ 组合 配置与组合 restart backoff、预算和 crash-loop 只上报断连事实 ❌ ✅ ❌ 展示运行态 事件转发 server 包 TUF/digest 校验与 side-by-side 安装 ❌ 只消费已安装 receipt ❌ ✅ 安装/activation 展示确认 ✅ 组合 Marketplace consumer 当前 document text / editor revision ❌ ❌ 借用 snapshot ❌ ✅ 文件 I/O authority diagnostics freshness 与 position conversion 提供协议事实 ❌ ✅ ❌ ✅ Editor 绘制，Native hover 协议投影 mode/path durable preference ❌ 只消费 preference ❌ ❌ Settings UI / adapter ✅ authority workspace trust / executable policy ❌ 只消费结果 只消费 definition ❌ 协调 authority zeta-editor 保持纯 presentation，不依赖 catalog、zeta-lsp 或协调层。Native host 组合 catalog、 editor 和 zeta-language-service，只在 adapter 中转换文档与事件； Desktop 的 legacy editor runtime host 可以消费相同系统语义，但不需要复用 Native paint types。 App Server 已经是 Desktop 的 workspace/document authority 与 LSP IPC boundary；它只组合 provider、配置、 zeta-language-service 和协议 DTO，不复制 LSP framing、process supervisor 或 Editor presentation。 3. 可靠性与失败语义 - 有界 transport：header 上限为 16 KiB，单消息上限为 4 MiB；非法 framing 或 envelope 明确终止 connection。 - 初始化 gate：initialize 失败、超时或选择不支持的位置编码时，不返回部分 ready client。 - 请求隔离：每个 request 使用唯一整数 ID 和独立 completion；普通请求超时后发送协议取消。 - 文档顺序：同一 URI 的 open/change/save/close 在 document lock 下排序；change 版本只在 notification 成功写入 transport 后提交。 - 宿主回调：事件 callback 必须快速返回；阻塞 callback 会阻塞该 server 的协议进展。 - 进程回收：规范关闭失败仍继续 exit 和 cleanup；直接 drop 只作为 fail-safe kill。 - 替换恢复：新服务器先按 URI 排序重放当前全文；任一 open 失败就清理 replacement 并保留 原 route。成功后 server incarnation 递增、LSP document version 从 1 重新开始。 - 请求观测：Delivered、Empty、Failed、Cancelled、StaleDiscarded 与 Rejected 显式 区分；cold/warm 以 server, incarnation, request kind 计算，避免把重启后的首次请求算作 warm。 意外 EOF、framing failure 或 transport channel 关闭会产生 TransportClosed。Supervisor 只接受 当前 service generation 和 server epoch 的断连事件：Starting 阶段直接进入失败决策，Ready 阶段 先清空该 route 的 diagnostics、标记文档 unrouted、退休旧 route/bindings，再按有限指数退避启动 新进程。默认最多重启五次，延迟从 250 ms 增长到 4 s 上限；Ready 持续 60 s 后连续失败预算重置， 预算耗尽则进入可见 CrashLoop。禁用或热重配会使旧 launch、timer 和协议事件失效。新实例 Ready 后从协调层保留的 authoritative editor snapshots 重放，不从已退休 router 复用旧文本。 4. 当前实现与演进 当前状态 - 独立 zeta-lsp crate、Cargo/Bazel target 和 typed lsp-types public surface； - stdio child 与 caller-provided async transport； - initialize/initialized、workspace configuration、动态 capability registration、work-done progress、push/document-pull diagnostics、日志和消息事件； - full/incremental document synchronization、save policy 和单调 version； - generic typed requests、deadline cancellation、shutdown/exit； - 唯一 language route、EditorHost revision binding、显式 server replacement 和全文 replay； - 独立 zeta-language-server-catalog、Rust built-in identity、frozen PATH resolution 和 policy gate； - LanguageServerProviderRegistry、ManagedNodeRuntime 和 verified CSS package provider； - Desktop 通过 allowlisted ZETAELECTRONRUNASNODEPATH 复用 exact Electron executable，且只在 language-server child 的 clean environment 中设置 ELECTRONRUNASNODE=1； - standalone/headless package 保留锁定 Node.js runtime 与 license 作为非 Electron 回退； - App Server 将 activation-confirmed provider definition 与显式启用的 built-in catalog definition 合并后交给同一 supervisor； - 独立 zeta-language-service supervisor、显式 enablement、resolved definition 校验、generation gate； - Native 文档 open/change/save/close、workspace replacement 与 event-loop adapter； - zeta-config mode/path schema、App Server typed mutation/config notification，以及三个内置 server 的 Native Settings selector 与独立 draft； - config generation gate、server replacement 与全部打开文档 replay； - diagnostics freshness 校验、UTF-8/UTF-16 position conversion 和 product-neutral event； - Native revision gate、CodeEditor severity squiggle/soft-wrap projection 与 hover detail； - transport-close detection、断连 route retirement、server epoch gate、有限指数退避与 crash-loop state； - Native Settings 的 Starting/Ready/BackingOff/CrashLoop/Failed/Stopped runtime projection； - hover/completion/resolve/command/navigation/hierarchy/rename/code-action/formatting/signature-help/inlay-hints/linked-editing product facade、 capability gate、三重 freshness gate、Renderer latest-request gate 与可见交互； - 语言请求 in-flight task registry、显式 cancellation，以及 content-free cold/warm latency/result-count/ terminal-outcome metrics sink； - Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors、Folding 的 revision-bound facade 与 Desktop Editor provider； - document/workspace pull diagnostics 的 capability gate、report projection、App Server route 与 Desktop Problems 数据接入； - 多 definition target 使用现有 Peek 列表选择，单目标直接导航； - Rust、JSON/JSONC、Shell 三项 built-in catalog definitions； - verified package staging、SHA-256、side-by-side update 与 receipt； - 通用 Language Marketplace TUF consumer、delegated publisher 校验、有界 ZIP extraction、Marketplace tree digest、consumer SemVer compatibility 与 stable error mapping； - Desktop Settings/Languages exact package confirmation，App Server language/marketplace/list / install RPC，以及 profile activation receipt 启动重建和安装后的热 registry replacement； - in-memory protocol vertical tests。 计划 1. 把 workspace trust 的明确结果映射为 catalog execution policy。 2. 增加 pull diagnostics 的 result-id cache、partial-result progress 与 workspace/diagnostic/refresh`。 3. 为其他 language-server family 增加 schema 2 runtime metadata 和 catalog provider adapter，并为 未激活 version 增加独立 garbage collection policy。 4. 先评估已记录的重复请求、冷暖延迟和取消率；只有收益显著时才设计 session navigation cache。 cache 必须绑定 server incarnation、configuration/service generation、request shape 和 document revision；当前没有安装或持久化 navigation cache。 潜在方向 远程 workspace 或共享 daemon 出现后，可以把 server execution 放到 App Server 后方；前置条件是 定义 workspace authority、document content transport、cancellation、incarnation、privacy 和 disconnect recovery。当前本地 editor request 不承担这套远程成本。 长期不变量是：协议运行时不拥有编辑器文档，编辑器组件不解析 LSP，宿主不把旧文档版本的结果 应用到当前 revision，未实现的 server capability 不对外宣称。"
  },
  {
    "slug": "editor-extensions",
    "sourcePath": "docs/editor-extensions.md",
    "title": "编辑器扩展系统",
    "description": "Zeta 有两种不能互相冒充的 Editor Extension。声明式扩展读取主机限定目录中的 package.json、language/TextMate/snippet/theme/debugger 资源，不执行包内代码；可执行扩展则必须先 进入 Plugin 的 immutable package 与 enable/grant authority，再由每扩展独立进…",
    "group": "界面与体验",
    "markdown": "> 本文是 Zeta 编辑器扩展的跨层架构权威文档，明确区分两条当前边界：声明式静态 Editor\n> Extension 与 Plugin 授权的 Zeta 原生可执行 Host v1。静态目录实现见\n> [`zeta-rs/extensions/README.md`](../zeta-rs/extensions/README.md)，声明式 Workbench 投影见\n> [`desktop/src/zeta/workbench/services/extensions/README.md`](../desktop/src/zeta/workbench/services/extensions/README.md)，\n> 可执行进程与 RPC 实现见\n> [`zeta-rs/editor-extension-host/README.md`](../zeta-rs/editor-extension-host/README.md)。Plugin 安装、版本、\n> enable/grant 和 package authority 由 [`plugins.md`](plugins.md) 维护。\n\n## 快速理解\n\nZeta 有两种不能互相冒充的 Editor Extension。声明式扩展读取主机限定目录中的\n`package.json`、language/TextMate/snippet/theme/debugger 资源，不执行包内代码；可执行扩展则必须先\n进入 Plugin 的 immutable package 与 enable/grant authority，再由每扩展独立进程通过 Zeta Host RPC\nv1 注册窄 provider。两者可以最终服务同一个 Workbench，但 manifest、身份、信任、刷新和失败语义\n始终分开。\n\n| 用户或产品场景 | 当前行为 | 明确不会发生 |\n| --- | --- | --- |\n| 打开受支持的源码文件 | 使用内置声明式扩展提供的语言关联、配置、grammar 和 snippet | 不下载扩展，不执行 manifest 中的脚本 |\n| 主机配置静态用户扩展目录 | 扫描该目录的直接子包，非法包产生诊断 | Workspace 或 Renderer 不能提交任意扩展根 |\n| Marketplace Plugin 声明 `declarativeExtensions[]` | 仅 effective exact package 的静态目录进入 catalog | 安装本身不激活，也不获得代码执行权限 |\n| 用户静态包与内置包使用同一扩展 ID | 内置包优先，用户包被报告为重复项 | 可变用户文件不能静默替换产品资源 |\n| Plugin 声明可执行 Editor Extension | 校验 exact entrypoint、process permission、Host API v1、activation event 和 capability ceiling | 安装或 manifest 校验本身不会启动进程 |\n| 已授权可执行扩展启动 | Runtime core 为它创建独立进程，完成版本握手和整批 registration validation | 不把它加载进 App Server 进程，不继承主机环境 |\n| 可执行扩展崩溃 | 清除旧 incarnation 注册，按有界预算重新握手和激活；超限进入 crash loop | 不无限重启，不把旧请求重绑定到新进程 |\n| 调用超时或取消后没有 terminal response | 结果标为 unknown outcome，终止旧 incarnation 后恢复 | 不声称副作用没有发生 |\n| 需要 VS Code Extension API 或 Marketplace compatibility | 当前不支持 | Host RPC v1 不是 Node Extension Host，也不是 VS Code API 子集 |\n| 生产环境没有可实施 hard limits 的 launcher | executable Host capability 必须为 false，失败关闭 | 不允许退化到无 sandbox 的第三方执行 |\n\n当前声明式装载链已接入 App Server 与 Workbench。Plugin executable declaration、独立 Host runtime、\nApp Server broker 以及 Workbench 的 Commands/Language/Tasks/task-backed Testing 窄桥已实现；由于生产\nlauncher 尚未实现，默认产品仍以 capability=false 失败关闭，不能描述为可启用第三方代码的生产路径。\n后续章节分别说明两条流程、所有权、信任与失败边界、完成度及演进。\n\n## 1. 两条装载与激活路径\n\n### 1.1 声明式静态扩展\n\n```mermaid\nflowchart TD\n    package[\"产品根 / Plugin authority exact package / 用户根\"] --> scan[\"zeta-extensions 扫描并冻结包快照\"]\n    scan -->|非法包| diagnostic[\"目录诊断；不注册该包\"]\n    scan --> catalog[\"不可变目录代次与 package digest\"]\n    catalog --> protocol[\"App Server DTO 与有代次约束的资源读取\"]\n    protocol --> parse[\"Workbench 严格解析支持的贡献\"]\n    parse --> prepare[\"准备语言、snippet、theme、grammar、debugger 注册\"]\n    prepare -->|任一步失败| previous[\"保留上一批可用注册并报告失败\"]\n    prepare --> commit[\"在统一事件屏障内提交 Workbench 注册\"]\n    commit --> consumers[\"Aster / TextMate Worker / Debug service\"]\n```\n\n产品构建把仓库根目录 `extensions/` 复制到包内 `zeta-resources/extensions/`。App Server 同时把当前\nPlugin activation snapshot 中的 `declarativeExtensions[]` 解析为 exact immutable package directories。\n`zeta-extensions` 按 built-in → Plugin → profile user 顺序扫描，每个扩展 ID 的第一个有效包获胜。每次\n`Refresh` 或 Plugin activation generation 改变都产生单调递增的目录代次，并把包内 regular-file bytes 冻结为当前\n内存快照。资源读取必须携带该代次，只能读取当前快照；刷新后请求旧代次会得到 generation\nconflict，而不是从已变化的磁盘路径读取内容。\n\nRenderer 不接收主机路径。它先取得目录描述，再以“目录代次 + 扩展 ID + 包内相对路径”请求资源。\nApp Server 把有界 bytes 放入 connection-owned `ResourceStore`，前端通过分块 API 读取并释放临时\n资源。Workbench 最后把声明式贡献投影到各自领域 registry。\n\nWorkbench 启动会保留初次 `AppServerExtensionService.start()` 的 Promise，在扩展激活完成或失败后\n才恢复 working-copy backup 并推进 `AfterRestored`。App Server 连接状态从任一非 `ready` 状态回到\n`ready` 时，组合根调用 `reload()`；该请求复用 single-runner 合并规则。\n\n### 1.2 Plugin 授权的可执行 Host v1\n\n```mermaid\nflowchart TD\n    manifest[\"Plugin editorExtensions declaration\"] --> validate[\"package/digest/entrypoint/process permission validation\"]\n    validate --> authority[\"enable + grant + activation generation + Workspace trust\"]\n    authority --> launcher{\"production enforcing launcher available?\"}\n    launcher -->|no| unavailable[\"capability=false / isolationUnavailable\"]\n    launcher -->|yes| process[\"one isolated process per extension\"]\n    process --> handshake[\"Initialize Host RPC v1\"]\n    handshake --> activate[\"Activate + capability-bounded registration set\"]\n    activate --> broker[\"App Server broker projection\"]\n    broker --> domains[\"Commands / Language / Debug / Tasks / Testing owners\"]\n    process -->|exit or invalid protocol| recovery[\"clear registrations + bounded restart\"]\n    recovery --> handshake\n```\n\nPlugin v1 的 `editorExtensions[]` 指向包内一个可直接启动、自己实现 Zeta Host RPC v1 的程序。Plugin\nauthority 只决定哪一个 immutable package/digest/generation 当前可被调用；它不启动进程。Host adapter\n还必须绑定 active Workspace trust，随后把绝对 executable 交给能够实施 sandbox 与 hard limits 的\n平台 launcher。默认安全策略缺少该 launcher 时失败关闭，不能自动改用可信开发 launcher。\n\n一个 `ExtensionHostSupervisor` 只监管一个扩展程序。它先取得 live activation lease，再 spawn、执行\nInitialize/Activate，最后一次发布整批 registrations。每次 provider invocation 重新取得 lease，并绑定\nrequest ID、process incarnation 和 activation generation。崩溃后的新进程会重新握手和激活；旧\nregistration、pending request 和 lease 不会迁移。\n\nManifest activation events 当前是经过验证并传给 runtime 的 facts；`zeta-editor-extension-host` 不自行\n监听 Workbench 事件或决定何时 lazy start。activation-event matching 必须由 App Server composition\n明确实现并测试，不能因为协议携带该字段就宣称按事件激活已经可用。\n\n## 2. 所有权\n\n| 能力 | 权威所有者 | 不负责 |\n| --- | --- | --- |\n| 内置静态资源源码与上游 provenance | 根目录 `extensions/` | 运行时扫描、Extension API |\n| 静态包扫描、路径/文件类型校验、快照、摘要与目录代次 | `zeta-extensions` | Editor 贡献语义、任意代码执行 |\n| 静态可信根选择和顺序 | App Server 产品组合根 | 由 Renderer 提交任意主机路径 |\n| Plugin 静态目录选择 | `zeta-plugins` activation authority + App Server provider | 解析静态 `package.json`、授予代码执行 |\n| 静态 DTO、connection resource 与错误映射 | App Server / `platform/extensions` adapter | Workbench 领域注册 |\n| 声明式 catalog 与生命周期 | `IExtensionService` / `AppServerExtensionService` | transport DTO、Plugin enable/grant |\n| Plugin package、install/update/rollback、enable/grant 与 activation generation | `zeta-plugins` | 启动进程、Host RPC、provider registry |\n| 可执行进程、Host RPC、incarnation、取消和 crash recovery | `zeta-editor-extension-host` | Plugin discovery、Workspace trust decision、领域 payload |\n| Plugin + Workspace live authority adapter、Host fleet 与客户端 RPC | App Server composition | OS sandbox implementation、Workbench UI |\n| 生产 sandbox、hard resources 与 killable process tree | 注入的 platform `ExtensionHostLauncher` | package enable/grant 或 provider semantics |\n| Host snapshot normalization 与 transport | `platform/extensionHost` adapter | 领域 provider ownership |\n| Host fleet 生命周期和原子 provider 编排 | `IExtensionHostService` implementation | generated DTO 作为 domain API |\n| Renderer Host service 安装与启动阻塞 | Code 产品入口选择的 `workbench/contrib/extensionHost` | 通用 Workbench 或 Academic 产品隐式安装 |\n| Commands、Language、Debug、Tasks、Testing 注册与调用 shape | 各自 Workbench domain owner | package 安装、进程监管 |\n\nFrontend common contract 使用 Workbench 自己的 snapshot/descriptor/failure 类型；generated DTO 和\n资源传输 shape 只存在于运行时 adapter。`src/zeta/base` 不认识扩展、语言、grammar 或 Host RPC。\n\n静态 `package.json` catalog 与 Plugin manifest 之间没有隐式转换。未来即使共享安装 UI，也必须保留\n两种 package identity、authority、generation 和 failure semantics；不能把“静态资源目录可读”转换成\n“允许执行包内程序”。\n\n## 3. 当前支持的声明式贡献\n\n| `package.json` 贡献 | 当前状态 | 当前边界 |\n| --- | --- | --- |\n| `languages` | ✅ | ID、aliases、extensions、filenames、filename patterns、MIME type、first-line pattern |\n| language `configuration` | ✅ | 读取 JSONC 并注册 comments、brackets、indentation、on-enter 等语言配置 |\n| `grammars` | ✅ | root/injection grammar、embedded languages、token types、balanced/unbalanced bracket scopes |\n| `snippets` | ✅ | 有 prefix 的 snippet 投影为 completion provider；file template 进入可查询 template catalog |\n| `themes` | ✅ | 严格解析并注册可选择 Workbench color theme，同时投影活动主题的 TextMate token scope rules |\n| `debuggers` | ✅（窄契约） | 唯一 debugger type 映射到显式 adapter program/args；不提供 VS Code Debug Extension API |\n| `configurationDefaults`、`semanticTokenScopes` | 尚未完成 | 内置 manifest 可包含，但当前 loader 不投影 |\n| JavaScript、LSP server declaration、动态 UI | ❌ | 不执行、不隐式信任 |\n\n内置包当前覆盖 CSS、HTML、JavaScript、JSON/JSONC、Markdown、Python、Rust、Shell、SQL、\nTypeScript、XML、YAML 和四个默认主题文档。Manifest 中的 `%...%` 本地化占位符当前没有 NLS\n解析；未解析占位符使用 theme document 自身的 name 或稳定 manifest ID 回退。\n\nTheme document 当前必须是自包含资源：`include` 会被拒绝，`uiTheme` 只接受 `vs`、`vs-dark`、\n`hc-black` 或 `hc-light`，颜色必须是合法十六进制值，token settings 只允许\n`foreground`、`background` 和受支持的 `fontStyle`。未知 Workbench color token ID 可以保留在\ncatalog 中，但投影为产品主题时会被忽略。\n\n## 4. 可执行 Host v1 的窄契约\n\n### 4.1 清单与激活上限\n\n每个 Plugin `editorExtensions[]` item 必须有唯一 manifest-local ID、唯一 package-relative entrypoint、\n数值 `runtimeApiVersion: 1`、非空且有界的 activation events 和 capabilities。entrypoint 必须是 immutable\npackage 内 regular file，并有完全相同路径的 `process` permission。regular-file 校验不证明当前 OS、\nCPU、ABI、executable bit 或代码签名可运行；launcher 仍需在目标平台失败关闭。\n\nv1 activation event 为 `startup`、`onCommand`、`onLanguage`、`onDemand`、`onDebugType`、\n`onTaskType` 和 `onTestProfile`。`workspaceContains` 当前没有 Workspace-owned bounded scanner，因而\n明确不在 schema 中；扩展程序不得自行扫描工作区来模拟这一 trigger。\n\n### 4.2 RPC 与注册\n\nHost RPC v1 是 newline-delimited strict JSON request/response 协议，不是 JSON-RPC，也没有扩展主动\n发送的 event stream。每个消息都绑定 protocol version、request ID、incarnation 和 activation\ngeneration。未知 request、response kind 不匹配、correlation 不一致、重复/未知 request ID、超限 frame\n或未声明 capability 均使当前 incarnation 失败关闭。\n\n| Registration kind | Runtime v1 ceiling | 产品接入状态 |\n| --- | --- | --- |\n| Command | command ID、title 与 brokered invocation | 已接入；按 registration、incarnation 与 activation generation 调用 |\n| Language Provider | language IDs + operation set | Runtime vocabulary 已实现；Frontend v1 已投影 completion、Parameter Hints、hover、formatting、Inlay Hints、Linked Editing；其余 operation 仍部分接入 |\n| Debug Adapter | debugger type | Runtime contract 已实现；Frontend 当前只保留 snapshot 并报告 unsupported bridge，不启动 DAP session |\n| Task Provider | task type | 已接入；只发布用户可选择的 canonical Task，不自动执行命令 |\n| Test Profile Provider | provider ID、label | 已接入 task-backed profile；不冒充完整 test tree/controller API |\n\n`capabilities` 是注册种类的最大集合，不代表 extension 已注册 provider，也不批准某次调用的副作用。\nApp Server 必须对每个 registration 建立 owner-bound identity，并让领域 owner 定义 operation/payload；\nRenderer 不能把任意 Host RPC method 直接透传给扩展进程。\n\nLanguage Provider 调用会把当前 immutable editor snapshot 的完整 normalized text、version、language ID、\n可选 resource URI 及本次 position/range/options 交给扩展程序；因此 grant 该 capability 必须被产品明确\n解释为“允许 broker 披露当前参与调用的文档内容”，但不等于任意工作区文件读取。传输仍受 512 KiB\npayload ceiling、Workspace/Plugin live authority、activation generation 和 incarnation fence 约束，超限\n请求拒绝。\nFrontend v1 当前只投影 completion、Parameter Hints、hover、formatting、Inlay Hints 和 Linked Editing；\nHost vocabulary 中虽有 definition、references、rename、code action 等 operation，尚无严格 Workbench\ncodec 的 operation 会发布 `unsupportedRegistrationBridge` 并使 aggregate state 降级；同一 registration\n中已有严格 codec 的子集继续保持 active。Parameter Hints 已进入 v1；它不是完整 VS Code Signature\nHelp API。Task Provider\n只能提出用户可选择的任务，实际命令仍由 canonical Task/Terminal 安全边界执行；Test\nProfile Provider 只能引用同一扩展当前 Task Provider 贡献的任务。Debug Adapter 也必须经专用 DAP\nsession seam，不能把任意 executable descriptor 从扩展进程直接交给通用 process API。若这些 broker\n规则不存在，对应 registration 应保持不可用，不能因为 runtime capability 已声明就直接透传。\n\n## 5. 信任、完整性和资源限制\n\n### 5.1 声明式静态资源\n\n主机和 Plugin activation authority 是静态来源的唯一入口。内置根固定优先于 Plugin exact package，\nPlugin 又优先于用户 profile 根；Workspace、Renderer、静态 manifest 和网页内容均不能提交任意绝对路径。目录扫描拒绝 symlink、hard link、special file、越界相对路径和不\n满足 manifest 身份的包。当前限制为：manifest 最大 4 MiB，单文件最大 16 MiB，每包最多 4096 个\nregular files、8192 个 filesystem entries，总 bytes 最大 64 MiB。\n\n目录代次解决“目录描述与资源 bytes 属于同一快照”，package SHA-256 解决“一个完整包快照的内容\n身份”。`manifestSha256` 只校验 transport 暴露的 canonical `manifestJson` bytes；它不能代替完整\npackage digest，原始 `package.json` bytes 已包含在 `packageSha256` 的整包身份中。当前快照只保留\n最新代，旧代资源请求明确失败，不做隐式重绑定。\n\n静态 profile 根当前没有独立 Editor Extension registry、signature、revocation 或 grant authority。由\nMarketplace 分发的静态目录则继承外层 Plugin 的 TUF/digest/revocation 与 enable/grant authority，但\n仍不获得任意代码执行；两种来源都只能贡献第 3 节列出的声明式数据。\n\n### 5.2 可执行进程\n\n可执行扩展必须同时满足三层 gate：Plugin exact package/digest 的 enable + grant authority、active\nWorkspace trust，以及平台 launcher 对 sandbox/hard limits 的实际实施。任一 gate 失效都拒绝新 activation\n和 invocation；App Server 还须取消 connection-owned invocation，并在 disable/update/uninstall 或\nWorkspace 切换时停止旧进程。\n\n默认 process/protocol limits 为 1 MiB frame、512 KiB payload、256 registrations、32 个普通和 8 个\ncontrol in-flight requests、256 KiB stderr、10 秒 startup、30 秒 request、2 秒 cancel grace、5 秒\nshutdown。默认 hard limits 请求 512 MiB memory、300 秒 CPU 和单进程。restart policy 在 60 秒窗口内\n最多允许 5 次，以 100 ms 起步、最高 5 秒指数退避。确切实现和修改义务见 Host crate README。\n\n`TrustedDevelopmentLauncher` 只允许显式可信本地开发。生产第三方执行必须注入能实施所请求隔离的\nlauncher；当前没有这样的 product launcher 时，App Server 对外能力必须为 false，而不是放宽 policy。\n\n## 6. 失败、刷新和恢复语义\n\n### 6.1 声明式刷新\n\nRust 扫描以包为单位失败关闭：一个无效包产生结构化诊断且不进入目录，其他有效包仍可发布。请求\n不安全、缺失、过大或旧代资源分别返回 typed error，不回退到工作区文件系统或磁盘最新内容。\n\nWorkbench 刷新采用 single-runner 队列。同一时间只有一次装载；装载期间的多个刷新请求合并为恰好\n一次 follow-up refresh，所有等待者等待队列 drain。服务销毁后不再提交 in-flight 结果、启动后续刷新\n或发送普通失败事件。\n\nManifest、配置、snippet、theme 和 debugger 解析及注册组成一条失败安全的激活序列；失败时释放\n候选注册并尝试恢复上一批可用状态。提交阶段使用同步事件屏障：各领域 registry 先更新状态，全部\n成功后才依次投递已缓冲事件，因此监听者查询其他 registry 时会看到同一扩展代次。同步提交失败会\n丢弃候选事件并恢复上一批可用状态。TextMate grammar 的 bytes 解析和 catalog materialization 仍由\n`TextMateGrammarService` 拥有；Worker 运行期的后续故障属于 TextMate 自身生命周期。\n\n### 6.2 可执行调用与故障恢复\n\n启动和每次 invocation 都持有 live authority lease。caller cancellation 与 absolute UTC deadline 会发送\n独立 control request；cancel grace 内没有 terminal response 时返回 unknown outcome，终止旧进程并进入\nrecovery。不能把 timeout 映射为“未执行”。connection 断开时 App Server 必须取消该 connection 拥有的\ninvocation，防止后台结果泄漏或孤儿 lease。\n\nHost exit、invalid protocol 或 unknown outcome 会清空旧 registration，终止 process tree，并在 authority\n仍有效且 restart budget 未耗尽时重新 Initialize/Activate。空闲 crash 由 App Server health loop 调用\n`reconcile()` 检测；Runtime core 不自带后台 poller。进入 crash loop 后只发布 terminal failure，不无限\n重启。客户端 snapshot 和 registration replace 必须是整批原子操作，不能让旧/new incarnation provider\n混用。\n\n## 7. 当前实现状态与明确限制\n\n| 子系统 | 状态 | 实现证据或缺口 |\n| --- | --- | --- |\n| 静态 package discovery、snapshot、digest、资源读取 | 已实现 | `zeta-extensions` + App Server extension operations |\n| Plugin 声明式 Extension 分发与 live activation | 已实现 | `declarativeExtensions[]`、dynamic source provider、Workbench Plugin generation refresh |\n| 声明式语言、grammar、snippet、theme、debugger 投影 | 已实现 | `AppServerExtensionService` 与领域 registry tests |\n| Plugin executable declaration、exact process permission、authority | 已实现 | `zeta-plugins` manifest/package/authority tests |\n| Host RPC v1、独立进程监管、取消、配额、restart | 已实现 | `zeta-editor-extension-host` standalone tests |\n| App Server Host fleet、Workspace gate、async invoke/cancel/read | 已实现 | exact operation broker、连接配额/TTL、退役取消与 changed notification |\n| Workbench Commands/Language/Tasks/Testing bridge | 已实现（窄契约） | 原子投影、取消、stale fence 与 last-good 测试；Testing 仅 task-backed profile |\n| Workbench executable Debug bridge | 尚未完成 | registration 可见并产生诊断，但没有异步 Host-broker DAP session seam |\n| 生产第三方 platform launcher | 尚未完成 | 无 launcher 时 capability=false；可信开发 launcher 不计生产支持 |\n| Activation-event-driven lazy start | 尚未完成 | Manifest/协议携带 facts；尚无完整事件匹配调度证据 |\n| VS Code Node Extension API / Marketplace compatibility | 非目标 | Host RPC v1 是独立 Zeta 协议 |\n\n当前不支持 generic Node/WASM loader、扩展主动 event stream、publisher signature/revocation feed、\nper-platform artifact selector、跨重启 invocation 恢复或多个扩展共享一个 Host process。完整 test tree、\n任意 Webview/UI contribution、任意 App Server method registration 和扩展直接 filesystem/network access\n也不属于 v1 provider bridge。\n\n## 8. 计划演进\n\n### 近期\n\n- 为 executable Debug Adapter 建立专用异步 DAP session seam；补齐 definition、references、rename、\n  code action 等尚无严格 codec 的 Language Provider operation；\n- 让 activation events 真正驱动按需启动，避免当前 App Server fleet 的 eager activation；\n- 在安装/更新授权 UI 中展示 Editor Extension capabilities、文档内容披露与 task command 提议权限；\n- 在至少一个产品平台实现并验收 production `ExtensionHostLauncher`，覆盖 sandbox、memory/CPU/process\n  limits、空环境和 process-tree termination；在此之前保持 capability=false。\n\n### 潜在方向\n\n只有真实生态需求与独立 threat model 成立后，才分别评审 extension-originated diagnostics/event、\nWASM component、remote Host、更多 provider kind 或 activation-event lazy scheduling。任何新 wire 行为都\n必须版本化，先有 App Server/domain owner 和背压/权限语义，不能借 v1 unknown field 静默扩展。\n\nNode/VS Code compatibility 若未来立项，仍是独立产品项目：需要 Node runtime、版本化 VS Code API、\n兼容矩阵、Marketplace/package migration 和独立安全模型，不能宣称为 Host RPC v1 的增量开关。\n\n## 9. 长期不变量\n\n- Renderer 永不直接读取主机扩展路径，也不直接访问扩展进程 stdio。\n- 静态扩展资源读取绑定精确目录代次；刷新不能使旧描述读取到新 bytes。\n- 内置静态产品包不能被可变 profile 包以同 ID 静默覆盖。\n- 安装、启用或 manifest validation 都不等于 execution authority；每次 activation/invocation 复核 live\n  Plugin + Workspace gate。\n- 生产第三方扩展必须逐进程隔离并实施 hard limits；缺失实施者时失败关闭。\n- 请求、响应和 registration 同时绑定 extension、incarnation 与 activation generation；恢复不重用旧\n  identity 或 lease。\n- Workbench 消费领域类型；transport DTO 不进入 frontend common service contract。\n- 各 contribution 由语言、TextMate、Debug、Tasks、Testing 等领域 registry 拥有；extension service 只\n  负责编排和生命周期。\n- 声明式 catalog 与 Plugin executable authority 保持概念分离；共享产品 UI 不能合并二者的 manifest\n  或运行时语义。\n\n## 10. 实现证据与验证\n\n静态链与 Host runtime 的实现细节、private symbols 和 modification checklist 分别见三份关联 README。\n跨层修改至少运行：\n\n```text\ncargo test --manifest-path Cargo.toml -p zeta-extensions\ncargo test --manifest-path Cargo.toml -p zeta-plugins\npowershell -NoProfile -ExecutionPolicy Bypass -File zeta-rs/editor-extension-host/check-standalone.ps1\ncorepack pnpm --dir desktop test:extensions\ncorepack pnpm --dir desktop typecheck:extensions\ncorepack pnpm --dir desktop test:unit\ncorepack pnpm --dir desktop test:scripts\nnpm --prefix docs-site run check:docs\n```\n\n`test:extensions` 与 `typecheck:extensions` 覆盖静态链、Host transport/domain projection 与 Workbench\nprovider seams。App Server runtime 当前由 sibling Rust tests 和 Host standalone suite 封住；根 workspace\n仍需在缺失的本地 crate 恢复后补跑完整 `zeta-app-server` package test。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-两条装载与激活路径",
        "title": "1. 两条装载与激活路径"
      },
      {
        "depth": 3,
        "id": "11-声明式静态扩展",
        "title": "1.1 声明式静态扩展"
      },
      {
        "depth": 3,
        "id": "12-plugin-授权的可执行-host-v1",
        "title": "1.2 Plugin 授权的可执行 Host v1"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 2,
        "id": "3-当前支持的声明式贡献",
        "title": "3. 当前支持的声明式贡献"
      },
      {
        "depth": 2,
        "id": "4-可执行-host-v1-的窄契约",
        "title": "4. 可执行 Host v1 的窄契约"
      },
      {
        "depth": 3,
        "id": "41-清单与激活上限",
        "title": "4.1 清单与激活上限"
      },
      {
        "depth": 3,
        "id": "42-rpc-与注册",
        "title": "4.2 RPC 与注册"
      },
      {
        "depth": 2,
        "id": "5-信任完整性和资源限制",
        "title": "5. 信任、完整性和资源限制"
      },
      {
        "depth": 3,
        "id": "51-声明式静态资源",
        "title": "5.1 声明式静态资源"
      },
      {
        "depth": 3,
        "id": "52-可执行进程",
        "title": "5.2 可执行进程"
      },
      {
        "depth": 2,
        "id": "6-失败刷新和恢复语义",
        "title": "6. 失败、刷新和恢复语义"
      },
      {
        "depth": 3,
        "id": "61-声明式刷新",
        "title": "6.1 声明式刷新"
      },
      {
        "depth": 3,
        "id": "62-可执行调用与故障恢复",
        "title": "6.2 可执行调用与故障恢复"
      },
      {
        "depth": 2,
        "id": "7-当前实现状态与明确限制",
        "title": "7. 当前实现状态与明确限制"
      },
      {
        "depth": 2,
        "id": "8-计划演进",
        "title": "8. 计划演进"
      },
      {
        "depth": 3,
        "id": "近期",
        "title": "近期"
      },
      {
        "depth": 3,
        "id": "潜在方向",
        "title": "潜在方向"
      },
      {
        "depth": 2,
        "id": "9-长期不变量",
        "title": "9. 长期不变量"
      },
      {
        "depth": 2,
        "id": "10-实现证据与验证",
        "title": "10. 实现证据与验证"
      }
    ],
    "searchText": " 本文是 Zeta 编辑器扩展的跨层架构权威文档，明确区分两条当前边界：声明式静态 Editor Extension 与 Plugin 授权的 Zeta 原生可执行 Host v1。静态目录实现见 zeta-rs/extensions/README.md，声明式 Workbench 投影见 desktop/src/zeta/workbench/services/extensions/README.md， 可执行进程与 RPC 实现见 zeta-rs/editor-extension-host/README.md。Plugin 安装、版本、 enable/grant 和 package authority 由 plugins.md 维护。 快速理解 Zeta 有两种不能互相冒充的 Editor Extension。声明式扩展读取主机限定目录中的 package.json、language/TextMate/snippet/theme/debugger 资源，不执行包内代码；可执行扩展则必须先 进入 Plugin 的 immutable package 与 enable/grant authority，再由每扩展独立进程通过 Zeta Host RPC v1 注册窄 provider。两者可以最终服务同一个 Workbench，但 manifest、身份、信任、刷新和失败语义 始终分开。 用户或产品场景 当前行为 明确不会发生 --- --- --- 打开受支持的源码文件 使用内置声明式扩展提供的语言关联、配置、grammar 和 snippet 不下载扩展，不执行 manifest 中的脚本 主机配置静态用户扩展目录 扫描该目录的直接子包，非法包产生诊断 Workspace 或 Renderer 不能提交任意扩展根 Marketplace Plugin 声明 declarativeExtensions 仅 effective exact package 的静态目录进入 catalog 安装本身不激活，也不获得代码执行权限 用户静态包与内置包使用同一扩展 ID 内置包优先，用户包被报告为重复项 可变用户文件不能静默替换产品资源 Plugin 声明可执行 Editor Extension 校验 exact entrypoint、process permission、Host API v1、activation event 和 capability ceiling 安装或 manifest 校验本身不会启动进程 已授权可执行扩展启动 Runtime core 为它创建独立进程，完成版本握手和整批 registration validation 不把它加载进 App Server 进程，不继承主机环境 可执行扩展崩溃 清除旧 incarnation 注册，按有界预算重新握手和激活；超限进入 crash loop 不无限重启，不把旧请求重绑定到新进程 调用超时或取消后没有 terminal response 结果标为 unknown outcome，终止旧 incarnation 后恢复 不声称副作用没有发生 需要 VS Code Extension API 或 Marketplace compatibility 当前不支持 Host RPC v1 不是 Node Extension Host，也不是 VS Code API 子集 生产环境没有可实施 hard limits 的 launcher executable Host capability 必须为 false，失败关闭 不允许退化到无 sandbox 的第三方执行 当前声明式装载链已接入 App Server 与 Workbench。Plugin executable declaration、独立 Host runtime、 App Server broker 以及 Workbench 的 Commands/Language/Tasks/task-backed Testing 窄桥已实现；由于生产 launcher 尚未实现，默认产品仍以 capability=false 失败关闭，不能描述为可启用第三方代码的生产路径。 后续章节分别说明两条流程、所有权、信任与失败边界、完成度及演进。 1. 两条装载与激活路径 1.1 声明式静态扩展 ``mermaid flowchart TD package \"产品根 / Plugin authority exact package / 用户根\" -- scan \"zeta-extensions 扫描并冻结包快照\" scan -- 非法包 diagnostic \"目录诊断；不注册该包\" scan -- catalog \"不可变目录代次与 package digest\" catalog -- protocol \"App Server DTO 与有代次约束的资源读取\" protocol -- parse \"Workbench 严格解析支持的贡献\" parse -- prepare \"准备语言、snippet、theme、grammar、debugger 注册\" prepare -- 任一步失败 previous \"保留上一批可用注册并报告失败\" prepare -- commit \"在统一事件屏障内提交 Workbench 注册\" commit -- consumers \"Aster / TextMate Worker / Debug service\" ` 产品构建把仓库根目录 extensions/ 复制到包内 zeta-resources/extensions/。App Server 同时把当前 Plugin activation snapshot 中的 declarativeExtensions 解析为 exact immutable package directories。 zeta-extensions 按 built-in → Plugin → profile user 顺序扫描，每个扩展 ID 的第一个有效包获胜。每次 Refresh 或 Plugin activation generation 改变都产生单调递增的目录代次，并把包内 regular-file bytes 冻结为当前 内存快照。资源读取必须携带该代次，只能读取当前快照；刷新后请求旧代次会得到 generation conflict，而不是从已变化的磁盘路径读取内容。 Renderer 不接收主机路径。它先取得目录描述，再以“目录代次 + 扩展 ID + 包内相对路径”请求资源。 App Server 把有界 bytes 放入 connection-owned ResourceStore，前端通过分块 API 读取并释放临时 资源。Workbench 最后把声明式贡献投影到各自领域 registry。 Workbench 启动会保留初次 AppServerExtensionService.start 的 Promise，在扩展激活完成或失败后 才恢复 working-copy backup 并推进 AfterRestored。App Server 连接状态从任一非 ready 状态回到 ready 时，组合根调用 reload ；该请求复用 single-runner 合并规则。 1.2 Plugin 授权的可执行 Host v1 `mermaid flowchart TD manifest \"Plugin editorExtensions declaration\" -- validate \"package/digest/entrypoint/process permission validation\" validate -- authority \"enable + grant + activation generation + Workspace trust\" authority -- launcher \"production enforcing launcher available?\" launcher -- no unavailable \"capability=false / isolationUnavailable\" launcher -- yes process \"one isolated process per extension\" process -- handshake \"Initialize Host RPC v1\" handshake -- activate \"Activate + capability-bounded registration set\" activate -- broker \"App Server broker projection\" broker -- domains \"Commands / Language / Debug / Tasks / Testing owners\" process -- exit or invalid protocol recovery \"clear registrations + bounded restart\" recovery -- handshake ` Plugin v1 的 editorExtensions 指向包内一个可直接启动、自己实现 Zeta Host RPC v1 的程序。Plugin authority 只决定哪一个 immutable package/digest/generation 当前可被调用；它不启动进程。Host adapter 还必须绑定 active Workspace trust，随后把绝对 executable 交给能够实施 sandbox 与 hard limits 的 平台 launcher。默认安全策略缺少该 launcher 时失败关闭，不能自动改用可信开发 launcher。 一个 ExtensionHostSupervisor 只监管一个扩展程序。它先取得 live activation lease，再 spawn、执行 Initialize/Activate，最后一次发布整批 registrations。每次 provider invocation 重新取得 lease，并绑定 request ID、process incarnation 和 activation generation。崩溃后的新进程会重新握手和激活；旧 registration、pending request 和 lease 不会迁移。 Manifest activation events 当前是经过验证并传给 runtime 的 facts；zeta-editor-extension-host 不自行 监听 Workbench 事件或决定何时 lazy start。activation-event matching 必须由 App Server composition 明确实现并测试，不能因为协议携带该字段就宣称按事件激活已经可用。 2. 所有权 能力 权威所有者 不负责 --- --- --- 内置静态资源源码与上游 provenance 根目录 extensions/ 运行时扫描、Extension API 静态包扫描、路径/文件类型校验、快照、摘要与目录代次 zeta-extensions Editor 贡献语义、任意代码执行 静态可信根选择和顺序 App Server 产品组合根 由 Renderer 提交任意主机路径 Plugin 静态目录选择 zeta-plugins activation authority + App Server provider 解析静态 package.json、授予代码执行 静态 DTO、connection resource 与错误映射 App Server / platform/extensions adapter Workbench 领域注册 声明式 catalog 与生命周期 IExtensionService / AppServerExtensionService transport DTO、Plugin enable/grant Plugin package、install/update/rollback、enable/grant 与 activation generation zeta-plugins 启动进程、Host RPC、provider registry 可执行进程、Host RPC、incarnation、取消和 crash recovery zeta-editor-extension-host Plugin discovery、Workspace trust decision、领域 payload Plugin + Workspace live authority adapter、Host fleet 与客户端 RPC App Server composition OS sandbox implementation、Workbench UI 生产 sandbox、hard resources 与 killable process tree 注入的 platform ExtensionHostLauncher package enable/grant 或 provider semantics Host snapshot normalization 与 transport platform/extensionHost adapter 领域 provider ownership Host fleet 生命周期和原子 provider 编排 IExtensionHostService implementation generated DTO 作为 domain API Renderer Host service 安装与启动阻塞 Code 产品入口选择的 workbench/contrib/extensionHost 通用 Workbench 或 Academic 产品隐式安装 Commands、Language、Debug、Tasks、Testing 注册与调用 shape 各自 Workbench domain owner package 安装、进程监管 Frontend common contract 使用 Workbench 自己的 snapshot/descriptor/failure 类型；generated DTO 和 资源传输 shape 只存在于运行时 adapter。src/zeta/base 不认识扩展、语言、grammar 或 Host RPC。 静态 package.json catalog 与 Plugin manifest 之间没有隐式转换。未来即使共享安装 UI，也必须保留 两种 package identity、authority、generation 和 failure semantics；不能把“静态资源目录可读”转换成 “允许执行包内程序”。 3. 当前支持的声明式贡献 package.json 贡献 当前状态 当前边界 --- --- --- languages ✅ ID、aliases、extensions、filenames、filename patterns、MIME type、first-line pattern language configuration ✅ 读取 JSONC 并注册 comments、brackets、indentation、on-enter 等语言配置 grammars ✅ root/injection grammar、embedded languages、token types、balanced/unbalanced bracket scopes snippets ✅ 有 prefix 的 snippet 投影为 completion provider；file template 进入可查询 template catalog themes ✅ 严格解析并注册可选择 Workbench color theme，同时投影活动主题的 TextMate token scope rules debuggers ✅（窄契约） 唯一 debugger type 映射到显式 adapter program/args；不提供 VS Code Debug Extension API configurationDefaults、semanticTokenScopes 尚未完成 内置 manifest 可包含，但当前 loader 不投影 JavaScript、LSP server declaration、动态 UI ❌ 不执行、不隐式信任 内置包当前覆盖 CSS、HTML、JavaScript、JSON/JSONC、Markdown、Python、Rust、Shell、SQL、 TypeScript、XML、YAML 和四个默认主题文档。Manifest 中的 %...% 本地化占位符当前没有 NLS 解析；未解析占位符使用 theme document 自身的 name 或稳定 manifest ID 回退。 Theme document 当前必须是自包含资源：include 会被拒绝，uiTheme 只接受 vs、vs-dark、 hc-black 或 hc-light，颜色必须是合法十六进制值，token settings 只允许 foreground、background 和受支持的 fontStyle。未知 Workbench color token ID 可以保留在 catalog 中，但投影为产品主题时会被忽略。 4. 可执行 Host v1 的窄契约 4.1 清单与激活上限 每个 Plugin editorExtensions item 必须有唯一 manifest-local ID、唯一 package-relative entrypoint、 数值 runtimeApiVersion: 1、非空且有界的 activation events 和 capabilities。entrypoint 必须是 immutable package 内 regular file，并有完全相同路径的 process permission。regular-file 校验不证明当前 OS、 CPU、ABI、executable bit 或代码签名可运行；launcher 仍需在目标平台失败关闭。 v1 activation event 为 startup、onCommand、onLanguage、onDemand、onDebugType、 onTaskType 和 onTestProfile。workspaceContains 当前没有 Workspace-owned bounded scanner，因而 明确不在 schema 中；扩展程序不得自行扫描工作区来模拟这一 trigger。 4.2 RPC 与注册 Host RPC v1 是 newline-delimited strict JSON request/response 协议，不是 JSON-RPC，也没有扩展主动 发送的 event stream。每个消息都绑定 protocol version、request ID、incarnation 和 activation generation。未知 request、response kind 不匹配、correlation 不一致、重复/未知 request ID、超限 frame 或未声明 capability 均使当前 incarnation 失败关闭。 Registration kind Runtime v1 ceiling 产品接入状态 --- --- --- Command command ID、title 与 brokered invocation 已接入；按 registration、incarnation 与 activation generation 调用 Language Provider language IDs + operation set Runtime vocabulary 已实现；Frontend v1 已投影 completion、Parameter Hints、hover、formatting、Inlay Hints、Linked Editing；其余 operation 仍部分接入 Debug Adapter debugger type Runtime contract 已实现；Frontend 当前只保留 snapshot 并报告 unsupported bridge，不启动 DAP session Task Provider task type 已接入；只发布用户可选择的 canonical Task，不自动执行命令 Test Profile Provider provider ID、label 已接入 task-backed profile；不冒充完整 test tree/controller API capabilities 是注册种类的最大集合，不代表 extension 已注册 provider，也不批准某次调用的副作用。 App Server 必须对每个 registration 建立 owner-bound identity，并让领域 owner 定义 operation/payload； Renderer 不能把任意 Host RPC method 直接透传给扩展进程。 Language Provider 调用会把当前 immutable editor snapshot 的完整 normalized text、version、language ID、 可选 resource URI 及本次 position/range/options 交给扩展程序；因此 grant 该 capability 必须被产品明确 解释为“允许 broker 披露当前参与调用的文档内容”，但不等于任意工作区文件读取。传输仍受 512 KiB payload ceiling、Workspace/Plugin live authority、activation generation 和 incarnation fence 约束，超限 请求拒绝。 Frontend v1 当前只投影 completion、Parameter Hints、hover、formatting、Inlay Hints 和 Linked Editing； Host vocabulary 中虽有 definition、references、rename、code action 等 operation，尚无严格 Workbench codec 的 operation 会发布 unsupportedRegistrationBridge 并使 aggregate state 降级；同一 registration 中已有严格 codec 的子集继续保持 active。Parameter Hints 已进入 v1；它不是完整 VS Code Signature Help API。Task Provider 只能提出用户可选择的任务，实际命令仍由 canonical Task/Terminal 安全边界执行；Test Profile Provider 只能引用同一扩展当前 Task Provider 贡献的任务。Debug Adapter 也必须经专用 DAP session seam，不能把任意 executable descriptor 从扩展进程直接交给通用 process API。若这些 broker 规则不存在，对应 registration 应保持不可用，不能因为 runtime capability 已声明就直接透传。 5. 信任、完整性和资源限制 5.1 声明式静态资源 主机和 Plugin activation authority 是静态来源的唯一入口。内置根固定优先于 Plugin exact package， Plugin 又优先于用户 profile 根；Workspace、Renderer、静态 manifest 和网页内容均不能提交任意绝对路径。目录扫描拒绝 symlink、hard link、special file、越界相对路径和不 满足 manifest 身份的包。当前限制为：manifest 最大 4 MiB，单文件最大 16 MiB，每包最多 4096 个 regular files、8192 个 filesystem entries，总 bytes 最大 64 MiB。 目录代次解决“目录描述与资源 bytes 属于同一快照”，package SHA-256 解决“一个完整包快照的内容 身份”。manifestSha256 只校验 transport 暴露的 canonical manifestJson bytes；它不能代替完整 package digest，原始 package.json bytes 已包含在 packageSha256 的整包身份中。当前快照只保留 最新代，旧代资源请求明确失败，不做隐式重绑定。 静态 profile 根当前没有独立 Editor Extension registry、signature、revocation 或 grant authority。由 Marketplace 分发的静态目录则继承外层 Plugin 的 TUF/digest/revocation 与 enable/grant authority，但 仍不获得任意代码执行；两种来源都只能贡献第 3 节列出的声明式数据。 5.2 可执行进程 可执行扩展必须同时满足三层 gate：Plugin exact package/digest 的 enable + grant authority、active Workspace trust，以及平台 launcher 对 sandbox/hard limits 的实际实施。任一 gate 失效都拒绝新 activation 和 invocation；App Server 还须取消 connection-owned invocation，并在 disable/update/uninstall 或 Workspace 切换时停止旧进程。 默认 process/protocol limits 为 1 MiB frame、512 KiB payload、256 registrations、32 个普通和 8 个 control in-flight requests、256 KiB stderr、10 秒 startup、30 秒 request、2 秒 cancel grace、5 秒 shutdown。默认 hard limits 请求 512 MiB memory、300 秒 CPU 和单进程。restart policy 在 60 秒窗口内 最多允许 5 次，以 100 ms 起步、最高 5 秒指数退避。确切实现和修改义务见 Host crate README。 TrustedDevelopmentLauncher 只允许显式可信本地开发。生产第三方执行必须注入能实施所请求隔离的 launcher；当前没有这样的 product launcher 时，App Server 对外能力必须为 false，而不是放宽 policy。 6. 失败、刷新和恢复语义 6.1 声明式刷新 Rust 扫描以包为单位失败关闭：一个无效包产生结构化诊断且不进入目录，其他有效包仍可发布。请求 不安全、缺失、过大或旧代资源分别返回 typed error，不回退到工作区文件系统或磁盘最新内容。 Workbench 刷新采用 single-runner 队列。同一时间只有一次装载；装载期间的多个刷新请求合并为恰好 一次 follow-up refresh，所有等待者等待队列 drain。服务销毁后不再提交 in-flight 结果、启动后续刷新 或发送普通失败事件。 Manifest、配置、snippet、theme 和 debugger 解析及注册组成一条失败安全的激活序列；失败时释放 候选注册并尝试恢复上一批可用状态。提交阶段使用同步事件屏障：各领域 registry 先更新状态，全部 成功后才依次投递已缓冲事件，因此监听者查询其他 registry 时会看到同一扩展代次。同步提交失败会 丢弃候选事件并恢复上一批可用状态。Tex"
  },
  {
    "slug": "editor-architecture",
    "sourcePath": "docs/editor-architecture.md",
    "title": "Aster：多内核、可装配能力与产品边界",
    "description": "Aster 是 Zeta 唯一的可组装编辑器内核，源码保持在一个扁平的 editor 领域模块中。它包含两个独立的同步权威：行式文本 TextModel 和结构化文档 DocumentModel。两者共同使用 VS Code 风格的 common / browser / contrib / test 分区；Code 与 Academic 不拥有第二份 editor 源码，…",
    "group": "界面与体验",
    "markdown": "> 本文是跨 editor、document、language、browser view、Workbench 和过渡 adapter 的 canonical 架构文档。扁平模块的实现和修改契约见 [`desktop/src/zeta/editor/README.md`](../desktop/src/zeta/editor/README.md)；行式文本与结构化文档 engine 的详细契约分别见 [`text-engine.md`](../desktop/src/zeta/editor/text-engine.md) 和 [`document-engine.md`](../desktop/src/zeta/editor/document-engine.md)。\n\n## 快速理解\n\nAster 是 Zeta 唯一的可组装编辑器内核，源码保持在一个扁平的 `editor` 领域模块中。它包含两个独立的同步权威：行式文本 `TextModel` 和结构化文档 `DocumentModel`。两者共同使用 VS Code 风格的 `common / browser / contrib / test` 分区；Code 与 Academic 不拥有第二份 editor 源码，而是在产品入口静态加载不同的 contribution bundle。\n\n| 使用场景 | 产品加载入口 | 编辑能力 |\n| --- | --- | --- |\n| Code | `editor.code.all.ts` + `workbench/contrib/codeEditor` | 行式 editor 能力 + code/diff pane/input 与文件服务接线 |\n| Academic | `editor.academic.all.ts` + `workbench/contrib/academic` | 结构化 editor 能力 + Academic profile、document pane 与 embedded code factory |\n| 完整宿主与集成测试 | `editor.all.ts` | 两组 contribution 的并集 |\n| DOM-free 调用 | `editor.api.ts` | 两个 model、schema、transaction、serialization 和坐标 API；不注册 pane |\n\nAster 是整个内核的品牌，不是某一个 engine 的别名。两个 engine 只称为 Text Engine 与 Document Engine；`TextModel` 与 `DocumentModel` 不互相继承，也不通过一个带大量可选方法的万能 model 接口合并。可共享能力必须依赖小而完整的 capability contract；schema validation、transaction、selection mapping、IME commit 和 model lifecycle 仍属于对应 engine，不能变成任意开关 contribution。\n\nAster 是当前唯一的产品 editor runtime。旧 Alpha/Gama editor ID、DOM class、目录和兼容 pane 已删除；架构与测试不得再以兼容为理由重新引入第二套编辑状态。\n\n## 所有权\n\n| 层 | 当前状态 | 责任 |\n| --- | --- | --- |\n| `editor/common` | 两个同步内核与纯投影状态已具备 | `TextModel`、`DocumentModel`、坐标、selection、transaction、history、schema、serialization、cursor、纯 viewport 与版本化语言状态；不得引用 Workbench、Electron 或 generated DTO |\n| `editor/browser` | 两个 engine 的 widget 与 DOM projection 已具备 | code/document/diff widget、DOM input、viewport、editor contribution registry 与 frontend-contract adapter；不得引用 Workbench 或选择产品版本 |\n| `editor/contrib` | 行式与结构化 feature 已按能力组织 | 命令、controller、可移除投影、schema、citation 和 collaboration；不得注册 pane、拥有第二套 model 或读取产品 ID |\n| `editor.*.all.ts` | editor 能力静态装配已具备 | Code、Academic 与完整 editor contribution 清单；不得注册 Workbench pane/input |\n| `workbench/contrib/{codeEditor,documentEditor,academic}` | 产品宿主适配已具备 | pane/input、文件与 working-copy 接线、Academic profile、embedded factory 和产品注册；不得实现编辑事务或视图内部行为 |\n| `workbench/services/textMate` | 已具备 | grammar revision registry、真实 TextMate/Oniguruma runtime、增量行状态缓存、Aster provider/module adapter、版本化 catalog/theme wire、独立 browser Worker、声明式扩展资源、活动主题 token color、embedded language 与 bracket metadata 均已接通 |\n| Document service | 已具备 | `IFileService` 将 App Server `fs/changed` 映射为工作区失效事件，`ITextFileService` 转发；Aster 模型服务提供 dirty、快照保存、显式 revert、CRLF/LF 保留、干净模型重载、脏模型外改状态与 expected-revision/CAS；Workbench 提供 workspace-scoped IndexedDB working-copy 恢复 |\n| Selection/decorations | 基础具备 | selection、实例控制器、tracked range、decoration collection |\n| Language model | 已具备 Code 主路径 | 版本化 token/diagnostic/completion、TextMate 与 parser facts、跨文件 definition/declaration/references/implementation/type definition、Peek、workspace symbols、call/type hierarchy、rename、code action 与有序 WorkspaceEdit 均接通 App Server；更广的语言覆盖由 language-server catalog 演进，不再复制 editor contract |\n| Browser view | 部分具备 | common viewport、虚拟行 DOM、字体行宽、gutter、selection/caret、基础 decoration、hit-test、active-position reveal、至多 160 行的有界 density minimap（WebGL 优先、DOM 回退、click/drag scroll）、diagnostic severity marker、可见行缩进参考线已完成；富交互与主题细化尚未完成 |\n| Input controller | 部分具备 | IME 内核事务、pointer selection、Alt+Shift 列选择、键盘导航、textarea 编辑、completion 键盘/鼠标接受、Ctrl+Space invoke、trigger character 与 incomplete refresh、plain/syntax-marked safe HTML 选区与空选区整行 copy/cut/paste、单个显式文本文件 clipboard paste/drop、前端本地 MIME paste provider 与内置 `text/uri-list`、基础 composition DOM event 已完成；Android/macOS 特化仍未完成 |\n| Accessibility | 部分具备 | editor label、聚焦 textarea 的文本/主选区镜像、multi-selection `aria-description`、completion/listbox ARIA、dialog state 与 cursor/selection/save live-region announcements、forced-colors focus/selection/caret/diagnostic 语义已具备；完整 screen-reader navigation 与平台辅助输入仍待真实平台验证 |\n| Large-file policy | 已具备 | 模型创建时固定判断 20 MiB/30 万行 tokenization、50 MiB synchronization、256M text-unit heap 阈值；保留编辑/滚动/查找/保存，关闭或限制全量后台 token、diagnostic、folding、CodeLens、Inlay Hint、symbol、occurrence 与 bracket colorization |\n| Workbench Editor Part | 已有独立实现 | tabs、pane 生命周期、可见性和产品 contribution |\n\n`src/zeta/base` 继续保持领域无关。编辑器位置、文档版本、selection 和\ndecoration 等身份只能由 `editor` 领域定义，不得为了复用而下沉到\n`base`。\n\n## 环境分层与 base 联动\n\n依赖方向是 `contrib/browser → browser/common → common → base/common`，同时\n`browser → base/browser`。`base` 反向引用 editor 严格禁止；\n这不表示 editor 要回避 base，恰恰相反，通用机制必须优先复用 base。\n\n| Editor 层 | 应复用的 base 能力 | 仍由 editor 定义 |\n| --- | --- | --- |\n| `common` | event、lifecycle、IME realm coordination、cancellation、通用 geometry | 两个 engine 的 position/range、model version、history、selection、transaction、schema、language request/lane/result identity、snapshot version gate 和纯 view-model 语义 |\n| `browser` | DOM lifecycle、通用控件基础、platform/keybinding 状态、通用 layout primitive | code/document viewport、行与节点投影、textarea/input adapter、字体测量、editor ARIA |\n| Workbench host | platform service、context key、commands、configuration、theme | editor pane 接线、document/workspace 绑定和外部区域布局 |\n| Transition adapter | 对应第三方 renderer API | 仅适配，不得反向定义 Zeta common/browser contract |\n\n目录结构遵循运行环境，而不是功能名称倒置嵌套：\n\n```text\neditor/\n  common/{core,model,commands,services,...}\n  browser/{view,widget,services,...}\n  contrib/<feature>/{common,browser}\n  test/{common,browser}\n  editor.code.all.ts\n  editor.academic.all.ts\n  editor.all.ts\n  editor.api.ts\n  editor.main.ts\n```\n\n当前 `editor/common` 已直接复用 `base/common/event`、\n`base/common/lifecycle`、`base/common/ime` 和 `base/common/layout.ISize`。\n其中 composition 开始必须\n遵守共享 `IME.enabled` 状态，避免编辑器与 keybinding/inputbox 各自维护\n互相冲突的输入法开关。`ISize` 只表达通用几何；scroll clamp、visible line\n和 overscan 仍由 Aster 定义，未反向污染 base。\n\n## 当前数据流\n\n当前产品普通文本走以下路径：\n\n```text\nEditorInput → ITextFileService.resolve\n        ↓\nWorkbench BrowserTextResourceStore → Editor BrowserTextModelService → Aster TextModel\n        ↓\nAster language session → Analysis/completion workers\n        ↓\nAster viewport / input\n        ↓ Ctrl/Cmd+S\nITextFileService.save → IFileService.writeFile → App Server\n```\n\n`preferredEditorId` 只是 Workbench 在 code、document、diff、PDF 等现有 pane 间做显式选择的通用机制，不再承载旧 editor 兼容入口。\n\n## 目标数据流\n\n```text\nDocument service\n        ↓ load/save/conflict\nAster TextModel\n        ↓ transaction snapshots\nSelection + Decoration model\n        ↓\nLanguage snapshots ─────→ Language workers\n        ↓\nViewport + Layout model\n        ↓\nInput controller ←──────→ Native DOM renderer\n```\n\n## 长期不变量\n\n- 一个文档在一个 renderer realm 中只有一个权威 `TextModel`。\n- 所有文本变化都经过原子 transaction，版本、事件和 history 同步提交。\n- 文件保存、外部变更与冲突由 Document service 决定，不能由 view 决定。\n- worker 只消费带版本的不可变 snapshot；过期结果不能重新写入新版本。\n- view 可以丢弃并重建，不能因此丢失文本、selection 或 undo history。\n- IME composition 在提交前不是普通编辑事务，取消 composition 不得污染\n  history。\n- accessibility 是 view/input contract 的组成部分，不是渲染完成后的补丁。\n- 旧 Alpha/Gama identity、DOM vocabulary 和 runtime 类型不得重新进入 editor、document 或 Workbench 公共接口。\n\n## 分阶段实施\n\n### Current：文本事务与 Piece Tree 内核\n\n已实现 LF 规范化、UTF-16 坐标、原子非重叠编辑、版本、同步事件和事务级\nundo/redo。存储已经切换到 TypeScript `PieceTreeTextBuffer`：piece 引用不可变\noriginal/add buffer，确定性 treap 在子树维护字符数、换行数和 piece 数。随机\n差分测试将 1,000 次 replace、range read、坐标和行内容与普通 string oracle\n对比。\n\n### Current 1：基础存储硬化\n\n相邻且引用同一段连续 source 的 piece 现在会在编辑边界合并。版本化\nsnapshot 捕获不可变 source segment，不需要在创建时拼接全文，并且在后续\n编辑或模型销毁后仍可读取。undo/redo 同时受事务数量和保存文本单元预算\n约束，超限时从最旧的连续可达历史开始裁剪。\n\n事务提交后会按 retained/live 比例、绝对可回收文本量或 piece 数触发同步\ncompaction。当前文本被重建为新的 immutable original buffer，add buffer\n清空；版本、事件和 history 均不变化。旧 snapshot 继续引用捕获时的 source，\n因此 compaction 不会破坏 worker 读取的一致性。\n\n仓库提供非 gating 的 2 MiB benchmark runner，覆盖构建、分散编辑、坐标\n往返、snapshot range read 和 churn compaction。下一步仍需建立跨平台性能\n预算，并评估超大文档是否需要增量或空闲调度压实。只要 snapshot 仍被外部\n持有，它捕获的旧 source 就按契约继续占用内存，不能把当前 buffer 的统计值\n误认为进程总内存。\n\n### Current 2：Selection 值与模型锚点（部分完成）\n\n`TextSelection` 保存 anchor/active 方向，`TextSelectionSet` 保存稳定的多光标\n顺序和 primary selection。它们是不可变值，不是 `TextModel` 上的全局“当前\n光标”：同一文档可以同时被多个 editor instance 展示，每个实例必须拥有自己\n的 selection state。\n\n`TextModel.trackRange` 提供文档级通用锚点，并要求调用者显式选择四种边界\nstickiness 之一。每次事务完成文本变更后、发送 change event 前，所有锚点会\n按完整事务统一映射；undo/redo 走同一路径。\n\n每个 undo step 会分配稳定的 `transactionId`，同组提交、撤销与重做沿用该\n身份，而每次提交仍产生新 version。`EditorSelectionController` 以此保存一个\neditor instance 的命令前后选区；另一个实例共享同一 `TextModel` 时仍拥有\n独立 selection。命令执行期间若同步 listener 再次修改模型，控制器不会把\n已经过期的 post-selection 写进新版本，而是保留 tracked mapping 的安全结果。\n\n键盘导航已按 Current 15 直接生成 `TextSelectionSet`；文本输入与删除已按\nCurrent 16、clipboard 已按 Current 17 生成统一的 selection-after contract。\nIME composition 的单 selection 内核路径见 Current 5，browser 映射见\nCurrent 18。\n\n### Current 3：Decoration collection\n\n`TextDecorationCollection<TMetadata>` 让 diagnostics、search、diff 等 owner\n在同一个 `TextModel` 上维护彼此独立的装饰集合。集合提供 realm 内唯一且\n稳定的 ID、原子 `replaceAll`、增量 add/update/delete，以及区分集合内容变化\n和锚点范围变化的事件。范围移动继续由 `TrackedRangeStickiness` 决定。\n\ncommon 不解释泛型 metadata，也不定义 CSS class、颜色、z-index 或 DOM 结构。\n这些视觉规则必须由对应 renderer component 或语义 token owner 按\n`docs/ui-styling-ownership.md` 投影。browser renderer 已按 Current 10 消费\ncollection；token/diagnostic 的 snapshot version 存储见 Current 22，向\ndecoration 与文本行的 projection 仍未完成。\n\n### Current 4：结构安全的输入 Undo 合并\n\n命令可显式选择 `CoalesceTyping`、`CoalesceBackspace` 或 `CoalesceDelete`。\n控制器为同模式的连续命令复用 nominal `TextEditHistoryGroup`；只有每个光标\n都继续执行同方向、相邻且非空的操作时，`TextModel` 才复用同一\n`transactionId` 并合并 inverse edits。多光标前序编辑导致的后续 offset\nshift 会在合并时累计修正。\n\nTyping group 可以从替换单个或多个 selection 开始，随后继续普通插入或\n向前覆盖替换；合并后的 inverse edits 同时删除整段新输入并恢复最初被替换\n的文本。每次合并都会重新计算 history text-unit 占用，不能绕过内存预算。\n\n同一事务删除相邻区间时，模型会先合并汇聚到同一 offset 的 inverse\ninsertions，保证 undo script 没有共享起点歧义。若跨事务多光标删除会使\n不同光标的 inverse insertions 汇聚，模型拒绝合并并保留新的 undo step。\n\n实现不会保存全文 snapshot，也不会把无法证明等价的 edit script 强行折叠。\n显式 selection 变化、undo/redo、外部编辑、非相邻操作和模式变化都会形成\n新的 undo step。输入层也可调用 `pushUndoStop()` 切断当前组；该操作本身\n不会产生空历史项。\n\n### Current 5：IME composition 内核事务\n\n`EditorSelectionController.beginComposition()` 为当前单 selection 建立受保护的\nhistory revision。每次 `update` 都完整替换上一版 provisional text，产生可观察\n的 model version，但沿用同一 `transactionId` 和最初的 inverse edit。\n`commit` 最终只保留一个 selection-aware undo step；`cancel` 恢复原文本和\n原 selection，并消费该历史项，不生成 redo。\n\n受保护 revision 在关闭前不会被 history budget 驱逐，因此即使预算为零也能\n无损取消；commit 后立即重新执行预算裁剪。最终 composition text 与原文本\n相同时不会保留空 undo step。普通 command、selection 变化、undo/redo 在\ncomposition 活跃期间被拒绝，直接发生的外部 model edit 会使 session 失效。\n\nDOM 无关的内核生命周期只支持单 selection。browser 已按 Current 18 接入\n基础 `compositionstart`、`compositionupdate`、`compositionend` 和候选窗\nanchor；Android diff、macOS 长按及其他平台差异仍未完成。\n\n### Current 6：固定行高 viewport 内核\n\n`EditorViewportModel` 已在 `editor/common` 中建立 DOM 无关的 view-model\n边界。browser 层负责测量并输入 viewport size、content width 和 line height；\ncommon 层负责内容尺寸、横纵滚动约束、可见行范围和 overscan render range。\n输出 layout 是不可变快照并携带 `TextModel.version`，因此即使一次编辑没有\n改变行数，renderer 仍能按版本丢弃旧投影。resize 与 line-height 更新尽量\n保持原 fractional top-line anchor，模型收缩则把 scroll position 约束回新的\n内容边界。\n\ncommon 契约明确限定为固定行高，不包含软换行、折叠、inline widget、字体\n测量或 DOM。`editor/browser` 已按 Current 7 消费这个 contract；浏览器职责\n没有进入 common，也没有下沉进 base。\n\n### Current 7：只读虚拟行 browser projection\n\n`EditorViewport` 已把 common viewport 接到原生 DOM。它创建并拥有\n`.aster-editor` scroll host、content extent 和 overscanned line layer；\n滚动窗口重叠的行按 line index 复用 DOM identity，模型版本变化同步更新当前\nrender range，文档收缩则把 common 与 DOM 的滚动坐标一并约束回有效范围。\n模型文本通过 `textContent` 投影，不作为 HTML 解析。\n\n该组件复用 `base/browser/dom`、`base/browser/geometry` 和\n`base/common/lifecycle`，内部 DOM 与 focus 样式由组件自己的 CSS 拥有。\nWorkbench host 只能负责根节点外部尺寸，不能穿透覆盖行节点。当时它只是固定\n行高只读 renderer；后续 Current 18、20、26 与 33 已分别补齐输入、clipboard、\nsoft wrap 与基础 accessibility projection。\n\n### Current 8：字体度量与增量横向宽度\n\n`AsterDomTextMeasurer` 从 line layer computed style 解析 font、letter spacing、\ntab size 和左右 padding。普通文本段由 Canvas 按当前字体 shaping 和 fallback\n测量，tab 按实测 space glyph 推进到下一个 stop。Canvas 不可用时使用\nfont-size 派生的 fallback advance；测试和未来专用字体引擎通过小型\n`TextMeasurer` contract 注入。\n\n`AsterLineWidthIndex` 首先同步测量一个有界行片段，之后通过可取消的 idle\n切片完成其余非换行行；未测量期间最大值只作为下界，完成后恢复精确值。稳定\n后它按 `TextModelChange` 的旧行范围合并同一行多处编辑，只重测事务影响的新\n行组。宽度计数集合维护当前最大值，最长行缩短时无需全文重测即可收缩 content\nwidth 并夹紧横向滚动。字体加载完成或显式 `refreshFontMetrics()` 会以同一策略\n重建，因为此时每个缓存值都可能失效。随机差分测试覆盖 400 次多区间事务、\n换行变化和 undo/redo，并与逐次全文扫描结果比较。\n\n### Current 9：Gutter 与 selection/caret projection\n\n每个虚拟行现在由 sticky line-number gutter、文本节点和 overlay 组成。gutter\n宽度按当前总行数的数字位数测量，并计入横向 content width；滚动复用行节点\n时，行号、文本和 overlay 一起按 line index 更新。组件通过稳定的 `.active`\n类投影 primary selection 的活动行，状态样式由 Aster 自己的 CSS 拥有，\nWorkbench 不通过深层 selector 覆盖。\n\n`EditorViewport` 可观察一个调用方持有的\n`EditorSelectionController`。`createAsterSelectionGeometry` 保留\nanchor/active 方向、多 selection 顺序和 primary 身份，只为当前 overscan\n范围生成矩形。水平坐标复用当前 `TextMeasurer` 测量行前缀；被选中的\n换行符占一个实测 space cell，因此终点位于下一行 column 0 时仍有明确视觉\n范围。overlay 在主 caret 上投影 `.primary` 身份，为后续 presentation 保留\n稳定状态；CSS 不使用 ARIA attribute 作为视觉 selector。\n\n这一步本身只消费已有 selection 状态，不产生输入；pointer policy 见\nCurrent 12–15，普通文本输入见 Current 16，clipboard 见 Current 17，\ncomposition 见 Current 18。caret blink 与完整 screen-reader contract 仍未\n实现；销毁 view 只释放自己的 controller listener，不销毁共享 controller\n或 `TextModel`。\n\n### Current 10：Decoration presentation\n\n`createAsterDecorationSource<TMetadata>` 把一个调用方持有的\n`TextDecorationCollection` 适配为 browser presentation。resolver 显式选择\n`SearchMatch`、四种 diagnostic underline 或不投影；因此 common\nmetadata 继续 opaque，browser 也不接受任意 caller CSS class。对应 CSS 由\nAster component 自己拥有，并消费已有 search、error、warning 语义 token。\n\nViewport 可同时观察多个 source，只为当前 overscan 范围创建 decoration\nrectangle。selection 与 decoration 共用 `createAsterRangeRectangles`，统一\nend-exclusive 多行范围、换行符可见宽度、字体前缀测量和 clipping 规则。\ncollection 的 content/range event 会同步重建可见 decoration；文本事务移动\ntracked range 后，DOM 继续使用同一 decoration ID。\n\n每个 source 的 resolved snapshot 会缓存到下一次 collection event，因此滚动\n不会重复调用 caller metadata resolver。`AsterDecorationLineIndex` 将这些 snapshot\n建为按 logical-line interval 查询的不可变树；layout 仅取与当前 rendered logical\nspan 相交的 decoration 后再做 visual clipping。索引不拥有 collection、metadata\nresolver 或 geometry，仅消除滚动期间对完整诊断/search 集合的重复扫描。\n\nViewport 只拥有 source event registration，销毁时不销毁 source、collection\n或共享 `TextModel`。`AsterDiagnosticHoverController` 只消费 marker 当前的\n诊断文本并在 scroll 时关闭；`EditorViewport` 的 overview ruler 与 document\nminimap 都聚合每行最高 severity，且不在滚动时重扫 diagnostics。当前 presentation 还不包含\n诊断版本拒绝；这些不能从已有矩形投影推断为完成。\n\n### Current 11：Pointer hit testing\n\n`EditorViewport.getTargetAtClientPoint` 接受与 PointerEvent 结构兼容的\n`clientX/clientY`，结合 root bounds、固定行高、sticky gutter 和当前横纵\nscroll，返回 `Gutter`、`Text`、`EmptyContent` 或 `AfterLines`。每个结果都\n携带合法 `TextPosition`，但查询本身不 focus、不 capture pointer，也不修改\n`EditorSelectionController`；输入策略仍由后续 controller 拥有。\n\n文本横向命中使用相邻 caret midpoint。前缀宽度复用 `TextMeasurer`，\n因此 tab stop 与当前 selection/decoration/line-width 投影一致。\n`Intl.Segmenter` 可用时只返回 grapheme boundary，不会把 emoji 或组合字符\n命中到任意 UTF-16 内部；fallback 至少保持 Unicode code-point boundary。\n空行、行前 padding、行尾外侧和最后一行下方拥有显式 target kind，不要求\n调用方从 column 猜测语义。\n\n固定行高、无 layout 的测试环境和普通 LTR 文本继续使用 `TextMeasurer`。\n当 viewport 为 `auto`/`rtl` 且浏览器提供布局时，`domTextGeometry.ts` 把跨\nsemantic-token span 的 UTF-16 offset 映射到 DOM `Range`：selection/caret、\ndecoration、composition anchor、pointer hit-test 与 wrapped vertical navigation\n均改用浏览器视觉坐标。缺少 Range layout 时确定性回退到度量路径。inline advance\ndecoration 与原生 browser-driven wrapping 尚未完成；pointer selection policy 见\nCurrent 12。\n\n### Current 12：Pointer selection controller\n\n`AsterPointerSelectionController` 把 hit target 转成一个\n`EditorSelectionController` 的显式 selection policy。主键点击放置单 caret，\nShift-click 保留原 primary anchor；字符拖动持续更新 active。gutter 点击和\n拖动按整行选择，存在下一行时把 LF 包含在 end-exclusive range 中，向上拖动\n保留 backward direction；gutter Shift-click 把原 anchor 扩展到目标行边界。\n\nbrowser 提供的双击计数按 `common/getWordSelectionRange` 的完整 segment\n选择；拖动继续按词扩展并保持正反方向。三击内容按整行选择并沿用 gutter 的\n行拖动规则。Shift-double/triple click 保留原 primary anchor，再分别扩展到\n目标词或目标行边界。默认词边界优先使用 `Intl.Segmenter`，word-like 文本、\n连续空白与标点都形成可选择 segment；fallback 至少保持 Unicode code-point\n边界，行尾命中选择前一个 segment，且所有范围保持单行、UTF-16 end-exclusive。\n\n活动拖动拥有一个 collapsed position 或完整 word 的 `TrackedRange` anchor，\n因此拖动期间发生同步模型事务时不会继续使用 stale `TextPosition`。adapter 同时拥有 window 级\npointermove/up/cancel/blur listener 和 native pointer capture；完成、cancel、\nblur、dispose 或初始化失败都会释放这些临时资源。Viewport、pointer adapter\n和 selection controller 会比较只读 `textModel` 身份，跨文档接线直接拒绝。\n\n该 adapter 不拥有 Viewport、selection controller 或 `TextModel`，也不执行\n文本事务。拖出 viewport 的滚动策略见 Current 13，多光标策略见 Current 14。\n右键 context menu 发生在现有 selection 内时保留 selection，否则先将 primary\nselection 折叠到命中位置再交给宿主菜单。touch 特化仍未实现，因此尚不是完整\npointer input stack。\n\n### Current 13：Pointer drag autoscroll\n\n`EditorViewport.getNearestTargetAtClientPoint` 与严格的\n`getTargetAtClientPoint` 分离：前者只供已开始的 drag 把越界 client point\n夹到最近 viewport 边缘，后者仍对普通查询返回 `undefined`。因此越界拖动会\n先同步扩展到当前可见边缘，不必等待 animation frame。\n\n`AsterPointerAutoScroller` 保存最新 point，并通过\n`base/browser/AnimationFrameScheduler` 逐帧更新。横纵轴速度分别由越界距离\n计算，起速为 240 px/s、上限为 2400 px/s；frame duration 限制在 4–50ms，\n避免后台恢复或异常时间戳造成大跳。每次滚动后重新执行最近命中，所以字符、\n词、整行与 Shift 扩选继续使用 Current 12 的同一 selection policy。\n\npointer 回到 viewport、到达对应 scroll maximum、pointerup、cancel、blur、\ndispose 或新 drag 开始都会取消后续工作并释放 scheduler。该策略不修改\ncommon viewport 或文本模型，也不把 wheel、touch kinetic scrolling 或\n任意 selection reveal policy 推断为已经完成。\n\n### Current 14：Pointer multi-cursor policy\n\n`AsterPointerSelectionControllerOptions.multiCursorModifier` 显式选择\n`Alt`（默认）或 `ControlOrMeta`。只有配置的精确 chord 且未按 Shift 时才新增\nselection；因此 Alt 模式不会吞掉 Ctrl/Meta，ControlOrMeta 模式也不会吞掉\nAlt，带 Shift 的 chord 继续使用 Current 12 的普通扩选语义。\n\n修饰键单击、双击、三击和拖动分别复用已有字符、词和整行 selection policy，\n新 selection 追加为 primary。点击 gesture 起点处已有的 range 会切换移除，\n但最后一个 selection 不会被删除；拖动离开该起点则用新 range 替换它。\n命中另一个相同 range 时只切换 primary，新增 range 与旧 range 重叠时移除旧\nrange，避免后续文本命令生成重叠 edit。\n\n活动 gesture 为原 selection 集合单独创建 temporary tracked ranges，而不是\n保存裸值。因此同步模型事务、autoscroll 和重复 pointermove 后仍保持原顺序、\n方向与 primary 身份。pointerup、cancel、blur、dispose 或新 gesture 会连同\nactive anchor 一起释放这些临时所有权。Alt+Shift primary drag 已生成 common\nmulti-selection 的列选择，不依赖浏览器 DOM selection；box selection 的虚拟空白与\ntouch 多点手势仍未实现。\n\n### Current 15：Keyboard cursor navigation\n\n`common/textSegmentation` 统一拥有 grapheme boundary 与 word segment。\npointer hit-test、double-click word selection 和键盘导航共享这一个 seam；\n`Intl.Segmenter` 不可用时退化到 Unicode code point 与显式 word/whitespace/\nother 分类，避免三份 fallback 漂移。\n\n`navigateEditorCursors` 对每个 selection 执行 `CharacterLeft/Right`、\n`WordLeft/Right`、`LineUp/Down`、`LineStart/End`、\n`DocumentStart/End` 或 `PageUp/Down`。`Move` 与 `Extend` 是显式 mode；\n后者保留 anchor。字符移动不拆 grapheme，词移动只跳 word-like segment 并可\n跨行，纵向移动保留每个 selection 的 preferred UTF-16 column，在短行后可\n恢复；目标列会夹到 grapheme boundary。完全相同的结果会合并并重新映射\nprimary，multi-selection 顺序保持稳定。\n\n`AsterKeyboardNavigationController` 只负责 browser routing。Windows/Linux\n使用 Ctrl+Arrow 词跳转和 Ctrl+Home/End 文档跳转；macOS 使用\nOption+Arrow 词跳转、Command+Left/Right 行边界和\nCommand+Up/Down 文档边界。Shift 统一选择 `Extend`。未知 chord、\nAltGraph、composition keydown 和已被上层处理的事件不会被消费。\n\nPage 命令的行数来自当前固定行高 viewport。成功导航后\n`EditorViewport.revealPosition` 同步调整横纵 scroll，使 primary active\nposition 可见；垂直 reveal 使用完整行，水平 reveal 使用同一\n`TextMeasurer` 的前缀与 space-cell 宽度。外部 selection 变化会清除\npreferred columns。该 controller 不处理字符输入、Backspace/Delete、Enter、\nTab、clipboard、composition DOM event 或平台辅助功能命令。\n\n### Current 16：Textarea editing surface\n\n`common/editCommands` 从调用方的当前 `TextSelectionSet` 构造统一的\n`EditorEditCommand`。普通输入替换每个 selection；Backspace/Delete 优先删除\n已有范围，否则分别删除前一个或后一个 grapheme，行边界则删除 LF 并合并\n两行。输入先规范化为 LF，再计算 transaction 后的 UTF-16 caret offset。\n多 selection 的所有 range 都基于事务前文档，重叠或共享插入起点会在修改\n前拒绝；相邻删除使 caret 汇聚时，完全相同的结果会合并并重映射 primary。\nundo 仍恢复命令前的原多 selection，redo 恢复合并后的结果。\n\n`AsterTextInputController` 创建并拥有一个不可见 textarea。Viewport root\n获得焦点时转移到该 textarea；focus 状态通过稳定的 `.input-focused` 类投影，\n视觉规则由 Aster component CSS 持有。非 composition `beforeinput` 将\n`insertText`、replacement text、Enter、Backspace、Delete、history undo/redo\n映射到 common 命令，普通 Tab 由 keydown 映射为 `\\t`。成功编辑后清空\ntextarea 并 reveal primary active position。它与 viewport、selection\ncontroller 必须引用同一个 `TextModel`，销毁 adapter 只移除自己的 DOM 和\nlistener。\n\n普通 `beforeinput` router 明确忽略 composition input，由组合持有的\ncomposition controller 按 Current 18 处理；clipboard 路径见 Current 17。\ndead key、平台 IME 特化和 screen-reader mirror 仍不属于这个普通输入\nrouter。\n\n### Current 17：Selection clipboard adapter\n\n`getSelectionTexts` 按 `TextSelectionSet` 拥有的稳定顺序读取范围文本。\n`createPasteTextCommand` 把同一文本应用到所有 selection，\n`createDistributedPasteTextCommand` 要求文本数量精确匹配 selection 数量，\n`createCutCommand` 只删除非空范围。三者都生成统一的 selection-after\noffset，并使用 `Isolated` history，确保 paste/cut 不会与前后的连续 typing\n合成一个 undo step。\n\n`AsterClipboardController` 由 textarea input controller 组合持有并监听原生\ncopy/cut/paste。copy 写入可移植的 `text/plain`、转义后的预格式化 `text/html`；\nEditor 的多 selection 复制额外写入版本化 `application/x-aster-editor`\n元数据。粘贴时，合法且数量匹配的元数据逐 selection 分发；外部纯文本或无效\n元数据则把同一文本应用到每个 selection。没有 `text/plain` 的外部 HTML 会在\ninert template 中转换为确定性文本，脚本、样式和 noscript 内容不进入 model。\n所有输入最终仍经过 common command 和权威\n`TextModel`，成功 cut/paste 后清空 textarea 并 reveal primary。\n\nclipboard 输出的换行约定由显式 `AsterClipboardLineEnding` 控制，默认按宿主\nWindows 选择 CRLF，其余平台选择 LF；进入 model 时仍统一规范化为 LF。浏览器\n拒绝自定义 MIME 时保留 plain text，不影响跨应用复制。空 selection 的显式\n策略和整行 round-trip 见 Current 20。\n\n当前已有前端本地 `AsterClipboardPasteProvider`：它只能读取事件期捕获的不可变\n文本 MIME 快照，按声明顺序执行，并且异步结果必须仍匹配原 model version 与\nselection 才能提交。内置 `text/uri-list` provider 会忽略注释行。异步 Clipboard\n在原生 event 没有文本、metadata、受支持文件或匹配 provider 时，于同一用户手势先读取\nrich `text/plain`/`text/html`，再回退 `readText()`；延迟、拒绝、空值或 stale 结果都不能\n修改 model。若 copy/cut event 缺少 `clipboardData`，Async writer 写入同一 portable\nplain/HTML payload，且 cut 仅在写入成功后提交；这些路径不绕开 common command。\n\n### Current 18：Textarea composition adapter\n\n`AsterCompositionController` 把 textarea 的 `compositionstart/update/end`\n映射到一个 `EditorCompositionSession`。开始前要求共享 `IME.enabled` 且只有\n一个 selection；否则取消浏览器事件。每次 update 把完整 provisional string\n替换进同一个受保护 revision。textarea value 与 event data 一致时读取\nselectionStart/End 和方向，换算为规范化文本的相对 offset；无法可靠对应时\n保守地把 caret 放在 composition text 末尾。`compositionend` 更新最终文本\n并 commit，因此整个序列仍只有一个 selection-aware undo step。\n\nEscape 后的 end、blur、adapter disposal 或运行期间 `IME.disable()` 会 cancel，\n无损恢复 composition 前文本与 selection。外部 model edit 即使没有移动\nselection，也会通过直接 model observation 发现内核 session 已失效，清除\nbrowser presentation 并忽略迟到的 update/end。重复 start 不会打开第二个\nrevision。`onDidChange` 对上层发布 composition active 状态。\n\n开始时 Viewport reveal composition 起点，并通过\n`getPositionContentCoordinates` 把隐藏 textarea 移到对应的测量 caret；\n后续 selection、line height 和 layout 变化会重算位置，为原生候选窗提供\ncontent-coordinate anchor。DOM 通过稳定的 `.composing` 和 `.ime-input`\n状态类投影，样式继续由 Aster component 持有。\n\n当前实现面向提供完整 `CompositionEvent.data` 的桌面式事件流。Android\n通常需要从 textarea 前后状态推导 replacement，macOS 长按可能把前一个字符\n带入 composition；iOS 的额外空 end、dead key、IME clause segmentation、\n多 selection IME、候选窗越界回退和更复杂的 composition clipboard UX 仍需\n独立适配与真实平台验证。活跃 session 收到空 end 会提交空 provisional text，\n因此可表达删除原 selection；Escape/blur 才是明确 cancel。已经关闭 session\n后的额外 end 被忽略，iOS 若在仍活跃时发出伪空 end，平台 adapter 必须提供\n更强的识别信号。\n\n### Current 19：Composition range projection\n\n`EditorCompositionSession.currentRange` 只在 session 活跃时返回当前 provisional\ntext 占据的 model range；commit、cancel 或外部失效后读取会拒绝，避免把\nstale offset 暴露给 renderer。\n\n`EditorViewport.setCompositionRange` 把该范围保存为 Viewport 自己拥有的\n临时 `TrackedRange`。因此 model event 先于 browser controller 清理到达时，\n投影仍能读取映射后的合法范围，而不会使用旧 `TextRange` 越界。替换或清除\npresentation 会释放旧 tracked handle，Viewport 销毁也只释放自己的临时\n所有权。\n\ncomposition rectangle 复用 `createAsterRangeRectangles`，与 selection 和\ndecoration 共享 tab prefix measurement、end-exclusive 多行语义、selected\nnewline cell 和 overscan clipping。每个虚拟行拥有独立 composition layer；\n`.composing .aster-editor-composition` 由组件 CSS 投影 underline。commit、\ncancel、blur、IME disable、外部失效和 disposal 都同步清空该 layer。当前空\nprovisional range 不绘制标记，也尚未区分 IME clause 的 primary/secondary\nsegment。\n\n### Current 20：Empty-selection whole-line clipboard\n\n`EditorEmptySelectionClipboardPolicy` 显式选择 `Line` 或 `Ignore`，browser\n默认 `Line`。`getEditorClipboardEntries` 按稳定 selection 顺序生成文本、\nsource range 和 `Selection`/`Line` paste mode。collapsed selection 的整行\n文本总以 LF 结束；存在下一行时 source range 包含后继 LF，最后一行则包含\n前驱 LF，唯一一行只删除自身内容。`Ignore` 保持浏览器原行为，不用含糊\nboolean 表达策略。\n\n`createClipboardCutCommand` 合并相交或相邻 source ranges，所以同一行多个\ncaret、整行范围与普通 selection 重叠时只生成一个无歧义 deletion script。\n每个原 selection 的目标 offset 再统一映射过合并后的删除；完全汇聚的 caret\n按已有规则去重，undo 仍恢复原始多 selection。\n\nEditor clipboard metadata 升级为 version 2，除每个 selection 的文本外还携带\npaste mode。全部 payload 为 `Line` 且目标 selection 都 collapsed 时，\n`createLinePasteCommand` 在各目标行首插入并把 caret 保留在原内容列。同一\n目标行的多个 caret 会按稳定 selection 顺序合并 payload 为一次 insertion，\n不会产生共享起点 edit。混合 line/selection metadata 或非空目标会降级为\n普通逐 selection paste，避免 line mode 隐式绕过目标范围。\n\nplain-text 输出会连续拼接 line entries，避免每个自带 LF 的行之间出现额外\n空行；跨平台输出仍由 `AsterClipboardLineEnding` 转换。当前已经有\ncopy-with-syntax、单个用户提供文本文件、内置 `text/uri-list` 与可注册的本地\nMIME paste provider。若原生 paste event 没有可用文本、metadata、受支持文件或\nprovider，浏览器层在同一用户手势内先读取 rich Async Clipboard，再回退 plain text；\n其异步结果仍必须通过 captured model revision 与完整 selection set 的闸门。copy/cut\nevent 没有 `clipboardData` 时，Async writer 输出等价的 plain/HTML payload，cut 等待成功。\n\n### Current 21：Versioned language request boundary\n\n`editor/common/LanguageRequestCoordinator` 为每个请求捕获不可变\n`TextSnapshot`、单调 request ID、具名 lane 和调用方 payload。同一 lane 是\nlatest-wins；不同 lane 可以共享一个惰性创建的 worker 并发运行。该 adapter\n直接复用标准 `AbortSignal`，模型事务、同 lane 新请求、调用方取消、worker\n重启和 coordinator disposal 都有显式取消原因。\n\nworker value 不直接返回给任意状态写入路径。协调器在同步调用 result applier\n之前同时检查 active request identity 与当前 `TextModel.version`；任何晚到\n结果只返回 `Cancelled` outcome。模型先销毁时同样拒绝应用，而已捕获 snapshot\n仍保持可读。协调器拥有 worker 和 model listener，但不拥有 `TextModel`。\n\n当前 active request 的 worker rejection 会使该实例失效并被释放，同时以\n`WorkerRestarted` 取消其他 lane；下一次请求再惰性创建 worker。失败请求保留\n原始 error，result applier 自身失败不会误伤健康 worker。`LanguageWorkerRequest`\n是 adapter contract 而非可直接 structured-clone 的 wire schema；具体\nbrowser/process worker 必须自己传输文本并保留 request/model version。\n\n定向测试已覆盖同 lane 覆盖、跨 lane 并发、模型变更、调用方取消、worker\n故障重建、两种销毁顺序、晚到结果拒绝和 application failure isolation。\ntoken/diagnostic result store 见 Current 22，completion result/session 见\nCurrent 25，completion full-snapshot wire 见 Current 27；增量同步、\ntoken/diagnostic transport 与自动 retry 在该阶段仍未定义。completion 增量\n同步见 Current 28。\n\n### Current 22：Versioned token and diagnostic result state\n\n`VersionedLanguageResultStore<TResult>` 是 Current 21 同步 application gate\n之后的持久状态 owner。`VersionedLanguageResult` 同时携带 realm-local\n`TextModel` identity 与 version，跨模型接线在 normalization 前直接拒绝，\n不能把两个 version 1 文档混为一体。一个 store 对应一个单调 request-ID\ndomain；低于 high-water mark 的结果返回\n`SupersededRequest`，相同 ID 返回 `DuplicateRequest`。显式 `clear()` 只清内容，\n不重置 high-water，因而旧的同版本 worker 结果不能重新写回。\n\nstore 发出不可变的 `Result`、`ModelChanged` 和 `Cleared` 事件。任何文本事务\n都会整体移除当前 language result，而不是用 tracked range 把旧 token 或\ndiagnostic 映射到新文本。这样 `TextDecorationCollection` 继续保持领域无关，\nlanguage owner 也不能把 stale analysis 伪装成仍然有效的装饰。\n\n`createLanguageTokenStore` 要求 token 是非空、单行、按位置排序且不重叠的\n`TextRange`，token type 是非空 ID，modifier 必须唯一。结果和所有 entry/\nmodifier 数组都会复制并冻结。`createLanguageDiagnosticStore` 允许 diagnostic\n重叠与 collapsed point range，但验证 model range、severity、非空 message、\nfinite/string code 和 source。任一 entry 失败时不会替换既有 result。\n\nnormalizer 完成后 store 会再次检查 model version 和 request high-water，\n所以 validation 期间的同步模型编辑或重入写入不能发布捕获版本。模型先销毁\n时返回 `ModelUnavailable`；store disposal 不拥有模型。diagnostic projection\n见 Current 23；semantic-token 行索引和 browser projection 见 Current 24；\ncompletion result/session 见 Current 25，completion wire schema 见 Current 27；\ntoken/diagnostic worker wire schema 仍未完成。\n\n### Current 23：Versioned diagnostic decoration projection\n\n`LanguageDiagnosticDecorationBridge` 观察一个 typed diagnostic store，并拥有\n`TextDecorationCollection<LanguageDiagnostic>`。当前 result 会在构造时投影，\n后续 result replacement、显式 clear 和 model invalidation 都走 collection\n的原子 `replaceAll`。bridge 不拥有 store 或 `TextModel`，dispose 只释放自己\n的 listener、tracked ranges 和 collection。\n\n该 collection 在 store 之后注册 model listener。文本事务先触发 store 的\n`ModelChanged` clear，再由 bridge 清空 collection；轮到 collection 自己观察\n同一事务时已经没有旧 range，因此不会先发一次虚假的 `Range` movement 再\n清空。测试明确证明 model version 2 只出现一个空 `Content` event。\n\nbrowser 的 `createAsterLanguageDiagnosticSource` 为 Error、Warning、Information\n和 Hint 映射命名 underline，并继续消费 Aster component 的 severity theme token 和\nCSS。Viewport 还会在每个可见 logical line 的首个 visual row 放置该行最高严重级别的\ngutter marker；`AsterDiagnosticHoverController` 会把该 marker 的当前文本呈现为\ncomponent-owned rich hover，并在 scroll 时关闭。Viewport 的非交互 overview ruler\n聚合每行最高 severity，且不改变 content hit-test。它不是可操作 glyph margin。\nViewport 只拥有 source event registration，不拥有 bridge、collection、store 或 model。\nF8/Shift+F8 的 diagnostic navigation 会在选择目标后通过同一 viewport live region\n宣读 severity、source/code（若有）和 message，不依赖视觉 hover。\n\n当前 whole-result replacement 会重新分配 decoration ID；若后续 hover/action\n需要跨同版本 refresh 保持身份，必须增加明确的 diagnostic identity/reconcile\n契约，不能从 message 或数组位置静默猜测。大规模 diagnostics 也仍需行范围\n索引，当前 viewport clipping 会扫描 source 的全部 resolved decorations。\n\n### Current 24：Versioned semantic-token line projection\n\n`LanguageTokenLineIndex` 观察一个\n`VersionedLanguageResultStore<LanguageTokenResult>`，按实际含 token 的行\n建立稀疏索引。每条可见虚拟行可做 O(1) 查询；同 model version 的新 request\n原子替换全部 line buckets，模型事务则沿 store 的 `ModelChanged` 顺序先清空\n索引。index 不拥有 store 或 `TextModel`，也不把旧 token 通过 tracked range\n映射到新文本。\n\nbrowser 的 `createAsterSemanticTokenSource` 把 common token type 显式解析为\nAster 的 `Comment`、`Keyword`、`String`、`Number`、`Regexp`、`Type`、\n`Function`、`Variable` 或 `Operator` presentation。未知 type 默认省略；\n语言 owner 可以提供 resolver，但返回值仍必须属于该命名 enum，worker\n字符串永远不能直接成为 CSS class。source 的全量 `lines` 查询仍可生成并整体验证\n稀疏 snapshot；Viewport 使用 Current 35 的逐行查询，不再解析屏幕外 token。\n\n`projectAsterSemanticTokenLine` 只使用 text node 和 component-owned `span`，\n不使用 HTML 拼接。每次分段都验证非空、排序、不重叠和行内边界，并要求\nfragment 的完整 `textContent` 与模型行逐 UTF-16 code unit 相同。Viewport\n先解析完整目标虚拟窗口，再修改其中的 row；同版本 token refresh\n只重投影当前虚拟窗口，重叠行继续保留 row DOM identity。模型编辑先移除\nstale span，再由既有 model-version reconciliation 投影新文本。Viewport\n只拥有 event registration，不拥有 source、index、store 或 model。\n\n颜色由 `platform/theme/common/colors/editorColors.ts` 以\n`editor.semanticToken.*Foreground` 注册并编译为 design-token 产物；\nAster CSS 只消费这些语义变量。当前 modifiers 交给 resolver 解释，尚未定义\n字体粗细/斜体 contract；持久 relative-range token storage、multi-splice delta、\n嵌入语言优先级和超大 token snapshot 的调度仍未完成。\n\n### Current 25：Versioned completion result、session 与 widget\n\n`createLanguageCompletionStore` 继续复用\n`VersionedLanguageResultStore<LanguageCompletionResult>`。result 显式携带\n触发 `TextPosition`、`isIncomplete` 和 immutable items；每个 item 必须提供\nitem ID、命名 kind、label、明确的 `TextRange` 与 `insertText`。替换范围必须\n停留在触发行且包含触发位置。detail/documentation/filter/sort/preselect 都在\n接受前验证并复制冻结，多个 preselect、重复 provider/item identity、跨行或不包含触发点的范围\n会 failure-atomically 拒绝整个 result。插入文本在 store 边界归一化为 LF。\n\n`LanguageCompletionSessionController` 是 per-editor 状态，不进入共享\n`TextModel`。它只在 selection 是位于 result 触发点的单一 collapsed caret\n时打开；同版本新 request 通过 provider/item identity 保留 focus，默认使用唯一 preselect\n或第一项。next/previous 循环移动，显式 cancel 和 selection change 只关闭\n本地 session，不清除 caller-owned store。store、selection controller 或\nmodel 都不由 session 拥有。\n\n接受 item 时，`createLanguageCompletionAcceptCommand` 只使用 item 的明确\nrange/insertText，生成一个 isolated `EditorEditCommand`，并把 caret 放在\n归一化插入文本末尾。它不从 label、detail 或 DOM 推断编辑。模型事件造成的\nstore invalidation 在 accept 期间由 session 协调，最终只发布一次\n`Accepted` close；undo 通过既有 selection history 同时恢复文本与触发 caret。\n\nbrowser 的 `CompletionWidget` 由 `AsterTextInputController` 可选托管。\nwidget 使用 viewport 的 content coordinates 锚定在触发 caret 下方，以\n`.visible`、`.focused` 状态类同步 listbox/option ARIA。上下键循环 focus，\nEnter/Tab 接受，Escape 本地取消；活跃 session 才截获这些键，其余事件继续\n走既有 navigation/input controllers。鼠标在 mousedown 同一事务中 focus\n并接受，避免重绘后的 detached option 丢失 click。label/detail/kind 全部用\n`textContent`，kind 只映射命名 enum，CSS 由 widget 自己拥有并仅消费现有\ntheme tokens。\n\n当前结果协议和接受链路已具备：deferred resolve/documentation、commit character、\nnon-overlapping additional edits，以及 `$n`/`${n}`/`${n:default}`/`${n|a,b|}` snippet\ntabstop 会话，都通过同一 item contract、Worker transport 与 isolated undo 交付。\nchoice 的镜像 occurrence 通过 `Alt+↑/↓` 原子循环替换；session 为 `${TM_FILENAME}`、\n`${TM_FILENAME_BASE}`、`${TM_DIRECTORY}` 与 `${TM_FILEPATH}` 注入 editor input\n变量，未知变量必须有显式 default。transform 仍不在 Aster 当前受支持 grammar，\n不能被静默降级。provider 生产和触发链见 Current 26；completion Worker wire\ntransport 见 Current 27。\n\n### Current 26：Completion provider registry、host 与触发链\n\n`LanguageCompletionProviderRegistry` 在 common 层保存确定性的注册顺序，\n并以 provider ID、显式 language ID 或 `*` selector、Unicode code-point trigger\ncharacters 形成最小注册契约。注册返回的 disposable 只撤销该项；registry\n不拥有 provider。`Invoke`、`TriggerCharacter` 与 `IncompleteRefresh` 是封闭的\ndiscriminated context，避免用 boolean 或可歧义的 optional 参数表达触发原因。\n\n`LanguageCompletionProviderWorker` 实现既有 `LanguageWorker` 边界，是当前\n进程内 provider host。一次请求先捕获唯一 snapshot 行索引，再并发调用全部\n匹配 provider；每个 provider 的结果都在该 snapshot 上验证。普通 provider\n异常和无效结果按 provider 隔离并上报，取消则直接向 coordinator 传播。合并\n结果保持 registry/item 顺序，以 `providerId + item.id` 作为 identity，只保留\n第一个 preselect，并对 `isIncomplete` 做 OR。`LanguageCompletionService`\n拥有 coordinator、host 和 result store，但不拥有 registry 或 model。\n\nbrowser 输入层通过 `AsterTextInputControllerOptions.completion.requests`\n显式接入 service 和 language ID。Ctrl+Space 发送 `Invoke`；已注册的 trigger\ncharacter 在文本事务完成后，以新的 model version 和 caret 发送请求；当前\n结果为 incomplete 时，普通输入发送 `IncompleteRefresh`。请求只发生在单一\ncollapsed selection 上，失败不会回滚已经成功的文本事务。session 必须精确\n观察该 service 的 result store，避免同模型、不同 request-ID domain 被误接。\n\n当前 incomplete refresh 会重新查询所有匹配 provider，不复用旧版本 complete\nitems，因为旧 range 不能越过 model-version gate。host 隔离在\n`LanguageWorker` 后；具体浏览器 Worker transport 见 Current 27。\n\n### Current 27：Completion Worker wire 与 lexical provider\n\n`LanguageWorkerWireClient` / `LanguageWorkerWireServer` 在 common 层只依赖\n结构化 `LanguageWorkerWirePort`，不依赖 DOM `Worker`、`MessagePort` 或 browser\nevent 类型。协议使用版本化的 request/cancel/result/failure envelope。每次\nrequest 传输 request ID、lane、完整 LF snapshot DTO 和 domain payload；\nserver 会核对 snapshot version、UTF-16 length、line count 与文本，再创建\n只读 `TextSnapshot`。取消在 client 立即结束本地 promise，同时发送 cancel\n使 server 的 `AbortSignal` 中止。remote failure 只传 name/message，不传递\n可执行对象或不可靠的跨 realm prototype。\n\n`languageCompletionWireCodec` 是 completion domain 对 wire 的唯一 codec。\nrenderer 发出的 `TextPosition`/context 被编码为 plain DTO；Worker 返回的\nposition/range/item 也全部是 plain DTO。client 在自己的 realm 重建\n`TextPosition` / `TextRange`，然后再次按原始 captured snapshot 做完整结果\nnormalization。无效 range、kind、identity 或 malformed protocol 不能进入\nresult store。transport terminal failure 会使 pending request 失败；既有\ncoordinator 随即销毁 client，下一次请求创建新的 Worker。\n\nbrowser 的 `createAsterCompletionWorkerFactory` 创建 Vite module Worker、\nDOM Worker port adapter 与 typed wire client。Worker entry 创建自己的 provider\nregistry、`LanguageCompletionProviderWorker` 和 wire server，并注册\n`aster.word` lexical provider。该 provider 在 snapshot 内收集确定性排序的\n同前缀 word，使用完整 active word 作为 replacement range，默认最多返回\n100 项并准确标记 incomplete。\n\n`LanguageCompletionService.workerFactory` 允许调用方显式选择该 transport；\nservice 仍拥有每个 factory result，而 registry/model 仍由调用方拥有。当前\n默认值继续使用进程内 provider host，因此现有调用方不会隐式创建线程。\nAster 产品 composition root 尚未接入该 factory；接入时 renderer registry\n与 Worker registry 的触发元数据同步由 Current 29 负责。\n\nCurrent 27 的第一版 wire 每次请求复制完整 snapshot，是正确但\nO(document length) 的同步基线；Current 28 已用增量文档镜像替代正常路径。\nCurrent 29 已补齐 Worker provider catalog/metadata 与 Worker 预热。token 与\ndiagnostic codec、resolve request 仍未完成。\n\n### Current 28：增量 Worker 文档镜像\n\n`LanguageWorkerModelSynchronizer` 是独立于 `LanguageWorker.run` 的可选能力。\n`LanguageRequestCoordinator` 收到 `TextModelChange` 时先以 `ModelChanged`\n取消所有旧版本 request，再把同一个 immutable change 交给当前支持同步的\nworker。同步失败只丢弃该 worker；下一请求惰性创建新实例并从 full snapshot\n恢复。普通进程内 worker 不需要实现该接口。\n\nwire protocol v2 把 request snapshot 分为 `full` 与 `reference`。client 第一次\n请求或失去连续版本时发送 full；成功把消息交给有序 channel 后记录 mirror\nversion。后续每个 model commit 发送 `sync(previousVersion, modelVersion,\nchanges)`，其中 changes 只包含 pre-transaction UTF-16 range offset/length\n和 LF text。连续同步后的请求只发送 version/length/lineCount reference，\n不再传全文。若 client 观察到版本跳跃，它不猜测缺失事务，而是清空本地 mirror\n状态，使下一请求重新发送 full。\n\nWorker server 用 `LanguageWorkerDocumentMirror` 独占一个 Piece Tree，而不是\n用字符串拼接应用 delta。所有 change 会先验证版本连续、顺序、非重叠、范围\n和 LF，再按 offset 逆序写入，因此验证失败是原子的。每个 request 从 Piece\nTree 捕获独立 immutable snapshot；同步新版本不会改变正在取消中的旧 request\nsnapshot。undo、redo 和多 edit transaction 使用同一 `TextModelChange`\ncontract，无需 transport 特判。\n\n同一 Worker port 是可靠有序 channel，因此 cancel、sync、reference request\n按发送顺序处理，不增加每次 sync acknowledgement。server 拒绝 sync 时会清空\n镜像并发送 terminal `syncFailure`；client poison 后，coordinator 在下一请求\n替换整个 Worker。协议仍保留 full 作为初始化、版本跳跃和 Worker 重建的恢复\n路径。一个 coordinator/worker 只镜像一个 model，当前不引入多文档 identity。\n\n### Current 29：Worker provider catalog 与首次 trigger handshake\n\n`LanguageCompletionProviderMetadata` 从可执行 `provideCompletions` 函数中拆出\nprovider ID、language selectors 和 trigger characters。\n`LanguageCompletionProviderRegistry.providerCatalog` 是带单调 revision 的\nimmutable snapshot；注册和撤销都会在 registry 顺序下发布新 catalog。\ncatalog normalization 会复制冻结所有数组，并拒绝重复 provider ID、language\nID 或 trigger character，因此 renderer 不会直接信任 Worker plain objects。\n\ncatalog 使用独立的 `aster.completion-provider-catalog` side-channel，\n不把 completion 特有 metadata 塞进通用 language-worker envelope。\n`LanguageCompletionCatalogWirePublisher` 观察 Worker 内真正执行 provider 的\nregistry，启动时发送当前 snapshot，之后发送每个 revision。\n`LanguageCompletionCatalogWorkerClient` 同时委托 typed request client 和观察\ncatalog；首次 catalog 有明确 readiness promise，后续 revision 必须严格递增。\nmalformed/stale catalog 会 invalidate 底层 wire client。\n\n自定义 `LanguageCompletionService.workerFactory` 现在会被 service 预热。\nfactory result 若实现 `LanguageCompletionProviderCatalogSource`，service 会切换\n到该远端 source；普通自定义 worker 和默认进程内 host 继续使用 caller-owned\nregistry catalog。`requestTriggerCharacter` 捕获当前 model version，启动或\n重建 Worker，等待首次 catalog，再只对真实匹配 provider 发请求。等待期间模型\n变化会丢弃该 trigger，而不是用旧位置请求新版本。\n\n`AsterTextInputController` 使用该异步 trigger API。若 catalog 证明字符不受\n支持，且先前 result 是 incomplete，它只在 model version、单一 collapsed\nselection 和 caret 都未变化时回退到 `IncompleteRefresh`。Worker terminal\nfailure 或 disposal 会先发布 empty catalog；下一次 trigger 让 coordinator\n创建新 Worker，并重新完成 catalog handshake，旧 metadata 不会继续路由输入。\n\n当前 catalog 描述 Worker 已加载的 provider，但还不能要求 Worker 动态加载\n任意 renderer 函数。后续需要命名 provider module/feature manifest 与受控加载\n协议；函数本身始终不能通过 structured clone 传输。\n\n### Current 30：命名 Worker provider module 与首次请求 barrier\n\n`LanguageCompletionProviderModuleRegistry` 在 Worker realm 内保存调用方拥有的\n命名 module definition，只向 renderer 发布 ID 与单调 revision。\n`LanguageCompletionProviderModuleHost` 按 module ID 串行化 Active/Inactive\n操作。一次 load 返回的 provider batch 先全部校验，再通过\n`LanguageCompletionProviderRegistry.registerMany` 原子注册；重复 ID、无效\nprovider 或 load failure 都不会泄漏部分 registry 状态。deactivate、module\n撤销和 host disposal 会释放整批 provider，并只发布一次 provider catalog\nrevision。\n\n独立的 `aster.completion-provider-modules` 协议只传 module ID、期望状态、\ncatalog 和结果，不传函数或 renderer 对象。\n`LanguageCompletionProviderModuleWireServer` 在 Worker 内委托 module host；\n`LanguageCompletionProviderModuleWireClient` 验证 catalog revision 与 request\nresponse identity。stale/malformed catalog 或协议不匹配会 invalidate 共享\ntyped Worker client，因此 catalog、activation 和 request 不会形成彼此独立的\n半失效状态。\n\n`LanguageCompletionCatalogWorkerClient` 将 required-module activation 合成首次\nrequest readiness barrier。browser factory 声明需要 `aster.word`；Worker entry\n只注册同名 module definition，不再无条件注册 lexical provider。客户端先等\nmodule catalog，再请求激活；Worker 原子注册 provider 并发布 provider catalog，\n最后返回 activation result。共享 port 的有序性保证 catalog revision 先于\nbarrier 释放，立即发出的首次 Invoke 也不会跑到空 registry。Worker 重建会\n重新执行同一 handshake。\n\n### Current 31：版本绑定的 completion resolve\n\n列表 item 新增显式 `hasDeferredDetails`，但 provider 的 `resolveData` 不进入\nrenderer item 或通用 completion DTO。`LanguageCompletionProviderWorker` 在一次\n成功列表请求结束时保存 provider realm 内的解析记录，key 同时包含 completion\nrequest ID、model version、provider ID 和 item ID。新结果替换 cache；provider\n撤销或同 ID replacement 也会使旧记录失效。\n\nprovider resolver 只能返回 `LanguageCompletionItemDetails`。runtime normalizer\n只接受 `detail` 与 `documentation`，拒绝 `insertText`、range、label 等字段，\n因此延迟解析不能改变已验证的 edit identity。`resolveData` 在缓存前通过\nstructured clone 与 provider 后续 mutation 隔离；resolve failure 走 provider\nerror policy，但不会 poison 一个协议仍健康的 Worker。\n\n独立的 `aster.completion-resolve` side-channel 提供 resolve/result/failure/\ncancel。它复用同一 Worker port，却不扩张通用 language request envelope。\n客户端核对 response target；错配或 malformed response 会 invalidate 共享\nWorker。普通 provider failure 只拒绝对应 resolve。浏览器取消会发送 cancel，\nWorker 使用独立 `AbortController` 终止 provider work。\n\n`LanguageCompletionService` 在调用 Worker 前后都核对当前 result identity。\n`LanguageCompletionSessionController` 只为 focused item 自动解析，focus、\nresult、selection、accept、cancel 或 disposal 都会 abort 旧请求。session 用\nComplete/Loading/Failed/Unavailable 命名状态投影详情；失败不会关闭候选列表，\n已解析文本也不能参与 acceptance。browser widget 只用 text node 展示选中项\ndetail/documentation，并以 `.resolving` 和 `aria-busy` 投影加载状态。\n\n### Current 32：共享 token/diagnostic Worker transport\n\n通用 `LanguageWorkerWireCodec` 从单一 `lane` 演进为显式 `lanes` 集合，codec 的\npayload/result encode/decode 都接收当前 lane。request envelope 与协议版本无需\n变化；client 使用 pending request 的 lane 解码 result，server 在 decode 前拒绝\n未声明 lane。completion 继续声明单 lane，而 analysis codec 同时声明 `tokens`\n与 `diagnostics`。\n\n`LanguageAnalysisProviderRegistry` 保存 caller-owned provider。token lane 按注册\n顺序选择第一个匹配 provider，避免多个语义 token batch 产生未定义覆盖规则；\ndiagnostic lane 并发执行全部匹配 provider，并按注册顺序合并，允许诊断 overlap。\nprovider failure 通过 Worker realm 的 error policy 报告并与健康 batch 隔离。\n所有 range、token 排序/非重叠、modifier、severity、message、code 与 source\n都先针对 captured snapshot 归一化。\n\n`LanguageAnalysisService` 用一个 `LanguageRequestCoordinator` 和一个 Worker\n同时拥有两条 latest-wins lane，并将结果分别应用到 token/diagnostic versioned\nstore。两 lane 可并发；任一 lane 的新请求只 supersede 自己，model transaction\n则先取消两者，再向共享 `LanguageWorkerModelSynchronizer` 发送一次增量 change。\n因此首个请求发送 full snapshot，同一轮的另一个 lane 使用 reference，后续\n请求在有序 sync 后继续 reference。无效 DTO 在 renderer realm 被拒绝，terminal\nWorker failure 由 coordinator 在下一请求重建。\n\nbrowser 的 `createAsterAnalysisWorkerFactory` 创建独立 Vite module Worker。\ncompletion 与 analysis 共用抽取后的 browser/dedicated Worker port adapter，\n但 provider host、协议 client 和 failure domain 独立。Worker entry 注册\n`aster.lexical` baseline provider，为 TypeScript/JavaScript/JSON 以及 Rust 提供确定性的\ncomment/string/number/identifier/keyword/operator token；Rust profile 还识别 ordinary/raw string\n和 character literal，以及未终止 string/template/comment/raw string 和 bracket balance\ndiagnostic。它不是 parser 或语言\n服务器；在 Current 32 边界内每条 lane 仍完整扫描 captured snapshot，只有文档\ntransport 已经增量化。产品 composition root 尚未选择该 factory。\n\n### Current 33：版本化增量词法分析\n\n通用 Worker server 在成功应用一笔有序同步事务后，向实现\n`LanguageWorkerDocumentSynchronizationObserver` 的 Worker host 投递\n`previousVersion`、`modelVersion`、规范化 change 和更新后的不可变 snapshot。\n`LanguageAnalysisProviderWorker` 再把该事件投递给注册表中声明\n`synchronizeDocument` 的 provider。单个 provider 的同步失败只进入该 provider\n的错误策略，不会清空 Worker mirror，也不会阻断健康 token/diagnostic lane。\n\n`aster.lexical` 现在由一个 token 与 diagnostic lane 共享的版本缓存计算结果。\n逐行扫描只保存相对列范围、括号/诊断事件和显式\n`normal`、`blockComment`、`multilineString` 与带 delimiter hash 数的 Rust `rawString` 输入输出状态。事务更新先复用文本相同的\n前缀，再从首个变化行向后传播状态；进入相同文本后缀且输入状态与旧缓存一致时，\n剩余行整体复用。行号发生移动时，相对结果在聚合阶段重新绑定当前 snapshot，\n因此不会保留旧版本绝对坐标。\n\n同一版本的另一条 lane 直接读取已聚合缓存，不再重复扫描。缓存 observer 记录\n完整/增量更新、实际扫描行数和复用行数，测试据此锁定 1,000 行文档的单行编辑\n预算；跨行状态测试证明传播在闭合点后收敛，120 轮确定性随机编辑逐版本与全量\n新缓存 oracle 比对 token 和 diagnostic。该能力仍是确定性词法基线，不代表\nTypeScript AST、语义类型检查或语言服务器级增量图已经完成。\n\n### Current 34：可确认基准的 analysis result delta\n\nWorker wire protocol v3 为每条 lane 增加可选的 `resultBaseRequestId`。client 只\n发送最后一次通过 renderer application gate 的 request ID；server 仅在自己最后保存的同 lane result\n具有完全相同 ID 时把它交给 codec。若响应在取消竞态中丢失、请求乱序、Worker\n重建或任一侧没有基准，server 发送 full result。这个握手避免把“server 已发送”\n误当成“client 已接收”，也不需要额外 acknowledgement 消息。codec 必须显式\n选择 `stateless` 或 `confirmedBase` result protocol；completion 不持有无用基准。\n\nanalysis codec 的 delta 保存绝对相同的 item 前缀，对文档总行数变化后的 item\n后缀执行统一行位移，并只传输中间 splice。DTO 绑定 base request ID、行位移、\n起始 item、删除数量和插入 item。renderer realm 在重建前校验基准、lane、\nsnapshot 行数差、splice 边界以及全部 token/diagnostic contract；delta 不能减少\n传输 item 数时直接使用 full。completion codec 继续发送 full result，不承担\nanalysis 特有规则。\n\n100 轮随机编辑证明两条 analysis lane 的 delta 重建与 full oracle 相同。\n1,000 行且超过 3,000 token 的文档在首行普通编辑后最多传输 4 个 token item；\n测试还主动制造 client 漏收 server result 的状态，证明下一请求回退 full。\n\n当前限制必须保持显式：v3 只有一个连续 item splice，距离很远的多个变化区域\n可能回退 full；renderer 的 `VersionedLanguageResultStore` 仍接受完整数组，\n`LanguageTokenLineIndex` 仍在发布后重建。行原生 renderer store 与 multi-splice\n协议尚未完成，不能把 Worker 传输增量描述成端到端零复制。\n\n### Current 35：application-confirmed token line state\n\n`LanguageWorkerWireClient` 不再在 decode 时立即晋升 analysis 基准。它先暂存\n结果；`LanguageRequestCoordinator` 只有在 request identity、model version、\nstore normalization 和同步 application callback 全部通过后，才以\n`LanguageWorkerResultDisposition.Applied` 确认。取消、stale result 和 application\nfailure 都发送 `Discarded`，因此下一请求不会引用 renderer 从未接受的基准。\n\ntoken delta 的 splice 元数据不进入可克隆 DTO，也不开放给 provider。codec 在\nrenderer realm 完成 DTO 与 snapshot 校验后，通过 realm-local `WeakMap` 绑定；\nprovider snapshot normalizer 会剥离该提示，token store normalizer 只保留已绑定\n元数据。\n\n模型事务发生时，`LanguageTokenLineIndex` 立即清空可见 bucket，同时可将上一份\nimmutable state 保留为隐藏 confirmed base。匹配 delta 到达后，index 只重建\nsplice 边界覆盖的稀疏 token 行；行号未变化的 prefix/suffix line object 直接\n复用。base 不匹配或 full result 到达时仍执行完整 rebuild。change event 显式\n报告 `rebuiltLineCount` 与 `reusedLineCount`。\n\nbrowser 不再在 source event 上解析全部 sparse lines。\n`AsterSemanticTokenSource.getLineTokens` 只解析指定行，Viewport 先解析整个目标\n虚拟窗口，再原子更新该窗口。1,000 个 token 行的单行编辑测试证明 1 行重建、\n999 行复用；100 轮随机 wire delta 与 full token oracle 一致；1,000 行文档的一行\nviewport 只调用一次 presentation resolver。\n\n当前 store 仍保留完整 token 数组，换行编辑也仍需为绝对 range 重建 shifted\nsuffix。下一步需要 relative-range persistent line storage，才能把 line delta\n从投影层继续下沉到 canonical result ownership。\n\n### Current 36：multi-splice 与相对 token 行基座\n\nlanguage Worker protocol v4 将 analysis delta 从单个连续 splice 扩展为按 base item\n坐标排序的 splice 列表。`createLanguageAnalysisItemSplices` 先以完全相同的 snapshot\n行建立有序稳定锚点，再要求对应 token/diagnostic item 在各自累计行位移下完全相同；\n锚点之间才作为变化区传输。每个 splice 记录其后续未变化区间的累计行位移，因此两个\n相距很远的编辑不再把中间数千个 item 合并进一个巨大变化区。无法证明排序、稳定锚点\n或传输收益时仍回退单 splice 或 full result，正确性不依赖差分质量。\n\nrenderer 解码器依次验证 base ID、splice 顺序、非重叠边界、最终行数位移和完整结果\ncontract。通过验证后，它把 base/result item 坐标、插入数量及 splice 前后行位移作为\nrealm-local `WeakMap` 元数据交给 token store；provider 无法伪造该提示。协议版本提升\n到 v4，避免旧 Worker 把新的 result payload 当成 v3 解析。\n\n`LanguageTokenLineIndex` 的持久状态不再以绝对 `TextRange` 作为行内容基座。每个稀疏\n行保存不可变的相对列 payload；multi-splice 元数据把完整 base item 空隙映射到 result\nitem 空隙。行号不变时复用同一个公开 line object，行号移动时只创建新的轻量绑定并\n复用 payload，绝对 range 在 `getLineTokens` 或 `line.tokens` 首次查询时才生成。base\n缺失、元数据不连续或当前 token 与 payload 不一致时仍完整重建。\n\n验证覆盖两个相距 800 行的编辑只产生两个 splice，40 轮双区域事务与 full oracle\n一致，1,000 行中的两个变化行只重建 2 行并复用 998 行，以及插入一行后只重建新行、\n复用原有 1,000 个相对 payload。`VersionedLanguageResultStore` 目前仍发布完整绝对\ntoken 数组；若要进一步消除 renderer 端整数组分配，需要后续引入持久 result tree，\n不能把当前状态描述为端到端零复制。\n\n本阶段同时固定服务抽取边界：\n\n| 能力 | 所有者 | 当前状态 |\n| --- | --- | --- |\n| event、lifecycle、URI、resource collection | `base` | ✅ 复用现有领域无关基座；禁止 editor 反向依赖 |\n| 原始资源 I/O 与粗粒度失效 | `platform/files` | ✅ App Server-backed UTF-8 read/write 与 `fs/changed` projection |\n| load/save 传输、共享文档引用与 Aster dirty/revert/conflict policy | `workbench/services/textfile/common` / `BrowserTextModelService` | ✅ CAS 与 workspace-scoped working-copy 备份恢复均已接通 |\n| 文本事务、selection、decoration、language result | `editor/common` | ✅ editor 领域所有权 |\n| TextMate grammar/runtime 与 token provider | 独立 `workbench/services/textMate` adapter | ✅ runtime/provider/browser WASM、内置资源、声明式 extension discovery、活动主题、embedded language 与 bracket metadata 已接通 |\n\n因此 Current 36 不为 token delta 新造 service，也不提前创建没有保存、冲突或 grammar\nconsumer 的空壳 service。Current 43 在 Analysis provider 成为真实 consumer 后才抽取\nTextMate adapter。未来若文件 I/O、dirty 状态或 TextMate runtime import 出现在\n`TextModel`、token index 或 Aster browser component 中，应视为所有权漂移并\n立即沿上述边界抽取。\n\n### Current 37：共享 provider module 基座与 Analysis 激活屏障\n\nCompletion 原先拥有的 module registry、host、catalog、activation 状态机和\nwire request/response 校验已经抽为 `editor/common` 内的通用语言域设施。\nCompletion 与 Analysis 仅保留 provider 类型和协议描述符的薄封装。该抽取不会进入\n`base`：module ID、provider batch 与 Worker catalog 都是编辑器语言域语义，反向下沉\n会违反 base 的领域无关边界。\n\n两个真实调用方都需要“等待异步准备，同时响应调用方 `AbortSignal`”，因此仅将这一\n无领域语义的机制抽为 `base/common/cancellation.raceCancellation`。它不拥有也不取消\n底层任务，只结束当前调用方的等待，并统一产生可识别的 `CancellationError`。\n\nAnalysis Worker entry 不再直接把 lexical provider 无条件注册进 provider registry。\n它发布 `aster.lexical` module definition，由\n`LanguageAnalysisModuleWorkerClient` 在首个 token/diagnostic 请求前完成 catalog\n握手与激活。激活以一个 provider batch 原子提交；碰撞、加载失败或加载期间撤销都不\n泄漏部分 provider。required module 失败会使预热 Worker 失效，下一次请求由\n`LanguageAnalysisService` 创建全新 Worker 并重新握手。\n\n该组合客户端仍转发 Current 35 的 `Applied`/`Discarded` result settlement，所以\n只有 Renderer 真正接受的 Analysis 结果才成为下一次 delta 基线。测试覆盖激活先于\n首请求、批量注册回滚、确认基线转发、失败后的 Worker 重建，并继续回归 Completion\nmodule 协议。\n\n这条 module seam 是 `workbench/services/textMate` adapter 的接入点。Current 37 落地时\nTextMate grammar、scope 解析和 runtime 依赖后来由 Current 43–46 在 Aster/`base`\n之外建立，并已接通产品 grammar 资源与 extension discovery。同样，\n`workbench/services/textfile/common` 已由 Text Engine、Document Engine 与 Explorer 的\n共享 loading/save 边界证明需要。它只转发 I/O 与粗粒度 invalidation；Aster 保持 dirty、\nrevert、共享模型引用和外改 policy，不能反向塞入 `TextModel` 或 `base`。\n\n### Current 38：语言配置基座与 lexical cache 身份\n\n对照 VS Code 源码后，Aster 保留了“language configuration 与 tokenization runtime\n分离”的架构结论，但没有移植其完整 service、配置覆盖层或 support class。\nCurrent 38 的首个真实消费者只需要 comments 与 brackets，因此当时\n`LanguageConfigurationRegistry` 先定义这两个可组合字段。贡献按 priority 和注册顺序\n逐字段合并，`null` 可以显式清空继承值，撤销贡献会恢复剩余配置并发布新的不可变\nrevision。`languageId.ts` 统一 concrete language ID 与 `*` provider selector 的校验，\n这些都是 editor 领域身份，不能下沉到 `base`。\n\nAnalysis Worker realm 在加载 `aster.lexical` module 前建立并拥有该 registry，注册\nECMAScript、JSON、JSONC 与 Rust 的内置配置，再把配置 source 注入 lexical provider。\nscanner 被编译为 `LanguageLexicalLineScanner`，comments、任意长度 bracket token、\nkeyword、ordinary string、ECMAScript regular-expression、hash-delimited raw string 与 character-literal profile 均由明确的\n语言配置/lexical profile 提供，不再依赖文件级\n全局常量。\n\n此前 `LanguageLexicalAnalysisCache` 只以 model version 判断命中；同一快照先按\nTypeScript、再按 JSON 请求时可能错误复用第一份结果。现在 provider 按\n`languageId + resolved configuration identity` 拥有独立 cache，每个 cache 内仍由\ntoken/diagnostic 两条 lane 共享扫描结果。配置 revision 改变时，即使 model version\n不变，也会创建新 scanner/cache。JSON 不再继承 JavaScript 的注释与 template literal\n规则，JSONC 则显式保留注释配置。\n\n测试覆盖 priority/order 合并、逐字段保留、显式清空、验证原子性、贡献撤销、\nregistry 独立生命周期、同版本跨语言隔离、JSON/JSONC 差异、配置变更后的 cache\n替换，以及既有 1,000 行增量扫描与随机编辑 oracle。\n\n该 registry 是原生输入的 bracket/comment command、wordPattern 与 `workbench/services/textMate` adapter\n可共同消费的 Aster common 基座。Current 38 落地时尚未加入 indentation、on-enter\n或 word-pattern 字段；Current 41 在 Enter command 成为真实消费者后加入前两者。\nword-pattern 现已驱动 browser pointer、键盘导航、按词删除与 occurrence；TextMate\ngrammar runtime 与 scope tokenization 由 Current 43 在独立 adapter 中补齐。\n\n### Current 39：语言配对输入事务\n\n`LanguageConfiguration` 新增 `autoClosingPairs`、`surroundingPairs` 与\n`autoCloseBefore`。未显式贡献 auto-closing 时回退到最终 brackets；未显式贡献\nsurrounding 时再回退到最终 auto-closing。相同 open/close 的 quote pair 合法，\nbracket 与 block-comment pair 仍要求角色不同。字段继续遵循 Current 38 的\npriority/order、`null` 清空、revision 与撤销语义。\n\nECMAScript、JSON 与 JSONC 的内置配置已从 lexical profile 文件迁移到\n`languageBuiltinConfigurations.ts`。这是所有权修正：comments、brackets 与编辑\npair 是 language configuration，lexical 文件只拥有 keyword/string scanner profile。\nAnalysis Worker 仍显式拥有自己的 registry；原生输入 composition root 也可以注册\n同一组配置，但两边不共享可变实例或生命周期。\n\n`createSelectionEditCommand` 把原有 edit command 内部的多 selection 偏移映射提升为\neditor-common 事务构造器。`createLanguagePairTypeCommand` 在这一边界上实现：\n\n- 非空 selection 输入 surrounding opener 时保留方向并包围原文本；\n- collapsed caret 输入 opener 时仅在行尾或 `autoCloseBefore` 允许的后继字符前插入\n  完整 pair，并把 caret 留在中间；\n- 输入已位于 caret 后的 closer 时只移动 selection，不产生 model version；\n- `createLanguagePairBackspaceCommand` 删除空 pair 两侧，同时让同一多光标事务中的\n  其他 selection 保持普通 Backspace 语义。\n\n`AsterTextInputControllerOptions.language` 显式接收 concrete language ID 与\ncaller-owned `LanguageConfigurationSource`。每次相关 `beforeinput` 都读取当前 resolved\nrevision，因此配置贡献变化无需重建 View。若同时接入 completion requests，两条路径\n必须使用相同 language ID。DOM 层只做事件适配；pair 决策、事务、selection 映射与\nundo 仍由 common/controller/model 拥有。\n\n测试覆盖单字符和多字符 pair、后继字符策略、quote overtype、正反向 selection\nsurround、多光标偏移、空 pair Backspace、undo、配置 revision 热更新和浏览器\n`beforeinput` 路由。Current 39 落地时尚无自动闭合来源追踪；该缺口由 Current 40\n补齐。自动缩进与 on-enter 由 Current 41 补齐基础事务；token-context `notIn` 条件\n由 Current 42 补齐 string/comment 基线。\n\n### Current 40：每编辑器实例的自动闭合来源\n\n字符相等不足以证明 closer 来自自动闭合。`createLanguagePairTypeCommand` 现在为实际\nauto-close edit 生成 post-transaction `LanguageAutoClosingAction`；调用者只有在返回的\nmodel version 仍是当前已提交版本时，才能把 action 交给\n`LanguageAutoClosingTracker`。同步 listener 引发重入编辑时，过期 action 会被丢弃。\n\n`LanguageAutoClosingTracker` 位于 `editor/common`，但由每个输入 controller 实例拥有。\n它复用 `TextModel.trackRange`，分别追踪 enclosing range 与 closer range，并借用同一\nmodel 的 `EditorSelectionController`：\n\n| 关注点 | 所有者 | 结论 |\n| --- | --- | --- |\n| tracked-range 映射算法 | `TextModel` / Aster text core | ✅ 复用，不建立第二套 decoration |\n| “由自动闭合产生”的身份 | `LanguageAutoClosingTracker` | editor-instance 状态，不进入共享 model |\n| DOM `beforeinput` 与 tracker 生命周期 | `AsterTextInputController` | browser 只适配事件和记录提交回执 |\n| language pair 配置 | `LanguageConfigurationRegistry` | 继续由 caller-owned registry 提供 |\n| URI、文件保存、TextMate runtime | 各自 platform/adapter service | ❌ 不与自动闭合来源耦合 |\n| 通用 `base` | domain-neutral primitives | ❌ 不新增 editor identity 或反向依赖 |\n\n只有 tracker 对当前位置和 closer 给出正向信任时，pair command 才允许无事务\novertype；只有 trusted pair 仍为空时，Backspace 才删除两侧。用户源码中原本存在的\n`()`、字符串或 bracket 即使字符完全匹配，也保持普通插入/删除语义。\n\n外部编辑会通过 tracked range 移动仍有效的 pair；closer/open 改写、跨行、所有\nselection 离开 enclosing range 都会永久释放对应记录。多光标记录独立失效。\nundo 令 pair 消失时来源随之失效；redo 只恢复 model/selection 历史，不伪造已经释放\n的短生命周期来源。测试覆盖以上路径、过期 version、销毁后借用依赖仍存活，以及\nbrowser controller 的自动记录链路。\n\n### Current 41：语言感知 Enter 与实例缩进\n\n`LanguageConfiguration` 现在拥有 `indentationRules` 与 `onEnterRules` 两个真实消费字段。\n它们继续遵守 field-wise priority/order、`null` 清空、registration disposal 和 revision\n语义。RegExp 在注册时克隆，resolved rule/action/pattern 全部冻结；Analysis Worker\n使用的 isolated built-in source 也返回自己的冻结副本，不共享 caller 可变\n`lastIndex`。\n\n`createLanguageEnterCommand` 的决策顺序是：\n\n1. 按配置顺序匹配 `previousLineText`、`beforeText`、`afterText` 的显式\n   `onEnterRules`；\n2. 匹配 caret 两侧的 language brackets，生成 Indent 或 IndentOutdent；\n3. 读取 increase/decrease/indent-next/unindented patterns；\n4. 无规则命中时保留当前行在 caret 前的 leading indentation。\n\nIndent、IndentOutdent、Outdent、appendText 与 removeText 都转换为\n`EditorSelectionEdit`。多个 selection 仍以同一 pre-change snapshot 计算，交给\n`createSelectionEditCommand` 一次提交。`BeginCoalescedTyping` 在 Enter 前切断旧 typing\nhistory group，但让 Enter 后紧邻输入继续加入新 group：一次 undo 删除 Enter 与随后\n输入，再一次 undo 才删除 Enter 前的输入。\n\n缩进样式没有放入 language registry。`EditorIndentationOptions` 明确选择 Tabs 或\nSpaces，并提供 tabSize；visual-column 归一化负责 mixed whitespace、shift 与 unshift。\nbrowser controller 只拥有 resolved 实例选项并在每次 Enter 读取当前 language\nrevision。\n\n| 候选抽象 | 当前归属 | 评估 |\n| --- | --- | --- |\n| event/lifecycle | `base/common` | ✅ 继续复用 |\n| Tabs/Spaces/tabSize | Aster editor instance | 值对象即可，尚不需要 service |\n| indentation/on-enter language rules | `LanguageConfigurationRegistry` | ✅ 已有多个 input/Worker composition root |\n| 持久化 editor setting | future Workbench settings service | 尚未完成；未来只映射为实例 options |\n| TextFile resolve/save 与 invalidation；Aster dirty/conflict | `workbench/services/textfile` / `BrowserTextModelService` | ✅，不与 Enter command 耦合；CAS 与 workspace-scoped recovery 已接通 |\n| TextMate grammar/runtime | `workbench/services/textMate` adapter | ✅；不得进入 Enter 或 `base` |\n\nCurrent 41 初始 matcher 使用 model 原始行文本，尚未像 VS Code 的\n`IndentationContextProcessor` 一样移除 string/comment token 中的 bracket-looking\n文本。该缺口由 Current 42 的同步 lexical context 补齐；common 仍不反向 import\nWorker/browser，editor token 语义也没有进入 `base`。\n\n测试覆盖规则组合/清空/验证、全局 RegExp 隔离、ECMAScript/JSON built-ins、显式规则\n优先级、doc-comment continuation、bracket indent-outdent、increase/decrease/ignore、\ntabs/spaces visual-column 归一化、selection replacement、多光标偏移、undo group、\n动态 revision 与 browser `beforeinput`。\n\n### Current 42：同步 lexical input context\n\nWorker 的 `LanguageLexicalAnalysisCache` 会生成整份 token/diagnostic 结果，不能直接放到\n按键热路径。`LanguageLexicalContextSource` 因此定义更小的同步查询契约：\n\n- identity 必须绑定一个 `TextModel` 与 concrete language ID；\n- `getStructuralLineContent` 返回指定原始列范围的结构文本；\n- `getTokenTypeAt` 返回 caret 所处的 baseline lexical token type。\n\n默认实现 `LanguageLexicalContextIndex` 复用 `createLanguageLexicalLineScanner`，按需从最近\n已知 lexical state 扫描到目标行。model transaction 只截断 first changed line 之后的\ncache；resolved configuration identity 改变时才重建 scanner。它借用 model 与\nconfiguration source，不拥有两者。\n\n结构文本不是把整个 string/comment 清空。与 VS Code 的\n`IndentationLineProcessor` 边界一致，它只删除这些 token span 内由当前 language\n配置声明的 bracket token，保留引号、comment marker、普通文字与 whitespace。这样\n`/** ... */`、line-comment continuation 等显式 on-enter rule 仍能匹配，同时 `\"{\"`、\n`// {`、`/[{]/` 和跨行 block comment 内的括号不会触发 Indent/IndentOutdent。\n\n`LanguageAutoClosingPair` 同时新增冻结的 `notIn`。当前 closed vocabulary 是\n`string | comment`，因为 baseline scanner 只对这两类上下文提供可靠结构边界。\n`createLanguagePairTypeCommand` 在 auto-close 前查询同一个 source；已记录 closer 的\novertype 仍先由 `LanguageAutoClosingTracker` 决定，不会被 `notIn` 破坏。\n\n| 生命周期/能力 | 当前所有者 | 结论 |\n| --- | --- | --- |\n| 行 scanner 编译与 lazy state cache | `LanguageLexicalContextIndex` | Aster common、可共享注入 |\n| 默认 input context | `AsterTextInputController` | controller 创建并销毁本地 index |\n| document-level 共享 | future composition root | 可直接注入 source，无需改 command |\n| full token/diagnostic analysis | Analysis Worker | 继续异步，不阻塞按键 |\n| lifecycle/event primitives | `base/common` | ✅ 复用 |\n| lexical/token semantics | `base` | ❌ 禁止下沉 |\n\n当前 ECMAScript baseline 能识别一行 regular-expression literal，并区分除法；它仍不识别\nembedded language、template interpolation 中重新进入代码的区域，也没有 suffix-state\nconvergence 优化。这些是明确的下一阶段，而不是当前能力。测试已覆盖 partial slice、string、closed 与\nunterminated string boundary、line/block comment、多行状态、外部编辑、configuration\nrevision、销毁、跨 model/language 拒绝、Enter、auto-closing `notIn` 与 browser\n路由。\n\n### Current 43：独立 TextMate runtime adapter\n\nTextMate grammar 已经出现真实消费者：Aster Analysis provider/module seam 可以接收比\nbaseline lexical scanner 更高保真的 token provider。因此 runtime 不进入\n`editor`，而是建立单向依赖 `workbench/services/textMate → editor/common →\nbase/common`。`base` 不认识 grammar、scope、language ID 或 token provider；\nAster 也不 import `vscode-textmate`、`vscode-oniguruma` 或 WASM。\n\n`TextMateGrammarRegistry` 拥有 grammar contribution identity。一个 definition 由唯一\n`scopeName`、可选 root `languageId`、injection targets 和延迟 loader 组成；注册、\n撤销都会产生新的 immutable snapshot，旧 snapshot 仍能完成已经开始的请求。\nloader 只返回 raw grammar 文本或对象，不接收 URI/`IFileService`。资源定位、extension\nmanifest 与信任验证由当前 product composition root 负责，避免 token Worker\n反向拥有 platform I/O。\n\n`TextMateTokenizationService` 针对 snapshot 建立 `vscode-textmate.Registry`，通过注入\n的 `IOnigLib` 加载 grammar/injection，并按行保存 input/output `StateStack` 与相对列\ntoken。文档变化后先复用相同行前缀，再从第一处变化扫描；进入相同文本后缀且新的\ninput state 与旧 state `equals` 时复用剩余 suffix。grammar revision 即使 model\nversion 未变化，也建立新的 runtime generation；旧 generation 只在最后一个捕获它\n的请求退出后 dispose。\n\nscope mapping 是显式可替换的 `TextMateScopeResolver`。默认 resolver 只投影稳定的\ncomment/string/regexp/number/operator/keyword/function/type/parameter/variable/tag/\nproperty/constant/punctuation/invalid vocabulary。`TextMateScopeThemeModel` 现在提供\n可 structured-clone 的 revisioned selector rule，覆盖 token type 和 modifier；Renderer\n通过独立 theme wire 更新 Worker，Worker 清空仅样式 cache 后由下一请求重算。embedded\nlanguage identity、balanced/unbalanced bracket metadata 与 exact scope styling 已接通。`createTextMateAnalysisProvider` 把结果转为 Aster\n`LanguageTokenResult`，`createTextMateAnalysisModule` 再接入现有 module activation\n协议。\n\nAster token provider 新增 `tokenPriority`，默认值为 `0`，相同优先级保留注册顺序。\nTextMate provider 使用 `100`，因此一个 Worker 可同时激活 lexical fallback 与\nTextMate。Current 43 初始 provider 捕获当时的 root language selectors；该静态限制由\nCurrent 44 的 wildcard/`undefined` fallback chain 与版本化 catalog transport 移除。\npriority 只影响 token lane；diagnostic provider 继续按原合同合并。\n\nbrowser 边界由 `createBrowserTextMateOnigLib` 独占：它通过 Vite asset URL fetch\n`onig.wasm`，每个 Worker realm 只初始化一次，并只把 `IOnigLib` 交给 common。\nCommonJS package 在 Node ESM 和 Vite realm 的导出形态不同，适配层统一处理\nnamespace/default 两种形式；该差异不泄漏给调用方。\n\n| 能力 | 所有者 | 当前状态 |\n| --- | --- | --- |\n| grammar identity、revision、injection graph | `workbench/services/textMate/common` | ✅ |\n| TextMate runtime 与 incremental state | `TextMateTokenizationService` | ✅ |\n| Aster provider/module integration | `workbench/services/textMate/common` | ✅ |\n| Oniguruma WASM URL/fetch | `workbench/services/textMate/browser` | ✅ |\n| baseline structural diagnostics | `aster.lexical` | ✅，继续独立 |\n| grammar resource/extension manifest loading | product extension/resource layer | ✅ App Server extension discovery 与声明式资源投影已接通；不执行 extension JavaScript |\n| scope-theme selector、token type、modifier 与 embedded-language projection | TextMate/theme adapter | ✅ |\n| URI、文件保存、dirty/conflict | platform/textfile / `BrowserTextModelService` | ✅，不得混入 TextMate；CAS/恢复已接通 |\n\n真实 WASM 测试覆盖跨行 string/comment state、单行编辑只重扫一行、多行状态变化扫描到\nsuffix convergence、同 model version grammar revision 替换、scope validation、\ncancellation、独立生命周期，以及经过 `LanguageAnalysisService` application gate 的\nprovider priority。独立 Vite entry build 证明 browser WASM adapter 可被 Worker\n打包。`createBrowserEditorPart` consumes the Workbench `ITextMateService`/`BrowserTextMateService` for product Aster panes and subscribes\nto catalog/theme revisions before scheduling a replacement analysis request.\nThis proves extension-packaged JSON/JSONC and caller-resolved session contributions\nare active in the product path; Rust owns extension manifest/resource discovery and\nthe Workbench composition root projects those contributions into TextMate.\n\n### Current 44：版本化 grammar catalog 与动态 token fallback\n\nGrammar loader 是函数，不能跨 structured clone；而 Electron Renderer 能通过 platform\nservice 读取 extension/resource，dedicated Worker 则不应拥有 IPC 或文件权限。因此\nTextMate 新增显式的完整 catalog 边界：\n\n1. `materializeTextMateGrammarCatalog` 把一个 immutable registry snapshot 的 raw\n   grammar 文本/对象解析为有界 content catalog；\n2. `TextMateGrammarCatalogModel` 在 Renderer 维护严格递增的完整 revision；\n3. `TextMateGrammarCatalogWireClient` 通过 Analysis Worker 已有 port 发送 catalog；\n4. `TextMateGrammarCatalogWireServer` 先完整验证并构造候选 registry，再原子替换\n   `TextMateGrammarCatalogStore`；\n5. `TextMateTokenizationService` 在下一请求捕获新的 registry snapshot，同 model\n   version 也不会复用旧 grammar runtime。\n\nWire 限制 grammar 数、单 grammar UTF-16 长度和 catalog 总长度，拒绝重复 scope/root\nlanguage、空内容、乱序 revision 与未知 response。远端拒绝、malformed response 或\ntransport failure 会 poison catalog client 并 invalidate 整个 Analysis Worker；下一\n请求由 `LanguageRequestCoordinator` 建立新 Worker，并把 catalog source 当前 revision\n发送给从零开始的 store。普通 dispose 只结束 pending catalog wait，不反向拥有 source\n或 port。\n\n静态 root selector 会让动态 catalog 增加语言时必须重注册 provider。为消除这个耦合，\nAster token lane 从“选一个 provider”演进为明确的 priority fallback chain：\n\n- `LanguageAnalysisProviderRegistry.getTokenProviders` 按 `tokenPriority` 降序返回，\n  同 priority 保留注册顺序；\n- provider 返回 `undefined` 表示“本次不拥有”，继续下一个 provider；\n- provider failure 被隔离并报告，然后继续 fallback；\n- 第一个返回有效结果的 provider 经过原有 snapshot normalizer 和 application gate；\n- 所有 provider 都缺席时才发布 empty token result。\n\nTextMate provider 因而声明 wildcard、priority `100`；catalog 没有该 language root 时\n返回 `undefined`，priority `0` 的 `aster.lexical` 自动接管。该 fallback 语义只属于\ntoken provider；diagnostic lane 仍然并发合并所有匹配 provider。\n\n`TextMateAnalysisModuleWorkerClient` 同时持有 Aster module client 与 grammar catalog\nclient。catalog updates 串行发送，每个 Analysis request 都等待调用时最新的 scheduled\nrevision。`textMateAnalysisWorkerMain.ts` 是独立 composition root，拥有：\n\n| Worker 内能力 | 所有者 |\n| --- | --- |\n| Analysis request/result 与 document mirror | Aster Worker wire |\n| provider module activation | Aster generic module wire |\n| grammar catalog replacement | TextMate catalog wire/store |\n| TextMate provider/runtime | `textmate.grammars` / `TextMateTokenizationService` |\n| deterministic fallback/diagnostics | `aster.lexical` |\n| WASM fetch/init | `workbench/services/textMate/browser` |\n\n这种组合保持依赖为 `textmate/browser → textmate/common → editor/common → base`；\nAster 原有 Worker 不 import TextMate，`base` 也不增加任何 editor identity。\n\n真实 structured-clone 测试证明 initial catalog 在首请求前落地、TextMate 优先、\n无 grammar language 回落 lexical、同 Worker revision 热更新改变同版本 token，\nstale revision poison client，以及 registry snapshot 到 wire catalog 的 materialization。\n独立 Vite build 同时产出 TextMate Worker chunk 与 466,610-byte Oniguruma WASM。\n\nCurrent 44 落地时还缺真实 grammar resource owner 和产品层消费者。Current 45 已补入\n首批内置 grammar 与 catalog service，后续 extension service 也已接通声明式资源 discovery；`createBrowserEditorPart` 选择\nAster pane 的 Worker factory 并调度 catalog 变化后的 model token request。catalog change 本身不隐式遍历或重算所有\n文档，这个调度责任不得偷偷进入通用 catalog model。\n\n### Current 45：TextMate grammar service 与首批真实资源\n\nVS Code/Node 兼容 Extension Host 当前仍不存在；Zeta-native executable Host RPC v1 的 runtime core\n已经实现，App Server/Workbench provider bridge 和 production enforcing launcher 的状态由\n[`editor-extensions.md`](editor-extensions.md) 维护。Current 45 落地前还没有 grammar contribution\nservice；现有\n`IFileService` 的合同是“工作区文件读取”，并不拥有产品内置资源。因而真实 grammar\n不能硬编码进 Worker，也不能通过给 workspace file service 增加 TextMate 特例来读取。\nCurrent 45 建立了独立的 editor-domain 服务边界：\n\n| 能力 | 所有者 | 结论 |\n| --- | --- | --- |\n| URI、事件、取消与生命周期原语 | `base/common` | ✅ 复用，保持领域无关 |\n| 工作区用户文件读取 | `platform/files` | ✅ 合同不变 |\n| grammar contribution、异步装载和 catalog 发布 | `TextMateGrammarService` | ✅ `workbench/services/textMate/common` |\n| 产品内置 grammar 资源解析与贡献 | `AppServerExtensionService` | ✅ 通过 generation-bound extension API 接入 |\n| catalog 传输和 tokenization | TextMate dedicated Worker | ✅ 继续无文件权限 |\n| extension manifest 与外部 grammar resource | extension service | ✅ 不可变 generation-bound 声明式 discovery；不执行 extension JavaScript |\n| TextFile resolve/save/invalidation；Aster dirty/save/revert/conflict | `textfile` / `BrowserTextModelService` | ✅，未与 grammar service 混合；CAS/working-copy 恢复已完成 |\n\n`TextMateGrammarService` 拥有 registration 和 `TextMateGrammarCatalogModel`。每次贡献变化\n都会捕获 immutable registry snapshot；较新 revision 会取消较旧 materialization，只有最新且\n完整的 catalog 才能发布。loader 失败通过 `onDidFailCatalog` 报告，已经可用的 catalog 保持\n不变；`whenReady()` 明确等待当前最新 revision。服务销毁先关闭调度和 listener，再释放注册，\n不会在 teardown 中启动新的 catalog 工作。\n\n首批产品资源从相邻 VS Code 源码树的 `extensions/json/syntaxes` 移入：\n`JSON.tmLanguage.json` 和 `JSONC.tmLanguage.json`。两者当前位于根目录 `extensions/json/`，保留\n上游 revision/provenance，由 App Server extension resource API 读取，再通过现有 catalog wire 传给 Worker。\n`common` 不 import raw asset，Worker 不访问文件系统，`base` 不识别 grammar、scope 或\nlanguage identity。\n\n`BrowserTextMateService` 把内置 grammar service 和\n`createTextMateAnalysisWorkerFactory` 组合为一个可销毁的产品接入单元。Current 45\n落地时 Workbench 尚无 Aster `EditorPane`，因此当时未选择该 support，也未在 catalog\nrevision 变化时为打开文档请求重分析；Current 46 已补齐这两个真实消费者。\n\n测试覆盖 contribution 装载、latest-revision-wins、失败保持 last-good catalog、撤销后重发布，\n并使用真实 Oniguruma 对移入的 VS Code JSON grammar 执行 tokenization。扩展与打包测试同时\n验证 manifest 引用的真实资源仍存在。\n\n### Current 46：Workbench TextFile 边界与 Aster 产品 Pane\n\nAster 已从独立内核演进为由真实 `IEditorPane` 宿主的编辑器能力，但仍保持“Workbench 负责资源与宿主、\n编辑器域负责模型和交互语义”的单向依赖。Code 与 Academic 产品通过 Workbench contribution 注册 Aster 为\n普通文本的默认 editor；document、diff 和 PDF 继续通过各自明确的 pane descriptor 参与选择。\n\n`IEditorPart.openEditor` 是产品调用面，`EditorPaneRegistry` 是实现选择边界，`IEditorPane` 是\n被选实现的生命周期 contract。产品调用方不选择 parser、analysis service 或 transport；descriptor\n只绑定当前受支持的 code、document、diff、PDF 等资源视图，App Server 不知道最终选择了哪个 pane。\n\n| 能力 | 当前所有者 | 状态 |\n| --- | --- | --- |\n| URI、取消、事件、生命周期原语 | `base/common` | ✅ 复用，保持领域无关 |\n| 工作区原始文件读取 | `platform/files` | ✅ |\n| file/bootstrap 内容决策 | `ITextFileService` | ✅ Workbench service |\n| URI 到 Aster `TextModel` 的共享引用 | `BrowserTextModelService` | ✅ editor-owned |\n| Aster viewport、native input、基础键盘/指针与 text drop | `CodeEditorWidget` | ✅ document session 与 embedded widget 共用的浏览器编辑表面 |\n| Aster language、folding、diagnostic、save 与文档命令组合 | `EditorPart` + editor contribution registry | ✅ per-editor runtime；可独立能力由产品 bundle 选择 |\n| original/modified 版本 gate、diff result 与前端计算取消 | `DiffModel` / `IDiffComputationService` | ✅ common model；browser Worker 为当前实现 |\n| JSON/JSONC TextMate 与 Analysis Worker | `workbench/services/textMate` (`ITextMateService`) | ✅ 产品 Aster pane 已选择 |\n| Completion Worker | `createBrowserEditorPart` | ✅ 产品 Aster pane 已选择 |\n| dirty、save/revert、CRLF/LF、粗粒度外改重载与冲突状态 | `BrowserTextModelService` | ✅；CAS 与 Workbench 备份恢复已完成，非 UTF-8 编码仍是单独能力 |\n\n打开资源时，`ExplorerViewPane` 只提交 `{ resource, label }`；它不再预读文件或伪造\n`initialText`。`EditorPart` 选定 descriptor 后把 `ITextFileService` 注入 pane。\nAster pane 先通过 `BrowserTextModelService.acquire` 获取引用：已有资源模型保持权威，\n新资源才调用 TextFile resolve。最后一个引用释放时模型销毁。TextFile service\n不吸收任何编辑器的 transaction、undo 或 selection 类型。\n\n`TextModel` 本身只接受一个领域无关的可取消 maintenance scheduler；没有注入时保持\n同步压缩默认行为。浏览器 `BrowserTextModelService` 为文件模型注入 idle scheduler，\n所以达到 piece-tree 回收阈值不会把 O(document length) 压缩塞进一次编辑事务；调度\n任务在模型关闭时取消，快照、history 与 `TextModel.version` 不因维护而改变。\n\n产品 Aster session 组合 `BrowserTextMateService` 与 Completion Worker。\n它等待初始 grammar catalog，再发 token/diagnostic 请求；catalog revision 变化会触发\n当前文档重新分析。直接构造的 Aster session 仍使用本地 lexical/word provider，方便\n独立嵌入和确定性测试。详细 TextFile 实现契约见\n[`desktop/src/zeta/workbench/services/textfile/README.md`](../desktop/src/zeta/workbench/services/textfile/README.md)，\nAster 内部契约见\n[`desktop/src/zeta/editor/text-engine.md`](../desktop/src/zeta/editor/text-engine.md)。\n\nGrammar catalog 由共享 Workbench `ITextMateService` 拥有，声明式 extension resource contribution\n会更新其 revision；每个文档的 Analysis Worker 仍由其 model coordinator 独立拥有，避免故障域和增量 mirror 互相污染。\n\n本阶段明确没有把 TextFile、TextMate 或 document identity 下沉到 `base`。当前 host\n已具备原子写入与粗粒度变更通知，Aster 因而拥有 dirty/save/revert 和外改 policy；\nexpected-revision write、workspace-scoped working-copy 备份恢复已经接通；非 UTF-8 编码恢复仍是独立能力。\n\n### Proposed 3：语言边界\n\n在 Current 21–36 的请求、结果、基础 diagnostic、semantic-token 与\ncompletion provider/accept/wire 之上定义 rich diagnostic identity、\nactionable glyph 与语言专用增量语法/语义分析。\n所有结果必须继续绑定请求时的 model version，不得绕过同步 application gate。\n\n### Proposed 4：原生 view 与输入\n\n在现有虚拟行、字体度量、gutter、selection/caret、基础 decoration、\nhit-test、pointer selection、keyboard navigation、普通 textarea 编辑、\n基础 clipboard 与桌面式 composition 之上补齐 Android/iOS IME\n差异、macOS clause presentation 与跨平台辅助技术验收；富\ndecoration 与 versioned language result 沿 language boundary 演进。\n每增加一种输入路径，都必须生成同一种 Zeta transaction。\n\n## 评估与迁移门槛\n\n| 阶段 | 必须证明 |\n| --- | --- |\n| Storage | 大文件编辑、随机事务回放、snapshot 一致性和内存上限 |\n| Model state | 多光标、tracked range、undo selection 和 decoration 稳定性 |\n| Language | cancellation、版本拒绝、worker crash recovery |\n| View | viewport 正确性、字体变化、滚动稳定性和主题切换 |\n| Input | 主流 IME、clipboard、dead key、组合取消和浏览器差异 |\n| Accessibility | screen reader 导航、ARIA、键盘完整操作和高对比度 |\n\n这些证据决定现有能力能否从“部分具备”升级为“已具备”；它们不再对应任何旧 runtime 迁移状态。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "环境分层与-base-联动",
        "title": "环境分层与 base 联动"
      },
      {
        "depth": 2,
        "id": "当前数据流",
        "title": "当前数据流"
      },
      {
        "depth": 2,
        "id": "目标数据流",
        "title": "目标数据流"
      },
      {
        "depth": 2,
        "id": "长期不变量",
        "title": "长期不变量"
      },
      {
        "depth": 2,
        "id": "分阶段实施",
        "title": "分阶段实施"
      },
      {
        "depth": 3,
        "id": "current文本事务与-piece-tree-内核",
        "title": "Current：文本事务与 Piece Tree 内核"
      },
      {
        "depth": 3,
        "id": "current-1基础存储硬化",
        "title": "Current 1：基础存储硬化"
      },
      {
        "depth": 3,
        "id": "current-2selection-值与模型锚点部分完成",
        "title": "Current 2：Selection 值与模型锚点（部分完成）"
      },
      {
        "depth": 3,
        "id": "current-3decoration-collection",
        "title": "Current 3：Decoration collection"
      },
      {
        "depth": 3,
        "id": "current-4结构安全的输入-undo-合并",
        "title": "Current 4：结构安全的输入 Undo 合并"
      },
      {
        "depth": 3,
        "id": "current-5ime-composition-内核事务",
        "title": "Current 5：IME composition 内核事务"
      },
      {
        "depth": 3,
        "id": "current-6固定行高-viewport-内核",
        "title": "Current 6：固定行高 viewport 内核"
      },
      {
        "depth": 3,
        "id": "current-7只读虚拟行-browser-projection",
        "title": "Current 7：只读虚拟行 browser projection"
      },
      {
        "depth": 3,
        "id": "current-8字体度量与增量横向宽度",
        "title": "Current 8：字体度量与增量横向宽度"
      },
      {
        "depth": 3,
        "id": "current-9gutter-与-selectioncaret-projection",
        "title": "Current 9：Gutter 与 selection/caret projection"
      },
      {
        "depth": 3,
        "id": "current-10decoration-presentation",
        "title": "Current 10：Decoration presentation"
      },
      {
        "depth": 3,
        "id": "current-11pointer-hit-testing",
        "title": "Current 11：Pointer hit testing"
      },
      {
        "depth": 3,
        "id": "current-12pointer-selection-controller",
        "title": "Current 12：Pointer selection controller"
      },
      {
        "depth": 3,
        "id": "current-13pointer-drag-autoscroll",
        "title": "Current 13：Pointer drag autoscroll"
      },
      {
        "depth": 3,
        "id": "current-14pointer-multi-cursor-policy",
        "title": "Current 14：Pointer multi-cursor policy"
      },
      {
        "depth": 3,
        "id": "current-15keyboard-cursor-navigation",
        "title": "Current 15：Keyboard cursor navigation"
      },
      {
        "depth": 3,
        "id": "current-16textarea-editing-surface",
        "title": "Current 16：Textarea editing surface"
      },
      {
        "depth": 3,
        "id": "current-17selection-clipboard-adapter",
        "title": "Current 17：Selection clipboard adapter"
      },
      {
        "depth": 3,
        "id": "current-18textarea-composition-adapter",
        "title": "Current 18：Textarea composition adapter"
      },
      {
        "depth": 3,
        "id": "current-19composition-range-projection",
        "title": "Current 19：Composition range projection"
      },
      {
        "depth": 3,
        "id": "current-20empty-selection-whole-line-clipboard",
        "title": "Current 20：Empty-selection whole-line clipboard"
      },
      {
        "depth": 3,
        "id": "current-21versioned-language-request-boundary",
        "title": "Current 21：Versioned language request boundary"
      },
      {
        "depth": 3,
        "id": "current-22versioned-token-and-diagnostic-result-state",
        "title": "Current 22：Versioned token and diagnostic result state"
      },
      {
        "depth": 3,
        "id": "current-23versioned-diagnostic-decoration-projection",
        "title": "Current 23：Versioned diagnostic decoration projection"
      },
      {
        "depth": 3,
        "id": "current-24versioned-semantic-token-line-projection",
        "title": "Current 24：Versioned semantic-token line projection"
      },
      {
        "depth": 3,
        "id": "current-25versioned-completion-resultsession-与-widget",
        "title": "Current 25：Versioned completion result、session 与 widget"
      },
      {
        "depth": 3,
        "id": "current-26completion-provider-registryhost-与触发链",
        "title": "Current 26：Completion provider registry、host 与触发链"
      },
      {
        "depth": 3,
        "id": "current-27completion-worker-wire-与-lexical-provider",
        "title": "Current 27：Completion Worker wire 与 lexical provider"
      },
      {
        "depth": 3,
        "id": "current-28增量-worker-文档镜像",
        "title": "Current 28：增量 Worker 文档镜像"
      },
      {
        "depth": 3,
        "id": "current-29worker-provider-catalog-与首次-trigger-handshake",
        "title": "Current 29：Worker provider catalog 与首次 trigger handshake"
      },
      {
        "depth": 3,
        "id": "current-30命名-worker-provider-module-与首次请求-barrier",
        "title": "Current 30：命名 Worker provider module 与首次请求 barrier"
      },
      {
        "depth": 3,
        "id": "current-31版本绑定的-completion-resolve",
        "title": "Current 31：版本绑定的 completion resolve"
      },
      {
        "depth": 3,
        "id": "current-32共享-tokendiagnostic-worker-transport",
        "title": "Current 32：共享 token/diagnostic Worker transport"
      },
      {
        "depth": 3,
        "id": "current-33版本化增量词法分析",
        "title": "Current 33：版本化增量词法分析"
      },
      {
        "depth": 3,
        "id": "current-34可确认基准的-analysis-result-delta",
        "title": "Current 34：可确认基准的 analysis result delta"
      },
      {
        "depth": 3,
        "id": "current-35application-confirmed-token-line-state",
        "title": "Current 35：application-confirmed token line state"
      },
      {
        "depth": 3,
        "id": "current-36multi-splice-与相对-token-行基座",
        "title": "Current 36：multi-splice 与相对 token 行基座"
      },
      {
        "depth": 3,
        "id": "current-37共享-provider-module-基座与-analysis-激活屏障",
        "title": "Current 37：共享 provider module 基座与 Analysis 激活屏障"
      },
      {
        "depth": 3,
        "id": "current-38语言配置基座与-lexical-cache-身份",
        "title": "Current 38：语言配置基座与 lexical cache 身份"
      },
      {
        "depth": 3,
        "id": "current-39语言配对输入事务",
        "title": "Current 39：语言配对输入事务"
      },
      {
        "depth": 3,
        "id": "current-40每编辑器实例的自动闭合来源",
        "title": "Current 40：每编辑器实例的自动闭合来源"
      },
      {
        "depth": 3,
        "id": "current-41语言感知-enter-与实例缩进",
        "title": "Current 41：语言感知 Enter 与实例缩进"
      },
      {
        "depth": 3,
        "id": "current-42同步-lexical-input-context",
        "title": "Current 42：同步 lexical input context"
      },
      {
        "depth": 3,
        "id": "current-43独立-textmate-runtime-adapter",
        "title": "Current 43：独立 TextMate runtime adapter"
      },
      {
        "depth": 3,
        "id": "current-44版本化-grammar-catalog-与动态-token-fallback",
        "title": "Current 44：版本化 grammar catalog 与动态 token fallback"
      },
      {
        "depth": 3,
        "id": "current-45textmate-grammar-service-与首批真实资源",
        "title": "Current 45：TextMate grammar service 与首批真实资源"
      },
      {
        "depth": 3,
        "id": "current-46workbench-textfile-边界与-aster-产品-pane",
        "title": "Current 46：Workbench TextFile 边界与 Aster 产品 Pane"
      },
      {
        "depth": 3,
        "id": "proposed-3语言边界",
        "title": "Proposed 3：语言边界"
      },
      {
        "depth": 3,
        "id": "proposed-4原生-view-与输入",
        "title": "Proposed 4：原生 view 与输入"
      },
      {
        "depth": 2,
        "id": "评估与迁移门槛",
        "title": "评估与迁移门槛"
      }
    ],
    "searchText": " 本文是跨 editor、document、language、browser view、Workbench 和过渡 adapter 的 canonical 架构文档。扁平模块的实现和修改契约见 desktop/src/zeta/editor/README.md；行式文本与结构化文档 engine 的详细契约分别见 text-engine.md 和 document-engine.md。 快速理解 Aster 是 Zeta 唯一的可组装编辑器内核，源码保持在一个扁平的 editor 领域模块中。它包含两个独立的同步权威：行式文本 TextModel 和结构化文档 DocumentModel。两者共同使用 VS Code 风格的 common / browser / contrib / test 分区；Code 与 Academic 不拥有第二份 editor 源码，而是在产品入口静态加载不同的 contribution bundle。 使用场景 产品加载入口 编辑能力 --- --- --- Code editor.code.all.ts + workbench/contrib/codeEditor 行式 editor 能力 + code/diff pane/input 与文件服务接线 Academic editor.academic.all.ts + workbench/contrib/academic 结构化 editor 能力 + Academic profile、document pane 与 embedded code factory 完整宿主与集成测试 editor.all.ts 两组 contribution 的并集 DOM-free 调用 editor.api.ts 两个 model、schema、transaction、serialization 和坐标 API；不注册 pane Aster 是整个内核的品牌，不是某一个 engine 的别名。两个 engine 只称为 Text Engine 与 Document Engine；TextModel 与 DocumentModel 不互相继承，也不通过一个带大量可选方法的万能 model 接口合并。可共享能力必须依赖小而完整的 capability contract；schema validation、transaction、selection mapping、IME commit 和 model lifecycle 仍属于对应 engine，不能变成任意开关 contribution。 Aster 是当前唯一的产品 editor runtime。旧 Alpha/Gama editor ID、DOM class、目录和兼容 pane 已删除；架构与测试不得再以兼容为理由重新引入第二套编辑状态。 所有权 层 当前状态 责任 --- --- --- editor/common 两个同步内核与纯投影状态已具备 TextModel、DocumentModel、坐标、selection、transaction、history、schema、serialization、cursor、纯 viewport 与版本化语言状态；不得引用 Workbench、Electron 或 generated DTO editor/browser 两个 engine 的 widget 与 DOM projection 已具备 code/document/diff widget、DOM input、viewport、editor contribution registry 与 frontend-contract adapter；不得引用 Workbench 或选择产品版本 editor/contrib 行式与结构化 feature 已按能力组织 命令、controller、可移除投影、schema、citation 和 collaboration；不得注册 pane、拥有第二套 model 或读取产品 ID editor..all.ts editor 能力静态装配已具备 Code、Academic 与完整 editor contribution 清单；不得注册 Workbench pane/input workbench/contrib/ codeEditor,documentEditor,academic 产品宿主适配已具备 pane/input、文件与 working-copy 接线、Academic profile、embedded factory 和产品注册；不得实现编辑事务或视图内部行为 workbench/services/textMate 已具备 grammar revision registry、真实 TextMate/Oniguruma runtime、增量行状态缓存、Aster provider/module adapter、版本化 catalog/theme wire、独立 browser Worker、声明式扩展资源、活动主题 token color、embedded language 与 bracket metadata 均已接通 Document service 已具备 IFileService 将 App Server fs/changed 映射为工作区失效事件，ITextFileService 转发；Aster 模型服务提供 dirty、快照保存、显式 revert、CRLF/LF 保留、干净模型重载、脏模型外改状态与 expected-revision/CAS；Workbench 提供 workspace-scoped IndexedDB working-copy 恢复 Selection/decorations 基础具备 selection、实例控制器、tracked range、decoration collection Language model 已具备 Code 主路径 版本化 token/diagnostic/completion、TextMate 与 parser facts、跨文件 definition/declaration/references/implementation/type definition、Peek、workspace symbols、call/type hierarchy、rename、code action 与有序 WorkspaceEdit 均接通 App Server；更广的语言覆盖由 language-server catalog 演进，不再复制 editor contract Browser view 部分具备 common viewport、虚拟行 DOM、字体行宽、gutter、selection/caret、基础 decoration、hit-test、active-position reveal、至多 160 行的有界 density minimap（WebGL 优先、DOM 回退、click/drag scroll）、diagnostic severity marker、可见行缩进参考线已完成；富交互与主题细化尚未完成 Input controller 部分具备 IME 内核事务、pointer selection、Alt+Shift 列选择、键盘导航、textarea 编辑、completion 键盘/鼠标接受、Ctrl+Space invoke、trigger character 与 incomplete refresh、plain/syntax-marked safe HTML 选区与空选区整行 copy/cut/paste、单个显式文本文件 clipboard paste/drop、前端本地 MIME paste provider 与内置 text/uri-list、基础 composition DOM event 已完成；Android/macOS 特化仍未完成 Accessibility 部分具备 editor label、聚焦 textarea 的文本/主选区镜像、multi-selection aria-description、completion/listbox ARIA、dialog state 与 cursor/selection/save live-region announcements、forced-colors focus/selection/caret/diagnostic 语义已具备；完整 screen-reader navigation 与平台辅助输入仍待真实平台验证 Large-file policy 已具备 模型创建时固定判断 20 MiB/30 万行 tokenization、50 MiB synchronization、256M text-unit heap 阈值；保留编辑/滚动/查找/保存，关闭或限制全量后台 token、diagnostic、folding、CodeLens、Inlay Hint、symbol、occurrence 与 bracket colorization Workbench Editor Part 已有独立实现 tabs、pane 生命周期、可见性和产品 contribution src/zeta/base 继续保持领域无关。编辑器位置、文档版本、selection 和 decoration 等身份只能由 editor 领域定义，不得为了复用而下沉到 base。 环境分层与 base 联动 依赖方向是 contrib/browser → browser/common → common → base/common，同时 browser → base/browser。base 反向引用 editor 严格禁止； 这不表示 editor 要回避 base，恰恰相反，通用机制必须优先复用 base。 Editor 层 应复用的 base 能力 仍由 editor 定义 --- --- --- common event、lifecycle、IME realm coordination、cancellation、通用 geometry 两个 engine 的 position/range、model version、history、selection、transaction、schema、language request/lane/result identity、snapshot version gate 和纯 view-model 语义 browser DOM lifecycle、通用控件基础、platform/keybinding 状态、通用 layout primitive code/document viewport、行与节点投影、textarea/input adapter、字体测量、editor ARIA Workbench host platform service、context key、commands、configuration、theme editor pane 接线、document/workspace 绑定和外部区域布局 Transition adapter 对应第三方 renderer API 仅适配，不得反向定义 Zeta common/browser contract 目录结构遵循运行环境，而不是功能名称倒置嵌套： ``text editor/ common/ core,model,commands,services,... browser/ view,widget,services,... contrib/<feature / common,browser test/ common,browser editor.code.all.ts editor.academic.all.ts editor.all.ts editor.api.ts editor.main.ts ` 当前 editor/common 已直接复用 base/common/event、 base/common/lifecycle、base/common/ime 和 base/common/layout.ISize。 其中 composition 开始必须 遵守共享 IME.enabled 状态，避免编辑器与 keybinding/inputbox 各自维护 互相冲突的输入法开关。ISize 只表达通用几何；scroll clamp、visible line 和 overscan 仍由 Aster 定义，未反向污染 base。 当前数据流 当前产品普通文本走以下路径： `text EditorInput → ITextFileService.resolve ↓ Workbench BrowserTextResourceStore → Editor BrowserTextModelService → Aster TextModel ↓ Aster language session → Analysis/completion workers ↓ Aster viewport / input ↓ Ctrl/Cmd+S ITextFileService.save → IFileService.writeFile → App Server ` preferredEditorId 只是 Workbench 在 code、document、diff、PDF 等现有 pane 间做显式选择的通用机制，不再承载旧 editor 兼容入口。 目标数据流 `text Document service ↓ load/save/conflict Aster TextModel ↓ transaction snapshots Selection + Decoration model ↓ Language snapshots ─────→ Language workers ↓ Viewport + Layout model ↓ Input controller ←──────→ Native DOM renderer ` 长期不变量 - 一个文档在一个 renderer realm 中只有一个权威 TextModel。 - 所有文本变化都经过原子 transaction，版本、事件和 history 同步提交。 - 文件保存、外部变更与冲突由 Document service 决定，不能由 view 决定。 - worker 只消费带版本的不可变 snapshot；过期结果不能重新写入新版本。 - view 可以丢弃并重建，不能因此丢失文本、selection 或 undo history。 - IME composition 在提交前不是普通编辑事务，取消 composition 不得污染 history。 - accessibility 是 view/input contract 的组成部分，不是渲染完成后的补丁。 - 旧 Alpha/Gama identity、DOM vocabulary 和 runtime 类型不得重新进入 editor、document 或 Workbench 公共接口。 分阶段实施 Current：文本事务与 Piece Tree 内核 已实现 LF 规范化、UTF-16 坐标、原子非重叠编辑、版本、同步事件和事务级 undo/redo。存储已经切换到 TypeScript PieceTreeTextBuffer：piece 引用不可变 original/add buffer，确定性 treap 在子树维护字符数、换行数和 piece 数。随机 差分测试将 1,000 次 replace、range read、坐标和行内容与普通 string oracle 对比。 Current 1：基础存储硬化 相邻且引用同一段连续 source 的 piece 现在会在编辑边界合并。版本化 snapshot 捕获不可变 source segment，不需要在创建时拼接全文，并且在后续 编辑或模型销毁后仍可读取。undo/redo 同时受事务数量和保存文本单元预算 约束，超限时从最旧的连续可达历史开始裁剪。 事务提交后会按 retained/live 比例、绝对可回收文本量或 piece 数触发同步 compaction。当前文本被重建为新的 immutable original buffer，add buffer 清空；版本、事件和 history 均不变化。旧 snapshot 继续引用捕获时的 source， 因此 compaction 不会破坏 worker 读取的一致性。 仓库提供非 gating 的 2 MiB benchmark runner，覆盖构建、分散编辑、坐标 往返、snapshot range read 和 churn compaction。下一步仍需建立跨平台性能 预算，并评估超大文档是否需要增量或空闲调度压实。只要 snapshot 仍被外部 持有，它捕获的旧 source 就按契约继续占用内存，不能把当前 buffer 的统计值 误认为进程总内存。 Current 2：Selection 值与模型锚点（部分完成） TextSelection 保存 anchor/active 方向，TextSelectionSet 保存稳定的多光标 顺序和 primary selection。它们是不可变值，不是 TextModel 上的全局“当前 光标”：同一文档可以同时被多个 editor instance 展示，每个实例必须拥有自己 的 selection state。 TextModel.trackRange 提供文档级通用锚点，并要求调用者显式选择四种边界 stickiness 之一。每次事务完成文本变更后、发送 change event 前，所有锚点会 按完整事务统一映射；undo/redo 走同一路径。 每个 undo step 会分配稳定的 transactionId，同组提交、撤销与重做沿用该 身份，而每次提交仍产生新 version。EditorSelectionController 以此保存一个 editor instance 的命令前后选区；另一个实例共享同一 TextModel 时仍拥有 独立 selection。命令执行期间若同步 listener 再次修改模型，控制器不会把 已经过期的 post-selection 写进新版本，而是保留 tracked mapping 的安全结果。 键盘导航已按 Current 15 直接生成 TextSelectionSet；文本输入与删除已按 Current 16、clipboard 已按 Current 17 生成统一的 selection-after contract。 IME composition 的单 selection 内核路径见 Current 5，browser 映射见 Current 18。 Current 3：Decoration collection TextDecorationCollection<TMetadata 让 diagnostics、search、diff 等 owner 在同一个 TextModel 上维护彼此独立的装饰集合。集合提供 realm 内唯一且 稳定的 ID、原子 replaceAll、增量 add/update/delete，以及区分集合内容变化 和锚点范围变化的事件。范围移动继续由 TrackedRangeStickiness 决定。 common 不解释泛型 metadata，也不定义 CSS class、颜色、z-index 或 DOM 结构。 这些视觉规则必须由对应 renderer component 或语义 token owner 按 docs/ui-styling-ownership.md 投影。browser renderer 已按 Current 10 消费 collection；token/diagnostic 的 snapshot version 存储见 Current 22，向 decoration 与文本行的 projection 仍未完成。 Current 4：结构安全的输入 Undo 合并 命令可显式选择 CoalesceTyping、CoalesceBackspace 或 CoalesceDelete。 控制器为同模式的连续命令复用 nominal TextEditHistoryGroup；只有每个光标 都继续执行同方向、相邻且非空的操作时，TextModel 才复用同一 transactionId 并合并 inverse edits。多光标前序编辑导致的后续 offset shift 会在合并时累计修正。 Typing group 可以从替换单个或多个 selection 开始，随后继续普通插入或 向前覆盖替换；合并后的 inverse edits 同时删除整段新输入并恢复最初被替换 的文本。每次合并都会重新计算 history text-unit 占用，不能绕过内存预算。 同一事务删除相邻区间时，模型会先合并汇聚到同一 offset 的 inverse insertions，保证 undo script 没有共享起点歧义。若跨事务多光标删除会使 不同光标的 inverse insertions 汇聚，模型拒绝合并并保留新的 undo step。 实现不会保存全文 snapshot，也不会把无法证明等价的 edit script 强行折叠。 显式 selection 变化、undo/redo、外部编辑、非相邻操作和模式变化都会形成 新的 undo step。输入层也可调用 pushUndoStop 切断当前组；该操作本身 不会产生空历史项。 Current 5：IME composition 内核事务 EditorSelectionController.beginComposition 为当前单 selection 建立受保护的 history revision。每次 update 都完整替换上一版 provisional text，产生可观察 的 model version，但沿用同一 transactionId 和最初的 inverse edit。 commit 最终只保留一个 selection-aware undo step；cancel 恢复原文本和 原 selection，并消费该历史项，不生成 redo。 受保护 revision 在关闭前不会被 history budget 驱逐，因此即使预算为零也能 无损取消；commit 后立即重新执行预算裁剪。最终 composition text 与原文本 相同时不会保留空 undo step。普通 command、selection 变化、undo/redo 在 composition 活跃期间被拒绝，直接发生的外部 model edit 会使 session 失效。 DOM 无关的内核生命周期只支持单 selection。browser 已按 Current 18 接入 基础 compositionstart、compositionupdate、compositionend 和候选窗 anchor；Android diff、macOS 长按及其他平台差异仍未完成。 Current 6：固定行高 viewport 内核 EditorViewportModel 已在 editor/common 中建立 DOM 无关的 view-model 边界。browser 层负责测量并输入 viewport size、content width 和 line height； common 层负责内容尺寸、横纵滚动约束、可见行范围和 overscan render range。 输出 layout 是不可变快照并携带 TextModel.version，因此即使一次编辑没有 改变行数，renderer 仍能按版本丢弃旧投影。resize 与 line-height 更新尽量 保持原 fractional top-line anchor，模型收缩则把 scroll position 约束回新的 内容边界。 common 契约明确限定为固定行高，不包含软换行、折叠、inline widget、字体 测量或 DOM。editor/browser 已按 Current 7 消费这个 contract；浏览器职责 没有进入 common，也没有下沉进 base。 Current 7：只读虚拟行 browser projection EditorViewport 已把 common viewport 接到原生 DOM。它创建并拥有 .aster-editor scroll host、content extent 和 overscanned line layer； 滚动窗口重叠的行按 line index 复用 DOM identity，模型版本变化同步更新当前 render range，文档收缩则把 common 与 DOM 的滚动坐标一并约束回有效范围。 模型文本通过 textContent 投影，不作为 HTML 解析。 该组件复用 base/browser/dom、base/browser/geometry 和 base/common/lifecycle，内部 DOM 与 focus 样式由组件自己的 CSS 拥有。 Workbench host 只能负责根节点外部尺寸，不能穿透覆盖行节点。当时它只是固定 行高只读 renderer；后续 Current 18、20、26 与 33 已分别补齐输入、clipboard、 soft wrap 与基础 accessibility projection。 Current 8：字体度量与增量横向宽度 AsterDomTextMeasurer 从 line layer computed style 解析 font、letter spacing、 tab size 和左右 padding。普通文本段由 Canvas 按当前字体 shaping 和 fallback 测量，tab 按实测 space glyph 推进到下一个 stop。Canvas 不可用时使用 font-size 派生的 fallback advance；测试和未来专用字体引擎通过小型 TextMeasurer contract 注入。 AsterLineWidthIndex 首先同步测量一个有界行片段，之后通过可取消的 idle 切片完成其余非换行行；未测量期间最大值只作为下界，完成后恢复精确值。稳定 后它按 TextModelChange 的旧行范围合并同一行多处编辑，只重测事务影响的新 行组。宽度计数集合维护当前最大值，最长行缩短时无需全文重测即可收缩 content width 并夹紧横向滚动。字体加载完成或显式 refreshFontMetrics 会以同一策略 重建，因为此时每个缓存值都可能失效。随机差分测试覆盖 400 次多区间事务、 换行变化和 undo/redo，并与逐次全文扫描结果比较。 Current 9：Gutter 与 selection/caret projection 每个虚拟行现在由 sticky line-number gutter、文本节点和 overlay 组成。gutter 宽度按当前总行数的数字位数测量，并计入横向 content width；滚动复用行节点 时，行号、文本和 overlay 一起按 line index 更新。组件通过稳定的 .active 类投影 primary selection 的活动行，状态样式由 Aster 自己的 CSS 拥有， Workbench 不通过深层 selector 覆盖。 EditorViewport 可观察一个调用方持有的 EditorSelectionController。createAsterSelectionGeometry 保留 anchor/active 方向、多 selection 顺序和 primary 身份，只为当前 overscan 范围生成矩形。水平坐标复用当前 TextMeasurer 测量行前缀；被选中的 换行符占一个实测 space cell，因此终点位于下一行 column 0 时仍有明确视觉 范围。overlay 在主 caret 上投影 .primary 身份，为后续 presentation 保留 稳定状态；CSS 不使用 ARIA attribu"
  },
  {
    "slug": "editor-core",
    "sourcePath": "docs/editor-core.md",
    "title": "编辑器核心：Native Rust 与 Aster TypeScript 边界",
    "description": "两套 editor 共享产品语义和 conformance 目标，但不共享同步运行时状态。Aster 的输入必须在 Renderer 当前事件 循环内完成；逐键 IPC、WASM 双写或远端 undo authority 都会破坏 IME、selection 和渲染一致性。Zeterm 则直接链接 Rust crate，不需要 Browser transport。",
    "group": "界面与体验",
    "markdown": "> 状态：Current。`zeta-editor-core` 是 Zeterm Native editor 的纯 Rust document core；Aster 是 Zeta\n> Renderer 内独立的 TypeScript editor，不通过 WASM 或 App Server RPC 复用该同步 core。Rust 实现契约见\n> [`zeta-editor-core`](../zeta-rs/editor-core/README.md)，app presentation 见\n> [`zeta-editor`](../zeterm/editor/README.md)，Aster 实现见\n> [`desktop/src/zeta/editor/text-engine.md`](../desktop/src/zeta/editor/text-engine.md)。\n\n## 当前所有权\n\n| 能力 | Aster（Zeta TS Renderer） | Zeterm（Native Rust） |\n| --- | --- | --- |\n| 文本存储、transaction、version | `TextModel` / `PieceTreeTextBuffer` | `EditorCoreDocument` / `CodeEditorDocument` |\n| undo/redo 与 typing grouping | `TextModelHistory` | `EditorCoreDocument` + Native command policy |\n| selection、tracked range、IME | Aster common/browser | `zeta-editor` |\n| layout 与 presentation | DOM、CSS、virtual viewport | `zeta-ui`、`zui`、GPU scene |\n| 文件、LSP、workspace search | 异步调用 Rust App Server service | Native product adapter 按需组合 Rust crate |\n\n两套 editor 共享产品语义和 conformance 目标，但不共享同步运行时状态。Aster 的输入必须在 Renderer 当前事件\n循环内完成；逐键 IPC、WASM 双写或远端 undo authority 都会破坏 IME、selection 和渲染一致性。Zeterm 则直接链接\nRust crate，不需要 Browser transport。\n\n## Aster 与 Rust 后端的数据流\n\n```text\nBrowser input\n  → Aster TextModel 同步提交\n  → versioned TextModelChange\n     ├─ 同步更新 selection / tracked ranges / viewport\n     ├─ 异步同步前端 language Worker\n     └─ 异步发送 Rust file / LSP / search service\n\nRust result(resource + document version + request identity)\n  → Aster freshness gate\n     ├─ current: result store / decoration / widget\n     └─ stale: discard\n```\n\nRust 后端适合拥有文件读写与冲突检测、language-server 生命周期和 document sync、workspace search、Git/diff、\nformatting、rename、code action 和 parser-grade 分析。Aster 始终拥有这些结果的交互状态与 presentation，并通过自己\n的 `TextModel.applyEdits` 应用后端返回的 edits。\n\n## `zeta-editor-core` 的 Native 契约\n\n`zeta-editor-core` 接受 UTF-16 code-unit range、revision-bound atomic edit 和显式 post-selection，拒绝 stale\nrevision、代理对中间 offset、重叠 edit 和无效 selection。Native `CodeEditorDocument` 用 persistent core 持有\ncommitted text、selection、revision 与 history；Native text projection 只服务 line index、syntax、folding 和绘制。\n\n该 crate 不依赖 `zui`、`zeta-ui`、Native host、DOM、文件或 transport。若未来出现第二个真实 Rust consumer，应该\n直接依赖其纯 Rust API；不得为 Aster 的同步输入路径预先增加 WASM 或 App Server adapter。\n\n## 长期不变量\n\n- Aster 的同步 text/history/selection authority 在 TypeScript Renderer。\n- Rust App Server 结果必须携带并比较 Aster document version；后端不可阻塞输入。\n- Zeterm Native 直接消费 Rust editor/core/UI crate，不绕经 Electron 或 Browser API。\n- `zeta-editor-core` 不依赖 presentation 或 transport；Aster 不依赖 `zeta-editor-core`。\n- 只有基准与真实消费者证明必要时才提取共享 wire contract，不能以潜在复用代替当前所有权。",
    "headings": [
      {
        "depth": 2,
        "id": "当前所有权",
        "title": "当前所有权"
      },
      {
        "depth": 2,
        "id": "aster-与-rust-后端的数据流",
        "title": "Aster 与 Rust 后端的数据流"
      },
      {
        "depth": 2,
        "id": "zeta-editor-core-的-native-契约",
        "title": "zeta-editor-core 的 Native 契约"
      },
      {
        "depth": 2,
        "id": "长期不变量",
        "title": "长期不变量"
      }
    ],
    "searchText": " 状态：Current。zeta-editor-core 是 Zeterm Native editor 的纯 Rust document core；Aster 是 Zeta Renderer 内独立的 TypeScript editor，不通过 WASM 或 App Server RPC 复用该同步 core。Rust 实现契约见 zeta-editor-core，app presentation 见 zeta-editor，Aster 实现见 desktop/src/zeta/editor/text-engine.md。 当前所有权 能力 Aster（Zeta TS Renderer） Zeterm（Native Rust） --- --- --- 文本存储、transaction、version TextModel / PieceTreeTextBuffer EditorCoreDocument / CodeEditorDocument undo/redo 与 typing grouping TextModelHistory EditorCoreDocument + Native command policy selection、tracked range、IME Aster common/browser zeta-editor layout 与 presentation DOM、CSS、virtual viewport zeta-ui、zui、GPU scene 文件、LSP、workspace search 异步调用 Rust App Server service Native product adapter 按需组合 Rust crate 两套 editor 共享产品语义和 conformance 目标，但不共享同步运行时状态。Aster 的输入必须在 Renderer 当前事件 循环内完成；逐键 IPC、WASM 双写或远端 undo authority 都会破坏 IME、selection 和渲染一致性。Zeterm 则直接链接 Rust crate，不需要 Browser transport。 Aster 与 Rust 后端的数据流 ``text Browser input → Aster TextModel 同步提交 → versioned TextModelChange ├─ 同步更新 selection / tracked ranges / viewport ├─ 异步同步前端 language Worker └─ 异步发送 Rust file / LSP / search service Rust result resource + document version + request identity → Aster freshness gate ├─ current: result store / decoration / widget └─ stale: discard ` Rust 后端适合拥有文件读写与冲突检测、language-server 生命周期和 document sync、workspace search、Git/diff、 formatting、rename、code action 和 parser-grade 分析。Aster 始终拥有这些结果的交互状态与 presentation，并通过自己 的 TextModel.applyEdits 应用后端返回的 edits。 zeta-editor-core 的 Native 契约 zeta-editor-core 接受 UTF-16 code-unit range、revision-bound atomic edit 和显式 post-selection，拒绝 stale revision、代理对中间 offset、重叠 edit 和无效 selection。Native CodeEditorDocument 用 persistent core 持有 committed text、selection、revision 与 history；Native text projection 只服务 line index、syntax、folding 和绘制。 该 crate 不依赖 zui、zeta-ui、Native host、DOM、文件或 transport。若未来出现第二个真实 Rust consumer，应该 直接依赖其纯 Rust API；不得为 Aster 的同步输入路径预先增加 WASM 或 App Server adapter。 长期不变量 - Aster 的同步 text/history/selection authority 在 TypeScript Renderer。 - Rust App Server 结果必须携带并比较 Aster document version；后端不可阻塞输入。 - Zeterm Native 直接消费 Rust editor/core/UI crate，不绕经 Electron 或 Browser API。 - zeta-editor-core 不依赖 presentation 或 transport；Aster 不依赖 zeta-editor-core`。 - 只有基准与真实消费者证明必要时才提取共享 wire contract，不能以潜在复用代替当前所有权。"
  },
  {
    "slug": "pdf",
    "sourcePath": "docs/pdf.md",
    "title": "zeta-pdf：PDF 文档入库、系统边界与演进",
    "description": "zeta-pdf 是 Zeta 的 PDF 原生处理边界：它通过随安装包发布的 PDFium 读取页面并提取原生文字。页面渲染是为 OCR 准备的 Proposed extension，当前尚未实现。",
    "group": "界面与体验",
    "markdown": "> 当前 crate 接口、PDFium binding 与 native smoke test 见\n> [`zeta-rs/utils/pdf/README.md`](../zeta-rs/utils/pdf/README.md)。本文拥有 PDF ingestion、OCR、\n> retrieval 与 citation 的跨系统边界和演进方向。\n\n## 快速理解\n\n`zeta-pdf` 是 Zeta 的 **PDF 原生处理边界**：它通过随安装包发布的\nPDFium 读取页面并提取原生文字。页面渲染是为 OCR 准备的 Proposed extension，当前尚未实现。\n\n它不是 PDF 知识库、RAG 或 Agent Memory。持久化的文档、chunk、索引和检索\n策略应由上层 document-library / app-server 领域服务拥有；`zeta-pdf` 只提供\n可测试、可替换的 PDF 解析能力。\n\n```text\nDesktop 预览                   Agent 知识库\n───────────                   ─────────────\nChromium PDF Viewer            document-library / App Server\n（用户查看）                         │\n                                      ▼\n                         zeta-pdf（PDFium 原生边界）\n                           ├─ 原生文字提取\n                           └─ 页面渲染（仅 OCR 需要时）\n                                      │\n                                      ▼\n                          OCR / chunk / FTS / 向量索引 / RAG\n```\n\nElectron/Chromium 的 PDF Viewer 可以负责普通预览，不需要由 `zeta-pdf`\n渲染页面。只有 Agent 入库、文字定位、扫描页 OCR 或生成缩略图等后端工作才\n使用 PDFium。\n\n| 用户需求 | 当前路径 | 当前状态 |\n| --- | --- | --- |\n| 在 Desktop 的 Code Workbench 中查看 workspace PDF | Chromium PDF Viewer | 已实现：Explorer 打开 `.pdf`，受限读取后以 Blob URL 交给浏览器阅读器 |\n| 让 Agent 读取原生文字 | PDF 处理边界提取逐页文字 | 已实现 |\n| 识别扫描版 PDF | 渲染页面后交给 OCR | 计划设计 |\n| 建立可搜索知识库 | 文档库负责身份、切片和索引 | 尚未实现 |\n| 从回答跳回原页 | 文档库保存页码和来源范围 | 计划设计 |\n\n## 2. 当前基础与发布契约\n\n当前 crate 位于 [`zeta-rs/utils/pdf`](../zeta-rs/utils/pdf)，包名为\n`zeta-pdf`，Rust crate 名为 `zeta_pdf`。它已经提供：\n\n- `PdfiumRuntime::from_bundled_root()`：从发布目录解析当前平台的动态库；\n- `PdfTextExtractor::bind()`：进程启动时显式绑定 PDFium；\n- `PdfTextExtractor::extract_file()`：按页、按原顺序提取原生文字；\n- `ExtractedPdfPage::page_number`：稳定的 **一基页码**，可直接用于引用和\n  Desktop 跳页；空白页也会保留，供上层判定是否需要 OCR。\n\nPDFium 由 [`scripts/fetch-pdfium.mjs`](../scripts/fetch-pdfium.mjs) 在 CI / 发布\n构建时按 [`third_party/pdfium/runtime-lock.json`](../third_party/pdfium/runtime-lock.json)\n锁定的版本下载、校验并 stage 到：\n\n```text\nresources/native/pdfium/\n├── macOS:   lib/libpdfium.dylib\n├── Windows: bin/pdfium.dll\n└── Linux:   lib/libpdfium.so\n```\n\n运行时不搜索系统 PDFium，也不要求最终用户另行安装。App Server 启动时绑定一次，\n将 `PdfTextExtractor` 作为进程内共享服务复用；不能每个导入请求重新装载动态库。\n\n## 3. 明确的职责边界\n\n| 层 | 负责 | 不负责 |\n| --- | --- | --- |\n| `zeta-pdf` | Current：PDFium 绑定、原生文字、PDF 错误；Proposed：页级几何与渲染 | 文件持久化、OCR 模型、chunk、embedding、检索、Memory |\n| document-library（新增领域服务） | 文档身份、内容哈希、版本、页面记录、导入状态、chunk 与引用 | PDFium 动态库加载、浏览器预览 UI |\n| OCR worker | 仅对需要识别的渲染页做 OCR，返回文字与置信度 | 判断文档归属、写检索索引 |\n| 检索服务 | FTS / 向量检索、融合、rerank、过滤 | 修改源 PDF 或 Agent 记忆 |\n| Desktop | 导入入口、进度、引用跳转、Chromium PDF 预览 | 直接保存本地绝对路径、解析不可信 PDF |\n\n特别地，PDF 知识库和 Agent Memory 必须分开：PDF chunk 是可复现、可引用的\n客观文档证据；Memory 是用户偏好、确认的决策和会变化的项目状态。回答时可以\n分别检索两者，但不得把 PDF 片段写成用户 Memory。\n\n### 3.1 当前 Workbench 阅读器\n\n`desktop/src/zeta/workbench/contrib/pdf` 是一个 Workbench contribution，不属于\n`editor` 的文本或结构化文档 engine。它匹配 `application/pdf` 和 `.pdf` resource；通过\n`IFileService.readFileBytes` 请求 App Server 的 workspace-relative `fs/readBinaryFile`，\n再创建 `application/pdf` Blob URL 并嵌入 Chromium 的原生 PDF Viewer。\n\n这条路径的边界是刻意的：Renderer 不会收到主机绝对路径，`file:` URL 也不会进入\nPDF 阅读器；后端将预览读取限制为 16 MiB，并经连接所有的 ResourceStore 以 256 KiB\n分块读取，避免二进制内容超过 JSONL 帧上限。页面、缩放、搜索、打印与下载由 Chromium\nPDF Viewer 自己负责，贡献只拥有匹配、加载、可见性和 Blob URL 生命周期。\n\n## 4. 身份、所有权与引用\n\n内部接口使用下列三个层次的身份，避免把临时上传资源泄漏到持久知识库。\n\n```text\nResourceId（连接所有、短暂）\n        │  导入期间 materialize\n        ▼\nDocumentId（持久、面向用户的文档） ── DocumentRevisionId / SHA-256（不可变内容）\n        │\n        ├─ PageNumber（NonZeroU32；一基）\n        └─ PdfChunkId（持久 chunk） ── SourceSpan { revision, page range, offsets }\n```\n\n- `ResourceId` 是 App Server `ResourceStore` 的 connection-owned 临时资源。断开\n  连接或 `resource/release` 后便不再有效；它绝不能写入 `documents`、citation 或\n  vector metadata。\n- `DocumentId` 表示用户看到的一份逻辑文档；`DocumentRevisionId` 或内容\n  SHA-256 表示一份不可变的源文件内容。内容哈希用于去重、重新索引和审计。\n- `PageNumber` 以 `NonZeroU32` 表示，一律一基。任何 PDFium 零基索引转换只能\n  留在 `zeta-pdf` 内部。\n- 引用只记录 durable source span，例如\n  `{ document_id, revision_id, page_start, page_end, char_start, char_end }`；不要\n  记录桌面文件路径或临时资源 ID。\n\n源文件在导入开始时必须复制或原子移动到应用拥有的不可变存储，再开始耗时解析。\n这也允许连接结束后继续执行 job、断点恢复和可靠地重新索引。\n\n## 5. 导入生命周期\n\n当前 App Server 只有临时 Resource API，下面的 `document/import` 和 job/progress\n接口均为 **Proposed**；实现前不得把它们伪装成当前 JSON-RPC 方法。\n\n```text\n选择本地 PDF / Resource 上传\n          │\n          ▼\nmaterialize 到 app-owned staging（仍可读取 Resource 时完成）\n          │\n          ▼\n计算 SHA-256 → 去重 / 创建 DocumentRevision\n          │\n          ▼\n原子写入不可变 source store\n          │\n          ▼\nnative extract（zeta-pdf）→ PageRecord\n          │                         │\n          │                  仅空白/低质量页\n          │                         ▼\n          │                    render + OCR\n          ▼\n结构恢复 → chunk → FTS / embedding → 索引发布\n          │\n          ▼\nDocumentIngestionJob 完成；Desktop 收到进度与可引用 revision\n```\n\n解析、OCR 与 embedding 都是可长时间运行且可能重试的工作，不能阻塞一条 JSON-RPC\nrequest。`document/import` 应立即返回 durable `DocumentImportReceipt { document_id,\nrevision_id, job_id }`，进度经 server notification / subscription 报告。只有索引\n完整发布后，该 revision 才对检索可见。\n\n建议状态为：\n\n```text\nstaging → hashing → extracting → ocr_pending? → chunking → indexing → ready\n                                                  └────────→ failed | cancelled\n```\n\n`failed` 仍保留已持久化 source、失败阶段和可诊断的错误码；其未完成 chunk 不得混入\n检索结果。重试创建新的 job，不覆盖已有的可用 revision。\n\n## 6. 内部接口\n\n### 6.1 `zeta-pdf` 保持窄接口\n\n现有的 `PdfTextExtractor` 是 PDFium adapter。后续扩展应该围绕页面事实，而不是\n暴露 PDFium 的对象、FFI handle 或底层页索引给上层：\n\n```rust\n/// Extracts page-level PDF facts from an application-owned immutable source.\n///\n/// Implementations must preserve source page order and return one-based page\n/// numbers. They do not persist data or decide retrieval policy.\npub trait PdfPageExtractor: Send + Sync {\n    fn extract(&self, source: &PdfSource) -> Result<ExtractedPdfDocument, PdfError>;\n}\n\npub struct PdfSource {\n    pub revision_id: DocumentRevisionId,\n    pub path: OwnedPdfPath,\n    pub content_digest: ContentDigest,\n}\n\npub struct ExtractedPdfPage {\n    pub page_number: PageNumber,\n    pub text: String,\n    pub extraction: NativeTextExtraction,\n}\n```\n\n这是目标形状，不要求当前立刻引入 trait。当前 `PdfTextExtractor` 可先作为唯一\nPDFium 实现；在确实需要 fixture/fake 或第二个解析后端时，再以该 trait 收口。\n`PdfSource` 只接受应用拥有的文件，不能接受 `ResourceId` 或 Desktop 路径。\n\n渲染单独使用显式请求类型，避免 `render_page(page, true)` 这类难读的布尔参数：\n\n```rust\npub enum PdfRenderPurpose {\n    Ocr,\n    Thumbnail,\n}\n\npub struct PdfPageRenderRequest {\n    pub page_number: PageNumber,\n    pub purpose: PdfRenderPurpose,\n    pub max_pixel_size: PixelSize,\n}\n```\n\n### 6.2 document-library 拥有持久化与编排\n\n将导入服务放在上层 crate（建议 `zeta-document-library`），依赖 `zeta_pdf`，而非\n反向让工具 crate 依赖 App Server、数据库或向量库：\n\n```rust\n/// Coordinates durable PDF ingestion without exposing storage implementation\n/// details to callers.\npub trait PdfImportService: Send + Sync {\n    fn import(&self, request: PdfImportRequest) -> Result<DocumentImportReceipt, ImportError>;\n}\n\npub struct PdfImportRequest {\n    pub source: ImportSource,\n    pub duplicate_policy: DuplicatePolicy,\n    pub extraction_mode: TextExtractionMode,\n}\n\npub enum ImportSource {\n    OwnedStagingFile { path: StagedPdfPath },\n}\n\npub enum DuplicatePolicy {\n    ReuseReadyRevision,\n    ReindexExistingRevision,\n}\n\npub enum TextExtractionMode {\n    NativeTextOnly,\n    NativeTextThenOcrWhenNeeded,\n}\n```\n\nApp Server 的 Resource adapter 负责在调用此接口前把临时 resource materialize 成\n`StagedPdfPath`。如此 `zeta-pdf` 和 document-library 不依赖 connection lifecycle，\n也不会在 public API 中出现难以理解的 `Option<ResourceId>` 或行为不明的 bool。\n\n持久化记录至少应包含：\n\n```text\nDocument              逻辑身份、标题、当前可用 revision\nDocumentRevision      digest、原始文件位置、页数、导入版本、状态\nPageRecord            revision、PageNumber、文字、Native/OCR 来源、质量与错误\nPdfChunk              content、heading path、token 数、SourceSpan\nIndexPublication      FTS/vector generation、可见状态与失败原因\n```\n\n### 6.3 OCR、chunk 与检索接口\n\nOCR 是页级 fallback，不是默认路径：原生文字为空、乱码或质量低于显式阈值的页才进入\n`ocr_pending`。OCR 返回的 `ExtractionProvenance::Ocr { engine, confidence }` 必须写入\n`PageRecord`，使引用和调试能区分原生文字与识别文字。\n\nchunk 以标题、段落、列表和页面边界优先，保留 `SourceSpan`；第一版可采用目标 token\n范围与明确重叠策略，但不得按字符切断标题与正文。双栏、表格和公式应标记已知限制，\n而不是伪造结构化结果。\n\n检索层接受 `RetrievalQuery`（文本、document scope、revision filter、结果数），返回\n带 `SourceSpan` 的 `RetrievedChunk`。它可以内部组合 FTS/BM25、dense vector、RRF 和\nrerank；这些索引实现不能泄漏为 Agent tool 的稳定参数。Agent 最终只接收证据与引用，\n并与单独检索到的用户 Memory 分栏放入 prompt。\n\n## 7. 错误、安全与资源限制\n\n错误需要结构化而非仅保存 PDFium 文本。最少区分：\n\n```text\npdfium_unavailable   固定运行时缺失、校验或装载失败\ninvalid_pdf          损坏或不支持的 PDF\npassword_required    需要密码；不持久化明文密码\nextraction_failed    原生文字阶段失败\nrender_failed        OCR 前的页面渲染失败\nocr_unavailable      OCR worker/模型不可用\nstorage_failed        source/page/chunk 持久化失败\nindex_failed          FTS/vector 索引发布失败\ncancelled             job 被明确取消\n```\n\nPDF 是不可信输入。导入层应限制文件大小、页数、单页像素和总渲染预算；记录 digest\n与解析器版本；不把调用者本地绝对路径返给 renderer；并尽快把原生解析/OCR 放入可重启\n的隔离 worker，避免畸形 PDF 的 native crash 影响 App Server。密码仅在一次导入操作的\n受保护内存中使用，绝不进入日志、job payload 或检索索引。\n\n## 8. Desktop 与引用跳转\n\nDesktop 只用 `DocumentId + DocumentRevisionId + PageNumber` 请求受控的应用协议 URL，\n再交给 Chromium PDF Viewer 打开。Renderer 不直接获得 source store 路径。\n\nRAG 回答携带的 citation 至少有文档标题、document/revision ID 和一基页码范围；点击\n引用即可打开指定 revision 并跳到该页。文字矩形高亮需要以后补充页级坐标/span 映射，\n它不应阻塞第一版的可靠页码引用。\n\n## 9. 实施顺序与验收\n\n1. **运行时基础（已开始）**：固定 PDFium 下载、SHA-256 校验、发布 stage，及真实\n   PDF 的 bind/extract smoke test。\n2. **持久导入**：materialize Resource、内容哈希、immutable source、revision/job\n   状态及页级原生文字；断开上传连接后 job 仍可完成。\n3. **OCR 与渲染**：仅 fallback 页面渲染；覆盖扫描件、密码、超限与 worker 崩溃重试。\n4. **知识库**：结构化 chunk、FTS/vector 的原子发布、带 source span 的检索与 citation。\n5. **体验**：Desktop 受控预览 URL、进度订阅、引用跳页；后续再考虑坐标高亮。\n\n每一阶段都需要：固定版本 fixture、损坏/密码/空白扫描页 fixture、重试与取消测试，\n以及验证未 ready revision 永远不会出现在检索结果中的集成测试。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-当前基础与发布契约",
        "title": "2. 当前基础与发布契约"
      },
      {
        "depth": 2,
        "id": "3-明确的职责边界",
        "title": "3. 明确的职责边界"
      },
      {
        "depth": 3,
        "id": "31-当前-workbench-阅读器",
        "title": "3.1 当前 Workbench 阅读器"
      },
      {
        "depth": 2,
        "id": "4-身份所有权与引用",
        "title": "4. 身份、所有权与引用"
      },
      {
        "depth": 2,
        "id": "5-导入生命周期",
        "title": "5. 导入生命周期"
      },
      {
        "depth": 2,
        "id": "6-内部接口",
        "title": "6. 内部接口"
      },
      {
        "depth": 3,
        "id": "61-zeta-pdf-保持窄接口",
        "title": "6.1 zeta-pdf 保持窄接口"
      },
      {
        "depth": 3,
        "id": "62-document-library-拥有持久化与编排",
        "title": "6.2 document-library 拥有持久化与编排"
      },
      {
        "depth": 3,
        "id": "63-ocrchunk-与检索接口",
        "title": "6.3 OCR、chunk 与检索接口"
      },
      {
        "depth": 2,
        "id": "7-错误安全与资源限制",
        "title": "7. 错误、安全与资源限制"
      },
      {
        "depth": 2,
        "id": "8-desktop-与引用跳转",
        "title": "8. Desktop 与引用跳转"
      },
      {
        "depth": 2,
        "id": "9-实施顺序与验收",
        "title": "9. 实施顺序与验收"
      }
    ],
    "searchText": " 当前 crate 接口、PDFium binding 与 native smoke test 见 zeta-rs/utils/pdf/README.md。本文拥有 PDF ingestion、OCR、 retrieval 与 citation 的跨系统边界和演进方向。 快速理解 zeta-pdf 是 Zeta 的 PDF 原生处理边界：它通过随安装包发布的 PDFium 读取页面并提取原生文字。页面渲染是为 OCR 准备的 Proposed extension，当前尚未实现。 它不是 PDF 知识库、RAG 或 Agent Memory。持久化的文档、chunk、索引和检索 策略应由上层 document-library / app-server 领域服务拥有；zeta-pdf 只提供 可测试、可替换的 PDF 解析能力。 ``text Desktop 预览 Agent 知识库 ─────────── ───────────── Chromium PDF Viewer document-library / App Server （用户查看） │ ▼ zeta-pdf（PDFium 原生边界） ├─ 原生文字提取 └─ 页面渲染（仅 OCR 需要时） │ ▼ OCR / chunk / FTS / 向量索引 / RAG ` Electron/Chromium 的 PDF Viewer 可以负责普通预览，不需要由 zeta-pdf 渲染页面。只有 Agent 入库、文字定位、扫描页 OCR 或生成缩略图等后端工作才 使用 PDFium。 用户需求 当前路径 当前状态 --- --- --- 在 Desktop 的 Code Workbench 中查看 workspace PDF Chromium PDF Viewer 已实现：Explorer 打开 .pdf，受限读取后以 Blob URL 交给浏览器阅读器 让 Agent 读取原生文字 PDF 处理边界提取逐页文字 已实现 识别扫描版 PDF 渲染页面后交给 OCR 计划设计 建立可搜索知识库 文档库负责身份、切片和索引 尚未实现 从回答跳回原页 文档库保存页码和来源范围 计划设计 2. 当前基础与发布契约 当前 crate 位于 zeta-rs/utils/pdf，包名为 zeta-pdf，Rust crate 名为 zetapdf。它已经提供： - PdfiumRuntime::frombundledroot ：从发布目录解析当前平台的动态库； - PdfTextExtractor::bind ：进程启动时显式绑定 PDFium； - PdfTextExtractor::extractfile ：按页、按原顺序提取原生文字； - ExtractedPdfPage::pagenumber：稳定的 一基页码，可直接用于引用和 Desktop 跳页；空白页也会保留，供上层判定是否需要 OCR。 PDFium 由 scripts/fetch-pdfium.mjs 在 CI / 发布 构建时按 thirdparty/pdfium/runtime-lock.json 锁定的版本下载、校验并 stage 到： `text resources/native/pdfium/ ├── macOS: lib/libpdfium.dylib ├── Windows: bin/pdfium.dll └── Linux: lib/libpdfium.so ` 运行时不搜索系统 PDFium，也不要求最终用户另行安装。App Server 启动时绑定一次， 将 PdfTextExtractor 作为进程内共享服务复用；不能每个导入请求重新装载动态库。 3. 明确的职责边界 层 负责 不负责 --- --- --- zeta-pdf Current：PDFium 绑定、原生文字、PDF 错误；Proposed：页级几何与渲染 文件持久化、OCR 模型、chunk、embedding、检索、Memory document-library（新增领域服务） 文档身份、内容哈希、版本、页面记录、导入状态、chunk 与引用 PDFium 动态库加载、浏览器预览 UI OCR worker 仅对需要识别的渲染页做 OCR，返回文字与置信度 判断文档归属、写检索索引 检索服务 FTS / 向量检索、融合、rerank、过滤 修改源 PDF 或 Agent 记忆 Desktop 导入入口、进度、引用跳转、Chromium PDF 预览 直接保存本地绝对路径、解析不可信 PDF 特别地，PDF 知识库和 Agent Memory 必须分开：PDF chunk 是可复现、可引用的 客观文档证据；Memory 是用户偏好、确认的决策和会变化的项目状态。回答时可以 分别检索两者，但不得把 PDF 片段写成用户 Memory。 3.1 当前 Workbench 阅读器 desktop/src/zeta/workbench/contrib/pdf 是一个 Workbench contribution，不属于 editor 的文本或结构化文档 engine。它匹配 application/pdf 和 .pdf resource；通过 IFileService.readFileBytes 请求 App Server 的 workspace-relative fs/readBinaryFile， 再创建 application/pdf Blob URL 并嵌入 Chromium 的原生 PDF Viewer。 这条路径的边界是刻意的：Renderer 不会收到主机绝对路径，file: URL 也不会进入 PDF 阅读器；后端将预览读取限制为 16 MiB，并经连接所有的 ResourceStore 以 256 KiB 分块读取，避免二进制内容超过 JSONL 帧上限。页面、缩放、搜索、打印与下载由 Chromium PDF Viewer 自己负责，贡献只拥有匹配、加载、可见性和 Blob URL 生命周期。 4. 身份、所有权与引用 内部接口使用下列三个层次的身份，避免把临时上传资源泄漏到持久知识库。 `text ResourceId（连接所有、短暂） │ 导入期间 materialize ▼ DocumentId（持久、面向用户的文档） ── DocumentRevisionId / SHA-256（不可变内容） │ ├─ PageNumber（NonZeroU32；一基） └─ PdfChunkId（持久 chunk） ── SourceSpan revision, page range, offsets ` - ResourceId 是 App Server ResourceStore 的 connection-owned 临时资源。断开 连接或 resource/release 后便不再有效；它绝不能写入 documents、citation 或 vector metadata。 - DocumentId 表示用户看到的一份逻辑文档；DocumentRevisionId 或内容 SHA-256 表示一份不可变的源文件内容。内容哈希用于去重、重新索引和审计。 - PageNumber 以 NonZeroU32 表示，一律一基。任何 PDFium 零基索引转换只能 留在 zeta-pdf 内部。 - 引用只记录 durable source span，例如 documentid, revisionid, pagestart, pageend, charstart, charend ；不要 记录桌面文件路径或临时资源 ID。 源文件在导入开始时必须复制或原子移动到应用拥有的不可变存储，再开始耗时解析。 这也允许连接结束后继续执行 job、断点恢复和可靠地重新索引。 5. 导入生命周期 当前 App Server 只有临时 Resource API，下面的 document/import 和 job/progress 接口均为 Proposed；实现前不得把它们伪装成当前 JSON-RPC 方法。 `text 选择本地 PDF / Resource 上传 │ ▼ materialize 到 app-owned staging（仍可读取 Resource 时完成） │ ▼ 计算 SHA-256 → 去重 / 创建 DocumentRevision │ ▼ 原子写入不可变 source store │ ▼ native extract（zeta-pdf）→ PageRecord │ │ │ 仅空白/低质量页 │ ▼ │ render + OCR ▼ 结构恢复 → chunk → FTS / embedding → 索引发布 │ ▼ DocumentIngestionJob 完成；Desktop 收到进度与可引用 revision ` 解析、OCR 与 embedding 都是可长时间运行且可能重试的工作，不能阻塞一条 JSON-RPC request。document/import 应立即返回 durable DocumentImportReceipt documentid, revisionid, jobid ，进度经 server notification / subscription 报告。只有索引 完整发布后，该 revision 才对检索可见。 建议状态为： `text staging → hashing → extracting → ocrpending? → chunking → indexing → ready └────────→ failed cancelled ` failed 仍保留已持久化 source、失败阶段和可诊断的错误码；其未完成 chunk 不得混入 检索结果。重试创建新的 job，不覆盖已有的可用 revision。 6. 内部接口 6.1 zeta-pdf 保持窄接口 现有的 PdfTextExtractor 是 PDFium adapter。后续扩展应该围绕页面事实，而不是 暴露 PDFium 的对象、FFI handle 或底层页索引给上层： `rust /// Extracts page-level PDF facts from an application-owned immutable source. /// /// Implementations must preserve source page order and return one-based page /// numbers. They do not persist data or decide retrieval policy. pub trait PdfPageExtractor: Send + Sync fn extract &self, source: &PdfSource - Result<ExtractedPdfDocument, PdfError ; pub struct PdfSource pub revisionid: DocumentRevisionId, pub path: OwnedPdfPath, pub contentdigest: ContentDigest, pub struct ExtractedPdfPage pub pagenumber: PageNumber, pub text: String, pub extraction: NativeTextExtraction, ` 这是目标形状，不要求当前立刻引入 trait。当前 PdfTextExtractor 可先作为唯一 PDFium 实现；在确实需要 fixture/fake 或第二个解析后端时，再以该 trait 收口。 PdfSource 只接受应用拥有的文件，不能接受 ResourceId 或 Desktop 路径。 渲染单独使用显式请求类型，避免 renderpage page, true 这类难读的布尔参数： `rust pub enum PdfRenderPurpose Ocr, Thumbnail, pub struct PdfPageRenderRequest pub pagenumber: PageNumber, pub purpose: PdfRenderPurpose, pub maxpixelsize: PixelSize, ` 6.2 document-library 拥有持久化与编排 将导入服务放在上层 crate（建议 zeta-document-library），依赖 zetapdf，而非 反向让工具 crate 依赖 App Server、数据库或向量库： `rust /// Coordinates durable PDF ingestion without exposing storage implementation /// details to callers. pub trait PdfImportService: Send + Sync fn import &self, request: PdfImportRequest - Result<DocumentImportReceipt, ImportError ; pub struct PdfImportRequest pub source: ImportSource, pub duplicatepolicy: DuplicatePolicy, pub extractionmode: TextExtractionMode, pub enum ImportSource OwnedStagingFile path: StagedPdfPath , pub enum DuplicatePolicy ReuseReadyRevision, ReindexExistingRevision, pub enum TextExtractionMode NativeTextOnly, NativeTextThenOcrWhenNeeded, ` App Server 的 Resource adapter 负责在调用此接口前把临时 resource materialize 成 StagedPdfPath。如此 zeta-pdf 和 document-library 不依赖 connection lifecycle， 也不会在 public API 中出现难以理解的 Option<ResourceId 或行为不明的 bool。 持久化记录至少应包含： `text Document 逻辑身份、标题、当前可用 revision DocumentRevision digest、原始文件位置、页数、导入版本、状态 PageRecord revision、PageNumber、文字、Native/OCR 来源、质量与错误 PdfChunk content、heading path、token 数、SourceSpan IndexPublication FTS/vector generation、可见状态与失败原因 ` 6.3 OCR、chunk 与检索接口 OCR 是页级 fallback，不是默认路径：原生文字为空、乱码或质量低于显式阈值的页才进入 ocrpending。OCR 返回的 ExtractionProvenance::Ocr engine, confidence 必须写入 PageRecord，使引用和调试能区分原生文字与识别文字。 chunk 以标题、段落、列表和页面边界优先，保留 SourceSpan；第一版可采用目标 token 范围与明确重叠策略，但不得按字符切断标题与正文。双栏、表格和公式应标记已知限制， 而不是伪造结构化结果。 检索层接受 RetrievalQuery（文本、document scope、revision filter、结果数），返回 带 SourceSpan 的 RetrievedChunk。它可以内部组合 FTS/BM25、dense vector、RRF 和 rerank；这些索引实现不能泄漏为 Agent tool 的稳定参数。Agent 最终只接收证据与引用， 并与单独检索到的用户 Memory 分栏放入 prompt。 7. 错误、安全与资源限制 错误需要结构化而非仅保存 PDFium 文本。最少区分： `text pdfiumunavailable 固定运行时缺失、校验或装载失败 invalidpdf 损坏或不支持的 PDF passwordrequired 需要密码；不持久化明文密码 extractionfailed 原生文字阶段失败 renderfailed OCR 前的页面渲染失败 ocrunavailable OCR worker/模型不可用 storagefailed source/page/chunk 持久化失败 indexfailed FTS/vector 索引发布失败 cancelled job 被明确取消 ` PDF 是不可信输入。导入层应限制文件大小、页数、单页像素和总渲染预算；记录 digest 与解析器版本；不把调用者本地绝对路径返给 renderer；并尽快把原生解析/OCR 放入可重启 的隔离 worker，避免畸形 PDF 的 native crash 影响 App Server。密码仅在一次导入操作的 受保护内存中使用，绝不进入日志、job payload 或检索索引。 8. Desktop 与引用跳转 Desktop 只用 DocumentId + DocumentRevisionId + PageNumber` 请求受控的应用协议 URL， 再交给 Chromium PDF Viewer 打开。Renderer 不直接获得 source store 路径。 RAG 回答携带的 citation 至少有文档标题、document/revision ID 和一基页码范围；点击 引用即可打开指定 revision 并跳到该页。文字矩形高亮需要以后补充页级坐标/span 映射， 它不应阻塞第一版的可靠页码引用。 9. 实施顺序与验收 1. 运行时基础（已开始）：固定 PDFium 下载、SHA-256 校验、发布 stage，及真实 PDF 的 bind/extract smoke test。 2. 持久导入：materialize Resource、内容哈希、immutable source、revision/job 状态及页级原生文字；断开上传连接后 job 仍可完成。 3. OCR 与渲染：仅 fallback 页面渲染；覆盖扫描件、密码、超限与 worker 崩溃重试。 4. 知识库：结构化 chunk、FTS/vector 的原子发布、带 source span 的检索与 citation。 5. 体验：Desktop 受控预览 URL、进度订阅、引用跳页；后续再考虑坐标高亮。 每一阶段都需要：固定版本 fixture、损坏/密码/空白扫描页 fixture、重试与取消测试， 以及验证未 ready revision 永远不会出现在检索结果中的集成测试。"
  },
  {
    "slug": "typst",
    "sourcePath": "docs/typst.md",
    "title": "Typst 文档编译",
    "description": "本文负责跨 crate 的架构与信任模型。编译器实现细节以 zeta-rs/utils/typst/README.md 为准。",
    "group": "界面与体验",
    "markdown": "```yaml\nstatus: current narrow integration\nowner: zeta-rs/utils/typst and zeta-rs/app-server\nconsumers:\n  - desktop\nlastUpdated: 2026-07-28\n```\n\n本文负责跨 crate 的架构与信任模型。编译器实现细节以\n[`zeta-rs/utils/typst/README.md`](../zeta-rs/utils/typst/README.md) 为准。\n\n## 快速理解\n\nZeta 将 Typst 0.15.1 作为 Rust 库嵌入，不调用系统安装的 `typst` 可执行文件。第一阶段能力把\n内存中的 Typst 源码字符串转换为当前连接拥有的 PDF 资源。这样既能让 Agent 编辑文本表示并\n生成排版后的论文，也不需要授予渲染进程或编译器访问宿主路径的权限。\n\n这项能力补充而不取代 Academic 编辑器。Aster Document Engine 拥有结构化论文编辑和 Agent 可见的文档\n状态；确定性序列化器负责把该状态转换为 Typst 源码；Typst 只负责排版和 PDF 输出。legacy editor runtime\n仍然是 Code 产品的编辑器，不属于这条论文处理路径。\n\n| 想完成什么 | 当前能力 | 当前限制 |\n| --- | --- | --- |\n| 把 Typst 源码编译成 PDF | 已实现内存编译和临时 PDF 资源 | 仅支持单文件源码 |\n| 从 Academic 文档生成 Typst | 计划由确定性序列化器完成 | 尚未实现 |\n| 在 Workbench 预览 workspace PDF | Chromium PDF Viewer 贡献 | 已实现；Typst 临时资源尚未桥接到该贡献 |\n| 使用本地文件、网络或外部包 | 明确拒绝 | 保持编译器信任边界 |\n| 保存最终 PDF | 调用方显式读取并导出 | 临时资源不会自动持久化 |\n\n## 所有权与端到端流程\n\n| 组件 | 职责 |\n| --- | --- |\n| `zeta-typst` | 编译器 `World`、内置字体、源码限制、诊断和 PDF 字节 |\n| `zeta-app-server-protocol` | `document/typst/compile` 数据结构与能力协商 |\n| `zeta-app-server` | 请求分发和当前连接拥有的 PDF 资源创建 |\n| Desktop Main/Preload | 精确 IPC 校验和类型化能力桥接 |\n| Academic Workbench 贡献 | Aster Document Engine；未来的 Typst 序列化、诊断、预览和保存/导出 |\n\n计划中的 Academic 渲染流程如下；序列化器和把临时资源打开到 PDF 阅读器的桥接尚未实现：\n\n```text\nAcademic Aster 文档\n→ 确定性 Typst 序列化器\n→ Typst 源码字符串\n→ 沙箱化 Preload API：typst.compile\n→ 可信 Electron Main IPC 路由\n→ document/typst/compile\n→ zeta-typst 内存 World\n→ PDF 字节\n→ App Server ResourceStore\n→ resource/read 分块读取\n→ Workbench PDF 预览或显式导出\n```\n\n编译失败返回 `{ status: \"failed\", diagnostics }`。编译成功返回\n`{ status: \"success\", resource, warnings }`。生成的资源使用 `application/pdf`，生存期为\n300 秒，大小上限为 16 MiB，由当前连接拥有，并沿用现有的 Base64 分块读取契约。\n\n## 安全性与确定性\n\n当前不变量：\n\n- 源码按 UTF-8 字节计算，最大 1 MiB；\n- 只存在虚拟文件 `/main.typ`；\n- 拒绝其他项目文件和包；\n- 不访问网络，也不下载 Typst Universe 包；\n- 不加载系统字体或任意字体文件；\n- 不能读取当前日期；\n- PDF 字节从不获得宿主路径，并始终由当前连接拥有；\n- Typst 及其直接依赖精确固定在 0.15.1。\n\n渲染进程沙箱与编译器边界解决的是不同问题。Electron 沙箱限制渲染进程权限；\n`InMemoryWorld` 限制 Typst 源码可以向 Rust 宿主请求什么。两层边界都必须保留。\n\n## 当前状态\n\n已实现：\n\n- 使用内置字体把 Typst 编译为 PDF；\n- 类型化 App Server 方法、能力、诊断和生成产物；\n- Desktop Preload API 以及资源元数据、读取和释放接口；\n- Academic Aster 文档编辑器面板和产品级注册；\n- 上游 Typst 与内置字体许可证声明；\n- PDF 输出、诊断、所有权和宿主文件访问拒绝的单元及集成测试。\n\n当前限制：\n\n- 不支持多文件项目、图片、参考文献、包导入、系统字体、增量编译、取消或强制 CPU 截止时间；\n- 尚无 Aster Document Engine 到 Typst 的序列化器、诊断投影，或将 Typst 临时资源打开到 PDF 阅读器的桥接；\n- 调用方显式读取并持久化前，输出只是临时资源；\n- 编译当前同步执行，方法被声明为全局独占。\n\n## 分阶段演进\n\n近期计划先实现确定性的 Aster Document Engine 到 Typst 序列化器，再把诊断投影回结构化文档，并把\n返回的临时 PDF 资源交给现有阅读器。\n\n多文件论文后续应使用有大小限制、不可变的内存文件映射。参考文献和图片只能通过显式资源类型、\n总字节数与数量限制、规范化虚拟路径以及无法逃逸项目根目录的测试接入。\n\n如果确实需要包支持，必须另行定义版本固定、下载权限、缓存完整性、许可证声明、离线行为以及\n恶意 WASM/插件隔离策略。这些不属于当前能力。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与端到端流程",
        "title": "所有权与端到端流程"
      },
      {
        "depth": 2,
        "id": "安全性与确定性",
        "title": "安全性与确定性"
      },
      {
        "depth": 2,
        "id": "当前状态",
        "title": "当前状态"
      },
      {
        "depth": 2,
        "id": "分阶段演进",
        "title": "分阶段演进"
      }
    ],
    "searchText": "``yaml status: current narrow integration owner: zeta-rs/utils/typst and zeta-rs/app-server consumers: - desktop lastUpdated: 2026-07-28 ` 本文负责跨 crate 的架构与信任模型。编译器实现细节以 zeta-rs/utils/typst/README.md 为准。 快速理解 Zeta 将 Typst 0.15.1 作为 Rust 库嵌入，不调用系统安装的 typst 可执行文件。第一阶段能力把 内存中的 Typst 源码字符串转换为当前连接拥有的 PDF 资源。这样既能让 Agent 编辑文本表示并 生成排版后的论文，也不需要授予渲染进程或编译器访问宿主路径的权限。 这项能力补充而不取代 Academic 编辑器。Aster Document Engine 拥有结构化论文编辑和 Agent 可见的文档 状态；确定性序列化器负责把该状态转换为 Typst 源码；Typst 只负责排版和 PDF 输出。legacy editor runtime 仍然是 Code 产品的编辑器，不属于这条论文处理路径。 想完成什么 当前能力 当前限制 --- --- --- 把 Typst 源码编译成 PDF 已实现内存编译和临时 PDF 资源 仅支持单文件源码 从 Academic 文档生成 Typst 计划由确定性序列化器完成 尚未实现 在 Workbench 预览 workspace PDF Chromium PDF Viewer 贡献 已实现；Typst 临时资源尚未桥接到该贡献 使用本地文件、网络或外部包 明确拒绝 保持编译器信任边界 保存最终 PDF 调用方显式读取并导出 临时资源不会自动持久化 所有权与端到端流程 组件 职责 --- --- zeta-typst 编译器 World、内置字体、源码限制、诊断和 PDF 字节 zeta-app-server-protocol document/typst/compile 数据结构与能力协商 zeta-app-server 请求分发和当前连接拥有的 PDF 资源创建 Desktop Main/Preload 精确 IPC 校验和类型化能力桥接 Academic Workbench 贡献 Aster Document Engine；未来的 Typst 序列化、诊断、预览和保存/导出 计划中的 Academic 渲染流程如下；序列化器和把临时资源打开到 PDF 阅读器的桥接尚未实现： `text Academic Aster 文档 → 确定性 Typst 序列化器 → Typst 源码字符串 → 沙箱化 Preload API：typst.compile → 可信 Electron Main IPC 路由 → document/typst/compile → zeta-typst 内存 World → PDF 字节 → App Server ResourceStore → resource/read 分块读取 → Workbench PDF 预览或显式导出 ` 编译失败返回 status: \"failed\", diagnostics 。编译成功返回 status: \"success\", resource, warnings 。生成的资源使用 application/pdf，生存期为 300 秒，大小上限为 16 MiB，由当前连接拥有，并沿用现有的 Base64 分块读取契约。 安全性与确定性 当前不变量： - 源码按 UTF-8 字节计算，最大 1 MiB； - 只存在虚拟文件 /main.typ； - 拒绝其他项目文件和包； - 不访问网络，也不下载 Typst Universe 包； - 不加载系统字体或任意字体文件； - 不能读取当前日期； - PDF 字节从不获得宿主路径，并始终由当前连接拥有； - Typst 及其直接依赖精确固定在 0.15.1。 渲染进程沙箱与编译器边界解决的是不同问题。Electron 沙箱限制渲染进程权限； InMemoryWorld` 限制 Typst 源码可以向 Rust 宿主请求什么。两层边界都必须保留。 当前状态 已实现： - 使用内置字体把 Typst 编译为 PDF； - 类型化 App Server 方法、能力、诊断和生成产物； - Desktop Preload API 以及资源元数据、读取和释放接口； - Academic Aster 文档编辑器面板和产品级注册； - 上游 Typst 与内置字体许可证声明； - PDF 输出、诊断、所有权和宿主文件访问拒绝的单元及集成测试。 当前限制： - 不支持多文件项目、图片、参考文献、包导入、系统字体、增量编译、取消或强制 CPU 截止时间； - 尚无 Aster Document Engine 到 Typst 的序列化器、诊断投影，或将 Typst 临时资源打开到 PDF 阅读器的桥接； - 调用方显式读取并持久化前，输出只是临时资源； - 编译当前同步执行，方法被声明为全局独占。 分阶段演进 近期计划先实现确定性的 Aster Document Engine 到 Typst 序列化器，再把诊断投影回结构化文档，并把 返回的临时 PDF 资源交给现有阅读器。 多文件论文后续应使用有大小限制、不可变的内存文件映射。参考文献和图片只能通过显式资源类型、 总字节数与数量限制、规范化虚拟路径以及无法逃逸项目根目录的测试接入。 如果确实需要包支持，必须另行定义版本固定、下载权限、缓存完整性、许可证声明、离线行为以及 恶意 WASM/插件隔离策略。这些不属于当前能力。"
  },
  {
    "slug": "chat-session-inspector",
    "sourcePath": "docs/chat-session-inspector.md",
    "title": "Chat Session Inspector：信息架构与分阶段设计",
    "description": "Chat 右上角的辅助入口应打开 Session Inspector，而不是第二套 Session 导航。",
    "group": "界面与体验",
    "markdown": "> 状态：Proposed。本文定义 Chat 内 Session Inspector 的产品语义、跨层所有权、\n> Plan 投影边界、响应式交互和分阶段落地顺序；不表示这些能力已经完成。\n> Desktop 的进程与权威状态边界以\n> [`zeta-desktop-architecture.md`](zeta-desktop-architecture.md) 为准；\n> Session、Thread、Turn 与 ThreadItem 的 canonical 语义以\n> [`protocol.md`](protocol.md) 为准；\n> App Server 的订阅和恢复契约以\n> [`zeta-app-server-api.md`](zeta-app-server-api.md) 为准；\n> Renderer 组件与 CSS 所有权以\n> [`ui-styling-ownership.md`](ui-styling-ownership.md) 为准。\n\n## 快速理解\n\nChat 右上角的辅助入口应打开 **Session Inspector**，而不是第二套 Session 导航。\n\n产品层级固定为：\n\n```text\nAuxiliary Bar / Chat\n├── Session tabs                 切换 Session\n├── 当前 Session 的 ChatPane\n│   └── 当前 Thread transcript\n└── Session Inspector\n    └── 当前 Thread 的 Plan、状态和辅助投影\n```\n\n第一阶段只实现 **Plan**，后续再按真实后端 contract 增加 Threads、Activity、Changes、\nContext 和 Artifacts。无论增加多少功能，Inspector 都只跟随当前\n`Session + Thread`，不得自己维护另一份 Session 选择状态。\n\n关键决策如下：\n\n| 问题 | 决策 |\n| --- | --- |\n| Inspector 的作用域 | 当前 `Session + Thread` |\n| Session 导航 | 继续由 Chat 顶部 tabs 和 History 入口拥有 |\n| 第一项功能 | 当前 Thread 的 Plan |\n| Plan 的权威 owner | Rust canonical Thread/Turn model |\n| Renderer 的职责 | 可丢弃、可重建的 Plan 视图投影 |\n| 第一阶段数据入口 | `ThreadUpdate::PlanUpdated` contract；production emitter 尚未接通 |\n| 第一阶段恢复限制 | 重连或重新选择后无法从 snapshot 恢复结构化 Plan |\n| 生产完整形态 | `session/thread/read` 必须返回可恢复的结构化 Plan |\n| Inspector 对 Plan 的修改 | 第一阶段只读；不直接勾选或静默改变步骤 |\n| 订阅策略 | Transcript 与 Inspector 共享已有 Thread projection，不建立第二条独立订阅 |\n| 窄宽度行为 | 抽屉/覆盖层；不得把 Chat 正文压缩到不可用宽度 |\n| 宽布局行为 | 空间足够时可并排显示 |\n\n## 2. 要解决的问题\n\n当前 Chat 已经同时出现三个容易混淆的导航概念：\n\n- Workbench 的 Auxiliary Bar 承载整个 Chat；\n- Chat title tabs 映射活动 Session；\n- Chat 内还有一个名为 Agent Sidebar 的嵌套区域和开关。\n\n如果嵌套区域再次按 Session 组织内容，用户无法判断顶部 tabs 和内部 Sidebar 谁拥有主导航，\n关闭、切换和恢复的语义也会重复。另一方面，Agent 执行过程中确实存在适合持续可见、但不适合\n反复插入 transcript 的辅助状态，Plan 是当前最明确的真实消费者。\n\n本设计解决：\n\n1. 固定 Session、Thread 和 Inspector 的导航层级；\n2. 让 Plan 在当前工作期间持续可见；\n3. 保持 Plan 权威状态与 Renderer 展示分离；\n4. 防止第二条订阅、文本解析或本地持久化形成冲突状态；\n5. 为后续辅助能力提供受控扩展方向。\n\n## 3. 不包含的目标\n\nSession Inspector 不负责：\n\n- 展示或管理所有 Session 历史；\n- 替代 Chat title tabs、History Quick Pick 或未来专用 Session 页面；\n- 执行 Agent 规划、判断步骤是否完成或修改 canonical Plan；\n- 从 Markdown、Agent 消息或 Tool Result 猜测结构化 Plan；\n- 承载完整 Terminal、Browser、Settings 或 Editor；\n- 仅凭 Git working tree 将文件变化归因到某个 Session；\n- 在 Renderer 中持久化一份服务端无法验证的 Plan；\n- 把 `ViewContainerLocation.AgentSidebar` 扩展成没有真实消费者的通用插件平台。\n\n## 4. 当前实现状态\n\n以下均为 Current，可从 Desktop 与生成协议代码验证：\n\n| 能力 | 当前状态 | 边界 |\n| --- | --- | --- |\n| Chat 注册到 Auxiliary Bar | ✅ | Chat 是独立 View contribution |\n| 一个活动 Session 对应一个 Chat tab 和保留的 `ChatPane` | ✅ | tab 只切 Session |\n| `IWorkbenchSessionService` 统一保存当前 Session/Thread 选择 | ✅ | 不拥有 transcript |\n| Chat title 提供 Agent Sidebar toggle | ✅ | 默认隐藏 |\n| Chat 内创建嵌套 `SidebarPart` | ✅ | 当前没有可用的 Agent Sidebar 内容装配 |\n| `ViewContainerLocation.AgentSidebar` | ✅ 扩展点 | 尚无真实 ViewContainer consumer |\n| durable `ThreadItem::Plan` 类型与 reducer | ✅ | 只有 Markdown `text`；尚未发现 production producer |\n| `PlanUpdate { explanation, steps }` | ✅ | 结构化步骤状态 |\n| `PlanStepStatus` | ✅ | 只有 `pending`、`inProgress`、`completed` |\n| `ThreadUpdate::PlanUpdated` 类型与 broker routing | ✅ | transient；尚未发现 production producer |\n| `ChatPaneModel` 接收 `planUpdated` | 部分具备 | 当前分支明确忽略该 update |\n| Chat transcript 渲染 Plan item | ✅ | 收到 item 时渲染 Markdown，不提供结构化步骤投影 |\n| 结构化 Plan 从 `session/thread/read` 恢复 | ❌ | `Thread` / `Turn` snapshot 当前没有该字段 |\n\n因此，“把 Plan 放进 Inspector”已有协议和 Renderer 切入点，但 P1 仍需接通真实\n`planUpdated` producer。即使完成实时 vertical slice，也不能把它描述为跨重启、重连或重新\n选择后可靠恢复。\n\n## 5. 用户可见信息架构\n\n### 5.1 导航归属\n\n| 用户动作 | 唯一 owner | Inspector 的反应 |\n| --- | --- | --- |\n| 切换 Session | Chat title tabs / History | 跟随新 Session 的当前 Thread |\n| 切换 Thread | Thread navigation | 跟随新 Thread |\n| 显示或隐藏辅助内容 | Session Inspector toggle | 不改变 Session/Thread 选择 |\n| 关闭 Session | Session tab | Inspector 释放该 Session 投影 |\n| 更新 Plan | Agent runtime / canonical Thread | Inspector 只刷新展示 |\n\nSession 不得被注册为 Inspector 内的 ViewContainer、ViewPane 或 tab。未来 Inspector 如果需要\n内部导航，导航项必须是 Plan、Threads、Activity、Changes 等**功能视图**，而不是 Session\n实例。\n\n### 5.2 目标布局\n\nSession Inspector 的目标信息布局如下。阶段标签表达实施顺序，不表示所有区块会在 P1\n同时出现：\n\n```text\nSession Inspector\n│\n├── Plan                                      P1 / P2\n│   ├── ✓ Understand terminal layout\n│   ├── ● Implement tabs behavior\n│   ├── ○ Add tests\n│   └── ○ Verify UI\n│\n├── Threads                                   P3\n│   ├── ● Main Thread\n│   └── ○ Layout investigation\n│\n├── Activity                                  P3\n│   ├── Turn: running\n│   └── Waiting: none\n│\n├── Changes                                   P4\n│   ├── M terminalViewPane.ts\n│   ├── M terminalTabsLayout.ts\n│   └── M terminal.css\n│\n├── Context                                   P4\n│   ├── Workspace: zeta\n│   ├── AGENTS.md\n│   └── 3 referenced files\n│\n└── Artifacts                                 P4\n    └── Generated outputs\n```\n\n布局规则：\n\n- Plan 始终位于首位，是 P1 唯一必须实现的内容；\n- Threads 只展示当前 Session 内的 Thread，不展示其他 Session；\n- Activity 与 Plan 分开：Turn 等待、审批和运行状态不能伪装成 Plan step 状态；\n- Changes 只有具备 Session/Thread attribution 后才能出现；\n- Context 只展示 canonical context/attachment projection，不从 transcript 猜测；\n- Artifacts 只有具备稳定 identity、lifecycle 和 open contract 后才能出现；\n- 尚未达到对应阶段的区块不渲染，不用 disabled placeholder 提前占位；\n- 多个区块同时存在时使用纵向 section；Plan 默认展开，其余区块允许折叠；\n- 折叠状态是可丢弃的 Renderer UI 状态，不能写入 Session、Thread 或 Plan。\n\n### 5.3 Plan 视图\n\nPlan 默认显示在 Inspector 的第一个位置：\n\n```text\nPLAN\n\n说明文本（可选）\n\n✓ Understand terminal layout\n● Implement tabs behavior\n○ Add tests\n○ Verify UI\n```\n\n展示规则：\n\n- 保留 `PlanUpdate.steps` 的服务端顺序；\n- `pending`、`inProgress`、`completed` 使用不同图标、文本和稳定状态 class；\n- 不推断 `blocked` step；等待审批、用户输入或 capability 属于 Turn/Activity 状态；\n- 不因为 UI 认为计划异常而重排、去重或自动修正步骤；\n- `explanation` 为空时不保留空白占位；\n- 第一个版本只读，不提供可勾选 checkbox；\n- 没有结构化 Plan 时显示 `No active plan`，不得回退解析 Plan Markdown；\n- Thread 切换、stream incarnation 变化、更新空洞或 reconnect 时，实时 Plan 必须清空，\n  直到收到新 `planUpdated` 或未来从 durable snapshot 恢复。\n\n当前 `PlanStep` 没有 step ID，也没有 transcript item correlation，因此第一阶段不支持：\n\n- 点击步骤精确跳转到对应消息或 Tool Call；\n- 对单个步骤做稳定的跨更新动画 identity；\n- 用户直接修改单个步骤；\n- 将重复文本步骤视为同一个步骤。\n\n这些交互只有在 canonical contract 增加稳定 identity/correlation 后才能设计。\n\n### 5.4 空、加载和错误状态\n\n| 状态 | 展示 |\n| --- | --- |\n| 当前 Thread 正在加载 | 与 Chat 共用 projection loading 状态；显示轻量 loading |\n| 已加载但没有 Plan | `No active plan` |\n| 收到实时 Plan | 显示 explanation 和步骤 |\n| Thread 切换 | 立即移除旧 Thread Plan，再绑定新投影 |\n| reconnect / stream gap | 清除不可恢复的实时 Plan，等待 resync |\n| Thread projection error | 显示与 Chat 一致的可恢复错误，不伪造旧 Plan |\n| 没有活动 Session | `Start a new chat to view its plan` |\n\n不得使用上一个 Session 或 Thread 的 Plan 填充 loading/empty 状态。\n\n## 6. 响应式布局\n\n当前 Chat 通常位于较窄的 Auxiliary Bar。现有固定 220px 的嵌套 Sidebar 会显著压缩 transcript，\n因此目标布局不能始终并排。\n\n| Chat 可用宽度 | Proposed 行为 |\n| --- | --- |\n| 小于 720px | Inspector 作为右侧抽屉覆盖 Chat body，默认隐藏 |\n| 大于等于 720px | Inspector 可与 Chat body 并排，建议宽 280px |\n| 无活动 Session | toggle 可保留，但打开后只显示 empty state |\n\n布局应保证：\n\n- Chat 正文并排时至少保留 360px；\n- Inspector 建议最小宽度 240px；\n- 抽屉打开后可通过 toggle、关闭按钮或 `Escape` 关闭；\n- 抽屉不改变 Session/Thread 选择；\n- focus 进入抽屉后关闭应返回触发按钮；\n- 是否采用 split 或 drawer 由 Chat host 可用宽度决定，不写入 durable settings。\n\nInspector 是 Chat contribution 内的局部布局区域，不是新的 Workbench Part。是否复用基础\nprimitive 由实现决定，但不应继续用一个完整 `SidebarPart` 嵌套另一个 Workbench\nAuxiliary Bar 来获得视觉效果。\n\n## 7. 组件与数据所有权\n\n### 7.1 跨层所有权\n\n| 组件/层 | 拥有 | 明确不拥有 |\n| --- | --- | --- |\n| Rust Protocol / Core | Plan 结构、状态词汇、Turn 归属、durable 恢复语义 | Desktop 布局与展开状态 |\n| App Server | snapshot、subscribe/update delivery、sequence 与 reconnect contract | Inspector 是否可见 |\n| `IWorkbenchSessionService` | 当前 Session/Thread 选择和 Session topology projection | Thread transcript 与 Plan |\n| Thread projection owner | 一个 Thread 的 snapshot、transient items、Plan live state 和订阅生命周期 | Session tabs 和 Inspector 布局 |\n| `ChatViewPane` | Session tabs、当前 ChatPane、Inspector 外部布局和 active projection 绑定 | Plan 状态判断 |\n| `SessionInspector` | 局部导航、empty/loading 呈现、focus 与响应式行为 | Session 选择和 App Server 调用 |\n| Plan view/widget | Plan DOM、ARIA、步骤状态投影 | 修改 canonical Plan |\n\n### 7.2 单一 Thread 订阅\n\n当前每个保留的 `ChatPaneModel` 已经拥有选中 Thread 的 `thread.subscribe`、`thread.read`、\nstream cursor 和 reconnect 处理。Inspector 不得再为同一 Thread 独立调用\n`thread.subscribe`，否则两个消费者的 unsubscribe、cursor 和 reconnect 生命周期会相互干扰。\n\n第一阶段应复用现有 projection：\n\n```text\nthread.subscribe / thread.read / thread update\n  → ChatPaneModel（唯一订阅 owner）\n  ├── transcript items\n  └── live Plan projection\n       → ChatPane / ChatViewPane\n       → SessionInspector\n       → Plan view\n```\n\n如果 Plan、Activity、Changes 等消费者增长到 Chat 私有 model 不再合适，应提取共享的\nThread projection service。提取前必须有至少两个真实消费者，并保持以下 contract：\n\n- acquisition/release 不导致一个消费者释放另一个消费者仍在使用的订阅；\n- 同一 Thread 只有一个 stream cursor authority；\n- reconnect 与 gap recovery 统一执行；\n- UI consumer 只能读取 projection，不直接写 canonical state。\n\n不得为了让 Inspector 快速工作而把 `ChatPaneModel` 整体注册成全局 service，或让\n`SessionInspector` 通过 DOM 查找当前 ChatPane。\n\n## 8. Plan 的可靠性与持久性\n\n### 8.1 当前契约\n\n当前存在两种不同的 Plan 表示：\n\n| 表示 | 结构 | Durability | 当前用途 |\n| --- | --- | --- | --- |\n| `ThreadItem::Plan` | `text: String` | durable | transcript 中的 Plan 文本 |\n| `ThreadUpdate::PlanUpdated` | `PlanUpdate { explanation, steps }` | transient | 低延迟结构化进度 |\n\n两者不能互相冒充。Renderer 解析 Markdown 会丢失状态、顺序语义和未来兼容性；把 transient\nPlan 写入 local storage 又会产生服务端无法确认的陈旧状态。\n\n### 8.2 计划中的持久化契约\n\n生产完整版本应让 Plan 成为 Turn 的可恢复 readable state。推荐方向是：\n\n```text\nTurn {\n  ...\n  plan?: PlanUpdate\n}\n```\n\n并由明确的 durable Thread event 更新该字段。具体 Rust variant 与迁移由 Protocol/Core\n实现评审确定，但必须满足：\n\n1. event 足以从空状态重建 `Turn.plan`；\n2. `session/thread/read` 和 `session/thread/subscribe` snapshot 返回同一结构化 Plan；\n3. `planUpdated` 可以继续作为低延迟 preview，但不能成为唯一恢复来源；\n4. preview 被 committed state 覆盖，而不是覆盖 committed state；\n5. Plan 归属 `TurnId`，不提升为 Session 级全局状态；\n6. completed Turn 可以保留最终 Plan，供 Inspector 显示 `Last plan`；\n7. 新活动 Turn 没有 Plan 时，不自动继承上一 Turn 的可编辑状态；\n8. status 词汇的新增或约束由 Rust canonical contract 校验，Renderer 不自行扩展。\n\n在该 contract 落地前，Desktop 文档、UI 和测试必须继续把 Plan Inspector 标为实时投影。\n\n## 9. Renderer 组件设计\n\n目标组件边界为：\n\n```text\nChatViewPane\n├── ChatTitleControl\n├── active ChatPane\n└── SessionInspector\n    └── PlanView\n```\n\n`SessionInspector` 是 Chat contribution 拥有的 composed control，不是通用 base 组件。只有当\n第二个领域出现相同的、与 Session 无关的交互 contract 后，才考虑下沉通用 primitive。\n\n建议状态输入使用带语义的联合类型，而不是多个 bool：\n\n```ts\ntype PlanPresentation =\n  | { readonly phase: \"loading\" }\n  | { readonly phase: \"empty\" }\n  | { readonly phase: \"ready\"; readonly plan: PlanUpdate }\n  | { readonly phase: \"error\"; readonly message: string };\n```\n\n这是 Proposed 示例，用于说明调用方可读性，不表示当前 API 已经存在。\n\n### 9.1 Action 与上下文状态\n\n用户可见名称应从泛化的 `Agent Sidebar` 收敛为 `Session Inspector`：\n\n- `Show Session Inspector`\n- `Hide Session Inspector`\n- toolbar `aria-label` 保持 `Chat layout`\n\n最终只允许一个 visible context state。若保留旧 command ID 用于兼容，它必须委托同一命令和\n同一 context key，不得产生 Agent Sidebar 与 Session Inspector 两套可见状态。\n\n### 9.2 CSS 所有权\n\n| 样式 | Owner |\n| --- | --- |\n| Chat body 与 Inspector 的 split/drawer 几何 | `ChatViewPane` / Chat layout CSS |\n| Inspector 内部 header、section 和 focus | `SessionInspector` |\n| Plan step 排列、status class 与图标 | Plan view |\n| toggle checked 状态 | Action → Button 的 `.checked` 状态投影 |\n| 颜色和尺寸值 | semantic design token |\n\n视觉 selector 使用稳定 class，例如 `.in-progress`、`.completed`；ARIA attribute 只表达\n无障碍语义，不作为 CSS 状态 selector。Host CSS 不穿透修改共享 Button、TabList 或\nScrollableElement 的内部状态。\n\n### 9.3 无障碍\n\n- Inspector root 使用明确的 `aria-label=\"Session Inspector\"`；\n- Plan 使用有序列表语义；\n- 每个步骤同时提供可读状态文本，不能只靠颜色或图标；\n- `inProgress` 可使用 `aria-current=\"step\"` 表达当前位置，但 CSS 仍选择状态 class；\n- transcript 的 `role=\"log\"` 不应因 Plan Inspector 更新而重复播报整个列表；\n- drawer 模式必须有可靠的 focus return 和 `Escape` 行为；\n- loading、empty 和 error 不使用持续刷新的 `aria-live=\"assertive\"`。\n\n## 10. 后续功能边界\n\nPlan 之外的功能只有在数据 owner 明确后才能加入：\n\n| 功能 | 适合度 | 前置条件 | 建议阶段 |\n| --- | --- | --- | --- |\n| Plan | ✅ | 当前 transient contract 已有；durable contract 后续补齐 | P1/P2 |\n| Threads | ✅ | Session topology、Thread title/read 和选择交互统一 | P3 |\n| Activity | ✅ | 从 Turn status 与 tool events 做只读投影 | P3 |\n| Changes | 适合，但尚未完成 | 必须有 Session/Thread attribution，不能只读全局 Git diff | P4 |\n| Context | 适合，但尚未完成 | canonical context/attachment projection | P4 |\n| Artifacts | 适合，但尚未完成 | artifact identity、lifecycle 与 open contract | P4 |\n| Session history | ❌ | 已由 tabs/History 拥有 | 不进入 Inspector |\n| 完整 Terminal/Browser | ❌ | 属于现有 Workbench surface | 不进入 Inspector |\n\nThreads 进入 Inspector 后仍是当前 Session 内的 Thread topology，不是第二份 Session history。\n只有后端能证明变化与当前 Session/Thread 的关联时，变更（Changes）才能显示为“本次会话\n修改”；否则只能叫“工作区变更”，并留在 SCM/Files 等已有接口面。\n\n## 11. 被拒绝的替代方案\n\n| 方案 | 判断 | 原因 |\n| --- | --- | --- |\n| Inspector 内再列所有 Session | ❌ | 与 Chat tabs/History 重复，产生双主导航 |\n| 从最新 Plan Markdown 解析步骤 | ❌ | 非 canonical，状态和兼容性不可恢复 |\n| 把实时 Plan 写入 Renderer local storage | ❌ | 形成无法由服务端确认的陈旧 authority |\n| Inspector 自己订阅当前 Thread | ❌ | 重复 subscription、unsubscribe 和 cursor authority |\n| 用 `blocked` 扩展前端 step 状态 | ❌ | 当前 protocol 没有该状态 |\n| 用户直接勾选 Plan step | 尚不采用 | 当前没有修改 canonical Plan 的 command contract |\n| 始终固定 220px 并排 | ❌ | Auxiliary Bar 中会使 Chat 正文不可用 |\n| 永久嵌套完整 `SidebarPart` | 暂不采用 | Inspector 是 Chat 局部区域，尚无独立 Workbench composite 需求 |\n| 为未来插件预建多级 ViewContainer | 暂不采用 | 当前只有 Plan 这个真实消费者 |\n\n## 12. 分阶段实施\n\n### P1：实时 Plan Inspector\n\n目标：接通 `planUpdated` producer，并做正确但明确可丢失的实时展示。\n\n实施范围：\n\n1. 让 Agent execution vertical slice 产生真实 `PlanUpdate`，并通过现有 broker 发布\n   `planUpdated`；\n2. 将用户可见名称改为 Session Inspector；\n3. 用 Chat-owned `SessionInspector` 替换空的嵌套 Sidebar host；\n4. 让现有 Thread projection 保存当前 `PlanUpdate`；\n5. 将 active ChatPane projection 绑定给 Inspector，不增加订阅；\n6. Thread/session 切换和 reconnect/gap 时清除旧实时 Plan；\n7. 实现 drawer/split 响应式布局；\n8. 增加 producer、model、view、toggle、focus 和跨 Session 隔离测试。\n\n完成条件：\n\n- 真实 Agent Plan update 能从 runtime 到达 Desktop，而不依赖测试 fixture；\n- 一个 Session 的 Plan 不会出现在另一个 Session/Thread；\n- `planUpdated` 顺序和状态被原样显示；\n- 隐藏 Inspector 不停止 Chat 的 Thread subscription；\n- Inspector 打开或关闭不改变当前 Session/Thread；\n- 重连后不显示无法确认的旧 Plan；\n- 窄 Auxiliary Bar 中 Chat 正文不会被固定侧栏永久压缩；\n- UI 不解析 transcript Markdown。\n\n### P2：可恢复的结构化 Plan\n\n目标：刷新、重连和重新选择 Thread 后仍能从 canonical snapshot 恢复 Plan。\n\n实施范围：\n\n1. Protocol/Core 接受 durable Turn Plan contract；\n2. App Server schema 与生成 TypeScript 同步更新；\n3. reducer/store/recovery 证明 event 可重建 `Turn.plan`；\n4. `session/thread/read` / subscribe snapshot 返回结构化 Plan；\n5. Desktop projection 先应用 snapshot，再接受 transient preview；\n6. 增加 Rust contract/store/recovery 与 Desktop reconnect 测试。\n\n完成条件：\n\n- 丢弃所有 transient update 后，Inspector 仍能从 snapshot 显示最后 committed Plan；\n- stream gap 不会让旧 preview 覆盖 committed Plan；\n- App Server crash/reconnect 后 Plan 与 Thread snapshot 一致；\n- completed Turn 的最终 Plan 显示语义明确。\n\n### P3：Threads 与活动\n\n目标：增加已有 canonical 数据能够支持的辅助投影。\n\n- Threads 只展示当前 Session membership/lineage；\n- Activity 展示当前 Turn status、等待类型和运行状态；\n- 不复制 transcript 中完整 Tool Call/Result 内容；\n- 不改变 Session tabs 的主导航职责。\n\n### P4：变更、上下文与产物\n\n这些能力必须分别先完成 attribution、identity 和 lifecycle contract。不能为了填充 Inspector\n而从 DOM、Git 全局状态或聊天文本推断。\n\n## 13. 测试与验收\n\n### 13.1 P1 自动化测试\n\n| 测试层 | 必须覆盖 |\n| --- | --- |\n| Projection unit | 接收正确 Thread 的 `planUpdated`、忽略其他 Thread、switch/reset/gap/reconnect |\n| Chat view | toggle、active Session 切换、empty/loading/error、无第二条 subscribe |\n| Plan view | explanation、三种 status、顺序、重复 step 文本、空 steps |\n| Accessibility | label、ordered list、状态文本、focus return、Escape |\n| Responsive policy | width breakpoint 对应 drawer/split，不依赖 JSDOM 实际布局猜测 |\n| Regression | Session tabs、composer draft retention、archive、transcript scroll 不受影响 |\n\n### 13.2 P2 契约测试\n\n- durable Plan event serde/schema；\n- reducer 从空状态重建 `Turn.plan`；\n- store restart recovery；\n- subscribe snapshot + committed gap；\n- transient preview 与 committed Plan 的覆盖顺序；\n- Desktop reconnect 和 stream cursor incarnation 变化。\n\n### 13.3 人工验收场景\n\n1. 在 Session A 运行一个包含 Plan 的任务并打开 Inspector；\n2. 切换到没有 Plan 的 Session B，确认不泄漏 A 的内容；\n3. 返回 A，P1 应按实时限制展示 empty 或新 update，P2 应从 snapshot 恢复；\n4. 在窄 Auxiliary Bar 打开 Inspector，确认以 drawer 呈现；\n5. 在未来 Editor/New Window 宽布局打开 Chat，确认可以 split；\n6. 中断、失败、等待审批时，step status 不被 Renderer 擅自改写。\n\n## 14. 长期不变量\n\n无论实现如何演进，都必须保持：\n\n1. Session tabs 是 Chat 的 Session 主导航，Inspector 不复制它；\n2. Inspector 的内容始终绑定当前 `Session + Thread`；\n3. Plan 语义和 durability 属于 Rust canonical model，Renderer 只是 projection；\n4. transient update 可以全部丢失而不损坏 authoritative state；\n5. 一个 Thread 只有一个客户端 subscription/cursor authority；\n6. Inspector 可见性是可丢弃 UI 状态，不进入 Session/Thread canonical model；\n7. 用户可见状态不能由 Markdown、DOM 或全局 Git 状态猜测；\n8. 新增 Inspector 功能前必须先说明 owner、作用域、恢复语义和已有 surface 边界；\n9. 窄布局不能为了显示辅助信息而永久破坏 Chat 的主要输入与 transcript；\n10. UI 状态 class、ARIA 与 CSS owner 遵循 Renderer styling canonical contract。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-要解决的问题",
        "title": "2. 要解决的问题"
      },
      {
        "depth": 2,
        "id": "3-不包含的目标",
        "title": "3. 不包含的目标"
      },
      {
        "depth": 2,
        "id": "4-当前实现状态",
        "title": "4. 当前实现状态"
      },
      {
        "depth": 2,
        "id": "5-用户可见信息架构",
        "title": "5. 用户可见信息架构"
      },
      {
        "depth": 3,
        "id": "51-导航归属",
        "title": "5.1 导航归属"
      },
      {
        "depth": 3,
        "id": "52-目标布局",
        "title": "5.2 目标布局"
      },
      {
        "depth": 3,
        "id": "53-plan-视图",
        "title": "5.3 Plan 视图"
      },
      {
        "depth": 3,
        "id": "54-空加载和错误状态",
        "title": "5.4 空、加载和错误状态"
      },
      {
        "depth": 2,
        "id": "6-响应式布局",
        "title": "6. 响应式布局"
      },
      {
        "depth": 2,
        "id": "7-组件与数据所有权",
        "title": "7. 组件与数据所有权"
      },
      {
        "depth": 3,
        "id": "71-跨层所有权",
        "title": "7.1 跨层所有权"
      },
      {
        "depth": 3,
        "id": "72-单一-thread-订阅",
        "title": "7.2 单一 Thread 订阅"
      },
      {
        "depth": 2,
        "id": "8-plan-的可靠性与持久性",
        "title": "8. Plan 的可靠性与持久性"
      },
      {
        "depth": 3,
        "id": "81-当前契约",
        "title": "8.1 当前契约"
      },
      {
        "depth": 3,
        "id": "82-计划中的持久化契约",
        "title": "8.2 计划中的持久化契约"
      },
      {
        "depth": 2,
        "id": "9-renderer-组件设计",
        "title": "9. Renderer 组件设计"
      },
      {
        "depth": 3,
        "id": "91-action-与上下文状态",
        "title": "9.1 Action 与上下文状态"
      },
      {
        "depth": 3,
        "id": "92-css-所有权",
        "title": "9.2 CSS 所有权"
      },
      {
        "depth": 3,
        "id": "93-无障碍",
        "title": "9.3 无障碍"
      },
      {
        "depth": 2,
        "id": "10-后续功能边界",
        "title": "10. 后续功能边界"
      },
      {
        "depth": 2,
        "id": "11-被拒绝的替代方案",
        "title": "11. 被拒绝的替代方案"
      },
      {
        "depth": 2,
        "id": "12-分阶段实施",
        "title": "12. 分阶段实施"
      },
      {
        "depth": 3,
        "id": "p1实时-plan-inspector",
        "title": "P1：实时 Plan Inspector"
      },
      {
        "depth": 3,
        "id": "p2可恢复的结构化-plan",
        "title": "P2：可恢复的结构化 Plan"
      },
      {
        "depth": 3,
        "id": "p3threads-与活动",
        "title": "P3：Threads 与活动"
      },
      {
        "depth": 3,
        "id": "p4变更上下文与产物",
        "title": "P4：变更、上下文与产物"
      },
      {
        "depth": 2,
        "id": "13-测试与验收",
        "title": "13. 测试与验收"
      },
      {
        "depth": 3,
        "id": "131-p1-自动化测试",
        "title": "13.1 P1 自动化测试"
      },
      {
        "depth": 3,
        "id": "132-p2-契约测试",
        "title": "13.2 P2 契约测试"
      },
      {
        "depth": 3,
        "id": "133-人工验收场景",
        "title": "13.3 人工验收场景"
      },
      {
        "depth": 2,
        "id": "14-长期不变量",
        "title": "14. 长期不变量"
      }
    ],
    "searchText": " 状态：Proposed。本文定义 Chat 内 Session Inspector 的产品语义、跨层所有权、 Plan 投影边界、响应式交互和分阶段落地顺序；不表示这些能力已经完成。 Desktop 的进程与权威状态边界以 zeta-desktop-architecture.md 为准； Session、Thread、Turn 与 ThreadItem 的 canonical 语义以 protocol.md 为准； App Server 的订阅和恢复契约以 zeta-app-server-api.md 为准； Renderer 组件与 CSS 所有权以 ui-styling-ownership.md 为准。 快速理解 Chat 右上角的辅助入口应打开 Session Inspector，而不是第二套 Session 导航。 产品层级固定为： ``text Auxiliary Bar / Chat ├── Session tabs 切换 Session ├── 当前 Session 的 ChatPane │ └── 当前 Thread transcript └── Session Inspector └── 当前 Thread 的 Plan、状态和辅助投影 ` 第一阶段只实现 Plan，后续再按真实后端 contract 增加 Threads、Activity、Changes、 Context 和 Artifacts。无论增加多少功能，Inspector 都只跟随当前 Session + Thread，不得自己维护另一份 Session 选择状态。 关键决策如下： 问题 决策 --- --- Inspector 的作用域 当前 Session + Thread Session 导航 继续由 Chat 顶部 tabs 和 History 入口拥有 第一项功能 当前 Thread 的 Plan Plan 的权威 owner Rust canonical Thread/Turn model Renderer 的职责 可丢弃、可重建的 Plan 视图投影 第一阶段数据入口 ThreadUpdate::PlanUpdated contract；production emitter 尚未接通 第一阶段恢复限制 重连或重新选择后无法从 snapshot 恢复结构化 Plan 生产完整形态 session/thread/read 必须返回可恢复的结构化 Plan Inspector 对 Plan 的修改 第一阶段只读；不直接勾选或静默改变步骤 订阅策略 Transcript 与 Inspector 共享已有 Thread projection，不建立第二条独立订阅 窄宽度行为 抽屉/覆盖层；不得把 Chat 正文压缩到不可用宽度 宽布局行为 空间足够时可并排显示 2. 要解决的问题 当前 Chat 已经同时出现三个容易混淆的导航概念： - Workbench 的 Auxiliary Bar 承载整个 Chat； - Chat title tabs 映射活动 Session； - Chat 内还有一个名为 Agent Sidebar 的嵌套区域和开关。 如果嵌套区域再次按 Session 组织内容，用户无法判断顶部 tabs 和内部 Sidebar 谁拥有主导航， 关闭、切换和恢复的语义也会重复。另一方面，Agent 执行过程中确实存在适合持续可见、但不适合 反复插入 transcript 的辅助状态，Plan 是当前最明确的真实消费者。 本设计解决： 1. 固定 Session、Thread 和 Inspector 的导航层级； 2. 让 Plan 在当前工作期间持续可见； 3. 保持 Plan 权威状态与 Renderer 展示分离； 4. 防止第二条订阅、文本解析或本地持久化形成冲突状态； 5. 为后续辅助能力提供受控扩展方向。 3. 不包含的目标 Session Inspector 不负责： - 展示或管理所有 Session 历史； - 替代 Chat title tabs、History Quick Pick 或未来专用 Session 页面； - 执行 Agent 规划、判断步骤是否完成或修改 canonical Plan； - 从 Markdown、Agent 消息或 Tool Result 猜测结构化 Plan； - 承载完整 Terminal、Browser、Settings 或 Editor； - 仅凭 Git working tree 将文件变化归因到某个 Session； - 在 Renderer 中持久化一份服务端无法验证的 Plan； - 把 ViewContainerLocation.AgentSidebar 扩展成没有真实消费者的通用插件平台。 4. 当前实现状态 以下均为 Current，可从 Desktop 与生成协议代码验证： 能力 当前状态 边界 --- --- --- Chat 注册到 Auxiliary Bar ✅ Chat 是独立 View contribution 一个活动 Session 对应一个 Chat tab 和保留的 ChatPane ✅ tab 只切 Session IWorkbenchSessionService 统一保存当前 Session/Thread 选择 ✅ 不拥有 transcript Chat title 提供 Agent Sidebar toggle ✅ 默认隐藏 Chat 内创建嵌套 SidebarPart ✅ 当前没有可用的 Agent Sidebar 内容装配 ViewContainerLocation.AgentSidebar ✅ 扩展点 尚无真实 ViewContainer consumer durable ThreadItem::Plan 类型与 reducer ✅ 只有 Markdown text；尚未发现 production producer PlanUpdate explanation, steps ✅ 结构化步骤状态 PlanStepStatus ✅ 只有 pending、inProgress、completed ThreadUpdate::PlanUpdated 类型与 broker routing ✅ transient；尚未发现 production producer ChatPaneModel 接收 planUpdated 部分具备 当前分支明确忽略该 update Chat transcript 渲染 Plan item ✅ 收到 item 时渲染 Markdown，不提供结构化步骤投影 结构化 Plan 从 session/thread/read 恢复 ❌ Thread / Turn snapshot 当前没有该字段 因此，“把 Plan 放进 Inspector”已有协议和 Renderer 切入点，但 P1 仍需接通真实 planUpdated producer。即使完成实时 vertical slice，也不能把它描述为跨重启、重连或重新 选择后可靠恢复。 5. 用户可见信息架构 5.1 导航归属 用户动作 唯一 owner Inspector 的反应 --- --- --- 切换 Session Chat title tabs / History 跟随新 Session 的当前 Thread 切换 Thread Thread navigation 跟随新 Thread 显示或隐藏辅助内容 Session Inspector toggle 不改变 Session/Thread 选择 关闭 Session Session tab Inspector 释放该 Session 投影 更新 Plan Agent runtime / canonical Thread Inspector 只刷新展示 Session 不得被注册为 Inspector 内的 ViewContainer、ViewPane 或 tab。未来 Inspector 如果需要 内部导航，导航项必须是 Plan、Threads、Activity、Changes 等功能视图，而不是 Session 实例。 5.2 目标布局 Session Inspector 的目标信息布局如下。阶段标签表达实施顺序，不表示所有区块会在 P1 同时出现： `text Session Inspector │ ├── Plan P1 / P2 │ ├── ✓ Understand terminal layout │ ├── ● Implement tabs behavior │ ├── ○ Add tests │ └── ○ Verify UI │ ├── Threads P3 │ ├── ● Main Thread │ └── ○ Layout investigation │ ├── Activity P3 │ ├── Turn: running │ └── Waiting: none │ ├── Changes P4 │ ├── M terminalViewPane.ts │ ├── M terminalTabsLayout.ts │ └── M terminal.css │ ├── Context P4 │ ├── Workspace: zeta │ ├── AGENTS.md │ └── 3 referenced files │ └── Artifacts P4 └── Generated outputs ` 布局规则： - Plan 始终位于首位，是 P1 唯一必须实现的内容； - Threads 只展示当前 Session 内的 Thread，不展示其他 Session； - Activity 与 Plan 分开：Turn 等待、审批和运行状态不能伪装成 Plan step 状态； - Changes 只有具备 Session/Thread attribution 后才能出现； - Context 只展示 canonical context/attachment projection，不从 transcript 猜测； - Artifacts 只有具备稳定 identity、lifecycle 和 open contract 后才能出现； - 尚未达到对应阶段的区块不渲染，不用 disabled placeholder 提前占位； - 多个区块同时存在时使用纵向 section；Plan 默认展开，其余区块允许折叠； - 折叠状态是可丢弃的 Renderer UI 状态，不能写入 Session、Thread 或 Plan。 5.3 Plan 视图 Plan 默认显示在 Inspector 的第一个位置： `text PLAN 说明文本（可选） ✓ Understand terminal layout ● Implement tabs behavior ○ Add tests ○ Verify UI ` 展示规则： - 保留 PlanUpdate.steps 的服务端顺序； - pending、inProgress、completed 使用不同图标、文本和稳定状态 class； - 不推断 blocked step；等待审批、用户输入或 capability 属于 Turn/Activity 状态； - 不因为 UI 认为计划异常而重排、去重或自动修正步骤； - explanation 为空时不保留空白占位； - 第一个版本只读，不提供可勾选 checkbox； - 没有结构化 Plan 时显示 No active plan，不得回退解析 Plan Markdown； - Thread 切换、stream incarnation 变化、更新空洞或 reconnect 时，实时 Plan 必须清空， 直到收到新 planUpdated 或未来从 durable snapshot 恢复。 当前 PlanStep 没有 step ID，也没有 transcript item correlation，因此第一阶段不支持： - 点击步骤精确跳转到对应消息或 Tool Call； - 对单个步骤做稳定的跨更新动画 identity； - 用户直接修改单个步骤； - 将重复文本步骤视为同一个步骤。 这些交互只有在 canonical contract 增加稳定 identity/correlation 后才能设计。 5.4 空、加载和错误状态 状态 展示 --- --- 当前 Thread 正在加载 与 Chat 共用 projection loading 状态；显示轻量 loading 已加载但没有 Plan No active plan 收到实时 Plan 显示 explanation 和步骤 Thread 切换 立即移除旧 Thread Plan，再绑定新投影 reconnect / stream gap 清除不可恢复的实时 Plan，等待 resync Thread projection error 显示与 Chat 一致的可恢复错误，不伪造旧 Plan 没有活动 Session Start a new chat to view its plan 不得使用上一个 Session 或 Thread 的 Plan 填充 loading/empty 状态。 6. 响应式布局 当前 Chat 通常位于较窄的 Auxiliary Bar。现有固定 220px 的嵌套 Sidebar 会显著压缩 transcript， 因此目标布局不能始终并排。 Chat 可用宽度 Proposed 行为 --- --- 小于 720px Inspector 作为右侧抽屉覆盖 Chat body，默认隐藏 大于等于 720px Inspector 可与 Chat body 并排，建议宽 280px 无活动 Session toggle 可保留，但打开后只显示 empty state 布局应保证： - Chat 正文并排时至少保留 360px； - Inspector 建议最小宽度 240px； - 抽屉打开后可通过 toggle、关闭按钮或 Escape 关闭； - 抽屉不改变 Session/Thread 选择； - focus 进入抽屉后关闭应返回触发按钮； - 是否采用 split 或 drawer 由 Chat host 可用宽度决定，不写入 durable settings。 Inspector 是 Chat contribution 内的局部布局区域，不是新的 Workbench Part。是否复用基础 primitive 由实现决定，但不应继续用一个完整 SidebarPart 嵌套另一个 Workbench Auxiliary Bar 来获得视觉效果。 7. 组件与数据所有权 7.1 跨层所有权 组件/层 拥有 明确不拥有 --- --- --- Rust Protocol / Core Plan 结构、状态词汇、Turn 归属、durable 恢复语义 Desktop 布局与展开状态 App Server snapshot、subscribe/update delivery、sequence 与 reconnect contract Inspector 是否可见 IWorkbenchSessionService 当前 Session/Thread 选择和 Session topology projection Thread transcript 与 Plan Thread projection owner 一个 Thread 的 snapshot、transient items、Plan live state 和订阅生命周期 Session tabs 和 Inspector 布局 ChatViewPane Session tabs、当前 ChatPane、Inspector 外部布局和 active projection 绑定 Plan 状态判断 SessionInspector 局部导航、empty/loading 呈现、focus 与响应式行为 Session 选择和 App Server 调用 Plan view/widget Plan DOM、ARIA、步骤状态投影 修改 canonical Plan 7.2 单一 Thread 订阅 当前每个保留的 ChatPaneModel 已经拥有选中 Thread 的 thread.subscribe、thread.read、 stream cursor 和 reconnect 处理。Inspector 不得再为同一 Thread 独立调用 thread.subscribe，否则两个消费者的 unsubscribe、cursor 和 reconnect 生命周期会相互干扰。 第一阶段应复用现有 projection： `text thread.subscribe / thread.read / thread update → ChatPaneModel（唯一订阅 owner） ├── transcript items └── live Plan projection → ChatPane / ChatViewPane → SessionInspector → Plan view ` 如果 Plan、Activity、Changes 等消费者增长到 Chat 私有 model 不再合适，应提取共享的 Thread projection service。提取前必须有至少两个真实消费者，并保持以下 contract： - acquisition/release 不导致一个消费者释放另一个消费者仍在使用的订阅； - 同一 Thread 只有一个 stream cursor authority； - reconnect 与 gap recovery 统一执行； - UI consumer 只能读取 projection，不直接写 canonical state。 不得为了让 Inspector 快速工作而把 ChatPaneModel 整体注册成全局 service，或让 SessionInspector 通过 DOM 查找当前 ChatPane。 8. Plan 的可靠性与持久性 8.1 当前契约 当前存在两种不同的 Plan 表示： 表示 结构 Durability 当前用途 --- --- --- --- ThreadItem::Plan text: String durable transcript 中的 Plan 文本 ThreadUpdate::PlanUpdated PlanUpdate explanation, steps transient 低延迟结构化进度 两者不能互相冒充。Renderer 解析 Markdown 会丢失状态、顺序语义和未来兼容性；把 transient Plan 写入 local storage 又会产生服务端无法确认的陈旧状态。 8.2 计划中的持久化契约 生产完整版本应让 Plan 成为 Turn 的可恢复 readable state。推荐方向是： `text Turn ... plan?: PlanUpdate ` 并由明确的 durable Thread event 更新该字段。具体 Rust variant 与迁移由 Protocol/Core 实现评审确定，但必须满足： 1. event 足以从空状态重建 Turn.plan； 2. session/thread/read 和 session/thread/subscribe snapshot 返回同一结构化 Plan； 3. planUpdated 可以继续作为低延迟 preview，但不能成为唯一恢复来源； 4. preview 被 committed state 覆盖，而不是覆盖 committed state； 5. Plan 归属 TurnId，不提升为 Session 级全局状态； 6. completed Turn 可以保留最终 Plan，供 Inspector 显示 Last plan； 7. 新活动 Turn 没有 Plan 时，不自动继承上一 Turn 的可编辑状态； 8. status 词汇的新增或约束由 Rust canonical contract 校验，Renderer 不自行扩展。 在该 contract 落地前，Desktop 文档、UI 和测试必须继续把 Plan Inspector 标为实时投影。 9. Renderer 组件设计 目标组件边界为： `text ChatViewPane ├── ChatTitleControl ├── active ChatPane └── SessionInspector └── PlanView ` SessionInspector 是 Chat contribution 拥有的 composed control，不是通用 base 组件。只有当 第二个领域出现相同的、与 Session 无关的交互 contract 后，才考虑下沉通用 primitive。 建议状态输入使用带语义的联合类型，而不是多个 bool： `ts type PlanPresentation = readonly phase: \"loading\" readonly phase: \"empty\" readonly phase: \"ready\"; readonly plan: PlanUpdate readonly phase: \"error\"; readonly message: string ; ` 这是 Proposed 示例，用于说明调用方可读性，不表示当前 API 已经存在。 9.1 Action 与上下文状态 用户可见名称应从泛化的 Agent Sidebar 收敛为 Session Inspector： - Show Session Inspector - Hide Session Inspector - toolbar aria-label 保持 Chat layout 最终只允许一个 visible context state。若保留旧 command ID 用于兼容，它必须委托同一命令和 同一 context key，不得产生 Agent Sidebar 与 Session Inspector 两套可见状态。 9.2 CSS 所有权 样式 Owner --- --- Chat body 与 Inspector 的 split/drawer 几何 ChatViewPane / Chat layout CSS Inspector 内部 header、section 和 focus SessionInspector Plan step 排列、status class 与图标 Plan view toggle checked 状态 Action → Button 的 .checked 状态投影 颜色和尺寸值 semantic design token 视觉 selector 使用稳定 class，例如 .in-progress、.completed；ARIA attribute 只表达 无障碍语义，不作为 CSS 状态 selector。Host CSS 不穿透修改共享 Button、TabList 或 ScrollableElement 的内部状态。 9.3 无障碍 - Inspector root 使用明确的 aria-label=\"Session Inspector\"； - Plan 使用有序列表语义； - 每个步骤同时提供可读状态文本，不能只靠颜色或图标； - inProgress 可使用 aria-current=\"step\" 表达当前位置，但 CSS 仍选择状态 class； - transcript 的 role=\"log\" 不应因 Plan Inspector 更新而重复播报整个列表； - drawer 模式必须有可靠的 focus return 和 Escape 行为； - loading、empty 和 error 不使用持续刷新的 aria-live=\"assertive\"。 10. 后续功能边界 Plan 之外的功能只有在数据 owner 明确后才能加入： 功能 适合度 前置条件 建议阶段 --- --- --- --- Plan ✅ 当前 transient contract 已有；durable contract 后续补齐 P1/P2 Threads ✅ Session topology、Thread title/read 和选择交互统一 P3 Activity ✅ 从 Turn status 与 tool events 做只读投影 P3 Changes 适合，但尚未完成 必须有 Session/Thread attribution，不能只读全局 Git diff P4 Context 适合，但尚未完成 canonical context/attachment projection P4 Artifacts 适合，但尚未完成 artifact identity、lifecycle 与 open contract P4 Session history ❌ 已由 tabs/History 拥有 不进入 Inspector 完整 Terminal/Browser ❌ 属于现有 Workbench surface 不进入 Inspector Threads 进入 Inspector 后仍是当前 Session 内的 Thread topology，不是第二份 Session history。 只有后端能证明变化与当前 Session/Thread 的关联时，变更（Changes）才能显示为“本次会话 修改”；否则只能叫“工作区变更”，并留在 SCM/Files 等已有接口面。 11. 被拒绝的替代方案 方案 判断 原因 --- --- --- Inspector 内再列所有 Session ❌ 与 Chat tabs/History 重复，产生双主导航 从最新 Plan Markdown 解析步骤 ❌ 非 canonical，状态和兼容性不可恢复 把实时 Plan 写入 Renderer local storage ❌ 形成无法由服务端确认的陈旧 authority Inspector 自己订阅当前 Thread ❌ 重复 subscription、unsubscribe 和 cursor authority 用 blocked 扩展前端 step 状态 ❌ 当前 protocol 没有该状态 用户直接勾选 Plan step 尚不采用 当前没有修改 canonical Plan 的 command contract 始终固定 220px 并排 ❌ Auxiliary Bar 中会使 Chat 正文不可用 永久嵌套完整 SidebarPart 暂不采用 Inspector 是 Chat 局部区域，尚无独立 Workbench composite 需求 为未来插件预建多级 ViewContainer 暂不采用 当前只有 Plan 这个真实消费者 12. 分阶段实施 P1：实时 Plan Inspector 目标：接通 planUpdated producer，并做正确但明确可丢失的实时展示。 实施范围： 1. 让 Agent execution vertical slice 产生真实 PlanUpdate，并通过现有 broker 发布 planUpdated； 2. 将用户可见名称改为 Session Inspector； 3. 用 Chat-owned SessionInspector 替换空的嵌套 Sidebar host； 4. 让现有 Thread projection 保存当前 PlanUpdate； 5. 将 active ChatPane projection 绑定给 Inspector，不增加订阅； 6. Thread/session 切换和 reconnect/gap 时清除旧实时 Plan； 7. 实现 drawer/split 响应式布局； 8. 增加 producer、model、view、toggle、focus 和跨 Session 隔离测试。 完成条件： - 真实 Agent Plan update 能从 runtime 到达 Desktop，而不依赖测试 fixture； - 一个 Session 的 Plan 不会出现在另一个 Session/Thread；"
  },
  {
    "slug": "tui",
    "sourcePath": "docs/tui.md",
    "title": "zeta code TUI 架构与产品支持边界",
    "description": "zeta code 的 TUI 把 App Server 的权威状态转换成终端中的可交互呈现；它拥有单写者的界面状态和事件循环， 不拥有产品事实或后台执行。",
    "group": "界面与体验",
    "markdown": "> 物理位置：`zeta-code/tui/`；ANSI/Ratatui adapter：`zeta-code/ansi-escape/`\n> 宿主：`zeta-code/cli/`\n> 文档所有权：本文是 TUI 跨 crate ownership、长期不变量、产品支持边界与已接受架构迁移顺序的 canonical 文档。\n> 当前实现接口与事件循环：[`zeta-code/tui/README.md`](../zeta-code/tui/README.md)\n> 产品接口基线：[`zeta-app-server-api.md`](zeta-app-server-api.md)  \n> App Server 启动与连接基线：[`app-server-client.md`](app-server-client.md)  \n> Workspace 边界基线：[`zeta-rs-architecture.md`](zeta-rs-architecture.md)\n> 产品线归属基线：[`product-lines.md`](product-lines.md)\n\n## 快速理解\n\n`zeta code` 的 TUI 把 App Server 的权威状态转换成终端中的可交互呈现；它拥有单写者的界面状态和事件循环，\n不拥有产品事实或后台执行。\n\n| 发生的事情 | TUI 负责 | 不负责 |\n| --- | --- | --- |\n| 用户按键或提交命令 | 转成类型化界面命令并发给客户端 | 直接修改 Session 或 Thread |\n| 服务端产生更新 | 归约为单写者 presentation state 并请求重绘 | 重新解释领域事件 |\n| 高频流式文本到达 | 通过专用临时数据面更新显示 | 把每个片段伪装成持久事实 |\n| 检测到序列缺口 | 暂停推断并请求权威快照 | 用本地状态填补缺口 |\n| 请求完成、过期或取消 | 按关联身份丢弃过期结果并更新交互 | 决定工具重试或 Agent 恢复 |\n| 渲染一帧 | 纯读取当前呈现状态 | 在绘制过程中触发业务副作用 |\n| 加载主题 | 消费共享 snapshot 的明确子集并按终端能力降级 | 复制 Desktop 的全部主题能力或默认色目录 |\n| 外部 Agent 来源已经进入统一 Skill catalog | 浏览、启用或禁用已有条目 | 导入外部 Agent 配置、选择目录或扫描用户主目录 |\n\n## 1. 结论\n\nTUI 正式采用以下长期稳态架构基准：\n\n> **窄而稳定的终端基础设施 + 单写者的 presentation state + 分域 event/command +\n> 垂直功能切片 + 高频流式事件专用数据面 + 无业务副作用的渲染。**\n\n这套基准约束新功能和迁移方案，但不是对 Codex TUI 目录、feature catalog 或领域状态的复制。\nZeta 的产品 authority、typed contract 和 crate dependency direction 仍由本仓库现有架构决定。\n\n状态必须按下表阅读：\n\n| 内容 | 状态 | Canonical owner |\n| --- | --- | --- |\n| 当前 wakeable event loop、typed Thread subscription、snapshot/transient resync 与交互 | Current | [`zeta-code/tui/README.md`](../zeta-code/tui/README.md) |\n| 本文的 ownership 与长期不变量 | Accepted architecture baseline | 本文 |\n| independent request driver、wakeable event pump | Current | [`app-server-client.md`](app-server-client.md) 与 crate README |\n| 非阻塞 request completion dispatch、有界 transient data plane、Session/Thread/interaction 垂直切片 | Current | [`zeta-code/tui/README.md`](../zeta-code/tui/README.md) |\n| plain-text transcript、必要 popup mouse hit 与 embedded session | Current product support boundary | 本文与 crate README |\n| active-Turn follow-up queue、多行 composer、copy/export、分页历史与 suspend/resume | Current | [`zeta-code/tui/README.md`](../zeta-code/tui/README.md) |\n| 尚无 `zeta code` 产品要求或 canonical contract 的 feature | 非目标或 Potential；不构成实现承诺 | 对应产品线、领域与 App Server API 文档 |\n\n本文只为已经接受的 `zeta code` 能力规定架构与迁移顺序。“某能力在 TUI 中不存在”不自动产生\n产品 backlog。Native Agent Timeline 的 Markdown、table、selection、折叠与虚拟化由\n[`zeterm/docs/native-agent-console.md`](../zeterm/docs/native-agent-console.md) 规划，具体 Native\nMarkdown 组件由 [`zeterm/markdown`](../zeterm/markdown/README.md) 拥有；TUI 不追求与其 feature\nparity。Vim、remote selector/reconnect、通用 structured Mention 和 durable blob 只有在产品文档\n接受需求并确定 canonical owner 后，才可能成为某条产品线的实施项。\n\nZeta 已经在 TUI 外部拥有：\n\n- `zeta-core` 中权威的 `SessionCoordinator` 和 `ThreadController`；\n- `zeta-app-server-protocol` 中唯一的 wire contract；\n- `zeta-app-server-client` 中共享的 App Server 启动、初始化、请求/事件连接与关闭层；\n- CLI 交付的启动配置与产品入口参数。\n- `zeta-theme` 中与 Desktop/Native 共享的 manifest、用户主题解析和 device preference loader。\n- `zeta-ansi-escape` 中独立的 ANSI SGR → Ratatui presentation adapter；它不拥有 PTY/terminal state。\n\n主题边界是“部分接入”而不是“尚未复制完成”：TUI chrome 读取 accent、composer chrome、错误、\n成功、警告、弱化文字和选择高亮；Theme Pane preview 额外读取有限的 syntax/diff token。选择高亮由\n`tui.highlightForeground` 独立表达，不借用编辑器关键字色。`ui/theme.rs` 将透明色先合成到 terminal\nbackground，再按 TrueColor、ANSI-256、ANSI-16、Monochrome 投影；其他 Desktop/Native token\n不进入 TUI API。\n`tui.colorTheme` 缺失时跟随 `workbench.colorTheme`，选择属于 device-local JSON，不属于 Agent\nconfig、Session store 或 TOML。无参数 `/theme` 打开由 `features/theme` 拥有、不可搜索的固定\nZeta Code Theme Pane 以 `Theme` 为标题，顶部分隔线与标题、标题与第一个候选项之间各保留一行；固定选项为 Auto、Dark/Light、对应 colorblind-friendly 与 ANSI-only 模式，以及 Custom\ncolor theme。候选行编号展示，cursor 选择色和 syntax/diff preview 随候选主题变化；Enter 原子保存、\n即时切换并关闭整个 Theme flow 返回主界面；保存成功后 transcript 以独立的状态圆点显示实际执行的 `/theme <id>`，下一行通过 `└─` 结构连接符归属结果说明，两行正文保持同列对齐；保存失败时则保留当前 Pane 以显示错误。移动 cursor 时，Theme Pane 分隔线、上方 welcome banner\n框线使用候选 highlight；独立 `Diff preview` 区域不画左右边框，只用候选 muted token 绘制上下\n较高对比度的长节虚线。主题列表与 preview 间保留两行，palette 来源说明与操作提示间保留一行。preview 下方标明\nGitHub、GitHub Colorblind、ANSI 16 colors 或 User-defined 配色来源。`/theme <id>` 保留直接切换。通用 Selection Pane 的搜索是独立、\n可配置的底座；启用搜索的 feature 必须先按 Space 进入 search mode，footer 明示 `Space search`。\n所有本地 command 都可使用同一个“命令 + 结果” transcript 形式：命令本身不带箭头符号，独立的状态圆点为 Running 显示 `◉`，为 Succeeded 显示 `●`；结果行使用与状态位结构相连的 `└─` 表达归属，正文与命令文字同列对齐并使用弱化色，之后才空行。当前没有折叠交互；待有多行 command output 时再基于这个分组添加展开/收起。\nAuto 在终端 raw mode 建立后、输入事件线程启动前发出一次 OSC 11 背景色查询，并按实际 RGB\n亮度选择 Light/Dark；120 ms 内没有有效响应时读取 `COLORFGBG`，仍无法识别才安全回退 Dark。\n检测结果在当前 TUI 会话内缓存，主题面板与再次选择 Auto 不会重复查询；显式模式不受该判断影响。\n\n因此 TUI 必须是可丢弃、可重新同步的 presentation shell，而不是第二个 Agent runtime 或\nApp Server facade。产品权威状态的依赖链固定为：\n\n```text\nzeta-cli\n  → zeta-tui\n    ├─ zeta-ansi-escape → ansi-to-tui / ratatui\n    └─ zeta-app-server-client\n       → App Server dispatcher\n       → zeta-core\n```\n\n本地只读能力不必统一绕行 App Server：composer 的 workspace path mention 直接调用\n`zeta-file-search`；需要 workspace authority、跨进程一致性或 watcher revision 的目录浏览与 Git\n状态通过 typed App Server filesystem/Git contract。原则不是“所有数据经过一个 facade”，而是\n“每个 feature 消费事实 owner 已提供的 public typed interface”。\n\n进程内模式只是一种 transport 优化。TUI 仍然经过 initialize、typed request/response、\ndispatcher 和 notification decode，不得直接依赖 Core、Storage、Exec、Sandbox 或 Model\nProvider。\n\n## 2. 基准采用与 Zeta 修正\n\n长期基准中的通用结构按下表落到 Zeta：\n\n| 基准概念 | Zeta 中的处理 |\n| --- | --- |\n| `app/` | 保留，但只拥有 TUI 状态、事件协调和退出流程 |\n| `app_server/` | 不建立；typed RPC 已由 `zeta-app-server-client` 和 protocol crate 拥有 |\n| `thread/` | 作为 `features/thread/` 保留；拥有 active Thread 的可重建展示状态和交互流程 |\n| `chat/` | 不建立总括目录；Turn flow 归 `features/thread/`，composer/transcript 归 `components/` |\n| `ui/` | 保留，只放可复用 Ratatui 原语 |\n| `terminal/` | 保留，只负责真实终端生命周期和能力 |\n| `features/` | 保留，但只添加 Zeta 已有或已接受产品契约支持的功能 |\n| `platform/` | 不建立泛化 facade；窄 OS adapter 放入 `host/` 的明确子模块 |\n\n尤其不能在 TUI 中定义第二个 `ThreadController`。`zeta-core::ThreadController` 是 Thread 执行、\n持久化、顺序和恢复的 authority；TUI 的 Thread 数据只是由 snapshot 和 update 构成的\nread model。\n\n同样，TUI 内部不应出现另一个聚合 account、session、thread、turn、config 等 RPC 域的\nfacade。CLI 使用 `zeta-app-server-client` 建立并初始化 `AppServerSession`，再把该运行会话\n的所有权交给 TUI；TUI 围绕其 cloneable request handle 与 event stream 补充交互客户端所需的\n请求调度、订阅和错误映射。该 `AppServerSession` 是 connection/runtime owner，不是产品\n`Session`。\n\n## 3. 产品状态与 TUI 状态\n\n权威产品模型与 sequence/cursor 语义统一见 [`protocol.md`](protocol.md)。TUI 只消费这些\ncanonical snapshot/update，不在本地重新定义产品实体。\n\nTUI 可以在使用相应数据的 feature 中保存以下可重建状态：\n\n- 当前选中的 Session、Thread、Turn 或 Item；\n- 当前页面需要的 canonical Session/Thread snapshot；\n- 每个 aggregate 最后确认的 durable sequence；\n- 当前 runtime 的 transient stream cursor；\n- composer 草稿、光标、选择区和输入历史；\n- scroll、折叠、tab、overlay 和 picker 状态；\n- 正在发送的 typed command 及其稳定 `CommandId`；\n- connection、subscription、resync 和可展示错误状态。\n\nTUI 不得保存或推导：\n\n- Session membership、fork lineage 或 lifecycle 的第二份权威状态机；\n- Thread、Turn、ThreadItem 或 Tool Call 的执行状态机；\n- writer lease、command receipt 或持久化恢复状态；\n- approval、sandbox 或 tool execution policy；\n- 从日志、stderr 或人类文本解析出的产品终态。\n\n命名时必须区分三种生命周期：\n\n- `ProductSession` 或 protocol 中的 `Session`：产品任务；\n- `AppServerConnection`：RPC connection；\n- `TerminalSession`：raw mode、alternate screen 等终端资源。\n\n禁止用无修饰的本地 `SessionState` 同时表达上述多个概念。\n\n## 长期架构不变量\n\n以下约束是架构评审中的默认拒绝条件，而不是可选风格。若实现确实需要偏离，必须先在本文记录\n原因、影响范围、恢复策略与退出条件。\n\n### 单写者\n\nTUI semantic/presentation state 只能由主事件循环写入。后台 task、transport callback、host\nadapter 和 renderer 只能产生 event 或完成结果，不能直接修改共享 feature state。\n\n单写者不表示所有工作都在主线程同步执行。RPC、文件搜索、大型 transcript projection 和其他昂贵工作应\n异步执行，但其结果必须重新进入有序 event loop，并由当前 state owner 判断是否仍然有效。\n\n### 状态分类\n\n| 类别 | 示例 | Owner 与约束 |\n| --- | --- | --- |\n| 可重建 presentation state | active Thread snapshot、pending command、connection/resync、可展示错误 | 对应 feature 或 `app/`；可由 typed input 重建 |\n| 局部交互状态 | draft、cursor、selection、scroll、overlay 展开 | 明确生命周期的 component；不提升为全局 store |\n| runtime resource | terminal handle、channel、task、clock、client、cache | driver/client/terminal；不得进入可 replay state |\n| 产品权威状态 | Thread/Turn reducer、writer lease、approval policy、durable recovery | TUI 外的 canonical owner；TUI 不复制 |\n\n### 纯渲染\n\n`view`、layout 和 Ratatui renderer 只能读取 state 与只读环境信息。禁止在 draw path 中：\n\n- 发起 RPC、读取文件或写配置；\n- spawn task 或改变 subscription；\n- 推进 Turn、animation 或 paste 等语义状态；\n- 根据 frame 次数决定业务结果；\n- 查询 Git、usage 或其他可能阻塞的接口。\n\n时间驱动变化由明确的 timer event 推进；render cache 属于 runtime resource，并使用稳定\nidentity、revision、width、theme/capability revision 等显式 key 失效。\n\n### 显式副作用与局部协议\n\n跨边界行为必须先成为 typed command/intent，再由 driver、feature request module 或窄 host\nadapter 执行，完成结果重新成为 event。顶层 `AppEvent`/`AppCommand` 只负责领域路由；具体\n变体留在 feature 内，不能重新形成覆盖所有 RPC 的大枚举。\n\n不建立高度泛型的 `Feature` trait、全局 `Services` facade 或任意闭包 callback 总线。普通\nRust struct/enum/function 和一致约定是默认方案；只有多个真实调用方证明语义相同时才提取\n共享抽象。\n\n### 请求关联与过期结果\n\n任何可能乱序完成的读取或计算必须携带 generation、目标 scope 和必要的 request identity。\n写操作还必须遵守 canonical `CommandId` 与 expected sequence 语义。旧 generation、错误 scope\n或已取消请求的结果不得覆盖当前 state。\n\n### 控制面、数据面与恢复\n\n用户意图、退出、interrupt、写请求结果、committed update、错误和 subscription lifecycle\n属于有序控制面；token delta、process output 和 tool progress 等高频事件进入按 aggregate\n隔离的 bounded 数据面。所有 lag、overflow、cursor gap、identity mismatch 和已经实现的\nconnection recovery 路径必须定义 resync contract，不能只记录 warning 后继续猜测状态。\n\n### 内部优先\n\n目标边界先在 `zeta-tui` crate 内通过 private module 和 `pub(crate)` 验证。只有 runtime 或\ncomponent 被至少两个真实消费者使用、API 变化频率下降且抽取能减少依赖时，才评估独立 crate\n或公共插件 API。窄的第三方类型适配层可以按依赖隔离提前成为产品内 crate，但必须保持单向、\n无产品状态且有明确 failure semantics；`zeta-ansi-escape` 是当前唯一此类例外。\n\n## 4. 目标目录\n\n目标结构如下：\n\n```text\nzeta-code/tui/\n├── src/\n│   ├── app/\n│   │   ├── mod.rs\n│   │   ├── state.rs\n│   │   ├── event.rs\n│   │   ├── command.rs\n│   │   ├── event_loop.rs\n│   │   ├── dispatch.rs\n│   │   ├── bootstrap.rs\n│   │   └── shutdown.rs\n│   ├── client/\n│   │   ├── mod.rs\n│   │   ├── event_pump.rs\n│   │   ├── pending.rs\n│   │   ├── subscription.rs\n│   │   ├── command_id.rs\n│   │   ├── notification.rs\n│   │   └── error.rs\n│   ├── features/\n│   │   ├── mod.rs\n│   │   ├── thread/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── update.rs\n│   │   │   ├── command.rs\n│   │   │   ├── request.rs\n│   │   │   ├── view.rs\n│   │   │   └── thread_tests.rs\n│   │   ├── sessions/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── command.rs\n│   │   │   ├── request.rs\n│   │   │   ├── view.rs\n│   │   │   └── sessions_tests.rs\n│   │   ├── config/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── command.rs\n│   │   │   ├── request.rs\n│   │   │   ├── view.rs\n│   │   │   └── config_tests.rs\n│   │   ├── skills/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── command.rs\n│   │   │   ├── request.rs\n│   │   │   ├── view.rs\n│   │   │   └── skills_tests.rs\n│   │   ├── workspace_files/\n│   │   │   ├── mod.rs\n│   │   │   ├── search.rs\n│   │   │   ├── completion.rs\n│   │   │   └── workspace_files_tests.rs\n│   │   └── status_line/\n│   │       ├── mod.rs\n│   │       ├── model.rs\n│   │       ├── refresh.rs\n│   │       ├── layout.rs\n│   │       ├── view.rs\n│   │       └── status_line_tests.rs\n│   ├── components/\n│   │   ├── mod.rs\n│   │   ├── interaction/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── view_stack.rs\n│   │   │   └── interaction_tests.rs\n│   │   ├── composer/\n│   │   │   ├── mod.rs\n│   │   │   ├── state.rs\n│   │   │   ├── editor.rs\n│   │   │   ├── attachments.rs\n│   │   │   ├── pending_pastes.rs\n│   │   │   ├── slash_commands.rs    # TUI-local command execution metadata only\n│   │   │   ├── view.rs\n│   │   │   └── composer_tests.rs\n│   │   ├── transcript/\n│   │   │   ├── mod.rs\n│   │   │   ├── row.rs\n│   │   │   ├── layout.rs\n│   │   │   ├── view.rs\n│   │   │   └── transcript_tests.rs\n│   │   └── selection/\n│   │       ├── mod.rs\n│   │       ├── state.rs\n│   │       ├── view.rs\n│   │       └── selection_tests.rs\n│   ├── ui/\n│   │   ├── mod.rs\n│   │   ├── layout.rs\n│   │   ├── scroll.rs\n│   │   ├── overlay.rs\n│   │   ├── keymap.rs\n│   │   ├── text.rs\n│   │   └── theme.rs\n│   ├── terminal/\n│   │   ├── mod.rs\n│   │   ├── session.rs\n│   │   ├── event_stream.rs\n│   │   ├── frame_scheduler.rs\n│   │   ├── scrollback.rs\n│   │   ├── reflow.rs\n│   │   ├── cursor.rs\n│   │   └── capabilities.rs\n│   ├── host/\n│   │   ├── mod.rs\n│   │   ├── clipboard.rs\n│   │   ├── external_editor.rs\n│   │   ├── notification.rs\n│   │   └── ide.rs\n│   └── lib.rs\n├── tests/\n│   ├── event_loop.rs\n│   ├── subscription_recovery.rs\n│   └── support/\n│       ├── mod.rs\n│       ├── fake_client.rs\n│       └── terminal_harness.rs\n├── Cargo.toml\n└── README.md\n```\n\n上图只列当前已有能力和已确定的下一阶段 owner，不罗列尚无 contract 的未来 feature。每个\n子目录都给出首批具体文件；实现初期不足以形成独立 owner 时继续使用单文件，不能只创建一个\n空 `mod.rs` 或 slash-only 占位目录。文件名表达职责，不要求一次性生成整棵目录树。\n\n## 5. `app/`：应用协调\n\n`app/` 负责：\n\n- 接收 terminal event、client result、server notification 和 background completion；\n- 把输入转换为语义明确的 `AppCommand`；\n- 协调 active feature、component、overlay 和请求结果；\n- 管理焦点、顶层模式、退出与恢复终端；\n- 决定何时请求重绘。\n\n建议把可测试状态与副作用 driver 分开：\n\n```rust\nstruct TuiApp {\n    connection: ConnectionViewState,\n    active_view: ActiveView,\n    thread: ThreadFeatureState,\n    sessions: SessionsFeatureState,\n    status_line: StatusLineModel,\n    overlays: OverlayStack,\n    pending_commands: PendingCommands,\n}\n\nstruct AppDriver<C> {\n    app: TuiApp,\n    client: C,\n    terminal: TerminalSession,\n}\n```\n\n`TuiApp` 处理事件并产生 command；`AppDriver` 执行 I/O，再把结果作为事件送回。这样 reducer\n式状态测试不需要打开终端或 App Server。\n\n`app/` 可以协调其他模块，但禁止：\n\n- 实现具体 Ratatui widget；\n- 拼接 JSON-RPC method string 或手写 wire JSON；\n- 执行 Session/Thread 领域 reducer；\n- 直接运行 shell、模型或 Agent tool；\n- 成为每个 feature 私有状态的杂物桶。\n\n`bootstrap.rs` 只负责 TUI 启动流程，例如打开 terminal、读取入口所需 snapshot、进入指定\nSession。CLI 可以构造 start options，但 App Server composition、channel 建立、initialize、\nschema gate 和 shutdown 属于 `zeta-app-server-client`，TUI 不复制这些步骤。\n\n## 6. `client/`：TUI 到 typed 客户端的窄适配\n\n`client/` 不是新的 App Server facade。它只负责交互客户端特有的：\n\n- 持有 cloneable `AppServerClient` request handle 和 `AppServerEvents`；\n- 驱动 pending typed request completion；\n- 持续消费 `AppServerEvents`，将 `ServerNotification` 转换为内部事件；\n- 维护 subscription transport lifecycle；\n- 为一次逻辑写操作分配并保留稳定 `CommandId`；\n- 将 stable server error 映射为可操作的 TUI 错误类别。\n\nRPC Params、Result、Notification 和错误码仍由：\n\n```text\nzeta-app-server-protocol\nzeta-app-server-client\n```\n\n唯一拥有。`client/` 不得复制 DTO、兼容旧 method、直接序列化 JSON-RPC 或暴露\n`execute(method: &str, ...)`。\n\n具体领域请求不汇总成 `ClientOperation` 大枚举。`features/thread/request.rs`、\n`features/config/request.rs` 等模块直接调用 `AppServerClient` 的 typed method，并把完成结果\n转换为自己的 feature event；Session/Thread/Turn mutation 统一使用 `request_session`，\n`client/` 只提供执行与投递机制，不复制 App Server 的领域 contract。\n\n一次逻辑写操作在超时或响应丢失后重试时必须复用原 `CommandId` 和 exact typed payload。\n用户再次点击或再次提交是新命令，必须生成新 ID。`expectedSequence` 来自目标 aggregate\n在对应 feature 中最后确认的 canonical snapshot，不能使用 JSON-RPC request ID、stream\ncursor 或另一个 aggregate 的 sequence。\n\n这些 identity 不得互换：\n\n| Identity | 解决的问题 | 生命周期与规则 |\n| --- | --- | --- |\n| JSON-RPC request ID | 匹配一次 transport request/response | 每次 transport attempt 独立；不表示产品幂等性 |\n| `CommandId` | 标识一次逻辑写意图 | exact retry 必须复用；新的用户意图必须新建 |\n| expected sequence | 防止基于过期 aggregate state 写入 | 来自目标 aggregate 最后确认的 durable sequence |\n| request generation | 丢弃旧搜索、读取或计算结果 | scope 内单调；旧 generation 不改变当前 state |\n| durable sequence | 排序 committed aggregate update | 按 Session/Thread 分别连续验证 |\n| stream instance/cursor | 排序一次 transient stream | 不能作为 durable completion 或 expected sequence |\n\npending request 至少记录 result route、scope、generation、cancellation state 和 timeout policy。\n取消只表示 TUI 不再接受结果；除非 typed contract 明确支持取消，不能假定远端副作用没有发生。\n写请求在结果未知时不得自动生成新 `CommandId` 重放。\n\n共享 client 使用 bounded request/event channel、per-request completion 与独立 wakeable\n`AppServerEvents` 替换 TUI 的 `round_trip + drain_notifications` 路径。TUI 的\n`client::RequestTask` 执行 typed request，`app::request_completion` 在单写者 loop 校验 scope 并\n安装结果；用户 request intent 在单槽执行器前保序排队，Quit 和纯本地操作仍可立即处理。TUI\n不能通过直连 Core、读取日志或私有 transport method 绕过。\n\n## 7. `features/thread/`：active Thread 的唯一 TUI 所有者\n\nZeta 的主要交互对象是 Thread，因此不再建立通用 `projection/` 和 `conversation/` 两层。\n`features/thread/` 是 active Thread 在 TUI 内的唯一状态 owner：\n\n| 文件 | 职责 |\n| --- | --- |\n| `state.rs` | 保存当前 `ThreadId`、canonical snapshot、最后确认 sequence、transient items 和 view-local selection |\n| `update.rs` | 校验并应用 typed snapshot/update，处理 committed/transient 替换和 resync intent |\n| `command.rs` | 定义 start、interrupt、fork 等用户意图及 pending command 状态 |\n| `request.rs` | 直接调用 `AppServerClient` 的 Thread/Turn typed methods |\n| `view.rs` | 将 `ThreadFeatureState` 与 transcript/composer component 组装成当前页面 |\n| `thread_tests.rs` | 覆盖 sequence、resync、Turn flow 和可见 item 顺序 |\n\n这里保存的是可由 server snapshot 重建的客户端状态，不是第二个 `ThreadController`：\n\n- 输入只允许 typed snapshot/update 和明确的本地 UI intent；\n- 不执行业务校验、持久化或 Tool 副作用；\n- 不补造缺失 event；\n- 不从展示文本推断 Turn/Item 终态；\n- 遇到未知 update、sequence 空洞或 identity 不一致时产生 resubscribe/resync intent。\n\nSession 列表和 Session 页面状态归 `features/sessions/`，不会进入一个跨领域\n`ProjectionStore`。其他 feature 同样只缓存自己页面真正需要的 typed result。\n\n### 7.1 持久化序列\n\nSession 与 Thread 的 durable sequence 由各自 feature 分别跟踪。Thread 收到 committed update\n时：\n\n1. `durableSequence <= localSequence`：作为重复 delivery 忽略；\n2. `durableSequence == localSequence + 1`：应用到 active Thread state；\n3. `durableSequence > localSequence + 1`：停止增量合并并重新 subscribe；\n4. update identity 与当前 aggregate 不一致：视为协议错误，不路由到当前视图。\n\nsubscribe result 必须作为一个完整的 resync package 处理：\n\n1. 校验 snapshot identity，以及返回 gap 在 `afterSequence` 之后连续；\n2. 直接把 snapshot 安装为当前 canonical 基线；\n3. 不把已经包含在 snapshot 中的 gap event 再应用一次；\n4. 丢弃已经排队且 sequence 不大于 snapshot sequence 的重复 notification；\n5. 从 snapshot sequence 开始继续应用新的连续 live notification。\n\n只有 Session 与 Thread 确实出现相同的连续序列算法后，才提取一个只处理\n`aggregate identity + sequence` 的小型 helper；不能重新演变成保存所有领域状态的 store。\n\n### 7.2 临时流游标\n\ntransient update 由 `features/thread/update.rs` 处理，只影响低延迟显示：\n\n- cursor 只在同一 `streamInstanceId` 内连续；\n- `streamInstanceId` 改变时清空旧 transient buffer；\n- cursor 出现空洞时丢弃不可信 transient 内容并重新同步；\n- committed Item 到达后替换相同 Item 的 transient 版本；\n- transient 文本永远不能决定 Turn completed、failed 或 interrupted。\n\n## 8. `components/`：有局部状态的交互与呈现组件\n\n`components/` 与 `ui/` 的区别是：component 可以拥有局部交互状态，也可以理解少量产品展示\nvalue；但不能调用领域接口或保存 canonical aggregate。\n\n| Component | 拥有 | 不拥有 |\n| --- | --- | --- |\n| `interaction/` | composer 与 temporary view stack 的焦点、push/pop 和 routing | feature catalog、RPC、Thread lifecycle |\n| `composer/` | draft、Unicode cursor、attachments、paste bindings、slash parsing | Turn start、config mutation、App Server client |\n| `transcript/` | plain-text visible row、wrapping 与 scroll | canonical Thread snapshot、sequence、transient cursor；不复制 Native Markdown/diff/table 组件 |\n| `selection/` | tabs、query、filtered indices、selection 和通用列表渲染 | Session/Skill identity 的业务 action |\n\n`features/thread/update.rs` 完成 committed/transient item 合并并暴露有稳定 identity 的可见\nitems；`components/transcript/` 只负责把这些 items 布局和渲染。composer 提交时只产生\n`ComposerSubmission`，由 `features/thread/command.rs` 转成 Turn intent。这样同一份 Thread\n状态不会同时由 feature 和 component 保存。\n\ncomponent 可以依赖 `ui/` 原语和必要的 canonical value type，但禁止：\n\n- 直接调用任何 crate/App Server 领域接口；\n- 保存 Session membership、Thread lineage 或 durable sequence；\n- 决定 Tool 是否允许执行；\n- 根据展示文本判断产品终态；\n- 通过 callback/闭包把任意业务副作用藏进通用组件。\n\n## 9. `ui/`：可复用 Ratatui 原语\n\n`ui/` 可以包含：\n\n- layout helper；\n- selection list、tabs、picker 和 overlay 容器；\n- scroll state；\n- key hint 和 keymap 展示；\n- theme、color、spacing；\n- 通用 loading、error 和 empty-state widget。\n\n`ui/` 不得出现：\n\n- `SessionId`、`ThreadId`、`TurnId`；\n- `AppServerClient`、RPC Params 或 `ServerNotification`；\n- approval、model、plugin 等产品状态；\n- 完整的 Session browser 或 Thread transcript。\n\n通用 tabs、过滤和选择状态放在 `components/selection/`；“恢复哪个 Session”的 row model、\ntyped ID 和 action 属于 `features/sessions/`。`ui/` 只提供这两个上层模块共同需要的纯布局与\n样式函数。\n\n## 10. `terminal/`：真实终端基础设施\n\n宿主终端身份、multiplexer、色彩等级与背景回退解释的 crate contract 见\n[`zeta-terminal-detection`](../zeta-rs/terminal-detection/README.md)；本节只定义 TUI 对真实终端\nI/O 和 crossterm 生命周期的所有权。\n\n`terminal/` 负责：\n\n- raw mode、alternate screen 和 bracketed paste；\n- Crossterm event 读取；\n- Ratatui backend 和 frame scheduling；\n- terminal resize、reflow、cursor 和 scrollback；\n- 在独占 input window 中执行 terminal response probe 和控制序列；host terminal 身份、色彩等级及\n  background fallback 解释由 `zeta-terminal-detection` 提供；\n- panic、错误和正常退出时恢复终端。\n\n`TerminalSession` 必须使用 RAII 恢复 raw mode、alternate screen、paste mode 和 cursor。\n启动中途任一步失败，也必须回滚之前成功启用的能力。\n\n`terminal/` 不知道产品 Session、Thread、Turn、feature 或 App Server。它可以产生\n`TerminalEvent`，但不能发送 Agent `session/request`、打开 approval popup 或根据 Agent 状态决定文案。\n\n## 11. `features/`：Zeta 功能的垂直切片\n\n一个完整 feature 可以包含：\n\n```text\nfeatures/<name>/\n├── mod.rs\n├── state.rs\n├── command.rs\n├── request.rs\n├── view.rs\n└── <name>_tests.rs\n```\n\n不是每个 feature 都需要全部文件。`command.rs` 表达用户意图，`request.rs` 直接调用数据\nowner 的公开 typed interface，`view.rs` 组装 component。没有请求的 feature 不创建空\n`request.rs`，也不建立 `service.rs`、provider registry 或 feature-local facade。\n\n目标树中已经确定的首批 feature：\n\n| Feature | 职责 |\n| --- | --- |\n| `thread` | active Thread snapshot/update、Turn start/interrupt、transient merge 与页面组装 |\n| `sessions` | Session list/create/resume/archive 与 Thread create/fork/rewind/switch/archive |\n| `interactions` | owner-directed approval 与 structured user-input view/response mapping |\n| `config` | typed config read/update UI |\n| `skills` | typed catalog、enablement intent 和 selection row model |\n| `workspace_files` | `zeta-file-search` mention completion + typed filesystem browser/preview |\n| `status_line` | 汇集既有接口结果并执行 item 排列、降级与渲染 |\n\nresources 等已经有 typed contract、但尚无 `zeta code` 用户场景的能力不提前出现在\n目录树中，也不构成 TUI backlog。只有产品文档先接受具体场景后，才按同一文件契约\n添加 feature。\n\napproval、request-user-input 与 MCP 已在 canonical contract 完整后作为垂直切片接入；TUI 只声明\n实际支持的 interaction kind，App Server 选择唯一 owner，deadline/cancellation 不由 view 决定。\n以下能力仍不能仅因为其他产品存在就提前创建：\n\n- plugins、connectors 和 account/login surface；\n- hooks、goals、usage 和 review；\n- feedback、updates 和 visualization；\n- sub-agent 或 side conversation 导航。\n\n这些能力必须先进入 Zeta canonical domain 和 App Server API，具备 typed\nrequest/response/notification、顺序、取消、错误与恢复语义，然后 TUI 才添加对应垂直切片。\n未来 interaction kind 也必须先扩展同一个 capability/owner/request/response/deadline contract；\nTUI 不能靠检查 ToolCall 名称或 arguments JSON 自行弹窗并决定策略。\n\n外部 Agent 配置导入是明确的 Desktop-only 产品边界，不属于上述“等待 canonical contract\n后再进入 TUI”的潜在功能。TUI 不提供 `/add-dir`、`/import-agent`、目录选择器或等价的\n配置 mutation，也不主动扫描 `~/.codex`、`~/.claude` 等目录。Desktop 已经导入的外部 Skill\n仍可通过 App Server 统一 catalog 出现在 TUI `/skills` 中；这只是消费既有来源，不使 TUI\n成为导入或文件访问授权 owner。Desktop 工作流见\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#22-外部-agent-配置导入仅限-desktop)，\nSkill 来源边界见 [`skills.md`](skills.md#151-外部-agent-skill-导入仅限-desktop)。\n\nFeature 之间不能依赖彼此的私有模块。跨功能结果由 `app/` 协调，交互复用通过\n`components/`，纯布局复用通过 `ui/`；只有重复已经出现且语义一致时才提取公开的小型 value\ntype。\n\n### 11.1 `status_line/`：接口结果的展示模型\n\n长期应把 status line 作为完整的 presentation subsystem，但“完整”只表示它独立拥有 item\n选择、排列、宽度降级和渲染，不表示它接管数据来源。各项数据仍由相应领域 crate 或 App\nServer contract 的公开接口拥有，TUI 在事件/更新阶段直接调用这些接口，再把结果映射为可丢弃\n的 `StatusLineModel`。禁止建立通用 `StatusProvider`、`StatusStore`，也禁止在 draw 路径查询\nGit、配置或 Thread。\n\n| Item | 权威接口 | TUI 的职责 | 当前状态 |\n| --- | --- | --- | --- |\n| preferred model | `AppServerClient::read_config` | 把 `ConfigReadResult::preferred_model` 映射为长/短文案 | 已实现 |\n| workspace | `TuiOptions::workspace_root` | 保留完整路径和 basename 两种展示值 | 已实现 |\n| Git branch/state | App Server `git/status` + `git/statusChanged`，其 owner 调用 `zeta-git` | startup/read 与 notification 映射 branch/dirty/count | 已实现 |\n| Thread/Turn/usage | App Server typed snapshot/update | 消费 contract 已提供的字段，不从 transcript 推导 | contract 尚未提供完整 usage |\n| connection/runtime state | `client/` 与 `app/` 本地状态 | 只在已接受的用户场景中映射 | Potential；embedded TUI 当前无独立 connection UI 需求 |\n\n依赖方向固定为：\n\n```text\nowning crate interface / typed App Server result\n                    │\n                    ▼\n            app update coordination\n                    │\n                    ▼\n              StatusLineModel\n                    │\n                    ▼\n          pure layout + Ratatui render\n```\n\n`status_line/` 未来可以定义稳定的 item identity、用户选择与顺序、separator、alignment 和\noverflow policy；配置持久化必须先进入 typed config contract，不能由 renderer 私存。昂贵或\n异步接口在后台完成后以 event 更新模型；失败只影响对应 item，并保留其明确的 unavailable/\nstale 语义。任何新 item 都应先回答“哪个 crate/interface 拥有这个事实”，再添加展示映射和\n宽度测试。\n\n当前实现由 `features/status_line/model.rs` 拥有 model、Git projection 与宽度降级，\n`features/status_line/view.rs` 只负责右对齐渲染。usage 和可配置 item/order 缺少已接受的\ntyped contract，因此不是当前 TUI 完成项。\n\n## 12. `host/`：窄宿主能力\n\n`host/` 只放非终端 OS adapter，例如 clipboard、external editor、desktop notification 和\nIDE IPC。每个模块必须暴露窄能力，不能形成一个无所不包的 `PlatformService` 或\n`HostContext`。\n\n职责按“何时”与“如何”拆开：\n\n```text\nfeatures/thread：何时通知用户 Turn 已结束\nhost/notification：如何调用某个 OS 通知后端\n\ncomponents/composer、components/transcript：产生 copy/open-editor intent\nhost/clipboard、host/external_editor：如何访问宿主能力\n```\n\n宿主 adapter 不得反向依赖 component 或 feature workflow。\n\n## 13. 事件与命令流\n\n顶层数据流固定为：\n\n```text\nTerminalEvent / ClientResult / ServerNotification\n                      │\n                      ▼\n                   AppEvent\n                      │\n                      ▼\n              TuiApp state transition\n                │                 │\n                ▼                 ▼\n           AppCommand          redraw\n                │\n                ▼\n        typed client / host adapter\n                │\n                └──────────────► AppEvent\n```\n\n建议区分：\n\n- `AppEvent`：已经发生的事实；\n- `AppCommand`：等待执行的副作用意图；\n- feature event/command：feature 内部语义；\n- `TerminalEvent`：原始 key、paste、resize、tick；\n- `ServerNotification`：App Server typed notification。\n\n不要把所有类型压成一个包含任意闭包、JSON 或字符串 method 的总线。原始 key event 应先由\n当前焦点和 keymap 转换为用户意图，再进入业务 command。\n\n单个 event-loop iteration 应有界，长请求、resource 读取和大型 transcript projection 不得阻塞\nterminal event pump。重绘可以合并，但 committed update、输入和退出事件不能因为 frame\nthrottle 丢失。\n\n### 13.1 输入、焦点与浮层\n\n输入传播顺序固定为：\n\n```text\nsystem capture\n      ↓\ntop overlay\n      ↓\nactive feature\n      ↓\nfocused component\n      ↓\nparent bubble\n      ↓\nglobal fallback\n```\n\n局部 handler 返回 `Handled`、`Propagate` 或 typed intent；不能通过默认空 hook 把 approval、\nplugin 或 Session 等领域行为继续塞进通用 view trait。内置 overlay 优先使用可穷举的 enum 和\n显式 stack，只有真实外部扩展场景才使用动态 view object。\n\nCtrl-C 等冲突键按当前交互层级解释：可取消 overlay 优先关闭 overlay，局部搜索优先取消搜索，\nactive Turn 转成 interrupt，只有空闲顶层状态才退出。相同语义必须由键盘、鼠标和 command\nsurface 复用同一 intent，不各自执行副作用。\n\n### 13.2 控制面与流式数据面\n\n普通控制总线不承担全部 streaming 流量：\n\n```text\nApp Server streaming update\n        ↓\ntyped protocol decode\n        ↓\nper-Thread bounded stream buffer\n        ↓\nbatch / coalesce / fair scheduling\n        ↓\nfeatures/thread semantic update\n        ↓\ncoalesced frame request\n```\n\n| 事件类别 | Delivery class | 规则 |\n| --- | --- | --- |\n| 退出、用户提交、interrupt、approval response | Must deliver / ordered | 最高控制优先级，不被后台流量饿死 |\n| committed update、command completion、fatal error | Ordered lossless | 进入控制面；不得被 frame throttle 合并掉 |\n| assistant/process text delta | Ordered + recoverable gap | 正常路径连续应用；overflow 可丢 transient，但必须由 cursor gap 清除 projection 并 resync |\n| tool/progress preview | Latest wins / coalescable | 允许保留最新 revision |\n| draw request | Coalescable | 多次请求合并为一次 frame |\n| resize | Latest wins | 保留最新尺寸并触发必要 reflow |\n| 搜索建议等后台查询 | Latest generation wins | scope 或 generation 不匹配即丢弃 |\n| telemetry | Best effort | 不得反压用户交互 |\n\n每轮先处理高优先级控制事件，再按固定数量或时间预算消费数据面；一批数据只请求一次 frame。\ncompletion 是控制面 barrier：应用 completion 前必须消费或明确作废属于该 Turn 的先前数据。\nbuffer overflow、receiver lag 或 cursor gap 立即把对应 projection 标为需要 resync，不能静默\n丢弃 Must-deliver 语义。\n\n当前实现的三层上限是 App Server 每 connection 4096 条 notification、共享 client 1024 条 event、\nTUI EventPump 1024 条 runtime event；App Server 满载时先清 transient，control-only overflow 关闭\nconnection。Thread projection 每个 transient row 限 256 KiB、最多 1024 个 identity。当前 TUI\n产品支持边界不包含自动 reconnect；connection overflow 会显示 failure 并要求重启。这与已有\ncursor-gap snapshot resync 是两个不同恢复层级，不能把前者写成已承诺的 TUI 恢复阶段。\n\n### 13.3 展示映射\n\nTUI 不应长期保存原始 transport envelope。protocol crate 已经提供稳定 canonical\nsnapshot/update 时，feature 直接消费这些 typed value；只有满足以下至少一项时才增加\npresentation mapping：\n\n- wire object 字段庞大或变化频繁，而界面只需要稳定子集；\n- Desktop/TUI 等多个前端确实共享同一种 presentation semantic；\n- replay、脱敏 trace 或多种 render mode 需要稳定 identity/revision；\n- server-originated agent request 需要转换成不会泄漏 transport 细节的交互模型。\n\n不机械包装稳定 ID、简单 enum 或已经 canonical 的产品 value。mapping 只能降低耦合，不能\n成为第二套领域模型或兼容私有 wire DTO 的长期层。\n\n## 14. 公共 API 与 CLI 所有权\n\n`zeta-tui` 的公开 API 应保持很小：\n\n```rust\npub fn run(\n    session: AppServerSession,\n    options: TuiOptions,\n) -> Result<TuiExit, TuiError>;\n```\n\n`run` 从已经初始化的 `AppServerSession` 获得 request handle 与 event stream，并在退出路径\n显式调用 `shutdown()`、等待 background driver join。它不接受启动参数后自行选择 transport，\n也不接受生命周期不明确的裸 transport/client。当前 Rust host 保持同步入口，connection\nnotification 与 terminal input 已由独立 source 唤醒；共享 typed method 保持同步返回，但 TUI\n通过 `RequestTask → app::request_completion` 将等待和结果应用分开。\n\n入口建议使用 enum 表达，避免含义不明的 bool 或 `Option`：\n\n```rust\npub enum TuiEntry {\n    CreateSession { title: String },\n    OpenSession { session_id: SessionId },\n    OpenThread {\n        session_id: SessionId,\n        thread_id: ThreadId,\n    },\n}\n```\n\nCLI 负责：\n\n- 判断 stdin/stdout 是否为 TTY；\n- 解析命令行和选择 interactive 模式；\n- 加载 config 和 state root；\n- 选择 in-process、daemon 或 remote transport；\n- initialize 并校验 schema hash；\n- 构造 `TuiOptions` 和已初始化 `AppServerSession`；\n- 把 `TuiExit` 映射为进程退出码。\n\nTUI 负责 terminal 打开后的交互，不应反向读取 CLI arguments 或自行打开本地 App Server。\n\n## 15. 依赖规则\n\n| 模块 | 可以依赖 | 禁止依赖 |\n| --- | --- | --- |\n| `app` | client、features、components、ui、terminal、host | Core、Storage、具体 widget 实现、JSON-RPC 字符串 |\n| `client` | app-server-client、app-server-protocol、内部 value type | Ratatui、view、Core、私有 wire DTO |\n| `features` | owner crate 的 public interface、typed App Server client、components、ui、canonical value | 其他 feature 私有模块、Core、手写 RPC |\n| `components` | ui、Ratatui、必要的 canonical presentation value | client、领域请求、canonical aggregate authority |\n| `ui` | Ratatui 和纯展示 value | 产品 ID、App Server、业务状态 |\n| `terminal` | Crossterm、Ratatui backend、纯 terminal value | AppEvent、产品 ID、feature |\n| `host` | 窄 OS library | app、components、feature workflow |\n\n整体方向：\n\n```text\napp\n├─ client ─────────────────────► zeta-app-server-client\n├─ features ───────────────────► owning crate public interfaces\n│  └─ components ──────────────► ui + Ratatui\n├─ components ─────────────────► ui + Ratatui\n├─ ui ─────────────────────────► Ratatui\n├─ terminal ───────────────────► Crossterm + Ratatui backend\n└─ host ───────────────────────► narrow OS libraries\n```\n\nfeature 定义自己的 intent，并由同目录 `request.rs` 直接映射到 owner interface；`app` 只调度\nintent 和 result event，不维护一份跨领域 operation enum。view 产生 Ratatui render data，\n由 `app` 在 terminal frame 中装配；feature、component 和 ui 都不依赖 terminal。\n\n禁止新增含义模糊的 `runtime`、`service`、`common` 或 `platform` 聚合层。共享代码必须根据\n其真正职责进入 feature、component、ui、client、terminal 或某个窄 host module。\n\n## 16. 当前实现与产品支持边界\n\n当前 `zeta-code/tui/src/` 已完成第一阶段物理 ownership 重排，并迁到 owned session/event\ncontract：\n\n```text\napp/\n├── mod.rs\n├── bootstrap.rs\n├── command.rs\n├── dispatch.rs\n├── event.rs\n├── event_loop.rs\n├── frame/\n├── help.rs\n├── request_completion.rs\n├── state.rs\n└── state_tests.rs\nclient/\n├── mod.rs\n├── command_id.rs\n├── event_pump.rs\n├── notification.rs\n├── request_task.rs\n└── *_tests.rs\ncomponents/\n├── mod.rs\n├── composer/\n├── interaction/\n├── selection/\n└── transcript/\nfeatures/\n├── mod.rs\n├── config/\n├── interactions.rs + interactions/\n├── sessions/\n├── skills/\n├── status_line/\n├── thread/\n│   ├── presentation.rs\n│   ├── projection.rs\n│   ├── request.rs\n│   ├── state.rs\n│   ├── subscription.rs\n│   └── update.rs\n└── workspace_files/\nhost/\n└── clipboard.rs\nterminal/\n├── mod.rs\n├── session.rs\n└── session_tests.rs\nui/\n├── mod.rs\n├── layout.rs\n├── layout_tests.rs\n└── theme.rs\nlib.rs\nlib_tests.rs\n```\n\n已经落地的边界：\n\n- 通过 `zeta-app-server-client` 的 typed method 工作；\n- 明确声明权威 Thread/Turn 状态留在 App Server 后面；\n- `App` 不再持有 file-search worker/channel；`AppCommand` 描述待执行副作用，外部结果统一以\n  `AppEvent` 进入 `App::update`，`FileSearchManager` 由 event loop 持有；\n- `client/event_pump.rs` 独立等待 terminal 与 `AppServerEvents`，并把两者汇入单写者 loop；\n  `client/notification.rs` 把共享 connection event 映射成 typed `ClientEvent`，保留\n  `agent/request`、`skills/changed`、Git、`ThreadUpdateEnvelope` 和 connection failure；event\n  channel 有界，Tick 可丢弃而 input/control 不静默丢失；\n- `client/RequestTask` 在 worker 执行 typed request，`app/request_completion.rs` 校验 scope 并把\n  completion 安装到单写者 state；同一 request slot 前的用户 intent 保序排队，全部 product\n  command、Turn mutation、文件浏览与 subscription switch 均不在 draw/input 线程等待；\n- `features/thread/ThreadSubscription` 在启动和 active Thread 切换时调用 typed\n  `session/thread/subscribe`/`session/thread/unsubscribe`，验证 Session/Thread scope，并用最后确认的 snapshot\n  sequence 丢弃重复或旧 scope update；stream instance/cursor 单独排序 transient，gap/runtime\n  switch 清除不可信 row 并请求 snapshot；\n- newer durable sequence（包括 gap）只触发 `session/thread/read` resync；TUI 不执行 `ThreadEvent`\n  reducer。`refresh_turn` 不再二次 drain notification，因此不会吞掉订阅事件；\n- `features/thread/ThreadFeatureState` 已成为 active Thread snapshot 与当前 transcript projection\n  的唯一 TUI owner；本地 optimistic user message、notice 与 failure 也通过 feature event\n  进入同一 owner，下一份 canonical snapshot 会替换 projection；\n- `features/thread/projection.rs` 显示完整 ThreadItem，并有界保存最多 1024 个 transient identity、\n  每个 row 256 KiB；`components/transcript` 只负责 role/detail layout 和 scroll；\n- `components/transcript` 已拥有 transcript row wrapping、role chrome、empty state 与只读\n  Ratatui view，以及 component-facing `Message`/`MessageRole`；它不依赖 feature、`App` 或保存\n  canonical Thread；\n- `features/thread/request.rs` 只构造并执行 typed Thread/Turn request，返回 typed result；\n  request module 不引用或更新 `App`。event loop 把结果转换为 `AppEvent`，presentation module\n  只把 canonical Turn snapshot 分类为可展示 outcome；\n- `features/sessions/ActiveConversation` 拥有当前 product Session/Thread identity 与 sequence，\n  create/fork/rewind/resume/switch/archive 返回 conversation change/notice，不直接写 `App`；新的\n  canonical snapshot 由后台 subscription completion 安装。Session picker、Thread active/archived\n  tabs 与 replacement lifecycle 由同一 feature 拥有；\n- `features/interactions` 把 owner-directed full request 转成 approval 或多问题 user-input Pane，\n  只返回 exact typed response；owner selection、deadline 与 cancellation 留在 App Server；\n- `features/config/request.rs` 与 `features/skills/request.rs` 分别拥有已有 typed config/MCP/model\n  与 Skill catalog/enablement 调用，App 不再内联这些领域 payload；\n- `components/selection` 已同时拥有 generic tabs/query/filter/selection state、输入 outcome 与\n  Ratatui view；`InteractionPane`、App 和产品 view builder 只消费该 component contract；\n- `ui/layout.rs` 拥有跨 presentation surface 复用的纯 geometry；`ui/theme.rs` 只拥有共享主题\n  snapshot 到终端色彩能力的窄投影，用户文件解析与完整 token catalog 留在 `zeta-theme`；\n  component 不反向依赖 frame coordinator；\n- `App` 处理 presentation coordination 与全局键，并直接委托 `InteractionPane` 的\n  composer/temporary-view 输入；`ChatWidget` 与过渡目录 `toppane/` 已移除，不再存在第二份\n  transcript 或模糊的 top-pane owner；\n- composer、editor、attachment、paste、slash/mention state 与各自纯 view 已迁入\n  `components/composer/`；temporary stack 已迁入 `components/interaction/`；\n- update-driven snapshot resync 先应用完整 canonical Thread，再把\n  completed/waiting/failed/interrupted 映射为 presentation lifecycle；active Turn 的定时\n  snapshot polling 已移除，Turn completion 不再单独追加 agent 文本；\n- `InteractionPane` 保留 composer 并拥有 temporary view stack；generic selection view 已支持\n  tabs、直接输入搜索、过滤、循环选择、左右/Tab 切页和 Esc/Ctrl-C 出栈；`/help` 提供\n  Commands/Keys，`/skills` 从 typed `skills/list` 提供\n  All/Enabled/Disabled/Errors catalog tabs；\n- `/skills` 只消费 App Server catalog snapshot，不读取 `zeta-skills` filesystem；\n  `Space` 将 exact `SkillId` 转成 revision-checked `skill/enablement/set`，成功后刷新页面；\n  `skills/changed` 也会刷新前台页面。enablement 不等于正文 activation，TUI 当前没有\n  Skill context injection；\n- `app/frame/` 只装配 frame；各 component/feature view 拥有自己的 surface。layout 把所有\n  interaction surface 显式锚定在 terminal 底部：composer/footer\n  固定到底部，slash/mention popup 从 composer 上沿向上展开，temporary view 保持底边不动并\n  只向上占用 transcript 空间；\n- `TerminalModeGuard` 在任一 mode 获取失败时按逆序恢复已经获取的 terminal mode，显式\n  restore 和 Drop 共享幂等清理路径；\n- `StatusLineModel` 直接映射 typed config/Git result 与 `TuiOptions::workspace_root`；\n  `features/status_line/view.rs` 只读取模型，并按可用宽度从完整 model/workspace/Git 降级到短值\n  或省略号；\n- `ChatComposer` 协调提交、popup keys、range completion application 与 structured local\n  command dispatch；`zeta-slash-commands` 拥有 slash grammar、catalog、matches、selection 与\n  dismiss，Ratatui popup renderer 根据自身 viewport 投影可见范围，`TextArea` 只拥有 UTF-8\n  编辑状态、原子 command element 和局部 keymap 扩展边界；当前没有 Vim 产品要求；\n- bracketed paste 使用独立事件路径；超过 1000 个 Unicode scalar value 的内容由 `PendingPastes`\n  绑定到 `TextArea` 原子占位符，并在提交前展开；\n- 粘贴 PNG/JPEG/GIF/WEBP 本地文件路径会由 `Attachments` 立即读取并绑定为 `[Image #N]`\n  原子占位符；提交保持 text/image 顺序并通过 typed `session/request` 进入 durable Thread history；\n- `Ctrl-V` 产生独立 `AppCommand::ReadClipboardImage`，由 `host/clipboard.rs` 读取文件列表\n  或 RGBA 位图、编码 PNG，并复用 `Attachments` 的校验、占位符与结构化提交；\n- event loop 持有的 `FileSearchManager` 通过 `zeta-file-search::PathSearchHandle` 在后台增量\n  遍历 workspace，并使用完整 `nucleo` engine 更新 `@token` fuzzy results；snapshot 作为\n  `AppEvent` 回到单写者。`Mentions` 只拥有 token/popup 状态、高亮、keyboard/mouse selection\n  和原子文本路径 completion。旧 query snapshot 会在 manager 和 popup 边界被丢弃；两者都不\n  读取候选文件内容，也不构造结构化 app/plugin Mention；\n- crate root `lib.rs` 只保留 public startup contract 与错误类型；事件循环、bootstrap、\n  built-in dispatch、Thread request 和 frame coordination 都有明确的 private owner；\n- 所有写请求通过 `client::new_command_id` 分配 typed `CommandId`，不再在 crate root 复制\n  command ID 拼装逻辑。\n\n当前产品支持边界与非目标：\n\n- transcript 采用 bounded plain-text wrapping、分页键盘滚动、最后回复 copy 与当前已加载 history\n  window 的 Markdown export。\n  Native Agent Timeline 的 Markdown/table、任意 pointer selection、折叠与虚拟化属于 `zeterm`，\n  不是 TUI 的“尚未完成”；\n- Mouse 只覆盖 slash/file-mention popup 的必要左键命中。完整 pointer/selection 交互不属于当前\n  `zeta code` 要求；Vim mode/motion/operator 也没有被产品文档接受；\n- 当前入口消费 embedded `AppServerSession`，不提供 remote selector 或自动 reconnect。若未来接受\n  远程产品需求，connection/recovery contract 必须先进入 `zeta-app-server-client`；\n- workspace mention 当前只插入原子文本路径。通用 app/plugin Mention、login、compact、service\n  tier、usage、review 等没有已接受 contract 的 surface 不注册；\n- 图片输入已形成“本地路径/系统 clipboard → 草稿 data URL → App Server 分块上传 →\n  `ImageAttachmentRef` → durable `UserImageAttachment` → provider 临时 image block”纵切。TUI\n  不建立私有 blob store；Thread history 与 command receipt 不持久化 data URL；\n- status line 已有 model/workspace/Git；usage 与稳定 item/order 没有 typed contract，因此不是通过\n  transcript 推导的 TUI 缺口；\n- Config surface 可读 provider、MCP、Skill source、Plugin request、Hook、language server 状态；\n  当前只有已有 typed mutation 的 model/MCP/Skill 可修改，TUI 不接管 Desktop-only 外部 Agent\n  导入或凭据配置。\n\n新增能力必须先证明是 `zeta code` 产品要求，再按 canonical contract 和垂直 feature 接入；不能\n因为 Native 已有 richer component，或某能力技术上可实现，就把它复制成 TUI backlog。\n\n## 17. 已接受的架构迁移顺序\n\n当前阶段状态：\n\n| 边界 | 状态 |\n| --- | --- |\n| terminal session RAII 与 partial-failure rollback | Current |\n| `app/state.rs`、`app/event.rs`、`app/command.rs` | Current |\n| runtime file-search worker 与 `App` state 分离 | Current |\n| typed notification 适配、active Thread subscription 与 snapshot gap/resync | Current |\n| independent request driver 与 wakeable notification pump | Current |\n| request completion 的非阻塞 app command dispatch | Current |\n| canonical `features/thread` snapshot 与 transcript projection owner | Current |\n| `components/transcript` 与 `ui` layout/theme 原语 | Current |\n| `components/selection` state/view 边界 | Current |\n| composer/interaction component 物理边界 | Current |\n| Thread transient merge、cursor recovery 与 bounded data plane | Current |\n| Session/Thread picker、archive 与恢复 | Current |\n| owner-directed approval / user input / deadline | Current |\n| 多行 composer 与 active-Turn follow-up queue | Current |\n| bounded Thread history window 与 Ctrl-Home 增量加载 | Current |\n| command copy/export 与 Ctrl-Z suspend/resume | Current |\n\n### 阶段零：固定行为与性能基线\n\n在移动 owner 或改变 event loop 前：\n\n1. 固定 terminal open/partial-failure/Drop restore、输入、submit/interrupt、resize 和 popup 的\n   行为测试；\n2. 记录 startup first frame、输入到 frame、snapshot/update 到 frame、render duration 和\n   resize/reflow duration；\n3. 清点 `lib.rs`、`App`、`InteractionPane` 的状态 owner、I/O 调用、task/channel 与 protocol\n   type 引用；\n4. 为 dependency direction 建立可在 CI 执行的检查；\n5. 确认每个迁移切片只有一个 source of truth，旧层只允许单向转换。\n\n退出条件：关键行为可自动验证，性能回退可比较，当前 implementation limitation 已在 crate\nREADME 中记录。\n\n### 阶段一：建立边界\n\n1. 将 terminal RAII 移入 `terminal/session.rs`；（Current）\n2. 将纯 TUI state/event/command 移入 `app/`；（Current）\n3. 将 notification adapter、`CommandId` 与 subscription tracking 移入明确 owner；\n   （wakeable event pump、notification adapter、`CommandId` 与 Thread subscription tracking\n   以及 request completion 的 app-level 非阻塞调度均为 Current）\n4. 建立 `features/thread/`，用 canonical Thread snapshot 替换扁平 message authority；\n   （snapshot/projection owner、typed request、durable sequence/gap snapshot resync 为 Current；\n   transient merge 和完整 ThreadItem projection 也为 Current）\n5. 把 bootstrap `toppane/` 与各 presentation surface 按职责迁入\n   `components/transcript/`、`components/interaction/`、`components/selection/` 和\n   `components/composer/`；（Current；旧 `toppane/` 与顶层 `render/` 已移除）\n   保留 `InteractionPane → ChatComposer → TextArea` 的局部 ownership；\n6. 将 public `run` 收敛为接收 owned `AppServerSession`，并在所有正常/错误退出路径显式\n   shutdown。（Current）\n\n退出条件：terminal 不依赖产品状态；可测试 TUI state 不含 client、terminal、channel 或 task；\n现有入口与用户可见行为不变。\n\n### 阶段二：订阅与恢复\n\n1. 使用 `session/subscribe` 和 `session/thread/subscribe`；（Current）\n2. 实现 durable gap、duplicate、aggregate mismatch 和 resync；（active Thread snapshot\n   resync 为 Current）\n3. 实现 transient cursor、runtime switch、gap reset 和 committed snapshot 替换；（Current）\n4. 把稳定 `CommandId` 生命周期集中到 `client/command_id.rs`；（Current）\n\n退出条件：Turn 执行期间输入和 redraw 不被 request completion 阻塞；duplicate、gap、lag、\nconnection close 和 runtime 切换都有确定结果；当前 embedded client 不会把结果未知的写入当成\n失败后新命令重放。Remote reconnect 不在本阶段范围内。\n\n### 阶段三：核心交互\n\n1. 在 `features/sessions/` 完成 Session list/resume/archive；（Current）\n2. 完成 Thread create/fork/rewind/switch/archive；（Current）\n3. 让 Turn start/interrupt 全部经过 `features/thread/request.rs`；（Current）\n4. 让 Thread projection 与 `components/transcript/` 展示完整 ThreadItem；（Current）\n5. 在对应 component 内完成当前产品要求的 scroll 与 composer history；（Current）\n\n退出条件：active Thread state 只有一个 TUI owner；完整 typed Item lifecycle 可呈现；transient\n流量不会决定 durable terminal state；render 不推进 semantic state。\n\n### 阶段四：垂直功能\n\n按已接受的 App Server contract 逐个添加 config、resources、approval 等 feature。config、MCP、\nSkill、workspace file browser、Git status、approval 与 user input 已按该规则接入；每个后续 feature\n同时交付 state、typed command、view、错误/恢复行为和测试，不采用先建一个全局\n`services/` 再逐步塞逻辑的方式。\n\n不进行一次性 `git mv` 大重排。每一步都应保持 crate 可编译、现有入口可运行，并让新 owner\n和测试一起迁移。\n\n每个新增 feature 的退出条件相同：功能主要修改集中在自己的目录，不向顶层大 enum 或\n`App` 或 component 增加完整领域状态，所有异步结果携带 scope/generation，failure 与 resync\n行为和 feature 一起交付。\n\n### 潜在阶段：抽取公共能力\n\n独立 runtime/component crate 或第三方 UI API 不是当前 roadmap。只有至少两个真实消费者已经\n使用同一内部边界、API 稳定多个版本且抽取能减少依赖与转换时才重新评估；否则继续在单 crate\n内使用 private module。当前 `zeta-ansi-escape` 是已经完成的窄 dependency adapter，不是可复用\ncomponent 或第三方 UI API，也不放宽其他 presentation state 的抽取门槛。\n\n## 18. 测试\n\n测试按 owner 放置：\n\n- 新单元测试模块使用 sibling `*_tests.rs` 和显式 `#[path = \"..._tests.rs\"]`；\n- `features/thread` 测试覆盖连续 update、重复 delivery、durable gap、runtime 切换、\n  transient/committed 合并和 resync；\n- feature request 测试使用 fake/mock typed client 验证 payload、稳定 CommandId 和错误映射；\n- component 测试覆盖 Unicode width、plain-text wrapping、resize、局部交互和纯渲染；\n- client 测试覆盖 event pump、pending completion 和 subscription transport lifecycle；\n- terminal 测试覆盖部分初始化失败与 Drop 恢复；\n- feature 测试覆盖 key intent → command → result event → view state；\n- crate 级 `tests/` 覆盖 create/resume/fork/interrupt 和 subscription recovery；\n- Ratatui `TestBackend` 与 snapshot 只验证稳定布局，不替代状态断言。\n\n测试支持代码也按 owner 拆分。只有确实跨多个模块且 API 稳定的 fixture 才进入 crate 级\n`tests/support/`，不能建立全局 `test_utils.rs` 杂物箱。\n\nRust 模块目标保持在 500 行以内；文件接近 800 行时，新功能必须进入新模块。新增 public\ntrait 必须有 doc comment，说明其职责、实现约束和调用方预期。\n\n目标测试矩阵（Proposed）：\n\n| 层级 | 输入与断言 | 不应依赖 |\n| --- | --- | --- |\n| feature state/update | state + event → state + command + invalidation | terminal、真实网络、真实时间 |\n| client contract | typed request/result/notification、identity 与 error mapping | Ratatui、Core private API |\n| component | local intent、Unicode/grapheme、layout 与 view model | RPC、canonical reducer |\n| render golden | 40/80/120 列、CJK/emoji、长 URL、capability、resize | 业务副作用 |\n| terminal behavior | cursor、scroll region、clear、suspend/resume、partial open failure | 产品 feature |\n| trace replay | 初始 presentation state + 脱敏 event trace → digest/key frames | 原始敏感 payload |\n| end-to-end | create/resume/fork/interrupt、subscription recovery、shutdown | 私有 transport shortcut |\n\nproperty test 优先覆盖 generation 单调、stale result 不改变状态、overlay stack 合法、\ntranscript revision/cache invalidation、cursor 保持在 viewport、shutdown 后无活动 task，以及\nbounded buffer 不丢 Must-deliver event。\n\nCI 应以简单、可解释的检查固定依赖方向：\n\n- `terminal/` 不依赖 `app`、feature 或产品 ID；\n- `ui/` 不依赖 App Server 与产品状态；\n- `components/` 不依赖 client 或领域请求；\n- renderer/view 不调用 I/O 或 `tokio::spawn`；\n- feature 不手写 JSON-RPC，不依赖其他 feature 的 private module；\n- 只有 client/protocol boundary 解释 transport notification。\n\n## 评估、可观测性与发布\n\n架构迁移必须以行为与性能不回退为前提。至少跟踪：\n\n- startup first-frame duration；\n- terminal input 到 visible feedback 的 P50/P95；\n- stream/update 到 frame 的 P50/P95；\n- render 与 resize/reflow duration；\n- frame request、实际 frame、coalesced frame 数量；\n- control/data queue depth、batch size、overflow/lag；\n- stale result 丢弃、request timeout/cancel 和 resync 原因；\n- shutdown task drain/cancel 结果。\n\n日志记录 event/command 名称、feature、request identity metadata、scope、duration 与结果类别，\n不记录用户输入、模型输出、文件内容或 secret。可重放 trace 默认脱敏 payload，并明确区分\nmetadata-only 诊断与包含测试 fixture 的受控 trace。\n\n每个迁移 PR 必须：\n\n1. 只迁移一个明确 owner 或一条完整垂直切片；\n2. 保持旧入口可用并可独立回滚；\n3. 给出行为测试以及相关性能对比；\n4. 删除旧 source of truth，或明确兼容 adapter 的单向性与删除条件；\n5. 同步 crate README 的当前事实和本文的阶段状态。\n\n## 主要风险与防线\n\n| 风险 | 识别信号 | 防线 |\n| --- | --- | --- |\n| Action/command 形式化过度 | cursor movement 等局部操作也穿过全局总线 | 只有跨 component/feature、异步或需 trace 的行为进入顶层 |\n| 重新形成巨型 `AppEvent`/`AppCommand` | 新 feature 必须持续修改全局变体 | 顶层只路由 feature-local enum |\n| 机械 protocol mapping | 稳定 ID/enum 被逐一包装 | 只转换高变、展示语义不同或 replay 所需对象 |\n| 控制面与数据面双状态源 | buffer 和 feature 都决定 active Turn | buffer 只存未合并事件，主循环仍是唯一 semantic writer |\n| 迁移期双 owner | old presentation container 与新 feature 双向同步 | 每个切片明确唯一 source of truth，adapter 只能单向 |\n| 过早拆 crate | public API 和类型转换快速增长 | 默认先用 private module；窄 dependency adapter 必须保持无产品状态并单独固定 failure contract |\n| terminal 行为回退 | 架构重排同时改变 cursor/viewport | 先固定 behavior test，terminal 与 feature 分阶段迁移 |\n| 数据面饿死输入 | delta backlog 提高按键延迟 | 控制优先级、每轮 budget、batch/coalesce 与 resync |\n\n明确拒绝只拆文件、全面重写、每个组件一个 actor、全局响应式 signal、ECS，以及在产品 contract\n稳定前开放第三方 UI API。这些方案不能改善 Zeta 当前最关键的 authority、顺序和恢复边界，\n却会增加新的生命周期与同步成本。\n\n## 19. 验收\n\n- TUI crate 不直接依赖 Core、Storage、Exec、Sandbox 或 Model Provider；\n- CLI 向 TUI 传入已初始化 typed client，TUI 不自行建立第二套 composition root；\n- 每个 feature 的本地状态可以从相应 typed snapshot/result 完整重建；\n- 不存在跨 Session/Thread/config 等领域的通用 `ProjectionStore`；\n- active Thread snapshot、sequence 和 transient merge 只有 `features/thread` 一个 TUI owner；\n- durable sequence 和 transient cursor 分开处理；\n- sequence gap、runtime 切换和未知 update 会触发 resync，而不是猜测状态；\n- `expectedSequence` 来自正确 aggregate，逻辑重试复用原 `CommandId`；\n- transcript 展示完整 typed Turn/ThreadItem lifecycle；\n- Ctrl-C 在 Turn 运行时优先发出 `session/request` 的 `InterruptTurn`，空闲时才退出；\n- terminal 在正常退出、错误、panic 和 Unix termination signal 路径均可恢复；\n- feature 只通过 owner crate 的 public interface 或已接受的 App Server contract 工作；\n- UI 原语、terminal 基础设施和 host adapter 不依赖产品状态；\n- control plane 不被 streaming data plane 饿死，overflow/lag 会触发确定 resync；\n- stale request result 不能覆盖新的 scope/generation；\n- render 不执行 I/O、不 spawn、不推进 semantic state；\n- migration 不保留双向同步的第二 source of truth；\n- 单元、feature-state、transport、render 和端到端测试覆盖主要恢复路径。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-基准采用与-zeta-修正",
        "title": "2. 基准采用与 Zeta 修正"
      },
      {
        "depth": 2,
        "id": "3-产品状态与-tui-状态",
        "title": "3. 产品状态与 TUI 状态"
      },
      {
        "depth": 2,
        "id": "长期架构不变量",
        "title": "长期架构不变量"
      },
      {
        "depth": 3,
        "id": "单写者",
        "title": "单写者"
      },
      {
        "depth": 3,
        "id": "状态分类",
        "title": "状态分类"
      },
      {
        "depth": 3,
        "id": "纯渲染",
        "title": "纯渲染"
      },
      {
        "depth": 3,
        "id": "显式副作用与局部协议",
        "title": "显式副作用与局部协议"
      },
      {
        "depth": 3,
        "id": "请求关联与过期结果",
        "title": "请求关联与过期结果"
      },
      {
        "depth": 3,
        "id": "控制面数据面与恢复",
        "title": "控制面、数据面与恢复"
      },
      {
        "depth": 3,
        "id": "内部优先",
        "title": "内部优先"
      },
      {
        "depth": 2,
        "id": "4-目标目录",
        "title": "4. 目标目录"
      },
      {
        "depth": 2,
        "id": "5-app应用协调",
        "title": "5. app/：应用协调"
      },
      {
        "depth": 2,
        "id": "6-clienttui-到-typed-客户端的窄适配",
        "title": "6. client/：TUI 到 typed 客户端的窄适配"
      },
      {
        "depth": 2,
        "id": "7-featuresthreadactive-thread-的唯一-tui-所有者",
        "title": "7. features/thread/：active Thread 的唯一 TUI 所有者"
      },
      {
        "depth": 3,
        "id": "71-持久化序列",
        "title": "7.1 持久化序列"
      },
      {
        "depth": 3,
        "id": "72-临时流游标",
        "title": "7.2 临时流游标"
      },
      {
        "depth": 2,
        "id": "8-components有局部状态的交互与呈现组件",
        "title": "8. components/：有局部状态的交互与呈现组件"
      },
      {
        "depth": 2,
        "id": "9-ui可复用-ratatui-原语",
        "title": "9. ui/：可复用 Ratatui 原语"
      },
      {
        "depth": 2,
        "id": "10-terminal真实终端基础设施",
        "title": "10. terminal/：真实终端基础设施"
      },
      {
        "depth": 2,
        "id": "11-featureszeta-功能的垂直切片",
        "title": "11. features/：Zeta 功能的垂直切片"
      },
      {
        "depth": 3,
        "id": "111-statusline接口结果的展示模型",
        "title": "11.1 statusline/：接口结果的展示模型"
      },
      {
        "depth": 2,
        "id": "12-host窄宿主能力",
        "title": "12. host/：窄宿主能力"
      },
      {
        "depth": 2,
        "id": "13-事件与命令流",
        "title": "13. 事件与命令流"
      },
      {
        "depth": 3,
        "id": "131-输入焦点与浮层",
        "title": "13.1 输入、焦点与浮层"
      },
      {
        "depth": 3,
        "id": "132-控制面与流式数据面",
        "title": "13.2 控制面与流式数据面"
      },
      {
        "depth": 3,
        "id": "133-展示映射",
        "title": "13.3 展示映射"
      },
      {
        "depth": 2,
        "id": "14-公共-api-与-cli-所有权",
        "title": "14. 公共 API 与 CLI 所有权"
      },
      {
        "depth": 2,
        "id": "15-依赖规则",
        "title": "15. 依赖规则"
      },
      {
        "depth": 2,
        "id": "16-当前实现与产品支持边界",
        "title": "16. 当前实现与产品支持边界"
      },
      {
        "depth": 2,
        "id": "17-已接受的架构迁移顺序",
        "title": "17. 已接受的架构迁移顺序"
      },
      {
        "depth": 3,
        "id": "阶段零固定行为与性能基线",
        "title": "阶段零：固定行为与性能基线"
      },
      {
        "depth": 3,
        "id": "阶段一建立边界",
        "title": "阶段一：建立边界"
      },
      {
        "depth": 3,
        "id": "阶段二订阅与恢复",
        "title": "阶段二：订阅与恢复"
      },
      {
        "depth": 3,
        "id": "阶段三核心交互",
        "title": "阶段三：核心交互"
      },
      {
        "depth": 3,
        "id": "阶段四垂直功能",
        "title": "阶段四：垂直功能"
      },
      {
        "depth": 3,
        "id": "潜在阶段抽取公共能力",
        "title": "潜在阶段：抽取公共能力"
      },
      {
        "depth": 2,
        "id": "18-测试",
        "title": "18. 测试"
      },
      {
        "depth": 2,
        "id": "评估可观测性与发布",
        "title": "评估、可观测性与发布"
      },
      {
        "depth": 2,
        "id": "主要风险与防线",
        "title": "主要风险与防线"
      },
      {
        "depth": 2,
        "id": "19-验收",
        "title": "19. 验收"
      }
    ],
    "searchText": " 物理位置：zeta-code/tui/；ANSI/Ratatui adapter：zeta-code/ansi-escape/ 宿主：zeta-code/cli/ 文档所有权：本文是 TUI 跨 crate ownership、长期不变量、产品支持边界与已接受架构迁移顺序的 canonical 文档。 当前实现接口与事件循环：zeta-code/tui/README.md 产品接口基线：zeta-app-server-api.md App Server 启动与连接基线：app-server-client.md Workspace 边界基线：zeta-rs-architecture.md 产品线归属基线：product-lines.md 快速理解 zeta code 的 TUI 把 App Server 的权威状态转换成终端中的可交互呈现；它拥有单写者的界面状态和事件循环， 不拥有产品事实或后台执行。 发生的事情 TUI 负责 不负责 --- --- --- 用户按键或提交命令 转成类型化界面命令并发给客户端 直接修改 Session 或 Thread 服务端产生更新 归约为单写者 presentation state 并请求重绘 重新解释领域事件 高频流式文本到达 通过专用临时数据面更新显示 把每个片段伪装成持久事实 检测到序列缺口 暂停推断并请求权威快照 用本地状态填补缺口 请求完成、过期或取消 按关联身份丢弃过期结果并更新交互 决定工具重试或 Agent 恢复 渲染一帧 纯读取当前呈现状态 在绘制过程中触发业务副作用 加载主题 消费共享 snapshot 的明确子集并按终端能力降级 复制 Desktop 的全部主题能力或默认色目录 外部 Agent 来源已经进入统一 Skill catalog 浏览、启用或禁用已有条目 导入外部 Agent 配置、选择目录或扫描用户主目录 1. 结论 TUI 正式采用以下长期稳态架构基准： 窄而稳定的终端基础设施 + 单写者的 presentation state + 分域 event/command + 垂直功能切片 + 高频流式事件专用数据面 + 无业务副作用的渲染。 这套基准约束新功能和迁移方案，但不是对 Codex TUI 目录、feature catalog 或领域状态的复制。 Zeta 的产品 authority、typed contract 和 crate dependency direction 仍由本仓库现有架构决定。 状态必须按下表阅读： 内容 状态 Canonical owner --- --- --- 当前 wakeable event loop、typed Thread subscription、snapshot/transient resync 与交互 Current zeta-code/tui/README.md 本文的 ownership 与长期不变量 Accepted architecture baseline 本文 independent request driver、wakeable event pump Current app-server-client.md 与 crate README 非阻塞 request completion dispatch、有界 transient data plane、Session/Thread/interaction 垂直切片 Current zeta-code/tui/README.md plain-text transcript、必要 popup mouse hit 与 embedded session Current product support boundary 本文与 crate README active-Turn follow-up queue、多行 composer、copy/export、分页历史与 suspend/resume Current zeta-code/tui/README.md 尚无 zeta code 产品要求或 canonical contract 的 feature 非目标或 Potential；不构成实现承诺 对应产品线、领域与 App Server API 文档 本文只为已经接受的 zeta code 能力规定架构与迁移顺序。“某能力在 TUI 中不存在”不自动产生 产品 backlog。Native Agent Timeline 的 Markdown、table、selection、折叠与虚拟化由 zeterm/docs/native-agent-console.md 规划，具体 Native Markdown 组件由 zeterm/markdown 拥有；TUI 不追求与其 feature parity。Vim、remote selector/reconnect、通用 structured Mention 和 durable blob 只有在产品文档 接受需求并确定 canonical owner 后，才可能成为某条产品线的实施项。 Zeta 已经在 TUI 外部拥有： - zeta-core 中权威的 SessionCoordinator 和 ThreadController； - zeta-app-server-protocol 中唯一的 wire contract； - zeta-app-server-client 中共享的 App Server 启动、初始化、请求/事件连接与关闭层； - CLI 交付的启动配置与产品入口参数。 - zeta-theme 中与 Desktop/Native 共享的 manifest、用户主题解析和 device preference loader。 - zeta-ansi-escape 中独立的 ANSI SGR → Ratatui presentation adapter；它不拥有 PTY/terminal state。 主题边界是“部分接入”而不是“尚未复制完成”：TUI chrome 读取 accent、composer chrome、错误、 成功、警告、弱化文字和选择高亮；Theme Pane preview 额外读取有限的 syntax/diff token。选择高亮由 tui.highlightForeground 独立表达，不借用编辑器关键字色。ui/theme.rs 将透明色先合成到 terminal background，再按 TrueColor、ANSI-256、ANSI-16、Monochrome 投影；其他 Desktop/Native token 不进入 TUI API。 tui.colorTheme 缺失时跟随 workbench.colorTheme，选择属于 device-local JSON，不属于 Agent config、Session store 或 TOML。无参数 /theme 打开由 features/theme 拥有、不可搜索的固定 Zeta Code Theme Pane 以 Theme 为标题，顶部分隔线与标题、标题与第一个候选项之间各保留一行；固定选项为 Auto、Dark/Light、对应 colorblind-friendly 与 ANSI-only 模式，以及 Custom color theme。候选行编号展示，cursor 选择色和 syntax/diff preview 随候选主题变化；Enter 原子保存、 即时切换并关闭整个 Theme flow 返回主界面；保存成功后 transcript 以独立的状态圆点显示实际执行的 /theme <id ，下一行通过 └─ 结构连接符归属结果说明，两行正文保持同列对齐；保存失败时则保留当前 Pane 以显示错误。移动 cursor 时，Theme Pane 分隔线、上方 welcome banner 框线使用候选 highlight；独立 Diff preview 区域不画左右边框，只用候选 muted token 绘制上下 较高对比度的长节虚线。主题列表与 preview 间保留两行，palette 来源说明与操作提示间保留一行。preview 下方标明 GitHub、GitHub Colorblind、ANSI 16 colors 或 User-defined 配色来源。/theme <id 保留直接切换。通用 Selection Pane 的搜索是独立、 可配置的底座；启用搜索的 feature 必须先按 Space 进入 search mode，footer 明示 Space search。 所有本地 command 都可使用同一个“命令 + 结果” transcript 形式：命令本身不带箭头符号，独立的状态圆点为 Running 显示 ◉，为 Succeeded 显示 ●；结果行使用与状态位结构相连的 └─ 表达归属，正文与命令文字同列对齐并使用弱化色，之后才空行。当前没有折叠交互；待有多行 command output 时再基于这个分组添加展开/收起。 Auto 在终端 raw mode 建立后、输入事件线程启动前发出一次 OSC 11 背景色查询，并按实际 RGB 亮度选择 Light/Dark；120 ms 内没有有效响应时读取 COLORFGBG，仍无法识别才安全回退 Dark。 检测结果在当前 TUI 会话内缓存，主题面板与再次选择 Auto 不会重复查询；显式模式不受该判断影响。 因此 TUI 必须是可丢弃、可重新同步的 presentation shell，而不是第二个 Agent runtime 或 App Server facade。产品权威状态的依赖链固定为： ``text zeta-cli → zeta-tui ├─ zeta-ansi-escape → ansi-to-tui / ratatui └─ zeta-app-server-client → App Server dispatcher → zeta-core ` 本地只读能力不必统一绕行 App Server：composer 的 workspace path mention 直接调用 zeta-file-search；需要 workspace authority、跨进程一致性或 watcher revision 的目录浏览与 Git 状态通过 typed App Server filesystem/Git contract。原则不是“所有数据经过一个 facade”，而是 “每个 feature 消费事实 owner 已提供的 public typed interface”。 进程内模式只是一种 transport 优化。TUI 仍然经过 initialize、typed request/response、 dispatcher 和 notification decode，不得直接依赖 Core、Storage、Exec、Sandbox 或 Model Provider。 2. 基准采用与 Zeta 修正 长期基准中的通用结构按下表落到 Zeta： 基准概念 Zeta 中的处理 --- --- app/ 保留，但只拥有 TUI 状态、事件协调和退出流程 appserver/ 不建立；typed RPC 已由 zeta-app-server-client 和 protocol crate 拥有 thread/ 作为 features/thread/ 保留；拥有 active Thread 的可重建展示状态和交互流程 chat/ 不建立总括目录；Turn flow 归 features/thread/，composer/transcript 归 components/ ui/ 保留，只放可复用 Ratatui 原语 terminal/ 保留，只负责真实终端生命周期和能力 features/ 保留，但只添加 Zeta 已有或已接受产品契约支持的功能 platform/ 不建立泛化 facade；窄 OS adapter 放入 host/ 的明确子模块 尤其不能在 TUI 中定义第二个 ThreadController。zeta-core::ThreadController 是 Thread 执行、 持久化、顺序和恢复的 authority；TUI 的 Thread 数据只是由 snapshot 和 update 构成的 read model。 同样，TUI 内部不应出现另一个聚合 account、session、thread、turn、config 等 RPC 域的 facade。CLI 使用 zeta-app-server-client 建立并初始化 AppServerSession，再把该运行会话 的所有权交给 TUI；TUI 围绕其 cloneable request handle 与 event stream 补充交互客户端所需的 请求调度、订阅和错误映射。该 AppServerSession 是 connection/runtime owner，不是产品 Session。 3. 产品状态与 TUI 状态 权威产品模型与 sequence/cursor 语义统一见 protocol.md。TUI 只消费这些 canonical snapshot/update，不在本地重新定义产品实体。 TUI 可以在使用相应数据的 feature 中保存以下可重建状态： - 当前选中的 Session、Thread、Turn 或 Item； - 当前页面需要的 canonical Session/Thread snapshot； - 每个 aggregate 最后确认的 durable sequence； - 当前 runtime 的 transient stream cursor； - composer 草稿、光标、选择区和输入历史； - scroll、折叠、tab、overlay 和 picker 状态； - 正在发送的 typed command 及其稳定 CommandId； - connection、subscription、resync 和可展示错误状态。 TUI 不得保存或推导： - Session membership、fork lineage 或 lifecycle 的第二份权威状态机； - Thread、Turn、ThreadItem 或 Tool Call 的执行状态机； - writer lease、command receipt 或持久化恢复状态； - approval、sandbox 或 tool execution policy； - 从日志、stderr 或人类文本解析出的产品终态。 命名时必须区分三种生命周期： - ProductSession 或 protocol 中的 Session：产品任务； - AppServerConnection：RPC connection； - TerminalSession：raw mode、alternate screen 等终端资源。 禁止用无修饰的本地 SessionState 同时表达上述多个概念。 长期架构不变量 以下约束是架构评审中的默认拒绝条件，而不是可选风格。若实现确实需要偏离，必须先在本文记录 原因、影响范围、恢复策略与退出条件。 单写者 TUI semantic/presentation state 只能由主事件循环写入。后台 task、transport callback、host adapter 和 renderer 只能产生 event 或完成结果，不能直接修改共享 feature state。 单写者不表示所有工作都在主线程同步执行。RPC、文件搜索、大型 transcript projection 和其他昂贵工作应 异步执行，但其结果必须重新进入有序 event loop，并由当前 state owner 判断是否仍然有效。 状态分类 类别 示例 Owner 与约束 --- --- --- 可重建 presentation state active Thread snapshot、pending command、connection/resync、可展示错误 对应 feature 或 app/；可由 typed input 重建 局部交互状态 draft、cursor、selection、scroll、overlay 展开 明确生命周期的 component；不提升为全局 store runtime resource terminal handle、channel、task、clock、client、cache driver/client/terminal；不得进入可 replay state 产品权威状态 Thread/Turn reducer、writer lease、approval policy、durable recovery TUI 外的 canonical owner；TUI 不复制 纯渲染 view、layout 和 Ratatui renderer 只能读取 state 与只读环境信息。禁止在 draw path 中： - 发起 RPC、读取文件或写配置； - spawn task 或改变 subscription； - 推进 Turn、animation 或 paste 等语义状态； - 根据 frame 次数决定业务结果； - 查询 Git、usage 或其他可能阻塞的接口。 时间驱动变化由明确的 timer event 推进；render cache 属于 runtime resource，并使用稳定 identity、revision、width、theme/capability revision 等显式 key 失效。 显式副作用与局部协议 跨边界行为必须先成为 typed command/intent，再由 driver、feature request module 或窄 host adapter 执行，完成结果重新成为 event。顶层 AppEvent/AppCommand 只负责领域路由；具体 变体留在 feature 内，不能重新形成覆盖所有 RPC 的大枚举。 不建立高度泛型的 Feature trait、全局 Services facade 或任意闭包 callback 总线。普通 Rust struct/enum/function 和一致约定是默认方案；只有多个真实调用方证明语义相同时才提取 共享抽象。 请求关联与过期结果 任何可能乱序完成的读取或计算必须携带 generation、目标 scope 和必要的 request identity。 写操作还必须遵守 canonical CommandId 与 expected sequence 语义。旧 generation、错误 scope 或已取消请求的结果不得覆盖当前 state。 控制面、数据面与恢复 用户意图、退出、interrupt、写请求结果、committed update、错误和 subscription lifecycle 属于有序控制面；token delta、process output 和 tool progress 等高频事件进入按 aggregate 隔离的 bounded 数据面。所有 lag、overflow、cursor gap、identity mismatch 和已经实现的 connection recovery 路径必须定义 resync contract，不能只记录 warning 后继续猜测状态。 内部优先 目标边界先在 zeta-tui crate 内通过 private module 和 pub crate 验证。只有 runtime 或 component 被至少两个真实消费者使用、API 变化频率下降且抽取能减少依赖时，才评估独立 crate 或公共插件 API。窄的第三方类型适配层可以按依赖隔离提前成为产品内 crate，但必须保持单向、 无产品状态且有明确 failure semantics；zeta-ansi-escape 是当前唯一此类例外。 4. 目标目录 目标结构如下： `text zeta-code/tui/ ├── src/ │ ├── app/ │ │ ├── mod.rs │ │ ├── state.rs │ │ ├── event.rs │ │ ├── command.rs │ │ ├── eventloop.rs │ │ ├── dispatch.rs │ │ ├── bootstrap.rs │ │ └── shutdown.rs │ ├── client/ │ │ ├── mod.rs │ │ ├── eventpump.rs │ │ ├── pending.rs │ │ ├── subscription.rs │ │ ├── commandid.rs │ │ ├── notification.rs │ │ └── error.rs │ ├── features/ │ │ ├── mod.rs │ │ ├── thread/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── update.rs │ │ │ ├── command.rs │ │ │ ├── request.rs │ │ │ ├── view.rs │ │ │ └── threadtests.rs │ │ ├── sessions/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── command.rs │ │ │ ├── request.rs │ │ │ ├── view.rs │ │ │ └── sessionstests.rs │ │ ├── config/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── command.rs │ │ │ ├── request.rs │ │ │ ├── view.rs │ │ │ └── configtests.rs │ │ ├── skills/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── command.rs │ │ │ ├── request.rs │ │ │ ├── view.rs │ │ │ └── skillstests.rs │ │ ├── workspacefiles/ │ │ │ ├── mod.rs │ │ │ ├── search.rs │ │ │ ├── completion.rs │ │ │ └── workspacefilestests.rs │ │ └── statusline/ │ │ ├── mod.rs │ │ ├── model.rs │ │ ├── refresh.rs │ │ ├── layout.rs │ │ ├── view.rs │ │ └── statuslinetests.rs │ ├── components/ │ │ ├── mod.rs │ │ ├── interaction/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── viewstack.rs │ │ │ └── interactiontests.rs │ │ ├── composer/ │ │ │ ├── mod.rs │ │ │ ├── state.rs │ │ │ ├── editor.rs │ │ │ ├── attachments.rs │ │ │ ├── pendingpastes.rs │ │ │ ├── slashcommands.rs TUI-local command execution metadata only │ │ │ ├── view.rs │ │ │ └── composertests.rs │ │ ├── transcript/ │ │ │ ├── mod.rs │ │ │ ├── row.rs │ │ │ ├── layout.rs │ │ │ ├── view.rs │ │ │ └── transcripttests.rs │ │ └── selection/ │ │ ├── mod.rs │ │ ├── state.rs │ │ ├── view.rs │ │ └── selectiontests.rs │ ├── ui/ │ │ ├── mod.rs │ │ ├── layout.rs │ │ ├── scroll.rs │ │ ├── overlay.rs │ │ ├── keymap.rs │ │ ├── text.rs │ │ └── theme.rs │ ├── terminal/ │ │ ├── mod.rs │ │ ├── session.rs │ │ ├── eventstream.rs │ │ ├── framescheduler.rs │ │ ├── scrollback.rs │ │ ├── reflow.rs │ │ ├── cursor.rs │ │ └── capabilities.rs │ ├── host/ │ │ ├── mod.rs │ │ ├── clipboard.rs │ │ ├── externaleditor.rs │ │ ├── notification.rs │ │ └── ide.rs │ └── lib.rs ├── tests/ │ ├── eventloop.rs │ ├── subscriptionrecovery.rs │ └── support/ │ ├── mod.rs │ ├── fakeclient.rs │ └── terminalharness.rs ├── Cargo.toml └── README.md ` 上图只列当前已有能力和已确定的下一阶段 owner，不罗列尚无 contract 的未来 feature。每个 子目录都给出首批具体文件；实现初期不足以形成独立 owner 时继续使用单文件，不能只创建一个 空 mod.rs 或 slash-only 占位目录。文件名表达职责，不要求一次性生成整棵目录树。 5. app/：应用协调 app/ 负责： - 接收 terminal event、client result、server notification 和 background completion； - 把输入转换为语义明确的 AppCommand； - 协调 active feature、component、overlay 和请求结果； - 管理焦点、顶层模式、退出与恢复终端； - 决定何时请求重绘。 建议把可测试状态与副作用 driver 分开： `rust struct TuiApp connection: ConnectionViewState, activeview: ActiveView, thread: ThreadFeatureState, sessions: SessionsFeatureState, statusline: StatusLineModel, overlays: OverlayStack, pendingcommands: PendingCommands, struct AppDriver<C app: TuiApp, client: C, terminal: TerminalSession, ` TuiApp 处理事件并产生 command；AppDriver 执行 I/O，再把结果作为事件送回。这样 reducer 式状态测试不需要打开终端或 App Server。 app/ 可以协调其他模块，但禁止： - 实现具体 Ratatui widget； - 拼接 JSON-RPC method string 或手写 wire JSON； - 执行 Session/Thread 领域 reducer； - 直接运行 shell、模型或 Agent tool； - 成为每个 feature 私有状态的杂物桶。 bootstrap.rs 只负责 TUI 启动流程，例如打开 terminal、读取入口所需 snapshot、进入指定 Session。CLI 可以构造 start options，但 App Server composition、channel 建立、initialize、 schema gate 和 shutdown 属于 zeta-app-server-client，TUI"
  },
  {
    "slug": "zeterm/README",
    "sourcePath": "zeterm/docs/README.md",
    "title": "zeterm 产品文档",
    "description": "zeterm/docs 只记录纯 Rust Desktop 产品的用户语义、跨 crate 组合、终端兼容性、输入与发布边界。 它不替代 zeterm/README.md 或各 presentation crate README，也不把产品状态机复制进文档。",
    "group": "zeterm 产品",
    "markdown": "> 状态：Current。本文是 `zeterm` 专属系统文档的导航和边界说明；跨产品线、共享后端和通用\n> crate 契约分别由 [`product-lines.md`](../../docs/product-lines.md)、\n> [`zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md) 和各 crate README 维护。\n\n## 快速理解\n\n`zeterm/docs` 只记录纯 Rust Desktop 产品的用户语义、跨 crate 组合、终端兼容性、输入与发布边界。\n它不替代 `zeterm/README.md` 或各 presentation crate README，也不把产品状态机复制进文档。\n\n| 你要理解什么 | 先读哪份文档 | canonical owner |\n| --- | --- | --- |\n| Agent-first 产品结构与 Thread/Composer 语义 | [`native-agent-console.md`](native-agent-console.md) | `zeterm` 产品宿主 + App Server contract |\n| Terminal Surface、PTY、grid 与兼容性 | [`native-terminal-ui.md`](native-terminal-ui.md) | `zeta-terminal` + `zeterm` terminal host |\n| 键盘、IME、caret 与输入路由 | [`native-text-input.md`](native-text-input.md) | `zui` / `zeta-ui` / `zeta-winit` / `zeterm` adapter |\n| 稳定产品命令身份、请求与注册式执行 | [`native-terminal-ui.md`](native-terminal-ui.md) + [`zeta-commands`](../commands/README.md) | `ZetermCommandId` + `CommandRequest` + `CommandRegistry` |\n| UI scene 到 GPU 的依赖方向 | [`rendering-architecture.md`](rendering-architecture.md) | `zui` → `zeta-renderer` → `zeta-wgpu` |\n| 通用 icon asset 与产品 icon catalog 的边界 | [`rendering-architecture.md`](rendering-architecture.md) + [`zeta-icon`](../icon/README.md) | `zeta-icon` contract + optional `zeta-icons` catalog |\n| zeterm 产品 pane topology 与 sidebar geometry | [`zeta-layout`](../layout/README.md) | `zeta-layout` + host state adapter |\n| Composer panel 与 interaction list geometry | [`zeta-composer`](../composer/README.md) | `zeta-composer` + Native state/scene adapter |\n| 通用 UI 脱离产品宿主的最小验证 | [`zui-demo`](../zui-demo/README.md) | `zui` / `zeta-ui` / `zeta-renderer` |\n| Native UI 组件和宿主迁移边界 | [`ui-component-migration-plan.md`](ui-component-migration-plan.md) | zeterm-owned UI crates |\n| 从旧 Native workspace 的迁移状态 | [`zeterm-app-migration-plan.md`](zeterm-app-migration-plan.md) | `zeterm/` + root Cargo workspace |\n| Native 弃用与长期 owner | [`native-deprecation-plan.md`](native-deprecation-plan.md) | `zui` / `zeta-ui` / `zeterm` host boundary |\n| 构建、签名和发布输入 | [`zeterm-release-graph.md`](zeterm-release-graph.md) | root Cargo/Bazel graph + `zeterm/packaging` |\n\n## 文档边界\n\n- `zeterm/docs` 解释产品行为、跨 crate 数据流、ownership、兼容性和阶段性迁移；\n- `zeterm/README.md` 解释产品宿主当前源码路径、接入义务和 crate 组合；\n- `zeterm/<crate>/README.md` 解释单个 crate 的 public contract、关键 private symbol、测试和修改影响；\n- `docs/` 只保留跨产品、共享后端、通用 UI 规范和不属于某一产品宿主的系统文档。\n\n如果产品行为和 crate 实现发生冲突，先修正拥有该行为的实现契约，再同步本目录的系统文档；不要在\n`zeterm/src`、`zui` 或共享后端之间建立第二份权威状态。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "文档边界",
        "title": "文档边界"
      }
    ],
    "searchText": " 状态：Current。本文是 zeterm 专属系统文档的导航和边界说明；跨产品线、共享后端和通用 crate 契约分别由 product-lines.md、 zeta-rs-architecture.md 和各 crate README 维护。 快速理解 zeterm/docs 只记录纯 Rust Desktop 产品的用户语义、跨 crate 组合、终端兼容性、输入与发布边界。 它不替代 zeterm/README.md 或各 presentation crate README，也不把产品状态机复制进文档。 你要理解什么 先读哪份文档 canonical owner --- --- --- Agent-first 产品结构与 Thread/Composer 语义 native-agent-console.md zeterm 产品宿主 + App Server contract Terminal Surface、PTY、grid 与兼容性 native-terminal-ui.md zeta-terminal + zeterm terminal host 键盘、IME、caret 与输入路由 native-text-input.md zui / zeta-ui / zeta-winit / zeterm adapter 稳定产品命令身份、请求与注册式执行 native-terminal-ui.md + zeta-commands ZetermCommandId + CommandRequest + CommandRegistry UI scene 到 GPU 的依赖方向 rendering-architecture.md zui → zeta-renderer → zeta-wgpu 通用 icon asset 与产品 icon catalog 的边界 rendering-architecture.md + zeta-icon zeta-icon contract + optional zeta-icons catalog zeterm 产品 pane topology 与 sidebar geometry zeta-layout zeta-layout + host state adapter Composer panel 与 interaction list geometry zeta-composer zeta-composer + Native state/scene adapter 通用 UI 脱离产品宿主的最小验证 zui-demo zui / zeta-ui / zeta-renderer Native UI 组件和宿主迁移边界 ui-component-migration-plan.md zeterm-owned UI crates 从旧 Native workspace 的迁移状态 zeterm-app-migration-plan.md zeterm/ + root Cargo workspace Native 弃用与长期 owner native-deprecation-plan.md zui / zeta-ui / zeterm host boundary 构建、签名和发布输入 zeterm-release-graph.md root Cargo/Bazel graph + zeterm/packaging 文档边界 - zeterm/docs 解释产品行为、跨 crate 数据流、ownership、兼容性和阶段性迁移； - zeterm/README.md 解释产品宿主当前源码路径、接入义务和 crate 组合； - zeterm/<crate /README.md 解释单个 crate 的 public contract、关键 private symbol、测试和修改影响； - docs/ 只保留跨产品、共享后端、通用 UI 规范和不属于某一产品宿主的系统文档。 如果产品行为和 crate 实现发生冲突，先修正拥有该行为的实现契约，再同步本目录的系统文档；不要在 zeterm/src、zui 或共享后端之间建立第二份权威状态。"
  },
  {
    "slug": "zeterm/native-agent-console",
    "sourcePath": "zeterm/docs/native-agent-console.md",
    "title": "Native Agent Console：产品结构与执行边界",
    "description": "Native 产品的主界面是一条 Agent Thread timeline。用户消息、Agent 消息、Agent 发起的 Tool Call 和用户直接发起的 Shell command 都进入同一个 Thread；它们共享 Composer 与 presentation primitive，但保持不同的提交语义。",
    "group": "zeterm 产品",
    "markdown": "> 状态：Current。本文定义 Native Agent-first Console 的 canonical 产品结构。terminal\n> grid、BlockList 与 PTY compatibility 实现契约仍由\n> [`native-terminal-ui.md`](native-terminal-ui.md) 和\n> [`zeta-terminal` README](../../zeta-rs/terminal/README.md) 维护；Session、Thread、Turn 与\n> ThreadItem 的权威语义由 [`protocol.md`](../../docs/protocol.md) 维护。\n\n## 快速理解\n\nNative 产品的主界面是一条 Agent Thread timeline。用户消息、Agent 消息、Agent 发起的 Tool\nCall 和用户直接发起的 Shell command 都进入同一个 Thread；它们共享 Composer 与 presentation\nprimitive，但保持不同的提交语义。\n\n```text\nAgentWorkspace\n├─ ThreadTimeline\n│  ├─ UserMessage\n│  ├─ AgentMessage\n│  ├─ CommandCard\n│  ├─ ApprovalRequest\n│  └─ FileChange\n├─ AgentComposer\n│  ├─ ComposerInteractionPane → active View stack\n│  │  └─ current: SlashCommandView | ModelPickerView\n│  ├─ ComposerInfoBar → contextual hints (`/ for commands`)\n│  ├─ ComposerEditor → CodeEditor(Compact)\n│  ├─ ComposerContextToolbar\n│  └─ Agent | Shell mode\n└─ optional TerminalPane\n```\n\nTerminal 不再拥有产品 Session、Thread 或 transcript。普通 shell execution 作为 typed Tool\nCall 投影到 CommandCard；只有 `vim`、`top`、`ssh` 等需要持续直接输入和 terminal protocol\ncompatibility 的程序进入独立 TerminalPane。\n\n## 当前状态与目标\n\n| 能力 | 当前实现 | 目标 |\n| --- | --- | --- |\n| Native 产品根 | `AgentWorkspace` + `ThreadTimeline` | ✅ |\n| Composer | `AgentComposer` 显式 Agent/Shell mode | ✅ |\n| Composer 可展开交互 Pane | 展示当前 View；Slash 状态委托 `zeta-slash-commands`，Native 拥有 `/model` 子 View 和 WGPU row projection | ✅ |\n| Agent authority | App Server `session/subscribe` aggregate snapshot/update + gap resubscribe | ✅ |\n| Agent Tool Call | durable ToolCall/ToolResult → CommandCard | ✅ |\n| 用户直接 Shell | `session/request` → `StartShellTurn` → model-free durable Shell Turn | ✅ |\n| 流式 Agent text | transient Item delta + durable final item | ✅ |\n| 流式 Tool output | typed stdout/stderr delta + durable final ToolResult | 部分具备；local adapter 当前在捕获完成时发布分流 delta |\n| 交互式 terminal | 独立 `WorkspaceSurface::Terminal`，`Cmd/Ctrl+J` 切换 | ✅；当前为全主区域 Surface |\n\n`BlockList` 只保留为 terminal compatibility model，不再绘制 Agent 主界面，也不拥有 Agent\nThread transcript。\n\n`zeterm` 的 embedded App Server 在 `agent_session` 中显式选择 `SessionStateMode::Ephemeral`：\n当前进程内仍使用同一 Session/Thread/Core contract，但 Session、Thread 和用户消息不会从共享\nprofile SQLite 恢复或写入。`zeta-cli`/TUI 的默认 durable composition 不因此改变。\n\n## 所有权\n\n| 状态或能力 | Owner | Native 义务 |\n| --- | --- | --- |\n| Session、Thread、Turn 与 durable ThreadItem | Core / App Server | 订阅 snapshot/update，不复制 reducer |\n| transient Agent/Tool delta | App Server update stream | 检测 stream cursor gap；gap 后重新订阅 |\n| Timeline scroll、selection、collapse | Native presentation | 可丢弃、可从 snapshot 重建 |\n| Composer text、mode、IME 与 caret | Native `ComposerEditor` + `zeta-editor::CodeEditorDocument` | mode 明确，不猜测输入内容 |\n| Composer 面板与 interaction list geometry | [`zeta-composer`](../composer/README.md) | 只消费 logical bounds、preferred heights 与 item count，返回 panel、固定行、list viewport 和 selection scroll geometry |\n| Composer 信息栏 | Native `composer_panel` | 消费 `zeta-composer` 返回的 info-bar bounds，展示当前 mode 的提示信息 |\n| Composer 交互 Pane 宿主 | Native `composer_panel` + `ComposerInteractionPaneState` + `zeta-ui::{ScrollView,ListView}` | 只放置 active View、保留 viewport offset，并委托 `zeta-composer` 与通用 UI 基座完成 geometry、裁剪、滚动与可见范围；不解释 Slash、Model 或 Plan |\n| Composer active View model | Native `ComposerInteractionModel` | 当前实现选择 Slash Command / Model Picker View；不拥有 Pane 几何与滚动 |\n| Slash Command interaction model | `zeta-slash-commands::SlashCommandsState` | 消费初始化命令快照，拥有 query、selection、dismiss 与 completion；滚动属于各 renderer |\n| Composer Changes 文件数与增删行 | `zeta-git::GitTextDiffSnapshot` | 只过滤工作区范围并展示；点击后刷新并展开 Changes Pane |\n| Agent submission | `session/request` → `StartTurn` | 提交 UserInput 并消费 canonical result |\n| 用户直接 Shell submission | Shell Turn contract | 经过同一 policy、approval、sandbox 与 durable commit |\n| Agent-managed Tool execution | Core Tool scheduler | 只投影 ToolCall/ToolResult |\n| Terminal grid、alternate screen 与 direct input | `zeta-terminal` + TerminalPane host | 不反推 Agent/Turn 状态 |\n| Files、Changes 与 Diff presentation | Native sidebar + typed workspace services | 不从 terminal output 推断 |\n| 主题 catalog、用户 JSON 与颜色变换 | `zeta-theme` + shared manifest | Native 只把 snapshot 映射为组件 palette，不维护第二份默认色表 |\n\n## Composer 语义\n\n同一输入框拥有显式模式：\n\n```rust\nenum ComposerMode {\n    Agent,\n    Shell,\n}\n\nenum ComposerSubmission {\n    AgentMessage(String),\n    ShellCommand(String),\n}\n```\n\nAgent mode 通过 `session/request` 的 `StartTurn` operation 提交。Shell mode 通过同一 Session\nrequest 的 `StartShellTurn` operation 提交；\n不得增加 `session/request` 中 `StartTurn` 一类模糊参数，也不得根据文本是否像命令来猜 mode。\n\nComposer 使用 `CodeEditorPresentation::Compact`：保留多行文档、selection、undo/redo 和 IME，\n隐藏文件编辑器的 header、行号与 marker gutter。内容从紧凑基线自动增长，最多显示八行，超过后\n由 retained viewport 跟随 caret。`Enter` 提交当前 mode，`Shift+Enter` 插入换行；Shell mode\n在首行/末行边界使用 Up/Down 浏览已提交命令，并在返回末端时恢复尚未提交的 draft。该行为属于\n`AgentComposer`，不属于通用 `CodeEditor`。\n\n`zeta-composer::ComposerPanelLayout` 定义 Composer Panel 的固定顺序“信息栏 → 输入框 → 底部 toolbar”；\n信息栏用于显示 `/ for commands`\n等上下文提示。其上方的 Composer Interaction Pane 是可展开、可收起的 View 宿主：Panel 只为它\n分配位置，`ComposerInteractionPaneState` 只保留 offset，`zeta-ui::ScrollView` / `ListView` 只根据\nviewport 与 content geometry 完成裁剪、滚动、scrollbar 和可见范围投影。三者都不解释挂载的是\nSlash、Model、Plan 还是未来的其他 View。当前 Agent mode 输入 `/` 时，Slash Command View 使其\n在信息栏上方出现；没有 active View 时 Pane 收起。整个 Panel 增高并压缩 ThreadTimeline，不覆盖\n已有消息，也不移动固定的三行。共享 `SlashCommandsState` 按当前无空格前缀过滤并保留键盘选择与\ndismiss；Native renderer 单独保留滚动。根 View 关闭时交互区收起，子 View 关闭时返回上一层。\n`/model` 将模型选择 View 压入同一个 View 栈。方向键移动选择，Enter 进入或确认，Tab 补全 Slash\nCommand，Escape 在子 View 中返回上一层、在根 View 中关闭。列表最多显示八行，超出后由方向键或\n滚轮滚动；关闭时保留 Composer draft。模型目录来自 App Server，选中项以精确 `ModelRef` 写入\n当前 Session。\n\nShell Turn 不调用模型。`StartShellTurn` 原子记录 Turn acceptance、精确 `shell-command`\nToolCall 与 Turn start，随后复用 Tool scheduler 的 policy、workspace sandbox、one-time\napproval、unknown-outcome recovery 和 durable ToolResult；结束时直接记录 TurnCompleted。\nAgent 后续可以从 Thread context 看见这些事实。\n\n## 投影与恢复\n\nNative 首次选择 Session 时调用 `session/subscribe`。App Server 在这个 Session port 内装配\nSession snapshot、durable gap、child Thread projection 和 connection-local live update；Native\n只选择 active Thread 并应用 projection，不直接访问 Thread subscription endpoint，也不实现第二份\nThread reducer：\n\n- durable update 到达后，以 canonical snapshot/gap 推进 projection；\n- transient `ItemStarted` / `ItemDelta` 只改善低延迟展示；\n- `streamInstanceId` 改变或 sequence 出现空洞时，立即丢弃 transient buffer 并重新订阅；\n- UI 不从 Markdown、terminal output 或当前可见文字推断 Turn terminal state；\n- ToolCall 与 ToolResult 只按稳定 `toolCallId` 聚合成 CommandCard。\n\n## CommandCard 与 TerminalPane\n\nCommandCard 适用于有界、非交互式执行：\n\n```text\nShell · Agent\n$ cargo test\nCompiling...\ntest result: ok\nexit 0 · 4.2s\n```\n\n用户与 Agent 发起的命令使用相同 presentation，通过所属 Turn 的 kind 表达来源。stdout/stderr\ndelta 是 transient presentation；最终 ToolResult 是恢复 authority。\n\nTerminal Surface 适用于持续交互程序。`Cmd/Ctrl+J` 在 Agent 与 Terminal Surface 间切换；\nTerminal 激活后 keyboard/IME/paste 可直接编码到 PTY。后台 PTY 的 alternate-screen 状态不能\n抢占 Agent Surface。Terminal transcript 不自动伪装为 ThreadItem；需要进入 Agent context 的\n结果必须通过 typed Tool/attachment contract 提交。\n\n## 当前限制与下一阶段\n\n| 项目 | 当前边界 | 下一阶段 |\n| --- | --- | --- |\n| Tool output latency | stdout/stderr 类型与 UI 增量已贯通；local adapter 在进程完成后发布捕获结果 | `zeta-tool-executor` pipe reader 实时发布有界 chunk |\n| Terminal layout | 独立全主区域 Surface | 可调整 TerminalPane / 多 Pane tree |\n| Agent message | ThreadTimeline 基本文本布局 | Markdown block、selection、折叠与虚拟化 |\n| Interaction UI | durable approval 已可恢复 | Timeline 内 approval card 与响应控件 |\n| Composer interaction | Slash Command 与模型选择 View 栈已接入 | 继续增加只消费 typed catalog 的命令专属 View |\n| Session UI | 通过 App Server Session/Thread snapshot 创建、投影并切换多个 Session Tab | 独立的 Session action mutation、跨窗口恢复与每个 tab 的完整本地 presentation state |",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "当前状态与目标",
        "title": "当前状态与目标"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "composer-语义",
        "title": "Composer 语义"
      },
      {
        "depth": 2,
        "id": "投影与恢复",
        "title": "投影与恢复"
      },
      {
        "depth": 2,
        "id": "commandcard-与-terminalpane",
        "title": "CommandCard 与 TerminalPane"
      },
      {
        "depth": 2,
        "id": "当前限制与下一阶段",
        "title": "当前限制与下一阶段"
      }
    ],
    "searchText": " 状态：Current。本文定义 Native Agent-first Console 的 canonical 产品结构。terminal grid、BlockList 与 PTY compatibility 实现契约仍由 native-terminal-ui.md 和 zeta-terminal README 维护；Session、Thread、Turn 与 ThreadItem 的权威语义由 protocol.md 维护。 快速理解 Native 产品的主界面是一条 Agent Thread timeline。用户消息、Agent 消息、Agent 发起的 Tool Call 和用户直接发起的 Shell command 都进入同一个 Thread；它们共享 Composer 与 presentation primitive，但保持不同的提交语义。 ``text AgentWorkspace ├─ ThreadTimeline │ ├─ UserMessage │ ├─ AgentMessage │ ├─ CommandCard │ ├─ ApprovalRequest │ └─ FileChange ├─ AgentComposer │ ├─ ComposerInteractionPane → active View stack │ │ └─ current: SlashCommandView ModelPickerView │ ├─ ComposerInfoBar → contextual hints / for commands │ ├─ ComposerEditor → CodeEditor Compact │ ├─ ComposerContextToolbar │ └─ Agent Shell mode └─ optional TerminalPane ` Terminal 不再拥有产品 Session、Thread 或 transcript。普通 shell execution 作为 typed Tool Call 投影到 CommandCard；只有 vim、top、ssh 等需要持续直接输入和 terminal protocol compatibility 的程序进入独立 TerminalPane。 当前状态与目标 能力 当前实现 目标 --- --- --- Native 产品根 AgentWorkspace + ThreadTimeline ✅ Composer AgentComposer 显式 Agent/Shell mode ✅ Composer 可展开交互 Pane 展示当前 View；Slash 状态委托 zeta-slash-commands，Native 拥有 /model 子 View 和 WGPU row projection ✅ Agent authority App Server session/subscribe aggregate snapshot/update + gap resubscribe ✅ Agent Tool Call durable ToolCall/ToolResult → CommandCard ✅ 用户直接 Shell session/request → StartShellTurn → model-free durable Shell Turn ✅ 流式 Agent text transient Item delta + durable final item ✅ 流式 Tool output typed stdout/stderr delta + durable final ToolResult 部分具备；local adapter 当前在捕获完成时发布分流 delta 交互式 terminal 独立 WorkspaceSurface::Terminal，Cmd/Ctrl+J 切换 ✅；当前为全主区域 Surface BlockList 只保留为 terminal compatibility model，不再绘制 Agent 主界面，也不拥有 Agent Thread transcript。 zeterm 的 embedded App Server 在 agentsession 中显式选择 SessionStateMode::Ephemeral： 当前进程内仍使用同一 Session/Thread/Core contract，但 Session、Thread 和用户消息不会从共享 profile SQLite 恢复或写入。zeta-cli/TUI 的默认 durable composition 不因此改变。 所有权 状态或能力 Owner Native 义务 --- --- --- Session、Thread、Turn 与 durable ThreadItem Core / App Server 订阅 snapshot/update，不复制 reducer transient Agent/Tool delta App Server update stream 检测 stream cursor gap；gap 后重新订阅 Timeline scroll、selection、collapse Native presentation 可丢弃、可从 snapshot 重建 Composer text、mode、IME 与 caret Native ComposerEditor + zeta-editor::CodeEditorDocument mode 明确，不猜测输入内容 Composer 面板与 interaction list geometry zeta-composer 只消费 logical bounds、preferred heights 与 item count，返回 panel、固定行、list viewport 和 selection scroll geometry Composer 信息栏 Native composerpanel 消费 zeta-composer 返回的 info-bar bounds，展示当前 mode 的提示信息 Composer 交互 Pane 宿主 Native composerpanel + ComposerInteractionPaneState + zeta-ui:: ScrollView,ListView 只放置 active View、保留 viewport offset，并委托 zeta-composer 与通用 UI 基座完成 geometry、裁剪、滚动与可见范围；不解释 Slash、Model 或 Plan Composer active View model Native ComposerInteractionModel 当前实现选择 Slash Command / Model Picker View；不拥有 Pane 几何与滚动 Slash Command interaction model zeta-slash-commands::SlashCommandsState 消费初始化命令快照，拥有 query、selection、dismiss 与 completion；滚动属于各 renderer Composer Changes 文件数与增删行 zeta-git::GitTextDiffSnapshot 只过滤工作区范围并展示；点击后刷新并展开 Changes Pane Agent submission session/request → StartTurn 提交 UserInput 并消费 canonical result 用户直接 Shell submission Shell Turn contract 经过同一 policy、approval、sandbox 与 durable commit Agent-managed Tool execution Core Tool scheduler 只投影 ToolCall/ToolResult Terminal grid、alternate screen 与 direct input zeta-terminal + TerminalPane host 不反推 Agent/Turn 状态 Files、Changes 与 Diff presentation Native sidebar + typed workspace services 不从 terminal output 推断 主题 catalog、用户 JSON 与颜色变换 zeta-theme + shared manifest Native 只把 snapshot 映射为组件 palette，不维护第二份默认色表 Composer 语义 同一输入框拥有显式模式： `rust enum ComposerMode Agent, Shell, enum ComposerSubmission AgentMessage String , ShellCommand String , ` Agent mode 通过 session/request 的 StartTurn operation 提交。Shell mode 通过同一 Session request 的 StartShellTurn operation 提交； 不得增加 session/request 中 StartTurn 一类模糊参数，也不得根据文本是否像命令来猜 mode。 Composer 使用 CodeEditorPresentation::Compact：保留多行文档、selection、undo/redo 和 IME， 隐藏文件编辑器的 header、行号与 marker gutter。内容从紧凑基线自动增长，最多显示八行，超过后 由 retained viewport 跟随 caret。Enter 提交当前 mode，Shift+Enter 插入换行；Shell mode 在首行/末行边界使用 Up/Down 浏览已提交命令，并在返回末端时恢复尚未提交的 draft。该行为属于 AgentComposer，不属于通用 CodeEditor。 zeta-composer::ComposerPanelLayout 定义 Composer Panel 的固定顺序“信息栏 → 输入框 → 底部 toolbar”； 信息栏用于显示 / for commands 等上下文提示。其上方的 Composer Interaction Pane 是可展开、可收起的 View 宿主：Panel 只为它 分配位置，ComposerInteractionPaneState 只保留 offset，zeta-ui::ScrollView / ListView 只根据 viewport 与 content geometry 完成裁剪、滚动、scrollbar 和可见范围投影。三者都不解释挂载的是 Slash、Model、Plan 还是未来的其他 View。当前 Agent mode 输入 / 时，Slash Command View 使其 在信息栏上方出现；没有 active View 时 Pane 收起。整个 Panel 增高并压缩 ThreadTimeline，不覆盖 已有消息，也不移动固定的三行。共享 SlashCommandsState 按当前无空格前缀过滤并保留键盘选择与 dismiss；Native renderer 单独保留滚动。根 View 关闭时交互区收起，子 View 关闭时返回上一层。 /model 将模型选择 View 压入同一个 View 栈。方向键移动选择，Enter 进入或确认，Tab 补全 Slash Command，Escape 在子 View 中返回上一层、在根 View 中关闭。列表最多显示八行，超出后由方向键或 滚轮滚动；关闭时保留 Composer draft。模型目录来自 App Server，选中项以精确 ModelRef 写入 当前 Session。 Shell Turn 不调用模型。StartShellTurn 原子记录 Turn acceptance、精确 shell-command ToolCall 与 Turn start，随后复用 Tool scheduler 的 policy、workspace sandbox、one-time approval、unknown-outcome recovery 和 durable ToolResult；结束时直接记录 TurnCompleted。 Agent 后续可以从 Thread context 看见这些事实。 投影与恢复 Native 首次选择 Session 时调用 session/subscribe。App Server 在这个 Session port 内装配 Session snapshot、durable gap、child Thread projection 和 connection-local live update；Native 只选择 active Thread 并应用 projection，不直接访问 Thread subscription endpoint，也不实现第二份 Thread reducer： - durable update 到达后，以 canonical snapshot/gap 推进 projection； - transient ItemStarted / ItemDelta 只改善低延迟展示； - streamInstanceId 改变或 sequence 出现空洞时，立即丢弃 transient buffer 并重新订阅； - UI 不从 Markdown、terminal output 或当前可见文字推断 Turn terminal state； - ToolCall 与 ToolResult 只按稳定 toolCallId 聚合成 CommandCard。 CommandCard 与 TerminalPane CommandCard 适用于有界、非交互式执行： `text Shell · Agent $ cargo test Compiling... test result: ok exit 0 · 4.2s ` 用户与 Agent 发起的命令使用相同 presentation，通过所属 Turn 的 kind 表达来源。stdout/stderr delta 是 transient presentation；最终 ToolResult 是恢复 authority。 Terminal Surface 适用于持续交互程序。Cmd/Ctrl+J 在 Agent 与 Terminal Surface 间切换； Terminal 激活后 keyboard/IME/paste 可直接编码到 PTY。后台 PTY 的 alternate-screen 状态不能 抢占 Agent Surface。Terminal transcript 不自动伪装为 ThreadItem；需要进入 Agent context 的 结果必须通过 typed Tool/attachment contract 提交。 当前限制与下一阶段 项目 当前边界 下一阶段 --- --- --- Tool output latency stdout/stderr 类型与 UI 增量已贯通；local adapter 在进程完成后发布捕获结果 zeta-tool-executor` pipe reader 实时发布有界 chunk Terminal layout 独立全主区域 Surface 可调整 TerminalPane / 多 Pane tree Agent message ThreadTimeline 基本文本布局 Markdown block、selection、折叠与虚拟化 Interaction UI durable approval 已可恢复 Timeline 内 approval card 与响应控件 Composer interaction Slash Command 与模型选择 View 栈已接入 继续增加只消费 typed catalog 的命令专属 View Session UI 通过 App Server Session/Thread snapshot 创建、投影并切换多个 Session Tab 独立的 Session action mutation、跨窗口恢复与每个 tab 的完整本地 presentation state "
  },
  {
    "slug": "zeterm/native-terminal-ui",
    "sourcePath": "zeterm/docs/native-terminal-ui.md",
    "title": "zeterm：Terminal compatibility 结构与演进",
    "description": "zeterm 的产品根节点是一块完整的现代终端界面，不是由 Sidebar、Panel、Editor 等通用 Workbench Part 拼装出来的 IDE 外壳。Top Bar、Session 导航和附加操作都服务于终端会话；活动 终端始终占据窗口的主要内容区域。",
    "group": "zeterm 产品",
    "markdown": "> 状态：Compatibility。Agent-first 产品结构、Thread authority、统一 Composer 与 direct Shell\n> Turn 以 [`native-agent-console.md`](native-agent-console.md) 为 canonical。本文只维护独立\n> Terminal Surface、PTY、grid、screen mode、selection 与 terminal protocol 的兼容性边界；\n> 下文仍出现的 terminal-first 产品描述属于历史实现记录，不再代表当前主界面。\n\n> 本文是 `zeterm` terminal compatibility 和分阶段演进的 canonical 文档。\n> 三条公开产品线与宿主边界见 [`product-lines.md`](../../docs/product-lines.md)；本文只负责 `zeterm`\n> 的纯 Rust Desktop 终端实现。\n> 当前源码所有权、调用路径和测试入口见\n> [`zeterm` README](../README.md)；terminal grid 与 BlockList 的实现契约见\n> [`zeta-terminal` README](../../zeta-rs/terminal/README.md)；文本输入、IME 与 caret 的跨 crate\n> 所有权见 [`native-text-input.md`](native-text-input.md)；原生窗口 chrome 与控件占位的实现\n> 契约见 [`zeta-winit` README](../winit/README.md)。\n\n## 快速理解\n\n`zeterm` 的产品根节点是一块完整的现代终端界面，不是由 Sidebar、Panel、Editor 等通用\nWorkbench Part 拼装出来的 IDE 外壳。Top Bar、Session 导航和附加操作都服务于终端会话；活动\n终端始终占据窗口的主要内容区域。\n\n当前视觉采用浅色扁平界面：Top Bar、Block 输出画布与底部输入面板只用背景层级和一像素\n分隔线建立结构，不使用悬浮卡片、厚描边或大圆角。未接入真实状态的搜索、Agent、Git 和\nSession action 不以静态装饰出现。\n\n| 用户场景 | 界面行为 | 当前状态 | 深入阅读 |\n| --- | --- | --- | --- |\n| 打开 `zeterm` | 启动默认 shell；主屏上方显示 Block 输出，底部固定显示命令编辑器 | 最小 PTY/BlockList 纵切已实现 | [当前实现](#4-当前实现) |\n| 输入 shell 命令 | 键盘、IME 和 paste 先编辑底部 composer；Enter 才建立 Block 并把完整命令写入 PTY | 单行 Block Input Editor 已实现 | [当前实现](#4-当前实现) |\n| 使用 `vim`、`top` 等交互式 TUI | alternate screen 临时接管 Terminal Workspace，切回 primary 后恢复 BlockList 与底部输入 | 部分具备；scroll region、常见 query 与主流 mouse modes 已接通 | [当前实现](#4-当前实现) |\n| 浏览较早的主屏输出 | 在终端内容区滚轮上翻 Block transcript 或 cell history，新输出不抢走当前阅读位置 | 会话内有界回滚已实现；跨重启持久化尚无 | [当前实现](#4-当前实现) |\n| 复制或粘贴终端文本 | 主屏优先复制 composer selection，再复制 Block 输出 selection；macOS 使用 Cmd+C/V，Windows/Linux 的 alternate terminal 使用 Ctrl+Shift+C/V | 快捷键已通过统一 command resolver；未加 Shift 的 terminal Ctrl+C 继续透传 | [当前实现](#4-当前实现) |\n| 自定义快捷键 | 按 `Cmd/Ctrl+,` 打开设置并点击命令录制，或编辑 `<ZETA_PROFILE_ROOT>/keybindings.json` | 最多四段 Chord；设置写入采用原子替换，错误更新保留上一份有效规则并显示诊断 | [当前实现](#4-当前实现) |\n| 查看当前会话导航 | Top Bar 按钮展开垂直 Session TabList；拖动右边界可调整宽度 | App Server Session Tab 的创建、投影和切换已实现；不显示 fixture | [当前实现](#4-当前实现) |\n| 浏览工作区文件或变更 | Top Bar 按钮展开右栏；Changes / Files 切换单一 Pane，Files 可刷新 Git 状态或模糊搜索 | 根目录、上游领先/落后数和文本 MultiDiff 已接入 | [当前实现](#4-当前实现) |\n| 操作当前会话 | 右键 Session Tab，在锚点附近打开 Pin、Close、Rename、Fork 菜单 | 菜单呈现、定位、关闭和键盘交互已实现；四项 mutation 尚未执行 | [当前实现](#4-当前实现) |\n| 切换多个会话 | 在同一垂直 TabList 选择另一个 App Server Session | 已实现；每个 Session Tab 绑定并保留一个独立本地 PTY | [当前实现](#4-当前实现) |\n| 在 macOS 使用 Top Bar | 左侧 action 避开系统红绿灯占位并保留组件间距 | 70px host 占位 + 8px Titlebar 间距已实现 | [尺寸语义](#5-尺寸语义) |\n| 调整窗口尺寸 | 从同一 viewport 重算 rows/columns，并同步 resize grid 与 PTY | 已实现 | [尺寸语义](#5-尺寸语义) |\n| 拆分终端 | 只在 Terminal Workspace 内拆成多个 Pane，并调整 Pane 比例 | Grid 几何入口已完成；多 Pane runtime 尚未实现 | [Terminal Pane 分屏](#62-terminal-pane-分屏) |\n\n## 1. 产品命名\n\n命名规则只有一条：仓库内部统一属于 Zeta，只有跨过发布边界、真正交付给用户的 Native 终端\n应用使用全小写名称 `zeterm`。它来自 “Zeta Terminal”，但完整形式不作为第二个品牌。\n\n| 边界 | 规范名称 | 示例 |\n| --- | --- | --- |\n| 共享仓库、crate 和内部标识 | `zeta` / `zeta-*` | `zeta-ui`、`zeta-terminal-*` |\n| `zeterm` Cargo package、发布的终端应用及其用户可见表面 | `zeterm` | package、executable、app bundle、窗口标题、Top Bar 和输入提示 |\n\n因此不把内部 crate、CSS、协议或测试标识重命名为 `zeterm-*`。公开发布前仍需单独核查 `zeterm`\n的商标、域名、应用商店和软件包名称可用性；核查结果只影响发布层名称。\n\n## 2. 产品结构\n\n当前产品结构：\n\n```text\nzeterm\n├─ TopBar\n│  ├─ window drag region\n│  ├─ session sidebar toggle ActionBar\n│  └─ agent sidebar toggle ActionBar\n├─ SessionSidebar (collapsible)\n│  └─ SessionTabList\n│     └─ SessionTab\n│        ├─ App Server Session → active Thread\n│        └─ TerminalSession → dedicated PTY\n├─ SessionContextMenu (transient overlay)\n│  └─ Pin / Close / Rename / Fork\n├─ TerminalWorkspace\n│  └─ active TerminalSession\n│     ├─ BlockOutputViewport → BlockList\n│     └─ CommandInputEditor (fixed bottom)\n└─ AgentSidebar (collapsible)\n   ├─ Pane ActionBar: Changes / Files\n   └─ Active Pane\n      ├─ Files: toolbar + file tree / fuzzy search\n      └─ Changes: MultiDiffEditor\n         ├─ file A section → DiffEditor\n         └─ file B section → DiffEditor\n```\n\nprimary screen 的结构固定为“上方输出、底部输入”。键盘、IME 和 paste 先进入\n`CommandInputEditor`；Enter 通过 host-owned command boundary 建立 Block，再把命令写入 PTY。\nshell prompt、命令回显和行编辑不再作为 primary screen 的产品界面。输出仍由真实 PTY 产生，\nBlockList 只投影已提交命令及其 printable output，不使用静态 transcript。\n\nalternate screen 是协议兼容的明确例外：`vim`、`top` 等程序请求 alternate buffer 后，全幅 grid\n临时接管 Terminal Workspace，输入直接交给该程序；退出 alternate screen 后恢复固定底部\ncomposer。这个切换不能改变 primary screen 的 Block 输入语义。\n\nTop Bar 不是独立工作区，也不拥有终端 Session。它只提供窗口拖动、会话入口和少量全局操作。\nSession Navigation 当前使用可折叠、可通过右边界 Sash 调整宽度的垂直 TabList，但不构成\n可注册任意区域的通用 Sidebar Part。TabList 投影 App Server Session/Thread；Terminal Workspace\n呈现当前 Session Tab 对应的独立本地 PTY compatibility surface。\n\n## 3. 所有权\n\n| 能力 | 最终 owner | 职责边界 |\n| --- | --- | --- |\n| Window、Top Bar 与 Terminal Workspace 外部布局 | `zeterm/zeterm` product host | 决定窗口区域和活动会话，不进入 `zeta-ui` |\n| 单轴 Pane 尺寸约束、Sash track 与 feedback geometry | `zui::SplitViewLayout` / `zeta-ui::Sash` | 不持有产品显隐、preferred width、pointer capture 或持久化 |\n| 递归 Pane geometry 与 owning-split Sash 路由 | `zui::GridLayout` | 递归组合 SplitView；不持有 Terminal Session、Agent content、Pane Tree mutation 或 active Pane |\n| Terminal Pane Tree、Pane-to-Session binding 与 Pane 状态 | 后续 native Terminal Workspace model | 已确认是终端分屏必需边界；当前尚未完成。当前 Session Tab 到 Terminal PTY 的一对一 binding 已完成 |\n| Session Navigation 显隐、preferred width 与 resize gesture | `zeterm/src/session_sidebar` | 使用通用 Split/Sash geometry；不拥有 Session lifecycle |\n| Agent Sidebar 显隐、preferred width 与 resize gesture | `zeterm/src/agent_sidebar` | 使用通用 Split/Sash geometry，宽度限制为 240–560px，并为 main Pane 保留至少 240px；不拥有内部 Pane、文件或 diff model |\n| Agent Sidebar 内部 Pane composition | [`zeta-agent-sidebar`](../agent-sidebar/README.md) 的 `AgentSidebar` / `AgentSidebarNavigation` | 只组合跨功能切换；Files/SCM 功能布局分别由各自子模块拥有，zeterm 仅提供 shell slot |\n| Files 树、模糊搜索与领先/落后显示 | [`zeta-agent-sidebar`](../agent-sidebar/README.md) / `zeta-file-search` / `zeta-git` | `zeta-agent-sidebar::files::FilesState` 保存可丢弃 UI 状态；zeterm 适配目录 DTO 并执行动作，Git 命令解析和模糊匹配器仍由各自 crate 拥有 |\n| 多文件差异内容与视口 binding | `zeta-agent-sidebar::EditorPane` / `zeta-editor::MultiDiffEditor` | Agent Sidebar 保存 changed-file collection 与每文件 `DiffEditorState`；Native 只提供 SCM 投影 |\n| 通用 UI 滚动 geometry、交互映射与状态 transition | `zeta-ui::ScrollView` / `ScrollState` / `ScrollbarController` | MultiDiff 复用完整 logical-pixel 状态和交互映射；BlockOutputViewport 通过 Native adapter 复用 clip、内容坐标和 scrollbar paint；Terminal 仍保留底部相对行锚定与输出增长策略 |\n| Top Bar 内部 action 排列 | `zeta-ui::ActionBar` | 后续有真实 action 时使用；只拥有 representation geometry 和 paint |\n| 通用 Tab surface 与横/纵排列 | `zeta-ui::Tab` / `TabList` | 只拥有 presentation state、item size/gap、surface paint 和同源 bounds；不拥有 product content 或 tabpanel |\n| Session Tabs 与活动 Session presentation | Native session navigation control | 消费权威 Session projection，不复制 Session lifecycle |\n| 锚点浮层定位、viewport 约束与 layer 合成 | `zeta-ui::ContextView` | 不拥有显示生命周期、输入路由或产品 action |\n| 无边框下拉 surface、可选 header、纵向 item geometry 与默认选择 | `zeta-ui::Dropdown` | 组合 ContextView/ActionBar；不拥有产品查询、选择 identity、关闭或 command |\n| 柔和阴影、2px menu padding、4px radius、纵向 item geometry 与默认选择 | `zeta-ui::ContextMenu` | 组合 ContextView/ActionBar；不拥有 Session identity、关闭或 command |\n| Session Tab 右键菜单生命周期与 command identity | `zeterm/src/session_context_menu` | 保存目标、锚点与恢复焦点；菜单关闭后不保留第二份 Session 状态 |\n| Product command identity 与注册式执行 | [`zeta-commands`](../commands/README.md) / `zeterm/src/command_dispatch` | pointer、menu 和 shortcut 只提供入口，业务行为汇合到同一 `CommandRequest`，再由宿主注册的 handler 执行 |\n| 平台无关按键、规则顺序与冲突解析 | [`zeta-keybinding`](../keybinding/README.md) | 不读取 winit event、focus、terminal state 或用户配置，不执行产品 command |\n| winit 按键转换、Native context 与 Chord 生命周期 | `zeterm/src/keybindings` | 内建 Copy/Paste；1.5 秒超时，失焦或 IME 取消；保持 alternate terminal Control 序列透传 |\n| Native 用户快捷键资源 | `zeterm/src/keybindings_resource` | 读取 `<ZETA_PROFILE_ROOT>/keybindings.json`；完整校验成功才替换，坏更新保留上一份规则 |\n| 快捷键模型、设置、录制和提示 | [`zeta-keybinding`](../keybinding/README.md) | 同一 feature crate 拥有规则解析、浮层 lifecycle、录制 deadline、诊断呈现和组件样式；zeterm 提供产品 command、事件 adapter 与保存接线 |\n| Terminal Session product state | App Server/terminal session runtime | 拥有进程、cwd、环境、输出与退出状态 |\n| Terminal grid、screen/mode state、基础 escape sequence 与 BlockList | `zeta-terminal::TerminalCore` | 不由 `UiScene` 或 `InputBox` 推断 |\n| PTY process、write、resize 与 exit | `zeterm/src/terminal_session` + `zeta-utils-pty` | process mechanism 与 terminal model 分离；创建在后台 worker 完成 |\n| Session Tab 到本地 PTY 的一对一 binding、活动/非活动 runtime 切换 | `zeterm/src/terminal_workspace` | Native adapter 管理 pending key、ready 顺序和非活动 runtime；不拥有 App Server Session/Thread authority |\n| cell scrollback retention | `zeta-terminal::TerminalGrid` | 会话内最多保留 10,000 行；不负责跨重启持久化 |\n| scroll position | `zeterm/src/terminal_scrollback` | 可丢弃的 presentation state，不写回 terminal model |\n| terminal output selection | `zeterm/src/terminal_selection` | 可丢弃的 viewport state；文本来自 terminal/Block projection |\n| 跨重启历史持久化与完整 terminal compatibility | 后续 terminal runtime | 尚未完成 |\n| BlockList / TerminalOutput presentation | Native terminal session view | 呈现 runtime output；不能成为第二份权威输出存储 |\n| Primary Block Input Editor 与 IME candidate area | `zeterm/src/terminal_composer` + `input_method` | 编辑 host-owned `TextInput`；Enter 才提交真实 command boundary |\n| 命中、指针状态、focus、键盘导航与 accessibility semantics | `zui` | 只分发稳定控件身份和 activation intent，不保存 Session、文件、对话或文档状态 |\n| 平台 accessibility publication | 后续 `zeta-winit` adapter | 当前尚未接 AccessKit/平台 API，内部语义树不等于屏幕阅读器已可用 |\n| alternate-screen direct input | `zeterm/src/terminal_input` + `input_method` + `TerminalCore` | 仅在 TUI 接管期间编码 key/IME/paste 并写入 PTY |\n| shell command completion boundary | `zeterm/src/terminal_session` bootstrap + `zeta-terminal::TerminalCore` | 当前 zsh 使用 OSC 133 `D`；其他 shell 只有基础 prompt/echo suppression |\n| Rect、icon、text scene 与 GPU draw | `zui` / `zeta-wgpu` | 不拥有 Session、PTY、窗口布局或产品 reducer |\n\n`zeterm/zeterm` 可以保存活动 Tab、hover、focus、scroll position 等可丢弃 presentation state，但\nSession、Thread、Turn、PTY process 和 durable output 必须来自对应 runtime。\n\n## 4. 当前实现\n\n| 当前实现 | 当前事实 | 目标映射 |\n| --- | --- | --- |\n| `titlebar::Titlebar` | 绘制 32px 窗口顶区、拖拽区和左右 sidebar toggle `ActionBar`；不显示标题文案 | Top Bar |\n| `zeta-winit::WindowControlInsets` | 按 native chrome policy 提供覆盖产品内容的左右逻辑占位；macOS full-size titlebar 当前为左侧 70px | 原生窗口控件安全区 |\n| `session_tab_list::SessionTabList` | 组合 `zeta-ui::TabList` 的无边框 4px 圆角 surface；自身绘制与两行信息块等高的白色状态容器及会话名/工作区截断文字，并注册 Session Tab 语义 | 通用 TabList 已支持 6px 间隔的多项布局；App Server Session projection/switching 已接入，每个 Session Tab 绑定独立 Terminal PTY |\n| `session_context_menu::SessionContextMenu` | 右键当前真实 Session Tab 后，用通用 `ContextMenu` 基座绘制 Pin、Close、Rename、Fork；基座提供 renderer 柔和阴影、2px padding 与 4px radius，默认选择 Pin；菜单子树打开时成为 modal interaction scope，hover 同步 roving focus 并在移出后保留最后一项，同时支持菜单外点击、Escape、上下键、Tab、Enter/Space 与焦点恢复 | 下层控件在菜单打开期间不接收 pointer、focus 或 activation；四项已映射为稳定 product action，单 Session runtime 尚不执行真实 pin/close/rename/fork transition |\n| `ShellLayout` | 组合扁平 titlebar、可选 Sessions sidebar，并把剩余区域交给 `TerminalWorkspaceLayout` | primary screen 窗口外层布局 |\n| `zeta-layout::TerminalWorkspaceLayout` / `zui::GridLayout` | 把活动 Terminal 与可选 Agent Sidebar 投影为递归 Grid Leaf；alternate screen 使用完整活动 Terminal Leaf | Agent Sidebar 已接入；多 Terminal Pane runtime 尚未完成 |\n| `SessionSidebarState` / `Sash` | 保存 preferred width 和 drag-start snapshot；从同一 track 生成 8px 命中区与 2px hover/active feedback | 侧栏宽度限制为 160–480px，并始终为 main Pane 保留至少 240px |\n| `AgentSidebarState` / `Sash` | 保存右栏显隐、preferred width 与 drag-start snapshot；从同一 track 生成 8px 命中区和 2px hover/active feedback | 宽度限制为 240–560px，并始终为 main Pane 保留至少 240px；内部内容由 `zeta-agent-sidebar::AgentSidebar` 拥有 |\n| `zeta-agent-sidebar::AgentSidebarNavigation` | 跨功能 Changes/Files ActionBar 与导航语义 | 不拥有 Files/SCM 功能布局 |\n| `zeta-agent-sidebar::files::FilesLayout` / `FilesToolbar` / `FilesPane` / `zeta-file-search` | Files 自己拥有 36px 功能 toolbar、根目录文件树与工作区路径模糊匹配结果 | Search 输入已接键盘、剪贴板和 IME；目录展开、滚动和文件打开动作已由 Native adapter 接线 |\n| `zeta-agent-sidebar::scm::ScmLayout` / `EditorPaneState` / `zeta-editor::MultiDiffEditor` | SCM 自己拥有 Changes toolbar slot 与整体纵向视口；Native 将 `zeta-git` snapshot 映射为 `ScmDiff` | 启动、Refresh 与 command completion 更新；binary、非 UTF-8 或单侧超过 2 MiB 的文件跳过 |\n| `zeta-commands::{ZetermCommandId, CommandRequest, CommandRegistry}` / `keybindings::NativeKeybindings` / `keybindings_resource::KeybindingsResource` / `keyboard_shortcuts` | pointer/menu 与标准化键盘事件汇合到同一 command request，再由宿主注册的 handler 执行；resolver 支持 `when`、Builtin/User precedence、blocker 和最多四段 Chord；资源轮询外部编辑，设置录制采用原子写入 | ✅；内建 Copy/Paste、1.5 秒 Chord timeout、失焦/IME 取消、冲突诊断、Chord 提示与设置 UI 已实现 |\n| `TerminalCore` / `TerminalGrid` | 增量解析 ANSI，维护 cell、cursor、wrap、erase 与基础 SGR | 当前最小 terminal emulator core |\n| Unicode terminal text | CJK 按双 cell 保存；组合符、ZWJ Emoji 与 flag 序列保留在 leading cell；renderer 使用系统 outline fallback | macOS 已规避不可栅格化的 `GB18030 Bitmap`；复杂 BiDi 行级布局尚未完成 |\n| primary/alternate screen | 解析 `47/1047/1048/1049`，切换 active grid，并在 resize 时同步两块 grid | 已实现基础 buffer lifecycle |\n| DEC mode 与 terminal input | 记录 cursor key、cursor visibility、bracketed paste 和 mouse request state；编码基础 key | 常见 input mode 已形成闭环 |\n| scroll region 与 terminal query | 支持 margin scrolling、origin-relative cursor、line insert/delete，并把 DA/DSR/CPR reply 写回 PTY | 常见纵切已实现，尚非完整 query family |\n| terminal mouse report | alternate screen 内把 pointer cell、button/motion/wheel 和 modifiers 编码为 1000/1002/1003 legacy 或 1006 SGR report | 不接管 titlebar；1005/1015 尚未实现 |\n| 主屏 scrollback | full-screen scroll 把 cell rows 保留到 10,000 行有界历史；局部 scroll region 和 alternate screen 不进入历史 | `CSI 3 J` 清理历史；当前没有磁盘持久化 |\n| Native 回滚浏览 | 主屏滚轮浏览 Block transcript/cell history，并在阅读旧输出时保持新输出锚定；`TerminalOutputScrollView` 把行窗口映射到通用 `ScrollView` | alternate screen 的应用鼠标报告优先于产品滚动；Terminal 行锚定不下沉到 UI 基座 |\n| resize reflow | 主屏按 soft-wrap metadata 重排 history/live rows，并映射 cursor、pending wrap 与 wide cells | alternate screen 和自定义 scroll region 保持 fixed-grid resize |\n| selection / clipboard | 主屏复制 composer 或 Block 输出 selection，paste 编辑 composer；alternate screen 按 terminal mode 写入 PTY | 尚无双击词、三击行和 selection auto-scroll |\n| OSC title | 解析 OSC 0/2 并同步 native window 与 Session Tab | 标题去 control characters，限制为 256 字符 |\n| `BlockList` | host submit 建立 Block，保存有界 printable output，过滤 PTY echo，并在 OSC 133 `D` 上完成当前 Block | primary screen 的权威 output projection |\n| `terminal_session::TerminalSession` | 启动默认 shell、抑制原生 prompt/echo、提交整条命令、转发 PTY output/exit、处理 resize | zsh 已有最小 completion hook；其他 POSIX shell 只有基础 bootstrap |\n| `TerminalComposer` / `terminal_input` | primary screen 编辑 `TextInput` 并在 Enter 时提交 | 当前为单行 |\n| `input_method` | 根据 window、screen 与 focus 选择 Disabled/Composer/TerminalGrid，转换 IME 事件并同步 candidate area | preedit 状态由共享 `TextInput` 模型维护 |\n| 输入上下文工具栏 | Bottom Widget 最底部用 `ActionBar` 排列四个带图标标签的 `Button`：Local、当前工作区目录、Git branch 与 diff count | 目录按钮复用带 header slot 的 `Dropdown`，分支按钮复用带同类 header 的 `ContextMenu`；两者第一行均默认聚焦 Search Box，并分别实时过滤当前层级子目录与本地分支。成功后替换 Files 根、搜索索引和 Git/Changes 投影；环境选择器尚未绑定 |\n| 统一 UI 分发 | `zui` 的 `ElementId`、父子 `UiNode`、反向 hit-test、focus order、同组导航、`UiIntent` 与每帧 accessibility snapshot | 当前 Titlebar、Session TabList、Session Menu/MenuItem、Sash separator、terminal output、composer、toolbar 和 Button 已接入；平台 accessibility adapter 尚无 |\n| primary/alternate Native presentation | primary 绘制 BlockList + 固定底部 composer；alternate 绘制全幅 active grid/cursor | Warp 式主屏与 TUI compatibility 已分流 |\n| `ActionBar` / `Button` | presentation-only action 与 icon button | 保持通用 primitive，不接收 terminal domain state |\n| `TabList` / `Tab` | presentation-only Tab 排列与 surface | 当前用于 Session navigation；changed-file diff 不再使用 Tab |\n| 完整 DEC/query/mouse family、跨重启历史持久化 | 尚未实现 | 后续 terminal compatibility / Session durability 纵切 |\n| session restoration、split panes | 尚未实现 | 后续产品能力；Terminal Tabs 已通过 Session Tab + 独立 PTY 实现 |\n\n### 4.1 用户快捷键资源\n\n`<ZETA_PROFILE_ROOT>/keybindings.json` 是严格 JSON 数组。未设置 `ZETA_PROFILE_ROOT` 时，Native 与\nApp Server 使用同一个操作系统用户 profile state 根；切换工作区不会切换用户快捷键或\nConfig/Session/Thread authority。每条规则必须包含 `key` 和 `command`；\n`command: null` 表示 blocker。`mac`、`linux`、`win` 可以用平台专属按键覆盖 `key`，设为\n`null` 表示该平台禁用该条规则。\n\n```json\n[\n  {\n    \"key\": \"primary+k primary+b\",\n    \"command\": \"workbench.action.toggleSideBar\",\n    \"when\": \"textInputFocus\",\n    \"mac\": \"primary+k primary+b\",\n    \"linux\": \"ctrl+k ctrl+b\",\n    \"win\": \"ctrl+k ctrl+b\"\n  },\n  {\n    \"key\": \"ctrl+v\",\n    \"command\": null,\n    \"when\": \"terminalFocus\"\n  }\n]\n```\n\n| 字段或语法 | 当前支持 | 失败语义 |\n| --- | --- | --- |\n| modifier | `primary`、`ctrl`、`meta`、`alt`、`shift` | 重复 modifier，或 `primary` 与显式 `ctrl`/`meta` 组合时拒绝整份资源 |\n| key identity | 逻辑键名或 `[PhysicalCode]` | 空键、单个 Chord 多键、超过四段时拒绝整份资源 |\n| `when` 语法 | 省略表示 always；支持 `!`、`&&`、`||`、括号、`==`、`!=`、布尔值与字符串值 | 语法错误或未知 context key 时拒绝整份资源 |\n| context key | `textInputFocus`、`terminalFocus`、`agentSurfaceVisible`、`terminalSurfaceVisible`、`sessionSidebarVisible`、`agentSidebarVisible`、`fileSearchVisible`、`composerMode` | 布尔 key 参与真假组合；`composerMode` 可与字符串比较 |\n| command | Copy/Paste、composer mode、左右 sidebar、Changes/Files、Refresh、File Search 与打开快捷键设置的当前稳定 command ID | 未知或尚未执行真实 transition 的 command ID 拒绝整份资源 |\n| 更新 | 每秒比较资源内容，完整编译后替换 Builtin + User rule set | 文件过大、读取失败、JSON 错误、未知字段或任一规则无效时继续使用上一份完整规则 |\n\n设置页使用深灰色 keycap 分别呈现 modifier 和按键字符；同一 Chord 紧密排列，多段 Chord 使用\n更大间距。点击命令进入录制，暂停一秒后保存；Escape 取消录制或关闭浮层，窗口失焦取消录制。\n用户规则优先于内建规则；同来源、同 priority 的冲突由资源中靠后的规则获胜，并在设置页显示\n诊断。等待第二段按键时，底部提示已经输入的 keycap；错误按键会消费并退出 Chord，1.5 秒\n超时、窗口失焦或 IME 事件也会退出。\n\n当前可绑定的 command ID 为：\n\n- `editor.action.clipboardCopyAction`、`editor.action.clipboardPasteAction`；\n- `workbench.action.toggleComposerMode`、`workbench.action.toggleSideBar`、\n  `workbench.action.toggleAuxiliaryBar`；\n- `workbench.action.showAgentChanges`、`workbench.action.showAgentFiles`；\n- `workbench.action.refreshAgentFiles`、`workbench.action.toggleAgentFileSearch`；\n- `workbench.action.openKeyboardShortcuts`。\n\n当前“terminal core 纵切完成”指以下端到端路径已经同时成立：\n\n- 当前 Session Tab 的默认 shell PTY 的 command submit、output、reply、resize 和 exit 都经过同一\n  `TerminalSession`，非活动 Tab 的 PTY 事件按 terminal key 路由到自己的 `TerminalCore`；\n- primary grid、alternate grid、BlockList、scrollback、reflow 与 command echo filtering 各有单一\n  权威 owner；\n- primary composer 与 alternate direct input、IME、应用鼠标、产品滚轮、selection 和\n  clipboard 按当前 screen/mode 正确分流；\n- 当前 state 能直接生成 scene、cursor、title 和可复制文本，不从 renderer 反推 terminal state。\n- 简中、日文、韩文、组合音标、阿拉伯文字形与 Emoji 已覆盖 shaping/raster regression；terminal\n  model 另覆盖 CJK cell width 和 extended grapheme ownership。\n- 当前交互节点从同一份 bounds 生成 paint state、hit-test、cursor、focus navigation 与\n  accessibility semantics；不存在另一份按坐标猜测控件身份的 hover 表。\n\nSession Tab 到 PTY 的创建不在 native event loop 内同步等待：`TerminalWorkspace` 先分配\n`TerminalSessionKey` 并记录 pending binding，`TerminalSession::spawn_async` 在后台 worker\n完成 shell/PTY 启动后发送 `NativeEvent::TerminalReady`；UI 线程只在 ready event 到达时接管\n已创建的 `TerminalSession`。ready 之前到达的 output/exit event 按 key 暂存，避免 PTY 启动和\n事件投递的竞态把首批输出丢掉。切换到仍在创建中的 Session 会立即返回，完成顺序不决定最终\nactive tab，最终选择由最近一次 activation request 决定。\n\n这一定义不把“完整 xterm compatibility”、跨重启 Session durability 或 split\npanes 伪装成本阶段能力；它们仍是表中单独列出的后续纵切。\n\n当前 Top Bar 会显示左右两个 sidebar toggle。左侧展开后投影 App Server Session/Thread 列表，\n新增 Session 通过 worker 创建并在 snapshot 到达后加入同一垂直 TabList；切换 tab 重新订阅对应\nThread。右侧展开后包含 Changes / Files ActionBar、Files-only toolbar 与单一 active pane。\nFiles 默认投影工作区\n根目录，Search 使用模糊路径索引；Changes 把当前 Git 状态快照的全部文本变更作为\n`MultiDiffEditorItem` 放入同一个滚动文档，每项保留独立的 DiffEditor viewport。没有文件或\n变更时仍显示真实空态，不用 fixture 冒充 file tree、plan 或 diff。\n当前 Tab 可通过右键打开真实浮层菜单；它只形成 Pin、Close、Rename、Fork 的 presentation 和\ncommand identity。相应 mutation 仍必须通过 App Server 的权威 Session contract 执行。\n\n## 5. 尺寸语义\n\n窗口 resize 的长期执行顺序是：\n\n1. `NativeApp` 接收 physical extent 与 scale factor；\n2. `NativeApp` 从 `NativeWindow` 读取窗口控件左右占位，product layout 计算 logical Top Bar、\n   可选 Session Sidebar 与剩余 Workspace；`SplitViewLayout` 约束外部 effective width，\n   `GridLayout` 把剩余区域投影为活动 Terminal 与可选 Agent Sidebar Leaf，Titlebar action\n   在占位外另加 8px 组件间距；\n3. primary screen 用 output viewport 计算 rows/columns，固定底部 composer 不计入输出行数；\n   alternate screen 用完整 Terminal Workspace 计算 rows/columns；\n4. `TerminalWorkspace::resize_all` 让每个绑定的 `TerminalSession::resize` 更新\n   primary/alternate grid，并把 active screen 的相同 rows/columns 发送给对应 PTY；\n5. host 从同一份 terminal state 构造下一帧 scene。\n\nSession Sidebar 默认宽度为 200 logical pixels，可在 160–480px 范围内调整，并始终为\nTerminal Workspace 保留至少 240px。`SessionSidebarState` 保存 preferred width；窗口临时变窄\n只改变当前 effective width，收起、重新展开或恢复窗口尺寸后仍使用用户首选值。Sash 拖动触发\n同一条 Shell bounds → terminal grid → PTY resize 链路，pointer cell mapping、output 与\ncomposer 共享调整后的 workspace。当前不增加 `PanelHeight`、任意区域拖拽或通用 Workbench\nPart 系统。\n\nAgent Sidebar 默认宽度为 320 logical pixels，可在 240–560px 范围内调整；剩余区域不足\n480px 时，即使显隐状态为展开也会临时隐藏。右侧 Sash 拖动触发同一条 Shell bounds →\nterminal grid → PTY resize 链路，不能绕过这条 Terminal Workspace 最小宽度约束。\n\n窗口控件占位由 `zeta-winit` 的 chrome adapter 统一拥有，不属于通用 `ActionBar` 样式。\nmacOS 当前使用集中且受测试的 70 logical pixel policy；由于 `winit` 尚无安全的 system button\ngeometry API，RTL 换边和未来 Windows controls overlay 仍是 adapter 扩展点，不能描述为当前\n能力，也不能在 `titlebar::Titlebar` 再引入平台常量。实现契约见\n[`zeta-winit/README.md`](../winit/README.md)。\n\n当前 Session Tab 的白色状态圆形只投影 native runtime 可确认的通用 `Active` 语义，尚未绘制\n状态 SVG。Planning、Thinking、Editing 等 Agent 阶段尚无权威 Session projection，不能由 UI\n从 terminal 输出推断；后续应由 App Server 提供 typed 状态，再把对应 SVG 映射到同一圆形容器。\n\n## 6. 分阶段演进\n\n### 6.1 近期结构整理\n\n- 把当前 zsh 最小 bootstrap 演进为可协商版本的 shell integration，可靠产生 command\n  start/end、cwd、exit status，并覆盖更多 shell；\n- 把当前单行 composer 演进为支持换行、历史、补全和建议的 Block Input Editor；\n- shell integration 提供 cwd 更新后，协调 input context toolbar 选中的工作区与 shell 内的\n  `cd`；当前目录标签表示用户选择的 Files/Git 工作区，不能据此推断 PTY 内部 cwd；\n- 为 Session action menu 接入 App Server 的 pin/close/rename/fork mutation，并为每个 tab 保留\n  独立的完整本地 presentation state；\n- file tree、tabs、chat 和 editor 接入时复用 `zui`：各组件只注册稳定 identity、\n  父子关系、语义和 intent，业务模型仍由各自 owner 保存；\n- 接入 AccessKit 或平台原生 accessibility adapter，直接发布现有语义树与 focus identity；\n- 让 root layout 继续只决定 Top Bar、可选 Session Navigation 和 Terminal Workspace 的外部\n  bounds，并把单轴约束委托给 `SplitViewLayout`；\n- 保持 `zeta-ui` presentation-only，不把 Session 或 terminal reducer 下沉到组件层。\n\n### 6.2 Terminal Pane 分屏\n\nTerminal Workspace 已确认需要 Pane 分屏。当前先完成通用 `GridLayout` 递归几何，并让现有\n活动终端作为单 Leaf 接入；下一阶段由 native Terminal Workspace 增加产品 Pane Tree：\n\n```text\nTerminalWorkspace\n└─ PaneNode\n   ├─ Leaf(TerminalSessionId)\n   └─ Split {\n        axis,\n        ratio,\n        first,\n        second\n      }\n```\n\nTerminal Pane Splitter 复用当前 `GridLayout`、`SplitViewLayout` 与 `Sash` geometry，但\nPane Tree、Pane-to-Session binding、active Pane、ratio、逐 Pane scroll/selection/composer\n和 Terminal Session lifecycle 仍只属于 Terminal Workspace。通用 presentation primitive\n不等于可以注册任意产品区域的 Workbench Part/Sash 系统。\n\n## 7. 明确不做什么\n\n- 不构建 VS Code 风格的通用 Workbench Part、Panel、Auxiliary Bar 或区域注册系统；\n- 不让每个视觉区域都具有可拖拽尺寸；\n- 不把当前 immutable `GridLayout` 扩张成持有产品状态的 retained Workbench Grid，也不增加\n  动态 Part 注册或任意区域 resize；\n- 不用静态 session fixture 冒充真实 Block 输出；composer 必须提交到真实 PTY；\n- 不把 terminal grid、PTY、scrollback 或 Session 生命周期放入 renderer；\n- 不把当前静态 transcript/message fixture 描述成真实 terminal output；\n- 不把当前基础 ANSI subset 描述成完整 xterm/VT compatibility；\n- 不因 alternate screen 和基础 direct input 已接通，就声称完整支持 `vim`、`top` 等交互式 TUI；\n- 不把当前 alternate-screen 1000/1002/1003/1006 支持描述成所有 screen/mouse protocol 均已完成；\n- 不因参考现代终端产品而复制某个外部产品的内部模型或视觉细节。\n\n## 8. 长期不变量\n\n- 终端会话是窗口主体，chrome 和导航服务于终端而不是与终端平级；\n- primary screen 始终由上方 BlockOutputViewport 与固定底部 CommandInputEditor 组成；\n- product host 决定活动 Session、布局和事件路由，`zeta-ui` 只消费 presentation state；\n- pointer、menu、shortcut 与后续 command palette 必须执行同一个 product command identity；\n- hit-test、hover/press/capture、focus、键盘导航、cursor 和 accessibility semantics 必须共享\n  同一个 `ElementId`，不能由各组件建立彼此不一致的状态表；\n- terminal viewport、grid rows/columns 和 PTY size 必须来自同一条尺寸链路；\n- Session Navigation 不拥有 Session lifecycle 或 durable output；\n- Terminal Pane Tree 只属于 Terminal Workspace 内部；通用 Grid/Split/Sash geometry 不拥有\n  该产品拓扑；\n- 当前实现、计划迁移和潜在能力必须在文档中保持明确分离。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-产品命名",
        "title": "1. 产品命名"
      },
      {
        "depth": 2,
        "id": "2-产品结构",
        "title": "2. 产品结构"
      },
      {
        "depth": 2,
        "id": "3-所有权",
        "title": "3. 所有权"
      },
      {
        "depth": 2,
        "id": "4-当前实现",
        "title": "4. 当前实现"
      },
      {
        "depth": 3,
        "id": "41-用户快捷键资源",
        "title": "4.1 用户快捷键资源"
      },
      {
        "depth": 2,
        "id": "5-尺寸语义",
        "title": "5. 尺寸语义"
      },
      {
        "depth": 2,
        "id": "6-分阶段演进",
        "title": "6. 分阶段演进"
      },
      {
        "depth": 3,
        "id": "61-近期结构整理",
        "title": "6.1 近期结构整理"
      },
      {
        "depth": 3,
        "id": "62-terminal-pane-分屏",
        "title": "6.2 Terminal Pane 分屏"
      },
      {
        "depth": 2,
        "id": "7-明确不做什么",
        "title": "7. 明确不做什么"
      },
      {
        "depth": 2,
        "id": "8-长期不变量",
        "title": "8. 长期不变量"
      }
    ],
    "searchText": " 状态：Compatibility。Agent-first 产品结构、Thread authority、统一 Composer 与 direct Shell Turn 以 native-agent-console.md 为 canonical。本文只维护独立 Terminal Surface、PTY、grid、screen mode、selection 与 terminal protocol 的兼容性边界； 下文仍出现的 terminal-first 产品描述属于历史实现记录，不再代表当前主界面。 本文是 zeterm terminal compatibility 和分阶段演进的 canonical 文档。 三条公开产品线与宿主边界见 product-lines.md；本文只负责 zeterm 的纯 Rust Desktop 终端实现。 当前源码所有权、调用路径和测试入口见 zeterm README；terminal grid 与 BlockList 的实现契约见 zeta-terminal README；文本输入、IME 与 caret 的跨 crate 所有权见 native-text-input.md；原生窗口 chrome 与控件占位的实现 契约见 zeta-winit README。 快速理解 zeterm 的产品根节点是一块完整的现代终端界面，不是由 Sidebar、Panel、Editor 等通用 Workbench Part 拼装出来的 IDE 外壳。Top Bar、Session 导航和附加操作都服务于终端会话；活动 终端始终占据窗口的主要内容区域。 当前视觉采用浅色扁平界面：Top Bar、Block 输出画布与底部输入面板只用背景层级和一像素 分隔线建立结构，不使用悬浮卡片、厚描边或大圆角。未接入真实状态的搜索、Agent、Git 和 Session action 不以静态装饰出现。 用户场景 界面行为 当前状态 深入阅读 --- --- --- --- 打开 zeterm 启动默认 shell；主屏上方显示 Block 输出，底部固定显示命令编辑器 最小 PTY/BlockList 纵切已实现 当前实现 输入 shell 命令 键盘、IME 和 paste 先编辑底部 composer；Enter 才建立 Block 并把完整命令写入 PTY 单行 Block Input Editor 已实现 当前实现 使用 vim、top 等交互式 TUI alternate screen 临时接管 Terminal Workspace，切回 primary 后恢复 BlockList 与底部输入 部分具备；scroll region、常见 query 与主流 mouse modes 已接通 当前实现 浏览较早的主屏输出 在终端内容区滚轮上翻 Block transcript 或 cell history，新输出不抢走当前阅读位置 会话内有界回滚已实现；跨重启持久化尚无 当前实现 复制或粘贴终端文本 主屏优先复制 composer selection，再复制 Block 输出 selection；macOS 使用 Cmd+C/V，Windows/Linux 的 alternate terminal 使用 Ctrl+Shift+C/V 快捷键已通过统一 command resolver；未加 Shift 的 terminal Ctrl+C 继续透传 当前实现 自定义快捷键 按 Cmd/Ctrl+, 打开设置并点击命令录制，或编辑 <ZETAPROFILEROOT /keybindings.json 最多四段 Chord；设置写入采用原子替换，错误更新保留上一份有效规则并显示诊断 当前实现 查看当前会话导航 Top Bar 按钮展开垂直 Session TabList；拖动右边界可调整宽度 App Server Session Tab 的创建、投影和切换已实现；不显示 fixture 当前实现 浏览工作区文件或变更 Top Bar 按钮展开右栏；Changes / Files 切换单一 Pane，Files 可刷新 Git 状态或模糊搜索 根目录、上游领先/落后数和文本 MultiDiff 已接入 当前实现 操作当前会话 右键 Session Tab，在锚点附近打开 Pin、Close、Rename、Fork 菜单 菜单呈现、定位、关闭和键盘交互已实现；四项 mutation 尚未执行 当前实现 切换多个会话 在同一垂直 TabList 选择另一个 App Server Session 已实现；每个 Session Tab 绑定并保留一个独立本地 PTY 当前实现 在 macOS 使用 Top Bar 左侧 action 避开系统红绿灯占位并保留组件间距 70px host 占位 + 8px Titlebar 间距已实现 尺寸语义 调整窗口尺寸 从同一 viewport 重算 rows/columns，并同步 resize grid 与 PTY 已实现 尺寸语义 拆分终端 只在 Terminal Workspace 内拆成多个 Pane，并调整 Pane 比例 Grid 几何入口已完成；多 Pane runtime 尚未实现 Terminal Pane 分屏 1. 产品命名 命名规则只有一条：仓库内部统一属于 Zeta，只有跨过发布边界、真正交付给用户的 Native 终端 应用使用全小写名称 zeterm。它来自 “Zeta Terminal”，但完整形式不作为第二个品牌。 边界 规范名称 示例 --- --- --- 共享仓库、crate 和内部标识 zeta / zeta- zeta-ui、zeta-terminal- zeterm Cargo package、发布的终端应用及其用户可见表面 zeterm package、executable、app bundle、窗口标题、Top Bar 和输入提示 因此不把内部 crate、CSS、协议或测试标识重命名为 zeterm-。公开发布前仍需单独核查 zeterm 的商标、域名、应用商店和软件包名称可用性；核查结果只影响发布层名称。 2. 产品结构 当前产品结构： ``text zeterm ├─ TopBar │ ├─ window drag region │ ├─ session sidebar toggle ActionBar │ └─ agent sidebar toggle ActionBar ├─ SessionSidebar collapsible │ └─ SessionTabList │ └─ SessionTab │ ├─ App Server Session → active Thread │ └─ TerminalSession → dedicated PTY ├─ SessionContextMenu transient overlay │ └─ Pin / Close / Rename / Fork ├─ TerminalWorkspace │ └─ active TerminalSession │ ├─ BlockOutputViewport → BlockList │ └─ CommandInputEditor fixed bottom └─ AgentSidebar collapsible ├─ Pane ActionBar: Changes / Files └─ Active Pane ├─ Files: toolbar + file tree / fuzzy search └─ Changes: MultiDiffEditor ├─ file A section → DiffEditor └─ file B section → DiffEditor ` primary screen 的结构固定为“上方输出、底部输入”。键盘、IME 和 paste 先进入 CommandInputEditor；Enter 通过 host-owned command boundary 建立 Block，再把命令写入 PTY。 shell prompt、命令回显和行编辑不再作为 primary screen 的产品界面。输出仍由真实 PTY 产生， BlockList 只投影已提交命令及其 printable output，不使用静态 transcript。 alternate screen 是协议兼容的明确例外：vim、top 等程序请求 alternate buffer 后，全幅 grid 临时接管 Terminal Workspace，输入直接交给该程序；退出 alternate screen 后恢复固定底部 composer。这个切换不能改变 primary screen 的 Block 输入语义。 Top Bar 不是独立工作区，也不拥有终端 Session。它只提供窗口拖动、会话入口和少量全局操作。 Session Navigation 当前使用可折叠、可通过右边界 Sash 调整宽度的垂直 TabList，但不构成 可注册任意区域的通用 Sidebar Part。TabList 投影 App Server Session/Thread；Terminal Workspace 呈现当前 Session Tab 对应的独立本地 PTY compatibility surface。 3. 所有权 能力 最终 owner 职责边界 --- --- --- Window、Top Bar 与 Terminal Workspace 外部布局 zeterm/zeterm product host 决定窗口区域和活动会话，不进入 zeta-ui 单轴 Pane 尺寸约束、Sash track 与 feedback geometry zui::SplitViewLayout / zeta-ui::Sash 不持有产品显隐、preferred width、pointer capture 或持久化 递归 Pane geometry 与 owning-split Sash 路由 zui::GridLayout 递归组合 SplitView；不持有 Terminal Session、Agent content、Pane Tree mutation 或 active Pane Terminal Pane Tree、Pane-to-Session binding 与 Pane 状态 后续 native Terminal Workspace model 已确认是终端分屏必需边界；当前尚未完成。当前 Session Tab 到 Terminal PTY 的一对一 binding 已完成 Session Navigation 显隐、preferred width 与 resize gesture zeterm/src/sessionsidebar 使用通用 Split/Sash geometry；不拥有 Session lifecycle Agent Sidebar 显隐、preferred width 与 resize gesture zeterm/src/agentsidebar 使用通用 Split/Sash geometry，宽度限制为 240–560px，并为 main Pane 保留至少 240px；不拥有内部 Pane、文件或 diff model Agent Sidebar 内部 Pane composition zeta-agent-sidebar 的 AgentSidebar / AgentSidebarNavigation 只组合跨功能切换；Files/SCM 功能布局分别由各自子模块拥有，zeterm 仅提供 shell slot Files 树、模糊搜索与领先/落后显示 zeta-agent-sidebar / zeta-file-search / zeta-git zeta-agent-sidebar::files::FilesState 保存可丢弃 UI 状态；zeterm 适配目录 DTO 并执行动作，Git 命令解析和模糊匹配器仍由各自 crate 拥有 多文件差异内容与视口 binding zeta-agent-sidebar::EditorPane / zeta-editor::MultiDiffEditor Agent Sidebar 保存 changed-file collection 与每文件 DiffEditorState；Native 只提供 SCM 投影 通用 UI 滚动 geometry、交互映射与状态 transition zeta-ui::ScrollView / ScrollState / ScrollbarController MultiDiff 复用完整 logical-pixel 状态和交互映射；BlockOutputViewport 通过 Native adapter 复用 clip、内容坐标和 scrollbar paint；Terminal 仍保留底部相对行锚定与输出增长策略 Top Bar 内部 action 排列 zeta-ui::ActionBar 后续有真实 action 时使用；只拥有 representation geometry 和 paint 通用 Tab surface 与横/纵排列 zeta-ui::Tab / TabList 只拥有 presentation state、item size/gap、surface paint 和同源 bounds；不拥有 product content 或 tabpanel Session Tabs 与活动 Session presentation Native session navigation control 消费权威 Session projection，不复制 Session lifecycle 锚点浮层定位、viewport 约束与 layer 合成 zeta-ui::ContextView 不拥有显示生命周期、输入路由或产品 action 无边框下拉 surface、可选 header、纵向 item geometry 与默认选择 zeta-ui::Dropdown 组合 ContextView/ActionBar；不拥有产品查询、选择 identity、关闭或 command 柔和阴影、2px menu padding、4px radius、纵向 item geometry 与默认选择 zeta-ui::ContextMenu 组合 ContextView/ActionBar；不拥有 Session identity、关闭或 command Session Tab 右键菜单生命周期与 command identity zeterm/src/sessioncontextmenu 保存目标、锚点与恢复焦点；菜单关闭后不保留第二份 Session 状态 Product command identity 与注册式执行 zeta-commands / zeterm/src/commanddispatch pointer、menu 和 shortcut 只提供入口，业务行为汇合到同一 CommandRequest，再由宿主注册的 handler 执行 平台无关按键、规则顺序与冲突解析 zeta-keybinding 不读取 winit event、focus、terminal state 或用户配置，不执行产品 command winit 按键转换、Native context 与 Chord 生命周期 zeterm/src/keybindings 内建 Copy/Paste；1.5 秒超时，失焦或 IME 取消；保持 alternate terminal Control 序列透传 Native 用户快捷键资源 zeterm/src/keybindingsresource 读取 <ZETAPROFILEROOT /keybindings.json；完整校验成功才替换，坏更新保留上一份规则 快捷键模型、设置、录制和提示 zeta-keybinding 同一 feature crate 拥有规则解析、浮层 lifecycle、录制 deadline、诊断呈现和组件样式；zeterm 提供产品 command、事件 adapter 与保存接线 Terminal Session product state App Server/terminal session runtime 拥有进程、cwd、环境、输出与退出状态 Terminal grid、screen/mode state、基础 escape sequence 与 BlockList zeta-terminal::TerminalCore 不由 UiScene 或 InputBox 推断 PTY process、write、resize 与 exit zeterm/src/terminalsession + zeta-utils-pty process mechanism 与 terminal model 分离；创建在后台 worker 完成 Session Tab 到本地 PTY 的一对一 binding、活动/非活动 runtime 切换 zeterm/src/terminalworkspace Native adapter 管理 pending key、ready 顺序和非活动 runtime；不拥有 App Server Session/Thread authority cell scrollback retention zeta-terminal::TerminalGrid 会话内最多保留 10,000 行；不负责跨重启持久化 scroll position zeterm/src/terminalscrollback 可丢弃的 presentation state，不写回 terminal model terminal output selection zeterm/src/terminalselection 可丢弃的 viewport state；文本来自 terminal/Block projection 跨重启历史持久化与完整 terminal compatibility 后续 terminal runtime 尚未完成 BlockList / TerminalOutput presentation Native terminal session view 呈现 runtime output；不能成为第二份权威输出存储 Primary Block Input Editor 与 IME candidate area zeterm/src/terminalcomposer + inputmethod 编辑 host-owned TextInput；Enter 才提交真实 command boundary 命中、指针状态、focus、键盘导航与 accessibility semantics zui 只分发稳定控件身份和 activation intent，不保存 Session、文件、对话或文档状态 平台 accessibility publication 后续 zeta-winit adapter 当前尚未接 AccessKit/平台 API，内部语义树不等于屏幕阅读器已可用 alternate-screen direct input zeterm/src/terminalinput + inputmethod + TerminalCore 仅在 TUI 接管期间编码 key/IME/paste 并写入 PTY shell command completion boundary zeterm/src/terminalsession bootstrap + zeta-terminal::TerminalCore 当前 zsh 使用 OSC 133 D；其他 shell 只有基础 prompt/echo suppression Rect、icon、text scene 与 GPU draw zui / zeta-wgpu 不拥有 Session、PTY、窗口布局或产品 reducer zeterm/zeterm 可以保存活动 Tab、hover、focus、scroll position 等可丢弃 presentation state，但 Session、Thread、Turn、PTY process 和 durable output 必须来自对应 runtime。 4. 当前实现 当前实现 当前事实 目标映射 --- --- --- titlebar::Titlebar 绘制 32px 窗口顶区、拖拽区和左右 sidebar toggle ActionBar；不显示标题文案 Top Bar zeta-winit::WindowControlInsets 按 native chrome policy 提供覆盖产品内容的左右逻辑占位；macOS full-size titlebar 当前为左侧 70px 原生窗口控件安全区 sessiontablist::SessionTabList 组合 zeta-ui::TabList 的无边框 4px 圆角 surface；自身绘制与两行信息块等高的白色状态容器及会话名/工作区截断文字，并注册 Session Tab 语义 通用 TabList 已支持 6px 间隔的多项布局；App Server Session projection/switching 已接入，每个 Session Tab 绑定独立 Terminal PTY sessioncontextmenu::SessionContextMenu 右键当前真实 Session Tab 后，用通用 ContextMenu 基座绘制 Pin、Close、Rename、Fork；基座提供 renderer 柔和阴影、2px padding 与 4px radius，默认选择 Pin；菜单子树打开时成为 modal interaction scope，hover 同步 roving focus 并在移出后保留最后一项，同时支持菜单外点击、Escape、上下键、Tab、Enter/Space 与焦点恢复 下层控件在菜单打开期间不接收 pointer、focus 或 activation；四项已映射为稳定 product action，单 Session runtime 尚不执行真实 pin/close/rename/fork transition ShellLayout 组合扁平 titlebar、可选 Sessions sidebar，并把剩余区域交给 TerminalWorkspaceLayout primary screen 窗口外层布局 zeta-layout::TerminalWorkspaceLayout / zui::GridLayout 把活动 Terminal 与可选 Agent Sidebar 投影为递归 Grid Leaf；alternate screen 使用完整活动 Terminal Leaf Agent Sidebar 已接入；多 Terminal Pane runtime 尚未完成 SessionSidebarState / Sash 保存 preferred width 和 drag-start snapshot；从同一 track 生成 8px 命中区与 2px hover/active feedback 侧栏宽度限制为 160–480px，并始终为 main Pane 保留至少 240px AgentSidebarState / Sash 保存右栏显隐、preferred width 与 drag-start snapshot；从同一 track 生成 8px 命中区和 2px hover/active feedback 宽度限制为 240–560px，并始终为 main Pane 保留至少 240px；内部内容由 zeta-agent-sidebar::AgentSidebar 拥有 zeta-agent-sidebar::AgentSidebarNavigation 跨功能 Changes/Files ActionBar 与导航语义 不拥有 Files/SCM 功能布局 zeta-agent-sidebar::files::FilesLayout / FilesToolbar / FilesPane / zeta-file-search Files 自己拥有 36px 功能 toolbar、根目录文件树与工作区路径模糊匹配结果 Search 输入已接键盘、剪贴板和 IME；目录展开、滚动和文件打开动作已由 Native adapter 接线 zeta-agent-sidebar::scm::ScmLayout / EditorPaneState / zeta-editor::MultiDiffEditor SCM 自己拥有 Changes toolbar slot 与整体纵向视口；Native 将 zeta-git snapshot 映射为 ScmDiff 启动、Refresh 与 command completion 更新；binary、非 UTF-8 或单侧超过 2 MiB 的文件跳过 zeta-commands:: ZetermCommandId, CommandRequest, CommandRegistry / keybindings::NativeKeybindings / keybindingsresource::KeybindingsResource / keyboardshortcuts pointer/menu 与标准化键盘事件汇合到同一 command request，再由宿主注册的 handler 执行；resolver 支持 when、Builtin/User precedence、blocker 和最多四段 Chord；资源轮询外部编辑，设置录制采用原子写入 ✅；内建 Copy/Paste、1.5 秒 Chord timeout、失焦/IME 取消、冲突诊断、Chord 提示与设置 UI 已实现 TerminalCore / TerminalGrid 增量解析 ANSI，维护 cell、cursor、wrap、erase 与基础 SGR 当前最小 terminal emulator core Unicode terminal text CJK 按双 cell 保存；组合符、ZWJ Emoji 与 flag 序列保留在 leading cell；renderer 使用系统 outline fallback macOS 已规避不可栅格化的 GB18030 Bitmap；复杂 BiDi 行级布局尚未完成 primary/alternate screen 解析 47/1047/1048/1049，切换 active grid，并在 resize 时同步两块 grid 已实现基础 buffer lifecycle DEC mode 与 terminal input 记录 cursor key、cursor visibility、bracketed paste 和 mouse request state；编码基础 key 常见 input mode 已形成闭环 scroll region 与 terminal query 支持 margin scrolling、origin-relative cursor、line insert/delete，并把 DA/DSR/CPR reply 写回 PTY 常见纵切已实现，尚非完整 query family terminal mouse report alternate screen 内把 pointer cell、button/motion/wheel 和 modifiers 编码为 1000/1002/1003 legacy 或 1006 SGR report 不接管 titlebar；1005/1015 尚未实现 主屏 scrollback full-screen scroll 把 cell rows 保留到 10,000 行有界历史；局部 scroll region 和 alternate screen 不进入历史 CSI 3 J 清理历史；当前没有磁盘持久化 Native 回滚浏览 主屏滚轮浏览 Block t"
  },
  {
    "slug": "zeterm/native-text-input",
    "sourcePath": "zeterm/docs/native-text-input.md",
    "title": "Native Text 输入：分层、IME 与当前实现",
    "description": "Native 文本输入采用单向依赖，不建立同时理解窗口事件、编辑状态和 GPU scene 的总括 widget：",
    "group": "zeterm 产品",
    "markdown": "> 状态：Current。\n> 本文拥有 Native 单行控件与多行代码编辑器输入的跨 crate ownership、用户语义和演进边界。具体源码接口与\n> 修改路径分别由 [`zeterm`](../README.md)、\n> [`zui`](../zui/README.md)、[`zeta-ui`](../ui/README.md) 和\n> [`zeta-winit`](../winit/README.md) 说明。\n\n## 快速理解\n\nNative 文本输入采用单向依赖，不建立同时理解窗口事件、编辑状态和 GPU scene 的总括 widget：\n\n```text\nwinit keyboard / IME event\n  → zeta-winit platform forwarding\n  → zeterm/zeterm focus and event routing\n  ├─ single-line → zui TextInput → TextInputLayoutEngine → zeta-ui InputBox\n  └─ multiline   → zeta-editor CodeEditorDocument / CodeEditorViewport → CodeEditor\n  → zui UiScene\n```\n\n`zui` 不依赖 `zeta-ui` 或 `zeta-winit`；`TextInput` 不保存 `winit::Ime`、`KeyEvent` 或 GPU 类型；平台\nadapter 不理解 committed text、selection 或 composition。这个边界让 Unicode 编辑语义、\nshaping geometry 和真实平台接入可以分别测试。\n\n| 用户行为 | 当前支持 | 由谁负责 |\n| --- | --- | --- |\n| 键盘输入和光标移动 | ✅ | `TextInput` 或 `CodeEditorDocument` |\n| 中文、日文等输入法预编辑与提交 | ✅ | Native 路由 + 当前输入模型 |\n| 选择、退格和字素级移动 | ✅ | `TextInput` 或 `CodeEditorDocument` |\n| 文件编辑器鼠标放置光标、拖选与越界自动滚动 | ✅ | Native pointer/timer adapter + `CodeEditor` hit-test |\n| 文件编辑器剪贴板、撤销和重做 | ✅ | Native clipboard adapter + `CodeEditorDocument` |\n| 文件编辑器查找/替换与自动缩进 | ✅ | Native find widget + `CodeEditorDocument` search/indent contracts |\n| 多行编辑与 viewport soft wrap | ✅ | `zeta-editor::CodeEditorDocument` / editor-owned visual projection |\n\n## 2. 所有权\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| 原生 keyboard/IME event 与候选框 API | `zeta-winit` / `winit` | 委托 |\n| Composer、文件 Editor 与搜索框 focus、event routing、IME activation | `zeterm/src/main.rs::NativeApp` | ✅ |\n| Committed text、selection、grapheme movement | `zui::TextInput` | ✅ |\n| Preedit/commit/cancel composition state | `zui::TextInput` | ✅ |\n| 单行 shaping、selection/caret/preedit geometry | `zui::TextInputLayoutEngine` | ✅ |\n| Caret blink phase state machine | `zui::CaretBlinkController` | ✅ |\n| Input-box chrome、状态与 scene composition | `zeta-ui::InputBox` | ✅ |\n| Blink deadline scheduling 与 redraw | `zeterm/src/main.rs::NativeApp` | ✅ |\n| 文件 Editor mouse caret、drag selection、clipboard 与 viewport | `file_editor_input` + `zeta-editor` | ✅ |\n| 文件 Editor undo/redo 与 vertical navigation | `zeta-editor::CodeEditorDocument` | ✅ |\n| 平台 accessibility adapter | 尚无完整 owner | 尚未完成 |\n\n`TextInput` 是非 component 基座：拥有编辑状态、composition 和 shaping contract，但不实现\n`Component`，也不拥有边框、背景、placeholder 或 hover/focus 视觉。`InputBox` 才是\n`Component`，它组合 `TextInput` 的 immutable layout 与自己的 chrome/style。\n`CaretBlinkController` 同样不是 component；它只计算时间相位，不创建 timer 或请求 redraw。\n\n## 3. 用户语义与不变量\n\n- committed text 与 preedit 永远分离；`Preedit` 只改变临时 composition，`Commit` 才写入文本；\n- selection、cursor 和 IME cursor range 都使用 UTF-8 byte index；\n- 普通移动和删除以 extended grapheme cluster 为边界，不拆开组合附加符或 emoji sequence；\n- composition 在视觉上临时替换 active selection，但不修改 committed text；只有 `Commit` 才原子\n  替换 selection，cancel 保留原始文本和选择；\n- IME 没有提供 preedit cursor 时隐藏 caret；提供 range 时使用 range end 定位 caret；\n- composer、文件 Editor 或搜索框获得焦点时启用 IME；输入目标切换时清除原目标未提交的 preedit；\n- 获得焦点或发生 editing/composition activity 时 caret 立即可见，之后按 deadline 切换相位；\n- active selection 隐藏普通 caret；IME preedit cursor 仍遵守平台提供的 visible/hidden range；\n- 候选框锚点来自 shaped caret 的 logical window coordinates，不使用字符数估算；\n- 单行模型拒绝换行与控制字符。\n\n## 4. 当前端到端路径\n\n```text\npointer release on composer / file editor / search input\n  → ShellInteraction focuses the selected text input\n  → NativeWindow::enable_ime\n  → rebuild ShellPresentation\n  → TextInputLayoutEngine shapes content\n  → NativeWindow::set_ime_cursor_area(shaped caret)\n\nWindowEvent::KeyboardInput\n  → NativeApp routes by Workspace Surface and focused element\n  ├─ single-line → TextInputCommand → TextInput\n  └─ file editor → CodeEditorCommand → active CodeEditorDocument\n  → rebuild and redraw\n\nWindowEvent::Ime(Preedit)\n  → NativeApp maps platform event to TextInputCompositionEvent\n  → active TextInput or CodeEditorDocument updates temporary composition\n  → owning component paints preedit underline and caret\n\nWindowEvent::Ime(Commit)\n  → TextInput atomically inserts committed text\n  → composition clears\n  → rebuild, reposition candidate area, redraw\n\nApplicationHandler::about_to_wait\n  → CaretBlinkController::advance\n  → rebuild only when the visible phase changes\n  → ControlFlow::WaitUntil(next blink deadline)\n```\n\n## 5. 关键取舍\n\n| 选择 | 结论 | 原因 |\n| --- | --- | --- |\n| `TextInput` 直接处理 `WindowEvent` | ❌ | 造成 base → platform 反向依赖，无法纯测试 |\n| `TextInput` 实现 `Component` 并拥有 chrome | ❌ | 混合编辑基座与具体 input-box presentation |\n| 用字符数估算 caret | ❌ | 比例字体、fallback、emoji 和 BiDi 会产生错误候选框位置 |\n| preedit 直接写入 committed text | ❌ | cancel/update 会破坏文本和 selection |\n| `TextInput` base + `InputBox` component | ✅ | 编辑语义可复用，具体组件拥有自己的 chrome |\n| 独立 layout engine + immutable snapshot | ✅ | shaping 几何可复用，InputBox 保持纯 scene composition |\n\n## 6. 当前限制与演进前提\n\n单行控件支持键盘插入、grapheme-safe 左右移动与删除、Shift selection、Home/End、Select All、\nIME composition 和 caret blink；其 mouse selection、clipboard、undo/redo 与 password variant\n仍未完成。文件 Editor 另外支持多行 navigation、pointer caret/drag、clipboard、undo/redo、结构\n折叠、find/replace、自动缩进、soft wrap、垂直 viewport 和拖选越界自动滚动，但尚无平台 accessibility adapter。\n\n两类输入必须继续消费各自 owner 的同源 hit-test/layout，不能在 Native 另建字符宽度估算。\n`TextInput` 基座留在 `zui`，输入框 chrome 留在 `zeta-ui`；多行文档、命令和可见行投影留在\n`zeta-editor`。\n\n## 7. 长期不变量\n\n- platform adapter 只暴露窄 forwarding contract；\n- authoritative editing state 不进入 renderer；\n- render component 不执行产品命令；\n- composition 与 committed text 保持可区分；\n- cursor/selection geometry 与实际 shaping 使用同一文本语义；\n- 产品领域状态不得下沉到 `zui`、`zeta-ui` 或 `zeta-winit`。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 2,
        "id": "3-用户语义与不变量",
        "title": "3. 用户语义与不变量"
      },
      {
        "depth": 2,
        "id": "4-当前端到端路径",
        "title": "4. 当前端到端路径"
      },
      {
        "depth": 2,
        "id": "5-关键取舍",
        "title": "5. 关键取舍"
      },
      {
        "depth": 2,
        "id": "6-当前限制与演进前提",
        "title": "6. 当前限制与演进前提"
      },
      {
        "depth": 2,
        "id": "7-长期不变量",
        "title": "7. 长期不变量"
      }
    ],
    "searchText": " 状态：Current。 本文拥有 Native 单行控件与多行代码编辑器输入的跨 crate ownership、用户语义和演进边界。具体源码接口与 修改路径分别由 zeterm、 zui、zeta-ui 和 zeta-winit 说明。 快速理解 Native 文本输入采用单向依赖，不建立同时理解窗口事件、编辑状态和 GPU scene 的总括 widget： ``text winit keyboard / IME event → zeta-winit platform forwarding → zeterm/zeterm focus and event routing ├─ single-line → zui TextInput → TextInputLayoutEngine → zeta-ui InputBox └─ multiline → zeta-editor CodeEditorDocument / CodeEditorViewport → CodeEditor → zui UiScene ` zui 不依赖 zeta-ui 或 zeta-winit；TextInput 不保存 winit::Ime、KeyEvent 或 GPU 类型；平台 adapter 不理解 committed text、selection 或 composition。这个边界让 Unicode 编辑语义、 shaping geometry 和真实平台接入可以分别测试。 用户行为 当前支持 由谁负责 --- --- --- 键盘输入和光标移动 ✅ TextInput 或 CodeEditorDocument 中文、日文等输入法预编辑与提交 ✅ Native 路由 + 当前输入模型 选择、退格和字素级移动 ✅ TextInput 或 CodeEditorDocument 文件编辑器鼠标放置光标、拖选与越界自动滚动 ✅ Native pointer/timer adapter + CodeEditor hit-test 文件编辑器剪贴板、撤销和重做 ✅ Native clipboard adapter + CodeEditorDocument 文件编辑器查找/替换与自动缩进 ✅ Native find widget + CodeEditorDocument search/indent contracts 多行编辑与 viewport soft wrap ✅ zeta-editor::CodeEditorDocument / editor-owned visual projection 2. 所有权 能力 当前 owner 状态 --- --- --- 原生 keyboard/IME event 与候选框 API zeta-winit / winit 委托 Composer、文件 Editor 与搜索框 focus、event routing、IME activation zeterm/src/main.rs::NativeApp ✅ Committed text、selection、grapheme movement zui::TextInput ✅ Preedit/commit/cancel composition state zui::TextInput ✅ 单行 shaping、selection/caret/preedit geometry zui::TextInputLayoutEngine ✅ Caret blink phase state machine zui::CaretBlinkController ✅ Input-box chrome、状态与 scene composition zeta-ui::InputBox ✅ Blink deadline scheduling 与 redraw zeterm/src/main.rs::NativeApp ✅ 文件 Editor mouse caret、drag selection、clipboard 与 viewport fileeditorinput + zeta-editor ✅ 文件 Editor undo/redo 与 vertical navigation zeta-editor::CodeEditorDocument ✅ 平台 accessibility adapter 尚无完整 owner 尚未完成 TextInput 是非 component 基座：拥有编辑状态、composition 和 shaping contract，但不实现 Component，也不拥有边框、背景、placeholder 或 hover/focus 视觉。InputBox 才是 Component，它组合 TextInput 的 immutable layout 与自己的 chrome/style。 CaretBlinkController 同样不是 component；它只计算时间相位，不创建 timer 或请求 redraw。 3. 用户语义与不变量 - committed text 与 preedit 永远分离；Preedit 只改变临时 composition，Commit 才写入文本； - selection、cursor 和 IME cursor range 都使用 UTF-8 byte index； - 普通移动和删除以 extended grapheme cluster 为边界，不拆开组合附加符或 emoji sequence； - composition 在视觉上临时替换 active selection，但不修改 committed text；只有 Commit 才原子 替换 selection，cancel 保留原始文本和选择； - IME 没有提供 preedit cursor 时隐藏 caret；提供 range 时使用 range end 定位 caret； - composer、文件 Editor 或搜索框获得焦点时启用 IME；输入目标切换时清除原目标未提交的 preedit； - 获得焦点或发生 editing/composition activity 时 caret 立即可见，之后按 deadline 切换相位； - active selection 隐藏普通 caret；IME preedit cursor 仍遵守平台提供的 visible/hidden range； - 候选框锚点来自 shaped caret 的 logical window coordinates，不使用字符数估算； - 单行模型拒绝换行与控制字符。 4. 当前端到端路径 `text pointer release on composer / file editor / search input → ShellInteraction focuses the selected text input → NativeWindow::enableime → rebuild ShellPresentation → TextInputLayoutEngine shapes content → NativeWindow::setimecursorarea shaped caret WindowEvent::KeyboardInput → NativeApp routes by Workspace Surface and focused element ├─ single-line → TextInputCommand → TextInput └─ file editor → CodeEditorCommand → active CodeEditorDocument → rebuild and redraw WindowEvent::Ime Preedit → NativeApp maps platform event to TextInputCompositionEvent → active TextInput or CodeEditorDocument updates temporary composition → owning component paints preedit underline and caret WindowEvent::Ime Commit → TextInput atomically inserts committed text → composition clears → rebuild, reposition candidate area, redraw ApplicationHandler::abouttowait → CaretBlinkController::advance → rebuild only when the visible phase changes → ControlFlow::WaitUntil next blink deadline ` 5. 关键取舍 选择 结论 原因 --- --- --- TextInput 直接处理 WindowEvent ❌ 造成 base → platform 反向依赖，无法纯测试 TextInput 实现 Component 并拥有 chrome ❌ 混合编辑基座与具体 input-box presentation 用字符数估算 caret ❌ 比例字体、fallback、emoji 和 BiDi 会产生错误候选框位置 preedit 直接写入 committed text ❌ cancel/update 会破坏文本和 selection TextInput base + InputBox component ✅ 编辑语义可复用，具体组件拥有自己的 chrome 独立 layout engine + immutable snapshot ✅ shaping 几何可复用，InputBox 保持纯 scene composition 6. 当前限制与演进前提 单行控件支持键盘插入、grapheme-safe 左右移动与删除、Shift selection、Home/End、Select All、 IME composition 和 caret blink；其 mouse selection、clipboard、undo/redo 与 password variant 仍未完成。文件 Editor 另外支持多行 navigation、pointer caret/drag、clipboard、undo/redo、结构 折叠、find/replace、自动缩进、soft wrap、垂直 viewport 和拖选越界自动滚动，但尚无平台 accessibility adapter。 两类输入必须继续消费各自 owner 的同源 hit-test/layout，不能在 Native 另建字符宽度估算。 TextInput 基座留在 zui，输入框 chrome 留在 zeta-ui；多行文档、命令和可见行投影留在 zeta-editor。 7. 长期不变量 - platform adapter 只暴露窄 forwarding contract； - authoritative editing state 不进入 renderer； - render component 不执行产品命令； - composition 与 committed text 保持可区分； - cursor/selection geometry 与实际 shaping 使用同一文本语义； - 产品领域状态不得下沉到 zui、zeta-ui 或 zeta-winit`。"
  },
  {
    "slug": "zeterm/rendering-architecture",
    "sourcePath": "zeterm/docs/rendering-architecture.md",
    "title": "UI 渲染架构",
    "description": "组件声明“如何布局、画什么”，统一渲染接口决定“由哪个后端执行”，具体 GPU crate 才知道“如何提交”。 因此布局、组件检查器和产品交互不会因为 wgpu、Metal 或 Vulkan 实现替换而变化。",
    "group": "zeterm 产品",
    "markdown": "> 状态：当前实现。\n> 本文拥有组件到图形后端之间的跨 crate 边界与替换规则。具体接口和实现细节分别见\n> [`zui`](../zui/README.md)、[`zeta-ui`](../ui/README.md)、\n> [`zeta-renderer`](../renderer/README.md) 与\n> [`zeta-wgpu`](../wgpu/README.md)。通用图标资产契约见 [`zeta-icon`](../icon/README.md)，\n> 产品语义目录见 [`zeta-icons`](../icons/README.md)。\n\n## 快速理解\n\n组件声明“如何布局、画什么”，统一渲染接口决定“由哪个后端执行”，具体 GPU crate 才知道“如何提交”。\n因此布局、组件检查器和产品交互不会因为 wgpu、Metal 或 Vulkan 实现替换而变化。\n\n| 常见问题 | 当前行为 | 替换后端时是否改组件 |\n| --- | --- | --- |\n| 组件如何绘制矩形、文字、图标和图片？ | 向 `UiScene` 写入后端无关 primitive | ❌ |\n| 谁执行一帧？ | product host 调用 `dyn Renderer` | ❌ |\n| 谁接触 device、queue、shader 和 surface？ | 仅具体 backend crate | 不适用 |\n| 当前后端是什么？ | `zeta-wgpu::WgpuRenderer` | 不适用 |\n| 可以增加 raw Metal/Vulkan backend 吗？ | ✅，实现 `Renderer` 并替换 composition-root factory | ❌ |\n\n```mermaid\nflowchart LR\n    C[\"Component / product composition\"] --> E[\"zui Element / ComputedElement\"]\n    C --> I[\"zeta-icon asset\"]\n    E --> S[\"zui::UiScene\"]\n    I --> S\n    S --> R[\"zeta-renderer::Renderer\"]\n    R --> W[\"zeta-wgpu::WgpuRenderer\"]\n    R -. \"可替换\" .-> M[\"future Metal backend\"]\n    R -. \"可替换\" .-> V[\"future Vulkan backend\"]\n```\n\n## 边界与所有权\n\n| 层 | 决定什么 | 明确禁止 |\n| --- | --- | --- |\n| Component / product | 状态、声明式 Element、primitive 顺序、clip 与 overlay | GPU handle、shader、backend feature、手写检查元数据 |\n| `zui` | Element layout、immutable scene contract、logical coordinates、paint semantics、inspection 与有序 `SceneBatch` | 具体组件、`wgpu::*` 或其他图形 API |\n| `zeta-icon` | renderer-independent icon identity、SVG definition 与 symbolic/multicolor rendering mode | 产品语义目录、布局、GPU atlas |\n| `zeta-ui` | 基于 `zui` 的 Button、ActionBar、TabList、ContextView 等可复用组件 | scene/backend ownership、产品状态 |\n| `zeta-renderer` | target size、frame outcome、统一 backend error 与执行接口 | surface、window、pipeline、atlas |\n| backend crate | physical conversion、batch execution、resource cache、shader、submit、present | 产品状态、组件 layout、输入路由 |\n| product composition root | 选择并初始化 backend，实现类型擦除 | 把 backend 类型传播回组件或 scene |\n\n布局检查器消费 `UiScene` 同步生成的 `InspectionFrame`。所有 `Component` 和产品 composition\nsurface 都先声明 zui Element；computed layout 自动生成尺寸、resolved padding、gap、radius、实际\ngap regions、authored style 与声明位置。采集位于 backend 之前，所以切换 GPU API 不会改变结果。\n\n`ShellPresentation` 由 `zui::UiFrame<InteractionFrame>` 作为单一 frame owner，再保存\naccessibility projection；只有从该 frame 投影出的 `UiScene` 交给 `Renderer`。命中、焦点、cursor、\ncommand dispatch 和 accessibility 不属于 GPU 协议，也不会因更换 backend 而重新实现。旧 Native split\nhost boundary 已删除；剩余 retained cleanup 和发布边界见\n[`native-deprecation-plan.md`](native-deprecation-plan.md)。\n\n## 当前执行流程\n\n1. Component 通过 `UiScene::draw_component` 组合子组件；入口把必需的 `Component::element` 解析成\n   一次 `ComputedElement`，同步用于自动 inspection 与 `paint_element`。拥有 content closure 的\n   composition surface 使用 `UiScene::with_element` 进入同一管线。\n2. `UiScene` 按 composition layer 与 primitive 插入顺序产生连续的 `SceneBatch`；不同 primitive\n   类型之间的覆盖顺序不会被 backend 重排。\n3. `ShellPresentation` 保存单一 frame owner 派生的 scene、inspection、interaction 与 accessibility\n   snapshot，不获得任何 GPU 对象。\n4. Host 只把 scene 传给 `Box<dyn Renderer>::render_scene`。\n5. 当前 `renderer_backend::create` 选择 `WgpuRenderer`；只有该 adapter 知道具体类型。\n6. `zeta-wgpu` 把 batch 对应的 logical primitive range 转为 physical instances/glyph buffers，按\n   scene 顺序切换 pipeline、提交并 present。\n\n当前 wgpu 在 macOS、Linux 和 Windows 上本身可以选择 Metal、Vulkan 或 DX12 backend；本文边界\n解决的是更进一步的实现替换，例如绕过 wgpu 编写专用 Metal/Vulkan renderer。\n\n## 长期不变量\n\n- `zui` 不直接依赖或引用任何组件 crate、窗口系统或具体 GPU API；\n- `zui` 通过 `zeta-icon` 消费通用图标资产契约，不依赖 `zeta-icons` 产品目录；\n- `zeta-ui` 只依赖 `zui`，不拥有 scene/backend contract；\n- Component 只能产生 scene primitive，不接受 backend context；\n- backend 不重新解释产品布局、组件身份或交互状态；\n- primitive 的 back-to-front 顺序由 scene contract 决定，backend 不得按自身 pipeline 偏好重排；\n- interaction/accessibility frame 不进入 `Renderer` 或具体 GPU crate；\n- host 只依赖 `Renderer`，具体 backend 类型只出现在 composition root；\n- backend-specific 优化不能污染 scene API，除非该能力对所有后端都有稳定语义。\n\n`component_composition_tests::zui_contract_does_not_depend_on_a_gpu_backend_or_component_crate`\n会扫描 `zui` manifest 和源码，阻止重新引入 `wgpu` 或反向依赖 `zeta-ui`；\n`component_renderer_and_dispatch_crates_depend_on_zui_in_the_forward_direction` 固定依赖方向；\n`gpu_backend_does_not_own_interaction_or_accessibility_frames` 阻止\ninput/accessibility ownership 漂入 backend。Rust 类型系统则保证 Native 的 renderer 字段只暴露 trait。\n\n## 当前状态与演进\n\n当前已完成 scene/backend 分离、ordered scene batching、presentation/interaction 与 GPU execution\n分离、`Renderer` trait、wgpu adapter、Native trait-object ownership，以及 rect/image/icon/text\npipeline 从 `zui` 向 `zeta-wgpu` 的迁移；通用 icon contract 已下沉到 `zeta-icon`，并有\n`zui-demo` 作为无产品状态的宿主 smoke test。`zeta-ui` 当前兼容 re-export `zui` API，便于产品组件\n逐步迁移 import；renderer、dispatch 与 backend 已直接依赖 `zui`。尚未实现 backend capability\nnegotiation、运行时热切换、raw Metal/Vulkan backend 或跨 backend golden-image 一致性测试。\n\n新增 backend 时，应建立独立 crate、实现 `Renderer`、在 product composition root 替换 factory，\n并针对 primitive、色彩空间、clip、layer、字体 fallback 与 surface recovery 建立一致性测试。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "边界与所有权",
        "title": "边界与所有权"
      },
      {
        "depth": 2,
        "id": "当前执行流程",
        "title": "当前执行流程"
      },
      {
        "depth": 2,
        "id": "长期不变量",
        "title": "长期不变量"
      },
      {
        "depth": 2,
        "id": "当前状态与演进",
        "title": "当前状态与演进"
      }
    ],
    "searchText": " 状态：当前实现。 本文拥有组件到图形后端之间的跨 crate 边界与替换规则。具体接口和实现细节分别见 zui、zeta-ui、 zeta-renderer 与 zeta-wgpu。通用图标资产契约见 zeta-icon， 产品语义目录见 zeta-icons。 快速理解 组件声明“如何布局、画什么”，统一渲染接口决定“由哪个后端执行”，具体 GPU crate 才知道“如何提交”。 因此布局、组件检查器和产品交互不会因为 wgpu、Metal 或 Vulkan 实现替换而变化。 常见问题 当前行为 替换后端时是否改组件 --- --- --- 组件如何绘制矩形、文字、图标和图片？ 向 UiScene 写入后端无关 primitive ❌ 谁执行一帧？ product host 调用 dyn Renderer ❌ 谁接触 device、queue、shader 和 surface？ 仅具体 backend crate 不适用 当前后端是什么？ zeta-wgpu::WgpuRenderer 不适用 可以增加 raw Metal/Vulkan backend 吗？ ✅，实现 Renderer 并替换 composition-root factory ❌ ``mermaid flowchart LR C \"Component / product composition\" -- E \"zui Element / ComputedElement\" C -- I \"zeta-icon asset\" E -- S \"zui::UiScene\" I -- S S -- R \"zeta-renderer::Renderer\" R -- W \"zeta-wgpu::WgpuRenderer\" R -. \"可替换\" .- M \"future Metal backend\" R -. \"可替换\" .- V \"future Vulkan backend\" ` 边界与所有权 层 决定什么 明确禁止 --- --- --- Component / product 状态、声明式 Element、primitive 顺序、clip 与 overlay GPU handle、shader、backend feature、手写检查元数据 zui Element layout、immutable scene contract、logical coordinates、paint semantics、inspection 与有序 SceneBatch 具体组件、wgpu:: 或其他图形 API zeta-icon renderer-independent icon identity、SVG definition 与 symbolic/multicolor rendering mode 产品语义目录、布局、GPU atlas zeta-ui 基于 zui 的 Button、ActionBar、TabList、ContextView 等可复用组件 scene/backend ownership、产品状态 zeta-renderer target size、frame outcome、统一 backend error 与执行接口 surface、window、pipeline、atlas backend crate physical conversion、batch execution、resource cache、shader、submit、present 产品状态、组件 layout、输入路由 product composition root 选择并初始化 backend，实现类型擦除 把 backend 类型传播回组件或 scene 布局检查器消费 UiScene 同步生成的 InspectionFrame。所有 Component 和产品 composition surface 都先声明 zui Element；computed layout 自动生成尺寸、resolved padding、gap、radius、实际 gap regions、authored style 与声明位置。采集位于 backend 之前，所以切换 GPU API 不会改变结果。 ShellPresentation 由 zui::UiFrame<InteractionFrame 作为单一 frame owner，再保存 accessibility projection；只有从该 frame 投影出的 UiScene 交给 Renderer。命中、焦点、cursor、 command dispatch 和 accessibility 不属于 GPU 协议，也不会因更换 backend 而重新实现。旧 Native split host boundary 已删除；剩余 retained cleanup 和发布边界见 native-deprecation-plan.md。 当前执行流程 1. Component 通过 UiScene::drawcomponent 组合子组件；入口把必需的 Component::element 解析成 一次 ComputedElement，同步用于自动 inspection 与 paintelement。拥有 content closure 的 composition surface 使用 UiScene::withelement 进入同一管线。 2. UiScene 按 composition layer 与 primitive 插入顺序产生连续的 SceneBatch；不同 primitive 类型之间的覆盖顺序不会被 backend 重排。 3. ShellPresentation 保存单一 frame owner 派生的 scene、inspection、interaction 与 accessibility snapshot，不获得任何 GPU 对象。 4. Host 只把 scene 传给 Box<dyn Renderer ::renderscene。 5. 当前 rendererbackend::create 选择 WgpuRenderer；只有该 adapter 知道具体类型。 6. zeta-wgpu 把 batch 对应的 logical primitive range 转为 physical instances/glyph buffers，按 scene 顺序切换 pipeline、提交并 present。 当前 wgpu 在 macOS、Linux 和 Windows 上本身可以选择 Metal、Vulkan 或 DX12 backend；本文边界 解决的是更进一步的实现替换，例如绕过 wgpu 编写专用 Metal/Vulkan renderer。 长期不变量 - zui 不直接依赖或引用任何组件 crate、窗口系统或具体 GPU API； - zui 通过 zeta-icon 消费通用图标资产契约，不依赖 zeta-icons 产品目录； - zeta-ui 只依赖 zui，不拥有 scene/backend contract； - Component 只能产生 scene primitive，不接受 backend context； - backend 不重新解释产品布局、组件身份或交互状态； - primitive 的 back-to-front 顺序由 scene contract 决定，backend 不得按自身 pipeline 偏好重排； - interaction/accessibility frame 不进入 Renderer 或具体 GPU crate； - host 只依赖 Renderer，具体 backend 类型只出现在 composition root； - backend-specific 优化不能污染 scene API，除非该能力对所有后端都有稳定语义。 componentcompositiontests::zuicontractdoesnotdependonagpubackendorcomponentcrate 会扫描 zui manifest 和源码，阻止重新引入 wgpu 或反向依赖 zeta-ui； componentrendereranddispatchcratesdependonzuiintheforwarddirection 固定依赖方向； gpubackenddoesnotowninteractionoraccessibilityframes 阻止 input/accessibility ownership 漂入 backend。Rust 类型系统则保证 Native 的 renderer 字段只暴露 trait。 当前状态与演进 当前已完成 scene/backend 分离、ordered scene batching、presentation/interaction 与 GPU execution 分离、Renderer trait、wgpu adapter、Native trait-object ownership，以及 rect/image/icon/text pipeline 从 zui 向 zeta-wgpu 的迁移；通用 icon contract 已下沉到 zeta-icon，并有 zui-demo 作为无产品状态的宿主 smoke test。zeta-ui 当前兼容 re-export zui API，便于产品组件 逐步迁移 import；renderer、dispatch 与 backend 已直接依赖 zui。尚未实现 backend capability negotiation、运行时热切换、raw Metal/Vulkan backend 或跨 backend golden-image 一致性测试。 新增 backend 时，应建立独立 crate、实现 Renderer`、在 product composition root 替换 factory， 并针对 primitive、色彩空间、clip、layer、字体 fallback 与 surface recovery 建立一致性测试。"
  },
  {
    "slug": "zeterm/ui-component-migration-plan",
    "sourcePath": "zeterm/docs/ui-component-migration-plan.md",
    "title": "ZUI 组件组合迁移",
    "description": "本文是 zui 组件组合迁移的系统级计划。zui 的实现契约见 zeterm/zui/README.md，Files/SCM 的产品组件见 zeterm/agent-sidebar。app 宿主的渐进式弃用边界见 docs/native-deprecation-plan.md。本文拥有跨 crate 的边界、阶段和长期不变量；具体类型细节以源码和 crate README…",
    "group": "zeterm 产品",
    "markdown": "本文是 `zui` 组件组合迁移的系统级计划。`zui` 的实现契约见 [`zeterm/zui/README.md`](../zui/README.md)，Files/SCM 的产品组件见 [`zeterm/agent-sidebar`](../agent-sidebar)。app 宿主的渐进式弃用边界见 [`docs/native-deprecation-plan.md`](native-deprecation-plan.md)。本文拥有跨 crate 的边界、阶段和长期不变量；具体类型细节以源码和 crate README 为准。\n\n## 快速理解\n\n迁移的结果是：一个组件只组合一次，框架从这次组合同时得到布局、绘制、交互和 inspector 语义；动画由稳定组件身份和帧时钟驱动，不能再由组件各自安装定时器或维护第二套几何树。\n\n| 常见问题 | 当前迁移结果 | 长期规则 |\n| --- | --- | --- |\n| inspector 为什么能深入到树项内部？ | Files 已经把 `FilesTreeItem`、Disclosure、Icon、Label 作为真实子组件组合 | inspector 跟随组件组合，不从绘制 primitive 反推节点 |\n| 交互节点如何和 inspector 对上？ | 两者通过同一个 `ElementId` 对齐，边界来自同一个 `ComputedElement` | 组件身份不能用可变数组下标替代 |\n| hover/selected 如何进入绘制？ | 组件读取宿主投影的当前状态，绘制与交互节点在同一次 frame 组合中生成 | 状态由产品模型拥有，样式由组件拥有，平台事件由宿主适配 |\n| 滚动和虚拟化如何处理？ | `TreeView/ListView/ScrollView` 提供 frame 组合入口；绘制可保留 overscan，交互只注册可见项 | 可见范围、裁剪和 hit-test 必须共享同一份布局 |\n| 动画如何工作？ | `AnimationRegistry` 按 `ElementId + property` 保留 track，`ComponentContext` 携带当前单调时钟 | 动画是跨帧状态层，聚合 deadline 后向帧调度器请求失效，不创建平台 timer |\n| 迁移期间旧代码怎么办？ | 旧 split scene/interaction 宿主边界已删除 | 新组件走 `UiFrame::draw_component`；不保留组件级 `register_interactions` 或平行输出字段 |\n\n一次组件帧的逻辑顺序是：\n\n```mermaid\nflowchart LR\n    A[宿主状态与输入] --> B[稳定 ElementId]\n    T[帧时钟] --> C[动画采样与 deadline]\n    B --> D[一次组件组合]\n    C --> D\n    D --> E[ComputedElement 布局]\n    E --> F[paint scene]\n    E --> G[interaction hit-test/focus/action]\n    E --> H[inspector/accessibility semantics]\n    C --> I[FrameScheduler invalidation]\n    I --> J[宿主事件循环唤醒]\n    J --> A\n```\n\n## 设计范围与非目标\n\n迁移范围是 retained native UI 的组件组合边界：组件身份、布局、绘制、交互、语义、动画采样以及它们与宿主帧调度的连接。\n\n以下内容不迁入 `zui` 的产品组件层：\n\n- 文件系统、Git、会话、编辑器文档等权威业务状态；\n- 平台事件、窗口事件循环、timer 安装和命令副作用；\n- 产品特有的颜色、文案、业务 action 和数据加载；\n- 通过 paint primitive 数量推断组件身份的 inspector 实现。\n\n## 长期组件契约\n\n“组件有五个输出面”仍然成立，但不是完整运行时模型。完整模型分成输出面、时间层和宿主生命周期三类：\n\n| 层 | 解决的问题 | 所有者 | 是否生成独立树 |\n| --- | --- | --- | --- |\n| identity/tree | 这是谁、父子关系是什么、跨帧如何复用 | `zui` 契约，产品组件分配稳定 ID | 否，作为所有输出的共同骨架 |\n| layout | 当前帧的边界、间距、可见范围 | `zui` 与通用 UI 组件 | 否 |\n| paint | 当前帧画什么 primitive | 组件与 `UiScene` | 否 |\n| interaction | hover、focus、hit-test、action、失效级别 | `zui` 交互节点与宿主分派 | 否 |\n| semantics | inspector、accessibility role、label、selection、expansion | 组件声明，`zui` 收集 | 否 |\n| animation/time | 状态如何随时间过渡、何时请求下一帧 | `zui` 动画原语；宿主唤醒 | 否，是跨帧输入层 |\n| lifecycle/retention | mount、update、unmount、fragment 保留和退出动画 | 宿主与 `zui` retained runtime | 否，是帧之间的协调层 |\n\n动画的关键约束：\n\n- 动画键必须是稳定 `ElementId` 加属性身份，例如“某个 tree item 的 opacity”，不能使用本次可见数组中的 index；\n- 组件只读取当前采样值并声明目标状态，不能创建线程、timer 或直接唤醒平台；\n- `ScalarAnimation::next_deadline` 与 `FrameScheduler` 连接，动画变化通常请求 `Render`、`Fragment` 或 `Rebuild` 中最小的失效范围；\n- 如果动画改变尺寸或位置，采样值必须先进入 layout，再由同一份 `ComputedElement` 驱动 paint、hit-test 和 semantics；只改变颜色或透明度时可以保持布局并走较小失效范围；\n- 组件卸载时的 exit animation 属于 lifecycle/retention，不得让 inspector 保留一个已经不在当前语义树中的幽灵节点。\n\n## 所有权与调用路径\n\n| 对象 | 决定什么 | 执行什么 | 保存什么 |\n| --- | --- | --- | --- |\n| 产品模型/宿主 | 选择、展开、搜索、滚动、业务 action 和目标状态 | 将输入事件映射为模型更新 | 权威业务状态与 retained viewport |\n| `Component` | 根元素、语义身份、交互节点和子组件组合 | 声明状态投影并调用子组件 | 不保存平台 timer；可读取宿主传入的 retained state |\n| `ComponentContext`/`UiFrame` | 当前父链、frame clock 和共享输出边界 | 一次组合同时注册 inspection 与 interaction、写入 scene | 当前 frame 的临时输出 |\n| `UiScene` | primitive 顺序、layer、clip 和 inspection 树 | 记录绘制与布局检查数据 | 当前 scene 及 retained fragment 边界 |\n| `InteractionFrame` | 当前交互节点、祖先链和 focus scope | hit-test、focus、accessibility snapshot | 当前 frame 的交互节点 |\n| `FrameScheduler` | 下一帧需要的最强失效级别 | 合并动画、输入和模型变化 | pending invalidation 与 fragment IDs |\n\n典型调用路径如下：\n\n1. 宿主把平台 pointer/key 事件投影到产品状态或 `UiDispatch`。\n2. 宿主提供 retained animation binding 和显式时钟；组件以稳定 key 声明目标，runtime 返回当前采样值和下一次 deadline。\n3. 组件以稳定 `ElementId` 创建 `ComponentElement`，通过 `ComponentContext::draw_component` 或\n   `ComponentContext::with_component` 组合子组件与自定义 paint。\n4. `ComputedElement` 只计算一次；同一个边界进入 scene paint、interaction node 和 inspection node。\n5. 宿主从 `FrameScheduler` 获取失效等级，按 deadline 请求下一次平台 redraw。\n\n## 当前实现状态\n\n当前实现、迁移中内容和未来方向明确分开：\n\n| 能力 | 当前状态 | 代码证据 |\n| --- | --- | --- |\n| Element identity | 已具备 | `ComponentElement::with_identity`、`ComputedElement::identity`、`InspectionNode::element_id` |\n| 共享组件组合 | 已具备 | `ComponentContext`、`UiFrame::draw_component`；split adapter 仅用于迁移 |\n| Files tree 深层 inspector | 已迁移 | `FilesTreeView`、`FilesTreeItem`、`FilesTreeDisclosure`、`FilesTreeIcon`、`FilesTreeLabel` |\n| Files tree 与 toolbar 交互 | 已迁移 | `FilesPane`、`FilesToolbar`、`FilesTreeItem::interaction_node` |\n| SCM 根与深层组件树 | 已迁移 | `EditorPane::interaction_node`、`MultiDiffEditor`、`MultiDiffSection`、`DiffEditor` 与 `CodeEditor` |\n| 通用列表/树的 frame 组合 | 已具备 | `ScrollView::draw_components`、`ListView::draw_components`、`TreeView::draw_components` |\n| 浮层与 modal 组件组合 | 已具备 | `ContextView/ContextMenu/Dropdown::*draw_components*`、`ComponentContext::set_modal_root` |\n| Native overlay 迁移 | 已完成 | Session menu、workspace path picker、Git branch menu、keyboard shortcuts、settings page |\n| Native file editor 迁移 | 已完成 | `FileEditorPane` 与独立 `file_editor_pane_interaction`；TabList、SearchBar、Notice、Document、CodeEditor 均进入组合 inspector |\n| 标量动画与稳定 track | 已具备 | `ScalarAnimation`、`AnimationKey`、`AnimationRegistry`、`AnimationAdvanceReport` |\n| 宿主 deadline 聚合 | 已具备 | `FrameDeadlineSet`；Native 的 `about_to_wait` 只负责投影到事件循环 |\n| 动画自动绑定组件属性 | 当前产品接入已完成 | `AnimationBinding`、`ComponentContext::bind_scalar` 和 `AnimationRegistry` 已连通；Language Server switch 与 SCM fold height 都由 retained runtime 持有 track |\n| 全 Native 迁移 | 主路径已完成，产品边界持续收敛 | 组件级 `register_interactions` 与 split host 已清零；Shell 宿主组合使用 `UiFrame`/`ComponentContext::with_component`，后续只允许新增产品语义，不得回写 framework runtime |\n| Native split host 边界 | ✅ 已完成 | `ShellPresentation` 独占 `UiFrame<InteractionFrame>`；旧 split API 已删除 |\n| Retained fragment lifecycle | 当前产品路径已闭合 | `RetainedRuntime` 聚合 `RetainedFragmentRegistry` 与 `AnimationRegistry`；NativeApp 已登记 Shell fragment、消费 removed IDs 并恢复 scene/interaction checkpoint；现有产品 fragment 明确采用即时 unmount，未来若需要退出视觉必须显式声明 exit spec |\n\n## 分阶段迁移\n\n### 阶段一：共享契约与兼容入口\n\n目标是让新组件可以一次组合产生共享输出，同时不要求整个 Native shell 一次性重写。\n\n- [x] 为 `ComponentElement` 和 `InspectionNode` 增加稳定身份连接；\n- [x] 增加 `Component::interaction_node` 和 `Component::compose` 默认契约；\n- [x] 增加 `ComponentContext`、`UiFrame` 和统一 frame composition 入口；\n- [x] 清零 split scene/interaction 调用并删除旧兼容入口；\n- [x] 将当前帧时钟传入组合上下文，保持动画原语不依赖平台 timer；\n- [x] 为 animation property key、deadline 聚合和组件失效范围补充通用契约测试。\n\n### 阶段二：Files 纵向切片\n\n- [x] `FilesPane` 根节点通过同一 `ElementId` 对齐 inspector 与 interaction；\n- [x] Tree row 使用真实组件组合，深入到 disclosure、icon、label；\n- [x] `TreeView/ListView/ScrollView` 支持 context 组合，并保留裁剪和 scrollbar paint；\n- [x] overscan 只服务 paint，交互节点限制在可见范围；\n- [x] 用测试验证身份、边界、label、祖先链和可见行数。\n- [x] Files toolbar 与 sidebar navigation 通过共享组合生成 root、action bar 和按钮节点。\n\n### 阶段三：SCM 纵向切片\n\n- [x] `EditorPane` 拥有自身 interaction root；\n- [x] `MultiDiffEditor` 的 section、file header、fold control、scrollbar 改成真实子组件，并通过 `ComponentContext::draw_component` 生成共享 inspector/interaction 树；\n- [x] `DiffEditor` 继续组合真实 `CodeEditor` 子组件，SCM 不再手工拼接 `UiNode` 子节点；\n- [x] 将当前保留旧 fold 行为的 index-based identity 替换为 host-provided stable file identity，并为 diff section 的折叠动画定义 property key。\n\n当前 SCM slice 已完成“结构迁移”与“跨列表重排的稳定身份”：`EditorPaneState` 按 changed-file path\n分配 `MultiDiffEditorItemIdentity`，并在快照重排时复用对应的 `DiffEditorState`。section、header、diff body\n和 fold control 都从同一 identity 派生；`fold_animation_key` 使用 section 的\n`AnimationProperty::Height`。`MultiDiffEditor::fold_element_id` 仍保留原有 item/region 编码，供未迁移的\nstandalone caller 兼容，但产品 SCM 路径不再使用它。SCM host 已将目标 section height 通过\n`ComponentContext::bind_scalar` 接入 retained `AnimationBinding`；由于高度会重新定位后续 section，动画使用\n`FrameInvalidation::Rebuild`，而不是伪装成 fragment-local paint 更新。animation track 随 stable section identity\n自动清理，不由 editor 组件保存动画对象。\n\n### 阶段四：宿主收敛\n\n- [x] Native `ShellPresentation` 以 `UiFrame` 作为内部唯一 frame owner；\n- [x] 将剩余 `register_interactions` 调用迁移到 `interaction_node`/`compose`；\n- [x] 删除组件级重复注册路径和 `draw_component_with_interaction` split host 兼容边界；\n- [x] 将动画 deadline 聚合与 `FrameScheduler` 的最小失效等级接通，并提供 `ComponentContext::bind_scalar`；产品属性按 vertical slice 逐步声明绑定。\n\n### 阶段五：生命周期与动画完善\n\n- [x] 为 retained fragment 的 mount/update/unmount 固定 state owner、exit deadline 和 cleanup 时机；\n- [x] 支持稳定 identity 下的标量属性动画、反向 retarget 和取消，并通过 `AnimationBinding` 让组件声明目标属性；\n- [x] 固定 terminal fragment 移除、interaction checkpoint 恢复和 inspector ghost node 禁止规则；Shell 已接入 `removed_ids` cleanup，当前无退出视觉规格的产品 fragment 明确直接 unmount；\n- [x] 为 lifecycle deadline 和 animation advance 增加 deterministic clock 测试，并覆盖 Shell/Files/SCM/overlay 的一帧结构回归。\n\n## 验收不变量\n\n后续任何组件迁移都必须满足：\n\n- 一个可交互组件的 `UiNode.id` 与其 `ComputedElement.identity` 相同；\n- interaction bounds、inspection bounds 和 paint 使用同一个 computed geometry，不复制计算公式；\n- inspector 只出现当前组合树中的节点；overscan、缓存和退出动画不能制造可交互幽灵节点；\n- 父链由组件组合上下文生成；跨宿主边界才允许组件显式指定 parent；\n- 动画推进不直接调用平台 API；deadline 由宿主调度，采样值由组件消费；\n- 产品状态、平台事件和副作用不进入 `zui` base/presentation 的通用契约；\n- 迁移期间兼容入口不得与新组合路径同时注册同一 `ElementId`。\n\n## 测试与发布\n\n每个 vertical slice 至少包含三类测试：\n\n- **结构契约**：检查 inspector 与 interaction 的 identity、bounds、label 和祖先链；\n- **可见性契约**：检查虚拟化、clip、overscan 不改变可交互范围；\n- **状态/时间契约**：检查 selected/hovered/focus 和动画在显式时钟下的采样、失效和完成状态。\n\n当前阶段的最小验证入口：\n\n```text\ncargo test -p zui --lib\ncargo test -p zeta-ui --lib\ncargo test -p zeta-keybinding --lib\ncargo test -p zeta-settings --lib\ncargo test -p zeta-agent-sidebar --lib\ncargo test --manifest-path Cargo.toml -p zeterm layout_inspector\ncargo test --manifest-path Cargo.toml -p zeterm file_editor_pane\ncargo test --manifest-path Cargo.toml -p zeterm shell_scene\n```\n\nNative 后续统一 `UiFrame` 前，先保持各 vertical slice 的 targeted tests 通过；任何一条路径产生重复 `ElementId`、不同 bounds 或不同 parent，都视为迁移阻断问题。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "设计范围与非目标",
        "title": "设计范围与非目标"
      },
      {
        "depth": 2,
        "id": "长期组件契约",
        "title": "长期组件契约"
      },
      {
        "depth": 2,
        "id": "所有权与调用路径",
        "title": "所有权与调用路径"
      },
      {
        "depth": 2,
        "id": "当前实现状态",
        "title": "当前实现状态"
      },
      {
        "depth": 2,
        "id": "分阶段迁移",
        "title": "分阶段迁移"
      },
      {
        "depth": 3,
        "id": "阶段一共享契约与兼容入口",
        "title": "阶段一：共享契约与兼容入口"
      },
      {
        "depth": 3,
        "id": "阶段二files-纵向切片",
        "title": "阶段二：Files 纵向切片"
      },
      {
        "depth": 3,
        "id": "阶段三scm-纵向切片",
        "title": "阶段三：SCM 纵向切片"
      },
      {
        "depth": 3,
        "id": "阶段四宿主收敛",
        "title": "阶段四：宿主收敛"
      },
      {
        "depth": 3,
        "id": "阶段五生命周期与动画完善",
        "title": "阶段五：生命周期与动画完善"
      },
      {
        "depth": 2,
        "id": "验收不变量",
        "title": "验收不变量"
      },
      {
        "depth": 2,
        "id": "测试与发布",
        "title": "测试与发布"
      }
    ],
    "searchText": "本文是 zui 组件组合迁移的系统级计划。zui 的实现契约见 zeterm/zui/README.md，Files/SCM 的产品组件见 zeterm/agent-sidebar。app 宿主的渐进式弃用边界见 docs/native-deprecation-plan.md。本文拥有跨 crate 的边界、阶段和长期不变量；具体类型细节以源码和 crate README 为准。 快速理解 迁移的结果是：一个组件只组合一次，框架从这次组合同时得到布局、绘制、交互和 inspector 语义；动画由稳定组件身份和帧时钟驱动，不能再由组件各自安装定时器或维护第二套几何树。 常见问题 当前迁移结果 长期规则 --- --- --- inspector 为什么能深入到树项内部？ Files 已经把 FilesTreeItem、Disclosure、Icon、Label 作为真实子组件组合 inspector 跟随组件组合，不从绘制 primitive 反推节点 交互节点如何和 inspector 对上？ 两者通过同一个 ElementId 对齐，边界来自同一个 ComputedElement 组件身份不能用可变数组下标替代 hover/selected 如何进入绘制？ 组件读取宿主投影的当前状态，绘制与交互节点在同一次 frame 组合中生成 状态由产品模型拥有，样式由组件拥有，平台事件由宿主适配 滚动和虚拟化如何处理？ TreeView/ListView/ScrollView 提供 frame 组合入口；绘制可保留 overscan，交互只注册可见项 可见范围、裁剪和 hit-test 必须共享同一份布局 动画如何工作？ AnimationRegistry 按 ElementId + property 保留 track，ComponentContext 携带当前单调时钟 动画是跨帧状态层，聚合 deadline 后向帧调度器请求失效，不创建平台 timer 迁移期间旧代码怎么办？ 旧 split scene/interaction 宿主边界已删除 新组件走 UiFrame::drawcomponent；不保留组件级 registerinteractions 或平行输出字段 一次组件帧的逻辑顺序是： ``mermaid flowchart LR A 宿主状态与输入 -- B 稳定 ElementId T 帧时钟 -- C 动画采样与 deadline B -- D 一次组件组合 C -- D D -- E ComputedElement 布局 E -- F paint scene E -- G interaction hit-test/focus/action E -- H inspector/accessibility semantics C -- I FrameScheduler invalidation I -- J 宿主事件循环唤醒 J -- A ` 设计范围与非目标 迁移范围是 retained native UI 的组件组合边界：组件身份、布局、绘制、交互、语义、动画采样以及它们与宿主帧调度的连接。 以下内容不迁入 zui 的产品组件层： - 文件系统、Git、会话、编辑器文档等权威业务状态； - 平台事件、窗口事件循环、timer 安装和命令副作用； - 产品特有的颜色、文案、业务 action 和数据加载； - 通过 paint primitive 数量推断组件身份的 inspector 实现。 长期组件契约 “组件有五个输出面”仍然成立，但不是完整运行时模型。完整模型分成输出面、时间层和宿主生命周期三类： 层 解决的问题 所有者 是否生成独立树 --- --- --- --- identity/tree 这是谁、父子关系是什么、跨帧如何复用 zui 契约，产品组件分配稳定 ID 否，作为所有输出的共同骨架 layout 当前帧的边界、间距、可见范围 zui 与通用 UI 组件 否 paint 当前帧画什么 primitive 组件与 UiScene 否 interaction hover、focus、hit-test、action、失效级别 zui 交互节点与宿主分派 否 semantics inspector、accessibility role、label、selection、expansion 组件声明，zui 收集 否 animation/time 状态如何随时间过渡、何时请求下一帧 zui 动画原语；宿主唤醒 否，是跨帧输入层 lifecycle/retention mount、update、unmount、fragment 保留和退出动画 宿主与 zui retained runtime 否，是帧之间的协调层 动画的关键约束： - 动画键必须是稳定 ElementId 加属性身份，例如“某个 tree item 的 opacity”，不能使用本次可见数组中的 index； - 组件只读取当前采样值并声明目标状态，不能创建线程、timer 或直接唤醒平台； - ScalarAnimation::nextdeadline 与 FrameScheduler 连接，动画变化通常请求 Render、Fragment 或 Rebuild 中最小的失效范围； - 如果动画改变尺寸或位置，采样值必须先进入 layout，再由同一份 ComputedElement 驱动 paint、hit-test 和 semantics；只改变颜色或透明度时可以保持布局并走较小失效范围； - 组件卸载时的 exit animation 属于 lifecycle/retention，不得让 inspector 保留一个已经不在当前语义树中的幽灵节点。 所有权与调用路径 对象 决定什么 执行什么 保存什么 --- --- --- --- 产品模型/宿主 选择、展开、搜索、滚动、业务 action 和目标状态 将输入事件映射为模型更新 权威业务状态与 retained viewport Component 根元素、语义身份、交互节点和子组件组合 声明状态投影并调用子组件 不保存平台 timer；可读取宿主传入的 retained state ComponentContext/UiFrame 当前父链、frame clock 和共享输出边界 一次组合同时注册 inspection 与 interaction、写入 scene 当前 frame 的临时输出 UiScene primitive 顺序、layer、clip 和 inspection 树 记录绘制与布局检查数据 当前 scene 及 retained fragment 边界 InteractionFrame 当前交互节点、祖先链和 focus scope hit-test、focus、accessibility snapshot 当前 frame 的交互节点 FrameScheduler 下一帧需要的最强失效级别 合并动画、输入和模型变化 pending invalidation 与 fragment IDs 典型调用路径如下： 1. 宿主把平台 pointer/key 事件投影到产品状态或 UiDispatch。 2. 宿主提供 retained animation binding 和显式时钟；组件以稳定 key 声明目标，runtime 返回当前采样值和下一次 deadline。 3. 组件以稳定 ElementId 创建 ComponentElement，通过 ComponentContext::drawcomponent 或 ComponentContext::withcomponent 组合子组件与自定义 paint。 4. ComputedElement 只计算一次；同一个边界进入 scene paint、interaction node 和 inspection node。 5. 宿主从 FrameScheduler 获取失效等级，按 deadline 请求下一次平台 redraw。 当前实现状态 当前实现、迁移中内容和未来方向明确分开： 能力 当前状态 代码证据 --- --- --- Element identity 已具备 ComponentElement::withidentity、ComputedElement::identity、InspectionNode::elementid 共享组件组合 已具备 ComponentContext、UiFrame::drawcomponent；split adapter 仅用于迁移 Files tree 深层 inspector 已迁移 FilesTreeView、FilesTreeItem、FilesTreeDisclosure、FilesTreeIcon、FilesTreeLabel Files tree 与 toolbar 交互 已迁移 FilesPane、FilesToolbar、FilesTreeItem::interactionnode SCM 根与深层组件树 已迁移 EditorPane::interactionnode、MultiDiffEditor、MultiDiffSection、DiffEditor 与 CodeEditor 通用列表/树的 frame 组合 已具备 ScrollView::drawcomponents、ListView::drawcomponents、TreeView::drawcomponents 浮层与 modal 组件组合 已具备 ContextView/ContextMenu/Dropdown::drawcomponents、ComponentContext::setmodalroot Native overlay 迁移 已完成 Session menu、workspace path picker、Git branch menu、keyboard shortcuts、settings page Native file editor 迁移 已完成 FileEditorPane 与独立 fileeditorpaneinteraction；TabList、SearchBar、Notice、Document、CodeEditor 均进入组合 inspector 标量动画与稳定 track 已具备 ScalarAnimation、AnimationKey、AnimationRegistry、AnimationAdvanceReport 宿主 deadline 聚合 已具备 FrameDeadlineSet；Native 的 abouttowait 只负责投影到事件循环 动画自动绑定组件属性 当前产品接入已完成 AnimationBinding、ComponentContext::bindscalar 和 AnimationRegistry 已连通；Language Server switch 与 SCM fold height 都由 retained runtime 持有 track 全 Native 迁移 主路径已完成，产品边界持续收敛 组件级 registerinteractions 与 split host 已清零；Shell 宿主组合使用 UiFrame/ComponentContext::withcomponent，后续只允许新增产品语义，不得回写 framework runtime Native split host 边界 ✅ 已完成 ShellPresentation 独占 UiFrame<InteractionFrame ；旧 split API 已删除 Retained fragment lifecycle 当前产品路径已闭合 RetainedRuntime 聚合 RetainedFragmentRegistry 与 AnimationRegistry；NativeApp 已登记 Shell fragment、消费 removed IDs 并恢复 scene/interaction checkpoint；现有产品 fragment 明确采用即时 unmount，未来若需要退出视觉必须显式声明 exit spec 分阶段迁移 阶段一：共享契约与兼容入口 目标是让新组件可以一次组合产生共享输出，同时不要求整个 Native shell 一次性重写。 - x 为 ComponentElement 和 InspectionNode 增加稳定身份连接； - x 增加 Component::interactionnode 和 Component::compose 默认契约； - x 增加 ComponentContext、UiFrame 和统一 frame composition 入口； - x 清零 split scene/interaction 调用并删除旧兼容入口； - x 将当前帧时钟传入组合上下文，保持动画原语不依赖平台 timer； - x 为 animation property key、deadline 聚合和组件失效范围补充通用契约测试。 阶段二：Files 纵向切片 - x FilesPane 根节点通过同一 ElementId 对齐 inspector 与 interaction； - x Tree row 使用真实组件组合，深入到 disclosure、icon、label； - x TreeView/ListView/ScrollView 支持 context 组合，并保留裁剪和 scrollbar paint； - x overscan 只服务 paint，交互节点限制在可见范围； - x 用测试验证身份、边界、label、祖先链和可见行数。 - x Files toolbar 与 sidebar navigation 通过共享组合生成 root、action bar 和按钮节点。 阶段三：SCM 纵向切片 - x EditorPane 拥有自身 interaction root； - x MultiDiffEditor 的 section、file header、fold control、scrollbar 改成真实子组件，并通过 ComponentContext::drawcomponent 生成共享 inspector/interaction 树； - x DiffEditor 继续组合真实 CodeEditor 子组件，SCM 不再手工拼接 UiNode 子节点； - x 将当前保留旧 fold 行为的 index-based identity 替换为 host-provided stable file identity，并为 diff section 的折叠动画定义 property key。 当前 SCM slice 已完成“结构迁移”与“跨列表重排的稳定身份”：EditorPaneState 按 changed-file path 分配 MultiDiffEditorItemIdentity，并在快照重排时复用对应的 DiffEditorState。section、header、diff body 和 fold control 都从同一 identity 派生；foldanimationkey 使用 section 的 AnimationProperty::Height。MultiDiffEditor::foldelementid 仍保留原有 item/region 编码，供未迁移的 standalone caller 兼容，但产品 SCM 路径不再使用它。SCM host 已将目标 section height 通过 ComponentContext::bindscalar 接入 retained AnimationBinding；由于高度会重新定位后续 section，动画使用 FrameInvalidation::Rebuild，而不是伪装成 fragment-local paint 更新。animation track 随 stable section identity 自动清理，不由 editor 组件保存动画对象。 阶段四：宿主收敛 - x Native ShellPresentation 以 UiFrame 作为内部唯一 frame owner； - x 将剩余 registerinteractions 调用迁移到 interactionnode/compose； - x 删除组件级重复注册路径和 drawcomponentwithinteraction split host 兼容边界； - x 将动画 deadline 聚合与 FrameScheduler 的最小失效等级接通，并提供 ComponentContext::bindscalar；产品属性按 vertical slice 逐步声明绑定。 阶段五：生命周期与动画完善 - x 为 retained fragment 的 mount/update/unmount 固定 state owner、exit deadline 和 cleanup 时机； - x 支持稳定 identity 下的标量属性动画、反向 retarget 和取消，并通过 AnimationBinding 让组件声明目标属性； - x 固定 terminal fragment 移除、interaction checkpoint 恢复和 inspector ghost node 禁止规则；Shell 已接入 removedids cleanup，当前无退出视觉规格的产品 fragment 明确直接 unmount； - x 为 lifecycle deadline 和 animation advance 增加 deterministic clock 测试，并覆盖 Shell/Files/SCM/overlay 的一帧结构回归。 验收不变量 后续任何组件迁移都必须满足： - 一个可交互组件的 UiNode.id 与其 ComputedElement.identity 相同； - interaction bounds、inspection bounds 和 paint 使用同一个 computed geometry，不复制计算公式； - inspector 只出现当前组合树中的节点；overscan、缓存和退出动画不能制造可交互幽灵节点； - 父链由组件组合上下文生成；跨宿主边界才允许组件显式指定 parent； - 动画推进不直接调用平台 API；deadline 由宿主调度，采样值由组件消费； - 产品状态、平台事件和副作用不进入 zui base/presentation 的通用契约； - 迁移期间兼容入口不得与新组合路径同时注册同一 ElementId。 测试与发布 每个 vertical slice 至少包含三类测试： - 结构契约：检查 inspector 与 interaction 的 identity、bounds、label 和祖先链； - 可见性契约：检查虚拟化、clip、overscan 不改变可交互范围； - 状态/时间契约：检查 selected/hovered/focus 和动画在显式时钟下的采样、失效和完成状态。 当前阶段的最小验证入口： `text cargo test -p zui --lib cargo test -p zeta-ui --lib cargo test -p zeta-keybinding --lib cargo test -p zeta-settings --lib cargo test -p zeta-agent-sidebar --lib cargo test --manifest-path Cargo.toml -p zeterm layoutinspector cargo test --manifest-path Cargo.toml -p zeterm fileeditorpane cargo test --manifest-path Cargo.toml -p zeterm shellscene ` Native 后续统一 UiFrame 前，先保持各 vertical slice 的 targeted tests 通过；任何一条路径产生重复 ElementId`、不同 bounds 或不同 parent，都视为迁移阻断问题。"
  },
  {
    "slug": "zeterm/native-deprecation-plan",
    "sourcePath": "zeterm/docs/native-deprecation-plan.md",
    "title": "Native UI 兼容边界与渐进式弃用计划",
    "description": "zeterm 产品宿主不会整体弃用；它现在由 zeterm/ 承载。要逐步弃用的是迁移前留下的 Native UI split scene/interaction boundary，以及重复定义通用 UI runtime 的兼容入口。通用机制归 zui，可复用 组件归 zeta-ui 或对应领域 crate，zeterm 只保留产品适配。",
    "group": "zeterm 产品",
    "markdown": "> 状态：当前迁移计划。`zeterm` 产品宿主已经迁移到 `zeterm/`；本文只拥有 Native UI split boundary\n> 与兼容 API 的弃用阶段和删除条件；`zui`\n> 的具体 API 契约见 [`zeterm/zui/README.md`](../zui/README.md)，zeterm 的源码路径和\n> 产品接入义务见 [`zeterm/README.md`](../README.md)。完整产品根迁移见\n> [`zeterm-app-migration-plan.md`](zeterm-app-migration-plan.md)。\n\n## 快速理解\n\n`zeterm` 产品宿主不会整体弃用；它现在由 `zeterm/` 承载。要逐步弃用的是迁移前留下的 Native UI\nsplit scene/interaction boundary，以及重复定义通用 UI runtime 的兼容入口。通用机制归 `zui`，可复用\n组件归 `zeta-ui` 或对应领域 crate，zeterm 只保留产品适配。\n\n| 读者关心的对象 | 当前 canonical owner | Native 状态 | 下一步 |\n| --- | --- | --- | --- |\n| Element、layout、ComputedElement、scene、inspection | `zui` | 委托 | 禁止在 Native 复制几何或检查树 |\n| interaction、focus、capture、失效等级 | `zui` | 委托 | 产品事件只映射为 `UiIntent` 和失效请求 |\n| animation、deadline、retained fragment lifecycle | `zui` | 委托；Native 已持有 `RetainedRuntime` 并接入 Shell cleanup | 新产品 fragment 必须显式选择即时 unmount 或 exit spec；禁止 Native 自建 runtime |\n| Button、Tree、List、Diff 等通用组件 | `zeta-ui` / 领域 crate | 委托 | Native 只提供状态投影和 action adapter |\n| 窗口、平台事件、App Server、文件/Git/Session 状态 | `zeterm/zeterm` | ✅ 保留 | 不迁入 `zui` |\n| Shell/Composer/Inspector 的产品组合 | `zeterm/zeterm` | ✅ 保留 | 通过 `UiFrame` 组合 |\n| `ShellPresentation` 的 frame ownership | `zeterm/zeterm` | ✅ 已完成 | 继续保持单一 `UiFrame<InteractionFrame>` owner |\n| `UiScene::draw_component_with_interaction`、`UiFrame::parts_mut`、`UiFrame::into_parts` | `zui` 旧兼容 API | ✅ 已删除 | 禁止重新引入平行输出入口 |\n\n这里的弃用阶段已经结束：旧 split composition API 不再可调用。本文保留它作为迁移记录；以后如果\n出现同名 API、平行 `UiScene`/`InteractionFrame` 字段或第二个 frame owner，应视为架构回退，而不是\n兼容需求。\n\n## 边界原则\n\n- `zui` 拥有后端无关的 frame、layout、paint、inspection、interaction、animation、deadline 和 retained\n  lifecycle 契约；这些能力不能因为 Native 接入方便而在 Native 再实现一份。\n- `zeta-ui` 和领域 crate 拥有可复用组件的内部几何、状态投影和交互节点；Workbench Part 只组合它们，\n  不用深层选择器或重复 hit-test 覆盖组件内部规则。\n- Native 拥有产品状态、平台事件适配、App Server/文件/Git/Session 映射、command 执行和具体产品\n  Part/Overlay 的组合。它可以保存可丢弃的 presentation state，但不拥有通用 UI runtime。\n- renderer 只消费 `UiScene`；它不接触 interaction、accessibility、产品 command 或 retained owner。\n- `zui` 的兼容入口只兼容输出形状，不兼容第二套组件组合、检查树或交互注册路径。\n\n## 当前迁移状态\n\n通用能力已经收回 `zui`：`ComponentContext`/`UiFrame` 统一组件组合，`FrameScheduler` 统一失效，\n`AnimationRegistry` 和 `RetainedRuntime` 统一时间与跨帧生命周期。Files、SCM、浮层、编辑器和大多数\nzeterm overlay 已经通过共享组合生成 inspection 与 interaction。\n\n`ShellPresentation` 已把 `UiScene` 与 `InteractionFrame` 收拢到同一个 `UiFrame<InteractionFrame>`；\naccessibility projection 仍是宿主消费的语义快照，不是第二个 frame owner。组件组合统一通过\n`UiFrame::draw_component`、`ComponentContext::draw_component` 或 `ComponentContext::with_component` 完成，旧\nsplit composition adapter 已删除。`RetainedRuntime` 的 fragment removed-ID cleanup 和 `AnimationBinding` 已接入\nShell；当前 Language Server switch 使用即时 unmount，SCM fold height 使用 retained scalar + full rebuild，迁移\n主路径没有遗留的 framework 兼容入口。未来新增退出动画或产品属性时，必须沿同一 zui contract 接入，不得通过恢复旧 API 解决。\n\n## 分阶段发布\n\n### 阶段一：建立迁移信号（已完成）\n\n- 将 split scene/interaction 入口迁移到 `UiFrame`，清零调用后删除旧入口；\n- 在 zeterm README 和迁移计划中明确：`zeterm` 是产品宿主，不是通用 UI framework；\n- 继续禁止组件级 `register_interactions`、Native 自有 animation timer 和重复 layout/inspection 树；\n- 保持现有 targeted tests 通过，弃用告警本身不作为错误。\n\n### 阶段二：统一 Shell frame 所有者（已完成）\n\n- 让 `ShellPresentation` 内部持有 `UiFrame<InteractionFrame>`，并由它独占 frame owner；\n- overlay、inspector 和 retained fragment rebuild 都从同一个 frame owner 派生 scene、interaction 和\n  semantics snapshot；\n- renderer 继续只接收 `UiScene` 投影，避免把 GPU 边界扩大；\n- 清零 `zeterm` 生产代码中的 `draw_component_with_interaction` 调用并删除 API。\n\n### 阶段三：统一 fragment cleanup（已完成框架接线）\n\n- [x] 消费 `RetainedRuntimeAdvanceReport::fragment().removed_ids` 时，在同一个 cleanup 路径移除 scene fragment、\n  interaction checkpoint 和 semantics；\n- [x] 让 exit animation 只保留仍在 retained presentation 中的节点，禁止 inspector 或 hit-test ghost node；\n- [x] 将 cleanup、deadline 和 redraw invalidation 固定为 deterministic clock 测试；当前产品 fragment 已明确即时\n  unmount，未来启用 exit retention 时必须同时提交产品状态与动画规格。\n\n### 阶段四：删除兼容入口（已完成）\n\n只有以下条件同时满足，才删除 deprecated API 和 split host adapter：\n\n- Native 与领域组件生产代码不再调用 `draw_component_with_interaction`，旧 API 已删除；\n- `UiFrame::parts_mut` 和 `into_parts` 已删除；\n- Shell 的 scene、interaction、inspection/accessibility snapshot 来自同一帧组合；\n- shell、overlay、Files、SCM、editor 和 retained lifecycle targeted tests 已覆盖共享身份、bounds、parent、\n  可见范围和时间推进；\n- `zui` README、zeterm README、渲染架构文档和本计划已同步到当前 owner。\n\n## 识别架构漂移\n\n以下改动应被视为 Native 继续拥有 framework 机制的信号，并在 review 中阻断或要求迁移说明：\n\n- 在 Native 新增通用 `Rect`/layout 计算、inspection metadata 或基于 paint primitive 反推控件树；\n- 在 Native 为 hover、focus、动画、fragment exit 或 deadline 安装独立 timer/registry；\n- 一个组件同时拥有 `compose`/`interaction_node` 和手写第二次 interaction registration；\n- 为了适配一个产品 Part，给 `zui` 增加产品 identity、文件/Git/Session 类型或反向依赖；\n- 用 host-specific deep selector 覆盖 `zeta-ui` 组件内部的 selected、hover、active、focus 或 disabled 样式。\n\n这些规则不要求 Native 变薄到没有产品逻辑；它们要求“产品决定什么”和“框架如何表达/调度”保持可定位的\n边界。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "边界原则",
        "title": "边界原则"
      },
      {
        "depth": 2,
        "id": "当前迁移状态",
        "title": "当前迁移状态"
      },
      {
        "depth": 2,
        "id": "分阶段发布",
        "title": "分阶段发布"
      },
      {
        "depth": 3,
        "id": "阶段一建立迁移信号已完成",
        "title": "阶段一：建立迁移信号（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段二统一-shell-frame-所有者已完成",
        "title": "阶段二：统一 Shell frame 所有者（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段三统一-fragment-cleanup已完成框架接线",
        "title": "阶段三：统一 fragment cleanup（已完成框架接线）"
      },
      {
        "depth": 3,
        "id": "阶段四删除兼容入口已完成",
        "title": "阶段四：删除兼容入口（已完成）"
      },
      {
        "depth": 2,
        "id": "识别架构漂移",
        "title": "识别架构漂移"
      }
    ],
    "searchText": " 状态：当前迁移计划。zeterm 产品宿主已经迁移到 zeterm/；本文只拥有 Native UI split boundary 与兼容 API 的弃用阶段和删除条件；zui 的具体 API 契约见 zeterm/zui/README.md，zeterm 的源码路径和 产品接入义务见 zeterm/README.md。完整产品根迁移见 zeterm-app-migration-plan.md。 快速理解 zeterm 产品宿主不会整体弃用；它现在由 zeterm/ 承载。要逐步弃用的是迁移前留下的 Native UI split scene/interaction boundary，以及重复定义通用 UI runtime 的兼容入口。通用机制归 zui，可复用 组件归 zeta-ui 或对应领域 crate，zeterm 只保留产品适配。 读者关心的对象 当前 canonical owner Native 状态 下一步 --- --- --- --- Element、layout、ComputedElement、scene、inspection zui 委托 禁止在 Native 复制几何或检查树 interaction、focus、capture、失效等级 zui 委托 产品事件只映射为 UiIntent 和失效请求 animation、deadline、retained fragment lifecycle zui 委托；Native 已持有 RetainedRuntime 并接入 Shell cleanup 新产品 fragment 必须显式选择即时 unmount 或 exit spec；禁止 Native 自建 runtime Button、Tree、List、Diff 等通用组件 zeta-ui / 领域 crate 委托 Native 只提供状态投影和 action adapter 窗口、平台事件、App Server、文件/Git/Session 状态 zeterm/zeterm ✅ 保留 不迁入 zui Shell/Composer/Inspector 的产品组合 zeterm/zeterm ✅ 保留 通过 UiFrame 组合 ShellPresentation 的 frame ownership zeterm/zeterm ✅ 已完成 继续保持单一 UiFrame<InteractionFrame owner UiScene::drawcomponentwithinteraction、UiFrame::partsmut、UiFrame::intoparts zui 旧兼容 API ✅ 已删除 禁止重新引入平行输出入口 这里的弃用阶段已经结束：旧 split composition API 不再可调用。本文保留它作为迁移记录；以后如果 出现同名 API、平行 UiScene/InteractionFrame 字段或第二个 frame owner，应视为架构回退，而不是 兼容需求。 边界原则 - zui 拥有后端无关的 frame、layout、paint、inspection、interaction、animation、deadline 和 retained lifecycle 契约；这些能力不能因为 Native 接入方便而在 Native 再实现一份。 - zeta-ui 和领域 crate 拥有可复用组件的内部几何、状态投影和交互节点；Workbench Part 只组合它们， 不用深层选择器或重复 hit-test 覆盖组件内部规则。 - Native 拥有产品状态、平台事件适配、App Server/文件/Git/Session 映射、command 执行和具体产品 Part/Overlay 的组合。它可以保存可丢弃的 presentation state，但不拥有通用 UI runtime。 - renderer 只消费 UiScene；它不接触 interaction、accessibility、产品 command 或 retained owner。 - zui 的兼容入口只兼容输出形状，不兼容第二套组件组合、检查树或交互注册路径。 当前迁移状态 通用能力已经收回 zui：ComponentContext/UiFrame 统一组件组合，FrameScheduler 统一失效， AnimationRegistry 和 RetainedRuntime 统一时间与跨帧生命周期。Files、SCM、浮层、编辑器和大多数 zeterm overlay 已经通过共享组合生成 inspection 与 interaction。 ShellPresentation 已把 UiScene 与 InteractionFrame 收拢到同一个 UiFrame<InteractionFrame ； accessibility projection 仍是宿主消费的语义快照，不是第二个 frame owner。组件组合统一通过 UiFrame::drawcomponent、ComponentContext::drawcomponent 或 ComponentContext::withcomponent 完成，旧 split composition adapter 已删除。RetainedRuntime 的 fragment removed-ID cleanup 和 AnimationBinding 已接入 Shell；当前 Language Server switch 使用即时 unmount，SCM fold height 使用 retained scalar + full rebuild，迁移 主路径没有遗留的 framework 兼容入口。未来新增退出动画或产品属性时，必须沿同一 zui contract 接入，不得通过恢复旧 API 解决。 分阶段发布 阶段一：建立迁移信号（已完成） - 将 split scene/interaction 入口迁移到 UiFrame，清零调用后删除旧入口； - 在 zeterm README 和迁移计划中明确：zeterm 是产品宿主，不是通用 UI framework； - 继续禁止组件级 registerinteractions、Native 自有 animation timer 和重复 layout/inspection 树； - 保持现有 targeted tests 通过，弃用告警本身不作为错误。 阶段二：统一 Shell frame 所有者（已完成） - 让 ShellPresentation 内部持有 UiFrame<InteractionFrame ，并由它独占 frame owner； - overlay、inspector 和 retained fragment rebuild 都从同一个 frame owner 派生 scene、interaction 和 semantics snapshot； - renderer 继续只接收 UiScene 投影，避免把 GPU 边界扩大； - 清零 zeterm 生产代码中的 drawcomponentwithinteraction 调用并删除 API。 阶段三：统一 fragment cleanup（已完成框架接线） - x 消费 RetainedRuntimeAdvanceReport::fragment .removedids 时，在同一个 cleanup 路径移除 scene fragment、 interaction checkpoint 和 semantics； - x 让 exit animation 只保留仍在 retained presentation 中的节点，禁止 inspector 或 hit-test ghost node； - x 将 cleanup、deadline 和 redraw invalidation 固定为 deterministic clock 测试；当前产品 fragment 已明确即时 unmount，未来启用 exit retention 时必须同时提交产品状态与动画规格。 阶段四：删除兼容入口（已完成） 只有以下条件同时满足，才删除 deprecated API 和 split host adapter： - Native 与领域组件生产代码不再调用 drawcomponentwithinteraction，旧 API 已删除； - UiFrame::partsmut 和 intoparts 已删除； - Shell 的 scene、interaction、inspection/accessibility snapshot 来自同一帧组合； - shell、overlay、Files、SCM、editor 和 retained lifecycle targeted tests 已覆盖共享身份、bounds、parent、 可见范围和时间推进； - zui README、zeterm README、渲染架构文档和本计划已同步到当前 owner。 识别架构漂移 以下改动应被视为 Native 继续拥有 framework 机制的信号，并在 review 中阻断或要求迁移说明： - 在 Native 新增通用 Rect/layout 计算、inspection metadata 或基于 paint primitive 反推控件树； - 在 Native 为 hover、focus、动画、fragment exit 或 deadline 安装独立 timer/registry； - 一个组件同时拥有 compose/interactionnode 和手写第二次 interaction registration； - 为了适配一个产品 Part，给 zui 增加产品 identity、文件/Git/Session 类型或反向依赖； - 用 host-specific deep selector 覆盖 zeta-ui 组件内部的 selected、hover、active、focus 或 disabled 样式。 这些规则不要求 Native 变薄到没有产品逻辑；它们要求“产品决定什么”和“框架如何表达/调度”保持可定位的 边界。"
  },
  {
    "slug": "zeterm/zeterm-app-migration-plan",
    "sourcePath": "zeterm/docs/zeterm-app-migration-plan.md",
    "title": "zeterm 应用根迁移计划",
    "description": "zeta-rs 作为 desktop 和 zeterm 共用的 Rust backend；zeterm/ 作为 zeterm 的独立产品宿主； desktop/ 继续作为 Electron 宿主。当前 zeta-rs/native 把产品宿主、Native UI framework 和 GPU 接线放在共享 Rust workspace 中，边界已经不再清晰。本计划先把宿…",
    "group": "zeterm 产品",
    "markdown": "> 状态：当前迁移计划。本文拥有 `zeterm` 从 `zeta-rs/native` 迁移到仓库根 `zeterm/` 的阶段、边界和验收条件；\n> 共享 Rust backend 的 crate contract 由对应 `zeta-rs` README 维护，Native UI framework 的迁移细节由\n> [`ui-component-migration-plan.md`](ui-component-migration-plan.md) 维护。\n\n## 快速理解\n\n`zeta-rs` 作为 `desktop` 和 `zeterm` 共用的 Rust backend；`zeterm/` 作为 `zeterm` 的独立产品宿主；\n`desktop/` 继续作为 Electron 宿主。当前 `zeta-rs/native` 把产品宿主、Native UI framework 和 GPU\n接线放在共享 Rust workspace 中，边界已经不再清晰。本计划先把宿主整体迁到 `zeterm/`，再把只服务\nNative UI 的 crate 从共享 workspace 中分离。\n\n| 读者关心的对象 | 当前路径 | 目标 owner | 迁移状态 |\n| --- | --- | --- | --- |\n| `zeterm` binary、窗口生命周期、平台事件 | `zeta-rs/native` | `zeterm/` | 阶段一迁移 |\n| `zeterm` Root/Shell/Workspace 产品布局 | `zeta-rs/native/src` | `zeterm/layout` + `zeterm/composer` + `zeterm/src` | Root/Workspace 与 Composer panel/list geometry 已抽取，Shell/Composer state/scene composition 仍在宿主 |\n| 通用 icon asset contract | 旧 Native icon types | `zeterm/icon` (`zeta-icon`) | 已完成；产品 catalog 保留在 `zeterm/icons` |\n| Element、Scene、Interaction、Animation、Retained Runtime | `zeterm/zui` | zeterm-owned crates in root workspace | 已迁入 zeterm |\n| Button、Tree、List、Editor/Sidebar presentation | `zeterm/ui`、`editor`、`agent-sidebar` | zeterm-owned crates in root workspace | 已迁入 zeterm |\n| Renderer、wgpu、winit | `zeterm/renderer`、`wgpu`、`winit` | zeterm-owned crates in root workspace | 已迁入 zeterm |\n| App Server、Core、Protocol、Session、File/Git、Diff、Terminal model | `zeta-rs/*` | `zeta-rs` | 保留 |\n| 纯 Rust editor transaction、syntax、language service | `zeta-rs/editor-core`、`syntax`、`language-service` | `zeta-rs` | 保留；presentation 与底层分离 |\n\n`zeta-rs` 的“共享”按宿主无关的 Rust 语义和 backend contract 判断，不要求 Electron TypeScript 直接\n链接 Rust crate；Electron 通过 App Server protocol 使用同一套 authority。只有产品布局、窗口、GPU、\nNative interaction composition 和平台生命周期不能继续进入 `zeta-rs` shared layer。\n\n## 目标结构\n\n迁移完成后的物理结构为：\n\n```text\nzeta/\n├── zeterm/\n│   ├── Cargo.toml              # zeterm application package\n│   ├── README.md\n│   ├── src/                    # zeterm product host and composition root\n│   ├── zui/                    # backend-neutral native UI framework crate\n│   ├── icon/                   # renderer-independent icon asset contract\n│   ├── layout/                 # zeterm product pane topology\n│   ├── composer/               # zeterm Composer panel/list geometry\n│   ├── zui-demo/               # product-independent framework smoke host\n│   ├── ui/                     # native-only reusable components\n│   ├── renderer/               # native rendering crate\n│   ├── wgpu/                   # native GPU backend crate\n│   ├── winit/                  # native platform adapter crate\n│   └── ...                     # other native-only presentation crates\n├── zeta-rs/\n│   ├── ...                     # shared Rust backend crates\n│   ├── core/\n│   ├── app-server*/\n│   ├── protocol/\n│   ├── editor-core/\n│   ├── terminal/\n│   └── ...\n└── desktop/                    # Electron product host\n```\n\n依赖方向固定为：\n\n```text\ndesktop ───────→ zeta-rs protocol/App Server\nzeterm/zeterm ────→ zeta-rs backend/domain\n             └─→ Native UI workspace\nzeta-rs ───────→ no zeterm/desktop product host\n```\n\n## 阶段与当前状态\n\n### 阶段零：冻结旧 Native（已完成）\n\n- [x] 在 `AGENTS.md` 中禁止向 `zeta-rs/native` 新增能力；\n- [x] 将 Native split scene/interaction host API 迁移到 `UiFrame`，并删除旧兼容入口；\n- [x] 明确 `zui`、`zeta-ui`、renderer 和生命周期机制的长期 owner；\n- [x] 迁移期间将 `zeta-rs/native` 作为只读迁移源，期间不再把它当作新功能落点；阶段一完成后已删除。\n\n### 阶段一：迁移 `zeterm` 产品宿主（已完成）\n\n- [x] 在仓库根建立 `zeterm/` 产品目录，并以 `zeterm` 作为发布 binary；\n- [x] 将当前 `zeta-rs/native` 的产品宿主、测试和 README 整体迁移到 `zeterm/`，保留现有模块边界和行为；\n- [x] 将 zeterm 的依赖改为明确的 `zeta-rs` path dependency，不让 `zeta-rs` 反向知道 zeterm；\n- [x] 从 shared crate 集合移除 `native` member 和产品入口；\n- [x] 更新 `just zeterm`、package/build 入口和产品线文档；\n- [x] 阶段完成后删除旧 `zeta-rs/native` 路径，不保留两个可运行宿主。\n\n阶段一先解决产品宿主和共享 backend 的边界；阶段二继续把 Native-only crate 物理移动到\n`zeterm/` 的直接子目录。现在由仓库根 Cargo workspace 统一解析所有 crate，zeterm 仍作为独立 package 和发布\n边界存在；Bazel 使用同一个 root graph。\n\n### 阶段二：分离 Native UI workspace（核心迁移已完成）\n\n- [x] 将 `zui`、`zeta-ui`、`zeta-renderer`、`zeta-wgpu`、`zeta-winit` 迁入 `zeterm/` 的直接子 crate；\n- [x] 保留 `zeta-editor-core`、`zeta-syntax` 等纯 Rust core 在 `zeta-rs`，将 `zeta-editor` presentation 迁入 zeterm；\n- [x] 将 `zeta-agent-sidebar`、Native settings UI 和 Native keybinding UI 迁入 zeterm-side crates；\n- [x] 将 Markdown presentation crate 迁入 zeterm-side crates，Theme manifest/resolver 保留在 shared backend；\n- [x] 保证 `zeta-rs` backend crates 不再直接依赖 Native UI crate。\n\n当前根 `Cargo.toml` 是唯一 workspace，`zeterm/Cargo.toml` 是 `zeterm` package。`zeterm/BUILD.bazel` 已\n提供 `//zeterm:zeterm`、`//zeterm:zeterm_sources`、`//zeterm:zeterm_release_inputs`、\n`//zeterm:zeterm_package_contract` 和 `//zeterm:zeterm_ci`；所有 zeterm/shared crates 通过单一 `@crates`\nhub 消费 rules_rs 生成的 package deps。`bazel build //zeterm:zeterm` 已在当前 macOS toolchain 通过；\n此前由 apple-cf Swift bridge 引起的限制已随 zui 的字体目录实现迁移而消除。\n\n### 阶段三：收敛应用组合根\n\n- [x] `zeterm/src` 只保留窗口/平台适配、产品状态投影、command 执行和具体 Part/Overlay 组合；\n- [x] `ShellPresentation` 使用单一 `zui::UiFrame` owner；\n- [x] 清零 split scene/interaction API 的生产调用并删除旧入口；\n- [x] retained fragment cleanup、animation deadline 和 redraw invalidation 全部从 `zui::RetainedRuntime`/\n      `AnimationRegistry` 派生；具体产品 fragment 是否采用 exit retention 仍由产品状态决定；\n- [x] 删除剩余 `zeta-native` 命名、旧 build target 和旧文档引用；迁移历史中的旧名称只保留在迁移历史、\n      AGENTS 约束和边界检查器中。\n\n### 阶段四：独立产品发布\n\n- [x] `zeterm` 能独立构建、测试，并通过 package staging 生成带 binary digest 的 unsigned `zeterm` artifact；\n- [x] 为 `zeterm` 建立发布输入和 boundary CI graph；`//zeterm:zeterm_ci` 验证 workspace ownership，\n      `//zeterm:zeterm_release_inputs` 固定 package/signing contract；Cargo workspace 仍是唯一 canonical\n      Rust build graph；\n- [x] 建立 `zeterm/` 与 `zeta-rs` shared crates 的 metadata-derived hermetic Bazel Rust graph；\n      单一 root workspace 已消除跨 workspace path dependency bridge；\n- [x] 在 CI 入口中通过完整 `//zeterm:zeterm` build 验证平台 toolchain；本机已通过同一 target，\n      `//zeterm:zeterm_ci` 继续验证 boundary/package/signing contract。\n- [x] 接入 provider-neutral 的 platform signing/verification job；`scripts/release_zeterm_package.sh`\n      负责 Build → Stage → Sign → Verify，平台 CI 只需绑定 native tool 和 secret 环境；只发布\n      `verified` artifact；\n- [x] `desktop` 构建不拉入 Native UI/GPU 依赖；desktop 侧只保留自身的 platform adapter，边界检查未发现\n      对 zeterm UI/GPU crate 的依赖。\n- [ ] `zeta-rs` backend 可独立测试、发布和被多个宿主复用；\n- [ ] App Server protocol、theme manifest 和 shared domain revision 具备明确兼容策略；\n- [x] packaging、CI、开发命令和文档只引用新的 product roots。\n\n### 阶段五：通用框架可移植性（当前演进）\n\n- [x] 将 `IconId`、SVG definition 和 rendering mode 下沉到独立的 `zeta-icon` contract；`zeta-icons`\n      只保留可选的 zeterm product catalog，`zui`/`zeta-ui` 不再依赖该 catalog；\n- [x] 将 Root/Inspector 和 Terminal Workspace 的 pane topology 抽到 `zeta-layout`，Native 只投影\n      `AgentSidebarState` 为 `SidebarLayoutSpec`；\n- [x] 建立 `zui-demo`，只依赖 `zui`、`zeta-ui` 和 `zeta-renderer`，以 recording backend 验证通用\n      组件可脱离 zeterm product host 组合；\n- [x] 将 Composer panel、interaction list 与 selection scroll geometry 抽到 `zeta-composer`；Native\n      只投影 item count、preferred height 与 scene/state adapter；\n- [ ] 将更多 Shell/Composer/Session domain composition 按 owner 分批抽到产品领域 crate；不把产品\n      state、command 或平台事件下沉到 `zui`；\n- [ ] 在拥有第二个真实宿主前，不拆分 `zui` 的内部 foundation/layout/presentation/runtime 为更多\n      独立 crate；先保持单一 framework contract，降低跨 crate API churn。\n\n### 当前审计结论（2026-08-03）\n\n物理迁移已经完成：旧 Native UI 目录、旧 workspace、旧产品 build target 和 shared backend 到 UI 的\n反向依赖均已清除；根 Cargo workspace、Bazel target graph、`zeterm` binary build、targeted tests 和\nboundary CI 均已通过。\n\n剩余两个未勾选项是产品演进工作，不是迁移残留：一是为 shared backend 建立独立发布 artifact/CI\n流程，二是为 App Server、theme manifest 和 shared domain revision 定义跨版本兼容策略。它们可以在\n当前迁移后的边界上继续建设，不应再把 UI 从 `zeta-rs` 搬迁作为前置条件。\n\n## 迁移约束\n\n- 不复制 `zeta-rs/native` 形成第二个并行宿主；迁移必须保持单一运行入口。\n- 不在旧 Native 中修建新功能；若迁移过程中发现缺少通用能力，先在正确的下层 owner 实现，再由\n  zeterm 做最小接线。\n- 不把 `zeterm/editor`、`zeterm/agent-sidebar` 等 presentation crate 直接误判为 shared backend；\n  先按“headless model/core”和“Native presentation”拆分。\n- 不让 `zeterm` 类型、产品命令、窗口事件或布局类型进入 `zeta-rs`。\n- 每个迁移阶段都必须保留 deterministic unit tests 和至少一个产品 targeted test；测试失败时先区分\n  迁移回归与工作区已有的无关 dirty change。\n\n## 验收入口\n\n阶段一完成后至少通过：\n\n```text\ncargo check --manifest-path Cargo.toml --workspace\ncargo check --manifest-path Cargo.toml -p zeterm\ncargo test --manifest-path Cargo.toml -p zeterm\ncargo test --manifest-path Cargo.toml --workspace\njust --dry-run zeterm\n```\n\n`just --dry-run zeterm` 只验证入口已经指向根 workspace 的 `zeterm` package；真正启动窗口时使用\n`just zeterm`。根 workspace 的 zeterm package 与 shared backend 已通过 workspace check 和 targeted\ncheck/test 验证；Bazel graph 的完整入口是 `bazel build //zeterm:zeterm`，发布 contract 入口是\n`bazel test //zeterm:zeterm_ci`，package staging 使用 `just zeterm-package`，\n签名发布流程使用 `scripts/release_zeterm_package.sh`，发布细节见\n[`zeterm-release-graph.md`](zeterm-release-graph.md)。\n\n并满足以下结构检查：\n\n- `rg 'zeta-rs/native|zeta-native'` 只剩迁移历史、兼容说明或明确的旧版本记录；\n- 根 `Cargo.toml` 是唯一 Cargo workspace；`zeterm/Cargo.toml` 不声明 nested workspace；\n- `zeterm` 只向下依赖 `zeta-rs` shared crates 与 zeterm-owned UI crates；\n- 任何 shared crate 都不依赖 `zeterm`、窗口实例、GPU handle 或产品 layout。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "目标结构",
        "title": "目标结构"
      },
      {
        "depth": 2,
        "id": "阶段与当前状态",
        "title": "阶段与当前状态"
      },
      {
        "depth": 3,
        "id": "阶段零冻结旧-native已完成",
        "title": "阶段零：冻结旧 Native（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段一迁移-zeterm-产品宿主已完成",
        "title": "阶段一：迁移 zeterm 产品宿主（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段二分离-native-ui-workspace核心迁移已完成",
        "title": "阶段二：分离 Native UI workspace（核心迁移已完成）"
      },
      {
        "depth": 3,
        "id": "阶段三收敛应用组合根",
        "title": "阶段三：收敛应用组合根"
      },
      {
        "depth": 3,
        "id": "阶段四独立产品发布",
        "title": "阶段四：独立产品发布"
      },
      {
        "depth": 3,
        "id": "阶段五通用框架可移植性当前演进",
        "title": "阶段五：通用框架可移植性（当前演进）"
      },
      {
        "depth": 3,
        "id": "当前审计结论2026-08-03",
        "title": "当前审计结论（2026-08-03）"
      },
      {
        "depth": 2,
        "id": "迁移约束",
        "title": "迁移约束"
      },
      {
        "depth": 2,
        "id": "验收入口",
        "title": "验收入口"
      }
    ],
    "searchText": " 状态：当前迁移计划。本文拥有 zeterm 从 zeta-rs/native 迁移到仓库根 zeterm/ 的阶段、边界和验收条件； 共享 Rust backend 的 crate contract 由对应 zeta-rs README 维护，Native UI framework 的迁移细节由 ui-component-migration-plan.md 维护。 快速理解 zeta-rs 作为 desktop 和 zeterm 共用的 Rust backend；zeterm/ 作为 zeterm 的独立产品宿主； desktop/ 继续作为 Electron 宿主。当前 zeta-rs/native 把产品宿主、Native UI framework 和 GPU 接线放在共享 Rust workspace 中，边界已经不再清晰。本计划先把宿主整体迁到 zeterm/，再把只服务 Native UI 的 crate 从共享 workspace 中分离。 读者关心的对象 当前路径 目标 owner 迁移状态 --- --- --- --- zeterm binary、窗口生命周期、平台事件 zeta-rs/native zeterm/ 阶段一迁移 zeterm Root/Shell/Workspace 产品布局 zeta-rs/native/src zeterm/layout + zeterm/composer + zeterm/src Root/Workspace 与 Composer panel/list geometry 已抽取，Shell/Composer state/scene composition 仍在宿主 通用 icon asset contract 旧 Native icon types zeterm/icon zeta-icon 已完成；产品 catalog 保留在 zeterm/icons Element、Scene、Interaction、Animation、Retained Runtime zeterm/zui zeterm-owned crates in root workspace 已迁入 zeterm Button、Tree、List、Editor/Sidebar presentation zeterm/ui、editor、agent-sidebar zeterm-owned crates in root workspace 已迁入 zeterm Renderer、wgpu、winit zeterm/renderer、wgpu、winit zeterm-owned crates in root workspace 已迁入 zeterm App Server、Core、Protocol、Session、File/Git、Diff、Terminal model zeta-rs/ zeta-rs 保留 纯 Rust editor transaction、syntax、language service zeta-rs/editor-core、syntax、language-service zeta-rs 保留；presentation 与底层分离 zeta-rs 的“共享”按宿主无关的 Rust 语义和 backend contract 判断，不要求 Electron TypeScript 直接 链接 Rust crate；Electron 通过 App Server protocol 使用同一套 authority。只有产品布局、窗口、GPU、 Native interaction composition 和平台生命周期不能继续进入 zeta-rs shared layer。 目标结构 迁移完成后的物理结构为： ``text zeta/ ├── zeterm/ │ ├── Cargo.toml zeterm application package │ ├── README.md │ ├── src/ zeterm product host and composition root │ ├── zui/ backend-neutral native UI framework crate │ ├── icon/ renderer-independent icon asset contract │ ├── layout/ zeterm product pane topology │ ├── composer/ zeterm Composer panel/list geometry │ ├── zui-demo/ product-independent framework smoke host │ ├── ui/ native-only reusable components │ ├── renderer/ native rendering crate │ ├── wgpu/ native GPU backend crate │ ├── winit/ native platform adapter crate │ └── ... other native-only presentation crates ├── zeta-rs/ │ ├── ... shared Rust backend crates │ ├── core/ │ ├── app-server/ │ ├── protocol/ │ ├── editor-core/ │ ├── terminal/ │ └── ... └── desktop/ Electron product host ` 依赖方向固定为： `text desktop ───────→ zeta-rs protocol/App Server zeterm/zeterm ────→ zeta-rs backend/domain └─→ Native UI workspace zeta-rs ───────→ no zeterm/desktop product host ` 阶段与当前状态 阶段零：冻结旧 Native（已完成） - x 在 AGENTS.md 中禁止向 zeta-rs/native 新增能力； - x 将 Native split scene/interaction host API 迁移到 UiFrame，并删除旧兼容入口； - x 明确 zui、zeta-ui、renderer 和生命周期机制的长期 owner； - x 迁移期间将 zeta-rs/native 作为只读迁移源，期间不再把它当作新功能落点；阶段一完成后已删除。 阶段一：迁移 zeterm 产品宿主（已完成） - x 在仓库根建立 zeterm/ 产品目录，并以 zeterm 作为发布 binary； - x 将当前 zeta-rs/native 的产品宿主、测试和 README 整体迁移到 zeterm/，保留现有模块边界和行为； - x 将 zeterm 的依赖改为明确的 zeta-rs path dependency，不让 zeta-rs 反向知道 zeterm； - x 从 shared crate 集合移除 native member 和产品入口； - x 更新 just zeterm、package/build 入口和产品线文档； - x 阶段完成后删除旧 zeta-rs/native 路径，不保留两个可运行宿主。 阶段一先解决产品宿主和共享 backend 的边界；阶段二继续把 Native-only crate 物理移动到 zeterm/ 的直接子目录。现在由仓库根 Cargo workspace 统一解析所有 crate，zeterm 仍作为独立 package 和发布 边界存在；Bazel 使用同一个 root graph。 阶段二：分离 Native UI workspace（核心迁移已完成） - x 将 zui、zeta-ui、zeta-renderer、zeta-wgpu、zeta-winit 迁入 zeterm/ 的直接子 crate； - x 保留 zeta-editor-core、zeta-syntax 等纯 Rust core 在 zeta-rs，将 zeta-editor presentation 迁入 zeterm； - x 将 zeta-agent-sidebar、Native settings UI 和 Native keybinding UI 迁入 zeterm-side crates； - x 将 Markdown presentation crate 迁入 zeterm-side crates，Theme manifest/resolver 保留在 shared backend； - x 保证 zeta-rs backend crates 不再直接依赖 Native UI crate。 当前根 Cargo.toml 是唯一 workspace，zeterm/Cargo.toml 是 zeterm package。zeterm/BUILD.bazel 已 提供 //zeterm:zeterm、//zeterm:zetermsources、//zeterm:zetermreleaseinputs、 //zeterm:zetermpackagecontract 和 //zeterm:zetermci；所有 zeterm/shared crates 通过单一 @crates hub 消费 rulesrs 生成的 package deps。bazel build //zeterm:zeterm 已在当前 macOS toolchain 通过； 此前由 apple-cf Swift bridge 引起的限制已随 zui 的字体目录实现迁移而消除。 阶段三：收敛应用组合根 - x zeterm/src 只保留窗口/平台适配、产品状态投影、command 执行和具体 Part/Overlay 组合； - x ShellPresentation 使用单一 zui::UiFrame owner； - x 清零 split scene/interaction API 的生产调用并删除旧入口； - x retained fragment cleanup、animation deadline 和 redraw invalidation 全部从 zui::RetainedRuntime/ AnimationRegistry 派生；具体产品 fragment 是否采用 exit retention 仍由产品状态决定； - x 删除剩余 zeta-native 命名、旧 build target 和旧文档引用；迁移历史中的旧名称只保留在迁移历史、 AGENTS 约束和边界检查器中。 阶段四：独立产品发布 - x zeterm 能独立构建、测试，并通过 package staging 生成带 binary digest 的 unsigned zeterm artifact； - x 为 zeterm 建立发布输入和 boundary CI graph；//zeterm:zetermci 验证 workspace ownership， //zeterm:zetermreleaseinputs 固定 package/signing contract；Cargo workspace 仍是唯一 canonical Rust build graph； - x 建立 zeterm/ 与 zeta-rs shared crates 的 metadata-derived hermetic Bazel Rust graph； 单一 root workspace 已消除跨 workspace path dependency bridge； - x 在 CI 入口中通过完整 //zeterm:zeterm build 验证平台 toolchain；本机已通过同一 target， //zeterm:zetermci 继续验证 boundary/package/signing contract。 - x 接入 provider-neutral 的 platform signing/verification job；scripts/releasezetermpackage.sh 负责 Build → Stage → Sign → Verify，平台 CI 只需绑定 native tool 和 secret 环境；只发布 verified artifact； - x desktop 构建不拉入 Native UI/GPU 依赖；desktop 侧只保留自身的 platform adapter，边界检查未发现 对 zeterm UI/GPU crate 的依赖。 - zeta-rs backend 可独立测试、发布和被多个宿主复用； - App Server protocol、theme manifest 和 shared domain revision 具备明确兼容策略； - x packaging、CI、开发命令和文档只引用新的 product roots。 阶段五：通用框架可移植性（当前演进） - x 将 IconId、SVG definition 和 rendering mode 下沉到独立的 zeta-icon contract；zeta-icons 只保留可选的 zeterm product catalog，zui/zeta-ui 不再依赖该 catalog； - x 将 Root/Inspector 和 Terminal Workspace 的 pane topology 抽到 zeta-layout，Native 只投影 AgentSidebarState 为 SidebarLayoutSpec； - x 建立 zui-demo，只依赖 zui、zeta-ui 和 zeta-renderer，以 recording backend 验证通用 组件可脱离 zeterm product host 组合； - x 将 Composer panel、interaction list 与 selection scroll geometry 抽到 zeta-composer；Native 只投影 item count、preferred height 与 scene/state adapter； - 将更多 Shell/Composer/Session domain composition 按 owner 分批抽到产品领域 crate；不把产品 state、command 或平台事件下沉到 zui； - 在拥有第二个真实宿主前，不拆分 zui 的内部 foundation/layout/presentation/runtime 为更多 独立 crate；先保持单一 framework contract，降低跨 crate API churn。 当前审计结论（2026-08-03） 物理迁移已经完成：旧 Native UI 目录、旧 workspace、旧产品 build target 和 shared backend 到 UI 的 反向依赖均已清除；根 Cargo workspace、Bazel target graph、zeterm binary build、targeted tests 和 boundary CI 均已通过。 剩余两个未勾选项是产品演进工作，不是迁移残留：一是为 shared backend 建立独立发布 artifact/CI 流程，二是为 App Server、theme manifest 和 shared domain revision 定义跨版本兼容策略。它们可以在 当前迁移后的边界上继续建设，不应再把 UI 从 zeta-rs 搬迁作为前置条件。 迁移约束 - 不复制 zeta-rs/native 形成第二个并行宿主；迁移必须保持单一运行入口。 - 不在旧 Native 中修建新功能；若迁移过程中发现缺少通用能力，先在正确的下层 owner 实现，再由 zeterm 做最小接线。 - 不把 zeterm/editor、zeterm/agent-sidebar 等 presentation crate 直接误判为 shared backend； 先按“headless model/core”和“Native presentation”拆分。 - 不让 zeterm 类型、产品命令、窗口事件或布局类型进入 zeta-rs。 - 每个迁移阶段都必须保留 deterministic unit tests 和至少一个产品 targeted test；测试失败时先区分 迁移回归与工作区已有的无关 dirty change。 验收入口 阶段一完成后至少通过： `text cargo check --manifest-path Cargo.toml --workspace cargo check --manifest-path Cargo.toml -p zeterm cargo test --manifest-path Cargo.toml -p zeterm cargo test --manifest-path Cargo.toml --workspace just --dry-run zeterm ` just --dry-run zeterm 只验证入口已经指向根 workspace 的 zeterm package；真正启动窗口时使用 just zeterm。根 workspace 的 zeterm package 与 shared backend 已通过 workspace check 和 targeted check/test 验证；Bazel graph 的完整入口是 bazel build //zeterm:zeterm，发布 contract 入口是 bazel test //zeterm:zetermci，package staging 使用 just zeterm-package， 签名发布流程使用 scripts/releasezetermpackage.sh，发布细节见 zeterm-release-graph.md。 并满足以下结构检查： - rg 'zeta-rs/native zeta-native' 只剩迁移历史、兼容说明或明确的旧版本记录； - 根 Cargo.toml 是唯一 Cargo workspace；zeterm/Cargo.toml 不声明 nested workspace； - zeterm 只向下依赖 zeta-rs shared crates 与 zeterm-owned UI crates； - 任何 shared crate 都不依赖 zeterm`、窗口实例、GPU handle 或产品 layout。"
  },
  {
    "slug": "zeterm/zeterm-release-graph",
    "sourcePath": "zeterm/docs/zeterm-release-graph.md",
    "title": "zeterm 发布图与产品边界",
    "description": "本文拥有 zeterm 的 package staging、签名顺序和 CI contract；源码 owner 与 workspace 迁移见 zeterm-app-migration-plan.md，zeterm crate 的实现 owner 见 zeterm/README.md。",
    "group": "zeterm 产品",
    "markdown": "本文拥有 `zeterm` 的 package staging、签名顺序和 CI contract；源码 owner 与 workspace 迁移见\n[`zeterm-app-migration-plan.md`](zeterm-app-migration-plan.md)，zeterm crate 的实现 owner 见\n[`zeterm/README.md`](../README.md)。\n\n## 快速理解\n\n根 `Cargo.toml` 是唯一 canonical Cargo build graph；`zeterm/Cargo.toml` 仍是 `zeterm` 产品 package\n和发布边界。Bazel 只读取同一个 root `Cargo.toml/Cargo.lock`，通过单一 `@crates` hub 生成 zeterm\nowned crates、shared backend crates 和 `zeterm` 的 target graph，不再需要跨 workspace metadata\npatch 或重复 product hub。\n\n`zeterm/` 与 `zeta-rs/` 仍保持物理目录和依赖 ownership 分离：zeterm 可以依赖 shared backend，shared\nbackend 不得依赖 zeterm/UI。workspace 是统一构建图，不替代产品架构边界；boundary script 和 CI\n继续验证反向依赖不会出现。\n\n| 层 | 当前 owner | 入口 | 状态 |\n| --- | --- | --- | --- |\n| Rust compile/test | Root Cargo workspace / zeterm package | `cargo check/test --manifest-path Cargo.toml -p zeterm` | ✅ |\n| Source/manifest input graph | Bazel zeterm package | `//zeterm:zeterm_sources` | ✅ |\n| Zeterm Rust target graph analysis | Bazel + patched `rules_rs` | `//zeterm:zeterm` | ✅ |\n| Package/signing input contract | `zeterm/packaging/*.json` | `//zeterm:zeterm_release_inputs` | ✅ |\n| Unsigned package staging | `scripts/build_zeterm_package.py` | `just zeterm-package` | ✅ |\n| Workspace boundary CI | Bazel | `bazel test //zeterm:zeterm_ci` | ✅ |\n| Platform signing and verification | `scripts/release_zeterm_package.sh` | policy file中的 native tool | ✅ 已接入 provider-neutral job |\n| Hermetic Bazel Rust compile graph | `rules_rs` + single `@crates` hub | `//zeterm:zeterm` | ✅ 完整 zeterm binary build 已通过 |\n\n### 根级 Bazel 基础设施\n\n当前根级 Bazel 层只拥有仓库通用的构建边界，不拥有具体产品的业务组合：\n\n- [`MODULE.bazel`](../../MODULE.bazel) 固定 `rules_rs`、LLVM Rust toolchain、hermetic macOS SDK 和系统 Framework；\n- [`.bazelrc`](../../.bazelrc) 禁止探测本机 Xcode，并按宿主系统选择 Linux/Windows 的 platform constraint；\n- [`BUILD.bazel`](../../BUILD.bazel) 提供 `//:zeta` 产品入口、`disable_xcode` 和宿主 platform 定义；\n- `zeterm/BUILD.bazel`、`zeta-code/BUILD.bazel` 和各 crate 的 `BUILD.bazel` 继续拥有各自产品/ crate target。\n\n当前不引入 Codex 专属的 RBE、Wine 或 workspace-root test launcher。RBE 需要真实的远程执行/缓存后端和 CI 凭证；\nWine 只有在 Linux 主机交叉运行 Windows 测试时才有价值；专用 launcher 只有在测试需要 Codex 式 workspace-root、runfiles\n环境或特殊变量时才应增加。它们属于后续 CI 能力，不是本地 hermetic 构建的前置条件。\n\n## 阶段顺序\n\n```mermaid\nflowchart LR\n    A[Cargo check/test] --> B[Unsigned package staging]\n    B --> C[Binary SHA-256 digest]\n    C --> D{Platform signer}\n    D --> E[Signature record]\n    E --> F[Native verification]\n    F --> G[Publish artifact]\n    H[Bazel boundary CI] --> A\n    H --> B\n```\n\n### 1. Build\n\n所有 zeterm-owned crate 和 shared backend 通过根 `Cargo.toml` 解析。Bazel 的 `//zeterm:zeterm` 使用同一份\nCargo metadata-derived dependency graph；package builder 只接受 Cargo 生成的 `zeterm` binary 或\n从 zeterm package 构建它，它不从 `zeta-rs` 的旧 Native target 取 binary。\n\n### 2. Stage\n\n```bash\njust zeterm-package \\\n  --package-dir /absolute/path/to/zeterm-package \\\n  --zeterm-bin /absolute/path/to/zeterm\n```\n\n输出最小目录：\n\n```text\nzeterm-package/\n├── bin/zeterm\n├── zeterm-package.json\n└── zeterm-signing-policy.json\n```\n\n`zeterm-package.json` 固定 product、target、profile、binary path 和 SHA-256；staging 拒绝覆盖已有\n目录，并把状态标成 `unsigned`。这一步不取得密钥、不签名，也不宣称 artifact 可发布。\n\n### 3. Sign 与 verify\n\n`zeterm/packaging/zeterm-signing-policy.json` 是 release job 的输入，不是开发机默认行为：\n\n- macOS 使用 `codesign` 和 `ZETERM_MACOS_SIGNING_IDENTITY`，验证后再打包/公证；\n- Linux 使用 `cosign sign-blob`，签名文件和 binary digest 一起进入 provenance artifact；\n- Windows 使用 `signtool` 和 `ZETERM_WINDOWS_CERTIFICATE`，验证 Authenticode chain；\n- 签名 job 只能读取 staging 输出，不能重建 binary；verify job 必须重新计算 digest，并检查与\n  `zeterm-package.json`、signature record 一致；\n- 本地 unsigned package 只用于开发和测试，release job 必须拒绝 `signing.status != verified`。\n\n密钥、证书和 token 不进入仓库，不由 Bazel rule 参数传递；CI secret store 和平台 signer 是发布\n系统 owner。签名证明来源和完整性，不替代运行时安全审查。\n\nprovider-neutral job 入口是：\n\n```bash\nZETERM_PACKAGE_DIR=/absolute/path/to/zeterm-package \\\nZETERM_PLATFORM=darwin \\\nZETERM_MACOS_SIGNING_IDENTITY=\"Developer ID Application: ...\" \\\nscripts/release_zeterm_package.sh\n```\n\nLinux 使用 `ZETERM_COSIGN_IDENTITY` 指向 cosign key，Windows 使用\n`ZETERM_WINDOWS_CERTIFICATE` 指向 CI 证书文件或证书存储标识。脚本只把 identity 传给 native signer，\n不写入 metadata 或 signature record；CI provider\n负责把这些变量绑定到 secret store。脚本完成后，`zeterm-package.json` 和\n`zeterm-signature.json` 都必须是 `verified` 状态，才允许进入 publish step。\n\n## CI 契约\n\n当前已经可以在没有平台签名环境的机器上验证结构：\n\n```bash\nbazel test //zeterm:zeterm_ci\n```\n\n`//zeterm:zeterm_ci` 同时运行 workspace boundary、package staging、release contract 和 signing state\ntransition tests。\n\n具体 CI provider 只需要把上面的 Build → Stage → Sign → Verify → Publish 节点接入 secret store；\n不要把密钥逻辑写入 `zeterm/src`、`zui` 或通用 Bazel Rust macro。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 3,
        "id": "根级-bazel-基础设施",
        "title": "根级 Bazel 基础设施"
      },
      {
        "depth": 2,
        "id": "阶段顺序",
        "title": "阶段顺序"
      },
      {
        "depth": 3,
        "id": "1-build",
        "title": "1. Build"
      },
      {
        "depth": 3,
        "id": "2-stage",
        "title": "2. Stage"
      },
      {
        "depth": 3,
        "id": "3-sign-与-verify",
        "title": "3. Sign 与 verify"
      },
      {
        "depth": 2,
        "id": "ci-契约",
        "title": "CI 契约"
      }
    ],
    "searchText": "本文拥有 zeterm 的 package staging、签名顺序和 CI contract；源码 owner 与 workspace 迁移见 zeterm-app-migration-plan.md，zeterm crate 的实现 owner 见 zeterm/README.md。 快速理解 根 Cargo.toml 是唯一 canonical Cargo build graph；zeterm/Cargo.toml 仍是 zeterm 产品 package 和发布边界。Bazel 只读取同一个 root Cargo.toml/Cargo.lock，通过单一 @crates hub 生成 zeterm owned crates、shared backend crates 和 zeterm 的 target graph，不再需要跨 workspace metadata patch 或重复 product hub。 zeterm/ 与 zeta-rs/ 仍保持物理目录和依赖 ownership 分离：zeterm 可以依赖 shared backend，shared backend 不得依赖 zeterm/UI。workspace 是统一构建图，不替代产品架构边界；boundary script 和 CI 继续验证反向依赖不会出现。 层 当前 owner 入口 状态 --- --- --- --- Rust compile/test Root Cargo workspace / zeterm package cargo check/test --manifest-path Cargo.toml -p zeterm ✅ Source/manifest input graph Bazel zeterm package //zeterm:zetermsources ✅ Zeterm Rust target graph analysis Bazel + patched rulesrs //zeterm:zeterm ✅ Package/signing input contract zeterm/packaging/.json //zeterm:zetermreleaseinputs ✅ Unsigned package staging scripts/buildzetermpackage.py just zeterm-package ✅ Workspace boundary CI Bazel bazel test //zeterm:zetermci ✅ Platform signing and verification scripts/releasezetermpackage.sh policy file中的 native tool ✅ 已接入 provider-neutral job Hermetic Bazel Rust compile graph rulesrs + single @crates hub //zeterm:zeterm ✅ 完整 zeterm binary build 已通过 根级 Bazel 基础设施 当前根级 Bazel 层只拥有仓库通用的构建边界，不拥有具体产品的业务组合： - MODULE.bazel 固定 rulesrs、LLVM Rust toolchain、hermetic macOS SDK 和系统 Framework； - .bazelrc 禁止探测本机 Xcode，并按宿主系统选择 Linux/Windows 的 platform constraint； - BUILD.bazel 提供 //:zeta 产品入口、disablexcode 和宿主 platform 定义； - zeterm/BUILD.bazel、zeta-code/BUILD.bazel 和各 crate 的 BUILD.bazel 继续拥有各自产品/ crate target。 当前不引入 Codex 专属的 RBE、Wine 或 workspace-root test launcher。RBE 需要真实的远程执行/缓存后端和 CI 凭证； Wine 只有在 Linux 主机交叉运行 Windows 测试时才有价值；专用 launcher 只有在测试需要 Codex 式 workspace-root、runfiles 环境或特殊变量时才应增加。它们属于后续 CI 能力，不是本地 hermetic 构建的前置条件。 阶段顺序 ``mermaid flowchart LR A Cargo check/test -- B Unsigned package staging B -- C Binary SHA-256 digest C -- D Platform signer D -- E Signature record E -- F Native verification F -- G Publish artifact H Bazel boundary CI -- A H -- B ` 1. Build 所有 zeterm-owned crate 和 shared backend 通过根 Cargo.toml 解析。Bazel 的 //zeterm:zeterm 使用同一份 Cargo metadata-derived dependency graph；package builder 只接受 Cargo 生成的 zeterm binary 或 从 zeterm package 构建它，它不从 zeta-rs 的旧 Native target 取 binary。 2. Stage `bash just zeterm-package \\ --package-dir /absolute/path/to/zeterm-package \\ --zeterm-bin /absolute/path/to/zeterm ` 输出最小目录： `text zeterm-package/ ├── bin/zeterm ├── zeterm-package.json └── zeterm-signing-policy.json ` zeterm-package.json 固定 product、target、profile、binary path 和 SHA-256；staging 拒绝覆盖已有 目录，并把状态标成 unsigned。这一步不取得密钥、不签名，也不宣称 artifact 可发布。 3. Sign 与 verify zeterm/packaging/zeterm-signing-policy.json 是 release job 的输入，不是开发机默认行为： - macOS 使用 codesign 和 ZETERMMACOSSIGNINGIDENTITY，验证后再打包/公证； - Linux 使用 cosign sign-blob，签名文件和 binary digest 一起进入 provenance artifact； - Windows 使用 signtool 和 ZETERMWINDOWSCERTIFICATE，验证 Authenticode chain； - 签名 job 只能读取 staging 输出，不能重建 binary；verify job 必须重新计算 digest，并检查与 zeterm-package.json、signature record 一致； - 本地 unsigned package 只用于开发和测试，release job 必须拒绝 signing.status != verified。 密钥、证书和 token 不进入仓库，不由 Bazel rule 参数传递；CI secret store 和平台 signer 是发布 系统 owner。签名证明来源和完整性，不替代运行时安全审查。 provider-neutral job 入口是： `bash ZETERMPACKAGEDIR=/absolute/path/to/zeterm-package \\ ZETERMPLATFORM=darwin \\ ZETERMMACOSSIGNINGIDENTITY=\"Developer ID Application: ...\" \\ scripts/releasezetermpackage.sh ` Linux 使用 ZETERMCOSIGNIDENTITY 指向 cosign key，Windows 使用 ZETERMWINDOWSCERTIFICATE 指向 CI 证书文件或证书存储标识。脚本只把 identity 传给 native signer， 不写入 metadata 或 signature record；CI provider 负责把这些变量绑定到 secret store。脚本完成后，zeterm-package.json 和 zeterm-signature.json 都必须是 verified 状态，才允许进入 publish step。 CI 契约 当前已经可以在没有平台签名环境的机器上验证结构： `bash bazel test //zeterm:zetermci ` //zeterm:zetermci 同时运行 workspace boundary、package staging、release contract 和 signing state transition tests。 具体 CI provider 只需要把上面的 Build → Stage → Sign → Verify → Publish 节点接入 secret store； 不要把密钥逻辑写入 zeterm/src、zui` 或通用 Bazel Rust macro。"
  },
  {
    "slug": "core",
    "sourcePath": "docs/core.md",
    "title": "会话与执行系统",
    "description": "Core 是一次 Agent 工作的权威协调者：它推进 Turn、安排模型和工具调用，并保证状态按可恢复的 顺序提交，但不亲自实现模型、工具、网络或界面。",
    "group": "Agent 与运行时",
    "markdown": "> 物理位置：`zeta-rs/core/`\n> Rust crate：`zeta_core`\n> Canonical value 与 wire-neutral contract：[`protocol.md`](protocol.md)\n> 上下文详细设计：[`core-context.md`](core-context.md)\n> 多 Agent 详细设计：[`core-multi-agent.md`](core-multi-agent.md)\n> 跨 Core、App Server、provider 与 Tool 的执行演进：\n> [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md)\n> Tool shared contract、registry/binding 与 source adapter：[`tools.md`](tools.md)\n> Config、Plugin、MCP 与 Skill snapshot：[`config.md`](config.md)\n> Provider credential：[`model-provider.md`](model-provider.md#6-供应商凭据与-codex-边界)\n> Secret persistence：[`secrets.md`](secrets.md)\n> Cancellation tree 实现：[`zeta-async-utils` README](../zeta-rs/async-utils/README.md)\n> Session/Thread store ports：[`zeta-session-store`](../zeta-rs/session-store/README.md) /\n> [`zeta-thread-store`](../zeta-rs/thread-store/README.md)\n> Local recovery composition：[`zeta-rollout`](../zeta-rs/rollout/README.md)\n\n## 快速理解\n\nCore 是一次 Agent 工作的权威协调者：它推进 Turn、安排模型和工具调用，并保证状态按可恢复的\n顺序提交，但不亲自实现模型、工具、网络或界面。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| 一次工作保存在哪里？ | Session 聚合多个 Thread；每个 Thread 独立保存 Turn、Item 和逻辑序列 | [产品模型](#4-产品模型与执行模型) |\n| 谁推进一个 Turn？ | `ThreadController` 保持单写者顺序，选定的 `TurnExecutionBackend` 推进执行；默认实现是 `TurnExecutor` | [核心组件](#5-核心组件) |\n| 模型、工具和策略由谁实现？ | Core 只拥有消费方端口和调用顺序，具体实现由外部服务注入 | [依赖方向](#6-依赖方向与服务端口) |\n| 什么时候算已经执行？ | 执行授权和开始事实必须先持久化，之后才能跨过副作用边界 | [提交与安全点](#7-durable-commit并发与安全点) |\n| Core 是否拥有 UI 或传输？ | 不拥有；Desktop、CLI、TUI 和 App Server 只是不同入口 | [所有权边界](#3-所有权边界) |\n\n## 1. 定位\n\n`zeta-core` 是 Zeta 的 Agent 执行控制面，只负责五类事情：\n\n1. Agent 生命周期；\n2. Turn 执行；\n3. 上下文管理；\n4. Tool 调度；\n5. 服务编排。\n\n这里的“Agent 生命周期”包括 Session、Thread、Turn、delegation 的 durable 状态迁移，以及加载、\n取消、等待、恢复和执行实例隔离。模型进程、MCP server、浏览器、终端和网络连接自身的生命周期\n不属于 Core。\n\n“服务编排”表示 Core 通过窄的 typed port 规定 model、tool、store、policy 与 capability 的调用\n顺序、提交边界和 failure semantics。它不表示 service locator、依赖注入容器或具体服务实现。\n\nCore 必须保留纯 reducer、durable commit ordering 和 recovery。它们不是额外的产品能力，而是\nAgent 生命周期能够成为 authority 的前提。\n\n长期不建立 `CoreRuntime`、`AgentRuntime`、`SessionRuntime` 或 `ThreadRuntime` 公共组件。\n进程内执行状态确实存在，但由 `ThreadController`、`LoadedThreadState`、`TurnExecution` 等准确\n名称表达。只有 Tokio、Wasm 或外部 Tool execution environment 这类真正的执行环境才使用\n`runtime` 一词，而且其实现不属于 Core。\n\n## 2. 当前状态\n\n已经落地：\n\n- Session/Thread reducer、projection 与 store port；\n- `SessionCoordinator` 及可恢复 create/fork saga；\n- `ThreadController`、command receipt、replay、conflict detection 与 recovery；\n- per-Thread loaded projection、FIFO mutation gate、explicit incarnation、bounded execution mailbox\n  与 idle eviction；\n- durable Turn、Tool Call、Tool Result 与 interaction lifecycle；\n- provider-independent `ModelService`；\n- `TextDelta` / `ReasoningDelta` Core streaming contract；\n- `TurnExecutor` 的顺序 model → tool → model 循环；\n- `HookService` 的 `beforeTool`、`afterTool` 与 durable Turn completion safe-point 调度；\n- Tool 内用户输入的 durable request、exact live waiter 唤醒、取消与重启后 unknown-outcome 收口；\n- `TurnExecutionBackend` 委托边界、Codex 完整 Turn adapter 与 immutable remote thread binding；\n- durable Turn policy revision binding 与 recovery fail-closed；\n- `ContextInput` / `ContextPlan`、纯 planner、per-loaded-Thread `ContextManager`、\n  `ContextPlan → ModelRequest` 的 `ContextAssembler`；\n- durable context checkpoint、model-backed compaction、commit 后重规划与当前\n  `ModelInvocationSnapshot` 纵向切片。\n\n尚未完成：\n\n- App Server 独立 outbound worker；\n- 完整 `TurnPolicySnapshot` 中除 revision 外的 execution limit/agent role 集合，以及\n  `ModelInvocationSnapshot` 的独立 provider/config/catalog revision 集合；\n- 其余 provider/local tokenizer adapter、usage 校准、prompt cache/reference baseline 与跨 Thread\n  context seed；\n- 并行 Tool、通用 deadline、声明式 retry 与 reconciliation；\n- durable multi-Agent delegation、跨 Thread message/result 与 Agent tree resource budget；\n- 所有 durable boundary 的 fault injection。\n\n当前实现是演进地基，不应被描述成完整的异步、多 Agent 执行系统。\n\n跨层组件状态总账与状态标记词表（已实现 / 部分 / 仅设计 / 推迟）由\n[`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md#2-组件状态总账)\n维护；本文各组件章节挂对应标记，不重复总表。\n\n## 3. 所有权边界\n\n### 3.1 核心拥有\n\n- Session membership、lineage、shared defaults 与 lifecycle 的协调；\n- 每个 Thread 的逻辑单写者、durable commit、加载和恢复；\n- Turn acceptance、执行、等待、取消、完成、失败和恢复；\n- Agent delegation 的 spawn、message、join、cancel 与结果合并；\n- command idempotency、expected sequence 和 committed update ordering；\n- Turn policy snapshot 与 model invocation snapshot；\n- 从 durable facts 派生 provider-neutral context；\n- context window、预算、checkpoint 选择和 compaction 协调；\n- model → tool → model loop 及 sequential/parallel Tool 调度；\n- approval、structured user input、dynamic tool 与 capability wait；\n- cancellation tree、deadline、迟到结果和旧 incarnation 拒绝；\n- crash 后模型、Tool、interaction 与 delegation 的恢复决策；\n- 将外部错误映射为稳定 Core outcome。\n\n### 3.2 核心不拥有\n\n- canonical shared value 的定义；它属于 `zeta-protocol`；\n- stored envelope、event framing、JSONL/SQLite 和 fsync；\n- provider HTTP DTO、endpoint、SSE decoder、认证与 credential；\n- model catalog 的发现、缓存、合并与筛选；\n- Tool、MCP transport、child process、browser 或 terminal 的具体实现；\n- sandbox/OS enforcement、approval UI 与 connection owner；\n- Config 文件、环境变量、secret persistence 或 credential refresh；\n- JSON-RPC method、subscription cursor 与 transport lifecycle；\n- Desktop、CLI、TUI 的 projection；\n- provider prompt cache、连接池或模型驻留；\n- 未经单独设计和授权的跨 Thread 长期 memory。\n\n### 3.3 容易混淆的边界\n\n| 概念 | Core 拥有 | 外层拥有 |\n| --- | --- | --- |\n| Model | 调用时机、snapshot、结果消费、safe point | provider 解析、HTTP、认证、wire codec |\n| Tool | 选择、approval、排序、取消、结果提交、失败决策 | schema adapter、执行、sandbox、MCP/进程 I/O |\n| Context | 选择策略、预算、compaction 协调、纯组装 | provider cache、未经授权的 memory retrieval |\n| Agent | delegation、Thread 绑定、消息、结果、取消、预算 | UI 呈现、远端进程或 capability 生命周期 |\n| Service orchestration | 调用 typed ports、状态推进、补偿与降级 | 构造 adapter、配置加载、进程启动、网络监听 |\n\n## 4. 产品模型与执行模型\n\n### 4.1 Product Session\n\n`Session` 是产品级根 aggregate，拥有：\n\n- task lifecycle；\n- Thread membership；\n- root/fork/spawn lineage；\n- shared defaults；\n- 自己的 durable sequence。\n\nSession 不持有任何 Thread transcript，不代理 token delta，也不等待 model/Tool I/O。同一 Session\n下的 Thread 必须能够并行执行。\n\n### 4.2 Thread\n\n`Thread` 是独立的历史、排序、上下文和恢复边界，拥有：\n\n- immutable `SessionId`；\n- durable sequence；\n- ordered Turn 与 Item；\n- compaction checkpoint；\n- pending interaction/delegation facts；\n- 一个逻辑状态提交者。\n\n每个已加载 Thread 可以持有一个可丢弃、可重建的 `ContextManager`。ContextManager 不改变\nThread 的 authority；它只能从 durable Thread facts 和不可变 snapshot 派生状态。\n\n### 4.3 Agent\n\n当前长期方案中，一个活跃 Agent 绑定一个 Thread。`ThreadId` 可以先作为 Agent 的执行身份，\n不急于增加一对一的 `AgentId` aggregate。父子工作的相关性由独立 `DelegationId` 表达。\n\n只有未来出现“一个 Agent 身份需要跨多个 Thread 延续”的真实需求时，才引入 `AgentId`。\n无论是否引入，context 始终绑定 Thread，而不是绑定 Session 或整个 Agent tree。\n\n### 4.4 Turn 与操作\n\n必须区分：\n\n```text\nProduct lifecycle\n  Session / Thread / Turn / Delegation 的 durable status\n\nLoaded execution lifecycle\n  Thread load / idle / executing / closing + incarnation\n\nOperation lifecycle\n  model invocation / tool execution / interaction delivery / agent message delivery\n```\n\n三层不能复用一个 status、operation ID 或 cancellation token。\n\n## 5. 核心组件\n\n```text\nMultiAgentCoordinator (multi-Agent only)\n  ├─ topology operations ──► SessionCoordinator\n  └─ cross-Thread work ────► ThreadController\n\nSessionCoordinator ────────► ThreadController\nmembership / defaults        one logical writer\n                                   │\n                          LoadedThreadState\n                          └─ ContextManager\n                                   │\n                                   ▼\n                              TurnExecutor\n                  context → model → tools → model\n                     │          │          │\n                     ▼          ▼          ▼\n               ContextPlan  ToolScheduler  interactions\n                     │          │\n                     ▼          ▼\n              ModelService   ToolService\n```\n\n### 5.1 SessionCoordinator（已实现）\n\n负责：\n\n- 创建、读取和恢复 Session；\n- create、attach、fork、spawn、archive Thread 的结构协调；\n- root/parent/child lineage；\n- Session lifecycle 与 shared defaults；\n- 跨 Session/Thread stream 的可恢复 saga。\n\n不负责：\n\n- Thread transcript 或 context；\n- Turn loop；\n- token/reasoning streaming；\n- 等待 model、Tool 或 child Agent 完成。\n\n### 5.2 ThreadController（已实现）\n\n每个已加载 Thread 对应一个逻辑状态提交者。实现可以是 mailbox task 或 keyed executor，但必须\n满足：\n\n- 同一 Thread 的结构性 mutation FIFO；\n- 不同 Thread 可并行；\n- model、Tool 和 Agent I/O 不占 projection lock 或 writer lease；\n- interrupt、interaction response、Tool completion、model completion 和 Agent result 都回到\n  同一提交序列；\n- 每次 load 生成新的 `ThreadIncarnationId`；\n- completion 携带 operation ID 与 incarnation；\n- 空闲状态可以回收，恢复只依赖 durable facts。\n\n“一个 Thread 一个常驻 task”只是实现选择，不是公共契约。\n\n### 5.3 LoadedThreadState（已实现）\n\n这是 Core-private 的进程内状态：\n\n```text\nLoadedThreadState\n├─ durable projection snapshot\n├─ ThreadIncarnationId\n├─ ThreadActivity\n│  ├─ Idle\n│  ├─ Executing(TurnExecution)\n│  └─ Waiting(Interaction | Delegation)\n├─ ContextManager\n├─ bounded mailbox\n└─ cancellation/task ownership\n```\n\n它不能进入 protocol，也不能成为第二个 durable aggregate。进程重启后必须能够丢弃并重建。\n\n当前实现由 private `loaded_thread::LoadedThreads` 保存轻量 Thread slot registry；\n`loaded_thread::ThreadSlot` 的 ticket gate 串行结构性 mutation，slot 内的\n`LoadedThreadState` 保存 projection 与 incarnation。`mailbox::ThreadExecutionMailboxes`\n为每个 incarnation 创建有界 FIFO lane，`ThreadExecutionContext` 将 operation ID、\nincarnation 与 cancellation 绑定。worker 空闲 30 秒后只回收匹配 incarnation 的 lane 和\nprojection；下一次访问从 `ThreadStore` 重建。运行中的 execution 会阻止显式 recovery，\n因此 completion 不可能跨越 incarnation；测试中的强制 stale context 也会被拒绝。\n\n### 5.4 TurnExecutor（已实现：顺序循环）\n\n负责一个已经 accepted 的 Turn：\n\n```text\nfreeze TurnPolicySnapshot\n→ prepare context\n→ create ModelInvocationSnapshot\n→ invoke model\n→ consume stream/final response\n→ no Tool Call ──────────────────────────────► complete\n→ validate and schedule Tool Calls\n→ approval if required\n→ durable Tool Call intent\n→ execute Tools\n→ durable Tool Result / UnknownOutcome\n→ prepare next invocation\n```\n\nTurnExecutor 只能提出 typed effect；ThreadController 负责实际 durable append。它不能直接修改\nprojection、写 store 或提前发布 committed update。\n\n当前单 Agent loop 不设置固定的模型调用次数上限：只要模型仍产生 Tool Call 等 follow-up，\nTurn 就继续执行。安全边界应由可取消的 token/cost/deadline policy 和 durable usage accounting\n表达，不能使用 approval 或 recovery 后会重置的进程内计数器。\n\n### 5.5 TurnExecutionBackend（已实现）\n\n`TurnExecutionBackend` 是推进已创建 durable Turn 的 consumer-owned port。`TurnExecutor` 实现\nZeta 自己的 provider-independent model/tool loop；外部 runtime 若同时拥有 Agent loop、Tool 和\napproval（例如 Codex App Server），则实现这个 port，而不是伪装成 `ModelService`。\n\nCore 始终保留 Thread event、interaction、cancellation 和 terminal outcome authority。委托后端只能\n通过 `ThreadController` 提交 durable item/interaction；远端 thread continuity 使用 immutable\n`TurnExecutionBinding` 保存，并绑定 opaque execution scope。后端在第一次远程请求前写入\n`TurnExecutionAttempted`；因此 in-flight remote Turn 不可在未知结果后透明重放。\n\n### 5.6 ContextManager 与 ContextAssembler（核心纵向切片已实现）\n\n两者必须分开：\n\n- `ContextManager`：每个 loaded Thread 一个，协调 sequence 检查与 prepare；当前不持有 cache、\n  baseline 或 token estimate；\n- `ContextAssembler`：无状态纯组件，把已经确定的 `ContextPlan` 组装成\n  provider-neutral `ModelRequest`；\n- `ContextPlan`：一次 invocation 的不可变派生结果；\n- durable Thread history：唯一权威历史。\n\n未来若有性能证据，ContextManager 可以缓存按 `ThreadSequence`、policy revision 和 model\ncapability revision 索引的派生视图，但不能维护第二份 canonical transcript。完整契约见\n[`core-context.md`](core-context.md)。\n\n### 5.7 MultiAgentCoordinator（部分实现）\n\n这是只在多 Agent 模式下参与的跨 Thread 协调组件。当前已负责 Fresh spawn、durable\ndelegation、message/result delivery、recovery 与结构性 tree budget；目标完整职责包括：\n\n- spawn、send、join、cancel、close；\n- durable delegation 与 delivery identity；\n- parent/child cancellation；\n- 最大深度、子 Agent 数、并发、token/cost/deadline budget；\n- 协调 SessionCoordinator 创建 child Thread；\n- 协调跨 Thread message/result 的 durable delivery。\n\n它不持有任何 Thread context，也不读取 live ContextManager。父子 Agent 的上下文传递必须通过\n不可变 `AgentContextSeed`，结果必须通过显式 durable Item 合并。完整契约见\n[`core-multi-agent.md`](core-multi-agent.md)。\n\n单 Agent 执行已经由 ThreadController、TurnExecutor、ContextManager 和 ToolScheduler 完整表达，\n不再建立泛化的 `agent/` facade。`multi_agent/` 只包含单 Thread 之外的 delegation、spawn、\nmessage、join、tree budget 和 recovery。\n\n## 6. 依赖方向与服务端口\n\n目标依赖：\n\n```text\n                         zeta-protocol\n                              ▲\n                 ┌────────────┼────────────┐\n                 │            │            │\n        zeta-session-store  zeta-core  zeta-thread-store\n                 ▲            ▲            ▲\n                 │            │ ports      │\n                 └──── composition root ───┘\n                              │\n              model / tool / policy / capability adapters\n```\n\n允许：\n\n- `zeta-core → zeta-protocol`；\n- `zeta-core → zeta-history`，构造与回放 persisted Thread record；\n- `zeta-core → zeta-tools`；\n- `zeta-core → zeta-session-store`；\n- `zeta-core → zeta-thread-store`；\n- `zeta-core → zeta-async-utils`；\n- 外层 adapter 实现 Core 定义的 consumer-owned port。\n\n禁止：\n\n```text\nzeta-core → zeta-app-server(-protocol)\nzeta-core → zeta-storage / zeta-rollout\nzeta-core → zeta-api / concrete model provider\nzeta-core → shell-command / file-system / apply-patch / MCP implementation\nzeta-core → config files / credentials / secrets\nzeta-core → Desktop / CLI / TUI\n```\n\n最小端口集合：\n\n| Port | 状态 | Core 为什么消费 | 实现位置 |\n| --- | --- | --- | --- |\n| `SessionStore` | 已实现 | Session load/append | storage adapter |\n| `ThreadStore` | 已实现 | Thread load/append | storage adapter |\n| `WriterLease<Id>` | 已实现 | aggregate 单写者 | storage/host |\n| `TurnExecutionBackend` | 已实现 | 推进完整 Turn，同时保留 Core state authority | `TurnExecutor` / Codex adapter |\n| `ModelService` | 已实现（同步；流式默认桥接见 `ModelStreamSink`） | provider-neutral model invocation | model-provider adapter |\n| `ToolService` | 已实现（含 `prepare` / `review_evidence` / `execute_streaming`） | 已物化 Tool call 的执行 | built-in/MCP host |\n| `ToolInteractionService` | 已实现 | 工具运行中提交 durable 用户输入请求，并等待 exact Thread/Turn/request 的响应或取消 | Core tool orchestrator；MCP adapter 消费 |\n| `HookService` | 已实现 | 在 Core-owned tool/Turn safe point 调用 host-owned declarative Hook runtime | App Server Hook adapter |\n| `ActionPolicyService` | 已实现 | action approval/sandbox 决策 | host policy layer |\n| `ThreadUpdateSink` | 已实现 | committed/transient update 发布 | App Server subscription hub |\n| `ToolOutputSink` | 已实现 | Tool Call transient output | App Server/host |\n| `CapabilityBroker` | 仅设计 | Turn-scoped capability 解析 | App Server/host |\n| `ContextCompactionService` | 已实现 | 从不可变 source 生成有界候选 summary | Core contract + model-backed adapter |\n| `Clock` | 仅设计 | deadline 与可测试时间 | host |\n| `IdGenerator` | 仅设计 | 稳定 operation identity | host |\n\n`zeta-history` 不是服务端口：它只提供 `StoredEvent`、command receipt 和可读 schema range 等纯\n数据契约。Core 直接消费这些类型，I/O 仍只经过 `ThreadStore`。\n\n当前端口均为同步调用 + `CancellationToken` 协作取消；执行内核不承诺 async 化，理由与重新\n评审条件见\n[`zeta-agent-runtime-architecture.md` R2](zeta-agent-runtime-architecture.md#42-r2同步执行内核流式经-sink)。\n\nPort 必须是 consumer-owned interface。新 trait 必须说明角色、实现不变量、取消和错误约束。\n公共 request 不使用语义含糊的 `bool` 或 `Option` 参数。\n\n目标 host policy adapter 由 [`auto-review.md`](auto-review.md) 中的 `zeta-action-policy` 实现：\ndeterministic rule/grant 是 authority，LLM classifier 只提供 recommendation。Core 只消费最终\ndecision，并负责 durable approval、retry 与 unknown-outcome lifecycle。\n\nComposition root 负责读取各 authority、构造 provider/tool/store/policy adapter、materialize\ncredential、注入 Core 并启动 transport。Core 不提供隐藏全局状态的 service registry。\n\n## 7. Durable commit、并发与安全点\n\n### 7.1 提交顺序\n\n所有权威 mutation 使用同一顺序：\n\n```text\nreceive intent/completion\n→ validate projection + operation identity + incarnation\n→ build typed event batch\n→ reduce into candidate projection\n→ append atomic batch\n→ install committed projection\n→ publish committed update\n→ schedule next external operation\n```\n\n必须满足：\n\n- append 失败不安装 candidate projection；\n- notification 不早于 durable append；\n- live commit 与 recovery 使用同一 reducer；\n- command receipt 与首个业务 event 原子提交；\n- transient delta 可以丢失，final Item 必须 durable；\n- model/Tool/Agent completion 不冒用用户 `CommandId`。\n\n### 7.2 安全点\n\n默认 safe point：\n\n- model invocation 开始前；\n- 一组 Tool Results durable commit 后；\n- interaction resolved 后恢复执行前；\n- Agent message/result durable delivery 后；\n- Turn 尚未开始或已经 terminal；\n- compaction checkpoint 已验证并 durable commit 后。\n\nmodel、provider、tool availability、context policy 与非安全配置只在 safe point 进入新 snapshot。\n安全策略允许执行中单调收紧，不能静默放宽。\n\n### 7.3 取消\n\n```text\nCore shutdown source\n└─ root Agent/Thread source\n   ├─ Turn source\n   │  ├─ model invocation source\n   │  ├─ tool execution source\n   │  └─ interaction source\n   └─ child Agent source\n      └─ child Turn/operation sources\n```\n\n父取消传播到所有后代；取消 child 不影响 parent 或 sibling。Cancellation 是协作式 best effort，\n不能证明外部副作用未发生，也不能越过 durable terminal event 提前对外宣称完成。\n\n当前单 Agent execution 的实际链路已经闭合：\n\n```text\nsession/request { type: interruptTurn }\n→ ThreadExecutionMailboxes::cancel(exact ThreadId + TurnId)\n→ TurnExecutor-owned CancellationToken\n├─ ModelService → ModelInvoker → OperationClient\n└─ ToolScheduler → ToolService → exec / MCP adapter\n```\n\n同步 provider HTTP 在 token 取消后停止 Core/operation 层等待并禁止 retry；因为当前\n`zeta-http-client` 仍是同步 unary transport，已经进入 socket 的 attempt 由 bounded transport\ntimeout 收束，其迟到 response 被丢弃。已越过 durable execution-start boundary 的 Tool 不会被\n伪装为安全未执行：terminal Turn 保留 execution-start marker 且没有 Tool Result，恢复时按 unknown\noutcome 处理，exact call 不自动重放。\n\nTool 内交互使用相同 cancellation tree，但不会结束当前工具执行再重启另一个 executor。工具调用先\n注册 exact `ThreadId + TurnId + RequestId` live waiter，再提交 durable `InteractionRequested`；响应\ndurable commit 后，`ThreadController` 唤醒该 waiter。App Server 只有在\n`live_execution_woken=false` 时才调用 `TurnExecutionBackend::resume`，用于进程恢复后的 pending\ninteraction。interrupt、deadline 或 owner disconnect 会取消 waiter；进程重启后没有 live MCP\nrequest，已越过 execution-start boundary 的调用保持 unknown outcome，不能自动重放。\n\n## 8. 模型调用与流式处理\n\n一个 Turn 固定（durable policy revision binding 已实现；完整 execution limits/agent role\n快照仍为**部分**，见\n[`zeta-agent-runtime-architecture.md` R1](zeta-agent-runtime-architecture.md#41-r1策略冻结-durable-化)——\n`TurnAccepted` 持久化 policy revision，进程内 `TurnPolicySnapshot` 只是冻结 fact 的派生\n视图。模型选择已通过 `TurnAccepted` 携带 model 实现冻结）：\n\n```rust\npub struct TurnPolicySnapshot {\n    // approval, sandbox intent, capability scope,\n    // execution limits, agent role and policy revision\n}\n```\n\n每次 provider 调用当前固定 selected model、`ContextPlan` 与 tools；独立\nprovider/config/catalog revision 集合仍是扩展点：\n\n```rust\npub struct ModelInvocationSnapshot {\n    // resolved model, ContextPlan, tools, output/reasoning settings,\n    // provider/config/catalog revisions and invocation identity\n}\n```\n\n`ModelService` 契约必须：\n\n- 可取消；当前端口保持同步，真实 wire streaming 通过 sink 扩展；\n- 输入 canonical request/snapshot，而不是 prompt `String`；\n- 输出 typed stream event 与 typed terminal response；\n- 区分 refusal、overflow、rate limit、authentication、transport 与 invalid response；\n- 保留受控 usage/response reference，不泄漏 wire DTO；\n- 不在 trait 内选择全局默认模型。\n\nTransient streaming 必须携带 stream incarnation 与 cursor，通过有界 channel 发布。Channel\n饱和时允许合并或丢弃非关键 delta；durable completion 不能依赖 transient stream，也不能被它\n阻塞。\n\n## 9. 上下文\n\n每次 model invocation 都从以下不可变输入派生：\n\n```text\ndurable Thread snapshot\n+ TurnPolicySnapshot\n+ resolved model limits/capabilities\n+ instruction/environment snapshot\n+ available Tool definitions\n+ verified compaction checkpoints\n+ optional AgentContextSeed / delivered Agent results\n```\n\n组织过程固定为：\n\n```text\nContextManager\n  → validate source revisions\n  → select history/injected fragments/checkpoint\n  → apply precedence\n  → resolve model-neutral budget through zeta-context-engine\n  → produce ContextPlan\nContextAssembler\n  → validate structural invariants\n  → assemble provider-neutral ModelRequest\n```\n\nCompaction 不删除 event log。任何影响未来 prompt 的 checkpoint、seed 或 result 都必须先成为\ndurable fact。ContextManager 的缓存和 baseline 可以丢失。详细的 ownership、预算、compaction、\n恢复与目录设计见 [`core-context.md`](core-context.md)。\n\n## 10. 多 Agent\n\n多 Agent 使用同一 Session 下的独立 Thread：\n\n```text\nSession\n└─ parent Thread\n   ├─ child Thread A\n   └─ child Thread B\n```\n\n但三种关系不能混用：\n\n- fork lineage：产品分支关系；\n- Agent delegation：谁委派了什么工作；\n- context inheritance：child 首次 invocation 能看到什么。\n\nSpawn 必须固定 parent Thread sequence，并选择显式 inheritance mode。Spawn 后父子 context\n立即分叉，不共享可变历史。父子 communication/result 使用 stable message/delegation ID 和\ndurable delivery；不能直接读取对方 ContextManager，也不能自动把 child transcript 拼进 parent\nprompt。\n\nSession 只串行拓扑提交；MultiAgentCoordinator 负责 delegation；ThreadController 负责各自\nThread；ContextManager 负责各自 context。详细契约见\n[`core-multi-agent.md`](core-multi-agent.md)。\n\n## 11. 工具、交互与能力\n\n### 11.1 工具边界\n\n```text\nmodel emits Tool Calls\n→ durable Tool Call intents\n→ ToolService materializes ActionReviewRequest\n→ ActionPolicyService evaluates sandbox / grant / approval / block\n→ durable approval wait and exact continuation when required\n→ durable ToolExecutionStarted with action/policy/authority identity\n→ execute outside Thread writer\n→ durable ordered Tool Results / UnknownOutcome\n→ next model invocation\n```\n\n`ToolScheduler` 拥有校验、approval gate、并行计划、deadline、cancellation、operation correlation、\ndeterministic result ordering、retry 与 unknown-outcome 决策。\n\n`ToolService` adapter 拥有具体参数转换、MCP/process/browser/terminal I/O、sandbox enforcement、\n有界输出采集和 tool-specific reconciliation。Adapter 不得写 Thread store。\n\n并行 Tool 只有在 policy、Tool definition 和 resource conflict 检查均允许时启用。完成顺序不决定\ntranscript 顺序；默认按 model call order 提交结果。\n\nRetry policy 使用自描述 enum：\n\n```text\nNever\nSafeRead\nIdempotentWrite(operation key)\nReconcileBeforeRetry\n```\n\n未声明幂等性的写操作和 outcome 未知的副作用默认不自动重试。\n\n### 11.2 交互\n\nApproval、structured user input、dynamic tool 和 capability request 使用同一模式：\n\n```text\nCore commits InteractionRequested\n→ App Server selects owner and delivers\n→ client resolves exact RequestId\n→ Core validates Thread/Turn/RequestId/sequence\n→ Core commits InteractionResolved/Cancelled\n→ execution resumes at a safe point\n```\n\nCore 拥有 durable request/wait/resolve/cancel。App Server 拥有 connection owner、投递、\ndisconnect 和 outbound queue。\n\nApproval 使用独立 typed payload，不复用普通 user-input 文本：\n\n```text\nActionApprovalRequest\n  action_digest: SHA-256\n  policy_revision\n  complete capability set\n  reason\n  sandbox_denial?: structured safe-to-retry denial\n\nActionApprovalResponse\n  ApproveOnce | Decline\n```\n\nCore 的 `ActionPolicyService` 接收 immutable `ActionReviewRequest` 并返回 `ExecutionDecision`。\n确定性规则由注入的 `zeta-action-policy` authority 消费 `zeta-execpolicy` snapshot 后求值；Core\n不解析规则。`RunExecPolicyGranted` 会被物化为绑定 exact `ToolCallId` 的\n`ExecPolicyToolGrant`，并在副作用前持久化为包含 rule ID 与 exec-policy revision 的\n`ToolExecutionAuthority::ExecPolicyGranted`。\n`AskUser` 必须先通过 `durable_approval_request` 绑定并转换，之后才能提交\n`InteractionRequested`。Reducer 校验 digest、revision、非空 capability scope 和重复\ncapability，并把 Turn 转为 `WaitingForApproval`。响应通过原有的 RequestId、Thread/Turn identity\n和 sequence gate 提交；`ApproveOnce` 只表示该 exact request 获得一次性批准，不创建持久或\n跨 action grant。Tool scheduler 将它物化为绑定 RequestId、ToolCallId 和完整 approval payload\n的 `OneTimeToolGrant`，并在恢复时重新验证 action digest、policy revision 与 capability。\n\n当 sandboxed attempt 返回结构化 `SafeToRetry` denial 时，Core 先执行二次 policy review。\n`AskUser` 会把完整 denial 写入新的 durable `ActionApprovalRequest`，不先提交 Tool Result。\n`ApproveOnce` 只授权原始 action/policy/capability/ToolCall 组合的一次非 sandbox 重试；Core 在重试前\n提交 `ToolExecutionEscalated`。若重启后已有 escalation marker 而没有 Tool Result，scheduler\n提交 outcome-unknown failure 并禁止再次执行。`Decline`、`MayHaveSideEffects` denial 或 binding\n漂移都不会触发重试。\n\n副作用开始前必须提交 `ToolExecutionStarted`。如果恢复时存在 unresolved Tool Call 但没有\nstart marker，scheduler 可以从 policy safe point 继续；如果 start marker 已存在而 Tool Result\n缺失，则提交 outcome-unknown Tool failure，禁止自动重放。Host 在完成 Tool/Policy service\n装配后通过 `TurnExecutor::resume_recovered_tool_continuations` 恢复这些 Turn。\n\n## 12. 恢复、错误与背压\n\n### 12.1 恢复原则\n\n恢复只依赖 durable facts：\n\n- terminal Turn 保持 terminal；\n- waiting interaction 恢复为 waiting；\n- in-flight model request 从最后 safe point 按明确 policy 重建；\n- 已提交 Tool intent 但没有 result 的副作用调用进入 `UnknownOutcome` 或 reconciliation；\n- `Cancelling` 最终收敛到 `Interrupted`；\n- reload 生成新 Thread incarnation，旧 completion 全部拒绝；\n- Session create/fork/spawn saga 继续或补偿；\n- Agent message/result delivery 通过 stable ID 去重并继续；\n- ContextManager 从 Thread history、checkpoint 与 seed 重建。\n\n### 12.2 错误分类\n\nCore error 至少区分：\n\n- validation / invalid transition；\n- optimistic concurrency / command conflict；\n- aggregate not found；\n- store / lease failure；\n- model config/capability/transient/permanent failure；\n- Tool unavailable/denied/failed/unknown outcome；\n- interaction timeout/cancel/owner unavailable；\n- delegation unavailable/failed/delivery conflict；\n- overloaded/closing/stale completion；\n- internal invariant violation。\n\n内部错误可以保留 source chain；wire mapping 只暴露稳定、安全的 code 与 message。\n\n### 12.3 背压\n\n以下集合必须有界：\n\n- per-Thread mailbox；\n- model delta channel；\n- parallel Tool set；\n- active child Agent set；\n- cross-Agent delivery queue；\n- committed/transient update publisher；\n- App Server outbound queue。\n\nTransient delta 可以合并或丢弃；durable event、terminal outcome、interaction request 和 Agent\nresult 不能无声丢失。\n\n## 13. 目标目录与公开 API\n\n以下是**目标目录**，不是当前布局。当前实现为扁平模块\n（`session_coordinator.rs`、`thread_controller.rs` 及其子目录、`thread_reducer.rs`、\n`turn/`、`context/assembler.rs` 等）；`thread_controller.rs`（1189 LoC）、\n`session_coordinator.rs`（998 LoC）与 `thread_reducer.rs`（947 LoC）已越过下方 800 LoC\n约束，是\n[`zeta-agent-runtime-architecture.md` 阶段 A](zeta-agent-runtime-architecture.md#阶段-a地基修整)\n的拆分对象；在拆分完成前，这三个文件不再接受新功能。\n\n```text\nzeta-rs/core/src/\n├─ lib.rs\n├─ error.rs\n├─ services.rs\n├─ session/\n│  ├─ coordinator.rs\n│  ├─ reducer.rs\n│  ├─ projection.rs\n│  └─ saga.rs\n├─ thread/\n│  ├─ controller.rs\n│  ├─ loaded.rs\n│  ├─ reducer.rs\n│  ├─ projection.rs\n│  ├─ commit.rs\n│  └─ recovery.rs\n├─ turn/\n│  ├─ executor.rs\n│  ├─ lifecycle.rs\n│  ├─ invocation.rs\n│  └─ interaction.rs\n├─ context/\n│  ├─ model.rs\n│  ├─ plan.rs\n│  ├─ assembler.rs\n│  └─ validation.rs\n├─ context_manager/\n│  ├─ manager.rs\n│  ├─ selection.rs\n│  ├─ window.rs\n│  └─ compaction.rs\n├─ multi_agent.rs\n├─ multi_agent/\n│  ├─ coordinator.rs\n│  ├─ budget.rs\n│  ├─ context.rs\n│  └─ coordinator_tests.rs\n└─ tool/\n   ├─ scheduler.rs\n   ├─ policy.rs\n   └─ outcome.rs\n```\n\n模型无关预算、精准/估算 token 计量结果和边界判定位于同级 `zeta-context-engine` crate，不在 Core\n目录内复制；Core `context` 只保留 Thread 内容选择、结构校验与组装。\n\n约束：\n\n- 模块默认 private，只从 `lib.rs` named export 稳定 API；\n- implementation module 目标低于 500 LoC；\n- 超过约 800 LoC 的文件不继续增加新功能；\n- 新测试使用 sibling `*_tests.rs` 与显式 `#[path = \"..._tests.rs\"]`；\n- 不创建 `common.rs`、`utils.rs`、service registry 或巨型 facade；\n- public trait 必须有职责与实现约束 doc comment；\n- public API 不使用语义含糊的 bool/Option 参数；\n- loaded phase、mailbox message、task handle、ContextManager cache 与 incarnation 不 re-export。\n\n公开面只保留：\n\n- `SessionCoordinator` / `ThreadController` / `MultiAgentCoordinator` handle；\n- typed request/result；\n- read-only projection/snapshot；\n- consumer-owned service ports；\n- 稳定 Core error；\n- 最小 update/subscription source。\n\n## 14. 测试与演进\n\n### 14.1 验证矩阵\n\nPure tests：\n\n- Session/Thread/delegation reducer 的合法与非法 transition；\n- context precedence、Tool 配对、budget 与 checkpoint selection；\n- Tool schedule 与 deterministic aggregation；\n- snapshot 和 safe-point semantics。\n\nConcurrency tests：\n\n- 同 Thread FIFO、不同 Thread/Agent 并行；\n- I/O 期间 projection lock 和 writer lease 已释放；\n- interrupt/completion race；\n- interrupt during active model transport / Tool execution；\n- duplicate/late/stale-incarnation completion；\n- child cancellation 不影响 parent/sibling；\n- mailbox、Tool、Agent capacity saturation；\n- idle eviction 后恢复。\n\nFault-injection tests：\n\n- Turn accepted/started；\n- Tool Call committed/execution started/result committed；\n- interaction requested/delivered/resolved；\n- Session Thread planned/created/attached；\n- delegation requested/child created/result sent/result received；\n- compaction generated/checkpoint committed。\n\n### 14.2 落地顺序\n\n跨层阶段划分（阶段 A–E）与完成条件由\n[`zeta-agent-runtime-architecture.md` §7](zeta-agent-runtime-architecture.md#7-分阶段实施计划)\n权威维护；下面是 Core 内部视角的对应顺序：\n\n1. 将现有 Session/Thread 大文件按 aggregate 拆入目标目录（阶段 A）；\n2. 已引入 `LoadedThreadState`、显式 incarnation、idle eviction、FIFO mutation gate 和有界\n   execution mailbox；\n3. 已落地 durable policy revision binding 与 `ModelInvocationSnapshot` 核心纵向切片；完整\n   policy/provider revision 集合按真实需求扩展；\n4. 已引入独立 `context/` 与 `context_manager`，完成纯内容选择、checkpoint 与 compaction；通用预算\n   数学和 token 计量契约已提取到 `zeta-context-engine`；\n5. 扩展现有 ToolScheduler：已完成 durable one-time approval、safe sandbox escalation、顺序\n   Tool 与 UnknownOutcome 基线，后续增加并行计划、deadline、声明式 retry、reconciliation 与\n   resource conflict；\n6. 已增加 `multi_agent.rs`、`MultiAgentCoordinator`、delegation protocol 与 Fresh context\n   inheritance；Selected/ForkedPrefix、durable join 和 cancellation tree 继续演进；\n7. 完成 provider wire streaming、outbound writer 与 fault injection；\n8. 通过 context continuity、多 Agent 隔离、取消和副作用恢复评测。\n\n当前不创建独立 `zeta-agent` crate。只有出现至少两个真实执行宿主，并证明 Agent loop 不依赖\nThread projection、store、command receipt 或 App Server 时，才评审提取。\n\n## 15. 固定决策\n\n- Core 是 Agent 执行控制面，不是通用业务服务层；\n- Session 是任务与 Thread 拓扑边界，不是共享 history/context；\n- Thread 是 history、context、执行和恢复边界；\n- 每个 loaded Thread 一个 ContextManager，不共享可变 context；\n- ContextManager 是可重建派生协调状态，不是第二份 canonical history；\n- ContextAssembler 是纯组装组件，不承担 context 生命周期；\n- 模型无关 context budget 与 token measurement 属于 `zeta-context-engine`；\n- Agent delegation、fork lineage 与 context inheritance 是三种不同关系；\n- child Agent 使用独立 Thread，父子只通过 durable seed/message/result 交流；\n- 每个 Thread 的 context budget 与整个 Agent tree 的资源预算分开；\n- 每个 Thread 一个逻辑写者，不要求常驻 actor；\n- model、Tool、Agent I/O 永远在 durable commit 临界区之外；\n- Tool 调度属于 Core，Tool 实现与 sandbox enforcement 不属于 Core；\n- Core 通过 consumer-owned typed ports 编排服务；\n- composition、Config、credential、transport 和 connection lifecycle 留在宿主层；\n- Core 不把 `Runtime` 设计成组件、aggregate 或公共 API；\n- transient delta 可丢失，final Item 和 terminal state 必须 durable；\n- crash 后不静默重放 outcome 未知的副作用；\n- model、provider、tool、policy 与 context 配置只在 safe point 进入新 snapshot。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-定位",
        "title": "1. 定位"
      },
      {
        "depth": 2,
        "id": "2-当前状态",
        "title": "2. 当前状态"
      },
      {
        "depth": 2,
        "id": "3-所有权边界",
        "title": "3. 所有权边界"
      },
      {
        "depth": 3,
        "id": "31-核心拥有",
        "title": "3.1 核心拥有"
      },
      {
        "depth": 3,
        "id": "32-核心不拥有",
        "title": "3.2 核心不拥有"
      },
      {
        "depth": 3,
        "id": "33-容易混淆的边界",
        "title": "3.3 容易混淆的边界"
      },
      {
        "depth": 2,
        "id": "4-产品模型与执行模型",
        "title": "4. 产品模型与执行模型"
      },
      {
        "depth": 3,
        "id": "41-product-session",
        "title": "4.1 Product Session"
      },
      {
        "depth": 3,
        "id": "42-thread",
        "title": "4.2 Thread"
      },
      {
        "depth": 3,
        "id": "43-agent",
        "title": "4.3 Agent"
      },
      {
        "depth": 3,
        "id": "44-turn-与操作",
        "title": "4.4 Turn 与操作"
      },
      {
        "depth": 2,
        "id": "5-核心组件",
        "title": "5. 核心组件"
      },
      {
        "depth": 3,
        "id": "51-sessioncoordinator已实现",
        "title": "5.1 SessionCoordinator（已实现）"
      },
      {
        "depth": 3,
        "id": "52-threadcontroller已实现",
        "title": "5.2 ThreadController（已实现）"
      },
      {
        "depth": 3,
        "id": "53-loadedthreadstate已实现",
        "title": "5.3 LoadedThreadState（已实现）"
      },
      {
        "depth": 3,
        "id": "54-turnexecutor已实现顺序循环",
        "title": "5.4 TurnExecutor（已实现：顺序循环）"
      },
      {
        "depth": 3,
        "id": "55-turnexecutionbackend已实现",
        "title": "5.5 TurnExecutionBackend（已实现）"
      },
      {
        "depth": 3,
        "id": "56-contextmanager-与-contextassembler核心纵向切片已实现",
        "title": "5.6 ContextManager 与 ContextAssembler（核心纵向切片已实现）"
      },
      {
        "depth": 3,
        "id": "57-multiagentcoordinator部分实现",
        "title": "5.7 MultiAgentCoordinator（部分实现）"
      },
      {
        "depth": 2,
        "id": "6-依赖方向与服务端口",
        "title": "6. 依赖方向与服务端口"
      },
      {
        "depth": 2,
        "id": "7-durable-commit并发与安全点",
        "title": "7. Durable commit、并发与安全点"
      },
      {
        "depth": 3,
        "id": "71-提交顺序",
        "title": "7.1 提交顺序"
      },
      {
        "depth": 3,
        "id": "72-安全点",
        "title": "7.2 安全点"
      },
      {
        "depth": 3,
        "id": "73-取消",
        "title": "7.3 取消"
      },
      {
        "depth": 2,
        "id": "8-模型调用与流式处理",
        "title": "8. 模型调用与流式处理"
      },
      {
        "depth": 2,
        "id": "9-上下文",
        "title": "9. 上下文"
      },
      {
        "depth": 2,
        "id": "10-多-agent",
        "title": "10. 多 Agent"
      },
      {
        "depth": 2,
        "id": "11-工具交互与能力",
        "title": "11. 工具、交互与能力"
      },
      {
        "depth": 3,
        "id": "111-工具边界",
        "title": "11.1 工具边界"
      },
      {
        "depth": 3,
        "id": "112-交互",
        "title": "11.2 交互"
      },
      {
        "depth": 2,
        "id": "12-恢复错误与背压",
        "title": "12. 恢复、错误与背压"
      },
      {
        "depth": 3,
        "id": "121-恢复原则",
        "title": "12.1 恢复原则"
      },
      {
        "depth": 3,
        "id": "122-错误分类",
        "title": "12.2 错误分类"
      },
      {
        "depth": 3,
        "id": "123-背压",
        "title": "12.3 背压"
      },
      {
        "depth": 2,
        "id": "13-目标目录与公开-api",
        "title": "13. 目标目录与公开 API"
      },
      {
        "depth": 2,
        "id": "14-测试与演进",
        "title": "14. 测试与演进"
      },
      {
        "depth": 3,
        "id": "141-验证矩阵",
        "title": "14.1 验证矩阵"
      },
      {
        "depth": 3,
        "id": "142-落地顺序",
        "title": "14.2 落地顺序"
      },
      {
        "depth": 2,
        "id": "15-固定决策",
        "title": "15. 固定决策"
      }
    ],
    "searchText": " 物理位置：zeta-rs/core/ Rust crate：zetacore Canonical value 与 wire-neutral contract：protocol.md 上下文详细设计：core-context.md 多 Agent 详细设计：core-multi-agent.md 跨 Core、App Server、provider 与 Tool 的执行演进： zeta-agent-runtime-architecture.md Tool shared contract、registry/binding 与 source adapter：tools.md Config、Plugin、MCP 与 Skill snapshot：config.md Provider credential：model-provider.md Secret persistence：secrets.md Cancellation tree 实现：zeta-async-utils README Session/Thread store ports：zeta-session-store / zeta-thread-store Local recovery composition：zeta-rollout 快速理解 Core 是一次 Agent 工作的权威协调者：它推进 Turn、安排模型和工具调用，并保证状态按可恢复的 顺序提交，但不亲自实现模型、工具、网络或界面。 读者首先会问 直接答案 深入阅读 --- --- --- 一次工作保存在哪里？ Session 聚合多个 Thread；每个 Thread 独立保存 Turn、Item 和逻辑序列 产品模型 谁推进一个 Turn？ ThreadController 保持单写者顺序，选定的 TurnExecutionBackend 推进执行；默认实现是 TurnExecutor 核心组件 模型、工具和策略由谁实现？ Core 只拥有消费方端口和调用顺序，具体实现由外部服务注入 依赖方向 什么时候算已经执行？ 执行授权和开始事实必须先持久化，之后才能跨过副作用边界 提交与安全点 Core 是否拥有 UI 或传输？ 不拥有；Desktop、CLI、TUI 和 App Server 只是不同入口 所有权边界 1. 定位 zeta-core 是 Zeta 的 Agent 执行控制面，只负责五类事情： 1. Agent 生命周期； 2. Turn 执行； 3. 上下文管理； 4. Tool 调度； 5. 服务编排。 这里的“Agent 生命周期”包括 Session、Thread、Turn、delegation 的 durable 状态迁移，以及加载、 取消、等待、恢复和执行实例隔离。模型进程、MCP server、浏览器、终端和网络连接自身的生命周期 不属于 Core。 “服务编排”表示 Core 通过窄的 typed port 规定 model、tool、store、policy 与 capability 的调用 顺序、提交边界和 failure semantics。它不表示 service locator、依赖注入容器或具体服务实现。 Core 必须保留纯 reducer、durable commit ordering 和 recovery。它们不是额外的产品能力，而是 Agent 生命周期能够成为 authority 的前提。 长期不建立 CoreRuntime、AgentRuntime、SessionRuntime 或 ThreadRuntime 公共组件。 进程内执行状态确实存在，但由 ThreadController、LoadedThreadState、TurnExecution 等准确 名称表达。只有 Tokio、Wasm 或外部 Tool execution environment 这类真正的执行环境才使用 runtime 一词，而且其实现不属于 Core。 2. 当前状态 已经落地： - Session/Thread reducer、projection 与 store port； - SessionCoordinator 及可恢复 create/fork saga； - ThreadController、command receipt、replay、conflict detection 与 recovery； - per-Thread loaded projection、FIFO mutation gate、explicit incarnation、bounded execution mailbox 与 idle eviction； - durable Turn、Tool Call、Tool Result 与 interaction lifecycle； - provider-independent ModelService； - TextDelta / ReasoningDelta Core streaming contract； - TurnExecutor 的顺序 model → tool → model 循环； - HookService 的 beforeTool、afterTool 与 durable Turn completion safe-point 调度； - Tool 内用户输入的 durable request、exact live waiter 唤醒、取消与重启后 unknown-outcome 收口； - TurnExecutionBackend 委托边界、Codex 完整 Turn adapter 与 immutable remote thread binding； - durable Turn policy revision binding 与 recovery fail-closed； - ContextInput / ContextPlan、纯 planner、per-loaded-Thread ContextManager、 ContextPlan → ModelRequest 的 ContextAssembler； - durable context checkpoint、model-backed compaction、commit 后重规划与当前 ModelInvocationSnapshot 纵向切片。 尚未完成： - App Server 独立 outbound worker； - 完整 TurnPolicySnapshot 中除 revision 外的 execution limit/agent role 集合，以及 ModelInvocationSnapshot 的独立 provider/config/catalog revision 集合； - 其余 provider/local tokenizer adapter、usage 校准、prompt cache/reference baseline 与跨 Thread context seed； - 并行 Tool、通用 deadline、声明式 retry 与 reconciliation； - durable multi-Agent delegation、跨 Thread message/result 与 Agent tree resource budget； - 所有 durable boundary 的 fault injection。 当前实现是演进地基，不应被描述成完整的异步、多 Agent 执行系统。 跨层组件状态总账与状态标记词表（已实现 / 部分 / 仅设计 / 推迟）由 zeta-agent-runtime-architecture.md 维护；本文各组件章节挂对应标记，不重复总表。 3. 所有权边界 3.1 核心拥有 - Session membership、lineage、shared defaults 与 lifecycle 的协调； - 每个 Thread 的逻辑单写者、durable commit、加载和恢复； - Turn acceptance、执行、等待、取消、完成、失败和恢复； - Agent delegation 的 spawn、message、join、cancel 与结果合并； - command idempotency、expected sequence 和 committed update ordering； - Turn policy snapshot 与 model invocation snapshot； - 从 durable facts 派生 provider-neutral context； - context window、预算、checkpoint 选择和 compaction 协调； - model → tool → model loop 及 sequential/parallel Tool 调度； - approval、structured user input、dynamic tool 与 capability wait； - cancellation tree、deadline、迟到结果和旧 incarnation 拒绝； - crash 后模型、Tool、interaction 与 delegation 的恢复决策； - 将外部错误映射为稳定 Core outcome。 3.2 核心不拥有 - canonical shared value 的定义；它属于 zeta-protocol； - stored envelope、event framing、JSONL/SQLite 和 fsync； - provider HTTP DTO、endpoint、SSE decoder、认证与 credential； - model catalog 的发现、缓存、合并与筛选； - Tool、MCP transport、child process、browser 或 terminal 的具体实现； - sandbox/OS enforcement、approval UI 与 connection owner； - Config 文件、环境变量、secret persistence 或 credential refresh； - JSON-RPC method、subscription cursor 与 transport lifecycle； - Desktop、CLI、TUI 的 projection； - provider prompt cache、连接池或模型驻留； - 未经单独设计和授权的跨 Thread 长期 memory。 3.3 容易混淆的边界 概念 Core 拥有 外层拥有 --- --- --- Model 调用时机、snapshot、结果消费、safe point provider 解析、HTTP、认证、wire codec Tool 选择、approval、排序、取消、结果提交、失败决策 schema adapter、执行、sandbox、MCP/进程 I/O Context 选择策略、预算、compaction 协调、纯组装 provider cache、未经授权的 memory retrieval Agent delegation、Thread 绑定、消息、结果、取消、预算 UI 呈现、远端进程或 capability 生命周期 Service orchestration 调用 typed ports、状态推进、补偿与降级 构造 adapter、配置加载、进程启动、网络监听 4. 产品模型与执行模型 4.1 Product Session Session 是产品级根 aggregate，拥有： - task lifecycle； - Thread membership； - root/fork/spawn lineage； - shared defaults； - 自己的 durable sequence。 Session 不持有任何 Thread transcript，不代理 token delta，也不等待 model/Tool I/O。同一 Session 下的 Thread 必须能够并行执行。 4.2 Thread Thread 是独立的历史、排序、上下文和恢复边界，拥有： - immutable SessionId； - durable sequence； - ordered Turn 与 Item； - compaction checkpoint； - pending interaction/delegation facts； - 一个逻辑状态提交者。 每个已加载 Thread 可以持有一个可丢弃、可重建的 ContextManager。ContextManager 不改变 Thread 的 authority；它只能从 durable Thread facts 和不可变 snapshot 派生状态。 4.3 Agent 当前长期方案中，一个活跃 Agent 绑定一个 Thread。ThreadId 可以先作为 Agent 的执行身份， 不急于增加一对一的 AgentId aggregate。父子工作的相关性由独立 DelegationId 表达。 只有未来出现“一个 Agent 身份需要跨多个 Thread 延续”的真实需求时，才引入 AgentId。 无论是否引入，context 始终绑定 Thread，而不是绑定 Session 或整个 Agent tree。 4.4 Turn 与操作 必须区分： ``text Product lifecycle Session / Thread / Turn / Delegation 的 durable status Loaded execution lifecycle Thread load / idle / executing / closing + incarnation Operation lifecycle model invocation / tool execution / interaction delivery / agent message delivery ` 三层不能复用一个 status、operation ID 或 cancellation token。 5. 核心组件 `text MultiAgentCoordinator multi-Agent only ├─ topology operations ──► SessionCoordinator └─ cross-Thread work ────► ThreadController SessionCoordinator ────────► ThreadController membership / defaults one logical writer │ LoadedThreadState └─ ContextManager │ ▼ TurnExecutor context → model → tools → model │ │ │ ▼ ▼ ▼ ContextPlan ToolScheduler interactions │ │ ▼ ▼ ModelService ToolService ` 5.1 SessionCoordinator（已实现） 负责： - 创建、读取和恢复 Session； - create、attach、fork、spawn、archive Thread 的结构协调； - root/parent/child lineage； - Session lifecycle 与 shared defaults； - 跨 Session/Thread stream 的可恢复 saga。 不负责： - Thread transcript 或 context； - Turn loop； - token/reasoning streaming； - 等待 model、Tool 或 child Agent 完成。 5.2 ThreadController（已实现） 每个已加载 Thread 对应一个逻辑状态提交者。实现可以是 mailbox task 或 keyed executor，但必须 满足： - 同一 Thread 的结构性 mutation FIFO； - 不同 Thread 可并行； - model、Tool 和 Agent I/O 不占 projection lock 或 writer lease； - interrupt、interaction response、Tool completion、model completion 和 Agent result 都回到 同一提交序列； - 每次 load 生成新的 ThreadIncarnationId； - completion 携带 operation ID 与 incarnation； - 空闲状态可以回收，恢复只依赖 durable facts。 “一个 Thread 一个常驻 task”只是实现选择，不是公共契约。 5.3 LoadedThreadState（已实现） 这是 Core-private 的进程内状态： `text LoadedThreadState ├─ durable projection snapshot ├─ ThreadIncarnationId ├─ ThreadActivity │ ├─ Idle │ ├─ Executing TurnExecution │ └─ Waiting Interaction Delegation ├─ ContextManager ├─ bounded mailbox └─ cancellation/task ownership ` 它不能进入 protocol，也不能成为第二个 durable aggregate。进程重启后必须能够丢弃并重建。 当前实现由 private loadedthread::LoadedThreads 保存轻量 Thread slot registry； loadedthread::ThreadSlot 的 ticket gate 串行结构性 mutation，slot 内的 LoadedThreadState 保存 projection 与 incarnation。mailbox::ThreadExecutionMailboxes 为每个 incarnation 创建有界 FIFO lane，ThreadExecutionContext 将 operation ID、 incarnation 与 cancellation 绑定。worker 空闲 30 秒后只回收匹配 incarnation 的 lane 和 projection；下一次访问从 ThreadStore 重建。运行中的 execution 会阻止显式 recovery， 因此 completion 不可能跨越 incarnation；测试中的强制 stale context 也会被拒绝。 5.4 TurnExecutor（已实现：顺序循环） 负责一个已经 accepted 的 Turn： `text freeze TurnPolicySnapshot → prepare context → create ModelInvocationSnapshot → invoke model → consume stream/final response → no Tool Call ──────────────────────────────► complete → validate and schedule Tool Calls → approval if required → durable Tool Call intent → execute Tools → durable Tool Result / UnknownOutcome → prepare next invocation ` TurnExecutor 只能提出 typed effect；ThreadController 负责实际 durable append。它不能直接修改 projection、写 store 或提前发布 committed update。 当前单 Agent loop 不设置固定的模型调用次数上限：只要模型仍产生 Tool Call 等 follow-up， Turn 就继续执行。安全边界应由可取消的 token/cost/deadline policy 和 durable usage accounting 表达，不能使用 approval 或 recovery 后会重置的进程内计数器。 5.5 TurnExecutionBackend（已实现） TurnExecutionBackend 是推进已创建 durable Turn 的 consumer-owned port。TurnExecutor 实现 Zeta 自己的 provider-independent model/tool loop；外部 runtime 若同时拥有 Agent loop、Tool 和 approval（例如 Codex App Server），则实现这个 port，而不是伪装成 ModelService。 Core 始终保留 Thread event、interaction、cancellation 和 terminal outcome authority。委托后端只能 通过 ThreadController 提交 durable item/interaction；远端 thread continuity 使用 immutable TurnExecutionBinding 保存，并绑定 opaque execution scope。后端在第一次远程请求前写入 TurnExecutionAttempted；因此 in-flight remote Turn 不可在未知结果后透明重放。 5.6 ContextManager 与 ContextAssembler（核心纵向切片已实现） 两者必须分开： - ContextManager：每个 loaded Thread 一个，协调 sequence 检查与 prepare；当前不持有 cache、 baseline 或 token estimate； - ContextAssembler：无状态纯组件，把已经确定的 ContextPlan 组装成 provider-neutral ModelRequest； - ContextPlan：一次 invocation 的不可变派生结果； - durable Thread history：唯一权威历史。 未来若有性能证据，ContextManager 可以缓存按 ThreadSequence、policy revision 和 model capability revision 索引的派生视图，但不能维护第二份 canonical transcript。完整契约见 core-context.md。 5.7 MultiAgentCoordinator（部分实现） 这是只在多 Agent 模式下参与的跨 Thread 协调组件。当前已负责 Fresh spawn、durable delegation、message/result delivery、recovery 与结构性 tree budget；目标完整职责包括： - spawn、send、join、cancel、close； - durable delegation 与 delivery identity； - parent/child cancellation； - 最大深度、子 Agent 数、并发、token/cost/deadline budget； - 协调 SessionCoordinator 创建 child Thread； - 协调跨 Thread message/result 的 durable delivery。 它不持有任何 Thread context，也不读取 live ContextManager。父子 Agent 的上下文传递必须通过 不可变 AgentContextSeed，结果必须通过显式 durable Item 合并。完整契约见 core-multi-agent.md。 单 Agent 执行已经由 ThreadController、TurnExecutor、ContextManager 和 ToolScheduler 完整表达， 不再建立泛化的 agent/ facade。multiagent/ 只包含单 Thread 之外的 delegation、spawn、 message、join、tree budget 和 recovery。 6. 依赖方向与服务端口 目标依赖： `text zeta-protocol ▲ ┌────────────┼────────────┐ │ │ │ zeta-session-store zeta-core zeta-thread-store ▲ ▲ ▲ │ │ ports │ └──── composition root ───┘ │ model / tool / policy / capability adapters ` 允许： - zeta-core → zeta-protocol； - zeta-core → zeta-history，构造与回放 persisted Thread record； - zeta-core → zeta-tools； - zeta-core → zeta-session-store； - zeta-core → zeta-thread-store； - zeta-core → zeta-async-utils； - 外层 adapter 实现 Core 定义的 consumer-owned port。 禁止： `text zeta-core → zeta-app-server -protocol zeta-core → zeta-storage / zeta-rollout zeta-core → zeta-api / concrete model provider zeta-core → shell-command / file-system / apply-patch / MCP implementation zeta-core → config files / credentials / secrets zeta-core → Desktop / CLI / TUI ` 最小端口集合： Port 状态 Core 为什么消费 实现位置 --- --- --- --- SessionStore 已实现 Session load/append storage adapter ThreadStore 已实现 Thread load/append storage adapter WriterLease<Id 已实现 aggregate 单写者 storage/host TurnExecutionBackend 已实现 推进完整 Turn，同时保留 Core state authority TurnExecutor / Codex adapter ModelService 已实现（同步；流式默认桥接见 ModelStreamSink） provider-neutral model invocation model-provider adapter ToolService 已实现（含 prepare / reviewevidence / executestreaming） 已物化 Tool call 的执行 built-in/MCP host ToolInteractionService 已实现 工具运行中提交 durable 用户输入请求，并等待 exact Thread/Turn/request 的响应或取消 Core tool orchestrator；MCP adapter 消费 HookService 已实现 在 Core-owned tool/Turn safe point 调用 host-owned declarative Hook runtime App Server Hook adapter ActionPolicyService 已实现 action appr"
  },
  {
    "slug": "agent-harness-design",
    "sourcePath": "docs/agent-harness-design.md",
    "title": "Agent Harness 设计",
    "description": "一个可用的 coding agent harness 需要的每个环节，对照 Zeta 当前实现：",
    "group": "Agent 与运行时",
    "markdown": "> 状态：Accepted（2026-08-03；同日补齐 loop 失败策略、steering、skills/slash、provider\n> 差异矩阵与评测设计，达到可实现深度）\n> 定位：回答\"Core 的 agent loop 具体怎么搭起来\"——一次模型调用长什么样、Turn 内循环与\n> 失败弹性、steering、提示词组织、工具选择与注册时机、上下文裁剪压缩、prompt cache、评测。\n>\n> 分工：执行内核（单写者、durable commit、恢复、取消）由\n> [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) 负责；上下文生命\n> 周期抽象（ContextPlan/Manager/checkpoint）由 [`core-context.md`](core-context.md) 负责；\n> 工具三层契约（定义/绑定/执行接口）由 [`tools.md`](tools.md) 负责；**逐工具规格（schema、\n> 描述正文、错误文案）与系统提示词扩写正文**由\n> [`agent-tools-spec.md`](agent-tools-spec.md) 负责。本文负责 harness 的**行为策略**。\n\n## 快速理解\n\n| 问题 | 结论 | 深入阅读 |\n| --- | --- | --- |\n| 模型调用失败怎么办？ | 按错误类别分流：退避重试 / 压缩后重试 / 立即失败 / 作为正常结果完成 | [§7](#7-turn-内循环与失败策略) |\n| Agent 运行中用户发消息怎么办？ | 排队 + 下一个安全点注入当前 Turn；reducer 已允许，缺一条 `turn/steer` 命令 | [§8](#8-steering-与并发输入) |\n| 提示词怎么做？ | 四层：静态身份/策略 + per-profile 工具指导 + 会话冻结环境快照 + 工作区指令；动态走 append-only reminder | [§4](#4-提示词) |\n| 工具选哪些？ | 共享七件套 + 按模型家族切编辑工具形态；逐工具规格见 tools-spec | [§5](#5-工具集) |\n| 工具什么时候注册？ | Turn 接受时冻结；内置静态平铺，MCP 超阈值切检索式；不做运行时动态增删 | [§6](#6-工具注册时机) |\n| 上下文怎么裁剪/压缩？ | 输入侧逐条限幅；历史语义单元保留；阈值用 `ModelInfo.effective_auto_compact_token_limit` | [§9](#9-上下文裁剪)、[§10](#10-压缩) |\n| 缓存怎么搞？ | 前缀字节稳定 + append-only；`cache_control` 由 Anthropic adapter 注入 | [§11](#11-prompt-cache) |\n| 怎么知道 harness 变好了？ | 三层任务集 + token/成功率指标 + 前缀稳定性回归断言 | [§14](#14-评测) |\n\n## 1. 现状差距\n\n一个可用的 coding agent harness 需要的每个环节，对照 Zeta 当前实现：\n\n| 环节 | 可用 harness 需要 | Zeta 现状 |\n| --- | --- | --- |\n| System prompt | 每次调用注入身份、策略、工具指导、环境 | 部分：`SYSTEM_PROMPT` 已经通过 `HarnessInstructions` 和 `ContextPlan` 注入；per-profile 工具指导仍未完成 |\n| 环境上下文 | cwd、平台、日期、git 状态、项目指令 | ✅ Local Workspace host 在 model safe point 提供环境与 `.zeta/instructions` snapshot |\n| 工具面 | 读/搜/改/执行闭环 | ❌ 只接了 `shell-command`；`file-system-tool`（仅 read/list/metadata）、`apply-patch`、`file-search` 是孤立 crate |\n| 模型失败弹性 | 429/5xx 退避重试、溢出压缩重试、空响应处理 | ❌ 模型一报错 Turn 即 fail；`ApiError` 只有 `HttpStatus(u16)` 无分类 |\n| Steering | 运行中排队注入用户消息 | 部分：reducer 已允许向 Running Turn 追加 Item、executor 每轮重读 snapshot；缺 `turn/steer` 命令 |\n| 工具结果限幅 | 模型侧截断 + 保留头尾 | 部分：shell 有 256 KiB 执行上限，但无模型输入预算 |\n| 上下文预算 | 窗口估算、溢出显式处理 | ✅ 已知/配置窗口走确定性预算；未知窗口明确退回 provider-managed |\n| 压缩 | 阈值触发、durable checkpoint | ✅ `COMPACTION_PROMPT`、source digest、原子 commit、恢复校验与 commit 后重规划已接通 |\n| Prompt cache | 前缀稳定 + 断点标注 | ❌ 只解析 `cache_read_input_tokens`，不写 `cache_control` |\n| 多 Tool Call/响应 | 模型一次响应多个调用 | ❌ `parallel_tool_calls` 硬编码 `false` |\n| 计划工具 | 长任务显式计划状态 | 部分：`ThreadItem::Plan` 存在，无工具产生它，assembler 跳过它 |\n| 评测 | 任务集 + 指标回路 | ❌ 无 |\n\n## 2. 一次模型调用的目标形态\n\n```text\nModelRequest\n├─ instructions                          ── 静态层（会话内不变，缓存友好）\n│  ├─ 身份 + 指令优先级 + 安全策略          （zeta-prompts SYSTEM_PROMPT，静态）\n│  ├─ 工具使用指导 + 输出风格               （per tool-profile，profile 内静态，\n│  │                                        正文见 agent-tools-spec.md 附录 A）\n│  ├─ Skill 清单                           （名称+一行描述，预算上限内，会话稳定）\n│  └─ 环境快照                             （字段见 §4.2，Thread 首 Turn 冻结）\n├─ input\n│  ├─ [0] 工作区指令 message               （Global `.zeta/instructions`，§4.3，调用内冻结）\n│  ├─ [1..] durable history                （append-only，语义单元完整，\n│  │                                        可含 checkpoint summary 替代更早历史）\n│  └─ [last] 当前 Turn 输入 / steering 消息（+ append-only reminder 块）\n└─ tools                                  ── Turn 接受时冻结的 ToolProfile\n```\n\n三条硬规则，所有后续设计都从它们推导：\n\n1. **前缀稳定**：`instructions`、`tools`、已有 history 在 Turn 之间逐字节不变，新内容只\n   append（[§11](#11-prompt-cache) 的前提）。\n2. **语义单元完整**：Tool Call/Result 成对；一个 Turn 的内容原子保留或原子被 checkpoint\n   吸收（[`core-context.md`](core-context.md) §6.3）。\n3. **变化走安全点**：工具面、模型、策略、环境快照只在 Turn 边界或压缩边界变化。\n\n## 3. Turn 内循环\n\n一次 Turn 的执行循环（现有 `TurnExecutor::execute_steps` 的目标形态）：\n\n```text\nloop:\n  1. 检查取消\n  2. 读最新 durable snapshot（steering 消息在此自然进入）\n  3. 预算检查：估算占用 ≥ 阈值 → 先压缩（§10），压缩失败且未超硬限 → 继续\n  4. ContextManager.prepare → ContextPlan → ContextAssembler → ModelRequest\n  5. 调用模型（含 §7 重试策略），流式 delta 经 sink 发布\n  6. 响应分类：\n     ├─ 有 Tool Call → durable 记录 → ToolScheduler 顺序执行（审批/沙箱/\n     │                  升级语义见 core.md §11）→ 回到 1\n     ├─ 只有文本   → durable 提交 agent message → Turn Completed\n     ├─ Refusal    → 作为最终消息提交 → Turn Completed（不是失败）\n     └─ 空响应     → §7 空响应处理\n```\n\n无固定迭代次数上限（runtime 文档决策）；失控防护见 [§7.3](#73-失控防护)。\n\n## 4. 提示词\n\n### 4.1 四层结构\n\n| 层 | 内容 | 变化频率 | 存放位置 |\n| --- | --- | --- | --- |\n| 身份与策略 | 身份、指令优先级、注入防护、工作行为 | 随产品版本 | `zeta-prompts` `SYSTEM_PROMPT`（已存在） |\n| 工具指导 + 输出风格 | 工具组合惯例、验证纪律、简洁度、路径引用格式 | 随 tool profile | `zeta-prompts` 新模板；正文已写好，见 [`agent-tools-spec.md` 附录 A](agent-tools-spec.md#附录-a系统提示词扩写正文) |\n| 环境快照 | 见 §4.2 | Thread 首 Turn 冻结；压缩边界刷新 | host 提供 `AgentEnvironmentSnapshot`，Core 渲染 |\n| 工作区指令 | Global `.zeta/instructions` | model invocation 内冻结；文件变化影响后续调用 | `input[0]` 独立 message |\n\n外部产品如何组织项目指令只是参照系；Zeta 的原生 artifact、目录和加载策略由\n[`agent-customizations.md`](agent-customizations.md) 定义。共同点是**静态与动态严格分离**——\n这决定缓存命中率，比放 system 还是首条消息更重要。\n\n### 4.2 环境快照：精确字段\n\n```text\n<environment>\nworking_directory: /abs/path\nis_git_repo: true\nplatform: darwin | linux | windows\nos_version: <uname/ver 摘要>\nshell: zsh\ntoday: 2026-08-03            ← 天级，不含时间\ngit_branch: main\ngit_main_branch: main\ngit_status: <porcelain 摘要，最多 40 行，超出标注截断>\ngit_recent_commits: <最近 5 条 oneline>\n</environment>\nThis snapshot was taken at session start and does not update. Run commands\n(e.g. `git status`) when you need current state.\n```\n\n冻结纪律：Thread 首 Turn 采集一次写死；压缩重建窗口是唯一例行刷新点；需要新鲜状态时模型\n自己调工具。**不逐 Turn 刷新**——那会击穿全部缓存。\n\n### 4.3 Workspace Instruction 发现与注入\n\n- 发现：只读取 workspace root 的 `.zeta/instructions/*.md` 原生文件；`AGENTS.md` 和其他生态\n  格式必须经 `zeta-agent-import`，native loader 不兼容扫描；\n- 注入：当前只把 `load: global` 条目渲染为 `input[0]` user message，并标注其优先级低于\n  system 与安全策略；\n- 大小：每个文件最多 32 KiB、直接条目最多 128，非法条目产生隔离 diagnostic；\n- `load: contextual` / `on-demand`：catalog 已保留类型化策略，但资源匹配和显式选择尚未实现；\n- 目录不存在或没有合法 Global 条目：省略 `input[0]`，不放占位符；\n- 文件变化：Workspace watcher 触发 catalog refresh；已经组装的 model request 不变，后续\n  model invocation 从 `HarnessInstructionsProvider` 读取新 snapshot。\n\n### 4.4 动态注入：append-only reminder\n\n运行时事件（策略变化、文件被外部修改、计划停滞、按需 Instruction、检索到的 MCP\n工具定义）以 `<system-reminder>` 文本块附着在**新增**内容上进入历史，永不回改已有消息。\nreminder 声明自己是背景信息而非用户指令。这是 Skill 激活、hook 输出、审批结果回填的统一\n入口。\n\n## 5. 工具集\n\n### 5.1 设计原则\n\n1. **匹配训练分布**：Anthropic 系对 str-replace Edit/Glob/Grep 有深度训练，OpenAI 系对\n   `apply_patch`（V4A）有专门训练。多 provider 的 Zeta **必须按模型家族分 profile**。\n2. **结构化优于 shell 万能**：结构化工具才能给 `zeta-action-policy` 精确的审查材料（参数级\n   capability、路径级沙箱判定），审批 UX 与 diff 展示也依赖结构。\n3. **工具描述就是提示词**：进入每次调用的 tools 前缀，与 system prompt 同级打磨。\n4. **少而精**：v1 ≤ 10 个。\n\n### 5.2 工具面与 profile\n\n```text\nToolProfile = 共享核心 + 家族特定编辑工具\n\n共享核心：shell / read_file / write_file / glob / grep / update_plan\n家族特定：\n  anthropic / google / 默认 → edit（str-replace + 唯一性）\n  openai                    → apply_patch（V4A）\nMCP（按 §6 阈值）：直接平铺 或 search_tools + call_mcp_tool\n```\n\n逐工具规格（schema、描述正文、校验、错误文案、限幅、capability 注记）：\n[`agent-tools-spec.md`](agent-tools-spec.md)。\n\nProfile 解析发生在 Turn 接受安全点：`ModelSelection` 解析目标模型 → 家族→profile 映射\n（属 `model-provider-config` 声明层）→ 冻结进本 Turn。Core 不认识\"模型家族\"。跨 Turn 换\n模型后，历史中另一 profile 的 Tool Call/Result 只是 transcript，无需处理；只有当前可调用\n集合须与 profile 一致。\n\n`parallel_tool_calls` 改为 profile 属性：允许模型一次响应发多个 Tool Call（执行侧仍按现有\n调度器串行），Anthropic/OpenAI profile 默认开。\n\n## 6. 工具注册时机\n\n| 方案 | 结论 |\n| --- | --- |\n| 全量平铺（Codex 式） | ✅ 内置工具 + 小规模 MCP 采用 |\n| 检索式（Claude ToolSearch 式） | ✅ MCP 超阈值采用 |\n| 运行时动态增删 | ❌ 不采纳：打破 Turn 冻结、每次变化击穿缓存、审批/恢复无法绑定稳定工具面 |\n\n规则：\n\n1. 内置工具静态平铺，Turn 接受时随 profile 冻结；\n2. MCP 聚合定义 ≤ 15 个工具且 ≤ 5k tokens 时平铺进冻结集；超阈值整体切检索式——注入\n   `search_tools` + `call_mcp_tool` 两个元工具，被发现的定义以 reminder append 进历史\n   （不改 tools 数组，保前缀）；\n3. 配置/MCP server 变化走 [`tools.md`](tools.md) §7 的 registry snapshot，只在 Turn 边界\n   推进。\n\n## 7. Turn 内循环与失败策略\n\n### 7.1 模型调用错误分类与处理\n\n| 错误 | 判定依据 | 处理 |\n| --- | --- | --- |\n| 限流 | HTTP 429 | 退避重试；优先遵循 `Retry-After`（上限 60s） |\n| 过载/服务端错误 | HTTP 5xx、529 | 退避重试 |\n| 传输失败 | `ApiError::Transport`（超时/断连） | 退避重试 |\n| 上下文溢出 | provider 错误体解析（**需新增分类**，见 §7.4） | 触发压缩（§10）→ 重试 1 次 → 仍溢出则 Turn fail（stable error `context_overflow`） |\n| 认证失败 | HTTP 401/403 | 不重试；Turn fail（stable error `provider_auth`），提示用户检查凭据 |\n| 无效请求 | HTTP 400 / `InvalidRequest` | 不重试；Turn fail——这是 harness bug 信号，错误体全文入日志 |\n| 无效响应 | `InvalidResponse` | 重试 1 次（可能是瞬时截断）→ fail |\n| 空响应 | 无文本、无 Tool Call、无 Refusal | 重试 1 次（同一请求）→ 仍空则 Turn fail（stable error `model_empty_response`） |\n| Refusal | `ResponseItem::Refusal` | **不是错误**：作为最终消息提交，Turn Completed |\n| 取消 | token 触发 | 传播；Turn → Interrupted |\n\n退避参数：基数 1s、倍率 2、上限 30s、抖动 ±25%、**最多 4 次尝试**（1 次初始 + 3 次重\n试）。退避等待期间以 ≤100ms 粒度轮询 cancellation token（或用带超时的 condvar），interrupt\n必须立即生效。重试不跨越安全边界：只重试**未产生任何 durable 副作用**的模型调用本身；流式\n场景下已发布的 transient delta 通过新 stream incarnation 作废（runtime 文档 §4.2）。\n\n### 7.2 工具执行失败\n\n工具失败默认是**正常结果**（`is_error: true` 的 Tool Result 回给模型继续处理），不使 Turn\n失败——已是现状。政策拒绝的 circuit breaker（连续拒绝中断 Turn）已实现。\n\n### 7.3 失控防护\n\n- **重复失败调用**：同一工具 + 相同参数 digest 连续失败 ≥3 次 → 在下一条 Tool Result 附\n  reminder（\"stop repeating this exact call; change approach or report the blocker\"）；\n  ≥5 次 → Turn fail（stable error `tool_repetition`）。计数按 Turn 内连续窗口，成功即清零。\n- **Turn 资源上限**：durable usage 记账（§9.3）之上可配置 per-Turn token/成本上限，超限\n  在下一个安全点终止（stable error `turn_budget_exhausted`）。v1 默认不设限，仅记账。\n- 无迭代次数硬上限（与 runtime 文档一致：上限应由可取消的资源策略表达，不用进程内计数器）。\n\n### 7.4 需要的接线（现状缺口）\n\n- `ApiError` 增加分类：`RateLimited { retry_after }`、`Overloaded`、`ContextOverflow`、\n  `AuthFailed`——由各 provider 请求构造器从 HTTP 状态 + 错误体映射（Anthropic\n  `overloaded_error`/`invalid_request_error` + `context` 字样；OpenAI `context_length_exceeded`\n   等）。`ModelProviderError`/`CoreError` 透传类别；\n- 重试循环位于 `ModelService` 之上的 executor 侧（重试是 harness 策略，不属于 provider\n  adapter）。\n\n## 8. Steering 与并发输入\n\nAgent 运行中用户发来新消息：**排队 + 下一个安全点注入当前 Turn**，不打断进行中的模型调用\n或工具执行。\n\n```text\nturn/steer { command_id, expected_sequence, turn_id, input: Vec<UserInput> }\n→ 校验 Turn 处于 Running / WaitingForApproval / WaitingForUserInput\n→ durable 提交 UserMessage/UserImage Item（reducer 已允许向非终态 Turn 追加）\n→ command receipt 幂等回放\n→ executor 下一轮循环重读 snapshot，消息自然进入下一次模型调用\n```\n\n- **机制已就绪**：reducer 只拒绝向 Cancelling/终态 Turn 追加 Item；executor 每轮循环重读\n  snapshot——所缺的只是 `ThreadCommand::SteerTurn` 命令 + App Server method（protocol\n  变更，归入阶段 A 的同批 schema 同步）；\n- 模型调用进行中：消息落盘等待，本次响应返回并处理完工具后，下一次调用可见；不自动\n  interrupt。客户端可提供\"打断并说\"= `session/request` InterruptTurn + 新 StartTurn；\n- WaitingForApproval 期间：允许 steer，恢复执行后可见；\n- Cancelling/终态：拒绝（稳定错误），客户端引导开新 Turn；\n- 多条 steer 按提交顺序进入历史；append-only，缓存友好；\n- Turn 处于 Idle（无运行 Turn）时 steer 无意义：返回错误，客户端用 `session/request` StartTurn。\n\n## 9. 上下文裁剪\n\n### 9.1 输入侧逐条限幅\n\n进入模型的每条内容在**选入时**限幅（执行侧上限保护进程与存储，模型侧限幅保护窗口预算，\n两者分开；数值是起点参考值，由 §14 评测调）：\n\n| 内容 | 限幅 | 方式 |\n| --- | --- | --- |\n| shell 结果 | 30 KiB | 头尾各半，中间标注截断字节数 |\n| read_file | 2000 行 / 行内 2000 字符 | 尾部提示用 offset 继续 |\n| grep / glob | 100 条 | 标注总命中数 |\n| MCP 工具结果 | 25 KiB | 同 shell |\n| 图片 | 按 provider 上限降采样 | adapter 层 |\n\n### 9.2 历史选择：不做静默滑窗\n\n- 预算内全量保留；接近预算触发压缩（§10），更早历史被 checkpoint 原子吸收；\n- 永不静默删除：当前 Turn 输入、安全约束、未完成 Tool Call/Result 组\n  （[`core-context.md`](core-context.md) §7.3 的显式 overflow outcome）。\n\n### 9.3 预算来源与 usage 校准\n\n当前实现：\n\n- 窗口与阈值来自 `ModelInfo`，或用户按模型 ID 配置的\n  `ModelProviderConfig.model_context`；自动压缩阈值不超过窗口的 90%；\n- `ContextWindow::Unknown` 不猜 128k，而是明确使用 `ContextBudget::ProviderManaged`；\n- `zeta-context-engine` 已统一压力线、模型硬窗口、精准计量与带保守记账余量的估算结果；\n- 生产 planner 仍由 `deterministic-bytes-v1` 以 bytes/4 加结构开销做确定性估算，并在诊断中记录\n  revision；最终 request 接近压力线或 compaction 后会调用声明式 model binding 对应的 remote\n  preflight：OpenAI exact，Anthropic、Google、Kimi、Z.AI estimated；本地 tokenizer adapter 尚未\n  接入。\n\nusage 按 Thread 持久化和基于 provider usage 的 EMA 校准尚未实现；在此之前不能把粗估写成精确\ntokenizer 保证。\n\n## 10. 压缩\n\n### 10.1 触发\n\n- **自动（已实现）**：估算历史超过 Core-managed input budget → 下一个 model safe point 先压缩、\n  durable commit，再从新 snapshot 重规划；\n- **手动（Proposed）**：`/compact` 与用户保留提示尚未实现；\n- **溢出恢复（Proposed）**：provider `ContextOverflow` 的压缩后单次重试仍依赖稳定错误分类。\n\n### 10.2 流程与精确规则\n\n机制（checkpoint schema、digest、失效回退）归 [`core-context.md`](core-context.md) §8。\n当前规则：\n\n- **tail 选择**：从最新 Turn 向前按完整 Turn 单元保留；当前 Turn 无条件保留，checkpoint 只\n  覆盖 durable 前缀；\n- **压缩调用**：独立模型调用使用被吸收前缀、上一个 checkpoint 和 `COMPACTION_PROMPT`；不带\n  tools，summary target 在剩余预算内有界；\n- **分批处理**：压缩请求本身也必须装入同一模型窗口。过长前缀分批提交 checkpoint；若单个\n  新 Turn 连同 compaction envelope 都无法装入，则返回 `CompactionSourceTooLarge`，不循环重试；\n- **失败路径**：空 summary、Tool Call、超目标 summary、source digest/Item/range 不一致或 store\n  commit 失败都会失败即关闭；未 commit 的 summary 不进入 projection。\n\n热文件重注入和用户定向压缩属于 Proposed，当前不会在压缩过程中重新读取 Workspace 文件。\n\n### 10.3 窗口重建\n\n```text\ninstructions（下一个 model safe point 重新冻结）\n+ 工作区指令（mandatory fragment）\n+ checkpoint summary\n+ tail 原文\n```\n\n## 11. Prompt cache\n\n### 11.1 三家机制\n\n| Provider | 机制 | 要求 |\n| --- | --- | --- |\n| Anthropic | 显式 `cache_control` 断点（≤4），默认 5 分钟 TTL，读 ≈0.1× / 写 ≈1.25× | 前缀逐字节一致；断点前 ≥ 最小 token 数（约 1024） |\n| OpenAI | 自动前缀缓存（≥1024 tokens） | 无需标注，前缀逐字节一致 |\n| Google | 隐式（2.5+）+ 显式 cache API | 同上 |\n\n### 11.2 组装硬约束\n\n1. 序列化确定性：工具列表顺序固定（内置按 profile 声明序 → MCP 按 server+name 排序）；\n2. append-only（§2 规则）；reminder 附着新内容；\n3. 前缀不放每次变化的值（时间戳、实时 git status——§4.2 冻结纪律）；\n4. 压缩、换 profile、换模型必然击穿缓存——接受为一次性成本。\n\n### 11.3 落点\n\n`cache_control` 不进 canonical `ModelRequest`，由 `zeta-api` 的 `anthropic_messages` 构造器\n注入：断点 1 = tools 末尾；断点 2 = system 末尾；断点 3 = 倒数第二条 user 消息末尾（滚动，\n命中上一轮全部历史）。观测：`cached_input_tokens` 已解析；**同一 Thread 连续 Turn 命中率\n应接近 100%，低于即组装层引入了前缀抖动，按 bug 处理**（§14 的回归断言）。\n\n## 12. Skills 与 slash commands\n\n- **slash commands**（`zeta-rs/slash-commands` 已有 catalog）：在 `session/request` StartTurn 之前由\n  App Server 展开，展开后的正文作为 durable UserMessage 进入 Turn 输入；消息内保留\n  `<command-name>` 标注供模型识别来源。不在模型侧解析斜杠语法。\n- **Skills**（`zeta-rs/skills` 已有 runtime；发现/信任归 [`skills.md`](skills.md)）：\n  - v1 已实现：用户提交 exact `SkillRef`，App Server 冻结 digest/generation/reason，Core 在每个\n    model safe point 重载 exact `SKILL.md`，以 `ActivatedSkill` instruction layer 注入；raw path\n    输入被拒绝；\n  - 自动候选检索（推迟）：只注入被 selector 激活的 Skill，不能把整个 catalog 正文或清单随\n    catalog 数量线性塞进 prompt。\n\n## 13. Provider 差异矩阵\n\ncanonical 层（`ModelRequest`）保持 provider 中立，差异全部压进 `zeta-api` 两个请求构造器。\nauthoring 规则以最严格交集为准：\n\n| 维度 | Anthropic Messages | OpenAI Responses | canonical 规则 |\n| --- | --- | --- | --- |\n| instructions | `system` 字段 | `instructions` 字段 | 单值 `instructions`，adapter 映射 |\n| 角色 | user/assistant | + developer | v1 canonical 只产生 System/User/Assistant；`Developer` 保留给 OpenAI 路径未来用 |\n| 工具 schema | `input_schema`，无 strict | strict 模式：`additionalProperties:false` + 全字段 `required` | **按通用子集 authoring**（[`agent-tools-spec.md` §1](agent-tools-spec.md#1-schema-约定)）：顶层 object、全 required、可选性用 `[\"T\",\"null\"]`，两边同一份 schema 直接可用 |\n| tool_choice | auto/any/tool | auto/required/function/none | 现有 `ToolChoice` 已覆盖 |\n| 并行 Tool Call | 支持（`disable_parallel_tool_use`） | 支持（`parallel_tool_calls`） | profile 属性透传 |\n| Tool result | user 消息内 `tool_result` block | `function_call_output` item | assembler 已配对，adapter 负责 wire 形态 |\n| Tool call id | `tool_use.id` 回传 | `call_id` 回传 | `ToolCallId` 原样往返，adapter 不改写 |\n| 图片 | base64 source block | data URI / URL | `ContentPart::ImageUrl` 承载，adapter 转换（已实现） |\n| 缓存 | 显式断点（§11.3） | 自动 | adapter 差异，canonical 无感 |\n| reasoning | thinking + budget_tokens | `reasoning.effort` | `ReasoningConfig.effort` 映射：Anthropic 按档位换算 budget；v1 不把 reasoning 内容回灌历史（assembler 已跳过） |\n| 空响应/拒绝 | `stop_reason` + 空 content | `refusal` item | 统一映射 `ResponseItem::Refusal` / 空响应走 §7.1 |\n| 错误分类 | `overloaded_error` 等错误体 | `context_length_exceeded` 等 code | 映射进 §7.4 的 `ApiError` 新分类 |\n\n## 14. 评测\n\nharness 的每层都直接影响成功率与 token 成本；没有评测回路，§9/§10 的所有参考值无法调优。\n\n### 14.1 任务集\n\n仓库内 `evals/harness/` 下的封闭夹具，每题 = 冻结的小型 git 仓库 + 任务提示 +\n`verify.sh`（退出码 0 = 成功）：\n\n| 层 | 数量 | 内容 | 考察 |\n| --- | --- | --- | --- |\n| T1 | 10 | 单文件 bug 修复 | 工具基本功、编辑正确率 |\n| T2 | 5 | 跨文件小功能 | 搜索/多文件编辑/验证纪律 |\n| T3 | 2 | 长会话（>30 次工具调用） | 计划工具、失控防护 |\n| T4 | 2 | 强制压缩任务（低阈值配置） | 压缩后连贯性（约束保留、不重做已完成工作） |\n\n### 14.2 指标\n\n每次运行记录：成功与否、input/cached/output tokens、工具调用次数、墙钟时间。\n按 profile × provider 出对比表。\n\n### 14.3 运行方式\n\n- **PR smoke（无真模型）**：组装快照测试——固定 Thread 夹具跑 assembler，断言\n  （a）请求字节稳定（连续 Turn 前缀不变，§11 回归）；（b）限幅与结构不变量；用\n  deterministic fake `ModelService` 跑 T1 子集的 loop 行为（重试、steering、循环终止）；\n- **nightly（真模型）**：全任务集 × 主力 profile（anthropic / openai 各一），指标入库\n  看趋势；\n- M0–M6 每个里程碑的验收 = 对应任务层在接线前后的指标对比。\n\n## 15. 落地顺序\n\n与 [`zeta-agent-runtime-architecture.md` 阶段 A–E](zeta-agent-runtime-architecture.md#7-分阶段实施计划)\n的关系：M0–M2 不依赖阶段 A/B 可立即做；M3 落在阶段 B 内；M4–M6 与阶段 B/C 并行。\n\n| 里程碑 | 内容 | 关键改动点 | 前置接线 |\n| --- | --- | --- | --- |\n| M0（部分完成）提示词接线 | SYSTEM_PROMPT、环境快照和 Global `.zeta/instructions` 已注入；工具指导仍待完成 | `ContextAssembler`、host 环境快照、`WorkspaceCustomizations` | 无 |\n| M1 工具最小闭环 | 七件套 + profile + `parallel_tool_calls`；评测 T1/T2 建立 | `local_tools.rs`、`file-system-tool` 扩展、rg/file-search 封装、apply_patch 接入 | 家族→profile 映射进 `model-provider-config` |\n| M2 失败弹性 + steering | §7 重试/分类/失控防护；`turn/steer` | executor 重试层、`ApiError` 分类、SteerTurn command | protocol schema 同步（与阶段 A 的 R1 同批） |\n| M3（部分完成）限幅/预算/压缩 | ContextPlan、配置窗口与 durable compaction 已完成；逐工具限幅、usage 记账和 T4 仍待完成 | ContextPlan 选入路径、checkpoint、usage 持久化 | 无；窗口未知时 provider-managed |\n| M4 缓存 | §11.3 断点注入；前缀稳定回归断言 | `anthropic_messages`、组装快照测试 | 无 |\n| M5 MCP 策略 | §6 阈值平铺/检索式 | MCP registry snapshot 之上的策略层 | 无 |\n| M6（部分完成）Skills/slash | slash 与 explicit SkillRef 已接通；自动 selector 与 Desktop picker 待完成 | App Server 展开、frozen activation、ActivatedSkill layer | 无 |\n\nM0+M1（+M2 弹性子集）合起来构成\"接入模型即可 coding\"的最小闭环，其**文件级实施计划**见\n[`agent-harness-implementation-plan.md`](agent-harness-implementation-plan.md)。\n\n## 16. 参考\n\n- [`agent-tools-spec.md`](agent-tools-spec.md) — 逐工具规格与提示词正文\n- [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) — 执行内核与阶段计划\n- [`core-context.md`](core-context.md) — ContextPlan / checkpoint 机制\n- [`tools.md`](tools.md) — 工具三层契约与 registry snapshot\n- [`skills.md`](skills.md) / [`slash-commands` crate](../zeta-rs/slash-commands/) — 扩展来源\n- [`zeta-prompts` README](../zeta-rs/prompts/README.md) — 提示词资产 ownership\n- Claude Code harness 行为（Edit 唯一性、Read 限幅、system-reminder、压缩后文件重注入）\n- Codex harness 行为（apply_patch V4A、AGENTS.md、environment_context、update_plan）",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-现状差距",
        "title": "1. 现状差距"
      },
      {
        "depth": 2,
        "id": "2-一次模型调用的目标形态",
        "title": "2. 一次模型调用的目标形态"
      },
      {
        "depth": 2,
        "id": "3-turn-内循环",
        "title": "3. Turn 内循环"
      },
      {
        "depth": 2,
        "id": "4-提示词",
        "title": "4. 提示词"
      },
      {
        "depth": 3,
        "id": "41-四层结构",
        "title": "4.1 四层结构"
      },
      {
        "depth": 3,
        "id": "42-环境快照精确字段",
        "title": "4.2 环境快照：精确字段"
      },
      {
        "depth": 3,
        "id": "43-workspace-instruction-发现与注入",
        "title": "4.3 Workspace Instruction 发现与注入"
      },
      {
        "depth": 3,
        "id": "44-动态注入append-only-reminder",
        "title": "4.4 动态注入：append-only reminder"
      },
      {
        "depth": 2,
        "id": "5-工具集",
        "title": "5. 工具集"
      },
      {
        "depth": 3,
        "id": "51-设计原则",
        "title": "5.1 设计原则"
      },
      {
        "depth": 3,
        "id": "52-工具面与-profile",
        "title": "5.2 工具面与 profile"
      },
      {
        "depth": 2,
        "id": "6-工具注册时机",
        "title": "6. 工具注册时机"
      },
      {
        "depth": 2,
        "id": "7-turn-内循环与失败策略",
        "title": "7. Turn 内循环与失败策略"
      },
      {
        "depth": 3,
        "id": "71-模型调用错误分类与处理",
        "title": "7.1 模型调用错误分类与处理"
      },
      {
        "depth": 3,
        "id": "72-工具执行失败",
        "title": "7.2 工具执行失败"
      },
      {
        "depth": 3,
        "id": "73-失控防护",
        "title": "7.3 失控防护"
      },
      {
        "depth": 3,
        "id": "74-需要的接线现状缺口",
        "title": "7.4 需要的接线（现状缺口）"
      },
      {
        "depth": 2,
        "id": "8-steering-与并发输入",
        "title": "8. Steering 与并发输入"
      },
      {
        "depth": 2,
        "id": "9-上下文裁剪",
        "title": "9. 上下文裁剪"
      },
      {
        "depth": 3,
        "id": "91-输入侧逐条限幅",
        "title": "9.1 输入侧逐条限幅"
      },
      {
        "depth": 3,
        "id": "92-历史选择不做静默滑窗",
        "title": "9.2 历史选择：不做静默滑窗"
      },
      {
        "depth": 3,
        "id": "93-预算来源与-usage-校准",
        "title": "9.3 预算来源与 usage 校准"
      },
      {
        "depth": 2,
        "id": "10-压缩",
        "title": "10. 压缩"
      },
      {
        "depth": 3,
        "id": "101-触发",
        "title": "10.1 触发"
      },
      {
        "depth": 3,
        "id": "102-流程与精确规则",
        "title": "10.2 流程与精确规则"
      },
      {
        "depth": 3,
        "id": "103-窗口重建",
        "title": "10.3 窗口重建"
      },
      {
        "depth": 2,
        "id": "11-prompt-cache",
        "title": "11. Prompt cache"
      },
      {
        "depth": 3,
        "id": "111-三家机制",
        "title": "11.1 三家机制"
      },
      {
        "depth": 3,
        "id": "112-组装硬约束",
        "title": "11.2 组装硬约束"
      },
      {
        "depth": 3,
        "id": "113-落点",
        "title": "11.3 落点"
      },
      {
        "depth": 2,
        "id": "12-skills-与-slash-commands",
        "title": "12. Skills 与 slash commands"
      },
      {
        "depth": 2,
        "id": "13-provider-差异矩阵",
        "title": "13. Provider 差异矩阵"
      },
      {
        "depth": 2,
        "id": "14-评测",
        "title": "14. 评测"
      },
      {
        "depth": 3,
        "id": "141-任务集",
        "title": "14.1 任务集"
      },
      {
        "depth": 3,
        "id": "142-指标",
        "title": "14.2 指标"
      },
      {
        "depth": 3,
        "id": "143-运行方式",
        "title": "14.3 运行方式"
      },
      {
        "depth": 2,
        "id": "15-落地顺序",
        "title": "15. 落地顺序"
      },
      {
        "depth": 2,
        "id": "16-参考",
        "title": "16. 参考"
      }
    ],
    "searchText": " 状态：Accepted（2026-08-03；同日补齐 loop 失败策略、steering、skills/slash、provider 差异矩阵与评测设计，达到可实现深度） 定位：回答\"Core 的 agent loop 具体怎么搭起来\"——一次模型调用长什么样、Turn 内循环与 失败弹性、steering、提示词组织、工具选择与注册时机、上下文裁剪压缩、prompt cache、评测。 分工：执行内核（单写者、durable commit、恢复、取消）由 zeta-agent-runtime-architecture.md 负责；上下文生命 周期抽象（ContextPlan/Manager/checkpoint）由 core-context.md 负责； 工具三层契约（定义/绑定/执行接口）由 tools.md 负责；逐工具规格（schema、 描述正文、错误文案）与系统提示词扩写正文由 agent-tools-spec.md 负责。本文负责 harness 的行为策略。 快速理解 问题 结论 深入阅读 --- --- --- 模型调用失败怎么办？ 按错误类别分流：退避重试 / 压缩后重试 / 立即失败 / 作为正常结果完成 §7 Agent 运行中用户发消息怎么办？ 排队 + 下一个安全点注入当前 Turn；reducer 已允许，缺一条 turn/steer 命令 §8 提示词怎么做？ 四层：静态身份/策略 + per-profile 工具指导 + 会话冻结环境快照 + 工作区指令；动态走 append-only reminder §4 工具选哪些？ 共享七件套 + 按模型家族切编辑工具形态；逐工具规格见 tools-spec §5 工具什么时候注册？ Turn 接受时冻结；内置静态平铺，MCP 超阈值切检索式；不做运行时动态增删 §6 上下文怎么裁剪/压缩？ 输入侧逐条限幅；历史语义单元保留；阈值用 ModelInfo.effectiveautocompacttokenlimit §9、§10 缓存怎么搞？ 前缀字节稳定 + append-only；cachecontrol 由 Anthropic adapter 注入 §11 怎么知道 harness 变好了？ 三层任务集 + token/成功率指标 + 前缀稳定性回归断言 §14 1. 现状差距 一个可用的 coding agent harness 需要的每个环节，对照 Zeta 当前实现： 环节 可用 harness 需要 Zeta 现状 --- --- --- System prompt 每次调用注入身份、策略、工具指导、环境 部分：SYSTEMPROMPT 已经通过 HarnessInstructions 和 ContextPlan 注入；per-profile 工具指导仍未完成 环境上下文 cwd、平台、日期、git 状态、项目指令 ✅ Local Workspace host 在 model safe point 提供环境与 .zeta/instructions snapshot 工具面 读/搜/改/执行闭环 ❌ 只接了 shell-command；file-system-tool（仅 read/list/metadata）、apply-patch、file-search 是孤立 crate 模型失败弹性 429/5xx 退避重试、溢出压缩重试、空响应处理 ❌ 模型一报错 Turn 即 fail；ApiError 只有 HttpStatus u16 无分类 Steering 运行中排队注入用户消息 部分：reducer 已允许向 Running Turn 追加 Item、executor 每轮重读 snapshot；缺 turn/steer 命令 工具结果限幅 模型侧截断 + 保留头尾 部分：shell 有 256 KiB 执行上限，但无模型输入预算 上下文预算 窗口估算、溢出显式处理 ✅ 已知/配置窗口走确定性预算；未知窗口明确退回 provider-managed 压缩 阈值触发、durable checkpoint ✅ COMPACTIONPROMPT、source digest、原子 commit、恢复校验与 commit 后重规划已接通 Prompt cache 前缀稳定 + 断点标注 ❌ 只解析 cachereadinputtokens，不写 cachecontrol 多 Tool Call/响应 模型一次响应多个调用 ❌ paralleltoolcalls 硬编码 false 计划工具 长任务显式计划状态 部分：ThreadItem::Plan 存在，无工具产生它，assembler 跳过它 评测 任务集 + 指标回路 ❌ 无 2. 一次模型调用的目标形态 ``text ModelRequest ├─ instructions ── 静态层（会话内不变，缓存友好） │ ├─ 身份 + 指令优先级 + 安全策略 （zeta-prompts SYSTEMPROMPT，静态） │ ├─ 工具使用指导 + 输出风格 （per tool-profile，profile 内静态， │ │ 正文见 agent-tools-spec.md 附录 A） │ ├─ Skill 清单 （名称+一行描述，预算上限内，会话稳定） │ └─ 环境快照 （字段见 §4.2，Thread 首 Turn 冻结） ├─ input │ ├─ 0 工作区指令 message （Global .zeta/instructions，§4.3，调用内冻结） │ ├─ 1.. durable history （append-only，语义单元完整， │ │ 可含 checkpoint summary 替代更早历史） │ └─ last 当前 Turn 输入 / steering 消息（+ append-only reminder 块） └─ tools ── Turn 接受时冻结的 ToolProfile ` 三条硬规则，所有后续设计都从它们推导： 1. 前缀稳定：instructions、tools、已有 history 在 Turn 之间逐字节不变，新内容只 append（§11 的前提）。 2. 语义单元完整：Tool Call/Result 成对；一个 Turn 的内容原子保留或原子被 checkpoint 吸收（core-context.md §6.3）。 3. 变化走安全点：工具面、模型、策略、环境快照只在 Turn 边界或压缩边界变化。 3. Turn 内循环 一次 Turn 的执行循环（现有 TurnExecutor::executesteps 的目标形态）： `text loop: 1. 检查取消 2. 读最新 durable snapshot（steering 消息在此自然进入） 3. 预算检查：估算占用 ≥ 阈值 → 先压缩（§10），压缩失败且未超硬限 → 继续 4. ContextManager.prepare → ContextPlan → ContextAssembler → ModelRequest 5. 调用模型（含 §7 重试策略），流式 delta 经 sink 发布 6. 响应分类： ├─ 有 Tool Call → durable 记录 → ToolScheduler 顺序执行（审批/沙箱/ │ 升级语义见 core.md §11）→ 回到 1 ├─ 只有文本 → durable 提交 agent message → Turn Completed ├─ Refusal → 作为最终消息提交 → Turn Completed（不是失败） └─ 空响应 → §7 空响应处理 ` 无固定迭代次数上限（runtime 文档决策）；失控防护见 §7.3。 4. 提示词 4.1 四层结构 层 内容 变化频率 存放位置 --- --- --- --- 身份与策略 身份、指令优先级、注入防护、工作行为 随产品版本 zeta-prompts SYSTEMPROMPT（已存在） 工具指导 + 输出风格 工具组合惯例、验证纪律、简洁度、路径引用格式 随 tool profile zeta-prompts 新模板；正文已写好，见 agent-tools-spec.md 附录 A 环境快照 见 §4.2 Thread 首 Turn 冻结；压缩边界刷新 host 提供 AgentEnvironmentSnapshot，Core 渲染 工作区指令 Global .zeta/instructions model invocation 内冻结；文件变化影响后续调用 input 0 独立 message 外部产品如何组织项目指令只是参照系；Zeta 的原生 artifact、目录和加载策略由 agent-customizations.md 定义。共同点是静态与动态严格分离—— 这决定缓存命中率，比放 system 还是首条消息更重要。 4.2 环境快照：精确字段 `text <environment workingdirectory: /abs/path isgitrepo: true platform: darwin linux windows osversion: <uname/ver 摘要 shell: zsh today: 2026-08-03 ← 天级，不含时间 gitbranch: main gitmainbranch: main gitstatus: <porcelain 摘要，最多 40 行，超出标注截断 gitrecentcommits: <最近 5 条 oneline </environment This snapshot was taken at session start and does not update. Run commands e.g. git status when you need current state. ` 冻结纪律：Thread 首 Turn 采集一次写死；压缩重建窗口是唯一例行刷新点；需要新鲜状态时模型 自己调工具。不逐 Turn 刷新——那会击穿全部缓存。 4.3 Workspace Instruction 发现与注入 - 发现：只读取 workspace root 的 .zeta/instructions/.md 原生文件；AGENTS.md 和其他生态 格式必须经 zeta-agent-import，native loader 不兼容扫描； - 注入：当前只把 load: global 条目渲染为 input 0 user message，并标注其优先级低于 system 与安全策略； - 大小：每个文件最多 32 KiB、直接条目最多 128，非法条目产生隔离 diagnostic； - load: contextual / on-demand：catalog 已保留类型化策略，但资源匹配和显式选择尚未实现； - 目录不存在或没有合法 Global 条目：省略 input 0 ，不放占位符； - 文件变化：Workspace watcher 触发 catalog refresh；已经组装的 model request 不变，后续 model invocation 从 HarnessInstructionsProvider 读取新 snapshot。 4.4 动态注入：append-only reminder 运行时事件（策略变化、文件被外部修改、计划停滞、按需 Instruction、检索到的 MCP 工具定义）以 <system-reminder 文本块附着在新增内容上进入历史，永不回改已有消息。 reminder 声明自己是背景信息而非用户指令。这是 Skill 激活、hook 输出、审批结果回填的统一 入口。 5. 工具集 5.1 设计原则 1. 匹配训练分布：Anthropic 系对 str-replace Edit/Glob/Grep 有深度训练，OpenAI 系对 applypatch（V4A）有专门训练。多 provider 的 Zeta 必须按模型家族分 profile。 2. 结构化优于 shell 万能：结构化工具才能给 zeta-action-policy 精确的审查材料（参数级 capability、路径级沙箱判定），审批 UX 与 diff 展示也依赖结构。 3. 工具描述就是提示词：进入每次调用的 tools 前缀，与 system prompt 同级打磨。 4. 少而精：v1 ≤ 10 个。 5.2 工具面与 profile `text ToolProfile = 共享核心 + 家族特定编辑工具 共享核心：shell / readfile / writefile / glob / grep / updateplan 家族特定： anthropic / google / 默认 → edit（str-replace + 唯一性） openai → applypatch（V4A） MCP（按 §6 阈值）：直接平铺 或 searchtools + callmcptool ` 逐工具规格（schema、描述正文、校验、错误文案、限幅、capability 注记）： agent-tools-spec.md。 Profile 解析发生在 Turn 接受安全点：ModelSelection 解析目标模型 → 家族→profile 映射 （属 model-provider-config 声明层）→ 冻结进本 Turn。Core 不认识\"模型家族\"。跨 Turn 换 模型后，历史中另一 profile 的 Tool Call/Result 只是 transcript，无需处理；只有当前可调用 集合须与 profile 一致。 paralleltoolcalls 改为 profile 属性：允许模型一次响应发多个 Tool Call（执行侧仍按现有 调度器串行），Anthropic/OpenAI profile 默认开。 6. 工具注册时机 方案 结论 --- --- 全量平铺（Codex 式） ✅ 内置工具 + 小规模 MCP 采用 检索式（Claude ToolSearch 式） ✅ MCP 超阈值采用 运行时动态增删 ❌ 不采纳：打破 Turn 冻结、每次变化击穿缓存、审批/恢复无法绑定稳定工具面 规则： 1. 内置工具静态平铺，Turn 接受时随 profile 冻结； 2. MCP 聚合定义 ≤ 15 个工具且 ≤ 5k tokens 时平铺进冻结集；超阈值整体切检索式——注入 searchtools + callmcptool 两个元工具，被发现的定义以 reminder append 进历史 （不改 tools 数组，保前缀）； 3. 配置/MCP server 变化走 tools.md §7 的 registry snapshot，只在 Turn 边界 推进。 7. Turn 内循环与失败策略 7.1 模型调用错误分类与处理 错误 判定依据 处理 --- --- --- 限流 HTTP 429 退避重试；优先遵循 Retry-After（上限 60s） 过载/服务端错误 HTTP 5xx、529 退避重试 传输失败 ApiError::Transport（超时/断连） 退避重试 上下文溢出 provider 错误体解析（需新增分类，见 §7.4） 触发压缩（§10）→ 重试 1 次 → 仍溢出则 Turn fail（stable error contextoverflow） 认证失败 HTTP 401/403 不重试；Turn fail（stable error providerauth），提示用户检查凭据 无效请求 HTTP 400 / InvalidRequest 不重试；Turn fail——这是 harness bug 信号，错误体全文入日志 无效响应 InvalidResponse 重试 1 次（可能是瞬时截断）→ fail 空响应 无文本、无 Tool Call、无 Refusal 重试 1 次（同一请求）→ 仍空则 Turn fail（stable error modelemptyresponse） Refusal ResponseItem::Refusal 不是错误：作为最终消息提交，Turn Completed 取消 token 触发 传播；Turn → Interrupted 退避参数：基数 1s、倍率 2、上限 30s、抖动 ±25%、最多 4 次尝试（1 次初始 + 3 次重 试）。退避等待期间以 ≤100ms 粒度轮询 cancellation token（或用带超时的 condvar），interrupt 必须立即生效。重试不跨越安全边界：只重试未产生任何 durable 副作用的模型调用本身；流式 场景下已发布的 transient delta 通过新 stream incarnation 作废（runtime 文档 §4.2）。 7.2 工具执行失败 工具失败默认是正常结果（iserror: true 的 Tool Result 回给模型继续处理），不使 Turn 失败——已是现状。政策拒绝的 circuit breaker（连续拒绝中断 Turn）已实现。 7.3 失控防护 - 重复失败调用：同一工具 + 相同参数 digest 连续失败 ≥3 次 → 在下一条 Tool Result 附 reminder（\"stop repeating this exact call; change approach or report the blocker\"）； ≥5 次 → Turn fail（stable error toolrepetition）。计数按 Turn 内连续窗口，成功即清零。 - Turn 资源上限：durable usage 记账（§9.3）之上可配置 per-Turn token/成本上限，超限 在下一个安全点终止（stable error turnbudgetexhausted）。v1 默认不设限，仅记账。 - 无迭代次数硬上限（与 runtime 文档一致：上限应由可取消的资源策略表达，不用进程内计数器）。 7.4 需要的接线（现状缺口） - ApiError 增加分类：RateLimited retryafter 、Overloaded、ContextOverflow、 AuthFailed——由各 provider 请求构造器从 HTTP 状态 + 错误体映射（Anthropic overloadederror/invalidrequesterror + context 字样；OpenAI contextlengthexceeded 等）。ModelProviderError/CoreError 透传类别； - 重试循环位于 ModelService 之上的 executor 侧（重试是 harness 策略，不属于 provider adapter）。 8. Steering 与并发输入 Agent 运行中用户发来新消息：排队 + 下一个安全点注入当前 Turn，不打断进行中的模型调用 或工具执行。 `text turn/steer commandid, expectedsequence, turnid, input: Vec<UserInput → 校验 Turn 处于 Running / WaitingForApproval / WaitingForUserInput → durable 提交 UserMessage/UserImage Item（reducer 已允许向非终态 Turn 追加） → command receipt 幂等回放 → executor 下一轮循环重读 snapshot，消息自然进入下一次模型调用 ` - 机制已就绪：reducer 只拒绝向 Cancelling/终态 Turn 追加 Item；executor 每轮循环重读 snapshot——所缺的只是 ThreadCommand::SteerTurn 命令 + App Server method（protocol 变更，归入阶段 A 的同批 schema 同步）； - 模型调用进行中：消息落盘等待，本次响应返回并处理完工具后，下一次调用可见；不自动 interrupt。客户端可提供\"打断并说\"= session/request InterruptTurn + 新 StartTurn； - WaitingForApproval 期间：允许 steer，恢复执行后可见； - Cancelling/终态：拒绝（稳定错误），客户端引导开新 Turn； - 多条 steer 按提交顺序进入历史；append-only，缓存友好； - Turn 处于 Idle（无运行 Turn）时 steer 无意义：返回错误，客户端用 session/request StartTurn。 9. 上下文裁剪 9.1 输入侧逐条限幅 进入模型的每条内容在选入时限幅（执行侧上限保护进程与存储，模型侧限幅保护窗口预算， 两者分开；数值是起点参考值，由 §14 评测调）： 内容 限幅 方式 --- --- --- shell 结果 30 KiB 头尾各半，中间标注截断字节数 readfile 2000 行 / 行内 2000 字符 尾部提示用 offset 继续 grep / glob 100 条 标注总命中数 MCP 工具结果 25 KiB 同 shell 图片 按 provider 上限降采样 adapter 层 9.2 历史选择：不做静默滑窗 - 预算内全量保留；接近预算触发压缩（§10），更早历史被 checkpoint 原子吸收； - 永不静默删除：当前 Turn 输入、安全约束、未完成 Tool Call/Result 组 （core-context.md §7.3 的显式 overflow outcome）。 9.3 预算来源与 usage 校准 当前实现： - 窗口与阈值来自 ModelInfo，或用户按模型 ID 配置的 ModelProviderConfig.modelcontext；自动压缩阈值不超过窗口的 90%； - ContextWindow::Unknown 不猜 128k，而是明确使用 ContextBudget::ProviderManaged； - zeta-context-engine 已统一压力线、模型硬窗口、精准计量与带保守记账余量的估算结果； - 生产 planner 仍由 deterministic-bytes-v1 以 bytes/4 加结构开销做确定性估算，并在诊断中记录 revision；最终 request 接近压力线或 compaction 后会调用声明式 model binding 对应的 remote preflight：OpenAI exact，Anthropic、Google、Kimi、Z.AI estimated；本地 tokenizer adapter 尚未 接入。 usage 按 Thread 持久化和基于 provider usage 的 EMA 校准尚未实现；在此之前不能把粗估写成精确 tokenizer 保证。 10. 压缩 10.1 触发 - 自动（已实现）：估算历史超过 Core-managed input budget → 下一个 model safe point 先压缩、 durable commit，再从新 snapshot 重规划； - 手动（Proposed）：/compact 与用户保留提示尚未实现； - 溢出恢复（Proposed）：provider ContextOverflow 的压缩后单次重试仍依赖稳定错误分类。 10.2 流程与精确规则 机制（checkpoint schema、digest、失效回退）归 core-context.md §8。 当前规则： - tail 选择：从最新 Turn 向前按完整 Turn 单元保留；当前 Turn 无条件保留，checkpoint 只 覆盖 durable 前缀； - 压缩调用：独立模型调用使用被吸收前缀、上一个 checkpoint 和 COMPACTIONPROMPT；不带 tools，summary target 在剩余预算内有界； - 分批处理：压缩请求本身也必须装入同一模型窗口。过长前缀分批提交 checkpoint；若单个 新 Turn 连同 compaction envelope 都无法装入，则返回 CompactionSourceTooLarge，不循环重试； - 失败路径：空 summary、Tool Call、超目标 summary、source digest/Item/range 不一致或 store commit 失败都会失败即关闭；未 commit 的 summary 不进入 projection。 热文件重注入和用户定向压缩属于 Proposed，当前不会在压缩过程中重新读取 Workspace 文件。 10.3 窗口重建 `text instructions（下一个 model safe point 重新冻结） + 工作区指令（mandatory fragment） + checkpoint summary + tail 原文 ` 11. Prompt cache 11.1 三家机制 Provider 机制 要求 --- --- --- Anthropic 显式 cachecontrol 断点（≤4），默认 5 分钟 TTL，读 ≈0.1× / 写 ≈1.25× 前缀逐字节一致；断点前 ≥ 最小 token 数（约 1024） OpenAI 自动前缀缓存（≥1024 tokens） 无需标注，前缀逐字节一致 Google 隐式（2.5+）+ 显式 cache API 同上 11.2 组装硬约束 1. 序列化确定性：工具列表顺序固定（内置按 profile 声明序 → MCP 按 server+name 排序）； 2. append-only（§2 规则）；reminder 附着新内容； 3. 前缀不放每次变化的值（时间戳、实时 git status——§4.2 冻结纪律）； 4. 压缩、换 profile、换模型必然击穿缓存——接受为一次性成本。 11.3 落点 cachecontrol 不进 canonical ModelRequest，由 zeta-api 的 anthropicmessages 构造器 注入：断点 1 = tools 末尾；断点 2 = system 末尾；断点 3 = 倒数第二条 user 消息末尾（滚动， 命中上一轮全部历史）。观测：cachedinputtokens 已解析；同一 Thread 连续 Turn 命中率 应接近 100%，低于即组装层引入了前缀抖动，按 bug 处理（§14 的回归断言）。 12. Skills 与 slash commands - slash commands（zeta-rs/slash-commands 已有 catalog）：在 session/request StartTurn 之前由 App Server 展开，展开后的正文作为 durable UserMessage 进入 Turn 输入；消息内保留 <command-name 标注供模型识别来源。不在模型侧解析斜杠语法。 - Skills（zeta-rs/skills 已有 runtime；发现/信任归 skills.md）： - v1 已实现：用户提交 exact SkillRef，App Server 冻结 digest/generation/reason，Core 在每个 model safe point 重载 exact SKILL.md，以 ActivatedSkill instruction layer 注入；raw path 输入被拒绝； - 自动候选检索（推迟）：只注入被 selector 激活的 Skill，不能把整个 catalog 正文或清单随 catalog 数量线性塞进 prompt。 13. Provider 差异矩阵 canonical 层（ModelRequest）保持 provider 中立，差异全部压进 zeta-api 两个请求构造器。 authoring 规则以最严格交集为准： 维度 Anthropic Messages OpenAI Responses canonical 规则 --- --- --- --- instructions system 字段 i"
  },
  {
    "slug": "agent-tools-spec",
    "sourcePath": "docs/agent-tools-spec.md",
    "title": "Agent 工具规格",
    "description": "所有工具 schema 按最严格 provider 交集编写，同一份 schema 直接用于 Anthropic inputschema 与 OpenAI strict 模式：",
    "group": "Agent 与运行时",
    "markdown": "> 状态：Accepted（2026-08-03）\n> 定位：[`agent-harness-design.md`](agent-harness-design.md) §5 工具面的实现规格：逐工具的\n> JSON schema、**描述正文（模型可见的实际英文文本）**、参数校验规则、错误文案、输出格式与\n> 限幅、capability 注记；附录 A 是系统提示词的工具指导与输出风格扩写正文（M0 时移入\n> `zeta-prompts` 模板并 bump revision）。\n>\n> 工具的三层契约（定义/绑定/执行接口）归 [`tools.md`](tools.md)；审批/沙箱/升级语义归\n> [`core.md`](core.md) §11。本文写\"每个工具具体长什么样\"。描述与错误文案是模型可见提示词\n> 的一部分：修改需要与 system prompt 同级 review，并跑\n> [`agent-harness-design.md` §14](agent-harness-design.md#14-评测) 的评测对比。\n\n## 1. Schema 约定\n\n所有工具 schema 按**最严格 provider 交集**编写，同一份 schema 直接用于 Anthropic\n`input_schema` 与 OpenAI strict 模式：\n\n1. 顶层必须是 `{\"type\": \"object\"}`；\n2. `additionalProperties: false`；\n3. **所有**属性进 `required`；可选性用类型并集表达：`{\"type\": [\"string\", \"null\"]}`；\n4. 只用 `string` / `number` / `integer` / `boolean` / `array` / `object` / `enum`；\n   顶层不用 `oneOf` / `anyOf` / `allOf`；\n5. 每个参数带 `description`；数值参数写明单位与默认值；\n6. 工具名用 `snake_case`，动词开头或名词短语，与描述首句一致。\n\n错误文案原则：错误信息是模型的下一条输入——必须**可自纠**（说明哪个参数错、给出正确\n形态或下一步动作），不写只有人类能懂的内部错误。\n\n## 2. shell\n\n| | |\n| --- | --- |\n| 状态 | 已接入（`zeta-shell-command` + `local_tools.rs`），schema 需按本节收敛 |\n| 执行 | 沙箱 + 审批走 `zeta-action-policy`；执行上限 256 KiB / 默认 30s（已有） |\n| 模型侧限幅 | 30 KiB，头尾各半，中间标注 `[... N bytes truncated ...]` |\n\n**description（模型可见）：**\n\n```text\nExecutes a shell command in the workspace and returns its combined stdout and\nstderr with the exit code.\n\nUsage notes:\n- Use dedicated tools instead of shell equivalents when available: read_file\n  instead of cat, grep instead of grep/rg, glob instead of find, edit or\n  apply_patch instead of sed -i. Dedicated tools produce better results.\n- Commands run with the workspace root as the default working directory; state\n  such as environment variables does not persist between calls. Chain dependent\n  steps with && in a single call.\n- Long output is truncated from the middle; rerun with a narrower command (e.g.\n  pipe through head/tail) if you need the omitted part.\n- Never run interactive commands that wait for input (e.g. git rebase -i,\n  npm init without -y); they will hang until the timeout.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"command\": {\n      \"type\": \"string\",\n      \"description\": \"The shell command to execute, run via the login shell with -c.\"\n    },\n    \"timeout_ms\": {\n      \"type\": [\"integer\", \"null\"],\n      \"description\": \"Timeout in milliseconds. Default 30000, maximum 600000.\"\n    },\n    \"working_directory\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"Absolute path to run in. Defaults to the workspace root. Must stay inside the workspace.\"\n    }\n  },\n  \"required\": [\"command\", \"timeout_ms\", \"working_directory\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**\n\n| 情形 | 结果（`is_error: true` 的 Tool Result 文本） |\n| --- | --- |\n| `command` 为空/全空白 | `command must not be empty` |\n| `working_directory` 在 workspace 外 | `working_directory is outside the workspace: {path}. Use a path under {root}` |\n| `timeout_ms` 超上限 | `timeout_ms exceeds the maximum of 600000` |\n| 超时 | `command timed out after {n} ms. Partial output:\\n{截断输出}` |\n| 非零退出 | 正常结果：输出 + `exit code: {n}`（非零退出不是工具错误） |\n\n**输出格式：**合并流文本 + 末行 `exit code: {n}`。沙箱拒绝走 `SandboxDenialOutput` 结构化\n路径（[`core.md`](core.md) §11），不进本表。\n\n## 3. read_file\n\n| | |\n| --- | --- |\n| 状态 | `file-system-tool` 已有 `read` 操作，需改造为独立工具 + 行号/offset/limit/图片 |\n| 模型侧限幅 | 默认 2000 行；单行 > 2000 字符截断并标注 |\n\n**description：**\n\n```text\nReads a file from the workspace and returns its content with line numbers.\n\nUsage notes:\n- Returns at most 2000 lines starting from `offset` (1-based). The last line\n  of a truncated read says how many lines remain; call again with a larger\n  offset to continue.\n- Lines longer than 2000 characters are truncated with a marker.\n- Image files (png, jpg, gif, webp) are returned as viewable images.\n- You must read a file before editing or overwriting it.\n- Prefer reading whole files (omit offset/limit) unless the file is too large.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"path\": {\n      \"type\": \"string\",\n      \"description\": \"Absolute path to the file to read.\"\n    },\n    \"offset\": {\n      \"type\": [\"integer\", \"null\"],\n      \"description\": \"1-based line number to start from. Defaults to 1.\"\n    },\n    \"limit\": {\n      \"type\": [\"integer\", \"null\"],\n      \"description\": \"Maximum lines to return. Defaults to 2000.\"\n    }\n  },\n  \"required\": [\"path\", \"offset\", \"limit\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**\n\n| 情形 | 文案 |\n| --- | --- |\n| 文件不存在 | `file not found: {path}`；若同目录有相近命名，附 `did you mean {candidate}?` |\n| 是目录 | `{path} is a directory. Use glob to list its files` |\n| 二进制且非图片 | `{path} is a binary file and cannot be displayed as text` |\n| 超出 workspace | `path is outside the workspace: {path}` |\n| 空文件 | 正常结果：`(file is empty)` |\n\n**输出格式：**`cat -n` 风格行号 + 制表符；截断尾注\n`[... {n} more lines, continue with offset={next}]`。durable Tool Result 存完整读取结果的\n限幅版（执行侧即模型侧，读取本身无副作用）。\n\n## 4. write_file\n\n| | |\n| --- | --- |\n| 状态 | 新增（`file-system-tool` 扩展） |\n| capability | 写路径进入 `ActionReviewRequest`，沙箱/审批按路径判定 |\n\n**description：**\n\n```text\nCreates or overwrites a file with the given content.\n\nUsage notes:\n- Overwriting an existing file you have not read in this conversation fails;\n  read it first.\n- Prefer edit (or apply_patch) for modifying existing files; use write_file\n  for new files or full rewrites you have already read.\n- Parent directories are created automatically.\n- Never proactively create documentation files unless explicitly requested.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"path\": {\n      \"type\": \"string\",\n      \"description\": \"Absolute path of the file to write.\"\n    },\n    \"content\": {\n      \"type\": \"string\",\n      \"description\": \"Full content to write. The previous content is replaced entirely.\"\n    }\n  },\n  \"required\": [\"path\", \"content\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**\n\n| 情形 | 文案 |\n| --- | --- |\n| 覆盖未读过的既有文件 | `{path} exists but has not been read in this conversation. Read it first, or choose a new path` |\n| 超出 workspace | `path is outside the workspace: {path}` |\n| 目标是目录 | `{path} is a directory` |\n\n\"读过\"判定：本 Thread durable history 中存在该路径成功的 `read_file` Tool Result，且其后\n无该文件的外部修改通知 reminder。此校验由工具实现基于 Thread 提供的已读路径集完成（Core\n在 prepare 阶段传入，工具不读 Thread store——遵守 [`tools.md`](tools.md) 依赖边界）。\n\n## 5. edit（anthropic / google / 默认 profile）\n\n| | |\n| --- | --- |\n| 状态 | 新增（`file-system-tool` 扩展） |\n| 核心不变量 | `old_string` 唯一命中，否则拒绝——这条校验挡住大部分错误编辑 |\n\n**description：**\n\n```text\nPerforms an exact string replacement in a file.\n\nUsage notes:\n- You must read the file first; the edit fails otherwise.\n- old_string must match the file content exactly, including whitespace and\n  indentation, and must identify a unique location. If it matches more than\n  one location, extend it with surrounding lines until unique, or set\n  replace_all to true to change every occurrence.\n- Do not include line-number prefixes from read_file output in old_string.\n- For moving or renaming files use shell with git mv; for full rewrites use\n  write_file.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"path\": {\n      \"type\": \"string\",\n      \"description\": \"Absolute path of the file to modify.\"\n    },\n    \"old_string\": {\n      \"type\": \"string\",\n      \"description\": \"Exact text to replace. Must be unique in the file unless replace_all is true.\"\n    },\n    \"new_string\": {\n      \"type\": \"string\",\n      \"description\": \"Replacement text. Must differ from old_string.\"\n    },\n    \"replace_all\": {\n      \"type\": [\"boolean\", \"null\"],\n      \"description\": \"Replace every occurrence. Defaults to false.\"\n    }\n  },\n  \"required\": [\"path\", \"old_string\", \"new_string\", \"replace_all\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**\n\n| 情形 | 文案 |\n| --- | --- |\n| 未读过该文件 | `{path} has not been read in this conversation. Read it first` |\n| 0 命中 | `old_string not found in {path}. Re-read the file: the content may differ from what you expect (check whitespace and indentation)` |\n| ≥2 命中且非 replace_all | `old_string matches {n} locations in {path}. Extend it with more surrounding context to make it unique, or set replace_all to true` |\n| old == new | `new_string must differ from old_string` |\n| 读后被外部修改 | `{path} changed on disk after your last read. Read it again before editing` |\n\n**输出格式：**成功返回替换处 ±4 行的带行号片段（模型自查 + UI 可渲染 diff）。\n\n## 6. apply_patch（openai profile）\n\n| | |\n| --- | --- |\n| 状态 | `zeta-apply-patch` crate 已存在，需接入 profile |\n| 格式 | V4A envelope（OpenAI 系模型的训练格式，原样采用不发明方言） |\n\n**description：**\n\n```text\nApplies a patch to create, delete, or modify files using the V4A format:\n\n*** Begin Patch\n*** Update File: path/to/file\n@@ context line\n-removed line\n+added line\n*** End Patch\n\nUsage notes:\n- Use *** Add File: / *** Delete File: / *** Update File: headers; an Update\n  may include *** Move to: for renames.\n- Context lines must match the current file content exactly. Read the file\n  first if you are not certain.\n- Keep patches minimal and focused; unrelated files must not appear.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"input\": {\n      \"type\": \"string\",\n      \"description\": \"The full patch text, starting with *** Begin Patch and ending with *** End Patch.\"\n    }\n  },\n  \"required\": [\"input\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**格式解析失败 →\n`invalid patch: {原因} at line {n}. The patch must start with *** Begin Patch and use *** Update File: / *** Add File: / *** Delete File: headers`；\n上下文行不匹配 →\n`patch does not apply to {path}: context mismatch near \"{片段}\". Read the current file content and regenerate the patch`。\n成功输出各文件的变更摘要（`M path (+a/-b)`）。\n\n## 7. glob\n\n| | |\n| --- | --- |\n| 状态 | 新增（`zeta-file-search` 已有路径索引，补 glob 语义） |\n| 限幅 | 100 条，按修改时间降序 |\n\n**description：**\n\n```text\nFinds files by glob pattern, sorted by most recently modified.\n\n- Supports patterns like \"**/*.rs\" or \"src/**/*.test.ts\".\n- Returns at most 100 paths; narrow the pattern if truncated.\n- Use grep to search file contents; use glob to find files by name.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"pattern\": {\n      \"type\": \"string\",\n      \"description\": \"Glob pattern to match file paths against.\"\n    },\n    \"path\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"Directory to search in. Defaults to the workspace root.\"\n    }\n  },\n  \"required\": [\"pattern\", \"path\"],\n  \"additionalProperties\": false\n}\n```\n\n**错误文案：**非法 pattern → `invalid glob pattern: {原因}`；无命中 → 正常结果\n`no files match {pattern}`（不是错误）。截断尾注 `[{total} matches, showing first 100]`。\n\n## 8. grep\n\n| | |\n| --- | --- |\n| 状态 | 新增封装（`RipgrepExecutable` 已被 App Server 发现和管理） |\n| 限幅 | 100 条命中；单行 > 500 字符截断 |\n\n**description：**\n\n```text\nSearches file contents with a regular expression (ripgrep syntax).\n\n- Full regex support, e.g. \"fn\\\\s+resolve\" or \"TODO|FIXME\".\n- Results are file:line:content, capped at 100 matches; narrow with `glob`\n  or `path` if truncated.\n- Literal braces or dots must be escaped: use \"foo\\\\.bar\" to match \"foo.bar\".\n- Always use this tool instead of invoking grep or rg through shell.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"pattern\": {\n      \"type\": \"string\",\n      \"description\": \"Regular expression to search for (ripgrep syntax).\"\n    },\n    \"path\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"File or directory to search. Defaults to the workspace root.\"\n    },\n    \"glob\": {\n      \"type\": [\"string\", \"null\"],\n      \"description\": \"Restrict to files matching this glob, e.g. \\\"*.rs\\\".\"\n    },\n    \"case_insensitive\": {\n      \"type\": [\"boolean\", \"null\"],\n      \"description\": \"Case-insensitive search. Defaults to false.\"\n    }\n  },\n  \"required\": [\"pattern\", \"path\", \"glob\", \"case_insensitive\"],\n  \"additionalProperties\": false\n}\n```\n\n**错误文案：**正则非法 → 透传 rg 的错误信息 + `escape literal characters like . ( ) { } with a backslash`；\n无命中 → 正常结果 `no matches`。默认遵守 .gitignore（rg 行为）。\n\n## 9. update_plan\n\n| | |\n| --- | --- |\n| 状态 | 新增薄工具，durable 提交 `ThreadItem::Plan`（protocol 已有该 Item） |\n| 组装 | assembler 停止跳过 Plan：**最新一条** Plan 注入当前窗口，旧 Plan 被压缩吸收 |\n\n**description：**\n\n```text\nRecords or updates your plan for a multi-step task.\n\n- Use for tasks that need 3 or more distinct steps; skip it for trivial work.\n- Keep exactly one step in_progress at a time. Mark a step completed as soon\n  as it is done; update the plan when scope changes rather than following a\n  stale plan.\n- Steps are short imperative phrases (\"Fix parser offset bug\"), not essays.\n```\n\n**parameters：**\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"plan\": {\n      \"type\": \"array\",\n      \"description\": \"The full plan, replacing any previous plan.\",\n      \"items\": {\n        \"type\": \"object\",\n        \"properties\": {\n          \"step\": { \"type\": \"string\", \"description\": \"Short imperative description.\" },\n          \"status\": { \"type\": \"string\", \"enum\": [\"pending\", \"in_progress\", \"completed\"] }\n        },\n        \"required\": [\"step\", \"status\"],\n        \"additionalProperties\": false\n      }\n    }\n  },\n  \"required\": [\"plan\"],\n  \"additionalProperties\": false\n}\n```\n\n**校验与错误文案：**空 plan → `plan must contain at least one step`；\n多个 in_progress → `keep at most one step in_progress (found {n})`；\nstep 为空 → `every step needs a non-empty description`。成功输出 `plan updated`。\n\n## 10. MCP 元工具（检索式模式，[harness §6](agent-harness-design.md#6-工具注册时机)）\n\n### 10.1 search_tools\n\n```text\nSearches the available MCP tools by keyword and returns matching tool\ndefinitions. Use when the task needs a capability not covered by your core\ntools (e.g. a service integration). Found definitions become callable via\ncall_mcp_tool.\n```\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"query\": { \"type\": \"string\", \"description\": \"Keywords describing the needed capability.\" }\n  },\n  \"required\": [\"query\"],\n  \"additionalProperties\": false\n}\n```\n\n返回 ≤ 5 个定义（name + description + schema），以 reminder 形式进入历史\n（[harness §4.4](agent-harness-design.md#44-动态注入append-only-reminder)），不修改 tools\n数组。无命中 → `no MCP tools match \"{query}\". Available servers: {list}`。\n\n### 10.2 call_mcp_tool\n\n```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"tool\": { \"type\": \"string\", \"description\": \"Fully qualified name server__tool from search_tools results.\" },\n    \"arguments\": { \"type\": \"object\", \"description\": \"Arguments matching the tool's schema.\", \"additionalProperties\": true }\n  },\n  \"required\": [\"tool\", \"arguments\"],\n  \"additionalProperties\": false\n}\n```\n\n未先 search 的调用 → `unknown tool {name}; use search_tools first`。参数校验错误透传 MCP\nserver 的 schema 错误。执行、审批与结果转换沿用 [`tools.md`](tools.md) §10 的 MCP 适配器。\n\n## 11. 内建子代理工具\n\n| | |\n| --- | --- |\n| 状态 | 已接入可信 Workspace 的 App Server Tool composition |\n| 执行 | `MultiAgentCoordinator` + 独立 child Thread；不经 MCP 自调用 |\n| 权限 | child 只获得 spawn 时冻结的 tool name ceiling 与 active Skill digest |\n\n### 11.1 spawn_agent\n\n创建一个独立历史的 child Agent Thread，立即返回 `delegation_id`、`child_thread_id` 和\n`child_turn_id`。参数为完整 `task: string`、可空短标签 `name` 和可空 `context`。`context`\n支持 `fresh`、`full`、`lastTurns`、`checkpointAndTail`、`selected`；选中内容在 spawn 时固定\nsource sequence、物化内容与 digest，再随 immutable seed 注入。Skill 与 Tool ceiling 同样冻结。\n\n### 11.2 send_agent_message\n\n参数为 `delegation_id: string` 与非空 `message: string`。消息先写 sender outbox，再写 child\ninbox；相同 Tool Call identity 重放时只投递一次。\n\n### 11.3 wait_agent\n\n参数可以选择单个 `delegation_id`、多个 `delegation_ids`，或省略二者冻结 parent 当前全部\ndelegation；`policy` 支持 `all`、`any`、`quorum`，最长等待 30000 ms。调用先提交 durable join，\n再从 exact-once delegation results 求值；超时返回 waiting join，满足时返回 `satisfiedBy` 与\nbounded results。进程恢复会重新求值 waiting join。\n\nApp Server 的 parent Turn interrupt 与 Session stop 会提交 cancellation facts，并向 live child\ndescendants 递归传播；Desktop Agent Sidebar 根据 Session lineage 与 thread projection 显示根\nThread、Agent child、fork/rewind branch 及当前 execution status。\n\n## 附录 A：系统提示词扩写正文\n\n以下为 `SYSTEM_PROMPT` 的工具指导与输出风格扩写段，M0 时追加进\n`zeta-rs/prompts/templates/system/`（工具指导按 profile 分模板）并 bump revision。现有\nbase.md 的身份/优先级/防注入/工作行为四段保留在前。\n\n### A.1 工具指导（共享段）\n\n```text\n## Tool usage\n\n- Search before you read, read before you edit: locate code with grep and\n  glob, read the relevant files, then make changes. Do not edit code you have\n  not seen.\n- Use the dedicated tools (read_file, grep, glob, edit) instead of their shell\n  equivalents (cat, rg, find, sed). Use shell for builds, tests, git, and\n  anything without a dedicated tool.\n- Prefer several small, verifiable changes over one large speculative change.\n- After a code change, verify it with the narrowest relevant check (the\n  affected test, a typecheck, a targeted build) before moving on. Do not claim\n  success without having verified.\n- When a command or tool fails twice with the same error, stop repeating it.\n  Diagnose, try a different approach, or report the blocker.\n- For tasks with 3 or more distinct steps, maintain a plan with update_plan\n  and keep it current.\n```\n\n### A.2 工具指导（profile 差异段）\n\nedit profile（anthropic / google / 默认）：\n\n```text\n- Modify files with edit using an exact unique snippet; extend old_string\n  with surrounding lines when the match is ambiguous. Use write_file only for\n  new files or full rewrites of files you have read.\n```\n\napply_patch profile（openai）：\n\n```text\n- Modify files with apply_patch. Keep each patch focused on one logical\n  change; regenerate the patch from current file content when context lines\n  fail to match.\n```\n\n### A.3 输出风格（共享段）\n\n```text\n## Output style\n\n- Your replies render in a developer-facing client. Be concise and direct:\n  answer first, qualifications after, no filler (\"Great!\", \"Certainly\").\n- Reference code as `path:line` so the user can jump to it. Use fenced code\n  blocks only for code, commands, or file content - not for emphasis.\n- After completing a task, summarize what changed and what you verified in a\n  few sentences. Report failures plainly with the relevant output; never\n  claim an unverified result.\n- Ask the user only when a decision genuinely belongs to them (destructive\n  actions, ambiguous requirements with materially different readings);\n  otherwise pick the reasonable default and note the assumption.\n```\n\n## 附录 B：修改清单\n\n修改本文任何工具的 schema、描述或错误文案时同步：\n\n1. 对应实现 crate 的 schema/文案常量与测试；\n2. `zeta-prompts` 中引用该工具名的指导段（附录 A）与 revision；\n3. [`agent-harness-design.md` §14](agent-harness-design.md#14-评测) 的组装快照 fixture；\n4. 评测跑 T1/T2 对比，回归超阈值不合入。",
    "headings": [
      {
        "depth": 2,
        "id": "1-schema-约定",
        "title": "1. Schema 约定"
      },
      {
        "depth": 2,
        "id": "2-shell",
        "title": "2. shell"
      },
      {
        "depth": 2,
        "id": "3-readfile",
        "title": "3. readfile"
      },
      {
        "depth": 2,
        "id": "4-writefile",
        "title": "4. writefile"
      },
      {
        "depth": 2,
        "id": "5-editanthropic-google-默认-profile",
        "title": "5. edit（anthropic / google / 默认 profile）"
      },
      {
        "depth": 2,
        "id": "6-applypatchopenai-profile",
        "title": "6. applypatch（openai profile）"
      },
      {
        "depth": 2,
        "id": "7-glob",
        "title": "7. glob"
      },
      {
        "depth": 2,
        "id": "8-grep",
        "title": "8. grep"
      },
      {
        "depth": 2,
        "id": "9-updateplan",
        "title": "9. updateplan"
      },
      {
        "depth": 2,
        "id": "10-mcp-元工具检索式模式harness-6",
        "title": "10. MCP 元工具（检索式模式，harness §6）"
      },
      {
        "depth": 3,
        "id": "101-searchtools",
        "title": "10.1 searchtools"
      },
      {
        "depth": 3,
        "id": "102-callmcptool",
        "title": "10.2 callmcptool"
      },
      {
        "depth": 2,
        "id": "11-内建子代理工具",
        "title": "11. 内建子代理工具"
      },
      {
        "depth": 3,
        "id": "111-spawnagent",
        "title": "11.1 spawnagent"
      },
      {
        "depth": 3,
        "id": "112-sendagentmessage",
        "title": "11.2 sendagentmessage"
      },
      {
        "depth": 3,
        "id": "113-waitagent",
        "title": "11.3 waitagent"
      },
      {
        "depth": 2,
        "id": "附录-a系统提示词扩写正文",
        "title": "附录 A：系统提示词扩写正文"
      },
      {
        "depth": 3,
        "id": "a1-工具指导共享段",
        "title": "A.1 工具指导（共享段）"
      },
      {
        "depth": 2,
        "id": "tool-usage",
        "title": "Tool usage"
      },
      {
        "depth": 3,
        "id": "a2-工具指导profile-差异段",
        "title": "A.2 工具指导（profile 差异段）"
      },
      {
        "depth": 3,
        "id": "a3-输出风格共享段",
        "title": "A.3 输出风格（共享段）"
      },
      {
        "depth": 2,
        "id": "output-style",
        "title": "Output style"
      },
      {
        "depth": 2,
        "id": "附录-b修改清单",
        "title": "附录 B：修改清单"
      }
    ],
    "searchText": " 状态：Accepted（2026-08-03） 定位：agent-harness-design.md §5 工具面的实现规格：逐工具的 JSON schema、描述正文（模型可见的实际英文文本）、参数校验规则、错误文案、输出格式与 限幅、capability 注记；附录 A 是系统提示词的工具指导与输出风格扩写正文（M0 时移入 zeta-prompts 模板并 bump revision）。 工具的三层契约（定义/绑定/执行接口）归 tools.md；审批/沙箱/升级语义归 core.md §11。本文写\"每个工具具体长什么样\"。描述与错误文案是模型可见提示词 的一部分：修改需要与 system prompt 同级 review，并跑 agent-harness-design.md §14 的评测对比。 1. Schema 约定 所有工具 schema 按最严格 provider 交集编写，同一份 schema 直接用于 Anthropic inputschema 与 OpenAI strict 模式： 1. 顶层必须是 \"type\": \"object\" ； 2. additionalProperties: false； 3. 所有属性进 required；可选性用类型并集表达： \"type\": \"string\", \"null\" ； 4. 只用 string / number / integer / boolean / array / object / enum； 顶层不用 oneOf / anyOf / allOf； 5. 每个参数带 description；数值参数写明单位与默认值； 6. 工具名用 snakecase，动词开头或名词短语，与描述首句一致。 错误文案原则：错误信息是模型的下一条输入——必须可自纠（说明哪个参数错、给出正确 形态或下一步动作），不写只有人类能懂的内部错误。 2. shell --- --- 状态 已接入（zeta-shell-command + localtools.rs），schema 需按本节收敛 执行 沙箱 + 审批走 zeta-action-policy；执行上限 256 KiB / 默认 30s（已有） 模型侧限幅 30 KiB，头尾各半，中间标注 ... N bytes truncated ... description（模型可见）： ``text Executes a shell command in the workspace and returns its combined stdout and stderr with the exit code. Usage notes: - Use dedicated tools instead of shell equivalents when available: readfile instead of cat, grep instead of grep/rg, glob instead of find, edit or applypatch instead of sed -i. Dedicated tools produce better results. - Commands run with the workspace root as the default working directory; state such as environment variables does not persist between calls. Chain dependent steps with && in a single call. - Long output is truncated from the middle; rerun with a narrower command e.g. pipe through head/tail if you need the omitted part. - Never run interactive commands that wait for input e.g. git rebase -i, npm init without -y ; they will hang until the timeout. ` parameters： `json \"type\": \"object\", \"properties\": \"command\": \"type\": \"string\", \"description\": \"The shell command to execute, run via the login shell with -c.\" , \"timeoutms\": \"type\": \"integer\", \"null\" , \"description\": \"Timeout in milliseconds. Default 30000, maximum 600000.\" , \"workingdirectory\": \"type\": \"string\", \"null\" , \"description\": \"Absolute path to run in. Defaults to the workspace root. Must stay inside the workspace.\" , \"required\": \"command\", \"timeoutms\", \"workingdirectory\" , \"additionalProperties\": false ` 校验与错误文案： 情形 结果（iserror: true 的 Tool Result 文本） --- --- command 为空/全空白 command must not be empty workingdirectory 在 workspace 外 workingdirectory is outside the workspace: path . Use a path under root timeoutms 超上限 timeoutms exceeds the maximum of 600000 超时 command timed out after n ms. Partial output:\\n 截断输出 非零退出 正常结果：输出 + exit code: n （非零退出不是工具错误） 输出格式：合并流文本 + 末行 exit code: n 。沙箱拒绝走 SandboxDenialOutput 结构化 路径（core.md §11），不进本表。 3. readfile --- --- 状态 file-system-tool 已有 read 操作，需改造为独立工具 + 行号/offset/limit/图片 模型侧限幅 默认 2000 行；单行 2000 字符截断并标注 description： `text Reads a file from the workspace and returns its content with line numbers. Usage notes: - Returns at most 2000 lines starting from offset 1-based . The last line of a truncated read says how many lines remain; call again with a larger offset to continue. - Lines longer than 2000 characters are truncated with a marker. - Image files png, jpg, gif, webp are returned as viewable images. - You must read a file before editing or overwriting it. - Prefer reading whole files omit offset/limit unless the file is too large. ` parameters： `json \"type\": \"object\", \"properties\": \"path\": \"type\": \"string\", \"description\": \"Absolute path to the file to read.\" , \"offset\": \"type\": \"integer\", \"null\" , \"description\": \"1-based line number to start from. Defaults to 1.\" , \"limit\": \"type\": \"integer\", \"null\" , \"description\": \"Maximum lines to return. Defaults to 2000.\" , \"required\": \"path\", \"offset\", \"limit\" , \"additionalProperties\": false ` 校验与错误文案： 情形 文案 --- --- 文件不存在 file not found: path ；若同目录有相近命名，附 did you mean candidate ? 是目录 path is a directory. Use glob to list its files 二进制且非图片 path is a binary file and cannot be displayed as text 超出 workspace path is outside the workspace: path 空文件 正常结果： file is empty 输出格式：cat -n 风格行号 + 制表符；截断尾注 ... n more lines, continue with offset= next 。durable Tool Result 存完整读取结果的 限幅版（执行侧即模型侧，读取本身无副作用）。 4. writefile --- --- 状态 新增（file-system-tool 扩展） capability 写路径进入 ActionReviewRequest，沙箱/审批按路径判定 description： `text Creates or overwrites a file with the given content. Usage notes: - Overwriting an existing file you have not read in this conversation fails; read it first. - Prefer edit or applypatch for modifying existing files; use writefile for new files or full rewrites you have already read. - Parent directories are created automatically. - Never proactively create documentation files unless explicitly requested. ` parameters： `json \"type\": \"object\", \"properties\": \"path\": \"type\": \"string\", \"description\": \"Absolute path of the file to write.\" , \"content\": \"type\": \"string\", \"description\": \"Full content to write. The previous content is replaced entirely.\" , \"required\": \"path\", \"content\" , \"additionalProperties\": false ` 校验与错误文案： 情形 文案 --- --- 覆盖未读过的既有文件 path exists but has not been read in this conversation. Read it first, or choose a new path 超出 workspace path is outside the workspace: path 目标是目录 path is a directory \"读过\"判定：本 Thread durable history 中存在该路径成功的 readfile Tool Result，且其后 无该文件的外部修改通知 reminder。此校验由工具实现基于 Thread 提供的已读路径集完成（Core 在 prepare 阶段传入，工具不读 Thread store——遵守 tools.md 依赖边界）。 5. edit（anthropic / google / 默认 profile） --- --- 状态 新增（file-system-tool 扩展） 核心不变量 oldstring 唯一命中，否则拒绝——这条校验挡住大部分错误编辑 description： `text Performs an exact string replacement in a file. Usage notes: - You must read the file first; the edit fails otherwise. - oldstring must match the file content exactly, including whitespace and indentation, and must identify a unique location. If it matches more than one location, extend it with surrounding lines until unique, or set replaceall to true to change every occurrence. - Do not include line-number prefixes from readfile output in oldstring. - For moving or renaming files use shell with git mv; for full rewrites use writefile. ` parameters： `json \"type\": \"object\", \"properties\": \"path\": \"type\": \"string\", \"description\": \"Absolute path of the file to modify.\" , \"oldstring\": \"type\": \"string\", \"description\": \"Exact text to replace. Must be unique in the file unless replaceall is true.\" , \"newstring\": \"type\": \"string\", \"description\": \"Replacement text. Must differ from oldstring.\" , \"replaceall\": \"type\": \"boolean\", \"null\" , \"description\": \"Replace every occurrence. Defaults to false.\" , \"required\": \"path\", \"oldstring\", \"newstring\", \"replaceall\" , \"additionalProperties\": false ` 校验与错误文案： 情形 文案 --- --- 未读过该文件 path has not been read in this conversation. Read it first 0 命中 oldstring not found in path . Re-read the file: the content may differ from what you expect check whitespace and indentation ≥2 命中且非 replaceall oldstring matches n locations in path . Extend it with more surrounding context to make it unique, or set replaceall to true old == new newstring must differ from oldstring 读后被外部修改 path changed on disk after your last read. Read it again before editing 输出格式：成功返回替换处 ±4 行的带行号片段（模型自查 + UI 可渲染 diff）。 6. applypatch（openai profile） --- --- 状态 zeta-apply-patch crate 已存在，需接入 profile 格式 V4A envelope（OpenAI 系模型的训练格式，原样采用不发明方言） description： `text Applies a patch to create, delete, or modify files using the V4A format: Begin Patch Update File: path/to/file @@ context line -removed line +added line End Patch Usage notes: - Use Add File: / Delete File: / Update File: headers; an Update may include Move to: for renames. - Context lines must match the current file content exactly. Read the file first if you are not certain. - Keep patches minimal and focused; unrelated files must not appear. ` parameters： `json \"type\": \"object\", \"properties\": \"input\": \"type\": \"string\", \"description\": \"The full patch text, starting with Begin Patch and ending with End Patch.\" , \"required\": \"input\" , \"additionalProperties\": false ` 校验与错误文案：格式解析失败 → invalid patch: 原因 at line n . The patch must start with Begin Patch and use Update File: / Add File: / Delete File: headers； 上下文行不匹配 → patch does not apply to path : context mismatch near \" 片段 \". Read the current file content and regenerate the patch。 成功输出各文件的变更摘要（M path +a/-b ）。 7. glob --- --- 状态 新增（zeta-file-search 已有路径索引，补 glob 语义） 限幅 100 条，按修改时间降序 description： `text Finds files by glob pattern, sorted by most recently modified. - Supports patterns like \"/.rs\" or \"src//.test.ts\". - Returns at most 100 paths; narrow the pattern if truncated. - Use grep to search file contents; use glob to find files by name. ` parameters： `json \"type\": \"object\", \"properties\": \"pattern\": \"type\": \"string\", \"description\": \"Glob pattern to match file paths against.\" , \"path\": \"type\": \"string\", \"null\" , \"description\": \"Directory to search in. Defaults to the workspace root.\" , \"required\": \"pattern\", \"path\" , \"additionalProperties\": false ` 错误文案：非法 pattern → invalid glob pattern: 原因 ；无命中 → 正常结果 no files match pattern （不是错误）。截断尾注 total matches, showing first 100 。 8. grep --- --- 状态 新增封装（RipgrepExecutable 已被 App Server 发现和管理） 限幅 100 条命中；单行 500 字符截断 description： `text Searches file contents with a regular expression ripgrep syntax . - Full regex support, e.g. \"fn\\\\s+resolve\" or \"TODO FIXME\". - Results are file:line:content, capped at 100 matches; narrow with glob or path if truncated. - Literal braces or dots must be escaped: use \"foo\\\\.bar\" to match \"foo.bar\". - Always use this tool instead of invoking grep or rg through shell. ` parameters： `json \"type\": \"object\", \"properties\": \"pattern\": \"type\": \"string\", \"description\": \"Regular expression to search for ripgrep syntax .\" , \"path\": \"type\": \"string\", \"null\" , \"description\": \"File or directory to search. Defaults to the workspace root.\" , \"glob\": \"type\": \"string\", \"null\" , \"description\": \"Restrict to files matching this glob, e.g. \\\".rs\\\".\" , \"caseinsensitive\": \"type\": \"boolean\", \"null\" , \"description\": \"Case-insensitive search. Defaults to false.\" , \"required\": \"pattern\", \"path\", \"glob\", \"caseinsensitive\" , \"additionalProperties\": false ` 错误文案：正则非法 → 透传 rg 的错误信息 + escape literal characters like . with a backslash； 无命中 → 正常结果 no matches。默认遵守 .gitignore（rg 行为）。 9. updateplan --- --- 状态 新增薄工具，durable 提交 ThreadItem::Plan（protocol 已有该 Item） 组装 assembler 停止跳过 Plan：最新一条 Plan 注入当前窗口，旧 Plan 被压缩吸收 description： `text Records or updates your plan for a multi-step task. - Use for tasks that need 3 or more distinct steps; skip it for trivial work. - Keep exactly one step inprogress at a time. Mark a step completed as soon as it is done; update the plan when scope changes rather than following a stale plan. - Steps are short imperative phrases \"Fix parser offset bug\" , not essays. ` parameters： `json \"type\": \"object\", \"properties\": \"plan\": \"type\": \"array\", \"description\": \"The full plan, replacing any previous plan.\", \"items\": \"type\": \"object\", \"properties\": \"step\": \"type\": \"string\", \"description\": \"Short imperative description.\" , \"status\": \"type\": \"string\", \"enum\": \"pending\", \"inprogress\", \"completed\" , \"required\": \"step\", \"status\" , \"additionalProperties\": false , \"required\": \"plan\" , \"additionalProperties\": false ` 校验与错误文案：空 plan → plan must contain at least one step； 多个 inprogress → keep at most one step inprogress found n ； step 为空 → every step needs a non-empty description。成功输出 plan updated。 10. MCP 元工具（检索式模式，harness §6） 10.1 searchtools `text Searches the available MCP tools by keyword and returns matching tool definitions. Use when the task needs a capability not covered by your core tools e.g. a service integration . Found definitions become callable via callmcptool. ` `json \"type\": \"object\", \"properties\": \"query\": \"type\": \"string\", \"description\": \"Keywords describing the needed capability.\" , \"required\": \"query\" , \"additionalProperties\": false ` 返回 ≤ 5 个定义（name + description + schema），以 reminder 形式进入历史 （harness §4.4），不修改 tools 数组。无命中 → no MCP tools match \" query \". Av"
  },
  {
    "slug": "agent-harness-implementation-plan",
    "sourcePath": "docs/agent-harness-implementation-plan.md",
    "title": "Coding 最小闭环实施计划（M0+M1+基础弹性）",
    "description": "做（对应 harness 文档 M0 + M1 + M2 最小子集）：",
    "group": "Agent 与运行时",
    "markdown": "> 状态：部分完成（2026-08-12；WI1 的 Workspace Instruction 纵向切片已完成）\n> 目标：完成后**接入任意已配置的模型即可执行真实 coding 任务**——理解仓库、搜代码、\n> 读代码、改代码、跑命令、要审批、被打断。\n> 规格依据：[`agent-harness-design.md`](agent-harness-design.md)（行为策略）、\n> [`agent-tools-spec.md`](agent-tools-spec.md)（schema/描述/错误文案，本文不重复）。\n> 本文只负责：文件级改动点、顺序依赖、每项验收。\n\n## 0. 范围\n\n**做**（对应 harness 文档 M0 + M1 + M2 最小子集）：\n\n| 工作项 | 内容 |\n| --- | --- |\n| WI1 | 提示词接线：instructions 注入 + 环境快照 + Global `.zeta/instructions` |\n| WI2 | read_file / write_file / edit 三工具 |\n| WI3 | grep / glob 两工具（均基于已管理的 ripgrep） |\n| WI4 | 本地工具组合改造：单工具 → 多工具套件分发 |\n| WI5 | `parallel_tool_calls` 放开 + 工具描述接入 |\n| WI6 | 模型调用基础弹性：429/5xx 退避、空响应重试 |\n\n**明确不做**（后续里程碑，不阻塞\"能 coding\"）：`apply_patch` 与 ToolProfile 机制（v0 单\nprofile 用 edit，接 OpenAI 系时补）、`update_plan`、MCP 阈值策略、压缩、prompt cache、\n`turn/steer`、Contextual/OnDemand Instruction 选择。\n\n## 1. WI1 提示词接线\n\n### 1.1 `zeta-rs/prompts`\n\n- `templates/system/base.md`：追加 [`agent-tools-spec.md` 附录 A](agent-tools-spec.md#附录-a系统提示词扩写正文)\n  的 A.1（工具指导共享段）+ A.2 edit 变体 + A.3（输出风格）。v0 单 profile，不拆\n  per-profile 模板；\n- `src/system.rs`：bump `SYSTEM_PROMPT` revision；\n- 按 crate README 修改清单同步 `prompt_tests.rs`（正文非空/trailing newline 已覆盖，无新增）。\n\n### 1.2 `zeta-rs/core`\n\n- 新增 `src/context/instructions.rs`（private module）：\n\n  ```text\n  HarnessInstructions {\n      system_body: String,          // SYSTEM_PROMPT body（含工具指导与输出风格）\n      environment: String,          // 渲染后的 <environment> 块\n      workspace_instructions: Option<String>,  // 已校验的 Global Workspace Instructions\n  }\n  ```\n\n  渲染规则按 [`agent-harness-design.md` §4.2](agent-harness-design.md#42-环境快照精确字段)：\n  environment 块字段全集 + 冻结声明文案。\n- `src/context/assembler.rs`：`assemble(snapshot, tools)` →\n  `assemble(snapshot, tools, instructions: &HarnessInstructions)`：\n  - `ModelRequest.instructions = Some(system_body + \"\\n\\n\" + environment)`；\n  - `workspace_instructions` 存在时作为 `input[0]` user message，正文包裹\n    \"工作区指令，优先级低于系统与安全策略\"标注（文案照 harness §4.3）；\n  - `parallel_tool_calls: true`（WI5，顺手在此改）。\n- `src/turn/executor.rs`：`TurnExecutor` 保留固定 `with_instructions` builder，并新增\n  `with_instructions_provider`；每次 model invocation 先取得不可变 snapshot，再调用 assembler。\n- `lib.rs`：导出 `HarnessInstructions` 与 `HarnessInstructionsProvider`。\n\n### 1.3 `zeta-rs/app-server`\n\n- `src/local.rs`：环境采集函数提供 cwd、platform、\n  OS 版本（`uname -r` 级）、shell、日期（天级）、`git branch --show-current` /\n  `git status --porcelain`（≤40 行）/ `git log --oneline -5`，git 命令失败按非 git 仓库\n  处理；`server/workspace_customizations.rs` 读取 `.zeta/instructions` 和 `.zeta/agents`，构造\n  `HarnessInstructions` snapshot 并接入 Workspace watcher。环境只在 workspace runtime 创建时\n  采集一次；Instruction 文件变化只影响后续 model invocation。\n\n### 1.4 验收\n\n- assembler 与 catalog 单测：instructions 注入、`input[0]` 存在/缺省、格式/大小限制与非 Global\n  条目不自动注入；\n- **字节稳定测试**：同一 snapshot 连续 assemble 两次，序列化结果逐字节相等（M4 缓存回归\n  的基线，现在就建）；\n- 手动冒烟：CLI 连本地 App Server + 真实 provider，模型应遵循合法 Global Workspace\n  Instruction，并能使用环境中的 git 状态回答。\n\n## 2. WI2 read_file / write_file / edit\n\n### 2.1 `zeta-rs/file-system-tool`\n\n现状是单个 operation-enum 工具（read/list/metadata，64 KiB 字节上限）。改造为三个模型可见\n工具（schema/描述/错误文案全部照 [`agent-tools-spec.md`](agent-tools-spec.md) §3–§5）：\n\n- 新增 `src/read.rs`：行式读取（替代 64 KiB 字节截断）——2000 行默认、offset/limit、\n  单行 2000 字符截断、`cat -n` 行号、空文件与二进制/图片分支（v0 图片可先返回\n  \"binary file\" 错误，图片 content 留到 provider 图片链路验证后）；\n- 新增 `src/write.rs`：全量写 + 父目录创建 + 已读前置校验；\n- 新增 `src/edit.rs`：唯一命中校验（0 命中 / 多命中 / old==new / 读后外部修改四分支错误\n  文案）+ `replace_all` + 成功输出 ±4 行带行号片段；\n- 旧 operation-enum 工具保留给现有消费者，标记 deprecated，agent 路径不再暴露。\n\n### 2.2 已读文件集（edit/write 前置）\n\n- `zeta-rs/core/src/services.rs`：`ToolService::execute` /`execute_streaming` 增加\n  `facts: &ToolExecutionFacts` 参数：\n\n  ```text\n  ToolExecutionFacts { read_paths: BTreeSet<PathBuf> }\n  ```\n\n- `src/turn/tool_scheduler.rs`：执行前从 `ThreadSnapshot` 推导——历史中成功的 read_file\n  Tool Result 提取路径（解析 Tool Call arguments 的 path 字段）；\n- 前置违规在 `execute` 内返回 `is_error` Tool Result（不在 prepare 阶段报错——prepare\n  失败会 fail Turn，而前置违规是模型可自纠的正常失败）；\n- 波及面：`NoTools`、MCP 适配器、所有测试 fake 的签名同步（机械改动）。\n\n### 2.3 验收\n\n- 三工具各自的单测覆盖 tools-spec 错误文案表的每一行；\n- 集成测：fake model 发起 read → edit → 验证文件变更 durable 落盘；未读先改被拒且 Turn\n  不失败。\n\n## 3. WI3 grep / glob\n\n两者都基于已被 App Server 发现和管理的 `RipgrepExecutable`\n（`zeta-rs/shell-command/src/ripgrep.rs`），**不扩展 `file-search` crate**（它是 TUI 模糊\n搜索，语义不同）：\n\n- 新增 `zeta-rs/app-server/src/local_tools/grep.rs`：`rg -n --no-heading` + glob/大小写参\n  数映射，100 条与 500 字符/行限幅，正则错误透传 + 转义提示文案；\n- 新增 `local_tools/glob.rs`：`rg --files --glob <pattern>`，结果按 mtime 降序，100 条\n  限幅；\n- 两者 capability 为只读 + workspace 路径域，沙箱兼容（`SandboxCompatibility` 按现有\n  shell 只读判定复用）。\n\n验收：单测（限幅、排序、gitignore 默认遵守、非法 pattern 文案）。\n\n## 4. WI4 本地工具组合改造\n\n`zeta-rs/app-server/src/local_tools.rs` 现状是 `LocalShellToolService` 单工具（definitions\n返回一个、prepare/materialize 按单一 name 校验）。改造：\n\n- 新增 `local_tools/suite.rs`：`LocalToolSuite` 持有 shell / read_file / write_file /\n  edit / grep / glob 六个成员，`definitions()` 聚合（顺序 = tools-spec 章节序，固定——\n  这是缓存前缀稳定性的一部分）；`prepare` / `execute` 按 `call.name` 分发；\n- 每个工具的 `ActionReviewRequest` 构造：read/grep/glob → 只读 capability + 路径；\n  write/edit → 写 capability + 精确路径（喂给 `zeta-action-policy` 做审批/沙箱判定，复用现\n  有 shell 的 provenance/digest 模式，`source_id` 按工具名区分）；\n- `LOCAL_POLICY_REVISION` bump（`local-shell-v2` → `local-tools-v3`）；\n- `compose_local_tools` 返回签名不变，内部换 suite。\n\n验收：现有 `local_tools_tests.rs` / `tool_composition_tests.rs` 迁移 + 新工具的\nprepare→policy→execute 路径各一条；审批弹窗场景（写文件首次要求批准）手动验证。\n\n## 5. WI5 描述与并行调用\n\n- 工具描述/schema 常量：各工具实现文件内以 `const` 持有 tools-spec 的正文（描述即提示词，\n  改动走 tools-spec 附录 B 的修改清单）；\n- `parallel_tool_calls: true` 已在 WI1 assembler 改动中带入；调度器保持串行执行（模型可\n  一次发多个调用，执行顺序 = 调用顺序，现有 `next_pending_call` 循环天然支持——确认\n  多 Tool Call 消息的 durable 记录顺序测试即可）。\n\n## 6. WI6 模型调用基础弹性\n\n范围是 [`agent-harness-design.md` §7.1](agent-harness-design.md#71-模型调用错误分类与处理)\n的最小子集（429/5xx/传输错误退避 + 空响应重试；溢出→压缩链路属 M3 不做）：\n\n- `zeta-rs/zeta-api/src/error.rs`：`ApiError` 增加\n  `RateLimited { retry_after_ms: Option<u64> }` 与 `Overloaded`；两个请求构造器从\n  HTTP 429/5xx/529 + `Retry-After` 头映射（`HttpStatus` 保底不删）；\n- `zeta-rs/model-provider` / `zeta-rs/core`：错误类别透传（`CoreError` 增加\n  `ModelTransient(String)` 类别，adapter 层映射）；\n- `zeta-rs/core/src/turn/executor.rs`：模型调用点外包重试环——基数 1s、倍率 2、上限\n  30s、抖动 ±25%、最多 4 次尝试；退避等待以 ≤100ms 步长轮询 cancellation；仅\n  `ModelTransient` 触发；空响应（无文本/无 Tool Call/无 Refusal）同请求重试 1 次后按现\n  有失败路径走；\n- Refusal 分支：现状空响应与 refusal 都走 `response_failure_message` 失败——改为\n  `ResponseItem::Refusal` 单独分支，作为最终 agent message 完成 Turn（harness §7.1）。\n\n验收：fake `ModelService` 注入 429×2→成功、5xx×4（耗尽失败）、空→成功、Refusal 完成、\n退避中 interrupt 立即取消，五条并发/时序测试。\n\n## 7. 顺序与依赖\n\n```text\nWI1 提示词（独立，先行——一天级，立刻可感知）\n   │\nWI4 组合改造 ──► WI2 文件三工具 ──┐\n   │            WI3 grep/glob ────┤──► WI5 描述收口 ──► 端到端验收\nWI6 弹性（独立，可并行）──────────┘\n```\n\n- WI4 先于 WI2/WI3（套件骨架就位，工具逐个挂入）；\n- WI2 的 `ToolService` 签名变更尽早合（波及面大、纯机械）；\n- WI6 与工具线完全无关，可并行。\n\n## 8. 端到端验收\n\n全部合入后，用真实 provider 手动跑三个场景（同时作为\n[`agent-harness-design.md` §14](agent-harness-design.md#14-评测) 任务集 T1/T2 的首批夹具）：\n\n1. **修 bug**：夹具仓库一个单文件 bug，提示\"tests 里有一个失败，修掉它\"——期望：\n   grep 定位 → read → edit → shell 跑测试 → 报告，全程无人工纠偏；\n2. **跨文件小功能**：加一个函数并在两处调用——期望 glob/grep 探索 + 多文件 edit；\n3. **权限路径**：让它写一个新文件——期望首次写触发审批，批准后完成，拒绝后 agent 报告\n   而非重试。\n\n回归门（每个 WI 合入都跑）：\n\n```bash\ncargo fmt --manifest-path zeta-rs/Cargo.toml --all -- --check\ncargo clippy --manifest-path zeta-rs/Cargo.toml --workspace --all-targets -- -D warnings\ncargo test --manifest-path zeta-rs/Cargo.toml --workspace\n```\n\n（本计划无 protocol schema 变更——`turn/steer`、checkpoint 等都在范围外，因此不涉及\n`pnpm verify:protocol` 之外的客户端同步。）\n\n## 9. 风险与开放点\n\n| 风险 | 处理 |\n| --- | --- |\n| `ToolService` 签名变更波及 MCP 适配器与全部测试 fake | 纯机械；单独一个提交先行合入 |\n| 行式 read 与现有 64 KiB 字节上限的关系 | 新 read.rs 独立实现行式；旧 operation 工具不动 |\n| git 环境采集在超大仓库慢（status 全量） | `--porcelain` 截断 40 行 + 3s 超时，超时省略该字段 |\n| OpenAI 系模型在 edit 工具上的正确率 | 已知取舍（v0 单 profile）；接 OpenAI 主力模型前补 apply_patch + profile |\n| Refusal 语义改变现有失败测试 | executor 测试同步改（原空响应失败测试拆成 空响应/Refusal 两条） |",
    "headings": [
      {
        "depth": 2,
        "id": "0-范围",
        "title": "0. 范围"
      },
      {
        "depth": 2,
        "id": "1-wi1-提示词接线",
        "title": "1. WI1 提示词接线"
      },
      {
        "depth": 3,
        "id": "11-zeta-rsprompts",
        "title": "1.1 zeta-rs/prompts"
      },
      {
        "depth": 3,
        "id": "12-zeta-rscore",
        "title": "1.2 zeta-rs/core"
      },
      {
        "depth": 3,
        "id": "13-zeta-rsapp-server",
        "title": "1.3 zeta-rs/app-server"
      },
      {
        "depth": 3,
        "id": "14-验收",
        "title": "1.4 验收"
      },
      {
        "depth": 2,
        "id": "2-wi2-readfile-writefile-edit",
        "title": "2. WI2 readfile / writefile / edit"
      },
      {
        "depth": 3,
        "id": "21-zeta-rsfile-system-tool",
        "title": "2.1 zeta-rs/file-system-tool"
      },
      {
        "depth": 3,
        "id": "22-已读文件集editwrite-前置",
        "title": "2.2 已读文件集（edit/write 前置）"
      },
      {
        "depth": 3,
        "id": "23-验收",
        "title": "2.3 验收"
      },
      {
        "depth": 2,
        "id": "3-wi3-grep-glob",
        "title": "3. WI3 grep / glob"
      },
      {
        "depth": 2,
        "id": "4-wi4-本地工具组合改造",
        "title": "4. WI4 本地工具组合改造"
      },
      {
        "depth": 2,
        "id": "5-wi5-描述与并行调用",
        "title": "5. WI5 描述与并行调用"
      },
      {
        "depth": 2,
        "id": "6-wi6-模型调用基础弹性",
        "title": "6. WI6 模型调用基础弹性"
      },
      {
        "depth": 2,
        "id": "7-顺序与依赖",
        "title": "7. 顺序与依赖"
      },
      {
        "depth": 2,
        "id": "8-端到端验收",
        "title": "8. 端到端验收"
      },
      {
        "depth": 2,
        "id": "9-风险与开放点",
        "title": "9. 风险与开放点"
      }
    ],
    "searchText": " 状态：部分完成（2026-08-12；WI1 的 Workspace Instruction 纵向切片已完成） 目标：完成后接入任意已配置的模型即可执行真实 coding 任务——理解仓库、搜代码、 读代码、改代码、跑命令、要审批、被打断。 规格依据：agent-harness-design.md（行为策略）、 agent-tools-spec.md（schema/描述/错误文案，本文不重复）。 本文只负责：文件级改动点、顺序依赖、每项验收。 0. 范围 做（对应 harness 文档 M0 + M1 + M2 最小子集）： 工作项 内容 --- --- WI1 提示词接线：instructions 注入 + 环境快照 + Global .zeta/instructions WI2 readfile / writefile / edit 三工具 WI3 grep / glob 两工具（均基于已管理的 ripgrep） WI4 本地工具组合改造：单工具 → 多工具套件分发 WI5 paralleltoolcalls 放开 + 工具描述接入 WI6 模型调用基础弹性：429/5xx 退避、空响应重试 明确不做（后续里程碑，不阻塞\"能 coding\"）：applypatch 与 ToolProfile 机制（v0 单 profile 用 edit，接 OpenAI 系时补）、updateplan、MCP 阈值策略、压缩、prompt cache、 turn/steer、Contextual/OnDemand Instruction 选择。 1. WI1 提示词接线 1.1 zeta-rs/prompts - templates/system/base.md：追加 agent-tools-spec.md 附录 A 的 A.1（工具指导共享段）+ A.2 edit 变体 + A.3（输出风格）。v0 单 profile，不拆 per-profile 模板； - src/system.rs：bump SYSTEMPROMPT revision； - 按 crate README 修改清单同步 prompttests.rs（正文非空/trailing newline 已覆盖，无新增）。 1.2 zeta-rs/core - 新增 src/context/instructions.rs（private module）： ``text HarnessInstructions systembody: String, // SYSTEMPROMPT body（含工具指导与输出风格） environment: String, // 渲染后的 <environment 块 workspaceinstructions: Option<String , // 已校验的 Global Workspace Instructions ` 渲染规则按 agent-harness-design.md §4.2： environment 块字段全集 + 冻结声明文案。 - src/context/assembler.rs：assemble snapshot, tools → assemble snapshot, tools, instructions: &HarnessInstructions ： - ModelRequest.instructions = Some systembody + \"\\n\\n\" + environment ； - workspaceinstructions 存在时作为 input 0 user message，正文包裹 \"工作区指令，优先级低于系统与安全策略\"标注（文案照 harness §4.3）； - paralleltoolcalls: true（WI5，顺手在此改）。 - src/turn/executor.rs：TurnExecutor 保留固定 withinstructions builder，并新增 withinstructionsprovider；每次 model invocation 先取得不可变 snapshot，再调用 assembler。 - lib.rs：导出 HarnessInstructions 与 HarnessInstructionsProvider。 1.3 zeta-rs/app-server - src/local.rs：环境采集函数提供 cwd、platform、 OS 版本（uname -r 级）、shell、日期（天级）、git branch --show-current / git status --porcelain（≤40 行）/ git log --oneline -5，git 命令失败按非 git 仓库 处理；server/workspacecustomizations.rs 读取 .zeta/instructions 和 .zeta/agents，构造 HarnessInstructions snapshot 并接入 Workspace watcher。环境只在 workspace runtime 创建时 采集一次；Instruction 文件变化只影响后续 model invocation。 1.4 验收 - assembler 与 catalog 单测：instructions 注入、input 0 存在/缺省、格式/大小限制与非 Global 条目不自动注入； - 字节稳定测试：同一 snapshot 连续 assemble 两次，序列化结果逐字节相等（M4 缓存回归 的基线，现在就建）； - 手动冒烟：CLI 连本地 App Server + 真实 provider，模型应遵循合法 Global Workspace Instruction，并能使用环境中的 git 状态回答。 2. WI2 readfile / writefile / edit 2.1 zeta-rs/file-system-tool 现状是单个 operation-enum 工具（read/list/metadata，64 KiB 字节上限）。改造为三个模型可见 工具（schema/描述/错误文案全部照 agent-tools-spec.md §3–§5）： - 新增 src/read.rs：行式读取（替代 64 KiB 字节截断）——2000 行默认、offset/limit、 单行 2000 字符截断、cat -n 行号、空文件与二进制/图片分支（v0 图片可先返回 \"binary file\" 错误，图片 content 留到 provider 图片链路验证后）； - 新增 src/write.rs：全量写 + 父目录创建 + 已读前置校验； - 新增 src/edit.rs：唯一命中校验（0 命中 / 多命中 / old==new / 读后外部修改四分支错误 文案）+ replaceall + 成功输出 ±4 行带行号片段； - 旧 operation-enum 工具保留给现有消费者，标记 deprecated，agent 路径不再暴露。 2.2 已读文件集（edit/write 前置） - zeta-rs/core/src/services.rs：ToolService::execute /executestreaming 增加 facts: &ToolExecutionFacts 参数： `text ToolExecutionFacts readpaths: BTreeSet<PathBuf ` - src/turn/toolscheduler.rs：执行前从 ThreadSnapshot 推导——历史中成功的 readfile Tool Result 提取路径（解析 Tool Call arguments 的 path 字段）； - 前置违规在 execute 内返回 iserror Tool Result（不在 prepare 阶段报错——prepare 失败会 fail Turn，而前置违规是模型可自纠的正常失败）； - 波及面：NoTools、MCP 适配器、所有测试 fake 的签名同步（机械改动）。 2.3 验收 - 三工具各自的单测覆盖 tools-spec 错误文案表的每一行； - 集成测：fake model 发起 read → edit → 验证文件变更 durable 落盘；未读先改被拒且 Turn 不失败。 3. WI3 grep / glob 两者都基于已被 App Server 发现和管理的 RipgrepExecutable （zeta-rs/shell-command/src/ripgrep.rs），不扩展 file-search crate（它是 TUI 模糊 搜索，语义不同）： - 新增 zeta-rs/app-server/src/localtools/grep.rs：rg -n --no-heading + glob/大小写参 数映射，100 条与 500 字符/行限幅，正则错误透传 + 转义提示文案； - 新增 localtools/glob.rs：rg --files --glob <pattern ，结果按 mtime 降序，100 条 限幅； - 两者 capability 为只读 + workspace 路径域，沙箱兼容（SandboxCompatibility 按现有 shell 只读判定复用）。 验收：单测（限幅、排序、gitignore 默认遵守、非法 pattern 文案）。 4. WI4 本地工具组合改造 zeta-rs/app-server/src/localtools.rs 现状是 LocalShellToolService 单工具（definitions 返回一个、prepare/materialize 按单一 name 校验）。改造： - 新增 localtools/suite.rs：LocalToolSuite 持有 shell / readfile / writefile / edit / grep / glob 六个成员，definitions 聚合（顺序 = tools-spec 章节序，固定—— 这是缓存前缀稳定性的一部分）；prepare / execute 按 call.name 分发； - 每个工具的 ActionReviewRequest 构造：read/grep/glob → 只读 capability + 路径； write/edit → 写 capability + 精确路径（喂给 zeta-action-policy 做审批/沙箱判定，复用现 有 shell 的 provenance/digest 模式，sourceid 按工具名区分）； - LOCALPOLICYREVISION bump（local-shell-v2 → local-tools-v3）； - composelocaltools 返回签名不变，内部换 suite。 验收：现有 localtoolstests.rs / toolcompositiontests.rs 迁移 + 新工具的 prepare→policy→execute 路径各一条；审批弹窗场景（写文件首次要求批准）手动验证。 5. WI5 描述与并行调用 - 工具描述/schema 常量：各工具实现文件内以 const 持有 tools-spec 的正文（描述即提示词， 改动走 tools-spec 附录 B 的修改清单）； - paralleltoolcalls: true 已在 WI1 assembler 改动中带入；调度器保持串行执行（模型可 一次发多个调用，执行顺序 = 调用顺序，现有 nextpendingcall 循环天然支持——确认 多 Tool Call 消息的 durable 记录顺序测试即可）。 6. WI6 模型调用基础弹性 范围是 agent-harness-design.md §7.1 的最小子集（429/5xx/传输错误退避 + 空响应重试；溢出→压缩链路属 M3 不做）： - zeta-rs/zeta-api/src/error.rs：ApiError 增加 RateLimited retryafterms: Option<u64 与 Overloaded；两个请求构造器从 HTTP 429/5xx/529 + Retry-After 头映射（HttpStatus 保底不删）； - zeta-rs/model-provider / zeta-rs/core：错误类别透传（CoreError 增加 ModelTransient String 类别，adapter 层映射）； - zeta-rs/core/src/turn/executor.rs：模型调用点外包重试环——基数 1s、倍率 2、上限 30s、抖动 ±25%、最多 4 次尝试；退避等待以 ≤100ms 步长轮询 cancellation；仅 ModelTransient 触发；空响应（无文本/无 Tool Call/无 Refusal）同请求重试 1 次后按现 有失败路径走； - Refusal 分支：现状空响应与 refusal 都走 responsefailuremessage 失败——改为 ResponseItem::Refusal 单独分支，作为最终 agent message 完成 Turn（harness §7.1）。 验收：fake ModelService 注入 429×2→成功、5xx×4（耗尽失败）、空→成功、Refusal 完成、 退避中 interrupt 立即取消，五条并发/时序测试。 7. 顺序与依赖 `text WI1 提示词（独立，先行——一天级，立刻可感知） │ WI4 组合改造 ──► WI2 文件三工具 ──┐ │ WI3 grep/glob ────┤──► WI5 描述收口 ──► 端到端验收 WI6 弹性（独立，可并行）──────────┘ ` - WI4 先于 WI2/WI3（套件骨架就位，工具逐个挂入）； - WI2 的 ToolService 签名变更尽早合（波及面大、纯机械）； - WI6 与工具线完全无关，可并行。 8. 端到端验收 全部合入后，用真实 provider 手动跑三个场景（同时作为 agent-harness-design.md §14 任务集 T1/T2 的首批夹具）： 1. 修 bug：夹具仓库一个单文件 bug，提示\"tests 里有一个失败，修掉它\"——期望： grep 定位 → read → edit → shell 跑测试 → 报告，全程无人工纠偏； 2. 跨文件小功能：加一个函数并在两处调用——期望 glob/grep 探索 + 多文件 edit； 3. 权限路径：让它写一个新文件——期望首次写触发审批，批准后完成，拒绝后 agent 报告 而非重试。 回归门（每个 WI 合入都跑）： `bash cargo fmt --manifest-path zeta-rs/Cargo.toml --all -- --check cargo clippy --manifest-path zeta-rs/Cargo.toml --workspace --all-targets -- -D warnings cargo test --manifest-path zeta-rs/Cargo.toml --workspace ` （本计划无 protocol schema 变更——turn/steer、checkpoint 等都在范围外，因此不涉及 pnpm verify:protocol 之外的客户端同步。） 9. 风险与开放点 风险 处理 --- --- ToolService 签名变更波及 MCP 适配器与全部测试 fake 纯机械；单独一个提交先行合入 行式 read 与现有 64 KiB 字节上限的关系 新 read.rs 独立实现行式；旧 operation 工具不动 git 环境采集在超大仓库慢（status 全量） --porcelain` 截断 40 行 + 3s 超时，超时省略该字段 OpenAI 系模型在 edit 工具上的正确率 已知取舍（v0 单 profile）；接 OpenAI 主力模型前补 applypatch + profile Refusal 语义改变现有失败测试 executor 测试同步改（原空响应失败测试拆成 空响应/Refusal 两条） "
  },
  {
    "slug": "core-context",
    "sourcePath": "docs/core-context.md",
    "title": "上下文系统",
    "description": "上下文系统决定某一次模型调用能看到哪些信息；它从可持久化历史生成模型输入，但不能成为第二份 对话历史。",
    "group": "Agent 与运行时",
    "markdown": "> 状态：核心纵向切片已实现。`ContextInput`、纯规划器、`ContextPlan`、每个已加载 Thread 的\n> `ContextManager`、持久化 checkpoint、模型压缩编排和 `ContextAssembler` 已接入\n> `TurnExecutor`。Skill 正文通过通用 `TurnInputContributor` 在 invocation safe point 注入；\n> 通用预算、精准/估算计量结果和边界判定已拆入 `zeta-context-engine`；OpenAI exact，以及\n> Anthropic、Google、Kimi、Z.AI estimated remote preflight 已接入，DeepSeek/Hugging Face local\n> tokenizer adapter 已接入；Hugging Face 公共模型支持按需发现、下载和缓存，其他 provider 的固定\n> 资产目录、prompt cache/reference baseline、跨 Thread seed 与自动 Skill 选择仍是扩展点。\n>\n> Core 总体边界：[`core.md`](core.md)\n> Canonical Thread/Turn/Item contract：[`protocol.md`](protocol.md)\n> 通用 extension contract：[`zeta-rs/ext/extension-api/README.md`](../zeta-rs/ext/extension-api/README.md)\n> 多 Agent context inheritance：[`core-multi-agent.md`](core-multi-agent.md)\n> 内置模型提示词资产：[`zeta-prompts`](../zeta-rs/prompts/README.md)\n> 预算与 token 计量实现：[`zeta-context-engine`](../zeta-rs/context-engine/README.md)\n\n## 快速理解\n\n上下文系统决定某一次模型调用能看到哪些信息；它从可持久化历史生成模型输入，但不能成为第二份\n对话历史。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| Thread 历史就是模型上下文吗？ | 不是；历史是权威事实，上下文是按预算选择出的派生窗口 | [权威与派生关系](#3-权威与派生关系) |\n| 多个 Thread 会共享上下文吗？ | 不会；每个 Thread 有独立的 `ContextManager`、窗口和压缩检查点 | [为什么不放进 Session](#2-为什么不放进-session) |\n| 一次模型调用使用什么？ | 使用不可变的 `ContextPlan`，再由 `ContextAssembler` 组装请求 | [数据模型](#4-数据模型) |\n| 超出模型预算怎么办？ | 先按规则选择和裁剪；需要压缩时产生可恢复检查点 | [上下文预算](#7-上下文预算)、[压缩](#8-压缩) |\n| 模型或配置变化会污染旧窗口吗？ | 不会静默复用；相关 revision 变化会使派生状态失效并重建 | [供应商变更](#9-上下文窗口与供应商变更) |\n\n## 1. 结论\n\nZeta 长期必须区分四个概念：\n\n```text\nThread history\n  durable authority\n\nContextManager\n  per-loaded-Thread 的可重建上下文协调状态\n\nContextPlan\n  一次 model invocation 的不可变选择结果\n\nContextAssembler\n  ContextPlan → provider-neutral ModelRequest 的纯组装\n```\n\n上下文不是 Session 级对象。每个 Thread 有独立 history、sequence、context window、compaction\ncheckpoint 和 ContextManager。父子 Agent、fork Thread 或 side conversation 都不能共享可变\nContextManager。\n\nContextManager 也不能成为第二份 canonical history。它允许保存 cache、baseline、token estimate\n和当前 window revision，但所有影响未来 prompt 的事实必须能够从 durable Thread history、\ncheckpoint、context seed 与 policy snapshot 重建。\n\n## 2. 为什么不放进 Session\n\nZeta 的 `Session` 是多个 Thread 的产品容器，只拥有 membership、lineage、shared defaults 和\nlifecycle。若 Session 持有 context，会产生无法接受的问题：\n\n- 多个 Thread 竞争同一 history/window；\n- child Agent 看到 parent 或 sibling 的未授权内容；\n- 一个 Thread 的 compaction 改变另一个 Thread；\n- provider/model 切换无法按 Thread safe point 生效；\n- Session lock 被 model/context I/O 放大；\n- Thread 无法独立恢复、并行和回收。\n\n其他代码库中名为 `Session` 的对象可能实际代表一条 conversation/thread 的加载实例。Zeta 必须\n按领域语义映射，不能按目录或类型名照搬。\n\n## 3. 权威与派生关系\n\n```text\nSession event stream\n  └─ membership / lineage / defaults\n\nThread event stream\n  ├─ Turns / Items / Tool Calls / Tool Results\n  ├─ interactions / Agent deliveries\n  ├─ compaction checkpoints\n  └─ context seed references\n          │\n          ▼ reduce\n    durable ThreadSnapshot\n          │\n          ▼ derive\n      ContextManager\n          │\n          ▼ prepare\n       ContextPlan\n          │\n          ▼ assemble\n       ModelRequest\n```\n\n规则：\n\n- event stream 是 authority；\n- `ThreadSnapshot` 是可重建 projection；\n- `ContextManager` 是进程内可重建派生状态；\n- `ContextPlan` 只属于一次 invocation；\n- provider cache 是可丢弃优化；\n- compaction summary 是带 provenance 的 durable 派生 artifact，不替代原始事件。\n\n## 4. 数据模型\n\n以下类型表达当前规划语义；`ContextBudget` 由通用 `zeta-context-engine` 提供，其余选择模型仍由\nCore 内部拥有。live Config、provider client 和 mutable manager 都不会进入不可变规划输入。\n\n### 4.1 ContextInput\n\n```rust\nstruct ContextInput {\n    source_thread_sequence: u64,\n    current_turn_id: TurnId,\n    instructions: Vec<InstructionFragment>,\n    items: Vec<ThreadItem>,\n    checkpoints: Vec<ContextCheckpoint>,\n    item_sequences: BTreeMap<ItemId, u64>,\n    tools: Vec<ToolDefinition>,\n    budget: ContextBudget,\n}\n```\n\n`ContextInput` 是一次 prepare 操作看到的完整不可变输入。它不得包含 live config manager、\ncredential store、provider client 或可变 Session history。\n\n### 4.2 ContextPlan\n\n```rust\nstruct ContextPlan {\n    source_thread_sequence: u64,\n    instructions: Vec<InstructionFragment>,\n    omitted_instructions: Vec<OmittedInstruction>,\n    checkpoint: Option<ContextCheckpoint>,\n    selected_items: Vec<ThreadItem>,\n    tools: Vec<ToolDefinition>,\n    budget: ContextBudgetReport,\n}\n```\n\nContextPlan 必须可诊断：\n\n- 从哪个 Thread sequence 派生；\n- 选中了什么；\n- 省略了什么以及原因；\n- token 预算如何分配；\n- 使用了哪个 checkpoint。\n\n模型选择冻结在同一次 `ModelInvocationSnapshot`；策略版本和 Skill 激活来源冻结在 durable Turn，\n不复制进 `ContextPlan`。\n\nSkill 的发现、启用、模型选择和入口解析不属于上下文系统。外部 runtime 根据 durable activation\n解析出 `PromptFragment { source, layer, retention, body }`，也可以贡献 metadata-only catalog\nfragment；Core 只校验 fragment provenance，按 Skill layer、`Required / BestEffort` 预算语义\n注入。模型随后通过普通 Tool Call/Result 按需读取 Skill 正文。catalog generation、斜杠入口和\nactivation reason 不进入模型可见 Skill 正文。\n\n它不要求成为公共 wire model。测试和诊断可以使用 Core-private readable view。\n\n### 4.3 ContextManager 状态\n\n```rust\nstruct ContextManager {\n    observed_thread_sequence: u64,\n}\n```\n\n首版 manager 只拒绝倒退的 Thread sequence，然后调用纯规划器；它没有 cache、reference baseline\n或 token estimate。未来增加的派生字段仍必须可丢失。若某个字段丢失会改变恢复后的语义，它就\n不应只存在 ContextManager 中，而应先成为 durable fact。\n\n## 5. 组件职责\n\n### 5.1 ContextManager\n\n负责：\n\n- 检查 Thread/policy/model revision；\n- 选择 context window 与有效 checkpoint；\n- 组织 instruction、seed、history、Tool Result 和 Agent result；\n- 调用纯 budget/selection planner；\n- 决定是否需要 compaction；\n- 管理 reference baseline 与 cache invalidation；\n- 生成 `ContextPlan` 或明确的 `ContextPreparation` outcome。\n\n不负责：\n\n- 写 Thread store；\n- 直接修改 Thread projection；\n- 持有 provider client；\n- 读取 live Config/MCP/Skill manager；\n- 将未经验证的 memory 自动注入；\n- 直接执行 summary model；\n- 跨 Thread 读取另一个 live ContextManager。\n\n### 5.2 ContextPlanner\n\n选择和预算算法应保持纯函数：\n\n```text\nContextInput + ContextPolicy\n  → Ready(ContextPlan)\n  | NeedsCompaction(CompactionPlan)\n  | ContextError\n```\n\n把 planner 保持为纯函数可以独立验证 precedence、budget、Tool pairing、checkpoint selection 和\ndeterminism。`ContextManager` 只协调生命周期与缓存。\n\n### 5.3 ContextAssembler\n\n只负责：\n\n- 将 resolved instructions 写入 canonical request；\n- 将 selected Item 转成 provider-neutral `InputItem`；\n- 保持 Tool Call/Tool Result 配对与顺序；\n- 应用已经确定的 tools、tool choice 和 output limit；\n- 拒绝损坏 JSON、悬空引用和不支持的 canonical shape。\n\n它不重新选择历史、不决定 compaction，也不读取 ThreadController。\n\n当前 `ContextAssembler` 只接受 `ContextPlan`；Thread snapshot 的读取和选择由\nContextManager/Planner 完成。\n\n### 5.4 通用预算引擎\n\n[`zeta-context-engine`](../zeta-rs/context-engine/README.md) 只拥有模型无关的预算数学和 token 计量\n结果：它区分普通请求的压缩压力线与模型硬窗口，接受精准 preflight/本地 tokenizer 结果或带保守\n记账余量的估算，并返回 `Fits`、`NeedsCompaction` 或 `ExceedsContextWindow`。\n\n它不读取 Thread、不选择历史、不执行 provider 请求，也不消费响应 usage。Core planner 使用它解析\n预算边界，仍拥有 instruction precedence、完整语义单元选择和 compaction outcome。provider adapter\n只负责产生与所选模型匹配的计量结果，不能拥有另一套预算公式。\n\n### 5.5 内置提示词资产\n\n[`zeta-prompts`](../zeta-rs/prompts/README.md) 只拥有四类 Zeta 内置、模型可见的提示词资产：system、\ncompaction、goals 和通用 review。它提供稳定的 asset ID、revision 与 compile-time body，但不决定\n何时注入，也不读取 Thread、Config、Skill、MCP 或 provider runtime。\n\n需要某类提示词的功能模块负责触发条件和生命周期；当资产进入 Agent 的 canonical context 后，仍由\nCore context pipeline 负责 instruction layer、precedence、budget、provenance 和最终 request 组装。\n`zeta-auto-review` 的 prompt/schema/revision 专用契约继续由该 crate 自己拥有。\n\n当前 assembler 会把同一 Turn 中相邻的 `UserMessage` / `UserImage` 按 durable 顺序合并成一个\nprovider-neutral user `Message`，分别映射为 `ContentPart::Text` 与\n`ContentPart::ImageUrl`。图片内容在 Turn 接受阶段已经由 Core 校验；provider adapter 只负责\n转换为各自的 URL/base64 image block。\n\n## 6. 上下文组织\n\n### 6.1 逻辑层次\n\n当前 instruction fragment 使用以下层级：\n\n```text\n1. System\n2. Product\n3. Workspace\n4. Skill\n```\n\n验证过的 checkpoint 随后替代其覆盖的历史前缀，未覆盖 tail、Tool Call/Result 和当前 Turn 输入\n保持 durable 顺序。Session defaults、Agent role/seed 和 reference resources 尚未接入；接入时必须\n增加明确 layer/retention/provenance，而不能依赖字符串拼接顺序。Provider adapter 只能做 wire\n映射，不能改变 Core 已解析的 precedence。\n\n### 6.2 指令优先级\n\n每段 instruction 必须带：\n\n- source kind；\n- source revision；\n- scope；\n- precedence；\n- provenance；\n- sensitivity/visibility metadata。\n\n低优先级内容不能覆盖高优先级约束。Agent delegation 可以收紧 policy 或增加任务说明，但不能\n放宽 system/session policy ceiling。\n\n### 6.3 结构不变量\n\n任何 ContextPlan 必须满足：\n\n- 当前用户输入不可被预算算法删除；\n- Tool Result 必须引用可见 Tool Call；\n- 需要继续执行的 Tool Call/Result group 原子保留；\n- Agent result 必须引用可见 delegation/message identity；\n- checkpoint 与 tail 不重复覆盖同一 source range；\n- omission 不破坏角色和调用边界；\n- provider 不支持的内容只能按显式降级策略转换或报错；\n- reasoning/plan 是否回灌必须由 canonical contract 明确允许。\n\n## 7. 上下文预算\n\n### 7.1 预算输入\n\n预算至少包含：\n\n- model context window；\n- reserved output tokens；\n- system/developer instruction cost；\n- Tool definitions cost；\n- current Turn minimum；\n- Tool/Agent continuation minimum；\n- safety margin；\n- compaction threshold；\n- tokenizer/estimate revision。\n\n预算计量与响应 usage 是两类事实：前者在调用前判断候选请求能否发送，后者在调用完成后记录实际\n消耗。精准计量可来自匹配模型的本地 tokenizer 或 provider preflight；provider preflight 本身不\n等于精准，准确度必须由 provider 契约独立声明。估算的 measured value 用于诊断，预算边界使用带\n策略余量的 accounted value；该余量不冒充 provider 承诺的数学硬上界。\n\n不得使用一个含义不明的 `max_tokens: Option<u32>` 同时表达 context window、output limit 和缺省。\n这些值使用独立 newtype/enum。\n\n### 7.2 预算顺序\n\n推荐顺序：\n\n```text\nmodel context window\n− reserved output budget\n− mandatory instructions/tools/current input\n− safety margin\n= history budget\n```\n\n历史选择按完整语义单元处理，而不是按字符串尾部截断：\n\n- Turn group；\n- Tool Call/Result group；\n- Agent delegation/result group；\n- checkpoint + uncovered tail；\n- attachment/artifact reference group。\n\n自动压缩阈值只决定何时产生压力，不是模型硬窗口。普通规划使用 pressure limit；独立压缩请求\n可以使用完整 context window，但仍扣除 reserved output 与 safety margin，并且它自身必须通过\n预算估算。这样低阈值会更早压缩，却不会错误拒绝仍可装入模型硬窗口的 source prefix。\n\nCore-managed plan 会把同一份 `reserved_output` 写入该次不可变 `ModelRequest`。Provider 配置只在\n请求没有显式 limit 时提供缺省，因此 budget 与实际输出上限不会在 invoke 前因配置刷新而漂移。\n\n### 7.3 Overflow 结果\n\n预算不足必须产生明确 outcome：\n\n- `NeedsCompaction`；\n- `CurrentInputTooLarge`；\n- `MandatoryInstructionsTooLarge`；\n- `ToolDefinitionsTooLarge`；\n- `CheckpointCapacityTooSmall`；\n- `CompactionSourceTooLarge`；\n- `UnsupportedContextShape`。\n\n不能静默删除当前输入、权限约束或未完成 Tool/Agent continuation。\n\n已知窗口可以来自内置 `ModelInfo`，也可以由 `ModelProviderConfig.model_context` 按模型 ID 配置；\n窗口未知时使用 `ContextBudget::ProviderManaged`，Core 不假装拥有可靠上限。预算解析、精准/估算\n计量结果和统一边界判定已由 `zeta-context-engine` 提供；生产 planner 首轮仍使用带 revision 的\n确定性 byte estimate。最终 canonical request 会按“官方 preflight → 匹配模型的本地整请求计数 →\nCore 保守估算”降级：OpenAI Responses 为 exact；Anthropic、Google native `countTokens`、Kimi\nestimate 与 Z.AI tokenizer 为 estimated remote；本地 `hf-chat-template + tokenizers` 结果为\nestimated。只有 provider definition 明确声明且 model policy 允许的 binding 才会触发远程计量。\n\n## 8. 压缩\n\n### 8.1 持久化检查点\n\nCheckpoint 至少记录：\n\n- stable checkpoint ID；\n- source Thread；\n- covered sequence range；\n- referenced Item/Event IDs；\n- source digest；\n- summary 内容或 artifact reference；\n- schema、prompt 与 context policy revision；\n- generator/model metadata；\n- creation time；\n- verification status。\n\n原始 event log 永不因 compaction 删除。\n\n### 8.2 压缩流程\n\n```text\nContextManager detects pressure\n→ returns NeedsCompaction(CompactionPlan)\n→ Turn/Core orchestration invokes CompactionService\n→ validate summary + provenance + digest\n→ ThreadController durable commits checkpoint\n→ install new ThreadSnapshot\n→ ContextManager invalidates old window/cache\n→ prepare again from committed snapshot\n```\n\nSummary model I/O 不持有 Thread writer。只有 checkpoint durable commit 后，后续 invocation 才能\n依赖它。\n\n### 8.3 恢复与失效\n\n以下情况会使 checkpoint commit 或 event replay 失败即关闭，损坏的摘要不会进入 projection：\n\n- source range 不存在或重叠错误；\n- digest 不匹配；\n- schema/policy 不兼容；\n- referenced Item 缺失；\n- checkpoint 来自另一个 Thread；\n- schema/policy revision 缺失；\n- checkpoint range 倒退或不是 Thread 前缀。\n\n当前不会在已损坏 event stream 上静默跳过 checkpoint 后继续恢复；原始事件也从不因压缩删除，\n因此后续可以增加显式修复/隔离工具，而不用依赖摘要作为唯一副本。\n\nProvider/model 切换不自动使 checkpoint 失效，但必须重新按新 model limits 和 capabilities 规划。\n\n## 9. 上下文窗口与供应商变更\n\nContext window 是模型可见历史的派生窗口，不是新的 history aggregate。\n\n每次 model/provider/context policy 变化：\n\n```text\nwait for safe point\n→ freeze new revisions\n→ invalidate incompatible cached plan/baseline\n→ rebuild from durable ThreadSnapshot\n→ create new ModelInvocationSnapshot\n```\n\nProvider response ID、prompt cache key 和 connection state 可以作为 adapter 优化，但不能成为\n恢复正确性的前提。\n\n## 10. 多 Agent 规则\n\n每个 child Agent 的 ContextManager 只读取：\n\n- child Thread 的 durable history；\n- spawn 时固定的 `AgentContextSeed`；\n- durable delivered parent/peer messages；\n- child 自己的 policy/environment snapshot。\n\n它不能读取 parent/sibling 的 live history 或 ContextManager。\n\nSpawn context mode 必须显式：\n\n```text\nFresh\nSelected(item/artifact references)\nForkedPrefix(full | last turns | checkpoint plus tail)\n```\n\n所有模式都固定 parent Thread sequence 和 provenance。Spawn 后 parent compaction 或继续执行不改变\nchild 的 seed。Child result 回到 parent 后只是一个有界、可追踪的 durable result Item，由 parent\nContextManager 决定是否选入下一次 ContextPlan。\n\n详细协议见 [`core-multi-agent.md`](core-multi-agent.md)。\n\n## 11. 目录与可见性\n\n```text\nzeta-rs/\n├─ context-engine/\n│  └─ src/\n│     ├─ budget.rs\n│     ├─ measurement.rs\n│     ├─ planner.rs\n│     └─ *_tests.rs\n└─ core/src/\n   ├─ context.rs\n   ├─ context/\n   │  ├─ model.rs\n   │  ├─ plan.rs\n   │  ├─ assembler.rs\n   │  ├─ compaction.rs\n   │  ├─ instructions.rs\n   │  ├─ invocation.rs\n   │  ├─ planner.rs\n   │  ├─ skills.rs\n   │  └─ *_tests.rs\n   ├─ context_manager.rs\n   └─ context_manager_tests.rs\n```\n\n`context-engine` 保存可跨产品和 provider 复用的预算 value 与纯判定；Core `context` 保存 Thread 内容\n选择和组装算法；`context_manager` 保存 per-loaded-Thread 协调逻辑。Core 两个模块默认 private。\n`ContextSource` 是 Core 的通用可选 evidence port：host 可在 Turn 第一次 model invocation 前返回带\nprovenance 的 bounded evidence。Core 对已知窗口最多分配 input budget 的 1/8，并把内容作为 user-level\n`trust=\"untrusted-data\"` 数据插在当前用户输入之前，不能提升为 system/workspace instructions。普通来源\n失败降级为空；Turn cancellation 继续传播。CodeIndex 自动召回默认关闭，由产品设置显式开启。\n只有确有外部消费者时才从 `lib.rs` 导出窄 value/port，不能公开 cache、baseline、window mutable\nstate 或 ContextManager 自身。\n\n长期 ownership：\n\n```text\nThreadController\n└─ LoadedThreadState\n   └─ ContextManager\n\nTurnExecutor\n└─ asks ContextManager to prepare\n   └─ receives immutable ContextPlan\n```\n\n## 12. 当前实现与扩展点\n\n| 能力 | 状态 | 边界 |\n| --- | --- | --- |\n| 不可变输入、纯规划器和调用快照 | ✅ 已实现 | 同一输入产生确定性选择；倒退 sequence 被拒绝 |\n| 通用 Skill 指令端口与预算行为 | ✅ 已实现 | 外部返回 `Required / BestEffort`；Core 不拥有 Skill 发现或选择 |\n| durable checkpoint、摘要生成、commit 后重规划 | ✅ 已实现 | 原始 event log 永不删除；压缩请求本身也受预算限制 |\n| 已知模型窗口的生产启用 | ✅ 已实现 | 可通过 `model_context` 配置；未知模型退回 provider-managed |\n| 通用预算与精准/估算计量契约 | ✅ 已实现 | `zeta-context-engine` 统一压力线、硬窗口和保守记账判定 |\n| provider input-token preflight | 部分具备 | OpenAI exact；Anthropic、Google、Kimi、Z.AI estimated；官方接口失败时降级到本地或 Core 估算 |\n| 请求级本地 token 计数 | 部分具备 | HF 公共模型按需发现/下载/缓存；其他 provider 需固定资产清单；多模态 processor 尚未接入 |\n| provider usage 校准 | 尚未完成 | usage 与调用前预算保持独立，尚未建立按模型隔离的校准数据 |\n| cache/reference baseline | 推迟 | 只有性能证据证明重复组装是瓶颈后才增加 |\n| Agent seed、跨 Thread 选择与 reference resources | 尚未完成 | 不能读取其他 live `ContextManager` |\n\n后续扩展不得先复制 mutable history。cache、baseline 或 usage estimate 必须是可丢弃派生状态；会\n改变恢复语义的内容必须先进入 durable Thread fact。\n\n## 13. 验证\n\n必须覆盖：\n\n- 相同 ContextInput 产生字节级等价的 ContextPlan；\n- instruction precedence；\n- budget boundary 与 mandatory overflow；\n- Tool Call/Result 原子保留；\n- Agent delegation/result 原子保留（阶段 D protocol 类型落地后启用）；\n- checkpoint + tail 无遗漏、无重复；\n- corrupt checkpoint replay fail closed，且原始 event log 保持完整；\n- provider/model/policy revision invalidation；\n- ContextManager 丢失后从 durable facts 重建等价结果；\n- parent/child/sibling context 隔离（多 Agent 运行时落地后启用）；\n- compaction crash before/after durable commit；\n- current input 永不被静默删除。\n\n## 14. 固定决策\n\n- Context 属于一次 model invocation；\n- ContextManager 属于一个 loaded Thread；\n- Session 和 Agent tree 不拥有共享 ContextManager；\n- Thread event stream 是唯一 history authority；\n- ContextManager 可丢失、可重建；\n- ContextAssembler 保持纯组装；\n- 模型无关预算公式和 token 计量结果属于 `zeta-context-engine`；\n- 选择、预算和 compaction 决策不进入 provider adapter；\n- checkpoint durable，但不删除原始 history；\n- 多 Agent 只通过 immutable seed 和 durable message/result 传递 context；\n- 跨 Thread memory 在单独 RFC 完成前不进入 ContextManager。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-为什么不放进-session",
        "title": "2. 为什么不放进 Session"
      },
      {
        "depth": 2,
        "id": "3-权威与派生关系",
        "title": "3. 权威与派生关系"
      },
      {
        "depth": 2,
        "id": "4-数据模型",
        "title": "4. 数据模型"
      },
      {
        "depth": 3,
        "id": "41-contextinput",
        "title": "4.1 ContextInput"
      },
      {
        "depth": 3,
        "id": "42-contextplan",
        "title": "4.2 ContextPlan"
      },
      {
        "depth": 3,
        "id": "43-contextmanager-状态",
        "title": "4.3 ContextManager 状态"
      },
      {
        "depth": 2,
        "id": "5-组件职责",
        "title": "5. 组件职责"
      },
      {
        "depth": 3,
        "id": "51-contextmanager",
        "title": "5.1 ContextManager"
      },
      {
        "depth": 3,
        "id": "52-contextplanner",
        "title": "5.2 ContextPlanner"
      },
      {
        "depth": 3,
        "id": "53-contextassembler",
        "title": "5.3 ContextAssembler"
      },
      {
        "depth": 3,
        "id": "54-通用预算引擎",
        "title": "5.4 通用预算引擎"
      },
      {
        "depth": 3,
        "id": "55-内置提示词资产",
        "title": "5.5 内置提示词资产"
      },
      {
        "depth": 2,
        "id": "6-上下文组织",
        "title": "6. 上下文组织"
      },
      {
        "depth": 3,
        "id": "61-逻辑层次",
        "title": "6.1 逻辑层次"
      },
      {
        "depth": 3,
        "id": "62-指令优先级",
        "title": "6.2 指令优先级"
      },
      {
        "depth": 3,
        "id": "63-结构不变量",
        "title": "6.3 结构不变量"
      },
      {
        "depth": 2,
        "id": "7-上下文预算",
        "title": "7. 上下文预算"
      },
      {
        "depth": 3,
        "id": "71-预算输入",
        "title": "7.1 预算输入"
      },
      {
        "depth": 3,
        "id": "72-预算顺序",
        "title": "7.2 预算顺序"
      },
      {
        "depth": 3,
        "id": "73-overflow-结果",
        "title": "7.3 Overflow 结果"
      },
      {
        "depth": 2,
        "id": "8-压缩",
        "title": "8. 压缩"
      },
      {
        "depth": 3,
        "id": "81-持久化检查点",
        "title": "8.1 持久化检查点"
      },
      {
        "depth": 3,
        "id": "82-压缩流程",
        "title": "8.2 压缩流程"
      },
      {
        "depth": 3,
        "id": "83-恢复与失效",
        "title": "8.3 恢复与失效"
      },
      {
        "depth": 2,
        "id": "9-上下文窗口与供应商变更",
        "title": "9. 上下文窗口与供应商变更"
      },
      {
        "depth": 2,
        "id": "10-多-agent-规则",
        "title": "10. 多 Agent 规则"
      },
      {
        "depth": 2,
        "id": "11-目录与可见性",
        "title": "11. 目录与可见性"
      },
      {
        "depth": 2,
        "id": "12-当前实现与扩展点",
        "title": "12. 当前实现与扩展点"
      },
      {
        "depth": 2,
        "id": "13-验证",
        "title": "13. 验证"
      },
      {
        "depth": 2,
        "id": "14-固定决策",
        "title": "14. 固定决策"
      }
    ],
    "searchText": " 状态：核心纵向切片已实现。ContextInput、纯规划器、ContextPlan、每个已加载 Thread 的 ContextManager、持久化 checkpoint、模型压缩编排和 ContextAssembler 已接入 TurnExecutor。Skill 正文通过通用 TurnInputContributor 在 invocation safe point 注入； 通用预算、精准/估算计量结果和边界判定已拆入 zeta-context-engine；OpenAI exact，以及 Anthropic、Google、Kimi、Z.AI estimated remote preflight 已接入，DeepSeek/Hugging Face local tokenizer adapter 已接入；Hugging Face 公共模型支持按需发现、下载和缓存，其他 provider 的固定 资产目录、prompt cache/reference baseline、跨 Thread seed 与自动 Skill 选择仍是扩展点。 Core 总体边界：core.md Canonical Thread/Turn/Item contract：protocol.md 通用 extension contract：zeta-rs/ext/extension-api/README.md 多 Agent context inheritance：core-multi-agent.md 内置模型提示词资产：zeta-prompts 预算与 token 计量实现：zeta-context-engine 快速理解 上下文系统决定某一次模型调用能看到哪些信息；它从可持久化历史生成模型输入，但不能成为第二份 对话历史。 读者首先会问 直接答案 深入阅读 --- --- --- Thread 历史就是模型上下文吗？ 不是；历史是权威事实，上下文是按预算选择出的派生窗口 权威与派生关系 多个 Thread 会共享上下文吗？ 不会；每个 Thread 有独立的 ContextManager、窗口和压缩检查点 为什么不放进 Session 一次模型调用使用什么？ 使用不可变的 ContextPlan，再由 ContextAssembler 组装请求 数据模型 超出模型预算怎么办？ 先按规则选择和裁剪；需要压缩时产生可恢复检查点 上下文预算、压缩 模型或配置变化会污染旧窗口吗？ 不会静默复用；相关 revision 变化会使派生状态失效并重建 供应商变更 1. 结论 Zeta 长期必须区分四个概念： ``text Thread history durable authority ContextManager per-loaded-Thread 的可重建上下文协调状态 ContextPlan 一次 model invocation 的不可变选择结果 ContextAssembler ContextPlan → provider-neutral ModelRequest 的纯组装 ` 上下文不是 Session 级对象。每个 Thread 有独立 history、sequence、context window、compaction checkpoint 和 ContextManager。父子 Agent、fork Thread 或 side conversation 都不能共享可变 ContextManager。 ContextManager 也不能成为第二份 canonical history。它允许保存 cache、baseline、token estimate 和当前 window revision，但所有影响未来 prompt 的事实必须能够从 durable Thread history、 checkpoint、context seed 与 policy snapshot 重建。 2. 为什么不放进 Session Zeta 的 Session 是多个 Thread 的产品容器，只拥有 membership、lineage、shared defaults 和 lifecycle。若 Session 持有 context，会产生无法接受的问题： - 多个 Thread 竞争同一 history/window； - child Agent 看到 parent 或 sibling 的未授权内容； - 一个 Thread 的 compaction 改变另一个 Thread； - provider/model 切换无法按 Thread safe point 生效； - Session lock 被 model/context I/O 放大； - Thread 无法独立恢复、并行和回收。 其他代码库中名为 Session 的对象可能实际代表一条 conversation/thread 的加载实例。Zeta 必须 按领域语义映射，不能按目录或类型名照搬。 3. 权威与派生关系 `text Session event stream └─ membership / lineage / defaults Thread event stream ├─ Turns / Items / Tool Calls / Tool Results ├─ interactions / Agent deliveries ├─ compaction checkpoints └─ context seed references │ ▼ reduce durable ThreadSnapshot │ ▼ derive ContextManager │ ▼ prepare ContextPlan │ ▼ assemble ModelRequest ` 规则： - event stream 是 authority； - ThreadSnapshot 是可重建 projection； - ContextManager 是进程内可重建派生状态； - ContextPlan 只属于一次 invocation； - provider cache 是可丢弃优化； - compaction summary 是带 provenance 的 durable 派生 artifact，不替代原始事件。 4. 数据模型 以下类型表达当前规划语义；ContextBudget 由通用 zeta-context-engine 提供，其余选择模型仍由 Core 内部拥有。live Config、provider client 和 mutable manager 都不会进入不可变规划输入。 4.1 ContextInput `rust struct ContextInput sourcethreadsequence: u64, currentturnid: TurnId, instructions: Vec<InstructionFragment , items: Vec<ThreadItem , checkpoints: Vec<ContextCheckpoint , itemsequences: BTreeMap<ItemId, u64 , tools: Vec<ToolDefinition , budget: ContextBudget, ` ContextInput 是一次 prepare 操作看到的完整不可变输入。它不得包含 live config manager、 credential store、provider client 或可变 Session history。 4.2 ContextPlan `rust struct ContextPlan sourcethreadsequence: u64, instructions: Vec<InstructionFragment , omittedinstructions: Vec<OmittedInstruction , checkpoint: Option<ContextCheckpoint , selecteditems: Vec<ThreadItem , tools: Vec<ToolDefinition , budget: ContextBudgetReport, ` ContextPlan 必须可诊断： - 从哪个 Thread sequence 派生； - 选中了什么； - 省略了什么以及原因； - token 预算如何分配； - 使用了哪个 checkpoint。 模型选择冻结在同一次 ModelInvocationSnapshot；策略版本和 Skill 激活来源冻结在 durable Turn， 不复制进 ContextPlan。 Skill 的发现、启用、模型选择和入口解析不属于上下文系统。外部 runtime 根据 durable activation 解析出 PromptFragment source, layer, retention, body ，也可以贡献 metadata-only catalog fragment；Core 只校验 fragment provenance，按 Skill layer、Required / BestEffort 预算语义 注入。模型随后通过普通 Tool Call/Result 按需读取 Skill 正文。catalog generation、斜杠入口和 activation reason 不进入模型可见 Skill 正文。 它不要求成为公共 wire model。测试和诊断可以使用 Core-private readable view。 4.3 ContextManager 状态 `rust struct ContextManager observedthreadsequence: u64, ` 首版 manager 只拒绝倒退的 Thread sequence，然后调用纯规划器；它没有 cache、reference baseline 或 token estimate。未来增加的派生字段仍必须可丢失。若某个字段丢失会改变恢复后的语义，它就 不应只存在 ContextManager 中，而应先成为 durable fact。 5. 组件职责 5.1 ContextManager 负责： - 检查 Thread/policy/model revision； - 选择 context window 与有效 checkpoint； - 组织 instruction、seed、history、Tool Result 和 Agent result； - 调用纯 budget/selection planner； - 决定是否需要 compaction； - 管理 reference baseline 与 cache invalidation； - 生成 ContextPlan 或明确的 ContextPreparation outcome。 不负责： - 写 Thread store； - 直接修改 Thread projection； - 持有 provider client； - 读取 live Config/MCP/Skill manager； - 将未经验证的 memory 自动注入； - 直接执行 summary model； - 跨 Thread 读取另一个 live ContextManager。 5.2 ContextPlanner 选择和预算算法应保持纯函数： `text ContextInput + ContextPolicy → Ready ContextPlan NeedsCompaction CompactionPlan ContextError ` 把 planner 保持为纯函数可以独立验证 precedence、budget、Tool pairing、checkpoint selection 和 determinism。ContextManager 只协调生命周期与缓存。 5.3 ContextAssembler 只负责： - 将 resolved instructions 写入 canonical request； - 将 selected Item 转成 provider-neutral InputItem； - 保持 Tool Call/Tool Result 配对与顺序； - 应用已经确定的 tools、tool choice 和 output limit； - 拒绝损坏 JSON、悬空引用和不支持的 canonical shape。 它不重新选择历史、不决定 compaction，也不读取 ThreadController。 当前 ContextAssembler 只接受 ContextPlan；Thread snapshot 的读取和选择由 ContextManager/Planner 完成。 5.4 通用预算引擎 zeta-context-engine 只拥有模型无关的预算数学和 token 计量 结果：它区分普通请求的压缩压力线与模型硬窗口，接受精准 preflight/本地 tokenizer 结果或带保守 记账余量的估算，并返回 Fits、NeedsCompaction 或 ExceedsContextWindow。 它不读取 Thread、不选择历史、不执行 provider 请求，也不消费响应 usage。Core planner 使用它解析 预算边界，仍拥有 instruction precedence、完整语义单元选择和 compaction outcome。provider adapter 只负责产生与所选模型匹配的计量结果，不能拥有另一套预算公式。 5.5 内置提示词资产 zeta-prompts 只拥有四类 Zeta 内置、模型可见的提示词资产：system、 compaction、goals 和通用 review。它提供稳定的 asset ID、revision 与 compile-time body，但不决定 何时注入，也不读取 Thread、Config、Skill、MCP 或 provider runtime。 需要某类提示词的功能模块负责触发条件和生命周期；当资产进入 Agent 的 canonical context 后，仍由 Core context pipeline 负责 instruction layer、precedence、budget、provenance 和最终 request 组装。 zeta-auto-review 的 prompt/schema/revision 专用契约继续由该 crate 自己拥有。 当前 assembler 会把同一 Turn 中相邻的 UserMessage / UserImage 按 durable 顺序合并成一个 provider-neutral user Message，分别映射为 ContentPart::Text 与 ContentPart::ImageUrl。图片内容在 Turn 接受阶段已经由 Core 校验；provider adapter 只负责 转换为各自的 URL/base64 image block。 6. 上下文组织 6.1 逻辑层次 当前 instruction fragment 使用以下层级： `text 1. System 2. Product 3. Workspace 4. Skill ` 验证过的 checkpoint 随后替代其覆盖的历史前缀，未覆盖 tail、Tool Call/Result 和当前 Turn 输入 保持 durable 顺序。Session defaults、Agent role/seed 和 reference resources 尚未接入；接入时必须 增加明确 layer/retention/provenance，而不能依赖字符串拼接顺序。Provider adapter 只能做 wire 映射，不能改变 Core 已解析的 precedence。 6.2 指令优先级 每段 instruction 必须带： - source kind； - source revision； - scope； - precedence； - provenance； - sensitivity/visibility metadata。 低优先级内容不能覆盖高优先级约束。Agent delegation 可以收紧 policy 或增加任务说明，但不能 放宽 system/session policy ceiling。 6.3 结构不变量 任何 ContextPlan 必须满足： - 当前用户输入不可被预算算法删除； - Tool Result 必须引用可见 Tool Call； - 需要继续执行的 Tool Call/Result group 原子保留； - Agent result 必须引用可见 delegation/message identity； - checkpoint 与 tail 不重复覆盖同一 source range； - omission 不破坏角色和调用边界； - provider 不支持的内容只能按显式降级策略转换或报错； - reasoning/plan 是否回灌必须由 canonical contract 明确允许。 7. 上下文预算 7.1 预算输入 预算至少包含： - model context window； - reserved output tokens； - system/developer instruction cost； - Tool definitions cost； - current Turn minimum； - Tool/Agent continuation minimum； - safety margin； - compaction threshold； - tokenizer/estimate revision。 预算计量与响应 usage 是两类事实：前者在调用前判断候选请求能否发送，后者在调用完成后记录实际 消耗。精准计量可来自匹配模型的本地 tokenizer 或 provider preflight；provider preflight 本身不 等于精准，准确度必须由 provider 契约独立声明。估算的 measured value 用于诊断，预算边界使用带 策略余量的 accounted value；该余量不冒充 provider 承诺的数学硬上界。 不得使用一个含义不明的 maxtokens: Option<u32 同时表达 context window、output limit 和缺省。 这些值使用独立 newtype/enum。 7.2 预算顺序 推荐顺序： `text model context window − reserved output budget − mandatory instructions/tools/current input − safety margin = history budget ` 历史选择按完整语义单元处理，而不是按字符串尾部截断： - Turn group； - Tool Call/Result group； - Agent delegation/result group； - checkpoint + uncovered tail； - attachment/artifact reference group。 自动压缩阈值只决定何时产生压力，不是模型硬窗口。普通规划使用 pressure limit；独立压缩请求 可以使用完整 context window，但仍扣除 reserved output 与 safety margin，并且它自身必须通过 预算估算。这样低阈值会更早压缩，却不会错误拒绝仍可装入模型硬窗口的 source prefix。 Core-managed plan 会把同一份 reservedoutput 写入该次不可变 ModelRequest。Provider 配置只在 请求没有显式 limit 时提供缺省，因此 budget 与实际输出上限不会在 invoke 前因配置刷新而漂移。 7.3 Overflow 结果 预算不足必须产生明确 outcome： - NeedsCompaction； - CurrentInputTooLarge； - MandatoryInstructionsTooLarge； - ToolDefinitionsTooLarge； - CheckpointCapacityTooSmall； - CompactionSourceTooLarge； - UnsupportedContextShape。 不能静默删除当前输入、权限约束或未完成 Tool/Agent continuation。 已知窗口可以来自内置 ModelInfo，也可以由 ModelProviderConfig.modelcontext 按模型 ID 配置； 窗口未知时使用 ContextBudget::ProviderManaged，Core 不假装拥有可靠上限。预算解析、精准/估算 计量结果和统一边界判定已由 zeta-context-engine 提供；生产 planner 首轮仍使用带 revision 的 确定性 byte estimate。最终 canonical request 会按“官方 preflight → 匹配模型的本地整请求计数 → Core 保守估算”降级：OpenAI Responses 为 exact；Anthropic、Google native countTokens、Kimi estimate 与 Z.AI tokenizer 为 estimated remote；本地 hf-chat-template + tokenizers 结果为 estimated。只有 provider definition 明确声明且 model policy 允许的 binding 才会触发远程计量。 8. 压缩 8.1 持久化检查点 Checkpoint 至少记录： - stable checkpoint ID； - source Thread； - covered sequence range； - referenced Item/Event IDs； - source digest； - summary 内容或 artifact reference； - schema、prompt 与 context policy revision； - generator/model metadata； - creation time； - verification status。 原始 event log 永不因 compaction 删除。 8.2 压缩流程 `text ContextManager detects pressure → returns NeedsCompaction CompactionPlan → Turn/Core orchestration invokes CompactionService → validate summary + provenance + digest → ThreadController durable commits checkpoint → install new ThreadSnapshot → ContextManager invalidates old window/cache → prepare again from committed snapshot ` Summary model I/O 不持有 Thread writer。只有 checkpoint durable commit 后，后续 invocation 才能 依赖它。 8.3 恢复与失效 以下情况会使 checkpoint commit 或 event replay 失败即关闭，损坏的摘要不会进入 projection： - source range 不存在或重叠错误； - digest 不匹配； - schema/policy 不兼容； - referenced Item 缺失； - checkpoint 来自另一个 Thread； - schema/policy revision 缺失； - checkpoint range 倒退或不是 Thread 前缀。 当前不会在已损坏 event stream 上静默跳过 checkpoint 后继续恢复；原始事件也从不因压缩删除， 因此后续可以增加显式修复/隔离工具，而不用依赖摘要作为唯一副本。 Provider/model 切换不自动使 checkpoint 失效，但必须重新按新 model limits 和 capabilities 规划。 9. 上下文窗口与供应商变更 Context window 是模型可见历史的派生窗口，不是新的 history aggregate。 每次 model/provider/context policy 变化： `text wait for safe point → freeze new revisions → invalidate incompatible cached plan/baseline → rebuild from durable ThreadSnapshot → create new ModelInvocationSnapshot ` Provider response ID、prompt cache key 和 connection state 可以作为 adapter 优化，但不能成为 恢复正确性的前提。 10. 多 Agent 规则 每个 child Agent 的 ContextManager 只读取： - child Thread 的 durable history； - spawn 时固定的 AgentContextSeed； - durable delivered parent/peer messages； - child 自己的 policy/environment snapshot。 它不能读取 parent/sibling 的 live history 或 ContextManager。 Spawn context mode 必须显式： `text Fresh Selected item/artifact references ForkedPrefix full last turns checkpoint plus tail ` 所有模式都固定 parent Thread sequence 和 provenance。Spawn 后 parent compaction 或继续执行不改变 child 的 seed。Child result 回到 parent 后只是一个有界、可追踪的 durable result Item，由 parent ContextManager 决定是否选入下一次 ContextPlan。 详细协议见 core-multi-agent.md。 11. 目录与可见性 `text zeta-rs/ ├─ context-engine/ │ └─ src/ │ ├─ budget.rs │ ├─ measurement.rs │ ├─ planner.rs │ └─ tests.rs └─ core/src/ ├─ context.rs ├─ context/ │ ├─ model.rs │ ├─ plan.rs │ ├─ assembler.rs │ ├─ compaction.rs │ ├─ instructions.rs │ ├─ invocation.rs │ ├─ planner.rs │ ├─ skills.rs │ └─ tests.rs ├─ contextmanager.rs └─ contextmanagertests.rs ` context-engine 保存可跨产品和 provider 复用的预算 value 与纯判定；Core context 保存 Thread 内容 选择和组装算法；contextmanager 保存 per-loaded-Thread 协调逻辑。Core 两个模块默认 private。 ContextSource 是 Core 的通用可选 evidence port：host 可在 Turn 第一次 model in"
  },
  {
    "slug": "core-multi-agent",
    "sourcePath": "docs/core-multi-agent.md",
    "title": "多 Agent 协作系统",
    "description": "多 Agent 不是多个任务共享一份可变上下文，而是同一 Session 中多个相互关联、可独立恢复的 Thread。",
    "group": "Agent 与运行时",
    "markdown": "> 状态：核心纵向切片已实现（2026-08-12）。`DelegationId`、`AgentMessageId`、`AgentJoinId`、`AgentContextSeed`、\n> `ThreadOrigin::AgentSpawn`、durable delegation/message/result events、Fresh child Thread spawn、\n> exact-once delivery、结构性 tree budget，以及 App Server 的 `spawn_agent`、\n> `send_agent_message`、`wait_agent` 工具已落地。`Selected/ForkedPrefix` 在 spawn 时物化并进入\n> immutable seed；All/Any/Quorum join、向下 cancellation tree 与 Desktop Agent-tree projection\n> 均使用 durable Thread/Session facts。Agent definition 自动选择、跨产品 UI 和完整 fault matrix\n> 仍按后续需求演进。原落地顺序分为契约冻结\n> （[阶段 D](zeta-agent-runtime-architecture.md#阶段-d多-agent-契约冻结)）与运行时\n> （[阶段 E](zeta-agent-runtime-architecture.md#阶段-emultiagentcoordinator)）；理由见\n> [`zeta-agent-runtime-architecture.md` R4](zeta-agent-runtime-architecture.md#44-r4多-agent-契约冻结先行)。\n>\n> Core 总体边界：[`core.md`](core.md)\n> Context 与 ContextManager：[`core-context.md`](core-context.md)\n> Canonical Session/Thread/Turn contract：[`protocol.md`](protocol.md)\n> 外部 MCP Host 调用 Zeta 与 remote Agent bridge：[`mcp-server.md`](mcp-server.md)\n\n## 快速理解\n\n多 Agent 不是多个任务共享一份可变上下文，而是同一 Session 中多个相互关联、可独立恢复的\nThread。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| 子 Agent 在系统中是什么？ | 一个拥有独立 `ThreadId`、Turn、上下文和取消域的 Thread | [身份与聚合边界](#2-身份与聚合边界) |\n| 父子 Agent 共享历史或模型状态吗？ | 不共享可变状态；只通过明确的种子、消息和结果传递信息 | [上下文隔离](#11-上下文隔离) |\n| 创建、分叉和生成有什么区别？ | 创建建立新 Thread，分叉固定已有序列点，生成额外记录委托关系 | [创建、分叉与生成](#4-创建create分叉fork-与生成spawn) |\n| 子 Agent 如何回传结果？ | 结果通过可持久化消息和委托终态回到调用方，不靠进程内引用 | [结果与汇合](#8-结果与汇合) |\n| 取消父 Agent 会发生什么？ | App Server 的 Turn interrupt/Session stop 会向所有 live descendants 传播；child 取消不反向影响 parent/sibling | [取消与终态语义](#9-取消与终态语义) |\n\n## 1. 结论\n\nZeta 的多 Agent 使用同一 Session 下的独立 Thread：\n\n```text\nSession\n└─ parent Thread / Agent\n   ├─ child Thread / Agent A\n   └─ child Thread / Agent B\n```\n\n每个 Thread 独立拥有：\n\n- durable sequence、Turn 和 Item；\n- ThreadController 的逻辑提交序列；\n- ContextManager、context window 与 compaction；\n- model/tool/policy snapshot；\n- cancellation domain；\n- 恢复和 terminal outcome。\n\n多 Agent 不是多个执行 task 共享一份 `SessionHistory`。父子和 sibling 不共享 mutable context、\nprojection、provider conversation ID 或 Tool state。\n\n同进程 child Agent 由本架构定义的 `MultiAgentCoordinator` 创建，不通过 `zeta-mcp-server`\n自调用。跨 runtime Zeta 可以使用 MCP transport，但调用方仍必须在本地拥有 delegation、budget、\ncancellation 和 result delivery；MCP 只承担远端执行通信。\n\n长期必须把三种关系分开：\n\n1. **Thread lineage**：产品里的 root/fork/spawn 拓扑；\n2. **Agent delegation**：谁委派了什么工作以及结果状态；\n3. **Context inheritance**：child 首次 invocation 能看到 parent 的哪些内容。\n\n只记录 `parentThreadId` 不能表达后两者。\n\n## 2. 身份与聚合边界\n\n### 2.1 SessionId\n\n标识一个产品任务及其 Thread 拓扑。Session 只串行 membership、lineage、shared defaults 和\nlifecycle，不串行 child Thread 执行。\n\n### 2.2 ThreadId\n\n标识一条独立 Agent execution branch。当前阶段一个活跃 Agent 对应一个 Thread，因此可以用\n`ThreadId` 作为 Agent execution identity。\n\n暂不增加与 Thread 一一对应的 `AgentId`。只有出现下列真实需求时才引入：\n\n- 一个 Agent 身份跨多个 Thread 延续；\n- Thread 被替换但 Agent identity 必须保持；\n- 产品需要独立查询 Agent，而不能从 Thread/delegation 投影得到。\n\n### 2.3 DelegationId\n\n`DelegationId` 是必需的独立身份，标识一次 parent → child 工作委派。它不能由数组位置、child\nThread ID 或 Tool call completion 顺序隐式表达。\n\n一个 delegation 至少关联：\n\n- parent Thread/Turn；\n- parent sequence anchor；\n- child Thread；\n- delegated task；\n- context seed；\n- lifecycle；\n- result/delivery identity。\n\nAgent spawn 可能由模型 Tool Call、用户操作或系统策略触发，因此 `DelegationId` 不直接等同\n`ToolCallId`。\n\n### 2.4 AgentMessageId\n\n跨 Thread message/result 需要稳定 `AgentMessageId`，用于：\n\n- sender retry；\n- receiver deduplication；\n- crash recovery；\n- delivery receipt；\n- provenance。\n\n## 3. MultiAgentCoordinator\n\n`MultiAgentCoordinator` 是 Core 内部显式的多 Agent 协调组件。单 Agent 执行不经过它；\nThreadController、TurnExecutor、ContextManager 和 ToolScheduler 已经构成完整的单 Agent 路径。\n\n负责：\n\n- spawn、send、join、cancel、close；\n- delegation lifecycle；\n- context seed 物化；\n- parent/child cancellation tree；\n- max depth、max children、并发和总资源预算；\n- 协调 SessionCoordinator 创建 child Thread；\n- 协调 ThreadController 提交 parent/child durable facts；\n- 跨 Thread message/result 的 outbox/inbox delivery；\n- crash reconciliation。\n\n不负责：\n\n- 持有 Thread transcript；\n- 读取 live ContextManager；\n- 直接执行 child Turn；\n- model/Tool I/O；\n- Session membership reducer；\n- provider connection 或远端 Agent transport；\n- UI projection。\n\nMultiAgentCoordinator 可以协调多个 aggregate，但不能建立跨所有 Thread 的大锁。长 I/O、等待\nchild 和等待 delivery receipt 都在 aggregate writer 之外。\n\n## 4. 创建（Create）、分叉（Fork）与生成（Spawn）\n\n三种操作语义不同：\n\n| 操作 | 目的 | parent 关系 | context |\n| --- | --- | --- | --- |\n| Create | 新建独立 Thread | 无 | fresh |\n| Fork | 创建产品历史分支 | immutable lineage anchor | 明确 fork selection |\n| Spawn | 委派 child Agent | delegation + parent anchor | 明确 AgentContextSeed |\n\nFork 不自动等于 sub-agent。用户可以 fork side conversation；Agent 也可以在 fresh context 下\nspawn child。\n\n目标 `ThreadOrigin` 至少能区分：\n\n```rust\nenum ThreadOrigin {\n    Root,\n    Fork {\n        parent_thread_id: ThreadId,\n        parent_sequence: u64,\n    },\n    AgentSpawn {\n        parent_thread_id: ThreadId,\n        parent_sequence: u64,\n        delegation_id: DelegationId,\n    },\n}\n```\n\n`ThreadOrigin` 只描述来源和不可变 anchor。具体 context inheritance 放在独立\n`AgentContextSeed` 中，避免把拓扑与 prompt policy 混成一个 enum。\n\n当前实现的 `Fork` 只保存 lineage anchor，并创建空 child Thread；它不是完整的 history fork 或\nAgent spawn contract。文档和 API 在实现 inheritance 前必须保持这一事实。\n\n## 5. AgentContextSeed\n\nSpawn 时必须创建不可变、可验证的 seed：\n\n```rust\nstruct AgentContextSeed {\n    delegation_id: DelegationId,\n    parent_thread_id: ThreadId,\n    parent_sequence: u64,\n    task: DelegatedTask,\n    role: AgentRoleSnapshot,\n    inheritance: AgentContextMode,\n    selected_sources: Vec<ContextSourceRef>,\n    policy_ceiling: DelegatedPolicyCeiling,\n    capability_scope: DelegatedCapabilityScope,\n    digest: ContextSeedDigest,\n}\n```\n\n### 5.1 继承模式\n\n使用自描述 enum：\n\n```rust\nenum AgentContextMode {\n    Fresh,\n    Selected(SelectedContext),\n    ForkedPrefix(ForkedContext),\n}\n\nenum ForkedContext {\n    Full,\n    LastTurns(NonZeroU32),\n    CheckpointAndTail,\n}\n```\n\n不使用 `inherit_context: bool` 或 `last_turns: Option<u32>`。\n\n语义：\n\n- `Fresh`：只注入 shared constraints、Agent role 和 delegated task；\n- `Selected`：注入显式 Item/artifact references；\n- `ForkedPrefix`：从固定 parent sequence 选择完整或裁剪历史。\n\n所有模式都必须固定 source sequence 和 provenance。Spawn 后 parent 继续执行、compaction、\nprovider change 或 policy refresh 都不能偷偷改变 child seed。\n\n### 5.2 策略继承\n\nChild policy 必须满足：\n\n```text\neffective child policy\n  = session/system ceiling\n  ∩ parent delegated ceiling\n  ∩ child role restrictions\n  ∩ current host safety policy\n```\n\nChild 可以被进一步收紧，不能静默获得 parent 没有的 capability 或放宽 approval。\n\n### 5.3 种子持久化\n\nSeed 可以作为 child creation fact 或 stable artifact reference 持久化，但必须在 child 首次执行\n前 durable。若 seed 引用 parent Item：\n\n- source Thread/sequence 必须固定；\n- original event log 必须仍可解析；\n- digest 必须可验证；\n- visibility/sensitivity policy 必须允许；\n- materialization 失败时 child 不得在不完整上下文下静默启动。\n\n## 6. Spawn 事务\n\nSpawn 跨 parent Thread、Session 和 child Thread，必须使用可恢复 saga：\n\n```text\n1. parent commits DelegationRequested\n2. Session commits ChildThreadCreationPlanned\n3. child Thread is created with AgentSpawn origin + ContextSeed\n4. Session commits ChildThreadAttached\n5. parent commits DelegationStarted(child ThreadId)\n6. child Turn is accepted and scheduled\n```\n\n每一步都使用同一个 `DelegationId`。Crash recovery：\n\n- 只有 step 1：继续计划 child，或按明确 policy 取消；\n- 已计划但未创建：创建确定身份的 child；\n- child 已创建但未 attach：完成 attach；\n- attach 后 parent 未记录 started：补交 started；\n- child 已运行：不得创建第二个 child；\n- saga 失败：提交可解释的 terminal delegation outcome。\n\n如果 storage 将来提供 multi-stream transaction，可以优化提交次数，但不能改变可观察语义。\n\n## 7. 通信\n\n### 7.1 显式消息\n\nParent、child 和 sibling 只能通过显式 Agent message 交流：\n\n```rust\nstruct AgentMessage {\n    message_id: AgentMessageId,\n    delegation_id: Option<DelegationId>,\n    sender_thread_id: ThreadId,\n    receiver_thread_id: ThreadId,\n    sender_sequence: u64,\n    content: AgentMessageContent,\n    provenance: AgentMessageProvenance,\n}\n```\n\n不能通过：\n\n- 读取另一个 ContextManager；\n- 共享 mutable `Vec<Message>`；\n- 复用 provider conversation ID；\n- 直接修改 receiver projection；\n- 把跨 Agent message 冒充普通 user input。\n\n### 7.2 Durable 投递\n\n跨 Thread delivery 使用 outbox/inbox 语义：\n\n```text\nsender commits AgentMessageSent\n→ delivery worker/coordinator attempts receiver append\n→ receiver deduplicates AgentMessageId\n→ receiver commits AgentMessageReceived\n→ sender records receipt when required\n```\n\n目标是 at-least-once transport + exactly-once durable application，而不是假设一次函数调用天然\nexactly once。\n\nReceiver terminal/archived、policy rejection 和 delivery timeout 都需要明确 outcome。消息不能\n无声丢失。\n\n### 7.3 引导\n\n发送给正在执行 child 的新 instruction 是 steering，不等于修改它已经冻结的 invocation：\n\n- 先 durable delivery；\n- 当前 invocation 是否 interrupt 由明确 policy 决定；\n- 默认在下一个 model safe point 生效；\n- safety tightening 可以立即传播；\n- 普通 steering 不回写历史快照。\n\n## 8. 结果与汇合\n\nChild 完成时生成有界结果：\n\n```rust\nstruct DelegationResult {\n    delegation_id: DelegationId,\n    child_thread_id: ThreadId,\n    status: DelegationResultStatus,\n    summary: String,\n    artifacts: Vec<ArtifactRef>,\n    source_range: ThreadSequenceRange,\n    digest: DelegationResultDigest,\n}\n```\n\n结果 flow：\n\n```text\nchild commits terminal result\n→ result delivery uses stable AgentMessageId\n→ parent commits DelegationResultReceived\n→ waiting join condition is re-evaluated\n→ parent resumes at safe point\n→ parent ContextManager may select the bounded result\n```\n\nParent 不自动继承 child transcript。需要更多细节时，parent 可以通过显式 message 请求，或在\n授权范围内引用 child artifact。\n\nJoin policy 使用 enum：\n\n```text\nAll\nAny\nQuorum(NonZeroU32)\nExplicit(Vec<DelegationId>)\n```\n\nJoin 等待必须 durable；它不能靠一个进程内 `join_all` future 表达。\n\n## 9. 取消与终态语义\n\nCancellation tree：\n\n```text\nparent Agent source\n├─ child A source\n│  └─ child A operations\n└─ child B source\n   └─ child B operations\n```\n\n规则：\n\n- parent cancellation 传播到所有 live descendants；\n- child cancellation 不影响 parent 或 sibling；\n- Session shutdown 可以取消所有 roots；\n- cancel signal 是 best effort；\n- child 已执行的 Tool 副作用仍按 UnknownOutcome/reconciliation 处理；\n- parent 取消后迟到的 child result 可以 durable 记录为 late，但不能恢复已 terminal parent Turn；\n- terminal delegation 状态必须 durable，不能只看 task handle 是否结束。\n\n可区分的 terminal outcome 至少包括：\n\n- completed；\n- failed；\n- cancelled；\n- policy denied；\n- capacity rejected；\n- context seed invalid；\n- delivery failed；\n- unknown outcome。\n\n## 10. 资源预算与调度\n\n必须区分：\n\n```text\nPer-invocation context budget\n  ContextManager 负责\n\nPer-Turn execution limits\n  TurnExecutor 负责 token / cost / deadline 等 durable policy；不使用固定模型调用次数\n\nPer-Agent-tree resource budget\n  MultiAgentCoordinator 负责\n```\n\nAgent tree budget 至少包含：\n\n- max depth；\n- max live children；\n- max total descendants；\n- model concurrency；\n- Tool concurrency；\n- cumulative token/cost budget；\n- deadline；\n- optional per-role quota。\n\nReservation 必须在 spawn 前完成。并发 slot 是进程内资源，可以在 crash 后重建；已经消耗的\nusage 和 durable delegation 状态不能只存在内存。\n\nCapacity saturation 返回稳定 retryable/terminal outcome，不能无限排队。不同 Session 之间还需要\n宿主级 fairness，但其全局 scheduler 实现不必进入 Core aggregate。\n\n## 11. 上下文隔离\n\n每个 child ContextManager 只读取：\n\n- child Thread history；\n- child creation 时的 AgentContextSeed；\n- durable delivered message/result；\n- child 自己的 TurnPolicySnapshot；\n- composition root 提供的 child environment/capability snapshot。\n\n不能读取：\n\n- parent/sibling live ContextManager；\n- parent 在 anchor sequence 之后的 history；\n- Session 中其他 Thread 的 transcript；\n- 未授权的 cross-thread memory；\n- parent provider cache。\n\nParent result injection 规则：\n\n- result 是独立 canonical Item；\n- 必须携带 delegation/child/provenance；\n- 由 parent ContextManager 参与预算和选择；\n- 不能跳过 instruction precedence；\n- child 的不可信文本不能升级为 system fact。\n\n## 12. 批准与能力\n\nChild 的 approval request 仍属于 child Thread/Turn。Interaction 必须携带 child identity 和\ndelegation provenance。\n\n默认规则：\n\n- parent Agent 不是安全 authority；\n- parent 不能替用户批准超出 delegated ceiling 的 action；\n- UI 可以聚合显示同一 Agent tree 的 requests；\n- resolve 必须命中精确 RequestId；\n- parent cancel 可取消 child pending approval；\n- disconnect 与 owner selection 仍由 App Server 负责。\n\nCapability handle 不能跨 Agent 隐式共享。若 child 需要 browser/terminal/resource：\n\n- spawn seed 声明 capability scope；\n- host 在 child Turn safe point 解析 handle；\n- handle lifecycle 仍由宿主拥有；\n- child completion/cancel 触发明确 release policy。\n\n## 13. 恢复\n\n恢复顺序：\n\n1. replay Session topology；\n2. replay parent/child Thread；\n3. reconcile incomplete spawn saga；\n4. rebuild delegation projection；\n5. redeliver unacknowledged messages/results；\n6. rebuild cancellation and capacity state；\n7. rebuild each child ContextManager；\n8. schedule eligible non-terminal Turns。\n\n必须拒绝：\n\n- 重复 spawn 相同 delegation 的第二个 child；\n- 重复应用相同 AgentMessageId；\n- 旧 incarnation 的 completion；\n- source digest 不一致的 context seed；\n- terminal parent/child 上的非法恢复；\n- policy scope 已失效但仍尝试继续的 operation。\n\n## 14. Protocol 影响\n\n目标 canonical contract 需要逐步增加：\n\n- `DelegationId`、`AgentMessageId`；\n- `ThreadOrigin::AgentSpawn` 或等价来源模型；\n- `AgentContextSeed` / stable seed reference；\n- delegation requested/started/terminal facts；\n- Agent message sent/received/delivery outcome；\n- `DelegationResult` Item；\n- durable join/wait state；\n- readable Agent tree/delegation projection；\n- stable error/outcome code。\n\n不要把 Core-private mailbox、task handle、ContextManager 或 cancellation node 暴露到 protocol。\n\nProtocol 变更必须同步更新：\n\n- Rust canonical types；\n- JSON Schema；\n- generated TypeScript；\n- App Server method/update contract；\n- store envelope/schema version；\n- Desktop/CLI/TUI projection；\n- contract fixtures 和 recovery tests。\n\n## 15. 当前目录与扩展方向\n\n```text\ncore/src/\n├─ multi_agent.rs\n└─ multi_agent/\n   ├─ coordinator.rs\n   ├─ budget.rs\n   ├─ context.rs\n   └─ coordinator_tests.rs\n```\n\n当前纵向切片把 spawn、delivery 与 recovery 放在 `coordinator.rs`，避免先按想象拆出空模块。\n只有 cancellation、join 或 projection 形成独立 owner 且实现增长时，才拆为对应的 named child\nmodule。\n\n相关 ownership：\n\n```text\nMultiAgentCoordinator\n├─ uses SessionCoordinator for topology saga\n├─ uses ThreadController for durable parent/child mutations\n├─ creates immutable AgentContextSeed\n├─ owns delegation/delivery/resource policy\n└─ never owns ContextManager\n```\n\n模块默认 private。若 App Server 需要操作多 Agent，只公开 typed command/result 和 read-only\nprojection，不公开 coordinator 内部状态机。\n\n## 16. 落地顺序\n\n落地分两段执行；跨层阶段定义与完成条件由\n[`zeta-agent-runtime-architecture.md` §7](zeta-agent-runtime-architecture.md#7-分阶段实施计划)\n权威维护。\n\n**阶段 D｜契约冻结（已完成当前切片）：**\n\n1. 冻结 `DelegationId`、Agent spawn 与 message/result 语义；\n2. 区分 `ThreadOrigin::Fork` 与 Agent spawn；\n3. 定义 ContextSeed 及 `Fresh/Selected/ForkedPrefix`；\n4. delegation requested/started/terminal facts、`DelegationResult` Item 进入 canonical\n   protocol，同步 Rust types / JSON Schema / generated TS / fixtures。\n\n**阶段 E｜运行时（核心纵向切片已完成）：**\n\n5. ✅ parent delegation durable facts 的 Core 接线；\n6. ✅ 用可恢复 saga 创建 Fresh/Selected/ForkedPrefix child Thread；\n7. ✅ child execution、durable All/Any/Quorum join 与显式 `wait_agent` result；\n8. ✅ outbox/inbox exact-once delivery 与 steering；\n9. ✅ 结构性 Agent tree budget 与 parent-to-descendant cancellation tree；\n10. ✅ Desktop Workbench 从 Session lineage + thread projections 构造 Agent tree；\n11. 部分具备：spawn/join/cancellation recovery、duplicate delivery、digest corruption 与\n    context/tool isolation 已测试；更完整的 late-result/unknown-outcome fault matrix 继续补充。\n\n第一阶段不需要：\n\n- 独立 `zeta-agent` crate；\n- 与 Thread 一一对应的 Agent aggregate；\n- 任意 Agent graph workflow engine；\n- 跨机器 Agent transport；\n- 自动共享长期 memory；\n- parent/child provider cache 共享。\n\n## 17. 验证\n\n必须覆盖：\n\n- spawn 的每个 durable boundary crash；\n- 相同 DelegationId 幂等恢复；\n- fork 与 spawn 语义不混淆；\n- Fresh/Selected/ForkedPrefix 的可见内容；\n- parent anchor 后新增内容对子 Agent 不可见；\n- parent/child/sibling ContextManager 隔离；\n- message at-least-once transport + exactly-once apply；\n- duplicate/late/out-of-order result；\n- join All/Any/Quorum；\n- parent cancel、child cancel 和 sibling independence；\n- max depth/children/concurrency/budget；\n- child Tool UnknownOutcome；\n- invalid seed digest；\n- parent terminal 后的迟到 result；\n- restart 后 delegation、delivery、join 与 context 等价恢复。\n\n## 18. 固定决策\n\n- 一个活跃 Agent 当前绑定一个独立 Thread；\n- ThreadId 暂时足够表达 Agent execution identity；\n- DelegationId 与 AgentMessageId 是独立稳定身份；\n- fork lineage、Agent delegation 与 context inheritance 分离；\n- child Agent 不共享 parent/sibling mutable context；\n- context seed 在 spawn 时固定 parent sequence；\n- MultiAgentCoordinator 协调拓扑和 delivery，但不拥有 context；\n- parent/child 只通过 durable message/result 通信；\n- parent 不自动继承 child transcript；\n- per-Thread context budget 与 Agent tree resource budget 分离；\n- cancellation 向下传播，不向 parent/sibling 反向传播；\n- 多 Agent 恢复只依赖 durable facts；\n- 在出现真实多 Thread Agent identity 需求前不增加 Agent aggregate。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-身份与聚合边界",
        "title": "2. 身份与聚合边界"
      },
      {
        "depth": 3,
        "id": "21-sessionid",
        "title": "2.1 SessionId"
      },
      {
        "depth": 3,
        "id": "22-threadid",
        "title": "2.2 ThreadId"
      },
      {
        "depth": 3,
        "id": "23-delegationid",
        "title": "2.3 DelegationId"
      },
      {
        "depth": 3,
        "id": "24-agentmessageid",
        "title": "2.4 AgentMessageId"
      },
      {
        "depth": 2,
        "id": "3-multiagentcoordinator",
        "title": "3. MultiAgentCoordinator"
      },
      {
        "depth": 2,
        "id": "4-创建create分叉fork与生成spawn",
        "title": "4. 创建（Create）、分叉（Fork）与生成（Spawn）"
      },
      {
        "depth": 2,
        "id": "5-agentcontextseed",
        "title": "5. AgentContextSeed"
      },
      {
        "depth": 3,
        "id": "51-继承模式",
        "title": "5.1 继承模式"
      },
      {
        "depth": 3,
        "id": "52-策略继承",
        "title": "5.2 策略继承"
      },
      {
        "depth": 3,
        "id": "53-种子持久化",
        "title": "5.3 种子持久化"
      },
      {
        "depth": 2,
        "id": "6-spawn-事务",
        "title": "6. Spawn 事务"
      },
      {
        "depth": 2,
        "id": "7-通信",
        "title": "7. 通信"
      },
      {
        "depth": 3,
        "id": "71-显式消息",
        "title": "7.1 显式消息"
      },
      {
        "depth": 3,
        "id": "72-durable-投递",
        "title": "7.2 Durable 投递"
      },
      {
        "depth": 3,
        "id": "73-引导",
        "title": "7.3 引导"
      },
      {
        "depth": 2,
        "id": "8-结果与汇合",
        "title": "8. 结果与汇合"
      },
      {
        "depth": 2,
        "id": "9-取消与终态语义",
        "title": "9. 取消与终态语义"
      },
      {
        "depth": 2,
        "id": "10-资源预算与调度",
        "title": "10. 资源预算与调度"
      },
      {
        "depth": 2,
        "id": "11-上下文隔离",
        "title": "11. 上下文隔离"
      },
      {
        "depth": 2,
        "id": "12-批准与能力",
        "title": "12. 批准与能力"
      },
      {
        "depth": 2,
        "id": "13-恢复",
        "title": "13. 恢复"
      },
      {
        "depth": 2,
        "id": "14-protocol-影响",
        "title": "14. Protocol 影响"
      },
      {
        "depth": 2,
        "id": "15-当前目录与扩展方向",
        "title": "15. 当前目录与扩展方向"
      },
      {
        "depth": 2,
        "id": "16-落地顺序",
        "title": "16. 落地顺序"
      },
      {
        "depth": 2,
        "id": "17-验证",
        "title": "17. 验证"
      },
      {
        "depth": 2,
        "id": "18-固定决策",
        "title": "18. 固定决策"
      }
    ],
    "searchText": " 状态：核心纵向切片已实现（2026-08-12）。DelegationId、AgentMessageId、AgentJoinId、AgentContextSeed、 ThreadOrigin::AgentSpawn、durable delegation/message/result events、Fresh child Thread spawn、 exact-once delivery、结构性 tree budget，以及 App Server 的 spawnagent、 sendagentmessage、waitagent 工具已落地。Selected/ForkedPrefix 在 spawn 时物化并进入 immutable seed；All/Any/Quorum join、向下 cancellation tree 与 Desktop Agent-tree projection 均使用 durable Thread/Session facts。Agent definition 自动选择、跨产品 UI 和完整 fault matrix 仍按后续需求演进。原落地顺序分为契约冻结 （阶段 D）与运行时 （阶段 E）；理由见 zeta-agent-runtime-architecture.md R4。 Core 总体边界：core.md Context 与 ContextManager：core-context.md Canonical Session/Thread/Turn contract：protocol.md 外部 MCP Host 调用 Zeta 与 remote Agent bridge：mcp-server.md 快速理解 多 Agent 不是多个任务共享一份可变上下文，而是同一 Session 中多个相互关联、可独立恢复的 Thread。 读者首先会问 直接答案 深入阅读 --- --- --- 子 Agent 在系统中是什么？ 一个拥有独立 ThreadId、Turn、上下文和取消域的 Thread 身份与聚合边界 父子 Agent 共享历史或模型状态吗？ 不共享可变状态；只通过明确的种子、消息和结果传递信息 上下文隔离 创建、分叉和生成有什么区别？ 创建建立新 Thread，分叉固定已有序列点，生成额外记录委托关系 创建、分叉与生成 子 Agent 如何回传结果？ 结果通过可持久化消息和委托终态回到调用方，不靠进程内引用 结果与汇合 取消父 Agent 会发生什么？ App Server 的 Turn interrupt/Session stop 会向所有 live descendants 传播；child 取消不反向影响 parent/sibling 取消与终态语义 1. 结论 Zeta 的多 Agent 使用同一 Session 下的独立 Thread： ``text Session └─ parent Thread / Agent ├─ child Thread / Agent A └─ child Thread / Agent B ` 每个 Thread 独立拥有： - durable sequence、Turn 和 Item； - ThreadController 的逻辑提交序列； - ContextManager、context window 与 compaction； - model/tool/policy snapshot； - cancellation domain； - 恢复和 terminal outcome。 多 Agent 不是多个执行 task 共享一份 SessionHistory。父子和 sibling 不共享 mutable context、 projection、provider conversation ID 或 Tool state。 同进程 child Agent 由本架构定义的 MultiAgentCoordinator 创建，不通过 zeta-mcp-server 自调用。跨 runtime Zeta 可以使用 MCP transport，但调用方仍必须在本地拥有 delegation、budget、 cancellation 和 result delivery；MCP 只承担远端执行通信。 长期必须把三种关系分开： 1. Thread lineage：产品里的 root/fork/spawn 拓扑； 2. Agent delegation：谁委派了什么工作以及结果状态； 3. Context inheritance：child 首次 invocation 能看到 parent 的哪些内容。 只记录 parentThreadId 不能表达后两者。 2. 身份与聚合边界 2.1 SessionId 标识一个产品任务及其 Thread 拓扑。Session 只串行 membership、lineage、shared defaults 和 lifecycle，不串行 child Thread 执行。 2.2 ThreadId 标识一条独立 Agent execution branch。当前阶段一个活跃 Agent 对应一个 Thread，因此可以用 ThreadId 作为 Agent execution identity。 暂不增加与 Thread 一一对应的 AgentId。只有出现下列真实需求时才引入： - 一个 Agent 身份跨多个 Thread 延续； - Thread 被替换但 Agent identity 必须保持； - 产品需要独立查询 Agent，而不能从 Thread/delegation 投影得到。 2.3 DelegationId DelegationId 是必需的独立身份，标识一次 parent → child 工作委派。它不能由数组位置、child Thread ID 或 Tool call completion 顺序隐式表达。 一个 delegation 至少关联： - parent Thread/Turn； - parent sequence anchor； - child Thread； - delegated task； - context seed； - lifecycle； - result/delivery identity。 Agent spawn 可能由模型 Tool Call、用户操作或系统策略触发，因此 DelegationId 不直接等同 ToolCallId。 2.4 AgentMessageId 跨 Thread message/result 需要稳定 AgentMessageId，用于： - sender retry； - receiver deduplication； - crash recovery； - delivery receipt； - provenance。 3. MultiAgentCoordinator MultiAgentCoordinator 是 Core 内部显式的多 Agent 协调组件。单 Agent 执行不经过它； ThreadController、TurnExecutor、ContextManager 和 ToolScheduler 已经构成完整的单 Agent 路径。 负责： - spawn、send、join、cancel、close； - delegation lifecycle； - context seed 物化； - parent/child cancellation tree； - max depth、max children、并发和总资源预算； - 协调 SessionCoordinator 创建 child Thread； - 协调 ThreadController 提交 parent/child durable facts； - 跨 Thread message/result 的 outbox/inbox delivery； - crash reconciliation。 不负责： - 持有 Thread transcript； - 读取 live ContextManager； - 直接执行 child Turn； - model/Tool I/O； - Session membership reducer； - provider connection 或远端 Agent transport； - UI projection。 MultiAgentCoordinator 可以协调多个 aggregate，但不能建立跨所有 Thread 的大锁。长 I/O、等待 child 和等待 delivery receipt 都在 aggregate writer 之外。 4. 创建（Create）、分叉（Fork）与生成（Spawn） 三种操作语义不同： 操作 目的 parent 关系 context --- --- --- --- Create 新建独立 Thread 无 fresh Fork 创建产品历史分支 immutable lineage anchor 明确 fork selection Spawn 委派 child Agent delegation + parent anchor 明确 AgentContextSeed Fork 不自动等于 sub-agent。用户可以 fork side conversation；Agent 也可以在 fresh context 下 spawn child。 目标 ThreadOrigin 至少能区分： `rust enum ThreadOrigin Root, Fork parentthreadid: ThreadId, parentsequence: u64, , AgentSpawn parentthreadid: ThreadId, parentsequence: u64, delegationid: DelegationId, , ` ThreadOrigin 只描述来源和不可变 anchor。具体 context inheritance 放在独立 AgentContextSeed 中，避免把拓扑与 prompt policy 混成一个 enum。 当前实现的 Fork 只保存 lineage anchor，并创建空 child Thread；它不是完整的 history fork 或 Agent spawn contract。文档和 API 在实现 inheritance 前必须保持这一事实。 5. AgentContextSeed Spawn 时必须创建不可变、可验证的 seed： `rust struct AgentContextSeed delegationid: DelegationId, parentthreadid: ThreadId, parentsequence: u64, task: DelegatedTask, role: AgentRoleSnapshot, inheritance: AgentContextMode, selectedsources: Vec<ContextSourceRef , policyceiling: DelegatedPolicyCeiling, capabilityscope: DelegatedCapabilityScope, digest: ContextSeedDigest, ` 5.1 继承模式 使用自描述 enum： `rust enum AgentContextMode Fresh, Selected SelectedContext , ForkedPrefix ForkedContext , enum ForkedContext Full, LastTurns NonZeroU32 , CheckpointAndTail, ` 不使用 inheritcontext: bool 或 lastturns: Option<u32 。 语义： - Fresh：只注入 shared constraints、Agent role 和 delegated task； - Selected：注入显式 Item/artifact references； - ForkedPrefix：从固定 parent sequence 选择完整或裁剪历史。 所有模式都必须固定 source sequence 和 provenance。Spawn 后 parent 继续执行、compaction、 provider change 或 policy refresh 都不能偷偷改变 child seed。 5.2 策略继承 Child policy 必须满足： `text effective child policy = session/system ceiling ∩ parent delegated ceiling ∩ child role restrictions ∩ current host safety policy ` Child 可以被进一步收紧，不能静默获得 parent 没有的 capability 或放宽 approval。 5.3 种子持久化 Seed 可以作为 child creation fact 或 stable artifact reference 持久化，但必须在 child 首次执行 前 durable。若 seed 引用 parent Item： - source Thread/sequence 必须固定； - original event log 必须仍可解析； - digest 必须可验证； - visibility/sensitivity policy 必须允许； - materialization 失败时 child 不得在不完整上下文下静默启动。 6. Spawn 事务 Spawn 跨 parent Thread、Session 和 child Thread，必须使用可恢复 saga： `text 1. parent commits DelegationRequested 2. Session commits ChildThreadCreationPlanned 3. child Thread is created with AgentSpawn origin + ContextSeed 4. Session commits ChildThreadAttached 5. parent commits DelegationStarted child ThreadId 6. child Turn is accepted and scheduled ` 每一步都使用同一个 DelegationId。Crash recovery： - 只有 step 1：继续计划 child，或按明确 policy 取消； - 已计划但未创建：创建确定身份的 child； - child 已创建但未 attach：完成 attach； - attach 后 parent 未记录 started：补交 started； - child 已运行：不得创建第二个 child； - saga 失败：提交可解释的 terminal delegation outcome。 如果 storage 将来提供 multi-stream transaction，可以优化提交次数，但不能改变可观察语义。 7. 通信 7.1 显式消息 Parent、child 和 sibling 只能通过显式 Agent message 交流： `rust struct AgentMessage messageid: AgentMessageId, delegationid: Option<DelegationId , senderthreadid: ThreadId, receiverthreadid: ThreadId, sendersequence: u64, content: AgentMessageContent, provenance: AgentMessageProvenance, ` 不能通过： - 读取另一个 ContextManager； - 共享 mutable Vec<Message ； - 复用 provider conversation ID； - 直接修改 receiver projection； - 把跨 Agent message 冒充普通 user input。 7.2 Durable 投递 跨 Thread delivery 使用 outbox/inbox 语义： `text sender commits AgentMessageSent → delivery worker/coordinator attempts receiver append → receiver deduplicates AgentMessageId → receiver commits AgentMessageReceived → sender records receipt when required ` 目标是 at-least-once transport + exactly-once durable application，而不是假设一次函数调用天然 exactly once。 Receiver terminal/archived、policy rejection 和 delivery timeout 都需要明确 outcome。消息不能 无声丢失。 7.3 引导 发送给正在执行 child 的新 instruction 是 steering，不等于修改它已经冻结的 invocation： - 先 durable delivery； - 当前 invocation 是否 interrupt 由明确 policy 决定； - 默认在下一个 model safe point 生效； - safety tightening 可以立即传播； - 普通 steering 不回写历史快照。 8. 结果与汇合 Child 完成时生成有界结果： `rust struct DelegationResult delegationid: DelegationId, childthreadid: ThreadId, status: DelegationResultStatus, summary: String, artifacts: Vec<ArtifactRef , sourcerange: ThreadSequenceRange, digest: DelegationResultDigest, ` 结果 flow： `text child commits terminal result → result delivery uses stable AgentMessageId → parent commits DelegationResultReceived → waiting join condition is re-evaluated → parent resumes at safe point → parent ContextManager may select the bounded result ` Parent 不自动继承 child transcript。需要更多细节时，parent 可以通过显式 message 请求，或在 授权范围内引用 child artifact。 Join policy 使用 enum： `text All Any Quorum NonZeroU32 Explicit Vec<DelegationId ` Join 等待必须 durable；它不能靠一个进程内 joinall future 表达。 9. 取消与终态语义 Cancellation tree： `text parent Agent source ├─ child A source │ └─ child A operations └─ child B source └─ child B operations ` 规则： - parent cancellation 传播到所有 live descendants； - child cancellation 不影响 parent 或 sibling； - Session shutdown 可以取消所有 roots； - cancel signal 是 best effort； - child 已执行的 Tool 副作用仍按 UnknownOutcome/reconciliation 处理； - parent 取消后迟到的 child result 可以 durable 记录为 late，但不能恢复已 terminal parent Turn； - terminal delegation 状态必须 durable，不能只看 task handle 是否结束。 可区分的 terminal outcome 至少包括： - completed； - failed； - cancelled； - policy denied； - capacity rejected； - context seed invalid； - delivery failed； - unknown outcome。 10. 资源预算与调度 必须区分： `text Per-invocation context budget ContextManager 负责 Per-Turn execution limits TurnExecutor 负责 token / cost / deadline 等 durable policy；不使用固定模型调用次数 Per-Agent-tree resource budget MultiAgentCoordinator 负责 ` Agent tree budget 至少包含： - max depth； - max live children； - max total descendants； - model concurrency； - Tool concurrency； - cumulative token/cost budget； - deadline； - optional per-role quota。 Reservation 必须在 spawn 前完成。并发 slot 是进程内资源，可以在 crash 后重建；已经消耗的 usage 和 durable delegation 状态不能只存在内存。 Capacity saturation 返回稳定 retryable/terminal outcome，不能无限排队。不同 Session 之间还需要 宿主级 fairness，但其全局 scheduler 实现不必进入 Core aggregate。 11. 上下文隔离 每个 child ContextManager 只读取： - child Thread history； - child creation 时的 AgentContextSeed； - durable delivered message/result； - child 自己的 TurnPolicySnapshot； - composition root 提供的 child environment/capability snapshot。 不能读取： - parent/sibling live ContextManager； - parent 在 anchor sequence 之后的 history； - Session 中其他 Thread 的 transcript； - 未授权的 cross-thread memory； - parent provider cache。 Parent result injection 规则： - result 是独立 canonical Item； - 必须携带 delegation/child/provenance； - 由 parent ContextManager 参与预算和选择； - 不能跳过 instruction precedence； - child 的不可信文本不能升级为 system fact。 12. 批准与能力 Child 的 approval request 仍属于 child Thread/Turn。Interaction 必须携带 child identity 和 delegation provenance。 默认规则： - parent Agent 不是安全 authority； - parent 不能替用户批准超出 delegated ceiling 的 action； - UI 可以聚合显示同一 Agent tree 的 requests； - resolve 必须命中精确 RequestId； - parent cancel 可取消 child pending approval； - disconnect 与 owner selection 仍由 App Server 负责。 Capability handle 不能跨 Agent 隐式共享。若 child 需要 browser/terminal/resource： - spawn seed 声明 capability scope； - host 在 child Turn safe point 解析 handle； - handle lifecycle 仍由宿主拥有； - child completion/cancel 触发明确 release policy。 13. 恢复 恢复顺序： 1. replay Session topology； 2. replay parent/child Thread； 3. reconcile incomplete spawn saga； 4. rebuild delegation projection； 5. redeliver unacknowledged messages/results； 6. rebuild cancellation and capacity state； 7. rebuild each child ContextManager； 8. schedule eligible non-terminal Turns。 必须拒绝： - 重复 spawn 相同 delegation 的第二个 child； - 重复应用相同 AgentMessageId； - 旧 incarnation 的 completion； - source digest 不一致的 context seed； - terminal parent/child 上的非法恢复； - policy scope 已失效但仍尝试继续的 operation。 14. Protocol 影响 目标 canonical contract 需要逐步增加： - DelegationId、AgentMessageId； - ThreadOrigin::AgentSpawn 或等价来源模型； - AgentContextSeed / stable seed reference； - delegation requested/started/terminal facts； - Agent message sent/received/delivery outcome； - DelegationResult Item； - durable join/wait state； - readable Agent tree/delegation projection； - stable error/outcome code。 不要把 Core-private mailbox、task handle、ContextManager 或 cancellation node 暴露到 protocol。 Protocol 变更必须同步更新： - Rust canonical types； - JSON Schema； - generated TypeScript； - App Server method/update contract； - store envelope/schema version； - Desktop/CLI/TUI projection； - contract fixtures 和 reco"
  },
  {
    "slug": "agent-customizations",
    "sourcePath": "docs/agent-customizations.md",
    "title": "Agent 自定义系统",
    "description": "Zeta 只把 Instructions、Skills 和 Agents 作为 Agent 自定义领域对象。Prompt 是运行时送入模型的 信息形式，Slash Command 是调用入口；两者都不是第四种可持久化自定义对象。项目级原生对象统一 位于小写 .zeta/，其他产品的目录和格式必须经过 zeta-agent-import，不能由原生 loader",
    "group": "Agent 与运行时",
    "markdown": "> 本文是 Zeta 原生 Agent 自定义对象、物理命名空间、加载语义和外部生态导入边界的跨系统\n> canonical owner。\n>\n> 状态：架构边界已接受（2026-08-12）；Workspace 原生 catalog slice 已实现：Global\n> Instructions 会进入后续 model invocation，Skills 已有 metadata catalog、显式 activation 和通用\n> context injection，Agents 完成定义发现。Contextual/OnDemand Instruction 选择、自动 Skill\n> selection、Agent 执行选择和完整 import apply 仍未实现。\n>\n> Skill 的格式、来源与激活细节见 [`skills.md`](skills.md)；外部格式发现和转换实现契约见\n> [`zeta-agent-import` README](../zeta-rs/agent-import/README.md)；配置与事务边界见\n> [`config.md`](config.md)；最终模型输入由 [`core-context.md`](core-context.md) 定义。\n\n## 快速理解\n\nZeta 只把 Instructions、Skills 和 Agents 作为 Agent 自定义领域对象。Prompt 是运行时送入模型的\n信息形式，Slash Command 是调用入口；两者都不是第四种可持久化自定义对象。项目级原生对象统一\n位于小写 `.zeta/`，其他产品的目录和格式必须经过 `zeta-agent-import`，不能由原生 loader\n顺便扫描。\n\n| 用户想表达什么 | Zeta 对象 | 何时进入运行时 | 典型入口 |\n| --- | --- | --- | --- |\n| “在这个环境里应长期遵循什么” | Instructions | 全局、上下文匹配或显式按需加载 | 自动解析或用户选择 |\n| “这类工作应该怎样完成” | Skills | 被用户选择或模型匹配后渐进加载 | picker、Slash Command 或模型选择 |\n| “由哪种执行配置来工作” | Agents | 启动主执行者或委托执行者时冻结配置引用 | Agent picker 或 delegation |\n| “现在请完成这件事” | 当前 Turn 的用户输入 | 构造本次 `ModelRequest` 时 | 普通消息 |\n| “快速调用某个能力” | 不是新对象 | 解析后调用已有 Skill 或产品命令 | `/name` |\n| “把别的 Agent 配置带进来” | Import workflow | 用户确认并由目标 authority 发布后 | Desktop import |\n\n## 1. 领域对象只有三类\n\n| 对象 | 回答的问题 | 拥有 | 明确不拥有 |\n| --- | --- | --- | --- |\n| Instructions | Agent 应遵循什么长期或作用域指导？ | 指令正文、作用范围、加载策略、优先级来源和内容摘要 | 可执行脚本、工具授权、模型调用本身 |\n| Skills | Agent 如何完成一类可复用工作？ | `SKILL.md`、渐进加载、引用资源、选择策略和来源身份 | 工具实现、脚本执行权限、当前任务实例 |\n| Agents | 使用什么执行配置？ | 模型/工具/Skill/Instruction 的类型化引用、执行角色和委托约束 | Thread 运行时身份、凭据、批准或工具实现 |\n\n三类对象不能按文件扩展名区分语义，也不能合并成一个泛化的 `PromptArtifact`。它们会以不同方式\n参与上下文和执行，因此需要独立 authority、校验与 snapshot。\n\n以下概念不进入 artifact type：\n\n- **Prompt**：模型输入的底层信息形式。Zeta 的 provider-neutral 运行时输出是 `ModelRequest`，\n  不建立 `Prompt` 领域对象或 `prompts/` 原生目录。\n- **Task**：当前工作由 `Turn` 的用户输入和 durable Thread history 表达。重复工作流使用 Skill；\n  需要只允许用户主动调用时，使用 `UserOnly` Skill，而不是新增 Task/Preset 类型。\n- **Slash Command**：调用已有对象或产品命令的 UI 投影，不是持久化定义格式。\n- **Agent runtime identity**：Agent definition 是配置对象；执行分支仍由 Thread 标识，不能仅因\n  新增定义文件就在协议中发明第二套运行时身份。\n\n## 2. 类型、来源和加载策略是三条独立轴\n\n一个 artifact 的类型不能同时承担“从哪里来”和“何时加载”。目标模型必须分别表达：\n\n| 轴 | 典型取值 | 决定什么 |\n| --- | --- | --- |\n| Artifact kind | Instructions / Skills / Agents | 对象结构、authority 与 runtime contribution |\n| Scope/source | Built-in / User / Workspace / Plugin | 生命周期、优先级、可写位置与失效方式 |\n| Provenance | Zeta native / imported from external ecosystem | 审计、冲突解释和重新导入来源 |\n| Activation policy | 按对象类型定义的 named enum | 自动加载、上下文匹配、用户调用或模型选择 |\n\n“Imported”不是 User/Workspace 的替代 scope。导入后的对象仍属于明确的 User 或 Workspace\nauthority，同时保留外部生态、来源位置 identity 和 digest 等 provenance。Plugin 贡献继续由\nPlugin package 拥有，不能复制成普通 Workspace 文件后丢失 package/version 身份。\n\n### 2.1 Instruction 加载策略\n\nInstruction authority 的 canonical policy 使用穷尽枚举，不通过 `applyTo` 是否存在或\n`alwaysApply: bool` 间接猜测：\n\n```rust\npub enum InstructionLoadPolicy {\n    Global,\n    Contextual { patterns: Vec<GlobPattern> },\n    OnDemand,\n}\n```\n\n- `Global`：在对应 scope 的每次 Agent interaction 中加载。\n- `Contextual`：只有当前资源或工作上下文命中 pattern 时加载。\n- `OnDemand`：只在用户或上层已验证引用显式选择时加载。\n\n外部格式中的 `applyTo`、`globs`、`alwaysApply` 等字段由 `zeta-agent-import` adapter 转换成这三种\n语义。`applyTo: \"**\"` 仍是“恰好匹配所有文件的上下文规则”，不能偷偷改写成 Global；真正的\nGlobal 必须由来源格式中明确等价的语义产生。\n\n### 2.2 Skill 调用策略\n\nSkill 的可发现性与调用方也使用 named policy，而不是两个容易产生非法组合的布尔字段：\n\n```rust\npub enum SkillInvocationPolicy {\n    UserOrModel,\n    UserOnly,\n    ModelOnly,\n}\n```\n\n保存的 review、fix-tests、create-component 等重复任务属于 `UserOnly` 或 `UserOrModel` Skill。\n当 `UserOnly` Skill 被投影为 `/review` 时，Skill 仍是领域对象，`/review` 只是调用它的入口。\n\n## 3. `.zeta` 是 Workspace 原生命名空间\n\n项目级 Zeta 原生对象统一规划到小写 `.zeta/`。大写 `.ZETA` 不作为第二个兼容名称，避免在\n大小写敏感文件系统上形成两个 authority。\n\n```text\n<workspace_root>/.zeta/\n├── config.toml       # Current：严格只读 Workspace intent\n├── instructions/     # Current：有界发现；Global 自动注入\n├── skills/           # Current：metadata-only Workspace Skill source\n└── agents/           # Current：有界 definition catalog；尚不执行\n```\n\n三个 artifact root 都使用固定原生布局：Instructions 和 Agents 是直接 `.md` 文件，Skills 是\n`<name>/SKILL.md` 目录。Workspace 激活时，App Server 只把可信 root 交给对应 runtime；\n`zeta-skills-extension` 拥有 Skill catalog 与 watcher refresh。模型调用不在 Core context assembly\n中扫描 catalog：Global Instructions 使用冻结的 `HarnessInstructions` snapshot；已激活 Skill 由\nextension 按 durable digest 精确加载正文。读取 catalog 本身仍不会激活 Skill 或执行 Agent definition。\n\n| Scope/source | 物理 owner | 是否经过 `zeta-agent-import` |\n| --- | --- | --- |\n| Built-in | release/package resources | ❌ 原生 authority 直接加载 |\n| User | `<profile_root>` 下的 Zeta-owned artifact root（Proposed） | ❌ 原生 authority 直接加载 |\n| Workspace | `<workspace_root>/.zeta/{instructions,skills,agents}`（Current catalog slice） | ❌ 原生 authority 直接加载 |\n| Plugin | Plugin package contribution | ❌ 由 Plugin snapshot 交给目标 authority |\n| External ecosystem | `.codex`、`.agents`、`.claude` 等已知布局 | ✅ 只经 `zeta-agent-import` |\n\n`<profile_root>` 本身已经是 Zeta 的用户级命名空间，因此不再嵌套一个 `~/.zeta` 兼容目录。\nWorkspace `.zeta` 继续作为受保护 metadata；普通文件搜索、Agent 工具写入和外部 source\nregistration 不能把它当作任意内容目录。\n\n原生 loader 只读取自己的 canonical roots。它不得自动扫描 `AGENTS.md`、`.codex/`、`.agents/`、\n`.claude/`、`.github/` 或其他产品目录；否则 compatibility policy 会散落到三个 authority 中，\n外部格式也会反向定义 Zeta schema。\n\n## 4. `zeta-agent-import` 是外部反腐化层\n\n`zeta-agent-import` 的“Agent”表示外部 Agent 生态，不表示它只导入 Agents artifact。它统一处理\nCodex、Claude 以及未来明确支持的其他生态中的 Instructions、Skills、Agents 和设置类内容。\n\n```mermaid\nflowchart LR\n    external[\"外部 Agent 生态<br/>已知目录与格式\"] --> inspect[\"zeta-agent-import<br/>discover / inspect / parse / normalize\"]\n    inspect --> preview[\"App Server import coordinator<br/>preview / conflict / user confirmation\"]\n    preview --> instruction[\"Instruction authority\"]\n    preview --> skill[\"Skill authority / Config source\"]\n    preview --> agent[\"Agent definition authority\"]\n    preview --> config[\"MCP / Plugin / Hook / other Config authorities\"]\n    instruction --> runtime[\"immutable activation snapshots\"]\n    skill --> runtime\n    agent --> runtime\n```\n\n| 责任 | `zeta-agent-import` | App Server coordinator | 目标 authority |\n| --- | --- | --- | --- |\n| 已知外部路径与敏感排除 | ✅ | ❌ | ❌ |\n| source-specific bounded parser | ✅（Proposed） | ❌ | ❌ |\n| normalized preview fragment 与 provenance | ✅（Proposed） | 组合 | 最终复核 |\n| 用户选择、冲突预览与 apply orchestration | ❌ | ✅（Proposed） | 提供 prepare/publish contract |\n| Zeta canonical schema 与领域校验 | ❌ | ❌ | ✅ |\n| `.zeta` 原生发现与加载 | ❌ | 协调 snapshot | ✅ |\n| 持久化、enablement 与 runtime activation | ❌ | 调用 | ✅ |\n\nparser 输出必须是目标明确、可审查的 typed fragment，例如 Instruction、Skill source、Agent\ndefinition 或 Config mutation fragment；不能输出一段“以后再解释”的原始 JSON/Markdown。外部字段\n无法确定性映射时必须标记 unsupported，不能 raw passthrough，也不能让 `zeta-agent-import` 依赖\n`zeta-config`、Core 或具体产品 UI。\n\n当前 `zeta-agent-import` 只完成 metadata-only `AgentPathInspection`，尚未读取正文或生成 normalized\nfragment。上图除 inspection 之外的 import apply 路径均为 Proposed。\n\n## 5. Import 与来源注册不等价\n\n| 操作 | 权威正文在哪里 | 外部变化是否自动影响 Zeta | 撤销语义 | 适合对象 |\n| --- | --- | --- | --- | --- |\n| Import | Zeta 原生 authority | ❌，再次导入需显式触发 | 删除/回滚 Zeta artifact 或 import receipt | Instructions、Agents、需要独立管理的 Skills |\n| Register source | 外部只读 root | ✅，刷新后产生新 catalog generation | 禁用或移除 source | 当前已支持的 standalone Skill source |\n\n两条路径可以复用相同的外部 discovery、containment 和 parser，但不能共享生命周期决定：\n\n- Import 不授予来源目录的持续文件访问；apply 前必须重读并校验 identity/digest。\n- Source registration 只保存窄 root 与 provenance，不复制整个外部配置目录。\n- 注册 Skill source 不允许顺便激活其中的 Instructions、Agents、Hooks、MCP 或执行规则。\n- `zeta-add-dir` 是第三条独立路径：它管理目录访问生命周期，不等于 Import 或持久 source\n  registration。\n\n首个完整 apply 可以只支持 Config 内部的原子 batch，但不能假装同时原子提交尚不存在的\nInstruction/Agent authority。在跨 authority prepare/publish 可用前，对应条目必须保持 unsupported。\n\n## 6. 运行时只消费冻结快照\n\n```text\n原生与已导入来源\n→ 各 artifact authority 发现、校验、解析\n→ Config/enablement 与作用域解析\n→ Instruction / Skill / Agent immutable snapshots\n→ ContextPlan / ContextAssembler\n→ ModelRequest\n```\n\nCore 不读取 customization 文件，也不在组装模型请求时扫描目录。每次 model invocation 消费已经\n冻结的 snapshot 或由 extension 根据 durable provenance 贡献的精确 fragment；文件或配置变化只在\n下一个安全点生效，且不能替换 in-flight Skill 的 digest。最终输入可以包含系统内置指令、作用域\nInstructions、Agent references、已激活 Skill 内容、用户消息和 Tool results，但它仍是\n`ModelRequest`，不是一个需要持久化的 Prompt artifact。\n\nSlash Command catalog 同样只做调用投影：产品命令绑定到 product action，用户可调用 Skill 绑定到\n稳定 `SkillRef`。Slash Command 名称、补全和展示状态不能代替目标对象的 identity、权限或\nactivation validation。\n\n## 7. 当前状态与实施顺序\n\n| 能力 | 状态 | 实现证据或前置条件 |\n| --- | --- | --- |\n| `.zeta/config.toml` Workspace intent | 已实现 | `zeta-config` / App Server local composition |\n| Skill built-in/user/Workspace catalog 与 enablement | 部分具备 | `zeta-skills`、`SkillRuntime::compose_sources` 与 [`skills.md`](skills.md) |\n| Skill activation snapshot 与通用 context injection | 已实现 | validated `SkillRef`、正文加载、safe-point freezing 与 extension contributors |\n| Codex/Claude known-path inspection | 已实现 | `zeta-agent-import::inspect_agent_paths` |\n| Workspace Instructions authority | 部分具备 | `zeta-instructions` + `WorkspaceCustomizations`；Global 注入已实现，其他选择策略未实现 |\n| Workspace Agents authority | 部分具备 | `zeta-agents` + `WorkspaceCustomizations`；catalog/refresh 已实现，list/selection/runtime 未实现 |\n| `.zeta/{instructions,skills,agents}` loader | 已实现 | 固定原生 roots、有界校验、Workspace activation 与 watcher refresh |\n| External parser、preview 与 apply | 尚未完成 | typed fragments、digest、wire contract、transaction/receipt |\n| Skill → Slash Command 动态投影 | 已实现 | TUI/Desktop `/name` 绑定 stable `SkillRef`；`/skills` 只管理 |\n\n实施顺序：\n\n1. 固定本文的三类对象、命名空间和 import boundary，代码与文档不再新增 Prompt/Task artifact。\n2. 已完成 Workspace 三类 catalog、Global Instruction safe-point injection 与 watcher refresh。\n3. 下一步实现 Contextual/OnDemand Instruction 解析与自动 Skill selection；Skill 显式 activation、\n   Workspace source、用户可调用投影和通用 context injection 已接通。\n4. Agent definition catalog 保持只读，直到 list API、cross-authority reference resolution 和\n   multi-agent runtime 同时具备后再开放执行选择。\n5. 为 `zeta-agent-import` 增加 source-specific bounded parsers 和 typed preview fragments；App\n   Server 只对已经具备 target authority 的条目开放 apply。\n\n## 8. 长期不变量\n\n- Zeta 原生 Agent customization 只有 Instructions、Skills、Agents 三类领域对象。\n- Prompt、Task、Preset 和 Slash Command 不成为第四种 artifact；重复任务使用 Skill。\n- Workspace 原生目录只使用小写 `.zeta`，不维护 `.ZETA` alias。\n- 外部生态格式只由 `zeta-agent-import` 理解；原生 authority 不扫描兼容目录。\n- 类型、scope/source、provenance 与 activation policy 分开建模。\n- 导入成功不授予工具、脚本、网络、凭据、沙箱绕过或长期执行批准。\n- Preview 不是 authorization；apply 前必须重读并验证 source identity/digest。\n- 原生 loader、Import、source registration 与 `add-dir` 保持四条不同生命周期路径。\n- ContextAssembler 只消费冻结 snapshot 并产生 `ModelRequest`，不拥有 artifact discovery。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-领域对象只有三类",
        "title": "1. 领域对象只有三类"
      },
      {
        "depth": 2,
        "id": "2-类型来源和加载策略是三条独立轴",
        "title": "2. 类型、来源和加载策略是三条独立轴"
      },
      {
        "depth": 3,
        "id": "21-instruction-加载策略",
        "title": "2.1 Instruction 加载策略"
      },
      {
        "depth": 3,
        "id": "22-skill-调用策略",
        "title": "2.2 Skill 调用策略"
      },
      {
        "depth": 2,
        "id": "3-zeta-是-workspace-原生命名空间",
        "title": "3. .zeta 是 Workspace 原生命名空间"
      },
      {
        "depth": 2,
        "id": "4-zeta-agent-import-是外部反腐化层",
        "title": "4. zeta-agent-import 是外部反腐化层"
      },
      {
        "depth": 2,
        "id": "5-import-与来源注册不等价",
        "title": "5. Import 与来源注册不等价"
      },
      {
        "depth": 2,
        "id": "6-运行时只消费冻结快照",
        "title": "6. 运行时只消费冻结快照"
      },
      {
        "depth": 2,
        "id": "7-当前状态与实施顺序",
        "title": "7. 当前状态与实施顺序"
      },
      {
        "depth": 2,
        "id": "8-长期不变量",
        "title": "8. 长期不变量"
      }
    ],
    "searchText": " 本文是 Zeta 原生 Agent 自定义对象、物理命名空间、加载语义和外部生态导入边界的跨系统 canonical owner。 状态：架构边界已接受（2026-08-12）；Workspace 原生 catalog slice 已实现：Global Instructions 会进入后续 model invocation，Skills 已有 metadata catalog、显式 activation 和通用 context injection，Agents 完成定义发现。Contextual/OnDemand Instruction 选择、自动 Skill selection、Agent 执行选择和完整 import apply 仍未实现。 Skill 的格式、来源与激活细节见 skills.md；外部格式发现和转换实现契约见 zeta-agent-import README；配置与事务边界见 config.md；最终模型输入由 core-context.md 定义。 快速理解 Zeta 只把 Instructions、Skills 和 Agents 作为 Agent 自定义领域对象。Prompt 是运行时送入模型的 信息形式，Slash Command 是调用入口；两者都不是第四种可持久化自定义对象。项目级原生对象统一 位于小写 .zeta/，其他产品的目录和格式必须经过 zeta-agent-import，不能由原生 loader 顺便扫描。 用户想表达什么 Zeta 对象 何时进入运行时 典型入口 --- --- --- --- “在这个环境里应长期遵循什么” Instructions 全局、上下文匹配或显式按需加载 自动解析或用户选择 “这类工作应该怎样完成” Skills 被用户选择或模型匹配后渐进加载 picker、Slash Command 或模型选择 “由哪种执行配置来工作” Agents 启动主执行者或委托执行者时冻结配置引用 Agent picker 或 delegation “现在请完成这件事” 当前 Turn 的用户输入 构造本次 ModelRequest 时 普通消息 “快速调用某个能力” 不是新对象 解析后调用已有 Skill 或产品命令 /name “把别的 Agent 配置带进来” Import workflow 用户确认并由目标 authority 发布后 Desktop import 1. 领域对象只有三类 对象 回答的问题 拥有 明确不拥有 --- --- --- --- Instructions Agent 应遵循什么长期或作用域指导？ 指令正文、作用范围、加载策略、优先级来源和内容摘要 可执行脚本、工具授权、模型调用本身 Skills Agent 如何完成一类可复用工作？ SKILL.md、渐进加载、引用资源、选择策略和来源身份 工具实现、脚本执行权限、当前任务实例 Agents 使用什么执行配置？ 模型/工具/Skill/Instruction 的类型化引用、执行角色和委托约束 Thread 运行时身份、凭据、批准或工具实现 三类对象不能按文件扩展名区分语义，也不能合并成一个泛化的 PromptArtifact。它们会以不同方式 参与上下文和执行，因此需要独立 authority、校验与 snapshot。 以下概念不进入 artifact type： - Prompt：模型输入的底层信息形式。Zeta 的 provider-neutral 运行时输出是 ModelRequest， 不建立 Prompt 领域对象或 prompts/ 原生目录。 - Task：当前工作由 Turn 的用户输入和 durable Thread history 表达。重复工作流使用 Skill； 需要只允许用户主动调用时，使用 UserOnly Skill，而不是新增 Task/Preset 类型。 - Slash Command：调用已有对象或产品命令的 UI 投影，不是持久化定义格式。 - Agent runtime identity：Agent definition 是配置对象；执行分支仍由 Thread 标识，不能仅因 新增定义文件就在协议中发明第二套运行时身份。 2. 类型、来源和加载策略是三条独立轴 一个 artifact 的类型不能同时承担“从哪里来”和“何时加载”。目标模型必须分别表达： 轴 典型取值 决定什么 --- --- --- Artifact kind Instructions / Skills / Agents 对象结构、authority 与 runtime contribution Scope/source Built-in / User / Workspace / Plugin 生命周期、优先级、可写位置与失效方式 Provenance Zeta native / imported from external ecosystem 审计、冲突解释和重新导入来源 Activation policy 按对象类型定义的 named enum 自动加载、上下文匹配、用户调用或模型选择 “Imported”不是 User/Workspace 的替代 scope。导入后的对象仍属于明确的 User 或 Workspace authority，同时保留外部生态、来源位置 identity 和 digest 等 provenance。Plugin 贡献继续由 Plugin package 拥有，不能复制成普通 Workspace 文件后丢失 package/version 身份。 2.1 Instruction 加载策略 Instruction authority 的 canonical policy 使用穷尽枚举，不通过 applyTo 是否存在或 alwaysApply: bool 间接猜测： ``rust pub enum InstructionLoadPolicy Global, Contextual patterns: Vec<GlobPattern , OnDemand, ` - Global：在对应 scope 的每次 Agent interaction 中加载。 - Contextual：只有当前资源或工作上下文命中 pattern 时加载。 - OnDemand：只在用户或上层已验证引用显式选择时加载。 外部格式中的 applyTo、globs、alwaysApply 等字段由 zeta-agent-import adapter 转换成这三种 语义。applyTo: \"\" 仍是“恰好匹配所有文件的上下文规则”，不能偷偷改写成 Global；真正的 Global 必须由来源格式中明确等价的语义产生。 2.2 Skill 调用策略 Skill 的可发现性与调用方也使用 named policy，而不是两个容易产生非法组合的布尔字段： `rust pub enum SkillInvocationPolicy UserOrModel, UserOnly, ModelOnly, ` 保存的 review、fix-tests、create-component 等重复任务属于 UserOnly 或 UserOrModel Skill。 当 UserOnly Skill 被投影为 /review 时，Skill 仍是领域对象，/review 只是调用它的入口。 3. .zeta 是 Workspace 原生命名空间 项目级 Zeta 原生对象统一规划到小写 .zeta/。大写 .ZETA 不作为第二个兼容名称，避免在 大小写敏感文件系统上形成两个 authority。 `text <workspaceroot /.zeta/ ├── config.toml Current：严格只读 Workspace intent ├── instructions/ Current：有界发现；Global 自动注入 ├── skills/ Current：metadata-only Workspace Skill source └── agents/ Current：有界 definition catalog；尚不执行 ` 三个 artifact root 都使用固定原生布局：Instructions 和 Agents 是直接 .md 文件，Skills 是 <name /SKILL.md 目录。Workspace 激活时，App Server 只把可信 root 交给对应 runtime； zeta-skills-extension 拥有 Skill catalog 与 watcher refresh。模型调用不在 Core context assembly 中扫描 catalog：Global Instructions 使用冻结的 HarnessInstructions snapshot；已激活 Skill 由 extension 按 durable digest 精确加载正文。读取 catalog 本身仍不会激活 Skill 或执行 Agent definition。 Scope/source 物理 owner 是否经过 zeta-agent-import --- --- --- Built-in release/package resources ❌ 原生 authority 直接加载 User <profileroot 下的 Zeta-owned artifact root（Proposed） ❌ 原生 authority 直接加载 Workspace <workspaceroot /.zeta/ instructions,skills,agents （Current catalog slice） ❌ 原生 authority 直接加载 Plugin Plugin package contribution ❌ 由 Plugin snapshot 交给目标 authority External ecosystem .codex、.agents、.claude 等已知布局 ✅ 只经 zeta-agent-import <profileroot 本身已经是 Zeta 的用户级命名空间，因此不再嵌套一个 /.zeta 兼容目录。 Workspace .zeta 继续作为受保护 metadata；普通文件搜索、Agent 工具写入和外部 source registration 不能把它当作任意内容目录。 原生 loader 只读取自己的 canonical roots。它不得自动扫描 AGENTS.md、.codex/、.agents/、 .claude/、.github/ 或其他产品目录；否则 compatibility policy 会散落到三个 authority 中， 外部格式也会反向定义 Zeta schema。 4. zeta-agent-import 是外部反腐化层 zeta-agent-import 的“Agent”表示外部 Agent 生态，不表示它只导入 Agents artifact。它统一处理 Codex、Claude 以及未来明确支持的其他生态中的 Instructions、Skills、Agents 和设置类内容。 `mermaid flowchart LR external \"外部 Agent 生态<br/ 已知目录与格式\" -- inspect \"zeta-agent-import<br/ discover / inspect / parse / normalize\" inspect -- preview \"App Server import coordinator<br/ preview / conflict / user confirmation\" preview -- instruction \"Instruction authority\" preview -- skill \"Skill authority / Config source\" preview -- agent \"Agent definition authority\" preview -- config \"MCP / Plugin / Hook / other Config authorities\" instruction -- runtime \"immutable activation snapshots\" skill -- runtime agent -- runtime ` 责任 zeta-agent-import App Server coordinator 目标 authority --- --- --- --- 已知外部路径与敏感排除 ✅ ❌ ❌ source-specific bounded parser ✅（Proposed） ❌ ❌ normalized preview fragment 与 provenance ✅（Proposed） 组合 最终复核 用户选择、冲突预览与 apply orchestration ❌ ✅（Proposed） 提供 prepare/publish contract Zeta canonical schema 与领域校验 ❌ ❌ ✅ .zeta 原生发现与加载 ❌ 协调 snapshot ✅ 持久化、enablement 与 runtime activation ❌ 调用 ✅ parser 输出必须是目标明确、可审查的 typed fragment，例如 Instruction、Skill source、Agent definition 或 Config mutation fragment；不能输出一段“以后再解释”的原始 JSON/Markdown。外部字段 无法确定性映射时必须标记 unsupported，不能 raw passthrough，也不能让 zeta-agent-import 依赖 zeta-config、Core 或具体产品 UI。 当前 zeta-agent-import 只完成 metadata-only AgentPathInspection，尚未读取正文或生成 normalized fragment。上图除 inspection 之外的 import apply 路径均为 Proposed。 5. Import 与来源注册不等价 操作 权威正文在哪里 外部变化是否自动影响 Zeta 撤销语义 适合对象 --- --- --- --- --- Import Zeta 原生 authority ❌，再次导入需显式触发 删除/回滚 Zeta artifact 或 import receipt Instructions、Agents、需要独立管理的 Skills Register source 外部只读 root ✅，刷新后产生新 catalog generation 禁用或移除 source 当前已支持的 standalone Skill source 两条路径可以复用相同的外部 discovery、containment 和 parser，但不能共享生命周期决定： - Import 不授予来源目录的持续文件访问；apply 前必须重读并校验 identity/digest。 - Source registration 只保存窄 root 与 provenance，不复制整个外部配置目录。 - 注册 Skill source 不允许顺便激活其中的 Instructions、Agents、Hooks、MCP 或执行规则。 - zeta-add-dir 是第三条独立路径：它管理目录访问生命周期，不等于 Import 或持久 source registration。 首个完整 apply 可以只支持 Config 内部的原子 batch，但不能假装同时原子提交尚不存在的 Instruction/Agent authority。在跨 authority prepare/publish 可用前，对应条目必须保持 unsupported。 6. 运行时只消费冻结快照 `text 原生与已导入来源 → 各 artifact authority 发现、校验、解析 → Config/enablement 与作用域解析 → Instruction / Skill / Agent immutable snapshots → ContextPlan / ContextAssembler → ModelRequest ` Core 不读取 customization 文件，也不在组装模型请求时扫描目录。每次 model invocation 消费已经 冻结的 snapshot 或由 extension 根据 durable provenance 贡献的精确 fragment；文件或配置变化只在 下一个安全点生效，且不能替换 in-flight Skill 的 digest。最终输入可以包含系统内置指令、作用域 Instructions、Agent references、已激活 Skill 内容、用户消息和 Tool results，但它仍是 ModelRequest，不是一个需要持久化的 Prompt artifact。 Slash Command catalog 同样只做调用投影：产品命令绑定到 product action，用户可调用 Skill 绑定到 稳定 SkillRef。Slash Command 名称、补全和展示状态不能代替目标对象的 identity、权限或 activation validation。 7. 当前状态与实施顺序 能力 状态 实现证据或前置条件 --- --- --- .zeta/config.toml Workspace intent 已实现 zeta-config / App Server local composition Skill built-in/user/Workspace catalog 与 enablement 部分具备 zeta-skills、SkillRuntime::composesources 与 skills.md Skill activation snapshot 与通用 context injection 已实现 validated SkillRef、正文加载、safe-point freezing 与 extension contributors Codex/Claude known-path inspection 已实现 zeta-agent-import::inspectagentpaths Workspace Instructions authority 部分具备 zeta-instructions + WorkspaceCustomizations；Global 注入已实现，其他选择策略未实现 Workspace Agents authority 部分具备 zeta-agents + WorkspaceCustomizations；catalog/refresh 已实现，list/selection/runtime 未实现 .zeta/ instructions,skills,agents loader 已实现 固定原生 roots、有界校验、Workspace activation 与 watcher refresh External parser、preview 与 apply 尚未完成 typed fragments、digest、wire contract、transaction/receipt Skill → Slash Command 动态投影 已实现 TUI/Desktop /name 绑定 stable SkillRef；/skills 只管理 实施顺序： 1. 固定本文的三类对象、命名空间和 import boundary，代码与文档不再新增 Prompt/Task artifact。 2. 已完成 Workspace 三类 catalog、Global Instruction safe-point injection 与 watcher refresh。 3. 下一步实现 Contextual/OnDemand Instruction 解析与自动 Skill selection；Skill 显式 activation、 Workspace source、用户可调用投影和通用 context injection 已接通。 4. Agent definition catalog 保持只读，直到 list API、cross-authority reference resolution 和 multi-agent runtime 同时具备后再开放执行选择。 5. 为 zeta-agent-import 增加 source-specific bounded parsers 和 typed preview fragments；App Server 只对已经具备 target authority 的条目开放 apply。 8. 长期不变量 - Zeta 原生 Agent customization 只有 Instructions、Skills、Agents 三类领域对象。 - Prompt、Task、Preset 和 Slash Command 不成为第四种 artifact；重复任务使用 Skill。 - Workspace 原生目录只使用小写 .zeta，不维护 .ZETA alias。 - 外部生态格式只由 zeta-agent-import 理解；原生 authority 不扫描兼容目录。 - 类型、scope/source、provenance 与 activation policy 分开建模。 - 导入成功不授予工具、脚本、网络、凭据、沙箱绕过或长期执行批准。 - Preview 不是 authorization；apply 前必须重读并验证 source identity/digest。 - 原生 loader、Import、source registration 与 add-dir 保持四条不同生命周期路径。 - ContextAssembler 只消费冻结 snapshot 并产生 ModelRequest`，不拥有 artifact discovery。"
  },
  {
    "slug": "exec",
    "sourcePath": "docs/exec.md",
    "title": "无界面 Agent 执行",
    "description": "zeta-exec 的长期角色是无交互 Agent runner。它把一次本地 CLI 请求或远程调度 Job 转换为 canonical Session/Thread/Turn command，持续消费 App Server result/update，并输出机器可处理的 终态、事件和退出状态。",
    "group": "Agent 与运行时",
    "markdown": "> 目标物理位置：`zeta-rs/exec/`  \n> 当前状态：阶段 1–2 已实现；可靠自动化、远程 worker 与远程执行环境仍是 Proposed\n> 当前 crate 实现契约：[`zeta-rs/exec/README.md`](../zeta-rs/exec/README.md)\n> App Server Client：[`app-server-client.md`](app-server-client.md)  \n> App Server contract：[`zeta-app-server-api.md`](zeta-app-server-api.md)  \n> Canonical 产品模型：[`protocol.md`](protocol.md)\n\n## 快速理解\n\n`zeta-exec` 的长期角色是无交互 Agent runner。它把一次本地 CLI 请求或远程调度 Job 转换为\ncanonical Session/Thread/Turn command，持续消费 App Server result/update，并输出机器可处理的\n终态、事件和退出状态。\n\n它不是 shell command executor，也不是第二个 Agent runtime：\n\n```text\nlocal CLI / remote scheduler\n            │\n            ▼\n        zeta-exec\n            │ typed App Server request/update\n            ▼\n zeta-app-server-client\n            ▼\n      zeta-app-server\n            ▼\n         zeta-core\n            │ tool execution port\n            ▼\n zeta-tool-executor / future zeta-exec-server\n```\n\n长期必须区分：\n\n- `zeta-exec`：运行完整的 headless Agent Job；\n- `zeta-tool-executor`：在本机执行一个经过 approval/sandbox 的 process；\n- `zeta-exec-server`：未来把 process/filesystem execution 暴露给远程环境；\n- scheduler protocol：提交、租约、取消和观察远程 Agent Job。\n\n这四者不能共享一个含义含糊的 `exec` API。\n\n| 用户或调度器想做什么 | 正确入口 | 当前状态 |\n| --- | --- | --- |\n| 无交互地运行完整 Agent 任务 | `zeta-exec` | 本地 run-once 已实现 |\n| 执行一次已经批准的本地命令 | `zeta-tool-executor` | 已实现并与 Agent runner 分离 |\n| 在远程机器执行进程或文件操作 | 远程执行服务 | 潜在方向 |\n| 排队、租约和取消远程 Agent 任务 | 调度协议 | 潜在方向 |\n\n## 2. Codex 参考与 Zeta 取舍\n\n本方案参考本地 `../codex` workspace 的三层边界：\n\n- `codex-rs/exec/` 是非交互 Agent 产品入口；\n- `codex-rs/app-server-client/` 被 exec 与 TUI 共享，集中处理 embedded App Server startup、\n  initialize、typed request/event channel、backpressure 和 bounded shutdown；\n- `codex-rs/exec-server/` 单独负责 process、PTY、filesystem 和远程 execution environment。\n\nZeta 应采用：\n\n- exec 与 TUI 共用 App Server Client；\n- embedded hot path 使用 typed channel；\n- request completion 与 server event stream 分离；\n- cloneable request handle；\n- bounded queue、lag signal 与显式 shutdown；\n- headless exec 只通过 App Server 创建/恢复 Thread、启动/中断 Turn；\n- remote Agent scheduling 与 remote process execution 分层。\n\nZeta 不照搬：\n\n- exec 直接依赖 Core、rollout 或 provider 私有类型；\n- caller 自由选择任意 `request_typed<T>` result 类型；\n- 一个数千行 client/exec module；\n- remote event 使用 unbounded queue；\n- 复制一套与 canonical ThreadItem 平行的内部领域状态机；\n- 按具体 notification 名字硬编码永久的 lossless/best-effort 列表。\n\nZeta 已有 durable aggregate sequence、snapshot + gap subscribe 和 typed command replay，应利用\n这些契约实现 resync，而不是依赖“所有实时 event 永不丢失”。\n\n## 3. 当前仓库偏差与迁移\n\n命名迁移已经完成：\n\n```text\nformer zeta-rs/exec\n  → zeta-rs/tool-executor\n  → crate zeta-tool-executor\n\ncurrent zeta-rs/exec\n  → crate/binary zeta-exec\n  → headless Agent runner\n```\n\n`zeta-tool-executor` 继续拥有 `CommandRequest`、process capture、timeout、sandbox 与 approval start\ngate；`zeta-exec` 当前拥有 new/resume/fork、Turn start/interrupt、事件输出与终态映射。Sandbox 的\n共享 policy、macOS backend、Linux Bubblewrap 和 Windows AppContainer crate 边界见\n[`sandboxing.md`](sandboxing.md)。\n\n若后续需要远程 process/filesystem execution：\n\n```text\nzeta-tool-executor\n  → local backend of zeta-exec-server\n\nzeta-exec-server-protocol\n  → process / PTY / filesystem / HTTP execution contract\n```\n\n不能直接把当前 `ToolExecutor` 扩张为远程 Agent scheduler，否则 scheduler job、Agent lifecycle、\nprocess lifecycle 和 sandbox authority 会混在同一 crate。\n\n## 4. 职责与非职责\n\n### 4.1 `zeta-exec` 拥有\n\n- 一次 headless run 的输入、工作目录和输出选项；\n- create/resume/fork 哪种产品入口意图；\n- 使用 `zeta-app-server-client` 启动或连接 App Server；\n- 创建/选择 Session 与 Thread，启动一个 Turn；\n- 关联当前 Job、Session、Thread 与 Turn；\n- 同时等待 request completion、App Server event、cancel 和 shutdown；\n- 把 canonical update 映射为 human/JSONL/scheduler event；\n- headless approval 行为；\n- Ctrl-C 与 scheduler cancellation 到 `session/request` `InterruptTurn` 的映射；\n- terminal Turn status 到 `ExecOutcome`、退出码和 scheduler status 的映射；\n- run-once 与长期 worker 的应用级生命周期。\n\n### 4.2 `zeta-exec` 不拥有\n\n- Session/Thread/Turn reducer；\n- model loop、tool loop 或 context assembly；\n- command receipt、writer lease 或 rollout recovery；\n- App Server initialize、channel wiring 和 connection shutdown 的具体实现；\n- process spawn、PTY、filesystem sandbox 或 network proxy；\n- scheduler 的全局 queue、placement、quota 或 billing authority；\n- 从日志、stderr 或人类文本推断 Turn 终态；\n- 直接读取 Core、store 或 rollout 作为 App Server 旁路。\n\n## 5. 本地单次运行\n\n当前支持一次进程运行一个 Agent Job：\n\n```text\nparse ExecRunRequest\n  → start embedded AppServerSession\n  → create/read Session\n  → create/read Thread\n  → subscribe Thread\n  → start Turn\n  → consume result + AppServerEvents\n  → observe terminal durable update\n  → unsubscribe\n  → shutdown AppServerSession\n  → emit ExecOutcome\n```\n\n建议状态机：\n\n```text\nPreparing\n  → StartingAppServer\n  → StartingThread\n  → Running\n  → Cancelling\n  → Terminal\n  → Closing\n  → Closed\n```\n\n任意阶段失败都必须产生明确 `ExecOutcome`。Connection EOF 不是 Turn failure；若终态未知，\nexec 应重新连接/read/subscribe 或返回 `OutcomeUnknown`，不能伪造 `Failed`。\n\n## 6. 公共接口\n\n当前 library API 以 typed request、sink 和 cancellation signal 为边界：\n\n```rust\nimpl ExecRunner {\n    pub fn run(\n        &self,\n        request: ExecRunRequest,\n        output: impl ExecEventSink,\n        cancellation: &impl ExecCancellation,\n    ) -> Result<ExecOutcome, ExecError>;\n}\n```\n\n输入使用 enum，而不是多个 bool：\n\n```rust\npub struct ExecRunRequest {\n    pub run_id: ExecRunId,\n    pub entry: ExecEntry,\n    pub approval: HeadlessApprovalMode,\n}\n\npub enum ExecEntry {\n    New { title: String, input: Vec<InputItem> },\n    Resume { session_id: SessionId, thread_id: ThreadId, input: Vec<InputItem> },\n    Fork { session_id: SessionId, parent_thread_id: ThreadId, title: String, input: Vec<InputItem> },\n}\n\npub enum AppServerTarget {\n    Embedded(EmbeddedAppServerOptions),\n}\n```\n\n`AppServerTarget::Remote(RemoteAppServerOptions)` 仍是 Proposed。它将表示连接相同 App Server\ncontract，不表示 scheduler job protocol，也不表示 remote process executor。\n\n`zeta-cli` 负责参数和帮助；`zeta-exec` 负责这些参数解析后的运行语义。\n\n## 7. 输出契约\n\nHuman 输出可以演进；JSONL 与 scheduler event 是机器契约，必须 versioned、typed 且与 stdout\n诊断隔离。\n\n```rust\npub struct ExecEvent {\n    pub schema_version: u32,\n    pub run_id: ExecRunId,\n    pub event: ExecEventKind,\n}\n\npub enum ExecEventKind {\n    RunStarted {\n        origin: ExecOrigin,\n        session_id: SessionId,\n        thread_id: ThreadId,\n    },\n    TurnStarted { session_id: SessionId, thread_id: ThreadId, turn_id: TurnId },\n    ThreadUpdated { update: Box<ThreadUpdateEnvelope> },\n    RunCompleted { outcome: ExecOutcome },\n}\n\npub enum ExecOrigin {\n    Local,\n}\n```\n\n当前 schema version 是 1。Item-specific event、warning 与\n`Scheduled { job_id, attempt_id }` origin 属于阶段 3/4 的扩展点；当前先机械传递 canonical\n`ThreadUpdateEnvelope`，避免过早复制一套 item lifecycle。\n\n`ExecEvent` 是外部展示/automation envelope，不是新的 authoritative domain model：\n\n- ID、status、item payload 优先机械复用 protocol；\n- 展示所需的 delta 可以映射，但不能改变 durable 事实；\n- terminal event 必须由 canonical Turn terminal status 产生；\n- JSONL stdout 每行一个完整 event；\n- stderr 只用于诊断；\n- human 模式默认只把最终 Agent message 写入 stdout，进度写 stderr；\n- event schema 变更必须有 fixture 与 compatibility policy。\n\n## 8. 无界面批准与服务端请求\n\n无交互运行不能等待不存在的用户界面。\n\n```rust\npub enum HeadlessApprovalMode {\n    DenyInteractiveRequests,\n    AutomaticReview,\n    BypassPermissions,\n}\n```\n\n当前规则：\n\n- `DenyInteractiveRequests` 在 Turn 进入 approval/user-input/capability wait 时发送 typed\n  `InterruptTurn`，观察 canonical `Interrupted` 后返回 `RequiresInteraction`；\n- `AutomaticReview` 映射为 App Server `AutoReview`，但无法呈现的 user-input/capability 仍会中断；\n- `BypassPermissions` 只由显式 `--dangerously-bypass-permissions` 选择，绝不是 worker 默认值；\n- Ctrl-C 和 turn timeout 都先发送 typed interrupt，再等待 bounded terminal observation；\n- 当前没有 remote reviewer channel，不能把交互请求悬挂给不存在的 UI。\n\n远程 reviewer 的 Proposed 规则：\n\n- remote scheduler 支持 reviewer 时，请求必须携带 Job/Attempt/Thread/Turn/Tool identity；\n- reviewer response 必须防重放并校验 action digest；\n- approval timeout、scheduler disconnect 和 worker shutdown 都必须结束 pending server request；\n\n因此 App Server Client 的长期 event stream 需要同时支持：\n\n- `ServerNotification`；\n- `ServerRequest`；\n- `Lagged/Desynced`；\n- `ConnectionClosed`。\n\n## 9. 远程调度\n\n### 9.1 两种运行模式\n\n```text\nRunOnce\n  = 一个 zeta-exec process\n  = 一个 App Server session\n  = 一个前台 Agent Job\n\nWorker\n  = 一个长期 zeta-exec worker\n  = 一个或多个隔离的 App Server session\n  = 多个有 lease 的 Agent Job\n```\n\nWorker mode 不能简单地在循环中调用 CLI `main`。它需要明确：\n\n- worker registration；\n- capability/host/workspace advertisement；\n- job lease 与续租；\n- attempt fencing；\n- cancellation；\n- event ack/cursor；\n- reconnect 与 replay；\n- draining shutdown；\n- tenant、credential、workspace 和 state-root isolation。\n\n### 9.2 身份\n\n以下身份不能混用：\n\n| Identity | Owner | 用途 |\n| --- | --- | --- |\n| `ExecRunId` | zeta-exec | 当前 runner 内的一次运行 |\n| `JobId` | scheduler | 一次逻辑远程任务 |\n| `AttemptId` | scheduler | 一次 placement/execution attempt |\n| lease/fencing token | scheduler | 阻止过期 worker 继续提交 |\n| `CommandId` | App Server caller | durable command 幂等与 replay |\n| Session/Thread/Turn ID | App Server domain | canonical Agent lifecycle |\n| JSON-RPC request ID | App Server Client | 当前 connection 的 result pairing |\n| durable sequence | aggregate | update replay 与 resync |\n| scheduler event cursor | scheduler protocol | scheduler-side delivery ack |\n\n推荐保存稳定映射：\n\n```text\nJobId + AttemptId\n  ↔ SessionId + ThreadId + TurnId\n  + last durable sequence\n  + scheduler event cursor\n```\n\nScheduler receipt 不是 App Server command receipt。两者可以在同一 Job flow 中关联，但不得放进\n同一个 ID 或假设原子跨系统事务。\n\n### 9.3 远程 Job 流程\n\n```text\nscheduler assigns Job(attempt, lease)\n  → worker validates isolation/capability\n  → zeta-exec starts/selects AppServerSession\n  → stable CommandId creates/resumes Session/Thread/Turn\n  → worker streams mapped ExecEvent\n  → scheduler acks event cursor\n  → worker observes terminal durable update\n  → worker commits terminal Job result with fencing token\n```\n\n断线恢复时：\n\n- scheduler 可以重新投递同一 `JobId` 的新 `AttemptId`；\n- worker 使用 mapping 与 App Server read/subscribe 判断原 Turn 是否仍运行或已终态；\n- 相同逻辑 App Server command 重试复用原 `CommandId + exact payload`；\n- durable gap 通过 snapshot + committed gap 恢复；\n- transient delta 丢失只影响低延迟展示，不能影响 terminal Job result；\n- 过期 lease 的 worker 不得提交 scheduler terminal result。\n\n## 10. 远程 Agent 调度与远程执行平面\n\n远程调度完整 Agent Job：\n\n```text\nscheduler → zeta-exec worker → App Server → Core\n```\n\n远程执行某个 tool process/filesystem operation：\n\n```text\nCore tool port → zeta-exec-server client → remote zeta-exec-server\n```\n\n二者的 security 和 lifecycle 不同：\n\n| Plane | Authority | Stable unit | Disconnect behavior |\n| --- | --- | --- | --- |\n| Agent scheduling | scheduler + App Server | Job/Session/Thread/Turn | durable recovery/reschedule |\n| Process execution | executor connection | process/filesystem request | terminate, resume or report unknown by exec protocol |\n\n`zeta-exec-server` 不得接受 Agent `session/request`，`zeta-exec` scheduler adapter 不得提供裸\n`process/start` 旁路。\n\n## 11. 工作进程隔离与并发\n\n第一版 remote worker 应保守选择：\n\n- 一个 Job 一个 workspace lease；\n- 一个 tenant/credential scope 一个 App Server authority；\n- 同一 Thread 只有一个 active writer；\n- worker 只在 capability、sandbox 和 state-root scope 完全相容时复用 App Server session；\n- 不在不同用户间共享 Resource ownership、subscription 或 transient cursor；\n- worker restart 依赖 durable store 恢复，不依赖内存 pending table。\n\n并发可以随 App Server per-Thread serialization 演进，但 scheduler placement 不能把“不同\nThread 可并行”误解为“不同 tenant 可以无隔离共享进程”。\n\n## 12. 背压与重放\n\n所有 queue 有界：\n\n- App Server Client request channel；\n- App Server event channel；\n- exec output sink；\n- remote scheduler outbound buffer。\n\n处理规则：\n\n- durable update 不静默丢弃；\n- event queue 满时发出 `Lagged/Desynced` 并按 aggregate sequence resubscribe；\n- transient delta 可以按明确 policy 丢弃或合并；\n- terminal update 在成功交付或完成 snapshot resync 前不能宣称 Job 完成；\n- scheduler event 使用独立 cursor/ack，不能复用 Thread durable sequence；\n- scheduler 长时间不 ack 时施加 backpressure、持久化 spool 或终止 attempt，不能无限占用内存。\n\n## 13. 取消与关闭\n\nRunOnce Ctrl-C：\n\n```text\nsignal\n  → session/request { type: interruptTurn } with stable CommandId\n  → wait terminal update within deadline\n  → unsubscribe\n  → AppServerSession.shutdown()\n  → interrupted ExecOutcome\n```\n\nWorker Job cancellation：\n\n```text\nscheduler cancel(JobId, AttemptId, fencing token)\n  → validate current attempt\n  → session/request { type: interruptTurn }\n  → publish terminal Job event\n  → keep worker/App Server alive for other jobs\n```\n\nWorker shutdown 应有具名模式：\n\n```rust\npub enum WorkerShutdownMode {\n    Drain,\n    InterruptActiveJobs,\n}\n```\n\n`Drop` 只做 best-effort cleanup；正常路径必须等待 job terminal handling 与 App Server Client\nshutdown。\n\n## 14. 目标 crate 与依赖\n\n```text\nzeta-cli\n├─► zeta-tui ─────┐\n└─► zeta-exec ────┤\n                  ▼\n       zeta-app-server-client\n                  ▼\n          zeta-app-server\n                  ▼\n              zeta-core\n                  │\n                  ▼\n        zeta-shell-command\n                  ▼\n        zeta-tool-executor\n```\n\n未来可增加：\n\n```text\nzeta-exec-server-protocol\nzeta-exec-server\nzeta-scheduler-protocol\n```\n\n`zeta-exec` 可依赖 app-server-client、app-server-protocol、protocol 和窄 CLI/output utility。\n它不依赖 Core、store、rollout、model provider、sandboxing 或 tool executor。\n\n## 15. 实施阶段\n\n### 阶段 1：消除命名冲突（当前状态）\n\n- 已将原 process `zeta-exec` 迁移为 `zeta-tool-executor`；\n- 已更新直接依赖与 tools、skills、plugins、MCP 文档中的 ownership；\n- `zeta-exec` crate 名称现由 headless runner 使用。\n\n### 阶段 2：本地无界面纵向切片（当前状态）\n\n- 复用 owned App Server Client request/event session；\n- 实现 new/resume/fork、Turn start/interrupt；\n- human/JSONL output；\n- terminal status 与退出码；\n- 显式 shutdown。\n\n### 阶段 3：可靠自动化（计划）\n\n- 稳定 ExecEvent schema；\n- output schema、last-message file、stdin 与 workspace input；\n- approval delegation；\n- reconnect/read/subscribe resync；\n- integration fixtures。\n\n### 阶段 4：远程工作进程（计划）\n\n- scheduler protocol；\n- Job/Attempt/lease/fencing；\n- worker registration、heartbeat 和 draining；\n- event cursor/ack 与 reconnect；\n- tenant/workspace isolation。\n\n### 阶段 5：远程执行环境（潜在方向）\n\n- 独立 exec-server protocol；\n- process/PTY/filesystem handlers；\n- authenticated transport；\n- 与 Agent scheduler 分开的 reliability 与 security model。\n\n## 16. 验证要求\n\n阶段 1–2 已由 crate/CLI tests 覆盖：\n\n- exec 与 TUI 使用同一个 App Server Client startup/connection contract；\n- exec 不直接依赖 Core、store、provider、sandbox 或 tool executor；\n- start/resume/interrupt 只通过 typed App Server API；\n- request result 与 notification 可并行消费；\n- JSONL stdout 永远是合法机器事件；\n- terminal outcome 只来自 canonical Turn status；\n- connection 断开不会伪造 Turn failure；\n- Ctrl-C 发送 typed `session/request` `InterruptTurn`；\n- headless approval 不会永久等待不存在的 UI；\n\n阶段 3–5 尚需满足：\n\n- durable gap 可在断线后通过 reconnect/read/subscribe 恢复；\n- remote cancel 同样发送 typed `session/request` `InterruptTurn`；\n- Job/Attempt/Command/request/sequence identity 有独立 contract tests；\n- 过期 lease 无法提交 scheduler terminal result；\n- worker shutdown 不泄漏 Job、App Server connection 或后台 task；\n- remote Agent scheduling 与 remote process execution 没有协议旁路。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-codex-参考与-zeta-取舍",
        "title": "2. Codex 参考与 Zeta 取舍"
      },
      {
        "depth": 2,
        "id": "3-当前仓库偏差与迁移",
        "title": "3. 当前仓库偏差与迁移"
      },
      {
        "depth": 2,
        "id": "4-职责与非职责",
        "title": "4. 职责与非职责"
      },
      {
        "depth": 3,
        "id": "41-zeta-exec-拥有",
        "title": "4.1 zeta-exec 拥有"
      },
      {
        "depth": 3,
        "id": "42-zeta-exec-不拥有",
        "title": "4.2 zeta-exec 不拥有"
      },
      {
        "depth": 2,
        "id": "5-本地单次运行",
        "title": "5. 本地单次运行"
      },
      {
        "depth": 2,
        "id": "6-公共接口",
        "title": "6. 公共接口"
      },
      {
        "depth": 2,
        "id": "7-输出契约",
        "title": "7. 输出契约"
      },
      {
        "depth": 2,
        "id": "8-无界面批准与服务端请求",
        "title": "8. 无界面批准与服务端请求"
      },
      {
        "depth": 2,
        "id": "9-远程调度",
        "title": "9. 远程调度"
      },
      {
        "depth": 3,
        "id": "91-两种运行模式",
        "title": "9.1 两种运行模式"
      },
      {
        "depth": 3,
        "id": "92-身份",
        "title": "9.2 身份"
      },
      {
        "depth": 3,
        "id": "93-远程-job-流程",
        "title": "9.3 远程 Job 流程"
      },
      {
        "depth": 2,
        "id": "10-远程-agent-调度与远程执行平面",
        "title": "10. 远程 Agent 调度与远程执行平面"
      },
      {
        "depth": 2,
        "id": "11-工作进程隔离与并发",
        "title": "11. 工作进程隔离与并发"
      },
      {
        "depth": 2,
        "id": "12-背压与重放",
        "title": "12. 背压与重放"
      },
      {
        "depth": 2,
        "id": "13-取消与关闭",
        "title": "13. 取消与关闭"
      },
      {
        "depth": 2,
        "id": "14-目标-crate-与依赖",
        "title": "14. 目标 crate 与依赖"
      },
      {
        "depth": 2,
        "id": "15-实施阶段",
        "title": "15. 实施阶段"
      },
      {
        "depth": 3,
        "id": "阶段-1消除命名冲突当前状态",
        "title": "阶段 1：消除命名冲突（当前状态）"
      },
      {
        "depth": 3,
        "id": "阶段-2本地无界面纵向切片当前状态",
        "title": "阶段 2：本地无界面纵向切片（当前状态）"
      },
      {
        "depth": 3,
        "id": "阶段-3可靠自动化计划",
        "title": "阶段 3：可靠自动化（计划）"
      },
      {
        "depth": 3,
        "id": "阶段-4远程工作进程计划",
        "title": "阶段 4：远程工作进程（计划）"
      },
      {
        "depth": 3,
        "id": "阶段-5远程执行环境潜在方向",
        "title": "阶段 5：远程执行环境（潜在方向）"
      },
      {
        "depth": 2,
        "id": "16-验证要求",
        "title": "16. 验证要求"
      }
    ],
    "searchText": " 目标物理位置：zeta-rs/exec/ 当前状态：阶段 1–2 已实现；可靠自动化、远程 worker 与远程执行环境仍是 Proposed 当前 crate 实现契约：zeta-rs/exec/README.md App Server Client：app-server-client.md App Server contract：zeta-app-server-api.md Canonical 产品模型：protocol.md 快速理解 zeta-exec 的长期角色是无交互 Agent runner。它把一次本地 CLI 请求或远程调度 Job 转换为 canonical Session/Thread/Turn command，持续消费 App Server result/update，并输出机器可处理的 终态、事件和退出状态。 它不是 shell command executor，也不是第二个 Agent runtime： ``text local CLI / remote scheduler │ ▼ zeta-exec │ typed App Server request/update ▼ zeta-app-server-client ▼ zeta-app-server ▼ zeta-core │ tool execution port ▼ zeta-tool-executor / future zeta-exec-server ` 长期必须区分： - zeta-exec：运行完整的 headless Agent Job； - zeta-tool-executor：在本机执行一个经过 approval/sandbox 的 process； - zeta-exec-server：未来把 process/filesystem execution 暴露给远程环境； - scheduler protocol：提交、租约、取消和观察远程 Agent Job。 这四者不能共享一个含义含糊的 exec API。 用户或调度器想做什么 正确入口 当前状态 --- --- --- 无交互地运行完整 Agent 任务 zeta-exec 本地 run-once 已实现 执行一次已经批准的本地命令 zeta-tool-executor 已实现并与 Agent runner 分离 在远程机器执行进程或文件操作 远程执行服务 潜在方向 排队、租约和取消远程 Agent 任务 调度协议 潜在方向 2. Codex 参考与 Zeta 取舍 本方案参考本地 ../codex workspace 的三层边界： - codex-rs/exec/ 是非交互 Agent 产品入口； - codex-rs/app-server-client/ 被 exec 与 TUI 共享，集中处理 embedded App Server startup、 initialize、typed request/event channel、backpressure 和 bounded shutdown； - codex-rs/exec-server/ 单独负责 process、PTY、filesystem 和远程 execution environment。 Zeta 应采用： - exec 与 TUI 共用 App Server Client； - embedded hot path 使用 typed channel； - request completion 与 server event stream 分离； - cloneable request handle； - bounded queue、lag signal 与显式 shutdown； - headless exec 只通过 App Server 创建/恢复 Thread、启动/中断 Turn； - remote Agent scheduling 与 remote process execution 分层。 Zeta 不照搬： - exec 直接依赖 Core、rollout 或 provider 私有类型； - caller 自由选择任意 requesttyped<T result 类型； - 一个数千行 client/exec module； - remote event 使用 unbounded queue； - 复制一套与 canonical ThreadItem 平行的内部领域状态机； - 按具体 notification 名字硬编码永久的 lossless/best-effort 列表。 Zeta 已有 durable aggregate sequence、snapshot + gap subscribe 和 typed command replay，应利用 这些契约实现 resync，而不是依赖“所有实时 event 永不丢失”。 3. 当前仓库偏差与迁移 命名迁移已经完成： `text former zeta-rs/exec → zeta-rs/tool-executor → crate zeta-tool-executor current zeta-rs/exec → crate/binary zeta-exec → headless Agent runner ` zeta-tool-executor 继续拥有 CommandRequest、process capture、timeout、sandbox 与 approval start gate；zeta-exec 当前拥有 new/resume/fork、Turn start/interrupt、事件输出与终态映射。Sandbox 的 共享 policy、macOS backend、Linux Bubblewrap 和 Windows AppContainer crate 边界见 sandboxing.md。 若后续需要远程 process/filesystem execution： `text zeta-tool-executor → local backend of zeta-exec-server zeta-exec-server-protocol → process / PTY / filesystem / HTTP execution contract ` 不能直接把当前 ToolExecutor 扩张为远程 Agent scheduler，否则 scheduler job、Agent lifecycle、 process lifecycle 和 sandbox authority 会混在同一 crate。 4. 职责与非职责 4.1 zeta-exec 拥有 - 一次 headless run 的输入、工作目录和输出选项； - create/resume/fork 哪种产品入口意图； - 使用 zeta-app-server-client 启动或连接 App Server； - 创建/选择 Session 与 Thread，启动一个 Turn； - 关联当前 Job、Session、Thread 与 Turn； - 同时等待 request completion、App Server event、cancel 和 shutdown； - 把 canonical update 映射为 human/JSONL/scheduler event； - headless approval 行为； - Ctrl-C 与 scheduler cancellation 到 session/request InterruptTurn 的映射； - terminal Turn status 到 ExecOutcome、退出码和 scheduler status 的映射； - run-once 与长期 worker 的应用级生命周期。 4.2 zeta-exec 不拥有 - Session/Thread/Turn reducer； - model loop、tool loop 或 context assembly； - command receipt、writer lease 或 rollout recovery； - App Server initialize、channel wiring 和 connection shutdown 的具体实现； - process spawn、PTY、filesystem sandbox 或 network proxy； - scheduler 的全局 queue、placement、quota 或 billing authority； - 从日志、stderr 或人类文本推断 Turn 终态； - 直接读取 Core、store 或 rollout 作为 App Server 旁路。 5. 本地单次运行 当前支持一次进程运行一个 Agent Job： `text parse ExecRunRequest → start embedded AppServerSession → create/read Session → create/read Thread → subscribe Thread → start Turn → consume result + AppServerEvents → observe terminal durable update → unsubscribe → shutdown AppServerSession → emit ExecOutcome ` 建议状态机： `text Preparing → StartingAppServer → StartingThread → Running → Cancelling → Terminal → Closing → Closed ` 任意阶段失败都必须产生明确 ExecOutcome。Connection EOF 不是 Turn failure；若终态未知， exec 应重新连接/read/subscribe 或返回 OutcomeUnknown，不能伪造 Failed。 6. 公共接口 当前 library API 以 typed request、sink 和 cancellation signal 为边界： `rust impl ExecRunner pub fn run &self, request: ExecRunRequest, output: impl ExecEventSink, cancellation: &impl ExecCancellation, - Result<ExecOutcome, ExecError ; ` 输入使用 enum，而不是多个 bool： `rust pub struct ExecRunRequest pub runid: ExecRunId, pub entry: ExecEntry, pub approval: HeadlessApprovalMode, pub enum ExecEntry New title: String, input: Vec<InputItem , Resume sessionid: SessionId, threadid: ThreadId, input: Vec<InputItem , Fork sessionid: SessionId, parentthreadid: ThreadId, title: String, input: Vec<InputItem , pub enum AppServerTarget Embedded EmbeddedAppServerOptions , ` AppServerTarget::Remote RemoteAppServerOptions 仍是 Proposed。它将表示连接相同 App Server contract，不表示 scheduler job protocol，也不表示 remote process executor。 zeta-cli 负责参数和帮助；zeta-exec 负责这些参数解析后的运行语义。 7. 输出契约 Human 输出可以演进；JSONL 与 scheduler event 是机器契约，必须 versioned、typed 且与 stdout 诊断隔离。 `rust pub struct ExecEvent pub schemaversion: u32, pub runid: ExecRunId, pub event: ExecEventKind, pub enum ExecEventKind RunStarted origin: ExecOrigin, sessionid: SessionId, threadid: ThreadId, , TurnStarted sessionid: SessionId, threadid: ThreadId, turnid: TurnId , ThreadUpdated update: Box<ThreadUpdateEnvelope , RunCompleted outcome: ExecOutcome , pub enum ExecOrigin Local, ` 当前 schema version 是 1。Item-specific event、warning 与 Scheduled jobid, attemptid origin 属于阶段 3/4 的扩展点；当前先机械传递 canonical ThreadUpdateEnvelope，避免过早复制一套 item lifecycle。 ExecEvent 是外部展示/automation envelope，不是新的 authoritative domain model： - ID、status、item payload 优先机械复用 protocol； - 展示所需的 delta 可以映射，但不能改变 durable 事实； - terminal event 必须由 canonical Turn terminal status 产生； - JSONL stdout 每行一个完整 event； - stderr 只用于诊断； - human 模式默认只把最终 Agent message 写入 stdout，进度写 stderr； - event schema 变更必须有 fixture 与 compatibility policy。 8. 无界面批准与服务端请求 无交互运行不能等待不存在的用户界面。 `rust pub enum HeadlessApprovalMode DenyInteractiveRequests, AutomaticReview, BypassPermissions, ` 当前规则： - DenyInteractiveRequests 在 Turn 进入 approval/user-input/capability wait 时发送 typed InterruptTurn，观察 canonical Interrupted 后返回 RequiresInteraction； - AutomaticReview 映射为 App Server AutoReview，但无法呈现的 user-input/capability 仍会中断； - BypassPermissions 只由显式 --dangerously-bypass-permissions 选择，绝不是 worker 默认值； - Ctrl-C 和 turn timeout 都先发送 typed interrupt，再等待 bounded terminal observation； - 当前没有 remote reviewer channel，不能把交互请求悬挂给不存在的 UI。 远程 reviewer 的 Proposed 规则： - remote scheduler 支持 reviewer 时，请求必须携带 Job/Attempt/Thread/Turn/Tool identity； - reviewer response 必须防重放并校验 action digest； - approval timeout、scheduler disconnect 和 worker shutdown 都必须结束 pending server request； 因此 App Server Client 的长期 event stream 需要同时支持： - ServerNotification； - ServerRequest； - Lagged/Desynced； - ConnectionClosed。 9. 远程调度 9.1 两种运行模式 `text RunOnce = 一个 zeta-exec process = 一个 App Server session = 一个前台 Agent Job Worker = 一个长期 zeta-exec worker = 一个或多个隔离的 App Server session = 多个有 lease 的 Agent Job ` Worker mode 不能简单地在循环中调用 CLI main。它需要明确： - worker registration； - capability/host/workspace advertisement； - job lease 与续租； - attempt fencing； - cancellation； - event ack/cursor； - reconnect 与 replay； - draining shutdown； - tenant、credential、workspace 和 state-root isolation。 9.2 身份 以下身份不能混用： Identity Owner 用途 --- --- --- ExecRunId zeta-exec 当前 runner 内的一次运行 JobId scheduler 一次逻辑远程任务 AttemptId scheduler 一次 placement/execution attempt lease/fencing token scheduler 阻止过期 worker 继续提交 CommandId App Server caller durable command 幂等与 replay Session/Thread/Turn ID App Server domain canonical Agent lifecycle JSON-RPC request ID App Server Client 当前 connection 的 result pairing durable sequence aggregate update replay 与 resync scheduler event cursor scheduler protocol scheduler-side delivery ack 推荐保存稳定映射： `text JobId + AttemptId ↔ SessionId + ThreadId + TurnId + last durable sequence + scheduler event cursor ` Scheduler receipt 不是 App Server command receipt。两者可以在同一 Job flow 中关联，但不得放进 同一个 ID 或假设原子跨系统事务。 9.3 远程 Job 流程 `text scheduler assigns Job attempt, lease → worker validates isolation/capability → zeta-exec starts/selects AppServerSession → stable CommandId creates/resumes Session/Thread/Turn → worker streams mapped ExecEvent → scheduler acks event cursor → worker observes terminal durable update → worker commits terminal Job result with fencing token ` 断线恢复时： - scheduler 可以重新投递同一 JobId 的新 AttemptId； - worker 使用 mapping 与 App Server read/subscribe 判断原 Turn 是否仍运行或已终态； - 相同逻辑 App Server command 重试复用原 CommandId + exact payload； - durable gap 通过 snapshot + committed gap 恢复； - transient delta 丢失只影响低延迟展示，不能影响 terminal Job result； - 过期 lease 的 worker 不得提交 scheduler terminal result。 10. 远程 Agent 调度与远程执行平面 远程调度完整 Agent Job： `text scheduler → zeta-exec worker → App Server → Core ` 远程执行某个 tool process/filesystem operation： `text Core tool port → zeta-exec-server client → remote zeta-exec-server ` 二者的 security 和 lifecycle 不同： Plane Authority Stable unit Disconnect behavior --- --- --- --- Agent scheduling scheduler + App Server Job/Session/Thread/Turn durable recovery/reschedule Process execution executor connection process/filesystem request terminate, resume or report unknown by exec protocol zeta-exec-server 不得接受 Agent session/request，zeta-exec scheduler adapter 不得提供裸 process/start 旁路。 11. 工作进程隔离与并发 第一版 remote worker 应保守选择： - 一个 Job 一个 workspace lease； - 一个 tenant/credential scope 一个 App Server authority； - 同一 Thread 只有一个 active writer； - worker 只在 capability、sandbox 和 state-root scope 完全相容时复用 App Server session； - 不在不同用户间共享 Resource ownership、subscription 或 transient cursor； - worker restart 依赖 durable store 恢复，不依赖内存 pending table。 并发可以随 App Server per-Thread serialization 演进，但 scheduler placement 不能把“不同 Thread 可并行”误解为“不同 tenant 可以无隔离共享进程”。 12. 背压与重放 所有 queue 有界： - App Server Client request channel； - App Server event channel； - exec output sink； - remote scheduler outbound buffer。 处理规则： - durable update 不静默丢弃； - event queue 满时发出 Lagged/Desynced 并按 aggregate sequence resubscribe； - transient delta 可以按明确 policy 丢弃或合并； - terminal update 在成功交付或完成 snapshot resync 前不能宣称 Job 完成； - scheduler event 使用独立 cursor/ack，不能复用 Thread durable sequence； - scheduler 长时间不 ack 时施加 backpressure、持久化 spool 或终止 attempt，不能无限占用内存。 13. 取消与关闭 RunOnce Ctrl-C： `text signal → session/request type: interruptTurn with stable CommandId → wait terminal update within deadline → unsubscribe → AppServerSession.shutdown → interrupted ExecOutcome ` Worker Job cancellation： `text scheduler cancel JobId, AttemptId, fencing token → validate current attempt → session/request type: interruptTurn → publish terminal Job event → keep worker/App Server alive for other jobs ` Worker shutdown 应有具名模式： `rust pub enum WorkerShutdownMode Drain, InterruptActiveJobs, ` Drop 只做 best-effort cleanup；正常路径必须等待 job terminal handling 与 App Server Client shutdown。 14. 目标 crate 与依赖 `text zeta-cli ├─► zeta-tui ─────┐ └─► zeta-exec ────┤ ▼ zeta-app-server-client ▼ zeta-app-server ▼ zeta-core │ ▼ zeta-shell-command ▼ zeta-tool-executor ` 未来可增加： `text zeta-exec-server-protocol zeta-exec-server zeta-scheduler-protocol ` zeta-exec 可依赖 app-server-client、app-server-protocol、protocol 和窄 CLI/output utility。 它不依赖 Core、store、rollout、model provider、sandboxing 或 tool executor。 15. 实施阶段 阶段 1：消除命名冲突（当前状态） - 已将原 process zeta-exec 迁移为 zeta-tool-executor； - 已更新直接依赖与 tools、skills、plugins、MCP 文档中的 ownership； - zeta-exec crate 名称现由 headless runner 使用。 阶段 2：本地无界面纵向切片（当前状态） - 复用 owned App Server Client request/event session； - 实现 new/resume/fork、Turn start/interrupt； - human/JSONL output； - terminal status 与退出码； - 显式 shutdown。 阶段 3：可靠自动化（计划） - 稳定 ExecEvent schema； - output schema、last-message file、stdin 与 workspace input； - approval delegation； - reconnect/read/subscribe resync； - integration fixtures。 阶段 4：远程工作"
  },
  {
    "slug": "tools",
    "sourcePath": "docs/tools.md",
    "title": "工具系统",
    "description": "工具系统把本地工具、MCP 和动态来源转换成统一、可验证的能力目录；它定义和绑定工具，但最终 授权、执行顺序与持久化仍由相邻系统负责。",
    "group": "Agent 与运行时",
    "markdown": "> 计划物理位置：`zeta-rs/tools/`  \n> Rust crate：`zeta_tools`  \n> 当前状态：T1 主链完成，T2 MCP/dynamic 主链完成；MCP host runtime 已提取到 `ext/mcp`。T3 的\n> Tool Search、capability-bearing extension、Connector discovery projection、Plugin catalog projection 与\n> typed discovery request 已接入共享契约；`ext/web-search` 已提供默认关闭、宿主注入 backend 后才注册的\n> `web_search` 工具。Tool Call 已持久化 registry\n> generation、definition digest、source chain 与 direct/code-mode caller；Tool Result 已能持久化结构化\n> 图片内容。Code Mode 当前完成冻结 registry 投影和 durable nested-call bridge，尚无 cell/runtime host；\n> Plugin local content store 已落地，但安装 authority、activation、grant，以及 dynamic owner 断连后的持久化重启\n> 端到端 fixture 仍未完成。\n> Canonical value 与 durable Tool Item：[`protocol.md`](protocol.md)  \n> Core 调度、approval 与恢复：[`core.md`](core.md)  \n> MCP client runtime：[`mcp.md`](mcp.md)  \n> Plugin authority：[`plugins.md`](plugins.md)  \n> Config 与 runtime snapshot：[`config.md`](config.md)  \n> Provider wire adapter：[`zeta-api.md`](zeta-api.md)\n\n## 快速理解\n\n工具系统把本地工具、MCP 和动态来源转换成统一、可验证的能力目录；它定义和绑定工具，但最终\n授权、执行顺序与持久化仍由相邻系统负责。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| 不同来源的工具为什么能统一调用？ | 每个来源先转换成统一定义、规格、绑定和调用值 | [三层工具契约](#3-三层工具契约) |\n| 工具名称就是执行身份吗？ | 不是；稳定身份还包含来源、绑定和快照 generation | [身份、来源与绑定](#5-身份来源与绑定) |\n| Agent 当前能看到哪些工具？ | 由不可变注册表快照和暴露范围决定，运行中不会被静默改写 | [注册表与快照](#7-注册表与快照) |\n| 谁决定工具能不能执行？ | 权限系统决定授权；Core 调度；工具执行器落实调用 | [当前本地工具来源](#41-当前本地工具来源运行时) |\n| 当前完成到哪里？ | 统一 registry/executor/search、durable provenance 和结构化图片主链已落地；Plugin 安装 authority 与 Code Mode runtime 尚未完成 | [当前仓库审计](#2-当前仓库审计) |\n\n## 1. 结论\n\n`zeta-tools` 是 Zeta 工具子系统的共享类型与纯适配层。它定义一个工具如何被描述、暴露、搜索、\n绑定、调用和返回结果，并提供 MCP、dynamic tool、code mode 与图片精度等跨模块转换能力。\n\n它解决的是：\n\n```text\n不同工具来源\n  local / MCP / dynamic / connector / future extension\n            │\n            ▼\n统一 host-side ToolDefinition / ToolSpec / ToolBinding\n            │\n      registry + search + adaptation\n            │\n            ▼\nToolInvocation ──► ToolExecutor ──► ToolExecutionOutcome\n            │\n            ▼\nCore durable Tool Call / Tool Result lifecycle\n```\n\n长期职责固定为：\n\n- provider-neutral 的 `ToolDefinition`、`ToolSpec`、schema 和 exposure 类型；\n- model-emitted `ToolCall` 到 host-materialized `ToolInvocation` 之间的共享值；\n- executable tool 的 `ToolExecutor` / `ToolOutput` 接口；\n- immutable `ToolRegistrySnapshot`、binding、definition digest 与来源信息；\n- MCP descriptor/result 到 Zeta 工具值的纯转换；\n- dynamic tool definition/result 的校验与转换；\n- deferred tool loading、tool search 索引和值类型；\n- Plugin/Connector 发现结果和 install/enable/connect 请求的纯展示值；\n- code mode 的工具定义投影、命名、nested-call/result 适配；\n- tool output image 的 detail capability 检查、标准化和降级；\n- schema、输出、日志预览和 provider capability 的共享校验规则。\n\n`zeta-tools` 不拥有：\n\n- model → tool → model loop、approval、并行计划、retry 或 `UnknownOutcome` 决策；\n- Thread/Turn reducer、durable append、Tool Call/Result commit 或 recovery；\n- MCP transport、JSON-RPC session、process supervision、OAuth 或 reconnect；\n- Plugin 安装、启用、grant、版本、digest、marketplace 或 package store；\n- local tool、MCP server、browser、terminal 或 code-mode runtime 的具体 I/O；\n- sandbox、filesystem/network enforcement、credential materialization；\n- provider HTTP DTO、Responses API/Anthropic wire encoding；\n- App Server connection owner、interaction delivery 或客户端 UI。\n\n一句话边界：\n\n> `zeta-tools` 统一“工具在 host 中是什么以及如何跨表示转换”；Core 决定“何时、按什么策略调用”；\n> source runtime 决定“如何完成具体 I/O”。\n\n## 2. 当前仓库审计\n\n当前已有可复用地基：\n\n- `zeta-protocol` 已定义 `ToolName`、`ToolCallId`、`ToolDefinition`、`ToolCall`、`ToolResult`、\n  `ContentPart`、`ImageDetail` 和 durable `ThreadItem::ToolCall/ToolResult`；\n- `zeta-protocol` 已定义 `DynamicToolSpec`、`DynamicToolCall`、`DynamicToolResponse` 和对应\n  Agent interaction；\n- `zeta-core` 已定义 `ToolService::prepare/execute` port，并完成 policy-gated、可恢复的顺序\n  model → tool → model vertical slice；\n- `zeta-core::ContextAssembler` 已能重建 Tool Call/Result pairing；\n- `zeta-api` 已分别把 canonical function tool 和图片 detail 转成 OpenAI Responses、\n  Chat Completions 与 Anthropic Messages wire payload；\n- `zeta-shell-command`、`zeta-file-system` 与 `zeta-apply-patch` 各自提供独立 executor；App Server\n  已用 `ToolExecutorRuntime` 把它们接入 durable facts、policy authority、streaming output 与统一\n  registry，旧 read/write/edit 名称只保留 hidden migration route；\n- `zeta-mcp` 的 tools-only catalog/binding runtime 与 `zeta-mcp-extension` 的 host/Core adapter 已进入共享\n  registry/search projection；client-hosted dynamic tool 已进入同一 approval、durable interaction、\n  exact owner 与 unknown-outcome 链；\n- `zeta-extension-api::ReadOnlyToolContributor` 和显式声明 network/credential scope 的\n  `CapabilityToolContributor` 已进入同一 registry/policy/runtime；当前 `skills-read` 走前者，\n  `web_search` 走后者并要求 exact one-time approval。`zeta-plugins` 已把验证后的本地 catalog 投影为不可执行的 discovery\n  snapshot，`zeta-tools` 已定义 generation-bound install/enable/connect request；`zeta-plugins` 已能把\n  local package stage、复验并原子 promote 到 content-addressed store，实际安装 authority 仍处于 Proposed；\n- App Server 以 frozen `ToolBinding` 的 runtime key 执行，不再在执行阶段按 live tool name 猜 source\n  service；model invocation 会同时冻结 definitions 与 generation-bound binder，hot reload 只影响后续\n  model safe point，已绑定调用保留原 generation 和 policy 直到排空。\n- Core 在 durable Tool Call 中保存 registry generation、definition digest、source chain 和 caller；恢复\n  时 exact binding 不匹配会失败关闭，不会按同名新工具重放。Shell Turn 和 Code Mode nested call 也走\n  同一绑定入口；\n- Tool Executor、MCP 和 dynamic output 已转换为结构化 `ContentPart`，durable Tool Result 保留可选\n  content，旧纯文本记录继续兼容读取；模型调用边界会再次按 provider capability 清理 `Original`。\n\n当前剩余问题集中在尚未收口的跨边界能力，而不是再增加一套 registry：\n\n- dynamic owner 断连已经 fail closed，但还缺“持久化后重启恢复”的完整 App Server fixture；\n- Plugin/Connector 已有 typed discovery，Plugin manifest 可声明一个 Connector 到其 MCP contribution\n  的绑定；`zeta-connectors` 拥有 account connection state，`zeta-connectors-extension` 拥有 Plugin\n  projection、SQLite authority 与 API-token credential orchestration，ready binding 已接入 MCP/App Server\n  hot composition。local package store 也已落地。仍缺 OAuth/refresh/远端 revoke、用户确认交互和\n  Plugin activation，不能用 executable registry 反向代替 package authority；\n- `zeta-web-search-extension` 已有 bounded request、executor、JSON HTTP backend 与 App Server opt-in 安装口；\n  当前没有默认生产 Search provider 或 credential UI，宿主未注入 backend 时工具完全不可用；\n- provider adapter 可能分别决定 namespace flattening、strict schema 和 image detail fallback；\n- tool search、Plugin discovery 和 install request 容易被混成一个有隐式副作用的“发现服务”；\n- Code Mode 已有确定性投影和 durable nested-call bridge，但还缺 exec/wait、cell lifecycle、host transport\n  与 broker；runtime 若直接持有 executor，仍会绕过普通 tool 的 approval、durable commit 和 tracing。\n\n后续扩展必须继续经过现有 `zeta-tools` 窄共享契约和 App Server composition root，不能在 Plugin、\nConnector、code mode 或 provider adapter 内另建可执行 registry。\n\n本方案参考本地 `../codex/codex-rs/tools/` 已验证的提取方向：共享 definition/spec、MCP/dynamic\nadapter、tool search/discovery、code-mode bridge、image-detail normalization 与 executor\ncontract 可以离开 Core。Zeta 不直接照搬其中的 Responses API DTO 或 Core runtime context：\nprovider wire 继续属于 `zeta-api`，durable scheduling/recovery 继续属于 `zeta-core`。\n\n## 3. 三层工具契约\n\n工具系统固定分为三层：\n\n| 层 | Owner | 典型类型 | 生命周期 |\n| --- | --- | --- | --- |\n| Canonical product contract | `zeta-protocol` | `ToolName`、`ToolCallId`、durable Tool Item、dynamic interaction | 可序列化、可持久化 |\n| Host tool contract | `zeta-tools` | `ToolDefinition`、`ToolSpec`、binding、invocation、output、executor | process-local 或 snapshot-scoped |\n| Execution orchestration | `zeta-core` | `ToolScheduler`、`ToolService` port、approval、retry、recovery | Turn/operation-scoped |\n\n### 3.1 `zeta-protocol` 继续拥有\n\n- 所有跨进程和 durable identity；\n- model output 中 canonical `ToolCall` 的语义；\n- Thread transcript 中 Tool Call/Result 事实；\n- dynamic tool 的 Agent request/response envelope；\n- provider-independent message content 和图片 detail 枚举；\n- App Server 客户端确实需要读取的稳定工具值。\n\n`zeta-tools` 不重新定义 `ToolName`、`ToolCallId` 或另一套 durable Tool Item。\n\n### 3.2 `zeta-tools` 拥有\n\n- 比 model request DTO 更完整的 host-side definition/spec；\n- schema normalization、definition digest 和 name/binding 规则；\n- executor authoring contract；\n- source-neutral registry、search、code mode 和 output adapter；\n- 从 source-specific projection 到 host tool contract 的纯函数。\n\n当前 `zeta-protocol::ToolDefinition` 在迁移期保留为 model invocation 的最小 canonical value。\n`zeta-tools::ToolDefinition` 通过显式、可失败的 adapter 转成它。两者不能依赖同名 re-export\n长期共存；迁移完成后应根据真实消费者选择以下一个方向：\n\n1. protocol 类型明确命名为 `ModelToolDefinition`，host 类型保留 `ToolDefinition`；或\n2. 如果完整 host definition 也确实需要跨进程共享，再将纯子集提升到 protocol。\n\n不能用 `pub use` 假装两个不同生命周期的类型语义相同。\n\n### 3.3 `zeta-core` 继续拥有\n\n- 当前 invocation 使用哪个 registry snapshot；\n- model call 后的 schema/availability 校验时机；\n- sequential/parallel plan；\n- approval、deadline、cancellation 与 resource conflict；\n- Tool Call intent 的 durable commit；\n- result ordering、retry 和 uncertain outcome 的最终映射；\n- completion 的 operation/incarnation 校验；\n- next model invocation 与 Turn terminal decision。\n\nCore 的 `ToolService` 是 consumer-owned port；它可以由外层 `ToolRegistryAdapter` 实现，但不能\n被 `ToolExecutor` 取代。前者是 Core 的编排入口，后者是工具作者的执行接口。\n\n## 4. 依赖方向与组合\n\n目标依赖：\n\n```text\n                         zeta-protocol\n                               ▲\n                               │ canonical IDs/content\n                         zeta-tools\n                   shared types / pure adapters\n                    ▲       ▲        ▲\n                    │       │        │\n          local tool crates  zeta-mcp  code-mode adapter\n      shell-command / file-system / apply-patch\n                    \\       |        /\n                     \\      |       /\n                      App Server composition\n                     /       |        \\\n                    ▼        ▼         ▼\n             Core ToolService  Plugin catalog  zeta-api\n                adapter        projection      provider wire\n                    │\n                    ▼\n                 zeta-core\n```\n\n允许：\n\n- `zeta-tools → zeta-protocol`；\n- `zeta-core → zeta-tools + zeta-protocol`；\n- `zeta-mcp → zeta-tools`；\n- `zeta-shell-command → zeta-tools + zeta-tool-executor + zeta-sandboxing`；\n- local App Server composition → `zeta-install-context + zeta-shell-command`；\n- `zeta-file-system → zeta-tools + zeta-sandboxing`；\n- `zeta-tui → zeta-file-search`；后者提供只读路径索引，不依赖 `zeta-tools`，也不注册为模型\n  Tool；\n- catalog/runtime manager 可依赖 `zeta-file-watcher` 获取 coarse invalidation hint；后者不依赖\n  `zeta-tools`，不读取文件内容，也不注册为模型 Tool；\n- `zeta-apply-patch → zeta-tools + zeta-sandboxing`；\n- `zeta-action-policy → zeta-execpolicy + zeta-sandboxing`；\n- `zeta-execpolicy` 不依赖 sandbox、Core、Tool 或配置 I/O；\n- `zeta-auto-review → zeta-action-policy + zeta-sandboxing`；\n- local process executor 可依赖 `zeta-sandboxing` 与当前平台 backend；Linux backend 仅通过\n  `zeta-bwrap` 构造 Bubblewrap argv；\n- `zeta-api → zeta-tools + zeta-protocol`；\n- App Server 组合 Tool registry、source runtime、policy 和 Core port。\n\nSandbox backend 的内部调度、平台 crate 边界和 fail-closed 规则见\n[`sandboxing.md`](sandboxing.md)。这里的 sandbox manager 不是 Core `ToolScheduler`。\nAction classifier、exact grant 与最终 execution decision 见\n[`auto-review.md`](auto-review.md)。\n\n禁止：\n\n```text\nzeta-tools → zeta-core\nzeta-tools → zeta-mcp live runtime\nzeta-tools → zeta-plugins authority\nzeta-tools → zeta-app-server\nzeta-tools → provider HTTP client\nzeta-tools → credential or secret store\nzeta-tools → ThreadStore / SessionStore\n```\n\nMCP adapter 的公开输入使用 `zeta-tools` 自己的纯 `McpToolProjection`，不在 public API 暴露\n某个 MCP SDK 的 wire DTO。`zeta-mcp` 负责从当前 protocol revision 的 wire model 产生 projection；\n`zeta-tools` 负责从 projection 产生 host definition。这样 MCP SDK 升级不会迫使所有工具消费者\n一起升级。\n\n### 4.1 当前本地工具来源运行时\n\n本地 Workspace 工具不再有聚合 crate。每个 crate 只提供一个独立 `ToolExecutor`，可由\nApp Server composition root 按 policy 单独注册：\n\n| Crate / tool name | 可做的事 | 明确不做的事 |\n| --- | --- | --- |\n| `zeta-shell-command` / `shell-command` | 在批准的相对 Workspace 工作目录执行显式 program/arguments；复用 `zeta-tool-executor` 的 approval、timeout 和输出上限 | 不隐式启动 shell，不绕过 process policy |\n| `zeta-file-system` / `file-system` | 读取 UTF-8 文件、列目录、读取 metadata | 不写入、删除、移动或重命名 |\n| `zeta-apply-patch` / `apply-patch` | 预检后更新、添加或删除普通文件；replacement 按文件原子写入 | 不接受绝对/`..` 路径，不直接提供任意写入 API |\n\n三者均在构造时固定 `ToolEnvironmentId + WorkspaceRoot`，要求它与\n`ToolExecutionContext.environment_id` 一致，且只接受与自身 `ToolDefinition` digest 相符的冻结\nbinding。`apply-patch` 在所有 hunk 校验完成前不写入；\n若多文件 commit 中途失败，返回 `OutcomeUncertain`，由 Core 决定后续恢复语义。\n\n`zeta-file-system` 还提供 host-only 的 `find_nearest_ancestor_with_markers`，用于从一个本地路径\n向上发现最近的项目 marker。它不是模型 Tool，不读取 marker 配置，也不施加 `WorkspaceRoot`\ncontainment；调用方仍拥有项目根语义和搜索边界。实现与错误策略由\n[`zeta-rs/file-system/README.md`](../zeta-rs/file-system/README.md) 维护。\n\n搜索分成模型侧命令搜索与交互式路径搜索：\n\n| Surface | 所有权 | 模型可见 |\n| --- | --- | --- |\n| `zeta-shell-command` + `rg` | 内容搜索、`rg --files` 路径枚举和 `-g` glob filtering | `shell-command` Tool |\n| `zeta-file-search` | ignore-aware 路径索引、fuzzy matching、`PathSearchHandle` 和 CLI | 否 |\n| `zeta-file-watcher` | 多订阅者路径失效提示、missing-path fallback、throttle/debounce 与 overflow rescan hint | 否 |\n\n模型侧不注册独立 `glob`、`grep` 或 `text-search` Tool：内容搜索使用 `rg PATTERN`，路径枚举\n使用 `rg --files`，glob 作为 `-g` 参数传给 `rg`。交互式路径搜索契约由\n[`zeta-rs/file-search/README.md`](../zeta-rs/file-search/README.md) 维护；TUI 直接持有\n`PathSearchHandle`，不启动 CLI，Core 也不把路径搜索注册成 Tool。\n\n`zeta-file-watcher` 同样不是搜索或读取接口。它只把 OS mutation/error 转成\n`PathsChanged`/`RescanRequired`；consumer 必须重新扫描并校验 own state。其 ref-count、路径匹配、\nRAII 与 failure contract 由\n[`zeta-rs/file-watcher/README.md`](../zeta-rs/file-watcher/README.md) 维护。\n\n当前 local App Server 启动时通过 `zeta-install-context` 按 `ZETA_RG_PATH`、package\n`zeta-path/`、Zeta executable 同目录、host `PATH` 的顺序生成 `rg` candidates，再由\n`zeta-shell-command` 验证并冻结 canonical executable identity；未找到时启用本地工具的\ncomposition 直接失败。canonical release package 由\n[`scripts/build_zeta_package.py`](../scripts/build_zeta_package.py) 按\n[`third_party/ripgrep/runtime-lock.json`](../third_party/ripgrep/runtime-lock.json) 下载并校验\ntarget-specific ripgrep archive，把 executable 放到 `zeta-path/rg[.exe]`；源码开发启动仍可使用\n`ZETA_RG_PATH` 或 host `PATH`。\nfilesystem 与 shell Tool 复用同一个启动时 canonicalized `WorkspaceRoot`，CLI 优先采用\n`ZETA_WORKSPACE_ROOT`，否则采用当前目录。模型只看到 `program = \"rg\"`，host 强制加入\n`--no-config`，拒绝 preprocessor、hostname command、archive search、symlink follow 和外部\npattern/ignore file 参数，包括 `-f/path`、`-LH` 等紧凑短参数形式，并用只读、断网 sandbox\n执行。进程输出采用总 byte budget 截断并返回显式 truncation marker，Turn cancellation 会终止\n已启动的子进程。\n\n当前限制：package metadata 记录锁定的 ripgrep version、archive digest 与 binary digest，但\nApp Server 尚未执行 `rg --version` capability probe；本地 override/PATH candidate 也不保证与\npackage 锁定版本相同。该 runtime prerequisite 属于命令执行与安装诊断，不应通过重新增加一套\n内容搜索 Tool 解决。crate 内实现契约见\n[`zeta-rs/shell-command/README.md`](../zeta-rs/shell-command/README.md)。\n\n## 5. 身份、来源与绑定\n\n### 5.1 Name 不是执行身份\n\n`ToolName` 是模型可见、在一次 invocation tool set 中唯一的路由名。它不是：\n\n- MCP remote tool 的永久 identity；\n- Plugin contribution ID；\n- Connector account ID；\n- executor instance ID；\n- `ToolCallId`；\n- registry generation。\n\n同名工具可以在不同 source 中存在，但进入一个 model invocation 前必须解析为唯一 alias 或产生\n明确 collision diagnostic。\n\n### 5.2 绑定\n\n目标共享值：\n\n```rust\npub struct ToolBinding {\n    pub registry_generation: ToolRegistryGeneration,\n    pub id: ToolBindingId,\n    pub exposed_name: ToolName,\n    pub definition_digest: ToolDefinitionDigest,\n    pub source: ToolSourceProvenance,\n    pub runtime_key: ToolRuntimeKey,\n}\n```\n\n语义：\n\n- `ToolBindingId` 在一个 `ToolRegistrySnapshot` 中稳定且唯一；\n- `ToolRuntimeKey` 是 host router 的 opaque key，不进入 model request 或 Thread transcript；\n- `ToolDefinitionDigest` 覆盖 model-visible name、description、schema、loading 与 invocation kind；\n- `ToolSourceProvenance` 保存稳定、可审计的 source chain，但不保存 credential、PID、session ID\n  或 mutable package path。\n\n一个 Plugin 贡献的 MCP tool 同时保留：\n\n```text\ndistribution provenance = PluginId + version + package digest + contribution ID\nexecution provenance    = McpServerId + exact remote tool name + catalog generation\n```\n\n二者不能互相替代。\n\n### 5.3 Durable 来源\n\nprocess-local `ToolBindingId` 不能单独用于 crash recovery。当前 Tool Call durable fact 保存：\n\n```text\nexposed ToolName\n+ stable source reference\n+ definition digest\n+ registry/catalog generation provenance\n```\n\n`ToolCallBinding` 已进入 protocol，history schema revision 已同步提升。Core 在记录 model、Shell 或\nCode Mode nested call 前先冻结该值，App Server reloadable service 同时保留对应 registry/policy\ngeneration。恢复时若 exact source generation 已不存在：\n\n- 未开始执行的调用变成稳定 `Unavailable`；\n- 已开始且副作用 outcome 不确定的调用进入 Core `UnknownOutcome`；\n- 不能仅按旧 `ToolName` 在新 registry 中重新查找并执行。\n\n## 6. 工具定义、模式与规格\n\n### 6.1 叶级定义\n\n`ToolDefinition` 描述一个可调用 leaf tool：\n\n```rust\npub struct ToolDefinition {\n    pub name: ToolName,\n    pub description: String,\n    pub invocation: ToolInvocationKind,\n    pub output_schema: ToolOutputSchema,\n    pub schema_mode: ToolSchemaMode,\n    pub loading: ToolLoading,\n}\n\npub enum ToolInvocationKind {\n    Function { input_schema: ToolInputSchema },\n    Freeform { format: FreeformFormat },\n}\n\npub enum ToolSchemaMode {\n    ProviderDefault,\n    Strict,\n}\n\npub enum ToolLoading {\n    Eager,\n    Deferred,\n}\n\npub enum ToolOutputSchema {\n    Unspecified,\n    Schema(ToolSchema),\n}\n```\n\n不使用 `strict: bool` 或 `defer_loading: bool` 作为 host API。Provider wire 需要的 bool 只在\n`zeta-api` adapter 最后一层产生。\n\nDescription 是 untrusted model context：\n\n- 有 byte/token 上限；\n- 保留 source provenance；\n- 不能含有改变 approval、sandbox 或 instruction precedence 的权威语义；\n- source runtime 不能通过 description 宣称自己是 `SafeRead` 并获得自动 retry。\n\n### 6.2 工具模式\n\n工具 schema 是受约束的 JSON Schema，不是任意 unchecked `serde_json::Value`：\n\n```rust\npub struct ToolSchema {\n    canonical: serde_json::Value,\n    digest: ToolSchemaDigest,\n}\n\npub struct ToolInputSchema(ToolSchema);\n```\n\n`ToolInputSchema` 额外要求根节点满足 Zeta function-argument contract。公开构造只能经过\nparser/validator。至少限制：\n\n- 总 bytes、nesting depth、node/property/enum 数；\n- string length、property name 和 description length；\n- `$ref`、recursive schema 和 remote reference；\n- unsupported keyword 与 dialect；\n- `required` 必须引用已存在 property；\n- object 的 `properties` / `additionalProperties` 语义；\n- provider 严格模式所需的完整 required set；\n- schema canonicalization 与稳定 digest。\n\nCanonical dialect 以 Zeta 支持的 JSON Schema 子集表达。MCP 2020-12 schema、dynamic schema 和\nprovider function schema 都先进入同一 parser。不能为通过某个 provider 校验而静默删除有语义的\nconstraint；只能：\n\n```text\nexactly supported → preserve\nexplicitly approximated → diagnostic + capability snapshot records approximation\nunsafe/ambiguous → reject definition\n```\n\n### 6.3 聚合规格\n\n`ToolSpec` 是 model-facing aggregate：\n\n```rust\npub enum ToolSpec {\n    Callable(ToolDefinition),\n    Namespace(ToolNamespaceSpec),\n    Search(ToolSearchSpec),\n    ProviderHosted(ProviderHostedToolSpec),\n}\n```\n\n规则：\n\n- `Callable` 的 function/freeform 形态由 `ToolDefinition::invocation` 唯一决定，并且必须有\n  host binding；\n- `Namespace` 是展示和 provider capability 层的组合，不改变 leaf binding；\n- `Search` 只搜索冻结 registry 中的 deferred definitions；\n- `ProviderHosted` 由 provider 执行，必须有独立 outcome/capability contract，不能伪装成\n  `ToolExecutor`；\n- provider 不支持 namespace 时，由 `zeta-api` 根据 frozen alias table flatten，不能自由拼接\n  字符串；\n- provider 不支持 freeform 或 hosted kind 时，在 model invocation 前返回 capability error。\n\n### 6.4 暴露范围\n\n工具是否初始暴露使用自描述 enum：\n\n```rust\npub enum ToolExposure {\n    Direct,\n    Deferred,\n    DirectModelOnly,\n    Hidden,\n}\n```\n\n- `Direct`：进入初始 model tool set，也可成为 code-mode nested tool；\n- `Deferred`：只进入 search index，被搜索选中后才进入后续 model invocation；\n- `DirectModelOnly`：直接暴露给模型，但不进入 code mode；\n- `Hidden`：只注册 dispatch，不向模型或 tool search 暴露。\n\nExposure 不表示 authorization。`Direct` 工具仍可在具体 invocation 时要求 approval。\n\n## 7. 注册表与快照\n\n### 7.1 注册表输入\n\nApp Server composition root 收集：\n\n```text\nbuilt-in registrations\n+ ready MCP catalog projections\n+ active connector adapters\n+ accepted dynamic tool specs\n+ host capability tools\n+ code-mode bridge tools\nconstrained by policy/grants/model capability\n= ToolRegistrySnapshot\n```\n\n`zeta-tools` 提供 builder 和 immutable snapshot value，但不读取任何 live manager。\n\n### 7.2 快照\n\n```rust\npub struct ToolRegistrySnapshot {\n    pub generation: ToolRegistryGeneration,\n    pub entries: Vec<RegisteredTool>,\n    pub diagnostics: Vec<ToolRegistryDiagnostic>,\n}\n\npub struct RegisteredTool {\n    pub definition: ToolDefinition,\n    pub binding: ToolBinding,\n    pub exposure: ToolExposure,\n    pub search: ToolSearchMetadata,\n    pub execution: ToolExecutionMetadata,\n}\n```\n\nBuilder 必须确定性执行：\n\n1. validate definitions；\n2. resolve reserved names；\n3. assign exposed aliases；\n4. collision check；\n5. compute definition digests；\n6. bind source runtime keys；\n7. build deferred search entries；\n8. project code-mode-eligible definitions；\n9. sort entries and diagnostics；\n10. publish new generation。\n\n同一输入 generation 集合必须产生同一 snapshot。新 snapshot 只有在 consumer-visible tool set、\nbinding、definition 或 diagnostic gate 变化时递增 generation。\n\n### 7.3 安全点与排空\n\n- Turn/model invocation 只读取冻结 snapshot；\n- model 产生的 Tool Call 必须在产生它的 invocation snapshot 中解析；\n- MCP list-changed、Plugin update、dynamic tool removal 不改变 in-flight binding；\n- old snapshot 持有 runtime lease，直到其 model calls 和 Tool invocations drain；\n- 新工具默认只在下一个 model safe point 生效；\n- policy 的单调收紧可以取消旧 invocation，但不能将旧 binding 静默换成新 executor。\n\n`ToolRegistrySnapshot` 是 process-local 派生值，不进入 Config document 或 Thread event。只有调用\n恢复所需的稳定 provenance 进入 durable fact。\n\n## 8. 工具 Call、调用与执行接口\n\n### 8.1 三种调用值\n\n必须区分：\n\n```text\nModel ToolCall\n  模型输出：call id + exposed name + raw payload\n\nMaterialized ToolInvocation\n  host 已解析 binding、校验 schema、固定环境和 operation identity\n\nDurable Tool Call Item\n  Core 已提交的调用事实\n```\n\n不能让 executor 接收一个只有 name/JSON 的值后自行从 live registry、Config 或 Thread 查找环境。\n\n### 8.2 载荷\n\n```rust\npub enum ToolPayload {\n    FunctionArguments(serde_json::Value),\n    FreeformInput(String),\n    SearchQuery(ToolSearchQuery),\n}\n```\n\nFunction arguments 在 materialization 前完成 JSON parse 和 schema validation。原始 provider\narguments 可以留在 invocation trace 供诊断，但 executor 只接收 canonical payload。若 source\n确实需要 exact raw bytes，应由具名 `RawPayloadRequirement` capability 明确声明，不能成为默认。\n\n### 8.3 具体化调用\n\n```rust\npub struct ToolInvocation {\n    pub operation_id: ToolOperationId,\n    pub call_id: ToolCallId,\n    pub turn_id: TurnId,\n    pub binding: ToolBinding,\n    pub payload: ToolPayload,\n    pub context: ToolExecutionContext,\n}\n```\n\n`ToolExecutionContext` 只包含 executor 被允许看到的 environment identity 与 cancellation token。\nCore/App Server 接入时会将它扩展为具名、受限的 capability handle，例如：\n\n- effective working directory reference；\n- approved filesystem capability；\n- approved network capability；\n- resource writer；\n- redacted locale/timezone；\n- explicitly selected credential capability。\n\n它不默认包含完整 conversation history、Config、secret store、Thread controller 或 App Server handle。\n需要额外上下文的工具必须在 registration 时声明具名 capability，并经过 policy/materialize。\n\n### 8.4 执行器接口\n\n`zeta-tools` 定义工具作者接口：\n\n```rust\n/// Executes one fully materialized tool invocation.\n///\n/// Implementations must execute only the supplied binding and payload, observe\n/// cancellation and limits, never read or mutate Thread state, and report\n/// whether an externally visible operation may have started when a trustworthy\n/// result cannot be produced.\npub trait ToolExecutor: Send + Sync {\n    fn definition(&self) -> ToolDefinition;\n\n    fn exposure(&self) -> ToolExposure {\n        ToolExposure::Direct\n    }\n\n    fn search_metadata(&self) -> ToolSearchMetadata;\n\n    fn concurrency(&self) -> ToolConcurrency;\n\n    fn execute(&self, invocation: ToolInvocation) -> ToolExecutionFuture<'_>;\n}\n```\n\n新 trait 必须保留上述角色和实现约束的 doc comment。接口不接受含糊的 `bool` / `Option` 参数。\n`definition`、`exposure`、`search_metadata` 和 `concurrency` 必须确定、无 I/O；composition root\n只在构造新 registry snapshot 时读取它们。\n\n`ToolConcurrency` 使用 typed policy hint：\n\n```rust\npub enum ToolConcurrency {\n    Exclusive,\n    ParallelSafe,\n    ConflictClass(ToolConflictClass),\n}\n```\n\n它只是 executor 声明；Core 仍将它与 Turn policy、approval 和 resource conflict 一起计算最终\nschedule。\n\n### 8.5 核心消费方端口\n\nCore port 接收 materialized request 并返回 source-neutral outcome：\n\n```rust\n/// Routes a Core-approved invocation to the executor bound by the frozen\n/// registry snapshot.\n///\n/// Implementations must preserve operation and call identity, reject stale\n/// bindings, enforce host capabilities, and never commit Thread state.\npub trait ToolService: Send + Sync {\n    fn execute(&self, invocation: ToolInvocation) -> ToolExecutionFuture<'_>;\n}\n```\n\n长期不再通过 `definitions()` 在执行时读取 live list。Definitions 由独立 snapshot 输入\n`ContextManager`；execution 只解析 invocation 中冻结的 binding。\n\n## 9. 输出与结果\n\n### 9.1 面向模型的输出\n\n```rust\npub struct ToolOutput {\n    pub status: ToolOutputStatus,\n    pub content: Vec<ToolContent>,\n    pub structured: ToolStructuredOutput,\n    pub provenance: ToolOutputProvenance,\n}\n\npub enum ToolOutputStatus {\n    Success,\n    Error,\n}\n\npub enum ToolContent {\n    Text(String),\n    Image(ToolImage),\n    Resource(ToolResourceRef),\n}\n\npub enum ToolStructuredOutput {\n    Absent,\n    Json(serde_json::Value),\n}\n```\n\nTool-level API failure、业务校验失败和 command exit non-zero 通常是\n`ToolOutputStatus::Error`，作为 Tool Result 返回模型。它们不是自动终止 Turn 的 infrastructure\nerror。\n\n所有 output 施加：\n\n- 总 bytes、content part 数和每 part 上限；\n- UTF-8 与 JSON/schema validation；\n- MIME、URI scheme 和 resource ownership 校验；\n- binary/image 的 resource indirection；\n- deterministic truncation 与明确 truncation marker；\n- 日志预览与 model-facing full output 分离；\n- external context provenance。\n\n### 9.2 执行结果\n\n```rust\npub enum ToolExecutionOutcome {\n    Returned(ToolOutput),\n    NotStarted(ToolStartFailure),\n    OutcomeUncertain(ToolUncertainOutcome),\n}\n```\n\n边界：\n\n- `Returned`：executor 对工具业务结果有可信终态；\n- `NotStarted`：能够证明外部动作尚未开始，例如 binding stale、local validation failed；\n- `OutcomeUncertain`：请求可能已到达外部系统，但断线/崩溃使终态未知。\n\n`zeta-tools` 定义准确值，Core 决定：\n\n- 是否将 `NotStarted` retry；\n- 是否把 `OutcomeUncertain` durable 映射为 `UnknownOutcome`；\n- Tool Result 是否继续 model loop；\n- Turn 是否失败。\n\n取消只是一种执行信号，不自动等于 `NotStarted`。Executor 必须根据外部动作是否可能开始返回准确\noutcome。\n\n当前 Core 的 sandbox escalation 已采用更窄的恢复规则：只有 executor 明确返回\n`SafeToRetry` denial 才进入二次 policy review；需要用户决定时先 durable 保存 denial 和 exact\napproval binding。批准后在非 sandbox 重试前提交 escalation marker；若 marker 已存在而结果缺失，\n恢复只产生 unknown-outcome failure，不重复调用工具。`MayHaveSideEffects` denial 永不自动重放。\n\n### 9.3 输出适配器\n\n共享 adapter 负责：\n\n```text\nToolOutput\n  ├─► canonical zeta-protocol::ToolResult content\n  ├─► code-mode nested result\n  ├─► bounded telemetry preview\n  └─► provider-neutral next ModelRequest input\n```\n\nProvider wire encoding 仍由 `zeta-api` 完成。不能让每个 executor 自己生成 OpenAI/Anthropic\nJSON。\n\n## 10. MCP 工具转换\n\n### 10.1 两段式适配器\n\nMCP 转换固定为：\n\n```text\nMCP wire Tool\n  → zeta-mcp: revision-specific parse + exact remote identity\n  → McpToolProjection\n  → zeta-tools: schema normalization + host definition + output adapter\n  → RegisteredTool + MCP executor binding\n```\n\n纯 projection：\n\n```rust\npub struct McpToolProjection {\n    pub remote_name: String,\n    pub description: String,\n    pub input_schema: serde_json::Value,\n    pub output_schema: McpOutputSchemaProjection,\n    pub annotations: McpToolAnnotationClaims,\n}\n```\n\n它不包含 transport、request ID、credential、connection handle 或 SDK-specific type。\n\n### 10.2 定义转换\n\n`mcp_tool_definition` 必须：\n\n- 接收已经 collision-checked 的 exposed `ToolName`；\n- 保留 exact remote name 到 MCP executor binding；\n- 对缺失/null object `properties` 按明确兼容规则补为空 object；\n- 用统一 `ToolSchema` parser 处理 input/output；\n- 将 MCP annotations 标记为 `UntrustedSourceClaim`；\n- 计算 definition/schema digest；\n- 根据 host policy 选择 `Eager` 或 `Deferred`，不能听任 server 自己扩大 exposure；\n- 拒绝 schema bomb、invalid name、超长 description 和不支持 content contract。\n\nMCP `outputSchema` 只约束 `structuredContent`。MCP call result 外层 `content`、`isError` 和 `_meta`\n有独立 envelope；不能把 remote output schema 错当成整个 Tool Result schema。\n\n### 10.3 结果转换\n\n转换规则保留：\n\n- text → `ToolContent::Text`；\n- image/blob → 经过 size/MIME/resource policy 的 `ToolContent::Image/Resource`；\n- resource link/embedded resource → 保留 MCP source provenance；\n- `structuredContent` → 校验 output schema 后进入 `ToolStructuredOutput::Json`；\n- `isError: true` → `ToolOutputStatus::Error`；\n- MCP protocol error → `NotStarted` 或 infrastructure error，取决于请求是否发送；\n- transport lost after send → `OutcomeUncertain`，不能伪造普通 error output。\n\nMCP adapter 不决定 approval、retry 或 durable state，也不能根据 server `readOnlyHint` 单独把调用\n标为可自动重试。\n\n## 11. 动态工具\n\n“Dynamic tool”必须区分两个概念：\n\n```text\nDynamic definition\n  Turn/client/extension 在运行时提供一个新的 ToolSpec\n\nDynamic execution\n  一次调用通过 Agent interaction 交给外部 owner 返回结果\n```\n\n二者通常一起出现，但不能让“动态”成为绕过 registry 和 policy 的通用后门。\n\n### 11.1 定义适配器\n\n`DynamicToolSpec → ToolDefinition` 经过与 MCP/built-in 相同的：\n\n- `ToolName` validation；\n- schema parser 和 complexity limit；\n- description/output limit；\n- exposure/policy resolution；\n- alias collision check；\n- definition digest；\n- registry generation。\n\nDynamic source 不能提交 unchecked provider wire schema，也不能选择 reserved tool name。\n\n### 11.2 执行\n\nclient-owned dynamic tool 使用：\n\n```text\nmodel emits Tool Call\n→ Core resolves frozen dynamic binding\n→ validate + approval\n→ durable Tool Call\n→ durable InteractionRequested { DynamicToolCall }\n→ App Server delivers to exact owner\n→ client returns DynamicToolResponse\n→ output adapter validates call id、content 与 image/resource policy，并分配有效 detail\n→ durable InteractionResolved + Tool Result\n```\n\n`RequestId`、`ToolCallId` 和 App Server JSON-RPC request ID 始终分开。\n\nDynamic tool owner disconnect 时：\n\n- 尚未投递：等待能够精确承载该 tool name 的 owner；\n- 已投递且 side effect 可能开始：持久化 `OwnerDisconnected` cancellation，按 unknown outcome\n  收口；\n- 不能将请求转交给同名的新 owner，也不能自动重试；\n- 原 interaction 取消后，重连客户端的迟到 response 会被拒绝。\n\n### 11.3 运行时注册\n\n进程内 extension 也可以动态注册 `Arc<dyn ToolExecutor>`，但必须由 composition root 生成新的\nregistry snapshot。不能从正在执行的 executor 内部直接修改当前 snapshot。\n\n## 12. 工具搜索\n\n### 12.1 搜索对象\n\n`tool_search` 只搜索：\n\n- 已安装；\n- 已启用；\n- 已完成 grant/credential gate；\n- 当前 registry snapshot 中可用；\n- exposure 为 `Deferred`；\n- 对当前 model/Turn policy 兼容；\n- 尚未直接暴露给当前 invocation 的工具。\n\n它不搜索 marketplace 中未安装的 Plugin，也不触发 install、enable、connect 或 credential flow。\n\n### 12.2 搜索值\n\n```rust\npub struct ToolSearchMetadata {\n    pub text: String,\n    pub source: ToolSearchSourceInfo,\n    pub tags: Vec<ToolSearchTag>,\n}\n\npub struct ToolSearchQuery {\n    pub text: String,\n    pub limit: ToolSearchLimit,\n}\n\npub struct ToolSearchResult {\n    pub registry_generation: ToolRegistryGeneration,\n    pub matches: Vec<ToolSearchMatch>,\n}\n\npub struct ToolSearchMatch {\n    pub binding_id: ToolBindingId,\n    pub score: ToolSearchScore,\n    pub loadable_spec: LoadableToolSpec,\n}\n```\n\n`LoadableToolSpec` 带 definition digest 与 binding correlation。模型不能修改 search result 中的\nname/schema 后要求 host 执行。\n\n第一版 `ToolSearchLimit` 默认值为 8，并受 host-configured hard cap 限制。超出 hard cap 返回\ntyped validation error，不以无限结果或静默全量 catalog 作为 fallback。\n\n当前 App Server 的默认 exposure policy 是：built-in Workspace 与 client-hosted dynamic port 直接\n暴露，MCP port 默认 Deferred，extension executor 使用自身声明的 exposure；单个 host contribution\n还可在 composition 时显式覆盖。只有 registry 中确实存在 Deferred 工具时才增加 `tool_search`。\n这个默认值是 host policy，不属于 registry 的来源特例；以后可根据真实调用覆盖率调整，但 Plugin、\nConnector 和 dynamic contribution 仍必须经过同一 exposure contract，不能各自创建搜索入口。\n\n### 12.3 索引与排序\n\n第一版使用 snapshot-local、可重建的 deterministic index：\n\n- name 与拆词后的 alias；\n- description；\n- schema property name/description；\n- namespace；\n- source display name 与受控 tags。\n\n限制 query bytes、result limit、index size 和单 entry text。相同输入 snapshot/query 必须产生\n稳定 ordering，以 score、source priority、exposed name、binding ID 做 tie-break。\n\n向量或远端搜索只有在独立隐私、缓存和 failure contract 完成后加入。search index 不包含 secret、\ntool output、conversation history 或未经允许的 Plugin package content。\n\n当前默认模式是 `Lexical`：自然语言 query 使用 exact/substring name 优先和 snapshot-local BM25；\n调用方明确选择 `regex` strategy 时，使用有 query byte 上限的线性时间 Regex 匹配完整 search\ndocument。默认返回 8 个、硬上限 32 个。这条路径不调用模型，也不发送工具元数据到进程外。\n\nUser Config 可把 `toolSearch.mode` 设为 `hybridEmbedding`，并用独立的\n`toolSearch.embeddingModel` 选择 exact `ModelRef`。App Server 通过 `SemanticModelProvider` 把该选择\n和对应 provider config 解析成 `EmbeddingInvoker`；它不借用 CodeIndex 的模型选择或启用状态。\n`toolSearch/configure` 在提交配置前先发送一条不包含工具元数据的固定 readiness probe；缺少模型、\nprovider/credential/runtime、请求失败、响应数量错误或零向量都会返回 `ToolSearchUnavailable`，配置\n不提交。若 hybrid 配置来自外部 TOML 或进程启动恢复，但运行时不可达，App Server 把\n`config/read.toolSearch.embeddingStatus` 投影为 `unavailable`；自然语言搜索明确失败，不静默改用\nBM25。用户显式切回 `lexical` 后才恢复纯词法自然语言搜索。\n\n门禁通过后，自然语言搜索为 query 和当前 deferred tool documents 生成 embedding，再用\nreciprocal-rank fusion 与 BM25 排名合并；Regex 始终保持本地词法路径。向量只缓存在当前 registry\ngeneration 的内存中。后续真实 embedding 调用失败、返回数量/维度错误或向量无有效模长时，当前\n`tool_search` 直接返回明确错误，不静默回落到 BM25。模型调用继续属于 `model-provider`；\nsnapshot-local 目录、输入准备、召回融合、generation 校验、过滤与截断仍属于 Tool Search owner。\n\nTrusted local Workspace 还会注册 direct built-in `search_code`。它不属于 deferred Tool Search：Agent\n可以显式传入自然语言 query 与最多 20 条结果，App Server 使用 canonical CodeRetrieval 编排本地 FTS、\n已授权 semantic 和可选 cloud candidates，再返回 bounded、current-source-verified excerpts。Policy 只\n为 exact `workspace-code-index-read-only` grant 放行；伪造或复用其他 unsandboxed grant 会被拒绝。\n\n`zeta-rs/tools/src/registry_search_eval_tests.rs` 保存一份跨 coding、GitHub、Slack、Calendar、\nBrowser 和 Database 的离线查询集，比较当前 BM25 排序与 uniform token-overlap baseline，并以\nTop-1、Top-3 和 MRR 设置回归门槛。该 synthetic fixture 只防止明显退化，不替代基于匿名真实调用\n构建的长期评测集；纯语义同义词和跨语言查询单独计分，不冒充词法方案已经解决的能力。\n\n### 12.4 加载流程\n\nHost-executed flow：\n\n```text\nmodel calls tool_search\n→ Core durably records search call/result\n→ validate result binding against same registry generation\n→ next model invocation includes selected definitions\n```\n\n如果 provider 原生支持 deferred tool loading，`zeta-api` 可以把同一 `LoadableToolSpec` 编码成\nprovider feature，但 provider 返回的 loaded tool 仍必须关联 frozen binding。Provider 不能加载\nhost registry 中不存在的 tool。\n\n## 13. Plugin 与 Connector 发现\n\n### 13.1 与工具搜索分离\n\nPlugin discovery 面向“当前不可执行、可能需要安装或启用的扩展能力”：\n\n```text\ntool_search\n  = 在当前已授权 registry 内找 callable tool\n\nplugin discovery\n  = 在 Plugin/Connector catalog 中找候选扩展\n```\n\nPlugin discovery result 永远不是 `ToolDefinition`，不能直接进入 model tool set。\n\n### 13.2 共享发现值\n\n`zeta-tools` 只定义跨 Plugin manager、App Server 和 Agent-facing helper 共享的纯值：\n\n```rust\npub enum DiscoverableCapability {\n    Plugin(DiscoverablePluginInfo),\n    Connector(DiscoverableConnectorInfo),\n}\n\npub enum DiscoveryAction {\n    Install,\n    Enable,\n    Connect,\n}\n\npub struct DiscoverablePluginInfo {\n    pub id: PluginDiscoveryId,\n    pub display_name: String,\n    pub description: String,\n    pub has_skills: CapabilityPresence,\n    pub mcp_servers: Vec<DiscoveryContributionName>,\n    pub connectors: Vec<ConnectorDiscoveryId>,\n    pub action: DiscoveryAction,\n}\n```\n\n这些值是 catalog projection，不是 Plugin authority record。Exact version、digest、origin、\npermissions 和 credential slots 必须在真正 install/enable command 前由 Plugin manager 再读取并\n展示。\n\n### 13.3 面向 Agent 的辅助程序工具\n\n当前已实现的是 generation-bound discovery snapshot、client capability filtering 和 typed request；\n这些值本身不能成为 `ToolDefinition`。以下两个 Agent helper 仍是下一阶段接线：\n\n```text\nlist_available_plugins_to_install\nrequest_plugin_install\n```\n\n但其语义必须是：\n\n- list 读取 App Server 提供的 immutable discovery snapshot；\n- request 产生 typed interaction/Plugin command intent；\n- install/enable 需要用户确认和 exact candidate；\n- helper 不下载 package、不写 Config、不授予 permission；\n- Plugin manager 返回成功后，Tool registry 仍等到新的 activation/MCP generation 在 safe point\n  发布；\n- 安装完成不等于相关 MCP server 已 Ready。\n\n客户端差异通过 typed `ClientCapabilities` 过滤，不通过 `\"tui\"` 等 client-name 字符串硬编码。\n\n## 14. 代码模式\n\n### 14.1 定位\n\nCode mode 是普通工具集合的另一种模型调用表示：模型通过一个 code execution surface，在代码中\n调用多个 nested tools。它不是第二套 tool runtime，也不是 approval/sandbox 旁路。\n\n`zeta-tools` 拥有：\n\n- `ToolSpec → CodeModeToolDefinition` 的纯投影；\n- namespace/name normalization 和 collision table；\n- description 中 code-mode 调用示例的确定性增强；\n- eligible nested tool 收集和排序；\n- nested payload/result 与普通 `ToolPayload` / `ToolOutput` 的转换；\n- tool output image detail 的统一 sanitize。\n\n它不拥有：\n\n- code cell/session lifecycle；\n- JavaScript/Python/Wasm runtime；\n- execute/yield/wait/cancel；\n- remote code-mode host transport；\n- nested tool dispatch、approval 或 durable commit；\n- cell recovery 和 resource accounting。\n\n这些由独立 code-mode runtime 和 Core adapter 拥有。\n\n### 14.2 投影\n\n```rust\npub struct CodeModeToolBinding {\n    pub code_name: CodeModeToolName,\n    pub binding_id: ToolBindingId,\n    pub definition_digest: ToolDefinitionDigest,\n    pub definition: CodeModeToolDefinition,\n}\n```\n\n映射必须：\n\n- 基于 frozen registry snapshot；\n- 对 namespace 和 leaf name 做规范化；\n- collision check 后保存显式 mapping table；\n- 不依赖运行时把 `namespace__name` 反向猜回 `ToolName`；\n- 排除 `Hidden`、`DirectModelOnly`、provider-hosted 和不能安全嵌套的 tool；\n- 保留 input/output schema；\n- 对相同 snapshot 产生稳定排序。\n\n### 14.3 嵌套调用路径\n\n```text\nmodel emits code-mode execute call\n→ Core durably records outer call\n→ code runtime parses/starts cell\n→ nested call references CodeModeToolBinding\n→ adapter materializes ordinary ToolInvocation\n→ normal approval / durable nested Tool Call / ToolService dispatch\n→ ToolOutput converted to code-mode result\n→ cell continues/yields/terminates\n→ outer Tool Result durable commit\n```\n\n每个 nested call 必须拥有独立 `ToolCallId` / `ToolOperationId` 和可审计 lifecycle。Code runtime\n不能拿到裸 executor map 后直接执行副作用。\n\nouter call 与 nested calls 的 transcript 展示可以由客户端折叠，但 durable facts 不能只保留一段\nopaque code output，使内部副作用不可恢复。\n\n当前实现已完成 `CodeModeProjection::from_registry` 的稳定命名、排序和 collision check，并由\n`TurnExecutor::record_code_mode_nested_call` 为 nested call 写入普通 durable Tool Call。caller 记录\nparent call、cell ID 和 runtime call ID；测试随后使用普通 `ToolScheduler` 完成同一个调用。exec/wait、\ncell host、异步 broker、yield/terminate 和 runtime crash recovery 尚未实现，因此当前不能宣称 Code\nMode 可供模型实际执行代码。\n\n### 14.4 结果结构\n\n- structured JSON 保持 JSON，不先 stringify 再 parse；\n- text 保持明确 string；\n- image/resource 使用统一 `ToolContent`；\n- MCP result 保留 content、structured content、is-error 和 provenance；\n- output truncation 在 code-mode budget 与普通 tool output budget 两层都可解释；\n- cell yield 不是 Tool Result terminal outcome；\n- code runtime crash 后，已开始的 nested side effect 仍按普通 uncertain-outcome 规则处理。\n\n## 15. 图片精度\n\n### 15.1 为什么属于共享工具层\n\n图片可以来自 built-in tool、MCP、dynamic owner、code mode 或 future connector。它们最终进入同一\nprovider-neutral model context。若每个 source/provider 分别降级 `Original`，同一图片会得到不一致\n行为，因此 capability normalization 放在 `zeta-tools`。\n\n### 15.2 请求与决策\n\n保留 canonical：\n\n```text\nAuto / Low / High / Original\n```\n\n“未指定”使用具名选择，而不是把 `None` 的语义散落在调用方：\n\n```rust\npub enum ImageDetailSelection {\n    ProviderDefault,\n    Explicit(ImageDetail),\n}\n\npub struct ImageDetailDecision {\n    pub requested: ImageDetailSelection,\n    pub effective: ImageDetailSelection,\n    pub reason: ImageDetailDecisionReason,\n}\n```\n\n`ImageDetailDecisionReason` 至少区分：\n\n- `Supported`；\n- `ProviderDefaultSelected`；\n- `OriginalUnsupportedDowngraded`；\n- `SourcePolicyDowngraded`。\n\n### 15.3 规范化\n\n输入：\n\n```text\nrequested detail\n+ resolved model capability snapshot\n+ source/content policy\n= effective detail + diagnostic\n```\n\n规则：\n\n- model 支持 `Original` 时保留；\n- model 不支持时默认降级为 `ProviderDefault`；未来若引入其他 fallback，必须使用全局具名\n  policy，不能由各 adapter 自由选择；\n- `Low` / `High` 不因 provider default 改写；\n- `Auto` 与 `ProviderDefault` 保持不同语义；\n- provider wire adapter 只编码 `effective` value；\n- model invocation 前再次 sanitize 所有 tool output image，防止 MCP/dynamic/code-mode path 漏检；\n- downgrade 是可观察 diagnostic，不伪装成原精度已发送。\n\n`Original` 只控制模型读取精度，不授予读取本地文件、绕过 Resource ownership 或发送无限原始\nbytes 的权限。\n\n### 15.4 图像安全\n\n共享图片字节的解码、资源限制、缩放、重编码、metadata policy 与 bounded cache 由\n[`zeta-utils-image`](../zeta-rs/utils/image/README.md) 实现；本节拥有跨 Core、Tool、模型与\nprovider 的安全和策略边界，crate README 拥有具体实现契约。\n\n- data URL、remote URL 和 durable attachment reference 使用不同 typed source；\n- 限制 decoded bytes、pixel count、dimensions、frame count 和 MIME；\n- SVG、animated image 和 metadata 使用明确 policy；\n- remote URL fetch 属于 host-owned `zeta-attachments` admission，不在 `zeta-tools`；它使用 direct\n  transport、DNS-time public-address enforcement、逐跳 redirect 复核和 bounded body；\n- 接受后的图片以 content digest + 验证后的媒体元数据进入 Thread；只有 model invocation adapter\n  才把 verified bytes 临时 materialize 为 provider data URL；\n- telemetry 不记录完整 data URL 或图片 bytes；\n- dynamic/MCP adapter 若携带 image detail，它也只是请求；未携带时使用\n  `ImageDetailSelection::ProviderDefault`，最终均由 model capability normalization 决定。\n\n## 16. 供应商适配器\n\n`zeta-tools` 保持 provider-neutral；`zeta-api` 负责：\n\n```text\nToolSpec + ModelCapabilitySnapshot\n  → OpenAI Responses / Chat Completions / Anthropic wire tools\n\nprovider tool call/result\n  → canonical ToolCall / provider-neutral content\n```\n\n共享层负责提供确定性输入，provider 层负责 wire 差异：\n\n| 能力 | `zeta-tools` | `zeta-api` |\n| --- | --- | --- |\n| name/schema validation | canonical 规则 | provider-specific final gate |\n| namespace | binding 与 flatten plan | wire encoding |\n| strict schema | typed intent/capability requirement | provider bool/field |\n| deferred loading | search/loadable spec | native feature encoding 或 host fallback |\n| freeform | canonical format | provider grammar/custom tool DTO |\n| image detail | effective decision | provider string/field |\n| output content | `ToolOutput` | provider request item |\n\nProvider adapter 不能：\n\n- 重新选择 tool alias；\n- 从 live registry 加载 definition；\n- 将 unsupported schema 静默变宽；\n- 把 provider call ID 当 canonical `ToolCallId` 而不经过 validation；\n- 对同一个 Tool Result 产生与其他 provider 不同的业务 success/error 语义。\n\n## 17. 安全、策略与可观察性\n\n### 17.1 信任\n\n以下全部默认不可信：\n\n- Tool description 和 schema；\n- MCP annotation/result；\n- dynamic tool definition/result；\n- Plugin discovery metadata；\n- code-mode source 和 nested arguments；\n- remote image/resource URL；\n- provider 返回的 tool arguments。\n\nDefinition validation 不等于 invocation approval，Plugin signature 不等于 tool 安全，MCP\n`readOnlyHint` 不等于 host `SafeRead`。\n\n### 17.2 Secret 与日志\n\n共享 output/log helper 必须：\n\n- 默认只生成 bounded preview；\n- 区分 model-visible output、audit metadata 和 telemetry preview；\n- 对 credential、authorization header、cookie、data URL 和 filesystem path 执行 redaction；\n- 不把完整 arguments/output 放进普通 error display；\n- 允许 source adapter 提供 typed sensitive-field markers；\n- 不因 serialization error fallback 输出整个 raw payload。\n\n### 17.3 策略输入\n\n`ToolExecutionMetadata` 可以携带 source claim 和 host-derived classification：\n\n```text\nsource claim\n  read-only / destructive / idempotent / open-world\n\nhost classification\n  effect class / approval requirement / retry eligibility / resource conflicts\n```\n\nCore/host policy 只信任后者。Source claim 只用于 diagnostic 和保守收紧。\n\n## 18. 错误契约\n\n至少区分：\n\n```text\nToolDefinitionInvalid\nToolSchemaTooComplex\nToolSchemaUnsupported\nToolNameReserved\nToolNameCollision\nToolBindingStale\nToolUnavailable\nToolArgumentsInvalid\nToolOutputInvalid\nToolOutputTooLarge\nToolExecutionNotStarted\nToolExecutionFailed\nToolOutcomeUncertain\nToolSearchQueryInvalid\nToolSearchOverloaded\nPluginDiscoveryUnavailable\nDynamicToolOwnerUnavailable\nCodeModeUnsupportedTool\nCodeModeBindingCollision\nImageDetailDowngraded\nInternalToolInvariant\n```\n\n职责：\n\n- `zeta-tools` 产生 definition/schema/adapter/binding 错误；\n- source runtime 产生 transport/executor detail；\n- Core 映射 approval/retry/recovery/Turn outcome；\n- App Server 映射 stable RPC error；\n- provider adapter 产生 provider capability/wire error。\n\n内部错误可以保留 source chain，但 public error 不泄露 secret、raw MCP frame、Plugin writable path、\n完整 arguments 或 output。\n\n## 19. 目标目录与公开 API\n\n第一版保持一个 crate，模块默认 private，`lib.rs` 只做精确导出：\n\n```text\nzeta-rs/tools/src/\n├── lib.rs\n├── error.rs\n├── identity.rs\n├── definition/\n│   ├── mod.rs\n│   ├── model.rs\n│   ├── schema.rs\n│   ├── spec.rs\n│   └── validation.rs\n├── registry/\n│   ├── mod.rs\n│   ├── binding.rs\n│   ├── builder.rs\n│   ├── snapshot.rs\n│   └── provenance.rs\n├── execution/\n│   ├── mod.rs\n│   ├── invocation.rs\n│   ├── executor.rs\n│   ├── output.rs\n│   └── outcome.rs\n├── adapters/\n│   ├── mod.rs\n│   ├── protocol.rs\n│   ├── mcp.rs\n│   └── dynamic.rs\n├── search/\n│   ├── mod.rs\n│   ├── index.rs\n│   ├── query.rs\n│   └── loadable.rs\n├── discovery/\n│   ├── mod.rs\n│   ├── model.rs\n│   └── install_request.rs\n├── code_mode/\n│   ├── mod.rs\n│   ├── definition.rs\n│   ├── naming.rs\n│   └── output.rs\n├── image_detail.rs\n└── *_tests.rs\n```\n\n目录只随 vertical slice 创建，不预建空模块。每个 implementation 的测试放在 sibling\n`*_tests.rs`，并使用：\n\n```rust\n#[cfg(test)]\n#[path = \"schema_tests.rs\"]\nmod tests;\n```\n\n目标 public API 只包含：\n\n- validated definition/schema/spec；\n- registry snapshot/binding/provenance；\n- invocation/payload/output/outcome；\n- `ToolExecutor` 和必要 future 类型；\n- MCP/dynamic/protocol pure adapters；\n- search/discovery values；\n- code-mode/image-detail pure helpers。\n\n不公开：\n\n- mutable global registry；\n- executor concrete map；\n- MCP SDK wire DTO；\n- Plugin manager record；\n- provider Responses/Anthropic DTO；\n- Core TurnContext/ThreadController；\n- secret/resource concrete handle；\n- unchecked schema constructor。\n\n若 code-mode runtime、MCP adapter 或 search engine 后续形成独立重量级依赖，应提取独立 crate，\n但 `zeta-tools` 仍只保留窄 bridge contract。第一版不创建 `tools-common`、`tools-types`、\n`tool-runtime` 等无独立消费者的小 crate。\n\n## 20. 迁移顺序\n\n### 阶段 T0：固定共享边界（完成）\n\n- ✅ 创建 `zeta-tools`；\n- ✅ 复用 protocol `ToolName` / `ToolCallId`，不复制 identity；\n- ✅ 建立受限 schema parser、`ToolDefinition`、function/freeform invocation 与基础 output content；\n- ✅ 为当前 protocol `ToolDefinition/ToolResult` 提供 adapter；\n- ✅ 为 dynamic tool 与 wire-neutral MCP projection 提供 definition adapter；\n- ✅ 加入 schema/name/output fixture。\n\n完成条件：OpenAI/Anthropic 当前 function tool request round-trip 不回归，且新 crate 不依赖 Core。\n\n### 阶段 T1：执行器与注册表（主执行链已接入）\n\n- ✅ 定义 `ToolBinding`、`ToolExecutor`、materialized invocation、cancellation context 与 outcome；\n- ✅ built-in process、filesystem 与 apply-patch tool 实现 executor contract；\n- ✅ App Server 构造 immutable registry snapshot，并在 Workspace replacement 时单调递增 generation；\n- ✅ Core `ToolService` adapter 按 frozen binding/runtime key 路由，并把 durable Turn facts 投影为 `ToolInvocation`；\n- ✅ prepared call 在 hot reload 后继续使用原 Tool/Policy generation，sandbox retry 不切换 generation；\n- ✅ definitions 与 execution route 由同一个 immutable registry snapshot 产生，不按 live name 直接猜 service。\n\n完成条件：registry 更新不能劫持 in-flight call，stale binding 被明确拒绝。\n\n### 阶段 T2：MCP 与动态适配器（MCP/dynamic 与 durable provenance 主链完成）\n\n- ✅ `zeta-mcp` 输出 `McpToolProjection` 并建立 immutable catalog/binding；\n- ✅ 接通 MCP schema/name/result conversion 与调用取消；\n- ✅ MCP App Server/Core adapter 接入逐次 approval、durable commit 和 unknown outcome；\n- ✅ MCP host integration 从 App Server 私有模块提取到 `zeta-rs/ext/mcp`；App Server 仅组合和 safe-point replacement；\n- ✅ dynamic definition 进入共享 registry，execution 经过 approval、durable interaction 与 Tool Result commit；\n- ✅ dynamic request 固定 definition digest、call id、name 与 arguments；同名新定义不能认领旧 response；\n- ✅ 多连接 delivery 还按 initialize capability 的 exact dynamic tool name 选 owner；\n- ✅ 已投递 dynamic owner 断连/退订后不重新分配，按 unknown outcome 且不重试收口；\n- ✅ Tool Call durable provenance 保存 registry generation、definition digest、source chain 与 caller；\n- 部分具备：MCP transport-lost 已覆盖；dynamic owner-disconnect 已覆盖 broker/Core continuation，\n  仍需补持久化重启的完整端到端 fixture。\n\n完成条件：MCP/dynamic 工具都不能绕过普通 approval、commit 和 recovery。\n\n### 阶段 T3：工具搜索与 Plugin 发现\n\n- ✅ deferred exposure 和 deterministic search index；\n- ✅ host tool-search vertical slice；\n- ✅ Core 根据 successful durable search result 在下一 model step 增量暴露定义；\n- ✅ `ReadOnlyToolContributor` 产出的 host extension executor 进入共享 registry/policy/runtime（当前包括统一的 `skills-read`）；\n- ✅ `CapabilityToolContributor` 冻结 exact network/credential scope，并通过普通一次性 approval 执行；\n- ✅ `ext/web-search` 提供 eager `web_search`、可注入 backend 和默认关闭门禁；\n- ✅ `zeta-connectors` 分离 Connector account lifecycle，`ext/connectors` 提供 discovery 与 ready MCP binding projection；\n- ✅ enabled connector/MCP catalog 与 local/dynamic/extension port 做统一 collision check；\n- ✅ Plugin/Connector catalog-only discovery value、generation-bound snapshot 与 local Plugin projection；\n- ✅ typed install/enable/connect request 和通用 client capability filtering；\n- ✅ local Plugin package store 已完成 staging、copy-time identity check、digest 复验和 content-addressed\n  atomic promotion；\n- 尚未完成：Connector OAuth/connect/revoke、默认生产 Web Search provider、Agent helper、用户确认\n  interaction 与 Plugin 安装/启用 authority；dynamic owner 的 exact\n  tool-name filtering 已完成。\n\n完成条件：search 只返回当前 registry 工具，Plugin discovery 不产生可执行 binding，install 无隐式\ngrant。\n\n当前受控检索回归包含 20 个代表工具、140 个 collision/distractor 工具和中英文 semantic cases：\n\n| 路径 | Corpus | Top-1 | Top-3 | 说明 |\n| --- | ---: | ---: | ---: | --- |\n| BM25 | 20 | 95% | 100% | 默认、离线、零模型成本 |\n| BM25 | 160 | 91% | 100% | 验证大 catalog 下的 lexical precision |\n| BM25 | 10 个中文/同义 case | 0% | 0% | 明确暴露纯词法边界 |\n| controlled semantic + RRF | 同上 | 100% | 100% | 验证 hybrid 编排；不代表任何具体 embedding 模型质量 |\n\n真实 embedding 的模型质量必须按具体 provider/model 另做可重复 eval；CI 中的 controlled semantic\nranking 只证明 gate、document embedding、cosine ranking 和 hybrid merge 接线不会退化成伪 fallback。\n\n### 阶段 T4：代码模式\n\n- ✅ code-mode definition projection、naming/collision table；\n- ✅ nested call 写入 durable binding 并重新进入普通 ToolScheduler path；\n- ✅ structured/MCP/image result adapter；\n- 尚未完成：exec/wait host、cell lifecycle、yield/cancel/uncertain outcome；\n- 部分具备：nested binding 已保存 outer call identity，outer call 自身的 durable runtime lifecycle 尚未完成。\n\n完成条件：code mode 的任意 nested side effect 都可审批、可审计、可取消，并具有准确恢复语义。\n\n### 阶段 T5：图片精度与供应商能力收敛\n\n- ✅ 统一 `ImageDetailSelection/Decision`；\n- ✅ model invocation 最终 capability gate；\n- ✅ provider adapter 只接收 effective detail；\n- ✅ Tool Executor/MCP/dynamic output 统一转为 durable structured content 并 sanitize；\n- 部分具备：downgrade decision fixture 已有；data URL byte/pixel/MIME 限制和跨 provider 端到端\n  wire fixture 尚未完成。\n\n完成条件：unsupported `Original` 不会到达 provider wire，且调用方能解释实际使用的 detail。\n\n## 21. 验证门\n\n除 workspace 常规检查外，必须覆盖：\n\n- invalid/reserved/colliding ToolName；\n- schema depth/size/property/enum/ref bomb；\n- schema canonicalization 和 digest stability；\n- required/additionalProperties/provider strict conversion；\n- registry deterministic build、generation、old-snapshot drain；\n- stale binding 与同名新 executor 不会互换；\n- model ToolCall → materialized invocation → durable item identity；\n- tool-level error、not-started 和 uncertain outcome 分类；\n- output bytes/content/schema/MIME/resource limit；\n- MCP missing properties、outputSchema、isError、protocol error 和 transport loss；\n- dynamic owner disconnect、late response 和 RequestId/ToolCallId mismatch；\n- deferred search ranking、limit、generation 和 forged result；\n- Plugin discovery 与 tool search 隔离；\n- install/enable/connect 无隐式 grant；\n- code-mode namespace/name collision、nested approval 和 result shape；\n- structured JSON 不被意外 stringify；\n- image Auto/Low/High/Original capability matrix；\n- unsupported Original 的稳定降级；\n- telemetry 不泄露 secret、data URL、raw MCP frame 或完整敏感 payload；\n- 所有新 test module 使用 sibling `*_tests.rs`。\n\n协议或 provider contract 变化还必须重新生成并提交 JSON Schema、TypeScript 和对应 fixtures。\n\n## 22. 固定决策\n\n- `zeta-tools` 是共享类型与纯适配层，不是第二个 Core；\n- `ToolName` 是 model-facing alias，不是 source/executor identity；\n- `ToolCall` 与 materialized `ToolInvocation` 是不同生命周期；\n- Core 的 `ToolService` port 与工具作者的 `ToolExecutor` interface 分开；\n- definition、binding、registry snapshot 在 model safe point 冻结；\n- in-flight call 永远按原 binding 执行，不按 name 重查 live registry；\n- MCP wire/lifecycle 属于 `zeta-mcp`，MCP-to-tool pure conversion 属于 `zeta-tools`；\n- Plugin authority/install/grant 属于 `zeta-plugins`，discovery DTO 不拥有 mutation；\n- tool search 只搜索已安装、已授权、当前可用的 deferred tools；\n- Plugin discovery 不能直接产生 executable ToolDefinition；\n- dynamic tool 不绕过 schema、registry、approval 或 durable interaction；\n- code mode 是普通工具调用的另一种表示，不是执行旁路；\n- nested code-mode tools 继续使用普通 binding、approval、commit 和 recovery；\n- tool output 使用 typed content/structured value，不让各 provider/executor 自定义 wire JSON；\n- `Original` 图片精度只在 model capability 支持时保留；\n- provider adapter 只编码 effective tool/image value，不重新做 host policy；\n- 所有 schema、description、arguments、outputs 和 discovery metadata 默认不可信；\n- `zeta-tools` 不读取 Config、Plugin/MCP live manager、credential、ThreadStore 或 provider client；\n- 新 public trait 必须带角色和实现约束 doc comment；\n- 新 API 不使用让调用方写出 `foo(false)` / `bar(None)` 的含糊参数；\n- 模块默认 private，`lib.rs` 精确导出，implementation tests 放 sibling 文件。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-当前仓库审计",
        "title": "2. 当前仓库审计"
      },
      {
        "depth": 2,
        "id": "3-三层工具契约",
        "title": "3. 三层工具契约"
      },
      {
        "depth": 3,
        "id": "31-zeta-protocol-继续拥有",
        "title": "3.1 zeta-protocol 继续拥有"
      },
      {
        "depth": 3,
        "id": "32-zeta-tools-拥有",
        "title": "3.2 zeta-tools 拥有"
      },
      {
        "depth": 3,
        "id": "33-zeta-core-继续拥有",
        "title": "3.3 zeta-core 继续拥有"
      },
      {
        "depth": 2,
        "id": "4-依赖方向与组合",
        "title": "4. 依赖方向与组合"
      },
      {
        "depth": 3,
        "id": "41-当前本地工具来源运行时",
        "title": "4.1 当前本地工具来源运行时"
      },
      {
        "depth": 2,
        "id": "5-身份来源与绑定",
        "title": "5. 身份、来源与绑定"
      },
      {
        "depth": 3,
        "id": "51-name-不是执行身份",
        "title": "5.1 Name 不是执行身份"
      },
      {
        "depth": 3,
        "id": "52-绑定",
        "title": "5.2 绑定"
      },
      {
        "depth": 3,
        "id": "53-durable-来源",
        "title": "5.3 Durable 来源"
      },
      {
        "depth": 2,
        "id": "6-工具定义模式与规格",
        "title": "6. 工具定义、模式与规格"
      },
      {
        "depth": 3,
        "id": "61-叶级定义",
        "title": "6.1 叶级定义"
      },
      {
        "depth": 3,
        "id": "62-工具模式",
        "title": "6.2 工具模式"
      },
      {
        "depth": 3,
        "id": "63-聚合规格",
        "title": "6.3 聚合规格"
      },
      {
        "depth": 3,
        "id": "64-暴露范围",
        "title": "6.4 暴露范围"
      },
      {
        "depth": 2,
        "id": "7-注册表与快照",
        "title": "7. 注册表与快照"
      },
      {
        "depth": 3,
        "id": "71-注册表输入",
        "title": "7.1 注册表输入"
      },
      {
        "depth": 3,
        "id": "72-快照",
        "title": "7.2 快照"
      },
      {
        "depth": 3,
        "id": "73-安全点与排空",
        "title": "7.3 安全点与排空"
      },
      {
        "depth": 2,
        "id": "8-工具-call调用与执行接口",
        "title": "8. 工具 Call、调用与执行接口"
      },
      {
        "depth": 3,
        "id": "81-三种调用值",
        "title": "8.1 三种调用值"
      },
      {
        "depth": 3,
        "id": "82-载荷",
        "title": "8.2 载荷"
      },
      {
        "depth": 3,
        "id": "83-具体化调用",
        "title": "8.3 具体化调用"
      },
      {
        "depth": 3,
        "id": "84-执行器接口",
        "title": "8.4 执行器接口"
      },
      {
        "depth": 3,
        "id": "85-核心消费方端口",
        "title": "8.5 核心消费方端口"
      },
      {
        "depth": 2,
        "id": "9-输出与结果",
        "title": "9. 输出与结果"
      },
      {
        "depth": 3,
        "id": "91-面向模型的输出",
        "title": "9.1 面向模型的输出"
      },
      {
        "depth": 3,
        "id": "92-执行结果",
        "title": "9.2 执行结果"
      },
      {
        "depth": 3,
        "id": "93-输出适配器",
        "title": "9.3 输出适配器"
      },
      {
        "depth": 2,
        "id": "10-mcp-工具转换",
        "title": "10. MCP 工具转换"
      },
      {
        "depth": 3,
        "id": "101-两段式适配器",
        "title": "10.1 两段式适配器"
      },
      {
        "depth": 3,
        "id": "102-定义转换",
        "title": "10.2 定义转换"
      },
      {
        "depth": 3,
        "id": "103-结果转换",
        "title": "10.3 结果转换"
      },
      {
        "depth": 2,
        "id": "11-动态工具",
        "title": "11. 动态工具"
      },
      {
        "depth": 3,
        "id": "111-定义适配器",
        "title": "11.1 定义适配器"
      },
      {
        "depth": 3,
        "id": "112-执行",
        "title": "11.2 执行"
      },
      {
        "depth": 3,
        "id": "113-运行时注册",
        "title": "11.3 运行时注册"
      },
      {
        "depth": 2,
        "id": "12-工具搜索",
        "title": "12. 工具搜索"
      },
      {
        "depth": 3,
        "id": "121-搜索对象",
        "title": "12.1 搜索对象"
      },
      {
        "depth": 3,
        "id": "122-搜索值",
        "title": "12.2 搜索值"
      },
      {
        "depth": 3,
        "id": "123-索引与排序",
        "title": "12.3 索引与排序"
      },
      {
        "depth": 3,
        "id": "124-加载流程",
        "title": "12.4 加载流程"
      },
      {
        "depth": 2,
        "id": "13-plugin-与-connector-发现",
        "title": "13. Plugin 与 Connector 发现"
      },
      {
        "depth": 3,
        "id": "131-与工具搜索分离",
        "title": "13.1 与工具搜索分离"
      },
      {
        "depth": 3,
        "id": "132-共享发现值",
        "title": "13.2 共享发现值"
      },
      {
        "depth": 3,
        "id": "133-面向-agent-的辅助程序工具",
        "title": "13.3 面向 Agent 的辅助程序工具"
      },
      {
        "depth": 2,
        "id": "14-代码模式",
        "title": "14. 代码模式"
      },
      {
        "depth": 3,
        "id": "141-定位",
        "title": "14.1 定位"
      },
      {
        "depth": 3,
        "id": "142-投影",
        "title": "14.2 投影"
      },
      {
        "depth": 3,
        "id": "143-嵌套调用路径",
        "title": "14.3 嵌套调用路径"
      },
      {
        "depth": 3,
        "id": "144-结果结构",
        "title": "14.4 结果结构"
      },
      {
        "depth": 2,
        "id": "15-图片精度",
        "title": "15. 图片精度"
      },
      {
        "depth": 3,
        "id": "151-为什么属于共享工具层",
        "title": "15.1 为什么属于共享工具层"
      },
      {
        "depth": 3,
        "id": "152-请求与决策",
        "title": "15.2 请求与决策"
      },
      {
        "depth": 3,
        "id": "153-规范化",
        "title": "15.3 规范化"
      },
      {
        "depth": 3,
        "id": "154-图像安全",
        "title": "15.4 图像安全"
      },
      {
        "depth": 2,
        "id": "16-供应商适配器",
        "title": "16. 供应商适配器"
      },
      {
        "depth": 2,
        "id": "17-安全策略与可观察性",
        "title": "17. 安全、策略与可观察性"
      },
      {
        "depth": 3,
        "id": "171-信任",
        "title": "17.1 信任"
      },
      {
        "depth": 3,
        "id": "172-secret-与日志",
        "title": "17.2 Secret 与日志"
      },
      {
        "depth": 3,
        "id": "173-策略输入",
        "title": "17.3 策略输入"
      },
      {
        "depth": 2,
        "id": "18-错误契约",
        "title": "18. 错误契约"
      },
      {
        "depth": 2,
        "id": "19-目标目录与公开-api",
        "title": "19. 目标目录与公开 API"
      },
      {
        "depth": 2,
        "id": "20-迁移顺序",
        "title": "20. 迁移顺序"
      },
      {
        "depth": 3,
        "id": "阶段-t0固定共享边界完成",
        "title": "阶段 T0：固定共享边界（完成）"
      },
      {
        "depth": 3,
        "id": "阶段-t1执行器与注册表主执行链已接入",
        "title": "阶段 T1：执行器与注册表（主执行链已接入）"
      },
      {
        "depth": 3,
        "id": "阶段-t2mcp-与动态适配器mcpdynamic-与-durable-provenance-主链完成",
        "title": "阶段 T2：MCP 与动态适配器（MCP/dynamic 与 durable provenance 主链完成）"
      },
      {
        "depth": 3,
        "id": "阶段-t3工具搜索与-plugin-发现",
        "title": "阶段 T3：工具搜索与 Plugin 发现"
      },
      {
        "depth": 3,
        "id": "阶段-t4代码模式",
        "title": "阶段 T4：代码模式"
      },
      {
        "depth": 3,
        "id": "阶段-t5图片精度与供应商能力收敛",
        "title": "阶段 T5：图片精度与供应商能力收敛"
      },
      {
        "depth": 2,
        "id": "21-验证门",
        "title": "21. 验证门"
      },
      {
        "depth": 2,
        "id": "22-固定决策",
        "title": "22. 固定决策"
      }
    ],
    "searchText": " 计划物理位置：zeta-rs/tools/ Rust crate：zetatools 当前状态：T1 主链完成，T2 MCP/dynamic 主链完成；MCP host runtime 已提取到 ext/mcp。T3 的 Tool Search、capability-bearing extension、Connector discovery projection、Plugin catalog projection 与 typed discovery request 已接入共享契约；ext/web-search 已提供默认关闭、宿主注入 backend 后才注册的 websearch 工具。Tool Call 已持久化 registry generation、definition digest、source chain 与 direct/code-mode caller；Tool Result 已能持久化结构化 图片内容。Code Mode 当前完成冻结 registry 投影和 durable nested-call bridge，尚无 cell/runtime host； Plugin local content store 已落地，但安装 authority、activation、grant，以及 dynamic owner 断连后的持久化重启 端到端 fixture 仍未完成。 Canonical value 与 durable Tool Item：protocol.md Core 调度、approval 与恢复：core.md MCP client runtime：mcp.md Plugin authority：plugins.md Config 与 runtime snapshot：config.md Provider wire adapter：zeta-api.md 快速理解 工具系统把本地工具、MCP 和动态来源转换成统一、可验证的能力目录；它定义和绑定工具，但最终 授权、执行顺序与持久化仍由相邻系统负责。 读者首先会问 直接答案 深入阅读 --- --- --- 不同来源的工具为什么能统一调用？ 每个来源先转换成统一定义、规格、绑定和调用值 三层工具契约 工具名称就是执行身份吗？ 不是；稳定身份还包含来源、绑定和快照 generation 身份、来源与绑定 Agent 当前能看到哪些工具？ 由不可变注册表快照和暴露范围决定，运行中不会被静默改写 注册表与快照 谁决定工具能不能执行？ 权限系统决定授权；Core 调度；工具执行器落实调用 当前本地工具来源 当前完成到哪里？ 统一 registry/executor/search、durable provenance 和结构化图片主链已落地；Plugin 安装 authority 与 Code Mode runtime 尚未完成 当前仓库审计 1. 结论 zeta-tools 是 Zeta 工具子系统的共享类型与纯适配层。它定义一个工具如何被描述、暴露、搜索、 绑定、调用和返回结果，并提供 MCP、dynamic tool、code mode 与图片精度等跨模块转换能力。 它解决的是： ``text 不同工具来源 local / MCP / dynamic / connector / future extension │ ▼ 统一 host-side ToolDefinition / ToolSpec / ToolBinding │ registry + search + adaptation │ ▼ ToolInvocation ──► ToolExecutor ──► ToolExecutionOutcome │ ▼ Core durable Tool Call / Tool Result lifecycle ` 长期职责固定为： - provider-neutral 的 ToolDefinition、ToolSpec、schema 和 exposure 类型； - model-emitted ToolCall 到 host-materialized ToolInvocation 之间的共享值； - executable tool 的 ToolExecutor / ToolOutput 接口； - immutable ToolRegistrySnapshot、binding、definition digest 与来源信息； - MCP descriptor/result 到 Zeta 工具值的纯转换； - dynamic tool definition/result 的校验与转换； - deferred tool loading、tool search 索引和值类型； - Plugin/Connector 发现结果和 install/enable/connect 请求的纯展示值； - code mode 的工具定义投影、命名、nested-call/result 适配； - tool output image 的 detail capability 检查、标准化和降级； - schema、输出、日志预览和 provider capability 的共享校验规则。 zeta-tools 不拥有： - model → tool → model loop、approval、并行计划、retry 或 UnknownOutcome 决策； - Thread/Turn reducer、durable append、Tool Call/Result commit 或 recovery； - MCP transport、JSON-RPC session、process supervision、OAuth 或 reconnect； - Plugin 安装、启用、grant、版本、digest、marketplace 或 package store； - local tool、MCP server、browser、terminal 或 code-mode runtime 的具体 I/O； - sandbox、filesystem/network enforcement、credential materialization； - provider HTTP DTO、Responses API/Anthropic wire encoding； - App Server connection owner、interaction delivery 或客户端 UI。 一句话边界： zeta-tools 统一“工具在 host 中是什么以及如何跨表示转换”；Core 决定“何时、按什么策略调用”； source runtime 决定“如何完成具体 I/O”。 2. 当前仓库审计 当前已有可复用地基： - zeta-protocol 已定义 ToolName、ToolCallId、ToolDefinition、ToolCall、ToolResult、 ContentPart、ImageDetail 和 durable ThreadItem::ToolCall/ToolResult； - zeta-protocol 已定义 DynamicToolSpec、DynamicToolCall、DynamicToolResponse 和对应 Agent interaction； - zeta-core 已定义 ToolService::prepare/execute port，并完成 policy-gated、可恢复的顺序 model → tool → model vertical slice； - zeta-core::ContextAssembler 已能重建 Tool Call/Result pairing； - zeta-api 已分别把 canonical function tool 和图片 detail 转成 OpenAI Responses、 Chat Completions 与 Anthropic Messages wire payload； - zeta-shell-command、zeta-file-system 与 zeta-apply-patch 各自提供独立 executor；App Server 已用 ToolExecutorRuntime 把它们接入 durable facts、policy authority、streaming output 与统一 registry，旧 read/write/edit 名称只保留 hidden migration route； - zeta-mcp 的 tools-only catalog/binding runtime 与 zeta-mcp-extension 的 host/Core adapter 已进入共享 registry/search projection；client-hosted dynamic tool 已进入同一 approval、durable interaction、 exact owner 与 unknown-outcome 链； - zeta-extension-api::ReadOnlyToolContributor 和显式声明 network/credential scope 的 CapabilityToolContributor 已进入同一 registry/policy/runtime；当前 skills-read 走前者， websearch 走后者并要求 exact one-time approval。zeta-plugins 已把验证后的本地 catalog 投影为不可执行的 discovery snapshot，zeta-tools 已定义 generation-bound install/enable/connect request；zeta-plugins 已能把 local package stage、复验并原子 promote 到 content-addressed store，实际安装 authority 仍处于 Proposed； - App Server 以 frozen ToolBinding 的 runtime key 执行，不再在执行阶段按 live tool name 猜 source service；model invocation 会同时冻结 definitions 与 generation-bound binder，hot reload 只影响后续 model safe point，已绑定调用保留原 generation 和 policy 直到排空。 - Core 在 durable Tool Call 中保存 registry generation、definition digest、source chain 和 caller；恢复 时 exact binding 不匹配会失败关闭，不会按同名新工具重放。Shell Turn 和 Code Mode nested call 也走 同一绑定入口； - Tool Executor、MCP 和 dynamic output 已转换为结构化 ContentPart，durable Tool Result 保留可选 content，旧纯文本记录继续兼容读取；模型调用边界会再次按 provider capability 清理 Original。 当前剩余问题集中在尚未收口的跨边界能力，而不是再增加一套 registry： - dynamic owner 断连已经 fail closed，但还缺“持久化后重启恢复”的完整 App Server fixture； - Plugin/Connector 已有 typed discovery，Plugin manifest 可声明一个 Connector 到其 MCP contribution 的绑定；zeta-connectors 拥有 account connection state，zeta-connectors-extension 拥有 Plugin projection、SQLite authority 与 API-token credential orchestration，ready binding 已接入 MCP/App Server hot composition。local package store 也已落地。仍缺 OAuth/refresh/远端 revoke、用户确认交互和 Plugin activation，不能用 executable registry 反向代替 package authority； - zeta-web-search-extension 已有 bounded request、executor、JSON HTTP backend 与 App Server opt-in 安装口； 当前没有默认生产 Search provider 或 credential UI，宿主未注入 backend 时工具完全不可用； - provider adapter 可能分别决定 namespace flattening、strict schema 和 image detail fallback； - tool search、Plugin discovery 和 install request 容易被混成一个有隐式副作用的“发现服务”； - Code Mode 已有确定性投影和 durable nested-call bridge，但还缺 exec/wait、cell lifecycle、host transport 与 broker；runtime 若直接持有 executor，仍会绕过普通 tool 的 approval、durable commit 和 tracing。 后续扩展必须继续经过现有 zeta-tools 窄共享契约和 App Server composition root，不能在 Plugin、 Connector、code mode 或 provider adapter 内另建可执行 registry。 本方案参考本地 ../codex/codex-rs/tools/ 已验证的提取方向：共享 definition/spec、MCP/dynamic adapter、tool search/discovery、code-mode bridge、image-detail normalization 与 executor contract 可以离开 Core。Zeta 不直接照搬其中的 Responses API DTO 或 Core runtime context： provider wire 继续属于 zeta-api，durable scheduling/recovery 继续属于 zeta-core。 3. 三层工具契约 工具系统固定分为三层： 层 Owner 典型类型 生命周期 --- --- --- --- Canonical product contract zeta-protocol ToolName、ToolCallId、durable Tool Item、dynamic interaction 可序列化、可持久化 Host tool contract zeta-tools ToolDefinition、ToolSpec、binding、invocation、output、executor process-local 或 snapshot-scoped Execution orchestration zeta-core ToolScheduler、ToolService port、approval、retry、recovery Turn/operation-scoped 3.1 zeta-protocol 继续拥有 - 所有跨进程和 durable identity； - model output 中 canonical ToolCall 的语义； - Thread transcript 中 Tool Call/Result 事实； - dynamic tool 的 Agent request/response envelope； - provider-independent message content 和图片 detail 枚举； - App Server 客户端确实需要读取的稳定工具值。 zeta-tools 不重新定义 ToolName、ToolCallId 或另一套 durable Tool Item。 3.2 zeta-tools 拥有 - 比 model request DTO 更完整的 host-side definition/spec； - schema normalization、definition digest 和 name/binding 规则； - executor authoring contract； - source-neutral registry、search、code mode 和 output adapter； - 从 source-specific projection 到 host tool contract 的纯函数。 当前 zeta-protocol::ToolDefinition 在迁移期保留为 model invocation 的最小 canonical value。 zeta-tools::ToolDefinition 通过显式、可失败的 adapter 转成它。两者不能依赖同名 re-export 长期共存；迁移完成后应根据真实消费者选择以下一个方向： 1. protocol 类型明确命名为 ModelToolDefinition，host 类型保留 ToolDefinition；或 2. 如果完整 host definition 也确实需要跨进程共享，再将纯子集提升到 protocol。 不能用 pub use 假装两个不同生命周期的类型语义相同。 3.3 zeta-core 继续拥有 - 当前 invocation 使用哪个 registry snapshot； - model call 后的 schema/availability 校验时机； - sequential/parallel plan； - approval、deadline、cancellation 与 resource conflict； - Tool Call intent 的 durable commit； - result ordering、retry 和 uncertain outcome 的最终映射； - completion 的 operation/incarnation 校验； - next model invocation 与 Turn terminal decision。 Core 的 ToolService 是 consumer-owned port；它可以由外层 ToolRegistryAdapter 实现，但不能 被 ToolExecutor 取代。前者是 Core 的编排入口，后者是工具作者的执行接口。 4. 依赖方向与组合 目标依赖： `text zeta-protocol ▲ │ canonical IDs/content zeta-tools shared types / pure adapters ▲ ▲ ▲ │ │ │ local tool crates zeta-mcp code-mode adapter shell-command / file-system / apply-patch \\ / \\ / App Server composition / \\ ▼ ▼ ▼ Core ToolService Plugin catalog zeta-api adapter projection provider wire │ ▼ zeta-core ` 允许： - zeta-tools → zeta-protocol； - zeta-core → zeta-tools + zeta-protocol； - zeta-mcp → zeta-tools； - zeta-shell-command → zeta-tools + zeta-tool-executor + zeta-sandboxing； - local App Server composition → zeta-install-context + zeta-shell-command； - zeta-file-system → zeta-tools + zeta-sandboxing； - zeta-tui → zeta-file-search；后者提供只读路径索引，不依赖 zeta-tools，也不注册为模型 Tool； - catalog/runtime manager 可依赖 zeta-file-watcher 获取 coarse invalidation hint；后者不依赖 zeta-tools，不读取文件内容，也不注册为模型 Tool； - zeta-apply-patch → zeta-tools + zeta-sandboxing； - zeta-action-policy → zeta-execpolicy + zeta-sandboxing； - zeta-execpolicy 不依赖 sandbox、Core、Tool 或配置 I/O； - zeta-auto-review → zeta-action-policy + zeta-sandboxing； - local process executor 可依赖 zeta-sandboxing 与当前平台 backend；Linux backend 仅通过 zeta-bwrap 构造 Bubblewrap argv； - zeta-api → zeta-tools + zeta-protocol； - App Server 组合 Tool registry、source runtime、policy 和 Core port。 Sandbox backend 的内部调度、平台 crate 边界和 fail-closed 规则见 sandboxing.md。这里的 sandbox manager 不是 Core ToolScheduler。 Action classifier、exact grant 与最终 execution decision 见 auto-review.md。 禁止： `text zeta-tools → zeta-core zeta-tools → zeta-mcp live runtime zeta-tools → zeta-plugins authority zeta-tools → zeta-app-server zeta-tools → provider HTTP client zeta-tools → credential or secret store zeta-tools → ThreadStore / SessionStore ` MCP adapter 的公开输入使用 zeta-tools 自己的纯 McpToolProjection，不在 public API 暴露 某个 MCP SDK 的 wire DTO。zeta-mcp 负责从当前 protocol revision 的 wire model 产生 projection； zeta-tools 负责从 projection 产生 host definition。这样 MCP SDK 升级不会迫使所有工具消费者 一起升级。 4.1 当前本地工具来源运行时 本地 Workspace 工具不再有聚合 crate。每个 crate 只提供一个独立 ToolExecutor，可由 App Server composition root 按 policy 单独注册： Crate / tool name 可做的事 明确不做的事 --- --- --- zeta-shell-command / shell-command 在批准的相对 Workspace 工作目录执行显式 program/arguments；复用 zeta-tool-executor 的 approval、timeout 和输出上限 不隐式启动 shell，不绕过 process policy zeta-file-system / file-system 读取 UTF-8 文件、列目录、读取 metadata 不写入、删除、移动或重命名 zeta-apply-patch / apply-patch 预检后更新、添加或删除普通文件；replacement 按文件原子写入 不接受绝对/.. 路径，不直接提供任意写入 API 三者均在构造时固定 ToolEnvironmentId + WorkspaceRoot，要求它与 ToolExecutionContext.environmentid 一致，且只接受与自身 ToolDefinition digest 相符的冻结 binding。apply-patch 在所有 hunk 校验完成前不写入； 若多文件 commit 中途失败，返回 OutcomeUncertain，由 Core 决定后续恢复语义。 zeta-file-system 还提供 host-only 的 findnearestancestorwithmarkers，用于从一个本地路径 向上发现最近的项目 marker。它不是模型 Tool，不读取 marker 配置，也不施加 WorkspaceRoot containment；调用方仍拥有项目根语义和搜索边界。实现与错误策略由 zeta-rs/file-system/README.md 维护。 搜索分成模型侧命令搜索与交互式路径搜索： Surface 所有权 模型可见 --- --- --- zeta-shell-command + rg 内容搜索、rg --files 路径枚举和 -g glob filtering shell-command Tool zeta-file-search ignore-aware 路径索引、fuzzy matching、PathSearchHandle 和 CLI 否 zeta-file-watcher 多订阅者路径失效提示、missing-path fallback、throttle/debounce 与 overflow rescan hint 否 模型侧不注册独立 glob、grep 或 text-search Tool：内容搜索使用 rg PATTERN，路径枚举 使用 rg --files，glob 作为 -g 参数传给 rg。交互式路径搜索契约由 zeta-rs/file-search/README.md 维护；TUI 直接持有 PathSearchHandle，不启动 CLI，Core 也不把路径搜索注册成 Tool。 zeta-file-watcher 同样不是搜索或读取接口。它只把 OS mutation/error 转成 PathsChanged/RescanRequired；consumer 必须重新扫描并校验 own state。其 ref-count、路径匹配、 RAII 与 failure contract 由 zeta-rs/file-watcher/README.md 维护。 当前 local App Server 启动时通过 zeta-install-context 按 ZETARGPATH、package zeta-path/、Zeta executable 同目录、host PATH 的顺序生成 rg candidates，再由 zeta-shell-command 验证并冻结 canonical executable identity；未找到时启用本地工具的 composition 直接失败。canonical release package 由 scripts/buildzetapackage.py 按 thirdparty/ripgrep/runtime-lock.json 下载并校验 target-specific ripgrep archive，把 executable 放到 zeta-path/rg .exe ；源码开发启动仍可使用 ZETARGPATH 或 host PA"
  },
  {
    "slug": "skills",
    "sourcePath": "docs/skills.md",
    "title": "Skill 指令系统",
    "description": "Skill 是按任务逐步加载的工作方法和参考资料，不是工具权限。Agent 可以按照 Skill 建议行动， 但任何脚本、网络或文件操作仍经过正常工具、权限和沙箱流程。",
    "group": "Agent 与运行时",
    "markdown": "> 物理位置：`zeta-rs/skills/`\n> Rust crate：`zeta_skills`\n> 当前状态：Phase S0、S1 显式选择、模型按需读取、通用 package resource、有界文本模型切片与 binary asset Resource materialization 已实现；Renderer preview、script execution adapter 与 S3–S4 仍为 Proposed。TUI 与 Desktop 已把\n> 可直接调用的 Skill 投影为统一斜杠面板中的 `/name`；`/skills` 只承担目录管理。\n> Crate 实现契约：[`zeta-rs/skills/README.md`](../zeta-rs/skills/README.md)\n> Runtime extension 实现契约：[`zeta-rs/ext/skills/README.md`](../zeta-rs/ext/skills/README.md)\n> 通用扩展生命周期：[`zeta-rs/ext/extension-api/README.md`](../zeta-rs/ext/extension-api/README.md)\n> Core architecture：[`core.md`](core.md)\n> Agent runtime：[`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md)\n> Config authority 与 runtime snapshot 接入：[`config.md`](config.md)\n> Instructions/Skills/Agents 领域划分与外部导入：[`agent-customizations.md`](agent-customizations.md)\n> Plugin 分发边界：[`plugins.md`](plugins.md)\n> MCP runtime：[`mcp.md`](mcp.md)\n\n> 外部格式核对日期：2026-07-28。Skill package 以\n> [Agent Skills specification](https://agentskills.io/specification) 为兼容基线；Zeta 自己的\n> source、trust、activation 和执行权限语义由本文定义。\n\n## 快速理解\n\nSkill 是按任务逐步加载的工作方法和参考资料，不是工具权限。Agent 可以按照 Skill 建议行动，\n但任何脚本、网络或文件操作仍经过正常工具、权限和沙箱流程。\n\n| 发生的事情 | 加载什么 | 安全边界 |\n| --- | --- | --- |\n| 启动或刷新 Skill 目录 | 只读取名称、描述和来源等元数据 | 不把全部正文塞入上下文 |\n| 用户输入 `/commit` 等 Skill 命令 | 接受 Turn 时完整读取对应 `SKILL.md` | 斜杠面板只持有元数据和精确 `SkillRef` |\n| 用户打开 `/skills` | 浏览、启用、禁用和查看诊断 | 不从管理面板直接执行 Skill |\n| 模型按需选择 Skill | 模型先看到有界元数据目录，再调用 `skills-read` 加载正文 | 后端不做关键词分类，也不暴露本地路径 |\n| 正文引用参考资料 | 只在当前任务确实需要时读取 | 路径必须受来源目录约束 |\n| Skill 建议运行脚本或工具 | 转成普通工具请求 | 不授予文件、网络、凭据或沙箱绕过能力 |\n| Plugin 提供 Skill | 保留 Plugin、版本和内容摘要来源 | Plugin 启用不等于 Skill 自动执行 |\n\n## 1. 结论\n\n`zeta-skills` 是 Zeta 的 Skill 文件、catalog、解析和 exact activation 底层 authority；\n`zeta-skills-extension` 负责来源组合、选择策略、watcher 和向 Core 注入上下文。Skill 是一个以\n`SKILL.md` 为入口的指令目录，可以附带 references、scripts 和 assets。它帮助 Agent 判断“这类\n任务应采用什么工作流”，但本身不是可执行权限、tool implementation、MCP server 或 Plugin。\n\n核心原则：\n\n```text\n发现时只加载 metadata\n→ 任务匹配后完整加载 SKILL.md\n→ 只有真实需要时读取 references/scripts/assets\n```\n\nSkill 内容是带来源的外部 instruction：\n\n- 它低于 Zeta system/developer/product policy；\n- 它不能授权 tool、network、filesystem、credential 或 sandbox bypass；\n- 它可以建议使用工具或脚本，但实际执行仍经过标准 tool loop；\n- 它不能通过“忽略上层规则”改变 instruction precedence；\n- 未激活 Skill 的正文不得占用每次模型调用 context。\n\n## 2. 当前仓库状态\n\n当前 `zeta-skills` 已实现 S0 format/catalog：built-in/user/Workspace controlled root、bounded\nfrontmatter parse、metadata-only scan、完整 `SKILL.md` digest、isolated diagnostic 和 immutable\ncatalog generation。内置内容由 `zeta-rs/skills/assets/` 拥有，release staging 将其复制到\n`zeta-resources/skills/`，`zeta-install-context` 提供 directory candidate，host 再构造\n`SkillSourceRoot::built_in`。当前正式内置内容只有 `skill-creator`；新增 built-in 需要明确的\n产品语义、触发边界和选择评测，不能把 catalog fixture 直接升级为产品能力。实现细节与 limits 由\n[`zeta-rs/skills/README.md`](../zeta-rs/skills/README.md) 维护。\n\n`zeta-skills-extension` 当前拥有 catalog runtime：它组合 release built-in root、user config 中\n明确 enabled 的绝对 source root 和 active Workspace 的 `.zeta/skills`，叠加 durable per-Skill\nenablement，缓存 immutable projection，并通过通用 contributor 提供 Turn activation 与 context\nfragment。App Server 提供 `skills/list`、`skill/enablement/set`、digest-pinned\n`skill/resource/open` 与 `skills/changed` 协议投影。\n`zeta-file-watcher` 的\ninvalidation 会触发完整重扫；只有 entry、diagnostic 或 enablement 的 consumer-visible projection\n变化才推进 runtime generation。共享的 `SkillName`、`SkillSourceId` 与 `SkillId` 已下沉到\n`zeta-protocol`，因此 config、catalog、App Server 与客户端不再靠 raw string 隐式绑定。\n\nTUI 与 Desktop 消费同一 typed catalog，并把 enabled、compatible、名称无歧义且不与已有命令\n冲突的 Skill 直接投影为 `/name`。选择 `/commit` 后，客户端保留用户可见的 `/commit …` 文本，\n同时提交 exact pinned `SkillRef`；目录发现阶段不读取正文。`skills/changed` 会刷新动态命令列表。\n\nTUI `/skills` 提供 All/Enabled/Disabled/Manage/Errors tabs、搜索、左右切页和上下选择；只有 Manage\n中的动作修改后续 catalog eligibility。它是管理入口，不是日常执行 Skill 的二级 picker。\n\n当前显式激活链已经接通。协议输入为：\n\n```rust\nUserInput::Skill { skill: SkillRef }\n```\n\nCore 在接受 Turn 前调用 extension registry；`zeta-skills-extension` 从当前 enabled/compatible\ncatalog 解析显式 `SkillRef`，完整读取受控根中的 `SKILL.md`，冻结\n`SkillId + content digest + catalog generation + activation reason` 并持久化到 `TurnAccepted`。\n每个 model safe point，Core 再调用 `TurnInputContributor`，接收按 frozen digest 解析出的\n`PromptFragment` 并交给 context planner。客户端不能提交 raw path；文件缺失、换\nidentity、hard link、越界 symlink 或 digest 变化都会失败即关闭，不会悄悄跟随新内容。\n\nSkill body 不复制进 Thread history；durable activation provenance 足以审计，恢复执行则要求原\nsource 仍能提供 exact bytes。已经开始的 Turn 即使随后被 catalog disable，仍按冻结 snapshot\n完成；内容改变则停止，而不是替换 in-flight 指令。\n\n上述 exact-byte 要求适用于尚需继续执行的 Turn。重复提交已经 accepted 的同一 `commandId` 时，\nApp Server 先按 durable command receipt 校验输入并返回原 Turn 结果，不重新读取当前 catalog 或\nSkill 文件；因此源文件删除不会破坏已完成命令的幂等重放。\n\n当前 runtime source composition 包含 built-in、user、active Workspace 的原生 `.zeta/skills` source，\n以及 effective Plugin manifest 声明的 exact Skill directory。Plugin activation generation 变化会触发\ncatalog refresh，未声明的同包 sibling directory 不会进入 catalog。Workspace config 中额外声明的独立\nSkill source intent 与 script execution adapter 尚未接入；正文读取、通用 package resource resolver、有界 UTF-8 模型\n读取、binary asset Resource materialization、compatibility gate、显式激活和模型按需读取已接入。\n\n仓库已有可复用边界：\n\n- `zeta-protocol` 的 UserInput、Resource、ToolName 和 provider-independent model contract；\n- Core `ContextAssembler`、`ModelInvocationSnapshot` 与 durable checkpoint pipeline；\n- App Server 的 typed command、Resource store 和 generated client contract；\n- Plugin manager 提供 digest-pinned immutable Skill contribution root；\n- 目标 `zeta-tool-executor` / `zeta-sandboxing` 负责脚本执行，而不是 Skill loader；产品层\n  `zeta-exec` 是 headless Agent runner。\n\n## 3. 格式基线\n\n[Agent Skills specification](https://agentskills.io/specification) 规定：\n\n```text\nskill-name/\n├── SKILL.md          # required: YAML frontmatter + Markdown instructions\n├── scripts/          # optional executable helpers\n├── references/       # optional on-demand documentation\n├── assets/           # optional templates/static resources\n└── ...\n```\n\n`SKILL.md` 必须有：\n\n```yaml\n---\nname: code-review\ndescription: Reviews code changes for correctness and maintainability. Use for PR or diff review.\n---\n```\n\n兼容字段：\n\n| Field | Zeta 处理 |\n| --- | --- |\n| `name` | 严格校验，并要求与 Skill 目录名一致 |\n| `description` | discovery/selection metadata，必须同时说明做什么和何时使用 |\n| `license` | 展示与分发 metadata，不影响 runtime permission |\n| `compatibility` | 环境提示；解析为 warning/gate，但不自动安装依赖 |\n| `metadata` | string map；只允许 namespaced extension 影响 Zeta 展示 |\n| `allowed-tools` | experimental hint；绝不作为 Zeta approval grant |\n| Markdown body | Skill 激活后作为完整 instruction 加载 |\n\nZeta 不修改第三方 Skill format 来塞入 executable grant、credential 或 MCP launch config。这些属于\nPlugin manifest/config。独立 Skill 需要额外能力时只能产生明确 compatibility diagnostic。\n\n## 4. 职责与非职责\n\n### 4.1 Skill manager 拥有\n\n- Skill source 的注册、隔离、扫描和 invalidation；\n- `SKILL.md` frontmatter strict parse 与 Agent Skills format validation；\n- stable Skill identity、source、digest、availability 和 compatibility projection；\n- metadata-only catalog 与 immutable generation；\n- 显式选择、自动候选检索和冲突解释；\n- 激活时完整读取 `SKILL.md`；\n- 按需、安全地解析 relative references/scripts/assets；\n- context budget、递归深度、文件数、单文件和总 bytes 限制；\n- `SkillActivationSnapshot`、provenance 和 content digest；\n- file change/update 后只在下一个 model safe point 生效；\n- 不含正文和 secret 的 diagnostics/telemetry。\n\n### 4.2 Skill manager 不拥有\n\n- Plugin package 下载、签名、安装、enablement 或 grants；\n- script 执行、shell、dependency install、network 或 filesystem mutation；\n- MCP session、tool call 或 resource read；\n- Agent loop、model selection、prompt tokenization 算法；\n- system/developer instruction 或 product policy authority；\n- Thread event append、Tool Result 或 retry；\n- 把 Skill 自动转成全局 memory；\n- 扫描整个 home directory 或任意绝对 path。\n\n## 5. 目标依赖与组合\n\n```text\nconfig / Workspace / Plugin roots\n                │\n                ▼\n          zeta-skills\n  file / catalog / exact loader\n                │\n                ▼\n     zeta-skills-extension\n source composition / watcher / selection / fragment contribution\n                │ implements\n                ▼\n       zeta-extension-api\n activation + model-safe-point contracts\n                │ invoked by\n                ▼\n       Core ContextAssembler\n                │\n                ▼\n           ModelRequest\n\nApp Server 只在组合根安装 extension，并把 Config、RPC DTO 和 `skills/changed` 接到对应端口。\n```\n\n规则：\n\n- `zeta-skills` 不依赖 `zeta-core`、stores、App Server 或 Plugin live manager；\n- `zeta-file-identity` 只提供已打开文件的跨平台 identity/link-count，不拥有 Skill path\n  policy；具体 Win32/Unix contract 见\n  [`zeta-rs/file-identity/README.md`](../zeta-rs/file-identity/README.md)；\n- Skill source 以窄 `SkillSourceRoot`/file resolver port 注入；\n- Plugin manager 提供 immutable package root，Skill manager 仍重新校验 Skill format；\n- Core ContextAssembler 只接收通用 `PromptFragment`，不依赖 Skill catalog 或 filesystem；\n- `zeta-skills-extension` 在 safe point 按 frozen digest 精确重读已激活正文，但不重新做选择；\n- Skill scripts 通过普通 Tool/exec port 执行，`zeta-skills` 不依赖\n  `zeta-tool-executor`；\n- 只有 `SkillId`、`SkillRef`、选择 intent 等至少跨两个组件共享的稳定 value 才进入\n  `zeta-protocol`；catalog cache、filesystem handle、parse error 留在 `zeta-skills`。\n\n## 6. 身份、来源与优先级\n\n### 6.1 稳定身份\n\nSkill format 的 `name` 只在一个 source root 内唯一。Zeta identity 必须包含 source：\n\n```rust\npub struct SkillId {\n    pub source: SkillSourceId,\n    pub name: SkillName,\n}\n\npub struct SkillRef {\n    pub id: SkillId,\n    pub version: SkillVersionSelector,\n}\n```\n\nwire `SkillRef` 通过显式 version selector 区分两种用户意图：\n\n- `FollowLatest`：解析当前 source 中同一 Skill ID，适合普通 picker；\n- `PinnedDigest`：必须匹配 exact content，适合 Plugin lock、replay 和审计。\n\n使用具名 enum，不使用 `Option<digest>` 让调用者猜含义：\n\n```rust\npub enum SkillVersionSelector {\n    FollowLatest,\n    PinnedDigest { digest: ContentDigest },\n}\n```\n\n### 6.2 来源\n\n```text\nBuiltIn { release }\nUser { configuredRoot }\nWorkspace { workspaceId, configuredRoot }\nPlugin { pluginId, version, packageDigest }\nLocalDevelopment { canonicalRoot }\n```\n\nsource root 是 host 验证后的 opaque handle，不是未经校验的字符串 path。\n\n### 6.3 优先级\n\nPrecedence 只用于候选排序，不用于静默覆盖：\n\n```text\n用户在当前 Turn 显式选择\n> workspace enabled\n> user enabled\n> Plugin enabled\n> built-in fallback\n```\n\n两个不同 `SkillId` 即使 `name` 相同也同时存在，UI 和模型目录都必须显示 source。模型调用\n`skills-read` 时提交 exact source，因此不按目录扫描顺序取同名条目。\n\n显式 Skill 可以和自动 Skill 同时激活，但去重按 exact `SkillId + digest`，不是 display name。\n\n## 7. 发现与目录\n\n### 7.1 元数据-only 发现\n\n启动/刷新时只：\n\n1. 枚举受控 source root 下的直接 Skill 目录；\n2. 打开该目录的 `SKILL.md`；\n3. 读取有界 frontmatter 和必要 metadata；\n4. 校验 name/description/path；\n5. 计算内容 identity 所需的 digest 或 filesystem validator；\n6. 建立 catalog entry。\n\n不得在 discovery 时：\n\n- 加载 Markdown body 到全局 prompt；\n- 递归读取 references/assets；\n- 执行 scripts；\n- 探测网络或安装 compatibility dependency；\n- 跟随逃出 source root 的 symlink；\n- 因单个坏 Skill 让整个 built-in/user catalog 丢失。\n\nPlugin package 是原子 contribution：Plugin 内声明的 required Skill 无效时，由 Plugin resolver 决定\n整个 generation 是否失败；普通 user source 则可按 entry 隔离错误。\n\n### 7.2 目录快照\n\n```rust\npub struct SkillCatalogSnapshot {\n    pub generation: u64,\n    pub entries: Vec<SkillCatalogEntry>,\n    pub diagnostics: Vec<SkillDiagnostic>,\n}\n\npub struct SkillCatalogEntry {\n    pub id: SkillId,\n    pub description: String,\n    pub source: SkillSourceView,\n    pub content_digest: ContentDigest,\n    pub compatibility: SkillCompatibility,\n    pub availability: SkillAvailability,\n}\n```\n\n排序必须确定，相同 generation 的序列化结果稳定。只有 consumer-visible metadata、digest、\navailability 或 diagnostic 变化才递增 generation。\n\n当前 `zeta-file-watcher` 已提供共享、多订阅者的 filesystem invalidation substrate：\n`PathsChanged` 传递排序去重后的 coarse path hint，backend error/overflow 通过\n`RescanRequired` 传递 subscriber 自己的 watched roots。其 backend/ref-count/path fallback contract\n由 [`zeta-rs/file-watcher/README.md`](../zeta-rs/file-watcher/README.md) 维护。\n\nWatcher 仍只发 invalidation hint。当前 App Server adapter 订阅 built-in/user roots、active\nWorkspace 的 `.zeta` metadata root 与 user config authority path；收到普通 change、backend error\n或 overflow 后都调用\n`SkillCatalog::refresh` 重新扫描/校验，再按可见 projection 决定是否发布 `skills/changed`。\nWatcher backend 无法启动时不会阻止 App Server 启动，调用方仍可用\n`skills/list { reload: \"refresh\" }` 显式重扫；当前没有对外 watcher-health projection。\n这条 catalog refresh 路径不代表 activation safe-point composition 已完成。\n\n## 8. 选择\n\n### 8.1 显式选择\n\n用户通过统一斜杠面板中的 `/name`，或直接通过 `session/request` `StartTurn` typed input 选择 exact\n`SkillRef`。显式选择：\n\n- 由用户预先固定，不依赖模型是否决定读取某个 Skill；\n- 仍要通过 enablement、availability、compatibility 和 trust policy；\n- 若 pinned digest 已变化，返回 `SkillContentChanged`，不能悄悄跟随新内容；\n- 若 source 不再存在，historical display 仍保留 ID/digest，但新 Turn 不能激活；\n- 不允许客户端提供 catalog 外 absolute path。\n\n当前 wire shape 已迁移为：\n\n```text\nUserInput::Skill {\n  skill: SkillRef\n}\n```\n\n旧 `name + path` serde shape 会被拒绝，不保留双入口。App Server schema、generated TypeScript、\nCore、TUI 和 Desktop 共享同一 `SkillRef` contract。TUI 与 Desktop 的斜杠目录只加载 metadata，\n真正接受 Turn 时才由 App Server 加载完整 `SKILL.md`。\n\n### 8.2 模型按需选择（已实现）\n\n每次模型调用的 Skill 层会收到一个最多 8 KiB 的 `<available-skills>` 目录。目录只包含当前\nenabled、compatible Skill 的稳定 source、name 与截断后的 description，不包含正文或本地路径。\n模型判断某个 Skill 适用时，使用目录中的 exact `source + name` 调用 `skills-read`；该工具通过\n`SkillRuntime` 的受控 catalog 完整读取并校验 `SKILL.md`，把正文与 content digest 作为普通工具\n结果返回。工具结果进入 durable Turn history，并参与下一次模型调用。\n\n这条路径没有后端关键词分类器：模型负责语义判断，runtime 负责目录边界、enablement、\ncompatibility、exact identity 与文件安全。手动选择与模型选择复用同一个 catalog/loader，但持久化\n形态不同：手动选择在 `TurnAccepted.activated_skills` 中预先冻结；模型选择发生在 tool loop 中，\n由 durable Tool Result 保存已读取内容。\n\n`zeta-extension-api::ReadOnlyToolContributor` 是通用接入面，`SkillReadTool` 属于\n`zeta-skills-extension`，App Server 只把 executor 适配到已有 Extension ToolPort 与策略管线。\n因此该能力不依赖受信任 Workspace 的 shell/file tool，也不要求产品端了解 Skill 读取协议。\n\n### 8.3 后端自动候选检索（未实现，也不是主路径）\n\n如果未来 Skill 数量大到元数据目录也不能有效容纳，可以增加仅用于压缩候选集的检索层；它不是\n当前主选择机制，也不能替模型决定激活。候选检索仍不能把所有 Skill body 注入模型：\n\n```text\nenabled catalog\n→ compatibility/source/policy filter\n→ description + user intent retrieval\n→ bounded candidate shortlist\n→ deterministic threshold/ambiguity handling\n→ activate selected Skill\n```\n\n候选检索可以使用 lexical/keyword matcher；后续 embedding 必须可测、可解释并受候选数/延迟\n预算限制。检索结果仍进入同一个 metadata catalog，最终由模型调用 `skills-read`。\n\n规则：\n\n- `description` 是唯一 startup selection text；\n- 低置信度时不自动激活；\n- 自动 Skill 数量有上限；\n- 互斥或顺序依赖在没有 accepted contract 前不通过自由文本猜测；\n- 候选检索结果不得被记录为“已激活”；只有成功读取才产生 Tool Result；\n- 用户明确说不使用某 Skill 时，当前 Turn 不得重新自动加入；\n- Skill 不能通过 description 要求自己在所有任务激活。\n\n## 9. 激活与渐进加载\n\n[Agent Skills progressive disclosure](https://agentskills.io/specification#progressive-disclosure)\n分三层：\n\n| Level | 内容 | 加载时机 |\n| --- | --- | --- |\n| Metadata | name + description + source/compatibility | catalog/selection |\n| Instructions | 完整 `SKILL.md` body | Skill 激活 |\n| Resources | references/scripts/assets | 指令明确需要且 Agent 决定读取 |\n\n激活流程：\n\n```text\nresolve exact SkillId + version selector\n→ open file relative to immutable/validated source root\n→ revalidate frontmatter and digest\n→ read complete bounded SKILL.md\n→ parse referenced-path inventory\n→ build instruction fragment with provenance\n→ publish SkillActivationSnapshot\n```\n\n```rust\npub struct SkillActivationSnapshot {\n    pub id: SkillId,\n    pub source: SkillSourceView,\n    pub content_digest: ContentDigest,\n    pub catalog_generation: u64,\n    pub instructions: String,\n    pub root: SkillRootHandle,\n    pub diagnostics: Vec<SkillDiagnostic>,\n}\n```\n\nsnapshot 对一次 model invocation 不可变。Skill 文件变化只使 catalog stale；已经开始的调用仍使用\n旧 bytes/digest。下一个 model safe point 重新解析，并在 pinned mismatch 时要求用户决定。\n\n## 10. 上下文分层\n\nCore ContextAssembler 使用明确层级：\n\n```text\n1. system safety / platform policy\n2. Zeta developer/product instructions\n3. workspace policy and active Turn constraints\n4. Skill instructions（带 source + digest 边界）\n5. user input\n6. retrieved references/resources/tool results（不可信数据）\n```\n\nSkill 不能把自己声明为更高层。多个 Skill 的顺序由 activation controller 确定并记录，不依赖\nfilesystem enumeration order。\n\n每个 instruction fragment 包含：\n\n- exact Skill ID；\n- source kind；\n- digest；\n- bounded body；\n- `Required / BestEffort` context retention。\n\ncatalog generation、activation reason、斜杠或 picker 等入口信息留在 durable provenance 和外部\nselection/runtime，不写入模型可见 Skill 正文。外部 adapter 负责把具体选择策略映射为\n`Required / BestEffort`；Core 不解释 Skill 是显式还是自动选中的。\n\nCore ContextAssembler 应用总 token budget。超限时：\n\n1. 不截断 frontmatter 与 body 到无法解释；\n2. 优先减少自动 Skill 数；\n3. 显式 Skill 仍超限则返回可见错误或要求用户选择；\n4. references 不预载；\n5. 不能把被截断的 Skill 标成完整激活。\n\n## 11. 参考资料、脚本与资源\n\n### 11.1 文件解析器\n\n当前 package 文件通过 `SkillCatalog::read_resource` 和 `SkillResourcePath` 的 rooted contract；\n它绑定 pinned `SKILL.md` digest，允许 Skill root 下非空无 traversal 的相对路径，拒绝 symlink、\nhard link、special file、越界路径和超过 256 KiB 的内容，并返回原始 bytes 与内容摘要。\n`SkillResourceKind` 只标识 reference/script/asset 等用途，不切换 resolver，也不授予执行权限。\n\n跨 host/provider 抽象仍可使用同一读取语义：\n\n```rust\n/// Resolves bounded, read-only files beneath one validated Skill root.\n///\n/// Implementations must reject absolute paths, traversal, escaping links, special files and\n/// content that exceeds the supplied limit. A returned file must retain its digest and provenance.\npub trait SkillFileResolver: Send + Sync {\n    fn read(\n        &self,\n        request: SkillFileReadRequest,\n    ) -> impl Future<Output = Result<SkillFile, SkillFileError>> + Send;\n}\n```\n\nrequest 使用 `SkillResourcePath`、`SkillResourceKind` 和具名 size policy，不使用 raw `PathBuf` 或\n`allow_large: bool`。\n\n相对路径始终从 Skill package root 解析，调用方传完整路径，例如 `references/api.md` 或\n`scripts/check.py`。当前每次调用只读一个文件，不递归展开文件中的链接，因此不会产生 runtime\nresource cycle。未来若增加自动递归解析，必须再引入明确 depth budget 和 visited-set。\n\n### 11.2 参考资料\n\n- 作为不可信 data/context 读取，不提升 instruction precedence；\n- 只有当前任务需要时加载；\n- lower layer 保留相对 path、kind、digest 与 bytes；模型工具只接受 UTF-8 text；\n- Markdown 中的外部 URL 不自动 fetch；\n- reference 再引用的文件必须由模型再次显式调用统一的 `skills-read` resource target；\n- 超大表格/文档通过 Resource store 分块，而不是塞入单个 model message。\n\n### 11.3 脚本\n\n`scripts/` 中存在文件不代表它可执行。Skill manager 只返回 file metadata/handle。\n\n执行必须：\n\n```text\nSkill instruction suggests script\n→ resolve exact script under Skill root\n→ materialize command without shell concatenation\n→ evaluate Plugin/source trust and executable grant\n→ approval + sandbox + resource limits\n→ zeta-tool-executor\n→ durable Tool Call/Result\n```\n\n独立 user/workspace Skill 默认没有 Plugin activation grant。用户可以显式通过普通 exec tool 运行\n已审阅脚本，但仍遵守 workspace sandbox。`allowed-tools` 是 Agent Skills experimental field，\n只能作为作者意图/兼容性提示，不能跳过 Zeta approval。\n\nSkill manager 绝不自动执行：\n\n- dependency installer；\n- `postinstall` / `setup` / shell hook；\n- 网络下载器；\n- 脚本用来“探测 compatibility”；\n- 首次激活 callback。\n\n### 11.4 资源\n\nAssets 是模板、图片或静态数据：\n\n- 不自动加入 context；\n- 按 MIME/size/digest 读取；\n- 写入工作区时必须通过对应 tool 和 approval；\n- 模板展开后的结果是新 artifact，不反向修改 immutable Skill；\n- HTML/SVG 等 active content 在 UI preview 中必须 sanitize/sandbox。\n\n当前 App Server 的 `skill/resource/open` 要求 exact `SkillId + SKILL.md digest + package-relative\npath`，重新经过 `SkillRuntime::read_resource` 的 enablement、compatibility、root containment 与\ndigest 校验，再把 bytes 写入 connection-owned `ResourceStore`。PNG/JPEG/GIF/WebP/PDF 只有在扩展名\n与文件签名同时匹配时才投影对应 MIME；HTML/SVG 强制投影为 `application/octet-stream`。客户端随后\n沿用 `resource/metadata`、`resource/read` 与 `resource/release`，不能从该接口执行 script 或写回\nSkill package。Renderer 的 sandboxed/sanitized preview 仍未实现。\n\n## 12. 兼容性\n\nAgent Skills `compatibility` 是自由文本，不能作为 machine-enforced permission。Zeta 处理为：\n\n```text\nCompatible\nIncompatible { reason }\nUnknown { warning }\nRequiresUserAction { requirements }\n```\n\nBuiltIn/Plugin Skill 可以由受控 manifest contribution 提供额外 machine-readable requirement：\n\n```text\nrequired Zeta version\nrequired tool capability\nrequired MCP contribution ID\nrequired platform/architecture\nrequired package-relative asset\n```\n\n这些 requirement 由 Plugin/Skill resolver 校验，不能藏在 Markdown 并在运行时自动安装。\n\n如果 Skill 依赖某 MCP tool：\n\n- dependency 指向 stable namespaced contribution ID，不指向 model-facing alias；\n- MCP server unavailable 时 Skill 可以 `Blocked` 或按 manifest 明确声明 `Degraded`；\n- 不能通过 tool name 字符串碰撞绑定到另一个 Plugin/server；\n- Skill 激活 snapshot 和 MCP tool catalog snapshot 必须在同一 App Server composition generation\n  中解析。\n\n## 13. 信任、prompt injection 与数据安全\n\nSkill instructions 可能恶意或被供应链篡改。所有来源都需要 provenance：\n\n```text\nBuiltInVerified\nSignedPlugin { publisher, digest }\nUnsignedLocalDevelopment\nUserManaged\nWorkspaceManaged\n```\n\ntrust label 不改变 instruction precedence。即使 BuiltIn Skill 也不能绕过 platform safety。\n\n必须防御：\n\n- frontmatter/YAML alias 或 parser resource exhaustion；\n- Markdown/HTML active content；\n- path traversal、absolute path、symlink/hardlink escape；\n- reference cycle 和 recursive expansion；\n- oversized body/file/tree；\n- Skill 名称 Unicode/confusable collision；\n- workspace repository 提交 Skill 后自动执行；\n- 指令要求读取或发送 credential；\n- 指令伪造 system/developer message；\n- 更新后同 ID 内容改变但 historical/pinned invocation 无提示；\n- Skill asset 在 Renderer 中触发 script/network。\n\n默认不把 Skill body、references、scripts 或 assets 写入 telemetry。可记录 ID、source、digest、\nactivation reason、bytes/token count、duration 和 error code。\n\n## 14. 持久化、恢复与来源\n\nSkill catalog 是可重建 projection，不是 authority。Authority 分布为：\n\n- BuiltIn Skill：Zeta release；\n- Plugin Skill：Plugin installed/activation authority；\n- User/workspace Skill：显式 configured source；\n- current Turn selection：typed UserInput/command。\n\nThread history 当前在 `TurnAccepted.activated_skills` 保留显式 activation 的 stable identity、\ndigest、catalog generation 与 reason。模型按需选择不伪造该 pre-Turn activation：`skills-read`\n成功结果通过标准 Tool Call/Result 持久化；后端 automatic selector 仍未实现。\n\n无论是否进入 Thread event，每次 model invocation 的 diagnostic/provenance snapshot 都应记录：\n\n```text\nSkillId\nsource\ncontent digest\ncatalog generation\nactivation reason\nloaded references and digests\neffective instruction bytes/token estimate\n```\n\nSkill 内容本身不复制进 canonical Thread snapshot。Historical display 即使 source 已删除，也可依赖\nstored identity/digest；重新执行必须重新授权/解析，不能假定旧 path 仍安全。\n\n## 15. App Server API 与客户端\n\n| Method | 语义 | 状态 |\n| --- | --- | --- |\n| `skills/list` | 读取 metadata-only catalog；`refresh` 请求重扫 | ✅ 已实现 |\n| `skill/enablement/set` | 按 exact `SkillId` 修改 future eligibility | ✅ 已实现 |\n| `session/request` StartTurn Skill input | 选择 exact `SkillRef` | ✅ 已实现 |\n| `skill/resource/open` | 将 digest-pinned package resource materialize 到 connection-owned Resource store | ✅ 已实现 |\n| `skill/read` | 读取 entry metadata/diagnostics，不默认返回完整 body | Proposed |\n\nSkill install/remove 不属于 Skill manager：\n\n- Plugin Skill 通过 Plugin methods 管理；\n- user/workspace standalone Skill 通过明确的 source/config 或将来 artifact import 管理；\n- 客户端不能向 `skill/read` 传 arbitrary filesystem path。\n\n客户端斜杠面板展示 name 和 description，并在独立管理界面展示 source、version/digest 摘要、\ncompatibility、availability 和 trust。同名 Skill 不静默合并；名称有歧义或与已有命令冲突时不投影\n为无来源限定的 `/name`。自动激活结果在 Turn/status 中可解释，但 UI 不需要显示 Skill 正文。\n\n### 15.1 外部 Agent Skill 导入（仅限 Desktop）\n\n[`zeta-agent-import`](../zeta-rs/agent-import/README.md) 当前已经能只读发现 Codex 的\n`~/.agents/skills`、项目 `.agents/skills`、Claude 的 `~/.claude/skills` 和项目\n`.claude/skills`，并把 canonical path、来源、scope 与 review category 放入 metadata-only\n`AgentPathInspection`；它不读取或转换 Skill 正文，也不修改 Config。\n\n用户可见的导入工作流只在 Desktop 提供。Desktop 的目录选择、导入预览、冲突确认和撤销入口，\n以及 App Server/Config authority 把用户确认结果保存为明确用户 Skill 来源的 apply path 仍是\n计划设计。Skill manager 继续拥有来源 containment、格式、摘要和身份校验。\n\nTUI `/skills` 只浏览、启用或禁用 App Server 已发布的统一 catalog。若某个外部来源已经通过\nDesktop 导入，其 Skill 可以与其他来源一起出现在 TUI catalog 中；TUI 不提供外部目录发现、\n`/add-dir`、`/import-agent`、导入配置 mutation 或用户主目录扫描。\n\n外部导入必须遵守以下边界：\n\n- 只注册用户明确选择的窄 Skill 根，不开放整个 `~/.codex`、`~/.claude` 或用户主目录；\n- 不读取或导入认证文件、凭据、日志与历史记录，`~/.codex/auth.json` 明确排除；\n- Claude 的 `~/.claude.json` 同时包含 OAuth session、MCP、per-project state 和 cache，当前\n  整体排除；\n- 保留外部 Agent 类型、规范化来源根和内容摘要，不能把外部 Skill 冒充 built-in 或 workspace\n  来源；\n- 导入来源必须可查询、禁用和移除，移除后不能继续激活其中的 Skill；\n- 导入只建立只读内容来源，不授予脚本执行、网络、凭据或沙箱绕过能力。\n\n外部路径发现和导入计划由 `zeta-agent-import` 拥有，来源注册与内容解析仍属于 Config/Skill\n边界，不属于通用 `utils`；只有不理解外部 Agent 格式的路径规范化、目录 containment 和文件\nidentity 原语可以下沉到已有基础 crate。Desktop 交互所有权与其他外部配置类型的映射见\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#22-外部-agent-配置导入仅限-desktop)；\nTUI 的长期非职责见 [`tui.md`](tui.md#11-featureszeta-功能的垂直切片)。\n\n附加目录激活不是 Import。`zeta-add-dir` 已拥有 directory source 与 contribution policy 的纯\n领域 contract；未来由启动参数或会话命令加入的目录，可以在授权有效期内投影明确 allowlist 中的\nSkills 与 Agent definitions；持久 `additionalDirectories` 只授予文件访问，不能发现或激活任何 Skill。\n该临时投影可以复用 `zeta-agent-import` 的安全路径 inspection，但不写入 Config、不产生\nimported source，也不把附加目录提升为 Workspace。完整语义与 `/cd` 的区别见\n[`workspace-security.md`](workspace-security.md#工作目录附加目录与-cd)。\n\n## 16. 错误与诊断\n\n至少区分：\n\n```text\nSourceUnavailable\nSkillNotFound\nSkillAmbiguous\nInvalidFrontmatter\nInvalidSkillName\nDescriptionInvalid\nPathEscapesRoot\nUnsupportedFileType\nContentTooLarge\nReferenceCycle\nContentChanged\nIncompatible\nDependencyUnavailable\nPermissionRequired\nCatalogStale\n```\n\n错误携带安全的 Skill/source identity 和相对 path；不返回 host private absolute root、Skill 正文\n或 secret。diagnostic 必须能解释为什么 Skill 没被发现、没被选择、没被激活或只处于 degraded。\n\n## 17. 性能与预算\n\n建议第一版本地上限作为 Zeta policy，而不是 Agent Skills 标准：\n\n| 项目 | 初始 policy |\n| --- | --- |\n| 每 source Skill 数 | 有界，超限产生 source diagnostic |\n| frontmatter bytes | 小型固定上限 |\n| `SKILL.md` bytes | 与 context budget 联动，硬上限独立存在 |\n| 模型可读取的 Skill 正文 | 由正常 Tool Result 与上下文预算约束 |\n| reference depth | 1 为推荐，硬上限不超过少量层级 |\n| loaded reference bytes | per invocation 总预算 |\n| file count/tree depth | 防目录/归档资源耗尽 |\n\n具体数值在实现 benchmark 和真实 Skill corpus 后固定到 policy/config。测试注入 tokenizer estimator\n和 fake filesystem，不依赖真实 home directory。\n\n## 18. 目标目录\n\n```text\nzeta-rs/skills/src/\n├── lib.rs\n├── identity.rs\n├── source.rs\n├── format/\n│   ├── mod.rs\n│   ├── frontmatter.rs\n│   └── validation.rs\n├── catalog/\n│   ├── mod.rs\n│   ├── scanner.rs\n│   ├── entry.rs\n│   └── snapshot.rs\n├── selection/\n│   ├── mod.rs\n│   ├── explicit.rs\n│   └── automatic.rs\n├── activation/\n│   ├── mod.rs\n│   ├── loader.rs\n│   ├── resolver.rs\n│   └── snapshot.rs\n├── compatibility.rs\n├── diagnostic.rs\n├── error.rs\n└── *_tests.rs\n```\n\n不建立同时负责 Plugin/MCP/Skill 的 `extensions` 巨型 crate。实现模块超过约 500 LoC 时按\ncatalog/selection/activation 拆分；新 trait 必须有职责和实现约束 doc comment；新测试模块使用\n显式 sibling `#[path = \"..._tests.rs\"]`。\n\n## 19. 分阶段实施\n\n### 阶段 S0：format、目录与运行时 browser（当前状态）\n\n- Agent Skills frontmatter/parser/validator；\n- BuiltIn + controlled user + native Workspace source；\n- metadata-only scan、digest 和 immutable catalog；\n- path/size/cycle security fixtures；\n- App Server `skills/list`、`skills/changed` 与 watcher refresh；\n- durable per-Skill enablement overlay 和 TUI catalog browser/toggle。\n\n完成条件：启动不会加载所有 Skill body，坏 entry 不会逃出 root 或拖垮整个 catalog；source\n变化与 enablement 变化只发布新的 metadata projection。\n\n### 阶段 S1：显式选择纵向切片（核心、TUI 与 Desktop 已实现）\n\n- `SkillRef`/version contract 与 raw path rejection；\n- 激活时完整、安全地加载 `SKILL.md` 并冻结 digest；\n- `TurnAccepted` durable provenance 与 recovery fail-closed；\n- Core ContextAssembler layering、budget 和 provenance；\n- App Server/generated contract，以及 TUI/Desktop 统一斜杠面板中的直接 Skill 命令；\n- TUI `/skills` 目录管理和 enablement mutation。\n\n完成条件：客户端不能通过 raw path 激活 catalog 外文件，已开始 invocation 使用 frozen digest。\n\n当前限制：同名 Skill 或与 local/server command 同名的 Skill 不投影为无歧义的 `/name`；调用这类\nSkill 需要未来带来源限定的选择交互。CLI 没有独立可视化目录浏览器，但 typed protocol 入口不受影响。\n\n### 阶段 S1.5：模型按需读取（当前状态）\n\n- 每个模型 safe point 注入 metadata-only `<available-skills>`，固定上限 8 KiB；\n- `ReadOnlyToolContributor` 把 `skills-read` 接入共享 Tool registry/policy/runtime；\n- exact source/name、enablement、compatibility 与 content digest 由 `SkillRuntime` 校验；\n- 成功读取通过标准 durable Tool Call/Result 进入下一次模型调用；\n- 同一个 `skills-read` 使用 tagged target 区分完整说明与 package resource；resource target 必须提交\n  exact source/name + pinned Skill digest 和完整 package-relative path；\n- 后端不实现 keyword classifier，也不把模型选择伪装为 pre-Turn activation。\n\n完成条件：模型只看到目录元数据，正文必须经 exact read 才进入上下文；restricted Workspace 下仍\n可使用受控的进程内 Skill reader，且 App Server 不拥有 Skill 选择或文件加载逻辑。\n\n### 阶段 S2：资源与脚本（资源读取、文本切片与 binary materialization 已实现）\n\n- ✅ rooted package resource reader、kind/digest/bytes 与统一模型工具接入；\n- ✅ 模型工具只把 UTF-8 资源作为文本返回，binary asset 明确拒绝注入文本上下文；\n- ✅ assets 的安全 MIME 投影与 connection-owned Resource integration；\n- Renderer 的 sanitized/sandboxed preview；\n- scripts 只通过 exec/tool/approval/sandbox；\n- reference cycle、MIME 和 active-content policy。\n\n完成条件：读取 Skill 不产生副作用，运行脚本一定产生标准 durable Tool Call/Result。\n\n### 阶段 S3：Plugin 与 MCP 依赖（Plugin source vertical slice 已完成）\n\n- ✅ Plugin Skill exact source；\n- ✅ Plugin activation generation 触发 catalog composition refresh；\n- machine-readable MCP/tool requirements；\n- Plugin Skill update/disable 的 frozen Turn snapshot drain 仍需专项验证。\n\n完成条件：Skill 不能按名称碰撞绑定错误工具，Plugin update 不改变 in-flight invocation。\n\n### 阶段 S4：大目录候选检索\n\n- 仅在元数据目录规模需要时增加 bounded lexical retrieval；\n- threshold、ambiguity 和 explicit opt-out；\n- 候选解释与 quality evaluation；\n- 检索只缩小模型可见目录，不直接激活 Skill；有证据后再评审 embedding retrieval。\n\n完成条件：候选检索有稳定离线评测，低置信度时保留“不推荐候选”，context 不随 catalog 数量\n线性增长，最终选择仍由模型显式调用 `skills-read`。\n\n## 20. 验证门\n\n除常规 workspace 检查外，必须覆盖：\n\n- Agent Skills name/description/frontmatter contract；\n- YAML/parser resource exhaustion；\n- metadata-only startup 与 body lazy load；\n- source/name ambiguity 和 deterministic order；\n- raw path rejection、traversal、symlink/hardlink 和 normalization collision；\n- file change race、digest pin 和 catalog generation；\n- explicit selection、auto threshold、opt-out 和 max active Skill；\n- instruction precedence 与 prompt injection boundary；\n- context/token/bytes budget；\n- reference cycle/depth、external URL 不自动 fetch；\n- script 不被 loader 执行且必须经过 exec/approval/sandbox；\n- Plugin/MCP dependency identity、disable/update/drain；\n- schema/telemetry/error 不泄露 Skill 正文、secret 或 private absolute root。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-当前仓库状态",
        "title": "2. 当前仓库状态"
      },
      {
        "depth": 2,
        "id": "3-格式基线",
        "title": "3. 格式基线"
      },
      {
        "depth": 2,
        "id": "4-职责与非职责",
        "title": "4. 职责与非职责"
      },
      {
        "depth": 3,
        "id": "41-skill-manager-拥有",
        "title": "4.1 Skill manager 拥有"
      },
      {
        "depth": 3,
        "id": "42-skill-manager-不拥有",
        "title": "4.2 Skill manager 不拥有"
      },
      {
        "depth": 2,
        "id": "5-目标依赖与组合",
        "title": "5. 目标依赖与组合"
      },
      {
        "depth": 2,
        "id": "6-身份来源与优先级",
        "title": "6. 身份、来源与优先级"
      },
      {
        "depth": 3,
        "id": "61-稳定身份",
        "title": "6.1 稳定身份"
      },
      {
        "depth": 3,
        "id": "62-来源",
        "title": "6.2 来源"
      },
      {
        "depth": 3,
        "id": "63-优先级",
        "title": "6.3 优先级"
      },
      {
        "depth": 2,
        "id": "7-发现与目录",
        "title": "7. 发现与目录"
      },
      {
        "depth": 3,
        "id": "71-元数据-only-发现",
        "title": "7.1 元数据-only 发现"
      },
      {
        "depth": 3,
        "id": "72-目录快照",
        "title": "7.2 目录快照"
      },
      {
        "depth": 2,
        "id": "8-选择",
        "title": "8. 选择"
      },
      {
        "depth": 3,
        "id": "81-显式选择",
        "title": "8.1 显式选择"
      },
      {
        "depth": 3,
        "id": "82-模型按需选择已实现",
        "title": "8.2 模型按需选择（已实现）"
      },
      {
        "depth": 3,
        "id": "83-后端自动候选检索未实现也不是主路径",
        "title": "8.3 后端自动候选检索（未实现，也不是主路径）"
      },
      {
        "depth": 2,
        "id": "9-激活与渐进加载",
        "title": "9. 激活与渐进加载"
      },
      {
        "depth": 2,
        "id": "10-上下文分层",
        "title": "10. 上下文分层"
      },
      {
        "depth": 2,
        "id": "11-参考资料脚本与资源",
        "title": "11. 参考资料、脚本与资源"
      },
      {
        "depth": 3,
        "id": "111-文件解析器",
        "title": "11.1 文件解析器"
      },
      {
        "depth": 3,
        "id": "112-参考资料",
        "title": "11.2 参考资料"
      },
      {
        "depth": 3,
        "id": "113-脚本",
        "title": "11.3 脚本"
      },
      {
        "depth": 3,
        "id": "114-资源",
        "title": "11.4 资源"
      },
      {
        "depth": 2,
        "id": "12-兼容性",
        "title": "12. 兼容性"
      },
      {
        "depth": 2,
        "id": "13-信任prompt-injection-与数据安全",
        "title": "13. 信任、prompt injection 与数据安全"
      },
      {
        "depth": 2,
        "id": "14-持久化恢复与来源",
        "title": "14. 持久化、恢复与来源"
      },
      {
        "depth": 2,
        "id": "15-app-server-api-与客户端",
        "title": "15. App Server API 与客户端"
      },
      {
        "depth": 3,
        "id": "151-外部-agent-skill-导入仅限-desktop",
        "title": "15.1 外部 Agent Skill 导入（仅限 Desktop）"
      },
      {
        "depth": 2,
        "id": "16-错误与诊断",
        "title": "16. 错误与诊断"
      },
      {
        "depth": 2,
        "id": "17-性能与预算",
        "title": "17. 性能与预算"
      },
      {
        "depth": 2,
        "id": "18-目标目录",
        "title": "18. 目标目录"
      },
      {
        "depth": 2,
        "id": "19-分阶段实施",
        "title": "19. 分阶段实施"
      },
      {
        "depth": 3,
        "id": "阶段-s0format目录与运行时-browser当前状态",
        "title": "阶段 S0：format、目录与运行时 browser（当前状态）"
      },
      {
        "depth": 3,
        "id": "阶段-s1显式选择纵向切片核心tui-与-desktop-已实现",
        "title": "阶段 S1：显式选择纵向切片（核心、TUI 与 Desktop 已实现）"
      },
      {
        "depth": 3,
        "id": "阶段-s15模型按需读取当前状态",
        "title": "阶段 S1.5：模型按需读取（当前状态）"
      },
      {
        "depth": 3,
        "id": "阶段-s2资源与脚本资源读取文本切片与-binary-materialization-已实现",
        "title": "阶段 S2：资源与脚本（资源读取、文本切片与 binary materialization 已实现）"
      },
      {
        "depth": 3,
        "id": "阶段-s3plugin-与-mcp-依赖plugin-source-vertical-slice-已完成",
        "title": "阶段 S3：Plugin 与 MCP 依赖（Plugin source vertical slice 已完成）"
      },
      {
        "depth": 3,
        "id": "阶段-s4大目录候选检索",
        "title": "阶段 S4：大目录候选检索"
      },
      {
        "depth": 2,
        "id": "20-验证门",
        "title": "20. 验证门"
      }
    ],
    "searchText": " 物理位置：zeta-rs/skills/ Rust crate：zetaskills 当前状态：Phase S0、S1 显式选择、模型按需读取、通用 package resource、有界文本模型切片与 binary asset Resource materialization 已实现；Renderer preview、script execution adapter 与 S3–S4 仍为 Proposed。TUI 与 Desktop 已把 可直接调用的 Skill 投影为统一斜杠面板中的 /name；/skills 只承担目录管理。 Crate 实现契约：zeta-rs/skills/README.md Runtime extension 实现契约：zeta-rs/ext/skills/README.md 通用扩展生命周期：zeta-rs/ext/extension-api/README.md Core architecture：core.md Agent runtime：zeta-agent-runtime-architecture.md Config authority 与 runtime snapshot 接入：config.md Instructions/Skills/Agents 领域划分与外部导入：agent-customizations.md Plugin 分发边界：plugins.md MCP runtime：mcp.md 外部格式核对日期：2026-07-28。Skill package 以 Agent Skills specification 为兼容基线；Zeta 自己的 source、trust、activation 和执行权限语义由本文定义。 快速理解 Skill 是按任务逐步加载的工作方法和参考资料，不是工具权限。Agent 可以按照 Skill 建议行动， 但任何脚本、网络或文件操作仍经过正常工具、权限和沙箱流程。 发生的事情 加载什么 安全边界 --- --- --- 启动或刷新 Skill 目录 只读取名称、描述和来源等元数据 不把全部正文塞入上下文 用户输入 /commit 等 Skill 命令 接受 Turn 时完整读取对应 SKILL.md 斜杠面板只持有元数据和精确 SkillRef 用户打开 /skills 浏览、启用、禁用和查看诊断 不从管理面板直接执行 Skill 模型按需选择 Skill 模型先看到有界元数据目录，再调用 skills-read 加载正文 后端不做关键词分类，也不暴露本地路径 正文引用参考资料 只在当前任务确实需要时读取 路径必须受来源目录约束 Skill 建议运行脚本或工具 转成普通工具请求 不授予文件、网络、凭据或沙箱绕过能力 Plugin 提供 Skill 保留 Plugin、版本和内容摘要来源 Plugin 启用不等于 Skill 自动执行 1. 结论 zeta-skills 是 Zeta 的 Skill 文件、catalog、解析和 exact activation 底层 authority； zeta-skills-extension 负责来源组合、选择策略、watcher 和向 Core 注入上下文。Skill 是一个以 SKILL.md 为入口的指令目录，可以附带 references、scripts 和 assets。它帮助 Agent 判断“这类 任务应采用什么工作流”，但本身不是可执行权限、tool implementation、MCP server 或 Plugin。 核心原则： ``text 发现时只加载 metadata → 任务匹配后完整加载 SKILL.md → 只有真实需要时读取 references/scripts/assets ` Skill 内容是带来源的外部 instruction： - 它低于 Zeta system/developer/product policy； - 它不能授权 tool、network、filesystem、credential 或 sandbox bypass； - 它可以建议使用工具或脚本，但实际执行仍经过标准 tool loop； - 它不能通过“忽略上层规则”改变 instruction precedence； - 未激活 Skill 的正文不得占用每次模型调用 context。 2. 当前仓库状态 当前 zeta-skills 已实现 S0 format/catalog：built-in/user/Workspace controlled root、bounded frontmatter parse、metadata-only scan、完整 SKILL.md digest、isolated diagnostic 和 immutable catalog generation。内置内容由 zeta-rs/skills/assets/ 拥有，release staging 将其复制到 zeta-resources/skills/，zeta-install-context 提供 directory candidate，host 再构造 SkillSourceRoot::builtin。当前正式内置内容只有 skill-creator；新增 built-in 需要明确的 产品语义、触发边界和选择评测，不能把 catalog fixture 直接升级为产品能力。实现细节与 limits 由 zeta-rs/skills/README.md 维护。 zeta-skills-extension 当前拥有 catalog runtime：它组合 release built-in root、user config 中 明确 enabled 的绝对 source root 和 active Workspace 的 .zeta/skills，叠加 durable per-Skill enablement，缓存 immutable projection，并通过通用 contributor 提供 Turn activation 与 context fragment。App Server 提供 skills/list、skill/enablement/set、digest-pinned skill/resource/open 与 skills/changed 协议投影。 zeta-file-watcher 的 invalidation 会触发完整重扫；只有 entry、diagnostic 或 enablement 的 consumer-visible projection 变化才推进 runtime generation。共享的 SkillName、SkillSourceId 与 SkillId 已下沉到 zeta-protocol，因此 config、catalog、App Server 与客户端不再靠 raw string 隐式绑定。 TUI 与 Desktop 消费同一 typed catalog，并把 enabled、compatible、名称无歧义且不与已有命令 冲突的 Skill 直接投影为 /name。选择 /commit 后，客户端保留用户可见的 /commit … 文本， 同时提交 exact pinned SkillRef；目录发现阶段不读取正文。skills/changed 会刷新动态命令列表。 TUI /skills 提供 All/Enabled/Disabled/Manage/Errors tabs、搜索、左右切页和上下选择；只有 Manage 中的动作修改后续 catalog eligibility。它是管理入口，不是日常执行 Skill 的二级 picker。 当前显式激活链已经接通。协议输入为： `rust UserInput::Skill skill: SkillRef ` Core 在接受 Turn 前调用 extension registry；zeta-skills-extension 从当前 enabled/compatible catalog 解析显式 SkillRef，完整读取受控根中的 SKILL.md，冻结 SkillId + content digest + catalog generation + activation reason 并持久化到 TurnAccepted。 每个 model safe point，Core 再调用 TurnInputContributor，接收按 frozen digest 解析出的 PromptFragment 并交给 context planner。客户端不能提交 raw path；文件缺失、换 identity、hard link、越界 symlink 或 digest 变化都会失败即关闭，不会悄悄跟随新内容。 Skill body 不复制进 Thread history；durable activation provenance 足以审计，恢复执行则要求原 source 仍能提供 exact bytes。已经开始的 Turn 即使随后被 catalog disable，仍按冻结 snapshot 完成；内容改变则停止，而不是替换 in-flight 指令。 上述 exact-byte 要求适用于尚需继续执行的 Turn。重复提交已经 accepted 的同一 commandId 时， App Server 先按 durable command receipt 校验输入并返回原 Turn 结果，不重新读取当前 catalog 或 Skill 文件；因此源文件删除不会破坏已完成命令的幂等重放。 当前 runtime source composition 包含 built-in、user、active Workspace 的原生 .zeta/skills source， 以及 effective Plugin manifest 声明的 exact Skill directory。Plugin activation generation 变化会触发 catalog refresh，未声明的同包 sibling directory 不会进入 catalog。Workspace config 中额外声明的独立 Skill source intent 与 script execution adapter 尚未接入；正文读取、通用 package resource resolver、有界 UTF-8 模型 读取、binary asset Resource materialization、compatibility gate、显式激活和模型按需读取已接入。 仓库已有可复用边界： - zeta-protocol 的 UserInput、Resource、ToolName 和 provider-independent model contract； - Core ContextAssembler、ModelInvocationSnapshot 与 durable checkpoint pipeline； - App Server 的 typed command、Resource store 和 generated client contract； - Plugin manager 提供 digest-pinned immutable Skill contribution root； - 目标 zeta-tool-executor / zeta-sandboxing 负责脚本执行，而不是 Skill loader；产品层 zeta-exec 是 headless Agent runner。 3. 格式基线 Agent Skills specification 规定： `text skill-name/ ├── SKILL.md required: YAML frontmatter + Markdown instructions ├── scripts/ optional executable helpers ├── references/ optional on-demand documentation ├── assets/ optional templates/static resources └── ... ` SKILL.md 必须有： `yaml --- name: code-review description: Reviews code changes for correctness and maintainability. Use for PR or diff review. --- ` 兼容字段： Field Zeta 处理 --- --- name 严格校验，并要求与 Skill 目录名一致 description discovery/selection metadata，必须同时说明做什么和何时使用 license 展示与分发 metadata，不影响 runtime permission compatibility 环境提示；解析为 warning/gate，但不自动安装依赖 metadata string map；只允许 namespaced extension 影响 Zeta 展示 allowed-tools experimental hint；绝不作为 Zeta approval grant Markdown body Skill 激活后作为完整 instruction 加载 Zeta 不修改第三方 Skill format 来塞入 executable grant、credential 或 MCP launch config。这些属于 Plugin manifest/config。独立 Skill 需要额外能力时只能产生明确 compatibility diagnostic。 4. 职责与非职责 4.1 Skill manager 拥有 - Skill source 的注册、隔离、扫描和 invalidation； - SKILL.md frontmatter strict parse 与 Agent Skills format validation； - stable Skill identity、source、digest、availability 和 compatibility projection； - metadata-only catalog 与 immutable generation； - 显式选择、自动候选检索和冲突解释； - 激活时完整读取 SKILL.md； - 按需、安全地解析 relative references/scripts/assets； - context budget、递归深度、文件数、单文件和总 bytes 限制； - SkillActivationSnapshot、provenance 和 content digest； - file change/update 后只在下一个 model safe point 生效； - 不含正文和 secret 的 diagnostics/telemetry。 4.2 Skill manager 不拥有 - Plugin package 下载、签名、安装、enablement 或 grants； - script 执行、shell、dependency install、network 或 filesystem mutation； - MCP session、tool call 或 resource read； - Agent loop、model selection、prompt tokenization 算法； - system/developer instruction 或 product policy authority； - Thread event append、Tool Result 或 retry； - 把 Skill 自动转成全局 memory； - 扫描整个 home directory 或任意绝对 path。 5. 目标依赖与组合 `text config / Workspace / Plugin roots │ ▼ zeta-skills file / catalog / exact loader │ ▼ zeta-skills-extension source composition / watcher / selection / fragment contribution │ implements ▼ zeta-extension-api activation + model-safe-point contracts │ invoked by ▼ Core ContextAssembler │ ▼ ModelRequest App Server 只在组合根安装 extension，并把 Config、RPC DTO 和 skills/changed 接到对应端口。 ` 规则： - zeta-skills 不依赖 zeta-core、stores、App Server 或 Plugin live manager； - zeta-file-identity 只提供已打开文件的跨平台 identity/link-count，不拥有 Skill path policy；具体 Win32/Unix contract 见 zeta-rs/file-identity/README.md； - Skill source 以窄 SkillSourceRoot/file resolver port 注入； - Plugin manager 提供 immutable package root，Skill manager 仍重新校验 Skill format； - Core ContextAssembler 只接收通用 PromptFragment，不依赖 Skill catalog 或 filesystem； - zeta-skills-extension 在 safe point 按 frozen digest 精确重读已激活正文，但不重新做选择； - Skill scripts 通过普通 Tool/exec port 执行，zeta-skills 不依赖 zeta-tool-executor； - 只有 SkillId、SkillRef、选择 intent 等至少跨两个组件共享的稳定 value 才进入 zeta-protocol；catalog cache、filesystem handle、parse error 留在 zeta-skills。 6. 身份、来源与优先级 6.1 稳定身份 Skill format 的 name 只在一个 source root 内唯一。Zeta identity 必须包含 source： `rust pub struct SkillId pub source: SkillSourceId, pub name: SkillName, pub struct SkillRef pub id: SkillId, pub version: SkillVersionSelector, ` wire SkillRef 通过显式 version selector 区分两种用户意图： - FollowLatest：解析当前 source 中同一 Skill ID，适合普通 picker； - PinnedDigest：必须匹配 exact content，适合 Plugin lock、replay 和审计。 使用具名 enum，不使用 Option<digest 让调用者猜含义： `rust pub enum SkillVersionSelector FollowLatest, PinnedDigest digest: ContentDigest , ` 6.2 来源 `text BuiltIn release User configuredRoot Workspace workspaceId, configuredRoot Plugin pluginId, version, packageDigest LocalDevelopment canonicalRoot ` source root 是 host 验证后的 opaque handle，不是未经校验的字符串 path。 6.3 优先级 Precedence 只用于候选排序，不用于静默覆盖： `text 用户在当前 Turn 显式选择 workspace enabled user enabled Plugin enabled built-in fallback ` 两个不同 SkillId 即使 name 相同也同时存在，UI 和模型目录都必须显示 source。模型调用 skills-read 时提交 exact source，因此不按目录扫描顺序取同名条目。 显式 Skill 可以和自动 Skill 同时激活，但去重按 exact SkillId + digest，不是 display name。 7. 发现与目录 7.1 元数据-only 发现 启动/刷新时只： 1. 枚举受控 source root 下的直接 Skill 目录； 2. 打开该目录的 SKILL.md； 3. 读取有界 frontmatter 和必要 metadata； 4. 校验 name/description/path； 5. 计算内容 identity 所需的 digest 或 filesystem validator； 6. 建立 catalog entry。 不得在 discovery 时： - 加载 Markdown body 到全局 prompt； - 递归读取 references/assets； - 执行 scripts； - 探测网络或安装 compatibility dependency； - 跟随逃出 source root 的 symlink； - 因单个坏 Skill 让整个 built-in/user catalog 丢失。 Plugin package 是原子 contribution：Plugin 内声明的 required Skill 无效时，由 Plugin resolver 决定 整个 generation 是否失败；普通 user source 则可按 entry 隔离错误。 7.2 目录快照 `rust pub struct SkillCatalogSnapshot pub generation: u64, pub entries: Vec<SkillCatalogEntry , pub diagnostics: Vec<SkillDiagnostic , pub struct SkillCatalogEntry pub id: SkillId, pub description: String, pub source: SkillSourceView, pub contentdigest: ContentDigest, pub compatibility: SkillCompatibility, pub availability: SkillAvailability, ` 排序必须确定，相同 generation 的序列化结果稳定。只有 consumer-visible metadata、digest、 availability 或 diagnostic 变化才递增 generation。 当前 zeta-file-watcher 已提供共享、多订阅者的 filesystem invalidation substrate： PathsChanged 传递排序去重后的 coarse path hint，backend error/overflow 通过 RescanRequired 传递 subscriber 自己的 watched roots。其 backend/ref-count/path fallback contract 由 zeta-rs/file-watcher/README.md 维护。 Watcher 仍只发 invalidation hint。当前 App Server adapter 订阅 built-in/user roots、active Workspace 的 .zeta metadata root 与 user config authority path；收到普通 change、backend error 或 overflow 后都调用 SkillCatalog::refresh 重新扫描/校验，再按可见 projection 决定是否发布 skills/changed。 Watcher backend 无法启动时不会阻止 App Server 启动，调用方仍可用 skills/list reload: \"refresh\" 显式重扫；当前没有对外 watcher-health projection。 这条 catalog refresh 路径不代表 activation safe-point composition 已完成。 8. 选择 8.1 显式选择 用户通过统一斜杠面板中的 /name，或直接通过 session/request StartTurn typed input 选择 exact SkillRef。显式选择： - 由用户预先固定，不依赖模型是否决定读取某个 Skill； - 仍要通过 enablement、availability、compatibility 和 trust policy； - 若 pinned digest 已变化，返回 SkillContentChanged，不能悄悄跟随新内容； - 若 source 不再存在，historical display 仍保留 ID/digest，但新 Turn 不能激活； - 不允许客户端提供 catalog 外 absolute path。 当前 wire shape 已迁移为： `text UserInput::Skill skill: SkillRef ` 旧 name + path serde shape 会被拒绝，不保留双入口。App Server schema、generated TypeScript、 Core、TUI 和 Desktop 共享同一 SkillRef contract。TUI 与 Desktop 的斜杠目录只加载 metadata， 真正接受 Turn 时才由 App Server 加载完整 SKILL.md。 8.2 模型按需选择（已实现） 每次模型调用的 Skill 层会收到一个最多 8 KiB 的 <available-skills 目录。目录只包含当前 enabled、compatible Skill 的稳定 source、name 与截断后的 description，不包含正文或本地路径。 模型判断某个 Skill 适用时，使用目录中的 exact source + name 调用 skills-read；该工具通过 SkillRuntime 的受控 catalog 完整读取并校验 SKILL.md，把正文与 content digest 作为普通工具 结果返回。工具结果进入 durable Turn history，并参与下一次模型调用。 这条路径没有后端关键词分类器：模型负责语义判断，runtime 负责目录边界、enablement、 compatibility、exact identity 与文件安全。手动选择与模型选择复用同一个 catalog/loader，但持久化 形态不同：手动选择在 TurnAccepted.activatedskills 中预先冻结；模型选择发生在 tool loop 中， 由 durable Tool Result 保存已读取内容。 zeta-extension-api::ReadOnlyToolContributor 是通用接入面，SkillReadTool 属于 zeta-skills-extension，App Server 只把 executor 适配到已有 Extension ToolPort 与策略管线。 因此该能力不依赖受信任 Workspace 的 shell/file tool，也不要求产品端了解 Skill 读取协议。 8.3 后端自动候选检索（未实现，"
  },
  {
    "slug": "plugins",
    "sourcePath": "docs/plugins.md",
    "title": "插件系统",
    "description": "Plugin 是经过校验和版本管理的扩展包，不是安装后便能执行任意代码的进程内插件。安装、启用、 授权和实际调用是四个独立阶段。",
    "group": "Agent 与运行时",
    "markdown": "> 物理位置：`zeta-rs/plugins/`\n> Rust crate：`zeta_plugins`\n> 当前状态：PL0 已实现并支持 `ConnectorContribution` 引用 `McpServerContribution` 的声明校验；PL1 的\n> content-addressed store、Marketplace-first install/staged update/rollback、profile reconcile、durable installed/enabled/granted/effective authority、exact `PluginActivationSnapshot`、live generation publish 与 invocation drain 已实现；Connector domain 已提取到\n> `zeta-rs/connectors`，Plugin projection、durable authority 与 API-token connect/revoke 位于\n> `zeta-rs/ext/connectors`；App Server 已能从注入的 activation 自动接线 Connector 与 MCP，通用 OAuth\n> PKCE/device 状态机、App Server control plane、Desktop/TUI 产品入口与 GitHub providers 已实现；\n> TUF 远端 Marketplace、delegated publisher、离线签名目录缓存、按需安全 ZIP ingestion 与 revocation\n> tombstone 已实现；PL4 的可执行 Editor Extension 安装/授权声明已实现，Host runtime 不由本 crate 拥有\n> 当前 crate 实现契约：[`zeta-rs/plugins/README.md`](../zeta-rs/plugins/README.md)\n> 远端分发实现契约：[`zeta-rs/plugin-marketplace/README.md`](../zeta-rs/plugin-marketplace/README.md)\n> Connector account/lifecycle：[`connectors.md`](connectors.md)\n> MCP runtime：[`mcp.md`](mcp.md)\n> Skill runtime：[`skills.md`](skills.md)\n> Config authority 与 runtime snapshot 接入：[`config.md`](config.md)\n> Editor Extension 双轨系统边界：[`editor-extensions.md`](editor-extensions.md)\n> 可执行 Host runtime 实现：[`zeta-rs/editor-extension-host/README.md`](../zeta-rs/editor-extension-host/README.md)\n\n## 快速理解\n\nPlugin 是经过校验和版本管理的扩展包，不是安装后便能执行任意代码的进程内插件。安装、启用、\n授权和实际调用是四个独立阶段。\n\n| 用户动作 | 系统发生什么 | 不会自动发生什么 |\n| --- | --- | --- |\n| 安装 Plugin | 校验不可变包、清单、来源和内容摘要后写入本地存储 | 不启用贡献、不授予权限 |\n| 在用户或 Workspace 中启用 | 允许其贡献参与解析 | 不连接 Connector、不启动 MCP、不执行脚本 |\n| 批准请求的能力 | 记录精确的进程、网络、目录或凭据授权 | 不批准未来每一次工具调用 |\n| 激活贡献 | 生成带来源和 generation 的不可变快照 | 不把 live manager 注入 Agent |\n| 更新或回滚 | 并存校验后的版本并原子切换 | 不原地修改已安装包 |\n| 卸载 | 撤销后续激活并清理可回收内容 | 不删除其他领域拥有的秘密或历史 |\n| 打开正式打包的 Zeta | 从产品内固定的 root 刷新官方 HTTPS Marketplace | 不信任服务器提供的新 root，不自动安装或启用 Plugin |\n| 浏览 Marketplace | 读取已签名 manifest、能力、权限与包统计；离线时可使用仍有效的目录缓存 | 不预下载所有 Plugin ZIP |\n| 安装远端 Plugin | 重新检查 TUF 与撤销状态，只下载所选 exact ZIP，再校验内容摘要 | 不因已浏览或已下载而自动启用、授权 |\n\n## 1. 结论\n\n`zeta-plugins` 是 Zeta 的扩展分发、安装、解析、启用和版本管理控制面。Plugin 是一个不可变、\n可校验的包，可以贡献 Skill、Connector declaration、MCP server declaration、可执行 Editor Extension 声明和静态资源；Plugin manager 将这些贡献\n解析为带来源和 digest 的 activation snapshot，再由 App Server 注入对应 runtime。\n\nPlugin 不是：\n\n- 第三方 Rust dynamic library 或稳定 in-process ABI；\n- 一段安装后自动获得系统权限的代码；\n- Skill、MCP server、tool 或 connector 的同义词；\n- Thread/Turn 执行状态或 durable transcript；\n- secret container；\n- 允许扩展绕过 approval、sandbox、credential 和 network policy 的信任标记。\n\n边界固定为：\n\n```mermaid\nflowchart TD\n    P[\"Plugin activation snapshot\"] --> S[\"SkillContribution → Skill runtime\"]\n    P --> C[\"ConnectorContribution → Connector runtime\"]\n    P --> M[\"McpServerContribution\"]\n    P --> E[\"EditorExtensionContribution → executable Host RPC v1 program\"]\n    P --> D[\"DeclarativeExtensionContribution → static package.json catalog\"]\n    P --> A[\"StaticAssetContribution → Resource consumer\"]\n    C -. \"references declaration\" .-> M\n    C -->|\"connected\"| B[\"Ready MCP binding\"]\n    M -->|\"standalone activation\"| R[\"MCP runtime\"]\n    M --> B\n    E --> H[\"zeta-editor-extension-host supervisor\"]\n    D --> X[\"zeta-extensions immutable snapshot\"]\n    B --> R\n    R --> T[\"Tool Registry / Core\"]\n```\n\nPlugin 只负责声明控制面和 package lifecycle。Skill、Connector、MCP 和 Resource consumer 分别拥有自己的运行时\n语义；它们不是 Plugin manager 内部的 live 子对象。Plugin、Connector 与 MCP 的 canonical 关系由\n[`connectors.md`](connectors.md) 维护。\n\n静态 Editor Extension 保持另一套内容边界：它读取自己的 `package.json` 和声明式\nlanguage/TextMate/snippet/theme/debugger 资源。Plugin v1 现在可用 `declarativeExtensions[]` 指向包内\n静态 Extension 目录；只有 effective exact Plugin package 会被 App Server 投影到 `zeta-extensions`。\n这共享 install/enable/grant/revocation lifecycle，但不合并两种 manifest，也不把静态内容变成可执行\nruntime。其 canonical 文档是 [`editor-extensions.md`](editor-extensions.md)。\n\nPlugin v1 现在提供显式 `editorExtensions[]` bridge。每项指向包内一个可直接启动、自己实现 Zeta Host\nRPC v1 的程序；它不是由通用 Node/WASM runtime 加载的脚本。Plugin manager 只验证并授权声明，\n`zeta-editor-extension-host` supervisor 才拥有逐扩展进程隔离、RPC、crash recovery 和 provider\nlifecycle。静态 `package.json` catalog 不会被隐式转换成该 executable declaration。\n\n`declarativeExtensions[]` 是另一条显式 bridge：每项只有 manifest-local ID 和 package-relative\ndirectory，目录必须包含 regular `package.json`。它不需要 `process` permission；安装只存储 bytes，\nenable + grant 后才进入静态 catalog，disable、grant revoke、package revoke、update 或 uninstall 会使\n下一次 catalog refresh 移除旧 exact package。Workbench 监听 Plugin activation generation 并自动刷新。\n生产第三方执行还必须由产品注入能够实施 sandbox、memory/CPU/process hard limits 和 process-tree\ntermination 的 platform launcher；没有该 launcher 时 Host capability 必须为 false，不能用\n`TrustedDevelopmentLauncher` 降级。该 v1 是 Zeta-native executable RPC，不是 VS Code/Node Extension\nHost。\n\n安装、启用、授权和调用是四个不同动作：\n\n1. **Install**：包进入本地 content store；\n2. **Enable**：某个 user/workspace profile 允许其贡献参与解析；\n3. **Grant**：允许所需 process/network/root/credential capability；\n4. **Invoke**：Agent 的某次 tool/script 操作仍经过 runtime approval 与 sandbox。\n\n任何一个动作都不能隐含下一个动作。\n\n## 2. 当前仓库审计\n\n当前已创建 `zeta-plugins`，实现 strict v1 manifest、Plugin identity/SemVer、portable\npackage-relative path、本地 package 安全校验、确定性 digest、只读 local-development discovery，\n以及 stage-copy-revalidate-atomic-promote 的 local content-addressed store。实现细节、limits 与\nfailure semantics 由 crate\n[`README`](../zeta-rs/plugins/README.md) 维护。\n\nUser/Workspace TOML 与 App Server 已能表达 exact Plugin request 和 desired enablement。产品 host 注册\n受管 Marketplace root；resolver 只按 catalog 中的 exact ID/version/digest 安装并协调 user-profile\nenablement，不自动授予权限。Workspace request 只能读取 profile 中可用、待 profile 安装或版本不匹配\n三种结果，不能以 Workspace trust 自动扩大 profile authority。Package store 安全保存 immutable object，\n并把 exact installed package 解析为 generation-bound activation snapshot；App Server 可据此自动构造\nSkill source、Connector catalog、durable authority 和 package-rooted MCP provider。Plugin authority\n分别持久化 installed/enabled/granted/effective refs 和 command receipts，并驱动 live activation 切换；\nApp Server 已暴露 Marketplace list/install/update/rollback 及 enable/disable/grant/revokeGrant/uninstall。\n`docs/tui.md` 也明确要求 Plugin domain projection 进入 canonical\nApp Server contract 后，TUI 才能增加管理 feature。TUI 已有可复用的 interaction view stack 与\ntabs/search/selection presentation primitive，但当前没有 Plugin view model 或 `/plugins` command；\n这些 UI 基础设施不改变本节的 backend gate。\n\n已有可复用边界：\n\n- `zeta-config` 提供 ordinary config authority、typed patch 和 `CommandId` replay；\n- 各 credential domain 是生命周期 owner，`zeta-secrets` 是 opaque secret persistence owner；\n- `zeta-sandboxing`、`zeta-tool-executor` 和 host capability 是已分离的本地进程执行权限边界；\n  产品层 `zeta-exec` 只运行完整的无界面 Agent Turn；\n- App Server 是本地 runtime 的 composition root；\n- Resource store 可承载大块只读内容，但不是 Plugin package authority；\n- `zeta-protocol` 已固定“共享纯语义进入 protocol，I/O 和 policy 留在执行层”的规则。\n\n因此第一版不应从“动态加载代码”开始，而应先完成一个 declarative package：\n\n```text\nPlugin v1 contributions = Skills + Connectors + MCP server declarations\n                        + executable Editor Extension declarations + static assets\n```\n\n第三方 UI、native library、hooks、model provider adapter 和任意 App Server method registration\n不在 v1。\n\n## 3. 职责与非职责\n\n### 3.1 Plugin manager 拥有\n\n- Plugin package layout 和 manifest schema；\n- stable Plugin identity、version、digest 和 origin；\n- package staging、validation、atomic install、side-by-side update 和 recoverable remove；\n- user/workspace enablement 和 version pin；\n- contribution discovery、path containment、compatibility 和 conflict validation；\n- requested permissions、credential slots 与 user grants 的差异计算；\n- immutable `PluginActivationSnapshot` 和 generation；\n- package provenance、signature/trust result、revocation/blocked diagnostics；\n- enabled Plugin 向 Skill/Connector/MCP runtime 的 normalized contribution projection；\n- executable Editor Extension 的 exact program、Host RPC v1、activation trigger 与 capability ceiling 声明；\n- install/update/enable/disable/uninstall 的 typed command replay；\n- 不含秘密的 audit record 和 health projection。\n\n### 3.2 Plugin manager 不拥有\n\n- Skill 的自动选择、prompt layering 或 context budget；\n- MCP JSON-RPC、process supervision、Connector connection/OAuth 或 tools/resources/prompts catalog；\n- Editor Extension Host RPC transport、process supervision、crash recovery 或 provider registry；\n- script、binary 或 MCP tool 的实际执行；\n- API token、OAuth token、cookie 或 private key；\n- OS sandbox、network enforcement 或 per-call approval 的最终实现；\n- Thread reducer、Tool Call/Result commit 或 Agent retry；\n- Marketplace 搜索和支付业务；\n- 第三方 UI iframe、Renderer code execution 或 Electron preload extension；\n- 任意 native ABI、WASM ABI 或 provider adapter ABI。\n\n## 4. 目标依赖与组合\n\n```text\n                         zeta-protocol\n                              ▲\n                              │ shared IDs only when accepted\n                              │\n                         zeta-plugins\n        manifest / package store / resolver / authority / snapshot\n                    ▲                         ▲\n                    │ package source          │ trust verifier\n                    │                         │\n             filesystem/registry        signature service\n                    \\                         /\n                     \\                       /\n                      App Server composition\n                         │          │          │\n             SkillContribution  ConnectorContribution  McpServerContribution\n                         │          │          │\n                         ▼          ▼          ▼\n                    zeta-skills  zeta-connectors   zeta-mcp\n```\n\n具体规则：\n\n- `zeta-plugins` 不依赖 `zeta-skills`、`zeta-connectors` 或 `zeta-mcp` live runtime；\n- Plugin manager 只输出 normalized descriptor 和 immutable root handle；\n- App Server 将 Skill contribution 注册到 Skill source，将 Connector contribution 交给 Connector adapter，\n  并将独立或 ready-bound MCP contribution 解析为 `McpServerDefinition`；\n- contribution consumer 必须再次执行自己领域的校验，不能因为 package 已验证就跳过 schema、\n  content 或 runtime policy；\n- Plugin state 不进入 SessionStore/ThreadStore；\n- App Server protocol 只暴露稳定 Plugin view 和 command DTO，不暴露内部 filesystem path、\n  lock、transaction journal 或 signature library type。\n\n## 5. 包与清单\n\n### 5.1 v1 布局\n\n```text\nplugin-root/\n├── .zeta-plugin/\n│   └── plugin.json\n├── skills/\n│   └── code-review/\n│       ├── SKILL.md\n│       ├── references/\n│       ├── scripts/\n│       └── assets/\n├── mcp/\n│   └── review-server.json\n├── bin/\n│   └── review-server\n├── assets/\n└── LICENSE\n```\n\n目录可以省略，但 `.zeta-plugin/plugin.json` 必须存在。manifest 中只允许相对 package root 的\nslash-separated path；绝对路径、`..`、空 segment、NUL、平台 device path 和 escape symlink\n全部拒绝。\n\n### 5.2 清单结构\n\n以下是当前 v1 语义示例；当前 authority 是 Rust strict parser，尚未发布独立 JSON Schema：\n\n```json\n{\n  \"schemaVersion\": 1,\n  \"id\": \"acme/code-review\",\n  \"version\": \"1.2.0\",\n  \"displayName\": \"Acme Code Review\",\n  \"description\": \"Review workflows and an optional MCP server.\",\n  \"license\": \"Apache-2.0\",\n  \"compatibility\": {\n    \"zeta\": \">=0.1.0\"\n  },\n  \"contributions\": {\n    \"skills\": [\n      { \"id\": \"code-review\", \"path\": \"skills/code-review\" }\n    ],\n    \"mcpServers\": [\n      { \"id\": \"review\", \"definition\": \"mcp/review-server.json\" }\n    ],\n    \"assets\": [\n      { \"id\": \"icon\", \"path\": \"assets/icon.png\" }\n    ],\n    \"editorExtensions\": [\n      {\n        \"id\": \"review-runtime\",\n        \"entrypoint\": \"bin/review-extension-host\",\n        \"runtimeApiVersion\": 1,\n        \"activationEvents\": [\n          { \"type\": \"onCommand\", \"id\": \"acme.review.run\" },\n          { \"type\": \"onLanguage\", \"id\": \"rust\" },\n          { \"type\": \"onDemand\", \"capability\": \"testProfileProvider\" }\n        ],\n        \"capabilities\": [\"command\", \"languageProvider\", \"testProfileProvider\"]\n      }\n    ]\n  },\n  \"permissions\": [\n    { \"type\": \"process\", \"executable\": \"bin/review-server\" },\n    { \"type\": \"process\", \"executable\": \"bin/review-extension-host\" },\n    { \"type\": \"workspace\", \"access\": \"read\" },\n    { \"type\": \"network\", \"hosts\": [\"api.example.com\"] }\n  ],\n  \"credentialSlots\": [\n    { \"name\": \"api-token\", \"kind\": \"secretText\", \"requiredFor\": [\"mcp:review\"] }\n  ]\n}\n```\n\nManifest 必须 strict-parse：\n\n- unknown top-level/security-sensitive field 明确报错；\n- 自定义展示 metadata 只能放在 namespaced `metadata`；\n- `schemaVersion` 是 manifest format version，不是 Plugin version；\n- Plugin version 使用一个已选择并统一实现的 version scheme；\n- 同一 Plugin version 的内容 digest 必须唯一；registry 不得让相同\n  `(PluginId, version)` 指向不同内容；\n- manifest 只声明 credential slot，不包含 secret value；\n- permission 使用 tagged enum，不使用 `network: true`、`workspace: \"all\"` 一类含糊开关。\n\n`editorExtensions[]` 是 strict typed control-plane declaration：ID 与 entrypoint 各自唯一；entrypoint\n必须是包内 regular file，并有完全相同路径的 `process` permission；`runtimeApiVersion` 仅接受数值\n`1`；activation event 和 capability ceiling 都必须 non-empty、unique、bounded。v1 triggers 是\n`startup`、`onCommand`、`onLanguage`、`onDemand`、`onDebugType`、`onTaskType` 和\n`onTestProfile`。除 `startup` 外，trigger 不得请求 ceiling 中未声明的 capability。\n\n`workspaceContains` 当前故意不在 schema 中：安全实现必须由 Workspace authority 提供 bounded scanner\n与 ignore/trust 语义，不能让扩展程序自行扫描工作区，也不能让 Host 静默忽略一个看似有效的 trigger。\n\nexact `process` permission 只给 supervisor 一条 package-relative launch ceiling；provider invocation\n仍走已经启动的 RPC session，不会每次重新直接执行 entrypoint。regular-file/containment 校验不承诺该\nartifact 在当前 OS、CPU 或 ABI 上可运行，也不检查 Unix executable bit、Windows PE/扩展名或代码签名。\n当前 v1 没有 per-platform artifact selector；supervisor 必须在 activation 时检查 launchability 并对不兼容\nartifact fail closed。\n\n### 5.3 身份\n\n推荐 Plugin ID 为 `publisher/name`，两段都使用 lowercase ASCII、数字和单连字符，并限制总长度。\ndisplay name 可本地化且可变化，不能充当 identity。\n\n```rust\npub struct PluginId(String);\npub struct PluginVersion(String);\npub struct PluginPackageDigest(String);\n\npub struct InstalledPluginRef {\n    pub id: PluginId,\n    pub version: PluginVersion,\n    pub digest: PluginPackageDigest,\n}\n```\n\nPlugin contribution identity 是：\n\n```text\n(PluginId, contribution kind, manifest-local contribution ID)\n```\n\n升级时即使 path 改变，只要 manifest-local ID 不变，用户 grant 和配置才能被有控制地重新评估。\n\n## 6. 包来源、来源与信任\n\nPackage source 使用显式 enum：\n\n```text\nBuiltIn\nLocalDevelopment { canonicalPath }\nRegistry { registryId, package, version, digest }\nArchive { sourceUri, digest }\n```\n\nv1 至少支持 BuiltIn 和显式 LocalDevelopment；remote registry 必须在下载、签名和更新 threat\nmodel 完成后再启用。\n\n每个 installed record 保存：\n\n- exact Plugin ID/version/digest；\n- manifest digest；\n- source type 与 origin；\n- install timestamp；\n- signature verification result 与 signer identity（若有）；\n- Zeta version/manifest schema compatibility；\n- requested permissions 和 granted permission revision；\n- package quarantine/blocked reason；\n- previous active version，供原子 rollback。\n\n信任规则：\n\n- BuiltIn 由 Zeta release trust 继承，但仍经过 manifest/content validation；\n- marketplace/registry package 必须 digest-pinned；生产 channel 还应验证受信 publisher 签名；\n- LocalDevelopment 可以 unsigned，但 UI、status 和 audit 必须显著标记；\n- signature 只证明来源/完整性，不证明 Skill 指令安全、MCP tool 无副作用或没有漏洞；\n- publisher/release 被撤销后禁止新 activation，并停止或隔离受影响 runtime；\n- trust store、revocation feed 和 signing key rotation 属于独立安全 policy，不能硬编码在 manifest。\n\n## 7. Install store 与事务\n\n### 7.1 内容寻址的不可变存储\n\nPackage 解包后放入 content-addressed directory：\n\n```text\nplugin-store/\n├── objects/<sha256>/...\n├── staging/<operation-id>/...\n└── authority.json 或 typed database\n```\n\nruntime 永远从 immutable object root 读取。不能原地修改 active package，也不能让 update 覆盖\n旧 version 目录。\n\n安装流程：\n\n```text\nresolve source\n→ download/copy into unique staging\n→ enforce archive count/size/depth limits\n→ reject traversal, special files and escaping links\n→ compute digest\n→ verify origin/signature\n→ parse and validate manifest\n→ validate all contribution paths\n→ fsync + atomic promote to object store\n→ commit installed authority record\n```\n\n任何一步失败都不改变 active snapshot。staging cleanup 可恢复且不得把 broad root 当删除目标。\n\n### 7.2 权威与投影\n\nPlugin authority 至少保存：\n\n- installed exact package refs；\n- per-profile desired enablement/version pin；\n- grants 和 credential slot bindings；\n- typed command receipts；\n- active snapshot generation；\n- rollback target。\n\npackage directory 是 immutable content，authority record 决定“已安装/已启用”；UI cache、搜索索引和\nhealth 是可重建 projection。\n\n不得只靠扫描目录推断 enablement，也不得只靠 config 中一个 path 宣布包已验证。\n\n## 8. 状态模型\n\n不要用一个巨型 enum 表达所有正交状态。至少拆分：\n\n```text\nInstallState\n  NotInstalled / Staged / Installed / Quarantined\n\nEnablement\n  Disabled / Enabled\n\nActivationState\n  Inactive / Resolving / Active / Blocked / Broken\n\nRuntimeHealth\n  Unknown / Healthy / Degraded / Unavailable\n```\n\n例子：\n\n- Installed + Disabled + Inactive：包存在但不参与解析；\n- Installed + Enabled + Blocked：缺 grant 或 Zeta version 不兼容；\n- Installed + Enabled + Active + Degraded：Skill 可用，但某个 MCP server 当前认证失败；\n- Quarantined：任何 contribution 都不能激活。\n\nhealth 不改变 package authority，MCP crash 也不能让 Plugin 变成“未安装”。\n\n## 9. 范围、优先级与冲突\n\nActivation profile 至少区分：\n\n```text\nBuiltInProfile\nUserProfile\nWorkspaceProfile { workspaceId }\n```\n\nWorkspace 声明可以请求某 Plugin/version，但不能静默下载、启用或授予权限。首次进入 workspace\n时必须展示 package origin、digest、permissions 和 credential slots。\n\n解析规则：\n\n- exact `PluginId` 在一个 profile resolution 中只能有一个 active version；\n- workspace pin 可以覆盖 user 的版本选择，但必须产生可见的 `VersionPinOverride`；\n- 两个不同 Plugin 的 contribution 同名不能按 source priority 静默覆盖；\n- Skill/Connector/MCP consumer 使用 namespaced identity；\n- manifest-local duplicate ID 使整个 package validation 失败；\n- incompatible/blocked Plugin 不提供部分“看起来能用”的 contribution，除非 manifest 明确声明\n  独立 optional contribution group，且 resolver 能原子判断。\n\n第一版不支持 Plugin 依赖其他 Plugin。长期若加入依赖，必须使用 lock snapshot、cycle detection、\nexact resolved versions 和冲突解释；不能在启动时执行隐式 package-manager install。\n\n## 10. 权限与凭据\n\n### 10.1 两层授权\n\nPlugin grant 与 action approval 分开：\n\n```text\nActivation grant\n  允许启动哪个 executable、连接哪些 host、暴露哪些 roots、绑定哪个 CredentialRef\n\nInvocation approval\n  允许本次 materialized tool/script 操作及其具体参数\n```\n\nGrant 只限定最大能力，不能预先批准任意未来 side effect。MCP tool annotation 和 Skill\n`allowed-tools` 也不能扩大 grant。\n\n### 10.2 权限类型\n\n目标使用 tagged values，例如：\n\n```text\nProcessLaunch { packageRelativeExecutable }\nWorkspaceRead { rootSelector }\nWorkspaceWrite { rootSelector }\nNetworkConnect { scheme, hostPattern, portPolicy }\nCredentialUse { slot }\nHostCapability { capabilityKind }\n```\n\n以下声明 v1 拒绝：\n\n- unrestricted filesystem；\n- arbitrary shell；\n- 任意 host/port 网络 wildcard；\n- 读取所有 process env；\n- Electron/Renderer/Node raw access；\n- 注册任意 App Server method；\n- 把 credential 注入日志、argv 或 manifest placeholder 后回写。\n\nSecret materialization 在启动/请求的最后时刻由 credential adapter 完成。Plugin manager 只看到\nslot → `CredentialRef` binding 和 revision。\n\n## 11. 贡献激活\n\nResolver 输出 immutable snapshot：\n\n```rust\npub struct PluginActivationSnapshot {\n    pub profile: PluginProfileId,\n    pub generation: u64,\n    pub plugins: Vec<ResolvedPlugin>,\n    pub skills: Vec<ResolvedSkillContribution>,\n    pub mcp_servers: Vec<ResolvedMcpServerContribution>,\n    pub diagnostics: Vec<PluginDiagnostic>,\n}\n```\n\n只有 consumer-visible resolution 变化才递增 generation。snapshot 中的所有 package root、path、\nversion、digest、grant 和 credential binding revision 都已冻结。\n\n激活顺序：\n\n```text\nload authority\n→ resolve exact packages\n→ validate compatibility and grants\n→ produce new snapshot off to the side\n→ Skill manager validates/indexes new skill source\n→ MCP manager validates definitions and prepares sessions\n→ atomically publish generation\n→ retire previous generation after references drain\n```\n\n如果 Skill 或 MCP consumer 校验失败，默认不发布半个 Plugin generation。optional contribution\n只有在 manifest schema 明确表达隔离边界后才能独立降级。\n\nAgent/Turn 使用自己开始时捕获的 activation/catalog snapshot。Plugin update 不改变已开始的 model\ninvocation；新版本只在下一个 safe point 生效。\n\n## 12. 更新、回滚与卸载\n\n### 12.1 更新\n\n```text\nfetch exact candidate\n→ stage and verify\n→ diff permissions/contributions/credentials\n→ require consent for any grant expansion\n→ resolve candidate generation\n→ publish atomically\n→ drain previous generation\n→ retain rollback object\n```\n\n相同 Plugin version 出现不同 digest 必须拒绝，不能当普通 update。permission expansion、new\ncredential slot、new executable 或 endpoint 变化都需要重新授权。\n\n### 12.2 回滚\n\nRollback 是切换 authority 的 exact package ref，不重新下载，也不修改旧 object。若旧版本已被\nrevoked/quarantined，则不可 rollback。\n\n### 12.3 Disable 与卸载\n\nDisable：\n\n- 阻止新 Turn 捕获 contribution；\n- 停止/排空对应 MCP session；\n- 保留 package、配置和可解释 diagnostics；\n- 不删除 historical Thread 中的 tool/skill provenance。\n\nUninstall：\n\n- 必须先 disable；\n- active generation 和 in-flight runtime 引用归零后才移除 installed record；\n- object 只有在无任何 version/profile/rollback 引用时才进入垃圾回收；\n- credential value 不随 Plugin package 删除；只解除 slot binding，是否删除 secret 由 credential\n  owner 决定；\n- workspace 中的声明保留为 unresolved request，不能静默重装。\n\n## 13. Skill、Connector 与 MCP 的明确关系\n\n| 行为 | Plugin manager | Skill manager | Connector / Auth | MCP runtime |\n| --- | --- | --- | --- | --- |\n| 校验 package digest/path | 负责 | 不负责 | 不负责 | 不负责 |\n| 解析 `SKILL.md` | 不负责 | 负责 | 不负责 | 不负责 |\n| 选择/加载 Skill | 不负责 | 负责 | 不负责 | 不负责 |\n| 管理外部账号连接状态 | 只声明 Connector | 不负责 | Connector runtime 负责 | 不负责 |\n| OAuth/API-key 交互和 secret bytes | 只声明 credential slot | 不负责 | Auth adapter 执行，Secrets owner 保存 | 只消费 materialized credential |\n| 发布 ready runtime binding | 只提供 declaration/provenance | 不负责 | connected generation 负责 | 只消费 binding |\n| 解析 MCP JSON-RPC | 不负责 | 不负责 | 不负责 | 负责 |\n| 启动 MCP process/session | 只声明/grant | 不负责 | 不启动 | 负责 |\n| 执行 script/tool | 不负责 | 不执行 | 不执行 Tool | MCP tool 只执行协议调用 |\n| approval/sandbox | 只提供最大 grant | 不扩大权限 | 连接成功不代表调用获批 | 服从 Agent/host policy |\n\n一个 Skill 可以指示 Agent 使用同 Plugin 的 MCP tool，但关联必须通过 stable contribution identity\n解析。Skill 文本不能通过写一句“此工具已批准”跳过 grant 或 approval。\n\n### 13.1 Connector 与 built-in 工具\n\n`Connector` 是外部产品连接的 domain object，不是 package format。Plugin 可以贡献 Connector declaration\n和它引用的 MCP declaration，但二者进入独立的 Connector/MCP runtime 后，identity 和 lifecycle 仍然不同。\nConnector 当前通过 MCP binding 连接能力 runtime；未来只有出现真实的非 MCP consumer 后，才扩展其他\nbinding variant。\n\n当前 v1 manifest 已允许 `contributions.connectors[]` 用 manifest-local ID 引用同包的一个\n`mcpServers[]`。`zeta-connectors-extension::ConnectorCatalog` 将声明转换为\n`zeta-connectors::ConnectorSnapshot`：disconnected entry 进入 discovery，只有认证 owner 通过合法\ngeneration transition 发布 `ConnectorAccount` 后才输出 ready MCP server ID。当前 API-token adapter、\nSQLite authority、exact activation 到 package-rooted MCP provider 的自动构造、独立 Plugin MCP、\nConnector-bound MCP composition、通用 OAuth PKCE 状态机、Desktop browser callback 与 GitHub provider\n已实现。Plugin enable/update 已能 live replacement，并通过 exact invocation lease 阻止旧 contribution\n在 authority commit 后开始 dispatch。完整边界由 [`connectors.md`](connectors.md) 维护。\n\n| 概念 | Identity/lifecycle | 例子 |\n| --- | --- | --- |\n| Plugin | package ID + version + digest；install/update/uninstall | 一组 GitHub 扩展贡献 |\n| Connector | account/tenant + credential binding；connect/revoke | 用户的某个 GitHub organization |\n| MCP server | server definition + connection generation | GitHub MCP session |\n| Skill | source + name + content digest；select/activate | PR review workflow |\n| Built-in tool | Zeta release 中的 compiled capability | 本地受控 command executor |\n\n卸载 Plugin 只解除其 contribution 和 credential-slot binding，不能擅自删除对应 auth domain\n中可能被其他 connector 使用的 secret。反过来，revoke connector credential 会让相关 MCP\nruntime 不可用，但不会把 Plugin 标成未安装。\n\n## 14. App Server API 与客户端\n\n当前 App Server surface 与后续演进：\n\n| 状态 | Method | Authority/effect |\n| --- | --- | --- |\n| ✅ 当前 | `plugin/list` | 读取 installed/enabled/granted/effective projection |\n| ✅ 当前 | `plugin/enable` / `plugin/disable` | exact-package CAS 修改 profile enablement |\n| ✅ 当前 | `plugin/grant` / `plugin/revokeGrant` | exact-package CAS 修改 explicit grants |\n| ✅ 当前 | `plugin/uninstall` | 仅在 disabled + revoked 后移除 authority reference |\n| ✅ 当前 | `plugin/marketplace/list` / `plugin/install` | 只接受 host 注册 catalog 中的 exact digest，不接受客户端路径 |\n| ✅ 当前 | `plugin/update` / `plugin/rollback` | staged newer package；rollback 只切到已安装且已授权的旧 digest |\n| 尚未完成 | operation read API | 长操作 progress/result |\n\n已实现 mutation 使用 `CommandId + expectedRevision + exact package payload`；安装入口不会接受 Renderer\n传入的任意宿主文件路径。`Managed` Marketplace root 由产品分发层注册，`LocalDevelopment` 仅在 host\n显式开启时可用。读取模式不决定运营方信任：官方远端源仍为 `ProductManaged`，只有由 host 明确接入、\n固定独立 root 并声明 non-empty `allowedPublishers` 的第三方源才是 `VerifiedExternal`；任一签名 target\n越过该 namespace scope 会使整源 fail-closed。不同远端源实际发布同一 publisher namespace 时，组合也会\n因 owner ambiguity 失败，而不是按源顺序覆盖。`RemoteManaged` Marketplace 已通过 host-pinned\nTUF root 同步 HTTPS catalog：\ntimestamp/snapshot/targets 的 threshold、rollback 与 expiry 检查由 TUF verifier 执行；package 必须来自\n`publishers/<publisher>` delegated role。远端协议是产品无关的 `marketplacePackage` exact identity 与\n`marketplaceCatalog` discovery envelope；Zeta 只解释其中可选的 `consumerMetadata.zeta`，不含该 adapter\n的 package 会被忽略，而不会让整个源失败。刷新只缓存签名 metadata、generic discovery metadata 与撤销\ntarget，不预取 ZIP；安装或更新时才重新检查当前 TUF/revocation authority、读取一个 exact target，并通过\ntarget hash/length、受限 ZIP extraction、manifest identity 与 `MarketplaceV1` normalized digest。transport 失败可打开\n仍未过期的最后目录缓存；离线安装只允许此前已经 materialize 且再次通过 exact digest 的包。顶层\nrevocation target 会写入 durable exact-package tombstone，不因后续 feed 缺项自动恢复。可搜索的 Desktop\n目录、按需 package cache quota/GC 已实现；download progress 与 permission/contribution diff 仍未实现。\n\n正式 package 把只读配置和公开信任根放在\n`zeta-resources/product-services/{product-services.json,marketplace-root.json}`。`zeta-cli` 通过\n`zeta-install-context` 发现该资源，默认注册 `https://chogng.github.io/marketplace/` 的 `zeta`\nMarketplace；`ZETA_PRODUCT_SERVICES_PATH` 与 App Server 的 `--product-services PATH` 仍是产品宿主的\n显式覆盖入口。远端 metadata、Plugin、用户配置和 Workspace 都不能更换这份 root。发行源仓库仍为\nprivate，Pages 只暴露经过 Marketplace 自身独立 validator 和 TUF verifier 复核的 `metadata/` 与\n`targets/` 静态产物。Marketplace 不依赖 Zeta 源码或发布状态；Zeta 只是通过 pinned root 和 consumer\nadapter 选择性消费它。\n默认产品文件只启用官方源；第三方源只能由 host 在同一只读文件中追加，不能从 Plugin、自身远端 metadata\n或普通用户设置提升为发行信任。\n\n客户端必须展示：\n\n- exact ID/version/digest/origin；\n- signed/unsigned/revoked/quarantined 状态；\n- requested 与 granted permissions diff；\n- 贡献的 Skills、Connectors 和 MCP servers；\n- credential slots 只显示绑定状态，不显示 secret；\n- update 的 contribution/permission diff；\n- blocked/broken 的稳定诊断。\n\nCLI/TUI/Desktop 不直接扫描 Plugin 目录，不解析 manifest，也不自己启动 MCP server。\n\n## 15. 安全\n\nPackage ingestion 必须防御：\n\n- archive path traversal、absolute path 和 drive/device path；\n- symlink/hardlink escape；\n- zip bomb、文件数/单文件/总大小/嵌套深度超限；\n- duplicate normalized path 和 Unicode/path normalization collision；\n- special file、socket、FIFO、device node；\n- executable bit/平台 artifact 欺骗；\n- manifest schema bomb、超长字符串和重复 contribution；\n- TOCTOU：validation 后 object 必须不可变并按 digest 打开；\n- malicious update 改写相同 version；\n- unsigned local package 被 UI 伪装成 trusted registry package。\n\nRuntime 还必须假定 package 内容含 prompt injection 或恶意程序。签名、安装和启用都不能替代\nsandbox、data egress control 与 per-action approval。\n\n## 16. 错误与诊断\n\n至少区分：\n\n```text\nSourceUnavailable\nPackageDigestMismatch\nSignatureInvalid\nPackageUnsafe\nManifestInvalid\nCompatibilityMismatch\nPermissionGrantRequired\nCredentialBindingRequired\nContributionConflict\nContributionInvalid\nActivationFailed\nPackageRevoked\nPackageInUse\nCommandConflict\n```\n\n错误中不包含 secret、完整 env 或敏感 file content。diagnostic 必须能回答：\n\n- 哪个 exact package 被选择；\n- 为什么某版本被 pin/阻止；\n- 哪个 permission/credential 缺失；\n- 哪个 contribution/path 冲突；\n- 当前 active generation 与上一个 rollback generation；\n- 哪个 MCP/Skill consumer 拒绝了 contribution。\n\n## 17. PL1+目标目录\n\nPL1+ 继续保持单 crate，目标扩展为：\n\n```text\nzeta-rs/plugins/src/\n├── lib.rs\n├── identity.rs\n├── manifest/\n│   ├── mod.rs\n│   ├── model.rs\n│   └── validation.rs\n├── package/\n│   ├── mod.rs\n│   ├── source.rs\n│   ├── staging.rs\n│   ├── store.rs\n│   └── trust.rs\n├── authority/\n│   ├── mod.rs\n│   ├── command.rs\n│   ├── record.rs\n│   └── recovery.rs\n├── resolution/\n│   ├── mod.rs\n│   ├── grant.rs\n│   ├── conflict.rs\n│   └── snapshot.rs\n├── contribution.rs\n├── diagnostic.rs\n├── error.rs\n└── *_tests.rs\n```\n\n不建立通用 `extension host`、`plugin service` 或 `dynamic loader`。如果 manifest 纯值层后来确有\n三个以上独立消费者，再提取 `zeta-plugin-manifest`；第一版不要先拆空 crate。\n\n## 18. 分阶段实施\n\n### 阶段 PL0：清单+ local validation（已完成）\n\n- 固定 v1 schema、identity、path 和 digest；\n- local development package discovery；\n- filesystem path/schema 安全 fixtures；Archive source 尚未启用，因此 archive ingestion\n  fixtures 随对应 source 一起加入；\n- App Server `plugin/list` 与 lifecycle mutation 已接入；任意 host path install 有意不开放给 Renderer。\n\n当前完成条件：任何 contribution path 都不能逃出已验证 local snapshot root；安装时必须复制到\ncontent-addressed object、重新验证 exact digest，再原子 promote。mutable local root 不会被发布给 runtime。\n\n### 阶段 PL1：权威+激活（已完成）\n\n- ✅ local content-addressed store：unique staging、copy-time validation、digest revalidation、atomic promote；\n- ✅ install/enable/disable/grant/revoke/uninstall typed authority commands；\n- ✅ exact installed package resolution 与 activation snapshot generation；\n- ✅ durable install/enable/disable/uninstall authority record 与 typed command replay；\n- ✅ live revision/activation subscription、App Server lifecycle RPC/reconcile 与 exact invocation drain；\n- ✅ Plugin Skill contribution 的 exact immutable source projection 与 live catalog refresh；\n- ✅ declarative Extension contribution 的 exact immutable source projection、precedence 与 live refresh；\n- ✅ Connector contribution 的 normalized projection；\n- ✅ user-profile exact Marketplace reconcile；Workspace request 保持只读、不能自动 grant；\n- 尚未完成：startup orphan recovery。\n\n完成条件：失败激活不改变上一 generation，重启可恢复唯一 active package set。\n\n### 阶段 PL2：MCP 贡献\n\n- ✅ package-rooted strict MCP definition 与 standalone/Connector projection；\n- ✅ manifest process/network/credential ceiling 校验；\n- ✅ 与 `zeta-mcp` 的 prepare/publish 和 safe-point old-generation drain；\n- ✅ Plugin disable/update authority 触发 live activation、safe-point replacement 与 dispatch drain。\n\n完成条件：安装不启动进程，enable 无 grant 不启动，update 不劫持 in-flight tool binding。\n\n### 阶段 PL3：远端目录、signature 与更新增强\n\n- ✅ host-registered Marketplace metadata 与 digest-pinned materialized package ingestion；\n- ✅ TUF 远端 signed discovery catalog、离线 metadata cache 与按需 exact package download；\n- ✅ delegated publisher signature、root rotation/expiry/rollback 与顶层 revocation feed；\n- permission/contribution diff；\n- ✅ side-by-side staged update 与 exact rollback；\n- ✅ 每 Marketplace materialized cache count/bytes budget、signed-target-aware eviction、protected\n  install handoff 与 retained/evicted/excess report；installed content store 不受 cache GC 影响。\n\n完成条件：相同 ID/version 不可换内容，grant expansion 必须重新 consent。\n\n### 阶段 PL4：生态扩展评审\n\n当前已完成的窄边界：\n\n- ✅ Plugin manifest/immutable package 对可执行 Editor Extension program 的 strict declaration；\n- ✅ exact process permission、Host RPC API v1、activation/capability ceiling 与 regular-file 校验；\n- 委托 `zeta-editor-extension-host`：进程隔离、RPC、crash supervisor、provider lifecycle；\n- ❌ generic Node/WASM loader、VS Code Extension API compatibility 与 `workspaceContains` scanner。\n\n其余能力只有在具体需求有独立 threat model 和 stable port 后，才分别评审：\n\n- declarative hooks；\n- WASM capability component；\n- provider adapter；\n- 静态 UI contribution。\n\n禁止直接加入 native dynamic library 或任意 Renderer JavaScript。\n\n## 19. 验证门\n\n除常规 Rust workspace 检查外，必须覆盖：\n\n- manifest unknown field、invalid ID/version、duplicate contribution；\n- Editor Extension runtime API、exact executable permission、activation/capability bound 与 entrypoint file type；\n- path traversal、symlink/hardlink、normalization collision 和 archive bomb；\n- declarative Extension 目录、`package.json` file type、source precedence 与 activation refresh；\n- digest/signature/revocation；\n- install 每个 durable boundary 的 crash recovery；\n- enable/grant `CommandId` replay 和 payload conflict；\n- workspace/user pin conflict；\n- permission/credential diff；\n- activation prepare 失败、publish race、generation drain；\n- update/rollback/uninstall 的 reference safety；\n- disabled/quarantined package不能贡献 Skill 或启动 MCP；\n- logs/schema/App Server response 不泄露 secret 或内部 writable path。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-当前仓库审计",
        "title": "2. 当前仓库审计"
      },
      {
        "depth": 2,
        "id": "3-职责与非职责",
        "title": "3. 职责与非职责"
      },
      {
        "depth": 3,
        "id": "31-plugin-manager-拥有",
        "title": "3.1 Plugin manager 拥有"
      },
      {
        "depth": 3,
        "id": "32-plugin-manager-不拥有",
        "title": "3.2 Plugin manager 不拥有"
      },
      {
        "depth": 2,
        "id": "4-目标依赖与组合",
        "title": "4. 目标依赖与组合"
      },
      {
        "depth": 2,
        "id": "5-包与清单",
        "title": "5. 包与清单"
      },
      {
        "depth": 3,
        "id": "51-v1-布局",
        "title": "5.1 v1 布局"
      },
      {
        "depth": 3,
        "id": "52-清单结构",
        "title": "5.2 清单结构"
      },
      {
        "depth": 3,
        "id": "53-身份",
        "title": "5.3 身份"
      },
      {
        "depth": 2,
        "id": "6-包来源来源与信任",
        "title": "6. 包来源、来源与信任"
      },
      {
        "depth": 2,
        "id": "7-install-store-与事务",
        "title": "7. Install store 与事务"
      },
      {
        "depth": 3,
        "id": "71-内容寻址的不可变存储",
        "title": "7.1 内容寻址的不可变存储"
      },
      {
        "depth": 3,
        "id": "72-权威与投影",
        "title": "7.2 权威与投影"
      },
      {
        "depth": 2,
        "id": "8-状态模型",
        "title": "8. 状态模型"
      },
      {
        "depth": 2,
        "id": "9-范围优先级与冲突",
        "title": "9. 范围、优先级与冲突"
      },
      {
        "depth": 2,
        "id": "10-权限与凭据",
        "title": "10. 权限与凭据"
      },
      {
        "depth": 3,
        "id": "101-两层授权",
        "title": "10.1 两层授权"
      },
      {
        "depth": 3,
        "id": "102-权限类型",
        "title": "10.2 权限类型"
      },
      {
        "depth": 2,
        "id": "11-贡献激活",
        "title": "11. 贡献激活"
      },
      {
        "depth": 2,
        "id": "12-更新回滚与卸载",
        "title": "12. 更新、回滚与卸载"
      },
      {
        "depth": 3,
        "id": "121-更新",
        "title": "12.1 更新"
      },
      {
        "depth": 3,
        "id": "122-回滚",
        "title": "12.2 回滚"
      },
      {
        "depth": 3,
        "id": "123-disable-与卸载",
        "title": "12.3 Disable 与卸载"
      },
      {
        "depth": 2,
        "id": "13-skillconnector-与-mcp-的明确关系",
        "title": "13. Skill、Connector 与 MCP 的明确关系"
      },
      {
        "depth": 3,
        "id": "131-connector-与-built-in-工具",
        "title": "13.1 Connector 与 built-in 工具"
      },
      {
        "depth": 2,
        "id": "14-app-server-api-与客户端",
        "title": "14. App Server API 与客户端"
      },
      {
        "depth": 2,
        "id": "15-安全",
        "title": "15. 安全"
      },
      {
        "depth": 2,
        "id": "16-错误与诊断",
        "title": "16. 错误与诊断"
      },
      {
        "depth": 2,
        "id": "17-pl1目标目录",
        "title": "17. PL1+目标目录"
      },
      {
        "depth": 2,
        "id": "18-分阶段实施",
        "title": "18. 分阶段实施"
      },
      {
        "depth": 3,
        "id": "阶段-pl0清单-local-validation已完成",
        "title": "阶段 PL0：清单+ local validation（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段-pl1权威激活已完成",
        "title": "阶段 PL1：权威+激活（已完成）"
      },
      {
        "depth": 3,
        "id": "阶段-pl2mcp-贡献",
        "title": "阶段 PL2：MCP 贡献"
      },
      {
        "depth": 3,
        "id": "阶段-pl3远端目录signature-与更新增强",
        "title": "阶段 PL3：远端目录、signature 与更新增强"
      },
      {
        "depth": 3,
        "id": "阶段-pl4生态扩展评审",
        "title": "阶段 PL4：生态扩展评审"
      },
      {
        "depth": 2,
        "id": "19-验证门",
        "title": "19. 验证门"
      }
    ],
    "searchText": " 物理位置：zeta-rs/plugins/ Rust crate：zetaplugins 当前状态：PL0 已实现并支持 ConnectorContribution 引用 McpServerContribution 的声明校验；PL1 的 content-addressed store、Marketplace-first install/staged update/rollback、profile reconcile、durable installed/enabled/granted/effective authority、exact PluginActivationSnapshot、live generation publish 与 invocation drain 已实现；Connector domain 已提取到 zeta-rs/connectors，Plugin projection、durable authority 与 API-token connect/revoke 位于 zeta-rs/ext/connectors；App Server 已能从注入的 activation 自动接线 Connector 与 MCP，通用 OAuth PKCE/device 状态机、App Server control plane、Desktop/TUI 产品入口与 GitHub providers 已实现； TUF 远端 Marketplace、delegated publisher、离线签名目录缓存、按需安全 ZIP ingestion 与 revocation tombstone 已实现；PL4 的可执行 Editor Extension 安装/授权声明已实现，Host runtime 不由本 crate 拥有 当前 crate 实现契约：zeta-rs/plugins/README.md 远端分发实现契约：zeta-rs/plugin-marketplace/README.md Connector account/lifecycle：connectors.md MCP runtime：mcp.md Skill runtime：skills.md Config authority 与 runtime snapshot 接入：config.md Editor Extension 双轨系统边界：editor-extensions.md 可执行 Host runtime 实现：zeta-rs/editor-extension-host/README.md 快速理解 Plugin 是经过校验和版本管理的扩展包，不是安装后便能执行任意代码的进程内插件。安装、启用、 授权和实际调用是四个独立阶段。 用户动作 系统发生什么 不会自动发生什么 --- --- --- 安装 Plugin 校验不可变包、清单、来源和内容摘要后写入本地存储 不启用贡献、不授予权限 在用户或 Workspace 中启用 允许其贡献参与解析 不连接 Connector、不启动 MCP、不执行脚本 批准请求的能力 记录精确的进程、网络、目录或凭据授权 不批准未来每一次工具调用 激活贡献 生成带来源和 generation 的不可变快照 不把 live manager 注入 Agent 更新或回滚 并存校验后的版本并原子切换 不原地修改已安装包 卸载 撤销后续激活并清理可回收内容 不删除其他领域拥有的秘密或历史 打开正式打包的 Zeta 从产品内固定的 root 刷新官方 HTTPS Marketplace 不信任服务器提供的新 root，不自动安装或启用 Plugin 浏览 Marketplace 读取已签名 manifest、能力、权限与包统计；离线时可使用仍有效的目录缓存 不预下载所有 Plugin ZIP 安装远端 Plugin 重新检查 TUF 与撤销状态，只下载所选 exact ZIP，再校验内容摘要 不因已浏览或已下载而自动启用、授权 1. 结论 zeta-plugins 是 Zeta 的扩展分发、安装、解析、启用和版本管理控制面。Plugin 是一个不可变、 可校验的包，可以贡献 Skill、Connector declaration、MCP server declaration、可执行 Editor Extension 声明和静态资源；Plugin manager 将这些贡献 解析为带来源和 digest 的 activation snapshot，再由 App Server 注入对应 runtime。 Plugin 不是： - 第三方 Rust dynamic library 或稳定 in-process ABI； - 一段安装后自动获得系统权限的代码； - Skill、MCP server、tool 或 connector 的同义词； - Thread/Turn 执行状态或 durable transcript； - secret container； - 允许扩展绕过 approval、sandbox、credential 和 network policy 的信任标记。 边界固定为： ``mermaid flowchart TD P \"Plugin activation snapshot\" -- S \"SkillContribution → Skill runtime\" P -- C \"ConnectorContribution → Connector runtime\" P -- M \"McpServerContribution\" P -- E \"EditorExtensionContribution → executable Host RPC v1 program\" P -- D \"DeclarativeExtensionContribution → static package.json catalog\" P -- A \"StaticAssetContribution → Resource consumer\" C -. \"references declaration\" .- M C -- \"connected\" B \"Ready MCP binding\" M -- \"standalone activation\" R \"MCP runtime\" M -- B E -- H \"zeta-editor-extension-host supervisor\" D -- X \"zeta-extensions immutable snapshot\" B -- R R -- T \"Tool Registry / Core\" ` Plugin 只负责声明控制面和 package lifecycle。Skill、Connector、MCP 和 Resource consumer 分别拥有自己的运行时 语义；它们不是 Plugin manager 内部的 live 子对象。Plugin、Connector 与 MCP 的 canonical 关系由 connectors.md 维护。 静态 Editor Extension 保持另一套内容边界：它读取自己的 package.json 和声明式 language/TextMate/snippet/theme/debugger 资源。Plugin v1 现在可用 declarativeExtensions 指向包内 静态 Extension 目录；只有 effective exact Plugin package 会被 App Server 投影到 zeta-extensions。 这共享 install/enable/grant/revocation lifecycle，但不合并两种 manifest，也不把静态内容变成可执行 runtime。其 canonical 文档是 editor-extensions.md。 Plugin v1 现在提供显式 editorExtensions bridge。每项指向包内一个可直接启动、自己实现 Zeta Host RPC v1 的程序；它不是由通用 Node/WASM runtime 加载的脚本。Plugin manager 只验证并授权声明， zeta-editor-extension-host supervisor 才拥有逐扩展进程隔离、RPC、crash recovery 和 provider lifecycle。静态 package.json catalog 不会被隐式转换成该 executable declaration。 declarativeExtensions 是另一条显式 bridge：每项只有 manifest-local ID 和 package-relative directory，目录必须包含 regular package.json。它不需要 process permission；安装只存储 bytes， enable + grant 后才进入静态 catalog，disable、grant revoke、package revoke、update 或 uninstall 会使 下一次 catalog refresh 移除旧 exact package。Workbench 监听 Plugin activation generation 并自动刷新。 生产第三方执行还必须由产品注入能够实施 sandbox、memory/CPU/process hard limits 和 process-tree termination 的 platform launcher；没有该 launcher 时 Host capability 必须为 false，不能用 TrustedDevelopmentLauncher 降级。该 v1 是 Zeta-native executable RPC，不是 VS Code/Node Extension Host。 安装、启用、授权和调用是四个不同动作： 1. Install：包进入本地 content store； 2. Enable：某个 user/workspace profile 允许其贡献参与解析； 3. Grant：允许所需 process/network/root/credential capability； 4. Invoke：Agent 的某次 tool/script 操作仍经过 runtime approval 与 sandbox。 任何一个动作都不能隐含下一个动作。 2. 当前仓库审计 当前已创建 zeta-plugins，实现 strict v1 manifest、Plugin identity/SemVer、portable package-relative path、本地 package 安全校验、确定性 digest、只读 local-development discovery， 以及 stage-copy-revalidate-atomic-promote 的 local content-addressed store。实现细节、limits 与 failure semantics 由 crate README 维护。 User/Workspace TOML 与 App Server 已能表达 exact Plugin request 和 desired enablement。产品 host 注册 受管 Marketplace root；resolver 只按 catalog 中的 exact ID/version/digest 安装并协调 user-profile enablement，不自动授予权限。Workspace request 只能读取 profile 中可用、待 profile 安装或版本不匹配 三种结果，不能以 Workspace trust 自动扩大 profile authority。Package store 安全保存 immutable object， 并把 exact installed package 解析为 generation-bound activation snapshot；App Server 可据此自动构造 Skill source、Connector catalog、durable authority 和 package-rooted MCP provider。Plugin authority 分别持久化 installed/enabled/granted/effective refs 和 command receipts，并驱动 live activation 切换； App Server 已暴露 Marketplace list/install/update/rollback 及 enable/disable/grant/revokeGrant/uninstall。 docs/tui.md 也明确要求 Plugin domain projection 进入 canonical App Server contract 后，TUI 才能增加管理 feature。TUI 已有可复用的 interaction view stack 与 tabs/search/selection presentation primitive，但当前没有 Plugin view model 或 /plugins command； 这些 UI 基础设施不改变本节的 backend gate。 已有可复用边界： - zeta-config 提供 ordinary config authority、typed patch 和 CommandId replay； - 各 credential domain 是生命周期 owner，zeta-secrets 是 opaque secret persistence owner； - zeta-sandboxing、zeta-tool-executor 和 host capability 是已分离的本地进程执行权限边界； 产品层 zeta-exec 只运行完整的无界面 Agent Turn； - App Server 是本地 runtime 的 composition root； - Resource store 可承载大块只读内容，但不是 Plugin package authority； - zeta-protocol 已固定“共享纯语义进入 protocol，I/O 和 policy 留在执行层”的规则。 因此第一版不应从“动态加载代码”开始，而应先完成一个 declarative package： `text Plugin v1 contributions = Skills + Connectors + MCP server declarations + executable Editor Extension declarations + static assets ` 第三方 UI、native library、hooks、model provider adapter 和任意 App Server method registration 不在 v1。 3. 职责与非职责 3.1 Plugin manager 拥有 - Plugin package layout 和 manifest schema； - stable Plugin identity、version、digest 和 origin； - package staging、validation、atomic install、side-by-side update 和 recoverable remove； - user/workspace enablement 和 version pin； - contribution discovery、path containment、compatibility 和 conflict validation； - requested permissions、credential slots 与 user grants 的差异计算； - immutable PluginActivationSnapshot 和 generation； - package provenance、signature/trust result、revocation/blocked diagnostics； - enabled Plugin 向 Skill/Connector/MCP runtime 的 normalized contribution projection； - executable Editor Extension 的 exact program、Host RPC v1、activation trigger 与 capability ceiling 声明； - install/update/enable/disable/uninstall 的 typed command replay； - 不含秘密的 audit record 和 health projection。 3.2 Plugin manager 不拥有 - Skill 的自动选择、prompt layering 或 context budget； - MCP JSON-RPC、process supervision、Connector connection/OAuth 或 tools/resources/prompts catalog； - Editor Extension Host RPC transport、process supervision、crash recovery 或 provider registry； - script、binary 或 MCP tool 的实际执行； - API token、OAuth token、cookie 或 private key； - OS sandbox、network enforcement 或 per-call approval 的最终实现； - Thread reducer、Tool Call/Result commit 或 Agent retry； - Marketplace 搜索和支付业务； - 第三方 UI iframe、Renderer code execution 或 Electron preload extension； - 任意 native ABI、WASM ABI 或 provider adapter ABI。 4. 目标依赖与组合 `text zeta-protocol ▲ │ shared IDs only when accepted │ zeta-plugins manifest / package store / resolver / authority / snapshot ▲ ▲ │ package source │ trust verifier │ │ filesystem/registry signature service \\ / \\ / App Server composition │ │ │ SkillContribution ConnectorContribution McpServerContribution │ │ │ ▼ ▼ ▼ zeta-skills zeta-connectors zeta-mcp ` 具体规则： - zeta-plugins 不依赖 zeta-skills、zeta-connectors 或 zeta-mcp live runtime； - Plugin manager 只输出 normalized descriptor 和 immutable root handle； - App Server 将 Skill contribution 注册到 Skill source，将 Connector contribution 交给 Connector adapter， 并将独立或 ready-bound MCP contribution 解析为 McpServerDefinition； - contribution consumer 必须再次执行自己领域的校验，不能因为 package 已验证就跳过 schema、 content 或 runtime policy； - Plugin state 不进入 SessionStore/ThreadStore； - App Server protocol 只暴露稳定 Plugin view 和 command DTO，不暴露内部 filesystem path、 lock、transaction journal 或 signature library type。 5. 包与清单 5.1 v1 布局 `text plugin-root/ ├── .zeta-plugin/ │ └── plugin.json ├── skills/ │ └── code-review/ │ ├── SKILL.md │ ├── references/ │ ├── scripts/ │ └── assets/ ├── mcp/ │ └── review-server.json ├── bin/ │ └── review-server ├── assets/ └── LICENSE ` 目录可以省略，但 .zeta-plugin/plugin.json 必须存在。manifest 中只允许相对 package root 的 slash-separated path；绝对路径、..、空 segment、NUL、平台 device path 和 escape symlink 全部拒绝。 5.2 清单结构 以下是当前 v1 语义示例；当前 authority 是 Rust strict parser，尚未发布独立 JSON Schema： `json \"schemaVersion\": 1, \"id\": \"acme/code-review\", \"version\": \"1.2.0\", \"displayName\": \"Acme Code Review\", \"description\": \"Review workflows and an optional MCP server.\", \"license\": \"Apache-2.0\", \"compatibility\": \"zeta\": \" =0.1.0\" , \"contributions\": \"skills\": \"id\": \"code-review\", \"path\": \"skills/code-review\" , \"mcpServers\": \"id\": \"review\", \"definition\": \"mcp/review-server.json\" , \"assets\": \"id\": \"icon\", \"path\": \"assets/icon.png\" , \"editorExtensions\": \"id\": \"review-runtime\", \"entrypoint\": \"bin/review-extension-host\", \"runtimeApiVersion\": 1, \"activationEvents\": \"type\": \"onCommand\", \"id\": \"acme.review.run\" , \"type\": \"onLanguage\", \"id\": \"rust\" , \"type\": \"onDemand\", \"capability\": \"testProfileProvider\" , \"capabilities\": \"command\", \"languageProvider\", \"testProfileProvider\" , \"permissions\": \"type\": \"process\", \"executable\": \"bin/review-server\" , \"type\": \"process\", \"executable\": \"bin/review-extension-host\" , \"type\": \"workspace\", \"access\": \"read\" , \"type\": \"network\", \"hosts\": \"api.example.com\" , \"credentialSlots\": \"name\": \"api-token\", \"kind\": \"secretText\", \"requiredFor\": \"mcp:review\" ` Manifest 必须 strict-parse： - unknown top-level/security-sensitive field 明确报错； - 自定义展示 metadata 只能放在 namespaced metadata； - schemaVersion 是 manifest format version，不是 Plugin version； - Plugin version 使用一个已选择并统一实现的 version scheme； - 同一 Plugin version 的内容 digest 必须唯一；registry 不得让相同 PluginId, version 指向不同内容； - manifest 只声明 credential slot，不包含 secret value； - permission 使用 tagged enum，不使用 network: true、workspace: \"all\" 一类含糊开关。 editorExtensions 是 strict typed control-plane declaration：ID 与 entrypoint 各自唯一；entrypoint 必须是包内 regular file，并有完全相同路径的 process permission；runtimeApiVersion 仅接受数值 1；activation event 和 capability ceiling 都必须 non-empty、unique、bounded。v1 triggers 是 startup、onCommand、onLanguage、onDemand、onDebugType、onTaskType 和 onTestProfile。除 startup 外，trigger 不得请求 ceiling 中未声明的 capability。 workspaceContains 当前故意不在 schema 中：安全实现必须由 Workspace authority 提供 bounded scanner 与 ignore/trust 语义，不能让扩展程序自行扫描工作区，也不能让 Host 静默忽略一个看似有效的 trigger。 exact process permission 只给 supervisor 一条 package-relative launch ceiling；provider invocation 仍走已经启动的 RPC session，不会每次重新直接执行 entrypoint。regular-file/containment 校验不承诺该 artifact 在当前 OS、CPU 或 ABI 上可运行，也不检查 Unix executable bit、Windows PE/扩展名或代码签名。 当前 v1 没有 per-platform artifact selector；supervisor 必须在 activation 时检查 launchability 并对不兼容 artifact fail closed。 5.3 身份 推荐 Plugin ID 为 publisher/name，两段都使用 lowercase ASCII、数字和单连字符，并限制总长度。 display name 可本地化且可变化，不能充当 identity。 `rust pub struct PluginId String ; pub struct PluginVersion String ; pub struct PluginPackageDigest String ; pub struct InstalledPluginRef pub id: PluginId, pub version: PluginVersion, pub digest: PluginPackageDigest, ` Plugin contribution identity 是： `text Plugin"
  },
  {
    "slug": "slash-commands",
    "sourcePath": "docs/slash-commands.md",
    "title": "Slash Commands 与 Slash Launcher 架构",
    "description": "Slash Command 是一种真正可调用的命令；斜杠启动面板只是用户输入 / 后出现的快速选择器。面板 可以展示命令、Skills 或其他产品列表，选中什么由列表来源解释，不能因为它们出现在同一个面板里 就都变成 Slash Command。",
    "group": "Agent 与运行时",
    "markdown": "> 本文拥有 Slash Commands 的跨产品语义、运行时边界与当前接入状态。Rust 实现细节由\n> [`zeta-slash-commands` README](../zeta-rs/slash-commands/README.md) 拥有；通用斜杠启动面板\n> （Slash Launcher）的实现契约由\n> [`zeta-slash-launcher` README](../zeta-rs/slash-launcher/README.md) 拥有；App Server wire snapshot\n> 由 [`zeta-app-server-api.md`](zeta-app-server-api.md) 拥有。Slash Command 与\n> Instructions/Skills/Agents artifact 的关系由\n> [`agent-customizations.md`](agent-customizations.md) 统一定义。\n\n## 快速理解\n\nSlash Command 是一种真正可调用的命令；斜杠启动面板只是用户输入 `/` 后出现的快速选择器。面板\n可以展示命令、Skills 或其他产品列表，选中什么由列表来源解释，不能因为它们出现在同一个面板里\n就都变成 Slash Command。\n\n| 用户看到或产品要做的事 | 正确抽象 | 谁决定内容 |\n| --- | --- | --- |\n| 输入 `/` 后出现快速选择面板 | Slash Launcher | 产品选择并组合列表 |\n| TUI 展示可执行 `/command` | Slash Command list | `zeta-code` 的命令 adapter |\n| Desktop 展示可调用 Skills | Skill list | Desktop 的 Skill adapter |\n| zeterm 同时展示命令、Skills 或其他动作 | 多列表 Launcher | zeterm 选择所需列表及顺序 |\n| 选中一项后真正执行或注入上下文 | 来源自己的 typed binding | 对应产品/领域 owner |\n\nApp Server 当前在 `initialize.slashCommands` 发布 server commands；每个 client 再与自身真正可执行的\nlocal commands 合并。命令定义、名称冲突、补全和提交解析属于 Slash Commands；列表组合、跨来源\n匹配和面板选择属于 Slash Launcher；行布局、DOM/WGPU/Ratatui 绘制和平台输入事件仍留在各 renderer。\n\n## Launcher 分层\n\n`zeta-slash-launcher` 只接受产品构造的 `SlashLauncherList`，并返回稳定的\n`(list_id, item_id)` 选择。它不依赖 `zeta-slash-commands` 或 `zeta-skills`，因此产品可以自由选择：\n\n- TUI 只传 Slash Command list；\n- Desktop 只传 Skill list；\n- zeterm 或未来产品按需拼接多个列表；\n- 新列表通过产品 adapter 加入，不修改 Launcher 的领域模型。\n\n选中项的业务 target、执行 handler、Skill 上下文注入和授权都留在列表来源。Launcher 不允许按展示\n名称猜测业务对象，也不拥有 App Server protocol。\n\n**当前状态**：通用 crate、列表组合、查询和选择状态已经实现；三种产品尚未迁移到该 crate。下面\n表格描述的是迁移前的现有 Slash Command 接入，不能据此把 Skill projection 当作 Launcher 的长期\n抽象。\n\n| 现有 Surface | Catalog 来源 | Core/adapter | Renderer owner |\n| --- | --- | --- | --- |\n| TUI | built-ins + initialize snapshot + enabled Skill metadata | 直接使用 `zeta-slash-commands`；Skill target 保存在 client binding | Ratatui popup |\n| Native zeta-ui | local `/model` + initialize snapshot | 直接使用 `zeta-slash-commands`；Native 另拥有 model picker | WGPU composer interaction rows |\n| Desktop Chat | Workbench actions + initialize snapshot + enabled Skill metadata | canonical generated `SlashCommandDefinition` + separate action/Skill binding | Aster completion widget；textarea/legacy editor runtime 可复用同一 catalog |\n\nTUI 与 Desktop 已把用户可调用 Skill 以 `/name` 投影到同一补全菜单。启动与刷新只读取 Skill\nmetadata；entry 绑定 exact pinned `SkillRef`，不会把 Skill 正文复制成 command definition。\n\n## 所有权与执行\n\n```text\nApp Server composition\n  → initialize.slashCommands (immutable server snapshot)\n  → client merges executable local commands\n  → validated SlashCommandCatalog\n  → input/query/matches/selection/completion\n  → renderer projection\n  → activation\n      local  → client product command\n      server → unchanged /name text in session/request StartTurn.input\n      skill  → exact SkillRef + unchanged /name text in session/request StartTurn.input\n```\n\n`SlashCommandDefinition` 是三端共享的 Slash Command catalog entry model，不是被调用对象的领域\nmodel。`origin`、Workbench `actionId`、TUI dispatcher identity 和 Skill `SkillRef` 都是与 entry\n分离的 client binding，不能包装成另一种 Slash Command。Skill authority 继续拥有 enablement、\ncompatibility 和 activation validation。\n\nApp Server 不执行 server-advertised Slash Command；它只声明 discoverability 与参数能力。Local command\n必须存在真实 client execution path，否则不能进入 catalog。Desktop 的 `/new`、`/history` 属于\nWorkbench command mapping；Native 的 `/model` 属于 Session model selector；TUI 的 `/theme` 属于\ndevice-local presentation preference：无参数时打开由 `features/theme` 拥有的固定 Zeta Code\nTheme Pane，带 ID 时静默直接切换；Theme Pane 不启用搜索，通用 Selection Pane 则以显式\n`Space search` 进入搜索模式。其他 built-ins 属于 TUI coordination。任意 local/server 同名都拒绝\n整份合并结果，不按客户端优先级静默覆盖。Skill 是动态投影：只有 enabled、compatible、名称唯一且\n不与 local/server command 冲突的 entry 才进入 `/name`；冲突项保留在 `/skills` 管理与诊断目录，\n不会覆盖已有命令或任意选择一个 source。\n\n## Config 边界\n\n`initialize.slashCommands` 不进入通用 config，也不由 slash command view 读取 config。它是 connection\n初始化时冻结的 server capability snapshot。Skill command 是 client 从 `skills/list` metadata 生成的\n动态 projection；`skills/changed` 会使客户端重建这部分 catalog，不修改 server snapshot。\n\n## 当前限制\n\nRust surfaces 直接共享 `zeta-slash-commands` 的 headless state。Desktop 直接消费同一个 generated\n`SlashCommandDefinition` model，并由 Aster Editor 的通用 completion/session state 投影交互；TypeScript\n只保留运行时 catalog binding。Rust crate 与 Desktop adapter 共同执行\n`zeta-rs/slash-commands/fixtures/conformance.json`，确保名称校验、大小写、前缀匹配和参数规则一致。\n\n## 修改影响\n\n新增 wire 字段先修改 `zeta-app-server-protocol` 并重新生成 TypeScript/schema。修改名称、参数、匹配或\n输入规则必须同时更新 crate tests、跨运行时 fixture、TUI/native adapter tests 和 Desktop tests。\n纯视觉变化只修改对应 renderer，不得把 host-specific state 反推到 catalog 或 protocol。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "launcher-分层",
        "title": "Launcher 分层"
      },
      {
        "depth": 2,
        "id": "所有权与执行",
        "title": "所有权与执行"
      },
      {
        "depth": 2,
        "id": "config-边界",
        "title": "Config 边界"
      },
      {
        "depth": 2,
        "id": "当前限制",
        "title": "当前限制"
      },
      {
        "depth": 2,
        "id": "修改影响",
        "title": "修改影响"
      }
    ],
    "searchText": " 本文拥有 Slash Commands 的跨产品语义、运行时边界与当前接入状态。Rust 实现细节由 zeta-slash-commands README 拥有；通用斜杠启动面板 （Slash Launcher）的实现契约由 zeta-slash-launcher README 拥有；App Server wire snapshot 由 zeta-app-server-api.md 拥有。Slash Command 与 Instructions/Skills/Agents artifact 的关系由 agent-customizations.md 统一定义。 快速理解 Slash Command 是一种真正可调用的命令；斜杠启动面板只是用户输入 / 后出现的快速选择器。面板 可以展示命令、Skills 或其他产品列表，选中什么由列表来源解释，不能因为它们出现在同一个面板里 就都变成 Slash Command。 用户看到或产品要做的事 正确抽象 谁决定内容 --- --- --- 输入 / 后出现快速选择面板 Slash Launcher 产品选择并组合列表 TUI 展示可执行 /command Slash Command list zeta-code 的命令 adapter Desktop 展示可调用 Skills Skill list Desktop 的 Skill adapter zeterm 同时展示命令、Skills 或其他动作 多列表 Launcher zeterm 选择所需列表及顺序 选中一项后真正执行或注入上下文 来源自己的 typed binding 对应产品/领域 owner App Server 当前在 initialize.slashCommands 发布 server commands；每个 client 再与自身真正可执行的 local commands 合并。命令定义、名称冲突、补全和提交解析属于 Slash Commands；列表组合、跨来源 匹配和面板选择属于 Slash Launcher；行布局、DOM/WGPU/Ratatui 绘制和平台输入事件仍留在各 renderer。 Launcher 分层 zeta-slash-launcher 只接受产品构造的 SlashLauncherList，并返回稳定的 listid, itemid 选择。它不依赖 zeta-slash-commands 或 zeta-skills，因此产品可以自由选择： - TUI 只传 Slash Command list； - Desktop 只传 Skill list； - zeterm 或未来产品按需拼接多个列表； - 新列表通过产品 adapter 加入，不修改 Launcher 的领域模型。 选中项的业务 target、执行 handler、Skill 上下文注入和授权都留在列表来源。Launcher 不允许按展示 名称猜测业务对象，也不拥有 App Server protocol。 当前状态：通用 crate、列表组合、查询和选择状态已经实现；三种产品尚未迁移到该 crate。下面 表格描述的是迁移前的现有 Slash Command 接入，不能据此把 Skill projection 当作 Launcher 的长期 抽象。 现有 Surface Catalog 来源 Core/adapter Renderer owner --- --- --- --- TUI built-ins + initialize snapshot + enabled Skill metadata 直接使用 zeta-slash-commands；Skill target 保存在 client binding Ratatui popup Native zeta-ui local /model + initialize snapshot 直接使用 zeta-slash-commands；Native 另拥有 model picker WGPU composer interaction rows Desktop Chat Workbench actions + initialize snapshot + enabled Skill metadata canonical generated SlashCommandDefinition + separate action/Skill binding Aster completion widget；textarea/legacy editor runtime 可复用同一 catalog TUI 与 Desktop 已把用户可调用 Skill 以 /name 投影到同一补全菜单。启动与刷新只读取 Skill metadata；entry 绑定 exact pinned SkillRef，不会把 Skill 正文复制成 command definition。 所有权与执行 ``text App Server composition → initialize.slashCommands immutable server snapshot → client merges executable local commands → validated SlashCommandCatalog → input/query/matches/selection/completion → renderer projection → activation local → client product command server → unchanged /name text in session/request StartTurn.input skill → exact SkillRef + unchanged /name text in session/request StartTurn.input ` SlashCommandDefinition 是三端共享的 Slash Command catalog entry model，不是被调用对象的领域 model。origin、Workbench actionId、TUI dispatcher identity 和 Skill SkillRef 都是与 entry 分离的 client binding，不能包装成另一种 Slash Command。Skill authority 继续拥有 enablement、 compatibility 和 activation validation。 App Server 不执行 server-advertised Slash Command；它只声明 discoverability 与参数能力。Local command 必须存在真实 client execution path，否则不能进入 catalog。Desktop 的 /new、/history 属于 Workbench command mapping；Native 的 /model 属于 Session model selector；TUI 的 /theme 属于 device-local presentation preference：无参数时打开由 features/theme 拥有的固定 Zeta Code Theme Pane，带 ID 时静默直接切换；Theme Pane 不启用搜索，通用 Selection Pane 则以显式 Space search 进入搜索模式。其他 built-ins 属于 TUI coordination。任意 local/server 同名都拒绝 整份合并结果，不按客户端优先级静默覆盖。Skill 是动态投影：只有 enabled、compatible、名称唯一且 不与 local/server command 冲突的 entry 才进入 /name；冲突项保留在 /skills 管理与诊断目录， 不会覆盖已有命令或任意选择一个 source。 Config 边界 initialize.slashCommands 不进入通用 config，也不由 slash command view 读取 config。它是 connection 初始化时冻结的 server capability snapshot。Skill command 是 client 从 skills/list metadata 生成的 动态 projection；skills/changed 会使客户端重建这部分 catalog，不修改 server snapshot。 当前限制 Rust surfaces 直接共享 zeta-slash-commands 的 headless state。Desktop 直接消费同一个 generated SlashCommandDefinition model，并由 Aster Editor 的通用 completion/session state 投影交互；TypeScript 只保留运行时 catalog binding。Rust crate 与 Desktop adapter 共同执行 zeta-rs/slash-commands/fixtures/conformance.json，确保名称校验、大小写、前缀匹配和参数规则一致。 修改影响 新增 wire 字段先修改 zeta-app-server-protocol` 并重新生成 TypeScript/schema。修改名称、参数、匹配或 输入规则必须同时更新 crate tests、跨运行时 fixture、TUI/native adapter tests 和 Desktop tests。 纯视觉变化只修改对应 renderer，不得把 host-specific state 反推到 catalog 或 protocol。"
  },
  {
    "slug": "mcp",
    "sourcePath": "docs/mcp.md",
    "title": "MCP 集成系统",
    "description": "MCP 客户端把外部 Server 的工具转换成 Zeta 的工具目录；方向相反的 MCP Server 把 Zeta Agent 暴露给外部 Host。两条路径共享协议概念，但不共享运行时所有权。",
    "group": "Agent 与运行时",
    "markdown": "> Product runtime 当前实现：[`zeta-rs/mcp/`](../zeta-rs/mcp/README.md)，\n> Rust crate：`zeta_mcp`\n> Low-level client 当前实现：[`zeta-rs/rmcp-client/`](../zeta-rs/rmcp-client/README.md)，\n> Rust crate：`zeta_rmcp_client`\n> 当前状态：low-level client、tools-only product runtime、Config/Connector/Plugin hot composition、\n> `tools/list_changed` rebuild、App Server/Core tools vertical slice、Connector disconnect dispatch fence、\n> 独立 Config MCP OAuth 编排、form elicitation 与只读 lifecycle projection 已实现\n> Core architecture：[`core.md`](core.md)\n> Agent runtime：[`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md)\n> Tool shared contract 与纯转换：[`tools.md`](tools.md)\n> Config authority 与 runtime snapshot 接入：[`config.md`](config.md)\n> Plugin 分发边界：[`plugins.md`](plugins.md)\n> Connector account 与 ready binding：[`connectors.md`](connectors.md)\n> Skill 指令边界：[`skills.md`](skills.md)\n> 将 Zeta Agent 暴露为 MCP server：[`mcp-server.md`](mcp-server.md)\n\n> 官方规范核对日期：2026-07-25。第一版以 MCP `2025-11-25` protocol revision 为实现目标。\n> MCP 仍会演进；wire schema、授权流程和 experimental capability 必须以实现时的\n> [官方规范](https://modelcontextprotocol.io/specification/2025-11-25/architecture/index) 与\n> schema fixture 为准。\n\n## 快速理解\n\nMCP 客户端把外部 Server 的工具转换成 Zeta 的工具目录；方向相反的 MCP Server 把 Zeta Agent\n暴露给外部 Host。两条路径共享协议概念，但不共享运行时所有权。\n\n| 场景 | 使用的边界 | 当前状态 |\n| --- | --- | --- |\n| Zeta 连接外部 MCP Server | `zeta-rmcp-client` 建立单连接，`zeta-mcp` 管理多 Server 和工具目录 | 工具纵向切片已实现 |\n| 外部 Host 调用 Zeta Agent | 独立的 `zeta-mcp-server` 通过 App Server 启动和继续 Agent | 见 MCP Server 文档 |\n| MCP 暴露工具 | 转成带来源、绑定和失效 generation 的统一工具 | 已实现基础目录与调用路由 |\n| MCP 暴露资源或提示词 | 进入各自的上下文和产品契约 | 仍属计划设计 |\n| Server 需要 bearer 或 OAuth | 凭据保存在 SecretStore；具体 provider adapter 决定 discovery、scope 与 token wire | 独立 Config 与 Connector 路径均已具备窄实现 |\n| MCP 工具运行中请求表单输入 | 转成 Core durable 用户交互并唤醒同一次工具执行 | 基础类型已实现；URL、多选和跨重启恢复不支持 |\n| Plugin 独立声明 MCP Server | Plugin 只贡献声明，经激活和策略解析后由 MCP runtime 启动 | 安装不等于连接或授权 |\n| Plugin 声明需要外部账号的 Connector | Connector connected 后发布 ready MCP binding | Connector 不启动 MCP session |\n| 用户或 Workspace 直接配置 MCP Server | 配置经凭据、grant 和 policy 解析后直接进入 MCP runtime | 不必须先安装 Plugin 或创建 Connector |\n\n## 1. 结论\n\nMCP client 分成两个边界：Current `zeta-rmcp-client` 负责官方 RMCP SDK、单 server session、\ninitialize、原始 tools API 和 stdio/Streamable HTTP transport；Current `zeta-mcp` tools-only\nruntime 负责多 server 启动、provider-neutral tool catalog/binding、分页/大小限制、调用路由、\n取消与失效标记。Current App Server adapter 将 user config 与 ready Connector snapshot 接入 Core\n`ToolService`、逐次用户 approval 和 durable result，并在两类 generation 变化时重建 tool port。\nConnector API-token materialization、package-rooted Plugin MCP、通用 Connector OAuth PKCE 编排与 Desktop\nbrowser callback 已实现。独立 Config credential reference materialization、provider-injected OAuth\nPKCE/callback/refresh/revoke、process-local connect/disconnect/status，以及 MCP form elicitation 到 Core\ndurable interaction 的同调用恢复也已实现。通用自动 OAuth discovery、内建具体 provider、resources、\nprompts、reconnect/health 和更完整的 interaction surface 仍是 Proposed。\n\n方向相反的 `zeta-mcp-server` 通过 App Server 将 Zeta Agent 暴露给外部 MCP Host。两者不共享\nruntime ownership，也不互相依赖；具体边界见 [`mcp-server.md`](mcp-server.md)。\n\n它不是：\n\n- Plugin 的安装器、包管理器或信任根；\n- Skill 的发现器、选择器或指令加载器；\n- 绕过 Zeta tool approval、sandbox 和 durable Tool Call/Result 的执行捷径；\n- MCP server 输出、tool annotation 或 prompt 内容的信任背书；\n- Zeta Session、Thread、Turn 或 App Server connection 的替代品。\n\nPlugin、Connector 与 MCP 的 canonical 关系由 [`connectors.md`](connectors.md) 维护。从 MCP runtime 视角看，\n边界可压缩为：\n\n> Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Tool 是 Agent 最终消费的能力。\n\n```mermaid\nflowchart TD\n    U[\"User / Workspace MCP configuration\"] --> R[\"MCP runtime\"]\n    P[\"Standalone Plugin MCP contribution\"] --> R\n    C[\"Connected Connector\"] --> B[\"Ready MCP binding\"]\n    B --> R\n    R --> T[\"Tool Registry / Core / Agent\"]\n```\n\n这三条是并列的 declaration/readiness source，不是必须逐层包装的类型层次。Plugin 可以声明 MCP server，\n也可以携带 Skill 或 Connector；独立配置的 MCP server 和独立安装的 Skill 同样是一等来源。\n安装 Plugin 不等于允许其 MCP server 启动，Connector connected 不等于自动批准每次 Tool call。\n\n## 2. 当前仓库审计\n\n当前仓库已有低层 client、tools-only product runtime、配置面与启动时 tools vertical slice：\n\n- [`core.md`](core.md) 与\n  [`zeta-agent-runtime-architecture.md`](zeta-agent-runtime-architecture.md) 已将 MCP adapter\n  放在 Core tool port 之外，并要求 HTTP/MCP cancellation 贯穿工具执行；\n- `zeta-rmcp-client` 已用官方 RMCP SDK 实现单连接 initialize、tools list/call、\n  progress/list-changed/elicitation host callback、caller cancellation、request deadline、有界\n  shutdown，以及 local stdio 与 reqwest Streamable HTTP connector；其实现契约见\n  [`zeta-rs/rmcp-client/README.md`](../zeta-rs/rmcp-client/README.md)；\n- `zeta-mcp` 已实现多 server `RequireAll` / `AllowPartial` 启动、分页和 byte/tool limits、\n  deterministic alias、exact remote identity、connection/catalog generation、不可变\n  catalog/binding、list-changed stale 标记、可取消 tools/call、结果大小限制和有界 shutdown；\n  其实现契约见 [`zeta-rs/mcp/README.md`](../zeta-rs/mcp/README.md)；\n- `zeta-config` 与 App Server config operations 已有 MCP server declaration CRUD；配置存在不\n  等于 runtime 已启用；\n- `zeta-protocol` 已有 provider-independent `ToolDefinition`、`ToolCall`、`ToolResult`、\n  `ToolName` 和 durable Thread tool item；\n- `zeta-core` 已有 approval policy 基础，`zeta-tool-executor` / `zeta-sandboxing` 已形成独立\n  本地进程执行边界；产品层 `zeta-exec` 不参与单个 MCP tool process 的执行；\n- App Server 已把 enabled user declaration materialize 为 `McpServerDefinition`，通过持续运行\n  的 Tokio worker 桥接同步 Core `ToolService`，合并 local/MCP definitions，并为每次 MCP call\n  生成 exact `ActionSource::McpServer` review、durable one-time approval 与 unknown outcome；\n- `McpRuntimeOwner::prepare_call` 会把 exact `McpToolBinding` 与 JSON-object arguments 收敛为\n  一个 `McpPreparedCall`；worker 在进入 remote session 前仍以自己的 immutable catalog 复核\n  binding。App Server 的 generation-bound `BoundToolCall` 继续负责跨 prepare/execute 的保留，\n  因而 runtime replacement 不会把旧 Tool Call 重绑定到同名新工具；\n- `compose_mcp_tools_with_connectors` 已通过 host-injected `ConnectorMcpRuntimeProvider` 读取 ready\n  Connector credential，并用 exact connector ID / connection generation / definition digest 在 prepare\n  与 dispatch 前 fail closed；本地 reconcile loop 同时订阅 Config 与 Connector authority；\n- 直接 Config MCP 的 HTTPS bearer credential 已在连接启动前由 host SecretStore materialize；\n  `McpOAuthService` 已为 host 注入的 exact provider 提供 S256 PKCE、state、redirect/resource/target\n  复核、一次性 callback、私有 runtime/lifecycle credential envelope、refresh/revoke 和 App Server RPC；\n- MCP form elicitation 已通过 task-local exact Tool-call binding 转成 Core durable\n  `AgentRequest::UserInput`，解决或取消后唤醒同一次 live execution；URL elicitation、数组/多选、完整\n  JSON Schema format 校验和跨重启 remote request 恢复仍不支持；\n- 自动 reconnect/health state machine、resource/prompt product adapter 和 progress 产品投影仍未实现。\n\n因此 low-level protocol/transport、独立 tools-only runtime、窄 App Server/Core tools slice、\nprocess-local connect/disconnect/status、provider-injected 独立 OAuth lifecycle 和基础 form interaction\n是 Current；自动 provider discovery、完整 interaction surface 与其他 MCP capability 仍是 Proposed。\n\n## 3. 标准基线\n\nMCP 是 host-client-server 架构：host 为每个 server 创建隔离 client，一个 client 与一个 server\n维持一个 stateful session。初始化阶段先协商 protocol version 和 capabilities，之后才能进入正常\noperation。[官方架构](https://modelcontextprotocol.io/specification/2025-11-25/architecture/index)\n同时要求 host 控制权限、用户授权、模型集成和跨 server 隔离。\n\n支持矩阵明确区分低层代码与产品可用性：\n\n| MCP surface | Low-level client | Product runtime 策略 |\n| --- | --- | --- |\n| Base JSON-RPC lifecycle | Current | Current 多 session startup/shutdown；reconnect/health Proposed |\n| stdio | Current direct-local + injectable transport | Current absolute executable startup；sandboxed launcher Proposed |\n| Streamable HTTP | Current unauthenticated/bearer transport | Current unauthenticated/SecretStore bearer；provider-injected OAuth lifecycle Current；自动 discovery/reconnect Proposed |\n| Tools | Current 原始 list/call | Current catalog/binding/Core approval/durable result；Config/Connector hot rebuild Current |\n| Resources | 尚未暴露 | 首发只做 list/read，显式进入 context |\n| Prompts | 尚未暴露 | 首发只做 list/get，不当作 Skill |\n| Roots | 尚未暴露 | 只暴露已授权 workspace root，不能替代 OS sandbox |\n| Sampling | 尚未暴露 | 默认不声明；需独立预算、隐私和审批 |\n| Elicitation | Current host callback | Current form → durable Core interaction；URL/array/multiselect/跨重启 remote recovery 不支持 |\n| Tasks | 尚未暴露 | experimental，不等同 Zeta Turn/Task |\n\nMCP 标准 transport 是\n[stdio 与 Streamable HTTP](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports)。\nHTTP+SSE 是旧 revision 的兼容面，不进入 Zeta 第一版。\n\n## 4. 职责与非职责\n\n### 4.1 当前状态 `zeta-rmcp-client` 拥有\n\n- MCP wire request/response/notification codec 和 protocol revision negotiation；\n- 每个 configured server 的隔离 client session；\n- stdio 与 Streamable HTTP transport adapter；\n- initialize → operation → shutdown lifecycle；\n- request ID correlation、per-request deadline 和 cancel propagation；\n- 原始 tools list/call 与 progress、list-changed、elicitation host callback；\n- caller-provided RMCP transport 接入点。\n\n### 4.2 当前状态 `zeta-mcp` 拥有\n\n- runtime-ready server set、connection/catalog generation 与原子/部分启动策略；\n- tools 的分页 discovery、list-changed invalidation 和 immutable catalog；\n- MCP tool schema、content 和 error 到 `McpToolProjection` / source result 的转换与大小限制；\n- exact remote tool identity、catalog generation 和 host binding 所需的 source correlation；\n- startup/shutdown diagnostics 和 caller cancellation 传播。\n\nProposed 扩展仍包括 resources/prompts、reconnect/health state machine、roots、sampling、URL 或更复杂\nform elicitation，以及通用 OAuth metadata discovery 和 concrete provider adapter。\n\n### 4.3 两个 MCP 客户端 crate 都不拥有\n\n- Plugin package 下载、签名、解压、版本解析、enable/disable authority；\n- API token、OAuth refresh token、client secret 的持久化；\n- workspace、filesystem 或 network 的最终授权策略；\n- model selection、token budget、Core ContextAssembler 或 Agent loop；\n- tool approval、side-effect classification 的最终判定；\n- Thread event append、reducer、unknown-outcome recovery decision；\n- MCP server 自己的业务正确性、rate limit 或下游事务；\n- UI 中 MCP picker、OAuth browser window、confirmation dialog 的布局；\n- 把 server instructions、prompt、resource 或 tool result 提升为 system instruction。\n\n独立 MCP declaration 的 auth adapter 拥有对应 credential/OAuth lifecycle；Connector-bound MCP 的认证和账号\nlifecycle 则属于 Connector auth adapter。两者都把 opaque token persistence 委托给 `zeta-secrets`，并只向\nMCP runtime 提供 materialized credential/binding。`zeta-sandboxing` / host capability 实施资源边界，\nAgent runtime 决定何时调用工具，Core 负责 durable commit，App Server 是 composition root。\n\n## 5. 目标依赖与运行时结构\n\n```mermaid\nflowchart TD\n    P[\"zeta-plugins：validated MCP declarations\"] --> A[\"App Server composition\"]\n    C[\"zeta-connectors：ready runtime bindings\"] --> A\n    U[\"User / Workspace MCP configuration\"] --> A\n    H[\"Credential materializer + process/HTTP host adapters\"] --> A\n    A --> M[\"zeta-mcp：multi-server runtime\"]\n    M --> R[\"zeta-rmcp-client：protocol session / transport\"]\n    M --> Z[\"zeta-tools：provider-neutral definitions/results\"]\n    Z --> K[\"zeta-core ToolService\"]\n    K --> D[\"Session / Thread durable records\"]\n```\n\n规则：\n\n- `zeta-rmcp-client` 依赖官方 `rmcp` 和通用 async/HTTP 库，不依赖 Zeta product crate；\n- `zeta-mcp` 可以依赖 `zeta-rmcp-client`、`zeta-tools`、`zeta-protocol` 和通用 async/JSON 库；\n- `zeta-mcp` 不依赖 `zeta-core`、stores、App Server、Desktop、CLI 或 Plugin manager；\n- `zeta-mcp` 将 revision-specific wire descriptor 转成纯 `McpToolProjection`；\n  `zeta-tools` 再负责 schema normalization、model-facing definition 和 source-neutral output；\n- `zeta-core` 定义 consumer-owned `ToolService` port；App Server adapter 将 MCP tool\n  handle/catalog 适配到该 port，Core 不读取 MCP wire DTO；\n- Plugin manager 只产出经过验证的 server declaration，不持有 live MCP session；\n- Connector runtime 只产出 generation-bound ready binding，不持有 live MCP session 或 Tool registry；\n- App Server 注入 credential materializer、process launcher、HTTP transport、workspace roots 和\n  policy，不能让 `zeta-mcp` 自己扫描全局环境；\n- RMCP wire DTO 停在 `zeta-rmcp-client` / `zeta-mcp` adapter boundary，不进入\n  `zeta-protocol`、Core 或 App Server protocol。\n\n每个 live server 运行时：\n\n```text\nMcpServerDefinition\n    → policy/grant resolution\n    → transport connect\n    → initialize + capability negotiation\n    → immutable capability/catalog snapshot\n    → bounded request router\n    → graceful shutdown / reconnect\n```\n\n## 6. 身份、定义与快照\n\n### 6.1 Server 身份\n\n配置必须为每个 server 分配稳定 `McpServerId`。它标识 Zeta 配置中的逻辑 server，不使用 endpoint、\nchild PID、MCP session ID 或 Plugin 显示名称充当 identity。\n\n```rust\npub struct McpServerId(String);\n\npub struct McpServerDefinition {\n    pub id: McpServerId,\n    pub display_name: String,\n    pub transport: McpTransportDefinition,\n    pub credential: McpCredentialBinding,\n    pub policy: McpServerPolicy,\n}\n\npub enum McpTransportDefinition {\n    Stdio(StdioServerDefinition),\n    StreamableHttp(StreamableHttpServerDefinition),\n}\n\npub enum McpCredentialBinding {\n    Unauthenticated,\n    Reference(CredentialRef),\n}\n```\n\nPlugin 贡献的 logical ID 解析为带 Plugin namespace 的 `McpServerId`；用户配置与 workspace 配置\n使用各自 source namespace。不同来源不能通过同名静默覆盖。\n\n### 6.2 远程 primitive 身份\n\nMCP tool name 只保证在单个 server 内唯一；resource URI 和 prompt name 同样不能脱离 server\n解释。Zeta identity 必须保留二元组：\n\n```text\nMcpToolRef     = (McpServerId, exact remote tool name)\nMcpResourceRef = (McpServerId, exact resource URI)\nMcpPromptRef   = (McpServerId, exact prompt name)\n```\n\n不得 lowercase、重写或丢弃 exact remote identity。model-facing alias 只是一次 catalog snapshot\n内的路由名，不是远端 identity。\n\n### 6.3 工具 alias 与绑定\n\n当前 `zeta-protocol::ToolName` 允许 1–128 个 ASCII 字母、数字、`_`、`-`，而 MCP tool name\n还可能包含 `.`。不能把不兼容字符简单替换成 `_` 后假定没有冲突。\n\n目标使用显式绑定：\n\n```rust\npub struct McpToolBinding {\n    pub exposed_name: ToolName,\n    pub remote: McpToolRef,\n    pub schema_digest: ContentDigest,\n    pub catalog_generation: u64,\n}\n```\n\nalias 由可读 server/tool slug 加短 digest 生成，生成后做全 catalog collision check。Agent 发起\ntool call 时必须从冻结的 binding snapshot 解析，不得根据字符串重新猜 server。list-changed 后\n新的 binding 只在下一个 model safe point 生效；旧调用不能被静默路由到另一个工具。\n\n### 6.4 Immutable 快照\n\n```rust\npub struct McpCatalogSnapshot {\n    pub server: McpServerId,\n    pub connection_generation: u64,\n    pub catalog_generation: u64,\n    pub negotiated: NegotiatedMcpCapabilities,\n    pub tools: Vec<McpToolDescriptor>,\n    pub resources: Vec<McpResourceDescriptor>,\n    pub prompts: Vec<McpPromptDescriptor>,\n    pub freshness: McpCatalogFreshness,\n}\n```\n\n只有 consumer-visible catalog 变化才递增 `catalog_generation`。重连一定产生新的\n`connection_generation`，即使 catalog 内容相同；这样 late response、旧 subscription 和旧\nrequest ID 都不能污染新连接。\n\n## 7. 连接生命周期\n\n当前 runtime owner 发布 `Connected`、`Stale`、`Unavailable`；App Server 再结合 durable enablement 与\nprocess-local intent 投影 `Disabled`、`Disconnected`。这已经避免把“已配置但启动失败”压成\n`connected: bool`，但自动重连状态机尚未实现。\n\n完整目标状态仍是 Proposed：\n\n```text\nDisabled\n→ Starting\n→ Initializing\n→ Ready\n→ Degraded / Reconnecting\n→ Stopping\n→ Stopped\n```\n\n另有 terminal `Blocked`/`Misconfigured` 诊断状态。状态与 health、enablement 分开，避免\n“已启用但认证失败”被错误显示为未安装。\n\n[MCP lifecycle](https://modelcontextprotocol.io/specification/2025-11-25/basic/lifecycle) 要求：\n\n1. client 首先发送 `initialize`；\n2. 双方协商 protocol revision 与 capabilities；\n3. client 发送 `notifications/initialized` 后进入 operation；\n4. 不支持 server 返回 revision 时断开，而不是尝试按最新 DTO 继续；\n5. shutdown 停止接受新调用，取消或等待有界的 in-flight request，再关闭 transport。\n\n当前本地 policy：\n\n- initialize、list 和 call 使用分别配置的 deadline；\n- request ID 只在一个 connection generation 内相关；\n- cancellation 发送 MCP cancel notification，并停止本地等待；\n- cancel 不证明远端副作用没有发生；\n- server crash/EOF 使所有未完成请求进入 transport-lost 分类；\n- `tools/list_changed` 将 catalog 标为 stale，并触发 host 构造完整 replacement generation。\n\nProposed reconnect policy 使用 backoff + jitter，并受全局启动并发限制；成功后必须重新 initialize 和\ndiscovery，不能复用旧 server capability、request 或 binding。\n\n## 8. 传输\n\n### 8.1 stdio\n\nstdio supervisor 负责：\n\n- 通过已批准的、解析后的 executable path 启动 child；\n- 使用显式 argv，不通过 shell 拼接；\n- 设置最小 env allowlist 和显式 working directory；\n- 将 child 放入适用的 process/sandbox boundary；\n- stdout 只解码 MCP JSON-RPC，stderr 作为有界 diagnostic stream；\n- 限制单帧大小、stdout/stderr buffer、并发请求和 shutdown deadline；\n- 终止时处理完整 child process tree。\n\nPlugin manifest 中的 command/args/env 只是请求；真正启动前必须经过 install trust、runtime grant\n和 workspace policy。`PATH` 查找结果必须在 activation snapshot 中冻结，不能每次调用重新解析。\n\n### 8.2 Streamable HTTP\n\nHTTP adapter 必须实现：\n\n- 单一 MCP endpoint 的 POST/GET 与 JSON/SSE content negotiation；\n- `MCP-Protocol-Version`、可选 `MCP-Session-Id` 和 session termination；\n- SSE event ID / `Last-Event-ID` resume，但不把 event ID 当 Zeta durable sequence；\n- Origin、redirect、TLS、proxy 和 endpoint allowlist policy；\n- 401 challenge、OAuth metadata discovery 和 credential refresh；\n- response/body/SSE frame 大小及 idle/read deadline；\n- 同一 MCP message 不跨多个 SSE stream 重复交付。\n\n断开连接不等于 MCP request cancellation；client 必须显式 cancel。HTTP session ID 是远端 transport\nstate，不进入 Zeta Session/Thread。\n\n第一版不实现 custom transport。新增 transport 必须先定义 threat model、framing、cancellation、\nauthentication 和 shutdown contract，不能只提供 `send(Value)`。\n\n## 9. 服务端原语\n\n### 9.1 工具\n\n`zeta-mcp` 先将 revision-specific descriptor 转成保留 exact remote identity 的\n`McpToolProjection`，再由 [`tools.md`](tools.md) 的共享 adapter 产生 host tool definition。\n完整转换链必须：\n\n- 完整处理分页，并施加 tool count/page/bytes 上限；\n- 校验 name、input schema、可选 output schema 和 schema complexity；\n- 保留 exact remote descriptor 与 digest；\n- 将 annotation 标为 `UntrustedServerClaim`；\n- 用本地 policy 计算 approval/risk，而不是采信 server 的 read-only/destructive hint；\n- 对 model 不支持的 content type 给出明确降级或拒绝。\n\nMCP `2025-11-25` 的 embedded schema 默认使用 JSON Schema 2020-12；tool output 若声明\n`outputSchema`，Zeta 应校验 `structuredContent`。规范明确区分\n[protocol error 与 tool execution error](https://modelcontextprotocol.io/specification/2025-11-25/server/tools)：\n\n- unknown tool、malformed request、server protocol failure → MCP protocol error；\n- API failure、业务校验失败等 `isError: true` → `ToolOutputStatus::Error`，再由 Core 映射为\n  canonical error Tool Result。\n\n### 9.2 资源\n\nResource 是 application-controlled context，不是可自动执行的 tool。第一版：\n\n- 支持 `resources/list`、template list 和 `resources/read`；\n- 对每个 read 施加 scheme、MIME、byte/token 和 content-part 数量上限；\n- resource URI 始终与 `McpServerId` 绑定；\n- resource/list-changed 只使 catalog stale；\n- subscription 只为用户已选或 Core ContextAssembler 正在引用的 resource 建立；\n- update notification 触发重新读取，不直接信任 notification payload；\n- binary 内容通过 Resource/content store 传递，不内嵌到普通日志。\n\nServer 返回的 `file://` URI 不授予本地文件权限。只有 MCP `resources/read` 的返回内容可作为该\nserver resource；Zeta 不绕过 server 去读取同名本地路径。\n\n### 9.3 提示词\n\nMCP Prompt 是 user-controlled、带参数的远端模板：\n\n- 必须由用户显式选择或明确的产品 command 触发；\n- `prompts/get` 返回内容按外部、不可信 instruction 处理；\n- 不加入全局 developer/system instruction；\n- 不持久注册成 Skill，也不因名称相同覆盖本地 Skill；\n- prompt 中的 embedded resource 继续遵守 resource size、MIME 和 provenance policy。\n\n如果用户要将稳定 MCP Prompt 保存为 Skill，应通过显式导入/复制流程生成新的本地 artifact，\n记录来源与 digest；运行时不能把二者自动等同。\n\n## 10. 客户端功能\n\n### 10.1 根目录\n\nRoots 只告诉 server 当前 workspace 边界。它不是 sandbox，也不证明 server process 无法读取\nroot 之外的文件。\n\n- root 来自 host 授权后的 workspace snapshot；\n- 只发送 canonicalized `file://` URI；\n- path traversal、symlink policy 和 root 可达性由 host 校验；\n- workspace 切换后发送 list-changed，并使旧 root grant 失效；\n- local MCP process 仍必须使用 OS/process sandbox；\n- remote MCP server 只得到 root metadata，不自动得到文件内容。\n\n### 10.2 采样（Sampling）\n\nSampling 允许 MCP server 请求 host 调用模型，涉及费用、凭据、conversation disclosure 和递归\ntool loop。第一版不声明 sampling capability。\n\n后续启用必须通过独立 `McpSamplingPolicy`：\n\n- server 不能指定或读取 provider credential；\n- Core ContextAssembler 只提供请求所需最小 context；\n- 每 server/session 有 token、费用、并发和递归深度预算；\n- user approval 与 model choice 由 Zeta host 决定；\n- sampling 不能调用发起它的同一 tool 形成无界递归；\n- request/result 进入可审计 trace，但不伪装成用户 Turn。\n\n### 10.3 询问（Elicitation）\n\nElicitation 依赖 Zeta 的 typed Agent request/response delivery。App Server owner selection、deadline、\ndisconnect re-selection 和 durable recovery 已完成。当前 MCP adapter 只在 App Server 为 Tool call\n注入 `ToolInteractionService` 时声明 form capability；RMCP callback 再通过 task-local binding 关联 exact\nThread、Turn、request 与并发中的 MCP call，不能仅因共享 contract 存在就启用。\n\n当前支持 string、number、integer、boolean 和单选 enum/oneOf，保留 required、长度和数值范围；每个\n表单最多 32 个字段、100 个选项、每段 4096 个字符。数组、多选、未知 schema，以及名称或说明看似\n承载 password、secret、token、credential 或 API key 的字段会失败关闭。当前声明\n`schemaValidation=false`，因为尚未承诺完整 JSON Schema format 校验。URL elicitation 没有产品安全\n契约，返回 `Decline`。\n\n表单被持久化为 Core `AgentRequest::UserInput`。解决后 live waiter 唤醒同一次工具执行，App Server\n不会再启动第二个 backend resume；取消映射为 MCP `Cancel`。Zeta interaction 可以 durable，但 remote\nMCP connection/request 本身不能跨重启恢复；恢复后已 started 且无 terminal result 的调用保持\nunknown outcome，不能向新 connection 发送旧 response。\n\n### 10.4 实验性任务\n\nMCP task ID 只标识 MCP 协议中的异步 request 状态，不是 Zeta 产品 Session、Thread、Turn 或\nCodex 意义上的“任务”。在官方 capability 稳定且 Zeta 定义好 polling、expiry、authorization\nbinding、cancel 和 recovery 前不启用。\n\n## 11. 工具 loop、取消与 unknown 结果\n\n```text\nMCP catalog snapshot\n    → Agent model receives exposed ToolDefinition\n    → model emits Tool Call\n    → resolve frozen McpToolBinding\n    → validate arguments\n    → evaluate Zeta approval + policy\n    → durable commit Tool Call\n    → MCP tools/call outside Thread state owner\n    → validate result\n    → durable commit Tool Result / UnknownOutcome\n```\n\nMCP adapter 不直接写 Thread。取消和 retry 遵循 Agent Runtime 的统一工具语义：\n\n- 尚未发送：可安全取消；\n- 已发送但 server 明确返回 execution error：记录失败结果；\n- 连接丢失且工具可能有副作用：`UnknownOutcome`；\n- read-only 也只有本地 policy 明确认定且 retry strategy 允许时才自动重试；\n- server annotation 不能单独把调用升级为 `SafeRead`；\n- reconnect 后不得自动重放未完成 write；\n- late result 只能关联原 connection generation 和 ToolCallId，不能完成另一个调用。\n\n## 12. 安全与授权\n\n### 12.1 信任边界\n\n以下全部默认不可信：\n\n- server instructions；\n- tool name、description、annotations 和 schema；\n- prompt 和 resource 内容；\n- tool output、resource link 与 embedded resource；\n- Plugin 声明的 command、endpoint、env 和 permission request。\n\nMCP 内容可能包含 prompt injection。Zeta 必须带 provenance 传入 Core ContextAssembler，用边界标记其\n来源，且不能允许其覆盖 system/developer policy、approval 或 sandbox。\n\n### 12.2 HTTP 授权\n\n远程授权遵循\n[MCP authorization](https://modelcontextprotocol.io/specification/2025-11-25/basic/authorization)\n与 [security best practices](https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices)：\n\n- access/refresh token 的领域 lifecycle 属于 MCP auth，opaque bytes 只存于 `zeta-secrets`；\n- config、Plugin manifest、snapshot、日志和 Thread event 只保存 `CredentialRef`；\n- OAuth 使用 PKCE、state 和 exact redirect URI；\n- token 必须绑定目标 resource/audience；\n- 禁止把发给 MCP server 的 token 原样透传给下游服务；\n- redirect 和 authorization server metadata 必须经过 allowlist/consent；\n- localhost callback 使用随机 state、短期 listener 和一次性 completion；\n- logout/revoke 后立即停止依赖该 credential 的 session。\n\n当前独立 Config MCP 的实现把上述责任拆为两层。`McpOAuthService` 拥有 process-local flow、S256\nPKCE、state、exact redirect/resource/Config target 复核、一次性 callback 和 SecretStore envelope；\nhost 注入的 exact `McpOAuthProvider` 拥有 authorization-server discovery、endpoint allowlist、client\nidentity、scope、token response parsing、audience、refresh 和 remote revoke。Zeta 当前不提供通用自动\ndiscovery 或内建 concrete provider；未注入 provider 时 OAuth capability 明确 unavailable。\n\nSecretStore envelope 分离 runtime bearer 与 lifecycle secret。MCP transport 只能 materialize bearer，\nrefresh/revoke 只能取得 lifecycle 部分；旧 raw bearer 仍可读取以兼容已有配置。App Server callback 会\n重新读取当前 Config target，防止浏览器授权期间 endpoint 或 credential reference 被替换。revoke 在\n调用 provider 前先设置 disconnect intent，并等待 active Tool generation 移除该 server；remote\nrevoke 失败时本地 secret 不删除，但 runtime 保持断开，避免凭据仍被继续使用。\n\n### 12.3 数据外发\n\n每次 tool call 前，approval UI 应展示目标 server、tool、materialized arguments 和潜在数据范围。\nSecret field 按 schema/policy 脱敏展示，但实际发送仍需用户已授予的 credential/egress policy。\n\nResource、prompt、sampling 和 roots 也是数据出站面，不能只审计 `tools/call`。\n\n## 13. Config、App Server 与持久化\n\nConfig authority、Plugin contribution、MCP manager reconcile 与 Core safe point 的完整组合流程\n见 [`config.md`](config.md#6-pluginmcp-与-skill-接入流程)。\n\nMCP ordinary config 属于 config/plugin authority，不属于 Thread event：\n\n```text\nserver definition\nenabled scope\ntransport declaration\ncredential reference\nworkspace/root grant\nrequested runtime permissions\nlocal tool policy override\n```\n\nsecret、PID、request ID、OAuth verifier、SSE cursor 和 live session ID 不持久化为 ordinary config。\n\n当前 App Server 在启动安全点读取已启用的用户声明和 ready Connector snapshot，并由后台 reconcile\n同时订阅 Config 与 Connector generation。新 MCP runtime 完整启动后才原子发布到 future model safe\npoint；模型可见 definitions 和响应 binder 属于同一冻结 generation，旧 generation 由已绑定调用持有到\n排空。Connector list/API-token connect/disconnect 与 changed notification 已有 typed RPC；MCP\n`tools/list_changed` 会触发同一 reconcile 并只在完整启动成功后发布下一代。\n`mcp/server/connect`、`mcp/server/disconnect` 已作为 process-local intent 接入同一 reconcile loop；\n`mcp/server/status` 返回 active Config/Plugin/Connector runtime 的 redacted 连接状态、catalog/connection\ngeneration、tool count 和启动诊断。`mcp/oauth/start|complete|refresh|revoke` 已接入独立 Config MCP\nOAuth coordinator；complete/refresh 强制 reconcile 为 connect，revoke 先强制 disconnect。\n\n当前与计划中的 App Server 接口面为：\n\n| 类别 | 方法示例 | 语义 |\n| --- | --- | --- |\n| Config | `mcp/server/upsert`、`mcp/server/remove`、`mcp/server/enablement/set` | Current typed command 管理定义和 enablement；读取并入 `config/read` |\n| Runtime | `mcp/server/connect`、`mcp/server/disconnect` | process-local lifecycle intent，已接入 reconcile |\n| Catalog | `mcp/tool/list`、`mcp/resource/list`、`mcp/prompt/list` | Proposed 读取 snapshot |\n| Auth | `mcp/oauth/start`、`mcp/oauth/complete`、`mcp/oauth/refresh`、`mcp/oauth/revoke` | Current process-local one-shot OAuth flow 与 credential lifecycle |\n| Diagnostics | `mcp/server/status`、`mcp/server/log/read` | status 已实现；log/read 尚未实现 |\n\n命名只是目标语义，实施时必须与 `zeta-app-server-protocol` 同步生成 schema/TypeScript。配置修改\n继续使用 `CommandId` 和 exact typed payload replay；connect/disconnect 是 runtime intent，不占用\nSession/Thread sequence。\n\n## 14. 错误与可观测性\n\n错误至少分为：\n\n```text\nDefinitionError\nPermissionDenied\nCredentialUnavailable\nTransportStartFailed\nTransportLost\nInitializeRejected\nUnsupportedProtocolVersion\nCapabilityUnavailable\nCatalogInvalid\nSchemaInvalid\nRequestTimedOut\nRequestCancelled\nProtocolError\nToolExecutionError\nOutputRejected\nUnknownOutcome\n```\n\n不能把它们全部压成 `McpError(String)`。用户可见错误保持稳定 code 和安全 message；诊断可额外\n记录 server ID、connection generation、method、duration 和 redacted upstream code。\n\n永不记录：\n\n- Authorization/Cookie/header；\n- OAuth code/verifier/token；\n- 完整 tool arguments/result 默认值；\n- resource/prompt 正文；\n- 可能包含秘密的 endpoint query；\n- child process 完整 env。\n\nmetrics 可记录连接状态、初始化耗时、catalog generation、call latency、timeout/cancel/error count、\nqueue saturation 和 output rejection。\n\n## 15. 目标目录\n\n当前仅工具运行时保持单 crate、私有模块和显式公共导出：\n\n```text\nzeta-rs/mcp/src/\n├── lib.rs\n├── definition.rs\n├── error.rs\n├── session.rs\n├── catalog.rs\n├── output.rs\n├── runtime.rs\n└── *_tests.rs\n```\n\n不预建空模块。每个新增 trait 必须带 doc comment，说明实现方如何处理 cancellation、deadline、\nsecurity 和 identity。resources/prompts/auth/reconnect 落地时按独立 ownership 新增模块，不扩充\n现有 runtime 大文件；测试使用显式 sibling `#[path = \"..._tests.rs\"]`。\n\n## 16. 分阶段实施\n\n### 阶段 M0：低层契约与样例（工具范围完成）\n\n- ✅ RMCP SDK 固定 protocol lifecycle 与 capability negotiation；\n- ✅ 使用 fake/in-process transport 覆盖 initialize、tools 与 cancellation；\n- ✅ 验证 catalog page/tool/byte 和 output byte limits；\n- revision mismatch、oversized frame/schema bomb 与 late response fixture 尚待补充。\n\n完成条件：fake server 可覆盖正常、错误、late response、cancel 和 version mismatch。\n\n### 阶段 M1：stdio +工具纵向切片（窄启动时接线完成）\n\n- ✅ direct stdio connector 与 graceful shutdown；\n- ✅ tools/list pagination、catalog snapshot 和 list-changed stale 标记；\n- ✅ frozen tool binding、argument shape/output size validation 与 protocol cancellation；\n- ✅ 接入 Core Turn tool loop、逐次 approval、durable Tool Call/Result 和 UnknownOutcome；\n- Config/Connector hot rebuild、model-safe-point binding 与 old-generation drain 已接入；\n- ✅ `tools/list_changed` 进入 host reconcile，并在模型安全点替换 catalog；\n- ✅ 基础 form elicitation 进入 durable Core interaction，并恢复同一次 live Tool execution；\n- sandbox/process supervisor 仍未接入。\n\n完成条件：server crash、取消和 Thread recovery 不会静默重放有副作用调用。\n\n### 阶段 M2：资源、提示词与根目录\n\n- resource/prompt list/read/get；\n- context provenance、size/token budget 和 Resource store；\n- workspace root projection 与 list-changed；\n- Desktop/CLI/TUI 的只读 catalog 与显式选择入口。\n\n完成条件：任何 MCP 内容都不会在未选择时自动进入模型 context。\n\n### 阶段 M3：Streamable HTTP + OAuth（基础传输部分具备）\n\n- ✅ RMCP reqwest Streamable HTTP connector 支持 unauthenticated/bearer session；\n- ✅ Connector OAuth 通用 state/PKCE/exact redirect/one-shot exchange、App Server RPC 与 Desktop callback 编排；\n- ✅ 直接 Config HTTPS bearer credential 的 host SecretStore materialization；\n- ✅ 独立 Config MCP 的 provider-injected PKCE/start/complete/refresh/revoke、target revalidation 与\n  runtime/lifecycle secret isolation；\n- session resume/reconnect、protected resource metadata、自动 discovery、内建具体 provider，以及完整\n  endpoint/origin/redirect/egress policy 尚未具备。\n\n完成条件：token 不进入 config/log/event，断线不被误判为取消或成功。\n\n### 阶段 M4：可选客户端功能\n\n- ✅ App Server owner-directed interaction 与基础 MCP form elicitation；\n- URL、数组/多选 elicitation、sampling 和 tasks 仍需分别评审；\n- 每项独立 capability gate、budget、approval 和 recovery tests；\n- 未完成的 capability 不在 initialize 中声明。\n\n## 17. 验证门\n\n除 workspace 常规 `fmt/clippy/test` 外，MCP 必须覆盖：\n\n- revision negotiation 和 capability misuse；\n- stdio stdout 污染、oversized frame、stderr flood、child crash；\n- HTTP origin、redirect、session hijack、SSE duplicate/resume；\n- pagination loop、list-changed race 和 catalog generation；\n- tool alias collision、schema bomb、invalid structured output；\n- approval denial、cancel-before-send、cancel-after-send、late response；\n- disconnect 后 read retry 与 write unknown outcome；\n- root traversal/symlink、resource URI confusion 和 oversized content；\n- OAuth state/PKCE/audience/token redaction；\n- reconnect 时旧 request、subscription 和 binding 不污染新 generation；\n- shutdown deadline 与无 orphan child/process tree。\n\n实现必须使用 deterministic fake clock/transport/server；单元测试不能依赖公网 MCP server 或真实\nOAuth provider。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-当前仓库审计",
        "title": "2. 当前仓库审计"
      },
      {
        "depth": 2,
        "id": "3-标准基线",
        "title": "3. 标准基线"
      },
      {
        "depth": 2,
        "id": "4-职责与非职责",
        "title": "4. 职责与非职责"
      },
      {
        "depth": 3,
        "id": "41-当前状态-zeta-rmcp-client-拥有",
        "title": "4.1 当前状态 zeta-rmcp-client 拥有"
      },
      {
        "depth": 3,
        "id": "42-当前状态-zeta-mcp-拥有",
        "title": "4.2 当前状态 zeta-mcp 拥有"
      },
      {
        "depth": 3,
        "id": "43-两个-mcp-客户端-crate-都不拥有",
        "title": "4.3 两个 MCP 客户端 crate 都不拥有"
      },
      {
        "depth": 2,
        "id": "5-目标依赖与运行时结构",
        "title": "5. 目标依赖与运行时结构"
      },
      {
        "depth": 2,
        "id": "6-身份定义与快照",
        "title": "6. 身份、定义与快照"
      },
      {
        "depth": 3,
        "id": "61-server-身份",
        "title": "6.1 Server 身份"
      },
      {
        "depth": 3,
        "id": "62-远程-primitive-身份",
        "title": "6.2 远程 primitive 身份"
      },
      {
        "depth": 3,
        "id": "63-工具-alias-与绑定",
        "title": "6.3 工具 alias 与绑定"
      },
      {
        "depth": 3,
        "id": "64-immutable-快照",
        "title": "6.4 Immutable 快照"
      },
      {
        "depth": 2,
        "id": "7-连接生命周期",
        "title": "7. 连接生命周期"
      },
      {
        "depth": 2,
        "id": "8-传输",
        "title": "8. 传输"
      },
      {
        "depth": 3,
        "id": "81-stdio",
        "title": "8.1 stdio"
      },
      {
        "depth": 3,
        "id": "82-streamable-http",
        "title": "8.2 Streamable HTTP"
      },
      {
        "depth": 2,
        "id": "9-服务端原语",
        "title": "9. 服务端原语"
      },
      {
        "depth": 3,
        "id": "91-工具",
        "title": "9.1 工具"
      },
      {
        "depth": 3,
        "id": "92-资源",
        "title": "9.2 资源"
      },
      {
        "depth": 3,
        "id": "93-提示词",
        "title": "9.3 提示词"
      },
      {
        "depth": 2,
        "id": "10-客户端功能",
        "title": "10. 客户端功能"
      },
      {
        "depth": 3,
        "id": "101-根目录",
        "title": "10.1 根目录"
      },
      {
        "depth": 3,
        "id": "102-采样sampling",
        "title": "10.2 采样（Sampling）"
      },
      {
        "depth": 3,
        "id": "103-询问elicitation",
        "title": "10.3 询问（Elicitation）"
      },
      {
        "depth": 3,
        "id": "104-实验性任务",
        "title": "10.4 实验性任务"
      },
      {
        "depth": 2,
        "id": "11-工具-loop取消与-unknown-结果",
        "title": "11. 工具 loop、取消与 unknown 结果"
      },
      {
        "depth": 2,
        "id": "12-安全与授权",
        "title": "12. 安全与授权"
      },
      {
        "depth": 3,
        "id": "121-信任边界",
        "title": "12.1 信任边界"
      },
      {
        "depth": 3,
        "id": "122-http-授权",
        "title": "12.2 HTTP 授权"
      },
      {
        "depth": 3,
        "id": "123-数据外发",
        "title": "12.3 数据外发"
      },
      {
        "depth": 2,
        "id": "13-configapp-server-与持久化",
        "title": "13. Config、App Server 与持久化"
      },
      {
        "depth": 2,
        "id": "14-错误与可观测性",
        "title": "14. 错误与可观测性"
      },
      {
        "depth": 2,
        "id": "15-目标目录",
        "title": "15. 目标目录"
      },
      {
        "depth": 2,
        "id": "16-分阶段实施",
        "title": "16. 分阶段实施"
      },
      {
        "depth": 3,
        "id": "阶段-m0低层契约与样例工具范围完成",
        "title": "阶段 M0：低层契约与样例（工具范围完成）"
      },
      {
        "depth": 3,
        "id": "阶段-m1stdio-工具纵向切片窄启动时接线完成",
        "title": "阶段 M1：stdio +工具纵向切片（窄启动时接线完成）"
      },
      {
        "depth": 3,
        "id": "阶段-m2资源提示词与根目录",
        "title": "阶段 M2：资源、提示词与根目录"
      },
      {
        "depth": 3,
        "id": "阶段-m3streamable-http-oauth基础传输部分具备",
        "title": "阶段 M3：Streamable HTTP + OAuth（基础传输部分具备）"
      },
      {
        "depth": 3,
        "id": "阶段-m4可选客户端功能",
        "title": "阶段 M4：可选客户端功能"
      },
      {
        "depth": 2,
        "id": "17-验证门",
        "title": "17. 验证门"
      }
    ],
    "searchText": " Product runtime 当前实现：zeta-rs/mcp/， Rust crate：zetamcp Low-level client 当前实现：zeta-rs/rmcp-client/， Rust crate：zetarmcpclient 当前状态：low-level client、tools-only product runtime、Config/Connector/Plugin hot composition、 tools/listchanged rebuild、App Server/Core tools vertical slice、Connector disconnect dispatch fence、 独立 Config MCP OAuth 编排、form elicitation 与只读 lifecycle projection 已实现 Core architecture：core.md Agent runtime：zeta-agent-runtime-architecture.md Tool shared contract 与纯转换：tools.md Config authority 与 runtime snapshot 接入：config.md Plugin 分发边界：plugins.md Connector account 与 ready binding：connectors.md Skill 指令边界：skills.md 将 Zeta Agent 暴露为 MCP server：mcp-server.md 官方规范核对日期：2026-07-25。第一版以 MCP 2025-11-25 protocol revision 为实现目标。 MCP 仍会演进；wire schema、授权流程和 experimental capability 必须以实现时的 官方规范 与 schema fixture 为准。 快速理解 MCP 客户端把外部 Server 的工具转换成 Zeta 的工具目录；方向相反的 MCP Server 把 Zeta Agent 暴露给外部 Host。两条路径共享协议概念，但不共享运行时所有权。 场景 使用的边界 当前状态 --- --- --- Zeta 连接外部 MCP Server zeta-rmcp-client 建立单连接，zeta-mcp 管理多 Server 和工具目录 工具纵向切片已实现 外部 Host 调用 Zeta Agent 独立的 zeta-mcp-server 通过 App Server 启动和继续 Agent 见 MCP Server 文档 MCP 暴露工具 转成带来源、绑定和失效 generation 的统一工具 已实现基础目录与调用路由 MCP 暴露资源或提示词 进入各自的上下文和产品契约 仍属计划设计 Server 需要 bearer 或 OAuth 凭据保存在 SecretStore；具体 provider adapter 决定 discovery、scope 与 token wire 独立 Config 与 Connector 路径均已具备窄实现 MCP 工具运行中请求表单输入 转成 Core durable 用户交互并唤醒同一次工具执行 基础类型已实现；URL、多选和跨重启恢复不支持 Plugin 独立声明 MCP Server Plugin 只贡献声明，经激活和策略解析后由 MCP runtime 启动 安装不等于连接或授权 Plugin 声明需要外部账号的 Connector Connector connected 后发布 ready MCP binding Connector 不启动 MCP session 用户或 Workspace 直接配置 MCP Server 配置经凭据、grant 和 policy 解析后直接进入 MCP runtime 不必须先安装 Plugin 或创建 Connector 1. 结论 MCP client 分成两个边界：Current zeta-rmcp-client 负责官方 RMCP SDK、单 server session、 initialize、原始 tools API 和 stdio/Streamable HTTP transport；Current zeta-mcp tools-only runtime 负责多 server 启动、provider-neutral tool catalog/binding、分页/大小限制、调用路由、 取消与失效标记。Current App Server adapter 将 user config 与 ready Connector snapshot 接入 Core ToolService、逐次用户 approval 和 durable result，并在两类 generation 变化时重建 tool port。 Connector API-token materialization、package-rooted Plugin MCP、通用 Connector OAuth PKCE 编排与 Desktop browser callback 已实现。独立 Config credential reference materialization、provider-injected OAuth PKCE/callback/refresh/revoke、process-local connect/disconnect/status，以及 MCP form elicitation 到 Core durable interaction 的同调用恢复也已实现。通用自动 OAuth discovery、内建具体 provider、resources、 prompts、reconnect/health 和更完整的 interaction surface 仍是 Proposed。 方向相反的 zeta-mcp-server 通过 App Server 将 Zeta Agent 暴露给外部 MCP Host。两者不共享 runtime ownership，也不互相依赖；具体边界见 mcp-server.md。 它不是： - Plugin 的安装器、包管理器或信任根； - Skill 的发现器、选择器或指令加载器； - 绕过 Zeta tool approval、sandbox 和 durable Tool Call/Result 的执行捷径； - MCP server 输出、tool annotation 或 prompt 内容的信任背书； - Zeta Session、Thread、Turn 或 App Server connection 的替代品。 Plugin、Connector 与 MCP 的 canonical 关系由 connectors.md 维护。从 MCP runtime 视角看， 边界可压缩为： Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Tool 是 Agent 最终消费的能力。 ``mermaid flowchart TD U \"User / Workspace MCP configuration\" -- R \"MCP runtime\" P \"Standalone Plugin MCP contribution\" -- R C \"Connected Connector\" -- B \"Ready MCP binding\" B -- R R -- T \"Tool Registry / Core / Agent\" ` 这三条是并列的 declaration/readiness source，不是必须逐层包装的类型层次。Plugin 可以声明 MCP server， 也可以携带 Skill 或 Connector；独立配置的 MCP server 和独立安装的 Skill 同样是一等来源。 安装 Plugin 不等于允许其 MCP server 启动，Connector connected 不等于自动批准每次 Tool call。 2. 当前仓库审计 当前仓库已有低层 client、tools-only product runtime、配置面与启动时 tools vertical slice： - core.md 与 zeta-agent-runtime-architecture.md 已将 MCP adapter 放在 Core tool port 之外，并要求 HTTP/MCP cancellation 贯穿工具执行； - zeta-rmcp-client 已用官方 RMCP SDK 实现单连接 initialize、tools list/call、 progress/list-changed/elicitation host callback、caller cancellation、request deadline、有界 shutdown，以及 local stdio 与 reqwest Streamable HTTP connector；其实现契约见 zeta-rs/rmcp-client/README.md； - zeta-mcp 已实现多 server RequireAll / AllowPartial 启动、分页和 byte/tool limits、 deterministic alias、exact remote identity、connection/catalog generation、不可变 catalog/binding、list-changed stale 标记、可取消 tools/call、结果大小限制和有界 shutdown； 其实现契约见 zeta-rs/mcp/README.md； - zeta-config 与 App Server config operations 已有 MCP server declaration CRUD；配置存在不 等于 runtime 已启用； - zeta-protocol 已有 provider-independent ToolDefinition、ToolCall、ToolResult、 ToolName 和 durable Thread tool item； - zeta-core 已有 approval policy 基础，zeta-tool-executor / zeta-sandboxing 已形成独立 本地进程执行边界；产品层 zeta-exec 不参与单个 MCP tool process 的执行； - App Server 已把 enabled user declaration materialize 为 McpServerDefinition，通过持续运行 的 Tokio worker 桥接同步 Core ToolService，合并 local/MCP definitions，并为每次 MCP call 生成 exact ActionSource::McpServer review、durable one-time approval 与 unknown outcome； - McpRuntimeOwner::preparecall 会把 exact McpToolBinding 与 JSON-object arguments 收敛为 一个 McpPreparedCall；worker 在进入 remote session 前仍以自己的 immutable catalog 复核 binding。App Server 的 generation-bound BoundToolCall 继续负责跨 prepare/execute 的保留， 因而 runtime replacement 不会把旧 Tool Call 重绑定到同名新工具； - composemcptoolswithconnectors 已通过 host-injected ConnectorMcpRuntimeProvider 读取 ready Connector credential，并用 exact connector ID / connection generation / definition digest 在 prepare 与 dispatch 前 fail closed；本地 reconcile loop 同时订阅 Config 与 Connector authority； - 直接 Config MCP 的 HTTPS bearer credential 已在连接启动前由 host SecretStore materialize； McpOAuthService 已为 host 注入的 exact provider 提供 S256 PKCE、state、redirect/resource/target 复核、一次性 callback、私有 runtime/lifecycle credential envelope、refresh/revoke 和 App Server RPC； - MCP form elicitation 已通过 task-local exact Tool-call binding 转成 Core durable AgentRequest::UserInput，解决或取消后唤醒同一次 live execution；URL elicitation、数组/多选、完整 JSON Schema format 校验和跨重启 remote request 恢复仍不支持； - 自动 reconnect/health state machine、resource/prompt product adapter 和 progress 产品投影仍未实现。 因此 low-level protocol/transport、独立 tools-only runtime、窄 App Server/Core tools slice、 process-local connect/disconnect/status、provider-injected 独立 OAuth lifecycle 和基础 form interaction 是 Current；自动 provider discovery、完整 interaction surface 与其他 MCP capability 仍是 Proposed。 3. 标准基线 MCP 是 host-client-server 架构：host 为每个 server 创建隔离 client，一个 client 与一个 server 维持一个 stateful session。初始化阶段先协商 protocol version 和 capabilities，之后才能进入正常 operation。官方架构 同时要求 host 控制权限、用户授权、模型集成和跨 server 隔离。 支持矩阵明确区分低层代码与产品可用性： MCP surface Low-level client Product runtime 策略 --- --- --- Base JSON-RPC lifecycle Current Current 多 session startup/shutdown；reconnect/health Proposed stdio Current direct-local + injectable transport Current absolute executable startup；sandboxed launcher Proposed Streamable HTTP Current unauthenticated/bearer transport Current unauthenticated/SecretStore bearer；provider-injected OAuth lifecycle Current；自动 discovery/reconnect Proposed Tools Current 原始 list/call Current catalog/binding/Core approval/durable result；Config/Connector hot rebuild Current Resources 尚未暴露 首发只做 list/read，显式进入 context Prompts 尚未暴露 首发只做 list/get，不当作 Skill Roots 尚未暴露 只暴露已授权 workspace root，不能替代 OS sandbox Sampling 尚未暴露 默认不声明；需独立预算、隐私和审批 Elicitation Current host callback Current form → durable Core interaction；URL/array/multiselect/跨重启 remote recovery 不支持 Tasks 尚未暴露 experimental，不等同 Zeta Turn/Task MCP 标准 transport 是 stdio 与 Streamable HTTP。 HTTP+SSE 是旧 revision 的兼容面，不进入 Zeta 第一版。 4. 职责与非职责 4.1 当前状态 zeta-rmcp-client 拥有 - MCP wire request/response/notification codec 和 protocol revision negotiation； - 每个 configured server 的隔离 client session； - stdio 与 Streamable HTTP transport adapter； - initialize → operation → shutdown lifecycle； - request ID correlation、per-request deadline 和 cancel propagation； - 原始 tools list/call 与 progress、list-changed、elicitation host callback； - caller-provided RMCP transport 接入点。 4.2 当前状态 zeta-mcp 拥有 - runtime-ready server set、connection/catalog generation 与原子/部分启动策略； - tools 的分页 discovery、list-changed invalidation 和 immutable catalog； - MCP tool schema、content 和 error 到 McpToolProjection / source result 的转换与大小限制； - exact remote tool identity、catalog generation 和 host binding 所需的 source correlation； - startup/shutdown diagnostics 和 caller cancellation 传播。 Proposed 扩展仍包括 resources/prompts、reconnect/health state machine、roots、sampling、URL 或更复杂 form elicitation，以及通用 OAuth metadata discovery 和 concrete provider adapter。 4.3 两个 MCP 客户端 crate 都不拥有 - Plugin package 下载、签名、解压、版本解析、enable/disable authority； - API token、OAuth refresh token、client secret 的持久化； - workspace、filesystem 或 network 的最终授权策略； - model selection、token budget、Core ContextAssembler 或 Agent loop； - tool approval、side-effect classification 的最终判定； - Thread event append、reducer、unknown-outcome recovery decision； - MCP server 自己的业务正确性、rate limit 或下游事务； - UI 中 MCP picker、OAuth browser window、confirmation dialog 的布局； - 把 server instructions、prompt、resource 或 tool result 提升为 system instruction。 独立 MCP declaration 的 auth adapter 拥有对应 credential/OAuth lifecycle；Connector-bound MCP 的认证和账号 lifecycle 则属于 Connector auth adapter。两者都把 opaque token persistence 委托给 zeta-secrets，并只向 MCP runtime 提供 materialized credential/binding。zeta-sandboxing / host capability 实施资源边界， Agent runtime 决定何时调用工具，Core 负责 durable commit，App Server 是 composition root。 5. 目标依赖与运行时结构 `mermaid flowchart TD P \"zeta-plugins：validated MCP declarations\" -- A \"App Server composition\" C \"zeta-connectors：ready runtime bindings\" -- A U \"User / Workspace MCP configuration\" -- A H \"Credential materializer + process/HTTP host adapters\" -- A A -- M \"zeta-mcp：multi-server runtime\" M -- R \"zeta-rmcp-client：protocol session / transport\" M -- Z \"zeta-tools：provider-neutral definitions/results\" Z -- K \"zeta-core ToolService\" K -- D \"Session / Thread durable records\" ` 规则： - zeta-rmcp-client 依赖官方 rmcp 和通用 async/HTTP 库，不依赖 Zeta product crate； - zeta-mcp 可以依赖 zeta-rmcp-client、zeta-tools、zeta-protocol 和通用 async/JSON 库； - zeta-mcp 不依赖 zeta-core、stores、App Server、Desktop、CLI 或 Plugin manager； - zeta-mcp 将 revision-specific wire descriptor 转成纯 McpToolProjection； zeta-tools 再负责 schema normalization、model-facing definition 和 source-neutral output； - zeta-core 定义 consumer-owned ToolService port；App Server adapter 将 MCP tool handle/catalog 适配到该 port，Core 不读取 MCP wire DTO； - Plugin manager 只产出经过验证的 server declaration，不持有 live MCP session； - Connector runtime 只产出 generation-bound ready binding，不持有 live MCP session 或 Tool registry； - App Server 注入 credential materializer、process launcher、HTTP transport、workspace roots 和 policy，不能让 zeta-mcp 自己扫描全局环境； - RMCP wire DTO 停在 zeta-rmcp-client / zeta-mcp adapter boundary，不进入 zeta-protocol、Core 或 App Server protocol。 每个 live server 运行时： `text McpServerDefinition → policy/grant resolution → transport connect → initialize + capability negotiation → immutable capability/catalog snapshot → bounded request router → graceful shutdown / reconnect ` 6. 身份、定义与快照 6.1 Server 身份 配置必须为每个 server 分配稳定 McpServerId。它标识 Zeta 配置中的逻辑 server，不使用 endpoint、 child PID、MCP session ID 或 Plugin 显示名称充当 identity。 `rust pub struct McpServerId String ; pub struct McpServerDefinition pub id: McpServerId, pub displayname: String, pub transport: McpTransportDefinition, pub credential: McpCredentialBinding, pub policy: McpServerPolicy, pub enum McpTransportDefinition Stdio StdioServerDefinition , StreamableHttp StreamableHttpServerDefinition , pub enum McpCredentialBinding Unauthenticated, Reference CredentialRef , ` Plugin 贡献的 logical ID 解析为带 Plugin namespace 的 McpServerId；用户配置与 workspace 配置 使用各自 source namespace。不同来源不能通过同名静默覆盖。 6.2 远程 primitive 身份 MCP tool name 只保证在单个 server 内唯一；resource URI 和 prompt name 同样不能脱离 server 解释。Zeta identity 必须保留二元组： `text McpToolRef = McpServerId, exact remote tool name McpResourceRef = McpServerId, exact resource URI McpPromptRef = McpServerId, exact prompt name ` 不得 lowercase、重写或丢弃 exact remote identity。model-facing alias 只是一次 catalog snapshot 内的路由名，不是远端 identity。 6.3 工具 alias 与绑定 当前 zeta-protocol::ToolName 允许 1–128 个 ASCII 字母、数字、、-，而 MCP tool name 还可能包含 .。不能把不兼容字符简单替换成 后假定没有冲突。 目标使用显式绑定： `rust pub struct McpToolBinding pub exposedname: ToolName, pub remote: McpT"
  },
  {
    "slug": "mcp-server",
    "sourcePath": "docs/mcp-server.md",
    "title": "zeta-mcp-server：将 Zeta Agent 暴露为 MCP 能力",
    "description": "本文是“外部 MCP Host 如何调用 Zeta Agent”的 canonical 系统文档。MCP client session、 外部 capability catalog 和 tool/resource/prompt adapter 由 mcp.md 拥有；本地 parent/child delegation、context inheritance、join、b…",
    "group": "Agent 与运行时",
    "markdown": "> 物理位置：`zeta-rs/mcp-server/`  \n> Rust crate：`zeta_mcp_server`，binary：`zeta-mcp-server`  \n> 当前状态：stdio 与 authenticated Streamable HTTP、bounded progress、form interaction 和\n> durable invocation recovery 已实现；独立 App Server event driver、SSE redelivery、\n> multi-tenant control plane 与 remote Agent bridge 仍为 Proposed  \n> Crate implementation contract：[`zeta-rs/mcp-server/README.md`](../zeta-rs/mcp-server/README.md)  \n> MCP client runtime：[`mcp.md`](mcp.md)  \n> App Server wire contract：[`zeta-app-server-api.md`](zeta-app-server-api.md)  \n> App Server client lifecycle：[`app-server-client.md`](app-server-client.md)  \n> 内建多 Agent：[`core-multi-agent.md`](core-multi-agent.md)\n\n本文是“外部 MCP Host 如何调用 Zeta Agent”的 canonical 系统文档。MCP client session、\n外部 capability catalog 和 tool/resource/prompt adapter 由 [`mcp.md`](mcp.md) 拥有；本地\nparent/child delegation、context inheritance、join、budget 和 recovery 由\n[`core-multi-agent.md`](core-multi-agent.md) 拥有。\n\n## 快速理解\n\nZeta 应提供独立的 MCP server adapter，使 Codex、其他 Agent host 或另一个 Zeta 实例能够把\nZeta Agent 作为外部能力调用。它通过 App Server 的 canonical Session/Thread/Turn API 启动和\n继续任务，不直接组装 `zeta-core`、model provider、Tool runtime、policy 或 stores。\n\n```text\nExternal MCP Host\n        │ MCP over stdio / Streamable HTTP\n        ▼\nzeta-mcp-server\n        │ typed App Server client\n        ▼\nApp Server composition root\n        │\n        ▼\nSession / Thread / TurnExecutor\n```\n\n同进程 Zeta Agent 创建 child Agent 时不经过 MCP server。该路径仍由\n`MultiAgentCoordinator` 直接建立 durable delegation：\n\n```text\nlocal parent Agent ──► MultiAgentCoordinator ──► child Thread\n```\n\n另一个进程或机器上的 Zeta 可以通过 MCP 被调用；若调用方希望把它视为正式 child Agent，\n调用方必须在本地先建立 delegation，再由 remote Agent adapter 将执行映射到 MCP。MCP\ntransport 本身不创造 parent/child 语义。\n\n| 调用者想做什么 | 应使用的入口 | 是否建立 Zeta 内部父子关系 |\n| --- | --- | --- |\n| 外部 MCP Host 调用 Zeta | MCP Server | ❌ |\n| 本地 Agent 创建子 Agent | 多 Agent 协调系统 | ✅ |\n| 另一台机器上的 Zeta 执行任务 | MCP 或未来远程适配器 | 默认不建立 |\n| 把远端 Zeta 纳入本地 Agent 树 | 先建立本地委托，再映射远端执行 | ✅ |\n\n## 2. 产品场景与边界\n\n| 场景 | 首选入口 | 语义 |\n| --- | --- | --- |\n| Zeta Desktop、CLI 或 TUI 控制本地 Zeta | App Server | 完整产品控制面 |\n| 本地 Agent 创建 child Agent | `MultiAgentCoordinator` | 原生 delegation 和 Agent tree |\n| Codex、Claude 或其他 MCP Host 调用 Zeta | `zeta-mcp-server` | Zeta 作为外部 Agent tool |\n| Zeta A 调用独立的 Zeta B | `zeta-mcp` → `zeta-mcp-server` | 跨 runtime Agent 调用 |\n| Zeta A 将 Zeta B 纳入自己的 Agent tree | remote Agent adapter + MCP | 本地 delegation，远端 execution |\n\n`zeta-mcp-server` 不是：\n\n- `zeta-mcp` 的服务端模块；两者方向和依赖独立；\n- 内建多 Agent 的替代品；\n- App Server 的第二套 Session/Thread authority；\n- scheduler、worker lease 或跨机器 execution protocol；\n- 允许调用方注入任意 system/developer instruction、raw config 或未授权 workspace 的后门；\n- 将 MCP request ID 当作 durable Zeta identity 的兼容层。\n\n## 3. 当前仓库状态\n\n当前已实现：\n\n- `zeta-mcp-server` crate、独立 binary 和 `zeta mcp-server` CLI 入口；\n- MCP `2025-11-25` stdio 与 Streamable HTTP initialize、ping、tools/list、tools/call、\n  `notifications/cancelled`、progress 和 form elicitation；\n- `zeta` 创建 Session/root Thread/Turn，`zeta-reply` 继续同一 principal 获得授权的 Thread；\n- typed App Server client 映射、Thread subscription、bounded polling/notification drain、\n  有界 progress 和 final result；\n- caller-generated invocation identity、进程内 single-flight、跨进程重启 exact replay、\n  payload conflict 和 principal-scoped Thread binding；\n- approval/user-input 到 MCP `elicitation/create` 的映射，以及 exact typed interaction resolve；\n- bearer authentication、Origin 校验、MCP session/protocol header、DELETE termination、\n  SSE Tool call response 和连接数限制；\n- deadline/EOF/client cancellation 到精确 `session/request` `InterruptTurn` 的传播、unknown-outcome grace，\n  以及 client-cancelled request response suppression；\n- protocol、真实 in-process App Server、binary stdio restart 和 HTTP socket/SSE tests；\n- App Server 已提供 `session/create`、`session/request`、带 Session scope 的 Thread subscription 和\n  canonical Session/Thread updates；\n- `zeta-app-server-client` 已有 typed methods、schema hash 校验和 embedded\n  `InProcessTransport`；\n- `open_local_app_server` 是本地 composition root，`InProcessAppServer` 允许多个 MCP HTTP\n  session 连接同一个 embedded host；\n- Session、Thread、Turn 和 Item 已由 store/reducer 持久化。\n\n当前缺口：\n\n- MCP adapter 当前仍通过 `InProcessTransport` 的同步 `round_trip` 与 request 后\n  `drain_notifications` 工作，以 Thread subscription + bounded polling/drain 转发进度与\n  interaction；\n- 通用 `AppServerSession`、可克隆 request handle、独立 event stream 和显式 shutdown 已由\n  `zeta-app-server-client` 提供并被 CLI/TUI 使用，MCP adapter 尚未迁移到该 event driver；\n- `MultiAgentCoordinator` 和 remote Agent adapter 仍是 Proposed；\n- dynamic Tool interaction、artifact reference 和命名 execution profile 尚未完成；\n- HTTP 尚无 independent GET SSE、`Last-Event-ID` redelivery、OAuth、multi-tenant workspace\n  binding、built-in TLS 或 remote App Server backend；\n- receipt rows 已进入 profile SQLite，但 single-flight active set 仍为进程内；同一 profile 当前\n  只能运行一个 MCP server process。\n\n本文以下章节同时固定 Current surface 与 Proposed 演进；当前 HTTP listener 是 remote MCP\nadapter，不等于已具备公网 multi-tenant service 或 remote App Server execution plane。\n\n## 4. 所有权\n\n### 4.1 `zeta-mcp-server` 拥有\n\n- MCP server initialize、capability advertisement、stdio 与 HTTP session lifecycle；\n- MCP tool definition、input/output schema 和 server-side validation；\n- MCP request 与 App Server request/Turn 的 correlation；\n- MCP cancellation 到精确 App Server operation 的传播；\n- Thread update 到有界 progress/final result 的转换；\n- approval/user-input 与 MCP form elicitation 的双向映射；\n- principal-scoped invocation receipt、replay 与 caller-to-Thread binding；\n- HTTP bearer、Origin、session/protocol validation 和 connection limit；\n- caller-visible `session_id`、`thread_id` 和 `turn_id` 的安全投影；\n- connection-scoped pending call、backpressure、deadline 和 bounded diagnostics；\n- MCP wire error 与 App Server/domain error 的稳定映射；\n- Proposed：artifact reference、MCP task capability、SSE resume、OAuth/multi-tenant control\n  plane、remote App Server backend 和 remote Agent bridge。\n\n### 4.2 App Server 与核心保持拥有\n\n| 能力 | Canonical owner |\n| --- | --- |\n| Session/Thread/Turn 创建和状态迁移 | App Server → Core |\n| transcript、Item 和 durable sequence | Thread store/reducer |\n| model、Tool、policy、approval 和 sandbox | Core ports + composition root |\n| Config、credential 和 workspace resolution | 对应 authority/runtime |\n| 本地 child delegation、join、budget 和 cancellation tree | `MultiAgentCoordinator` |\n| model/tool execution | `TurnExecutor` / `ToolScheduler` |\n| Desktop/TUI interaction owner selection | App Server |\n\n`zeta-mcp-server` 不直接依赖 store，不写 Thread event，不持有第二份 transcript，也不根据 MCP\nconnection 状态推断 durable Turn 已成功或失败。\n\n## 5. 依赖方向与部署形态\n\n目标依赖：\n\n```text\nzeta-mcp-server\n        │\n        ▼\nzeta-app-server-client ──► zeta-app-server-protocol\n        │\n        ├─ embedded backend ──► zeta-app-server\n        └─ future remote backend ──► existing App Server\n```\n\n禁止：\n\n```text\nzeta-mcp-server ─X─► zeta-core internals\nzeta-mcp-server ─X─► session/thread stores\nzeta-core       ─X─► zeta-mcp-server\nzeta-mcp       ─X─► zeta-mcp-server\n```\n\nstdio 与 Streamable HTTP 都通过 `zeta-app-server-client` 连接同一个 embedded App Server\ncomposition。HTTP MCP session 拥有独立 App Server connection，但共享同一个 host 和 durable\nreceipt authority；它不会引入另一套 execution engine。Remote App Server backend 仍为\nProposed。\n\n## 6. Agent 工具接口面\n\n当前开发期只暴露最小 Agent-as-tool surface：\n\n| Tool | 作用 | 返回时机 |\n| --- | --- | --- |\n| `zeta` | 创建独立 Session/root Thread 并执行第一个 Turn | Turn terminal 或 interaction policy 阻塞 |\n| `zeta-reply` | 向指定 Thread 提交后续 Turn | 该 Turn terminal 或阻塞 |\n\n当前 `zeta` 输入：\n\n- `invocationId`：1–128 bytes 的受限 ASCII caller identity，用于 principal-scoped\n  single-flight、跨重启 replay/resume；\n- `prompt`：非空，最大 256 KiB；\n- 可选 `timeoutMs`：由 server 的 10 分钟上限约束。\n\n`zeta-reply` 额外接收 principal-authorized `threadId`。Workspace 由启动 server 的\n`ZETA_WORKSPACE_ROOT` 固定，不是 Tool argument；model、provider、policy 和 credential 继续由\nApp Server/config authority 解析。\n\nAdapter 派生的 App Server `commandId` 同时包含 principal hash、`invocationId` 和 operation，\n因此两个 HTTP principal 使用相同 invocation identity 也不会互相重放 Session/Turn。\n\n第一版不接受：\n\n- 任意 raw config map；\n- 调用方提供的 secret value；\n- 任意 system/developer instruction replacement；\n- `danger-full-access` 一类绕过 host policy 的 sandbox override；\n- 把 parent transcript 整体作为隐式 context 读取的请求。\n\n成功结果至少投影：\n\n```text\ninvocation identity\nsession_id\nthread_id\nturn_id\nterminal status\nbounded final content\n```\n\n结果不包含完整 transcript、secret、raw model response、未过滤 Tool output 或内部 store location。\n`zeta-reply` 必须校验 caller 是否仍有权访问目标 Thread，不能仅凭猜到的 `thread_id` 继续任务。\nstdio 使用 local-user principal；HTTP principal 由 bearer token 的不可逆 hash 派生。\ncaller-to-Thread binding 持久化在 profile SQLite，因此新的 connection/process 仍可恢复授权，但\n不能跨 principal 猜测 `thread_id`。\n\n## 7. 端到端流程\n\n### 7.1 启动（Start）\n\n```text\nMCP tools/call(zeta)\n→ validate schema, caller scope, invocation identity and limits\n→ start initialized in-process App Server client\n→ session/create\n→ session/request { type: createThread }\n→ session/thread/subscribe\n→ session/request { type: startTurn }\n→ bounded session/thread/read polling + notification drain\n→ project bounded progress\n→ forward approval/user input through elicitation/create when needed\n→ resolve exact interaction identity\n→ map terminal/blocked state to bounded MCP result\n→ return final content + stable Zeta identities\n```\n\n这是 Current subscription + bounded polling/drain 路径。它可在 Turn 运行期间投影增量\nprogress 和 interaction，但 correctness 仍以 App Server durable snapshot/update 和最终 result\n为准。MCP 后续可迁移到已经存在的 `AppServerSession` event driver，而不改变 authority。\n\n### 7.2 继续（Continue）\n\n```text\nMCP tools/call(zeta-reply)\n→ authenticate caller-to-thread binding\n→ validate thread is eligible for another Turn\n→ session/request { type: startTurn }\n→ poll the exact Thread/Turn snapshot\n→ return exact Turn result\n```\n\n继续已有 Thread 不创建新的 Session，也不把调用方当前对话自动复制进 Zeta context。调用方想\n传递的新信息必须存在于本次显式 prompt 或授权 artifact reference 中。\n\n### 7.3 取消与断开连接\n\nMCP cancel notification 必须映射到该 MCP request 对应的精确 Turn/operation。Transport EOF 或\nHTTP disconnect 不自动证明 Turn cancelled：\n\n- server 发出 best-effort cancellation；\n- 当前 adapter 调用精确 `session/request` `InterruptTurn`，并给 canonical terminal state 两秒 grace；\n- client 发送 `notifications/cancelled` 后，server 不再发送该 request 的 JSON-RPC response；\n- 若连接已无法接收结果，持久状态仍由 App Server/Core 决定；\n- 同一 principal 使用原 invocation identity 可在新 connection/process replay 或 resume；\n- HTTP transport disconnect 不自动 interrupt；stdio EOF 会因为本地进程拥有该 connection 而\n  cancel active work；\n- 已开始 Tool 的副作用继续遵循 Zeta UnknownOutcome/reconciliation 语义。\n\n## 8. 与内建子代理的关系\n\n| 维度 | 内建 child Agent | MCP 调用 Zeta |\n| --- | --- | --- |\n| identity | `DelegationId` + child `ThreadId` | invocation identity + 独立 Session/Thread |\n| topology | 同一 Session 的 Agent tree | 默认无 parent lineage |\n| context | immutable `AgentContextSeed` | 显式 prompt/artifact input |\n| policy | parent delegated ceiling 的交集 | server execution profile + caller grant |\n| lifecycle | spawn/send/join/cancel/close | MCP call/reply/cancel |\n| recovery | durable delegation/result delivery | invocation dedupe + App Server recovery |\n| UI | native Agent tree | external/MCP-origin task |\n\n不能仅因为调用方也是 Zeta，就把 MCP-created Thread 标记为 `AgentSpawn`。只有 remote Agent adapter\n已经在调用方 Core 内创建 `DelegationId`、context seed、budget reservation 和 result delivery\ncontract 时，它才能把远端执行结果作为 child result 接回：\n\n```text\nparent Thread\n→ MultiAgentCoordinator creates Delegation\n→ RemoteAgentAdapter invokes remote Zeta over MCP\n→ remote Zeta runs independent Thread\n→ adapter validates bounded result\n→ parent records DelegationResultReceived\n```\n\n远端 Zeta 的 `thread_id` 是 execution correlation，不替代调用方的 `DelegationId`。\n\n## 9. 交互、权限与信任\n\nAgent invocation 可能触发 shell、filesystem、network、credential 或 external mutation。MCP\nserver 不能因为调用者请求了某种能力就自动批准。\n\n当前规则：\n\n- stdio caller 与 OS user 相同不等于拥有所有 workspace/capability；\n- workspace、execution profile 和 model 都由 host authority 解析；\n- 调用方文本始终是 user-controlled content，不能升级为 system policy；\n- 若 client 不支持所需 interaction/elicitation，Turn 必须以可解释 blocked outcome 暂停或失败；\n- 疑似请求 password、token、credential、payment card 或 private key 的 user-input 不通过 form\n  elicitation 转发；\n- approval response 必须绑定精确 interaction/request identity；\n- server 不代表用户批准 side effect；\n- MCP progress、log 和 error 不包含 token、credential、完整 environment 或敏感 prompt。\n\n当前 HTTP 已有 bearer authentication、精确 Origin allowlist 和 process connection limit，但\n尚无 tenant/workspace provisioning、OAuth、redirect/egress control plane、built-in TLS 或\nper-principal distributed rate limit。公网部署必须由 TLS/auth reverse proxy 补齐这些边界。\n\n## 10. Progress、结果与兼容性\n\nMCP client 对 progress 和 custom notification 的支持不一致。最终 `tools/call` result 是唯一必须\n可消费的完成载体；notification 只提供增量体验，不能成为 correctness authority。\n\n事件投影规则：\n\n- 只发送 caller 有权看到的事件；\n- 只投影 bounded lifecycle message，连续重复 message 去重，每次 call 最多 256 条；\n- progress 使用 caller 的 exact `progressToken`；Zeta session/thread/turn identity 保留在最终\n  structured result；\n- approval、blocked、cancelled、failed 和 unknown outcome 不压成普通成功文本；\n- 客户端忽略所有 progress 时仍能从最终 result 得到有界、准确的 terminal outcome；\n- App Server event schema 演进先在 typed adapter 内处理，不把内部 DTO 原样变成 MCP public API。\n\n## 11. 失败与恢复\n\n| Failure | Required semantics |\n| --- | --- |\n| schema/caller validation failure | 不创建 Session，返回 stable invalid-request error |\n| App Server 尚未 ready | 不启动 Turn，返回 infrastructure error |\n| duplicate invocation | 同一 payload exact replay/resume 或 in-progress；不同 payload conflict |\n| transport loss before start commit | 可安全重试同一 invocation identity |\n| transport loss after start commit | 新 connection/process 以 durable receipt + deterministic command identity resume/replay |\n| interaction unsupported | durable blocked/failed outcome，不自动批准 |\n| Turn cancelled | client cancel 不返回原 response；server deadline 返回 canonical interrupted/unknown outcome |\n| output exceeds limit | 当前返回截断标记且不修改 durable Thread；artifact reference 为 Proposed |\n| embedded server shutdown | bounded cancellation、driver join 和清晰 process exit |\n\nMCP connection、request ID 和 process PID 都不是 durable authority。当前 adapter 将\nprincipal-scoped receipt 原子持久化到 `<ZETA_PROFILE_ROOT>/state.sqlite3` 的\n`mcp_invocation_receipts` / `mcp_thread_bindings`；它只负责外部 invocation correlation 与\nThread authorization，canonical Session/Thread/Turn 状态仍以 App Server/Core 为准。SQLite\n提交支持多 connection，但 active single-flight set 仍为进程内，因此同一 profile 当前只能有一个\nMCP server runtime。\n\n## 12. 可观测性\n\n允许记录：\n\n- protocol revision、transport 和 negotiated capability；\n- redacted caller/server identity；\n- invocation/session/thread/turn correlation 的安全 hash 或 opaque ID；\n- startup、queue、Turn 和 result latency；\n- cancel、blocked、error、unknown outcome 和 truncation count；\n- active connection/call count 和 backpressure。\n\n禁止记录：\n\n- 完整 prompt、reasoning、Tool arguments/result；\n- secret、authorization header、完整 environment；\n- raw MCP frame；\n- 未验证 workspace path 或 artifact body。\n\n## 13. 分阶段落地\n\n### 阶段 MS0：契约与 App Server 前置条件\n\n状态：部分具备。\n\n- [x] 完成 owned async `AppServerSession`、request handle、event stream 和 explicit shutdown；\n- 固定 MCP revision、tool schema、invocation identity 和 error/result fixture；\n- 使用 fake App Server session 覆盖 start、event、terminal、cancel 和 duplicate；\n- 明确 external invocation provenance，不与 `AgentSpawn` 混用。\n\n完成条件：adapter 不依赖同步 `drain_notifications`，重复 start 不会创建第二个任务。\n\n当前已完成 tool schema、invocation identity、error/result、duplicate 和 cancel fixture，以及\n共享的 owned async `AppServerSession` 与独立 event stream。MCP adapter 当前仍用 subscription +\nbounded polling/drain 提供实时能力，迁移到该通用 session 是后续适配工作。\n\n### 阶段 MS1：stdio `zeta` 纵向切片\n\n状态：Current 已完成。\n\n- stdio initialize、tools/list 和 tools/call；\n- embedded App Server composition；\n- `zeta` → Session/root Thread/Turn；\n- bounded progress 和 final result；\n- safe execution profile、deadline、cancel 和 graceful shutdown。\n\n完成条件：MCP Inspector/fake client 可以启动一个 Zeta Turn，并在 client 不消费 progress 时仍得到\n准确结果。\n\n当前已完成 stdio、embedded composition、Session/root Thread/Turn、deadline、cancel、graceful\nshutdown、bounded progress 和 final result。\n\n### 阶段 MS2：continue 与交互\n\n状态：Current 部分完成。\n\n- `zeta-reply` 和 caller-to-thread authorization；\n- interaction/approval 的 owner-directed delivery；\n- reconnect/query/replay；\n- artifact reference 和 output truncation。\n\n完成条件：继续请求不会串到错误 Thread，unsupported approval 不会被静默放行。\n\n当前已完成 `zeta-reply`、principal-scoped durable Thread authorization、approval/user-input\ndelivery、跨重启 replay/resume 和 output truncation；dynamic Tool interaction 与 artifact\nreference 尚未完成。\n\n### 阶段 MS3：远程部署\n\n状态：Current 部分完成。\n\n- 已完成 Streamable HTTP POST/DELETE、JSON/SSE 响应、Bearer 认证、Origin 白名单、安全会话\n  ID、协议标头校验和进程连接数量限制；\n- 已完成 HTTP 会话重建后，使用持久化调用身份恢复 Agent 调用；\n- 尚未实现独立 GET SSE、`Last-Event-ID` 重投递、OAuth、租户/工作区绑定、按主体分布式限流、\n  内置 TLS 以及重定向/数据外发控制面；\n- 远程 App Server 后端仍处于计划阶段。\n\n完成条件：向公网暴露前完成租户隔离、凭据、断开连接和滥用防护测试。\n\n### 阶段 MS4：远程 Agent 桥接\n\n- `MultiAgentCoordinator` 的 remote Agent port；\n- local `DelegationId` 到 remote invocation 的绑定；\n- context seed projection、budget/deadline 和 cancellation；\n- bounded result/artifact validation 与 durable delivery。\n\n完成条件：远端执行失败、迟到或重复结果不会破坏本地 Agent tree。\n\n## 14. 验证\n\n除 workspace 常规 `fmt/clippy/test` 外，必须覆盖：\n\n- protocol revision、initialize 和 tools schema fixtures；\n- start/continue 的 exact identity mapping；\n- duplicate、late response、cancel、EOF 和 App Server restart；\n- event gap、subscription cursor、out-of-order update 和 terminal result；\n- unsupported progress/elicitation client；\n- workspace/profile/model authorization；\n- prompt injection、secret/log redaction 和 output limit；\n- approval identity mismatch；\n- external invocation 与 `AgentSpawn` provenance 不混淆；\n- fake clock、fake App Server 和 deterministic transport；单元测试不依赖公网或真实模型。\n\n## 15. 长期不变量\n\n- App Server/Core 始终是 Session/Thread/Turn authority；\n- `zeta-mcp-server` 始终是 adapter，不建立第二套 Agent runtime；\n- 同进程子代理始终走 `MultiAgentCoordinator`，不经 MCP 自调用；\n- MCP request ID 不等于 Session、Thread、Turn、Delegation 或 invocation identity；\n- 外部调用默认创建独立 root task，不伪装成 child Agent；\n- retry 不得静默重复启动可能产生副作用的 Agent Turn；\n- 调用方不能通过 MCP 参数扩大 host policy、workspace、credential 或 sandbox 权限；\n- transport 断开不等于 durable operation 已取消；\n- progress 是体验层，最终结果和 App Server durable state 才是 correctness authority。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-产品场景与边界",
        "title": "2. 产品场景与边界"
      },
      {
        "depth": 2,
        "id": "3-当前仓库状态",
        "title": "3. 当前仓库状态"
      },
      {
        "depth": 2,
        "id": "4-所有权",
        "title": "4. 所有权"
      },
      {
        "depth": 3,
        "id": "41-zeta-mcp-server-拥有",
        "title": "4.1 zeta-mcp-server 拥有"
      },
      {
        "depth": 3,
        "id": "42-app-server-与核心保持拥有",
        "title": "4.2 App Server 与核心保持拥有"
      },
      {
        "depth": 2,
        "id": "5-依赖方向与部署形态",
        "title": "5. 依赖方向与部署形态"
      },
      {
        "depth": 2,
        "id": "6-agent-工具接口面",
        "title": "6. Agent 工具接口面"
      },
      {
        "depth": 2,
        "id": "7-端到端流程",
        "title": "7. 端到端流程"
      },
      {
        "depth": 3,
        "id": "71-启动start",
        "title": "7.1 启动（Start）"
      },
      {
        "depth": 3,
        "id": "72-继续continue",
        "title": "7.2 继续（Continue）"
      },
      {
        "depth": 3,
        "id": "73-取消与断开连接",
        "title": "7.3 取消与断开连接"
      },
      {
        "depth": 2,
        "id": "8-与内建子代理的关系",
        "title": "8. 与内建子代理的关系"
      },
      {
        "depth": 2,
        "id": "9-交互权限与信任",
        "title": "9. 交互、权限与信任"
      },
      {
        "depth": 2,
        "id": "10-progress结果与兼容性",
        "title": "10. Progress、结果与兼容性"
      },
      {
        "depth": 2,
        "id": "11-失败与恢复",
        "title": "11. 失败与恢复"
      },
      {
        "depth": 2,
        "id": "12-可观测性",
        "title": "12. 可观测性"
      },
      {
        "depth": 2,
        "id": "13-分阶段落地",
        "title": "13. 分阶段落地"
      },
      {
        "depth": 3,
        "id": "阶段-ms0契约与-app-server-前置条件",
        "title": "阶段 MS0：契约与 App Server 前置条件"
      },
      {
        "depth": 3,
        "id": "阶段-ms1stdio-zeta-纵向切片",
        "title": "阶段 MS1：stdio zeta 纵向切片"
      },
      {
        "depth": 3,
        "id": "阶段-ms2continue-与交互",
        "title": "阶段 MS2：continue 与交互"
      },
      {
        "depth": 3,
        "id": "阶段-ms3远程部署",
        "title": "阶段 MS3：远程部署"
      },
      {
        "depth": 3,
        "id": "阶段-ms4远程-agent-桥接",
        "title": "阶段 MS4：远程 Agent 桥接"
      },
      {
        "depth": 2,
        "id": "14-验证",
        "title": "14. 验证"
      },
      {
        "depth": 2,
        "id": "15-长期不变量",
        "title": "15. 长期不变量"
      }
    ],
    "searchText": " 物理位置：zeta-rs/mcp-server/ Rust crate：zetamcpserver，binary：zeta-mcp-server 当前状态：stdio 与 authenticated Streamable HTTP、bounded progress、form interaction 和 durable invocation recovery 已实现；独立 App Server event driver、SSE redelivery、 multi-tenant control plane 与 remote Agent bridge 仍为 Proposed Crate implementation contract：zeta-rs/mcp-server/README.md MCP client runtime：mcp.md App Server wire contract：zeta-app-server-api.md App Server client lifecycle：app-server-client.md 内建多 Agent：core-multi-agent.md 本文是“外部 MCP Host 如何调用 Zeta Agent”的 canonical 系统文档。MCP client session、 外部 capability catalog 和 tool/resource/prompt adapter 由 mcp.md 拥有；本地 parent/child delegation、context inheritance、join、budget 和 recovery 由 core-multi-agent.md 拥有。 快速理解 Zeta 应提供独立的 MCP server adapter，使 Codex、其他 Agent host 或另一个 Zeta 实例能够把 Zeta Agent 作为外部能力调用。它通过 App Server 的 canonical Session/Thread/Turn API 启动和 继续任务，不直接组装 zeta-core、model provider、Tool runtime、policy 或 stores。 ``text External MCP Host │ MCP over stdio / Streamable HTTP ▼ zeta-mcp-server │ typed App Server client ▼ App Server composition root │ ▼ Session / Thread / TurnExecutor ` 同进程 Zeta Agent 创建 child Agent 时不经过 MCP server。该路径仍由 MultiAgentCoordinator 直接建立 durable delegation： `text local parent Agent ──► MultiAgentCoordinator ──► child Thread ` 另一个进程或机器上的 Zeta 可以通过 MCP 被调用；若调用方希望把它视为正式 child Agent， 调用方必须在本地先建立 delegation，再由 remote Agent adapter 将执行映射到 MCP。MCP transport 本身不创造 parent/child 语义。 调用者想做什么 应使用的入口 是否建立 Zeta 内部父子关系 --- --- --- 外部 MCP Host 调用 Zeta MCP Server ❌ 本地 Agent 创建子 Agent 多 Agent 协调系统 ✅ 另一台机器上的 Zeta 执行任务 MCP 或未来远程适配器 默认不建立 把远端 Zeta 纳入本地 Agent 树 先建立本地委托，再映射远端执行 ✅ 2. 产品场景与边界 场景 首选入口 语义 --- --- --- Zeta Desktop、CLI 或 TUI 控制本地 Zeta App Server 完整产品控制面 本地 Agent 创建 child Agent MultiAgentCoordinator 原生 delegation 和 Agent tree Codex、Claude 或其他 MCP Host 调用 Zeta zeta-mcp-server Zeta 作为外部 Agent tool Zeta A 调用独立的 Zeta B zeta-mcp → zeta-mcp-server 跨 runtime Agent 调用 Zeta A 将 Zeta B 纳入自己的 Agent tree remote Agent adapter + MCP 本地 delegation，远端 execution zeta-mcp-server 不是： - zeta-mcp 的服务端模块；两者方向和依赖独立； - 内建多 Agent 的替代品； - App Server 的第二套 Session/Thread authority； - scheduler、worker lease 或跨机器 execution protocol； - 允许调用方注入任意 system/developer instruction、raw config 或未授权 workspace 的后门； - 将 MCP request ID 当作 durable Zeta identity 的兼容层。 3. 当前仓库状态 当前已实现： - zeta-mcp-server crate、独立 binary 和 zeta mcp-server CLI 入口； - MCP 2025-11-25 stdio 与 Streamable HTTP initialize、ping、tools/list、tools/call、 notifications/cancelled、progress 和 form elicitation； - zeta 创建 Session/root Thread/Turn，zeta-reply 继续同一 principal 获得授权的 Thread； - typed App Server client 映射、Thread subscription、bounded polling/notification drain、 有界 progress 和 final result； - caller-generated invocation identity、进程内 single-flight、跨进程重启 exact replay、 payload conflict 和 principal-scoped Thread binding； - approval/user-input 到 MCP elicitation/create 的映射，以及 exact typed interaction resolve； - bearer authentication、Origin 校验、MCP session/protocol header、DELETE termination、 SSE Tool call response 和连接数限制； - deadline/EOF/client cancellation 到精确 session/request InterruptTurn 的传播、unknown-outcome grace， 以及 client-cancelled request response suppression； - protocol、真实 in-process App Server、binary stdio restart 和 HTTP socket/SSE tests； - App Server 已提供 session/create、session/request、带 Session scope 的 Thread subscription 和 canonical Session/Thread updates； - zeta-app-server-client 已有 typed methods、schema hash 校验和 embedded InProcessTransport； - openlocalappserver 是本地 composition root，InProcessAppServer 允许多个 MCP HTTP session 连接同一个 embedded host； - Session、Thread、Turn 和 Item 已由 store/reducer 持久化。 当前缺口： - MCP adapter 当前仍通过 InProcessTransport 的同步 roundtrip 与 request 后 drainnotifications 工作，以 Thread subscription + bounded polling/drain 转发进度与 interaction； - 通用 AppServerSession、可克隆 request handle、独立 event stream 和显式 shutdown 已由 zeta-app-server-client 提供并被 CLI/TUI 使用，MCP adapter 尚未迁移到该 event driver； - MultiAgentCoordinator 和 remote Agent adapter 仍是 Proposed； - dynamic Tool interaction、artifact reference 和命名 execution profile 尚未完成； - HTTP 尚无 independent GET SSE、Last-Event-ID redelivery、OAuth、multi-tenant workspace binding、built-in TLS 或 remote App Server backend； - receipt rows 已进入 profile SQLite，但 single-flight active set 仍为进程内；同一 profile 当前 只能运行一个 MCP server process。 本文以下章节同时固定 Current surface 与 Proposed 演进；当前 HTTP listener 是 remote MCP adapter，不等于已具备公网 multi-tenant service 或 remote App Server execution plane。 4. 所有权 4.1 zeta-mcp-server 拥有 - MCP server initialize、capability advertisement、stdio 与 HTTP session lifecycle； - MCP tool definition、input/output schema 和 server-side validation； - MCP request 与 App Server request/Turn 的 correlation； - MCP cancellation 到精确 App Server operation 的传播； - Thread update 到有界 progress/final result 的转换； - approval/user-input 与 MCP form elicitation 的双向映射； - principal-scoped invocation receipt、replay 与 caller-to-Thread binding； - HTTP bearer、Origin、session/protocol validation 和 connection limit； - caller-visible sessionid、threadid 和 turnid 的安全投影； - connection-scoped pending call、backpressure、deadline 和 bounded diagnostics； - MCP wire error 与 App Server/domain error 的稳定映射； - Proposed：artifact reference、MCP task capability、SSE resume、OAuth/multi-tenant control plane、remote App Server backend 和 remote Agent bridge。 4.2 App Server 与核心保持拥有 能力 Canonical owner --- --- Session/Thread/Turn 创建和状态迁移 App Server → Core transcript、Item 和 durable sequence Thread store/reducer model、Tool、policy、approval 和 sandbox Core ports + composition root Config、credential 和 workspace resolution 对应 authority/runtime 本地 child delegation、join、budget 和 cancellation tree MultiAgentCoordinator model/tool execution TurnExecutor / ToolScheduler Desktop/TUI interaction owner selection App Server zeta-mcp-server 不直接依赖 store，不写 Thread event，不持有第二份 transcript，也不根据 MCP connection 状态推断 durable Turn 已成功或失败。 5. 依赖方向与部署形态 目标依赖： `text zeta-mcp-server │ ▼ zeta-app-server-client ──► zeta-app-server-protocol │ ├─ embedded backend ──► zeta-app-server └─ future remote backend ──► existing App Server ` 禁止： `text zeta-mcp-server ─X─► zeta-core internals zeta-mcp-server ─X─► session/thread stores zeta-core ─X─► zeta-mcp-server zeta-mcp ─X─► zeta-mcp-server ` stdio 与 Streamable HTTP 都通过 zeta-app-server-client 连接同一个 embedded App Server composition。HTTP MCP session 拥有独立 App Server connection，但共享同一个 host 和 durable receipt authority；它不会引入另一套 execution engine。Remote App Server backend 仍为 Proposed。 6. Agent 工具接口面 当前开发期只暴露最小 Agent-as-tool surface： Tool 作用 返回时机 --- --- --- zeta 创建独立 Session/root Thread 并执行第一个 Turn Turn terminal 或 interaction policy 阻塞 zeta-reply 向指定 Thread 提交后续 Turn 该 Turn terminal 或阻塞 当前 zeta 输入： - invocationId：1–128 bytes 的受限 ASCII caller identity，用于 principal-scoped single-flight、跨重启 replay/resume； - prompt：非空，最大 256 KiB； - 可选 timeoutMs：由 server 的 10 分钟上限约束。 zeta-reply 额外接收 principal-authorized threadId。Workspace 由启动 server 的 ZETAWORKSPACEROOT 固定，不是 Tool argument；model、provider、policy 和 credential 继续由 App Server/config authority 解析。 Adapter 派生的 App Server commandId 同时包含 principal hash、invocationId 和 operation， 因此两个 HTTP principal 使用相同 invocation identity 也不会互相重放 Session/Turn。 第一版不接受： - 任意 raw config map； - 调用方提供的 secret value； - 任意 system/developer instruction replacement； - danger-full-access 一类绕过 host policy 的 sandbox override； - 把 parent transcript 整体作为隐式 context 读取的请求。 成功结果至少投影： `text invocation identity sessionid threadid turnid terminal status bounded final content ` 结果不包含完整 transcript、secret、raw model response、未过滤 Tool output 或内部 store location。 zeta-reply 必须校验 caller 是否仍有权访问目标 Thread，不能仅凭猜到的 threadid 继续任务。 stdio 使用 local-user principal；HTTP principal 由 bearer token 的不可逆 hash 派生。 caller-to-Thread binding 持久化在 profile SQLite，因此新的 connection/process 仍可恢复授权，但 不能跨 principal 猜测 threadid。 7. 端到端流程 7.1 启动（Start） `text MCP tools/call zeta → validate schema, caller scope, invocation identity and limits → start initialized in-process App Server client → session/create → session/request type: createThread → session/thread/subscribe → session/request type: startTurn → bounded session/thread/read polling + notification drain → project bounded progress → forward approval/user input through elicitation/create when needed → resolve exact interaction identity → map terminal/blocked state to bounded MCP result → return final content + stable Zeta identities ` 这是 Current subscription + bounded polling/drain 路径。它可在 Turn 运行期间投影增量 progress 和 interaction，但 correctness 仍以 App Server durable snapshot/update 和最终 result 为准。MCP 后续可迁移到已经存在的 AppServerSession event driver，而不改变 authority。 7.2 继续（Continue） `text MCP tools/call zeta-reply → authenticate caller-to-thread binding → validate thread is eligible for another Turn → session/request type: startTurn → poll the exact Thread/Turn snapshot → return exact Turn result ` 继续已有 Thread 不创建新的 Session，也不把调用方当前对话自动复制进 Zeta context。调用方想 传递的新信息必须存在于本次显式 prompt 或授权 artifact reference 中。 7.3 取消与断开连接 MCP cancel notification 必须映射到该 MCP request 对应的精确 Turn/operation。Transport EOF 或 HTTP disconnect 不自动证明 Turn cancelled： - server 发出 best-effort cancellation； - 当前 adapter 调用精确 session/request InterruptTurn，并给 canonical terminal state 两秒 grace； - client 发送 notifications/cancelled 后，server 不再发送该 request 的 JSON-RPC response； - 若连接已无法接收结果，持久状态仍由 App Server/Core 决定； - 同一 principal 使用原 invocation identity 可在新 connection/process replay 或 resume； - HTTP transport disconnect 不自动 interrupt；stdio EOF 会因为本地进程拥有该 connection 而 cancel active work； - 已开始 Tool 的副作用继续遵循 Zeta UnknownOutcome/reconciliation 语义。 8. 与内建子代理的关系 维度 内建 child Agent MCP 调用 Zeta --- --- --- identity DelegationId + child ThreadId invocation identity + 独立 Session/Thread topology 同一 Session 的 Agent tree 默认无 parent lineage context immutable AgentContextSeed 显式 prompt/artifact input policy parent delegated ceiling 的交集 server execution profile + caller grant lifecycle spawn/send/join/cancel/close MCP call/reply/cancel recovery durable delegation/result delivery invocation dedupe + App Server recovery UI native Agent tree external/MCP-origin task 不能仅因为调用方也是 Zeta，就把 MCP-created Thread 标记为 AgentSpawn。只有 remote Agent adapter 已经在调用方 Core 内创建 DelegationId、context seed、budget reservation 和 result delivery contract 时，它才能把远端执行结果作为 child result 接回： `text parent Thread → MultiAgentCoordinator creates Delegation → RemoteAgentAdapter invokes remote Zeta over MCP → remote Zeta runs independent Thread → adapter validates bounded result → parent records DelegationResultReceived ` 远端 Zeta 的 threadid 是 execution correlation，不替代调用方的 DelegationId。 9. 交互、权限与信任 Agent invocation 可能触发 shell、filesystem、network、credential 或 external mutation。MCP server 不能因为调用者请求了某种能力就自动批准。 当前规则： - stdio caller 与 OS user 相同不等于拥有所有 workspace/capability； - workspace、execution profile 和 model 都由 host authority 解析； - 调用方文本始终是 user-controlled content，不能升级为 system policy； - 若 client 不支持所需 interaction/elicitation，Turn 必须以可解释 blocked outcome 暂停或失败； - 疑似请求 password、token、credential、payment card 或 private key 的 user-input 不通过 form elicitation 转发； - approval response 必须绑定精确 interaction/request identity； - server 不代表用户批准 side effect； - MCP progress、log 和 error 不包含 token、credential、完整 environment 或敏感 prompt。 当前 HTTP 已有 bearer authentication、精确 Origin allowlist 和 process connection limit，但 尚无 tenant/workspace provisioning、OAuth、redirect/egress control plane、built-in TLS 或 per-principal distributed rate limit。公网部署必须由 TLS/auth reverse proxy 补齐这些边界。 10. Progress、结果与兼容性 MCP client 对 progress 和 custom notification 的支持不一致。最终 tools/call result 是唯一必须 可消费的完成载体；notification 只提供增量体验，不能成为 correctness authority。 事件投影规则： - 只发送 caller 有权看到的事件； - 只投影 bounded lifecycle message，连续重复 message 去重，每次 call 最多 256 条； - progress 使用 caller 的 exact progressToken；Zeta session/thread/turn identity 保留在最终 structured result； - approval、blocked、cancelled、failed 和 unknown outcome 不压成普通成功文本； - 客户端忽略所有 progress 时仍能从最终 result 得到有界、准确的 terminal outcome； - App Server event schema 演进先在 typed adapter 内处理，不把内部 DTO 原样变成 MCP public API。 11. 失败与恢复 Failure Required semantics --- --- sch"
  },
  {
    "slug": "permissions",
    "sourcePath": "docs/permissions.md",
    "title": "权限系统",
    "description": "Zeta 使用分层权限系统来平衡功能和安全性：能在明确沙箱边界内完成的动作优先受限执行；需要越过 边界的动作再结合用户意图、风险和已有授权决定是自动批准、询问用户还是阻止。",
    "group": "安全与权限",
    "markdown": "> 文档所有权：本文件是 Zeta 权限系统产品语义、用户心智模型、跨 crate 执行路径和长期\n> 不变量的权威文档。\n>\n> 文档状态：当前契约 + 明确的当前限制 + 计划演进。\n> Agent 自定义 artifact、外部 Import 与只读 source registration 的领域边界见\n> [`agent-customizations.md`](agent-customizations.md)。\n\n## 快速理解\n\nZeta 使用分层权限系统来平衡功能和安全性：能在明确沙箱边界内完成的动作优先受限执行；需要越过\n边界的动作再结合用户意图、风险和已有授权决定是自动批准、询问用户还是阻止。\n\n| 工具类型 | 示例 | 需要批准 | 批准后的行为 |\n| --- | --- | --- | --- |\n| 工作区只读 | 读取文件、搜索、`Grep` | 通常不需要；前提是路径在允许范围内并可使用只读沙箱 `ReadOnly` | 不适用 |\n| 工作区文件修改 | `Edit`、`Write`、创建或移动文件 | 在工作区可写沙箱 `WorkspaceWrite` 和允许写入范围内通常不需要；越过范围时需要重新判断 | 如果询问，只批准当前调用 |\n| 本地命令 | `cargo test`、`git status`、Shell 执行 | 可在匹配的沙箱策略中运行时不需要；必须在沙箱外执行且无法自动授权时需要 | 只批准当前工具调用 |\n| 网络访问 | 下载依赖、HTTP 请求、访问远端 API | 需要显式网络能力 `Network`；当前意图或目标作用范围不足以授权时需要 | 只批准当前动作和网络作用范围 |\n| 凭证与外部修改 | 使用令牌（token）、`git push`、创建 PR、修改云资源 | 根据用户意图和风险判断；极高风险直接阻止，不向用户请求放行 | 只批准当前动作、凭证用途和目标资源 |\n| 系统与界面控制 | 修改系统配置、控制浏览器或桌面 UI | 沙箱无法覆盖且没有足够执行授权时需要 | 只批准当前动作和能力集合 |\n\n批准交互当前仍只有一次性 `ApproveOnce`，不会因历史点击自动升级。长期规则是另一条显式配置\n路径：User Config 可以持久化 typed execution-policy rule；Workspace 配置只能增加拒绝、强制沙箱\n或强制审批等限制，不能给自己授予沙箱外执行权。当前还没有把规则编辑器包装成“此项目始终允许”\n的批准按钮。\n\n### 核心规则\n\n| 规则 | 含义 |\n| --- | --- |\n| 授权单位 | 对一次已经解析清楚的具体动作授权，不对整个工具全局放行 |\n| 判断输入 | 动作、来源、最小能力、作用范围、沙箱兼容性、用户意图和风险 |\n| 用户决定 | 一次性批准 `ApproveOnce`、拒绝 `Decline`，或通过配置保存精确 digest 规则 |\n| 一次性绑定 | 批准请求、工具调用、动作摘要、完整能力集合和策略版本 |\n| 安全原则 | 可以减少无效询问，但不能用模糊匹配、模型自信或历史点击替代精确授权 |\n\n### 每个 Turn 的交互模式\n\nTUI 当前在 footer 最左侧显示并用 Shift-Tab 循环三种模式。模式在提交时冻结到\n`TurnAccepted`，所以运行中切换只影响后续 Turn，包括排队的 follow-up。\n\n| Footer 文案 | 模式 | authoritative policy 返回 `AskUser` 时 |\n| --- | --- | --- |\n| `ask permissions on` | `AskPermissions` | 创建 durable approval，由用户 approve once 或 decline |\n| `auto review on` | `AutoReview` | 调用配置的审查模型，再由 `ActionPolicyEngine` 应用风险与授权矩阵；模型不可用或失败时继续询问用户 |\n| `bypass permissions on` | `BypassPermissions` | 跳过这次交互并签发精确绑定的 bypass authority |\n\n`BypassPermissions` 不是关闭全部安全检查。base policy 始终先运行；确定性 `Block`、策略版本不匹配、\n无效 action/capability binding、沙箱硬约束和 policy error 都不会被改写。bypass authority 仍绑定\n当前 action digest、完整 capability set、policy revision 和 exact Tool Call，并在副作用开始前\ndurable 记录。\n\n### 系统内部如何表达\n\n上表描述用户行为；系统内部将每次判断表示为以下类型化结果：\n\n| 系统结果 | 用户含义 | 谁拥有最终决定 |\n| --- | --- | --- |\n| `RunSandboxed` | 在明确的文件系统和网络限制中执行 | 确定性策略 |\n| `RunExecPolicyGranted` | 命中显式 `AllowUnsandboxed` 规则；authority 精确绑定 rule、exec-policy revision、动作与能力 | `zeta-execpolicy` 求值，`ActionPolicyEngine` 签发最终 grant |\n| `RunAutoReviewed` | 不适用沙箱或需要额外能力，但上下文风险满足自动授权条件 | 策略引擎 `ActionPolicyEngine`；风险审查器只提供建议 |\n| `RunUnsandboxed` | 使用已有的精确用户授权执行 | 用户授权 + `ActionPolicyEngine` 精确匹配 |\n| `RunWithPermissionBypass` | 当前 Turn 选择跳过本来需要的交互，但仍保留精确绑定与审计 | 可信产品 policy adapter；只能替换 `AskUser` |\n| `AskUser` | 缺少足够、明确的执行授权 | 用户 |\n| `ReviseAction` | 当前动作过宽，Agent 应提出更小、更安全的动作 | `ActionPolicyEngine` |\n| `Block` | 命中确定性禁令、极高风险、审查失败或沙箱硬约束 | 确定性策略 |\n\n`AskUser` 不是异常，也不等于系统“不够聪明”。它表示当前上下文不足以安全地替用户作决定。\n\n## 权限不是一个开关\n\n权限系统由六层相互独立的约束组成：\n\n| 层 | 解决的问题 | 不负责什么 |\n| --- | --- | --- |\n| 动作解析 | 把工具参数、工作目录、解析后的路径、环境和来源变成精确动作 | 不批准执行 |\n| 能力模型 | 描述动作需要的最小能力与作用范围 | 不判断用户意图 |\n| 确定性规则 | `zeta-execpolicy` 组合 Host / Organization / User / Workspace layer 并返回纯 effect | 不签发 grant、不执行工具 |\n| 最终 action policy | `zeta-action-policy` 把 rule effect、exact grants、sandbox 与 reviewer 结果合成最终决定 | 不解析或持久化规则、不执行工具 |\n| Auto Review | 根据标明信任来源的上下文给出风险建议 | 不能签发最终执行授权 |\n| 持久化批准与执行 | `ConfigStore` 保存精确用户规则；Core 保存一次性批准和副作用起点 | 不改变前面的安全判断 |\n\n因此：\n\n- 沙箱负责**强制执行边界**，不负责用户批准；\n- Auto Review 是**风险审查建议层**，不是第二套权限系统；\n- 批准界面负责**取得用户授权**，不是策略引擎；\n- 工具或 MCP 服务器声称“只读”只能作为证据，不能成为执行授权；\n- Skill 的 `allowed-tools` 是提示，不是权限；\n- Plugin 声明的权限、Plugin 安装授权和单次工具调用批准是三个不同层次。\n\n## 识别哪些能力\n\n当前能力类型 `CapabilityKind` 定义八类能力。真正参与授权匹配的是“类型 + 作用范围”，不只是\n下表中的名称。\n\n| 能力 | 典型动作 | 需要特别说明的作用范围 |\n| --- | --- | --- |\n| 文件读取 `FileRead` | 读取文件、搜索内容 | 目录、文件集合、是否越过工作区 |\n| 文件写入 `FileWrite` | 创建、编辑、删除或移动文件 | 精确路径、可写根、破坏性范围 |\n| 启动进程 `ProcessSpawn` | 启动本地程序或 Shell 命令 | 可执行文件、参数、工作目录和环境变量 |\n| 网络访问 `Network` | HTTP、下载、远端连接 | 主机、协议、端口、请求范围 |\n| 使用凭证 `CredentialUse` | 使用令牌、账号或密钥 | 凭证标识、目标服务、用途 |\n| 外部修改 `ExternalMutation` | 修改 GitHub、Linear、云资源等外部状态 | 服务、资源标识、操作类型 |\n| 系统配置 `SystemConfiguration` | 修改系统级设置或安装环境 | 系统资源、变更范围、恢复方式 |\n| 用户界面 `UserInterface` | 控制浏览器或桌面界面 | 应用、页面、交互目标 |\n\n能力集合必须是完成动作所需的最小集合。把多个未来可能需要的能力预先合并，会扩大授权范围，也会\n让批准说明失真。\n\n## 一次动作如何获得执行权\n\n当前权威决策顺序如下：\n\n```text\nAgent 提出工具调用\n  → 主机解析精确动作、来源、能力集合和沙箱兼容性\n  → zeta-execpolicy：按 typed selector 求值 Host / Organization / User / Workspace rules\n  → ActionPolicyEngine：映射 Deny / RequireSandbox / RequireApproval / AllowUnsandboxed\n  → AllowUnsandboxed：签发绑定 rule、exec-policy revision、动作与能力的 RunExecPolicyGranted\n  → 精确用户授权：匹配动作摘要、能力集合和策略版本\n  → 可用沙箱：RunSandboxed\n  → Auto Review：Approve / ReviseAction / AskUser / Deny\n  → 策略引擎 ActionPolicyEngine：RunAutoReviewed / ReviseAction / AskUser / Block\n  → Core 持久化记录执行授权与 ToolExecutionStarted\n  → 执行器按 Sandboxed 或 Unrestricted 授权执行\n  → 持久记录确定结果\n```\n\n顺序本身就是安全契约：\n\n1. 更严格 effect 始终胜出；Workspace layer 不能产生 `AllowUnsandboxed`；\n2. historical exact grant 必须精确匹配，不能按工具名称、命令前缀或自然语言摘要复用；typed\n   command-prefix rule 则是独立、显式、带 revision 的 policy 对象；\n3. 风险审查器不能构造自动审查授权 `AutoReviewGrant`；\n4. 执行器只消费显式的沙箱授权 `Sandboxed(policy)` 或无限制授权 `Unrestricted`；\n5. 沙箱启动失败不得静默降级为无限制执行。\n\n## 用户批准到底批准了什么\n\n当前协议的即时用户决定仍只有两种；持久化规则是独立的 Config authority，不会改变 `ApproveOnce`：\n\n| 决定 | 当前语义 |\n| --- | --- |\n| 一次性批准 `ApproveOnce` | 允许当前批准请求对应的当前工具调用执行一次 |\n| 拒绝 `Decline` | 拒绝当前请求；原工具调用以明确失败结束 |\n\n一次性授权绑定以下标识：\n\n- `RequestId`；\n- `ToolCallId`；\n- 主机规范化后生成的动作摘要 `ActionDigest`；\n- 完整 `CapabilitySet`；\n- `ActionPolicyRevision`。\n\n恢复中的批准也必须重新匹配准备执行的动作。只要路径、参数、环境、来源、能力集合或策略\n版本发生安全相关变化，就不能沿用旧批准。\n\n### 长期规则与一次性批准必须分开\n\n当前已实现 User rule 的 typed 持久化、command prefix/network/capability/source/action selector、\nHost/Organization/User/Workspace layer composition、semantic revision 和运行时重组。仍未实现：\n\n- “本次会话始终允许”；\n- “此项目始终允许”；\n- approval UI 中的“保存为长期规则”；\n- rule 到期时间和统一规则管理 UI；\n- Organization layer 的产品级远端分发 adapter；\n- 由 Agent 自行把一次批准升级为长期规则。\n\nUser rule 可以用 typed source、command prefix、network target 或 capability scope 授权；它不是历史\napproval 的模糊复用。Workspace rule 只允许收紧。任何规则变更都会产生新的 exec-policy revision，\n并进入新的 `ActionPolicyRevision`；旧 Turn 和旧 grant 不会静默获得更宽权限。\n\n这些能力未来即使加入，也必须拥有独立、可审计的作用范围和撤销语义，不能改变\n`ApproveOnce` 的含义。\n\n### 受控内容来源不是长期执行批准\n\nDesktop 外部 Agent 配置导入计划允许用户把明确选择的 Codex `~/.agents/skills`、Claude\n`~/.claude/skills` 等目录注册为可撤销的只读内容来源。当前 `zeta-agent-import` 只实现已知\n路径的 metadata-only 检查和 `AgentPathInspection`，尚未实现 Desktop 确认与 Config apply。未来\napply 操作产生类型化配置和受控来源身份，不产生按工具、命令前缀或路径模式匹配的执行授权，\n因此不属于“是，不再询问”或“此项目始终允许”。\n\n注册后的读取只能发生在经过规范化和 containment 校验的窄来源根内；整个 `~/.codex`、\n`~/.claude` 或用户主目录不会因此变成 Agent 可浏览范围，`~/.codex/auth.json` 和包含 OAuth\nsession/cache 的 `~/.claude.json` 也不会进入当前发现计划。Skill 中的脚本、网络请求或其他\n副作用仍按当前动作单独进入权限与沙箱判断。\n\n该导入入口仅由 Desktop 提供；TUI 可以消费 App Server 已发布的统一 Skill catalog，但不能创建\n或管理外部来源。完整产品边界见\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#22-外部-agent-配置导入仅限-desktop)\n和 [`skills.md`](skills.md#151-外部-agent-skill-导入仅限-desktop)。\n\n## 沙箱在权限系统中的位置\n\n本地执行的沙箱策略由文件系统约束和网络约束组成：\n\n| 文件系统 | 含义 |\n| --- | --- |\n| 只读 `ReadOnly` | 可读取允许范围，不写入 |\n| 工作区可写 `WorkspaceWrite` | 只允许写入经过解析和验证的工作区根目录 |\n| 完全访问 `FullAccess` | 不以工作区文件边界约束动作 |\n\n| 网络 | 含义 |\n| --- | --- |\n| 禁止 `Denied` | 平台后端必须实际阻止网络访问 |\n| 允许 `Allowed` | 动作可使用网络；仍不自动获得凭证使用或外部修改授权 |\n\n工作区可写策略 `WorkspaceWrite` 只是约束描述，不能代替平台强制执行。对于要求受限执行的动作，\n后端缺失、不支持相应策略或不能证明约束已生效时必须失败即关闭（fail closed）。\n\n平台后端、支持矩阵和当前限制见 [`sandboxing.md`](sandboxing.md)。\n\n## 沙箱拒绝后为什么不能直接重跑\n\n“命令在沙箱里失败”不能推出“应该在沙箱外再试一次”。失败可能来自参数错误、程序错误、资源\n不存在或部分副作用已经发生。\n\n当前只允许一种受控升级路径：\n\n1. 执行器返回类型化的沙箱拒绝结果；\n2. 拒绝结果明确标记为可安全重试 `SafeToRetry`；\n3. 长度受限的证据回到策略审查；\n4. 策略重新判断；\n5. 获得新的明确执行授权后，最多进行一次沙箱外重试。\n\n以下情况不得自动重放：\n\n- 普通非零退出；\n- 沙箱拒绝前可能已产生副作用；\n- 工具已经开始，但崩溃或恢复后结果未知；\n- 证据无法证明失败来自沙箱强制执行；\n- 新动作与原动作摘要或能力集合不一致。\n\n“不知道是否成功”必须保留为未知执行结果（unknown outcome），而不是假设失败后再次执行。\n\n## 谁负责什么\n\n| 组件 | 当前责任 | 明确不拥有 |\n| --- | --- | --- |\n| 主机与工具适配器 | 解析精确动作、来源、最小能力和沙箱兼容性 | 最终批准 |\n| `zeta-execpolicy` | typed selector、layer validation、effect precedence、semantic revision 与纯求值 | 最终 grant、Tool 执行、配置 I/O |\n| `zeta-action-policy` | effect 映射、exact grant、sandbox、风险门槛和最终类型化结果 | 规则解析/持久化、工具执行、UI |\n| `zeta-config` | User rule 的 typed TOML mutation/persistence；Workspace restriction 的 strict-read intent | 规则求值、最终执行授权 |\n| `zeta-auto-review` | 生成受 schema 约束的风险审查结论 | 覆盖策略、签发授权 |\n| Core 的 `ToolScheduler` | 持久化批准、一次性授权、执行生命周期和恢复语义 | 操作系统沙箱强制执行 |\n| `zeta-exec` 与工具执行器 | 消费明确执行授权并返回类型化结果 | 自行提权 |\n| `zeta-sandboxing` | 选择并启动平台强制执行后端 | 批准、重试和持久化 |\n| App Server、CLI 与 Desktop | 暴露权限 CRUD、展示动作、作用范围、风险和用户选择 | 改写权威策略 |\n\n任何一层同时承担“描述动作、判断风险、签发权限、执行副作用”中的多项职责，都会削弱审计边界。\n\n## 当前实现状态\n\n| 能力 | 状态 | 边界 |\n| --- | --- | --- |\n| 精确绑定动作、能力集合和策略版本 | 当前已实现 | 规范字节的完整性仍依赖主机动作解析器 |\n| Host/Organization/User/Workspace typed layer 与 semantic revision | 当前已实现 | Organization 的产品分发 adapter 尚未接入 |\n| source、command prefix、network、capability、action selector | 当前已实现 | selector 只消费 host-materialized typed fields |\n| User rule 持久化与 Workspace 只收紧规则 | 当前已实现 | 统一规则编辑 UI、expiry 尚未实现 |\n| exec-policy exact durable execution authority | 当前已实现 | 绑定 rule ID、exec-policy revision、action、capabilities 与 Tool Call |\n| Auto Review 类型化建议与风险门槛 | 当前已实现 | 当前是单次审查，没有分层审查或多审查器协作 |\n| 持久化批准请求与 `ApproveOnce` / `Decline` | 当前已实现 | 各客户端的呈现体验尚未完全统一 |\n| TUI 的 Ask / Auto Review / Bypass per-Turn 模式 | 当前已实现 | 模式在提交时冻结；review model 当前在 App Server 启动时解析 |\n| 副作用前记录工具执行开始 | 当前已实现 | 崩溃后的未知结果不自动重放 |\n| 类型化沙箱拒绝再审查 | 当前已实现 | 最多一次；真实平台拒绝样本仍有限 |\n| macOS、Linux 和 Windows 平台沙箱 | 部分具备 | 具体支持和集成验收以沙箱文档为准 |\n| rule expiry | 尚未完成 | 需要独立时间与撤销语义 |\n| 面向用户的统一权限解释器与历史审计页 | 尚未完成 | 协议和 Core 契约可作为后续 UI 基础 |\n\n## 计划方向：让权限更容易理解，而不是更模糊\n\n后续产品演进应优先改善解释和管理能力：\n\n1. 批准界面用自然语言展示“动作、目标、能力、作用范围、来源、沙箱差异和风险理由”；\n2. 将“为什么这次询问”和“为什么不能在沙箱中完成”分开解释；\n3. 提供可查询的权限决定历史，但对密钥和敏感参数做结构化脱敏；\n4. 为已实现的 User/Workspace rules 提供可解释的管理 UI，并为 Organization 分发和 expiry 增加\n   独立 adapter；\n5. 用真实沙箱拒绝、危险自动批准率和人工标签评估 Auto Review，而不是只统计减少了多少弹窗；\n6. 各客户端共享生成的协议和相同决定语义，不各自创造权限模式。\n\n长期规则不是一次性批准的“快捷保存”。它已经是独立、typed、revision-bound 的策略对象；未来 UI\n若提供保存入口，也必须生成并展示该对象，而不能偷偷复用历史点击。\n\n## 这也是开发方法\n\n权限文档不只是功能介绍，它可以作为契约驱动开发（contract-driven development）的入口：\n\n1. **先写用户语义**：用户看见什么、批准什么、拒绝后发生什么；\n2. **再划分授权职责**：谁能建议、谁能签发、谁能执行、谁只能展示；\n3. **定义不变量**：优先拒绝、精确绑定、失败即关闭、未知结果不重放；\n4. **落到类型**：使用枚举、newtype 和类型化分支表达，不使用含糊的布尔值；\n5. **映射持久化边界**：副作用前记录什么，恢复时依据什么继续；\n6. **派生测试**：每条重要语义都应有单元测试、集成测试或平台验收证据；\n7. **标注状态**：当前实现、当前限制和计划设计永远分开。\n\n文档先行不代表“文档说了就算实现”。正确的关系是：\n\n```text\n产品语义\n  → 权威文档\n  → 类型化契约\n  → 责任方实现\n  → 测试与验收\n  → 文档中的当前状态\n```\n\n如果代码无法证明文档中的当前行为，应修正实现或把文档降为计划设计；不能用模糊措辞掩盖\n二者不一致。\n\n## 审查清单\n\n修改权限、工具、Plugin、MCP、沙箱或批准界面时，至少确认：\n\n- [ ] 动作摘要是否覆盖所有安全相关输入；\n- [ ] 能力是否最小且作用范围明确；\n- [ ] 确定性规则是否仍早于用户授权和风险审查器；\n- [ ] 风险审查器是否仍只能建议、不能签发执行授权；\n- [ ] 批准是否绑定精确请求、工具调用、能力集合和策略版本；\n- [ ] 副作用前是否已持久化记录执行开始；\n- [ ] 未知执行结果是否不会自动重放；\n- [ ] 沙箱失败是否不会静默降级；\n- [ ] 新 UI 选项是否在协议和 Core 中有唯一、明确的权威语义；\n- [ ] 当前实现与计划设计是否仍清楚分离。\n\n确定性规则语言见 [`zeta-execpolicy` README](../zeta-rs/execpolicy/README.md)；最终决策顺序与 grant\nbinding 见 [`zeta-action-policy` README](../zeta-rs/action-policy/README.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 3,
        "id": "核心规则",
        "title": "核心规则"
      },
      {
        "depth": 3,
        "id": "每个-turn-的交互模式",
        "title": "每个 Turn 的交互模式"
      },
      {
        "depth": 3,
        "id": "系统内部如何表达",
        "title": "系统内部如何表达"
      },
      {
        "depth": 2,
        "id": "权限不是一个开关",
        "title": "权限不是一个开关"
      },
      {
        "depth": 2,
        "id": "识别哪些能力",
        "title": "识别哪些能力"
      },
      {
        "depth": 2,
        "id": "一次动作如何获得执行权",
        "title": "一次动作如何获得执行权"
      },
      {
        "depth": 2,
        "id": "用户批准到底批准了什么",
        "title": "用户批准到底批准了什么"
      },
      {
        "depth": 3,
        "id": "长期规则与一次性批准必须分开",
        "title": "长期规则与一次性批准必须分开"
      },
      {
        "depth": 3,
        "id": "受控内容来源不是长期执行批准",
        "title": "受控内容来源不是长期执行批准"
      },
      {
        "depth": 2,
        "id": "沙箱在权限系统中的位置",
        "title": "沙箱在权限系统中的位置"
      },
      {
        "depth": 2,
        "id": "沙箱拒绝后为什么不能直接重跑",
        "title": "沙箱拒绝后为什么不能直接重跑"
      },
      {
        "depth": 2,
        "id": "谁负责什么",
        "title": "谁负责什么"
      },
      {
        "depth": 2,
        "id": "当前实现状态",
        "title": "当前实现状态"
      },
      {
        "depth": 2,
        "id": "计划方向让权限更容易理解而不是更模糊",
        "title": "计划方向：让权限更容易理解，而不是更模糊"
      },
      {
        "depth": 2,
        "id": "这也是开发方法",
        "title": "这也是开发方法"
      },
      {
        "depth": 2,
        "id": "审查清单",
        "title": "审查清单"
      }
    ],
    "searchText": " 文档所有权：本文件是 Zeta 权限系统产品语义、用户心智模型、跨 crate 执行路径和长期 不变量的权威文档。 文档状态：当前契约 + 明确的当前限制 + 计划演进。 Agent 自定义 artifact、外部 Import 与只读 source registration 的领域边界见 agent-customizations.md。 快速理解 Zeta 使用分层权限系统来平衡功能和安全性：能在明确沙箱边界内完成的动作优先受限执行；需要越过 边界的动作再结合用户意图、风险和已有授权决定是自动批准、询问用户还是阻止。 工具类型 示例 需要批准 批准后的行为 --- --- --- --- 工作区只读 读取文件、搜索、Grep 通常不需要；前提是路径在允许范围内并可使用只读沙箱 ReadOnly 不适用 工作区文件修改 Edit、Write、创建或移动文件 在工作区可写沙箱 WorkspaceWrite 和允许写入范围内通常不需要；越过范围时需要重新判断 如果询问，只批准当前调用 本地命令 cargo test、git status、Shell 执行 可在匹配的沙箱策略中运行时不需要；必须在沙箱外执行且无法自动授权时需要 只批准当前工具调用 网络访问 下载依赖、HTTP 请求、访问远端 API 需要显式网络能力 Network；当前意图或目标作用范围不足以授权时需要 只批准当前动作和网络作用范围 凭证与外部修改 使用令牌（token）、git push、创建 PR、修改云资源 根据用户意图和风险判断；极高风险直接阻止，不向用户请求放行 只批准当前动作、凭证用途和目标资源 系统与界面控制 修改系统配置、控制浏览器或桌面 UI 沙箱无法覆盖且没有足够执行授权时需要 只批准当前动作和能力集合 批准交互当前仍只有一次性 ApproveOnce，不会因历史点击自动升级。长期规则是另一条显式配置 路径：User Config 可以持久化 typed execution-policy rule；Workspace 配置只能增加拒绝、强制沙箱 或强制审批等限制，不能给自己授予沙箱外执行权。当前还没有把规则编辑器包装成“此项目始终允许” 的批准按钮。 核心规则 规则 含义 --- --- 授权单位 对一次已经解析清楚的具体动作授权，不对整个工具全局放行 判断输入 动作、来源、最小能力、作用范围、沙箱兼容性、用户意图和风险 用户决定 一次性批准 ApproveOnce、拒绝 Decline，或通过配置保存精确 digest 规则 一次性绑定 批准请求、工具调用、动作摘要、完整能力集合和策略版本 安全原则 可以减少无效询问，但不能用模糊匹配、模型自信或历史点击替代精确授权 每个 Turn 的交互模式 TUI 当前在 footer 最左侧显示并用 Shift-Tab 循环三种模式。模式在提交时冻结到 TurnAccepted，所以运行中切换只影响后续 Turn，包括排队的 follow-up。 Footer 文案 模式 authoritative policy 返回 AskUser 时 --- --- --- ask permissions on AskPermissions 创建 durable approval，由用户 approve once 或 decline auto review on AutoReview 调用配置的审查模型，再由 ActionPolicyEngine 应用风险与授权矩阵；模型不可用或失败时继续询问用户 bypass permissions on BypassPermissions 跳过这次交互并签发精确绑定的 bypass authority BypassPermissions 不是关闭全部安全检查。base policy 始终先运行；确定性 Block、策略版本不匹配、 无效 action/capability binding、沙箱硬约束和 policy error 都不会被改写。bypass authority 仍绑定 当前 action digest、完整 capability set、policy revision 和 exact Tool Call，并在副作用开始前 durable 记录。 系统内部如何表达 上表描述用户行为；系统内部将每次判断表示为以下类型化结果： 系统结果 用户含义 谁拥有最终决定 --- --- --- RunSandboxed 在明确的文件系统和网络限制中执行 确定性策略 RunExecPolicyGranted 命中显式 AllowUnsandboxed 规则；authority 精确绑定 rule、exec-policy revision、动作与能力 zeta-execpolicy 求值，ActionPolicyEngine 签发最终 grant RunAutoReviewed 不适用沙箱或需要额外能力，但上下文风险满足自动授权条件 策略引擎 ActionPolicyEngine；风险审查器只提供建议 RunUnsandboxed 使用已有的精确用户授权执行 用户授权 + ActionPolicyEngine 精确匹配 RunWithPermissionBypass 当前 Turn 选择跳过本来需要的交互，但仍保留精确绑定与审计 可信产品 policy adapter；只能替换 AskUser AskUser 缺少足够、明确的执行授权 用户 ReviseAction 当前动作过宽，Agent 应提出更小、更安全的动作 ActionPolicyEngine Block 命中确定性禁令、极高风险、审查失败或沙箱硬约束 确定性策略 AskUser 不是异常，也不等于系统“不够聪明”。它表示当前上下文不足以安全地替用户作决定。 权限不是一个开关 权限系统由六层相互独立的约束组成： 层 解决的问题 不负责什么 --- --- --- 动作解析 把工具参数、工作目录、解析后的路径、环境和来源变成精确动作 不批准执行 能力模型 描述动作需要的最小能力与作用范围 不判断用户意图 确定性规则 zeta-execpolicy 组合 Host / Organization / User / Workspace layer 并返回纯 effect 不签发 grant、不执行工具 最终 action policy zeta-action-policy 把 rule effect、exact grants、sandbox 与 reviewer 结果合成最终决定 不解析或持久化规则、不执行工具 Auto Review 根据标明信任来源的上下文给出风险建议 不能签发最终执行授权 持久化批准与执行 ConfigStore 保存精确用户规则；Core 保存一次性批准和副作用起点 不改变前面的安全判断 因此： - 沙箱负责强制执行边界，不负责用户批准； - Auto Review 是风险审查建议层，不是第二套权限系统； - 批准界面负责取得用户授权，不是策略引擎； - 工具或 MCP 服务器声称“只读”只能作为证据，不能成为执行授权； - Skill 的 allowed-tools 是提示，不是权限； - Plugin 声明的权限、Plugin 安装授权和单次工具调用批准是三个不同层次。 识别哪些能力 当前能力类型 CapabilityKind 定义八类能力。真正参与授权匹配的是“类型 + 作用范围”，不只是 下表中的名称。 能力 典型动作 需要特别说明的作用范围 --- --- --- 文件读取 FileRead 读取文件、搜索内容 目录、文件集合、是否越过工作区 文件写入 FileWrite 创建、编辑、删除或移动文件 精确路径、可写根、破坏性范围 启动进程 ProcessSpawn 启动本地程序或 Shell 命令 可执行文件、参数、工作目录和环境变量 网络访问 Network HTTP、下载、远端连接 主机、协议、端口、请求范围 使用凭证 CredentialUse 使用令牌、账号或密钥 凭证标识、目标服务、用途 外部修改 ExternalMutation 修改 GitHub、Linear、云资源等外部状态 服务、资源标识、操作类型 系统配置 SystemConfiguration 修改系统级设置或安装环境 系统资源、变更范围、恢复方式 用户界面 UserInterface 控制浏览器或桌面界面 应用、页面、交互目标 能力集合必须是完成动作所需的最小集合。把多个未来可能需要的能力预先合并，会扩大授权范围，也会 让批准说明失真。 一次动作如何获得执行权 当前权威决策顺序如下： ``text Agent 提出工具调用 → 主机解析精确动作、来源、能力集合和沙箱兼容性 → zeta-execpolicy：按 typed selector 求值 Host / Organization / User / Workspace rules → ActionPolicyEngine：映射 Deny / RequireSandbox / RequireApproval / AllowUnsandboxed → AllowUnsandboxed：签发绑定 rule、exec-policy revision、动作与能力的 RunExecPolicyGranted → 精确用户授权：匹配动作摘要、能力集合和策略版本 → 可用沙箱：RunSandboxed → Auto Review：Approve / ReviseAction / AskUser / Deny → 策略引擎 ActionPolicyEngine：RunAutoReviewed / ReviseAction / AskUser / Block → Core 持久化记录执行授权与 ToolExecutionStarted → 执行器按 Sandboxed 或 Unrestricted 授权执行 → 持久记录确定结果 ` 顺序本身就是安全契约： 1. 更严格 effect 始终胜出；Workspace layer 不能产生 AllowUnsandboxed； 2. historical exact grant 必须精确匹配，不能按工具名称、命令前缀或自然语言摘要复用；typed command-prefix rule 则是独立、显式、带 revision 的 policy 对象； 3. 风险审查器不能构造自动审查授权 AutoReviewGrant； 4. 执行器只消费显式的沙箱授权 Sandboxed policy 或无限制授权 Unrestricted； 5. 沙箱启动失败不得静默降级为无限制执行。 用户批准到底批准了什么 当前协议的即时用户决定仍只有两种；持久化规则是独立的 Config authority，不会改变 ApproveOnce： 决定 当前语义 --- --- 一次性批准 ApproveOnce 允许当前批准请求对应的当前工具调用执行一次 拒绝 Decline 拒绝当前请求；原工具调用以明确失败结束 一次性授权绑定以下标识： - RequestId； - ToolCallId； - 主机规范化后生成的动作摘要 ActionDigest； - 完整 CapabilitySet； - ActionPolicyRevision。 恢复中的批准也必须重新匹配准备执行的动作。只要路径、参数、环境、来源、能力集合或策略 版本发生安全相关变化，就不能沿用旧批准。 长期规则与一次性批准必须分开 当前已实现 User rule 的 typed 持久化、command prefix/network/capability/source/action selector、 Host/Organization/User/Workspace layer composition、semantic revision 和运行时重组。仍未实现： - “本次会话始终允许”； - “此项目始终允许”； - approval UI 中的“保存为长期规则”； - rule 到期时间和统一规则管理 UI； - Organization layer 的产品级远端分发 adapter； - 由 Agent 自行把一次批准升级为长期规则。 User rule 可以用 typed source、command prefix、network target 或 capability scope 授权；它不是历史 approval 的模糊复用。Workspace rule 只允许收紧。任何规则变更都会产生新的 exec-policy revision， 并进入新的 ActionPolicyRevision；旧 Turn 和旧 grant 不会静默获得更宽权限。 这些能力未来即使加入，也必须拥有独立、可审计的作用范围和撤销语义，不能改变 ApproveOnce 的含义。 受控内容来源不是长期执行批准 Desktop 外部 Agent 配置导入计划允许用户把明确选择的 Codex /.agents/skills、Claude /.claude/skills 等目录注册为可撤销的只读内容来源。当前 zeta-agent-import 只实现已知 路径的 metadata-only 检查和 AgentPathInspection，尚未实现 Desktop 确认与 Config apply。未来 apply 操作产生类型化配置和受控来源身份，不产生按工具、命令前缀或路径模式匹配的执行授权， 因此不属于“是，不再询问”或“此项目始终允许”。 注册后的读取只能发生在经过规范化和 containment 校验的窄来源根内；整个 /.codex、 /.claude 或用户主目录不会因此变成 Agent 可浏览范围，/.codex/auth.json 和包含 OAuth session/cache 的 /.claude.json 也不会进入当前发现计划。Skill 中的脚本、网络请求或其他 副作用仍按当前动作单独进入权限与沙箱判断。 该导入入口仅由 Desktop 提供；TUI 可以消费 App Server 已发布的统一 Skill catalog，但不能创建 或管理外部来源。完整产品边界见 zeta-desktop-architecture.md 和 skills.md。 沙箱在权限系统中的位置 本地执行的沙箱策略由文件系统约束和网络约束组成： 文件系统 含义 --- --- 只读 ReadOnly 可读取允许范围，不写入 工作区可写 WorkspaceWrite 只允许写入经过解析和验证的工作区根目录 完全访问 FullAccess 不以工作区文件边界约束动作 网络 含义 --- --- 禁止 Denied 平台后端必须实际阻止网络访问 允许 Allowed 动作可使用网络；仍不自动获得凭证使用或外部修改授权 工作区可写策略 WorkspaceWrite 只是约束描述，不能代替平台强制执行。对于要求受限执行的动作， 后端缺失、不支持相应策略或不能证明约束已生效时必须失败即关闭（fail closed）。 平台后端、支持矩阵和当前限制见 sandboxing.md。 沙箱拒绝后为什么不能直接重跑 “命令在沙箱里失败”不能推出“应该在沙箱外再试一次”。失败可能来自参数错误、程序错误、资源 不存在或部分副作用已经发生。 当前只允许一种受控升级路径： 1. 执行器返回类型化的沙箱拒绝结果； 2. 拒绝结果明确标记为可安全重试 SafeToRetry； 3. 长度受限的证据回到策略审查； 4. 策略重新判断； 5. 获得新的明确执行授权后，最多进行一次沙箱外重试。 以下情况不得自动重放： - 普通非零退出； - 沙箱拒绝前可能已产生副作用； - 工具已经开始，但崩溃或恢复后结果未知； - 证据无法证明失败来自沙箱强制执行； - 新动作与原动作摘要或能力集合不一致。 “不知道是否成功”必须保留为未知执行结果（unknown outcome），而不是假设失败后再次执行。 谁负责什么 组件 当前责任 明确不拥有 --- --- --- 主机与工具适配器 解析精确动作、来源、最小能力和沙箱兼容性 最终批准 zeta-execpolicy typed selector、layer validation、effect precedence、semantic revision 与纯求值 最终 grant、Tool 执行、配置 I/O zeta-action-policy effect 映射、exact grant、sandbox、风险门槛和最终类型化结果 规则解析/持久化、工具执行、UI zeta-config User rule 的 typed TOML mutation/persistence；Workspace restriction 的 strict-read intent 规则求值、最终执行授权 zeta-auto-review 生成受 schema 约束的风险审查结论 覆盖策略、签发授权 Core 的 ToolScheduler 持久化批准、一次性授权、执行生命周期和恢复语义 操作系统沙箱强制执行 zeta-exec 与工具执行器 消费明确执行授权并返回类型化结果 自行提权 zeta-sandboxing 选择并启动平台强制执行后端 批准、重试和持久化 App Server、CLI 与 Desktop 暴露权限 CRUD、展示动作、作用范围、风险和用户选择 改写权威策略 任何一层同时承担“描述动作、判断风险、签发权限、执行副作用”中的多项职责，都会削弱审计边界。 当前实现状态 能力 状态 边界 --- --- --- 精确绑定动作、能力集合和策略版本 当前已实现 规范字节的完整性仍依赖主机动作解析器 Host/Organization/User/Workspace typed layer 与 semantic revision 当前已实现 Organization 的产品分发 adapter 尚未接入 source、command prefix、network、capability、action selector 当前已实现 selector 只消费 host-materialized typed fields User rule 持久化与 Workspace 只收紧规则 当前已实现 统一规则编辑 UI、expiry 尚未实现 exec-policy exact durable execution authority 当前已实现 绑定 rule ID、exec-policy revision、action、capabilities 与 Tool Call Auto Review 类型化建议与风险门槛 当前已实现 当前是单次审查，没有分层审查或多审查器协作 持久化批准请求与 ApproveOnce / Decline 当前已实现 各客户端的呈现体验尚未完全统一 TUI 的 Ask / Auto Review / Bypass per-Turn 模式 当前已实现 模式在提交时冻结；review model 当前在 App Server 启动时解析 副作用前记录工具执行开始 当前已实现 崩溃后的未知结果不自动重放 类型化沙箱拒绝再审查 当前已实现 最多一次；真实平台拒绝样本仍有限 macOS、Linux 和 Windows 平台沙箱 部分具备 具体支持和集成验收以沙箱文档为准 rule expiry 尚未完成 需要独立时间与撤销语义 面向用户的统一权限解释器与历史审计页 尚未完成 协议和 Core 契约可作为后续 UI 基础 计划方向：让权限更容易理解，而不是更模糊 后续产品演进应优先改善解释和管理能力： 1. 批准界面用自然语言展示“动作、目标、能力、作用范围、来源、沙箱差异和风险理由”； 2. 将“为什么这次询问”和“为什么不能在沙箱中完成”分开解释； 3. 提供可查询的权限决定历史，但对密钥和敏感参数做结构化脱敏； 4. 为已实现的 User/Workspace rules 提供可解释的管理 UI，并为 Organization 分发和 expiry 增加 独立 adapter； 5. 用真实沙箱拒绝、危险自动批准率和人工标签评估 Auto Review，而不是只统计减少了多少弹窗； 6. 各客户端共享生成的协议和相同决定语义，不各自创造权限模式。 长期规则不是一次性批准的“快捷保存”。它已经是独立、typed、revision-bound 的策略对象；未来 UI 若提供保存入口，也必须生成并展示该对象，而不能偷偷复用历史点击。 这也是开发方法 权限文档不只是功能介绍，它可以作为契约驱动开发（contract-driven development）的入口： 1. 先写用户语义：用户看见什么、批准什么、拒绝后发生什么； 2. 再划分授权职责：谁能建议、谁能签发、谁能执行、谁只能展示； 3. 定义不变量：优先拒绝、精确绑定、失败即关闭、未知结果不重放； 4. 落到类型：使用枚举、newtype 和类型化分支表达，不使用含糊的布尔值； 5. 映射持久化边界：副作用前记录什么，恢复时依据什么继续； 6. 派生测试：每条重要语义都应有单元测试、集成测试或平台验收证据； 7. 标注状态：当前实现、当前限制和计划设计永远分开。 文档先行不代表“文档说了就算实现”。正确的关系是： `text 产品语义 → 权威文档 → 类型化契约 → 责任方实现 → 测试与验收 → 文档中的当前状态 ` 如果代码无法证明文档中的当前行为，应修正实现或把文档降为计划设计；不能用模糊措辞掩盖 二者不一致。 审查清单 修改权限、工具、Plugin、MCP、沙箱或批准界面时，至少确认： - 动作摘要是否覆盖所有安全相关输入； - 能力是否最小且作用范围明确； - 确定性规则是否仍早于用户授权和风险审查器； - 风险审查器是否仍只能建议、不能签发执行授权； - 批准是否绑定精确请求、工具调用、能力集合和策略版本； - 副作用前是否已持久化记录执行开始； - 未知执行结果是否不会自动重放； - 沙箱失败是否不会静默降级； - 新 UI 选项是否在协议和 Core 中有唯一、明确的权威语义； - 当前实现与计划设计是否仍清楚分离。 确定性规则语言见 zeta-execpolicy README；最终决策顺序与 grant binding 见 zeta-action-policy` README。"
  },
  {
    "slug": "auto-review",
    "sourcePath": "docs/auto-review.md",
    "title": "Auto 审查：产品语义、系统边界与演进",
    "description": "Auto Review 只在确定性规则无法独立判断时提供风险建议；它不能覆盖安全规则，也不能自行签发 执行授权。",
    "group": "安全与权限",
    "markdown": "> 文档所有权：本文件是 Auto Review 跨 crate 风险判断语义和演进方向的权威文档。\n>\n> 文档状态：当前架构 + 明确的未来演进。\n\n## 快速理解\n\nAuto Review 只在确定性规则无法独立判断时提供风险建议；它不能覆盖安全规则，也不能自行签发\n执行授权。\n\n| 用户或开发者会遇到的情况 | 系统行为 | 最终责任方 |\n| --- | --- | --- |\n| 动作明确安全且沙箱足够 | 直接选择受限执行，不调用风险审查器 | 权限策略 |\n| 动作命中确定性禁止规则 | 直接阻止，模型不能覆盖 | 权限策略 |\n| 风险依赖用户意图和目标范围 | 请求 Auto Review 给出批准、修改、询问或拒绝建议 | 风险审查器给建议，策略引擎作决定 |\n| 建议不完整、越权或解析失败 | 失败即关闭，转为阻止或询问用户 | 策略引擎 |\n| 沙箱报告结构化能力不足 | 只有确认未产生副作用且可安全重试时才重新审查 | Core 与权限策略 |\n| 动作已开始但结果未知 | 不自动重放或提权重试 | Core 恢复逻辑 |\n\n## 1. 决策摘要\n\nZeta 把 Auto Review 定义为“受确定性策略约束的风险审查建议层”，而不是第二套权限系统。\n\n核心决策：\n\n- 主机先解析精确动作、来源、最小能力和沙箱兼容性；\n- `zeta-execpolicy` 对 Host / Organization / User / Workspace typed rules 做纯求值；更严格 effect\n  优先，Workspace 不能产生 `AllowUnsandboxed`；该结果和 exact `UserAllowlist` 都优先于 LLM；\n- `UserAllowlist` 只接受绑定动作摘要、完整能力集合与策略版本的精确沙箱外授权，不接受工具名称\n  或命令前缀；\n- 风险审查器只给出批准 `Approve`、修改动作 `ReviseAction`、询问用户 `AskUser` 或拒绝 `Deny`\n  四种建议；\n- 策略引擎 `ActionPolicyEngine` 是唯一把建议转成执行决定并签发授权的责任方；\n- 自动授权绑定审查结论、动作摘要、完整能力集合和策略版本；\n- 用户意图与证据必须带有明确的信任边界，仓库、工具和 Agent 内容默认不可信；\n- 审查失败必须失败即关闭；\n- 工具跨过副作用边界前必须持久化记录执行授权；\n- 已开始但结果未知的动作不得由风险审查器自动重放；\n- 确认且可安全重放的沙箱拒绝可以携带结构化执行证据回到风险审查器；普通命令失败、\n  部分副作用可能发生或结果未知时不得自动提权重跑。\n\n这套设计追求的不是“尽量少弹窗”，而是在危险自动批准率接近零的前提下，减少可确定安全场景的\n无效打断。\n\n## 2. 产品问题\n\n纯沙箱模式无法覆盖所有真实 Agent 动作：\n\n- 某些工作需要网络访问、外部服务修改、凭证或 UI 能力；\n- 某些工具本身不适用本地进程沙箱；\n- 用户经常明确请求一个有副作用但合理的动作；\n- 动作的安全性依赖用户意图、目标作用范围、来源和上下文，而不只依赖工具名称。\n\n只使用静态授权列表会过度阻塞；把权限直接交给 LLM 又不可接受。Auto Review 位于两者之间：\n用模型理解上下文和风险，用确定性策略保留最终授权权。\n\nAuto Review 不解决：\n\n- 工具是否正确实现操作系统强制执行；\n- 凭证是否可用；\n- 外部服务是否接受请求；\n- 动作执行后的业务成功与否；\n- 已开始动作的严格单次执行保证；\n- 用户组织的完整策略语言。\n\n## 3. 系统所有权\n\n| 组件 | 拥有 | 不拥有 |\n| --- | --- | --- |\n| 工具主机 | 精确解析动作、来源、最小能力和审查证据 | 最终策略决定 |\n| `zeta-execpolicy` | typed rules、layer merge、effect precedence、semantic revision 与纯求值 | 最终 grant、LLM、配置 I/O |\n| `zeta-action-policy` | rule effect 映射、exact grants、风险审查接口、风险与授权门槛和最终决定 | 规则持久化、LLM 提示词与模型提供方 |\n| `zeta-auto-review` | 审查提示词、严格响应和审查结论绑定 | 签发授权、执行工具、批准界面 |\n| App Server | 配置安全点、审查模型解析和模型提供方适配器 | 审查建议的授权语义 |\n| Core | 工具调度、类型化批准、持久化执行起点与提权、安全重试门槛、恢复和拒绝断路器 | 模型风险判断、操作系统沙箱 |\n| 工具执行器与沙箱 | 执行选定授权、返回结构化结果、分类平台拒绝和隔离资源 | 放宽能力或重新解释用户意图 |\n| Desktop、CLI 与 TUI | 展示批准和风险信息、收集用户决定 | 自行创建授权 |\n\n关键依赖方向：\n\n```text\n工具主机 ──准备完成的动作与证据──► Core\nCore ──审查请求──► zeta-action-policy ──纯规则求值──► zeta-execpolicy\nzeta-action-policy ──建议调用──► zeta-auto-review\nzeta-auto-review ──模型请求──► App Server 审查适配器\nzeta-action-policy ──最终决定──► Core\nCore ──精确执行授权──► 工具执行器与沙箱\n```\n\n`zeta-action-policy` 不能依赖 `zeta-auto-review`。它依赖自己定义的风险审查接口\n`ActionClassifier`，因此未来可以替换模型实现、关闭 Auto Review 或增加确定性审查器，而不改变\n权限系统的最终授权责任。\n\n## 4. 端到端决策模型\n\n### 4.1 当前：执行前决策\n\n```mermaid\nflowchart TD\n    A[\"Agent 提出工具动作\"] --> B[\"主机解析精确动作<br/>作用范围 / 来源 / 能力\"]\n    B --> C{\"确定性策略\"}\n    C -- \"拒绝\" --> X[\"阻止\"]\n    C -- \"typed allow rule\" --> PG[\"ActionPolicyEngine 签发 exact ExecPolicy grant\"]\n    C -- \"匹配精确用户授权\" --> G[\"使用精确沙箱外授权执行\"]\n    C -- \"沙箱可用\" --> S[\"在沙箱中执行\"]\n    C -- \"需要上下文判断\" --> R[\"Auto Review 风险审查器\"]\n    R --> V{\"主机验证建议\"}\n    V -- \"符合自动批准条件\" --> AG[\"ActionPolicyEngine 签发 AutoReviewGrant\"]\n    V -- \"存在更安全路径\" --> SA[\"返回结构化 ReviseAction\"]\n    V -- \"授权含糊\" --> U[\"询问用户\"]\n    V -- \"危险或极高风险\" --> X\n    V -- \"审查失败\" --> F[\"按明确失败策略 Block 或 AskUser\"]\n    U -- \"一次性批准\" --> UG[\"创建绑定请求与调用的用户授权\"]\n    U -- \"拒绝\" --> D[\"记录被拒绝的工具结果\"]\n    AG --> E[\"持久化记录 ToolExecutionStarted\"]\n    PG --> E\n    UG --> E\n    G --> E\n    S --> E\n    E --> T[\"工具执行器落实选定执行授权\"]\n```\n\n顺序本身是安全契约：\n\n1. 精确确定性规则不得被模型覆盖；\n2. 内置强制沙箱规则不得被用户授权列表覆盖；\n3. 用户授权必须显式表示沙箱外执行权，不能由普通命令授权列表静默生成；\n4. 沙箱能够满足动作时，不需要风险审查器扩权；\n5. 风险审查建议必须重新经过主机不变量验证；\n6. 授权必须绑定当前动作，而不是绑定工具名称或自然语言摘要；\n7. 持久化执行起点必须先于副作用。\n\n### 4.2 当前：沙箱拒绝回流\n\n为了减少沙箱权限不足造成的人工中断，执行前决策后有一条严格受限的失败回流：\n\n```text\n沙箱内执行\n├─ 成功 → 完成\n├─ 普通命令失败 → 返回 Agent\n├─ 超时或信号终止 → 返回 Agent\n└─ 已确认的沙箱拒绝\n   ├─ 可安全重放 → 带拒绝证据重新审查\n   └─ 可能已有部分副作用或结果未知 → 不自动重放\n\n重新审查\n└─ 风险审查建议 → ActionPolicyEngine 校验\n   ├─ 符合 Approve 条件 → 使用精确授权重试\n   ├─ ReviseAction → Agent 提出更安全的动作\n   ├─ AskUser → 请求用户批准\n   └─ Deny → Block\n```\n\n这条回流只适用于平台后端能够结构化确认的沙箱强制拒绝。Core 和风险审查器不能从任意标准错误\n输出、非零退出码或自然语言猜测沙箱拒绝；Seatbelt 与 Bubblewrap 后端只对各自认识的强制执行或\n设置特征产生类型化分类。回流请求必须继续绑定原动作摘要、能力集合、策略版本与工具调用，并携带\n长度受限且不含密钥的拒绝证据。\n\n是否自动重试是 Core 的持久化执行决定，不是风险审查建议的隐含效果。如果第一次尝试可能已写入\n文件、发出请求或产生其他副作用，即使风险审查器返回批准 `Approve`，Core 也不得自动重放原调用；\n它只能记录终止或未知结果，并让 Agent 修改动作或请求用户决定。\n\n`zeta-protocol::SandboxDenialOutput` 保存原因、退出状态、标准输出、标准错误输出、聚合输出和\n重放安全性。Core 从中截取最多 500 字符的原因和 2,000 字符的聚合输出给风险审查器；原结构化\n拒绝与第二次执行授权一起持久化写入 `ThreadEvent::ToolExecutionEscalated`。第二次执行只允许\n一次，不形成循环。\n\n## 5. 风险与用户授权矩阵\n\n风险审查器对批准 `Approve` 同时输出风险等级和用户授权状态。策略引擎 `ActionPolicyEngine` 使用固定\n矩阵：\n\n| 风险 | 明确授权 | 隐含授权 | 缺少或含糊 |\n| --- | --- | --- | --- |\n| 低 | 自动批准 | 自动批准 | 询问用户 `AskUser` |\n| 中 | 自动批准 | 自动批准 | 询问用户 `AskUser` |\n| 高 | 自动批准 | 询问用户 `AskUser` | 询问用户 `AskUser` |\n| 极高 | 阻止 `Block` | 阻止 `Block` | 阻止 `Block` |\n\n这里的“自动批准”仍然要求：\n\n- 建议的能力集合与已解析动作完全一致；\n- 审查结论的动作摘要与策略版本精确匹配；\n- 策略没有确定性拒绝或强制沙箱冲突；\n- Core 在执行前验证授权与当前工具调用的绑定。\n\n矩阵是当前策略，不是永恒产品规则。调整它需要安全评测、用户交互影响分析和策略回归测试，不能\n只修改提示词措辞。\n\n## 6. 四种用户可见结果\n\n### `Approve`\n\n表示风险审查器认为精确动作与用户意图相符。它不会直接执行；只有符合上一节矩阵时，策略引擎\n`ActionPolicyEngine` 才签发自动审查授权 `AutoReviewGrant`。\n\n用户体验目标：明确、低到可控风险的动作不产生重复确认。\n\n### `ReviseAction`\n\n表示目标可以继续，但当前动作的能力或作用范围过宽。返回值携带最大能力集合\n`maximum_capabilities`，父 Agent 必须选择实质上更安全的动作，而不是换一种命令重试同一危险\n动作。\n\n用户体验目标：优先产生安全进展，而不是简单拒绝或把所有判断推给用户。\n\n### `AskUser`\n\n用于授权缺失、目标含糊或高风险动作没有明确用户授权。批准请求绑定动作摘要、能力集合和策略\n版本；批准只对当前工具调用生效。\n\n用户体验目标：问题应描述具体副作用与作用范围，而不是只显示“需要权限”。\n\n### `Deny`\n\n用于极高风险、破坏性操作、数据外泄、凭证探测或规避策略。拒绝 `Deny` 作为结构化工具失败返回，\n要求 Agent 不得通过间接命令或替代工具绕过。\n\n用户体验目标：停止危险路径，同时在可能时允许选择不同的安全目标。\n\n## 7. 上下文、信任与隐私\n\n风险审查器需要理解用户意图，但“更多上下文”并不天然更安全。Zeta 使用证据代理，而不是复制\n完整对话记录：\n\n- 用户直接指令标记为可信用户意图；\n- 主机解析的动作与来源标记为可信主机元数据；\n- Agent 消息、计划、仓库文件和工具结果标记为不可信内容；\n- 证据限制条目数和字符数；\n- 凭证、密钥和无关工具输出必须在进入风险审查器前移除。\n\n信任标签只说明来源，不能证明内容真实。例如通过可信文件系统适配器读取的 README，内容仍属于\n不可信仓库内容。\n\n当前边界：\n\n- Core 选择当前 Turn 最近的用户直接消息；\n- 工具主机可提供动作专用、只读且不含密钥的证据；\n- 风险审查器没有工具、凭证、修改能力或可变 Agent 记忆；\n- 动作与上下文中的提示词注入只能作为数据，不能改变风险审查策略。\n\n未来若引入记忆、组织策略或外部信誉证据，必须定义独立来源和优先级，不能把它们拼成无类型\n提示词。\n\n## 8. 失败、重试与恢复原则\n\nAuto Review 的失败模式必须显式：\n\n- 模型不可用、超时、取消、JSON 格式错误和能力不匹配都不能产生授权；\n- 主机使用 `ReviewFailurePolicy::Block` 或 `AskUser`，不存在失败即放行；\n- 沙箱内进程返回非零退出码，不代表应该自动进行沙箱外重试；\n- 只有执行器结构化确认且标记为可安全重试 `SafeToRetry` 的沙箱强制拒绝才能进入二次审查；\n- 沙箱拒绝前可能已经完成部分副作用；不能证明可安全重放时，即使获得新授权也不得自动重放原\n  工具调用；\n- 动作、能力集合、工作目录、环境、来源或策略版本改变后必须重新审查；\n- 工具已持久化记录执行开始但没有终止结果时，结果视为未知，不自动重放。\n\n风险审查拒绝会作为结构化反馈返回 Agent。单个 Turn 连续 3 次，或最近 50 个工具结果中累计\n10 次审查拒绝，会触发断路器并中断该 Turn。这是为了防止模型通过不断改写命令试探策略边界，\n不是限制用户重新发起一个明确的新请求。\n\n## 9. 审计与可观测性\n\n一次可审计的执行至少关联：\n\n- 动作摘要与工具调用标识；\n- 策略版本、审查协议版本和审查结论 ID；\n- 审查建议与最终决定；\n- 匹配的确定性规则、现有授权或用户批准标识；\n- 持久化执行授权；\n- 沙箱拒绝的原始结构化输出、重放安全性与持久化提权授权；\n- 执行是否已开始，以及终止或未知结果。\n\n审查结论 ID 对规范化建议建立稳定身份，因此模型 JSON 的空格变化不会制造新结论；审查协议或\n策略语义变化必须使用新版本。\n\n安全指标按优先级排序：\n\n1. 危险动作错误自动批准率；\n2. 提示词注入与规避策略通过率；\n3. 不必要的询问用户比例；\n4. 建议、风险与授权准确率；\n5. `ReviseAction` 后安全完成目标的比例；\n6. 不同模型与审查协议版本的一致性和漂移。\n\n## 10. 当前实现状态\n\n已经实现：\n\n- 确定性策略优先的 `ActionPolicyEngine` 决策顺序；\n- 四种风险审查建议；\n- 建议与能力集合两层严格 JSON 解析，以及策略拥有的精确或子集验证；\n- 64 KiB 序列化输入上限，以及适配器和风险审查器两层 16 KiB 响应上限；\n- 提示词、响应模式与审查协议版本的原子绑定；\n- 风险 × 用户授权自动批准矩阵；\n- 绑定审查结论的 `AutoReviewGrant` 与绑定工具调用的执行授权；\n- 长度受限的用户意图和带信任标签的证据；\n- 类型化持久化批准、执行起点标记和未知结果不重放；\n- 协议拥有的结构化进程与沙箱结果；\n- Seatbelt 与 Bubblewrap 后端专用的拒绝分类；\n- 可安全重试的沙箱拒绝二次审查、精确授权、持久化提权与单次重试；\n- 可能已有副作用时禁止重放；\n- 结构化的安全动作建议、拒绝反馈与单 Turn 断路器；\n- App Server 不可变且不带工具的审查模型适配器；\n- 合成种子样本集和 Cargo、Bazel 离线契约测试。\n\n当前仍有限：\n\n- 风险审查器是单次模型调用，没有分层审查或多审查器协作；\n- 沙箱拒绝分类器覆盖 macOS Seatbelt、Linux Bubblewrap 的已知特征，以及 Windows AppContainer\n  辅助程序可信的启动前强制执行标记；真实平台拒绝样本集\n  仍不足；\n- 沙箱拒绝后的询问用户 `AskUser` 当前终止原工具调用，由 Agent 发起新的批准路径，不在同一个\n  已开始调用上建立可恢复的批准等待；\n- 提示词策略是编译期常量；\n- 没有真实模型基准运行器、影子模式遥测或人工标注流水线；\n- 没有组织策略引导；\n- 样本集规模小，主要用于证明契约与建立回归入口，不能代表生产分布；\n- 没有足够数据支持微调决策。\n\n## 11. 演进方向\n\n### 近期：建立可测量性\n\n- 实现显式模型评测运行器，不进入默认 CI；\n- 按模型和审查协议版本输出安全指标；\n- 扩充真实缺陷、人工改判和策略回归的匿名合成复现；\n- 为错误自动批准设置发布门槛；\n- 先运行影子模式，再讨论扩大自动批准覆盖率；\n- 用真实平台拒绝样本评估分类器的误报与漏报，并扩展 Windows 后端。\n\n### 中期：提高可控性\n\n- 将私有版本化响应模式导出为评测和工具可消费的产物；\n- 增加组织策略引导，并与不可信上下文分层；\n- 改善询问原因和 `ReviseAction` 的可执行性评测；\n- 增加风险审查器校准与高风险二次检查，但保持确定性授权责任。\n\n### 潜在方向：专用模型\n\n只有在积累足够高质量、经过隐私审查的标签后，才评估：\n\n- 小型专用风险分类器；\n- 模型微调；\n- 分层模型路由；\n- 多风险审查器意见不一致时的升级处理。\n\n训练模型不是评测的前置条件。没有可靠评测集时训练只会把未知错误固化进模型。\n\n## 12. 长期不变量\n\n无论未来使用何种模型或数据，以下边界不变：\n\n- 风险审查器不成为能力授权方；\n- 模型输出不能覆盖主机解析的动作标识；\n- 不可信内容不能改变风险审查策略；\n- 审查失败不产生授权；\n- 高风险或极高风险的放宽必须经过确定性策略和可审计版本；\n- 未知执行结果不由风险审查器自动重放；\n- 凭证和密钥不进入评测样本集或普通审查上下文。\n\nAuto Review 的实现细节、错误语义和修改指南见\n[`zeta-auto-review` README](../zeta-rs/auto-review/README.md)；确定性决策引擎与授权实现见\n[`zeta-action-policy` README](../zeta-rs/action-policy/README.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-决策摘要",
        "title": "1. 决策摘要"
      },
      {
        "depth": 2,
        "id": "2-产品问题",
        "title": "2. 产品问题"
      },
      {
        "depth": 2,
        "id": "3-系统所有权",
        "title": "3. 系统所有权"
      },
      {
        "depth": 2,
        "id": "4-端到端决策模型",
        "title": "4. 端到端决策模型"
      },
      {
        "depth": 3,
        "id": "41-当前执行前决策",
        "title": "4.1 当前：执行前决策"
      },
      {
        "depth": 3,
        "id": "42-当前沙箱拒绝回流",
        "title": "4.2 当前：沙箱拒绝回流"
      },
      {
        "depth": 2,
        "id": "5-风险与用户授权矩阵",
        "title": "5. 风险与用户授权矩阵"
      },
      {
        "depth": 2,
        "id": "6-四种用户可见结果",
        "title": "6. 四种用户可见结果"
      },
      {
        "depth": 3,
        "id": "approve",
        "title": "Approve"
      },
      {
        "depth": 3,
        "id": "reviseaction",
        "title": "ReviseAction"
      },
      {
        "depth": 3,
        "id": "askuser",
        "title": "AskUser"
      },
      {
        "depth": 3,
        "id": "deny",
        "title": "Deny"
      },
      {
        "depth": 2,
        "id": "7-上下文信任与隐私",
        "title": "7. 上下文、信任与隐私"
      },
      {
        "depth": 2,
        "id": "8-失败重试与恢复原则",
        "title": "8. 失败、重试与恢复原则"
      },
      {
        "depth": 2,
        "id": "9-审计与可观测性",
        "title": "9. 审计与可观测性"
      },
      {
        "depth": 2,
        "id": "10-当前实现状态",
        "title": "10. 当前实现状态"
      },
      {
        "depth": 2,
        "id": "11-演进方向",
        "title": "11. 演进方向"
      },
      {
        "depth": 3,
        "id": "近期建立可测量性",
        "title": "近期：建立可测量性"
      },
      {
        "depth": 3,
        "id": "中期提高可控性",
        "title": "中期：提高可控性"
      },
      {
        "depth": 3,
        "id": "潜在方向专用模型",
        "title": "潜在方向：专用模型"
      },
      {
        "depth": 2,
        "id": "12-长期不变量",
        "title": "12. 长期不变量"
      }
    ],
    "searchText": " 文档所有权：本文件是 Auto Review 跨 crate 风险判断语义和演进方向的权威文档。 文档状态：当前架构 + 明确的未来演进。 快速理解 Auto Review 只在确定性规则无法独立判断时提供风险建议；它不能覆盖安全规则，也不能自行签发 执行授权。 用户或开发者会遇到的情况 系统行为 最终责任方 --- --- --- 动作明确安全且沙箱足够 直接选择受限执行，不调用风险审查器 权限策略 动作命中确定性禁止规则 直接阻止，模型不能覆盖 权限策略 风险依赖用户意图和目标范围 请求 Auto Review 给出批准、修改、询问或拒绝建议 风险审查器给建议，策略引擎作决定 建议不完整、越权或解析失败 失败即关闭，转为阻止或询问用户 策略引擎 沙箱报告结构化能力不足 只有确认未产生副作用且可安全重试时才重新审查 Core 与权限策略 动作已开始但结果未知 不自动重放或提权重试 Core 恢复逻辑 1. 决策摘要 Zeta 把 Auto Review 定义为“受确定性策略约束的风险审查建议层”，而不是第二套权限系统。 核心决策： - 主机先解析精确动作、来源、最小能力和沙箱兼容性； - zeta-execpolicy 对 Host / Organization / User / Workspace typed rules 做纯求值；更严格 effect 优先，Workspace 不能产生 AllowUnsandboxed；该结果和 exact UserAllowlist 都优先于 LLM； - UserAllowlist 只接受绑定动作摘要、完整能力集合与策略版本的精确沙箱外授权，不接受工具名称 或命令前缀； - 风险审查器只给出批准 Approve、修改动作 ReviseAction、询问用户 AskUser 或拒绝 Deny 四种建议； - 策略引擎 ActionPolicyEngine 是唯一把建议转成执行决定并签发授权的责任方； - 自动授权绑定审查结论、动作摘要、完整能力集合和策略版本； - 用户意图与证据必须带有明确的信任边界，仓库、工具和 Agent 内容默认不可信； - 审查失败必须失败即关闭； - 工具跨过副作用边界前必须持久化记录执行授权； - 已开始但结果未知的动作不得由风险审查器自动重放； - 确认且可安全重放的沙箱拒绝可以携带结构化执行证据回到风险审查器；普通命令失败、 部分副作用可能发生或结果未知时不得自动提权重跑。 这套设计追求的不是“尽量少弹窗”，而是在危险自动批准率接近零的前提下，减少可确定安全场景的 无效打断。 2. 产品问题 纯沙箱模式无法覆盖所有真实 Agent 动作： - 某些工作需要网络访问、外部服务修改、凭证或 UI 能力； - 某些工具本身不适用本地进程沙箱； - 用户经常明确请求一个有副作用但合理的动作； - 动作的安全性依赖用户意图、目标作用范围、来源和上下文，而不只依赖工具名称。 只使用静态授权列表会过度阻塞；把权限直接交给 LLM 又不可接受。Auto Review 位于两者之间： 用模型理解上下文和风险，用确定性策略保留最终授权权。 Auto Review 不解决： - 工具是否正确实现操作系统强制执行； - 凭证是否可用； - 外部服务是否接受请求； - 动作执行后的业务成功与否； - 已开始动作的严格单次执行保证； - 用户组织的完整策略语言。 3. 系统所有权 组件 拥有 不拥有 --- --- --- 工具主机 精确解析动作、来源、最小能力和审查证据 最终策略决定 zeta-execpolicy typed rules、layer merge、effect precedence、semantic revision 与纯求值 最终 grant、LLM、配置 I/O zeta-action-policy rule effect 映射、exact grants、风险审查接口、风险与授权门槛和最终决定 规则持久化、LLM 提示词与模型提供方 zeta-auto-review 审查提示词、严格响应和审查结论绑定 签发授权、执行工具、批准界面 App Server 配置安全点、审查模型解析和模型提供方适配器 审查建议的授权语义 Core 工具调度、类型化批准、持久化执行起点与提权、安全重试门槛、恢复和拒绝断路器 模型风险判断、操作系统沙箱 工具执行器与沙箱 执行选定授权、返回结构化结果、分类平台拒绝和隔离资源 放宽能力或重新解释用户意图 Desktop、CLI 与 TUI 展示批准和风险信息、收集用户决定 自行创建授权 关键依赖方向： ``text 工具主机 ──准备完成的动作与证据──► Core Core ──审查请求──► zeta-action-policy ──纯规则求值──► zeta-execpolicy zeta-action-policy ──建议调用──► zeta-auto-review zeta-auto-review ──模型请求──► App Server 审查适配器 zeta-action-policy ──最终决定──► Core Core ──精确执行授权──► 工具执行器与沙箱 ` zeta-action-policy 不能依赖 zeta-auto-review。它依赖自己定义的风险审查接口 ActionClassifier，因此未来可以替换模型实现、关闭 Auto Review 或增加确定性审查器，而不改变 权限系统的最终授权责任。 4. 端到端决策模型 4.1 当前：执行前决策 `mermaid flowchart TD A \"Agent 提出工具动作\" -- B \"主机解析精确动作<br/ 作用范围 / 来源 / 能力\" B -- C \"确定性策略\" C -- \"拒绝\" -- X \"阻止\" C -- \"typed allow rule\" -- PG \"ActionPolicyEngine 签发 exact ExecPolicy grant\" C -- \"匹配精确用户授权\" -- G \"使用精确沙箱外授权执行\" C -- \"沙箱可用\" -- S \"在沙箱中执行\" C -- \"需要上下文判断\" -- R \"Auto Review 风险审查器\" R -- V \"主机验证建议\" V -- \"符合自动批准条件\" -- AG \"ActionPolicyEngine 签发 AutoReviewGrant\" V -- \"存在更安全路径\" -- SA \"返回结构化 ReviseAction\" V -- \"授权含糊\" -- U \"询问用户\" V -- \"危险或极高风险\" -- X V -- \"审查失败\" -- F \"按明确失败策略 Block 或 AskUser\" U -- \"一次性批准\" -- UG \"创建绑定请求与调用的用户授权\" U -- \"拒绝\" -- D \"记录被拒绝的工具结果\" AG -- E \"持久化记录 ToolExecutionStarted\" PG -- E UG -- E G -- E S -- E E -- T \"工具执行器落实选定执行授权\" ` 顺序本身是安全契约： 1. 精确确定性规则不得被模型覆盖； 2. 内置强制沙箱规则不得被用户授权列表覆盖； 3. 用户授权必须显式表示沙箱外执行权，不能由普通命令授权列表静默生成； 4. 沙箱能够满足动作时，不需要风险审查器扩权； 5. 风险审查建议必须重新经过主机不变量验证； 6. 授权必须绑定当前动作，而不是绑定工具名称或自然语言摘要； 7. 持久化执行起点必须先于副作用。 4.2 当前：沙箱拒绝回流 为了减少沙箱权限不足造成的人工中断，执行前决策后有一条严格受限的失败回流： `text 沙箱内执行 ├─ 成功 → 完成 ├─ 普通命令失败 → 返回 Agent ├─ 超时或信号终止 → 返回 Agent └─ 已确认的沙箱拒绝 ├─ 可安全重放 → 带拒绝证据重新审查 └─ 可能已有部分副作用或结果未知 → 不自动重放 重新审查 └─ 风险审查建议 → ActionPolicyEngine 校验 ├─ 符合 Approve 条件 → 使用精确授权重试 ├─ ReviseAction → Agent 提出更安全的动作 ├─ AskUser → 请求用户批准 └─ Deny → Block ` 这条回流只适用于平台后端能够结构化确认的沙箱强制拒绝。Core 和风险审查器不能从任意标准错误 输出、非零退出码或自然语言猜测沙箱拒绝；Seatbelt 与 Bubblewrap 后端只对各自认识的强制执行或 设置特征产生类型化分类。回流请求必须继续绑定原动作摘要、能力集合、策略版本与工具调用，并携带 长度受限且不含密钥的拒绝证据。 是否自动重试是 Core 的持久化执行决定，不是风险审查建议的隐含效果。如果第一次尝试可能已写入 文件、发出请求或产生其他副作用，即使风险审查器返回批准 Approve，Core 也不得自动重放原调用； 它只能记录终止或未知结果，并让 Agent 修改动作或请求用户决定。 zeta-protocol::SandboxDenialOutput 保存原因、退出状态、标准输出、标准错误输出、聚合输出和 重放安全性。Core 从中截取最多 500 字符的原因和 2,000 字符的聚合输出给风险审查器；原结构化 拒绝与第二次执行授权一起持久化写入 ThreadEvent::ToolExecutionEscalated。第二次执行只允许 一次，不形成循环。 5. 风险与用户授权矩阵 风险审查器对批准 Approve 同时输出风险等级和用户授权状态。策略引擎 ActionPolicyEngine 使用固定 矩阵： 风险 明确授权 隐含授权 缺少或含糊 --- --- --- --- 低 自动批准 自动批准 询问用户 AskUser 中 自动批准 自动批准 询问用户 AskUser 高 自动批准 询问用户 AskUser 询问用户 AskUser 极高 阻止 Block 阻止 Block 阻止 Block 这里的“自动批准”仍然要求： - 建议的能力集合与已解析动作完全一致； - 审查结论的动作摘要与策略版本精确匹配； - 策略没有确定性拒绝或强制沙箱冲突； - Core 在执行前验证授权与当前工具调用的绑定。 矩阵是当前策略，不是永恒产品规则。调整它需要安全评测、用户交互影响分析和策略回归测试，不能 只修改提示词措辞。 6. 四种用户可见结果 Approve 表示风险审查器认为精确动作与用户意图相符。它不会直接执行；只有符合上一节矩阵时，策略引擎 ActionPolicyEngine 才签发自动审查授权 AutoReviewGrant。 用户体验目标：明确、低到可控风险的动作不产生重复确认。 ReviseAction 表示目标可以继续，但当前动作的能力或作用范围过宽。返回值携带最大能力集合 maximumcapabilities，父 Agent 必须选择实质上更安全的动作，而不是换一种命令重试同一危险 动作。 用户体验目标：优先产生安全进展，而不是简单拒绝或把所有判断推给用户。 AskUser 用于授权缺失、目标含糊或高风险动作没有明确用户授权。批准请求绑定动作摘要、能力集合和策略 版本；批准只对当前工具调用生效。 用户体验目标：问题应描述具体副作用与作用范围，而不是只显示“需要权限”。 Deny 用于极高风险、破坏性操作、数据外泄、凭证探测或规避策略。拒绝 Deny 作为结构化工具失败返回， 要求 Agent 不得通过间接命令或替代工具绕过。 用户体验目标：停止危险路径，同时在可能时允许选择不同的安全目标。 7. 上下文、信任与隐私 风险审查器需要理解用户意图，但“更多上下文”并不天然更安全。Zeta 使用证据代理，而不是复制 完整对话记录： - 用户直接指令标记为可信用户意图； - 主机解析的动作与来源标记为可信主机元数据； - Agent 消息、计划、仓库文件和工具结果标记为不可信内容； - 证据限制条目数和字符数； - 凭证、密钥和无关工具输出必须在进入风险审查器前移除。 信任标签只说明来源，不能证明内容真实。例如通过可信文件系统适配器读取的 README，内容仍属于 不可信仓库内容。 当前边界： - Core 选择当前 Turn 最近的用户直接消息； - 工具主机可提供动作专用、只读且不含密钥的证据； - 风险审查器没有工具、凭证、修改能力或可变 Agent 记忆； - 动作与上下文中的提示词注入只能作为数据，不能改变风险审查策略。 未来若引入记忆、组织策略或外部信誉证据，必须定义独立来源和优先级，不能把它们拼成无类型 提示词。 8. 失败、重试与恢复原则 Auto Review 的失败模式必须显式： - 模型不可用、超时、取消、JSON 格式错误和能力不匹配都不能产生授权； - 主机使用 ReviewFailurePolicy::Block 或 AskUser，不存在失败即放行； - 沙箱内进程返回非零退出码，不代表应该自动进行沙箱外重试； - 只有执行器结构化确认且标记为可安全重试 SafeToRetry 的沙箱强制拒绝才能进入二次审查； - 沙箱拒绝前可能已经完成部分副作用；不能证明可安全重放时，即使获得新授权也不得自动重放原 工具调用； - 动作、能力集合、工作目录、环境、来源或策略版本改变后必须重新审查； - 工具已持久化记录执行开始但没有终止结果时，结果视为未知，不自动重放。 风险审查拒绝会作为结构化反馈返回 Agent。单个 Turn 连续 3 次，或最近 50 个工具结果中累计 10 次审查拒绝，会触发断路器并中断该 Turn。这是为了防止模型通过不断改写命令试探策略边界， 不是限制用户重新发起一个明确的新请求。 9. 审计与可观测性 一次可审计的执行至少关联： - 动作摘要与工具调用标识； - 策略版本、审查协议版本和审查结论 ID； - 审查建议与最终决定； - 匹配的确定性规则、现有授权或用户批准标识； - 持久化执行授权； - 沙箱拒绝的原始结构化输出、重放安全性与持久化提权授权； - 执行是否已开始，以及终止或未知结果。 审查结论 ID 对规范化建议建立稳定身份，因此模型 JSON 的空格变化不会制造新结论；审查协议或 策略语义变化必须使用新版本。 安全指标按优先级排序： 1. 危险动作错误自动批准率； 2. 提示词注入与规避策略通过率； 3. 不必要的询问用户比例； 4. 建议、风险与授权准确率； 5. ReviseAction 后安全完成目标的比例； 6. 不同模型与审查协议版本的一致性和漂移。 10. 当前实现状态 已经实现： - 确定性策略优先的 ActionPolicyEngine 决策顺序； - 四种风险审查建议； - 建议与能力集合两层严格 JSON 解析，以及策略拥有的精确或子集验证； - 64 KiB 序列化输入上限，以及适配器和风险审查器两层 16 KiB 响应上限； - 提示词、响应模式与审查协议版本的原子绑定； - 风险 × 用户授权自动批准矩阵； - 绑定审查结论的 AutoReviewGrant 与绑定工具调用的执行授权； - 长度受限的用户意图和带信任标签的证据； - 类型化持久化批准、执行起点标记和未知结果不重放； - 协议拥有的结构化进程与沙箱结果； - Seatbelt 与 Bubblewrap 后端专用的拒绝分类； - 可安全重试的沙箱拒绝二次审查、精确授权、持久化提权与单次重试； - 可能已有副作用时禁止重放； - 结构化的安全动作建议、拒绝反馈与单 Turn 断路器； - App Server 不可变且不带工具的审查模型适配器； - 合成种子样本集和 Cargo、Bazel 离线契约测试。 当前仍有限： - 风险审查器是单次模型调用，没有分层审查或多审查器协作； - 沙箱拒绝分类器覆盖 macOS Seatbelt、Linux Bubblewrap 的已知特征，以及 Windows AppContainer 辅助程序可信的启动前强制执行标记；真实平台拒绝样本集 仍不足； - 沙箱拒绝后的询问用户 AskUser 当前终止原工具调用，由 Agent 发起新的批准路径，不在同一个 已开始调用上建立可恢复的批准等待； - 提示词策略是编译期常量； - 没有真实模型基准运行器、影子模式遥测或人工标注流水线； - 没有组织策略引导； - 样本集规模小，主要用于证明契约与建立回归入口，不能代表生产分布； - 没有足够数据支持微调决策。 11. 演进方向 近期：建立可测量性 - 实现显式模型评测运行器，不进入默认 CI； - 按模型和审查协议版本输出安全指标； - 扩充真实缺陷、人工改判和策略回归的匿名合成复现； - 为错误自动批准设置发布门槛； - 先运行影子模式，再讨论扩大自动批准覆盖率； - 用真实平台拒绝样本评估分类器的误报与漏报，并扩展 Windows 后端。 中期：提高可控性 - 将私有版本化响应模式导出为评测和工具可消费的产物； - 增加组织策略引导，并与不可信上下文分层； - 改善询问原因和 ReviseAction 的可执行性评测； - 增加风险审查器校准与高风险二次检查，但保持确定性授权责任。 潜在方向：专用模型 只有在积累足够高质量、经过隐私审查的标签后，才评估： - 小型专用风险分类器； - 模型微调； - 分层模型路由； - 多风险审查器意见不一致时的升级处理。 训练模型不是评测的前置条件。没有可靠评测集时训练只会把未知错误固化进模型。 12. 长期不变量 无论未来使用何种模型或数据，以下边界不变： - 风险审查器不成为能力授权方； - 模型输出不能覆盖主机解析的动作标识； - 不可信内容不能改变风险审查策略； - 审查失败不产生授权； - 高风险或极高风险的放宽必须经过确定性策略和可审计版本； - 未知执行结果不由风险审查器自动重放； - 凭证和密钥不进入评测样本集或普通审查上下文。 Auto Review 的实现细节、错误语义和修改指南见 zeta-auto-review README；确定性决策引擎与授权实现见 zeta-action-policy` README。"
  },
  {
    "slug": "sandboxing",
    "sourcePath": "docs/sandboxing.md",
    "title": "沙箱架构",
    "description": "沙箱系统落实已经决定好的执行能力：它限制文件、网络和进程范围，但不判断用户意图，也不负责 批准动作。",
    "group": "安全与权限",
    "markdown": "> 文档所有权：本文件是操作系统沙箱策略、平台后端和强制执行边界的权威文档。\n\n## 快速理解\n\n沙箱系统落实已经决定好的执行能力：它限制文件、网络和进程范围，但不判断用户意图，也不负责\n批准动作。\n\n| 执行请求 | 系统行为 | 当前边界 |\n| --- | --- | --- |\n| 工作区只读且禁止网络 | 选择当前平台的只读沙箱后端 | macOS、Linux 已接入；Windows 仍需真实平台验收 |\n| 工作区可写且禁止网络 | 只开放工作区写入，继续限制其他路径和网络 | 各平台按自身机制翻译相同共享策略 |\n| 完全文件访问且允许网络 | 共享策略允许直接执行，不伪装成受限沙箱 | 仍须先经过权限决定 |\n| 后端缺失或无法表达策略 | 失败即关闭，不降级为普通进程 | 返回明确的后端不可用错误 |\n| 沙箱拒绝动作 | 返回结构化执行证据 | 是否重试或扩权由 Core 与权限系统决定 |\n\n## 1. 定位\n\nZeta 将动作审查、沙箱策略、平台选择、命令构建和操作系统强制执行分开：\n\n```mermaid\nflowchart TD\n    scheduler[\"Core 工具调度器<br/><code>ToolScheduler</code>\"] --> policy[\"策略判断<br/><code>zeta-action-policy</code>\"]\n    policy --> review{\"需要动作审查？\"}\n    review -- \"是\" --> autoReview[\"动作审查<br/><code>zeta-auto-review</code>\"]\n    review -- \"否\" --> executor[\"本地工具或命令执行器\"]\n    autoReview --> executor\n    executor --> sandboxing[\"共享沙箱层<br/><code>zeta-sandboxing</code>\"]\n    sandboxing --> macos[\"macOS Seatbelt 后端\"]\n    sandboxing --> linux[\"Linux 后端<br/><code>zeta-linux-sandbox</code>\"]\n    linux --> bwrap[\"Bubblewrap 参数与入口<br/><code>zeta-bwrap</code>\"]\n    sandboxing --> windows[\"Windows AppContainer 后端<br/><code>zeta-windows-sandbox</code>\"]\n```\n\n沙箱管理器 `SandboxManager` 只调度沙箱后端：它验证工作区相对路径、解析当前平台的策略，并生成\n可执行的主机启动计划。它不负责工具并行计划、用户批准、重试、确定性结果排序或工具调用与结果的\n持久化；这些仍属于 Core 的工具调度器 `ToolScheduler`。动作审查的风险判断见\n[`auto-review.md`](auto-review.md)；授权、用户批准与最终执行决定的整体语义见\n[`permissions.md`](permissions.md)。\n\n## 2. crate 边界\n\n### 2.0 `zeta-install-context`\n\n`zeta-install-context` 描述当前可执行文件所在的包布局，并提供 `zeta-path/` 可执行文件候选路径\n与 `zeta-resources/` 资源候选路径。它不选择沙箱策略，不验证辅助程序的能力或摘要，也不启动或\n复制资源。\n\n当前本地 `rg`、Linux Bubblewrap 与 Windows AppContainer 的运行时组合已经接入该契约。规范包\n写入 `zeta-package.json` 与 `zeta-path/rg`；Linux 另带 `zeta-resources/bwrap`，Windows 另带\n命令运行器和沙箱设置辅助程序。平台后端在主机组合阶段完成候选项验证、能力与协议探测，并冻结\n规范身份。\n\n### 2.1 `zeta-bwrap`\n\n`zeta-bwrap` 同时提供 Linux Bubblewrap 的类型化参数构建器和很薄的上游 C 入口包装器：\n\n- 接受显式挂载权限、命名空间、工作目录和内部命令；\n- 始终使用程序与参数数组，不经过 Shell；\n- 生成可检查的 `BwrapCommand`，不自行启动工具；\n- 包构建从校验和锁定的上游源码编译 `bwrap_main`，不启用 setuid 支持；\n- 不拥有 Zeta 的沙箱策略 `SandboxPolicy`、工作区授权、用户批准或降级决策。\n\n调用方不能注入任意拼接后的 `bwrap` 参数。新增 Bubblewrap 能力应先成为类型化操作，再由 Linux\n策略适配器决定是否使用。\n\n### 2.2 `zeta-sandboxing`\n\n`zeta-sandboxing` 是共享契约与后端管理器：\n\n- `SandboxPolicy`、`FileSystemAccess`、`NetworkAccess`；\n- `SandboxCommand` 与 `PreparedCommand`；\n- 沙箱后端契约 `SandboxBackend`；\n- `SandboxManager` 的工作区路径验证与后端分派；\n- 当前 macOS Seatbelt 命令转换；\n- 现有工作区根目录约束 `WorkspaceRoot`。\n\nmacOS 实现暂时保留在本 crate，因为 Seatbelt 转换层很薄，且平台选择与共享策略紧密。\n当 macOS 原生实现需要独立 FFI、辅助程序、较重依赖，或接近 500 行代码时，再提取为\n`zeta-macos-sandbox`；提取不能改变共享策略。\n\n### 2.3 `zeta-linux-sandbox`\n\n`zeta-linux-sandbox` 把共享策略翻译为 `zeta-bwrap` 操作：\n\n- 非完全访问 `FullAccess` 的文件系统默认从只读根目录开始；\n- 工作区可写 `WorkspaceWrite` 通过更具体的读写挂载重新开放工作区；\n- 禁止网络时使用独立网络命名空间；\n- 添加用户与进程命名空间、新建的 `/proc`、`/dev`、会话和父进程退出约束；\n- Bubblewrap 不可用或不支持所需能力时必须返回错误。\n\n该 crate 当前拥有系统或随包提供的 `bwrap` 发现、所需 CLI 能力探测和规范身份冻结。后续仍拥有\n版本诊断、WSL 检查、seccomp 与受管网络桥接；这些细节不能进入共享策略。\n\n### 2.4 `zeta-windows-sandbox`\n\n物理目录保留上游习惯名 `windows-sandbox-rs/`，Cargo 包与 API 名为\n`zeta-windows-sandbox` / `zeta_windows_sandbox`。它拥有：\n\n- 从共享策略到 Windows 文件系统与网络授权的解析；\n- AppContainer 配置文件与能力、ACL、子进程策略和 Job Object 强制执行；\n- Windows 辅助程序与启动器的生命周期和平台诊断。\n\n当前 `zeta-command-runner.exe` 先调用 `zeta-windows-sandbox-setup.exe` 创建或复用按\n规范工作区与读写模式隔离的 AppContainer 配置文件，为工作区和冻结的内部可执行文件安装 ACL，\n再以零能力的 AppContainer 令牌启动进程。零网络能力负责断网，配置文件 SID 与 ACL 负责文件\n访问；子进程限制与单进程 Job Object 补充进程树控制。当前只支持只读或工作区可写模式\n`ReadOnly` / `WorkspaceWrite` 与禁止网络 `NetworkDenied` 的组合；其他受限组合必须失败即关闭。\n\n这不是 Codex 专用本地用户与 WFP 防火墙实现的复制。Zeta v1 选择 Windows 原生 AppContainer\n边界，并明确记录 ACL 是持久化的文件系统元数据。辅助程序已接入包、资源发现、App Server 与\nMSVC 目标交叉检查；真实 Windows AppContainer、ACL 和网络集成测试尚未完成，因此暂不标记为\n生产环境强制执行。\nWindows 测试人员应按\n[`windows-sandbox-acceptance-runbook.md`](windows-sandbox-acceptance-runbook.md)\n回填实际结果、退出码、执行记录和 ACL 证据，再与固定预期结果比对。\n\n## 3. 依赖方向\n\n允许：\n\n```text\nzeta-linux-sandbox   → zeta-bwrap + zeta-sandboxing\nzeta-windows-sandbox → zeta-install-context + zeta-sandboxing\n主机组合              → zeta-install-context + 平台沙箱 + 工具运行时\nzeta-action-policy   → zeta-execpolicy + zeta-sandboxing\nzeta-execpolicy      → no sandbox dependency\nzeta-auto-review     → zeta-action-policy + zeta-sandboxing\n主机执行器            → zeta-sandboxing + 当前平台后端\n```\n\n禁止：\n\n```text\nzeta-bwrap → zeta-sandboxing / protocol / core\n平台沙箱 → zeta-core / ThreadStore / 批准界面\nzeta-sandboxing → shell-command / file-system / apply-patch / app-server / provider\nzeta-sandboxing → zeta-action-policy / zeta-auto-review\nzeta-execpolicy → zeta-action-policy / zeta-sandboxing / Core\nzeta-install-context → zeta-sandboxing / 平台沙箱 / shell-command\n```\n\n平台后端通过 `SandboxBackend` 注入。共享管理器不依赖所有平台实现，因此不会形成\n“共享沙箱 ↔ 平台 crate”循环，也不会把 Windows 原生依赖带入 Linux 或 macOS 可执行文件。\n\n## 4. 安全不变量\n\n- 非“完全访问 + 允许网络” `FullAccess + AllowedNetwork` 请求必须由平台沙箱强制执行；\n- 后端缺失、版本过旧或策略无法完整表达时必须失败即关闭；\n- 工作区根目录约束 `WorkspaceRoot` 不是操作系统沙箱，不能作为降级方案；\n- 模型或工具参数不能选择后端、扩大挂载、授予网络或要求降级；\n- 命令与 `bwrap` 参数始终以结构化参数数组传递；\n- 符号链接、不存在的写入路径、嵌套拒绝和只读例外必须在进入真实执行前处理；\n- 能力探测与实际启动使用同一个已解析可执行文件，避免检查与执行之间的竞态；\n- 诊断必须区分后端不可用、策略不受支持、设置失败和沙箱拒绝。\n\n## 5. 实施顺序\n\n1. 类型化策略、后端契约与命令构建；\n2. 将进程执行器改为消费准备命令 `PreparedCommand`；\n3. Linux 的 `bwrap` 发现、探测与随包分发；\n4. Linux 真实命名空间集成测试与 seccomp；\n5. Windows AppContainer 启动器、ACL 与网络强制执行和 Windows CI；\n6. macOS Seatbelt 配置文件兼容性与集成测试；\n7. 受管网络代理、PTY、取消和进程树终止集成。\n\nLinux 随包源码、构建和发现已完成；真实 Linux 命名空间集成与 seccomp 仍是当前限制。Windows\n辅助程序、构建、发现和强制执行路径已完成，仍需通过真实 Windows 集成测试后才能标记为生产环境\n强制执行。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-定位",
        "title": "1. 定位"
      },
      {
        "depth": 2,
        "id": "2-crate-边界",
        "title": "2. crate 边界"
      },
      {
        "depth": 3,
        "id": "20-zeta-install-context",
        "title": "2.0 zeta-install-context"
      },
      {
        "depth": 3,
        "id": "21-zeta-bwrap",
        "title": "2.1 zeta-bwrap"
      },
      {
        "depth": 3,
        "id": "22-zeta-sandboxing",
        "title": "2.2 zeta-sandboxing"
      },
      {
        "depth": 3,
        "id": "23-zeta-linux-sandbox",
        "title": "2.3 zeta-linux-sandbox"
      },
      {
        "depth": 3,
        "id": "24-zeta-windows-sandbox",
        "title": "2.4 zeta-windows-sandbox"
      },
      {
        "depth": 2,
        "id": "3-依赖方向",
        "title": "3. 依赖方向"
      },
      {
        "depth": 2,
        "id": "4-安全不变量",
        "title": "4. 安全不变量"
      },
      {
        "depth": 2,
        "id": "5-实施顺序",
        "title": "5. 实施顺序"
      }
    ],
    "searchText": " 文档所有权：本文件是操作系统沙箱策略、平台后端和强制执行边界的权威文档。 快速理解 沙箱系统落实已经决定好的执行能力：它限制文件、网络和进程范围，但不判断用户意图，也不负责 批准动作。 执行请求 系统行为 当前边界 --- --- --- 工作区只读且禁止网络 选择当前平台的只读沙箱后端 macOS、Linux 已接入；Windows 仍需真实平台验收 工作区可写且禁止网络 只开放工作区写入，继续限制其他路径和网络 各平台按自身机制翻译相同共享策略 完全文件访问且允许网络 共享策略允许直接执行，不伪装成受限沙箱 仍须先经过权限决定 后端缺失或无法表达策略 失败即关闭，不降级为普通进程 返回明确的后端不可用错误 沙箱拒绝动作 返回结构化执行证据 是否重试或扩权由 Core 与权限系统决定 1. 定位 Zeta 将动作审查、沙箱策略、平台选择、命令构建和操作系统强制执行分开： ``mermaid flowchart TD scheduler \"Core 工具调度器<br/ <code ToolScheduler</code \" -- policy \"策略判断<br/ <code zeta-action-policy</code \" policy -- review \"需要动作审查？\" review -- \"是\" -- autoReview \"动作审查<br/ <code zeta-auto-review</code \" review -- \"否\" -- executor \"本地工具或命令执行器\" autoReview -- executor executor -- sandboxing \"共享沙箱层<br/ <code zeta-sandboxing</code \" sandboxing -- macos \"macOS Seatbelt 后端\" sandboxing -- linux \"Linux 后端<br/ <code zeta-linux-sandbox</code \" linux -- bwrap \"Bubblewrap 参数与入口<br/ <code zeta-bwrap</code \" sandboxing -- windows \"Windows AppContainer 后端<br/ <code zeta-windows-sandbox</code \" ` 沙箱管理器 SandboxManager 只调度沙箱后端：它验证工作区相对路径、解析当前平台的策略，并生成 可执行的主机启动计划。它不负责工具并行计划、用户批准、重试、确定性结果排序或工具调用与结果的 持久化；这些仍属于 Core 的工具调度器 ToolScheduler。动作审查的风险判断见 auto-review.md；授权、用户批准与最终执行决定的整体语义见 permissions.md。 2. crate 边界 2.0 zeta-install-context zeta-install-context 描述当前可执行文件所在的包布局，并提供 zeta-path/ 可执行文件候选路径 与 zeta-resources/ 资源候选路径。它不选择沙箱策略，不验证辅助程序的能力或摘要，也不启动或 复制资源。 当前本地 rg、Linux Bubblewrap 与 Windows AppContainer 的运行时组合已经接入该契约。规范包 写入 zeta-package.json 与 zeta-path/rg；Linux 另带 zeta-resources/bwrap，Windows 另带 命令运行器和沙箱设置辅助程序。平台后端在主机组合阶段完成候选项验证、能力与协议探测，并冻结 规范身份。 2.1 zeta-bwrap zeta-bwrap 同时提供 Linux Bubblewrap 的类型化参数构建器和很薄的上游 C 入口包装器： - 接受显式挂载权限、命名空间、工作目录和内部命令； - 始终使用程序与参数数组，不经过 Shell； - 生成可检查的 BwrapCommand，不自行启动工具； - 包构建从校验和锁定的上游源码编译 bwrapmain，不启用 setuid 支持； - 不拥有 Zeta 的沙箱策略 SandboxPolicy、工作区授权、用户批准或降级决策。 调用方不能注入任意拼接后的 bwrap 参数。新增 Bubblewrap 能力应先成为类型化操作，再由 Linux 策略适配器决定是否使用。 2.2 zeta-sandboxing zeta-sandboxing 是共享契约与后端管理器： - SandboxPolicy、FileSystemAccess、NetworkAccess； - SandboxCommand 与 PreparedCommand； - 沙箱后端契约 SandboxBackend； - SandboxManager 的工作区路径验证与后端分派； - 当前 macOS Seatbelt 命令转换； - 现有工作区根目录约束 WorkspaceRoot。 macOS 实现暂时保留在本 crate，因为 Seatbelt 转换层很薄，且平台选择与共享策略紧密。 当 macOS 原生实现需要独立 FFI、辅助程序、较重依赖，或接近 500 行代码时，再提取为 zeta-macos-sandbox；提取不能改变共享策略。 2.3 zeta-linux-sandbox zeta-linux-sandbox 把共享策略翻译为 zeta-bwrap 操作： - 非完全访问 FullAccess 的文件系统默认从只读根目录开始； - 工作区可写 WorkspaceWrite 通过更具体的读写挂载重新开放工作区； - 禁止网络时使用独立网络命名空间； - 添加用户与进程命名空间、新建的 /proc、/dev、会话和父进程退出约束； - Bubblewrap 不可用或不支持所需能力时必须返回错误。 该 crate 当前拥有系统或随包提供的 bwrap 发现、所需 CLI 能力探测和规范身份冻结。后续仍拥有 版本诊断、WSL 检查、seccomp 与受管网络桥接；这些细节不能进入共享策略。 2.4 zeta-windows-sandbox 物理目录保留上游习惯名 windows-sandbox-rs/，Cargo 包与 API 名为 zeta-windows-sandbox / zetawindowssandbox。它拥有： - 从共享策略到 Windows 文件系统与网络授权的解析； - AppContainer 配置文件与能力、ACL、子进程策略和 Job Object 强制执行； - Windows 辅助程序与启动器的生命周期和平台诊断。 当前 zeta-command-runner.exe 先调用 zeta-windows-sandbox-setup.exe 创建或复用按 规范工作区与读写模式隔离的 AppContainer 配置文件，为工作区和冻结的内部可执行文件安装 ACL， 再以零能力的 AppContainer 令牌启动进程。零网络能力负责断网，配置文件 SID 与 ACL 负责文件 访问；子进程限制与单进程 Job Object 补充进程树控制。当前只支持只读或工作区可写模式 ReadOnly / WorkspaceWrite 与禁止网络 NetworkDenied 的组合；其他受限组合必须失败即关闭。 这不是 Codex 专用本地用户与 WFP 防火墙实现的复制。Zeta v1 选择 Windows 原生 AppContainer 边界，并明确记录 ACL 是持久化的文件系统元数据。辅助程序已接入包、资源发现、App Server 与 MSVC 目标交叉检查；真实 Windows AppContainer、ACL 和网络集成测试尚未完成，因此暂不标记为 生产环境强制执行。 Windows 测试人员应按 windows-sandbox-acceptance-runbook.md 回填实际结果、退出码、执行记录和 ACL 证据，再与固定预期结果比对。 3. 依赖方向 允许： `text zeta-linux-sandbox → zeta-bwrap + zeta-sandboxing zeta-windows-sandbox → zeta-install-context + zeta-sandboxing 主机组合 → zeta-install-context + 平台沙箱 + 工具运行时 zeta-action-policy → zeta-execpolicy + zeta-sandboxing zeta-execpolicy → no sandbox dependency zeta-auto-review → zeta-action-policy + zeta-sandboxing 主机执行器 → zeta-sandboxing + 当前平台后端 ` 禁止： `text zeta-bwrap → zeta-sandboxing / protocol / core 平台沙箱 → zeta-core / ThreadStore / 批准界面 zeta-sandboxing → shell-command / file-system / apply-patch / app-server / provider zeta-sandboxing → zeta-action-policy / zeta-auto-review zeta-execpolicy → zeta-action-policy / zeta-sandboxing / Core zeta-install-context → zeta-sandboxing / 平台沙箱 / shell-command ` 平台后端通过 SandboxBackend 注入。共享管理器不依赖所有平台实现，因此不会形成 “共享沙箱 ↔ 平台 crate”循环，也不会把 Windows 原生依赖带入 Linux 或 macOS 可执行文件。 4. 安全不变量 - 非“完全访问 + 允许网络” FullAccess + AllowedNetwork 请求必须由平台沙箱强制执行； - 后端缺失、版本过旧或策略无法完整表达时必须失败即关闭； - 工作区根目录约束 WorkspaceRoot 不是操作系统沙箱，不能作为降级方案； - 模型或工具参数不能选择后端、扩大挂载、授予网络或要求降级； - 命令与 bwrap 参数始终以结构化参数数组传递； - 符号链接、不存在的写入路径、嵌套拒绝和只读例外必须在进入真实执行前处理； - 能力探测与实际启动使用同一个已解析可执行文件，避免检查与执行之间的竞态； - 诊断必须区分后端不可用、策略不受支持、设置失败和沙箱拒绝。 5. 实施顺序 1. 类型化策略、后端契约与命令构建； 2. 将进程执行器改为消费准备命令 PreparedCommand； 3. Linux 的 bwrap` 发现、探测与随包分发； 4. Linux 真实命名空间集成测试与 seccomp； 5. Windows AppContainer 启动器、ACL 与网络强制执行和 Windows CI； 6. macOS Seatbelt 配置文件兼容性与集成测试； 7. 受管网络代理、PTY、取消和进程树终止集成。 Linux 随包源码、构建和发现已完成；真实 Linux 命名空间集成与 seccomp 仍是当前限制。Windows 辅助程序、构建、发现和强制执行路径已完成，仍需通过真实 Windows 集成测试后才能标记为生产环境 强制执行。"
  },
  {
    "slug": "workspace-security",
    "sourcePath": "docs/workspace-security.md",
    "title": "工作区安全边界",
    "description": "工作区安全包含两个独立判断：路径包含关系决定“能访问哪里”，行为信任决定“能执行什么”。 一个 canonical root 通过路径校验，不代表它可以启动进程、加载可执行配置或改变仓库。",
    "group": "安全与权限",
    "markdown": "> 本文拥有跨 crate 的工作区身份、目录作用域和信任语义。Rust 精确契约由\n> [`zeta-rs/workspace/README.md`](../zeta-rs/workspace/README.md) 与\n> [`zeta-rs/add-dir/README.md`](../zeta-rs/add-dir/README.md) 分别维护。Agent 自定义 artifact 与\n> 外部 Import/source registration 的生命周期由\n> [`agent-customizations.md`](agent-customizations.md) 维护。\n\n## 快速理解\n\n工作区安全包含两个独立判断：路径包含关系决定“能访问哪里”，行为信任决定“能执行什么”。\n一个 canonical root 通过路径校验，不代表它可以启动进程、加载可执行配置或改变仓库。\n\n| 用户操作 | 项目身份如何变化 | 文件访问如何变化 | 当前状态 |\n| --- | --- | --- | --- |\n| 打开工作区 | 建立主工作目录和当前项目 | 默认允许访问主目录 | 已实现 |\n| `add-dir` | 不变 | 增加附加目录 | 尚未实现 |\n| `/cd` | 切换主工作目录和当前项目 | 重新建立默认访问与项目配置作用域 | 尚未实现 |\n| 搜索内容 | 不变 | 默认搜索主目录；未来也搜索附加目录 | 已实现单 root |\n| 导入外部 Agent 配置 | 不变 | 使用一次性导入来源，不产生持续目录授权 | 只读检查已实现 |\n\n## 两道安全门\n\n| 安全门 | 回答的问题 | 输入 | 结果 |\n| --- | --- | --- | --- |\n| 结构边界 | 路径是否属于该工作区？ | host path + 文件系统状态 | `WorkspaceRoot` / 相对路径 |\n| 行为信任 | 该工作区是否可以激活高风险行为？ | exact root + host 信任策略 | `TrustedWorkspace` |\n\n通过 containment 不会自动授予信任。一个 canonical root 的信任不能授权另一个 root。\n\n## 当前实现\n\n`zeta-workspace` 拥有 canonical root identity，并保留 host 最初请求的绝对路径别名，用于 watcher\n投影。这能处理 macOS `/var` 与 `/private/var` 等 namespace 差异，同时保持 canonical\ncontainment。\n\nApp Server 的 Files 与 Git watcher 对普通 root 使用推荐的原生后端。当 requested namespace 与\ncanonical namespace 不同时，它会显式改用 polling，避免 FSEvents 在应用层投影前丢弃 alias\nevent。Watcher 构造和 root 注册全部成功后，runtime authority 才能提交。\n\nApp Server 可以提交 Restricted root，并安装文件读写与 invalidation watcher；它不会安装本地\nTool、Terminal、Git mutation 或依赖外部进程的内容 Search。当前 Search 使用冻结的 `rg`，因此\nRestricted 模式仍不可用，直到存在 restricted-safe backend 或更窄的 sandbox contract。\n\n信任 capability vocabulary 当前覆盖进程执行、可执行配置、Workspace extension、\nWorkspace-declared Tool 与仓库 mutation。Host 负责解析信任决定；仓库文件和普通产品 client\npayload 都不是 authority。`WorkspaceTrustId` 对 canonical root 的平台原生 bytes 做 hash，因此\nUser Config 可以保存 opaque key 而不存储明文路径。\n\n`zeta-config` 在 `UserConfigDocument.workspace_trust` 中保存用户明确的 Restricted/Trusted\n决定。缺失项解析为 Restricted；checked-in `.zeta/config.toml` 若声明该 section 会被拒绝。\nClient 请求 `workspace/switch` 时，App Server 在 authority-switch safe point 读取最新 User\nsnapshot。可信本地 host 在接受 client 请求前固定的 root，则单独标记为 `HostConfiguration`。\n\nUser Config 变化也是主动撤销信号。App Server 会失效共享 capability lease，移除本地\nTool/Git/Search/Terminal，终止 PTY 与 Search process，并在后续工作使用旧 authority 前中断活动\nTurn。Service entry point 会重新检查 lease，因此 stale `Arc` 不能绕过 runtime removal。相同\nRestricted root 的安全 Files 与 watcher 会继续保留。\n\n## 工作目录、附加目录与 `/cd`\n\n当前 App Server 只支持一个活动 Workspace root；`add-dir`、附加目录授权和 multi-root Workspace\n均尚未实现。目标模型必须区分三种操作：\n\n- **主工作目录**：当前项目、默认相对路径、项目配置和 session discovery 的根。\n- **附加目录**：额外文件访问 root；它不成为第二个项目，也不改变主工作目录。\n- **`/cd`**：替换主工作目录，并重新建立项目配置与 session discovery 作用域。\n\n因此 `add-dir` 不只是 App Server 命令，而是独立领域边界。`zeta-add-dir` 拥有“一个主工作目录\n+ 若干显式附加目录”的访问作用域、directory source lifetime、canonical deduplication 与\ncontribution policy；`zeta-workspace` 继续只拥有单 root identity、containment 与 trust token。\nApp Server 负责在 safe point 解析每个 root 的 capability 与 trust、协调 consumer 和 revocation。\n\n| 操作 | 改变当前项目 | Files / Search 可访问 | 自动加载配置 | 生命周期 |\n| --- | --- | --- | --- | --- |\n| 启动时的主目录 | ✅ | 主目录 | 完整项目配置 | 直到 `/cd` 或进程结束 |\n| 启动参数 `--add-dir` | ❌ | 主目录 + 附加目录 | 只允许明确列出的贡献 | 本次启动 |\n| 会话命令 `/add-dir` | ❌ | 主目录 + 附加目录 | 只允许明确列出的贡献 | 当前会话 |\n| 持久 `additionalDirectories` | ❌ | 主目录 + 附加目录 | ❌，只提供文件访问 | 配置有效期间 |\n| `/cd` | ✅ | 以新主目录重新解析 | 加载新主目录的完整项目配置 | 直到再次切换 |\n\n目标配置激活必须与文件访问授权分离。命令行或会话添加的目录可以按 allowlist 贡献 Skills、\nAgent definitions、`enabledPlugins` 和 `extraKnownMarketplaces`；Hook、大部分 permission、output style、\ncommand、`CLAUDE.md`、`.claude/rules/` 与 `CLAUDE.local.md` 默认不生效。Host compatibility\nswitch 可以允许最后三类 instruction，但它不能扩大 Files 权限。持久 `additionalDirectories`\n始终是 file-access-only，不执行任何附加目录配置发现。\n\nRust API 不使用 `bool` 表达这些差异。当前 `zeta-add-dir` 已用\n`AdditionalDirectorySource` 保留 directory origin，并解析\n`AdditionalDirectoryContributionPolicy`；不能把“目录已授权”直接解释为“目录配置已激活”。\n\n## 与外部 Agent 导入的关系\n\n`zeta-agent-import` 继续拥有 Codex/Claude 的已知布局、candidate classification 和敏感路径排除。\n它可以为附加目录的 allowlisted contribution discovery 提供 source-specific inspection，也可以\n服务于用户显式触发的 Import workflow，但两条调用路径不能合并：\n\n| 路径 | 目的 | 是否写入 Zeta Config | 是否授予持续文件访问 |\n| --- | --- | --- | --- |\n| `add-dir` contribution discovery | 在目录授权有效期间投影允许的贡献 | ❌ | ✅，由 `add-dir` authority 授予 |\n| `import-agent` | 预览、选择并迁移外部配置 | 用户确认后由目标领域决定 | ❌ |\n\nImport source 不是 `add-dir`。`zeta-agent-import` 不能添加 root、持久化 Workspace 布局或授予持续\n文件访问。两条流程只共享 host directory picker、path containment primitive 和安全的\nsource-specific parser；从用户 home 导入配置不能把 home 隐式变成 Workspace 或附加目录。\n\n## 必需的产品流程\n\n```text\nhost 解析目录\n  → 建立 WorkspaceRoot\n  → 为精确 canonical identity 读取信任决定\n  ├─ Restricted：只允许 browse / edit / watch\n  └─ Trusted：按高风险能力签发 root-bound capability\n       → Terminal / Tool process / LSP / MCP / extension / Git mutation\n```\n\nTrust revocation 或 root replacement 必须先拆除 capability 和 process，再发布新的 Workspace\nauthority。活动 Turn 会阻止 authority switch，直到旧 runtime 能一致退休。\n\n## 当前限制\n\n- Trust prompt UI 与 organization policy resolution 尚未实现。User persistence 属于\n  `zeta-config`；policy composition 与 UI consent 属于 host/security layer。\n- Restricted content Search 尚未安装，因为当前 backend 会启动 `rg`。\n- Revocation 会取消活动 Turn 及 cooperative Tool cancellation token。已经越过操作系统\n  side-effect boundary 的 process 仍可能返回 unknown outcome；撤销 capability 不能回滚外部副作用。\n- `WorkspaceTrustId` 当前绑定 canonical path，而不是 filesystem object identity；同一路径上的\n  内容替换不会自动失效信任。\n- Path validation 与后续 I/O 不是 atomic；在 hostile concurrent mutation 的 threat model 下，\n  仍需 handle-relative filesystem API。\n\n## 后续计划\n\n1. 把 `zeta-add-dir` 的纯 directory scope 接入 App Server，明确区分主工作目录、附加目录和\n   directory source。\n2. 为每个附加 root 单独解析 capability、trust 与 contribution policy，只原子重建允许看到该\n   root 的 runtime。\n3. 增加 `/cd` authority switch；它替换项目 identity 并重新加载主目录配置，而不是复用\n   `add-dir` mutation。\n4. 在 host trust resolver 中增加 filesystem identity change invalidation。\n5. Electron Main 的 Workspace transition 要求明确 trust decision；Renderer 只投影已提交状态。\n6. Terminal、Workspace MCP/LSP/extension activation、Hook 和 repository mutation 全部使用\n   root-bound capability。\n7. 增加 restricted-safe content Search backend，或 narrow sandboxed host-owned Search\n   capability。\n\n潜在方向包括 organization-managed trust policy、signed Workspace provenance 与\nhandle-relative filesystem enforcement。这些都不是当前行为。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "两道安全门",
        "title": "两道安全门"
      },
      {
        "depth": 2,
        "id": "当前实现",
        "title": "当前实现"
      },
      {
        "depth": 2,
        "id": "工作目录附加目录与-cd",
        "title": "工作目录、附加目录与 /cd"
      },
      {
        "depth": 2,
        "id": "与外部-agent-导入的关系",
        "title": "与外部 Agent 导入的关系"
      },
      {
        "depth": 2,
        "id": "必需的产品流程",
        "title": "必需的产品流程"
      },
      {
        "depth": 2,
        "id": "当前限制",
        "title": "当前限制"
      },
      {
        "depth": 2,
        "id": "后续计划",
        "title": "后续计划"
      }
    ],
    "searchText": " 本文拥有跨 crate 的工作区身份、目录作用域和信任语义。Rust 精确契约由 zeta-rs/workspace/README.md 与 zeta-rs/add-dir/README.md 分别维护。Agent 自定义 artifact 与 外部 Import/source registration 的生命周期由 agent-customizations.md 维护。 快速理解 工作区安全包含两个独立判断：路径包含关系决定“能访问哪里”，行为信任决定“能执行什么”。 一个 canonical root 通过路径校验，不代表它可以启动进程、加载可执行配置或改变仓库。 用户操作 项目身份如何变化 文件访问如何变化 当前状态 --- --- --- --- 打开工作区 建立主工作目录和当前项目 默认允许访问主目录 已实现 add-dir 不变 增加附加目录 尚未实现 /cd 切换主工作目录和当前项目 重新建立默认访问与项目配置作用域 尚未实现 搜索内容 不变 默认搜索主目录；未来也搜索附加目录 已实现单 root 导入外部 Agent 配置 不变 使用一次性导入来源，不产生持续目录授权 只读检查已实现 两道安全门 安全门 回答的问题 输入 结果 --- --- --- --- 结构边界 路径是否属于该工作区？ host path + 文件系统状态 WorkspaceRoot / 相对路径 行为信任 该工作区是否可以激活高风险行为？ exact root + host 信任策略 TrustedWorkspace 通过 containment 不会自动授予信任。一个 canonical root 的信任不能授权另一个 root。 当前实现 zeta-workspace 拥有 canonical root identity，并保留 host 最初请求的绝对路径别名，用于 watcher 投影。这能处理 macOS /var 与 /private/var 等 namespace 差异，同时保持 canonical containment。 App Server 的 Files 与 Git watcher 对普通 root 使用推荐的原生后端。当 requested namespace 与 canonical namespace 不同时，它会显式改用 polling，避免 FSEvents 在应用层投影前丢弃 alias event。Watcher 构造和 root 注册全部成功后，runtime authority 才能提交。 App Server 可以提交 Restricted root，并安装文件读写与 invalidation watcher；它不会安装本地 Tool、Terminal、Git mutation 或依赖外部进程的内容 Search。当前 Search 使用冻结的 rg，因此 Restricted 模式仍不可用，直到存在 restricted-safe backend 或更窄的 sandbox contract。 信任 capability vocabulary 当前覆盖进程执行、可执行配置、Workspace extension、 Workspace-declared Tool 与仓库 mutation。Host 负责解析信任决定；仓库文件和普通产品 client payload 都不是 authority。WorkspaceTrustId 对 canonical root 的平台原生 bytes 做 hash，因此 User Config 可以保存 opaque key 而不存储明文路径。 zeta-config 在 UserConfigDocument.workspacetrust 中保存用户明确的 Restricted/Trusted 决定。缺失项解析为 Restricted；checked-in .zeta/config.toml 若声明该 section 会被拒绝。 Client 请求 workspace/switch 时，App Server 在 authority-switch safe point 读取最新 User snapshot。可信本地 host 在接受 client 请求前固定的 root，则单独标记为 HostConfiguration。 User Config 变化也是主动撤销信号。App Server 会失效共享 capability lease，移除本地 Tool/Git/Search/Terminal，终止 PTY 与 Search process，并在后续工作使用旧 authority 前中断活动 Turn。Service entry point 会重新检查 lease，因此 stale Arc 不能绕过 runtime removal。相同 Restricted root 的安全 Files 与 watcher 会继续保留。 工作目录、附加目录与 /cd 当前 App Server 只支持一个活动 Workspace root；add-dir、附加目录授权和 multi-root Workspace 均尚未实现。目标模型必须区分三种操作： - 主工作目录：当前项目、默认相对路径、项目配置和 session discovery 的根。 - 附加目录：额外文件访问 root；它不成为第二个项目，也不改变主工作目录。 - /cd：替换主工作目录，并重新建立项目配置与 session discovery 作用域。 因此 add-dir 不只是 App Server 命令，而是独立领域边界。zeta-add-dir 拥有“一个主工作目录 + 若干显式附加目录”的访问作用域、directory source lifetime、canonical deduplication 与 contribution policy；zeta-workspace 继续只拥有单 root identity、containment 与 trust token。 App Server 负责在 safe point 解析每个 root 的 capability 与 trust、协调 consumer 和 revocation。 操作 改变当前项目 Files / Search 可访问 自动加载配置 生命周期 --- --- --- --- --- 启动时的主目录 ✅ 主目录 完整项目配置 直到 /cd 或进程结束 启动参数 --add-dir ❌ 主目录 + 附加目录 只允许明确列出的贡献 本次启动 会话命令 /add-dir ❌ 主目录 + 附加目录 只允许明确列出的贡献 当前会话 持久 additionalDirectories ❌ 主目录 + 附加目录 ❌，只提供文件访问 配置有效期间 /cd ✅ 以新主目录重新解析 加载新主目录的完整项目配置 直到再次切换 目标配置激活必须与文件访问授权分离。命令行或会话添加的目录可以按 allowlist 贡献 Skills、 Agent definitions、enabledPlugins 和 extraKnownMarketplaces；Hook、大部分 permission、output style、 command、CLAUDE.md、.claude/rules/ 与 CLAUDE.local.md 默认不生效。Host compatibility switch 可以允许最后三类 instruction，但它不能扩大 Files 权限。持久 additionalDirectories 始终是 file-access-only，不执行任何附加目录配置发现。 Rust API 不使用 bool 表达这些差异。当前 zeta-add-dir 已用 AdditionalDirectorySource 保留 directory origin，并解析 AdditionalDirectoryContributionPolicy；不能把“目录已授权”直接解释为“目录配置已激活”。 与外部 Agent 导入的关系 zeta-agent-import 继续拥有 Codex/Claude 的已知布局、candidate classification 和敏感路径排除。 它可以为附加目录的 allowlisted contribution discovery 提供 source-specific inspection，也可以 服务于用户显式触发的 Import workflow，但两条调用路径不能合并： 路径 目的 是否写入 Zeta Config 是否授予持续文件访问 --- --- --- --- add-dir contribution discovery 在目录授权有效期间投影允许的贡献 ❌ ✅，由 add-dir authority 授予 import-agent 预览、选择并迁移外部配置 用户确认后由目标领域决定 ❌ Import source 不是 add-dir。zeta-agent-import 不能添加 root、持久化 Workspace 布局或授予持续 文件访问。两条流程只共享 host directory picker、path containment primitive 和安全的 source-specific parser；从用户 home 导入配置不能把 home 隐式变成 Workspace 或附加目录。 必需的产品流程 ``text host 解析目录 → 建立 WorkspaceRoot → 为精确 canonical identity 读取信任决定 ├─ Restricted：只允许 browse / edit / watch └─ Trusted：按高风险能力签发 root-bound capability → Terminal / Tool process / LSP / MCP / extension / Git mutation ` Trust revocation 或 root replacement 必须先拆除 capability 和 process，再发布新的 Workspace authority。活动 Turn 会阻止 authority switch，直到旧 runtime 能一致退休。 当前限制 - Trust prompt UI 与 organization policy resolution 尚未实现。User persistence 属于 zeta-config；policy composition 与 UI consent 属于 host/security layer。 - Restricted content Search 尚未安装，因为当前 backend 会启动 rg。 - Revocation 会取消活动 Turn 及 cooperative Tool cancellation token。已经越过操作系统 side-effect boundary 的 process 仍可能返回 unknown outcome；撤销 capability 不能回滚外部副作用。 - WorkspaceTrustId 当前绑定 canonical path，而不是 filesystem object identity；同一路径上的 内容替换不会自动失效信任。 - Path validation 与后续 I/O 不是 atomic；在 hostile concurrent mutation 的 threat model 下， 仍需 handle-relative filesystem API。 后续计划 1. 把 zeta-add-dir 的纯 directory scope 接入 App Server，明确区分主工作目录、附加目录和 directory source。 2. 为每个附加 root 单独解析 capability、trust 与 contribution policy，只原子重建允许看到该 root 的 runtime。 3. 增加 /cd authority switch；它替换项目 identity 并重新加载主目录配置，而不是复用 add-dir` mutation。 4. 在 host trust resolver 中增加 filesystem identity change invalidation。 5. Electron Main 的 Workspace transition 要求明确 trust decision；Renderer 只投影已提交状态。 6. Terminal、Workspace MCP/LSP/extension activation、Hook 和 repository mutation 全部使用 root-bound capability。 7. 增加 restricted-safe content Search backend，或 narrow sandboxed host-owned Search capability。 潜在方向包括 organization-managed trust policy、signed Workspace provenance 与 handle-relative filesystem enforcement。这些都不是当前行为。"
  },
  {
    "slug": "secrets",
    "sourcePath": "docs/secrets.md",
    "title": "凭据与秘密系统",
    "description": "秘密存储只安全保存不透明字节；“这个秘密是什么、何时刷新、谁可以使用”始终由对应业务领域 负责。",
    "group": "安全与权限",
    "markdown": "> - 物理位置：`zeta-rs/secrets/`\n> - Rust crate：`zeta_secrets`\n> - 层次：host secret persistence primitive\n> - 当前实现：typed key/value、`load/store/delete` port、OS keyring、ephemeral memory、unavailable 与显式文件 backend\n> - Crate 实现、安全义务与测试：[`zeta-rs/secrets/README.md`](../zeta-rs/secrets/README.md)\n> - OS keyring adapter：[`zeta-rs/keyring-store/README.md`](../zeta-rs/keyring-store/README.md)\n> - Direct-provider credential：[`model-provider.md`](model-provider.md#6-供应商凭据与-codex-边界)\n> - Interactive login control plane：[`login.md`](login.md)\n> - App Server 登录控制面：[`zeta-app-server-api.md`](zeta-app-server-api.md#11-account-与登录)\n\n## 快速理解\n\n秘密存储只安全保存不透明字节；“这个秘密是什么、何时刷新、谁可以使用”始终由对应业务领域\n负责。\n\n| 调用方需求 | 秘密存储负责 | 秘密存储不负责 |\n| --- | --- | --- |\n| 保存或读取一个敏感值 | 按不透明 `SecretKey` 执行 `load/store/delete` | 解释它是 API key、OAuth token 还是其他凭据 |\n| 在不同宿主持久化 | 使用系统钥匙串、显式文件或临时后端 | 替调用方选择账户、供应商或授权范围 |\n| 后端不可用 | 返回明确错误 | 静默降级到不安全文件或普通配置 |\n| 记录诊断信息 | 只记录脱敏错误和不敏感键 | 输出秘密字节、认证标头或完整命令 |\n| 删除秘密 | 返回已删除或不存在的明确结果 | 撤销远端 token 或完成供应商登出 |\n\n## 1. 结论\n\n`zeta-secrets` 是长期保留的独立基础设施 crate，但它不是 credential authority，也不是统一 OAuth\n框架。它只回答一件事：\n\n> 在不进入普通配置、日志、telemetry 或产品事件的前提下，按 opaque key\n> 安全地读取、写入和删除 opaque secret bytes。\n\n单独提取它的原因是 Provider credential、MCP OAuth、Plugin/Connector credential slot 等多个领域\n都会需要同一种 host secret persistence；这些领域的登录协议和生命周期却并不相同。共享存储\nprimitive，不能共享一套虚假的 `CredentialManager`。\n\n## 2. 所有权\n\n### `zeta-secrets` 拥有\n\n- `SecretKey`、`SecretValue` 和 secret-store error；\n- `SecretStore::load/store/delete`；\n- OS keyring、显式 file、ephemeral 等 backend adapter；\n- Zeta namespace isolation；\n- secret value 的 `Debug` 脱敏和内存清理；\n- backend access、atomic replacement、权限和 negative logging tests。\n\n### `zeta-secrets` 不拥有\n\n- API key、OAuth token bundle、AWS/Google/Azure identity 的业务类型；\n- Provider/account/workspace/tenant/credential revision；\n- PKCE、browser callback、device-code、refresh、revoke；\n- `Authorization` 或 Provider-specific header；\n- endpoint、HTTP retry、SSE/WebSocket、telemetry；\n- App Server RPC、Desktop 登录 UI；\n- 哪个 Plugin/MCP/provider 可以使用哪个 secret 的 grant policy。\n\n因此 secrets crate 中禁止出现 `OpenAiToken`、`AnthropicCredential`、`McpOAuthSession`、\n`AccountSnapshot` 等领域类型。\n\n## 3. 分层\n\n```text\nstatic declaration\n  zeta-config / model-provider-config\n  └─ 只保存 CredentialRef、account selection、provider auth mode\n\ndomain runtime\n  model-provider::credential / MCP auth / Connector auth\n  ├─ 定义 direct credential/token 类型\n  ├─ refresh、revoke、scope 校验\n  └─ 把领域 identity 映射为 SecretKey\n\ninteractive login\n  zeta-login + provider-specific adapter\n  └─ 只发布 redacted account 状态；是否使用 SecretStore 由 exact adapter 决定\n\nsecret persistence\n  zeta-secrets\n  └─ load / store / delete opaque bytes\n\nwire and transport\n  zeta-api          ── method/path/protocol headers/body/event codec\n  zeta-client       ── operation retry/framing/telemetry\n  zeta-http-client  ── HTTP/WebSocket/proxy/TLS/pool/transport diagnostics\n```\n\n`zeta-api`、`zeta-client` 和 `zeta-http-client` 都不依赖 `zeta-secrets`。它们接收已经构造完成的\n请求或已经解析的 sensitive transport value，不查 keychain，也不刷新 token。\n\n## 4. 公共接口\n\n```rust\npub trait SecretStore: Send + Sync {\n    fn load(&self, key: &SecretKey) -> Result<Option<SecretValue>, SecretStoreError>;\n    fn store(&self, key: &SecretKey, value: &SecretValue) -> Result<(), SecretStoreError>;\n    fn delete(&self, key: &SecretKey)\n        -> Result<DeleteSecretOutcome, SecretStoreError>;\n}\n```\n\n- `SecretKey` 可记录，因为它必须是不含 secret/PII 的 opaque identity；\n- `SecretValue` 不实现 `Clone`、`Display`、serialization，`Debug` 固定脱敏；\n- delete 使用 `DeleteSecretOutcome::{Deleted, NotFound}`，不返回含义模糊的 `bool`；\n- backend error 必须先净化，不能携带 secret、raw backend response 或完整 command line；\n- store 不枚举所有 secret，避免领域绕过自己的 authority 扫描其他 namespace。\n\n消费领域拥有 key schema。例如 Provider runtime 可以使用：\n\n```text\nprovider/openai/account/{opaque-account-id}/api-key\nprovider/openai/account/{opaque-account-id}/platform-service-token\n```\n\n这里的 account segment 必须是 opaque ID，不能直接放 email、token 或 workspace name。\nChatGPT/Codex subscription token 不在 Zeta namespace 中：它由 upstream Codex App Server own storage\n管理，Zeta 只能保存 redacted account reference。\nMCP/Connector 使用自己的 namespace，不能把 Provider key schema 当成通用 credential schema。\n\n## 5. Backend 策略\n\n长期 backend：\n\n| Host | 默认 backend | 说明 |\n| --- | --- | --- |\n| Desktop | OS keyring | macOS Keychain、Windows Credential Manager、Linux Secret Service |\n| CLI/TUI interactive | OS keyring | 不可用时必须显式选择 file 或 ephemeral，不能静默降级 |\n| CI/exec | ephemeral / injected | secret 由进程环境或调用方注入，不自动落盘 |\n| Explicit file | opt-in | 只用于无法使用 OS keyring 的 host |\n\n生产 backend 必须一次实现完整的 `load/store/delete`。不保留旧\n`MacosKeychainCredentialStore::read_secret`，因为只读 adapter 会迫使 writer 形成第二套 authority。\n\n显式 file backend 的最低要求：\n\n- parent directory 私有，Unix file mode 至少 `0600`；\n- Zeta 专属文件名和 schema version；\n- temp file + fsync + atomic replace；\n- 并发写入有确定的 serialization；\n- delete 清理所有 fallback copy；\n- error、backup、fixture 和 telemetry 不包含 secret。\n\nOS keyring backend 不得把 secret 放进 process arguments。只有能使用安全平台 API 或安全 stdin\ncontract 时才能实现；不能为了“先接上”调用会在进程列表暴露 password 的命令行。\n\n## 6. 生命周期与一致性\n\n`zeta-secrets` 不提供跨多个 key 的业务事务。领域 runtime 负责：\n\n1. 验证新 credential/token bundle；\n2. 编码成有 schema version 的领域 payload；\n3. store 到 exact `SecretKey`；\n4. 持久化成功后才发布不含 secret 的 account/credential revision；\n5. logout/revoke 时，即使远端 revoke 失败也执行本地 delete；\n6. credential rotation 后使旧 materialized request snapshot 失效。\n\n同一个 credential 的 single-flight refresh 属于 Provider/MCP/Connector auth manager，不属于\nsecret backend。Backend 可以序列化物理写入，但不能推断 token expiry 或 account identity。\n\n## 7. 当前实现位置\n\n```text\nzeta-rs/secrets/\n├── BUILD.bazel\n├── Cargo.toml\n├── README.md\n└── src/\n    ├── lib.rs\n    ├── error.rs\n    ├── value.rs\n    ├── store.rs\n    ├── memory.rs\n    ├── file.rs\n    ├── secrets_tests.rs\n    └── file_tests.rs\n\nzeta-rs/keyring-store/\n├── BUILD.bazel\n├── Cargo.toml\n├── README.md\n└── src/\n    ├── lib.rs\n    └── lib_tests.rs\n```\n\n当前显式文件实现位于 `zeta-secrets/src/file.rs`；平台 keyring adapter 位于独立的\n`zeta-keyring-store`，避免基础 value/port crate 引入平台依赖。只有同一 crate 内多个 backend 需要共同\n私有基础设施时才引入 `backend/` 层级。\n\n## 8. 依赖方向\n\n允许：\n\n```text\nmodel-provider direct credential ──▶ zeta-secrets\nMCP runtime ─────▶ zeta-secrets\nConnector auth ──▶ zeta-secrets\nlogin adapter ───▶ zeta-secrets       # only when the provider owns token persistence\nApp Server composition ──▶ domain auth services\n```\n\n禁止：\n\n```text\nzeta-secrets ──▶ model-provider / MCP / Plugin / zeta-api / zeta-client / zeta-http-client / zeta-core\nzeta-api ──────▶ zeta-secrets\nzeta-client ───▶ zeta-secrets\nzeta-http-client ──▶ zeta-secrets\nzeta-core ─────▶ zeta-secrets\nzeta-config ───▶ secret value\nDesktop renderer ──▶ SecretStore\n```\n\n## 9. 测试门\n\n- load/store/replace/delete/not-found contract；\n- `SecretValue` 的 `Debug` 和 error negative logging；\n- invalid/control-character key rejection；\n- namespace collision；\n- concurrent backend access；\n- backend unavailable/access denied 分类；\n- OS keyring adapter contract 的完整 round trip；真实平台 keyring round trip 由 opt-in host test 验证；\n- file permission、atomic replace、crash recovery；\n- logout/delete 后所有 fallback copy 均不可读取；\n- schema、App Server DTO、Thread event 和 rollout 中无 secret-bearing field。\n\n## 10. 固定决策\n\n1. 长期保留独立 `zeta-secrets` crate。\n2. 删除 `zeta-credentials`；不建立同义的统一 credential/OAuth authority。\n3. Direct-provider credential 属于 `zeta-model-provider`，interactive login 属于 `zeta-login`；\n   MCP/Connector 各自拥有登录状态机。\n4. secrets 只保存 opaque bytes，不理解 token 或 account。\n5. Config 只保存 reference，不保存 secret。\n6. API/client/Core 不读取 secret store。\n7. 本地 Connector composition 默认使用按 profile 隔离的 OS keyring；keyring operation 失败时明确\n   fail closed，不自动读取文件副本。显式文件 backend 只由 host 主动注入，并只在能强制 0700/0600\n   权限的 Unix host 启用。\n8. App Server protocol 和普通 server operation 不暴露 secret；local composition 只把\n   `SecretStore` 注入 Connector/MCP credential adapter。ChatGPT/Codex 的凭据仍归 upstream Codex\n   App Server。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 3,
        "id": "zeta-secrets-拥有",
        "title": "zeta-secrets 拥有"
      },
      {
        "depth": 3,
        "id": "zeta-secrets-不拥有",
        "title": "zeta-secrets 不拥有"
      },
      {
        "depth": 2,
        "id": "3-分层",
        "title": "3. 分层"
      },
      {
        "depth": 2,
        "id": "4-公共接口",
        "title": "4. 公共接口"
      },
      {
        "depth": 2,
        "id": "5-backend-策略",
        "title": "5. Backend 策略"
      },
      {
        "depth": 2,
        "id": "6-生命周期与一致性",
        "title": "6. 生命周期与一致性"
      },
      {
        "depth": 2,
        "id": "7-当前实现位置",
        "title": "7. 当前实现位置"
      },
      {
        "depth": 2,
        "id": "8-依赖方向",
        "title": "8. 依赖方向"
      },
      {
        "depth": 2,
        "id": "9-测试门",
        "title": "9. 测试门"
      },
      {
        "depth": 2,
        "id": "10-固定决策",
        "title": "10. 固定决策"
      }
    ],
    "searchText": " - 物理位置：zeta-rs/secrets/ - Rust crate：zetasecrets - 层次：host secret persistence primitive - 当前实现：typed key/value、load/store/delete port、OS keyring、ephemeral memory、unavailable 与显式文件 backend - Crate 实现、安全义务与测试：zeta-rs/secrets/README.md - OS keyring adapter：zeta-rs/keyring-store/README.md - Direct-provider credential：model-provider.md - Interactive login control plane：login.md - App Server 登录控制面：zeta-app-server-api.md 快速理解 秘密存储只安全保存不透明字节；“这个秘密是什么、何时刷新、谁可以使用”始终由对应业务领域 负责。 调用方需求 秘密存储负责 秘密存储不负责 --- --- --- 保存或读取一个敏感值 按不透明 SecretKey 执行 load/store/delete 解释它是 API key、OAuth token 还是其他凭据 在不同宿主持久化 使用系统钥匙串、显式文件或临时后端 替调用方选择账户、供应商或授权范围 后端不可用 返回明确错误 静默降级到不安全文件或普通配置 记录诊断信息 只记录脱敏错误和不敏感键 输出秘密字节、认证标头或完整命令 删除秘密 返回已删除或不存在的明确结果 撤销远端 token 或完成供应商登出 1. 结论 zeta-secrets 是长期保留的独立基础设施 crate，但它不是 credential authority，也不是统一 OAuth 框架。它只回答一件事： 在不进入普通配置、日志、telemetry 或产品事件的前提下，按 opaque key 安全地读取、写入和删除 opaque secret bytes。 单独提取它的原因是 Provider credential、MCP OAuth、Plugin/Connector credential slot 等多个领域 都会需要同一种 host secret persistence；这些领域的登录协议和生命周期却并不相同。共享存储 primitive，不能共享一套虚假的 CredentialManager。 2. 所有权 zeta-secrets 拥有 - SecretKey、SecretValue 和 secret-store error； - SecretStore::load/store/delete； - OS keyring、显式 file、ephemeral 等 backend adapter； - Zeta namespace isolation； - secret value 的 Debug 脱敏和内存清理； - backend access、atomic replacement、权限和 negative logging tests。 zeta-secrets 不拥有 - API key、OAuth token bundle、AWS/Google/Azure identity 的业务类型； - Provider/account/workspace/tenant/credential revision； - PKCE、browser callback、device-code、refresh、revoke； - Authorization 或 Provider-specific header； - endpoint、HTTP retry、SSE/WebSocket、telemetry； - App Server RPC、Desktop 登录 UI； - 哪个 Plugin/MCP/provider 可以使用哪个 secret 的 grant policy。 因此 secrets crate 中禁止出现 OpenAiToken、AnthropicCredential、McpOAuthSession、 AccountSnapshot 等领域类型。 3. 分层 ``text static declaration zeta-config / model-provider-config └─ 只保存 CredentialRef、account selection、provider auth mode domain runtime model-provider::credential / MCP auth / Connector auth ├─ 定义 direct credential/token 类型 ├─ refresh、revoke、scope 校验 └─ 把领域 identity 映射为 SecretKey interactive login zeta-login + provider-specific adapter └─ 只发布 redacted account 状态；是否使用 SecretStore 由 exact adapter 决定 secret persistence zeta-secrets └─ load / store / delete opaque bytes wire and transport zeta-api ── method/path/protocol headers/body/event codec zeta-client ── operation retry/framing/telemetry zeta-http-client ── HTTP/WebSocket/proxy/TLS/pool/transport diagnostics ` zeta-api、zeta-client 和 zeta-http-client 都不依赖 zeta-secrets。它们接收已经构造完成的 请求或已经解析的 sensitive transport value，不查 keychain，也不刷新 token。 4. 公共接口 `rust pub trait SecretStore: Send + Sync fn load &self, key: &SecretKey - Result<Option<SecretValue , SecretStoreError ; fn store &self, key: &SecretKey, value: &SecretValue - Result< , SecretStoreError ; fn delete &self, key: &SecretKey - Result<DeleteSecretOutcome, SecretStoreError ; ` - SecretKey 可记录，因为它必须是不含 secret/PII 的 opaque identity； - SecretValue 不实现 Clone、Display、serialization，Debug 固定脱敏； - delete 使用 DeleteSecretOutcome:: Deleted, NotFound ，不返回含义模糊的 bool； - backend error 必须先净化，不能携带 secret、raw backend response 或完整 command line； - store 不枚举所有 secret，避免领域绕过自己的 authority 扫描其他 namespace。 消费领域拥有 key schema。例如 Provider runtime 可以使用： `text provider/openai/account/ opaque-account-id /api-key provider/openai/account/ opaque-account-id /platform-service-token ` 这里的 account segment 必须是 opaque ID，不能直接放 email、token 或 workspace name。 ChatGPT/Codex subscription token 不在 Zeta namespace 中：它由 upstream Codex App Server own storage 管理，Zeta 只能保存 redacted account reference。 MCP/Connector 使用自己的 namespace，不能把 Provider key schema 当成通用 credential schema。 5. Backend 策略 长期 backend： Host 默认 backend 说明 --- --- --- Desktop OS keyring macOS Keychain、Windows Credential Manager、Linux Secret Service CLI/TUI interactive OS keyring 不可用时必须显式选择 file 或 ephemeral，不能静默降级 CI/exec ephemeral / injected secret 由进程环境或调用方注入，不自动落盘 Explicit file opt-in 只用于无法使用 OS keyring 的 host 生产 backend 必须一次实现完整的 load/store/delete。不保留旧 MacosKeychainCredentialStore::readsecret，因为只读 adapter 会迫使 writer 形成第二套 authority。 显式 file backend 的最低要求： - parent directory 私有，Unix file mode 至少 0600； - Zeta 专属文件名和 schema version； - temp file + fsync + atomic replace； - 并发写入有确定的 serialization； - delete 清理所有 fallback copy； - error、backup、fixture 和 telemetry 不包含 secret。 OS keyring backend 不得把 secret 放进 process arguments。只有能使用安全平台 API 或安全 stdin contract 时才能实现；不能为了“先接上”调用会在进程列表暴露 password 的命令行。 6. 生命周期与一致性 zeta-secrets 不提供跨多个 key 的业务事务。领域 runtime 负责： 1. 验证新 credential/token bundle； 2. 编码成有 schema version 的领域 payload； 3. store 到 exact SecretKey； 4. 持久化成功后才发布不含 secret 的 account/credential revision； 5. logout/revoke 时，即使远端 revoke 失败也执行本地 delete； 6. credential rotation 后使旧 materialized request snapshot 失效。 同一个 credential 的 single-flight refresh 属于 Provider/MCP/Connector auth manager，不属于 secret backend。Backend 可以序列化物理写入，但不能推断 token expiry 或 account identity。 7. 当前实现位置 `text zeta-rs/secrets/ ├── BUILD.bazel ├── Cargo.toml ├── README.md └── src/ ├── lib.rs ├── error.rs ├── value.rs ├── store.rs ├── memory.rs ├── file.rs ├── secretstests.rs └── filetests.rs zeta-rs/keyring-store/ ├── BUILD.bazel ├── Cargo.toml ├── README.md └── src/ ├── lib.rs └── libtests.rs ` 当前显式文件实现位于 zeta-secrets/src/file.rs；平台 keyring adapter 位于独立的 zeta-keyring-store，避免基础 value/port crate 引入平台依赖。只有同一 crate 内多个 backend 需要共同 私有基础设施时才引入 backend/ 层级。 8. 依赖方向 允许： `text model-provider direct credential ──▶ zeta-secrets MCP runtime ─────▶ zeta-secrets Connector auth ──▶ zeta-secrets login adapter ───▶ zeta-secrets only when the provider owns token persistence App Server composition ──▶ domain auth services ` 禁止： `text zeta-secrets ──▶ model-provider / MCP / Plugin / zeta-api / zeta-client / zeta-http-client / zeta-core zeta-api ──────▶ zeta-secrets zeta-client ───▶ zeta-secrets zeta-http-client ──▶ zeta-secrets zeta-core ─────▶ zeta-secrets zeta-config ───▶ secret value Desktop renderer ──▶ SecretStore ` 9. 测试门 - load/store/replace/delete/not-found contract； - SecretValue 的 Debug 和 error negative logging； - invalid/control-character key rejection； - namespace collision； - concurrent backend access； - backend unavailable/access denied 分类； - OS keyring adapter contract 的完整 round trip；真实平台 keyring round trip 由 opt-in host test 验证； - file permission、atomic replace、crash recovery； - logout/delete 后所有 fallback copy 均不可读取； - schema、App Server DTO、Thread event 和 rollout 中无 secret-bearing field。 10. 固定决策 1. 长期保留独立 zeta-secrets crate。 2. 删除 zeta-credentials；不建立同义的统一 credential/OAuth authority。 3. Direct-provider credential 属于 zeta-model-provider，interactive login 属于 zeta-login； MCP/Connector 各自拥有登录状态机。 4. secrets 只保存 opaque bytes，不理解 token 或 account。 5. Config 只保存 reference，不保存 secret。 6. API/client/Core 不读取 secret store。 7. 本地 Connector composition 默认使用按 profile 隔离的 OS keyring；keyring operation 失败时明确 fail closed，不自动读取文件副本。显式文件 backend 只由 host 主动注入，并只在能强制 0700/0600 权限的 Unix host 启用。 8. App Server protocol 和普通 server operation 不暴露 secret；local composition 只把 SecretStore` 注入 Connector/MCP credential adapter。ChatGPT/Codex 的凭据仍归 upstream Codex App Server。"
  },
  {
    "slug": "windows-sandbox-acceptance-runbook",
    "sourcePath": "docs/windows-sandbox-acceptance-runbook.md",
    "title": "Windows 沙箱手工验收手册",
    "description": "本文属于：",
    "group": "安全与权限",
    "markdown": "> 状态：Current validation procedure；真实 Windows 结果待回填。\n>\n> 本文拥有 Windows AppContainer sandbox 的手工验收步骤、golden expectations 和结果回填格式。\n> 实现契约见\n> [`zeta-windows-sandbox` README](../zeta-rs/windows-sandbox-rs/README.md)，跨平台边界见\n> [`sandboxing.md`](sandboxing.md)。\n\n## 快速理解\n\n本文属于：\n\n- **Manual Acceptance Test Plan**：手工验收测试计划；\n- **Acceptance Runbook**：可直接交给测试人员执行的验收操作手册；\n- **Test Oracle**：用来判断实际结果 PASS/FAIL 的预期结果与规则；\n- **Golden Expectations**：本文为 Test Oracle 固定下来的具体输出、exit code 和副作用。\n\n测试人员只负责记录实际结果和证据，不应根据实际行为修改预期结果。实际结果与本文不一致时，\n应作为实现缺陷、环境不满足或文档错误返回维护者分析。\n\n| 你是谁 | 如何使用本文 | 不应该做什么 |\n| --- | --- | --- |\n| 测试人员 | 按顺序执行步骤并记录实际结果和证据 | 根据实际行为修改预期结果 |\n| 实现维护者 | 分析失败属于实现、环境还是文档问题 | 把失败步骤直接标成通过 |\n| 发布负责人 | 用完整结果判断 Windows 沙箱是否可交付 | 在缺少证据时推断通过 |\n\n## 2. 验收范围\n\n本 Runbook 验证当前已实现的 Windows v1 contract：\n\n| 能力 | 验收目标 |\n| --- | --- |\n| Package | Windows 包必须包含 command runner、sandbox setup 和 `rg.exe` |\n| Helper discovery protocol | 两个 helper 返回精确 probe 字符串 |\n| ReadOnly | 可以读取 Workspace，不能写入 |\n| WorkspaceWrite | 可以写 Workspace，但不能写 protected metadata |\n| Profile isolation | Workspace 与 ro/rw mode 不累积 authority |\n| Filesystem boundary | 不能读写 Workspace 外的用户文件 |\n| NetworkDenied | 不能访问 host loopback HTTP server |\n| Process containment | sandboxed program 不能创建子进程 |\n| Denial evidence | setup/pre-launch failure 使用保留 exit code `125` |\n| Exit-code authenticity | inner process 的 `125` 被重映射为 `124` |\n| Temporary program | staged executable 在 runner 退出后被清理 |\n\n不在本次验收范围：\n\n- Codex dedicated local users、private desktop 或 WFP firewall；\n- shell/PTY 产品能力；\n- managed network proxy；\n- installer/uninstaller 对历史 AppContainer profile 和 ACL 的清理；\n- 性能与大规模 Workspace benchmark。\n\n## 3. 安全要求\n\n必须满足：\n\n1. 使用 Windows 10/11 x64 或 ARM64 的普通、非 elevated PowerShell；\n2. 使用本文创建的临时 Workspace；\n3. **不要在真实代码仓库、用户文档目录或生产机器目录上执行 ACL 测试**；\n4. 测试目录在结果交付前不要删除；\n5. 不要使用公司 secret、token 或真实敏感文件作为 outside-workspace fixture。\n\n原因：AppContainer profile 和 Workspace ACL 是持久 Windows 状态，不会随测试进程退出自动撤销。\n\n## 4. 环境与证据目录\n\n从 Zeta repository root 打开 PowerShell 7，执行：\n\n```powershell\nSet-StrictMode -Version Latest\n$ErrorActionPreference = \"Stop\"\n\n$Repo = (Resolve-Path \".\").Path\n$Target = \"x86_64-pc-windows-msvc\" # ARM64 改为 aarch64-pc-windows-msvc\n$RunRoot = Join-Path $env:TEMP (\"zeta-windows-sandbox-acceptance-\" + [guid]::NewGuid())\n$Package = Join-Path $RunRoot \"package\"\n$WorkspaceA = Join-Path $RunRoot \"workspace-a\"\n$WorkspaceB = Join-Path $RunRoot \"workspace-b\"\n$Outside = Join-Path $RunRoot \"outside\"\n$Transcript = Join-Path $RunRoot \"acceptance-transcript.txt\"\n\nNew-Item -ItemType Directory -Path $RunRoot | Out-Null\nNew-Item -ItemType Directory -Path $WorkspaceA | Out-Null\nNew-Item -ItemType Directory -Path $WorkspaceB | Out-Null\nNew-Item -ItemType Directory -Path $Outside | Out-Null\nNew-Item -ItemType Directory -Path (Join-Path $WorkspaceA \".git\") | Out-Null\n\nStart-Transcript -Path $Transcript\n\nGet-ComputerInfo |\n  Select-Object WindowsProductName, WindowsVersion, OsBuildNumber, OsArchitecture\nrustc -Vv\ncargo -V\npython --version\n\n$Identity = [Security.Principal.WindowsIdentity]::GetCurrent()\n$Principal = [Security.Principal.WindowsPrincipal]::new($Identity)\n$Elevated = $Principal.IsInRole(\n  [Security.Principal.WindowsBuiltInRole]::Administrator\n)\n\"Elevated=$Elevated\"\n```\n\nGolden expectation：\n\n- `Elevated=False`；\n- Windows、Rust、Cargo、Python 信息完整写入 transcript；\n- `$RunRoot`、两个 Workspace 和 outside fixture 是全新临时目录。\n\n如果 `Elevated=True`，停止测试并重新打开非管理员 PowerShell。\n\n## 5. WPK-01：构建 canonical Windows 包\n\n执行：\n\n```powershell\npython scripts/build_zeta_package.py `\n  --target $Target `\n  --package-dir $Package\n\nif ($LASTEXITCODE -ne 0) {\n  throw \"package build failed with exit code $LASTEXITCODE\"\n}\n\n$Runner = Join-Path $Package \"zeta-resources\\zeta-command-runner.exe\"\n$Setup = Join-Path $Package \"zeta-resources\\zeta-windows-sandbox-setup.exe\"\n$Rg = Join-Path $Package \"zeta-path\\rg.exe\"\n$MetadataPath = Join-Path $Package \"zeta-package.json\"\n$Metadata = Get-Content $MetadataPath -Raw | ConvertFrom-Json\n\n@($Runner, $Setup, $Rg, $MetadataPath) |\n  ForEach-Object { [pscustomobject]@{ Path = $_; Exists = Test-Path $_ -PathType Leaf } } |\n  Format-Table -AutoSize\n```\n\nGolden expectation：\n\n- package build exit code 为 `0`；\n- 四个文件的 `Exists` 全部为 `True`；\n- `zeta-package.json.target` 等于 `$Target`；\n- `components.windowsSandbox.source` 为 `cargo-build`，或使用显式 prebuilt helper 时为\n  `local-override` / `mixed`。\n\n## 6. WPK-02：校验辅助程序摘要\n\n执行：\n\n```powershell\n$RunnerHash = (Get-FileHash $Runner -Algorithm SHA256).Hash.ToLowerInvariant()\n$SetupHash = (Get-FileHash $Setup -Algorithm SHA256).Hash.ToLowerInvariant()\n\n[pscustomobject]@{\n  RunnerHashMatches = (\n    $RunnerHash -eq $Metadata.components.windowsSandbox.commandRunnerSha256\n  )\n  SetupHashMatches = (\n    $SetupHash -eq $Metadata.components.windowsSandbox.sandboxSetupSha256\n  )\n} | Format-List\n```\n\nGolden expectation：\n\n- `RunnerHashMatches=True`；\n- `SetupHashMatches=True`。\n\n## 7. WPR-01：辅助程序协议探测\n\n执行：\n\n```powershell\n$RunnerProbe = (& $Runner --probe | Out-String).Trim()\n$RunnerProbeCode = $LASTEXITCODE\n$SetupProbe = (& $Setup --probe | Out-String).Trim()\n$SetupProbeCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  RunnerCode = $RunnerProbeCode\n  RunnerProbe = $RunnerProbe\n  SetupCode = $SetupProbeCode\n  SetupProbe = $SetupProbe\n} | Format-List\n```\n\nGolden expectation：\n\n```text\nRunnerCode  = 0\nRunnerProbe = zeta-windows-command-runner-v1\nSetupCode   = 0\nSetupProbe  = zeta-windows-sandbox-setup-v1\n```\n\n其他文字、额外 stdout 或不同 protocol version 都判定失败。\n\n## 8. 创建测试样例\n\n执行：\n\n```powershell\n$Sentinel = \"ZETA_WINDOWS_SANDBOX_SENTINEL_7F3A\"\n$Needle = Join-Path $WorkspaceA \"needle.txt\"\n$Secret = Join-Path $Outside \"outside-secret.txt\"\n$WorkspaceBFile = Join-Path $WorkspaceB \"workspace-b.txt\"\n$FsProbeSource = Join-Path $Outside \"fs-probe.rs\"\n$FsProbe = Join-Path $Outside \"fs-probe.exe\"\n$Cmd = Join-Path $env:SystemRoot \"System32\\cmd.exe\"\n$Curl = Join-Path $env:SystemRoot \"System32\\curl.exe\"\n\nSet-Content -Path $Needle -Value $Sentinel -NoNewline\nSet-Content -Path $Secret -Value $Sentinel -NoNewline\nSet-Content -Path $WorkspaceBFile -Value \"workspace-b\" -NoNewline\n\n@'\nuse std::process::exit;\n\nfn main() {\n    let mut arguments = std::env::args_os().skip(1);\n    let operation = arguments.next().expect(\"missing operation\");\n    match operation.to_str() {\n        Some(\"read\") => {\n            let path = arguments.next().expect(\"missing read path\");\n            match std::fs::read_to_string(path) {\n                Ok(value) => print!(\"{value}\"),\n                Err(error) => {\n                    eprintln!(\"read-failed: {error}\");\n                    exit(31);\n                }\n            }\n        }\n        Some(\"write\") => {\n            let path = arguments.next().expect(\"missing write path\");\n            let value = arguments.next().expect(\"missing write value\");\n            if let Err(error) = std::fs::write(path, value.to_string_lossy().as_bytes()) {\n                eprintln!(\"write-failed: {error}\");\n                exit(32);\n            }\n        }\n        Some(\"exit\") => {\n            let code = arguments\n                .next()\n                .expect(\"missing exit code\")\n                .to_string_lossy()\n                .parse()\n                .expect(\"invalid exit code\");\n            exit(code);\n        }\n        _ => exit(2),\n    }\n}\n'@ | Set-Content -Path $FsProbeSource -Encoding UTF8\n\nrustc $FsProbeSource -o $FsProbe\nif ($LASTEXITCODE -ne 0) {\n  throw \"filesystem probe compilation failed\"\n}\n\n$StagedBefore = @(\n  Get-ChildItem $env:TEMP -Directory -Filter \"zeta-sandbox-program-*\" -ErrorAction SilentlyContinue |\n    Select-Object -ExpandProperty FullName\n)\n```\n\n## 9. WRT-01：ReadOnly 可以运行打包的 rg\n\n执行：\n\n```powershell\n$RgOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $Rg --no-heading --line-number $Sentinel . 2>&1 |\n  Out-String\n)\n$RgCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $RgCode\n  ContainsNeedle = $RgOutput.Contains(\"needle.txt\")\n  ContainsSentinel = $RgOutput.Contains($Sentinel)\n  Output = $RgOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=0`；\n- `ContainsNeedle=True`；\n- `ContainsSentinel=True`；\n- output 不含 `zeta-windows-sandbox:`。\n\n## 10. WRT-02：ReadOnly 拒绝 Workspace 写入\n\n执行：\n\n```powershell\n$ReadOnlyMarker = Join-Path $WorkspaceA \"read-only-write.txt\"\n$ReadOnlyOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe write $ReadOnlyMarker \"blocked\" 2>&1 |\n  Out-String\n)\n$ReadOnlyCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $ReadOnlyCode\n  MarkerExists = Test-Path $ReadOnlyMarker\n  Output = $ReadOnlyOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=32`；\n- `MarkerExists=False`。\n\nWindows 本地化后的 access-denied 文案不作为 golden string；文件不存在才是 authoritative result。\n\n## 11. WRT-03：WorkspaceWrite 允许普通 Workspace 写入\n\n执行：\n\n```powershell\n$WriteMarker = Join-Path $WorkspaceA \"workspace-write.txt\"\n$WriteOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access workspace-write `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe write $WriteMarker \"workspace-write-ok\" 2>&1 |\n  Out-String\n)\n$WriteCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $WriteCode\n  MarkerExists = Test-Path $WriteMarker\n  MarkerContent = if (Test-Path $WriteMarker) {\n    (Get-Content $WriteMarker -Raw).Trim()\n  } else {\n    \"\"\n  }\n  Output = $WriteOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=0`；\n- `MarkerExists=True`；\n- `MarkerContent=workspace-write-ok`。\n\n## 12. WRT-04：rw 配置档案不得污染同一 Workspace 的 ro 配置档案\n\nWRT-03 已经创建过 WorkspaceWrite profile。现在再次以 ReadOnly 执行：\n\n```powershell\n$ReadAfterWriteMarker = Join-Path $WorkspaceA \"ro-after-rw.txt\"\n$ReadAfterWriteOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe write $ReadAfterWriteMarker \"blocked\" 2>&1 |\n  Out-String\n)\n$ReadAfterWriteCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $ReadAfterWriteCode\n  MarkerExists = Test-Path $ReadAfterWriteMarker\n  Output = $ReadAfterWriteOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=32`；\n- `MarkerExists=False`。\n\n如果文件被创建，说明 ro/rw profile authority 发生累积，必须判定失败。\n\n## 13. WRT-05：WorkspaceWrite 拒绝 protected 元数据\n\n执行：\n\n```powershell\n$GitMarker = Join-Path $WorkspaceA \".git\\zeta-write-probe.txt\"\n$GitOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access workspace-write `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe write $GitMarker \"blocked\" 2>&1 |\n  Out-String\n)\n$GitCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $GitCode\n  MarkerExists = Test-Path $GitMarker\n  Output = $GitOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=32`；\n- `MarkerExists=False`。\n\n## 14. WRT-06：拒绝读取与写入 Workspace 外文件\n\n执行读取测试：\n\n```powershell\n$OutsideReadOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access workspace-write `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe read $Secret 2>&1 |\n  Out-String\n)\n$OutsideReadCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $OutsideReadCode\n  SecretLeaked = $OutsideReadOutput.Contains($Sentinel)\n  Output = $OutsideReadOutput.Trim()\n} | Format-List\n```\n\n执行写入测试：\n\n```powershell\n$OutsideWriteMarker = Join-Path $Outside \"outside-write.txt\"\n$OutsideWriteOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access workspace-write `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe write $OutsideWriteMarker \"blocked\" 2>&1 |\n  Out-String\n)\n$OutsideWriteCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $OutsideWriteCode\n  MarkerExists = Test-Path $OutsideWriteMarker\n  Output = $OutsideWriteOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- read `ExitCode=31`；\n- write `ExitCode=32`；\n- `SecretLeaked=False`；\n- `MarkerExists=False`。\n\n## 15. WRT-07：不同 Workspace 不得共享配置档案权威\n\nWorkspace A 已经获得 ro 和 rw ACL。以 Workspace B 的 ReadOnly profile 尝试读取 A：\n\n```powershell\n$CrossWorkspaceOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceB `\n    --cwd $WorkspaceB `\n    -- $FsProbe read $WriteMarker 2>&1 |\n  Out-String\n)\n$CrossWorkspaceCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $CrossWorkspaceCode\n  ContentLeaked = $CrossWorkspaceOutput.Contains(\"workspace-write-ok\")\n  Output = $CrossWorkspaceOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=31`；\n- `ContentLeaked=False`。\n\n## 16. WRT-08：NetworkDenied 拒绝宿主回环\n\n先证明 host server 正常：\n\n```powershell\n$Server = Start-Process `\n  -FilePath \"python\" `\n  -ArgumentList \"-m\", \"http.server\", \"8765\", \"--bind\", \"127.0.0.1\" `\n  -WorkingDirectory $WorkspaceA `\n  -WindowStyle Hidden `\n  -PassThru\n\nStart-Sleep -Seconds 2\n\n$HostNetworkOutput = (& $Curl --silent --show-error --max-time 5 `\n  \"http://127.0.0.1:8765/needle.txt\" 2>&1 | Out-String)\n$HostNetworkCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  HostExitCode = $HostNetworkCode\n  HostContainsSentinel = $HostNetworkOutput.Contains($Sentinel)\n} | Format-List\n```\n\n环境前置 golden expectation：\n\n- `HostExitCode=0`；\n- `HostContainsSentinel=True`。\n\n然后执行 sandboxed curl：\n\n```powershell\ntry {\n  $SandboxNetworkOutput = (\n    & $Runner `\n      --setup-helper $Setup `\n      --access read-only `\n      --workspace $WorkspaceA `\n      --cwd $WorkspaceA `\n      -- $Curl --silent --show-error --max-time 5 `\n        \"http://127.0.0.1:8765/needle.txt\" 2>&1 |\n    Out-String\n  )\n  $SandboxNetworkCode = $LASTEXITCODE\n\n  [pscustomobject]@{\n    ExitCode = $SandboxNetworkCode\n    ResponseLeaked = $SandboxNetworkOutput.Contains($Sentinel)\n    Output = $SandboxNetworkOutput.Trim()\n  } | Format-List\n}\nfinally {\n  Stop-Process -Id $Server.Id -Force -ErrorAction SilentlyContinue\n}\n```\n\nGolden expectation：\n\n- `ExitCode` 非 `0`；\n- `ResponseLeaked=False`。\n\n如果 host 前置检查失败，本项记为 `BLOCKED`，不能记为 sandbox `PASS`。\n\n## 17. WRT-09：拒绝创建子进程\n\n创建并编译一个只用于验收的 native probe：\n\n```powershell\n$SpawnProbeSource = Join-Path $Outside \"spawn-probe.rs\"\n$SpawnProbe = Join-Path $Outside \"spawn-probe.exe\"\n$NestedMarker = Join-Path $WorkspaceA \"nested-child.txt\"\n\n@'\nuse std::process::{Command, exit};\n\nfn main() {\n    let mut arguments = std::env::args_os().skip(1);\n    let marker = arguments.next().expect(\"missing marker\");\n    let command = arguments.next().expect(\"missing command\");\n    let script = format!(\"echo nested>\\\"{}\\\"\", marker.to_string_lossy());\n    match Command::new(command)\n        .args([\"/d\", \"/s\", \"/c\"])\n        .arg(script)\n        .status()\n    {\n        Err(error) => {\n            eprintln!(\"spawn-blocked: {error}\");\n            exit(41);\n        }\n        Ok(status) => {\n            eprintln!(\"child-started: {status}\");\n            exit(42);\n        }\n    }\n}\n'@ | Set-Content -Path $SpawnProbeSource -Encoding UTF8\n\nrustc $SpawnProbeSource -o $SpawnProbe\nif ($LASTEXITCODE -ne 0) {\n  throw \"spawn probe compilation failed\"\n}\n\n$SpawnOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access workspace-write `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $SpawnProbe $NestedMarker $Cmd 2>&1 |\n  Out-String\n)\n$SpawnCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $SpawnCode\n  MarkerExists = Test-Path $NestedMarker\n  SpawnWasBlocked = $SpawnOutput.Contains(\"spawn-blocked:\")\n  Output = $SpawnOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=41`；\n- `MarkerExists=False`；\n- `SpawnWasBlocked=True`；\n- output 不含 `child-started:`。\n\n## 18. WRT-10：inner exit code 不能伪造强制执行拒绝\n\n执行：\n\n```powershell\n$ReservedOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceA `\n    -- $FsProbe exit 125 2>&1 |\n  Out-String\n)\n$ReservedCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $ReservedCode\n  HasEnforcementMarker = $ReservedOutput.Contains(\"zeta-windows-sandbox:\")\n  Output = $ReservedOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=124`；\n- `HasEnforcementMarker=False`。\n\n`125` 是 runner 自己的可信 pre-launch failure code；child 返回的 `125` 必须被重映射。\n\n## 19. WRT-11：真实 pre-launch 失败使用保留状态\n\n故意把 cwd 指向 Workspace 外：\n\n```powershell\n$PrelaunchOutput = (\n  & $Runner `\n    --setup-helper $Setup `\n    --access read-only `\n    --workspace $WorkspaceA `\n    --cwd $WorkspaceB `\n    -- $Rg --files 2>&1 |\n  Out-String\n)\n$PrelaunchCode = $LASTEXITCODE\n\n[pscustomobject]@{\n  ExitCode = $PrelaunchCode\n  HasEnforcementMarker = $PrelaunchOutput.Contains(\"zeta-windows-sandbox:\")\n  MentionsOutsideWorkspace = $PrelaunchOutput.Contains(\n    \"working directory resolves outside workspace\"\n  )\n  Output = $PrelaunchOutput.Trim()\n} | Format-List\n```\n\nGolden expectation：\n\n- `ExitCode=125`；\n- `HasEnforcementMarker=True`；\n- `MentionsOutsideWorkspace=True`；\n- inner `rg` 没有启动。\n\n## 20. WRT-12：staged executable 被清理\n\n执行：\n\n```powershell\n$StagedAfter = @(\n  Get-ChildItem $env:TEMP -Directory -Filter \"zeta-sandbox-program-*\" -ErrorAction SilentlyContinue |\n    Select-Object -ExpandProperty FullName\n)\n$NewStagedDirectories = @($StagedAfter | Where-Object { $_ -notin $StagedBefore })\n\n[pscustomobject]@{\n  NewStagedDirectoryCount = $NewStagedDirectories.Count\n  NewStagedDirectories = ($NewStagedDirectories -join \";\")\n} | Format-List\n```\n\nGolden expectation：\n\n- `NewStagedDirectoryCount=0`。\n\n如果 antivirus 暂时持有 executable，可等待 10 秒后重试一次；仍有残留则判定失败并保留目录。\n\n## 21. 完成与证据\n\n执行：\n\n```powershell\nicacls $WorkspaceA\nicacls (Join-Path $WorkspaceA \".git\")\nStop-Transcript\n\n\"Evidence root: $RunRoot\"\n```\n\n不要立即删除 `$RunRoot`。先把以下文件交给维护者：\n\n1. `acceptance-transcript.txt`；\n2. `zeta-package.json`；\n3. 下方已回填的结果表；\n4. 所有 `FAIL` / `BLOCKED` 项的完整 stdout、stderr 和 exit code；\n5. `icacls` 输出。\n\n确认维护者收到证据后，才可清理：\n\n```powershell\nRemove-Item -Recurse -Force $RunRoot\n```\n\n## 22. 结果回填表\n\n测试人员复制此表并填写 `实际结果`、`判定` 和 `证据位置`：\n\n| ID | Golden expectation 摘要 | 实际结果 | 判定 | 证据位置 |\n| --- | --- | --- | --- | --- |\n| ENV-01 | 非 elevated Windows 10/11；工具版本完整 |  | PASS/FAIL |  |\n| WPK-01 | package build=0；四个 required files 存在 |  | PASS/FAIL |  |\n| WPK-02 | runner/setup SHA-256 与 metadata 一致 |  | PASS/FAIL |  |\n| WPR-01 | 两个 probe code=0 且字符串精确匹配 |  | PASS/FAIL |  |\n| WRT-01 | sandboxed rg code=0，读取到 sentinel |  | PASS/FAIL |  |\n| WRT-02 | ReadOnly 写入 code=32，marker 不存在 |  | PASS/FAIL |  |\n| WRT-03 | WorkspaceWrite 写入成功 |  | PASS/FAIL |  |\n| WRT-04 | rw profile 未污染 ro；write code=32 |  | PASS/FAIL |  |\n| WRT-05 | `.git` 写入 code=32，marker 不存在 |  | PASS/FAIL |  |\n| WRT-06 | Workspace 外 read=31、write=32，无泄漏 |  | PASS/FAIL |  |\n| WRT-07 | Workspace B read A code=31，无泄漏 |  | PASS/FAIL |  |\n| WRT-08 | host loopback 可用；sandbox loopback 被拒绝 |  | PASS/FAIL/BLOCKED |  |\n| WRT-09 | nested process 被拒绝，marker 不存在 |  | PASS/FAIL |  |\n| WRT-10 | child 125 被映射为 124，无 enforcement marker |  | PASS/FAIL |  |\n| WRT-11 | pre-launch failure=125，含可信 marker |  | PASS/FAIL |  |\n| WRT-12 | 没有新增 staged executable directory |  | PASS/FAIL |  |\n\n## 23. 验收门槛\n\n结论只允许以下三种：\n\n| 结论 | 条件 |\n| --- | --- |\n| `ACCEPTED` | ENV、WPK、WPR、WRT 全部 PASS |\n| `REJECTED` | 任一安全项 FAIL |\n| `INCONCLUSIVE` | 环境前置不成立，或 WRT-08 为 BLOCKED |\n\n以下任一结果必须直接 `REJECTED`：\n\n- ReadOnly 或 outside-workspace 写入成功；\n- outside secret 出现在 sandbox output；\n- `.git` marker 被创建；\n- Workspace/profile authority 串用；\n- loopback response 泄漏；\n- nested child marker 被创建；\n- child 自己能够让 runner 返回可信 denial code `125`；\n- helper 缺失、digest 不匹配或 probe protocol 不一致。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-验收范围",
        "title": "2. 验收范围"
      },
      {
        "depth": 2,
        "id": "3-安全要求",
        "title": "3. 安全要求"
      },
      {
        "depth": 2,
        "id": "4-环境与证据目录",
        "title": "4. 环境与证据目录"
      },
      {
        "depth": 2,
        "id": "5-wpk-01构建-canonical-windows-包",
        "title": "5. WPK-01：构建 canonical Windows 包"
      },
      {
        "depth": 2,
        "id": "6-wpk-02校验辅助程序摘要",
        "title": "6. WPK-02：校验辅助程序摘要"
      },
      {
        "depth": 2,
        "id": "7-wpr-01辅助程序协议探测",
        "title": "7. WPR-01：辅助程序协议探测"
      },
      {
        "depth": 2,
        "id": "8-创建测试样例",
        "title": "8. 创建测试样例"
      },
      {
        "depth": 2,
        "id": "9-wrt-01readonly-可以运行打包的-rg",
        "title": "9. WRT-01：ReadOnly 可以运行打包的 rg"
      },
      {
        "depth": 2,
        "id": "10-wrt-02readonly-拒绝-workspace-写入",
        "title": "10. WRT-02：ReadOnly 拒绝 Workspace 写入"
      },
      {
        "depth": 2,
        "id": "11-wrt-03workspacewrite-允许普通-workspace-写入",
        "title": "11. WRT-03：WorkspaceWrite 允许普通 Workspace 写入"
      },
      {
        "depth": 2,
        "id": "12-wrt-04rw-配置档案不得污染同一-workspace-的-ro-配置档案",
        "title": "12. WRT-04：rw 配置档案不得污染同一 Workspace 的 ro 配置档案"
      },
      {
        "depth": 2,
        "id": "13-wrt-05workspacewrite-拒绝-protected-元数据",
        "title": "13. WRT-05：WorkspaceWrite 拒绝 protected 元数据"
      },
      {
        "depth": 2,
        "id": "14-wrt-06拒绝读取与写入-workspace-外文件",
        "title": "14. WRT-06：拒绝读取与写入 Workspace 外文件"
      },
      {
        "depth": 2,
        "id": "15-wrt-07不同-workspace-不得共享配置档案权威",
        "title": "15. WRT-07：不同 Workspace 不得共享配置档案权威"
      },
      {
        "depth": 2,
        "id": "16-wrt-08networkdenied-拒绝宿主回环",
        "title": "16. WRT-08：NetworkDenied 拒绝宿主回环"
      },
      {
        "depth": 2,
        "id": "17-wrt-09拒绝创建子进程",
        "title": "17. WRT-09：拒绝创建子进程"
      },
      {
        "depth": 2,
        "id": "18-wrt-10inner-exit-code-不能伪造强制执行拒绝",
        "title": "18. WRT-10：inner exit code 不能伪造强制执行拒绝"
      },
      {
        "depth": 2,
        "id": "19-wrt-11真实-pre-launch-失败使用保留状态",
        "title": "19. WRT-11：真实 pre-launch 失败使用保留状态"
      },
      {
        "depth": 2,
        "id": "20-wrt-12staged-executable-被清理",
        "title": "20. WRT-12：staged executable 被清理"
      },
      {
        "depth": 2,
        "id": "21-完成与证据",
        "title": "21. 完成与证据"
      },
      {
        "depth": 2,
        "id": "22-结果回填表",
        "title": "22. 结果回填表"
      },
      {
        "depth": 2,
        "id": "23-验收门槛",
        "title": "23. 验收门槛"
      }
    ],
    "searchText": " 状态：Current validation procedure；真实 Windows 结果待回填。 本文拥有 Windows AppContainer sandbox 的手工验收步骤、golden expectations 和结果回填格式。 实现契约见 zeta-windows-sandbox README，跨平台边界见 sandboxing.md。 快速理解 本文属于： - Manual Acceptance Test Plan：手工验收测试计划； - Acceptance Runbook：可直接交给测试人员执行的验收操作手册； - Test Oracle：用来判断实际结果 PASS/FAIL 的预期结果与规则； - Golden Expectations：本文为 Test Oracle 固定下来的具体输出、exit code 和副作用。 测试人员只负责记录实际结果和证据，不应根据实际行为修改预期结果。实际结果与本文不一致时， 应作为实现缺陷、环境不满足或文档错误返回维护者分析。 你是谁 如何使用本文 不应该做什么 --- --- --- 测试人员 按顺序执行步骤并记录实际结果和证据 根据实际行为修改预期结果 实现维护者 分析失败属于实现、环境还是文档问题 把失败步骤直接标成通过 发布负责人 用完整结果判断 Windows 沙箱是否可交付 在缺少证据时推断通过 2. 验收范围 本 Runbook 验证当前已实现的 Windows v1 contract： 能力 验收目标 --- --- Package Windows 包必须包含 command runner、sandbox setup 和 rg.exe Helper discovery protocol 两个 helper 返回精确 probe 字符串 ReadOnly 可以读取 Workspace，不能写入 WorkspaceWrite 可以写 Workspace，但不能写 protected metadata Profile isolation Workspace 与 ro/rw mode 不累积 authority Filesystem boundary 不能读写 Workspace 外的用户文件 NetworkDenied 不能访问 host loopback HTTP server Process containment sandboxed program 不能创建子进程 Denial evidence setup/pre-launch failure 使用保留 exit code 125 Exit-code authenticity inner process 的 125 被重映射为 124 Temporary program staged executable 在 runner 退出后被清理 不在本次验收范围： - Codex dedicated local users、private desktop 或 WFP firewall； - shell/PTY 产品能力； - managed network proxy； - installer/uninstaller 对历史 AppContainer profile 和 ACL 的清理； - 性能与大规模 Workspace benchmark。 3. 安全要求 必须满足： 1. 使用 Windows 10/11 x64 或 ARM64 的普通、非 elevated PowerShell； 2. 使用本文创建的临时 Workspace； 3. 不要在真实代码仓库、用户文档目录或生产机器目录上执行 ACL 测试； 4. 测试目录在结果交付前不要删除； 5. 不要使用公司 secret、token 或真实敏感文件作为 outside-workspace fixture。 原因：AppContainer profile 和 Workspace ACL 是持久 Windows 状态，不会随测试进程退出自动撤销。 4. 环境与证据目录 从 Zeta repository root 打开 PowerShell 7，执行： ``powershell Set-StrictMode -Version Latest $ErrorActionPreference = \"Stop\" $Repo = Resolve-Path \".\" .Path $Target = \"x8664-pc-windows-msvc\" ARM64 改为 aarch64-pc-windows-msvc $RunRoot = Join-Path $env:TEMP \"zeta-windows-sandbox-acceptance-\" + guid ::NewGuid $Package = Join-Path $RunRoot \"package\" $WorkspaceA = Join-Path $RunRoot \"workspace-a\" $WorkspaceB = Join-Path $RunRoot \"workspace-b\" $Outside = Join-Path $RunRoot \"outside\" $Transcript = Join-Path $RunRoot \"acceptance-transcript.txt\" New-Item -ItemType Directory -Path $RunRoot Out-Null New-Item -ItemType Directory -Path $WorkspaceA Out-Null New-Item -ItemType Directory -Path $WorkspaceB Out-Null New-Item -ItemType Directory -Path $Outside Out-Null New-Item -ItemType Directory -Path Join-Path $WorkspaceA \".git\" Out-Null Start-Transcript -Path $Transcript Get-ComputerInfo Select-Object WindowsProductName, WindowsVersion, OsBuildNumber, OsArchitecture rustc -Vv cargo -V python --version $Identity = Security.Principal.WindowsIdentity ::GetCurrent $Principal = Security.Principal.WindowsPrincipal ::new $Identity $Elevated = $Principal.IsInRole Security.Principal.WindowsBuiltInRole ::Administrator \"Elevated=$Elevated\" ` Golden expectation： - Elevated=False； - Windows、Rust、Cargo、Python 信息完整写入 transcript； - $RunRoot、两个 Workspace 和 outside fixture 是全新临时目录。 如果 Elevated=True，停止测试并重新打开非管理员 PowerShell。 5. WPK-01：构建 canonical Windows 包 执行： `powershell python scripts/buildzetapackage.py --target $Target --package-dir $Package if $LASTEXITCODE -ne 0 throw \"package build failed with exit code $LASTEXITCODE\" $Runner = Join-Path $Package \"zeta-resources\\zeta-command-runner.exe\" $Setup = Join-Path $Package \"zeta-resources\\zeta-windows-sandbox-setup.exe\" $Rg = Join-Path $Package \"zeta-path\\rg.exe\" $MetadataPath = Join-Path $Package \"zeta-package.json\" $Metadata = Get-Content $MetadataPath -Raw ConvertFrom-Json @ $Runner, $Setup, $Rg, $MetadataPath ForEach-Object pscustomobject @ Path = $; Exists = Test-Path $ -PathType Leaf Format-Table -AutoSize ` Golden expectation： - package build exit code 为 0； - 四个文件的 Exists 全部为 True； - zeta-package.json.target 等于 $Target； - components.windowsSandbox.source 为 cargo-build，或使用显式 prebuilt helper 时为 local-override / mixed。 6. WPK-02：校验辅助程序摘要 执行： `powershell $RunnerHash = Get-FileHash $Runner -Algorithm SHA256 .Hash.ToLowerInvariant $SetupHash = Get-FileHash $Setup -Algorithm SHA256 .Hash.ToLowerInvariant pscustomobject @ RunnerHashMatches = $RunnerHash -eq $Metadata.components.windowsSandbox.commandRunnerSha256 SetupHashMatches = $SetupHash -eq $Metadata.components.windowsSandbox.sandboxSetupSha256 Format-List ` Golden expectation： - RunnerHashMatches=True； - SetupHashMatches=True。 7. WPR-01：辅助程序协议探测 执行： `powershell $RunnerProbe = & $Runner --probe Out-String .Trim $RunnerProbeCode = $LASTEXITCODE $SetupProbe = & $Setup --probe Out-String .Trim $SetupProbeCode = $LASTEXITCODE pscustomobject @ RunnerCode = $RunnerProbeCode RunnerProbe = $RunnerProbe SetupCode = $SetupProbeCode SetupProbe = $SetupProbe Format-List ` Golden expectation： `text RunnerCode = 0 RunnerProbe = zeta-windows-command-runner-v1 SetupCode = 0 SetupProbe = zeta-windows-sandbox-setup-v1 ` 其他文字、额外 stdout 或不同 protocol version 都判定失败。 8. 创建测试样例 执行： `powershell $Sentinel = \"ZETAWINDOWSSANDBOXSENTINEL7F3A\" $Needle = Join-Path $WorkspaceA \"needle.txt\" $Secret = Join-Path $Outside \"outside-secret.txt\" $WorkspaceBFile = Join-Path $WorkspaceB \"workspace-b.txt\" $FsProbeSource = Join-Path $Outside \"fs-probe.rs\" $FsProbe = Join-Path $Outside \"fs-probe.exe\" $Cmd = Join-Path $env:SystemRoot \"System32\\cmd.exe\" $Curl = Join-Path $env:SystemRoot \"System32\\curl.exe\" Set-Content -Path $Needle -Value $Sentinel -NoNewline Set-Content -Path $Secret -Value $Sentinel -NoNewline Set-Content -Path $WorkspaceBFile -Value \"workspace-b\" -NoNewline @' use std::process::exit; fn main let mut arguments = std::env::argsos .skip 1 ; let operation = arguments.next .expect \"missing operation\" ; match operation.tostr Some \"read\" = let path = arguments.next .expect \"missing read path\" ; match std::fs::readtostring path Ok value = print! \" value \" , Err error = eprintln! \"read-failed: error \" ; exit 31 ; Some \"write\" = let path = arguments.next .expect \"missing write path\" ; let value = arguments.next .expect \"missing write value\" ; if let Err error = std::fs::write path, value.tostringlossy .asbytes eprintln! \"write-failed: error \" ; exit 32 ; Some \"exit\" = let code = arguments .next .expect \"missing exit code\" .tostringlossy .parse .expect \"invalid exit code\" ; exit code ; = exit 2 , '@ Set-Content -Path $FsProbeSource -Encoding UTF8 rustc $FsProbeSource -o $FsProbe if $LASTEXITCODE -ne 0 throw \"filesystem probe compilation failed\" $StagedBefore = @ Get-ChildItem $env:TEMP -Directory -Filter \"zeta-sandbox-program-\" -ErrorAction SilentlyContinue Select-Object -ExpandProperty FullName ` 9. WRT-01：ReadOnly 可以运行打包的 rg 执行： `powershell $RgOutput = & $Runner --setup-helper $Setup --access read-only --workspace $WorkspaceA --cwd $WorkspaceA -- $Rg --no-heading --line-number $Sentinel . 2 &1 Out-String $RgCode = $LASTEXITCODE pscustomobject @ ExitCode = $RgCode ContainsNeedle = $RgOutput.Contains \"needle.txt\" ContainsSentinel = $RgOutput.Contains $Sentinel Output = $RgOutput.Trim Format-List `` Golden expectation： - ExitCode=0； - ContainsNeedle=True； - ContainsSentinel=True； - output 不含 zeta-windows-sandbox:。 10. WRT-02：ReadOnly 拒绝 Workspace 写入 执行： `powershell $ReadOnlyMarker = Join-Path $WorkspaceA \"read-only-write.txt\" $ReadOnlyOutput = & $Runner --setup-helper $Setup --access read-only --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe write $ReadOnlyMarker \"blocked\" 2 &1 Out-String $ReadOnlyCode = $LASTEXITCODE pscustomobject @ ExitCode = $ReadOnlyCode MarkerExists = Test-Path $ReadOnlyMarker Output = $ReadOnlyOutput.Trim Format-List `` Golden expectation： - ExitCode=32； - MarkerExists=False。 Windows 本地化后的 access-denied 文案不作为 golden string；文件不存在才是 authoritative result。 11. WRT-03：WorkspaceWrite 允许普通 Workspace 写入 执行： `powershell $WriteMarker = Join-Path $WorkspaceA \"workspace-write.txt\" $WriteOutput = & $Runner --setup-helper $Setup --access workspace-write --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe write $WriteMarker \"workspace-write-ok\" 2 &1 Out-String $WriteCode = $LASTEXITCODE pscustomobject @ ExitCode = $WriteCode MarkerExists = Test-Path $WriteMarker MarkerContent = if Test-Path $WriteMarker Get-Content $WriteMarker -Raw .Trim else \"\" Output = $WriteOutput.Trim Format-List `` Golden expectation： - ExitCode=0； - MarkerExists=True； - MarkerContent=workspace-write-ok。 12. WRT-04：rw 配置档案不得污染同一 Workspace 的 ro 配置档案 WRT-03 已经创建过 WorkspaceWrite profile。现在再次以 ReadOnly 执行： `powershell $ReadAfterWriteMarker = Join-Path $WorkspaceA \"ro-after-rw.txt\" $ReadAfterWriteOutput = & $Runner --setup-helper $Setup --access read-only --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe write $ReadAfterWriteMarker \"blocked\" 2 &1 Out-String $ReadAfterWriteCode = $LASTEXITCODE pscustomobject @ ExitCode = $ReadAfterWriteCode MarkerExists = Test-Path $ReadAfterWriteMarker Output = $ReadAfterWriteOutput.Trim Format-List `` Golden expectation： - ExitCode=32； - MarkerExists=False。 如果文件被创建，说明 ro/rw profile authority 发生累积，必须判定失败。 13. WRT-05：WorkspaceWrite 拒绝 protected 元数据 执行： `powershell $GitMarker = Join-Path $WorkspaceA \".git\\zeta-write-probe.txt\" $GitOutput = & $Runner --setup-helper $Setup --access workspace-write --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe write $GitMarker \"blocked\" 2 &1 Out-String $GitCode = $LASTEXITCODE pscustomobject @ ExitCode = $GitCode MarkerExists = Test-Path $GitMarker Output = $GitOutput.Trim Format-List `` Golden expectation： - ExitCode=32； - MarkerExists=False。 14. WRT-06：拒绝读取与写入 Workspace 外文件 执行读取测试： `powershell $OutsideReadOutput = & $Runner --setup-helper $Setup --access workspace-write --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe read $Secret 2 &1 Out-String $OutsideReadCode = $LASTEXITCODE pscustomobject @ ExitCode = $OutsideReadCode SecretLeaked = $OutsideReadOutput.Contains $Sentinel Output = $OutsideReadOutput.Trim Format-List `` 执行写入测试： `powershell $OutsideWriteMarker = Join-Path $Outside \"outside-write.txt\" $OutsideWriteOutput = & $Runner --setup-helper $Setup --access workspace-write --workspace $WorkspaceA --cwd $WorkspaceA -- $FsProbe write $OutsideWriteMarker \"blocked\" 2 &1 Out-String $OutsideWriteCode = $LASTEXITCODE pscustomobject @ ExitCode = $OutsideWriteCode MarkerExists = Test-Path $OutsideWriteMarker Output = $OutsideWriteOutput.Trim Format-List `` Golden expectation： - read ExitCode=31； - write ExitCode=32； - SecretLeaked=False； - MarkerExists=False。 15. WRT-07：不同 Workspace 不得共享配置档案权威 Workspace A 已经获得 ro 和 rw ACL。以 Workspace B 的 ReadOnly profile 尝试读取 A： `powershell $CrossWorkspaceOutput = & $Runner --setup-helper $Setup --access read-only --workspace $WorkspaceB --cwd $WorkspaceB -- $FsProbe read $WriteMarker 2 &1 Out-String $CrossWorkspaceCode = $LASTEXITCODE pscustomobject @ ExitCode = $CrossWorkspaceCode ContentLeaked = $CrossWorkspaceOutput.Contains \"workspace-write-ok\" Output = $CrossWorkspaceOutput.Trim Format-List `` Golden expectation： - ExitCode=31； - ContentLeaked=False。 16. WRT-08：NetworkDenied 拒绝宿主回环 先证明 host server 正常： `powershell $Server = Start-Process -FilePath \"python\" -ArgumentList \"-m\", \"http.server\", \"8765\", \"--bind\", \"127.0.0.1\" -WorkingDirectory $WorkspaceA -WindowStyle Hidden -PassThru Start-Sleep -Seconds 2 $HostNetworkOutput = & $Curl --silent --show-error --max-time 5 \"http://127.0.0.1:8765/needle.txt\" 2 &1 Out-String $HostNetworkCode = $LASTEXITCODE pscustomobject @ HostExitCode = $HostNetworkCode HostContainsSentinel = $HostNetworkOutput.Contains $Sentinel Format-List ` 环境前置 golden expectation： - HostExitCode=0； - HostContainsSentinel=True。 然后执行 sandboxed curl： `powershell try $SandboxNetworkOutput = & $Runner --setup-helper $Setup --access read-only --workspace $WorkspaceA --cwd $WorkspaceA -- $Curl --silent --show-error --max-time 5 \"http://127.0.0.1:8765/needle.txt\" 2 &1 Out-String $SandboxNetworkCode = $LASTEXITCODE pscustomobject @ ExitCode = $SandboxNetworkCode ResponseLeaked = $SandboxNetworkOutput.Contains $Sentinel Output = $SandboxNetworkOutput.Trim Format-List finally "
  },
  {
    "slug": "zeta-api",
    "sourcePath": "docs/zeta-api.md",
    "title": "模型 API 协议",
    "description": "zeta-api 是纯模型 API 协议层。它接收 provider-independent canonical model value，负责：",
    "group": "API 与协议",
    "markdown": "> - 物理位置：`zeta-rs/zeta-api/`\n> - Rust crate：`zeta_api`\n> - 层次：模型 API 协议层\n> - 当前状态：OpenAI Responses、OpenAI-compatible Chat Completions 与 Anthropic Messages 已具备\n>   unary codec、原生 HTTP/SSE invocation、canonical delta 与 terminal response assembly\n> - Crate codec 与 decoder 实现：[`zeta-rs/zeta-api/README.md`](../zeta-rs/zeta-api/README.md)\n> - Canonical contract：[`protocol.md`](protocol.md#6-provider-independent-model-contract)\n> - Provider runtime：[`model-provider.md`](model-provider.md)\n> - Operation client：[`zeta-client.md`](zeta-client.md)\n> - 底层网络：[`zeta-http-client` README](../zeta-rs/http-client/README.md)\n> - Direct-provider credential：[`model-provider.md`](model-provider.md#6-供应商凭据与-codex-边界)\n> - Secret persistence：[`secrets.md`](secrets.md)\n> - Model catalog control plane：[`models-manager.md`](models-manager.md)\n> - Subscription runtime adapter：[`codex-app-server.md`](codex-app-server.md)\n\n> Provider 官方资料核对日期：2026-07-26。请求字段、事件类型、缓存语义和错误结构会持续变化；\n> 实现必须以官方文档和脱敏 contract fixture 为准，不能仅凭 OpenAI-compatible 标签推断。\n\n## 快速理解\n\n`zeta-api` 是纯模型 API 协议层。它接收 provider-independent canonical model value，负责：\n\n- 定义 API relative endpoint、HTTP method 和协议 headers；\n- 将 canonical request 编码为具体 API request body；\n- 将 unary response/error 解码为 canonical response/error；\n- 将已经完成 framing 的 SSE event 解码为 canonical stream event；\n- 解释 Provider 级 terminal、heartbeat、usage、tool、reasoning 和 prompt-cache 字段；\n- 提供 catalog endpoint 的 request/response codec。\n\n它不再拥有 Provider registry，也不再实现 transport、retry、SSE framing 或 telemetry。\n\n目标横向结构是：\n\n```text\nendpoint/    requests/    sse/\n```\n\n三者同级：\n\n- `endpoint/` 描述 method、relative path、required headers 和所绑定的 codec；\n- `requests/` 描述 request/unary response/error JSON；\n- `sse/` 描述具体 API 的 SSE event schema、lifecycle 和 canonical assembly。\n\n通用 HTTP/WebSocket backend、proxy/TLS/pool 与 transport diagnostics 属于 `zeta-http-client`；\noperation retry、SSE/NDJSON framing 和 operation telemetry 属于 `zeta-client`。\n\n| 需要处理的内容 | 本层是否负责 | 交给谁 |\n| --- | --- | --- |\n| 把统一模型请求编码成供应商 JSON | ✅ | 本层 |\n| 解释供应商响应、错误和流式事件 | ✅ | 本层 |\n| 选择供应商、模型和凭据 | ❌ | 模型调用系统 |\n| 判断是否安全重试并执行等待 | ❌ | 模型调用操作层 |\n| 建立 HTTP 连接、代理和 TLS | ❌ | 网络层 |\n| 推进 Agent Turn 和工具循环 | ❌ | 会话与执行系统 |\n\n## 2. 四层关系\n\n```text\nzeta-model-provider-config\n  声明 Provider、默认 base URL、允许的 API profile\n                │\n                ▼\nzeta-model-provider\n  解析 credential/target，选择 API endpoint 和 retry policy\n                │\n                ▼\nzeta-api\n  endpoint + requests + SSE protocol codec\n                │\n                ▼\nzeta-client\n  operation retry + framing + telemetry\n                │\n                ▼\nzeta-http-client\n  HTTP/WebSocket execution + network policy\n```\n\n依赖与控制流不是同一个方向。Rust 依赖建议为：\n\n```text\nzeta-http-client\n       ▲\n       │\nzeta-client      zeta-protocol\n      ▲              ▲\n      └──── zeta-api ─┘\n               ▲\n               │\n      zeta-model-provider\n```\n\n规则：\n\n- `zeta-client` 不依赖 `zeta-api`；\n- `zeta-http-client` 不依赖 `zeta-client` 或 `zeta-api`；\n- `zeta-api` 可以使用 operation client 的 request/response/SSE value；\n- `zeta-api` 不依赖 model-provider/config/models-manager/Core；\n- model-provider 选择 `zeta-api` endpoint 并注入 resolved runtime values；\n- Provider registry 只存在于 model-provider。\n\n## 3. 当前实现审计\n\n当前 crate 已实现：\n\n- provider-independent `ApiEndpoint` 和 `ApiProtocol`；\n- `endpoint/`、`requests/`、`sse/` 三个顶级 codec 模块；\n- OpenAI Responses unary codec；\n- Anthropic Messages unary codec；\n- OpenAI-compatible Chat Completions unary codec；\n- OpenAI Responses 原生 HTTP/SSE invocation 与 canonical text/reasoning delta decoder；\n- Anthropic Messages 原生 HTTP/SSE lifecycle decoder（text/thinking/tool fragment、`ping`、usage、terminal validation）；\n- OpenAI-compatible Chat Completions 原生 HTTP/SSE decoder（indexed Tool Call 重组、usage-only chunk、`[DONE]`）；\n- 基础 text/tool/reasoning/usage/stop reason 映射；\n- API endpoint fixtures；\n- 对当前 `zeta-client::HttpClient` unary byte port 的临时依赖；raw port 后续迁入\n  `zeta-http-client`。\n\n需要修正：\n\n| 当前设计 | 目标 |\n| --- | --- |\n| legacy provider-named codec source files | 已删除；三份 unary codec 已物理移动到 `requests/` |\n| `JsonHttpTransport` / `UreqJsonHttpTransport` | 已替换为 `ClientRequest`/`ClientResponse` 与 `HttpClient` |\n| `ResolvedApiTarget` 同时承担 runtime 和协议职责 | 仍需将其演进为 typed client request；API 只追加协议 path/header |\n| transport 直接返回 `serde_json::Value` | client 已返回 status/headers/body bytes，API 负责 JSON |\n| provider facade 与 wire codec 两套目录 | Provider facade 已只留在 model-provider；API dispatch 只按 endpoint/profile |\n\n三种已支持 endpoint 的 streaming 与 unary 都是当前实现；尚未实现的是 NDJSON、WebSocket 和更多\nprovider-specific stream profile，不能把这些候选能力描述成现状。\n\n## 4. `endpoint / requests / sse`\n\n### 4.1 `endpoint/`\n\nEndpoint 是一个具体在线 operation contract，例如：\n\n- OpenAI `POST /v1/responses`；\n- OpenAI Chat `POST /v1/chat/completions`；\n- Anthropic `POST /v1/messages`；\n- Gemini `POST ...:generateContent`；\n- Gemini `POST ...:streamGenerateContent?alt=sse`；\n- Ollama `POST /api/chat`；\n- Anthropic `GET /v1/models`；\n- Ollama `GET /api/tags`。\n\nEndpoint 拥有：\n\n```text\nHTTP method\nrelative path/path builder\nquery schema\nprotocol-required headers\nrequest media type\nexpected response media type\nrequest codec identity\nunary response codec identity\nstream codec identity\noperation retry evidence\n```\n\nEndpoint 不拥有：\n\n- 默认 base URL；\n- credential headers；\n- proxy、redirect 或 connection pool；\n- retry attempt loop；\n- JSON DTO implementation；\n- SSE byte framing。\n\nEndpoint path 必须相对。它不能替换 resolved target 的 scheme/host，也不能通过字符串修剪猜测另一\nAPI 的地址。\n\n### 4.2 `requests/`\n\n`requests/` 负责 JSON/body 层：\n\n- canonical `ModelRequest` → wire request；\n- typed invocation option → wire field/header intent；\n- unary body → canonical `ModelResponse`；\n- HTTP error body → typed API error evidence；\n- usage、tool、content、reasoning 和 stop reason；\n- prompt/context cache 参数和 usage；\n- catalog request/response/pagination DTO。\n\n每个 request codec 必须对 canonical intent 做三选一：\n\n1. 准确编码；\n2. 以文档化等价语义编码；\n3. 返回 typed `UnsupportedFeature` / `InvalidRequest`。\n\n不能静默丢弃 tool choice、reasoning、image、strict schema、parallel tool call 或 cache intent。\n\n### 4.3 `sse/`\n\n`sse/` 不处理 TCP chunk、换行或 `data:` 拼接。它消费 `zeta-client` 已完成 framing 的\n`SseFrame`：\n\n```rust\n/// Decodes already-framed SSE values for one concrete API profile.\n///\n/// Implementations validate provider event lifecycles and emit only canonical model events.\npub trait SseDecoder {\n    type State;\n\n    fn decode(\n        &self,\n        state: &mut Self::State,\n        frame: &zeta_client::SseFrame,\n    ) -> Result<Vec<ModelStreamEvent>, ApiError>;\n}\n```\n\n名称仅表达目标语义。具体 API 可以使用 enum/associated type，避免不必要的 dynamic dispatch。\n\n`sse/` 拥有：\n\n- event name 和 `data` JSON schema；\n- Provider event lifecycle；\n- `ping`/comment/terminal 的协议解释；\n- text/reasoning/tool arguments/usage delta；\n- 未知 optional event 的 forward compatibility；\n- 跨 event canonical assembler；\n- EOF 时 terminal validation；\n- 最终 response 与 unary response 的语义一致性。\n\n`sse/` 不拥有：\n\n- HTTP connection；\n- retry/backoff；\n- byte buffer；\n- CRLF/LF 和 multiline `data` framing；\n- idle timer；\n- transport metrics backend。\n\n### 4.4 非 SSE 流\n\nOllama native API 使用 NDJSON，不能塞进名为 `sse/` 的模块。主架构仍以用户确定的三条同级主轴\n为中心，但遇到已验证的非 SSE 协议时增加同级模块：\n\n```text\nendpoint/    requests/    sse/    websocket/    ndjson/\n```\n\n`ndjson/` 只解释由 `zeta-client::NdjsonRecord` 完成 framing 的 API object。未来若接入 WebSocket\n或 gRPC，也按真实协议增加同级 codec，不能伪装成 SSE。\n\n### 4.5 OpenAI Platform 与 ChatGPT/Codex 端点清单\n\n此前把候选名仅与公开 OpenAI Platform `/v1` OpenAPI 比对，遗漏了 ChatGPT/Codex service\nsurface。这是错误的。`../codex/codex-rs/codex-api/src/endpoint/mod.rs` 的 endpoint 集合明确包含\n`compact`、`images`、`memories`、`models`、`realtime_call`、`realtime_websocket`、`responses`、\n`responses_websocket` 与 `search`。\n\n它们不能视为同一条 API：公开 Platform API 通常以 `https://api.openai.com/v1` 为 base，而 Codex\nservice 使用 ChatGPT 登录态和独立的 Codex base。Zeta 不把后者变成一个 `zeta-api` API surface：\n`zeta-api` 的 OpenAI endpoint binding 只描述公开 Platform API，Codex subscription 交给其独立\nruntime adapter。\n\n```rust\n/// Selects a public OpenAI API surface with its own base URL,\n/// credential kind, headers, and supported endpoint set.\npub enum OpenAiServiceSurface {\n    PlatformApi,\n}\n```\n\n`zeta-model-provider` 解析 `OpenAiServiceSurface` 和 direct-provider credential；Codex subscription\n则由 [`zeta-codex-app-server`](codex-app-server.md) 持有其独立 target/auth/runtime。`zeta-api` 仅根据\n已选 direct API surface 编解码对应 contract。绝不能用 Platform API key 访问 Codex profile，也不能让\n任意 custom OpenAI-compatible URL 冒充 ChatGPT/Codex service。Codex 官方的\n[Memories 文档](https://developers.openai.com/codex/memories)也确认：本地 Codex memory store 是\n独立能力；远端 summarization operation 只是其生成路径的一部分，不是 memory 的权威存储。\n\nChatGPT subscription 登录与其 backend contract 不进入本 crate：`zeta-codex-app-server` 通过\n上游 Codex App Server 执行。特别禁止复制 `codex-api/src/endpoint/session.rs`，也禁止让\n`zeta-api` 接收 Codex token、ChatGPT account routing header 或私有 backend URL。\n\n上游模块是能力清单，不是 Zeta 要直接复刻的 HTTP endpoint 清单：\n\n| 上游 capability | Zeta 长期接入点 |\n| --- | --- |\n| ChatGPT login、token refresh、account/rate-limit、Codex thread/turn | `zeta-codex-app-server`，通过上游 JSON-RPC |\n| Codex thread/turn stream 与 approvals | Core `TurnExecutionBackend` 的 Codex adapter；不引入 ChatGPT backend URL 或 header |\n| Codex compact | 有明确产品语义和公开 App Server contract 后再增加单独 vertical slice |\n| memories、search、images、realtime | 只有 Zeta 的产品需求和上游公开 App Server contract 同时满足时，才在 Codex adapter 增加单独 vertical slice |\n| OpenAI Platform Responses/Chat/Images/Realtime | 继续按公开 Platform API 在 `zeta-api` 编解码 |\n\n上游 `responses_websocket`、`realtime_websocket` 的存在不等于 Zeta 应直连其 socket。对 subscription\n路径，连接与 protocol 由 upstream Codex 承担；对公开 Platform API，才按真实协议在\n`zeta-api::websocket`、`zeta-client` 和 `zeta-http-client` 分层实现。\n\n## 5. 供应商运行时联动\n\n`zeta-api` 不知道当前 Provider registry，但可以提供包含兼容差异的 endpoint profile：\n\n```text\nmodel-provider::providers::deepseek\n  → 选择 OpenAI Chat endpoint\n  → 选择 DeepSeek-compatible request/SSE/error profile\n\nmodel-provider::providers::xai\n  → 显式选择 Responses 或 Chat endpoint\n\nmodel-provider::providers::google\n  → 显式选择 Interactions、GenerateContent 或 compatible Chat\n```\n\nProvider-specific wire quirk 的实现仍属于 API 协议层，例如：\n\n- DeepSeek prompt cache hit/miss usage；\n- DeepSeek SSE comment heartbeat；\n- Anthropic `ping` 和 content block lifecycle；\n- Z.AI HTTP/business error 双层状态；\n- MiniMax `base_resp`；\n- Ollama NDJSON `done`/`error`。\n\n但选择哪个 quirk/profile 的责任属于 model-provider。`zeta-api` 不通过 Provider ID、URL 或 model\nname 自行选择。\n\n可以用 typed profile：\n\n```rust\npub enum OpenAiChatProfile {\n    Baseline,\n    DeepSeek,\n    Xai,\n    QwenCompatible,\n    Zai,\n    MiniMax,\n}\n```\n\n这个 enum 表达 wire 差异，不是 Provider registry。没有实质差异的 profile 不需要提前创建。\n\n## 6. 与 `zeta-client` 的边界\n\n### 6.1 请求\n\n```text\nmodel-provider\n  ResolvedTarget + runtime headers\n        │\nzeta-api::endpoint\n  relative path + protocol headers\n        │\nzeta-api::requests\n  body bytes\n        │\nzeta-client::ClientOperation\n        │\nzeta_http_client::HttpRequest\n```\n\nHeader ownership：\n\n| Header | Owner |\n| --- | --- |\n| Authorization、API key、tenant/deployment | model-provider/credential runtime |\n| Content-Type、Accept | zeta-api endpoint |\n| Anthropic version/beta feature | zeta-api endpoint/typed request |\n| traceparent、tracestate、user-agent | zeta-http-client |\n\nHeader merge 必须有冲突规则，禁止简单拼接后让后写值静默覆盖 secret 或协议 header。\n\n### 6.2 Unary 响应\n\n`zeta-client` 通过 `zeta-http-client` 返回：\n\n```text\nstatus + headers + bounded bytes + attempt/timing evidence\n```\n\n`zeta-api::requests` 决定：\n\n- success JSON schema；\n- error JSON schema；\n- HTTP 200 body 是否仍表示业务错误；\n- request ID/retry evidence 的安全提取；\n- canonical response/error。\n\n### 6.3 流式处理响应\n\n```text\nzeta-client\n  bytes → SseFrame/NdjsonRecord\n        │\nzeta-api::sse/ndjson\n  frame → canonical ModelStreamEvent\n```\n\n底层 transport idle timer 在任意合法 wire activity 时更新；operation client 维护 frame activity。\nAPI decoder 维护 semantic progress，并过滤不应暴露为模型输出的 heartbeat。\n\n### 6.4 重试\n\n`zeta-client` 执行 retry，`zeta-api` 只提供协议事实：\n\n- HTTP status；\n- `Retry-After`；\n- Provider error code/type；\n- 是否出现 terminal/semantic event；\n- operation 是否有文档化 idempotency evidence。\n\nModel-provider 选择最终 typed policy。API 不 sleep、不创建 attempt loop，也不 fallback。\n\n### 6.5 遥测\n\n`zeta-http-client` 拥有 HTTP/WebSocket diagnostics 与 redaction；`zeta-client` 拥有\noperation/attempt/stream telemetry。API 只提供低基数 protocol classification：\n\n```text\napi.profile\napi.operation\napi.result_class\napi.stream.event_class\napi.stream.heartbeat_class\n```\n\nAPI 不记录 raw request/response/SSE payload。Exact model ID、prompt、tool arguments 和 secret 不得\n进入默认 telemetry。\n\n## 7. Canonical 契约\n\nCanonical values 由 `zeta-protocol` 拥有。`zeta-api` 只引用或显式 re-export 必要类型：\n\n```text\nModelRequest\n├── instructions\n├── input: Message | ToolResult\n├── tools / tool choice\n├── reasoning intent\n├── output limit\n└── sampling intent\n\nModelResponse\n├── output: Text | Refusal | Reasoning | ToolCall\n├── usage\n└── stop reason\n```\n\n以下内容不进入 protocol：\n\n- Provider JSON DTO；\n- HTTP status/header/URL；\n- SSE event name、NDJSON object；\n- Provider raw error；\n- prompt-cache 原始字段；\n- profile compatibility toggle。\n\n只有两个以上独立组件需要相同语义时，才将新 value 提升到 protocol。\n\n## 8. 流式处理、heartbeat 与完成语义\n\n状态机：\n\n```text\nCreated\n  → Decoding\n       ├─ Heartbeat*\n       ├─ SemanticEvent*\n       └─ Terminal\n  → Completed\n  → Failed\n  → Cancelled\n```\n\n分工示例：\n\n| Wire input | `zeta-client` | `zeta-api` | Canonical output |\n| --- | --- | --- | --- |\n| Anthropic `event: ping` | 生成 `SseEvent`、更新 frame activity | 识别为 heartbeat | 无 |\n| DeepSeek `: keep-alive` | 生成 comment frame、更新 activity | profile 确认为 liveness | 无 |\n| `data: [DONE]` | 生成 data event | profile 校验 terminal | `Completed` |\n| OpenAI typed delta | 生成 data event | 解码/组装 | text/tool/usage delta |\n| Ollama JSON line | 生成 NDJSON record | 解码 `done/error` | delta/completed/error |\n\nEOF 不自动等于成功。需要文档化 terminal event/marker 的 API，在 terminal 之前 EOF 必须返回\ntruncated stream。\n\n`last_wire_activity` 属于 client；`last_semantic_progress` 属于 API/runtime。不能因为模型长时间\nreasoning 而把“无文本”误判为连接死亡。\n\n## 9. Prompt/上下文缓存\n\nPrompt cache 的 wire 语义属于 `requests/`：\n\n- OpenAI prompt cache key/retention/breakpoint；\n- Anthropic `cache_control`、`5m`/`1h` 和 creation/read usage；\n- Gemini implicit cache 和 explicit cached-content reference；\n- xAI Chat header 与 Responses body key 的差异；\n- DeepSeek/Qwen/Z.AI 自动缓存 usage。\n\n不能强行统一为一个 `cache: bool`。Canonical contract 可以表达最小 provider-independent intent，\n精确配置使用 typed profile option。\n\nOllama `keep_alive` 是模型 residency request 字段，不是 prompt cache，也不是 stream heartbeat。\n\nModels manager 只把 cache capability 当 metadata，不参与一次 invocation 的 cache 生命周期。\n\n## 10. 目录协议格式\n\nCatalog request/response codec 也按同一结构组织：\n\n```text\nendpoint/anthropic/models.rs\nrequests/catalog/anthropic.rs\n```\n\nAPI 层负责：\n\n- GET path/query/header；\n- 分页 token 保真；\n- JSON DTO；\n- response headers 中 ETag/Last-Modified/Cache-Control evidence；\n- Provider-neutral observation page。\n\n它不负责 refresh、TTL、SWR、singleflight、merge、filter 或 availability interpretation。调用链：\n\n```text\nmodels-manager::ModelCatalogSource\n        ▲\nmodel-provider runtime adapter\n        │\nzeta-api catalog codec\n        │\nzeta-client\n```\n\nInference endpoint 与 catalog endpoint 可能不共享 base URL，model-provider 必须显式解析 target。\n\n## 11. 错误\n\n目标错误只表达协议事实：\n\n```rust\npub enum ApiError {\n    InvalidRequest(InvalidRequestError),\n    UnsupportedFeature(UnsupportedFeatureError),\n    Provider(ProviderError),\n    InvalidResponse(InvalidResponseError),\n    InvalidStream(InvalidStreamError),\n    Client(zeta_client::ClientError),\n}\n```\n\n所有权：\n\n- `zeta-http-client`：DNS/TCP/TLS/proxy/HTTP/attempt deadline/cancellation；\n- `zeta-client`：operation deadline/retry/framing；\n- `zeta-api`：Provider error body/event、invalid JSON、invalid lifecycle；\n- model-provider：Provider/model/target identity 和 runtime resolution；\n- Agent runtime：retry/fallback/用户提示/Turn outcome；\n- App Server：stable product error mapping。\n\nProvider error message 默认不是稳定公共 API，不能未经清洗直接展示或记录。\n\n## 12. 目标目录\n\n`endpoint / requests / sse` 是同级主目录；当已验证的 API 使用另一种 wire protocol 时，才增加\n`websocket/` 或 `ndjson/` 这样的同级 codec 目录：\n\n```text\nzeta-rs/zeta-api/\n├── BUILD.bazel\n├── Cargo.toml\n├── README.md\n├── src/\n│   ├── lib.rs\n│   ├── endpoint/\n│   │   ├── mod.rs\n│   │   ├── openai_platform/\n│   │   │   ├── mod.rs\n│   │   │   ├── responses.rs\n│   │   │   ├── compact.rs\n│   │   │   ├── responses_websocket.rs\n│   │   │   ├── realtime_websocket.rs\n│   │   │   ├── models.rs\n│   │   │   ├── images.rs\n│   │   │   ├── conversations.rs\n│   │   │   ├── vector_store_search.rs\n│   │   │   └── realtime/{mod,calls,session}.rs\n│   │   ├── anthropic/{messages,models}.rs\n│   │   ├── gemini/{interactions,generate_content,models}.rs\n│   │   ├── ollama/{chat,tags}.rs\n│   │   └── endpoint_tests.rs\n│   │\n│   ├── requests/\n│   │   ├── mod.rs\n│   │   ├── openai_platform/\n│   │   │   ├── responses/\n│   │   │   │   ├── mod.rs\n│   │   │   │   ├── request.rs\n│   │   │   │   ├── response.rs\n│   │   │   │   ├── error.rs\n│   │   │   │   ├── usage.rs\n│   │   │   │   ├── cache.rs\n│   │   │   │   ├── tools.rs\n│   │   │   │   └── request_tests.rs\n│   │   │   ├── compact.rs\n│   │   │   ├── images/{mod,generation,edit,variation}.rs\n│   │   │   └── realtime/calls.rs\n│   │   ├── anthropic/messages/\n│   │   ├── gemini/{interactions,generate_content}/\n│   │   ├── ollama/chat/\n│   │   └── catalog/\n│   │       ├── anthropic.rs\n│   │       ├── gemini.rs\n│   │       ├── openai.rs\n│   │       ├── deepseek.rs\n│   │       └── ollama.rs\n│   │\n│   ├── sse/\n│   │   ├── mod.rs\n│   │   ├── assembler.rs\n│   │   ├── openai_platform/{responses,chat}.rs\n│   │   ├── anthropic/messages.rs\n│   │   ├── gemini/{interactions,generate_content}.rs\n│   │   └── sse_tests.rs\n│   │\n│   ├── websocket/\n│   │   ├── mod.rs\n│   │   ├── openai_platform/responses.rs\n│   │   └── websocket_tests.rs\n│   │\n│   ├── ndjson/\n│   │   ├── mod.rs\n│   │   ├── ollama/chat.rs\n│   │   └── ndjson_tests.rs\n│   │\n│   ├── options/\n│   │   ├── mod.rs\n│   │   ├── profile.rs\n│   │   ├── cache.rs\n│   │   └── options_tests.rs\n│   └── error/\n│       ├── mod.rs\n│       ├── invalid_request.rs\n│       ├── unsupported_feature.rs\n│       ├── provider.rs\n│       ├── invalid_response.rs\n│       ├── invalid_stream.rs\n│       └── error_tests.rs\n└── tests/\n    ├── endpoint_contracts.rs\n    ├── final_response_parity.rs\n    └── fixtures/\n        ├── openai/\n        ├── anthropic/\n        ├── google/\n        ├── deepseek/\n        └── ollama/\n```\n\n`endpoint/`、`requests/`、`sse/` 按职责横向分离，但相同 API profile 的命名必须一致，测试通过\nfixture 把三者重新绑定。目录不是创建空文件的要求；只有实现 vertical slice 时才新增模块。\n\n任何 Rust module 接近 500 LoC 时按 request/response/error/usage/cache 拆分，超过约 800 LoC\n不继续堆功能。新 test module 使用 sibling `*_tests.rs`。\n\n## 13. 公共接口\n\n只导出：\n\n- endpoint/profile selector；\n- typed invocation options；\n- canonical encode/decode entry point；\n- streaming decoder/handle 所需窄接口；\n- catalog observation page；\n- structured `ApiError`；\n- 必要的 canonical value re-export。\n\n不导出：\n\n- Provider registry；\n- concrete HTTP client；\n- raw Provider DTO；\n- 任意 JSON option；\n- telemetry backend；\n- credential/header store。\n\n新增 public trait 必须说明实现者对 validation、terminal lifecycle、unknown event、secret redaction\n和 final-response parity 的责任。\n\n## 14. 供应商/配置档案验证矩阵\n\n| Provider runtime | API profile | Streaming | 已确认的协议差异 |\n| --- | --- | --- | --- |\n| OpenAI | Responses | typed Responses SSE | terminal event、prompt cache usage |\n| ChatGPT/Codex subscription | upstream Codex App Server semantic runtime | upstream event mapping | 不在 `zeta-api` 直连或编解码；由 `zeta-codex-app-server` adapter 验证 |\n| Anthropic | Messages | named Messages SSE | `ping`、content-block lifecycle、cache creation/read |\n| OpenAI-compatible | Chat Completions | data SSE + `[DONE]` baseline | 只保证最小 configured contract |\n| Google | Interactions / GenerateContent / compatible Chat | typed SSE / GenerateContent SSE | profile 不得静默互换 |\n| xAI | Responses / Chat | 对应 typed SSE | cache routing 在两个 profile 中不同 |\n| Qwen | compatible Chat / DashScope native | 对应 stream contract | compatible 与 `X-DashScope-SSE` 不混用 |\n| DeepSeek | Chat | Chat SSE + comment heartbeat | keep-alive、hit/miss usage、unary whitespace |\n| Ollama | native Chat / compatible Chat | NDJSON / Chat SSE | `keep_alive` 是 residency |\n| Hugging Face | routed Chat | Chat SSE baseline | router/downstream error 需独立验证 |\n| Z.AI | Chat | compatible SSE | HTTP/business code、stream finish reason |\n| MiniMax | Anthropic Messages / Chat | 两种 SSE | `base_resp`、profile 显式选择 |\n\n`Unknown` 不等于“不支持”，也不等于“与 OpenAI 相同”。未取得官方文档或 fixture 的高级行为不向\n上声明。\n\n## 15. 测试\n\n### 15.1 端点\n\n- method、relative path、query；\n- required protocol headers；\n- header 冲突和 secret redaction；\n- expected response media type；\n- endpoint/profile 绑定；\n- custom target 不能被 endpoint 替换 origin。\n\n### 15.2 请求\n\n- canonical request → exact wire fixture；\n- unary fixture → canonical response；\n- tool/reasoning/image/usage/stop reason；\n- prompt cache 参数和 usage；\n- unsupported intent 不静默丢弃；\n- HTTP 200 business error；\n- malformed/oversized/unknown response；\n- catalog pagination token 保真。\n\n### 15.3 SSE/NDJSON\n\n- 从 `zeta-client::SseFrame` 开始测试，不在 API crate 重复 byte fragmentation 测试；\n- text/reasoning/tool argument 顺序；\n- Anthropic `ping`；\n- DeepSeek comment heartbeat；\n- `[DONE]` 和 typed terminal event；\n- unknown optional event；\n- stream error event；\n- EOF before terminal；\n- final assembled response 与 unary fixture 等价；\n- Ollama `done/error` NDJSON object。\n\nByte fragmentation、CRLF/LF、multiline data 和 retry timing 的测试属于 `zeta-client`；transport\nidle deadline、proxy/TLS、pool 和 HTTP diagnostics 的测试属于 `zeta-http-client`。\n\n## 16. 迁移计划\n\n### 阶段 1：建立共享客户端分层\n\n- 在 `zeta-http-client` 建立 HTTP request/response/config port；\n- 把 `HttpHeader`、raw request/response bytes 和 `UreqHttpClient` 迁入底层 crate；\n- `zeta-client` 保留 operation retry 与 framing；\n- 保住现有同步 unary 行为；\n- `zeta-api` 开始依赖 operation client port。\n\n### 阶段 2：拆端点与请求\n\n- 已将 `Api` Provider enum 的分派移到 model-provider；\n- 已建立 OpenAI Responses、Anthropic Messages、OpenAI Chat endpoint；\n- 已把现有 JSON conversion 物理移入 `requests/`；\n- 已删除 Provider 级空 facade。\n\n### 阶段 3：SSE 纵向切片（已完成首批 endpoint）\n\n- 在 client 实现 SSE framing；\n- 已在 API 实现 OpenAI Responses `sse/` decoder；\n- 已接 Anthropic Messages decoder 与 `ping`/content-block lifecycle；\n- 已接 OpenAI-compatible Chat Completions decoder、Tool Call fragment assembly 与 `[DONE]`；\n- 三种 endpoint 均由 model-provider 暴露 canonical stream，并返回 terminal response。\n\n### 阶段 4：兼容差异与 NDJSON\n\n- DeepSeek comment heartbeat/usage；\n- Ollama native NDJSON；\n- Google native profiles；\n- 逐家补 error/cache/stream fixtures。\n\n### 阶段 4.5：ChatGPT/Codex 服务接口面\n\n- 建立 [`zeta-codex-app-server`](codex-app-server.md)，由 upstream Codex managed login 提供\n  ChatGPT/Codex subscription runtime；\n- 将 Platform API key、Codex subscription runtime 和 custom-compatible target 设为互斥 binding；\n- 仅在 upstream App Server 的公开 JSON-RPC 支持范围内接入 models、thread/turn 和 stream；\n- 为 realtime call / WebSocket 三套 wire adapter 建立脱敏 contract fixture；\n- 不复制 Codex `EndpointSession` 的 transport/retry/telemetry 所有权。\n\n### 阶段 5：目录协议格式\n\n- 实现 list endpoint/request/response codec；\n- model-provider 实现 models manager source；\n- manager 负责 refresh/cache/merge。\n\n## 17. 固定决策\n\n1. `zeta-api` 是协议层，不是 Provider registry。\n2. `endpoint/`、`requests/`、`sse/` 是同级主目录；已验证的 WebSocket/NDJSON protocol 才新增同级 codec。\n3. 非 SSE 协议使用真实模块，例如 `websocket/`、`ndjson/`。\n4. Raw transport 与 network policy 属于 `zeta-http-client`；retry、SSE framing 和 operation\n   telemetry 属于 `zeta-client`。\n5. `zeta-api::sse` 只解释已经 framed 的 API event。\n6. Provider/profile 选择属于 `zeta-model-provider`。\n7. Config 只声明 profile，不持有 runtime API object。\n8. Canonical values 属于 `zeta-protocol`。\n9. Prompt cache wire mapping 属于 `requests/`，catalog cache 属于 models manager。\n10. Inference retry safety 由 runtime policy 显式选择，client 执行。\n11. Provider error 不以 raw JSON/String 穿透产品 API。\n12. OpenAI Responses、OpenAI-compatible Chat Completions 与 Anthropic Messages 已接通 live HTTP/SSE\n    execution；NDJSON、WebSocket 和未验证 provider profile 仍须按真实协议另行实现。\n13. `OpenAiServiceSurface` 只用于公开 Platform API endpoint binding；ChatGPT/Codex subscription\n    不共享 base URL、credential 或 custom endpoint override，而是独立 runtime。\n14. ChatGPT/Codex subscription auth 与 backend protocol 属于 upstream Codex App Server；本 crate\n    不定义其 OAuth wire codec、token/header value 或私有 backend target。\n\n## 18. 官方资料索引\n\n### OpenAI\n\n- [Responses streaming](https://developers.openai.com/api/docs/guides/streaming-responses)\n- [Prompt caching](https://developers.openai.com/api/docs/guides/prompt-caching)\n- [Models](https://developers.openai.com/api/docs/models)\n- [Codex Memories](https://developers.openai.com/codex/memories)\n- [Codex authentication](https://learn.chatgpt.com/docs/auth)\n- [Codex app-server](https://learn.chatgpt.com/docs/app-server)\n- 本地 Codex source snapshot：`../codex/codex-rs/codex-api/src/endpoint/`（验证\n  `memories/trace_summarize`、`alpha/search`、Responses/Realtime WebSocket 和 service target contract）\n\n### Anthropic\n\n- [Streaming Messages](https://platform.claude.com/docs/en/build-with-claude/streaming)\n- [Prompt caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)\n- [API errors](https://platform.claude.com/docs/en/api/errors)\n- [List models](https://platform.claude.com/docs/en/api/models/list)\n\n### Google Gemini\n\n- [Interactions and text generation](https://ai.google.dev/gemini-api/docs/text-generation)\n- [`streamGenerateContent`](https://ai.google.dev/api/generate-content)\n- [Models API](https://ai.google.dev/api/models)\n- [Context caching](https://ai.google.dev/gemini-api/docs/caching/)\n\n### xAI\n\n- [Streaming](https://docs.x.ai/developers/model-capabilities/text/streaming)\n- [Prompt caching](https://docs.x.ai/developers/advanced-api-usage/prompt-caching)\n- [Model APIs](https://docs.x.ai/developers/rest-api-reference/inference/models)\n\n### Qwen / Alibaba Cloud 模型 Studio\n\n- [Streaming output](https://help.aliyun.com/en/model-studio/stream)\n- [Context cache](https://help.aliyun.com/en/model-studio/context-cache)\n- [Models and regional endpoints](https://help.aliyun.com/en/model-studio/models)\n\n### DeepSeek\n\n- [Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion)\n- [Rate limit and request keep-alive](https://api-docs.deepseek.com/quick_start/rate_limit)\n- [List models](https://api-docs.deepseek.com/api/list-models)\n- [Context caching](https://api-docs.deepseek.com/news/news0802/)\n\n### Ollama\n\n- [Native chat](https://docs.ollama.com/api/chat)\n- [NDJSON streaming](https://docs.ollama.com/api/streaming)\n- [Streaming errors](https://docs.ollama.com/api/errors)\n- [List installed models](https://docs.ollama.com/api/tags)\n\n### 其他\n\n- [Hugging Face Chat Completion](https://huggingface.co/docs/inference-providers/tasks/chat-completion)\n- [Z.AI Chat Completion](https://docs.z.ai/api-reference/llm/chat-completion)\n- [Z.AI context caching](https://docs.z.ai/guides/capabilities/cache)\n- [MiniMax OpenAI-compatible API](https://platform.minimax.io/docs/api-reference/text-openai-api)\n- [MiniMax Anthropic-compatible API](https://platform.minimax.io/docs/api-reference/text-anthropic-api)\n\nKimi 与 MiMo 保持 configured compatible endpoint，直到取得完整官方 streaming/cache/catalog\nreference 或经授权的脱敏 contract fixture。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-四层关系",
        "title": "2. 四层关系"
      },
      {
        "depth": 2,
        "id": "3-当前实现审计",
        "title": "3. 当前实现审计"
      },
      {
        "depth": 2,
        "id": "4-endpoint-requests-sse",
        "title": "4. endpoint / requests / sse"
      },
      {
        "depth": 3,
        "id": "41-endpoint",
        "title": "4.1 endpoint/"
      },
      {
        "depth": 3,
        "id": "42-requests",
        "title": "4.2 requests/"
      },
      {
        "depth": 3,
        "id": "43-sse",
        "title": "4.3 sse/"
      },
      {
        "depth": 3,
        "id": "44-非-sse-流",
        "title": "4.4 非 SSE 流"
      },
      {
        "depth": 3,
        "id": "45-openai-platform-与-chatgptcodex-端点清单",
        "title": "4.5 OpenAI Platform 与 ChatGPT/Codex 端点清单"
      },
      {
        "depth": 2,
        "id": "5-供应商运行时联动",
        "title": "5. 供应商运行时联动"
      },
      {
        "depth": 2,
        "id": "6-与-zeta-client-的边界",
        "title": "6. 与 zeta-client 的边界"
      },
      {
        "depth": 3,
        "id": "61-请求",
        "title": "6.1 请求"
      },
      {
        "depth": 3,
        "id": "62-unary-响应",
        "title": "6.2 Unary 响应"
      },
      {
        "depth": 3,
        "id": "63-流式处理响应",
        "title": "6.3 流式处理响应"
      },
      {
        "depth": 3,
        "id": "64-重试",
        "title": "6.4 重试"
      },
      {
        "depth": 3,
        "id": "65-遥测",
        "title": "6.5 遥测"
      },
      {
        "depth": 2,
        "id": "7-canonical-契约",
        "title": "7. Canonical 契约"
      },
      {
        "depth": 2,
        "id": "8-流式处理heartbeat-与完成语义",
        "title": "8. 流式处理、heartbeat 与完成语义"
      },
      {
        "depth": 2,
        "id": "9-prompt上下文缓存",
        "title": "9. Prompt/上下文缓存"
      },
      {
        "depth": 2,
        "id": "10-目录协议格式",
        "title": "10. 目录协议格式"
      },
      {
        "depth": 2,
        "id": "11-错误",
        "title": "11. 错误"
      },
      {
        "depth": 2,
        "id": "12-目标目录",
        "title": "12. 目标目录"
      },
      {
        "depth": 2,
        "id": "13-公共接口",
        "title": "13. 公共接口"
      },
      {
        "depth": 2,
        "id": "14-供应商配置档案验证矩阵",
        "title": "14. 供应商/配置档案验证矩阵"
      },
      {
        "depth": 2,
        "id": "15-测试",
        "title": "15. 测试"
      },
      {
        "depth": 3,
        "id": "151-端点",
        "title": "15.1 端点"
      },
      {
        "depth": 3,
        "id": "152-请求",
        "title": "15.2 请求"
      },
      {
        "depth": 3,
        "id": "153-ssendjson",
        "title": "15.3 SSE/NDJSON"
      },
      {
        "depth": 2,
        "id": "16-迁移计划",
        "title": "16. 迁移计划"
      },
      {
        "depth": 3,
        "id": "阶段-1建立共享客户端分层",
        "title": "阶段 1：建立共享客户端分层"
      },
      {
        "depth": 3,
        "id": "阶段-2拆端点与请求",
        "title": "阶段 2：拆端点与请求"
      },
      {
        "depth": 3,
        "id": "阶段-3sse-纵向切片已完成首批-endpoint",
        "title": "阶段 3：SSE 纵向切片（已完成首批 endpoint）"
      },
      {
        "depth": 3,
        "id": "阶段-4兼容差异与-ndjson",
        "title": "阶段 4：兼容差异与 NDJSON"
      },
      {
        "depth": 3,
        "id": "阶段-45chatgptcodex-服务接口面",
        "title": "阶段 4.5：ChatGPT/Codex 服务接口面"
      },
      {
        "depth": 3,
        "id": "阶段-5目录协议格式",
        "title": "阶段 5：目录协议格式"
      },
      {
        "depth": 2,
        "id": "17-固定决策",
        "title": "17. 固定决策"
      },
      {
        "depth": 2,
        "id": "18-官方资料索引",
        "title": "18. 官方资料索引"
      },
      {
        "depth": 3,
        "id": "openai",
        "title": "OpenAI"
      },
      {
        "depth": 3,
        "id": "anthropic",
        "title": "Anthropic"
      },
      {
        "depth": 3,
        "id": "google-gemini",
        "title": "Google Gemini"
      },
      {
        "depth": 3,
        "id": "xai",
        "title": "xAI"
      },
      {
        "depth": 3,
        "id": "qwen-alibaba-cloud-模型-studio",
        "title": "Qwen / Alibaba Cloud 模型 Studio"
      },
      {
        "depth": 3,
        "id": "deepseek",
        "title": "DeepSeek"
      },
      {
        "depth": 3,
        "id": "ollama",
        "title": "Ollama"
      },
      {
        "depth": 3,
        "id": "其他",
        "title": "其他"
      }
    ],
    "searchText": " - 物理位置：zeta-rs/zeta-api/ - Rust crate：zetaapi - 层次：模型 API 协议层 - 当前状态：OpenAI Responses、OpenAI-compatible Chat Completions 与 Anthropic Messages 已具备 unary codec、原生 HTTP/SSE invocation、canonical delta 与 terminal response assembly - Crate codec 与 decoder 实现：zeta-rs/zeta-api/README.md - Canonical contract：protocol.md - Provider runtime：model-provider.md - Operation client：zeta-client.md - 底层网络：zeta-http-client README - Direct-provider credential：model-provider.md - Secret persistence：secrets.md - Model catalog control plane：models-manager.md - Subscription runtime adapter：codex-app-server.md Provider 官方资料核对日期：2026-07-26。请求字段、事件类型、缓存语义和错误结构会持续变化； 实现必须以官方文档和脱敏 contract fixture 为准，不能仅凭 OpenAI-compatible 标签推断。 快速理解 zeta-api 是纯模型 API 协议层。它接收 provider-independent canonical model value，负责： - 定义 API relative endpoint、HTTP method 和协议 headers； - 将 canonical request 编码为具体 API request body； - 将 unary response/error 解码为 canonical response/error； - 将已经完成 framing 的 SSE event 解码为 canonical stream event； - 解释 Provider 级 terminal、heartbeat、usage、tool、reasoning 和 prompt-cache 字段； - 提供 catalog endpoint 的 request/response codec。 它不再拥有 Provider registry，也不再实现 transport、retry、SSE framing 或 telemetry。 目标横向结构是： ``text endpoint/ requests/ sse/ ` 三者同级： - endpoint/ 描述 method、relative path、required headers 和所绑定的 codec； - requests/ 描述 request/unary response/error JSON； - sse/ 描述具体 API 的 SSE event schema、lifecycle 和 canonical assembly。 通用 HTTP/WebSocket backend、proxy/TLS/pool 与 transport diagnostics 属于 zeta-http-client； operation retry、SSE/NDJSON framing 和 operation telemetry 属于 zeta-client。 需要处理的内容 本层是否负责 交给谁 --- --- --- 把统一模型请求编码成供应商 JSON ✅ 本层 解释供应商响应、错误和流式事件 ✅ 本层 选择供应商、模型和凭据 ❌ 模型调用系统 判断是否安全重试并执行等待 ❌ 模型调用操作层 建立 HTTP 连接、代理和 TLS ❌ 网络层 推进 Agent Turn 和工具循环 ❌ 会话与执行系统 2. 四层关系 `text zeta-model-provider-config 声明 Provider、默认 base URL、允许的 API profile │ ▼ zeta-model-provider 解析 credential/target，选择 API endpoint 和 retry policy │ ▼ zeta-api endpoint + requests + SSE protocol codec │ ▼ zeta-client operation retry + framing + telemetry │ ▼ zeta-http-client HTTP/WebSocket execution + network policy ` 依赖与控制流不是同一个方向。Rust 依赖建议为： `text zeta-http-client ▲ │ zeta-client zeta-protocol ▲ ▲ └──── zeta-api ─┘ ▲ │ zeta-model-provider ` 规则： - zeta-client 不依赖 zeta-api； - zeta-http-client 不依赖 zeta-client 或 zeta-api； - zeta-api 可以使用 operation client 的 request/response/SSE value； - zeta-api 不依赖 model-provider/config/models-manager/Core； - model-provider 选择 zeta-api endpoint 并注入 resolved runtime values； - Provider registry 只存在于 model-provider。 3. 当前实现审计 当前 crate 已实现： - provider-independent ApiEndpoint 和 ApiProtocol； - endpoint/、requests/、sse/ 三个顶级 codec 模块； - OpenAI Responses unary codec； - Anthropic Messages unary codec； - OpenAI-compatible Chat Completions unary codec； - OpenAI Responses 原生 HTTP/SSE invocation 与 canonical text/reasoning delta decoder； - Anthropic Messages 原生 HTTP/SSE lifecycle decoder（text/thinking/tool fragment、ping、usage、terminal validation）； - OpenAI-compatible Chat Completions 原生 HTTP/SSE decoder（indexed Tool Call 重组、usage-only chunk、 DONE ）； - 基础 text/tool/reasoning/usage/stop reason 映射； - API endpoint fixtures； - 对当前 zeta-client::HttpClient unary byte port 的临时依赖；raw port 后续迁入 zeta-http-client。 需要修正： 当前设计 目标 --- --- legacy provider-named codec source files 已删除；三份 unary codec 已物理移动到 requests/ JsonHttpTransport / UreqJsonHttpTransport 已替换为 ClientRequest/ClientResponse 与 HttpClient ResolvedApiTarget 同时承担 runtime 和协议职责 仍需将其演进为 typed client request；API 只追加协议 path/header transport 直接返回 serdejson::Value client 已返回 status/headers/body bytes，API 负责 JSON provider facade 与 wire codec 两套目录 Provider facade 已只留在 model-provider；API dispatch 只按 endpoint/profile 三种已支持 endpoint 的 streaming 与 unary 都是当前实现；尚未实现的是 NDJSON、WebSocket 和更多 provider-specific stream profile，不能把这些候选能力描述成现状。 4. endpoint / requests / sse 4.1 endpoint/ Endpoint 是一个具体在线 operation contract，例如： - OpenAI POST /v1/responses； - OpenAI Chat POST /v1/chat/completions； - Anthropic POST /v1/messages； - Gemini POST ...:generateContent； - Gemini POST ...:streamGenerateContent?alt=sse； - Ollama POST /api/chat； - Anthropic GET /v1/models； - Ollama GET /api/tags。 Endpoint 拥有： `text HTTP method relative path/path builder query schema protocol-required headers request media type expected response media type request codec identity unary response codec identity stream codec identity operation retry evidence ` Endpoint 不拥有： - 默认 base URL； - credential headers； - proxy、redirect 或 connection pool； - retry attempt loop； - JSON DTO implementation； - SSE byte framing。 Endpoint path 必须相对。它不能替换 resolved target 的 scheme/host，也不能通过字符串修剪猜测另一 API 的地址。 4.2 requests/ requests/ 负责 JSON/body 层： - canonical ModelRequest → wire request； - typed invocation option → wire field/header intent； - unary body → canonical ModelResponse； - HTTP error body → typed API error evidence； - usage、tool、content、reasoning 和 stop reason； - prompt/context cache 参数和 usage； - catalog request/response/pagination DTO。 每个 request codec 必须对 canonical intent 做三选一： 1. 准确编码； 2. 以文档化等价语义编码； 3. 返回 typed UnsupportedFeature / InvalidRequest。 不能静默丢弃 tool choice、reasoning、image、strict schema、parallel tool call 或 cache intent。 4.3 sse/ sse/ 不处理 TCP chunk、换行或 data: 拼接。它消费 zeta-client 已完成 framing 的 SseFrame： `rust /// Decodes already-framed SSE values for one concrete API profile. /// /// Implementations validate provider event lifecycles and emit only canonical model events. pub trait SseDecoder type State; fn decode &self, state: &mut Self::State, frame: &zetaclient::SseFrame, - Result<Vec<ModelStreamEvent , ApiError ; ` 名称仅表达目标语义。具体 API 可以使用 enum/associated type，避免不必要的 dynamic dispatch。 sse/ 拥有： - event name 和 data JSON schema； - Provider event lifecycle； - ping/comment/terminal 的协议解释； - text/reasoning/tool arguments/usage delta； - 未知 optional event 的 forward compatibility； - 跨 event canonical assembler； - EOF 时 terminal validation； - 最终 response 与 unary response 的语义一致性。 sse/ 不拥有： - HTTP connection； - retry/backoff； - byte buffer； - CRLF/LF 和 multiline data framing； - idle timer； - transport metrics backend。 4.4 非 SSE 流 Ollama native API 使用 NDJSON，不能塞进名为 sse/ 的模块。主架构仍以用户确定的三条同级主轴 为中心，但遇到已验证的非 SSE 协议时增加同级模块： `text endpoint/ requests/ sse/ websocket/ ndjson/ ` ndjson/ 只解释由 zeta-client::NdjsonRecord 完成 framing 的 API object。未来若接入 WebSocket 或 gRPC，也按真实协议增加同级 codec，不能伪装成 SSE。 4.5 OpenAI Platform 与 ChatGPT/Codex 端点清单 此前把候选名仅与公开 OpenAI Platform /v1 OpenAPI 比对，遗漏了 ChatGPT/Codex service surface。这是错误的。../codex/codex-rs/codex-api/src/endpoint/mod.rs 的 endpoint 集合明确包含 compact、images、memories、models、realtimecall、realtimewebsocket、responses、 responseswebsocket 与 search。 它们不能视为同一条 API：公开 Platform API 通常以 https://api.openai.com/v1 为 base，而 Codex service 使用 ChatGPT 登录态和独立的 Codex base。Zeta 不把后者变成一个 zeta-api API surface： zeta-api 的 OpenAI endpoint binding 只描述公开 Platform API，Codex subscription 交给其独立 runtime adapter。 `rust /// Selects a public OpenAI API surface with its own base URL, /// credential kind, headers, and supported endpoint set. pub enum OpenAiServiceSurface PlatformApi, ` zeta-model-provider 解析 OpenAiServiceSurface 和 direct-provider credential；Codex subscription 则由 zeta-codex-app-server 持有其独立 target/auth/runtime。zeta-api 仅根据 已选 direct API surface 编解码对应 contract。绝不能用 Platform API key 访问 Codex profile，也不能让 任意 custom OpenAI-compatible URL 冒充 ChatGPT/Codex service。Codex 官方的 Memories 文档也确认：本地 Codex memory store 是 独立能力；远端 summarization operation 只是其生成路径的一部分，不是 memory 的权威存储。 ChatGPT subscription 登录与其 backend contract 不进入本 crate：zeta-codex-app-server 通过 上游 Codex App Server 执行。特别禁止复制 codex-api/src/endpoint/session.rs，也禁止让 zeta-api 接收 Codex token、ChatGPT account routing header 或私有 backend URL。 上游模块是能力清单，不是 Zeta 要直接复刻的 HTTP endpoint 清单： 上游 capability Zeta 长期接入点 --- --- ChatGPT login、token refresh、account/rate-limit、Codex thread/turn zeta-codex-app-server，通过上游 JSON-RPC Codex thread/turn stream 与 approvals Core TurnExecutionBackend 的 Codex adapter；不引入 ChatGPT backend URL 或 header Codex compact 有明确产品语义和公开 App Server contract 后再增加单独 vertical slice memories、search、images、realtime 只有 Zeta 的产品需求和上游公开 App Server contract 同时满足时，才在 Codex adapter 增加单独 vertical slice OpenAI Platform Responses/Chat/Images/Realtime 继续按公开 Platform API 在 zeta-api 编解码 上游 responseswebsocket、realtimewebsocket 的存在不等于 Zeta 应直连其 socket。对 subscription 路径，连接与 protocol 由 upstream Codex 承担；对公开 Platform API，才按真实协议在 zeta-api::websocket、zeta-client 和 zeta-http-client 分层实现。 5. 供应商运行时联动 zeta-api 不知道当前 Provider registry，但可以提供包含兼容差异的 endpoint profile： `text model-provider::providers::deepseek → 选择 OpenAI Chat endpoint → 选择 DeepSeek-compatible request/SSE/error profile model-provider::providers::xai → 显式选择 Responses 或 Chat endpoint model-provider::providers::google → 显式选择 Interactions、GenerateContent 或 compatible Chat ` Provider-specific wire quirk 的实现仍属于 API 协议层，例如： - DeepSeek prompt cache hit/miss usage； - DeepSeek SSE comment heartbeat； - Anthropic ping 和 content block lifecycle； - Z.AI HTTP/business error 双层状态； - MiniMax baseresp； - Ollama NDJSON done/error。 但选择哪个 quirk/profile 的责任属于 model-provider。zeta-api 不通过 Provider ID、URL 或 model name 自行选择。 可以用 typed profile： `rust pub enum OpenAiChatProfile Baseline, DeepSeek, Xai, QwenCompatible, Zai, MiniMax, ` 这个 enum 表达 wire 差异，不是 Provider registry。没有实质差异的 profile 不需要提前创建。 6. 与 zeta-client 的边界 6.1 请求 `text model-provider ResolvedTarget + runtime headers │ zeta-api::endpoint relative path + protocol headers │ zeta-api::requests body bytes │ zeta-client::ClientOperation │ zetahttpclient::HttpRequest ` Header ownership： Header Owner --- --- Authorization、API key、tenant/deployment model-provider/credential runtime Content-Type、Accept zeta-api endpoint Anthropic version/beta feature zeta-api endpoint/typed request traceparent、tracestate、user-agent zeta-http-client Header merge 必须有冲突规则，禁止简单拼接后让后写值静默覆盖 secret 或协议 header。 6.2 Unary 响应 zeta-client 通过 zeta-http-client 返回： `text status + headers + bounded bytes + attempt/timing evidence ` zeta-api::requests 决定： - success JSON schema； - error JSON schema； - HTTP 200 body 是否仍表示业务错误； - request ID/retry evidence 的安全提取； - canonical response/error。 6.3 流式处理响应 `text zeta-client bytes → SseFrame/NdjsonRecord │ zeta-api::sse/ndjson frame → canonical ModelStreamEvent ` 底层 transport idle timer 在任意合法 wire activity 时更新；operation client 维护 frame activity。 API decoder 维护 semantic progress，并过滤不应暴露为模型输出的 heartbeat。 6.4 重试 zeta-client 执行 retry，zeta-api 只提供协议事实： - HTTP status； - Retry-After； - Provider error code/type； - 是否出现 terminal/semantic event； - operation 是否有文档化 idempotency evidence。 Model-provider 选择最终 typed policy。API 不 sleep、不创建 attempt loop，也不 fallback。 6.5 遥测 zeta-http-client 拥有 HTTP/WebSocket diagnostics 与 redaction；zeta-client 拥有 operation/attempt/stream telemetry。API 只提供低基数 protocol classification： `text api.profile api.operation api.resultclass api.stream.eventclass api.stream.heartbeatclass ` API 不记录 raw request/response/SSE payload。Exact model ID、prompt、tool arguments 和 secret 不得 进入默认 telemetry。 7. Canonical 契约 Canonical values 由 zeta-protocol 拥有。zeta-api 只引用或显式 re-export 必要类型： `text ModelRequest ├── instructions ├── input: Message ToolResult ├── tools / tool choice ├── reasoning intent ├── output limit └── sampling intent ModelResponse ├── output: Text Refusal Reasoning ToolCall ├── usage └── stop reason ` 以下内容不进入 protocol： - Provider JSON DTO； - HTTP status/header/URL； - SSE event name、NDJSON object； - Provider raw error； - prompt-cache 原始字段； - profile compatibility toggle。 只有两个以上独立组件需要相同语义时，才将新 value 提升到 protocol。 8. 流式处理、heartbeat 与完成语义 状态机： `text Created → Decoding ├─ Heartbeat ├─ SemanticEvent └─ Terminal → Completed → Failed → Cancelled ` 分工示例： Wire input zeta-client zeta-api Canonical output --- --- --- --- Anthropic event: ping 生成 SseEvent、更新 frame activity 识别为 heartbeat 无 DeepSeek : keep-alive 生成 comment frame、更新 activity profile 确认为 liveness 无 data: DONE 生成 data event profile 校验 terminal Completed OpenAI typed delta 生成 data event 解码/组装 text/tool/usage delta Ollama JSON line 生成 NDJSON record 解码 done/err"
  },
  {
    "slug": "zeta-api-interface-requirements",
    "sourcePath": "docs/zeta-api-interface-requirements.md",
    "title": "Zeta API 接口文档规范",
    "description": "当前已经接受的产品契约见 zeta-app-server-api.md。",
    "group": "API 与协议",
    "markdown": "> 本文规定 Desktop、CLI 或其他客户端提交 Zeta App Server 产品 API 需求时必须包含的内容。  \n> zeta-rs 是已接受契约的 owner，并据此实现 Rust 协议、handler、typed client、生成类型和测试。\n\n当前已经接受的产品契约见\n[`zeta-app-server-api.md`](zeta-app-server-api.md)。\n\n## 快速理解\n\n这份规范帮助需求提出方把“希望增加一个接口”写成能够实现、测试和长期维护的产品契约。先说明\n用户问题和权威状态，再定义方法、字段、错误、取消、幂等和恢复；不能只提交一组 DTO 或方法名。\n\n| 你正在确认什么 | 文档必须给出的答案 |\n| --- | --- |\n| 为什么需要接口 | 用户或客户端要完成的真实任务，以及明确不做什么 |\n| 谁拥有状态 | Server、连接、Session、Thread、Turn 或能力句柄中的唯一所有者 |\n| 成功意味着什么 | 可观察结果、持久化边界和它不保证的内容 |\n| 失败后怎么办 | 稳定错误、能否重试、取消和迟到结果处理 |\n| 如何安全演进 | 能力协商、版本、生成物和所有调用方同步方式 |\n\n## 1. 文档头\n\n每份 API 文档必须先写：\n\n```yaml\ntitle: Browser Capability API\nstatus: draft | review | accepted | deprecated\ncapabilityVersion: 1\nowner: zeta-rs\nrustOwner: zeta-rs\nrequestedBy: desktop\nconsumers: [desktop, cli]\nlastUpdated: YYYY-MM-DD\n```\n\n还需说明：\n\n- 解决的问题；\n- 不在范围内的能力；\n- 权威状态由哪一端持有；\n- 哪些客户端需要该能力；\n- 当前开发期 breaking-change 与调用方同步更新策略。\n\n## 2. 方法清单\n\n先提供一张完整清单：\n\n| Method | Direction | Consumers | Side effect | Idempotent | Capability | Summary |\n|---|---|---|---:|---:|---|---|\n| `session/request` (`createThread`) | Client → Server | Desktop, CLI | yes | required | sessions | 创建 Thread |\n| `browser/observe` | Server → Client | Desktop host | no | n/a | browser | 观察目标 |\n\nDirection 只能使用：\n\n- Client → Server request；\n- Server → Client request；\n- Server → Client notification；\n- 双向 Resource request。\n\n同一 method 在 in-process、stdio、Unix socket 和 WebSocket 上必须保持相同语义。若某个\ntransport 不支持该能力，必须通过 initialize capability 明确声明，不能提供绕过 dispatcher\n的隐藏 Rust 方法。\n\n## 3. 每个请求必填内容\n\n每个 request 单独一节，包含：\n\n1. method 字符串；\n2. direction；\n3. 前置条件；\n4. capability 与版本；\n5. Params 的字段表；\n6. Result 的字段表；\n7. 所有错误码；\n8. deadline 和 timeout；\n9. cancellation 行为；\n10. idempotency 规则；\n11. owner/connection/thread/turn 路由；\n12. 成功 JSON fixture；\n13. 每类错误 JSON fixture。\n\n字段表格式：\n\n| Field | Type | Required | Nullable | Constraints | Meaning |\n|---|---|---:|---:|---|---|\n| `threadId` | `ThreadId` | yes | no | existing | 目标 Thread |\n| `maxBytes` | integer | yes | no | 1..262144 | 最大 chunk |\n\n`required` 和 `nullable` 必须分开说明。不能用“可选”同时表达字段缺失和 JSON `null`。\n\n## 4. JSON 样例\n\n请求：\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 42,\n  \"method\": \"session/request\",\n  \"params\": {\n    \"commandId\": \"command_01...\",\n    \"sessionId\": \"session_123\",\n    \"expectedSequence\": 17,\n    \"request\": {\n      \"type\": \"startTurn\",\n      \"threadId\": \"thread_123\",\n      \"input\": [\n        { \"type\": \"text\", \"text\": \"分析当前网页\" }\n      ]\n    }\n  }\n}\n```\n\n成功响应：\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 42,\n  \"result\": {\n    \"turnId\": \"turn_456\",\n    \"sequence\": 18\n  }\n}\n```\n\n错误响应：\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 42,\n  \"error\": {\n    \"code\": -32004,\n    \"message\": \"CommandConflict\",\n    \"data\": null\n  }\n}\n```\n\nFixtures 是测试输入，必须是可解析的完整 JSON，不能只写 TypeScript 示例对象。\n\n## 5. 生命周期和状态机\n\n涉及状态的接口必须给出：\n\n- 状态全集；\n- 初始状态；\n- 合法转换；\n- 终态；\n- 失败和取消的区别；\n- connection 断开时的行为；\n- process crash/restart 后的行为。\n\n示例：\n\n```text\nCreated → Running → Completed\n                  → Failed\n                  → Cancelling → Interrupted\n```\n\n## 6. 顺序与一致性\n\n必须说明：\n\n- 哪个字段提供 durable sequence；\n- 哪个字段提供 stream sequence；\n- response 与 notification 的因果顺序；\n- snapshot 与实时订阅如何避免事件空洞；\n- 客户端发现空洞后如何 resync；\n- 哪些通知可以合并，哪些不能丢弃。\n\n## 7. 类型化命令重放\n\n所有持久化副作用请求必须说明：\n\n- `commandId` 是否必填；\n- 对应的 typed command payload；\n- receipt 持久化在哪个 aggregate；\n- `expectedSequence` 针对哪个 aggregate；\n- 相同 ID + 相同 command 的稳定结果；\n- 相同 ID + 不同 command 的 `CommandConflict`；\n- 重启恢复后如何重放。\n\nJSON-RPC `id` 只做当前连接的 response pairing，不能代替 `commandId`。\n\n## 8. 超时、取消和迟到响应\n\nServer → Client 请求必须定义：\n\n- deadline 字段和单位；\n- Turn 结束时如何取消；\n- connection 断开时如何清理；\n- `serverRequest/resolved` 通知；\n- deadline 后的迟到 response 是否忽略；\n- UI 如何关闭等待状态。\n\n## 9. Connection 与所有者\n\n必须分别说明：\n\n- Thread subscription；\n- Turn owner；\n- CapabilityHandle owner；\n- Resource owner；\n- Browser Target owner。\n\n不能用“当前活动客户端”或“当前活动 Tab”这种可变全局概念代替稳定 owner。\n\n## 10. 资源\n\n大对象不得在普通业务消息中使用 Base64 或暴露本地路径。接口文档需要定义：\n\n- `resourceId`；\n- MIME type；\n- size；\n- SHA-256；\n- owner connection；\n- TTL；\n- 单资源与单连接 quota；\n- chunk 最大值；\n- offset 范围；\n- release、过期和断连清理。\n\n## 11. 安全\n\n每个涉及 Browser、文件、命令、网络、下载或外部 URL 的接口必须说明：\n\n- 信任边界；\n- 输入验证；\n- origin/路径/目标 allowlist；\n- secret 和敏感字段脱敏；\n- approval 是否需要；\n- approval 与 action digest 如何绑定；\n- host policy 的二次校验；\n- 审计记录。\n\n## 12. 错误码\n\n每个错误码必须稳定，并包含：\n\n| Error | Retryable | Client action | Data |\n|---|---:|---|---|\n| `ServerOverloaded` | yes | jitter backoff | `retryAfterMs` |\n| `BrowserTargetUnavailable` | no | 刷新目标列表 | `targetId` |\n\n不要只写“失败时返回 error”。\n\n## 13. 开发期破坏性变更\n\n文档必须标注：\n\n- 哪些 Rust/TypeScript DTO、registry 和 schema 同步改变；\n- Desktop、CLI 与 TUI 哪些调用方必须同一变更迁移；\n- 旧开发数据是否需要清空；\n- 未知 notification/request 的稳定错误行为；\n- schema hash 和 fixtures 是否已重新生成。\n\n当前开发阶段不保留旧 route、deprecated alias、DTO mapper 或 storage upcast。发布后的版本与\nmigration 政策必须另立 RFC，不能提前把兼容逻辑放进 Core。\n\n## 14. Rust 实现验收\n\nAPI 被视为完成，必须同时具备：\n\n- Rust DTO；\n- wire-specific DTO 与 canonical model 的必要机械映射；\n- handler 与路由；\n- capability/version 校验；\n- error code；\n- timeout/cancellation；\n- idempotency；\n- Rust tests；\n- JSON fixtures；\n- in-process App Server Client contract test；\n- stdio transport contract test；\n- TypeScript 生成；\n- JSON Schema 生成；\n- schema hash；\n- Desktop 与 CLI contract fixtures。\n\n缺少其中任何一项，接口状态仍是 `draft` 或 `review`，不能标记为 `accepted`。\n\n## 15. 可复制模板\n\n建议从独立文件\n[`zeta-api-interface-template.md`](zeta-api-interface-template.md)\n复制一份作为新接口文档。以下内容与该模板的要求一致：\n\n````markdown\n# <Capability / Domain> API\n\n## Metadata\n\n- Status:\n- Capability version:\n- Owner:\n- Rust owner:\n- Requested by:\n- Consumers:\n- Last updated:\n\n## Scope\n\n### In scope\n\n### Out of scope\n\n### State owner\n\n## Method inventory\n\n| Method | Direction | Consumers | Side effect | Idempotent | Capability | Summary |\n\n## `<method/name>`\n\n### Semantics\n\n### Preconditions\n\n### Params\n\n| Field | Type | Required | Nullable | Constraints | Meaning |\n\n### Result\n\n| Field | Type | Required | Nullable | Constraints | Meaning |\n\n### Errors\n\n| Error | Retryable | Client action | Data |\n\n### Routing and ownership\n\n### Idempotency\n\n### Deadline and cancellation\n\n### Ordering\n\n### Security\n\n### Request fixture\n\n```json\n{}\n```\n\n### Success fixture\n\n```json\n{}\n```\n\n### Error fixtures\n\n```json\n{}\n```\n\n## State machine\n\n## Compatibility\n\n## Acceptance tests\n````",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-文档头",
        "title": "1. 文档头"
      },
      {
        "depth": 2,
        "id": "2-方法清单",
        "title": "2. 方法清单"
      },
      {
        "depth": 2,
        "id": "3-每个请求必填内容",
        "title": "3. 每个请求必填内容"
      },
      {
        "depth": 2,
        "id": "4-json-样例",
        "title": "4. JSON 样例"
      },
      {
        "depth": 2,
        "id": "5-生命周期和状态机",
        "title": "5. 生命周期和状态机"
      },
      {
        "depth": 2,
        "id": "6-顺序与一致性",
        "title": "6. 顺序与一致性"
      },
      {
        "depth": 2,
        "id": "7-类型化命令重放",
        "title": "7. 类型化命令重放"
      },
      {
        "depth": 2,
        "id": "8-超时取消和迟到响应",
        "title": "8. 超时、取消和迟到响应"
      },
      {
        "depth": 2,
        "id": "9-connection-与所有者",
        "title": "9. Connection 与所有者"
      },
      {
        "depth": 2,
        "id": "10-资源",
        "title": "10. 资源"
      },
      {
        "depth": 2,
        "id": "11-安全",
        "title": "11. 安全"
      },
      {
        "depth": 2,
        "id": "12-错误码",
        "title": "12. 错误码"
      },
      {
        "depth": 2,
        "id": "13-开发期破坏性变更",
        "title": "13. 开发期破坏性变更"
      },
      {
        "depth": 2,
        "id": "14-rust-实现验收",
        "title": "14. Rust 实现验收"
      },
      {
        "depth": 2,
        "id": "15-可复制模板",
        "title": "15. 可复制模板"
      },
      {
        "depth": 2,
        "id": "metadata",
        "title": "Metadata"
      },
      {
        "depth": 2,
        "id": "scope",
        "title": "Scope"
      },
      {
        "depth": 3,
        "id": "in-scope",
        "title": "In scope"
      },
      {
        "depth": 3,
        "id": "out-of-scope",
        "title": "Out of scope"
      },
      {
        "depth": 3,
        "id": "state-owner",
        "title": "State owner"
      },
      {
        "depth": 2,
        "id": "method-inventory",
        "title": "Method inventory"
      },
      {
        "depth": 2,
        "id": "methodname",
        "title": "<method/name>"
      },
      {
        "depth": 3,
        "id": "semantics",
        "title": "Semantics"
      },
      {
        "depth": 3,
        "id": "preconditions",
        "title": "Preconditions"
      },
      {
        "depth": 3,
        "id": "params",
        "title": "Params"
      },
      {
        "depth": 3,
        "id": "result",
        "title": "Result"
      },
      {
        "depth": 3,
        "id": "errors",
        "title": "Errors"
      },
      {
        "depth": 3,
        "id": "routing-and-ownership",
        "title": "Routing and ownership"
      },
      {
        "depth": 3,
        "id": "idempotency",
        "title": "Idempotency"
      },
      {
        "depth": 3,
        "id": "deadline-and-cancellation",
        "title": "Deadline and cancellation"
      },
      {
        "depth": 3,
        "id": "ordering",
        "title": "Ordering"
      },
      {
        "depth": 3,
        "id": "security",
        "title": "Security"
      },
      {
        "depth": 3,
        "id": "request-fixture",
        "title": "Request fixture"
      },
      {
        "depth": 3,
        "id": "success-fixture",
        "title": "Success fixture"
      },
      {
        "depth": 3,
        "id": "error-fixtures",
        "title": "Error fixtures"
      },
      {
        "depth": 2,
        "id": "state-machine",
        "title": "State machine"
      },
      {
        "depth": 2,
        "id": "compatibility",
        "title": "Compatibility"
      },
      {
        "depth": 2,
        "id": "acceptance-tests",
        "title": "Acceptance tests"
      }
    ],
    "searchText": " 本文规定 Desktop、CLI 或其他客户端提交 Zeta App Server 产品 API 需求时必须包含的内容。 zeta-rs 是已接受契约的 owner，并据此实现 Rust 协议、handler、typed client、生成类型和测试。 当前已经接受的产品契约见 zeta-app-server-api.md。 快速理解 这份规范帮助需求提出方把“希望增加一个接口”写成能够实现、测试和长期维护的产品契约。先说明 用户问题和权威状态，再定义方法、字段、错误、取消、幂等和恢复；不能只提交一组 DTO 或方法名。 你正在确认什么 文档必须给出的答案 --- --- 为什么需要接口 用户或客户端要完成的真实任务，以及明确不做什么 谁拥有状态 Server、连接、Session、Thread、Turn 或能力句柄中的唯一所有者 成功意味着什么 可观察结果、持久化边界和它不保证的内容 失败后怎么办 稳定错误、能否重试、取消和迟到结果处理 如何安全演进 能力协商、版本、生成物和所有调用方同步方式 1. 文档头 每份 API 文档必须先写： ``yaml title: Browser Capability API status: draft review accepted deprecated capabilityVersion: 1 owner: zeta-rs rustOwner: zeta-rs requestedBy: desktop consumers: desktop, cli lastUpdated: YYYY-MM-DD ` 还需说明： - 解决的问题； - 不在范围内的能力； - 权威状态由哪一端持有； - 哪些客户端需要该能力； - 当前开发期 breaking-change 与调用方同步更新策略。 2. 方法清单 先提供一张完整清单： Method Direction Consumers Side effect Idempotent Capability Summary --- --- --- ---: ---: --- --- session/request createThread Client → Server Desktop, CLI yes required sessions 创建 Thread browser/observe Server → Client Desktop host no n/a browser 观察目标 Direction 只能使用： - Client → Server request； - Server → Client request； - Server → Client notification； - 双向 Resource request。 同一 method 在 in-process、stdio、Unix socket 和 WebSocket 上必须保持相同语义。若某个 transport 不支持该能力，必须通过 initialize capability 明确声明，不能提供绕过 dispatcher 的隐藏 Rust 方法。 3. 每个请求必填内容 每个 request 单独一节，包含： 1. method 字符串； 2. direction； 3. 前置条件； 4. capability 与版本； 5. Params 的字段表； 6. Result 的字段表； 7. 所有错误码； 8. deadline 和 timeout； 9. cancellation 行为； 10. idempotency 规则； 11. owner/connection/thread/turn 路由； 12. 成功 JSON fixture； 13. 每类错误 JSON fixture。 字段表格式： Field Type Required Nullable Constraints Meaning --- --- ---: ---: --- --- threadId ThreadId yes no existing 目标 Thread maxBytes integer yes no 1..262144 最大 chunk required 和 nullable 必须分开说明。不能用“可选”同时表达字段缺失和 JSON null。 4. JSON 样例 请求： `json \"jsonrpc\": \"2.0\", \"id\": 42, \"method\": \"session/request\", \"params\": \"commandId\": \"command01...\", \"sessionId\": \"session123\", \"expectedSequence\": 17, \"request\": \"type\": \"startTurn\", \"threadId\": \"thread123\", \"input\": \"type\": \"text\", \"text\": \"分析当前网页\" ` 成功响应： `json \"jsonrpc\": \"2.0\", \"id\": 42, \"result\": \"turnId\": \"turn456\", \"sequence\": 18 ` 错误响应： `json \"jsonrpc\": \"2.0\", \"id\": 42, \"error\": \"code\": -32004, \"message\": \"CommandConflict\", \"data\": null ` Fixtures 是测试输入，必须是可解析的完整 JSON，不能只写 TypeScript 示例对象。 5. 生命周期和状态机 涉及状态的接口必须给出： - 状态全集； - 初始状态； - 合法转换； - 终态； - 失败和取消的区别； - connection 断开时的行为； - process crash/restart 后的行为。 示例： `text Created → Running → Completed → Failed → Cancelling → Interrupted ` 6. 顺序与一致性 必须说明： - 哪个字段提供 durable sequence； - 哪个字段提供 stream sequence； - response 与 notification 的因果顺序； - snapshot 与实时订阅如何避免事件空洞； - 客户端发现空洞后如何 resync； - 哪些通知可以合并，哪些不能丢弃。 7. 类型化命令重放 所有持久化副作用请求必须说明： - commandId 是否必填； - 对应的 typed command payload； - receipt 持久化在哪个 aggregate； - expectedSequence 针对哪个 aggregate； - 相同 ID + 相同 command 的稳定结果； - 相同 ID + 不同 command 的 CommandConflict； - 重启恢复后如何重放。 JSON-RPC id 只做当前连接的 response pairing，不能代替 commandId。 8. 超时、取消和迟到响应 Server → Client 请求必须定义： - deadline 字段和单位； - Turn 结束时如何取消； - connection 断开时如何清理； - serverRequest/resolved 通知； - deadline 后的迟到 response 是否忽略； - UI 如何关闭等待状态。 9. Connection 与所有者 必须分别说明： - Thread subscription； - Turn owner； - CapabilityHandle owner； - Resource owner； - Browser Target owner。 不能用“当前活动客户端”或“当前活动 Tab”这种可变全局概念代替稳定 owner。 10. 资源 大对象不得在普通业务消息中使用 Base64 或暴露本地路径。接口文档需要定义： - resourceId； - MIME type； - size； - SHA-256； - owner connection； - TTL； - 单资源与单连接 quota； - chunk 最大值； - offset 范围； - release、过期和断连清理。 11. 安全 每个涉及 Browser、文件、命令、网络、下载或外部 URL 的接口必须说明： - 信任边界； - 输入验证； - origin/路径/目标 allowlist； - secret 和敏感字段脱敏； - approval 是否需要； - approval 与 action digest 如何绑定； - host policy 的二次校验； - 审计记录。 12. 错误码 每个错误码必须稳定，并包含： Error Retryable Client action Data --- ---: --- --- ServerOverloaded yes jitter backoff retryAfterMs BrowserTargetUnavailable no 刷新目标列表 targetId 不要只写“失败时返回 error”。 13. 开发期破坏性变更 文档必须标注： - 哪些 Rust/TypeScript DTO、registry 和 schema 同步改变； - Desktop、CLI 与 TUI 哪些调用方必须同一变更迁移； - 旧开发数据是否需要清空； - 未知 notification/request 的稳定错误行为； - schema hash 和 fixtures 是否已重新生成。 当前开发阶段不保留旧 route、deprecated alias、DTO mapper 或 storage upcast。发布后的版本与 migration 政策必须另立 RFC，不能提前把兼容逻辑放进 Core。 14. Rust 实现验收 API 被视为完成，必须同时具备： - Rust DTO； - wire-specific DTO 与 canonical model 的必要机械映射； - handler 与路由； - capability/version 校验； - error code； - timeout/cancellation； - idempotency； - Rust tests； - JSON fixtures； - in-process App Server Client contract test； - stdio transport contract test； - TypeScript 生成； - JSON Schema 生成； - schema hash； - Desktop 与 CLI contract fixtures。 缺少其中任何一项，接口状态仍是 draft 或 review，不能标记为 accepted。 15. 可复制模板 建议从独立文件 zeta-api-interface-template.md 复制一份作为新接口文档。以下内容与该模板的要求一致： ``markdown <Capability / Domain API Metadata - Status: - Capability version: - Owner: - Rust owner: - Requested by: - Consumers: - Last updated: Scope In scope Out of scope State owner Method inventory Method Direction Consumers Side effect Idempotent Capability Summary <method/name Semantics Preconditions Params Field Type Required Nullable Constraints Meaning Result Field Type Required Nullable Constraints Meaning Errors Error Retryable Client action Data Routing and ownership Idempotency Deadline and cancellation Ordering Security Request fixture `json ` Success fixture `json ` Error fixtures `json ` State machine Compatibility Acceptance tests ```"
  },
  {
    "slug": "zeta-api-interface-template",
    "sourcePath": "docs/zeta-api-interface-template.md",
    "title": "<Capability / Domain> API",
    "description": "`<用一段面向调用方的文字说明：这项能力解决什么问题，成功后用户能观察到什么，以及最重要的 限制是什么。不要从方法名或 DTO 开始。>`",
    "group": "API 与协议",
    "markdown": "> 使用本模板前请先阅读\n> [Zeta API 接口文档规范](zeta-api-interface-requirements.md)。\n> 删除所有占位说明后再提交评审。\n\n## 快速理解\n\n`<用一段面向调用方的文字说明：这项能力解决什么问题，成功后用户能观察到什么，以及最重要的\n限制是什么。不要从方法名或 DTO 开始。>`\n\n| 常见场景 | 系统行为 | 调用方需要做什么 |\n| --- | --- | --- |\n| `<主要成功场景>` | `<可观察结果>` | `<下一步或无需操作>` |\n| `<需要等待、批准或恢复的场景>` | `<状态与失败语义>` | `<重试、取消或重新读取>` |\n| `<不支持的场景>` | `<明确拒绝或降级方式>` | `<替代入口>` |\n\n## 元数据\n\n```yaml\ntitle: <Capability / Domain> API\nstatus: draft\ncapabilityVersion: 1\nowner: zeta-rs\nrustOwner: zeta-rs\nrequestedBy: desktop\nconsumers:\n  - desktop\n  - cli\nlastUpdated: YYYY-MM-DD\n```\n\n## 范围\n\n### 范围内\n\n- `<本接口解决的问题>`\n\n### 范围外\n\n- `<明确不处理的能力>`\n\n### 状态所有者\n\n`<说明权威状态属于 Desktop、zeta-rs、connection、thread、turn 或 capability handle。>`\n\n### 兼容策略\n\n`<开发期说明 breaking change 如何同步更新所有调用方；发布后再说明兼容范围和升级方式。>`\n\n## 方法清单\n\n| Method | Direction | Consumers | Side effect | Idempotent | Capability | Summary |\n|---|---|---|---:|---:|---|---|\n| `<domain/method>` | `<Client → Server request>` | `<Desktop/CLI/host>` | `<yes/no>` | `<required/n/a>` | `<domain/v1>` | `<用途>` |\n\n## `<domain/method>`\n\n### 语义\n\n`<准确描述成功语义，以及它不保证什么。>`\n\n### 前置条件\n\n- `<连接、资源、状态或权限前置条件>`\n\n### 参数\n\n| Field | Type | Required | Nullable | Constraints | Meaning |\n|---|---|---:|---:|---|---|\n| `<field>` | `<type>` | `<yes/no>` | `<yes/no>` | `<范围/格式>` | `<含义>` |\n\n### 结果\n\n| Field | Type | Required | Nullable | Constraints | Meaning |\n|---|---|---:|---:|---|---|\n| `<field>` | `<type>` | `<yes/no>` | `<yes/no>` | `<范围/格式>` | `<含义>` |\n\n### 错误\n\n| Error | Retryable | Client action | Data |\n|---|---:|---|---|\n| `<StableErrorName>` | `<yes/no>` | `<处理方式>` | `<稳定字段>` |\n\n### 路由与所有权\n\n`<说明 connection、thread、turn、capability、resource 或 browser target 的 owner 和路由。>`\n\n### 幂等性\n\n`<副作用请求必须定义 commandId、typed command receipt、expectedSequence、冲突和重启重放语义。>`\n\n### 截止时间与取消\n\n`<定义 deadline 单位、主动取消、断连、Turn 终止和迟到响应处理。>`\n\n### 顺序\n\n`<定义 durable sequence、stream sequence、response/notification 因果顺序和 resync。>`\n\n### 安全性\n\n`<定义验证、allowlist、审批、action digest、脱敏、host policy 和审计。>`\n\n### 请求样例\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 1,\n  \"method\": \"<domain/method>\",\n  \"params\": {}\n}\n```\n\n### 成功样例\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 1,\n  \"result\": {}\n}\n```\n\n### 错误样例\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 1,\n  \"error\": {\n    \"code\": -32000,\n    \"message\": \"<StableErrorName>\",\n    \"data\": {}\n  }\n}\n```\n\n## 通知\n\n`<逐项定义通知字段、顺序、丢弃/合并策略。没有通知时写 None。>`\n\n## 生命周期与状态机\n\n```text\n<Initial> → <Running> → <Terminal>\n```\n\n`<说明失败、取消、断连和进程重启后的状态。>`\n\n## 资源生命周期\n\n`<如涉及大对象，定义 ResourceRef、digest、TTL、quota、chunk 和 release；否则写 None。>`\n\n## 兼容性\n\n- Synchronized callers: `<Rust/Desktop/CLI/TUI 需同一变更迁移的入口>`\n- Existing development data: `<是否必须清空>`\n- Unknown notifications: `<处理方式>`\n- Unknown requests: `<稳定错误>`\n- Schema regeneration: `<fixtures/hash/generated TypeScript>`\n- Transport parity: `<in-process/stdio/socket/websocket 的一致性或 capability 限制>`\n\n## 验收测试\n\n- [ ] Success fixture parses and round-trips.\n- [ ] Every stable error has a fixture.\n- [ ] Lifecycle transitions are covered.\n- [ ] Idempotency replay and conflict are covered.\n- [ ] Deadline, cancellation, disconnect, and late response are covered.\n- [ ] Ordering gap and resync are covered.\n- [ ] Security rejection paths are covered.\n- [ ] Rust DTO and mapper tests pass.\n- [ ] In-process client and external transport use the same dispatcher behavior.\n- [ ] TypeScript generation compiles.\n- [ ] JSON Schema and schema hash are updated.\n- [ ] Contract fixtures pass for every declared consumer.",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "元数据",
        "title": "元数据"
      },
      {
        "depth": 2,
        "id": "范围",
        "title": "范围"
      },
      {
        "depth": 3,
        "id": "范围内",
        "title": "范围内"
      },
      {
        "depth": 3,
        "id": "范围外",
        "title": "范围外"
      },
      {
        "depth": 3,
        "id": "状态所有者",
        "title": "状态所有者"
      },
      {
        "depth": 3,
        "id": "兼容策略",
        "title": "兼容策略"
      },
      {
        "depth": 2,
        "id": "方法清单",
        "title": "方法清单"
      },
      {
        "depth": 2,
        "id": "domainmethod",
        "title": "<domain/method>"
      },
      {
        "depth": 3,
        "id": "语义",
        "title": "语义"
      },
      {
        "depth": 3,
        "id": "前置条件",
        "title": "前置条件"
      },
      {
        "depth": 3,
        "id": "参数",
        "title": "参数"
      },
      {
        "depth": 3,
        "id": "结果",
        "title": "结果"
      },
      {
        "depth": 3,
        "id": "错误",
        "title": "错误"
      },
      {
        "depth": 3,
        "id": "路由与所有权",
        "title": "路由与所有权"
      },
      {
        "depth": 3,
        "id": "幂等性",
        "title": "幂等性"
      },
      {
        "depth": 3,
        "id": "截止时间与取消",
        "title": "截止时间与取消"
      },
      {
        "depth": 3,
        "id": "顺序",
        "title": "顺序"
      },
      {
        "depth": 3,
        "id": "安全性",
        "title": "安全性"
      },
      {
        "depth": 3,
        "id": "请求样例",
        "title": "请求样例"
      },
      {
        "depth": 3,
        "id": "成功样例",
        "title": "成功样例"
      },
      {
        "depth": 3,
        "id": "错误样例",
        "title": "错误样例"
      },
      {
        "depth": 2,
        "id": "通知",
        "title": "通知"
      },
      {
        "depth": 2,
        "id": "生命周期与状态机",
        "title": "生命周期与状态机"
      },
      {
        "depth": 2,
        "id": "资源生命周期",
        "title": "资源生命周期"
      },
      {
        "depth": 2,
        "id": "兼容性",
        "title": "兼容性"
      },
      {
        "depth": 2,
        "id": "验收测试",
        "title": "验收测试"
      }
    ],
    "searchText": " 使用本模板前请先阅读 Zeta API 接口文档规范。 删除所有占位说明后再提交评审。 快速理解 <用一段面向调用方的文字说明：这项能力解决什么问题，成功后用户能观察到什么，以及最重要的 限制是什么。不要从方法名或 DTO 开始。 常见场景 系统行为 调用方需要做什么 --- --- --- <主要成功场景 <可观察结果 <下一步或无需操作 <需要等待、批准或恢复的场景 <状态与失败语义 <重试、取消或重新读取 <不支持的场景 <明确拒绝或降级方式 <替代入口 元数据 ``yaml title: <Capability / Domain API status: draft capabilityVersion: 1 owner: zeta-rs rustOwner: zeta-rs requestedBy: desktop consumers: - desktop - cli lastUpdated: YYYY-MM-DD ` 范围 范围内 - <本接口解决的问题 范围外 - <明确不处理的能力 状态所有者 <说明权威状态属于 Desktop、zeta-rs、connection、thread、turn 或 capability handle。 兼容策略 <开发期说明 breaking change 如何同步更新所有调用方；发布后再说明兼容范围和升级方式。 方法清单 Method Direction Consumers Side effect Idempotent Capability Summary --- --- --- ---: ---: --- --- <domain/method <Client → Server request <Desktop/CLI/host <yes/no <required/n/a <domain/v1 <用途 <domain/method 语义 <准确描述成功语义，以及它不保证什么。 前置条件 - <连接、资源、状态或权限前置条件 参数 Field Type Required Nullable Constraints Meaning --- --- ---: ---: --- --- <field <type <yes/no <yes/no <范围/格式 <含义 结果 Field Type Required Nullable Constraints Meaning --- --- ---: ---: --- --- <field <type <yes/no <yes/no <范围/格式 <含义 错误 Error Retryable Client action Data --- ---: --- --- <StableErrorName <yes/no <处理方式 <稳定字段 路由与所有权 <说明 connection、thread、turn、capability、resource 或 browser target 的 owner 和路由。 幂等性 <副作用请求必须定义 commandId、typed command receipt、expectedSequence、冲突和重启重放语义。 截止时间与取消 <定义 deadline 单位、主动取消、断连、Turn 终止和迟到响应处理。 顺序 <定义 durable sequence、stream sequence、response/notification 因果顺序和 resync。 安全性 <定义验证、allowlist、审批、action digest、脱敏、host policy 和审计。 请求样例 `json \"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"<domain/method \", \"params\": ` 成功样例 `json \"jsonrpc\": \"2.0\", \"id\": 1, \"result\": ` 错误样例 `json \"jsonrpc\": \"2.0\", \"id\": 1, \"error\": \"code\": -32000, \"message\": \"<StableErrorName \", \"data\": ` 通知 <逐项定义通知字段、顺序、丢弃/合并策略。没有通知时写 None。 生命周期与状态机 `text <Initial → <Running → <Terminal ` <说明失败、取消、断连和进程重启后的状态。 资源生命周期 <如涉及大对象，定义 ResourceRef、digest、TTL、quota、chunk 和 release；否则写 None。 兼容性 - Synchronized callers: <Rust/Desktop/CLI/TUI 需同一变更迁移的入口 - Existing development data: <是否必须清空 - Unknown notifications: <处理方式 - Unknown requests: <稳定错误 - Schema regeneration: <fixtures/hash/generated TypeScript - Transport parity: <in-process/stdio/socket/websocket 的一致性或 capability 限制 ` 验收测试 - Success fixture parses and round-trips. - Every stable error has a fixture. - Lifecycle transitions are covered. - Idempotency replay and conflict are covered. - Deadline, cancellation, disconnect, and late response are covered. - Ordering gap and resync are covered. - Security rejection paths are covered. - Rust DTO and mapper tests pass. - In-process client and external transport use the same dispatcher behavior. - TypeScript generation compiles. - JSON Schema and schema hash are updated. - Contract fixtures pass for every declared consumer."
  },
  {
    "slug": "zeta-app-server-api",
    "sourcePath": "docs/zeta-app-server-api.md",
    "title": "Zeta App Server API",
    "description": "本文描述当前开发期的唯一 App Server 契约。项目不保留旧 wire API、旧 DTO 或旧持久化格式 的兼容入口；Rust DTO、生成的 TypeScript 和 JSON Schema 必须始终一致。",
    "group": "API 与协议",
    "markdown": "```yaml\ntitle: Zeta App Server API\nstatus: development\nowner: zeta-rs\nconsumers:\n  - desktop\n  - cli\nlastUpdated: 2026-08-13\n```\n\n本文描述当前开发期的唯一 App Server 契约。项目不保留旧 wire API、旧 DTO 或旧持久化格式\n的兼容入口；Rust DTO、生成的 TypeScript 和 JSON Schema 必须始终一致。\n\n当前 [`zeta-mcp-server`](mcp-server.md) stdio/Streamable HTTP 接口面是本 API 的外层\nAgent-as-tool adapter；它通过 App Server client 复用这里定义的 Session/Thread/Turn contract，\n不建立第二套 execution API。该 adapter 已将 Thread subscription/update 投影为 MCP progress，\n将 approval/user-input 映射为 form elicitation，并在共享 profile SQLite 中持久化外部 invocation\nreceipt。Receipt 只拥有 MCP correlation/recovery，不改变本 API 的 durable authority。\n\n具体 method registry、artifact generator 与 schema fixture 见\n[`zeta-app-server-protocol` README](../zeta-rs/app-server-protocol/README.md)；JSON-RPC dispatch、\nsubscription broker、resource store 与 local composition 见\n[`zeta-app-server` README](../zeta-rs/app-server/README.md)。本文拥有跨客户端 API 语义与演进方向，\n两个 README 拥有当前实现接口与修改路径。\n\n## 快速理解\n\nApp Server API 是 Desktop、CLI、TUI 和其他适配器访问 Zeta 产品能力的唯一版本化接口；它暴露\nSession、Thread、Turn 和更新流，不建立第二套领域模型。\n\n| 客户端需求 | 使用方式 | 关键保证 |\n| --- | --- | --- |\n| 创建一次工作 | 创建 Session，再创建 Thread 并启动 Turn | 产品身份与持久化语义来自统一协议 |\n| 关闭一个 Session Tab | 前端通过 `session/request` 提交 `request.type = stop` | 持久化停止 Session，并中断其所有活动子 Turn；仅断开连接不会停止 Session |\n| 持续显示执行进度 | 订阅 Thread 更新并按序列消费 | 发现缺口时重新读取快照，不猜测丢失状态 |\n| 修改配置或资源 | 调用类型化方法并携带命令身份 | 重复命令可重放结果，冲突载荷会被拒绝 |\n| 响应批准或用户输入 | 回复等待中的类型化请求 | 回复绑定精确请求和当前 Thread |\n| 让 Agent 操作 Desktop 浏览器 | Desktop 初始化时声明 browser host | Rust 保留批准和目标 owner，Electron Main 只执行语义动作 |\n| 连接本地 App Server | 先初始化并校验能力和模式哈希 | 初始化前不能调用产品方法 |\n| 协议发生不兼容变化 | 同步修改 Rust 类型、生成物和调用方 | 开发期不保留隐藏的旧 DTO 入口 |\n\n### 唯一外部门禁\n\n这条规则适用于所有 `Session`、`Thread`、`Turn`、`ThreadItem` 产品能力：App Server 同时是\n客户端请求进入 Core 的唯一入口，也是 Core 更新离开系统的唯一出口。\n\n| 参与者 | 允许路径 | 禁止路径 |\n| --- | --- | --- |\n| Desktop、CLI、TUI 和其他客户端 | 版本化 App Server 协议 → 分发器 | 直接链接 Core、Store、Provider 或读取私有运行时接口 |\n| 进程内宿主 | 类型化客户端 → 同一个 App Server 分发器 | 为性能增加隐藏的进程内业务方法 |\n| App Server | 校验、路由、订阅、DTO 编解码和事件投影 → Core | 复制领域归约器（reducer）、直接写 Store 或消费 Provider 内部流 |\n| Core | 持有 Session/Thread/Turn/Item 权威状态并调用内部端口 | 依赖 App Server wire、客户端状态或 UI 生命周期 |\n\n```mermaid\nflowchart LR\n    client[\"Desktop / CLI / TUI / other client\"] --> request[\"versioned App Server request\"]\n    request --> dispatcher[\"App Server dispatcher\"]\n    dispatcher --> core[\"Core Session/Thread/Turn runtime\"]\n    core --> provider[\"Provider / tools / stores\"]\n    core --> dispatcher\n    dispatcher --> events[\"App Server result + update stream\"]\n    events --> client\n```\n\n进程内 App Server、stdio/JSONL 和未来的远程 App Server 只是不同传输方式。它们必须保持相同的\n请求、结果、错误和通知语义。`zeterm` 当前的直接 Rust 终端/PTY 组合只覆盖\n终端宿主；一旦该宿主承载 Agent 的 Session/Thread/Turn/Item 能力，也必须接入同一 App Server\n门禁，不能新增 Core 旁路。\n\n## 1. 产品模型\n\nCanonical 产品实体和内部契约的详细定义见 [`protocol.md`](protocol.md)。本 API 直接暴露\n其中的 readable Session/Thread/Turn/ThreadItem view，不维护第二份领域定义。\n\n- App Server connection/session 只是传输生命周期，不能与产品 Session 混用。\n\nSession 不嵌入 Thread 历史，只保存 membership、lineage 和 lifecycle。Fork 的 lineage 固定为\n`parentThreadId + parentSequence`，因此父 Thread 后续继续执行不会改变已创建分支的起点。\n\n## 2. 一致性模型\n\n每个修改命令都使用：\n\n- `commandId`：客户端生成的稳定命令身份；\n- `expectedSequence`：客户端观察到的目标 aggregate sequence；\n- typed command payload：参与重放与冲突判断。\n\n同一 `commandId + typed payload` 重试返回原结果；同一 `commandId` 携带不同 payload 返回\n`CommandConflict`。JSON-RPC request `id` 只做当前 connection 的 response pairing，不能替代\n`commandId`。\n\nSession 与每个 Thread 拥有独立 durable sequence：\n\n- Session sequence 只排序 topology 与 lifecycle；\n- Thread sequence 只排序该分支内的 Turn 与 Item；\n- 修改一个 Thread 不会占用 Session 或其他 Thread 的 sequence；\n- `expectedSequence` 始终针对 method 所修改的 aggregate。\n\n## 3. 传输\n\n当前外部 transport 是 UTF-8 JSONL/stdio：\n\n- 每行一个完整 JSON-RPC 2.0 message；\n- 单条 message 最大 1,048,576 bytes；\n- stdout 只允许协议 message，stderr 只用于诊断；\n- 同一 request 的 response 先于由它产生的 causal notifications；\n- connection 断开后 request ID、subscription、Resource 与 Terminal ownership 全部失效。\n\nIn-process client 使用 protocol-owned typed request/event channel，可以省略 JSON string\n编解码，但必须经过相同 initialize gate、method dispatcher、result/error envelope 与\nnotification contract，不能拥有隐藏业务接口。JSONL/stdio、WebSocket 等外部 transport 才在\n边界执行 wire encoding。\n\n## 4. 初始化\n\n`initialize` 必须是 connection 的首个 request。\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": 1,\n  \"method\": \"initialize\",\n  \"params\": {\n    \"clientInfo\": { \"name\": \"zeta-desktop\", \"version\": \"0.1.0\" },\n    \"capabilities\": {\n      \"notifications\": true,\n      \"browser\": { \"version\": 1, \"observe\": true, \"input\": true }\n    }\n  }\n}\n```\n\n返回值包含 `serverInfo`、完整 schema 的 `schemaHash`、server capability，以及 composition\n边界冻结的 `slashCommands` snapshot：\n\n```json\n{\n  \"capabilities\": {\n    \"sessions\": true,\n    \"threads\": true,\n    \"turns\": true,\n    \"resources\": true,\n    \"fileSystem\": true,\n    \"workspaceSearch\": true,\n    \"codeIndex\": true,\n    \"cloudCodeIndex\": false,\n    \"terminal\": true,\n    \"mcp\": true,\n    \"mcpOAuth\": false,\n    \"typst\": true,\n    \"updateReplay\": true\n  },\n  \"slashCommands\": [\n    {\n      \"name\": \"diagnose\",\n      \"description\": \"inspect the current workspace\",\n      \"argumentMode\": \"optional\"\n    }\n  ]\n}\n```\n\nschema hash 不一致时客户端必须拒绝继续运行。\n`slashCommands` 每项的 `name` 只能使用 lowercase ASCII letters、digits 与 interior hyphens，\ndescription 不能为空，同一 snapshot 中 name 必须唯一。该 snapshot 负责 discoverability 与\ninline argument parsing；提交仍通过 `session/request` 的 `StartTurn.input`，并保留 `/name`、text/image 顺序。\n校验、local/server 合并与 Rust client 交互状态的 canonical owner 是\n[`zeta-slash-commands`](../zeta-rs/slash-commands/README.md)；App Server 只组合并发布 server snapshot。\n三种 client surface 的合并、执行与渲染边界见 [`slash-commands.md`](slash-commands.md)。\n\n### 客户端-hosted 浏览器能力\n\nBrowser 是当前 Server → Client request capability。只有在 `initialize` 中提交 version 1 browser\ncapability 的连接才能成为宿主；`observe` 与 `input` 分别声明页面观察和语义输入支持。该声明是\n短暂的 connection routing authority，不进入 Session、Thread 或持久化配置。\n\n| Host method | Params | Result | 当前语义 |\n| --- | --- | --- | --- |\n| `browser/create` | `{ url }` | `{ targetId }` | 创建一个隔离、默认隐藏的新目标 |\n| `browser/observe` | exact target + 三个 include flag | 页面状态 + 可选 AX/DOM/PNG | 不执行脚本，不返回 Electron 对象 |\n| `browser/perform` | tagged semantic action | `{ targetId }` | 导航、node click/type、滚动、后退或刷新 |\n| `browser/close` | `{ targetId }` | `null` | 关闭精确目标 |\n\n请求 ID 使用保留的非空字符串，与 Client → Server 正整数 ID 隔离。App Server 把 create result 的\n`targetId` 绑定到响应它的连接；此后不能由另一连接响应，也不能把观察或动作结果切换到另一个\n目标。connection 退出后 target authority 立即失效，Desktop 同时回收由该 host connection 创建的\n原生目标。\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": \"browser-host:1:7\",\n  \"method\": \"browser/perform\",\n  \"params\": {\n    \"action\": {\n      \"type\": \"click\",\n      \"targetId\": \"browser_target_123\",\n      \"target\": { \"nodeId\": \"42\" }\n    }\n  }\n}\n```\n\n成功响应必须保留同一 ID 和 target：\n\n```json\n{\n  \"jsonrpc\": \"2.0\",\n  \"id\": \"browser-host:1:7\",\n  \"result\": { \"targetId\": \"browser_target_123\" }\n}\n```\n\nApp Server deadline 为 30 秒。Turn 取消或 deadline 到达时，Server 发送\n`$/cancelRequest { id }`；client 终止尚未开始的步骤，并用 `-32800` 返回已取消的在途请求。\n已放弃请求的晚到 terminal response 可以丢弃，已完成请求的重复 response 仍是协议错误。\n截图 payload 只允许 `image/png`，App Server 校验 decoded length、Base64 和 PNG signature 后才\n创建 connection-owned Resource。\n\n该契约从不接受任意 CDP method、JavaScript source、localhost 调试端口或 Node sidecar 配置。\n完整 Browser Tool generation 同时要求可信工作区和至少一个同时声明 `observe + input` 的 live\nversion 1 宿主；Restricted、信任撤销或最后一个完整宿主断开都会移除该工具 port。恢复条件后\n只影响后续 Tool generation，不恢复或重放旧调用。\nDesktop 当前实现和 Playwright 后续边界见\n[`zeta-desktop-architecture.md#7-浏览器能力`](zeta-desktop-architecture.md#7-浏览器能力)。\n\n## 5. 方法清单\n\n| Method | Aggregate | Effect |\n| --- | --- | --- |\n| `session/create` | new Session | 创建任务 |\n| `session/read` | Session | 读取 canonical snapshot |\n| `session/list` | global | 列出 Session |\n| `session/subscribe` | connection | Session snapshot + `afterSequence` 之后的 durable gap + child Thread projections |\n| `session/request` | Session | canonical typed mutation request；覆盖 Session、child Thread 与 Turn 操作 |\n| `session/unsubscribe` | connection | 删除订阅 |\n| `session/request` | Session | 通过 tagged request 完成 Session、child Thread 和 Turn mutation |\n| `model/list` | global model catalog | 列出当前已配置 direct provider 与已登录 ChatGPT account 可选择的 Codex 模型 |\n| `session/thread/read` | Session + Thread | 读取 canonical Thread snapshot |\n| `session/thread/subscribe` | Session + Thread + connection | snapshot + `afterSequence` 之后的 durable gap |\n| `session/thread/unsubscribe` | Session + Thread + connection | 删除 child Thread 订阅 |\n| `config/read` | config | 读取配置 |\n| `connector/list` | Connector authority | 读取不含 secret/reference 的外部账号连接投影 |\n| `connector/connect/apiToken` | Connector authority + secret store | retry-safe 保存 API token 并发布 connected account |\n| `connector/connect/oauth/start` / `complete` / `cancel` | Connector OAuth owner | 启动 exact PKCE flow，一次性消费 callback state/code，或显式结束 abandoned flow |\n| `connector/disconnect` | Connector authority + secret store | 先撤销 runtime readiness，再报告 credential cleanup 状态 |\n| `connector/credential/cleanup` | Connector credential owner | 重试 durable post-disconnect secret 删除义务 |\n| `plugin/list` | Plugin authority | 分别投影 installed/enabled/granted/effective package 状态 |\n| `plugin/marketplace/list` / `plugin/install` / `plugin/update` / `plugin/rollback` | Plugin Marketplace + Plugin authority | 只接受 Host 注册 Marketplace 中由 `marketplaceId`、package identity 和 digest 精确绑定的条目；安装后不自动启用或 grant |\n| `plugin/enable` / `disable` / `grant` / `revokeGrant` / `uninstall` | Plugin authority | exact-package CAS lifecycle mutation |\n| `language/marketplace/list` | Language Marketplace + activation authority | 读取 signed package catalog、host compatibility 与当前 profile activation 投影 |\n| `language/marketplace/install` | Language Marketplace + activation authority + provider registry | revision-check 后重新匹配并下载 exact signed package，激活 exact receipt，并热替换 provider registry；调用前的用户确认由 Desktop 拥有 |\n| `config/update` | config | typed command 更新配置 |\n| `execPolicy/rule/upsert` / `execPolicy/rule/remove` | config + local policy runtime | revision-safe 持久化 User typed rule，并为未来 Tool safe point 重组 policy snapshot |\n| `toolSearch/configure` | config + semantic model runtime | 选择词法模式，或探活 exact embedding 模型后启用混合 Tool Search |\n| `workspace/codeIndex/semantic/configure` / `authorize` / `revoke` | config + Workspace | 独立配置 semantic CodeIndex，并显式管理源码外发授权 |\n| `workspace/codeIndex/semantic/cancel` / `retry` | semantic index job | 取消或重新调度 exact-generation 本地语义 projection；status 返回无内容进度计数 |\n| `languageServer/configure` / `languageServer/remove` | config | revision-safe 修改或恢复 language-server mode/path preference |\n| `provider/configure` / `provider/remove` | config | 修改 Provider declaration |\n| `mcp/server/upsert` / `mcp/server/remove` / `mcp/server/enablement/set` | config | 修改 standalone MCP desired config |\n| `mcp/server/connect` / `mcp/server/disconnect` | runtime | 设置 process-local lifecycle intent，不改变 Config revision |\n| `mcp/server/status` | read | 读取 active Config/Plugin/Connector MCP runtime 的 redacted lifecycle 与 generation projection |\n| `mcp/oauth/start` / `mcp/oauth/complete` | MCP OAuth owner | 为 exact standalone Config server 启动和一次性完成 PKCE flow |\n| `mcp/oauth/refresh` / `mcp/oauth/revoke` | MCP OAuth owner + secret store | 轮换 runtime/lifecycle credential；或先断开 runtime、远端 revoke 后删除本地 secret |\n| `skill/source/add` / `skill/source/remove` / `skill/source/enablement/set` | config | 修改 User Skill source |\n| `plugin/request/upsert` / `plugin/request/remove` / `plugin/request/enablement/set` | config | 修改 exact Plugin request；不安装或激活 |\n| `hook/upsert` / `hook/remove` / `hook/enablement/set` | config + Hook runtime | 修改 declarative Hook；trusted Workspace 的未来 safe point 按 immutable snapshot 执行匹配的 sandbox process |\n| `skills/list` | global Skill catalog | 读取 cached projection 或请求完整 refresh |\n| `skill/enablement/set` | config + Skill catalog | revision-checked 启用/禁用 exact `SkillId` |\n| `skill/resource/open` | Skill runtime + Resource | 将 digest-pinned package resource materialize 为 connection-owned resource |\n| `resource/metadata` | Resource | 读取元数据 |\n| `resource/read` | Resource | 分块读取 |\n| `resource/release` | Resource | 释放 connection-owned resource |\n| `fs/getMetadata` | workspace | 读取根相对路径的 metadata |\n| `fs/readDirectory` | workspace | 枚举根相对目录的直接子项 |\n| `fs/readFile` | workspace | 读取不超过 10 MiB 的 UTF-8 文件 |\n| `fs/writeFile` | workspace | 原子替换或新建不超过 10 MiB 的 UTF-8 文件 |\n| `syntax/analyze` | stateless syntax | 返回同一 revision 的 bounded token/fold/symbol/diagnostic facts |\n| `syntax/selectionRanges` | stateless syntax | 只沿当前 UTF-16 selections 返回 bounded parser ancestor scopes |\n| `git/status` | workspace | 读取 HEAD、upstream 和 index/worktree change snapshot |\n| `git/textDiff` | workspace | 读取 status 及有界 UTF-8 HEAD/worktree text diff projection |\n| `git/branch/list` | workspace | 列出现有本地分支及 current/upstream 信息 |\n| `git/branch/switch` | workspace | 切换到 host 重新解析确认存在的本地分支 |\n| `git/stage` | workspace | stage 一组 workspace-relative path |\n| `git/unstage` | workspace | 从 index 移除一组 path 的 staged change |\n| `git/discardWorktree` | workspace | 恢复 tracked working-tree change，不删除 untracked 文件 |\n| `git/commit` | workspace | 使用有界非空 message 创建 commit |\n| `git/fetch` | workspace | non-interactive fetch all remotes 并 prune |\n| `git/pull` | workspace | non-interactive fast-forward-only pull |\n| `git/push` | workspace | 按当前 Git upstream/default 配置 push |\n| `workspace/search/start` | connection + workspace | 启动有界内容搜索 |\n| `workspace/search/read` | connection + search job | 按游标读取最多 200 条结果 |\n| `workspace/search/cancel` | connection + search job | 取消并释放搜索 |\n| `workspace/codeIndex/status` | workspace | 读取本地 index lifecycle 与 generation counters |\n| `workspace/codeIndex/search` | workspace | 返回有界、revision-bound 的本地 lexical chunks |\n| `workspace/symbolIndex/status` / `search` | workspace | 读取 declaration projection 状态并执行有界 local fuzzy symbol query |\n| `workspace/codeIntelligence/document/synchronize` / `close` | workspace + editor document | 发布或释放 ephemeral dirty snapshot；不持久化 overlay |\n| `workspace/codeIndex/retrieve` | workspace | 融合已启用召回源，返回复核、去重、受预算约束的 excerpts |\n| `workspace/codeIndex/rebuild` | workspace | 同步执行一次 full reconcile |\n| `workspace/codeIndex/cloud/status` | workspace | 读取 selected deployment、grant 与 local/remote generation state |\n| `workspace/codeIndex/cloud/preview` | workspace | 本地计算 proposed scope 的 chunk 外发单位与 bytes，不授权、不触网 |\n| `workspace/codeIndex/cloud/authorize` | workspace | 持久化 root-bound destination/scope/byte grant |\n| `workspace/codeIndex/cloud/sync` | workspace | 按 grant 复核 source revision 后调用 provider publication |\n| `workspace/codeIndex/cloud/revoke` | workspace | 先持久化 Revoking，再请求 provider 幂等删除 |\n| `terminal/profile/list` | workspace | 列出 App Server 冻结的可信 Shell Profile |\n| `terminal/create` | connection + workspace | 在可信 workspace root 启动 PTY |\n| `terminal/write` | connection + Terminal | 写入有界 UTF-8 输入 batch |\n| `terminal/resize` | connection + Terminal | 修改 PTY rows/cols |\n| `terminal/read` | connection + Terminal | 按 sequence 拉取有界 Base64 输出 |\n| `terminal/close` | connection + Terminal | 终止并释放 PTY |\n\nConnector account 是 GitHub、Slack 等外部产品账号，不是第 11 节的 Zeta account/login control plane。\n\n### Connector 外部账号连接\n\n`initialize.capabilities.connectors` 只有在 host 注入 `ConnectorCredentialService` 时为 true；\n`initialize.capabilities.plugins` 只有在 host 注入 live Plugin authority 时为 true。客户端收到\n`connector/changed { generation }` 后重新调用 `connector/list`；notification 不携带 account body、\ncredential reference 或 secret。\n\nconnect/disconnect 都携带 `commandId` 与 `expectedGeneration`。API-token connect 还携带单调的\n`connectionGeneration`、外部 account ID/display name 和 `apiToken`。Secret field 只存在于 inbound\nrequest；App Server 把它移出通用 JSON value 后包装为 zeroizing `SecretValue`，任何 list/result/error\n都不得回显。一次 successful connect 会因 Begin 和 Complete 两次 commit 推进两个 snapshot generation。\n\ndisconnect 先提交新的 disconnected generation，再删除 secret。结果中的 `credentialCleanup` 为\n`deleted`、`alreadyAbsent` 或 `retryRequired`；后者不回滚 disconnect。客户端不得因为 cleanup 需要重试\n而继续显示 tools ready。失败的删除会持久化为 `credentialCleanupPending`，并由\n`connector/credential/cleanup` 收敛。`reauthorizationRequired` 同样不是 ready 状态，通常表示 Plugin package/runtime\nauthorization revision 已改变。\n\nOAuth start 返回 browser navigation URL 与 opaque flow ID；callback state/code 只进入 inbound-only\ncomplete DTO。PKCE verifier 留在 Connector OAuth service 内存，Desktop 的随机 loopback callback listener\n由 Electron main 持有，Renderer 不接触 verifier 或 provider token。具体 provider adapter 必须由产品\ncomposition 显式注入。\n\n### 独立 MCP OAuth\n\n`initialize.capabilities.mcp` 表示 host 安装了 Config-backed MCP runtime surface；\n`initialize.capabilities.mcpOAuth` 只有在 host 注入至少一个 standalone MCP OAuth provider 时为 true。\n这两个 capability 分开：有 MCP Config/状态 RPC 不代表任意 server 都有 OAuth adapter。\n\n`mcp/oauth/start` 只接受 exact server ID 与 redirect URI，返回 browser navigation URL 和 process-local\nflow ID。目标必须是带 credential reference 的 HTTPS Streamable HTTP server；redirect 只允许 HTTPS 或\n本机 loopback HTTP。`mcp/oauth/complete` 的 state 与 authorization code 是 inbound-only zeroizing\nfield，不出现在 result/error/debug；flow 一次性消费，并在 exchange 前重新读取当前 Config target。\n\n`mcp/oauth/refresh` 轮换 SecretStore 中分离的 runtime bearer 与 lifecycle secret，并触发 connect\nreconcile。`mcp/oauth/revoke` 先设置 disconnect intent，等待 active Tool generation 移除该 server，\n再调用 provider remote revoke；只有远端成功后才删除本地 secret。具体 discovery、client identity、\nscope、token parsing、audience 和 provider\nendpoint policy 属于 host 注入的 provider adapter，不属于 App Server protocol。\n\nPlugin request 是 config intent；Plugin lifecycle authority 是另一层事实。`plugin/list` 不把它们压成\n一个布尔值，而是分别返回 enabled、granted 与 effective，只有 exact installed package 同时 enabled 且\ngranted 时才进入 activation。`plugin/install`、`plugin/update` 和 `plugin/rollback` 只接受 Host 注册\nMarketplace 中按 marketplace、package identity 与 digest 精确绑定的条目；Renderer 不能提交任意 host\nfilesystem path。\n\nZeta account control plane 另见[第 11 节](#11-account-与登录)。其 Rust DTO、TypeScript 与 JSON Schema\n由同一个 registry 生成并同步提交。\n\n### 文件系统\n\nFilesystem method 的 `path` 是配置 workspace root 下的相对路径；空字符串表示 root。\n绝对路径、父目录逃逸和解析后越过 root 的 symlink 会在可信 Rust 边界被拒绝。Desktop 的\nIPC 层会先做同形状校验以便快速失败，但不承担最终授权。\n\n当前 contract 提供 `fs/getMetadata`、`fs/readDirectory`、`fs/readFile` 和 `fs/writeFile`，\n用于单根 Folder 的 Explorer、文本文件打开和后端保存。读写均限制为不超过 10 MiB 的 UTF-8；\n写入在目标同目录完成有界临时写、flush 和原子替换，保留现有文件权限，不隐式创建父目录。\n\nApp Server 对整个 workspace root 建立递归 watcher。普通变化经 75ms debounce 后发布\n`fs/changed { type: \"pathsChanged\", paths }`，其中 path 全部是 workspace-relative；backend\n可能丢失事件时发布 `fs/changed { type: \"rescanRequired\" }`。两种通知都是失效 hint，不是\ndurable event 或文件内容事实，客户端收到后必须重新读取自己拥有的视图。\n\nFilesystem contract 仍不包含重命名、删除、多根 Workspace 或跨请求 snapshot 一致性。当前\nDesktop 尚未调用 `fs/writeFile` 或消费 `fs/changed`；这些属于独立的前端接入阶段。\n\n### 语法事实\n\n`syntax/analyze` 接收最多 4 MiB 的完整 UTF-8 text、language 与 host revision，返回同一 revision 的\nbounded tokens、folding ranges、document symbols、parse diagnostics 和 `hasErrors`。它是 stateless\nprojection，不建立 App Server-owned editor document，也不返回 Tree-sitter node。\n\n`syntax/selectionRanges` 使用同一 document envelope，并额外接受最多 1,024 个 UTF-16 ranges；server\n拒绝越界位置和 surrogate pair 中间位置，只沿每个 exact selection 的 named parser ancestors 返回\n默认最多 64 层，去重后按 source order 投影。该 operation 与普通 analyze 分离，避免 token/diagnostic\n请求携带整棵树的 selection nodes。Desktop 必须用 captured snapshot revision、request cancellation 与\n当前 selection set 做 stale gate。\n\n### Git SCM\n\n`initialize.capabilities.git` 表示 server 已冻结可信 workspace root 并安装 Git backend。\n`git/status` 不接受路径参数，返回 `GitStatusResult`：标识当前 Git runtime incarnation 的\n`streamInstanceId`、在该实例内单调递增的 workspace status revision、\nHEAD 的 branch/detached/unborn\n状态、可选 upstream ahead/behind，以及每个 workspace-relative path 的 index/worktree status、\nrename original path、conflict 和 submodule flags。Revision 表示 App Server 观察到的投影版本，\n不是 durable CAS token，客户端不能把一次 snapshot 当作后续 mutation 的 compare-and-swap 前提。\n当 workspace 是更大 repository 的子目录时，server 会过滤 workspace 外 change，并将保留路径\n重新映射为 workspace-relative。\n\nApp Server 通过 `zeta-file-watcher` 接收 workspace、Git metadata 和相关 ancestor `.gitignore`\ninvalidation hint，100ms debounce 后重新读取 authoritative Git status。投影内容变化时 revision\n递增并向支持 notification 的连接发送 `git/statusChanged { status }`；内容未变时不发送。\nWatcher 初始化失败时显式 `git/status` 与 mutation 仍可用。客户端只在相同\n`streamInstanceId` 内比较 revision 并忽略不大于当前 revision 的通知或响应；实例变化表示\nApp Server 已重启，客户端必须接受新 snapshot，并在连接重新 ready 时主动执行 `git/status`\n恢复权威状态。\n\n`git/textDiff` 返回同一 workspace 范围内的 `GitStatusResult`、每个可展示文本变化的 original/\nmodified UTF-8 source，以及文件级和聚合增删行统计。单侧文件上限为 2 MiB；binary、symlink、\n非 UTF-8、不可读或超限内容仍保留在 status 中，但不进入 text diff。客户端可以用 source 构建\npresentation diff，不得直接读取 Git revision 或复制 Git 统计规则。\n\n`git/branch/list` 返回当前仓库的现有本地分支。`git/branch/switch` 只接受有界非空 branch name；\nserver 会重新列出当前仓库分支并按 exact name 解析后才执行 mutation，因此客户端提交的字符串\n不会直接成为未经确认的 Git argv。成功结果包含新的 status；脏工作树或 linked worktree 冲突由\nGit 拒绝，server 不重试或丢弃用户内容。\n\nMutation contract 提供 `git/stage`、`git/unstage`、`git/discardWorktree`、`git/commit`、\n`git/branch/switch`、`git/fetch`、`git/pull` 和 `git/push`。Path mutation 接受 1–5000 个 workspace-relative path；\nRust service 负责最终边界校验和 repository-relative 映射。Commit message 必须非空、无 NUL，\n且不超过 64 KiB UTF-8。每个成功 mutation 都返回新的 status；commit 另外返回 object ID。\n\nRemote operation 禁用 terminal/credential prompt，pull 固定使用 fast-forward only。Discard 只恢复\ntracked working tree，不删除 untracked 文件。Operation 在单 workspace runtime 内串行执行；当前\n没有可观测 queue、progress 或 caller cancellation。跨层 ownership、当前 UI 和演进顺序见\n[`git.md`](git.md)。\n\n### Workspace 搜索\n\n`initialize.capabilities.workspaceSearch` 表示 server 已安装 workspace 内容搜索 backend。\n客户端通过 `workspace/search/start` 获得 connection-owned `searchId`，用\n`workspace/search/read` 的 `afterMatch` cursor 分批读取结果，最后调用\n`workspace/search/cancel` 释放作业。每个结果包含 workspace-relative path、1-based line\nnumber、单行 preview 和 UTF-16 match ranges。\n\n查询、glob、batch 和总结果都有协议上限；Rust backend 重新校验 workspace 边界并直接启动\n冻结的 ripgrep executable。未知 ID、跨 connection 访问和并发超限使用稳定的\n`SearchNotFound`、`SearchNotOwner` 与 `SearchBusy` error name。执行失败作为 terminal\nread result 的脱敏 `error` 返回。完整 ownership 与当前 UI 限制见\n[`search.md`](search.md)。\n\n### Workspace 代码索引\n\n`initialize.capabilities.codeIndex` 表示 local composition 可以在当前 workspace authority 内建立\n本地代码索引。`workspace/codeIndex/status` 返回 `empty/indexing/ready/stale/failed` 和 published\ngeneration counters；`workspace/codeIndex/search` 接受最多 8 KiB query 与 1–100 个结果上限，\n返回 root-relative path、language、source revision、chunk key/hash、UTF-8 byte/line span、当前\n验证过的 content 与 lexical score。初始 generation 尚未发布时返回 `CodeIndexNotReady`。\n\n`workspace/symbolIndex/status` 投影 `empty/indexing/ready/stale/failed` 与 source/symbol generation；\n`workspace/symbolIndex/search` 对当前持久 projection 和 dirty overlay 做 Nucleo fuzzy query，返回 UTF-16\ndeclaration/selection ranges、source revision、score 与 matched name indices。它不声称 reference 或 type\n语义；LSP workspace symbols 由 Desktop provider aggregator 并发补充。\n\n`workspace/codeIndex/retrieve` 使用相同 query/result 数量上限；内部始终按 Workspace excerpt identity\n校验和去重，但返回面只投影 revision-bound excerpt、RRF score、\n`localSymbol/localLexical/localSemantic/cloudSemantic` origins 和显式 degradations。未授权云能力时仍可\n使用本地 symbol/FTS 与已配置 local semantic；已启用云能力时只查询 durable state 中 exact ready\nremote generation。各来源失败会保留其他 local hits 并分别返回\n`localSymbolQueryFailed`、`localSemanticQueryFailed` 或 `cloudQueryFailed`；复核失败或 content budget\n丢弃也会返回计数，不把 provider candidate body 当作 source authority。\n\n`workspace/codeIntelligence/document/synchronize` 接收 Editor-authoritative full snapshot；CodeIndex\n首先校验 path、language、revision 与 text，再建立 canonical in-memory chunks，SymbolIndex 随后投影\ndeclarations。同一 dirty path 的磁盘 symbol、FTS、vector 和 cloud candidates 全部被抑制；保存后只有\n磁盘 generation 的 content hash 对齐才 handoff。`close`、Workspace replacement 或 host lifecycle\n释放 overlay。响应只包含 generation 和 dirty document count，不泄露正文。\n\n`workspace/codeIndex/rebuild` 是 global-exclusive、同步 manual reconcile；通常由 watcher-driven\nruntime 自动维护，不应在每次查询前调用。该能力不创建 embedding/network 请求，也不等价于产品\n文字/正则搜索。完整 chunking、持久化、stale gate 与隐私边界见\n[`code-index.md`](code-index.md)。\n\n`initialize.capabilities.cloudCodeIndex` 表示 host 已注入非空 provider registry；未激活或受限\nWorkspace 即使支持该方法，也没有 active cloud controller，调用会返回\n`CloudCodeIndexUnavailable`。云端只有一种 publication contract：上传 Workspace 已切块并复核的\nexact chunks；provider 不得读取完整 source 后重新切块。客户端必须先用\n`workspace/codeIndex/cloud/preview` 展示 file/chunk/unit/byte shape，再用 authorize 固定\nprovider、tenant、collection、path scope 和 `maxEgressBytes`；该 ceiling 计算 source-content bytes，\n不包含 transport metadata overhead，authorize 本身不上传。旧 `mode` 字段按未知字段拒绝，不能把\n旧 `managed` consent 静默解释为新的 chunk-only grant。\n\n同一 root 同时只允许一个 grant。destination、scope 或 byte ceiling 变化必须先 revoke；\n每次 sync 都重检 byte ceiling 和 source revision。状态为\n`localOnly/granted/syncing/ready/stale/revoking/failed`。revoke 在 provider call 前持久化\n`revoking`，删除失败保留 pending grant供幂等重试；Workspace 信任撤销也会自动触发删除并移除\ncloud runtime。默认 local composition 没有 concrete provider，所以示例 capability 为 false，当前\n不会发起云网络请求。\n\n### 集成终端\n\n`initialize.capabilities.terminal` 表示 local composition 已提供可信 workspace root 和 PTY\nruntime。`terminal/profile/list` 只返回稳定 `profileId`、显示标题与 default 标记，不暴露\nprogram、args 或 environment。`terminal/create` 接受 rows/cols 和 `default | profileId`\ntagged selection；Rust owner 把 ID 解析到冻结的本机 Shell Profile，并以显式 environment\nallowlist 在 workspace root 启动。客户端不能提交任意 executable、environment 或绝对 cwd。\nTerminal ID 绑定创建它的 App Server connection，跨 connection 操作返回 `TerminalNotOwner`。\n\nElectron 与 Vite development host 只把 Shell 正常运行所需的用户目录、临时目录、locale、\n`PATH`、Windows system/profile 或 Unix XDG 变量传入 App Server；token、API key 和其他未列出\n变量会在 host 边界丢弃。Rust `TerminalEnvironment` 再按同一类别过滤，并覆盖\n`TERM=xterm-256color`、`COLORTERM=truecolor` 与 `TERM_PROGRAM=zeta`。`zeta-utils-pty` 在\nspawn 前执行 `env_clear`，所以 PTY 看不到最终 map 之外的 App Server 环境。Terminal request DTO\n拒绝 unknown field；通过 `terminal/create.environment` 夹带变量会返回 `InvalidParams`。\n\n当前同步 JSONL transport 不支持独立于 request 的高频主动输出。客户端通过\n`terminal/read { terminalId, afterSequence, maxChunks }` 拉取最多 128 个 raw-byte chunk；\n每个 chunk 使用标准 Base64，并以单调 sequence 排序。Server 保留最多 1 MiB 输出，cursor\n落后于 ring 时返回 `outputGap: true`，客户端必须显式显示截断而不能把缺口当作连续输出。\n`exited` 只在 authoritative process exit 且尾部输出流关闭后为 true。\n\n当前 terminal 不持久化、不跨 App Server 重启恢复，也不支持用户或 Workspace 环境变量修改、\n`.env` 自动加载或远程 attach。\n正常客户端在实例关闭后调用 `terminal/close`；connection 结束时 server 终止该 connection\n拥有的剩余 PTY。App Server 重启后的显式 Relaunch 会创建新 PTY，不能冒充原进程恢复。\n\n## 6. 会话命令\n\n### 创建（Create）\n\n```json\n{\n  \"method\": \"session/create\",\n  \"params\": {\n    \"commandId\": \"command_session_1\",\n    \"title\": \"Investigate repository\"\n  }\n}\n```\n\n返回 `{ \"session\": Session }`。首次 durable event 为 `sessionCreated`，并在同一 atomic batch\n保存 typed command receipt。\n\n### 创建 Thread\n\n```json\n{\n  \"method\": \"session/request\",\n  \"params\": {\n    \"commandId\": \"command_thread_1\",\n    \"sessionId\": \"session_1\",\n    \"expectedSequence\": 1,\n    \"request\": { \"type\": \"createThread\", \"title\": \"Main\" }\n  }\n}\n```\n\n创建采用可恢复 saga：\n\n1. Session 写入 `threadCreationPlanned`，membership 状态为 `creating`；\n2. 创建带相同 `sessionId` 的 Thread stream；\n3. Session 写入 `threadAttached`，membership 状态变为 `active`。\n\n恢复时发现 `creating` membership 会继续完成后两步，而不是创建另一个 Thread。\n\n### 分叉 Thread\n\n`session/request` 的 `request.type = forkThread` 比 create 多一个 `parentThreadId`。Server 在执行\n命令时读取父 Thread 的当前 sequence，并把它持久化进 `ThreadOrigin::Fork`。Fork 只复制 lineage\n起点；它不让两个 Thread 共享后续 sequence。\n\n### 生命周期\n\n`session/request` 的所有 mutation 都要求 `commandId`、`sessionId` 与 `expectedSequence`。\n`request.type` 明确选择 `archiveThread`、`complete`、`archive` 或 `stop`；Archived Session 不允许\n再修改。停止请求会先 durable archive Session，再中断该 Session 下所有活动 child Turn。连接断开\n只释放订阅、请求和资源 ownership，不隐式触发停止。\n\n### 会话模型\n\n`session/request::SetModel` 使用 `commandId`、`sessionId`、`expectedSequence` 和 provider-scoped\n`ModelRef`。选择结果写入 Session event stream，只影响该 Session。`session/request::StartTurn` 将 Session\n当前模型复制到新 Turn；后续 Session 或全局配置变化不会改变已经启动的 Turn。\n\n`model/list` 合并 App Server 当前已配置 direct provider 目录与 upstream Codex account-filtered\n目录。后者使用稳定 `openai-chatgpt` provider identity；选择时再次校验 exact model，实际 entitlement\n仍在 upstream 调用时最终验证。登录状态不会隐式改变 Session model。\n\n## 7. Thread 与 Turn\n\n`session/thread/read` 返回：\n\n```text\nThread {\n  sessionId,\n  threadId,\n  title,\n  status,\n  sequence,\n  turns: Turn[]\n}\n```\n\n每个 Turn 始终包含完整的 `items: ThreadItem[]`、可选 `pendingInteraction` metadata 与可选稳定\n错误。`pendingInteraction` 不含 interaction payload；完整请求只能通过 owner-directed delivery\n获得。客户端不得从日志文本或瞬态 delta 推断权威终态。\n\n`session/request` 的 `StartTurn` 参数：\n\n```json\n{\n  \"commandId\": \"command_turn_1\",\n  \"sessionId\": \"session_1\",\n  \"expectedSequence\": 1,\n  \"request\": {\n    \"type\": \"startTurn\",\n    \"threadId\": \"thread_1\",\n    \"input\": [\n      { \"type\": \"text\", \"text\": \"Describe this image\" },\n      {\n        \"type\": \"imageAttachment\",\n        \"attachment\": {\n          \"contentDigest\": \"sha256:...\",\n          \"mediaType\": \"png\",\n          \"encodedBytes\": 12345,\n          \"width\": 1024,\n          \"height\": 768\n        }\n      }\n    ]\n  }\n}\n```\n\nacceptance、user items 与 started facts 作为一个 atomic Thread batch 提交。最终 Agent item\n与 completed fact 也作为一个 atomic batch 提交。Provider 失败时持久化稳定\n`StableTurnError`；持久化失败时内存投影不得伪造终态。\n\n`input` 是保持顺序的非空 tagged union。文本项必须非空；新客户端应先调用\n`attachment/upload/start`，按服务端返回的 `maxChunkBytes` 顺序调用\n`attachment/upload/write`，然后调用 `attachment/upload/finish` 取得\n`ImageAttachmentRef`。远程 HTTP(S) 图片使用 `attachment/importRemote`，由 host 执行 public-only\nDNS/redirect/size/image 校验。上传 session 归 connection 所有，断连、取消或 idle timeout 会清理；\nApp Server 不接受本地路径。Core 持久化 `UserImageAttachment`，command receipt、Thread history 与\nsnapshot 均只保存 content digest 和验证后的媒体元数据。旧 `image` URL input 仅作为兼容入口，\n在 durable Thread append 前同样会被归一化为 attachment reference。\n\n`session/request` 的 `InterruptTurn` 同样携带 `commandId`、Session/Thread/Turn identity 与\n`expectedSequence`，成功返回新的 Thread sequence。\n\n`session/request` 的 `ResolveInteraction` 携带同样的 aggregate identity、`commandId`、`expectedSequence`，\n以及 outstanding interaction 的 `requestId` 和 typed response。它只接受该 Turn 当前 pending\ninteraction 的同一 request kind；相同 `commandId + typed payload` 会重放原结果，错误的\n`requestId` 或 response kind 会被拒绝。该 method 解决已 durable 的 interaction，不用于创建\n新的 Agent request。\n\nconnection 在 initialize 时通过\n`agentInteractions { version: 1, kinds: [...], dynamicTools?: [...] }` 声明实际支持的 interaction\nkind；承载 dynamic tool 时还必须列出 exact hosted tool name。App Server 只在声明对应 capability\n且订阅该 Session-owned Thread 的 connection 中确定性选择一个 owner，并通过 `agent/request`\n主动投递 full `AgentRequestEnvelope`。approval/user-input 在 owner 断连或退订后可以重选；已经\n投递的 dynamic tool 不会转交给另一连接，而是 durable 取消并按 unknown outcome 收口，避免不确定\n副作用被重复执行。ownership 始终是短暂 delivery state，不写入 Thread snapshot/event。非 owner\nresolve 返回 `AgentInteractionNotOwner`。\n\n可选 `InteractionDeadline` 是 durable absolute Unix millisecond instant。App Server runtime 在\nmutation gate 下重读 exact pending request，过期后持久化 `DeadlineElapsed` cancellation 并将 Turn\n失败为可重试 `InteractionDeadlineElapsed`；过期响应返回 `AgentInteractionExpired`。Core reducer\n只归约 durable fact，不运行 timer，TUI 也不拥有 deadline policy。\n\n## 8. 更新流\n\n与 Session/Thread 交互相关的 notification method 包括：\n\n- `session/update`，payload 为 `SessionUpdateEnvelope`；\n- `session/thread/update`，payload 为 Session subscription 的 `ThreadUpdateEnvelope`；\n- `agent/request`，payload 为仅发送给 selected owner 的 full `AgentRequestEnvelope`；\n- `config/changed`，payload 为已提交的 Config `revision` 与 `generation`；\n- `skills/changed`，payload 为新的 catalog `generation`；\n- `git/statusChanged`，payload 为新的 workspace Git status；\n- `fs/changed`，payload 为相对路径变化或 scoped rescan hint。\n\ndurable update 使用 `durableSequence`。Thread 的低延迟非 durable update 可额外携带\n`streamCursor { streamInstanceId, sequence }`，两者不能混为一个计数器：\n\n- durable sequence 可用于恢复、重放和 optimistic concurrency；\n- stream cursor 只用于检测当前 runtime 的瞬态 update 空洞；\n- streamInstanceId 变化时客户端丢弃旧瞬态 cursor，并以 durable snapshot/gap 重新同步。\n\n`session/request` 是产品 mutation 的 canonical aggregate port。请求固定携带 `commandId`、\n`sessionId`、`expectedSequence` 和 tagged `request` operation；其中 `expectedSequence` 对 Session\n操作指向 Session，对 child Thread/Turn 操作指向 request 选择的 child Thread。结果通过 tagged\n`SessionRequestResult` 区分 Session、child Thread 和 Turn 返回值。旧的独立 Session/Thread/Turn\nmutation 方法不在 registry 中；客户端不得重新引入平行入口。\n\nSession subscription 和显式 `session/thread/subscribe` 都使用同一个 `session/thread/update`\nnotification；通知 payload 始终带有 Session/Thread scope。\n\n`session/subscribe` 原子建立 Session subscription，并返回当前 Session snapshot、Session 的\ncommitted update gap，以及每个 child Thread 的 `SessionThreadProjection` snapshot/gap；同一\nconnection 会接收这些 child Thread 的实时 update。产品宿主应先应用 aggregate snapshot/gap，再\n接收实时 notification；发现 durable 空洞时重新执行 `session/subscribe`。\n需要单独读取一个 Thread 的客户端使用 `session/thread/read` 和\n`session/thread/subscribe`，并始终携带 `sessionId`；这保证了 Thread scope 在协议边界被验证。\n\n## 9. 配置与资源\n\n`config/update` 使用 `commandId`。Patch 字段三态语义为：\n\n- 缺失：不修改；\n- `null`：清除；\n- value：替换。\n\n`execPolicy/rule/upsert` 接收完整 typed rule：selector 支持 action digest/kind、trusted source、\ntokenized command prefix、structured network target、capability scope 和显式 `all`；effect 支持\n`continue`、`allowUnsandboxed`、`requireApproval`、`requireSandbox` 与带理由的 `deny`。\n`execPolicy/rule/remove` 按 stable rule ID 删除。两者都使用 `commandId + expectedRevision`，并走\nConfig authority 的 exact receipt/atomic TOML contract。`config/read.execPolicyRules` 返回当前 User\nrules；Workspace restrictions 不作为 User 配置回写。\n\nConfig authority 提交 consumer-visible change 后向所有 connection 发布 `config/changed`。\nnotification 是重新 `config/read` 的失效提示，不包含完整 desired document；no-op command 与\nexact replay 不推进 revision/generation，也不发布 change。外部 TOML 编辑与同一 profile 的其他\nSQLite connection 提交也会被观察并投影。\n\n`config/read` 当前返回 Agent preference、Provider、standalone MCP、Skill source、exact Plugin\nrequest、declarative Hook、language-server mode/path preference、semantic CodeIndex 配置和 Tool\nSearch 配置，以及 User `execPolicyRules`。`toolSearch.embeddingStatus` 明确区分 `disabled`、`ready` 和带脱敏原因的\n`unavailable`；不能只根据 desired `mode` 推断 embedding 已可用。Plugin request 的 `enabled` 只表示期望参与未来 activation；\nHook 的 `enabled` 也不表示 process 已获准或已经执行。两者的 runtime/lifecycle projection 必须由\n后续独立领域 API 返回，不能从 Config desired state 推断。\n\n`toolSearch/configure` 的 `hybridEmbedding` 必须携带 exact `embeddingModel`。App Server 在 durable\ncommit 前从 Provider Config 解析模型并发送固定 readiness probe；失败返回\n`ToolSearchUnavailable`，不会把混合模式写入配置。默认 `lexical` 完全本地运行。Tool Search 的模型\n选择与 semantic CodeIndex 的模型和 Workspace source-egress grant 相互独立。外部配置或启动恢复的\nhybrid 模型不可用时，`embeddingStatus` 为 `unavailable`，自然语言搜索明确失败而不回退 BM25；\n显式 Regex 仍保持本地运行。\n\nProvider DTO 的 `modelContext` 以模型 ID 映射 `contextWindow` 和可选\n`autoCompactTokenLimit`，用于 Core context budget。它是非 secret declarative metadata；零值在\n配置 mutation 时被拒绝，未知窗口不会在 App Server 内被替换成猜测值。\n\n`skills/list` 返回 source-qualified `SkillId`、description、source kind、content digest、\ncompatibility、effective enablement 和 isolated diagnostics。`reload: \"cached\"` 可复用当前\nprojection；`reload: \"refresh\"` 要求 server 重扫受控 roots。`skill/enablement/set` 必须携带\nconfig `expectedRevision` 与 exact discovered `SkillId`，结果使用标准 config command receipt。\nenablement 或 filesystem/config invalidation 导致可见 projection 变化时发布\n`skills/changed`；notification 是重新 list 的提示，不包含 catalog body，也不表示 Skill 已注入\n正在运行的 Turn。\n\n`session/request` 的 StartTurn input 可以携带 `Skill { skill: SkillRef }`。App Server 只接受当前\ncatalog 中 enabled 且 compatible 的 exact Skill，随后冻结 digest、catalog generation 与\nactivation reason；客户端 raw path 没有 wire 入口。正文不会出现在 `skills/list`，而是在执行\nsafe point 从受控 source 按 frozen digest 重载。\n\n`skill/resource/open` 接受 exact `SkillId`、`skillContentDigest` 与 package-relative `path`。服务端\n重新验证当前 enablement、compatibility、Skill digest、source containment 和文件 identity，再将有界\nbytes 写入当前 connection 的 Resource store。图片/PDF MIME 只有在扩展名与文件签名匹配时发布；\nHTML/SVG 等 active content 返回 `application/octet-stream`。后续读取和释放统一使用\n`resource/read`、`resource/metadata` 与 `resource/release`。\n\nResource bytes 使用标准 RFC 4648 Base64；`decodedLength` 是原始 byte 数，单 chunk 最大\n262,144 bytes。客户端用 `decodedLength` 推进 offset，并在结束后校验 size 与 SHA-256。\n\n## 10. 稳定错误\n\n标准 JSON-RPC errors 为 `ParseError`、`InvalidRequest`、`MethodNotFound` 和\n`InvalidParams`。产品稳定错误包括：\n\n- `NotInitialized`\n- `AlreadyInitialized`\n- `ServerOverloaded`\n- `CommandConflict`\n- `CoreOperationFailed`\n- `ResourceNotFound`\n- `ResourceNotOwner`\n- `ResourceTooLarge`\n- `InvalidResourceChunkSize`\n- `InvalidResourceOffset`\n- `TerminalUnavailable`\n- `TerminalNotFound`\n- `TerminalNotOwner`\n- `TerminalBusy`\n- `TerminalOperationFailed`\n- `GitUnavailable`\n- `GitNotRepository`\n- `GitOperationFailed`\n- `ConfigUnavailable`\n- `McpServerNotFound`\n- `McpRuntimeUnavailable`\n- `McpOAuthUnavailable`\n- `McpOAuthInvalidCallback`\n- `McpOAuthExpired`\n- `McpOAuthOperationFailed`\n- `ConnectorsUnavailable`\n- `ConnectorGenerationConflict`\n- `ConnectorOperationFailed`\n- `SkillsUnavailable`\n- `SkillNotFound`\n- `SkillOperationFailed`\n\n当前 `error.data` 为 `null`。客户端必须匹配稳定 code/name，不能解析人类错误文本。\n\n## 11. Account 与登录\n\nAccount 是 App Server 暴露给客户端的 redacted 控制面，不是 secret/token authority。当前 method：\n\n```text\naccount/read\naccount/login/start\naccount/login/cancel\naccount/logout\n\naccount/login/completed\naccount/updated\n```\n\n当前交互登录 method：\n\n```rust\npub enum AccountLoginMethod {\n    OpenAiChatGptBrowser,\n    OpenAiChatGptDeviceCode,\n}\n```\n\n上述 RPC、revisioned account projection 和 `account/login/completed` / `account/updated` 主动通知已\n实现，并通过注入的 `LoginService` 工作；未安装服务时返回稳定 `AccountUnavailable`。本地默认组合\n安装 deferred `zeta-codex-app-server` driver，第一次 `account/read` 或 login command 才启动上游\n`codex app-server`；二进制缺失、握手失败或 account method 不兼容时返回稳定账户错误。API key 继续\n属于对应模型凭据领域，不进入 account/login payload。\n\nProvider 是否支持 interactive login、credential 的实际所有者和 refresh 语义由\n[`zeta-login`](login.md) 的 exact driver 决定。对 ChatGPT/Codex subscription，该 driver 是\n[`zeta-codex-app-server`](codex-app-server.md)：它委托上游 Codex App Server 完成浏览器/设备码\n登录及 refresh。Zeta App Server 只编排和映射 redacted control plane：\n\n```text\napp-server-protocol/src/protocol/account.rs\n  └─ login/account request、redacted result、notification DTO\n\napp-server/src/server/account/\n  ├─ mod.rs       ── start/cancel/read/logout dispatch 与 notification\n  ├─ login.rs     ── LoginService composition 与 redacted RPC mapping\n  └─ account_tests.rs\n\nlogin/\n  └─ user-visible login lifecycle 与 redacted account projection\n\ncodex-app-server/\n  └─ upstream process、JSON-RPC、managed login 与 subscription runtime adapter\n\nzeta-secrets\n  └─ direct-provider/API-key 或 exact OAuth-owner 的 opaque secret bytes\n```\n\nBrowser 打开、URL 展示和 device code UI 属于 Desktop/CLI/TUI。Renderer 可以接收 `loginId`、\nauthorization URL、一次性 user code 和以下 redacted metadata：\n\n- opaque account ID；\n- email/display name（Provider 返回且 UI 需要时）；\n- workspace/organization display metadata；\n- plan/status；\n- credential revision；\n- reauthentication required 状态。\n\n禁止进入 RPC/schema：\n\n- access token、refresh token、API key；\n- authorization/cookie header map；\n- PKCE verifier、authorization code；\n- secret-store key 的内部 namespace；\n- Codex internal/unstable `chatgptAuthTokens` login variant。\n\nChatGPT/Codex login 的顺序固定为：\n\n1. App Server 调用 `LoginService::begin`；\n2. Codex adapter 向上游发出 `account/login/start`，返回 authorization URL 或 device-code UI 指令；\n3. client 打开 URL 或展示 code；\n4. 上游 Codex 绑定 callback listener、验证 callback、持久化并刷新 credential；\n5. Codex adapter 转发 redacted completed/account-updated event；\n6. `zeta-login` 发布 account revision，Zeta App Server 返回 completed 并通知客户端。\n\nZeta App Server 绝不接触 callback code、OAuth state、PKCE verifier、access/refresh token、\n`~/.codex/auth.json` 或上游 keychain entry。`account/login/cancel` 必须取消 exact upstream login；\nlogout 转发 upstream logout，并将失败映射为稳定的 redacted diagnostic。\n\n## 12. 权威来源\n\n- Rust DTO 与 registry：`zeta-rs/app-server-protocol/src/protocol/`\n- JSON Schema：`zeta-rs/app-server-protocol/schema/schema.json`\n- TypeScript：`zeta-rs/app-server-protocol/schema/types.ts`\n- Desktop 同步产物：`desktop/generated/app-server/types.ts`\n\n修改契约后执行：\n\n```bash\ncargo run --manifest-path Cargo.toml \\\n  -p zeta-app-server-protocol --bin write_schema_fixtures\nnode desktop/scripts/sync-app-server-protocol.mjs\n```\n\n生成产物、Rust contract tests 和 Desktop TypeScript 编译必须同时通过。\n\n## 13. Typst 文档编译\n\n`initialize.capabilities.typst` 表示支持 `document/typst/compile`。该方法接受\n`{ \"source\": string }`，返回由当前连接拥有的 `application/pdf` 资源和警告，或者类型化源码\n诊断。源码按 UTF-8 字节计算，最大 1 MiB。\n\n当前编译器只暴露内存中的 `/main.typ`，不暴露宿主文件、网络访问、包下载、系统字体或当前\n日期。PDF 字节沿用 `resource/metadata`、`resource/read` 和 `resource/release` 生命周期。\n跨进程所有权和计划演进见 [`typst.md`](typst.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 3,
        "id": "唯一外部门禁",
        "title": "唯一外部门禁"
      },
      {
        "depth": 2,
        "id": "1-产品模型",
        "title": "1. 产品模型"
      },
      {
        "depth": 2,
        "id": "2-一致性模型",
        "title": "2. 一致性模型"
      },
      {
        "depth": 2,
        "id": "3-传输",
        "title": "3. 传输"
      },
      {
        "depth": 2,
        "id": "4-初始化",
        "title": "4. 初始化"
      },
      {
        "depth": 3,
        "id": "客户端-hosted-浏览器能力",
        "title": "客户端-hosted 浏览器能力"
      },
      {
        "depth": 2,
        "id": "5-方法清单",
        "title": "5. 方法清单"
      },
      {
        "depth": 3,
        "id": "connector-外部账号连接",
        "title": "Connector 外部账号连接"
      },
      {
        "depth": 3,
        "id": "独立-mcp-oauth",
        "title": "独立 MCP OAuth"
      },
      {
        "depth": 3,
        "id": "文件系统",
        "title": "文件系统"
      },
      {
        "depth": 3,
        "id": "语法事实",
        "title": "语法事实"
      },
      {
        "depth": 3,
        "id": "git-scm",
        "title": "Git SCM"
      },
      {
        "depth": 3,
        "id": "workspace-搜索",
        "title": "Workspace 搜索"
      },
      {
        "depth": 3,
        "id": "workspace-代码索引",
        "title": "Workspace 代码索引"
      },
      {
        "depth": 3,
        "id": "集成终端",
        "title": "集成终端"
      },
      {
        "depth": 2,
        "id": "6-会话命令",
        "title": "6. 会话命令"
      },
      {
        "depth": 3,
        "id": "创建create",
        "title": "创建（Create）"
      },
      {
        "depth": 3,
        "id": "创建-thread",
        "title": "创建 Thread"
      },
      {
        "depth": 3,
        "id": "分叉-thread",
        "title": "分叉 Thread"
      },
      {
        "depth": 3,
        "id": "生命周期",
        "title": "生命周期"
      },
      {
        "depth": 3,
        "id": "会话模型",
        "title": "会话模型"
      },
      {
        "depth": 2,
        "id": "7-thread-与-turn",
        "title": "7. Thread 与 Turn"
      },
      {
        "depth": 2,
        "id": "8-更新流",
        "title": "8. 更新流"
      },
      {
        "depth": 2,
        "id": "9-配置与资源",
        "title": "9. 配置与资源"
      },
      {
        "depth": 2,
        "id": "10-稳定错误",
        "title": "10. 稳定错误"
      },
      {
        "depth": 2,
        "id": "11-account-与登录",
        "title": "11. Account 与登录"
      },
      {
        "depth": 2,
        "id": "12-权威来源",
        "title": "12. 权威来源"
      },
      {
        "depth": 2,
        "id": "13-typst-文档编译",
        "title": "13. Typst 文档编译"
      }
    ],
    "searchText": "``yaml title: Zeta App Server API status: development owner: zeta-rs consumers: - desktop - cli lastUpdated: 2026-08-13 ` 本文描述当前开发期的唯一 App Server 契约。项目不保留旧 wire API、旧 DTO 或旧持久化格式 的兼容入口；Rust DTO、生成的 TypeScript 和 JSON Schema 必须始终一致。 当前 zeta-mcp-server stdio/Streamable HTTP 接口面是本 API 的外层 Agent-as-tool adapter；它通过 App Server client 复用这里定义的 Session/Thread/Turn contract， 不建立第二套 execution API。该 adapter 已将 Thread subscription/update 投影为 MCP progress， 将 approval/user-input 映射为 form elicitation，并在共享 profile SQLite 中持久化外部 invocation receipt。Receipt 只拥有 MCP correlation/recovery，不改变本 API 的 durable authority。 具体 method registry、artifact generator 与 schema fixture 见 zeta-app-server-protocol README；JSON-RPC dispatch、 subscription broker、resource store 与 local composition 见 zeta-app-server README。本文拥有跨客户端 API 语义与演进方向， 两个 README 拥有当前实现接口与修改路径。 快速理解 App Server API 是 Desktop、CLI、TUI 和其他适配器访问 Zeta 产品能力的唯一版本化接口；它暴露 Session、Thread、Turn 和更新流，不建立第二套领域模型。 客户端需求 使用方式 关键保证 --- --- --- 创建一次工作 创建 Session，再创建 Thread 并启动 Turn 产品身份与持久化语义来自统一协议 关闭一个 Session Tab 前端通过 session/request 提交 request.type = stop 持久化停止 Session，并中断其所有活动子 Turn；仅断开连接不会停止 Session 持续显示执行进度 订阅 Thread 更新并按序列消费 发现缺口时重新读取快照，不猜测丢失状态 修改配置或资源 调用类型化方法并携带命令身份 重复命令可重放结果，冲突载荷会被拒绝 响应批准或用户输入 回复等待中的类型化请求 回复绑定精确请求和当前 Thread 让 Agent 操作 Desktop 浏览器 Desktop 初始化时声明 browser host Rust 保留批准和目标 owner，Electron Main 只执行语义动作 连接本地 App Server 先初始化并校验能力和模式哈希 初始化前不能调用产品方法 协议发生不兼容变化 同步修改 Rust 类型、生成物和调用方 开发期不保留隐藏的旧 DTO 入口 唯一外部门禁 这条规则适用于所有 Session、Thread、Turn、ThreadItem 产品能力：App Server 同时是 客户端请求进入 Core 的唯一入口，也是 Core 更新离开系统的唯一出口。 参与者 允许路径 禁止路径 --- --- --- Desktop、CLI、TUI 和其他客户端 版本化 App Server 协议 → 分发器 直接链接 Core、Store、Provider 或读取私有运行时接口 进程内宿主 类型化客户端 → 同一个 App Server 分发器 为性能增加隐藏的进程内业务方法 App Server 校验、路由、订阅、DTO 编解码和事件投影 → Core 复制领域归约器（reducer）、直接写 Store 或消费 Provider 内部流 Core 持有 Session/Thread/Turn/Item 权威状态并调用内部端口 依赖 App Server wire、客户端状态或 UI 生命周期 `mermaid flowchart LR client \"Desktop / CLI / TUI / other client\" -- request \"versioned App Server request\" request -- dispatcher \"App Server dispatcher\" dispatcher -- core \"Core Session/Thread/Turn runtime\" core -- provider \"Provider / tools / stores\" core -- dispatcher dispatcher -- events \"App Server result + update stream\" events -- client ` 进程内 App Server、stdio/JSONL 和未来的远程 App Server 只是不同传输方式。它们必须保持相同的 请求、结果、错误和通知语义。zeterm 当前的直接 Rust 终端/PTY 组合只覆盖 终端宿主；一旦该宿主承载 Agent 的 Session/Thread/Turn/Item 能力，也必须接入同一 App Server 门禁，不能新增 Core 旁路。 1. 产品模型 Canonical 产品实体和内部契约的详细定义见 protocol.md。本 API 直接暴露 其中的 readable Session/Thread/Turn/ThreadItem view，不维护第二份领域定义。 - App Server connection/session 只是传输生命周期，不能与产品 Session 混用。 Session 不嵌入 Thread 历史，只保存 membership、lineage 和 lifecycle。Fork 的 lineage 固定为 parentThreadId + parentSequence，因此父 Thread 后续继续执行不会改变已创建分支的起点。 2. 一致性模型 每个修改命令都使用： - commandId：客户端生成的稳定命令身份； - expectedSequence：客户端观察到的目标 aggregate sequence； - typed command payload：参与重放与冲突判断。 同一 commandId + typed payload 重试返回原结果；同一 commandId 携带不同 payload 返回 CommandConflict。JSON-RPC request id 只做当前 connection 的 response pairing，不能替代 commandId。 Session 与每个 Thread 拥有独立 durable sequence： - Session sequence 只排序 topology 与 lifecycle； - Thread sequence 只排序该分支内的 Turn 与 Item； - 修改一个 Thread 不会占用 Session 或其他 Thread 的 sequence； - expectedSequence 始终针对 method 所修改的 aggregate。 3. 传输 当前外部 transport 是 UTF-8 JSONL/stdio： - 每行一个完整 JSON-RPC 2.0 message； - 单条 message 最大 1,048,576 bytes； - stdout 只允许协议 message，stderr 只用于诊断； - 同一 request 的 response 先于由它产生的 causal notifications； - connection 断开后 request ID、subscription、Resource 与 Terminal ownership 全部失效。 In-process client 使用 protocol-owned typed request/event channel，可以省略 JSON string 编解码，但必须经过相同 initialize gate、method dispatcher、result/error envelope 与 notification contract，不能拥有隐藏业务接口。JSONL/stdio、WebSocket 等外部 transport 才在 边界执行 wire encoding。 4. 初始化 initialize 必须是 connection 的首个 request。 `json \"jsonrpc\": \"2.0\", \"id\": 1, \"method\": \"initialize\", \"params\": \"clientInfo\": \"name\": \"zeta-desktop\", \"version\": \"0.1.0\" , \"capabilities\": \"notifications\": true, \"browser\": \"version\": 1, \"observe\": true, \"input\": true ` 返回值包含 serverInfo、完整 schema 的 schemaHash、server capability，以及 composition 边界冻结的 slashCommands snapshot： `json \"capabilities\": \"sessions\": true, \"threads\": true, \"turns\": true, \"resources\": true, \"fileSystem\": true, \"workspaceSearch\": true, \"codeIndex\": true, \"cloudCodeIndex\": false, \"terminal\": true, \"mcp\": true, \"mcpOAuth\": false, \"typst\": true, \"updateReplay\": true , \"slashCommands\": \"name\": \"diagnose\", \"description\": \"inspect the current workspace\", \"argumentMode\": \"optional\" ` schema hash 不一致时客户端必须拒绝继续运行。 slashCommands 每项的 name 只能使用 lowercase ASCII letters、digits 与 interior hyphens， description 不能为空，同一 snapshot 中 name 必须唯一。该 snapshot 负责 discoverability 与 inline argument parsing；提交仍通过 session/request 的 StartTurn.input，并保留 /name、text/image 顺序。 校验、local/server 合并与 Rust client 交互状态的 canonical owner 是 zeta-slash-commands；App Server 只组合并发布 server snapshot。 三种 client surface 的合并、执行与渲染边界见 slash-commands.md。 客户端-hosted 浏览器能力 Browser 是当前 Server → Client request capability。只有在 initialize 中提交 version 1 browser capability 的连接才能成为宿主；observe 与 input 分别声明页面观察和语义输入支持。该声明是 短暂的 connection routing authority，不进入 Session、Thread 或持久化配置。 Host method Params Result 当前语义 --- --- --- --- browser/create url targetId 创建一个隔离、默认隐藏的新目标 browser/observe exact target + 三个 include flag 页面状态 + 可选 AX/DOM/PNG 不执行脚本，不返回 Electron 对象 browser/perform tagged semantic action targetId 导航、node click/type、滚动、后退或刷新 browser/close targetId null 关闭精确目标 请求 ID 使用保留的非空字符串，与 Client → Server 正整数 ID 隔离。App Server 把 create result 的 targetId 绑定到响应它的连接；此后不能由另一连接响应，也不能把观察或动作结果切换到另一个 目标。connection 退出后 target authority 立即失效，Desktop 同时回收由该 host connection 创建的 原生目标。 `json \"jsonrpc\": \"2.0\", \"id\": \"browser-host:1:7\", \"method\": \"browser/perform\", \"params\": \"action\": \"type\": \"click\", \"targetId\": \"browsertarget123\", \"target\": \"nodeId\": \"42\" ` 成功响应必须保留同一 ID 和 target： `json \"jsonrpc\": \"2.0\", \"id\": \"browser-host:1:7\", \"result\": \"targetId\": \"browsertarget123\" ` App Server deadline 为 30 秒。Turn 取消或 deadline 到达时，Server 发送 $/cancelRequest id ；client 终止尚未开始的步骤，并用 -32800 返回已取消的在途请求。 已放弃请求的晚到 terminal response 可以丢弃，已完成请求的重复 response 仍是协议错误。 截图 payload 只允许 image/png，App Server 校验 decoded length、Base64 和 PNG signature 后才 创建 connection-owned Resource。 该契约从不接受任意 CDP method、JavaScript source、localhost 调试端口或 Node sidecar 配置。 完整 Browser Tool generation 同时要求可信工作区和至少一个同时声明 observe + input 的 live version 1 宿主；Restricted、信任撤销或最后一个完整宿主断开都会移除该工具 port。恢复条件后 只影响后续 Tool generation，不恢复或重放旧调用。 Desktop 当前实现和 Playwright 后续边界见 zeta-desktop-architecture.md 7-浏览器能力。 5. 方法清单 Method Aggregate Effect --- --- --- session/create new Session 创建任务 session/read Session 读取 canonical snapshot session/list global 列出 Session session/subscribe connection Session snapshot + afterSequence 之后的 durable gap + child Thread projections session/request Session canonical typed mutation request；覆盖 Session、child Thread 与 Turn 操作 session/unsubscribe connection 删除订阅 session/request Session 通过 tagged request 完成 Session、child Thread 和 Turn mutation model/list global model catalog 列出当前已配置 direct provider 与已登录 ChatGPT account 可选择的 Codex 模型 session/thread/read Session + Thread 读取 canonical Thread snapshot session/thread/subscribe Session + Thread + connection snapshot + afterSequence 之后的 durable gap session/thread/unsubscribe Session + Thread + connection 删除 child Thread 订阅 config/read config 读取配置 connector/list Connector authority 读取不含 secret/reference 的外部账号连接投影 connector/connect/apiToken Connector authority + secret store retry-safe 保存 API token 并发布 connected account connector/connect/oauth/start / complete / cancel Connector OAuth owner 启动 exact PKCE flow，一次性消费 callback state/code，或显式结束 abandoned flow connector/disconnect Connector authority + secret store 先撤销 runtime readiness，再报告 credential cleanup 状态 connector/credential/cleanup Connector credential owner 重试 durable post-disconnect secret 删除义务 plugin/list Plugin authority 分别投影 installed/enabled/granted/effective package 状态 plugin/marketplace/list / plugin/install / plugin/update / plugin/rollback Plugin Marketplace + Plugin authority 只接受 Host 注册 Marketplace 中由 marketplaceId、package identity 和 digest 精确绑定的条目；安装后不自动启用或 grant plugin/enable / disable / grant / revokeGrant / uninstall Plugin authority exact-package CAS lifecycle mutation language/marketplace/list Language Marketplace + activation authority 读取 signed package catalog、host compatibility 与当前 profile activation 投影 language/marketplace/install Language Marketplace + activation authority + provider registry revision-check 后重新匹配并下载 exact signed package，激活 exact receipt，并热替换 provider registry；调用前的用户确认由 Desktop 拥有 config/update config typed command 更新配置 execPolicy/rule/upsert / execPolicy/rule/remove config + local policy runtime revision-safe 持久化 User typed rule，并为未来 Tool safe point 重组 policy snapshot toolSearch/configure config + semantic model runtime 选择词法模式，或探活 exact embedding 模型后启用混合 Tool Search workspace/codeIndex/semantic/configure / authorize / revoke config + Workspace 独立配置 semantic CodeIndex，并显式管理源码外发授权 workspace/codeIndex/semantic/cancel / retry semantic index job 取消或重新调度 exact-generation 本地语义 projection；status 返回无内容进度计数 languageServer/configure / languageServer/remove config revision-safe 修改或恢复 language-server mode/path preference provider/configure / provider/remove config 修改 Provider declaration mcp/server/upsert / mcp/server/remove / mcp/server/enablement/set config 修改 standalone MCP desired config mcp/server/connect / mcp/server/disconnect runtime 设置 process-local lifecycle intent，不改变 Config revision mcp/server/status read 读取 active Config/Plugin/Connector MCP runtime 的 redacted lifecycle 与 generation projection mcp/oauth/start / mcp/oauth/complete MCP OAuth owner 为 exact standalone Config server 启动和一次性完成 PKCE flow mcp/oauth/refresh / mcp/oauth/revoke MCP OAuth owner + secret store 轮换 runtime/lifecycle credential；或先断开 runtime、远端 revoke 后删除本地 secret skill/source/add / skill/source/remove / skill/source/enablement/set config 修改 User Skill source plugin/request/upsert / plugin/request/remove / plugin/request/enablement/set config 修改 exact Plugin request；不安装或激活 hook/upsert / hook/remove / hook/enablement/set config + Hook runtime 修改 declarative Hook；trusted Workspace 的未来 safe point 按 immutable snapshot 执行匹配的 sandbox process skills/list global Skill catalog 读取 cached projection 或请求完整 refresh skill/enablement/set config + Skill catalog revision-checked 启用/禁用 exact SkillId skill/resource/open Skill runtime + Resource 将 digest-pinned package resource materialize 为 connection-owned resource resource/metadata Resource 读取元数据 resource/read Resource 分块读取 resource/release Resource 释放 connection-owned resource fs/getMetadata workspace 读取根相对路径的 metadata fs/readDirectory workspace 枚举根相对目录的直接子项 fs/readFile workspace 读取不超过 10 MiB 的 UTF-8 文件 fs/writeFile workspace 原子替换或新建不超过 10 MiB 的 UTF-8 文件 syntax/analyze stateless syntax 返回同一 revision 的 bounded token/fold/symbol/diagnostic facts syntax/selectionRanges stateless syntax 只沿当前 UTF-16 selections 返回 bounded parser ancestor scopes git/status workspace 读取 HEAD、upstream 和 index/worktree change snapshot git/textDiff workspace 读取 status 及有界 UTF-8 HEAD/worktree text diff projection git/branch/list workspace 列出现有本地分支及 current/upstream 信息 git/branch/switch workspace 切换到 host 重新解析确认存在的本地分支 git/stage workspace stage 一组 workspace-relative path git/unstage workspace 从 index 移除一组 path 的 staged change git/discardWorktree workspace 恢复 tracked working-tree change，不删除 untracked 文件 git/commit workspace 使用有界非空 message 创建 commit git/fetch workspace non-interactive fetch all remotes 并 prune git/pull workspace non-interactive fast-forward-only pull git/push workspace 按当前 Git upstream/default 配置 push workspace/search/start connection + workspace 启动有界内容搜索 workspace/search/read connection + search job 按游标读取最多 200 条结果 workspace/search/cancel connection + search job 取消并释放搜索 workspace/codeIndex/status workspace 读取本地 index lifecycle 与 generation counters workspace/codeIndex/search workspace 返回有界、revision-bound 的本地 lexical chu"
  },
  {
    "slug": "app-server-client",
    "sourcePath": "docs/app-server-client.md",
    "title": "App Server 客户端架构与演进方案",
    "description": "App Server 客户端把“启动后端、初始化连接、配对请求、转发事件和正确关闭”封装成一个可交付的 就绪会话，使 CLI、TUI 和无交互宿主不必各自实现一遍。",
    "group": "API 与协议",
    "markdown": "> 物理位置：`zeta-rs/app-server-client/`  \n> 主要消费者：`zeta-exec` 非交互执行宿主、`zeta-tui`  \n> Wire contract：[`zeta-app-server-api.md`](zeta-app-server-api.md)  \n> Canonical 产品模型：[`protocol.md`](protocol.md)  \n> Headless 与远程调度：[`exec.md`](exec.md)\n> MCP Agent server consumer：[`mcp-server.md`](mcp-server.md)\n> 当前 crate contract：[`zeta-rs/app-server-client/README.md`](../zeta-rs/app-server-client/README.md)\n\n## 快速理解\n\nApp Server 客户端把“启动后端、初始化连接、配对请求、转发事件和正确关闭”封装成一个可交付的\n就绪会话，使 CLI、TUI 和无交互宿主不必各自实现一遍。\n\n| 调用方动作 | 客户端保证 | 调用方仍负责 |\n| --- | --- | --- |\n| 启动本地 App Server | 只有进程和双向通道都建立后才开始初始化 | 提供启动配置和产品参数 |\n| 取得 ready client | 已完成能力、版本和模式校验 | 决定接下来调用哪个产品方法 |\n| 发出多个并发请求 | 按请求 ID 配对结果并结束等待者 | 处理领域结果和用户交互 |\n| 接收通知 | 独立转发服务端事件，不阻塞请求结果 | 更新自己的呈现状态 |\n| 关闭宿主 | 拒绝新请求、结束等待者并等待后台任务退出 | 决定产品级退出或重连策略 |\n| 将来连接远程 App Server | 复用同一公共 facade 和产品契约 | 远程调度和工作进程协议不属于本客户端 |\n\n## 1. 结论\n\n`zeta-app-server-client` 存在的原因，不只是复用一组 typed RPC method。`zeta-exec` 与\n`zeta-tui` 都需要完成同一套本地 App Server 宿主流程：\n\n1. 根据启动配置创建并启动 App Server；\n2. 建立 client 到 App Server 的请求通道；\n3. 建立 App Server 到 client 的结果与事件通道；\n4. 通过请求通道完成 `initialize` 与 schema/capability 校验；\n5. 只在初始化成功后向调用方交付 ready client；\n6. 运行期间正确配对请求结果，并持续转发 server notification；\n7. 退出时关闭 connection、结束 pending 请求并等待后台任务停止。\n\n这套启动、连接和关闭逻辑不能在 `zeta-exec` 与 `zeta-tui` 中各写一份，因此抽成共享 crate。\n\n```text\nzeta-exec ─┐\n           ├─► zeta-app-server-client ─► start App Server\nzeta-tui ──┘             │\n                         ├─ request channel ──► App Server\n                         └─ result/event  ◄──── App Server\n```\n\n直接依赖和启动 `zeta-app-server` 是 embedded backend 的职责，不是依赖方向错误。长期可以在\n同一 public facade 下增加 remote App Server backend，但它只连接相同的 App Server contract，\n不是 scheduler protocol，也不是 remote process executor。Desktop 的 JSONL/stdio client\n仍不要求复用这个 Rust crate。\n\n`zeta-exec` 是无交互界面的 Agent 执行宿主。当前它与 TUI 一样启动 embedded App Server；\n后续远程调度系统以它作为 headless execution entry。Job/Attempt/lease/event cursor 属于\n[`exec.md`](exec.md) 定义的 scheduler adapter，不进入 App Server Client。\n\n当前 `zeta-cli` 的交互式和无界面提示词路径已经使用自有\n`AppServerSession`、可克隆请求句柄、独立 `AppServerEvents` 与显式关闭。\n`zeta-mcp-server` 的 per-session adapter 当前使用同步 client 和 bounded polling/drain；这是\nMCP 外层生命周期的实现选择，不改变 App Server 的 canonical request contract。\n\n## 2. 抽象单位：一个运行中的 App Server Session\n\n共享层的顶层抽象应是一个有明确所有权的运行会话，而不是裸 transport：\n\n```rust\npub struct AppServerSession {\n    client: AppServerRequestHandle,\n    events: Option<AppServerEvents>,\n    // private shutdown state and joined driver tasks\n}\n```\n\n`AppServerSession` 拥有：\n\n- embedded App Server runner，或一个 remote App Server connection；\n- 唯一 App Server connection；\n- request channel 的 server 端；\n- server message/event channel 的 client 端；\n- connection driver 与 server runner 的后台任务；\n- initialize/ready/closing/closed 生命周期；\n- 显式 shutdown 与 task join。\n\n它向宿主提供两个运行时端点：\n\n```rust\nimpl AppServerSession {\n    pub fn client(&self) -> AppServerRequestHandle;\n    pub fn take_events(&mut self) -> Result<AppServerEvents, TakeEventsError>;\n    pub fn shutdown(self) -> Result<(), ShutdownError>;\n}\n```\n\n- `AppServerRequestHandle` 是共享 request ID allocator 的可克隆 typed request handle，供\n  feature/task 发送 request；\n- `AppServerEvents` 是单消费者事件流，供 app event loop 持续接收 notification 与 connection\n  lifecycle event；\n- `AppServerSession` 保持所有权直到显式 shutdown，避免最后一个 client clone 的偶然 drop\n  决定 server 生命周期。\n\n结果与事件必须区分：\n\n- request result 通过该 request 自己的 completion/oneshot 返回；\n- server notification 通过独立 `AppServerEvents` 流返回；\n- connection closed、protocol failure 等连接事件也通过 `AppServerEvents` 告知宿主；\n- notification 不得依附某次 request，不能通过 `drain_notifications()` 拉取。\n\n## 3. 为什么由这个 crate 统一连接 App Server\n\n`zeta-exec` 和 `zeta-tui` 都是本地 App Server 的宿主，而不是已经存在的外部 server 的普通\n调用方。两者需要相同的 composition：\n\n- 用户 profile root 与本地 SQLite state repository；\n- config、credentials、model provider 和 tool runtime；\n- App Server dispatcher；\n- connection-scoped subscription 与 Resource ownership；\n- client/server channel；\n- initialize 参数、schema hash 与 capabilities；\n- shutdown 与错误回收。\n\n如果这些步骤留在各自 consumer，会形成两套不同的启动语义：一个可能忘记 schema gate，另一个\n可能没有事件 pump，第三个关闭时遗留后台任务。共享 crate 必须保证它们拿到的是同一种 ready\nconnection。\n\n依赖方向应是：\n\n```text\nzeta-exec / zeta-tui\n          │\n          ▼\nzeta-app-server-client\n   ├─► zeta-app-server\n   ├─► zeta-app-server-protocol\n   └─► channel/task runtime\n```\n\nConsumer 不直接创建 `AppServer`、`ConnectionState`、dispatcher 或 notification broker。\n这些类型可以在 client crate 内部使用，但不能泄漏到 `zeta-exec` 或 TUI 的业务代码。\n\n当前 `zeta-exec` 已是非交互 Agent 宿主，并通过本 crate 启动 embedded App Server；底层 process\nexecution 已迁移到独立 `zeta-tool-executor`。后续 remote scheduler 仍位于 `zeta-exec` 上层，不能让\n“执行一个 tool process”和“宿主化完整 App Server”重新共享同一个模块或协议。\n\n长期 backend 选择必须是显式 enum：\n\n```rust\npub enum AppServerTarget {\n    Embedded(EmbeddedAppServerOptions),\n    Remote(RemoteAppServerOptions),\n}\n```\n\n- `Embedded` 由本 crate 组合并启动本地 App Server；\n- `Remote` 连接 daemon/remote App Server，完成相同 initialize 与 event wiring；\n- 两者暴露相同 typed request handle、event stream 与 shutdown contract；\n- backend 差异不能泄漏成 Core 私有方法；\n- remote scheduler 仍位于 `zeta-exec` 上层，不等于 `RemoteAppServerOptions`。\n\n## 4. 启动流程\n\n建议暴露两个自解释入口，并可由 target enum 统一选择：\n\n```rust\nlet mut session = AppServerSession::start_embedded(options)?;\nlet client = session.client();\nlet events = session.take_events()?;\n```\n\n`connect_remote` 仍是 Proposed backend；当前 Current backend 是 embedded。\n\nEmbedded start 必须按以下顺序执行：\n\n```text\nvalidate options\n  → build App Server composition\n  → create one server connection\n  → create bounded request/result-event channels\n  → start server runner and connection driver\n  → send initialize through the normal request path\n  → validate schema hash and required capabilities\n  → return Ready AppServerSession\n```\n\n必须保持的 invariant：\n\n- `initialize` 是 connection 的首个 request；\n- initialize 也经过正式 request channel、dispatcher 和 result pairing，不能直接调用内部\n  server method；\n- schema hash 不一致或缺少 required capability 时，`start` 失败；\n- `start` 失败必须关闭已创建的 connection/channel 并 join 已启动的 task；\n- 调用方永远拿不到半初始化的 `AppServerClient`；\n- start options 完整描述 composition，不依赖 consumer 在启动前偷偷修改全局状态。\n\nRemote connect 必须：\n\n```text\nvalidate endpoint/auth\n  → connect bounded transport\n  → send initialize as first request\n  → validate schema/capabilities\n  → start request/event driver\n  → return Ready AppServerSession\n```\n\nRemote connection failure 必须结束 pending request 并发出 connection event；自动 reconnect 只有\n在能重新建立 subscription、Resource ownership 和 transient cursor 时才能增加，不能隐藏状态\n丢失。\n\n启动配置应使用自解释类型，而不是 bool：\n\n```rust\npub struct AppServerStartOptions {\n    pub profile_root: PathBuf,\n    pub client_info: ClientInfo,\n    pub required_capabilities: RequiredCapabilities,\n}\n```\n\n若 `zeta-exec` 与 TUI 的 capability 不同，可以分别构造 typed\n`RequiredCapabilities`，不能用 `start(..., true, false)` 表达。\n\n## 5. 请求通道与结果配对\n\n`AppServerClient` 的 typed method 将请求送入后台 connection driver：\n\n```text\ntyped params\n  → allocate connection-local request ID\n  → build typed ClientRequest\n  → request channel\n  → App Server dispatcher\n  → response channel\n  → match pending request ID\n  → typed result / typed server error\n```\n\nEmbedded hot path 推荐使用 protocol-owned typed request enum：\n\n```rust\nenum ClientRequest {\n    Initialize(InitializeParams),\n    SessionCreate(SessionCreateParams),\n    SessionRequest(SessionRequestParams),\n    SessionThreadSubscribe(SessionThreadSubscribeParams),\n    // generated/registered remaining methods\n}\n```\n\nClient worker 在 request 外附加 connection-local request ID 与 oneshot completion。In-process\nchannel 不需要先序列化为 JSON string，但仍必须经过相同 initialize gate、method dispatcher、\nresult/error envelope 和 notification contract。JSON/JSONL/WebSocket backend 在 transport\n边界执行 wire encoding。\n\n公共接口仍然是类型化方法：\n\n```rust\nlet result = client.request_session(SessionRequestParams { /* ... */ })?;\n```\n\n调用方不拼 method string、不处理 JSON-RPC request ID，也不直接操作 request channel。\n当前 typed method 同步等待自己的 completion，但 request 在独立 driver 上执行；需要在 UI\n主循环中避免等待异常缓慢 request 时，由 consumer 把 completion 重新投递成 app event。\n\n请求 driver 负责：\n\n- 分配只在当前 connection 内有效的 request ID；\n- 保存 `requestId → completion` pending table；\n- 校验 response ID、envelope 与 typed result；\n- 允许多个调用方持有 `AppServerClient` clone；\n- connection 关闭时一次性结束全部 pending request；\n- 将 server stable error 保留为 typed `AppServerError`，包括 code、name 和 data；\n- 对 unknown、duplicate、retired request ID 做明确 protocol error 处理。\n\nJSON-RPC request ID 不能代替产品 `CommandId`。修改操作的 `CommandId`、\n`expectedSequence` 和 exact typed payload 由调用方创建并传入，client 只负责无损发送。超时或\nresponse 丢失后的业务重试策略不属于 connection driver。\n\n## 6. 事件通道\n\nApp Server notification 与 request result 是两条独立交付路径：\n\n```text\n                         ┌─ matching response ─► request completion\nApp Server outbound ─────┤\n                         └─ notification ──────► AppServerEvents\n```\n\n当前公共事件契约是：\n\n```rust\npub enum AppServerEvent {\n    Notification(ServerNotification),\n    ConnectionClosed(ConnectionCloseReason),\n}\n```\n\n当前 App Server API 包含 `session/update`、`session/thread/update`、owner-directed\n`agent/request`、`connector/changed`、`skills/changed`、Git 与 filesystem notification，均由 `ServerNotification`\ntyped enum 表达。approval/user-input 通过 `agent/request` + canonical\n`SessionRequest::ResolveInteraction` 完成，不在 client crate 建第二套 server-request envelope。\n显式 `Lagged`/`Desynced` lifecycle event 尚未提供；当前 transient overflow 通过 cursor gap 触发\nconsumer snapshot resync，control-only overflow 关闭 connection。\n\n`ServerNotification`、method registry 与 payload decoder 由 protocol crate 的同一个\n`server_notifications!` 定义生成。该总枚举跨 crate 标记为非穷尽：client boundary 必须严格解码\n每一个已注册 method，而 consumer 只能在自己的 projection 中选择所拥有的 capability，并为其余\n通知保留 fallback。新增 Document 或其他领域通知因此不会要求无关产品同步增加 ignore\narm。\n\n事件 driver 负责：\n\n- 在没有新 request 时仍持续接收 notification；\n- 解码 `session/update` 与 `session/thread/update`；\n- 保留 durable sequence 与 transient stream cursor；\n- 保证同一 request 的 response 先于它产生的 causal notification 交付；\n- 已知 notification 在 remote decode 或 typed validation 失败时关闭 connection 并报告\n  protocol failure；\n- server request 无 consumer/handler 时及时返回 stable rejection，不能让 approval 永久等待；\n- event receiver 被 consumer drop 时执行明确 policy，而不是无限积压；\n- bounded channel 满时不静默丢弃 durable update。\n\nEvent channel 不负责：\n\n- 把 update 应用到 Session/Thread projection；\n- 判断 durable sequence gap 后如何 resubscribe；\n- 推导 Turn 或 Item 的权威终态；\n- 将 notification 变成日志文本；\n- 合并不同 aggregate 的 sequence。\n\n这些属于 `zeta-exec` 的输出/终态协调或 TUI projection。\n\nConsumer projection 的依赖方向是：\n\n```text\nprotocol registry（method + payload + decoder）\n                    │\n                    ▼\nAppServerEvents::Notification(ServerNotification)\n                    │\n                    ├─► TUI notification projection ─► ClientEvent\n                    ├─► zeterm agent/session projection ─► AgentSessionEvent\n                    └─► headless completion projection\n```\n\n产品 projection 不得穷尽列出未拥有的 notification。TUI 当前拥有 Connector Pane，因此由\nConnector capability 消费 `ConnectorsChanged` 并触发 canonical list refresh；其他产品若不拥有\nConnector UI，则无需增加对应分支。Connector 状态不能塞进通用 connection lifecycle event。\n\n## 7. Driver 与 App Server 的连接方式\n\n共享层需要同时驱动请求与事件，不能保留“调用一次、顺便 drain 一次”的模型：\n\n```text\nAppServerClient clones\n        │\n        ▼\nbounded request channel\n        │\n        ▼\nconnection driver\n  ├─ dispatch request ─────────► App Server\n  ├─ pair response ────────────► request completion\n  ├─ receive notification ─────► AppServerEvents\n  └─ observe shutdown ─────────► close + join\n```\n\n进程内连接仍应经过同一个 App Server protocol dispatcher。Typed channel 可以避免 JSON 编解码、\nstdio 与子进程开销，但不能直接调用 Core、Store 或私有 App Server operation。它复用相同\nrequest/result/error/notification 语义，而不是复制第二份 in-process response contract。\n\nApp Server 当前通过 connection-scoped `ConnectionNotifications` 提供条件变量唤醒的 outbound\nsource；client event pump 与 request driver 独立，因此空闲连接和长 Turn 的 update 都无需\n新 request 即可到达。runner 同时处理：\n\n- 新 client request；\n- 新 server notification；\n- shutdown signal。\n\n这属于 client session 与 App Server runner 共同遵守的 channel contract。\n\n## 8. 正确关闭\n\n显式 `AppServerSession::shutdown()` 是正常退出路径。它必须：\n\n1. 原子地把 session 状态从 `Ready` 改为 `Closing`；\n2. 拒绝新 request；\n3. 结束或明确失败所有尚未完成的 request completion；\n4. 关闭 App Server connection，使 subscription 与 Resource ownership 失效；\n5. 停止 server runner 和 connection driver；\n6. 关闭 event stream，并给 consumer 一个最终 `ConnectionClosed` 原因；\n7. join 所有由 session 启动的后台 task；\n8. 返回 shutdown 期间发生的错误。\n\n关闭 connection 不等于 `session/request` 的 `InterruptTurn`。若产品要求退出前中断某个 Turn，\n`zeta-exec` 或 TUI 必须先发送 typed `session/request` 并等待所需终态，再调用 session shutdown。Client session\n不能猜测要中断哪个 Thread/Turn。\n\n`Drop` 只能做 best-effort cancellation，不能替代显式 shutdown，因为 `Drop` 无法可靠等待\n后台任务结束。测试和正常 consumer 路径必须调用并等待 `shutdown()`。\n\n需要定义以下边界情况：\n\n- event receiver 先于 session 被 drop；\n- 所有 client handle 被 drop，但 session owner 仍存在；\n- session shutdown 时仍有 client clone；\n- initialize 期间 consumer 取消启动；\n- App Server runner panic；\n- request completion receiver 被调用方取消；\n- shutdown 被重复触发。\n\n## 9. 消费方使用方式\n\n### 9.1 `zeta-exec`\n\n非交互宿主：\n\n1. 启动 `AppServerSession`；\n2. 从 client handle 发送 Session/Thread/Turn typed request；\n3. 同时消费 request result 与 `AppServerEvents`；\n4. 根据 canonical Turn terminal update 产生 human/JSON/JSONL 输出和退出码；\n5. Ctrl-C 时按产品策略发送 `session/request` 的 `InterruptTurn`；\n6. 最后显式 shutdown session。\n\n它不自行创建 App Server、初始化 connection 或实现 notification pump。\n\n后续 scheduler-facing protocol、worker registration、lease、heartbeat、Job/Attempt mapping 和\nevent ack 属于 [`exec.md`](exec.md)。App Server Client 只提供 embedded/remote App Server\nconnection，不理解 scheduler Job。\n\n### 9.2 `zeta-tui`\n\nTUI：\n\n1. 启动同一种 `AppServerSession`；\n2. 把 client handle 交给 command dispatcher；\n3. 把 `AppServerEvents` 接入 terminal/app event loop；\n4. request 执行期间继续处理键盘、重绘和 server update；\n5. 退出前恢复 terminal，并显式 shutdown session。\n\nTUI 不再接收一个同步 `&mut AppServerClient<T>`，也不调用 `drain_notifications()`。\n\n## 10. 当前实现审计\n\n当前已经落地：\n\n- `AppServerSession::start_embedded` 只在 initialize 与 schema gate 成功后返回 ready session；\n- `AppServerRequestHandle` 可克隆，clone 共享 connection-local atomic request ID allocator；\n- bounded request channel、per-request completion 与独立 connection driver；\n- `ConnectionNotifications` 在 server publish 时主动唤醒独立 event pump；\n- response completion 在同一 request 产生的 causal notification 交付前发送；\n- `AppServerEvents` 是单消费者 typed notification/connection lifecycle stream；\n- `AppServerEvents` channel 限 1024 项，背压到 App Server 4096 项 connection queue；transient\n  backlog 可清除，durable/control 不静默丢弃；\n- `shutdown` 拒绝后续请求、关闭 connection、唤醒 event pump 并 join 两个 background task；\n- CLI interactive/headless 路径使用同一个 `AppServerSession::start_embedded` composition；\n- `start_in_process_client` 已经体现“由共享 crate 创建本地 App Server”的正确方向；\n- `open_in_process_app_server` 返回可克隆的 `InProcessAppServer` host；\n- `InProcessAppServer::connect` 为同一个 `Arc<AppServer>` 建立各自 initialize 完成的 typed\n  connection，当前供 MCP HTTP session 共享一个 embedded composition；\n- `InProcessTransport::from_shared_server` 明确表达共享 host，不要求每个 transport 重建\n  SQLite repository/config/model composition；\n- typed client methods；\n- protocol method registry；\n- external JSON-RPC request/response 编解码；\n- response ID 校验；\n- schema hash 校验；\n- successful `InitializeResult` 保存在 `AppServerClient::initialization` snapshot 中，consumer\n  可读取 server capabilities 与动态 slash catalog，而无需重复 handshake；\n- typed `list_skills` / `set_skill_enablement` method；\n- protocol registry 同源生成 known notification typed decode，包括 `agent/request`、\n  `connector/changed`、`skills/changed` 与 Git status；\n- TUI notification adapter 只投影自己拥有的 Agent、Connector、Skill、Git 与 Thread capability，\n  未拥有领域不进入 `ClientEvent`。\n\n同步适配面与剩余工作：\n\n| 边界 | 状态 |\n| --- | --- |\n| `start_in_process_client` / generic `AppServerClient<T>` | MCP、rust-app 与 contract tests 的同步适配面；TUI/CLI 不再依赖 drain |\n| typed method 同步等待 completion | shared handle 保持同步 typed API；TUI 已用 `RequestTask` 把等待移出单写者 loop |\n| bounded event/data plane | Current：1024 event + 4096 server queue；显式 `Lagged` event 尚未提供 |\n| remote backend | 尚未实现；不能声称 reconnect/subscription restoration |\n| initialize gate 只存在于一个 helper | 裸 `AppServerClient::new` 可以在未初始化时发送业务请求 |\n| server error 被压成 code/string | 丢失 typed error name/data |\n\n因此 owned embedded session 的 request/event/shutdown 与有界交付主路径已经完成；下一阶段集中\n在 typed error、显式 lag lifecycle、remote backend/reconnect，不再回退到 notification drain。\n\n## 11. 目标模块\n\n建议按所有权拆分，而不是按 RPC domain 复制 protocol：\n\n```text\napp-server-client/src/\n├── lib.rs\n├── session.rs\n├── start.rs\n├── client.rs\n├── events.rs\n├── driver.rs\n├── embedded.rs\n├── remote.rs\n├── pending.rs\n├── shutdown.rs\n├── error.rs\n├── session_tests.rs\n└── driver_tests.rs\n```\n\n- `session.rs`：顶层 owner 与 ready/closing/closed lifecycle；\n- `start.rs`：App Server composition、channel 创建和 initialize；\n- `client.rs`：cloneable typed request handle；\n- `events.rs`：单消费者 event stream；\n- `driver.rs`：request/result/notification/shutdown multiplexing；\n- `embedded.rs`：typed in-process channel 与 local App Server runner；\n- `remote.rs`：相同 contract 的 remote App Server transport；\n- `pending.rs`：request ID 与 completion pairing；\n- `shutdown.rs`：关闭顺序与 task join；\n- `error.rs`：startup/client/protocol/server/shutdown error。\n\n不创建第二套 Session/Thread/Turn DTO，也不按 method 建大量重复 wrapper 模块。Wire Params、\nResult、Notification 与 error 的 source of truth 仍是 `zeta-app-server-protocol`。\n新增 test module 使用显式 `#[path = \"..._tests.rs\"]` 引入相邻测试文件。\n\n## 12. 验证要求\n\n共享层的测试重点是完整 session 生命周期：\n\n- `start` 创建 App Server 并只在 initialize/schema gate 成功后返回；\n- initialize 是 dispatcher 收到的首个 request；\n- startup 任一步失败都会关闭 channel 并 join 已启动 task；\n- `zeta-exec` 与 TUI 使用相同 start path；\n- embedded 与 remote backend 通过相同 request/event contract suite；\n- 多个 client clone 可以并发发送 request，result 按 request ID 正确配对；\n- request 执行期间 event stream 可以持续收到 notification；\n- 空闲时由其他 runtime activity 产生的 notification 无需 polling request 即可到达；\n- response 先于其 causal notification 交付；\n- event channel 满不会静默丢失 durable update；\n- connection failure 会结束全部 pending request；\n- shutdown 后新 request 明确失败；\n- shutdown 会关闭 subscription/Resource ownership 并 join 全部 task；\n- event receiver、client handle 和 session owner 以不同顺序 drop 时都不会泄漏；\n- in-process typed channel 仍经过 protocol dispatcher，不存在 Core 私有旁路；\n- remote backend reconnect 不会偷偷复用旧 subscription、Resource ownership 或 transient\n  cursor。\n\n验证入口：\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-app-server-client\n```\n\n修改 wire contract 时，还必须按\n[`zeta-app-server-api.md`](zeta-app-server-api.md#11-source-of-truth) 重新生成 schema 与\nTypeScript，并验证所有 consumer。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-抽象单位一个运行中的-app-server-session",
        "title": "2. 抽象单位：一个运行中的 App Server Session"
      },
      {
        "depth": 2,
        "id": "3-为什么由这个-crate-统一连接-app-server",
        "title": "3. 为什么由这个 crate 统一连接 App Server"
      },
      {
        "depth": 2,
        "id": "4-启动流程",
        "title": "4. 启动流程"
      },
      {
        "depth": 2,
        "id": "5-请求通道与结果配对",
        "title": "5. 请求通道与结果配对"
      },
      {
        "depth": 2,
        "id": "6-事件通道",
        "title": "6. 事件通道"
      },
      {
        "depth": 2,
        "id": "7-driver-与-app-server-的连接方式",
        "title": "7. Driver 与 App Server 的连接方式"
      },
      {
        "depth": 2,
        "id": "8-正确关闭",
        "title": "8. 正确关闭"
      },
      {
        "depth": 2,
        "id": "9-消费方使用方式",
        "title": "9. 消费方使用方式"
      },
      {
        "depth": 3,
        "id": "91-zeta-exec",
        "title": "9.1 zeta-exec"
      },
      {
        "depth": 3,
        "id": "92-zeta-tui",
        "title": "9.2 zeta-tui"
      },
      {
        "depth": 2,
        "id": "10-当前实现审计",
        "title": "10. 当前实现审计"
      },
      {
        "depth": 2,
        "id": "11-目标模块",
        "title": "11. 目标模块"
      },
      {
        "depth": 2,
        "id": "12-验证要求",
        "title": "12. 验证要求"
      }
    ],
    "searchText": " 物理位置：zeta-rs/app-server-client/ 主要消费者：zeta-exec 非交互执行宿主、zeta-tui Wire contract：zeta-app-server-api.md Canonical 产品模型：protocol.md Headless 与远程调度：exec.md MCP Agent server consumer：mcp-server.md 当前 crate contract：zeta-rs/app-server-client/README.md 快速理解 App Server 客户端把“启动后端、初始化连接、配对请求、转发事件和正确关闭”封装成一个可交付的 就绪会话，使 CLI、TUI 和无交互宿主不必各自实现一遍。 调用方动作 客户端保证 调用方仍负责 --- --- --- 启动本地 App Server 只有进程和双向通道都建立后才开始初始化 提供启动配置和产品参数 取得 ready client 已完成能力、版本和模式校验 决定接下来调用哪个产品方法 发出多个并发请求 按请求 ID 配对结果并结束等待者 处理领域结果和用户交互 接收通知 独立转发服务端事件，不阻塞请求结果 更新自己的呈现状态 关闭宿主 拒绝新请求、结束等待者并等待后台任务退出 决定产品级退出或重连策略 将来连接远程 App Server 复用同一公共 facade 和产品契约 远程调度和工作进程协议不属于本客户端 1. 结论 zeta-app-server-client 存在的原因，不只是复用一组 typed RPC method。zeta-exec 与 zeta-tui 都需要完成同一套本地 App Server 宿主流程： 1. 根据启动配置创建并启动 App Server； 2. 建立 client 到 App Server 的请求通道； 3. 建立 App Server 到 client 的结果与事件通道； 4. 通过请求通道完成 initialize 与 schema/capability 校验； 5. 只在初始化成功后向调用方交付 ready client； 6. 运行期间正确配对请求结果，并持续转发 server notification； 7. 退出时关闭 connection、结束 pending 请求并等待后台任务停止。 这套启动、连接和关闭逻辑不能在 zeta-exec 与 zeta-tui 中各写一份，因此抽成共享 crate。 ``text zeta-exec ─┐ ├─► zeta-app-server-client ─► start App Server zeta-tui ──┘ │ ├─ request channel ──► App Server └─ result/event ◄──── App Server ` 直接依赖和启动 zeta-app-server 是 embedded backend 的职责，不是依赖方向错误。长期可以在 同一 public facade 下增加 remote App Server backend，但它只连接相同的 App Server contract， 不是 scheduler protocol，也不是 remote process executor。Desktop 的 JSONL/stdio client 仍不要求复用这个 Rust crate。 zeta-exec 是无交互界面的 Agent 执行宿主。当前它与 TUI 一样启动 embedded App Server； 后续远程调度系统以它作为 headless execution entry。Job/Attempt/lease/event cursor 属于 exec.md 定义的 scheduler adapter，不进入 App Server Client。 当前 zeta-cli 的交互式和无界面提示词路径已经使用自有 AppServerSession、可克隆请求句柄、独立 AppServerEvents 与显式关闭。 zeta-mcp-server 的 per-session adapter 当前使用同步 client 和 bounded polling/drain；这是 MCP 外层生命周期的实现选择，不改变 App Server 的 canonical request contract。 2. 抽象单位：一个运行中的 App Server Session 共享层的顶层抽象应是一个有明确所有权的运行会话，而不是裸 transport： `rust pub struct AppServerSession client: AppServerRequestHandle, events: Option<AppServerEvents , // private shutdown state and joined driver tasks ` AppServerSession 拥有： - embedded App Server runner，或一个 remote App Server connection； - 唯一 App Server connection； - request channel 的 server 端； - server message/event channel 的 client 端； - connection driver 与 server runner 的后台任务； - initialize/ready/closing/closed 生命周期； - 显式 shutdown 与 task join。 它向宿主提供两个运行时端点： `rust impl AppServerSession pub fn client &self - AppServerRequestHandle; pub fn takeevents &mut self - Result<AppServerEvents, TakeEventsError ; pub fn shutdown self - Result< , ShutdownError ; ` - AppServerRequestHandle 是共享 request ID allocator 的可克隆 typed request handle，供 feature/task 发送 request； - AppServerEvents 是单消费者事件流，供 app event loop 持续接收 notification 与 connection lifecycle event； - AppServerSession 保持所有权直到显式 shutdown，避免最后一个 client clone 的偶然 drop 决定 server 生命周期。 结果与事件必须区分： - request result 通过该 request 自己的 completion/oneshot 返回； - server notification 通过独立 AppServerEvents 流返回； - connection closed、protocol failure 等连接事件也通过 AppServerEvents 告知宿主； - notification 不得依附某次 request，不能通过 drainnotifications 拉取。 3. 为什么由这个 crate 统一连接 App Server zeta-exec 和 zeta-tui 都是本地 App Server 的宿主，而不是已经存在的外部 server 的普通 调用方。两者需要相同的 composition： - 用户 profile root 与本地 SQLite state repository； - config、credentials、model provider 和 tool runtime； - App Server dispatcher； - connection-scoped subscription 与 Resource ownership； - client/server channel； - initialize 参数、schema hash 与 capabilities； - shutdown 与错误回收。 如果这些步骤留在各自 consumer，会形成两套不同的启动语义：一个可能忘记 schema gate，另一个 可能没有事件 pump，第三个关闭时遗留后台任务。共享 crate 必须保证它们拿到的是同一种 ready connection。 依赖方向应是： `text zeta-exec / zeta-tui │ ▼ zeta-app-server-client ├─► zeta-app-server ├─► zeta-app-server-protocol └─► channel/task runtime ` Consumer 不直接创建 AppServer、ConnectionState、dispatcher 或 notification broker。 这些类型可以在 client crate 内部使用，但不能泄漏到 zeta-exec 或 TUI 的业务代码。 当前 zeta-exec 已是非交互 Agent 宿主，并通过本 crate 启动 embedded App Server；底层 process execution 已迁移到独立 zeta-tool-executor。后续 remote scheduler 仍位于 zeta-exec 上层，不能让 “执行一个 tool process”和“宿主化完整 App Server”重新共享同一个模块或协议。 长期 backend 选择必须是显式 enum： `rust pub enum AppServerTarget Embedded EmbeddedAppServerOptions , Remote RemoteAppServerOptions , ` - Embedded 由本 crate 组合并启动本地 App Server； - Remote 连接 daemon/remote App Server，完成相同 initialize 与 event wiring； - 两者暴露相同 typed request handle、event stream 与 shutdown contract； - backend 差异不能泄漏成 Core 私有方法； - remote scheduler 仍位于 zeta-exec 上层，不等于 RemoteAppServerOptions。 4. 启动流程 建议暴露两个自解释入口，并可由 target enum 统一选择： `rust let mut session = AppServerSession::startembedded options ?; let client = session.client ; let events = session.takeevents ?; ` connectremote 仍是 Proposed backend；当前 Current backend 是 embedded。 Embedded start 必须按以下顺序执行： `text validate options → build App Server composition → create one server connection → create bounded request/result-event channels → start server runner and connection driver → send initialize through the normal request path → validate schema hash and required capabilities → return Ready AppServerSession ` 必须保持的 invariant： - initialize 是 connection 的首个 request； - initialize 也经过正式 request channel、dispatcher 和 result pairing，不能直接调用内部 server method； - schema hash 不一致或缺少 required capability 时，start 失败； - start 失败必须关闭已创建的 connection/channel 并 join 已启动的 task； - 调用方永远拿不到半初始化的 AppServerClient； - start options 完整描述 composition，不依赖 consumer 在启动前偷偷修改全局状态。 Remote connect 必须： `text validate endpoint/auth → connect bounded transport → send initialize as first request → validate schema/capabilities → start request/event driver → return Ready AppServerSession ` Remote connection failure 必须结束 pending request 并发出 connection event；自动 reconnect 只有 在能重新建立 subscription、Resource ownership 和 transient cursor 时才能增加，不能隐藏状态 丢失。 启动配置应使用自解释类型，而不是 bool： `rust pub struct AppServerStartOptions pub profileroot: PathBuf, pub clientinfo: ClientInfo, pub requiredcapabilities: RequiredCapabilities, ` 若 zeta-exec 与 TUI 的 capability 不同，可以分别构造 typed RequiredCapabilities，不能用 start ..., true, false 表达。 5. 请求通道与结果配对 AppServerClient 的 typed method 将请求送入后台 connection driver： `text typed params → allocate connection-local request ID → build typed ClientRequest → request channel → App Server dispatcher → response channel → match pending request ID → typed result / typed server error ` Embedded hot path 推荐使用 protocol-owned typed request enum： `rust enum ClientRequest Initialize InitializeParams , SessionCreate SessionCreateParams , SessionRequest SessionRequestParams , SessionThreadSubscribe SessionThreadSubscribeParams , // generated/registered remaining methods ` Client worker 在 request 外附加 connection-local request ID 与 oneshot completion。In-process channel 不需要先序列化为 JSON string，但仍必须经过相同 initialize gate、method dispatcher、 result/error envelope 和 notification contract。JSON/JSONL/WebSocket backend 在 transport 边界执行 wire encoding。 公共接口仍然是类型化方法： `rust let result = client.requestsession SessionRequestParams / ... / ?; ` 调用方不拼 method string、不处理 JSON-RPC request ID，也不直接操作 request channel。 当前 typed method 同步等待自己的 completion，但 request 在独立 driver 上执行；需要在 UI 主循环中避免等待异常缓慢 request 时，由 consumer 把 completion 重新投递成 app event。 请求 driver 负责： - 分配只在当前 connection 内有效的 request ID； - 保存 requestId → completion pending table； - 校验 response ID、envelope 与 typed result； - 允许多个调用方持有 AppServerClient clone； - connection 关闭时一次性结束全部 pending request； - 将 server stable error 保留为 typed AppServerError，包括 code、name 和 data； - 对 unknown、duplicate、retired request ID 做明确 protocol error 处理。 JSON-RPC request ID 不能代替产品 CommandId。修改操作的 CommandId、 expectedSequence 和 exact typed payload 由调用方创建并传入，client 只负责无损发送。超时或 response 丢失后的业务重试策略不属于 connection driver。 6. 事件通道 App Server notification 与 request result 是两条独立交付路径： `text ┌─ matching response ─► request completion App Server outbound ─────┤ └─ notification ──────► AppServerEvents ` 当前公共事件契约是： `rust pub enum AppServerEvent Notification ServerNotification , ConnectionClosed ConnectionCloseReason , ` 当前 App Server API 包含 session/update、session/thread/update、owner-directed agent/request、connector/changed、skills/changed、Git 与 filesystem notification，均由 ServerNotification typed enum 表达。approval/user-input 通过 agent/request + canonical SessionRequest::ResolveInteraction 完成，不在 client crate 建第二套 server-request envelope。 显式 Lagged/Desynced lifecycle event 尚未提供；当前 transient overflow 通过 cursor gap 触发 consumer snapshot resync，control-only overflow 关闭 connection。 ServerNotification、method registry 与 payload decoder 由 protocol crate 的同一个 servernotifications! 定义生成。该总枚举跨 crate 标记为非穷尽：client boundary 必须严格解码 每一个已注册 method，而 consumer 只能在自己的 projection 中选择所拥有的 capability，并为其余 通知保留 fallback。新增 Document 或其他领域通知因此不会要求无关产品同步增加 ignore arm。 事件 driver 负责： - 在没有新 request 时仍持续接收 notification； - 解码 session/update 与 session/thread/update； - 保留 durable sequence 与 transient stream cursor； - 保证同一 request 的 response 先于它产生的 causal notification 交付； - 已知 notification 在 remote decode 或 typed validation 失败时关闭 connection 并报告 protocol failure； - server request 无 consumer/handler 时及时返回 stable rejection，不能让 approval 永久等待； - event receiver 被 consumer drop 时执行明确 policy，而不是无限积压； - bounded channel 满时不静默丢弃 durable update。 Event channel 不负责： - 把 update 应用到 Session/Thread projection； - 判断 durable sequence gap 后如何 resubscribe； - 推导 Turn 或 Item 的权威终态； - 将 notification 变成日志文本； - 合并不同 aggregate 的 sequence。 这些属于 zeta-exec 的输出/终态协调或 TUI projection。 Consumer projection 的依赖方向是： `text protocol registry（method + payload + decoder） │ ▼ AppServerEvents::Notification ServerNotification │ ├─► TUI notification projection ─► ClientEvent ├─► zeterm agent/session projection ─► AgentSessionEvent └─► headless completion projection ` 产品 projection 不得穷尽列出未拥有的 notification。TUI 当前拥有 Connector Pane，因此由 Connector capability 消费 ConnectorsChanged 并触发 canonical list refresh；其他产品若不拥有 Connector UI，则无需增加对应分支。Connector 状态不能塞进通用 connection lifecycle event。 7. Driver 与 App Server 的连接方式 共享层需要同时驱动请求与事件，不能保留“调用一次、顺便 drain 一次”的模型： `text AppServerClient clones │ ▼ bounded request channel │ ▼ connection driver ├─ dispatch request ─────────► App Server ├─ pair response ────────────► request completion ├─ receive notification ─────► AppServerEvents └─ observe shutdown ─────────► close + join ` 进程内连接仍应经过同一个 App Server protocol dispatcher。Typed channel 可以避免 JSON 编解码、 stdio 与子进程开销，但不能直接调用 Core、Store 或私有 App Server operation。它复用相同 request/result/error/notification 语义，而不是复制第二份 in-process response contract。 App Server 当前通过 connection-scoped ConnectionNotifications 提供条件变量唤醒的 outbound source；client event pump 与 request driver 独立，因此空闲连接和长 Turn 的 update 都无需 新 request 即可到达。runner 同时处理： - 新 client request； - 新 server notification； - shutdown signal。 这属于 client session 与 App Server runner 共同遵守的 channel contract。 8. 正确关闭 显式 AppServerSession::shutdown 是正常退出路径。它必须： 1. 原子地把 session 状态从 Ready 改为 Closing； 2. 拒绝新 request； 3. 结束或明确失败所有尚未完成的 request completion； 4. 关闭 App Server connection，使 subscription 与 Resource ownership 失效； 5. 停止 server runner 和 connection driver； 6. 关闭 event stream，并给 consumer 一个最终 ConnectionClosed 原因； 7. join 所有由 session 启动的后台 task； 8. 返回 shutdown 期间发生的错误。 关闭 connection 不等于 session/request 的 InterruptTurn。若产品要求退出前中断某个 Turn， zeta-exec 或 TUI 必须先发送 typed session/request 并等待所需终态，再调用 session shutdown。Client session 不能猜测要中断哪个 Thread/Turn。 Drop 只能做 best-effort cancellation，不能替代显式 shutdown，因为 Drop 无法可靠等待 后台任务结束。测试和正常 consumer 路径必须调用并等待 shutdown 。 需要定义以下边界情况： - event receiver 先于 session 被 drop； - 所有 client handle 被 drop，但 session owner 仍存在； - session shutdown 时仍有 client clone； - initialize 期间 consumer 取消启动； - App Server runner panic； - request completion receiver 被调用方取消； - shutdown 被重复触发。 9. 消费方使用方式 9.1 zeta-exec 非交互宿主： 1. 启动 AppServerSession； 2. 从 client handle 发送 Session/Thread/Turn typed request； 3. 同时消费 request result 与 AppServerEvents； 4. 根据 canonical Turn terminal update 产生 human/JSON/JSONL 输出和退出码； 5. Ctrl-C 时按产品策略发送 session/request 的 InterruptTurn； 6. 最后显式 shutdown session。 它不自行创建 App Server、初始化 connection 或实现 notification pump。 后续 scheduler-facing protoco"
  },
  {
    "slug": "codex-app-server",
    "sourcePath": "docs/codex-app-server.md",
    "title": "zeta-codex-app-server 架构",
    "description": "Codex App Server 适配器是本机桥接层：它让 Zeta 使用用户自己的 ChatGPT/Codex 订阅，同时把 登录凭据、远端 Agent 循环和私有后端兼容性继续留给上游 Codex 管理。",
    "group": "API 与协议",
    "markdown": "> 物理位置：`zeta-rs/codex-app-server/`\n> Rust crate：`zeta_codex_app_server`\n> 当前状态：managed account/login、thread/Turn streaming 与 Core Turn backend adapter 已实现；\n> 默认 App Server 已通过显式 `openai-chatgpt` 模型选择接入订阅 Turn\n> 登录控制面：[`login.md`](login.md)\n> Core 执行边界：[`core.md`](core.md)\n> Provider runtime：[`model-provider.md`](model-provider.md)\n> 上游依据：[Codex App Server](https://learn.chatgpt.com/docs/app-server)\n\n## 快速理解\n\nCodex App Server 适配器是本机桥接层：它让 Zeta 使用用户自己的 ChatGPT/Codex 订阅，同时把\n登录凭据、远端 Agent 循环和私有后端兼容性继续留给上游 Codex 管理。\n\n| 场景 | 正确路径 | 明确禁止 |\n| --- | --- | --- |\n| 使用 OpenAI Platform API key | model-provider → `zeta-api` → `zeta-client` → HTTP | 转换成 Codex 订阅凭据 |\n| 使用 ChatGPT/Codex 订阅 | Core `TurnExecutionBackend` → 本适配器 → 本地 `codex app-server` | 直连私有 ChatGPT 后端 |\n| 用户登录 | Zeta 登录控制面委托上游浏览器或设备码流程 | 读取或复制上游 token |\n| 上游版本不兼容 | 返回明确的协议或能力错误 | 猜测未声明方法仍然可用 |\n| 上游进程崩溃 | 进行中的 Turn 按未知结果失败，不重放 | 盲目重放模型、工具或审批动作 |\n\n## 1. 结论\n\n`zeta-codex-app-server` 是对**外部上游 `codex app-server`** 的本机 adapter。它与\n`zeta-app-server` 不是同一个服务：前者是 Zeta 使用的 Codex runtime client，后者是 Zeta 向\nDesktop、CLI 和 TUI 暴露的产品 RPC server。\n\n上游 Codex 保留 OAuth login、credential persistence/refresh、模型请求、远端工具循环和 Codex\nbackend compatibility 的所有权。Zeta 只适配可检查的本地 JSON-RPC contract，并把可持久化的结果\n投影回 Core。\n\n### 当前实现\n\n| 能力 | 状态 | 当前边界 |\n| --- | --- | --- |\n| 懒启动本地 `codex app-server`、stdio JSONL、initialize/initialized | ✅ | 有界 frame、单 writer、exact request ID、进程退出失败传播 |\n| `account/read`、browser/device-code start、cancel、logout | ✅ | 只映射脱敏账户与用户可见授权指令 |\n| login completion/account updated 主动通知 | ✅ | 上游 login ID 映射为 Zeta-owned ID，兼容早到通知 |\n| read-only / workspace-write thread 与 Turn streaming | ✅ | typed thread/start/resume、turn/start/interrupt 与增量事件 |\n| command/file approval、structured user input | ✅ | 先建立 durable Core interaction，再 once-only 回答 exact upstream request |\n| Core `TurnExecutionBackend` adapter | ✅ | Core 保留 Thread authority；Codex 执行完整远端 Agent loop |\n| completed remote thread binding | ✅ | 成功 Turn 后持久化，重建后使用 thread/resume；不持久化或重放 in-flight Turn |\n| 默认产品执行 composition | ✅ | `model/list` 投影 account-filtered Codex models；持久化 Turn model 的 exact provider 决定路由 |\n| permission approval、diff/image/secret input、rate limit | 尚未完成 | 必须按独立语义切片接入或明确拒绝 |\n\n当前兼容门以 initialize response 与所需 method/shape 为准；还没有声明固定的上游 semver 范围。\nmethod 缺失或响应 shape 不兼容会明确失败，不会改为直连 ChatGPT 私有 backend。\n\n## 2. 所有权\n\n`zeta-codex-app-server` 拥有：\n\n- `codex app-server` 子进程启动、initialize handshake、连接 generation 和显式 shutdown；\n- bounded JSONL framing、request ID correlation、response/server-request/event dispatch；\n- account/login 的安全 adapter；\n- Codex thread/Turn、approval、user-input 与 stream event 的 typed adapter；\n- `CodexTurnExecutionBackend` 对 Core item、interaction、cancellation 和 terminal outcome 的映射；\n- 上游进程故障、版本不兼容和 unknown-outcome 的 fail-closed 处理。\n\n它不拥有：\n\n- ChatGPT OAuth client、PKCE verifier、callback listener、device-code polling 或 token refresh；\n- 读取 `~/.codex/auth.json`、Keychain 或任何上游 token；\n- 直接请求 `chatgpt.com/backend-api/codex`；\n- Zeta 的模型选择、Core Thread authority、产品 UI 或 HTTP provider runtime；\n- 上游未公开能力的兼容猜测。\n\n## 3. 两条 OpenAI 执行路径\n\n```text\nOpenAI Platform API key\n  zeta-model-provider → zeta-api → zeta-client → zeta-http-client → api.openai.com\n\nChatGPT/Codex subscription\n  Core TurnExecutionBackend → CodexTurnExecutionBackend\n                            → CodexTurnDriver\n                            → shared CodexAppServerRuntime\n                            → local codex app-server\n                            → Codex-managed ChatGPT login/backend\n```\n\n两条路径的 credential、endpoint、模型/功能集和错误语义不能互相降级或转换。Platform API key\n不能访问 Codex subscription runtime；Codex token 不能作为 `zeta-api` 的 Bearer credential。\n\n## 4. 登录与共享 runtime\n\n```text\nZeta account/login/start\n  → zeta-login\n  → CodexAppServerLoginDriver\n  → shared CodexAppServerRuntime\n  → upstream account/login/start { type: chatgpt }\n```\n\n`CodexAppServerLoginDriver` 与 `CodexTurnDriver` 共享同一份懒启动 runtime 和连接 generation，但不\n共享 token。上游负责 callback listener、credential persistence 与 refresh；Zeta 不调用\ninternal-only `chatgptAuthTokens` 变体，也不读取上游 auth storage。\n\n## 5. Core Turn 执行边界\n\n`TurnExecutionBackend` 由 Core 定义，是“推进一个已持久化 Turn”的 consumer-owned port。默认\n`TurnExecutor` 实现 Zeta 自己的 model → tool 循环；`CodexTurnExecutionBackend` 则把完整 Agent loop\n委托给 Codex。它不是 raw model invoker，因此不属于 `zeta-model-provider`。\n\n```text\nThreadController accepts/starts Turn\n  → selected TurnExecutionBackend.start\n  → Codex thread/start or thread/resume\n  → Codex turn/start\n  ← item deltas / approval request / user input / completed\n  → transient Core stream updates\n  → durable Core interactions and items\n  → terminal TurnCompleted or TurnFailed\n```\n\n命令与文件审批、structured user input 必须先成为 durable Core interaction，之后才能回答 exact\nupstream JSON-RPC request。重复 response、错误 response kind、旧 connection generation 或未知 request\n都被拒绝。含 secret 的 user input 当前直接拒绝，因为 Core 的 durable response 不能存 secret。\n\n默认产品组合把上游 `model/list` 的可用项投影成 `provider = openai-chatgpt` 的 ModelRef；只有用户将\n该精确 ModelRef 选入 Session，随后复制到 Turn，App Server 才会选择 Codex backend。登录状态本身\n不会切换已有 Session/Thread，也不会改变 direct-provider 路径。\n\n在第一次远程请求前，adapter 先追加 `TurnExecutionAttempted`。因此进程丢失后，已有 attempt 的\nin-flight Turn 被视为结果未知并失败，不能重放。成功完成后 adapter 追加 immutable\n`TurnExecutionBound`，保存 backend ID、remote thread ID 与 opaque Workspace authority scope。这个\nappend 发生在 Turn completion 之后，不与 completion 原子提交；若 binding append 失败，Turn 仍保持\ncompleted，但后续不能保证恢复远端 thread continuity。in-flight remote Turn ID 从不持久化或重放。\n\n## 6. 失败与安全语义\n\n- spawn、pipe、framing、timeout、process exit 与 connection close 变成稳定错误；\n- active Turn 在连接关闭后失败，不能把“未收到 terminal”解释为“上游未执行”；\n- connection generation 阻止旧 server request 被发送到重启后的新进程；\n- unknown method 以 JSON-RPC method-not-found 回答，invalid request 以 invalid-params 回答；\n- 不支持的 secret input、local image、approval capability 与 schema shape fail closed；\n- stderr、credential-bearing payload 与 authorization query 不进入 RPC error 或 telemetry；\n- read-only 与 workspace-write 是显式构造选项，workspace 必须是绝对路径。\n\n## 7. 当前限制与下一步\n\n1. 按 canonical product contract 增加 `item/permissions/requestApproval`、diff 更新、image input、\n   rate-limit 与 richer completed-item projection。\n2. 若要支持 secret user input，先建立不把 secret response 写入 durable Thread event 的专用通道。\n3. 固定并测试受支持的 Codex CLI/App Server 版本范围与 capability gate。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 3,
        "id": "当前实现",
        "title": "当前实现"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 2,
        "id": "3-两条-openai-执行路径",
        "title": "3. 两条 OpenAI 执行路径"
      },
      {
        "depth": 2,
        "id": "4-登录与共享-runtime",
        "title": "4. 登录与共享 runtime"
      },
      {
        "depth": 2,
        "id": "5-core-turn-执行边界",
        "title": "5. Core Turn 执行边界"
      },
      {
        "depth": 2,
        "id": "6-失败与安全语义",
        "title": "6. 失败与安全语义"
      },
      {
        "depth": 2,
        "id": "7-当前限制与下一步",
        "title": "7. 当前限制与下一步"
      }
    ],
    "searchText": " 物理位置：zeta-rs/codex-app-server/ Rust crate：zetacodexappserver 当前状态：managed account/login、thread/Turn streaming 与 Core Turn backend adapter 已实现； 默认 App Server 已通过显式 openai-chatgpt 模型选择接入订阅 Turn 登录控制面：login.md Core 执行边界：core.md Provider runtime：model-provider.md 上游依据：Codex App Server 快速理解 Codex App Server 适配器是本机桥接层：它让 Zeta 使用用户自己的 ChatGPT/Codex 订阅，同时把 登录凭据、远端 Agent 循环和私有后端兼容性继续留给上游 Codex 管理。 场景 正确路径 明确禁止 --- --- --- 使用 OpenAI Platform API key model-provider → zeta-api → zeta-client → HTTP 转换成 Codex 订阅凭据 使用 ChatGPT/Codex 订阅 Core TurnExecutionBackend → 本适配器 → 本地 codex app-server 直连私有 ChatGPT 后端 用户登录 Zeta 登录控制面委托上游浏览器或设备码流程 读取或复制上游 token 上游版本不兼容 返回明确的协议或能力错误 猜测未声明方法仍然可用 上游进程崩溃 进行中的 Turn 按未知结果失败，不重放 盲目重放模型、工具或审批动作 1. 结论 zeta-codex-app-server 是对外部上游 codex app-server 的本机 adapter。它与 zeta-app-server 不是同一个服务：前者是 Zeta 使用的 Codex runtime client，后者是 Zeta 向 Desktop、CLI 和 TUI 暴露的产品 RPC server。 上游 Codex 保留 OAuth login、credential persistence/refresh、模型请求、远端工具循环和 Codex backend compatibility 的所有权。Zeta 只适配可检查的本地 JSON-RPC contract，并把可持久化的结果 投影回 Core。 当前实现 能力 状态 当前边界 --- --- --- 懒启动本地 codex app-server、stdio JSONL、initialize/initialized ✅ 有界 frame、单 writer、exact request ID、进程退出失败传播 account/read、browser/device-code start、cancel、logout ✅ 只映射脱敏账户与用户可见授权指令 login completion/account updated 主动通知 ✅ 上游 login ID 映射为 Zeta-owned ID，兼容早到通知 read-only / workspace-write thread 与 Turn streaming ✅ typed thread/start/resume、turn/start/interrupt 与增量事件 command/file approval、structured user input ✅ 先建立 durable Core interaction，再 once-only 回答 exact upstream request Core TurnExecutionBackend adapter ✅ Core 保留 Thread authority；Codex 执行完整远端 Agent loop completed remote thread binding ✅ 成功 Turn 后持久化，重建后使用 thread/resume；不持久化或重放 in-flight Turn 默认产品执行 composition ✅ model/list 投影 account-filtered Codex models；持久化 Turn model 的 exact provider 决定路由 permission approval、diff/image/secret input、rate limit 尚未完成 必须按独立语义切片接入或明确拒绝 当前兼容门以 initialize response 与所需 method/shape 为准；还没有声明固定的上游 semver 范围。 method 缺失或响应 shape 不兼容会明确失败，不会改为直连 ChatGPT 私有 backend。 2. 所有权 zeta-codex-app-server 拥有： - codex app-server 子进程启动、initialize handshake、连接 generation 和显式 shutdown； - bounded JSONL framing、request ID correlation、response/server-request/event dispatch； - account/login 的安全 adapter； - Codex thread/Turn、approval、user-input 与 stream event 的 typed adapter； - CodexTurnExecutionBackend 对 Core item、interaction、cancellation 和 terminal outcome 的映射； - 上游进程故障、版本不兼容和 unknown-outcome 的 fail-closed 处理。 它不拥有： - ChatGPT OAuth client、PKCE verifier、callback listener、device-code polling 或 token refresh； - 读取 /.codex/auth.json、Keychain 或任何上游 token； - 直接请求 chatgpt.com/backend-api/codex； - Zeta 的模型选择、Core Thread authority、产品 UI 或 HTTP provider runtime； - 上游未公开能力的兼容猜测。 3. 两条 OpenAI 执行路径 ``text OpenAI Platform API key zeta-model-provider → zeta-api → zeta-client → zeta-http-client → api.openai.com ChatGPT/Codex subscription Core TurnExecutionBackend → CodexTurnExecutionBackend → CodexTurnDriver → shared CodexAppServerRuntime → local codex app-server → Codex-managed ChatGPT login/backend ` 两条路径的 credential、endpoint、模型/功能集和错误语义不能互相降级或转换。Platform API key 不能访问 Codex subscription runtime；Codex token 不能作为 zeta-api 的 Bearer credential。 4. 登录与共享 runtime `text Zeta account/login/start → zeta-login → CodexAppServerLoginDriver → shared CodexAppServerRuntime → upstream account/login/start type: chatgpt ` CodexAppServerLoginDriver 与 CodexTurnDriver 共享同一份懒启动 runtime 和连接 generation，但不 共享 token。上游负责 callback listener、credential persistence 与 refresh；Zeta 不调用 internal-only chatgptAuthTokens 变体，也不读取上游 auth storage。 5. Core Turn 执行边界 TurnExecutionBackend 由 Core 定义，是“推进一个已持久化 Turn”的 consumer-owned port。默认 TurnExecutor 实现 Zeta 自己的 model → tool 循环；CodexTurnExecutionBackend 则把完整 Agent loop 委托给 Codex。它不是 raw model invoker，因此不属于 zeta-model-provider。 `text ThreadController accepts/starts Turn → selected TurnExecutionBackend.start → Codex thread/start or thread/resume → Codex turn/start ← item deltas / approval request / user input / completed → transient Core stream updates → durable Core interactions and items → terminal TurnCompleted or TurnFailed ` 命令与文件审批、structured user input 必须先成为 durable Core interaction，之后才能回答 exact upstream JSON-RPC request。重复 response、错误 response kind、旧 connection generation 或未知 request 都被拒绝。含 secret 的 user input 当前直接拒绝，因为 Core 的 durable response 不能存 secret。 默认产品组合把上游 model/list 的可用项投影成 provider = openai-chatgpt 的 ModelRef；只有用户将 该精确 ModelRef 选入 Session，随后复制到 Turn，App Server 才会选择 Codex backend。登录状态本身 不会切换已有 Session/Thread，也不会改变 direct-provider 路径。 在第一次远程请求前，adapter 先追加 TurnExecutionAttempted。因此进程丢失后，已有 attempt 的 in-flight Turn 被视为结果未知并失败，不能重放。成功完成后 adapter 追加 immutable TurnExecutionBound，保存 backend ID、remote thread ID 与 opaque Workspace authority scope。这个 append 发生在 Turn completion 之后，不与 completion 原子提交；若 binding append 失败，Turn 仍保持 completed，但后续不能保证恢复远端 thread continuity。in-flight remote Turn ID 从不持久化或重放。 6. 失败与安全语义 - spawn、pipe、framing、timeout、process exit 与 connection close 变成稳定错误； - active Turn 在连接关闭后失败，不能把“未收到 terminal”解释为“上游未执行”； - connection generation 阻止旧 server request 被发送到重启后的新进程； - unknown method 以 JSON-RPC method-not-found 回答，invalid request 以 invalid-params 回答； - 不支持的 secret input、local image、approval capability 与 schema shape fail closed； - stderr、credential-bearing payload 与 authorization query 不进入 RPC error 或 telemetry； - read-only 与 workspace-write 是显式构造选项，workspace 必须是绝对路径。 7. 当前限制与下一步 1. 按 canonical product contract 增加 item/permissions/requestApproval`、diff 更新、image input、 rate-limit 与 richer completed-item projection。 2. 若要支持 secret user input，先建立不把 secret response 写入 durable Thread event 的专用通道。 3. 固定并测试受支持的 Codex CLI/App Server 版本范围与 capability gate。"
  },
  {
    "slug": "protocol",
    "sourcePath": "docs/protocol.md",
    "title": "产品协议",
    "description": "协议层定义 Zeta 各组件共同使用的对象、意图、事实和更新；它让不同进程说同一种语言，但不执行 业务逻辑，也不选择存储或传输。",
    "group": "API 与协议",
    "markdown": "> 物理位置：`zeta-rs/protocol/`  \n> 当前状态：Session-first canonical contract 与 Agent interaction delivery/deadline 已落地\n> Crate 类型、helper 与同步修改关系：[`zeta-rs/protocol/README.md`](../zeta-rs/protocol/README.md)\n> 上层接口：[Zeta App Server API](zeta-app-server-api.md)  \n> 执行计划：[Zeta Agent 执行架构与演进方案](zeta-agent-runtime-architecture.md)\n\n> 文档所有权：本文件拥有跨组件语义、当前审计与演进方向；crate README 拥有当前源码接口、\n> serialization invariant 与修改路径。\n\n## 快速理解\n\n协议层定义 Zeta 各组件共同使用的对象、意图、事实和更新；它让不同进程说同一种语言，但不执行\n业务逻辑，也不选择存储或传输。\n\n| 概念 | 回答的问题 | 不包含 |\n| --- | --- | --- |\n| Session、Thread、Turn、ThreadItem | 系统中的产品对象是什么？ | actor、锁、任务和数据库 |\n| Command | 调用方想改变什么？ | 实际状态迁移和副作用 |\n| Event | 哪个事实已经可靠提交？ | 文件格式和写盘策略 |\n| Update | 消费方应该观察到什么变化？ | UI 呈现和重连策略 |\n| 请求与响应 | Turn 正在等待什么外部输入？ | 批准界面和工具实现 |\n| 稳定 ID、序列与游标 | 对象是谁、状态到第几版、从哪里继续？ | 传输 request ID 和连接状态 |\n\n## 1. 结论\n\n`zeta-protocol` 是 Zeta 跨 crate、跨进程和跨客户端共享的语义词汇表。它定义“系统中的对象、\n意图、事实和更新是什么”，但不决定“由谁、何时、通过什么 I/O 执行”。\n\n它的长期职责固定为：\n\n- canonical `Session → Thread → Turn → ThreadItem` 产品模型；\n- 稳定 ID 与共享 value object；\n- `SessionCommand` / `ThreadCommand` 产品意图；\n- `SessionEvent` / `ThreadEvent` durable fact；\n- `SessionUpdate` / `ThreadUpdate` consumer-facing update；\n- provider-independent model、tool、input、request/response 和稳定错误值。\n\n它不得拥有：\n\n- actor、mailbox、channel、task、锁和进程内执行实例；\n- reducer、状态迁移策略、重试、超时、取消执行和恢复流程；\n- 文件、数据库、JSONL、rollout framing、checksum 和 schema migration；\n- JSON-RPC method、request ID、connection state 和 transport；\n- provider HTTP payload、endpoint、credential 和 client；\n- tool execution、sandbox、approval policy 和副作用。\n\n一句话判断边界：\n\n> 至少两个独立组件需要以相同语义理解的纯数据契约，可以进入 protocol；只服务于一个执行层\n> 或带有 I/O、策略和生命周期行为的类型，不进入 protocol。\n\n## 2. 所有权与依赖方向\n\n`zeta-protocol` 不依赖任何 Zeta 内部 crate，目前只依赖序列化和 schema 生成库：\n\n```text\nserde / serde_json / schemars / ts-rs\n                   │\n                   ▼\n             zeta-protocol\n                   ▲\n       ┌───────────┼────────────┬──────────────┐\n       │           │            │              │\n   zeta-core  history/store  zeta-api   app-server-protocol\n       ▲                        ▲              ▲\n       │                        │              │\n  agent/execution          providers      clients\n```\n\n各消费者的准确职责：\n\n| 消费者 | 使用 protocol 的方式 | 不得反向放入 protocol |\n| --- | --- | --- |\n| `zeta-core` | reducer 输入、projection、execution intent | actor message、policy、effect、recovery |\n| `zeta-session-store` | 持久化 `SessionEvent` | sequence、timestamp、receipt envelope |\n| `zeta-history` | 为 `ThreadEvent` 定义 persisted record envelope | Store、SQLite、reducer、UI history |\n| `zeta-thread-store` | 完整读取和追加 `zeta_history::StoredEvent` | record serde shape、SQLite、reducer、UI Turn page |\n| `zeta-api` | canonical model value 与 provider wire 互转 | HTTP DTO、header、endpoint |\n| `zeta-model-provider` | model catalog 与 invocation request/response | transport、credential、retry |\n| `zeta-app-server-protocol` | 机械复用 canonical view/update | JSON-RPC params、result、method registry |\n| Desktop / CLI / TUI | 通过生成契约或 client 消费 snapshot/update | 客户端权威状态机 |\n\n禁止出现：\n\n```text\nzeta-protocol → zeta-core\nzeta-protocol → zeta-storage\nzeta-protocol → zeta-app-server-protocol\nzeta-protocol → provider implementation\n```\n\n## 3. Canonical 产品模型\n\n唯一产品层级是：\n\n```text\nSession\n└─ Thread\n   └─ Turn\n      └─ ThreadItem\n```\n\n### 3.1 Session\n\n`Session` 是任务与 Thread 拓扑的 readable model，拥有：\n\n- task title 和 lifecycle；\n- Thread membership；\n- root/fork lineage；\n- Session 自己的 durable sequence。\n\nSession 不嵌入 Thread 的 Turn/Item 历史。`ThreadOrigin::Fork` 保存\n`parentThreadId + parentSequence`，表达不可变的分支历史锚点。\n\n当前 canonical `ThreadOrigin` 包含 `Root`、`Fork`、`Rewind` 与 `AgentSpawn`。Agent spawn 使用\n独立 `DelegationId`、不可变 `AgentContextSeed` 和跨 Thread message/result events；不复用\n`Fork`。当前实现边界与尚未完成的 join/cancellation 语义见\n[`core-multi-agent.md`](core-multi-agent.md)。\n\n长期若 Session shared defaults 需要被多个客户端读取、修改并持久恢复，应以明确的\n`SessionSettings` value 和 typed event 进入本契约；当前代码尚未形成该闭环，不能把 Core\n配置快照直接塞进 `Session`。\n\n### 3.2 Thread\n\n`Thread` 是一个独立执行分支，拥有：\n\n- immutable `SessionId`；\n- Thread title 和 lifecycle；\n- ordered Turns；\n- Thread 自己的 durable sequence。\n\nSession sequence 与 Thread sequence 是两个 aggregate 的 revision，不是 ID，也不是同一条\n全局计数器。\n\n### 3.3 Turn\n\n`Turn` 表示一次用户意图驱动的 Agent 执行，不等同于一条聊天消息。当前状态词汇已经包含：\n\n- `Created`、`Running`；\n- `WaitingForApproval`、`WaitingForUserInput`、`WaitingForCapability`；\n- `Cancelling`；\n- `Completed`、`Failed`、`Interrupted`。\n\n其中 `WaitingForUserInput` 与 `WaitingForCapability` 已有 durable interaction event、Core reducer\n和 recovery 支持；它们不能再被解释成单纯预留 enum。App Server 已按 initialize capability +\nSession-owned Thread subscription 选择 ephemeral owner，通过 `agent/request` 主动投递，并执行\n按 interaction 语义决定的断连处理和 durable deadline cancellation；这些 runtime state 仍不属于\ncanonical Thread。\n\n### 3.4 ThreadItem\n\n`ThreadItem` 是 Turn transcript 的 durable 业务单元，当前包含：\n\n- User Message；\n- User Image；\n- Agent Message；\n- Reasoning；\n- Plan；\n- Tool Call；\n- Tool Result。\n\nItem 必须拥有稳定 `ItemId` 并归属一个 `TurnId`。Token delta 不是 durable Item；最终完成的\nItem 才能进入 authoritative Thread history。\n\n## 4. 四类契约必须分离\n\n### 4.1 Command：请求改变状态\n\n```text\nSessionCommand\n  Create / CreateThread / ForkThread / ArchiveThread / Complete / Archive\n\nThreadCommand\n  StartTurn / InterruptTurn / ResolveUserInput / ResolveDynamicTool\n```\n\nCommand 表达产品意图，不代表已经发生，也不能直接写入 projection。\n\n`CommandId` 用于 retry-safe typed identity。`expectedSequence` 用于 optimistic\nconcurrency。JSON-RPC request ID 不得替代 CommandId。\n\n前端关闭 Tab 不会扩展这组 canonical command。它是 App Server 的产品动作：外部通过\n`session/request` 的 `Stop` operation 映射到 Core 的内部停止编排，持久化既有的 Session archive fact，并协调\n中断其 child Thread 中的活动 Turn。这样 UI 的 Tab/session 语义不会泄漏进 `zeta-protocol`；连接\n断开也不会被解释成产品 Session 停止。\n\n所有 durable side effect 的 receipt 必须保存：\n\n```text\nCommandId\n+ exact typed command payload\n+ response sequence / stable result\n```\n\n重放规则固定为：\n\n- 相同 `CommandId`、相同 typed payload：返回原结果；\n- 相同 `CommandId`、不同 typed payload：返回 `CommandConflict`；\n- 首次执行：校验 `expectedSequence`，并将 receipt 与首个业务 Event 原子提交。\n\n重放时先匹配 receipt，再返回稳定结果，因此响应丢失后的安全重试不受 aggregate 后续 sequence\n前进影响。不得用 JSON-RPC method + serialized params、sidecar idempotency ledger 或裸 request\nID 复制这套语义。\n\n`SessionCommandEnvelope`、`ThreadCommandEnvelope` 曾经只在 protocol 内定义，没有第二个\n执行层或 transport 消费者，已删除。Core command receipt 与 App Server params 各自属于其\n拥有的执行/transport 层；将来只有确有至少两个消费者的共享 command wrapper 才能重新进入\nprotocol。\n\n### 4.2 Event：已经 durable 的事实\n\n`SessionEvent` 与 `ThreadEvent` 是 reducer 和 store 唯一接受的领域事实。\n\n```text\nCommand\n  → Core validate/policy\n  → build Event\n  → Store append\n  → reducer\n  → publish committed Update\n```\n\nEvent 不携带 storage sequence、timestamp、schema version、event ID 或 command receipt。\nSession stored envelope 由 `zeta-session-store` 定义；Thread persisted record 由\n`zeta-history` 定义。Core 构造 record，Store 只校验并提交。\n\n必须保持：\n\n- Event 使用过去式事实命名；\n- Event 足以从空状态重建 canonical projection；\n- transient delta、delivery ack、RPC request 和 provider response 不进入 Event；\n- reducer policy 不编码进 Event helper。\n\n委托执行有两个 durable fact。`ThreadEvent::TurnExecutionAttempted` 在 backend 跨越外部副作用边界\n前写入 Turn；恢复时已有 attempt 的 in-flight Turn 结果未知，禁止重放。成功完成后，\n`ThreadEvent::TurnExecutionBound` 将 Thread 不可变地绑定到\n`{ backend, remoteThreadId, executionScope }`，并投影为\n`ThreadSnapshot.turn_execution_binding`。它不保存 access token、远端 Turn ID、pending request 或\nstream cursor；execution scope 是不含路径和凭据的 opaque Workspace authority identity，同一 Thread\n不能跨 scope 或改绑到另一个 backend/remote thread。\n\n### 4.3 更新：面向消费者的变化\n\n`SessionUpdate` / `ThreadUpdate` 服务于 UI、CLI、TUI 和订阅客户端。\n\n`Committed { event }` 携带 durable fact；`ItemStarted`、`ItemDelta`、`PlanUpdated` 是可丢失的\n低延迟更新。客户端必须能仅依赖 snapshot + durable committed gap 恢复正确状态。\n\n```text\ndurableSequence\n  = aggregate 已提交到第几个事实\n\nStreamCursor { streamInstanceId, sequence }\n  = 某次 transient stream emitter incarnation 中瞬态消息的位置\n```\n\n两者不能合并。进程重启后 stream cursor 可以失效，durable sequence 不能失效。\n\n当前瞬态 update、Core streaming model loop，以及 OpenAI Responses、OpenAI-compatible Chat\nCompletions、Anthropic Messages 的生产 HTTP/SSE 路径已经贯通。三种 endpoint 都使用各自原生\nwire stream；其他 SSE profile、NDJSON 与 WebSocket 仍需按真实协议逐项接入。\n\n### 4.4 Agent 请求/响应：Turn 中的双向等待\n\n`TurnInteraction` 是一个 Turn 当前未完成交互的 durable value，拥有 `RequestId`、可选 `ItemId`、\ntyped `AgentRequest` 与可选绝对 deadline。Readable `Turn` 只公开不含 payload 的\n`PendingInteraction`（request ID、kind、optional item/deadline），避免普通 Thread read 被误用为\ndelivery channel。`AgentRequestEnvelope` 在完整 `TurnInteraction` 基础上补齐\n`SessionId + ThreadId + TurnId`，供 App Server 构造 delivery；`AgentResponseEnvelope` 只做\nresponse correlation。\n\n它们用于关联：\n\n- structured user input；\n- dynamic tool call；\n- 将来明确接受的 capability interaction。\n\n它们不是 ThreadCommand 或 JSON-RPC envelope。对应的 durable Thread facts 是：\n\n```text\nInteractionRequested { interaction }\nInteractionResolved { requestId, response }\nInteractionCancelled { requestId, reason }\n```\n\n`InteractionRequested` 将 Running Turn 转为对应 waiting status，resolved/cancelled 关闭同一个\npending interaction并返回 Running。App Server 的 canonical `SessionRequest::ResolveInteraction`\n是 client 发起的 retry-safe aggregate command，必须带 exact `RequestId`；它不是 Agent request\n自身。\n\n完整生命周期必须明确：\n\n```text\nrequest fact committed\n→ owner connection selected (App Server ephemeral state)\n→ delivered\n→ response / timeout / cancellation / disconnect\n→ durable Turn transition\n```\n\n这里的 `owner connection` 不能进入 `ThreadEvent`、`TurnInteraction` 或 canonical Thread snapshot：\nconnection ID 在断开后失效，不是产品身份。App Server 必须维护短暂的 `RequestId → connection`\ndelivery assignment；断开时，尚未产生外部副作用的 approval/user-input 可以重新选择并重投递，\n已经投递的 dynamic tool 必须追加 `InteractionCancelled { reason: OwnerDisconnected }` 并按\nunknown outcome 收口，不能交给另一连接重试。任何分支都不得让 durable Turn 永远停在 waiting。\ndeadline 同样是 durable absolute instant，但 timer 和超时 policy 属于执行控制层。\n\n当前已完成 canonical contract、Core reducer/recovery、`session/request::ResolveInteraction`、\ninitialize capability、connection owner selection、`agent/request` 主动 delivery、按 interaction\n语义处理 owner 断连与 deadline timer。具体 host 只声明它实际支持的 interaction kind；对于\ndynamic tool，还必须在 `dynamicTools` 中声明它实际承载的 exact tool name，仅声明\n`DynamicTool` kind 不会获得其他动态工具的执行权。\n\n## 5. Sequence、Cursor 与 ID\n\n### 5.1 ID 回答“是谁”\n\n当前主要 ID：\n\n| ID | 身份范围 |\n| --- | --- |\n| `SessionId` | 一个产品任务 |\n| `ThreadId` | 一个独立执行分支 |\n| `TurnId` | 一次 Agent 执行 |\n| `ItemId` | 一个 transcript item |\n| `ToolCallId` | 一次工具调用 |\n| `CommandId` | 一个 retry-safe command |\n| `RequestId` | 一个双向 Agent request |\n| `StreamInstanceId` | 一次 transient stream emitter incarnation |\n\n### 5.2 Sequence 回答“第几个状态版本”\n\n- Session sequence 只排序 membership、lineage 和 Session lifecycle；\n- Thread sequence 只排序 Turn、Item 和执行终态；\n- stream cursor 只排序同一 stream incarnation 的 transient update；\n- fork parent sequence 是历史锚点。\n\n任何 ID 都不能由 sequence 推导，任何 sequence 也不能充当 ID。\n\nSession 与 Thread 使用独立 sequence，是因为它们是不同的冲突域：\n\n| Sequence | 排序内容 | 并发边界 |\n| --- | --- | --- |\n| Session | membership、fork lineage、Session lifecycle | 同一 Session 的拓扑修改 |\n| Thread | Turn、Item、工具与执行终态 | 同一 Thread 的执行修改 |\n\n如果合成一条 sequence，子 Thread 的执行会无意义地争用 Session revision，多分支并行会产生\n虚假的 optimistic-concurrency 冲突，单个 Thread 的恢复也会被迫读取整个 Session 的历史。\n逻辑 sequence 独立不代表物理实现重复；不同 aggregate 可以复用同一个 event-stream engine，\n只保留各自的 typed validation、reducer 和 writer scope。\n\n### 5.3 当前校验不一致\n\n`SessionId`、`ThreadId`、`TurnId`、`ItemId`、`CommandId`、`RequestId`、`StreamInstanceId`、\n`ToolCallId`、`ProviderId`、`ModelId` 与 `ToolName` 都使用 fallible constructor 与 validated\ndeserialize。JSON Schema 的最小长度只是外部提示，不能替代 Rust 侧校验。\n\n长期需要统一：\n\n- 外部输入反序列化必须拒绝空 ID；\n- 内部 ID constructor 不应制造无效值；\n- ID 格式只规定必要约束，不把当前生成算法写入 protocol；\n- 不使用裸 `String` 表达已经拥有专用 ID/newtype 的概念。\n\n## 6. 供应商无关的模型契约\n\n`model/invocation.rs` 定义 model invocation 的 canonical value：\n\n- `ModelRequest`；\n- input message / content / tool result；\n- tool definition / tool choice；\n- reasoning config；\n- `ModelResponse` / response item；\n- token usage / stop reason。\n\n`zeta-api` 负责把它转换为 OpenAI、Anthropic、Qwen 等 provider wire。Provider 特有字段不能\n反向污染 canonical model。\n\n`model/catalog.rs` 定义 model catalog metadata：\n\n- provider/model identity；\n- context window；\n- tools、reasoning、parallel tool call、personality capability；\n- reasoning effort；\n- auto-compaction threshold metadata；\n- model availability、catalog freshness、lifecycle 与 metadata quality 的跨 crate 值。\n\n目录 cache、scope、refresh、generation、字段 provenance 和 typed resolution 属于\n[`zeta-models-manager`](../zeta-rs/models-manager/README.md)，不进入纯 protocol value 层。完整\nsnapshot wire wrapper 尚未落地；当前 App Server 仍投影精简 `model/list` DTO。\n\n这里的 compaction 仅是声明信息。Context builder、token accounting policy、summary\ncheckpoint 和 compaction 执行都不属于 protocol。\n\n当前需要继续收敛的重复表示：\n\n| 概念 | 当前表示 | 长期处理 |\n| --- | --- | --- |\n| Tool name | `ToolName` | 已用于 durable Item、dynamic tool 和 model contract |\n| Tool arguments | `String`、`serde_json::Value` 并存 | 按 durable canonical 与 provider value 分层 |\n| Tool call ID | `ToolCallId` | 已用于 durable Item、dynamic tool 和 model contract |\n| Model response provenance | 无真实消费者 | 已删除 speculative `ResponseItemId` 与 provider raw response ID；有可证明的跨 provider 需求后再引入 |\n| Usage | model response 有 usage，Thread history 未建模 | 先明确 billing/diagnostic/product ownership |\n\n## 7. Config 值的边界\n\n`Theme`、`Personality`、`ApprovalMode`、`SandboxMode`、`WebSearchMode` 可以留在 protocol，\n前提是它们确实被多个组件以同一语义使用。\n\n以下内容属于 `zeta-config`，不能进入 protocol：\n\n- 文件位置与读取；\n- defaults、layer merge 和 precedence；\n- provider registry resolution；\n- credential reference resolution；\n- 更新事务与 authority snapshot。\n\n`Patch<T>` 只表达 missing/null/value 三态。字段是否允许 clear、如何持久化、冲突如何处理，\n仍由拥有该配置的应用层决定。\n\n## 8. 目录结构与公开 API\n\n### 8.1 当前已实现结构\n\n目录已经按 aggregate-first 迁移；每个实现模块仍保持在约 200 LoC 以下，因此 `item` 与\n`model` 暂时只拆到已有的稳定边界：\n\n```text\nsrc/\n├── ids/{session,thread,command,interaction,tool}.rs\n├── session/{model,status,origin,command,event,update}.rs\n├── thread/{model,status,command,event,update}.rs\n├── turn/{model,status}.rs\n├── item/{mod,plan}.rs\n├── interaction/{envelope,user_input,request_user_input,dynamic_tool}.rs\n├── model/{invocation,catalog}.rs\n├── config/{values,patch}.rs\n├── stream.rs\n├── tool_name.rs\n├── error.rs\n├── contract_tests.rs\n└── lib.rs\n```\n\n模块保持 private，`lib.rs` 已使用 named exports 暴露唯一的 crate API；新增内部类型不会被\nglob re-export 意外变成 public API。\n\n`ids/` 不再按模糊的 `product`、`operation` 分类；文件名必须直接表达 identity 的所属范围：\n\n- `session.rs` 只拥有 Session identity；\n- `thread.rs` 拥有 Thread 层级中的 Thread、Turn、Item identity；\n- `command.rs`、`interaction.rs`、`tool.rs` 分别拥有 retry command、双向 interaction 和 tool\n  execution 的 correlation identity；\n- stream emitter identity 与 `StreamCursor` 同属一个 cursor contract，因此定义在 `stream.rs`，\n  不在 `ids/` 中伪装成通用执行 identity。\n\n这种拆法比按“产品/操作”分组更稳定，因为后两者不是 protocol 中可审计的领域对象，也混淆了\n`CommandId`、`RequestId` 和 `ToolCallId` 互不相同的生命周期。\n\n### 8.2 长期目标结构\n\n长期按领域所有权组织，而不是把所有 aggregate 的 command、event 或 update 各放进一个横向\n大模块：\n\n```text\nzeta-rs/protocol/\n├── Cargo.toml\n└── src/\n    ├── lib.rs                         # private modules + named public exports\n    ├── contract_tests.rs              # crate-level serde/schema contract tests\n    ├── ids/\n    │   ├── mod.rs\n    │   ├── session_id.rs              # SessionId\n    │   ├── thread_id.rs               # ThreadId\n    │   ├── turn_id.rs                 # TurnId\n    │   ├── item_id.rs                 # ItemId\n    │   ├── command_id.rs              # CommandId\n    │   ├── request_id.rs              # RequestId\n    │   └── tool_call_id.rs            # ToolCallId\n    ├── session/\n    │   ├── mod.rs\n    │   ├── model.rs                   # Session / SessionThread\n    │   ├── status.rs                  # SessionStatus / SessionThreadStatus\n    │   ├── origin.rs                  # ThreadOrigin\n    │   ├── settings.rs                # 仅放已接受的 shared semantic defaults\n    │   ├── command.rs                 # SessionCommand\n    │   ├── event.rs                   # SessionEvent\n    │   └── update.rs                  # SessionUpdate + envelope\n    ├── thread/\n    │   ├── mod.rs\n    │   ├── model.rs                   # Thread\n    │   ├── status.rs                  # ThreadStatus\n    │   ├── command.rs                 # ThreadCommand\n    │   ├── event.rs                   # ThreadEvent\n    │   └── update.rs                  # ThreadUpdate / ItemDelta + envelope\n    ├── turn/\n    │   ├── mod.rs\n    │   ├── model.rs                   # Turn\n    │   └── status.rs                  # TurnStatus\n    ├── item/\n    │   ├── mod.rs                     # ThreadItem public sum type\n    │   ├── message.rs\n    │   ├── reasoning.rs\n    │   ├── plan.rs\n    │   └── tool.rs\n    ├── interaction/\n    │   ├── mod.rs\n    │   ├── envelope.rs                # AgentRequest / AgentResponse correlation\n    │   ├── user_input.rs\n    │   ├── request_user_input.rs\n    │   └── dynamic_tool.rs\n    ├── model/\n    │   ├── mod.rs                     # provider-independent model contract\n    │   ├── request.rs\n    │   ├── response.rs\n    │   ├── content.rs\n    │   ├── tool.rs\n    │   ├── reasoning.rs\n    │   └── catalog.rs                 # ModelRef / ModelInfo / capabilities\n    ├── config/\n    │   ├── mod.rs\n    │   ├── values.rs                  # shared semantic config values only\n    │   └── patch.rs\n    ├── stream.rs                      # StreamInstanceId + StreamCursor，不与 durable sequence 混放\n    ├── tool_name.rs                   # 跨 item/model/interaction 的 validated value\n    └── error.rs\n```\n\n这里选择 aggregate-first，而不是顶层 `command/`、`event/`、`update/`，原因是：\n\n- Session 与 Thread 是独立 aggregate，各自的 model、intent、fact 和 consumer update 应共同\n  演进；\n- 修改一个 aggregate 时，其 serde shape、状态词汇和 contract tests 可以放在同一目录审查；\n- 避免未来形成同时承载 Session、Thread 以及其他 aggregate 的巨型横向模块；\n- `turn` 和 `item` 是 Thread 下的共享产品概念，但也被客户端、trace 和 provider-neutral\n  context 使用，因此保留独立顶层模块。\n\n目标树表达所有权，不要求立即创建所有空文件。模块只有在已有类型或真实 vertical slice 需要\n时才建立；小型 payload 在拆分能提高内聚前可继续留在父模块。\n\n### 8.3 已完成的文件迁移\n\n| 当前文件 | 目标位置 |\n| --- | --- |\n| `ids.rs`、`session_id.rs`、`thread_id.rs` | `ids/` |\n| `response_item_id.rs` | 删除；没有真实 provenance 消费者 |\n| `session.rs` | `session/model.rs`、`session/status.rs`、`session/origin.rs` |\n| `thread.rs`、`turn.rs` | `thread/`、`turn/` |\n| `items.rs`、`plan_tool.rs` | `item/` |\n| `command.rs` | 按 aggregate 拆到 `session/command.rs` 与 `thread/command.rs` |\n| `session_event.rs`、`thread_event.rs` | 对应 aggregate 的 `event.rs` |\n| `thread_update.rs` | 拆到 `session/update.rs`、`thread/update.rs` 与 `stream.rs` |\n| `agent_request.rs`、`user_input.rs`、`request_user_input.rs`、`dynamic_tools.rs` | `interaction/` |\n| `models.rs`、`zeta_models.rs` | `model/`；后者明确重命名为 `catalog.rs` |\n| `config_types.rs`、`patch.rs` | `config/` |\n| `tool_name.rs`、`error.rs` | 暂留明确的顶层共享值模块 |\n| `protocol_tests.rs` | `contract_tests.rs`；细粒度测试随所属模块迁移 |\n\n迁移保持所有模块 private，`lib.rs` 以 named exports 维持唯一公共 API。模块级测试放在实现\n文件的 sibling `*_tests.rs` 中并用显式 `#[path = \"..._tests.rs\"]` 引入；跨模块 serde/schema\n约束保留在 `contract_tests.rs`。开发阶段直接同步调用方，不增加旧 module alias 或兼容 re-export。\n\n## 9. 当前完成度\n\n| 能力 | 状态 | 客观判断 |\n| --- | --- | --- |\n| Session-first hierarchy | 已完成 | Core、stores、App Server 和客户端已经使用 |\n| Session shared defaults | 未完成 | 尚无 canonical settings、typed event 与恢复闭环 |\n| Session/Thread 独立 sequence | 已完成 | model、store 和 fork lineage 已覆盖 |\n| durable Event 与 live Update 分离 | 基础完成 | store 类型只能接受 durable event |\n| typed Session/Thread command | 基础完成 | Core receipt 已使用；无消费者的 shared envelope 已删除 |\n| Tab 关闭到 Session 停止 | 已完成 | Chat 前端 → App Server `session/request` Stop → Core 内部停止编排；连接断开不触发停止 |\n| ThreadItem durable transcript | 基础完成 | text/image message、reasoning、plan、tool item 可重建 |\n| transient Item streaming | 类型已定义 | 当前没有完整异步 model stream producer |\n| Agent request/response | 基础完成 | durable request/resolve/cancel、deadline value、request correlation 和 typed resolve 已实现；owner delivery/timer 未实现 |\n| waiting Turn lifecycle | 基础完成 | event/reducer/recovery 已实现；异步 Agent loop 的继续执行尚未实现 |\n| provider-independent model values | 基础完成 | provider adapters 已使用，tool name/call ID 已收敛 |\n| stable error taxonomy | 早期基础 | 当前只有少量 Turn error code |\n| usage/compaction provenance | 未完成 | 只有 model usage 与 threshold metadata |\n| ID validation | 已完成 | constructor 与 deserialize 都拒绝空的 canonical ID |\n| public API discipline | 已完成 | private modules、named exports 与 speculative envelope 清理已落地 |\n\n## 10. 演进方案\n\n### 阶段 P0：冻结职责边界\n\n- 以本文作为 `zeta-protocol` crate 架构基线；\n- 新类型必须说明至少两个真实消费者；\n- review 阻止 execution、wire、storage 和 provider DTO 泄漏；\n- contract tests 固定 Session/Thread/Event/Update 的关键 serde shape。\n\n完成条件：新增 protocol 类型都能明确回答“谁拥有行为，谁只是共享语义”。\n\n### 阶段 P1：统一身份与公共 API（完成）\n\n- 产品 ID 已使用 validated construction/deserialization；\n- 同一工具概念的裸 `String` 已迁移到 `ToolName` / `ToolCallId`；\n- 没有真实消费者的 `ResponseItemId` 与 command envelope 已删除；\n- aggregate-first 模块、private modules 和 named exports 已落地，未预建空模块。\n\n完成条件：无效 ID 无法通过公开 constructor 或 deserialize 创建，公共 API 可被显式审计。\n\n### 阶段 P2：补齐异步 Turn 交互契约（完成）\n\n该阶段必须与 `zeta-agent`、Core 和 App Server 的 vertical slice 同步完成：\n\n- 已明确 owner 是 App Server ephemeral delivery state，deadline/cancel/disconnect 的 durable\n  boundary；\n- 已增加 `InteractionRequested` / `InteractionResolved` / `InteractionCancelled` facts 与\n  `TurnInteraction` snapshot；\n- 已让 waiting Turn status 可由 durable event 重建，recovery 保留仍可行动的 wait；\n- 已接通带 `RequestId` 的 `session/request::ResolveInteraction`；\n- App Server 已把 request、initialize capability、owner selection、主动 delivery、deadline 与\n  disconnect re-selection 组成真实的 Server → Client vertical slice；\n- 保证 Agent request 与 Thread command 不混为一类。\n\n完成条件已满足：进程等待响应时可恢复明确 waiting 状态；full request 只投递给 selected owner；\n断连可重选；deadline 形成 durable cancellation + stable Turn failure。\n\n### 阶段 P3：收敛模型/工具契约\n\n- Context builder 只输出一套 canonical `ModelRequest`；\n- provider adapter 不读取 ThreadItem 或 Core snapshot；\n- tool definition/call/result 的 name、ID 和 arguments 表示统一；\n- 明确 parallel tool call、refusal、reasoning 和 partial output 语义；\n- 加入跨 provider contract fixtures。\n\n完成条件：同一 canonical request 可通过不同 provider adapter，结果可无损回到 Agent loop\n需要的共享语义。\n\n### 阶段 P4：补齐流式处理、usage 与压缩来源\n\n- 根据真实 streaming producer 增加最小必要 delta；\n- 明确 transient update 的 stream cursor 和重连降级；\n- 评审 usage 是否属于 durable product fact、diagnostic trace 或 billing projection；\n- compaction checkpoint 必须引用其覆盖的原始历史范围；\n- 不在 protocol 中实现 token policy 或 summary 算法。\n\n完成条件：客户端丢失所有 transient update 后仍能从 snapshot + durable gap 恢复，compaction\n不会破坏原始历史可追溯性。\n\n### 阶段 P5：发布后的版本政策\n\n当前开发阶段直接修改 canonical 类型并同步所有调用方，不保留 deprecated alias 或双写。\n\n真正发布并需要读取用户长期数据后，再引入：\n\n- 明确的 semantic compatibility policy；\n- stored-event schema migration；\n- App Server wire version negotiation；\n- trace/export format version；\n- breaking change 的离线迁移工具。\n\n这些版本机制分别属于拥有其格式的 crate，不能全部塞入 `zeta-protocol`。\n\n## 11. 变更规则\n\n新增或修改一个 protocol 类型时，必须同时检查：\n\n1. 它是 command、event、update、request/response，还是 readable model；\n2. authority 属于 Session、Thread、执行控制、connection 还是 provider；\n3. 是否 durable；如果 durable，是否足够重建 projection；\n4. 是否需要 aggregate sequence、stream cursor 或独立 ID；\n5. 是否包含 provider、transport、storage 或执行策略细节；\n6. 至少两个真实消费者是谁；\n7. Rust serde、JSON Schema、TypeScript 和 App Server DTO 是否需要同步；\n8. 是否需要更新 reducer、store validator、fixtures 和客户端 projection。\n\n禁止：\n\n- 为未来可能需要而提前增加没有调用方的 enum variant；\n- 使用 `String`、`bool`、含义模糊的 `Option` 代替已有强类型语义；\n- 把 request、update 或 provider output 直接追加为 durable event；\n- 让客户端根据文本、日志或 variant 名推断未声明状态；\n- 用 schema derive 代替 Rust constructor/deserialize validation；\n- 为兼容开发期旧代码保留 alias、旧 route 对应类型或隐式转换。\n\n## 12. 验证门\n\n每次修改 `zeta-protocol` 至少执行：\n\n```bash\ncargo fmt --manifest-path Cargo.toml --all -- --check\ncargo clippy --manifest-path Cargo.toml -p zeta-protocol --all-targets -- -D warnings\ncargo test --manifest-path Cargo.toml -p zeta-protocol\ncargo test --manifest-path Cargo.toml -p zeta-app-server-protocol\n```\n\n如果改动进入 App Server external contract，还必须：\n\n```bash\ncargo run --manifest-path Cargo.toml \\\n  -p zeta-app-server-protocol --bin write_schema_fixtures\nnode desktop/scripts/sync-app-server-protocol.mjs\ncorepack pnpm --dir desktop run typecheck:renderer\n```\n\n关键 contract tests 必须覆盖：\n\n- canonical hierarchy 与 fork lineage；\n- durable event 不混入 transient wrapper；\n- Session/Thread sequence 独立；\n- durable sequence 与 stream cursor 独立；\n- ID 的空值和非法值；\n- Tool/Model canonical value 的 provider round-trip；\n- Agent request correlation 与 response mismatch；\n- generated schema/TypeScript fixture 一致性。\n\n## 13. 验收标准\n\n`zeta-protocol` 达到长期稳定状态时，应满足：\n\n- 所有跨组件共享概念只有一个 canonical 名称和表示；\n- Core、store、provider 和 App Server wire 的行为细节均未泄漏；\n- durable fact 可以完整重建产品 projection；\n- transient update 可全部丢失而不损坏权威状态；\n- Session、Thread、执行实例和 request 的身份/顺序边界不会混淆；\n- 所有公开 ID 和 value object 在构造与反序列化时保持不变量；\n- bidirectional request 在 response、timeout、cancel、disconnect 和恢复上有完整契约；\n- provider adapter 只做 canonical model 与 wire 的机械转换；\n- public exports、serde shape、schema 与 fixtures 都可被自动验证；\n- 不存在无调用方的 speculative contract 或开发期兼容层。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-所有权与依赖方向",
        "title": "2. 所有权与依赖方向"
      },
      {
        "depth": 2,
        "id": "3-canonical-产品模型",
        "title": "3. Canonical 产品模型"
      },
      {
        "depth": 3,
        "id": "31-session",
        "title": "3.1 Session"
      },
      {
        "depth": 3,
        "id": "32-thread",
        "title": "3.2 Thread"
      },
      {
        "depth": 3,
        "id": "33-turn",
        "title": "3.3 Turn"
      },
      {
        "depth": 3,
        "id": "34-threaditem",
        "title": "3.4 ThreadItem"
      },
      {
        "depth": 2,
        "id": "4-四类契约必须分离",
        "title": "4. 四类契约必须分离"
      },
      {
        "depth": 3,
        "id": "41-command请求改变状态",
        "title": "4.1 Command：请求改变状态"
      },
      {
        "depth": 3,
        "id": "42-event已经-durable-的事实",
        "title": "4.2 Event：已经 durable 的事实"
      },
      {
        "depth": 3,
        "id": "43-更新面向消费者的变化",
        "title": "4.3 更新：面向消费者的变化"
      },
      {
        "depth": 3,
        "id": "44-agent-请求响应turn-中的双向等待",
        "title": "4.4 Agent 请求/响应：Turn 中的双向等待"
      },
      {
        "depth": 2,
        "id": "5-sequencecursor-与-id",
        "title": "5. Sequence、Cursor 与 ID"
      },
      {
        "depth": 3,
        "id": "51-id-回答是谁",
        "title": "5.1 ID 回答“是谁”"
      },
      {
        "depth": 3,
        "id": "52-sequence-回答第几个状态版本",
        "title": "5.2 Sequence 回答“第几个状态版本”"
      },
      {
        "depth": 3,
        "id": "53-当前校验不一致",
        "title": "5.3 当前校验不一致"
      },
      {
        "depth": 2,
        "id": "6-供应商无关的模型契约",
        "title": "6. 供应商无关的模型契约"
      },
      {
        "depth": 2,
        "id": "7-config-值的边界",
        "title": "7. Config 值的边界"
      },
      {
        "depth": 2,
        "id": "8-目录结构与公开-api",
        "title": "8. 目录结构与公开 API"
      },
      {
        "depth": 3,
        "id": "81-当前已实现结构",
        "title": "8.1 当前已实现结构"
      },
      {
        "depth": 3,
        "id": "82-长期目标结构",
        "title": "8.2 长期目标结构"
      },
      {
        "depth": 3,
        "id": "83-已完成的文件迁移",
        "title": "8.3 已完成的文件迁移"
      },
      {
        "depth": 2,
        "id": "9-当前完成度",
        "title": "9. 当前完成度"
      },
      {
        "depth": 2,
        "id": "10-演进方案",
        "title": "10. 演进方案"
      },
      {
        "depth": 3,
        "id": "阶段-p0冻结职责边界",
        "title": "阶段 P0：冻结职责边界"
      },
      {
        "depth": 3,
        "id": "阶段-p1统一身份与公共-api完成",
        "title": "阶段 P1：统一身份与公共 API（完成）"
      },
      {
        "depth": 3,
        "id": "阶段-p2补齐异步-turn-交互契约完成",
        "title": "阶段 P2：补齐异步 Turn 交互契约（完成）"
      },
      {
        "depth": 3,
        "id": "阶段-p3收敛模型工具契约",
        "title": "阶段 P3：收敛模型/工具契约"
      },
      {
        "depth": 3,
        "id": "阶段-p4补齐流式处理usage-与压缩来源",
        "title": "阶段 P4：补齐流式处理、usage 与压缩来源"
      },
      {
        "depth": 3,
        "id": "阶段-p5发布后的版本政策",
        "title": "阶段 P5：发布后的版本政策"
      },
      {
        "depth": 2,
        "id": "11-变更规则",
        "title": "11. 变更规则"
      },
      {
        "depth": 2,
        "id": "12-验证门",
        "title": "12. 验证门"
      },
      {
        "depth": 2,
        "id": "13-验收标准",
        "title": "13. 验收标准"
      }
    ],
    "searchText": " 物理位置：zeta-rs/protocol/ 当前状态：Session-first canonical contract 与 Agent interaction delivery/deadline 已落地 Crate 类型、helper 与同步修改关系：zeta-rs/protocol/README.md 上层接口：Zeta App Server API 执行计划：Zeta Agent 执行架构与演进方案 文档所有权：本文件拥有跨组件语义、当前审计与演进方向；crate README 拥有当前源码接口、 serialization invariant 与修改路径。 快速理解 协议层定义 Zeta 各组件共同使用的对象、意图、事实和更新；它让不同进程说同一种语言，但不执行 业务逻辑，也不选择存储或传输。 概念 回答的问题 不包含 --- --- --- Session、Thread、Turn、ThreadItem 系统中的产品对象是什么？ actor、锁、任务和数据库 Command 调用方想改变什么？ 实际状态迁移和副作用 Event 哪个事实已经可靠提交？ 文件格式和写盘策略 Update 消费方应该观察到什么变化？ UI 呈现和重连策略 请求与响应 Turn 正在等待什么外部输入？ 批准界面和工具实现 稳定 ID、序列与游标 对象是谁、状态到第几版、从哪里继续？ 传输 request ID 和连接状态 1. 结论 zeta-protocol 是 Zeta 跨 crate、跨进程和跨客户端共享的语义词汇表。它定义“系统中的对象、 意图、事实和更新是什么”，但不决定“由谁、何时、通过什么 I/O 执行”。 它的长期职责固定为： - canonical Session → Thread → Turn → ThreadItem 产品模型； - 稳定 ID 与共享 value object； - SessionCommand / ThreadCommand 产品意图； - SessionEvent / ThreadEvent durable fact； - SessionUpdate / ThreadUpdate consumer-facing update； - provider-independent model、tool、input、request/response 和稳定错误值。 它不得拥有： - actor、mailbox、channel、task、锁和进程内执行实例； - reducer、状态迁移策略、重试、超时、取消执行和恢复流程； - 文件、数据库、JSONL、rollout framing、checksum 和 schema migration； - JSON-RPC method、request ID、connection state 和 transport； - provider HTTP payload、endpoint、credential 和 client； - tool execution、sandbox、approval policy 和副作用。 一句话判断边界： 至少两个独立组件需要以相同语义理解的纯数据契约，可以进入 protocol；只服务于一个执行层 或带有 I/O、策略和生命周期行为的类型，不进入 protocol。 2. 所有权与依赖方向 zeta-protocol 不依赖任何 Zeta 内部 crate，目前只依赖序列化和 schema 生成库： ``text serde / serdejson / schemars / ts-rs │ ▼ zeta-protocol ▲ ┌───────────┼────────────┬──────────────┐ │ │ │ │ zeta-core history/store zeta-api app-server-protocol ▲ ▲ ▲ │ │ │ agent/execution providers clients ` 各消费者的准确职责： 消费者 使用 protocol 的方式 不得反向放入 protocol --- --- --- zeta-core reducer 输入、projection、execution intent actor message、policy、effect、recovery zeta-session-store 持久化 SessionEvent sequence、timestamp、receipt envelope zeta-history 为 ThreadEvent 定义 persisted record envelope Store、SQLite、reducer、UI history zeta-thread-store 完整读取和追加 zetahistory::StoredEvent record serde shape、SQLite、reducer、UI Turn page zeta-api canonical model value 与 provider wire 互转 HTTP DTO、header、endpoint zeta-model-provider model catalog 与 invocation request/response transport、credential、retry zeta-app-server-protocol 机械复用 canonical view/update JSON-RPC params、result、method registry Desktop / CLI / TUI 通过生成契约或 client 消费 snapshot/update 客户端权威状态机 禁止出现： `text zeta-protocol → zeta-core zeta-protocol → zeta-storage zeta-protocol → zeta-app-server-protocol zeta-protocol → provider implementation ` 3. Canonical 产品模型 唯一产品层级是： `text Session └─ Thread └─ Turn └─ ThreadItem ` 3.1 Session Session 是任务与 Thread 拓扑的 readable model，拥有： - task title 和 lifecycle； - Thread membership； - root/fork lineage； - Session 自己的 durable sequence。 Session 不嵌入 Thread 的 Turn/Item 历史。ThreadOrigin::Fork 保存 parentThreadId + parentSequence，表达不可变的分支历史锚点。 当前 canonical ThreadOrigin 包含 Root、Fork、Rewind 与 AgentSpawn。Agent spawn 使用 独立 DelegationId、不可变 AgentContextSeed 和跨 Thread message/result events；不复用 Fork。当前实现边界与尚未完成的 join/cancellation 语义见 core-multi-agent.md。 长期若 Session shared defaults 需要被多个客户端读取、修改并持久恢复，应以明确的 SessionSettings value 和 typed event 进入本契约；当前代码尚未形成该闭环，不能把 Core 配置快照直接塞进 Session。 3.2 Thread Thread 是一个独立执行分支，拥有： - immutable SessionId； - Thread title 和 lifecycle； - ordered Turns； - Thread 自己的 durable sequence。 Session sequence 与 Thread sequence 是两个 aggregate 的 revision，不是 ID，也不是同一条 全局计数器。 3.3 Turn Turn 表示一次用户意图驱动的 Agent 执行，不等同于一条聊天消息。当前状态词汇已经包含： - Created、Running； - WaitingForApproval、WaitingForUserInput、WaitingForCapability； - Cancelling； - Completed、Failed、Interrupted。 其中 WaitingForUserInput 与 WaitingForCapability 已有 durable interaction event、Core reducer 和 recovery 支持；它们不能再被解释成单纯预留 enum。App Server 已按 initialize capability + Session-owned Thread subscription 选择 ephemeral owner，通过 agent/request 主动投递，并执行 按 interaction 语义决定的断连处理和 durable deadline cancellation；这些 runtime state 仍不属于 canonical Thread。 3.4 ThreadItem ThreadItem 是 Turn transcript 的 durable 业务单元，当前包含： - User Message； - User Image； - Agent Message； - Reasoning； - Plan； - Tool Call； - Tool Result。 Item 必须拥有稳定 ItemId 并归属一个 TurnId。Token delta 不是 durable Item；最终完成的 Item 才能进入 authoritative Thread history。 4. 四类契约必须分离 4.1 Command：请求改变状态 `text SessionCommand Create / CreateThread / ForkThread / ArchiveThread / Complete / Archive ThreadCommand StartTurn / InterruptTurn / ResolveUserInput / ResolveDynamicTool ` Command 表达产品意图，不代表已经发生，也不能直接写入 projection。 CommandId 用于 retry-safe typed identity。expectedSequence 用于 optimistic concurrency。JSON-RPC request ID 不得替代 CommandId。 前端关闭 Tab 不会扩展这组 canonical command。它是 App Server 的产品动作：外部通过 session/request 的 Stop operation 映射到 Core 的内部停止编排，持久化既有的 Session archive fact，并协调 中断其 child Thread 中的活动 Turn。这样 UI 的 Tab/session 语义不会泄漏进 zeta-protocol；连接 断开也不会被解释成产品 Session 停止。 所有 durable side effect 的 receipt 必须保存： `text CommandId + exact typed command payload + response sequence / stable result ` 重放规则固定为： - 相同 CommandId、相同 typed payload：返回原结果； - 相同 CommandId、不同 typed payload：返回 CommandConflict； - 首次执行：校验 expectedSequence，并将 receipt 与首个业务 Event 原子提交。 重放时先匹配 receipt，再返回稳定结果，因此响应丢失后的安全重试不受 aggregate 后续 sequence 前进影响。不得用 JSON-RPC method + serialized params、sidecar idempotency ledger 或裸 request ID 复制这套语义。 SessionCommandEnvelope、ThreadCommandEnvelope 曾经只在 protocol 内定义，没有第二个 执行层或 transport 消费者，已删除。Core command receipt 与 App Server params 各自属于其 拥有的执行/transport 层；将来只有确有至少两个消费者的共享 command wrapper 才能重新进入 protocol。 4.2 Event：已经 durable 的事实 SessionEvent 与 ThreadEvent 是 reducer 和 store 唯一接受的领域事实。 `text Command → Core validate/policy → build Event → Store append → reducer → publish committed Update ` Event 不携带 storage sequence、timestamp、schema version、event ID 或 command receipt。 Session stored envelope 由 zeta-session-store 定义；Thread persisted record 由 zeta-history 定义。Core 构造 record，Store 只校验并提交。 必须保持： - Event 使用过去式事实命名； - Event 足以从空状态重建 canonical projection； - transient delta、delivery ack、RPC request 和 provider response 不进入 Event； - reducer policy 不编码进 Event helper。 委托执行有两个 durable fact。ThreadEvent::TurnExecutionAttempted 在 backend 跨越外部副作用边界 前写入 Turn；恢复时已有 attempt 的 in-flight Turn 结果未知，禁止重放。成功完成后， ThreadEvent::TurnExecutionBound 将 Thread 不可变地绑定到 backend, remoteThreadId, executionScope ，并投影为 ThreadSnapshot.turnexecutionbinding。它不保存 access token、远端 Turn ID、pending request 或 stream cursor；execution scope 是不含路径和凭据的 opaque Workspace authority identity，同一 Thread 不能跨 scope 或改绑到另一个 backend/remote thread。 4.3 更新：面向消费者的变化 SessionUpdate / ThreadUpdate 服务于 UI、CLI、TUI 和订阅客户端。 Committed event 携带 durable fact；ItemStarted、ItemDelta、PlanUpdated 是可丢失的 低延迟更新。客户端必须能仅依赖 snapshot + durable committed gap 恢复正确状态。 `text durableSequence = aggregate 已提交到第几个事实 StreamCursor streamInstanceId, sequence = 某次 transient stream emitter incarnation 中瞬态消息的位置 ` 两者不能合并。进程重启后 stream cursor 可以失效，durable sequence 不能失效。 当前瞬态 update、Core streaming model loop，以及 OpenAI Responses、OpenAI-compatible Chat Completions、Anthropic Messages 的生产 HTTP/SSE 路径已经贯通。三种 endpoint 都使用各自原生 wire stream；其他 SSE profile、NDJSON 与 WebSocket 仍需按真实协议逐项接入。 4.4 Agent 请求/响应：Turn 中的双向等待 TurnInteraction 是一个 Turn 当前未完成交互的 durable value，拥有 RequestId、可选 ItemId、 typed AgentRequest 与可选绝对 deadline。Readable Turn 只公开不含 payload 的 PendingInteraction（request ID、kind、optional item/deadline），避免普通 Thread read 被误用为 delivery channel。AgentRequestEnvelope 在完整 TurnInteraction 基础上补齐 SessionId + ThreadId + TurnId，供 App Server 构造 delivery；AgentResponseEnvelope 只做 response correlation。 它们用于关联： - structured user input； - dynamic tool call； - 将来明确接受的 capability interaction。 它们不是 ThreadCommand 或 JSON-RPC envelope。对应的 durable Thread facts 是： `text InteractionRequested interaction InteractionResolved requestId, response InteractionCancelled requestId, reason ` InteractionRequested 将 Running Turn 转为对应 waiting status，resolved/cancelled 关闭同一个 pending interaction并返回 Running。App Server 的 canonical SessionRequest::ResolveInteraction 是 client 发起的 retry-safe aggregate command，必须带 exact RequestId；它不是 Agent request 自身。 完整生命周期必须明确： `text request fact committed → owner connection selected App Server ephemeral state → delivered → response / timeout / cancellation / disconnect → durable Turn transition ` 这里的 owner connection 不能进入 ThreadEvent、TurnInteraction 或 canonical Thread snapshot： connection ID 在断开后失效，不是产品身份。App Server 必须维护短暂的 RequestId → connection delivery assignment；断开时，尚未产生外部副作用的 approval/user-input 可以重新选择并重投递， 已经投递的 dynamic tool 必须追加 InteractionCancelled reason: OwnerDisconnected 并按 unknown outcome 收口，不能交给另一连接重试。任何分支都不得让 durable Turn 永远停在 waiting。 deadline 同样是 durable absolute instant，但 timer 和超时 policy 属于执行控制层。 当前已完成 canonical contract、Core reducer/recovery、session/request::ResolveInteraction、 initialize capability、connection owner selection、agent/request 主动 delivery、按 interaction 语义处理 owner 断连与 deadline timer。具体 host 只声明它实际支持的 interaction kind；对于 dynamic tool，还必须在 dynamicTools 中声明它实际承载的 exact tool name，仅声明 DynamicTool kind 不会获得其他动态工具的执行权。 5. Sequence、Cursor 与 ID 5.1 ID 回答“是谁” 当前主要 ID： ID 身份范围 --- --- SessionId 一个产品任务 ThreadId 一个独立执行分支 TurnId 一次 Agent 执行 ItemId 一个 transcript item ToolCallId 一次工具调用 CommandId 一个 retry-safe command RequestId 一个双向 Agent request StreamInstanceId 一次 transient stream emitter incarnation 5.2 Sequence 回答“第几个状态版本” - Session sequence 只排序 membership、lineage 和 Session lifecycle； - Thread sequence 只排序 Turn、Item 和执行终态； - stream cursor 只排序同一 stream incarnation 的 transient update； - fork parent sequence 是历史锚点。 任何 ID 都不能由 sequence 推导，任何 sequence 也不能充当 ID。 Session 与 Thread 使用独立 sequence，是因为它们是不同的冲突域： Sequence 排序内容 并发边界 --- --- --- Session membership、fork lineage、Session lifecycle 同一 Session 的拓扑修改 Thread Turn、Item、工具与执行终态 同一 Thread 的执行修改 如果合成一条 sequence，子 Thread 的执行会无意义地争用 Session revision，多分支并行会产生 虚假的 optimistic-concurrency 冲突，单个 Thread 的恢复也会被迫读取整个 Session 的历史。 逻辑 sequence 独立不代表物理实现重复；不同 aggregate 可以复用同一个 event-stream engine， 只保留各自的 typed validation、reducer 和 writer scope。 5.3 当前校验不一致 SessionId、ThreadId、TurnId、ItemId、CommandId、RequestId、StreamInstanceId、 ToolCallId、ProviderId、ModelId 与 ToolName 都使用 fallible constructor 与 validated deserialize。JSON Schema 的最小长度只是外部提示，不能替代 Rust 侧校验。 长期需要统一： - 外部输入反序列化必须拒绝空 ID； - 内部 ID constructor 不应制造无效值； - ID 格式只规定必要约束，不把当前生成算法写入 protocol； - 不使用裸 String 表达已经拥有专用 ID/newtype 的概念。 6. 供应商无关的模型契约 model/invocation.rs 定义 model invocation 的 canonical value： - ModelRequest； - input message / content / tool result； - tool definition / tool choice； - reasoning config； - ModelResponse / response item； - token usage / stop reason。 zeta-api 负责把它转换为 OpenAI、Anthropic、Qwen 等 provider wire。Provider 特有字段不能 反向污染 canonical model。 model/catalog.rs 定义 model catalog metadata： - provider/model identity； - context window； - tools、reasoning、parallel tool call、personality capability； - reasoning effort； - auto-compaction threshold metadata； - model availability、catalog freshness、lifecycle 与 metadata quality 的跨 crate 值。 目录 cache、scope、refresh、generation、字段 provenance 和 typed resolution 属于 zeta-models-manager，不进入纯 protocol value 层。完整 snapshot wire wrapper 尚未落地；当前 App Server 仍投影精简 model/list DTO。 这里的 compaction 仅是声明信息。Context builder、token accounting policy、summary checkpoint 和 compaction 执行都不属于 protocol。 当前需要继续收敛的重复表示： 概念 当前表示 长期处理 --- --- --- Tool name ToolName 已用于 durable Item、dynamic tool 和 model contract Tool arguments String、serdejson::Value 并存 按 durable canonical 与 provider value 分层 Tool call ID ToolCallId 已用于 durable Item、dynamic tool 和 model contract Model response provenance 无真实消费者 已删除 speculative ResponseItemId 与 provider raw response ID；有可证明的跨 provider 需求后再引入 Usage model response 有 usage，Thread history 未建模 先明确 billing/diagnostic/product ownership 7. Config 值的边界 Theme、Personality、ApprovalMode、SandboxMode、WebSearchMode 可以留在 protocol， 前提是它们确实被多个组件以同一语义使用。 以下内容属于 zeta-config，不能进入 protocol： - 文件位置与读取； - defaults、layer merge 和 precedence； - provider registry resolution； - credential reference resolution； - 更新事务与 authority snapshot。 Patch<T 只表达 missing/null/value 三态。字段是否允许 clear、如何持久化、冲突如何处理， 仍由拥有该配置的"
  },
  {
    "slug": "zeta-client",
    "sourcePath": "docs/zeta-client.md",
    "title": "模型调用操作层",
    "description": "zeta-client 是 Provider-neutral 的 API operation client。它把调用方已经构造完成的 request 通过共享 zeta-http-client 执行，并统一处理：",
    "group": "API 与协议",
    "markdown": "> - 物理位置：`zeta-rs/zeta-client/`\n> - Rust crate：`zeta_client`\n> - 层次：Zeta API operation client、retry 与 stream framing\n> - 当前状态：typed unary request/response、安全 retry loop、增量 SSE framer 和 telemetry wrapper\n>   已实现；底层 HTTP port 与 production backend 已在 `zeta-http-client`\n> - 底层网络：[`zeta-http-client` README](../zeta-rs/http-client/README.md)\n> - API 协议层：[`zeta-api.md`](zeta-api.md)\n> - Provider runtime：[`model-provider.md`](model-provider.md)\n\n> 标准依据：\n> [WHATWG Server-sent events](https://html.spec.whatwg.org/dev/server-sent-events.html)、\n> [RFC 9110 HTTP Semantics](https://www.rfc-editor.org/rfc/rfc9110.html)、\n> [W3C Trace Context](https://www.w3.org/TR/trace-context/) 和\n> [OpenTelemetry HTTP semantic conventions](https://opentelemetry.io/docs/specs/semconv/http/)。\n\n## 快速理解\n\n`zeta-client` 是 Provider-neutral 的 API operation client。它把调用方已经构造完成的 request\n通过共享 `zeta-http-client` 执行，并统一处理：\n\n- operation retry safety、classifier、attempt loop、backoff、jitter 和 `Retry-After`；\n- 包含全部 attempt/backoff 的 overall operation deadline；\n- SSE 与 NDJSON framing；\n- wire/frame activity 与 idle policy 的组合；\n- framed stream backpressure；\n- operation、attempt 和 stream telemetry。\n\n它不创建 HTTP backend，也不拥有 proxy、TLS、证书、redirect、连接池或 transport logging。\n这些能力统一属于 [`zeta-http-client`](../zeta-rs/http-client/README.md)，本文件不重复其规则。\n\n它也不知道 OpenAI、ChatGPT、Codex、Anthropic 或任何 `ModelRequest`。Provider JSON、event 和\nerror 语义属于 `zeta-api`。\n\n```text\nmodel-provider\n  ├── resolve target/auth/retry safety\n  ├──▶ zeta-api encode\n  └──▶ zeta-client operation\n          ├── retry/framing/operation telemetry\n          └──▶ zeta-http-client\n                  └── proxy/TLS/redirect/timeout/pool/backend\n\nzeta-client 负责“怎样执行并组织一个 Zeta API operation”\nzeta-api    负责“这个 API 的 bytes/event 表示什么”\n```\n\n当前实现仍直接导出 `HttpHeader`、`ClientRequest`/`ClientResponse`、`HttpClient` 和\n`UreqHttpClient`。这是新底层 crate 落地前的迁移状态，不是长期 ownership；现有\n`RetryPolicy`、SSE framer 和 operation telemetry 是保留在 `zeta-client` 的能力。\n\n| 调用中发生的事情 | 本层是否负责 | 说明 |\n| --- | --- | --- |\n| 按明确策略重试一次操作 | ✅ | 包含退避、抖动和总截止时间 |\n| 把字节流切成 SSE 或 NDJSON 记录 | ✅ | 不解释记录中的供应商语义 |\n| 选择模型、服务地址或凭据 | ❌ | 由模型调用系统决定 |\n| 解释 OpenAI 或 Anthropic 事件 | ❌ | 由模型 API 协议层负责 |\n| 处理代理、TLS 和连接池 | ❌ | 由网络层负责 |\n\n## 2. 拥有与不拥有\n\n### 2.1 拥有\n\n- operation request context；\n- typed replay safety 和 retry policy；\n- retry classifier、attempt state machine、backoff、jitter 与 `Retry-After`；\n- operation overall deadline 和 attempt budget 分配；\n- WHATWG SSE framing；\n- NDJSON record framing；\n- UTF-8、CRLF/LF、chunk boundary 和 frame/record hard limit；\n- byte/frame activity timestamp；\n- framed stream bounded channel 和 backpressure；\n- attempt、retry、framing 与 stream operation telemetry；\n- transport facts 到 operation error 的安全映射。\n\n### 2.2 不拥有\n\n- HTTP backend、DNS/TCP、proxy、TLS、证书、redirect 或连接池；\n- transport attempt timeout、raw response body limit 或 HTTP redaction；\n- Provider registry、Provider ID 或模型选择；\n- base URL 默认值和 credential lookup；\n- OAuth login、token persistence、refresh 或 revoke；\n- API relative endpoint path；\n- Provider request/response JSON；\n- `data:` JSON 的 schema；\n- Anthropic `ping`、OpenAI terminal event 或 `[DONE]` 的语义；\n- prompt cache 参数；\n- tool/reasoning/usage/stop reason 归一化；\n- 是否 fallback model/provider；\n- catalog refresh/merge；\n- Agent Turn、Thread 或 durable state。\n\n## 3. 与上下层的接口\n\n`zeta-api` 构造 protocol request 并消费 operation result；`zeta-client` 只包装 operation policy：\n\n```text\nzeta-api::endpoint + zeta-api::requests\n        │ encoded request + response decoder\n        ▼\nzeta-client\n        │ HttpRequest\n        ▼\nzeta-http-client\n        │ HttpResponse | raw byte stream\n        ▼\nzeta-client retry/framing\n        │ response facts | SseFrame | NdjsonRecord\n        ▼\nzeta-api decoder\n        │\n        ▼\ncanonical ModelResponse | ModelStreamEvent\n```\n\n目标 public shape 可以是：\n\n```rust\npub struct ClientOperation {\n    pub request: zeta_http_client::HttpRequest,\n    pub retry: RetryPolicy,\n    pub deadline: OperationDeadline,\n    pub telemetry: OperationTelemetry,\n}\n\npub struct ClientOperationResponse {\n    pub response: zeta_http_client::HttpResponse,\n    pub attempts: AttemptSummary,\n    pub timing: OperationTiming,\n}\n```\n\n示例名称不要求逐字实现，但 `zeta-client` 不复制 URL/header/body、proxy、TLS 或 pool 类型，也不能\n直接返回 `serde_json::Value`。JSON decoding 是 API 协议职责。\n\n普通 OAuth exchange、catalog discovery、Plugin/MCP request 等不需要 operation retry/framing 的\n调用方可以直接使用 `zeta-http-client`，不强制经过本 crate。\n\n## 4. 重试\n\n### 4.1 机制和策略分开\n\n`zeta-client` 拥有 retry 机制，但不猜测 operation 是否可以重放：\n\n```text\ncaller/API/runtime\n  提供 RetryPolicy + RetryClassifier\n        ↓\nzeta-client\n  分配 attempt budget\n        ↓\nzeta-http-client execute one attempt\n        ↓\nzeta-client classify → wait → next attempt\n```\n\n建议 typed safety：\n\n```rust\npub enum RetrySafety {\n    Never,\n    Idempotent,\n    ExplicitIdempotencyKey,\n}\n\npub struct RetryPolicy {\n    pub safety: RetrySafety,\n    pub max_attempts: NonZeroU8,\n    pub backoff: BackoffPolicy,\n    pub retry_after: RetryAfterPolicy,\n}\n```\n\n禁止使用 `retry: bool`。调用点必须清楚表达为什么允许重放。\n\n认证恢复不属于普通 retry loop。收到 `401 Unauthorized` 时，client 返回 response facts；\n`zeta-model-provider` 只可按 direct-provider credential 的正式语义执行一次受限 rebuild/refresh。\nclient 不读取 `zeta-secrets`、不持有 refresh token，也不根据 401 自行重试。ChatGPT/Codex\nsubscription refresh 归 upstream Codex App Server，Zeta 只接收稳定的 reauthentication outcome。\n\n`zeta-client` 不提供带登录状态的 `OAuthClient` facade。未来一个 officially supported OAuth adapter\n需要普通 HTTP execution 时，它可使用 `zeta-http-client`，但其生命周期属于 `zeta-login` 和 exact\nprovider adapter，而不是 operation retry/framing layer。\n\nRFC 9110 指出，client 不应自动重试非幂等请求，除非它知道请求语义实际幂等或能确定原请求未被\n应用。因此：\n\n- inference POST 默认 `Never`；\n- catalog GET 可以由其调用方选择 `Idempotent` operation policy；\n- 显式 idempotency key 只有在 Provider 文档确认语义后才能启用；\n- client 不根据“未收到 response byte”猜测 POST 安全；\n- 一次 auth recovery 不能嵌套出第二套无限 retry；\n- retry budget 同时受 max attempts 和 operation deadline 限制。\n\n### 4.2 尝试分类器\n\nClassifier 可以使用：\n\n- `zeta-http-client` 返回的 transport failure phase；\n- 是否已发送完整 request body；\n- 是否已收到 response headers/body/frame；\n- HTTP status；\n- `Retry-After`；\n- 调用方从 bounded error body 得到的 typed classification；\n- 是否已经向消费者发布 semantic output。\n\nClient 不解析 Provider error JSON。需要 body-aware classification 时，`zeta-api` 提供受限的\nclassifier hook；hook 只能返回 retry decision 和低敏感 evidence，不把 raw body 写入 telemetry。\n\n### 4.3 退避\n\n- exponential backoff 必须 bounded；\n- jitter 必须可测试并可注入；\n- `Retry-After` 解析失败时使用本地 policy，不 panic；\n- 超出 overall operation deadline 时不启动新 attempt；\n- cancellation 立即中止 backoff 并传递给活跃 transport attempt；\n- 测试使用 fake clock，不真实 sleep。\n\n## 5. SSE 与 NDJSON\n\n### 5.1 SSE 分帧\n\n`zeta-client::stream::sse` 消费 `zeta-http-client` 的 raw byte stream，并按 WHATWG event stream\nformat 处理：\n\n- UTF-8；\n- CRLF、CR 和 LF；\n- `event`、`data`、`id`、`retry` field；\n- 多行 `data` 拼接；\n- comment line；\n- 空行 dispatch；\n- 跨任意 byte chunk boundary；\n- EOF 和尾部不完整 event；\n- frame/buffer hard limit。\n\nSSE 的 `retry:` field 可以作为 frame evidence 保留，但不能自动触发 inference stream 重连。\nEventSource 的自动重连语义不能直接套用到可能计费、可能已经产生副作用的模型 POST；只有具体 API\n文档定义 resumable stream，并由 runtime 显式选择策略时，client 才能重新连接。\n\n输出是 Provider-neutral frame：\n\n```rust\npub struct SseEvent {\n    pub event: Option<String>,\n    pub data: String,\n    pub id: Option<String>,\n}\n\npub enum SseFrame {\n    Event(SseEvent),\n    Comment,\n}\n```\n\n具体 API decoder 决定：\n\n- `event: ping` 是否只是 liveness；\n- `data: [DONE]` 是否终止；\n- `response.output_text.delta` 如何映射；\n- 未知 event 是否可忽略；\n- 何时形成 canonical completed。\n\n### 5.2 NDJSON 分帧\n\nNDJSON framer 只输出 bounded JSON record bytes，不解析 Ollama `done` 或 `error`：\n\n```rust\npub struct NdjsonRecord {\n    pub bytes: BoundedBytes,\n}\n```\n\n空行、尾部不完整 JSON、超限 record 和 invalid UTF-8 形成 client framing error；record 中字段的\n语义错误由 `zeta-api` 返回。\n\n### 5.3 活性\n\n`zeta-http-client` 维护 raw wire activity 和 transport idle timeout；`zeta-client` 维护：\n\n- `last_frame_activity`；\n- first frame time；\n- operation 是否已经发布 frame。\n\nClient 不维护“模型是否有语义进展”。Anthropic `ping`、comment heartbeat 和长 reasoning 都可能\n维持连接但不产生 text。`last_semantic_progress` 由 API/runtime 在解码后维护。\n\n## 6. 操作 deadline、取消与背压\n\nDeadline 分层：\n\n| Owner | Deadline |\n| --- | --- |\n| `zeta-http-client` | DNS/connect/TLS/first-byte/idle/single-attempt |\n| `zeta-client` | 包含 retry/backoff 的 overall operation |\n| runtime | Turn、auth recovery 或产品流程 deadline |\n\nOperation client 在每次 attempt 前计算 remaining budget，并把 bounded attempt deadline 交给\n`zeta-http-client`。任一上层 deadline 结束后不能启动新 attempt。\n\nunary 与 streaming 路径的 cancellation 都从 runtime 贯穿 operation preflight、活跃 attempt 的\n本地等待和 retry timer。streaming attempt 使用有界 channel 把 raw chunks 交给调用线程，取消或\nconsumer failure 会断开接收端；已经发布任意 chunk 的 attempt 发生 transport failure 后绝不重放，\n避免重复 delta。取消返回独立 `ClientError::Cancelled`，不会包装成 retryable network failure，也\n不会启动下一次 attempt。由于 `zeta-http-client` 仍使用同步 `ureq`，已经进入 socket read 的 worker\n不能被 token 强制关闭；它由 bounded transport timeout 收束，迟到的 chunk 或 response 不再被接受。\n\n当前生产 wire streaming 已覆盖 OpenAI Responses、OpenAI-compatible Chat Completions 与 Anthropic\nMessages SSE。三种 endpoint 都使用原生 wire stream；其他 SSE profile、NDJSON 与 WebSocket 仍需按\n真实协议逐项接入。\n\nRaw stream 的 socket/buffer backpressure 属于 `zeta-http-client`；SSE/NDJSON framed channel 的\nbackpressure 属于 `zeta-client`。Client 不能合并或丢弃它不理解的 Provider payload。\n\n## 7. 遥测\n\nTelemetry 分为两层：\n\n| `zeta-http-client` | `zeta-client` |\n| --- | --- |\n| DNS/connect/TLS/HTTP timing | operation duration |\n| proxy/redirect/pool evidence | attempt count |\n| request/response byte count | retry reason/backoff |\n| status class/transport error | first frame/frame count |\n| transport timeout/cancel | framing/operation outcome |\n| HTTP redaction policy | low-cardinality API metadata |\n\n业务调用方只提供低基数 operation metadata，例如：\n\n```text\noperation.kind = model_inference | model_catalog\napi.profile = openai_responses | anthropic_messages\nprovider.kind = openai | anthropic | custom\n```\n\n禁止把 exact model ID、URL、header value、credential、prompt、tool arguments/output、reasoning、\nraw response body 或 stream payload 放进 operation log/label。底层完整规则以\n[`zeta-http-client` README](../zeta-rs/http-client/README.md#telemetry) 为准。\n\n## 8. 错误\n\n目标 operation error 只增加本层语义：\n\n```rust\npub enum ClientError {\n    Transport(zeta_http_client::HttpClientError),\n    OperationDeadlineExceeded,\n    Cancelled,\n    Framing(StreamFramingError),\n    RetryExhausted(RetryExhaustedError),\n}\n```\n\nProvider error 不是 `ClientError`，由 `zeta-api` 解码。Error 可以包含 attempt、phase 和 bounded\ntiming evidence，但不能复制 transport secret 或 raw body。\n\n## 9. 目标目录\n\n```text\nzeta-rs/zeta-client/\n├── BUILD.bazel\n├── Cargo.toml\n└── src/\n    ├── lib.rs\n    ├── operation/\n    │   ├── mod.rs\n    │   ├── request.rs\n    │   ├── response.rs\n    │   ├── deadline.rs\n    │   └── operation_tests.rs\n    ├── retry/\n    │   ├── mod.rs\n    │   ├── policy.rs\n    │   ├── classifier.rs\n    │   ├── backoff.rs\n    │   └── retry_tests.rs\n    ├── stream/\n    │   ├── mod.rs\n    │   ├── sse.rs\n    │   ├── ndjson.rs\n    │   ├── activity.rs\n    │   ├── limits.rs\n    │   ├── sse_tests.rs\n    │   └── ndjson_tests.rs\n    ├── telemetry/\n    │   ├── mod.rs\n    │   ├── operation.rs\n    │   ├── attempt.rs\n    │   ├── stream.rs\n    │   └── telemetry_tests.rs\n    └── error.rs\n```\n\nBackend、proxy、TLS、redirect、raw HTTP types 和 pool implementation 都不出现在该目录；它们属于\n`zeta-http-client`。\n\n## 10. 公共接口\n\n长期 public API 只导出：\n\n- `ZetaClient` 或等价 operation executor；\n- `ClientOperation` / `ClientOperationResponse`；\n- `SseFrame` / `SseEvent`；\n- `NdjsonRecord`；\n- `RetryPolicy` / `RetrySafety`；\n- operation deadline/cancellation value；\n- `ClientError` 和安全 attempt evidence；\n- 必要的 operation telemetry context。\n\nHTTP request/response/client/config/error 来自 `zeta-http-client`，不在本 crate 建立平行 value。\n新增 public trait 必须说明实现者对 retry、operation deadline、framing、redaction 和 backpressure\n的责任。\n\n## 11. 测试\n\n- retry max attempts、jitter、`Retry-After` 和 overall budget；\n- non-idempotent request 默认不 replay；\n- cancellation during backoff/attempt/stream；\n- transport failure phase 到 classifier 的映射；\n- 任意 byte fragmentation 与 UTF-8 split；\n- SSE multiline data、comment、CRLF/LF 和 EOF；\n- NDJSON blank line、oversized/incomplete record；\n- framed channel backpressure；\n- operation telemetry 低基数和 secret negative tests；\n- fake clock/fake `zeta-http-client`，不访问真实 Provider。\n\nProxy、TLS、redirect、HTTP timeout 和 pool tests 只属于 `zeta-http-client`，不在本 crate 重复。\n\n## 12. 迁移顺序\n\n1. 建立 `zeta-http-client` 的 raw HTTP value、client/config/error port。\n2. 将 `HttpHeader`、raw request/response、`HttpClient` 和 `UreqHttpClient` 从本 crate 迁出。\n3. 本 crate 改为依赖 `zeta-http-client` 并保留现有 unary behavior。\n4. 把 operation retry loop 改为执行一个或多个 bounded transport attempts。\n5. 增加 live raw stream execution 后接入现有 SSE framer。\n6. 增加 NDJSON framer、operation deadline 与 framed backpressure。\n7. 拆分 transport telemetry 与 operation telemetry。\n8. 更新 `zeta-api` 和 `model-provider`，删除旧 raw transport compatibility surface。\n\n当前处于开发阶段，迁移时直接更新全部调用方，不建立旧 `UreqHttpClient` public API 的长期兼容层。\n\n## 13. 固定决策\n\n1. `zeta-client` 是 API operation client，不是 workspace HTTP backend。\n2. `zeta-http-client` 独占 proxy、TLS、redirect、attempt timeout、pool 和 transport redaction。\n3. `zeta-client` 拥有 retry 机制，调用方提供 typed retry safety/policy。\n4. `zeta-client` 拥有 SSE/NDJSON framing，不解释 Provider event。\n5. Inference POST 默认不透明 retry。\n6. Wire activity、frame activity 与 semantic progress 分开。\n7. Client 不依赖 Provider registry、config、Core 或 App Server。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "2-拥有与不拥有",
        "title": "2. 拥有与不拥有"
      },
      {
        "depth": 3,
        "id": "21-拥有",
        "title": "2.1 拥有"
      },
      {
        "depth": 3,
        "id": "22-不拥有",
        "title": "2.2 不拥有"
      },
      {
        "depth": 2,
        "id": "3-与上下层的接口",
        "title": "3. 与上下层的接口"
      },
      {
        "depth": 2,
        "id": "4-重试",
        "title": "4. 重试"
      },
      {
        "depth": 3,
        "id": "41-机制和策略分开",
        "title": "4.1 机制和策略分开"
      },
      {
        "depth": 3,
        "id": "42-尝试分类器",
        "title": "4.2 尝试分类器"
      },
      {
        "depth": 3,
        "id": "43-退避",
        "title": "4.3 退避"
      },
      {
        "depth": 2,
        "id": "5-sse-与-ndjson",
        "title": "5. SSE 与 NDJSON"
      },
      {
        "depth": 3,
        "id": "51-sse-分帧",
        "title": "5.1 SSE 分帧"
      },
      {
        "depth": 3,
        "id": "52-ndjson-分帧",
        "title": "5.2 NDJSON 分帧"
      },
      {
        "depth": 3,
        "id": "53-活性",
        "title": "5.3 活性"
      },
      {
        "depth": 2,
        "id": "6-操作-deadline取消与背压",
        "title": "6. 操作 deadline、取消与背压"
      },
      {
        "depth": 2,
        "id": "7-遥测",
        "title": "7. 遥测"
      },
      {
        "depth": 2,
        "id": "8-错误",
        "title": "8. 错误"
      },
      {
        "depth": 2,
        "id": "9-目标目录",
        "title": "9. 目标目录"
      },
      {
        "depth": 2,
        "id": "10-公共接口",
        "title": "10. 公共接口"
      },
      {
        "depth": 2,
        "id": "11-测试",
        "title": "11. 测试"
      },
      {
        "depth": 2,
        "id": "12-迁移顺序",
        "title": "12. 迁移顺序"
      },
      {
        "depth": 2,
        "id": "13-固定决策",
        "title": "13. 固定决策"
      }
    ],
    "searchText": " - 物理位置：zeta-rs/zeta-client/ - Rust crate：zetaclient - 层次：Zeta API operation client、retry 与 stream framing - 当前状态：typed unary request/response、安全 retry loop、增量 SSE framer 和 telemetry wrapper 已实现；底层 HTTP port 与 production backend 已在 zeta-http-client - 底层网络：zeta-http-client README - API 协议层：zeta-api.md - Provider runtime：model-provider.md 标准依据： WHATWG Server-sent events、 RFC 9110 HTTP Semantics、 W3C Trace Context 和 OpenTelemetry HTTP semantic conventions。 快速理解 zeta-client 是 Provider-neutral 的 API operation client。它把调用方已经构造完成的 request 通过共享 zeta-http-client 执行，并统一处理： - operation retry safety、classifier、attempt loop、backoff、jitter 和 Retry-After； - 包含全部 attempt/backoff 的 overall operation deadline； - SSE 与 NDJSON framing； - wire/frame activity 与 idle policy 的组合； - framed stream backpressure； - operation、attempt 和 stream telemetry。 它不创建 HTTP backend，也不拥有 proxy、TLS、证书、redirect、连接池或 transport logging。 这些能力统一属于 zeta-http-client，本文件不重复其规则。 它也不知道 OpenAI、ChatGPT、Codex、Anthropic 或任何 ModelRequest。Provider JSON、event 和 error 语义属于 zeta-api。 ``text model-provider ├── resolve target/auth/retry safety ├──▶ zeta-api encode └──▶ zeta-client operation ├── retry/framing/operation telemetry └──▶ zeta-http-client └── proxy/TLS/redirect/timeout/pool/backend zeta-client 负责“怎样执行并组织一个 Zeta API operation” zeta-api 负责“这个 API 的 bytes/event 表示什么” ` 当前实现仍直接导出 HttpHeader、ClientRequest/ClientResponse、HttpClient 和 UreqHttpClient。这是新底层 crate 落地前的迁移状态，不是长期 ownership；现有 RetryPolicy、SSE framer 和 operation telemetry 是保留在 zeta-client 的能力。 调用中发生的事情 本层是否负责 说明 --- --- --- 按明确策略重试一次操作 ✅ 包含退避、抖动和总截止时间 把字节流切成 SSE 或 NDJSON 记录 ✅ 不解释记录中的供应商语义 选择模型、服务地址或凭据 ❌ 由模型调用系统决定 解释 OpenAI 或 Anthropic 事件 ❌ 由模型 API 协议层负责 处理代理、TLS 和连接池 ❌ 由网络层负责 2. 拥有与不拥有 2.1 拥有 - operation request context； - typed replay safety 和 retry policy； - retry classifier、attempt state machine、backoff、jitter 与 Retry-After； - operation overall deadline 和 attempt budget 分配； - WHATWG SSE framing； - NDJSON record framing； - UTF-8、CRLF/LF、chunk boundary 和 frame/record hard limit； - byte/frame activity timestamp； - framed stream bounded channel 和 backpressure； - attempt、retry、framing 与 stream operation telemetry； - transport facts 到 operation error 的安全映射。 2.2 不拥有 - HTTP backend、DNS/TCP、proxy、TLS、证书、redirect 或连接池； - transport attempt timeout、raw response body limit 或 HTTP redaction； - Provider registry、Provider ID 或模型选择； - base URL 默认值和 credential lookup； - OAuth login、token persistence、refresh 或 revoke； - API relative endpoint path； - Provider request/response JSON； - data: JSON 的 schema； - Anthropic ping、OpenAI terminal event 或 DONE 的语义； - prompt cache 参数； - tool/reasoning/usage/stop reason 归一化； - 是否 fallback model/provider； - catalog refresh/merge； - Agent Turn、Thread 或 durable state。 3. 与上下层的接口 zeta-api 构造 protocol request 并消费 operation result；zeta-client 只包装 operation policy： `text zeta-api::endpoint + zeta-api::requests │ encoded request + response decoder ▼ zeta-client │ HttpRequest ▼ zeta-http-client │ HttpResponse raw byte stream ▼ zeta-client retry/framing │ response facts SseFrame NdjsonRecord ▼ zeta-api decoder │ ▼ canonical ModelResponse ModelStreamEvent ` 目标 public shape 可以是： `rust pub struct ClientOperation pub request: zetahttpclient::HttpRequest, pub retry: RetryPolicy, pub deadline: OperationDeadline, pub telemetry: OperationTelemetry, pub struct ClientOperationResponse pub response: zetahttpclient::HttpResponse, pub attempts: AttemptSummary, pub timing: OperationTiming, ` 示例名称不要求逐字实现，但 zeta-client 不复制 URL/header/body、proxy、TLS 或 pool 类型，也不能 直接返回 serdejson::Value。JSON decoding 是 API 协议职责。 普通 OAuth exchange、catalog discovery、Plugin/MCP request 等不需要 operation retry/framing 的 调用方可以直接使用 zeta-http-client，不强制经过本 crate。 4. 重试 4.1 机制和策略分开 zeta-client 拥有 retry 机制，但不猜测 operation 是否可以重放： `text caller/API/runtime 提供 RetryPolicy + RetryClassifier ↓ zeta-client 分配 attempt budget ↓ zeta-http-client execute one attempt ↓ zeta-client classify → wait → next attempt ` 建议 typed safety： `rust pub enum RetrySafety Never, Idempotent, ExplicitIdempotencyKey, pub struct RetryPolicy pub safety: RetrySafety, pub maxattempts: NonZeroU8, pub backoff: BackoffPolicy, pub retryafter: RetryAfterPolicy, ` 禁止使用 retry: bool。调用点必须清楚表达为什么允许重放。 认证恢复不属于普通 retry loop。收到 401 Unauthorized 时，client 返回 response facts； zeta-model-provider 只可按 direct-provider credential 的正式语义执行一次受限 rebuild/refresh。 client 不读取 zeta-secrets、不持有 refresh token，也不根据 401 自行重试。ChatGPT/Codex subscription refresh 归 upstream Codex App Server，Zeta 只接收稳定的 reauthentication outcome。 zeta-client 不提供带登录状态的 OAuthClient facade。未来一个 officially supported OAuth adapter 需要普通 HTTP execution 时，它可使用 zeta-http-client，但其生命周期属于 zeta-login 和 exact provider adapter，而不是 operation retry/framing layer。 RFC 9110 指出，client 不应自动重试非幂等请求，除非它知道请求语义实际幂等或能确定原请求未被 应用。因此： - inference POST 默认 Never； - catalog GET 可以由其调用方选择 Idempotent operation policy； - 显式 idempotency key 只有在 Provider 文档确认语义后才能启用； - client 不根据“未收到 response byte”猜测 POST 安全； - 一次 auth recovery 不能嵌套出第二套无限 retry； - retry budget 同时受 max attempts 和 operation deadline 限制。 4.2 尝试分类器 Classifier 可以使用： - zeta-http-client 返回的 transport failure phase； - 是否已发送完整 request body； - 是否已收到 response headers/body/frame； - HTTP status； - Retry-After； - 调用方从 bounded error body 得到的 typed classification； - 是否已经向消费者发布 semantic output。 Client 不解析 Provider error JSON。需要 body-aware classification 时，zeta-api 提供受限的 classifier hook；hook 只能返回 retry decision 和低敏感 evidence，不把 raw body 写入 telemetry。 4.3 退避 - exponential backoff 必须 bounded； - jitter 必须可测试并可注入； - Retry-After 解析失败时使用本地 policy，不 panic； - 超出 overall operation deadline 时不启动新 attempt； - cancellation 立即中止 backoff 并传递给活跃 transport attempt； - 测试使用 fake clock，不真实 sleep。 5. SSE 与 NDJSON 5.1 SSE 分帧 zeta-client::stream::sse 消费 zeta-http-client 的 raw byte stream，并按 WHATWG event stream format 处理： - UTF-8； - CRLF、CR 和 LF； - event、data、id、retry field； - 多行 data 拼接； - comment line； - 空行 dispatch； - 跨任意 byte chunk boundary； - EOF 和尾部不完整 event； - frame/buffer hard limit。 SSE 的 retry: field 可以作为 frame evidence 保留，但不能自动触发 inference stream 重连。 EventSource 的自动重连语义不能直接套用到可能计费、可能已经产生副作用的模型 POST；只有具体 API 文档定义 resumable stream，并由 runtime 显式选择策略时，client 才能重新连接。 输出是 Provider-neutral frame： `rust pub struct SseEvent pub event: Option<String , pub data: String, pub id: Option<String , pub enum SseFrame Event SseEvent , Comment, ` 具体 API decoder 决定： - event: ping 是否只是 liveness； - data: DONE 是否终止； - response.outputtext.delta 如何映射； - 未知 event 是否可忽略； - 何时形成 canonical completed。 5.2 NDJSON 分帧 NDJSON framer 只输出 bounded JSON record bytes，不解析 Ollama done 或 error： `rust pub struct NdjsonRecord pub bytes: BoundedBytes, ` 空行、尾部不完整 JSON、超限 record 和 invalid UTF-8 形成 client framing error；record 中字段的 语义错误由 zeta-api 返回。 5.3 活性 zeta-http-client 维护 raw wire activity 和 transport idle timeout；zeta-client 维护： - lastframeactivity； - first frame time； - operation 是否已经发布 frame。 Client 不维护“模型是否有语义进展”。Anthropic ping、comment heartbeat 和长 reasoning 都可能 维持连接但不产生 text。lastsemanticprogress 由 API/runtime 在解码后维护。 6. 操作 deadline、取消与背压 Deadline 分层： Owner Deadline --- --- zeta-http-client DNS/connect/TLS/first-byte/idle/single-attempt zeta-client 包含 retry/backoff 的 overall operation runtime Turn、auth recovery 或产品流程 deadline Operation client 在每次 attempt 前计算 remaining budget，并把 bounded attempt deadline 交给 zeta-http-client。任一上层 deadline 结束后不能启动新 attempt。 unary 与 streaming 路径的 cancellation 都从 runtime 贯穿 operation preflight、活跃 attempt 的 本地等待和 retry timer。streaming attempt 使用有界 channel 把 raw chunks 交给调用线程，取消或 consumer failure 会断开接收端；已经发布任意 chunk 的 attempt 发生 transport failure 后绝不重放， 避免重复 delta。取消返回独立 ClientError::Cancelled，不会包装成 retryable network failure，也 不会启动下一次 attempt。由于 zeta-http-client 仍使用同步 ureq，已经进入 socket read 的 worker 不能被 token 强制关闭；它由 bounded transport timeout 收束，迟到的 chunk 或 response 不再被接受。 当前生产 wire streaming 已覆盖 OpenAI Responses、OpenAI-compatible Chat Completions 与 Anthropic Messages SSE。三种 endpoint 都使用原生 wire stream；其他 SSE profile、NDJSON 与 WebSocket 仍需按 真实协议逐项接入。 Raw stream 的 socket/buffer backpressure 属于 zeta-http-client；SSE/NDJSON framed channel 的 backpressure 属于 zeta-client。Client 不能合并或丢弃它不理解的 Provider payload。 7. 遥测 Telemetry 分为两层： zeta-http-client zeta-client --- --- DNS/connect/TLS/HTTP timing operation duration proxy/redirect/pool evidence attempt count request/response byte count retry reason/backoff status class/transport error first frame/frame count transport timeout/cancel framing/operation outcome HTTP redaction policy low-cardinality API metadata 业务调用方只提供低基数 operation metadata，例如： `text operation.kind = modelinference modelcatalog api.profile = openairesponses anthropicmessages provider.kind = openai anthropic custom ` 禁止把 exact model ID、URL、header value、credential、prompt、tool arguments/output、reasoning、 raw response body 或 stream payload 放进 operation log/label。底层完整规则以 zeta-http-client README 为准。 8. 错误 目标 operation error 只增加本层语义： `rust pub enum ClientError Transport zetahttpclient::HttpClientError , OperationDeadlineExceeded, Cancelled, Framing StreamFramingError , RetryExhausted RetryExhaustedError , ` Provider error 不是 ClientError，由 zeta-api 解码。Error 可以包含 attempt、phase 和 bounded timing evidence，但不能复制 transport secret 或 raw body。 9. 目标目录 `text zeta-rs/zeta-client/ ├── BUILD.bazel ├── Cargo.toml └── src/ ├── lib.rs ├── operation/ │ ├── mod.rs │ ├── request.rs │ ├── response.rs │ ├── deadline.rs │ └── operationtests.rs ├── retry/ │ ├── mod.rs │ ├── policy.rs │ ├── classifier.rs │ ├── backoff.rs │ └── retrytests.rs ├── stream/ │ ├── mod.rs │ ├── sse.rs │ ├── ndjson.rs │ ├── activity.rs │ ├── limits.rs │ ├── ssetests.rs │ └── ndjsontests.rs ├── telemetry/ │ ├── mod.rs │ ├── operation.rs │ ├── attempt.rs │ ├── stream.rs │ └── telemetrytests.rs └── error.rs ` Backend、proxy、TLS、redirect、raw HTTP types 和 pool implementation 都不出现在该目录；它们属于 zeta-http-client。 10. 公共接口 长期 public API 只导出： - ZetaClient 或等价 operation executor； - ClientOperation / ClientOperationResponse； - SseFrame / SseEvent； - NdjsonRecord； - RetryPolicy / RetrySafety； - operation deadline/cancellation value； - ClientError 和安全 attempt evidence； - 必要的 operation telemetry context。 HTTP request/response/client/config/error 来自 zeta-http-client，不在本 crate 建立平行 value。 新增 public trait 必须说明实现者对 retry、operation deadline、framing、redaction 和 backpressure 的责任。 11. 测试 - retry max attempts、jitter、Retry-After 和 overall budget； - non-idempotent request 默认不 replay； - cancellation during backoff/attempt/stream； - transport failure phase 到 classifier 的映射； - 任意 byte fragmentation 与 UTF-8 split； - SSE multiline data、comment、CRLF/LF 和 EOF； - NDJSON blank line、oversized/incomplete record； - framed channel backpressure； - operation telemetry 低基数和 secret negative tests； - fake clock/fake zeta-http-client，不访问真实 Provider。 Proxy、TLS、redirect、HTTP timeout 和 pool tests 只属于 zeta-http-client，不在本 crate 重复。 12. 迁移顺序 1. 建立 zeta-http-client 的 raw HTTP value、client/config/error port。 2. 将 HttpHeader、raw request/response、HttpClient 和 UreqHttpClient 从本 crate 迁出。 3. 本 crate 改为依赖 zeta-http-client 并保留现有 unary behavior。 4. 把 operation retry loop 改为执行一个或多个 bounded transport attempts。 5. 增加 live raw stream execution 后接入现有 SSE framer。 6. 增加 NDJSON framer、operation deadline 与 framed backpressure。 7. 拆分 transport telemetry 与 operation telemetry。 8. 更新 zeta-api 和 model-provider，删除旧 raw transport compatibility surface。 当前处于开发阶段，迁移时直接更新全部调用方，不建立旧 UreqHttpClient public API 的长期兼容层。 13. 固定决策 1. zeta-client 是 API operation client，不是 workspace HTTP backend。 2. zeta-http-client 独占 proxy、TLS、redirect、attempt timeout、pool 和 transport redaction。 3. zeta-client 拥有 retry 机制，调用方提供 typed retry safety/policy。 4. zeta-client` 拥有 SSE/NDJSON framing，不解释 Provider event。 5. Inference POST 默认不透明 retry。 6. Wire activity、frame activity 与 semantic progress 分开。 7. Client 不依赖 Provider registry、config、Core 或 App Server。"
  },
  {
    "slug": "models-manager",
    "sourcePath": "docs/models-manager.md",
    "title": "模型目录系统",
    "description": "模型目录系统回答“当前有哪些模型可选、这些信息有多可信”；它管理发现、内存缓存和合并，不执行 模型调用。当前静态目录、动态 source port、snapshot/generation、singleflight、merge/filter/resolve 已经落地；真实 provider discovery 与完整客户端协议仍按后续阶段推进。",
    "group": "模型与配置",
    "markdown": "> 物理位置：`zeta-rs/models-manager/`\n> Rust crate：`zeta_models_manager`\n> 当前状态：Phase 1 core 已实现；动态 provider adapters、持久缓存和完整 App Server snapshot API 尚未实现\n> Crate 实现说明：[`zeta-models-manager` README](../zeta-rs/models-manager/README.md)\n> Canonical model contract：[`protocol.md`](protocol.md#6-provider-independent-model-contract)\n> Provider wire adapter：[`zeta-api.md`](zeta-api.md)\n> Provider runtime：[`model-provider.md`](model-provider.md)\n> Operation client：[`zeta-client.md`](zeta-client.md)\n> 底层网络：[`zeta-http-client` README](../zeta-rs/http-client/README.md)\n\n> 官方资料核对日期：2026-07-25。Provider API 与模型生命周期会持续变化，本文固定架构边界和\n> 合并语义；具体 endpoint、字段映射与内置 metadata 必须以实现时的官方文档和 contract test\n> 为准。\n\n## 快速理解\n\n模型目录系统回答“当前有哪些模型可选、这些信息有多可信”；它管理发现、内存缓存和合并，不执行\n模型调用。当前静态目录、动态 source port、snapshot/generation、singleflight、merge/filter/resolve\n已经落地；真实 provider discovery 与完整客户端协议仍按后续阶段推进。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| 模型列表来自哪里？ | 供应商发现、内置资料、用户配置和本地状态按字段来源合并 | [核心领域模型](#5-核心领域模型) |\n| 为什么某个模型没有出现？ | 可能是供应商不支持发现、缓存尚未刷新、生命周期过滤或能力不匹配 | [供应商发现策略](#6-供应商发现策略) |\n| 什么时候访问供应商？ | 依据新鲜度、显式刷新和缓存状态决定，并使用 singleflight 合并并发刷新 | [何时请求](#7-何时请求) |\n| 模型目录能否证明模型支持某项能力？ | 不能；缺失字段表示未知，不能被解释为明确不支持 | [发现能力不是模型能力](#63-发现能力不是模型能力) |\n| 谁真正调用模型？ | 已选模型交给模型运行时，目录系统不拥有请求、重试或传输 | [职责与非职责](#3-职责与非职责) |\n\n## 1. 结论\n\n`zeta-models-manager` 是 Zeta 的模型目录控制面。它统一管理各个 provider 可用模型的发现、\n缓存、合并、筛选和面向上层的 catalog snapshot，负责决定：\n\n- 何时请求 provider，何时直接使用缓存或静态 metadata；\n- 如何把 provider 动态结果、内置 metadata、用户配置和本地模型状态合并；\n- 如何在“不知道”与“明确不支持”之间保持区别；\n- 如何按 Agent 所需能力、生命周期和用户策略筛选模型；\n- 如何向 App Server、Desktop、CLI、TUI 和 Agent runtime 暴露一致的模型信息。\n\n它不是新的模型调用 client，也不是第二个 provider registry。边界固定为：\n\n```text\nprovider adapter 负责“如何向某家服务取回原始模型目录”\nmodels manager 负责“何时取、如何缓存、如何合并、如何解释和如何发布”\nmodel provider 负责“如何用已选模型执行一次调用”\n```\n\n模型列表接口不能被当作完整 metadata authority。各家返回信息差异很大：\n\n- OpenAI、DeepSeek 一类接口主要返回 ID、owner、created 等基础信息；\n- Anthropic、Gemini、xAI 能返回部分或较丰富的 capability、token limit、modality、\n  alias 或 pricing metadata；\n- Hugging Face 的 Hub catalog、Inference Providers router 和实际下游 provider 是不同 scope，\n  coverage 需要单独声明；\n- 一些 provider 目前只有官方静态模型矩阵，没有文档化的 runtime catalog endpoint；\n- OpenAI-compatible 自定义服务对 `GET /models` 的支持和字段质量都不能假定。\n\n因此 manager 必须做字段级 merge，并为每个字段保留来源与可信度；禁止因为某次响应没有某个\n字段，就把已有的已知值覆盖成“不支持”，也禁止根据 model ID 字符串猜测工具、推理或上下文\n能力。\n\n## 2. 当前实现状态与仓库审计\n\n当前模型相关职责分布如下：\n\n| 位置 | 已有职责 | 不应继续扩张的方向 |\n| --- | --- | --- |\n| `zeta-protocol::model::catalog` | identity、`ModelInfo`、capability、availability/freshness/lifecycle/quality value | 请求调度、缓存、provider DTO、refresh state |\n| `zeta-model-provider-config` | provider definition、endpoint/default、静态 seed models、配置归一化 | HTTP、凭据读取、动态 discovery、TTL |\n| `zeta-models-manager` | scope、静态 seed、memory cache、source port、singleflight、merge/filter/resolve、snapshot generation | provider DTO、secret、调用、Config persistence、UI |\n| `zeta-model-provider` | provider runtime、adapter 选择、模型调用、manager static resolution consumer | catalog policy、跨 provider merge、UI 查询 |\n| `zeta-api` | endpoint/request/event 的 Provider wire codec | transport、retry、catalog authority、用户筛选 |\n| `zeta-http-client` | HTTP/WebSocket execution、proxy/TLS/pool、transport diagnostics | Provider DTO、catalog policy、模型选择 |\n| `zeta-client` | operation retry、SSE/NDJSON framing、telemetry | Provider DTO、catalog policy、模型选择 |\n| `zeta-config` | 用户配置 authority、patch/merge/persistence | provider 请求和进程内 refresh task |\n| App Server / clients | 组合、RPC、展示与交互；Local App Server 已投影 shared manager | 各自维护模型表或推断 capability |\n\n`ProviderDefinition.models` 当前只作为启动 seed 和内置 metadata 来源，不兼任动态可用性缓存。\n`ModelProviderRuntime::resolve_model` 已消费 manager 的 static resolution；Local App Server 的\n`model/list` 和 Session model validation 也使用同一个 manager clone，不再独立维护\n`ListedOnly` / `AllowUnlisted` 判断。\n\n当前实现边界如下：\n\n| 能力 | 状态 | 实现证据 |\n| --- | --- | --- |\n| 静态 seed、确定排序、typed resolution | ✅ | `ModelsManager::{static_snapshot,list_static,resolve_static}` |\n| 动态 source port、scope 校验、partial/complete merge | ✅ | `ModelCatalogSource`、`commit_discovery`、`apply_discovery` |\n| per-scope memory cache、freshness、singleflight | ✅ | `ManagedScope`、`ScopeState`、`ModelsManager::{read,refresh}` |\n| 字段 provenance 与 Unknown 保留 | ✅ | `CatalogRecord`、`ModelMetadataProvenance` |\n| model-provider/App Server 静态目录统一 | ✅ | `ModelProviderRuntime::models_manager`、`ConfigBackedModelService` |\n| 真实 provider discovery adapters | 尚未完成 | Phase 2 |\n| persisted observation cache、backoff/jitter、并发总闸 | 尚未完成 | Phase 4 / 后续 core hardening |\n| `model/refresh`、`model/updated`、完整 snapshot DTO | 尚未完成 | Phase 3 |\n\n现有 `ModelInfo` 也存在后续需要修正的语义缺口：\n\n- 一个 `ContextWindow` 无法区分 total context、最大输入和最大输出；\n- capability 只覆盖 tools、reasoning、parallel tools 和 personality，无法表达 input modality、\n  structured output 或 workload kind；\n- 没有 availability、freshness、lifecycle、字段来源和诊断信息；\n- `Option<u32>` 同时可能表示“未知”“未配置”或“不适用”。\n\n这些缺口应通过 protocol value 演进解决，不能在 manager 对外 API 中重新发明一套同名 DTO。\n\n## 3. 职责与非职责\n\n### 3.1 Manager 拥有\n\n- provider catalog scope 的稳定识别；\n- 首次加载、按需 refresh、显式 refresh 和后台 refresh 策略；\n- per-scope cache、TTL、stale-while-revalidate、stale-if-error；\n- per-scope singleflight、并发上限、退避和抖动；\n- provider discovery、内置 metadata、用户 override 和历史 cache 的字段级 merge；\n- model lifecycle、availability、metadata quality 和 freshness projection；\n- capability/workload/visibility filter；\n- `(ProviderId, ModelId)` 的解析、校验和稳定排序；\n- immutable `ModelCatalogSnapshot` 及 generation 变化；\n- cache/refresh/merge 的诊断信息和不含秘密的 telemetry。\n\n### 3.2 Manager 不拥有\n\n- API key、OAuth token、secret persistence 或 credential refresh；\n- provider HTTP DTO、认证 header、endpoint 拼接和分页 wire codec；\n- completions/responses/messages 请求执行；\n- prompt/context KV cache 的创建、breakpoint、TTL、usage 解析或计费语义；\n- SSE/NDJSON/WebSocket 的 ping、空行、注释帧、读超时和断线重连；\n- 本地模型进程的预热、驻留和卸载；\n- Config 文件 authority 或 provider definition registry 的注册事务；\n- 模型价格账单、quota authority 或 rate-limit enforcement；\n- Session、Thread、Turn 的 durable state；\n- UI 中 picker 的布局、搜索输入和用户交互；\n- 通过真实 inference 请求“试探”模型能力。\n\n价格可以将来作为带来源和更新时间的展示 metadata 进入 snapshot，但不能被当作 billing\nauthority；实际费用以 provider 账单和调用 usage 为准。\n\n### 3.3 四类不能混用的“缓存/保活”\n\n厂商文档中的 cache、keep-alive 和 heartbeat 不是同一个概念，必须按运行时边界拆开：\n\n| 机制 | 含义 | 所属层 |\n| --- | --- | --- |\n| Catalog cache | Zeta 缓存模型列表、availability 和 metadata observation | `zeta-models-manager` |\n| Prompt/context cache | 厂商复用 prompt prefix/KV tensor，影响推理延迟、费用和 usage | `zeta-api` provider adapter |\n| Stream liveness | raw byte activity/读超时属于 `zeta-http-client`；SSE/NDJSON frame activity 属于 `zeta-client` | Provider event 语义由 `zeta-api` 解释 |\n| Model residency | 本地模型是否继续驻留 CPU/GPU 内存 | `zeta-model-provider` 本地 runtime；wire 参数仍由 `zeta-api` 编码 |\n\n官方行为已经证明这些机制不能抽象成 manager 中的统一 `heartbeat`：\n\n- [Claude streaming](https://platform.claude.com/docs/en/build-with-claude/streaming) 允许在\n  SSE 消息事件之间发送 `ping`；\n- [DeepSeek request keep-alive](https://api-docs.deepseek.com/quick_start/rate_limit/) 对非流式\n  请求发送空行，对流式请求发送 SSE `: keep-alive` 注释；\n- [Ollama streaming](https://docs.ollama.com/api/streaming) 使用\n  `application/x-ndjson`，而请求参数\n  [`keep_alive`](https://docs.ollama.com/api/generate) 表示模型在内存中的驻留时间。\n\nPrompt cache 同样是调用语义而非 catalog cache：\n[OpenAI Prompt Caching](https://developers.openai.com/api/docs/guides/prompt-caching)、\n[Claude Prompt Caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching) 和\n[Gemini Context Caching](https://ai.google.dev/gemini-api/docs/caching/) 的启用方式、TTL、\nbreakpoint 和 usage 字段均不同。Manager 最多把“某模型是否支持某类 prompt cache”作为带来源的\ncapability metadata 暴露，不能创建、续期或命中厂商推理 cache。\n\n具体依赖边界固定为：\n\n- `zeta-protocol` 只在确实需要跨 crate/进程表达时定义 provider-independent 的 cache intent、\n  normalized usage 和 stream event value；\n- `zeta-api` 将 canonical request 转成 `prompt_cache_key`、`cache_control`、`cached_content`\n  等 provider wire 字段，解析 cache usage，并过滤/解释 provider-specific 心跳帧；\n- `zeta-model-provider` 选择具体 `zeta-api` endpoint/profile，提供 resolved endpoint、固定 header 和\n  retry policy，但不解释某家厂商的 cache breakpoint、TTL 或 SSE event；\n- `zeta-http-client` 执行 HTTP、transport timeout 和 diagnostics；`zeta-client` 执行 operation\n  retry 与 SSE/NDJSON framing，但都不解释 Provider event；\n- `zeta-models-manager` 只维护模型是否支持相关能力的 catalog metadata，不参与一次调用的\n  cache 生命周期或流式连接。\n\n## 4. 依赖方向与组合\n\n目标依赖关系中，箭头表示“依赖”：\n\n```text\nzeta-models-manager\n  ├──▶ zeta-protocol\n  └──▶ zeta-model-provider-config\n\nzeta-model-provider\n  ├──▶ zeta-model-provider-config\n  ├──▶ zeta-models-manager       # implements ModelCatalogSource\n  ├──▶ zeta-api ───▶ zeta-client\n  ├──▶ zeta-client\n  ├──▶ zeta-http-client\n  └──▶ zeta-secrets             # direct-provider credential only\n\nApp Server composition\n  ├──▶ zeta-model-provider\n  └──▶ zeta-models-manager\n\nzeta-codex-app-server\n  └──▶ zeta-model-provider       # subscription runtime/catalog observation adapter\n```\n\n具体规则：\n\n- `zeta-models-manager` 可依赖 `zeta-protocol` 和 `zeta-model-provider-config`；\n- `zeta-models-manager` 不依赖 `zeta-model-provider`、`zeta-api`、`zeta-client`、\n  `zeta-http-client`、`zeta-secrets` 或 App Server；\n- manager 定义并拥有 `ModelCatalogSource` port，因为它是该 port 的消费者；\n- `zeta-model-provider` 实现该 port，复用已有 adapter、normalized endpoint、operation/HTTP\n  clients 和 credential materialization；\n- App Server composition root 将同一个 provider runtime 分别作为模型调用能力和 catalog source\n  注入，不能再建第二个 HTTP client registry；\n- provider-specific discovery DTO 留在 `zeta-api` 或 `zeta-model-provider` 的私有 adapter\n  module，不能进入 protocol。\n\nGoogle 和 Ollama 说明了为什么 discovery endpoint 不能从调用 base URL 盲目拼接：\n\n- 当前 Google 调用走 `.../v1beta/openai`，但原生模型目录是\n  `generativelanguage.googleapis.com/v1beta/models`；\n- 当前 Ollama OpenAI-compatible 调用走 `http://localhost:11434/v1`，但本地模型目录是\n  `/api/tags`，单模型详情是 `/api/show`。\n\nProvider definition 应显式声明 discovery strategy 或由具体 adapter 解析，禁止 manager 用\n`trim_end_matches(\"/v1\")` 一类字符串启发式生成 endpoint。\n\n## 5. 核心领域模型\n\n以下是目标语义，不要求第一版逐字采用这些 Rust 名称。\n\n### 5.1 目录范围\n\n模型可用性不仅取决于 `ProviderId`，还可能取决于：\n\n- normalized base URL 和 region；\n- credential account、organization、project 或 tenant；\n- provider 配置 revision；\n- 本地 Ollama daemon 或自定义 gateway 实例；\n- Hugging Face 的 provider routing/account 权限。\n\n因此 cache key 不能只是 `ProviderId`：\n\n```rust\npub struct CatalogScopeKey {\n    pub provider: ProviderId,\n    pub source_scope: CatalogSourceScopeId,\n}\n```\n\n`CatalogSourceScopeId` 是由 composition/provider layer 生成的 opaque、稳定 fingerprint。它可以\n包含 endpoint identity、credential reference identity 和 config revision 的 hash，但不得包含、\n记录或向客户端暴露 secret、token、完整认证 header。\n\n凭据内容轮换但 identity 不变时，credential layer 必须提供 revision，使新旧权限不会错误复用\n同一 cache。旧 scope 由 LRU/保留期回收，不做危险的全局 cache clear。\n\n### 5.2 发现结果\n\nSource port 返回 provider-neutral、带覆盖范围的结果：\n\n```rust\npub struct DiscoveredCatalog {\n    pub scope: CatalogScopeKey,\n    pub coverage: DiscoveryCoverage,\n    pub models: Vec<DiscoveredModel>,\n    pub validator: Option<CatalogValidator>,\n    pub cache_hint: CatalogCacheHint,\n    pub fetched_at: SystemTime,\n}\n\npub enum DiscoveryCoverage {\n    CompleteFor(ModelWorkload),\n    Partial,\n}\n\npub enum CatalogCacheHint {\n    NotSpecified,\n    RevalidateAfter(Duration),\n    ImmutableUntilSourceRevision,\n}\n```\n\n关键语义：\n\n- `CompleteFor(AgentText)` 表示“本响应完整覆盖该 credential/scope 下可用于 Agent text\n  workload 的模型”，此时缺席才可影响 availability；\n- `Partial` 表示搜索结果、单模型补充信息或 provider 明确不保证完整性，缺席不能形成 tombstone；\n- `CatalogValidator` 表达 ETag、Last-Modified 或 provider revision，而不是随意的字符串；\n- `CatalogCacheHint` 只转述官方协议或实际 HTTP 响应给出的证据；manager 会用本地最小/最大\n  freshness、错误退避和用户策略计算最终刷新时间；\n- provider 没有文档化 cache header、revision 或刷新周期时必须返回 `NotSpecified`，不能根据\n  其他 OpenAI-compatible 服务的行为类推；\n- raw response、认证 header 和 provider request ID 不进入 snapshot。\n\n### 5.3 元数据 patch 与来源\n\nDiscovery、内置 catalog 和配置 override 都转换为 patch，而不是直接构造最终 `ModelInfo`：\n\n```rust\npub struct SourcedModelPatch {\n    pub model: ModelRef,\n    pub source: MetadataSource,\n    pub fields: ModelMetadataPatch,\n}\n\npub enum MetadataSource {\n    ProviderLive,\n    UserConfigured,\n    BuiltinCurated,\n    ProviderSeed,\n    PersistedObservation,\n}\n```\n\n每个可合并字段内部保留 `value + source + observed_at`。对外可以压缩为\n`MetadataQuality` 和少量 warnings，但诊断接口必须能解释“这个 200K context 从哪里来”。\n\n`CapabilitySupport::{Supported, Unsupported, Unknown}` 的三态必须保留。`Unknown` 不是\n`Unsupported`，缺失字段也不是否定证据。\n\n### 5.4 快照\n\n所有消费者读取 immutable snapshot：\n\n```rust\npub struct ModelCatalogSnapshot {\n    pub scope: CatalogScopeKey,\n    pub generation: u64,\n    pub freshness: CatalogFreshness,\n    pub entries: Vec<ModelCatalogEntry>,\n    pub warnings: Vec<CatalogWarning>,\n}\n\npub enum CatalogFreshness {\n    Fresh,\n    StaleUsable,\n    Expired,\n    StaticOnly,\n}\n```\n\n同一 scope 只有在 consumer-visible 内容变化时才递增 generation。单纯 refresh 得到完全相同的\n结果，只更新内部 observation time，不制造无意义的 `model/updated`。\n\n排序必须确定，snapshot 中 model ID 不得重复。相同 generation 的序列化结果必须稳定，便于\nApp Server、Desktop 和 contract tests 比较。\n\n## 6. 供应商发现策略\n\n### 6.1 官方能力矩阵\n\n`Discovery mode` 描述 Zeta 如何组合实时 availability 与 curated metadata，不代表厂商承诺了\n客户端刷新频率。\n\n| Provider | Discovery mode | 推荐 discovery | 官方响应可提供的信息 | 初始策略 |\n| --- | --- | --- | --- | --- |\n| OpenAI | Hybrid | [`GET /v1/models`](https://developers.openai.com/api/reference/resources/models/methods/list) | ID、created、owner；官方描述为基础信息 | 动态 availability + 内置能力 metadata |\n| Anthropic | Dynamic/Hybrid | [`GET /v1/models`](https://platform.claude.com/docs/en/api/models/list) | ID、display name、token limits，并可返回 thinking、effort、image、structured output 等 capability | 优先使用动态字段，内置补缺 |\n| Google Gemini | Dynamic/Hybrid | [`models.list`](https://ai.google.dev/api/models) | display name、input/output limit、generation methods、thinking 等 | 使用原生 Gemini endpoint，不走 OpenAI-compatible base URL |\n| xAI | Dynamic/Hybrid | [`GET /v1/language-models`](https://docs.x.ai/developers/rest-api-reference/inference/models) | modality、aliases、fingerprint、部分价格；比 `/v1/models` 丰富 | Agent text 使用 language-models |\n| Kimi | Unknown/StaticOnly | 当前可访问的[官方平台文档](https://platform.moonshot.ai/docs/)未确认本设计所需的 authenticated list contract | `Unknown` | 使用 curated metadata；取得官方 reference/fixture 前不猜 `/models` |\n| DeepSeek | Hybrid | [`GET /models`](https://api-docs.deepseek.com/api/list-models) | 基础 ID、owner | 动态 availability + 内置能力 metadata |\n| Ollama | Dynamic local | [`GET /api/tags`](https://docs.ollama.com/api/tags) + 按需 `/api/show` | 本地已安装模型、family、size、quantization；详情可补 template/model info/capability | 高频短 TTL；只对可见候选按需取详情 |\n| Hugging Face | Dynamic/Hybrid | [Router `GET /v1/models`](https://huggingface.co/docs/inference-providers/hub-api) | modality、live provider、context、tools、structured output、价格/延迟等 | 过滤 conversational/live route，限制结果规模 |\n| MiniMax | StaticOnly（待核实） | [官方 models guide](https://platform.minimax.io/docs/guides/models-intro) | 静态模型、feature 和 endpoint 信息 | 未确认 authenticated list API 前不猜 `/models` |\n| Qwen / Model Studio | StaticOnly（待核实） | [官方模型矩阵](https://help.aliyun.com/en/model-studio/models) | region、API surface、模型能力由静态文档描述 | 未确认官方 inference list 前不假定 `/models` |\n| Z.AI | StaticOnly（待核实） | [官方模型矩阵](https://docs.z.ai/guides/overview/overview) | 模型类型、context 和能力由静态文档描述 | 动态 endpoint 需官方文档确认 |\n| Xiaomi MiMo | Unknown/StaticOnly | 当前未找到可访问、可核对的官方网络协议 reference | `Unknown` | 仅使用已审阅的 configured metadata；动态 endpoint 需官方文档确认 |\n| OpenAI-compatible | Unknown/Hybrid | best-effort `GET /models` | 完全取决于 gateway | adapter capability 检测；失败后转静态/用户配置 |\n\n该表只说明“可从哪里发现什么”，不把某个具体 model ID 固化成架构。内置 metadata 需要带\n`reviewed_at` 和官方 source URL，并通过定期维护更新。\n\n上述官方模型列表文档主要规定 endpoint、认证、分页和响应 schema。除非 provider profile\n另外引用了明确的 HTTP cache 文档，否则不得认为厂商承诺 `Cache-Control`、ETag、\nLast-Modified、固定 TTL、catalog push 或 heartbeat。实现时还必须通过不含秘密的 contract test\n记录真实响应头；观察到但未文档化的 header 只能作为优化，不能成为正确性前提。\n\n### 6.2 供应商验证记录\n\n每个内置 provider 必须维护一份可审阅的 profile，可以是对应 adapter module 的测试 fixture 和\n文档注释，不要求拆成独立 crate。至少记录：\n\n```text\nofficial_sources\nverified_at\ndiscovery_endpoint_and_auth\nresponse_schema_and_pagination\ncoverage_semantics\nstatic / dynamic / hybrid / unknown\ndocumented_cache_headers_or_revision\nobserved_cache_headers\nrefresh_triggers\nnegative-cache-invalidators\n```\n\n其中“官方未说明”和“尚未验证”必须保持为 `Unknown`，不能填写从相似 provider 推导出的默认值。\n官方文档变化后，先更新 profile/fixture，再调整 adapter 和内置 metadata。\n\n### 6.3 发现能力不是模型能力\n\nProvider definition 需要显式表达目录能力：\n\n```rust\npub enum ModelDiscoveryStrategy {\n    Native(ProviderDiscoveryKind),\n    OpenAiCompatible,\n    StaticOnly,\n    Disabled,\n}\n```\n\n不能使用 `supports_discovery: bool`，因为 `Native`、best-effort compatible、static-only 和\n明确禁用是不同语义。`ProviderDiscoveryKind` 由 provider runtime 解释，manager 不分支判断\nAnthropic、Gemini 或 Ollama。\n\nDiscovery wire strategy 与 refresh mode 也要分开：前者决定 endpoint/codec，后者由 manager 根据\nsource profile、HTTP cache hint、本地 policy 和当前错误状态计算。不能因为实现了 `GET /models`\n就自动启动固定周期轮询，也不能因为存在静态 seed 就跳过用户显式 refresh。\n\n### 6.4 禁止推理探测\n\nManager 不通过发送“hello”、空 prompt、tool call 或超小 `max_tokens` 请求探测能力，原因是：\n\n- 会产生费用、配额消耗和外部副作用；\n- 失败无法可靠区分“不支持”、参数不兼容、权限或临时故障；\n- 会把 catalog refresh 变成真实模型调用；\n- 可能把用户凭据用于未明确触发的内容生成。\n\n能力只能来自官方 discovery 字段、经过审阅的内置 metadata 或用户显式 trust override。\n\n## 7. 何时请求\n\n### 7.1 Read 策略\n\n调用方使用具名策略，不传 `fresh: bool`：\n\n```rust\npub enum CatalogReadPolicy {\n    CachePreferred,\n    RequireFresh,\n    CacheOnly,\n}\n```\n\n语义固定为：\n\n| 当前状态 | `CachePreferred` | `RequireFresh` | `CacheOnly` |\n| --- | --- | --- | --- |\n| Fresh | 立即返回 | 立即返回，除非调用方显式执行 refresh | 立即返回 |\n| StaleUsable | 立即返回并触发后台 refresh | 等待/join refresh | 返回 stale |\n| Expired | 等待/join refresh；失败按 stale-if-error policy 决定 | 等待 refresh，失败返回错误 | 返回 expired snapshot |\n| 无 cache | 等待首次 discovery | 等待首次 discovery | 返回 cache miss |\n| StaticOnly | 返回静态 snapshot | 返回静态 snapshot 和不可动态刷新的状态 | 返回静态 snapshot |\n\n### 7.2 刷新触发条件\n\n允许触发请求的事件：\n\n- App Server 启动后，对 preferred/current provider 做低优先级 warm-up；\n- 某个 scope 首次执行 `model/list`；\n- cache 从 Fresh 进入 StaleUsable 后的首次读取；\n- 用户显式执行 refresh；\n- provider config、endpoint、credential revision 或 tenant scope 改变；\n- 本地 Ollama picker 打开或本地 daemon 状态恢复；\n- provider adapter 明确发送 catalog invalidation（将来可选）。\n\n不得在以下位置隐式阻塞网络：\n\n- protocol deserialize；\n- Config reducer/commit；\n- Thread reducer 或 rollout recovery；\n- 已开始的模型调用中途；\n- 仅渲染已有 snapshot 的 UI frame。\n\nTurn 创建 `ModelInvocationSnapshot` 时只解析已经选定的 `ModelRef`。普通 TTL refresh 不改变已经\n开始的调用；新 catalog 只影响下一次 picker read 或下一个安全点的 model resolution。\n\n### 7.3 Singleflight 与并发\n\n- 相同 `CatalogScopeKey` 同时最多一个 refresh；\n- `RequireFresh` 和首次读取 join 现有 refresh，不重复请求；\n- 不同 scope 可以并行，但受全局和 per-provider 并发上限约束；\n- 慢 provider 不持有 catalog write lock；网络 I/O 完成后以短临界区 compare-and-swap snapshot；\n- refresh result 必须校验 scope generation，旧配置启动的晚到响应不得覆盖新 scope；\n- shutdown/cancellation 停止 refresh task，不提交半个分页结果。\n\n分页 discovery 要么产生声明为 `CompleteFor` 的完整结果，要么失败/降级为 `Partial`，不能把只取到\n第一页的结果伪装成完整目录。\n\n## 8. 目录缓存语义\n\n本章只讨论 Zeta 对模型目录 observation 的缓存，不讨论厂商 prompt/context cache、stream\nkeep-alive 或本地模型驻留。后三者的 provider-specific 行为分别留在 `zeta-api` codec、\n`zeta-http-client` transport、`zeta-client` framing 和本地 runtime。\n\n### 8.1 两层缓存\n\n第一版至少提供：\n\n1. process-local memory cache：读取热路径、singleflight state 和 immutable snapshots；\n2. 可选 persisted observation cache：改善重启和离线体验，但只是可删除 projection。\n\n持久 cache 通过 `CatalogCacheStore` port 注入。Manager 决定 key、schema、freshness 和写入时机；\n文件/数据库 adapter 决定物理 I/O。持久层不得成为模型可用性的 authority。\n\n持久记录只保存 normalized metadata、scope fingerprint、官方 source revision 和时间，不保存：\n\n- API key、token、cookie、认证 header；\n- raw provider response；\n- 完整 endpoint query 中可能包含的秘密；\n- provider 返回的非必要账号信息。\n\n### 8.2 Freshness 策略\n\n使用具名 policy：\n\n```rust\npub struct CatalogFreshnessPolicy {\n    pub fresh_for: Duration,\n    pub stale_while_revalidate_for: Duration,\n    pub stale_if_error_for: Duration,\n}\n```\n\n以下是 Zeta 的建议默认值，不是厂商官方 TTL，也不是跨 provider 的协议保证：\n\n| Source | Fresh | 后台刷新可用期 | 临时错误最大 stale |\n| --- | --- | --- | --- |\n| 远程 provider | 15 分钟 | 24 小时 | 7 天 |\n| 本地 Ollama | 2 秒 | 30 秒 | 5 分钟 |\n| StaticOnly | 不过期 | 不适用 | 不适用 |\n| Unsupported discovery negative cache | 24 小时 | 不适用 | 配置变化时立即失效 |\n\n这些值由 manager policy/config 调整，不进入 `zeta-protocol`。测试必须使用注入 clock，禁止真实\nsleep。\n\n### 8.3 HTTP 校验器与退避\n\n- provider 官方文档或实际响应支持 ETag/Last-Modified 时使用 conditional request；\n- `304` 只延长 freshness，不重建 generation；\n- `429`、`5xx`、timeout 和网络错误使用 exponential backoff + jitter；\n- `401/403` 不做长时间自动重试，等待 credential/config revision 或用户显式 refresh；\n- `404/405/501` 可标记 discovery unsupported，并进入 negative cache；\n- 成功请求会清除该 scope 的 transient backoff；\n- 用户显式 refresh 可以绕过 TTL，但仍要 join singleflight 和服从最小防抖间隔。\n\n实际响应中观察到但官方未保证的 validator 可以用于性能优化；服务器停止返回 validator 时必须\n退回普通 discovery，不能导致目录不可刷新。`Cache-Control` 也必须经过 manager 配置的\nminimum/maximum freshness clamp，不能让异常 gateway 无限延长 availability。\n\n### 8.4 Heartbeat 与健康恢复\n\nModels manager 第一版没有通用 heartbeat：\n\n- catalog 请求是有边界的普通 HTTP discovery；transport timeout/cancel 由 `zeta-http-client`\n  执行，operation retry 由 `zeta-client` 执行；\n- completions/messages 的 SSE/NDJSON framing 由 `zeta-client` 完成，Provider heartbeat event\n  由 `zeta-api` decoder 消费，不能进入\n  `DiscoveredCatalog`；\n- Ollama daemon 的存活和模型驻留由本地 provider runtime 管理，manager 只响应\n  `source revision changed` 或显式 invalidation；\n- 不允许通过定期 inference 请求充当 provider 健康探测。\n\n只有当某 provider 官方提供模型目录 watch/change feed 时，未来才增加 provider-specific\ninvalidation source。长连接的 transport ping/pong 由 `zeta-http-client` 管理，SSE/NDJSON frame\nactivity 由 `zeta-client` 管理；manager 只接收\n`CatalogInvalidated { scope, revision }` 这类领域事件。\n\n## 9. 合并规则\n\n### 9.1 身份\n\n唯一 identity 始终是：\n\n```text\n(ProviderId, exact ModelId)\n```\n\n- 不把不同 provider 的同名模型合并；\n- 不 lowercase 或改写 provider model ID；\n- alias 仍以调用时的 exact ID 保留；\n- 只有 provider 明确返回 alias target 时，UI 才可分组展示；\n- stable alias 与 dated/pinned version 不自动互换；\n- 历史 Thread 中的 `ModelRef` 即使不再可用也必须可显示。\n\n### 9.2 字段优先级\n\n推荐默认优先级：\n\n```text\n用户显式 trust override\n    > provider live 的明确字段\n    > 内置 curated metadata\n    > ProviderDefinition seed\n    > persisted observation\n    > Unknown\n```\n\n但 availability 与 visibility 不使用这条简单顺序：\n\n- availability 由当前 scope 下最近一次完整 provider observation 决定；\n- user hidden/disabled 是本地 visibility/policy，不伪装成 provider unavailable；\n- 静态 seed 表示“Zeta 知道该模型”，不表示当前 credential 一定有权限；\n- provider 完整列表缺席可把该 scope 的模型标为 `Unavailable`，但不删除 metadata；\n- partial result 缺席不改变 availability；\n- persisted observation 在 live refresh 后只能补缺，不能覆盖更新的 live 字段。\n\n### 9.3 Patch 规则\n\n- `Unknown` 不覆盖 `Supported` 或 `Unsupported`；\n- provider 明确返回 unsupported 可以覆盖旧的 curated supported；\n- 数值 limit 只接受正数并记录其语义类型；\n- 冲突的 total/input/output limit 不互相强行换算，产生 warning；\n- display name 可由用户自定义，但 exact model ID 永不改变；\n- provider 未返回 lifecycle 时，不根据 ID 中是否含 `preview` 直接断言；内置规则可以给出\n  `Inferred` label，但不能当作 provider fact；\n- model 从 live catalog 消失时保留一段 tombstone retention，供历史显示、配置诊断和 UI\n  迁移提示使用。\n\n### 9.4 用户配置分层\n\n用户 override 分为两类，不能混成一个任意 JSON patch：\n\n```text\nPolicy override\n  hidden / pinned / display alias / preferred / allowed lifecycle\n\nTrust override\n  context limits / modalities / tools / reasoning / structured output\n```\n\nPolicy override 不改变事实 metadata。Trust override 用于自定义 gateway 或 provider 文档缺失场景，\n必须显式标记为 `UserConfigured`，经过静态 validation，并在诊断中可见。\n\n对于 limit，manager 可以允许用户收紧已知值；若用户扩大 provider 已知上限，应产生\n`OverridesProviderLimit` warning，避免静默制造不安全的 context budget。\n\n## 10. 筛选、解析与排序\n\n### 10.1 筛选流程\n\n面向 Agent 模型 picker 的顺序固定为：\n\n```text\nscope/provider 选择\n→ workload kind\n→ provider availability\n→ lifecycle / local policy\n→ required capabilities\n→ 用户搜索\n→ 稳定排序\n```\n\n第一步先排除 embedding、image-only、speech-only 等不能处理当前 canonical `ModelRequest` 的模型。\n不能因为 `/models` 返回了某个 ID，就默认它是聊天模型。\n\nCapability query 使用集合与显式 unknown policy：\n\n```rust\npub struct ModelQuery {\n    pub workload: ModelWorkload,\n    pub required_capabilities: BTreeSet<ModelCapability>,\n    pub unknown_capability: UnknownCapabilityPolicy,\n    pub availability: AvailabilityFilter,\n}\n\npub enum UnknownCapabilityPolicy {\n    IncludeWithWarning,\n    Exclude,\n}\n```\n\n禁止使用 `tools: bool`、`include_unknown: bool` 等难以扩展的参数组合。\n\n### 10.2 解析不是简单列表查找\n\nAgent runtime 应调用 manager 的 typed resolution：\n\n```text\nresolve(ModelRef, ModelRequirements, ResolutionPolicy)\n    → ResolvedModel\n```\n\n它需要检查：\n\n- provider/config scope 是否匹配；\n- model 是否满足 `ListedOnly` / `AllowUnlisted`；\n- 当前 workload 和强制 capability 是否满足；\n- model 是否明确 unavailable/retired；\n- 选择使用的 metadata snapshot generation；\n- 是否带有 stale、unknown 或 user-trust warning。\n\nCatalog 不应成为错误的授权替代品：\n\n- `AllowUnlisted` 下，用户显式填写的 model ID 可以继续进入 provider 调用，manager 返回\n  unverified metadata；\n- `ListedOnly` 下，只接受当前可用或本地明确注册的条目；\n- provider 最终仍可能因权限、region 或下线返回错误，runtime 必须保留真实 provider error；\n- manager 不把一次调用失败永久改写为 model unavailable，除非随后完整 refresh 证实。\n\n### 10.3 稳定排序\n\n默认排序建议使用：\n\n1. user pinned/preferred；\n2. provider 配置顺序；\n3. usable availability；\n4. stable/preview/legacy lifecycle；\n5. curated family rank；\n6. case-preserving display name；\n7. exact model ID。\n\n不得通过字符串或语义版本猜测“最大/最新”模型。Provider 的 `latest` alias 作为普通、可能漂移的\nalias 展示，并明确 lifecycle，不自动排在 pinned version 之前。\n\n## 11. 向上暴露\n\n### 11.1 协议值\n\n建议在 `zeta-protocol` 演进共享值，而不是在 App Server wire 层复制：\n\n```text\nModelCatalogSnapshot\nModelCatalogEntry\nModelAvailability\nModelLifecycle\nCatalogFreshness\nMetadataQuality\nCatalogWarning\nTokenLimits\nModelModality / ModelWorkload\n```\n\n`ModelInfo` 建议收敛为 provider-independent 的模型事实与 Zeta 默认 metadata：\n\n- exact `ModelId`、display name、description；\n- `TokenLimits { context, input, output }`，每项使用 Known/Unknown/NotApplicable；\n- input/output modalities；\n- tools、parallel tool calls、reasoning、structured output 等 capability；\n- supported/default reasoning effort；\n- Zeta personality/default 和 auto-compaction recommendation。\n\nAvailability、freshness、source quality 和 warnings 属于 `ModelCatalogEntry/Snapshot`，不塞进\n可跨 scope 复用的 `ModelInfo`。\n\n当前 `effective_auto_compact_token_limit()` 使用 context 的 90% 推导，只能作为临时 fallback。\n长期 context builder 应从明确的 input/context/output limits 和预留策略计算 budget；manager\n提供事实和 recommendation，不执行 compaction。\n\n### 11.2 App Server API\n\n建议增加：\n\n```text\nmodel/list\nmodel/refresh\nmodel/updated\n```\n\n- `model/list` 接受 typed query/read policy，返回 snapshot；\n- `model/refresh` 是显式动作，返回新 snapshot 或带 last-known snapshot 的 typed error；\n- `model/updated` 只在 consumer-visible generation 变化后通知；\n- initialize result 可包含 catalog capability/version，不内嵌完整目录；\n- App Server 负责 connection subscription 和 DTO 映射，不维护第二份 catalog；\n- Desktop、CLI、TUI 都消费同一 snapshot，不自行读取 provider 文档或拼 `/models`。\n\nProvider credential 缺失时，静态模型仍可展示为 `AuthenticationRequired/Unverified`；API 不返回\ncredential account 的秘密信息。普通客户端也不需要看到 opaque cache scope fingerprint。\n\n### 11.3 Agent 运行时\n\n`ResolvedModel` 在一次 model invocation 开始时进入 immutable `ModelInvocationSnapshot`：\n\n```text\nModelRef\n+ resolved limits/capabilities\n+ provider config revision\n+ catalog generation\n+ warnings/policy decisions\n```\n\n刷新 catalog 不修改 in-flight invocation。下一次调用若发现 selected model 已明确 retired 或不再\n满足强制能力，ThreadRuntime 在安全点返回 typed capability/config error，不能在后台静默切换到\n另一模型。\n\n## 12. 错误与降级\n\n建议错误分类：\n\n| 类别 | 对 cache 的处理 | 对用户的结果 |\n| --- | --- | --- |\n| Authentication / permission | 当前 availability 降为需认证或未验证；保留 metadata | 展示配置提示，不泄露响应 |\n| Discovery unsupported | negative cache；使用 static/config | 正常返回 `StaticOnly` + warning |\n| Rate limited / timeout / network | stale-if-error + backoff | 返回 stale snapshot 和刷新诊断 |\n| Provider 5xx | stale-if-error + backoff | 同上 |\n| Invalid provider payload | 不提交坏 snapshot；保留 last-known | provider schema error |\n| Pagination incomplete | 只可提交 Partial，或整体失败 | 不因缺席下架模型 |\n| Empty complete result | 提交空 live availability，保留 configured/history tombstone | 明确显示当前 scope 无可用模型 |\n| Cache corrupt/schema mismatch | 丢弃可删除 cache，重新发现 | 不影响 Config/Thread recovery |\n| User override invalid | 拒绝该配置事务 | typed config error |\n\n错误对象应携带 provider、scope-safe identity、phase、retryability 和 last-known freshness，但不得\n携带 API key、Authorization header 或未经裁剪的 provider body。\n\n## 13. 安全、隐私与可观测性\n\n### 13.1 安全\n\n- 仅对用户已配置或明确使用的 provider 发起 authenticated discovery；\n- 不在启动时并发扫描所有内置 provider；\n- scope fingerprint 使用抗碰撞 hash，不可由客户端反推出 credential reference；\n- provider raw payload 默认不落盘；\n- response logging 只记录大小、状态码、字段计数和 schema path；\n- 自定义 base URL 继续经过 provider config 的 URL validation 和后续网络 policy；\n- persisted cache 使用与本地配置相同或更严格的文件权限；\n- cache delete 是可恢复操作，不影响 durable Session/Thread。\n\n### 13.2 遥测\n\n建议指标：\n\n```text\ncatalog_read_total{provider,result=fresh|stale|miss|static}\ncatalog_refresh_total{provider,result}\ncatalog_refresh_duration_ms{provider}\ncatalog_models_observed{provider,coverage}\ncatalog_singleflight_join_total{provider}\ncatalog_snapshot_generation_total{provider}\ncatalog_metadata_conflict_total{provider,field}\ncatalog_stale_age_seconds{provider}\n```\n\n禁止把 exact custom endpoint、model ID、credential account 或 provider response text 默认放入\n低基数 telemetry label。详细诊断留在本地、受控日志。\n\n## 14. 目录结构与公开 API\n\n遵守 workspace 的小模块和公开 API 规则，建议：\n\n```text\nzeta-rs/models-manager/\n├── Cargo.toml\n├── README.md\n└── src/\n    ├── lib.rs                 # 只做显式 re-export\n    ├── manager.rs             # read / refresh / resolve orchestration\n    ├── source.rs              # ModelCatalogSource port + discovery values\n    ├── snapshot.rs            # immutable snapshot/generation\n    ├── cache.rs               # cache state + freshness\n    ├── merge.rs               # field-level provenance merge\n    ├── filter.rs              # query/filter/sort\n    ├── policy.rs              # named policies\n    ├── error.rs\n    ├── manager_tests.rs\n    ├── cache_tests.rs\n    ├── merge_tests.rs\n    └── filter_tests.rs\n```\n\n模块默认 private，仅导出调用方真正需要的 manager、port、query、policy 和 error。新增 public\ntrait 必须带 doc comments，说明实现者如何处理 scope、分页、coverage、cancellation 和 secret。\n\n`merge.rs`、`filter.rs` 尽量保持纯函数，使用固定 clock/source input，便于 property test 和\ntable-driven test。\n\n## 15. 测试要求\n\n### 15.1 契约测试\n\n每个动态 provider adapter 至少保存经过脱敏和裁剪的官方响应 fixture，验证：\n\n- endpoint 与认证/header 选择；\n- pagination；\n- 文档化及观察到的 cache header/validator 映射，缺失时回到 `NotSpecified`；\n- model ID 保真；\n- coverage 分类；\n- capability/limit mapping；\n- unknown field forward compatibility；\n- 401、429、5xx 和 schema error。\n\nFixture 不是 provider 当前模型列表的 authority，只用于 wire contract。\n\n### 15.2 Manager 测试\n\n必须覆盖：\n\n- fresh/stale/expired/cache miss 状态表；\n- 相同 scope singleflight、不同 scope 并行；\n- refresh 晚到响应不能覆盖新 config revision；\n- partial response 缺席不 tombstone；\n- complete response 缺席改变 availability 但保留 metadata；\n- `Unknown` 不覆盖 known；\n- live/curated/user override 冲突与 provenance；\n- 401、429、5xx、unsupported discovery 的不同退化；\n- deterministic generation 和排序；\n- `ListedOnly` / `AllowUnlisted` resolution；\n- custom compatible provider 无 `/models` 时仍可使用显式模型；\n- persisted cache schema mismatch 可安全丢弃；\n- telemetry/log 不含 secret fixture。\n\n测试 module 放在独立 sibling `*_tests.rs` 文件，并通过显式 `#[path = \"..._tests.rs\"]` 引入。\n\n### 15.3 集成测试\n\nApp Server 使用 fake clock、fake source 和 memory cache 验证：\n\n```text\nmodel/list(stale)\n→ 立即返回 stale generation N\n→ 后台 refresh\n→ generation N+1\n→ model/updated\n→ 下一次 list 返回 fresh N+1\n```\n\n不在普通 CI 依赖真实 provider 网络或真实 API key。真实 provider smoke test 必须 opt-in、限频且不\n执行 inference。\n\n## 16. 分阶段落地\n\n### 阶段 1：纯目录核心\n\n- ✅ 创建 `zeta-models-manager`；\n- ✅ 定义 source port、scope、snapshot、read/refresh policy；\n- ✅ 从 `ProviderDefinition.models` 读取 seed；\n- ✅ 实现 memory cache、singleflight、merge/filter/resolve；\n- ✅ 接入 fake source/fake clock 单元测试；\n- 部分具备：protocol 已补 availability/freshness/lifecycle/quality；完整 snapshot wrapper 随 Phase 3\n  App Server API 一并落地，当前 snapshot 是 manager 的进程内 immutable value。\n\n当前结果：无网络可以从统一 manager 获得确定 catalog，provider runtime 与 Local App Server 已迁移；\n其他产品 picker 尚未接入，因此跨 Desktop/CLI/TUI 的完成条件仍留在 Phase 3。\n\n### 阶段 2：高价值动态供应商\n\n按 metadata 质量和现有产品价值优先接入：\n\n1. OpenAI、Anthropic；\n2. Gemini、DeepSeek；\n3. Ollama；\n4. xAI、Hugging Face；\n5. Kimi、MiniMax、Qwen、Z.AI、MiMo 等先使用 curated/static source，在有官方、可验证\n   authenticated endpoint 后再接动态 discovery。\n\n每接入一个 provider，先建立第 6.2 节的验证记录，再实现 adapter。同时把 wire DTO/fixture 放到\n正确层，不在 manager 中写 provider switch。\n\n完成条件：动态列表失败时可稳定回退静态 snapshot；各 provider 的 coverage 与 Unknown 语义经过\ncontract test；未文档化 cache header 不成为正确性依赖。\n\n### 阶段 3：App Server 与客户端\n\n- 增加 `model/list`、`model/refresh`、`model/updated`；\n- Desktop/CLI/TUI picker 迁移到统一 snapshot；\n- provider 配置或凭据 revision 触发新 scope；\n- Agent runtime 在安全点使用 typed resolution。\n\n完成条件：所有产品入口看到相同目录、相同 warning 和相同排序，refresh 不影响 in-flight\ninvocation。\n\n### 阶段 4：持久缓存与维护流程\n\n- 增加可删除的 persisted observation cache；\n- 内置 metadata 加 source URL、reviewed_at 和维护校验；\n- 建立官方文档变化的人工/自动审计流程；\n- 根据真实指标调整 TTL、backoff 和 warm-up 范围。\n\n完成条件：重启/离线仍能快速展示 last-known catalog，cache 损坏可删除重建且不影响 durable\n产品状态。\n\n## 17. 固定决策\n\n1. `zeta-models-manager` 是 catalog control plane，不是 provider invocation runtime。\n2. 唯一模型 identity 是 exact `(ProviderId, ModelId)`。\n3. 可用性按 endpoint + credential/tenant + config revision scope 缓存。\n4. Provider list API 只声明它实际返回的字段；缺失字段保持 `Unknown`。\n5. 合并是字段级 patch + provenance，不是整条 `ModelInfo` 后写覆盖前写。\n6. 完整 discovery 的缺席与 partial discovery 的缺席语义不同。\n7. manager 使用 stale-while-revalidate、singleflight 和 typed read policy。\n8. 不用 inference probing 发现模型或能力。\n9. 静态 seed 不证明当前账号可用，动态列表也不一定提供完整能力 metadata。\n10. Catalog refresh 不修改已开始的 model invocation，也不静默替换用户选中的模型。\n11. Persisted catalog 是可删除 projection，不是 Config、Thread 或 billing authority。\n12. Provider runtime/认证留在 `zeta-model-provider`，wire endpoint/codec 留在 `zeta-api`，\n    raw HTTP/network policy 留在 `zeta-http-client`，retry/framing 留在 `zeta-client`。\n13. Prompt/context cache、stream heartbeat 和本地模型驻留不属于 catalog manager。\n14. Catalog TTL 是可配置的 Zeta policy；provider cache hint/validator 只有证据充分时才参与计算。\n15. Provider discovery 必须逐家验证；`../pi` 或 OpenAI-compatible 行为不能作为其他厂商的协议\n    证明。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-当前实现状态与仓库审计",
        "title": "2. 当前实现状态与仓库审计"
      },
      {
        "depth": 2,
        "id": "3-职责与非职责",
        "title": "3. 职责与非职责"
      },
      {
        "depth": 3,
        "id": "31-manager-拥有",
        "title": "3.1 Manager 拥有"
      },
      {
        "depth": 3,
        "id": "32-manager-不拥有",
        "title": "3.2 Manager 不拥有"
      },
      {
        "depth": 3,
        "id": "33-四类不能混用的缓存保活",
        "title": "3.3 四类不能混用的“缓存/保活”"
      },
      {
        "depth": 2,
        "id": "4-依赖方向与组合",
        "title": "4. 依赖方向与组合"
      },
      {
        "depth": 2,
        "id": "5-核心领域模型",
        "title": "5. 核心领域模型"
      },
      {
        "depth": 3,
        "id": "51-目录范围",
        "title": "5.1 目录范围"
      },
      {
        "depth": 3,
        "id": "52-发现结果",
        "title": "5.2 发现结果"
      },
      {
        "depth": 3,
        "id": "53-元数据-patch-与来源",
        "title": "5.3 元数据 patch 与来源"
      },
      {
        "depth": 3,
        "id": "54-快照",
        "title": "5.4 快照"
      },
      {
        "depth": 2,
        "id": "6-供应商发现策略",
        "title": "6. 供应商发现策略"
      },
      {
        "depth": 3,
        "id": "61-官方能力矩阵",
        "title": "6.1 官方能力矩阵"
      },
      {
        "depth": 3,
        "id": "62-供应商验证记录",
        "title": "6.2 供应商验证记录"
      },
      {
        "depth": 3,
        "id": "63-发现能力不是模型能力",
        "title": "6.3 发现能力不是模型能力"
      },
      {
        "depth": 3,
        "id": "64-禁止推理探测",
        "title": "6.4 禁止推理探测"
      },
      {
        "depth": 2,
        "id": "7-何时请求",
        "title": "7. 何时请求"
      },
      {
        "depth": 3,
        "id": "71-read-策略",
        "title": "7.1 Read 策略"
      },
      {
        "depth": 3,
        "id": "72-刷新触发条件",
        "title": "7.2 刷新触发条件"
      },
      {
        "depth": 3,
        "id": "73-singleflight-与并发",
        "title": "7.3 Singleflight 与并发"
      },
      {
        "depth": 2,
        "id": "8-目录缓存语义",
        "title": "8. 目录缓存语义"
      },
      {
        "depth": 3,
        "id": "81-两层缓存",
        "title": "8.1 两层缓存"
      },
      {
        "depth": 3,
        "id": "82-freshness-策略",
        "title": "8.2 Freshness 策略"
      },
      {
        "depth": 3,
        "id": "83-http-校验器与退避",
        "title": "8.3 HTTP 校验器与退避"
      },
      {
        "depth": 3,
        "id": "84-heartbeat-与健康恢复",
        "title": "8.4 Heartbeat 与健康恢复"
      },
      {
        "depth": 2,
        "id": "9-合并规则",
        "title": "9. 合并规则"
      },
      {
        "depth": 3,
        "id": "91-身份",
        "title": "9.1 身份"
      },
      {
        "depth": 3,
        "id": "92-字段优先级",
        "title": "9.2 字段优先级"
      },
      {
        "depth": 3,
        "id": "93-patch-规则",
        "title": "9.3 Patch 规则"
      },
      {
        "depth": 3,
        "id": "94-用户配置分层",
        "title": "9.4 用户配置分层"
      },
      {
        "depth": 2,
        "id": "10-筛选解析与排序",
        "title": "10. 筛选、解析与排序"
      },
      {
        "depth": 3,
        "id": "101-筛选流程",
        "title": "10.1 筛选流程"
      },
      {
        "depth": 3,
        "id": "102-解析不是简单列表查找",
        "title": "10.2 解析不是简单列表查找"
      },
      {
        "depth": 3,
        "id": "103-稳定排序",
        "title": "10.3 稳定排序"
      },
      {
        "depth": 2,
        "id": "11-向上暴露",
        "title": "11. 向上暴露"
      },
      {
        "depth": 3,
        "id": "111-协议值",
        "title": "11.1 协议值"
      },
      {
        "depth": 3,
        "id": "112-app-server-api",
        "title": "11.2 App Server API"
      },
      {
        "depth": 3,
        "id": "113-agent-运行时",
        "title": "11.3 Agent 运行时"
      },
      {
        "depth": 2,
        "id": "12-错误与降级",
        "title": "12. 错误与降级"
      },
      {
        "depth": 2,
        "id": "13-安全隐私与可观测性",
        "title": "13. 安全、隐私与可观测性"
      },
      {
        "depth": 3,
        "id": "131-安全",
        "title": "13.1 安全"
      },
      {
        "depth": 3,
        "id": "132-遥测",
        "title": "13.2 遥测"
      },
      {
        "depth": 2,
        "id": "14-目录结构与公开-api",
        "title": "14. 目录结构与公开 API"
      },
      {
        "depth": 2,
        "id": "15-测试要求",
        "title": "15. 测试要求"
      },
      {
        "depth": 3,
        "id": "151-契约测试",
        "title": "15.1 契约测试"
      },
      {
        "depth": 3,
        "id": "152-manager-测试",
        "title": "15.2 Manager 测试"
      },
      {
        "depth": 3,
        "id": "153-集成测试",
        "title": "15.3 集成测试"
      },
      {
        "depth": 2,
        "id": "16-分阶段落地",
        "title": "16. 分阶段落地"
      },
      {
        "depth": 3,
        "id": "阶段-1纯目录核心",
        "title": "阶段 1：纯目录核心"
      },
      {
        "depth": 3,
        "id": "阶段-2高价值动态供应商",
        "title": "阶段 2：高价值动态供应商"
      },
      {
        "depth": 3,
        "id": "阶段-3app-server-与客户端",
        "title": "阶段 3：App Server 与客户端"
      },
      {
        "depth": 3,
        "id": "阶段-4持久缓存与维护流程",
        "title": "阶段 4：持久缓存与维护流程"
      },
      {
        "depth": 2,
        "id": "17-固定决策",
        "title": "17. 固定决策"
      }
    ],
    "searchText": " 物理位置：zeta-rs/models-manager/ Rust crate：zetamodelsmanager 当前状态：Phase 1 core 已实现；动态 provider adapters、持久缓存和完整 App Server snapshot API 尚未实现 Crate 实现说明：zeta-models-manager README Canonical model contract：protocol.md Provider wire adapter：zeta-api.md Provider runtime：model-provider.md Operation client：zeta-client.md 底层网络：zeta-http-client README 官方资料核对日期：2026-07-25。Provider API 与模型生命周期会持续变化，本文固定架构边界和 合并语义；具体 endpoint、字段映射与内置 metadata 必须以实现时的官方文档和 contract test 为准。 快速理解 模型目录系统回答“当前有哪些模型可选、这些信息有多可信”；它管理发现、内存缓存和合并，不执行 模型调用。当前静态目录、动态 source port、snapshot/generation、singleflight、merge/filter/resolve 已经落地；真实 provider discovery 与完整客户端协议仍按后续阶段推进。 读者首先会问 直接答案 深入阅读 --- --- --- 模型列表来自哪里？ 供应商发现、内置资料、用户配置和本地状态按字段来源合并 核心领域模型 为什么某个模型没有出现？ 可能是供应商不支持发现、缓存尚未刷新、生命周期过滤或能力不匹配 供应商发现策略 什么时候访问供应商？ 依据新鲜度、显式刷新和缓存状态决定，并使用 singleflight 合并并发刷新 何时请求 模型目录能否证明模型支持某项能力？ 不能；缺失字段表示未知，不能被解释为明确不支持 发现能力不是模型能力 谁真正调用模型？ 已选模型交给模型运行时，目录系统不拥有请求、重试或传输 职责与非职责 1. 结论 zeta-models-manager 是 Zeta 的模型目录控制面。它统一管理各个 provider 可用模型的发现、 缓存、合并、筛选和面向上层的 catalog snapshot，负责决定： - 何时请求 provider，何时直接使用缓存或静态 metadata； - 如何把 provider 动态结果、内置 metadata、用户配置和本地模型状态合并； - 如何在“不知道”与“明确不支持”之间保持区别； - 如何按 Agent 所需能力、生命周期和用户策略筛选模型； - 如何向 App Server、Desktop、CLI、TUI 和 Agent runtime 暴露一致的模型信息。 它不是新的模型调用 client，也不是第二个 provider registry。边界固定为： ``text provider adapter 负责“如何向某家服务取回原始模型目录” models manager 负责“何时取、如何缓存、如何合并、如何解释和如何发布” model provider 负责“如何用已选模型执行一次调用” ` 模型列表接口不能被当作完整 metadata authority。各家返回信息差异很大： - OpenAI、DeepSeek 一类接口主要返回 ID、owner、created 等基础信息； - Anthropic、Gemini、xAI 能返回部分或较丰富的 capability、token limit、modality、 alias 或 pricing metadata； - Hugging Face 的 Hub catalog、Inference Providers router 和实际下游 provider 是不同 scope， coverage 需要单独声明； - 一些 provider 目前只有官方静态模型矩阵，没有文档化的 runtime catalog endpoint； - OpenAI-compatible 自定义服务对 GET /models 的支持和字段质量都不能假定。 因此 manager 必须做字段级 merge，并为每个字段保留来源与可信度；禁止因为某次响应没有某个 字段，就把已有的已知值覆盖成“不支持”，也禁止根据 model ID 字符串猜测工具、推理或上下文 能力。 2. 当前实现状态与仓库审计 当前模型相关职责分布如下： 位置 已有职责 不应继续扩张的方向 --- --- --- zeta-protocol::model::catalog identity、ModelInfo、capability、availability/freshness/lifecycle/quality value 请求调度、缓存、provider DTO、refresh state zeta-model-provider-config provider definition、endpoint/default、静态 seed models、配置归一化 HTTP、凭据读取、动态 discovery、TTL zeta-models-manager scope、静态 seed、memory cache、source port、singleflight、merge/filter/resolve、snapshot generation provider DTO、secret、调用、Config persistence、UI zeta-model-provider provider runtime、adapter 选择、模型调用、manager static resolution consumer catalog policy、跨 provider merge、UI 查询 zeta-api endpoint/request/event 的 Provider wire codec transport、retry、catalog authority、用户筛选 zeta-http-client HTTP/WebSocket execution、proxy/TLS/pool、transport diagnostics Provider DTO、catalog policy、模型选择 zeta-client operation retry、SSE/NDJSON framing、telemetry Provider DTO、catalog policy、模型选择 zeta-config 用户配置 authority、patch/merge/persistence provider 请求和进程内 refresh task App Server / clients 组合、RPC、展示与交互；Local App Server 已投影 shared manager 各自维护模型表或推断 capability ProviderDefinition.models 当前只作为启动 seed 和内置 metadata 来源，不兼任动态可用性缓存。 ModelProviderRuntime::resolvemodel 已消费 manager 的 static resolution；Local App Server 的 model/list 和 Session model validation 也使用同一个 manager clone，不再独立维护 ListedOnly / AllowUnlisted 判断。 当前实现边界如下： 能力 状态 实现证据 --- --- --- 静态 seed、确定排序、typed resolution ✅ ModelsManager:: staticsnapshot,liststatic,resolvestatic 动态 source port、scope 校验、partial/complete merge ✅ ModelCatalogSource、commitdiscovery、applydiscovery per-scope memory cache、freshness、singleflight ✅ ManagedScope、ScopeState、ModelsManager:: read,refresh 字段 provenance 与 Unknown 保留 ✅ CatalogRecord、ModelMetadataProvenance model-provider/App Server 静态目录统一 ✅ ModelProviderRuntime::modelsmanager、ConfigBackedModelService 真实 provider discovery adapters 尚未完成 Phase 2 persisted observation cache、backoff/jitter、并发总闸 尚未完成 Phase 4 / 后续 core hardening model/refresh、model/updated、完整 snapshot DTO 尚未完成 Phase 3 现有 ModelInfo 也存在后续需要修正的语义缺口： - 一个 ContextWindow 无法区分 total context、最大输入和最大输出； - capability 只覆盖 tools、reasoning、parallel tools 和 personality，无法表达 input modality、 structured output 或 workload kind； - 没有 availability、freshness、lifecycle、字段来源和诊断信息； - Option<u32 同时可能表示“未知”“未配置”或“不适用”。 这些缺口应通过 protocol value 演进解决，不能在 manager 对外 API 中重新发明一套同名 DTO。 3. 职责与非职责 3.1 Manager 拥有 - provider catalog scope 的稳定识别； - 首次加载、按需 refresh、显式 refresh 和后台 refresh 策略； - per-scope cache、TTL、stale-while-revalidate、stale-if-error； - per-scope singleflight、并发上限、退避和抖动； - provider discovery、内置 metadata、用户 override 和历史 cache 的字段级 merge； - model lifecycle、availability、metadata quality 和 freshness projection； - capability/workload/visibility filter； - ProviderId, ModelId 的解析、校验和稳定排序； - immutable ModelCatalogSnapshot 及 generation 变化； - cache/refresh/merge 的诊断信息和不含秘密的 telemetry。 3.2 Manager 不拥有 - API key、OAuth token、secret persistence 或 credential refresh； - provider HTTP DTO、认证 header、endpoint 拼接和分页 wire codec； - completions/responses/messages 请求执行； - prompt/context KV cache 的创建、breakpoint、TTL、usage 解析或计费语义； - SSE/NDJSON/WebSocket 的 ping、空行、注释帧、读超时和断线重连； - 本地模型进程的预热、驻留和卸载； - Config 文件 authority 或 provider definition registry 的注册事务； - 模型价格账单、quota authority 或 rate-limit enforcement； - Session、Thread、Turn 的 durable state； - UI 中 picker 的布局、搜索输入和用户交互； - 通过真实 inference 请求“试探”模型能力。 价格可以将来作为带来源和更新时间的展示 metadata 进入 snapshot，但不能被当作 billing authority；实际费用以 provider 账单和调用 usage 为准。 3.3 四类不能混用的“缓存/保活” 厂商文档中的 cache、keep-alive 和 heartbeat 不是同一个概念，必须按运行时边界拆开： 机制 含义 所属层 --- --- --- Catalog cache Zeta 缓存模型列表、availability 和 metadata observation zeta-models-manager Prompt/context cache 厂商复用 prompt prefix/KV tensor，影响推理延迟、费用和 usage zeta-api provider adapter Stream liveness raw byte activity/读超时属于 zeta-http-client；SSE/NDJSON frame activity 属于 zeta-client Provider event 语义由 zeta-api 解释 Model residency 本地模型是否继续驻留 CPU/GPU 内存 zeta-model-provider 本地 runtime；wire 参数仍由 zeta-api 编码 官方行为已经证明这些机制不能抽象成 manager 中的统一 heartbeat： - Claude streaming 允许在 SSE 消息事件之间发送 ping； - DeepSeek request keep-alive 对非流式 请求发送空行，对流式请求发送 SSE : keep-alive 注释； - Ollama streaming 使用 application/x-ndjson，而请求参数 keepalive 表示模型在内存中的驻留时间。 Prompt cache 同样是调用语义而非 catalog cache： OpenAI Prompt Caching、 Claude Prompt Caching 和 Gemini Context Caching 的启用方式、TTL、 breakpoint 和 usage 字段均不同。Manager 最多把“某模型是否支持某类 prompt cache”作为带来源的 capability metadata 暴露，不能创建、续期或命中厂商推理 cache。 具体依赖边界固定为： - zeta-protocol 只在确实需要跨 crate/进程表达时定义 provider-independent 的 cache intent、 normalized usage 和 stream event value； - zeta-api 将 canonical request 转成 promptcachekey、cachecontrol、cachedcontent 等 provider wire 字段，解析 cache usage，并过滤/解释 provider-specific 心跳帧； - zeta-model-provider 选择具体 zeta-api endpoint/profile，提供 resolved endpoint、固定 header 和 retry policy，但不解释某家厂商的 cache breakpoint、TTL 或 SSE event； - zeta-http-client 执行 HTTP、transport timeout 和 diagnostics；zeta-client 执行 operation retry 与 SSE/NDJSON framing，但都不解释 Provider event； - zeta-models-manager 只维护模型是否支持相关能力的 catalog metadata，不参与一次调用的 cache 生命周期或流式连接。 4. 依赖方向与组合 目标依赖关系中，箭头表示“依赖”： `text zeta-models-manager ├──▶ zeta-protocol └──▶ zeta-model-provider-config zeta-model-provider ├──▶ zeta-model-provider-config ├──▶ zeta-models-manager implements ModelCatalogSource ├──▶ zeta-api ───▶ zeta-client ├──▶ zeta-client ├──▶ zeta-http-client └──▶ zeta-secrets direct-provider credential only App Server composition ├──▶ zeta-model-provider └──▶ zeta-models-manager zeta-codex-app-server └──▶ zeta-model-provider subscription runtime/catalog observation adapter ` 具体规则： - zeta-models-manager 可依赖 zeta-protocol 和 zeta-model-provider-config； - zeta-models-manager 不依赖 zeta-model-provider、zeta-api、zeta-client、 zeta-http-client、zeta-secrets 或 App Server； - manager 定义并拥有 ModelCatalogSource port，因为它是该 port 的消费者； - zeta-model-provider 实现该 port，复用已有 adapter、normalized endpoint、operation/HTTP clients 和 credential materialization； - App Server composition root 将同一个 provider runtime 分别作为模型调用能力和 catalog source 注入，不能再建第二个 HTTP client registry； - provider-specific discovery DTO 留在 zeta-api 或 zeta-model-provider 的私有 adapter module，不能进入 protocol。 Google 和 Ollama 说明了为什么 discovery endpoint 不能从调用 base URL 盲目拼接： - 当前 Google 调用走 .../v1beta/openai，但原生模型目录是 generativelanguage.googleapis.com/v1beta/models； - 当前 Ollama OpenAI-compatible 调用走 http://localhost:11434/v1，但本地模型目录是 /api/tags，单模型详情是 /api/show。 Provider definition 应显式声明 discovery strategy 或由具体 adapter 解析，禁止 manager 用 trimendmatches \"/v1\" 一类字符串启发式生成 endpoint。 5. 核心领域模型 以下是目标语义，不要求第一版逐字采用这些 Rust 名称。 5.1 目录范围 模型可用性不仅取决于 ProviderId，还可能取决于： - normalized base URL 和 region； - credential account、organization、project 或 tenant； - provider 配置 revision； - 本地 Ollama daemon 或自定义 gateway 实例； - Hugging Face 的 provider routing/account 权限。 因此 cache key 不能只是 ProviderId： `rust pub struct CatalogScopeKey pub provider: ProviderId, pub sourcescope: CatalogSourceScopeId, ` CatalogSourceScopeId 是由 composition/provider layer 生成的 opaque、稳定 fingerprint。它可以 包含 endpoint identity、credential reference identity 和 config revision 的 hash，但不得包含、 记录或向客户端暴露 secret、token、完整认证 header。 凭据内容轮换但 identity 不变时，credential layer 必须提供 revision，使新旧权限不会错误复用 同一 cache。旧 scope 由 LRU/保留期回收，不做危险的全局 cache clear。 5.2 发现结果 Source port 返回 provider-neutral、带覆盖范围的结果： `rust pub struct DiscoveredCatalog pub scope: CatalogScopeKey, pub coverage: DiscoveryCoverage, pub models: Vec<DiscoveredModel , pub validator: Option<CatalogValidator , pub cachehint: CatalogCacheHint, pub fetchedat: SystemTime, pub enum DiscoveryCoverage CompleteFor ModelWorkload , Partial, pub enum CatalogCacheHint NotSpecified, RevalidateAfter Duration , ImmutableUntilSourceRevision, ` 关键语义： - CompleteFor AgentText 表示“本响应完整覆盖该 credential/scope 下可用于 Agent text workload 的模型”，此时缺席才可影响 availability； - Partial 表示搜索结果、单模型补充信息或 provider 明确不保证完整性，缺席不能形成 tombstone； - CatalogValidator 表达 ETag、Last-Modified 或 provider revision，而不是随意的字符串； - CatalogCacheHint 只转述官方协议或实际 HTTP 响应给出的证据；manager 会用本地最小/最大 freshness、错误退避和用户策略计算最终刷新时间； - provider 没有文档化 cache header、revision 或刷新周期时必须返回 NotSpecified，不能根据 其他 OpenAI-compatible 服务的行为类推； - raw response、认证 header 和 provider request ID 不进入 snapshot。 5.3 元数据 patch 与来源 Discovery、内置 catalog 和配置 override 都转换为 patch，而不是直接构造最终 ModelInfo： `rust pub struct SourcedModelPatch pub model: ModelRef, pub source: MetadataSource, pub fields: ModelMetadataPatch, pub enum MetadataSource ProviderLive, UserConfigured, BuiltinCurated, ProviderSeed, PersistedObservation, ` 每个可合并字段内部保留 value + source + observedat。对外可以压缩为 MetadataQuality 和少量 warnings，但诊断接口必须能解释“这个 200K context 从哪里来”。 CapabilitySupport:: Supported, Unsupported, Unknown 的三态必须保留。Unknown 不是 Unsupported，缺失字段也不是否定证据。 5.4 快照 所有消费者读取 immutable snapshot： `rust pub struct ModelCatalogSnapshot pub scope: CatalogScopeKey, pub generation: u64, pub freshness: CatalogFreshness, pub entries: Vec<ModelCatalogEntry , pub warnings: Vec<CatalogWarning , pub enum CatalogFreshness Fresh, StaleUsable, Expired, StaticOnly, ` 同一 scope 只有在 consumer-visible 内容变化时才递增 generation。单纯 refresh 得到完全相同的 结果，只更新内部 observation time，不制造无意义的 model/updated。 排序必须确定，snapshot 中 model ID 不得重复。相同 generation 的序列化结果必须稳定，便于 App Server、Desktop 和 contract tests 比较。 6. 供应商发现策略 6.1 官方能力矩阵 Discovery mode 描述 Zeta 如何组合实时 availability 与 curated metadata，不代表厂商承诺了 客户端刷新频率。 Provider Discovery mode 推荐 discovery 官方响应可提供的信息 初始策略 --- --- --- --- --- OpenAI Hybrid GET /v1/models ID、created、owner；官方描述为基础信息 动态 availability + 内置能力 metadata Anthropic Dynamic/Hybrid GET /v1/models ID、display name、token limits，并可返回 thinking、effort、image、structured output 等 capability 优先使用动态字段，内置补缺 Google Gemini Dynamic/Hybrid models.list display name、input/output limit、generation methods、thinking 等 使用原生 Gemini endpoint，不走 OpenAI-compatible base URL xAI Dynamic/Hybrid GET /v1/language-models modality、aliases、fingerprint、部分价格；比 /v1/models 丰富 Agent text 使用 language-models Kimi Unknown/StaticOnly 当前可访问的官方平台文档未确认本设计所需的 authenticated list contract Unknown 使用 curated metadata；取得官方 reference/fixture 前不猜 /models DeepSeek Hybrid GET /models 基础 ID、owner 动态 availability + 内置能力 metadata Ollama Dynamic local GET /api/tags + 按需 /api/show 本地已安装"
  },
  {
    "slug": "model-provider",
    "sourcePath": "docs/model-provider.md",
    "title": "模型调用系统",
    "description": "模型调用系统把用户选择的模型变成一次可执行调用：先确定供应商、模型、服务地址和凭据，再把 请求交给协议、重试和网络层。配置在调用开始时冻结，因此一次调用不会在执行途中悄悄换模型或 凭据。",
    "group": "模型与配置",
    "markdown": "> - 物理位置：`zeta-rs/model-provider/`\n> - Rust crate：`zeta_model_provider`\n> - 层次：Provider 运行时与选择层\n> - 当前状态：completion runtime 已直接组合 `zeta-api` endpoint profile、`zeta-client` operation\n>   retry/stream framing 与 `zeta-http-client` transport；OpenAI Responses、OpenAI-compatible Chat\n>   Completions、Anthropic Messages 已使用原生 wire streaming；semantic OpenAI API-key\n>   materialization 已有 host-injected `SecretStore` 路径，持久化 credential 产品闭环尚未实现\n> - Crate 实现与 adapter 调用图：[`zeta-rs/model-provider/README.md`](../zeta-rs/model-provider/README.md)\n> - 声明配置层：[`model-provider-config.md`](model-provider-config.md)\n> - API 协议层：[`zeta-api.md`](zeta-api.md)\n> - Operation client：[`zeta-client.md`](zeta-client.md)\n> - 底层网络：[`zeta-http-client` README](../zeta-rs/http-client/README.md)\n> - Secret persistence：[`secrets.md`](secrets.md)\n> - Interactive login control plane：[`login.md`](login.md)\n> - ChatGPT/Codex subscription runtime：[`codex-app-server.md`](codex-app-server.md)\n\n## 快速理解\n\n模型调用系统把用户选择的模型变成一次可执行调用：先确定供应商、模型、服务地址和凭据，再把\n请求交给协议、重试和网络层。配置在调用开始时冻结，因此一次调用不会在执行途中悄悄换模型或\n凭据。\n\n| 常见问题 | 系统行为 | 深入阅读 |\n| --- | --- | --- |\n| 本次到底使用哪个模型？ | 根据供应商定义、用户选择和本次调用覆盖生成不可变绑定 | [一次调用如何形成](#1-一次调用如何形成) |\n| 自定义服务地址会影响什么？ | 只在供应商配置明确允许时生效，不会把一个服务偷偷当成另一个协议 | [供应商与 API 端点](#5-供应商与-api-端点的联动) |\n| 凭据由这里保存吗？ | 不保存；这里只取得本次调用所需的凭据，保存和登录属于相邻系统 | [供应商凭据](#6-供应商凭据与-codex-边界) |\n| 失败后会自动重试吗？ | 只有调用类型明确允许安全重试时才会重试；模型推理默认不能仅凭“没收到输出”重跑 | [重试分工](#8-重试分工) |\n| 当前已经能做什么？ | 已具备三种 endpoint 的 unary/streaming completion 与 embedding/rerank；持久化凭据闭环尚未完成 | [当前实现审计](#3-当前实现审计) |\n\n## 1. 一次调用如何形成\n\n```mermaid\nflowchart TD\n    request[\"Agent 发起模型调用\"] --> select[\"读取用户选择和调用覆盖\"]\n    select --> definition[\"校验供应商定义与模型能力\"]\n    definition --> identity[\"解析 direct-provider API key 或云身份\"]\n    identity --> credential[\"凭据系统提供本次调用材料\"]\n    credential --> binding[\"冻结模型、端点、凭据范围与重试策略\"]\n    binding --> encode[\"协议层编码请求\"]\n    encode --> operation[\"操作层执行截止时间、重试和流式分帧\"]\n    operation --> transport[\"网络层完成 HTTP / WebSocket 传输\"]\n    transport --> result[\"返回统一模型结果\"]\n```\n\n这条流程中，模型调用系统只拥有“选择并冻结本次调用绑定”。协议层解释请求和响应，操作层处理\n安全重试与分帧，网络层负责真实连接；任何一层都不能根据 URL 或模型名称重新猜测上层已经作出\n的选择。\n\nChatGPT/Codex subscription 不是 raw model invocation：上游 Codex 同时拥有 Agent/tool/approval\n循环，因此产品在 Core 层选择独立的 `TurnExecutionBackend`，不把它塞进本流程。\n\n一次绑定必须明确回答：\n\n- 使用哪个供应商和模型；\n- 使用哪个 API 配置档案、服务地址和部署目标；\n- 使用哪种凭据来源和作用范围；\n- 哪些调用默认值与供应商规则生效；\n- 这类操作能否安全重试。\n\n## 2. 谁负责什么\n\n| 责任 | 最终所有者 | 模型调用系统在其中做什么 |\n| --- | --- | --- |\n| 供应商和模型选择 | 模型调用系统 | 解析选择并生成不可变调用绑定 |\n| 合法配置形态 | 模型供应商配置 | 提供定义，模型调用系统只消费 |\n| 凭据保存和登录 | 凭据与登录系统 | 请求本次需要的敏感材料，不自行持久化 |\n| API 请求和响应含义 | 模型 API 协议层 | 选择明确的协议配置档案 |\n| 重试、截止时间和流式分帧 | 操作客户端 | 选择重试策略，不执行重试循环 |\n| HTTP、代理和 TLS | 网络层 | 提供解析后的目标和标头 |\n| 模型目录刷新 | 模型目录系统 | 提供目录访问适配器，不保存第二份缓存 |\n\n### 2.1 本系统拥有\n\n- 供应商运行时注册与适配器选择；\n- 规范化配置到不可变运行时的解析；\n- 供应商、模型和 API 配置档案的组合；\n- 服务地址、部署、账户与区域作用范围的最终解析；\n- 凭据引用到本次调用材料的绑定；\n- 供应商级调用默认值与重试策略选择；\n- 模型调用器 `ModelInvoker` 的生命周期；\n- 协议、操作客户端和共享网络客户端的组合；\n- 模型目录系统所需的运行时适配器。\n\n### 2.2 本系统不拥有\n\n- 可持久化配置模式和内置供应商定义；\n- 供应商请求、响应和流式事件的数据结构；\n- `/responses`、`/messages` 等 relative path；\n- JSON、SSE 事件或 NDJSON 对象的协议解释；\n- DNS/TCP/TLS/proxy/connection pool；\n- 重试循环、退避等待和 `Retry-After` 处理；\n- 流式分帧、字节缓冲和空闲计时；\n- 模型目录刷新、缓存、合并与筛选；\n- Agent 循环、工具循环和 Thread/Turn 持久状态；\n- 浏览器登录、OAuth 回调、令牌刷新撤销和上游凭据保存。\n\n## 3. 当前实现审计\n\n当前实现已经具备正确的基本方向：\n\n- `ModelProviderRuntime` 拥有配置 registry 和 lazy transport owner；\n- `Provider` 保存 normalized config、definition 和 adapter；\n- `ModelInvoker` 表达不可变 Provider/model selection；\n- `src/providers/` 按外部服务组织 runtime adapter；\n- `NormalizedModelProviderConfig::api_profile` 显式选择 `ApiEndpoint`；\n- `SemanticRuntimeResolver` 将 OpenAI-compatible/OpenAI/Ollama 的 exact config 解析为 immutable embedding/rerank invoker；\n- `model-provider → zeta-api`、`zeta-client` 和 `zeta-http-client`，没有反向依赖。\n\n已移除的重复分派为：\n\n```text\nmodel-provider-config::ProviderAdapter\n        ↓\nmodel-provider::providers::instantiate\n        ↓\nzeta-api::Api::{OpenAi, DeepSeek, Google, ...}\n```\n\n`zeta-api::Api` 曾复制了一次 Provider registry。现在 `zeta-api` 仅公开 endpoint profile；\n`model-provider::providers/` 保留唯一的 Provider runtime 选择，并把声明式 `ApiProfile` 映射为\n`ApiEndpoint`。\n\n`HttpClient` 和 `UreqHttpClient` 定义在 `zeta-http-client`。Runtime 持有共享 lazy operation client：\nApp Server 启动不构造 socket/TLS backend，第一次真实 operation 才 fallibly 创建 transport；API codec\n构造 opaque byte request，`zeta-client` 组合 retry/framing，底层保留 status/header/body transport evidence。\n\n## 4. 运行时解析流程\n\n```text\nModelRuntimeRequest\n  ├─ ModelRef\n  ├─ ModelProviderConfig\n  └─ invocation policy\n        │\n        ▼\nProviderConfigRegistry::normalize\n        │\n        ▼\nProvider runtime adapter\n  ├─ resolve credential\n  ├─ resolve target/deployment\n  ├─ choose API profile\n  ├─ choose retry policy\n  └─ bind zeta-api endpoint + zeta-client\n        │\n        ▼\nArc<dyn ModelInvoker>\n```\n\n解析结果必须不可变。配置或 credential revision 变化后创建新的 runtime，不在飞行中的调用对象\n上做可变热更新。\n\n## 5. 供应商与 API 端点的联动\n\nProvider 名称不能等同于 API 协议。同一 Provider 可以选择多个正式 endpoint：\n\n| Provider | 可选 API profile 示例 |\n| --- | --- |\n| OpenAI Platform | Responses |\n| ChatGPT/Codex subscription | upstream Codex App Server 的 thread/turn 与公开 capability（独立 runtime） |\n| xAI | Responses、Chat Completions |\n| Google | Gemini Interactions、GenerateContent、OpenAI-compatible Chat |\n| MiniMax | Anthropic Messages、OpenAI-compatible Chat |\n| Ollama | native Chat NDJSON、OpenAI-compatible Chat |\n| DeepSeek | OpenAI Chat、Anthropic-compatible endpoint |\n\n### 5.1 Input-token 计量矩阵\n\n这里的“支持”只表示调用前 budget measurement，不是调用完成后的 response usage。所有 remote\nbinding 都由 `ProviderDefinition.input_token_count` 明确声明 profile、target 和 model policy；未声明\n时 fail closed，不能因为 Chat Completions 外形兼容就推断存在 count endpoint。\n\n| Provider | 官方计量面 | 当前 Zeta 状态 | 边界 |\n| --- | --- | --- | --- |\n| OpenAI | [`POST /responses/input_tokens`](https://developers.openai.com/api/reference/resources/responses/subresources/input_tokens) | ✅ exact remote | 与 Responses request 同 codec；直接读取 `input_tokens` |\n| Anthropic | [`POST /v1/messages/count_tokens`](https://docs.anthropic.com/en/api/messages-count-tokens) | 部分具备：estimated remote | provider preflight 后按 1%/至少 32 tokens 保守记账 |\n| Google | [`models.countTokens`](https://ai.google.dev/api/tokens) | 部分具备：estimated remote | native `generateContentRequest`；当前 invocation 是 OpenAI-compatible，且仅声明 model 可用 |\n| Kimi | [`estimate-token-count`](https://platform.kimi.ai/docs/api/estimate) | 部分具备：estimated remote | 文档 schema 只有 model/messages；带 tools/reasoning 的当前请求退回 unavailable |\n| Z.AI | [`POST /tokenizer`](https://docs.z.ai/api-reference/tools/tokenizer) | 部分具备：estimated remote | 使用 `usage.total_tokens`，支持 tools；带 Tool Call/Result 历史暂退 unavailable |\n| xAI | [`tokenize-text`](https://docs.x.ai/developers/rest-api-reference/inference/other) | ❌ full-request preflight unavailable | 只 tokenize 裸文本；[billing FAQ](https://docs.x.ai/developers/faq/billing) 说明 inference 还会加入预定义 tokens |\n| Qwen | [text generation](https://help.aliyun.com/en/model-studio/text-generation) | ❌ preflight unavailable | chat template 会增加控制 token，不能按裸文本计数 |\n| DeepSeek | [token usage / offline tokenizer](https://api-docs.deepseek.com/quick_start/token_usage) | 部分具备：local/estimated | 已接入完整 `ModelRef` binding、请求级模板渲染与 tokenizer runtime；当前仍需宿主提供固定资产清单 |\n| Ollama | [API usage fields](https://github.com/ollama/ollama/blob/main/docs/api.md) | ❌ preflight unavailable | `prompt_eval_count` 是调用完成后的 usage |\n| Hugging Face Router | [per-model tokenizer API](https://huggingface.co/docs/tokenizers/main/api/tokenizer) | 部分具备：local/estimated | 公共 `owner/repo` 首次使用时解析 immutable commit，按需下载并校验 `tokenizer.json`、`tokenizer_config.json` 与 standalone template；重启复用磁盘缓存 |\n| MiniMax | [Chat API](https://platform.minimaxi.com/docs/api-reference/text-post) | ❌ verified preflight unavailable | 当前官方文档只确认 response usage |\n| MiMo | [Responses API](https://mimo.mi.com/docs/en-US/api/chat/responses) | ❌ verified preflight unavailable | 当前官方文档只确认 response `usage.input_tokens` |\n| Generic OpenAI-compatible | 无统一标准 | ❌ unavailable | 必须由具体 provider definition 显式增加 count profile |\n\n目标调用形态：\n\n```rust\nlet binding = ApiBinding::OpenAiChat {\n    endpoint: zeta_api::endpoint::OpenAiChat::deepseek(),\n    target: resolved_target,\n};\n```\n\n以上只是语义示例。关键约束：\n\n- Provider module 选择 endpoint/profile；\n- `zeta-api` 实现 request/response/event codec；\n- `zeta-client` 组织 operation attempt，并通过 `zeta-http-client` 执行；\n- runtime 不根据 URL 或 model ID 猜 profile；\n- profile 变化必须是显式配置或 built-in definition 变化；\n- 已有配置不能静默迁移到另一正式 API。\n\n### 5.2 OpenAI 服务接口面不是 OpenAI-compatible 配置档案\n\n公开 OpenAI Platform API 与 ChatGPT/Codex service 都可能使用 `responses`、`models` 或 realtime\n这样的相对 path，但它们的 base URL、credential、可用 operation 和 wire 差异不能由 path 推断。\n上游 Codex 的 memories/search 等能力是 subscription runtime capability，不是 `zeta-api` endpoint；\n详见 [`zeta-api.md`](zeta-api.md#45-openai-platform-与-chatgptcodex-endpoint-清单)。\n\n因此 direct-provider runtime binding 包含如下事实：\n\n```rust\npub struct OpenAiExecutionBinding {\n    endpoint: zeta_api::ApiEndpoint,\n    target: ResolvedApiTarget,\n    credential_scope: CredentialScope,\n}\n```\n\nChatGPT/Codex subscription 不是 `OpenAiCompatibleAdapter` 的用户自定义 base URL 选项。Platform API\nkey、custom-compatible credential 与 ChatGPT/Codex runtime 彼此不能复用或降级转换。\n\nChatGPT/Codex subscription 不构造 `ResolvedApiTarget + Bearer token` binding。它只能由已认证的\n[`zeta-codex-app-server`](codex-app-server.md) runtime 执行；上游 Codex 选择 allow-listed target、管理\ncredential refresh，并执行实际 backend request 和 Agent loop。\n\n## 6. 供应商凭据与 Codex 边界\n\nProvider 的共同点只到“调用前需要可用身份”为止。API key、AWS credential chain、Google ADC、\nMicrosoft identity 和签名请求仍由本 crate 的 direct-provider runtime materialize；它们的 secret\nbytes 可以保存在 [`zeta-secrets`](secrets.md)，但浏览器 login 不属于本 crate。\n\nChatGPT/Codex subscription 是独立 runtime：\n\n```text\nzeta-app-server → zeta-login → zeta-codex-app-server → upstream Codex App Server\n                         │\n                         └─ implements Core TurnExecutionBackend\n```\n\n`TurnExecutionBackend` 是 Core 消费的窄 port。它接收已创建的 durable Turn，并把完整远端执行结果\n投影回 Core；它不接受 raw access token、header map 或 arbitrary ChatGPT base URL。产品 composition\n根据持久化 Turn model 的 exact provider 显式选择 default `TurnExecutor` 或 Codex backend；\n`openai-chatgpt` 由上游 account-filtered model catalog 提供，`zeta-model-provider` 不参与远端 Agent loop。\n\n401 recovery 也按身份所有者处理：direct-provider credential 可由其 provider runtime 做一次受限\nrefresh/rebuild；Codex subscription 由 upstream Codex 自己刷新，Zeta 只接收 reauthentication-required\n或稳定 execution error。`zeta-client` 不读取 secrets，也不自行刷新或重试认证。\n\n## 7. 标头和目标\n\n| 内容 | Owner |\n| --- | --- |\n| Platform/default provider base URL | `model-provider-config` |\n| 用户 base URL override | 仅 Platform/custom-compatible provider；由 `model-provider-config` 声明、runtime 解析 |\n| ChatGPT/Codex service target | upstream Codex App Server；不接受 Zeta generic user override |\n| resolved absolute target | `model-provider` |\n| API key/cloud identity/tenant header | `model-provider` + direct-provider credential layer |\n| relative path、method、content type | `zeta-api::endpoint` |\n| API version、协议 beta header | `zeta-api::endpoint/requests` |\n| trace propagation、user agent | `zeta-http-client` |\n| operation attempt header | `zeta-client` |\n\nRuntime 合并 headers 时必须使用 typed origin 和冲突规则。认证 header 不得被协议层覆盖，协议\n必需 header 不得被用户任意删除；所有 secret header 的 `Debug` 必须脱敏。\n\n## 8. 重试分工\n\n`zeta-client` 拥有 retry 机制，但 runtime 选择 retry policy：\n\n```text\nmodel-provider\n  选择 Never / SafeRead / ExplicitIdempotency 等策略\n        ↓\nzeta-client\n  执行 attempt loop、backoff、jitter、Retry-After 和 telemetry\n        ↓\nzeta-api\n  提供 provider error/status 的事实分类\n```\n\nInference 通常是 POST，不能仅因“尚未收到 token”就假定安全重试。默认规则：\n\n- 没有 Provider 明确 idempotency contract 时，inference 使用 `Never`；\n- catalog GET 可以使用有上限的 `SafeRead`；\n- 已产生 semantic output 后禁止透明 retry；\n- direct-provider credential error、validation error 不重试；\n- Codex subscription refresh 和 upstream process outcome 由 `zeta-codex-app-server` 与 upstream\n  Codex contract 处理，不能套用 HTTP retry；\n- fallback model/provider 是 Agent policy，不是 client retry；\n- runtime 只选择 typed policy，不自己 sleep 或写 attempt loop。\n\n## 9. 流式处理分工\n\n```text\nzeta-client (direct-provider path)\n  从 zeta-http-client 消费 HTTP byte stream\n  → SSE/NDJSON framing\n  → idle deadline / cancellation / backpressure\n        ↓\nzeta-api::sse\n  provider event decode\n  → ping/comment/terminal interpretation\n  → canonical ModelStreamEvent\n        ↓\nmodel-provider\n  attach provider/model identity\n  → ModelInvoker stream\n```\n\nDeepSeek `: keep-alive` 的 frame 边界由 client 识别，作为无 payload 的 SSE comment 交给协议层；\n协议层确认它不形成 model output。Anthropic `event: ping` 同样由协议层过滤。Runtime 不解析\n`data:` 行或 provider event JSON。\n\n## 10. 目录来源\n\n`zeta-model-provider` 是 models manager 与实际 Provider network runtime 的连接点：\n\n```text\nmodels-manager::ModelCatalogSource\n        ▲ implemented by\nmodel-provider\n  ├─ resolved catalog target\n  ├─ credential scope\n  ├─ zeta-api catalog request codec\n  └─ zeta-client operation → zeta-http-client HTTP execution\n```\n\nRuntime 负责 scope identity、credential revision 和 catalog API binding；manager 负责何时刷新、\n缓存、merge 和发布 snapshot。Runtime 不维护第二份 catalog cache。\n\nCodex subscription 的模型/额度 observation 只能由 `zeta-codex-app-server` 在上游公开 account 或\nmodel operation 支持的范围内提供；它不使 models manager 直读或猜测 ChatGPT backend catalog。\n\n## 11. 依赖方向\n\n箭头表示“依赖”：\n\n```text\nzeta-model-provider\n  ├──▶ zeta-model-provider-config\n  ├──▶ zeta-api ───▶ zeta-client\n  ├──▶ zeta-client\n  ├──▶ zeta-http-client\n  ├──▶ zeta-secrets\n  └──▶ zeta-protocol\n\nzeta-codex-app-server\n  ├──▶ zeta-login\n  ├──▶ zeta-core                 # implements TurnExecutionBackend\n  └──▶ zeta-protocol\n\nApp Server composition\n  ├──▶ zeta-model-provider\n  ├──▶ zeta-login\n  └──▶ zeta-codex-app-server\n```\n\n更准确地说：\n\n- `zeta-client` 不依赖 Provider、API codec、Core 或 config；\n- `zeta-http-client` 不依赖 Provider、API codec、Core、config 或 secret store；\n- `zeta-api` 可依赖 `zeta-client` 的 operation/SSE value；\n- `zeta-model-provider` 依赖 config、API、operation client、HTTP client、secrets 和 protocol；\n  它不定义或消费完整 Agent-loop backend；\n- config 不反向依赖 runtime；\n- API/client 都不反向依赖 model-provider。\n- `zeta-codex-app-server` 实现 interactive login 与 Core `TurnExecutionBackend`；\n- Zeta App Server 只组合和映射 redacted control-plane DTO，不实现 OAuth host；\n- model-provider 不依赖 App Server、Desktop、CLI 或 TUI。\n\n## 12. 目标目录\n\n```text\nzeta-rs/model-provider/\n├── BUILD.bazel\n├── Cargo.toml\n├── README.md\n└── src/\n    ├── lib.rs\n    ├── runtime/\n    │   ├── mod.rs\n    │   ├── provider.rs\n    │   ├── model_invoker.rs\n    │   ├── resolution.rs\n    │   └── runtime_tests.rs\n    ├── binding/\n    │   ├── mod.rs\n    │   ├── api.rs\n    │   ├── target.rs\n    │   ├── headers.rs\n    │   ├── retry.rs\n    │   └── binding_tests.rs\n    ├── credential/\n    │   ├── mod.rs\n    │   ├── api_key.rs\n    │   ├── materialized.rs\n    │   ├── recovery.rs\n    │   ├── cloud/\n    │   └── credential_tests.rs\n    ├── providers/\n    │   ├── mod.rs\n    │   ├── openai.rs\n    │   ├── anthropic.rs\n    │   ├── google.rs\n    │   ├── deepseek.rs\n    │   ├── ollama.rs\n    │   └── ...\n    ├── catalog/\n    │   ├── mod.rs\n    │   ├── source.rs\n    │   └── catalog_tests.rs\n    └── error.rs\n```\n\nProvider module 只保留确实属于 runtime selection 的差异。Request DTO、SSE event struct 和 HTTP\nclient implementation 不得放入 `providers/`。\n\n## 13. 公共接口\n\nPublic API 只暴露：\n\n- `ModelProvider`；\n- `ModelProviderRuntime` 或更窄的 runtime factory；\n- immutable `ModelInvoker`；\n- `ModelRuntimeRequest`；\n- 安全的 `ModelProviderError`；\n- models manager 所需的 source adapter。\n\nProvider adapter trait 默认 crate-private。新 public trait 必须写 doc comment，说明配置 snapshot、\ncredential、cancellation、retry 和 secret redaction 责任。\n\nPublic API 不导出 ChatGPT token、PKCE verifier、通用 secret header map 或 `SecretStore`。登录账户\nprojection 由 [`zeta-login`](login.md) 提供；App Server 只读取该 redacted projection。\n\n## 14. 迁移顺序\n\n1. 建立 `zeta-http-client` 的 unary request/response/config port；\n2. 将当前 `zeta-client::UreqHttpClient` 和 raw transport value 迁入底层 crate；\n3. 让 `zeta-client` 通过共享 transport 执行 operation retry/framing；\n4. 将 `zeta-api::Api` 改为 endpoint/profile API；\n5. `model-provider/src/providers/*` 直接选择 endpoint/profile；\n6. 增加 streaming binding；\n7. 以 OpenAI API key 建立第一个 direct-provider credential vertical slice；\n8. 建立 `zeta-login` 与 `zeta-codex-app-server`，以 upstream managed ChatGPT login 和 Core\n   `TurnExecutionBackend` 接入第一个完整 Codex Turn vertical slice；\n9. 实现 models manager 的 catalog source；\n10. 删除旧 Provider 级双重 dispatch。\n\n迁移期间不创建空模块，也不同时保留两套长期 public facade。\n\n## 15. 固定决策\n\n1. Provider registry 只存在于 `zeta-model-provider`。\n2. Runtime 选择 API endpoint，但不实现 wire codec。\n3. Runtime 选择 retry policy，但 retry attempt loop 属于 `zeta-client`。\n4. Direct-provider runtime 解析自己的 credential；secret 不进入 config、普通 inference DTO 或\n   telemetry。ChatGPT/Codex subscription credential 始终留在 upstream Codex。\n5. Runtime 不解析 SSE/NDJSON framing。\n6. 动态 catalog cache 属于 models manager。\n7. `zeta-api` 和 `zeta-client` 不反向依赖 runtime。\n8. direct-provider credential lifecycle 属于本 crate；`zeta-secrets` 只持久化 opaque secret。\n9. interactive login lifecycle 属于 `zeta-login`；ChatGPT subscription 由\n   `zeta-codex-app-server` 委托给 upstream Codex。\n10. 不建立统一 Provider OAuth，也不接入第三方 subscription token。\n11. `model-provider` 不创建或消费 ChatGPT Agent backend；产品在 Core `TurnExecutionBackend` 边界\n    选择 Codex adapter。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-一次调用如何形成",
        "title": "1. 一次调用如何形成"
      },
      {
        "depth": 2,
        "id": "2-谁负责什么",
        "title": "2. 谁负责什么"
      },
      {
        "depth": 3,
        "id": "21-本系统拥有",
        "title": "2.1 本系统拥有"
      },
      {
        "depth": 3,
        "id": "22-本系统不拥有",
        "title": "2.2 本系统不拥有"
      },
      {
        "depth": 2,
        "id": "3-当前实现审计",
        "title": "3. 当前实现审计"
      },
      {
        "depth": 2,
        "id": "4-运行时解析流程",
        "title": "4. 运行时解析流程"
      },
      {
        "depth": 2,
        "id": "5-供应商与-api-端点的联动",
        "title": "5. 供应商与 API 端点的联动"
      },
      {
        "depth": 3,
        "id": "51-input-token-计量矩阵",
        "title": "5.1 Input-token 计量矩阵"
      },
      {
        "depth": 3,
        "id": "52-openai-服务接口面不是-openai-compatible-配置档案",
        "title": "5.2 OpenAI 服务接口面不是 OpenAI-compatible 配置档案"
      },
      {
        "depth": 2,
        "id": "6-供应商凭据与-codex-边界",
        "title": "6. 供应商凭据与 Codex 边界"
      },
      {
        "depth": 2,
        "id": "7-标头和目标",
        "title": "7. 标头和目标"
      },
      {
        "depth": 2,
        "id": "8-重试分工",
        "title": "8. 重试分工"
      },
      {
        "depth": 2,
        "id": "9-流式处理分工",
        "title": "9. 流式处理分工"
      },
      {
        "depth": 2,
        "id": "10-目录来源",
        "title": "10. 目录来源"
      },
      {
        "depth": 2,
        "id": "11-依赖方向",
        "title": "11. 依赖方向"
      },
      {
        "depth": 2,
        "id": "12-目标目录",
        "title": "12. 目标目录"
      },
      {
        "depth": 2,
        "id": "13-公共接口",
        "title": "13. 公共接口"
      },
      {
        "depth": 2,
        "id": "14-迁移顺序",
        "title": "14. 迁移顺序"
      },
      {
        "depth": 2,
        "id": "15-固定决策",
        "title": "15. 固定决策"
      }
    ],
    "searchText": " - 物理位置：zeta-rs/model-provider/ - Rust crate：zetamodelprovider - 层次：Provider 运行时与选择层 - 当前状态：completion runtime 已直接组合 zeta-api endpoint profile、zeta-client operation retry/stream framing 与 zeta-http-client transport；OpenAI Responses、OpenAI-compatible Chat Completions、Anthropic Messages 已使用原生 wire streaming；semantic OpenAI API-key materialization 已有 host-injected SecretStore 路径，持久化 credential 产品闭环尚未实现 - Crate 实现与 adapter 调用图：zeta-rs/model-provider/README.md - 声明配置层：model-provider-config.md - API 协议层：zeta-api.md - Operation client：zeta-client.md - 底层网络：zeta-http-client README - Secret persistence：secrets.md - Interactive login control plane：login.md - ChatGPT/Codex subscription runtime：codex-app-server.md 快速理解 模型调用系统把用户选择的模型变成一次可执行调用：先确定供应商、模型、服务地址和凭据，再把 请求交给协议、重试和网络层。配置在调用开始时冻结，因此一次调用不会在执行途中悄悄换模型或 凭据。 常见问题 系统行为 深入阅读 --- --- --- 本次到底使用哪个模型？ 根据供应商定义、用户选择和本次调用覆盖生成不可变绑定 一次调用如何形成 自定义服务地址会影响什么？ 只在供应商配置明确允许时生效，不会把一个服务偷偷当成另一个协议 供应商与 API 端点 凭据由这里保存吗？ 不保存；这里只取得本次调用所需的凭据，保存和登录属于相邻系统 供应商凭据 失败后会自动重试吗？ 只有调用类型明确允许安全重试时才会重试；模型推理默认不能仅凭“没收到输出”重跑 重试分工 当前已经能做什么？ 已具备三种 endpoint 的 unary/streaming completion 与 embedding/rerank；持久化凭据闭环尚未完成 当前实现审计 1. 一次调用如何形成 ``mermaid flowchart TD request \"Agent 发起模型调用\" -- select \"读取用户选择和调用覆盖\" select -- definition \"校验供应商定义与模型能力\" definition -- identity \"解析 direct-provider API key 或云身份\" identity -- credential \"凭据系统提供本次调用材料\" credential -- binding \"冻结模型、端点、凭据范围与重试策略\" binding -- encode \"协议层编码请求\" encode -- operation \"操作层执行截止时间、重试和流式分帧\" operation -- transport \"网络层完成 HTTP / WebSocket 传输\" transport -- result \"返回统一模型结果\" ` 这条流程中，模型调用系统只拥有“选择并冻结本次调用绑定”。协议层解释请求和响应，操作层处理 安全重试与分帧，网络层负责真实连接；任何一层都不能根据 URL 或模型名称重新猜测上层已经作出 的选择。 ChatGPT/Codex subscription 不是 raw model invocation：上游 Codex 同时拥有 Agent/tool/approval 循环，因此产品在 Core 层选择独立的 TurnExecutionBackend，不把它塞进本流程。 一次绑定必须明确回答： - 使用哪个供应商和模型； - 使用哪个 API 配置档案、服务地址和部署目标； - 使用哪种凭据来源和作用范围； - 哪些调用默认值与供应商规则生效； - 这类操作能否安全重试。 2. 谁负责什么 责任 最终所有者 模型调用系统在其中做什么 --- --- --- 供应商和模型选择 模型调用系统 解析选择并生成不可变调用绑定 合法配置形态 模型供应商配置 提供定义，模型调用系统只消费 凭据保存和登录 凭据与登录系统 请求本次需要的敏感材料，不自行持久化 API 请求和响应含义 模型 API 协议层 选择明确的协议配置档案 重试、截止时间和流式分帧 操作客户端 选择重试策略，不执行重试循环 HTTP、代理和 TLS 网络层 提供解析后的目标和标头 模型目录刷新 模型目录系统 提供目录访问适配器，不保存第二份缓存 2.1 本系统拥有 - 供应商运行时注册与适配器选择； - 规范化配置到不可变运行时的解析； - 供应商、模型和 API 配置档案的组合； - 服务地址、部署、账户与区域作用范围的最终解析； - 凭据引用到本次调用材料的绑定； - 供应商级调用默认值与重试策略选择； - 模型调用器 ModelInvoker 的生命周期； - 协议、操作客户端和共享网络客户端的组合； - 模型目录系统所需的运行时适配器。 2.2 本系统不拥有 - 可持久化配置模式和内置供应商定义； - 供应商请求、响应和流式事件的数据结构； - /responses、/messages 等 relative path； - JSON、SSE 事件或 NDJSON 对象的协议解释； - DNS/TCP/TLS/proxy/connection pool； - 重试循环、退避等待和 Retry-After 处理； - 流式分帧、字节缓冲和空闲计时； - 模型目录刷新、缓存、合并与筛选； - Agent 循环、工具循环和 Thread/Turn 持久状态； - 浏览器登录、OAuth 回调、令牌刷新撤销和上游凭据保存。 3. 当前实现审计 当前实现已经具备正确的基本方向： - ModelProviderRuntime 拥有配置 registry 和 lazy transport owner； - Provider 保存 normalized config、definition 和 adapter； - ModelInvoker 表达不可变 Provider/model selection； - src/providers/ 按外部服务组织 runtime adapter； - NormalizedModelProviderConfig::apiprofile 显式选择 ApiEndpoint； - SemanticRuntimeResolver 将 OpenAI-compatible/OpenAI/Ollama 的 exact config 解析为 immutable embedding/rerank invoker； - model-provider → zeta-api、zeta-client 和 zeta-http-client，没有反向依赖。 已移除的重复分派为： `text model-provider-config::ProviderAdapter ↓ model-provider::providers::instantiate ↓ zeta-api::Api:: OpenAi, DeepSeek, Google, ... ` zeta-api::Api 曾复制了一次 Provider registry。现在 zeta-api 仅公开 endpoint profile； model-provider::providers/ 保留唯一的 Provider runtime 选择，并把声明式 ApiProfile 映射为 ApiEndpoint。 HttpClient 和 UreqHttpClient 定义在 zeta-http-client。Runtime 持有共享 lazy operation client： App Server 启动不构造 socket/TLS backend，第一次真实 operation 才 fallibly 创建 transport；API codec 构造 opaque byte request，zeta-client 组合 retry/framing，底层保留 status/header/body transport evidence。 4. 运行时解析流程 `text ModelRuntimeRequest ├─ ModelRef ├─ ModelProviderConfig └─ invocation policy │ ▼ ProviderConfigRegistry::normalize │ ▼ Provider runtime adapter ├─ resolve credential ├─ resolve target/deployment ├─ choose API profile ├─ choose retry policy └─ bind zeta-api endpoint + zeta-client │ ▼ Arc<dyn ModelInvoker ` 解析结果必须不可变。配置或 credential revision 变化后创建新的 runtime，不在飞行中的调用对象 上做可变热更新。 5. 供应商与 API 端点的联动 Provider 名称不能等同于 API 协议。同一 Provider 可以选择多个正式 endpoint： Provider 可选 API profile 示例 --- --- OpenAI Platform Responses ChatGPT/Codex subscription upstream Codex App Server 的 thread/turn 与公开 capability（独立 runtime） xAI Responses、Chat Completions Google Gemini Interactions、GenerateContent、OpenAI-compatible Chat MiniMax Anthropic Messages、OpenAI-compatible Chat Ollama native Chat NDJSON、OpenAI-compatible Chat DeepSeek OpenAI Chat、Anthropic-compatible endpoint 5.1 Input-token 计量矩阵 这里的“支持”只表示调用前 budget measurement，不是调用完成后的 response usage。所有 remote binding 都由 ProviderDefinition.inputtokencount 明确声明 profile、target 和 model policy；未声明 时 fail closed，不能因为 Chat Completions 外形兼容就推断存在 count endpoint。 Provider 官方计量面 当前 Zeta 状态 边界 --- --- --- --- OpenAI POST /responses/inputtokens ✅ exact remote 与 Responses request 同 codec；直接读取 inputtokens Anthropic POST /v1/messages/counttokens 部分具备：estimated remote provider preflight 后按 1%/至少 32 tokens 保守记账 Google models.countTokens 部分具备：estimated remote native generateContentRequest；当前 invocation 是 OpenAI-compatible，且仅声明 model 可用 Kimi estimate-token-count 部分具备：estimated remote 文档 schema 只有 model/messages；带 tools/reasoning 的当前请求退回 unavailable Z.AI POST /tokenizer 部分具备：estimated remote 使用 usage.totaltokens，支持 tools；带 Tool Call/Result 历史暂退 unavailable xAI tokenize-text ❌ full-request preflight unavailable 只 tokenize 裸文本；billing FAQ 说明 inference 还会加入预定义 tokens Qwen text generation ❌ preflight unavailable chat template 会增加控制 token，不能按裸文本计数 DeepSeek token usage / offline tokenizer 部分具备：local/estimated 已接入完整 ModelRef binding、请求级模板渲染与 tokenizer runtime；当前仍需宿主提供固定资产清单 Ollama API usage fields ❌ preflight unavailable promptevalcount 是调用完成后的 usage Hugging Face Router per-model tokenizer API 部分具备：local/estimated 公共 owner/repo 首次使用时解析 immutable commit，按需下载并校验 tokenizer.json、tokenizerconfig.json 与 standalone template；重启复用磁盘缓存 MiniMax Chat API ❌ verified preflight unavailable 当前官方文档只确认 response usage MiMo Responses API ❌ verified preflight unavailable 当前官方文档只确认 response usage.inputtokens Generic OpenAI-compatible 无统一标准 ❌ unavailable 必须由具体 provider definition 显式增加 count profile 目标调用形态： `rust let binding = ApiBinding::OpenAiChat endpoint: zetaapi::endpoint::OpenAiChat::deepseek , target: resolvedtarget, ; ` 以上只是语义示例。关键约束： - Provider module 选择 endpoint/profile； - zeta-api 实现 request/response/event codec； - zeta-client 组织 operation attempt，并通过 zeta-http-client 执行； - runtime 不根据 URL 或 model ID 猜 profile； - profile 变化必须是显式配置或 built-in definition 变化； - 已有配置不能静默迁移到另一正式 API。 5.2 OpenAI 服务接口面不是 OpenAI-compatible 配置档案 公开 OpenAI Platform API 与 ChatGPT/Codex service 都可能使用 responses、models 或 realtime 这样的相对 path，但它们的 base URL、credential、可用 operation 和 wire 差异不能由 path 推断。 上游 Codex 的 memories/search 等能力是 subscription runtime capability，不是 zeta-api endpoint； 详见 zeta-api.md。 因此 direct-provider runtime binding 包含如下事实： `rust pub struct OpenAiExecutionBinding endpoint: zetaapi::ApiEndpoint, target: ResolvedApiTarget, credentialscope: CredentialScope, ` ChatGPT/Codex subscription 不是 OpenAiCompatibleAdapter 的用户自定义 base URL 选项。Platform API key、custom-compatible credential 与 ChatGPT/Codex runtime 彼此不能复用或降级转换。 ChatGPT/Codex subscription 不构造 ResolvedApiTarget + Bearer token binding。它只能由已认证的 zeta-codex-app-server runtime 执行；上游 Codex 选择 allow-listed target、管理 credential refresh，并执行实际 backend request 和 Agent loop。 6. 供应商凭据与 Codex 边界 Provider 的共同点只到“调用前需要可用身份”为止。API key、AWS credential chain、Google ADC、 Microsoft identity 和签名请求仍由本 crate 的 direct-provider runtime materialize；它们的 secret bytes 可以保存在 zeta-secrets，但浏览器 login 不属于本 crate。 ChatGPT/Codex subscription 是独立 runtime： `text zeta-app-server → zeta-login → zeta-codex-app-server → upstream Codex App Server │ └─ implements Core TurnExecutionBackend ` TurnExecutionBackend 是 Core 消费的窄 port。它接收已创建的 durable Turn，并把完整远端执行结果 投影回 Core；它不接受 raw access token、header map 或 arbitrary ChatGPT base URL。产品 composition 根据持久化 Turn model 的 exact provider 显式选择 default TurnExecutor 或 Codex backend； openai-chatgpt 由上游 account-filtered model catalog 提供，zeta-model-provider 不参与远端 Agent loop。 401 recovery 也按身份所有者处理：direct-provider credential 可由其 provider runtime 做一次受限 refresh/rebuild；Codex subscription 由 upstream Codex 自己刷新，Zeta 只接收 reauthentication-required 或稳定 execution error。zeta-client 不读取 secrets，也不自行刷新或重试认证。 7. 标头和目标 内容 Owner --- --- Platform/default provider base URL model-provider-config 用户 base URL override 仅 Platform/custom-compatible provider；由 model-provider-config 声明、runtime 解析 ChatGPT/Codex service target upstream Codex App Server；不接受 Zeta generic user override resolved absolute target model-provider API key/cloud identity/tenant header model-provider + direct-provider credential layer relative path、method、content type zeta-api::endpoint API version、协议 beta header zeta-api::endpoint/requests trace propagation、user agent zeta-http-client operation attempt header zeta-client Runtime 合并 headers 时必须使用 typed origin 和冲突规则。认证 header 不得被协议层覆盖，协议 必需 header 不得被用户任意删除；所有 secret header 的 Debug 必须脱敏。 8. 重试分工 zeta-client 拥有 retry 机制，但 runtime 选择 retry policy： `text model-provider 选择 Never / SafeRead / ExplicitIdempotency 等策略 ↓ zeta-client 执行 attempt loop、backoff、jitter、Retry-After 和 telemetry ↓ zeta-api 提供 provider error/status 的事实分类 ` Inference 通常是 POST，不能仅因“尚未收到 token”就假定安全重试。默认规则： - 没有 Provider 明确 idempotency contract 时，inference 使用 Never； - catalog GET 可以使用有上限的 SafeRead； - 已产生 semantic output 后禁止透明 retry； - direct-provider credential error、validation error 不重试； - Codex subscription refresh 和 upstream process outcome 由 zeta-codex-app-server 与 upstream Codex contract 处理，不能套用 HTTP retry； - fallback model/provider 是 Agent policy，不是 client retry； - runtime 只选择 typed policy，不自己 sleep 或写 attempt loop。 9. 流式处理分工 `text zeta-client direct-provider path 从 zeta-http-client 消费 HTTP byte stream → SSE/NDJSON framing → idle deadline / cancellation / backpressure ↓ zeta-api::sse provider event decode → ping/comment/terminal interpretation → canonical ModelStreamEvent ↓ model-provider attach provider/model identity → ModelInvoker stream ` DeepSeek : keep-alive 的 frame 边界由 client 识别，作为无 payload 的 SSE comment 交给协议层； 协议层确认它不形成 model output。Anthropic event: ping 同样由协议层过滤。Runtime 不解析 data: 行或 provider event JSON。 10. 目录来源 zeta-model-provider 是 models manager 与实际 Provider network runtime 的连接点： `text models-manager::ModelCatalogSource ▲ implemented by model-provider ├─ resolved catalog target ├─ credential scope ├─ zeta-api catalog request codec └─ zeta-client operation → zeta-http-client HTTP execution ` Runtime 负责 scope identity、credential revision 和 catalog API binding；manager 负责何时刷新、 缓存、merge 和发布 snapshot。Runtime 不维护第二份 catalog cache。 Codex subscription 的模型/额度 observation 只能由 zeta-codex-app-server 在上游公开 account 或 model operation 支持的范围内提供；它不使 models manager 直读或猜测 ChatGPT backend catalog。 11. 依赖方向 箭头表示“依赖”： `text zeta-model-provider ├──▶ zeta-model-provider-config ├──▶ zeta-api ───▶ zeta-client ├──▶ zeta-client ├──▶ zeta-http-client ├──▶ zeta-secrets └──▶ zeta-protocol zeta-codex-app-server ├──▶ zeta-login ├──▶ zeta-core implements TurnExecutionBackend └──▶ zeta-protocol App Server composition ├──▶ zeta-model-provider ├──▶ zeta-login └──▶ zeta-codex-app-server ` 更准确地说： - zeta-client 不依赖 Provider、API codec、Core 或 config； - zeta-http-client 不依赖 Provider、API codec、Core、config 或 secret store； - zeta-api 可依赖 zeta-client 的 operation/SSE value； - zeta-model-provider 依赖 config、API、operation client、HTTP client、secrets 和 protocol； 它不定义或消费完整 Agent-loop backend； - config 不反向依赖 runtime； - API/client 都不反向依赖 model-provider。 - zeta-codex-app-server 实现 interactive login 与 Core TurnExecutionBackend； - Zeta App Server 只组合和映射 redacted control-plane DTO，不实现 OAuth host； - model-provider 不依赖 App Server、Desktop、CLI 或 TUI。 12. 目标目录 `text zeta-rs/model-provider/ ├── BUILD.bazel ├── Cargo.toml ├── README.md └── src/ ├── lib.rs ├── runtime/ │ ├── mod.rs │ ├── provider.rs │ ├── modelinvoker."
  },
  {
    "slug": "model-provider-config",
    "sourcePath": "docs/model-provider-config.md",
    "title": "模型供应商配置",
    "description": "模型供应商配置只描述“允许怎样配置”，并以确定性方式校验和归一化；它不读取凭据、不访问网络， 也不执行模型请求。",
    "group": "模型与配置",
    "markdown": "> - 物理位置：`zeta-rs/model-provider-config/`\n> - Rust crate：`zeta_model_provider_config`\n> - 层次：声明配置层\n> - 当前状态：基础实现已包含声明式默认 `ApiProfile` 和独立 input-token count binding；invocation\n>   多 profile allow-list 与用户 override 仍待实现\n> - Crate 实现与修改路径：[`zeta-rs/model-provider-config/README.md`](../zeta-rs/model-provider-config/README.md)\n> - Provider runtime：[`model-provider.md`](model-provider.md)\n> - API 协议层：[`zeta-api.md`](zeta-api.md)\n\n## 快速理解\n\n模型供应商配置只描述“允许怎样配置”，并以确定性方式校验和归一化；它不读取凭据、不访问网络，\n也不执行模型请求。\n\n| 读者首先会问 | 直接答案 | 深入阅读 |\n| --- | --- | --- |\n| 这里保存什么？ | 可序列化的供应商定义、API 配置档案、默认值和静态模型资料 | [目标配置模型](#4-目标配置模型) |\n| 用户覆盖如何生效？ | 按字段规则合并后进行确定性规范化，相同输入必须得到相同结果 | [合并与规范化](#5-合并与规范化) |\n| 可以在这里读取 API key 吗？ | 不可以；配置只能保存不敏感的凭据引用 | [拥有与不拥有](#2-拥有与不拥有) |\n| 可以探测端点是否可用吗？ | 不可以；网络、凭据和运行时状态不能参与静态配置校验 | [Base URL 边界](#6-base-url-与端点的边界) |\n| 当前完成到哪里？ | 已有基础声明和默认 `ApiProfile`，多档案允许列表与用户覆盖仍待实现 | [当前实现审计](#3-当前实现审计) |\n\n## 1. 结论\n\n`zeta-model-provider-config` 描述“一个 Provider 可以怎样被配置”。它只包含可序列化、可校验、\n可生成 schema 的声明值，不创建 HTTP client，不读取 credential，也不执行模型调用。\n\n这里的“静态”不是 Rust 编译期常量，而是指：\n\n- 值可以在进程外持久化和传输；\n- 校验不依赖网络、credential 或当前进程状态；\n- 相同 definition、用户配置和 override 必须得到相同的 normalized config；\n- 任何 secret、连接、重试计数或动态 catalog observation 都不能进入该层。\n\n一句话边界：\n\n```text\nmodel-provider-config 负责描述和归一化\nmodel-provider        负责解析并运行\nzeta-api              负责协议编解码\nzeta-client           负责 API operation retry/framing\nzeta-http-client      负责底层网络传输\n```\n\n## 2. 拥有与不拥有\n\n### 2.1 拥有\n\n- `ProviderId` 对应的声明式 Provider definition；\n- Provider display name 和非敏感静态 metadata；\n- 默认 base URL 或“必须由用户配置”的 endpoint policy；\n- runtime adapter identity；\n- 允许选择的 API profile 及默认 profile；\n- input-token count profile、独立 target 与 model eligibility policy；\n- base URL normalization 规则；\n- 静态 seed models 和 model catalog policy；\n- 非敏感调用默认值，例如最大输出 token；\n- 用户按模型 ID 提供的 context window 与 automatic compaction threshold；\n- 用户 `ModelProviderConfig`；\n- definition、用户配置和 override 的确定性 merge；\n- 静态 validation、JSON Schema 和配置错误。\n\n### 2.2 不拥有\n\n- API key、OAuth token、cookie 或签名；\n- credential reference 的解析和 refresh；\n- resolved runtime header；\n- DNS、TCP、TLS、proxy、HTTP client 或连接池；\n- retry、deadline、cancellation、SSE 或 telemetry；\n- Provider request/response DTO；\n- prompt cache 的运行时资源和 usage；\n- 动态模型 discovery、TTL、availability 或 catalog snapshot；\n- `ModelInvoker`、运行中请求或可变 Provider 状态。\n\n## 3. 当前实现审计\n\n当前 crate 已有：\n\n- `ModelProviderConfig`；\n- `ModelContextConfig`；\n- `NormalizedModelProviderConfig`；\n- `ProviderDefinition`；\n- `ApiProfile`（definition 的显式默认 API profile）；\n- `InputTokenCountDefinition` 与 normalized count target/model policy；\n- `ProviderConfigRegistry`；\n- `ProviderAdapter`；\n- `EndpointPolicy`；\n- `ModelCatalogPolicy`；\n- built-in Provider definitions；\n- 静态校验、registry merge 和 schema tests。\n\n当前需要演进的地方：\n\n| 当前形态 | 问题 | 目标 |\n| --- | --- | --- |\n| `ProviderAdapter` 同时近似 Provider 名称和 runtime 实现 | 容易与 `zeta-api::Api` 再建一套分派 | 明确它是 runtime adapter identity，API endpoint 由 runtime 选择 |\n| `EndpointPolicy` 只描述 base URL | 名称容易被理解为 `/messages` 等协议 endpoint | 改称或文档化为 `BaseUrlPolicy` 语义 |\n| definition 目前只有一个 `api_profile` | 无法表达 Google、xAI、Ollama 等多个正式 API profile | 扩展为 typed default/allowed API profile policy |\n| count binding 已独立声明 profile/target/models | invocation 与 count 可能不共享 base path | 保持 definition 显式，禁止 runtime 剥 URL 或猜 model 前缀 |\n| 静态 models 同时参与运行时可用性判断 | 容易与 models manager catalog 重复 | 仅作为 seed metadata 和 fallback evidence |\n\n迁移期间可以保留现有类型名，但新代码不能继续扩大这些歧义。\n\n## 4. 目标配置模型\n\n以下类型表达目标语义，名称可在实现时调整：\n\n```rust\npub struct ProviderDefinition {\n    pub id: ProviderId,\n    pub display_name: String,\n    pub runtime_adapter: RuntimeAdapterKind,\n    pub base_url: BaseUrlPolicy,\n    pub api_profiles: ApiProfilePolicy,\n    pub input_token_count: Option<InputTokenCountDefinition>,\n    pub model_catalog_policy: ModelCatalogPolicy,\n    pub seed_models: Vec<Model>,\n    pub defaults: ProviderDefaults,\n    pub base_url_normalization: BaseUrlNormalization,\n}\n\npub struct ApiProfilePolicy {\n    pub default: ApiProfileConfig,\n    pub allowed: BTreeSet<ApiProfileConfig>,\n}\n\npub enum ApiProfileConfig {\n    OpenAiResponses,\n    OpenAiChatCompletions,\n    ChatGptCodex,\n    AnthropicMessages,\n    GeminiInteractions,\n    GeminiGenerateContent,\n    OllamaChat,\n}\n```\n\n`ApiProfileConfig` 是可序列化的配置值，不是 `zeta-api` runtime object。`zeta-model-provider`\n负责把它映射为具体 endpoint implementation，配置 crate 不依赖 `zeta-api`。\n\n`ChatGptCodex` 不是“再加一个可填写 base URL 的 OpenAI-compatible profile”。它只能由 built-in\ndefinition 声明为需要已配置 Codex subscription runtime 的 service surface：target、credential 和\nbackend selection 由 [`zeta-codex-app-server`](codex-app-server.md) 及 upstream Codex 管理，用户配置\n不得覆盖为任意 URL，Platform API key 也不得用于该 profile。上游提供的 memories/search/realtime\n能力不自动成为 `zeta-api` endpoint；只有通过公开 App Server contract 映射的能力才可接入。\n\nProvider-specific compatibility 也必须 typed：\n\n```rust\npub enum ProviderCompatibilityConfig {\n    ProviderDefault,\n    OpenAiCompatible(OpenAiCompatibilityConfig),\n}\n```\n\n不能增加如下通用逃生口：\n\n```rust\nprovider_options: serde_json::Value\n```\n\n它会绕过 schema、validation、secret 审计和 profile 配对检查。\n\n## 5. 合并与规范化\n\n输入优先级固定为：\n\n```text\nbuilt-in ProviderDefinition\n    ↓\nregistered definition replacement/extension\n    ↓\nuser ModelProviderConfig\n    ↓\ninvocation-independent host override\n    ↓\nNormalizedModelProviderConfig\n```\n\nNormalization 必须：\n\n- 验证 Provider 是否已注册；\n- 选择明确的 API profile；\n- 验证 profile 在该 definition 的 allowed set 中；\n- 解析默认或用户 base URL；\n- 按声明规则 trim/保留 trailing slash；\n- 验证 HTTP(S) scheme，但不做 DNS 或网络请求；\n- 合并非敏感 defaults；\n- 保留“未知”和“未配置”的区别；\n- 返回不可含 secret 的 `ProviderConfigError`。\n\nNormalization 不得：\n\n- 根据 URL 猜 Provider 或 API profile；\n- 读取环境变量或 credential store；\n- 请求 `/models` 验证模型；\n- 探测 endpoint 是否在线；\n- 将配置缺失静默替换为另一个 Provider；\n- 根据 model ID 字符串猜 capability。\n\n## 6. Base URL 与端点的边界\n\n三种概念必须分开：\n\n| 概念 | Owner | 示例 |\n| --- | --- | --- |\n| 默认 base URL | `model-provider-config` | `https://api.anthropic.com` |\n| resolved runtime target | `model-provider` | base URL + credential/runtime headers |\n| relative API endpoint | `zeta-api` | `POST /v1/messages` |\n\n配置层禁止把完整 invocation URL 当成通用字符串模板，也禁止通过删除 `/v1` 猜测 catalog 或原生\nendpoint。Google 和 Ollama 的 invocation/catalog 地址可能不共享同一个 base path，必须由\ndefinition/runtime 显式声明。\n\n## 7. 静态模型元数据\n\n`ProviderDefinition.seed_models` 是：\n\n- 启动 seed；\n- Provider 没有动态 discovery 时的静态来源；\n- display name、capability 和 limit 的内置 metadata evidence；\n- 离线配置校验的辅助信息。\n\n它不是：\n\n- 当前 credential 可访问模型的实时 authority；\n- availability cache；\n- pricing/billing authority；\n- runtime health probe。\n\n动态发现、缓存、字段级 merge 和筛选属于\n[`zeta-models-manager`](models-manager.md)。`ListedOnly` / `AllowUnlisted` 的最终判断应逐步消费\nmanager resolution，而不是让静态 definition 永久承担动态 catalog。\n\n## 8. 依赖方向\n\n```text\nzeta-protocol\n      ▲\n      │ shared IDs/model metadata\nzeta-model-provider-config\n      ▲\n      │ normalized declaration\nzeta-model-provider\n```\n\n允许：\n\n- `zeta-model-provider-config → zeta-protocol`；\n- `zeta-model-provider → zeta-model-provider-config`。\n\n禁止：\n\n```text\nzeta-model-provider-config → zeta-model-provider\nzeta-model-provider-config → zeta-api\nzeta-model-provider-config → zeta-client\nzeta-model-provider-config → zeta-http-client\nzeta-model-provider-config → credentials\nzeta-model-provider-config → Core/App Server\n```\n\n## 9. 目标目录\n\n```text\nzeta-rs/model-provider-config/\n├── BUILD.bazel\n├── Cargo.toml\n├── README.md\n└── src/\n    ├── lib.rs\n    ├── config/\n    │   ├── mod.rs\n    │   ├── user.rs\n    │   ├── normalized.rs\n    │   └── config_tests.rs\n    ├── definition/\n    │   ├── mod.rs\n    │   ├── adapter.rs\n    │   ├── api_profile.rs\n    │   ├── base_url.rs\n    │   ├── defaults.rs\n    │   └── definition_tests.rs\n    ├── registry/\n    │   ├── mod.rs\n    │   ├── normalize.rs\n    │   └── registry_tests.rs\n    ├── providers/\n    │   ├── mod.rs\n    │   ├── openai.rs\n    │   ├── anthropic.rs\n    │   ├── google.rs\n    │   ├── deepseek.rs\n    │   └── ...\n    └── error.rs\n```\n\n目录只在有实现和测试的 vertical slice 中创建。新 test module 使用 sibling\n`*_tests.rs`；public trait/type 写明实现和调用约束。\n\n## 10. 验收\n\n- 所有 public 配置值可序列化、反序列化并生成 schema；\n- normalization 是确定性的；\n- unknown Provider、非法 URL、非法 profile 组合返回 typed error；\n- debug/error 不含 secret；\n- config crate 没有网络、credential 或 runtime dependency；\n- built-in definitions 的 ID、base URL 和默认 profile 有 contract tests；\n- 自定义 OpenAI-compatible 配置必须显式选择 compatible profile；\n- 未配置行为不会通过 `bool` 或含糊 `Option` 控制。\n\n## 11. 固定决策\n\n1. 本 crate 是声明配置层，不是运行时。\n2. “静态”表示无需网络和进程状态，不表示编译期硬编码。\n3. 默认 base URL 属于本 crate，resolved target 不属于。\n4. API profile 可以被声明，但具体 `zeta-api` endpoint object 由 runtime 选择。\n5. Secret、transport、retry、SSE、telemetry 和动态 catalog 永不进入本 crate。\n6. Provider-specific option 必须 typed，禁止任意 JSON escape hatch。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-拥有与不拥有",
        "title": "2. 拥有与不拥有"
      },
      {
        "depth": 3,
        "id": "21-拥有",
        "title": "2.1 拥有"
      },
      {
        "depth": 3,
        "id": "22-不拥有",
        "title": "2.2 不拥有"
      },
      {
        "depth": 2,
        "id": "3-当前实现审计",
        "title": "3. 当前实现审计"
      },
      {
        "depth": 2,
        "id": "4-目标配置模型",
        "title": "4. 目标配置模型"
      },
      {
        "depth": 2,
        "id": "5-合并与规范化",
        "title": "5. 合并与规范化"
      },
      {
        "depth": 2,
        "id": "6-base-url-与端点的边界",
        "title": "6. Base URL 与端点的边界"
      },
      {
        "depth": 2,
        "id": "7-静态模型元数据",
        "title": "7. 静态模型元数据"
      },
      {
        "depth": 2,
        "id": "8-依赖方向",
        "title": "8. 依赖方向"
      },
      {
        "depth": 2,
        "id": "9-目标目录",
        "title": "9. 目标目录"
      },
      {
        "depth": 2,
        "id": "10-验收",
        "title": "10. 验收"
      },
      {
        "depth": 2,
        "id": "11-固定决策",
        "title": "11. 固定决策"
      }
    ],
    "searchText": " - 物理位置：zeta-rs/model-provider-config/ - Rust crate：zetamodelproviderconfig - 层次：声明配置层 - 当前状态：基础实现已包含声明式默认 ApiProfile 和独立 input-token count binding；invocation 多 profile allow-list 与用户 override 仍待实现 - Crate 实现与修改路径：zeta-rs/model-provider-config/README.md - Provider runtime：model-provider.md - API 协议层：zeta-api.md 快速理解 模型供应商配置只描述“允许怎样配置”，并以确定性方式校验和归一化；它不读取凭据、不访问网络， 也不执行模型请求。 读者首先会问 直接答案 深入阅读 --- --- --- 这里保存什么？ 可序列化的供应商定义、API 配置档案、默认值和静态模型资料 目标配置模型 用户覆盖如何生效？ 按字段规则合并后进行确定性规范化，相同输入必须得到相同结果 合并与规范化 可以在这里读取 API key 吗？ 不可以；配置只能保存不敏感的凭据引用 拥有与不拥有 可以探测端点是否可用吗？ 不可以；网络、凭据和运行时状态不能参与静态配置校验 Base URL 边界 当前完成到哪里？ 已有基础声明和默认 ApiProfile，多档案允许列表与用户覆盖仍待实现 当前实现审计 1. 结论 zeta-model-provider-config 描述“一个 Provider 可以怎样被配置”。它只包含可序列化、可校验、 可生成 schema 的声明值，不创建 HTTP client，不读取 credential，也不执行模型调用。 这里的“静态”不是 Rust 编译期常量，而是指： - 值可以在进程外持久化和传输； - 校验不依赖网络、credential 或当前进程状态； - 相同 definition、用户配置和 override 必须得到相同的 normalized config； - 任何 secret、连接、重试计数或动态 catalog observation 都不能进入该层。 一句话边界： ``text model-provider-config 负责描述和归一化 model-provider 负责解析并运行 zeta-api 负责协议编解码 zeta-client 负责 API operation retry/framing zeta-http-client 负责底层网络传输 ` 2. 拥有与不拥有 2.1 拥有 - ProviderId 对应的声明式 Provider definition； - Provider display name 和非敏感静态 metadata； - 默认 base URL 或“必须由用户配置”的 endpoint policy； - runtime adapter identity； - 允许选择的 API profile 及默认 profile； - input-token count profile、独立 target 与 model eligibility policy； - base URL normalization 规则； - 静态 seed models 和 model catalog policy； - 非敏感调用默认值，例如最大输出 token； - 用户按模型 ID 提供的 context window 与 automatic compaction threshold； - 用户 ModelProviderConfig； - definition、用户配置和 override 的确定性 merge； - 静态 validation、JSON Schema 和配置错误。 2.2 不拥有 - API key、OAuth token、cookie 或签名； - credential reference 的解析和 refresh； - resolved runtime header； - DNS、TCP、TLS、proxy、HTTP client 或连接池； - retry、deadline、cancellation、SSE 或 telemetry； - Provider request/response DTO； - prompt cache 的运行时资源和 usage； - 动态模型 discovery、TTL、availability 或 catalog snapshot； - ModelInvoker、运行中请求或可变 Provider 状态。 3. 当前实现审计 当前 crate 已有： - ModelProviderConfig； - ModelContextConfig； - NormalizedModelProviderConfig； - ProviderDefinition； - ApiProfile（definition 的显式默认 API profile）； - InputTokenCountDefinition 与 normalized count target/model policy； - ProviderConfigRegistry； - ProviderAdapter； - EndpointPolicy； - ModelCatalogPolicy； - built-in Provider definitions； - 静态校验、registry merge 和 schema tests。 当前需要演进的地方： 当前形态 问题 目标 --- --- --- ProviderAdapter 同时近似 Provider 名称和 runtime 实现 容易与 zeta-api::Api 再建一套分派 明确它是 runtime adapter identity，API endpoint 由 runtime 选择 EndpointPolicy 只描述 base URL 名称容易被理解为 /messages 等协议 endpoint 改称或文档化为 BaseUrlPolicy 语义 definition 目前只有一个 apiprofile 无法表达 Google、xAI、Ollama 等多个正式 API profile 扩展为 typed default/allowed API profile policy count binding 已独立声明 profile/target/models invocation 与 count 可能不共享 base path 保持 definition 显式，禁止 runtime 剥 URL 或猜 model 前缀 静态 models 同时参与运行时可用性判断 容易与 models manager catalog 重复 仅作为 seed metadata 和 fallback evidence 迁移期间可以保留现有类型名，但新代码不能继续扩大这些歧义。 4. 目标配置模型 以下类型表达目标语义，名称可在实现时调整： `rust pub struct ProviderDefinition pub id: ProviderId, pub displayname: String, pub runtimeadapter: RuntimeAdapterKind, pub baseurl: BaseUrlPolicy, pub apiprofiles: ApiProfilePolicy, pub inputtokencount: Option<InputTokenCountDefinition , pub modelcatalogpolicy: ModelCatalogPolicy, pub seedmodels: Vec<Model , pub defaults: ProviderDefaults, pub baseurlnormalization: BaseUrlNormalization, pub struct ApiProfilePolicy pub default: ApiProfileConfig, pub allowed: BTreeSet<ApiProfileConfig , pub enum ApiProfileConfig OpenAiResponses, OpenAiChatCompletions, ChatGptCodex, AnthropicMessages, GeminiInteractions, GeminiGenerateContent, OllamaChat, ` ApiProfileConfig 是可序列化的配置值，不是 zeta-api runtime object。zeta-model-provider 负责把它映射为具体 endpoint implementation，配置 crate 不依赖 zeta-api。 ChatGptCodex 不是“再加一个可填写 base URL 的 OpenAI-compatible profile”。它只能由 built-in definition 声明为需要已配置 Codex subscription runtime 的 service surface：target、credential 和 backend selection 由 zeta-codex-app-server 及 upstream Codex 管理，用户配置 不得覆盖为任意 URL，Platform API key 也不得用于该 profile。上游提供的 memories/search/realtime 能力不自动成为 zeta-api endpoint；只有通过公开 App Server contract 映射的能力才可接入。 Provider-specific compatibility 也必须 typed： `rust pub enum ProviderCompatibilityConfig ProviderDefault, OpenAiCompatible OpenAiCompatibilityConfig , ` 不能增加如下通用逃生口： `rust provideroptions: serdejson::Value ` 它会绕过 schema、validation、secret 审计和 profile 配对检查。 5. 合并与规范化 输入优先级固定为： `text built-in ProviderDefinition ↓ registered definition replacement/extension ↓ user ModelProviderConfig ↓ invocation-independent host override ↓ NormalizedModelProviderConfig ` Normalization 必须： - 验证 Provider 是否已注册； - 选择明确的 API profile； - 验证 profile 在该 definition 的 allowed set 中； - 解析默认或用户 base URL； - 按声明规则 trim/保留 trailing slash； - 验证 HTTP S scheme，但不做 DNS 或网络请求； - 合并非敏感 defaults； - 保留“未知”和“未配置”的区别； - 返回不可含 secret 的 ProviderConfigError。 Normalization 不得： - 根据 URL 猜 Provider 或 API profile； - 读取环境变量或 credential store； - 请求 /models 验证模型； - 探测 endpoint 是否在线； - 将配置缺失静默替换为另一个 Provider； - 根据 model ID 字符串猜 capability。 6. Base URL 与端点的边界 三种概念必须分开： 概念 Owner 示例 --- --- --- 默认 base URL model-provider-config https://api.anthropic.com resolved runtime target model-provider base URL + credential/runtime headers relative API endpoint zeta-api POST /v1/messages 配置层禁止把完整 invocation URL 当成通用字符串模板，也禁止通过删除 /v1 猜测 catalog 或原生 endpoint。Google 和 Ollama 的 invocation/catalog 地址可能不共享同一个 base path，必须由 definition/runtime 显式声明。 7. 静态模型元数据 ProviderDefinition.seedmodels 是： - 启动 seed； - Provider 没有动态 discovery 时的静态来源； - display name、capability 和 limit 的内置 metadata evidence； - 离线配置校验的辅助信息。 它不是： - 当前 credential 可访问模型的实时 authority； - availability cache； - pricing/billing authority； - runtime health probe。 动态发现、缓存、字段级 merge 和筛选属于 zeta-models-manager。ListedOnly / AllowUnlisted 的最终判断应逐步消费 manager resolution，而不是让静态 definition 永久承担动态 catalog。 8. 依赖方向 `text zeta-protocol ▲ │ shared IDs/model metadata zeta-model-provider-config ▲ │ normalized declaration zeta-model-provider ` 允许： - zeta-model-provider-config → zeta-protocol； - zeta-model-provider → zeta-model-provider-config。 禁止： `text zeta-model-provider-config → zeta-model-provider zeta-model-provider-config → zeta-api zeta-model-provider-config → zeta-client zeta-model-provider-config → zeta-http-client zeta-model-provider-config → credentials zeta-model-provider-config → Core/App Server ` 9. 目标目录 `text zeta-rs/model-provider-config/ ├── BUILD.bazel ├── Cargo.toml ├── README.md └── src/ ├── lib.rs ├── config/ │ ├── mod.rs │ ├── user.rs │ ├── normalized.rs │ └── configtests.rs ├── definition/ │ ├── mod.rs │ ├── adapter.rs │ ├── apiprofile.rs │ ├── baseurl.rs │ ├── defaults.rs │ └── definitiontests.rs ├── registry/ │ ├── mod.rs │ ├── normalize.rs │ └── registrytests.rs ├── providers/ │ ├── mod.rs │ ├── openai.rs │ ├── anthropic.rs │ ├── google.rs │ ├── deepseek.rs │ └── ... └── error.rs ` 目录只在有实现和测试的 vertical slice 中创建。新 test module 使用 sibling tests.rs；public trait/type 写明实现和调用约束。 10. 验收 - 所有 public 配置值可序列化、反序列化并生成 schema； - normalization 是确定性的； - unknown Provider、非法 URL、非法 profile 组合返回 typed error； - debug/error 不含 secret； - config crate 没有网络、credential 或 runtime dependency； - built-in definitions 的 ID、base URL 和默认 profile 有 contract tests； - 自定义 OpenAI-compatible 配置必须显式选择 compatible profile； - 未配置行为不会通过 bool 或含糊 Option 控制。 11. 固定决策 1. 本 crate 是声明配置层，不是运行时。 2. “静态”表示无需网络和进程状态，不表示编译期硬编码。 3. 默认 base URL 属于本 crate，resolved target 不属于。 4. API profile 可以被声明，但具体 zeta-api` endpoint object 由 runtime 选择。 5. Secret、transport、retry、SSE、telemetry 和动态 catalog 永不进入本 crate。 6. Provider-specific option 必须 typed，禁止任意 JSON escape hatch。"
  },
  {
    "slug": "config",
    "sourcePath": "docs/config.md",
    "title": "配置系统",
    "description": "配置系统回答“在当前作用域下，用户想要什么值”；领域管理器回答“现在实际可用什么”，App Server 只在安全点把两者组合成运行时快照。",
    "group": "模型与配置",
    "markdown": "> 物理位置：`zeta-rs/config/`  \n> Rust crate：`zeta_config`  \n> 当前状态：TOML User Config authority、SQLite revision/generation 与 exact command receipt、\n> Provider map、standalone MCP declaration、Skill source/per-Skill enablement、Workspace TOML\n> read-only document、exact Plugin request、declarative Hook、User Workspace trust decision 和\n> scoped resolution、Tool Search 词法/混合 embedding 模式选择、User execution-policy rule\n> persistence 与 Workspace restrictive rule intent 已实现。\n> Local App Server 在 profile 下使用\n> `config.toml` 与 `state.sqlite3`，并在提交后原子切换未来的 model、Skill 与 MCP Tool safe point；\n> 已 prepare 的 Tool Call 保留旧 generation。Plugin contribution、grant 和完整环境组合仍是后续\n> vertical slice。\n> Core 边界：[`core.md`](core.md)  \n> Plugin 控制面：[`plugins.md`](plugins.md)  \n> MCP runtime：[`mcp.md`](mcp.md)  \n> Skill runtime：[`skills.md`](skills.md)  \n> Agent 自定义对象与外部导入边界：[`agent-customizations.md`](agent-customizations.md)\n> Direct-provider credential：[`model-provider.md`](model-provider.md#6-供应商凭据与-codex-边界)\n> Interactive login：[`login.md`](login.md)\n> Secret persistence：[`secrets.md`](secrets.md)\n\n## 快速理解\n\n配置系统回答“在当前作用域下，用户想要什么值”；领域管理器回答“现在实际可用什么”，App Server\n只在安全点把两者组合成运行时快照。\n\n| 用户或调用方的动作 | 直接结果 | 不代表什么 |\n| --- | --- | --- |\n| 修改用户配置 | 产生新的用户配置 revision | 不代表正在运行的 Turn 立即改变 |\n| Workspace 声明模型或 MCP | 进入作用域解析和信任校验 | 不会自动安装、授权或连接 |\n| Session 覆盖默认模型 | 影响后续模型安全点生成的快照 | 不改写用户或 Workspace 配置 |\n| Plugin、MCP 或 Skill 协调失败 | 保留期望配置并更新健康诊断 | 不会静默回滚用户想要的状态 |\n| 配置引用凭据 | 只保存不敏感的凭据引用 | 不保存 token、API key 或 OAuth 状态 |\n| 系统或组织设置安全要求 | 对解析结果施加约束 | 不是可以被低优先级配置覆盖的普通字段 |\n\n## 1. 结论\n\nZeta 对外提供统一配置体验，但不建立一个拥有所有产品状态的巨型 Config 数据库。\n\n长期固定三层语义：\n\n```text\nDesired configuration\n    用户、Workspace 和领域 authority 声明“想要什么”\n\nOperational state\n    Plugin/MCP/Skill manager 维护“当前实际是什么状态”\n\nRuntime snapshot\n    App Server 在 safe point 冻结“本次执行能使用什么”\n```\n\n`zeta-config` 是普通配置 authority 和 typed resolution 层。Plugin package、MCP connection、\nSkill catalog、credential 和 action policy 继续由各自领域拥有。App Server 是唯一组合层：\n它把各领域不可变 snapshot 汇合为 Agent 可消费的环境，而不把 live manager 或文件读取能力\n注入 Core。\n\n最重要的不变量是：\n\n- 一个 scope 内同一份期望状态只有一个 authority；\n- 配置提交成功不等于 Plugin 已激活、MCP 已连接、Skill 已可用或 Hook 已执行；\n- runtime reconcile 失败只更新 health/diagnostics，不静默改写用户期望；\n- Workspace 声明可以请求能力，但不能自行安装、授权、绑定 credential 或扩大 sandbox；\n- 运行中的 Turn 或 model request 只使用开始时冻结的 snapshot。\n\n## 2. 权威分布\n\n统一配置体验不等于统一物理 authority。长期 authority 分布如下：\n\n| Authority | 拥有 | 不拥有 |\n| --- | --- | --- |\n| User Config authority | Agent 默认值、Provider 配置、独立 MCP server、Skill source、Plugin request、Hook declaration、execution-policy rules、按 canonical root identity 的 Workspace trust decision | UI/device 偏好、installed Plugin package、live connection、Hook execution、secret、runtime health |\n| Desktop device preference authority | theme、zoom、window/device UI 偏好 | Agent/Provider/MCP/Skill desired config、Session/Thread |\n| Workspace config document | Workspace Agent 默认值、独立 MCP 声明、Plugin 请求、Workspace Skill source、Hook 请求、只收紧的 execution-policy rules | 自动安装、扩权 grant、Hook 执行、credential value、运行时状态 |\n| Plugin authority | installed exact package、effective activation、activation grant、credential-slot binding、rollback | TOML request、MCP session、Skill catalog、per-call approval |\n| Domain auth authority | credential type、refresh、scope、credential revision | 普通配置、Plugin manifest、Thread history |\n| Secret store | opaque secret bytes 的 load/store/delete | credential type、OAuth、scope、refresh |\n| Session authority | durable `SessionSettings` 和产品级共享默认值 | 完整 Core/runtime 配置快照 |\n| Host policy authority | system/organization requirements、action approval 和 sandbox decision | 用户偏好、package content、secret |\n\n`zeta-config` 可以解析 Workspace 中的 Plugin 请求，但请求进入 Plugin manager 后仍需与已安装\npackage、grant、trust 和 credential binding 一起解析。Workspace 文件本身永远不能成为\nPlugin installed/active authority。\n\nDirect-provider credential lifecycle 由 [`model-provider.md`](model-provider.md) 规定，interactive\nlogin 由 [`login.md`](login.md) 规定，storage policy 由 [`secrets.md`](secrets.md) 规定。Config 只\n保存 opaque `CredentialId`/account reference；不保存 credential kind 对应的 raw secret、OAuth token\nbundle、authorization header 或 refresh 状态。\n\n当前 Workspace document 由 host 提供 `WorkspaceId` 和 content revision；文件本身不能选择自己\n的 namespace 或 generation。它 strict-parse 为 TOML document，MCP/Skill/Hook ID 必须落在\n`workspace:<workspace-id>:` namespace，且 MCP declaration 不包含 credential binding。Workspace\nHook 只是 pending trust 的 process request，不能因为文件存在就执行。\n\n## 3. 配置来源与范围\n\n配置 source 按以下顺序参与解析：\n\n```text\nBuiltInDefaults\n    + UserConfig\n    + WorkspaceConfig\n    + SessionSettings\n    + LaunchOverrides\n    constrained by SystemRequirements\n    =\nResolvedConfigSnapshot\n```\n\n这不是无条件的递归 TOML/JSON merge。每个字段必须声明：\n\n- 可以出现在哪些 scope；\n- merge、replace、append 或 clear 语义；\n- Workspace 是否只能收紧；\n- 是否必须产生 provenance；\n- 更新影响下一个 Turn、下一次 model invocation，还是仅影响 UI；\n- 缺失、显式清除和继承的区别。\n\n典型规则：\n\n| 配置 | 允许的 source | 规则 |\n| --- | --- | --- |\n| Theme/UI preference | Desktop device authority | 不进入 Rust `UserConfigDocument`，Workspace、Session 和 launch 不能覆盖 |\n| Preferred model | User、Workspace、Session、launch | 只影响下一个 model safe point |\n| Provider endpoint/profile | User、host | Workspace 不能静默替换认证或网络边界 |\n| Standalone MCP server | User、Workspace | Workspace declaration 需要 trust/grant 后才能启动 |\n| Plugin request | User、Workspace | 只能请求 exact package/version 与 desired enablement，不能证明已安装、激活或授权 |\n| Skill source | User、Workspace | 必须经过 source containment、trust 和 compatibility 校验 |\n| Hook declaration | User、Workspace | 只声明 safe-point、tool matcher 与 process argv；执行仍需 trust、policy、approval 和 sandbox |\n| Tool Search mode/model | User | 默认纯本地 BM25/Regex；混合模式独立选择 exact embedding `ModelRef`，由 App Server 探活后启用 |\n| Sandbox/approval intent | User、Workspace、Session | 低信任 source 只能保持或收紧安全性 |\n| Execution-policy rules | Host、Organization、User、Workspace | User 可显式授权；Workspace 只能拒绝、强制沙箱/审批或 Continue，不能 `AllowUnsandboxed` |\n| Workspace trust | User、organization policy、trusted host composition | Workspace document 无权自授信；User 缺失决策默认 Restricted |\n| System requirements | System/organization | 是约束，不是“最高优先级普通配置” |\n\n## 4. Typed 配置模型\n\n配置 document 按领域分 section，但保持一个稳定的顶层 schema：\n\n```rust\npub struct UserConfigDocument {\n    pub agent: AgentConfig,\n    pub providers: ProvidersConfig,\n    pub mcp: McpConfig,\n    pub skills: SkillsConfig,\n    pub plugins: PluginsConfig,\n    pub hooks: HooksConfig,\n    pub tool_search: ToolSearchConfig,\n    pub exec_policy: UserExecPolicyConfig,\n    pub workspace_trust: WorkspaceTrustConfig,\n}\n\npub struct WorkspaceConfigDocument {\n    pub agent: WorkspaceAgentConfig,\n    pub mcp: WorkspaceMcpConfig,\n    pub plugin_requests: WorkspacePluginRequests,\n    pub skills: WorkspaceSkillsConfig,\n    pub hooks: HooksConfig,\n    pub exec_policy: WorkspaceExecPolicyConfig,\n}\n```\n\n`UserExecPolicyConfig` 持久化 typed `ExecPolicyRule`；`UserConfigCommand::UpsertExecPolicyRule` 和\n`RemoveExecPolicyRule` 走同一 expected-revision、receipt 与 atomic TOML replacement 路径。\n`WorkspaceExecPolicyConfig` 是 strict-read intent，validation 禁止 `AllowUnsandboxed`。\n`compose_exec_policy` 把它们与 trusted Host/Organization layers 组合为 immutable\n`ExecPolicySnapshot`；规则求值和 semantic revision 属于 `zeta-execpolicy`，最终 grant 属于\n`zeta-action-policy`，不属于 Config。\n\nUser Config 的 `PluginsConfig` 保存 exact package request 与 desired enablement；Workspace 保存\nscoped request。Plugin installed state、effective activation、grant、digest 和 rollback 不复制进\n普通 Config document，这些值属于 Plugin authority。\n\n`HooksConfig` 保存 namespaced Hook ID、`beforeTool`/`afterTool`/`turnCompleted` safe-point、\nexact tool-name matcher、process `program + args` 与 desired enablement。它不保存 PID、环境、\n执行队列、结果或 retry。Local App Server 的 Hook runtime 在 trusted Workspace 中按 immutable\n配置快照匹配事件，经 Host Policy 评估后使用统一 sandbox executor；Restricted Workspace 不安装\nprocess runner。`TurnCompleted` 在 durable completion 后 best-effort 执行，`beforeTool`/`afterTool`\n遵守 cancellation，并在配置变更后热替换未来 safe point。\n\nTheme 已从 Rust Config schema、command 和 App Server Config DTO 中移除。Desktop device\n配置只拥有 device/UI preference；它不能再作为 Agent、Provider、MCP、Skill、\nSession 或 Thread 的第二 authority。\n\n所有 section 必须是 typed schema。禁止使用以下通用逃生口：\n\n```rust\npub options: serde_json::Value\npub extra: HashMap<String, serde_json::Value>\n```\n\nProvider 配置继续由 `zeta-model-provider-config` 定义。`zeta-config` 可以保存\n`BTreeMap<ProviderId, ModelProviderConfig>`，但不重新定义 Provider normalization 或 runtime。\n`ModelProviderConfig.model_context` 以 `ModelId` 为 key 保存可选的 `context_window` 与\n`auto_compact_token_limit`。这些值只决定 Core 是否能使用确定性预算和 durable compaction，不\n改变 provider endpoint 或模型选择；窗口或阈值为零会在静态配置校验时被拒绝。目录和配置都无\n已知窗口时，Core 明确退回 provider-managed，不猜测默认窗口。\n\nDeferred Agent 工具检索只有两档 User intent：`toolSearch.mode = \"lexical\"`（默认）和\n`toolSearch.mode = \"hybridEmbedding\"`。混合模式还必须配置 `toolSearch.embeddingModel`；该\n`ModelRef` 与 CodeIndex 的 embedding/rerank 选择彼此独立，只共享 provider runtime 和 credential\nmaterialization。配置本身不保存模型实例、向量或 provider credential。\n\n`toolSearch/configure` 会在 durable commit 前解析模型并用固定文本完成 readiness probe。未配置\nprovider、credential/runtime 或 probe 失败时返回 `ToolSearchUnavailable`，不会打开门禁。外部 TOML\n或启动恢复出的不可用 hybrid 配置不会拖垮 App Server；`config/read` 通过\n`toolSearch.embeddingStatus = unavailable` 暴露原因，但自然语言 Tool Search 会明确失败，不会\n偷偷切到 BM25。显式 Regex 仍在本地运行；用户显式改回 `lexical` 后，BM25 才重新成为自然语言\n检索路径。门禁通过后，实际 embedding 调用失败同样使该次 Tool Search 明确失败。\n\nSemantic CodeIndex 使用独立的 `semanticCodeIndex.selection` 和按 Workspace trust ID 保存的 source-egress\ngrant。授权会冻结 embedding/rerank `ModelRef` 以及它们实际使用的 provider config；模型或 endpoint\n变化后 `activeWorkspaceAuthorized` 立即变为 false，App Server 卸载旧 semantic runtime。Desktop 的\nIndexing 设置页可保存 Ollama/unauthenticated OpenAI-compatible endpoint、模型选择并 authorize/revoke；\n普通聊天模型配置和 Workspace 文件读取权限都不会自动授予源码外发。\n\n自动审批模型是独立于主 Agent 模型的 User 配置：\n\n```rust\npub enum ApprovalReviewModelSelection {\n    Automatic,\n    Explicit { model: ModelRef },\n}\n```\n\n`Automatic` 跟随当前主 Agent 模型的 provider，再由该 provider 的声明选择适合审批的默认\nreview model；没有专用默认值的自定义或本地 provider 才复用当前模型。它不是一个全局固定\n模型。`Explicit` 允许用户锁定一个已经配置、且满足 review capability gate 的 provider/model。\n显式模型的 provider 不能在仍被引用时删除。配置层能够验证 provider、静态 catalog 与 endpoint；\ncredential、订阅 entitlement 和远端模型是否实际可调用，由创建 review runtime 时再次验证。\nWorkspace 配置不能覆盖审批模型，避免仓库内容自行降低 reviewer 强度。模型不可用或不兼容时\nfail closed，不得静默换成其他审批模型。\n\n`WorkspaceTrustConfig` 使用 `WorkspaceTrustId` 作为 key。该 ID 由 `zeta-workspace` 对 canonical\nroot 的平台原生 path bytes 做 SHA-256 得到，因此不把本机路径写入 User TOML，symlink/平台别名\n共享决定，移动根目录后必须重新决定。它目前仍是 path-bound identity：同一路径被其他目录内容\n替换时不会自动失效，identity-change detection 属于后续 host 持久化阶段。\n\nLocal App Server 把 User trust `ConfigChange` 同时作为撤销信号：Trusted → Restricted 会使当前\nroot-bound capability lease 永久失效，拆除 local Tool/Git/search/terminal runtime、终止相关\n进程并中断活跃 Turn；filesystem 与 watcher 作为 Restricted runtime 保留。普通 Workspace\ndocument 不进入这条 mutation 或撤销 authority。\n\n## 5. Resolved config 快照\n\n解析结果是不可变、带 generation 和来源信息的值：\n\n```rust\npub struct ResolvedConfigSnapshot {\n    pub generation: ConfigGeneration,\n    pub values: ResolvedConfig,\n    pub provenance: ConfigProvenance,\n    pub diagnostics: Vec<ConfigDiagnostic>,\n}\n```\n\n当前代码区分 User authority snapshot 与 `ScopedConfigSnapshot`：后者额外携带 host-observed\n`WorkspaceConfigRevision`、provenance 和 diagnostics。它保留 Workspace MCP、Skill 与 Plugin\n内容为 pending intent；只有 Workspace preferred model 在 provider 已由 User 配置时才覆盖 user\ndefault。审批模型始终来自 User/managed configuration，Workspace 无权覆盖。未配置的 provider\n产生 diagnostic，不会暗中选择或创建 endpoint。\n\n`ConfigGeneration` 只在 consumer-visible resolved value 或 diagnostics gate 发生变化时递增。\nsnapshot 不包含：\n\n- API key、OAuth token、cookie 或 private key；\n- Plugin package mutable path；\n- MCP PID、request ID、SSE cursor 或 live session；\n- Skill 正文或任意未限制的文件 handle；\n- 当前 Tool Call 或 Thread execution state。\n\n同一组 source revision、built-in definition 和 requirements 必须确定性地产生同一 resolved\nsnapshot。provenance 至少能回答：\n\n- 最终值来自哪个 scope/source；\n- 哪个更低优先级值被覆盖；\n- 哪个 Workspace 请求因为 trust、grant 或 compatibility 未生效；\n- 哪个 requirement 拒绝或收紧了配置；\n- 哪个 generation 被 Turn/model invocation 使用。\n\n## 6. Plugin、MCP 与 Skill 接入流程\n\n完整接入流程如下：\n\n```mermaid\nflowchart LR\n    ConfigCommand[\"Typed config command\"] --> ConfigAuthority[\"Config authority\"]\n    PluginCommand[\"Plugin lifecycle command\"] --> PluginAuthority[\"Plugin authority\"]\n\n    ConfigAuthority --> ConfigSnapshot[\"ResolvedConfigSnapshot\"]\n    PluginAuthority --> PluginManager[\"Plugin manager\"]\n    ConfigSnapshot --> PluginManager\n    PluginManager --> PluginSnapshot[\"PluginActivationSnapshot\"]\n\n    ConfigSnapshot --> McpManager[\"MCP manager\"]\n    ConfigSnapshot --> SkillManager[\"Skill manager\"]\n    PluginSnapshot --> McpManager\n    PluginSnapshot --> SkillManager\n\n    McpManager --> McpSnapshot[\"McpRuntimeSnapshot\"]\n    SkillManager --> SkillSnapshot[\"SkillCatalogSnapshot\"]\n\n    ConfigSnapshot --> Composition[\"App Server composition\"]\n    PluginSnapshot --> Composition\n    McpSnapshot --> Composition\n    SkillSnapshot --> Composition\n    Policy[\"Policy / trust snapshot\"] --> Composition\n    Credentials[\"Credential revisions\"] --> Composition\n\n    Composition --> Environment[\"AgentEnvironmentSnapshot\"]\n    Environment --> SafePoint[\"Turn / model safe-point snapshots\"]\n    SafePoint --> Core[\"zeta-core\"]\n```\n\n一次普通配置更新的状态推进是：\n\n```text\nvalidate typed command against ConfigRevision\n→ commit desired config\n→ publish ResolvedConfigSnapshot generation\n→ affected managers reconcile off to the side\n→ publish Plugin/MCP/Skill generations\n→ App Server builds a new AgentEnvironmentSnapshot\n→ future safe point starts using the new environment generation\n```\n\n任何中间 runtime failure 都不能让 Config authority 回退到另一个用户未请求的值。失败以\n`Blocked`、`Broken`、`Degraded` 或 `Unavailable` 等 typed state 和 diagnostic 表达。\n\n当前本地实现同时监听 TOML semantic digest 与 SQLite `data_version`。因此手工编辑文件或同一\nprofile 的另一个进程提交 Config 后，本进程都会发布新的\n`ConfigChange { revision, generation }`；不是只有发起 RPC 的进程能刷新 Skill/MCP runtime。\n\n## 7. Plugin 贡献规则\n\nPlugin manifest 可以贡献 Skill、MCP server declaration 和静态资源。配置只保存 Plugin\nidentity、用户请求或与 Plugin authority 关联的稳定 reference，不复制 manifest 内容。\n\nPlugin contribution identity 固定包含 namespace：\n\n```text\nplugin:<plugin-id>:skill:<local-id>\nplugin:<plugin-id>:mcp:<local-id>\n```\n\n禁止把以下内容从 package 复制回普通配置：\n\n- executable 或 package-relative path；\n- Skill 相对路径；\n- requested permissions；\n- contribution definition；\n- package digest 派生 metadata。\n\n否则 Plugin package 与 Config 会成为两个互相漂移的 authority。\n\nWorkspace 可以声明：\n\n```text\nPluginRequest {\n    plugin_id,\n    version_requirement,\n    requested_scope,\n}\n```\n\n解析流程必须是：\n\n```text\nWorkspace request\n→ resolve exact package candidate\n→ display origin/digest/permissions/credential slots\n→ explicit install or enable command\n→ activation grant\n→ PluginActivationSnapshot\n```\n\nWorkspace request 不能隐含任何后续步骤。\n\n## 8. MCP 配置与运行时\n\nMCP 有两类定义来源：\n\n```text\nStandalone MCP\n    User/Workspace Config 中的 typed server definition\n\nPlugin MCP\n    PluginActivationSnapshot 中的 normalized contribution\n```\n\n二者进入 MCP manager 前都必须拥有 namespaced `McpServerId`，不能按字符串优先级静默覆盖：\n\n```text\nuser:mcp:<id>\nworkspace:<workspace-id>:mcp:<id>\nplugin:<plugin-id>:mcp:<id>\n```\n\n当前 User Config 的 MCP section 已保存：\n\n- server definition；\n- explicit enablement；\n- transport declaration；\n- `CredentialRef`；\n\n`requested workspace/root access`、runtime permission 和 local tool policy 只有在 MCP manager 与\npolicy authority 一起实现时才会加入 typed declaration，不能先用松散 JSON 字段占位。\n\nMCP runtime snapshot 可以保存：\n\n- resolved server generation；\n- negotiated capabilities；\n- tools/resources/prompts catalog；\n- connection health；\n- redacted diagnostics。\n\nPID、OAuth verifier、access token、request ID、SSE cursor 和 live session ID 既不进入普通 Config，\n也不进入 Thread event。\n\nLocal App Server 当前在 Config commit 后重新 materialize standalone MCP definitions，并原子替换\n未来 Tool catalog。新的 prepare 使用新 generation；已经 prepare、正在 review 或执行的调用仍\n绑定旧 Tool/Policy generation，直到该调用结束。新配置无法 materialize 或产生重复 Tool 名时，\ndesired Config 保持已提交，runtime 保留上一份可用 generation 并记录 reconcile diagnostic。\n\n## 9. Skill 配置与目录\n\n普通 Config 只定义显式 user/workspace Skill source。Built-in Skill 来自 Zeta release，\nPlugin Skill 来自 `PluginActivationSnapshot`。\n\n当前 User Config 还保存 source-qualified per-Skill disabled override。Enabled 是默认值，因此\n重新启用会删除 override，不为每个已发现 Skill 写冗余记录。App Server 读取该 overlay 并投影\neffective enablement；catalog entry 消失时 desired override 可以保留，重新出现后仍按同一\n`SkillId` 生效。\n\nSkill manager 负责：\n\n```text\nresolved sources\n→ containment/trust validation\n→ metadata discovery\n→ compatibility and conflict resolution\n→ SkillCatalogSnapshot\n→ selected SkillActivationSnapshot\n```\n\n`ResolvedConfigSnapshot` 不携带 Skill 正文。Core `ContextAssembler` 只接收已经冻结的 activation\n内容和 provenance，不在 model request 组装期间重新扫描文件系统。\n\n## 10. App Server 接口面\n\n外部 API 提供统一产品体验，但 mutation 按领域保持 typed：\n\n| 类别 | 方法示例 | Authority/语义 |\n| --- | --- | --- |\n| Ordinary Config | `config/read`、`config/update`、`config/changed` | Config authority；commit notification 携带 revision/generation，不包含 theme/UI device preference |\n| Provider Config | `provider/configure`、`provider/remove` | Config authority 的 Provider section |\n| Standalone MCP Config | `mcp/server/upsert`、`mcp/server/remove`、`mcp/server/enablement/set` | Config authority 的 MCP section（已实现 desired config） |\n| MCP Runtime | `mcp/server/connect`、`mcp/server/disconnect`、`mcp/server/status` | process-local lifecycle intent 与 active runtime 的 redacted projection（已实现）；不改变 Config revision |\n| MCP OAuth | `mcp/oauth/start`、`mcp/oauth/complete`、`mcp/oauth/refresh`、`mcp/oauth/revoke` | exact Config target + SecretStore lifecycle；Config 只保存 credential reference |\n| Plugin Package | `plugin/list`、`plugin/marketplace/list`、`plugin/install`、`plugin/update`、`plugin/rollback`、`plugin/uninstall` | exact package Plugin authority（已实现） |\n| Plugin Activation | `plugin/enable`、`plugin/disable`、`plugin/grant`、`plugin/revokeGrant` | exact-package Plugin authority（已实现） |\n| Plugin Request Config | `plugin/request/upsert`、`plugin/request/remove`、`plugin/request/enablement/set` | Config authority 的 exact package request（已实现；不安装或激活） |\n| Skill Source | `skill/source/add`、`skill/source/remove`、`skill/source/enablement/set` | Config authority 的 Skill section（已实现 desired config） |\n| Skill Catalog | `skills/list`、`skill/enablement/set` | App Server metadata projection + Config authority per-Skill overlay（已实现 built-in/user） |\n| Hook Config/Runtime | `hook/upsert`、`hook/remove`、`hook/enablement/set` | Config authority 保存 declaration；Local App Server 在 trusted Workspace 运行匹配的 sandbox process |\n| Agent Import Apply | `agent/import/preview`、`agent/import/apply` | App Server 将用户选择的 normalized fragments 路由到 Config 与目标 artifact authorities（Proposed） |\n\n所有 durable mutation 使用 `CommandId`、对应 authority 的 expected revision、payload conflict\n检查和 exact typed response replay。Runtime connect/disconnect 不占用 Config、Session 或 Thread\nrevision。\n\n不要把所有领域重新塞回一个无限增长的通用 `config/update` JSON patch。\n\n### 10.1 外部 Agent Import 与 Config\n\nAgent 自定义对象与导入/source registration 的 canonical 边界见\n[`agent-customizations.md`](agent-customizations.md)。[`zeta-agent-import`](../zeta-rs/agent-import/README.md)\n当前只做 metadata-only inspection；它不依赖 `zeta-config`。未来 App Server import adapter 同时\n消费 inspection/parser output 与目标领域 typed command，把用户确认的外部内容转换为 Zeta\ndesired state：\n\n| 外部内容 | Config 或目标 authority | Apply 约束 |\n| --- | --- | --- |\n| Skill source | `AddSkillSource` | 保存 source-qualified identity 与 digest，不复制不受限目录 |\n| MCP declaration | `UpsertMcpServer` | credential 不进入 Config；初始连接与 approval 分离 |\n| Plugin request | `UpsertPluginRequest` | 必须解析成 exact package/version；不表示 installed/active |\n| Hook declaration | `UpsertHook` | 默认 disabled；执行 authority 不属于 Import 或 Config |\n| Instructions | Instruction authority | canonical target 尚未完成前不得 raw passthrough |\n| Agents | Agent definition authority | 不属于普通 Config document |\n| Execution rules | Policy review | 不生成 durable approval，不自动改写成 Hook |\n\nImport apply 不能循环调用多个独立 RPC 后接受 partial success。Config 部分的目标 contract 是一个\n带 `expected_config_revision`、source digest 和 selected item identity 的原子 batch：App Server\n重新读取并校验 source，构造全部 typed mutations，Config authority 对整批 validation 后一次提交\n并生成 exact import receipt。Conflict、unsupported field 或任一 Config mutation failure 都使\nConfig 子批次不提交。\n\nInstructions 与 Agents 等非 Config artifact 必须交给各自 authority；canonical target 尚未完成\n前，Import 必须将其标记为 unsupported，而不是强塞进 Config transaction。\n\nConfig 保存 normalized desired state 与必要 provenance reference，不保存外部原始文件、secret、\npreview content 或目录访问 grant。Import source 的文件访问由一次性 host authorization 或\n`zeta-add-dir` 分别拥有，不能通过 Config mutation 反向扩大。\n\n## 11. 运行时快照与安全点\n\nApp Server 根据多个 generation 构造不可变环境：\n\n```rust\npub struct AgentEnvironmentSnapshot {\n    pub config_generation: ConfigGeneration,\n    pub plugin_generation: PluginGeneration,\n    pub mcp_generation: McpGeneration,\n    pub skill_generation: SkillGeneration,\n    pub policy_revision: ActionPolicyRevision,\n}\n```\n\n这个类型表达语义，不要求所有 generation 同步递增。App Server 必须保证发布的组合在内部一致：\n\n- Plugin MCP contribution 引用的 package generation 仍可用；\n- Skill activation 引用的 source/digest 与 catalog generation 一致；\n- MCP credential binding revision 与 materialization 输入一致；\n- requirements 不允许的能力不会进入 Tool snapshot；\n- consumer-visible snapshot 切换是原子的。\n\nCore 不依赖 `zeta-config`、Plugin/MCP/Skill live manager 或 credential store。Core 只接收自己需要的\n窄值：\n\n```text\nTurnPolicySnapshot\n    生命周期：整个 Turn\n\nModelInvocationSnapshot\n    生命周期：一次 provider request\n\nToolRegistrySnapshot\n    生命周期：一个明确的执行 safe point\n```\n\n普通配置变化只影响下一个 safe point。安全要求在运行中不能静默放宽；需要立即生效的收紧由\n明确的 interrupt/cancel policy 处理。\n\n## 12. `zeta-config` 内部结构\n\n当前保持一个 crate，私有模块按已存在的 vertical slice 划分：\n\n```text\nzeta-rs/config/src/\n├── lib.rs\n├── command.rs          # typed mutation 与 receipt payload\n├── document.rs         # user document、revision/generation、resolved values\n├── mcp.rs              # standalone MCP declaration\n├── skills.rs           # source 与 per-Skill enablement\n├── plugins.rs          # exact package request（不拥有 install/activation）\n├── hooks.rs            # safe-point/matcher/process declaration（runtime 在 App Server）\n├── mutation.rs         # typed command reducer\n├── workspace.rs        # strict read-only Workspace document\n├── resolution.rs       # scoped merge、provenance、diagnostic\n├── store.rs            # TOML authority + SQLite transaction coordination\n├── store_file.rs       # strict TOML read、semantic digest 与 atomic replace\n├── store_schema.rs     # schema installation/version gate\n└── store_monitor.rs    # cross-connection commit observation\n```\n\n目录只随可测试的 vertical slice 创建，不预先生成空模块。模块默认 private，`lib.rs` 精确导出\n稳定 API。\n\n当前不拆 `config-types`、`config-layers`、`config-policy` 或 `config-edit` crate。只有出现第二个\n真实 storage/remote consumer、独立依赖边界和可单独测试的 vertical slice 后才提取。\n\n## 13. 依赖方向\n\n允许：\n\n```text\nzeta-config → zeta-protocol\nzeta-config → zeta-model-provider-config\n\nzeta-plugins / zeta-mcp / zeta-skills\n    → zeta-config 中与自己消费语义一致的纯配置值\n\nApp Server\n    → zeta-config + zeta-plugins + zeta-mcp + zeta-skills + credentials + policy\n```\n\n禁止：\n\n```text\nzeta-config → Plugin/MCP/Skill live runtime\nzeta-config → credential materialization\nzeta-config → zeta-core\nzeta-core → Config authority or config files\nPlugin/MCP/Skill manager → ThreadStore mutation\nWorkspace Config → grant or secret authority\n```\n\n如果某个 MCP/Plugin 配置类型增长到需要被多个 crate 独立消费，并且让 `zeta-config` 与 runtime\n形成不合理依赖，再提取纯声明 crate；不能先按名称拆出一组没有独立消费者的小 crate。\n\n## 14. 验收不变量\n\n- 每个配置 scope 和领域状态只有一个 authority；\n- Config、Plugin、MCP 和 Skill generation 可以独立恢复和推进；\n- 相同 source/revision 确定性地产生相同 resolved snapshot；\n- Workspace 配置不能扩大 grant、policy、sandbox 或 credential scope；\n- Plugin contribution 不复制回普通 Config；\n- standalone、Workspace 和 Plugin MCP 使用 namespaced identity；\n- runtime reconcile 失败保留 desired config，并产生 typed diagnostic；\n- secret 不进入 Config、Plugin snapshot、Thread event、schema fixture 或日志；\n- App Server 原子发布内部一致的 `AgentEnvironmentSnapshot`；\n- 已开始的 Turn/model invocation 不被后续配置变化静默修改；\n- 所有新 test module 使用 sibling `*_tests.rs`。\n\n## 15. TOML 权威、SQLite 状态与配置档案边界\n\n本地 host 解析一个用户级 `profile_root`。`ZETA_PROFILE_ROOT` 显式覆盖；未设置时使用操作系统\n的用户 state 目录。切换 workspace 不会切换用户 Config/Session/Thread authority。\n\n```text\n<profile_root>/\n├── config.toml         # human-authored User desired configuration\n├── state.sqlite3       # transaction metadata + Session/Thread machine state\n├── state.sqlite3-wal   # SQLite WAL（运行时）\n├── state.sqlite3-shm   # SQLite shared memory（运行时）\n└── leases/             # writer lease\n\n<workspace_root>/\n└── .zeta/config.toml   # strict read-only Workspace intent\n```\n\n`state.sqlite3` 当前包含：\n\n| Component | Tables | 事务不变量 |\n| --- | --- | --- |\n| Config metadata | `config_metadata`、`config_command_receipts` | SQLite 串行化 API writer，只保存 semantic digest、revision/generation 与 exact receipt，不保存 desired document |\n| Session | `session_streams`、`session_batches`、`session_events` | sequence CAS、batch/event identity 与完整 typed envelope 原子提交 |\n| Thread | `thread_streams`、`thread_batches`、`thread_events` | sequence CAS、batch/event identity 与完整 typed envelope 原子提交 |\n| MCP server adapter | `mcp_invocation_receipts`、`mcp_thread_bindings` | principal-scoped invocation replay 与 Thread authorization；不拥有 Agent state |\n| Schema | `zeta_schema_migrations` | component 独立 version gate；不支持的版本 fail closed |\n\n| 数据类别 | TOML | SQLite |\n| --- | --- | --- |\n| User/Workspace 声明式配置正文 | ✅ 唯一 authority | ❌ 不保存副本 |\n| revision、generation、semantic digest | ❌ | ✅ |\n| command / MCP invocation receipt | ❌ | ✅ |\n| Session、Thread event 与恢复状态 | ❌ | ✅ |\n| secret bytes | ❌ | ❌，由 Secret Store 拥有 |\n\nConfig command 先在 `BEGIN IMMEDIATE` 下校验 expected revision，再以 temp-file + rename 原子替换\n`config.toml`，最后提交 metadata 与 receipt。TOML 与 SQLite 无法组成单个跨文件 ACID 事务；\n若进程在两次提交之间退出，下次读取会根据 semantic digest 恢复并推进 revision，不会从 DB\n覆盖 TOML。外部编辑只有在整份 TOML strict parse 和 typed validation 成功后才进入新 generation；\n注释或格式变化不会推进 generation。\n\nSQLite 连接统一启用 foreign keys、WAL、`synchronous=FULL` 与 bounded busy timeout。Session、\nThread 和 receipt 是 machine state；`config.toml` 是可读、可审查、可手工编辑的唯一 User Config\nauthority。旧版 `config_authority.document_json` 会在首次打开时一次性迁出到 TOML，随后删除正文列。\n\n## 16. 实施顺序\n\n1. ✅ 消除普通配置的双 authority，建立 `ConfigRevision` 和唯一 Config authority；\n2. ✅ 区分 User Config authority 与 Workspace read-only config document；\n3. 部分具备：引入 typed `ResolvedConfigSnapshot`、Workspace scoped resolution、provenance 和\n   diagnostics；Session/launch/System requirements 的 merge rule 仍待实现；\n4. ✅ 将 Provider 配置改为 `ProviderId` keyed map；\n5. ✅ 实现 standalone MCP 和 Skill source 的 desired-config section；\n6. ✅ 实现 built-in/user Skill catalog、watcher refresh 与 durable per-Skill enablement overlay；\n7. ✅ 将 Session/Thread 与 Config transaction metadata 迁到 profile SQLite，User desired config\n   保持 TOML authority；\n8. ✅ 用本地与跨 connection commit signal 驱动 Skill/MCP reconcile；\n9. ✅ MCP Tool registry 对未来调用原子切换，并保持 prepared call generation；\n10. ✅ 实现分层 Plugin authority 与 `PluginActivationSnapshot`；\n11. 部分具备：Plugin Connector/MCP contribution 已接通 live manager；Skill contribution 与\n    workspace-profile resolver 尚未接入；\n12. 发布完整跨领域 `AgentEnvironmentSnapshot`；\n13. 增加 process-kill crash、permission monotonicity 和完整 generation consistency 测试。\n14. 增加 Agent Import 的 normalized fragment、原子 Config batch、receipt 与 rollback contract。\n\nWorkspace 配置使用 TOML；RPC 仍使用 typed JSON DTO。旧 DB 内嵌 Config document 只做一次性迁出，\n不继续作为 fallback 或第二 writer。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-权威分布",
        "title": "2. 权威分布"
      },
      {
        "depth": 2,
        "id": "3-配置来源与范围",
        "title": "3. 配置来源与范围"
      },
      {
        "depth": 2,
        "id": "4-typed-配置模型",
        "title": "4. Typed 配置模型"
      },
      {
        "depth": 2,
        "id": "5-resolved-config-快照",
        "title": "5. Resolved config 快照"
      },
      {
        "depth": 2,
        "id": "6-pluginmcp-与-skill-接入流程",
        "title": "6. Plugin、MCP 与 Skill 接入流程"
      },
      {
        "depth": 2,
        "id": "7-plugin-贡献规则",
        "title": "7. Plugin 贡献规则"
      },
      {
        "depth": 2,
        "id": "8-mcp-配置与运行时",
        "title": "8. MCP 配置与运行时"
      },
      {
        "depth": 2,
        "id": "9-skill-配置与目录",
        "title": "9. Skill 配置与目录"
      },
      {
        "depth": 2,
        "id": "10-app-server-接口面",
        "title": "10. App Server 接口面"
      },
      {
        "depth": 3,
        "id": "101-外部-agent-import-与-config",
        "title": "10.1 外部 Agent Import 与 Config"
      },
      {
        "depth": 2,
        "id": "11-运行时快照与安全点",
        "title": "11. 运行时快照与安全点"
      },
      {
        "depth": 2,
        "id": "12-zeta-config-内部结构",
        "title": "12. zeta-config 内部结构"
      },
      {
        "depth": 2,
        "id": "13-依赖方向",
        "title": "13. 依赖方向"
      },
      {
        "depth": 2,
        "id": "14-验收不变量",
        "title": "14. 验收不变量"
      },
      {
        "depth": 2,
        "id": "15-toml-权威sqlite-状态与配置档案边界",
        "title": "15. TOML 权威、SQLite 状态与配置档案边界"
      },
      {
        "depth": 2,
        "id": "16-实施顺序",
        "title": "16. 实施顺序"
      }
    ],
    "searchText": " 物理位置：zeta-rs/config/ Rust crate：zetaconfig 当前状态：TOML User Config authority、SQLite revision/generation 与 exact command receipt、 Provider map、standalone MCP declaration、Skill source/per-Skill enablement、Workspace TOML read-only document、exact Plugin request、declarative Hook、User Workspace trust decision 和 scoped resolution、Tool Search 词法/混合 embedding 模式选择、User execution-policy rule persistence 与 Workspace restrictive rule intent 已实现。 Local App Server 在 profile 下使用 config.toml 与 state.sqlite3，并在提交后原子切换未来的 model、Skill 与 MCP Tool safe point； 已 prepare 的 Tool Call 保留旧 generation。Plugin contribution、grant 和完整环境组合仍是后续 vertical slice。 Core 边界：core.md Plugin 控制面：plugins.md MCP runtime：mcp.md Skill runtime：skills.md Agent 自定义对象与外部导入边界：agent-customizations.md Direct-provider credential：model-provider.md Interactive login：login.md Secret persistence：secrets.md 快速理解 配置系统回答“在当前作用域下，用户想要什么值”；领域管理器回答“现在实际可用什么”，App Server 只在安全点把两者组合成运行时快照。 用户或调用方的动作 直接结果 不代表什么 --- --- --- 修改用户配置 产生新的用户配置 revision 不代表正在运行的 Turn 立即改变 Workspace 声明模型或 MCP 进入作用域解析和信任校验 不会自动安装、授权或连接 Session 覆盖默认模型 影响后续模型安全点生成的快照 不改写用户或 Workspace 配置 Plugin、MCP 或 Skill 协调失败 保留期望配置并更新健康诊断 不会静默回滚用户想要的状态 配置引用凭据 只保存不敏感的凭据引用 不保存 token、API key 或 OAuth 状态 系统或组织设置安全要求 对解析结果施加约束 不是可以被低优先级配置覆盖的普通字段 1. 结论 Zeta 对外提供统一配置体验，但不建立一个拥有所有产品状态的巨型 Config 数据库。 长期固定三层语义： ``text Desired configuration 用户、Workspace 和领域 authority 声明“想要什么” Operational state Plugin/MCP/Skill manager 维护“当前实际是什么状态” Runtime snapshot App Server 在 safe point 冻结“本次执行能使用什么” ` zeta-config 是普通配置 authority 和 typed resolution 层。Plugin package、MCP connection、 Skill catalog、credential 和 action policy 继续由各自领域拥有。App Server 是唯一组合层： 它把各领域不可变 snapshot 汇合为 Agent 可消费的环境，而不把 live manager 或文件读取能力 注入 Core。 最重要的不变量是： - 一个 scope 内同一份期望状态只有一个 authority； - 配置提交成功不等于 Plugin 已激活、MCP 已连接、Skill 已可用或 Hook 已执行； - runtime reconcile 失败只更新 health/diagnostics，不静默改写用户期望； - Workspace 声明可以请求能力，但不能自行安装、授权、绑定 credential 或扩大 sandbox； - 运行中的 Turn 或 model request 只使用开始时冻结的 snapshot。 2. 权威分布 统一配置体验不等于统一物理 authority。长期 authority 分布如下： Authority 拥有 不拥有 --- --- --- User Config authority Agent 默认值、Provider 配置、独立 MCP server、Skill source、Plugin request、Hook declaration、execution-policy rules、按 canonical root identity 的 Workspace trust decision UI/device 偏好、installed Plugin package、live connection、Hook execution、secret、runtime health Desktop device preference authority theme、zoom、window/device UI 偏好 Agent/Provider/MCP/Skill desired config、Session/Thread Workspace config document Workspace Agent 默认值、独立 MCP 声明、Plugin 请求、Workspace Skill source、Hook 请求、只收紧的 execution-policy rules 自动安装、扩权 grant、Hook 执行、credential value、运行时状态 Plugin authority installed exact package、effective activation、activation grant、credential-slot binding、rollback TOML request、MCP session、Skill catalog、per-call approval Domain auth authority credential type、refresh、scope、credential revision 普通配置、Plugin manifest、Thread history Secret store opaque secret bytes 的 load/store/delete credential type、OAuth、scope、refresh Session authority durable SessionSettings 和产品级共享默认值 完整 Core/runtime 配置快照 Host policy authority system/organization requirements、action approval 和 sandbox decision 用户偏好、package content、secret zeta-config 可以解析 Workspace 中的 Plugin 请求，但请求进入 Plugin manager 后仍需与已安装 package、grant、trust 和 credential binding 一起解析。Workspace 文件本身永远不能成为 Plugin installed/active authority。 Direct-provider credential lifecycle 由 model-provider.md 规定，interactive login 由 login.md 规定，storage policy 由 secrets.md 规定。Config 只 保存 opaque CredentialId/account reference；不保存 credential kind 对应的 raw secret、OAuth token bundle、authorization header 或 refresh 状态。 当前 Workspace document 由 host 提供 WorkspaceId 和 content revision；文件本身不能选择自己 的 namespace 或 generation。它 strict-parse 为 TOML document，MCP/Skill/Hook ID 必须落在 workspace:<workspace-id : namespace，且 MCP declaration 不包含 credential binding。Workspace Hook 只是 pending trust 的 process request，不能因为文件存在就执行。 3. 配置来源与范围 配置 source 按以下顺序参与解析： `text BuiltInDefaults + UserConfig + WorkspaceConfig + SessionSettings + LaunchOverrides constrained by SystemRequirements = ResolvedConfigSnapshot ` 这不是无条件的递归 TOML/JSON merge。每个字段必须声明： - 可以出现在哪些 scope； - merge、replace、append 或 clear 语义； - Workspace 是否只能收紧； - 是否必须产生 provenance； - 更新影响下一个 Turn、下一次 model invocation，还是仅影响 UI； - 缺失、显式清除和继承的区别。 典型规则： 配置 允许的 source 规则 --- --- --- Theme/UI preference Desktop device authority 不进入 Rust UserConfigDocument，Workspace、Session 和 launch 不能覆盖 Preferred model User、Workspace、Session、launch 只影响下一个 model safe point Provider endpoint/profile User、host Workspace 不能静默替换认证或网络边界 Standalone MCP server User、Workspace Workspace declaration 需要 trust/grant 后才能启动 Plugin request User、Workspace 只能请求 exact package/version 与 desired enablement，不能证明已安装、激活或授权 Skill source User、Workspace 必须经过 source containment、trust 和 compatibility 校验 Hook declaration User、Workspace 只声明 safe-point、tool matcher 与 process argv；执行仍需 trust、policy、approval 和 sandbox Tool Search mode/model User 默认纯本地 BM25/Regex；混合模式独立选择 exact embedding ModelRef，由 App Server 探活后启用 Sandbox/approval intent User、Workspace、Session 低信任 source 只能保持或收紧安全性 Execution-policy rules Host、Organization、User、Workspace User 可显式授权；Workspace 只能拒绝、强制沙箱/审批或 Continue，不能 AllowUnsandboxed Workspace trust User、organization policy、trusted host composition Workspace document 无权自授信；User 缺失决策默认 Restricted System requirements System/organization 是约束，不是“最高优先级普通配置” 4. Typed 配置模型 配置 document 按领域分 section，但保持一个稳定的顶层 schema： `rust pub struct UserConfigDocument pub agent: AgentConfig, pub providers: ProvidersConfig, pub mcp: McpConfig, pub skills: SkillsConfig, pub plugins: PluginsConfig, pub hooks: HooksConfig, pub toolsearch: ToolSearchConfig, pub execpolicy: UserExecPolicyConfig, pub workspacetrust: WorkspaceTrustConfig, pub struct WorkspaceConfigDocument pub agent: WorkspaceAgentConfig, pub mcp: WorkspaceMcpConfig, pub pluginrequests: WorkspacePluginRequests, pub skills: WorkspaceSkillsConfig, pub hooks: HooksConfig, pub execpolicy: WorkspaceExecPolicyConfig, ` UserExecPolicyConfig 持久化 typed ExecPolicyRule；UserConfigCommand::UpsertExecPolicyRule 和 RemoveExecPolicyRule 走同一 expected-revision、receipt 与 atomic TOML replacement 路径。 WorkspaceExecPolicyConfig 是 strict-read intent，validation 禁止 AllowUnsandboxed。 composeexecpolicy 把它们与 trusted Host/Organization layers 组合为 immutable ExecPolicySnapshot；规则求值和 semantic revision 属于 zeta-execpolicy，最终 grant 属于 zeta-action-policy，不属于 Config。 User Config 的 PluginsConfig 保存 exact package request 与 desired enablement；Workspace 保存 scoped request。Plugin installed state、effective activation、grant、digest 和 rollback 不复制进 普通 Config document，这些值属于 Plugin authority。 HooksConfig 保存 namespaced Hook ID、beforeTool/afterTool/turnCompleted safe-point、 exact tool-name matcher、process program + args 与 desired enablement。它不保存 PID、环境、 执行队列、结果或 retry。Local App Server 的 Hook runtime 在 trusted Workspace 中按 immutable 配置快照匹配事件，经 Host Policy 评估后使用统一 sandbox executor；Restricted Workspace 不安装 process runner。TurnCompleted 在 durable completion 后 best-effort 执行，beforeTool/afterTool 遵守 cancellation，并在配置变更后热替换未来 safe point。 Theme 已从 Rust Config schema、command 和 App Server Config DTO 中移除。Desktop device 配置只拥有 device/UI preference；它不能再作为 Agent、Provider、MCP、Skill、 Session 或 Thread 的第二 authority。 所有 section 必须是 typed schema。禁止使用以下通用逃生口： `rust pub options: serdejson::Value pub extra: HashMap<String, serdejson::Value ` Provider 配置继续由 zeta-model-provider-config 定义。zeta-config 可以保存 BTreeMap<ProviderId, ModelProviderConfig ，但不重新定义 Provider normalization 或 runtime。 ModelProviderConfig.modelcontext 以 ModelId 为 key 保存可选的 contextwindow 与 autocompacttokenlimit。这些值只决定 Core 是否能使用确定性预算和 durable compaction，不 改变 provider endpoint 或模型选择；窗口或阈值为零会在静态配置校验时被拒绝。目录和配置都无 已知窗口时，Core 明确退回 provider-managed，不猜测默认窗口。 Deferred Agent 工具检索只有两档 User intent：toolSearch.mode = \"lexical\"（默认）和 toolSearch.mode = \"hybridEmbedding\"。混合模式还必须配置 toolSearch.embeddingModel；该 ModelRef 与 CodeIndex 的 embedding/rerank 选择彼此独立，只共享 provider runtime 和 credential materialization。配置本身不保存模型实例、向量或 provider credential。 toolSearch/configure 会在 durable commit 前解析模型并用固定文本完成 readiness probe。未配置 provider、credential/runtime 或 probe 失败时返回 ToolSearchUnavailable，不会打开门禁。外部 TOML 或启动恢复出的不可用 hybrid 配置不会拖垮 App Server；config/read 通过 toolSearch.embeddingStatus = unavailable 暴露原因，但自然语言 Tool Search 会明确失败，不会 偷偷切到 BM25。显式 Regex 仍在本地运行；用户显式改回 lexical 后，BM25 才重新成为自然语言 检索路径。门禁通过后，实际 embedding 调用失败同样使该次 Tool Search 明确失败。 Semantic CodeIndex 使用独立的 semanticCodeIndex.selection 和按 Workspace trust ID 保存的 source-egress grant。授权会冻结 embedding/rerank ModelRef 以及它们实际使用的 provider config；模型或 endpoint 变化后 activeWorkspaceAuthorized 立即变为 false，App Server 卸载旧 semantic runtime。Desktop 的 Indexing 设置页可保存 Ollama/unauthenticated OpenAI-compatible endpoint、模型选择并 authorize/revoke； 普通聊天模型配置和 Workspace 文件读取权限都不会自动授予源码外发。 自动审批模型是独立于主 Agent 模型的 User 配置： `rust pub enum ApprovalReviewModelSelection Automatic, Explicit model: ModelRef , ` Automatic 跟随当前主 Agent 模型的 provider，再由该 provider 的声明选择适合审批的默认 review model；没有专用默认值的自定义或本地 provider 才复用当前模型。它不是一个全局固定 模型。Explicit 允许用户锁定一个已经配置、且满足 review capability gate 的 provider/model。 显式模型的 provider 不能在仍被引用时删除。配置层能够验证 provider、静态 catalog 与 endpoint； credential、订阅 entitlement 和远端模型是否实际可调用，由创建 review runtime 时再次验证。 Workspace 配置不能覆盖审批模型，避免仓库内容自行降低 reviewer 强度。模型不可用或不兼容时 fail closed，不得静默换成其他审批模型。 WorkspaceTrustConfig 使用 WorkspaceTrustId 作为 key。该 ID 由 zeta-workspace 对 canonical root 的平台原生 path bytes 做 SHA-256 得到，因此不把本机路径写入 User TOML，symlink/平台别名 共享决定，移动根目录后必须重新决定。它目前仍是 path-bound identity：同一路径被其他目录内容 替换时不会自动失效，identity-change detection 属于后续 host 持久化阶段。 Local App Server 把 User trust ConfigChange 同时作为撤销信号：Trusted → Restricted 会使当前 root-bound capability lease 永久失效，拆除 local Tool/Git/search/terminal runtime、终止相关 进程并中断活跃 Turn；filesystem 与 watcher 作为 Restricted runtime 保留。普通 Workspace document 不进入这条 mutation 或撤销 authority。 5. Resolved config 快照 解析结果是不可变、带 generation 和来源信息的值： `rust pub struct ResolvedConfigSnapshot pub generation: ConfigGeneration, pub values: ResolvedConfig, pub provenance: ConfigProvenance, pub diagnostics: Vec<ConfigDiagnostic , ` 当前代码区分 User authority snapshot 与 ScopedConfigSnapshot：后者额外携带 host-observed WorkspaceConfigRevision、provenance 和 diagnostics。它保留 Workspace MCP、Skill 与 Plugin 内容为 pending intent；只有 Workspace preferred model 在 provider 已由 User 配置时才覆盖 user default。审批模型始终来自 User/managed configuration，Workspace 无权覆盖。未配置的 provider 产生 diagnostic，不会暗中选择或创建 endpoint。 ConfigGeneration 只在 consumer-visible resolved value 或 diagnostics gate 发生变化时递增。 snapshot 不包含： - API key、OAuth token、cookie 或 private key； - Plugin package mutable path； - MCP PID、request ID、SSE cursor 或 live session； - Skill 正文或任意未限制的文件 handle； - 当前 Tool Call 或 Thread execution state。 同一组 source revision、built-in definition 和 requirements 必须确定性地产生同一 resolved snapshot。provenance 至少能回答： - 最终值来自哪个 scope/source； - 哪个更低优先级值被覆盖； - 哪个 Workspace 请求因为 trust、grant 或 compatibility 未生效； - 哪个 requirement 拒绝或收紧了配置； - 哪个 generation 被 Turn/model invocation 使用。 6. Plugin、MCP 与 Skill 接入流程 完整接入流程如下： `mermaid flowchart LR ConfigCommand \"Typed config command\" -- ConfigAuthority \"Config authority\" PluginCommand \"Plugin lifecycle command\" -- PluginAuthority \"Plugin authority\" ConfigAuthority -- ConfigSnapshot \"ResolvedConfigSnapshot\" PluginAuthority -- PluginManager \"Plugin manager\" ConfigSnapshot -- PluginManager PluginManager -- PluginSnapshot \"PluginActivationSnapshot\" ConfigSnapshot -- McpManager \"MCP manager\" ConfigSnapshot -- SkillManager \"Skill manager\" PluginSnapshot -- McpManager PluginSnapshot -- SkillManager McpManager -- McpSnapshot \"McpRuntimeSnapshot\" SkillManager -- SkillSnapshot \"SkillCatalogSnapshot\" ConfigSnapshot -- Composition \"App Server composition\" PluginSnapshot -- Composition McpSnapshot -- Composition SkillSnapshot -- Composition Policy \"Policy / trust snapshot\" -- Composition Credentials \"Credential revisions\" -- Composition Composition -- Environment \"AgentEnvironmentSnapshot\" Environment -- SafePoint \"Turn / model safe-point snapshots\" SafePoint -- Core \"zeta-core\" ` 一次普通配置更新的状态推"
  },
  {
    "slug": "login",
    "sourcePath": "docs/login.md",
    "title": "登录与账户系统",
    "description": "登录系统是面向用户的账户控制面，不是通用 OAuth 实现。当前 LoginService 已拥有稳定 login ID、 取消、完成、登出和 revisioned account projection；具体 ChatGPT/Codex 登录由 zeta-codex-app-server driver 委托上游 Codex App Server。",
    "group": "模型与配置",
    "markdown": "> 物理位置：`zeta-rs/login/`\n> Rust crate：`zeta_login`\n> 当前状态：控制面、App Server RPC、Codex managed-login driver 与 Codex Turn backend adapter 已实现；\n> 产品通过显式 `openai-chatgpt` 模型选择接入 subscription Turn\n> ChatGPT/Codex subscription adapter：[`codex-app-server.md`](codex-app-server.md)\n> Provider runtime：[`model-provider.md`](model-provider.md)\n> Secret persistence：[`secrets.md`](secrets.md)\n\n## 快速理解\n\n登录系统是面向用户的账户控制面，不是通用 OAuth 实现。当前 `LoginService` 已拥有稳定 login ID、\n取消、完成、登出和 revisioned account projection；具体 ChatGPT/Codex 登录由\n`zeta-codex-app-server` driver 委托上游 Codex App Server。\n\n| 用户动作或凭据类型 | 由谁处理 | Zeta 登录系统能看到什么 |\n| --- | --- | --- |\n| ChatGPT/Codex 浏览器或设备码登录 | 上游 Codex App Server | 授权地址、一次性用户码和脱敏账户状态 |\n| 登出、取消或切换账户 | 登录控制面协调，供应商适配器执行 | 稳定状态和脱敏结果 |\n| OpenAI、Anthropic 等 API key | 对应模型凭据领域 | 不属于交互式登录 |\n| AWS 凭据链、Google ADC、Azure 托管身份 | 对应供应商运行时 | 不包装成通用 OAuth |\n| access token、refresh token、cookie | 精确供应商适配器或上游服务 | 不进入 Zeta RPC、日志或遥测 |\n\n## 1. 结论\n\n`zeta-login` 是用户可见的身份登录控制面。它统一表达开始、取消、完成、登出、账户切换和\nreauthentication-required 状态；它不把不同 Provider 的 credential 协议伪装成一套通用 OAuth\n实现。\n\n当前实现是 provider-neutral control plane：`InteractiveLoginDriver` 接收 service-owned `LoginId`，\n只返回 browser/device-code UI instruction 和 redacted account snapshot。App Server 已暴露\n`account/read`、`account/login/start`、`account/login/cancel`、`account/logout`，并主动发布\n`account/login/completed` 与 `account/updated`。本地默认 composition 已安装\n**ChatGPT/Codex managed-login driver**：它懒启动上游 `codex app-server`，由上游完成 token 持久化\n和刷新；Zeta 不读取、保存、交换或刷新 ChatGPT token。上游 thread/Turn 已由\n`CodexTurnExecutionBackend` 映射到 Core。默认 App Server 将 ChatGPT account 可用的 Codex model\n投影为 `openai-chatgpt` provider；只有 Session 显式选中该 ModelRef 后，新 Turn 才走订阅后端。\n登录完成不会自动切换已有 Session 或 Thread 的执行路径。\n\n未来只有在 Provider 的官方条款和技术接口明确允许时，才增加新的登录 adapter。API key、AWS\ncredential chain、Google ADC 和 Azure managed identity 不是 interactive login 的变体，仍由各自\nProvider runtime 解析和 materialize。\n\n## 2. 所有权\n\n`zeta-login` 拥有：\n\n- redacted account/session projection；\n- login request 的生命周期和稳定 `LoginId`；\n- begin/cancel/logout/account-changed/reauthentication-required 的状态转换；\n- UI/CLI/App Server 所需的授权 URL、device code、进度和稳定错误的安全投影；\n- 对 provider-specific interactive login adapter 的最小 consumer-owned port。\n\n`zeta-login` 不拥有：\n\n- OAuth authorize/token/revoke HTTP codec；\n- PKCE、state、callback listener、refresh token 或 cookie；\n- API key、AWS SigV4、Google ADC 等非交互凭据的 materialization；\n- `SecretStore`、keychain 或 token 的序列化；\n- 模型 endpoint、模型请求、retry、SSE 或 telemetry；\n- Provider 选择和模型执行。\n\n## 3. 与 Codex subscription 的关系\n\n```text\nZeta Desktop / CLI / TUI\n             │ account/login/*\n             ▼\nzeta-app-server\n             ▼\nzeta-login\n             │ InteractiveLoginDriver\n             ▼\nzeta-codex-app-server\n             │ JSON-RPC + local child-process lifecycle\n             ▼\nupstream `codex app-server`\n             │ managed browser/device-code login and refresh\n             ▼\nChatGPT subscription\n```\n\n授权 URL 和一次性 device code 可以回传给 UI；access token、refresh token、PKCE verifier、callback\ncode、cookie 和上游 credential-file path 都不能进入 Zeta App Server RPC、desktop IPC、日志或\ntelemetry。\n\n上游 App Server 的 transport authentication（例如本地 WebSocket capability token）与 ChatGPT\nsubscription credential 是两种完全不同的 secret，二者不能复用。\n\n## 4. 最小公共边界\n\n当前公共 trait 保持 consumer-owned，并要求 driver 保留 service 分配的 exact `LoginId`：\n\n```rust\n/// Starts and observes one provider-owned interactive account login.\n///\n/// Implementations keep provider credentials private. They may return only\n/// redacted UI instructions and must make cancellation idempotent for one login.\npub trait InteractiveLoginDriver: Send + Sync {\n    fn read_account(&self) -> Result<Option<AccountSnapshot>, LoginError>;\n    fn begin(&self, request: BeginLoginRequest) -> Result<BeginLogin, LoginError>;\n    fn cancel(&self, login_id: &LoginId) -> Result<CancelLoginOutcome, LoginError>;\n    fn logout(&self, account: &AccountRef) -> Result<(), LoginError>;\n}\n```\n\n`BeginLogin` 是 tagged result，例如 `Browser { authorization_url }` 或\n`DeviceCode { verification_url, user_code }`，而不是多个可混淆的 `Option` 字段。完成事件只携带\nsuccess/failure 与 redacted account snapshot。\n\n不要在第一版预建“万能 OAuth driver”：resource/audience、dynamic client registration、device code、\nworkspace selection 和 consent semantics 不能由一个宽泛 DTO 正确覆盖。新增 Provider 时先定义其\n正式能力和专用 adapter，再决定是否能复用此控制面。\n\n## 5. 依赖方向\n\n```text\nzeta-app-server ──▶ zeta-login\nzeta-codex-app-server ──▶ zeta-login     # implements interactive login driver\nzeta-codex-app-server ──▶ zeta-core      # implements TurnExecutionBackend\n\nzeta-login -/-> zeta-secrets\nzeta-login -/-> zeta-api / zeta-client / zeta-http-client\nzeta-model-provider -/-> zeta-login\n```\n\n`zeta-app-server` 是 composition root：它已将 Codex adapter 注入 login service，并把 redacted\ncontrol plane 映射到 RPC。Codex Turn adapter 实现 Core 的 `TurnExecutionBackend`，不经过\n`zeta-model-provider`；产品层仍需用显式 model/backend selection 决定哪些新 Turn 使用它。用户点击\n登录本身不能启动浏览器之外的模型执行，也不能隐式替换执行后端。\n\n## 6. 固定决策\n\n1. `zeta-login` 是登录控制面，不是 credential manager 或 OAuth protocol crate。\n2. ChatGPT/Codex subscription 委托给上游 Codex App Server；不直接复用其 OAuth client ID 或\n   backend token。\n3. 仅在官方允许时增加其他 interactive login adapter；Anthropic subscription 不进入此范围。\n4. Secret persistence 仍是各 credential owner 对 `zeta-secrets` 的直接依赖；login service 不读取\n   secret bytes。\n5. API key 的录入可以由 App Server 的 account/settings command 触发，但它不是 OAuth login，不能\n   迫使 `zeta-login` 持有 API key。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-所有权",
        "title": "2. 所有权"
      },
      {
        "depth": 2,
        "id": "3-与-codex-subscription-的关系",
        "title": "3. 与 Codex subscription 的关系"
      },
      {
        "depth": 2,
        "id": "4-最小公共边界",
        "title": "4. 最小公共边界"
      },
      {
        "depth": 2,
        "id": "5-依赖方向",
        "title": "5. 依赖方向"
      },
      {
        "depth": 2,
        "id": "6-固定决策",
        "title": "6. 固定决策"
      }
    ],
    "searchText": " 物理位置：zeta-rs/login/ Rust crate：zetalogin 当前状态：控制面、App Server RPC、Codex managed-login driver 与 Codex Turn backend adapter 已实现； 产品通过显式 openai-chatgpt 模型选择接入 subscription Turn ChatGPT/Codex subscription adapter：codex-app-server.md Provider runtime：model-provider.md Secret persistence：secrets.md 快速理解 登录系统是面向用户的账户控制面，不是通用 OAuth 实现。当前 LoginService 已拥有稳定 login ID、 取消、完成、登出和 revisioned account projection；具体 ChatGPT/Codex 登录由 zeta-codex-app-server driver 委托上游 Codex App Server。 用户动作或凭据类型 由谁处理 Zeta 登录系统能看到什么 --- --- --- ChatGPT/Codex 浏览器或设备码登录 上游 Codex App Server 授权地址、一次性用户码和脱敏账户状态 登出、取消或切换账户 登录控制面协调，供应商适配器执行 稳定状态和脱敏结果 OpenAI、Anthropic 等 API key 对应模型凭据领域 不属于交互式登录 AWS 凭据链、Google ADC、Azure 托管身份 对应供应商运行时 不包装成通用 OAuth access token、refresh token、cookie 精确供应商适配器或上游服务 不进入 Zeta RPC、日志或遥测 1. 结论 zeta-login 是用户可见的身份登录控制面。它统一表达开始、取消、完成、登出、账户切换和 reauthentication-required 状态；它不把不同 Provider 的 credential 协议伪装成一套通用 OAuth 实现。 当前实现是 provider-neutral control plane：InteractiveLoginDriver 接收 service-owned LoginId， 只返回 browser/device-code UI instruction 和 redacted account snapshot。App Server 已暴露 account/read、account/login/start、account/login/cancel、account/logout，并主动发布 account/login/completed 与 account/updated。本地默认 composition 已安装 ChatGPT/Codex managed-login driver：它懒启动上游 codex app-server，由上游完成 token 持久化 和刷新；Zeta 不读取、保存、交换或刷新 ChatGPT token。上游 thread/Turn 已由 CodexTurnExecutionBackend 映射到 Core。默认 App Server 将 ChatGPT account 可用的 Codex model 投影为 openai-chatgpt provider；只有 Session 显式选中该 ModelRef 后，新 Turn 才走订阅后端。 登录完成不会自动切换已有 Session 或 Thread 的执行路径。 未来只有在 Provider 的官方条款和技术接口明确允许时，才增加新的登录 adapter。API key、AWS credential chain、Google ADC 和 Azure managed identity 不是 interactive login 的变体，仍由各自 Provider runtime 解析和 materialize。 2. 所有权 zeta-login 拥有： - redacted account/session projection； - login request 的生命周期和稳定 LoginId； - begin/cancel/logout/account-changed/reauthentication-required 的状态转换； - UI/CLI/App Server 所需的授权 URL、device code、进度和稳定错误的安全投影； - 对 provider-specific interactive login adapter 的最小 consumer-owned port。 zeta-login 不拥有： - OAuth authorize/token/revoke HTTP codec； - PKCE、state、callback listener、refresh token 或 cookie； - API key、AWS SigV4、Google ADC 等非交互凭据的 materialization； - SecretStore、keychain 或 token 的序列化； - 模型 endpoint、模型请求、retry、SSE 或 telemetry； - Provider 选择和模型执行。 3. 与 Codex subscription 的关系 ``text Zeta Desktop / CLI / TUI │ account/login/ ▼ zeta-app-server ▼ zeta-login │ InteractiveLoginDriver ▼ zeta-codex-app-server │ JSON-RPC + local child-process lifecycle ▼ upstream codex app-server │ managed browser/device-code login and refresh ▼ ChatGPT subscription ` 授权 URL 和一次性 device code 可以回传给 UI；access token、refresh token、PKCE verifier、callback code、cookie 和上游 credential-file path 都不能进入 Zeta App Server RPC、desktop IPC、日志或 telemetry。 上游 App Server 的 transport authentication（例如本地 WebSocket capability token）与 ChatGPT subscription credential 是两种完全不同的 secret，二者不能复用。 4. 最小公共边界 当前公共 trait 保持 consumer-owned，并要求 driver 保留 service 分配的 exact LoginId： `rust /// Starts and observes one provider-owned interactive account login. /// /// Implementations keep provider credentials private. They may return only /// redacted UI instructions and must make cancellation idempotent for one login. pub trait InteractiveLoginDriver: Send + Sync fn readaccount &self - Result<Option<AccountSnapshot , LoginError ; fn begin &self, request: BeginLoginRequest - Result<BeginLogin, LoginError ; fn cancel &self, loginid: &LoginId - Result<CancelLoginOutcome, LoginError ; fn logout &self, account: &AccountRef - Result< , LoginError ; ` BeginLogin 是 tagged result，例如 Browser authorizationurl 或 DeviceCode verificationurl, usercode ，而不是多个可混淆的 Option 字段。完成事件只携带 success/failure 与 redacted account snapshot。 不要在第一版预建“万能 OAuth driver”：resource/audience、dynamic client registration、device code、 workspace selection 和 consent semantics 不能由一个宽泛 DTO 正确覆盖。新增 Provider 时先定义其 正式能力和专用 adapter，再决定是否能复用此控制面。 5. 依赖方向 `text zeta-app-server ──▶ zeta-login zeta-codex-app-server ──▶ zeta-login implements interactive login driver zeta-codex-app-server ──▶ zeta-core implements TurnExecutionBackend zeta-login -/- zeta-secrets zeta-login -/- zeta-api / zeta-client / zeta-http-client zeta-model-provider -/- zeta-login ` zeta-app-server 是 composition root：它已将 Codex adapter 注入 login service，并把 redacted control plane 映射到 RPC。Codex Turn adapter 实现 Core 的 TurnExecutionBackend，不经过 zeta-model-provider；产品层仍需用显式 model/backend selection 决定哪些新 Turn 使用它。用户点击 登录本身不能启动浏览器之外的模型执行，也不能隐式替换执行后端。 6. 固定决策 1. zeta-login 是登录控制面，不是 credential manager 或 OAuth protocol crate。 2. ChatGPT/Codex subscription 委托给上游 Codex App Server；不直接复用其 OAuth client ID 或 backend token。 3. 仅在官方允许时增加其他 interactive login adapter；Anthropic subscription 不进入此范围。 4. Secret persistence 仍是各 credential owner 对 zeta-secrets 的直接依赖；login service 不读取 secret bytes。 5. API key 的录入可以由 App Server 的 account/settings command 触发，但它不是 OAuth login，不能 迫使 zeta-login` 持有 API key。"
  },
  {
    "slug": "git",
    "sourcePath": "docs/git.md",
    "title": "Git 与 Desktop SCM：系统边界和当前状态",
    "description": "Desktop Renderer、Native 和 TUI 不启动 Git 进程，也不解析 Git 输出。Workspace-scoped App Server 接收 typed Git intent，调用 zeta-git，再把 client-safe DTO 返回产品入口。",
    "group": "平台与交付",
    "markdown": "> 本文拥有 Git 跨进程 ownership、用户可见语义和演进状态。`zeta-git` 的命令、解析、\n> timeout 与失败细节以 [`zeta-rs/git/README.md`](../zeta-rs/git/README.md) 为准；\n> external wire shape 以 [`zeta-app-server-api.md`](zeta-app-server-api.md) 为准。\n\n## 快速理解\n\nDesktop Renderer、Native 和 TUI 不启动 Git 进程，也不解析 Git 输出。Workspace-scoped App\nServer 接收 typed Git intent，调用 `zeta-git`，再把 client-safe DTO 返回产品入口。\n\n```text\nDesktop SCM View\n  ↔ Electron typed IPC + generic notification\n  ↔ App Server GitRuntime\n  → GitService\n  → zeta-git\n  → system Git\n```\n\n这条依赖方向让 workspace path authority、Git executable identity、process limits 和 output\nparsing 留在 Rust host。Renderer 只拥有展示状态和用户 intent。\n\n| 用户操作 | 当前行为 | 关键限制 |\n| --- | --- | --- |\n| 查看更改 | 自动读取并投影工作区范围内的 Git 状态 | 当前只支持单工作区 |\n| 暂存或取消暂存 | 使用明确的工作区相对路径 | 不能越过工作区边界 |\n| 丢弃更改 | 只恢复已跟踪文件，并在界面确认 | 不删除未跟踪文件 |\n| 切换本地分支 | Native 点击底栏当前分支，在菜单中选择另一个本地分支；请求通过 App Server | 冲突时 Git 拒绝切换并保留当前工作树 |\n| 拉取远端 | 只允许 fast-forward | 需要交互认证时失败 |\n| 提交和推送 | 使用系统 Git 的当前仓库配置 | 尚无凭据提示和进度 UI |\n\n## 所有权\n\n| 层级 | 当前职责 | 不拥有 |\n| --- | --- | --- |\n| Desktop SCM | 分支/upstream、Merge/Staged/Working Tree 分组，提交输入，Git intent，以及按 revision 接收自动状态更新 | Git process、porcelain parser、任意 host path authority |\n| Electron bridge | 校验 workspace-relative path、commit message 和空参数，再把 typed Git intent 转发给 App Server | Git domain semantics、最终路径授权 |\n| App Server `GitRuntime` | 串行化 operation、维护 workspace projection/revision、消费 watcher hint、去重并发布状态 | Git command/parsing、Renderer state |\n| App Server `GitService` | 冻结 workspace root、映射 workspace/repository path、持有 Tokio runtime并调用 `zeta-git` | live projection、notification |\n| `zeta-app-server-protocol` | Git query/mutation、`git/statusChanged`、DTO、capability 和 stable error name | process/runtime state |\n| `zeta-git` | system Git identity、仓库发现、porcelain-v2 snapshot、HEAD/worktree 文本 Diff 与增删行统计、typed mutation 与结构化 parsing | App Server lifecycle、workspace product boundary、Renderer state |\n\n## 当前状态\n\n当前已经实现 status 与常用用户 mutation 的完整纵向切片：\n\n- App Server 只在 local composition 收到可信 workspace root 时声明\n  `initialize.capabilities.git = true`；\n- `git/status` 不接受路径；mutation 只接受 workspace-relative path，Electron 和 Rust 边界都会\n  拒绝绝对路径、空路径和父目录逃逸；\n- workspace 位于更大 repository 内时，App Server 会过滤 repository 其他目录并把 path 重新映射为\n  workspace-relative；mutation 则把合法 workspace path 映射回 repository-relative path；\n- 每次请求重新打开仓库并读取 authoritative snapshot，不把旧 snapshot 当作 mutation 前提；\n- response 保留 HEAD branch/detached/unborn、upstream ahead/behind、index/worktree 状态、\n  rename original path、conflict 和 submodule flags，并带 repository-relative `workspacePath`、\n  Git runtime `streamInstanceId` 与在该实例内单调递增的 workspace status revision；协议不暴露\n  repository 的 host 绝对路径；\n- `git/stage`、`git/unstage` 和 `git/discardWorktree` 使用明确 path set；discard 只恢复 tracked\n  working-tree 内容，不删除 untracked 文件，Desktop 在执行前要求确认；\n- `git/commit` 从 stdin 传入经过校验的 message，并返回新 commit object ID；\n- `git/textDiff` 返回 workspace-scoped status、受限 UTF-8 HEAD/worktree 文本与增删行统计；\n- `git/branch/list` 返回现有本地分支，`git/branch/switch` 只接受 branch name，并在 host 重新解析为\n  当前仓库真实分支后执行切换；\n- `git/fetch` 执行 all-remotes prune，`git/pull` 仅允许 fast-forward，`git/push` 使用 Git 当前\n  upstream/default 配置；所有 remote operation 都是 non-interactive；\n- 每个成功 mutation 都返回新的 `GitStatusResult`，Desktop 立即重绘；首次打开 View 也会自动刷新；\n- App Server 监听 workspace、Git metadata 和 workspace 上层 repository `.gitignore`，以 100ms\n  debounce 合并 burst；事件只触发重新查询，不直接成为 Git 状态；\n- 新 snapshot 按 workspace 投影去重；HEAD/change 未改变时 revision 不推进也不发通知，变化时通过\n  `git/statusChanged` 推送完整 snapshot；\n- SCM View 只在相同 `streamInstanceId` 内按 revision 拒绝旧 notification/response；连接重新\n  ready 时主动刷新，并接受新 runtime 从较小 revision 开始的 snapshot。已退役实例的迟到通知\n  不能覆盖新状态。Watcher 初始化失败不会关闭 Git RPC，用户仍可手动 Refresh。\n\nNative 通过 `zeta-app-server-client` 消费 `git/textDiff`，在 Composer 底栏展示\n`Changes files • +additions -deletions`，并从协议中的原始/修改文本重建 presentation-only\n`DiffDocument`。文件内容读取、replacement 计数和 binary/size skip 规则仍由 `zeta-git` 统一拥有；\nNative 只负责标签、侧栏状态和 MultiDiff presentation。点击 Changes action 会请求刷新 Git\nprojection、展开右栏并选择 Changes Pane。cwd picker 使用 `workspacePath` 从当前可信工作区还原\nrepository root 快捷项；选择该项会把工作区切换到 repository root，使 Changes 投影覆盖整个仓库。\n\nNative 的底栏分支按钮复用通用 `ContextMenu`，候选项来自 `git/branch/list`，切换通过\n`git/branch/switch`。Git 对脏工作树或 linked worktree 冲突保持权威：失败时不重试、不丢弃改动，\n菜单保留并显示失败；成功后使用新的 typed projection 刷新 Files、HEAD、Changes 和 MultiDiff。\n`zeterm/zeterm` 不再依赖 `zeta-git`。\n\n稳定失败边界为 `GitUnavailable`、`GitNotRepository` 和 `GitOperationFailed`。内部 executable、\nstderr、磁盘绝对路径和非 UTF-8 path 不进入 Renderer。\n\n## 当前限制\n\n- 当前是单 workspace `GitRuntime`，尚无 multi-repository registry；\n- operation 由 runtime mutex 串行化，但尚无可观测 queue、progress、caller cancellation 或 retry；\n- App Server 与 Native 已支持切换现有本地分支；系统仍无 branch 新建/删除/重命名、\n  tag/worktree mutation 或 credential prompt；`zeta code` TUI 只消费 branch/dirty 会话上下文，\n  当前产品定义不包含 SCM 管理 UI；\n- pull 固定为 fast-forward only；discard 不删除 untracked 文件；\n- 当前是单 workspace root contract，不是 multi-root repository collection；\n- SCM change row 尚未接入 editor diff/open workflow。\n\n这些限制必须在 UI 中保持可见：未实现的 mutation 不注册空命令，也不显示会误导用户的按钮。\n\n## 分阶段演进\n\n近期扩展顺序：\n\n1. 增加显式 repository identity 与 multi-root registry；\n2. 为长时间 remote operation 增加 progress、queue state 和 caller cancellation；\n3. 接入 diff/open 与更细粒度的错误 UI；\n4. 按明确产品语义增加 branch lifecycle 等额外 mutation，并让已接受该产品需求的\n   UI consumer 消费同一协议。协议存在不自动使其成为 TUI 功能。\n\n长期不变量是：Desktop、Native 与 TUI 不直接执行 Git；App Server adapter 不复制 Git\ncommand/parsing；watch event 只触发重新确认，不能自身成为 repository truth。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "当前状态",
        "title": "当前状态"
      },
      {
        "depth": 2,
        "id": "当前限制",
        "title": "当前限制"
      },
      {
        "depth": 2,
        "id": "分阶段演进",
        "title": "分阶段演进"
      }
    ],
    "searchText": " 本文拥有 Git 跨进程 ownership、用户可见语义和演进状态。zeta-git 的命令、解析、 timeout 与失败细节以 zeta-rs/git/README.md 为准； external wire shape 以 zeta-app-server-api.md 为准。 快速理解 Desktop Renderer、Native 和 TUI 不启动 Git 进程，也不解析 Git 输出。Workspace-scoped App Server 接收 typed Git intent，调用 zeta-git，再把 client-safe DTO 返回产品入口。 ``text Desktop SCM View ↔ Electron typed IPC + generic notification ↔ App Server GitRuntime → GitService → zeta-git → system Git ` 这条依赖方向让 workspace path authority、Git executable identity、process limits 和 output parsing 留在 Rust host。Renderer 只拥有展示状态和用户 intent。 用户操作 当前行为 关键限制 --- --- --- 查看更改 自动读取并投影工作区范围内的 Git 状态 当前只支持单工作区 暂存或取消暂存 使用明确的工作区相对路径 不能越过工作区边界 丢弃更改 只恢复已跟踪文件，并在界面确认 不删除未跟踪文件 切换本地分支 Native 点击底栏当前分支，在菜单中选择另一个本地分支；请求通过 App Server 冲突时 Git 拒绝切换并保留当前工作树 拉取远端 只允许 fast-forward 需要交互认证时失败 提交和推送 使用系统 Git 的当前仓库配置 尚无凭据提示和进度 UI 所有权 层级 当前职责 不拥有 --- --- --- Desktop SCM 分支/upstream、Merge/Staged/Working Tree 分组，提交输入，Git intent，以及按 revision 接收自动状态更新 Git process、porcelain parser、任意 host path authority Electron bridge 校验 workspace-relative path、commit message 和空参数，再把 typed Git intent 转发给 App Server Git domain semantics、最终路径授权 App Server GitRuntime 串行化 operation、维护 workspace projection/revision、消费 watcher hint、去重并发布状态 Git command/parsing、Renderer state App Server GitService 冻结 workspace root、映射 workspace/repository path、持有 Tokio runtime并调用 zeta-git live projection、notification zeta-app-server-protocol Git query/mutation、git/statusChanged、DTO、capability 和 stable error name process/runtime state zeta-git system Git identity、仓库发现、porcelain-v2 snapshot、HEAD/worktree 文本 Diff 与增删行统计、typed mutation 与结构化 parsing App Server lifecycle、workspace product boundary、Renderer state 当前状态 当前已经实现 status 与常用用户 mutation 的完整纵向切片： - App Server 只在 local composition 收到可信 workspace root 时声明 initialize.capabilities.git = true； - git/status 不接受路径；mutation 只接受 workspace-relative path，Electron 和 Rust 边界都会 拒绝绝对路径、空路径和父目录逃逸； - workspace 位于更大 repository 内时，App Server 会过滤 repository 其他目录并把 path 重新映射为 workspace-relative；mutation 则把合法 workspace path 映射回 repository-relative path； - 每次请求重新打开仓库并读取 authoritative snapshot，不把旧 snapshot 当作 mutation 前提； - response 保留 HEAD branch/detached/unborn、upstream ahead/behind、index/worktree 状态、 rename original path、conflict 和 submodule flags，并带 repository-relative workspacePath、 Git runtime streamInstanceId 与在该实例内单调递增的 workspace status revision；协议不暴露 repository 的 host 绝对路径； - git/stage、git/unstage 和 git/discardWorktree 使用明确 path set；discard 只恢复 tracked working-tree 内容，不删除 untracked 文件，Desktop 在执行前要求确认； - git/commit 从 stdin 传入经过校验的 message，并返回新 commit object ID； - git/textDiff 返回 workspace-scoped status、受限 UTF-8 HEAD/worktree 文本与增删行统计； - git/branch/list 返回现有本地分支，git/branch/switch 只接受 branch name，并在 host 重新解析为 当前仓库真实分支后执行切换； - git/fetch 执行 all-remotes prune，git/pull 仅允许 fast-forward，git/push 使用 Git 当前 upstream/default 配置；所有 remote operation 都是 non-interactive； - 每个成功 mutation 都返回新的 GitStatusResult，Desktop 立即重绘；首次打开 View 也会自动刷新； - App Server 监听 workspace、Git metadata 和 workspace 上层 repository .gitignore，以 100ms debounce 合并 burst；事件只触发重新查询，不直接成为 Git 状态； - 新 snapshot 按 workspace 投影去重；HEAD/change 未改变时 revision 不推进也不发通知，变化时通过 git/statusChanged 推送完整 snapshot； - SCM View 只在相同 streamInstanceId 内按 revision 拒绝旧 notification/response；连接重新 ready 时主动刷新，并接受新 runtime 从较小 revision 开始的 snapshot。已退役实例的迟到通知 不能覆盖新状态。Watcher 初始化失败不会关闭 Git RPC，用户仍可手动 Refresh。 Native 通过 zeta-app-server-client 消费 git/textDiff，在 Composer 底栏展示 Changes files • +additions -deletions，并从协议中的原始/修改文本重建 presentation-only DiffDocument。文件内容读取、replacement 计数和 binary/size skip 规则仍由 zeta-git 统一拥有； Native 只负责标签、侧栏状态和 MultiDiff presentation。点击 Changes action 会请求刷新 Git projection、展开右栏并选择 Changes Pane。cwd picker 使用 workspacePath 从当前可信工作区还原 repository root 快捷项；选择该项会把工作区切换到 repository root，使 Changes 投影覆盖整个仓库。 Native 的底栏分支按钮复用通用 ContextMenu，候选项来自 git/branch/list，切换通过 git/branch/switch。Git 对脏工作树或 linked worktree 冲突保持权威：失败时不重试、不丢弃改动， 菜单保留并显示失败；成功后使用新的 typed projection 刷新 Files、HEAD、Changes 和 MultiDiff。 zeterm/zeterm 不再依赖 zeta-git。 稳定失败边界为 GitUnavailable、GitNotRepository 和 GitOperationFailed。内部 executable、 stderr、磁盘绝对路径和非 UTF-8 path 不进入 Renderer。 当前限制 - 当前是单 workspace GitRuntime，尚无 multi-repository registry； - operation 由 runtime mutex 串行化，但尚无可观测 queue、progress、caller cancellation 或 retry； - App Server 与 Native 已支持切换现有本地分支；系统仍无 branch 新建/删除/重命名、 tag/worktree mutation 或 credential prompt；zeta code` TUI 只消费 branch/dirty 会话上下文， 当前产品定义不包含 SCM 管理 UI； - pull 固定为 fast-forward only；discard 不删除 untracked 文件； - 当前是单 workspace root contract，不是 multi-root repository collection； - SCM change row 尚未接入 editor diff/open workflow。 这些限制必须在 UI 中保持可见：未实现的 mutation 不注册空命令，也不显示会误导用户的按钮。 分阶段演进 近期扩展顺序： 1. 增加显式 repository identity 与 multi-root registry； 2. 为长时间 remote operation 增加 progress、queue state 和 caller cancellation； 3. 接入 diff/open 与更细粒度的错误 UI； 4. 按明确产品语义增加 branch lifecycle 等额外 mutation，并让已接受该产品需求的 UI consumer 消费同一协议。协议存在不自动使其成为 TUI 功能。 长期不变量是：Desktop、Native 与 TUI 不直接执行 Git；App Server adapter 不复制 Git command/parsing；watch event 只触发重新确认，不能自身成为 repository truth。"
  },
  {
    "slug": "documentation-guidelines",
    "sourcePath": "docs/documentation-guidelines.md",
    "title": "Zeta 文档分层与写作规范",
    "description": "本文是整个仓库文档系统的全局规范，不是权限、沙箱或少数示例文档的局部写法。所有现有文档、 新增文档和文档站页面都必须遵守；发现不一致时，将其视为需要修正的文档缺陷，不能把旧页面 当作例外或先例。",
    "group": "平台与交付",
    "markdown": "> 状态：Current repository convention。\n> 适用范围：crate README、`docs/*.md` 架构/设计文档及两者之间的引用关系。\n\n本文是整个仓库文档系统的全局规范，不是权限、沙箱或少数示例文档的局部写法。所有现有文档、\n新增文档和文档站页面都必须遵守；发现不一致时，将其视为需要修正的文档缺陷，不能把旧页面\n当作例外或先例。\n\n## 快速理解\n\nZeta 文档首先帮助读者建立正确的系统心智模型，然后才提供实现证据。系统文档以用户问题和\n端到端行为为主线，crate README 以安全修改实现为主线；两层文档互相链接，但不互相复制。\n\n| 读者正在做什么 | 应该先看到什么 | 实现细节放在哪里 |\n| --- | --- | --- |\n| 理解某项产品行为 | 常见场景、行为差异和例外 | 对应系统文档的后半部分 |\n| 判断架构边界 | 谁决定、谁执行、谁保存，以及失败后发生什么 | 系统文档的流程和所有权章节 |\n| 修改一个 crate | 公共契约、关键内部符号、测试和修改影响 | crate README |\n| 核对当前完成度 | 已实现、当前限制和计划方向的明确分隔 | 系统文档和实现证据链接 |\n\n所有文档统一采用下面的认知顺序：\n\n```text\n读者的问题\n  → 可观察行为\n  → 一次操作的流程\n  → 决定、执行和保存的责任\n  → 边界、失败与例外\n  → 当前实现和代码证据\n  → 计划演进\n```\n\n| 规范层 | 作用范围 | 统一入口 |\n| --- | --- | --- |\n| 信息结构与语言 | 全部 `docs/*.md` 和 crate README | 本文 |\n| 页面布局、字体、列表、表格和颜色 | 文档站全部页面 | 文档站全局样式 |\n| 标题展示、导航、目录和源码入口 | 文档站全部页面 | 统一页面组件 |\n| 最低机械约束 | 文档站收录的全部 Markdown | `npm run check:docs` |\n\n## 1. 目标\n\n同一个主题通常有两类读者：\n\n- 正在修改一个 crate 的实现维护者；\n- 正在理解或演进跨 crate 系统的架构设计者。\n\n两类读者需要的信息不同。crate README 不应是系统文档的缩略版，`docs/*.md` 也不应成为源码\n逐行说明。每份文档必须有明确所有权，并让读者知道去哪里查另一层信息。\n\n## 2. 两层文档的职责\n\n| 维度 | Crate `README.md` | `docs/<topic>.md` |\n| --- | --- | --- |\n| 核心问题 | “这个 crate 具体如何工作、如何安全修改？” | “这个系统为什么这样设计、各组件如何协作、往哪里演进？” |\n| 读者 | crate 实现者、reviewer、adapter 作者 | 架构维护者、跨团队实现者、产品/安全设计者 |\n| 范围 | 当前 crate 内部与直接 integration contract | 跨 crate、进程、持久化、UI 和用户语义 |\n| 细节 | 文件、类型、调用顺序、校验、错误、常量、测试、修改路径 | ownership、data flow、decision、tradeoff、风险、状态、阶段性演进 |\n| 当前事实 | 必须与代码逐项对应 | 以已验证 capability 和系统 contract 为准 |\n| 未来内容 | crate extension point 与已知限制 | 产品/架构方向、阶段、前置条件和长期不变量 |\n| 避免 | 泛泛价值描述、重复大架构、虚构 future API | 源码清单、private helper 逐项复述、重复 README |\n\n当一个主题同时有两层文档时：\n\n- 两份文档开头互相链接；\n- 明确哪份文档拥有什么 canonical information；\n- 相同内容只在一处详细解释，另一处给出必要摘要与链接；\n- 发生冲突时，先修正 canonical owner，再修正引用方。\n\n## 3. Crate README 必须回答的问题\n\nREADME 应优先帮助下一位修改代码的人。按适用性包含：\n\n1. crate 精确拥有和明确不拥有的职责；\n2. dependency direction 与禁止依赖；\n3. 目录/module 职责；\n4. public API 的调用者、实现者和不变量；\n5. 承载 ownership、validation、binding 和 failure semantics 的关键 private symbol；\n6. public/private interface 之间的真实调用图；\n7. 哪些内部接口变化意味着设计方向或 crate ownership 已经漂移；\n8. 核心调用路径及其顺序为何重要；\n9. input/output 的准确 shape、trust boundary 和 validation；\n10. error、cancellation、retry 与 failure semantics；\n11. host/adapter 接入要求；\n12. Cargo/Bazel 测试入口和 fixture/eval 组织；\n13. 常见修改的影响面与同步更新清单；\n14. 当前限制；\n15. 可能演进及其前置条件。\n\n不是所有 crate 都需要把这些问题拆成独立标题，但不能用“Main API”“Security”几个泛化 bullet\n替代真正的实现信息。\n\nREADME 中允许并鼓励记录容易从 API 表面误判的细节，例如：\n\n- limit 使用 byte 还是 character；\n- cancellation 在哪个 checkpoint 观察；\n- ID 对 raw input 还是 canonical value hash；\n- trait 无法从类型系统强制的 host obligation；\n- schema 是 provider hint 还是本地 authoritative validation；\n- 哪个修改必须 bump revision 或更新 fixture。\n\n### 3.1 内部接口地图\n\nREADME 应直接写出关键内部 symbol 的真实名称，例如 private struct、enum、method、conversion、\nschema builder、hash helper、limit constant 和 orchestration function。目标是让读者能够对照源码\n判断实现是否沿着设计方向发展，而不是让文档停留在无法证伪的架构语言。\n\n内部接口地图至少说明：\n\n- symbol 名称与可见性；\n- 它拥有的单一职责；\n- input/output 或前后调用关系；\n- 它不能承担的职责；\n- 修改该 symbol 需要同步检查的 contract、test 和文档。\n\n应当记录承载设计的 interface，不必罗列 trivial getter、纯格式化 helper 或没有独立 contract 的\n局部函数。判断标准是：该 symbol 如果被删除、绕过或迁移，是否可能改变 ownership、security、\ndurability、validation、binding 或 extension direction。\n\nREADME 还应给出一张文本或 Mermaid 调用图，把 public entry point 连接到关键 private interface。\n调用图必须使用当前源码中的真实名称。对于特别容易发生偏差的边界，应明确写出“出现什么代码\n意味着方向已经漂移”。\n\n## 4. `docs/*.md` 必须回答的问题\n\n系统文档应优先帮助跨边界决策。按适用性包含：\n\n1. 决策摘要；\n2. 要解决的产品/系统问题与 non-goal；\n3. component ownership table；\n4. end-to-end data/control flow；\n5. 用户可见语义；\n6. security、durability、privacy 或 compatibility boundary；\n7. 关键设计取舍及被拒绝的替代方案；\n8. current implementation status；\n9. observability、evaluation 和 rollout strategy；\n10. 近期、中期、潜在演进；\n11. 不随实现替换而改变的长期不变量。\n\n系统文档可以引用准确类型名来固定 contract，但不应复制 private function、文件树或所有错误\nvariant。实现细节链接到 crate README。\n\n### 4.1 先回答用户会遇到什么\n\n解释权限、模式、Tool、配置、交互或其他用户可见系统时，优先使用以下顺序：\n\n1. 用一句话说明系统如何解决问题；\n2. 紧接一张行为表，让读者直接比较常见场景；\n3. 再解释内部类型、执行流程、ownership 和例外。\n\n系统地图直接指向的权威子文档统一先提供“快速理解”章节。该章节紧跟文档所有权和状态说明，\n包含一段不依赖内部名词的摘要，以及一张回答常见问题、场景或行为的表格。后续章节才进入\ncrate、类型、协议字段和计划阶段。文档检查会验证这些权威子文档没有丢失阅读入口。\n\n这不是少数权威文档的特殊版式。全部 `docs/*.md` 都必须把“快速理解”作为第一个二级标题；\n规范、模板和验收手册也要先说明读者如何使用它们。标题前可以有一段简短的所有权、状态和相关\n文档说明，但不能先出现 crate 清单、类型定义、文件树或长篇实现状态。\n\n一级标题优先使用读者能够识别的系统或能力名称，例如“模型调用系统”“配置系统”和“工作区\n搜索”。只有文档确实描述单个 crate 的实现契约时，才把 crate 名称作为一级标题。物理路径、\ncrate 名称和内部层次属于实现索引，不应代替文档主题。\n\n“快速理解”至少包含：\n\n1. 一段不依赖内部类型名的结果摘要；\n2. 一张用常见问题、场景或行为作行的表格；\n3. 指向后续流程、边界、当前状态和实现证据的阅读入口。\n\n如果一次操作包含三个以上阶段，或者存在批准、失败、重试、平台分派等分支，在行为表之后使用\nMermaid 流程图。线性且没有分支的流程可使用编号列表，不为了装饰添加图。\n\n第一张表的列名应直接回答用户问题，例如：\n\n| 对象 | 典型示例 | 什么时候发生 | 用户需要做什么 | 决定有效多久 |\n| --- | --- | --- | --- | --- |\n| 某类动作或模式 | 用户能识别的真实操作 | 默认行为和关键条件 | 无需操作、确认、修改或停止 | 单次、会话、项目或永久 |\n\n具体主题可以删减列，但不能用 `ExecutionDecision`、内部 enum 或 crate 名称作为第一层解释。内部\nmapping 另设“系统内部如何表达”表格。表格之后只补充无法放进单元格的重要例外，避免先写多段\n抽象概念再让读者自行归纳行为。\n\n### 4.2 列表、表格与段落\n\n内容结构必须反映信息之间的真实关系，不能只靠换行制造视觉分组：\n\n- 三项及以上并列的概念、职责、规则、限制或选项使用项目符号列表；\n- 每一项需要“名称 + 解释”时，优先使用“**名称**：解释”的列表项；\n- 有先后顺序、执行步骤或优先级时使用编号列表；\n- 多个对象需要沿相同维度比较时使用表格；\n- 因果关系、设计理由、条件和例外使用连续段落；\n- ownership、状态迁移或跨组件流向难以线性表达时才使用图；\n- 调用路径包含分支、汇合或平台分派时使用 Mermaid 流程图，不用带箭头的代码块模拟流程。\n\n不要把并列概念写成用分号隔开的长段落，也不要用连续 `<br>` 或 Markdown 行尾空格伪造列表。\n如果每一行都能独立回答“它是什么、负责什么或有什么限制”，通常就应该是一个真正的列表项。\n\n### 4.3 中英文术语与代码标识符\n\n中文文档的叙述主体使用中文。英文只用于专有名称、行业通用缩写、无法替代的命令和精确代码\n标识符，不能把英文名词直接嵌进中文语法来代替已经明确的中文概念。\n\n| 内容类型 | 写法 | 示例 |\n| --- | --- | --- |\n| 用户可见概念 | 使用中文 | 权限、批准、拒绝、工作区、网络访问 |\n| 技术概念首次出现 | 中文名称（英文名称） | 执行授权（execution authority） |\n| 同一技术概念后续出现 | 只使用中文 | 执行授权 |\n| 代码类型、函数、枚举值 | 中文解释 + 反引号内的真实标识符 | 策略引擎 `ActionPolicyEngine`、只读模式 `ReadOnly` |\n| crate、命令、文件和配置键 | 保留真实名称并使用反引号 | `zeta-action-policy`、`cargo test`、`policy_revision` |\n| 平台、协议和产品专名 | 保留正式英文名称 | macOS、Linux、Windows、Bubblewrap、Rust、MCP |\n| 行业通用缩写 | 保留大写缩写，必要时首次解释 | API、HTTP、JSON、UI、CLI |\n\n不要机械地为每个英文词补中文括号，也不要在每次出现时重复英文。括号的作用是第一次建立术语\n映射；映射建立后，正文应恢复连续的中文叙述。只有讨论 wire field、public API、枚举分支或源码\n关系时，才再次使用精确标识符。\n\n以下写法不符合规范：\n\n```text\ngrant 绑定 action digest、capabilities 和 policy revision。\n```\n\n推荐写成：\n\n```text\n授权凭证绑定动作摘要（action digest）、完整能力集合（capability set）和策略版本\n（policy revision）。后文只写“动作摘要、能力集合和策略版本”。\n```\n\n用户行为表的标题和单元格优先使用中文。内部实现映射表可以出现 `RunSandboxed`、\n`ApproveOnce`、`ActionPolicyEngine` 等标识符，但必须同时解释其用户或系统含义。\n\n#### 核心术语表\n\n| 推荐中文 | 首次出现时的写法 | 相关代码标识符 |\n| --- | --- | --- |\n| 动作 | 动作（action） | `ResolvedAction` |\n| 动作摘要 | 动作摘要（action digest） | `ActionDigest` |\n| 来源 | 来源（provenance） | `ActionProvenance` |\n| 能力 | 能力（capability） | `Capability`、`CapabilityKind` |\n| 能力集合 | 能力集合（capability set） | `CapabilitySet` |\n| 作用范围 | 作用范围（scope） | capability scope |\n| 策略版本 | 策略版本（policy revision） | `ActionPolicyRevision` |\n| 确定性规则 | 确定性执行规则（deterministic execution policy） | `ExecPolicySnapshot`、`ExecPolicyRule`、`zeta-execpolicy` |\n| 最终动作策略 | 最终 action authority | `ActionPolicyEngine`、`ExecutionDecision`、`zeta-action-policy` |\n| 执行授权 | 执行授权（execution authority） | `ExecutionDecision`、`AutoReviewGrant` |\n| 一次性批准 | 一次性批准 | `ApproveOnce` |\n| 风险审查器 | 风险审查器（reviewer） | `ActionClassifier` |\n| 审查结论 | 审查结论（assessment） | `ClassifierAssessment` |\n| 证据 | 证据（evidence） | `ReviewEvidence` |\n| 信任边界 | 信任边界（trust boundary） | `ReviewEvidenceTrust` |\n| 持久化记录 | 持久化记录（durable record） | `ThreadEvent` |\n| 安全重试 | 可安全重试（safe to retry） | `SafeToRetry` |\n| 未知结果 | 未知执行结果（unknown outcome） | started without terminal result |\n| 失败即关闭 | 失败即关闭（fail closed） | `Block` 或 `AskUser` |\n\n术语表固定的是文档语言，不取代代码 API。代码重命名时同步更新“相关代码标识符”列；中文概念\n只有在产品语义变化时才调整。\n\n## 5. 状态必须显式\n\n未来设计不能伪装成当前实现。使用下列状态语言：\n\n| 状态 | 含义 |\n| --- | --- |\n| Current / 已实现 | 代码和测试中已经存在，可给出证据 |\n| Current limitation / 当前限制 | 已知缺口或有意保持的窄边界 |\n| Extension point / 扩展点 | 当前 contract 已预留，但尚无具体实现 |\n| Proposed / 计划设计 | 已有明确方向，仍可能在实现中调整 |\n| Potential / 潜在方向 | 需要数据、需求或前置能力，不构成承诺 |\n| Non-goal / 长期不做 | 用于防止 ownership 漂移 |\n\n禁止：\n\n- 用将来时段落描述并不存在的 public API，却不标 Proposed；\n- 把测试 fixture 写成 production capability；\n- 把“模型可能做到”写成系统保证；\n- 用 roadmap 掩盖当前 failure semantics；\n- 用虚构 PR、版本或完成度证明状态。\n\n## 6. 信息价值标准\n\n每个 section 至少满足一个目标：\n\n- 固定一个容易破坏的 contract；\n- 解释一个不直观的设计理由；\n- 指出一个可信边界或 failure mode；\n- 给出可执行的修改/验证路径；\n- 区分当前事实与未来选择；\n- 减少跨 crate ownership 误判。\n\n如果一段话删除后不会影响实现、review、debug 或架构决策，它通常信息价值不足。优先删除：\n\n- “provides a robust/flexible solution”一类宣传语；\n- 从 crate 名称即可推断的描述；\n- 与系统文档重复、但更不完整的流程；\n- 没有 owner、前置条件或状态的 future wishlist；\n- 没有说明后果的 API 名称列表。\n\n## 7. 推荐模板\n\n### 7.1 Crate README\n\n```text\n# crate-name\n> 文档所有权与系统文档链接\n\n一句话精确定义\n1. Crate 边界\n2. 文件与职责\n3. Public API / contracts\n4. 内部接口地图与真实调用图\n5. 核心执行路径\n6. Input / output / validation\n7. Error / cancellation / recovery\n8. Integration\n9. Tests / fixtures / eval\n10. 常见修改路径\n11. 当前限制\n12. 可能演进\n```\n\n### 7.2 系统文档\n\n```text\n# Topic：系统边界与演进\n> canonical ownership、状态与 README 链接\n\n快速理解\n1. 用户问题与可观察行为\n2. 一次操作的端到端流程\n3. 决定、执行和保存的责任边界\n4. 明确不负责什么\n5. 安全、可靠性和失败语义\n6. 当前实现与当前限制\n7. 实现证据和修改入口\n8. 近期与潜在演进\n9. 长期不变量\n```\n\n模板不是要求机械填满标题。小 crate 可以合并章节，但必须保留对维护真正有用的信息。\n\n## 8. 图表与代码示例\n\n- 只有关系、状态或顺序难以用短段落表达时才使用图；\n- 有分支或汇合的执行路径使用 `flowchart`，强调时间和参与者交互时使用 `sequenceDiagram`；\n- 静态目录、包含关系或单纯依赖集合可以使用短文本树；一旦读者需要沿箭头追踪执行，就改用流程图；\n- system diagram 表达 ownership/control flow，不罗列 private helper；\n- README 的 sequence 可以精确到 public method 和 validation checkpoint；\n- 示例必须与当前 API 一致，无法编译的伪代码应明确标注；\n- 不用大图重复紧邻的长列表；\n- 表格用于 exact mapping、decision matrix 和责任边界，不用于装饰。\n\n## 9. 更新规则\n\n以下变更通常要求更新 crate README：\n\n- public API、module ownership 或 dependency direction；\n- validation、limit、hash/binding、error 或 cancellation 语义；\n- adapter obligation；\n- test/eval 入口；\n- 已知限制被解决或新增。\n\n以下变更通常要求更新系统文档：\n\n- 用户可见 decision 或 interaction；\n- 跨 crate ownership、durable boundary 或 trust model；\n- policy matrix；\n- rollout/evaluation strategy；\n- 当前状态和演进阶段。\n\n同一变更影响两层时，两层都更新，但分别描述各自拥有的信息，不复制同一实现段落。\n\n## 10. 审查清单\n\n提交文档前确认：\n\n- [ ] 这些规则应用到了同类文档，而不是只修正当前页面；\n- [ ] 开头说明了文档所有权、读者和关联文档；\n- [ ] 当前事实能在代码、schema 或测试中验证；\n- [ ] crate README 包含实现者真正需要的细节；\n- [ ] README 写出了关键 private symbol 的真实名称、职责和调用关系；\n- [ ] README 指出了能够暴露 ownership/方向漂移的内部变化；\n- [ ] system doc 解释了 why、ownership、tradeoff 和 evolution；\n- [ ] 用户可见系统先用一句话和行为表说明常见场景，再进入内部类型与流程；\n- [ ] 并列概念使用列表，同维度比较使用表格，因果和例外使用段落；\n- [ ] 中文正文没有用裸英文名词代替已经定义的中文概念；\n- [ ] 技术概念首次建立中英文映射，后续优先使用中文；\n- [ ] 代码标识符、命令、crate 和配置键使用反引号并保留真实拼写；\n- [ ] Current、Proposed、Potential 没有混写；\n- [ ] 没有复制另一层已经 canonical 的大段内容；\n- [ ] failure、security、privacy、durability 边界没有只写 happy path；\n- [ ] 未来方向写明前置条件，没有伪装成承诺；\n- [ ] 命令、路径、类型名和链接仍然有效；\n- [ ] 删除泛化段落后，剩余内容仍有清晰重点。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-目标",
        "title": "1. 目标"
      },
      {
        "depth": 2,
        "id": "2-两层文档的职责",
        "title": "2. 两层文档的职责"
      },
      {
        "depth": 2,
        "id": "3-crate-readme-必须回答的问题",
        "title": "3. Crate README 必须回答的问题"
      },
      {
        "depth": 3,
        "id": "31-内部接口地图",
        "title": "3.1 内部接口地图"
      },
      {
        "depth": 2,
        "id": "4-docsmd-必须回答的问题",
        "title": "4. docs/.md 必须回答的问题"
      },
      {
        "depth": 3,
        "id": "41-先回答用户会遇到什么",
        "title": "4.1 先回答用户会遇到什么"
      },
      {
        "depth": 3,
        "id": "42-列表表格与段落",
        "title": "4.2 列表、表格与段落"
      },
      {
        "depth": 3,
        "id": "43-中英文术语与代码标识符",
        "title": "4.3 中英文术语与代码标识符"
      },
      {
        "depth": 2,
        "id": "5-状态必须显式",
        "title": "5. 状态必须显式"
      },
      {
        "depth": 2,
        "id": "6-信息价值标准",
        "title": "6. 信息价值标准"
      },
      {
        "depth": 2,
        "id": "7-推荐模板",
        "title": "7. 推荐模板"
      },
      {
        "depth": 3,
        "id": "71-crate-readme",
        "title": "7.1 Crate README"
      },
      {
        "depth": 3,
        "id": "72-系统文档",
        "title": "7.2 系统文档"
      },
      {
        "depth": 2,
        "id": "8-图表与代码示例",
        "title": "8. 图表与代码示例"
      },
      {
        "depth": 2,
        "id": "9-更新规则",
        "title": "9. 更新规则"
      },
      {
        "depth": 2,
        "id": "10-审查清单",
        "title": "10. 审查清单"
      }
    ],
    "searchText": " 状态：Current repository convention。 适用范围：crate README、docs/.md 架构/设计文档及两者之间的引用关系。 本文是整个仓库文档系统的全局规范，不是权限、沙箱或少数示例文档的局部写法。所有现有文档、 新增文档和文档站页面都必须遵守；发现不一致时，将其视为需要修正的文档缺陷，不能把旧页面 当作例外或先例。 快速理解 Zeta 文档首先帮助读者建立正确的系统心智模型，然后才提供实现证据。系统文档以用户问题和 端到端行为为主线，crate README 以安全修改实现为主线；两层文档互相链接，但不互相复制。 读者正在做什么 应该先看到什么 实现细节放在哪里 --- --- --- 理解某项产品行为 常见场景、行为差异和例外 对应系统文档的后半部分 判断架构边界 谁决定、谁执行、谁保存，以及失败后发生什么 系统文档的流程和所有权章节 修改一个 crate 公共契约、关键内部符号、测试和修改影响 crate README 核对当前完成度 已实现、当前限制和计划方向的明确分隔 系统文档和实现证据链接 所有文档统一采用下面的认知顺序： ``text 读者的问题 → 可观察行为 → 一次操作的流程 → 决定、执行和保存的责任 → 边界、失败与例外 → 当前实现和代码证据 → 计划演进 ` 规范层 作用范围 统一入口 --- --- --- 信息结构与语言 全部 docs/.md 和 crate README 本文 页面布局、字体、列表、表格和颜色 文档站全部页面 文档站全局样式 标题展示、导航、目录和源码入口 文档站全部页面 统一页面组件 最低机械约束 文档站收录的全部 Markdown npm run check:docs 1. 目标 同一个主题通常有两类读者： - 正在修改一个 crate 的实现维护者； - 正在理解或演进跨 crate 系统的架构设计者。 两类读者需要的信息不同。crate README 不应是系统文档的缩略版，docs/.md 也不应成为源码 逐行说明。每份文档必须有明确所有权，并让读者知道去哪里查另一层信息。 2. 两层文档的职责 维度 Crate README.md docs/<topic .md --- --- --- 核心问题 “这个 crate 具体如何工作、如何安全修改？” “这个系统为什么这样设计、各组件如何协作、往哪里演进？” 读者 crate 实现者、reviewer、adapter 作者 架构维护者、跨团队实现者、产品/安全设计者 范围 当前 crate 内部与直接 integration contract 跨 crate、进程、持久化、UI 和用户语义 细节 文件、类型、调用顺序、校验、错误、常量、测试、修改路径 ownership、data flow、decision、tradeoff、风险、状态、阶段性演进 当前事实 必须与代码逐项对应 以已验证 capability 和系统 contract 为准 未来内容 crate extension point 与已知限制 产品/架构方向、阶段、前置条件和长期不变量 避免 泛泛价值描述、重复大架构、虚构 future API 源码清单、private helper 逐项复述、重复 README 当一个主题同时有两层文档时： - 两份文档开头互相链接； - 明确哪份文档拥有什么 canonical information； - 相同内容只在一处详细解释，另一处给出必要摘要与链接； - 发生冲突时，先修正 canonical owner，再修正引用方。 3. Crate README 必须回答的问题 README 应优先帮助下一位修改代码的人。按适用性包含： 1. crate 精确拥有和明确不拥有的职责； 2. dependency direction 与禁止依赖； 3. 目录/module 职责； 4. public API 的调用者、实现者和不变量； 5. 承载 ownership、validation、binding 和 failure semantics 的关键 private symbol； 6. public/private interface 之间的真实调用图； 7. 哪些内部接口变化意味着设计方向或 crate ownership 已经漂移； 8. 核心调用路径及其顺序为何重要； 9. input/output 的准确 shape、trust boundary 和 validation； 10. error、cancellation、retry 与 failure semantics； 11. host/adapter 接入要求； 12. Cargo/Bazel 测试入口和 fixture/eval 组织； 13. 常见修改的影响面与同步更新清单； 14. 当前限制； 15. 可能演进及其前置条件。 不是所有 crate 都需要把这些问题拆成独立标题，但不能用“Main API”“Security”几个泛化 bullet 替代真正的实现信息。 README 中允许并鼓励记录容易从 API 表面误判的细节，例如： - limit 使用 byte 还是 character； - cancellation 在哪个 checkpoint 观察； - ID 对 raw input 还是 canonical value hash； - trait 无法从类型系统强制的 host obligation； - schema 是 provider hint 还是本地 authoritative validation； - 哪个修改必须 bump revision 或更新 fixture。 3.1 内部接口地图 README 应直接写出关键内部 symbol 的真实名称，例如 private struct、enum、method、conversion、 schema builder、hash helper、limit constant 和 orchestration function。目标是让读者能够对照源码 判断实现是否沿着设计方向发展，而不是让文档停留在无法证伪的架构语言。 内部接口地图至少说明： - symbol 名称与可见性； - 它拥有的单一职责； - input/output 或前后调用关系； - 它不能承担的职责； - 修改该 symbol 需要同步检查的 contract、test 和文档。 应当记录承载设计的 interface，不必罗列 trivial getter、纯格式化 helper 或没有独立 contract 的 局部函数。判断标准是：该 symbol 如果被删除、绕过或迁移，是否可能改变 ownership、security、 durability、validation、binding 或 extension direction。 README 还应给出一张文本或 Mermaid 调用图，把 public entry point 连接到关键 private interface。 调用图必须使用当前源码中的真实名称。对于特别容易发生偏差的边界，应明确写出“出现什么代码 意味着方向已经漂移”。 4. docs/.md 必须回答的问题 系统文档应优先帮助跨边界决策。按适用性包含： 1. 决策摘要； 2. 要解决的产品/系统问题与 non-goal； 3. component ownership table； 4. end-to-end data/control flow； 5. 用户可见语义； 6. security、durability、privacy 或 compatibility boundary； 7. 关键设计取舍及被拒绝的替代方案； 8. current implementation status； 9. observability、evaluation 和 rollout strategy； 10. 近期、中期、潜在演进； 11. 不随实现替换而改变的长期不变量。 系统文档可以引用准确类型名来固定 contract，但不应复制 private function、文件树或所有错误 variant。实现细节链接到 crate README。 4.1 先回答用户会遇到什么 解释权限、模式、Tool、配置、交互或其他用户可见系统时，优先使用以下顺序： 1. 用一句话说明系统如何解决问题； 2. 紧接一张行为表，让读者直接比较常见场景； 3. 再解释内部类型、执行流程、ownership 和例外。 系统地图直接指向的权威子文档统一先提供“快速理解”章节。该章节紧跟文档所有权和状态说明， 包含一段不依赖内部名词的摘要，以及一张回答常见问题、场景或行为的表格。后续章节才进入 crate、类型、协议字段和计划阶段。文档检查会验证这些权威子文档没有丢失阅读入口。 这不是少数权威文档的特殊版式。全部 docs/.md 都必须把“快速理解”作为第一个二级标题； 规范、模板和验收手册也要先说明读者如何使用它们。标题前可以有一段简短的所有权、状态和相关 文档说明，但不能先出现 crate 清单、类型定义、文件树或长篇实现状态。 一级标题优先使用读者能够识别的系统或能力名称，例如“模型调用系统”“配置系统”和“工作区 搜索”。只有文档确实描述单个 crate 的实现契约时，才把 crate 名称作为一级标题。物理路径、 crate 名称和内部层次属于实现索引，不应代替文档主题。 “快速理解”至少包含： 1. 一段不依赖内部类型名的结果摘要； 2. 一张用常见问题、场景或行为作行的表格； 3. 指向后续流程、边界、当前状态和实现证据的阅读入口。 如果一次操作包含三个以上阶段，或者存在批准、失败、重试、平台分派等分支，在行为表之后使用 Mermaid 流程图。线性且没有分支的流程可使用编号列表，不为了装饰添加图。 第一张表的列名应直接回答用户问题，例如： 对象 典型示例 什么时候发生 用户需要做什么 决定有效多久 --- --- --- --- --- 某类动作或模式 用户能识别的真实操作 默认行为和关键条件 无需操作、确认、修改或停止 单次、会话、项目或永久 具体主题可以删减列，但不能用 ExecutionDecision、内部 enum 或 crate 名称作为第一层解释。内部 mapping 另设“系统内部如何表达”表格。表格之后只补充无法放进单元格的重要例外，避免先写多段 抽象概念再让读者自行归纳行为。 4.2 列表、表格与段落 内容结构必须反映信息之间的真实关系，不能只靠换行制造视觉分组： - 三项及以上并列的概念、职责、规则、限制或选项使用项目符号列表； - 每一项需要“名称 + 解释”时，优先使用“名称：解释”的列表项； - 有先后顺序、执行步骤或优先级时使用编号列表； - 多个对象需要沿相同维度比较时使用表格； - 因果关系、设计理由、条件和例外使用连续段落； - ownership、状态迁移或跨组件流向难以线性表达时才使用图； - 调用路径包含分支、汇合或平台分派时使用 Mermaid 流程图，不用带箭头的代码块模拟流程。 不要把并列概念写成用分号隔开的长段落，也不要用连续 <br 或 Markdown 行尾空格伪造列表。 如果每一行都能独立回答“它是什么、负责什么或有什么限制”，通常就应该是一个真正的列表项。 4.3 中英文术语与代码标识符 中文文档的叙述主体使用中文。英文只用于专有名称、行业通用缩写、无法替代的命令和精确代码 标识符，不能把英文名词直接嵌进中文语法来代替已经明确的中文概念。 内容类型 写法 示例 --- --- --- 用户可见概念 使用中文 权限、批准、拒绝、工作区、网络访问 技术概念首次出现 中文名称（英文名称） 执行授权（execution authority） 同一技术概念后续出现 只使用中文 执行授权 代码类型、函数、枚举值 中文解释 + 反引号内的真实标识符 策略引擎 ActionPolicyEngine、只读模式 ReadOnly crate、命令、文件和配置键 保留真实名称并使用反引号 zeta-action-policy、cargo test、policyrevision 平台、协议和产品专名 保留正式英文名称 macOS、Linux、Windows、Bubblewrap、Rust、MCP 行业通用缩写 保留大写缩写，必要时首次解释 API、HTTP、JSON、UI、CLI 不要机械地为每个英文词补中文括号，也不要在每次出现时重复英文。括号的作用是第一次建立术语 映射；映射建立后，正文应恢复连续的中文叙述。只有讨论 wire field、public API、枚举分支或源码 关系时，才再次使用精确标识符。 以下写法不符合规范： `text grant 绑定 action digest、capabilities 和 policy revision。 ` 推荐写成： `text 授权凭证绑定动作摘要（action digest）、完整能力集合（capability set）和策略版本 （policy revision）。后文只写“动作摘要、能力集合和策略版本”。 ` 用户行为表的标题和单元格优先使用中文。内部实现映射表可以出现 RunSandboxed、 ApproveOnce、ActionPolicyEngine 等标识符，但必须同时解释其用户或系统含义。 核心术语表 推荐中文 首次出现时的写法 相关代码标识符 --- --- --- 动作 动作（action） ResolvedAction 动作摘要 动作摘要（action digest） ActionDigest 来源 来源（provenance） ActionProvenance 能力 能力（capability） Capability、CapabilityKind 能力集合 能力集合（capability set） CapabilitySet 作用范围 作用范围（scope） capability scope 策略版本 策略版本（policy revision） ActionPolicyRevision 确定性规则 确定性执行规则（deterministic execution policy） ExecPolicySnapshot、ExecPolicyRule、zeta-execpolicy 最终动作策略 最终 action authority ActionPolicyEngine、ExecutionDecision、zeta-action-policy 执行授权 执行授权（execution authority） ExecutionDecision、AutoReviewGrant 一次性批准 一次性批准 ApproveOnce 风险审查器 风险审查器（reviewer） ActionClassifier 审查结论 审查结论（assessment） ClassifierAssessment 证据 证据（evidence） ReviewEvidence 信任边界 信任边界（trust boundary） ReviewEvidenceTrust 持久化记录 持久化记录（durable record） ThreadEvent 安全重试 可安全重试（safe to retry） SafeToRetry 未知结果 未知执行结果（unknown outcome） started without terminal result 失败即关闭 失败即关闭（fail closed） Block 或 AskUser 术语表固定的是文档语言，不取代代码 API。代码重命名时同步更新“相关代码标识符”列；中文概念 只有在产品语义变化时才调整。 5. 状态必须显式 未来设计不能伪装成当前实现。使用下列状态语言： 状态 含义 --- --- Current / 已实现 代码和测试中已经存在，可给出证据 Current limitation / 当前限制 已知缺口或有意保持的窄边界 Extension point / 扩展点 当前 contract 已预留，但尚无具体实现 Proposed / 计划设计 已有明确方向，仍可能在实现中调整 Potential / 潜在方向 需要数据、需求或前置能力，不构成承诺 Non-goal / 长期不做 用于防止 ownership 漂移 禁止： - 用将来时段落描述并不存在的 public API，却不标 Proposed； - 把测试 fixture 写成 production capability； - 把“模型可能做到”写成系统保证； - 用 roadmap 掩盖当前 failure semantics； - 用虚构 PR、版本或完成度证明状态。 6. 信息价值标准 每个 section 至少满足一个目标： - 固定一个容易破坏的 contract； - 解释一个不直观的设计理由； - 指出一个可信边界或 failure mode； - 给出可执行的修改/验证路径； - 区分当前事实与未来选择； - 减少跨 crate ownership 误判。 如果一段话删除后不会影响实现、review、debug 或架构决策，它通常信息价值不足。优先删除： - “provides a robust/flexible solution”一类宣传语； - 从 crate 名称即可推断的描述； - 与系统文档重复、但更不完整的流程； - 没有 owner、前置条件或状态的 future wishlist； - 没有说明后果的 API 名称列表。 7. 推荐模板 7.1 Crate README `text crate-name 文档所有权与系统文档链接 一句话精确定义 1. Crate 边界 2. 文件与职责 3. Public API / contracts 4. 内部接口地图与真实调用图 5. 核心执行路径 6. Input / output / validation 7. Error / cancellation / recovery 8. Integration 9. Tests / fixtures / eval 10. 常见修改路径 11. 当前限制 12. 可能演进 ` 7.2 系统文档 `text Topic：系统边界与演进 canonical ownership、状态与 README 链接 快速理解 1. 用户问题与可观察行为 2. 一次操作的端到端流程 3. 决定、执行和保存的责任边界 4. 明确不负责什么 5. 安全、可靠性和失败语义 6. 当前实现与当前限制 7. 实现证据和修改入口 8. 近期与潜在演进 9. 长期不变量 ` 模板不是要求机械填满标题。小 crate 可以合并章节，但必须保留对维护真正有用的信息。 8. 图表与代码示例 - 只有关系、状态或顺序难以用短段落表达时才使用图； - 有分支或汇合的执行路径使用 flowchart，强调时间和参与者交互时使用 sequenceDiagram`； - 静态目录、包含关系或单纯依赖集合可以使用短文本树；一旦读者需要沿箭头追踪执行，就改用流程图； - system diagram 表达 ownership/control flow，不罗列 private helper； - README 的 sequence 可以精确到 public method 和 validation checkpoint； - 示例必须与当前 API 一致，无法编译的伪代码应明确标注； - 不用大图重复紧邻的长列表； - 表格用于 exact mapping、decision matrix 和责任边界，不用于装饰。 9. 更新规则 以下变更通常要求更新 crate README： - public API、module ownership 或 dependency direction； - validation、limit、hash/binding、error 或 cancellation 语义； - adapter obligation； - test/eval 入口； - 已知限制被解决或新增。 以下变更通常要求更新系统文档： - 用户可见 decision 或 interaction； - 跨 crate ownership、durable boundary 或 trust model； - policy matrix； - rollout/evaluation strategy； - 当前状态和演进阶段。 同一变更影响两层时，两层都更新，但分别描述各自拥有的信息，不复制同一实现段落。 10. 审查清单 提交文档前确认： - 这些规则应用到了同类文档，而不是只修正当前页面； - 开头说明了文档所有权、读者和关联文档； - 当前事实能在代码、schema 或测试中验证； - crate README 包含实现者真正需要的细节； - README 写出了关键 private symbol 的真实名称、职责和调用关系； - README 指出了能够暴露 ownership/方向漂移的内部变化； - system doc 解释了 why、ownership、tradeoff 和 evolution； - 用户可见系统先用一句话和行为表说明常见场景，再进入内部类型与流程； - 并列概念使用列表，同维度比较使用表格，因果和例外使用段落； - 中文正文没有用裸英文名词代替已经定义的中文概念； - 技术概念首次建立中英文映射，后续优先使用中文； - 代码标识符、命令、crate 和配置键使用反引号并保留真实拼写； - Current、Proposed、Potential 没有混写； - 没有复制另一层已经 canonical 的大段内容； - failure、security、privacy、durability 边界没有只写 happy path； - 未来方向写明前置条件，没有伪装成承诺； - 命令、路径、类型名和链接仍然有效； - 删除泛化段落后，剩余内容仍有清晰重点。"
  },
  {
    "slug": "zeta-code-architecture-codex-style-v2",
    "sourcePath": "docs/zeta-code-architecture-codex-style-v2.md",
    "title": "Zeta 共享长期架构",
    "description": "长期架构固定少数不会随实现替换而改变的边界：Session 管任务拓扑，Thread 管执行顺序， 共享协议只定义语义，Core 协调状态，存储只实现一套事件流。",
    "group": "计划与迁移",
    "markdown": "> 日期：2026-07-25\n> 状态：开发期目标架构\n> 原则：按长期领域边界直接演进，不保留开发期旧 API 或旧持久化格式的兼容层。\n> 产品线映射：`zeta code` 是 TUI，`zeta` 是 Electron Desktop，`zeterm` 是纯 Rust Desktop；\n> 三者共享 Session-first Agent 产品契约。`zeterm` 的终端/PTY 宿主可以直接组合终端运行时，但\n> 不得为 Agent 的 Session/Thread/Turn/Item 能力绕过 App Server。\n\n## 快速理解\n\n长期架构固定少数不会随实现替换而改变的边界：Session 管任务拓扑，Thread 管执行顺序，\n共享协议只定义语义，Core 协调状态，存储只实现一套事件流。\n\n| 长期问题 | 固定答案 | 主要边界 |\n| --- | --- | --- |\n| 产品根对象是什么？ | Session 是任务和 Thread 拓扑聚合，Thread 是独立执行聚合 | [聚合并发边界](#3-聚合并发边界) |\n| 谁拥有状态迁移？ | Core 中的纯 reducer 与协调器 | [Session 协调器](#5-session-协调器)、[Thread 控制器](#6-thread-控制器) |\n| 谁拥有持久化格式？ | 一个共享事件流引擎，Session/Thread 只保留类型化适配器 | [唯一物理事件流](#4-唯一物理-event-stream-引擎) |\n| 客户端能否拥有另一套状态？ | 不能；Desktop、CLI、TUI 只消费统一 App Server API | [App Server](#8-app-server) |\n| 外部请求从哪里进入？ | App Server 是 Session/Thread/Turn/Item 的唯一外部进入/输出门禁 | [App Server](#8-app-server) 与 [App Server API](zeta-app-server-api.md) |\n| 开发期旧接口如何处理？ | 直接迁移权威契约和调用方，不建立隐藏兼容层 | 本文固定原则 |\n\n## 1. 结论\n\nCanonical 产品模型、契约分类与 sequence/ID 语义统一由\n[`protocol.md`](protocol.md) 定义。系统架构只依赖一个关键结论：Session 是任务与 Thread\n拓扑 aggregate，Thread 是执行 aggregate；二者有父子领域关系，但不是同一个并发与顺序边界。\n\n```mermaid\nflowchart LR\n    Client[\"Desktop / CLI / TUI / Agent host\"] --> API[\"App Server Protocol\"]\n    API --> Server[\"App Server\"]\n    Server --> SR[\"Session Coordinator\"]\n    SR --> TR[\"Thread Controller\"]\n    SR --> SS[\"SessionStore\"]\n    TR --> TS[\"ThreadStore\"]\n    SS --> ES[\"Shared Event-Stream Engine\"]\n    TS --> ES\n```\n\n长期必须同时满足：\n\n- Core 不依赖 App Server wire 类型；\n- protocol 只包含跨组件共享的领域契约；\n- Session 与 Thread reducer 都是纯函数；\n- Session 与 Thread 各自拥有逻辑 sequence；\n- storage 的物理 framing、checksum、atomic append 和断尾恢复只有一套实现；\n- App Server 是产品能力的唯一外部门禁，只做传输、路由、订阅和 DTO 编解码，\n  不复制 Core reducer 或 Store authority；\n- Desktop、CLI、TUI 与任何 Agent host 消费同一个 Session-first 产品 API；进程内嵌也必须经过\n  同一个 dispatcher。\n\n## 2. Crate 边界\n\n```text\nzeta-protocol\n  └─ canonical shared semantic contract（详见 protocol.md）\n\nzeta-session-store\n  ├─ StoredSessionEvent\n  ├─ SessionCommandReceipt\n  └─ SessionStore\n\nzeta-history\n  ├─ StoredEvent\n  ├─ ThreadCommandReceipt\n  └─ persisted Thread record schema/version\n\nzeta-thread-store\n  ├─ ThreadEventBatch\n  └─ ThreadStore + append validation / full recovery\n\nzeta-core\n  ├─ SessionCoordinator + Session reducer\n  ├─ ThreadController + Thread reducer\n  └─ ports / lifecycle policy / recovery\n\nzeta-storage\n  ├─ shared event-stream engine\n  ├─ SessionStore typed adapter\n  ├─ ThreadStore typed adapter\n  ├─ writer lease\n  └─ rebuildable query projection\n\nzeta-rollout\n  ├─ local Session + Thread rollout repository\n  └─ durable recovery composition (Thread before Session)\n\nzeta-rollout-trace\n  └─ read-only, serializable Session rollout trace for export / diagnostics / evaluation\n\nzeta-app-server-protocol\n  ├─ JSON-RPC methods and envelopes\n  ├─ stable external errors\n  └─ JSON Schema / TypeScript generation\n\nzeta-app-server\n  ├─ connection lifecycle\n  ├─ dispatcher and serialization scopes\n  ├─ subscription/update delivery\n  └─ local adapter composition\n```\n\n依赖方向：\n\n```text\nprotocol\n  ↑\n  ├─ session-store\n  ├─ history\n  ├─ thread-store\n  ├─ core\n  └─ app-server-protocol\n\nhistory\n  ↑\n  ├─ thread-store\n  ├─ core\n  └─ rollout-trace\n\nhistory + session-store + thread-store + core\n  ↑\nstorage\n\ncore + rollout + app-server-protocol\n  ↑\napp-server\n\ncore + storage + history + session-store + thread-store\n  ↑\nrollout\n\nhistory + session-store + thread-store\n  ↑\nrollout-trace\n```\n\n禁止 `core → app-server-protocol`、`protocol → tokio/fs/database/JSON-RPC` 或\n`storage → app-server`。\n\n## 3. 聚合并发边界\n\nSession/Thread sequence 的定义与必须独立的理由见\n[`protocol.md` 的 Sequence、Cursor 与 ID](protocol.md#5-sequencecursor-与-id)。对系统实现\n的直接约束是：两种 aggregate 使用独立 writer/serialization scope，但共享同一个物理\nevent-stream engine。\n\n## 4. 唯一物理 event-stream 引擎\n\n`zeta-storage::event_stream` 独占以下职责：\n\n- JSONL batch framing；\n- format version 与 stream kind discriminator；\n- batch checksum；\n- atomic append + `sync_data`；\n- 未终止尾记录恢复；\n- typed payload serde。\n\nSession 与 Thread storage adapter 只负责：\n\n- ID 到 stream path 的映射；\n- 调用各自 store crate 的 batch validator；\n- typed error 映射；\n- list/load 接口。\n\n不再提供第二种单文件 `RolloutLog` API，也不读取旧 kind/payload、旧 schema 或隐式 Session\n数据。当前开发数据不符合新格式时明确失败或由开发者清空，不在领域代码中加入 upcast。\n\n`zeta-history` 只定义 persisted Thread record 及其版本兼容区间；它没有 Store、文件或数据库\nAPI。`zeta-rollout` 是这套权威历史的本地组合层：它同时打开 typed SessionStore、ThreadStore 与\nwriter lease，并且保证恢复顺序为 Thread 在前、Session 在后。它不复制 event framing 或 reducer。\n`zeta-rollout-trace` 只依赖两个 store port，将某个 Session 的 topology stream 和其计划的各\nThread stream 导出为只读 artifact。trace 保留每个 aggregate 自己的 sequence，绝不发明全局\nsequence，也不参与任何运行时写入或状态决策。\n\n## 5. Session 协调器\n\nSessionCoordinator 拥有：\n\n- canonical Session projection；\n- Session command receipt；\n- membership 与 lineage reducer；\n- create/fork/archive/complete lifecycle；\n- Session writer lease；\n- recovery。\n\n创建与 fork Thread 跨越 Session/Thread 两个 aggregate，使用可恢复 saga：\n\n```text\nSession: ThreadCreationPlanned(creating)\n    → Thread: ThreadCreated\n    → Session: ThreadAttached(active)\n```\n\n每一步可幂等重放。恢复发现 `creating` membership 时继续完成已有计划，不能生成新 ID。\nFork plan 同时保存 `parentThreadId` 与当时的 `parentSequence`。\n\nSession 不代理子 Thread 的执行，也不在每个 Thread event 上递增 Session sequence。\n\n## 6. Thread 控制器\n\nThread 是独立执行与恢复 aggregate：\n\n- `ThreadCreated` 固定不可变 `sessionId`；\n- `session/request` StartTurn acceptance、user items 与 started facts 原子提交；\n- final agent item 与 completed fact 原子提交；\n- tool call 与 tool result 都是 durable ThreadItem；\n- interrupt 与 failure 通过 typed event 收敛；\n- live write 与 recovery 共用同一个 reducer。\n\nThread snapshot 是 rollout 的可重建投影，不是第二份权威状态。\n\n## 7. 类型化命令回执\n\nCommand identity、冲突和重放语义统一见\n[`protocol.md` 的 Command 契约](protocol.md#41-command请求改变状态)。Store 的系统级职责\n仅是把 typed receipt 与首个业务 event 原子提交，并让 reducer recovery 恢复稳定结果；wire\nmethod 与 serialized params 不得成为 Core 的幂等身份。\n\n## 8. App Server\n\nApp Server 是 `Session → Thread → Turn → ThreadItem` 产品能力的唯一外部进入/输出门禁。\n完整的参与者、允许路径和禁止旁路见 [Zeta App Server API](zeta-app-server-api.md#唯一外部门禁)；\n本节只保留跨组件 ownership 和长期不变量。\n\n它暴露：\n\n- Session create/read/list/subscribe/lifecycle；\n- Session-owned Thread create/fork/archive；\n- Thread read/subscribe；\n- Turn start/interrupt；\n- Config 与 Resource；\n- `session/update` / `session/thread/update`。\n\nApp Server connection state 只保存 request ID、subscription cursor、resource ownership 与\ntransport state；它不是产品 Session，也不能成为 Core 领域状态的第二份 authority。\n\n订阅使用 snapshot + durable gap：\n\n```text\nsubscribe(afterSequence)\n  → capture current snapshot\n  → return committed updates after cursor\n  → continue live updates\n```\n\ndurable sequence 用于恢复与并发控制；`StreamCursor` 只用于同一 stream incarnation 中可能丢失的瞬态\ndelta。客户端不得把二者压成一个 sequence。\n\n## 9. Desktop、CLI、TUI 与 Native host\n\n所有需要 Agent 产品能力的入口都先创建或读取 Session，再在 Session 内创建/选择 Thread。任何\n入口都不得：\n\n- 直接创建无 Session 的 Thread；\n- 从 notification 文本推断权威状态；\n- 用 JSON-RPC request ID 代替 CommandId；\n- 在客户端维护第二份领域状态机；\n- 依赖旧 `thread/start`、`thread/resume` 或 `thread/list`。\n\n`zeterm` 当前直接组合终端/PTY 运行时的路径只服务终端宿主，不是 Agent 产品接口；如果未来\nNative host 暴露 Session/Thread/Turn/Item，则必须使用同一 App Server protocol 和 dispatcher。\n\nDesktop preload 只暴露自包含的 `ISandboxGlobals`，其中没有 Node API、Electron event 或不受约束\n的频道；`createElectronRendererApi()` 是它的唯一产品适配器，为 Workbench 提供强类型 capability。\nElectron Main 持有 JSON-RPC connection 和 trusted IPC validation，Workbench 不接触\nNode/Electron primitive 或底层 sandbox bridge。\n\n当前嵌入式浏览器同样遵守该边界：Electron Main 的 `BrowserViewMainService` 独占\n`WebContentsView` 和临时 browser session，Workbench 只通过\n`ZetaElectronRendererApi.browserView` 交换可序列化命令与状态。浏览器编辑器 UI 与\nRust/Agent browser capability 仍是后续工作，当前实现状态和安全策略以\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#7-browser-capability) 为准。\n\nRenderer 内受控 HTML 使用独立的 `WebviewElement` iframe boundary，而不是\n`WebContentsView` 或 Electron `<webview>` 标签。当前 iframe 采用 opaque origin、固定 CSP\n和 source/channel message validation；扩展宿主与独立 webview origin 尚未实现，精确状态见\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#61-iframe-webview)。\n\nMarkdown 也不以解析器输出作为信任边界。当前 Workbench 短内容使用\n`marked → DOMPurify → MarkdownElement`，完整预览使用\n`markdown-it → DOMPurify → MarkdownPreview → WebviewElement`。两条路径共享严格的标签、\n属性和 URL allowlist。DOMPurify 的直接适配只位于 `base/browser/domSanitize.ts`；\n`workbench/contrib/markdown` 通过 `MarkdownDocumentView` 接入平台预览并拥有产品链接策略，\n不复制安全边界。语法高亮、扩展插件及工作区资源映射仍未实现。具体所有权与当前限制见\n[`zeta-desktop-architecture.md`](zeta-desktop-architecture.md#62-markdown)。\n\n## 10. 演进规则\n\n当前处于开发阶段：\n\n- 领域语义错误时直接修正 canonical 类型和调用方；\n- wire、schema、fixture、Desktop 和 CLI 必须在同一变更内更新；\n- stored-event schema 不兼容时提升/重置当前 schema 并明确失败；\n- 不保留 deprecated alias、旧 route、隐式转换或双写；\n- 真正发布后再引入明确的版本政策与离线 migration 工具。\n\n## 11. 验证门\n\n```bash\ncargo fmt --check\ncargo clippy --workspace --all-targets -- -D warnings\ncargo test --workspace\ncorepack pnpm --dir desktop test:main\ncorepack pnpm --dir desktop typecheck:renderer\n```\n\n协议变更还必须重新生成 JSON Schema/TypeScript，并由 fixture test 校验生成结果逐字一致。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 2,
        "id": "2-crate-边界",
        "title": "2. Crate 边界"
      },
      {
        "depth": 2,
        "id": "3-聚合并发边界",
        "title": "3. 聚合并发边界"
      },
      {
        "depth": 2,
        "id": "4-唯一物理-event-stream-引擎",
        "title": "4. 唯一物理 event-stream 引擎"
      },
      {
        "depth": 2,
        "id": "5-session-协调器",
        "title": "5. Session 协调器"
      },
      {
        "depth": 2,
        "id": "6-thread-控制器",
        "title": "6. Thread 控制器"
      },
      {
        "depth": 2,
        "id": "7-类型化命令回执",
        "title": "7. 类型化命令回执"
      },
      {
        "depth": 2,
        "id": "8-app-server",
        "title": "8. App Server"
      },
      {
        "depth": 2,
        "id": "9-desktopclitui-与-native-host",
        "title": "9. Desktop、CLI、TUI 与 Native host"
      },
      {
        "depth": 2,
        "id": "10-演进规则",
        "title": "10. 演进规则"
      },
      {
        "depth": 2,
        "id": "11-验证门",
        "title": "11. 验证门"
      }
    ],
    "searchText": " 日期：2026-07-25 状态：开发期目标架构 原则：按长期领域边界直接演进，不保留开发期旧 API 或旧持久化格式的兼容层。 产品线映射：zeta code 是 TUI，zeta 是 Electron Desktop，zeterm 是纯 Rust Desktop； 三者共享 Session-first Agent 产品契约。zeterm 的终端/PTY 宿主可以直接组合终端运行时，但 不得为 Agent 的 Session/Thread/Turn/Item 能力绕过 App Server。 快速理解 长期架构固定少数不会随实现替换而改变的边界：Session 管任务拓扑，Thread 管执行顺序， 共享协议只定义语义，Core 协调状态，存储只实现一套事件流。 长期问题 固定答案 主要边界 --- --- --- 产品根对象是什么？ Session 是任务和 Thread 拓扑聚合，Thread 是独立执行聚合 聚合并发边界 谁拥有状态迁移？ Core 中的纯 reducer 与协调器 Session 协调器、Thread 控制器 谁拥有持久化格式？ 一个共享事件流引擎，Session/Thread 只保留类型化适配器 唯一物理事件流 客户端能否拥有另一套状态？ 不能；Desktop、CLI、TUI 只消费统一 App Server API App Server 外部请求从哪里进入？ App Server 是 Session/Thread/Turn/Item 的唯一外部进入/输出门禁 App Server 与 App Server API 开发期旧接口如何处理？ 直接迁移权威契约和调用方，不建立隐藏兼容层 本文固定原则 1. 结论 Canonical 产品模型、契约分类与 sequence/ID 语义统一由 protocol.md 定义。系统架构只依赖一个关键结论：Session 是任务与 Thread 拓扑 aggregate，Thread 是执行 aggregate；二者有父子领域关系，但不是同一个并发与顺序边界。 ``mermaid flowchart LR Client \"Desktop / CLI / TUI / Agent host\" -- API \"App Server Protocol\" API -- Server \"App Server\" Server -- SR \"Session Coordinator\" SR -- TR \"Thread Controller\" SR -- SS \"SessionStore\" TR -- TS \"ThreadStore\" SS -- ES \"Shared Event-Stream Engine\" TS -- ES ` 长期必须同时满足： - Core 不依赖 App Server wire 类型； - protocol 只包含跨组件共享的领域契约； - Session 与 Thread reducer 都是纯函数； - Session 与 Thread 各自拥有逻辑 sequence； - storage 的物理 framing、checksum、atomic append 和断尾恢复只有一套实现； - App Server 是产品能力的唯一外部门禁，只做传输、路由、订阅和 DTO 编解码， 不复制 Core reducer 或 Store authority； - Desktop、CLI、TUI 与任何 Agent host 消费同一个 Session-first 产品 API；进程内嵌也必须经过 同一个 dispatcher。 2. Crate 边界 `text zeta-protocol └─ canonical shared semantic contract（详见 protocol.md） zeta-session-store ├─ StoredSessionEvent ├─ SessionCommandReceipt └─ SessionStore zeta-history ├─ StoredEvent ├─ ThreadCommandReceipt └─ persisted Thread record schema/version zeta-thread-store ├─ ThreadEventBatch └─ ThreadStore + append validation / full recovery zeta-core ├─ SessionCoordinator + Session reducer ├─ ThreadController + Thread reducer └─ ports / lifecycle policy / recovery zeta-storage ├─ shared event-stream engine ├─ SessionStore typed adapter ├─ ThreadStore typed adapter ├─ writer lease └─ rebuildable query projection zeta-rollout ├─ local Session + Thread rollout repository └─ durable recovery composition Thread before Session zeta-rollout-trace └─ read-only, serializable Session rollout trace for export / diagnostics / evaluation zeta-app-server-protocol ├─ JSON-RPC methods and envelopes ├─ stable external errors └─ JSON Schema / TypeScript generation zeta-app-server ├─ connection lifecycle ├─ dispatcher and serialization scopes ├─ subscription/update delivery └─ local adapter composition ` 依赖方向： `text protocol ↑ ├─ session-store ├─ history ├─ thread-store ├─ core └─ app-server-protocol history ↑ ├─ thread-store ├─ core └─ rollout-trace history + session-store + thread-store + core ↑ storage core + rollout + app-server-protocol ↑ app-server core + storage + history + session-store + thread-store ↑ rollout history + session-store + thread-store ↑ rollout-trace ` 禁止 core → app-server-protocol、protocol → tokio/fs/database/JSON-RPC 或 storage → app-server。 3. 聚合并发边界 Session/Thread sequence 的定义与必须独立的理由见 protocol.md 的 Sequence、Cursor 与 ID。对系统实现 的直接约束是：两种 aggregate 使用独立 writer/serialization scope，但共享同一个物理 event-stream engine。 4. 唯一物理 event-stream 引擎 zeta-storage::eventstream 独占以下职责： - JSONL batch framing； - format version 与 stream kind discriminator； - batch checksum； - atomic append + syncdata； - 未终止尾记录恢复； - typed payload serde。 Session 与 Thread storage adapter 只负责： - ID 到 stream path 的映射； - 调用各自 store crate 的 batch validator； - typed error 映射； - list/load 接口。 不再提供第二种单文件 RolloutLog API，也不读取旧 kind/payload、旧 schema 或隐式 Session 数据。当前开发数据不符合新格式时明确失败或由开发者清空，不在领域代码中加入 upcast。 zeta-history 只定义 persisted Thread record 及其版本兼容区间；它没有 Store、文件或数据库 API。zeta-rollout 是这套权威历史的本地组合层：它同时打开 typed SessionStore、ThreadStore 与 writer lease，并且保证恢复顺序为 Thread 在前、Session 在后。它不复制 event framing 或 reducer。 zeta-rollout-trace 只依赖两个 store port，将某个 Session 的 topology stream 和其计划的各 Thread stream 导出为只读 artifact。trace 保留每个 aggregate 自己的 sequence，绝不发明全局 sequence，也不参与任何运行时写入或状态决策。 5. Session 协调器 SessionCoordinator 拥有： - canonical Session projection； - Session command receipt； - membership 与 lineage reducer； - create/fork/archive/complete lifecycle； - Session writer lease； - recovery。 创建与 fork Thread 跨越 Session/Thread 两个 aggregate，使用可恢复 saga： `text Session: ThreadCreationPlanned creating → Thread: ThreadCreated → Session: ThreadAttached active ` 每一步可幂等重放。恢复发现 creating membership 时继续完成已有计划，不能生成新 ID。 Fork plan 同时保存 parentThreadId 与当时的 parentSequence。 Session 不代理子 Thread 的执行，也不在每个 Thread event 上递增 Session sequence。 6. Thread 控制器 Thread 是独立执行与恢复 aggregate： - ThreadCreated 固定不可变 sessionId； - session/request StartTurn acceptance、user items 与 started facts 原子提交； - final agent item 与 completed fact 原子提交； - tool call 与 tool result 都是 durable ThreadItem； - interrupt 与 failure 通过 typed event 收敛； - live write 与 recovery 共用同一个 reducer。 Thread snapshot 是 rollout 的可重建投影，不是第二份权威状态。 7. 类型化命令回执 Command identity、冲突和重放语义统一见 protocol.md 的 Command 契约。Store 的系统级职责 仅是把 typed receipt 与首个业务 event 原子提交，并让 reducer recovery 恢复稳定结果；wire method 与 serialized params 不得成为 Core 的幂等身份。 8. App Server App Server 是 Session → Thread → Turn → ThreadItem 产品能力的唯一外部进入/输出门禁。 完整的参与者、允许路径和禁止旁路见 Zeta App Server API； 本节只保留跨组件 ownership 和长期不变量。 它暴露： - Session create/read/list/subscribe/lifecycle； - Session-owned Thread create/fork/archive； - Thread read/subscribe； - Turn start/interrupt； - Config 与 Resource； - session/update / session/thread/update。 App Server connection state 只保存 request ID、subscription cursor、resource ownership 与 transport state；它不是产品 Session，也不能成为 Core 领域状态的第二份 authority。 订阅使用 snapshot + durable gap： `text subscribe afterSequence → capture current snapshot → return committed updates after cursor → continue live updates ` durable sequence 用于恢复与并发控制；StreamCursor 只用于同一 stream incarnation 中可能丢失的瞬态 delta。客户端不得把二者压成一个 sequence。 9. Desktop、CLI、TUI 与 Native host 所有需要 Agent 产品能力的入口都先创建或读取 Session，再在 Session 内创建/选择 Thread。任何 入口都不得： - 直接创建无 Session 的 Thread； - 从 notification 文本推断权威状态； - 用 JSON-RPC request ID 代替 CommandId； - 在客户端维护第二份领域状态机； - 依赖旧 thread/start、thread/resume 或 thread/list。 zeterm 当前直接组合终端/PTY 运行时的路径只服务终端宿主，不是 Agent 产品接口；如果未来 Native host 暴露 Session/Thread/Turn/Item，则必须使用同一 App Server protocol 和 dispatcher。 Desktop preload 只暴露自包含的 ISandboxGlobals，其中没有 Node API、Electron event 或不受约束 的频道；createElectronRendererApi 是它的唯一产品适配器，为 Workbench 提供强类型 capability。 Electron Main 持有 JSON-RPC connection 和 trusted IPC validation，Workbench 不接触 Node/Electron primitive 或底层 sandbox bridge。 当前嵌入式浏览器同样遵守该边界：Electron Main 的 BrowserViewMainService 独占 WebContentsView 和临时 browser session，Workbench 只通过 ZetaElectronRendererApi.browserView 交换可序列化命令与状态。浏览器编辑器 UI 与 Rust/Agent browser capability 仍是后续工作，当前实现状态和安全策略以 zeta-desktop-architecture.md 为准。 Renderer 内受控 HTML 使用独立的 WebviewElement iframe boundary，而不是 WebContentsView 或 Electron <webview 标签。当前 iframe 采用 opaque origin、固定 CSP 和 source/channel message validation；扩展宿主与独立 webview origin 尚未实现，精确状态见 zeta-desktop-architecture.md。 Markdown 也不以解析器输出作为信任边界。当前 Workbench 短内容使用 marked → DOMPurify → MarkdownElement，完整预览使用 markdown-it → DOMPurify → MarkdownPreview → WebviewElement。两条路径共享严格的标签、 属性和 URL allowlist。DOMPurify 的直接适配只位于 base/browser/domSanitize.ts； workbench/contrib/markdown 通过 MarkdownDocumentView 接入平台预览并拥有产品链接策略， 不复制安全边界。语法高亮、扩展插件及工作区资源映射仍未实现。具体所有权与当前限制见 zeta-desktop-architecture.md。 10. 演进规则 当前处于开发阶段： - 领域语义错误时直接修正 canonical 类型和调用方； - wire、schema、fixture、Desktop 和 CLI 必须在同一变更内更新； - stored-event schema 不兼容时提升/重置当前 schema 并明确失败； - 不保留 deprecated alias、旧 route、隐式转换或双写； - 真正发布后再引入明确的版本政策与离线 migration 工具。 11. 验证门 `bash cargo fmt --check cargo clippy --workspace --all-targets -- -D warnings cargo test --workspace corepack pnpm --dir desktop test:main corepack pnpm --dir desktop typecheck:renderer `` 协议变更还必须重新生成 JSON Schema/TypeScript，并由 fixture test 校验生成结果逐字一致。"
  },
  {
    "slug": "connectors",
    "sourcePath": "docs/connectors.md",
    "title": "外部服务连接系统",
    "description": "Connector 管理“外部服务是否已经连接以及连接对应哪个账号”。在 Plugin 提供外部服务的 常见路径中，Plugin 声明 Connector 和 MCP server，Connector 在认证成功后发布就绪的 MCP 绑定。 这是 declaration 和 runtime 之间的数据流，不是 Plugin 在运行时包含 Connector、MCP ses…",
    "group": "其他系统文档",
    "markdown": "> 领域实现：[`zeta-rs/connectors/`](../zeta-rs/connectors/README.md)，Rust crate：\n> `zeta_connectors`。\n> Plugin/discovery 集成：[`zeta-rs/ext/connectors/`](../zeta-rs/ext/connectors/README.md)。\n> Plugin 分发边界：[`plugins.md`](plugins.md)。MCP 调用边界：[`mcp.md`](mcp.md)。\n> 当前状态：Connector domain、SQLite authority、API-token connect/disconnect、App Server 协议、\n> package-rooted Plugin activation、ready/standalone MCP composition、模型安全点 registry replacement、\n> in-flight dispatch drain、OS keyring 与显式文件 `SecretStore`、OAuth PKCE 状态机和产品连接入口已实现。\n> Desktop loopback 浏览器回调、通用 device poll、OAuth App Server 控制面、refresh/revoke、GitHub\n> brokered PKCE 与 public-client device adapters 已接通；正式 package 已内置官方 Plugin Marketplace\n> endpoint 与固定 TUF root，Connector 产品发行仍需注入真实 broker URL/client ID。\n\n## 快速理解\n\nConnector 管理“外部服务是否已经连接以及连接对应哪个账号”。在 Plugin 提供外部服务的\n常见路径中，Plugin 声明 Connector 和 MCP server，Connector 在认证成功后发布就绪的 MCP 绑定。\n这是 declaration 和 runtime 之间的数据流，不是 Plugin 在运行时包含 Connector、MCP session 或 Tool。\nConnector 不要求用户登录 Zeta，连接的账号是 GitHub、Slack、Google Drive 等外部产品账号。\n\n| 用户场景 | 系统发生什么 | 不会自动发生什么 |\n| --- | --- | --- |\n| 安装 GitHub Plugin | Plugin contribution 提供 GitHub Connector declaration | 不连接 GitHub、不启动 MCP |\n| 查看未连接的 GitHub | Connector 以 `Connect` candidate 出现在 discovery | 不向 Agent 暴露 GitHub tools |\n| 完成 GitHub OAuth | 认证 owner 保存 secret，并向 Connector 发布 non-secret account/reference | Connector 不读取 token bytes |\n| Connector 成为 connected | ready MCP binding 可以由 host materialize | 不绕过 MCP policy 或 Tool approval |\n| 用户断开 GitHub | connection generation 前进，ready binding 立即撤销 | 不删除 Plugin package 或 Thread 历史 |\n| 用户直接配置 MCP server | 经过配置、凭据和策略解析后直接进入 MCP runtime | 不必须伪造 Plugin 或 Connector |\n\n## 1. 结论\n\n系统边界固定为：\n\n> Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Tool 是 Agent 最终消费的能力。\n\n| 对象 | 回答的核心问题 | 产出 | 不拥有 |\n| --- | --- | --- | --- |\n| Plugin | “要给 Zeta 分发和启用哪些扩展贡献？” | 带版本、摘要和来源的 contribution declaration | 外部账号、OAuth 状态、MCP session |\n| Connector | “这个外部产品连上了吗，连的是哪个账号？” | generation-bound connection state 和 ready runtime binding | Plugin package、secret bytes、MCP transport |\n| MCP | “如何与 capability server 建立会话并发现、调用能力？” | session、capability catalog、绑定和调用结果 | Plugin/Connector authority、每次 Tool approval |\n| Tool Registry / Core | “Agent 当前可以调用什么，这次调用是否允许？” | provider-independent Tool definition、approval 和 durable result | Plugin 安装、connect/OAuth lifecycle |\n\n```mermaid\nflowchart TD\n    subgraph P[\"Plugin package：声明控制面\"]\n        PC[\"ConnectorContribution\"]\n        PM[\"McpServerContribution\"]\n        PO[\"Skill / static asset / other contributions\"]\n    end\n\n    PC -. \"references\" .-> PM\n    PC --> CP[\"Connector definition + Plugin provenance\"]\n    CP --> C[\"Connector runtime：account / state / generation\"]\n    A[\"Auth adapter + Secrets owner\"] -->|\"account + credential reference\"| C\n    C -->|\"connected\"| B[\"Ready MCP binding\"]\n    C -->|\"disconnected\"| D[\"Catalog discovery：Connect\"]\n    PM -->|\"standalone activation\"| M[\"MCP runtime：session / list_tools / call_tool\"]\n    PM --> B\n    U[\"User / Workspace MCP configuration\"] --> M\n    B --> M\n    M --> T[\"Tool Registry / Core / Agent\"]\n    PO --> O[\"Corresponding contribution consumer\"]\n```\n\n图中的连线表示合法的声明与运行时组合，不表示安装 Plugin 后会自动执行后续阶段。Plugin 可以没有\nConnector；Plugin 或 User/Workspace 也可以独立声明 MCP server。只有“该 MCP server 需要一个已连接的\n外部账号”时，才经过 Connector 路径；该路径只有在 connected 状态下才能发布 ready binding。\n当前 binding 使用 MCP server declaration，但 Connector domain 不拥有 MCP transport/session。\n\n### 1.1 关系是组合，不是运行时包含\n\n- Plugin 包可以同时声明 `ConnectorContribution` 和被它引用的 `McpServerContribution`，但 Plugin manager 只负责验证、分发和激活这些声明。\n- Connector runtime 消费 Connector declaration 并发布连接状态；OAuth/API-key 交互由认证 adapter 执行，secret bytes 由 Secrets owner 保存。\n- Ready binding 只是“已可以 materialize 哪个 runtime”的 generation-bound 描述，不是 live MCP session 或 Tool binding。\n- MCP runtime 消费 ready binding 或独立 MCP declaration，建立 session 并向统一 Tool Registry 贡献能力；Tool 不回归 Connector 或 Plugin 所有。\n\n## 2. 所有权边界\n\n| Owner | 拥有 | 明确不拥有 |\n| --- | --- | --- |\n| `zeta-plugins` | package、manifest、`ConnectorContribution`、安装/启用 provenance | 外部账号、OAuth、MCP session |\n| `zeta-connectors` | identity、definition、account projection、状态机、generation-bound snapshot | Plugin、secret storage、I/O runtime |\n| `zeta-connectors-extension` | Plugin 转换、Plugin provenance、discovery 与 ready-binding projection | 领域状态机、live authentication/MCP |\n| Connector auth adapter | connect/revoke、OAuth callback、credential refresh/materialization | Plugin package、Tool execution |\n| `zeta-mcp-extension` | ready declaration 到 live MCP tools runtime 的 host integration | Connector account authority |\n| `zeta-tools` / Core | Tool registry、approval、durable execution | Connect/OAuth lifecycle |\n\nConnector account 不是 Zeta account。Zeta login 只能作为云端 directory、同步或 managed policy 的可选\nadapter；不得成为 `ConnectorId`、connection generation 或本地 runtime readiness 的前置条件。\n\n## 3. 当前执行路径\n\n当前 API-token 路径为：\n\n1. `PluginActivationSnapshot::resolve` 将 exact installed package 固定为不可变激活快照；`ConnectorCatalog::from_activation` 从该快照投影带 package digest 的 `ConnectorDefinition`。\n2. App Server 通过 `connector/list` 返回不含 credential reference 的状态；`connector/connect/apiToken` 接收一次性 secret DTO。\n3. `ConnectorCredentialService` 先提交 `Connecting`，再把 token 写入 `SecretStore`，最后向 `ConnectorAuthority` 提交只含 account 与 opaque reference 的 `Connected`。\n4. SQLite authority 在一个事务中追加状态事件与 retry receipt；重复 command ID 只重放完全相同的请求。\n5. 本地 composition root 从激活快照自动构造 package-rooted MCP provider，并订阅 Config、Connector generation 与 MCP `tools/list_changed`；完整启动下一代 runtime 后替换 MCP tool port。\n6. 每个 Connector MCP call 在 prepare 和真正 dispatch 前复核 connector ID、connection generation 与 definition digest；disconnect 提交和 dispatch 使用同一 authority lock 线性化，已开始调用先排空，后续调用被拒绝。\n\n```mermaid\nflowchart TD\n    API[\"connector/connect/apiToken\"] --> B[\"BeginConnect receipt\"]\n    B --> S[\"SecretStore::store\"]\n    S --> C[\"CompleteConnect receipt\"]\n    C --> N[\"connector/changed\"]\n    N --> R[\"Config + Connector reconcile\"]\n    R --> M[\"ConnectorMcpRuntimeProvider\"]\n    M --> T[\"MCP Tool port\"]\n    T --> F[\"live authority fence\"]\n    D[\"connector/disconnect\"] --> F\n```\n\n当前 `ConnectorOAuthService` 已实现随机 state、PKCE S256、exact redirect、一次性 callback、超时、\nrefresh/revoke 与 stale-generation 防护。`ConnectorDeviceOAuthService` 拥有通用 device-code 生命周期、\nprovider poll interval、`slow_down + 5s`、expiry/cancel 与同一 Connector authority transition；provider adapter\n只负责 wire protocol。OAuth credential 在 SecretStore 中以 runtime access token + provider lifecycle bundle\n封装，MCP materialization 只得到 access token，refresh/revoke adapter 只得到 lifecycle bundle。\n\nGitHub 有三种产品 adapter：confidential direct adapter（host 注入 secret）、推荐的 brokered PKCE adapter\n（client 不持有 GitHub App secret）和 public-client device adapter（无 client secret；GitHub 不提供对应\npublic-client remote revoke，因此 UI 只显示 local disconnect）。Desktop 按 `oauthMethods` 优先 browser、否则\ndevice；browser host 与 TUI 可执行 device flow，user code 复制到 clipboard，且不进入 composer/history。\n\n## 4. 身份、凭据与 generation\n\n| 值 | 含义 | 不能替代 |\n| --- | --- | --- |\n| `ConnectorId` | 一个 connectable product declaration | Plugin ID、account ID |\n| `ConnectorAccountId` | provider 返回的外部 account/tenant identity | Zeta user ID |\n| `ConnectorCredentialRef` | auth owner 可解释的 non-secret reference | access/refresh token bytes |\n| `ConnectorConnectionGeneration` | connect/revoke attempt 的单调身份 | MCP catalog generation |\n| `ConnectorSnapshotGeneration` | 一次 immutable Connector catalog projection | config revision、Tool registry generation |\n\n连接完成必须引用当前 `Connecting` attempt 的 exact connection generation；disconnect 必须推进\nconnection generation；任何状态变化都必须同时推进 snapshot generation。这样晚到的 OAuth callback、\n旧 refresh result 或旧 UI command 不能重新激活已撤销的 binding。\n\n## 5. 安全与失败语义\n\n- Connector snapshot 只持有 credential reference，不持有 secret bytes。\n- disconnected、connecting 和 unavailable entry 都不能输出 ready runtime binding。\n- connect/revoke command 必须由 durable authority 按 expected generation 复核，不能信任客户端 snapshot。\n- MCP tool 仍经过普通 registry collision、policy、approval、durable Tool Call/Result 和 recovery。\n- Plugin 验证只证明 declaration/package 合法，不证明外部服务可信或未来调用已批准。\n\n## 6. 当前状态与后续阶段\n\n| 能力 | 状态 |\n| --- | --- |\n| 纯 Connector identity/definition/binding | ✅ 已实现 |\n| connection transition 与双 generation 防 stale | ✅ 已实现 |\n| Plugin manifest → Connector domain projection | ✅ 已实现 |\n| disconnected discovery / connected ready binding projection | ✅ 已实现 |\n| SQLite connection authority + exact retry receipts | ✅ 已实现 |\n| definition/package digest 变化触发 reauthorization | ✅ 重启恢复与 live Plugin activation 更新均已实现 |\n| API-token connect/disconnect + local secret cleanup | ✅ 已实现 |\n| OAuth state/PKCE/exchange 编排 | ✅ 已实现 provider port、App Server RPC 与 Desktop loopback callback |\n| refresh、远端 revoke | ✅ 已实现通用 lifecycle 与 GitHub adapter；远端失败保留本地连接供重试 |\n| `zeta-secrets` memory/unavailable backend | ✅ 已实现 |\n| OS keyring backend | ✅ 已实现并作为本地默认 Connector persistence |\n| 显式文件 backend | ✅ 已实现，保持 explicit opt-in，不作为自动 fallback |\n| App Server list/connect/disconnect + changed notification | ✅ 已实现 |\n| Desktop API-token UI；TUI 列表/断开/通知刷新 | ✅ 已实现 |\n| OAuth browser/device interaction | ✅ Desktop browser+device、browser host device、TUI device 已实现 |\n| ready binding → `zeta-mcp-extension` composition + dispatch fence | ✅ 已实现（host-injected provider） |\n| exact Plugin activation → Connector/MCP runtime provider | ✅ 已实现，支持 live install/enable/disable authority reconcile |\n| MCP `tools/list_changed` → safe-point rebuild | ✅ 已实现 |\n\n产品部署入口为 `LocalProductServicesConfig` / `--product-services PATH` /\n`ZETA_PRODUCT_SERVICES_PATH`。该只读 JSON 可声明 TUF Marketplace roots、broker URL 和 public client ID，不允许\nclient secret；`trustedRoot` 必须是配置文件目录内的普通相对路径，distribution/broker base URL 必须是无\ncredential/query/fragment 且以 `/` 结尾的 HTTPS URL。普通 Plugin manifest 和 user config 不能提供这些发行信任材料。官方 Marketplace 的配置和 root 已由\npackage 从 `resources/product-services/` 携带，远端签名 metadata 与 package 由独立 registry 发布。下一阶段\n是部署真实 broker 并增加其他服务 adapter；它们必须复用现有 authority、SecretStore、App\nServer safe point 和 MCP dispatch fence，不得创建第二套 Connector 状态机。\n\n```json\n{\n  \"schemaVersion\": 1,\n  \"marketplaces\": [{\n    \"id\": \"zeta-official\",\n    \"trust\": \"productManaged\",\n    \"metadataBaseUrl\": \"https://marketplace.zeta.example/metadata/\",\n    \"targetsBaseUrl\": \"https://marketplace.zeta.example/targets/\",\n    \"trustedRoot\": \"marketplace-root.json\"\n  }],\n  \"connectorOauth\": [{\n    \"type\": \"githubDevice\",\n    \"connectorId\": \"openai/github:connector:account\",\n    \"clientId\": \"PUBLIC_GITHUB_APP_CLIENT_ID\",\n    \"scopes\": [\"read:user\", \"repo\"]\n  }]\n}\n```\n\n第三方源使用 `\"trust\": \"verifiedExternal\"`，并必须同时提供 non-empty、无重复的\n`\"allowedPublishers\": [\"community-a\"]`；签名 catalog 出现 scope 外 publisher 或两个远端源实际占用\n同一 publisher namespace 时，App Server 启动失败。默认省略 `trust` 只为兼容旧产品文件，语义仍是\n`productManaged`，不能同时填写 `allowedPublishers`。\n\n使用 broker 时把 `type` 改为 `githubBrokered`，并增加 `brokerBaseUrl`；broker API 固定为\n`v1/oauth/github/{authorize,token,revoke}`，response 必须带 provider-validated account identity。OAuth adapter\n可先于对应 Plugin 安装而注册；它保持 dormant，直到 exact Connector contribution 进入 activation snapshot。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "1-结论",
        "title": "1. 结论"
      },
      {
        "depth": 3,
        "id": "11-关系是组合不是运行时包含",
        "title": "1.1 关系是组合，不是运行时包含"
      },
      {
        "depth": 2,
        "id": "2-所有权边界",
        "title": "2. 所有权边界"
      },
      {
        "depth": 2,
        "id": "3-当前执行路径",
        "title": "3. 当前执行路径"
      },
      {
        "depth": 2,
        "id": "4-身份凭据与-generation",
        "title": "4. 身份、凭据与 generation"
      },
      {
        "depth": 2,
        "id": "5-安全与失败语义",
        "title": "5. 安全与失败语义"
      },
      {
        "depth": 2,
        "id": "6-当前状态与后续阶段",
        "title": "6. 当前状态与后续阶段"
      }
    ],
    "searchText": " 领域实现：zeta-rs/connectors/，Rust crate： zetaconnectors。 Plugin/discovery 集成：zeta-rs/ext/connectors/。 Plugin 分发边界：plugins.md。MCP 调用边界：mcp.md。 当前状态：Connector domain、SQLite authority、API-token connect/disconnect、App Server 协议、 package-rooted Plugin activation、ready/standalone MCP composition、模型安全点 registry replacement、 in-flight dispatch drain、OS keyring 与显式文件 SecretStore、OAuth PKCE 状态机和产品连接入口已实现。 Desktop loopback 浏览器回调、通用 device poll、OAuth App Server 控制面、refresh/revoke、GitHub brokered PKCE 与 public-client device adapters 已接通；正式 package 已内置官方 Plugin Marketplace endpoint 与固定 TUF root，Connector 产品发行仍需注入真实 broker URL/client ID。 快速理解 Connector 管理“外部服务是否已经连接以及连接对应哪个账号”。在 Plugin 提供外部服务的 常见路径中，Plugin 声明 Connector 和 MCP server，Connector 在认证成功后发布就绪的 MCP 绑定。 这是 declaration 和 runtime 之间的数据流，不是 Plugin 在运行时包含 Connector、MCP session 或 Tool。 Connector 不要求用户登录 Zeta，连接的账号是 GitHub、Slack、Google Drive 等外部产品账号。 用户场景 系统发生什么 不会自动发生什么 --- --- --- 安装 GitHub Plugin Plugin contribution 提供 GitHub Connector declaration 不连接 GitHub、不启动 MCP 查看未连接的 GitHub Connector 以 Connect candidate 出现在 discovery 不向 Agent 暴露 GitHub tools 完成 GitHub OAuth 认证 owner 保存 secret，并向 Connector 发布 non-secret account/reference Connector 不读取 token bytes Connector 成为 connected ready MCP binding 可以由 host materialize 不绕过 MCP policy 或 Tool approval 用户断开 GitHub connection generation 前进，ready binding 立即撤销 不删除 Plugin package 或 Thread 历史 用户直接配置 MCP server 经过配置、凭据和策略解析后直接进入 MCP runtime 不必须伪造 Plugin 或 Connector 1. 结论 系统边界固定为： Plugin 管扩展分发，Connector 管外部账号连接，MCP 管协议会话与能力调用，Tool 是 Agent 最终消费的能力。 对象 回答的核心问题 产出 不拥有 --- --- --- --- Plugin “要给 Zeta 分发和启用哪些扩展贡献？” 带版本、摘要和来源的 contribution declaration 外部账号、OAuth 状态、MCP session Connector “这个外部产品连上了吗，连的是哪个账号？” generation-bound connection state 和 ready runtime binding Plugin package、secret bytes、MCP transport MCP “如何与 capability server 建立会话并发现、调用能力？” session、capability catalog、绑定和调用结果 Plugin/Connector authority、每次 Tool approval Tool Registry / Core “Agent 当前可以调用什么，这次调用是否允许？” provider-independent Tool definition、approval 和 durable result Plugin 安装、connect/OAuth lifecycle ``mermaid flowchart TD subgraph P \"Plugin package：声明控制面\" PC \"ConnectorContribution\" PM \"McpServerContribution\" PO \"Skill / static asset / other contributions\" end PC -. \"references\" .- PM PC -- CP \"Connector definition + Plugin provenance\" CP -- C \"Connector runtime：account / state / generation\" A \"Auth adapter + Secrets owner\" -- \"account + credential reference\" C C -- \"connected\" B \"Ready MCP binding\" C -- \"disconnected\" D \"Catalog discovery：Connect\" PM -- \"standalone activation\" M \"MCP runtime：session / listtools / calltool\" PM -- B U \"User / Workspace MCP configuration\" -- M B -- M M -- T \"Tool Registry / Core / Agent\" PO -- O \"Corresponding contribution consumer\" ` 图中的连线表示合法的声明与运行时组合，不表示安装 Plugin 后会自动执行后续阶段。Plugin 可以没有 Connector；Plugin 或 User/Workspace 也可以独立声明 MCP server。只有“该 MCP server 需要一个已连接的 外部账号”时，才经过 Connector 路径；该路径只有在 connected 状态下才能发布 ready binding。 当前 binding 使用 MCP server declaration，但 Connector domain 不拥有 MCP transport/session。 1.1 关系是组合，不是运行时包含 - Plugin 包可以同时声明 ConnectorContribution 和被它引用的 McpServerContribution，但 Plugin manager 只负责验证、分发和激活这些声明。 - Connector runtime 消费 Connector declaration 并发布连接状态；OAuth/API-key 交互由认证 adapter 执行，secret bytes 由 Secrets owner 保存。 - Ready binding 只是“已可以 materialize 哪个 runtime”的 generation-bound 描述，不是 live MCP session 或 Tool binding。 - MCP runtime 消费 ready binding 或独立 MCP declaration，建立 session 并向统一 Tool Registry 贡献能力；Tool 不回归 Connector 或 Plugin 所有。 2. 所有权边界 Owner 拥有 明确不拥有 --- --- --- zeta-plugins package、manifest、ConnectorContribution、安装/启用 provenance 外部账号、OAuth、MCP session zeta-connectors identity、definition、account projection、状态机、generation-bound snapshot Plugin、secret storage、I/O runtime zeta-connectors-extension Plugin 转换、Plugin provenance、discovery 与 ready-binding projection 领域状态机、live authentication/MCP Connector auth adapter connect/revoke、OAuth callback、credential refresh/materialization Plugin package、Tool execution zeta-mcp-extension ready declaration 到 live MCP tools runtime 的 host integration Connector account authority zeta-tools / Core Tool registry、approval、durable execution Connect/OAuth lifecycle Connector account 不是 Zeta account。Zeta login 只能作为云端 directory、同步或 managed policy 的可选 adapter；不得成为 ConnectorId、connection generation 或本地 runtime readiness 的前置条件。 3. 当前执行路径 当前 API-token 路径为： 1. PluginActivationSnapshot::resolve 将 exact installed package 固定为不可变激活快照；ConnectorCatalog::fromactivation 从该快照投影带 package digest 的 ConnectorDefinition。 2. App Server 通过 connector/list 返回不含 credential reference 的状态；connector/connect/apiToken 接收一次性 secret DTO。 3. ConnectorCredentialService 先提交 Connecting，再把 token 写入 SecretStore，最后向 ConnectorAuthority 提交只含 account 与 opaque reference 的 Connected。 4. SQLite authority 在一个事务中追加状态事件与 retry receipt；重复 command ID 只重放完全相同的请求。 5. 本地 composition root 从激活快照自动构造 package-rooted MCP provider，并订阅 Config、Connector generation 与 MCP tools/listchanged；完整启动下一代 runtime 后替换 MCP tool port。 6. 每个 Connector MCP call 在 prepare 和真正 dispatch 前复核 connector ID、connection generation 与 definition digest；disconnect 提交和 dispatch 使用同一 authority lock 线性化，已开始调用先排空，后续调用被拒绝。 `mermaid flowchart TD API \"connector/connect/apiToken\" -- B \"BeginConnect receipt\" B -- S \"SecretStore::store\" S -- C \"CompleteConnect receipt\" C -- N \"connector/changed\" N -- R \"Config + Connector reconcile\" R -- M \"ConnectorMcpRuntimeProvider\" M -- T \"MCP Tool port\" T -- F \"live authority fence\" D \"connector/disconnect\" -- F ` 当前 ConnectorOAuthService 已实现随机 state、PKCE S256、exact redirect、一次性 callback、超时、 refresh/revoke 与 stale-generation 防护。ConnectorDeviceOAuthService 拥有通用 device-code 生命周期、 provider poll interval、slowdown + 5s、expiry/cancel 与同一 Connector authority transition；provider adapter 只负责 wire protocol。OAuth credential 在 SecretStore 中以 runtime access token + provider lifecycle bundle 封装，MCP materialization 只得到 access token，refresh/revoke adapter 只得到 lifecycle bundle。 GitHub 有三种产品 adapter：confidential direct adapter（host 注入 secret）、推荐的 brokered PKCE adapter （client 不持有 GitHub App secret）和 public-client device adapter（无 client secret；GitHub 不提供对应 public-client remote revoke，因此 UI 只显示 local disconnect）。Desktop 按 oauthMethods 优先 browser、否则 device；browser host 与 TUI 可执行 device flow，user code 复制到 clipboard，且不进入 composer/history。 4. 身份、凭据与 generation 值 含义 不能替代 --- --- --- ConnectorId 一个 connectable product declaration Plugin ID、account ID ConnectorAccountId provider 返回的外部 account/tenant identity Zeta user ID ConnectorCredentialRef auth owner 可解释的 non-secret reference access/refresh token bytes ConnectorConnectionGeneration connect/revoke attempt 的单调身份 MCP catalog generation ConnectorSnapshotGeneration 一次 immutable Connector catalog projection config revision、Tool registry generation 连接完成必须引用当前 Connecting attempt 的 exact connection generation；disconnect 必须推进 connection generation；任何状态变化都必须同时推进 snapshot generation。这样晚到的 OAuth callback、 旧 refresh result 或旧 UI command 不能重新激活已撤销的 binding。 5. 安全与失败语义 - Connector snapshot 只持有 credential reference，不持有 secret bytes。 - disconnected、connecting 和 unavailable entry 都不能输出 ready runtime binding。 - connect/revoke command 必须由 durable authority 按 expected generation 复核，不能信任客户端 snapshot。 - MCP tool 仍经过普通 registry collision、policy、approval、durable Tool Call/Result 和 recovery。 - Plugin 验证只证明 declaration/package 合法，不证明外部服务可信或未来调用已批准。 6. 当前状态与后续阶段 能力 状态 --- --- 纯 Connector identity/definition/binding ✅ 已实现 connection transition 与双 generation 防 stale ✅ 已实现 Plugin manifest → Connector domain projection ✅ 已实现 disconnected discovery / connected ready binding projection ✅ 已实现 SQLite connection authority + exact retry receipts ✅ 已实现 definition/package digest 变化触发 reauthorization ✅ 重启恢复与 live Plugin activation 更新均已实现 API-token connect/disconnect + local secret cleanup ✅ 已实现 OAuth state/PKCE/exchange 编排 ✅ 已实现 provider port、App Server RPC 与 Desktop loopback callback refresh、远端 revoke ✅ 已实现通用 lifecycle 与 GitHub adapter；远端失败保留本地连接供重试 zeta-secrets memory/unavailable backend ✅ 已实现 OS keyring backend ✅ 已实现并作为本地默认 Connector persistence 显式文件 backend ✅ 已实现，保持 explicit opt-in，不作为自动 fallback App Server list/connect/disconnect + changed notification ✅ 已实现 Desktop API-token UI；TUI 列表/断开/通知刷新 ✅ 已实现 OAuth browser/device interaction ✅ Desktop browser+device、browser host device、TUI device 已实现 ready binding → zeta-mcp-extension composition + dispatch fence ✅ 已实现（host-injected provider） exact Plugin activation → Connector/MCP runtime provider ✅ 已实现，支持 live install/enable/disable authority reconcile MCP tools/listchanged → safe-point rebuild ✅ 已实现 产品部署入口为 LocalProductServicesConfig / --product-services PATH / ZETAPRODUCTSERVICESPATH。该只读 JSON 可声明 TUF Marketplace roots、broker URL 和 public client ID，不允许 client secret；trustedRoot 必须是配置文件目录内的普通相对路径，distribution/broker base URL 必须是无 credential/query/fragment 且以 / 结尾的 HTTPS URL。普通 Plugin manifest 和 user config 不能提供这些发行信任材料。官方 Marketplace 的配置和 root 已由 package 从 resources/product-services/ 携带，远端签名 metadata 与 package 由独立 registry 发布。下一阶段 是部署真实 broker 并增加其他服务 adapter；它们必须复用现有 authority、SecretStore、App Server safe point 和 MCP dispatch fence，不得创建第二套 Connector 状态机。 `json \"schemaVersion\": 1, \"marketplaces\": \"id\": \"zeta-official\", \"trust\": \"productManaged\", \"metadataBaseUrl\": \"https://marketplace.zeta.example/metadata/\", \"targetsBaseUrl\": \"https://marketplace.zeta.example/targets/\", \"trustedRoot\": \"marketplace-root.json\" , \"connectorOauth\": \"type\": \"githubDevice\", \"connectorId\": \"openai/github:connector:account\", \"clientId\": \"PUBLICGITHUBAPPCLIENTID\", \"scopes\": \"read:user\", \"repo\" ` 第三方源使用 \"trust\": \"verifiedExternal\"，并必须同时提供 non-empty、无重复的 \"allowedPublishers\": \"community-a\" ；签名 catalog 出现 scope 外 publisher 或两个远端源实际占用 同一 publisher namespace 时，App Server 启动失败。默认省略 trust 只为兼容旧产品文件，语义仍是 productManaged，不能同时填写 allowedPublishers。 使用 broker 时把 type 改为 githubBrokered，并增加 brokerBaseUrl；broker API 固定为 v1/oauth/github/ authorize,token,revoke `，response 必须带 provider-validated account identity。OAuth adapter 可先于对应 Plugin 安装而注册；它保持 dormant，直到 exact Connector contribution 进入 activation snapshot。"
  },
  {
    "slug": "debugging",
    "sourcePath": "docs/debugging.md",
    "title": "调试系统",
    "description": "Code 可以从 .vscode/launch.json 启动或附加到一个调试目标，并同时运行多个 DAP 会话。用户可以设置持久化行断点、选择线程和栈帧、递归展开变量、维护 Watch、在调试控制台求值、启用异常断点、读取适配器提供的虚拟源码，以及启动 compound 配置。调试前后的 Tasks 由 Workbench 编排；后端只负责受信任的适配器进程和 DAP …",
    "group": "其他系统文档",
    "markdown": "> 状态：Code 产品已具备通用 DAP 调试平台；Academic 不组装 Tasks、Testing 或 Debug。后端实现细节由 [`zeta-debug-adapter` README](../zeta-rs/debug-adapter/README.md) 拥有，Renderer 实现细节由 [Workbench Debug README](../desktop/src/zeta/workbench/services/debug/README.md) 拥有。\n\n## 快速理解\n\nCode 可以从 `.vscode/launch.json` 启动或附加到一个调试目标，并同时运行多个 DAP 会话。用户可以设置持久化行断点、选择线程和栈帧、递归展开变量、维护 Watch、在调试控制台求值、启用异常断点、读取适配器提供的虚拟源码，以及启动 compound 配置。调试前后的 Tasks 由 Workbench 编排；后端只负责受信任的适配器进程和 DAP framing。\n\n| 使用场景 | 当前结果 | 关键边界 |\n| --- | --- | --- |\n| 启动或附加 | ✅ `launch`、`attach`、重启、停止和 `runInTerminal` | Workbench 解释配置；后端启动适配器 |\n| 断点 | ✅ 工作区持久化行断点、适配器确认状态、异常断点 | Editor 只提供通用 gutter |\n| 停住后检查 | ✅ 线程选择、调用栈、作用域、递归变量树和 `sourceReference` | DAP Session 拥有请求语义 |\n| Watch 与控制台 | ✅ 持久 Watch、`evaluate` 和 REPL 输出 | Watch 持久；求值结果与输出临时 |\n| 多目标调试 | ✅ 多会话、会话切换、compound 和 `stopAll` | 后端会话仍按连接隔离 |\n| 调试任务 | ✅ `preLaunchTask`、`postDebugTask` | Tasks 负责执行和退出状态 |\n| 适配器发现 | ✅ 声明式 `contributes.debuggers`，仍可显式写 `debugAdapter` | 不执行扩展 JavaScript |\n| 完整 VS Code Debug 扩展 API | 非目标 | Zeta Host RPC v1 不是 VS Code/Node Extension API；兼容层需独立立项 |\n\n## 一次调试如何执行\n\n```mermaid\nflowchart LR\n    Launch[\"launch.json / declarative debugger\"] --> DebugService[\"Workbench Debug service\"]\n    Tasks[\"Tasks service\"] --> DebugService\n    DebugService --> Sessions[\"one or more DAP sessions\"]\n    Sessions --> Platform[\"Renderer debug process boundary\"]\n    Platform --> AppServer[\"App Server connection owner\"]\n    AppServer --> Runtime[\"zeta-debug-adapter\"]\n    Runtime --> Adapter[\"Debug adapter process\"]\n    Sessions --> View[\"Run and Debug view\"]\n    DebugService --> Gutter[\"Generic editor gutter\"]\n    Sessions --> Terminal[\"Integrated terminal\"]\n```\n\n1. `DebugService` 读取并验证 `.vscode/launch.json`。配置可显式声明适配器命令，也可按 `type` 从声明式扩展注册表解析；compound 只在 Workbench 中展开。\n2. 如果存在 `preLaunchTask`，Tasks 必须先返回成功；缺失、歧义、失败或取消都会阻止调试启动。\n3. App Server 校验同一工作区的可执行配置与进程执行能力，启动 stdio 适配器，并把会话归属绑定到发起连接。\n4. DAP Session 完成 `initialize`、`launch`/`attach`、行断点、异常断点和 `configurationDone`。反向 `runInTerminal` 请求委托给现有 Terminal service。\n5. `stopped` 事件驱动线程、栈帧、作用域、变量、Watch 和源码请求。多个会话独立保存运行状态，视图只选择其中一个作为当前检查对象。\n6. 适配器退出、用户停止、工作区切换、信任撤销或连接关闭都会回收进程。Workbench 随后运行对应的 `postDebugTask`。\n\n## 所有权边界\n\n| 能力 | Editor | Workbench Debug | Platform / App Server | `zeta-debug-adapter` |\n| --- | --- | --- | --- | --- |\n| 通用 gutter 槽位 | ✅ 拥有 | 投影断点 | ❌ | ❌ |\n| 断点、Watch、会话和 DAP 客户端语义 | ❌ | ✅ 拥有 | 传输 | ❌ |\n| launch、compound 与 Tasks 编排 | ❌ | ✅ 拥有 | ❌ | ❌ |\n| `runInTerminal` 产品组合 | ❌ | ✅ 委托 Terminal | 终端传输 | ❌ |\n| 连接权限与可信工作区 | ❌ | 请求 | ✅ 拥有 | 消费能力 |\n| 进程、framing、缓冲与回收 | ❌ | 消费 | 连接包装 | ✅ 拥有 |\n\nEditor 不得 import Debug service；它只提供无领域语义的 gutter decoration contract。后端 runtime 不得解析 launch 配置、持久化断点、决定当前线程或拥有 Workbench 会话选择。声明式扩展服务只贡献经过验证的适配器命令元数据；Zeta-native executable Host v1 是另一条逐扩展进程、Plugin + Workspace authority 与 brokered provider 边界，当前产品接入状态见 [`editor-extensions.md`](editor-extensions.md)。\n\n## 持久性与失败语义\n\n工作区存储保留行断点、Watch 表达式和按适配器类型划分的异常过滤器。适配器确认状态、调用栈、变量、控制台输出和活跃会话不会持久化。切换工作区时先保存旧状态，再恢复新工作区状态并停止旧会话。\n\ncompound 启动中任一配置失败时，已经启动的会话会回滚。自然退出和主动停止都只执行一次 `postDebugTask`。声明式适配器被卸载后，Workbench 会先清空旧 launch 候选再重新解析，避免继续执行已经失效的命令。\n\n## 当前实现与后续演进\n\n当前已实现：可信 stdio adapter、连接级归属、有界 framing/分页、显式和声明式适配器解析、初始化与请求配对、持久行断点、异常断点、线程/栈/递归变量、Watch/`evaluate`、调试控制台、虚拟源码、多会话、compound、restart、Tasks 生命周期、`runInTerminal`、Code-only 组装和断点 gutter。\n\n仍属于后续扩展：条件/日志/函数/数据/指令断点，socket/server adapter，跨进程会话恢复，以及 VS Code Debug Extension API 兼容层。Zeta Host v1 的 runtime core 已存在，但 production enforcing launcher 和跨层 Debug factory bridge 未完成验证前，不能把声明式适配器发现描述成可执行第三方扩展运行时。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "一次调试如何执行",
        "title": "一次调试如何执行"
      },
      {
        "depth": 2,
        "id": "所有权边界",
        "title": "所有权边界"
      },
      {
        "depth": 2,
        "id": "持久性与失败语义",
        "title": "持久性与失败语义"
      },
      {
        "depth": 2,
        "id": "当前实现与后续演进",
        "title": "当前实现与后续演进"
      }
    ],
    "searchText": " 状态：Code 产品已具备通用 DAP 调试平台；Academic 不组装 Tasks、Testing 或 Debug。后端实现细节由 zeta-debug-adapter README 拥有，Renderer 实现细节由 Workbench Debug README 拥有。 快速理解 Code 可以从 .vscode/launch.json 启动或附加到一个调试目标，并同时运行多个 DAP 会话。用户可以设置持久化行断点、选择线程和栈帧、递归展开变量、维护 Watch、在调试控制台求值、启用异常断点、读取适配器提供的虚拟源码，以及启动 compound 配置。调试前后的 Tasks 由 Workbench 编排；后端只负责受信任的适配器进程和 DAP framing。 使用场景 当前结果 关键边界 --- --- --- 启动或附加 ✅ launch、attach、重启、停止和 runInTerminal Workbench 解释配置；后端启动适配器 断点 ✅ 工作区持久化行断点、适配器确认状态、异常断点 Editor 只提供通用 gutter 停住后检查 ✅ 线程选择、调用栈、作用域、递归变量树和 sourceReference DAP Session 拥有请求语义 Watch 与控制台 ✅ 持久 Watch、evaluate 和 REPL 输出 Watch 持久；求值结果与输出临时 多目标调试 ✅ 多会话、会话切换、compound 和 stopAll 后端会话仍按连接隔离 调试任务 ✅ preLaunchTask、postDebugTask Tasks 负责执行和退出状态 适配器发现 ✅ 声明式 contributes.debuggers，仍可显式写 debugAdapter 不执行扩展 JavaScript 完整 VS Code Debug 扩展 API 非目标 Zeta Host RPC v1 不是 VS Code/Node Extension API；兼容层需独立立项 一次调试如何执行 ``mermaid flowchart LR Launch \"launch.json / declarative debugger\" -- DebugService \"Workbench Debug service\" Tasks \"Tasks service\" -- DebugService DebugService -- Sessions \"one or more DAP sessions\" Sessions -- Platform \"Renderer debug process boundary\" Platform -- AppServer \"App Server connection owner\" AppServer -- Runtime \"zeta-debug-adapter\" Runtime -- Adapter \"Debug adapter process\" Sessions -- View \"Run and Debug view\" DebugService -- Gutter \"Generic editor gutter\" Sessions -- Terminal \"Integrated terminal\" ` 1. DebugService 读取并验证 .vscode/launch.json。配置可显式声明适配器命令，也可按 type 从声明式扩展注册表解析；compound 只在 Workbench 中展开。 2. 如果存在 preLaunchTask，Tasks 必须先返回成功；缺失、歧义、失败或取消都会阻止调试启动。 3. App Server 校验同一工作区的可执行配置与进程执行能力，启动 stdio 适配器，并把会话归属绑定到发起连接。 4. DAP Session 完成 initialize、launch/attach、行断点、异常断点和 configurationDone。反向 runInTerminal 请求委托给现有 Terminal service。 5. stopped 事件驱动线程、栈帧、作用域、变量、Watch 和源码请求。多个会话独立保存运行状态，视图只选择其中一个作为当前检查对象。 6. 适配器退出、用户停止、工作区切换、信任撤销或连接关闭都会回收进程。Workbench 随后运行对应的 postDebugTask。 所有权边界 能力 Editor Workbench Debug Platform / App Server zeta-debug-adapter --- --- --- --- --- 通用 gutter 槽位 ✅ 拥有 投影断点 ❌ ❌ 断点、Watch、会话和 DAP 客户端语义 ❌ ✅ 拥有 传输 ❌ launch、compound 与 Tasks 编排 ❌ ✅ 拥有 ❌ ❌ runInTerminal 产品组合 ❌ ✅ 委托 Terminal 终端传输 ❌ 连接权限与可信工作区 ❌ 请求 ✅ 拥有 消费能力 进程、framing、缓冲与回收 ❌ 消费 连接包装 ✅ 拥有 Editor 不得 import Debug service；它只提供无领域语义的 gutter decoration contract。后端 runtime 不得解析 launch 配置、持久化断点、决定当前线程或拥有 Workbench 会话选择。声明式扩展服务只贡献经过验证的适配器命令元数据；Zeta-native executable Host v1 是另一条逐扩展进程、Plugin + Workspace authority 与 brokered provider 边界，当前产品接入状态见 editor-extensions.md。 持久性与失败语义 工作区存储保留行断点、Watch 表达式和按适配器类型划分的异常过滤器。适配器确认状态、调用栈、变量、控制台输出和活跃会话不会持久化。切换工作区时先保存旧状态，再恢复新工作区状态并停止旧会话。 compound 启动中任一配置失败时，已经启动的会话会回滚。自然退出和主动停止都只执行一次 postDebugTask。声明式适配器被卸载后，Workbench 会先清空旧 launch 候选再重新解析，避免继续执行已经失效的命令。 当前实现与后续演进 当前已实现：可信 stdio adapter、连接级归属、有界 framing/分页、显式和声明式适配器解析、初始化与请求配对、持久行断点、异常断点、线程/栈/递归变量、Watch/evaluate、调试控制台、虚拟源码、多会话、compound、restart、Tasks 生命周期、runInTerminal`、Code-only 组装和断点 gutter。 仍属于后续扩展：条件/日志/函数/数据/指令断点，socket/server adapter，跨进程会话恢复，以及 VS Code Debug Extension API 兼容层。Zeta Host v1 的 runtime core 已存在，但 production enforcing launcher 和跨层 Debug factory bridge 未完成验证前，不能把声明式适配器发现描述成可执行第三方扩展运行时。"
  },
  {
    "slug": "crates/auto-review/evals",
    "sourcePath": "zeta-rs/auto-review/evals/README.md",
    "title": "Auto Review 评估语料库",
    "description": "cases.jsonl 是用于衡量分类器行为的版本化种子集合，不把生产流量转成训练数据。每个非空行 都是一个独立 JSON 对象。",
    "group": "Crate 实现参考",
    "markdown": "`cases.jsonl` 是用于衡量分类器行为的版本化种子集合，不把生产流量转成训练数据。每个非空行\n都是一个独立 JSON 对象。\n\n输入部分对应 `ActionReviewRequest` 中与安全相关的内容：\n\n- 完整解析后的动作及其精确能力；\n- 宿主解析后的来源和沙箱限制；\n- 直接用户意图；\n- 带明确信任标签且长度受限的证据。\n\n预期部分记录分类器的标准建议，以及 `ActionPolicyEngine` 必须产生的最终决策。原因字段是给人阅读的\n审查说明；自动评分应比较建议、能力、风险、用户授权和最终决策，不能要求原因文字完全一致。\n\n提交到仓库的语料必须是合成且不含秘密的。不得粘贴生产提示词、仓库内容、凭据、用户标识或\n原始工具输出。来源于生产的案例需要单独进行隐私审查，并缩减为最小、可复现、匿名的结构。\n\n运行确定性契约：\n\n```text\ncargo test -p zeta-auto-review --test eval_contract\n```\n\n该测试不调用模型，也不访问网络。调用模型的评估程序必须是显式命令，记录模型和审查协议版本，\n并至少报告：\n\n- 危险动作自动批准率；\n- 不必要的用户交互率；\n- 建议、风险和授权准确率；\n- 更安全动作召回率；\n- 提示词注入与策略规避通过率。\n\n新增案例必须提供唯一稳定 ID、简短类别、非空理由，以及符合策略不变量的预期结果。人工推翻\n分类器决策或安全边界改变时，应优先加入对应的回归案例。",
    "headings": [],
    "searchText": "cases.jsonl 是用于衡量分类器行为的版本化种子集合，不把生产流量转成训练数据。每个非空行 都是一个独立 JSON 对象。 输入部分对应 ActionReviewRequest 中与安全相关的内容： - 完整解析后的动作及其精确能力； - 宿主解析后的来源和沙箱限制； - 直接用户意图； - 带明确信任标签且长度受限的证据。 预期部分记录分类器的标准建议，以及 ActionPolicyEngine 必须产生的最终决策。原因字段是给人阅读的 审查说明；自动评分应比较建议、能力、风险、用户授权和最终决策，不能要求原因文字完全一致。 提交到仓库的语料必须是合成且不含秘密的。不得粘贴生产提示词、仓库内容、凭据、用户标识或 原始工具输出。来源于生产的案例需要单独进行隐私审查，并缩减为最小、可复现、匿名的结构。 运行确定性契约： ``text cargo test -p zeta-auto-review --test evalcontract `` 该测试不调用模型，也不访问网络。调用模型的评估程序必须是显式命令，记录模型和审查协议版本， 并至少报告： - 危险动作自动批准率； - 不必要的用户交互率； - 建议、风险和授权准确率； - 更安全动作召回率； - 提示词注入与策略规避通过率。 新增案例必须提供唯一稳定 ID、简短类别、非空理由，以及符合策略不变量的预期结果。人工推翻 分类器决策或安全边界改变时，应优先加入对应的回归案例。"
  },
  {
    "slug": "crates/ext/connectors",
    "sourcePath": "zeta-rs/ext/connectors/README.md",
    "title": "Connectors extension",
    "description": "zeta-connectors-extension 拥有 Plugin projection、durable connection authority 和 credential orchestration。它不拥有 Connector state machine、secret backend、MCP session 或 Tool registry；这些分别 属于 zeta…",
    "group": "Crate 实现参考",
    "markdown": "> Connector domain contract 由 [`zeta-connectors`](../../connectors/README.md) 维护；跨系统语义见\n> [`docs/connectors.md`](../../../docs/connectors.md)。\n\n`zeta-connectors-extension` 拥有 Plugin projection、durable connection authority 和 credential\norchestration。它不拥有 Connector state machine、secret backend、MCP session 或 Tool registry；这些分别\n属于 `zeta-connectors`、`zeta-secrets` 和 `zeta-mcp-extension`。\n\n## 当前公共契约\n\n| Symbol | 职责 | 关键失败语义 |\n| --- | --- | --- |\n| `ConnectorCatalog::from_activation` | 从 exact activation package digest 构造授权兼容 revision | duplicate identity / invalid contribution fail closed |\n| `ConnectorCatalog::from_packages` | 用调用方提供的 package 集合构造投影 | 不提供 activation generation |\n| `ConnectorCatalog::from_manifests` | 无 package handle 时的较弱 declaration projection | 不覆盖 MCP definition 文件内容；生产 activation 应优先使用 package API |\n| `ConnectorAuthority::open_sqlite` | 恢复 snapshot、事件和 command receipts | event + receipt 在一个 SQLite transaction 中提交 |\n| `ConnectorAuthority::apply` | expected-generation CAS 与 exact command replay | 同 ID 不同 payload 为 `CommandConflict` |\n| `ConnectorCredentialService::connect_api_token` | Begin → secret store → Complete | secret store 失败时不会发布 `Connected` |\n| `ConnectorCredentialService::disconnect` | 先撤销 readiness，再 best-effort delete secret | cleanup 失败返回 `RetryRequired`，不回滚断连 |\n| `ConnectorAuthority::with_authorized_invocation` | 把 dispatch 与 disconnect commit 线性化 | stale generation/digest 不执行 closure |\n| `ConnectorOAuthService` | state + PKCE + exact redirect、one-shot callback、refresh/revoke 编排 | provider wire protocol 与 callback host 由 adapter/产品拥有 |\n| `ConnectorDeviceOAuthService` | device-code attempt、provider interval/slow-down、expiry/cancel 与 authority transition | device code 只驻留内存，不进入 protocol、history 或 SecretStore |\n| `ConnectorOAuthProvider` | 一个具体服务的授权 URL 与 code exchange 端口 | 不持久化 secret，不修改 authority |\n| `GitHubBrokeredOAuthProvider` | 经产品 broker 执行 PKCE exchange/refresh/revoke | client 不持有 GitHub App secret；broker deployment 不属于本 crate |\n| `GitHubDeviceOAuthProvider` | GitHub public-client device grant 与账户读取 | 无 client secret；不声明 GitHub 未提供的 refresh/remote revoke |\n| `GitHubOAuthProvider` | confidential direct GitHub adapter | 仅供可信 host 显式注入 client secret |\n\n## 内部调用路径\n\n```text\nConnectorCredentialService::connect_api_token\n  -> phase_command_id\n  -> ConnectorAuthority::apply(BeginConnect)\n       -> command::command_digest\n       -> authority::event_for_request\n       -> SqliteAuthority::persist\n  -> SecretStore::store\n  -> ConnectorAuthority::apply(CompleteConnect)\n\nConnectorAuthority::open_sqlite\n  -> SqliteAuthority::open\n  -> load_latest_records + load_receipts\n  -> PersistedRecord::restore\n  -> ReauthorizationRequired when definition digest changed\n```\n\n`authority::event_for_request` 拥有 command 到 durable event 的绑定；`SqliteAuthority::persist` 拥有\nevent/receipt 原子性；`auth::credential_key` 只生成 hashed non-PII key。若这些 helper 开始读取 token bytes、\n启动 MCP，或 App Server 直接写 SQLite，即表示 ownership 漂移。\n\n当前实现 API-token adapter、browser-code OAuth 的通用 PKCE 状态机与 device grant 状态机。`oauth::PendingOAuthAttempt`\n只在内存保存 flow ID、state 和 verifier；`ConnectorOAuthService::complete` 一次性消费 callback，\n并在过期、state mismatch、provider 或 credential failure 时提交 `Unavailable`。Desktop loopback\nlistener/browser interaction 已由 Electron main 持有。`device_oauth::PendingDeviceOAuthAttempt` 只在内存\n保存 device code，并严格执行 provider interval、GitHub `slow_down + 5s`、expiry 与 cancel。\n\n产品默认可选择 `GitHubBrokeredOAuthProvider`：client 只发送 PKCE verifier，GitHub App secret 留在发行方\nbroker；或选择 `GitHubDeviceOAuthProvider`：仅需 public client ID。旧的 `GitHubOAuthProvider` 继续作为可信\nhost 直连 adapter。OAuth secret 以 runtime token 与 lifecycle bundle 分层封装，MCP projection 只能拿到\nruntime token。远端 revoke 成功后才提交本地 disconnect；失败时保留 ready connection 以便重试。\n不能通过扩张 authority event payload 来保存 OAuth code、refresh token 或 raw credential。生产显式文件\nfile backend 位于 `zeta-secrets`，OS keyring adapter 位于独立的 `zeta-keyring-store`；均不属于本 crate。\n\n验证入口：\n\n```bash\ncargo test -p zeta-connectors-extension\ncargo clippy -p zeta-connectors-extension --all-targets --no-deps -- -D warnings\n```",
    "headings": [
      {
        "depth": 2,
        "id": "当前公共契约",
        "title": "当前公共契约"
      },
      {
        "depth": 2,
        "id": "内部调用路径",
        "title": "内部调用路径"
      }
    ],
    "searchText": " Connector domain contract 由 zeta-connectors 维护；跨系统语义见 docs/connectors.md。 zeta-connectors-extension 拥有 Plugin projection、durable connection authority 和 credential orchestration。它不拥有 Connector state machine、secret backend、MCP session 或 Tool registry；这些分别 属于 zeta-connectors、zeta-secrets 和 zeta-mcp-extension。 当前公共契约 Symbol 职责 关键失败语义 --- --- --- ConnectorCatalog::fromactivation 从 exact activation package digest 构造授权兼容 revision duplicate identity / invalid contribution fail closed ConnectorCatalog::frompackages 用调用方提供的 package 集合构造投影 不提供 activation generation ConnectorCatalog::frommanifests 无 package handle 时的较弱 declaration projection 不覆盖 MCP definition 文件内容；生产 activation 应优先使用 package API ConnectorAuthority::opensqlite 恢复 snapshot、事件和 command receipts event + receipt 在一个 SQLite transaction 中提交 ConnectorAuthority::apply expected-generation CAS 与 exact command replay 同 ID 不同 payload 为 CommandConflict ConnectorCredentialService::connectapitoken Begin → secret store → Complete secret store 失败时不会发布 Connected ConnectorCredentialService::disconnect 先撤销 readiness，再 best-effort delete secret cleanup 失败返回 RetryRequired，不回滚断连 ConnectorAuthority::withauthorizedinvocation 把 dispatch 与 disconnect commit 线性化 stale generation/digest 不执行 closure ConnectorOAuthService state + PKCE + exact redirect、one-shot callback、refresh/revoke 编排 provider wire protocol 与 callback host 由 adapter/产品拥有 ConnectorDeviceOAuthService device-code attempt、provider interval/slow-down、expiry/cancel 与 authority transition device code 只驻留内存，不进入 protocol、history 或 SecretStore ConnectorOAuthProvider 一个具体服务的授权 URL 与 code exchange 端口 不持久化 secret，不修改 authority GitHubBrokeredOAuthProvider 经产品 broker 执行 PKCE exchange/refresh/revoke client 不持有 GitHub App secret；broker deployment 不属于本 crate GitHubDeviceOAuthProvider GitHub public-client device grant 与账户读取 无 client secret；不声明 GitHub 未提供的 refresh/remote revoke GitHubOAuthProvider confidential direct GitHub adapter 仅供可信 host 显式注入 client secret 内部调用路径 ``text ConnectorCredentialService::connectapitoken - phasecommandid - ConnectorAuthority::apply BeginConnect - command::commanddigest - authority::eventforrequest - SqliteAuthority::persist - SecretStore::store - ConnectorAuthority::apply CompleteConnect ConnectorAuthority::opensqlite - SqliteAuthority::open - loadlatestrecords + loadreceipts - PersistedRecord::restore - ReauthorizationRequired when definition digest changed ` authority::eventforrequest 拥有 command 到 durable event 的绑定；SqliteAuthority::persist 拥有 event/receipt 原子性；auth::credentialkey 只生成 hashed non-PII key。若这些 helper 开始读取 token bytes、 启动 MCP，或 App Server 直接写 SQLite，即表示 ownership 漂移。 当前实现 API-token adapter、browser-code OAuth 的通用 PKCE 状态机与 device grant 状态机。oauth::PendingOAuthAttempt 只在内存保存 flow ID、state 和 verifier；ConnectorOAuthService::complete 一次性消费 callback， 并在过期、state mismatch、provider 或 credential failure 时提交 Unavailable。Desktop loopback listener/browser interaction 已由 Electron main 持有。deviceoauth::PendingDeviceOAuthAttempt 只在内存 保存 device code，并严格执行 provider interval、GitHub slowdown + 5s、expiry 与 cancel。 产品默认可选择 GitHubBrokeredOAuthProvider：client 只发送 PKCE verifier，GitHub App secret 留在发行方 broker；或选择 GitHubDeviceOAuthProvider：仅需 public client ID。旧的 GitHubOAuthProvider 继续作为可信 host 直连 adapter。OAuth secret 以 runtime token 与 lifecycle bundle 分层封装，MCP projection 只能拿到 runtime token。远端 revoke 成功后才提交本地 disconnect；失败时保留 ready connection 以便重试。 不能通过扩张 authority event payload 来保存 OAuth code、refresh token 或 raw credential。生产显式文件 file backend 位于 zeta-secrets，OS keyring adapter 位于独立的 zeta-keyring-store；均不属于本 crate。 验证入口： `bash cargo test -p zeta-connectors-extension cargo clippy -p zeta-connectors-extension --all-targets --no-deps -- -D warnings ``"
  },
  {
    "slug": "crates/ext/mcp",
    "sourcePath": "zeta-rs/ext/mcp/README.md",
    "title": "MCP extension",
    "description": "zeta-mcp-extension 拥有宿主侧 MCP 集成：它把 Config、Connector 和 Plugin authority 投影为 持续运行的 MCP session，把冻结的 MCP 工具适配为 zeta-core::ToolService，并提供审批策略、 生命周期状态、独立 MCP OAuth 编排和表单交互转换。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 `zeta-mcp-extension` 的实现契约；跨 crate 的产品语义、能力状态和演进由\n> [`docs/mcp.md`](../../../docs/mcp.md) 维护。\n\n`zeta-mcp-extension` 拥有宿主侧 MCP 集成：它把 Config、Connector 和 Plugin authority 投影为\n持续运行的 MCP session，把冻结的 MCP 工具适配为 `zeta-core::ToolService`，并提供审批策略、\n生命周期状态、独立 MCP OAuth 编排和表单交互转换。\n\n它不拥有 MCP wire/session 语义（属于 `zeta-mcp` 与 `zeta-rmcp-client`）、SecretStore 实现、\nPlugin 安装 authority、Connector 账号 authority、Core durable Thread 状态或 App Server RPC。\n\n## Crate 边界与模块\n\n| 模块 | 当前职责 | 不得承担 |\n| --- | --- | --- |\n| `composition.rs` | declaration materialization、运行时构造、Core 工具/策略适配、调用 authority fence | OAuth provider wire、Plugin 安装、Core durable event |\n| `runtime.rs` | 在专用 Tokio worker 上拥有 `zeta-mcp` runtime，并桥接同步工具调用 | 产品配置或审批决策 |\n| `connector.rs` | Connector/Plugin server publication 与运行时 invocation fence 契约 | Connector 登录状态机 |\n| `plugin.rs` | 从 exact active package 构造 package-rooted MCP/Connector server | package 下载、grant 或 enablement mutation |\n| `auth.rs`、`auth/*` | 独立 Config MCP 的 PKCE/state、一次性 callback、凭据 envelope、refresh/revoke 编排 | provider discovery、client registration、scope 或 token wire parsing |\n| `elicitation.rs` | MCP form schema 与 Core `RequestUserInput` 的有界双向转换 | UI、durable interaction ownership 或 URL elicitation |\n| `updates.rs` | `tools/list_changed` reconcile hint 与当前工具调用的 task-local interaction binding | 全局 interaction registry |\n| `status.rs` | redacted runtime snapshot 与 process-local connect/disconnect intent | durable desired config |\n\n依赖方向保持为：\n\n```text\nApp Server composition\n  → zeta-mcp-extension\n      → zeta-mcp\n          → zeta-rmcp-client\n      → zeta-core ports\n      → Config / Connector / Plugin / SecretStore contracts\n```\n\n`zeta-mcp` 和 `zeta-rmcp-client` 不得反向依赖本 crate、Core 或 App Server。\n\n## 公共契约\n\n| API | 调用者 | 契约 |\n| --- | --- | --- |\n| `compose_mcp_tools*` | App Server composition | 完整构造一个不可变 generation；失败时不发布半成品 runtime |\n| `McpToolComposition` | App Server tool composition | 同时返回 Tool service、policy、status 和保持 session 存活的 owner |\n| `ConnectorMcpRuntimeProvider` | Connector/Plugin host adapter | 只发布当前 authority 允许的 server definition 与 invocation fence |\n| `PluginConnectorMcpRuntimeProvider` | local Plugin composition | 从 exact activation 和 manifest permission 解析 package-rooted contribution |\n| `McpOAuthProvider` | 具体产品/provider adapter | 拥有 discovery、endpoint allowlist、client identity、scope、token parsing、audience、refresh 与 remote revoke |\n| `McpOAuthService` | App Server OAuth RPC | 只编排 PKCE/state、exact target binding、SecretStore persistence 和 lifecycle operation |\n| `McpRuntimeStatusSnapshot` | App Server status projection | 仅暴露 redacted state、generation、tool count 和安全诊断 |\n\n`McpOAuthProvider` 的实现必须返回不含凭据的稳定错误。把 provider-specific discovery、HTTP token\nresponse 或 client secret 放入 `McpOAuthService`，意味着 provider ownership 已发生漂移。\n\n## 工具组合与调用路径\n\n`compose_mcp_tools` 只处理 enabled user Config declaration；\n`compose_mcp_tools_with_connectors*` 还读取 exact ready Connector snapshot，并把 Plugin-specific\ntransport construction 委托给 `ConnectorMcpRuntimeProvider`。Config、Connector 和 Plugin 的\nserver ID 冲突会失败关闭。\n\n一次调用按以下顺序执行：\n\n1. `McpRuntimeOwner::prepare_call` 把 exact `McpToolBinding` 与 JSON-object arguments 冻结为\n   `McpPreparedCall`。\n2. `McpToolService::review_request` 复核 catalog generation、Connector authority 和 Plugin\n   runtime fence，并生成包含完整来源与参数的动作摘要。\n3. Core 持久化批准或自动审查结果后，`McpToolService::execute_with_optional_interactions` 再次检查\n   authority，并在 dispatch 前获取 `RuntimeInvocationLease`。\n4. `ConnectorAuthority::with_authorized_invocation` 把 exact connector ID、connection generation\n   和 definition digest 线性化到远端调用周围。\n5. worker 用自己持有的不可变 catalog 再校验 binding，随后才进入远端 session。\n\n已经 dispatch 的调用可以在 disconnect 提交后排空；disconnect 必须阻止旧 generation 的新调用。\n删除这两个 fence 中任意一个，或按 live tool name 重新查找 executor，都会破坏 generation binding。\n\n`McpCatalogUpdates` 把 `tools/list_changed` 转为 reconcile hint。App Server 构造完整的新 generation\n后，只在后续 model safe point 原子替换；已经 prepared 的调用继续持有旧 service generation。\n\n## 独立 MCP OAuth\n\nOAuth 只适用于带 credential reference 的、无 URL userinfo/fragment 的 HTTPS Streamable HTTP\nConfig server。`McpOAuthTarget` 绑定 server ID、endpoint 和 SecretKey 的摘要，不读取 secret bytes。\n\n流程如下：\n\n1. `McpOAuthService::start` 校验 HTTPS 或本地 loopback redirect，生成 256-bit state、verifier 和\n   S256 challenge，并要求 provider 返回的 authorization URL 精确包含 state、challenge、redirect\n   和 resource。\n2. `PendingOAuthAttempt` 只保存在进程内，最多 64 个、十分钟过期，drop 时清零 state/verifier。\n3. `complete` 先移除 flow，使 callback 一次性；随后常量时间比较 state，并复核当前 Config target\n   digest，防止浏览器流程期间 server 被换绑。\n4. provider exchange 成功后，`encode_oauth_credential` 把 runtime bearer 与 lifecycle secret 写入\n   私有 versioned envelope；连接 materialization 通过 `project_runtime_credential` 只能取得 bearer。\n5. `refresh` 原子替换 envelope 的两个部分；`revoke` 先完成 provider remote revoke，再删除本地\n   secret。App Server 在调用 revoke 前等待 active Tool generation 移除该 server，因此 provider\n   失败时仍保持失败关闭。\n\n旧的 raw bearer SecretStore value 仍可直接 materialize，以保持既有配置兼容。OAuth envelope 的\n格式是本 crate 私有存储格式，不得进入 Config、protocol DTO 或日志。\n\n`McpOAuthService` 本身不是通用 OAuth discovery client。没有 host 注入的 exact\n`McpOAuthProvider` 时，server 明确返回 unavailable。\n\n## MCP 表单交互\n\n只有在 App Server 为当前 Tool call 提供 `ToolInteractionService` 时，RMCP client 才声明 form\nelicitation。`McpCatalogUpdates` 使用 task-local binding 把并发 MCP call 隔离到各自的\nThread/Turn/ToolCall；不得改成进程级“当前请求”槽位。\n\n`handle_elicitation` 当前接受 string、number、integer、boolean 和单选 enum/oneOf，并保留 exact\nfield ID、required、长度/数值范围。它最多接受 32 个字段、100 个选项和每段 4096 个字符；数组、\n多选、未知 shape，以及名称或说明中含 password/secret/token/credential/API key 标记的字段都会\n被拒绝。`schemaValidation=false` 是刻意的：当前实现不承诺完整 JSON Schema format 校验。\n\n表单被转换为 Core durable `AgentRequest::UserInput`。用户响应唤醒同一个 live Tool execution；\n取消映射为 MCP `Cancel`。同步 durable wait 在 `spawn_blocking` 上运行，不能阻塞 RMCP current-thread\nruntime。没有 active interaction context 或收到 URL elicitation 时，host 返回 `Decline`。\n\n进程重启后 remote MCP request 不可恢复。Core 仍保留 durable interaction，但已 started 且没有\nterminal result 的 MCP Tool call 按未知结果处理，不能在新 connection 上重放旧 request。\n\n## 失败、取消与安全边界\n\n- 调用前取消返回 `CoreError::Cancelled`；已经发出的远端 mutation 若丢失响应则返回 unknown outcome。\n- `NotStarted` 和 invalid result 可以成为普通 tool failure；transport-lost 或无法证明未执行的错误\n  必须保持 unknown outcome。\n- SecretStore 只接收 opaque bytes；OAuth code、state、verifier、bearer 和 refresh material 不进入\n  Debug、status、Config 或 protocol response。\n- Plugin disable/update 先退休 exact package generation，再拒绝新 lease，并等待已获取 lease 的调用\n  排空。\n- Connector disconnect 以 authority lock 线性化；把它弱化为一次 best-effort state read 会产生\n  disconnect 后仍可 dispatch 的竞态。\n\n## 宿主集成\n\nLocal App Server 必须：\n\n- 共享 Connector runtime 使用的同一个 `SecretStore`；\n- 在 runtime 构造前注入 exact OAuth provider registry；\n- 把 `McpRuntimeStatusSnapshot` 和 reconcile subscription 接到 App Server；\n- 只把 process-local connect/disconnect intent 作为 desired config 的 overlay；\n- 在 Plugin/Connector/Config generation 变化时构造完整替代 runtime；\n- 通过 Core interaction port 处理 elicitation，而不是从 MCP worker 直接调用 UI。\n\n## 测试与修改检查\n\n```text\ncargo test -p zeta-mcp-extension --lib\ncargo test -p zeta-mcp --lib\ncargo test -p zeta-rmcp-client --lib\ncargo test -p zeta-app-server --lib\n```\n\n测试覆盖 OAuth PKCE/envelope/target revalidation/refresh/revoke、旧 bearer 兼容、Config/Connector/\nPlugin authority、runtime generation、并发 elicitation task-local 隔离和 App Server durable live wake。\n\n修改时同步检查：\n\n- public RPC 或 error mapping：更新 `zeta-app-server-protocol` registry、schema fixture 与 App Server test；\n- credential envelope：保留旧 bearer 读取兼容，并增加 secret projection/revoke test；\n- form 支持范围：同步更新 capability declaration、local validation 和并发/cancellation test；\n- binding 或 generation：同时检查 review、dispatch fence、prepared-call retention 与 disconnect test；\n- status state：同步更新 App Server DTO projection，不能加入 endpoint、credential 或 provider raw error。\n\n## 当前限制与扩展点\n\n- 当前没有 resources、prompts、roots、sampling 或 tasks 的产品 adapter。\n- 当前没有自动 reconnect/backoff、完整 health state machine 或 runtime log RPC。\n- 当前不拥有通用 MCP OAuth metadata discovery 或 concrete provider；必须由 host 注入 provider adapter。\n- URL elicitation、数组/多选和完整 JSON Schema format validation 尚未支持。\n- stdio 仍需要更完整的 sandbox/process-tree supervisor。\n- progress 可以在低层接收，但尚未投影为产品级 durable/transient update。",
    "headings": [
      {
        "depth": 2,
        "id": "crate-边界与模块",
        "title": "Crate 边界与模块"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "工具组合与调用路径",
        "title": "工具组合与调用路径"
      },
      {
        "depth": 2,
        "id": "独立-mcp-oauth",
        "title": "独立 MCP OAuth"
      },
      {
        "depth": 2,
        "id": "mcp-表单交互",
        "title": "MCP 表单交互"
      },
      {
        "depth": 2,
        "id": "失败取消与安全边界",
        "title": "失败、取消与安全边界"
      },
      {
        "depth": 2,
        "id": "宿主集成",
        "title": "宿主集成"
      },
      {
        "depth": 2,
        "id": "测试与修改检查",
        "title": "测试与修改检查"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 zeta-mcp-extension 的实现契约；跨 crate 的产品语义、能力状态和演进由 docs/mcp.md 维护。 zeta-mcp-extension 拥有宿主侧 MCP 集成：它把 Config、Connector 和 Plugin authority 投影为 持续运行的 MCP session，把冻结的 MCP 工具适配为 zeta-core::ToolService，并提供审批策略、 生命周期状态、独立 MCP OAuth 编排和表单交互转换。 它不拥有 MCP wire/session 语义（属于 zeta-mcp 与 zeta-rmcp-client）、SecretStore 实现、 Plugin 安装 authority、Connector 账号 authority、Core durable Thread 状态或 App Server RPC。 Crate 边界与模块 模块 当前职责 不得承担 --- --- --- composition.rs declaration materialization、运行时构造、Core 工具/策略适配、调用 authority fence OAuth provider wire、Plugin 安装、Core durable event runtime.rs 在专用 Tokio worker 上拥有 zeta-mcp runtime，并桥接同步工具调用 产品配置或审批决策 connector.rs Connector/Plugin server publication 与运行时 invocation fence 契约 Connector 登录状态机 plugin.rs 从 exact active package 构造 package-rooted MCP/Connector server package 下载、grant 或 enablement mutation auth.rs、auth/ 独立 Config MCP 的 PKCE/state、一次性 callback、凭据 envelope、refresh/revoke 编排 provider discovery、client registration、scope 或 token wire parsing elicitation.rs MCP form schema 与 Core RequestUserInput 的有界双向转换 UI、durable interaction ownership 或 URL elicitation updates.rs tools/listchanged reconcile hint 与当前工具调用的 task-local interaction binding 全局 interaction registry status.rs redacted runtime snapshot 与 process-local connect/disconnect intent durable desired config 依赖方向保持为： ``text App Server composition → zeta-mcp-extension → zeta-mcp → zeta-rmcp-client → zeta-core ports → Config / Connector / Plugin / SecretStore contracts ` zeta-mcp 和 zeta-rmcp-client 不得反向依赖本 crate、Core 或 App Server。 公共契约 API 调用者 契约 --- --- --- composemcptools App Server composition 完整构造一个不可变 generation；失败时不发布半成品 runtime McpToolComposition App Server tool composition 同时返回 Tool service、policy、status 和保持 session 存活的 owner ConnectorMcpRuntimeProvider Connector/Plugin host adapter 只发布当前 authority 允许的 server definition 与 invocation fence PluginConnectorMcpRuntimeProvider local Plugin composition 从 exact activation 和 manifest permission 解析 package-rooted contribution McpOAuthProvider 具体产品/provider adapter 拥有 discovery、endpoint allowlist、client identity、scope、token parsing、audience、refresh 与 remote revoke McpOAuthService App Server OAuth RPC 只编排 PKCE/state、exact target binding、SecretStore persistence 和 lifecycle operation McpRuntimeStatusSnapshot App Server status projection 仅暴露 redacted state、generation、tool count 和安全诊断 McpOAuthProvider 的实现必须返回不含凭据的稳定错误。把 provider-specific discovery、HTTP token response 或 client secret 放入 McpOAuthService，意味着 provider ownership 已发生漂移。 工具组合与调用路径 composemcptools 只处理 enabled user Config declaration； composemcptoolswithconnectors 还读取 exact ready Connector snapshot，并把 Plugin-specific transport construction 委托给 ConnectorMcpRuntimeProvider。Config、Connector 和 Plugin 的 server ID 冲突会失败关闭。 一次调用按以下顺序执行： 1. McpRuntimeOwner::preparecall 把 exact McpToolBinding 与 JSON-object arguments 冻结为 McpPreparedCall。 2. McpToolService::reviewrequest 复核 catalog generation、Connector authority 和 Plugin runtime fence，并生成包含完整来源与参数的动作摘要。 3. Core 持久化批准或自动审查结果后，McpToolService::executewithoptionalinteractions 再次检查 authority，并在 dispatch 前获取 RuntimeInvocationLease。 4. ConnectorAuthority::withauthorizedinvocation 把 exact connector ID、connection generation 和 definition digest 线性化到远端调用周围。 5. worker 用自己持有的不可变 catalog 再校验 binding，随后才进入远端 session。 已经 dispatch 的调用可以在 disconnect 提交后排空；disconnect 必须阻止旧 generation 的新调用。 删除这两个 fence 中任意一个，或按 live tool name 重新查找 executor，都会破坏 generation binding。 McpCatalogUpdates 把 tools/listchanged 转为 reconcile hint。App Server 构造完整的新 generation 后，只在后续 model safe point 原子替换；已经 prepared 的调用继续持有旧 service generation。 独立 MCP OAuth OAuth 只适用于带 credential reference 的、无 URL userinfo/fragment 的 HTTPS Streamable HTTP Config server。McpOAuthTarget 绑定 server ID、endpoint 和 SecretKey 的摘要，不读取 secret bytes。 流程如下： 1. McpOAuthService::start 校验 HTTPS 或本地 loopback redirect，生成 256-bit state、verifier 和 S256 challenge，并要求 provider 返回的 authorization URL 精确包含 state、challenge、redirect 和 resource。 2. PendingOAuthAttempt 只保存在进程内，最多 64 个、十分钟过期，drop 时清零 state/verifier。 3. complete 先移除 flow，使 callback 一次性；随后常量时间比较 state，并复核当前 Config target digest，防止浏览器流程期间 server 被换绑。 4. provider exchange 成功后，encodeoauthcredential 把 runtime bearer 与 lifecycle secret 写入 私有 versioned envelope；连接 materialization 通过 projectruntimecredential 只能取得 bearer。 5. refresh 原子替换 envelope 的两个部分；revoke 先完成 provider remote revoke，再删除本地 secret。App Server 在调用 revoke 前等待 active Tool generation 移除该 server，因此 provider 失败时仍保持失败关闭。 旧的 raw bearer SecretStore value 仍可直接 materialize，以保持既有配置兼容。OAuth envelope 的 格式是本 crate 私有存储格式，不得进入 Config、protocol DTO 或日志。 McpOAuthService 本身不是通用 OAuth discovery client。没有 host 注入的 exact McpOAuthProvider 时，server 明确返回 unavailable。 MCP 表单交互 只有在 App Server 为当前 Tool call 提供 ToolInteractionService 时，RMCP client 才声明 form elicitation。McpCatalogUpdates 使用 task-local binding 把并发 MCP call 隔离到各自的 Thread/Turn/ToolCall；不得改成进程级“当前请求”槽位。 handleelicitation 当前接受 string、number、integer、boolean 和单选 enum/oneOf，并保留 exact field ID、required、长度/数值范围。它最多接受 32 个字段、100 个选项和每段 4096 个字符；数组、 多选、未知 shape，以及名称或说明中含 password/secret/token/credential/API key 标记的字段都会 被拒绝。schemaValidation=false 是刻意的：当前实现不承诺完整 JSON Schema format 校验。 表单被转换为 Core durable AgentRequest::UserInput。用户响应唤醒同一个 live Tool execution； 取消映射为 MCP Cancel。同步 durable wait 在 spawnblocking 上运行，不能阻塞 RMCP current-thread runtime。没有 active interaction context 或收到 URL elicitation 时，host 返回 Decline。 进程重启后 remote MCP request 不可恢复。Core 仍保留 durable interaction，但已 started 且没有 terminal result 的 MCP Tool call 按未知结果处理，不能在新 connection 上重放旧 request。 失败、取消与安全边界 - 调用前取消返回 CoreError::Cancelled；已经发出的远端 mutation 若丢失响应则返回 unknown outcome。 - NotStarted 和 invalid result 可以成为普通 tool failure；transport-lost 或无法证明未执行的错误 必须保持 unknown outcome。 - SecretStore 只接收 opaque bytes；OAuth code、state、verifier、bearer 和 refresh material 不进入 Debug、status、Config 或 protocol response。 - Plugin disable/update 先退休 exact package generation，再拒绝新 lease，并等待已获取 lease 的调用 排空。 - Connector disconnect 以 authority lock 线性化；把它弱化为一次 best-effort state read 会产生 disconnect 后仍可 dispatch 的竞态。 宿主集成 Local App Server 必须： - 共享 Connector runtime 使用的同一个 SecretStore； - 在 runtime 构造前注入 exact OAuth provider registry； - 把 McpRuntimeStatusSnapshot 和 reconcile subscription 接到 App Server； - 只把 process-local connect/disconnect intent 作为 desired config 的 overlay； - 在 Plugin/Connector/Config generation 变化时构造完整替代 runtime； - 通过 Core interaction port 处理 elicitation，而不是从 MCP worker 直接调用 UI。 测试与修改检查 `text cargo test -p zeta-mcp-extension --lib cargo test -p zeta-mcp --lib cargo test -p zeta-rmcp-client --lib cargo test -p zeta-app-server --lib ` 测试覆盖 OAuth PKCE/envelope/target revalidation/refresh/revoke、旧 bearer 兼容、Config/Connector/ Plugin authority、runtime generation、并发 elicitation task-local 隔离和 App Server durable live wake。 修改时同步检查： - public RPC 或 error mapping：更新 zeta-app-server-protocol` registry、schema fixture 与 App Server test； - credential envelope：保留旧 bearer 读取兼容，并增加 secret projection/revoke test； - form 支持范围：同步更新 capability declaration、local validation 和并发/cancellation test； - binding 或 generation：同时检查 review、dispatch fence、prepared-call retention 与 disconnect test； - status state：同步更新 App Server DTO projection，不能加入 endpoint、credential 或 provider raw error。 当前限制与扩展点 - 当前没有 resources、prompts、roots、sampling 或 tasks 的产品 adapter。 - 当前没有自动 reconnect/backoff、完整 health state machine 或 runtime log RPC。 - 当前不拥有通用 MCP OAuth metadata discovery 或 concrete provider；必须由 host 注入 provider adapter。 - URL elicitation、数组/多选和完整 JSON Schema format validation 尚未支持。 - stdio 仍需要更完整的 sandbox/process-tree supervisor。 - progress 可以在低层接收，但尚未投影为产品级 durable/transient update。"
  },
  {
    "slug": "crates/utils/typst/licenses",
    "sourcePath": "zeta-rs/utils/typst/licenses/README.md",
    "title": "Typst 第三方许可证文本",
    "description": "本目录保存随 zeta-typst 分发的上游 Typst 组件和资源的许可证及声明文本。它不定义 Zeta 或 自有 zeta-typst 包装层的许可证；后两者仍受仓库根目录 LICENSE 约束。",
    "group": "Crate 实现参考",
    "markdown": "本目录保存随 `zeta-typst` 分发的上游 Typst 组件和资源的许可证及声明文本。它不定义 Zeta 或\n自有 `zeta-typst` 包装层的许可证；后两者仍受仓库根目录 `LICENSE` 约束。\n\n这些文件对应根目录 `Cargo.lock` 固定的 Typst 与 `typst-assets` 版本：\n\n- `Typst.txt`：Typst 的 Apache-2.0 许可证文本；\n- `Typst-NOTICE.txt`：Typst 携带的第三方声明；\n- `Typst-Assets-NOTICE.txt`：内置字体和资源的许可证与声明。\n\n`desktop/licenses/` 保存面向发布的副本。Typst 依赖、功能、字体或资源变化时，必须重新审查\n上游许可证材料，并保持两个位置逐字节一致。",
    "headings": [],
    "searchText": "本目录保存随 zeta-typst 分发的上游 Typst 组件和资源的许可证及声明文本。它不定义 Zeta 或 自有 zeta-typst 包装层的许可证；后两者仍受仓库根目录 LICENSE 约束。 这些文件对应根目录 Cargo.lock 固定的 Typst 与 typst-assets 版本： - Typst.txt：Typst 的 Apache-2.0 许可证文本； - Typst-NOTICE.txt：Typst 携带的第三方声明； - Typst-Assets-NOTICE.txt：内置字体和资源的许可证与声明。 desktop/licenses/ 保存面向发布的副本。Typst 依赖、功能、字体或资源变化时，必须重新审查 上游许可证材料，并保持两个位置逐字节一致。"
  },
  {
    "slug": "crates/ext/web-search",
    "sourcePath": "zeta-rs/ext/web-search/README.md",
    "title": "Web Search extension",
    "description": "zeta-web-search-extension provides the eager web-search agent tool. It validates a bounded, provider-neutral query contract and delegates execution to a host-injected WebSearchBackend.",
    "group": "Crate 实现参考",
    "markdown": "`zeta-web-search-extension` provides the eager `web-search` agent tool. It validates a bounded,\nprovider-neutral query contract and delegates execution to a host-injected `WebSearchBackend`.\n\nInstalling the extension declares exact network and optional credential scopes through the\ncapability-bearing extension API. App Server freezes those scopes into the action digest and asks\nfor one-time external-access approval before execution. With no backend installed, the tool is not\nregistered.\n\n`JsonWebSearchBackend` supports a JSON-over-HTTP service that accepts `WebSearchRequest` and returns\n`WebSearchResponse`. Zeta deliberately does not hard-code Codex's private standalone search\nendpoint; product hosts may bind an OpenAI-compatible gateway, an enterprise search service, or a\nself-hosted adapter.",
    "headings": [],
    "searchText": "zeta-web-search-extension provides the eager web-search agent tool. It validates a bounded, provider-neutral query contract and delegates execution to a host-injected WebSearchBackend. Installing the extension declares exact network and optional credential scopes through the capability-bearing extension API. App Server freezes those scopes into the action digest and asks for one-time external-access approval before execution. With no backend installed, the tool is not registered. JsonWebSearchBackend supports a JSON-over-HTTP service that accepts WebSearchRequest and returns WebSearchResponse. Zeta deliberately does not hard-code Codex's private standalone search endpoint; product hosts may bind an OpenAI-compatible gateway, an enterprise search service, or a self-hosted adapter."
  },
  {
    "slug": "crates/action-policy",
    "sourcePath": "zeta-rs/action-policy/README.md",
    "title": "zeta-action-policy",
    "description": "zeta-action-policy 是执行前最终决策 authority。它消费 immutable ExecPolicySnapshot、已完整 materialize 的 action、sandbox compatibility、exact one-action grants 和 advisory classifier 输出， 产生 typed ExecutionD…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责最终 action authority 的 crate 契约。确定性规则语言、layer merge 和 semantic\n> revision 由 [`zeta-execpolicy`](../execpolicy/README.md) 负责；端到端权限语义见\n> [`docs/permissions.md`](../../docs/permissions.md)，Auto Review 见\n> [`docs/auto-review.md`](../../docs/auto-review.md)。\n\n`zeta-action-policy` 是执行前最终决策 authority。它消费 immutable `ExecPolicySnapshot`、已完整\nmaterialize 的 action、sandbox compatibility、exact one-action grants 和 advisory classifier 输出，\n产生 typed `ExecutionDecision`。它不解析或持久化规则，不执行 Tool，不选择 sandbox backend，也不\n显示 approval UI。\n\n## 公共契约与所有权\n\n| 领域 | 关键类型 | 所有权 |\n| --- | --- | --- |\n| Action identity | `ResolvedAction`, `ActionDigest`, `ActionKind`, `ActionProvenance` | host 负责完整 materialization；缺失 cwd、argv、resolved path 或来源会破坏 grant binding |\n| Capability | `Capability`, `CapabilityKind`, `CapabilitySet` | exact `kind + scope`，canonical ordering |\n| Safe point | `ActionReviewRequest`, `ActionPolicyRevision`, `ActionReviewPhase` | request 与当前 action-policy snapshot 必须 revision 相等 |\n| Deterministic input | `zeta_execpolicy::ExecPolicySnapshot` | 只消费；selector、layer、revision 与 rule source 属于 `zeta-execpolicy` |\n| Exact grants | `DeterministicPolicyGrant`, `UnsandboxedGrant`, `AutoReviewGrant`, `PermissionBypassGrant` | 都绑定 action digest、完整 capabilities 和 action-policy revision |\n| Advisory port | `ActionClassifier`, `ClassifierAssessment`, `ClassifierRecommendation` | classifier 只能建议，不能签发 authority |\n| Final outcome | `ExecutionDecision`, `BlockReason`, `ApprovalRequest`, `SaferActionRequest` | caller 必须按 typed branch durable 记录并执行 |\n\n`ActionPolicyRevision::from_components` 把 exec-policy revision、exact-grant snapshot revision 和\nreviewer policy revision 组合为一个 safe-point identity。只更新其中一项也会使旧 request/grant\n失效。\n\n## 文件与调用关系\n\n```text\nsrc/action.rs      action、capability、provenance、request/revision\nsrc/classifier.rs  advisory classifier contract 与 assessment validation\nsrc/context.rs     trust-labeled review evidence\nsrc/grant.rs       explicit exact user grant\nsrc/grants.rs      exact grant snapshot lookup\nsrc/decision.rs    typed decisions 与 engine-only grants\nsrc/engine.rs      唯一最终 precedence entry\n```\n\n```text\nActionPolicyEngine::decide\n→ verify ActionPolicyRevision\n→ ExecPolicySnapshot::evaluate\n   → Deny              → Block\n   → RequireSandbox    → RunSandboxed，或 unavailable/denial 时 Block\n   → RequireApproval   → AskUser\n   → AllowUnsandboxed  → RunExecPolicyGranted(exact rule + revisions + action binding)\n   → Continue          → continue\n→ exact UserAllowlist match → RunUnsandboxed\n→ initial sandbox-supported action → RunSandboxed\n→ ActionClassifier::classify\n→ validate assessment digest/revision/capability constraints\n→ Auto Review risk/authorization matrix → grant / revise / ask / block\n```\n\n这一顺序是安全 contract：deterministic rule 先于历史 exact grant，sandbox fast path 先于模型审查；\nclassifier failure 按显式 `ReviewFailurePolicy` fail closed 或转人工。\n\n## 关键实现符号\n\n| Symbol | 职责 | 漂移信号 |\n| --- | --- | --- |\n| `ActionPolicyEngine::decide` | 唯一 top-level final decision entry | host 在外部重新实现 precedence |\n| `evaluate_exec_policy` | 把 trusted action fields 投影为 `ExecPolicySubject` | 用 summary 或未 materialize 字符串参与授权 |\n| `apply_exec_policy` | 把纯 effect 映射为 sandbox、approval、block 或 exact grant | `zeta-execpolicy` 自己签发执行 authority |\n| `ensure_revision` | request/engine safe-point equality | mismatch 被降级为普通 Tool failure |\n| `UserAllowlist::matching_grant` | exact digest + capabilities + revision lookup | Tool 名称或 command prefix 被当成 historical grant |\n| `apply_assessment` | classifier identity 与 capability constraints 复检 | 约束只存在于某个 classifier implementation |\n| `automatic_approval_decision` | Auto Review risk/authorization gate | classifier 或外层直接构造 `AutoReviewGrant` |\n| `DeterministicPolicyGrant::matches` | Core execution-time recheck | durable authority 不再绑定 exact action/revision |\n\n## 失败语义\n\n| Condition | Outcome |\n| --- | --- |\n| engine/request revision 不同 | `Err(PolicyError::RevisionMismatch)` |\n| allow effect 缺少 exact rule source | `Err(PolicyError::ExecPolicyAuthorityMissing)` |\n| classifier digest/revision 不匹配 | `Err(PolicyError::ClassifierBindingMismatch)` |\n| require-sandbox 但 action 不支持或已确认 denial | `Block(SandboxRequiredButUnavailable)` |\n| classifier failure | 按 `ReviewFailurePolicy` 返回 `Block` 或 `AskUser` |\n| approve/revise capability 违反约束 | `Block(ReviewFailed)` |\n\n`PolicyError` 是调用与 binding contract 被破坏；`Block` 是合法请求的 policy outcome，两者不能互换。\n\n## 验证与修改影响\n\n```text\ncargo test -p zeta-action-policy\nbazel test //zeta-rs/action-policy:action-policy-unit-tests\n```\n\n测试覆盖 deterministic effect mapping、exact grant binding、sandbox denial、revision mismatch、\nclassifier constraints、风险矩阵和失败策略。新增 effect/decision/capability 时必须同步这些组件：\n包括 `zeta-execpolicy`、Core scheduler/durable authority、App Server materializer、Auto Review schema、\nprotocol contract tests 和权限文档。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约与所有权",
        "title": "公共契约与所有权"
      },
      {
        "depth": 2,
        "id": "文件与调用关系",
        "title": "文件与调用关系"
      },
      {
        "depth": 2,
        "id": "关键实现符号",
        "title": "关键实现符号"
      },
      {
        "depth": 2,
        "id": "失败语义",
        "title": "失败语义"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      }
    ],
    "searchText": " 本 README 负责最终 action authority 的 crate 契约。确定性规则语言、layer merge 和 semantic revision 由 zeta-execpolicy 负责；端到端权限语义见 docs/permissions.md，Auto Review 见 docs/auto-review.md。 zeta-action-policy 是执行前最终决策 authority。它消费 immutable ExecPolicySnapshot、已完整 materialize 的 action、sandbox compatibility、exact one-action grants 和 advisory classifier 输出， 产生 typed ExecutionDecision。它不解析或持久化规则，不执行 Tool，不选择 sandbox backend，也不 显示 approval UI。 公共契约与所有权 领域 关键类型 所有权 --- --- --- Action identity ResolvedAction, ActionDigest, ActionKind, ActionProvenance host 负责完整 materialization；缺失 cwd、argv、resolved path 或来源会破坏 grant binding Capability Capability, CapabilityKind, CapabilitySet exact kind + scope，canonical ordering Safe point ActionReviewRequest, ActionPolicyRevision, ActionReviewPhase request 与当前 action-policy snapshot 必须 revision 相等 Deterministic input zetaexecpolicy::ExecPolicySnapshot 只消费；selector、layer、revision 与 rule source 属于 zeta-execpolicy Exact grants DeterministicPolicyGrant, UnsandboxedGrant, AutoReviewGrant, PermissionBypassGrant 都绑定 action digest、完整 capabilities 和 action-policy revision Advisory port ActionClassifier, ClassifierAssessment, ClassifierRecommendation classifier 只能建议，不能签发 authority Final outcome ExecutionDecision, BlockReason, ApprovalRequest, SaferActionRequest caller 必须按 typed branch durable 记录并执行 ActionPolicyRevision::fromcomponents 把 exec-policy revision、exact-grant snapshot revision 和 reviewer policy revision 组合为一个 safe-point identity。只更新其中一项也会使旧 request/grant 失效。 文件与调用关系 ``text src/action.rs action、capability、provenance、request/revision src/classifier.rs advisory classifier contract 与 assessment validation src/context.rs trust-labeled review evidence src/grant.rs explicit exact user grant src/grants.rs exact grant snapshot lookup src/decision.rs typed decisions 与 engine-only grants src/engine.rs 唯一最终 precedence entry ` `text ActionPolicyEngine::decide → verify ActionPolicyRevision → ExecPolicySnapshot::evaluate → Deny → Block → RequireSandbox → RunSandboxed，或 unavailable/denial 时 Block → RequireApproval → AskUser → AllowUnsandboxed → RunExecPolicyGranted exact rule + revisions + action binding → Continue → continue → exact UserAllowlist match → RunUnsandboxed → initial sandbox-supported action → RunSandboxed → ActionClassifier::classify → validate assessment digest/revision/capability constraints → Auto Review risk/authorization matrix → grant / revise / ask / block ` 这一顺序是安全 contract：deterministic rule 先于历史 exact grant，sandbox fast path 先于模型审查； classifier failure 按显式 ReviewFailurePolicy fail closed 或转人工。 关键实现符号 Symbol 职责 漂移信号 --- --- --- ActionPolicyEngine::decide 唯一 top-level final decision entry host 在外部重新实现 precedence evaluateexecpolicy 把 trusted action fields 投影为 ExecPolicySubject 用 summary 或未 materialize 字符串参与授权 applyexecpolicy 把纯 effect 映射为 sandbox、approval、block 或 exact grant zeta-execpolicy 自己签发执行 authority ensurerevision request/engine safe-point equality mismatch 被降级为普通 Tool failure UserAllowlist::matchinggrant exact digest + capabilities + revision lookup Tool 名称或 command prefix 被当成 historical grant applyassessment classifier identity 与 capability constraints 复检 约束只存在于某个 classifier implementation automaticapprovaldecision Auto Review risk/authorization gate classifier 或外层直接构造 AutoReviewGrant DeterministicPolicyGrant::matches Core execution-time recheck durable authority 不再绑定 exact action/revision 失败语义 Condition Outcome --- --- engine/request revision 不同 Err PolicyError::RevisionMismatch allow effect 缺少 exact rule source Err PolicyError::ExecPolicyAuthorityMissing classifier digest/revision 不匹配 Err PolicyError::ClassifierBindingMismatch require-sandbox 但 action 不支持或已确认 denial Block SandboxRequiredButUnavailable classifier failure 按 ReviewFailurePolicy 返回 Block 或 AskUser approve/revise capability 违反约束 Block ReviewFailed PolicyError 是调用与 binding contract 被破坏；Block 是合法请求的 policy outcome，两者不能互换。 验证与修改影响 `text cargo test -p zeta-action-policy bazel test //zeta-rs/action-policy:action-policy-unit-tests ` 测试覆盖 deterministic effect mapping、exact grant binding、sandbox denial、revision mismatch、 classifier constraints、风险矩阵和失败策略。新增 effect/decision/capability 时必须同步这些组件： 包括 zeta-execpolicy`、Core scheduler/durable authority、App Server materializer、Auto Review schema、 protocol contract tests 和权限文档。"
  },
  {
    "slug": "crates/add-dir",
    "sourcePath": "zeta-rs/add-dir/README.md",
    "title": "zeta-add-dir",
    "description": "zeta-add-dir 把“当前项目是什么”与“还允许访问哪些目录”分开。一个 DirectoryAccessScope 始终只有一个主工作目录；AdditionalDirectory 只扩大文件访问，不会 成为第二个项目或自动获得完整项目配置语义。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有主工作目录之外的持续目录访问作用域、来源生命周期和配置贡献策略的实现契约。\n> 跨 crate 产品语义由\n> [`docs/workspace-security.md`](../../docs/workspace-security.md#工作目录附加目录与-cd) 维护；\n> 单 root identity 与 trust token 由\n> [`zeta-rs/workspace/README.md`](../workspace/README.md) 维护；Instructions/Skills/Agents artifact 与\n> Import/source registration 的边界由\n> [`docs/agent-customizations.md`](../../docs/agent-customizations.md) 维护。\n\n`zeta-add-dir` 把“当前项目是什么”与“还允许访问哪些目录”分开。一个\n`DirectoryAccessScope` 始终只有一个主工作目录；`AdditionalDirectory` 只扩大文件访问，不会\n成为第二个项目或自动获得完整项目配置语义。\n\n## 快速理解\n\n| 输入来源 | 文件访问 | 配置贡献 | 生命周期 |\n| --- | --- | --- | --- |\n| `LaunchArgument` | 主目录 + 附加目录 | allowlisted project contributions | 本次启动 |\n| `SessionCommand` | 主目录 + 附加目录 | allowlisted project contributions | 当前会话 |\n| `PersistentConfiguration` | 主目录 + 附加目录 | ❌，`FileAccessOnly` | Config declaration 有效期间 |\n\nHost compatibility 可以把 transient source 的 instruction contribution 从 `Exclude` 切到\n`Include`；它不改变目录访问范围。多个 source 可以同时保留同一个 canonical root，移除其中一个\nsource 不会撤销其他 source 仍然提供的访问。\n\n## 所有权与非职责\n\n当前 crate 负责：\n\n- 主工作目录与附加目录的角色分离；\n- canonical root identity 去重和稳定 path ordering；\n- 同一 root 的多个 `AdditionalDirectorySource` 生命周期；\n- idempotent add/remove mutation result；\n- 从 active source 与 `AdditionalInstructionsPolicy` 解析 contribution policy；\n- Skills、Agent definitions、Plugin declaration 与 instruction file 的精确 allowlist。\n\n当前 crate 不负责：\n\n- directory picker、CLI flag、slash command、RPC 或 Renderer UI；\n- Config TOML schema、persistence revision 或 source precedence；\n- host trust decision、macOS privacy consent、sandbox profile 或 OS file handle；\n- Files/Search runtime rebuild、Watcher registration 或 Terminal cwd；\n- `zeta-agent-import` 的外部布局发现、preview、apply 或 migration。\n\n如果本 crate 开始依赖 App Server、Config、Search、Agent Import、Desktop 或 protocol DTO，说明\n领域边界已经漂移。上述 crate 只能依赖这里的纯 scope contract，并在自己的 adapter 中完成执行。\n\n## 公共契约\n\n| Symbol | 调用方责任 | crate 保证 |\n| --- | --- | --- |\n| `DirectoryAccessScope::new` | 传入 host 已建立的主 `WorkspaceRoot` | 创建只含主目录的 scope |\n| `add_directory` | 传入 canonical root 与 named source | 拒绝主目录、按 canonical identity 去重、保留多 source |\n| `remove_directory` | 指定 exact root 与 source | 只释放该 source，最后一个 source 消失时移除目录 |\n| `AdditionalDirectory::sources` | 检查 active lifetime | stable sorted source set |\n| `contribution_policy` | 显式选择 instruction compatibility policy | persistent source 始终 file-access-only |\n| `DirectoryScopeMutation` | 解释 idempotent mutation | 不用 ambiguous `bool` 表示变化 |\n\n`AdditionalDirectorySource` 不能被压缩为“persistent”布尔值。`LaunchArgument` 与\n`SessionCommand` 当前共享 contribution allowlist，但生命周期和撤销事件不同，必须保留 exact\nsource。相同 root 同时来自 session 与 Config 时，session source 允许临时 contribution；session\n撤销后，Config source 继续保留文件访问，但 contribution policy 退回 `FileAccessOnly`。\n\n## 配置贡献\n\n| Policy | 允许的 contribution |\n| --- | --- |\n| `FileAccessOnly` | 无 |\n| `AllowlistedProjectContributions` | Skills、Agent definitions、`enabledPlugins`、`extraKnownMarketplaces` |\n| `AllowlistedProjectContributionsWithInstructions` | 上述内容，加 project/local instruction 与 instruction rules |\n\nPolicy 描述“允许发现什么”，不是“已经加载、批准或执行”。Skill manager、Plugin authority 和\nAgent Import 仍需执行各自 validation、trust 与 activation contract。\n\n## 真实调用关系\n\n```text\nhost-selected working directory\n  → WorkspaceRoot::open\n  → DirectoryAccessScope::new\n\nhost-validated add-dir intent\n  → WorkspaceRoot::open\n  → DirectoryAccessScope::add_directory(root, source)\n  → AdditionalDirectory::contribution_policy(instructions)\n  → App Server adapters rebuild Files / Search / contribution projections\n```\n\n`/cd` 不调用 `add_directory`。Host 应建立新的主 `WorkspaceRoot` 和新的\n`DirectoryAccessScope`，再按产品生命周期规则决定哪些附加 source 仍然有效。\n\n## 失败与撤销\n\n- 主工作目录再次作为附加目录加入时，返回\n  `DirectoryScopeError::WorkingDirectoryCannotBeAdditional`。\n- 同一 canonical root 与 source 重复添加时，返回 `AlreadyPresent`。\n- 同一 canonical root 增加第二个 source 时，返回 `AddedSource`。\n- 删除不存在的 root/source 时，返回 `NotPresent`。\n- 删除一个 source 后仍有其他 source 时，返回 `RemovedSource`；最后一个 source 消失时返回\n  `RemovedDirectory`。\n\n本 crate 的 mutation 不执行 I/O，也不证明 runtime 已成功切换。App Server 必须先完成 trust、\nwatcher 和 consumer preparation，再原子发布新的 effective scope；失败时保留旧 scope。\n\n## 验证与修改影响\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-add-dir\nbazel test //zeta-rs/add-dir:add-dir-unit-tests\n```\n\n`scope_tests.rs` 覆盖主目录拒绝、canonical alias 去重、多 source retention、\nfile-access-only Config policy、transient allowlist 和逐 source revocation。修改 source、\ncontribution 或 mutation semantics 时，必须同步检查 Workspace security、Search root-qualified\nresult、Agent Import adapter 与未来 Config/RPC schema。\n\n## 当前限制\n\n当前 crate 只有纯领域模型，尚未接入 App Server、Config、Files、Search、CLI 或 Desktop。\n`add-dir`、`/cd` 与 `additionalDirectories` 都不是当前可用产品功能。OS privacy denial、\nroot-qualified URI、跨启动 restoration 和 runtime rollback contract 仍需在各 authority owner\n中实现。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与非职责",
        "title": "所有权与非职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "配置贡献",
        "title": "配置贡献"
      },
      {
        "depth": 2,
        "id": "真实调用关系",
        "title": "真实调用关系"
      },
      {
        "depth": 2,
        "id": "失败与撤销",
        "title": "失败与撤销"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制",
        "title": "当前限制"
      }
    ],
    "searchText": " 本 README 拥有主工作目录之外的持续目录访问作用域、来源生命周期和配置贡献策略的实现契约。 跨 crate 产品语义由 docs/workspace-security.md 维护； 单 root identity 与 trust token 由 zeta-rs/workspace/README.md 维护；Instructions/Skills/Agents artifact 与 Import/source registration 的边界由 docs/agent-customizations.md 维护。 zeta-add-dir 把“当前项目是什么”与“还允许访问哪些目录”分开。一个 DirectoryAccessScope 始终只有一个主工作目录；AdditionalDirectory 只扩大文件访问，不会 成为第二个项目或自动获得完整项目配置语义。 快速理解 输入来源 文件访问 配置贡献 生命周期 --- --- --- --- LaunchArgument 主目录 + 附加目录 allowlisted project contributions 本次启动 SessionCommand 主目录 + 附加目录 allowlisted project contributions 当前会话 PersistentConfiguration 主目录 + 附加目录 ❌，FileAccessOnly Config declaration 有效期间 Host compatibility 可以把 transient source 的 instruction contribution 从 Exclude 切到 Include；它不改变目录访问范围。多个 source 可以同时保留同一个 canonical root，移除其中一个 source 不会撤销其他 source 仍然提供的访问。 所有权与非职责 当前 crate 负责： - 主工作目录与附加目录的角色分离； - canonical root identity 去重和稳定 path ordering； - 同一 root 的多个 AdditionalDirectorySource 生命周期； - idempotent add/remove mutation result； - 从 active source 与 AdditionalInstructionsPolicy 解析 contribution policy； - Skills、Agent definitions、Plugin declaration 与 instruction file 的精确 allowlist。 当前 crate 不负责： - directory picker、CLI flag、slash command、RPC 或 Renderer UI； - Config TOML schema、persistence revision 或 source precedence； - host trust decision、macOS privacy consent、sandbox profile 或 OS file handle； - Files/Search runtime rebuild、Watcher registration 或 Terminal cwd； - zeta-agent-import 的外部布局发现、preview、apply 或 migration。 如果本 crate 开始依赖 App Server、Config、Search、Agent Import、Desktop 或 protocol DTO，说明 领域边界已经漂移。上述 crate 只能依赖这里的纯 scope contract，并在自己的 adapter 中完成执行。 公共契约 Symbol 调用方责任 crate 保证 --- --- --- DirectoryAccessScope::new 传入 host 已建立的主 WorkspaceRoot 创建只含主目录的 scope adddirectory 传入 canonical root 与 named source 拒绝主目录、按 canonical identity 去重、保留多 source removedirectory 指定 exact root 与 source 只释放该 source，最后一个 source 消失时移除目录 AdditionalDirectory::sources 检查 active lifetime stable sorted source set contributionpolicy 显式选择 instruction compatibility policy persistent source 始终 file-access-only DirectoryScopeMutation 解释 idempotent mutation 不用 ambiguous bool 表示变化 AdditionalDirectorySource 不能被压缩为“persistent”布尔值。LaunchArgument 与 SessionCommand 当前共享 contribution allowlist，但生命周期和撤销事件不同，必须保留 exact source。相同 root 同时来自 session 与 Config 时，session source 允许临时 contribution；session 撤销后，Config source 继续保留文件访问，但 contribution policy 退回 FileAccessOnly。 配置贡献 Policy 允许的 contribution --- --- FileAccessOnly 无 AllowlistedProjectContributions Skills、Agent definitions、enabledPlugins、extraKnownMarketplaces AllowlistedProjectContributionsWithInstructions 上述内容，加 project/local instruction 与 instruction rules Policy 描述“允许发现什么”，不是“已经加载、批准或执行”。Skill manager、Plugin authority 和 Agent Import 仍需执行各自 validation、trust 与 activation contract。 真实调用关系 ``text host-selected working directory → WorkspaceRoot::open → DirectoryAccessScope::new host-validated add-dir intent → WorkspaceRoot::open → DirectoryAccessScope::adddirectory root, source → AdditionalDirectory::contributionpolicy instructions → App Server adapters rebuild Files / Search / contribution projections ` /cd 不调用 adddirectory。Host 应建立新的主 WorkspaceRoot 和新的 DirectoryAccessScope，再按产品生命周期规则决定哪些附加 source 仍然有效。 失败与撤销 - 主工作目录再次作为附加目录加入时，返回 DirectoryScopeError::WorkingDirectoryCannotBeAdditional。 - 同一 canonical root 与 source 重复添加时，返回 AlreadyPresent。 - 同一 canonical root 增加第二个 source 时，返回 AddedSource。 - 删除不存在的 root/source 时，返回 NotPresent。 - 删除一个 source 后仍有其他 source 时，返回 RemovedSource；最后一个 source 消失时返回 RemovedDirectory。 本 crate 的 mutation 不执行 I/O，也不证明 runtime 已成功切换。App Server 必须先完成 trust、 watcher 和 consumer preparation，再原子发布新的 effective scope；失败时保留旧 scope。 验证与修改影响 `bash cargo test --manifest-path Cargo.toml -p zeta-add-dir bazel test //zeta-rs/add-dir:add-dir-unit-tests ` scopetests.rs 覆盖主目录拒绝、canonical alias 去重、多 source retention、 file-access-only Config policy、transient allowlist 和逐 source revocation。修改 source、 contribution 或 mutation semantics 时，必须同步检查 Workspace security、Search root-qualified result、Agent Import adapter 与未来 Config/RPC schema。 当前限制 当前 crate 只有纯领域模型，尚未接入 App Server、Config、Files、Search、CLI 或 Desktop。 add-dir、/cd 与 additionalDirectories` 都不是当前可用产品功能。OS privacy denial、 root-qualified URI、跨启动 restoration 和 runtime rollback contract 仍需在各 authority owner 中实现。"
  },
  {
    "slug": "crates/agent-import",
    "sourcePath": "zeta-rs/agent-import/README.md",
    "title": "zeta-agent-import",
    "description": "zeta-agent-import 只识别 Codex 和 Claude 已知的用户级、项目级配置位置，并生成仅元数据 （metadata-only）的 AgentPathInspection。当前实现不读取候选正文、不递归扫描未知目录、不修改 Zeta 配置，也不导入认证、会话、日志或历史记录。",
    "group": "Crate 实现参考",
    "markdown": "> 文档所有权：本 README 是外部 Agent 配置只读发现、路径校验、导入计划与失败语义的当前实现\n> 契约。\n>\n> 产品交互与长期演进由\n> [`docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md#22-外部-agent-配置导入仅限-desktop)\n> 拥有；Zeta 原生 Instructions/Skills/Agents、`.zeta` 命名空间和 Import/source registration\n> 边界由 [`docs/agent-customizations.md`](../../docs/agent-customizations.md) 拥有；Skill 来源与激活\n> 语义由 [`docs/skills.md`](../../docs/skills.md) 拥有。\n\n`zeta-agent-import` 只识别 Codex 和 Claude 已知的用户级、项目级配置位置，并生成仅元数据\n（metadata-only）的 `AgentPathInspection`。当前实现不读取候选正文、不递归扫描未知目录、不修改\nZeta 配置，也不导入认证、会话、日志或历史记录。\n\n## 1. Crate 边界与依赖方向\n\n本 crate 拥有：\n\n- 外部 Agent、用户/项目作用范围和导入条目类型；\n- Codex/Claude 已知相对路径与预期文件类型；\n- 调用方所选根目录的规范化和目录包含关系校验；\n- 确定性排序、去重、候选与安全诊断；\n- 私有绝对路径的 `Debug` 隐藏。\n\n本 crate 不拥有：\n\n- Desktop 目录选择、预览、确认、进度或撤销界面；\n- App Server 方法、配置 revision 或导入应用流程；\n- Config、Skill、MCP、Plugin 或 Multi-Agent 内容 schema；\n- 内容信任、工具批准、脚本执行、网络连接或凭据解析；\n- `CODEX_HOME`、`CLAUDE_CONFIG_DIR` 等宿主环境解析；\n- `.zeta/{instructions,skills,agents}` 的原生发现、加载或 runtime activation。\n\n生产代码依赖 [`zeta-utils-path`](../utils/path-utils/README.md) 的 host canonical containment\nprimitive，测试使用 workspace `tempfile`。它不得反向依赖\n`zeta-config`、`zeta-skills`、App Server、Core 或 Desktop。后续内容 parser 可以依赖窄格式\ncrate，但不能为了落库或 UI 方便引入上述高层领域。\n\n## 2. 公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `ExternalAgent` | 区分 `Codex` 与 `Claude` 布局 | 动态 provider registry |\n| `ImportScope` | 区分 `User` 与 `Project` 来源 | workspace trust 或配置优先级 |\n| `AgentImportLocation::{codex_user,codex_project,claude_user,claude_project}` | 将来源、作用范围和调用方选择的根目录绑定为一个发现输入 | 环境变量解析、文件访问授权 |\n| `inspect_agent_paths` | 校验所有输入根并检查已知相对位置 | 读取正文、转换内容或应用配置 |\n| `AgentImportCandidate` | 返回来源、作用范围、条目类型、审查类别、相对路径和 canonical host path | 表示内容已受信任、已批准或可执行 |\n| `AgentImportDiagnostic` | 说明一个已存在的已知路径为什么未进入候选 | 暴露根目录、正文或凭据 |\n| `AgentImportError` | 表达调用方选择的根目录整体无效 | 表达单个候选的隔离错误 |\n\n典型调用点保持自解释，不传递布尔模式或裸来源字符串：\n\n```rust\nuse zeta_agent_import::{AgentImportLocation, inspect_agent_paths};\n\nlet inspection = inspect_agent_paths([\n    AgentImportLocation::codex_user(user_home),\n    AgentImportLocation::claude_project(project_root),\n])?;\n```\n\n`AgentPathInspection` 只是检查结果和预览输入。调用方仍须让用户选择具体候选，并把确认结果交给相应领域重新\n解析和校验；不能把 `candidates()` 非空解释为可自动导入。\n\n## 3. 已知布局与审查分类\n\n`agent_paths.rs` 是外部 Agent 路径映射的唯一实现 owner。当前固定布局如下：\n\n| 来源 | 用户级路径 | 项目级路径 |\n| --- | --- | --- |\n| Codex | `~/.codex/{AGENTS.md,AGENTS.override.md,config.toml,agents/,rules/}`、`~/.agents/skills/` | `{AGENTS.md,AGENTS.override.md}`、`.codex/{config.toml,agents/,rules/}`、`.agents/skills/` |\n| Claude | `~/.claude/{CLAUDE.md,settings.json,skills/,agents/,rules/}` | `{CLAUDE.md,CLAUDE.local.md,.mcp.json}`、`.claude/{CLAUDE.md,settings.json,settings.local.json,skills/,agents/,rules/}` |\n\n路径映射依据 Codex 的\n[导入说明](https://learn.chatgpt.com/docs/import)和\n[Skill 位置说明](https://learn.chatgpt.com/docs/build-skills)，以及 Claude 的\n[设置位置说明](https://code.claude.com/docs/en/settings)和\n[Skill 位置说明](https://code.claude.com/docs/en/skills)。外部产品改变路径时，先更新\n官方契约证据和 fixture，再修改 `agent_paths.rs`；不能靠扫描整个 home 猜测新位置。\n\nClaude 的 legacy `.claude/commands/` 明确不在 Import surface 中。Zeta 当前没有接收外部 command\nbody、source provenance、enablement 与执行语义的产品契约；发现这些文件只会产生无法消费的候选项。\n\n| `ImportItemKind` | `ImportReviewCategory` | 原因 |\n| --- | --- | --- |\n| `Instructions`、`Skills`、`InstructionRules` | `Content` | 包含将进入模型上下文的外部指令 |\n| `Settings`、`Agents` | `Configuration` | 需要目标领域映射，不能按原格式直接生效 |\n| `McpServers` | `Connection` | 可能引入进程、网络、header 或重新登录要求 |\n| `ExecutionRules` | `ExecutionPolicy` | 可能改变命令是否提示、允许或阻止 |\n\n审查类别只用于 UI 分组和后续处理路由，不是授权结果。特别是 `ExecutionPolicy` 不能生成 Zeta\n长期批准，`Connection` 不能自动启动 MCP。\n\n`~/.codex/auth.json` 永不进入候选。`~/.claude.json` 同时包含 OAuth session、MCP、\nper-project state 和 cache，当前整体排除。未来若需要其中的非敏感 MCP 声明，必须增加专门的\n有界 parser 与结构化脱敏，不能把整个文件交给 Desktop、模型或普通日志。\n\n## 4. 文件与内部所有权\n\n| 文件 / private symbol | 单一职责 | 修改时同步检查 |\n| --- | --- | --- |\n| `import.rs` | 公共导入值类型、named constructor、getter 和私有路径 `Debug` | App Server DTO、Desktop preview、隐私测试 |\n| `agent_paths.rs::paths_for` | `ExternalAgent + ImportScope` 到固定 `AgentPath` 的穷尽映射 | 官方路径、review category、fixture 和本表 |\n| `agent_paths.rs::{file,directory}` | 构造带预期 entry 类型的 Agent 路径 | type-mismatch diagnostic |\n| `inspect_path.rs::inspect_agent_paths` | 逐 root 检查 Agent 路径、排序、去重并构造 immutable inspection | 多 root 失败语义和顺序测试 |\n| `inspect_path.rs::validate_import_root` | 拒绝不可用、非目录或 symlink root，并建立 `CanonicalPathRoot` | `AgentImportError` 与错误脱敏 |\n| `inspect_path.rs::inspect_path` | 检查一个 Agent 相对路径的 metadata、类型和 symlink，再委托通用 canonical containment | diagnostic code、候选 canonical path |\n| `error.rs` | 根目录级类型化错误与不含绝对路径的显示文本 | Desktop 错误映射与日志 |\n| `inspect_path_tests.rs` | 临时目录上的路径检查与隐私回归 | 新来源、路径、错误或 redaction |\n\n`lib.rs` 保持私有模块和显式 re-export。若调用方开始依赖 `agent_paths`/`inspect_path` 私有函数，或 crate\nroot 重新实现路径判断，说明公共 API 或 ownership 已经漂移。\n\n## 5. 真实调用路径\n\n```text\ninspect_agent_paths\n  → validate_import_root\n      → symlink_metadata(root)\n      → reject symlink / non-directory\n      → CanonicalPathRoot::new(root)\n  → agent_paths::paths_for(agent, scope)\n  → inspect_path for each fixed relative path\n      → missing: omit\n      → symlink_metadata(candidate)\n      → expected file/directory check\n      → CanonicalPathRoot::canonicalize_within(candidate)\n      → AgentImportCandidate | AgentImportDiagnostic\n  → sort + dedup candidates and diagnostics\n  → AgentPathInspection::new\n```\n\n发现只访问根目录和静态 `AgentPath` 指向的 metadata。它不会枚举 `.codex`、`.claude` 或\nSkill 目录的子项，也不会打开文件，因此当前 inspection 不能证明正文有效。\n\n## 6. 失败、隔离与隐私语义\n\n| 条件 | 结果 | 是否继续其他候选 |\n| --- | --- | --- |\n| 输入 root 不可访问 | `AgentImportError::RootUnavailable` | ❌ 整个调用失败 |\n| 输入 root 不是目录 | `AgentImportError::RootNotDirectory` | ❌ 整个调用失败 |\n| 输入 root 本身是 symlink | `AgentImportError::RootSymlinkNotAllowed` | ❌ 整个调用失败 |\n| 已知相对路径不存在 | 不产生候选或 diagnostic | ✅ |\n| 候选 metadata/canonicalize 失败 | `MetadataUnavailable` | ✅ |\n| 候选类型与 specification 不符 | `UnexpectedFileType` | ✅ |\n| 候选自身是 symlink | `SymlinkNotAllowed` | ✅ |\n| ancestor symlink 使 canonical candidate 逃出 root | `EscapesSelectedRoot` | ✅ |\n| 候选合法 | 保存 canonical path | ✅ |\n\n多 root 调用不是部分成功协议：任一输入 root 无效都会返回 `Err`，不会返回其他 root 的半份 inspection。\n单候选问题则通过 diagnostic 隔离，不影响同一 root 的其他候选。\n\n`AgentImportLocation` 和 `AgentImportCandidate` 的 `Debug` 隐藏绝对路径，错误和 diagnostic 只携带\n来源、作用范围、固定相对路径或 `io::ErrorKind`。但 `AgentImportCandidate::source_path()` 会\n显式返回 canonical host path，供可信 host adapter 后续读取；调用方不得把该值放入普通 telemetry、\nThread history、模型上下文或未脱敏错误。\n\n## 7. Host 接入义务\n\nDesktop/App Server 接入时必须：\n\n1. 只从用户明确选择或产品已定义的根目录构造 `AgentImportLocation`；\n2. 在受信 host 侧调用 `discover`，不接受 Renderer 提交任意候选路径；\n3. 用 `agent + scope + kind + relative path` 展示候选，默认不显示完整 home path；\n4. 让用户按条目确认，并将 review category 对应的风险和后续动作分开说明；\n5. 在应用前由目标领域重新打开、限制大小、解析、校验并生成自己的 typed mutation；\n6. 对读取与应用之间的文件变化重新校验 identity/digest，不能把 preview 当作冻结内容；\n7. 认证、凭据、连接批准和执行批准继续走各自 authority。\n\n当前没有 App Server DTO 或 inspection identity。任何 wire contract 都应传 source-qualified identity\n和受控相对路径，不应直接把 `source_path()` 序列化给 Renderer。\n\n### 7.1 应用到目标权威\n\nImport 的最终目的不是让 Zeta runtime 直接解释外部格式，而是把用户选中的内容转换成目标\nauthority 能重新校验的 typed fragment。App Server adapter 根据条目路由到 Config、Instruction、\nSkill、Agent 或其他 authority；本 crate 不拥有这些领域的 canonical schema。依赖方向保持为：\n\n```text\nexternal source\n  → zeta-agent-import normalized preview fragment\n  → App Server import adapter\n  → target authority typed command / prepare-publish contract\n```\n\n`zeta-agent-import` 不依赖 `zeta-config`，也不直接构造 `UserConfigCommand`。App Server adapter 同时\n依赖两者，负责 source-specific conversion、conflict preview、用户逐项确认和 Config transaction。\n这样外部格式变化不会反向决定 Zeta Config schema，inspection 成功也不会被误当成 configuration\ncommit。\n\n| Import item | Zeta apply target | 当前状态与安全边界 |\n| --- | --- | --- |\n| `Skills` | `AddSkillSource` / Skill source authority | Config command 已有；仍需 parser、digest、conflict 与 source identity |\n| `McpServers` | `UpsertMcpServer` | Config command 已有；导入时默认不连接，credential 必须剥离并单独绑定 |\n| Settings 内的 Plugin request | `UpsertPluginRequest` | Config command 已有；只接受可解析的 exact package/version request，不代表安装或激活 |\n| Settings 内的 Hook | `UpsertHook` | Config command 已有；导入后保持 disabled，执行仍需 trust、policy、approval 与 sandbox |\n| `Instructions`、`InstructionRules` | Zeta Instruction authority | 目标模型尚未完成，不能把原始文件塞入普通 Config |\n| `Agents` | Zeta Agent definition authority | 目标模型尚未完成 |\n| `Settings` 其他字段 | 对应 Zeta typed field-by-field mapping | 不支持项必须显示为 skipped/unsupported，禁止 raw passthrough |\n| `ExecutionRules` | Policy migration review | 不能生成长期 approval，也不能自动转换为 Hook |\n\n一次 Import 可能同时修改 Skill、MCP、Plugin 与 Hook section，因此 apply 必须先构造完整 plan。\nConfig 子批次使用 expected-revision 约束：\n重新校验 source identity/digest，验证全部 typed mutation，成功时一次推进 Config revision，任一项\n失败则不提交任何 Config 项。`Instructions` 与 `Agents` 等非 Config target 尚未完成；在对应\nauthority 可以 prepare/publish 前，它们必须保持 unsupported，不能伪装成同一 Config transaction。\n当前 Config 只有逐 command mutation；atomic import batch、跨 authority prepare/publish、import\nreceipt、provenance 与 remove/rollback contract 尚未实现。\n\n`zeta-add-dir` 与 Import workflow 是两条不同的 host path。前者授予附加目录的持续文件访问，并可能\n按 directory origin 临时投影 allowlisted Skills、Agent definitions 或 Plugin declaration；后者让用户\n预览、选择并迁移外部 Agent 配置，不授予持续文件访问。未来两条路径可以复用本 crate 的\nsource-specific inspection/parser，但不能复用 authority、lifecycle 或 apply decision。持久\n`additionalDirectories` 是 file-access-only，不能仅因目录可访问就调用本 crate 自动发现配置。\n本 crate 不依赖 `zeta-add-dir`；App Server adapter 负责把 allowlisted inspection projection\n映射到对应的 contribution policy。\n\n原生加载是第四条独立路径：Workspace `.zeta/{instructions,skills,agents}`、Zeta user root、\nbuilt-in resources 和 Plugin contribution 由各目标 authority 直接发现，不经过本 crate。若\nInstruction/Skill/Agent loader 开始扫描 `.codex`、`.agents` 或 `.claude`，或者本 crate 开始加载\n`.zeta` 并构造 runtime snapshot，都表示 compatibility boundary 已经漂移。\n\nImport 与 source registration 也不能画等号。Import 生成由 Zeta authority 独立管理的 artifact；\nsource registration 保存一个可撤销的窄只读来源，当前只对 standalone Skill source 具备 Config\ndesired-state 基础。两者可以复用 discovery/parser，但由目标 authority 决定 persistence、refresh\n和 removal semantics。\n\n## 8. 常见修改及影响面\n\n### 增加一个已知路径\n\n1. 在对应 `agent_paths.rs` source/scope 数组增加 `AgentPath`；\n2. 选择准确的 `ImportItemKind`、`ImportReviewCategory` 和 expected file/directory；\n3. 增加存在、缺失、错误类型和敏感排除测试；\n4. 更新本 README 的布局表，并检查 Desktop/Skill/权限文档是否受影响。\n\n### 增加一种外部 Agent\n\n1. 扩展 `ExternalAgent` 和 named `AgentImportLocation` constructor；\n2. 为 User/Project 分别定义静态已知路径，保持 `paths_for` 穷尽匹配；\n3. 增加官方路径证据、完整 fixture 和 redaction 测试；\n4. 定义目标领域映射前只返回 preview candidate，不增加隐式 apply；\n5. 同步 App Server DTO、Desktop picker 和系统文档。\n\n### 增加内容 parser\n\nparser 必须位于新的 private module，设置 byte/entry/depth 上限，拒绝未知 active content，并输出\n不含 secret 的 typed fragment。它不能在解析阶段写 Config、连接 MCP、执行 hook/script 或生成\n批准。若单个模块接近 500 LoC，应按来源或内容类型拆分并把测试放到 sibling `*_tests.rs`。\n\n## 9. 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-agent-import\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-agent-import --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/agent-import:agent-import-unit-tests\n```\n\n当前测试覆盖：\n\n- Codex user 候选与 `auth.json` 排除；\n- Claude user 候选与 `.claude.json` 排除；\n- Claude project 内容、配置和连接审查分类；\n- 已知路径错误文件类型的 diagnostic；\n- ancestor symlink 逃逸；\n- inspection `Debug` 不泄露临时 root。\n\n测试只使用 `tempfile`，不读取真实 home。修改根目录错误分支、去重或多 root 行为时，应补充对应\nfailure fixture；当前测试尚未覆盖所有 `AgentImportError` 分支，这是明确的测试缺口。\n\n## 10. 当前限制与扩展点\n\n- **Current**：Codex/Claude 已知路径的 metadata-only 检查、根目录/候选校验、确定性 inspection、\n  diagnostic 与私有路径 `Debug` 隐藏。\n- **Current limitation**：不解析正文，不能生成按条目选择的转换 diff，也不能识别设置文件内嵌\n  的 hook、Plugin、MCP 或 Agent definition 声明。\n- **Current limitation**：user constructor 只识别默认 home layout；自定义 `CODEX_HOME`、\n  `CLAUDE_CONFIG_DIR` 或独立外部 source root 尚无公共构造契约。\n- **Current limitation**：没有 stable inspection identity、TOCTOU revalidation 或 App Server wire\n  contract。\n- **Current limitation**：没有 normalized import fragment、Config batch adapter、import receipt 或\n  source-qualified rollback contract。\n- **Proposed**：增加有界的 source-specific parser，输出不含 secret 的 typed preview fragment。\n- **Proposed**：App Server 组合 parser 结果并调用各 authority；Desktop 只提交用户确认后的\n  exact inspection identity。\n- **Proposed**：为 host `add-dir` adapter 提供只返回 allowlisted contribution kind 的窄\n  inspection projection；它不等同完整 Import，也不处理 directory authorization。\n\n无论后续实现如何演进，以下不变量保持不变：不扫描整个 home、不导入认证状态、不把 preview\n当作执行授权、不让 Desktop 或本 crate 绕过目标领域的最终校验。",
    "headings": [
      {
        "depth": 2,
        "id": "1-crate-边界与依赖方向",
        "title": "1. Crate 边界与依赖方向"
      },
      {
        "depth": 2,
        "id": "2-公共契约",
        "title": "2. 公共契约"
      },
      {
        "depth": 2,
        "id": "3-已知布局与审查分类",
        "title": "3. 已知布局与审查分类"
      },
      {
        "depth": 2,
        "id": "4-文件与内部所有权",
        "title": "4. 文件与内部所有权"
      },
      {
        "depth": 2,
        "id": "5-真实调用路径",
        "title": "5. 真实调用路径"
      },
      {
        "depth": 2,
        "id": "6-失败隔离与隐私语义",
        "title": "6. 失败、隔离与隐私语义"
      },
      {
        "depth": 2,
        "id": "7-host-接入义务",
        "title": "7. Host 接入义务"
      },
      {
        "depth": 3,
        "id": "71-应用到目标权威",
        "title": "7.1 应用到目标权威"
      },
      {
        "depth": 2,
        "id": "8-常见修改及影响面",
        "title": "8. 常见修改及影响面"
      },
      {
        "depth": 3,
        "id": "增加一个已知路径",
        "title": "增加一个已知路径"
      },
      {
        "depth": 3,
        "id": "增加一种外部-agent",
        "title": "增加一种外部 Agent"
      },
      {
        "depth": 3,
        "id": "增加内容-parser",
        "title": "增加内容 parser"
      },
      {
        "depth": 2,
        "id": "9-验证",
        "title": "9. 验证"
      },
      {
        "depth": 2,
        "id": "10-当前限制与扩展点",
        "title": "10. 当前限制与扩展点"
      }
    ],
    "searchText": " 文档所有权：本 README 是外部 Agent 配置只读发现、路径校验、导入计划与失败语义的当前实现 契约。 产品交互与长期演进由 docs/zeta-desktop-architecture.md 拥有；Zeta 原生 Instructions/Skills/Agents、.zeta 命名空间和 Import/source registration 边界由 docs/agent-customizations.md 拥有；Skill 来源与激活 语义由 docs/skills.md 拥有。 zeta-agent-import 只识别 Codex 和 Claude 已知的用户级、项目级配置位置，并生成仅元数据 （metadata-only）的 AgentPathInspection。当前实现不读取候选正文、不递归扫描未知目录、不修改 Zeta 配置，也不导入认证、会话、日志或历史记录。 1. Crate 边界与依赖方向 本 crate 拥有： - 外部 Agent、用户/项目作用范围和导入条目类型； - Codex/Claude 已知相对路径与预期文件类型； - 调用方所选根目录的规范化和目录包含关系校验； - 确定性排序、去重、候选与安全诊断； - 私有绝对路径的 Debug 隐藏。 本 crate 不拥有： - Desktop 目录选择、预览、确认、进度或撤销界面； - App Server 方法、配置 revision 或导入应用流程； - Config、Skill、MCP、Plugin 或 Multi-Agent 内容 schema； - 内容信任、工具批准、脚本执行、网络连接或凭据解析； - CODEXHOME、CLAUDECONFIGDIR 等宿主环境解析； - .zeta/ instructions,skills,agents 的原生发现、加载或 runtime activation。 生产代码依赖 zeta-utils-path 的 host canonical containment primitive，测试使用 workspace tempfile。它不得反向依赖 zeta-config、zeta-skills、App Server、Core 或 Desktop。后续内容 parser 可以依赖窄格式 crate，但不能为了落库或 UI 方便引入上述高层领域。 2. 公共契约 Symbol 当前职责 不承担 --- --- --- ExternalAgent 区分 Codex 与 Claude 布局 动态 provider registry ImportScope 区分 User 与 Project 来源 workspace trust 或配置优先级 AgentImportLocation:: codexuser,codexproject,claudeuser,claudeproject 将来源、作用范围和调用方选择的根目录绑定为一个发现输入 环境变量解析、文件访问授权 inspectagentpaths 校验所有输入根并检查已知相对位置 读取正文、转换内容或应用配置 AgentImportCandidate 返回来源、作用范围、条目类型、审查类别、相对路径和 canonical host path 表示内容已受信任、已批准或可执行 AgentImportDiagnostic 说明一个已存在的已知路径为什么未进入候选 暴露根目录、正文或凭据 AgentImportError 表达调用方选择的根目录整体无效 表达单个候选的隔离错误 典型调用点保持自解释，不传递布尔模式或裸来源字符串： ``rust use zetaagentimport:: AgentImportLocation, inspectagentpaths ; let inspection = inspectagentpaths AgentImportLocation::codexuser userhome , AgentImportLocation::claudeproject projectroot , ?; ` AgentPathInspection 只是检查结果和预览输入。调用方仍须让用户选择具体候选，并把确认结果交给相应领域重新 解析和校验；不能把 candidates 非空解释为可自动导入。 3. 已知布局与审查分类 agentpaths.rs 是外部 Agent 路径映射的唯一实现 owner。当前固定布局如下： 来源 用户级路径 项目级路径 --- --- --- Codex /.codex/ AGENTS.md,AGENTS.override.md,config.toml,agents/,rules/ 、/.agents/skills/ AGENTS.md,AGENTS.override.md 、.codex/ config.toml,agents/,rules/ 、.agents/skills/ Claude /.claude/ CLAUDE.md,settings.json,skills/,agents/,rules/ CLAUDE.md,CLAUDE.local.md,.mcp.json 、.claude/ CLAUDE.md,settings.json,settings.local.json,skills/,agents/,rules/ 路径映射依据 Codex 的 导入说明和 Skill 位置说明，以及 Claude 的 设置位置说明和 Skill 位置说明。外部产品改变路径时，先更新 官方契约证据和 fixture，再修改 agentpaths.rs；不能靠扫描整个 home 猜测新位置。 Claude 的 legacy .claude/commands/ 明确不在 Import surface 中。Zeta 当前没有接收外部 command body、source provenance、enablement 与执行语义的产品契约；发现这些文件只会产生无法消费的候选项。 ImportItemKind ImportReviewCategory 原因 --- --- --- Instructions、Skills、InstructionRules Content 包含将进入模型上下文的外部指令 Settings、Agents Configuration 需要目标领域映射，不能按原格式直接生效 McpServers Connection 可能引入进程、网络、header 或重新登录要求 ExecutionRules ExecutionPolicy 可能改变命令是否提示、允许或阻止 审查类别只用于 UI 分组和后续处理路由，不是授权结果。特别是 ExecutionPolicy 不能生成 Zeta 长期批准，Connection 不能自动启动 MCP。 /.codex/auth.json 永不进入候选。/.claude.json 同时包含 OAuth session、MCP、 per-project state 和 cache，当前整体排除。未来若需要其中的非敏感 MCP 声明，必须增加专门的 有界 parser 与结构化脱敏，不能把整个文件交给 Desktop、模型或普通日志。 4. 文件与内部所有权 文件 / private symbol 单一职责 修改时同步检查 --- --- --- import.rs 公共导入值类型、named constructor、getter 和私有路径 Debug App Server DTO、Desktop preview、隐私测试 agentpaths.rs::pathsfor ExternalAgent + ImportScope 到固定 AgentPath 的穷尽映射 官方路径、review category、fixture 和本表 agentpaths.rs:: file,directory 构造带预期 entry 类型的 Agent 路径 type-mismatch diagnostic inspectpath.rs::inspectagentpaths 逐 root 检查 Agent 路径、排序、去重并构造 immutable inspection 多 root 失败语义和顺序测试 inspectpath.rs::validateimportroot 拒绝不可用、非目录或 symlink root，并建立 CanonicalPathRoot AgentImportError 与错误脱敏 inspectpath.rs::inspectpath 检查一个 Agent 相对路径的 metadata、类型和 symlink，再委托通用 canonical containment diagnostic code、候选 canonical path error.rs 根目录级类型化错误与不含绝对路径的显示文本 Desktop 错误映射与日志 inspectpathtests.rs 临时目录上的路径检查与隐私回归 新来源、路径、错误或 redaction lib.rs 保持私有模块和显式 re-export。若调用方开始依赖 agentpaths/inspectpath 私有函数，或 crate root 重新实现路径判断，说明公共 API 或 ownership 已经漂移。 5. 真实调用路径 `text inspectagentpaths → validateimportroot → symlinkmetadata root → reject symlink / non-directory → CanonicalPathRoot::new root → agentpaths::pathsfor agent, scope → inspectpath for each fixed relative path → missing: omit → symlinkmetadata candidate → expected file/directory check → CanonicalPathRoot::canonicalizewithin candidate → AgentImportCandidate AgentImportDiagnostic → sort + dedup candidates and diagnostics → AgentPathInspection::new ` 发现只访问根目录和静态 AgentPath 指向的 metadata。它不会枚举 .codex、.claude 或 Skill 目录的子项，也不会打开文件，因此当前 inspection 不能证明正文有效。 6. 失败、隔离与隐私语义 条件 结果 是否继续其他候选 --- --- --- 输入 root 不可访问 AgentImportError::RootUnavailable ❌ 整个调用失败 输入 root 不是目录 AgentImportError::RootNotDirectory ❌ 整个调用失败 输入 root 本身是 symlink AgentImportError::RootSymlinkNotAllowed ❌ 整个调用失败 已知相对路径不存在 不产生候选或 diagnostic ✅ 候选 metadata/canonicalize 失败 MetadataUnavailable ✅ 候选类型与 specification 不符 UnexpectedFileType ✅ 候选自身是 symlink SymlinkNotAllowed ✅ ancestor symlink 使 canonical candidate 逃出 root EscapesSelectedRoot ✅ 候选合法 保存 canonical path ✅ 多 root 调用不是部分成功协议：任一输入 root 无效都会返回 Err，不会返回其他 root 的半份 inspection。 单候选问题则通过 diagnostic 隔离，不影响同一 root 的其他候选。 AgentImportLocation 和 AgentImportCandidate 的 Debug 隐藏绝对路径，错误和 diagnostic 只携带 来源、作用范围、固定相对路径或 io::ErrorKind。但 AgentImportCandidate::sourcepath 会 显式返回 canonical host path，供可信 host adapter 后续读取；调用方不得把该值放入普通 telemetry、 Thread history、模型上下文或未脱敏错误。 7. Host 接入义务 Desktop/App Server 接入时必须： 1. 只从用户明确选择或产品已定义的根目录构造 AgentImportLocation； 2. 在受信 host 侧调用 discover，不接受 Renderer 提交任意候选路径； 3. 用 agent + scope + kind + relative path 展示候选，默认不显示完整 home path； 4. 让用户按条目确认，并将 review category 对应的风险和后续动作分开说明； 5. 在应用前由目标领域重新打开、限制大小、解析、校验并生成自己的 typed mutation； 6. 对读取与应用之间的文件变化重新校验 identity/digest，不能把 preview 当作冻结内容； 7. 认证、凭据、连接批准和执行批准继续走各自 authority。 当前没有 App Server DTO 或 inspection identity。任何 wire contract 都应传 source-qualified identity 和受控相对路径，不应直接把 sourcepath 序列化给 Renderer。 7.1 应用到目标权威 Import 的最终目的不是让 Zeta runtime 直接解释外部格式，而是把用户选中的内容转换成目标 authority 能重新校验的 typed fragment。App Server adapter 根据条目路由到 Config、Instruction、 Skill、Agent 或其他 authority；本 crate 不拥有这些领域的 canonical schema。依赖方向保持为： `text external source → zeta-agent-import normalized preview fragment → App Server import adapter → target authority typed command / prepare-publish contract ` zeta-agent-import 不依赖 zeta-config，也不直接构造 UserConfigCommand。App Server adapter 同时 依赖两者，负责 source-specific conversion、conflict preview、用户逐项确认和 Config transaction。 这样外部格式变化不会反向决定 Zeta Config schema，inspection 成功也不会被误当成 configuration commit。 Import item Zeta apply target 当前状态与安全边界 --- --- --- Skills AddSkillSource / Skill source authority Config command 已有；仍需 parser、digest、conflict 与 source identity McpServers UpsertMcpServer Config command 已有；导入时默认不连接，credential 必须剥离并单独绑定 Settings 内的 Plugin request UpsertPluginRequest Config command 已有；只接受可解析的 exact package/version request，不代表安装或激活 Settings 内的 Hook UpsertHook Config command 已有；导入后保持 disabled，执行仍需 trust、policy、approval 与 sandbox Instructions、InstructionRules Zeta Instruction authority 目标模型尚未完成，不能把原始文件塞入普通 Config Agents Zeta Agent definition authority 目标模型尚未完成 Settings 其他字段 对应 Zeta typed field-by-field mapping 不支持项必须显示为 skipped/unsupported，禁止 raw passthrough ExecutionRules Policy migration review 不能生成长期 approval，也不能自动转换为 Hook 一次 Import 可能同时修改 Skill、MCP、Plugin 与 Hook section，因此 apply 必须先构造完整 plan。 Config 子批次使用 expected-revision 约束： 重新校验 source identity/digest，验证全部 typed mutation，成功时一次推进 Config revision，任一项 失败则不提交任何 Config 项。Instructions 与 Agents 等非 Config target 尚未完成；在对应 authority 可以 prepare/publish 前，它们必须保持 unsupported，不能伪装成同一 Config transaction。 当前 Config 只有逐 command mutation；atomic import batch、跨 authority prepare/publish、import receipt、provenance 与 remove/rollback contract 尚未实现。 zeta-add-dir 与 Import workflow 是两条不同的 host path。前者授予附加目录的持续文件访问，并可能 按 directory origin 临时投影 allowlisted Skills、Agent definitions 或 Plugin declaration；后者让用户 预览、选择并迁移外部 Agent 配置，不授予持续文件访问。未来两条路径可以复用本 crate 的 source-specific inspection/parser，但不能复用 authority、lifecycle 或 apply decision。持久 additionalDirectories 是 file-access-only，不能仅因目录可访问就调用本 crate 自动发现配置。 本 crate 不依赖 zeta-add-dir；App Server adapter 负责把 allowlisted inspection projection 映射到对应的 contribution policy。 原生加载是第四条独立路径：Workspace .zeta/ instructions,skills,agents 、Zeta user root、 built-in resources 和 Plugin contribution 由各目标 authority 直接发现，不经过本 crate。若 Instruction/Skill/Agent loader 开始扫描 .codex、.agents 或 .claude，或者本 crate 开始加载 .zeta 并构造 runtime snapshot，都表示 compatibility boundary 已经漂移。 Import 与 source registration 也不能画等号。Import 生成由 Zeta authority 独立管理的 artifact； source registration 保存一个可撤销的窄只读来源，当前只对 standalone Skill source 具备 Config desired-state 基础。两者可以复用 discovery/parser，但由目标 authority 决定 persistence、refresh 和 removal semantics。 8. 常见修改及影响面 增加一个已知路径 1. 在对应 agentpaths.rs source/scope 数组增加 AgentPath； 2. 选择准确的 ImportItemKind、ImportReviewCategory 和 expected file/directory； 3. 增加存在、缺失、错误类型和敏感排除测试； 4. 更新本 README 的布局表，并检查 Desktop/Skill/权限文档是否受影响。 增加一种外部 Agent 1. 扩展 ExternalAgent 和 named AgentImportLocation constructor； 2. 为 User/Project 分别定义静态已知路径，保持 pathsfor 穷尽匹配； 3. 增加官方路径证据、完整 fixture 和 redaction 测试； 4. 定义目标领域映射前只返回 preview candidate，不增加隐式 apply； 5. 同步 App Server DTO、Desktop picker 和系统文档。 增加内容 parser parser 必须位于新的 private module，设置 byte/entry/depth 上限，拒绝未知 active content，并输出 不含 secret 的 typed fragment。它不能在解析阶段写 Config、连接 MCP、执行 hook/script 或生成 批准。若单个模块接近 500 LoC，应按来源或内容类型拆分并把测试放到 sibling tests.rs。 9. 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-agent-import cargo clippy --manifest-path Cargo.toml \\ -p zeta-agent-import --all-targets --no-deps -- -D warnings bazel test //zeta-rs/agent-import:agent-import-unit-tests ` 当前测试覆盖： - Codex user 候选与 auth.json 排除； - Claude user 候选与 .claude.json 排除； - Claude project 内容、配置和连接审查分类； - 已知路径错误文件类型的 diagnostic； - ancestor symlink 逃逸； - inspection Debug 不泄露临时 root。 测试只使用 tempfile，不读取真实 home。修改根目录错误分支、去重或多 root 行为时，应补充对应 failure fixture；当前测试尚未覆盖所有 AgentImportError 分支，这是明确的测试缺口。 10. 当前限制与扩展点 - Current：Codex/Claude 已知路径的 metadata-only 检查、根目录/候选校验、确定性 inspection、 diagnostic 与私有路径 Debug 隐藏。 - Current limitation：不解析正文，不能生成按条目选择的转换 diff，也不能识别设置文件内嵌 的 hook、Plugin、MCP 或 Agent definition 声明。 - Current limitation：user constructor 只识别默认 home layout；自定义 CODEXHOME、 CLAUDECONFIGDIR 或独立外部 source root 尚无公共构造契约。 - Current limitation：没有 stable inspection identity、TOCTOU revalidation 或 App Server wire contract。 - Current limitation：没有 normalized import fragment、Config batch adapter、import receipt 或 source-qualified rollback contract。 - Proposed：增加有界的 source-specific parser，输出不含 secret 的 typed preview fragment。 - Proposed：App Server 组合 parser 结果并调用各 authority；Desktop 只提交用户确认后的 exact inspection identity。 - Proposed：为 host add-dir` adapter 提供只返回 allowlisted contribution kind 的窄 inspection projection；它不等同完整 Import，也不处理 directory authorization。 无论后续实现如何演进，以下不变量保持不变：不扫描整个 home、不导入认证状态、不把 preview 当作执行授权、不让 Desktop 或本 crate 绕过目标领域的最终校验。"
  },
  {
    "slug": "crates/zeterm/agent-sidebar",
    "sourcePath": "zeterm/agent-sidebar/README.md",
    "title": "zeta-agent-sidebar",
    "description": "zeta-agent-sidebar owns the retained product state for the Agent Sidebar's Files and source-control-management (SCM) panes. It is the canonical implementation-level contract for this surf…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-agent-sidebar` owns the retained product state for the Agent Sidebar's\nFiles and source-control-management (SCM) panes. It is the canonical\nimplementation-level contract for this surface; the cross-runtime product\narchitecture remains documented in [`docs/native-terminal-ui.md`](../docs/native-terminal-ui.md).\n\n## Ownership\n\n| Concern | Owner | Boundary |\n| --- | --- | --- |\n| Active Files/Changes pane and retained pane state | `AgentSidebar` | Does not know windows, app-server clients, or platform events. |\n| Cross-pane selection and navigation | `AgentSidebarNavigation` | Owns only the Changes/Files switcher; it does not place Files or SCM controls. |\n| Hierarchical entries, expansion, search query, and list scrolling | `files::FilesState` | Receives host-projected `DirectoryEntry` values, never protocol DTOs. |\n| Files functional toolbar and tree/search geometry | `files::FilesLayout` / `files::FilesToolbar` / `files::FilesPane` | Owns Refresh, ahead/behind, Search, and the Files content bounds. |\n| Changed-file snapshot | `scm::ScmState` | Receives host-projected `ScmDiff` values; Git transport stays outside the crate. |\n| Changes functional layout and multi-diff presentation | `scm::ScmLayout` / `scm::EditorPane` | Owns the Changes content bounds and diff viewport interactions; delegates the nested inspection/interaction tree to `zeta-editor::MultiDiffEditor`. |\n| Read-directory/open-file side effects | Native host | Executes `AgentSidebarAction` and supplies the resulting snapshot. |\n\nThe crate may depend on UI primitives, path search, editor document types, and\nicons. It must not depend on `zeterm/zeterm`, app-server transport crates, a\nwindow backend, or a Git transport implementation.\n\n## Contract and execution path\n\n`AgentSidebar::files_mut()` exposes the Files model to its presentation owner.\n`FilesState::activate`, `navigate_right`, and `navigate_left` return an\n`AgentSidebarAction`; `OpenFile` and `LoadChildren` are host obligations rather\nthan in-crate side effects. The host maps an authoritative directory response\nto `DirectoryEntry` and calls `FilesState::refresh` or\n`FilesState::complete_directory_load`.\n\n```text\nnative event → FilesState interaction → AgentSidebarAction\n             → native host executes file-system request\n             → DirectoryEntry snapshot → FilesState\n```\n\nThe outer sidebar supplies only the host rectangle and cross-pane navigation.\n`FilesLayout`/`FilesToolbar` resolve the Files toolbar and content geometry;\n`ScmLayout`/`EditorPane` resolve the Changes geometry. Neither feature imports\nthe other feature's layout or state.\n\n`FilesTree` is private because stable node identity, loaded-child state, and\nvisible-row projection must change together. `FilesState::refresh` restarts the\npath search index for the active root; a failed index start leaves the tree\nusable but produces no search matches. Stale search snapshots are ignored by\ntheir revision.\n\n`ScmState` retains both the workspace-provided changed-file snapshot and the\n`EditorPaneState` used by `EditorPane`. The host still owns the outer shell slot,\ntheme-token mapping, and execution of refresh/open actions; it must not retain a\nsecond diff viewport beside `ScmState::editor`.\n\n`EditorPane::compose` is the SCM composition boundary. It draws `MultiDiffEditor`\nthrough `ComponentContext::draw_component` with the host identities for the\nmulti-diff root and scrollbar. `MultiDiffEditor` then owns the visible\n`MultiDiffSection`, file header, `DiffEditor`, `CodeEditor`, fold-control, and\nscrollbar components. The SCM host must not reconstruct child `UiNode`s; the\nold `ComponentContext::register_node` bridge has been deleted.\n\n## Modification guide\n\n- Add Files interactions through `AgentSidebarAction`; do not call the\n  filesystem or app-server directly from `files`.\n- Extend source-control capability under `scm`; do not add SCM-specific state\n  to `files` or generic UI primitives.\n- Preserve `FilesTree` element IDs when an entry still represents the same\n  path. Changing this invalidates focus and accessibility continuity.\n- Preserve the changed-file state in `EditorPaneState` while the\n  `MultiDiffEditor` component tree is rebuilt. `EditorPaneState` allocates a\n  stable `MultiDiffEditorItemIdentity` per changed-file path, reuses the\n  identity and `DiffEditorState` when snapshots reorder, and derives fold\n  interaction IDs from that identity. The height animation key is exposed by\n  `MultiDiffEditorItemIdentity`; transition binding and lifecycle cleanup remain\n  zui runtime work.\n- If a new host snapshot type is needed, add a crate-owned projection type and\n  adapt it in the host; do not expose a protocol DTO in the public API.\n\n## Verification\n\nRun `cargo check -p zeta-agent-sidebar`,\n`cargo test -p zeta-agent-sidebar`, and `cargo test --manifest-path Cargo.toml -p zeterm`. Files\nmodel and pane presentation tests live in this crate; native tests cover only\nthe protocol adapter and shell composition.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "contract-and-execution-path",
        "title": "Contract and execution path"
      },
      {
        "depth": 2,
        "id": "modification-guide",
        "title": "Modification guide"
      },
      {
        "depth": 2,
        "id": "verification",
        "title": "Verification"
      }
    ],
    "searchText": "zeta-agent-sidebar owns the retained product state for the Agent Sidebar's Files and source-control-management SCM panes. It is the canonical implementation-level contract for this surface; the cross-runtime product architecture remains documented in docs/native-terminal-ui.md. Ownership Concern Owner Boundary --- --- --- Active Files/Changes pane and retained pane state AgentSidebar Does not know windows, app-server clients, or platform events. Cross-pane selection and navigation AgentSidebarNavigation Owns only the Changes/Files switcher; it does not place Files or SCM controls. Hierarchical entries, expansion, search query, and list scrolling files::FilesState Receives host-projected DirectoryEntry values, never protocol DTOs. Files functional toolbar and tree/search geometry files::FilesLayout / files::FilesToolbar / files::FilesPane Owns Refresh, ahead/behind, Search, and the Files content bounds. Changed-file snapshot scm::ScmState Receives host-projected ScmDiff values; Git transport stays outside the crate. Changes functional layout and multi-diff presentation scm::ScmLayout / scm::EditorPane Owns the Changes content bounds and diff viewport interactions; delegates the nested inspection/interaction tree to zeta-editor::MultiDiffEditor. Read-directory/open-file side effects Native host Executes AgentSidebarAction and supplies the resulting snapshot. The crate may depend on UI primitives, path search, editor document types, and icons. It must not depend on zeterm/zeterm, app-server transport crates, a window backend, or a Git transport implementation. Contract and execution path AgentSidebar::filesmut exposes the Files model to its presentation owner. FilesState::activate, navigateright, and navigateleft return an AgentSidebarAction; OpenFile and LoadChildren are host obligations rather than in-crate side effects. The host maps an authoritative directory response to DirectoryEntry and calls FilesState::refresh or FilesState::completedirectoryload. ``text native event → FilesState interaction → AgentSidebarAction → native host executes file-system request → DirectoryEntry snapshot → FilesState ` The outer sidebar supplies only the host rectangle and cross-pane navigation. FilesLayout/FilesToolbar resolve the Files toolbar and content geometry; ScmLayout/EditorPane resolve the Changes geometry. Neither feature imports the other feature's layout or state. FilesTree is private because stable node identity, loaded-child state, and visible-row projection must change together. FilesState::refresh restarts the path search index for the active root; a failed index start leaves the tree usable but produces no search matches. Stale search snapshots are ignored by their revision. ScmState retains both the workspace-provided changed-file snapshot and the EditorPaneState used by EditorPane. The host still owns the outer shell slot, theme-token mapping, and execution of refresh/open actions; it must not retain a second diff viewport beside ScmState::editor. EditorPane::compose is the SCM composition boundary. It draws MultiDiffEditor through ComponentContext::drawcomponent with the host identities for the multi-diff root and scrollbar. MultiDiffEditor then owns the visible MultiDiffSection, file header, DiffEditor, CodeEditor, fold-control, and scrollbar components. The SCM host must not reconstruct child UiNodes; the old ComponentContext::registernode bridge has been deleted. Modification guide - Add Files interactions through AgentSidebarAction; do not call the filesystem or app-server directly from files. - Extend source-control capability under scm; do not add SCM-specific state to files or generic UI primitives. - Preserve FilesTree element IDs when an entry still represents the same path. Changing this invalidates focus and accessibility continuity. - Preserve the changed-file state in EditorPaneState while the MultiDiffEditor component tree is rebuilt. EditorPaneState allocates a stable MultiDiffEditorItemIdentity per changed-file path, reuses the identity and DiffEditorState when snapshots reorder, and derives fold interaction IDs from that identity. The height animation key is exposed by MultiDiffEditorItemIdentity; transition binding and lifecycle cleanup remain zui runtime work. - If a new host snapshot type is needed, add a crate-owned projection type and adapt it in the host; do not expose a protocol DTO in the public API. Verification Run cargo check -p zeta-agent-sidebar, cargo test -p zeta-agent-sidebar, and cargo test --manifest-path Cargo.toml -p zeterm`. Files model and pane presentation tests live in this crate; native tests cover only the protocol adapter and shell composition."
  },
  {
    "slug": "crates/agents",
    "sourcePath": "zeta-rs/agents/README.md",
    "title": "zeta-agents",
    "description": "zeta-agents 对一个 Workspace 的 .zeta/agents/.md 执行有界、非递归发现，校验 definition frontmatter 和 role instructions，并发布不可变 catalog snapshot。它不创建 Thread、不调用模型、 不解析外部 Agent 格式，也不把 tool/model references 解…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 Zeta 原生 Agent definition artifact 的当前实现契约。跨系统对象划分、`.zeta`\n> 命名空间与运行时边界由\n> [`docs/agent-customizations.md`](../../docs/agent-customizations.md) 维护；Thread execution identity\n> 与 multi-agent gate 由 [`docs/core-multi-agent.md`](../../docs/core-multi-agent.md) 维护。\n\n`zeta-agents` 对一个 Workspace 的 `.zeta/agents/*.md` 执行有界、非递归发现，校验 definition\nfrontmatter 和 role instructions，并发布不可变 catalog snapshot。它不创建 Thread、不调用模型、\n不解析外部 Agent 格式，也不把 tool/model references 解释成权限或可用性保证。\n\n## 快速理解\n\n| 文件状态 | 结果 |\n| --- | --- |\n| `.zeta/agents` 不存在 | 空 catalog，无错误 |\n| 合法 `.md` | 进入确定性 definition catalog |\n| 单文件格式错误 | 产生隔离 diagnostic，其他文件继续 |\n| definition 被发现 | 只证明声明有效，不表示 Agent 已启动或引用可用 |\n\n## 边界与格式\n\n```yaml\n---\nname: reviewer\ndescription: Reviews code changes for correctness and regressions.\nmodel: openai/gpt-5\ntools:\n  - read_file\nskills:\n  - code-review\ninstructions:\n  - rust-style\n---\n\nReview the requested change and report actionable findings.\n```\n\n`name` 必须与小写文件名一致；`description` 和非空 Markdown role body 必填。`model`、`tools`、\n`skills`、`instructions` 是经过基本语法校验的引用声明，最终解析继续由对应 authority 完成。\n重复或非法引用使单个 definition 失效，不能通过发现阶段授予工具或模型能力。\n\n实现限制为 64 个直接条目、每个文件 32 KiB。目录、symlink、非 Markdown、非法 UTF-8、未知\nfrontmatter 字段和空正文不会进入 catalog。\n\n## 内部所有权与调用路径\n\n| 文件 / symbol | 职责 |\n| --- | --- |\n| `catalog.rs::scan` | 固定 `.zeta/agents` 路径、entry limit、排序与隔离诊断 |\n| `catalog.rs::load_entry` | 文件、frontmatter、正文与引用校验 |\n| `catalog.rs::validate_references` | reference syntax 与重复拒绝 |\n| `model.rs::AgentDefinitionCatalogSnapshot` | immutable entries/diagnostics generation |\n\n```text\nAgentDefinitionCatalog::discover / refresh\n  → scan\n  → load_entry\n  → validate_references\n  → AgentDefinitionCatalogSnapshot\n```\n\n若本 crate 开始创建 Thread、决定模型、解析 Tool registry、授予权限、扫描 `.codex/.claude` 或\n管理 multi-agent lifecycle，表示 definition authority 已经越界。\n\n## 验证与当前限制\n\n```bash\ncargo test -p zeta-agents\ncargo clippy -p zeta-agents --all-targets --no-deps -- -D warnings\n```\n\n当前已实现 Workspace 原生 definition 发现、格式校验、不可变 snapshot 与 refresh；App Server\n的 `WorkspaceCustomizations` 在 Workspace 激活时拥有 catalog，并响应 filesystem invalidation。\n当前限制是 App Server list API、definition selection、跨 authority 引用解析和 multi-agent\nruntime consumption 尚未实现。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "边界与格式",
        "title": "边界与格式"
      },
      {
        "depth": 2,
        "id": "内部所有权与调用路径",
        "title": "内部所有权与调用路径"
      },
      {
        "depth": 2,
        "id": "验证与当前限制",
        "title": "验证与当前限制"
      }
    ],
    "searchText": " 本 README 拥有 Zeta 原生 Agent definition artifact 的当前实现契约。跨系统对象划分、.zeta 命名空间与运行时边界由 docs/agent-customizations.md 维护；Thread execution identity 与 multi-agent gate 由 docs/core-multi-agent.md 维护。 zeta-agents 对一个 Workspace 的 .zeta/agents/.md 执行有界、非递归发现，校验 definition frontmatter 和 role instructions，并发布不可变 catalog snapshot。它不创建 Thread、不调用模型、 不解析外部 Agent 格式，也不把 tool/model references 解释成权限或可用性保证。 快速理解 文件状态 结果 --- --- .zeta/agents 不存在 空 catalog，无错误 合法 .md 进入确定性 definition catalog 单文件格式错误 产生隔离 diagnostic，其他文件继续 definition 被发现 只证明声明有效，不表示 Agent 已启动或引用可用 边界与格式 ``yaml --- name: reviewer description: Reviews code changes for correctness and regressions. model: openai/gpt-5 tools: - readfile skills: - code-review instructions: - rust-style --- Review the requested change and report actionable findings. ` name 必须与小写文件名一致；description 和非空 Markdown role body 必填。model、tools、 skills、instructions 是经过基本语法校验的引用声明，最终解析继续由对应 authority 完成。 重复或非法引用使单个 definition 失效，不能通过发现阶段授予工具或模型能力。 实现限制为 64 个直接条目、每个文件 32 KiB。目录、symlink、非 Markdown、非法 UTF-8、未知 frontmatter 字段和空正文不会进入 catalog。 内部所有权与调用路径 文件 / symbol 职责 --- --- catalog.rs::scan 固定 .zeta/agents 路径、entry limit、排序与隔离诊断 catalog.rs::loadentry 文件、frontmatter、正文与引用校验 catalog.rs::validatereferences reference syntax 与重复拒绝 model.rs::AgentDefinitionCatalogSnapshot immutable entries/diagnostics generation `text AgentDefinitionCatalog::discover / refresh → scan → loadentry → validatereferences → AgentDefinitionCatalogSnapshot ` 若本 crate 开始创建 Thread、决定模型、解析 Tool registry、授予权限、扫描 .codex/.claude 或 管理 multi-agent lifecycle，表示 definition authority 已经越界。 验证与当前限制 `bash cargo test -p zeta-agents cargo clippy -p zeta-agents --all-targets --no-deps -- -D warnings ` 当前已实现 Workspace 原生 definition 发现、格式校验、不可变 snapshot 与 refresh；App Server 的 WorkspaceCustomizations` 在 Workspace 激活时拥有 catalog，并响应 filesystem invalidation。 当前限制是 App Server list API、definition selection、跨 authority 引用解析和 multi-agent runtime consumption 尚未实现。"
  },
  {
    "slug": "crates/zeta-api",
    "sourcePath": "zeta-rs/zeta-api/README.md",
    "title": "zeta-api",
    "description": "zeta-api 在 canonical zeta-protocol model values 与具体模型 API wire format 之间转换。 它当前实现 OpenAI Responses、OpenAI-compatible Chat Completions 和 Anthropic Messages 的 unary request/response codec、原…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 endpoint dispatch、wire codec 与 streaming event decoder 的实现边界。\n> Provider/runtime 的系统关系与演进见 [`docs/zeta-api.md`](../../docs/zeta-api.md)。\n\n`zeta-api` 在 canonical `zeta-protocol` model values 与具体模型 API wire format 之间转换。\n它当前实现 OpenAI Responses、OpenAI-compatible Chat Completions 和 Anthropic Messages 的\nunary request/response codec、原生 HTTP/SSE streaming invocation 与 terminal response assembly。\n独立 input-token codec 覆盖 OpenAI Responses、Anthropic Messages、Gemini\n`countTokens`、Kimi estimate 和 Z.AI tokenizer。\n\n它不选择 provider、model 或 credential，不拥有 base URL，也不执行 socket、retry 或 SSE\nframing。\n\n## Crate 边界\n\n```text\nzeta-model-provider\n  └─ 选择 ApiEndpoint + ResolvedApiTarget\n       └─ zeta-api\n          ├─ canonical request → provider JSON\n          ├─ provider JSON → canonical response\n          └─ framed SSE event → ModelStreamEvent\n               ├─ zeta-client：operation retry 与 SSE framing\n               └─ zeta-http-client：HTTP transport\n```\n\n本 crate 直接 re-export `zeta-protocol` 的 `ModelRequest`、`ModelResponse`、`Message`、\n`ContentPart`、`ToolDefinition`、`ToolCall` 等 canonical types。不要在这里创建第二套\nrequest/response domain model。\n\n## 公共契约\n\n| Symbol | 职责 | 不负责 |\n| --- | --- | --- |\n| `ApiEndpoint` | 指定 concrete endpoint family 并 dispatch codec | provider 或 model selection |\n| `ApiProtocol` | 暴露 endpoint 使用的 normalized protocol family | URL/provider 推断 |\n| `ApiEndpoint::complete_with_client` | 校验 canonical request，执行 unary encode/call/decode | retry、credential refresh |\n| `ApiEndpoint::count_input_tokens_with_client` | dispatch OpenAI Responses / Anthropic token-count codec | 准确度解释、调用频率、预算策略 |\n| `InputTokenCountEndpoint` | dispatch concrete provider preflight codec | model eligibility、exact/estimated 判断、保守余量 |\n| `OpenAiResponsesSseDecoder` | Responses SSE event schema 与 terminal lifecycle | SSE byte framing、reconnect |\n| `AnthropicMessagesSseDecoder` | Messages content-block lifecycle 与 canonical delta | transport liveness、tool JSON accumulation |\n| `ApiError` | request、transport、status 与 response codec failure | provider selection error |\n\n`ApiEndpoint` 是 wire contract，不是 vendor identity。同一 provider 可以暴露多个 profile；相同\ncompatible profile 也不能据此假设 cache、usage、error 或 streaming 语义完全相同。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `ApiEndpoint::method` | crate-private | 当前所有 endpoint 使用 `POST` | transport method 不由 provider adapter 重写 |\n| `ApiEndpoint::relative_path` | crate-private | `responses`、`chat/completions`、`v1/messages` | path 属于 endpoint protocol |\n| `ApiEndpoint::headers` | crate-private | 合并 target headers，并补 Anthropic version | credential headers 仍由 provider runtime 提供 |\n| `validate_request` | private | 拒绝空 model/input 与零 max tokens | 在任何 transport 调用前执行 |\n| `requests::post_json` | crate-private | JSON serialization、`ClientRequest`、status 与 JSON parse | 不选择 retry policy或 codec |\n| `requests::*::complete` | crate-private | 对应 endpoint 的 build/call/parse pipeline | endpoint dispatch 的唯一 codec target |\n| `requests::{google_count_tokens,kimi_estimate_tokens,zai_tokenizer}` | crate-private | provider count request/response JSON | 不声明准确度或调用频率 |\n| `requests::*::build_request` | private | canonical input → endpoint JSON | 不读取 provider config |\n| `requests::*::parse_response` | private | endpoint JSON → canonical output/usage/stop reason | malformed response fail closed |\n| `OpenAiResponsesSseDecoder::decode_event` | private | event type dispatch、delta extraction、terminal transition | 不解释 raw SSE bytes |\n| `AnthropicMessagesSseDecoder::{start_message,start_block,decode_block_delta,stop_block,stop_message}` | private | enforce message/block state machine | lifecycle 不能下沉到 UI |\n| `ContentBlockKind` | private | 将 block kind 与允许的 delta kind 绑定 | unknown block 可以忽略，known mismatch 必须拒绝 |\n\n## Unary 调用图\n\n```text\nApiEndpoint::complete_with_client(target, model, request, client)\n├─ validate_request\n└─ endpoint-specific requests::*::complete\n   ├─ build_request\n   └─ requests::post_json\n      ├─ serde_json::to_vec\n      ├─ ClientRequest::new\n      │  ├─ ApiEndpoint::method\n      │  ├─ ApiEndpoint::relative_path\n      │  └─ ApiEndpoint::headers\n      ├─ OperationClient::execute\n      ├─ reject non-2xx as ApiError::HttpStatus\n      └─ serde_json::from_slice\n         └─ parse_response\n```\n\nToken preflight 使用相同 canonical `ModelRequest`，发送到 profile 声明的 concrete count endpoint，\n并只返回 `InputTokenCount`。OpenAI/Anthropic count 复用对应 invocation builder；Kimi/Z.AI 从 Chat\nCompletions builder 中只保留各自文档允许的 input 字段；Gemini 把 canonical request 显式转换为\nnative `generateContentRequest`。本 crate 不把 exact/estimated 契约编码进 wire value；准确度由\n`zeta-model-provider` adapter 声明。普通 Chat Completions 没有标准 count endpoint，不能把任意\ncompatible provider 自动当作可计量。\n\n三套 codec 都处理 canonical messages、tools、tool choice、reasoning、usage 和 stop reason，但\n只共享 mechanical helpers；不能因为 JSON 外形相似就合并 protocol-specific semantics。\n\n## 流式处理解码器\n\nDecoder 接收 `zeta-client::SseFrame`，说明 SSE field parsing 已经完成。它们输出\n`zeta_protocol::ModelStreamEvent`：\n\n```text\nSseFrame::Event\n└─ Decoder::decode\n   ├─ parse event.data JSON\n   ├─ determine event type\n   ├─ validate lifecycle/schema\n   └─ emit TextDelta / ReasoningDelta / no event\n\nend of stream\n└─ Decoder::finish\n   └─ require protocol terminal state\n```\n\n`OpenAiResponsesSseDecoder`：\n\n- text 与 reasoning-summary delta 分别映射为 canonical delta；\n- `response.completed` 进入 terminal；\n- `response.failed`/`response.incomplete` 返回 failure；\n- terminal 后的任何 event、或 terminal 前 EOF 都是 invalid response；\n- unknown optional event 与 non-event frame 被忽略。\n\n`AnthropicMessagesSseDecoder`：\n\n- 要求 `message_start → content_block_* → message_stop`；\n- 用 `BTreeMap<u64, ContentBlockKind>` 跟踪并行 block index；\n- text/thinking delta 映射为 canonical delta；\n- ping 不形成输出；message-level usage/stop reason、signature 与 tool input JSON fragment 进入\n  terminal response assembly；\n- 未 start 的 delta、重复 block、unknown stop、open block 上的 message stop、terminal 前 EOF 都被拒绝。\n\n`OpenAiChatCompletionsSseDecoder`：\n\n- 按 choice 与 tool-call index 重组 text 和 function argument fragment；\n- 接受独立 usage chunk，并要求 `[DONE]` terminal；\n- finish reason、usage 和重组后的 Tool Calls 进入 terminal response；\n- malformed chunk、terminal 前 EOF、terminal 后事件与不一致的 indexed Tool Call 都被拒绝。\n\n三种 endpoint 都由 `ApiEndpoint::stream_with_client_and_cancellation` 发起原生 wire stream，经过\n`zeta-client` framing 后边解码边投递 canonical delta，并返回权威 terminal `ModelResponse`。\n\n## 错误语义\n\n| Condition | `ApiError` |\n| --- | --- |\n| canonical request 不满足基本 invariant | `InvalidRequest` |\n| `OperationClient` transport/operation failure | `Transport` |\n| unary response 为非 2xx | `HttpStatus(status)` |\n| response JSON、field 或 stream lifecycle 无效 | `InvalidResponse` |\n\n`From<zeta_client::ClientError>` 将 framing failure 映射为 `InvalidResponse`，其余 client failure\n映射为 `Transport`。Error text 不应包含 credential-bearing headers 或 raw sensitive payload。\n\n## 方向偏差检查\n\n- Codec 根据 provider name 或 URL 选择：profile ownership 从 provider config 漂移；\n- Provider adapter 手写 request JSON：wire ownership 从本 crate 漂移；\n- `requests::post_json` 自己重试：operation replay policy 下沉；\n- Decoder 接收 socket bytes 或管理 reconnect：framing/transport ownership 下沉；\n- Decoder 在 lifecycle validation 前向 UI 输出 event：invalid provider sequence 可能成为可信状态；\n- 本 crate 读取 credential/config store：composition authority 下沉；\n- 新建本地 canonical request type：`zeta-protocol` 不再是唯一 domain source。\n\n修改 endpoint 时同步检查 `ApiEndpoint` variants、`protocol`、`relative_path`、headers、request module、\nprovider `ApiProfile` mapping、unary tests 与 streaming decoder support。修改 canonical model field 时\n同步检查三套 `build_request`、三套 `parse_response` 和 contract fixtures。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-api\nbazel test //zeta-rs/zeta-api:zeta-api-unit-tests\n```\n\n当前 tests 重点覆盖三个 SSE decoder 的 delta mapping、Tool Call fragment assembly、usage、\nunknown optional event、terminal EOF、malformed JSON 与 Anthropic block lifecycle，并通过 injected\n`OperationClient`/provider contract tests 验证 request 与 response shape。\n\n当前三种 HTTP/SSE endpoint 已有端到端 streaming invocation；NDJSON、WebSocket、更多\nprovider-native catalog codec 和 provider-specific stream profile 仍是潜在演进。新增能力必须继续\n保持 canonical domain、wire codec、operation framing、transport 四层分离。",
    "headings": [
      {
        "depth": 2,
        "id": "crate-边界",
        "title": "Crate 边界"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "unary-调用图",
        "title": "Unary 调用图"
      },
      {
        "depth": 2,
        "id": "流式处理解码器",
        "title": "流式处理解码器"
      },
      {
        "depth": 2,
        "id": "错误语义",
        "title": "错误语义"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释 endpoint dispatch、wire codec 与 streaming event decoder 的实现边界。 Provider/runtime 的系统关系与演进见 docs/zeta-api.md。 zeta-api 在 canonical zeta-protocol model values 与具体模型 API wire format 之间转换。 它当前实现 OpenAI Responses、OpenAI-compatible Chat Completions 和 Anthropic Messages 的 unary request/response codec、原生 HTTP/SSE streaming invocation 与 terminal response assembly。 独立 input-token codec 覆盖 OpenAI Responses、Anthropic Messages、Gemini countTokens、Kimi estimate 和 Z.AI tokenizer。 它不选择 provider、model 或 credential，不拥有 base URL，也不执行 socket、retry 或 SSE framing。 Crate 边界 ``text zeta-model-provider └─ 选择 ApiEndpoint + ResolvedApiTarget └─ zeta-api ├─ canonical request → provider JSON ├─ provider JSON → canonical response └─ framed SSE event → ModelStreamEvent ├─ zeta-client：operation retry 与 SSE framing └─ zeta-http-client：HTTP transport ` 本 crate 直接 re-export zeta-protocol 的 ModelRequest、ModelResponse、Message、 ContentPart、ToolDefinition、ToolCall 等 canonical types。不要在这里创建第二套 request/response domain model。 公共契约 Symbol 职责 不负责 --- --- --- ApiEndpoint 指定 concrete endpoint family 并 dispatch codec provider 或 model selection ApiProtocol 暴露 endpoint 使用的 normalized protocol family URL/provider 推断 ApiEndpoint::completewithclient 校验 canonical request，执行 unary encode/call/decode retry、credential refresh ApiEndpoint::countinputtokenswithclient dispatch OpenAI Responses / Anthropic token-count codec 准确度解释、调用频率、预算策略 InputTokenCountEndpoint dispatch concrete provider preflight codec model eligibility、exact/estimated 判断、保守余量 OpenAiResponsesSseDecoder Responses SSE event schema 与 terminal lifecycle SSE byte framing、reconnect AnthropicMessagesSseDecoder Messages content-block lifecycle 与 canonical delta transport liveness、tool JSON accumulation ApiError request、transport、status 与 response codec failure provider selection error ApiEndpoint 是 wire contract，不是 vendor identity。同一 provider 可以暴露多个 profile；相同 compatible profile 也不能据此假设 cache、usage、error 或 streaming 语义完全相同。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- ApiEndpoint::method crate-private 当前所有 endpoint 使用 POST transport method 不由 provider adapter 重写 ApiEndpoint::relativepath crate-private responses、chat/completions、v1/messages path 属于 endpoint protocol ApiEndpoint::headers crate-private 合并 target headers，并补 Anthropic version credential headers 仍由 provider runtime 提供 validaterequest private 拒绝空 model/input 与零 max tokens 在任何 transport 调用前执行 requests::postjson crate-private JSON serialization、ClientRequest、status 与 JSON parse 不选择 retry policy或 codec requests::::complete crate-private 对应 endpoint 的 build/call/parse pipeline endpoint dispatch 的唯一 codec target requests:: googlecounttokens,kimiestimatetokens,zaitokenizer crate-private provider count request/response JSON 不声明准确度或调用频率 requests::::buildrequest private canonical input → endpoint JSON 不读取 provider config requests::::parseresponse private endpoint JSON → canonical output/usage/stop reason malformed response fail closed OpenAiResponsesSseDecoder::decodeevent private event type dispatch、delta extraction、terminal transition 不解释 raw SSE bytes AnthropicMessagesSseDecoder:: startmessage,startblock,decodeblockdelta,stopblock,stopmessage private enforce message/block state machine lifecycle 不能下沉到 UI ContentBlockKind private 将 block kind 与允许的 delta kind 绑定 unknown block 可以忽略，known mismatch 必须拒绝 Unary 调用图 `text ApiEndpoint::completewithclient target, model, request, client ├─ validaterequest └─ endpoint-specific requests::::complete ├─ buildrequest └─ requests::postjson ├─ serdejson::tovec ├─ ClientRequest::new │ ├─ ApiEndpoint::method │ ├─ ApiEndpoint::relativepath │ └─ ApiEndpoint::headers ├─ OperationClient::execute ├─ reject non-2xx as ApiError::HttpStatus └─ serdejson::fromslice └─ parseresponse ` Token preflight 使用相同 canonical ModelRequest，发送到 profile 声明的 concrete count endpoint， 并只返回 InputTokenCount。OpenAI/Anthropic count 复用对应 invocation builder；Kimi/Z.AI 从 Chat Completions builder 中只保留各自文档允许的 input 字段；Gemini 把 canonical request 显式转换为 native generateContentRequest。本 crate 不把 exact/estimated 契约编码进 wire value；准确度由 zeta-model-provider adapter 声明。普通 Chat Completions 没有标准 count endpoint，不能把任意 compatible provider 自动当作可计量。 三套 codec 都处理 canonical messages、tools、tool choice、reasoning、usage 和 stop reason，但 只共享 mechanical helpers；不能因为 JSON 外形相似就合并 protocol-specific semantics。 流式处理解码器 Decoder 接收 zeta-client::SseFrame，说明 SSE field parsing 已经完成。它们输出 zetaprotocol::ModelStreamEvent： `text SseFrame::Event └─ Decoder::decode ├─ parse event.data JSON ├─ determine event type ├─ validate lifecycle/schema └─ emit TextDelta / ReasoningDelta / no event end of stream └─ Decoder::finish └─ require protocol terminal state ` OpenAiResponsesSseDecoder： - text 与 reasoning-summary delta 分别映射为 canonical delta； - response.completed 进入 terminal； - response.failed/response.incomplete 返回 failure； - terminal 后的任何 event、或 terminal 前 EOF 都是 invalid response； - unknown optional event 与 non-event frame 被忽略。 AnthropicMessagesSseDecoder： - 要求 messagestart → contentblock → messagestop； - 用 BTreeMap<u64, ContentBlockKind 跟踪并行 block index； - text/thinking delta 映射为 canonical delta； - ping 不形成输出；message-level usage/stop reason、signature 与 tool input JSON fragment 进入 terminal response assembly； - 未 start 的 delta、重复 block、unknown stop、open block 上的 message stop、terminal 前 EOF 都被拒绝。 OpenAiChatCompletionsSseDecoder： - 按 choice 与 tool-call index 重组 text 和 function argument fragment； - 接受独立 usage chunk，并要求 DONE terminal； - finish reason、usage 和重组后的 Tool Calls 进入 terminal response； - malformed chunk、terminal 前 EOF、terminal 后事件与不一致的 indexed Tool Call 都被拒绝。 三种 endpoint 都由 ApiEndpoint::streamwithclientandcancellation 发起原生 wire stream，经过 zeta-client framing 后边解码边投递 canonical delta，并返回权威 terminal ModelResponse。 错误语义 Condition ApiError --- --- canonical request 不满足基本 invariant InvalidRequest OperationClient transport/operation failure Transport unary response 为非 2xx HttpStatus status response JSON、field 或 stream lifecycle 无效 InvalidResponse From<zetaclient::ClientError 将 framing failure 映射为 InvalidResponse，其余 client failure 映射为 Transport。Error text 不应包含 credential-bearing headers 或 raw sensitive payload。 方向偏差检查 - Codec 根据 provider name 或 URL 选择：profile ownership 从 provider config 漂移； - Provider adapter 手写 request JSON：wire ownership 从本 crate 漂移； - requests::postjson 自己重试：operation replay policy 下沉； - Decoder 接收 socket bytes 或管理 reconnect：framing/transport ownership 下沉； - Decoder 在 lifecycle validation 前向 UI 输出 event：invalid provider sequence 可能成为可信状态； - 本 crate 读取 credential/config store：composition authority 下沉； - 新建本地 canonical request type：zeta-protocol 不再是唯一 domain source。 修改 endpoint 时同步检查 ApiEndpoint variants、protocol、relativepath、headers、request module、 provider ApiProfile mapping、unary tests 与 streaming decoder support。修改 canonical model field 时 同步检查三套 buildrequest、三套 parseresponse 和 contract fixtures。 测试、限制与演进 `text cargo test -p zeta-api bazel test //zeta-rs/zeta-api:zeta-api-unit-tests ` 当前 tests 重点覆盖三个 SSE decoder 的 delta mapping、Tool Call fragment assembly、usage、 unknown optional event、terminal EOF、malformed JSON 与 Anthropic block lifecycle，并通过 injected OperationClient`/provider contract tests 验证 request 与 response shape。 当前三种 HTTP/SSE endpoint 已有端到端 streaming invocation；NDJSON、WebSocket、更多 provider-native catalog codec 和 provider-specific stream profile 仍是潜在演进。新增能力必须继续 保持 canonical domain、wire codec、operation framing、transport 四层分离。"
  },
  {
    "slug": "crates/app-server",
    "sourcePath": "zeta-rs/app-server/README.md",
    "title": "zeta-app-server",
    "description": "zeta-app-server 是产品客户端与 Zeta domain/runtime 的 application boundary。它解析 zeta-app-server-protocol request，调用 SessionCoordinator、Thread controller、 TurnExecutor 与 ConfigStore，再返回 typed resul…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 JSON-RPC dispatch、local composition、update broker、resource store 与 review\n> model adapter。External method contract 见\n> [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md)，canonical product model 见\n> [`docs/protocol.md`](../../docs/protocol.md)，workspace 搜索的跨层 ownership 见\n> [`docs/search.md`](../../docs/search.md)，外部 MCP client runtime 的跨 crate 语义见\n> [`docs/mcp.md`](../../docs/mcp.md)，Git/SCM 跨进程 ownership 见\n> [`docs/git.md`](../../docs/git.md)，Workspace identity 与 trust boundary 见\n> [`docs/workspace-security.md`](../../docs/workspace-security.md)。跨文件内容搜索的执行实现见\n> [`zeta-rs/search/README.md`](../search/README.md)。Desktop 浏览器能力的跨进程所有权和当前限制见\n> [`docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md#7-浏览器能力)。\n\n`zeta-app-server` 是产品客户端与 Zeta domain/runtime 的 application boundary。它解析\n`zeta-app-server-protocol` request，调用 `SessionCoordinator`、Thread controller、\n`TurnExecutor` 与 `ConfigStore`，再返回 typed result 或发布 canonical update。\n\n它不实现 reducer，不定义第二套 Session/Thread/Turn model，也不拥有 SQLite schema。\n\n## 运行时所有权\n\n```text\nJSONL / in-process caller\n└─ AppServer + ConnectionState\n   ├─ JSON-RPC validation / initialization gate\n   ├─ protocol method dispatch\n   ├─ SessionCoordinator / ThreadController\n   ├─ MultiAgentCoordinator → child Thread spawn/context seed + durable delivery/join/cancellation\n   ├─ TurnBackendHandle → exact Turn.model provider router\n   │                   ├─ TurnExecutor (direct provider)\n   │                   └─ CodexTurnExecutionBackend (openai-chatgpt subscription)\n   ├─ ConfigStore\n   ├─ optional WorkspaceFileSystem + filesystem watcher\n   ├─ optional GitRuntime → zeta-file-watcher + GitService → zeta-git\n   ├─ optional SearchService → zeta-search\n   ├─ optional CodeIndexRuntime → zeta-code-index + filesystem watcher\n   ├─ optional SymbolIndexRuntime → zeta-symbol-index + CodeIndex source/overlay authority\n   ├─ optional CodeIndexSemanticService → local SQLite vectors + host model adapters\n   ├─ optional CloudCodeIndexController → zeta-code-index-cloud + host provider registry\n   ├─ request-scoped CodeRetrievalService → symbol/lexical/semantic/remote RRF + verification + budget\n   ├─ optional TerminalService → zeta-utils-pty\n   ├─ optional LanguageServerProviderRegistry → zeta-language-service → zeta-lsp\n   ├─ reloadable MCP Tool generation + status/OAuth/form interaction → zeta-mcp-extension → zeta-mcp\n   ├─ ConnectorCredentialService → list/connect/disconnect + ready MCP reconcile\n   ├─ client-hosted dynamic tools → durable Agent interaction owner\n   ├─ BrowserHost → connection-owned reverse JSON-RPC browser capability\n   │              → built-in browser Tool port + one-time approval policy\n   ├─ read-only/capability ToolExecutor contributions → zeta-extension-api\n   ├─ optional Web Search executor → zeta-web-search-extension\n   ├─ trusted Workspace Agent tools → spawn_agent / send_agent_message / wait_agent\n   ├─ Skill RPC/config/event adapters → zeta-skills-extension\n   │                                  → zeta-skills + zeta-file-watcher\n   ├─ WorkspaceCustomizations → zeta-instructions + zeta-agents\n   ├─ connection-owned ResourceStore\n   └─ UpdateBroker → session/update, session/thread/update, config/changed, connector/changed, skills/changed, git/statusChanged, fs/changed\n```\n\nApp Server connection 不是 product Session。关闭 connection 只失去 connection-local subscription、\nrequest-ID set、notification queue 与 resource ownership。默认的 `SessionStateMode::Durable` 由\nCore/store 继续拥有 Session/Thread durable state；需要进程内生命周期的 host 可以显式选择\n`SessionStateMode::Ephemeral`，此时 coordinator 使用 in-memory stores，Session/Thread 与用户消息\n不会从 profile SQLite 恢复或写入。ConfigStore 仍可按 profile 保存普通配置。\n\n## 公共契约\n\n| Symbol | 职责 |\n| --- | --- |\n| `AppServer` | JSON-RPC application server 与 domain composition handle |\n| `ConnectionState` | 每个 logical connection 的 initialized/request-ID/notification state |\n| `AppServer::new` | 用 recovered `SessionCoordinator` + `ModelService` 构造 server |\n| `AppServer::connection` | 分配 connection ID 并注册 notification queue |\n| `AppServer::connection_notifications` | 返回可阻塞等待、主动唤醒的 connection outbound message source |\n| `AppServer::close_connection` | 注销 subscription、关闭 notification source，并释放 Resource/Terminal/Syntax owner |\n| `AppServer::handle_json` | 处理一个 JSON-RPC request string |\n| `AppServer::drain_notifications` | JSONL 与同步适配 caller 取出该 connection 的 serialized notifications |\n| `AppServer::{serve_stdio,serve_jsonl}` | 同步 JSON Lines service loop |\n| `AppServer::create_resource` | 创建 5 分钟 TTL 的 connection-owned resource |\n| `AppServer::with_config_store` | 开启 config/provider/MCP/Skill RPC |\n| `AppServer::with_connector_service` | 开启 Connector RPC、capability 与 generation notification watcher |\n| `AppServer::with_slash_command_catalog` | 安装 initialize 时下发的 immutable 动态命令 snapshot |\n| `AppServer::with_file_system` | 注入受 workspace 约束的 filesystem authority |\n| `AppServer::with_file_system_watcher` | 监听可信 workspace root 并发布相对路径 invalidation hint |\n| `AppServer::with_git_root` | 冻结 workspace root，开启 Git status/mutation、watcher 与 revision notification |\n| `AppServer::with_workspace_search` | 注入 workspace root 与冻结的 ripgrep executable，构造外部内容搜索服务 |\n| `AppServer::with_language_server_providers` | 注入已验证、已安装的 provider registry；activation confirmation 启用 packaged route，显式 Config `Disabled` 仍可关闭 |\n| `AppServer::with_language_marketplaces` | 安装 TUF catalog、activation authority 与共享 runtime adapter；提供 list/install RPC，成功后热替换 registry |\n| `AppServer::with_code_index_storage_root` | 配置按 root identity 分隔的 persistent index cache |\n| `LocalCodeIndexProviders::with_semantic_models` | 在 Workspace activation 前注入本地 semantic 使用的 immutable embedding/rerank adapters |\n| `AppServer::with_cloud_code_index_providers` | 注入冻结的 provider registry；空 registry 不广告 cloud capability |\n| `AppServer::with_cloud_code_index_storage_root` | local composition 配置按 root identity 分隔的 durable grant/deletion state |\n| `AppServer::with_tool_service` | 安装同一 server 内所有 Turn 使用的 Core Tool/Policy ports |\n| `AppServer::with_mcp_oauth_service` | 安装独立 Config MCP 的 process-local OAuth coordinator，并广告 `mcpOAuth` capability |\n| `AppServer::with_codex_turn_backend` | 将 `openai-chatgpt` 模型路由到 Codex complete-Turn backend；不读取登录状态做隐式切换 |\n| `AppServer::with_dynamic_tools` | 校验 client-hosted dynamic specs，并接入共享 registry、审批和 durable interaction 执行链 |\n| `AppServer::resume_recovered_agent_coordinations` | 恢复 Agent spawn/delivery/join/cancellation saga，并调度恢复期间新建的 child Turn |\n| `open_local_app_server` | 按 SessionStateMode 选择 durable/in-memory coordinator，打开 config 并组合 provider-backed model |\n| `open_local_app_server_with_cloud_providers` | 在 Workspace 激活前注入 cloud code-index providers；默认入口使用空 registry |\n| `open_local_app_server_with_code_index_providers` | 在 Workspace 激活前同时注入本地 semantic models 与可选 cloud providers |\n| `LocalAppServerOptions` | user profile root + SessionStateMode + optional Workspace/Connector/language-provider runtime + Codex App Server options + validated slash catalog + built-in Skill root selection + optional model operation client/MCP OAuth providers |\n| `LocalAppServerOptions::with_language_server_providers` | 在 local App Server 启动前注入额外 provider registry；receipt registry 由 composition 自动合并 |\n| `LocalAppServerOptions::with_remote_language_marketplace` | 注入 product-pinned TUF root；同步 signed catalog 并从 profile activation receipt 重建 provider |\n| `LocalAppServerOptions::with_mcp_oauth_providers` | 把 exact MCP server ID → provider adapter 注入使用共享 SecretStore 的 OAuth service |\n| `LocalConnectorRuntime` | Connector credential service + shared SecretStore + Plugin-specific MCP materializer |\n| `LocalAppServerOptions::with_plugin_activation` | 从 exact activation 构造 package-rooted Connector/MCP runtime |\n| `LocalConnectorRuntime::from_plugin_activation` | activation → Connector catalog + SQLite authority + Plugin MCP provider |\n| `SessionStateMode` | 明确选择 profile SQLite durable history 或 process-local ephemeral Session/Thread state |\n| `BuiltInSkillRoot` | auto-detected release root、explicit test/host root 或 unavailable 的自解释选择 |\n| `zeta_slash_commands::SlashCommandCatalog` | 委托共享 crate 校验动态命令并冻结 server-advertised snapshot；App Server 只拥有 composition |\n| `ReviewModelResolver` | 从 frozen config snapshot 选择 review-only model |\n| `ProviderReviewModel` | `ModelInvoker → zeta_auto_review::ReviewModel` adapter |\n\n`AppServer::new` 默认用 `TurnExecutor::without_tools`。`with_tool_service` 才会替换为有 Tool 和\nPolicy port 的 executor。`open_local_app_server` 会从 user config snapshot 连接明确 `enabled`\n的 unauthenticated 或 SecretStore-backed MCP server，并把 catalog 与本地工具组合。Host 可用\n`LocalAppServerOptions::with_plugin_activation` 注入固定 activation，或用 `with_plugin_authority` 注入 live\nauthority；两者都会自动构造 Connector catalog、SQLite authority 与 package-rooted Plugin MCP provider。\n没有注入时，local composition 打开 `<profile>/plugins` 的 durable `PluginActivationAuthority` 和按 profile\n隔离的 `KeyringSecretStore`。Authority generation 会重建 Connector definitions 与 Plugin MCP provider，\n并复用 MCP safe-point replacement。Keyring operation\n失败会 fail closed，不自动降级到文件副本；host 仍可通过显式 runtime 注入 `FileSecretStore`。Config、Connector\n或 MCP list-changed hint 会在后台构建新 generation；每次 model invocation 同时冻结可见 definitions 和响应后的 binder，因此 watcher 在模型\n响应前发布新 registry 也不会把旧响应劫持到同名新工具。已绑定调用继续持有原 Tool/Policy generation，\n直到 execute 排空；Plugin-backed call 在 dispatch 前获取 exact activation lease，Connector-bound call 再\n额外复核 live connection generation/digest，\ndisconnect 会等待已经 dispatch 的调用结束。每次 MCP tool\ncall 仍必须经过 durable one-time approval。Host 通过\n`LocalAppServerOptions::with_mcp_oauth_providers` 注入具体 provider 后，local composition 使用 Connector\nruntime 同一个 SecretStore 构造 `McpOAuthService`；App Server 只暴露 PKCE/callback/refresh/revoke RPC，\n不拥有 provider discovery、scope 或 token wire。Host 安装的 read-only\nextension executor（当前包括统一的 `skills-read`）和 client-hosted dynamic tools 也进入同一个 registry，\n不得绕过 binding、policy 或 durable result commit。它仅在调用方通过\n`LocalAppServerOptions::with_workspace_root` 提供统一 Workspace 根时同时组合 filesystem、\n`.zeta` 自定义 catalog、Workspace code index、Workspace search、Git SCM、connection-owned Terminal runtime、\n只读 `rg` registry；Zeta CLI 的 stdio 与\nin-process 路径都会使用同一个启动时解析结果：\n`ZETA_WORKSPACE_ROOT` 优先，否则使用当前目录。不能因为 protocol 暴露 approval interaction 就\n假设任意自定义 host 已经拥有 Tool registry。`rg` 安装候选来自\n[`zeta-install-context`](../install-context/README.md)，App Server 只负责把候选交给\n`RipgrepExecutable` 验证并组合成 Tool service。\n\nWeb Search 默认不注册。Embedding Tool Search 只负责在当前 registry 中找工具，与外部网页检索无关。\nHost 只有通过 `LocalAppServerOptions::with_web_search_backend` 注入\n`zeta-web-search-extension::WebSearchBackend` 后，`web_search` 才进入 eager tool set；其 endpoint host\n和 optional credential reference 会冻结到 action digest，并要求 exact one-time approval。共享扩展仅\n提供 provider-neutral contract 与 JSON HTTP adapter，不硬编码 Codex 私有 Search endpoint。\n\n默认 local composition 使用 lazy production model client：启动、配置读取和模型目录展示不会加载\nsystem roots 或 proxy。Embedded test 可通过 `LocalAppServerOptions::with_model_operation_client`\n注入离线 client；真实 transport 初始化失败在第一次模型 operation 返回，不得让 App Server 启动 panic。\n\nlocal composition 会配置 `<profile>/code-index-cloud` 的 durable state 位置，但默认 provider registry\n为空，因此不会安装 cloud controller、广告 `cloudCodeIndex` 或创建网络请求。具体 host 只有在注入\n接受 Workspace-owned exact chunks 且满足幂等 grant deletion 的 provider 后，才能启用云能力。\n\nlocal composition 同时配置 `<profile>/code-index-semantic` 并安装共享的\n`SemanticModelProvider` resolver，但 semantic CodeIndex 默认仍关闭，所以不会后台发送 chunks 或创建\nvectors。用户选择模型并对 exact Workspace 授权源码外发后，Trusted Workspace 的 lexical generation\n更新才会在 refresh worker 中同步本地 semantic SQLite。模型只返回 embedding/rerank 结果，召回与\n排序仍由本地 domain crate 完成；host 也可用 `LocalCodeIndexProviders::with_semantic_models` 注入测试\n或专用 immutable adapters。\n\nTrusted Workspace 同时获得 built-in read-only `search_code` 工具。它只接受\n`workspace-code-index-read-only` exact grant，调用 canonical `CodeRetrievalService`，并返回 bounded、\ncurrent-source-verified excerpts 与 degradation；未配置 semantic 时自然退回 lexical。semantic grant\n精确绑定 Workspace、model selection 与 provider config，provider URL 或模型变化会卸载旧 runtime 并\n要求重新授权。Desktop 当前可配置 Ollama/unauthenticated OpenAI-compatible endpoint；OpenAI API key\n仍需 host 注入 secure `SecretStore`，尚无产品 credential UI。\n\nTool Search 拥有独立的 `toolSearch.embeddingModel`，不复用 CodeIndex 的模型选择。只有 User Config\n明确设置 `toolSearch.mode = \"hybridEmbedding\"` 才会调用；默认 `lexical` 不产生 embedding 请求。\n`toolSearch/configure` 先解析 exact provider/model 并执行固定文本 readiness probe，失败返回\n`ToolSearchUnavailable` 且不提交配置。外部配置或启动恢复遇到不可用模型时，App Server 通过\n`config/read.toolSearch.embeddingStatus` 报告原因，hybrid 自然语言搜索明确失败；只有用户显式切回\n`lexical` 才启用纯 BM25。Regex 始终是本地显式策略。门禁通过后，Tool Search 只缓存当前 registry\ngeneration 的工具向量；实际调用或响应校验失败会使该次搜索明确失败，不静默回落到 BM25。\n\nTab 关闭的停止语义属于本层适配，而不是 `zeta-protocol` 的新 command：前端通过\n`session/request` 的 `Stop` operation 到达 Session mutation dispatcher，后者调用 Core 的\n`SessionCoordinator::stop`，再从 durable gap 发布 Session 和 child Thread updates。单纯\n`AppServer::close_connection` 只释放 connection-owned delivery/resource state，不停止产品\nSession。\n\n## 文件与职责\n\n```text\nsrc/\n├── server.rs\n│   └── server/\n│       ├── operations.rs          # Session/Thread/Turn/Resource methods\n│       ├── config_operations.rs   # Config/provider/MCP/Skill methods + DTO conversion\n│       ├── config_runtime.rs      # Config commit → config/changed fanout\n│       ├── connector_operations.rs # list/API-token connect/disconnect DTO adapter\n│       ├── connector_runtime.rs   # authority generation → connector/changed fanout\n│       ├── skill_operations.rs    # Skill catalog/enablement/resource DTO conversion and error mapping\n│       ├── start_turn.rs           # durable command replay before mutable model/Skill resolution\n│       ├── workspace_customizations.rs # Instruction/Agent catalogs + reloadable harness snapshot\n│       ├── fs_operations.rs       # root-relative filesystem DTO conversion/error mapping\n│       ├── fs_watcher.rs          # root watcher、内部 observer、相对路径投影与 fs/changed 发布\n│       ├── git_operations.rs      # Git RPC decode 与稳定错误映射\n│       ├── git_runtime.rs         # status projection/revision、watcher、去重与通知\n│       ├── interaction_runtime.rs # pending interaction deadline enforcement\n│       ├── multi_agent_tools.rs   # Core coordinator Tool adapter + child Turn scheduling\n│       ├── notification_queue.rs  # bounded per-connection queue + wake/close semantics\n│       ├── search_operations.rs   # search RPC decode、ownership 与稳定错误映射\n│       ├── code_index_operations.rs # status/search/rebuild DTO 与 error mapping\n│       ├── code_index_runtime.rs  # generation lifecycle、watcher reconcile 与 stale gate\n│       ├── cloud_code_index_operations.rs # preview/grant/sync/revoke DTO 与稳定错误映射\n│       ├── terminal_operations.rs # terminal RPC decode、ownership 与稳定错误映射\n│       ├── language_runtime.rs   # config + built-in/provider definitions → shared language-service\n│       ├── language_marketplace_runtime.rs # TUF catalog + activation + base provider registry rebuild\n│       ├── language_marketplace_operations.rs # list/install DTO、错误映射与热 registry replacement\n│       └── update_broker.rs       # per-connection subscription/cursor/fanout\n├── local.rs                       # local composition, session backend selection + model safe point\n├── local_tools.rs                 # frozen rg registry + Core Tool/Policy adapters\n├── browser_host.rs                # reverse JSON-RPC broker、target owner、timeout/cancellation + screenshot resource binding\n├── browser_tool.rs                # semantic browser Tool definitions、input validation 与 approval policy\n├── dynamic_tools.rs               # dynamic spec validation + durable interaction Tool adapter\n├── extension_tools.rs             # read-only/capability contributors → reviewed ToolExecutor port\n├── tool_executor_adapter.rs       # frozen payload/binding → ToolExecutor invocation\n├── tool_composition.rs            # local/MCP/dynamic/extension routing + generation-safe replacement\n├── review.rs                      # review-only provider adapter\n├── resource_store.rs              # per-resource/per-connection bounded in-memory resources\n├── git_service.rs                 # workspace root + GitClient + synchronous RPC runtime bridge\n├── terminal_environment.rs        # secret-excluding host environment、platform normalization 与 terminal identity\n├── terminal_profiles.rs           # trusted Shell discovery、ID 与 frozen environment\n└── terminal_service.rs            # PTY runtime、output ring 与 connection-owned sessions\n```\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `AppServer::dispatch` | private | initialization gate 后对 `ClientMethod` exhaustive dispatch | method string lookup只来自 protocol registry |\n| `AppServer::session_request` | `pub(super)` | 解码 canonical `session/request`，按 typed operation 调用 Session/Thread/Turn helper 并发布 durable gaps | 不在 App Server reducer 或 `zeta-protocol` 中复制 mutation model |\n| `decode<T>` | crate-private | params JSON → typed DTO，统一 InvalidParams | operation 不手读 arbitrary fields |\n| `result<T>` | crate-private | typed result → JSON，统一 serialization failure | external result shape 由 protocol DTO 决定 |\n| `core_error` | crate-private | Core error → stable App Server error | 不回传 internal error string |\n| `RpcError` | crate-private | internal code + stable `AppServerErrorName` | 不成为第二套 public error DTO |\n| `notify_session_updates` / `notify_thread_updates` | private methods | 从 durable store gap 读取并交给 broker | mutation 后从 authoritative history 发布 |\n| `AppServerThreadUpdates` | private sink | 将 TurnExecutor live/committed update 接入 broker | 不修改 canonical update |\n| `UpdateBroker` | private | subscription、durable cursor、weak queue fanout | 不持有 connection/session runtime authority |\n| `UpdateBroker::publish_thread_update` | private | committed 按 durable cursor；transient 直接给 subscriber | 两类 cursor semantics 不得混合 |\n| `BrowserHost` | crate-private | capable connection、pending reverse request 和 exact target owner 的唯一 broker | 不执行 Electron/CDP，不选择活动标签，不暴露任意 host method |\n| `BrowserHost::request` | private | 产生独立字符串 ID、发布 Server → Client request、等待 30 秒 deadline 并观察取消 | 不复用 client numeric request-ID set；取消发送 `$/cancelRequest` |\n| `BrowserHost::handle_response` | crate-private | 校验 owner、terminal response 和 retired request outcome | 非 owner 不能移除 pending；只忽略已放弃请求的晚到响应 |\n| `BrowserHost::register_screenshot` | private | 校验 PNG MIME、长度、Base64 和 signature，再创建 connection-owned Resource | 不信任 host 声明的媒体类型，不把 bytes 放进 Tool result |\n| `BrowserToolService` | crate-private | 十个内置语义浏览器工具的 schema、materialization 和 Core capability 调用 | URL/node/target 校验不委托给模型或 Desktop |\n| `BrowserToolPolicy` | crate-private | 只接受 built-in `BrowserInteraction` + 单个 `UserInterface` capability，并要求一次性批准 | 不复用 local shell、MCP 或 extension policy |\n| `InteractionDeadlineWatcher` | crate-private | 在 workspace mutation gate 下重检 durable pending request，持久化 `DeadlineElapsed` cancellation 并失败 Turn | 不选择 UI、不解释 approval policy、不修改 reducer |\n| `zeta-skills-extension::SkillRuntime` | external runtime | 组合 roots、缓存 metadata projection、叠加 enablement，并贡献 durable activation/context fragment | App Server 只安装 runtime、提供 config/event adapter 与协议 projection |\n| `start_turn::replayed_result` | private | 用 durable command receipt 校验重复 `StartTurn` 输入并返回原 Turn 结果 | 重放不重新读取 model config 或 Skill 文件；不同输入返回 `CommandConflict` |\n| `SkillConfigSnapshotProvider` adapter | crate-private implementation | 给 external runtime 最新 `SkillsConfig` 与 commit signal | implementation 不把客户端 path 直接升级为 trusted root |\n| `SkillRuntimeEventSink` adapter | `UpdateBroker` implementation | 把 runtime generation change 投影为 `skills/changed` | 不参与 catalog reconcile |\n| `WorkspaceCustomizations` | crate-private | 发现/刷新 `.zeta/instructions` 与 `.zeta/agents`，发布 future-invocation harness snapshot | 不执行 Agent、不激活 Skill、不解析外部生态格式 |\n| `WorkspaceFileChangeSink` | crate-private trait | 在 `fs/changed` 发布前把 projected invalidation 交给内部 runtime owner | callback 不把 watcher event 当作文件事实 |\n| `notification<T>` | private | canonical update → JSON-RPC notification | method 来自 `ServerNotificationMethod` |\n| `ResourceStore::resource` | private | cleanup + owner check | 所有 read/release 必须经过这里 |\n| `ResourceStore::cleanup` | private | lazy TTL eviction | resource 不持久化 |\n| `AppServer::file_system` | private | 读取注入的 `WorkspaceFileSystem` 或返回稳定 unavailable error | 不绕过 workspace authority |\n| `fs_watcher::project_event` | private | watcher path → root-relative invalidation 或 rescan hint | 不把 event 当作文件内容事实 |\n| `GitService` | crate-private | 持有 `MutateRepository` 的 `TrustedWorkspace`、映射 repository path、持有 Tokio runtime，并调用 `zeta-git` query/mutation API | 不从 client path 或 repository config 自行授予 trust |\n| `GitRuntime` | crate-private | 串行 operation、为每次 runtime incarnation 创建 `StreamInstanceId`、投影 workspace status、推进实例内 revision 并发布去重 notification | watcher event 不直接成为 Git truth |\n| `project_status` in `git_runtime` | private | `zeta-git` snapshot → renderer-safe protocol DTO | 不回传绝对 metadata path 或 internal stderr |\n| `file_type` in `fs_operations` | private | foundation file kind → protocol DTO | wire enum 只由 protocol crate 定义 |\n| `search_operations::{search_query, search_page}` | private | `WorkspaceSearch*` DTO 与 `zeta-search` 领域类型之间的显式转换 | 不复制查询校验、rg argv、job state 或 parsing |\n| `SearchService` | external crate | 持有 active workspace、frozen rg 和 owner-bound job map | App Server 不把 connection/DTO/UI 语义写入该 crate |\n| `CodeIndexRuntime` | crate-private | 串行 rebuild/refresh，投影 lifecycle，并在返回前 materialize | 不拥有 scan/chunk/schema，也不创建网络请求 |\n| `CodeIndexRefreshWorker` | private | 单 wake + merged paths/rescan priority 的后台刷新 | 不阻塞 filesystem notification thread，不建立无界 event queue |\n| `SymbolIndexRuntime` | crate-private | 在 canonical source generation 后 reconcile，保持 last-ready/stale 状态，并暴露 current index | 不扫描 Workspace、不请求 LSP、不拥有 UI fusion |\n| `WorkspaceDocumentOverlay` composition | crate-private | 先同步 CodeIndex canonical dirty snapshot，再投影 SymbolIndex；close/replacement 同步清理 | 不把 overlay 持久化或在每次编辑触发 embedding |\n| `CodeIndexSemanticService` | external crate | 同步 exact lexical generation、复用/持久化 vectors、本地 recall/rerank | App Server 不解释模型分数或拥有 vector schema |\n| `code_index_operations::project_status` | private | runtime state + last usable snapshot → stable protocol status | 不暴露 SQLite/internal error text |\n| `CloudCodeIndexController` | external crate | root-bound grant、publication/deletion lifecycle 与 provider port | App Server 不复制 consent state 或允许 provider 直接读 Workspace |\n| `cloud_code_index_operations::project_status` | private | cloud lifecycle + grant → stable protocol DTO | 不回传 credential、绝对路径或 provider error text |\n| `TerminalService` | crate-private | 持有 `ExecuteProcess` 的 `TrustedWorkspace`、Tokio runtime、PTY session map 与 1 MiB output ring | 不从 client path 自行授予 process authority |\n| `AppServerLanguageRuntime::configured_provider_definitions` | private | 只对 Config 显式启用的已注册 provider 生成 definition，并保留 authoritative native override | 不下载包、不自己启动 child、不复制 restart policy |\n| `TerminalEnvironment` | crate-private | 二次过滤 host environment、规范化 Windows key、固定 `TERM`/`COLORTERM`/`TERM_PROGRAM` | 不继承凭据或接受 client mutation |\n| `TerminalProfileCatalog` | crate-private | 冻结可信 Shell Profile、program 与 `TerminalEnvironment` | external DTO 不暴露 executable/args/environment |\n| `TerminalService::create` | crate-private | 将 default/profile ID 解析到 catalog 并启动 workspace-rooted PTY | client 不能提交 executable/environment |\n| `spawn_output_drainers` | private | raw output/exit 并发收束；尾部输出 EOF 后才标记 exited | 不在 exit code 到达时提前丢弃尾部 bytes |\n| `read_state` | private | after-sequence cursor → bounded Base64 chunks + gap/exited state | ring eviction 必须显式返回 `output_gap` |\n| `ConfigBackedModelService::resolve_config` | private | user snapshot + optional Workspace snapshot merge | 每次 invocation safe point 重新解析 |\n| `WorkspaceConfigTracker::read` | private | 内容变化才推进 synthetic workspace revision | 不监听/修改 workspace file |\n| `compose_local_tools_with_config` | crate-private | 要求 root-bound `ExecuteProcess` capability、组合 Host/User/Workspace exec-policy snapshot、冻结统一 revision、解析安装候选与 native sandbox | containment、trust、policy composition 或 discovery 失败时不降级成 unrestricted |\n| `LocalExecPolicyConfig::from_resolved` | crate-private | 从 safe-point `ResolvedConfig` 提取 User rules 与 Workspace restrictions | 不读取文件或自己决定 layer trust |\n| `LocalShellToolService::materialize` | private | parse call、约束 workspace 参数、冻结 rg executable | policy review 前不启动进程 |\n| `LocalShellPolicy::decide` | private | 将 frozen `ExecPolicySnapshot` 交给 `ActionPolicyEngine`，返回 exact typed decision | 不复制 rule precedence 或绕过 grant binding |\n| `HookRuntime` | crate-private | immutable Hook snapshot → exact safe-point match → host policy → sandboxed process executor | 不拥有 Hook Config persistence、Core scheduling 或交互式批准 UI |\n| `NativeHookProcessExecutor` | private | trusted Workspace + canonical action → shared process executor | Restricted Workspace 不构造此 adapter，不自行放宽 sandbox |\n| `zeta_mcp_extension::McpRuntimeOwner` | ext/mcp private | worker thread 持有 Tokio runtime 和 live `McpRuntime` | Core thread 不嵌套 `block_on` |\n| `zeta_mcp_extension::McpToolService::review_request` | ext/mcp private | exact binding/arguments/generation → MCP action digest | remote annotation 不授予只读信任 |\n| `zeta_mcp_extension::McpApprovalPolicy::decide` | ext/mcp private | 只接受已知 user MCP provenance 并返回 one-time approval | 不自动批准远端副作用 |\n| `mcp_operations::{mcp_oauth_start,mcp_oauth_complete,mcp_oauth_refresh,mcp_oauth_revoke}` | private | protocol DTO → exact Config target → `McpOAuthService` → runtime reconcile | 不解析 provider token response 或返回 secret bytes |\n| `McpRuntimeIntents::reconcile` | crate-private | 唤醒既有 Tool reconcile worker，使 OAuth/lifecycle mutation 立即生效 | 不修改 durable Config enablement |\n| `CompositeToolService` | private | frozen binding/runtime key → local、MCP、dynamic、extension runtime | duplicate name 在 composition 时失败，不按 live name 猜 executor |\n| `CompositeActionPolicyService` | private | trusted `ActionSource` → owning policy | 不依靠 trial-and-error policy fallback |\n| `ReloadableToolPorts` | crate-private | 原子替换未来 Tool generation，并为 prepared call 固定 service/policy | reconcile failure 保留上一份可用 runtime |\n| `DynamicToolService` | private | exact spec digest + arguments → durable `AgentRequest::DynamicTool`，并校验 owner response | 同名新定义不能认领旧 interaction |\n| `ExtensionToolReviewer` | private | host-installed executor + declared authority → frozen payload 与 exact Plugin provenance | capability contributor 不直接获得网络或 credential authority |\n| `ModelSnapshotResolver` | private trait | frozen config → immutable invoker | implementation 不持有 mutable config view |\n| `zeta_slash_commands::SlashCommandCatalog::new` | shared public constructor | 校验 lowercase ASCII/interior-hyphen name、非空描述与唯一性 | App Server 不复制 grammar、不执行命令、不引用 client-local commands |\n| `ProviderReviewModel::request` | private | system/input/schema → tool-disabled zero-temperature request | reviewer 不获得 Tool capability |\n| config `*_dto` / `*_from_dto` helpers | private | external DTO 与 config domain 显式转换 | invalid identity 映射 InvalidParams |\n\n## JSON-RPC 调用图\n\n```text\nAppServer::handle_json(connection, raw)\n├─ serde_json::from_str<Value>\n│  └─ ParseError\n├─ deserialize JsonRpcRequest<Value>\n├─ validate jsonrpc == 2.0 and positive numeric ID\n├─ reject duplicate request ID within connection\n├─ AppServer::dispatch\n│  ├─ initialize allowed before gate\n│  ├─ require connection.initialized\n│  ├─ client_method(request.method)\n│  └─ domain operation\n│     ├─ decode<Params>\n│     ├─ Core / Config / Resource call\n│     ├─ notify committed gaps when applicable\n│     └─ result<ResultDto>\n└─ JsonRpcSuccess or JsonRpcFailure → JSON string\n```\n\n`serve_jsonl` 对每一行先写 response，再 drain/write causal notifications。当前 JSONL loop 是\n同步串行的；owned embedded client 通过 `ConnectionNotifications` 在独立 event pump 中等待同一\nqueue 的 condition-variable wake。protocol registry 中的 `SerializationScopeDefinition`\n尚未接入并发 scheduler。\n\n同一 outbound queue 也承载 `BrowserHost` 的 Server → Client request。Desktop connection 只有在\n`initialize.capabilities.browser.version == 1` 且声明至少一种语义操作时才会注册为 browser host。\n内置浏览器 Tool port 还受 Workspace trust gate 约束：只有可信工作区与至少一个同时声明\n`observe + input` 的 live browser host 存在时才进入当前 Tool generation；最后一个完整 owner\n断开或信任撤销都会原子移除该 port。\n`BrowserHost` 使用 `browser-host:<connection>:<counter>` 字符串 ID，与 App Server 接受的 client\n正整数 ID 隔离；`serve_jsonl` 在普通 request dispatch 之前识别对应 response，并交给 broker\n完成 pending request。一次典型工具调用为：\n\n```text\nBrowserToolService::execute\n└─ BrowserHost::{create_target,observe,perform,close_target}\n   ├─ resolve capable connection / exact target owner\n   ├─ NotificationQueue::push(JsonRpcRequest)\n   ├─ Desktop browser handler\n   ├─ serve_jsonl → BrowserHost::handle_response\n   └─ typed Core result / connection-owned screenshot Resource\n```\n\n创建成功后 `target_id → connection_id` 固定；观察与动作必须回传同一 target ID。connection\n关闭会失败所有 pending request 并忘记其 target authority；Desktop Supervisor 同时关闭该连接\n创建的原生目标。取消和 30 秒超时都会先本地退休 pending request，再发送 `$/cancelRequest`；\n因此晚到的已放弃响应可忽略，已完成请求的重复响应仍是协议错误。\n\nTerminal 因此使用 bounded `terminal/read` pull：`TerminalService` 在独立 runtime 中持续 drain\nPTY raw bytes，保留最多 1 MiB，并按 sequence 返回最多 128 个 chunk。`terminal/write` 的单批\nUTF-8 输入上限为 64 KiB，rows/cols 上限均为 512；未知 ID、跨 connection 使用和 runtime\ncapacity 分别映射稳定 Terminal error。`serve_jsonl` 结束时调用 `close_owner`，不会把 PTY\n留给失效 connection。\n\n`terminal/profile/list` 从 composition 时冻结的 `TerminalProfileCatalog` 返回安全显示信息；\n`terminal/create.profile` 只能选择 default 或已列出的稳定 ID。Windows catalog 可发现 Command\nPrompt、PowerShell、Git Bash，以及 PATH 中已安装的 Zsh；Unix catalog 可发现默认 Shell 与已\n安装的常见 Shell；路径、args 和 environment 始终留在 Rust authority。Host 只传入明确的\nsession/environment allowlist，`TerminalEnvironment` 再排除未拥有变量并固定终端 identity；\n底层 PTY spawn 先 `env_clear`，不会意外继承 App Server 的其他环境。客户端提交 unknown\nterminal params（包括 `terminal/create.environment`）会在协议 decode 时返回 `InvalidParams`。\n\nInitialize 是每 connection 一次。重复 initialize 返回 `AlreadyInitialized`；初始化前的其他 method\n返回 `NotInitialized`。Request ID 只接受正整数，且在 connection 生命周期内不能重复。\n成功结果同时包含 composition 时冻结的 `slashCommands`。不同 connection 可获得同一 server\nsnapshot；单个 connection 生命周期中不会因 host 后续状态变化而更换 popup contract。\n\n## Session、Thread 与 Turn 编排\n\n典型 create path：\n\n```text\nsession/request { type: createThread }\n├─ SessionCoordinator::create_thread\n├─ subscribe caller to new Thread\n├─ notify_session_updates(previous session sequence)\n├─ notify_thread_updates(0)\n└─ return current Session + ThreadId\n```\n\n`session/request { type: rewindThread }` 是非破坏性回退：`SessionCoordinator::rewind_thread` 记录包含 source Thread、\nsource sequence 与 excluded Turn 的 Rewind lineage，再让 `ThreadController::create_rewound_thread`\n向新 Thread 写入单个 `HistoryImported` durable event。该事件只携带 checkpoint 之前的 terminal\nTurns；source Thread 及其后续历史不被改写。调用方随后订阅新 Thread，旧 Thread 仍可审计和恢复。\n\n`session/subscribe(afterSequence)` 是产品宿主使用的 aggregate port：它返回 Session snapshot、Session\ndurable gap 和每个 child Thread 的 projection，并在同一 connection 上建立\n`session/thread/update` child update delivery。\n`session/request` 是产品 mutation 的 canonical aggregate port：它携带一个 `CommandId`、Session\nsequence 和 typed `SessionRequest`，统一路由 Session lifecycle、child Thread lifecycle、Turn\nstart/interrupt 与 interaction resolve，并以 tagged `SessionRequestResult` 返回对应结果。\n旧的独立 Session/Thread/Turn mutation methods 已从 registry 和 dispatcher 删除；所有 mutation\n都通过 `session/request`。Thread snapshot 和 gap 使用带 Session scope 的\n`session/thread/read` / `session/thread/subscribe`。这些订阅都是 connection-local delivery state；\n真实 gap 来自 coordinator/store。\n\n`session/request::StartTurn`：\n\n1. 校验 Thread 属于 supplied Session；\n2. 校验 Session 仍为 active；\n3. 读取 Session 当前模型，并把它与请求携带的 `ApprovalMode` 一起作为 `TurnAccepted` 的 durable snapshot；\n4. `start_turn` 使用 typed command ID + exact expected sequence；\n5. replay 时同时校验 input、model 与 approval mode，terminal failure/interruption 不伪装成 success；\n6. 新 start 发布 durable update 后调用 `TurnExecutor::start`。\n\n`model/list` 由 `ModelCatalog` adapter 投影 shared `zeta-models-manager` 中当前已配置 provider 的\n可选模型；`session/request::SetModel` 先通过同一个 manager 的 typed resolution 校验，再提交 Session\ncommand。App Server 不读取 `ProviderDefinition.models` 或维护第二份排序/allow-unlisted 判断。全局\n`preferredModel` 只作为新 Session\n和历史无模型 Session 的默认值，不承担当前 Session 的模型切换。\n\n`session/request::ResolveInteraction` 使用 exact durable\n`RequestId`，且只接受 `UpdateBroker` 选出的、声明该 kind 并订阅 scope 的 connection；full request\n只通过 `agent/request` 投递，普通 Thread snapshot 保持 redacted。Dynamic owner 还必须在 initialize\ncapability 中声明 exact hosted tool name；仅声明 interaction kind 不能领取其他 dynamic tool。\napproval/user-input owner 断连或\n退订可以确定性重选；已投递的 dynamic tool 不会转交给另一个连接，而是持久化\n`InteractionCancelled(OwnerDisconnected)`，恢复原 Tool path并收口为 unknown-outcome failure，\n非 owner 返回 `AgentInteractionNotOwner`，过期响应返回 `AgentInteractionExpired`。当 response 是\nTool Call 对应的 approval、user input 或 dynamic tool result，且 Core 确实产生 `Resolved`\ndisposition，App Server 才考虑恢复 Tool path。Core result 的 `live_execution_woken` 表明同进程\n`ToolInteractionService` waiter 已取得响应；此时 App Server 不再调用 backend `resume`。只有恢复后\n没有 live waiter 的 durable interaction 才启动 executor recovery。这个判断依赖 exact\nThread/Turn/request 和 pending item binding，不能简化为“所有 interaction 都 restart”。\n同一路径同时恢复执行前 approval 与带结构化 `sandboxDenial` 的 sandbox escalation approval；\nApp Server 不解释或扩大授权，Core 会在恢复后重新校验 action、policy、capability 与 ToolCall\nbinding，并保证升级重试最多启动一次。\n\n`InteractionDeadlineWatcher` 每 50 ms 检查 broker 已知的 pending request，并在同一个\n`workspace_authority_gate` 内重新读取 Core snapshot；只有 exact request 仍 pending 且 wall-clock\ndeadline 已到时，才持久化 `InteractionCancelled(DeadlineElapsed)` 和稳定、可重试的\n`InteractionDeadlineElapsed` Turn failure。deadline policy 属于 App Server delivery/runtime，TUI\n只消费最终 snapshot；Core reducer 不运行 timer。\n\n## 更新代理\n\n每个 `Subscriber` 保存 weak notification queue，以及 `SessionId → durable sequence`、\n`ThreadId → durable sequence`：\n\n```text\ncommitted update\n└─ only sequence > subscriber cursor\n   ├─ enqueue\n   └─ advance durable cursor\n\ntransient Thread update\n└─ if subscribed: enqueue\n   └─ do not advance durable cursor\n```\n\n`notification_queue::NotificationQueue::{push,extend}` 在空 queue 获得新值时唤醒 listener；每个 connection 最多保留\n4096 条通知。达到上限时先清除可重建的 transient Thread notification，使下一 cursor gap 触发\nsnapshot resync；如果 4096 条都是 must-deliver control notification，则关闭 queue，让 client 走\nconnection failure/recovery，而不是丢 control fact 或继续增长内存。\n`AppServer::close_connection` 显式 unregister subscriber 并唤醒 blocked listener。Connection\n若未显式 close，最后一个 strong owner drop 后 broker 仍会在下一次 publish 时通过\n`Weak::upgrade` 失败清除 subscriber。共享 embedded client 和 TUI event pump 也使用 1024 项有界\nchannel，把 slow-consumer 背压传回该 queue。\n\nstdio/JSONL host 使用独立 notification waiter 和单一 writer thread。request response 与主动\nnotification 统一进入 256 项有界 outbound channel；dispatch gate 保证一次 request 内产生的 causal\nnotification 排在该 response 之后，而 request reader 阻塞等待下一行时 notification 仍能立即写出。\n\n## Local composition 与模型安全点\n\n`open_local_app_server` 的顺序：\n\n```text\nSessionStateMode::Durable\n└─ LocalStateRepository::open(profile_root)\n   ├─ SqliteSessionStore(profile_root/state.sqlite3)\n   ├─ SqliteThreadStore(profile_root/state.sqlite3)\n   └─ recover_coordinator\n\nSessionStateMode::Ephemeral\n└─ SessionCoordinator::with_store(InMemorySessionStore, ThreadController::with_store(InMemoryThreadStore))\n\nConfigStore::open_with_paths(profile_root/state.sqlite3, profile_root/config.toml)\n└─ read_snapshot preflight\n\nWorkspace authority\n├─ host-configured initial root → HostConfiguration capability\n└─ client workspace/switch → latest WorkspaceTrustConfig lookup\n   ├─ missing / Restricted → filesystem + watcher + local code index + customizations\n   └─ Trusted → ExplicitUserDecision capability + optional cloud controller when providers exist\n\nConfigChange trust revocation\n├─ revoke shared capability lease\n├─ persist cloud Revoking + request idempotent provider deletion + remove cloud controller\n├─ remove local Tool / Git / search / terminal ports\n├─ terminate PTY and search processes\n├─ interrupt active Turns\n└─ retain restricted filesystem + watcher + local code index + customizations\n\noptional WorkspaceConfigStore\n└─ WorkspaceConfigTracker::read preflight\n\nConfigBackedModelService\n└─ AppServer::new(...).with_config_store(...)\n   └─ SkillRuntime::new(...)\n      ├─ release built-in root\n      ├─ enabled user Skill source roots\n      ├─ active Workspace `.zeta/skills`\n      └─ SkillWatcher(source events + ConfigChange + Workspace binding)\n\nWorkspace activation\n└─ WorkspaceCustomizations::discover\n   ├─ InstructionCatalog(.zeta/instructions)\n   │  └─ Global content → HarnessInstructionsProvider\n   ├─ AgentDefinitionCatalog(.zeta/agents)\n   └─ FileSystemWatcher invalidation → bounded catalog refresh\n\nlocal tools + enabled user MCP declarations\n├─ materialize absolute stdio executable / HTTP endpoint / SecretStore credential\n├─ collect host read-only extension executors and accepted dynamic specs\n├─ combine duplicate-free model Tool names\n├─ install ReloadableToolPorts\n└─ ToolConfigWatcher(ConfigChange)\n\nPlugin lifecycle + Hook declaration\n├─ PluginActivationAuthority(<profile>/plugins)\n│  └─ live generation → Connector/MCP reconcile + invocation drain\n├─ plugin list/marketplace/install/update/rollback/grant/enable/disable/revoke/uninstall RPC\n└─ HookRuntime → trusted Workspace gate → Host Policy → sandboxed process executor\n\nLanguage-server preference\n└─ config/read + languageServer/configure|remove\n   ├─ App Server 持久化 mode/path，并负责 catalog resolution 与 LSP runtime\n   └─ language/hover|completions|locations|hierarchy|workspaceSymbols\n      + language/prepareRename|rename|codeActions|resolveCodeAction\n      + language/synchronize|close and revision-bound language/diagnostics notifications\n```\n\n每次 `ModelService::invoke` 重新读取 user config，与 optional workspace document 合并，再由\n`ModelSnapshotResolver` 生成 immutable invoker。因此 config change 影响下一次 invocation，不会\n改变已经运行的 invocation。`ProviderModelService` 把 Core token 传入\n`ModelInvoker::invoke_with_cancellation`；取消被保留为 `CoreError::Cancelled`，不会降级为普通\nmodel failure。production provider operation 会立即停止本地等待、禁止 retry，并丢弃同步 HTTP\nattempt 的迟到 response。\n\n## Skill 目录运行时\n\nLocal composition 总会安装 `SkillRuntime`。`BuiltInSkillRoot::AutoDetect` 先查询\n`InstallContext::bundled_resource_directory(\"skills\")`，Cargo 开发构建再回退到仓库\n`skills/assets`；测试与自定义 host 可用 explicit root，明确不提供时使用 `Unavailable`。\nAutoDetect 两处都找不到时 runtime 发布 built-in `SourceUnavailable` diagnostic，使客户端的 Errors\ntab 可解释“为什么没有内置 Skill”；显式 `Unavailable` 才表示 host 有意省略 built-in。\nUser source 只接受 config authority 中 enabled 的 absolute root reference，之后仍由\n`SkillSourceRoot::user` 做目录、canonicalization 与 source-kind 校验。\n\n```text\nskills/list\n└─ SkillRuntime::list(Cached | Refresh)\n   ├─ read current SkillsConfig\n   ├─ rebuild catalog when source composition changes\n   ├─ otherwise reuse cache or call SkillCatalog::refresh\n   └─ overlay durable per-Skill enablement\n\nskill/enablement/set\n├─ require exact discovered SkillId\n├─ ConfigStore::apply(SetSkillEnablement)\n└─ reconcile → publish skills/changed when projection changes\n\nskill/resource/open { SkillId, SKILL.md digest, package-relative path }\n├─ SkillRuntime::read_resource → exact digest/root/file-identity validation\n├─ conservative MIME projection; active HTML/SVG remains application/octet-stream\n└─ ResourceStore::create(connection owner, bytes, TTL)\n\nsession/request StartTurn { SkillRef }\n├─ start_turn::replayed_result\n│  └─ existing command → validate exact input and return durable result without mutable reads\n└─ Core extension lifecycle\n   ├─ SkillsExtension activation contributor → exact frozen activation\n   ├─ ThreadEvent::TurnAccepted persists FrozenSkillActivation\n   └─ TurnExecutor safe point → TurnInputContributor → PromptFragment → ContextPlan\n```\n\nWatcher 订阅当前 source roots；Config authority 通过 commit channel（包括 TOML 外部编辑与\nSQLite cross-connection change）触发配置 reconcile。change、overflow 或 backend rescan 提示都只触发\n完整 reconcile；entry、diagnostic 或 enablement projection 没变时 generation 不变，也不发\nnotification。Watcher 启动失败时 local App Server 仍可用，显式\n`skills/list { reload: \"refresh\" }` 是恢复路径。\n\n`zeta-skills-extension` 当前组合 built-in、user 与 active Workspace `.zeta/skills` source。显式正文\nactivation、durable provenance、通用 prompt fragment injection 和 invocation safe-point reload\n已实现；其 `ReadOnlyToolContributor` 现在把统一的 `skills-read` 作为普通 `ToolExecutor` 投影进\n共享 registry，由模型按 metadata 中的 exact source/name 按需加载正文，再用 pinned Skill digest\n读取单个有界 package-relative UTF-8 文件。Binary resource 通过 `skill/resource/open` 进入现有\nconnection-owned Resource store，并使用保守的 MIME/signature 校验；active HTML/SVG 不会被声明为\n可直接 preview。禁用只影响 future Turn eligibility，不能改变已经冻结的 Turn。Plugin source 和\n大目录候选检索的更高层决策、Renderer preview 与 script execution adapter 尚未实现。TUI 与 Desktop 已从\nmetadata-only catalog 生成直接 `/name` Skill command，\n而 `/skills` 只承担管理；正文变化或 source 消失会使恢复/后续 safe point 失败即\n关闭，不会用新 bytes 替换 frozen digest。\n\nMCP runtime 在 `open_local_app_server` 构造初始 generation；后台 `ToolConfigWatcher` 同时订阅 Config\n和 Connector authority，完整构造下一 generation 后原子切换 future model safe points，旧 generation\n由 model snapshot 和已绑定调用持有到排空。`enabled` 是建立连接/启动 server 的显式用户 intent；它不\n批准任何 tool call。stdio command 必须是存在的 absolute executable，独立 Config HTTP 当前只接受\nunauthenticated endpoint 或由共享 SecretStore materialize 的 credential reference；注入的 Connector\nruntime 可在 ready account 下 materialize credential-bearing transport。exact MCP OAuth provider 由\nhost 注入，`mcp/oauth/complete|refresh` 会设置 connect intent 并立即 reconcile，revoke 在 provider\n调用前先设置 disconnect intent，并等待 active Tool generation 移除该 server。Workspace MCP intent\n仍保持 pending trust，不会接入。\n`tools/list_changed` 通过 `McpCatalogUpdates` 触发同一 host reconcile；重建失败保留旧 generation\n并记录诊断，不发布半成品 catalog。\n启动与重建采用 `RequireAll`，任一 enabled server 无法 initialize 时保留旧 generation 并记录诊断，\n不会静默发布不完整 catalog。\n\n当 MCP server 在工具调用期间发起 form elicitation，RMCP host 通过 task-local exact call binding\n进入 Core `ToolInteractionService`。Core 先持久化 `AgentRequest::UserInput`，owner response 再唤醒同一\n工具执行；URL elicitation、数组/多选与完整 JSON Schema format 当前失败关闭或 decline。进程重启后\nremote request 不可恢复，已 started 的调用保持 unknown outcome，不能向新 MCP connection 重放。\n\n缺少 preferred model/provider 时创建 `UnavailableModel`，使 invocation 显式失败，不回退到 echo\n或任意默认 provider。\n\n## 审查模型适配器\n\n`ReviewModelResolver::resolve` 使用 frozen `ResolvedConfig`：\n\n```text\nresolve_approval_review_model(registry)\n├─ locate selected provider config\n├─ ModelProvider::runtime(ModelRuntimeRequest)\n└─ ProviderReviewModel { exact ModelRef, immutable invoker }\n```\n\n`ProviderReviewModel::request` 把 trusted reviewer prompt 放入 instructions，把 action JSON 放入\nuser text，并附 response schema；同时清空 tools、设置 `ToolChoice::None`、禁止 parallel tool calls\n并使用 temperature 0。Response 只拼接 text fragments，忽略 reasoning，拒绝 refusal、Tool Call 与\n空 JSON。\n\n`ApprovalModeActionPolicyService` 先调用 authoritative base policy，并且只处理其 `AskUser` 结果：\n\n| Turn 模式 | `AskUser` 后续行为 | 不变量 |\n| --- | --- | --- |\n| `AskPermissions` | 保留 durable approval interaction | 用户仍只能 approve once / decline |\n| `AutoReview` | 用冻结的 review runtime 创建 `LlmActionClassifier`，由 `ActionPolicyEngine` 应用风险矩阵 | reviewer 缺失或失败时回退 `AskUser` |\n| `BypassPermissions` | 签发绑定 action digest、完整 capabilities 与 policy revision 的 permission-bypass authority | 只跳过交互；不能改变 `Block`、revision error 或其他 base decision |\n\n这里负责选择/隔离 review runtime 和按 Turn 模式组合 policy；classifier schema validation 与\nAuto Review authorization decision 分别属于 `zeta-auto-review` 和 `zeta-action-policy`。review runtime\n当前在 local App Server 启动时从 frozen config 解析；配置变化需要重启 App Server 才能换用新模型。\n\n## 资源存储\n\nResource 是 in-memory、connection-owned、TTL-bounded：\n\n- 单个 resource 最大 `MAX_RESOURCE_BYTES` = 16 MiB；\n- 每个 connection 同时最多 128 个 resource、合计最多 64 MiB；\n- read chunk 最大 `MAX_READ_CHUNK_BYTES` = 262,144 bytes；\n- `create_resource` 当前固定 TTL 为 300 秒；\n- ID 是 process-local monotonic hex string；\n- metadata 包含 MIME、byte length 和 SHA-256；\n- read 返回 standard Base64、decoded length、offset 与 EOF；\n- cleanup 在 create/read/metadata/release path lazy 执行。\n\nResource 不跨重启恢复，也不能被另一 connection 读取或 release。Connection drop 当前不会立即遍历\n删除资源，只依赖 TTL lazy cleanup。\n\n浏览器截图是这一存储的当前 producer 之一。Host response 中的 PNG 最多 16 MiB，必须同时通过\ndecoded length、标准 Base64、`image/png` 和 PNG signature 校验；Tool result 只公开 resource ID、\nMIME、size 与 SHA-256，不复制图片 bytes。\n\n## 错误映射\n\n| Source | External error |\n| --- | --- |\n| malformed JSON | `ParseError` / `-32700` |\n| invalid envelope/ID/params | `InvalidRequest` / `InvalidParams` |\n| unknown method | `MethodNotFound` |\n| Core command ID conflict | `CommandConflict` |\n| other Core failure | `CoreOperationFailed` |\n| missing config store | `ConfigUnavailable` |\n| config sequence mismatch | `ConfigRevisionConflict` |\n| missing exact MCP server/runtime | `McpServerNotFound` / `McpRuntimeUnavailable` |\n| missing OAuth service/provider | `McpOAuthUnavailable` |\n| replayed, changed-target or state-mismatched OAuth callback | `McpOAuthInvalidCallback` |\n| expired OAuth flow | `McpOAuthExpired` |\n| provider exchange/refresh/revoke or credential store failure | `McpOAuthOperationFailed` |\n| missing Skill runtime | `SkillsUnavailable` |\n| invalid/missing exact Skill target | `SkillNotFound` |\n| Skill config/catalog failure | `SkillOperationFailed` |\n| resource ownership/bounds | corresponding stable resource error |\n| missing filesystem authority | `FileSystemUnavailable` |\n| filesystem path/I/O failure | `FileSystemOperationFailed` |\n| missing / initial code index | `CodeIndexUnavailable` / `CodeIndexNotReady` |\n| index I/O、SQLite 或 stale materialization | `CodeIndexOperationFailed` |\n| missing cloud controller | `CloudCodeIndexUnavailable` |\n| invalid/conflicting cloud grant | `CloudCodeIndexInvalidGrant` / `CloudCodeIndexConsentConflict` |\n| cloud byte ceiling exceeded | `CloudCodeIndexEgressLimitExceeded` |\n| missing capability/deletion guarantee | `CloudCodeIndexProviderUnavailable` |\n| cloud persistence/publication/deletion failure | `CloudCodeIndexOperationFailed` |\n| retrieval local failure / source mismatch | `CodeRetrievalOperationFailed` |\n| missing search backend | `SearchUnavailable` |\n| unknown/cross-connection search job | `SearchNotFound` / `SearchNotOwner` |\n| search job capacity exhausted | `SearchBusy` |\n| rg spawn/parse/exit failure | terminal `WorkspaceSearchReadResult.error` with stable redacted text |\n| missing terminal backend | `TerminalUnavailable` |\n| unknown/cross-connection Terminal | `TerminalNotFound` / `TerminalNotOwner` |\n| terminal capacity/runtime operation failure | `TerminalBusy` / `TerminalOperationFailed` |\n| poisoned lock/serialization invariant | `ServerOverloaded` or `InternalError` |\n\nExternal errors不携带 `CoreError`、`ConfigError` 或 backend error text。新增 error mapping 时先更新\nprotocol enum，再在本 crate 显式转换。\n\n## 方向偏差检查\n\n- `server.rs` 定义新的 RPC params/result：contract ownership 从 protocol crate 漂移；\n- App Server 直接构造/store event 或执行 reducer：Core ownership漂移；\n- App Server 直接解释 SQLite Session/Thread tables：repository/storage ownership 漂移；\n- ConnectionState 保存 product Session/Thread authoritative snapshot：delivery 与 domain混合；\n- Broker 用 transient event 推进 durable cursor：reconnect gap 可能丢失；\n- Model invocation 长期持有 mutable config snapshot：safe-point guarantee 被破坏；\n- Review model 保留 Tool definitions 或接受 model-selected provider：review isolation 失效；\n- Resource owner check只在某些 operation：跨 connection data leak；\n- Browser target 由“当前活动标签”解析，或非 owner response 能移除 pending：宿主边界漂移；\n- App Server 把任意 CDP method 暴露给 Core/Tool：语义 browser capability 退化为远程调试后门；\n- Error response回显 arbitrary internal text：external contract与安全边界漂移；\n- 新 method只改 `dispatch` 未改 protocol registry/artifacts：client/server contract分叉。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-app-server\nbazel test //zeta-rs/app-server:app-server-unit-tests\n```\n\n## Typst 集成\n\n`AppServer::typst_compile` 拥有从 `document/typst/compile` 到\n`zeta_typst::TypstCompiler` 的应用级桥接。它把预期内的源码失败映射为\n`TypstCompileResult::Failed`；成功时创建由当前连接拥有、有效期为 300 秒的\n`application/pdf` 资源；并通过 `typst_diagnostic_dto` 把编译器诊断映射为协议数据结构。\n\n编译器边界属于 `zeta-typst`；本 crate 不得在其周围增加宿主路径解析、包下载或字体发现。\n把当前连接拥有的临时资源改为持久化文档存储，是另一项所有权决策。跨 crate 信任模型见\n[`docs/typst.md`](../../docs/typst.md)。\n\n测试覆盖初始化/请求 ID、Session 优先流程、命令重放/冲突、分叉谱系、Turn 重放/模型只调用一次、\n多连接更新、重连后的持久化缺口、有界通知 overflow、连接拥有的资源、配置命令、owner-directed\n交互/批准解决、断连重选与 deadline、\n先响应后通知、模型配置安全点、\nWorkspace override、review-only request、只读 `rg` definition/materialization/policy/execution，\nMCP worker bridge、exact provenance/approval policy、local/MCP 路由与 collision rejection、\nconnect/disconnect/status、OAuth PKCE/one-shot callback/refresh/revoke/reconcile、form elicitation 的\ndurable live wake 与 concurrent call isolation、\nConfig commit/cross-connection notification、future Tool generation replacement 与 prepared-call\ngeneration retention，以及\n可信 Terminal Profile、真实 PTY create/write/read/exit、Terminal owner/error/ring limits，\nSkill 内置/用户/工作区来源组合、启用状态叠加、监听器刷新、精确激活、摘要变化失败即关闭、\n删除源文件后的 command receipt 重放与 `skills/changed`；工作区定制覆盖全局指令注入、Agent 目录刷新和\n`AGENTS.md` 非原生来源隔离。\nGit 覆盖 workspace projection、runtime stream identity、revision 去重、`git/statusChanged`、\ntext diff、local branch list/switch、path mutation 与 commit。Filesystem 覆盖有界原子写入、权限保留、root containment、\n相对路径 `fs/changed` 与 watcher overflow rescan。\nSyntax 覆盖 revision mismatch、Unicode UTF-16 projection、按需 selection ancestor scopes 与 invalid\nsurrogate boundary；CodeIndex/SymbolIndex 覆盖 persistent reopen、watcher reconcile、dirty overlay、\nsave handoff、stale materialization 和 symbol-aware retrieval。\nBrowser host 覆盖反向 request/response、非 owner 拒绝、target identity、断连 pending failure、\n截图 Resource ownership，以及 Desktop restart-safe handler registration 和原生目标回收。\n\nlocal tool 的参数白名单、discovery、取消与输出限制由\n[`zeta-shell-command`](../shell-command/README.md) 和\n[`zeta-tool-executor`](../tool-executor/README.md) 维护；\n本 README 只拥有 App Server 组合与 Core port binding。\n\n当前 JSONL 服务仍使用同步循环；自有嵌入式连接已具备可唤醒通知来源，以及显式订阅、资源和\n终端清理；尚无异步多连接调度器、序列化范围强制、持久化资源或完整网络服务\n生命周期。MCP desired config 的热更新、SecretStore credential materialization、provider-injected\nOAuth lifecycle、form elicitation 和未来 Tool catalog replacement 已实现；当前仍没有 stdio 进程\n沙箱、自动 OAuth discovery/内建 provider、完整 runtime health/log API、progress 产品投影、URL/复杂\nform elicitation 或 MCP resources/prompts。Plugin 安装、marketplace、grant、enable/disable、rollback、\nrevoke 和 uninstall authority 已具备 typed RPC；Renderer 管理 UI 不属于本 crate。MCP 与 dynamic image result 已进入 canonical\n`ContentPart`，Core 会把结构化内容写入 durable `ToolResult`，并在最终模型请求处按 model\ncapability 统一处理 image detail；旧 transcript 的纯 text shape 仍可读取。Reloadable Tool service\n会在调用落盘前冻结 generation、definition digest、source chain 和 process incarnation；重启后的\n历史调用当前选择失败关闭，而不是按同名 live tool 重放。演进这些能力时应保留 protocol registry\n唯一性、Core/store authority、snapshot + durable gap 和 per-invocation config safe point。\n\n## Static extension 资源\n\n跨层行为和信任边界由 [`docs/editor-extensions.md`](../../docs/editor-extensions.md) 维护，可复用包目录\n由 [`zeta-extensions`](../extensions/README.md) 拥有。本 crate 只按“内置目录优先、用户目录其次”的\n顺序组合主机根目录，把目录值适配为协议 DTO，并把打开的 bytes 放入 connection-owned\n`ResourceStore`。Renderer 调用方不能提交绝对主机路径。\n\n`extensions/list` 返回一个不可变目录代次。`extensions/resource/open` 绑定代次、扩展 ID 和包内相对\n路径；旧代请求明确失败，不会重新打开可变 package 文件。App Server 把目录诊断和错误映射到协议\n类别并拥有临时资源生命周期，但不解析 language、grammar、snippet、theme 或 debugger 贡献。未来\nZeterm consumer 应直接依赖 `zeta-extensions`，不能导入 App Server 或 Desktop transport code。",
    "headings": [
      {
        "depth": 2,
        "id": "运行时所有权",
        "title": "运行时所有权"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "json-rpc-调用图",
        "title": "JSON-RPC 调用图"
      },
      {
        "depth": 2,
        "id": "sessionthread-与-turn-编排",
        "title": "Session、Thread 与 Turn 编排"
      },
      {
        "depth": 2,
        "id": "更新代理",
        "title": "更新代理"
      },
      {
        "depth": 2,
        "id": "local-composition-与模型安全点",
        "title": "Local composition 与模型安全点"
      },
      {
        "depth": 2,
        "id": "skill-目录运行时",
        "title": "Skill 目录运行时"
      },
      {
        "depth": 2,
        "id": "审查模型适配器",
        "title": "审查模型适配器"
      },
      {
        "depth": 2,
        "id": "资源存储",
        "title": "资源存储"
      },
      {
        "depth": 2,
        "id": "错误映射",
        "title": "错误映射"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      },
      {
        "depth": 2,
        "id": "typst-集成",
        "title": "Typst 集成"
      },
      {
        "depth": 2,
        "id": "static-extension-资源",
        "title": "Static extension 资源"
      }
    ],
    "searchText": " 本 README 解释 JSON-RPC dispatch、local composition、update broker、resource store 与 review model adapter。External method contract 见 docs/zeta-app-server-api.md，canonical product model 见 docs/protocol.md，workspace 搜索的跨层 ownership 见 docs/search.md，外部 MCP client runtime 的跨 crate 语义见 docs/mcp.md，Git/SCM 跨进程 ownership 见 docs/git.md，Workspace identity 与 trust boundary 见 docs/workspace-security.md。跨文件内容搜索的执行实现见 zeta-rs/search/README.md。Desktop 浏览器能力的跨进程所有权和当前限制见 docs/zeta-desktop-architecture.md。 zeta-app-server 是产品客户端与 Zeta domain/runtime 的 application boundary。它解析 zeta-app-server-protocol request，调用 SessionCoordinator、Thread controller、 TurnExecutor 与 ConfigStore，再返回 typed result 或发布 canonical update。 它不实现 reducer，不定义第二套 Session/Thread/Turn model，也不拥有 SQLite schema。 运行时所有权 ``text JSONL / in-process caller └─ AppServer + ConnectionState ├─ JSON-RPC validation / initialization gate ├─ protocol method dispatch ├─ SessionCoordinator / ThreadController ├─ MultiAgentCoordinator → child Thread spawn/context seed + durable delivery/join/cancellation ├─ TurnBackendHandle → exact Turn.model provider router │ ├─ TurnExecutor direct provider │ └─ CodexTurnExecutionBackend openai-chatgpt subscription ├─ ConfigStore ├─ optional WorkspaceFileSystem + filesystem watcher ├─ optional GitRuntime → zeta-file-watcher + GitService → zeta-git ├─ optional SearchService → zeta-search ├─ optional CodeIndexRuntime → zeta-code-index + filesystem watcher ├─ optional SymbolIndexRuntime → zeta-symbol-index + CodeIndex source/overlay authority ├─ optional CodeIndexSemanticService → local SQLite vectors + host model adapters ├─ optional CloudCodeIndexController → zeta-code-index-cloud + host provider registry ├─ request-scoped CodeRetrievalService → symbol/lexical/semantic/remote RRF + verification + budget ├─ optional TerminalService → zeta-utils-pty ├─ optional LanguageServerProviderRegistry → zeta-language-service → zeta-lsp ├─ reloadable MCP Tool generation + status/OAuth/form interaction → zeta-mcp-extension → zeta-mcp ├─ ConnectorCredentialService → list/connect/disconnect + ready MCP reconcile ├─ client-hosted dynamic tools → durable Agent interaction owner ├─ BrowserHost → connection-owned reverse JSON-RPC browser capability │ → built-in browser Tool port + one-time approval policy ├─ read-only/capability ToolExecutor contributions → zeta-extension-api ├─ optional Web Search executor → zeta-web-search-extension ├─ trusted Workspace Agent tools → spawnagent / sendagentmessage / waitagent ├─ Skill RPC/config/event adapters → zeta-skills-extension │ → zeta-skills + zeta-file-watcher ├─ WorkspaceCustomizations → zeta-instructions + zeta-agents ├─ connection-owned ResourceStore └─ UpdateBroker → session/update, session/thread/update, config/changed, connector/changed, skills/changed, git/statusChanged, fs/changed ` App Server connection 不是 product Session。关闭 connection 只失去 connection-local subscription、 request-ID set、notification queue 与 resource ownership。默认的 SessionStateMode::Durable 由 Core/store 继续拥有 Session/Thread durable state；需要进程内生命周期的 host 可以显式选择 SessionStateMode::Ephemeral，此时 coordinator 使用 in-memory stores，Session/Thread 与用户消息 不会从 profile SQLite 恢复或写入。ConfigStore 仍可按 profile 保存普通配置。 公共契约 Symbol 职责 --- --- AppServer JSON-RPC application server 与 domain composition handle ConnectionState 每个 logical connection 的 initialized/request-ID/notification state AppServer::new 用 recovered SessionCoordinator + ModelService 构造 server AppServer::connection 分配 connection ID 并注册 notification queue AppServer::connectionnotifications 返回可阻塞等待、主动唤醒的 connection outbound message source AppServer::closeconnection 注销 subscription、关闭 notification source，并释放 Resource/Terminal/Syntax owner AppServer::handlejson 处理一个 JSON-RPC request string AppServer::drainnotifications JSONL 与同步适配 caller 取出该 connection 的 serialized notifications AppServer:: servestdio,servejsonl 同步 JSON Lines service loop AppServer::createresource 创建 5 分钟 TTL 的 connection-owned resource AppServer::withconfigstore 开启 config/provider/MCP/Skill RPC AppServer::withconnectorservice 开启 Connector RPC、capability 与 generation notification watcher AppServer::withslashcommandcatalog 安装 initialize 时下发的 immutable 动态命令 snapshot AppServer::withfilesystem 注入受 workspace 约束的 filesystem authority AppServer::withfilesystemwatcher 监听可信 workspace root 并发布相对路径 invalidation hint AppServer::withgitroot 冻结 workspace root，开启 Git status/mutation、watcher 与 revision notification AppServer::withworkspacesearch 注入 workspace root 与冻结的 ripgrep executable，构造外部内容搜索服务 AppServer::withlanguageserverproviders 注入已验证、已安装的 provider registry；activation confirmation 启用 packaged route，显式 Config Disabled 仍可关闭 AppServer::withlanguagemarketplaces 安装 TUF catalog、activation authority 与共享 runtime adapter；提供 list/install RPC，成功后热替换 registry AppServer::withcodeindexstorageroot 配置按 root identity 分隔的 persistent index cache LocalCodeIndexProviders::withsemanticmodels 在 Workspace activation 前注入本地 semantic 使用的 immutable embedding/rerank adapters AppServer::withcloudcodeindexproviders 注入冻结的 provider registry；空 registry 不广告 cloud capability AppServer::withcloudcodeindexstorageroot local composition 配置按 root identity 分隔的 durable grant/deletion state AppServer::withtoolservice 安装同一 server 内所有 Turn 使用的 Core Tool/Policy ports AppServer::withmcpoauthservice 安装独立 Config MCP 的 process-local OAuth coordinator，并广告 mcpOAuth capability AppServer::withcodexturnbackend 将 openai-chatgpt 模型路由到 Codex complete-Turn backend；不读取登录状态做隐式切换 AppServer::withdynamictools 校验 client-hosted dynamic specs，并接入共享 registry、审批和 durable interaction 执行链 AppServer::resumerecoveredagentcoordinations 恢复 Agent spawn/delivery/join/cancellation saga，并调度恢复期间新建的 child Turn openlocalappserver 按 SessionStateMode 选择 durable/in-memory coordinator，打开 config 并组合 provider-backed model openlocalappserverwithcloudproviders 在 Workspace 激活前注入 cloud code-index providers；默认入口使用空 registry openlocalappserverwithcodeindexproviders 在 Workspace 激活前同时注入本地 semantic models 与可选 cloud providers LocalAppServerOptions user profile root + SessionStateMode + optional Workspace/Connector/language-provider runtime + Codex App Server options + validated slash catalog + built-in Skill root selection + optional model operation client/MCP OAuth providers LocalAppServerOptions::withlanguageserverproviders 在 local App Server 启动前注入额外 provider registry；receipt registry 由 composition 自动合并 LocalAppServerOptions::withremotelanguagemarketplace 注入 product-pinned TUF root；同步 signed catalog 并从 profile activation receipt 重建 provider LocalAppServerOptions::withmcpoauthproviders 把 exact MCP server ID → provider adapter 注入使用共享 SecretStore 的 OAuth service LocalConnectorRuntime Connector credential service + shared SecretStore + Plugin-specific MCP materializer LocalAppServerOptions::withpluginactivation 从 exact activation 构造 package-rooted Connector/MCP runtime LocalConnectorRuntime::frompluginactivation activation → Connector catalog + SQLite authority + Plugin MCP provider SessionStateMode 明确选择 profile SQLite durable history 或 process-local ephemeral Session/Thread state BuiltInSkillRoot auto-detected release root、explicit test/host root 或 unavailable 的自解释选择 zetaslashcommands::SlashCommandCatalog 委托共享 crate 校验动态命令并冻结 server-advertised snapshot；App Server 只拥有 composition ReviewModelResolver 从 frozen config snapshot 选择 review-only model ProviderReviewModel ModelInvoker → zetaautoreview::ReviewModel adapter AppServer::new 默认用 TurnExecutor::withouttools。withtoolservice 才会替换为有 Tool 和 Policy port 的 executor。openlocalappserver 会从 user config snapshot 连接明确 enabled 的 unauthenticated 或 SecretStore-backed MCP server，并把 catalog 与本地工具组合。Host 可用 LocalAppServerOptions::withpluginactivation 注入固定 activation，或用 withpluginauthority 注入 live authority；两者都会自动构造 Connector catalog、SQLite authority 与 package-rooted Plugin MCP provider。 没有注入时，local composition 打开 <profile /plugins 的 durable PluginActivationAuthority 和按 profile 隔离的 KeyringSecretStore。Authority generation 会重建 Connector definitions 与 Plugin MCP provider， 并复用 MCP safe-point replacement。Keyring operation 失败会 fail closed，不自动降级到文件副本；host 仍可通过显式 runtime 注入 FileSecretStore。Config、Connector 或 MCP list-changed hint 会在后台构建新 generation；每次 model invocation 同时冻结可见 definitions 和响应后的 binder，因此 watcher 在模型 响应前发布新 registry 也不会把旧响应劫持到同名新工具。已绑定调用继续持有原 Tool/Policy generation， 直到 execute 排空；Plugin-backed call 在 dispatch 前获取 exact activation lease，Connector-bound call 再 额外复核 live connection generation/digest， disconnect 会等待已经 dispatch 的调用结束。每次 MCP tool call 仍必须经过 durable one-time approval。Host 通过 LocalAppServerOptions::withmcpoauthproviders 注入具体 provider 后，local composition 使用 Connector runtime 同一个 SecretStore 构造 McpOAuthService；App Server 只暴露 PKCE/callback/refresh/revoke RPC， 不拥有 provider discovery、scope 或 token wire。Host 安装的 read-only extension executor（当前包括统一的 skills-read）和 client-hosted dynamic tools 也进入同一个 registry， 不得绕过 binding、policy 或 durable result commit。它仅在调用方通过 LocalAppServerOptions::withworkspaceroot 提供统一 Workspace 根时同时组合 filesystem、 .zeta 自定义 catalog、Workspace code index、Workspace search、Git SCM、connection-owned Terminal runtime、 只读 rg registry；Zeta CLI 的 stdio 与 in-process 路径都会使用同一个启动时解析结果： ZETAWORKSPACEROOT 优先，否则使用当前目录。不能因为 protocol 暴露 approval interaction 就 假设任意自定义 host 已经拥有 Tool registry。rg 安装候选来自 zeta-install-context，App Server 只负责把候选交给 RipgrepExecutable 验证并组合成 Tool service。 Web Search 默认不注册。Embedding Tool Search 只负责在当前 registry 中找工具，与外部网页检索无关。 Host 只有通过 LocalAppServerOptions::withwebsearchbackend 注入 zeta-web-search-extension::WebSearchBackend 后，websearch 才进入 eager tool set；其 endpoint host 和 optional credential reference 会冻结到 action digest，并要求 exact one-time approval。共享扩展仅 提供 provider-neutral contract 与 JSON HTTP adapter，不硬编码 Codex 私有 Search endpoint。 默认 local composition 使用 lazy production model client：启动、配置读取和模型目录展示不会加载 system roots 或 proxy。Embedded test 可通过 LocalAppServerOptions::withmodeloperationclient 注入离线 client；真实 transport 初始化失败在第一次模型 operation 返回，不得让 App Server 启动 panic。 local composition 会配置 <profile /code-index-cloud 的 durable state 位置，但默认 provider registry 为空，因此不会安装 cloud controller、广告 cloudCodeIndex 或创建网络请求。具体 host 只有在注入 接受 Workspace-owned exact chunks 且满足幂等 grant deletion 的 provider 后，才能启用云能力。 local composition 同时配置 <profile /code-index-semantic 并安装共享的 SemanticModelProvider resolver，但 semantic CodeIndex 默认仍关闭，所以不会后台发送 chunks 或创建 vectors。用户选择模型并对 exact Workspace 授权源码外发后，Trusted Workspace 的 lexical generation 更新才会在 refresh worker 中同步本地 semantic SQLite。模型只返回 embedding/rerank 结果，召回与 排序仍由本地 domain crate 完成；host 也可用 LocalCodeIndexProviders::withsemanticmodels 注入测试 或专用 immutable adapters。 Trusted Workspace 同时获得 built-in read-only searchcode 工具。它只接受 workspace-code-index-read-only exact grant，调用 canonical CodeRetrievalService，并返回 bounded、 current-source-verified excerpts 与 degradation；未配置 semantic 时自然退回 lexical。semantic grant 精确绑定 Workspace、model selection 与 provider config，provider URL 或模型变化会卸载旧 runtime 并 要求重新授权。Desktop 当前可配置 Ollama/unauthenticated OpenAI-compatible endpoint；OpenAI API key 仍需 host 注入 secure SecretStore，尚无产品 credential UI。 Tool Search 拥有独立的 toolSearch.embeddingModel，不复用 CodeIndex 的模型选择。只有 User Config 明确设置 toolSearch.mode = \"hybridEmbedding\" 才会调用；默认 lexical 不产生 embedding 请求。 toolSearch/configure 先解析 exact provider/model 并执行固定文本 readiness probe，失败返回 ToolSearchUnavailable 且不提交配置。外部配置或启动恢复遇到不可用模型时，App Server 通过 config/read.toolSearch.embeddingStatus 报告原因，hybrid 自然语言搜索明确失败；只有用户显式切回 lexical 才启用纯 BM25。Regex 始终是本地显式策略。门禁通过后，Tool Search 只缓存当前 registry generation 的工具向量；实际调用或响应校验失败会使该次搜索明确失败，不静默回落到 BM25。 Tab 关闭的停止语义属于本层适配，而不是 zeta-protocol 的新 command：前端通过 session/request 的 Stop operation 到达 Session mutation dispatcher，后者调用 Core 的 SessionCoordinator::stop，再从 durable gap 发布 Session 和 child Thread updates。单纯 AppServer::closeconnection 只释放 connection-owned delivery/resource state，不停止产品 Session。 文件与职责 `text src/ ├── server.rs │ └── server/ │ ├── operations.rs Session/Thread/Turn/Resource methods │ ├── configoperations.rs Config/provider/MCP/Skill methods + DTO co"
  },
  {
    "slug": "crates/app-server-client",
    "sourcePath": "zeta-rs/app-server-client/README.md",
    "title": "zeta-app-server-client",
    "description": "AppServerSession 是 CLI/TUI 的 canonical embedded connection owner。它完成 App Server composition、connection 创建、正式 initialize/schema gate、request driver、wakeable notification pump 与显式 shutdown。…",
    "group": "Crate 实现参考",
    "markdown": "> 当前 crate 实现、调用路径、failure/shutdown 语义由本文说明。跨 consumer 的目标架构、\n> backend 取舍与后续阶段见 [`docs/app-server-client.md`](../../docs/app-server-client.md)；\n> wire contract 见 [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md)。\n\n## 结论\n\n`AppServerSession` 是 CLI/TUI 的 canonical embedded connection owner。它完成 App Server\ncomposition、connection 创建、正式 initialize/schema gate、request driver、wakeable\nnotification pump 与显式 shutdown。Consumer 不直接拥有 `AppServer` 或\n`ConnectionState`。\n\n同步适配面 `AppServerClient<T>`、`InProcessTransport`、`start_in_process_client` 和\n`drain_notifications` 仍供 MCP adapter、rust-app 与 contract tests 使用；它们不是新交互\nconsumer 的启动入口。新的 CLI/TUI 交互统一通过 `AppServerSession` 获取 request handle\n与独立 event stream。\n\n`InProcessClientOptions` 默认选择 `SessionStateMode::Durable`。需要进程内生命周期的 host\n必须显式调用 `with_session_state_mode(SessionStateMode::Ephemeral)`；该模式只替换\nSession/Thread coordinator 的存储，不改变 Config、Workspace、Tool 或 protocol contract。\n\n| 能力 | Owned session | Synchronous adapter |\n| --- | --- | --- |\n| ready initialize/schema gate | ✅ `start_embedded` | `start_in_process_client` 时具备 |\n| cloneable request handle | ✅ | 仅 `T: Clone` 时可克隆 |\n| 独立 wakeable event stream | ✅ | ❌，通过 `drain_notifications` 读取 |\n| 空闲/长 Turn notification | ✅ | 需要 consumer polling |\n| 有界 event/backpressure | ✅，1024 项 event channel | 由 consumer drain cadence 决定 |\n| 显式 connection shutdown/task join | ✅ | 由 host 管理 |\n| remote backend | 尚未完成 | ❌ |\n\n## 公共契约\n\n| Symbol | Owner 与 contract |\n| --- | --- |\n| `AppServerSession::start_embedded` | 创建 embedded composition；initialize/schema 校验失败时关闭已启动 driver |\n| `AppServerSession::client` | 返回共享 request ID allocator 的 cloneable `AppServerRequestHandle` |\n| `AppServerSession::take_events` | 单次取出 `AppServerEvents`；第二次返回 `TakeEventsError` |\n| `AppServerSession::shutdown` | 拒绝后续 request、关闭 connection、唤醒 event pump 并 join tasks |\n| `AppServerEvents::{recv,recv_timeout,try_recv}` | 接收 typed notification 与最终 connection close event |\n| `AppServerEvent::Notification` | client boundary 交付的 canonical `ServerNotification`；variant 与 payload decoder 由 protocol registry 同源生成，跨 crate 为非穷尽枚举 |\n| `AppServerEvent::ConnectionClosed` | 明确的 shutdown、driver stop 或 protocol failure |\n| `AppServerClient<T>` | typed JSON-RPC client；method 与 DTO source 仍来自 protocol crate |\n| `InProcessClientOptions::with_session_state_mode` | 明确选择 profile durable history 或 process-local ephemeral Session/Thread state |\n| `InProcessClientOptions::with_model_operation_client` | embedded host/test 注入离线或自定义 model transport；不改变 protocol/model semantics |\n| `AppServerClient::request_session` | Session aggregate 的 canonical typed mutation request；所有 Session mutation 统一由此进入 |\n| `AppServerClient::open_skill_resource` | 以 exact Skill digest 打开 package resource；bytes 仍由 connection-owned Resource API 分块读取 |\n| `ServerNotification::ConnectorsChanged` | `connector/changed` 的 typed generation invalidation；consumer 收到后重新 list |\n\n正常入口示意（`options`/`params` 由 host 与 protocol DTO 构造）：\n\n```rust\nlet mut session = AppServerSession::start_embedded(options)?;\nlet mut client = session.client();\nlet events = session.take_events()?;\n\nlet result = client.request_session(params)?;\nlet event = events.recv();\n\nsession.shutdown()?;\n```\n\nTyped method 当前同步等待该 request 的 completion，但实际 dispatch 位于独立 driver。\nNotification 不依附 request completion；consumer 不得对 session handle 调用\n`drain_notifications()`。\n\n## 文件与职责\n\n| 文件 | 当前 owner |\n| --- | --- |\n| `src/session.rs` | owned session、session transport、request/event threads、shutdown 与 connection lifecycle |\n| `src/in_process.rs` | embedded composition 与 initialized connection |\n| `src/profile.rs` | `ZETA_PROFILE_ROOT` 与 host-wide default profile state path |\n| `src/lib.rs` | generic typed JSON-RPC methods、request ID/result pairing 与 public exports |\n| `src/notification.rs` | 解析 notification envelope，并把 method/payload 交给 protocol-owned canonical decoder |\n| `src/session_tests.rs` | owned lifecycle、idle wakeup、clone identity 与 shutdown contract |\n| `src/client_tests.rs` | JSON-RPC pairing、schema、Session contract、Skill catalog/watcher contract |\n\n## 执行路径\n\n```text\nAppServerSession::start_embedded\n├─ resolve/receive profile root\n├─ open_in_process_app_server\n├─ AppServer::connection\n├─ AppServer::connection_notifications\n├─ start connection driver\n├─ start notification pump\n├─ AppServerClient::initialize\n│  └─ SessionTransport → request channel → AppServer::handle_json\n├─ validate schema hash\n└─ return ready session\n\nAppServerRequestHandle typed method\n├─ shared atomic request ID\n├─ bounded DriverCommand queue\n├─ connection driver → AppServer::handle_json\n└─ per-request completion → typed decode\n\nApp Server background update\n├─ UpdateBroker → NotificationQueue::push/extend\n├─ condition variable wake\n├─ notification pump → envelope decode\n├─ protocol::registry::decode_server_notification → canonical typed notification\n└─ bounded AppServerEvents (1024)\n```\n\nConnection driver 在 request response completion 发送之前持有 delivery barrier；event pump\n通过同一 barrier drain queue，因此同一 request 的 response 先于 causal notification 交付。\n\n## 失败与关闭\n\n- request channel 已关闭：返回 `ClientError::Transport`；\n- response ID/envelope/decode 不合法：返回 `ClientError::Protocol`；\n- server failure：保留 code/message 为 `ClientError::Server`；\n- known notification decode 失败：event stream 发送 `ProtocolFailure` 并关闭 connection；\n- event receiver 被 drop：pump 关闭 connection，surviving client clone 的后续 request 失败；\n- event channel 满时 pump 背压 App Server 的 4096 项 connection queue；该 queue 先清可重建\n  transient，control-only overflow 关闭 connection，不静默丢 durable/control fact；\n- 正常退出必须调用 `shutdown`；`Drop` 只做 best-effort signal，不等待 task；\n- `shutdown` 后仍存在的 client clone 不延长 connection 生命周期。\n\n当前 event channel 已有界；transient purge 依赖 consumer 的 stream cursor gap → snapshot resync。\n显式 `Lagged` event 与 remote reconnect 尚未实现，connection/control overflow 后 consumer 仍需按\n`ConnectionClosed` 处理恢复。\n\n## 内部所有权与漂移信号\n\n- `session::drive_connection` 唯一拥有 `ConnectionState` 与 request dispatch；\n- `session::pump_notifications` 唯一解码 session outbound notification；\n- `protocol::registry::server_notifications!` 同时生成 method registry、canonical\n  `ServerNotification` 与 payload decoder；client crate 不维护第二份 variant mapping；\n- `server::notification_queue::NotificationQueue`（App Server crate）拥有 wake/close queue primitive；\n- `AppServerClient::call` 拥有 request ID、JSON-RPC pairing 与 typed result decode；\n- `in_process::initialize_client` 只服务 embedded startup。\n\n以下变化表示 architecture drift，需要同步系统文档与测试：\n\n- CLI/TUI 恢复调用 `start_in_process_client` 或 `drain_notifications`；\n- consumer 直接创建 `AppServer`/`ConnectionState`；\n- notification 再次只在 request 后被 drain；\n- consumer 穷尽匹配完整 `ServerNotification`，或为未拥有的 capability 逐项添加 ignore arm；\n- session lifetime 由最后一个 client clone 的 drop 决定；\n- request driver 绕过 `AppServer::handle_json` 直连 Core/store；\n- shutdown 不再 join session 启动的 tasks。\n\n常见修改影响：\n\n| 修改 | 必须同步检查 |\n| --- | --- |\n| 新 typed RPC method | protocol registry/DTO、`AppServerClient` method、wire contract tests |\n| notification variant | protocol registry 与 protocol decoder tests；只有拥有该 capability 的 consumer projection 需要修改 |\n| request/event queue policy | causal barrier、shutdown unblock、durable loss/lag contract 与系统文档 |\n| connection-owned resource | `AppServer::close_connection` cleanup、cross-connection rejection tests |\n| 新 backend | initialize/schema gate、同一 session contract suite、failure/reconnect ownership |\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-app-server-client\ncargo test --manifest-path Cargo.toml -p zeta-tui\ncargo test --manifest-path Cargo.toml -p zeta-cli\n```\n\nSession tests覆盖空闲连接 notification、Turn completion event、有界 channel 关闭不死锁、显式\nshutdown、最终 `ConnectionClosed` 与 surviving clone rejection。Contract tests继续覆盖 JSON-RPC\npairing、schema gate、canonical Session mutation、Agent request、Skill watcher 和同步 client surface。",
    "headings": [
      {
        "depth": 2,
        "id": "结论",
        "title": "结论"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "执行路径",
        "title": "执行路径"
      },
      {
        "depth": 2,
        "id": "失败与关闭",
        "title": "失败与关闭"
      },
      {
        "depth": 2,
        "id": "内部所有权与漂移信号",
        "title": "内部所有权与漂移信号"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 当前 crate 实现、调用路径、failure/shutdown 语义由本文说明。跨 consumer 的目标架构、 backend 取舍与后续阶段见 docs/app-server-client.md； wire contract 见 docs/zeta-app-server-api.md。 结论 AppServerSession 是 CLI/TUI 的 canonical embedded connection owner。它完成 App Server composition、connection 创建、正式 initialize/schema gate、request driver、wakeable notification pump 与显式 shutdown。Consumer 不直接拥有 AppServer 或 ConnectionState。 同步适配面 AppServerClient<T 、InProcessTransport、startinprocessclient 和 drainnotifications 仍供 MCP adapter、rust-app 与 contract tests 使用；它们不是新交互 consumer 的启动入口。新的 CLI/TUI 交互统一通过 AppServerSession 获取 request handle 与独立 event stream。 InProcessClientOptions 默认选择 SessionStateMode::Durable。需要进程内生命周期的 host 必须显式调用 withsessionstatemode SessionStateMode::Ephemeral ；该模式只替换 Session/Thread coordinator 的存储，不改变 Config、Workspace、Tool 或 protocol contract。 能力 Owned session Synchronous adapter --- --- --- ready initialize/schema gate ✅ startembedded startinprocessclient 时具备 cloneable request handle ✅ 仅 T: Clone 时可克隆 独立 wakeable event stream ✅ ❌，通过 drainnotifications 读取 空闲/长 Turn notification ✅ 需要 consumer polling 有界 event/backpressure ✅，1024 项 event channel 由 consumer drain cadence 决定 显式 connection shutdown/task join ✅ 由 host 管理 remote backend 尚未完成 ❌ 公共契约 Symbol Owner 与 contract --- --- AppServerSession::startembedded 创建 embedded composition；initialize/schema 校验失败时关闭已启动 driver AppServerSession::client 返回共享 request ID allocator 的 cloneable AppServerRequestHandle AppServerSession::takeevents 单次取出 AppServerEvents；第二次返回 TakeEventsError AppServerSession::shutdown 拒绝后续 request、关闭 connection、唤醒 event pump 并 join tasks AppServerEvents:: recv,recvtimeout,tryrecv 接收 typed notification 与最终 connection close event AppServerEvent::Notification client boundary 交付的 canonical ServerNotification；variant 与 payload decoder 由 protocol registry 同源生成，跨 crate 为非穷尽枚举 AppServerEvent::ConnectionClosed 明确的 shutdown、driver stop 或 protocol failure AppServerClient<T typed JSON-RPC client；method 与 DTO source 仍来自 protocol crate InProcessClientOptions::withsessionstatemode 明确选择 profile durable history 或 process-local ephemeral Session/Thread state InProcessClientOptions::withmodeloperationclient embedded host/test 注入离线或自定义 model transport；不改变 protocol/model semantics AppServerClient::requestsession Session aggregate 的 canonical typed mutation request；所有 Session mutation 统一由此进入 AppServerClient::openskillresource 以 exact Skill digest 打开 package resource；bytes 仍由 connection-owned Resource API 分块读取 ServerNotification::ConnectorsChanged connector/changed 的 typed generation invalidation；consumer 收到后重新 list 正常入口示意（options/params 由 host 与 protocol DTO 构造）： ``rust let mut session = AppServerSession::startembedded options ?; let mut client = session.client ; let events = session.takeevents ?; let result = client.requestsession params ?; let event = events.recv ; session.shutdown ?; ` Typed method 当前同步等待该 request 的 completion，但实际 dispatch 位于独立 driver。 Notification 不依附 request completion；consumer 不得对 session handle 调用 drainnotifications 。 文件与职责 文件 当前 owner --- --- src/session.rs owned session、session transport、request/event threads、shutdown 与 connection lifecycle src/inprocess.rs embedded composition 与 initialized connection src/profile.rs ZETAPROFILEROOT 与 host-wide default profile state path src/lib.rs generic typed JSON-RPC methods、request ID/result pairing 与 public exports src/notification.rs 解析 notification envelope，并把 method/payload 交给 protocol-owned canonical decoder src/sessiontests.rs owned lifecycle、idle wakeup、clone identity 与 shutdown contract src/clienttests.rs JSON-RPC pairing、schema、Session contract、Skill catalog/watcher contract 执行路径 `text AppServerSession::startembedded ├─ resolve/receive profile root ├─ openinprocessappserver ├─ AppServer::connection ├─ AppServer::connectionnotifications ├─ start connection driver ├─ start notification pump ├─ AppServerClient::initialize │ └─ SessionTransport → request channel → AppServer::handlejson ├─ validate schema hash └─ return ready session AppServerRequestHandle typed method ├─ shared atomic request ID ├─ bounded DriverCommand queue ├─ connection driver → AppServer::handlejson └─ per-request completion → typed decode App Server background update ├─ UpdateBroker → NotificationQueue::push/extend ├─ condition variable wake ├─ notification pump → envelope decode ├─ protocol::registry::decodeservernotification → canonical typed notification └─ bounded AppServerEvents 1024 ` Connection driver 在 request response completion 发送之前持有 delivery barrier；event pump 通过同一 barrier drain queue，因此同一 request 的 response 先于 causal notification 交付。 失败与关闭 - request channel 已关闭：返回 ClientError::Transport； - response ID/envelope/decode 不合法：返回 ClientError::Protocol； - server failure：保留 code/message 为 ClientError::Server； - known notification decode 失败：event stream 发送 ProtocolFailure 并关闭 connection； - event receiver 被 drop：pump 关闭 connection，surviving client clone 的后续 request 失败； - event channel 满时 pump 背压 App Server 的 4096 项 connection queue；该 queue 先清可重建 transient，control-only overflow 关闭 connection，不静默丢 durable/control fact； - 正常退出必须调用 shutdown；Drop 只做 best-effort signal，不等待 task； - shutdown 后仍存在的 client clone 不延长 connection 生命周期。 当前 event channel 已有界；transient purge 依赖 consumer 的 stream cursor gap → snapshot resync。 显式 Lagged event 与 remote reconnect 尚未实现，connection/control overflow 后 consumer 仍需按 ConnectionClosed 处理恢复。 内部所有权与漂移信号 - session::driveconnection 唯一拥有 ConnectionState 与 request dispatch； - session::pumpnotifications 唯一解码 session outbound notification； - protocol::registry::servernotifications! 同时生成 method registry、canonical ServerNotification 与 payload decoder；client crate 不维护第二份 variant mapping； - server::notificationqueue::NotificationQueue（App Server crate）拥有 wake/close queue primitive； - AppServerClient::call 拥有 request ID、JSON-RPC pairing 与 typed result decode； - inprocess::initializeclient 只服务 embedded startup。 以下变化表示 architecture drift，需要同步系统文档与测试： - CLI/TUI 恢复调用 startinprocessclient 或 drainnotifications； - consumer 直接创建 AppServer/ConnectionState； - notification 再次只在 request 后被 drain； - consumer 穷尽匹配完整 ServerNotification，或为未拥有的 capability 逐项添加 ignore arm； - session lifetime 由最后一个 client clone 的 drop 决定； - request driver 绕过 AppServer::handlejson 直连 Core/store； - shutdown 不再 join session 启动的 tasks。 常见修改影响： 修改 必须同步检查 --- --- 新 typed RPC method protocol registry/DTO、AppServerClient method、wire contract tests notification variant protocol registry 与 protocol decoder tests；只有拥有该 capability 的 consumer projection 需要修改 request/event queue policy causal barrier、shutdown unblock、durable loss/lag contract 与系统文档 connection-owned resource AppServer::closeconnection cleanup、cross-connection rejection tests 新 backend initialize/schema gate、同一 session contract suite、failure/reconnect ownership 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-app-server-client cargo test --manifest-path Cargo.toml -p zeta-tui cargo test --manifest-path Cargo.toml -p zeta-cli ` Session tests覆盖空闲连接 notification、Turn completion event、有界 channel 关闭不死锁、显式 shutdown、最终 ConnectionClosed` 与 surviving clone rejection。Contract tests继续覆盖 JSON-RPC pairing、schema gate、canonical Session mutation、Agent request、Skill watcher 和同步 client surface。"
  },
  {
    "slug": "crates/app-server-protocol",
    "sourcePath": "zeta-rs/app-server-protocol/README.md",
    "title": "zeta-app-server-protocol",
    "description": "zeta-app-server-protocol 是 App Server 对外 wire contract 的唯一 Rust source。它定义类型化 参数、结果、错误、JSON-RPC 2.0 envelope，以及客户端方法、服务端通知和宿主方法注册表；同一套 定义还会生成 canonical ServerNotification、payload decoder、…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 App Server external RPC contract、method registry 与 artifact generator。\n> 面向客户端的 API 语义见 [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md)，\n> canonical product values 见 [`docs/protocol.md`](../../docs/protocol.md)，workspace 搜索的跨层\n> ownership 见 [`docs/search.md`](../../docs/search.md)，Terminal external 语义见\n> [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md#integrated-terminal)，Git/SCM\n> ownership 见 [`docs/git.md`](../../docs/git.md)，Desktop 浏览器宿主的跨进程语义见\n> [`docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md#7-浏览器能力)。\n\n`zeta-app-server-protocol` 是 App Server 对外 wire contract 的唯一 Rust source。它定义类型化\n参数、结果、错误、JSON-RPC 2.0 envelope，以及客户端方法、服务端通知和宿主方法注册表；同一套\n定义还会生成 canonical `ServerNotification`、payload decoder、JSON Schema、TypeScript binding\n和协商用 schema hash。\n\n它不拥有 connection、framing、dispatcher、业务执行、Core state、resource bytes 或 persistence。\n\n## Crate 边界\n\n```text\nzeta-protocol canonical values\n          │\n          ▼\nzeta-app-server-protocol\n├─ protocol DTO\n├─ client method / server notification / host method registry\n├─ generic JSON-RPC envelope\n└─ schema / TypeScript / hash generator\n          │\n          ├─ zeta-app-server dispatcher\n          └─ Desktop / CLI clients\n```\n\n语义与 JSON-RPC wrapper 分开：\n\n- `protocol::*` 定义 method params/result 与 App Server-specific DTO；\n- `rpc::*` 只定义 transport-neutral generic RPC envelope；\n- `export::*` 把 registry、DTO 和 envelope 聚合成 artifact。\n\n未来换编码时可以复用业务 DTO；不要把 `jsonrpc`、wire request ID 或 connection state放进\n`protocol::*` DTO。\n\n## 目录与职责\n\n```text\nzeta-rs/app-server-protocol/\n├── src/\n│   ├── protocol/\n│   │   ├── registry.rs       # method、notification、serialization scope\n│   │   ├── notification.rs   # canonical notification API re-export\n│   │   ├── common.rs         # handshake/shared wire values\n│   │   ├── browser.rs        # client-hosted semantic browser request/result DTO\n│   │   ├── initialize.rs\n│   │   ├── slash_commands.rs\n│   │   ├── session.rs\n│   │   ├── model.rs\n│   │   ├── language.rs       # language-server Config 与 Marketplace DTO\n│   │   ├── turn.rs\n│   │   ├── config.rs\n│   │   ├── connectors.rs     # redacted connection DTO + inbound-only secret\n│   │   ├── resources.rs\n│   │   ├── search.rs\n│   │   ├── code_index.rs\n│   │   ├── terminal.rs\n│   │   └── error.rs\n│   ├── rpc.rs                # generic JSON-RPC 2.0 envelopes\n│   ├── export.rs             # pure deterministic generators\n│   ├── schema_fixtures.rs    # contract/golden tests\n│   └── bin/\n│       ├── export.rs\n│       └── write_schema_fixtures.rs\n└── schema/\n    ├── schema.json\n    └── types.ts\n```\n\n## 公共契约\n\n### 注册表\n\n| Symbol | 职责 |\n| --- | --- |\n| `ClientMethod` | typed enum of every client-callable method |\n| `client_method` | exact string → `ClientMethod` lookup |\n| `CLIENT_METHODS` | method name、params/result type function、serialization scope |\n| `ServerNotificationMethod` | typed enum of server notifications |\n| `server_notification_method` | exact string → notification enum |\n| `SERVER_NOTIFICATIONS` | notification name 与 params type |\n| `ServerNotification` | 跨 crate 非穷尽的 canonical typed notification；consumer 只投影自己拥有的 capability |\n| `decode_server_notification` | registry-generated method/payload decoder；未知 method 无损保留为 `Unknown` |\n| `HostMethod` | App Server 可以向已声明能力的 client 发出的 typed request enum |\n| `HOST_METHODS` | host request name 与 params/result type 的唯一 registry |\n| `SerializationScopeDefinition` | dispatcher serialization requirement |\n\n`ClientCapabilities.browser` 是 connection-local browser host 声明。版本 1 用 `observe` 和 `input`\n显式表示 client 能处理的语义操作；它不授予 Rust 任意 CDP、Node 或浏览器对象访问权。\n`browser/create`、`browser/observe`、`browser/perform` 和 `browser/close` 是当前全部 host method。\n每个 action 都携带精确 `targetId`；点击和输入只使用观察结果中的 backend DOM node ID。\n\n方法注册表当前覆盖初始化、Session 生命周期/聚合订阅、canonical `session/request` mutation、\n带 Session scope 的 Thread 读取/订阅、模型目录、Connector list/API-token connect/disconnect、\n配置/供应商/MCP/Skill/Plugin request/Hook declaration 修改、signed language Marketplace list/install、\ndigest-pinned `skill/resource/open`、\nTurn 与 Resource metadata/read/release、filesystem metadata/read/write、workspace search start/read/cancel、\nworkspace code-index status/search/rebuild，以及 cloud code-index status/preview/authorize/sync/revoke。\nNotification 包含 `session/update`、Session-owned child 的 `session/thread/update`、owner-directed\n`agent/request`、`connector/changed`、`skills/changed`、`git/statusChanged` 与 `fs/changed`；Terminal 当前使用\nprofile/list 与 create/write/resize/read/close 的有界 pull\ncontract，不伪装成主动 notification stream。\n\n`SessionSubscribeResult` 是产品宿主的 aggregate port：`session/subscribe` 返回 Session snapshot、\nSession durable gap 和 `SessionThreadProjection` 列表；App Server 同时为这些 child Thread 建立\nconnection-local update delivery。需要单独读取或追赶一个 child Thread 时使用\n`session/thread/read` / `session/thread/subscribe`，两者都必须携带 `sessionId`。\n两种 Thread snapshot 请求都可携带 bounded history：`ThreadSnapshotHistory::Latest { turnLimit }`\n（范围为 `1..=MAX_THREAD_SNAPSHOT_TURNS`）取得连续的最新 Turn 窗口；\n`ThreadSnapshotHistory::Before { turnId, turnLimit }` 取得指定 durable Turn 之前的一页历史。两者的\nresult 都通过 `ThreadHistoryBoundary` 明确报告是否仍有更早 Turn 以及当前页最老 Turn identity，\n因此客户端可以使用 durable Turn identity 继续向前翻页，而不需要自行保存或重建 Thread history。\n`Before` 页返回的 `Thread.sequence` 仍是读取时的聚合序号，不表示该旧页覆盖了这一序号对应的最新\nTurns；客户端不得用旧页确认 durable update cursor，也不得用旧页替换已加载的最新窗口。\n省略 history 保持完整 snapshot 语义，供 rewind、MCP Agent 等需要完整历史的调用方使用。subscribe\n的 durable gap 从返回 snapshot 的 sequence 之后开始，不重复传输已经包含在 snapshot 中的事件；\n省略 history 的 reconnect 调用仍从客户端提供的 `afterSequence` 返回完整 gap。\n`SessionRequestParams` / `SessionRequest` 是 mutation 的 canonical Session contract：公共请求统一\n携带 `CommandId`、Session sequence 和 typed operation，结果通过 `SessionRequestResult` 的 tagged\nunion 返回。旧的独立 Session/Thread/Turn mutation methods 不在 registry 中。\n\n`ClientCapabilities.agentInteractions` 用 version + explicit kinds 声明 connection 能实际处理的\nAgent interaction；当前 App Server version 为 1。普通 Thread snapshot 只公开\n`PendingInteraction` metadata，full `TurnInteraction` 仅通过 `agent/request` 发送给一个同时满足\ncapability 和 Session-owned Thread subscription 的 connection。响应继续使用 canonical\n`SessionRequest::ResolveInteraction` + exact `RequestId`；非 owner/过期响应分别返回稳定\n`AgentInteractionNotOwner` / `AgentInteractionExpired`。owner、重选和 timer 都是 App Server runtime\n状态，不进入 DTO；deadline/cancel reason 则是 durable canonical fact。\nGit 注册 `git/status`、`git/textDiff`、`git/branch/list` 查询，以及分支切换、暂存、取消暂存、\n丢弃工作树修改、提交、抓取、拉取和推送等全局互斥变更方法；\nstatus 带 revision 和 repository-relative `workspacePath`，投影变化通过 `git/statusChanged` 发送\n完整 snapshot；该相对关系允许本地 host 提供 repository-root 导航，但不会把 host 绝对路径放进\n共享协议。\nFilesystem 注册 `fs/writeFile` global-exclusive mutation；`fs/changed` 只携带 workspace-relative\ninvalidation hint 或 rescan request，不成为 durable 文件事件。\n\n`ConnectorSecretDto` 只实现 inbound `Deserialize`，`Debug` 永远脱敏并在 Drop 时 zeroize；它不能增加\n`Clone`、`Serialize` 或普通字符串 getter。新增 Connector response 字段必须继续证明 schema/result 中\n没有 credential reference 或 value。修改 registry 后必须同步生成两份 schema fixture。\n\n### JSON-RPC\n\n`JsonRpcRequest<P>`、`JsonRpcNotification<P>`、`JsonRpcSuccess<R>`、`JsonRpcFailure<E>` 和\n`JsonRpcResponse<R,E>` 对 envelope 使用 `deny_unknown_fields`。`JsonRpcId` 保留 number、string\n与 null 的 parse shape；App Server 对 Client → Server request 实施更窄的 positive-integer ID\npolicy，Server → Client browser request 使用与其隔离的非空字符串 ID。\n\n`JsonRpcVersion` 只接受 `\"2.0\"`。\n\n### 导出\n\n| Symbol | 输出 |\n| --- | --- |\n| `json_schema()` | pretty JSON schema + trailing newline |\n| `typescript()` | DTO declarations、client/host method map、notification union、schema hash |\n| `schema_hash()` | `sha256:` + generated schema 的 deterministic digest |\n| `JSON_SCHEMA_FIXTURE` / `TYPESCRIPT_FIXTURE` | checked-in artifact 相对路径 |\n\nGenerator 只返回字符串，不读写 filesystem。只有 `write_schema_fixtures` binary 拥有 fixture 写入。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `client_methods!` | private macro | 一次定义 method enum、lookup、metadata、request/result schema enums | method 不得在 dispatcher 建第二份表 |\n| `host_methods!` | private macro | 一次定义 Server → Client method enum、lookup、metadata 与 request/result schema enums | Desktop handler 不手写平行 method name/type map |\n| `server_notifications!` | private macro | 一次定义 method enum、lookup、canonical runtime enum、payload decoder、metadata 与 schema enum | notification list 与 decode mapping 的唯一来源 |\n| `typescript_bindings!` | private macro | 建立显式 DTO declaration list | canonical/re-exported nested types 也必须可生成 |\n| `ClientRequestSchema` | crate-private generated enum | 聚合所有 request params 到 root schema | 不是 runtime dispatcher payload |\n| `ClientResultSchema` | crate-private generated enum | 聚合所有 method result | 与 method registry lockstep |\n| `HostRequestSchema` / `HostResultSchema` | crate-private generated enum | 聚合全部 reverse request params/result | 与 `HOST_METHODS` lockstep |\n| `ServerNotificationSchema` | crate-private generated enum | 聚合 notification params | 与 notification registry lockstep |\n| `TypeScriptBinding` | crate-private | 延迟调用每个 `TS::decl` | declaration order 必须 deterministic |\n| `type_name<T>` / `declaration<T>` | private | ts-rs adapter functions | 不手写 DTO field shape |\n| `ProtocolSchema` | private | JSON Schema root：client/host request/response、notification/error | artifact coverage 的唯一 root |\n| `protocol_schema` | private | `schema_for!(ProtocolSchema)` | hash 与 JSON 必须调用同一函数 |\n| `Artifact::{parse,contents}` | binary-private | stdout exporter format selection | 不影响 library contract |\n| `write_fixture` | binary-private | explicit golden fixture overwrite | tests 不能隐式调用 |\n\n`TYPESCRIPT_BINDINGS` 是一个容易遗漏的同步点。例如新增 canonical enum 被 RPC DTO 引用时，若\nts-rs 无法从 outer declaration 自动内联，就必须显式加入这里。编译通过不代表 TypeScript fixture\n完整。\n\n## 注册表→ artifact 调用图\n\n```text\nclient_methods!\n├─ ClientMethod / client_method\n├─ CLIENT_METHODS\n├─ ClientRequestSchema\n└─ ClientResultSchema\n\nserver_notifications!\n├─ ServerNotificationMethod / lookup\n├─ ServerNotification / decode_server_notification\n├─ SERVER_NOTIFICATIONS\n└─ ServerNotificationSchema\n\njson_schema()\n└─ protocol_schema()\n   └─ schema_for!(ProtocolSchema)\n\nschema_hash()\n└─ serialize protocol_schema()\n   └─ SHA-256\n\ntypescript()\n├─ schema_hash()\n├─ TYPESCRIPT_BINDINGS → TypeScriptBinding::declaration\n├─ SERVER_NOTIFICATIONS → ServerNotification union/map\n├─ HOST_METHODS → AppServerHostMethodMap + constants\n└─ CLIENT_METHODS → AppServerMethodMap + constants\n```\n\nRuntime dispatch 当前使用 `client_method(method)` 得到 typed method；`serialization` 已在 registry\n中声明，但现有同步 `AppServer::handle_json` 尚未读取它来建立 per-scope scheduler。它是待接入\ndispatcher 的 executable metadata，不能被误写成已经落实的并发保证。\n\n## DTO 建模约束\n\n- Canonical `Session`、`Thread`、`Turn`、`ThreadItem`、events、updates 与 typed IDs 直接\n  re-export/reuse `zeta-protocol`；不要复制相同语义 DTO。\n- RPC-only params/result 留在本 crate，例如 `SessionRequestParams`、`ConfigUpdateParams`、\n  `ResourceReadResult`。\n- `SyntaxAnalyzeResult` 只包含通用 snapshot facts；structural selections 使用独立的\n  `SyntaxSelectionRangesParams/Result`，避免普通分析序列化全树节点。\n- `WorkspaceDocumentOverlay*` DTO 只传 Editor-authoritative full snapshot 或相对 path，响应只返回\n  content-free generation/count；Symbol/CodeIndex store identity 不进入 wire。\n- `SymbolIndexSearchHitDto` 投影 revision-bound UTF-16 declaration/selection range，不承诺 stable semantic\n  symbol ID；retrieval origin 显式区分 local symbol/lexical/semantic/cloud。\n- Durable mutation params 带 `CommandId` 与 expected sequence/revision；JSON-RPC ID 不替代它。\n- `ConfigUpdateParams` 使用 `Patch<T>` 表达 missing/no-op、null/clear、value/set 三态。\n- `SessionRequest::ResolveInteraction` 使用 canonical `AgentResponse` 与 exact `RequestId`。\n- `AgentRequestEnvelope` 携带 Session/Thread/Turn aggregate context 和 full durable request，但不携带\n  connection owner；`ClientCapabilities.agentInteractions.kinds` 必须与可产生的 response kind 一致。\n- `SessionRequest::StartTurn.input` 是有序、非空的 tagged union：`text { text }` 或\n  `image { url }` 或 `skill { skill: SkillRef }`；图片在进入 wire contract 前必须已经从本地路径\n  规范化为 HTTP(S)/data URL，Skill 只能携带 source-qualified ID 与 version selector，不能携带\n  raw filesystem path。\n- `ProviderConfigDto.model_context` 是按模型 ID 索引的 context-window metadata，供 Core 决定\n  是否启用 deterministic budget/compaction；它不改变 endpoint normalization。\n- `InitializeResult.slash_commands` 是 server composition 在 handshake 时冻结的完整动态命令\n  snapshot；每项声明 canonical name、description 与 inline argument mode。动态命令提交仍是\n  普通 ordered Turn input，不引入第二套 command RPC。\n- Resource chunk 的 `data_base64` 是标准 Base64，`decoded_length` 是原始 bytes 数，最大 chunk\n  262,144 bytes。\n- Terminal raw output 使用 `TerminalOutputChunk.data_base64`；sequence 字段在 Rust 使用 `u64`，\n  TypeScript artifact 显式生成为 `number`，server 只产生 safe-lifetime 的单调值。`output_gap`\n  是 ring eviction 的显式信号，不能由客户端忽略。\n- `TerminalProfile` 只暴露稳定 ID、标题和 default 标记。Terminal create 只接受\n  `TerminalProfileSelection`，不包含 program、args、cwd 或 environment；这些 authority 留在\n  local App Server composition。Terminal params 与 profile selection 拒绝 unknown field，不能把\n  client-authored environment 藏在生成 DTO 之外。\n- `AppServerError` 的 `message` 是 stable `AppServerErrorName`，`data` 当前为 unit，不传任意\n  internal error text。\n- DTO 默认 camelCase；required/optional/nullability 要同时符合 serde、schemars 与 ts-rs。\n\n## 修改一个 RPC 的准确路径\n\n```text\n1. 在 protocol/<domain>.rs 定义/修改 Params 与 Result\n2. 在 client_methods! 中注册 method + types + serialization scope\n3. 若新增 TS dependency，更新 typescript_bindings!\n4. 更新 zeta-app-server dispatcher exhaustive match\n5. 运行 write_schema_fixtures\n6. 审阅 schema/schema.json 与 schema/types.ts\n7. 运行 contract tests\n8. 更新 docs/zeta-app-server-api.md 与 client\n```\n\n新增 notification 使用同一流程，但修改 `server_notifications!` 和 broker/connection publish path。\n由于该宏同时生成 runtime enum 与 decoder，不得再在 client crate 增加平行 decode match；只有实际\n拥有该 capability 的产品 projection 才需要增加消费逻辑。\n\n新增 Server → Client request 时改用 `host_methods!`，并同步实现 App Server broker 和 capable client\nhandler。host method 不填写 `SerializationScopeDefinition`，因为其顺序、owner、deadline 和取消由\n调用该 capability 的 broker 管理，不能借用 Client → Server dispatcher scope。\n\nFixture 更新必须显式执行：\n\n```text\ncargo run -p zeta-app-server-protocol --bin write_schema_fixtures\n```\n\n只查看生成结果：\n\n```text\ncargo run -p zeta-app-server-protocol --bin export -- json\ncargo run -p zeta-app-server-protocol --bin export -- typescript\n```\n\n## 模式哈希与兼容性\n\n`initialize` 返回 `SchemaHash`。当前 hash 是 `protocol_schema()` JSON serialization 的 SHA-256；\nJSON Schema generator/order 的任何确定性变化都可能改变 hash，不只 breaking API change。\n\n因此 hash 是“artifact exact match”信号，不是 semantic version。改变 DTO 后必须审阅 artifact\ndiff，明确客户端是否能同步升级；不要在测试中为了保留旧 hash 而隐藏真实 schema change。\n\n## 方向偏差检查\n\n- Dispatcher 中出现平行 method string/scope table：registry 不再唯一；\n- DTO 在 App Server implementation 内定义：schema/TS 生成无法覆盖；\n- `rpc.rs` 引用具体 Session/Config DTO：generic envelope 边界被破坏；\n- `export.rs` 读取/写入文件或环境：generator 不再 pure/deterministic；\n- Rust DTO 更新但 fixture 未变：binding list/root schema 可能漏项；\n- Client crate 出现按 method 维护的第二份 notification decoder：registry 不再是唯一事实源；\n- 产品穷尽枚举未拥有的 notification：wire capability 变化重新扩散到无关 consumer；\n- Canonical value 在本 crate 被复制：`zeta-protocol` source 分叉；\n- Connection owner、actor handle 或 resource bytes 进入 DTO：runtime state 泄漏到 wire；\n- `AppServerError` 传递内部 error string：安全与兼容性 contract 漂移；\n- Client 使用 arbitrary method string 而非 generated method map：compile-time contract 被绕过。\n- Desktop 手写 host method 字符串或结果 shape：reverse RPC artifact 与 Rust registry 分叉。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-app-server-protocol\nbazel test //zeta-rs/app-server-protocol:app-server-protocol-unit-tests\n```\n\n## Typst 文档契约\n\n`protocol::document` 拥有 `document/typst/compile` 的请求，以及对渲染进程安全的诊断和结果\n数据结构。该方法注册为全局独占，并通过 `ServerCapabilities::typst` 公布。PDF 内容由\n`ResourceMetadataResult` 引用，字节不会嵌入编译响应。\n\n修改这些数据结构时，必须重新生成 `schema/schema.json` 和 `schema/types.ts`，同步\n`desktop/generated/app-server/types.ts`，并更新\n[`docs/typst.md`](../../docs/typst.md) 中的跨进程契约。\n\n测试验证 method/notification 唯一性、JSON-RPC 2.0 envelope、TypeScript model/patch shape、root\nschema coverage、host method 名称与 request/result coverage、agent interaction capability/request/error、\nconfig 三态、MCP/Skill round trip、schema\nhash 和 checked-in fixtures exact match。\n\n当前只有 JSON-RPC 2.0 artifact，schema compatibility 依赖 exact hash + synchronized client build；\n尚无 semantic protocol version、migration layer 或 multi-version server。未来增加 Protobuf 或\ncompatibility window 时，应复用 `protocol::*` 语义和 registry，不把第二套 method ownership 带进\ntransport adapter。",
    "headings": [
      {
        "depth": 2,
        "id": "crate-边界",
        "title": "Crate 边界"
      },
      {
        "depth": 2,
        "id": "目录与职责",
        "title": "目录与职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 3,
        "id": "注册表",
        "title": "注册表"
      },
      {
        "depth": 3,
        "id": "json-rpc",
        "title": "JSON-RPC"
      },
      {
        "depth": 3,
        "id": "导出",
        "title": "导出"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "注册表-artifact-调用图",
        "title": "注册表→ artifact 调用图"
      },
      {
        "depth": 2,
        "id": "dto-建模约束",
        "title": "DTO 建模约束"
      },
      {
        "depth": 2,
        "id": "修改一个-rpc-的准确路径",
        "title": "修改一个 RPC 的准确路径"
      },
      {
        "depth": 2,
        "id": "模式哈希与兼容性",
        "title": "模式哈希与兼容性"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      },
      {
        "depth": 2,
        "id": "typst-文档契约",
        "title": "Typst 文档契约"
      }
    ],
    "searchText": " 本 README 解释 App Server external RPC contract、method registry 与 artifact generator。 面向客户端的 API 语义见 docs/zeta-app-server-api.md， canonical product values 见 docs/protocol.md，workspace 搜索的跨层 ownership 见 docs/search.md，Terminal external 语义见 docs/zeta-app-server-api.md，Git/SCM ownership 见 docs/git.md，Desktop 浏览器宿主的跨进程语义见 docs/zeta-desktop-architecture.md。 zeta-app-server-protocol 是 App Server 对外 wire contract 的唯一 Rust source。它定义类型化 参数、结果、错误、JSON-RPC 2.0 envelope，以及客户端方法、服务端通知和宿主方法注册表；同一套 定义还会生成 canonical ServerNotification、payload decoder、JSON Schema、TypeScript binding 和协商用 schema hash。 它不拥有 connection、framing、dispatcher、业务执行、Core state、resource bytes 或 persistence。 Crate 边界 ``text zeta-protocol canonical values │ ▼ zeta-app-server-protocol ├─ protocol DTO ├─ client method / server notification / host method registry ├─ generic JSON-RPC envelope └─ schema / TypeScript / hash generator │ ├─ zeta-app-server dispatcher └─ Desktop / CLI clients ` 语义与 JSON-RPC wrapper 分开： - protocol:: 定义 method params/result 与 App Server-specific DTO； - rpc:: 只定义 transport-neutral generic RPC envelope； - export:: 把 registry、DTO 和 envelope 聚合成 artifact。 未来换编码时可以复用业务 DTO；不要把 jsonrpc、wire request ID 或 connection state放进 protocol:: DTO。 目录与职责 `text zeta-rs/app-server-protocol/ ├── src/ │ ├── protocol/ │ │ ├── registry.rs method、notification、serialization scope │ │ ├── notification.rs canonical notification API re-export │ │ ├── common.rs handshake/shared wire values │ │ ├── browser.rs client-hosted semantic browser request/result DTO │ │ ├── initialize.rs │ │ ├── slashcommands.rs │ │ ├── session.rs │ │ ├── model.rs │ │ ├── language.rs language-server Config 与 Marketplace DTO │ │ ├── turn.rs │ │ ├── config.rs │ │ ├── connectors.rs redacted connection DTO + inbound-only secret │ │ ├── resources.rs │ │ ├── search.rs │ │ ├── codeindex.rs │ │ ├── terminal.rs │ │ └── error.rs │ ├── rpc.rs generic JSON-RPC 2.0 envelopes │ ├── export.rs pure deterministic generators │ ├── schemafixtures.rs contract/golden tests │ └── bin/ │ ├── export.rs │ └── writeschemafixtures.rs └── schema/ ├── schema.json └── types.ts ` 公共契约 注册表 Symbol 职责 --- --- ClientMethod typed enum of every client-callable method clientmethod exact string → ClientMethod lookup CLIENTMETHODS method name、params/result type function、serialization scope ServerNotificationMethod typed enum of server notifications servernotificationmethod exact string → notification enum SERVERNOTIFICATIONS notification name 与 params type ServerNotification 跨 crate 非穷尽的 canonical typed notification；consumer 只投影自己拥有的 capability decodeservernotification registry-generated method/payload decoder；未知 method 无损保留为 Unknown HostMethod App Server 可以向已声明能力的 client 发出的 typed request enum HOSTMETHODS host request name 与 params/result type 的唯一 registry SerializationScopeDefinition dispatcher serialization requirement ClientCapabilities.browser 是 connection-local browser host 声明。版本 1 用 observe 和 input 显式表示 client 能处理的语义操作；它不授予 Rust 任意 CDP、Node 或浏览器对象访问权。 browser/create、browser/observe、browser/perform 和 browser/close 是当前全部 host method。 每个 action 都携带精确 targetId；点击和输入只使用观察结果中的 backend DOM node ID。 方法注册表当前覆盖初始化、Session 生命周期/聚合订阅、canonical session/request mutation、 带 Session scope 的 Thread 读取/订阅、模型目录、Connector list/API-token connect/disconnect、 配置/供应商/MCP/Skill/Plugin request/Hook declaration 修改、signed language Marketplace list/install、 digest-pinned skill/resource/open、 Turn 与 Resource metadata/read/release、filesystem metadata/read/write、workspace search start/read/cancel、 workspace code-index status/search/rebuild，以及 cloud code-index status/preview/authorize/sync/revoke。 Notification 包含 session/update、Session-owned child 的 session/thread/update、owner-directed agent/request、connector/changed、skills/changed、git/statusChanged 与 fs/changed；Terminal 当前使用 profile/list 与 create/write/resize/read/close 的有界 pull contract，不伪装成主动 notification stream。 SessionSubscribeResult 是产品宿主的 aggregate port：session/subscribe 返回 Session snapshot、 Session durable gap 和 SessionThreadProjection 列表；App Server 同时为这些 child Thread 建立 connection-local update delivery。需要单独读取或追赶一个 child Thread 时使用 session/thread/read / session/thread/subscribe，两者都必须携带 sessionId。 两种 Thread snapshot 请求都可携带 bounded history：ThreadSnapshotHistory::Latest turnLimit （范围为 1..=MAXTHREADSNAPSHOTTURNS）取得连续的最新 Turn 窗口； ThreadSnapshotHistory::Before turnId, turnLimit 取得指定 durable Turn 之前的一页历史。两者的 result 都通过 ThreadHistoryBoundary 明确报告是否仍有更早 Turn 以及当前页最老 Turn identity， 因此客户端可以使用 durable Turn identity 继续向前翻页，而不需要自行保存或重建 Thread history。 Before 页返回的 Thread.sequence 仍是读取时的聚合序号，不表示该旧页覆盖了这一序号对应的最新 Turns；客户端不得用旧页确认 durable update cursor，也不得用旧页替换已加载的最新窗口。 省略 history 保持完整 snapshot 语义，供 rewind、MCP Agent 等需要完整历史的调用方使用。subscribe 的 durable gap 从返回 snapshot 的 sequence 之后开始，不重复传输已经包含在 snapshot 中的事件； 省略 history 的 reconnect 调用仍从客户端提供的 afterSequence 返回完整 gap。 SessionRequestParams / SessionRequest 是 mutation 的 canonical Session contract：公共请求统一 携带 CommandId、Session sequence 和 typed operation，结果通过 SessionRequestResult 的 tagged union 返回。旧的独立 Session/Thread/Turn mutation methods 不在 registry 中。 ClientCapabilities.agentInteractions 用 version + explicit kinds 声明 connection 能实际处理的 Agent interaction；当前 App Server version 为 1。普通 Thread snapshot 只公开 PendingInteraction metadata，full TurnInteraction 仅通过 agent/request 发送给一个同时满足 capability 和 Session-owned Thread subscription 的 connection。响应继续使用 canonical SessionRequest::ResolveInteraction + exact RequestId；非 owner/过期响应分别返回稳定 AgentInteractionNotOwner / AgentInteractionExpired。owner、重选和 timer 都是 App Server runtime 状态，不进入 DTO；deadline/cancel reason 则是 durable canonical fact。 Git 注册 git/status、git/textDiff、git/branch/list 查询，以及分支切换、暂存、取消暂存、 丢弃工作树修改、提交、抓取、拉取和推送等全局互斥变更方法； status 带 revision 和 repository-relative workspacePath，投影变化通过 git/statusChanged 发送 完整 snapshot；该相对关系允许本地 host 提供 repository-root 导航，但不会把 host 绝对路径放进 共享协议。 Filesystem 注册 fs/writeFile global-exclusive mutation；fs/changed 只携带 workspace-relative invalidation hint 或 rescan request，不成为 durable 文件事件。 ConnectorSecretDto 只实现 inbound Deserialize，Debug 永远脱敏并在 Drop 时 zeroize；它不能增加 Clone、Serialize 或普通字符串 getter。新增 Connector response 字段必须继续证明 schema/result 中 没有 credential reference 或 value。修改 registry 后必须同步生成两份 schema fixture。 JSON-RPC JsonRpcRequest<P 、JsonRpcNotification<P 、JsonRpcSuccess<R 、JsonRpcFailure<E 和 JsonRpcResponse<R,E 对 envelope 使用 denyunknownfields。JsonRpcId 保留 number、string 与 null 的 parse shape；App Server 对 Client → Server request 实施更窄的 positive-integer ID policy，Server → Client browser request 使用与其隔离的非空字符串 ID。 JsonRpcVersion 只接受 \"2.0\"。 导出 Symbol 输出 --- --- jsonschema pretty JSON schema + trailing newline typescript DTO declarations、client/host method map、notification union、schema hash schemahash sha256: + generated schema 的 deterministic digest JSONSCHEMAFIXTURE / TYPESCRIPTFIXTURE checked-in artifact 相对路径 Generator 只返回字符串，不读写 filesystem。只有 writeschemafixtures binary 拥有 fixture 写入。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- clientmethods! private macro 一次定义 method enum、lookup、metadata、request/result schema enums method 不得在 dispatcher 建第二份表 hostmethods! private macro 一次定义 Server → Client method enum、lookup、metadata 与 request/result schema enums Desktop handler 不手写平行 method name/type map servernotifications! private macro 一次定义 method enum、lookup、canonical runtime enum、payload decoder、metadata 与 schema enum notification list 与 decode mapping 的唯一来源 typescriptbindings! private macro 建立显式 DTO declaration list canonical/re-exported nested types 也必须可生成 ClientRequestSchema crate-private generated enum 聚合所有 request params 到 root schema 不是 runtime dispatcher payload ClientResultSchema crate-private generated enum 聚合所有 method result 与 method registry lockstep HostRequestSchema / HostResultSchema crate-private generated enum 聚合全部 reverse request params/result 与 HOSTMETHODS lockstep ServerNotificationSchema crate-private generated enum 聚合 notification params 与 notification registry lockstep TypeScriptBinding crate-private 延迟调用每个 TS::decl declaration order 必须 deterministic typename<T / declaration<T private ts-rs adapter functions 不手写 DTO field shape ProtocolSchema private JSON Schema root：client/host request/response、notification/error artifact coverage 的唯一 root protocolschema private schemafor! ProtocolSchema hash 与 JSON 必须调用同一函数 Artifact:: parse,contents binary-private stdout exporter format selection 不影响 library contract writefixture binary-private explicit golden fixture overwrite tests 不能隐式调用 TYPESCRIPTBINDINGS 是一个容易遗漏的同步点。例如新增 canonical enum 被 RPC DTO 引用时，若 ts-rs 无法从 outer declaration 自动内联，就必须显式加入这里。编译通过不代表 TypeScript fixture 完整。 注册表→ artifact 调用图 `text clientmethods! ├─ ClientMethod / clientmethod ├─ CLIENTMETHODS ├─ ClientRequestSchema └─ ClientResultSchema servernotifications! ├─ ServerNotificationMethod / lookup ├─ ServerNotification / decodeservernotification ├─ SERVERNOTIFICATIONS └─ ServerNotificationSchema jsonschema └─ protocolschema └─ schemafor! ProtocolSchema schemahash └─ serialize protocolschema └─ SHA-256 typescript ├─ schemahash ├─ TYPESCRIPTBINDINGS → TypeScriptBinding::declaration ├─ SERVERNOTIFICATIONS → ServerNotification union/map ├─ HOSTMETHODS → AppServerHostMethodMap + constants └─ CLIENTMETHODS → AppServerMethodMap + constants ` Runtime dispatch 当前使用 clientmethod method 得到 typed method；serialization 已在 registry 中声明，但现有同步 AppServer::handlejson 尚未读取它来建立 per-scope scheduler。它是待接入 dispatcher 的 executable metadata，不能被误写成已经落实的并发保证。 DTO 建模约束 - Canonical Session、Thread、Turn、ThreadItem、events、updates 与 typed IDs 直接 re-export/reuse zeta-protocol；不要复制相同语义 DTO。 - RPC-only params/result 留在本 crate，例如 SessionRequestParams、ConfigUpdateParams、 ResourceReadResult。 - SyntaxAnalyzeResult 只包含通用 snapshot facts；structural selections 使用独立的 SyntaxSelectionRangesParams/Result，避免普通分析序列化全树节点。 - WorkspaceDocumentOverlay DTO 只传 Editor-authoritative full snapshot 或相对 path，响应只返回 content-free generation/count；Symbol/CodeIndex store identity 不进入 wire。 - SymbolIndexSearchHitDto 投影 revision-bound UTF-16 declaration/selection range，不承诺 stable semantic symbol ID；retrieval origin 显式区分 local symbol/lexical/semantic/cloud。 - Durable mutation params 带 CommandId 与 expected sequence/revision；JSON-RPC ID 不替代它。 - ConfigUpdateParams 使用 Patch<T 表达 missing/no-op、null/clear、value/set 三态。 - SessionRequest::ResolveInteraction 使用 canonical AgentResponse 与 exact RequestId。 - AgentRequestEnvelope 携带 Session/Thread/Turn aggregate context 和 full durable request，但不携带 connection owner；ClientCapabilities.agentInteractions.kinds 必须与可产生的 response kind 一致。 - SessionRequest::StartTurn.input 是有序、非空的 tagged union：text text 或 image url 或 skill skill: SkillRef ；图片在进入 wire contract 前必须已经从本地路径 规范化为 HTTP S /data URL，Skill 只能携带 source-qualified ID 与 version selector，不能携带 raw filesystem path。 - ProviderConfigDto.modelcontext 是按模型 ID 索引的 context-window metadata，供 Core 决定 是否启用 deterministic budget/compaction；它不改变 endpoint normalization。 - InitializeResult.slashcommands 是 server composition 在 handshake 时冻结的完整动态命令 snapshot；每项声明 canonical name、description 与 inline argument mode。动态命令提交仍是 普通 ordered Turn input，不引入第二套 command RPC。 - Resource chunk 的 database64 是标准 Base64，decodedlength 是原始 bytes 数，最大 chunk 262,144 bytes。 - Terminal raw output 使用 TerminalOutputChunk.database64；sequence 字段在 Rust 使用 u64， TypeScript artifact 显式生成为 number，server 只产生 safe-lifetime 的单调值。outputgap 是 ring eviction 的显式信号，不能由客户端忽略。 - TerminalProfile 只暴露稳定 ID、标题和 default 标记。Terminal create 只接受 TerminalProfileSelection，不包含 program、args、cwd 或 environment；这些 authority 留在 local App Server composition。Terminal params 与 profile selection 拒绝 unknown field，不能把 client-authored environment 藏在生成 DTO 之外。 - AppServerError 的 message 是 stable AppServerErrorName，data 当前为 unit，不传任意 internal error text。 - DTO 默认 camelCase；required/optional/nullability 要同时符合 serde、schemars 与 ts-rs。 修改一个 RPC 的准确路径 `text 1. 在 protocol/<domain .rs 定义/修改 Params 与 Result 2. 在 clientmethods! 中注册 method + types + serialization scope 3. 若新增 TS dependency，更新 typescriptbindings! 4. 更新 zeta-app-server dispatch"
  },
  {
    "slug": "crates/async-utils",
    "sourcePath": "zeta-rs/async-utils/README.md",
    "title": "zeta-async-utils",
    "description": "zeta-async-utils 提供 runtime-independent cooperative cancellation。它只依赖 std，不拥有 task spawn、deadline timer、signal handling 或 OS interruption。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 cancellation tree 的当前接口与 race semantics。跨 Agent、model、Tool 与 process\n> 的 cancellation 演进见\n> [`docs/zeta-agent-runtime-architecture.md`](../../docs/zeta-agent-runtime-architecture.md)。\n\n`zeta-async-utils` 提供 runtime-independent cooperative cancellation。它只依赖 `std`，不拥有 task\nspawn、deadline timer、signal handling 或 OS interruption。\n\n## 公共契约\n\n| Symbol | 职责 | 关键语义 |\n| --- | --- | --- |\n| `CancellationSource<R>` | 一个 domain 的 cancel authority | clone 共享同一 domain；drop 不自动 cancel |\n| `CancellationToken<R>` | read-only observer 与 child-source factory | child cancel 不影响 parent/sibling |\n| `Cancellation<R>` | effective signal | 保留 first winner 的 reason 与 origin ID |\n| `CancelResult<R>` | cancel race 结果 | 区分本次 installed 与 already cancelled |\n| `Cancelled<R>` | 等待 token cancellation 的 future | drop 时注销 waiter |\n| `CancelOnDrop<R>` | scope-exit cancellation guard | `disarm` 后不 cancel |\n| `Cancelable<F, R>` | inner future 与 cancellation 的 race wrapper | cancellation 每次 poll 优先检查；获胜时 drop inner future |\n| `FutureCancellationExt` | 所有 Future 的 extension trait | 只在 drop-at-await 安全时使用 |\n\n默认 reason 是 `CancellationReason::{Requested, Shutdown, DeadlineExceeded}`。需要 domain-specific\nreason 时使用 `CancellationSource::<R>::new_typed()`；`R` 不要求 `Clone`，signal 通过 `Arc` 传播。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `Node<R>` | crate-private | 一个 cancellation domain 的 ID 与 mutex state | 不暴露给 runtime 或 protocol |\n| `State<R>` | private | signal、weak children、waiters、waiter ID allocator | state transition 始终在同一 mutex 下 |\n| `Signal<R>` | crate-private | origin + reason | descendants 共享同一 effective signal |\n| `Node::child_of` | crate-private | 原子注册 child 或继承 parent signal | child creation 与 parent cancel 不能丢信号 |\n| `cancel_tree` | crate-private | iterative propagation、drain waiters/children | 不递归；先标记整棵可达树，再 wake |\n| `poll_cancelled` | crate-private | register/refresh waiter waker | repoll 必须替换 stale waker |\n| `remove_waiter` | crate-private | future/drop cleanup | pending future drop 后不能泄漏 waker |\n| `Cancelable::poll` | private impl path | cancellation-first race | inner poll 中触发 cancel 时，本次 inner output 获胜 |\n| `NEXT_CANCELLATION_ID` / `next_id` | private | process-local unique domain ID | overflow panic，不复用 identity |\n\n## 调用图与 race 语义\n\n```text\nCancellationSource::cancel_with(reason)\n├─ Signal { origin: source.id, reason }\n├─ tree::cancel_tree(root, signal)\n│  ├─ iterative pending stack\n│  ├─ first signal per Node wins\n│  ├─ inherit effective signal into descendants\n│  ├─ drain weak children and waiters\n│  └─ wake after reachable nodes are marked\n└─ Cancelled | AlreadyCancelled(effective signal)\n\nCancellationToken::cancelled\n└─ Cancelled::poll\n   └─ token.poll_cancelled\n      └─ tree::poll_cancelled → ready signal or registered waiter\n\nfuture.with_cancellation(token)\n└─ Cancelable::poll\n   ├─ poll cancellation first\n   ├─ cancelled → drop inner, return Err\n   └─ active → poll inner, return Ok or Pending\n```\n\n重要 race：\n\n- child creation 与 parent cancellation 在 parent mutex 下序列化，因此不会 missed cancellation；\n- domain 已有 signal 后，新 child 直接继承 effective signal；\n- 两个 canceller 竞争时，每个 Node 的 first observed signal wins；\n- inner future 在自己的 `poll` 内触发 cancellation 并同时 Ready，本次 output wins，因为 cancellation\n  checkpoint 已经发生；\n- waker 在离开 mutex 后调用，避免 wake path 重入锁。\n\n方向偏差：\n\n- 使用 strong child reference：完成的 child tree 会被 parent 永久保活；\n- 递归传播：深 Agent tree 可能 stack overflow；\n- plain source drop 自动 cancel：clone owner lifecycle 变得不可预测；\n- `Cancelable` 尝试异步 cleanup：wrapper 越过 generic Future contract；\n- token 可以 cancel 自己：observer/authority capability split 被破坏。\n\n## 使用选择\n\n使用 `with_cancellation` 仅当取消时直接 drop inner future 是安全的。需要 flush、rollback、关闭\nprotocol 或释放远端 lease 时，把 `CancellationToken` 传入 worker，并在安全点调用 `check()` 或\nawait `cancelled()`。\n\n取消不能抢占阻塞系统调用、CPU 循环或没有观察令牌的分离任务。截止时间由调用方计时器决定，\n再用 `cancel_with(DeadlineExceeded)` 传播。\n\n## 测试与演进\n\n```text\ncargo test -p zeta-async-utils\nbazel test //zeta-rs/async-utils:async-utils-unit-tests\n```\n\n测试覆盖 non-`Unpin` future、prior cancellation precedence、drop cleanup、first-reason wins、\nconcurrent child creation、waker replacement、deep iterative propagation 和 RAII guard。\n\n当前没有 runtime-specific select macro、deadline timer、task group/join set 或 forced abort。未来\n若增加 structured task ownership，应建立在现有 source/token capability split 上，而不是把 Tokio\nhandle 或 executor type 放进本 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "调用图与-race-语义",
        "title": "调用图与 race 语义"
      },
      {
        "depth": 2,
        "id": "使用选择",
        "title": "使用选择"
      },
      {
        "depth": 2,
        "id": "测试与演进",
        "title": "测试与演进"
      }
    ],
    "searchText": " 本 README 解释 cancellation tree 的当前接口与 race semantics。跨 Agent、model、Tool 与 process 的 cancellation 演进见 docs/zeta-agent-runtime-architecture.md。 zeta-async-utils 提供 runtime-independent cooperative cancellation。它只依赖 std，不拥有 task spawn、deadline timer、signal handling 或 OS interruption。 公共契约 Symbol 职责 关键语义 --- --- --- CancellationSource<R 一个 domain 的 cancel authority clone 共享同一 domain；drop 不自动 cancel CancellationToken<R read-only observer 与 child-source factory child cancel 不影响 parent/sibling Cancellation<R effective signal 保留 first winner 的 reason 与 origin ID CancelResult<R cancel race 结果 区分本次 installed 与 already cancelled Cancelled<R 等待 token cancellation 的 future drop 时注销 waiter CancelOnDrop<R scope-exit cancellation guard disarm 后不 cancel Cancelable<F, R inner future 与 cancellation 的 race wrapper cancellation 每次 poll 优先检查；获胜时 drop inner future FutureCancellationExt 所有 Future 的 extension trait 只在 drop-at-await 安全时使用 默认 reason 是 CancellationReason:: Requested, Shutdown, DeadlineExceeded 。需要 domain-specific reason 时使用 CancellationSource::<R ::newtyped ；R 不要求 Clone，signal 通过 Arc 传播。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- Node<R crate-private 一个 cancellation domain 的 ID 与 mutex state 不暴露给 runtime 或 protocol State<R private signal、weak children、waiters、waiter ID allocator state transition 始终在同一 mutex 下 Signal<R crate-private origin + reason descendants 共享同一 effective signal Node::childof crate-private 原子注册 child 或继承 parent signal child creation 与 parent cancel 不能丢信号 canceltree crate-private iterative propagation、drain waiters/children 不递归；先标记整棵可达树，再 wake pollcancelled crate-private register/refresh waiter waker repoll 必须替换 stale waker removewaiter crate-private future/drop cleanup pending future drop 后不能泄漏 waker Cancelable::poll private impl path cancellation-first race inner poll 中触发 cancel 时，本次 inner output 获胜 NEXTCANCELLATIONID / nextid private process-local unique domain ID overflow panic，不复用 identity 调用图与 race 语义 ``text CancellationSource::cancelwith reason ├─ Signal origin: source.id, reason ├─ tree::canceltree root, signal │ ├─ iterative pending stack │ ├─ first signal per Node wins │ ├─ inherit effective signal into descendants │ ├─ drain weak children and waiters │ └─ wake after reachable nodes are marked └─ Cancelled AlreadyCancelled effective signal CancellationToken::cancelled └─ Cancelled::poll └─ token.pollcancelled └─ tree::pollcancelled → ready signal or registered waiter future.withcancellation token └─ Cancelable::poll ├─ poll cancellation first ├─ cancelled → drop inner, return Err └─ active → poll inner, return Ok or Pending ` 重要 race： - child creation 与 parent cancellation 在 parent mutex 下序列化，因此不会 missed cancellation； - domain 已有 signal 后，新 child 直接继承 effective signal； - 两个 canceller 竞争时，每个 Node 的 first observed signal wins； - inner future 在自己的 poll 内触发 cancellation 并同时 Ready，本次 output wins，因为 cancellation checkpoint 已经发生； - waker 在离开 mutex 后调用，避免 wake path 重入锁。 方向偏差： - 使用 strong child reference：完成的 child tree 会被 parent 永久保活； - 递归传播：深 Agent tree 可能 stack overflow； - plain source drop 自动 cancel：clone owner lifecycle 变得不可预测； - Cancelable 尝试异步 cleanup：wrapper 越过 generic Future contract； - token 可以 cancel 自己：observer/authority capability split 被破坏。 使用选择 使用 withcancellation 仅当取消时直接 drop inner future 是安全的。需要 flush、rollback、关闭 protocol 或释放远端 lease 时，把 CancellationToken 传入 worker，并在安全点调用 check 或 await cancelled 。 取消不能抢占阻塞系统调用、CPU 循环或没有观察令牌的分离任务。截止时间由调用方计时器决定， 再用 cancelwith DeadlineExceeded 传播。 测试与演进 `text cargo test -p zeta-async-utils bazel test //zeta-rs/async-utils:async-utils-unit-tests ` 测试覆盖 non-Unpin` future、prior cancellation precedence、drop cleanup、first-reason wins、 concurrent child creation、waker replacement、deep iterative propagation 和 RAII guard。 当前没有 runtime-specific select macro、deadline timer、task group/join set 或 forced abort。未来 若增加 structured task ownership，应建立在现有 source/token capability split 上，而不是把 Tokio handle 或 executor type 放进本 crate。"
  },
  {
    "slug": "crates/attachments",
    "sourcePath": "zeta-rs/attachments/README.md",
    "title": "zeta-attachments",
    "description": "zeta-attachments owns admission and durable identity for image bytes used by Threads. It sits between untrusted local/remote/upload inputs and zeta-core; model providers never read this",
    "group": "Crate 实现参考",
    "markdown": "`zeta-attachments` owns admission and durable identity for image bytes used by Threads. It sits\nbetween untrusted local/remote/upload inputs and `zeta-core`; model providers never read this\nstore directly.\n\nThe canonical path is:\n\n```text\nlocal bytes / remote URL / Tool image\n  -> bounded fetch or upload\n  -> zeta-utils-image validation and normalization\n  -> ImageAttachmentStore put-before-event\n  -> ImageAttachmentRef in Thread history\n  -> verified read and ephemeral data URL at model invocation\n```\n\n`FileImageAttachmentStore` writes content-addressed objects below `attachments/sha256`, fsyncs a\nsame-directory temporary file, and publishes it without replacing an existing object. A failed\nThread append can leave a harmless unreferenced object; garbage collection is intentionally a\nseparate maintenance operation.\n\n`ImageAttachments` is the canonical admission/read service. `reference_for_image` derives trusted\nmetadata only after `zeta-utils-image` validation; `verify_reference_bytes` rechecks digest, byte\nlength, encoding signature and dimensions whenever a reference is read. Product hosts install a\n`FileImageAttachmentStore`; tests and explicitly ephemeral hosts may use\n`MemoryImageAttachmentStore`.\n\n`SafeRemoteImageFetcher` uses a direct, redirect-rejecting HTTP client whose actual resolver\nrejects loopback, private, link-local, multicast, documentation, benchmark, and unspecified\naddresses. Redirects are followed manually and revalidated; HTTPS cannot downgrade to HTTP.\n\nCurrent limitation: this crate does not yet implement reference tracing or orphan garbage\ncollection. Removing an object before every durable Thread reference has expired is invalid.",
    "headings": [],
    "searchText": "zeta-attachments owns admission and durable identity for image bytes used by Threads. It sits between untrusted local/remote/upload inputs and zeta-core; model providers never read this store directly. The canonical path is: ``text local bytes / remote URL / Tool image - bounded fetch or upload - zeta-utils-image validation and normalization - ImageAttachmentStore put-before-event - ImageAttachmentRef in Thread history - verified read and ephemeral data URL at model invocation ` FileImageAttachmentStore writes content-addressed objects below attachments/sha256, fsyncs a same-directory temporary file, and publishes it without replacing an existing object. A failed Thread append can leave a harmless unreferenced object; garbage collection is intentionally a separate maintenance operation. ImageAttachments is the canonical admission/read service. referenceforimage derives trusted metadata only after zeta-utils-image validation; verifyreferencebytes rechecks digest, byte length, encoding signature and dimensions whenever a reference is read. Product hosts install a FileImageAttachmentStore; tests and explicitly ephemeral hosts may use MemoryImageAttachmentStore. SafeRemoteImageFetcher` uses a direct, redirect-rejecting HTTP client whose actual resolver rejects loopback, private, link-local, multicast, documentation, benchmark, and unspecified addresses. Redirects are followed manually and revalidated; HTTPS cannot downgrade to HTTP. Current limitation: this crate does not yet implement reference tracing or orphan garbage collection. Removing an object before every durable Thread reference has expired is invalid."
  },
  {
    "slug": "crates/auto-review",
    "sourcePath": "zeta-rs/auto-review/README.md",
    "title": "zeta-auto-review",
    "description": "zeta-auto-review 把一个已经由 host 完整解析的 ActionReviewRequest 交给独立 review model， 将严格 JSON 响应转换成绑定 action 与 policy revision 的 ClassifierAssessment。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 crate 内部实现、维护约束和修改路径。跨 `zeta-action-policy`、Core、App Server、\n> Tool 与 sandbox 的系统设计见 [`docs/auto-review.md`](../../docs/auto-review.md)。\n> 文档分层遵循 [`docs/documentation-guidelines.md`](../../docs/documentation-guidelines.md)。\n\n`zeta-auto-review` 把一个已经由 host 完整解析的 `ActionReviewRequest` 交给独立 review model，\n将严格 JSON 响应转换成绑定 action 与 policy revision 的 `ClassifierAssessment`。\n\n它只产生 advisory recommendation。它不能创建 grant、执行 Tool、修改 sandbox policy，也不拥有\n用户审批流程。\n\n## 核心产品流程（当前状态）\n\n本 crate 是下面流程中的 `classifier`，不是整条流程的 owner。跨 crate 的完整产品语义由\n[`docs/auto-review.md`](../../docs/auto-review.md#4-端到端决策模型) canonical 定义；这里保留\n最短摘要，因为这个调用位置直接决定 classifier 的输入和扩展方向。\n\n```text\nAgent action\n├─ deterministic deny → Block                                Current\n├─ 已有 exact grant → 直接执行                                Current\n└─ 其他 action\n   ├─ sandbox 不适用（如 Fetch / MCP / external）→ classifier Current\n   └─ local process\n      ├─ sandbox 无法满足 capability → classifier             Current\n      └─ sandbox 可以执行 → 先在 sandbox 中运行               Current\n         ├─ 成功或普通命令失败 → 返回执行结果                  Current\n         └─ 确认是 sandbox denial\n            ├─ safe to retry → classifier                     Current\n            └─ possible side effects / unknown → 不重放       Current\n\nclassifier\n└─ recommendation → ActionPolicyEngine 校验\n   ├─ Approve → exact grant\n   ├─ ReviseAction → Agent 换更安全的 action\n   ├─ AskUser → 请求用户批准\n   └─ Deny → Block\n```\n\n失败回流不能把所有 non-zero exit 都当成 sandbox denial。Tool executor 必须先区分\n普通命令失败与 sandbox enforcement denial，并保留可信的失败证据。即使确认是 denial，命令也\n可能在被拒绝前已经产生部分副作用；只有 Core 能证明没有副作用、或 action 可安全重放时，新的\nexact grant 才能触发自动重试。部分副作用可能发生或结果未知时，只能返回 Agent、请求用户或\n选择新的 action，不能自动重放原调用。\n\n## 1. Crate 边界\n\n本 crate 拥有：\n\n- versioned review protocol（system prompt、response schema 与 revision）；\n- provider-neutral `ReviewModel` port；\n- model input JSON 的序列化；\n- model input/response 大小与 strict wire JSON 校验；\n- 在 assessment 构造前调用 policy-owned capability constraint validation；\n- canonical assessment ID 的构造；\n- model、cancellation 和 invalid-response 错误分类；\n- auto-review seed eval corpus。\n\n本 crate 不拥有：\n\n- `ResolvedAction`、capability、review context 和 recommendation domain type；它们属于\n  `zeta-action-policy`；\n- recommendation 是否足以产生执行授权；它由 `ActionPolicyEngine` 决定；\n- review model 的选择、credential 和 provider runtime；它们由 App Server 组合；\n- user approval、durable Tool lifecycle 和 rejection circuit breaker；它们属于 Core；\n- OS enforcement；它属于 Tool executor 与 `zeta-sandboxing`。\n\n依赖方向必须保持：\n\n```text\nzeta-auto-review\n  ├─ zeta-action-policy\n  ├─ zeta-sandboxing（只读取 sandbox contract）\n  └─ zeta-async-utils\n\n禁止依赖：Core / App Server / provider runtime / credential store / approval UI\n```\n\n## 2. 文件与职责\n\n```text\nzeta-rs/auto-review/\n├── prompt.md                   # compile-time reviewer system policy\n├── src/\n│   ├── lib.rs                  # 私有模块与显式 public export\n│   ├── classifier.rs           # classifier orchestration、budgets、error mapping、binding\n│   ├── protocol.rs             # versioned prompt/schema、wire input/output、canonical response\n│   ├── review_model.rs         # provider-neutral model port 与 request/error contract\n│   └── classifier_tests.rs     # classifier 单元测试\n├── evals/\n│   ├── README.md               # corpus 格式、隐私与指标要求\n│   └── cases.jsonl             # versioned synthetic gold cases\n├── tests/\n│   └── eval_contract.rs        # 离线 corpus + ActionPolicyEngine contract\n├── BUILD.bazel\n└── Cargo.toml\n```\n\n不要把 provider adapter 或 Core orchestration 移入本 crate。需要增加内部实现时，保持 module\nprivate，并从 `lib.rs` 显式导出必要 API。\n\n## 3. 公共接口\n\n| API | 作用 | 实现者或调用者必须保证 |\n| --- | --- | --- |\n| `ReviewModel` | provider-neutral completion port | 观察 cancellation；按 request budget 收集 JSON；不提供 Tool、memory 或 mutation capability |\n| `ReviewModelRequest` | 一次 review 的 exact prompt payload 与 response budget | adapter 分别消费 system prompt、input JSON 和 response schema，并执行 `maximum_response_bytes` |\n| `ReviewModelError` | adapter failure contract | 区分 provider invocation failure 与 response oversize |\n| `LlmActionClassifier<M>` | 实现 `zeta_action_policy::ActionClassifier` | caller 只提供 review model；classifier 固定使用当前 `ReviewProtocol` |\n| `AutoReviewError` | fail-closed 的错误分类 | caller 只能映射为显式 failure policy，不能把错误当成批准 |\n\n`ActionReviewRequest`、`ClassifierAssessment`、`ClassifierRecommendation`、`RiskLevel` 和\n`UserAuthorization` 均来自 `zeta-action-policy`。这使 authorization authority 不依赖具体 LLM\nimplementation。\n\n## 4. 内部接口地图\n\n下面列出承载本 crate 设计方向的 private interface。它不是所有 helper 的机械清单；这些 symbol\n共同决定 model boundary、serialization、validation 和 assessment binding，修改时必须能说明\nownership 为什么仍然正确。\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `CURRENT_REVIEW_PROTOCOL` | private constant | 原子绑定 revision、system prompt 与 response schema | 修改 prompt/schema 语义必须在同一 module bump revision |\n| `MAX_MODEL_INPUT_BYTES` | private constant | serialized request 的 64 KiB 总上限 | Core 仍负责内容选择、截断与 secret removal |\n| `MAX_MODEL_RESPONSE_BYTES` | private constant | model response 的 16 KiB 上限 | 通过 request 交给 adapter 提前执行，classifier 再复检 |\n| `LlmActionClassifier::model_request` | private method | 将 policy request 组装为三个分离的 model payload | 只序列化，不重新解析 action、选择 model 或授予 capability |\n| `LlmActionClassifier::parse_recommendation` | private method | strict parse 后调用 policy-owned constraint validation | model capability 必须在 assessment 前验证，engine 仍会复检 |\n| `ModelInput` | private struct | 定义发送给 model 的 input JSON shape | 只借用 host-owned domain values，不拥有权限语义 |\n| `ModelInput::from` | private conversion | 映射 action、provenance、sandbox、revision、context | 不读取 Thread、config、credential 或 provider state |\n| `ModelSandboxCompatibility` | private enum | 把 sandbox contract 转成稳定的 tagged JSON | 只描述 host 结论，不让 model 修改 availability |\n| `ModelRecommendation` | private enum | strict deserialize target，拒绝 unknown fields | 不能直接成为 execution decision 或 grant |\n| `ModelCapability` | private struct | 对 capability 嵌套字段执行 `deny_unknown_fields` | 不直接反序列化 domain `Capability` 而丢弃未知字段 |\n| `From<ModelRecommendation> for ClassifierRecommendation` | private conversion impl | 转换成 `zeta-action-policy` advisory domain type | 转换后仍必须经过 host validation |\n| `response_json_bytes` | private function | 序列化 validated canonical recommendation | assessment ID 不 hash 未解析的 raw model text |\n| `RESPONSE_SCHEMA_JSON` | private constant | 定义四种 recommendation wire shape | 与 `ModelRecommendation` / `ModelCapability` lockstep 更新 |\n\n### 4.1 内部调用图\n\n```text\nActionClassifier::classify\n├─ cancellation.is_cancelled\n├─ LlmActionClassifier::model_request\n│  ├─ ModelInput::from\n│  ├─ serde_json::to_string(ModelInput)\n│  └─ input byte-limit check\n├─ ReviewModel::complete\n│  └─ adapter response byte-limit enforcement\n├─ cancellation.is_cancelled\n├─ classifier response byte-limit recheck\n├─ serde_json::from_str<ModelRecommendation>\n├─ From<ModelRecommendation> for ClassifierRecommendation\n├─ ClassifierRecommendation::validate_against\n├─ response_json_bytes\n├─ AssessmentId::from_response\n└─ ClassifierAssessment::new\n```\n\n这张图也用于识别实现漂移：\n\n- 如果 action resolution 出现在 `ModelInput::from`，host/policy ownership 已经漂移；\n- 如果 capability validation 只存在于 provider adapter，本地可信边界已经漂移；\n- 如果 raw response 在 parse/validation 前进入 assessment hash，audit identity 语义已经改变；\n- 如果 `ModelRecommendation` 直接映射为 execution authority，classifier 越过了 policy boundary；\n- 如果 model selection 或 mutable config read 进入 `model_request`，safe-point ownership 已经漂移。\n\n### 4.2 内部接口的同步修改关系\n\n| 修改 | 必须同步检查 |\n| --- | --- |\n| `ModelInput` field | `ModelInput::from`、trust/redaction owner、classifier tests、eval corpus |\n| `ModelRecommendation` variant/field | `RESPONSE_SCHEMA_JSON`、conversion impl、policy domain、Core handling、eval labels |\n| Capability JSON shape | `ModelCapability`、`RESPONSE_SCHEMA_JSON`、policy constraint validation、fixtures |\n| Prompt/schema policy | `CURRENT_REVIEW_PROTOCOL` revision、injection cases、model eval |\n| Assessment binding | `response_json_bytes`、`AssessmentId::from_response`、audit consumer、stable-ID tests |\n| Request/response limit | classifier constants、`ReviewModelRequest`、provider collection、large-payload tests |\n\n## 5. `classify()` 的准确执行顺序\n\n`LlmActionClassifier::classify()` 当前是同步的一次性调用：\n\n```text\n1. preflight cancellation check\n2. ActionReviewRequest → ModelInput JSON\n3. serialized input 64 KiB limit\n4. current ReviewProtocol → ReviewModelRequest\n5. ReviewModel::complete(...) with 16 KiB response budget\n6. model error/cancellation normalization\n7. post-call cancellation + response byte-limit recheck\n8. strict wire parse + policy-owned capability validation\n9. canonical recommendation → AssessmentId\n10. 返回 request-bound ClassifierAssessment\n```\n\n顺序具有安全含义：\n\n- 调用前已取消时，model 不应启动；\n- model 调用中或返回后发生取消时，结果被归类为 `Cancelled` 并丢弃；\n- input/response 上限分别是 64/16 KiB，使用 Rust `String::len()` 的 UTF-8 byte length；\n- adapter 在拼接 response fragment 时执行预算，classifier 在 parse 前再次检查；\n- `deny_unknown_fields` 在 recommendation 与嵌套 capability 两层拒绝额外字段；\n- assessment 复制 host-owned action digest 与 policy revision，模型不能覆盖它们。\n\n### 5.1 Assessment 身份\n\n`AssessmentId` 是下列内容的 SHA-256：\n\n```text\naction_digest\nNUL\npolicy_revision\nNUL\nreview_protocol_revision\nNUL\ncanonical_recommendation_json\n```\n\n这里使用解析后重新序列化的 canonical recommendation，而不是模型原始文本。因此仅改变 JSON\n空格或字段顺序不会产生新的 ID；改变 recommendation 语义、review protocol revision、action\n或 policy revision 会产生新 ID。\n\n`CURRENT_REVIEW_PROTOCOL` 在同一 private value 中绑定 system prompt、response schema 与\nrevision。修改 prompt/schema 语义时必须在 `protocol.rs` 同步 bump revision；caller 不能再传入\n一个与实际 prompt 不一致的 revision。\n\n## 6. 模型请求\n\n`ReviewModelRequest` 包含三个互相分离的 payload 和一个 response budget：\n\n| 字段 | 内容 | 信任级别 |\n| --- | --- | --- |\n| `system_prompt` | crate 内编译的 reviewer policy | trusted classifier instruction |\n| `input_json` | action、provenance、sandbox、phase、policy revision、review context | host metadata 与 untrusted content 的结构化混合 |\n| `response_schema_json` | 四种 recommendation 的严格 JSON schema | trusted output contract |\n| `maximum_response_bytes` | adapter 收集输出时必须执行的字节上限 | trusted classifier budget |\n\n`input_json` 的结构为：\n\n```text\naction\nprovenance\nsandbox\npolicy_revision\nphase\n  ├─ initial\n  └─ sandbox_denial\n       ├─ reason\n       └─ bounded output\ncontext\n  ├─ user_intent\n  └─ evidence[]\n       ├─ kind\n       ├─ trust\n       ├─ source\n       └─ content\n```\n\n本 crate 保留 evidence 的 trust label，但不负责生成或截断 context。Core/Tool host 必须在构造\n`ActionReviewRequest` 前完成内容长度限制、secret removal 和 trust classification。classifier\n对最终 serialized input 另设 64 KiB 总上限；它只拒绝超限 request，不替代 host 的选择与脱敏。\n\n`ReviewModel` 是安全边界而不只是 transport abstraction。trait 本身无法阻止恶意实现调用 Tool\n或读取 credential；可信 host adapter 必须确保 review runtime 没有这些能力。\n\n## 7. 响应契约与本地校验\n\n模型只能返回以下四种 shape：\n\n| Recommendation | 必填字段 | 本 crate 的额外检查 |\n| --- | --- | --- |\n| `approve` | `capabilities`, `risk`, `user_authorization`, `reason` | capabilities 必须非空且与 action required capabilities 完全相等 |\n| `revise_action` | `maximum_capabilities`, `reason` | maximum capabilities 必须是原 action capabilities 的子集，可以为空 |\n| `ask_user` | `reason` | 不接受额外字段 |\n| `deny` | `reason` | 不接受额外字段 |\n\nCapability 由精确的 `kind + scope` 组成。在 `approve` 中，模型不能通过扩大 scope、添加\ncapability kind 或遗漏原 capability 获得部分批准；`revise_action` 则有意允许返回原集合的\n子集。Wire 层使用 private `ModelCapability`，因此 capability object 内的未知字段也不会被\nserde 静默忽略。\n\nResponse schema 提供给 adapter 以约束生成，但本地 serde parse 和 host validation 才是最终\n可信边界。当前 App Server adapter 将 schema 放入 model instructions；即使 provider 不支持\n原生 structured output，malformed response 仍会在本 crate 被拒绝。Capability exact/subset\n规则由 `zeta-action-policy::ClassifierRecommendation::validate_against` 定义；auto-review 在构造\nassessment 前调用，`ActionPolicyEngine` 对任意 classifier implementation 的结果再次调用。\n\n本 crate 不执行 risk/authorization 到 execution decision 的映射。例如 high risk +\nimplicit authorization 即使被解析为 `Approve` recommendation，仍由 `ActionPolicyEngine` 转成\n`AskUser`。系统级矩阵见\n[`docs/auto-review.md`](../../docs/auto-review.md#5-风险与用户授权矩阵)。\n\n## 8. 错误与取消\n\n| 条件 | `AutoReviewError` | 是否产生 assessment |\n| --- | --- | --- |\n| model 调用前、调用中或返回后 cancellation | `Cancelled` | 否 |\n| request serialization failure | `InvalidRequest(String)` | 否 |\n| serialized input 超过 64 KiB | `RequestTooLarge { bytes }` | 否 |\n| `ReviewModelError::Invocation` | `Model(String)` | 否 |\n| response 超过 16 KiB | `ResponseTooLarge { bytes }` | 否 |\n| JSON 无效、字段缺失、unknown field、enum 无效 | `InvalidResponse(String)` | 否 |\n| approve capability 非 exact match | `InvalidResponse(String)` | 否 |\n| revise capability 不是 subset | `InvalidResponse(String)` | 否 |\n\n`ReviewModel::complete()` 返回 `Result<String, ReviewModelError>`。Port 只区分 invocation\nfailure 与 response oversize，不把完整 provider taxonomy 泄漏进 classifier。App Server 应在\n进入本 crate 前完成安全 redaction。错误最终如何成为 `Block` 或 `AskUser` 由\n`zeta-action-policy::ReviewFailurePolicy` 决定。\n\nCancellation 只能保证本 crate 在调用前后检查。正在进行的网络请求能否及时停止，取决于\n`ReviewModel` implementation 是否在 provider checkpoint 观察 token。\n\n## 9. Host 适配器接入要求\n\n当前 App Server 的 `ProviderReviewModel` 是参考 adapter。它从 frozen `ResolvedConfig`\nsafe-point snapshot 创建 immutable model runtime，并：\n\n- 不向 request 注册 Tool；\n- 将 `tool_choice` 固定为 `None`；\n- 禁止 parallel Tool Call；\n- 使用 temperature `0.0`；\n- 忽略 reasoning，只拼接 text；\n- 拒绝 refusal、Tool Call 和空 text；\n- 拼接每个 text fragment 前执行 `maximum_response_bytes`；\n- 在 provider invocation 前后检查 cancellation。\n\n新的 adapter 必须保持相同安全语义，但不要求使用相同 provider API。最小实现形态：\n\n```rust\nimpl ReviewModel for ProviderAdapter {\n    fn complete(\n        &self,\n        request: &ReviewModelRequest,\n        cancellation: &CancellationToken,\n    ) -> Result<String, ReviewModelError> {\n        // Invoke one immutable, tool-less reviewer, enforcing maximum_response_bytes\n        // while collecting its JSON text.\n    }\n}\n```\n\n不要在 adapter 内重新读取 mutable config、fallback 到未记录的 model，或把普通 Agent runtime\n连同其 Tool registry 直接复用给 reviewer。\n\n## 10. 测试与评估\n\n运行 crate 全部 Cargo 测试：\n\n```text\ncargo test -p zeta-auto-review\n```\n\n运行 Bazel targets：\n\n```text\nbazel test \\\n  //zeta-rs/auto-review:auto-review-unit-tests \\\n  //zeta-rs/auto-review:eval-contract-tests\n```\n\n测试分两层：\n\n- `classifier_tests.rs` 直接覆盖 parsing、nested strict fields、binding、capability escalation、\n  request/response budgets 和 cancellation；\n- `eval_contract.rs` 离线读取 `evals/cases.jsonl`，检查 schema/coverage，并把 gold\n  recommendation 送入真实 `ActionPolicyEngine` 验证最终 disposition。\n\n默认测试禁止访问网络或调用真实 model。Model-backed eval 必须是显式 runner，记录 model 与\nreview protocol revision，并输出 false-auto-approval 等安全指标。Corpus 的隐私和扩充规则见\n[`evals/README.md`](evals/README.md)。\n\n## 11. 常见修改路径\n\n### 修改 prompt 或审查策略表述\n\n1. 修改 [`prompt.md`](prompt.md)；\n2. 在同一 `CURRENT_REVIEW_PROTOCOL` 中 bump review protocol revision；\n3. 增加 prompt injection 与边界 case；\n4. 用显式 model eval runner 比较新旧 revision；\n5. 不因 wording 变化放宽本地 schema/validation。\n\n### 增加建议\n\n这不是 crate-local change。至少同时审查：\n\n1. `ModelRecommendation` 和 response schema；\n2. `zeta_action_policy::ClassifierRecommendation`；\n3. `ActionPolicyEngine` 的 final decision mapping；\n4. Core Tool scheduler 的 durable/feedback 行为；\n5. classifier tests、policy tests 和 eval corpus；\n6. `docs/auto-review.md` 的用户可见语义。\n\n### 增加证据类型\n\nEvidence domain type 属于 `zeta-action-policy`。新增 kind 后需要确认：\n\n- 哪个 host 负责产生它；\n- trust label 是否可能被伪造；\n- 是否含 repository/user/tool untrusted content；\n- 长度限制与 secret redaction 在哪里实施；\n- eval corpus 是否覆盖相应 injection path。\n\n### 更换供应商或增加模型\n\n通常不修改本 crate。应在 App Server/provider 组合层实现或选择新的 `ReviewModel`，并保持\nrequest、cancellation、tool-less runtime 与 immutable config snapshot contract。\n\n## 12. 当前限制\n\n当前实现有意保持较窄：\n\n- 一个 action 对应一次同步 completion，不支持 streaming、ensemble 或分阶段 review；\n- prompt 是 compile-time constant，没有 per-organization policy steering；\n- prompt/schema/revision 已在 private `ReviewProtocol` 中原子绑定，但 schema 尚未作为独立 artifact 导出；\n- reason 只要求是 string，production validator 尚未检查空字符串或 rationale quality；\n- context item budget 与 secret removal 依赖 Core/Tool host；crate 只执行 serialized input 总上限；\n- seed corpus 只验证格式与 policy contract，尚未运行真实 model benchmark；\n- 没有 calibration、shadow-mode telemetry 或 human override feedback loop。\n\n这些限制是当前事实，不应在调用方文档中描述成已经解决。\n\n## 13. 可能的演进\n\n以下是扩展方向，不是已承诺 API：\n\n1. 增加显式 model eval runner，比较 model/review protocol revision 并生成安全指标；\n2. 在有隐私审查的前提下，将 human override 转成匿名 regression case；\n3. 将当前 private versioned response schema 导出为 eval/tooling 可消费的 artifact；\n4. 增加组织级 policy steering，但继续与 untrusted action/context 分层；\n5. 当 one-shot reviewer 的误差有数据证据时，再评估 tiered review、ensemble 或专用模型；\n6. 只有积累足够高质量 label 后，才评估 fine-tuning；训练不是建立 eval corpus 的前置条件。\n\n任何演进都不能把 classifier 变成 capability authority，也不能允许 model error、malformed\nresponse 或 cancellation 产生授权。",
    "headings": [
      {
        "depth": 2,
        "id": "核心产品流程当前状态",
        "title": "核心产品流程（当前状态）"
      },
      {
        "depth": 2,
        "id": "1-crate-边界",
        "title": "1. Crate 边界"
      },
      {
        "depth": 2,
        "id": "2-文件与职责",
        "title": "2. 文件与职责"
      },
      {
        "depth": 2,
        "id": "3-公共接口",
        "title": "3. 公共接口"
      },
      {
        "depth": 2,
        "id": "4-内部接口地图",
        "title": "4. 内部接口地图"
      },
      {
        "depth": 3,
        "id": "41-内部调用图",
        "title": "4.1 内部调用图"
      },
      {
        "depth": 3,
        "id": "42-内部接口的同步修改关系",
        "title": "4.2 内部接口的同步修改关系"
      },
      {
        "depth": 2,
        "id": "5-classify-的准确执行顺序",
        "title": "5. classify() 的准确执行顺序"
      },
      {
        "depth": 3,
        "id": "51-assessment-身份",
        "title": "5.1 Assessment 身份"
      },
      {
        "depth": 2,
        "id": "6-模型请求",
        "title": "6. 模型请求"
      },
      {
        "depth": 2,
        "id": "7-响应契约与本地校验",
        "title": "7. 响应契约与本地校验"
      },
      {
        "depth": 2,
        "id": "8-错误与取消",
        "title": "8. 错误与取消"
      },
      {
        "depth": 2,
        "id": "9-host-适配器接入要求",
        "title": "9. Host 适配器接入要求"
      },
      {
        "depth": 2,
        "id": "10-测试与评估",
        "title": "10. 测试与评估"
      },
      {
        "depth": 2,
        "id": "11-常见修改路径",
        "title": "11. 常见修改路径"
      },
      {
        "depth": 3,
        "id": "修改-prompt-或审查策略表述",
        "title": "修改 prompt 或审查策略表述"
      },
      {
        "depth": 3,
        "id": "增加建议",
        "title": "增加建议"
      },
      {
        "depth": 3,
        "id": "增加证据类型",
        "title": "增加证据类型"
      },
      {
        "depth": 3,
        "id": "更换供应商或增加模型",
        "title": "更换供应商或增加模型"
      },
      {
        "depth": 2,
        "id": "12-当前限制",
        "title": "12. 当前限制"
      },
      {
        "depth": 2,
        "id": "13-可能的演进",
        "title": "13. 可能的演进"
      }
    ],
    "searchText": " 本 README 解释 crate 内部实现、维护约束和修改路径。跨 zeta-action-policy、Core、App Server、 Tool 与 sandbox 的系统设计见 docs/auto-review.md。 文档分层遵循 docs/documentation-guidelines.md。 zeta-auto-review 把一个已经由 host 完整解析的 ActionReviewRequest 交给独立 review model， 将严格 JSON 响应转换成绑定 action 与 policy revision 的 ClassifierAssessment。 它只产生 advisory recommendation。它不能创建 grant、执行 Tool、修改 sandbox policy，也不拥有 用户审批流程。 核心产品流程（当前状态） 本 crate 是下面流程中的 classifier，不是整条流程的 owner。跨 crate 的完整产品语义由 docs/auto-review.md canonical 定义；这里保留 最短摘要，因为这个调用位置直接决定 classifier 的输入和扩展方向。 ``text Agent action ├─ deterministic deny → Block Current ├─ 已有 exact grant → 直接执行 Current └─ 其他 action ├─ sandbox 不适用（如 Fetch / MCP / external）→ classifier Current └─ local process ├─ sandbox 无法满足 capability → classifier Current └─ sandbox 可以执行 → 先在 sandbox 中运行 Current ├─ 成功或普通命令失败 → 返回执行结果 Current └─ 确认是 sandbox denial ├─ safe to retry → classifier Current └─ possible side effects / unknown → 不重放 Current classifier └─ recommendation → ActionPolicyEngine 校验 ├─ Approve → exact grant ├─ ReviseAction → Agent 换更安全的 action ├─ AskUser → 请求用户批准 └─ Deny → Block ` 失败回流不能把所有 non-zero exit 都当成 sandbox denial。Tool executor 必须先区分 普通命令失败与 sandbox enforcement denial，并保留可信的失败证据。即使确认是 denial，命令也 可能在被拒绝前已经产生部分副作用；只有 Core 能证明没有副作用、或 action 可安全重放时，新的 exact grant 才能触发自动重试。部分副作用可能发生或结果未知时，只能返回 Agent、请求用户或 选择新的 action，不能自动重放原调用。 1. Crate 边界 本 crate 拥有： - versioned review protocol（system prompt、response schema 与 revision）； - provider-neutral ReviewModel port； - model input JSON 的序列化； - model input/response 大小与 strict wire JSON 校验； - 在 assessment 构造前调用 policy-owned capability constraint validation； - canonical assessment ID 的构造； - model、cancellation 和 invalid-response 错误分类； - auto-review seed eval corpus。 本 crate 不拥有： - ResolvedAction、capability、review context 和 recommendation domain type；它们属于 zeta-action-policy； - recommendation 是否足以产生执行授权；它由 ActionPolicyEngine 决定； - review model 的选择、credential 和 provider runtime；它们由 App Server 组合； - user approval、durable Tool lifecycle 和 rejection circuit breaker；它们属于 Core； - OS enforcement；它属于 Tool executor 与 zeta-sandboxing。 依赖方向必须保持： `text zeta-auto-review ├─ zeta-action-policy ├─ zeta-sandboxing（只读取 sandbox contract） └─ zeta-async-utils 禁止依赖：Core / App Server / provider runtime / credential store / approval UI ` 2. 文件与职责 `text zeta-rs/auto-review/ ├── prompt.md compile-time reviewer system policy ├── src/ │ ├── lib.rs 私有模块与显式 public export │ ├── classifier.rs classifier orchestration、budgets、error mapping、binding │ ├── protocol.rs versioned prompt/schema、wire input/output、canonical response │ ├── reviewmodel.rs provider-neutral model port 与 request/error contract │ └── classifiertests.rs classifier 单元测试 ├── evals/ │ ├── README.md corpus 格式、隐私与指标要求 │ └── cases.jsonl versioned synthetic gold cases ├── tests/ │ └── evalcontract.rs 离线 corpus + ActionPolicyEngine contract ├── BUILD.bazel └── Cargo.toml ` 不要把 provider adapter 或 Core orchestration 移入本 crate。需要增加内部实现时，保持 module private，并从 lib.rs 显式导出必要 API。 3. 公共接口 API 作用 实现者或调用者必须保证 --- --- --- ReviewModel provider-neutral completion port 观察 cancellation；按 request budget 收集 JSON；不提供 Tool、memory 或 mutation capability ReviewModelRequest 一次 review 的 exact prompt payload 与 response budget adapter 分别消费 system prompt、input JSON 和 response schema，并执行 maximumresponsebytes ReviewModelError adapter failure contract 区分 provider invocation failure 与 response oversize LlmActionClassifier<M 实现 zetaactionpolicy::ActionClassifier caller 只提供 review model；classifier 固定使用当前 ReviewProtocol AutoReviewError fail-closed 的错误分类 caller 只能映射为显式 failure policy，不能把错误当成批准 ActionReviewRequest、ClassifierAssessment、ClassifierRecommendation、RiskLevel 和 UserAuthorization 均来自 zeta-action-policy。这使 authorization authority 不依赖具体 LLM implementation。 4. 内部接口地图 下面列出承载本 crate 设计方向的 private interface。它不是所有 helper 的机械清单；这些 symbol 共同决定 model boundary、serialization、validation 和 assessment binding，修改时必须能说明 ownership 为什么仍然正确。 Symbol 可见性 当前职责 方向约束 --- --- --- --- CURRENTREVIEWPROTOCOL private constant 原子绑定 revision、system prompt 与 response schema 修改 prompt/schema 语义必须在同一 module bump revision MAXMODELINPUTBYTES private constant serialized request 的 64 KiB 总上限 Core 仍负责内容选择、截断与 secret removal MAXMODELRESPONSEBYTES private constant model response 的 16 KiB 上限 通过 request 交给 adapter 提前执行，classifier 再复检 LlmActionClassifier::modelrequest private method 将 policy request 组装为三个分离的 model payload 只序列化，不重新解析 action、选择 model 或授予 capability LlmActionClassifier::parserecommendation private method strict parse 后调用 policy-owned constraint validation model capability 必须在 assessment 前验证，engine 仍会复检 ModelInput private struct 定义发送给 model 的 input JSON shape 只借用 host-owned domain values，不拥有权限语义 ModelInput::from private conversion 映射 action、provenance、sandbox、revision、context 不读取 Thread、config、credential 或 provider state ModelSandboxCompatibility private enum 把 sandbox contract 转成稳定的 tagged JSON 只描述 host 结论，不让 model 修改 availability ModelRecommendation private enum strict deserialize target，拒绝 unknown fields 不能直接成为 execution decision 或 grant ModelCapability private struct 对 capability 嵌套字段执行 denyunknownfields 不直接反序列化 domain Capability 而丢弃未知字段 From<ModelRecommendation for ClassifierRecommendation private conversion impl 转换成 zeta-action-policy advisory domain type 转换后仍必须经过 host validation responsejsonbytes private function 序列化 validated canonical recommendation assessment ID 不 hash 未解析的 raw model text RESPONSESCHEMAJSON private constant 定义四种 recommendation wire shape 与 ModelRecommendation / ModelCapability lockstep 更新 4.1 内部调用图 `text ActionClassifier::classify ├─ cancellation.iscancelled ├─ LlmActionClassifier::modelrequest │ ├─ ModelInput::from │ ├─ serdejson::tostring ModelInput │ └─ input byte-limit check ├─ ReviewModel::complete │ └─ adapter response byte-limit enforcement ├─ cancellation.iscancelled ├─ classifier response byte-limit recheck ├─ serdejson::fromstr<ModelRecommendation ├─ From<ModelRecommendation for ClassifierRecommendation ├─ ClassifierRecommendation::validateagainst ├─ responsejsonbytes ├─ AssessmentId::fromresponse └─ ClassifierAssessment::new ` 这张图也用于识别实现漂移： - 如果 action resolution 出现在 ModelInput::from，host/policy ownership 已经漂移； - 如果 capability validation 只存在于 provider adapter，本地可信边界已经漂移； - 如果 raw response 在 parse/validation 前进入 assessment hash，audit identity 语义已经改变； - 如果 ModelRecommendation 直接映射为 execution authority，classifier 越过了 policy boundary； - 如果 model selection 或 mutable config read 进入 modelrequest，safe-point ownership 已经漂移。 4.2 内部接口的同步修改关系 修改 必须同步检查 --- --- ModelInput field ModelInput::from、trust/redaction owner、classifier tests、eval corpus ModelRecommendation variant/field RESPONSESCHEMAJSON、conversion impl、policy domain、Core handling、eval labels Capability JSON shape ModelCapability、RESPONSESCHEMAJSON、policy constraint validation、fixtures Prompt/schema policy CURRENTREVIEWPROTOCOL revision、injection cases、model eval Assessment binding responsejsonbytes、AssessmentId::fromresponse、audit consumer、stable-ID tests Request/response limit classifier constants、ReviewModelRequest、provider collection、large-payload tests 5. classify 的准确执行顺序 LlmActionClassifier::classify 当前是同步的一次性调用： `text 1. preflight cancellation check 2. ActionReviewRequest → ModelInput JSON 3. serialized input 64 KiB limit 4. current ReviewProtocol → ReviewModelRequest 5. ReviewModel::complete ... with 16 KiB response budget 6. model error/cancellation normalization 7. post-call cancellation + response byte-limit recheck 8. strict wire parse + policy-owned capability validation 9. canonical recommendation → AssessmentId 10. 返回 request-bound ClassifierAssessment ` 顺序具有安全含义： - 调用前已取消时，model 不应启动； - model 调用中或返回后发生取消时，结果被归类为 Cancelled 并丢弃； - input/response 上限分别是 64/16 KiB，使用 Rust String::len 的 UTF-8 byte length； - adapter 在拼接 response fragment 时执行预算，classifier 在 parse 前再次检查； - denyunknownfields 在 recommendation 与嵌套 capability 两层拒绝额外字段； - assessment 复制 host-owned action digest 与 policy revision，模型不能覆盖它们。 5.1 Assessment 身份 AssessmentId 是下列内容的 SHA-256： `text actiondigest NUL policyrevision NUL reviewprotocolrevision NUL canonicalrecommendationjson ` 这里使用解析后重新序列化的 canonical recommendation，而不是模型原始文本。因此仅改变 JSON 空格或字段顺序不会产生新的 ID；改变 recommendation 语义、review protocol revision、action 或 policy revision 会产生新 ID。 CURRENTREVIEWPROTOCOL 在同一 private value 中绑定 system prompt、response schema 与 revision。修改 prompt/schema 语义时必须在 protocol.rs 同步 bump revision；caller 不能再传入 一个与实际 prompt 不一致的 revision。 6. 模型请求 ReviewModelRequest 包含三个互相分离的 payload 和一个 response budget： 字段 内容 信任级别 --- --- --- systemprompt crate 内编译的 reviewer policy trusted classifier instruction inputjson action、provenance、sandbox、phase、policy revision、review context host metadata 与 untrusted content 的结构化混合 responseschemajson 四种 recommendation 的严格 JSON schema trusted output contract maximumresponsebytes adapter 收集输出时必须执行的字节上限 trusted classifier budget inputjson 的结构为： `text action provenance sandbox policyrevision phase ├─ initial └─ sandboxdenial ├─ reason └─ bounded output context ├─ userintent └─ evidence ├─ kind ├─ trust ├─ source └─ content ` 本 crate 保留 evidence 的 trust label，但不负责生成或截断 context。Core/Tool host 必须在构造 ActionReviewRequest 前完成内容长度限制、secret removal 和 trust classification。classifier 对最终 serialized input 另设 64 KiB 总上限；它只拒绝超限 request，不替代 host 的选择与脱敏。 ReviewModel 是安全边界而不只是 transport abstraction。trait 本身无法阻止恶意实现调用 Tool 或读取 credential；可信 host adapter 必须确保 review runtime 没有这些能力。 7. 响应契约与本地校验 模型只能返回以下四种 shape： Recommendation 必填字段 本 crate 的额外检查 --- --- --- approve capabilities, risk, userauthorization, reason capabilities 必须非空且与 action required capabilities 完全相等 reviseaction maximumcapabilities, reason maximum capabilities 必须是原 action capabilities 的子集，可以为空 askuser reason 不接受额外字段 deny reason 不接受额外字段 Capability 由精确的 kind + scope 组成。在 approve 中，模型不能通过扩大 scope、添加 capability kind 或遗漏原 capability 获得部分批准；reviseaction 则有意允许返回原集合的 子集。Wire 层使用 private ModelCapability，因此 capability object 内的未知字段也不会被 serde 静默忽略。 Response schema 提供给 adapter 以约束生成，但本地 serde parse 和 host validation 才是最终 可信边界。当前 App Server adapter 将 schema 放入 model instructions；即使 provider 不支持 原生 structured output，malformed response 仍会在本 crate 被拒绝。Capability exact/subset 规则由 zeta-action-policy::ClassifierRecommendation::validateagainst 定义；auto-review 在构造 assessment 前调用，ActionPolicyEngine 对任意 classifier implementation 的结果再次调用。 本 crate 不执行 risk/authorization 到 execution decision 的映射。例如 high risk + implicit authorization 即使被解析为 Approve recommendation，仍由 ActionPolicyEngine 转成 AskUser。系统级矩阵见 docs/auto-review.md。 8. 错误与取消 条件 AutoReviewError 是否产生 assessment --- --- --- model 调用前、调用中或返回后 cancellation Cancelled 否 request serialization failure InvalidRequest String 否 serialized input 超过 64 KiB RequestTooLarge bytes 否 ReviewModelError::Invocation Model String 否 response 超过 16 KiB ResponseTooLarge bytes 否 JSON 无效、字段缺失、unknown field、enum 无效 InvalidResponse String 否 approve capability 非 exact match InvalidResponse String 否 revise capability 不是 subset InvalidResponse String 否 ReviewModel::complete 返回 Result<String, ReviewModelError 。Port 只区分 invocation failure 与 response oversize，不把完整 provider taxonomy 泄漏进 classifier。App Server 应在 进入本 crate 前完成安全 redaction。错误最终如何成为 Block 或 AskUser 由 zeta-action-policy::ReviewFailurePolicy 决定。 Cancellation 只能保证本 crate 在调用前后检查。正在进行的网络请求能否及时停止，取决于 ReviewModel implementation 是否在 provider checkpoint 观察 token。 9. Host 适配器接入要求 当前 App Server 的 ProviderReviewModel 是参考 adapter。它从 frozen ResolvedConfig safe-point snapshot 创建 immutable model runtime，并： - 不向 request 注册 Tool； - 将 toolchoice 固定为 None； - 禁止 parallel Tool Call； - 使用 temperature 0.0； - 忽略 reasoning，只拼接 text； - 拒绝 refusal、Tool Call 和空 text； - 拼接每个 text fragment 前执行 maximumresponsebytes； - 在 provider invocation 前后检查 cancellation。 新的 adapter 必须保持相同安全语义，但不要求使用相同 provider API。最小实现形态： `rust impl ReviewModel for ProviderAdapter fn complete &self, request: &ReviewModelRequest, cancellation: &CancellationToken, - Result<String, ReviewModelError // Invoke one immutable, tool-less reviewer, enforcing maximumresponsebytes // while colle"
  },
  {
    "slug": "crates/bwrap",
    "sourcePath": "zeta-rs/bwrap/README.md",
    "title": "zeta-bwrap",
    "description": "本 crate 有两个彼此分离的 surface：",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 Bubblewrap argv construction 与 Cargo-built upstream wrapper 的实现契约；\n> shared sandbox policy 和平台选择见\n> [`docs/sandboxing.md`](../../docs/sandboxing.md)，上游 source provenance 见\n> [`third_party/bubblewrap`](../../third_party/bubblewrap/README.md)。\n\n本 crate 有两个彼此分离的 surface：\n\n| Surface | 所有权 | 不承担 |\n| --- | --- | --- |\n| library `BwrapCommandBuilder` | typed mount、namespace、cwd 与 inner argv construction | policy、进程启动、binary discovery |\n| binary `bwrap` | 把 Cargo argv 传给锁定 upstream C `bwrap_main` | Zeta policy、fallback、capability probe |\n\n`src/builder.rs` 只生成 `BwrapCommand`，从不经过 shell。`build.rs` 仅在 Linux target 且设置\n`ZETA_BWRAP_SOURCE_DIR` 时编译 `bubblewrap.c`、`bind-mount.c`、`network.c` 与 `utils.c`，并把\nupstream `main` 重命名为 `bwrap_main`。普通 workspace test 不设置该变量，因此不会隐式下载或\n编译 C；canonical package builder 先验证 source lock，再提供该目录。\n\n`bwrap` binary 也是 repository 中刻意收窄的 unsafe-code 例外：`src/main.rs` 仅负责把\nprocess arguments 转换后调用 upstream C entry point。用于构造 Bubblewrap argv 的 library\n仍遵守 workspace 的 `unsafe_code = \"forbid\"`。\n\n```text\nscripts/zeta_package/bubblewrap.py\n→ verify/extract locked upstream source\n→ ZETA_BWRAP_SOURCE_DIR\n→ build.rs\n→ static C library exposing bwrap_main\n→ src/main.rs\n→ zeta-resources/bwrap\n```\n\n新增 upstream source file、feature define 或 linker dependency 时，必须同步 source lock、\n`build.rs`、package tests、Bubblewrap notices 和 Linux release toolchain。把 policy resolution、\nexecutable selection 或 process execution加入本 crate 表示 ownership 漂移。\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-bwrap\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-bwrap --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/bwrap:bwrap-unit-tests\n```",
    "headings": [],
    "searchText": " 本 README 拥有 Bubblewrap argv construction 与 Cargo-built upstream wrapper 的实现契约； shared sandbox policy 和平台选择见 docs/sandboxing.md，上游 source provenance 见 thirdparty/bubblewrap。 本 crate 有两个彼此分离的 surface： Surface 所有权 不承担 --- --- --- library BwrapCommandBuilder typed mount、namespace、cwd 与 inner argv construction policy、进程启动、binary discovery binary bwrap 把 Cargo argv 传给锁定 upstream C bwrapmain Zeta policy、fallback、capability probe src/builder.rs 只生成 BwrapCommand，从不经过 shell。build.rs 仅在 Linux target 且设置 ZETABWRAPSOURCEDIR 时编译 bubblewrap.c、bind-mount.c、network.c 与 utils.c，并把 upstream main 重命名为 bwrapmain。普通 workspace test 不设置该变量，因此不会隐式下载或 编译 C；canonical package builder 先验证 source lock，再提供该目录。 bwrap binary 也是 repository 中刻意收窄的 unsafe-code 例外：src/main.rs 仅负责把 process arguments 转换后调用 upstream C entry point。用于构造 Bubblewrap argv 的 library 仍遵守 workspace 的 unsafecode = \"forbid\"。 ``text scripts/zetapackage/bubblewrap.py → verify/extract locked upstream source → ZETABWRAPSOURCEDIR → build.rs → static C library exposing bwrapmain → src/main.rs → zeta-resources/bwrap ` 新增 upstream source file、feature define 或 linker dependency 时，必须同步 source lock、 build.rs、package tests、Bubblewrap notices 和 Linux release toolchain。把 policy resolution、 executable selection 或 process execution加入本 crate 表示 ownership 漂移。 `bash cargo test --manifest-path Cargo.toml -p zeta-bwrap cargo clippy --manifest-path Cargo.toml \\ -p zeta-bwrap --all-targets --no-deps -- -D warnings bazel test //zeta-rs/bwrap:bwrap-unit-tests ``"
  },
  {
    "slug": "crates/code-index",
    "sourcePath": "zeta-rs/code-index/README.md",
    "title": "zeta-code-index",
    "description": "zeta-code-index 在一个已经授权的 WorkspaceRoot 内扫描 UTF-8 文件，在工作区侧按语法声明或 行边界切块，把可重建投影发布到本地 SQLite，并提供有界的 FTS5 词法检索。crate 不决定工作区 信任、不启动 watcher、不发送网络请求，也不拥有 App Server DTO。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有工作区代码索引的 crate 内部实现契约。跨 crate 的产品语义、隐私边界和演进决策由\n> [`docs/code-index.md`](../../docs/code-index.md) canonical 维护；App Server 的生命周期与 RPC\n> 适配见 [`zeta-rs/app-server/README.md`](../app-server/README.md)。显式云外发 grant 与 provider\n> lifecycle 由 [`zeta-code-index-cloud` README](../code-index-cloud/README.md) 拥有；本地 semantic\n> projection 见 [`zeta-code-index-semantic` README](../code-index-semantic/README.md)，多来源 candidate\n> 编排由 [`zeta-code-retrieval` README](../code-retrieval/README.md) 拥有。\n\n## 快速理解\n\n`zeta-code-index` 在一个已经授权的 `WorkspaceRoot` 内扫描 UTF-8 文件，在工作区侧按语法声明或\n行边界切块，把可重建投影发布到本地 SQLite，并提供有界的 FTS5 词法检索。crate 不决定工作区\n信任、不启动 watcher、不发送网络请求，也不拥有 App Server DTO。\n\n| 维护任务 | 应修改的位置 | 必须同时检查 |\n| --- | --- | --- |\n| 增加可结构化切块的语言 | `chunker::language_for_path`、`syntax_language` | `zeta-syntax` grammar、chunker version、fixture |\n| 修改 chunk identity 或边界 | `chunker` | `CHUNKER_VERSION`、持久化重建、stale materialization 测试 |\n| 修改 ignore 或扫描限制 | `scanner` | 增量 reconcile 是否仍等价于全量扫描 |\n| 修改 SQLite shape | `store::create_schema` | `SCHEMA_VERSION`、旧投影 reset、reopen 测试 |\n| 修改 watcher 策略 | host / App Server | `refresh_observed_paths` 只接收 hint，不把事件当事实 |\n\n## 所有权与依赖方向\n\n当前 crate 负责：\n\n- canonical root 内默认排除 hidden files、ignore-aware、无 symlink 的文件扫描；\n- UTF-8/binary/文件数/byte/chunk 数限制；\n- `zeta-syntax` declaration facts 优先、行边界 fallback 的本地切块；\n- 全文件 revision、chunk content hash 与带 chunker version 的稳定 key；\n- SQLite 原子 generation publication、FTS5 检索和 exact-file 更新；\n- 命中再次读取时的 root、revision、range 与 hash 校验；\n- canonical in-memory dirty document overlay、revision 单调性、同 revision 冲突拒绝与 save handoff；\n- 把 Workspace-owned chunk reference 投影为适合上层返回的 revision-bound excerpt reference。\n- 把符号等 verified source span 投影为 content-addressed excerpt reference。\n- 为受控外发 consumer 提供不含正文的 generation manifest，以及批量复核后的 source/chunk\n  materialization。\n\n当前 crate 不负责：\n\n- 工作区 trust、profile 路径、watcher thread 或 RPC lifecycle；\n- embedding/rerank 模型调用、本地向量 projection、云端同步、网络权限或数据外发策略；\n- Editor snapshot 采集/生命周期、LSP semantic facts、模型 Tool 和 UI result projection。\n\n依赖方向固定为 `zeta-code-index → zeta-workspace + zeta-syntax`。如果本 crate 开始依赖 App\nServer protocol、产品 host、HTTP/provider 或 Renderer，说明 backend-neutral ownership 已经漂移。\n\n## 文件与关键内部接口\n\n| 文件 / symbol | 可见性 | 职责 | 不能承担的职责 |\n| --- | --- | --- | --- |\n| `CodeIndex` | public | 组合 root、limits 与 `IndexStore`，暴露 rebuild/refresh/search/materialize | watcher、RPC、远端同步 |\n| `chunker::chunk_source` | private | syntax boundary + byte/line hard split，生成 stable identities | 文件遍历、持久化 |\n| `scanner::scan_workspace` | private | ignore-aware deterministic full scan 与资源计数 | 把 walk error 变成网络重试 |\n| `scanner::prepare_relative_file` | private | exact-file UTF-8/binary/size validation | 决定新文件是否被 ignore |\n| `IndexStore` | private | schema/root binding、transaction publication、FTS query | workspace authority |\n| `IndexStore::initialize` | private | root/schema/chunker compatibility gate | 静默复用不兼容投影 |\n| `CodeIndex::refresh_observed_paths` | public | 重新读取 hint；只对既有普通文件 exact update，其余 rebuild | 相信 watcher event 的类型或内容 |\n| `CodeIndex::materialize` | public | 当前源文件与旧 reference 的一致性证明 | 返回未经复核的 Agent context |\n| `CodeIndex::materialize_excerpt` | public | 复核 revision-bound excerpt 的 revision/range/hash | 允许云 provider 用它绕过 canonical chunk identity |\n| `CodeIndex::materialize_verified_excerpt` | public | 在当前磁盘或 overlay source 上复核任意有界 span，并生成 content-addressed excerpt | 让调用方绕过 Workspace source identity |\n| `CodeIndex::synchronize_overlay` / `close_overlay` | public | 接受 immutable editor snapshot、建立内存 chunks、协调 save handoff | 采集 Editor state 或写入持久 DB |\n| `CodeIndex::manifest` | public | 原子读取当前 generation 的 source/chunk references，不返回正文 | consent、network 或 provider routing |\n| `CodeIndex::materialize_sources` / `materialize_chunks` | public | 批量重读并复核 manifest references | 扩大 path scope 或跳过 revision gate |\n\n```mermaid\nflowchart LR\n    Host[\"Authorized host\"] --> Open[\"CodeIndex::open\"]\n    Open --> Scan[\"scanner::scan_workspace\"]\n    Scan --> Chunk[\"chunker::chunk_source\"]\n    Chunk --> Publish[\"IndexStore::replace_workspace\"]\n    Publish --> Search[\"IndexStore::search\"]\n    Search --> Verify[\"CodeIndex::materialize\"]\n```\n\n`CodeIndex::open` 只校验 limits、绑定 root identity 并打开投影，不扫描文件。host 必须先注册\nwatcher，再调用 `rebuild`；这样初始扫描期间的 mutation 仍会进入 watcher queue。Watcher event\n只是 invalidation hint：新文件、目录、ignore control file、容量已达上限的 generation 和不确定\n路径都触发完整 rebuild；只有数据库中已经存在的普通文件才允许 exact replacement。\n\n## 公共契约\n\n| API | 输入 | 保证 |\n| --- | --- | --- |\n| `CodeIndex::open` | `WorkspaceRoot`、`CodeIndexStorage`、`CodeIndexLimits` | persistent DB 只能绑定一个 `IndexRootId`；不隐式扫描 |\n| `rebuild` | 当前 root | deterministic scan 后在一个 transaction 中替换 generation |\n| `refresh_observed_paths` | host-observed paths | containment 后重读事实，返回 no-change/published/rebuilt |\n| `snapshot` | 无 | 返回 published generation 与 completeness counters |\n| `search` | `CodeIndexQuery` | 对 whitespace terms 做 literal-escaped FTS AND，最多返回 configured limit |\n| `materialize` | `ChunkReference` | revision/range/key/hash 任一不符即失败，不返回 stale content |\n| `materialize_excerpt` | `SourceExcerptReference` | revision/range/hash 任一不符即失败；这是通用投影 API，不授权云端定义不同 chunk boundary |\n| `materialize_verified_excerpt` | `IndexedSourceReference` + `ChunkSpan` | 先验证 current source/overlay，再产生包含 content hash 的 exact excerpt reference |\n| overlay synchronize/close | path、language、editor revision、text/hash | dirty path 完全替代同路径磁盘搜索与 materialization；save 只在磁盘 hash 对齐后 handoff |\n| `manifest` | 无 | 返回同一 published generation 的 source/chunk metadata，不复制 source content |\n| `materialize_sources` / `materialize_chunks` | manifest references | 当前 source revision 或 chunk identity 任一不符即整体失败 |\n\n默认上限是 50,000 个文件、单文件 4 MiB、总源文件 512 MiB、目标 chunk 8 KiB、hard chunk 12\nKiB、单文件 2,048 chunks、query 8 KiB、结果 100 条。所有 byte 上限按 UTF-8 bytes 计算。\n`truncated_file_count`、`file_limit_hit` 和 `source_bytes_limit_hit` 让不完整 generation 可观察。\n\n## 持久化与失败语义\n\n`IndexStore` 的 metadata 同时保存 root ID、schema version、chunker version 与 generation。root\n不匹配返回 `StorageRootMismatch`；schema 或 chunker 不匹配会 drop 并重建投影 schema，generation\n回到 0。全量 rebuild 和增量 publication 都使用 transaction，读者只看到旧 generation 或新\ngeneration，不看到半份索引。\n\nSQLite projection 是可删除缓存，不是 source of truth。当前 FTS 与 chunk table 都在本地保存原文\n片段；Unix database 固定为 `0600`。App Server 使用 profile 内按 root digest 分隔的数据库，并在\npersistent cache 打不开时降级到 memory projection。删除源文件会在 watcher reconcile\n后移除 projection，但 profile cache 的主动清理/retention UI 尚未实现。\n\n失败时：\n\n- I/O、SQLite 或 containment 错误返回 `CodeIndexError`，不发布半份 generation；\n- stale revision、超出复核 byte limit、非法 range 或 identity mismatch 在 `materialize` 处 fail closed；\n- query 为空或超过 byte limit 返回 `InvalidQuery`；\n- poisoned store mutex 表示进程内 invariant 已破坏，当前实现直接 panic，不伪装成可恢复 I/O 错误。\n\nOverlay 是进程内可丢弃 projection，不写 SQLite。相同 editor revision 只有在 language、text 与 content\nhash 都一致时才视为幂等；同 revision 不同内容会 fail closed。persistent search 命中 dirty path 会被\n抑制，`materialize`、`materialize_excerpt` 与 source materialization 都优先验证 overlay。磁盘\ngeneration 出现相同 content hash 后，overlay 才自动交回 persistent projection。\n\n## 验证与修改影响\n\n```bash\ncargo test -p zeta-code-index\ncargo clippy -p zeta-code-index --all-targets --no-deps\nbazel test //zeta-rs/code-index:code-index-unit-tests\n```\n\n测试在 sibling `code_index_tests.rs` 中覆盖 structural chunk、ignore/binary/limit、persistent reopen、\n并覆盖 exact refresh、stale reference、ignore policy reconcile、bounded query、dirty override、revision conflict、\nsave handoff 与 verified arbitrary excerpt。修改 schema/chunker/limits 时必须同时检查 App Server status\nDTO、schema fixtures 和 [`docs/code-index.md`](../../docs/code-index.md)。\n\n## 当前限制与扩展点\n\n- Current：单个 `WorkspaceRoot`、磁盘 generation + ephemeral dirty overlay、FTS5/overlay lexical 排序；\n  不是 semantic/vector search。\n- Current：JavaScript/JSX/JSON/JSONC/Rust/Shell/TypeScript/TSX 使用 `zeta-syntax` boundaries；其他\n  UTF-8 文件使用 line/byte fallback。\n- Current limitation：未实现 multi-root identity、主动 cache cleanup、可取消 full scan/progress；overlay\n  只做本地 lexical/chunk projection，不为每次编辑触发 embedding。\n- Extension point：`zeta-code-index-semantic` 消费 `manifest` 和复核后的 chunks 做本地 dense recall；\n  `zeta-code-index-cloud` 消费相同 authority 做显式远端 grant。本 crate 保持无模型、无网络、无\n  consent、无 provider dependency。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与依赖方向",
        "title": "所有权与依赖方向"
      },
      {
        "depth": 2,
        "id": "文件与关键内部接口",
        "title": "文件与关键内部接口"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "持久化与失败语义",
        "title": "持久化与失败语义"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 拥有工作区代码索引的 crate 内部实现契约。跨 crate 的产品语义、隐私边界和演进决策由 docs/code-index.md canonical 维护；App Server 的生命周期与 RPC 适配见 zeta-rs/app-server/README.md。显式云外发 grant 与 provider lifecycle 由 zeta-code-index-cloud README 拥有；本地 semantic projection 见 zeta-code-index-semantic README，多来源 candidate 编排由 zeta-code-retrieval README 拥有。 快速理解 zeta-code-index 在一个已经授权的 WorkspaceRoot 内扫描 UTF-8 文件，在工作区侧按语法声明或 行边界切块，把可重建投影发布到本地 SQLite，并提供有界的 FTS5 词法检索。crate 不决定工作区 信任、不启动 watcher、不发送网络请求，也不拥有 App Server DTO。 维护任务 应修改的位置 必须同时检查 --- --- --- 增加可结构化切块的语言 chunker::languageforpath、syntaxlanguage zeta-syntax grammar、chunker version、fixture 修改 chunk identity 或边界 chunker CHUNKERVERSION、持久化重建、stale materialization 测试 修改 ignore 或扫描限制 scanner 增量 reconcile 是否仍等价于全量扫描 修改 SQLite shape store::createschema SCHEMAVERSION、旧投影 reset、reopen 测试 修改 watcher 策略 host / App Server refreshobservedpaths 只接收 hint，不把事件当事实 所有权与依赖方向 当前 crate 负责： - canonical root 内默认排除 hidden files、ignore-aware、无 symlink 的文件扫描； - UTF-8/binary/文件数/byte/chunk 数限制； - zeta-syntax declaration facts 优先、行边界 fallback 的本地切块； - 全文件 revision、chunk content hash 与带 chunker version 的稳定 key； - SQLite 原子 generation publication、FTS5 检索和 exact-file 更新； - 命中再次读取时的 root、revision、range 与 hash 校验； - canonical in-memory dirty document overlay、revision 单调性、同 revision 冲突拒绝与 save handoff； - 把 Workspace-owned chunk reference 投影为适合上层返回的 revision-bound excerpt reference。 - 把符号等 verified source span 投影为 content-addressed excerpt reference。 - 为受控外发 consumer 提供不含正文的 generation manifest，以及批量复核后的 source/chunk materialization。 当前 crate 不负责： - 工作区 trust、profile 路径、watcher thread 或 RPC lifecycle； - embedding/rerank 模型调用、本地向量 projection、云端同步、网络权限或数据外发策略； - Editor snapshot 采集/生命周期、LSP semantic facts、模型 Tool 和 UI result projection。 依赖方向固定为 zeta-code-index → zeta-workspace + zeta-syntax。如果本 crate 开始依赖 App Server protocol、产品 host、HTTP/provider 或 Renderer，说明 backend-neutral ownership 已经漂移。 文件与关键内部接口 文件 / symbol 可见性 职责 不能承担的职责 --- --- --- --- CodeIndex public 组合 root、limits 与 IndexStore，暴露 rebuild/refresh/search/materialize watcher、RPC、远端同步 chunker::chunksource private syntax boundary + byte/line hard split，生成 stable identities 文件遍历、持久化 scanner::scanworkspace private ignore-aware deterministic full scan 与资源计数 把 walk error 变成网络重试 scanner::preparerelativefile private exact-file UTF-8/binary/size validation 决定新文件是否被 ignore IndexStore private schema/root binding、transaction publication、FTS query workspace authority IndexStore::initialize private root/schema/chunker compatibility gate 静默复用不兼容投影 CodeIndex::refreshobservedpaths public 重新读取 hint；只对既有普通文件 exact update，其余 rebuild 相信 watcher event 的类型或内容 CodeIndex::materialize public 当前源文件与旧 reference 的一致性证明 返回未经复核的 Agent context CodeIndex::materializeexcerpt public 复核 revision-bound excerpt 的 revision/range/hash 允许云 provider 用它绕过 canonical chunk identity CodeIndex::materializeverifiedexcerpt public 在当前磁盘或 overlay source 上复核任意有界 span，并生成 content-addressed excerpt 让调用方绕过 Workspace source identity CodeIndex::synchronizeoverlay / closeoverlay public 接受 immutable editor snapshot、建立内存 chunks、协调 save handoff 采集 Editor state 或写入持久 DB CodeIndex::manifest public 原子读取当前 generation 的 source/chunk references，不返回正文 consent、network 或 provider routing CodeIndex::materializesources / materializechunks public 批量重读并复核 manifest references 扩大 path scope 或跳过 revision gate ``mermaid flowchart LR Host \"Authorized host\" -- Open \"CodeIndex::open\" Open -- Scan \"scanner::scanworkspace\" Scan -- Chunk \"chunker::chunksource\" Chunk -- Publish \"IndexStore::replaceworkspace\" Publish -- Search \"IndexStore::search\" Search -- Verify \"CodeIndex::materialize\" ` CodeIndex::open 只校验 limits、绑定 root identity 并打开投影，不扫描文件。host 必须先注册 watcher，再调用 rebuild；这样初始扫描期间的 mutation 仍会进入 watcher queue。Watcher event 只是 invalidation hint：新文件、目录、ignore control file、容量已达上限的 generation 和不确定 路径都触发完整 rebuild；只有数据库中已经存在的普通文件才允许 exact replacement。 公共契约 API 输入 保证 --- --- --- CodeIndex::open WorkspaceRoot、CodeIndexStorage、CodeIndexLimits persistent DB 只能绑定一个 IndexRootId；不隐式扫描 rebuild 当前 root deterministic scan 后在一个 transaction 中替换 generation refreshobservedpaths host-observed paths containment 后重读事实，返回 no-change/published/rebuilt snapshot 无 返回 published generation 与 completeness counters search CodeIndexQuery 对 whitespace terms 做 literal-escaped FTS AND，最多返回 configured limit materialize ChunkReference revision/range/key/hash 任一不符即失败，不返回 stale content materializeexcerpt SourceExcerptReference revision/range/hash 任一不符即失败；这是通用投影 API，不授权云端定义不同 chunk boundary materializeverifiedexcerpt IndexedSourceReference + ChunkSpan 先验证 current source/overlay，再产生包含 content hash 的 exact excerpt reference overlay synchronize/close path、language、editor revision、text/hash dirty path 完全替代同路径磁盘搜索与 materialization；save 只在磁盘 hash 对齐后 handoff manifest 无 返回同一 published generation 的 source/chunk metadata，不复制 source content materializesources / materializechunks manifest references 当前 source revision 或 chunk identity 任一不符即整体失败 默认上限是 50,000 个文件、单文件 4 MiB、总源文件 512 MiB、目标 chunk 8 KiB、hard chunk 12 KiB、单文件 2,048 chunks、query 8 KiB、结果 100 条。所有 byte 上限按 UTF-8 bytes 计算。 truncatedfilecount、filelimithit 和 sourcebyteslimithit 让不完整 generation 可观察。 持久化与失败语义 IndexStore 的 metadata 同时保存 root ID、schema version、chunker version 与 generation。root 不匹配返回 StorageRootMismatch；schema 或 chunker 不匹配会 drop 并重建投影 schema，generation 回到 0。全量 rebuild 和增量 publication 都使用 transaction，读者只看到旧 generation 或新 generation，不看到半份索引。 SQLite projection 是可删除缓存，不是 source of truth。当前 FTS 与 chunk table 都在本地保存原文 片段；Unix database 固定为 0600。App Server 使用 profile 内按 root digest 分隔的数据库，并在 persistent cache 打不开时降级到 memory projection。删除源文件会在 watcher reconcile 后移除 projection，但 profile cache 的主动清理/retention UI 尚未实现。 失败时： - I/O、SQLite 或 containment 错误返回 CodeIndexError，不发布半份 generation； - stale revision、超出复核 byte limit、非法 range 或 identity mismatch 在 materialize 处 fail closed； - query 为空或超过 byte limit 返回 InvalidQuery； - poisoned store mutex 表示进程内 invariant 已破坏，当前实现直接 panic，不伪装成可恢复 I/O 错误。 Overlay 是进程内可丢弃 projection，不写 SQLite。相同 editor revision 只有在 language、text 与 content hash 都一致时才视为幂等；同 revision 不同内容会 fail closed。persistent search 命中 dirty path 会被 抑制，materialize、materializeexcerpt 与 source materialization 都优先验证 overlay。磁盘 generation 出现相同 content hash 后，overlay 才自动交回 persistent projection。 验证与修改影响 `bash cargo test -p zeta-code-index cargo clippy -p zeta-code-index --all-targets --no-deps bazel test //zeta-rs/code-index:code-index-unit-tests ` 测试在 sibling codeindextests.rs 中覆盖 structural chunk、ignore/binary/limit、persistent reopen、 并覆盖 exact refresh、stale reference、ignore policy reconcile、bounded query、dirty override、revision conflict、 save handoff 与 verified arbitrary excerpt。修改 schema/chunker/limits 时必须同时检查 App Server status DTO、schema fixtures 和 docs/code-index.md。 当前限制与扩展点 - Current：单个 WorkspaceRoot、磁盘 generation + ephemeral dirty overlay、FTS5/overlay lexical 排序； 不是 semantic/vector search。 - Current：JavaScript/JSX/JSON/JSONC/Rust/Shell/TypeScript/TSX 使用 zeta-syntax boundaries；其他 UTF-8 文件使用 line/byte fallback。 - Current limitation：未实现 multi-root identity、主动 cache cleanup、可取消 full scan/progress；overlay 只做本地 lexical/chunk projection，不为每次编辑触发 embedding。 - Extension point：zeta-code-index-semantic 消费 manifest 和复核后的 chunks 做本地 dense recall； zeta-code-index-cloud` 消费相同 authority 做显式远端 grant。本 crate 保持无模型、无网络、无 consent、无 provider dependency。"
  },
  {
    "slug": "crates/code-index-cloud",
    "sourcePath": "zeta-rs/code-index-cloud/README.md",
    "title": "zeta-code-index-cloud",
    "description": "zeta-code-index-cloud 不实现某一家云服务。它在本地 authoritative code index 与 host 注入的 provider adapter 之间建立显式、可持久化、可撤销的数据外发边界。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有云代码索引的 crate 内部 contract。两种部署选择、隐私边界和产品状态由\n> [`docs/code-index.md`](../../docs/code-index.md) canonical 维护；本地扫描、chunk identity 与\n> revision verification 见 [`zeta-code-index` README](../code-index/README.md)，候选融合与预算见\n> [`zeta-code-retrieval` README](../code-retrieval/README.md)。本地语义编排见\n> [`zeta-code-index-semantic` README](../code-index-semantic/README.md)；具体远端语义服务不属于本仓库。\n\n## 快速理解\n\n`zeta-code-index-cloud` 不实现某一家云服务。它在本地 authoritative code index 与 host 注入的\nprovider adapter 之间建立显式、可持久化、可撤销的数据外发边界。\n\n| 模式 | 外发单位 | 云端负责 | 当前可用性 |\n| --- | --- | --- | --- |\n| `LocalOnly` | 无 | 无 | ✅ 默认；不需要本 crate 发起 publication |\n| `Cloud` | Workspace 已按 revision 复核的 exact chunks | embedding、vector index/recall、rerank、排序/过滤/截断 | contract 已实现；缺 concrete provider |\n\nWorkspace 侧索引始终拥有扫描、ignore、读取、切块、root containment、source revision、chunk identity\n和容量边界。provider 只管理这些 chunks 的远端语义索引，不得读取完整 source 后重新切块。\n\n## 所有权与依赖方向\n\n当前 crate 负责：\n\n- root-bound `CloudCodeIndexGrant`，包括 provider/tenant/collection、path scope 和最大外发 bytes；\n- 无网络、无 consent mutation 的精确 `preview`；\n- provider capability 与幂等 grant deletion 保证的 preflight；\n- `Granted → Syncing → Ready/Stale/Failed` publication lifecycle 与 exact-generation query gate；\n- `Revoking` 先持久化、provider 删除成功后才回到 `LocalOnly` 的撤销 lifecycle；\n- 本地 generation 变化后的 stale projection；\n- root/schema-bound SQLite consent/deletion recovery state。\n\n当前 crate 不负责：\n\n- 文件扫描、ignore、chunking、revision verification 或本地 lexical retrieval；\n- provider credential、HTTP client、proxy/TLS、租户认证、远端 schema 或 retention 实现；\n- concrete 云端的 query 准备、embedding/vector recall/rerank 编排与候选排序策略；\n- Workspace trust、profile 路径、RPC DTO、UI consent copy 或自动同步调度；\n- 把云检索结果注入 Agent context。\n\n依赖方向是 `zeta-code-index-cloud → zeta-code-index`。App Server 组合 controller 与 provider\nregistry；concrete provider adapter 依赖网络层，但 `zeta-code-index` 不得反向依赖本 crate或网络。\n\n## 关键接口与调用关系\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `CloudCodeIndexController` | public | serialize preview/authorize/sync/query/revoke/status 并维护 durable phase | 开始拥有 fusion、background thread、Workspace watcher 或 UI state |\n| `CloudCodeIndexGrant` | public | 固定一次 consent 的 root/destination/selection/byte ceiling | 允许 sync 时静默扩大 scope 或 destination |\n| `CloudCodeIndexProvider` | public trait | provider publication、已排序 exact-generation query 与 grant-level idempotent deletion port | adapter 绕过 request 读取 Workspace 文件，或把模型分数交给客户端决定排序 |\n| `CloudCodeIndexProviderRegistry` | public | host composition 冻结 provider identity → adapter | runtime 从客户端输入动态加载任意实现 |\n| `CloudStateStore` | private | root/schema binding 与单份 durable state | 保存 credential、源码或 provider response body |\n| `preview_manifest` | private | 对当前 local generation 计算 unit/file/chunk/byte 数 | 估算规则与实际 materialization 分叉 |\n| `selected_chunks` | private | 应用 path scope 到 Workspace manifest 中的 publication reference | provider adapter 自行解释 path scope或重新切块 |\n\n```mermaid\nflowchart LR\n    UI[\"User selection\"] --> Preview[\"Controller::preview (local only)\"]\n    Preview --> Authorize[\"Controller::authorize\"]\n    Authorize --> Grant[\"Persist Granted\"]\n    Grant --> Sync[\"Persist Syncing\"]\n    Sync --> Verify[\"zeta-code-index materialize current revision\"]\n    Verify --> Provider[\"CloudCodeIndexProvider publish\"]\n    Provider --> Ready[\"Persist Ready + generations\"]\n    Ready --> Query[\"Provider query exact generation\"]\n    Query --> Recall[\"Cloud embed + vector recall + rerank + filter/truncate\"]\n    Recall --> Validate[\"Validate exact Workspace chunks / selection / generation\"]\n    Grant --> Revoke[\"Persist Revoking\"]\n    Ready --> Revoke\n    Revoke --> Delete[\"Provider idempotent delete_grant\"]\n    Delete --> Local[\"Clear grant → LocalOnly\"]\n```\n\n## Consent 契约\n\n客户端应先调用 `preview` 展示当前 generation 的 `file_count`、`chunk_count`、上传单元数和精确\nsource-content bytes；该数字不包含 provider serialization 或 transport metadata overhead。\n`preview` 不保存授权，也不调用 provider。确认后才构造 grant：\n\n- `id`：一次 durable consent identity；\n- `root_id`：必须与 controller 的 local index root 完全相同；\n- `destination`：固定 provider、tenant 与 collection；\n- `selection`：整个 index 或规范化的 workspace-relative path prefixes；\n- `max_egress_bytes`：不可为零，限制 source-content bytes，authorize 和每次 sync 都重新检查。\n\n同一 root 同时只能存在一个 grant。完全相同的 authorize 是幂等读取；任何不同 grant 都返回\n`ConsentConflict`，必须先 revoke，避免把“切换租户/范围”伪装成原授权续期。若 source\n变化使实际 publication 超过 byte ceiling，sync 返回 `EgressLimitExceeded`，不会自动放宽。\n\n## 供应商契约与删除策略\n\nprovider 在 grant 生效前必须声明 `IdempotentGrantDeletion`。实现者必须：\n\n- 把 grant ID 作为所有远端 object 的删除边界；\n- 对同一 `(grant ID, local generation)` 的 publication retry 做幂等 upsert/replace；\n- publication 只能消费 request 中的 `MaterializedChunk`，不得绕过 controller 读取 Workspace 文件，\n  也不得改变 chunk boundary 或 identity；\n- query 只读取 request 中的 grant 与 exact remote generation，完成 query 准备、embedding/vector\n  recall、可选 rerank、过滤和截断，并按最终相关性顺序返回 exact Workspace `ChunkReference`；\n- 可以把 embedding/rerank 模型调用委托给 `zeta-model-provider`，但不得把候选构造、排序或截断决策\n  下沉给 model adapter；\n- 对重复 `delete_grant` 返回成功，并删除该 grant 创建的 chunks、files、vectors 和 metadata；\n- 在 adapter 内绑定 credential、proxy/TLS、tenant isolation、request limit 与日志脱敏；\n- 不记录 publication content、credential 或 provider response body 到本地 durable state；\n- publication 部分成功后仍保证同一 grant 可以完整删除。\n\n`revoke` 在网络删除前保存 `Revoking`。只有 provider 确认删除成功后才清空 grant；失败时保留\n`Revoking`，进程重启后仍可用同一 controller 再次调用 `revoke`。Workspace trust 从 Trusted\n降为 Restricted 时，App Server 自动执行同一流程并立即移除 cloud controller；删除失败不会恢复\n外发能力，durable state 保持 pending deletion。\n\n## 持久化与失败语义\n\n`CloudStateStore` 的 metadata 保存 root ID、schema version 和序列化 lifecycle state。persistent\ndatabase 在 Unix 上拒绝非普通文件并固定为 `0600`；它不保存源码。root 不匹配返回\n`StorageRootMismatch`，schema 不兼容返回 `IncompatibleStorage`。\n\n- 进程在 `Syncing` 中断：重开后投影变为 `Failed`，已有 remote generation 时变为 `Stale`；\n- local generation 尚未首次发布：preview/authorize/sync 返回 not-ready，不把空 generation 发布为\n  ready；\n- provider publication 失败：无已知远端 generation 时 `Failed`，否则 `Stale`；\n- local generation 变化：`status` 动态返回 `Stale`，不把旧 remote generation 当作 current；\n- provider 返回空、过长或含控制字符的 generation：拒绝持久化为 ready；\n- query 时 remote generation、root、path scope、result bound 或 exact chunk identity 不合法：拒绝整个 provider result；\n- `Revoking` 中断或删除失败：保留 grant，允许幂等重试。\n\n## 验证与修改影响\n\n```bash\ncargo test -p zeta-code-index-cloud\ncargo clippy -p zeta-code-index-cloud --all-targets --no-deps\nbazel test //zeta-rs/code-index-cloud:code-index-cloud-unit-tests\n```\n\n测试位于 sibling `cloud_index_tests.rs`，覆盖 chunk-only payload/path scope、byte ceiling、\n删除失败持久化与重开重试、local generation stale。修改 grant、phase 或 provider guarantee 时必须\n同时检查 App Server RPC tests、generated schema/types 和 [`docs/code-index.md`](../../docs/code-index.md)。\n\n## 当前限制与扩展点\n\n- Current：domain/provider/App Server contract 已实现；默认 local composition 没有 concrete provider，\n  所以不会广告 `cloudCodeIndex` capability，也不会创建网络请求。\n- Current：sync 是显式同步 operation；尚无后台 debounce、progress、cancellation 或 retry scheduler。\n- Current：controller 会把选定 chunks 批量 materialize 到内存后调用 provider；超大 repository 应由\n  现有 local scan limits、grant byte ceiling 和未来的 streaming/batch contract 共同约束。\n- Current：本 crate 暴露 exact-generation provider query，要求云端返回 final relevance order 并校验返回边界；跨来源 candidate fusion、复核与预算由\n  `zeta-code-retrieval` 拥有。\n- Current：本仓库不实现或部署远端 embedding/vector database/rerank service；concrete provider 应在\n  独立服务或 adapter 项目中完成，再通过本 crate 的 typed port 注入。\n- Current limitation：没有 concrete remote search adapter、production vector store、remote retention receipt 或用户可见\n  deletion audit record。\n- Current limitation：controller 是单 root owner；Workspace switch 不隐式撤销 grant，inactive root\n  也没有全局后台 deletion scanner。App Server 会在该 root 下次受限 activation 时重试 pending\n  deletion；要求立即删除的产品应先显式 revoke。\n- Extension point：新增 provider 时先实现 credential/network policy 与远端 grant-level delete，再注入\n  registry；不得用一个通用 HTTP callback 绕过 typed chunk publication payload。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与依赖方向",
        "title": "所有权与依赖方向"
      },
      {
        "depth": 2,
        "id": "关键接口与调用关系",
        "title": "关键接口与调用关系"
      },
      {
        "depth": 2,
        "id": "consent-契约",
        "title": "Consent 契约"
      },
      {
        "depth": 2,
        "id": "供应商契约与删除策略",
        "title": "供应商契约与删除策略"
      },
      {
        "depth": 2,
        "id": "持久化与失败语义",
        "title": "持久化与失败语义"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 拥有云代码索引的 crate 内部 contract。两种部署选择、隐私边界和产品状态由 docs/code-index.md canonical 维护；本地扫描、chunk identity 与 revision verification 见 zeta-code-index README，候选融合与预算见 zeta-code-retrieval README。本地语义编排见 zeta-code-index-semantic README；具体远端语义服务不属于本仓库。 快速理解 zeta-code-index-cloud 不实现某一家云服务。它在本地 authoritative code index 与 host 注入的 provider adapter 之间建立显式、可持久化、可撤销的数据外发边界。 模式 外发单位 云端负责 当前可用性 --- --- --- --- LocalOnly 无 无 ✅ 默认；不需要本 crate 发起 publication Cloud Workspace 已按 revision 复核的 exact chunks embedding、vector index/recall、rerank、排序/过滤/截断 contract 已实现；缺 concrete provider Workspace 侧索引始终拥有扫描、ignore、读取、切块、root containment、source revision、chunk identity 和容量边界。provider 只管理这些 chunks 的远端语义索引，不得读取完整 source 后重新切块。 所有权与依赖方向 当前 crate 负责： - root-bound CloudCodeIndexGrant，包括 provider/tenant/collection、path scope 和最大外发 bytes； - 无网络、无 consent mutation 的精确 preview； - provider capability 与幂等 grant deletion 保证的 preflight； - Granted → Syncing → Ready/Stale/Failed publication lifecycle 与 exact-generation query gate； - Revoking 先持久化、provider 删除成功后才回到 LocalOnly 的撤销 lifecycle； - 本地 generation 变化后的 stale projection； - root/schema-bound SQLite consent/deletion recovery state。 当前 crate 不负责： - 文件扫描、ignore、chunking、revision verification 或本地 lexical retrieval； - provider credential、HTTP client、proxy/TLS、租户认证、远端 schema 或 retention 实现； - concrete 云端的 query 准备、embedding/vector recall/rerank 编排与候选排序策略； - Workspace trust、profile 路径、RPC DTO、UI consent copy 或自动同步调度； - 把云检索结果注入 Agent context。 依赖方向是 zeta-code-index-cloud → zeta-code-index。App Server 组合 controller 与 provider registry；concrete provider adapter 依赖网络层，但 zeta-code-index 不得反向依赖本 crate或网络。 关键接口与调用关系 Symbol 可见性 职责 漂移信号 --- --- --- --- CloudCodeIndexController public serialize preview/authorize/sync/query/revoke/status 并维护 durable phase 开始拥有 fusion、background thread、Workspace watcher 或 UI state CloudCodeIndexGrant public 固定一次 consent 的 root/destination/selection/byte ceiling 允许 sync 时静默扩大 scope 或 destination CloudCodeIndexProvider public trait provider publication、已排序 exact-generation query 与 grant-level idempotent deletion port adapter 绕过 request 读取 Workspace 文件，或把模型分数交给客户端决定排序 CloudCodeIndexProviderRegistry public host composition 冻结 provider identity → adapter runtime 从客户端输入动态加载任意实现 CloudStateStore private root/schema binding 与单份 durable state 保存 credential、源码或 provider response body previewmanifest private 对当前 local generation 计算 unit/file/chunk/byte 数 估算规则与实际 materialization 分叉 selectedchunks private 应用 path scope 到 Workspace manifest 中的 publication reference provider adapter 自行解释 path scope或重新切块 ``mermaid flowchart LR UI \"User selection\" -- Preview \"Controller::preview local only \" Preview -- Authorize \"Controller::authorize\" Authorize -- Grant \"Persist Granted\" Grant -- Sync \"Persist Syncing\" Sync -- Verify \"zeta-code-index materialize current revision\" Verify -- Provider \"CloudCodeIndexProvider publish\" Provider -- Ready \"Persist Ready + generations\" Ready -- Query \"Provider query exact generation\" Query -- Recall \"Cloud embed + vector recall + rerank + filter/truncate\" Recall -- Validate \"Validate exact Workspace chunks / selection / generation\" Grant -- Revoke \"Persist Revoking\" Ready -- Revoke Revoke -- Delete \"Provider idempotent deletegrant\" Delete -- Local \"Clear grant → LocalOnly\" ` Consent 契约 客户端应先调用 preview 展示当前 generation 的 filecount、chunkcount、上传单元数和精确 source-content bytes；该数字不包含 provider serialization 或 transport metadata overhead。 preview 不保存授权，也不调用 provider。确认后才构造 grant： - id：一次 durable consent identity； - rootid：必须与 controller 的 local index root 完全相同； - destination：固定 provider、tenant 与 collection； - selection：整个 index 或规范化的 workspace-relative path prefixes； - maxegressbytes：不可为零，限制 source-content bytes，authorize 和每次 sync 都重新检查。 同一 root 同时只能存在一个 grant。完全相同的 authorize 是幂等读取；任何不同 grant 都返回 ConsentConflict，必须先 revoke，避免把“切换租户/范围”伪装成原授权续期。若 source 变化使实际 publication 超过 byte ceiling，sync 返回 EgressLimitExceeded，不会自动放宽。 供应商契约与删除策略 provider 在 grant 生效前必须声明 IdempotentGrantDeletion。实现者必须： - 把 grant ID 作为所有远端 object 的删除边界； - 对同一 grant ID, local generation 的 publication retry 做幂等 upsert/replace； - publication 只能消费 request 中的 MaterializedChunk，不得绕过 controller 读取 Workspace 文件， 也不得改变 chunk boundary 或 identity； - query 只读取 request 中的 grant 与 exact remote generation，完成 query 准备、embedding/vector recall、可选 rerank、过滤和截断，并按最终相关性顺序返回 exact Workspace ChunkReference； - 可以把 embedding/rerank 模型调用委托给 zeta-model-provider，但不得把候选构造、排序或截断决策 下沉给 model adapter； - 对重复 deletegrant 返回成功，并删除该 grant 创建的 chunks、files、vectors 和 metadata； - 在 adapter 内绑定 credential、proxy/TLS、tenant isolation、request limit 与日志脱敏； - 不记录 publication content、credential 或 provider response body 到本地 durable state； - publication 部分成功后仍保证同一 grant 可以完整删除。 revoke 在网络删除前保存 Revoking。只有 provider 确认删除成功后才清空 grant；失败时保留 Revoking，进程重启后仍可用同一 controller 再次调用 revoke。Workspace trust 从 Trusted 降为 Restricted 时，App Server 自动执行同一流程并立即移除 cloud controller；删除失败不会恢复 外发能力，durable state 保持 pending deletion。 持久化与失败语义 CloudStateStore 的 metadata 保存 root ID、schema version 和序列化 lifecycle state。persistent database 在 Unix 上拒绝非普通文件并固定为 0600；它不保存源码。root 不匹配返回 StorageRootMismatch，schema 不兼容返回 IncompatibleStorage。 - 进程在 Syncing 中断：重开后投影变为 Failed，已有 remote generation 时变为 Stale； - local generation 尚未首次发布：preview/authorize/sync 返回 not-ready，不把空 generation 发布为 ready； - provider publication 失败：无已知远端 generation 时 Failed，否则 Stale； - local generation 变化：status 动态返回 Stale，不把旧 remote generation 当作 current； - provider 返回空、过长或含控制字符的 generation：拒绝持久化为 ready； - query 时 remote generation、root、path scope、result bound 或 exact chunk identity 不合法：拒绝整个 provider result； - Revoking 中断或删除失败：保留 grant，允许幂等重试。 验证与修改影响 `bash cargo test -p zeta-code-index-cloud cargo clippy -p zeta-code-index-cloud --all-targets --no-deps bazel test //zeta-rs/code-index-cloud:code-index-cloud-unit-tests ` 测试位于 sibling cloudindextests.rs，覆盖 chunk-only payload/path scope、byte ceiling、 删除失败持久化与重开重试、local generation stale。修改 grant、phase 或 provider guarantee 时必须 同时检查 App Server RPC tests、generated schema/types 和 docs/code-index.md。 当前限制与扩展点 - Current：domain/provider/App Server contract 已实现；默认 local composition 没有 concrete provider， 所以不会广告 cloudCodeIndex capability，也不会创建网络请求。 - Current：sync 是显式同步 operation；尚无后台 debounce、progress、cancellation 或 retry scheduler。 - Current：controller 会把选定 chunks 批量 materialize 到内存后调用 provider；超大 repository 应由 现有 local scan limits、grant byte ceiling 和未来的 streaming/batch contract 共同约束。 - Current：本 crate 暴露 exact-generation provider query，要求云端返回 final relevance order 并校验返回边界；跨来源 candidate fusion、复核与预算由 zeta-code-retrieval` 拥有。 - Current：本仓库不实现或部署远端 embedding/vector database/rerank service；concrete provider 应在 独立服务或 adapter 项目中完成，再通过本 crate 的 typed port 注入。 - Current limitation：没有 concrete remote search adapter、production vector store、remote retention receipt 或用户可见 deletion audit record。 - Current limitation：controller 是单 root owner；Workspace switch 不隐式撤销 grant，inactive root 也没有全局后台 deletion scanner。App Server 会在该 root 下次受限 activation 时重试 pending deletion；要求立即删除的产品应先显式 revoke。 - Extension point：新增 provider 时先实现 credential/network policy 与远端 grant-level delete，再注入 registry；不得用一个通用 HTTP callback 绕过 typed chunk publication payload。"
  },
  {
    "slug": "crates/code-index-semantic",
    "sourcePath": "zeta-rs/code-index-semantic/README.md",
    "title": "zeta-code-index-semantic",
    "description": "zeta-code-index-semantic 是嵌入 App Server 进程的本地语义管线，不是可部署的云服务。它读取 zeta-code-index 已发布并复核的当前 chunk，委托 zeta-model-provider 计算 embedding/rerank， 把 embedding 持久化到本地 SQLite，并在本地执行向量召回、分数解释和最终排序。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有本地语义代码索引的实现契约。跨来源召回见\n> [`zeta-code-retrieval`](../code-retrieval/README.md)，本地文件事实与词法索引见\n> [`zeta-code-index`](../code-index/README.md)，产品部署与隐私边界见\n> [`docs/code-index.md`](../../docs/code-index.md)。\n\n## 快速理解\n\n`zeta-code-index-semantic` 是嵌入 App Server 进程的本地语义管线，不是可部署的云服务。它读取\n`zeta-code-index` 已发布并复核的当前 chunk，委托 `zeta-model-provider` 计算 embedding/rerank，\n把 embedding 持久化到本地 SQLite，并在本地执行向量召回、分数解释和最终排序。\n\n| 场景 | 行为 | 数据位置 |\n| --- | --- | --- |\n| 首次同步 | 当前 manifest 的 exact chunks 分批调用 embedding | chunk 会发给所选模型；向量留在本地 |\n| 热查询 | 查询 embedding 后在本地做 cosine recall | 本地 SQLite/内存 |\n| 配置 rerank | 候选文本调用 rerank，crate 解释分数并排序 | 候选可能外发给模型 |\n| lexical generation 变化 | 按稳定 chunk identity 复用未变化向量，只计算新增/变化块，再发布新 generation | 不使用旧 generation 冒充当前索引 |\n| 未配置语义模型 | App Server 不构造本服务 | canonical retrieval 继续使用 FTS |\n\n## 所有权与调用关系\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `CodeIndexSemanticService` | public | 当前 generation 同步、query embedding、召回、可选 rerank 与最终顺序 | 接收任意远程 collection 或拥有网络 endpoint |\n| `CodeIndexVectorStore` | public trait | root/model/exact-generation replace、search、delete | 合并 generation 或改变 Workspace chunk identity |\n| `SqliteCodeIndexVectorStore` | public | 本地持久化 embedding；小集合精确扫描，大集合 SimHash ANN 候选 + 精确 cosine | 保存 credential、远端任务或租户数据 |\n| `InMemoryCodeIndexVectorStore` | public | 测试与临时 composition | 被描述为持久化 production store |\n| `CodeIndexEmbeddingModelId` | public | 绑定持久化向量与实际模型/维度语义 | 模型变化后静默复用旧向量 |\n\n```mermaid\nflowchart LR\n    Sync[\"CodeIndexSemanticService::sync\"] --> Manifest[\"CodeIndex::manifest\"]\n    Manifest --> Verify[\"CodeIndex::materialize_chunks\"]\n    Verify --> Reuse[\"CodeIndexVectorStore::reusable_embeddings\"]\n    Reuse --> Embed[\"EmbeddingInvoker::embed missing chunks\"]\n    Embed --> Replace[\"CodeIndexVectorStore::replace_generation\"]\n    Query[\"CodeIndexSemanticService::query\"] --> QueryEmbed[\"EmbeddingInvoker::embed(query)\"]\n    QueryEmbed --> Search[\"CodeIndexVectorStore::search\"]\n    Search --> Rerank[\"optional RerankInvoker::rerank\"]\n    Rerank --> Result[\"exact ChunkReference order\"]\n```\n\n## 持久化与失败语义\n\n`SqliteCodeIndexVectorStore` 保存路径、revision、chunk identity、range、content 与 little-endian `f32`\nembedding。同步先按 root、模型 ID、相对路径、language 与稳定 chunk key 读取可复用向量，只把缺失\nchunks 交给 embedding 模型；随后在 transaction 内完整发布新 generation。metadata 同时绑定 schema\nversion、root ID、lexical generation 和 embedding model ID。任一查询绑定不一致都显式拒绝。\n\n少于 2,048 chunks 时执行确定性 cosine 全量扫描。更大的 projection 使用 SQLite 中可重建的\n`simhash64-v1` signature 选出最多 900 个候选，再按原向量做精确 cosine 排序。ANN metadata 缺失或\n不匹配时自动退回全量扫描。两条路径保持同一 exact-generation store contract，并继续让 Workspace\n`ChunkReference` 成为唯一候选身份。\n\n持久化文件在 Unix 上要求普通文件并固定为 `0600`。该 projection 可从本地 lexical manifest 与模型\n重建，因此损坏或 schema 不兼容不能提升为源码 authority。\n\n## 验证与当前限制\n\n```bash\ncargo test -p zeta-code-index-semantic\ncargo clippy -p zeta-code-index-semantic --all-targets --no-deps\nbazel test //zeta-rs/code-index-semantic:code-index-semantic-unit-tests\n```\n\n- Current：内存与 SQLite exact-generation store、chunk 级向量复用、批量 embedding、本地 cosine\n  recall、可选 rerank；App Server watcher 在 lexical generation 更新后同步该 projection。\n- Current：`zeta-model-provider` 已提供 OpenAI-compatible embedding/rerank 与 OpenAI/Ollama embedding\n  runtime；App Server 只有在 durable model selection 与 exact Workspace consent 都有效时才构造本服务。\n- Current：App Server semantic job 串行调度同步；支持阶段/计数进度、协作取消、transient retry、逐批\n  embedding cache 和 privacy-safe metrics sink。metrics 不包含源码、路径、query、endpoint 或 secret。\n- Current limitation：SQLite ANN 是 rebuildable SimHash projection，不是 HNSW；仍需用真实仓库持续\n  量化 recall/latency，并保留 exact-generation authority、current-source verification 和全量 fallback。\n- Integration obligation：如果 invoker 会把文本发送到远程模型，产品 host 必须先取得独立的源码外发\n  同意；普通模型配置或 Workspace 文件权限不能被解释成“允许后台发送整个索引”。App Server 的 durable\n  grant 精确绑定 Workspace、模型和 provider config，任一变化都会卸载该 runtime。\n- Non-goal：本 crate 不提供 HTTP/gRPC endpoint、PostgreSQL、租户认证或任何云部署实现。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与调用关系",
        "title": "所有权与调用关系"
      },
      {
        "depth": 2,
        "id": "持久化与失败语义",
        "title": "持久化与失败语义"
      },
      {
        "depth": 2,
        "id": "验证与当前限制",
        "title": "验证与当前限制"
      }
    ],
    "searchText": " 本 README 拥有本地语义代码索引的实现契约。跨来源召回见 zeta-code-retrieval，本地文件事实与词法索引见 zeta-code-index，产品部署与隐私边界见 docs/code-index.md。 快速理解 zeta-code-index-semantic 是嵌入 App Server 进程的本地语义管线，不是可部署的云服务。它读取 zeta-code-index 已发布并复核的当前 chunk，委托 zeta-model-provider 计算 embedding/rerank， 把 embedding 持久化到本地 SQLite，并在本地执行向量召回、分数解释和最终排序。 场景 行为 数据位置 --- --- --- 首次同步 当前 manifest 的 exact chunks 分批调用 embedding chunk 会发给所选模型；向量留在本地 热查询 查询 embedding 后在本地做 cosine recall 本地 SQLite/内存 配置 rerank 候选文本调用 rerank，crate 解释分数并排序 候选可能外发给模型 lexical generation 变化 按稳定 chunk identity 复用未变化向量，只计算新增/变化块，再发布新 generation 不使用旧 generation 冒充当前索引 未配置语义模型 App Server 不构造本服务 canonical retrieval 继续使用 FTS 所有权与调用关系 Symbol 可见性 职责 漂移信号 --- --- --- --- CodeIndexSemanticService public 当前 generation 同步、query embedding、召回、可选 rerank 与最终顺序 接收任意远程 collection 或拥有网络 endpoint CodeIndexVectorStore public trait root/model/exact-generation replace、search、delete 合并 generation 或改变 Workspace chunk identity SqliteCodeIndexVectorStore public 本地持久化 embedding；小集合精确扫描，大集合 SimHash ANN 候选 + 精确 cosine 保存 credential、远端任务或租户数据 InMemoryCodeIndexVectorStore public 测试与临时 composition 被描述为持久化 production store CodeIndexEmbeddingModelId public 绑定持久化向量与实际模型/维度语义 模型变化后静默复用旧向量 ``mermaid flowchart LR Sync \"CodeIndexSemanticService::sync\" -- Manifest \"CodeIndex::manifest\" Manifest -- Verify \"CodeIndex::materializechunks\" Verify -- Reuse \"CodeIndexVectorStore::reusableembeddings\" Reuse -- Embed \"EmbeddingInvoker::embed missing chunks\" Embed -- Replace \"CodeIndexVectorStore::replacegeneration\" Query \"CodeIndexSemanticService::query\" -- QueryEmbed \"EmbeddingInvoker::embed query \" QueryEmbed -- Search \"CodeIndexVectorStore::search\" Search -- Rerank \"optional RerankInvoker::rerank\" Rerank -- Result \"exact ChunkReference order\" ` 持久化与失败语义 SqliteCodeIndexVectorStore 保存路径、revision、chunk identity、range、content 与 little-endian f32 embedding。同步先按 root、模型 ID、相对路径、language 与稳定 chunk key 读取可复用向量，只把缺失 chunks 交给 embedding 模型；随后在 transaction 内完整发布新 generation。metadata 同时绑定 schema version、root ID、lexical generation 和 embedding model ID。任一查询绑定不一致都显式拒绝。 少于 2,048 chunks 时执行确定性 cosine 全量扫描。更大的 projection 使用 SQLite 中可重建的 simhash64-v1 signature 选出最多 900 个候选，再按原向量做精确 cosine 排序。ANN metadata 缺失或 不匹配时自动退回全量扫描。两条路径保持同一 exact-generation store contract，并继续让 Workspace ChunkReference 成为唯一候选身份。 持久化文件在 Unix 上要求普通文件并固定为 0600。该 projection 可从本地 lexical manifest 与模型 重建，因此损坏或 schema 不兼容不能提升为源码 authority。 验证与当前限制 `bash cargo test -p zeta-code-index-semantic cargo clippy -p zeta-code-index-semantic --all-targets --no-deps bazel test //zeta-rs/code-index-semantic:code-index-semantic-unit-tests ` - Current：内存与 SQLite exact-generation store、chunk 级向量复用、批量 embedding、本地 cosine recall、可选 rerank；App Server watcher 在 lexical generation 更新后同步该 projection。 - Current：zeta-model-provider` 已提供 OpenAI-compatible embedding/rerank 与 OpenAI/Ollama embedding runtime；App Server 只有在 durable model selection 与 exact Workspace consent 都有效时才构造本服务。 - Current：App Server semantic job 串行调度同步；支持阶段/计数进度、协作取消、transient retry、逐批 embedding cache 和 privacy-safe metrics sink。metrics 不包含源码、路径、query、endpoint 或 secret。 - Current limitation：SQLite ANN 是 rebuildable SimHash projection，不是 HNSW；仍需用真实仓库持续 量化 recall/latency，并保留 exact-generation authority、current-source verification 和全量 fallback。 - Integration obligation：如果 invoker 会把文本发送到远程模型，产品 host 必须先取得独立的源码外发 同意；普通模型配置或 Workspace 文件权限不能被解释成“允许后台发送整个索引”。App Server 的 durable grant 精确绑定 Workspace、模型和 provider config，任一变化都会卸载该 runtime。 - Non-goal：本 crate 不提供 HTTP/gRPC endpoint、PostgreSQL、租户认证或任何云部署实现。"
  },
  {
    "slug": "crates/code-retrieval",
    "sourcePath": "zeta-rs/code-retrieval/README.md",
    "title": "zeta-code-retrieval",
    "description": "zeta-code-retrieval 把本地 symbol/FTS、本地 dense recall 和可选远端 provider 已排序候选融合成 一份可供 Agent 消费的代码片段。它不扫描、不生成 embedding、不拥有向量数据库，也不发起外发授权。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有代码召回编排的 crate 内部契约。跨 crate 产品语义由\n> [`docs/code-index.md`](../../docs/code-index.md) canonical 维护；本地词法索引、本地语义索引和远端\n> provider 生命周期分别见 [`zeta-code-index`](../code-index/README.md)、\n> [`zeta-code-index-semantic`](../code-index-semantic/README.md) 和\n> [`zeta-code-index-cloud`](../code-index-cloud/README.md)。\n\n## 快速理解\n\n`zeta-code-retrieval` 把本地 symbol/FTS、本地 dense recall 和可选远端 provider 已排序候选融合成\n一份可供 Agent 消费的代码片段。它不扫描、不生成 embedding、不拥有向量数据库，也不发起外发授权。\n\n| 部署 | 候选来源 | 可降级行为 |\n| --- | --- | --- |\n| lexical only | 本地 symbol + FTS | FTS 失败则调用失败；symbol 失败时保留 FTS 并报告 `LocalSymbolQueryFailed` |\n| local semantic | 本地 symbol + FTS + 本地向量召回 | 语义未同步或模型失败时保留 symbol/FTS，报告 `LocalSemanticQueryFailed` |\n| remote provider | 本地 symbol + FTS + 远端已排序候选 | 远端失败时保留本地结果，报告 `CloudQueryFailed` |\n| combined | 上述四者 | 三个可选来源独立降级 |\n\n## 所有权\n\n| 能力 | Owner |\n| --- | --- |\n| scan、ignore、chunk identity、FTS、源 revision 复核 | `zeta-code-index` |\n| syntax declaration projection、fuzzy symbol ranking | `zeta-symbol-index` |\n| query embedding、本地向量持久化/召回、可选 rerank 与来源内排序 | `zeta-code-index-semantic` |\n| 远端 grant、publication、query 与 deletion lifecycle | `zeta-code-index-cloud` + concrete provider |\n| embedding/rerank transport adapter | `zeta-model-provider` |\n| 多来源 RRF、identity 去重、降级、materialization 与 byte budget | `zeta-code-retrieval` |\n| trust、provider/model 注入、watcher、RPC 与 Agent 接入 | App Server |\n\n依赖方向固定为 `zeta-code-retrieval → {zeta-code-index, zeta-symbol-index,\nzeta-code-index-semantic, zeta-code-index-cloud}`。本 crate 不得反向依赖 App Server protocol、Core\nconversation state、产品 UI 或具体网络客户端。\n\n## 关键接口与调用关系\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `CodeRetrievalService` | public | 绑定相同 root 的必需 lexical 与可选 symbol/semantic/cloud 来源 | 开始拥有 watcher、credential 或模型调用 |\n| `CodeRetrievalQuery` | public | 校验非空、8 KiB query 与最多 100 条结果 | 接受无界输入 |\n| `CodeRetrievalBudget` | public | 固定单条与总 content byte 上限 | 拥有模型 token budget |\n| `CodeRetrievalResult` | public | 返回已复核 hits 与显式 degradations | 隐藏可选来源失败 |\n| `RetrievalDeployment` | private | 固定本次 service 可用来源 | 从请求参数动态扩大到远端 |\n| `add_ranked` | private | 按来源内 rank 计算 RRF 并合并相同 `SourceExcerptReference` | 比较不同来源不可比的原始分数 |\n\n```mermaid\nflowchart LR\n    Query[\"CodeRetrievalService::retrieve\"] --> FTS[\"CodeIndex::search\"]\n    Query --> Symbols[\"SymbolIndex::search\"]\n    Query --> Dense[\"CodeIndexSemanticService::query\"]\n    Query --> Remote[\"CloudCodeIndexController::query\"]\n    FTS --> Fuse[\"RRF by source rank\"]\n    Symbols --> MaterializeSymbol[\"materialize_verified_excerpt\"]\n    MaterializeSymbol --> Fuse\n    Dense --> Fuse\n    Remote --> Fuse\n    Fuse --> Dedup[\"SourceExcerptReference dedupe\"]\n    Dedup --> Verify[\"CodeIndex::materialize_excerpt\"]\n    Verify --> Budget[\"content byte budget\"]\n    Budget --> Result[\"hits + degradations\"]\n```\n\n`local_semantic`、`hybrid` 和 `local_semantic_with_cloud` 都验证 root identity。App Server 只在\nTrusted Workspace 且 host 注入语义模型时安装 local semantic；远端来源还必须有 durable grant。\n\n## 执行与失败语义\n\n一次 `retrieve` 先将请求数量乘以四作为各来源候选上限，并限制为 100。每个来源已经拥有自己的排序：\nFTS 由 `zeta-code-index` 排序，本地 dense/rerank 由 `zeta-code-index-semantic` 排序，远端列表由 provider\n排序。本 crate 只使用列表位置执行 rank constant 60 的 reciprocal rank fusion，不重新解释模型分数。\n\n本地 FTS 是必需来源；symbol、local semantic 与 cloud 是可降级来源。symbol declaration span 先由\n`CodeIndex::materialize_verified_excerpt` 对当前磁盘或 overlay 正文复核并生成 content-addressed\n`SourceExcerptReference`。融合后所有正文再由 `CodeIndex::materialize_excerpt` 重读；revision、range、\nline span 或 content hash 不匹配即丢弃。默认单条正文最多 32 KiB，总正文最多 128 KiB，均按 UTF-8\nbytes 计算。\n\n## 集成义务\n\n- App Server 必须先确认 `CodeIndexRuntime` 有可查询 generation。\n- symbol source 只能使用绑定同一个 root 的 `SymbolIndex`；它的 range 必须由 CodeIndex 转成 verified\n  excerpt，不能直接把 syntax row 当模型正文。\n- semantic projection 必须在 lexical generation 更新后同步；查询旧 generation 会显式失败并降级。\n- 远端 provider 只能返回已发布的 exact chunk references；正文仍以 Workspace 重读结果为准。\n- Agent context consumer 必须保留结果顺序与预算，不得重新加入已丢弃内容。\n- 本 crate 不负责取得 embedding/rerank 文本外发同意；安装 remote model invoker 前必须由产品 host 完成。\n\n## 验证、限制与扩展\n\n```bash\ncargo test -p zeta-code-retrieval\ncargo clippy -p zeta-code-retrieval --all-targets --no-deps\nbazel test //zeta-rs/code-retrieval:code-retrieval-unit-tests\n```\n\n测试位于 sibling `retrieval_tests.rs`，覆盖 lexical、exact symbol declaration、本地 semantic、远端顺序、\nRRF 去重、可选来源失败回退、stale candidate、dirty overlay 和 byte budget。修改\norigin/degradation、limit、RRF 或 excerpt identity 时，必须同步 App Server RPC tests、generated\nschema/types 与 [`docs/code-index.md`](../../docs/code-index.md)。\n\n- Current：同步串行查询来源；已在来源边界和 materialization 循环检查取消，并把取消传入本地 semantic\n  模型调用。cloud provider query 尚未接受 cancellation，retrieval 层也尚无独立 latency telemetry。\n- Current：CodeIndex overlay 是 dirty text authority；dirty path 的磁盘 lexical/semantic/cloud candidate\n  被过滤，本地 symbol/lexical 结果从 overlay materialize。\n- Extension point：可增加并发和可观测性，但 embedding/rerank 编排留在 semantic/provider owner，grant\n  留在 cloud owner，Agent Session state 留在上层 consumer。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "关键接口与调用关系",
        "title": "关键接口与调用关系"
      },
      {
        "depth": 2,
        "id": "执行与失败语义",
        "title": "执行与失败语义"
      },
      {
        "depth": 2,
        "id": "集成义务",
        "title": "集成义务"
      },
      {
        "depth": 2,
        "id": "验证限制与扩展",
        "title": "验证、限制与扩展"
      }
    ],
    "searchText": " 本 README 拥有代码召回编排的 crate 内部契约。跨 crate 产品语义由 docs/code-index.md canonical 维护；本地词法索引、本地语义索引和远端 provider 生命周期分别见 zeta-code-index、 zeta-code-index-semantic 和 zeta-code-index-cloud。 快速理解 zeta-code-retrieval 把本地 symbol/FTS、本地 dense recall 和可选远端 provider 已排序候选融合成 一份可供 Agent 消费的代码片段。它不扫描、不生成 embedding、不拥有向量数据库，也不发起外发授权。 部署 候选来源 可降级行为 --- --- --- lexical only 本地 symbol + FTS FTS 失败则调用失败；symbol 失败时保留 FTS 并报告 LocalSymbolQueryFailed local semantic 本地 symbol + FTS + 本地向量召回 语义未同步或模型失败时保留 symbol/FTS，报告 LocalSemanticQueryFailed remote provider 本地 symbol + FTS + 远端已排序候选 远端失败时保留本地结果，报告 CloudQueryFailed combined 上述四者 三个可选来源独立降级 所有权 能力 Owner --- --- scan、ignore、chunk identity、FTS、源 revision 复核 zeta-code-index syntax declaration projection、fuzzy symbol ranking zeta-symbol-index query embedding、本地向量持久化/召回、可选 rerank 与来源内排序 zeta-code-index-semantic 远端 grant、publication、query 与 deletion lifecycle zeta-code-index-cloud + concrete provider embedding/rerank transport adapter zeta-model-provider 多来源 RRF、identity 去重、降级、materialization 与 byte budget zeta-code-retrieval trust、provider/model 注入、watcher、RPC 与 Agent 接入 App Server 依赖方向固定为 zeta-code-retrieval → zeta-code-index, zeta-symbol-index, zeta-code-index-semantic, zeta-code-index-cloud 。本 crate 不得反向依赖 App Server protocol、Core conversation state、产品 UI 或具体网络客户端。 关键接口与调用关系 Symbol 可见性 职责 漂移信号 --- --- --- --- CodeRetrievalService public 绑定相同 root 的必需 lexical 与可选 symbol/semantic/cloud 来源 开始拥有 watcher、credential 或模型调用 CodeRetrievalQuery public 校验非空、8 KiB query 与最多 100 条结果 接受无界输入 CodeRetrievalBudget public 固定单条与总 content byte 上限 拥有模型 token budget CodeRetrievalResult public 返回已复核 hits 与显式 degradations 隐藏可选来源失败 RetrievalDeployment private 固定本次 service 可用来源 从请求参数动态扩大到远端 addranked private 按来源内 rank 计算 RRF 并合并相同 SourceExcerptReference 比较不同来源不可比的原始分数 ``mermaid flowchart LR Query \"CodeRetrievalService::retrieve\" -- FTS \"CodeIndex::search\" Query -- Symbols \"SymbolIndex::search\" Query -- Dense \"CodeIndexSemanticService::query\" Query -- Remote \"CloudCodeIndexController::query\" FTS -- Fuse \"RRF by source rank\" Symbols -- MaterializeSymbol \"materializeverifiedexcerpt\" MaterializeSymbol -- Fuse Dense -- Fuse Remote -- Fuse Fuse -- Dedup \"SourceExcerptReference dedupe\" Dedup -- Verify \"CodeIndex::materializeexcerpt\" Verify -- Budget \"content byte budget\" Budget -- Result \"hits + degradations\" ` localsemantic、hybrid 和 localsemanticwithcloud 都验证 root identity。App Server 只在 Trusted Workspace 且 host 注入语义模型时安装 local semantic；远端来源还必须有 durable grant。 执行与失败语义 一次 retrieve 先将请求数量乘以四作为各来源候选上限，并限制为 100。每个来源已经拥有自己的排序： FTS 由 zeta-code-index 排序，本地 dense/rerank 由 zeta-code-index-semantic 排序，远端列表由 provider 排序。本 crate 只使用列表位置执行 rank constant 60 的 reciprocal rank fusion，不重新解释模型分数。 本地 FTS 是必需来源；symbol、local semantic 与 cloud 是可降级来源。symbol declaration span 先由 CodeIndex::materializeverifiedexcerpt 对当前磁盘或 overlay 正文复核并生成 content-addressed SourceExcerptReference。融合后所有正文再由 CodeIndex::materializeexcerpt 重读；revision、range、 line span 或 content hash 不匹配即丢弃。默认单条正文最多 32 KiB，总正文最多 128 KiB，均按 UTF-8 bytes 计算。 集成义务 - App Server 必须先确认 CodeIndexRuntime 有可查询 generation。 - symbol source 只能使用绑定同一个 root 的 SymbolIndex；它的 range 必须由 CodeIndex 转成 verified excerpt，不能直接把 syntax row 当模型正文。 - semantic projection 必须在 lexical generation 更新后同步；查询旧 generation 会显式失败并降级。 - 远端 provider 只能返回已发布的 exact chunk references；正文仍以 Workspace 重读结果为准。 - Agent context consumer 必须保留结果顺序与预算，不得重新加入已丢弃内容。 - 本 crate 不负责取得 embedding/rerank 文本外发同意；安装 remote model invoker 前必须由产品 host 完成。 验证、限制与扩展 `bash cargo test -p zeta-code-retrieval cargo clippy -p zeta-code-retrieval --all-targets --no-deps bazel test //zeta-rs/code-retrieval:code-retrieval-unit-tests ` 测试位于 sibling retrievaltests.rs，覆盖 lexical、exact symbol declaration、本地 semantic、远端顺序、 RRF 去重、可选来源失败回退、stale candidate、dirty overlay 和 byte budget。修改 origin/degradation、limit、RRF 或 excerpt identity 时，必须同步 App Server RPC tests、generated schema/types 与 docs/code-index.md`。 - Current：同步串行查询来源；已在来源边界和 materialization 循环检查取消，并把取消传入本地 semantic 模型调用。cloud provider query 尚未接受 cancellation，retrieval 层也尚无独立 latency telemetry。 - Current：CodeIndex overlay 是 dirty text authority；dirty path 的磁盘 lexical/semantic/cloud candidate 被过滤，本地 symbol/lexical 结果从 overlay materialize。 - Extension point：可增加并发和可观测性，但 embedding/rerank 编排留在 semantic/provider owner，grant 留在 cloud owner，Agent Session state 留在上层 consumer。"
  },
  {
    "slug": "crates/codex-app-server",
    "sourcePath": "zeta-rs/codex-app-server/README.md",
    "title": "zeta-codex-app-server",
    "description": "This crate owns the local process and JSON-RPC adapter for upstream Codex App Server capabilities. Its current implementation covers managed ChatGPT account/login and complete Codex threa…",
    "group": "Crate 实现参考",
    "markdown": "This crate owns the local process and JSON-RPC adapter for upstream Codex App Server capabilities.\nIts current implementation covers managed ChatGPT account/login and complete Codex thread/Turn\ndelegation. OAuth, callbacks, device-code polling, credential persistence, token refresh, and the\nremote agent loop remain entirely inside upstream Codex.\n\n## Current execution path\n\n`CodexAppServerRuntime` lazily starts `codex app-server --listen stdio://` through\n`CodexAppServerProcess::start`. The shared runtime performs the `initialize`/`initialized` handshake,\nroutes bounded JSONL responses to exact request IDs, and sends account and Turn events to separate\nlisteners so event handling never deadlocks stdout.\n\n`CodexAppServerLoginDriver` maps Zeta-owned `LoginId` values to opaque upstream IDs. Early completion\nnotifications are retained until that mapping is installed. Successful completion reads only the\nupstream redacted account projection; upstream error text and all credential fields are discarded.\n\n`CodexModelCatalog` exposes the account-filtered upstream catalog under the stable\n`openai-chatgpt` provider identity. `CodexTurnDriver` exposes typed thread/Turn streaming, command and file-change approvals, structured\nuser input, interruption, and exact once-only server-request resolution. `CodexTurnExecutionBackend`\nimplements Core's `TurnExecutionBackend`: Core remains authoritative for durable Thread state,\ninteractions, cancellation, and terminal outcomes while Codex owns its remote agent loop. A remote\nthread binding, including its opaque Workspace authority scope, is persisted only after a successful\nTurn. The default App Server routes a Turn here only when its persisted model uses the exact\n`openai-chatgpt` provider; login state never changes execution implicitly.\n\n## Failure semantics\n\n- spawn, pipe, framing, timeout, and process-exit failures become stable unavailable errors;\n- missing methods and incompatible response shapes fail explicitly and never fall back to private\n  ChatGPT backend calls;\n- failed inference, approval, user-input, or login requests are not replayed after an unknown process\n  outcome;\n- connection generations prevent late server requests from being answered on a restarted process;\n- unsupported secret user input, local image input, or approval semantics fail closed;\n- stderr is not captured into RPC errors or telemetry.\n\nThe product App Server now selects this backend through the canonical model catalog and Session model\nselection. Permission approval, diff projection, images, secret user input, rate-limit observation,\nand richer completed-item projection remain explicit follow-up slices.",
    "headings": [
      {
        "depth": 2,
        "id": "current-execution-path",
        "title": "Current execution path"
      },
      {
        "depth": 2,
        "id": "failure-semantics",
        "title": "Failure semantics"
      }
    ],
    "searchText": "This crate owns the local process and JSON-RPC adapter for upstream Codex App Server capabilities. Its current implementation covers managed ChatGPT account/login and complete Codex thread/Turn delegation. OAuth, callbacks, device-code polling, credential persistence, token refresh, and the remote agent loop remain entirely inside upstream Codex. Current execution path CodexAppServerRuntime lazily starts codex app-server --listen stdio:// through CodexAppServerProcess::start. The shared runtime performs the initialize/initialized handshake, routes bounded JSONL responses to exact request IDs, and sends account and Turn events to separate listeners so event handling never deadlocks stdout. CodexAppServerLoginDriver maps Zeta-owned LoginId values to opaque upstream IDs. Early completion notifications are retained until that mapping is installed. Successful completion reads only the upstream redacted account projection; upstream error text and all credential fields are discarded. CodexModelCatalog exposes the account-filtered upstream catalog under the stable openai-chatgpt provider identity. CodexTurnDriver exposes typed thread/Turn streaming, command and file-change approvals, structured user input, interruption, and exact once-only server-request resolution. CodexTurnExecutionBackend implements Core's TurnExecutionBackend: Core remains authoritative for durable Thread state, interactions, cancellation, and terminal outcomes while Codex owns its remote agent loop. A remote thread binding, including its opaque Workspace authority scope, is persisted only after a successful Turn. The default App Server routes a Turn here only when its persisted model uses the exact openai-chatgpt provider; login state never changes execution implicitly. Failure semantics - spawn, pipe, framing, timeout, and process-exit failures become stable unavailable errors; - missing methods and incompatible response shapes fail explicitly and never fall back to private ChatGPT backend calls; - failed inference, approval, user-input, or login requests are not replayed after an unknown process outcome; - connection generations prevent late server requests from being answered on a restarted process; - unsupported secret user input, local image input, or approval semantics fail closed; - stderr is not captured into RPC errors or telemetry. The product App Server now selects this backend through the canonical model catalog and Session model selection. Permission approval, diff projection, images, secret user input, rate-limit observation, and richer completed-item projection remain explicit follow-up slices."
  },
  {
    "slug": "crates/collaboration",
    "sourcePath": "zeta-rs/collaboration/README.md",
    "title": "zeta-collaboration",
    "description": "zeta-collaboration owns the backend-neutral authority for ordered Document Engine structured-document rooms. It does not own HTTP, App Server JSON-RPC, Workbench state, sessions, users, o…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-collaboration` owns the backend-neutral authority for ordered Document Engine\nstructured-document rooms. It does not own HTTP, App Server JSON-RPC,\nWorkbench state, sessions, users, or document-schema semantics.\n\n`InMemoryDocumentCollaborationRooms` is composed by the App Server for clients\nin one process. `SqliteDocumentCollaborationRooms` is composed by the remote\nhost and persists room snapshots and ordered submissions across restarts. Both\nimplement the same `open`, `submit`, `replay`, and ephemeral presence operations.\nSQLite additionally owns remote-only room members, scoped credentials, audit\nevents, and cross-process presence leases.\n\n## Contract\n\n- `open` creates an unpredictable `document-` room ID or joins an existing room\n  only when its schema compatibility ID matches.\n- `submit` validates bounded JSON-object envelopes, assigns a monotonic\n  JavaScript-safe version, and returns `Accepted`, bounded rebase history, or\n  a canonical resync snapshot.\n- A repeated `(roomId, clientId, sequence)` is accepted only when its base\n  version, transaction, and resulting document are identical. The original\n  accepted update is returned; a changed reuse is rejected.\n- `replay` returns at most the newest 512 ordered updates. A client older than\n  that window receives the current snapshot instead.\n- `open_as`, `submit_as`, and `replay_as` require an active room member;\n  owners may list active members, create/revoke/rotate scoped member\n  credentials, and list audit events. Owners cannot revoke their own active\n  credential. Editors and owners submit; viewers only read and publish\n  presence.\n- `update_presence_as` / `replay_presence_as` are intentionally separate from\n  document history. Presence selections are bounded, structurally validated,\n  and expire after 60 seconds unless the client refreshes them.\n\nSQLite keeps submitted-operation rows after they leave the 512-update replay\nwindow so an old sequence can never be reused. Retention/compaction therefore\nneeds an explicit future protocol for expiring client identities; deleting\nthose rows without such a contract would break idempotency.\n\nThe crate validates the versioned JSON envelope, bounded generic node/mark/\nselection structure, and the known core Document Engine transaction kinds, not each\nprofile's full document schema.\n`RemoteDocumentCollaborationService` decodes every server value through the\nactive Document Engine schema before it can reach a `DocumentModel`. A Rust schema\nauthority belongs here only once the schema is independently represented in\nthe backend rather than copied from the browser.\n\n## Tests\n\n`cargo test -p zeta-collaboration` covers room creation, envelope rejection,\nordered replay, exact retries, SQLite recovery, memberships, credential\nrotation/revocation, audit order, and presence replay. App Server adapter\ncoverage is in `zeta-rs/app-server/src/server_tests.rs`.",
    "headings": [
      {
        "depth": 2,
        "id": "contract",
        "title": "Contract"
      },
      {
        "depth": 2,
        "id": "tests",
        "title": "Tests"
      }
    ],
    "searchText": "zeta-collaboration owns the backend-neutral authority for ordered Document Engine structured-document rooms. It does not own HTTP, App Server JSON-RPC, Workbench state, sessions, users, or document-schema semantics. InMemoryDocumentCollaborationRooms is composed by the App Server for clients in one process. SqliteDocumentCollaborationRooms is composed by the remote host and persists room snapshots and ordered submissions across restarts. Both implement the same open, submit, replay, and ephemeral presence operations. SQLite additionally owns remote-only room members, scoped credentials, audit events, and cross-process presence leases. Contract - open creates an unpredictable document- room ID or joins an existing room only when its schema compatibility ID matches. - submit validates bounded JSON-object envelopes, assigns a monotonic JavaScript-safe version, and returns Accepted, bounded rebase history, or a canonical resync snapshot. - A repeated roomId, clientId, sequence is accepted only when its base version, transaction, and resulting document are identical. The original accepted update is returned; a changed reuse is rejected. - replay returns at most the newest 512 ordered updates. A client older than that window receives the current snapshot instead. - openas, submitas, and replayas require an active room member; owners may list active members, create/revoke/rotate scoped member credentials, and list audit events. Owners cannot revoke their own active credential. Editors and owners submit; viewers only read and publish presence. - updatepresenceas / replaypresenceas are intentionally separate from document history. Presence selections are bounded, structurally validated, and expire after 60 seconds unless the client refreshes them. SQLite keeps submitted-operation rows after they leave the 512-update replay window so an old sequence can never be reused. Retention/compaction therefore needs an explicit future protocol for expiring client identities; deleting those rows without such a contract would break idempotency. The crate validates the versioned JSON envelope, bounded generic node/mark/ selection structure, and the known core Document Engine transaction kinds, not each profile's full document schema. RemoteDocumentCollaborationService decodes every server value through the active Document Engine schema before it can reach a DocumentModel. A Rust schema authority belongs here only once the schema is independently represented in the backend rather than copied from the browser. Tests cargo test -p zeta-collaboration covers room creation, envelope rejection, ordered replay, exact retries, SQLite recovery, memberships, credential rotation/revocation, audit order, and presence replay. App Server adapter coverage is in zeta-rs/app-server/src/servertests.rs."
  },
  {
    "slug": "crates/collaboration-server",
    "sourcePath": "zeta-rs/collaboration-server/README.md",
    "title": "zeta-collaboration-server",
    "description": "zeta-collaboration-server is the small remote product host for durable Document Engine collaboration. It owns bearer authentication, browser-origin policy, HTTP framing, long-poll deliver…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-collaboration-server` is the small remote product host for durable Document Engine\ncollaboration. It owns bearer authentication, browser-origin policy, HTTP\nframing, long-poll delivery, connection limits, and SQLite lifecycle. Ordered\nroom semantics remain in [`zeta-collaboration`](../collaboration/README.md).\nIt has no App Server, workspace, tool, terminal, or session authority.\n\n## Running one host\n\nRun the listener behind a TLS reverse proxy. The binary deliberately accepts\nonly an IP:port because it does not terminate TLS itself.\n\n```sh\nexport ZETA_COLLABORATION_BEARER_TOKEN='a-random-visible-ascii-token-at-least-32-characters'\nexport ZETA_COLLABORATION_ALLOWED_ORIGIN='https://desktop.example'\ncargo run -p zeta-collaboration-server -- 127.0.0.1:8421 /srv/zeta/collaboration.sqlite3\n```\n\n`ZETA_COLLABORATION_ALLOWED_ORIGIN` is a comma-separated allowlist. It must\ninclude the exact browser origin serving the Document Engine. The desktop toolbar asks for the\npublic HTTPS origin and bearer token, then creates or joins a room ID.\n\nThe host exposes:\n\n- `POST /v1/document-collaboration/rooms/open`\n- `POST /v1/document-collaboration/rooms/submit`\n- `GET /v1/document-collaboration/rooms/{roomId}/updates?afterVersion=N`\n- `POST /v1/document-collaboration/rooms/presence`\n- `GET /v1/document-collaboration/rooms/{roomId}/presence?afterGeneration=N`\n- `POST /v1/document-collaboration/rooms/invites`\n- `GET /v1/document-collaboration/rooms/{roomId}/members`\n- `POST /v1/document-collaboration/rooms/members/rotate-token`\n- `POST /v1/document-collaboration/rooms/members/revoke`\n- `GET /v1/document-collaboration/rooms/{roomId}/audit`\n\nEvery non-preflight request requires `Authorization: Bearer <token>`. The\ndeployment token bootstraps the persistent `server-admin` room owner; an owner\ncan issue a room-scoped bearer token for an owner, editor, or viewer. A scoped\ntoken cannot access another room. JSON responses use `Cache-Control: no-store`;\nbrowser requests receive CORS headers only for an allowed origin. `GET updates`\nand `GET presence` wait up to 25 seconds when no change is available, then\nreturn a canonical replay or current ephemeral selection set.\n\nMultiple processes can share one SQLite database. A local submission wakes\nsame-host polls immediately; other hosts recheck persisted state every 250 ms,\nso ordered writes and presence do not wait for the 25-second long-poll timeout.\n\n## Current security boundary\n\nThe deployment bearer is only a bootstrap credential, not a room share\ncapability. Room access is enforced through persistent identities and roles;\nissued tokens are stored only as SHA-256 hashes and are returned once when\ncreated or rotated. Owner-visible audit events record room creation, member\ninvitation/revocation, credential rotation, and accepted submissions. The host\nvalidates wire bounds, Document Engine envelopes, generic node/mark/selection structure,\nand known core transaction kinds; the active browser profile validates its own\nprofile-specific schema. Do not expose this endpoint directly to untrusted\nnetworks without TLS, a strong secret, and an origin allowlist.\n\n`cargo test -p zeta-collaboration-server` exercises CORS preflight, auth,\nroom-role enforcement, credential rotation, audit access, presence, ordered\nupdates, and external updates observed through a second host.",
    "headings": [
      {
        "depth": 2,
        "id": "running-one-host",
        "title": "Running one host"
      },
      {
        "depth": 2,
        "id": "current-security-boundary",
        "title": "Current security boundary"
      }
    ],
    "searchText": "zeta-collaboration-server is the small remote product host for durable Document Engine collaboration. It owns bearer authentication, browser-origin policy, HTTP framing, long-poll delivery, connection limits, and SQLite lifecycle. Ordered room semantics remain in zeta-collaboration. It has no App Server, workspace, tool, terminal, or session authority. Running one host Run the listener behind a TLS reverse proxy. The binary deliberately accepts only an IP:port because it does not terminate TLS itself. ``sh export ZETACOLLABORATIONBEARERTOKEN='a-random-visible-ascii-token-at-least-32-characters' export ZETACOLLABORATIONALLOWEDORIGIN='https://desktop.example' cargo run -p zeta-collaboration-server -- 127.0.0.1:8421 /srv/zeta/collaboration.sqlite3 ` ZETACOLLABORATIONALLOWEDORIGIN is a comma-separated allowlist. It must include the exact browser origin serving the Document Engine. The desktop toolbar asks for the public HTTPS origin and bearer token, then creates or joins a room ID. The host exposes: - POST /v1/document-collaboration/rooms/open - POST /v1/document-collaboration/rooms/submit - GET /v1/document-collaboration/rooms/ roomId /updates?afterVersion=N - POST /v1/document-collaboration/rooms/presence - GET /v1/document-collaboration/rooms/ roomId /presence?afterGeneration=N - POST /v1/document-collaboration/rooms/invites - GET /v1/document-collaboration/rooms/ roomId /members - POST /v1/document-collaboration/rooms/members/rotate-token - POST /v1/document-collaboration/rooms/members/revoke - GET /v1/document-collaboration/rooms/ roomId /audit Every non-preflight request requires Authorization: Bearer <token . The deployment token bootstraps the persistent server-admin room owner; an owner can issue a room-scoped bearer token for an owner, editor, or viewer. A scoped token cannot access another room. JSON responses use Cache-Control: no-store; browser requests receive CORS headers only for an allowed origin. GET updates and GET presence wait up to 25 seconds when no change is available, then return a canonical replay or current ephemeral selection set. Multiple processes can share one SQLite database. A local submission wakes same-host polls immediately; other hosts recheck persisted state every 250 ms, so ordered writes and presence do not wait for the 25-second long-poll timeout. Current security boundary The deployment bearer is only a bootstrap credential, not a room share capability. Room access is enforced through persistent identities and roles; issued tokens are stored only as SHA-256 hashes and are returned once when created or rotated. Owner-visible audit events record room creation, member invitation/revocation, credential rotation, and accepted submissions. The host validates wire bounds, Document Engine envelopes, generic node/mark/selection structure, and known core transaction kinds; the active browser profile validates its own profile-specific schema. Do not expose this endpoint directly to untrusted networks without TLS, a strong secret, and an origin allowlist. cargo test -p zeta-collaboration-server` exercises CORS preflight, auth, room-role enforcement, credential rotation, audit access, presence, ordered updates, and external updates observed through a second host."
  },
  {
    "slug": "crates/zeterm/commands",
    "sourcePath": "zeterm/commands/README.md",
    "title": "zeta-commands",
    "description": "zeta-commands owns the stable product command vocabulary and the runtime-free registration contract for the native zeterm host. Pointer activation, menu items, keyboard shortcuts, and fut…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-commands` owns the stable product command vocabulary and the runtime-free\nregistration contract for the native `zeterm` host. Pointer activation, menu\nitems, keyboard shortcuts, and future command-palette entries converge on a\n[`CommandRequest`](src/request.rs) before the host executes a state transition.\n\n## Ownership\n\n| Concern | Owner |\n| --- | --- |\n| Type-safe command identity | `ZetermCommandId` |\n| Complete product command catalog | `ZetermCommandId::ALL` |\n| Persisted command ID and shortcut label | `ZetermCommandId::id` / `ZetermCommandId::label` |\n| Currently user-bindable command set | `ZetermCommandId::BINDABLE` |\n| ID parsing and user-binding validation | `ZetermCommandId::from_id` / `ZetermCommandId::bindable_from_id` |\n| Cross-module execution request | `CommandRequest` |\n| Generic handler registration and lookup | `CommandRegistry<Context>` |\n| `ElementId` mapping | `zeterm` host adapter |\n| Keybinding context and platform defaults | `zeterm/src/keybindings.rs` |\n| Built-in handler registration and product state | `zeterm/src/command_dispatch.rs` |\n\nThis crate must remain independent of `NativeApp`, `zui`, `zeta-winit`,\nterminal state, workspace state, and the keybinding runtime. A command request\nis a stable product intent; it is not a domain state machine or a replacement\nfor typed Session, Editor, Terminal, or App Server APIs.\n\n## Execution path\n\n```text\npointer / menu / shortcut / command palette\n  → CommandRequest(ZetermCommandId)\n  → CommandRegistry<NativeApp>\n  → registered host handler\n  → product state owner\n  → invalidation / presentation rebuild\n```\n\n`CommandRegistry<Context>` owns only command-to-handler registration and lookup.\nThe registry is process-local to the host instance; it is not a global backend\nregistry. `zeterm` registers every built-in command during `NativeApp` startup,\nwhile each handler remains responsible for calling the domain state owner.\n`CommandRequest` currently carries only the command identity. A command that\nneeds parameters should add a typed request payload when that command is\nintroduced instead of passing UI element IDs or untyped service calls.\n\nThe string IDs are retained at the configuration boundary for existing\nkeybinding resources, including the current `workbench.action.*` compatibility\nnames. Internal callers use the enum so command consumers do not depend on\nspelling or on UI element identity. A future namespace migration must add an\nalias/compatibility path before changing persisted IDs.\n\n## Verification\n\n```bash\ncargo test -p zeta-commands\n```",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "execution-path",
        "title": "Execution path"
      },
      {
        "depth": 2,
        "id": "verification",
        "title": "Verification"
      }
    ],
    "searchText": "zeta-commands owns the stable product command vocabulary and the runtime-free registration contract for the native zeterm host. Pointer activation, menu items, keyboard shortcuts, and future command-palette entries converge on a CommandRequest before the host executes a state transition. Ownership Concern Owner --- --- Type-safe command identity ZetermCommandId Complete product command catalog ZetermCommandId::ALL Persisted command ID and shortcut label ZetermCommandId::id / ZetermCommandId::label Currently user-bindable command set ZetermCommandId::BINDABLE ID parsing and user-binding validation ZetermCommandId::fromid / ZetermCommandId::bindablefromid Cross-module execution request CommandRequest Generic handler registration and lookup CommandRegistry<Context ElementId mapping zeterm host adapter Keybinding context and platform defaults zeterm/src/keybindings.rs Built-in handler registration and product state zeterm/src/commanddispatch.rs This crate must remain independent of NativeApp, zui, zeta-winit, terminal state, workspace state, and the keybinding runtime. A command request is a stable product intent; it is not a domain state machine or a replacement for typed Session, Editor, Terminal, or App Server APIs. Execution path ``text pointer / menu / shortcut / command palette → CommandRequest ZetermCommandId → CommandRegistry<NativeApp → registered host handler → product state owner → invalidation / presentation rebuild ` CommandRegistry<Context owns only command-to-handler registration and lookup. The registry is process-local to the host instance; it is not a global backend registry. zeterm registers every built-in command during NativeApp startup, while each handler remains responsible for calling the domain state owner. CommandRequest currently carries only the command identity. A command that needs parameters should add a typed request payload when that command is introduced instead of passing UI element IDs or untyped service calls. The string IDs are retained at the configuration boundary for existing keybinding resources, including the current workbench.action. compatibility names. Internal callers use the enum so command consumers do not depend on spelling or on UI element identity. A future namespace migration must add an alias/compatibility path before changing persisted IDs. Verification `bash cargo test -p zeta-commands ``"
  },
  {
    "slug": "crates/zeterm/composer",
    "sourcePath": "zeterm/composer/README.md",
    "title": "zeta-composer",
    "description": "zeta-composer owns the product-level geometry contract for zeterm's command composer. It resolves the bottom-mounted composer panel, its fixed information/editor/toolbar rows, the optiona…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-composer` owns the product-level geometry contract for zeterm's command composer. It\nresolves the bottom-mounted composer panel, its fixed information/editor/toolbar rows, the\noptional interaction list, and fixed-height interaction-list measurement. The crate is a pure\nprojection layer: it receives logical bounds and item counts, then returns geometry or generic\n`zeta-ui` scroll commands.\n\n## Boundary\n\n| Capability | Owner | Status |\n| --- | --- | --- |\n| Composer panel and interaction-list geometry | `zeta-composer` | ✅ |\n| Composer text, history, mode and selected item state | `zeterm/src/agent_composer.rs` and related host modules | ✅ host-owned |\n| Code-editor document and text-input behavior | `zeterm/src/composer_editor.rs` | ✅ host-owned |\n| Interaction item projection and activation | `zeterm/src/composer_interaction.rs` | ✅ host-owned |\n| Scene painting, accessibility registration and product command mapping | `zeterm/src/composer_panel.rs` and host modules | ✅ host-owned |\n| Renderer, window and platform event APIs | lower framework/backend and host adapters | ❌ |\n\nThe dependency direction is:\n\n```text\nzeterm host → zeta-composer → zeta-ui → zui\n```\n\n`zeta-composer` must not import `NativeApp`, workspace/session state, `winit`, `wgpu`, renderer\nresources, or product icon catalogs. If a change needs those values, keep the adapter in\n`zeterm/src` and pass only the resulting preferred size, item count, or logical bounds into this\ncrate.\n\n## Execution path\n\n`ShellLayout` asks `ComposerPanelLayout::for_main` for panel geometry. The host then paints and\nregisters the returned regions. Composer selection and wheel routing use\n`interaction_list_bounds`, `interaction_content_size`, and\n`interaction_selection_scroll_command` while the host retains `ScrollState` and applies the\nresulting command.\n\nChanges to panel spacing, minimum output height, interaction row sizing, or list visibility must\nupdate `src/lib_tests.rs` and the native host tests that assert the final scene. Verify the crate\nwith:\n\n```text\ncargo test -p zeta-composer\n```",
    "headings": [
      {
        "depth": 2,
        "id": "boundary",
        "title": "Boundary"
      },
      {
        "depth": 2,
        "id": "execution-path",
        "title": "Execution path"
      }
    ],
    "searchText": "zeta-composer owns the product-level geometry contract for zeterm's command composer. It resolves the bottom-mounted composer panel, its fixed information/editor/toolbar rows, the optional interaction list, and fixed-height interaction-list measurement. The crate is a pure projection layer: it receives logical bounds and item counts, then returns geometry or generic zeta-ui scroll commands. Boundary Capability Owner Status --- --- --- Composer panel and interaction-list geometry zeta-composer ✅ Composer text, history, mode and selected item state zeterm/src/agentcomposer.rs and related host modules ✅ host-owned Code-editor document and text-input behavior zeterm/src/composereditor.rs ✅ host-owned Interaction item projection and activation zeterm/src/composerinteraction.rs ✅ host-owned Scene painting, accessibility registration and product command mapping zeterm/src/composerpanel.rs and host modules ✅ host-owned Renderer, window and platform event APIs lower framework/backend and host adapters ❌ The dependency direction is: ``text zeterm host → zeta-composer → zeta-ui → zui ` zeta-composer must not import NativeApp, workspace/session state, winit, wgpu, renderer resources, or product icon catalogs. If a change needs those values, keep the adapter in zeterm/src and pass only the resulting preferred size, item count, or logical bounds into this crate. Execution path ShellLayout asks ComposerPanelLayout::formain for panel geometry. The host then paints and registers the returned regions. Composer selection and wheel routing use interactionlistbounds, interactioncontentsize, and interactionselectionscrollcommand while the host retains ScrollState and applies the resulting command. Changes to panel spacing, minimum output height, interaction row sizing, or list visibility must update src/libtests.rs and the native host tests that assert the final scene. Verify the crate with: `text cargo test -p zeta-composer ``"
  },
  {
    "slug": "crates/config",
    "sourcePath": "zeta-rs/config/README.md",
    "title": "zeta-config",
    "description": "zeta-config 是普通、非 secret 用户配置的 TOML authority，也是严格 Workspace TOML document 的 typed parser/resolver。SQLite 只保存 transaction metadata 和 exact receipt。它不拥有这些内容： UI/device preference、credentia…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责 crate 当前实现契约。跨领域 source、scope、safe point 与长期演进由\n> [`docs/config.md`](../../docs/config.md) 规范。\n\n`zeta-config` 是普通、非 secret 用户配置的 TOML authority，也是严格 Workspace TOML document 的\ntyped parser/resolver。SQLite 只保存 transaction metadata 和 exact receipt。它不拥有这些内容：\nUI/device preference、credential bytes、Plugin package/activation、live MCP connection、Hook\nexecution、Skill 正文、Session/Thread 或 Core execution。\n\n## 当前 API 与所有权\n\n| Symbol | 职责 |\n| --- | --- |\n| `ConfigStore` | 原子替换 User `config.toml`，并在共享 `state.sqlite3` 中协调 revision/generation 与 exact command receipt |\n| `ConfigCommandRequest` / `UserConfigCommand` | typed mutation、expected revision 与 retry identity |\n| `ResolvedConfigSnapshot` | User authority 的 immutable runtime input |\n| `WorkspaceConfigStore` | strict-read `.zeta/config.toml`，不写 Workspace 文件 |\n| `WorkspaceTrustConfig` | 按 opaque `WorkspaceTrustId` 持久化 User 的 Restricted/Trusted 决策；缺失时 fail closed |\n| `UserExecPolicyConfig` | 持久化 typed User rules；通过 `UpsertExecPolicyRule` / `RemoveExecPolicyRule` 原子变更 |\n| `WorkspaceExecPolicyConfig` | strict-read Workspace restrictions；validation 禁止 `AllowUnsandboxed` |\n| `compose_exec_policy` | 将 trusted Host/Organization layers、User rules 与 Workspace restrictions 组合成 immutable snapshot |\n| `LanguageServersConfig` / `LanguageServerConfig` | 持久化 stable server ID 对应的 Disabled/Automatic/Enabled 与可选绝对 executable override |\n| `resolve_scoped_config` | User + Workspace 的受限 merge、provenance 与 diagnostic |\n| `ConfigChange` | metadata commit 后的 revision/generation signal，包括 TOML 外部编辑与其他 connection 的提交 |\n\n`UserConfigDocument` 当前包含 Agent defaults、Provider map、standalone MCP declaration、Skill\nsource/enablement、exact Plugin request、declarative Hook、language-server preference，以及 execution-policy\nrules 和 Workspace trust decision。Trust key\n由 host 对 canonical root 生成，document 不保存本地路径；User decision 不能冒充 organization\npolicy 或 host configuration。Plugin request 不安装或授权 package；Hook declaration 不执行\nprocess。Theme/UI preference 不在本 crate；Desktop device configuration 是独立 authority。\n\n## Durable 路径\n\n```text\nConfigStore::apply\n→ BEGIN IMMEDIATE\n→ exact command receipt lookup\n→ expected ConfigRevision compare\n→ apply typed UserConfigCommand\n→ validate full UserConfigDocument\n→ atomic replace config.toml when consumer-visible value changed\n→ update config_metadata digest/revision/generation\n→ insert config_command_receipts\n→ COMMIT\n→ publish ConfigChange\n```\n\n相同 command ID + 相同 expected revision/payload 返回原 receipt；相同 ID 配不同输入返回\n`CommandConflict`。No-op 会保存 receipt，但不推进 revision/generation，也不发布 change。\n\n`store_schema` 拥有 `config_metadata`、`config_command_receipts` 和 component migration gate；\nmetadata 不含 document 正文。`store_file` 拥有 strict TOML、semantic digest 与 temp-file rename。\n`store_monitor` 同时观察 TOML semantic change 与 SQLite `data_version`，并按 revision/generation\n去重。连接启用 WAL、foreign keys、`synchronous=FULL` 与 5 秒 busy timeout。\n\n## Resolution 与运行时边界\n\n`WorkspaceConfigStore` 只接收 host 提供的 `WorkspaceId`、文件 path 与 observed content；\nWorkspace 文档不能选择 namespace、credential binding 或 grant。`resolve_scoped_config` 当前只让\nWorkspace preferred model 在 User 已配置对应 Provider 时覆盖，并把 MCP/Skill/Plugin/Hook 内容\n保留为 pending intent；execution-policy rules 保留为只收紧 intent，并由 App Server 与 Host layer\n组合后在 tool safe point 激活。\n\nWorkspace trust 是另一条解析轴：`.zeta/config.toml` 不能声明 `workspaceTrust`；只有 User\n`config.toml` 的 `WorkspaceTrustConfig`、organization policy 或 trusted host composition 能产生\n信任来源。App Server 对 client 请求的 `workspace/switch` 在切换安全点重新读取 User snapshot，\n按目标根的 `WorkspaceTrustId` 授权；未记录的根保持 Restricted。进程启动时由 trusted host\n明确固定的初始根仍标记为 `HostConfiguration`，不等同于 client 后续选择的任意根。\nUser trust 从 Trusted 变为 Restricted 时，Config change 同时触发 App Server 撤销当前\nroot-bound lease、移除执行型服务并中断活跃 Turn；filesystem 与 watcher 保留。\n\nApp Server 在 model safe point 读取 resolved snapshot；Skill/MCP manager 订阅 `ConfigChange` 后在\n旁路 reconcile。Config commit 成功不等于 MCP 已连接、Skill 已可用、Plugin 已激活或 Hook 已执行，\nreconcile failure 不能回滚 desired document。\n\n当前 typed RPC mutation 会重新序列化完整 TOML document，因此键顺序保持确定，但手写注释和\n自定义排版可能被规范化；外部只改注释或排版不会推进 semantic generation。\n\n## 关键私有符号\n\n| Symbol | 作用 | 漂移信号 |\n| --- | --- | --- |\n| `store::ConfigAuthority` | TOML document + SQLite metadata 的一次一致观测 | 若 DB 重新保存 editable document，出现第二 authority |\n| `store_file::write_document` | TOML atomic replace 与 durability | 若 command 绕过它直接改文件，receipt/revision contract 会漂移 |\n| `store::read_receipt` / `write_receipt` | exact replay contract | 若 App Server 自己实现 dedupe，ownership 被复制 |\n| `store_schema::initialize` | component schema install/version gate | 若 Session/Thread tables 在此定义，物理边界混合 |\n| `store_monitor::publish` | cross-connection 去重 signal | signal 不能成为未提交状态的来源 |\n| `UserConfigDocument::validate` | 全文 typed invariant | 禁止用 arbitrary JSON escape hatch 绕开 |\n| `resolve_scoped_config` | scope/trust merge | Workspace 不能扩大 credential、grant 或 policy |\n| `exec_policy::{compose_exec_policy, UserExecPolicyConfig, WorkspaceExecPolicyConfig}` | rule source validation 与 typed composition | 不能复制 selector evaluation 或最终 grant authority |\n| `workspace_trust::WorkspaceTrustConfig::decision_for` | User trust lookup 与缺失时 Restricted | Workspace document 不能进入这条 mutation/persistence 路径 |\n\n## 失败与验证\n\n不支持的 SQLite/document schema、损坏 TOML、静态 Provider 错误、foreign namespace、revision\n冲突与 receipt payload conflict 都 fail closed。Secret 不得出现在 document、receipt、diagnostic\n或测试 fixture 中。\n\n```text\ncargo test -p zeta-config\n```\n\n测试覆盖 TOML durability/reopen、旧 DB document 迁出、external edit、revision/replay/conflict、\n以下 no-op generation 与 cross-connection signal、Provider/model invariant、MCP/Skill/Plugin/Hook\ndesired config、language-server ID/mode/absolute-path validation、execution-policy\nmutation/round-trip/layer constraints、Workspace strict TOML parsing、namespace 和 scoped resolution。",
    "headings": [
      {
        "depth": 2,
        "id": "当前-api-与所有权",
        "title": "当前 API 与所有权"
      },
      {
        "depth": 2,
        "id": "durable-路径",
        "title": "Durable 路径"
      },
      {
        "depth": 2,
        "id": "resolution-与运行时边界",
        "title": "Resolution 与运行时边界"
      },
      {
        "depth": 2,
        "id": "关键私有符号",
        "title": "关键私有符号"
      },
      {
        "depth": 2,
        "id": "失败与验证",
        "title": "失败与验证"
      }
    ],
    "searchText": " 本 README 负责 crate 当前实现契约。跨领域 source、scope、safe point 与长期演进由 docs/config.md 规范。 zeta-config 是普通、非 secret 用户配置的 TOML authority，也是严格 Workspace TOML document 的 typed parser/resolver。SQLite 只保存 transaction metadata 和 exact receipt。它不拥有这些内容： UI/device preference、credential bytes、Plugin package/activation、live MCP connection、Hook execution、Skill 正文、Session/Thread 或 Core execution。 当前 API 与所有权 Symbol 职责 --- --- ConfigStore 原子替换 User config.toml，并在共享 state.sqlite3 中协调 revision/generation 与 exact command receipt ConfigCommandRequest / UserConfigCommand typed mutation、expected revision 与 retry identity ResolvedConfigSnapshot User authority 的 immutable runtime input WorkspaceConfigStore strict-read .zeta/config.toml，不写 Workspace 文件 WorkspaceTrustConfig 按 opaque WorkspaceTrustId 持久化 User 的 Restricted/Trusted 决策；缺失时 fail closed UserExecPolicyConfig 持久化 typed User rules；通过 UpsertExecPolicyRule / RemoveExecPolicyRule 原子变更 WorkspaceExecPolicyConfig strict-read Workspace restrictions；validation 禁止 AllowUnsandboxed composeexecpolicy 将 trusted Host/Organization layers、User rules 与 Workspace restrictions 组合成 immutable snapshot LanguageServersConfig / LanguageServerConfig 持久化 stable server ID 对应的 Disabled/Automatic/Enabled 与可选绝对 executable override resolvescopedconfig User + Workspace 的受限 merge、provenance 与 diagnostic ConfigChange metadata commit 后的 revision/generation signal，包括 TOML 外部编辑与其他 connection 的提交 UserConfigDocument 当前包含 Agent defaults、Provider map、standalone MCP declaration、Skill source/enablement、exact Plugin request、declarative Hook、language-server preference，以及 execution-policy rules 和 Workspace trust decision。Trust key 由 host 对 canonical root 生成，document 不保存本地路径；User decision 不能冒充 organization policy 或 host configuration。Plugin request 不安装或授权 package；Hook declaration 不执行 process。Theme/UI preference 不在本 crate；Desktop device configuration 是独立 authority。 Durable 路径 ``text ConfigStore::apply → BEGIN IMMEDIATE → exact command receipt lookup → expected ConfigRevision compare → apply typed UserConfigCommand → validate full UserConfigDocument → atomic replace config.toml when consumer-visible value changed → update configmetadata digest/revision/generation → insert configcommandreceipts → COMMIT → publish ConfigChange ` 相同 command ID + 相同 expected revision/payload 返回原 receipt；相同 ID 配不同输入返回 CommandConflict。No-op 会保存 receipt，但不推进 revision/generation，也不发布 change。 storeschema 拥有 configmetadata、configcommandreceipts 和 component migration gate； metadata 不含 document 正文。storefile 拥有 strict TOML、semantic digest 与 temp-file rename。 storemonitor 同时观察 TOML semantic change 与 SQLite dataversion，并按 revision/generation 去重。连接启用 WAL、foreign keys、synchronous=FULL 与 5 秒 busy timeout。 Resolution 与运行时边界 WorkspaceConfigStore 只接收 host 提供的 WorkspaceId、文件 path 与 observed content； Workspace 文档不能选择 namespace、credential binding 或 grant。resolvescopedconfig 当前只让 Workspace preferred model 在 User 已配置对应 Provider 时覆盖，并把 MCP/Skill/Plugin/Hook 内容 保留为 pending intent；execution-policy rules 保留为只收紧 intent，并由 App Server 与 Host layer 组合后在 tool safe point 激活。 Workspace trust 是另一条解析轴：.zeta/config.toml 不能声明 workspaceTrust；只有 User config.toml 的 WorkspaceTrustConfig、organization policy 或 trusted host composition 能产生 信任来源。App Server 对 client 请求的 workspace/switch 在切换安全点重新读取 User snapshot， 按目标根的 WorkspaceTrustId 授权；未记录的根保持 Restricted。进程启动时由 trusted host 明确固定的初始根仍标记为 HostConfiguration，不等同于 client 后续选择的任意根。 User trust 从 Trusted 变为 Restricted 时，Config change 同时触发 App Server 撤销当前 root-bound lease、移除执行型服务并中断活跃 Turn；filesystem 与 watcher 保留。 App Server 在 model safe point 读取 resolved snapshot；Skill/MCP manager 订阅 ConfigChange 后在 旁路 reconcile。Config commit 成功不等于 MCP 已连接、Skill 已可用、Plugin 已激活或 Hook 已执行， reconcile failure 不能回滚 desired document。 当前 typed RPC mutation 会重新序列化完整 TOML document，因此键顺序保持确定，但手写注释和 自定义排版可能被规范化；外部只改注释或排版不会推进 semantic generation。 关键私有符号 Symbol 作用 漂移信号 --- --- --- store::ConfigAuthority TOML document + SQLite metadata 的一次一致观测 若 DB 重新保存 editable document，出现第二 authority storefile::writedocument TOML atomic replace 与 durability 若 command 绕过它直接改文件，receipt/revision contract 会漂移 store::readreceipt / writereceipt exact replay contract 若 App Server 自己实现 dedupe，ownership 被复制 storeschema::initialize component schema install/version gate 若 Session/Thread tables 在此定义，物理边界混合 storemonitor::publish cross-connection 去重 signal signal 不能成为未提交状态的来源 UserConfigDocument::validate 全文 typed invariant 禁止用 arbitrary JSON escape hatch 绕开 resolvescopedconfig scope/trust merge Workspace 不能扩大 credential、grant 或 policy execpolicy:: composeexecpolicy, UserExecPolicyConfig, WorkspaceExecPolicyConfig rule source validation 与 typed composition 不能复制 selector evaluation 或最终 grant authority workspacetrust::WorkspaceTrustConfig::decisionfor User trust lookup 与缺失时 Restricted Workspace document 不能进入这条 mutation/persistence 路径 失败与验证 不支持的 SQLite/document schema、损坏 TOML、静态 Provider 错误、foreign namespace、revision 冲突与 receipt payload conflict 都 fail closed。Secret 不得出现在 document、receipt、diagnostic 或测试 fixture 中。 `text cargo test -p zeta-config `` 测试覆盖 TOML durability/reopen、旧 DB document 迁出、external edit、revision/replay/conflict、 以下 no-op generation 与 cross-connection signal、Provider/model invariant、MCP/Skill/Plugin/Hook desired config、language-server ID/mode/absolute-path validation、execution-policy mutation/round-trip/layer constraints、Workspace strict TOML parsing、namespace 和 scoped resolution。"
  },
  {
    "slug": "crates/connectors",
    "sourcePath": "zeta-rs/connectors/README.md",
    "title": "zeta-connectors",
    "description": "zeta-connectors 是后端无关的 Connector 领域层。它定义外部产品连接是什么、连接状态如何合法 迁移，以及何时可以发布 runtime binding；它不发现 Plugin、不执行 OAuth、不保存 credential bytes， 也不启动 MCP session。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Connector identity、definition、connection state machine 与 immutable snapshot\n> 的当前实现契约。跨 Plugin、认证、MCP 和产品 UI 的系统语义由\n> [`docs/connectors.md`](../../docs/connectors.md) 维护。\n\n`zeta-connectors` 是后端无关的 Connector 领域层。它定义外部产品连接是什么、连接状态如何合法\n迁移，以及何时可以发布 runtime binding；它不发现 Plugin、不执行 OAuth、不保存 credential bytes，\n也不启动 MCP session。\n\n## 公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `ConnectorId` | 一个 connectable product declaration 的稳定 identity | Plugin identity、外部账号 identity |\n| `ConnectorDefinition` | display metadata、runtime-free binding 与 authorization revision | OAuth 配置、live transport |\n| `ConnectorDefinitionDigest` | connector ID、runtime binding 与授权 revision 的稳定摘要 | display copy、secret bytes |\n| `ConnectorRuntimeBinding` | 描述连接成功后可 materialize 的 runtime；当前为 MCP server ID | MCP session、tool registry |\n| `ConnectorAccount` | 外部产品账号与 non-secret credential reference | Zeta login、secret bytes |\n| `ConnectorConnection` | connection generation 与状态转换校验 | durable persistence、retry scheduling |\n| `ConnectorSnapshot` | generation-bound definitions + connection state | Plugin provenance、产品 discovery DTO |\n\n所有 identity 和展示文本都拒绝空值、控制字符、首尾空白与超长输入。`ConnectorCredentialRef` 只是\nauthentication adapter 可解释的引用，不能包含或暗示 credential bytes 由本 crate 持有。\n\n## 状态转换与失败语义\n\n一次连接必须按以下顺序发布：\n\n```text\nDisconnected / Unavailable / Connected\n  -> ConnectorConnectionUpdate::Begin(new connection generation)\n  -> Connecting\n  -> Connected(account with the exact same connection generation)\n     or Unavailable(the exact same connection generation)\n```\n\n`Disconnect` 表示 revoke 或显式断开，必须使用更大的 connection generation。每次 update 还必须通过\n`ConnectorSnapshot::with_connection_update` 在更大的 snapshot generation 下原子发布。旧 snapshot、\n旧 connection attempt、跳过 `Connecting` 的 direct connect 和 unknown Connector 都 fail closed。\n\n`Unavailable` 当前只保留 sanitized reason，不保留上一个 connected account；后续 reconnect 必须开始\n新的 connection generation。该约束避免 unavailable state 被误当成仍持有调用 authority。\n\n`DefinitionChanged` 只允许从 `Connected` 进入 `ReauthorizationRequired`，保持 account projection 供 UI\n解释，但 `ConnectorEntry::is_ready` 立即返回 false。Plugin adapter 必须把 exact package/runtime permission\ndigest 传给 `with_authorization_revision`；display name 或 description 变化不会改变 digest。\n\n## 内部所有权与调用路径\n\n```text\nConnectorDefinition::new\n  -> definition::validate_text\n  -> ConnectorDefinition::with_authorization_revision\n  -> ConnectorDefinition::digest\n  -> ConnectorSnapshot::new\n       -> sort + duplicate identity rejection\n\nConnectorSnapshot::with_connection_update\n  -> snapshot generation check\n  -> ConnectorConnection::apply\n       -> connection generation + transition check\n  -> publish cloned immutable snapshot\n```\n\n| Private symbol | Ownership | 漂移信号 |\n| --- | --- | --- |\n| `definition::validate_text` | definition/account/reason/revision 的 bounded plain-text invariant | 开始解释 provider 或 Plugin schema |\n| `definition::update_digest_field` | authorization compatibility digest 的 domain-separated framing | hash display copy 或 secret bytes |\n| `ConnectorConnection::apply` | connection transition 与 generation ordering | 执行 OAuth、读取 secrets、启动 MCP |\n| `ConnectorSnapshot::with_connection_update` | immutable atomic projection | 持久化 authority 或 App Server event delivery |\n\n如果本 crate 开始依赖 `zeta-plugins`、`zeta-mcp`、`zeta-secrets`、App Server 或产品 UI，即表示依赖\n方向发生漂移。对应适配应进入 `zeta-rs/ext/connectors` 或 authentication/runtime owner。\n\n## 集成义务\n\n调用方必须分别提供：\n\n- declaration source 到 `ConnectorDefinition` 的转换和 provenance；\n- OAuth/API-key 等 connect flow 与 credential persistence；\n- monotonic snapshot/connection generation authority；\n- ready binding 到具体 MCP 或 host adapter runtime 的 materialization；\n- App Server protocol、UI、audit 和 durable authority persistence。\n\n`zeta-connectors` 不要求用户登录 Zeta。云端目录、跨设备同步和 managed policy 可以作为上层 source\n或 authority adapter，但不能改变本地 Connector domain 的 identity 与 transition contract。\n\n## 验证\n\n```bash\ncargo test -p zeta-connectors\ncargo clippy -p zeta-connectors --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/connectors:connectors-unit-tests\n```\n\n当前测试覆盖 duplicate identity、必须经过 `Connecting`、connection generation 匹配、snapshot stale\nupdate 拒绝、definition digest / reauthorization，以及 disconnect 后 runtime readiness 撤销。\n\n## 当前限制与扩展点\n\n当前只定义 MCP runtime binding；将来只有在出现真实的非 MCP consumer 后，才增加新的 binding\nvariant。持久化 authority、API-token credential orchestration 和 App Server projection 已在\n`zeta-connectors-extension` / `zeta-app-server` 实现；OAuth、health/reconnect、生产 credential backend\n和产品 UI 仍属于上层 integration，而不是扩张本 crate 的 I/O ownership。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "状态转换与失败语义",
        "title": "状态转换与失败语义"
      },
      {
        "depth": 2,
        "id": "内部所有权与调用路径",
        "title": "内部所有权与调用路径"
      },
      {
        "depth": 2,
        "id": "集成义务",
        "title": "集成义务"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 Connector identity、definition、connection state machine 与 immutable snapshot 的当前实现契约。跨 Plugin、认证、MCP 和产品 UI 的系统语义由 docs/connectors.md 维护。 zeta-connectors 是后端无关的 Connector 领域层。它定义外部产品连接是什么、连接状态如何合法 迁移，以及何时可以发布 runtime binding；它不发现 Plugin、不执行 OAuth、不保存 credential bytes， 也不启动 MCP session。 公共契约 Symbol 当前职责 不承担 --- --- --- ConnectorId 一个 connectable product declaration 的稳定 identity Plugin identity、外部账号 identity ConnectorDefinition display metadata、runtime-free binding 与 authorization revision OAuth 配置、live transport ConnectorDefinitionDigest connector ID、runtime binding 与授权 revision 的稳定摘要 display copy、secret bytes ConnectorRuntimeBinding 描述连接成功后可 materialize 的 runtime；当前为 MCP server ID MCP session、tool registry ConnectorAccount 外部产品账号与 non-secret credential reference Zeta login、secret bytes ConnectorConnection connection generation 与状态转换校验 durable persistence、retry scheduling ConnectorSnapshot generation-bound definitions + connection state Plugin provenance、产品 discovery DTO 所有 identity 和展示文本都拒绝空值、控制字符、首尾空白与超长输入。ConnectorCredentialRef 只是 authentication adapter 可解释的引用，不能包含或暗示 credential bytes 由本 crate 持有。 状态转换与失败语义 一次连接必须按以下顺序发布： ``text Disconnected / Unavailable / Connected - ConnectorConnectionUpdate::Begin new connection generation - Connecting - Connected account with the exact same connection generation or Unavailable the exact same connection generation ` Disconnect 表示 revoke 或显式断开，必须使用更大的 connection generation。每次 update 还必须通过 ConnectorSnapshot::withconnectionupdate 在更大的 snapshot generation 下原子发布。旧 snapshot、 旧 connection attempt、跳过 Connecting 的 direct connect 和 unknown Connector 都 fail closed。 Unavailable 当前只保留 sanitized reason，不保留上一个 connected account；后续 reconnect 必须开始 新的 connection generation。该约束避免 unavailable state 被误当成仍持有调用 authority。 DefinitionChanged 只允许从 Connected 进入 ReauthorizationRequired，保持 account projection 供 UI 解释，但 ConnectorEntry::isready 立即返回 false。Plugin adapter 必须把 exact package/runtime permission digest 传给 withauthorizationrevision；display name 或 description 变化不会改变 digest。 内部所有权与调用路径 `text ConnectorDefinition::new - definition::validatetext - ConnectorDefinition::withauthorizationrevision - ConnectorDefinition::digest - ConnectorSnapshot::new - sort + duplicate identity rejection ConnectorSnapshot::withconnectionupdate - snapshot generation check - ConnectorConnection::apply - connection generation + transition check - publish cloned immutable snapshot ` Private symbol Ownership 漂移信号 --- --- --- definition::validatetext definition/account/reason/revision 的 bounded plain-text invariant 开始解释 provider 或 Plugin schema definition::updatedigestfield authorization compatibility digest 的 domain-separated framing hash display copy 或 secret bytes ConnectorConnection::apply connection transition 与 generation ordering 执行 OAuth、读取 secrets、启动 MCP ConnectorSnapshot::withconnectionupdate immutable atomic projection 持久化 authority 或 App Server event delivery 如果本 crate 开始依赖 zeta-plugins、zeta-mcp、zeta-secrets、App Server 或产品 UI，即表示依赖 方向发生漂移。对应适配应进入 zeta-rs/ext/connectors 或 authentication/runtime owner。 集成义务 调用方必须分别提供： - declaration source 到 ConnectorDefinition 的转换和 provenance； - OAuth/API-key 等 connect flow 与 credential persistence； - monotonic snapshot/connection generation authority； - ready binding 到具体 MCP 或 host adapter runtime 的 materialization； - App Server protocol、UI、audit 和 durable authority persistence。 zeta-connectors 不要求用户登录 Zeta。云端目录、跨设备同步和 managed policy 可以作为上层 source 或 authority adapter，但不能改变本地 Connector domain 的 identity 与 transition contract。 验证 `bash cargo test -p zeta-connectors cargo clippy -p zeta-connectors --all-targets --no-deps -- -D warnings bazel test //zeta-rs/connectors:connectors-unit-tests ` 当前测试覆盖 duplicate identity、必须经过 Connecting、connection generation 匹配、snapshot stale update 拒绝、definition digest / reauthorization，以及 disconnect 后 runtime readiness 撤销。 当前限制与扩展点 当前只定义 MCP runtime binding；将来只有在出现真实的非 MCP consumer 后，才增加新的 binding variant。持久化 authority、API-token credential orchestration 和 App Server projection 已在 zeta-connectors-extension / zeta-app-server` 实现；OAuth、health/reconnect、生产 credential backend 和产品 UI 仍属于上层 integration，而不是扩张本 crate 的 I/O ownership。"
  },
  {
    "slug": "crates/context-engine",
    "sourcePath": "zeta-rs/context-engine/README.md",
    "title": "zeta-context-engine",
    "description": "zeta-context-engine 把“模型最多能装多少”和“当前候选请求按什么计数”转换为明确的预算结论。 它允许不同 provider/model 提供精准 tokenizer 或预检结果；没有精准能力时，调用方提供带版本和 保守记账值的估算。响应后的 usage 只用于记账和校准，不能冒充调用前预算。",
    "group": "Crate 实现参考",
    "markdown": "> 本文拥有模型无关的上下文预算、输入 token 计量结果和边界判定实现契约。Thread 历史选择、\n> checkpoint 与压缩流程的系统语义由 [`docs/core-context.md`](../../docs/core-context.md) 拥有。\n\n`zeta-context-engine` 把“模型最多能装多少”和“当前候选请求按什么计数”转换为明确的预算结论。\n它允许不同 provider/model 提供精准 tokenizer 或预检结果；没有精准能力时，调用方提供带版本和\n保守记账值的估算。响应后的 usage 只用于记账和校准，不能冒充调用前预算。\n\n## 1. 边界与依赖\n\n| 拥有 | 不拥有 |\n| --- | --- |\n| context window、输出预留、安全余量和压缩阈值的独立类型 | 模型目录、配置覆盖、provider 选择或凭证 |\n| 压缩压力线与模型硬窗口的统一计算 | tokenizer 实现、provider 预检网络请求或重试 |\n| 精准计数与保守估算的统一结果模型 | Thread 历史选择、Skill 注入或 `ModelRequest` 组装 |\n| `Fits`、`NeedsCompaction`、`ExceedsContextWindow` 判定 | checkpoint 生成、压缩模型调用或 durable commit |\n\n本 crate 没有运行时依赖，也不依赖某个 provider SDK。`zeta-core`、App Server 或 provider adapter\n可以依赖它；它不得反向依赖这些协调与执行层。\n\n本 crate 有意不提供一个假装所有计量都同步的 provider trait。本地 tokenizer 是纯计算，provider\npreflight 可能涉及网络、取消和重试，两者的执行生命周期由上层 adapter 拥有；跨边界的通用接口是\n不可变 `ContextTokenMeasurement`。只有出现两个真实调用方需要同一种异步/取消端口时，才在对应的\nprovider 协调层提取执行 trait，预算引擎仍只消费计量结果。\n\n## 2. 公共契约\n\n### 2.1 预算\n\n- `ContextBudget::CoreManaged` 分别接收模型窗口、输出预留、安全余量和压缩阈值。\n- `ContextBudget::resolve` 生成 `ContextBudgetLimits`：`maximum_input` 是普通请求触发压缩的压力线，\n  `hard_maximum_input` 是扣除输出预留与安全余量后的硬上限。\n- `ContextBudget::ProviderManaged` 表示没有可信模型上限。它不是“无限窗口”，调用方不得把它报告为\n  已验证可装入。\n- 任何没有留下正数输入容量的 Core-managed 配置都返回 `ContextBudgetError`，不会依靠减法饱和后\n  继续运行。\n\n### 2.2 token 计量\n\n| 可用能力 | 构造方式 | 预算使用值 | 典型来源 |\n| --- | --- | --- | --- |\n| provider 调用前预检 | `provider_preflight(revision)` + 独立 accuracy | 精确值或保守值 | OpenAI/Anthropic count API |\n| 与所选模型匹配的本地 tokenizer | `local_tokenizer(revision)` + 独立 accuracy | 精确值或保守值 | 本地 tokenizer registry |\n| 无精准计数能力 | `ContextTokenMeasurement::estimated` | 保守记账值 | `deterministic-bytes-v1` 等 estimator |\n\n`ContextTokenMeasurement` 必须针对最终候选请求，而不是只数消息正文。调用方负责包含 instructions、\ntools、图片和 provider wire envelope 的成本。`LocalTokenizer` 的实现者还必须保证 tokenizer 与所选\n模型匹配；revision 变化时不得复用旧计量缓存。\n\n来源和准确度是两个独立维度：OpenAI Responses preflight 按 provider 契约记为 exact；Anthropic\ncount endpoint 同样来自 provider preflight，但官方只承诺 estimate，因此记为 estimated。估算同时\n保存 measured count 和 conservative accounted value；后者是预算策略余量，不宣称数学意义上的硬\n上界。`ContextBudgetPlanner` 只用 accounted value 做判定，revision 为空或 accounted value 小于\nmeasured count 都会失败。\n\n### 2.3 判定\n\n```text\nmodel/provider adapter or local estimator\n  → ContextTokenMeasurement\n  → ContextBudgetPlanner::assess(ContextBudget, measurement)\n      → ProviderManaged\n      → Fits\n      → NeedsCompaction\n      → ExceedsContextWindow\n```\n\n`NeedsCompaction` 表示请求超过主动压缩压力线、但仍未超过硬窗口；调用方可以先压缩再重建请求。\n`ExceedsContextWindow` 表示即使不考虑主动压缩策略，也已经越过模型硬容量，不能直接发送。\n\n## 3. 内部接口地图\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `ContextBudget::resolve` | public | 校验分配并计算压力线与硬上限 | provider 名称、Thread 或压缩 I/O 进入预算数学 |\n| `ContextTokenMeasurement::estimated` | public | 固定来源 revision 与保守记账不变量 | 允许无 revision 或用 measured value 直接判定 |\n| `ContextBudgetPlanner::assess` | public | 纯比较 accounted input 与两条边界 | 直接调用 tokenizer、usage API 或修改历史 |\n| `planner::subtract` | private | 为诊断值计算无下溢差值 | 承担模型配置或计量策略选择 |\n\n真实调用关系：\n\n```text\nmodel metadata/config\n  → ContextBudget::resolve\n\nprovider/local tokenizer/estimator adapter\n  → ContextTokenMeasurement\n\nContextBudget + ContextTokenMeasurement\n  → ContextBudgetPlanner::assess\n  → caller-owned selection / compaction / invocation\n```\n\n## 4. 接入与失败语义\n\n调用方按能力选择计量来源，统一优先级是“provider 官方预检、匹配模型的本地整请求计数、Core\n保守估算”。本 crate 不规定预检调用频率：本地计数可以每次调用；有额外网络往返的 provider\n预检通常由上层只在接近压力线、估算不确定或重试恢复时启用。无论采用哪种节奏，最终计量结果都\n进入同一个 `ContextBudgetPlanner`，不会形成 provider 专用预算算法。\n\nusage 属于调用完成后的事实。上层可以用它做成本记录、观测 estimator 偏差或更新校准参数，但不能\n用当前响应的 usage 决定已经发出的请求是否安全。若后续引入校准，校准数据必须按 provider、model、\ntokenizer/estimator revision 隔离。\n\n本 crate 的错误都是调用前、无副作用错误。它不重试、不访问网络，也不修改 Thread。调用方收到\n`NeedsCompaction` 或 `ExceedsContextWindow` 后负责执行对应流程，并在内容变化后重新计量。\n\n## 5. 测试与修改影响\n\n```bash\ncargo test -p zeta-context-engine\ncargo clippy -p zeta-context-engine --all-targets -- -D warnings\ncargo test -p zeta-core context\n```\n\n修改预算公式时必须同步检查 App Server 的模型配置映射和 Core planner 边界测试。修改计量不变量时\n必须同步检查所有 provider/tokenizer adapter、诊断字段和 estimator revision。新增 provider 能力时\n应实现 adapter，不应向本 crate 添加 provider 名称分支。\n\n## 6. 当前状态与扩展点\n\n- **Current**：预算类型已从 `zeta-core` 移入本 crate，Core 通过公共 `resolve` 契约消费压力线与硬上限。\n- **Current**：精准计数、保守估算和统一判定 value contract 已实现，并由无运行时依赖的单测覆盖。\n- **Current**：OpenAI Responses `/responses/input_tokens` 已作为 exact remote preflight 接入；\n  Anthropic Messages、Google `countTokens`、Kimi estimate 与 Z.AI tokenizer 已作为 estimated remote\n  preflight 接入，并使用 `max(32, ceil(count / 100))` 的保守记账余量。\n- **Current**：Core 对本地计数每次执行；remote preflight 只在距压力线 10%/至少 4096 tokens、\n  compaction 后复核时执行，计量发现低估后收紧容量并重新规划。\n- **Current**：`zeta-model-tokenizer` 已提供按完整 `ModelRef` 绑定、双资产 revision/digest 固定、\n  `hf-chat-template` 执行、按需下载/磁盘缓存/内存 LRU；Provider runtime 统一消费本地计数。本地结果\n  因远端可能追加 envelope 而记为 estimated，并使用 2%/至少 64 tokens 的保守余量。\n- **Current limitation**：Hugging Face 公共 `owner/repo` 已支持自动发现；其他 provider/model 仍需\n  固定资产清单。无法处理的请求返回 unavailable，Core 仍以 `deterministic-bytes-v1` 作为首轮估算。\n- **Current limitation**：usage 校准与计量缓存尚未实现。\n- **Extension point**：真实 adapter 落地后，可以增加“何时请求远端预检”的上层策略；该策略不得\n  改变本 crate 的预算公式，也不得让 usage 与调用前计量共用同一类型。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与依赖",
        "title": "1. 边界与依赖"
      },
      {
        "depth": 2,
        "id": "2-公共契约",
        "title": "2. 公共契约"
      },
      {
        "depth": 3,
        "id": "21-预算",
        "title": "2.1 预算"
      },
      {
        "depth": 3,
        "id": "22-token-计量",
        "title": "2.2 token 计量"
      },
      {
        "depth": 3,
        "id": "23-判定",
        "title": "2.3 判定"
      },
      {
        "depth": 2,
        "id": "3-内部接口地图",
        "title": "3. 内部接口地图"
      },
      {
        "depth": 2,
        "id": "4-接入与失败语义",
        "title": "4. 接入与失败语义"
      },
      {
        "depth": 2,
        "id": "5-测试与修改影响",
        "title": "5. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "6-当前状态与扩展点",
        "title": "6. 当前状态与扩展点"
      }
    ],
    "searchText": " 本文拥有模型无关的上下文预算、输入 token 计量结果和边界判定实现契约。Thread 历史选择、 checkpoint 与压缩流程的系统语义由 docs/core-context.md 拥有。 zeta-context-engine 把“模型最多能装多少”和“当前候选请求按什么计数”转换为明确的预算结论。 它允许不同 provider/model 提供精准 tokenizer 或预检结果；没有精准能力时，调用方提供带版本和 保守记账值的估算。响应后的 usage 只用于记账和校准，不能冒充调用前预算。 1. 边界与依赖 拥有 不拥有 --- --- context window、输出预留、安全余量和压缩阈值的独立类型 模型目录、配置覆盖、provider 选择或凭证 压缩压力线与模型硬窗口的统一计算 tokenizer 实现、provider 预检网络请求或重试 精准计数与保守估算的统一结果模型 Thread 历史选择、Skill 注入或 ModelRequest 组装 Fits、NeedsCompaction、ExceedsContextWindow 判定 checkpoint 生成、压缩模型调用或 durable commit 本 crate 没有运行时依赖，也不依赖某个 provider SDK。zeta-core、App Server 或 provider adapter 可以依赖它；它不得反向依赖这些协调与执行层。 本 crate 有意不提供一个假装所有计量都同步的 provider trait。本地 tokenizer 是纯计算，provider preflight 可能涉及网络、取消和重试，两者的执行生命周期由上层 adapter 拥有；跨边界的通用接口是 不可变 ContextTokenMeasurement。只有出现两个真实调用方需要同一种异步/取消端口时，才在对应的 provider 协调层提取执行 trait，预算引擎仍只消费计量结果。 2. 公共契约 2.1 预算 - ContextBudget::CoreManaged 分别接收模型窗口、输出预留、安全余量和压缩阈值。 - ContextBudget::resolve 生成 ContextBudgetLimits：maximuminput 是普通请求触发压缩的压力线， hardmaximuminput 是扣除输出预留与安全余量后的硬上限。 - ContextBudget::ProviderManaged 表示没有可信模型上限。它不是“无限窗口”，调用方不得把它报告为 已验证可装入。 - 任何没有留下正数输入容量的 Core-managed 配置都返回 ContextBudgetError，不会依靠减法饱和后 继续运行。 2.2 token 计量 可用能力 构造方式 预算使用值 典型来源 --- --- --- --- provider 调用前预检 providerpreflight revision + 独立 accuracy 精确值或保守值 OpenAI/Anthropic count API 与所选模型匹配的本地 tokenizer localtokenizer revision + 独立 accuracy 精确值或保守值 本地 tokenizer registry 无精准计数能力 ContextTokenMeasurement::estimated 保守记账值 deterministic-bytes-v1 等 estimator ContextTokenMeasurement 必须针对最终候选请求，而不是只数消息正文。调用方负责包含 instructions、 tools、图片和 provider wire envelope 的成本。LocalTokenizer 的实现者还必须保证 tokenizer 与所选 模型匹配；revision 变化时不得复用旧计量缓存。 来源和准确度是两个独立维度：OpenAI Responses preflight 按 provider 契约记为 exact；Anthropic count endpoint 同样来自 provider preflight，但官方只承诺 estimate，因此记为 estimated。估算同时 保存 measured count 和 conservative accounted value；后者是预算策略余量，不宣称数学意义上的硬 上界。ContextBudgetPlanner 只用 accounted value 做判定，revision 为空或 accounted value 小于 measured count 都会失败。 2.3 判定 ``text model/provider adapter or local estimator → ContextTokenMeasurement → ContextBudgetPlanner::assess ContextBudget, measurement → ProviderManaged → Fits → NeedsCompaction → ExceedsContextWindow ` NeedsCompaction 表示请求超过主动压缩压力线、但仍未超过硬窗口；调用方可以先压缩再重建请求。 ExceedsContextWindow 表示即使不考虑主动压缩策略，也已经越过模型硬容量，不能直接发送。 3. 内部接口地图 Symbol 可见性 职责 漂移信号 --- --- --- --- ContextBudget::resolve public 校验分配并计算压力线与硬上限 provider 名称、Thread 或压缩 I/O 进入预算数学 ContextTokenMeasurement::estimated public 固定来源 revision 与保守记账不变量 允许无 revision 或用 measured value 直接判定 ContextBudgetPlanner::assess public 纯比较 accounted input 与两条边界 直接调用 tokenizer、usage API 或修改历史 planner::subtract private 为诊断值计算无下溢差值 承担模型配置或计量策略选择 真实调用关系： `text model metadata/config → ContextBudget::resolve provider/local tokenizer/estimator adapter → ContextTokenMeasurement ContextBudget + ContextTokenMeasurement → ContextBudgetPlanner::assess → caller-owned selection / compaction / invocation ` 4. 接入与失败语义 调用方按能力选择计量来源，统一优先级是“provider 官方预检、匹配模型的本地整请求计数、Core 保守估算”。本 crate 不规定预检调用频率：本地计数可以每次调用；有额外网络往返的 provider 预检通常由上层只在接近压力线、估算不确定或重试恢复时启用。无论采用哪种节奏，最终计量结果都 进入同一个 ContextBudgetPlanner，不会形成 provider 专用预算算法。 usage 属于调用完成后的事实。上层可以用它做成本记录、观测 estimator 偏差或更新校准参数，但不能 用当前响应的 usage 决定已经发出的请求是否安全。若后续引入校准，校准数据必须按 provider、model、 tokenizer/estimator revision 隔离。 本 crate 的错误都是调用前、无副作用错误。它不重试、不访问网络，也不修改 Thread。调用方收到 NeedsCompaction 或 ExceedsContextWindow 后负责执行对应流程，并在内容变化后重新计量。 5. 测试与修改影响 `bash cargo test -p zeta-context-engine cargo clippy -p zeta-context-engine --all-targets -- -D warnings cargo test -p zeta-core context ` 修改预算公式时必须同步检查 App Server 的模型配置映射和 Core planner 边界测试。修改计量不变量时 必须同步检查所有 provider/tokenizer adapter、诊断字段和 estimator revision。新增 provider 能力时 应实现 adapter，不应向本 crate 添加 provider 名称分支。 6. 当前状态与扩展点 - Current：预算类型已从 zeta-core 移入本 crate，Core 通过公共 resolve 契约消费压力线与硬上限。 - Current：精准计数、保守估算和统一判定 value contract 已实现，并由无运行时依赖的单测覆盖。 - Current：OpenAI Responses /responses/inputtokens 已作为 exact remote preflight 接入； Anthropic Messages、Google countTokens、Kimi estimate 与 Z.AI tokenizer 已作为 estimated remote preflight 接入，并使用 max 32, ceil count / 100 的保守记账余量。 - Current：Core 对本地计数每次执行；remote preflight 只在距压力线 10%/至少 4096 tokens、 compaction 后复核时执行，计量发现低估后收紧容量并重新规划。 - Current：zeta-model-tokenizer 已提供按完整 ModelRef 绑定、双资产 revision/digest 固定、 hf-chat-template 执行、按需下载/磁盘缓存/内存 LRU；Provider runtime 统一消费本地计数。本地结果 因远端可能追加 envelope 而记为 estimated，并使用 2%/至少 64 tokens 的保守余量。 - Current limitation：Hugging Face 公共 owner/repo 已支持自动发现；其他 provider/model 仍需 固定资产清单。无法处理的请求返回 unavailable，Core 仍以 deterministic-bytes-v1` 作为首轮估算。 - Current limitation：usage 校准与计量缓存尚未实现。 - Extension point：真实 adapter 落地后，可以增加“何时请求远端预检”的上层策略；该策略不得 改变本 crate 的预算公式，也不得让 usage 与调用前计量共用同一类型。"
  },
  {
    "slug": "crates/debug-adapter",
    "sourcePath": "zeta-rs/debug-adapter/README.md",
    "title": "zeta-debug-adapter",
    "description": "This crate owns the backend-neutral runtime for trusted Debug Adapter Protocol (DAP) stdio processes. Cross-boundary product behavior and current system status are documented in docs/debu…",
    "group": "Crate 实现参考",
    "markdown": "This crate owns the backend-neutral runtime for trusted Debug Adapter Protocol (DAP) stdio processes. Cross-boundary product behavior and current system status are documented in [`docs/debugging.md`](../../docs/debugging.md); this README is the canonical implementation contract for this crate.\n\n## Ownership\n\n`DebugAdapterService` owns process lifetime, bounded output, DAP `Content-Length` framing, session identity, and cleanup. It requires both `LoadExecutableConfiguration` and `ExecuteProcess` capabilities for the same workspace root. It does not parse `.vscode/launch.json`, own breakpoints, implement DAP client state, authorize App Server connections, or render UI.\n\n| Symbol | Responsibility | Must not own |\n| --- | --- | --- |\n| `DebugAdapterService` | Validate active workspace capabilities and own at most eight adapter processes | Product configuration or connection identity |\n| `DebugAdapterCommand::new` | Bound program and arguments and reject NUL input | Executable discovery |\n| `read_message` / `encode_message` | Parse and create bounded DAP frames | Request pairing or DAP semantics |\n| `DebugAdapterState` | Retain at most 512 messages plus bounded stderr and exit state | Durable history |\n| `push_message` | Assign ordered sequence numbers and evict the oldest retained messages | Consumer cursors |\n| `refresh_process_state` / `terminate` | Observe exit and reap or kill the child | Restart policy |\n\nThe call path is `DebugAdapterService::start` → `tokio::process::Command` → `spawn_stdout_reader` / `spawn_stderr_reader`. Callers send JSON through `send`; `read` returns the next bounded page and advances only past messages in that page. A consumer that falls behind the retained prefix receives `output_gap = true` and must fail the session instead of silently skipping protocol events.\n\n## Failure and integration semantics\n\nInvalid commands and reads fail before mutation. Process launch, framing failure, broken pipes, and poisoned locks are explicit errors. `close` removes exactly one session and reaps its process; `terminate_all` is the fail-safe for trust retirement and drop. App Server connection ownership is deliberately implemented by its `debug_service::DebugAdapterService` wrapper rather than this crate.\n\nThe process environment is supplied by the caller and the runtime clears inherited variables before applying it. Expanding workspace variables and handling DAP reverse requests remain caller obligations.\n\n## Tests and modification impact\n\nRun `cargo test -p zeta-debug-adapter`. Framing coverage lives in `framing_tests.rs`; process/session tests should be added in sibling test files. Changes to messages or limits must be reviewed against App Server pagination and `docs/debugging.md`. Adding product configuration, UI state, connection IDs, or another process registry here would signal ownership drift.\n\n## Current limitations\n\nThe crate supports stdio adapters only. It does not implement socket/server adapters, durable session recovery, restart, telemetry, or language-specific adapter discovery.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "failure-and-integration-semantics",
        "title": "Failure and integration semantics"
      },
      {
        "depth": 2,
        "id": "tests-and-modification-impact",
        "title": "Tests and modification impact"
      },
      {
        "depth": 2,
        "id": "current-limitations",
        "title": "Current limitations"
      }
    ],
    "searchText": "This crate owns the backend-neutral runtime for trusted Debug Adapter Protocol DAP stdio processes. Cross-boundary product behavior and current system status are documented in docs/debugging.md; this README is the canonical implementation contract for this crate. Ownership DebugAdapterService owns process lifetime, bounded output, DAP Content-Length framing, session identity, and cleanup. It requires both LoadExecutableConfiguration and ExecuteProcess capabilities for the same workspace root. It does not parse .vscode/launch.json, own breakpoints, implement DAP client state, authorize App Server connections, or render UI. Symbol Responsibility Must not own --- --- --- DebugAdapterService Validate active workspace capabilities and own at most eight adapter processes Product configuration or connection identity DebugAdapterCommand::new Bound program and arguments and reject NUL input Executable discovery readmessage / encodemessage Parse and create bounded DAP frames Request pairing or DAP semantics DebugAdapterState Retain at most 512 messages plus bounded stderr and exit state Durable history pushmessage Assign ordered sequence numbers and evict the oldest retained messages Consumer cursors refreshprocessstate / terminate Observe exit and reap or kill the child Restart policy The call path is DebugAdapterService::start → tokio::process::Command → spawnstdoutreader / spawnstderrreader. Callers send JSON through send; read returns the next bounded page and advances only past messages in that page. A consumer that falls behind the retained prefix receives outputgap = true and must fail the session instead of silently skipping protocol events. Failure and integration semantics Invalid commands and reads fail before mutation. Process launch, framing failure, broken pipes, and poisoned locks are explicit errors. close removes exactly one session and reaps its process; terminateall is the fail-safe for trust retirement and drop. App Server connection ownership is deliberately implemented by its debugservice::DebugAdapterService wrapper rather than this crate. The process environment is supplied by the caller and the runtime clears inherited variables before applying it. Expanding workspace variables and handling DAP reverse requests remain caller obligations. Tests and modification impact Run cargo test -p zeta-debug-adapter. Framing coverage lives in framingtests.rs; process/session tests should be added in sibling test files. Changes to messages or limits must be reviewed against App Server pagination and docs/debugging.md. Adding product configuration, UI state, connection IDs, or another process registry here would signal ownership drift. Current limitations The crate supports stdio adapters only. It does not implement socket/server adapters, durable session recovery, restart, telemetry, or language-specific adapter discovery."
  },
  {
    "slug": "crates/diff",
    "sourcePath": "zeta-rs/diff/README.md",
    "title": "zeta-diff",
    "description": "zeta-diff 计算原始文本和修改后文本之间可复现、可取消且有资源上限的代码差异。它拥有精确 换行符解析、Myers 最短编辑路径、替换配对、Unicode 字素级内联范围、Git 风格 hunk header、 比较策略和输入验证；不拥有文件读取、Git 进程、语法高亮、编辑器光标、终端渲染或 UI 生命周期。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是共享代码差异数据层的 crate-level canonical contract。`zeta-editor` 与 TUI\n> 只消费这里的行映射、字符范围和 hunk，不在各自 presentation 中重新实现匹配算法。Editor\n> presentation 的 canonical contract 见 [`editor/README.md`](../editor/README.md)。\n\n`zeta-diff` 计算原始文本和修改后文本之间可复现、可取消且有资源上限的代码差异。它拥有精确\n换行符解析、Myers 最短编辑路径、替换配对、Unicode 字素级内联范围、Git 风格 hunk header、\n比较策略和输入验证；不拥有文件读取、Git 进程、语法高亮、编辑器光标、终端渲染或 UI 生命周期。\n\n## 所有权与接口\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `DiffEngine` | public | 冻结一次 diff 的策略；提供 text/bytes 与 cancellable/non-cancellable 入口 |\n| `DiffDocument::from_text` | public | 用默认三行上下文、精确比较和默认 limits 构建映射 |\n| `DiffDocument::with_options` | public | 以显式 `DiffOptions` 构建映射 |\n| `DiffOptions` | public | 组合 context、whitespace、case、line-ending、inline 与 limits policy |\n| `DiffLimits` | public | 限制每侧 bytes/lines、最大 edit distance 和 Myers trace cells |\n| `DiffCancellation` / `NeverCancel` | public | 由 host 提供低成本取消探针；crate 不拥有 timer 或 task lifecycle |\n| `DiffLine` / `LineEnding` | public | 保留一基行号、line text 和 LF/CRLF/CR/EOF terminator |\n| `DiffRow` / `DiffRowKind` | public | 表达 Context、Added、Removed、Modified 及两侧行 correspondence |\n| `InlineChange` | public | 用 UTF-8 byte range 表达 grapheme-boundary 内联变化 |\n| `DiffHunk` | public | 表达 Git-style old/new start/count 与 document row 半开范围 |\n| `engine::split_lines` | private | 单次扫描 UTF-8，保留精确 line ending 并观察 cancellation |\n| `myers::edits` | private | 在 edit-distance/trace limits 内计算最短行或 grapheme 编辑路径 |\n| `engine::map_rows` | private | 把编辑路径配对为稳定行映射，并为 Modified 行计算 inline ranges |\n| `engine::group_hunks` | private | 根据 context 合并相邻变化并生成 Git-style header |\n\n```text\nDiffEngine::compute[_bytes][_cancellable]\n├─ input bytes / UTF-8 / NUL / byte-limit validation\n├─ split_lines → exact LineEnding\n├─ comparison_keys\n│  └─ whitespace / case / line-ending policy\n├─ myers::edits\n│  └─ cancellation + edit-distance + trace-memory checkpoints\n├─ map_rows\n│  └─ inline::changes → grapheme Myers → UTF-8 byte ranges\n└─ group_hunks → DiffDocument\n```\n\n## 精确语义\n\n- 行号从 1 开始；Git-style hunk 的 empty side 在文件开头可以使用 start `0`。\n- `Added` 没有 original line，`Removed` 没有 modified line；`Modified` 是同一连续 change run\n  中按顺序配对的删除/新增行，不表示语法级语义修改。\n- `LineEndingPolicy::Sensitive` 是默认值，因此 LF、CRLF、CR 与缺少末尾换行会参与匹配；\n  `Ignore` 只改变 equality，不丢弃 `DiffLine` 保存的真实 ending。\n- `WhitespacePolicy` 和 `CaseSensitivity` 只改变匹配 key；`DiffRow` 始终保留原始两侧文本。\n- `InlineChange` 的 range 使用 UTF-8 byte offset，边界保证落在 Unicode grapheme boundary；\n  insertion/deletion 的缺失侧使用 empty range。\n- `DiffHunk::row_start..row_end` 只能通过同一个 `DiffDocument::rows_for_hunk` 读取。\n\n## 失败、资源与接入义务\n\n`DiffEngine` 在分行、comparison-key 构建、line Myers 和 inline Myers 中观察\n`DiffCancellation`。Host 应提供 cheap、thread-safe 且在单次计算中单调的实现。默认 limits\n限制每侧 8 MiB、200,000 行、20,000 edit distance 和 8,000,000 trace cells；达到边界返回\n`InputTooLarge`、`TooManyLines`、`EditDistanceLimit` 或 `TraceLimit`，不能继续分配到 OOM。\n\nbytes API 拒绝 NUL、无效 UTF-8，并分别返回 `BinaryInput`、`InvalidUtf8`。本 crate 不猜测编码、\n不读取磁盘，也不调用 Git：\n\n- `zeta-editor` 用 `DiffDocument` 驱动 side-by-side DiffEditor，`zeterm/zeterm` 只负责宿主接入；\n- `zeta-tui` 用同一 document 驱动 Ratatui 行渲染、左右导航和折叠；\n- `zeta-git` 继续拥有 Git patch check/apply、仓库进程和路径解析；\n- `zeta-ui` 继续拥有 TabList、文字和矩形 primitive，不依赖本 crate。\n\n## 测试、修改影响与限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-diff\ncargo clippy --manifest-path Cargo.toml -p zeta-diff --all-targets -- -D warnings\n```\n\n测试覆盖精确 line ending、缺失 final newline、空白/大小写策略、Unicode Emoji 字素范围、\nGit-style fixture/header、纯插入/删除 anchor、NUL/UTF-8/byte limit、取消、complexity limit 和\n两侧文本重建。`myers_tests` 穷举长度 0–5 的 binary sequences，对照独立动态规划 reference，\n验证编辑脚本有序、可重建且 edit distance 最短。\n\n修改 `DiffRow` 或 hunk header 必须同步检查 `zeta-editor`/TUI projection；修改 comparison key 必须保持\nsource text/ending 不丢失；修改 Myers trace 必须保留取消和 memory bound；修改 inline range\n必须继续使用 UTF-8 grapheme boundary。\n\n当前仍不拥有语法 token、移动代码检测、二进制 diff、非 UTF-8 解码、增量复用或 file watcher。\n这些能力出现第二个真实消费者和独立 contract 后再扩展；不能让 Native/TUI presentation\n反向决定共享 diff 语义。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与接口",
        "title": "所有权与接口"
      },
      {
        "depth": 2,
        "id": "精确语义",
        "title": "精确语义"
      },
      {
        "depth": 2,
        "id": "失败资源与接入义务",
        "title": "失败、资源与接入义务"
      },
      {
        "depth": 2,
        "id": "测试修改影响与限制",
        "title": "测试、修改影响与限制"
      }
    ],
    "searchText": " 本 README 是共享代码差异数据层的 crate-level canonical contract。zeta-editor 与 TUI 只消费这里的行映射、字符范围和 hunk，不在各自 presentation 中重新实现匹配算法。Editor presentation 的 canonical contract 见 editor/README.md。 zeta-diff 计算原始文本和修改后文本之间可复现、可取消且有资源上限的代码差异。它拥有精确 换行符解析、Myers 最短编辑路径、替换配对、Unicode 字素级内联范围、Git 风格 hunk header、 比较策略和输入验证；不拥有文件读取、Git 进程、语法高亮、编辑器光标、终端渲染或 UI 生命周期。 所有权与接口 Symbol 可见性 精确职责 --- --- --- DiffEngine public 冻结一次 diff 的策略；提供 text/bytes 与 cancellable/non-cancellable 入口 DiffDocument::fromtext public 用默认三行上下文、精确比较和默认 limits 构建映射 DiffDocument::withoptions public 以显式 DiffOptions 构建映射 DiffOptions public 组合 context、whitespace、case、line-ending、inline 与 limits policy DiffLimits public 限制每侧 bytes/lines、最大 edit distance 和 Myers trace cells DiffCancellation / NeverCancel public 由 host 提供低成本取消探针；crate 不拥有 timer 或 task lifecycle DiffLine / LineEnding public 保留一基行号、line text 和 LF/CRLF/CR/EOF terminator DiffRow / DiffRowKind public 表达 Context、Added、Removed、Modified 及两侧行 correspondence InlineChange public 用 UTF-8 byte range 表达 grapheme-boundary 内联变化 DiffHunk public 表达 Git-style old/new start/count 与 document row 半开范围 engine::splitlines private 单次扫描 UTF-8，保留精确 line ending 并观察 cancellation myers::edits private 在 edit-distance/trace limits 内计算最短行或 grapheme 编辑路径 engine::maprows private 把编辑路径配对为稳定行映射，并为 Modified 行计算 inline ranges engine::grouphunks private 根据 context 合并相邻变化并生成 Git-style header ``text DiffEngine::compute bytes cancellable ├─ input bytes / UTF-8 / NUL / byte-limit validation ├─ splitlines → exact LineEnding ├─ comparisonkeys │ └─ whitespace / case / line-ending policy ├─ myers::edits │ └─ cancellation + edit-distance + trace-memory checkpoints ├─ maprows │ └─ inline::changes → grapheme Myers → UTF-8 byte ranges └─ grouphunks → DiffDocument ` 精确语义 - 行号从 1 开始；Git-style hunk 的 empty side 在文件开头可以使用 start 0。 - Added 没有 original line，Removed 没有 modified line；Modified 是同一连续 change run 中按顺序配对的删除/新增行，不表示语法级语义修改。 - LineEndingPolicy::Sensitive 是默认值，因此 LF、CRLF、CR 与缺少末尾换行会参与匹配； Ignore 只改变 equality，不丢弃 DiffLine 保存的真实 ending。 - WhitespacePolicy 和 CaseSensitivity 只改变匹配 key；DiffRow 始终保留原始两侧文本。 - InlineChange 的 range 使用 UTF-8 byte offset，边界保证落在 Unicode grapheme boundary； insertion/deletion 的缺失侧使用 empty range。 - DiffHunk::rowstart..rowend 只能通过同一个 DiffDocument::rowsforhunk 读取。 失败、资源与接入义务 DiffEngine 在分行、comparison-key 构建、line Myers 和 inline Myers 中观察 DiffCancellation。Host 应提供 cheap、thread-safe 且在单次计算中单调的实现。默认 limits 限制每侧 8 MiB、200,000 行、20,000 edit distance 和 8,000,000 trace cells；达到边界返回 InputTooLarge、TooManyLines、EditDistanceLimit 或 TraceLimit，不能继续分配到 OOM。 bytes API 拒绝 NUL、无效 UTF-8，并分别返回 BinaryInput、InvalidUtf8。本 crate 不猜测编码、 不读取磁盘，也不调用 Git： - zeta-editor 用 DiffDocument 驱动 side-by-side DiffEditor，zeterm/zeterm 只负责宿主接入； - zeta-tui 用同一 document 驱动 Ratatui 行渲染、左右导航和折叠； - zeta-git 继续拥有 Git patch check/apply、仓库进程和路径解析； - zeta-ui 继续拥有 TabList、文字和矩形 primitive，不依赖本 crate。 测试、修改影响与限制 `bash cargo test --manifest-path Cargo.toml -p zeta-diff cargo clippy --manifest-path Cargo.toml -p zeta-diff --all-targets -- -D warnings ` 测试覆盖精确 line ending、缺失 final newline、空白/大小写策略、Unicode Emoji 字素范围、 Git-style fixture/header、纯插入/删除 anchor、NUL/UTF-8/byte limit、取消、complexity limit 和 两侧文本重建。myerstests 穷举长度 0–5 的 binary sequences，对照独立动态规划 reference， 验证编辑脚本有序、可重建且 edit distance 最短。 修改 DiffRow 或 hunk header 必须同步检查 zeta-editor`/TUI projection；修改 comparison key 必须保持 source text/ending 不丢失；修改 Myers trace 必须保留取消和 memory bound；修改 inline range 必须继续使用 UTF-8 grapheme boundary。 当前仍不拥有语法 token、移动代码检测、二进制 diff、非 UTF-8 解码、增量复用或 file watcher。 这些能力出现第二个真实消费者和独立 contract 后再扩展；不能让 Native/TUI presentation 反向决定共享 diff 语义。"
  },
  {
    "slug": "crates/zeterm/editor",
    "sourcePath": "zeterm/editor/README.md",
    "title": "zeta-editor",
    "description": "zeta-editor 拥有 Native UI 使用的多行代码编辑模型、caret/selection、键盘命令、有界 undo/redo、IME composition、语法 token、结构折叠、viewport soft wrap、代码行与视口绘制，以及由两个代码视口组成的并排差异 展示、适合窄 surface 的单列 unified 差异展示，以及纵向组合多个文…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是代码编辑器与差异编辑器 presentation 的 crate-level canonical contract。\n> 跨 crate 的依赖与产品宿主边界见\n> [`docs/zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md)；`zeta-diff` 算法契约见\n> [`diff/README.md`](../diff/README.md)；异步结构分析与 revision binding 见\n> [`docs/syntax-analysis.md`](../../docs/syntax-analysis.md)；文件保存基线与冲突状态由\n> [`zeta-text-file`](../text-file/README.md) 独立拥有。共享 Rust document core 与跨运行时迁移边界见\n> [`docs/editor-core.md`](../../docs/editor-core.md)。\n\n`zeta-editor` 拥有 Native UI 使用的多行代码编辑模型、caret/selection、键盘命令、有界\nundo/redo、IME composition、语法 token、结构折叠、viewport soft wrap、代码行与视口绘制，以及由两个代码视口组成的并排差异\n展示、适合窄 surface 的单列 unified 差异展示，以及纵向组合多个文件差异的\n`MultiDiffEditor`。它不读取文件、不执行 Git、不计算 diff，也不拥有 changed-file collection、\n活动 EditorHost 或平台事件。\n\n## 所有权与接口\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `CodeEditor` | public | 绘制可见代码行、caret/selection、preedit、syntax token、gutter 与 fold control；拥有 fold-control geometry 和 hit test，`within_viewport` 限制嵌入式宿主实际投影的行 |\n| `CodeEditorPresentation` | public | 选择带 document chrome 的普通编辑器或隐藏 gutter 的 compact 嵌入式编辑器 |\n| `CodeEditorLineWrapping` / `CodeEditorNavigation` | public | 选择不换行或 viewport soft wrap，并把 presentation 解析出的显示列宽和可见行容量交给 document 的上下键与翻页导航 |\n| `CodeEditorDocument` | public | 拥有 Native 的语言、行 range、composition、syntax snapshot、fold/visible-row projection；committed text、selection、revision 与 undo/redo 委托 persistent `zeta-editor-core`，同步 text projection 仅供 Native 计算/绘制 |\n| `CodeEditorRevision` | public | `zeta-editor-core::EditorCoreRevision` 的 Native 名称；为宿主提供与文本 mutation 绑定的单调 revision，navigation 不推进，insert/replace/undo/redo 推进 |\n| `CodeEditorFoldingRange` / `CodeEditorFoldState` | public | 表达零基 source-row 结构范围及每个 document 实例独立的展开状态；start row 保留可见 |\n| `CodeEditorFoldControl` | public | 发布当前帧可见 gutter control 的 editor-owned range、state 与命中 bounds |\n| `CodeEditorCommand` | public | 表达插入、自动缩进换行、indent/outdent、语言声明的行注释、行复制/移动/删除空行/合并/插入/行尾空白清理/排序/反转/去重、Unicode navigation、选择、删除与 undo/redo |\n| `CodeEditorIndentation` | public | 以显式 tabs/spaces 与 tab width policy 驱动换行、Tab 与 Shift+Tab、leading-whitespace close delimiter auto-outdent；Enter 保留 document line ending，不把缩进策略放进 Native |\n| `CodeEditorSearchQuery` / `CodeEditorSearchMatch` | public | 表达大小写策略、byte range 与 editor position；前后循环查找、单次/全部替换由 document 执行 |\n| `CodeEditorDiagnostic` / `CodeEditorDiagnosticSeverity` | public | 表达 UTF-8 document byte range、severity、message/source/code；不暴露 LSP 类型 |\n| `CodeEditorDiagnosticPalette` | public | 由宿主把 error/warning/information/hint theme token 映射为编辑器语义颜色 |\n| `CodeEditorLanguage` | public | 让宿主选择 PlainText、Shell、JSON、JSONC 或 Rust；parser、tree、revision 与 token projection 保持私有 |\n| `CodeEditorPalette` / `CodeEditorSyntaxPalette` | public | 由宿主把 resolved theme token 映射为组件命名输入；换主题只重建 style，不重新分析文本 |\n| `CodeEditorRowSource` | public trait | 惰性提供稳定 visual row；普通文档和 diff projection 共用 |\n| `CodeEditorRow` | public | 表达真实代码行、对齐 placeholder 或无行号 annotation，以及本帧 decoration |\n| `CodeEditorViewport` | public | 保存首个可见行和横向显示列，并执行有界滚动或 reveal-row |\n| `CodeEditorStyle` | public | 拥有代码 surface、header、gutter、文本与 syntax role 的 resolved presentation style；`light()` 只是安全 fallback |\n| `DiffEditor` | public | 按 presentation 组合双列或单列 `CodeEditor`，同步纵向 viewport 并绘制 diff decoration |\n| `DiffEditorDocument` | public | 接收已计算 `DiffDocument` 与 language，内部持有 original/modified 两个 language-aware `CodeEditorDocument`；不向宿主暴露 parser/revision/token |\n| `DiffEditorPresentation` | public | 显式选择 `SideBySide` 或适合窄嵌入 surface 的 `Unified` geometry |\n| `DiffEditorState` | public | 保存共享首行、两侧独立横向显示列和 Unified 未修改区间的展开状态 |\n| `DiffEditorFoldControl` | public | 向产品宿主发布可见未修改区间的行数、状态与命中 bounds |\n| `MultiDiffEditor` | public | 把多个文件标题和 `DiffEditor` section 组合为一个纵向裁剪 surface |\n| `MultiDiffEditorItem` | public | 为一帧借用文件名、`DiffEditorDocument`、两侧标签和该文件的 `DiffEditorState`；产品 host 应通过 `with_identity` 提供稳定 changed-file identity |\n| `MultiDiffEditorItemIdentity` | public | 从 host-owned stable slot 派生 section/header/diff/fold `ElementId`，并提供折叠 section 的 `AnimationProperty::Height` key |\n| `MultiDiffEditorLayout` | public | 用 `zeta-ui::VirtualListLayout` 缓存精确 item/state/presentation snapshot 的可变 section heights、prefix index 与总内容高度，供高频滚动复用 |\n| `zeta-ui::ScrollState` | delegated | 保存 MultiDiffEditor 整体 logical-pixel offset；clamp 与 transition 由通用滚动基座执行 |\n| `MultiDiffEditorStyle` | public | 拥有文件 header、section 间距与嵌套 DiffEditor 样式 |\n| `DiffEditorPalette` / `MultiDiffEditorPalette` | public | 让产品宿主通过命名字段注入 diff marker/background、scrollbar 与文件 header 视觉 |\n| `DiffSideRows` | private | 把 `DiffEditorDocument` 的一侧惰性转换为带 editor-owned syntax token 的 `CodeEditorRow` |\n| `UnifiedDiffRows` | private | 用 hunk source-range、修改行索引和 fold segment 紧凑表达单列 diff；按可见 visual index 随机映射代码行，不按文件总行数分配 row 数组 |\n| `MultiDiffSection` / `MultiDiffFileHeader` | private | 在共享 `ComponentContext` 中拥有每个 changed file 的 card/header geometry，并把同一 section 的 `DiffEditor` 放入 inspection ancestry |\n| `MultiDiffFoldControl` / `MultiDiffScrollbar` | private | 将 editor-owned fold geometry 和 ScrollView scrollbar geometry 投影为真实组件及 interaction semantics；产品 host 不再手工注册这些节点 |\n| `code_editor::layout::build_layout` | private | 从组件 bounds 计算 header/body/gutter/content |\n| `code_editor::text_metrics::display_columns_until` | private | 把 UTF-8 byte offset 映射到 Tab/Unicode 等宽显示列 |\n| `code_editor::wrapping::CodeEditorVisualProjection` | private | 按 grapheme/Tab display column 把折叠后的 source row 投影成 viewport visual line，并执行 caret/pointer 映射 |\n| `code_editor::editing` | private module | 执行 grapheme-safe mutation、跨行导航、selection 与有界 history |\n| `code_editor::folding` | private module | 规范化 syntax fold、保留 collapsed identity，并执行 source-row / visual-row 双向投影 |\n| `code_editor::decorations` | private module | 绘制 selection、syntax、composition、caret 并计算 IME caret bounds |\n| `code_editor::diagnostics` | private module | 把 document byte range 投影到 source row 与 soft-wrapped visual line，绘制波浪线并执行文本命中 |\n| `diff_editor::layout::build_layout` | private | 计算两个 pane 与中央 divider |\n\n```text\nCodeEditorDocument ─┐\n                    ├─ CodeEditorRowSource::row\nDiffSideRows ───────┘\n                           ↓\nCodeEditor::paint\n├─ zui Element(CodeEditor bounds) → automatic inspection\n├─ code_editor::layout::build_layout\n├─ CodeEditor::visible_row_range\n├─ wrapping::CodeEditorVisualProjection → source row / wrapped visual line mapping\n├─ CodeEditor::painted_row_range → host viewport row culling\n├─ gutter / marker / line background\n├─ text_metrics::display_columns_until\n└─ UiScene\n\nCodeEditorDocument::apply\n├─ CodeEditorCommand\n├─ grapheme / CRLF boundary navigation\n├─ selection replacement\n├─ checkpoint → core selection synchronization\n├─ committed mutation → core bounded undo / redo + revision\n└─ reindex_lines → CodeEditorAnalysis::synchronize\n   └─ SyntaxDocument incremental edit\n      ├─ token → line-relative CodeEditorSyntaxToken\n      └─ folding range → CodeEditorFoldingProjection\n         └─ visible row mapping / gutter fold control\n\nCodeEditorDocument::apply_composition\n├─ Preedit → separate composition state\n├─ Commit → one undoable replacement\n└─ Cancel → committed text unchanged\n\nCodeEditorDocument::find_next / find_previous / replace_current / replace_all\n├─ CodeEditorSearchQuery → committed-text byte ranges\n├─ match → CodeEditorPosition + selection/reveal\n└─ replacement → one checkpoint per command\n\nDiffEditor::paint\n├─ SideBySide\n│  ├─ DiffEditorDocument.original syntax → DiffSideRows → CodeEditor\n│  ├─ DiffEditorDocument.modified syntax → DiffSideRows → CodeEditor\n│  └─ divider\n└─ Unified\n   └─ UnifiedDiffRows → one CodeEditor\n      ├─ context → one row\n      └─ modified → removed row + added row\n      └─ long unchanged range → fold control + optional source rows\n\nMultiDiffEditor::compose\n├─ ScrollView::draw_components\n│  ├─ viewport clip / content origin\n│  └─ hover/active/fading scrollbar chrome\n├─ VirtualListLayout::visible_range → prefix-height binary search\n├─ visible MultiDiffSection only\n│  ├─ MultiDiffFileHeader\n│  ├─ DiffEditor::compose\n│  │  └─ CodeEditor component(s)\n│  └─ MultiDiffFoldControl component(s)\n└─ MultiDiffScrollbar → shared scrollbar semantics\n\nMultiDiffEditor::paint remains a scene-only compatibility path for callers that have not adopted\n`ComponentContext`; it must not become the product interaction path. `EditorPane::compose` uses the\nshared path above so one frame owns the complete inspection and interaction hierarchy.\n```\n\n`CodeEditorRowSource` 实现必须在一个 presentation frame 内保持 row ordering 和 line-number\nidentity 稳定，并只为请求的可见 index 构造行。普通文档返回真实编号行；差异投影可以返回没有\nline number 的 placeholder 或 fold annotation，但不能把它们写入 authoritative document。行内高亮使用\nUTF-8 byte range；越界或不落在字符边界上的 range 会被忽略，不得造成绘制 panic。\nCode row 的底层 text run 仍使用 `TextBlockWrap::None`；启用 `CodeEditorLineWrapping::Soft` 时，\n`CodeEditorVisualProjection` 按可用 display column 建立视觉行并分别 clip/paint，而不会向 document\n插入换行。宽字符 grapheme 与 whitespace 按 8px display cell\n边界分别投影；ASCII run 对齐一个 cell，CJK fallback 对齐两个 cell，使 text、space、\nselection 与 caret 共用同一坐标系。语法 token 保存语义 role，不保存 RGB；paint 从当前\n`CodeEditorStyle` 解析颜色，所以 theme snapshot 变化不使 syntax snapshot 失效。\n普通文档折叠保留 start row，隐藏到 syntax range end row（含）之间的 source row；caret、selection\n和 pointer position 始终使用 source row，viewport 与 paint 使用折叠加 soft-wrap 后的 visual row。\n折叠与换行投影负责双向映射，\n所以宿主不维护 hidden-line offset。父范围展开后，仍然有效的嵌套 collapsed state 会恢复生效。\n\nUnified projection 以 `DiffDocument::hunks` 的间隙作为可折叠区间，只保存少量 source segment\n和 `Modified` source-row index。展开超大未修改区间不会物化等量 `CodeEditorRow`；可见行通过\n二分映射回 source row。`MultiDiffEditor` 在一个 presentation 实例内通过\n`VirtualListLayout` 缓存每文件 section height 与 prefix index；paint 和 fold-control 查询都先\n二分定位可见 section，不再从首个文件线性累加 offset。\n高频输入宿主可继续保留 `measure_layout` 结果，使 scroll metrics、fold-control geometry 和 paint\n跨 presentation rebuild 共用同一组高度。item、document、`DiffEditorState`、style 或 presentation\n变化时必须重新测量。\n\n## 执行、失败与宿主义务\n\n`CodeEditor`、`DiffEditor` 和 `MultiDiffEditor` 构造与绘制没有 I/O 或独立 error channel。编辑命令以 Unicode\ngrapheme boundary 修改 committed text；CRLF 在删除时作为一个换行边界处理。`zeta-editor-core` 为 Native\n保留最多 100 个完整 snapshot，新编辑会清空 redo；当前不合并连续输入。IME preedit 与 committed text\n分离，只有 Commit 建立一个可撤销 checkpoint，Cancel 不修改文档。`CodeEditor::caret_bounds`\n向平台宿主提供候选框锚点，`text_position_at` 与 `CodeEditorDocument::move_to` /\n`set_selection` 支持指针选择。`CodeEditorPresentation::Compact` 只改变共享组件拥有的\nheader/gutter geometry，不改变 document、editing 或 row-source contract。\n直接键入语言允许的单个开括号或引号会创建匹配的 close delimiter；已有选区时会包裹并保留选区。\n只有仍被 document 追踪为自动创建的 close delimiter 才允许 overtype 或双侧 Backspace，手写源码中的\n相同字符保持普通插入/删除语义；IME Commit 和多字符输入保持原样，避免把 composition 或 paste 误判为键入。\nRust/JSONC 使用 `//`、Shell 使用 `#` 的行注释命令由语言声明决定；JSON 明确不接受该命令。\n启用 soft wrap 的宿主应把 `CodeEditor::navigation()` 交给\n`CodeEditorDocument::apply_in_view`，并使用 `visual_row_count` / `caret_visual_row` 驱动 viewport；\n宿主不得重新计算换行边界。\n`CodeEditor::fold_control_at` 负责 gutter 命中，宿主把返回值原样交给\n`CodeEditorDocument::toggle_fold_control`；范围计算、stale-control 校验、状态迁移、隐藏行、光标修正\n和 fold glyph 都不属于宿主。\n\n三个公开 editor Component 都通过 `Component::element` 声明各自 zui leaf，resolved bounds 与源码\n位置由 `UiScene::draw_component` 自动写入 inspection frame；宿主必须使用\n`UiFrame::draw_component`/`ComponentContext::draw_component`，使\n`MultiDiffEditor → ScrollView → MultiDiffSection → DiffEditor → CodeEditor` 的可见组合链进入同一帧的\nlayout inspection hierarchy。`MultiDiffFoldControl` 与 `MultiDiffScrollbar` 也必须沿这条链生成，\n不能回到手工注册 `UiNode`。该 metadata 不改变 document、viewport 或输入 ownership；需要把自定义\npaint 与子组件放在一个宿主根下时，使用 `ComponentContext::with_component` 保持 inspector 与 interaction\n父链一致。\n\n`DiffDocument` 的输入验证、资源限制和取消在进入本 crate 前由 `zeta-diff` 完成。宿主随后用\n语言构造 retained `DiffEditorDocument`；组件只根据该不可变 editor snapshot 生成 `UiScene`。\n\n产品宿主必须：\n\n- 为每个文件保存独立的 document identity 与 editor viewport，并在创建 document 时选择\n  `CodeEditorLanguage`；\n- 为 `MultiDiffEditorItem` 提供已排序的 changed-file snapshot，并保留整体\n  `zeta-ui::ScrollState`；产品 host 必须为 changed file 分配稳定的\n  `MultiDiffEditorItemIdentity`，不能用当前列表 index 作为 retained 或 animation key；\n- 保存每文件 `DiffEditorState`，并用 `fold_controls` 发布的 identity 与 bounds 路由未修改区间\n  的展开/收起输入；\n- 对普通文档，用 `CodeEditor::fold_control_at` 转交 gutter 点击并调用\n  `CodeEditorDocument::toggle_fold_control`，不得自行计算结构范围或隐藏行；\n- 把滚轮、平台按键和 IME 事件转换为 `CodeEditorCommand` / `TextInputCompositionEvent`；\n- 用 `caret_bounds` 同步平台 IME candidate area，并由 host 控制 caret blink；\n- 在接入异步 diff 或 LSP 结果时丢弃不再匹配当前 document revision 的结果；CodeEditor 的\n  tree-sitter revision 不暴露给宿主。\n- 把 `zeta-theme` 或其他主题 runtime 的 snapshot 映射成公开 palette；本 crate 不读取主题文件，\n  也不依赖具体产品宿主。\n\n`zeterm/zeterm` 是当前 GPU presentation host；`zeta-tui` 不依赖本 crate，而是直接消费\n`zeta-diff` 并拥有自己的 Ratatui projection。\n\n## 测试、修改影响与限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-editor\ncargo clippy --manifest-path Cargo.toml -p zeta-editor --all-targets -- -D warnings\n```\n\n测试覆盖 LF/CRLF/CR 行索引、多行 Unicode 编辑、跨行列导航、选择替换、有界撤销/重做、IME\n预编辑与提交、语法前景 token、结构折叠、source/visual row 映射、viewport soft wrap、\nwrapped pointer/caret/vertical navigation、fold-control hit test、折叠后\ncaret/selection/navigation、viewport、Tab/Unicode 列宽和\n无效 UTF-8 range，DiffEditor 的双 pane 映射、Unified 单列映射、字符级高亮和长未修改区间\n展开/收起，以及 MultiDiffEditor 的可变高度多文件 section、prefix-index viewport 定位、\n每文件 fold-control identity、整体纵向滚动与\n超大单文件的紧凑行映射与 row-level viewport culling。\n\n修改 `CodeEditorRowSource` 或 `CodeEditorRow` 必须同步检查普通文档和 `DiffSideRows`；修改显示列\n计算必须同步检查 text、horizontal scroll 与 inline decoration；修改 CodeEditor layout 必须同步\n检查 DiffEditor 两侧 geometry 和 pointer mapping。若 DiffEditor 再次直接绘制代码行、行号或\n字符范围，表示共享 CodeEditor ownership 已经漂移。若 `MultiDiffEditor` 绕过 `DiffEditor`\n直接生成左右代码行，或 `EditorPane` 重新手工注册 `MultiDiffSection`/fold/scrollbar interaction\nnodes，表示多文件组合层的 ownership 已经漂移。\n\n当前 history 使用完整 snapshot 且不做 typing coalescing；Native CodeEditor 的 tree-sitter 分析\n与文本 mutation 同步执行，大文件尚未迁移到 editor-owned worker。多光标、\n缩进和语言注释的 `#region` / `#endregion` 会生成编辑器自有的 folding candidates；多行 selection\n可通过 `ToggleManualFoldSelection` 生成临时 manual fold，任何文本 mutation 都会将它移除。\nminimap 尚未完成。`CodeEditorTextEdit` 允许产品 adapter 通过精确 UTF-8\nrange 使用同一 undo/revision/analysis mutation path。Diagnostics 已支持 revision 外部绑定、severity\n波浪线、跨行/soft-wrap 投影和 hover hit-test；文件、tabs、持久化与产品级\nEditorHost 明确不属于本 crate；Native `FileEditorHost` 已负责对应组合。普通 CodeEditor 的 syntax\n结构折叠与 Unified DiffEditor 的未修改区间折叠是两套独立语义，分别保存在各自 document/state 中。\nMultiDiffEditor section\n高度随 diff row 数量增长，并用可变高度 prefix index 二分剔除完全不可见的文件；部分可见的\n超大单文件只投影外层 viewport 内的行。\nsection/fold/scrollbar 的组件组合已完成；SCM host 现在为 changed file 分配稳定 identity，section、header、\ndiff body、fold control 和折叠高度 animation key 都从该 identity 派生。`MultiDiffEditor::fold_element_id`\n仍保留旧的 index/region 编码，供未迁移的 standalone caller 兼容。SCM host 已把目标 section height 接入\nretained `AnimationBinding`；高度会影响后续 section 的位置，因此使用 `FrameInvalidation::Rebuild`，并由 runtime\n按 section identity 保持 transition 连续性与 track cleanup。动画对象仍不属于 editor 组件。\nDiffEditor 已维护 original/modified 两侧的 language 与 syntax\nsnapshot；当前同步 parse 仍需以后基于大文件 profile 决定是否迁入 editor-owned worker。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与接口",
        "title": "所有权与接口"
      },
      {
        "depth": 2,
        "id": "执行失败与宿主义务",
        "title": "执行、失败与宿主义务"
      },
      {
        "depth": 2,
        "id": "测试修改影响与限制",
        "title": "测试、修改影响与限制"
      }
    ],
    "searchText": " 本 README 是代码编辑器与差异编辑器 presentation 的 crate-level canonical contract。 跨 crate 的依赖与产品宿主边界见 docs/zeta-rs-architecture.md；zeta-diff 算法契约见 diff/README.md；异步结构分析与 revision binding 见 docs/syntax-analysis.md；文件保存基线与冲突状态由 zeta-text-file 独立拥有。共享 Rust document core 与跨运行时迁移边界见 docs/editor-core.md。 zeta-editor 拥有 Native UI 使用的多行代码编辑模型、caret/selection、键盘命令、有界 undo/redo、IME composition、语法 token、结构折叠、viewport soft wrap、代码行与视口绘制，以及由两个代码视口组成的并排差异 展示、适合窄 surface 的单列 unified 差异展示，以及纵向组合多个文件差异的 MultiDiffEditor。它不读取文件、不执行 Git、不计算 diff，也不拥有 changed-file collection、 活动 EditorHost 或平台事件。 所有权与接口 Symbol 可见性 精确职责 --- --- --- CodeEditor public 绘制可见代码行、caret/selection、preedit、syntax token、gutter 与 fold control；拥有 fold-control geometry 和 hit test，withinviewport 限制嵌入式宿主实际投影的行 CodeEditorPresentation public 选择带 document chrome 的普通编辑器或隐藏 gutter 的 compact 嵌入式编辑器 CodeEditorLineWrapping / CodeEditorNavigation public 选择不换行或 viewport soft wrap，并把 presentation 解析出的显示列宽和可见行容量交给 document 的上下键与翻页导航 CodeEditorDocument public 拥有 Native 的语言、行 range、composition、syntax snapshot、fold/visible-row projection；committed text、selection、revision 与 undo/redo 委托 persistent zeta-editor-core，同步 text projection 仅供 Native 计算/绘制 CodeEditorRevision public zeta-editor-core::EditorCoreRevision 的 Native 名称；为宿主提供与文本 mutation 绑定的单调 revision，navigation 不推进，insert/replace/undo/redo 推进 CodeEditorFoldingRange / CodeEditorFoldState public 表达零基 source-row 结构范围及每个 document 实例独立的展开状态；start row 保留可见 CodeEditorFoldControl public 发布当前帧可见 gutter control 的 editor-owned range、state 与命中 bounds CodeEditorCommand public 表达插入、自动缩进换行、indent/outdent、语言声明的行注释、行复制/移动/删除空行/合并/插入/行尾空白清理/排序/反转/去重、Unicode navigation、选择、删除与 undo/redo CodeEditorIndentation public 以显式 tabs/spaces 与 tab width policy 驱动换行、Tab 与 Shift+Tab、leading-whitespace close delimiter auto-outdent；Enter 保留 document line ending，不把缩进策略放进 Native CodeEditorSearchQuery / CodeEditorSearchMatch public 表达大小写策略、byte range 与 editor position；前后循环查找、单次/全部替换由 document 执行 CodeEditorDiagnostic / CodeEditorDiagnosticSeverity public 表达 UTF-8 document byte range、severity、message/source/code；不暴露 LSP 类型 CodeEditorDiagnosticPalette public 由宿主把 error/warning/information/hint theme token 映射为编辑器语义颜色 CodeEditorLanguage public 让宿主选择 PlainText、Shell、JSON、JSONC 或 Rust；parser、tree、revision 与 token projection 保持私有 CodeEditorPalette / CodeEditorSyntaxPalette public 由宿主把 resolved theme token 映射为组件命名输入；换主题只重建 style，不重新分析文本 CodeEditorRowSource public trait 惰性提供稳定 visual row；普通文档和 diff projection 共用 CodeEditorRow public 表达真实代码行、对齐 placeholder 或无行号 annotation，以及本帧 decoration CodeEditorViewport public 保存首个可见行和横向显示列，并执行有界滚动或 reveal-row CodeEditorStyle public 拥有代码 surface、header、gutter、文本与 syntax role 的 resolved presentation style；light 只是安全 fallback DiffEditor public 按 presentation 组合双列或单列 CodeEditor，同步纵向 viewport 并绘制 diff decoration DiffEditorDocument public 接收已计算 DiffDocument 与 language，内部持有 original/modified 两个 language-aware CodeEditorDocument；不向宿主暴露 parser/revision/token DiffEditorPresentation public 显式选择 SideBySide 或适合窄嵌入 surface 的 Unified geometry DiffEditorState public 保存共享首行、两侧独立横向显示列和 Unified 未修改区间的展开状态 DiffEditorFoldControl public 向产品宿主发布可见未修改区间的行数、状态与命中 bounds MultiDiffEditor public 把多个文件标题和 DiffEditor section 组合为一个纵向裁剪 surface MultiDiffEditorItem public 为一帧借用文件名、DiffEditorDocument、两侧标签和该文件的 DiffEditorState；产品 host 应通过 withidentity 提供稳定 changed-file identity MultiDiffEditorItemIdentity public 从 host-owned stable slot 派生 section/header/diff/fold ElementId，并提供折叠 section 的 AnimationProperty::Height key MultiDiffEditorLayout public 用 zeta-ui::VirtualListLayout 缓存精确 item/state/presentation snapshot 的可变 section heights、prefix index 与总内容高度，供高频滚动复用 zeta-ui::ScrollState delegated 保存 MultiDiffEditor 整体 logical-pixel offset；clamp 与 transition 由通用滚动基座执行 MultiDiffEditorStyle public 拥有文件 header、section 间距与嵌套 DiffEditor 样式 DiffEditorPalette / MultiDiffEditorPalette public 让产品宿主通过命名字段注入 diff marker/background、scrollbar 与文件 header 视觉 DiffSideRows private 把 DiffEditorDocument 的一侧惰性转换为带 editor-owned syntax token 的 CodeEditorRow UnifiedDiffRows private 用 hunk source-range、修改行索引和 fold segment 紧凑表达单列 diff；按可见 visual index 随机映射代码行，不按文件总行数分配 row 数组 MultiDiffSection / MultiDiffFileHeader private 在共享 ComponentContext 中拥有每个 changed file 的 card/header geometry，并把同一 section 的 DiffEditor 放入 inspection ancestry MultiDiffFoldControl / MultiDiffScrollbar private 将 editor-owned fold geometry 和 ScrollView scrollbar geometry 投影为真实组件及 interaction semantics；产品 host 不再手工注册这些节点 codeeditor::layout::buildlayout private 从组件 bounds 计算 header/body/gutter/content codeeditor::textmetrics::displaycolumnsuntil private 把 UTF-8 byte offset 映射到 Tab/Unicode 等宽显示列 codeeditor::wrapping::CodeEditorVisualProjection private 按 grapheme/Tab display column 把折叠后的 source row 投影成 viewport visual line，并执行 caret/pointer 映射 codeeditor::editing private module 执行 grapheme-safe mutation、跨行导航、selection 与有界 history codeeditor::folding private module 规范化 syntax fold、保留 collapsed identity，并执行 source-row / visual-row 双向投影 codeeditor::decorations private module 绘制 selection、syntax、composition、caret 并计算 IME caret bounds codeeditor::diagnostics private module 把 document byte range 投影到 source row 与 soft-wrapped visual line，绘制波浪线并执行文本命中 diffeditor::layout::buildlayout private 计算两个 pane 与中央 divider ``text CodeEditorDocument ─┐ ├─ CodeEditorRowSource::row DiffSideRows ───────┘ ↓ CodeEditor::paint ├─ zui Element CodeEditor bounds → automatic inspection ├─ codeeditor::layout::buildlayout ├─ CodeEditor::visiblerowrange ├─ wrapping::CodeEditorVisualProjection → source row / wrapped visual line mapping ├─ CodeEditor::paintedrowrange → host viewport row culling ├─ gutter / marker / line background ├─ textmetrics::displaycolumnsuntil └─ UiScene CodeEditorDocument::apply ├─ CodeEditorCommand ├─ grapheme / CRLF boundary navigation ├─ selection replacement ├─ checkpoint → core selection synchronization ├─ committed mutation → core bounded undo / redo + revision └─ reindexlines → CodeEditorAnalysis::synchronize └─ SyntaxDocument incremental edit ├─ token → line-relative CodeEditorSyntaxToken └─ folding range → CodeEditorFoldingProjection └─ visible row mapping / gutter fold control CodeEditorDocument::applycomposition ├─ Preedit → separate composition state ├─ Commit → one undoable replacement └─ Cancel → committed text unchanged CodeEditorDocument::findnext / findprevious / replacecurrent / replaceall ├─ CodeEditorSearchQuery → committed-text byte ranges ├─ match → CodeEditorPosition + selection/reveal └─ replacement → one checkpoint per command DiffEditor::paint ├─ SideBySide │ ├─ DiffEditorDocument.original syntax → DiffSideRows → CodeEditor │ ├─ DiffEditorDocument.modified syntax → DiffSideRows → CodeEditor │ └─ divider └─ Unified └─ UnifiedDiffRows → one CodeEditor ├─ context → one row └─ modified → removed row + added row └─ long unchanged range → fold control + optional source rows MultiDiffEditor::compose ├─ ScrollView::drawcomponents │ ├─ viewport clip / content origin │ └─ hover/active/fading scrollbar chrome ├─ VirtualListLayout::visiblerange → prefix-height binary search ├─ visible MultiDiffSection only │ ├─ MultiDiffFileHeader │ ├─ DiffEditor::compose │ │ └─ CodeEditor component s │ └─ MultiDiffFoldControl component s └─ MultiDiffScrollbar → shared scrollbar semantics MultiDiffEditor::paint remains a scene-only compatibility path for callers that have not adopted ComponentContext; it must not become the product interaction path. EditorPane::compose uses the shared path above so one frame owns the complete inspection and interaction hierarchy. ` CodeEditorRowSource 实现必须在一个 presentation frame 内保持 row ordering 和 line-number identity 稳定，并只为请求的可见 index 构造行。普通文档返回真实编号行；差异投影可以返回没有 line number 的 placeholder 或 fold annotation，但不能把它们写入 authoritative document。行内高亮使用 UTF-8 byte range；越界或不落在字符边界上的 range 会被忽略，不得造成绘制 panic。 Code row 的底层 text run 仍使用 TextBlockWrap::None；启用 CodeEditorLineWrapping::Soft 时， CodeEditorVisualProjection 按可用 display column 建立视觉行并分别 clip/paint，而不会向 document 插入换行。宽字符 grapheme 与 whitespace 按 8px display cell 边界分别投影；ASCII run 对齐一个 cell，CJK fallback 对齐两个 cell，使 text、space、 selection 与 caret 共用同一坐标系。语法 token 保存语义 role，不保存 RGB；paint 从当前 CodeEditorStyle 解析颜色，所以 theme snapshot 变化不使 syntax snapshot 失效。 普通文档折叠保留 start row，隐藏到 syntax range end row（含）之间的 source row；caret、selection 和 pointer position 始终使用 source row，viewport 与 paint 使用折叠加 soft-wrap 后的 visual row。 折叠与换行投影负责双向映射， 所以宿主不维护 hidden-line offset。父范围展开后，仍然有效的嵌套 collapsed state 会恢复生效。 Unified projection 以 DiffDocument::hunks 的间隙作为可折叠区间，只保存少量 source segment 和 Modified source-row index。展开超大未修改区间不会物化等量 CodeEditorRow；可见行通过 二分映射回 source row。MultiDiffEditor 在一个 presentation 实例内通过 VirtualListLayout 缓存每文件 section height 与 prefix index；paint 和 fold-control 查询都先 二分定位可见 section，不再从首个文件线性累加 offset。 高频输入宿主可继续保留 measurelayout 结果，使 scroll metrics、fold-control geometry 和 paint 跨 presentation rebuild 共用同一组高度。item、document、DiffEditorState、style 或 presentation 变化时必须重新测量。 执行、失败与宿主义务 CodeEditor、DiffEditor 和 MultiDiffEditor 构造与绘制没有 I/O 或独立 error channel。编辑命令以 Unicode grapheme boundary 修改 committed text；CRLF 在删除时作为一个换行边界处理。zeta-editor-core 为 Native 保留最多 100 个完整 snapshot，新编辑会清空 redo；当前不合并连续输入。IME preedit 与 committed text 分离，只有 Commit 建立一个可撤销 checkpoint，Cancel 不修改文档。CodeEditor::caretbounds 向平台宿主提供候选框锚点，textpositionat 与 CodeEditorDocument::moveto / setselection 支持指针选择。CodeEditorPresentation::Compact 只改变共享组件拥有的 header/gutter geometry，不改变 document、editing 或 row-source contract。 直接键入语言允许的单个开括号或引号会创建匹配的 close delimiter；已有选区时会包裹并保留选区。 只有仍被 document 追踪为自动创建的 close delimiter 才允许 overtype 或双侧 Backspace，手写源码中的 相同字符保持普通插入/删除语义；IME Commit 和多字符输入保持原样，避免把 composition 或 paste 误判为键入。 Rust/JSONC 使用 //、Shell 使用 的行注释命令由语言声明决定；JSON 明确不接受该命令。 启用 soft wrap 的宿主应把 CodeEditor::navigation 交给 CodeEditorDocument::applyinview，并使用 visualrowcount / caretvisualrow 驱动 viewport； 宿主不得重新计算换行边界。 CodeEditor::foldcontrolat 负责 gutter 命中，宿主把返回值原样交给 CodeEditorDocument::togglefoldcontrol；范围计算、stale-control 校验、状态迁移、隐藏行、光标修正 和 fold glyph 都不属于宿主。 三个公开 editor Component 都通过 Component::element 声明各自 zui leaf，resolved bounds 与源码 位置由 UiScene::drawcomponent 自动写入 inspection frame；宿主必须使用 UiFrame::drawcomponent/ComponentContext::drawcomponent，使 MultiDiffEditor → ScrollView → MultiDiffSection → DiffEditor → CodeEditor 的可见组合链进入同一帧的 layout inspection hierarchy。MultiDiffFoldControl 与 MultiDiffScrollbar 也必须沿这条链生成， 不能回到手工注册 UiNode。该 metadata 不改变 document、viewport 或输入 ownership；需要把自定义 paint 与子组件放在一个宿主根下时，使用 ComponentContext::withcomponent 保持 inspector 与 interaction 父链一致。 DiffDocument 的输入验证、资源限制和取消在进入本 crate 前由 zeta-diff 完成。宿主随后用 语言构造 retained DiffEditorDocument；组件只根据该不可变 editor snapshot 生成 UiScene。 产品宿主必须： - 为每个文件保存独立的 document identity 与 editor viewport，并在创建 document 时选择 CodeEditorLanguage； - 为 MultiDiffEditorItem 提供已排序的 changed-file snapshot，并保留整体 zeta-ui::ScrollState；产品 host 必须为 changed file 分配稳定的 MultiDiffEditorItemIdentity，不能用当前列表 index 作为 retained 或 animation key； - 保存每文件 DiffEditorState，并用 foldcontrols 发布的 identity 与 bounds 路由未修改区间 的展开/收起输入； - 对普通文档，用 CodeEditor::foldcontrolat 转交 gutter 点击并调用 CodeEditorDocument::togglefoldcontrol，不得自行计算结构范围或隐藏行； - 把滚轮、平台按键和 IME 事件转换为 CodeEditorCommand / TextInputCompositionEvent； - 用 caretbounds 同步平台 IME candidate area，并由 host 控制 caret blink； - 在接入异步 diff 或 LSP 结果时丢弃不再匹配当前 document revision 的结果；CodeEditor 的 tree-sitter revision 不暴露给宿主。 - 把 zeta-theme 或其他主题 runtime 的 snapshot 映射成公开 palette；本 crate 不读取主题文件， 也不依赖具体产品宿主。 zeterm/zeterm 是当前 GPU presentation host；zeta-tui 不依赖本 crate，而是直接消费 zeta-diff 并拥有自己的 Ratatui projection。 测试、修改影响与限制 `bash cargo test --manifest-path Cargo.toml -p zeta-editor cargo clippy --manifest-path Cargo.toml -p zeta-editor --all-targets -- -D warnings ` 测试覆盖 LF/CRLF/CR 行索引、多行 Unicode 编辑、跨行列导航、选择替换、有界撤销/重做、IME 预编辑与提交、语法前景 token、结构折叠、source/visual row 映射、viewport soft wrap、 wrapped pointer/caret/vertical navigation、fold-control hit test、折叠后 caret/selection/navigation、viewport、Tab/Unicode 列宽和 无效 UTF-8 range，DiffEditor 的双 pane 映射、Unified 单列映射、字符级高亮和长未修改区间 展开/收起，以及 MultiDiffEdit"
  },
  {
    "slug": "crates/editor-core",
    "sourcePath": "zeta-rs/editor-core/README.md",
    "title": "zeta-editor-core",
    "description": "zeta-editor-core 拥有 revision-bound 的文本事务、多选区值、UTF-16 offset 边界与有界 undo/redo。 它不依赖 zui、zeta-ui、Native、DOM、文件、语法解析器或 IPC transport。当前真实消费者是 Native zeta-editor；Zeta Alpha 拥有独立的 TypeScript Te…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有纯 Rust 编辑器核心的实现契约。跨运行时的分层、前端服务边界与迁移阶段由\n> [`docs/editor-core.md`](../../docs/editor-core.md) 规范；Native presentation 的当前能力见\n> [`zeta-editor`](../editor/README.md)。\n\n`zeta-editor-core` 拥有 revision-bound 的文本事务、多选区值、UTF-16 offset 边界与有界 undo/redo。\n它不依赖 `zui`、`zeta-ui`、Native、DOM、文件、语法解析器或 IPC transport。当前真实消费者是 Native\n`zeta-editor`；Zeta Alpha 拥有独立的 TypeScript `TextModel`，不依赖本 crate。\n\n## 当前 API 与调用路径\n\n| Public API | 职责 | 不负责 |\n| --- | --- | --- |\n| `EditorCoreDocument` | 保存文本、selection、revision 与 bounded history | DOM、绘制、文件保存、语法结果 |\n| `EditorCoreTransaction` | 将同一旧 revision 上的 edit 和 post-selection 原子提交 | command/keybinding 解释 |\n| `EditorCoreUtf16Offset` | 固定 Browser-compatible UTF-16 code-unit 语义 | UTF-8 byte offset 暴露 |\n| `EditorCoreDocumentSnapshot` | 在显式同步点复制文本、revision 与 selections | viewport / line rendering |\n| `EditorCoreRevision` | 跨 adapter 共用的单调 revision value | 将 selection-only 变化误报为文本 mutation |\n| `EditorCoreEditError` | 报告 stale revision、无效 surrogate boundary、重叠 edit | transport error mapping |\n\n```text\nadapter input\n  → EditorCoreTransaction(base_revision, edits, selections)\n  → EditorCoreDocument::apply_transaction\n     → apply_edits (validate all UTF-16 ranges before mutation)\n     → validate_selections (against resulting text)\n     → checkpoint / revision advance\n  → EditorCoreDocumentSnapshot\n```\n\n`apply_edits` 接受同一旧文本上的无重叠 edit，排序后从后向前应用；调用者的输入顺序不影响结果。\noffset 落入代理对（surrogate pair）中间、edit 重叠、同起点 edit 或 stale revision 会拒绝整个事务，\n不改变文本、selection、history 或 revision。无文本变化的 transaction 可以更新 selection，但不推进\nrevision，也不建立 history 项。\n\n即使 DTO 经 JSON transport 反序列化而绕过 Rust 构造函数，`apply_transaction` 仍验证 ordered range\n和 non-empty/valid-primary selection set；不可信 payload 只会返回 `EditorCoreEditError`，不会让 document\n进入无效状态或触发 `replace_range` panic。\n\n`undo` 与 `redo` 恢复 text 和 selection，并各自推进 revision，使异步 adapter 能丢弃旧结果。\n`apply_transaction_with_history` 额外接受显式 `EditorCoreHistoryMerge`：Native command adapter 可以把已经判定为同一\ntyping 或 composition group 的连续 transaction 合并为一个 undo step；core 保留最早 pre-transaction snapshot。当前 history\n仅按 transaction 数量上限裁剪，默认保留 1,000 项；文本单位预算、tracked ranges、syntax 和 command semantics\n都是后续阶段，不应由 adapter 私自补出第二套文本 history。\n\nCurrent Native `CodeEditorDocument` 以 persistent `EditorCoreDocument` 持有 committed text、selection、\nrevision 与 history，并有 exact-edit/undo/redo conformance test。Native 保留同步 text projection，供现有\nline index、syntax、folding 与绘制读取；它不是第二个 history/revision owner。它还可通过\n`apply_core_transaction` 消费一个 single-selection `EditorCoreTransaction`：core 验证 UTF-16 transaction，\nNative 只投影 byte offset 并刷新 syntax/folding。multi-selection transaction 必须显式拒绝，直到 Native\npresentation 真正支持多 caret。\n\n## 修改影响与验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-editor-core\ncargo clippy --manifest-path Cargo.toml -p zeta-editor-core --all-targets -- -D warnings\n```\n\n修改 UTF-16 mapping、transaction validation 或 history 时必须同步检查 `document_tests.rs`。如果该 crate\n开始依赖 `zui`、DOM、文件 service、App Server transport，或让 adapter 把未校验 byte offset 传入 core，\n说明所有权已经漂移。\n\n## 当前限制与演进\n\nCurrent：Native `CodeEditorDocument` 是唯一产品消费者，并以同步 Rust 调用组合本 crate。Alpha 的 TypeScript\n`TextModel`、history、selection 与 tracked ranges 是另一个运行时边界，不通过 WASM、App Server 或 shadow document\n调用本 crate。Proposed：只有第二个真实 Rust consumer 出现后，才按其同步 Rust 调用面提取额外 adapter；不得为\nBrowser hot path 预建 transport。",
    "headings": [
      {
        "depth": 2,
        "id": "当前-api-与调用路径",
        "title": "当前 API 与调用路径"
      },
      {
        "depth": 2,
        "id": "修改影响与验证",
        "title": "修改影响与验证"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 拥有纯 Rust 编辑器核心的实现契约。跨运行时的分层、前端服务边界与迁移阶段由 docs/editor-core.md 规范；Native presentation 的当前能力见 zeta-editor。 zeta-editor-core 拥有 revision-bound 的文本事务、多选区值、UTF-16 offset 边界与有界 undo/redo。 它不依赖 zui、zeta-ui、Native、DOM、文件、语法解析器或 IPC transport。当前真实消费者是 Native zeta-editor；Zeta Alpha 拥有独立的 TypeScript TextModel，不依赖本 crate。 当前 API 与调用路径 Public API 职责 不负责 --- --- --- EditorCoreDocument 保存文本、selection、revision 与 bounded history DOM、绘制、文件保存、语法结果 EditorCoreTransaction 将同一旧 revision 上的 edit 和 post-selection 原子提交 command/keybinding 解释 EditorCoreUtf16Offset 固定 Browser-compatible UTF-16 code-unit 语义 UTF-8 byte offset 暴露 EditorCoreDocumentSnapshot 在显式同步点复制文本、revision 与 selections viewport / line rendering EditorCoreRevision 跨 adapter 共用的单调 revision value 将 selection-only 变化误报为文本 mutation EditorCoreEditError 报告 stale revision、无效 surrogate boundary、重叠 edit transport error mapping ``text adapter input → EditorCoreTransaction baserevision, edits, selections → EditorCoreDocument::applytransaction → applyedits validate all UTF-16 ranges before mutation → validateselections against resulting text → checkpoint / revision advance → EditorCoreDocumentSnapshot ` applyedits 接受同一旧文本上的无重叠 edit，排序后从后向前应用；调用者的输入顺序不影响结果。 offset 落入代理对（surrogate pair）中间、edit 重叠、同起点 edit 或 stale revision 会拒绝整个事务， 不改变文本、selection、history 或 revision。无文本变化的 transaction 可以更新 selection，但不推进 revision，也不建立 history 项。 即使 DTO 经 JSON transport 反序列化而绕过 Rust 构造函数，applytransaction 仍验证 ordered range 和 non-empty/valid-primary selection set；不可信 payload 只会返回 EditorCoreEditError，不会让 document 进入无效状态或触发 replacerange panic。 undo 与 redo 恢复 text 和 selection，并各自推进 revision，使异步 adapter 能丢弃旧结果。 applytransactionwithhistory 额外接受显式 EditorCoreHistoryMerge：Native command adapter 可以把已经判定为同一 typing 或 composition group 的连续 transaction 合并为一个 undo step；core 保留最早 pre-transaction snapshot。当前 history 仅按 transaction 数量上限裁剪，默认保留 1,000 项；文本单位预算、tracked ranges、syntax 和 command semantics 都是后续阶段，不应由 adapter 私自补出第二套文本 history。 Current Native CodeEditorDocument 以 persistent EditorCoreDocument 持有 committed text、selection、 revision 与 history，并有 exact-edit/undo/redo conformance test。Native 保留同步 text projection，供现有 line index、syntax、folding 与绘制读取；它不是第二个 history/revision owner。它还可通过 applycoretransaction 消费一个 single-selection EditorCoreTransaction：core 验证 UTF-16 transaction， Native 只投影 byte offset 并刷新 syntax/folding。multi-selection transaction 必须显式拒绝，直到 Native presentation 真正支持多 caret。 修改影响与验证 `bash cargo test --manifest-path Cargo.toml -p zeta-editor-core cargo clippy --manifest-path Cargo.toml -p zeta-editor-core --all-targets -- -D warnings ` 修改 UTF-16 mapping、transaction validation 或 history 时必须同步检查 documenttests.rs。如果该 crate 开始依赖 zui、DOM、文件 service、App Server transport，或让 adapter 把未校验 byte offset 传入 core， 说明所有权已经漂移。 当前限制与演进 Current：Native CodeEditorDocument 是唯一产品消费者，并以同步 Rust 调用组合本 crate。Alpha 的 TypeScript TextModel`、history、selection 与 tracked ranges 是另一个运行时边界，不通过 WASM、App Server 或 shadow document 调用本 crate。Proposed：只有第二个真实 Rust consumer 出现后，才按其同步 Rust 调用面提取额外 adapter；不得为 Browser hot path 预建 transport。"
  },
  {
    "slug": "crates/editor-extension-host",
    "sourcePath": "zeta-rs/editor-extension-host/README.md",
    "title": "zeta-editor-extension-host",
    "description": "zeta-editor-extension-host 监管一个已经由上层解析和授权的扩展程序。每个 ExtensionHostSupervisor 最多拥有一个扩展的一个活动进程 incarnation，通过有界 JSONL Host RPC v1 完成握手、激活、调用、取消、停用和关闭，并在授权仍有效时按有界策略恢复崩溃进程。它不发现或 安装 Plugin，不选择 ac…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Zeta 原生可执行 Editor Extension Host v1 的进程、RPC、授权门禁、取消与故障恢复\n> 实现权威文档。跨 Plugin、Workspace、App Server 和 Workbench 的产品语义由\n> [`docs/editor-extensions.md`](../../docs/editor-extensions.md) 维护；安装、启用、授权与包身份由\n> [`zeta-rs/plugins/README.md`](../plugins/README.md) 维护。\n\n`zeta-editor-extension-host` 监管一个已经由上层解析和授权的扩展程序。每个\n`ExtensionHostSupervisor` 最多拥有一个扩展的一个活动进程 incarnation，通过有界 JSONL Host RPC v1\n完成握手、激活、调用、取消、停用和关闭，并在授权仍有效时按有界策略恢复崩溃进程。它不发现或\n安装 Plugin，不选择 activation event，不实现 Workbench provider，也不兼容 VS Code Node Extension\nAPI。\n\n## 1. Crate 边界\n\n| 能力 | 本 crate 的职责 | 上层或平台职责 |\n| --- | --- | --- |\n| Package binding | 接收并绑定 `package_id`、digest、entrypoint 与 activation generation | Plugin authority 选择 immutable package 并解析绝对 executable |\n| Activation authority | 每次激活和调用前获取 `ActivationLease` | Adapter 同时复核 Plugin invocation fence 与 Workspace trust |\n| Process supervision | 每扩展一个进程、incarnation fencing、停用、关闭和有界重启 | 平台 launcher 安装 sandbox、hard limits 和 killable process tree |\n| Host RPC v1 | 版本、请求相关性、严格 shape、注册 ceiling 和 byte limits | 扩展程序实现协议；App Server 把注册投影到领域 owner |\n| Provider invocation | 路由到精确 registration、deadline、并发取消和结果校验 | Command、Language、Debug、Tasks、Testing 定义 payload 与消费结果 |\n| Diagnostics | 返回 typed `ExtensionHostError`，保留有界 stderr | App Server 清洗并映射客户端可见故障，不泄漏主机路径 |\n\n出现以下代码表示 ownership 漂移：本 crate 扫描 Plugin 目录、持久化 enable/grant、解释\n`package.json`、注册 Workbench provider、决定工作区信任，或自行把一个普通 Node/WASM 脚本当成\nentrypoint 加载。\n\n## 2. 文件与公共契约\n\n| 文件 | 关键公共契约 | 约束 |\n| --- | --- | --- |\n| `protocol.rs` | `ExtensionHostRequest`、`ExtensionHostResponse`、`RegistrationDescriptor` | Host RPC v1 的唯一 wire shape；严格校验 request/response correlation |\n| `authority.rs` | `ActivationAuthority`、`ActivationLease`、`ExtensionActivationSpec` | 授权是 live gate，不是 activation 时的一次布尔判断 |\n| `limits.rs` | `ExtensionHostLimits`、`ProcessIsolationPolicy` | 默认要求平台强制隔离；所有 byte/count/deadline limit 必须非零且一致 |\n| `process.rs` | `ExtensionHostLauncher`、`ExtensionHostProcess`、`ExtensionLaunchCommand` | launcher 必须在 entrypoint 执行前完成隔离，并清空继承环境 |\n| `supervisor.rs` | `ExtensionHostSupervisor`、`ExtensionHostSnapshot` | 一扩展一监管器；注册仅在完整 activation 成功后发布 |\n| `supervisor/invocation.rs` | `ExtensionInvocation`、`ExtensionInvocationHandle` | wait 与 cancel 可由不同线程并发调用；lease 持续到 terminal handling |\n| `restart.rs` | `RestartPolicy`、`RestartTracker` | 滑动窗口、指数退避和 terminal `CrashLoop` |\n| `error.rs` | `ExtensionHostError` | 区分拒绝、配额、协议、退出、超时和 unknown outcome |\n\n`ExtensionHostLauncher` 和 `ActivationAuthority` 是 host adapter 必须实现的两个端口。前者拥有物理\n隔离，后者拥有“当前是否仍可运行”的 live decision；不能用一个启动时缓存的 `true` 替代后者。\n\n## 3. 内部接口地图\n\n| Private symbol | 精确职责 | 不能承担 | 修改时同步检查 |\n| --- | --- | --- | --- |\n| `SupervisorState` | 保存 status、incarnation、live process、原子 registrations、invocation leases 与 restart tracker | Plugin 安装状态或 Workbench registry | supervisor lifecycle/recovery tests、系统文档 |\n| `ExtensionHostSupervisor::launch_and_activate` | 取得 activation lease，spawn，递增 incarnation，完成 handshake + activate 后一次发布 registrations | 平台 sandbox、activation-event matching | authority、handshake、capability ceiling、restart tests |\n| `ExtensionHostSupervisor::context` | 分配非零且不复用的 request ID，并绑定 incarnation 与 activation generation | 跨进程持久 ID | exhaustion/correlation tests |\n| `reserve_pending` | 在写 stdin 前预留 waiter，分别约束普通请求和 control request，并拒绝 request ID 重用 | provider-level scheduling | concurrent cancel、quota、duplicate-ID tests |\n| `spawn_stdout_reader` | 有界读取一行、strict decode、按 request ID 找 pending entry、执行 response validation | 接受 unsolicited extension events | malformed/oversized/correlation tests |\n| `read_bounded_line` | 在分配增长前执行 frame byte ceiling，并要求 newline-terminated frame | JSON semantic validation | exact-boundary tests |\n| `ExtensionInvocationHandle::wait` | 轮询 terminal response，观察 caller/deadline cancellation，执行 grace 后 unknown-outcome recovery | 把超时当成确认失败 | cancel/deadline/indeterminate/restart tests |\n| `ExtensionHostSupervisor::recover_locked` | 清除旧注册和 leases、终止旧进程、消费 restart budget、重新握手和激活 | 无限重启或跨授权恢复 | crash-window/backoff/authority-revoked tests |\n| `validate_registrations` | 检查注册数量、ID 唯一性、字段限制和 manifest capability ceiling | 实现 provider 业务语义 | protocol capability/duplicate/size tests |\n\n真实调用路径为：\n\n```mermaid\nflowchart TD\n    start[\"ExtensionHostSupervisor::start\"] --> lease[\"ExtensionActivationSpec::acquire\"]\n    lease --> spawn[\"ExtensionHostLauncher::spawn\"]\n    spawn --> initialize[\"initialize_and_activate: Initialize\"]\n    initialize --> activate[\"Activate + validate_registrations\"]\n    activate --> publish[\"SupervisorState atomically publishes Ready + registrations\"]\n    publish --> invoke[\"begin_invoke + per-call ActivationLease\"]\n    invoke --> response[\"ExtensionInvocationHandle::wait\"]\n    response -->|success or typed host failure| release[\"release invocation lease\"]\n    response -->|exit, invalid protocol, or no terminal result after cancel grace| recover[\"terminate + recover_failed_incarnation\"]\n    recover --> spawn\n```\n\n## 4. Host RPC v1\n\n传输是一行一个 UTF-8 JSON frame 的双向 stdio request/response 协议。每个 frame 都携带\n`protocolVersion`、`requestId`、`incarnation` 和 `activationGeneration`；任一字段为零、版本不匹配、\nresponse correlation 不完全一致、未知 request ID、错误 response kind 或超过 frame ceiling 都使当前\n进程失败关闭。请求 ID 空间耗尽时返回 `RequestIdentityExhausted`，不能环回并复用旧 identity。\n\n生命周期顺序固定为 `Initialize → Activate → Invoke* → Deactivate → Shutdown`。`Activate` 一次返回完整\n注册集合，只有整批验证通过才成为 `ExtensionHostSnapshot.registrations`。当前 registration kinds 为\n当前注册类型包括命令、语言供应商、调试适配器、任务供应商与测试配置供应商；语言 v1\noperation ceiling 包含 completion、Parameter Hints、definition、hover、references、rename、formatting、\ncode action、code lens、document symbols、folding、document links/colors、semantic tokens、Inlay Hints\n与 Linked Editing。\n\n`ActivateParams.activation_events` 是上层已经解析的 activation facts，监管器不会自行观察编辑器事件，\n也不会据此决定何时启动。`capabilities` 则是强制 registration ceiling：进程不能通过 activation 响应\n注册 manifest 未声明的 provider kind。协议支持的 provider kind 不等于 App Server/Workbench 已有对应\n业务适配器。\n\n默认 `ExtensionHostLimits` 为：\n\n| 限制 | 默认值 |\n| --- | ---: |\n| 单 frame | 1 MiB |\n| invocation payload | 512 KiB |\n| registrations | 256 |\n| ordinary in-flight requests | 32 |\n| in-flight control requests | 8 |\n| captured stderr | 256 KiB |\n| arguments | 128 entries / 32 KiB |\n| environment | 64 entries / 32 KiB |\n| startup / request / cancellation grace / shutdown | 10 s / 30 s / 2 s / 5 s |\n| restart budget | 60 s 内最多 5 次；100 ms 起步、最高 5 s 指数退避 |\n| hard resources | 512 MiB memory、300 s CPU、1 process |\n\n这些值按 bytes 或 entries 计数。硬资源值只是一项 launcher obligation；crate 内没有因为字段存在就\n宣称 OS 已经实施限制。\n\n## 5. 授权、隔离与生命周期\n\n`ExtensionActivationSpec` 把可序列化 activation params 与不可序列化 `ActivationAuthority` 绑定。启动、\ncrash recovery 和每次 `begin_invoke` 都重新取得 lease。disable、update、uninstall 或 Workspace trust\n撤销后，adapter 的 `authorizes/acquire` 必须立即拒绝新工作，并让既有 invocation drain 或被 host\ncomposition 主动取消；进程中自报的 package identity 不是授权依据。\n\n默认 `ProcessIsolationPolicy::RequirePlatformEnforcement` 要求产品提供的 launcher 在 entrypoint 运行前\n同时安装 sandbox、memory/CPU/process hard limit、独立 stdio、空继承环境和可整体终止的 process tree。\n任何一项无法保证都必须返回 `IsolationUnavailable`。当前 crate 唯一具体 launcher\n`TrustedDevelopmentLauncher` 只接受显式 `TrustedDevelopment` policy，并仅用于可信本地开发；它不是\n生产第三方扩展的安全边界。\n\n崩溃后旧 incarnation 的 registrations 立即清除，pending request 和 lease 不得迁移到新进程。恢复会\n重新执行 Initialize/Activate，再发布一整批新注册。空闲进程退出由 `reconcile()` 检出，因此 App Server\n必须运行 health loop；监管器本身没有常驻 polling thread。超过 restart window 后进入 `CrashLoop`，只\n能由上层显式重建或重启失败 runtime，不能无限自旋。\n\n## 6. 取消与失败语义\n\n`begin_invoke` 在发送前取得 invocation lease，并返回可跨线程取消的 handle。绝对 UTC deadline 与\n`request_timeout` 取较早者。调用方取消或 deadline 到达时，handle 发送独立 control request；这条\nrequest 不与普通 in-flight quota 竞争。grace 内若仍没有 terminal response，结果为\n`OutcomeIndeterminate`：监管器终止该 incarnation 并恢复，而不是声称扩展没有产生副作用。\n\n`Drop` 一个未完成 handle 会尝试取消、终止进程并释放 lease。Host exit、invalid protocol 和 unknown\noutcome 都会 fence 当前 incarnation；activation authority 被撤销时恢复失败关闭。`stderr()` 只保留有界\n前缀供上层诊断，App Server 对客户端暴露前仍必须清洗主机路径和实现细节。\n\n## 7. 接入义务\n\nApp Server 或其他 composition root 必须：\n\n1. 从 `PluginActivationAuthority` 的 exact immutable package、digest 和 generation 构造\n   `ExtensionActivationSpec`，并把 Workspace trust 加入同一 live gate；\n2. 只把经过 exact process permission 和 regular-file validation 的绝对 executable 交给 launcher；\n3. 注入能够实施 `RequirePlatformEnforcement` 的平台 launcher，若不存在则将生产能力标记为不可用；\n4. 定期调用 `reconcile()`，把 snapshot 变化原子投影到 provider owners；\n5. 使用异步 invocation session 或后台 waiter 暴露调用，使 cancel request 不被一个阻塞 RPC 串行化；\n6. connection 断开、authority 撤销和 shutdown 时取消 owned invocations 并调用 `shutdown()`；\n7. 只暴露清洗后的 failure code/message，不把 absolute executable、cwd 或原始 stderr 放进 wire DTO。\n\nZeterm 若接入同一执行语义应依赖本 crate，并提供自己的 composition adapter；不得依赖 Desktop 或把\nHost runtime 复制进产品 host。\n\n## 8. 测试与修改影响\n\n仓库 workspace 可加载时运行：\n\n```text\ncargo test --manifest-path Cargo.toml -p zeta-editor-extension-host\ncargo clippy --manifest-path Cargo.toml -p zeta-editor-extension-host --all-targets --no-deps -- -D warnings\n```\n\n当前仓库还提供不加载根 workspace 其他 crate 的离线入口：\n\n```text\npowershell -NoProfile -ExecutionPolicy Bypass -File zeta-rs/editor-extension-host/check-standalone.ps1\n```\n\n测试分别位于 sibling `*_tests.rs`，覆盖 strict protocol、frame/payload/registration/in-flight limits、\nresponse correlation、request-ID exhaustion、并发 cancel、authority leases、activation atomicity、\ndeadline/unknown outcome、crash recovery、backoff/crash loop 和 shutdown。修改 registration kind 或 wire\nshape 时还必须同步 Plugin manifest ceiling、App Server protocol DTO、Frontend adapter 和跨层文档；修改\nlauncher obligation 时必须增加各平台 isolation acceptance test，不能只更新 fake launcher 测试。\n\n## 9. 当前限制与扩展点\n\nCurrent：Host RPC v1、逐扩展进程监管、live activation/invocation lease、原子 registrations、并发取消、\n有界 stdio/stderr、incarnation/generation fencing、graceful shutdown 与有界 crash recovery 已实现。\n\n当前限制：\n\n- crate 没有生产平台 launcher；只有显式不安全的可信开发 launcher；\n- activation-event matching 和 lazy activation 属于上层 composition，监管器只接收 activation facts；\n- 空闲崩溃检测依赖上层 health loop；\n- v1 只有 request/response，没有 extension-originated event stream；\n- 没有 generic Node/WASM loader、VS Code Extension API、Marketplace compatibility 或多扩展共享进程；\n- 没有 publisher signature、revocation feed、跨平台 artifact selector 或 binary ABI 检查；这些属于 Plugin\n  supply-chain 与平台 launchability 演进，不应加入协议解析器。\n\n潜在演进必须由真实 consumer 驱动：若增加 extension-originated diagnostics/event、remote host 或新的\nprovider kind，应先固定 App Server/domain owner、背压与权限语义，再版本化协议；不能用 v1 unknown\nfield 或未声明 capability 静默协商新行为。",
    "headings": [
      {
        "depth": 2,
        "id": "1-crate-边界",
        "title": "1. Crate 边界"
      },
      {
        "depth": 2,
        "id": "2-文件与公共契约",
        "title": "2. 文件与公共契约"
      },
      {
        "depth": 2,
        "id": "3-内部接口地图",
        "title": "3. 内部接口地图"
      },
      {
        "depth": 2,
        "id": "4-host-rpc-v1",
        "title": "4. Host RPC v1"
      },
      {
        "depth": 2,
        "id": "5-授权隔离与生命周期",
        "title": "5. 授权、隔离与生命周期"
      },
      {
        "depth": 2,
        "id": "6-取消与失败语义",
        "title": "6. 取消与失败语义"
      },
      {
        "depth": 2,
        "id": "7-接入义务",
        "title": "7. 接入义务"
      },
      {
        "depth": 2,
        "id": "8-测试与修改影响",
        "title": "8. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "9-当前限制与扩展点",
        "title": "9. 当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 Zeta 原生可执行 Editor Extension Host v1 的进程、RPC、授权门禁、取消与故障恢复 实现权威文档。跨 Plugin、Workspace、App Server 和 Workbench 的产品语义由 docs/editor-extensions.md 维护；安装、启用、授权与包身份由 zeta-rs/plugins/README.md 维护。 zeta-editor-extension-host 监管一个已经由上层解析和授权的扩展程序。每个 ExtensionHostSupervisor 最多拥有一个扩展的一个活动进程 incarnation，通过有界 JSONL Host RPC v1 完成握手、激活、调用、取消、停用和关闭，并在授权仍有效时按有界策略恢复崩溃进程。它不发现或 安装 Plugin，不选择 activation event，不实现 Workbench provider，也不兼容 VS Code Node Extension API。 1. Crate 边界 能力 本 crate 的职责 上层或平台职责 --- --- --- Package binding 接收并绑定 packageid、digest、entrypoint 与 activation generation Plugin authority 选择 immutable package 并解析绝对 executable Activation authority 每次激活和调用前获取 ActivationLease Adapter 同时复核 Plugin invocation fence 与 Workspace trust Process supervision 每扩展一个进程、incarnation fencing、停用、关闭和有界重启 平台 launcher 安装 sandbox、hard limits 和 killable process tree Host RPC v1 版本、请求相关性、严格 shape、注册 ceiling 和 byte limits 扩展程序实现协议；App Server 把注册投影到领域 owner Provider invocation 路由到精确 registration、deadline、并发取消和结果校验 Command、Language、Debug、Tasks、Testing 定义 payload 与消费结果 Diagnostics 返回 typed ExtensionHostError，保留有界 stderr App Server 清洗并映射客户端可见故障，不泄漏主机路径 出现以下代码表示 ownership 漂移：本 crate 扫描 Plugin 目录、持久化 enable/grant、解释 package.json、注册 Workbench provider、决定工作区信任，或自行把一个普通 Node/WASM 脚本当成 entrypoint 加载。 2. 文件与公共契约 文件 关键公共契约 约束 --- --- --- protocol.rs ExtensionHostRequest、ExtensionHostResponse、RegistrationDescriptor Host RPC v1 的唯一 wire shape；严格校验 request/response correlation authority.rs ActivationAuthority、ActivationLease、ExtensionActivationSpec 授权是 live gate，不是 activation 时的一次布尔判断 limits.rs ExtensionHostLimits、ProcessIsolationPolicy 默认要求平台强制隔离；所有 byte/count/deadline limit 必须非零且一致 process.rs ExtensionHostLauncher、ExtensionHostProcess、ExtensionLaunchCommand launcher 必须在 entrypoint 执行前完成隔离，并清空继承环境 supervisor.rs ExtensionHostSupervisor、ExtensionHostSnapshot 一扩展一监管器；注册仅在完整 activation 成功后发布 supervisor/invocation.rs ExtensionInvocation、ExtensionInvocationHandle wait 与 cancel 可由不同线程并发调用；lease 持续到 terminal handling restart.rs RestartPolicy、RestartTracker 滑动窗口、指数退避和 terminal CrashLoop error.rs ExtensionHostError 区分拒绝、配额、协议、退出、超时和 unknown outcome ExtensionHostLauncher 和 ActivationAuthority 是 host adapter 必须实现的两个端口。前者拥有物理 隔离，后者拥有“当前是否仍可运行”的 live decision；不能用一个启动时缓存的 true 替代后者。 3. 内部接口地图 Private symbol 精确职责 不能承担 修改时同步检查 --- --- --- --- SupervisorState 保存 status、incarnation、live process、原子 registrations、invocation leases 与 restart tracker Plugin 安装状态或 Workbench registry supervisor lifecycle/recovery tests、系统文档 ExtensionHostSupervisor::launchandactivate 取得 activation lease，spawn，递增 incarnation，完成 handshake + activate 后一次发布 registrations 平台 sandbox、activation-event matching authority、handshake、capability ceiling、restart tests ExtensionHostSupervisor::context 分配非零且不复用的 request ID，并绑定 incarnation 与 activation generation 跨进程持久 ID exhaustion/correlation tests reservepending 在写 stdin 前预留 waiter，分别约束普通请求和 control request，并拒绝 request ID 重用 provider-level scheduling concurrent cancel、quota、duplicate-ID tests spawnstdoutreader 有界读取一行、strict decode、按 request ID 找 pending entry、执行 response validation 接受 unsolicited extension events malformed/oversized/correlation tests readboundedline 在分配增长前执行 frame byte ceiling，并要求 newline-terminated frame JSON semantic validation exact-boundary tests ExtensionInvocationHandle::wait 轮询 terminal response，观察 caller/deadline cancellation，执行 grace 后 unknown-outcome recovery 把超时当成确认失败 cancel/deadline/indeterminate/restart tests ExtensionHostSupervisor::recoverlocked 清除旧注册和 leases、终止旧进程、消费 restart budget、重新握手和激活 无限重启或跨授权恢复 crash-window/backoff/authority-revoked tests validateregistrations 检查注册数量、ID 唯一性、字段限制和 manifest capability ceiling 实现 provider 业务语义 protocol capability/duplicate/size tests 真实调用路径为： ``mermaid flowchart TD start \"ExtensionHostSupervisor::start\" -- lease \"ExtensionActivationSpec::acquire\" lease -- spawn \"ExtensionHostLauncher::spawn\" spawn -- initialize \"initializeandactivate: Initialize\" initialize -- activate \"Activate + validateregistrations\" activate -- publish \"SupervisorState atomically publishes Ready + registrations\" publish -- invoke \"begininvoke + per-call ActivationLease\" invoke -- response \"ExtensionInvocationHandle::wait\" response -- success or typed host failure release \"release invocation lease\" response -- exit, invalid protocol, or no terminal result after cancel grace recover \"terminate + recoverfailedincarnation\" recover -- spawn ` 4. Host RPC v1 传输是一行一个 UTF-8 JSON frame 的双向 stdio request/response 协议。每个 frame 都携带 protocolVersion、requestId、incarnation 和 activationGeneration；任一字段为零、版本不匹配、 response correlation 不完全一致、未知 request ID、错误 response kind 或超过 frame ceiling 都使当前 进程失败关闭。请求 ID 空间耗尽时返回 RequestIdentityExhausted，不能环回并复用旧 identity。 生命周期顺序固定为 Initialize → Activate → Invoke → Deactivate → Shutdown。Activate 一次返回完整 注册集合，只有整批验证通过才成为 ExtensionHostSnapshot.registrations。当前 registration kinds 为 当前注册类型包括命令、语言供应商、调试适配器、任务供应商与测试配置供应商；语言 v1 operation ceiling 包含 completion、Parameter Hints、definition、hover、references、rename、formatting、 code action、code lens、document symbols、folding、document links/colors、semantic tokens、Inlay Hints 与 Linked Editing。 ActivateParams.activationevents 是上层已经解析的 activation facts，监管器不会自行观察编辑器事件， 也不会据此决定何时启动。capabilities 则是强制 registration ceiling：进程不能通过 activation 响应 注册 manifest 未声明的 provider kind。协议支持的 provider kind 不等于 App Server/Workbench 已有对应 业务适配器。 默认 ExtensionHostLimits 为： 限制 默认值 --- ---: 单 frame 1 MiB invocation payload 512 KiB registrations 256 ordinary in-flight requests 32 in-flight control requests 8 captured stderr 256 KiB arguments 128 entries / 32 KiB environment 64 entries / 32 KiB startup / request / cancellation grace / shutdown 10 s / 30 s / 2 s / 5 s restart budget 60 s 内最多 5 次；100 ms 起步、最高 5 s 指数退避 hard resources 512 MiB memory、300 s CPU、1 process 这些值按 bytes 或 entries 计数。硬资源值只是一项 launcher obligation；crate 内没有因为字段存在就 宣称 OS 已经实施限制。 5. 授权、隔离与生命周期 ExtensionActivationSpec 把可序列化 activation params 与不可序列化 ActivationAuthority 绑定。启动、 crash recovery 和每次 begininvoke 都重新取得 lease。disable、update、uninstall 或 Workspace trust 撤销后，adapter 的 authorizes/acquire 必须立即拒绝新工作，并让既有 invocation drain 或被 host composition 主动取消；进程中自报的 package identity 不是授权依据。 默认 ProcessIsolationPolicy::RequirePlatformEnforcement 要求产品提供的 launcher 在 entrypoint 运行前 同时安装 sandbox、memory/CPU/process hard limit、独立 stdio、空继承环境和可整体终止的 process tree。 任何一项无法保证都必须返回 IsolationUnavailable。当前 crate 唯一具体 launcher TrustedDevelopmentLauncher 只接受显式 TrustedDevelopment policy，并仅用于可信本地开发；它不是 生产第三方扩展的安全边界。 崩溃后旧 incarnation 的 registrations 立即清除，pending request 和 lease 不得迁移到新进程。恢复会 重新执行 Initialize/Activate，再发布一整批新注册。空闲进程退出由 reconcile 检出，因此 App Server 必须运行 health loop；监管器本身没有常驻 polling thread。超过 restart window 后进入 CrashLoop，只 能由上层显式重建或重启失败 runtime，不能无限自旋。 6. 取消与失败语义 begininvoke 在发送前取得 invocation lease，并返回可跨线程取消的 handle。绝对 UTC deadline 与 requesttimeout 取较早者。调用方取消或 deadline 到达时，handle 发送独立 control request；这条 request 不与普通 in-flight quota 竞争。grace 内若仍没有 terminal response，结果为 OutcomeIndeterminate：监管器终止该 incarnation 并恢复，而不是声称扩展没有产生副作用。 Drop 一个未完成 handle 会尝试取消、终止进程并释放 lease。Host exit、invalid protocol 和 unknown outcome 都会 fence 当前 incarnation；activation authority 被撤销时恢复失败关闭。stderr 只保留有界 前缀供上层诊断，App Server 对客户端暴露前仍必须清洗主机路径和实现细节。 7. 接入义务 App Server 或其他 composition root 必须： 1. 从 PluginActivationAuthority 的 exact immutable package、digest 和 generation 构造 ExtensionActivationSpec，并把 Workspace trust 加入同一 live gate； 2. 只把经过 exact process permission 和 regular-file validation 的绝对 executable 交给 launcher； 3. 注入能够实施 RequirePlatformEnforcement 的平台 launcher，若不存在则将生产能力标记为不可用； 4. 定期调用 reconcile ，把 snapshot 变化原子投影到 provider owners； 5. 使用异步 invocation session 或后台 waiter 暴露调用，使 cancel request 不被一个阻塞 RPC 串行化； 6. connection 断开、authority 撤销和 shutdown 时取消 owned invocations 并调用 shutdown ； 7. 只暴露清洗后的 failure code/message，不把 absolute executable、cwd 或原始 stderr 放进 wire DTO。 Zeterm 若接入同一执行语义应依赖本 crate，并提供自己的 composition adapter；不得依赖 Desktop 或把 Host runtime 复制进产品 host。 8. 测试与修改影响 仓库 workspace 可加载时运行： `text cargo test --manifest-path Cargo.toml -p zeta-editor-extension-host cargo clippy --manifest-path Cargo.toml -p zeta-editor-extension-host --all-targets --no-deps -- -D warnings ` 当前仓库还提供不加载根 workspace 其他 crate 的离线入口： `text powershell -NoProfile -ExecutionPolicy Bypass -File zeta-rs/editor-extension-host/check-standalone.ps1 ` 测试分别位于 sibling tests.rs`，覆盖 strict protocol、frame/payload/registration/in-flight limits、 response correlation、request-ID exhaustion、并发 cancel、authority leases、activation atomicity、 deadline/unknown outcome、crash recovery、backoff/crash loop 和 shutdown。修改 registration kind 或 wire shape 时还必须同步 Plugin manifest ceiling、App Server protocol DTO、Frontend adapter 和跨层文档；修改 launcher obligation 时必须增加各平台 isolation acceptance test，不能只更新 fake launcher 测试。 9. 当前限制与扩展点 Current：Host RPC v1、逐扩展进程监管、live activation/invocation lease、原子 registrations、并发取消、 有界 stdio/stderr、incarnation/generation fencing、graceful shutdown 与有界 crash recovery 已实现。 当前限制： - crate 没有生产平台 launcher；只有显式不安全的可信开发 launcher； - activation-event matching 和 lazy activation 属于上层 composition，监管器只接收 activation facts； - 空闲崩溃检测依赖上层 health loop； - v1 只有 request/response，没有 extension-originated event stream； - 没有 generic Node/WASM loader、VS Code Extension API、Marketplace compatibility 或多扩展共享进程； - 没有 publisher signature、revocation feed、跨平台 artifact selector 或 binary ABI 检查；这些属于 Plugin supply-chain 与平台 launchability 演进，不应加入协议解析器。 潜在演进必须由真实 consumer 驱动：若增加 extension-originated diagnostics/event、remote host 或新的 provider kind，应先固定 App Server/domain owner、背压与权限语义，再版本化协议；不能用 v1 unknown field 或未声明 capability 静默协商新行为。"
  },
  {
    "slug": "crates/exec",
    "sourcePath": "zeta-rs/exec/README.md",
    "title": "zeta-exec",
    "description": "本 crate 把一次 new、resume 或 fork 意图转换成类型化的 Session/Thread/Turn 请求，观察 canonical Turn 终态，并输出版本化事件。它不执行 shell process，不拥有 Agent loop，也不直接读取 Core、rollout、store、model provider、sandbox 或 zeta-tool…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 维护无界面 Agent runner 的当前实现契约。跨进程调度、远程执行平面与阶段规划由\n> [`docs/exec.md`](../../docs/exec.md) 统一维护；App Server 连接契约见\n> [`docs/app-server-client.md`](../../docs/app-server-client.md)。\n\n本 crate 把一次 new、resume 或 fork 意图转换成类型化的 Session/Thread/Turn 请求，观察\ncanonical Turn 终态，并输出版本化事件。它不执行 shell process，不拥有 Agent loop，也不直接读取\nCore、rollout、store、model provider、sandbox 或 `zeta-tool-executor`。\n\n## 文件与职责\n\n| 文件 | 当前职责 |\n| --- | --- |\n| `src/model.rs` | run input、approval mode、versioned event、terminal outcome 与 exit code |\n| `src/run_id.rs` | `ExecRunId` 校验和进程内生成 |\n| `src/output.rs` | `ExecEventSink`、discard sink 与逐事件 flush 的 JSONL sink |\n| `src/connection.rs` | 私有 `ExecConnection` 端口及 embedded App Server adapter |\n| `src/runner.rs` | 公共 runner、timeout 配置、cancellation contract 与显式 shutdown |\n| `src/run_loop.rs` | prepare、subscribe、start、observe、interrupt 与 unsubscribe 编排 |\n| `src/turn_outcome.rs` | canonical terminal status、交互等待与公开 outcome 的唯一映射 |\n| `src/*_tests.rs` | schema、JSONL、终态、取消、交互与断线 contract tests |\n\n## 公共契约\n\n`ExecRunner::run` 是唯一产品执行入口。调用方提供：\n\n- `ExecRunRequest`：稳定 run identity、`ExecEntry` 与 `HeadlessApprovalMode`；\n- `ExecEventSink`：按顺序接收完整事件，失败时不自动重试；\n- `ExecCancellation`：廉价、非阻塞的取消观察点。\n\n`ExecEntry` 明确区分 `New`、`Resume` 和 `Fork`，避免由多个 bool 或缺省 ID 推断入口意图。\n`AppServerTarget` 当前只实现 `Embedded`；remote backend 仍是系统文档中的计划设计。\n\n`ExecEvent` 是 schema version 为 1 的展示/自动化 envelope。`ThreadUpdated` 机械携带 canonical\n`ThreadUpdateEnvelope`，不建立第二套 authoritative item 或 Turn 状态。`JsonLinesExecEventSink`\n每次写入一个完整 JSON object、换行并 flush；诊断文本必须由宿主写入 stderr。\n\n终态只由读取到的 canonical `TurnStatus::{Completed,Failed,Interrupted}` 产生：\n\n| `ExecOutcome` | 来源 | 默认退出码 |\n| --- | --- | --- |\n| `Completed` | canonical `Completed` | 0 |\n| `Failed` | canonical `Failed`，保留 `StableTurnError` | 1 |\n| `RequiresInteraction` | headless policy 请求 interrupt，随后观察到 canonical `Interrupted` | 2 |\n| `Interrupted` | canonical `Interrupted` | 130 |\n| `OutcomeUnknown` | Turn 已开始，但连接、观察或 interrupt handshake 未给出终态 | 75 |\n\n## 内部接口地图与调用路径\n\n`EmbeddedConnection` 是 `zeta-app-server-client` 的薄适配器；它只构造协议 Params、检查\n`SessionRequestResult` variant，并把 notification 映射为私有 `ConnectionEvent`。\n`prepare_run` 拥有 new/resume/fork 到 Session/Thread 选择的唯一映射。`terminal_outcome` 是\ncanonical Turn status 到公开 outcome 的唯一转换点。`required_interaction` 只决定无 UI 时何时请求\ninterrupt，不改变 App Server 内的批准结论。`best_effort_interrupt` 只用于 event sink 在 Turn 启动后\n失败的清理路径。\n\n```text\nExecRunner::run\n  → EmbeddedConnection::start\n  → ExecRunner::run_connected\n     → prepare_run\n     → ExecConnection::subscribe_thread\n     → ExecConnection::start_turn\n     → ExecRunner::drive_turn\n        → read_thread / poll_event\n        → interrupt_turn when cancellation, timeout or unsupported interaction wins\n        → terminal_outcome\n     → ExecConnection::unsubscribe_thread\n  → ExecConnection::close\n```\n\n`SessionRequestParams.expected_sequence` 在 Thread mutation 路径使用最新 Thread sequence；调用方不能\n硬编码新 Thread 的初始 sequence，也不能把 Session sequence 用于 StartTurn/InterruptTurn。\n每种 mutation 使用由 `ExecRunId + operation` 生成的稳定 `CommandId`，当前实现不自动 retry。\n\n若在本 crate 出现 Core reducer、provider request、rollout 读取、process spawn、sandbox policy 或\nscheduler lease table，说明 ownership 已漂移。底层进程执行只能进入\n[`zeta-tool-executor`](../tool-executor/README.md)。\n\n## 取消、交互与失败\n\nTurn 创建前取消返回 `ExecError::CancelledBeforeStart`。Turn 创建后取消和 timeout 都先发送类型化\n`SessionRequest::InterruptTurn`，然后在 `interrupt_timeout` 内继续读取 snapshot，只有观察到\ncanonical terminal status 才返回 `Interrupted` 或 `RequiresInteraction`。\n\n默认 `DenyInteractiveRequests` 使用普通 approval policy，但一旦 Turn 等待 approval、user input 或\ncapability，就请求 interrupt。`AutomaticReview` 允许 App Server 自动审查 approval，仍会停止无法呈现\n的 user input/capability。`BypassPermissions` 必须由宿主显式选择，不能成为远程 worker 默认值。\n\n连接关闭不会转换成 `Failed`。Runner 会做一次最终 Thread read；无法确认 terminal status 时返回\n`OutcomeUnknown`。当前没有 remote reconnect/resubscribe backend，因而 unknown outcome 必须交给上层\n显式处理，不能自动 replay 可能已经产生副作用的 Turn。\n\nSink 在 Turn 启动后失败会触发一次 best-effort interrupt，再返回 `ExecError::Output`。正常路径始终\nunsubscribe 并调用 `AppServerSession::shutdown`；shutdown 失败不会被静默吞掉。\n\n## 宿主接入与验证\n\n`zeta-cli` 负责参数、stdout/stderr 和 signal 注册；它不复制 runner 状态机。新的宿主应构造\n`EmbeddedAppServerOptions`、选择明确 approval mode，并根据 `ExecOutcome::exit_code` 映射进程或 Job\n状态。\n\n```bash\ncargo test -p zeta-exec\ncargo test -p zeta-cli\ncargo clippy -p zeta-exec --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/exec:exec-unit-tests\n```\n\n修改 event JSON shape 时必须更新 schema version、serialization fixture、本 README 和\n`docs/exec.md`。修改 interruption、terminal mapping 或 sequence 使用时必须同步覆盖 runner fake\nconnection tests 与 CLI exit behavior。\n\n## 当前限制与扩展点\n\n- 当前只支持 embedded App Server，不支持 daemon/remote transport；\n- 当前是同步 run-once runner，没有长期 worker、Job/Attempt、lease、fencing 或 event ack；\n- JSONL 已 versioned，但尚无跨版本 compatibility fixture、last-message file 或 stdin item stream；\n- 自动审查由 App Server 拥有；当前 headless runner 不实现远程 reviewer channel；\n- 断线后只做最终 read，不执行 reconnect + snapshot/gap resubscribe。\n\n这些能力的 ownership 与前置条件以 `docs/exec.md` 的阶段 3–5 为准。",
    "headings": [
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图与调用路径",
        "title": "内部接口地图与调用路径"
      },
      {
        "depth": 2,
        "id": "取消交互与失败",
        "title": "取消、交互与失败"
      },
      {
        "depth": 2,
        "id": "宿主接入与验证",
        "title": "宿主接入与验证"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 维护无界面 Agent runner 的当前实现契约。跨进程调度、远程执行平面与阶段规划由 docs/exec.md 统一维护；App Server 连接契约见 docs/app-server-client.md。 本 crate 把一次 new、resume 或 fork 意图转换成类型化的 Session/Thread/Turn 请求，观察 canonical Turn 终态，并输出版本化事件。它不执行 shell process，不拥有 Agent loop，也不直接读取 Core、rollout、store、model provider、sandbox 或 zeta-tool-executor。 文件与职责 文件 当前职责 --- --- src/model.rs run input、approval mode、versioned event、terminal outcome 与 exit code src/runid.rs ExecRunId 校验和进程内生成 src/output.rs ExecEventSink、discard sink 与逐事件 flush 的 JSONL sink src/connection.rs 私有 ExecConnection 端口及 embedded App Server adapter src/runner.rs 公共 runner、timeout 配置、cancellation contract 与显式 shutdown src/runloop.rs prepare、subscribe、start、observe、interrupt 与 unsubscribe 编排 src/turnoutcome.rs canonical terminal status、交互等待与公开 outcome 的唯一映射 src/tests.rs schema、JSONL、终态、取消、交互与断线 contract tests 公共契约 ExecRunner::run 是唯一产品执行入口。调用方提供： - ExecRunRequest：稳定 run identity、ExecEntry 与 HeadlessApprovalMode； - ExecEventSink：按顺序接收完整事件，失败时不自动重试； - ExecCancellation：廉价、非阻塞的取消观察点。 ExecEntry 明确区分 New、Resume 和 Fork，避免由多个 bool 或缺省 ID 推断入口意图。 AppServerTarget 当前只实现 Embedded；remote backend 仍是系统文档中的计划设计。 ExecEvent 是 schema version 为 1 的展示/自动化 envelope。ThreadUpdated 机械携带 canonical ThreadUpdateEnvelope，不建立第二套 authoritative item 或 Turn 状态。JsonLinesExecEventSink 每次写入一个完整 JSON object、换行并 flush；诊断文本必须由宿主写入 stderr。 终态只由读取到的 canonical TurnStatus:: Completed,Failed,Interrupted 产生： ExecOutcome 来源 默认退出码 --- --- --- Completed canonical Completed 0 Failed canonical Failed，保留 StableTurnError 1 RequiresInteraction headless policy 请求 interrupt，随后观察到 canonical Interrupted 2 Interrupted canonical Interrupted 130 OutcomeUnknown Turn 已开始，但连接、观察或 interrupt handshake 未给出终态 75 内部接口地图与调用路径 EmbeddedConnection 是 zeta-app-server-client 的薄适配器；它只构造协议 Params、检查 SessionRequestResult variant，并把 notification 映射为私有 ConnectionEvent。 preparerun 拥有 new/resume/fork 到 Session/Thread 选择的唯一映射。terminaloutcome 是 canonical Turn status 到公开 outcome 的唯一转换点。requiredinteraction 只决定无 UI 时何时请求 interrupt，不改变 App Server 内的批准结论。besteffortinterrupt 只用于 event sink 在 Turn 启动后 失败的清理路径。 ``text ExecRunner::run → EmbeddedConnection::start → ExecRunner::runconnected → preparerun → ExecConnection::subscribethread → ExecConnection::startturn → ExecRunner::driveturn → readthread / pollevent → interruptturn when cancellation, timeout or unsupported interaction wins → terminaloutcome → ExecConnection::unsubscribethread → ExecConnection::close ` SessionRequestParams.expectedsequence 在 Thread mutation 路径使用最新 Thread sequence；调用方不能 硬编码新 Thread 的初始 sequence，也不能把 Session sequence 用于 StartTurn/InterruptTurn。 每种 mutation 使用由 ExecRunId + operation 生成的稳定 CommandId，当前实现不自动 retry。 若在本 crate 出现 Core reducer、provider request、rollout 读取、process spawn、sandbox policy 或 scheduler lease table，说明 ownership 已漂移。底层进程执行只能进入 zeta-tool-executor。 取消、交互与失败 Turn 创建前取消返回 ExecError::CancelledBeforeStart。Turn 创建后取消和 timeout 都先发送类型化 SessionRequest::InterruptTurn，然后在 interrupttimeout 内继续读取 snapshot，只有观察到 canonical terminal status 才返回 Interrupted 或 RequiresInteraction。 默认 DenyInteractiveRequests 使用普通 approval policy，但一旦 Turn 等待 approval、user input 或 capability，就请求 interrupt。AutomaticReview 允许 App Server 自动审查 approval，仍会停止无法呈现 的 user input/capability。BypassPermissions 必须由宿主显式选择，不能成为远程 worker 默认值。 连接关闭不会转换成 Failed。Runner 会做一次最终 Thread read；无法确认 terminal status 时返回 OutcomeUnknown。当前没有 remote reconnect/resubscribe backend，因而 unknown outcome 必须交给上层 显式处理，不能自动 replay 可能已经产生副作用的 Turn。 Sink 在 Turn 启动后失败会触发一次 best-effort interrupt，再返回 ExecError::Output。正常路径始终 unsubscribe 并调用 AppServerSession::shutdown；shutdown 失败不会被静默吞掉。 宿主接入与验证 zeta-cli 负责参数、stdout/stderr 和 signal 注册；它不复制 runner 状态机。新的宿主应构造 EmbeddedAppServerOptions、选择明确 approval mode，并根据 ExecOutcome::exitcode 映射进程或 Job 状态。 `bash cargo test -p zeta-exec cargo test -p zeta-cli cargo clippy -p zeta-exec --all-targets --no-deps -- -D warnings bazel test //zeta-rs/exec:exec-unit-tests ` 修改 event JSON shape 时必须更新 schema version、serialization fixture、本 README 和 docs/exec.md。修改 interruption、terminal mapping 或 sequence 使用时必须同步覆盖 runner fake connection tests 与 CLI exit behavior。 当前限制与扩展点 - 当前只支持 embedded App Server，不支持 daemon/remote transport； - 当前是同步 run-once runner，没有长期 worker、Job/Attempt、lease、fencing 或 event ack； - JSONL 已 versioned，但尚无跨版本 compatibility fixture、last-message file 或 stdin item stream； - 自动审查由 App Server 拥有；当前 headless runner 不实现远程 reviewer channel； - 断线后只做最终 read，不执行 reconnect + snapshot/gap resubscribe。 这些能力的 ownership 与前置条件以 docs/exec.md` 的阶段 3–5 为准。"
  },
  {
    "slug": "crates/execpolicy",
    "sourcePath": "zeta-rs/execpolicy/README.md",
    "title": "zeta-execpolicy",
    "description": "zeta-execpolicy 是后端无关的确定性执行规则层。它把 host、organization、user 与 workspace 提供的 immutable rule layers 组合成一个带 semantic revision 的 snapshot，并对 host 已完整 materialize 的 action subject 做纯求值。",
    "group": "Crate 实现参考",
    "markdown": "`zeta-execpolicy` 是后端无关的确定性执行规则层。它把 host、organization、user 与 workspace\n提供的 immutable rule layers 组合成一个带 semantic revision 的 snapshot，并对 host 已完整\nmaterialize 的 action subject 做纯求值。\n\n它不执行 Tool、不选择 sandbox backend、不调用 Auto Review、不显示 approval UI，也不签发\nexecution grant。最终执行决定和 exact action binding 属于\n[`zeta-action-policy`](../action-policy/README.md)；跨 crate 权限语义由\n[`docs/permissions.md`](../../docs/permissions.md) 统一说明。\n\n## 公共契约\n\n| 类型 | 责任 |\n| --- | --- |\n| `ExecPolicySubject` | action digest、kind、可信来源、capability 与可选 command/network projection |\n| `ExecPolicySelector` | exact digest、source、command prefix、network、capability scope 与显式组合 |\n| `ExecPolicyLayer` | Host / Organization / User / Workspace rule collection |\n| `ExecPolicySnapshot` | validation、canonical layer ordering、semantic revision 与 deterministic evaluation |\n| `ExecPolicyEvaluation` | effective effect、exact source rule 与完整 matched-rule audit |\n| `ExecPolicyAmendment` | expected-revision 约束下的纯 User layer upsert/remove；不做文件 I/O |\n\n所有匹配规则中最严格的 effect 生效：`Deny > RequireSandbox > RequireApproval >\nAllowUnsandboxed > Continue`。因此较低信任层不能用 allow 覆盖更严格的上层约束。没有匹配时只\n允许 `Continue` 或 fail-closed `Deny` 两种 snapshot default；不存在隐式 default allow。\n\n## 依赖与运行路径\n\n```text\nConfig / trusted host adapters\n  → ExecPolicyLayer[]\n  → ExecPolicySnapshot::new\n  → ExecPolicySnapshot::evaluate\n  → ExecPolicyEvaluation\n  → zeta-action-policy::ActionPolicyEngine\n  → final ExecutionDecision / exact grant\n```\n\n`ExecPolicySnapshot` 不读取配置文件。Config authority 负责来源、trust、持久化和 atomic\nreplacement；本 crate 只负责 typed document 的 validation、merge semantics、revision 与纯变换。\n\n## 关键实现符号\n\n| Symbol | 职责 | 漂移信号 |\n| --- | --- | --- |\n| `ExecPolicySnapshot::new` | layer validation、network name normalization、canonical ordering 与 semantic revision | config adapter 自己实现第二套 hash/merge |\n| `ExecPolicySnapshot::evaluate` | selector match、effect precedence 与完整 audit | effect 直接变成 Tool authority |\n| `validate` | ID、rule 与 Workspace 不可扩权 invariant | Workspace layer 可产生 `AllowUnsandboxed` |\n| `ExecPolicySelector::matches` | 只匹配 host-materialized typed subject | 解析 summary 或 shell 字符串 |\n| `ExecPolicyAmendment::apply` | revision-bound User layer 纯变换 | 读取或写入 config/storage |\n\n`zeta-config::compose_exec_policy` 是当前持久化/来源 adapter：User rules 位于用户 `config.toml`，\nWorkspace rules 位于 strict-read `.zeta/config.toml` 且只能收紧。App Server 在运行时安全点把它们与\nHost layer 组合，并把 semantic revision 纳入 `ActionPolicyRevision`。\n\n## 修改约束\n\n- selector 只能消费 host materialized 的结构化字段，不能解析 summary；\n- command prefix 匹配 tokenized argv，不能对 shell string 做不可靠的 substring 判断；\n- network protocol 与 DNS host 在 snapshot 构造时规范化；domain suffix 必须按 DNS label boundary 匹配；\n- Workspace layer 不能通过 amendment API 扩权；\n- evaluation 不能构造 action-policy grant 或直接返回 Tool execution authority；\n- 新增 selector/effect 时必须同步 action-policy mapping、config schema 和 precedence 测试。\n\n```text\ncargo test -p zeta-execpolicy\nbazel test //zeta-rs/execpolicy:execpolicy-unit-tests\n```",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "依赖与运行路径",
        "title": "依赖与运行路径"
      },
      {
        "depth": 2,
        "id": "关键实现符号",
        "title": "关键实现符号"
      },
      {
        "depth": 2,
        "id": "修改约束",
        "title": "修改约束"
      }
    ],
    "searchText": "zeta-execpolicy 是后端无关的确定性执行规则层。它把 host、organization、user 与 workspace 提供的 immutable rule layers 组合成一个带 semantic revision 的 snapshot，并对 host 已完整 materialize 的 action subject 做纯求值。 它不执行 Tool、不选择 sandbox backend、不调用 Auto Review、不显示 approval UI，也不签发 execution grant。最终执行决定和 exact action binding 属于 zeta-action-policy；跨 crate 权限语义由 docs/permissions.md 统一说明。 公共契约 类型 责任 --- --- ExecPolicySubject action digest、kind、可信来源、capability 与可选 command/network projection ExecPolicySelector exact digest、source、command prefix、network、capability scope 与显式组合 ExecPolicyLayer Host / Organization / User / Workspace rule collection ExecPolicySnapshot validation、canonical layer ordering、semantic revision 与 deterministic evaluation ExecPolicyEvaluation effective effect、exact source rule 与完整 matched-rule audit ExecPolicyAmendment expected-revision 约束下的纯 User layer upsert/remove；不做文件 I/O 所有匹配规则中最严格的 effect 生效：Deny RequireSandbox RequireApproval AllowUnsandboxed Continue。因此较低信任层不能用 allow 覆盖更严格的上层约束。没有匹配时只 允许 Continue 或 fail-closed Deny 两种 snapshot default；不存在隐式 default allow。 依赖与运行路径 ``text Config / trusted host adapters → ExecPolicyLayer → ExecPolicySnapshot::new → ExecPolicySnapshot::evaluate → ExecPolicyEvaluation → zeta-action-policy::ActionPolicyEngine → final ExecutionDecision / exact grant ` ExecPolicySnapshot 不读取配置文件。Config authority 负责来源、trust、持久化和 atomic replacement；本 crate 只负责 typed document 的 validation、merge semantics、revision 与纯变换。 关键实现符号 Symbol 职责 漂移信号 --- --- --- ExecPolicySnapshot::new layer validation、network name normalization、canonical ordering 与 semantic revision config adapter 自己实现第二套 hash/merge ExecPolicySnapshot::evaluate selector match、effect precedence 与完整 audit effect 直接变成 Tool authority validate ID、rule 与 Workspace 不可扩权 invariant Workspace layer 可产生 AllowUnsandboxed ExecPolicySelector::matches 只匹配 host-materialized typed subject 解析 summary 或 shell 字符串 ExecPolicyAmendment::apply revision-bound User layer 纯变换 读取或写入 config/storage zeta-config::composeexecpolicy 是当前持久化/来源 adapter：User rules 位于用户 config.toml， Workspace rules 位于 strict-read .zeta/config.toml 且只能收紧。App Server 在运行时安全点把它们与 Host layer 组合，并把 semantic revision 纳入 ActionPolicyRevision。 修改约束 - selector 只能消费 host materialized 的结构化字段，不能解析 summary； - command prefix 匹配 tokenized argv，不能对 shell string 做不可靠的 substring 判断； - network protocol 与 DNS host 在 snapshot 构造时规范化；domain suffix 必须按 DNS label boundary 匹配； - Workspace layer 不能通过 amendment API 扩权； - evaluation 不能构造 action-policy grant 或直接返回 Tool execution authority； - 新增 selector/effect 时必须同步 action-policy mapping、config schema 和 precedence 测试。 `text cargo test -p zeta-execpolicy bazel test //zeta-rs/execpolicy:execpolicy-unit-tests ``"
  },
  {
    "slug": "crates/ext/extension-api",
    "sourcePath": "zeta-rs/ext/extension-api/README.md",
    "title": "zeta-extension-api",
    "description": "Canonical context ownership and ordering are documented in docs/core-context.md. Skill-specific lifecycle semantics are documented in docs/skills.md.",
    "group": "Crate 实现参考",
    "markdown": "Canonical context ownership and ordering are documented in\n[`docs/core-context.md`](../../../docs/core-context.md). Skill-specific lifecycle semantics are\ndocumented in [`docs/skills.md`](../../../docs/skills.md).\n\n`zeta-extension-api` owns the backend-neutral lifecycle contracts used to contribute agent\nbehavior without placing domain orchestration in Core or App Server. It contains no Skill catalog,\nfilesystem, JSON-RPC, product UI, or model-provider implementation.\n\nThe supported lifecycle is deliberately small:\n\n| Contract | Called by | Purpose |\n| --- | --- | --- |\n| `SkillActivationContributor` | Core before a new Turn is committed | Resolve explicit selections into durable activations |\n| `TurnInputContributor` | Core at each model-invocation safe point | Produce immutable, provenance-bearing prompt fragments |\n| `ReadOnlyToolContributor` | Host tool composition | Contribute in-process executors that require no ambient authority |\n| `ExtensionRegistry` | Core | Invoke installed contributors in registration order |\n\nApp Server may construct the registry and adapt extension events to protocol notifications, but it\nmust not implement contributor selection, loading, or prompt composition. For read-only tools it\nmay validate definitions and adapt executors to the normal tool policy/registry pipeline; the domain\noperation remains in the contributing extension. `ExtensionRegistry::contribute_read_only_tools`\nrejects duplicate model-visible names before host composition.\n\n`ReadOnlyToolContributor` is intentionally not a generic capability escape hatch. Its executors may\nread extension-owned source roots that were validated before registration; they must not use ambient\nfilesystem authority or perform filesystem mutation, process, network, credential, UI, or external\nmutation operations. A future extension that needs those capabilities requires a separate\nhost-reviewed contract.",
    "headings": [],
    "searchText": "Canonical context ownership and ordering are documented in docs/core-context.md. Skill-specific lifecycle semantics are documented in docs/skills.md. zeta-extension-api owns the backend-neutral lifecycle contracts used to contribute agent behavior without placing domain orchestration in Core or App Server. It contains no Skill catalog, filesystem, JSON-RPC, product UI, or model-provider implementation. The supported lifecycle is deliberately small: Contract Called by Purpose --- --- --- SkillActivationContributor Core before a new Turn is committed Resolve explicit selections into durable activations TurnInputContributor Core at each model-invocation safe point Produce immutable, provenance-bearing prompt fragments ReadOnlyToolContributor Host tool composition Contribute in-process executors that require no ambient authority ExtensionRegistry Core Invoke installed contributors in registration order App Server may construct the registry and adapt extension events to protocol notifications, but it must not implement contributor selection, loading, or prompt composition. For read-only tools it may validate definitions and adapt executors to the normal tool policy/registry pipeline; the domain operation remains in the contributing extension. ExtensionRegistry::contributereadonlytools rejects duplicate model-visible names before host composition. ReadOnlyToolContributor is intentionally not a generic capability escape hatch. Its executors may read extension-owned source roots that were validated before registration; they must not use ambient filesystem authority or perform filesystem mutation, process, network, credential, UI, or external mutation operations. A future extension that needs those capabilities requires a separate host-reviewed contract."
  },
  {
    "slug": "crates/extensions",
    "sourcePath": "zeta-rs/extensions/README.md",
    "title": "zeta-extensions",
    "description": "zeta-extensions 接收产品 host 选择的可信根以及 authority provider 选择的 exact immutable package 目录，把有效 package 冻结成 generation-bound 内存快照，并返回 manifest descriptor、诊断和有界 资源 bytes。它不拥有 JSON-RPC、connection …",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是静态扩展目录发现、不可变 package snapshot 与资源读取的实现权威文档。跨 App\n> Server、Workbench 和 Editor 的用户语义、信任边界与长期演进由\n> [`docs/editor-extensions.md`](../../docs/editor-extensions.md) 维护；内置 package set 的来源和分发\n> 规则见 [`extensions/README.md`](../../extensions/README.md)。\n\n`zeta-extensions` 接收产品 host 选择的可信根以及 authority provider 选择的 exact immutable package\n目录，把有效 package 冻结成 generation-bound 内存快照，并返回 manifest descriptor、诊断和有界\n资源 bytes。它不拥有 JSON-RPC、connection resource、Workbench、TextMate、安装 authority 或扩展代码执行。\n\n## 1. 边界与公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `ExtensionRoot` / `ExtensionRootKind` | 表达 host 已选择的 built-in/user root | 从 Renderer 接收路径、Workspace trust |\n| `DynamicExtensionSourceProvider` / `DynamicExtensionSourceSnapshot` | 按 authority generation 提供 exact immutable package 目录 | Plugin install/enable/grant、下载或签名验证 |\n| `ExtensionCatalog::list` | cached query 或 refresh scan，并发布单调 generation | 解析 editor `contributes` |\n| `ExtensionCatalogSnapshot` | 固定一代 descriptor、diagnostic 和完整 package identity | 保留历史 generation |\n| `ExtensionDescriptor` | `publisher.name`、版本、manifest JSON/摘要和 package 摘要 | enablement、grant、signature |\n| `ExtensionCatalog::open_resource` | 校验 generation、ID 和包内相对路径后读取当前 frozen bytes | 从 live filesystem 回退 |\n| `ExtensionCatalogError` | generation conflict、缺失 ID/resource、非法路径、limit/IO failure | transport error code |\n\n固定 precedence 为 built-in roots → dynamic Plugin-authorized exact packages → user roots；每个阶段中\n第一个有效 extension ID 获胜，后续重复项只产生 `DuplicateExtension` diagnostic。由此可变用户包不能\n静默覆盖产品或已授权 Plugin 资源，Plugin 也不能覆盖产品内置资源。\n\n## 2. 内部接口地图\n\n| Private symbol | 精确职责 | 不能承担 | 修改时同步检查 |\n| --- | --- | --- | --- |\n| `scan_root` | 稳定排序 root 的 direct children 并应用 first-wins precedence | 递归 marketplace discovery | duplicate/root-order tests、系统文档 |\n| `scan_dynamic_packages` | 稳定扫描 authority 给出的 exact package set | 读取 Marketplace、决定 enable/grant | generation/precedence tests、App Server provider tests |\n| `discover_package` | 校验 package/manifest identity 并建立 frozen snapshot | 解释 `contributes` | manifest、digest、file-type/limit tests |\n| `ExtensionPackageSnapshot::load` / `read_bounded_file` | 枚举 regular files、绑定 file identity、拒绝 link/special file、累计 limits 和 package digest | 暴露 canonical host path | symlink/hard-link/special/TOCTOU/digest tests |\n| `CatalogBudget` | 约束跨 root 的候选数、frozen bytes、manifest response 与 diagnostics | 改变 root precedence | 边界值、截断标记和原子 claim tests |\n| `validate_relative_path` | 校验 portable package-relative request path | canonicalize Workspace path | traversal/device/path-limit tests |\n| `is_within` | 检查已解析路径 containment | 定义 extension precedence | platform containment tests |\n| `mime_type` | 对少量静态资源后缀给出 transport hint | 内容嗅探或 Editor 解析 | resource tests |\n\n真实调用路径为：\n\n```text\nExtensionCatalog::list(Refresh)\n  -> ExtensionCatalog::scan\n  -> scan_root / scan_dynamic_packages\n  -> discover_package\n  -> frozen package files + ExtensionDescriptor\n  -> ExtensionCatalogSnapshot(generation)\n\nExtensionCatalog::open_resource(generation, id, path)\n  -> exact current-generation check\n  -> validate_relative_path\n  -> current frozen package snapshot lookup\n  -> ExtensionResource { mime_type, bytes }\n```\n\n如果本 crate 开始解释 grammar/snippet/theme、启动进程、拥有安装状态，或在资源读取时重新打开 live\npackage path，说明 ownership 已经漂移。\n\n## 3. 包、摘要与限制\n\n每个可信根的直接子目录是一个 package，必须包含 UTF-8 JSON `package.json`。必填身份字段为\n`name`、`publisher` 和 `version`，canonical ID 为 `publisher.name`。Descriptor 以 `manifest_sha256`\n校验暴露给客户端的 canonical `manifest_json` bytes，同时用确定性 `package_sha256` 绑定 package 内\n规范相对路径与全部原始 regular file bytes；digest 与 host absolute root 无关。\n\n当前 ingestion limits：\n\n| Limit | Value |\n| --- | ---: |\n| manifest bytes | 4 MiB |\n| single regular file | 16 MiB |\n| total package bytes | 64 MiB |\n| regular files per package | 4,096 |\n| total filesystem entries per package | 8,192 |\n| resource request path | 1,024 bytes |\n| package candidates per catalog | 4,096 |\n| frozen regular-file bytes per catalog | 256 MiB |\n| canonical manifest JSON per catalog response | 64 MiB |\n| diagnostics per catalog | 4,096 |\n| diagnostic text per catalog | 1 MiB |\n\nSymlink、hard link、special file、越界路径、非法身份或超限 package 整体失败，不发布部分 package。\nCatalog 达到总量上限后以 `ResourceTooLarge` diagnostic 跳过候选或停止继续枚举；diagnostic 自身达到\n上限时保留一个截断标记。Crate 只校验 envelope 并返回完整 manifest JSON；语言、TextMate、snippet、\ntheme 和 debugger 字段由产品 host 解释。\n\n## 4. Generation、失败与集成义务\n\n首次 `Cached` query 在没有 snapshot 时执行扫描；已有 snapshot 且 dynamic authority generation 未变时\n直接 clone 当前结果。dynamic generation 改变时，即使请求 `Cached` 也会重新扫描。每次\n`Refresh` 都产生新的单调 generation，并原子替换当前 discovered map。资源读取必须携带当前\ngeneration；旧代返回 `GenerationConflict`。Catalog 不保留多代 bytes，也不把旧 descriptor 隐式绑定\n到新磁盘内容。\n\n缺失可选 root 等价于空来源；存在但不可读的 root 产生 `SourceUnavailable`。单个无效 package 产生\n结构化 diagnostic 并跳过，其他有效 package 仍可发布。资源 ID/path 不存在、路径非法、资源超限或\n其他 IO failure 使用 typed `ExtensionCatalogError`；不会回退到工作区文件系统。\n\nHost 必须：\n\n- 只从 composition root 构造 `ExtensionRoot`，并保持 built-in-first precedence；\n- dynamic provider 只发布上游 authority 已选择的 exact immutable package 目录与单调 generation；\n- 把 snapshot generation 原样带到资源读取 API；\n- 把资源 bytes 放入自己的 connection/lifetime boundary，不暴露 package root；\n- 让产品领域解析 `contributes`，不能向本 crate 增加 editor-specific schema；\n- generation conflict 后重新 list 并重新准备整批贡献，不能只重试同一路径。\n\n## 5. 测试与修改影响\n\n运行：\n\n```text\ncargo test --manifest-path Cargo.toml -p zeta-extensions\ncargo clippy --manifest-path Cargo.toml -p zeta-extensions --all-targets --no-deps -- -D warnings\n```\n\n`catalog_tests.rs` 应覆盖 discovery、cached/refresh generation、built-in precedence、duplicate\ndiagnostic、package digest、完整 frozen-resource consistency、resource MIME、manifest/package/file\nlimits（包括 file/entry counts）、UTF-8、traversal、symlink/special file、缺失资源和\nstale-generation rejection。修改 wire DTO\n时还需运行 App Server protocol tests 和 Desktop unit tests；修改内置 package set 时还需运行 Python\n及 Node packaging tests。\n\n## 6. 当前限制与扩展点\n\nCurrent：静态 root discovery、Plugin-authorized exact package projection、built-in → Plugin → user\nprecedence、不可变单代 snapshot、完整 package digest、typed diagnostic/error 与有代次约束的 bounded\nresource read 已实现。当前不持久化 catalog，不保留旧 generation，也不自行提供远端 registry、下载、\nenable/disable、signature/revocation、permission grant 或任意代码执行；这些 lifecycle decisions 由\nprovider 上游的 `zeta-plugins` authority 完成。Plugin-authorized executable Editor Extension 由独立的\n[`zeta-editor-extension-host`](../editor-extension-host/README.md) 监管；静态 catalog 不会隐式进入该\n执行边界。\n\n未来若 Zeterm 需要同一静态 package 语义，应直接依赖本 crate；这只是明确的 extension point，当前\n仓库没有 Zeterm consumer。安装与信任控制面若需要演进，应与 `zeta-plugins` authority 明确对接，\n不得把 mutable download 或 runtime process 塞入 `ExtensionCatalog`。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与公共契约",
        "title": "1. 边界与公共契约"
      },
      {
        "depth": 2,
        "id": "2-内部接口地图",
        "title": "2. 内部接口地图"
      },
      {
        "depth": 2,
        "id": "3-包摘要与限制",
        "title": "3. 包、摘要与限制"
      },
      {
        "depth": 2,
        "id": "4-generation失败与集成义务",
        "title": "4. Generation、失败与集成义务"
      },
      {
        "depth": 2,
        "id": "5-测试与修改影响",
        "title": "5. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "6-当前限制与扩展点",
        "title": "6. 当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是静态扩展目录发现、不可变 package snapshot 与资源读取的实现权威文档。跨 App Server、Workbench 和 Editor 的用户语义、信任边界与长期演进由 docs/editor-extensions.md 维护；内置 package set 的来源和分发 规则见 extensions/README.md。 zeta-extensions 接收产品 host 选择的可信根以及 authority provider 选择的 exact immutable package 目录，把有效 package 冻结成 generation-bound 内存快照，并返回 manifest descriptor、诊断和有界 资源 bytes。它不拥有 JSON-RPC、connection resource、Workbench、TextMate、安装 authority 或扩展代码执行。 1. 边界与公共契约 Symbol 当前职责 不承担 --- --- --- ExtensionRoot / ExtensionRootKind 表达 host 已选择的 built-in/user root 从 Renderer 接收路径、Workspace trust DynamicExtensionSourceProvider / DynamicExtensionSourceSnapshot 按 authority generation 提供 exact immutable package 目录 Plugin install/enable/grant、下载或签名验证 ExtensionCatalog::list cached query 或 refresh scan，并发布单调 generation 解析 editor contributes ExtensionCatalogSnapshot 固定一代 descriptor、diagnostic 和完整 package identity 保留历史 generation ExtensionDescriptor publisher.name、版本、manifest JSON/摘要和 package 摘要 enablement、grant、signature ExtensionCatalog::openresource 校验 generation、ID 和包内相对路径后读取当前 frozen bytes 从 live filesystem 回退 ExtensionCatalogError generation conflict、缺失 ID/resource、非法路径、limit/IO failure transport error code 固定 precedence 为 built-in roots → dynamic Plugin-authorized exact packages → user roots；每个阶段中 第一个有效 extension ID 获胜，后续重复项只产生 DuplicateExtension diagnostic。由此可变用户包不能 静默覆盖产品或已授权 Plugin 资源，Plugin 也不能覆盖产品内置资源。 2. 内部接口地图 Private symbol 精确职责 不能承担 修改时同步检查 --- --- --- --- scanroot 稳定排序 root 的 direct children 并应用 first-wins precedence 递归 marketplace discovery duplicate/root-order tests、系统文档 scandynamicpackages 稳定扫描 authority 给出的 exact package set 读取 Marketplace、决定 enable/grant generation/precedence tests、App Server provider tests discoverpackage 校验 package/manifest identity 并建立 frozen snapshot 解释 contributes manifest、digest、file-type/limit tests ExtensionPackageSnapshot::load / readboundedfile 枚举 regular files、绑定 file identity、拒绝 link/special file、累计 limits 和 package digest 暴露 canonical host path symlink/hard-link/special/TOCTOU/digest tests CatalogBudget 约束跨 root 的候选数、frozen bytes、manifest response 与 diagnostics 改变 root precedence 边界值、截断标记和原子 claim tests validaterelativepath 校验 portable package-relative request path canonicalize Workspace path traversal/device/path-limit tests iswithin 检查已解析路径 containment 定义 extension precedence platform containment tests mimetype 对少量静态资源后缀给出 transport hint 内容嗅探或 Editor 解析 resource tests 真实调用路径为： ``text ExtensionCatalog::list Refresh - ExtensionCatalog::scan - scanroot / scandynamicpackages - discoverpackage - frozen package files + ExtensionDescriptor - ExtensionCatalogSnapshot generation ExtensionCatalog::openresource generation, id, path - exact current-generation check - validaterelativepath - current frozen package snapshot lookup - ExtensionResource mimetype, bytes ` 如果本 crate 开始解释 grammar/snippet/theme、启动进程、拥有安装状态，或在资源读取时重新打开 live package path，说明 ownership 已经漂移。 3. 包、摘要与限制 每个可信根的直接子目录是一个 package，必须包含 UTF-8 JSON package.json。必填身份字段为 name、publisher 和 version，canonical ID 为 publisher.name。Descriptor 以 manifestsha256 校验暴露给客户端的 canonical manifestjson bytes，同时用确定性 packagesha256 绑定 package 内 规范相对路径与全部原始 regular file bytes；digest 与 host absolute root 无关。 当前 ingestion limits： Limit Value --- ---: manifest bytes 4 MiB single regular file 16 MiB total package bytes 64 MiB regular files per package 4,096 total filesystem entries per package 8,192 resource request path 1,024 bytes package candidates per catalog 4,096 frozen regular-file bytes per catalog 256 MiB canonical manifest JSON per catalog response 64 MiB diagnostics per catalog 4,096 diagnostic text per catalog 1 MiB Symlink、hard link、special file、越界路径、非法身份或超限 package 整体失败，不发布部分 package。 Catalog 达到总量上限后以 ResourceTooLarge diagnostic 跳过候选或停止继续枚举；diagnostic 自身达到 上限时保留一个截断标记。Crate 只校验 envelope 并返回完整 manifest JSON；语言、TextMate、snippet、 theme 和 debugger 字段由产品 host 解释。 4. Generation、失败与集成义务 首次 Cached query 在没有 snapshot 时执行扫描；已有 snapshot 且 dynamic authority generation 未变时 直接 clone 当前结果。dynamic generation 改变时，即使请求 Cached 也会重新扫描。每次 Refresh 都产生新的单调 generation，并原子替换当前 discovered map。资源读取必须携带当前 generation；旧代返回 GenerationConflict。Catalog 不保留多代 bytes，也不把旧 descriptor 隐式绑定 到新磁盘内容。 缺失可选 root 等价于空来源；存在但不可读的 root 产生 SourceUnavailable。单个无效 package 产生 结构化 diagnostic 并跳过，其他有效 package 仍可发布。资源 ID/path 不存在、路径非法、资源超限或 其他 IO failure 使用 typed ExtensionCatalogError；不会回退到工作区文件系统。 Host 必须： - 只从 composition root 构造 ExtensionRoot，并保持 built-in-first precedence； - dynamic provider 只发布上游 authority 已选择的 exact immutable package 目录与单调 generation； - 把 snapshot generation 原样带到资源读取 API； - 把资源 bytes 放入自己的 connection/lifetime boundary，不暴露 package root； - 让产品领域解析 contributes，不能向本 crate 增加 editor-specific schema； - generation conflict 后重新 list 并重新准备整批贡献，不能只重试同一路径。 5. 测试与修改影响 运行： `text cargo test --manifest-path Cargo.toml -p zeta-extensions cargo clippy --manifest-path Cargo.toml -p zeta-extensions --all-targets --no-deps -- -D warnings ` catalogtests.rs 应覆盖 discovery、cached/refresh generation、built-in precedence、duplicate diagnostic、package digest、完整 frozen-resource consistency、resource MIME、manifest/package/file limits（包括 file/entry counts）、UTF-8、traversal、symlink/special file、缺失资源和 stale-generation rejection。修改 wire DTO 时还需运行 App Server protocol tests 和 Desktop unit tests；修改内置 package set 时还需运行 Python 及 Node packaging tests。 6. 当前限制与扩展点 Current：静态 root discovery、Plugin-authorized exact package projection、built-in → Plugin → user precedence、不可变单代 snapshot、完整 package digest、typed diagnostic/error 与有代次约束的 bounded resource read 已实现。当前不持久化 catalog，不保留旧 generation，也不自行提供远端 registry、下载、 enable/disable、signature/revocation、permission grant 或任意代码执行；这些 lifecycle decisions 由 provider 上游的 zeta-plugins authority 完成。Plugin-authorized executable Editor Extension 由独立的 zeta-editor-extension-host 监管；静态 catalog 不会隐式进入该 执行边界。 未来若 Zeterm 需要同一静态 package 语义，应直接依赖本 crate；这只是明确的 extension point，当前 仓库没有 Zeterm consumer。安装与信任控制面若需要演进，应与 zeta-plugins authority 明确对接， 不得把 mutable download 或 runtime process 塞入 ExtensionCatalog`。"
  },
  {
    "slug": "crates/file-icons",
    "sourcePath": "zeta-rs/file-icons/README.md",
    "title": "zeta-file-icons",
    "description": "zeta-file-icons 拥有 Rust 与 Desktop 共享的 Seti 文件图标资源，并为 Rust 客户端提供 manifest 校验和文件名解析能力。它不拥有 Explorer 状态、DOM 渲染、App Server RPC、终端 字体检测或 Nerd Font 二进制。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Seti manifest、资源所有权和文件名解析行为的实现权威文档。\n> Desktop 中的跨层归属见\n> [`docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md)。\n\n`zeta-file-icons` 拥有 Rust 与 Desktop 共享的 Seti 文件图标资源，并为 Rust 客户端提供\nmanifest 校验和文件名解析能力。它不拥有 Explorer 状态、DOM 渲染、App Server RPC、终端\n字体检测或 Nerd Font 二进制。\n\nSeti 是随仓库代码一起构建的内部资源，不是需要跨版本协商的网络协议。因此这里不生成 JSON\nSchema 或 TypeScript declarations；Desktop 直接从同步后的 JSON 推导其所需结构。\n\n## 目录与所有权\n\n```text\nzeta-rs/file-icons/\n├── src/\n│   ├── lib.rs\n│   ├── manifest.rs              # manifest shape、解析与引用校验\n│   └── resolver.rs              # 文件名到 Seti icon ID 的确定性解析\n└── seti/\n    ├── manifest.json            # Seti associations 与 browser glyph metadata\n    ├── seti.woff                # Desktop browser renderer 字体\n    └── LICENSE.txt\n```\n\n`seti/manifest.json` 是唯一匹配数据源。修改 associations 后必须通过 manifest validation 与\nresolver tests。Desktop 不得复制另一份 filename/extension association table。\n\n## 公共接口\n\n| Symbol | Contract |\n| --- | --- |\n| `SetiFileIconManifest` | manifest 根类型；拒绝未知字段 |\n| `parse_seti_manifest` | JSON parse 后校验所有 association 引用 |\n| `bundled_seti_manifest` | 惰性加载 checked-in manifest；资源损坏属于构建错误 |\n| `resolve_file_icon` | exact filename → longest compound extension → language → default |\n| `SetiColorScheme` | 显式选择 dark/light associations |\n\n`ResolvedSetiFileIcon` 返回稳定的 Seti icon ID 和 browser artwork。Rust TUI 后续应以 icon\nID 接入 terminal Seti codepoint adapter，而不是加载 WOFF。\n\n## 内部接口与调用路径\n\n| Symbol | 可见性 | 责任 |\n| --- | --- | --- |\n| `validate_associations` | private | 遍历一组 association 并保留出错键路径 |\n| `validate_reference` | private | 阻止 association 指向不存在的 icon definition |\n| `resolve_specific` | private | 执行 filename、compound extension、language 优先级 |\n| `extension_candidates` | private | 从最长复合扩展名开始产生候选项 |\n| `language_id_for_extension` | private | 补充 Seti language ID 与常见扩展名的别名 |\n\n```text\nparse_seti_manifest\n├── serde_json::from_str\n└── SetiFileIconManifest::validate\n    ├── validate_associations\n    └── validate_reference\n\nresolve_file_icon\n└── resolve_specific\n    ├── exact file name\n    ├── extension_candidates\n    └── language_id_for_extension\n```\n\nRenderer 可以拥有 glyph 绘制，但不能重新定义 Seti 匹配数据。当前 Rust 与 TypeScript\nadapter 各自实现同一解析顺序，并由两侧单元测试锁定关键 case。\n\n## Desktop 集成\n\n`desktop/scripts/sync-file-icons.mjs` 在 Desktop build、dev、test 和 Renderer typecheck\n之前把 manifest、WOFF 与 License 同步到 Desktop。也可以在仓库根目录单独执行：\n\n```text\ncorepack pnpm sync:file-icons\n```\n\n直接验证 Rust crate：\n\n```text\ncargo test --manifest-path Cargo.toml -p zeta-file-icons\ncargo fmt --manifest-path Cargo.toml -p zeta-file-icons -- --check\n```\n\n## 失败语义与当前限制\n\n- JSON syntax 或未知字段错误返回 `SetiManifestError::InvalidJson`。\n- association 指向缺失 definition 时返回带键路径的 `UnknownIconDefinition`。\n- `bundled_seti_manifest` 只在 checked-in 资源未通过校验时 panic。\n- manifest 包含 browser WOFF metadata，尚未包含 terminal Seti codepoint 或 ASCII\n  fallback；纯 Rust 终端渲染是扩展点，不是当前能力。\n- 真正接入 Rust TUI 后，应把两端解析 case 提升为共同消费的 conformance test data。",
    "headings": [
      {
        "depth": 2,
        "id": "目录与所有权",
        "title": "目录与所有权"
      },
      {
        "depth": 2,
        "id": "公共接口",
        "title": "公共接口"
      },
      {
        "depth": 2,
        "id": "内部接口与调用路径",
        "title": "内部接口与调用路径"
      },
      {
        "depth": 2,
        "id": "desktop-集成",
        "title": "Desktop 集成"
      },
      {
        "depth": 2,
        "id": "失败语义与当前限制",
        "title": "失败语义与当前限制"
      }
    ],
    "searchText": " 本 README 是 Seti manifest、资源所有权和文件名解析行为的实现权威文档。 Desktop 中的跨层归属见 docs/zeta-desktop-architecture.md。 zeta-file-icons 拥有 Rust 与 Desktop 共享的 Seti 文件图标资源，并为 Rust 客户端提供 manifest 校验和文件名解析能力。它不拥有 Explorer 状态、DOM 渲染、App Server RPC、终端 字体检测或 Nerd Font 二进制。 Seti 是随仓库代码一起构建的内部资源，不是需要跨版本协商的网络协议。因此这里不生成 JSON Schema 或 TypeScript declarations；Desktop 直接从同步后的 JSON 推导其所需结构。 目录与所有权 ``text zeta-rs/file-icons/ ├── src/ │ ├── lib.rs │ ├── manifest.rs manifest shape、解析与引用校验 │ └── resolver.rs 文件名到 Seti icon ID 的确定性解析 └── seti/ ├── manifest.json Seti associations 与 browser glyph metadata ├── seti.woff Desktop browser renderer 字体 └── LICENSE.txt ` seti/manifest.json 是唯一匹配数据源。修改 associations 后必须通过 manifest validation 与 resolver tests。Desktop 不得复制另一份 filename/extension association table。 公共接口 Symbol Contract --- --- SetiFileIconManifest manifest 根类型；拒绝未知字段 parsesetimanifest JSON parse 后校验所有 association 引用 bundledsetimanifest 惰性加载 checked-in manifest；资源损坏属于构建错误 resolvefileicon exact filename → longest compound extension → language → default SetiColorScheme 显式选择 dark/light associations ResolvedSetiFileIcon 返回稳定的 Seti icon ID 和 browser artwork。Rust TUI 后续应以 icon ID 接入 terminal Seti codepoint adapter，而不是加载 WOFF。 内部接口与调用路径 Symbol 可见性 责任 --- --- --- validateassociations private 遍历一组 association 并保留出错键路径 validatereference private 阻止 association 指向不存在的 icon definition resolvespecific private 执行 filename、compound extension、language 优先级 extensioncandidates private 从最长复合扩展名开始产生候选项 languageidforextension private 补充 Seti language ID 与常见扩展名的别名 `text parsesetimanifest ├── serdejson::fromstr └── SetiFileIconManifest::validate ├── validateassociations └── validatereference resolvefileicon └── resolvespecific ├── exact file name ├── extensioncandidates └── languageidforextension ` Renderer 可以拥有 glyph 绘制，但不能重新定义 Seti 匹配数据。当前 Rust 与 TypeScript adapter 各自实现同一解析顺序，并由两侧单元测试锁定关键 case。 Desktop 集成 desktop/scripts/sync-file-icons.mjs 在 Desktop build、dev、test 和 Renderer typecheck 之前把 manifest、WOFF 与 License 同步到 Desktop。也可以在仓库根目录单独执行： `text corepack pnpm sync:file-icons ` 直接验证 Rust crate： `text cargo test --manifest-path Cargo.toml -p zeta-file-icons cargo fmt --manifest-path Cargo.toml -p zeta-file-icons -- --check ` 失败语义与当前限制 - JSON syntax 或未知字段错误返回 SetiManifestError::InvalidJson。 - association 指向缺失 definition 时返回带键路径的 UnknownIconDefinition。 - bundledsetimanifest` 只在 checked-in 资源未通过校验时 panic。 - manifest 包含 browser WOFF metadata，尚未包含 terminal Seti codepoint 或 ASCII fallback；纯 Rust 终端渲染是扩展点，不是当前能力。 - 真正接入 Rust TUI 后，应把两端解析 case 提升为共同消费的 conformance test data。"
  },
  {
    "slug": "crates/file-identity",
    "sourcePath": "zeta-rs/file-identity/README.md",
    "title": "zeta-file-identity",
    "description": "zeta-file-identity 拥有从已打开文件读取稳定文件身份和硬链接数量的平台边界。信任决策需要把 后续读取绑定到同一文件系统对象时，调用方使用 FileInformation::fromfile；打开受控路径前 只需检查时，使用 FileInformation::frompath。",
    "group": "Crate 实现参考",
    "markdown": "`zeta-file-identity` 拥有从已打开文件读取稳定文件身份和硬链接数量的平台边界。信任决策需要把\n后续读取绑定到同一文件系统对象时，调用方使用 `FileInformation::from_file`；打开受控路径前\n只需检查时，使用 `FileInformation::from_path`。\n\n公共契约只暴露 `FileIdentity`、`FileInformation` 及其比较和链接数量访问器：\n\n- Unix 通过 `MetadataExt` 读取设备号、inode 和链接数量；\n- Windows 通过一个私有 FFI 边界调用 `GetFileInformationByHandle`，把卷序列号和文件索引\n  映射为相同的领域中立身份；\n- 操作系统错误保持为 `io::Error`；\n- 不支持的平台明确失败，不退化为路径字符串比较。\n\n```text\n受控路径\n└── FileInformation::from_path\n    └── File::open\n        └── FileInformation::from_file\n            └── platform::inspect\n                ├── Unix MetadataExt\n                └── Windows GetFileInformationByHandle\n```\n\n本 crate 不规范化路径、不拒绝符号链接、不决定是否允许硬链接、不监听修改，也不使用领域专用\n标志打开文件。这些义务仍属于调用方。把路径策略、Skill 语义或沙箱行为放进这里，意味着架构\n所有权已经漂移。\n\nSkills 目录是当前消费者：它拒绝多链接清单，并验证路径仍然指向已扫描内容所属的文件句柄。\n跨 crate 的所有权与信任语义见 [`../../docs/skills.md`](../../docs/skills.md)。\n\n当前限制：只支持 Zeta 使用的 Unix 与 Windows 主机系列。新增平台必须能够同时返回稳定身份和\n链接数量；退化为路径拼写或静默省略链接信息不符合本契约。\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-file-identity\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-file-identity --all-targets --no-deps -- -D warnings\n```",
    "headings": [
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": "zeta-file-identity 拥有从已打开文件读取稳定文件身份和硬链接数量的平台边界。信任决策需要把 后续读取绑定到同一文件系统对象时，调用方使用 FileInformation::fromfile；打开受控路径前 只需检查时，使用 FileInformation::frompath。 公共契约只暴露 FileIdentity、FileInformation 及其比较和链接数量访问器： - Unix 通过 MetadataExt 读取设备号、inode 和链接数量； - Windows 通过一个私有 FFI 边界调用 GetFileInformationByHandle，把卷序列号和文件索引 映射为相同的领域中立身份； - 操作系统错误保持为 io::Error； - 不支持的平台明确失败，不退化为路径字符串比较。 ``text 受控路径 └── FileInformation::frompath └── File::open └── FileInformation::fromfile └── platform::inspect ├── Unix MetadataExt └── Windows GetFileInformationByHandle ` 本 crate 不规范化路径、不拒绝符号链接、不决定是否允许硬链接、不监听修改，也不使用领域专用 标志打开文件。这些义务仍属于调用方。把路径策略、Skill 语义或沙箱行为放进这里，意味着架构 所有权已经漂移。 Skills 目录是当前消费者：它拒绝多链接清单，并验证路径仍然指向已扫描内容所属的文件句柄。 跨 crate 的所有权与信任语义见 ../../docs/skills.md。 当前限制：只支持 Zeta 使用的 Unix 与 Windows 主机系列。新增平台必须能够同时返回稳定身份和 链接数量；退化为路径拼写或静默省略链接信息不符合本契约。 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-file-identity cargo clippy --manifest-path Cargo.toml \\ -p zeta-file-identity --all-targets --no-deps -- -D warnings ``"
  },
  {
    "slug": "crates/file-search",
    "sourcePath": "zeta-rs/file-search/README.md",
    "title": "zeta-file-search",
    "description": "zeta-file-search 只拥有 workspace-relative 文件路径的后台索引、增量 fuzzy matching 和独立 CLI。它不读取候选文件内容，不注册模型 Tool，也不拥有 TUI popup/token 状态。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 workspace 文件路径 fuzzy search 的实现契约。TUI `@file` 交互由\n> [`zeta-code/tui/README.md`](../../zeta-code/tui/README.md) 维护。模型侧的文件内容搜索通过\n> [`zeta-rs/search/README.md`](../search/README.md) 完成；可执行文件的\n> discovery 与冻结边界见 [`zeta-rs/shell-command/README.md`](../shell-command/README.md)。\n\n`zeta-file-search` 只拥有 workspace-relative 文件路径的后台索引、增量 fuzzy matching 和独立\nCLI。它不读取候选文件内容，不注册模型 Tool，也不拥有 TUI popup/token 状态。\n\n## 文件与职责\n\n```text\nsrc/\n├── lib.rs                    # PathSearchHandle、worker、snapshot 与 public exports\n├── file_search_tests.rs      # 增量 query、ignore、排序和高亮索引\n├── main.rs                   # zeta-file-search 的薄进程入口与 exit status\n├── cli.rs                    # 参数、snapshot wait 与 stdout/stderr 输出\n└── cli_tests.rs              # CLI 参数、JSON 与 plain-text output\n```\n\n## 公共契约\n\n`PathSearchHandle::start(root, options)` 验证 root 是目录，返回后台搜索 handle 和\n`Receiver<PathSearchSnapshot>`：\n\n```text\nignore::WalkBuilder worker\n  └─ workspace-relative file path → Nucleo injector\n\nNucleo matcher worker\n  ├─ QueryChanged → incremental pattern reparse\n  ├─ injected path notification → tick\n  └─ PathSearchSnapshot → caller-owned receiver\n```\n\n`PathSearchHandle::update_query` 只更新 matcher pattern，不重启目录遍历。丢弃 handle 会设置\nshutdown 并唤醒 matcher；walker 在遍历 callback 中观察 shutdown。worker 是 detached thread，\n调用方不等待 join。\n\n`PathSearchSnapshot` 携带单调递增的 `query_revision`、query、按 score 降序且按 path 升序打破\n平局的前 N 项、匹配字符索引、扫描文件数以及 scan/search completion 状态。调用方必须同时检查\nrevision 和 query，避免输入从 A 变成 B 再回到 A 时接受第一次 A 的过期结果。\n\n路径 walker：\n\n- 使用 Git 作用域内的 `.gitignore`/ignore 语义；\n- 不跟随 symlink；\n- 显式跳过 `.git`、`.zeta`、`node_modules` 与 `target`；\n- 跳过非 UTF-8 relative path；\n- 只注入普通文件，不返回目录。\n\n## `zeta-file-search` CLI\n\n独立 binary 是 `PathSearchHandle` 的开发者/脚本入口，不是 TUI 启动的子进程：\n\n```bash\ncargo run --manifest-path Cargo.toml -p zeta-file-search -- src -C .\ncargo run --manifest-path Cargo.toml -p zeta-file-search -- \\\n  --json --compute-indices --limit 20 mention -C zeta-rs\n```\n\n| 参数 | 语义 |\n| --- | --- |\n| `[PATTERN]` | fuzzy pattern；省略时列出 workspace 文件 |\n| `-C, --cwd <DIR>` | 搜索 root；默认当前目录 |\n| `-l, --limit <N>` | 输出上限；默认 64 |\n| `--threads <N>` | walker 和 Nucleo worker 数；默认 2 |\n| `--json` | 每个 match 输出一行 JSON |\n| `--compute-indices` | JSON 包含 indices；TTY plain output 对命中字符加粗 |\n\nCLI 等待当前 `query_revision` 的 `search_complete` snapshot 后输出，因此不会显示中间结果。结果被\nlimit 截断时，warning 写入 stderr；JSON match 仍写入 stdout，方便逐行消费。省略 pattern 时仍\n通过 `PathSearchHandle` 列出文件，不回退执行 `ls` 或其他 shell 命令。\n\n## 内部接口地图\n\n| Symbol | 职责 | 不承担 |\n| --- | --- | --- |\n| `SearchInner` | 搜索 root、worker 配置、shutdown 与进度共享状态 | popup/query 生命周期 |\n| `walker_worker` | 遍历并向 Nucleo 注入 relative file path | 读取文件内容、排序结果 |\n| `matcher_worker` | 合并 query/walker/notify signal，驱动增量 tick | UI stale-result policy |\n| `build_snapshot` | 生成有界、稳定排序且带高亮索引的 immutable snapshot | 发送 UI event |\n| `cli::execute` | 等待 final snapshot 并选择 stdout/stderr 编码 | 扫描或 fuzzy matching |\n\n如果该 crate 开始读取候选文件内容、实现模型 Tool binding，或保存 TUI popup state，说明 ownership\n已经漂移；这些职责分别属于 `zeta-search`、Tool registry 和 `zeta-tui`。\n\n## 失败与取消\n\n- root 不存在或不是目录：`PathSearchHandle::start` 返回 `std::io::Error`，不启动 worker；\n- CLI 参数非法、root 不可用或 worker 在 completion 前退出：binary 输出带\n  `zeta-file-search:` 前缀的错误并返回非零状态；\n- 单个 walker entry 错误或非 UTF-8 path：跳过该 entry，搜索继续；\n- snapshot receiver 被丢弃：matcher 停止发送并退出；\n- handle 被丢弃：matcher 立即收到 shutdown，walker 在下一次 entry callback 停止。\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-file-search\ncargo clippy --manifest-path Cargo.toml -p zeta-file-search --all-targets --no-deps -- -D warnings\ncargo run --manifest-path Cargo.toml -p zeta-file-search -- --help\nbazel test //zeta-rs/file-search:file-search-unit-tests\nbazel build //zeta-rs/file-search:zeta-file-search\n```\n\n修改 Nucleo 配置、ignore 规则、排序或 snapshot completion 语义时，必须同步检查\n`file_search_tests.rs`、TUI file-search manager、mention renderer 和两层文档。",
    "headings": [
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "zeta-file-search-cli",
        "title": "zeta-file-search CLI"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "失败与取消",
        "title": "失败与取消"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 本 README 是 workspace 文件路径 fuzzy search 的实现契约。TUI @file 交互由 zeta-code/tui/README.md 维护。模型侧的文件内容搜索通过 zeta-rs/search/README.md 完成；可执行文件的 discovery 与冻结边界见 zeta-rs/shell-command/README.md。 zeta-file-search 只拥有 workspace-relative 文件路径的后台索引、增量 fuzzy matching 和独立 CLI。它不读取候选文件内容，不注册模型 Tool，也不拥有 TUI popup/token 状态。 文件与职责 ``text src/ ├── lib.rs PathSearchHandle、worker、snapshot 与 public exports ├── filesearchtests.rs 增量 query、ignore、排序和高亮索引 ├── main.rs zeta-file-search 的薄进程入口与 exit status ├── cli.rs 参数、snapshot wait 与 stdout/stderr 输出 └── clitests.rs CLI 参数、JSON 与 plain-text output ` 公共契约 PathSearchHandle::start root, options 验证 root 是目录，返回后台搜索 handle 和 Receiver<PathSearchSnapshot ： `text ignore::WalkBuilder worker └─ workspace-relative file path → Nucleo injector Nucleo matcher worker ├─ QueryChanged → incremental pattern reparse ├─ injected path notification → tick └─ PathSearchSnapshot → caller-owned receiver ` PathSearchHandle::updatequery 只更新 matcher pattern，不重启目录遍历。丢弃 handle 会设置 shutdown 并唤醒 matcher；walker 在遍历 callback 中观察 shutdown。worker 是 detached thread， 调用方不等待 join。 PathSearchSnapshot 携带单调递增的 queryrevision、query、按 score 降序且按 path 升序打破 平局的前 N 项、匹配字符索引、扫描文件数以及 scan/search completion 状态。调用方必须同时检查 revision 和 query，避免输入从 A 变成 B 再回到 A 时接受第一次 A 的过期结果。 路径 walker： - 使用 Git 作用域内的 .gitignore/ignore 语义； - 不跟随 symlink； - 显式跳过 .git、.zeta、nodemodules 与 target； - 跳过非 UTF-8 relative path； - 只注入普通文件，不返回目录。 zeta-file-search CLI 独立 binary 是 PathSearchHandle 的开发者/脚本入口，不是 TUI 启动的子进程： `bash cargo run --manifest-path Cargo.toml -p zeta-file-search -- src -C . cargo run --manifest-path Cargo.toml -p zeta-file-search -- \\ --json --compute-indices --limit 20 mention -C zeta-rs ` 参数 语义 --- --- PATTERN fuzzy pattern；省略时列出 workspace 文件 -C, --cwd <DIR 搜索 root；默认当前目录 -l, --limit <N 输出上限；默认 64 --threads <N walker 和 Nucleo worker 数；默认 2 --json 每个 match 输出一行 JSON --compute-indices JSON 包含 indices；TTY plain output 对命中字符加粗 CLI 等待当前 queryrevision 的 searchcomplete snapshot 后输出，因此不会显示中间结果。结果被 limit 截断时，warning 写入 stderr；JSON match 仍写入 stdout，方便逐行消费。省略 pattern 时仍 通过 PathSearchHandle 列出文件，不回退执行 ls 或其他 shell 命令。 内部接口地图 Symbol 职责 不承担 --- --- --- SearchInner 搜索 root、worker 配置、shutdown 与进度共享状态 popup/query 生命周期 walkerworker 遍历并向 Nucleo 注入 relative file path 读取文件内容、排序结果 matcherworker 合并 query/walker/notify signal，驱动增量 tick UI stale-result policy buildsnapshot 生成有界、稳定排序且带高亮索引的 immutable snapshot 发送 UI event cli::execute 等待 final snapshot 并选择 stdout/stderr 编码 扫描或 fuzzy matching 如果该 crate 开始读取候选文件内容、实现模型 Tool binding，或保存 TUI popup state，说明 ownership 已经漂移；这些职责分别属于 zeta-search、Tool registry 和 zeta-tui。 失败与取消 - root 不存在或不是目录：PathSearchHandle::start 返回 std::io::Error，不启动 worker； - CLI 参数非法、root 不可用或 worker 在 completion 前退出：binary 输出带 zeta-file-search: 前缀的错误并返回非零状态； - 单个 walker entry 错误或非 UTF-8 path：跳过该 entry，搜索继续； - snapshot receiver 被丢弃：matcher 停止发送并退出； - handle 被丢弃：matcher 立即收到 shutdown，walker 在下一次 entry callback 停止。 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-file-search cargo clippy --manifest-path Cargo.toml -p zeta-file-search --all-targets --no-deps -- -D warnings cargo run --manifest-path Cargo.toml -p zeta-file-search -- --help bazel test //zeta-rs/file-search:file-search-unit-tests bazel build //zeta-rs/file-search:zeta-file-search ` 修改 Nucleo 配置、ignore 规则、排序或 snapshot completion 语义时，必须同步检查 filesearchtests.rs`、TUI file-search manager、mention renderer 和两层文档。"
  },
  {
    "slug": "crates/file-system",
    "sourcePath": "zeta-rs/file-system/README.md",
    "title": "zeta-file-system",
    "description": "本 crate 拥有 workspace-scoped、consumer-neutral 的 filesystem contract。App Server 与 model tool adapter 都依赖它；本 crate 不依赖 JSON-RPC、Desktop、Tool schema 或 UI 状态。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 workspace filesystem primitive 的 canonical 实现文档。跨进程 ownership、\n> Desktop 投影与阶段状态见\n> [`../../docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md)。\n> Workspace identity、containment 与 trust 的 canonical ownership 见\n> [`../workspace/README.md`](../workspace/README.md)。\n\n本 crate 拥有 workspace-scoped、consumer-neutral 的 filesystem contract。App Server 与\nmodel tool adapter 都依赖它；本 crate 不依赖 JSON-RPC、Desktop、Tool schema 或 UI 状态。\n\n## 文件与公共契约\n\n| 文件 / symbol | 职责 |\n| --- | --- |\n| `service.rs` / `WorkspaceFileSystem` | consumer 共享 trait；实现必须约束所有输入到 authority root |\n| `local.rs` / `LocalFileSystem` | 当前 local implementation；组合 `zeta_workspace::WorkspaceRoot` 与宿主 I/O |\n| `types.rs` | `FileType`、`FileMetadata`、`DirectoryEntry`、`FileContent`、`FileWriteCondition` 与 content revision helper；不携带 wire/UI DTO |\n| `error.rs` / `FileSystemError` | path、文件/目录类型、读写上限、revision conflict、只读状态与 I/O failure |\n\n`WorkspaceFileSystem::{read_file,read_file_with_revision,write_file,write_file_with_condition,get_metadata,read_directory}` 都接收相对路径。\n`read_file` 的 limit 单位是 bytes，不做 UTF-8 解码；内容解释属于调用方。\n`write_file` 同样使用 byte limit，在现有父目录内原子替换或新建普通文件，并保留被替换文件\n的权限；它不隐式创建目录。\n`read_file_with_revision` 以当前字节的 SHA-256 返回 opaque revision；调用方不应把 hash\n算法当作业务协议。`write_file_with_condition(..., ExpectedRevision)` 只接受与读取版本一致\n的写入，并以 `RevisionConflict` 明确拒绝过期写入；`Unconditional` 只用于有意不依赖先前\n读取的 create/overwrite 操作。\n`read_directory` 只返回直接子项，并按 lossily represented child name 排序。\n\n## 执行与可信边界\n\n```text\nApp Server 或 Tool adapter\n→ WorkspaceFileSystem method\n→ LocalFileSystem::resolve_existing / resolve_for_write\n→ WorkspaceRoot containment\n→ canonicalize + root containment\n→ std::fs I/O\n```\n\n`LocalFileSystem::resolve_existing` 是关键 private boundary。它必须在 I/O 前拒绝 absolute\npath、parent traversal、missing path 和解析后越过 root 的 symlink。调用方在 IPC 或 Tool\nschema 层做的格式校验只用于快速失败，不能替代这里的 authority check。\n\n| Private symbol | 当前职责 | 架构漂移信号 |\n| --- | --- | --- |\n| `LocalFileSystem::resolve_existing` | 解析并约束读取目标 | 调用方绕过它直接拼接 workspace path |\n| `LocalFileSystem::resolve_for_write` | 约束现有或待创建文件及最近现有父目录 | mutation 在 authority check 前执行 I/O |\n| `local::atomic_write` | 同目录临时写、权限复制、flush、replace 与父目录 sync | 开始决定 RPC、dirty state 或冲突 UI |\n| `LocalFileSystem::write_file_with_condition` | 在本进程 filesystem instance 的写锁内检查 revision 并替换 | 在此 crate 中复制 App Server/UI conflict policy |\n| `local::metadata` | host metadata → consumer-neutral `FileMetadata` | 返回 wire DTO 或 Renderer identity |\n\n`read_file` 最多读取 `maximum_bytes + 1` 来可靠检测 overflow；limit 为零也失败。\n`write_file` 在打开临时文件前检查完整 content byte length，向目标同目录写入并 `sync_all`，\n再原子替换目标及同步父目录。现有普通文件的权限会复制到临时文件；只读文件、目录、缺失父\n目录和越过 root 的 symlink 都会失败。\n`LocalFileSystem` 把普通与条件写入串行化，因此同一 instance 的并发 writer 不会同时通过\n一个旧 revision。这个锁不是跨进程文件锁：独立的宿主进程仍可能在检查与替换之间修改文件；\nApp Server watcher 负责把后续外部变化投影为 invalidation。\n`get_metadata` 返回 millisecond Unix timestamp（宿主不提供或早于 epoch 时为 `None`）。\n错误可能包含宿主 I/O 诊断；App Server 必须在 external boundary 映射为稳定错误，不得直接\n回显。\n\n## 接入、测试与修改影响\n\nLocal host 通过 `WorkspaceRoot::open` 构造 `LocalFileSystem`，再以\n`Arc<dyn WorkspaceFileSystem>` 注入 consumer。remote implementation 可以实现同一 trait，\n但必须提供等价的 root confinement；trait 本身无法从类型系统证明这一义务。\n\n```text\ncargo test -p zeta-file-system\nbazel test //zeta-rs/file-system:file-system-unit-tests\n```\n\n修改类型或 trait 时同步检查 `zeta-file-system-tool`、`zeta-app-server` 与\n`zeta-app-server-protocol`。把 Tool schema、RPC DTO 或 Renderer URI 塞入本 crate，或绕过\n`WorkspaceRoot` 直接拼接不可信路径，都表示 ownership 漂移。\n\n## 当前限制与扩展点\n\n- Current：只有 in-process local implementation；具备有界原子文件写入及 instance-local\n  expected-revision write，不包含跨进程 compare-and-swap、rename/delete。\n- Current：watcher 是独立的 invalidation primitive，不进入 filesystem trait；App Server 负责\n  组合二者并发布产品 notification。\n- Current：non-UTF-8 directory entry name 使用 lossy conversion。\n- Extension point：remote backend、mutation、watcher 需要先定义独立 failure、ordering 与\n  invalidation contract，不能通过扩大当前 read method 的隐含语义实现。",
    "headings": [
      {
        "depth": 2,
        "id": "文件与公共契约",
        "title": "文件与公共契约"
      },
      {
        "depth": 2,
        "id": "执行与可信边界",
        "title": "执行与可信边界"
      },
      {
        "depth": 2,
        "id": "接入测试与修改影响",
        "title": "接入、测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 workspace filesystem primitive 的 canonical 实现文档。跨进程 ownership、 Desktop 投影与阶段状态见 ../../docs/zeta-desktop-architecture.md。 Workspace identity、containment 与 trust 的 canonical ownership 见 ../workspace/README.md。 本 crate 拥有 workspace-scoped、consumer-neutral 的 filesystem contract。App Server 与 model tool adapter 都依赖它；本 crate 不依赖 JSON-RPC、Desktop、Tool schema 或 UI 状态。 文件与公共契约 文件 / symbol 职责 --- --- service.rs / WorkspaceFileSystem consumer 共享 trait；实现必须约束所有输入到 authority root local.rs / LocalFileSystem 当前 local implementation；组合 zetaworkspace::WorkspaceRoot 与宿主 I/O types.rs FileType、FileMetadata、DirectoryEntry、FileContent、FileWriteCondition 与 content revision helper；不携带 wire/UI DTO error.rs / FileSystemError path、文件/目录类型、读写上限、revision conflict、只读状态与 I/O failure WorkspaceFileSystem:: readfile,readfilewithrevision,writefile,writefilewithcondition,getmetadata,readdirectory 都接收相对路径。 readfile 的 limit 单位是 bytes，不做 UTF-8 解码；内容解释属于调用方。 writefile 同样使用 byte limit，在现有父目录内原子替换或新建普通文件，并保留被替换文件 的权限；它不隐式创建目录。 readfilewithrevision 以当前字节的 SHA-256 返回 opaque revision；调用方不应把 hash 算法当作业务协议。writefilewithcondition ..., ExpectedRevision 只接受与读取版本一致 的写入，并以 RevisionConflict 明确拒绝过期写入；Unconditional 只用于有意不依赖先前 读取的 create/overwrite 操作。 readdirectory 只返回直接子项，并按 lossily represented child name 排序。 执行与可信边界 ``text App Server 或 Tool adapter → WorkspaceFileSystem method → LocalFileSystem::resolveexisting / resolveforwrite → WorkspaceRoot containment → canonicalize + root containment → std::fs I/O ` LocalFileSystem::resolveexisting 是关键 private boundary。它必须在 I/O 前拒绝 absolute path、parent traversal、missing path 和解析后越过 root 的 symlink。调用方在 IPC 或 Tool schema 层做的格式校验只用于快速失败，不能替代这里的 authority check。 Private symbol 当前职责 架构漂移信号 --- --- --- LocalFileSystem::resolveexisting 解析并约束读取目标 调用方绕过它直接拼接 workspace path LocalFileSystem::resolveforwrite 约束现有或待创建文件及最近现有父目录 mutation 在 authority check 前执行 I/O local::atomicwrite 同目录临时写、权限复制、flush、replace 与父目录 sync 开始决定 RPC、dirty state 或冲突 UI LocalFileSystem::writefilewithcondition 在本进程 filesystem instance 的写锁内检查 revision 并替换 在此 crate 中复制 App Server/UI conflict policy local::metadata host metadata → consumer-neutral FileMetadata 返回 wire DTO 或 Renderer identity readfile 最多读取 maximumbytes + 1 来可靠检测 overflow；limit 为零也失败。 writefile 在打开临时文件前检查完整 content byte length，向目标同目录写入并 syncall， 再原子替换目标及同步父目录。现有普通文件的权限会复制到临时文件；只读文件、目录、缺失父 目录和越过 root 的 symlink 都会失败。 LocalFileSystem 把普通与条件写入串行化，因此同一 instance 的并发 writer 不会同时通过 一个旧 revision。这个锁不是跨进程文件锁：独立的宿主进程仍可能在检查与替换之间修改文件； App Server watcher 负责把后续外部变化投影为 invalidation。 getmetadata 返回 millisecond Unix timestamp（宿主不提供或早于 epoch 时为 None）。 错误可能包含宿主 I/O 诊断；App Server 必须在 external boundary 映射为稳定错误，不得直接 回显。 接入、测试与修改影响 Local host 通过 WorkspaceRoot::open 构造 LocalFileSystem，再以 Arc<dyn WorkspaceFileSystem 注入 consumer。remote implementation 可以实现同一 trait， 但必须提供等价的 root confinement；trait 本身无法从类型系统证明这一义务。 `text cargo test -p zeta-file-system bazel test //zeta-rs/file-system:file-system-unit-tests ` 修改类型或 trait 时同步检查 zeta-file-system-tool、zeta-app-server 与 zeta-app-server-protocol。把 Tool schema、RPC DTO 或 Renderer URI 塞入本 crate，或绕过 WorkspaceRoot` 直接拼接不可信路径，都表示 ownership 漂移。 当前限制与扩展点 - Current：只有 in-process local implementation；具备有界原子文件写入及 instance-local expected-revision write，不包含跨进程 compare-and-swap、rename/delete。 - Current：watcher 是独立的 invalidation primitive，不进入 filesystem trait；App Server 负责 组合二者并发布产品 notification。 - Current：non-UTF-8 directory entry name 使用 lossy conversion。 - Extension point：remote backend、mutation、watcher 需要先定义独立 failure、ordering 与 invalidation contract，不能通过扩大当前 read method 的隐含语义实现。"
  },
  {
    "slug": "crates/file-system-tool",
    "sourcePath": "zeta-rs/file-system-tool/README.md",
    "title": "zeta-file-system-工具",
    "description": "本 crate 只把 zeta-file-system 适配为 model-visible file-system Tool。它拥有 Tool definition、binding 校验、UTF-8 text 解码和输出上限；不拥有宿主 filesystem access、 workspace confinement、App Server RPC、mutation 或 w…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 model-visible filesystem adapter 的 canonical 实现文档。共享 filesystem\n> authority 见 [`../file-system/README.md`](../file-system/README.md)，跨客户端 ownership\n> 见 [`../../docs/zeta-desktop-architecture.md`](../../docs/zeta-desktop-architecture.md)。\n\n本 crate 只把 `zeta-file-system` 适配为 model-visible `file-system` Tool。它拥有 Tool\ndefinition、binding 校验、UTF-8 text 解码和输出上限；不拥有宿主 filesystem access、\nworkspace confinement、App Server RPC、mutation 或 watcher。\n\n## 公共契约与内部接口\n\n| Symbol | 可见性 | 职责 |\n| --- | --- | --- |\n| `FileSystemTool` | public | 持有 environment binding、共享 filesystem、limits 与 immutable definition |\n| `FileSystemLimits` | public | 非零 read/list bounds；默认 64 KiB 与 1,000 direct entries |\n| `FileSystemToolError` | public | construction-time limit/definition failure |\n| `FileSystemInput` / `FileSystemOperation` | private | deny-unknown-fields 的 `read`、`list`、`metadata` input |\n| `validate_invocation` | private | cancellation、environment、exposed name 与 definition digest binding |\n| `file_system_definition` | private | provider-facing function schema |\n| `returned_json` / `returned_error` | private | `ToolExecutionOutcome` 编码与 success/error 分类 |\n\n调用路径：\n\n```text\nToolExecutor::execute\n→ FileSystemTool::run\n→ validate_invocation\n→ decode_arguments<FileSystemInput>\n→ read | list | metadata\n→ Arc<dyn WorkspaceFileSystem>\n→ ToolExecutionOutcome\n```\n\n`read` 要求 foundation 返回的 bytes 是有效 UTF-8 text；binary content 返回 Tool error。\n`list` 在 foundation 已排序的 direct children 上截断并返回 `truncated`。`metadata` 不返回\n`modified_at_millis`，因为当前 Tool output contract 未声明该字段。\n\n## 失败、接入与验证\n\n取消只在执行开始前观察。binding/environment mismatch 返回 `NotStarted`；输入、filesystem、\nUTF-8 与编码 failure 返回正常完成的 error `ToolOutput`。底层 filesystem error 会进入\nAgent-visible Tool error，不能把该适配器直接复用为 external client error boundary。\n\nHost 必须显式创建 `FileSystemTool` 并注册到对应 environment 的 Tool registry；crate 存在\n不代表当前 runtime 已自动暴露该 Tool。\n\n```text\ncargo test -p zeta-file-system-tool\nbazel test //zeta-rs/file-system-tool:file-system-tool-unit-tests\n```\n\n修改 operation/schema 时同步更新 schema binding tests；修改 foundation trait 时同步检查\n`zeta-file-system` 与 App Server adapter。若本 crate 开始直接调用 `std::fs`、持有\n`WorkspaceRoot`、实现 client RPC 或维护目录展开/刷新状态，即表示 ownership 漂移。\n\n## 当前限制与潜在方向\n\n- Current：只读 text/list/metadata，单次同步 foundation call。\n- Current：list 最多输出 configured direct entries，不分页。\n- Potential：binary/resource output、streaming 或 richer metadata 需要先扩展 Tool output\n  contract；这些不是当前能力承诺。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约与内部接口",
        "title": "公共契约与内部接口"
      },
      {
        "depth": 2,
        "id": "失败接入与验证",
        "title": "失败、接入与验证"
      },
      {
        "depth": 2,
        "id": "当前限制与潜在方向",
        "title": "当前限制与潜在方向"
      }
    ],
    "searchText": " 本 README 是 model-visible filesystem adapter 的 canonical 实现文档。共享 filesystem authority 见 ../file-system/README.md，跨客户端 ownership 见 ../../docs/zeta-desktop-architecture.md。 本 crate 只把 zeta-file-system 适配为 model-visible file-system Tool。它拥有 Tool definition、binding 校验、UTF-8 text 解码和输出上限；不拥有宿主 filesystem access、 workspace confinement、App Server RPC、mutation 或 watcher。 公共契约与内部接口 Symbol 可见性 职责 --- --- --- FileSystemTool public 持有 environment binding、共享 filesystem、limits 与 immutable definition FileSystemLimits public 非零 read/list bounds；默认 64 KiB 与 1,000 direct entries FileSystemToolError public construction-time limit/definition failure FileSystemInput / FileSystemOperation private deny-unknown-fields 的 read、list、metadata input validateinvocation private cancellation、environment、exposed name 与 definition digest binding filesystemdefinition private provider-facing function schema returnedjson / returnederror private ToolExecutionOutcome 编码与 success/error 分类 调用路径： ``text ToolExecutor::execute → FileSystemTool::run → validateinvocation → decodearguments<FileSystemInput → read list metadata → Arc<dyn WorkspaceFileSystem → ToolExecutionOutcome ` read 要求 foundation 返回的 bytes 是有效 UTF-8 text；binary content 返回 Tool error。 list 在 foundation 已排序的 direct children 上截断并返回 truncated。metadata 不返回 modifiedatmillis，因为当前 Tool output contract 未声明该字段。 失败、接入与验证 取消只在执行开始前观察。binding/environment mismatch 返回 NotStarted；输入、filesystem、 UTF-8 与编码 failure 返回正常完成的 error ToolOutput。底层 filesystem error 会进入 Agent-visible Tool error，不能把该适配器直接复用为 external client error boundary。 Host 必须显式创建 FileSystemTool 并注册到对应 environment 的 Tool registry；crate 存在 不代表当前 runtime 已自动暴露该 Tool。 `text cargo test -p zeta-file-system-tool bazel test //zeta-rs/file-system-tool:file-system-tool-unit-tests ` 修改 operation/schema 时同步更新 schema binding tests；修改 foundation trait 时同步检查 zeta-file-system 与 App Server adapter。若本 crate 开始直接调用 std::fs、持有 WorkspaceRoot`、实现 client RPC 或维护目录展开/刷新状态，即表示 ownership 漂移。 当前限制与潜在方向 - Current：只读 text/list/metadata，单次同步 foundation call。 - Current：list 最多输出 configured direct entries，不分页。 - Potential：binary/resource output、streaming 或 richer metadata 需要先扩展 Tool output contract；这些不是当前能力承诺。"
  },
  {
    "slug": "crates/file-watcher",
    "sourcePath": "zeta-rs/file-watcher/README.md",
    "title": "zeta-file-watcher",
    "description": "zeta-file-watcher 提供基于 notify 的多订阅者路径监听、共享 backend watch ref-count、 缺失路径回退、RAII 注销和异步事件合并。它只报告“可能发生了变化”；消费者必须重新读取并验证 自己拥有的状态，不能把 backend event 当作文件或 catalog 事实。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是共享 filesystem invalidation hint 的实现契约。Skill catalog 的扫描、校验与\n> snapshot 发布语义由 [`docs/skills.md`](../../docs/skills.md) 维护；Git status 的重新查询、\n> revision 与 notification 语义由 [`docs/git.md`](../../docs/git.md) 维护；workspace\n> `fs/changed` 的 root-relative 投影由\n> [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md#文件系统) 维护。\n\n`zeta-file-watcher` 提供基于 `notify` 的多订阅者路径监听、共享 backend watch ref-count、\n缺失路径回退、RAII 注销和异步事件合并。它只报告“可能发生了变化”；消费者必须重新读取并验证\n自己拥有的状态，不能把 backend event 当作文件或 catalog 事实。\n\nmacOS 默认使用 `notify` 的 FSEvents backend。workspace watcher 会递归覆盖整棵工作区；不能改用\nkqueue，因为它会为大量 watched entry 持有 file descriptor，并在大型工作区耗尽进程资源。\n当宿主确认 requested/canonical namespace 不同（例如 `/var` 与 `/private/var`）时，可显式选择\n`FileWatcherBackend::Polling`；该 fallback 只用于 alias root，不是所有 Workspace 的默认策略。\nhermetic Bazel toolchain 必须提供 FSEvents 所需的 `CoreServices` framework 链接边界；\nLinux/Windows 继续使用 `notify` 的推荐平台 backend。\n\n## 文件与职责\n\n```text\nsrc/\n├── lib.rs                 # public exports、WatchPath 与 FileWatcherEvent\n├── channel.rs             # lossless pending set、Receiver、throttle 与 debounce\n├── registration.rs        # subscriber registration 与 RAII guard\n├── state.rs               # private ref-count 和 per-subscriber state\n├── matching.rs            # canonical namespace、missing-path fallback 与 scope matching\n├── watcher.rs             # notify backend、watch reconfiguration 与 subscriber routing\n└── file_watcher_tests.rs  # channel、scope、fallback、RAII、overflow 与 live backend tests\n```\n\n所有实现模块均为 private；crate 只显式导出 `FileWatcher`、subscriber/registration handle、\nreceiver wrapper、`WatchPath` 和 `FileWatcherEvent`。\n\n## 公共契约\n\n`FileWatcher::new()` 必须在当前 Tokio runtime 内调用。成功后，一个 OS watcher 被所有逻辑\nsubscriber 共享：\n\n```text\nnotify callback\n  → Tokio event loop\n  → mutation filter / backend-error escalation\n  → per-subscriber path matching\n  → sorted + deduplicated pending hint\n  → Receiver / ThrottledWatchReceiver / DebouncedWatchReceiver\n```\n\n`Arc<FileWatcher>::add_subscriber()` 返回独立的 `FileWatcherSubscriber` 和 `Receiver`。\n`FileWatcherSubscriber::register_paths()` 接受一组 `WatchPath`，成功时返回\n`WatchRegistration`，backend 注册失败时返回 `notify::Error`：\n\n- 同一批次内重复的 exact path + recursive scope 会去重；\n- 跨 registration/subscriber 的相同 OS path 通过 `PathWatchCounts` ref-count；\n- 任意 recursive consumer 存在时 backend 使用 recursive mode；最后一个 recursive guard\n  释放后降级为 non-recursive；\n- `WatchRegistration` drop 只撤销该批注册；subscriber drop 撤销其全部注册并关闭 receiver；\n- `FileWatcher::noop()` 保留注册/生命周期语义但不连接 OS backend，用于明确的 optional-runtime\n  fallback；它不会自行产生事件。\n- `FileWatcher::new_with_backend()` 允许 authority owner 显式选择 recommended 或 polling；\n  不允许 backend failure 静默变成 noop。\n\n### Event 语义\n\n| Event | 含义 | Consumer obligation |\n| --- | --- | --- |\n| `PathsChanged { paths }` | backend 观察到这些 subscriber-visible path 附近发生 mutation | 对受影响 scope 重新扫描并校验 |\n| `RescanRequired { watched_paths }` | backend 报错，事件可能丢失 | 对列出的已注册 roots 做 scoped full rescan |\n\n两种 event 都是 hint。`PathsChanged` 不证明路径仍存在、内容有效或 mutation 已稳定；rename 也可能\n产生多个 backend event。`RescanRequired` 会覆盖同一 receiver 中尚未消费的 path-level hints。\n\n`ThrottledWatchReceiver` 在第一次事件后限制最小发送间隔，适合需要持续刷新但限制频率的 manager。\n`DebouncedWatchReceiver` 从第一条事件起收集固定窗口，适合一次 burst 后重建 snapshot。两者都保留\nshutdown 前已 pending 的事件。\n\n## Path 与 backend 边界\n\n`matching::actual_watch_path` 为已存在目标保存 subscriber 请求路径，同时使用 canonical path\n匹配可能被 OS 重写的 event namespace。缺失目标不会要求 broad recursive ancestor watch：\n\n1. 从最近的现有 directory ancestor 建立 non-recursive watch；\n2. ancestor event 只在目标出现/消失时映射回 subscriber 请求路径；\n3. 中间 component 出现后，`watcher::apply_actual_watch_move` 把 ref-count 和 backend watch\n   移到更近的路径；\n4. consumer 最终看到原始请求 namespace，而不是 `/private/var` 等 canonicalization artifact。\n\nBackend 只转发 create、modify 和 remove；access/open 事件被过滤。所有 backend callback error\n都按可能丢事件处理，并通过 `watcher::require_rescan` 向每个 subscriber 发送其 own watched roots。\n\n## 失败、并发与关闭\n\n- `FileWatcher::new()` 在 backend 初始化失败或当前线程没有 Tokio runtime 时返回\n  `notify::Error`；\n- registration failure 同步返回；调用方必须选择显式失败、诊断或可解释的 manual-refresh\n  fallback；\n- 单个 backend watch/unwatch reconfiguration 失败会写 warning；现有注册状态仍保留，后续\n  reconfiguration 可再次尝试；\n- state lock 与 backend lock 始终按 state → backend 顺序获取，注册、注销和 watch mode\n  reconfiguration 对其他操作呈一致顺序；\n- receiver 不使用有界 mpsc 丢弃 path；pending path 存入 `BTreeSet`，因此批次稳定排序并去重；\n- `FileWatcher` drop 释放 OS watcher sender，后台 loop 在 channel 关闭后退出；\n- watcher 不读取文件、不持有 catalog snapshot、不自动 retry/rescan，也不发布产品 update。\n\n注册时单个 backend `watch()` 失败当前只能记录 warning，不能同步回传给已经返回 guard 的 caller；\n需要强一致启动的 manager 必须在首次 full scan 后保留显式 refresh/fallback 路径。这是当前限制，\n不能把“registration 已进入 ref-count state”解释为 OS enforcement 已确认。\n\n## 内部接口地图\n\n| Symbol | 拥有 | 架构漂移信号 |\n| --- | --- | --- |\n| `WatchState` / `PathWatchCounts` | subscriber state 与共享 backend scope ref-count | 开始保存 consumer snapshot/content |\n| `actual_watch_path` / `changed_path_for_event` | fallback、canonical matching 与请求 namespace 恢复 | 开始解释 catalog 或 workspace 业务语义 |\n| `FileWatcher::notify_subscribers` | raw mutation 到 subscriber hint 的 routing | 直接发布 App Server/product event |\n| `WatchSender` / `PendingEvent` | 每个 subscriber 的去重 pending 状态 | 引入 durable queue/replay contract |\n| throttle/debounce wrappers | delivery cadence | 决定 manager 的扫描或 snapshot generation |\n\n如果需要 ignore rules、fuzzy index 或文件内容检索，应分别由 consumer、`zeta-file-search` 或\n`zeta-shell-command`/`rg` 负责，不应扩张本 crate。\n\n## 验证与修改影响\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-file-watcher\ncargo clippy --manifest-path Cargo.toml -p zeta-file-watcher \\\n  --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/file-watcher:file-watcher-unit-tests\n```\n\n修改 path normalization、recursive mode、missing-target fallback、backend error 或 event\ncoalescing 时，必须同步检查 `file_watcher_tests.rs`、本 README、\n[`docs/skills.md`](../../docs/skills.md) 与 [`docs/git.md`](../../docs/git.md) 的 snapshot\ninvalidation 语义。接入新 consumer 时应把\n其 owner 文档链接回本 README，而不是复制 backend 细节。",
    "headings": [
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 3,
        "id": "event-语义",
        "title": "Event 语义"
      },
      {
        "depth": 2,
        "id": "path-与-backend-边界",
        "title": "Path 与 backend 边界"
      },
      {
        "depth": 2,
        "id": "失败并发与关闭",
        "title": "失败、并发与关闭"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      }
    ],
    "searchText": " 本 README 是共享 filesystem invalidation hint 的实现契约。Skill catalog 的扫描、校验与 snapshot 发布语义由 docs/skills.md 维护；Git status 的重新查询、 revision 与 notification 语义由 docs/git.md 维护；workspace fs/changed 的 root-relative 投影由 docs/zeta-app-server-api.md 维护。 zeta-file-watcher 提供基于 notify 的多订阅者路径监听、共享 backend watch ref-count、 缺失路径回退、RAII 注销和异步事件合并。它只报告“可能发生了变化”；消费者必须重新读取并验证 自己拥有的状态，不能把 backend event 当作文件或 catalog 事实。 macOS 默认使用 notify 的 FSEvents backend。workspace watcher 会递归覆盖整棵工作区；不能改用 kqueue，因为它会为大量 watched entry 持有 file descriptor，并在大型工作区耗尽进程资源。 当宿主确认 requested/canonical namespace 不同（例如 /var 与 /private/var）时，可显式选择 FileWatcherBackend::Polling；该 fallback 只用于 alias root，不是所有 Workspace 的默认策略。 hermetic Bazel toolchain 必须提供 FSEvents 所需的 CoreServices framework 链接边界； Linux/Windows 继续使用 notify 的推荐平台 backend。 文件与职责 ``text src/ ├── lib.rs public exports、WatchPath 与 FileWatcherEvent ├── channel.rs lossless pending set、Receiver、throttle 与 debounce ├── registration.rs subscriber registration 与 RAII guard ├── state.rs private ref-count 和 per-subscriber state ├── matching.rs canonical namespace、missing-path fallback 与 scope matching ├── watcher.rs notify backend、watch reconfiguration 与 subscriber routing └── filewatchertests.rs channel、scope、fallback、RAII、overflow 与 live backend tests ` 所有实现模块均为 private；crate 只显式导出 FileWatcher、subscriber/registration handle、 receiver wrapper、WatchPath 和 FileWatcherEvent。 公共契约 FileWatcher::new 必须在当前 Tokio runtime 内调用。成功后，一个 OS watcher 被所有逻辑 subscriber 共享： `text notify callback → Tokio event loop → mutation filter / backend-error escalation → per-subscriber path matching → sorted + deduplicated pending hint → Receiver / ThrottledWatchReceiver / DebouncedWatchReceiver ` Arc<FileWatcher ::addsubscriber 返回独立的 FileWatcherSubscriber 和 Receiver。 FileWatcherSubscriber::registerpaths 接受一组 WatchPath，成功时返回 WatchRegistration，backend 注册失败时返回 notify::Error： - 同一批次内重复的 exact path + recursive scope 会去重； - 跨 registration/subscriber 的相同 OS path 通过 PathWatchCounts ref-count； - 任意 recursive consumer 存在时 backend 使用 recursive mode；最后一个 recursive guard 释放后降级为 non-recursive； - WatchRegistration drop 只撤销该批注册；subscriber drop 撤销其全部注册并关闭 receiver； - FileWatcher::noop 保留注册/生命周期语义但不连接 OS backend，用于明确的 optional-runtime fallback；它不会自行产生事件。 - FileWatcher::newwithbackend 允许 authority owner 显式选择 recommended 或 polling； 不允许 backend failure 静默变成 noop。 Event 语义 Event 含义 Consumer obligation --- --- --- PathsChanged paths backend 观察到这些 subscriber-visible path 附近发生 mutation 对受影响 scope 重新扫描并校验 RescanRequired watchedpaths backend 报错，事件可能丢失 对列出的已注册 roots 做 scoped full rescan 两种 event 都是 hint。PathsChanged 不证明路径仍存在、内容有效或 mutation 已稳定；rename 也可能 产生多个 backend event。RescanRequired 会覆盖同一 receiver 中尚未消费的 path-level hints。 ThrottledWatchReceiver 在第一次事件后限制最小发送间隔，适合需要持续刷新但限制频率的 manager。 DebouncedWatchReceiver 从第一条事件起收集固定窗口，适合一次 burst 后重建 snapshot。两者都保留 shutdown 前已 pending 的事件。 Path 与 backend 边界 matching::actualwatchpath 为已存在目标保存 subscriber 请求路径，同时使用 canonical path 匹配可能被 OS 重写的 event namespace。缺失目标不会要求 broad recursive ancestor watch： 1. 从最近的现有 directory ancestor 建立 non-recursive watch； 2. ancestor event 只在目标出现/消失时映射回 subscriber 请求路径； 3. 中间 component 出现后，watcher::applyactualwatchmove 把 ref-count 和 backend watch 移到更近的路径； 4. consumer 最终看到原始请求 namespace，而不是 /private/var 等 canonicalization artifact。 Backend 只转发 create、modify 和 remove；access/open 事件被过滤。所有 backend callback error 都按可能丢事件处理，并通过 watcher::requirerescan 向每个 subscriber 发送其 own watched roots。 失败、并发与关闭 - FileWatcher::new 在 backend 初始化失败或当前线程没有 Tokio runtime 时返回 notify::Error； - registration failure 同步返回；调用方必须选择显式失败、诊断或可解释的 manual-refresh fallback； - 单个 backend watch/unwatch reconfiguration 失败会写 warning；现有注册状态仍保留，后续 reconfiguration 可再次尝试； - state lock 与 backend lock 始终按 state → backend 顺序获取，注册、注销和 watch mode reconfiguration 对其他操作呈一致顺序； - receiver 不使用有界 mpsc 丢弃 path；pending path 存入 BTreeSet，因此批次稳定排序并去重； - FileWatcher drop 释放 OS watcher sender，后台 loop 在 channel 关闭后退出； - watcher 不读取文件、不持有 catalog snapshot、不自动 retry/rescan，也不发布产品 update。 注册时单个 backend watch 失败当前只能记录 warning，不能同步回传给已经返回 guard 的 caller； 需要强一致启动的 manager 必须在首次 full scan 后保留显式 refresh/fallback 路径。这是当前限制， 不能把“registration 已进入 ref-count state”解释为 OS enforcement 已确认。 内部接口地图 Symbol 拥有 架构漂移信号 --- --- --- WatchState / PathWatchCounts subscriber state 与共享 backend scope ref-count 开始保存 consumer snapshot/content actualwatchpath / changedpathforevent fallback、canonical matching 与请求 namespace 恢复 开始解释 catalog 或 workspace 业务语义 FileWatcher::notifysubscribers raw mutation 到 subscriber hint 的 routing 直接发布 App Server/product event WatchSender / PendingEvent 每个 subscriber 的去重 pending 状态 引入 durable queue/replay contract throttle/debounce wrappers delivery cadence 决定 manager 的扫描或 snapshot generation 如果需要 ignore rules、fuzzy index 或文件内容检索，应分别由 consumer、zeta-file-search 或 zeta-shell-command/rg 负责，不应扩张本 crate。 验证与修改影响 `bash cargo test --manifest-path Cargo.toml -p zeta-file-watcher cargo clippy --manifest-path Cargo.toml -p zeta-file-watcher \\ --all-targets --no-deps -- -D warnings bazel test //zeta-rs/file-watcher:file-watcher-unit-tests ` 修改 path normalization、recursive mode、missing-target fallback、backend error 或 event coalescing 时，必须同步检查 filewatchertests.rs、本 README、 docs/skills.md 与 docs/git.md` 的 snapshot invalidation 语义。接入新 consumer 时应把 其 owner 文档链接回本 README，而不是复制 backend 细节。"
  },
  {
    "slug": "crates/git",
    "sourcePath": "zeta-rs/git/README.md",
    "title": "zeta-git",
    "description": "zeta-git 是 Zeta 中“如何调用 Git、如何解释 Git 结果”的唯一实现 owner。完整 owner 不等于 当前已经实现完整 SCM：本阶段提供仓库打开、结构化状态快照、本地 branch、remote、最近 commit、 revision file content、HEAD-to-working-tree 文本 Diff/增删行统计、typed",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Zeta 本地 Git 实现的 crate-level canonical contract。它说明当前代码、关键\n> private symbol、失败语义与安全修改路径。Desktop/App Server Git 产品语义见\n> [`docs/git.md`](../../docs/git.md)；模型 Tool 和 approval 边界仍由 [`docs/tools.md`](../../docs/tools.md) 与\n> [`docs/sandboxing.md`](../../docs/sandboxing.md) 维护。\n\n`zeta-git` 是 Zeta 中“如何调用 Git、如何解释 Git 结果”的唯一实现 owner。完整 owner 不等于\n当前已经实现完整 SCM：本阶段提供仓库打开、结构化状态快照、本地 branch、remote、最近 commit、\nrevision file content、HEAD-to-working-tree 文本 Diff/增删行统计、typed\nstage/unstage/discard/commit/fetch/pull/push、local branch switch 和 patch check/apply；持续监听、\n状态缓存与 tag/worktree mutation 尚未实现。App Server 与 Desktop 已通过 Git SCM 纵向切片消费这些能力，但该 service/protocol/UI\n不属于本 crate。\n\n## 为什么不是普通 `git utils`\n\n本 crate 吸收了 Codex `git-utils` 中值得保留的 agent/host 侧优化，但把这些能力组织在一个明确的\nGit domain owner 下，而不是建立平级的 `zeta-git-utils`：\n\n| 容易被低估的能力 | 当前实现价值 |\n| --- | --- |\n| 有界进程 | query 默认 5 秒、mutation 默认 30 秒；stdout/stderr 各自有 8 MiB 上限，达到上限后仍继续 drain child，最终返回明确错误 |\n| 非交互执行 | 禁用 terminal prompt 和 credential-manager 交互，固定 `LC_ALL=C`，避免后台调用等待 UI/input |\n| repository config 隔离 | 所有内部命令禁用 configured hooks；query 禁用 optional locks |\n| fsmonitor 安全与性能 | 不执行 repository-selected fsmonitor helper；仅当配置确实是 boolean true 且 Git 声明 built-in daemon capability 时保留 daemon |\n| 机器可读 status | 使用 porcelain v2 + NUL 分隔，保留 index/worktree、rename original path、conflict、submodule 和 upstream ahead/behind |\n| 结构化 patch | 用 enum 区分 check/apply 与 forward/reverse；区分 `AppliedWithConflicts` 和未应用的 `Rejected`，不与 spawn/timeout/输出损坏混为一谈 |\n| 路径边界 | `git apply` 不启用 `--unsafe-paths`；diff header path 做排序、去重和 quoted-path 解析 |\n\n因此，新增 Git 能力时应扩展本 crate，而不是在 App Server、Desktop adapter 或 Tool 中直接新增\n`Command::new(\"git\")`。\n\n## 当前所有权\n\n| 文件 | 当前职责 | 关键 symbol |\n| --- | --- | --- |\n| `src/client.rs` | Git executable identity、process profile、timeout、bounded capture、non-interactive config | `GitClient`、`GitExecutionLimits`、private `GitInvocation`、`GitCommandProfile`、`read_bounded` |\n| `src/repository.rs` | 从已有 path 打开 working tree，解析 worktree/git/common metadata path | `GitRepository`、`GitRepositoryKind`、`existing_directory` |\n| `src/status.rs` | porcelain-v2 snapshot 与 HEAD/change/submodule model | `GitRepositorySnapshot`、`GitHead`、private `parse_status` |\n| `src/content.rs` | 有界读取 HEAD 或 index 中一个 repository-relative file | `GitFileRevision`、`GitClient::read_file_at_revision` |\n| `src/text_diff.rs` | 从同一次状态快照构建 repository-wide 或 path-scoped 的有界 UTF-8 HEAD/worktree Diff 与文件级、聚合增删行统计 | `GitTextDiffSnapshot`、`GitTextDiff`、`GitDiffStatistics`、`GitClient::text_diff_snapshot[_under]` |\n| `src/info.rs` | local branches、fetch/push remote URLs、bounded recent history | `GitBranch`、`GitRemote`、`GitCommitSummary` |\n| `src/mutation.rs` | path set/commit request validation 与常用 index/worktree/branch/remote mutation | `GitPathspecSet`、`GitCommitRequest`、`GitCommitResult`、`GitClient::switch_branch` |\n| `src/patch.rs` | patch request/result、stdin apply、path extraction 和 diagnostics 分类 | `GitPatchRequest`、`GitPatchResult`、private `parse_apply_diagnostics` |\n| `src/fsmonitor.rs` | effective config 与 built-in daemon capability 探测 | private `detect_fsmonitor_override` |\n| `src/path.rs` | porcelain path bytes 到 platform `PathBuf` | private `path_from_git_bytes` |\n| `src/error.rs` | transport、timeout、limit、Git exit 和 parse failure 的稳定区分 | `GitError` |\n\n所有实现 module 均为 private，`src/lib.rs` 显式导出 crate API。把 App Server repository registry、\nwatch subscription、operation queue 或 wire DTO 放进上述 module，意味着 ownership 已经漂移。\n\n## 公共接口与真实调用路径\n\n`GitClient` 冻结 executable path 和 `GitExecutionLimits`。`GitClient::system()` 使用进程启动环境\n解析 `git`；需要 bundled/显式 executable identity 的 host 应在启动阶段调用\n`GitClient::with_executable`，并长期复用返回值。\n\n```text\nGitClient::open_repository\n└─ GitClient::run_query_unchecked\n   └─ git rev-parse --path-format=absolute\n      ├─ worktree root\n      ├─ git dir\n      └─ common git dir\n\nGitClient::snapshot\n├─ detect_fsmonitor_override\n│  ├─ git config --null --get core.fsmonitor\n│  └─ git version --build-options\n└─ GitClient::run_query_with_fsmonitor\n   └─ git status --porcelain=v2 --branch -z\n      └─ parse_status\n\nGitClient::read_file_at_revision\n└─ GitClient::run_query_unchecked\n   └─ git show --no-textconv HEAD:path | :path\n      ├─ present bytes\n      └─ missing path → None\n\nGitClient::text_diff_snapshot[_under]\n├─ GitClient::snapshot\n├─ optional repository-relative path-prefix filter\n├─ GitClient::read_file_at_revision(Head)\n├─ bounded worktree file read\n└─ zeta_diff::DiffDocument::from_text\n   └─ GitDiffStatistics\n\nGitClient::local_branches / remotes / recent_commits\n└─ GitClient::run_query\n   └─ strict parser for the command-specific output\n\nGitClient::stage / unstage / discard_worktree\n├─ GitPathspecSet\n└─ GitClient::run_mutation\n   └─ git add | restore --staged | restore --worktree\n\nGitClient::switch_branch\n├─ GitBranch from GitClient::local_branches\n└─ GitClient::run_mutation\n   └─ git switch -- <local-branch>\n\nGitClient::commit / fetch / pull_fast_forward / push\n└─ GitClient::run_mutation[_with_stdin]\n   └─ git commit --file=- | fetch --all --prune | pull --ff-only | push\n\nGitClient::apply_patch\n├─ extract_patch_paths\n├─ GitClient::run_mutation_with_stdin\n│  └─ git apply --recount [--check | --3way] [-R] -\n└─ parse_apply_diagnostics\n```\n\n`GitRepository` 只能由 `open_repository` 构造，后续 API 依赖其中已解析的 worktree root。\n`GitRepositoryKind::LinkedWorktree` 表示 per-worktree `git_dir` 与 shared `common_dir` 不同；\n`Standard` 不进一步声称 repository 一定不是 submodule。\n\n## 状态契约\n\n`snapshot` 是一次有界 query，不是 live state。返回：\n\n- `GitHead::Branch`、`Detached` 或 `Unborn`；\n- branch upstream 与 ahead/behind；\n- 每个 path 独立的 index/worktree `GitChangeStatus`；\n- rename/copy 的 `original_path`；\n- porcelain submodule flags；\n- untracked path。\n\ntracked status 使用 NUL-delimited porcelain-v2，Unix 上 path bytes 可保留为非 UTF-8 `PathBuf`。\nbranch、remote URL 和 commit subject 当前要求 UTF-8。Snapshot 按 Git 输出顺序返回；当前实现不再\n额外排序，因为 rename record 的相邻 NUL payload 由 parser 顺序消费。\n\n`snapshot` 使用 `GIT_OPTIONAL_LOCKS=0`。它是 observation，不获得阻止并发 Git mutation 的锁；\n调用方不能把一次 snapshot 当作后续 mutation 的 compare-and-swap 前提。App Server\n`GitRuntime` 当前使用 watcher invalidation 重新查询，并仅在 workspace projection 改变时推进\nrevision；该 revision 仍不是 mutation CAS token。\n\n## 文本 Diff 与统计契约\n\n`text_diff_snapshot` / `text_diff_snapshot_under` 是 Git domain 对 HEAD-to-working-tree\n文本变化的权威投影。它先捕获\n`GitRepositorySnapshot`，再按其中的 repository-relative changed path 读取 HEAD 与当前工作区\n内容，并由 `zeta-diff` 生成 `DiffDocument`。`GitTextDiff` 同时保留构建该 document 的有界\n`original` / `modified` UTF-8 文本，供 App Server 投影跨进程 DTO；client 不直接依赖本 crate。\n`GitDiffStatistics` 将新增行计为 addition、删除行计为 deletion；一行 replacement 同时计入一次\naddition 和一次 deletion。调用方只能按 workspace prefix 请求、聚合和展示这些结果，不能自行读取\nGit revision 或复制统计规则。\n\n该读取序列不是 filesystem transaction：文件可能在 status 与内容读取之间变化。binary、\n非 UTF-8、symlink、非普通文件、不可读文件、任一侧超过 `GitTextDiffLimits`，或超过 diff engine\n限制或单文件内容查询失败的 path 仍保留在 repository status 中，但不会进入\n`GitTextDiffSnapshot::diffs` 和统计。\n因此 text diff 文件数明确表示“当前可展示的文本变化数”，不冒充全部 porcelain status entry\n数量。\n\n## 变更契约\n\n`GitPathspecSet` 在启动 Git 前拒绝空集合、空路径、绝对路径、`.`/`..` component 和 NUL。\n`GitCommitRequest` 拒绝空白 message、NUL 和超过 64 KiB 的内容。`stage`、`unstage` 与\n`discard_worktree` 只接收该 validated type，避免调用方把未经建模的 host path 直接拼进 argv。\n\n`unstage` 在 unborn repository 使用 `git rm --cached`，其他情况使用 `git restore --staged`。\n`discard_worktree` 只恢复 tracked path，不删除 untracked 内容。`commit` 从 stdin 读取 message，\n成功后返回 HEAD object ID。Remote mutation non-interactive；`fetch` 为 all-remotes prune，\n`pull_fast_forward` 明确使用 `--ff-only`，`push` 使用 repository 当前 upstream/default。\n\n## 补丁契约\n\n`GitPatchRequest` 用 `GitPatchExecution` 和 `GitPatchDirection` 避免 `apply(false, true)` 一类不透明\ncallsite：\n\n| Request | Git invocation | Working tree |\n| --- | --- | --- |\n| `Check + Forward` | `git apply --recount --check -` | 不修改 |\n| `Check + Reverse` | `git apply --recount --check -R -` | 不修改 |\n| `Apply + Forward` | `git apply --recount --3way -` | 可能修改并产生 conflict |\n| `Apply + Reverse` | `git apply --recount --3way -R -` | 可能修改并产生 conflict |\n\n成功 check 返回 `Applicable`，成功 apply 返回 `Applied`。当 Git 明确报告三方应用已经写入\nconflict 时返回 `AppliedWithConflicts`；其他 nonzero exit 返回 `Rejected`。后两者都表示进程已经\n启动并完成，调用方必须根据 disposition 判断 working tree 是否可能改变；spawn、stdin、timeout\n或 output limit failure 返回 `GitError`。`referenced_paths` 来自 diff header；check 不会把这些\npath 错称为 `applied_paths`。Diagnostics path 是对 Git 文本输出的 best-effort 分类，\n`exit_code` 与 disposition 比分类集合 authoritative。\n\n本实现不会像 Codex reverse helper 一样预先 stage existing path，因为前端 Git service 不应让\n“反向应用 patch”隐式改变 index。若未来需要该 workflow，必须建模成显式 operation。\n\n## Process、失败与取消\n\nprivate `GitCommandProfile` 固定三类执行：\n\n| Profile | Timeout | Optional locks | fsmonitor |\n| --- | ---: | --- | --- |\n| Query | query timeout | disabled | explicit safe override |\n| Configuration probe | query timeout | disabled | 不覆盖被探测值 |\n| Mutation | mutation timeout | Git 默认 | disabled |\n\n所有 profile 禁用 configured hooks、terminal prompts 和 color。stdout/stderr reader 在保留上限\n后继续 drain，避免 child 因 pipe backpressure 卡死；最终以 `OutputLimitExceeded` 失败，不返回\n截断后可能被误解析的数据。\n\n`GitError` 保留以下边界：\n\n- `InvalidConfiguration` / `InvalidStartPath`：spawn 前拒绝；\n- `NotAWorkingTree`：`rev-parse` 明确报告不在 working tree；\n- `Io` / `Runtime`：spawn、pipe、wait 或 Tokio task failure；\n- `TimedOut`：child 已启动，crate 发出 kill 并等待退出；\n- `OutputLimitExceeded`：child 已完成或已被完整 drain，但结果不用于 domain parsing；\n- `CommandFailed`：strict query 的 nonzero Git exit；\n- `InvalidOutput`：Git success output 不满足 parser contract。\n\n当前 API 没有调用方 cancellation token。Timeout 是唯一中断机制；未来 service cancellation 必须\n进入 `GitClient` 的 process lifecycle，不能只在 App Server 丢弃 future。\n\n## 集成边界\n\nCurrent：\n\n```text\nDesktop / Native / TUI\n  ↔ App Server git/* + git/statusChanged\n  → workspace-scoped GitRuntime\n  → workspace-scoped GitService\n  → zeta-git\n  → system git\n```\n\n当前实时状态投影：\n\n```text\nProduct client\n  ↕ app-server-protocol Git commands/snapshots/events\nApp Server GitRuntime\n  ├─ single-workspace projection + revision\n  ├─ zeta-file-watcher invalidation hints + debounce\n  ├─ operation serialization\n  ├─ status deduplication + notification\n  └─ zeta-git\n      └─ system git\n```\n\n当前 `GitService` 冻结 workspace root 和 async runtime，每次 operation 重新打开仓库；成功\nmutation 随后读取并返回新 snapshot。`GitRuntime` 串行化 operation、维护当前 workspace\nprojection/revision，并把 `zeta-file-watcher` hint 转换为重新查询；protocol 拥有 wire DTO；\nDesktop 只展示状态和发送 intent；agent Git Tool 还必须经过\npolicy/approval。它们都不能复制本 crate 的 command/parsing 实现。\n\n## 测试与修改\n\n实现测试全部位于独立 sibling 文件：\n\n- `client_tests.rs`：system Git runner 与 limits；\n- `repository_tests.rs`：nested start、non-repository、linked worktree；\n- `status_tests.rs`：index/worktree/untracked、rename 与 unmerged/unborn parser；\n- `content_tests.rs`：HEAD/index 内容与 missing path；\n- `text_diff_tests.rs`：modified/deleted/untracked 汇总、replacement 统计及 binary/size skip；\n- `info_tests.rs`：branch、remote fetch/push URL、history limit；\n- `mutation_tests.rs`：validation、stage/unstage/discard/commit、local branch switch 及失败时\n  保留当前分支和工作树，以及本地 bare remote 驱动的 fetch/fast-forward pull/push；\n- `patch_tests.rs`：quoted paths、check/apply、unapplied rejection、three-way conflict 与\n  Windows `core.autocrlf=true`；\n- `fsmonitor_tests.rs`：NUL config value 与 boolean spelling。\n\n测试仓库固定 `core.autocrlf=false` 与 LF，避免继承开发机全局配置；需要验证平台换行语义的测试\n必须显式覆盖 repository-local config。本地 remote 测试不访问网络或用户凭据。\n\n正常 workspace 状态下运行：\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-git\ncargo clippy --manifest-path Cargo.toml -p zeta-git \\\n  --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/git:git-unit-tests\n```\n\n修改 `GitCommandProfile`、env/config override、timeout 或 capture 时同步检查所有 command family；\n修改 porcelain parser 时增加 raw NUL fixture 和 live repository test；修改 patch diagnostics 时\n同时保留 disposition/exit 的 authoritative 边界；新增 public operation 时更新本 README 的\n当前能力与失败契约。\n\n## 当前限制与扩展点\n\n当前限制：\n\n- local branch switch 已实现；尚无新建/删除/重命名 branch、tag 或 worktree mutation；\n- App Server 已有单 workspace projection、watch、revision/event 和 operation serialization，\n  但尚无 multi-repository registry、可观测 queue、progress 或 caller cancellation；\n- 不支持 bare repository，`open_repository` 要求 working tree；\n- repository discovery 依赖支持 `rev-parse --path-format=absolute` 的 Git；\n- patch diagnostics parser 是 best effort，不承诺复现所有 Git 版本的自然语言；\n- text diff snapshot 不是原子快照，且当前只投影 UTF-8 普通文件，不表达 mode-only、binary 或\n  symlink 内容变化；\n- 未移植 Codex internal baseline，因为 resettable internal directory 不是用户 repository SCM；\n- remote URL 当前保留 Git 配置原值，尚无 canonical repository identity API。\n\n扩展顺序应优先保持 ownership：\n\n1. 在本 crate 增加新的明确 typed operation 与 parser；\n2. 为新增 mutation 定义 index/worktree side effect、failure 和 cancellation；\n3. 在当前 App Server `GitRuntime` 上增加 multi-repository registry 与 operation manager；\n4. 最后增加对应 protocol/desktop/agent adapter。\n\n只有出现多个独立底层 Git owner 且有稳定共享 primitive 时，才重新评估 `zeta-git-utils`；当前不应\n新增该 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "为什么不是普通-git-utils",
        "title": "为什么不是普通 git utils"
      },
      {
        "depth": 2,
        "id": "当前所有权",
        "title": "当前所有权"
      },
      {
        "depth": 2,
        "id": "公共接口与真实调用路径",
        "title": "公共接口与真实调用路径"
      },
      {
        "depth": 2,
        "id": "状态契约",
        "title": "状态契约"
      },
      {
        "depth": 2,
        "id": "文本-diff-与统计契约",
        "title": "文本 Diff 与统计契约"
      },
      {
        "depth": 2,
        "id": "变更契约",
        "title": "变更契约"
      },
      {
        "depth": 2,
        "id": "补丁契约",
        "title": "补丁契约"
      },
      {
        "depth": 2,
        "id": "process失败与取消",
        "title": "Process、失败与取消"
      },
      {
        "depth": 2,
        "id": "集成边界",
        "title": "集成边界"
      },
      {
        "depth": 2,
        "id": "测试与修改",
        "title": "测试与修改"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 Zeta 本地 Git 实现的 crate-level canonical contract。它说明当前代码、关键 private symbol、失败语义与安全修改路径。Desktop/App Server Git 产品语义见 docs/git.md；模型 Tool 和 approval 边界仍由 docs/tools.md 与 docs/sandboxing.md 维护。 zeta-git 是 Zeta 中“如何调用 Git、如何解释 Git 结果”的唯一实现 owner。完整 owner 不等于 当前已经实现完整 SCM：本阶段提供仓库打开、结构化状态快照、本地 branch、remote、最近 commit、 revision file content、HEAD-to-working-tree 文本 Diff/增删行统计、typed stage/unstage/discard/commit/fetch/pull/push、local branch switch 和 patch check/apply；持续监听、 状态缓存与 tag/worktree mutation 尚未实现。App Server 与 Desktop 已通过 Git SCM 纵向切片消费这些能力，但该 service/protocol/UI 不属于本 crate。 为什么不是普通 git utils 本 crate 吸收了 Codex git-utils 中值得保留的 agent/host 侧优化，但把这些能力组织在一个明确的 Git domain owner 下，而不是建立平级的 zeta-git-utils： 容易被低估的能力 当前实现价值 --- --- 有界进程 query 默认 5 秒、mutation 默认 30 秒；stdout/stderr 各自有 8 MiB 上限，达到上限后仍继续 drain child，最终返回明确错误 非交互执行 禁用 terminal prompt 和 credential-manager 交互，固定 LCALL=C，避免后台调用等待 UI/input repository config 隔离 所有内部命令禁用 configured hooks；query 禁用 optional locks fsmonitor 安全与性能 不执行 repository-selected fsmonitor helper；仅当配置确实是 boolean true 且 Git 声明 built-in daemon capability 时保留 daemon 机器可读 status 使用 porcelain v2 + NUL 分隔，保留 index/worktree、rename original path、conflict、submodule 和 upstream ahead/behind 结构化 patch 用 enum 区分 check/apply 与 forward/reverse；区分 AppliedWithConflicts 和未应用的 Rejected，不与 spawn/timeout/输出损坏混为一谈 路径边界 git apply 不启用 --unsafe-paths；diff header path 做排序、去重和 quoted-path 解析 因此，新增 Git 能力时应扩展本 crate，而不是在 App Server、Desktop adapter 或 Tool 中直接新增 Command::new \"git\" 。 当前所有权 文件 当前职责 关键 symbol --- --- --- src/client.rs Git executable identity、process profile、timeout、bounded capture、non-interactive config GitClient、GitExecutionLimits、private GitInvocation、GitCommandProfile、readbounded src/repository.rs 从已有 path 打开 working tree，解析 worktree/git/common metadata path GitRepository、GitRepositoryKind、existingdirectory src/status.rs porcelain-v2 snapshot 与 HEAD/change/submodule model GitRepositorySnapshot、GitHead、private parsestatus src/content.rs 有界读取 HEAD 或 index 中一个 repository-relative file GitFileRevision、GitClient::readfileatrevision src/textdiff.rs 从同一次状态快照构建 repository-wide 或 path-scoped 的有界 UTF-8 HEAD/worktree Diff 与文件级、聚合增删行统计 GitTextDiffSnapshot、GitTextDiff、GitDiffStatistics、GitClient::textdiffsnapshot under src/info.rs local branches、fetch/push remote URLs、bounded recent history GitBranch、GitRemote、GitCommitSummary src/mutation.rs path set/commit request validation 与常用 index/worktree/branch/remote mutation GitPathspecSet、GitCommitRequest、GitCommitResult、GitClient::switchbranch src/patch.rs patch request/result、stdin apply、path extraction 和 diagnostics 分类 GitPatchRequest、GitPatchResult、private parseapplydiagnostics src/fsmonitor.rs effective config 与 built-in daemon capability 探测 private detectfsmonitoroverride src/path.rs porcelain path bytes 到 platform PathBuf private pathfromgitbytes src/error.rs transport、timeout、limit、Git exit 和 parse failure 的稳定区分 GitError 所有实现 module 均为 private，src/lib.rs 显式导出 crate API。把 App Server repository registry、 watch subscription、operation queue 或 wire DTO 放进上述 module，意味着 ownership 已经漂移。 公共接口与真实调用路径 GitClient 冻结 executable path 和 GitExecutionLimits。GitClient::system 使用进程启动环境 解析 git；需要 bundled/显式 executable identity 的 host 应在启动阶段调用 GitClient::withexecutable，并长期复用返回值。 ``text GitClient::openrepository └─ GitClient::runqueryunchecked └─ git rev-parse --path-format=absolute ├─ worktree root ├─ git dir └─ common git dir GitClient::snapshot ├─ detectfsmonitoroverride │ ├─ git config --null --get core.fsmonitor │ └─ git version --build-options └─ GitClient::runquerywithfsmonitor └─ git status --porcelain=v2 --branch -z └─ parsestatus GitClient::readfileatrevision └─ GitClient::runqueryunchecked └─ git show --no-textconv HEAD:path :path ├─ present bytes └─ missing path → None GitClient::textdiffsnapshot under ├─ GitClient::snapshot ├─ optional repository-relative path-prefix filter ├─ GitClient::readfileatrevision Head ├─ bounded worktree file read └─ zetadiff::DiffDocument::fromtext └─ GitDiffStatistics GitClient::localbranches / remotes / recentcommits └─ GitClient::runquery └─ strict parser for the command-specific output GitClient::stage / unstage / discardworktree ├─ GitPathspecSet └─ GitClient::runmutation └─ git add restore --staged restore --worktree GitClient::switchbranch ├─ GitBranch from GitClient::localbranches └─ GitClient::runmutation └─ git switch -- <local-branch GitClient::commit / fetch / pullfastforward / push └─ GitClient::runmutation withstdin └─ git commit --file=- fetch --all --prune pull --ff-only push GitClient::applypatch ├─ extractpatchpaths ├─ GitClient::runmutationwithstdin │ └─ git apply --recount --check --3way -R - └─ parseapplydiagnostics ` GitRepository 只能由 openrepository 构造，后续 API 依赖其中已解析的 worktree root。 GitRepositoryKind::LinkedWorktree 表示 per-worktree gitdir 与 shared commondir 不同； Standard 不进一步声称 repository 一定不是 submodule。 状态契约 snapshot 是一次有界 query，不是 live state。返回： - GitHead::Branch、Detached 或 Unborn； - branch upstream 与 ahead/behind； - 每个 path 独立的 index/worktree GitChangeStatus； - rename/copy 的 originalpath； - porcelain submodule flags； - untracked path。 tracked status 使用 NUL-delimited porcelain-v2，Unix 上 path bytes 可保留为非 UTF-8 PathBuf。 branch、remote URL 和 commit subject 当前要求 UTF-8。Snapshot 按 Git 输出顺序返回；当前实现不再 额外排序，因为 rename record 的相邻 NUL payload 由 parser 顺序消费。 snapshot 使用 GITOPTIONALLOCKS=0。它是 observation，不获得阻止并发 Git mutation 的锁； 调用方不能把一次 snapshot 当作后续 mutation 的 compare-and-swap 前提。App Server GitRuntime 当前使用 watcher invalidation 重新查询，并仅在 workspace projection 改变时推进 revision；该 revision 仍不是 mutation CAS token。 文本 Diff 与统计契约 textdiffsnapshot / textdiffsnapshotunder 是 Git domain 对 HEAD-to-working-tree 文本变化的权威投影。它先捕获 GitRepositorySnapshot，再按其中的 repository-relative changed path 读取 HEAD 与当前工作区 内容，并由 zeta-diff 生成 DiffDocument。GitTextDiff 同时保留构建该 document 的有界 original / modified UTF-8 文本，供 App Server 投影跨进程 DTO；client 不直接依赖本 crate。 GitDiffStatistics 将新增行计为 addition、删除行计为 deletion；一行 replacement 同时计入一次 addition 和一次 deletion。调用方只能按 workspace prefix 请求、聚合和展示这些结果，不能自行读取 Git revision 或复制统计规则。 该读取序列不是 filesystem transaction：文件可能在 status 与内容读取之间变化。binary、 非 UTF-8、symlink、非普通文件、不可读文件、任一侧超过 GitTextDiffLimits，或超过 diff engine 限制或单文件内容查询失败的 path 仍保留在 repository status 中，但不会进入 GitTextDiffSnapshot::diffs 和统计。 因此 text diff 文件数明确表示“当前可展示的文本变化数”，不冒充全部 porcelain status entry 数量。 变更契约 GitPathspecSet 在启动 Git 前拒绝空集合、空路径、绝对路径、./.. component 和 NUL。 GitCommitRequest 拒绝空白 message、NUL 和超过 64 KiB 的内容。stage、unstage 与 discardworktree 只接收该 validated type，避免调用方把未经建模的 host path 直接拼进 argv。 unstage 在 unborn repository 使用 git rm --cached，其他情况使用 git restore --staged。 discardworktree 只恢复 tracked path，不删除 untracked 内容。commit 从 stdin 读取 message， 成功后返回 HEAD object ID。Remote mutation non-interactive；fetch 为 all-remotes prune， pullfastforward 明确使用 --ff-only，push 使用 repository 当前 upstream/default。 补丁契约 GitPatchRequest 用 GitPatchExecution 和 GitPatchDirection 避免 apply false, true 一类不透明 callsite： Request Git invocation Working tree --- --- --- Check + Forward git apply --recount --check - 不修改 Check + Reverse git apply --recount --check -R - 不修改 Apply + Forward git apply --recount --3way - 可能修改并产生 conflict Apply + Reverse git apply --recount --3way -R - 可能修改并产生 conflict 成功 check 返回 Applicable，成功 apply 返回 Applied。当 Git 明确报告三方应用已经写入 conflict 时返回 AppliedWithConflicts；其他 nonzero exit 返回 Rejected。后两者都表示进程已经 启动并完成，调用方必须根据 disposition 判断 working tree 是否可能改变；spawn、stdin、timeout 或 output limit failure 返回 GitError。referencedpaths 来自 diff header；check 不会把这些 path 错称为 appliedpaths。Diagnostics path 是对 Git 文本输出的 best-effort 分类， exitcode 与 disposition 比分类集合 authoritative。 本实现不会像 Codex reverse helper 一样预先 stage existing path，因为前端 Git service 不应让 “反向应用 patch”隐式改变 index。若未来需要该 workflow，必须建模成显式 operation。 Process、失败与取消 private GitCommandProfile 固定三类执行： Profile Timeout Optional locks fsmonitor --- ---: --- --- Query query timeout disabled explicit safe override Configuration probe query timeout disabled 不覆盖被探测值 Mutation mutation timeout Git 默认 disabled 所有 profile 禁用 configured hooks、terminal prompts 和 color。stdout/stderr reader 在保留上限 后继续 drain，避免 child 因 pipe backpressure 卡死；最终以 OutputLimitExceeded 失败，不返回 截断后可能被误解析的数据。 GitError 保留以下边界： - InvalidConfiguration / InvalidStartPath：spawn 前拒绝； - NotAWorkingTree：rev-parse 明确报告不在 working tree； - Io / Runtime：spawn、pipe、wait 或 Tokio task failure； - TimedOut：child 已启动，crate 发出 kill 并等待退出； - OutputLimitExceeded：child 已完成或已被完整 drain，但结果不用于 domain parsing； - CommandFailed：strict query 的 nonzero Git exit； - InvalidOutput：Git success output 不满足 parser contract。 当前 API 没有调用方 cancellation token。Timeout 是唯一中断机制；未来 service cancellation 必须 进入 GitClient 的 process lifecycle，不能只在 App Server 丢弃 future。 集成边界 Current： `text Desktop / Native / TUI ↔ App Server git/ + git/statusChanged → workspace-scoped GitRuntime → workspace-scoped GitService → zeta-git → system git ` 当前实时状态投影： `text Product client ↕ app-server-protocol Git commands/snapshots/events App Server GitRuntime ├─ single-workspace projection + revision ├─ zeta-file-watcher invalidation hints + debounce ├─ operation serialization ├─ status deduplication + notification └─ zeta-git └─ system git ` 当前 GitService 冻结 workspace root 和 async runtime，每次 operation 重新打开仓库；成功 mutation 随后读取并返回新 snapshot。GitRuntime 串行化 operation、维护当前 workspace projection/revision，并把 zeta-file-watcher hint 转换为重新查询；protocol 拥有 wire DTO； Desktop 只展示状态和发送 intent；agent Git Tool 还必须经过 policy/approval。它们都不能复制本 crate 的 command/parsing 实现。 测试与修改 实现测试全部位于独立 sibling 文件： - clienttests.rs：system Git runner 与 limits； - repositorytests.rs：nested start、non-repository、linked worktree； - statustests.rs：index/worktree/untracked、rename 与 unmerged/unborn parser； - contenttests.rs：HEAD/index 内容与 missing path； - textdifftests.rs：modified/deleted/untracked 汇总、replacement 统计及 binary/size skip； - infotests.rs：branch、remote fetch/push URL、history limit； - mutationtests.rs：validation、stage/unstage/discard/commit、local branch switch 及失败时 保留当前分支和工作树，以及本地 bare remote 驱动的 fetch/fast-forward pull/push； - patchtests.rs：quoted paths、check/apply、unapplied rejection、three-way conflict 与 Windows core.autocrlf=true； - fsmonitortests.rs：NUL config value 与 boolean spelling。 测试仓库固定 core.autocrlf=false 与 LF，避免继承开发机全局配置；需要验证平台换行语义的测试 必须显式覆盖 repository-local config。本地 remote 测试不访问网络或用户凭据。 正常 workspace 状态下运行： `bash cargo test --manifest-path Cargo.toml -p zeta-git cargo clippy --manifest-path Cargo.toml -p zeta-git \\ --all-targets --no-deps -- -D warnings bazel test //zeta-rs/git:git-unit-tests ` 修改 GitCommandProfile、env/config override、timeout 或 capture 时同步检查所有 command family； 修改 porcelain parser 时增加 raw NUL fixture 和 live repository test；修改 patch diagnostics 时 同时保留 disposition/exit 的 authoritative 边界；新增 public operation 时更新本 README 的 当前能力与失败契约。 当前限制与扩展点 当前限制： - local branch switch 已实现；尚无新建/删除/重命名 branch、tag 或 worktree mutation； - App Server 已有单 workspace projection、watch、revision/event 和 operation serialization， 但尚无 multi-repository registry、可观测 queue、progress 或 caller cancellation； - 不支持 bare repository，openrepository 要求 working tree； - repository discovery 依赖支持 rev-parse --path-format=absolute 的 Git； - patch diagnostics parser 是 best effort，不承诺复现所有 Git 版本的自然语言； - text diff snapshot 不是原子快照，且当前只投影 UTF-8 普通文件，不表达 mode-only、binary 或 symlink 内容变化； - 未移植 Codex internal baseline，因为 resettable internal directory 不是用户 repository SCM； - remote URL 当前保留 Git 配置原值，尚无 canonical repository identity API。 扩展顺序应优先保持 ownership： 1. 在本 crate 增加新的明确 typed operation 与 parser； 2. 为新增 mutation 定义 index/worktree side effect、failure 和 cancellation； 3. 在当前 App Server GitRuntime 上增加 multi-repository registry 与 operation manager； 4. 最后增加对应 protocol/desktop/agent adapter。 只有出现多个独立底层 Git owner 且有稳定共享 primitive 时，才重新评估 zeta-git-utils`；当前不应 新增该 crate。"
  },
  {
    "slug": "crates/history",
    "sourcePath": "zeta-rs/history/README.md",
    "title": "zeta-history",
    "description": "zeta-history 是纯数据层：它定义“一个 Thread 事实被长期保存时长什么样”。它不打开文件或 数据库，不做分页、事务、恢复、归约、模型调用，也不拥有 TUI 展示。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释模型历史与持久化 Thread record 的数据契约。Canonical `ThreadEvent` 语义见\n> [`docs/protocol.md`](../../docs/protocol.md)，恢复与追加接口见\n> [`zeta-thread-store`](../thread-store/README.md)，物理 SQLite 映射见\n> [`zeta-storage`](../storage/README.md)。\n\n`zeta-history` 是纯数据层：它定义“一个 Thread 事实被长期保存时长什么样”。它不打开文件或\n数据库，不做分页、事务、恢复、归约、模型调用，也不拥有 TUI 展示。\n\n## 职责边界\n\n| 问题 | Owner | 本 crate 是否负责 |\n| --- | --- | --- |\n| 哪些事实可以持久化？ | `zeta-protocol::ThreadEvent` | ❌，只引用 canonical fact |\n| 事实落盘时携带哪些稳定元数据？ | `zeta-history` | ✅ |\n| 如何完整读取、追加和报告 sequence conflict？ | `zeta-thread-store` | ❌ |\n| 如何返回有界 Turn 窗口？ | Core projection / App Server | ❌ |\n| 如何写入 SQLite、提交事务和恢复？ | `zeta-storage` / `zeta-core` | ❌ |\n| 如何显示旧消息？ | App Server client / TUI | ❌ |\n\n因此它与 Codex 的 `codex-history` 一样属于 persisted-history domain types，而不是第二个\nThread Store。Zeta 仍只有一份 authoritative Thread stream。\n\n## 公共契约\n\n| Symbol | 职责 | 关键约束 |\n| --- | --- | --- |\n| `StoredEvent` | 一个 durable `ThreadEvent` 的 canonical envelope | 保留 thread、sequence、event ID、时间戳与可选 command receipt |\n| `ThreadCommandReceipt` | 被接受命令的 exact typed copy | 支持恢复后的幂等结果与同 ID/different payload 冲突检测 |\n| `EventId` | 单条历史记录 identity | 同一 stream 内不得重复 |\n| `Timestamp` | Unix milliseconds | 只描述记录时间，不参与 aggregate ordering |\n| `CURRENT_STORED_EVENT_SCHEMA_VERSION` | 新记录使用的 schema | writer 只产生 current version |\n| `MINIMUM_SUPPORTED_EVENT_SCHEMA_VERSION` | recovery 可接受的下界 | reducer 显式拒绝区间外版本 |\n| `supports_stored_event_schema_version` | 判断 persisted record 是否可读 | 读取兼容区间与新写 current version 分开 |\n\n```text\nzeta-protocol::ThreadEvent\n          │\n          ▼\nzeta-core constructs StoredEvent\n          │\n          ▼\nzeta-thread-store validates recovery/append contract\n          │\n          ▼\nzeta-storage serializes exact record to SQLite\n          │\n          ├─► zeta-core replays it during recovery\n          └─► zeta-rollout-trace exports a read-only copy\n```\n\n关键私有实现是 `record::StoredEvent` 及其 serde shape。`StoredEvent::thread_id` 返回 envelope\nidentity；Store 校验还会确认 inner `ThreadEvent::thread_id()` 与它一致，不能从 payload 猜测或\n覆盖 envelope identity。\n\n## 兼容性、失败与修改影响\n\n本 crate 不定义 I/O error。反序列化失败由 storage adapter 映射成 storage failure；schema\n版本不受支持由 Core recovery 或 Store write validation 明确拒绝。`record` 的 field name、serde\ntag、默认值或类型变化都是持久化兼容性变化，必须同步审查：\n\n- `zeta-core` 的 record 构造与 reducer recovery；\n- `zeta-thread-store::validate_append_batch`；\n- `zeta-storage::sqlite::thread`；\n- `zeta-rollout-trace` export 与 fixtures。\n\n```text\ncargo test -p zeta-history\nbazel test //zeta-rs/history:history-unit-tests\n```\n\n## 当前限制与演进\n\n当前新写入使用 schema version `3`，reader 仍接受 minimum version `1`；version 3 覆盖 Agent\ncontext seed、delegation、message/result facts 和自动 Skill activation command snapshot。本 crate\n只抽取已经在生产路径中使用的 Thread history record；Session envelope 仍由\n`zeta-session-store` 拥有。它没有照搬 Codex 尚无真实消费者的 `InitialHistory`、rollout JSONL\nline 或 harness metadata。未来只有当 Core、import/fork 或另一种持久化后端需要共同理解新的\nhistory-only 数据时，才在这里增加类型；查询、缓存和物理写入仍留在 Store/Storage。",
    "headings": [
      {
        "depth": 2,
        "id": "职责边界",
        "title": "职责边界"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "兼容性失败与修改影响",
        "title": "兼容性、失败与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 解释模型历史与持久化 Thread record 的数据契约。Canonical ThreadEvent 语义见 docs/protocol.md，恢复与追加接口见 zeta-thread-store，物理 SQLite 映射见 zeta-storage。 zeta-history 是纯数据层：它定义“一个 Thread 事实被长期保存时长什么样”。它不打开文件或 数据库，不做分页、事务、恢复、归约、模型调用，也不拥有 TUI 展示。 职责边界 问题 Owner 本 crate 是否负责 --- --- --- 哪些事实可以持久化？ zeta-protocol::ThreadEvent ❌，只引用 canonical fact 事实落盘时携带哪些稳定元数据？ zeta-history ✅ 如何完整读取、追加和报告 sequence conflict？ zeta-thread-store ❌ 如何返回有界 Turn 窗口？ Core projection / App Server ❌ 如何写入 SQLite、提交事务和恢复？ zeta-storage / zeta-core ❌ 如何显示旧消息？ App Server client / TUI ❌ 因此它与 Codex 的 codex-history 一样属于 persisted-history domain types，而不是第二个 Thread Store。Zeta 仍只有一份 authoritative Thread stream。 公共契约 Symbol 职责 关键约束 --- --- --- StoredEvent 一个 durable ThreadEvent 的 canonical envelope 保留 thread、sequence、event ID、时间戳与可选 command receipt ThreadCommandReceipt 被接受命令的 exact typed copy 支持恢复后的幂等结果与同 ID/different payload 冲突检测 EventId 单条历史记录 identity 同一 stream 内不得重复 Timestamp Unix milliseconds 只描述记录时间，不参与 aggregate ordering CURRENTSTOREDEVENTSCHEMAVERSION 新记录使用的 schema writer 只产生 current version MINIMUMSUPPORTEDEVENTSCHEMAVERSION recovery 可接受的下界 reducer 显式拒绝区间外版本 supportsstoredeventschemaversion 判断 persisted record 是否可读 读取兼容区间与新写 current version 分开 ``text zeta-protocol::ThreadEvent │ ▼ zeta-core constructs StoredEvent │ ▼ zeta-thread-store validates recovery/append contract │ ▼ zeta-storage serializes exact record to SQLite │ ├─► zeta-core replays it during recovery └─► zeta-rollout-trace exports a read-only copy ` 关键私有实现是 record::StoredEvent 及其 serde shape。StoredEvent::threadid 返回 envelope identity；Store 校验还会确认 inner ThreadEvent::threadid 与它一致，不能从 payload 猜测或 覆盖 envelope identity。 兼容性、失败与修改影响 本 crate 不定义 I/O error。反序列化失败由 storage adapter 映射成 storage failure；schema 版本不受支持由 Core recovery 或 Store write validation 明确拒绝。record 的 field name、serde tag、默认值或类型变化都是持久化兼容性变化，必须同步审查： - zeta-core 的 record 构造与 reducer recovery； - zeta-thread-store::validateappendbatch； - zeta-storage::sqlite::thread； - zeta-rollout-trace export 与 fixtures。 `text cargo test -p zeta-history bazel test //zeta-rs/history:history-unit-tests ` 当前限制与演进 当前新写入使用 schema version 3，reader 仍接受 minimum version 1；version 3 覆盖 Agent context seed、delegation、message/result facts 和自动 Skill activation command snapshot。本 crate 只抽取已经在生产路径中使用的 Thread history record；Session envelope 仍由 zeta-session-store 拥有。它没有照搬 Codex 尚无真实消费者的 InitialHistory`、rollout JSONL line 或 harness metadata。未来只有当 Core、import/fork 或另一种持久化后端需要共同理解新的 history-only 数据时，才在这里增加类型；查询、缓存和物理写入仍留在 Store/Storage。"
  },
  {
    "slug": "crates/http-client",
    "sourcePath": "zeta-rs/http-client/README.md",
    "title": "zeta-http-client",
    "description": "zeta-http-client 是 provider-neutral outbound HTTP substrate。它拥有 reusable backend、 proxy route、TLS trust/mTLS、redirect、transport timeout、connection pool、bounded unary body 和 safe telemetry…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释当前同步 HTTP transport 的真实实现与内部接口。Operation retry、SSE framing 和\n> deadline 设计见 [`docs/zeta-client.md`](../../docs/zeta-client.md)。\n\n`zeta-http-client` 是 provider-neutral outbound HTTP substrate。它拥有 reusable backend、\nproxy route、TLS trust/mTLS、redirect、transport timeout、connection pool、bounded unary body 和\nsafe telemetry。上层通过 `HttpClient` 执行一个已经完整构造的 request，且每次只执行一次。\n\n它不解释 provider JSON，不拥有 model operation retry，也不实现 SSE/NDJSON/WebSocket。\n\n## 当前实现边界\n\n```text\nzeta-api / auth / catalog / other HTTP consumers\n                       │\n             optional zeta-client\n             retry + framing\n                       │ one raw attempt\n                       ▼\n              zeta-http-client\n              ├─ proxy selection\n              ├─ TLS + mTLS\n              ├─ redirects/timeouts\n              ├─ connection reuse\n              └─ bounded response\n                       │\n                 private ureq\n```\n\nWorkspace 其他 crate 不应直接创建 `ureq::Agent` 或平行的 proxy/TLS policy。普通 unary HTTP\nconsumer 可以直接依赖本 crate；需要 operation retry 或 SSE framing 的调用再经过 `zeta-client`。\n\n## 公共契约\n\n### 请求与执行\n\n| Symbol | 职责 |\n| --- | --- |\n| `HttpClient` | `execute(&HttpRequest)` 一次；implementation 不得 retry |\n| `UreqHttpClient` | fallible、reusable synchronous production client；没有 panic-based `Default` |\n| `HttpMethod::{Get,Post}` | 当前支持的 method |\n| `HttpRequest` | validated HTTP(S) URL、headers 与 raw body |\n| `HttpResponse` | status、headers 与 bounded raw body |\n| `HttpHeader` | name/value pair；`Debug` 永远隐藏 value |\n| `HttpClientError` | invalid request/configuration 或 sanitized transport failure |\n\n包括 3xx/4xx/5xx 在内的 HTTP 状态都是 `HttpResponse` 事实，不是传输错误。是否重试以及如何\n解释状态，由上层操作或协议决定。\n\n### 配置\n\n| Symbol | 当前 contract |\n| --- | --- |\n| `HttpClientConfig` | immutable builder-style config snapshot |\n| `ProxyPolicy` | `Direct`、构造时读取环境、explicit、explicit + bypass |\n| `ProxyBypass` | `NO_PROXY` 风格 exact/suffix/IP/port/`*` rules |\n| `RedirectPolicy` | 默认拒绝，或 bounded `Follow { max_hops }` |\n| `Timeout` / `TransportTimeouts` | connect/read/write/overall 的 disabled/after policy |\n| `ConnectionPoolPolicy` | total 与 per-host idle connection 上限 |\n| `ResponseBodyLimit` | unary body hard limit，默认 10 MiB |\n| `TlsPolicy` | system roots、system + custom DER、custom DER only |\n| `ClientIdentityPolicy` | no identity 或 DER certificate chain + private key |\n| `CertificateBundle` | non-empty DER certificate bundle，debug 只显示数量 |\n| `ClientIdentity` | DER chain 与 zeroizing private key，debug redacted |\n\n`HttpClientConfig::default()` 当前使用环境 proxy、拒绝 redirect、30 秒 connect timeout、60 秒\noverall timeout、system roots、无 client identity、100/1 idle pool 和 10 MiB response limit。\n\n环境 proxy 与 bypass 在 `UreqHttpClient::new` / `with_config` 时快照，不在每个 request 重新读取。\n两者都返回 `Result`；proxy、自定义证书与 mTLS 静态材料在构造时校验。System roots 在第一次实际\nHTTPS request（或 HTTPS proxy route）时惰性加载并缓存结果，因此拒绝 redirect 的纯 HTTP/loopback\n不依赖 host certificate store；允许 redirect 的 HTTP route 会预备 TLS，因为目标可能升级到 HTTPS。\n加载失败由该次需要 TLS 的 invocation path 处理。\n\n### 遥测\n\n`TelemetryHttpClient` 包装任意 `Arc<dyn HttpClient>`，向 `HttpClientTelemetry::record` 发出\n`HttpClientTelemetryEvent`。事件只有：\n\n- method；\n- status class 或 transport failure；\n- request/response body byte count；\n- elapsed duration。\n\nURL、header、certificate、request/response body 和 provider identity 不在 telemetry type 中。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `UreqHttpClient::{http_direct_agent,http_proxy_agent}` | private fields | 不依赖 system roots 的 HTTP direct/proxied reusable pools | backend type 不进入 public API |\n| `UreqHttpClient::{secure_tls_config,https_direct_agent,https_proxy_agent}` | private fields | 首次 HTTPS route 时加载 roots 并惰性构造 reusable pool | 成功或失败都由 `OnceLock` 缓存 |\n| `UreqHttpClient::agent_for` | private method | 根据 scheme、proxy scheme、authority 与 bypass 选择/准备 agent | caller 不手选 route |\n| `resolve_proxy` | private | materialize proxy URL 与 bypass snapshot | `Direct` 不能受环境影响 |\n| `proxy_url_from_environment` | private | 固定优先级读取 proxy env | 只在 client 构造时调用 |\n| `build_agent` | private | 应用 proxy、redirect、timeouts、pool、TLS | 每个 request 不重新 build agent |\n| `request_authority` | private | 提取 hostname/IP 与 optional port | 仅用于 bypass，不进入 diagnostics |\n| `build_tls_config` | private | trust roots + optional client auth | 保持 hostname/chain validation |\n| `system_root_store` | private | 加载 host system roots | failure 是 configuration error |\n| `add_certificate_bundle` | private | 将 DER roots 加入 rustls store | 不记录 certificate bytes |\n| `rule_matches` / `split_authority` | private | `NO_PROXY`-style match | port rule 必须 exact |\n| `is_http_url` | private | request construction 的 scheme/authority guard | 非 HTTP(S) 在 backend 前拒绝 |\n| `HttpStatusClass::from_status` | private | telemetry low-cardinality classification | 不暴露 exact URL/status label |\n\n## 构造调用图\n\n```text\nUreqHttpClient::new() / with_config(config) → Result\n├─ build_tls_config(SystemRoots::Skip)\n│  ├─ add_certificate_bundle     [SystemPlus/CustomOnly]\n│  └─ ClientIdentity::private_key [mTLS]\n├─ build HTTP agent(config, tls, None)\n├─ resolve_proxy\n│  ├─ proxy_url_from_environment [FromEnvironment]\n│  └─ ProxyBypass::from_environment\n└─ build HTTP proxy agent(config, tls, proxy_url) [when proxy exists]\n\nfirst HTTPS request / HTTPS proxy route\n├─ build_tls_config(SystemRoots::Load)\n│  └─ system_root_store          [SystemRoots/SystemPlus]\n└─ build and cache HTTPS direct/proxy agent\n```\n\n一份 client 对应一份不可变 config generation。配置、certificate 或 proxy 变化应创建新 client；\n不要原地修改正在复用连接的 generation。\n\n## 请求调用图\n\n```text\nHttpClient::execute(request)\n└─ UreqHttpClient::execute\n   ├─ agent_for\n   │  ├─ request_authority\n   │  └─ ProxyBypass::matches\n   │     └─ rule_matches\n   ├─ construct private ureq request + copy headers\n   ├─ send request body exactly once\n   ├─ retain HTTP error statuses as responses\n   ├─ collect response status + headers\n   ├─ read at most configured limit + 1 byte\n   └─ reject overflow / return HttpResponse\n\nTelemetryHttpClient::execute\n├─ inner.execute\n├─ classify safe outcome + byte counts\n└─ HttpClientTelemetry::record\n```\n\n`ResponseBodyLimit::new` 拒绝 `usize::MAX`，因为 execute 需要额外一字节检测 overflow。提高 limit\n不是 streaming 的替代方案。\n\n## Proxy、TLS 与 secret 约束\n\nEnvironment proxy priority 当前为：\n\n```text\nALL_PROXY → all_proxy → HTTPS_PROXY → https_proxy → HTTP_PROXY → http_proxy\n```\n\n`NO_PROXY`/`no_proxy` 支持 `*`、exact host/IP、domain suffix 和 optional port。Explicit proxy URL\n的 `Debug` 输出为 `[REDACTED]`；invalid proxy error 不回显 URL 或 credential。\n\nTLS 使用 rustls：\n\n- `SystemRoots` 在首次 HTTPS route 加载 host roots；\n- `SystemPlus` 在 system roots 上增加 DER CA；\n- `CustomOnly` 只使用 supplied DER CA；\n- optional mTLS key 接受 PKCS#1、PKCS#8 或 SEC1 DER；\n- private key 存在 `Zeroizing<Vec<u8>>` 中；\n- public config 没有关闭 hostname/certificate validation 的 escape hatch。\n\nPEM/file loading、secret lookup 与 credential rotation 不属于本 crate；caller 在构造 config 前把材料\n解析为 DER。\n\n## 错误与安全语义\n\n`HttpClientError` 只有：\n\n- `InvalidRequest`：例如非 HTTP(S) URL；\n- `InvalidConfiguration`：proxy/TLS/identity/limit 无效；\n- `Transport`：backend send/read/body-limit failure。\n\nClient construction 同样只返回这些 typed errors；本 crate 不提供会在系统证书或 proxy 初始化失败\n时 panic 的 `Default` 实现。\n\n当前 error taxonomy 不区分 DNS、connect、proxy、TLS 或 timeout phase。Backend error 被替换成\nsanitized crate-owned message，避免 URL、proxy credential、certificate 或 payload 泄漏。\n\nRedirect follow 直接使用 backend 的 bounded redirect policy。当前尚未实现 crate-owned 的\ncross-origin sensitive-header stripping、scheme-downgrade rule 或 body replay classifier；不能在\n上层文档中把这些未来要求写成已有保证。安全敏感调用默认应保持 `RedirectPolicy::Reject`。\n\n## 方向偏差检查\n\n- `zeta-client` 或 provider crate 直接构造 `ureq::Agent`：proxy/TLS/pool ownership 分裂；\n- `UreqHttpClient::execute` 出现 retry loop：operation replay authority 下沉；\n- `HttpClientError` 包含 backend error/URL/header：redaction 边界被绕过；\n- request path 调用 `build_agent` 或读环境变量：config snapshot 与 pool reuse 被破坏；\n- Provider JSON/status semantics 进入本 crate：wire protocol ownership 下沉；\n- 通过提高 `ResponseBodyLimit` 支持无限流：bounded unary contract 被绕过；\n- telemetry 增加 URL、header value、body 或 exact provider/model：低敏感 contract 漂移；\n- public API 暴露 `ureq`/`rustls` type：backend replaceability 消失。\n\n修改 config field 时同步检查 `HttpClientConfig` builder/getter/default、`build_agent` 或\n`build_tls_config`、debug redaction 与 tests。修改 request/response shape 时同步检查\n`TelemetryHttpClient`、operation client adapter 与 fake transports。\n\n## 测试\n\n```text\ncargo test -p zeta-http-client\nbazel test //zeta-rs/http-client:http-client-unit-tests\n```\n\n测试使用本地 TCP 样例，不访问真实供应商，覆盖：\n\n- header/proxy/certificate/private-key debug redaction；\n- invalid URL/config；\n- one-attempt、non-2xx preservation 与 redirect rejection；\n- bypass domain/IP/port matching 和 direct route；\n- 纯 HTTP 不加载 system roots，HTTPS 惰性加载并缓存失败；\n- custom trust/mTLS invalid material；\n- response body hard limit 与 `limit + 1` headroom；\n- telemetry 只发出 safe facts。\n\n## 当前限制与潜在演进\n\n当前实现是 synchronous、unary、fully buffered、one-attempt HTTP。它使用 bounded transport\ntimeout，但不直接接收 caller cancellation token；上层 `zeta-client` 可以在取消后停止等待并丢弃\n迟到 response，不能强制关闭已经进入 `ureq` 的 socket attempt。stream body、WebSocket、async\nport、per-phase diagnostics、custom redirect security policy 和 config-generation rollover\nmanager 仍未实现。\n\n这些能力可以演进，但顺序应保持：先定义 provider-neutral typed contract 与 failure/redaction\ninvariant，再实现 private backend；不要先暴露 backend-specific future/stream/socket types。Retry、\nSSE/NDJSON framing 与 provider event decoding仍分别留在 `zeta-client` 和 `zeta-api`。",
    "headings": [
      {
        "depth": 2,
        "id": "当前实现边界",
        "title": "当前实现边界"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 3,
        "id": "请求与执行",
        "title": "请求与执行"
      },
      {
        "depth": 3,
        "id": "配置",
        "title": "配置"
      },
      {
        "depth": 3,
        "id": "遥测",
        "title": "遥测"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "构造调用图",
        "title": "构造调用图"
      },
      {
        "depth": 2,
        "id": "请求调用图",
        "title": "请求调用图"
      },
      {
        "depth": 2,
        "id": "proxytls-与-secret-约束",
        "title": "Proxy、TLS 与 secret 约束"
      },
      {
        "depth": 2,
        "id": "错误与安全语义",
        "title": "错误与安全语义"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试",
        "title": "测试"
      },
      {
        "depth": 2,
        "id": "当前限制与潜在演进",
        "title": "当前限制与潜在演进"
      }
    ],
    "searchText": " 本 README 解释当前同步 HTTP transport 的真实实现与内部接口。Operation retry、SSE framing 和 deadline 设计见 docs/zeta-client.md。 zeta-http-client 是 provider-neutral outbound HTTP substrate。它拥有 reusable backend、 proxy route、TLS trust/mTLS、redirect、transport timeout、connection pool、bounded unary body 和 safe telemetry。上层通过 HttpClient 执行一个已经完整构造的 request，且每次只执行一次。 它不解释 provider JSON，不拥有 model operation retry，也不实现 SSE/NDJSON/WebSocket。 当前实现边界 ``text zeta-api / auth / catalog / other HTTP consumers │ optional zeta-client retry + framing │ one raw attempt ▼ zeta-http-client ├─ proxy selection ├─ TLS + mTLS ├─ redirects/timeouts ├─ connection reuse └─ bounded response │ private ureq ` Workspace 其他 crate 不应直接创建 ureq::Agent 或平行的 proxy/TLS policy。普通 unary HTTP consumer 可以直接依赖本 crate；需要 operation retry 或 SSE framing 的调用再经过 zeta-client。 公共契约 请求与执行 Symbol 职责 --- --- HttpClient execute &HttpRequest 一次；implementation 不得 retry UreqHttpClient fallible、reusable synchronous production client；没有 panic-based Default HttpMethod:: Get,Post 当前支持的 method HttpRequest validated HTTP S URL、headers 与 raw body HttpResponse status、headers 与 bounded raw body HttpHeader name/value pair；Debug 永远隐藏 value HttpClientError invalid request/configuration 或 sanitized transport failure 包括 3xx/4xx/5xx 在内的 HTTP 状态都是 HttpResponse 事实，不是传输错误。是否重试以及如何 解释状态，由上层操作或协议决定。 配置 Symbol 当前 contract --- --- HttpClientConfig immutable builder-style config snapshot ProxyPolicy Direct、构造时读取环境、explicit、explicit + bypass ProxyBypass NOPROXY 风格 exact/suffix/IP/port/ rules RedirectPolicy 默认拒绝，或 bounded Follow maxhops Timeout / TransportTimeouts connect/read/write/overall 的 disabled/after policy ConnectionPoolPolicy total 与 per-host idle connection 上限 ResponseBodyLimit unary body hard limit，默认 10 MiB TlsPolicy system roots、system + custom DER、custom DER only ClientIdentityPolicy no identity 或 DER certificate chain + private key CertificateBundle non-empty DER certificate bundle，debug 只显示数量 ClientIdentity DER chain 与 zeroizing private key，debug redacted HttpClientConfig::default 当前使用环境 proxy、拒绝 redirect、30 秒 connect timeout、60 秒 overall timeout、system roots、无 client identity、100/1 idle pool 和 10 MiB response limit。 环境 proxy 与 bypass 在 UreqHttpClient::new / withconfig 时快照，不在每个 request 重新读取。 两者都返回 Result；proxy、自定义证书与 mTLS 静态材料在构造时校验。System roots 在第一次实际 HTTPS request（或 HTTPS proxy route）时惰性加载并缓存结果，因此拒绝 redirect 的纯 HTTP/loopback 不依赖 host certificate store；允许 redirect 的 HTTP route 会预备 TLS，因为目标可能升级到 HTTPS。 加载失败由该次需要 TLS 的 invocation path 处理。 遥测 TelemetryHttpClient 包装任意 Arc<dyn HttpClient ，向 HttpClientTelemetry::record 发出 HttpClientTelemetryEvent。事件只有： - method； - status class 或 transport failure； - request/response body byte count； - elapsed duration。 URL、header、certificate、request/response body 和 provider identity 不在 telemetry type 中。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- UreqHttpClient:: httpdirectagent,httpproxyagent private fields 不依赖 system roots 的 HTTP direct/proxied reusable pools backend type 不进入 public API UreqHttpClient:: securetlsconfig,httpsdirectagent,httpsproxyagent private fields 首次 HTTPS route 时加载 roots 并惰性构造 reusable pool 成功或失败都由 OnceLock 缓存 UreqHttpClient::agentfor private method 根据 scheme、proxy scheme、authority 与 bypass 选择/准备 agent caller 不手选 route resolveproxy private materialize proxy URL 与 bypass snapshot Direct 不能受环境影响 proxyurlfromenvironment private 固定优先级读取 proxy env 只在 client 构造时调用 buildagent private 应用 proxy、redirect、timeouts、pool、TLS 每个 request 不重新 build agent requestauthority private 提取 hostname/IP 与 optional port 仅用于 bypass，不进入 diagnostics buildtlsconfig private trust roots + optional client auth 保持 hostname/chain validation systemrootstore private 加载 host system roots failure 是 configuration error addcertificatebundle private 将 DER roots 加入 rustls store 不记录 certificate bytes rulematches / splitauthority private NOPROXY-style match port rule 必须 exact ishttpurl private request construction 的 scheme/authority guard 非 HTTP S 在 backend 前拒绝 HttpStatusClass::fromstatus private telemetry low-cardinality classification 不暴露 exact URL/status label 构造调用图 `text UreqHttpClient::new / withconfig config → Result ├─ buildtlsconfig SystemRoots::Skip │ ├─ addcertificatebundle SystemPlus/CustomOnly │ └─ ClientIdentity::privatekey mTLS ├─ build HTTP agent config, tls, None ├─ resolveproxy │ ├─ proxyurlfromenvironment FromEnvironment │ └─ ProxyBypass::fromenvironment └─ build HTTP proxy agent config, tls, proxyurl when proxy exists first HTTPS request / HTTPS proxy route ├─ buildtlsconfig SystemRoots::Load │ └─ systemrootstore SystemRoots/SystemPlus └─ build and cache HTTPS direct/proxy agent ` 一份 client 对应一份不可变 config generation。配置、certificate 或 proxy 变化应创建新 client； 不要原地修改正在复用连接的 generation。 请求调用图 `text HttpClient::execute request └─ UreqHttpClient::execute ├─ agentfor │ ├─ requestauthority │ └─ ProxyBypass::matches │ └─ rulematches ├─ construct private ureq request + copy headers ├─ send request body exactly once ├─ retain HTTP error statuses as responses ├─ collect response status + headers ├─ read at most configured limit + 1 byte └─ reject overflow / return HttpResponse TelemetryHttpClient::execute ├─ inner.execute ├─ classify safe outcome + byte counts └─ HttpClientTelemetry::record ` ResponseBodyLimit::new 拒绝 usize::MAX，因为 execute 需要额外一字节检测 overflow。提高 limit 不是 streaming 的替代方案。 Proxy、TLS 与 secret 约束 Environment proxy priority 当前为： `text ALLPROXY → allproxy → HTTPSPROXY → httpsproxy → HTTPPROXY → httpproxy ` NOPROXY/noproxy 支持 、exact host/IP、domain suffix 和 optional port。Explicit proxy URL 的 Debug 输出为 REDACTED ；invalid proxy error 不回显 URL 或 credential。 TLS 使用 rustls： - SystemRoots 在首次 HTTPS route 加载 host roots； - SystemPlus 在 system roots 上增加 DER CA； - CustomOnly 只使用 supplied DER CA； - optional mTLS key 接受 PKCS 1、PKCS 8 或 SEC1 DER； - private key 存在 Zeroizing<Vec<u8 中； - public config 没有关闭 hostname/certificate validation 的 escape hatch。 PEM/file loading、secret lookup 与 credential rotation 不属于本 crate；caller 在构造 config 前把材料 解析为 DER。 错误与安全语义 HttpClientError 只有： - InvalidRequest：例如非 HTTP S URL； - InvalidConfiguration：proxy/TLS/identity/limit 无效； - Transport：backend send/read/body-limit failure。 Client construction 同样只返回这些 typed errors；本 crate 不提供会在系统证书或 proxy 初始化失败 时 panic 的 Default 实现。 当前 error taxonomy 不区分 DNS、connect、proxy、TLS 或 timeout phase。Backend error 被替换成 sanitized crate-owned message，避免 URL、proxy credential、certificate 或 payload 泄漏。 Redirect follow 直接使用 backend 的 bounded redirect policy。当前尚未实现 crate-owned 的 cross-origin sensitive-header stripping、scheme-downgrade rule 或 body replay classifier；不能在 上层文档中把这些未来要求写成已有保证。安全敏感调用默认应保持 RedirectPolicy::Reject。 方向偏差检查 - zeta-client 或 provider crate 直接构造 ureq::Agent：proxy/TLS/pool ownership 分裂； - UreqHttpClient::execute 出现 retry loop：operation replay authority 下沉； - HttpClientError 包含 backend error/URL/header：redaction 边界被绕过； - request path 调用 buildagent 或读环境变量：config snapshot 与 pool reuse 被破坏； - Provider JSON/status semantics 进入本 crate：wire protocol ownership 下沉； - 通过提高 ResponseBodyLimit 支持无限流：bounded unary contract 被绕过； - telemetry 增加 URL、header value、body 或 exact provider/model：低敏感 contract 漂移； - public API 暴露 ureq/rustls type：backend replaceability 消失。 修改 config field 时同步检查 HttpClientConfig builder/getter/default、buildagent 或 buildtlsconfig、debug redaction 与 tests。修改 request/response shape 时同步检查 TelemetryHttpClient、operation client adapter 与 fake transports。 测试 `text cargo test -p zeta-http-client bazel test //zeta-rs/http-client:http-client-unit-tests ` 测试使用本地 TCP 样例，不访问真实供应商，覆盖： - header/proxy/certificate/private-key debug redaction； - invalid URL/config； - one-attempt、non-2xx preservation 与 redirect rejection； - bypass domain/IP/port matching 和 direct route； - 纯 HTTP 不加载 system roots，HTTPS 惰性加载并缓存失败； - custom trust/mTLS invalid material； - response body hard limit 与 limit + 1 headroom； - telemetry 只发出 safe facts。 当前限制与潜在演进 当前实现是 synchronous、unary、fully buffered、one-attempt HTTP。它使用 bounded transport timeout，但不直接接收 caller cancellation token；上层 zeta-client 可以在取消后停止等待并丢弃 迟到 response，不能强制关闭已经进入 ureq 的 socket attempt。stream body、WebSocket、async port、per-phase diagnostics、custom redirect security policy 和 config-generation rollover manager 仍未实现。 这些能力可以演进，但顺序应保持：先定义 provider-neutral typed contract 与 failure/redaction invariant，再实现 private backend；不要先暴露 backend-specific future/stream/socket types。Retry、 SSE/NDJSON framing 与 provider event decoding仍分别留在 zeta-client 和 zeta-api`。"
  },
  {
    "slug": "crates/zeterm/icon",
    "sourcePath": "zeterm/icon/README.md",
    "title": "zeta-icon",
    "description": "zeta-icon defines the renderer-independent icon asset contract consumed by the reusable zui framework and zeta-ui components. It does not provide a semantic icon catalog, product artwork,…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-icon` defines the renderer-independent icon asset contract consumed by\nthe reusable `zui` framework and `zeta-ui` components. It does not provide a\nsemantic icon catalog, product artwork, layout, theme, or GPU implementation.\n\n## Ownership\n\n| Concern | Owner |\n| --- | --- |\n| Stable asset identity and validation | `IconId` |\n| Embedded artwork and rendering mode | `IconDefinition` / `IconRendering` |\n| Copyable icon asset value | `Icon` |\n| Product semantic icon catalog | An application crate such as `zeta-icons` |\n| Icon placement and scene primitive | `zui::PaintIcon` |\n| Rasterization, atlas, and GPU submission | Renderer backend |\n\nApplications may define their own `Icon` values without depending on the\n`zeterm` product icon catalog. `zeta-icons` is one optional catalog that maps\nZeta semantic icon identities to embedded SVG artwork.\n\n## Verification\n\n```bash\ncargo test -p zeta-icon\n```",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "verification",
        "title": "Verification"
      }
    ],
    "searchText": "zeta-icon defines the renderer-independent icon asset contract consumed by the reusable zui framework and zeta-ui components. It does not provide a semantic icon catalog, product artwork, layout, theme, or GPU implementation. Ownership Concern Owner --- --- Stable asset identity and validation IconId Embedded artwork and rendering mode IconDefinition / IconRendering Copyable icon asset value Icon Product semantic icon catalog An application crate such as zeta-icons Icon placement and scene primitive zui::PaintIcon Rasterization, atlas, and GPU submission Renderer backend Applications may define their own Icon values without depending on the zeterm product icon catalog. zeta-icons is one optional catalog that maps Zeta semantic icon identities to embedded SVG artwork. Verification ``bash cargo test -p zeta-icon ``"
  },
  {
    "slug": "crates/zeterm/icons",
    "sourcePath": "zeterm/icons/README.md",
    "title": "zeta-icons",
    "description": "zeta-icons 是可选的 product semantic catalog crate。通用 renderer-independent icon asset contract 由 zeta-icon 拥有；本 crate 把稳定的语义 icon library 与可替换的 resources/icons/.svg artwork 分层，并明确区分 IconRende…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 Rust 客户端的 product-icon identity、静态 SVG binding 与 rendering-mode\n> contract。跨客户端 ownership 由 [`docs/icons.md`](../../docs/icons.md) 定义；canonical\n> artwork 操作由 [`resources/README.md`](../../resources/README.md) 定义。\n\n`zeta-icons` 是可选的 product semantic catalog crate。通用 renderer-independent icon asset\ncontract 由 [`zeta-icon`](../icon/README.md) 拥有；本 crate 把稳定的语义 icon library 与可替换的\n`resources/icons/*.svg` artwork 分层，并明确区分 `IconRendering::Symbolic` 与\n`IconRendering::Multicolor`。它不依赖 `zeta-ui`，也不拥有布局、主题颜色、GPU atlas、\nrasterization 或 component。\n\n## 所有权\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `zeta-icon::{IconId, IconDefinition, IconRendering, Icon}` | re-export | 通用 icon asset contract；canonical owner 是 `zeta-icon` |\n| `library::icons` | private module、显式 re-export | 手工维护稳定的 `icons::*` typed semantic constants |\n| `library::ALL_ICONS` | private module、显式 re-export | 按 semantic ID 排序的公共 library catalog |\n| `icon_by_id` | public | 只解析已登记的 semantic ID，不解析 artwork filename |\n| `generated::artwork` | crate-private、generated | 把全部 canonical SVG bytes 绑定为 `IconDefinition` |\n| `generated::ALL_ARTWORK` | crate-private、generated | 供完整性测试覆盖全部 artwork，不构成产品 API |\n| `zeta-ui::PaintIcon` | external | logical placement、tint、clip 与 renderer submission |\n| `zeta-ui::IconLabel` | external | icon/text component layout |\n\n实际调用与生成关系：\n\n```text\nresources/icons/*.svg\n  → syncRustIcons\n  → generated::artwork\n  → library::icons\n  → zeta-icon::Icon / icon_by_id\n  → zeta-ui\n```\n\n`library::icons` 可以让多个 identity 共享 artwork，例如 `HISTORY` 使用 `REFRESH`，\n`DROPDOWN_INDICATOR` 使用 `CHEVRON_DOWN`。因此 SVG 文件重命名或替换不要求调用方迁移\n`IconId`。新增 SVG 不会自动扩大公共 API；只有产品语义确定后才应在 `library.rs` 登记。\nNative input context toolbar 当前登记并消费 `LOCAL`、`WORKING_DIRECTORY`、`GIT_BRANCH` 与\n`DIFF`；Native Files toolbar 另外消费 `REFRESH` 与 `SEARCH`，并用文本箭头呈现 upstream\ndistance；Files pane rows consume `FILES` for branches and `FILE_TEXT` for leaf/search entries。\n这些都是稳定语义 identity，不把底层 artwork filename 暴露给 Toolbar 或 file tree。\n\n## 生成与失败语义\n\n新增、删除或重命名 SVG 后运行：\n\n```bash\nnode scripts/sync-rust-icons.mjs\nnode scripts/sync-rust-icons.mjs --check\ncargo test --manifest-path Cargo.toml -p zeta-icons\n```\n\n`src/generated.rs` 是 checked-in build input，Cargo 和 Bazel 构建都不在 compile action 中运行\ngenerator。`syncRustIcons` 拒绝不规范的 filename、多 SVG root、active/external content，并根据\n固定 paint 判断 rendering mode；`--check` 在 checked-in output 过期时失败。\n\n新增产品语义时更新 `src/library.rs`、`src/icon_tests.rs`，并确认 Desktop\n`lxiconsLibrary.ts` 是否应保持同一跨客户端 identity。只替换现有 artwork 时更新 SVG 和生成文件，\n不修改 semantic ID。任何 runtime registry、GPU type、component style 或 file-extension matching\n进入本 crate 都表示 ownership 漂移；Seti file-icon resolution 继续由 `zeta-file-icons` 拥有。\n\n当前 crate 能携带 multicolor definition；`zeta-ui` 使用 fixed-color atlas 与 symbolic-mask\natlas 实现该 contract。跨 renderer 状态和演进见 [`docs/icons.md`](../../docs/icons.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "生成与失败语义",
        "title": "生成与失败语义"
      }
    ],
    "searchText": " 本 README 拥有 Rust 客户端的 product-icon identity、静态 SVG binding 与 rendering-mode contract。跨客户端 ownership 由 docs/icons.md 定义；canonical artwork 操作由 resources/README.md 定义。 zeta-icons 是可选的 product semantic catalog crate。通用 renderer-independent icon asset contract 由 zeta-icon 拥有；本 crate 把稳定的语义 icon library 与可替换的 resources/icons/.svg artwork 分层，并明确区分 IconRendering::Symbolic 与 IconRendering::Multicolor。它不依赖 zeta-ui，也不拥有布局、主题颜色、GPU atlas、 rasterization 或 component。 所有权 Symbol 可见性 精确职责 --- --- --- zeta-icon:: IconId, IconDefinition, IconRendering, Icon re-export 通用 icon asset contract；canonical owner 是 zeta-icon library::icons private module、显式 re-export 手工维护稳定的 icons:: typed semantic constants library::ALLICONS private module、显式 re-export 按 semantic ID 排序的公共 library catalog iconbyid public 只解析已登记的 semantic ID，不解析 artwork filename generated::artwork crate-private、generated 把全部 canonical SVG bytes 绑定为 IconDefinition generated::ALLARTWORK crate-private、generated 供完整性测试覆盖全部 artwork，不构成产品 API zeta-ui::PaintIcon external logical placement、tint、clip 与 renderer submission zeta-ui::IconLabel external icon/text component layout 实际调用与生成关系： ``text resources/icons/.svg → syncRustIcons → generated::artwork → library::icons → zeta-icon::Icon / iconbyid → zeta-ui ` library::icons 可以让多个 identity 共享 artwork，例如 HISTORY 使用 REFRESH， DROPDOWNINDICATOR 使用 CHEVRONDOWN。因此 SVG 文件重命名或替换不要求调用方迁移 IconId。新增 SVG 不会自动扩大公共 API；只有产品语义确定后才应在 library.rs 登记。 Native input context toolbar 当前登记并消费 LOCAL、WORKINGDIRECTORY、GITBRANCH 与 DIFF；Native Files toolbar 另外消费 REFRESH 与 SEARCH，并用文本箭头呈现 upstream distance；Files pane rows consume FILES for branches and FILETEXT for leaf/search entries。 这些都是稳定语义 identity，不把底层 artwork filename 暴露给 Toolbar 或 file tree。 生成与失败语义 新增、删除或重命名 SVG 后运行： `bash node scripts/sync-rust-icons.mjs node scripts/sync-rust-icons.mjs --check cargo test --manifest-path Cargo.toml -p zeta-icons ` src/generated.rs 是 checked-in build input，Cargo 和 Bazel 构建都不在 compile action 中运行 generator。syncRustIcons 拒绝不规范的 filename、多 SVG root、active/external content，并根据 固定 paint 判断 rendering mode；--check 在 checked-in output 过期时失败。 新增产品语义时更新 src/library.rs、src/icontests.rs，并确认 Desktop lxiconsLibrary.ts 是否应保持同一跨客户端 identity。只替换现有 artwork 时更新 SVG 和生成文件， 不修改 semantic ID。任何 runtime registry、GPU type、component style 或 file-extension matching 进入本 crate 都表示 ownership 漂移；Seti file-icon resolution 继续由 zeta-file-icons 拥有。 当前 crate 能携带 multicolor definition；zeta-ui 使用 fixed-color atlas 与 symbolic-mask atlas 实现该 contract。跨 renderer 状态和演进见 docs/icons.md`。"
  },
  {
    "slug": "crates/input-classifier",
    "sourcePath": "zeta-rs/input-classifier/README.md",
    "title": "zeta-input-classifier",
    "description": "zeta-input-classifier 拥有本地 Shell/Agent 自动路由的完整决策管线：自然语言 parser、最近提交历史、 确定性规则、BERT-Tiny v3 模型与 tokenizer、模型失败后的 fallback。Shell parser、command signatures、 工作区/PATH/alias token evidence 和 co…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-input-classifier` 拥有本地 Shell/Agent 自动路由的完整决策管线：自然语言 parser、最近提交历史、\n确定性规则、BERT-Tiny v3 模型与 tokenizer、模型失败后的 fallback。Shell parser、command signatures、\n工作区/PATH/alias token evidence 和 completion candidates 由\n[`zeta-shell-completion`](../shell-completion/README.md) canonical 拥有，本 crate 只消费它的 snapshot。\n产品宿主只提供当前工作目录、当前路由和会话位置，不得复制标签顺序、阈值或词典。分类结果不做\n命令风险判断，也不授权执行。\n\n## 公共契约\n\n| Symbol | 职责 | 接入要求 |\n| --- | --- | --- |\n| `InputClassifier` | 持有工作目录和 PATH 快照，执行整条分类管线 | 一个 Composer 持有一个实例；工作区切换时调用 `set_working_directory` |\n| `InputClassificationContext` | 提供 `current_route` 和 `InputConversation` | 只用于自动模式；显式模式必须优先 |\n| `InputConversation` | 区分普通输入与 Agent 回复后的短追问 | Agent Turn 完成后设为 `AgentFollowUp`，新提交或失败后复位 |\n| `InputHistoryEntry` | 提供按时间排序的 Shell 命令和 Agent prompt | Snapshot 必须按 Turn 顺序重建；command-not-found 不进入 Shell 历史 |\n| `InputClassification` | 返回路由、置信度与决策来源 | 置信度只用于诊断，不得作为执行授权 |\n| `start_background_warmup` | 后台解码一次内嵌模型和 tokenizer | 创建输入界面时调用，避免首次按键承担加载延迟 |\n| `shell_completions` / `shell_completion_snapshot` | 从分类器持有的同一 Shell context 返回补全项；snapshot 另含当前 token 的精确匹配状态 | UI 可投影结果，但不能建立第二套 parser/registry |\n| `replace_shell_aliases` / `set_shell_path_entries` | 更新宿主提供的 Shell 环境快照 | 只能传入当前执行环境的事实；不得猜测 alias |\n\nZeterm 只把 `InputRoute` 映射为 Composer 模式，并把真实 Turn 生命周期投影成\n`InputConversation` 和按顺序排列的 `InputHistoryEntry`。Shell parser、相似度计算、PATH/manifest\n检查和 fallback 不再属于产品宿主。\n\n## 执行路径\n\n```text\nInputClassifier::classify\n  → parse_query_into_tokens\n  → contextual rules\n      empty / current-Agent NL one-off / Agent follow-up\n  → InputHistory\n      newest Shell command / Agent prompt close match, cutoff 0.9\n  → NL one-off / Shell allowlist\n  → ShellContext::analyze\n      zeta-shell-completion::ShellCompletionEngine::analyze\n      executable + builtin + recursive signature + workspace/alias/path descriptions\n      strict token threshold\n  → EmbeddedClassifier\n      Tokenizer::encode_fast\n      candle_onnx::simple_eval\n      temperature scaling + softmax\n  → ordinary error: current_route\n  → panic: dictionary/stemmer heuristic fallback\n```\n\nShell token 阈值采用 Warp 当前严格方案：所有解析 token 都有 Shell 语义时直接判为 Shell；少于\n3 个自然语言 token 时，只要最后一个 command-position token 是已知命令也判为 Shell。其他输入交给模型，避免把\n`git status 是做什么的` 这类 command-prefix 问句提前截成命令。\n\n这里对齐的是判定顺序和阈值，不是复制 Warp 的补全器数据。两者当前的 Shell 证据边界如下：\n\n| 证据 | Warp | Zeta 当前实现 |\n| --- | --- | --- |\n| 顶层命令 | 补全器的 `ParsedTokensSnapshot` | `zeta-shell-completion` registry、Shell builtin 和 PATH 可执行文件 |\n| 参数 | 命令签名和 token description | 递归 command spec、精确 option/value、现有路径和工作区 target |\n| alias | 在分类前用当前会话的补全上下文展开 | engine 已实现有界展开；由产品宿主提供 alias snapshot |\n| 证据不足 | 交给 BERT-Tiny | 交给 BERT-Tiny |\n\n当前 Zeterm 的 Shell Turn 由 App Server 以 `/bin/sh -lc` 执行，尚未向 engine 提供交互 PTY alias 和动态\ngenerator 候选；因此静态 command grammar、PATH 和 workspace evidence 已生效，alias API 暂时没有产品数据源。\n如果后续让 Composer 直接执行到交互 PTY，应由 Zeterm adapter 提供带环境 revision 的 alias/动态候选快照；\n不应把 PTY 或命令执行运行时移进 `zeta-input-classifier` 或 `zeta-shell-completion`。\n\n`natural_language.rs` 的 fallback 会分别尝试排除和包含未完成的末 token，并按 1.0、0.8、\n0.6 的长度阈值使用 English stems、developer terms 和 command-overlap 词典打分。这三份词典为\nZeta 独立构建，没有导入或变换 Warp 的词表。英文候选数据来自 MIT 许可的 TextBlob bundled\nspelling-frequency data，并经过 frequency threshold 和 Snowball stemming；developer terms 和\ncommand-overlap 是为 Zeta 独立整理的集合。TextBlob 许可文本位于\n[`dictionaries/THIRD_PARTY_NOTICES.txt`](dictionaries/THIRD_PARTY_NOTICES.txt)。词典作为本 crate\n的数据内嵌进二进制，不引入单独运行时。\n\n## 模型资产与失败语义\n\n当前发布来自 `chogng/zeta-classifier` 的 `models/bert_tiny_v3`：\n\n- `bert_tiny_v3_candle.onnx`：Candle 0.9 专用 FP32、opset 16；\n- `bert_tiny_tokenizer.json`：训练分区适配 tokenizer；\n- `metadata.json`：标签、温度、算子与摘要；\n- 输出 index 0 为 Shell、index 1 为 Agent；softmax 前温度为 `1.6894922825552194`。\n\n模型和 tokenizer 通过 `include_bytes!` 固定进二进制，不在运行时下载。`EmbeddedClassifier` 是\n模型、tokenizer 和 CPU device 的唯一 owner。模型或 tokenizer 初始化失败会直接启用完整\n`HeuristicFallback`；初始化成功后的编码、推理或输出校验错误返回 `CurrentRouteFallback`，保留用户\n正在使用的模式；Candle panic 会被隔离，并在当前进程永久改走 `HeuristicFallback`。官方\n`candle-onnx` 的构建脚本需要系统 `protoc`，产品运行时不需要。\n\n更新模型时必须一起更新 ONNX、tokenizer、`metadata.json`、摘要常量、标签解释、温度和概率基线\n测试，不能只替换其中一个文件。\n\n## 内部 owner 与修改影响\n\n- `classifier::classify_with_model` 固定决策顺序与失败分支；改动会影响所有自动模式消费者。\n- `history::InputHistory` 拥有 0.9 相似度门槛和“最新匹配胜出”语义；宿主只提供有序事实。\n- `shell::ShellContext` 只适配 `zeta-shell-completion::ShellCompletionEngine` 与 classifier 阈值；parser、\n  command registry 和 completion 不得移回本 crate 或 Zeterm。\n- `rules` 只放低风险、确定性的上下文和 allowlist 短路；模糊规则不得绕过模型。\n- `model::EmbeddedClassifier` 绑定模型图、tokenizer、标签和温度；任一资产变更都需要概率测试。\n- `natural_language::classify_with_fallback_heuristic` 只在模型不可用或 panic 后接管；它使用 Zeta\n  自有词典，不应成为正常主路径。`natural_language` 保持为本 crate 的私有模块：产品消费者必须\n  调用完整 `InputClassifier`，不得直接依赖词典分数绕过历史、Shell evidence 和模型。\n\n当前没有第二个需要“原始自然语言分数”而非 Shell/Agent 路由的后端无关消费者，因此不拆独立\nnatural-language-detection crate。只有出现这样的真实消费者，并且能定义不依赖 `InputRoute`、\n`ShellTokenSnapshot` 和 classifier fallback 阈值的纯打分契约时，才应把词典和 scorer 一起抽出；\n路由顺序与失败语义仍由 `zeta-input-classifier` 拥有。\n\n## 验证\n\n```bash\ncargo test -p zeta-input-classifier\ncargo clippy -p zeta-input-classifier --all-targets -- -D warnings\n```\n\n测试覆盖 parser、历史冲突、严格 token 阈值、工作区命令、自然语言词典、follow-up、普通模型错误、模型 panic、\n模型路由样例、资产 SHA 和 Candle FP32 概率基线。当前模型仍可能误判自由措辞，例如\n`chmod 755 是什么意思`；用户显式选择始终覆盖自动分类。\n\n`InputClassifier::classify` 是同步调用；`start_background_warmup` 只提前解码模型和 tokenizer。\n当前 Zeterm 在编辑变更时直接调用分类，并从同一个 `InputClassifier` 请求 Shell completion；候选由\n`AgentComposer` 收敛为输入光标后的 ghost text，并通过 editor 的精确 text edit 应用；Slash/模型\n选择 Pane 不承载 Shell 候选。将模型推理移出 UI 线程、废弃\n过期结果和添加 debounce 属于 Zeterm adapter 的产品接线工作，不改变本 crate 的决策契约。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "执行路径",
        "title": "执行路径"
      },
      {
        "depth": 2,
        "id": "模型资产与失败语义",
        "title": "模型资产与失败语义"
      },
      {
        "depth": 2,
        "id": "内部-owner-与修改影响",
        "title": "内部 owner 与修改影响"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": "zeta-input-classifier 拥有本地 Shell/Agent 自动路由的完整决策管线：自然语言 parser、最近提交历史、 确定性规则、BERT-Tiny v3 模型与 tokenizer、模型失败后的 fallback。Shell parser、command signatures、 工作区/PATH/alias token evidence 和 completion candidates 由 zeta-shell-completion canonical 拥有，本 crate 只消费它的 snapshot。 产品宿主只提供当前工作目录、当前路由和会话位置，不得复制标签顺序、阈值或词典。分类结果不做 命令风险判断，也不授权执行。 公共契约 Symbol 职责 接入要求 --- --- --- InputClassifier 持有工作目录和 PATH 快照，执行整条分类管线 一个 Composer 持有一个实例；工作区切换时调用 setworkingdirectory InputClassificationContext 提供 currentroute 和 InputConversation 只用于自动模式；显式模式必须优先 InputConversation 区分普通输入与 Agent 回复后的短追问 Agent Turn 完成后设为 AgentFollowUp，新提交或失败后复位 InputHistoryEntry 提供按时间排序的 Shell 命令和 Agent prompt Snapshot 必须按 Turn 顺序重建；command-not-found 不进入 Shell 历史 InputClassification 返回路由、置信度与决策来源 置信度只用于诊断，不得作为执行授权 startbackgroundwarmup 后台解码一次内嵌模型和 tokenizer 创建输入界面时调用，避免首次按键承担加载延迟 shellcompletions / shellcompletionsnapshot 从分类器持有的同一 Shell context 返回补全项；snapshot 另含当前 token 的精确匹配状态 UI 可投影结果，但不能建立第二套 parser/registry replaceshellaliases / setshellpathentries 更新宿主提供的 Shell 环境快照 只能传入当前执行环境的事实；不得猜测 alias Zeterm 只把 InputRoute 映射为 Composer 模式，并把真实 Turn 生命周期投影成 InputConversation 和按顺序排列的 InputHistoryEntry。Shell parser、相似度计算、PATH/manifest 检查和 fallback 不再属于产品宿主。 执行路径 ``text InputClassifier::classify → parsequeryintotokens → contextual rules empty / current-Agent NL one-off / Agent follow-up → InputHistory newest Shell command / Agent prompt close match, cutoff 0.9 → NL one-off / Shell allowlist → ShellContext::analyze zeta-shell-completion::ShellCompletionEngine::analyze executable + builtin + recursive signature + workspace/alias/path descriptions strict token threshold → EmbeddedClassifier Tokenizer::encodefast candleonnx::simpleeval temperature scaling + softmax → ordinary error: currentroute → panic: dictionary/stemmer heuristic fallback ` Shell token 阈值采用 Warp 当前严格方案：所有解析 token 都有 Shell 语义时直接判为 Shell；少于 3 个自然语言 token 时，只要最后一个 command-position token 是已知命令也判为 Shell。其他输入交给模型，避免把 git status 是做什么的 这类 command-prefix 问句提前截成命令。 这里对齐的是判定顺序和阈值，不是复制 Warp 的补全器数据。两者当前的 Shell 证据边界如下： 证据 Warp Zeta 当前实现 --- --- --- 顶层命令 补全器的 ParsedTokensSnapshot zeta-shell-completion registry、Shell builtin 和 PATH 可执行文件 参数 命令签名和 token description 递归 command spec、精确 option/value、现有路径和工作区 target alias 在分类前用当前会话的补全上下文展开 engine 已实现有界展开；由产品宿主提供 alias snapshot 证据不足 交给 BERT-Tiny 交给 BERT-Tiny 当前 Zeterm 的 Shell Turn 由 App Server 以 /bin/sh -lc 执行，尚未向 engine 提供交互 PTY alias 和动态 generator 候选；因此静态 command grammar、PATH 和 workspace evidence 已生效，alias API 暂时没有产品数据源。 如果后续让 Composer 直接执行到交互 PTY，应由 Zeterm adapter 提供带环境 revision 的 alias/动态候选快照； 不应把 PTY 或命令执行运行时移进 zeta-input-classifier 或 zeta-shell-completion。 naturallanguage.rs 的 fallback 会分别尝试排除和包含未完成的末 token，并按 1.0、0.8、 0.6 的长度阈值使用 English stems、developer terms 和 command-overlap 词典打分。这三份词典为 Zeta 独立构建，没有导入或变换 Warp 的词表。英文候选数据来自 MIT 许可的 TextBlob bundled spelling-frequency data，并经过 frequency threshold 和 Snowball stemming；developer terms 和 command-overlap 是为 Zeta 独立整理的集合。TextBlob 许可文本位于 dictionaries/THIRDPARTYNOTICES.txt。词典作为本 crate 的数据内嵌进二进制，不引入单独运行时。 模型资产与失败语义 当前发布来自 chogng/zeta-classifier 的 models/berttinyv3： - berttinyv3candle.onnx：Candle 0.9 专用 FP32、opset 16； - berttinytokenizer.json：训练分区适配 tokenizer； - metadata.json：标签、温度、算子与摘要； - 输出 index 0 为 Shell、index 1 为 Agent；softmax 前温度为 1.6894922825552194。 模型和 tokenizer 通过 includebytes! 固定进二进制，不在运行时下载。EmbeddedClassifier 是 模型、tokenizer 和 CPU device 的唯一 owner。模型或 tokenizer 初始化失败会直接启用完整 HeuristicFallback；初始化成功后的编码、推理或输出校验错误返回 CurrentRouteFallback，保留用户 正在使用的模式；Candle panic 会被隔离，并在当前进程永久改走 HeuristicFallback。官方 candle-onnx 的构建脚本需要系统 protoc，产品运行时不需要。 更新模型时必须一起更新 ONNX、tokenizer、metadata.json、摘要常量、标签解释、温度和概率基线 测试，不能只替换其中一个文件。 内部 owner 与修改影响 - classifier::classifywithmodel 固定决策顺序与失败分支；改动会影响所有自动模式消费者。 - history::InputHistory 拥有 0.9 相似度门槛和“最新匹配胜出”语义；宿主只提供有序事实。 - shell::ShellContext 只适配 zeta-shell-completion::ShellCompletionEngine 与 classifier 阈值；parser、 command registry 和 completion 不得移回本 crate 或 Zeterm。 - rules 只放低风险、确定性的上下文和 allowlist 短路；模糊规则不得绕过模型。 - model::EmbeddedClassifier 绑定模型图、tokenizer、标签和温度；任一资产变更都需要概率测试。 - naturallanguage::classifywithfallbackheuristic 只在模型不可用或 panic 后接管；它使用 Zeta 自有词典，不应成为正常主路径。naturallanguage 保持为本 crate 的私有模块：产品消费者必须 调用完整 InputClassifier，不得直接依赖词典分数绕过历史、Shell evidence 和模型。 当前没有第二个需要“原始自然语言分数”而非 Shell/Agent 路由的后端无关消费者，因此不拆独立 natural-language-detection crate。只有出现这样的真实消费者，并且能定义不依赖 InputRoute、 ShellTokenSnapshot 和 classifier fallback 阈值的纯打分契约时，才应把词典和 scorer 一起抽出； 路由顺序与失败语义仍由 zeta-input-classifier 拥有。 验证 `bash cargo test -p zeta-input-classifier cargo clippy -p zeta-input-classifier --all-targets -- -D warnings ` 测试覆盖 parser、历史冲突、严格 token 阈值、工作区命令、自然语言词典、follow-up、普通模型错误、模型 panic、 模型路由样例、资产 SHA 和 Candle FP32 概率基线。当前模型仍可能误判自由措辞，例如 chmod 755 是什么意思；用户显式选择始终覆盖自动分类。 InputClassifier::classify 是同步调用；startbackgroundwarmup 只提前解码模型和 tokenizer。 当前 Zeterm 在编辑变更时直接调用分类，并从同一个 InputClassifier 请求 Shell completion；候选由 AgentComposer` 收敛为输入光标后的 ghost text，并通过 editor 的精确 text edit 应用；Slash/模型 选择 Pane 不承载 Shell 候选。将模型推理移出 UI 线程、废弃 过期结果和添加 debounce 属于 Zeterm adapter 的产品接线工作，不改变本 crate 的决策契约。"
  },
  {
    "slug": "crates/install-context",
    "sourcePath": "zeta-rs/install-context/README.md",
    "title": "zeta-install-context",
    "description": "zeta-install-context 在进程启动边界捕获 current executable、package layout、 ZETARGPATH、ZETABWRAPPATH、两个 Windows sandbox helper override 与 host PATH， 并为消费方提供稳定、有序的资源候选。它不验证或执行 binary，不拥有 Workspace、T…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Zeta 进程安装布局与随包资源位置的实现契约。Tool execution 与参数约束由\n> [`zeta-shell-command`](../shell-command/README.md) 维护；平台 sandbox 选择和 enforcement\n> 由 [`docs/sandboxing.md`](../../docs/sandboxing.md) 维护。\n\n`zeta-install-context` 在进程启动边界捕获 current executable、package layout、\n`ZETA_RG_PATH`、`ZETA_BWRAP_PATH`、两个 Windows sandbox helper override 与 host `PATH`，\n并为消费方提供稳定、有序的资源候选。它不验证或执行 binary，不拥有 Workspace、Tool policy、\napproval、sandbox capability probe、下载、更新或安装 mutation。\n\n## 布局与优先级\n\n当前识别的 package layout：\n\n```text\n<package>/\n├── zeta-package.json\n├── bin/\n│   └── zeta\n├── zeta-path/\n│   └── rg[.exe]\n└── zeta-resources/\n    ├── bwrap              # Linux\n    ├── zeta-command-runner.exe              # Windows\n    ├── zeta-windows-sandbox-setup.exe        # Windows\n    ├── node/bin/node[.exe] # packaged-node variant only\n    ├── skills/            # built-in Agent Skills\n    └── product-services/  # product Marketplace config + pinned public TUF root\n```\n\n只有 executable 位于 `bin/`，且 package root 同时存在普通文件 `zeta-package.json`、\n`zeta-path/` 与 `zeta-resources/` 时，才识别为 `InstallMethod::Package`；其他启动方式统一为\n`Other`。metadata 的生成和 package 内容校验由\n[`scripts/zeta_package`](../../scripts/zeta_package/README.md) 拥有，本 crate 只以其存在作为\nlayout marker，不解析或信任其中的字段。\n\n`InstallContext::executable_candidates(ManagedExecutable::Ripgrep)` 返回互斥分支：\n\n- 配置 `ZETA_RG_PATH` 时只返回 authoritative `ExplicitOverride`；\n- 否则返回 `SearchPaths`，顺序为 `<package>/zeta-path/rg`、Zeta executable 同目录的 legacy\n  candidate、启动时 host `PATH` candidates。\n\n类型不会在 override 分支暴露 fallback paths，因此 override 无效时消费方必须直接失败。普通候选\n可以逐个验证；第一个有效 candidate 必须 canonicalize 并冻结后才能进入 Tool binding。\n\n`ManagedExecutable::Bubblewrap` 使用相同的 mutually-exclusive contract：配置\n`ZETA_BWRAP_PATH` 时只返回 override；否则顺序为 package `zeta-resources/bwrap`、启动时 host\n`PATH` candidates。它不会采用 executable sibling legacy path。\n\n`ManagedExecutable::WindowsCommandRunner` 与 `WindowsSandboxSetup` 分别使用\n`ZETA_WINDOWS_COMMAND_RUNNER_PATH` 和 `ZETA_WINDOWS_SANDBOX_SETUP_PATH`。无 override 时顺序\n为 package `zeta-resources/`、启动时 host `PATH`；两个 helper 分开解析，但 Windows backend\n只有在两者都通过精确 probe 后才可用。\n\n## 公共契约\n\n| Symbol | 职责 | 不承担 |\n| --- | --- | --- |\n| `InstallContext::current` | 捕获当前安装与环境 snapshot | 持续观察环境变化 |\n| `PackageLayout` | 描述 metadata 与 package/bin/path/resources 路径 | 创建、解析或修改 package |\n| `executable_candidates` | 生成有来源和优先级的候选 | executable 验证或 capability probe |\n| `host_path_candidates` | 为 consumer-owned basename 查询冻结的 host PATH | 决定领域 identity、trust 或执行 |\n| `bundled_resource` | 返回现有普通 resource file | digest 验证或 materialization |\n| `bundled_resource_directory` | 返回现有 resource directory | tree validation、Skill discovery |\n\n调用关系：\n\n```text\nhost composition\n└─ InstallContext::current\n   ├─ executable_candidates(Ripgrep)\n   │  └─ RipgrepExecutable validation + canonical identity freeze\n   ├─ executable_candidates(Bubblewrap)\n   │  └─ LinuxSandbox validation + capability probe + canonical identity freeze\n   ├─ executable_candidates(WindowsCommandRunner / WindowsSandboxSetup)\n   │  └─ WindowsSandbox validation + protocol probe + canonical identity freeze\n   ├─ bundled_resource_directory(\"skills\")\n      └─ zeta-skills controlled BuiltIn source validation\n   ├─ Desktop host declaration or bundled_resource(\"node/bin/node[.exe]\")\n      └─ zeta-language-server-catalog::ManagedNodeRuntime validation and selection\n   └─ bundled_resource(\"product-services/product-services.json\")\n      └─ zeta-cli → LocalProductServicesConfig trust validation\n```\n\n如果该 crate 开始启动进程、解释 sandbox policy、下载资源或管理更新状态，说明 ownership 已经\n漂移。具有平台生命周期的 helper 选择、复制和验证应留在对应 sandbox backend。\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-install-context\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-install-context --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/install-context:install-context-unit-tests\n```",
    "headings": [
      {
        "depth": 2,
        "id": "布局与优先级",
        "title": "布局与优先级"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 本 README 是 Zeta 进程安装布局与随包资源位置的实现契约。Tool execution 与参数约束由 zeta-shell-command 维护；平台 sandbox 选择和 enforcement 由 docs/sandboxing.md 维护。 zeta-install-context 在进程启动边界捕获 current executable、package layout、 ZETARGPATH、ZETABWRAPPATH、两个 Windows sandbox helper override 与 host PATH， 并为消费方提供稳定、有序的资源候选。它不验证或执行 binary，不拥有 Workspace、Tool policy、 approval、sandbox capability probe、下载、更新或安装 mutation。 布局与优先级 当前识别的 package layout： ``text <package / ├── zeta-package.json ├── bin/ │ └── zeta ├── zeta-path/ │ └── rg .exe └── zeta-resources/ ├── bwrap Linux ├── zeta-command-runner.exe Windows ├── zeta-windows-sandbox-setup.exe Windows ├── node/bin/node .exe packaged-node variant only ├── skills/ built-in Agent Skills └── product-services/ product Marketplace config + pinned public TUF root ` 只有 executable 位于 bin/，且 package root 同时存在普通文件 zeta-package.json、 zeta-path/ 与 zeta-resources/ 时，才识别为 InstallMethod::Package；其他启动方式统一为 Other。metadata 的生成和 package 内容校验由 scripts/zetapackage 拥有，本 crate 只以其存在作为 layout marker，不解析或信任其中的字段。 InstallContext::executablecandidates ManagedExecutable::Ripgrep 返回互斥分支： - 配置 ZETARGPATH 时只返回 authoritative ExplicitOverride； - 否则返回 SearchPaths，顺序为 <package /zeta-path/rg、Zeta executable 同目录的 legacy candidate、启动时 host PATH candidates。 类型不会在 override 分支暴露 fallback paths，因此 override 无效时消费方必须直接失败。普通候选 可以逐个验证；第一个有效 candidate 必须 canonicalize 并冻结后才能进入 Tool binding。 ManagedExecutable::Bubblewrap 使用相同的 mutually-exclusive contract：配置 ZETABWRAPPATH 时只返回 override；否则顺序为 package zeta-resources/bwrap、启动时 host PATH candidates。它不会采用 executable sibling legacy path。 ManagedExecutable::WindowsCommandRunner 与 WindowsSandboxSetup 分别使用 ZETAWINDOWSCOMMANDRUNNERPATH 和 ZETAWINDOWSSANDBOXSETUPPATH。无 override 时顺序 为 package zeta-resources/、启动时 host PATH；两个 helper 分开解析，但 Windows backend 只有在两者都通过精确 probe 后才可用。 公共契约 Symbol 职责 不承担 --- --- --- InstallContext::current 捕获当前安装与环境 snapshot 持续观察环境变化 PackageLayout 描述 metadata 与 package/bin/path/resources 路径 创建、解析或修改 package executablecandidates 生成有来源和优先级的候选 executable 验证或 capability probe hostpathcandidates 为 consumer-owned basename 查询冻结的 host PATH 决定领域 identity、trust 或执行 bundledresource 返回现有普通 resource file digest 验证或 materialization bundledresourcedirectory 返回现有 resource directory tree validation、Skill discovery 调用关系： `text host composition └─ InstallContext::current ├─ executablecandidates Ripgrep │ └─ RipgrepExecutable validation + canonical identity freeze ├─ executablecandidates Bubblewrap │ └─ LinuxSandbox validation + capability probe + canonical identity freeze ├─ executablecandidates WindowsCommandRunner / WindowsSandboxSetup │ └─ WindowsSandbox validation + protocol probe + canonical identity freeze ├─ bundledresourcedirectory \"skills\" └─ zeta-skills controlled BuiltIn source validation ├─ Desktop host declaration or bundledresource \"node/bin/node .exe \" └─ zeta-language-server-catalog::ManagedNodeRuntime validation and selection └─ bundledresource \"product-services/product-services.json\" └─ zeta-cli → LocalProductServicesConfig trust validation ` 如果该 crate 开始启动进程、解释 sandbox policy、下载资源或管理更新状态，说明 ownership 已经 漂移。具有平台生命周期的 helper 选择、复制和验证应留在对应 sandbox backend。 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-install-context cargo clippy --manifest-path Cargo.toml \\ -p zeta-install-context --all-targets --no-deps -- -D warnings bazel test //zeta-rs/install-context:install-context-unit-tests ``"
  },
  {
    "slug": "crates/instructions",
    "sourcePath": "zeta-rs/instructions/README.md",
    "title": "zeta-instructions",
    "description": "zeta-instructions 对一个 Workspace 的 .zeta/instructions/.md 执行有界、非递归发现，校验 YAML frontmatter、三态加载策略和 UTF-8 Markdown 正文，并发布不可变 catalog snapshot。它不解析 Codex/Claude 格式，不组装模型请求，也不拥有 watcher、Workspa…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 Zeta 原生 Instruction artifact 的当前实现契约。跨系统对象划分、`.zeta`\n> 命名空间与外部导入边界由\n> [`docs/agent-customizations.md`](../../docs/agent-customizations.md) 维护。\n\n`zeta-instructions` 对一个 Workspace 的 `.zeta/instructions/*.md` 执行有界、非递归发现，校验\nYAML frontmatter、三态加载策略和 UTF-8 Markdown 正文，并发布不可变 catalog snapshot。它不解析\nCodex/Claude 格式，不组装模型请求，也不拥有 watcher、Workspace trust 或 UI。\n\n## 快速理解\n\n| 文件状态 | 结果 |\n| --- | --- |\n| `.zeta/instructions` 不存在 | 空 catalog，无错误 |\n| 合法 `.md` | 进入确定性 catalog |\n| 单文件格式错误 | 产生隔离 diagnostic，其他文件继续 |\n| `load: global` | 可由 runtime 注入后续模型调用 |\n| `load: contextual` / `on-demand` | 保留为类型化 artifact，等待显式上下文选择 |\n\n## 边界与公共契约\n\n`InstructionCatalog::discover` 固定原生 Workspace 相对路径；`refresh` 只在 entries 或 diagnostics\n变化时推进 generation。`InstructionCatalogSnapshot::global_content` 只渲染 `Global` 条目，并带\nartifact name 与相对路径 provenance。\n\nfrontmatter 必须显式声明：\n\n```yaml\n---\nname: rust-style\nload: contextual\npatterns:\n  - \"**/*.rs\"\n---\n```\n\n`load` 只接受 `global`、`contextual`、`on-demand`。只有 contextual 可以且必须声明非空\n`patterns`。文件名使用小写字母、数字和连字符；可选 `name` 存在时必须与文件名一致。\n\n实现限制为 128 个直接条目、每个文件 32 KiB。source/entry symlink、目录、非 Markdown、非法\nUTF-8、空正文和未知 frontmatter 字段均不会进入 catalog。\n\n## 内部所有权与调用路径\n\n| 文件 / symbol | 职责 |\n| --- | --- |\n| `catalog.rs::scan` | 固定路径、entry limit、排序与隔离诊断 |\n| `catalog.rs::load_entry` | metadata、类型、大小、UTF-8、frontmatter 和正文校验 |\n| `catalog.rs::load_policy` | 三态加载策略的不变量 |\n| `model.rs::InstructionCatalogSnapshot` | immutable entries/diagnostics 与 Global 渲染 |\n\n```text\nInstructionCatalog::discover / refresh\n  → scan\n  → load_entry\n  → load_policy\n  → InstructionCatalogSnapshot\n```\n\n若本 crate 开始扫描 `AGENTS.md`、`.codex`、`.claude`，执行 glob matching，读取当前 editor 状态，\n或直接修改 `ModelRequest`，表示原生 authority 与 compatibility/runtime 边界已经漂移。\n\n## 验证与当前限制\n\n```bash\ncargo test -p zeta-instructions\ncargo clippy -p zeta-instructions --all-targets --no-deps -- -D warnings\n```\n\n当前已实现原生 Workspace 发现、格式校验、不可变 snapshot 与 Global 内容渲染；App Server 的\n`WorkspaceCustomizations` 在 Workspace 激活时发现 catalog，由 filesystem invalidation refresh，并在\n下一次 model invocation 通过 `HarnessInstructionsProvider` 提供 Global 内容。\n当前限制是 contextual pattern matching、显式 on-demand selection、user/built-in/Plugin source\ncomposition，以及 catalog/diagnostic list API 尚未实现。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "边界与公共契约",
        "title": "边界与公共契约"
      },
      {
        "depth": 2,
        "id": "内部所有权与调用路径",
        "title": "内部所有权与调用路径"
      },
      {
        "depth": 2,
        "id": "验证与当前限制",
        "title": "验证与当前限制"
      }
    ],
    "searchText": " 本 README 拥有 Zeta 原生 Instruction artifact 的当前实现契约。跨系统对象划分、.zeta 命名空间与外部导入边界由 docs/agent-customizations.md 维护。 zeta-instructions 对一个 Workspace 的 .zeta/instructions/.md 执行有界、非递归发现，校验 YAML frontmatter、三态加载策略和 UTF-8 Markdown 正文，并发布不可变 catalog snapshot。它不解析 Codex/Claude 格式，不组装模型请求，也不拥有 watcher、Workspace trust 或 UI。 快速理解 文件状态 结果 --- --- .zeta/instructions 不存在 空 catalog，无错误 合法 .md 进入确定性 catalog 单文件格式错误 产生隔离 diagnostic，其他文件继续 load: global 可由 runtime 注入后续模型调用 load: contextual / on-demand 保留为类型化 artifact，等待显式上下文选择 边界与公共契约 InstructionCatalog::discover 固定原生 Workspace 相对路径；refresh 只在 entries 或 diagnostics 变化时推进 generation。InstructionCatalogSnapshot::globalcontent 只渲染 Global 条目，并带 artifact name 与相对路径 provenance。 frontmatter 必须显式声明： ``yaml --- name: rust-style load: contextual patterns: - \"/.rs\" --- ` load 只接受 global、contextual、on-demand。只有 contextual 可以且必须声明非空 patterns。文件名使用小写字母、数字和连字符；可选 name 存在时必须与文件名一致。 实现限制为 128 个直接条目、每个文件 32 KiB。source/entry symlink、目录、非 Markdown、非法 UTF-8、空正文和未知 frontmatter 字段均不会进入 catalog。 内部所有权与调用路径 文件 / symbol 职责 --- --- catalog.rs::scan 固定路径、entry limit、排序与隔离诊断 catalog.rs::loadentry metadata、类型、大小、UTF-8、frontmatter 和正文校验 catalog.rs::loadpolicy 三态加载策略的不变量 model.rs::InstructionCatalogSnapshot immutable entries/diagnostics 与 Global 渲染 `text InstructionCatalog::discover / refresh → scan → loadentry → loadpolicy → InstructionCatalogSnapshot ` 若本 crate 开始扫描 AGENTS.md、.codex、.claude，执行 glob matching，读取当前 editor 状态， 或直接修改 ModelRequest，表示原生 authority 与 compatibility/runtime 边界已经漂移。 验证与当前限制 `bash cargo test -p zeta-instructions cargo clippy -p zeta-instructions --all-targets --no-deps -- -D warnings ` 当前已实现原生 Workspace 发现、格式校验、不可变 snapshot 与 Global 内容渲染；App Server 的 WorkspaceCustomizations 在 Workspace 激活时发现 catalog，由 filesystem invalidation refresh，并在 下一次 model invocation 通过 HarnessInstructionsProvider` 提供 Global 内容。 当前限制是 contextual pattern matching、显式 on-demand selection、user/built-in/Plugin source composition，以及 catalog/diagnostic list API 尚未实现。"
  },
  {
    "slug": "crates/zeterm/keybinding",
    "sourcePath": "zeterm/keybinding/README.md",
    "title": "zeta-keybinding",
    "description": "zeta-keybinding 把已标准化的平台按键与有序规则解析为命令、阻止或等待后续按键，并提供 设置浮层、最多四段 Chord 的录制 lifecycle 和等待提示。它不读取窗口事件、不执行产品命令， 也不持久化用户配置。",
    "group": "Crate 实现参考",
    "markdown": "> Native 产品快捷键的跨组件路由与用户语义由\n> [`docs/native-terminal-ui.md`](../docs/native-terminal-ui.md) 维护。本 README 只拥有\n> 快捷键子系统的按键模型、规则解析、录制状态和设置呈现契约。\n\n`zeta-keybinding` 把已标准化的平台按键与有序规则解析为命令、阻止或等待后续按键，并提供\n设置浮层、最多四段 Chord 的录制 lifecycle 和等待提示。它不读取窗口事件、不执行产品命令，\n也不持久化用户配置。\n\n## 所有权\n\n| 能力 | Owner | 状态 |\n| --- | --- | --- |\n| 逻辑键、物理键、实际 modifier 与 portable primary modifier | `key` | ✅ |\n| 一至四段 `KeySequence` 及前缀匹配 | `KeySequence` | ✅ |\n| portable 字符串 parser、持久化 serializer 与 keycap label formatter | `parser` | ✅ |\n| `when` 布尔/字符串表达式的解析与求值 | `ContextExpression` | ✅ |\n| Builtin/User 来源、显式 priority 与注册顺序 | `BindingSet` | ✅ |\n| context 过滤后的确定性冲突解析 | `KeybindingResolver` | ✅ |\n| 设置浮层、命令行、深灰 keycap 与 Chord 提示 | `settings` | ✅ |\n| 录制、取消、一秒 quiet period 与 commit | `recording` | ✅ |\n| keycap 基础几何与 scene primitive | `zeta-ui` | 委托 |\n| modal scope、稳定 identity 与 accessibility node | `zui` | 委托 |\n| winit/browser/terminal 事件转换 | 产品或平台 adapter | ❌ |\n| context key 的定义和状态 | 产品 host | ❌ |\n| command 注册、可用性与执行 | 产品 command owner | ❌ |\n| Resolver Chord timeout 与 IME lifecycle | 窗口 host | ❌ |\n| 用户配置读取、schema 和热更新 | 产品配置 authority | ❌ |\n\n依赖方向固定为：\n\n```text\nproduct host → zeta-keybinding → zeta-ui → zui\n                              → zui\n```\n\n本 crate 不得依赖 `zeta-winit`、terminal、workspace、session、editor 或产品配置 domain。\n如果设置或录制模块开始定义产品 command、读取配置路径或转换平台事件，说明 ownership 已经漂移。\n\n## 接口地图\n\n| Symbol | 职责 |\n| --- | --- |\n| `LogicalKey` / `PhysicalKey` | 保存标准化逻辑身份或 adapter 提供的稳定物理代码 |\n| `ShortcutModifiers` | 声明 portable primary 或明确 Control/Meta，并在解析时按 host 展开 |\n| `KeyStroke` | 保存 adapter 已标准化的一次实际按键 |\n| `KeySequence::new` | 拒绝空序列和超过四段的序列 |\n| `parse_key_sequence` | 把空格分隔的 Chord、portable modifier 和 `[PhysicalCode]` 编译为 `KeySequence` |\n| `serialize_key_sequence` | 把按键序列写回平台无关的用户配置语法 |\n| `keycap_labels` / `format_key_sequence` | 按 host 生成分组 keycap label 或单行文本 |\n| `ContextExpression::parse` / `evaluate` | 解析 `!`、`&&`、`||`、括号及 `==`/`!=`，由 host 回调提供 context value |\n| `BindingSet::register_command` | 添加一个带来源、条件与显式优先级的命令规则 |\n| `BindingSet::register_blocker` | 添加一个消费按键但不执行命令的覆盖规则 |\n| `KeybindingResolver::resolve` | 由 host 提供 context predicate，返回 `ResolveResult` |\n| `KeyboardShortcutsState<Command>` | 保存设置显隐、当前录制、quiet-period deadline 和保存状态，不执行或持久化命令 |\n| `KeyboardShortcutsState::record` / `advance` | 接收 adapter 已标准化的 `Chord`，到期后返回 `ShortcutCommit<Command>` |\n| `KeyboardShortcutRow<Command>` | 绑定宿主 command、label、稳定 `ElementId` 和当前快捷键 |\n| `KeyboardShortcutsIds` | 由宿主分配 parent/root/close identity，避免组件制造全局冲突 |\n| `KeyboardShortcuts` | 注册 modal interaction tree、声明 overlay zui Element 并绘制设置浮层，只消费 caller-owned rows 和 diagnostics |\n| `paint_chord_hint` | 把 resolver 的 pending sequence 与 entered count 绘制为底部提示 |\n| `ShortcutRecording` / `ShortcutStatus` | private；分别承载录制 deadline 和保存提示，不越过 commit 边界 |\n\n解析顺序固定为：先过滤 context 和按键前缀，再比较 User/Builtin 来源、显式 priority 和注册顺序。\n获胜规则仍有后续 chord 时返回 `PendingChord`；完整 blocker 返回 `Blocked`。\n\n```text\nplatform event\n  → product adapter → KeyStroke\n  → BindingSet + product context predicate\n  → KeybindingResolver::resolve\n  → NoMatch / PendingChord / Command / Blocked\n  → product host owns lifecycle and execution\n\nstandardized Chord → KeyboardShortcutsState::record\n                   → KeyboardShortcutsState::advance\n                   → ShortcutCommit\n                   → host validation / persistence\n```\n\n## 接入与失败语义\n\n- Adapter 必须提供稳定的物理 key code，不能把键盘布局标签当成物理身份。\n- `ContextExpression` 只拥有通用表达式语法；可用 context key、当前 value 和未知 key 的接受策略仍由产品 host 拥有。\n- `Command` 结果只表示输入匹配，产品执行前仍需检查业务不变量。\n- `PendingChord` 不启动 timer 或修改 IME；窗口 host 必须拥有这些副作用。\n- Escape 和窗口失焦如何映射到 `cancel_recording` 由宿主决定；录制模块只提供 transition。\n- 宿主必须为设置 root、close 和每个 command row 提供跨 frame 稳定且互不冲突的 `ElementId`。\n- `ShortcutCommit` 不表示保存成功；宿主完成校验和写入后必须调用 `saved` 或 `save_failed`。\n- 用户配置替换应先构造完整的新 `BindingSet`，验证成功后再原子替换。\n\n## 测试与当前限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-keybinding\ncargo clippy --manifest-path Cargo.toml -p zeta-keybinding --all-targets -- -D warnings\n```\n\n测试覆盖 portable modifier、逻辑/物理身份、字符串 parser/serializer、host keycap label、\ncontext 表达式、来源与顺序优先级、blocker、Chord prefix、录制/取消、modal interaction 和\n设置绘制。当前没有键盘布局本地化 label、动态 registration disposable、设置搜索、分类或\n恢复默认值；冲突诊断、用户配置 schema、产品 context value 和 command executor 继续由产品\nhost 拥有。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "接口地图",
        "title": "接口地图"
      },
      {
        "depth": 2,
        "id": "接入与失败语义",
        "title": "接入与失败语义"
      },
      {
        "depth": 2,
        "id": "测试与当前限制",
        "title": "测试与当前限制"
      }
    ],
    "searchText": " Native 产品快捷键的跨组件路由与用户语义由 docs/native-terminal-ui.md 维护。本 README 只拥有 快捷键子系统的按键模型、规则解析、录制状态和设置呈现契约。 zeta-keybinding 把已标准化的平台按键与有序规则解析为命令、阻止或等待后续按键，并提供 设置浮层、最多四段 Chord 的录制 lifecycle 和等待提示。它不读取窗口事件、不执行产品命令， 也不持久化用户配置。 所有权 能力 Owner 状态 --- --- --- 逻辑键、物理键、实际 modifier 与 portable primary modifier key ✅ 一至四段 KeySequence 及前缀匹配 KeySequence ✅ portable 字符串 parser、持久化 serializer 与 keycap label formatter parser ✅ when 布尔/字符串表达式的解析与求值 ContextExpression ✅ Builtin/User 来源、显式 priority 与注册顺序 BindingSet ✅ context 过滤后的确定性冲突解析 KeybindingResolver ✅ 设置浮层、命令行、深灰 keycap 与 Chord 提示 settings ✅ 录制、取消、一秒 quiet period 与 commit recording ✅ keycap 基础几何与 scene primitive zeta-ui 委托 modal scope、稳定 identity 与 accessibility node zui 委托 winit/browser/terminal 事件转换 产品或平台 adapter ❌ context key 的定义和状态 产品 host ❌ command 注册、可用性与执行 产品 command owner ❌ Resolver Chord timeout 与 IME lifecycle 窗口 host ❌ 用户配置读取、schema 和热更新 产品配置 authority ❌ 依赖方向固定为： ``text product host → zeta-keybinding → zeta-ui → zui → zui ` 本 crate 不得依赖 zeta-winit、terminal、workspace、session、editor 或产品配置 domain。 如果设置或录制模块开始定义产品 command、读取配置路径或转换平台事件，说明 ownership 已经漂移。 接口地图 Symbol 职责 --- --- LogicalKey / PhysicalKey 保存标准化逻辑身份或 adapter 提供的稳定物理代码 ShortcutModifiers 声明 portable primary 或明确 Control/Meta，并在解析时按 host 展开 KeyStroke 保存 adapter 已标准化的一次实际按键 KeySequence::new 拒绝空序列和超过四段的序列 parsekeysequence 把空格分隔的 Chord、portable modifier 和 PhysicalCode 编译为 KeySequence serializekeysequence 把按键序列写回平台无关的用户配置语法 keycaplabels / formatkeysequence 按 host 生成分组 keycap label 或单行文本 ContextExpression::parse / evaluate 解析 !、&&、 、括号及 ==/!=，由 host 回调提供 context value BindingSet::registercommand 添加一个带来源、条件与显式优先级的命令规则 BindingSet::registerblocker 添加一个消费按键但不执行命令的覆盖规则 KeybindingResolver::resolve 由 host 提供 context predicate，返回 ResolveResult KeyboardShortcutsState<Command 保存设置显隐、当前录制、quiet-period deadline 和保存状态，不执行或持久化命令 KeyboardShortcutsState::record / advance 接收 adapter 已标准化的 Chord，到期后返回 ShortcutCommit<Command KeyboardShortcutRow<Command 绑定宿主 command、label、稳定 ElementId 和当前快捷键 KeyboardShortcutsIds 由宿主分配 parent/root/close identity，避免组件制造全局冲突 KeyboardShortcuts 注册 modal interaction tree、声明 overlay zui Element 并绘制设置浮层，只消费 caller-owned rows 和 diagnostics paintchordhint 把 resolver 的 pending sequence 与 entered count 绘制为底部提示 ShortcutRecording / ShortcutStatus private；分别承载录制 deadline 和保存提示，不越过 commit 边界 解析顺序固定为：先过滤 context 和按键前缀，再比较 User/Builtin 来源、显式 priority 和注册顺序。 获胜规则仍有后续 chord 时返回 PendingChord；完整 blocker 返回 Blocked。 `text platform event → product adapter → KeyStroke → BindingSet + product context predicate → KeybindingResolver::resolve → NoMatch / PendingChord / Command / Blocked → product host owns lifecycle and execution standardized Chord → KeyboardShortcutsState::record → KeyboardShortcutsState::advance → ShortcutCommit → host validation / persistence ` 接入与失败语义 - Adapter 必须提供稳定的物理 key code，不能把键盘布局标签当成物理身份。 - ContextExpression 只拥有通用表达式语法；可用 context key、当前 value 和未知 key 的接受策略仍由产品 host 拥有。 - Command 结果只表示输入匹配，产品执行前仍需检查业务不变量。 - PendingChord 不启动 timer 或修改 IME；窗口 host 必须拥有这些副作用。 - Escape 和窗口失焦如何映射到 cancelrecording 由宿主决定；录制模块只提供 transition。 - 宿主必须为设置 root、close 和每个 command row 提供跨 frame 稳定且互不冲突的 ElementId。 - ShortcutCommit 不表示保存成功；宿主完成校验和写入后必须调用 saved 或 savefailed。 - 用户配置替换应先构造完整的新 BindingSet，验证成功后再原子替换。 测试与当前限制 `bash cargo test --manifest-path Cargo.toml -p zeta-keybinding cargo clippy --manifest-path Cargo.toml -p zeta-keybinding --all-targets -- -D warnings `` 测试覆盖 portable modifier、逻辑/物理身份、字符串 parser/serializer、host keycap label、 context 表达式、来源与顺序优先级、blocker、Chord prefix、录制/取消、modal interaction 和 设置绘制。当前没有键盘布局本地化 label、动态 registration disposable、设置搜索、分类或 恢复默认值；冲突诊断、用户配置 schema、产品 context value 和 command executor 继续由产品 host 拥有。"
  },
  {
    "slug": "crates/zeterm/native-keybindings",
    "sourcePath": "zeterm/native-keybindings/README.md",
    "title": "zeta-keybindings-host",
    "description": "zeta-keybindings-host owns the reusable native-host layer around the generic zeta-keybinding model. It standardizes platform key events, owns chord timeout state, and validates/polls the …",
    "group": "Crate 实现参考",
    "markdown": "`zeta-keybindings-host` owns the reusable native-host layer around the generic\n[`zeta-keybinding`](../keybinding/README.md) model. It standardizes platform key\nevents, owns chord timeout state, and validates/polls the JSON user binding\nresource without knowing a product's commands or window lifecycle.\n\nThe product host supplies a [`KeybindingCatalog`](src/catalog.rs) implementation\nfor command identity, builtin bindings, context conditions, and context lookup.\nThe host remains responsible for translating a resolved command into product\nside effects, focus changes, IME lifecycle, and redraws.\n\n## Ownership\n\n| Concern | Owner | Boundary |\n| --- | --- | --- |\n| Logical/physical event normalization | `input` | Consumes `zeta-winit` events and returns generic keybinding values. |\n| Pending chord, timeout, blocker, and command resolution | `Keybindings<C>` | Uses the host-supplied catalog; does not execute commands. |\n| User resource size/shape/platform validation | `KeybindingsResource<C>` | Reads and atomically updates one JSON file; preserves the last valid rule set on rejected updates. |\n| Product command identity and builtin rules | Product host catalog | Implements `KeybindingCatalog`; product commands do not enter this crate's transport boundary. |\n| Command execution, focus, IME, and event-loop deadlines | Product host | Consumes resolution and deadline accessors. |\n\n## Execution path\n\n```text\nzeta-winit event\n  → recording_chord / Keybindings::resolve\n  → KeybindingCatalog context predicate\n  → NoMatch / Pending / Command / Blocked\n  → product host command executor\n\nkeybindings.json\n  → KeybindingsResource::poll\n  → catalog command/condition validation\n  → complete UserBinding set\n  → Keybindings::replace_user_bindings\n```\n\n`KeybindingsResource::poll` reads at most 1 MiB and accepts at most 1,024\nentries. A malformed or oversized update returns `Rejected` and leaves the\nengine's previous complete rule set in place. Writing a recorded binding\nreplaces only the same command's existing user rule and uses the shared\natomic-write helper.\n\n## Verification\n\n```bash\ncargo test -p zeta-keybindings-host\ncargo test -p zeterm\n```\n\nThe crate must not depend on `NativeApp`, App Server clients, terminal state,\nworkspace state, or product UI components. If it needs one of those types,\nextend the host catalog contract or keep the adapter in `zeterm/src`.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "execution-path",
        "title": "Execution path"
      },
      {
        "depth": 2,
        "id": "verification",
        "title": "Verification"
      }
    ],
    "searchText": "zeta-keybindings-host owns the reusable native-host layer around the generic zeta-keybinding model. It standardizes platform key events, owns chord timeout state, and validates/polls the JSON user binding resource without knowing a product's commands or window lifecycle. The product host supplies a KeybindingCatalog implementation for command identity, builtin bindings, context conditions, and context lookup. The host remains responsible for translating a resolved command into product side effects, focus changes, IME lifecycle, and redraws. Ownership Concern Owner Boundary --- --- --- Logical/physical event normalization input Consumes zeta-winit events and returns generic keybinding values. Pending chord, timeout, blocker, and command resolution Keybindings<C Uses the host-supplied catalog; does not execute commands. User resource size/shape/platform validation KeybindingsResource<C Reads and atomically updates one JSON file; preserves the last valid rule set on rejected updates. Product command identity and builtin rules Product host catalog Implements KeybindingCatalog; product commands do not enter this crate's transport boundary. Command execution, focus, IME, and event-loop deadlines Product host Consumes resolution and deadline accessors. Execution path ``text zeta-winit event → recordingchord / Keybindings::resolve → KeybindingCatalog context predicate → NoMatch / Pending / Command / Blocked → product host command executor keybindings.json → KeybindingsResource::poll → catalog command/condition validation → complete UserBinding set → Keybindings::replaceuserbindings ` KeybindingsResource::poll reads at most 1 MiB and accepts at most 1,024 entries. A malformed or oversized update returns Rejected and leaves the engine's previous complete rule set in place. Writing a recorded binding replaces only the same command's existing user rule and uses the shared atomic-write helper. Verification `bash cargo test -p zeta-keybindings-host cargo test -p zeterm ` The crate must not depend on NativeApp, App Server clients, terminal state, workspace state, or product UI components. If it needs one of those types, extend the host catalog contract or keep the adapter in zeterm/src`."
  },
  {
    "slug": "crates/keyring-store",
    "sourcePath": "zeta-rs/keyring-store/README.md",
    "title": "zeta-keyring-store",
    "description": "zeta-keyring-store 把平台原生 credential facility 适配为 zetasecrets::SecretStore。它不建立第二套 secret trait，不解释 credential 内容，也不拥有 OAuth、refresh、revoke、Connector account 或 Plugin credential slot lifec…",
    "group": "Crate 实现参考",
    "markdown": "> 跨系统 secret ownership 与 backend policy 由\n> [`docs/secrets.md`](../../docs/secrets.md) 维护；provider-neutral value/store contract 由\n> [`zeta-secrets`](../secrets/README.md) 维护。\n\n`zeta-keyring-store` 把平台原生 credential facility 适配为 `zeta_secrets::SecretStore`。它不建立第二套\nsecret trait，不解释 credential 内容，也不拥有 OAuth、refresh、revoke、Connector account 或 Plugin\ncredential slot lifecycle。\n\n## 公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `KeyringSecretStore::for_profile` | canonical profile root → isolated native keyring namespace | profile authority、fallback 选择 |\n| `SecretStore` implementation | opaque binary `load/store/delete` | token parsing、account lookup |\n\nOS-visible service 固定为 `com.zeta.secret-store.v1`。Account 是 profile namespace 与 `SecretKey` 的\ndomain-separated SHA-256，不包含 profile path、email、Connector ID 或原始 key。Value 使用 keyring 的\nbinary secret API，不进行 UTF-8 转换；输入上限为 1 MiB。\n\n## 内部调用路径\n\n```text\nKeyringSecretStore::for_profile\n├─ canonicalize profile root\n├─ profile_namespace\n└─ SystemKeyringBackend\n\nSecretStore::{load,store,delete}\n├─ KeyringSecretStore::account\n├─ keyring::Entry::new\n└─ get_secret / set_secret / delete_credential\n   └─ classify_keyring_error → sanitized SecretStoreError\n```\n\n`KeyringBackend` 是 private test seam；consumer 不得依赖 keyring crate types。`NoEntry` 映射为\n`None`/`NotFound`，`NoStorageAccess` 映射为 `AccessDenied`，其他 backend detail 统一净化为\n`BackendFailure`。错误不得包含 service、account、raw platform response 或 secret bytes。\n\n## 平台与失败语义\n\n- macOS：Apple native Keychain；\n- Windows：Windows Credential Manager；\n- Linux：native async persistent Secret Service；\n- FreeBSD/OpenBSD：同步 Secret Service；\n- 其他 target：构造时返回 `BackendUnavailable`。\n\n构造成功只表示平台 adapter 已编译；实际 desktop session/keyring daemon 拒绝访问时，具体 operation\n仍会 fail closed。这里不实现自动文件 fallback，避免 backend 暂时不可用时读取另一份过期 credential。\n\n## 验证与扩展点\n\n```text\ncargo test -p zeta-keyring-store\ncargo clippy -p zeta-keyring-store --all-targets --no-deps -- -D warnings\n```\n\n测试使用 private fake backend，不访问开发者真实 keyring，覆盖 binary round trip、replace/delete、\nprofile isolation、metadata hashing、error sanitization 和 size limit。新增平台 adapter 时必须继续实现\n完整三操作，并同步本文与 [`docs/secrets.md`](../../docs/secrets.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部调用路径",
        "title": "内部调用路径"
      },
      {
        "depth": 2,
        "id": "平台与失败语义",
        "title": "平台与失败语义"
      },
      {
        "depth": 2,
        "id": "验证与扩展点",
        "title": "验证与扩展点"
      }
    ],
    "searchText": " 跨系统 secret ownership 与 backend policy 由 docs/secrets.md 维护；provider-neutral value/store contract 由 zeta-secrets 维护。 zeta-keyring-store 把平台原生 credential facility 适配为 zetasecrets::SecretStore。它不建立第二套 secret trait，不解释 credential 内容，也不拥有 OAuth、refresh、revoke、Connector account 或 Plugin credential slot lifecycle。 公共契约 Symbol 当前职责 不承担 --- --- --- KeyringSecretStore::forprofile canonical profile root → isolated native keyring namespace profile authority、fallback 选择 SecretStore implementation opaque binary load/store/delete token parsing、account lookup OS-visible service 固定为 com.zeta.secret-store.v1。Account 是 profile namespace 与 SecretKey 的 domain-separated SHA-256，不包含 profile path、email、Connector ID 或原始 key。Value 使用 keyring 的 binary secret API，不进行 UTF-8 转换；输入上限为 1 MiB。 内部调用路径 ``text KeyringSecretStore::forprofile ├─ canonicalize profile root ├─ profilenamespace └─ SystemKeyringBackend SecretStore:: load,store,delete ├─ KeyringSecretStore::account ├─ keyring::Entry::new └─ getsecret / setsecret / deletecredential └─ classifykeyringerror → sanitized SecretStoreError ` KeyringBackend 是 private test seam；consumer 不得依赖 keyring crate types。NoEntry 映射为 None/NotFound，NoStorageAccess 映射为 AccessDenied，其他 backend detail 统一净化为 BackendFailure。错误不得包含 service、account、raw platform response 或 secret bytes。 平台与失败语义 - macOS：Apple native Keychain； - Windows：Windows Credential Manager； - Linux：native async persistent Secret Service； - FreeBSD/OpenBSD：同步 Secret Service； - 其他 target：构造时返回 BackendUnavailable。 构造成功只表示平台 adapter 已编译；实际 desktop session/keyring daemon 拒绝访问时，具体 operation 仍会 fail closed。这里不实现自动文件 fallback，避免 backend 暂时不可用时读取另一份过期 credential。 验证与扩展点 `text cargo test -p zeta-keyring-store cargo clippy -p zeta-keyring-store --all-targets --no-deps -- -D warnings ` 测试使用 private fake backend，不访问开发者真实 keyring，覆盖 binary round trip、replace/delete、 profile isolation、metadata hashing、error sanitization 和 size limit。新增平台 adapter 时必须继续实现 完整三操作，并同步本文与 docs/secrets.md`。"
  },
  {
    "slug": "crates/language-marketplace",
    "sourcePath": "zeta-rs/language-marketplace/README.md",
    "title": "zeta-language-marketplace",
    "description": "本 crate 拥有 TUF metadata refresh、签名 catalog 解析、兼容性判定、精确 package 下载、有界 ZIP 解压和 Marketplace v1 package digest 验证。它不拥有用户界面、Node 选择、provider registry、LSP process 或编辑器协议。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是签名语言包远程消费实现的 canonical contract。跨进程的产品语义和用户确认流程见\n> [`docs/lsp.md`](../../docs/lsp.md)；安装存储和 provider adapter 分别由\n> [`zeta-language-server-distribution`](../language-server-distribution/README.md) 与\n> [`zeta-language-server-catalog`](../language-server-catalog/README.md) 拥有。\n\n本 crate 拥有 TUF metadata refresh、签名 catalog 解析、兼容性判定、精确 package 下载、有界 ZIP\n解压和 Marketplace v1 package digest 验证。它不拥有用户界面、Node 选择、provider registry、LSP\nprocess 或编辑器协议。\n\n## Crate 边界\n\n| 能力 | 本 crate | 直接协作者 |\n| --- | --- | --- |\n| TUF root pin、过期检查、delegated publisher role | ✅ | `tough`、host product config |\n| `id/version/digest/manifest` 的 signed binding | ✅ | Marketplace publisher |\n| 用户确认和展示 | ❌ | Desktop Workbench |\n| side-by-side install 和 durable activation | 委托 | `zeta-language-server-distribution` |\n| server ID → provider、共享 Node runtime | ❌ | `zeta-language-server-catalog` |\n| LSP 进程与请求 | ❌ | `zeta-language-service`、`zeta-lsp` |\n\n## 文件与职责\n\n| 文件 | 关键符号 | 职责 |\n| --- | --- | --- |\n| `remote.rs` | `RemoteLanguageMarketplace`、`snapshot_from_repository`、`collect_entries` | refresh/cache TUF repository，列出并 materialize exact entry |\n| `model.rs` | `catalog_entries`、`LanguageMarketplaceEntry` | 校验 schema 1/2 manifest、server route 和 consumer SemVer |\n| `archive.rs` | `extract`、`verify_package`、`language_server_package` | 有界解压、Marketplace tree digest、distribution handoff |\n| `transport.rs` | `MarketplaceTransport` | 只允许 HTTPS，通过共享 `HttpClient` 读取 metadata/target |\n| `error.rs` | `LanguageMarketplaceErrorKind` | 对 App Server 暴露不含远程 body/本地路径的稳定错误分类 |\n\n## 调用路径\n\n```text\nRemoteLanguageMarketplace::sync\n  → RepositoryLoader + MarketplaceTransport\n  → snapshot_from_repository\n     → validate_language_index / read_revocations\n     → collect_entries → catalog_entries\n\nRemoteLanguageMarketplace::install\n  → materialize_exact（再次刷新并精确匹配 entry）\n  → read_target（TUF length/hash verification）\n  → archive::extract（路径、entry、单文件、总大小限制）\n  → archive::verify_package（marketplace-package-v1 digest）\n  → LanguageServerInstaller::install_verified\n  → LanguageServerActivationAuthority::activate\n```\n\n安装不能直接使用 UI 传来的 manifest 或 target path。`materialize_exact` 必须在当前未过期 TUF\nsnapshot 中重新找到 `marketplaceId/packageId/version/digest/serverId` 的同一条目；catalog revision\n冲突由 App Server 在下载前拒绝。\n\n## 验证与失败语义\n\n- metadata base、targets base 必须为无 userinfo/query/fragment 且以 `/` 结尾的 HTTPS URL；trusted\n  root 最大 1 MiB。\n- TUF 使用 `ExpirationEnforcement::Safe` 与 `Limits::default()`；网络 transport 失败时只可打开当前\n  未过期的完整缓存，签名、rollback 或 parse 失败不降级。\n- delegated role 必须是 `publishers/<publisher>`，target 必须是\n  `packages/<publisher>/<name>/<version>.zip`。\n- ZIP 最大 64 MiB、10,000 个 entry、单文件 16 MiB、展开总量 256 MiB；拒绝 symlink、encrypted\n  entry、duplicate path、反斜线和 parent traversal。\n- 解压后的 regular files 再按 Marketplace `marketplace-package-v1\\0` 算法计算 digest，并与 signed\n  `marketplacePackage.packageDigest` 以及 signed file count/size 同时比较。\n- schema 2 executable 必须明确 `node` 或 `direct` runtime；schema 1 的 `LegacyUnspecified` 只有被\n  product adapter 识别的 server ID 才能通过最终 compatibility probe。\n\n`LanguageMarketplaceErrorKind` 区分无效配置、不可信 metadata、分发不可用、不安全 package、cache\n不可用、不兼容和 activation 失败。错误消息不携带下载响应正文、URL credential 或 host path。\n\n## 集成义务\n\nHost 必须提供 product-pinned root、consumer ID/version、profile-scoped cache 和共享有界\n`HttpClient`。App Server 必须先向 UI 返回 `LanguageMarketplaceCompatibility`，仅在用户确认后提交 exact\ninstall command；即使 UI 出错，install 仍会在 Rust 端重新执行兼容性与 exact-snapshot 校验。\n\n新增 runtime 或 server family 时，应先扩展 Marketplace schema 与 catalog provider adapter，不得在\n本 crate 启动进程或读取 ambient `PATH`。出现 Node discovery、LSP JSON-RPC 或 Renderer dialog 代码即\n表示 ownership 漂移。\n\n## 测试\n\n```bash\ncargo test -p zeta-language-marketplace\n```\n\n`model_tests.rs` 覆盖 schema 1 CSS route 和 consumer SemVer；`archive_tests.rs` 覆盖有界解压、signed\nstatistics 与 traversal；`remote_distribution_tests.rs` 构造真实 signed TUF repository，验证 catalog\n不会预下载 package、exact install、activation 和 tamper rejection。跨 crate activation/reopen 测试位于\n`zeta-language-server-distribution/src/activation_tests.rs`。\n\n## 当前限制与扩展点\n\n当前 product adapter 只支持 `css-language-server`，并把 schema 1 的该入口视为 Node script。通用\nconsumer 已能解析 schema 2 `node/direct` runtime，但 direct native provider 必须先在 catalog crate\n实现，才能被最终 compatibility probe 接受。已安装但未激活的 orphan version 不进入 UI；它不会被\n执行，后续可增加独立 cache/install garbage collection authority。",
    "headings": [
      {
        "depth": 2,
        "id": "crate-边界",
        "title": "Crate 边界"
      },
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "调用路径",
        "title": "调用路径"
      },
      {
        "depth": 2,
        "id": "验证与失败语义",
        "title": "验证与失败语义"
      },
      {
        "depth": 2,
        "id": "集成义务",
        "title": "集成义务"
      },
      {
        "depth": 2,
        "id": "测试",
        "title": "测试"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是签名语言包远程消费实现的 canonical contract。跨进程的产品语义和用户确认流程见 docs/lsp.md；安装存储和 provider adapter 分别由 zeta-language-server-distribution 与 zeta-language-server-catalog 拥有。 本 crate 拥有 TUF metadata refresh、签名 catalog 解析、兼容性判定、精确 package 下载、有界 ZIP 解压和 Marketplace v1 package digest 验证。它不拥有用户界面、Node 选择、provider registry、LSP process 或编辑器协议。 Crate 边界 能力 本 crate 直接协作者 --- --- --- TUF root pin、过期检查、delegated publisher role ✅ tough、host product config id/version/digest/manifest 的 signed binding ✅ Marketplace publisher 用户确认和展示 ❌ Desktop Workbench side-by-side install 和 durable activation 委托 zeta-language-server-distribution server ID → provider、共享 Node runtime ❌ zeta-language-server-catalog LSP 进程与请求 ❌ zeta-language-service、zeta-lsp 文件与职责 文件 关键符号 职责 --- --- --- remote.rs RemoteLanguageMarketplace、snapshotfromrepository、collectentries refresh/cache TUF repository，列出并 materialize exact entry model.rs catalogentries、LanguageMarketplaceEntry 校验 schema 1/2 manifest、server route 和 consumer SemVer archive.rs extract、verifypackage、languageserverpackage 有界解压、Marketplace tree digest、distribution handoff transport.rs MarketplaceTransport 只允许 HTTPS，通过共享 HttpClient 读取 metadata/target error.rs LanguageMarketplaceErrorKind 对 App Server 暴露不含远程 body/本地路径的稳定错误分类 调用路径 ``text RemoteLanguageMarketplace::sync → RepositoryLoader + MarketplaceTransport → snapshotfromrepository → validatelanguageindex / readrevocations → collectentries → catalogentries RemoteLanguageMarketplace::install → materializeexact（再次刷新并精确匹配 entry） → readtarget（TUF length/hash verification） → archive::extract（路径、entry、单文件、总大小限制） → archive::verifypackage（marketplace-package-v1 digest） → LanguageServerInstaller::installverified → LanguageServerActivationAuthority::activate ` 安装不能直接使用 UI 传来的 manifest 或 target path。materializeexact 必须在当前未过期 TUF snapshot 中重新找到 marketplaceId/packageId/version/digest/serverId 的同一条目；catalog revision 冲突由 App Server 在下载前拒绝。 验证与失败语义 - metadata base、targets base 必须为无 userinfo/query/fragment 且以 / 结尾的 HTTPS URL；trusted root 最大 1 MiB。 - TUF 使用 ExpirationEnforcement::Safe 与 Limits::default ；网络 transport 失败时只可打开当前 未过期的完整缓存，签名、rollback 或 parse 失败不降级。 - delegated role 必须是 publishers/<publisher ，target 必须是 packages/<publisher /<name /<version .zip。 - ZIP 最大 64 MiB、10,000 个 entry、单文件 16 MiB、展开总量 256 MiB；拒绝 symlink、encrypted entry、duplicate path、反斜线和 parent traversal。 - 解压后的 regular files 再按 Marketplace marketplace-package-v1\\0 算法计算 digest，并与 signed marketplacePackage.packageDigest 以及 signed file count/size 同时比较。 - schema 2 executable 必须明确 node 或 direct runtime；schema 1 的 LegacyUnspecified 只有被 product adapter 识别的 server ID 才能通过最终 compatibility probe。 LanguageMarketplaceErrorKind 区分无效配置、不可信 metadata、分发不可用、不安全 package、cache 不可用、不兼容和 activation 失败。错误消息不携带下载响应正文、URL credential 或 host path。 集成义务 Host 必须提供 product-pinned root、consumer ID/version、profile-scoped cache 和共享有界 HttpClient。App Server 必须先向 UI 返回 LanguageMarketplaceCompatibility，仅在用户确认后提交 exact install command；即使 UI 出错，install 仍会在 Rust 端重新执行兼容性与 exact-snapshot 校验。 新增 runtime 或 server family 时，应先扩展 Marketplace schema 与 catalog provider adapter，不得在 本 crate 启动进程或读取 ambient PATH。出现 Node discovery、LSP JSON-RPC 或 Renderer dialog 代码即 表示 ownership 漂移。 测试 `bash cargo test -p zeta-language-marketplace ` modeltests.rs 覆盖 schema 1 CSS route 和 consumer SemVer；archivetests.rs 覆盖有界解压、signed statistics 与 traversal；remotedistributiontests.rs 构造真实 signed TUF repository，验证 catalog 不会预下载 package、exact install、activation 和 tamper rejection。跨 crate activation/reopen 测试位于 zeta-language-server-distribution/src/activationtests.rs。 当前限制与扩展点 当前 product adapter 只支持 css-language-server，并把 schema 1 的该入口视为 Node script。通用 consumer 已能解析 schema 2 node/direct` runtime，但 direct native provider 必须先在 catalog crate 实现，才能被最终 compatibility probe 接受。已安装但未激活的 orphan version 不进入 UI；它不会被 执行，后续可增加独立 cache/install garbage collection authority。"
  },
  {
    "slug": "crates/language-server-catalog",
    "sourcePath": "zeta-rs/language-server-catalog/README.md",
    "title": "zeta-language-server-catalog",
    "description": "zeta-language-server-catalog 拥有内置 server identity、用户启用意图、execution policy gate、 冻结候选的校验与 canonicalization，以及已验证 package 到 resolved definition 的 provider 绑定。它不启动进程、不读取 editor 文档、不执行 LSP、不下…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是语言服务器发现与 resolved definition 的 crate-level canonical contract。运行时\n> lifecycle 见 [`zeta-language-service`](../language-service/README.md)，协议 contract 见\n> [`zeta-lsp`](../lsp/README.md)，跨 crate 语义见 [`docs/lsp.md`](../../docs/lsp.md)。\n> TUF catalog 与 package materialization 见\n> [`zeta-language-marketplace`](../language-marketplace/README.md)。\n\n`zeta-language-server-catalog` 拥有内置 server identity、用户启用意图、execution policy gate、\n冻结候选的校验与 canonicalization，以及已验证 package 到 resolved definition 的 provider\n绑定。它不启动进程、不读取 editor 文档、不执行 LSP、不下载 server，也不决定\nworkspace trust。\n\n## 所有权与公共接口\n\n| API / type | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `LanguageServerCatalog` | 保存内置 server 与 preference，按 workspace 生成一次冻结 resolution | 持有 live client 或自动重启 |\n| `LanguageServerPreference` / `LanguageServerMode` | 表达 Disabled、Automatic、Enabled 和 authoritative executable override | 用布尔值混合启用与发现语义 |\n| `LanguageServerExecutionPolicy` | 接收产品宿主已经作出的 process allow/disallow 决策 | 自行读取或持久化 workspace trust |\n| `LanguageServerExecutableCandidates` | 注入有优先级的冻结候选；`InstallContext` 是当前实现 | 搜索时启动或 probe 进程 |\n| `LanguageServerCatalogResolution` | 同时返回 resolved definitions 与每个内置 server 的 availability | 表示 server 已经 initialize |\n| `LanguageServerDefinition` | 冻结唯一 route、canonical executable command 和 initialize options | 在 runtime 内重新查询 PATH |\n| `LanguageServerProvider` / `LanguageServerProviderRegistry` | 把已验证、已安装的 server 包和运行时绑定为稳定 language route 与 definition | 下载、验签、启动进程或监督重启 |\n| `LanguageServerProviderRegistry::from_activation` | 从 durable activation snapshot 重建已知 provider；拒绝当前 build 不支持的 server ID | 把未知 package 猜成 executable route |\n| `ManagedNodeRuntime` | 冻结 canonical Node-compatible executable；Desktop 使用 Electron run-as-Node，其他 package 使用 standalone Node，并生成 clean-environment command | 回退 host `PATH` 或允许 language pack 携带 Node |\n| `CssLanguageServerProvider` | 用共享 Node-compatible runtime 运行 verified CSS package 入口，route `css`/`less`/`scss` | 复制 LSP client/supervisor 或解释 Marketplace metadata |\n\n当前内置项包括 `rust-analyzer → rust`、`vscode-json-language-server --stdio → json/jsonc` 和\n`bash-language-server start → shellscript`。CSS 是独立 provider，不进入 PATH built-in 列表。Native/App Server 从\nConfig snapshot 映射各自的持久化 preference；\nPATH built-in 未配置时使用 `Disabled`，需要显式选择 `Automatic` 或 `Enabled` 才会生成 definition；\nactivation-backed provider 的用户安装确认已经构成启用意图，未配置时生成 packaged definition，显式\n`Disabled` 才关闭。允许执行且冻结\nPATH 中存在可执行文件时生成 definition；否则保持无 server。显式 override 是 authoritative，失效时不会回退 PATH。\n\n## 执行路径、失败和扩展\n\n```text\nNative composition\n├─ InstallContext::current → frozen host PATH\n├─ workspace process policy\n└─ LanguageServerCatalog::resolve\n   ├─ Disabled / disallowed → status only\n   ├─ explicit path → validate exactly that path\n   └─ automatic → candidates in source order\n      → canonicalize → regular file → executable permission\n      → LanguageServerDefinition\n      → zeta-language-service\n\nverified CSS package composition\n├─ InstalledLanguageServer::executable\n├─ Desktop: ZETA_ELECTRON_RUN_AS_NODE_PATH → exact Electron executable\n├─ other packaged hosts: InstallContext::bundled_resource(\"node/bin/node[.exe]\")\n└─ CssLanguageServerProvider::definition\n   ├─ packaged → selected runtime + package entrypoint + --stdio + clean environment\n   │  └─ Electron source only: ELECTRON_RUN_AS_NODE=1\n   └─ explicit override → authoritative native executable\n      → LanguageServerDefinition\n      → zeta-language-service\n```\n\n关键私有符号：\n\n- `BuiltinServer` 绑定每个内置 identity、executable、language route 和启动参数；`resolve_builtin`\n  统一执行 preference、policy 和 executable gate。\n- `valid_executable` 是候选进入 resolved definition 前的唯一文件校验 gate。\n- `has_executable_permission` 保持 Unix executable-bit 与其他平台文件语义分离。\n- `canonical_regular_file` / `canonical_executable` 是 provider 输入进入 definition 前的\n  canonical file gate；`ManagedNodeRuntime::command_for_script` 是 Node 启动环境的唯一 owner。\n- `CssLanguageServerProvider::definition` 是 CSS package/native override 分支的唯一绑定点；\n  `LanguageServerProviderRegistry` 拒绝重复 identity，避免静默替换 route owner。\n\n如果本 crate 开始持有 `LanguageServerClient`、child process、document revision 或 diagnostics，表示 runtime ownership\n向 catalog 漂移；如果 language-service 开始读取 PATH、选择 executable 或解释用户 preference，表示\n发现策略向 runtime 漂移。增加内置 server 时必须同时定义稳定 name、language IDs、启动参数、候选\n来源和不可用状态测试，不能把任意 workspace command 直接视为受信 definition。\n\n失败语义：静态 definition/name 无效会返回 `LanguageServerCatalogError`；candidate 缺失、不是普通\n文件、无法 canonicalize 或没有 Unix executable bit 都投影为 `ExecutableUnavailable`，不造成整个\n产品启动失败。`ExecutionDisallowed` 在访问候选前返回。\n\n## 测试与当前限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-language-server-catalog\ncargo clippy --manifest-path Cargo.toml -p zeta-language-server-catalog --all-targets -- -D warnings\n```\n\n测试覆盖三项 built-in 的 Automatic PATH resolution/route/launch arguments、execution policy gate、\n失效 explicit override 不回退，以及 CSS package 的 standalone/Electron runtime command、clean\nenvironment、native override 和 duplicate provider gate。\n\n当前限制：\n\n- ✅ Rust server 的 frozen PATH 发现、canonical executable 和 product-neutral availability；\n- ✅ `zeta-config` 持久化 mode/path、App Server typed mutation 与 Native 三项 server Settings selector；\n- ✅ JSON/JSONC、Shell server definitions 与持久化 mode/path 映射；\n- ✅ 独立 `zeta-language-server-distribution` 提供 checksum 验证、原子 staging、side-by-side 安装和回滚基础；\n- ✅ verified CSS package provider、managed Node-compatible runtime 启动命令、App Server provider 组合点与 native override；\n- ✅ Marketplace CSS target 的通用 TUF 下载/解压 adapter、compatibility probe、用户确认/安装 UI\n  和从 activation receipt 自动构建 provider registry；\n- 尚未完成：组织策略或更细的 per-server executable grant。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与公共接口",
        "title": "所有权与公共接口"
      },
      {
        "depth": 2,
        "id": "执行路径失败和扩展",
        "title": "执行路径、失败和扩展"
      },
      {
        "depth": 2,
        "id": "测试与当前限制",
        "title": "测试与当前限制"
      }
    ],
    "searchText": " 本 README 是语言服务器发现与 resolved definition 的 crate-level canonical contract。运行时 lifecycle 见 zeta-language-service，协议 contract 见 zeta-lsp，跨 crate 语义见 docs/lsp.md。 TUF catalog 与 package materialization 见 zeta-language-marketplace。 zeta-language-server-catalog 拥有内置 server identity、用户启用意图、execution policy gate、 冻结候选的校验与 canonicalization，以及已验证 package 到 resolved definition 的 provider 绑定。它不启动进程、不读取 editor 文档、不执行 LSP、不下载 server，也不决定 workspace trust。 所有权与公共接口 API / type 当前职责 明确不做 --- --- --- LanguageServerCatalog 保存内置 server 与 preference，按 workspace 生成一次冻结 resolution 持有 live client 或自动重启 LanguageServerPreference / LanguageServerMode 表达 Disabled、Automatic、Enabled 和 authoritative executable override 用布尔值混合启用与发现语义 LanguageServerExecutionPolicy 接收产品宿主已经作出的 process allow/disallow 决策 自行读取或持久化 workspace trust LanguageServerExecutableCandidates 注入有优先级的冻结候选；InstallContext 是当前实现 搜索时启动或 probe 进程 LanguageServerCatalogResolution 同时返回 resolved definitions 与每个内置 server 的 availability 表示 server 已经 initialize LanguageServerDefinition 冻结唯一 route、canonical executable command 和 initialize options 在 runtime 内重新查询 PATH LanguageServerProvider / LanguageServerProviderRegistry 把已验证、已安装的 server 包和运行时绑定为稳定 language route 与 definition 下载、验签、启动进程或监督重启 LanguageServerProviderRegistry::fromactivation 从 durable activation snapshot 重建已知 provider；拒绝当前 build 不支持的 server ID 把未知 package 猜成 executable route ManagedNodeRuntime 冻结 canonical Node-compatible executable；Desktop 使用 Electron run-as-Node，其他 package 使用 standalone Node，并生成 clean-environment command 回退 host PATH 或允许 language pack 携带 Node CssLanguageServerProvider 用共享 Node-compatible runtime 运行 verified CSS package 入口，route css/less/scss 复制 LSP client/supervisor 或解释 Marketplace metadata 当前内置项包括 rust-analyzer → rust、vscode-json-language-server --stdio → json/jsonc 和 bash-language-server start → shellscript。CSS 是独立 provider，不进入 PATH built-in 列表。Native/App Server 从 Config snapshot 映射各自的持久化 preference； PATH built-in 未配置时使用 Disabled，需要显式选择 Automatic 或 Enabled 才会生成 definition； activation-backed provider 的用户安装确认已经构成启用意图，未配置时生成 packaged definition，显式 Disabled 才关闭。允许执行且冻结 PATH 中存在可执行文件时生成 definition；否则保持无 server。显式 override 是 authoritative，失效时不会回退 PATH。 执行路径、失败和扩展 ``text Native composition ├─ InstallContext::current → frozen host PATH ├─ workspace process policy └─ LanguageServerCatalog::resolve ├─ Disabled / disallowed → status only ├─ explicit path → validate exactly that path └─ automatic → candidates in source order → canonicalize → regular file → executable permission → LanguageServerDefinition → zeta-language-service verified CSS package composition ├─ InstalledLanguageServer::executable ├─ Desktop: ZETAELECTRONRUNASNODEPATH → exact Electron executable ├─ other packaged hosts: InstallContext::bundledresource \"node/bin/node .exe \" └─ CssLanguageServerProvider::definition ├─ packaged → selected runtime + package entrypoint + --stdio + clean environment │ └─ Electron source only: ELECTRONRUNASNODE=1 └─ explicit override → authoritative native executable → LanguageServerDefinition → zeta-language-service ` 关键私有符号： - BuiltinServer 绑定每个内置 identity、executable、language route 和启动参数；resolvebuiltin 统一执行 preference、policy 和 executable gate。 - validexecutable 是候选进入 resolved definition 前的唯一文件校验 gate。 - hasexecutablepermission 保持 Unix executable-bit 与其他平台文件语义分离。 - canonicalregularfile / canonicalexecutable 是 provider 输入进入 definition 前的 canonical file gate；ManagedNodeRuntime::commandforscript 是 Node 启动环境的唯一 owner。 - CssLanguageServerProvider::definition 是 CSS package/native override 分支的唯一绑定点； LanguageServerProviderRegistry 拒绝重复 identity，避免静默替换 route owner。 如果本 crate 开始持有 LanguageServerClient、child process、document revision 或 diagnostics，表示 runtime ownership 向 catalog 漂移；如果 language-service 开始读取 PATH、选择 executable 或解释用户 preference，表示 发现策略向 runtime 漂移。增加内置 server 时必须同时定义稳定 name、language IDs、启动参数、候选 来源和不可用状态测试，不能把任意 workspace command 直接视为受信 definition。 失败语义：静态 definition/name 无效会返回 LanguageServerCatalogError；candidate 缺失、不是普通 文件、无法 canonicalize 或没有 Unix executable bit 都投影为 ExecutableUnavailable，不造成整个 产品启动失败。ExecutionDisallowed 在访问候选前返回。 测试与当前限制 `bash cargo test --manifest-path Cargo.toml -p zeta-language-server-catalog cargo clippy --manifest-path Cargo.toml -p zeta-language-server-catalog --all-targets -- -D warnings ` 测试覆盖三项 built-in 的 Automatic PATH resolution/route/launch arguments、execution policy gate、 失效 explicit override 不回退，以及 CSS package 的 standalone/Electron runtime command、clean environment、native override 和 duplicate provider gate。 当前限制： - ✅ Rust server 的 frozen PATH 发现、canonical executable 和 product-neutral availability； - ✅ zeta-config 持久化 mode/path、App Server typed mutation 与 Native 三项 server Settings selector； - ✅ JSON/JSONC、Shell server definitions 与持久化 mode/path 映射； - ✅ 独立 zeta-language-server-distribution` 提供 checksum 验证、原子 staging、side-by-side 安装和回滚基础； - ✅ verified CSS package provider、managed Node-compatible runtime 启动命令、App Server provider 组合点与 native override； - ✅ Marketplace CSS target 的通用 TUF 下载/解压 adapter、compatibility probe、用户确认/安装 UI 和从 activation receipt 自动构建 provider registry； - 尚未完成：组织策略或更细的 per-server executable grant。"
  },
  {
    "slug": "crates/language-server-distribution",
    "sourcePath": "zeta-rs/language-server-distribution/README.md",
    "title": "zeta-language-server-distribution",
    "description": "本 crate 拥有已下载 package 的路径校验、provider-supplied SHA-256 验证、private staging、原子发布、 version receipt、side-by-side update storage 与 durable activation authority。它不查询网络、不选择 release channel、不解压不可信…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 application-managed language-server 安装存储的 canonical contract。Server identity\n> 与 executable discovery 见 [`language-server-catalog`](../language-server-catalog/README.md)，跨 crate\n> 产品语义见 [`docs/lsp.md`](../../docs/lsp.md)，TUF consumer 见\n> [`zeta-language-marketplace`](../language-marketplace/README.md)。\n\n本 crate 拥有已下载 package 的路径校验、provider-supplied SHA-256 验证、private staging、原子发布、\nversion receipt、side-by-side update storage 与 durable activation authority。它不查询网络、不选择\nrelease channel、不解压不可信 archive、不调用全局包管理器，也不删除旧版本。\n\n## 公共接口与执行路径\n\n| API | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `LanguageServerPackageFile` | 表达 traversal-free regular/executable package file | 跟随 symlink 或接受绝对路径 |\n| `LanguageServerPackage` | 冻结 server/version/executable path/file set 并计算 deterministic SHA-256 | 声称 digest 来源可信 |\n| `LanguageServerInstaller` | 验证 expected digest、写 staging、receipt 并原子发布 version directory | 覆盖已发布版本或改变 Config |\n| `LanguageServerActivationAuthority` | 原子选择 exact installed version；每次 snapshot 重新扫描并校验 package digest | 下载 package、选择 runtime 或启动 server |\n| `InstalledLanguageServer` | 不透明的已验证安装 receipt，以只读访问器交给 provider versioned entrypoint 与 digest | 允许调用方手工伪造或表示 server 已激活/initialize |\n\n`InstalledLanguageServer::executable` 是 package manifest 声明的入口，不承诺它是可由 OS\n直接启动的 native binary。例如 CSS provider 把该路径解释为 JavaScript 入口，并且只使用\nZeta 托管的 Node 运行它；distribution 仍然只负责 bytes、digest 和安装路径的可验证性。\n\n```text\ntrusted server-specific provider\n  → immutable LanguageServerPackage + expected SHA-256\n  → validate identity / relative paths / duplicate paths / declared executable\n  → verify deterministic package digest\n  → <install-root>/.staging/<unique>/ files + installation.json\n  → atomic rename to <install-root>/<server>/<version>\n  → LanguageServerActivationAuthority atomically selects exact receipt\n  → restart reopens and rehashes every active installation\n```\n\n关键 private symbols：\n\n- `validate_identity` / `validate_relative_path` 在 server/version 与 package traversal 进入 filesystem 前\n  拒绝空值、`.`、`..`、绝对路径和 parent component；\n- `ensure_directory` 拒绝把 install root、server root 或 staging root 的 symlink 当作真实目录；\n- `StagingGuard` 在写入、权限或 receipt 失败时清理未发布 staging；\n- `InstallationReceipt` 绑定 server、version、executable relative path 和 digest；\n- `installed_from_receipt` 重新核对 receipt、每个文件内容和 executable mode，使完全相同的重复安装\n  幂等，并对 receipt 伪装或安装目录篡改 fail closed；\n- `set_executable` 只在 Unix 投影 package executable bit，不承担平台签名验证。\n\n如果本 crate 开始选择 URL、release channel 或 workspace enablement，表示 distribution provider/config\nownership 漂移；如果 catalog 开始写安装目录或校验 package bytes，表示 discovery 与 storage 被重新耦合。\n\n## 失败语义、测试和限制\n\nDigest mismatch 在创建 staging 前失败。写入后的任何失败由 guard 清理；已发布目录从不覆盖。更新通过\n安装新 version directory 并原子切换 activation receipt 完成，旧版本保留；回退通过\n`LanguageServerActivationAuthority::activate` 重新选择已经验证的 exact version 完成。\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-language-server-distribution\ncargo clippy --manifest-path Cargo.toml -p zeta-language-server-distribution --all-targets -- -D warnings\n```\n\n测试覆盖 traversal rejection、digest mismatch 不发布、version side-by-side、旧版本保留、同 package\n重复安装幂等、activation 跨进程重开，以及既有版本被篡改后即使 receipt 未变也拒绝复用或启动。\n\n当前限制：\n\n- ✅ verified package storage、atomic staging、receipt 与 side-by-side update/rollback 基础；\n- ✅ durable exact-version activation、启动时 re-open/re-hash 与篡改 fail closed；\n- 委托：TUF release metadata、有界 archive、compatibility probe 和安装确认由\n  `zeta-language-marketplace`、App Server 与 Desktop 分别拥有；\n- 尚未完成：下载进度/取消、未激活安装与旧版本 GC；\n- 扩展约束：本 crate 继续只消费已解析 package，不拥有网络、UI、provider 或 workspace enablement。",
    "headings": [
      {
        "depth": 2,
        "id": "公共接口与执行路径",
        "title": "公共接口与执行路径"
      },
      {
        "depth": 2,
        "id": "失败语义测试和限制",
        "title": "失败语义、测试和限制"
      }
    ],
    "searchText": " 本 README 是 application-managed language-server 安装存储的 canonical contract。Server identity 与 executable discovery 见 language-server-catalog，跨 crate 产品语义见 docs/lsp.md，TUF consumer 见 zeta-language-marketplace。 本 crate 拥有已下载 package 的路径校验、provider-supplied SHA-256 验证、private staging、原子发布、 version receipt、side-by-side update storage 与 durable activation authority。它不查询网络、不选择 release channel、不解压不可信 archive、不调用全局包管理器，也不删除旧版本。 公共接口与执行路径 API 当前职责 明确不做 --- --- --- LanguageServerPackageFile 表达 traversal-free regular/executable package file 跟随 symlink 或接受绝对路径 LanguageServerPackage 冻结 server/version/executable path/file set 并计算 deterministic SHA-256 声称 digest 来源可信 LanguageServerInstaller 验证 expected digest、写 staging、receipt 并原子发布 version directory 覆盖已发布版本或改变 Config LanguageServerActivationAuthority 原子选择 exact installed version；每次 snapshot 重新扫描并校验 package digest 下载 package、选择 runtime 或启动 server InstalledLanguageServer 不透明的已验证安装 receipt，以只读访问器交给 provider versioned entrypoint 与 digest 允许调用方手工伪造或表示 server 已激活/initialize InstalledLanguageServer::executable 是 package manifest 声明的入口，不承诺它是可由 OS 直接启动的 native binary。例如 CSS provider 把该路径解释为 JavaScript 入口，并且只使用 Zeta 托管的 Node 运行它；distribution 仍然只负责 bytes、digest 和安装路径的可验证性。 ``text trusted server-specific provider → immutable LanguageServerPackage + expected SHA-256 → validate identity / relative paths / duplicate paths / declared executable → verify deterministic package digest → <install-root /.staging/<unique / files + installation.json → atomic rename to <install-root /<server /<version → LanguageServerActivationAuthority atomically selects exact receipt → restart reopens and rehashes every active installation ` 关键 private symbols： - validateidentity / validaterelativepath 在 server/version 与 package traversal 进入 filesystem 前 拒绝空值、.、..、绝对路径和 parent component； - ensuredirectory 拒绝把 install root、server root 或 staging root 的 symlink 当作真实目录； - StagingGuard 在写入、权限或 receipt 失败时清理未发布 staging； - InstallationReceipt 绑定 server、version、executable relative path 和 digest； - installedfromreceipt 重新核对 receipt、每个文件内容和 executable mode，使完全相同的重复安装 幂等，并对 receipt 伪装或安装目录篡改 fail closed； - setexecutable 只在 Unix 投影 package executable bit，不承担平台签名验证。 如果本 crate 开始选择 URL、release channel 或 workspace enablement，表示 distribution provider/config ownership 漂移；如果 catalog 开始写安装目录或校验 package bytes，表示 discovery 与 storage 被重新耦合。 失败语义、测试和限制 Digest mismatch 在创建 staging 前失败。写入后的任何失败由 guard 清理；已发布目录从不覆盖。更新通过 安装新 version directory 并原子切换 activation receipt 完成，旧版本保留；回退通过 LanguageServerActivationAuthority::activate 重新选择已经验证的 exact version 完成。 `bash cargo test --manifest-path Cargo.toml -p zeta-language-server-distribution cargo clippy --manifest-path Cargo.toml -p zeta-language-server-distribution --all-targets -- -D warnings ` 测试覆盖 traversal rejection、digest mismatch 不发布、version side-by-side、旧版本保留、同 package 重复安装幂等、activation 跨进程重开，以及既有版本被篡改后即使 receipt 未变也拒绝复用或启动。 当前限制： - ✅ verified package storage、atomic staging、receipt 与 side-by-side update/rollback 基础； - ✅ durable exact-version activation、启动时 re-open/re-hash 与篡改 fail closed； - 委托：TUF release metadata、有界 archive、compatibility probe 和安装确认由 zeta-language-marketplace`、App Server 与 Desktop 分别拥有； - 尚未完成：下载进度/取消、未激活安装与旧版本 GC； - 扩展约束：本 crate 继续只消费已解析 package，不拥有网络、UI、provider 或 workspace enablement。"
  },
  {
    "slug": "crates/language-service",
    "sourcePath": "zeta-rs/language-service/README.md",
    "title": "zeta-language-service",
    "description": "zeta-language-service 位于产品宿主与 zeta-lsp 之间。它拥有语言服务启停、调用方已经解析并 信任的 server definitions、文档快照路由、editor revision freshness、LSP position 到 UTF-8 byte range 的转换，以及 supervisor thread 生命周期。它不读取文件、不…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是产品级语言服务协调层的 crate-level canonical contract。底层协议运行时见\n> [`zeta-lsp`](../lsp/README.md)，跨 crate 的能力、产品语义和演进阶段见\n> [`docs/lsp.md`](../../docs/lsp.md)。\n\n`zeta-language-service` 位于产品宿主与 `zeta-lsp` 之间。它拥有语言服务启停、调用方已经解析并\n信任的 server definitions、文档快照路由、editor revision freshness、LSP position 到 UTF-8 byte range\n的转换，以及 supervisor thread 生命周期。它不读取文件、不拥有 mutable editor text、不发现或\n安装 executable，也不绘制诊断、补全或 hover UI。\n\n## 所有权与公共接口\n\n| API / type | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `LanguageService` | 提供非阻塞文档 API并拥有 supervisor thread、Tokio runtime、router 与 clients | 保存编辑器文本或直接修改 UI |\n| `LanguageServiceConfiguration` | 固定 workspace root、启用状态和 resolved server definitions | 读取设置、PATH 或安装目录 |\n| `LanguageServerRestartPolicy` | 定义 Never 或有限指数退避、重启预算和 healthy-window reset | 发现 executable 或绘制错误 UI |\n| `LanguageServerState` | 向产品发布 Starting、Ready、BackingOff、CrashLoop、Failed、Stopped | 保存设置或作为协议状态 |\n| `zeta-language-server-catalog::LanguageServerDefinition` | catalog 委托的唯一 route、canonical command 与 initialize options | 在 runtime 内重新查询 PATH |\n| `LanguageServiceDocument` | 传递绝对路径、language ID、精确 editor revision 和 authoritative full text | 充当磁盘 revision 或文件缓存 |\n| `LanguageDiagnostics` | 绑定路径、精确 editor revision 和 product-neutral UTF-8 ranges | 保存 LSP URI、version 或 paint style |\n| `LanguageRequestId` / `LanguageDocumentPosition` | 非阻塞请求 identity 与 editor-owned UTF-8 source position | 暴露 LSP position encoding |\n| `LanguageHover` / `LanguageCompletions` / `LanguageDefinitions` | capability-gated、revision-fresh 的产品结果 | 绘制 popup、读取定义文件或持有 editor text |\n| `LanguageServiceEventSink` | 快速接收状态、诊断、消息和异步文档错误 | 在 callback 中阻塞或反向调用服务 |\n| `LanguageServiceMetricsSink` | 接收不含源码、路径、query 或 position 的请求结果指标 | 做 telemetry transport、保存用户内容或控制请求 |\n\n`LanguageServiceEnablement::Disabled` 会保留最新文档快照但不启动 server。调用方必须显式提供\n`Enabled` 和非歧义 catalog，服务才会把 resolved command 委托给 `zeta-lsp` 启动。\n\n## 内部执行路径\n\n```text\nNative / another product host\n  → LanguageService::synchronize_document(full snapshot)\n  → SupervisorCommand::Synchronize\n  → Supervisor\n      ├─ validate monotonic LanguageDocumentRevision\n      ├─ retain authoritative snapshot + file URI\n      └─ LanguageServerDocumentRouter::synchronize\n          → LanguageServerClient → LSP stdio process\n\nLanguageServerEvent::PublishDiagnostics\n  → ProtocolEventBridge\n  → reject stale supervisor generation / LSP document version\n  → projection::project_diagnostic(position encoding → UTF-8 byte range)\n  → LanguageServiceEvent::Diagnostics(editor revision)\n  → product event loop\n\nLanguageServerEvent::TransportClosed\n  → reject stale generation / server epoch\n  → clear diagnostics + mark matching documents unrouted\n  → retire disconnected router route\n  → bounded exponential backoff or CrashLoop\n  → launch fresh client + replay authoritative retained snapshots\n\nrequest_hover / request_completions / request_definition\n  → validate current routed revision + advertised capability\n  → convert editor UTF-8 position with negotiated encoding\n  → async typed LSP request\n  → reject stale generation / server epoch / editor revision\n  → project product-neutral result event\n  → emit cold/warm latency + result count + terminal outcome\n```\n\n关键私有符号：\n\n- `Supervisor` 拥有所有可变 server、route、document 和 URI binding；UI thread 不接触这些状态。\n- `SupervisorCommand` 是调用线程进入 supervisor 的唯一 mutation boundary。\n- `ProtocolEventBridge` 给每个 server event 标记 server identity、service generation 和 server epoch，\n  用于拒绝已停用、已替换或已重启实例的迟到事件。\n- `ServerRestartTracker` 只计算连续失败、healthy-window reset 和 bounded backoff；`Supervisor` 拥有\n  timer、route retirement、fresh launch 与 crash-loop 状态转换。\n- `validate_catalog` 在创建 thread 前拒绝重复 server name 和重复 language route。\n- `router_snapshot` 把产品文档转换为 `zeta-lsp` snapshot；协议类型不会泄漏到 Native adapter。\n- `projection::project_diagnostic` 根据 initialize 协商的位置编码生成 editor byte range。\n- `request_runtime` 拥有 capability gate、异步 request task 和三重 freshness gate；`requests` 只定义\n  product-neutral input/result 与 UTF-8/UTF-16 projection。\n- `metrics` 定义 content-free `LanguageRequestMetric`；`Supervisor` 按 `(server, epoch, request kind)`\n  区分 cold/warm，并拥有 in-flight task cancellation。\n\n如果宿主开始直接持有 `LanguageServerClient`，或者本 crate 开始读取 editor/file system，表示协调\n边界已经被绕过。若 `zeta-lsp` 开始决定用户是否启用某个 server 或从哪里安装它，则协议层发生了\nownership 漂移。\n\n## 校验、失败与宿主义务\n\n- workspace root 必须能转换为绝对 file URI；文档必须有非空绝对路径和 language ID。\n- 同一路径只接受递增 editor revision；相同 revision 是无操作，旧 revision 产生异步文档错误。\n- 一个 language ID 只能路由到一个 server。catalog 歧义在 thread 启动前同步返回错误。\n- 启用后异步启动 resolved command；单个 server 启动或 transport 失败按配置发布 `Failed`、\n  `BackingOff` 或 `CrashLoop`，不会使 supervisor panic。\n- 默认策略最多重启五次，延迟从 250 ms 指数增长并封顶 4 s；连续 Ready 60 s 后下一次失败从新预算开始。\n- 禁用、重配或 shutdown 会递增 generation、失效 server epoch、取消 launch，并让旧 timer 和旧实例\n  的迟到事件无法触发重启或发布结果；所有 in-flight requests 同时被取消。\n- `cancel_request` 会中止对应 task，并记录 `Cancelled`；freshness gate 丢弃的完成记录为\n  `StaleDiscarded`，capability/route 拒绝记录为 `Rejected`。\n- 意外断连会先清空该 server 文档的 diagnostics、退休 route 和旧 bindings；新实例 Ready 后只从\n  本 crate 保留的 authoritative product snapshots 重放。\n- diagnostics 只有在 LSP version 仍绑定到当前 editor revision 时才会发布；非法 range 被丢弃。\n- `shutdown` 是有界的显式关闭路径；直接 drop 只排队 best-effort shutdown，不能用于等待完成。\n\n产品宿主必须保存 authoritative editor document，并在每次文本 mutation 后发送 full snapshot；保存和\n关闭通过独立方法通知。宿主还必须提供经过配置、信任和 executable resolution 的 definitions，并把\n事件快速投递到自己的 event loop。诊断是否画下划线、hover/completion 的触发时机和 UI 状态不属于\n本 crate。\n\n## 测试、修改影响与当前限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-language-service\ncargo clippy --manifest-path Cargo.toml -p zeta-language-service --all-targets -- -D warnings\n```\n\n测试覆盖禁用模式的文档生命周期、启动失败隔离、catalog 歧义校验、精确重启预算、backoff 期间\n禁用、真实 stdio server 初始化后崩溃、UTF-8/UTF-16/emoji/CRLF position conversion，以及拒绝、\n取消和 content-free metrics。\n修改 enable/disable、generation 或 epoch 规则时必须补充迟到 timer/event 测试；修改文档 binding\n时必须同时检查 editor revision 与 LSP version；修改 projection 时必须覆盖 Unicode scalar 边界和\nserver 选定的位置编码。\n\n当前实现：\n\n- ✅ 产品级 supervisor、resolved definition 消费、显式启停、文档路由和规范 shutdown；\n- ✅ diagnostics freshness 与 product-neutral byte-range projection；\n- ✅ Native 已通过独立 adapter 接入 editor open/change/save/close 和 workspace replacement；\n- ✅ Native 通过独立 catalog 自动解析 PATH 中的 Rust、JSON/JSONC 与 Shell server，不可用时保持 Disabled；\n- ✅ Native 消费 App Server Config snapshot，三个内置 server 均支持独立持久化 mode/path、Settings UI、\n  热重配和打开文档重放；\n- ✅ Native host 已把 neutral diagnostics 接入 CodeEditor decoration 与 hover detail；本 crate 仍不拥有 UI；\n- ✅ 意外 transport close、断连 route retirement、有限指数退避、healthy-window reset、crash-loop gate\n  和 authoritative document replay；\n- ✅ Native Settings 显示 Starting、Ready、BackingOff、CrashLoop、Failed 和 Stopped，运行态不进入配置 authority；\n- ✅ hover/completion/resolve/command/navigation/hierarchy/rename/code-action/formatting/signature-help/inlay-hints/linked-editing 非阻塞 facade、capability gate、request identity 和 stale-result rejection；\n- ✅ Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors 与 Folding 的 product-neutral projection 和 revision freshness；\n- ✅ 静态/动态 capability gate、dynamic registration 与 work-done progress 事件；\n- ✅ document pull diagnostics 的 full/unchanged report projection；full report 复用现有 diagnostics 事件和 freshness 规则；\n- ✅ workspace diagnostics 的 capability gate 与跨文件 raw-coordinate projection；文件读取和 UTF-16 转换仍由 App Server Workspace authority 完成；\n- ✅ message severity、show/log 区分与 work-done progress 的 product-neutral 事件；\n- ✅ request kind/server incarnation/configuration + service generation/cold-warm/latency/result-count 与\n  delivered/empty/failed/cancelled/stale-discarded/rejected 指标；不记录路径、文本、query 或 position；\n- ✅ 显式 `cancel_request`、disable/reconfigure/shutdown 的 in-flight task cancellation；\n- ✅ Native pointer hover、latest-request gate、可滚动 completion window/安全 textEdit 接受，以及 F12\n  definition navigation；\n- ✅ Desktop 已把 completion resolve/commands、additional edits、semantic tokens 和文档特性接入现有 Editor contract；多 definition target 复用 Peek 列表；\n- 尚未完成：diagnostic result-id/partial-result/refresh；\n- 尚未完成：server-specific distribution provider 与产品级 message/install UI；\n- 尚未安装：references/navigation session cache；先由现有指标证明重复成本，再设计 incarnation/revision\n  完整绑定的 key 与失效规则；\n- Potential：远程 workspace authority 出现后再评估是否把 execution 放到 App Server。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与公共接口",
        "title": "所有权与公共接口"
      },
      {
        "depth": 2,
        "id": "内部执行路径",
        "title": "内部执行路径"
      },
      {
        "depth": 2,
        "id": "校验失败与宿主义务",
        "title": "校验、失败与宿主义务"
      },
      {
        "depth": 2,
        "id": "测试修改影响与当前限制",
        "title": "测试、修改影响与当前限制"
      }
    ],
    "searchText": " 本 README 是产品级语言服务协调层的 crate-level canonical contract。底层协议运行时见 zeta-lsp，跨 crate 的能力、产品语义和演进阶段见 docs/lsp.md。 zeta-language-service 位于产品宿主与 zeta-lsp 之间。它拥有语言服务启停、调用方已经解析并 信任的 server definitions、文档快照路由、editor revision freshness、LSP position 到 UTF-8 byte range 的转换，以及 supervisor thread 生命周期。它不读取文件、不拥有 mutable editor text、不发现或 安装 executable，也不绘制诊断、补全或 hover UI。 所有权与公共接口 API / type 当前职责 明确不做 --- --- --- LanguageService 提供非阻塞文档 API并拥有 supervisor thread、Tokio runtime、router 与 clients 保存编辑器文本或直接修改 UI LanguageServiceConfiguration 固定 workspace root、启用状态和 resolved server definitions 读取设置、PATH 或安装目录 LanguageServerRestartPolicy 定义 Never 或有限指数退避、重启预算和 healthy-window reset 发现 executable 或绘制错误 UI LanguageServerState 向产品发布 Starting、Ready、BackingOff、CrashLoop、Failed、Stopped 保存设置或作为协议状态 zeta-language-server-catalog::LanguageServerDefinition catalog 委托的唯一 route、canonical command 与 initialize options 在 runtime 内重新查询 PATH LanguageServiceDocument 传递绝对路径、language ID、精确 editor revision 和 authoritative full text 充当磁盘 revision 或文件缓存 LanguageDiagnostics 绑定路径、精确 editor revision 和 product-neutral UTF-8 ranges 保存 LSP URI、version 或 paint style LanguageRequestId / LanguageDocumentPosition 非阻塞请求 identity 与 editor-owned UTF-8 source position 暴露 LSP position encoding LanguageHover / LanguageCompletions / LanguageDefinitions capability-gated、revision-fresh 的产品结果 绘制 popup、读取定义文件或持有 editor text LanguageServiceEventSink 快速接收状态、诊断、消息和异步文档错误 在 callback 中阻塞或反向调用服务 LanguageServiceMetricsSink 接收不含源码、路径、query 或 position 的请求结果指标 做 telemetry transport、保存用户内容或控制请求 LanguageServiceEnablement::Disabled 会保留最新文档快照但不启动 server。调用方必须显式提供 Enabled 和非歧义 catalog，服务才会把 resolved command 委托给 zeta-lsp 启动。 内部执行路径 ``text Native / another product host → LanguageService::synchronizedocument full snapshot → SupervisorCommand::Synchronize → Supervisor ├─ validate monotonic LanguageDocumentRevision ├─ retain authoritative snapshot + file URI └─ LanguageServerDocumentRouter::synchronize → LanguageServerClient → LSP stdio process LanguageServerEvent::PublishDiagnostics → ProtocolEventBridge → reject stale supervisor generation / LSP document version → projection::projectdiagnostic position encoding → UTF-8 byte range → LanguageServiceEvent::Diagnostics editor revision → product event loop LanguageServerEvent::TransportClosed → reject stale generation / server epoch → clear diagnostics + mark matching documents unrouted → retire disconnected router route → bounded exponential backoff or CrashLoop → launch fresh client + replay authoritative retained snapshots requesthover / requestcompletions / requestdefinition → validate current routed revision + advertised capability → convert editor UTF-8 position with negotiated encoding → async typed LSP request → reject stale generation / server epoch / editor revision → project product-neutral result event → emit cold/warm latency + result count + terminal outcome ` 关键私有符号： - Supervisor 拥有所有可变 server、route、document 和 URI binding；UI thread 不接触这些状态。 - SupervisorCommand 是调用线程进入 supervisor 的唯一 mutation boundary。 - ProtocolEventBridge 给每个 server event 标记 server identity、service generation 和 server epoch， 用于拒绝已停用、已替换或已重启实例的迟到事件。 - ServerRestartTracker 只计算连续失败、healthy-window reset 和 bounded backoff；Supervisor 拥有 timer、route retirement、fresh launch 与 crash-loop 状态转换。 - validatecatalog 在创建 thread 前拒绝重复 server name 和重复 language route。 - routersnapshot 把产品文档转换为 zeta-lsp snapshot；协议类型不会泄漏到 Native adapter。 - projection::projectdiagnostic 根据 initialize 协商的位置编码生成 editor byte range。 - requestruntime 拥有 capability gate、异步 request task 和三重 freshness gate；requests 只定义 product-neutral input/result 与 UTF-8/UTF-16 projection。 - metrics 定义 content-free LanguageRequestMetric；Supervisor 按 server, epoch, request kind 区分 cold/warm，并拥有 in-flight task cancellation。 如果宿主开始直接持有 LanguageServerClient，或者本 crate 开始读取 editor/file system，表示协调 边界已经被绕过。若 zeta-lsp 开始决定用户是否启用某个 server 或从哪里安装它，则协议层发生了 ownership 漂移。 校验、失败与宿主义务 - workspace root 必须能转换为绝对 file URI；文档必须有非空绝对路径和 language ID。 - 同一路径只接受递增 editor revision；相同 revision 是无操作，旧 revision 产生异步文档错误。 - 一个 language ID 只能路由到一个 server。catalog 歧义在 thread 启动前同步返回错误。 - 启用后异步启动 resolved command；单个 server 启动或 transport 失败按配置发布 Failed、 BackingOff 或 CrashLoop，不会使 supervisor panic。 - 默认策略最多重启五次，延迟从 250 ms 指数增长并封顶 4 s；连续 Ready 60 s 后下一次失败从新预算开始。 - 禁用、重配或 shutdown 会递增 generation、失效 server epoch、取消 launch，并让旧 timer 和旧实例 的迟到事件无法触发重启或发布结果；所有 in-flight requests 同时被取消。 - cancelrequest 会中止对应 task，并记录 Cancelled；freshness gate 丢弃的完成记录为 StaleDiscarded，capability/route 拒绝记录为 Rejected。 - 意外断连会先清空该 server 文档的 diagnostics、退休 route 和旧 bindings；新实例 Ready 后只从 本 crate 保留的 authoritative product snapshots 重放。 - diagnostics 只有在 LSP version 仍绑定到当前 editor revision 时才会发布；非法 range 被丢弃。 - shutdown 是有界的显式关闭路径；直接 drop 只排队 best-effort shutdown，不能用于等待完成。 产品宿主必须保存 authoritative editor document，并在每次文本 mutation 后发送 full snapshot；保存和 关闭通过独立方法通知。宿主还必须提供经过配置、信任和 executable resolution 的 definitions，并把 事件快速投递到自己的 event loop。诊断是否画下划线、hover/completion 的触发时机和 UI 状态不属于 本 crate。 测试、修改影响与当前限制 `bash cargo test --manifest-path Cargo.toml -p zeta-language-service cargo clippy --manifest-path Cargo.toml -p zeta-language-service --all-targets -- -D warnings ` 测试覆盖禁用模式的文档生命周期、启动失败隔离、catalog 歧义校验、精确重启预算、backoff 期间 禁用、真实 stdio server 初始化后崩溃、UTF-8/UTF-16/emoji/CRLF position conversion，以及拒绝、 取消和 content-free metrics。 修改 enable/disable、generation 或 epoch 规则时必须补充迟到 timer/event 测试；修改文档 binding 时必须同时检查 editor revision 与 LSP version；修改 projection 时必须覆盖 Unicode scalar 边界和 server 选定的位置编码。 当前实现： - ✅ 产品级 supervisor、resolved definition 消费、显式启停、文档路由和规范 shutdown； - ✅ diagnostics freshness 与 product-neutral byte-range projection； - ✅ Native 已通过独立 adapter 接入 editor open/change/save/close 和 workspace replacement； - ✅ Native 通过独立 catalog 自动解析 PATH 中的 Rust、JSON/JSONC 与 Shell server，不可用时保持 Disabled； - ✅ Native 消费 App Server Config snapshot，三个内置 server 均支持独立持久化 mode/path、Settings UI、 热重配和打开文档重放； - ✅ Native host 已把 neutral diagnostics 接入 CodeEditor decoration 与 hover detail；本 crate 仍不拥有 UI； - ✅ 意外 transport close、断连 route retirement、有限指数退避、healthy-window reset、crash-loop gate 和 authoritative document replay； - ✅ Native Settings 显示 Starting、Ready、BackingOff、CrashLoop、Failed 和 Stopped，运行态不进入配置 authority； - ✅ hover/completion/resolve/command/navigation/hierarchy/rename/code-action/formatting/signature-help/inlay-hints/linked-editing 非阻塞 facade、capability gate、request identity 和 stale-result rejection； - ✅ Semantic Tokens、Document Symbols、CodeLens、Document Links、Document Colors 与 Folding 的 product-neutral projection 和 revision freshness； - ✅ 静态/动态 capability gate、dynamic registration 与 work-done progress 事件； - ✅ document pull diagnostics 的 full/unchanged report projection；full report 复用现有 diagnostics 事件和 freshness 规则； - ✅ workspace diagnostics 的 capability gate 与跨文件 raw-coordinate projection；文件读取和 UTF-16 转换仍由 App Server Workspace authority 完成； - ✅ message severity、show/log 区分与 work-done progress 的 product-neutral 事件； - ✅ request kind/server incarnation/configuration + service generation/cold-warm/latency/result-count 与 delivered/empty/failed/cancelled/stale-discarded/rejected 指标；不记录路径、文本、query 或 position； - ✅ 显式 cancelrequest`、disable/reconfigure/shutdown 的 in-flight task cancellation； - ✅ Native pointer hover、latest-request gate、可滚动 completion window/安全 textEdit 接受，以及 F12 definition navigation； - ✅ Desktop 已把 completion resolve/commands、additional edits、semantic tokens 和文档特性接入现有 Editor contract；多 definition target 复用 Peek 列表； - 尚未完成：diagnostic result-id/partial-result/refresh； - 尚未完成：server-specific distribution provider 与产品级 message/install UI； - 尚未安装：references/navigation session cache；先由现有指标证明重复成本，再设计 incarnation/revision 完整绑定的 key 与失效规则； - Potential：远程 workspace authority 出现后再评估是否把 execution 放到 App Server。"
  },
  {
    "slug": "crates/zeterm/layout",
    "sourcePath": "zeterm/layout/README.md",
    "title": "zeta-layout",
    "description": "zeta-layout owns zeterm's product-level pane topology while keeping the layout engine itself in zui. It resolves the root product/inspector split and the terminal workspace with an option…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-layout` owns zeterm's product-level pane topology while keeping the layout engine itself in\n[`zui`](../zui/README.md). It resolves the root product/inspector split and the terminal workspace\nwith an optional right-hand sidebar. It is intentionally independent of `NativeApp`, commands,\nterminal sessions, files, agents, renderers, and platform events.\n\n## Ownership and execution path\n\nHosts project product state into `InspectorPane` and `SidebarLayoutSpec`, then call\n`RootLayout::for_viewports` or `TerminalWorkspaceLayout::for_bounds`. The crate delegates sizing,\nhidden-pane behavior, leaf lookup, and resize snapshots to `zui::{GridLayout, SplitViewPane}`.\nThe host uses the returned rectangles to compose domain views and routes the returned\n`SplitViewResizeSnapshot` through its own pointer lifecycle.\n\n| Symbol | Owner | Contract |\n| --- | --- | --- |\n| `LogicalViewport` | `zeta-layout` | Logical dimensions and physical-to-logical conversion |\n| `InspectorPane` / `RootLayout` | `zeta-layout` | Product/inspector sibling topology |\n| `SidebarVisibility` / `SidebarLayoutSpec` | host projection + `zeta-layout` | Host-neutral sidebar sizing policy |\n| `TerminalWorkspaceLayout` | `zeta-layout` | Active workspace/sidebar bounds and resize geometry |\n| `GridLayout`, `SplitViewPane` | [`zui`](../zui/README.md) | Generic layout algorithms and constraints |\n| Session/agent/terminal state | product host/domain crates | State, persistence, events, and side effects |\n\n## Modification impact\n\nChanges to leaf topology, pane constraints, or resize geometry require updating the sibling tests in\n`src/root_tests.rs` and `src/terminal_workspace_tests.rs`, then the zeterm host adapter in\n`src/agent_sidebar.rs` and `src/shell_scene.rs`. If a change needs product state, command dispatch,\nor renderer/platform APIs here, it belongs in the host or domain crate instead.\n\nVerification:\n\n```text\ncargo test -p zeta-layout\n```",
    "headings": [
      {
        "depth": 2,
        "id": "ownership-and-execution-path",
        "title": "Ownership and execution path"
      },
      {
        "depth": 2,
        "id": "modification-impact",
        "title": "Modification impact"
      }
    ],
    "searchText": "zeta-layout owns zeterm's product-level pane topology while keeping the layout engine itself in zui. It resolves the root product/inspector split and the terminal workspace with an optional right-hand sidebar. It is intentionally independent of NativeApp, commands, terminal sessions, files, agents, renderers, and platform events. Ownership and execution path Hosts project product state into InspectorPane and SidebarLayoutSpec, then call RootLayout::forviewports or TerminalWorkspaceLayout::forbounds. The crate delegates sizing, hidden-pane behavior, leaf lookup, and resize snapshots to zui:: GridLayout, SplitViewPane . The host uses the returned rectangles to compose domain views and routes the returned SplitViewResizeSnapshot through its own pointer lifecycle. Symbol Owner Contract --- --- --- LogicalViewport zeta-layout Logical dimensions and physical-to-logical conversion InspectorPane / RootLayout zeta-layout Product/inspector sibling topology SidebarVisibility / SidebarLayoutSpec host projection + zeta-layout Host-neutral sidebar sizing policy TerminalWorkspaceLayout zeta-layout Active workspace/sidebar bounds and resize geometry GridLayout, SplitViewPane zui Generic layout algorithms and constraints Session/agent/terminal state product host/domain crates State, persistence, events, and side effects Modification impact Changes to leaf topology, pane constraints, or resize geometry require updating the sibling tests in src/roottests.rs and src/terminalworkspacetests.rs, then the zeterm host adapter in src/agentsidebar.rs and src/shellscene.rs. If a change needs product state, command dispatch, or renderer/platform APIs here, it belongs in the host or domain crate instead. Verification: ``text cargo test -p zeta-layout ``"
  },
  {
    "slug": "crates/linux-sandbox",
    "sourcePath": "zeta-rs/linux-sandbox/README.md",
    "title": "zeta-linux-sandbox",
    "description": "LinuxSandbox::discover 在 host composition 时冻结 Bubblewrap executable。候选由 zeta-install-context 提供：",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 shared sandbox policy 到 Linux Bubblewrap enforcement 的实现契约；\n> package source/build contract 见\n> [`scripts/zeta_package`](../../scripts/zeta_package/README.md)，跨平台决策见\n> [`docs/sandboxing.md`](../../docs/sandboxing.md)。\n\n`LinuxSandbox::discover` 在 host composition 时冻结 Bubblewrap executable。候选由\n`zeta-install-context` 提供：\n\n1. authoritative `ZETA_BWRAP_PATH`；\n2. package `zeta-resources/bwrap`；\n3. 启动时 host `PATH` 中的 `bwrap`。\n\n私有 `discovery::validate_candidate` canonicalize executable、拒绝非普通文件和非 executable；\n`probe_bubblewrap` 执行 `--help`，要求实际 binary 支持 builder 使用的 bind、read-only bind、\nnetwork namespace、parent-death 与 new-session flags。显式 override 无效时不 fallback；普通候选\n可以跳过并继续。选中的 canonical path 进入 `LinuxSandbox::bwrap_binary`，probe 与执行不会重新\n解析 `PATH`。\n\n`LinuxSandbox::prepare_command` 根据 `SandboxPolicy` 生成 `PreparedCommand`：非 FullAccess\nroot read-only，WorkspaceWrite 叠加 writable Workspace 和 read-only protected metadata，\nDenied network 添加独立 network namespace。`classify_denial` 区分 Bubblewrap setup failure 与\n可能已启动进程后的 OS denial。\n\n```text\nInstallContext::executable_candidates(Bubblewrap)\n→ discovery::validate_candidate\n→ discovery::probe_bubblewrap\n→ LinuxSandbox { canonical bwrap_binary }\n→ prepare_command\n→ zeta_bwrap::BwrapCommandBuilder\n→ CommandExecutor\n```\n\n当前限制：尚未加入 seccomp、WSL-specific diagnostics 或 managed-network bridge。修改 capability\nmarkers、mount order、protected metadata 或 denial classification 时，必须同步本 crate tests、\n`zeta-bwrap` builder tests、package contract 和系统文档。\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-linux-sandbox\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-linux-sandbox --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/linux-sandbox:linux-sandbox-unit-tests\n```",
    "headings": [],
    "searchText": " 本 README 拥有 shared sandbox policy 到 Linux Bubblewrap enforcement 的实现契约； package source/build contract 见 scripts/zetapackage，跨平台决策见 docs/sandboxing.md。 LinuxSandbox::discover 在 host composition 时冻结 Bubblewrap executable。候选由 zeta-install-context 提供： 1. authoritative ZETABWRAPPATH； 2. package zeta-resources/bwrap； 3. 启动时 host PATH 中的 bwrap。 私有 discovery::validatecandidate canonicalize executable、拒绝非普通文件和非 executable； probebubblewrap 执行 --help，要求实际 binary 支持 builder 使用的 bind、read-only bind、 network namespace、parent-death 与 new-session flags。显式 override 无效时不 fallback；普通候选 可以跳过并继续。选中的 canonical path 进入 LinuxSandbox::bwrapbinary，probe 与执行不会重新 解析 PATH。 LinuxSandbox::preparecommand 根据 SandboxPolicy 生成 PreparedCommand：非 FullAccess root read-only，WorkspaceWrite 叠加 writable Workspace 和 read-only protected metadata， Denied network 添加独立 network namespace。classifydenial 区分 Bubblewrap setup failure 与 可能已启动进程后的 OS denial。 ``text InstallContext::executablecandidates Bubblewrap → discovery::validatecandidate → discovery::probebubblewrap → LinuxSandbox canonical bwrapbinary → preparecommand → zetabwrap::BwrapCommandBuilder → CommandExecutor ` 当前限制：尚未加入 seccomp、WSL-specific diagnostics 或 managed-network bridge。修改 capability markers、mount order、protected metadata 或 denial classification 时，必须同步本 crate tests、 zeta-bwrap builder tests、package contract 和系统文档。 `bash cargo test --manifest-path Cargo.toml -p zeta-linux-sandbox cargo clippy --manifest-path Cargo.toml \\ -p zeta-linux-sandbox --all-targets --no-deps -- -D warnings bazel test //zeta-rs/linux-sandbox:linux-sandbox-unit-tests ``"
  },
  {
    "slug": "crates/login",
    "sourcePath": "zeta-rs/login/README.md",
    "title": "zeta-login",
    "description": "zeta-login owns Zeta's redacted interactive-account control plane. It assigns stable login IDs, tracks active attempts, projects revisioned account state, and emits completion/account eve…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-login` owns Zeta's redacted interactive-account control plane. It assigns stable login IDs,\ntracks active attempts, projects revisioned account state, and emits completion/account events.\nProvider adapters retain OAuth protocol, callback, credential persistence, refresh, and logout I/O.\n\n## Ownership and execution\n\n`LoginService` is the state owner. `InteractiveLoginDriver` is its consumer-owned provider port;\n`LoginEvents` is the product-host output port. The normal path is:\n\n```text\nLoginService::begin\n→ InteractiveLoginDriver::begin(BeginLoginRequest { exact LoginId, method })\n→ BeginLogin::Browser | BeginLogin::DeviceCode\n→ provider adapter observes completion\n→ LoginService::complete\n→ LoginEvents::login_completed\n→ LoginEvents::account_updated on success\n```\n\n`LoginService::cancel` only accepts an active exact `LoginId` and delegates idempotent cancellation.\n`LoginService::logout` passes only `AccountRef` to the driver, then clears the redacted projection and\nincrements its revision. `refresh` reads a redacted driver snapshot and publishes only when it changed.\n\n## Failure and security contract\n\n`LoginErrorKind` supplies stable internal categories; product protocols map those categories without\nforwarding provider payloads. Neither public types nor events contain access tokens, refresh tokens,\nAPI keys, cookies, authorization codes, PKCE state, secret-store references, or credential paths.\n\nThe production ChatGPT/Codex implementation lives in `zeta-codex-app-server` and delegates managed\nlogin to an upstream local Codex App Server. This crate remains provider-neutral and does not depend\non that adapter. Tests here use a fake driver to keep identity, cancellation, revision, and event\nsemantics independent of any provider process.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership-and-execution",
        "title": "Ownership and execution"
      },
      {
        "depth": 2,
        "id": "failure-and-security-contract",
        "title": "Failure and security contract"
      }
    ],
    "searchText": "zeta-login owns Zeta's redacted interactive-account control plane. It assigns stable login IDs, tracks active attempts, projects revisioned account state, and emits completion/account events. Provider adapters retain OAuth protocol, callback, credential persistence, refresh, and logout I/O. Ownership and execution LoginService is the state owner. InteractiveLoginDriver is its consumer-owned provider port; LoginEvents is the product-host output port. The normal path is: ``text LoginService::begin → InteractiveLoginDriver::begin BeginLoginRequest exact LoginId, method → BeginLogin::Browser BeginLogin::DeviceCode → provider adapter observes completion → LoginService::complete → LoginEvents::logincompleted → LoginEvents::accountupdated on success ` LoginService::cancel only accepts an active exact LoginId and delegates idempotent cancellation. LoginService::logout passes only AccountRef to the driver, then clears the redacted projection and increments its revision. refresh reads a redacted driver snapshot and publishes only when it changed. Failure and security contract LoginErrorKind supplies stable internal categories; product protocols map those categories without forwarding provider payloads. Neither public types nor events contain access tokens, refresh tokens, API keys, cookies, authorization codes, PKCE state, secret-store references, or credential paths. The production ChatGPT/Codex implementation lives in zeta-codex-app-server` and delegates managed login to an upstream local Codex App Server. This crate remains provider-neutral and does not depend on that adapter. Tests here use a fake driver to keep identity, cancellation, revision, and event semantics independent of any provider process."
  },
  {
    "slug": "crates/lsp",
    "sourcePath": "zeta-rs/lsp/README.md",
    "title": "zeta-lsp",
    "description": "zeta-lsp 负责启动或连接一个语言服务器、执行 LSP 生命周期、配对请求、同步打开文档的版本， 并把诊断和服务端消息交给产品宿主。它不选择或安装服务器，不读取文件，不拥有工作区配置、 编辑器状态、诊断展示、自动重启或 App Server API。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是低层 LSP 客户端运行时的 crate-level canonical contract。跨 crate 的产品语义、\n> 宿主职责和演进阶段见 [`docs/lsp.md`](../../docs/lsp.md)；Native 编辑器展示契约见\n> [`zeta-editor`](../editor/README.md)，产品级启停与路由见\n> [`zeta-language-service`](../language-service/README.md)，server 发现与 resolved command 见\n> [`zeta-language-server-catalog`](../language-server-catalog/README.md)。\n\n`zeta-lsp` 负责启动或连接一个语言服务器、执行 LSP 生命周期、配对请求、同步打开文档的版本，\n并把诊断和服务端消息交给产品宿主。它不选择或安装服务器，不读取文件，不拥有工作区配置、\n编辑器状态、诊断展示、自动重启或 App Server API。\n\n## 所有权与公共接口\n\n| API / type | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `LanguageServerClient` | initialize 后的单服务器 session、类型化请求、文档同步、transport-close 事实和关闭 | server discovery、共享进程、重启策略 |\n| `LanguageServerCommand` / `LanguageServerEnvironmentPolicy` | 保存宿主已经解析的 program、args、cwd，并明确选择继承或清空环境 | 验证 executable trust、安装或 sandbox |\n| `LanguageServerOptions` | client identity、workspace、capability、initialize options、host 和 deadline | 读取产品配置 |\n| `LanguageServerInitialization` | 冻结 server info、初始 capability、position encoding 和 document sync policy | 把随后动态注册伪装成 initialize 快照 |\n| `LanguageServerHost` | 快速接收事件，并按顺序回答 `workspace/configuration`，消费 progress/message/log 事件 | 直接修改 UI 或阻塞协议 driver |\n| `LanguageServerDocumentRouter` | 一个 language ID 到一个 initialized client 的路由、全文 snapshot 同步、replacement replay 与断连 route retirement | server discovery、重启判断或 backoff |\n| `LanguageDocumentSnapshot` | 绑定 URI、language ID、EditorHost revision 与完整 authoritative text | 从 filesystem 或 editor 自行取内容 |\n| `RoutedDocumentVersion` | 绑定 editor revision、server incarnation 与 LSP document version | 充当磁盘或 durable revision |\n| `DocumentChange` / `DocumentSave` | 用 tagged enum 表达完整/增量 change 和 save text | 从编辑器 mutation 自动计算 range |\n| `DocumentVersion` | 每个 open document 从 1 开始单调递增 | 表示磁盘 revision 或 durable identity |\n| `lsp_types` re-export | 暴露调用类型化请求所需的标准协议类型 | 承诺 LSP 3.18 的全部新增能力 |\n\n`LanguageServerClient::request<R>` 接受实现 `lsp_types::request::Request` 的请求类型。请求 ID 在\n同一 session 内单调递增；普通请求使用独立 deadline，超时后发送 `$/cancelRequest`。初始化和\n关闭使用各自 deadline。\n\n## 文件与内部接口\n\n| 文件 / private symbol | 精确职责 | 不能承担 |\n| --- | --- | --- |\n| `client.rs::LanguageServerClient::connect_inner` | 创建 driver，执行 initialize/initialized gate，并冻结 capability | server selection 或 restart loop |\n| `raw_client.rs::RawClient` | 分配 request ID、序列化 typed params/result、执行 deadline cancellation | 文档或产品状态 |\n| `driver.rs::run_driver` | 单一 writer 顺序、pending response table、服务端 request/notification dispatch，并上报意外 transport close | UI callback scheduling 或 restart policy |\n| `protocol.rs::{read_frame,write_frame}` | 有界 `Content-Length` framing | LSP method 语义 |\n| `protocol.rs::parse_message` | 区分 request/response/notification；保留合法 `result: null` | typed result decoding |\n| `document.rs::DocumentSyncPolicy::from_capability` | 把 server sync capability 冻结为明确策略 | 猜测未声明能力 |\n| `client.rs::reap_process` | `exit` 后有界等待，超时则终止 child | 自动拉起 replacement |\n| `router.rs::LanguageServerDocumentRouter` | 注册唯一 language route、保存当前全文和精确 revision binding | 选择 executable 或读取 editor |\n| `router.rs::replace_server` / `remove_disconnected_server` | 显式 replacement replay；或丢弃断连 route 和旧 document bindings | 检测 crash、退避或静默吞掉 replay failure |\n\n```text\nLanguageServerClient::start_stdio / connect\n  → driver::spawn_driver\n  → RawClient::request<Initialize>\n  → validate position encoding\n  → DocumentSyncPolicy::from_capability\n  → RawClient::notify<Initialized>\n  → ready LanguageServerClient\n\nLanguageServerClient::request<R>\n  → RawClient::request\n  → DriverCommand::Request\n  → driver pending table\n  → protocol::write_frame\n  → protocol::read_frame / parse_message\n  → matching typed result\n\nopen/change/save/close\n  → validate negotiated DocumentSyncPolicy\n  → serialize under document table lock\n  → assign DocumentVersion\n  → driver notification\n\nLanguageServerDocumentRouter::open_document / update_document\n  → resolve unique language route\n  → send full authoritative snapshot\n  → bind EditorDocumentRevision + LanguageServerIncarnation + DocumentVersion\n\nLanguageServerDocumentRouter::replace_server\n  → replay sorted current snapshots into replacement\n  → any failure: close replayed documents + shutdown replacement + keep old route\n  → success: increment incarnation + swap route + shutdown old client\n\nunexpected transport EOF / framing failure\n  → LanguageServerEvent::TransportClosed\n  → product supervisor decides recovery policy\n  → LanguageServerDocumentRouter::remove_disconnected_server\n  → abort unusable transport/process without an LSP shutdown handshake\n```\n\n若 `client.rs` 开始选择语言服务器、决定 enablement、读取磁盘或保存 UI 状态，表示 crate ownership 已经漂移；\n若 `zeta-editor` 直接启动进程或解析 LSP JSON，表示运行时边界被绕过。产品宿主应把 LSP range\n按 initialize 选定的 position encoding 转换成编辑器 byte range，不能假设所有服务器都使用\nUTF-8。\n\n## 协议、失败与宿主义务\n\nTransport 使用 LSP stdio 的 `Content-Length` framing。Header 最多 16 KiB，单条 JSON message\n最多 4 MiB；缺少或重复 `Content-Length`、非 JSON-RPC 2.0 envelope、非整数 client response ID\n和同时缺少 result/error 的 response 都会关闭当前 driver。服务端 request 目前支持\n`workspace/configuration`、`client/registerCapability`、`client/unregisterCapability` 和\n`window/workDoneProgress/create`；其他 request 返回 JSON-RPC `Method not found` 并产生\n`UnsupportedServerRequest` 事件。\n\n宿主必须：\n\n- 在启动前决定 executable trust、workspace root、environment 与 sandbox；执行包内 JavaScript\n  server 时应清空环境后只恢复明确 allowlist，不得继承 `NODE_OPTIONS` 或 `NODE_PATH`；\n- 让 `LanguageServerHost::on_event` 快速返回，把 UI 工作排入自己的 event loop；\n- 为 `workspace_configuration` 返回与请求项数量和顺序完全一致的结果；\n- 为每个文件维持稳定 URI 和 language ID，并只对 open document 发送 change/save/close；\n- 按 `position_encoding` 转换位置，按 document version 丢弃过期诊断或请求结果；\n- 消费 `TransportClosed` 并在产品层决定是否退避重启；断连 route 必须先退休，再从产品拥有的\n  authoritative snapshot 重放，不能复用 router 中的旧文本。\n\n调用 `replace_server` 时，replacement 使用的 `LanguageServerHost` 必须先暂存事件；只有方法成功并\n返回新 incarnation 后才能发布这些事件。Replay 期间服务器可能立即发送 diagnostics，router\n不会替产品宿主伪造跨 callback 的原子屏障。失败时旧 route 与所有原 document binding 保持不变。\n\n`LanguageServerClient::shutdown` 即使 shutdown request 失败也会尝试发送 `exit`、停止 driver 并回收\nchild，最后返回原 shutdown error。Client 被直接 drop 时 child 使用 `kill_on_drop`，但这不是规范\n关闭路径。`abort_disconnected` 只用于 transport 已关闭的 session：它跳过不可能完成的 LSP handshake，\n停止 driver 并终止残留 child；正常关闭不能走这条路径。\n\n## 测试、修改影响与当前限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-lsp\ncargo clippy --manifest-path Cargo.toml -p zeta-lsp --all-targets -- -D warnings\nbazel test //zeta-rs/lsp:lsp-unit-tests\n```\n\n`client_tests.rs` 使用真实内存双工 transport 覆盖 initialize/initialized、UTF-8 position\nencoding、`workspace/configuration`、诊断、open/change/save/close、typed hover、timeout\ncancellation、意外 transport close、shutdown/exit 和 framing validation。`router_tests.rs` 另外覆盖 route validation、\nstale editor revision、全文更新、replacement replay、incarnation reset、save/close 与多 client\nshutdown，以及断连 route retirement 后从 authoritative snapshot 重新注册和重放。\n\n修改 framing limit 或 envelope classification 时同步更新 transport tests；修改 capability\nadvertisement 时同步检查服务端 request handler；修改 document policy 时同步检查版本顺序和\nNative position conversion；增加 server request 时必须先定义宿主 authority 和 failure behavior。\n\n当前限制：\n\n- Current：兼容常见 LSP 3.17 生命周期、请求、push diagnostics 和 text synchronization；\n- Current：仅支持 stdio child 或 caller-provided async transport halves；\n- Current：宿主可注册 initialized client，以唯一 language route 同步全文 snapshot，并在显式\n  replacement 时重放当前文档和重置 server version/incarnation；\n- Current：服务端 request 已实现 `workspace/configuration`、动态 capability register/unregister 与 work-done progress token 创建；\n- Current：支持 diagnostic client capability、动态注册、typed document pull 与 typed workspace pull request；\n- 当前限制：没有 diagnostic refresh/result-id cache、workspace edit、semantic token delta 或 file-operation registration；\n- Current：`zeta-language-service` 已作为 product host 使用 router，负责显式启停、resolved definition、\n  revision freshness 和 Native event-loop 接线；\n- Current：Native 通过独立 catalog 自动解析 PATH 中的 `rust-analyzer`；缺失时不启动 server；\n- Current：Native 已通过 App Server Config authority 接入持久化 mode/path 和 Settings UI；\n- Current：driver 会区分规范关闭和意外 transport close；`zeta-language-service` 消费该事实，执行\n  有界指数退避、crash-loop gate、断连 route retirement 与 authoritative document replay；\n- Current：Native 已完成 diagnostics 下划线、hover detail 和 server runtime state 投影；语言请求结果\n  UI 仍未完成；\n- 当前限制：replacement host event gate 由调用方持有，router 不控制外部 callback queue；\n- Potential：更多 transport health facts 可以继续由本 crate 上报，但重启预算、安装路径和用户策略\n  必须继续由产品协调层或 catalog 拥有。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与公共接口",
        "title": "所有权与公共接口"
      },
      {
        "depth": 2,
        "id": "文件与内部接口",
        "title": "文件与内部接口"
      },
      {
        "depth": 2,
        "id": "协议失败与宿主义务",
        "title": "协议、失败与宿主义务"
      },
      {
        "depth": 2,
        "id": "测试修改影响与当前限制",
        "title": "测试、修改影响与当前限制"
      }
    ],
    "searchText": " 本 README 是低层 LSP 客户端运行时的 crate-level canonical contract。跨 crate 的产品语义、 宿主职责和演进阶段见 docs/lsp.md；Native 编辑器展示契约见 zeta-editor，产品级启停与路由见 zeta-language-service，server 发现与 resolved command 见 zeta-language-server-catalog。 zeta-lsp 负责启动或连接一个语言服务器、执行 LSP 生命周期、配对请求、同步打开文档的版本， 并把诊断和服务端消息交给产品宿主。它不选择或安装服务器，不读取文件，不拥有工作区配置、 编辑器状态、诊断展示、自动重启或 App Server API。 所有权与公共接口 API / type 当前职责 明确不做 --- --- --- LanguageServerClient initialize 后的单服务器 session、类型化请求、文档同步、transport-close 事实和关闭 server discovery、共享进程、重启策略 LanguageServerCommand / LanguageServerEnvironmentPolicy 保存宿主已经解析的 program、args、cwd，并明确选择继承或清空环境 验证 executable trust、安装或 sandbox LanguageServerOptions client identity、workspace、capability、initialize options、host 和 deadline 读取产品配置 LanguageServerInitialization 冻结 server info、初始 capability、position encoding 和 document sync policy 把随后动态注册伪装成 initialize 快照 LanguageServerHost 快速接收事件，并按顺序回答 workspace/configuration，消费 progress/message/log 事件 直接修改 UI 或阻塞协议 driver LanguageServerDocumentRouter 一个 language ID 到一个 initialized client 的路由、全文 snapshot 同步、replacement replay 与断连 route retirement server discovery、重启判断或 backoff LanguageDocumentSnapshot 绑定 URI、language ID、EditorHost revision 与完整 authoritative text 从 filesystem 或 editor 自行取内容 RoutedDocumentVersion 绑定 editor revision、server incarnation 与 LSP document version 充当磁盘或 durable revision DocumentChange / DocumentSave 用 tagged enum 表达完整/增量 change 和 save text 从编辑器 mutation 自动计算 range DocumentVersion 每个 open document 从 1 开始单调递增 表示磁盘 revision 或 durable identity lsptypes re-export 暴露调用类型化请求所需的标准协议类型 承诺 LSP 3.18 的全部新增能力 LanguageServerClient::request<R 接受实现 lsptypes::request::Request 的请求类型。请求 ID 在 同一 session 内单调递增；普通请求使用独立 deadline，超时后发送 $/cancelRequest。初始化和 关闭使用各自 deadline。 文件与内部接口 文件 / private symbol 精确职责 不能承担 --- --- --- client.rs::LanguageServerClient::connectinner 创建 driver，执行 initialize/initialized gate，并冻结 capability server selection 或 restart loop rawclient.rs::RawClient 分配 request ID、序列化 typed params/result、执行 deadline cancellation 文档或产品状态 driver.rs::rundriver 单一 writer 顺序、pending response table、服务端 request/notification dispatch，并上报意外 transport close UI callback scheduling 或 restart policy protocol.rs:: readframe,writeframe 有界 Content-Length framing LSP method 语义 protocol.rs::parsemessage 区分 request/response/notification；保留合法 result: null typed result decoding document.rs::DocumentSyncPolicy::fromcapability 把 server sync capability 冻结为明确策略 猜测未声明能力 client.rs::reapprocess exit 后有界等待，超时则终止 child 自动拉起 replacement router.rs::LanguageServerDocumentRouter 注册唯一 language route、保存当前全文和精确 revision binding 选择 executable 或读取 editor router.rs::replaceserver / removedisconnectedserver 显式 replacement replay；或丢弃断连 route 和旧 document bindings 检测 crash、退避或静默吞掉 replay failure ``text LanguageServerClient::startstdio / connect → driver::spawndriver → RawClient::request<Initialize → validate position encoding → DocumentSyncPolicy::fromcapability → RawClient::notify<Initialized → ready LanguageServerClient LanguageServerClient::request<R → RawClient::request → DriverCommand::Request → driver pending table → protocol::writeframe → protocol::readframe / parsemessage → matching typed result open/change/save/close → validate negotiated DocumentSyncPolicy → serialize under document table lock → assign DocumentVersion → driver notification LanguageServerDocumentRouter::opendocument / updatedocument → resolve unique language route → send full authoritative snapshot → bind EditorDocumentRevision + LanguageServerIncarnation + DocumentVersion LanguageServerDocumentRouter::replaceserver → replay sorted current snapshots into replacement → any failure: close replayed documents + shutdown replacement + keep old route → success: increment incarnation + swap route + shutdown old client unexpected transport EOF / framing failure → LanguageServerEvent::TransportClosed → product supervisor decides recovery policy → LanguageServerDocumentRouter::removedisconnectedserver → abort unusable transport/process without an LSP shutdown handshake ` 若 client.rs 开始选择语言服务器、决定 enablement、读取磁盘或保存 UI 状态，表示 crate ownership 已经漂移； 若 zeta-editor 直接启动进程或解析 LSP JSON，表示运行时边界被绕过。产品宿主应把 LSP range 按 initialize 选定的 position encoding 转换成编辑器 byte range，不能假设所有服务器都使用 UTF-8。 协议、失败与宿主义务 Transport 使用 LSP stdio 的 Content-Length framing。Header 最多 16 KiB，单条 JSON message 最多 4 MiB；缺少或重复 Content-Length、非 JSON-RPC 2.0 envelope、非整数 client response ID 和同时缺少 result/error 的 response 都会关闭当前 driver。服务端 request 目前支持 workspace/configuration、client/registerCapability、client/unregisterCapability 和 window/workDoneProgress/create；其他 request 返回 JSON-RPC Method not found 并产生 UnsupportedServerRequest 事件。 宿主必须： - 在启动前决定 executable trust、workspace root、environment 与 sandbox；执行包内 JavaScript server 时应清空环境后只恢复明确 allowlist，不得继承 NODEOPTIONS 或 NODEPATH； - 让 LanguageServerHost::onevent 快速返回，把 UI 工作排入自己的 event loop； - 为 workspaceconfiguration 返回与请求项数量和顺序完全一致的结果； - 为每个文件维持稳定 URI 和 language ID，并只对 open document 发送 change/save/close； - 按 positionencoding 转换位置，按 document version 丢弃过期诊断或请求结果； - 消费 TransportClosed 并在产品层决定是否退避重启；断连 route 必须先退休，再从产品拥有的 authoritative snapshot 重放，不能复用 router 中的旧文本。 调用 replaceserver 时，replacement 使用的 LanguageServerHost 必须先暂存事件；只有方法成功并 返回新 incarnation 后才能发布这些事件。Replay 期间服务器可能立即发送 diagnostics，router 不会替产品宿主伪造跨 callback 的原子屏障。失败时旧 route 与所有原 document binding 保持不变。 LanguageServerClient::shutdown 即使 shutdown request 失败也会尝试发送 exit、停止 driver 并回收 child，最后返回原 shutdown error。Client 被直接 drop 时 child 使用 killondrop，但这不是规范 关闭路径。abortdisconnected 只用于 transport 已关闭的 session：它跳过不可能完成的 LSP handshake， 停止 driver 并终止残留 child；正常关闭不能走这条路径。 测试、修改影响与当前限制 `bash cargo test --manifest-path Cargo.toml -p zeta-lsp cargo clippy --manifest-path Cargo.toml -p zeta-lsp --all-targets -- -D warnings bazel test //zeta-rs/lsp:lsp-unit-tests ` clienttests.rs 使用真实内存双工 transport 覆盖 initialize/initialized、UTF-8 position encoding、workspace/configuration、诊断、open/change/save/close、typed hover、timeout cancellation、意外 transport close、shutdown/exit 和 framing validation。routertests.rs 另外覆盖 route validation、 stale editor revision、全文更新、replacement replay、incarnation reset、save/close 与多 client shutdown，以及断连 route retirement 后从 authoritative snapshot 重新注册和重放。 修改 framing limit 或 envelope classification 时同步更新 transport tests；修改 capability advertisement 时同步检查服务端 request handler；修改 document policy 时同步检查版本顺序和 Native position conversion；增加 server request 时必须先定义宿主 authority 和 failure behavior。 当前限制： - Current：兼容常见 LSP 3.17 生命周期、请求、push diagnostics 和 text synchronization； - Current：仅支持 stdio child 或 caller-provided async transport halves； - Current：宿主可注册 initialized client，以唯一 language route 同步全文 snapshot，并在显式 replacement 时重放当前文档和重置 server version/incarnation； - Current：服务端 request 已实现 workspace/configuration、动态 capability register/unregister 与 work-done progress token 创建； - Current：支持 diagnostic client capability、动态注册、typed document pull 与 typed workspace pull request； - 当前限制：没有 diagnostic refresh/result-id cache、workspace edit、semantic token delta 或 file-operation registration； - Current：zeta-language-service 已作为 product host 使用 router，负责显式启停、resolved definition、 revision freshness 和 Native event-loop 接线； - Current：Native 通过独立 catalog 自动解析 PATH 中的 rust-analyzer；缺失时不启动 server； - Current：Native 已通过 App Server Config authority 接入持久化 mode/path 和 Settings UI； - Current：driver 会区分规范关闭和意外 transport close；zeta-language-service` 消费该事实，执行 有界指数退避、crash-loop gate、断连 route retirement 与 authoritative document replay； - Current：Native 已完成 diagnostics 下划线、hover detail 和 server runtime state 投影；语言请求结果 UI 仍未完成； - 当前限制：replacement host event gate 由调用方持有，router 不控制外部 callback queue； - Potential：更多 transport health facts 可以继续由本 crate 上报，但重启预算、安装路径和用户策略 必须继续由产品协调层或 catalog 拥有。"
  },
  {
    "slug": "crates/zeterm/markdown",
    "sourcePath": "zeterm/markdown/README.md",
    "title": "zeta-markdown",
    "description": "zeta-markdown 把有资源上限的 CommonMark/GFM 输入解析为只读文档 snapshot，并通过 zeta-ui primitive 生成 Native Markdown 组件。它拥有 Markdown 结构、脚注与数学片段投影、 syntect 代码高亮、RaTeX 数学排版、安全链接策略、图片解码/布局、文字命中/选择/搜索几何、 复制文本投影、…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Native Markdown 解析、布局和 presentation 的 crate-level canonical\n> contract。跨 crate 的 Rust Workspace 边界见\n> [`docs/zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md)；底层 scene、富文本和字体\n> shaping contract 见 [`ui/README.md`](../ui/README.md)。\n\n`zeta-markdown` 把有资源上限的 CommonMark/GFM 输入解析为只读文档 snapshot，并通过\n`zeta-ui` primitive 生成 Native Markdown 组件。它拥有 Markdown 结构、脚注与数学片段投影、\n`syntect` 代码高亮、RaTeX 数学排版、安全链接策略、图片解码/布局、文字命中/选择/搜索几何、\n复制文本投影、accessibility 语义树、滚动 snapshot 消费和主题 token；不拥有消息 identity、网络取图、\n平台 URL opener/clipboard、输入事件分发、持久化或产品生命周期。\n\n## 所有权与接口\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `MarkdownDocument::parse` | public | 在 4 MiB 输入、100,000 blocks 和 64 层嵌套上限内建立不可变文档 snapshot |\n| `MarkdownError` | public | 区分输入过大、嵌套过深和 block 数超限 |\n| `MarkdownLayoutEngine` | public | 保留可复用 font shaping state，把文档、bounds、`ScrollState` 和 style 投影为一帧 `Markdown` |\n| `MarkdownPresentation` | public | 绑定 caller-retained selection、search matches 与已授权解码的图片 snapshot |\n| `Markdown` / `MarkdownLink` | public | 保存当前帧可见 primitive、文本命中/选择几何、语义树和 viewport-clipped link hit fragments |\n| `MarkdownLinkPolicy` | public | 解析 document fragment 或绝对 URL，拒绝 credentials/未知 scheme，并只产出策略允许的 `MarkdownLinkTarget` |\n| `MarkdownImages` / `decode_markdown_image` | public | 接收 host 已授权取得的 bytes，在 16,777,216 pixel 上限内解码为 `zeta_ui::ImageData`；不执行 I/O |\n| `MarkdownSyntaxHighlighter` / `SyntectMarkdownHighlighter` | public | 定义 fenced-code byte-range 高亮 contract，并提供 bundled syntax/theme 实现 |\n| `render_markdown_math` / `MarkdownMathMode` | public | 在 64 KiB source 和 4,194,304 pixel 上限内把 inline/display LaTeX 排版并栅格化为 `ImageData` |\n| `MarkdownSelectionController` / `MarkdownDocument::text_for_selection` | public | 保存 pointer selection anchor/focus，并把合法范围投影为可写入 clipboard 的文本 |\n| `MarkdownDocument::search` | public | 在 copyable block text 上执行大小写敏感或 Unicode lowercase literal search |\n| `MarkdownSemanticNode` | public | 暴露 document/block/link/table-row/cell 的 role、label、level、destination 和 viewport bounds |\n| `MarkdownStyle` | public | 提供正文、标题、链接、代码、引用、列表和表格的 presentation token 与 geometry |\n| `document::DocumentBuilder` | private | 消费 `pulldown-cmark` event，维护 inline format、quote/list/table nesting 和当前 block |\n| `table::TableBuilder` | private | 把 table event 序列组装成保留 row/cell 边界的单个 `MarkdownTable` block |\n| `document::push_run` | private | 合并相邻同样式 run，避免解析事件制造无意义的 span 碎片 |\n| `inline_layout::layout_inline` | private | 调用 `TextLayoutEngine::layout_spans`，从同一次 shaping 取得 span fragments，并生成 code/link/strikethrough decoration |\n| `document_text` | private | 规范化 block plain text、selection copy 与 literal search range；不得读取 scene geometry |\n| `component_interaction` | private | 用同一次 shaping 的 UTF-8 cluster geometry 完成 point hit、selection/search fragments 和链接策略入口 |\n| `component_paint` | private | 生成 rect/image/text primitive，并构造与当前 viewport geometry 一致的语义节点 |\n| `math::MarkdownMathCache` | private | 按 source、mode、颜色和字号缓存 RaTeX 结果；失败项缓存为空并由布局回退为可见 source |\n| `table_layout::layout_table` | private | 从全部 cell 的 intrinsic width 计算列宽，再按列宽 wrapping cell，并保存 row height |\n| `component::ProjectedBlock` | private | 保存 shaping 后的单个 text/code/table/rule block 高度与宽度 |\n| `Markdown::emit` | private | 把自然文档坐标减去有效 viewport offset，剔除不可见 block 并生成 scene primitive |\n\n```text\nMarkdownDocument::parse\n└─ pulldown_cmark::Parser\n   └─ DocumentBuilder::consume\n      ├─ inline format/link stack\n      ├─ quote/list context\n      ├─ TableBuilder → MarkdownTable rows/cells\n      └─ MarkdownBlock\n\nMarkdownLayoutEngine::layout\n├─ MarkdownBlock + MarkdownStyle\n├─ zeta_ui::TextLayoutEngine\n│  └─ TextLayout size + per-span/per-UTF-8-cluster wrapped/BiDi visual fragments\n├─ SyntectMarkdownHighlighter → fenced-code TextSpan colors\n├─ MarkdownMathCache → RaTeX parse/layout/SVG → bounded RGBA ImageData\n├─ MarkdownPresentation → selection/search/image snapshots\n├─ layout_inline → inline backgrounds/decorations/link hit fragments\n├─ layout_table → intrinsic column sizing → wrapped cell layout\n├─ content_height + clamped zeta_ui::ScrollState\n└─ Markdown::emit\n   ├─ PaintRect: code/table/quote/rule/inline/selection/search decoration\n   ├─ PaintImage: decoded RGBA image + inline/display typeset math\n   ├─ MarkdownLink: viewport-clipped hit fragments\n   ├─ MarkdownSemanticNode: document/block/link/table structure\n   └─ TextBlock/TextSpan: wrapped rich text and highlighted code\n\nMarkdown::paint\n├─ zui Element(Markdown viewport bounds) → automatic inspection\n└─ UiScene::with_clip(viewport bounds)\n```\n\n`MarkdownLayoutEngine` 必须由 host 复用；每次 document、bounds、style 或 scroll snapshot 变化时重新\n生成 immutable `Markdown`。`Markdown` 不保存 parser 或 font system，也不接受输入事件。\n`zeta_ui::ScrollState` 是 product-owned retained state，组件只消费其 snapshot；wheel normalization、\nscrollbar 和 input routing 继续复用 `zeta-ui` 的通用 scroll contract。\n\n## 解析、信任与显示语义\n\n解析启用 CommonMark、GFM block quote、table、strikethrough、task-list、footnote 和 math 扩展。Soft break\n折叠为空格，hard break 保留换行；fenced code 的 info string 作为语言标签显示。表格保留\nrow/cell 边界，先以所有 cell 的 intrinsic width 分配整表列宽，再在各列内部独立换行；header\n使用独立背景和粗体，网格线只绘制一次。\n\nMarkdown 输入是不可信文本：\n\n- raw/inline HTML 只作为可见字符串保留，不解释 DOM、脚本、attribute 或 URL；\n- 图片 reference 可由 host 授权取得 bytes 后交给 `decode_markdown_image`；未加载时显示 alt\n  placeholder，解析和 layout 不发起网络、文件或 data-URL I/O；\n- inline/display math 由 pure-Rust RaTeX 解析、排版并栅格化；非法或超限表达式保留为可见\n  source，不中断其余文档布局；\n- link destination 进入 `MarkdownLink` 的同源命中 geometry；只有\n  `MarkdownLinkPolicy::evaluate` 成功产生的 external `MarkdownLinkTarget` 才能交给 host\n  opener；document fragment 通过 `Markdown::fragment_bounds` 路由到同一帧 heading/footnote geometry；\n- `text_for_selection` 只返回文本，真正写入系统 clipboard 仍由 platform host 执行；\n- crate 没有文件、网络、WebView、shell 或 platform clipboard 依赖，因此 parse/layout 不能触发外部副作用。\n\nhost 从 pointer event 调用 `Markdown::activate_link_at`，不能把 parser destination 直接交给\nshell。图片 loader 必须先应用 product policy，再把取得的 bytes 解码并按 destination 放进\n`MarkdownImages`。同一个 `ImageId` 在 renderer cache 生命周期内必须始终指向同一份 immutable pixels。\n\n## 测试、修改影响与限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-markdown\ncargo clippy --manifest-path Cargo.toml -p zeta-markdown --all-targets -- -D warnings\nbazel test //zeterm/markdown:markdown-unit-tests\n```\n\n测试覆盖 block/inline parsing、脚注与 math event、RaTeX inline/display scene projection 和非法公式\n回退、列表与任务 marker、引用 nesting、代码高亮、\n表格 header/body、raw HTML、输入上限、安全 URL/fragment policy、图片有界解码与 inline/block\nscene projection、Unicode search、selection/copy/hit geometry、语义 heading/table/link 节点、\n段落换行和 viewport clamp。修改 parser option 或 `DocumentBuilder` 时必须同步检查资源上限、\nHTML/image 信任边界和 block fixtures；修改 `ProjectedBlock`/spacing 时必须同步检查 content\nheight、offscreen culling 与 scroll clamp；修改 rich span/selection contract 时必须同步检查\n`zeta-ui::TextLayout` UTF-8 cluster geometry、renderer validation 和 `ui/README.md`。\n\n当前仍不拥有平台 URL 打开、clipboard 写入、网络/文件取图和 pointer/keyboard 事件路由；\n这些副作用由 `zeterm/zeterm` 或其他 host 绑定现有 policy/input service。尚未支持 Markdown source\n内声明的 table alignment、block-level retained cache，也尚未提供把 `MarkdownSemanticNode`\n自动写入特定平台 accessibility API 的 adapter。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与接口",
        "title": "所有权与接口"
      },
      {
        "depth": 2,
        "id": "解析信任与显示语义",
        "title": "解析、信任与显示语义"
      },
      {
        "depth": 2,
        "id": "测试修改影响与限制",
        "title": "测试、修改影响与限制"
      }
    ],
    "searchText": " 本 README 是 Native Markdown 解析、布局和 presentation 的 crate-level canonical contract。跨 crate 的 Rust Workspace 边界见 docs/zeta-rs-architecture.md；底层 scene、富文本和字体 shaping contract 见 ui/README.md。 zeta-markdown 把有资源上限的 CommonMark/GFM 输入解析为只读文档 snapshot，并通过 zeta-ui primitive 生成 Native Markdown 组件。它拥有 Markdown 结构、脚注与数学片段投影、 syntect 代码高亮、RaTeX 数学排版、安全链接策略、图片解码/布局、文字命中/选择/搜索几何、 复制文本投影、accessibility 语义树、滚动 snapshot 消费和主题 token；不拥有消息 identity、网络取图、 平台 URL opener/clipboard、输入事件分发、持久化或产品生命周期。 所有权与接口 Symbol 可见性 精确职责 --- --- --- MarkdownDocument::parse public 在 4 MiB 输入、100,000 blocks 和 64 层嵌套上限内建立不可变文档 snapshot MarkdownError public 区分输入过大、嵌套过深和 block 数超限 MarkdownLayoutEngine public 保留可复用 font shaping state，把文档、bounds、ScrollState 和 style 投影为一帧 Markdown MarkdownPresentation public 绑定 caller-retained selection、search matches 与已授权解码的图片 snapshot Markdown / MarkdownLink public 保存当前帧可见 primitive、文本命中/选择几何、语义树和 viewport-clipped link hit fragments MarkdownLinkPolicy public 解析 document fragment 或绝对 URL，拒绝 credentials/未知 scheme，并只产出策略允许的 MarkdownLinkTarget MarkdownImages / decodemarkdownimage public 接收 host 已授权取得的 bytes，在 16,777,216 pixel 上限内解码为 zetaui::ImageData；不执行 I/O MarkdownSyntaxHighlighter / SyntectMarkdownHighlighter public 定义 fenced-code byte-range 高亮 contract，并提供 bundled syntax/theme 实现 rendermarkdownmath / MarkdownMathMode public 在 64 KiB source 和 4,194,304 pixel 上限内把 inline/display LaTeX 排版并栅格化为 ImageData MarkdownSelectionController / MarkdownDocument::textforselection public 保存 pointer selection anchor/focus，并把合法范围投影为可写入 clipboard 的文本 MarkdownDocument::search public 在 copyable block text 上执行大小写敏感或 Unicode lowercase literal search MarkdownSemanticNode public 暴露 document/block/link/table-row/cell 的 role、label、level、destination 和 viewport bounds MarkdownStyle public 提供正文、标题、链接、代码、引用、列表和表格的 presentation token 与 geometry document::DocumentBuilder private 消费 pulldown-cmark event，维护 inline format、quote/list/table nesting 和当前 block table::TableBuilder private 把 table event 序列组装成保留 row/cell 边界的单个 MarkdownTable block document::pushrun private 合并相邻同样式 run，避免解析事件制造无意义的 span 碎片 inlinelayout::layoutinline private 调用 TextLayoutEngine::layoutspans，从同一次 shaping 取得 span fragments，并生成 code/link/strikethrough decoration documenttext private 规范化 block plain text、selection copy 与 literal search range；不得读取 scene geometry componentinteraction private 用同一次 shaping 的 UTF-8 cluster geometry 完成 point hit、selection/search fragments 和链接策略入口 componentpaint private 生成 rect/image/text primitive，并构造与当前 viewport geometry 一致的语义节点 math::MarkdownMathCache private 按 source、mode、颜色和字号缓存 RaTeX 结果；失败项缓存为空并由布局回退为可见 source tablelayout::layouttable private 从全部 cell 的 intrinsic width 计算列宽，再按列宽 wrapping cell，并保存 row height component::ProjectedBlock private 保存 shaping 后的单个 text/code/table/rule block 高度与宽度 Markdown::emit private 把自然文档坐标减去有效 viewport offset，剔除不可见 block 并生成 scene primitive ``text MarkdownDocument::parse └─ pulldowncmark::Parser └─ DocumentBuilder::consume ├─ inline format/link stack ├─ quote/list context ├─ TableBuilder → MarkdownTable rows/cells └─ MarkdownBlock MarkdownLayoutEngine::layout ├─ MarkdownBlock + MarkdownStyle ├─ zetaui::TextLayoutEngine │ └─ TextLayout size + per-span/per-UTF-8-cluster wrapped/BiDi visual fragments ├─ SyntectMarkdownHighlighter → fenced-code TextSpan colors ├─ MarkdownMathCache → RaTeX parse/layout/SVG → bounded RGBA ImageData ├─ MarkdownPresentation → selection/search/image snapshots ├─ layoutinline → inline backgrounds/decorations/link hit fragments ├─ layouttable → intrinsic column sizing → wrapped cell layout ├─ contentheight + clamped zetaui::ScrollState └─ Markdown::emit ├─ PaintRect: code/table/quote/rule/inline/selection/search decoration ├─ PaintImage: decoded RGBA image + inline/display typeset math ├─ MarkdownLink: viewport-clipped hit fragments ├─ MarkdownSemanticNode: document/block/link/table structure └─ TextBlock/TextSpan: wrapped rich text and highlighted code Markdown::paint ├─ zui Element Markdown viewport bounds → automatic inspection └─ UiScene::withclip viewport bounds ` MarkdownLayoutEngine 必须由 host 复用；每次 document、bounds、style 或 scroll snapshot 变化时重新 生成 immutable Markdown。Markdown 不保存 parser 或 font system，也不接受输入事件。 zetaui::ScrollState 是 product-owned retained state，组件只消费其 snapshot；wheel normalization、 scrollbar 和 input routing 继续复用 zeta-ui 的通用 scroll contract。 解析、信任与显示语义 解析启用 CommonMark、GFM block quote、table、strikethrough、task-list、footnote 和 math 扩展。Soft break 折叠为空格，hard break 保留换行；fenced code 的 info string 作为语言标签显示。表格保留 row/cell 边界，先以所有 cell 的 intrinsic width 分配整表列宽，再在各列内部独立换行；header 使用独立背景和粗体，网格线只绘制一次。 Markdown 输入是不可信文本： - raw/inline HTML 只作为可见字符串保留，不解释 DOM、脚本、attribute 或 URL； - 图片 reference 可由 host 授权取得 bytes 后交给 decodemarkdownimage；未加载时显示 alt placeholder，解析和 layout 不发起网络、文件或 data-URL I/O； - inline/display math 由 pure-Rust RaTeX 解析、排版并栅格化；非法或超限表达式保留为可见 source，不中断其余文档布局； - link destination 进入 MarkdownLink 的同源命中 geometry；只有 MarkdownLinkPolicy::evaluate 成功产生的 external MarkdownLinkTarget 才能交给 host opener；document fragment 通过 Markdown::fragmentbounds 路由到同一帧 heading/footnote geometry； - textforselection 只返回文本，真正写入系统 clipboard 仍由 platform host 执行； - crate 没有文件、网络、WebView、shell 或 platform clipboard 依赖，因此 parse/layout 不能触发外部副作用。 host 从 pointer event 调用 Markdown::activatelinkat，不能把 parser destination 直接交给 shell。图片 loader 必须先应用 product policy，再把取得的 bytes 解码并按 destination 放进 MarkdownImages。同一个 ImageId 在 renderer cache 生命周期内必须始终指向同一份 immutable pixels。 测试、修改影响与限制 `bash cargo test --manifest-path Cargo.toml -p zeta-markdown cargo clippy --manifest-path Cargo.toml -p zeta-markdown --all-targets -- -D warnings bazel test //zeterm/markdown:markdown-unit-tests ` 测试覆盖 block/inline parsing、脚注与 math event、RaTeX inline/display scene projection 和非法公式 回退、列表与任务 marker、引用 nesting、代码高亮、 表格 header/body、raw HTML、输入上限、安全 URL/fragment policy、图片有界解码与 inline/block scene projection、Unicode search、selection/copy/hit geometry、语义 heading/table/link 节点、 段落换行和 viewport clamp。修改 parser option 或 DocumentBuilder 时必须同步检查资源上限、 HTML/image 信任边界和 block fixtures；修改 ProjectedBlock/spacing 时必须同步检查 content height、offscreen culling 与 scroll clamp；修改 rich span/selection contract 时必须同步检查 zeta-ui::TextLayout UTF-8 cluster geometry、renderer validation 和 ui/README.md。 当前仍不拥有平台 URL 打开、clipboard 写入、网络/文件取图和 pointer/keyboard 事件路由； 这些副作用由 zeterm/zeterm 或其他 host 绑定现有 policy/input service。尚未支持 Markdown source 内声明的 table alignment、block-level retained cache，也尚未提供把 MarkdownSemanticNode` 自动写入特定平台 accessibility API 的 adapter。"
  },
  {
    "slug": "crates/mcp",
    "sourcePath": "zeta-rs/mcp/README.md",
    "title": "zeta-mcp",
    "description": "本 crate 把一组已经过宿主解析和授权的 MCP server definition 变成隔离 session、不可变工具 catalog、精确 binding 和可取消调用。它不读取 Config 或 Plugin，不解析 credential reference， 不审批工具调用，也不写 Core Thread。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 MCP 产品运行时当前实现的 canonical 文档。跨 crate 产品语义、授权、\n> durability 与演进阶段见 [`../../docs/mcp.md`](../../docs/mcp.md)；单连接协议和 transport\n> 实现见 [`../rmcp-client/README.md`](../rmcp-client/README.md)。\n\n本 crate 把一组已经过宿主解析和授权的 MCP server definition 变成隔离 session、不可变工具\ncatalog、精确 binding 和可取消调用。它不读取 Config 或 Plugin，不解析 credential reference，\n不审批工具调用，也不写 Core Thread。\n\n## 边界与文件\n\n| 文件 / symbol | 当前职责 |\n| --- | --- |\n| `definition.rs` / `McpServerDefinition` | 接收宿主已 materialize 的 stdio 或 Streamable HTTP transport |\n| `session.rs` / `McpSession` | product runtime 使用的单 session 窄接口 |\n| `session.rs` / `McpSessionFactory` | transport/session 创建扩展点；production 由 `RmcpSessionFactory` 实现 |\n| `catalog.rs` / `McpCatalogSnapshot` | 保存跨 server、不可变、按 generation 冻结的工具视图 |\n| `catalog.rs` / `McpToolBinding` | 保存 model alias、exact remote identity、definition digest 和两个 generation |\n| `runtime.rs` / `McpRuntime` | 启动 server set、发现 catalog、路由调用、观察失效和有界 shutdown |\n| `output.rs` / `project_tool_result` | 把不可信 RMCP result 投影到 `zeta-tools::ToolOutput` |\n| `error.rs` | 区分构造失败、未开始、结果未知和无效结果 |\n\n依赖方向固定为：\n\n```text\nzeta-mcp\n  → zeta-rmcp-client\n  → zeta-tools / zeta-protocol\n  → zeta-config::McpServerId\n```\n\n`zeta-mcp` 不应依赖 `zeta-core`、App Server、stores、Plugin manager 或 secret store。若本 crate\n开始读取用户配置、决定 approval 或追加 Thread event，即表示 ownership 已漂移。\n\n## 公共契约\n\n宿主先完成 enablement、trust、可执行文件解析和 credential materialization，再构造\n`McpServerDefinition`。`McpRuntime::start` 使用 production RMCP factory；\n`start_with_factory` 允许测试或宿主注入隔离 session。\n\n`McpStartupPolicy::RequireAll` 在任一 server 启动或 catalog 失败时关闭已启动 session 并整体失败；\n`AllowPartial` 保留健康 server，并通过 `McpStartupDiagnostic` 返回脱敏诊断。重复 server ID 永远\n拒绝。\n\n每个 `McpToolBinding` 同时固定：\n\n- model-visible `ToolName`；\n- `McpServerId + exact remote tool name`；\n- definition digest；\n- connection generation；\n- catalog generation。\n\n`McpRuntime::call_tool` 不按 remote name 重新搜索。binding 不属于当前 snapshot 时在发送前返回\n`NotStarted`，防止旧 catalog 劫持新连接。调用方应从 `catalog()` 获取 definition/binding，并在\nCore safe point 以整个新 runtime 替换旧 runtime。\n\n## 内部调用与校验\n\n```text\nMcpRuntime::start[_with_factory]\n→ reject_duplicate_servers\n→ McpSessionFactory::connect\n→ discover_server_tools\n   → McpSession::list_tools\n   → project_tool\n   → zeta_tools::from_mcp_tool_projection\n→ McpCatalogSnapshot::new\n\nMcpRuntime::call_tool\n→ 校验 cancellation、snapshot binding 和 object arguments\n→ McpSession::call_tool\n→ project_tool_result\n```\n\n`discover_server_tools` 是 catalog 的关键 private owner：它限制 page 数、每 server tool 数和序列化\nbyte 数，拒绝空/重复 cursor 与重复 remote tool name。`exposed_name` 对\n`server ID + NUL + exact remote name` 做 SHA-256，并生成带 12 个 hex 字符后缀的稳定合法 alias；\n可读 slug 不是 identity。\n\n`RuntimeClientHost` 拦截 `ToolListChanged` 并把该 server 标成 `Stale`，同时继续转发 progress、\nserver cancellation 和 elicitation。它不会原地修改 catalog；宿主必须在 safe point 重建\nruntime。\n\n`project_tool_result` 保留 text 和合法 `image/*` block，将其他 RMCP block 序列化为 text，并对\n最终 text/data URL 按 UTF-8 byte 数执行 output limit。MCP `isError` 映射为\n`ToolOutputStatus::Error`，不是 transport failure。\n\n## 失败、取消与恢复\n\n- 调用前 cancellation、非 object arguments 和 stale binding：`McpCallError::NotStarted`。\n- 请求发送后的 cancellation、timeout、断线或协议错误：\n  `McpCallError::OutcomeUncertain`；上层不得自动重放有副作用调用。\n- 无法安全投影或超过 output limit：`McpCallError::InvalidResult`。\n- `McpRuntime::shutdown(self)` 消费 runtime，逐 server shutdown 并返回失败诊断。\n\n`McpSession` 实现必须将 cancellation 传入 transport；production adapter 使用\n`RmcpClient::call_tool_with_cancellation` 发送 MCP cancellation notification。取消只停止本地等待\n和尽力通知 server，不证明远端副作用未发生。\n\n## 接入义务\n\nApp Server adapter 当前已负责 tools vertical slice 的：\n\n- 从 user `zeta-config` snapshot 选择已启用 server；\n- materialize absolute stdio executable 与 unauthenticated HTTP endpoint；\n- 把 `catalog().model_definitions()` 与 frozen binding 接入 Core `ToolService`；\n- 执行 approval、durable Tool Call/Result、unknown-outcome recovery；\n\nApp Server 尚未负责 Plugin/workspace trust、credential materialization、sandboxed stdio launcher、\nconfig/list-changed 后的自动 safe-point rebuild，以及 progress/elicitation/diagnostic delivery。\n\n## 测试与修改影响\n\n```text\ncargo test -p zeta-mcp\ncargo clippy -p zeta-mcp --all-targets -- -D warnings\nbazel test //zeta-rs/mcp:mcp-unit-tests\n```\n\n`catalog_tests.rs` 覆盖 alias 的合法性、边界和 exact identity；`runtime_tests.rs` 使用 fake\nfactory 覆盖多 server catalog、冻结 binding 路由、部分启动、失效通知和旧 generation 拒绝；\n`output_tests.rs` 覆盖 result limit 与 remote error status。修改 alias/hash、limit、binding 字段、\nstartup policy、cancellation 或 output projection 时，必须同步检查这些测试、本 README 和\n[`../../docs/mcp.md`](../../docs/mcp.md)。\n\n## 当前限制与扩展点\n\n- Current：tools 的多 server discovery/call 已实现；resources、prompts、roots、sampling 和\n  tasks 尚未进入 product runtime。\n- Current：list-changed 只标记 stale；自动 reconnect、backoff、health state machine 和 catalog\n  replacement 由后续 host lifecycle 实现；stale server 的新调用会 fail closed。\n- Current：production factory 可连接 direct stdio 与 unauthenticated/bearer Streamable HTTP；\n  sandboxed launcher、OAuth 和 credential refresh 需由宿主注入。\n- Current：App Server/Core tools adapter、one-time approval 与 durable result 已接入；runtime\n  snapshot 只在 App Server 启动时构造，且 App Server 当前采用 `RequireAll` startup。\n- Extension point：实现 `McpSessionFactory` 可注入受 sandbox 管理的 process、远端执行或自定义\n  HTTP stack，而不改变 catalog/binding 语义。",
    "headings": [
      {
        "depth": 2,
        "id": "边界与文件",
        "title": "边界与文件"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部调用与校验",
        "title": "内部调用与校验"
      },
      {
        "depth": 2,
        "id": "失败取消与恢复",
        "title": "失败、取消与恢复"
      },
      {
        "depth": 2,
        "id": "接入义务",
        "title": "接入义务"
      },
      {
        "depth": 2,
        "id": "测试与修改影响",
        "title": "测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 MCP 产品运行时当前实现的 canonical 文档。跨 crate 产品语义、授权、 durability 与演进阶段见 ../../docs/mcp.md；单连接协议和 transport 实现见 ../rmcp-client/README.md。 本 crate 把一组已经过宿主解析和授权的 MCP server definition 变成隔离 session、不可变工具 catalog、精确 binding 和可取消调用。它不读取 Config 或 Plugin，不解析 credential reference， 不审批工具调用，也不写 Core Thread。 边界与文件 文件 / symbol 当前职责 --- --- definition.rs / McpServerDefinition 接收宿主已 materialize 的 stdio 或 Streamable HTTP transport session.rs / McpSession product runtime 使用的单 session 窄接口 session.rs / McpSessionFactory transport/session 创建扩展点；production 由 RmcpSessionFactory 实现 catalog.rs / McpCatalogSnapshot 保存跨 server、不可变、按 generation 冻结的工具视图 catalog.rs / McpToolBinding 保存 model alias、exact remote identity、definition digest 和两个 generation runtime.rs / McpRuntime 启动 server set、发现 catalog、路由调用、观察失效和有界 shutdown output.rs / projecttoolresult 把不可信 RMCP result 投影到 zeta-tools::ToolOutput error.rs 区分构造失败、未开始、结果未知和无效结果 依赖方向固定为： ``text zeta-mcp → zeta-rmcp-client → zeta-tools / zeta-protocol → zeta-config::McpServerId ` zeta-mcp 不应依赖 zeta-core、App Server、stores、Plugin manager 或 secret store。若本 crate 开始读取用户配置、决定 approval 或追加 Thread event，即表示 ownership 已漂移。 公共契约 宿主先完成 enablement、trust、可执行文件解析和 credential materialization，再构造 McpServerDefinition。McpRuntime::start 使用 production RMCP factory； startwithfactory 允许测试或宿主注入隔离 session。 McpStartupPolicy::RequireAll 在任一 server 启动或 catalog 失败时关闭已启动 session 并整体失败； AllowPartial 保留健康 server，并通过 McpStartupDiagnostic 返回脱敏诊断。重复 server ID 永远 拒绝。 每个 McpToolBinding 同时固定： - model-visible ToolName； - McpServerId + exact remote tool name； - definition digest； - connection generation； - catalog generation。 McpRuntime::calltool 不按 remote name 重新搜索。binding 不属于当前 snapshot 时在发送前返回 NotStarted，防止旧 catalog 劫持新连接。调用方应从 catalog 获取 definition/binding，并在 Core safe point 以整个新 runtime 替换旧 runtime。 内部调用与校验 `text McpRuntime::start withfactory → rejectduplicateservers → McpSessionFactory::connect → discoverservertools → McpSession::listtools → projecttool → zetatools::frommcptoolprojection → McpCatalogSnapshot::new McpRuntime::calltool → 校验 cancellation、snapshot binding 和 object arguments → McpSession::calltool → projecttoolresult ` discoverservertools 是 catalog 的关键 private owner：它限制 page 数、每 server tool 数和序列化 byte 数，拒绝空/重复 cursor 与重复 remote tool name。exposedname 对 server ID + NUL + exact remote name 做 SHA-256，并生成带 12 个 hex 字符后缀的稳定合法 alias； 可读 slug 不是 identity。 RuntimeClientHost 拦截 ToolListChanged 并把该 server 标成 Stale，同时继续转发 progress、 server cancellation 和 elicitation。它不会原地修改 catalog；宿主必须在 safe point 重建 runtime。 projecttoolresult 保留 text 和合法 image/ block，将其他 RMCP block 序列化为 text，并对 最终 text/data URL 按 UTF-8 byte 数执行 output limit。MCP isError 映射为 ToolOutputStatus::Error，不是 transport failure。 失败、取消与恢复 - 调用前 cancellation、非 object arguments 和 stale binding：McpCallError::NotStarted。 - 请求发送后的 cancellation、timeout、断线或协议错误： McpCallError::OutcomeUncertain；上层不得自动重放有副作用调用。 - 无法安全投影或超过 output limit：McpCallError::InvalidResult。 - McpRuntime::shutdown self 消费 runtime，逐 server shutdown 并返回失败诊断。 McpSession 实现必须将 cancellation 传入 transport；production adapter 使用 RmcpClient::calltoolwithcancellation 发送 MCP cancellation notification。取消只停止本地等待 和尽力通知 server，不证明远端副作用未发生。 接入义务 App Server adapter 当前已负责 tools vertical slice 的： - 从 user zeta-config snapshot 选择已启用 server； - materialize absolute stdio executable 与 unauthenticated HTTP endpoint； - 把 catalog .modeldefinitions 与 frozen binding 接入 Core ToolService； - 执行 approval、durable Tool Call/Result、unknown-outcome recovery； App Server 尚未负责 Plugin/workspace trust、credential materialization、sandboxed stdio launcher、 config/list-changed 后的自动 safe-point rebuild，以及 progress/elicitation/diagnostic delivery。 测试与修改影响 `text cargo test -p zeta-mcp cargo clippy -p zeta-mcp --all-targets -- -D warnings bazel test //zeta-rs/mcp:mcp-unit-tests ` catalogtests.rs 覆盖 alias 的合法性、边界和 exact identity；runtimetests.rs 使用 fake factory 覆盖多 server catalog、冻结 binding 路由、部分启动、失效通知和旧 generation 拒绝； outputtests.rs 覆盖 result limit 与 remote error status。修改 alias/hash、limit、binding 字段、 startup policy、cancellation 或 output projection 时，必须同步检查这些测试、本 README 和 ../../docs/mcp.md。 当前限制与扩展点 - Current：tools 的多 server discovery/call 已实现；resources、prompts、roots、sampling 和 tasks 尚未进入 product runtime。 - Current：list-changed 只标记 stale；自动 reconnect、backoff、health state machine 和 catalog replacement 由后续 host lifecycle 实现；stale server 的新调用会 fail closed。 - Current：production factory 可连接 direct stdio 与 unauthenticated/bearer Streamable HTTP； sandboxed launcher、OAuth 和 credential refresh 需由宿主注入。 - Current：App Server/Core tools adapter、one-time approval 与 durable result 已接入；runtime snapshot 只在 App Server 启动时构造，且 App Server 当前采用 RequireAll startup。 - Extension point：实现 McpSessionFactory` 可注入受 sandbox 管理的 process、远端执行或自定义 HTTP stack，而不改变 catalog/binding 语义。"
  },
  {
    "slug": "crates/mcp-server",
    "sourcePath": "zeta-rs/mcp-server/README.md",
    "title": "zeta-mcp-server",
    "description": "zeta-mcp-server 通过 stdio 或已认证的 Streamable HTTP，把 Zeta Agent 执行能力暴露给 MCP 客户端。它是 zeta-app-server-client 上方的适配器；嵌入式 App Server 仍是唯一组合根，也是 Session、Thread、Turn、模型、工具、策略、工作区和持久化状态的权威所有者。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责当前 crate 的实现契约。跨 crate 产品架构和分阶段演进见\n> [`docs/mcp-server.md`](../../docs/mcp-server.md)；反方向的 MCP 客户端运行时见\n> [`docs/mcp.md`](../../docs/mcp.md)。\n\n`zeta-mcp-server` 通过 stdio 或已认证的 Streamable HTTP，把 Zeta Agent 执行能力暴露给 MCP\n客户端。它是 `zeta-app-server-client` 上方的适配器；嵌入式 App Server 仍是唯一组合根，也是\nSession、Thread、Turn、模型、工具、策略、工作区和持久化状态的权威所有者。\n\n## 1. 当前接口面\n\n启动本地 stdio 服务：\n\n```text\nzeta-mcp-server\nzeta mcp-server\nzeta mcp-server --listen stdio://\n```\n\n启动 HTTP 端点：\n\n```text\nZETA_MCP_BEARER_TOKEN=<at-least-32-visible-ASCII-characters> \\\n  zeta mcp-server --listen http://127.0.0.1:8787/mcp\n```\n\n两个入口都读取 `ZETA_PROFILE_ROOT` 和 `ZETA_WORKSPACE_ROOT`；前者默认使用操作系统用户 state\n目录，后者默认使用进程工作目录。HTTP 还必须提供 `ZETA_MCP_BEARER_TOKEN`；\n`ZETA_MCP_ALLOWED_ORIGIN` 可以额外允许一个\n精确浏览器来源。内置监听器只提供普通 HTTP，远程部署时应放在已认证的 TLS 反向代理之后。\n\n服务实现 MCP `2025-11-25`：\n\n| 方法 | 当前行为 |\n| --- | --- |\n| `initialize` | 公布工具能力，并协商表单询问支持 |\n| `ping` | 返回空结果 |\n| `tools/list` | 返回 `zeta` 和 `zeta-reply` |\n| `tools/call` | 启动或继续一个 App Server Thread |\n| `notifications/cancelled` | 请求中断相关 Turn |\n| 服务端 `elicitation/create` | 把批准和用户输入请求转发给支持该能力的客户端 |\n| `notifications/progress` | 投影数量受限且已脱敏的 Turn 生命周期更新 |\n\n`zeta` 要求调用方生成 `invocationId` 并提供提示词。`zeta-reply` 要求新的 `invocationId`、同一\n主体已获授权的 `threadId` 和提示词。两者都在 `structuredContent` 中返回稳定 Zeta 身份、\n终态或阻塞状态，以及长度受限的内容。\n\n## 2. Crate 边界\n\n本 crate 拥有：\n\n- MCP 分帧、初始化门控、工具模式和线协议校验；\n- stdio 生命周期以及 Streamable HTTP 端点和会话生命周期；\n- Bearer 认证、精确 Origin 校验和单进程连接数量限制；\n- 请求取消与进度令牌关联；\n- App Server 交互到 MCP 表单询问的投影；\n- 按主体隔离的持久化调用回执，以及调用方到 Thread 的绑定；\n- App Server 结果投影和输出截断。\n\n本 crate 不拥有：\n\n- Session、Thread、Turn 的状态迁移或存储；\n- 模型、工具、策略、沙箱、凭据或工作区权威；\n- 本地父子委托；\n- App Server 协议定义；\n- OAuth、租户配置、TLS 终止或远程 App Server 后端。\n\n如果本 crate 直接打开产品存储、构造 `TurnExecutor`、调用供应商或写入 Thread 事件，就说明\n架构所有权已经漂移。这些操作必须继续位于 App Server 客户端之后。\n\n## 3. 模块与关键符号\n\n| 模块/符号 | 职责 | 不得吸收 |\n| --- | --- | --- |\n| `lib.rs::{run_stdio,run_http}` | 校验选项，打开一个嵌入式 App Server 宿主和回执存储 | 协议分发或 Agent 状态 |\n| `options.rs::{McpServerOptions,HttpServerOptions}` | 宿主拥有的根目录、运行时限制和 HTTP 安全配置 | 调用方权限覆盖 |\n| `server.rs::McpServer` | 初始化门控、JSON-RPC 分发和活动调用取消 | Session/Thread 业务逻辑 |\n| `server/events.rs::McpAgentEvents` | 进度和询问的线协议投影 | 策略决策 |\n| `http.rs` / `http/wire.rs` | 已认证端点、MCP 会话和 SSE 分帧 | 持久化 Agent 权威 |\n| `protocol.rs` | 线协议数据结构、工具模式和输入限制 | 直接透传 App Server 数据结构 |\n| `agent.rs::AgentService` | 窄且可测试的 Agent 执行边界 | 模型/工具实现 |\n| `agent.rs::AppServerAgentService` | 把启动/回复映射到类型化 App Server 调用和精确 Turn 更新 | 直接访问 Core/存储 |\n| `agent/progress.rs` | 脱敏并限制供 MCP 进度使用的 Thread 更新 | 完整会话记录投影 |\n| `interaction.rs` | 类型化批准/用户输入和 MCP 表单询问映射 | 自动批准 |\n| `receipt.rs::ReceiptStore` | 按主体隔离的重放、单航班执行和 Thread 授权 | 产品会话记录存储 |\n| `agent/outcome.rs` | 终态/等待中 Turn 投影 | Thread 修改或 MCP 分帧 |\n\n当前调用路径：\n\n```text\nrun_stdio 或 run_http\n→ open_in_process_app_server\n→ 创建按主体隔离的 AppServerAgentService\n→ McpServer::handle_message\n→ tools/call\n→ session/create 或已授权的 session/thread/read\n→ 启动时通过 session/request { type: createThread }\n→ session/thread/subscribe + session/request { type: startTurn }\n→ 有界 session/thread/read 轮询和通知排空\n→ progress 和可选 elicitation/create\n→ 接受后调用 session/request { type: resolveInteraction }\n→ 有界 MCP CallToolResult\n```\n\n每个 HTTP MCP 会话获得独立的 App Server 连接，但共享同一个嵌入式 App Server 宿主和回执权威。\n`agent/progress.rs::committed` 只选择 MCP 明确拥有的 Turn 生命周期和交互事件；其他\n`ThreadEvent` 统一不进入进度投影。新增委托、上下文或其他 canonical event 不应迫使 MCP\n适配器逐项声明忽略，只有新的 MCP 可见语义才修改该 projection。\n\n## 4. 校验与限制\n\n- `invocationId`：1–128 个 ASCII 字母、数字、`.`、`_` 或 `-`；\n- 提示词：非空，最大 256 KiB；\n- 默认 Turn 超时：60 秒；\n- 调用方可请求的最大 Turn 超时：10 分钟；\n- 轮询间隔：10 毫秒；\n- MCP 工具结果内容：最大 256 KiB，包含截断标记；\n- 进度：每次调用最多 256 条生命周期通知，并移除连续重复项；\n- HTTP 请求体：最大 1 MiB；标头：最大 32 KiB；\n- 默认最大 HTTP 连接数：64；\n- 每个 MCP 会话最多保留 1024 个提前取消身份。\n\n服务不通过工具参数接受工作区路径、原始配置映射、秘密、开发者指令或沙箱覆盖。工作区和执行\n权威由宿主固定。\n\n## 5. 身份、恢复与继续\n\n`invocationId` 与 MCP JSON-RPC 请求 ID 以及所有 Zeta 产品身份互相独立。适配器为 Session、\nThread、Turn、交互解决和取消派生按主体命名空间隔离的稳定 App Server 命令 ID。\n\n回执以 SQLite 事务持久化到 `<ZETA_PROFILE_ROOT>/state.sqlite3` 的\n`mcp_invocation_receipts` / `mcp_thread_bindings`，并按主体隔离：\n\n- stdio 使用本地用户主体；\n- HTTP 从 Bearer 令牌派生不可逆主体标识；\n- 参数相同的已完成调用会重放已保存结果；\n- 同一身份配不同参数返回冲突；\n- 并发重复调用返回执行中；\n- 进程失败留下的运行中调用在重启后重新进入相同的确定性 App Server 命令，不分配新产品身份；\n- `zeta-reply` 只接受持久绑定到同一主体的 Thread。\n\n回执表是适配器恢复索引，不是权威 Agent 状态。Session、Thread、Turn 状态仍在 App Server\nstore tables 中。SQLite 支持同一 profile 的多 connection 原子提交，但 active single-flight set\n仍为进程内；一个 profile 当前只能由一个 MCP Server runtime 使用，分布式执行租约尚未实现。\n\n等待交互的结果保持可恢复，不会被封存为已完成。使用相同调用身份重试时，即使进程重启，也能\n继续精确的未完成交互。\n\n## 6. 进度、交互与失败\n\n调用方提供 `_meta.progressToken` 时，服务以同一精确令牌发出单调递增的\n`notifications/progress`。消息只描述数量受限的生命周期状态，不暴露推理、提示词、工具参数\n与结果、凭据或环境数据。\n\n只有初始化后的客户端声明支持表单询问时，批准和用户输入请求才映射为 MCP\n`elicitation/create`。接受的响应会转换回精确的类型化 App Server 请求身份，并通过\n`session/request` 的 `ResolveInteraction` 发送。拒绝、取消或客户端不支持时返回阻塞的工具结果，绝不自动\n批准。看起来在索要凭据或其他敏感值的用户输入问题不会通过表单询问发送。动态工具定义的交互\n类型尚未投影。\n\n长调用期间 stdio 继续读取，因此 `notifications/cancelled` 可以中断精确 Turn。stdio EOF\n会取消活动工作，因为进程拥有该连接。HTTP/SSE 写入失败不能证明持久化 Turn 已取消；显式\nMCP 取消仍是权威。客户端取消的请求不会返回 JSON-RPC 响应。\n\n取消有两秒宽限期。服务端截止时间到达后，如果 Turn 没有进入权威终态，结果为\n`outcomeUnknown`。无效 JSON-RPC 或协议请求使用 JSON-RPC 错误；工具参数、App Server 和\nAgent 结果失败使用 `CallToolResult.isError`。\n\n## 7. Streamable HTTP 安全性与生命周期\n\nHTTP 端点：\n\n- 接受 MCP 消息的 `POST`，返回 JSON 或 SSE；\n- 对通知和客户端响应返回 `202 Accepted`；\n- 初始化成功后使用安全随机 `MCP-Session-Id`；\n- 后续请求必须提供 `MCP-Protocol-Version: 2025-11-25`；\n- 支持通过 `DELETE` 终止会话；\n- 独立 `GET` 流返回 `405`；\n- 提供 Origin 标头时，必须精确匹配已配置值；\n- 比较 Bearer 凭据时不使用提前退出的字符串比较。\n\nHTTP 会话状态有意保持为进程本地状态。服务重启后，客户端重新初始化 MCP 会话，并以相同的\n持久化 `invocationId` 重试。独立 GET SSE 流、`Last-Event-ID` 重投递、OAuth、多租户工作区\n绑定和内置 TLS 均未实现。\n\n## 8. 测试与扩展点\n\n相邻单元和集成测试覆盖协议校验、进度令牌、询问身份、真实 App Server 启动/回复/进度、持久化\n回执重开、HTTP 认证/Origin/会话/协议/SSE、取消和截断。`tests/stdio.rs` 启动真实二进制，\n验证实时进度、重启重放以及重启后的 `zeta-reply`。\n\n```text\ncargo test -p zeta-mcp-server\ncargo clippy -p zeta-mcp-server --all-targets -- -D warnings\n```\n\n当前扩展点与限制：\n\n- 仍使用同步 App Server 请求和有界 Thread 轮询/排空；共享\n  `AppServerSession` 已由 `zeta-app-server-client` 提供并服务 CLI/TUI，MCP adapter 的 event-driven\n  迁移尚未完成；\n- 不支持资源、提示词、根目录、采样或 MCP task 能力；\n- 超大输出没有产物引用；\n- 不支持动态工具交互投影；\n- 不支持 HTTP 事件重放、OAuth/租户控制面、内置 TLS 或远程 App Server 后端；\n- 没有原生远程 Agent 到 `DelegationId` 的桥接。\n\n修改工具模式、限制、状态映射、回执身份、HTTP 安全或继续授权时，必须同步更新样例、测试、\n本 README 和 [`docs/mcp-server.md`](../../docs/mcp-server.md)。",
    "headings": [
      {
        "depth": 2,
        "id": "1-当前接口面",
        "title": "1. 当前接口面"
      },
      {
        "depth": 2,
        "id": "2-crate-边界",
        "title": "2. Crate 边界"
      },
      {
        "depth": 2,
        "id": "3-模块与关键符号",
        "title": "3. 模块与关键符号"
      },
      {
        "depth": 2,
        "id": "4-校验与限制",
        "title": "4. 校验与限制"
      },
      {
        "depth": 2,
        "id": "5-身份恢复与继续",
        "title": "5. 身份、恢复与继续"
      },
      {
        "depth": 2,
        "id": "6-进度交互与失败",
        "title": "6. 进度、交互与失败"
      },
      {
        "depth": 2,
        "id": "7-streamable-http-安全性与生命周期",
        "title": "7. Streamable HTTP 安全性与生命周期"
      },
      {
        "depth": 2,
        "id": "8-测试与扩展点",
        "title": "8. 测试与扩展点"
      }
    ],
    "searchText": " 本 README 负责当前 crate 的实现契约。跨 crate 产品架构和分阶段演进见 docs/mcp-server.md；反方向的 MCP 客户端运行时见 docs/mcp.md。 zeta-mcp-server 通过 stdio 或已认证的 Streamable HTTP，把 Zeta Agent 执行能力暴露给 MCP 客户端。它是 zeta-app-server-client 上方的适配器；嵌入式 App Server 仍是唯一组合根，也是 Session、Thread、Turn、模型、工具、策略、工作区和持久化状态的权威所有者。 1. 当前接口面 启动本地 stdio 服务： ``text zeta-mcp-server zeta mcp-server zeta mcp-server --listen stdio:// ` 启动 HTTP 端点： `text ZETAMCPBEARERTOKEN=<at-least-32-visible-ASCII-characters \\ zeta mcp-server --listen http://127.0.0.1:8787/mcp ` 两个入口都读取 ZETAPROFILEROOT 和 ZETAWORKSPACEROOT；前者默认使用操作系统用户 state 目录，后者默认使用进程工作目录。HTTP 还必须提供 ZETAMCPBEARERTOKEN； ZETAMCPALLOWEDORIGIN 可以额外允许一个 精确浏览器来源。内置监听器只提供普通 HTTP，远程部署时应放在已认证的 TLS 反向代理之后。 服务实现 MCP 2025-11-25： 方法 当前行为 --- --- initialize 公布工具能力，并协商表单询问支持 ping 返回空结果 tools/list 返回 zeta 和 zeta-reply tools/call 启动或继续一个 App Server Thread notifications/cancelled 请求中断相关 Turn 服务端 elicitation/create 把批准和用户输入请求转发给支持该能力的客户端 notifications/progress 投影数量受限且已脱敏的 Turn 生命周期更新 zeta 要求调用方生成 invocationId 并提供提示词。zeta-reply 要求新的 invocationId、同一 主体已获授权的 threadId 和提示词。两者都在 structuredContent 中返回稳定 Zeta 身份、 终态或阻塞状态，以及长度受限的内容。 2. Crate 边界 本 crate 拥有： - MCP 分帧、初始化门控、工具模式和线协议校验； - stdio 生命周期以及 Streamable HTTP 端点和会话生命周期； - Bearer 认证、精确 Origin 校验和单进程连接数量限制； - 请求取消与进度令牌关联； - App Server 交互到 MCP 表单询问的投影； - 按主体隔离的持久化调用回执，以及调用方到 Thread 的绑定； - App Server 结果投影和输出截断。 本 crate 不拥有： - Session、Thread、Turn 的状态迁移或存储； - 模型、工具、策略、沙箱、凭据或工作区权威； - 本地父子委托； - App Server 协议定义； - OAuth、租户配置、TLS 终止或远程 App Server 后端。 如果本 crate 直接打开产品存储、构造 TurnExecutor、调用供应商或写入 Thread 事件，就说明 架构所有权已经漂移。这些操作必须继续位于 App Server 客户端之后。 3. 模块与关键符号 模块/符号 职责 不得吸收 --- --- --- lib.rs:: runstdio,runhttp 校验选项，打开一个嵌入式 App Server 宿主和回执存储 协议分发或 Agent 状态 options.rs:: McpServerOptions,HttpServerOptions 宿主拥有的根目录、运行时限制和 HTTP 安全配置 调用方权限覆盖 server.rs::McpServer 初始化门控、JSON-RPC 分发和活动调用取消 Session/Thread 业务逻辑 server/events.rs::McpAgentEvents 进度和询问的线协议投影 策略决策 http.rs / http/wire.rs 已认证端点、MCP 会话和 SSE 分帧 持久化 Agent 权威 protocol.rs 线协议数据结构、工具模式和输入限制 直接透传 App Server 数据结构 agent.rs::AgentService 窄且可测试的 Agent 执行边界 模型/工具实现 agent.rs::AppServerAgentService 把启动/回复映射到类型化 App Server 调用和精确 Turn 更新 直接访问 Core/存储 agent/progress.rs 脱敏并限制供 MCP 进度使用的 Thread 更新 完整会话记录投影 interaction.rs 类型化批准/用户输入和 MCP 表单询问映射 自动批准 receipt.rs::ReceiptStore 按主体隔离的重放、单航班执行和 Thread 授权 产品会话记录存储 agent/outcome.rs 终态/等待中 Turn 投影 Thread 修改或 MCP 分帧 当前调用路径： `text runstdio 或 runhttp → openinprocessappserver → 创建按主体隔离的 AppServerAgentService → McpServer::handlemessage → tools/call → session/create 或已授权的 session/thread/read → 启动时通过 session/request type: createThread → session/thread/subscribe + session/request type: startTurn → 有界 session/thread/read 轮询和通知排空 → progress 和可选 elicitation/create → 接受后调用 session/request type: resolveInteraction → 有界 MCP CallToolResult ` 每个 HTTP MCP 会话获得独立的 App Server 连接，但共享同一个嵌入式 App Server 宿主和回执权威。 agent/progress.rs::committed 只选择 MCP 明确拥有的 Turn 生命周期和交互事件；其他 ThreadEvent 统一不进入进度投影。新增委托、上下文或其他 canonical event 不应迫使 MCP 适配器逐项声明忽略，只有新的 MCP 可见语义才修改该 projection。 4. 校验与限制 - invocationId：1–128 个 ASCII 字母、数字、.、 或 -； - 提示词：非空，最大 256 KiB； - 默认 Turn 超时：60 秒； - 调用方可请求的最大 Turn 超时：10 分钟； - 轮询间隔：10 毫秒； - MCP 工具结果内容：最大 256 KiB，包含截断标记； - 进度：每次调用最多 256 条生命周期通知，并移除连续重复项； - HTTP 请求体：最大 1 MiB；标头：最大 32 KiB； - 默认最大 HTTP 连接数：64； - 每个 MCP 会话最多保留 1024 个提前取消身份。 服务不通过工具参数接受工作区路径、原始配置映射、秘密、开发者指令或沙箱覆盖。工作区和执行 权威由宿主固定。 5. 身份、恢复与继续 invocationId 与 MCP JSON-RPC 请求 ID 以及所有 Zeta 产品身份互相独立。适配器为 Session、 Thread、Turn、交互解决和取消派生按主体命名空间隔离的稳定 App Server 命令 ID。 回执以 SQLite 事务持久化到 <ZETAPROFILEROOT /state.sqlite3 的 mcpinvocationreceipts / mcpthreadbindings，并按主体隔离： - stdio 使用本地用户主体； - HTTP 从 Bearer 令牌派生不可逆主体标识； - 参数相同的已完成调用会重放已保存结果； - 同一身份配不同参数返回冲突； - 并发重复调用返回执行中； - 进程失败留下的运行中调用在重启后重新进入相同的确定性 App Server 命令，不分配新产品身份； - zeta-reply 只接受持久绑定到同一主体的 Thread。 回执表是适配器恢复索引，不是权威 Agent 状态。Session、Thread、Turn 状态仍在 App Server store tables 中。SQLite 支持同一 profile 的多 connection 原子提交，但 active single-flight set 仍为进程内；一个 profile 当前只能由一个 MCP Server runtime 使用，分布式执行租约尚未实现。 等待交互的结果保持可恢复，不会被封存为已完成。使用相同调用身份重试时，即使进程重启，也能 继续精确的未完成交互。 6. 进度、交互与失败 调用方提供 meta.progressToken 时，服务以同一精确令牌发出单调递增的 notifications/progress。消息只描述数量受限的生命周期状态，不暴露推理、提示词、工具参数 与结果、凭据或环境数据。 只有初始化后的客户端声明支持表单询问时，批准和用户输入请求才映射为 MCP elicitation/create。接受的响应会转换回精确的类型化 App Server 请求身份，并通过 session/request 的 ResolveInteraction 发送。拒绝、取消或客户端不支持时返回阻塞的工具结果，绝不自动 批准。看起来在索要凭据或其他敏感值的用户输入问题不会通过表单询问发送。动态工具定义的交互 类型尚未投影。 长调用期间 stdio 继续读取，因此 notifications/cancelled 可以中断精确 Turn。stdio EOF 会取消活动工作，因为进程拥有该连接。HTTP/SSE 写入失败不能证明持久化 Turn 已取消；显式 MCP 取消仍是权威。客户端取消的请求不会返回 JSON-RPC 响应。 取消有两秒宽限期。服务端截止时间到达后，如果 Turn 没有进入权威终态，结果为 outcomeUnknown。无效 JSON-RPC 或协议请求使用 JSON-RPC 错误；工具参数、App Server 和 Agent 结果失败使用 CallToolResult.isError。 7. Streamable HTTP 安全性与生命周期 HTTP 端点： - 接受 MCP 消息的 POST，返回 JSON 或 SSE； - 对通知和客户端响应返回 202 Accepted； - 初始化成功后使用安全随机 MCP-Session-Id； - 后续请求必须提供 MCP-Protocol-Version: 2025-11-25； - 支持通过 DELETE 终止会话； - 独立 GET 流返回 405； - 提供 Origin 标头时，必须精确匹配已配置值； - 比较 Bearer 凭据时不使用提前退出的字符串比较。 HTTP 会话状态有意保持为进程本地状态。服务重启后，客户端重新初始化 MCP 会话，并以相同的 持久化 invocationId 重试。独立 GET SSE 流、Last-Event-ID 重投递、OAuth、多租户工作区 绑定和内置 TLS 均未实现。 8. 测试与扩展点 相邻单元和集成测试覆盖协议校验、进度令牌、询问身份、真实 App Server 启动/回复/进度、持久化 回执重开、HTTP 认证/Origin/会话/协议/SSE、取消和截断。tests/stdio.rs 启动真实二进制， 验证实时进度、重启重放以及重启后的 zeta-reply。 `text cargo test -p zeta-mcp-server cargo clippy -p zeta-mcp-server --all-targets -- -D warnings ` 当前扩展点与限制： - 仍使用同步 App Server 请求和有界 Thread 轮询/排空；共享 AppServerSession 已由 zeta-app-server-client 提供并服务 CLI/TUI，MCP adapter 的 event-driven 迁移尚未完成； - 不支持资源、提示词、根目录、采样或 MCP task 能力； - 超大输出没有产物引用； - 不支持动态工具交互投影； - 不支持 HTTP 事件重放、OAuth/租户控制面、内置 TLS 或远程 App Server 后端； - 没有原生远程 Agent 到 DelegationId 的桥接。 修改工具模式、限制、状态映射、回执身份、HTTP 安全或继续授权时，必须同步更新样例、测试、 本 README 和 docs/mcp-server.md`。"
  },
  {
    "slug": "crates/model-provider",
    "sourcePath": "zeta-rs/model-provider/README.md",
    "title": "zeta-model-provider",
    "description": "zeta-model-provider 把 validated declarative config 与 ModelRef(provider, model) 解析成 Arc<dyn ModelInvoker>。它选择 provider runtime 和 API profile；wire codec 属于 zeta-api， operation retry/framing…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 provider runtime instantiation、adapter selection 与 immutable model invoker。\n> Declarative config 见 [`zeta-model-provider-config`](../model-provider-config/README.md)，跨系统\n> credential/provider 设计见 [`docs/model-provider.md`](../../docs/model-provider.md)，模型目录实现见\n> [`zeta-models-manager`](../models-manager/README.md)。\n\n`zeta-model-provider` 把 validated declarative config 与 `ModelRef(provider, model)` 解析成\n`Arc<dyn ModelInvoker>`。它选择 provider runtime 和 API profile；wire codec 属于 `zeta-api`，\noperation retry/framing 属于 `zeta-client`，socket/TLS/proxy 属于 `zeta-http-client`。\n\n当前 `EmbeddingInvoker` / `RerankInvoker` 除 canonical、有序、provider-neutral 调用契约外，已接入\nOpenAI-compatible embedding/rerank wire codec、OpenAI/Ollama embedding runtime 和 exact provider\nconfig resolver。本 crate 仍只拥有模型 API 选择、credential materialization、请求适配和执行。\n它不决定代码如何切块、查询哪个向量索引、准备哪些 rerank 候选，也不拥有排序、过滤或截断\n策略；本地 CodeIndex 的这些策略属于 `zeta-code-index-semantic`，远端托管索引则属于具体 provider。\n\n## 公共契约\n\n| Symbol | 职责 | 生命周期 |\n| --- | --- | --- |\n| `ModelProvider` | `ModelRuntimeRequest → ModelInvoker` port | composition/invocation safe point |\n| `ModelProviderRuntime` | built-in concrete resolver | 持有 config registry、shared `ModelsManager`、lazy operation client 和可选 local tokenizer service |\n| `ModelRuntimeRequest` | exact `ModelRef + ModelProviderConfig` | immutable selection request |\n| `ModelInvoker` | canonical `ModelRequest → ModelResponse` | one immutable provider/model snapshot |\n| `ModelInvoker::{input_token_measurement_capability,measure_input_with_cancellation}` | frozen request 的 tokenizer/preflight port | 与 invocation 相同 immutable snapshot |\n| re-exported `LocalTokenizerBinding` / `LocalTokenizerRegistry` | 宿主安装资产后的通用本地 tokenizer 接口 | composition safe point 构建后注入 runtime |\n| `EmbeddingInvoker` | ordered text batch → finite equal-dimension vectors | one immutable embedding model snapshot |\n| `RerankInvoker` | query + ordered documents → ordered finite scores | one immutable rerank model snapshot |\n| `SemanticModelProvider` | exact model/config → embedding 或 rerank invoker | provider transport/credential boundary |\n| `Provider` | normalized provider runtime | definition、config、private adapter、client |\n| `UnavailableModel` | explicit failing invoker | host 无法配置 model 时 fail closed |\n| `EchoModel` | deterministic test/local fixture | 不是 production model |\n| `ModelProviderError` | config/model/API/credential/unavailable error | 保留 failure domain |\n\nApp Server 应在每次 model invocation safe point 重新 resolve invoker；config update 影响下一次\ninvocation，不原地修改已经运行的 `RegisteredModelInvoker`。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `ModelProviderRuntime::instantiate_normalized` | private method | definition lookup + `Provider::instantiate` | normalization success 后 provider 必须存在 |\n| `LazyOperationClient` | private struct | 第一次 operation 才创建 production HTTP client，并缓存结果 | App Server 启动和 config inspection 不接触 TLS/proxy |\n| `Provider::instantiate` | crate-private | enforce definition/config ID equality，materialize adapter | 不读取 mutable config/credential store |\n| `providers::instantiate` | crate-private function | exhaustive `ProviderAdapter` enum dispatch | provider selection 唯一 switch |\n| `ProviderAdapter` | crate-private trait | protocol + explicit token measurement capability + complete | 不按 model ID 或 URL 猜能力 |\n| `LocalInputTokenCounter` | crate-private struct | 官方预检不可用时把整份请求交给本地计数服务 | 不下载资产、不按 provider 猜 tokenizer revision |\n| `api_endpoint` | private function | `ApiProfile → zeta_api::ApiEndpoint` | 按 profile，不按 provider name 猜 |\n| provider `*Adapter::new` | crate-private | normalized base URL + fixed headers + endpoint | one immutable runtime snapshot |\n| `Provider::resolve_model` | private method | 委托 shared manager 的 static typed resolution | 不复制 catalog policy 或做远端请求 |\n| `RegisteredModelInvoker` | private struct | bind exact Provider + resolved Model | request 时只应用 normalized defaults |\n| `RegisteredModelInvoker::invoke` | private trait impl | clone canonical request、apply max tokens、complete | 不读取 product config |\n\n## 运行时调用图\n\n```text\nModelProviderRuntime::runtime(ModelRuntimeRequest)\n└─ build_model(config, model_ref)\n   ├─ ProviderConfigRegistry::normalize_for\n   ├─ instantiate_normalized\n   │  ├─ registry.get(definition)\n   │  └─ Provider::instantiate\n   │     └─ providers::instantiate(adapter kind, normalized config)\n   └─ Provider::build_model(model_id)\n      ├─ Provider::resolve_model\n      │  └─ ModelsManager::resolve_static\n      └─ RegisteredModelInvoker { provider, model }\n\nRegisteredModelInvoker::invoke(request)\n├─ clone canonical ModelRequest\n├─ apply normalized max_output_tokens when the request has no explicit limit\n└─ Provider::complete\n   ├─ resolve_model\n   └─ ProviderAdapter::complete\n      └─ zeta_api::ApiEndpoint::complete_with_client\n         └─ LazyOperationClient\n            ├─ first operation: build fallible production client\n            └─ OperationClient\n```\n\n`RegisteredModelInvoker::measure_input_with_cancellation` 复用同一个 `prepare_request`，因此 provider\n默认 `max_output_tokens` 与最终 invocation 一致；adapter 再把 canonical input 编成 count endpoint\n接受的 wire shape。`ProviderInputTokenCounter` 只 materialize config 已声明且 model policy 允许的\nprofile/target，adapter 不按 model ID 或 URL 猜能力。OpenAI Responses 声明 remote/exact；Anthropic、\nGoogle、Kimi 与 Z.AI 声明 remote/estimated。`Provider` 统一执行“官方预检 → 本地整请求计数 →\nunavailable”的优先级；因此任何 provider 只要注入精确 `ModelRef` binding 都能使用 local/estimated，\n不再由各 adapter 复制本地选择逻辑。\n\n`Provider::complete` 再次通过同一个 manager resolve model，因此 direct Provider callers 与 bound\ninvoker 使用相同 catalog policy。App Server 通过 `ModelProviderRuntime::models_manager()` 获得同一\n进程内 manager clone，目录展示与 invocation 不再分别读取 `ProviderDefinition.models`。\n\n## 供应商适配器模式\n\n每个 `src/providers/<name>.rs` 定义 private adapter，通常持有：\n\n- `ResolvedApiTarget`：normalized base URL 与 fixed headers；\n- `ApiEndpoint`：由 declarative `ApiProfile` 映射；\n- 必要 provider-fixed header。\n\nAdapter 不手写 request JSON；它委托 `zeta-api` codec。相同 provider adapter 可以选择不同\nprofile，但只有 configuration definition 可以决定 profile。\n\n新增 provider/profile 时同步检查 config enum/definition、`providers::instantiate` exhaustive match、\n`api_endpoint`、fixed headers、codec support、fake transport tests 和系统 provider matrix。\n\n## 错误与模型目录\n\n| Path | Error |\n| --- | --- |\n| invalid/unknown/mismatched config | `ModelProviderError::Config` |\n| listed-only unknown model | `ModelNotRegistered` |\n| wire/operation/transport initialization failure | `Api(ApiError)` |\n| explicitly unavailable host model | `Unavailable` |\n\n`AllowUnlisted` 由 manager 生成 unverified metadata，不证明 entitlement、capability 或 remote\navailability。`ListedOnly` 由 manager 在任何 network call 前拒绝。\n\n## 方向偏差检查\n\n- Core 按 provider ID branch：provider selection 泄漏出 runtime crate；\n- runtime 根据 URL 猜 profile：declarative config 被绕过；\n- provider adapter 自己序列化 wire JSON：codec ownership 从 `zeta-api` 漂移；\n- adapter 直接构造 ureq/reqwest client：network substrate/retry ownership 漂移；\n- `RegisteredModelInvoker` 读取 mutable config：safe-point snapshot 被破坏；\n- `EchoModel` 出现在 production fallback：配置失败被静默掩盖；\n- API codec/network layer 读取 secret store：credential materialization 必须停留在 provider runtime。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-model-provider\nbazel test //zeta-rs/model-provider:model-provider-unit-tests\n```\n\n测试使用注入的 `OperationClient` 捕获请求，覆盖 Responses/Chat/Anthropic 配置、结构化工具、\n自定义端点、默认值、供应商不匹配、目录策略、固定标头、取消传播和默认 HTTP 传输；lazy client\n测试另外断言构造前不访问 transport、只初始化一次，并把初始化失败作为普通 transport error 返回。\n\n当前 completion `ModelInvoker` 已有 concrete provider adapters；embedding/rerank 已有 canonical\ninvoker、request/response validation、OpenAI-compatible/Ollama runtime resolver，以及本地\n`zeta-code-index-semantic` 和 Tool Search consumers。\n当前 completion invocation 同时支持 unary 与 OpenAI Responses、OpenAI-compatible Chat Completions、\nAnthropic Messages 原生 HTTP/SSE stream；\nOpenAI semantic adapter 可以从 host 注入的 `SecretStore` materialize API key；Ollama 与\nOpenAI-compatible semantic endpoint 当前按 unauthenticated endpoint 调用。持久化 secret backend、\ncredential 设置 UI、更多 stream profile 与动态 catalog 的长期设计仍在系统文档中演进。完整\nCodex subscription Agent loop 通过 Core `TurnExecutionBackend` 接入，不属于本 crate。\n新增能力应保持 invoker immutable、profile explicit、\nprovider adapter private，以及 config/catalog/codec/operation/network 分层。\n\n当前 runtime 尚未实现 provider-specific `ModelCatalogSource`；动态 discovery endpoint/DTO 和 credential\nbinding 属于下一阶段 adapter 工作。静态 invocation resolution 已统一进入 manager。\n\n当前 input-token preflight 已贯穿 `ModelInvoker → ProviderAdapter → zeta-api → OperationClient` 的\ncaller-owned cancellation。所有 estimated endpoint 使用额外 1%/至少 32 tokens 的保守记账余量；这\n只是 Zeta 的预算策略，不是 provider 承诺的硬上界。Google 对不能无损映射到 native request 的远程\n图片返回 unavailable；Kimi 当前带 tools/reasoning 的请求返回 unavailable；Z.AI 当前带 Tool\nCall/Result 历史的请求返回 unavailable。DeepSeek/Hugging Face 的本地 tokenizer adapter 已实现，\n使用 2%/至少 64 tokens 的保守余量；宿主通过 `ModelProviderRuntime::with_local_tokenizers` 注入已经\n验证的 registry 或 manager。App Server 当前为 Hugging Face 公共 `owner/repo` 注入按需发现、下载、\n磁盘缓存和内存 LRU；其他 provider/model 仍需宿主提供固定资产清单。官方预检或本地计数的非取消\n错误不会中止真实模型调用，而是继续降级到下一计量来源。\n\n`RegisteredModelInvoker::invoke_with_cancellation` 把 caller token 逐层传给 private\n`ProviderAdapter::complete` 和 `OperationClient::execute_with_cancellation`。取消是独立的\n`ModelProviderError::Cancelled`，App Server 不会把它误报为 model failure。同步 HTTP attempt\n可能已进入底层 socket；operation 立即停止等待且不 retry，attempt 本身由 transport timeout\n有界结束，迟到 response 不会被接受。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "运行时调用图",
        "title": "运行时调用图"
      },
      {
        "depth": 2,
        "id": "供应商适配器模式",
        "title": "供应商适配器模式"
      },
      {
        "depth": 2,
        "id": "错误与模型目录",
        "title": "错误与模型目录"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释 provider runtime instantiation、adapter selection 与 immutable model invoker。 Declarative config 见 zeta-model-provider-config，跨系统 credential/provider 设计见 docs/model-provider.md，模型目录实现见 zeta-models-manager。 zeta-model-provider 把 validated declarative config 与 ModelRef provider, model 解析成 Arc<dyn ModelInvoker 。它选择 provider runtime 和 API profile；wire codec 属于 zeta-api， operation retry/framing 属于 zeta-client，socket/TLS/proxy 属于 zeta-http-client。 当前 EmbeddingInvoker / RerankInvoker 除 canonical、有序、provider-neutral 调用契约外，已接入 OpenAI-compatible embedding/rerank wire codec、OpenAI/Ollama embedding runtime 和 exact provider config resolver。本 crate 仍只拥有模型 API 选择、credential materialization、请求适配和执行。 它不决定代码如何切块、查询哪个向量索引、准备哪些 rerank 候选，也不拥有排序、过滤或截断 策略；本地 CodeIndex 的这些策略属于 zeta-code-index-semantic，远端托管索引则属于具体 provider。 公共契约 Symbol 职责 生命周期 --- --- --- ModelProvider ModelRuntimeRequest → ModelInvoker port composition/invocation safe point ModelProviderRuntime built-in concrete resolver 持有 config registry、shared ModelsManager、lazy operation client 和可选 local tokenizer service ModelRuntimeRequest exact ModelRef + ModelProviderConfig immutable selection request ModelInvoker canonical ModelRequest → ModelResponse one immutable provider/model snapshot ModelInvoker:: inputtokenmeasurementcapability,measureinputwithcancellation frozen request 的 tokenizer/preflight port 与 invocation 相同 immutable snapshot re-exported LocalTokenizerBinding / LocalTokenizerRegistry 宿主安装资产后的通用本地 tokenizer 接口 composition safe point 构建后注入 runtime EmbeddingInvoker ordered text batch → finite equal-dimension vectors one immutable embedding model snapshot RerankInvoker query + ordered documents → ordered finite scores one immutable rerank model snapshot SemanticModelProvider exact model/config → embedding 或 rerank invoker provider transport/credential boundary Provider normalized provider runtime definition、config、private adapter、client UnavailableModel explicit failing invoker host 无法配置 model 时 fail closed EchoModel deterministic test/local fixture 不是 production model ModelProviderError config/model/API/credential/unavailable error 保留 failure domain App Server 应在每次 model invocation safe point 重新 resolve invoker；config update 影响下一次 invocation，不原地修改已经运行的 RegisteredModelInvoker。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- ModelProviderRuntime::instantiatenormalized private method definition lookup + Provider::instantiate normalization success 后 provider 必须存在 LazyOperationClient private struct 第一次 operation 才创建 production HTTP client，并缓存结果 App Server 启动和 config inspection 不接触 TLS/proxy Provider::instantiate crate-private enforce definition/config ID equality，materialize adapter 不读取 mutable config/credential store providers::instantiate crate-private function exhaustive ProviderAdapter enum dispatch provider selection 唯一 switch ProviderAdapter crate-private trait protocol + explicit token measurement capability + complete 不按 model ID 或 URL 猜能力 LocalInputTokenCounter crate-private struct 官方预检不可用时把整份请求交给本地计数服务 不下载资产、不按 provider 猜 tokenizer revision apiendpoint private function ApiProfile → zetaapi::ApiEndpoint 按 profile，不按 provider name 猜 provider Adapter::new crate-private normalized base URL + fixed headers + endpoint one immutable runtime snapshot Provider::resolvemodel private method 委托 shared manager 的 static typed resolution 不复制 catalog policy 或做远端请求 RegisteredModelInvoker private struct bind exact Provider + resolved Model request 时只应用 normalized defaults RegisteredModelInvoker::invoke private trait impl clone canonical request、apply max tokens、complete 不读取 product config 运行时调用图 ``text ModelProviderRuntime::runtime ModelRuntimeRequest └─ buildmodel config, modelref ├─ ProviderConfigRegistry::normalizefor ├─ instantiatenormalized │ ├─ registry.get definition │ └─ Provider::instantiate │ └─ providers::instantiate adapter kind, normalized config └─ Provider::buildmodel modelid ├─ Provider::resolvemodel │ └─ ModelsManager::resolvestatic └─ RegisteredModelInvoker provider, model RegisteredModelInvoker::invoke request ├─ clone canonical ModelRequest ├─ apply normalized maxoutputtokens when the request has no explicit limit └─ Provider::complete ├─ resolvemodel └─ ProviderAdapter::complete └─ zetaapi::ApiEndpoint::completewithclient └─ LazyOperationClient ├─ first operation: build fallible production client └─ OperationClient ` RegisteredModelInvoker::measureinputwithcancellation 复用同一个 preparerequest，因此 provider 默认 maxoutputtokens 与最终 invocation 一致；adapter 再把 canonical input 编成 count endpoint 接受的 wire shape。ProviderInputTokenCounter 只 materialize config 已声明且 model policy 允许的 profile/target，adapter 不按 model ID 或 URL 猜能力。OpenAI Responses 声明 remote/exact；Anthropic、 Google、Kimi 与 Z.AI 声明 remote/estimated。Provider 统一执行“官方预检 → 本地整请求计数 → unavailable”的优先级；因此任何 provider 只要注入精确 ModelRef binding 都能使用 local/estimated， 不再由各 adapter 复制本地选择逻辑。 Provider::complete 再次通过同一个 manager resolve model，因此 direct Provider callers 与 bound invoker 使用相同 catalog policy。App Server 通过 ModelProviderRuntime::modelsmanager 获得同一 进程内 manager clone，目录展示与 invocation 不再分别读取 ProviderDefinition.models。 供应商适配器模式 每个 src/providers/<name .rs 定义 private adapter，通常持有： - ResolvedApiTarget：normalized base URL 与 fixed headers； - ApiEndpoint：由 declarative ApiProfile 映射； - 必要 provider-fixed header。 Adapter 不手写 request JSON；它委托 zeta-api codec。相同 provider adapter 可以选择不同 profile，但只有 configuration definition 可以决定 profile。 新增 provider/profile 时同步检查 config enum/definition、providers::instantiate exhaustive match、 apiendpoint、fixed headers、codec support、fake transport tests 和系统 provider matrix。 错误与模型目录 Path Error --- --- invalid/unknown/mismatched config ModelProviderError::Config listed-only unknown model ModelNotRegistered wire/operation/transport initialization failure Api ApiError explicitly unavailable host model Unavailable AllowUnlisted 由 manager 生成 unverified metadata，不证明 entitlement、capability 或 remote availability。ListedOnly 由 manager 在任何 network call 前拒绝。 方向偏差检查 - Core 按 provider ID branch：provider selection 泄漏出 runtime crate； - runtime 根据 URL 猜 profile：declarative config 被绕过； - provider adapter 自己序列化 wire JSON：codec ownership 从 zeta-api 漂移； - adapter 直接构造 ureq/reqwest client：network substrate/retry ownership 漂移； - RegisteredModelInvoker 读取 mutable config：safe-point snapshot 被破坏； - EchoModel 出现在 production fallback：配置失败被静默掩盖； - API codec/network layer 读取 secret store：credential materialization 必须停留在 provider runtime。 测试、限制与演进 `text cargo test -p zeta-model-provider bazel test //zeta-rs/model-provider:model-provider-unit-tests ` 测试使用注入的 OperationClient 捕获请求，覆盖 Responses/Chat/Anthropic 配置、结构化工具、 自定义端点、默认值、供应商不匹配、目录策略、固定标头、取消传播和默认 HTTP 传输；lazy client 测试另外断言构造前不访问 transport、只初始化一次，并把初始化失败作为普通 transport error 返回。 当前 completion ModelInvoker 已有 concrete provider adapters；embedding/rerank 已有 canonical invoker、request/response validation、OpenAI-compatible/Ollama runtime resolver，以及本地 zeta-code-index-semantic 和 Tool Search consumers。 当前 completion invocation 同时支持 unary 与 OpenAI Responses、OpenAI-compatible Chat Completions、 Anthropic Messages 原生 HTTP/SSE stream； OpenAI semantic adapter 可以从 host 注入的 SecretStore materialize API key；Ollama 与 OpenAI-compatible semantic endpoint 当前按 unauthenticated endpoint 调用。持久化 secret backend、 credential 设置 UI、更多 stream profile 与动态 catalog 的长期设计仍在系统文档中演进。完整 Codex subscription Agent loop 通过 Core TurnExecutionBackend 接入，不属于本 crate。 新增能力应保持 invoker immutable、profile explicit、 provider adapter private，以及 config/catalog/codec/operation/network 分层。 当前 runtime 尚未实现 provider-specific ModelCatalogSource；动态 discovery endpoint/DTO 和 credential binding 属于下一阶段 adapter 工作。静态 invocation resolution 已统一进入 manager。 当前 input-token preflight 已贯穿 ModelInvoker → ProviderAdapter → zeta-api → OperationClient 的 caller-owned cancellation。所有 estimated endpoint 使用额外 1%/至少 32 tokens 的保守记账余量；这 只是 Zeta 的预算策略，不是 provider 承诺的硬上界。Google 对不能无损映射到 native request 的远程 图片返回 unavailable；Kimi 当前带 tools/reasoning 的请求返回 unavailable；Z.AI 当前带 Tool Call/Result 历史的请求返回 unavailable。DeepSeek/Hugging Face 的本地 tokenizer adapter 已实现， 使用 2%/至少 64 tokens 的保守余量；宿主通过 ModelProviderRuntime::withlocaltokenizers 注入已经 验证的 registry 或 manager。App Server 当前为 Hugging Face 公共 owner/repo 注入按需发现、下载、 磁盘缓存和内存 LRU；其他 provider/model 仍需宿主提供固定资产清单。官方预检或本地计数的非取消 错误不会中止真实模型调用，而是继续降级到下一计量来源。 RegisteredModelInvoker::invokewithcancellation 把 caller token 逐层传给 private ProviderAdapter::complete 和 OperationClient::executewithcancellation。取消是独立的 ModelProviderError::Cancelled`，App Server 不会把它误报为 model failure。同步 HTTP attempt 可能已进入底层 socket；operation 立即停止等待且不 retry，attempt 本身由 transport timeout 有界结束，迟到 response 不会被接受。"
  },
  {
    "slug": "crates/model-provider-config",
    "sourcePath": "zeta-rs/model-provider-config/README.md",
    "title": "zeta-model-provider-config",
    "description": "本 crate 拥有 serializable、runtime-free 的 provider configuration。它不持有 API client、 credential、secret、connection pool 或 process-local adapter。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 provider declaration、registry validation 与 normalization。跨系统配置模型和\n> 演进见 [`docs/model-provider-config.md`](../../docs/model-provider-config.md)；runtime\n> instantiation 见 [`zeta-model-provider`](../model-provider/README.md)。\n\n本 crate 拥有 serializable、runtime-free 的 provider configuration。它不持有 API client、\ncredential、secret、connection pool 或 process-local adapter。\n\n## 公共模型\n\n| Symbol | 职责 | 关键语义 |\n| --- | --- | --- |\n| `ModelProviderConfig` | 用户/host 可配置值 | provider、base URL、max output 与 per-model context metadata |\n| `ModelContextConfig` | 单模型的 Core budget metadata | positive context window、optional auto-compact limit |\n| `ProviderDefinition` | provider-owned declaration | adapter identity、API profile、endpoint/catalog/defaults |\n| `NormalizedModelProviderConfig` | runtime-ready immutable config | provider/profile/base URL 已确定 |\n| `ProviderConfigRegistry` | definition authority | validate、register、merge、selection、normalize |\n| `ProviderAdapter` | serializable adapter identity | 不是 runtime trait/object |\n| `ApiProfile` | declarative wire profile | runtime 显式解析为 `zeta-api::ApiEndpoint` |\n| `InputTokenCountDefinition` | provider-owned preflight declaration | profile、target 与明确 model policy |\n| `NormalizedInputTokenCountConfig` | runtime-ready count snapshot | 已解析 base URL；不包含 client 或准确度策略 |\n| `EndpointPolicy` | provider default 或 configured-only | 不执行 DNS/network validation |\n| `ModelCatalogPolicy` | listed-only 或 allow-unlisted | 声明 static gate，由 `zeta-models-manager` 执行 canonical resolution |\n| `ApprovalReviewModelDefault` | automatic review default | active model 或 provider-declared model |\n| `ProviderConfigError` | static/normalization error | 不包含 transport/auth failure |\n\n`model_provider_config_schema()` 与 `provider_definition_schema()` 从 Rust types 生成 JSON Schema；\nschema 没有第二份手写来源。\n\n## 文件与内部接口\n\n```text\nsrc/\n├── config.rs       # user config、normalized config、URL helpers\n├── definition.rs   # provider declaration 与 validation\n├── input_token_count.rs # count profile、target、model policy 与 normalized snapshot\n├── registry.rs     # registration、merge、selection、normalization\n├── providers/      # one private built-in definition per provider\n├── error.rs\n└── lib.rs\n```\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `ModelProviderConfig::validate_static` | public method | zero output/context limits 与 configured URL shape | 不依赖 registry或网络 |\n| `ProviderDefinition::validate` | public method | name、default endpoint、defaults、catalog uniqueness | definition 自身必须独立有效 |\n| `InputTokenCountDefinition::validate` | crate-private method | count URL、non-empty/unique model list | 不探测远端 model availability |\n| `ProviderConfigRegistry::register` | public method | validate + reject duplicate | built-in/plugin 定义走相同路径 |\n| `ProviderConfigRegistry::merge` | public method | prevalidate incoming + explicit conflict policy | merge 不能 partial apply |\n| `ProviderConfigRegistry::normalize` | public method | config + definition → normalized snapshot | endpoint/default/profile precedence 在此唯一实现 |\n| `normalize_for` | public method | 先 enforce selected/configured provider identity | 防止 model ref 与 config 串线 |\n| `automatic_approval_review_model` | public method | provider default 或 active model fallback | 不证明远端 entitlement |\n| `validate_model_selection` | public compatibility/preflight method | listed catalog 的 deterministic static gate | 新 runtime consumer 使用 `zeta-models-manager` canonical resolution |\n| `normalize_base_url` | crate-private function | apply explicit normalization rule | 不追加 API route |\n| `is_http_url` | crate-private function | 最小 HTTP(S) shape check | 不是 full URL/network validator |\n| `providers::builtin` | crate-private function | 13 个 built-in definitions | 每个 provider 在 sibling module 独立定义 |\n| `default_provider` / `configured_provider` | private helpers | shared definition constructors | 不隐藏 provider-specific profile/default differences |\n\n## 规范化调用图\n\n```text\nProviderConfigRegistry::normalize(config)\n├─ ModelProviderConfig::validate_static\n├─ registry.get(config.provider)\n├─ choose base URL\n│  ├─ non-empty configured override\n│  ├─ EndpointPolicy::ProviderDefault\n│  └─ ConfiguredOnly without value → MissingBaseUrl\n├─ normalize_base_url(definition rule)\n├─ is_http_url\n├─ choose max_output_tokens\n│  └─ config value overrides provider default\n├─ normalize input token count\n│  ├─ InvocationBase → normalized invocation base\n│  ├─ ProviderDefault + no override → declared count base\n│  └─ ProviderDefault + endpoint override → disabled\n└─ NormalizedModelProviderConfig\n```\n\n`model_context` 不进入 `NormalizedModelProviderConfig`，因为它不改变 transport endpoint。Local App\nServer 在冻结一次模型调用预算时，按 selected `ModelId` 读取该 map；配置值优先于 built-in\n`ModelInfo.context_window`。没有可信窗口时 Core 使用 provider-managed，不在本 crate 猜测模型\n规格。\n\n```text\nProviderConfigRegistry::merge(incoming, policy)\n├─ validate every incoming definition\n├─ preflight all conflicts when RejectConflicts\n└─ extend map only after preflight succeeds\n```\n\nMerge 必须 preflight 后一次 extend；在循环中边验证边插入会造成 error 后 registry 部分变化。\n\n## 内置供应商定义\n\n每个 `providers/<name>.rs::definition()` 返回一个完整 `ProviderDefinition`。例如 OpenAI 选择\n`OpenAiResponses` 与同 base 的 count profile；Google invocation 使用 compatible base，但\n`countTokens` 使用单独声明的 native base；Anthropic 选择 `AnthropicMessages` 并声明默认 max\ntokens。Kimi、Google 和 Z.AI 的 count model list 是 definition 数据，不由 runtime 按 ID 前缀猜测。\nProvider matrix 和官方依据由系统文档维护，本 README 只固定 definition construction pattern。\n\n`InputTokenCountTarget::InvocationBase` 会跟随显式 endpoint override，适合 count 与 invocation 同一\nservice surface 的 provider。`ProviderDefault` 只在 invocation 也使用 provider 默认 endpoint 时启用；\n一旦用户配置代理或私有 endpoint，normalization 会关闭该 count binding，避免把同一请求绕过代理发到\n官方服务。\n\n新增 provider 时同步：\n\n1. 增加 `ProviderAdapter` variant；\n2. 新建 private `providers/<name>.rs::definition`；\n3. 加入 `providers::builtin()`；\n4. 在 runtime crate 增加对应 adapter mapping；\n5. 增加 definition/normalization/runtime tests；\n6. 更新系统 provider matrix 与 schema fixture。\n\n## 方向偏差检查\n\n- config crate 依赖 `zeta-api`/HTTP/secret：runtime state 下沉；\n- `ProviderAdapter` 直接存 trait object：declaration 不再 serializable；\n- runtime 根据 provider name 猜 API profile：显式 declaration 被绕过；\n- runtime 根据 model ID 前缀或 invocation URL 拼 count endpoint：显式 count declaration 被绕过；\n- normalization 追加 `/responses` 等 route：endpoint/profile ownership 混淆；\n- allow-unlisted 被描述为远端可用：static evidence 被夸大；\n- merge conflict 造成 partial registry mutation：config snapshot 不再原子。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-model-provider-config\nbazel test //zeta-rs/model-provider-config:model-provider-config-unit-tests\n```\n\n测试覆盖 serde/schema、defaults、configured-only endpoint、invalid URL/output/context tokens、merge semantics、\nautomatic review model、catalog gate、built-in completeness 与 provider mismatch。\n\n当前 URL validator 只接受具有非空 authority 的 HTTP(S) shape，不解析 credential、DNS、route 或\nreachability。Catalog 也是 declarative snapshot，不代表 account entitlement。未来可以扩充 schema、\ndefinition source 与 catalog metadata，但必须保持 runtime-free、deterministic normalization 和显式\nprofile selection。",
    "headings": [
      {
        "depth": 2,
        "id": "公共模型",
        "title": "公共模型"
      },
      {
        "depth": 2,
        "id": "文件与内部接口",
        "title": "文件与内部接口"
      },
      {
        "depth": 2,
        "id": "规范化调用图",
        "title": "规范化调用图"
      },
      {
        "depth": 2,
        "id": "内置供应商定义",
        "title": "内置供应商定义"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释 provider declaration、registry validation 与 normalization。跨系统配置模型和 演进见 docs/model-provider-config.md；runtime instantiation 见 zeta-model-provider。 本 crate 拥有 serializable、runtime-free 的 provider configuration。它不持有 API client、 credential、secret、connection pool 或 process-local adapter。 公共模型 Symbol 职责 关键语义 --- --- --- ModelProviderConfig 用户/host 可配置值 provider、base URL、max output 与 per-model context metadata ModelContextConfig 单模型的 Core budget metadata positive context window、optional auto-compact limit ProviderDefinition provider-owned declaration adapter identity、API profile、endpoint/catalog/defaults NormalizedModelProviderConfig runtime-ready immutable config provider/profile/base URL 已确定 ProviderConfigRegistry definition authority validate、register、merge、selection、normalize ProviderAdapter serializable adapter identity 不是 runtime trait/object ApiProfile declarative wire profile runtime 显式解析为 zeta-api::ApiEndpoint InputTokenCountDefinition provider-owned preflight declaration profile、target 与明确 model policy NormalizedInputTokenCountConfig runtime-ready count snapshot 已解析 base URL；不包含 client 或准确度策略 EndpointPolicy provider default 或 configured-only 不执行 DNS/network validation ModelCatalogPolicy listed-only 或 allow-unlisted 声明 static gate，由 zeta-models-manager 执行 canonical resolution ApprovalReviewModelDefault automatic review default active model 或 provider-declared model ProviderConfigError static/normalization error 不包含 transport/auth failure modelproviderconfigschema 与 providerdefinitionschema 从 Rust types 生成 JSON Schema； schema 没有第二份手写来源。 文件与内部接口 ``text src/ ├── config.rs user config、normalized config、URL helpers ├── definition.rs provider declaration 与 validation ├── inputtokencount.rs count profile、target、model policy 与 normalized snapshot ├── registry.rs registration、merge、selection、normalization ├── providers/ one private built-in definition per provider ├── error.rs └── lib.rs ` Symbol 可见性 当前职责 方向约束 --- --- --- --- ModelProviderConfig::validatestatic public method zero output/context limits 与 configured URL shape 不依赖 registry或网络 ProviderDefinition::validate public method name、default endpoint、defaults、catalog uniqueness definition 自身必须独立有效 InputTokenCountDefinition::validate crate-private method count URL、non-empty/unique model list 不探测远端 model availability ProviderConfigRegistry::register public method validate + reject duplicate built-in/plugin 定义走相同路径 ProviderConfigRegistry::merge public method prevalidate incoming + explicit conflict policy merge 不能 partial apply ProviderConfigRegistry::normalize public method config + definition → normalized snapshot endpoint/default/profile precedence 在此唯一实现 normalizefor public method 先 enforce selected/configured provider identity 防止 model ref 与 config 串线 automaticapprovalreviewmodel public method provider default 或 active model fallback 不证明远端 entitlement validatemodelselection public compatibility/preflight method listed catalog 的 deterministic static gate 新 runtime consumer 使用 zeta-models-manager canonical resolution normalizebaseurl crate-private function apply explicit normalization rule 不追加 API route ishttpurl crate-private function 最小 HTTP S shape check 不是 full URL/network validator providers::builtin crate-private function 13 个 built-in definitions 每个 provider 在 sibling module 独立定义 defaultprovider / configuredprovider private helpers shared definition constructors 不隐藏 provider-specific profile/default differences 规范化调用图 `text ProviderConfigRegistry::normalize config ├─ ModelProviderConfig::validatestatic ├─ registry.get config.provider ├─ choose base URL │ ├─ non-empty configured override │ ├─ EndpointPolicy::ProviderDefault │ └─ ConfiguredOnly without value → MissingBaseUrl ├─ normalizebaseurl definition rule ├─ ishttpurl ├─ choose maxoutputtokens │ └─ config value overrides provider default ├─ normalize input token count │ ├─ InvocationBase → normalized invocation base │ ├─ ProviderDefault + no override → declared count base │ └─ ProviderDefault + endpoint override → disabled └─ NormalizedModelProviderConfig ` modelcontext 不进入 NormalizedModelProviderConfig，因为它不改变 transport endpoint。Local App Server 在冻结一次模型调用预算时，按 selected ModelId 读取该 map；配置值优先于 built-in ModelInfo.contextwindow。没有可信窗口时 Core 使用 provider-managed，不在本 crate 猜测模型 规格。 `text ProviderConfigRegistry::merge incoming, policy ├─ validate every incoming definition ├─ preflight all conflicts when RejectConflicts └─ extend map only after preflight succeeds ` Merge 必须 preflight 后一次 extend；在循环中边验证边插入会造成 error 后 registry 部分变化。 内置供应商定义 每个 providers/<name .rs::definition 返回一个完整 ProviderDefinition。例如 OpenAI 选择 OpenAiResponses 与同 base 的 count profile；Google invocation 使用 compatible base，但 countTokens 使用单独声明的 native base；Anthropic 选择 AnthropicMessages 并声明默认 max tokens。Kimi、Google 和 Z.AI 的 count model list 是 definition 数据，不由 runtime 按 ID 前缀猜测。 Provider matrix 和官方依据由系统文档维护，本 README 只固定 definition construction pattern。 InputTokenCountTarget::InvocationBase 会跟随显式 endpoint override，适合 count 与 invocation 同一 service surface 的 provider。ProviderDefault 只在 invocation 也使用 provider 默认 endpoint 时启用； 一旦用户配置代理或私有 endpoint，normalization 会关闭该 count binding，避免把同一请求绕过代理发到 官方服务。 新增 provider 时同步： 1. 增加 ProviderAdapter variant； 2. 新建 private providers/<name .rs::definition； 3. 加入 providers::builtin ； 4. 在 runtime crate 增加对应 adapter mapping； 5. 增加 definition/normalization/runtime tests； 6. 更新系统 provider matrix 与 schema fixture。 方向偏差检查 - config crate 依赖 zeta-api/HTTP/secret：runtime state 下沉； - ProviderAdapter 直接存 trait object：declaration 不再 serializable； - runtime 根据 provider name 猜 API profile：显式 declaration 被绕过； - runtime 根据 model ID 前缀或 invocation URL 拼 count endpoint：显式 count declaration 被绕过； - normalization 追加 /responses 等 route：endpoint/profile ownership 混淆； - allow-unlisted 被描述为远端可用：static evidence 被夸大； - merge conflict 造成 partial registry mutation：config snapshot 不再原子。 测试、限制与演进 `text cargo test -p zeta-model-provider-config bazel test //zeta-rs/model-provider-config:model-provider-config-unit-tests `` 测试覆盖 serde/schema、defaults、configured-only endpoint、invalid URL/output/context tokens、merge semantics、 automatic review model、catalog gate、built-in completeness 与 provider mismatch。 当前 URL validator 只接受具有非空 authority 的 HTTP S shape，不解析 credential、DNS、route 或 reachability。Catalog 也是 declarative snapshot，不代表 account entitlement。未来可以扩充 schema、 definition source 与 catalog metadata，但必须保持 runtime-free、deterministic normalization 和显式 profile selection。"
  },
  {
    "slug": "crates/model-tokenizer",
    "sourcePath": "zeta-rs/model-tokenizer/README.md",
    "title": "zeta-model-tokenizer",
    "description": "zeta-model-tokenizer 对外只暴露整请求 countinputtokens(ModelRef, ModelRequest)。内部按需发现并 固定 tokenizer.json 与 tokenizerconfig.json，用 hf-chat-template 把 messages、Tools 与 Tool 历史渲染为 prompt，再用 tokenize…",
    "group": "Crate 实现参考",
    "markdown": "> 本文拥有本地 tokenizer 资产绑定、聊天模板执行与 token 计数的实现契约。预算数学与计量结果\n> 语义由 [`zeta-context-engine`](../context-engine/README.md) 拥有；provider 接入由\n> [`zeta-model-provider`](../model-provider/README.md) 拥有。\n\n`zeta-model-tokenizer` 对外只暴露整请求 `count_input_tokens(ModelRef, ModelRequest)`。内部按需发现并\n固定 `tokenizer.json` 与 `tokenizer_config.json`，用 `hf-chat-template` 把 messages、Tools 与 Tool\n历史渲染为 prompt，再用 `tokenizers` 计数。它不声称本地结果必然等于远端 provider 的最终 usage。\n\n## 1. 边界与公共契约\n\n| Symbol | 职责 | 接入义务 |\n| --- | --- | --- |\n| `PinnedTokenizerAsset` | 绑定本地路径、上游 revision 和文件 SHA-256 | 宿主必须先安装资产并提供不可变 revision |\n| `LocalTokenizerBinding` | 将 tokenizer、chat template 和模板全局变量绑定到完整 `ModelRef` | provider 与 model 任一变化都必须建立独立绑定 |\n| `LocalTokenizerRegistry::register` | 读取、验摘要、解析 tokenizer 并编译模板 | 启动或配置 safe point 调用；失败不得注册半成品 |\n| `LocalTokenizerService` | provider adapter 使用的只读计数端口 | 必须返回随两份资产变化的 source revision |\n| `LocalTokenizationOutcome` | 区分已计数和不支持的请求 | 图片等没有 processor 的输入必须明确返回 unsupported |\n| `ManagedLocalTokenizerService` | 首次使用时后台发现/下载、持久化摘要清单并维护内存 LRU | 网络失败不得阻塞模型调用或每次请求重复下载 |\n| `HuggingFaceTokenizerAssetDiscoverer` | 将 `owner/repo` 的 `main` 解析为 immutable commit 和完整模板材料 | standalone `chat_template.jinja` 优先于 config 内嵌模板 |\n\n`hf-chat-template` 负责 Hugging Face 的 special token、Python/Jinja 兼容方法、`tojson`、\n`strftime_now` 与 named template 语义；Zeta 仍拥有 revision/SHA、磁盘目录、后台准备和 LRU。\n\n## 2. 内部接口地图与调用路径\n\n| Symbol | 可见性 | 单一职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `LoadedTokenizer::load` | private | 校验并一次性加载 tokenizer 与模板配置 | 网络、Hub branch 解析或 provider 名称判断进入此处 |\n| `verified_asset` | private | SHA-256 校验后才返回字节 | 只信路径或只记录 revision 而不验内容 |\n| `request::render_input` | private | canonical request 转换为 HF 风格 `messages`/`tools` | provider wire JSON 或预算策略进入转换层 |\n| `LoadedTokenizer::count` | private | 模板渲染后用 `add_special_tokens=false` 编码 | 跳过 chat template，只对正文编码 |\n| `source_revision` | private | 组合两份 revision 与摘要 | tokenizer 或 template 任一变化后仍复用旧来源版本 |\n\n```text\nexact ModelRef + complete ModelRequest\n  → LocalTokenizerService::count_input_tokens\n     → memory LRU / pinned disk cache / background Hub preparation\n     → request::render_input\n     → hf-chat-template render\n     → Tokenizer::encode(add_special_tokens = false)\n     → LocalTokenCount(tokens, composite source revision)\n```\n\n聊天模板已经负责加入控制 token，因此编码阶段关闭 tokenizer 的额外 special-token 注入，避免重复。\n模板语法错误在注册时作为 `LocalTokenizerError` 返回；请求触发模板运行时拒绝、未注册模型和没有\n多模态 processor 的图片请求返回 `UnsupportedRequest`，让上层使用保守估算。已经通过加载校验的\ntokenizer 若仍无法编码渲染结果，则作为运行时错误返回，避免掩盖损坏状态。\n\n## 3. 测试、限制与扩展\n\n```bash\ncargo test -p zeta-model-tokenizer\ncargo clippy -p zeta-model-tokenizer --all-targets -- -D warnings\n```\n\n- **Current**：精确 `ModelRef` registry、双资产 revision/digest 固定、`hf-chat-template 1.0`、文本/Tool\n  整请求投影、按需后台下载、重启复用磁盘缓存和四项默认内存 LRU 已实现。\n- **Current**：named `tool_use` 模板按请求是否带 Tools 自动选择；仓库存在 standalone\n  `chat_template.jinja` 时按 Transformers 优先级覆盖 config 内嵌模板，日期函数使用本地时钟。\n- **Current limitation**：当前多模态输入没有 processor，明确 unsupported；依赖模型专有外部函数的\n  模板在渲染时降级为 unsupported。\n- **Current limitation**：本 crate 只报告本地模板栈的计数。远端 provider 是否添加隐藏 envelope、\n  改写 Tool schema 或使用不同 revision，由 provider adapter 决定准确度。\n- **Extension point**：新增 Hub/provider 资产来源时实现 `TokenizerAssetDiscoverer`；不得把下载、缓存\n  或模板运行时泄漏给 Electron/TS 调用方。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与公共契约",
        "title": "1. 边界与公共契约"
      },
      {
        "depth": 2,
        "id": "2-内部接口地图与调用路径",
        "title": "2. 内部接口地图与调用路径"
      },
      {
        "depth": 2,
        "id": "3-测试限制与扩展",
        "title": "3. 测试、限制与扩展"
      }
    ],
    "searchText": " 本文拥有本地 tokenizer 资产绑定、聊天模板执行与 token 计数的实现契约。预算数学与计量结果 语义由 zeta-context-engine 拥有；provider 接入由 zeta-model-provider 拥有。 zeta-model-tokenizer 对外只暴露整请求 countinputtokens ModelRef, ModelRequest 。内部按需发现并 固定 tokenizer.json 与 tokenizerconfig.json，用 hf-chat-template 把 messages、Tools 与 Tool 历史渲染为 prompt，再用 tokenizers 计数。它不声称本地结果必然等于远端 provider 的最终 usage。 1. 边界与公共契约 Symbol 职责 接入义务 --- --- --- PinnedTokenizerAsset 绑定本地路径、上游 revision 和文件 SHA-256 宿主必须先安装资产并提供不可变 revision LocalTokenizerBinding 将 tokenizer、chat template 和模板全局变量绑定到完整 ModelRef provider 与 model 任一变化都必须建立独立绑定 LocalTokenizerRegistry::register 读取、验摘要、解析 tokenizer 并编译模板 启动或配置 safe point 调用；失败不得注册半成品 LocalTokenizerService provider adapter 使用的只读计数端口 必须返回随两份资产变化的 source revision LocalTokenizationOutcome 区分已计数和不支持的请求 图片等没有 processor 的输入必须明确返回 unsupported ManagedLocalTokenizerService 首次使用时后台发现/下载、持久化摘要清单并维护内存 LRU 网络失败不得阻塞模型调用或每次请求重复下载 HuggingFaceTokenizerAssetDiscoverer 将 owner/repo 的 main 解析为 immutable commit 和完整模板材料 standalone chattemplate.jinja 优先于 config 内嵌模板 hf-chat-template 负责 Hugging Face 的 special token、Python/Jinja 兼容方法、tojson、 strftimenow 与 named template 语义；Zeta 仍拥有 revision/SHA、磁盘目录、后台准备和 LRU。 2. 内部接口地图与调用路径 Symbol 可见性 单一职责 漂移信号 --- --- --- --- LoadedTokenizer::load private 校验并一次性加载 tokenizer 与模板配置 网络、Hub branch 解析或 provider 名称判断进入此处 verifiedasset private SHA-256 校验后才返回字节 只信路径或只记录 revision 而不验内容 request::renderinput private canonical request 转换为 HF 风格 messages/tools provider wire JSON 或预算策略进入转换层 LoadedTokenizer::count private 模板渲染后用 addspecialtokens=false 编码 跳过 chat template，只对正文编码 sourcerevision private 组合两份 revision 与摘要 tokenizer 或 template 任一变化后仍复用旧来源版本 ``text exact ModelRef + complete ModelRequest → LocalTokenizerService::countinputtokens → memory LRU / pinned disk cache / background Hub preparation → request::renderinput → hf-chat-template render → Tokenizer::encode addspecialtokens = false → LocalTokenCount tokens, composite source revision ` 聊天模板已经负责加入控制 token，因此编码阶段关闭 tokenizer 的额外 special-token 注入，避免重复。 模板语法错误在注册时作为 LocalTokenizerError 返回；请求触发模板运行时拒绝、未注册模型和没有 多模态 processor 的图片请求返回 UnsupportedRequest，让上层使用保守估算。已经通过加载校验的 tokenizer 若仍无法编码渲染结果，则作为运行时错误返回，避免掩盖损坏状态。 3. 测试、限制与扩展 `bash cargo test -p zeta-model-tokenizer cargo clippy -p zeta-model-tokenizer --all-targets -- -D warnings ` - Current：精确 ModelRef registry、双资产 revision/digest 固定、hf-chat-template 1.0、文本/Tool 整请求投影、按需后台下载、重启复用磁盘缓存和四项默认内存 LRU 已实现。 - Current：named tooluse 模板按请求是否带 Tools 自动选择；仓库存在 standalone chattemplate.jinja 时按 Transformers 优先级覆盖 config 内嵌模板，日期函数使用本地时钟。 - Current limitation：当前多模态输入没有 processor，明确 unsupported；依赖模型专有外部函数的 模板在渲染时降级为 unsupported。 - Current limitation：本 crate 只报告本地模板栈的计数。远端 provider 是否添加隐藏 envelope、 改写 Tool schema 或使用不同 revision，由 provider adapter 决定准确度。 - Extension point：新增 Hub/provider 资产来源时实现 TokenizerAssetDiscoverer`；不得把下载、缓存 或模板运行时泄漏给 Electron/TS 调用方。"
  },
  {
    "slug": "crates/models-manager",
    "sourcePath": "zeta-rs/models-manager/README.md",
    "title": "zeta-models-manager",
    "description": "zeta-models-manager 是 provider-independent 的模型目录控制面。它从 ProviderDefinition.models 建立静态 seed，把 provider discovery observation 合并到按 scope 隔离的 内存目录，并发布带 generation 的 immutable snapshot。它拥有 fr…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 crate 当前实现的权威说明。跨 crate 的目录语义、provider 调研和分阶段演进见\n> [`docs/models-manager.md`](../../docs/models-manager.md)；provider declaration 见\n> [`zeta-model-provider-config`](../model-provider-config/README.md)，调用 runtime 见\n> [`zeta-model-provider`](../model-provider/README.md)。\n\n`zeta-models-manager` 是 provider-independent 的模型目录控制面。它从\n`ProviderDefinition.models` 建立静态 seed，把 provider discovery observation 合并到按 scope 隔离的\n内存目录，并发布带 generation 的 immutable snapshot。它拥有 freshness、singleflight、字段级 merge、\n筛选和 typed resolution；不拥有 provider HTTP DTO、credential、模型调用、prompt/context cache、\nConfig persistence 或 UI。\n\n## 公共契约\n\n| Symbol | 调用方用途 | 关键语义 |\n| --- | --- | --- |\n| `ModelsManager` | read、refresh、list、resolve | clone 共享同一个进程内 scope/cache authority |\n| `CatalogScopeKey` | 标识 provider + endpoint/account/config revision | source scope 必须是不含秘密的一向指纹 |\n| `ModelCatalogSource` | provider runtime 实现 discovery port | 返回完整或 partial observation，不提交半页结果 |\n| `ModelCatalogSnapshot` | 上层读取 immutable catalog | 仅消费者可见内容变化时 generation 递增 |\n| `CatalogReadPolicy` | `CachePreferred` / `RequireFresh` / `CacheOnly` | 禁止用 `fresh: bool` 模糊表达阻塞语义 |\n| `CatalogQuery` | list filter | availability 与 unknown capability policy 显式命名 |\n| `ModelRequirements` | invocation-safe resolution | unknown、unsupported、unavailable 和 retired 分开处理 |\n| `DiscoveredCatalog` | source 的一次完整提交 | `CompleteAgentCatalog` 缺席可下架；`Partial` 缺席不改变 availability |\n| `ModelMetadataPatch` | provider 明确返回的字段 | `Unknown` 不覆盖已有 known metadata |\n| `ResolvedModel` | exact model + catalog generation + warnings | `AllowUnlisted` 产生 unverified synthetic metadata |\n\n`CatalogSourceScopeId` 不是 endpoint 或 credential reference。Host 必须先对 normalized endpoint、tenant、\ncredential revision 和 provider config revision 生成不可逆、无秘密的稳定指纹；任一输入变化都使用新\nscope。晚到的旧请求只可能提交到旧 scope。\n\n## 模块与内部所有权\n\n```text\nsrc/\n├── manager.rs      # read/refresh/singleflight 与 list/resolve orchestration\n├── cache.rs        # per-scope state、clock/freshness 与 snapshot generation rebuild\n├── source.rs       # consumer-owned discovery port 与 observation patch\n├── snapshot.rs     # immutable snapshot、generation、provenance、warning\n├── merge.rs        # seed/live 字段级合并与 complete/partial availability\n├── filter.rs       # capability/availability query 与 resolution checks\n├── policy.rs       # freshness/read named policy\n├── scope.rs        # opaque scope identity\n├── error.rs        # typed manager failures\n└── manager_tests.rs\n```\n\n| Private symbol | 当前职责 | 不能扩张到 |\n| --- | --- | --- |\n| `ManagedScope` | 一个 scope 的 snapshot state 与 async refresh gate | provider transport 或 Config mutation |\n| `ScopeState` | records、validator、freshness evidence、last refresh result | durable Config/Thread authority |\n| `ModelsManager::ensure_scope` | lazy seed snapshot construction | network discovery |\n| `ModelsManager::commit_discovery` | scope/duplicate validation 后原子提交 observation | provider payload decoding |\n| `rebuild_snapshot` | 比较 consumer-visible contents 后决定 generation | 每次 read 无条件 bump generation |\n| `CatalogRecord` | 合并中的 `ModelInfo`、availability、lifecycle、provenance | 暴露 provider raw DTO |\n| `apply_discovery` | complete/partial availability 与 live patch merge | 按 model ID 猜 capability |\n| `matches_query` / `validate_requirements` | list 与 resolve 的 canonical gate | UI 搜索或排序偏好 persistence |\n\n如果 manager 开始拼 discovery URL、读取 API key、发送 inference、解释 SSE，或者 provider runtime/UI\n重新实现 `ListedOnly` / `AllowUnlisted` 判断，说明 ownership 已漂移。\n\n## 执行路径\n\n静态路径不访问网络：\n\n```text\nModelsManager::static_snapshot / list_static / resolve_static\n└─ ensure_scope(CatalogScopeKey::provider_seed)\n   ├─ ProviderConfigRegistry::get\n   ├─ seed_records(ProviderDefinition.models)\n   └─ ModelCatalogSnapshot { generation: 1, freshness: StaticOnly }\n```\n\n动态刷新路径：\n\n```text\nModelsManager::refresh(scope, source)\n├─ capture refresh_serial\n├─ await per-scope AsyncMutex\n├─ serial changed → join prior result\n├─ ModelCatalogSource::discover(previous validator)   # 不持有 catalog write lock\n├─ validate exact scope + duplicate IDs\n├─ apply_discovery\n│  ├─ Partial: 仅 observed model → Available\n│  └─ CompleteAgentCatalog: 缺席 record → Unavailable\n└─ rebuild_snapshot\n   └─ visible contents changed → generation + 1\n```\n\n不同 scope 使用不同 refresh gate，可以并行。相同 scope 的并发调用只执行一次 source request；等待者\n复用同一成功 snapshot 或同一 typed error。future 被 drop 即为 cancellation，未返回完整 outcome 前不\n修改 snapshot。\n\n## 合并、缓存与失败\n\n当前合并来源是 provider seed 与 provider live observation。Live 的明确字段覆盖 seed；\n`ContextWindow::Unknown` 和 `CapabilitySupport::Unknown` 不擦除 known 值。Provenance 与\n`ModelMetadataQuality` 随 snapshot entry 暴露。Static seed 的 availability 是 `Unverified`，不是\n账号 entitlement 证明。\n\nFreshness 使用 manager policy 与 source 明确给出的 cache hint 中更保守的时长：\n\n| 状态 | `CachePreferred` | `RequireFresh` | `CacheOnly` |\n| --- | --- | --- | --- |\n| Fresh | 立即返回 | 立即返回 | 立即返回 |\n| StaleUsable | 返回并后台 refresh | 等待/join refresh | 返回 stale |\n| Expired | 等待/join refresh | 等待/join refresh | 返回 expired |\n| 只有静态 seed | 离线时返回；有动态 source 时首次发现 | 有 source 时刷新，否则报错 | 返回 static |\n\nAuthentication/permission failure 把先前 `Available` 降为 `Unverified` 并保留 metadata；unsupported、\nrate limit、transient、invalid payload 均保留 last-known records，并产生不包含 secret/raw body 的 warning。\nExplicit `refresh` 仍返回 typed error，调用方可另行读取 last-known snapshot。\n\n## 集成义务\n\n- `zeta-model-provider-config` 提供 immutable `ProviderConfigRegistry` 和 seed，不依赖本 crate。\n- `zeta-model-provider` 持有并公开同一个 `ModelsManager` clone；`Provider::resolve_model` 消费 manager\n  的 static resolution，不再维护第二套 catalog gate。\n- Local App Server 从 provider runtime 取得该 manager；`model/list` 只投影 manager entries，Session\n  model validation 同样调用 manager。\n- 动态 provider adapter 应在 `zeta-model-provider`/`zeta-api` 边界实现 `ModelCatalogSource`，本 crate\n  不增加 provider switch。\n\n## 测试、修改影响与当前限制\n\n```text\ncargo test -p zeta-models-manager\nbazel test //zeta-rs/models-manager:models-manager-unit-tests\n```\n\n单元测试使用 fake source/fake clock，覆盖确定排序、listed/allow-unlisted、partial/complete 缺席、\nUnknown merge、fresh/stale/expired、304 generation 稳定和 per-scope singleflight。修改 merge、freshness、\nscope 或 resolution 时必须同步相应 table test、本文和系统文档；新增 protocol-visible 字段还要同步\nApp Server DTO/schema fixture。\n\n当前实现只有进程内 memory cache，没有 persisted observation、全局/per-provider 并发上限、退避抖动、\n用户 trust/policy override 或 provider 动态 adapter。App Server 仍使用既有精简 `model/list` DTO，尚未\n暴露 snapshot generation/freshness/warnings，也没有 `model/refresh` / `model/updated`。这些是系统文档\n后续阶段，不应被描述为当前行为。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "模块与内部所有权",
        "title": "模块与内部所有权"
      },
      {
        "depth": 2,
        "id": "执行路径",
        "title": "执行路径"
      },
      {
        "depth": 2,
        "id": "合并缓存与失败",
        "title": "合并、缓存与失败"
      },
      {
        "depth": 2,
        "id": "集成义务",
        "title": "集成义务"
      },
      {
        "depth": 2,
        "id": "测试修改影响与当前限制",
        "title": "测试、修改影响与当前限制"
      }
    ],
    "searchText": " 本 README 是 crate 当前实现的权威说明。跨 crate 的目录语义、provider 调研和分阶段演进见 docs/models-manager.md；provider declaration 见 zeta-model-provider-config，调用 runtime 见 zeta-model-provider。 zeta-models-manager 是 provider-independent 的模型目录控制面。它从 ProviderDefinition.models 建立静态 seed，把 provider discovery observation 合并到按 scope 隔离的 内存目录，并发布带 generation 的 immutable snapshot。它拥有 freshness、singleflight、字段级 merge、 筛选和 typed resolution；不拥有 provider HTTP DTO、credential、模型调用、prompt/context cache、 Config persistence 或 UI。 公共契约 Symbol 调用方用途 关键语义 --- --- --- ModelsManager read、refresh、list、resolve clone 共享同一个进程内 scope/cache authority CatalogScopeKey 标识 provider + endpoint/account/config revision source scope 必须是不含秘密的一向指纹 ModelCatalogSource provider runtime 实现 discovery port 返回完整或 partial observation，不提交半页结果 ModelCatalogSnapshot 上层读取 immutable catalog 仅消费者可见内容变化时 generation 递增 CatalogReadPolicy CachePreferred / RequireFresh / CacheOnly 禁止用 fresh: bool 模糊表达阻塞语义 CatalogQuery list filter availability 与 unknown capability policy 显式命名 ModelRequirements invocation-safe resolution unknown、unsupported、unavailable 和 retired 分开处理 DiscoveredCatalog source 的一次完整提交 CompleteAgentCatalog 缺席可下架；Partial 缺席不改变 availability ModelMetadataPatch provider 明确返回的字段 Unknown 不覆盖已有 known metadata ResolvedModel exact model + catalog generation + warnings AllowUnlisted 产生 unverified synthetic metadata CatalogSourceScopeId 不是 endpoint 或 credential reference。Host 必须先对 normalized endpoint、tenant、 credential revision 和 provider config revision 生成不可逆、无秘密的稳定指纹；任一输入变化都使用新 scope。晚到的旧请求只可能提交到旧 scope。 模块与内部所有权 ``text src/ ├── manager.rs read/refresh/singleflight 与 list/resolve orchestration ├── cache.rs per-scope state、clock/freshness 与 snapshot generation rebuild ├── source.rs consumer-owned discovery port 与 observation patch ├── snapshot.rs immutable snapshot、generation、provenance、warning ├── merge.rs seed/live 字段级合并与 complete/partial availability ├── filter.rs capability/availability query 与 resolution checks ├── policy.rs freshness/read named policy ├── scope.rs opaque scope identity ├── error.rs typed manager failures └── managertests.rs ` Private symbol 当前职责 不能扩张到 --- --- --- ManagedScope 一个 scope 的 snapshot state 与 async refresh gate provider transport 或 Config mutation ScopeState records、validator、freshness evidence、last refresh result durable Config/Thread authority ModelsManager::ensurescope lazy seed snapshot construction network discovery ModelsManager::commitdiscovery scope/duplicate validation 后原子提交 observation provider payload decoding rebuildsnapshot 比较 consumer-visible contents 后决定 generation 每次 read 无条件 bump generation CatalogRecord 合并中的 ModelInfo、availability、lifecycle、provenance 暴露 provider raw DTO applydiscovery complete/partial availability 与 live patch merge 按 model ID 猜 capability matchesquery / validaterequirements list 与 resolve 的 canonical gate UI 搜索或排序偏好 persistence 如果 manager 开始拼 discovery URL、读取 API key、发送 inference、解释 SSE，或者 provider runtime/UI 重新实现 ListedOnly / AllowUnlisted 判断，说明 ownership 已漂移。 执行路径 静态路径不访问网络： `text ModelsManager::staticsnapshot / liststatic / resolvestatic └─ ensurescope CatalogScopeKey::providerseed ├─ ProviderConfigRegistry::get ├─ seedrecords ProviderDefinition.models └─ ModelCatalogSnapshot generation: 1, freshness: StaticOnly ` 动态刷新路径： `text ModelsManager::refresh scope, source ├─ capture refreshserial ├─ await per-scope AsyncMutex ├─ serial changed → join prior result ├─ ModelCatalogSource::discover previous validator 不持有 catalog write lock ├─ validate exact scope + duplicate IDs ├─ applydiscovery │ ├─ Partial: 仅 observed model → Available │ └─ CompleteAgentCatalog: 缺席 record → Unavailable └─ rebuildsnapshot └─ visible contents changed → generation + 1 ` 不同 scope 使用不同 refresh gate，可以并行。相同 scope 的并发调用只执行一次 source request；等待者 复用同一成功 snapshot 或同一 typed error。future 被 drop 即为 cancellation，未返回完整 outcome 前不 修改 snapshot。 合并、缓存与失败 当前合并来源是 provider seed 与 provider live observation。Live 的明确字段覆盖 seed； ContextWindow::Unknown 和 CapabilitySupport::Unknown 不擦除 known 值。Provenance 与 ModelMetadataQuality 随 snapshot entry 暴露。Static seed 的 availability 是 Unverified，不是 账号 entitlement 证明。 Freshness 使用 manager policy 与 source 明确给出的 cache hint 中更保守的时长： 状态 CachePreferred RequireFresh CacheOnly --- --- --- --- Fresh 立即返回 立即返回 立即返回 StaleUsable 返回并后台 refresh 等待/join refresh 返回 stale Expired 等待/join refresh 等待/join refresh 返回 expired 只有静态 seed 离线时返回；有动态 source 时首次发现 有 source 时刷新，否则报错 返回 static Authentication/permission failure 把先前 Available 降为 Unverified 并保留 metadata；unsupported、 rate limit、transient、invalid payload 均保留 last-known records，并产生不包含 secret/raw body 的 warning。 Explicit refresh 仍返回 typed error，调用方可另行读取 last-known snapshot。 集成义务 - zeta-model-provider-config 提供 immutable ProviderConfigRegistry 和 seed，不依赖本 crate。 - zeta-model-provider 持有并公开同一个 ModelsManager clone；Provider::resolvemodel 消费 manager 的 static resolution，不再维护第二套 catalog gate。 - Local App Server 从 provider runtime 取得该 manager；model/list 只投影 manager entries，Session model validation 同样调用 manager。 - 动态 provider adapter 应在 zeta-model-provider/zeta-api 边界实现 ModelCatalogSource，本 crate 不增加 provider switch。 测试、修改影响与当前限制 `text cargo test -p zeta-models-manager bazel test //zeta-rs/models-manager:models-manager-unit-tests ` 单元测试使用 fake source/fake clock，覆盖确定排序、listed/allow-unlisted、partial/complete 缺席、 Unknown merge、fresh/stale/expired、304 generation 稳定和 per-scope singleflight。修改 merge、freshness、 scope 或 resolution 时必须同步相应 table test、本文和系统文档；新增 protocol-visible 字段还要同步 App Server DTO/schema fixture。 当前实现只有进程内 memory cache，没有 persisted observation、全局/per-provider 并发上限、退避抖动、 用户 trust/policy override 或 provider 动态 adapter。App Server 仍使用既有精简 model/list DTO，尚未 暴露 snapshot generation/freshness/warnings，也没有 model/refresh / model/updated`。这些是系统文档 后续阶段，不应被描述为当前行为。"
  },
  {
    "slug": "crates/utils/pdf",
    "sourcePath": "zeta-rs/utils/pdf/README.md",
    "title": "zeta-pdf",
    "description": "zeta-pdf 是 native PDF parsing boundary。它从 release-staged PDFium 动态库建立 process-local binding，并按源文档顺序返回每页原生文字与一基页码。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释当前 PDFium binding 与 page-text extraction。PDF ingestion、OCR、chunk、\n> retrieval 与 citation 的系统方向见 [`docs/pdf.md`](../../../docs/pdf.md)。\n\n`zeta-pdf` 是 native PDF parsing boundary。它从 release-staged PDFium 动态库建立 process-local\nbinding，并按源文档顺序返回每页原生文字与一基页码。\n\n当前 crate 不实现 page rendering、OCR、document identity、持久化、chunk、embedding、retrieval\n或 Agent memory。\n\n## 公共契约\n\n| Symbol | 职责 | 关键语义 |\n| --- | --- | --- |\n| `PdfiumRuntime` | 保存 exact bundled library path | 不搜索系统 library path |\n| `PdfiumRuntime::from_bundled_root` | root → platform-specific library path | root 应为 `resources/native/pdfium` |\n| `PdfiumRuntime::library_path` | 供 composition/preflight 检查 exact path | path 不是 document source |\n| `PdfTextExtractor::bind` | 检查 file 并绑定 PDFium | 启动时创建、跨 extraction 复用 |\n| `PdfTextExtractor::extract_file` | local PDF → ordered page text | 不持久化、不 OCR、不跳过 blank page |\n| `ExtractedPdfDocument` | ordered `pages` | `page_count()` 等于 vector length |\n| `ExtractedPdfPage` | `page_number + text` | page number 永远一基 |\n| `DocumentError` | missing bundled library 或 PDFium failure | caller 决定 external error redaction |\n\n`PdfTextExtractor` 持有 `pdfium_render::Pdfium`，但 PDFium handle/page/document types 不进入 public\nresult。上层只依赖 owned Rust values。\n\n## 内置运行时布局\n\n`platform_library_relative_path` 当前固定：\n\n```text\nmacOS    lib/libpdfium.dylib\nWindows  bin/pdfium.dll\nLinux    lib/libpdfium.so\nother    unsupported-platform\n```\n\nRelease build 先 stage runtime：\n\n```text\nnode scripts/fetch-pdfium.mjs --target darwin-arm64 \\\n  --output \"$STAGING/resources/native/pdfium\"\n```\n\nComposition root 显式绑定：\n\n```rust\nlet runtime =\n    PdfiumRuntime::from_bundled_root(resources_root.join(\"native/pdfium\"));\nlet extractor = PdfTextExtractor::bind(runtime)?;\n```\n\n`bind` 先用 `Path::is_file` 检查 exact library path，再调用\n`Pdfium::bind_to_library`。它不会 fallback 到 system-installed PDFium 或 ambient dynamic-library\nsearch path。Unsupported platform 当前也只会得到 missing-library error，没有独立\nUnsupportedPlatform variant。\n\n## 内部接口地图与调用图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `PdfTextExtractor::pdfium` | private field | process-local native binding | 不把 binding handle 交给 caller |\n| `platform_library_relative_path` | private function | compile-target → staged relative path | 与 fetch/staging layout lockstep |\n| page `enumerate` conversion | private path | zero-based PDFium index → one-based `u32` | conversion 只在 native boundary 出现 |\n\n```text\nPdfiumRuntime::from_bundled_root(root)\n└─ root.join(platform_library_relative_path())\n\nPdfTextExtractor::bind(runtime)\n├─ runtime.library_path.is_file\n├─ Pdfium::bind_to_library(exact path)\n└─ Pdfium::new(bindings)\n\nPdfTextExtractor::extract_file(path)\n├─ Pdfium::load_pdf_from_file(path, password=None)\n├─ document.pages().iter().enumerate()\n│  ├─ page_number = index + 1\n│  └─ page.text()?.all()\n└─ ExtractedPdfDocument { pages }\n```\n\nPage count 超过 `u32::MAX` 时，一基页码 conversion 会 panic；这被视为超出当前支持范围，而不是\n可恢复 `DocumentError`。\n\n## 输入、输出与失败\n\n`extract_file` 接受 caller 提供的 filesystem path，并以 `None` password 打开。它不会先复制文件、\n验证 ownership、限制大小、设置 deadline 或 sandbox PDFium。调用方必须确保 source 是\napplication-owned immutable file，并在更高层处理 untrusted document isolation、resource limit 与\ncancellation。\n\nBlank/image-only page 会保留为 `ExtractedPdfPage { text: \"\" }`，使上层明确决定是否进入 OCR。\n不要在本 crate 静默删除空页，否则 citation page number 会与源 PDF 漂移。\n\n`DocumentError::PdfiumLibraryMissing` 的 Display 包含 library path，`DocumentError::Pdfium` 委托\nPDFium error display。它们是 diagnostic error，不保证适合直接暴露给 external client。\n\n## 方向偏差检查\n\n- 使用 `Pdfium::bind_to_system_library` 或 ambient search：release runtime 不再 reproducible；\n- 每个 document 重新 `bind`：native lifecycle 与 composition ownership 漂移；\n- 返回 zero-based page index：citation/viewer contract 被破坏；\n- 过滤 blank page：源页码不再稳定；\n- Public API 暴露 PDFium page/handle：native backend 泄漏；\n- Crate 开始写 document DB、chunk 或 vector index：ingestion domain 下沉到 utility；\n- 把 future rendering/OCR 写成当前能力：实现状态失真。\n\n修改 staging layout 时同步检查 `platform_library_relative_path`、fetch script、runtime lock、release\npackaging 和 layout test。修改 page result 时同步检查 ingestion/citation consumer 与 native smoke\ntest。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-pdf\nbazel test //zeta-rs/utils/pdf:document-unit-tests\n```\n\n普通 tests 不加载 native library，只验证 platform staging path 与 missing-library preflight。\nOpt-in smoke test 需要：\n\n```text\nZETA_PDFIUM_ROOT=\"$STAGING/resources/native/pdfium\" \\\nZETA_PDFIUM_TEST_DOCUMENT=/absolute/path/to/fixture.pdf \\\ncargo test -p zeta-pdf\n```\n\n如果任一 env 缺失，native extraction test 会直接 skip。CI 若要宣称 native extraction 可用，必须\n显式提供两者并检查至少一页包含文字。\n\n当前只有 synchronous native text extraction。Page rendering、password input、structured error、\npage geometry、OCR hook、bounded execution 与 isolation 都是潜在扩展；它们应保持 native facts\n与 document-library orchestration 分离。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内置运行时布局",
        "title": "内置运行时布局"
      },
      {
        "depth": 2,
        "id": "内部接口地图与调用图",
        "title": "内部接口地图与调用图"
      },
      {
        "depth": 2,
        "id": "输入输出与失败",
        "title": "输入、输出与失败"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释当前 PDFium binding 与 page-text extraction。PDF ingestion、OCR、chunk、 retrieval 与 citation 的系统方向见 docs/pdf.md。 zeta-pdf 是 native PDF parsing boundary。它从 release-staged PDFium 动态库建立 process-local binding，并按源文档顺序返回每页原生文字与一基页码。 当前 crate 不实现 page rendering、OCR、document identity、持久化、chunk、embedding、retrieval 或 Agent memory。 公共契约 Symbol 职责 关键语义 --- --- --- PdfiumRuntime 保存 exact bundled library path 不搜索系统 library path PdfiumRuntime::frombundledroot root → platform-specific library path root 应为 resources/native/pdfium PdfiumRuntime::librarypath 供 composition/preflight 检查 exact path path 不是 document source PdfTextExtractor::bind 检查 file 并绑定 PDFium 启动时创建、跨 extraction 复用 PdfTextExtractor::extractfile local PDF → ordered page text 不持久化、不 OCR、不跳过 blank page ExtractedPdfDocument ordered pages pagecount 等于 vector length ExtractedPdfPage pagenumber + text page number 永远一基 DocumentError missing bundled library 或 PDFium failure caller 决定 external error redaction PdfTextExtractor 持有 pdfiumrender::Pdfium，但 PDFium handle/page/document types 不进入 public result。上层只依赖 owned Rust values。 内置运行时布局 platformlibraryrelativepath 当前固定： ``text macOS lib/libpdfium.dylib Windows bin/pdfium.dll Linux lib/libpdfium.so other unsupported-platform ` Release build 先 stage runtime： `text node scripts/fetch-pdfium.mjs --target darwin-arm64 \\ --output \"$STAGING/resources/native/pdfium\" ` Composition root 显式绑定： `rust let runtime = PdfiumRuntime::frombundledroot resourcesroot.join \"native/pdfium\" ; let extractor = PdfTextExtractor::bind runtime ?; ` bind 先用 Path::isfile 检查 exact library path，再调用 Pdfium::bindtolibrary。它不会 fallback 到 system-installed PDFium 或 ambient dynamic-library search path。Unsupported platform 当前也只会得到 missing-library error，没有独立 UnsupportedPlatform variant。 内部接口地图与调用图 Symbol 可见性 当前职责 方向约束 --- --- --- --- PdfTextExtractor::pdfium private field process-local native binding 不把 binding handle 交给 caller platformlibraryrelativepath private function compile-target → staged relative path 与 fetch/staging layout lockstep page enumerate conversion private path zero-based PDFium index → one-based u32 conversion 只在 native boundary 出现 `text PdfiumRuntime::frombundledroot root └─ root.join platformlibraryrelativepath PdfTextExtractor::bind runtime ├─ runtime.librarypath.isfile ├─ Pdfium::bindtolibrary exact path └─ Pdfium::new bindings PdfTextExtractor::extractfile path ├─ Pdfium::loadpdffromfile path, password=None ├─ document.pages .iter .enumerate │ ├─ pagenumber = index + 1 │ └─ page.text ?.all └─ ExtractedPdfDocument pages ` Page count 超过 u32::MAX 时，一基页码 conversion 会 panic；这被视为超出当前支持范围，而不是 可恢复 DocumentError。 输入、输出与失败 extractfile 接受 caller 提供的 filesystem path，并以 None password 打开。它不会先复制文件、 验证 ownership、限制大小、设置 deadline 或 sandbox PDFium。调用方必须确保 source 是 application-owned immutable file，并在更高层处理 untrusted document isolation、resource limit 与 cancellation。 Blank/image-only page 会保留为 ExtractedPdfPage text: \"\" ，使上层明确决定是否进入 OCR。 不要在本 crate 静默删除空页，否则 citation page number 会与源 PDF 漂移。 DocumentError::PdfiumLibraryMissing 的 Display 包含 library path，DocumentError::Pdfium 委托 PDFium error display。它们是 diagnostic error，不保证适合直接暴露给 external client。 方向偏差检查 - 使用 Pdfium::bindtosystemlibrary 或 ambient search：release runtime 不再 reproducible； - 每个 document 重新 bind：native lifecycle 与 composition ownership 漂移； - 返回 zero-based page index：citation/viewer contract 被破坏； - 过滤 blank page：源页码不再稳定； - Public API 暴露 PDFium page/handle：native backend 泄漏； - Crate 开始写 document DB、chunk 或 vector index：ingestion domain 下沉到 utility； - 把 future rendering/OCR 写成当前能力：实现状态失真。 修改 staging layout 时同步检查 platformlibraryrelativepath、fetch script、runtime lock、release packaging 和 layout test。修改 page result 时同步检查 ingestion/citation consumer 与 native smoke test。 测试、限制与演进 `text cargo test -p zeta-pdf bazel test //zeta-rs/utils/pdf:document-unit-tests ` 普通 tests 不加载 native library，只验证 platform staging path 与 missing-library preflight。 Opt-in smoke test 需要： `text ZETAPDFIUMROOT=\"$STAGING/resources/native/pdfium\" \\ ZETAPDFIUMTESTDOCUMENT=/absolute/path/to/fixture.pdf \\ cargo test -p zeta-pdf `` 如果任一 env 缺失，native extraction test 会直接 skip。CI 若要宣称 native extraction 可用，必须 显式提供两者并检查至少一页包含文字。 当前只有 synchronous native text extraction。Page rendering、password input、structured error、 page geometry、OCR hook、bounded execution 与 isolation 都是潜在扩展；它们应保持 native facts 与 document-library orchestration 分离。"
  },
  {
    "slug": "crates/plugin-marketplace",
    "sourcePath": "zeta-rs/plugin-marketplace/README.md",
    "title": "zeta-plugin-marketplace",
    "description": "zeta-plugin-marketplace owns verified remote distribution for product-managed Plugin Marketplaces. The cross-crate Plugin/Connector/MCP product model is canonical in docs/plugins.md; this…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-plugin-marketplace` owns verified remote distribution for product-managed Plugin\nMarketplaces. The cross-crate Plugin/Connector/MCP product model is canonical in\n[`docs/plugins.md`](../../docs/plugins.md); this README documents the exact implementation\ncontract of this crate.\n\n## Ownership\n\n`RemotePluginMarketplace` refreshes a TUF repository with a host-pinned root, verifies delegated\npublisher targets, projects signed discovery metadata into an immutable `PluginMarketplace`, and\ndefers bounded package download until an exact install or update requests the bytes.\n`RemotePluginMarketplaceConfig` is supplied by the trusted product composition root. Its transport\nmode is always remote, while `PluginMarketplaceTrust` separately distinguishes a product-managed\nsource from a host-approved external source. Untrusted App Server clients never supply URLs, roots,\ntarget names, archive paths, cache paths, or trust labels.\n\nThe key private owners are:\n\n- `MarketplaceTransport`: adapts the shared bounded HTTP client to TUF and rejects non-HTTPS\n  network fetches;\n- `metadata::published_publishers` / `metadata::published_plugins`: require every generic\n  `marketplacePackage` target to be signed by its exact `publishers/<publisher>` delegated role,\n  extract only the signed `consumerMetadata.zeta` adapter from `marketplaceCatalog`, and ignore\n  packages that do not target Zeta; legacy `zetaPlugin` targets remain read-only compatible;\n- `archive::extract`: enforces archive/entry/expanded-size limits and rejects traversal, links,\n  encryption, duplicate paths, and unsupported entries;\n- `RemotePluginMarketplace::materialize` / `catalog_packages`: verify the revocation feed and build\n  package descriptors without reading ZIP targets when signed discovery metadata is present;\n- `RemotePackageMaterializer` / `materialize_package`: reopen current TUF authority at install time,\n  reject newly revoked targets, download one exact ZIP, validate it through `LocalPluginPackage`, and\n  promote it into the digest-keyed package cache;\n- `signed_repository_digest`: binds an immutable catalog snapshot to snapshot and targets metadata,\n  including delegated role revisions;\n- `cache_repository`: retains verified metadata and the revocation target for offline browsing; it\n  does not prefetch Plugin archives;\n- `cache::prune`: bounds materialized package count and expanded bytes, evicts packages absent from\n  the current non-revoked signed target set first, then oldest materializations, and never touches\n  the separate installed content-addressed store;\n- `CacheCoordinator` / `CacheMaterializationLease`: serialize cache mutations across distributor\n  instances sharing one cache root and protect every digest still being copied into Plugin\n  authority;\n- `replace_directory` / `recover_complete_directory`: keeps the previous complete repository until\n  replacement succeeds and restores it after a crash between the two atomic renames.\n\nCall flow:\n\n```mermaid\nflowchart TD\n    C[\"product-services.json + pinned root\"] --> S[\"RemotePluginMarketplace::sync\"]\n    S --> T[\"TUF metadata, delegation, expiry, rollback\"]\n    T --> D[\"signed generic catalog + optional Zeta adapter\"]\n    D --> M[\"PluginMarketplace(RemoteManaged)\"]\n    M --> A[\"App Server discovery\"]\n    M -->|\"exact install/update\"| P[\"RemotePackageMaterializer\"]\n    P --> Z[\"one bounded ZIP + LocalPluginPackage validation\"]\n    Z --> Q[\"bounded digest materialization cache\"]\n    Q --> O[\"separate content-addressed Plugin authority store\"]\n```\n\n## Failure and offline semantics\n\n- Signature, threshold, version, expiry, delegation, target hash/length, package digest, and archive\n  failures are fail-closed.\n- Verified-external sources additionally require a host-pinned, non-empty publisher allowlist.\n  Every signed target must remain inside that scope; remote metadata cannot broaden it.\n- Only a transport failure may fall back to the last complete discovery cache. Cached metadata is\n  reopened through TUF with safe expiration enforcement; package directories are never trusted\n  without the current signed target and revocation metadata.\n- Offline browsing works while cached metadata remains valid. Offline installation works only when\n  that exact package was previously materialized; an uncached ZIP requires the distribution.\n- A catalog refresh accepts the product-independent `marketplacePackage` / `marketplaceCatalog`\n  envelope, then strict-parses only the optional Zeta consumer adapter. Packages for other consumers\n  are ignored without making the source invalid. Legacy targets without `zetaCatalog` use a\n  compatibility download during refresh until the publisher republishes generic metadata.\n- `LocalPluginPackage` retains the historical local digest algorithm for existing object stores and\n  explicitly selects `MarketplaceV1` for generic remote targets; installation preserves that choice\n  while copying and revalidating the immutable object.\n- TUF rollback metadata is copied into a staging datastore for each online refresh and committed\n  only after discovery validation and repository caching succeed. Install-time refresh commits its\n  rollback state only after the selected package passes archive and normalized-digest validation.\n- Revocations become durable exact-package tombstones in `zeta-plugins`; absence from a later feed\n  does not silently restore activation.\n- The default materialization budget is 32 packages and 1 GiB expanded bytes per Marketplace;\n  product composition may select a smaller bounded policy. Automatic reconciliation prioritizes\n  evicting unlisted or revoked digests and reports retained, evicted, and soft-excess counts/bytes.\n- The exact package currently being handed to install is protected until the authority has copied\n  it. A package larger than the selected byte budget may therefore create temporary reported\n  excess; the next unprotected sync or explicit `prune_cache` removes it. Installed objects are in\n  another store and survive cache eviction.\n\n## Extension and drift signals\n\nNew distribution protocols belong beside, not inside, `zeta-plugins` authority. Adding raw URL or\nfilesystem-path install RPCs, trusting a cached catalog without TUF reopen, treating a publisher\nrole as global revocation authority, or exposing Plugin credentials here would be architectural\ndrift.\n\nThe crate does not own Marketplace search/payment UX, Plugin activation/grants, Connector OAuth,\nMCP sessions, secret persistence, download progress UI, or installed-object retention policy.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "failure-and-offline-semantics",
        "title": "Failure and offline semantics"
      },
      {
        "depth": 2,
        "id": "extension-and-drift-signals",
        "title": "Extension and drift signals"
      }
    ],
    "searchText": "zeta-plugin-marketplace owns verified remote distribution for product-managed Plugin Marketplaces. The cross-crate Plugin/Connector/MCP product model is canonical in docs/plugins.md; this README documents the exact implementation contract of this crate. Ownership RemotePluginMarketplace refreshes a TUF repository with a host-pinned root, verifies delegated publisher targets, projects signed discovery metadata into an immutable PluginMarketplace, and defers bounded package download until an exact install or update requests the bytes. RemotePluginMarketplaceConfig is supplied by the trusted product composition root. Its transport mode is always remote, while PluginMarketplaceTrust separately distinguishes a product-managed source from a host-approved external source. Untrusted App Server clients never supply URLs, roots, target names, archive paths, cache paths, or trust labels. The key private owners are: - MarketplaceTransport: adapts the shared bounded HTTP client to TUF and rejects non-HTTPS network fetches; - metadata::publishedpublishers / metadata::publishedplugins: require every generic marketplacePackage target to be signed by its exact publishers/<publisher delegated role, extract only the signed consumerMetadata.zeta adapter from marketplaceCatalog, and ignore packages that do not target Zeta; legacy zetaPlugin targets remain read-only compatible; - archive::extract: enforces archive/entry/expanded-size limits and rejects traversal, links, encryption, duplicate paths, and unsupported entries; - RemotePluginMarketplace::materialize / catalogpackages: verify the revocation feed and build package descriptors without reading ZIP targets when signed discovery metadata is present; - RemotePackageMaterializer / materializepackage: reopen current TUF authority at install time, reject newly revoked targets, download one exact ZIP, validate it through LocalPluginPackage, and promote it into the digest-keyed package cache; - signedrepositorydigest: binds an immutable catalog snapshot to snapshot and targets metadata, including delegated role revisions; - cacherepository: retains verified metadata and the revocation target for offline browsing; it does not prefetch Plugin archives; - cache::prune: bounds materialized package count and expanded bytes, evicts packages absent from the current non-revoked signed target set first, then oldest materializations, and never touches the separate installed content-addressed store; - CacheCoordinator / CacheMaterializationLease: serialize cache mutations across distributor instances sharing one cache root and protect every digest still being copied into Plugin authority; - replacedirectory / recovercompletedirectory: keeps the previous complete repository until replacement succeeds and restores it after a crash between the two atomic renames. Call flow: ``mermaid flowchart TD C \"product-services.json + pinned root\" -- S \"RemotePluginMarketplace::sync\" S -- T \"TUF metadata, delegation, expiry, rollback\" T -- D \"signed generic catalog + optional Zeta adapter\" D -- M \"PluginMarketplace RemoteManaged \" M -- A \"App Server discovery\" M -- \"exact install/update\" P \"RemotePackageMaterializer\" P -- Z \"one bounded ZIP + LocalPluginPackage validation\" Z -- Q \"bounded digest materialization cache\" Q -- O \"separate content-addressed Plugin authority store\" ` Failure and offline semantics - Signature, threshold, version, expiry, delegation, target hash/length, package digest, and archive failures are fail-closed. - Verified-external sources additionally require a host-pinned, non-empty publisher allowlist. Every signed target must remain inside that scope; remote metadata cannot broaden it. - Only a transport failure may fall back to the last complete discovery cache. Cached metadata is reopened through TUF with safe expiration enforcement; package directories are never trusted without the current signed target and revocation metadata. - Offline browsing works while cached metadata remains valid. Offline installation works only when that exact package was previously materialized; an uncached ZIP requires the distribution. - A catalog refresh accepts the product-independent marketplacePackage / marketplaceCatalog envelope, then strict-parses only the optional Zeta consumer adapter. Packages for other consumers are ignored without making the source invalid. Legacy targets without zetaCatalog use a compatibility download during refresh until the publisher republishes generic metadata. - LocalPluginPackage retains the historical local digest algorithm for existing object stores and explicitly selects MarketplaceV1 for generic remote targets; installation preserves that choice while copying and revalidating the immutable object. - TUF rollback metadata is copied into a staging datastore for each online refresh and committed only after discovery validation and repository caching succeed. Install-time refresh commits its rollback state only after the selected package passes archive and normalized-digest validation. - Revocations become durable exact-package tombstones in zeta-plugins; absence from a later feed does not silently restore activation. - The default materialization budget is 32 packages and 1 GiB expanded bytes per Marketplace; product composition may select a smaller bounded policy. Automatic reconciliation prioritizes evicting unlisted or revoked digests and reports retained, evicted, and soft-excess counts/bytes. - The exact package currently being handed to install is protected until the authority has copied it. A package larger than the selected byte budget may therefore create temporary reported excess; the next unprotected sync or explicit prunecache removes it. Installed objects are in another store and survive cache eviction. Extension and drift signals New distribution protocols belong beside, not inside, zeta-plugins` authority. Adding raw URL or filesystem-path install RPCs, trusting a cached catalog without TUF reopen, treating a publisher role as global revocation authority, or exposing Plugin credentials here would be architectural drift. The crate does not own Marketplace search/payment UX, Plugin activation/grants, Connector OAuth, MCP sessions, secret persistence, download progress UI, or installed-object retention policy."
  },
  {
    "slug": "crates/plugins",
    "sourcePath": "zeta-rs/plugins/README.md",
    "title": "zeta-plugins",
    "description": "zeta-plugins 严格解析 declarative Plugin v1 package，验证 package-relative path 与本地文件树， 计算确定性 SHA-256 digest，并把 host 注册的 Marketplace catalog 解析为 exact、digest-pinned package。 Marketplace install/…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Plugin manifest、identity、local package validation 与 discovery 的当前实现\n> 契约。跨 crate 的安装、activation、权限和运行时演进由\n> [`docs/plugins.md`](../../docs/plugins.md) 维护；Connector account/lifecycle 由\n> [`docs/connectors.md`](../../docs/connectors.md) 维护。\n\n`zeta-plugins` 严格解析 declarative Plugin v1 package，验证 package-relative path 与本地文件树，\n计算确定性 SHA-256 digest，并把 host 注册的 Marketplace catalog 解析为 exact、digest-pinned package。\nMarketplace install/update 先复制、复验并原子提升到 content-addressed object store；durable authority\n分别持久化 installed/enabled/granted/effective refs、typed command receipts 和单调 activation generation，\n并发布 generation subscription 与 exact invocation lease。它不自行执行 Plugin，不保存 credential，\n也不解析 `SKILL.md` 或 MCP JSON-RPC。\n\n## 公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `PluginId` | 校验最长 128 bytes 的 `publisher/name` identity | display name、registry lookup |\n| `PluginVersion` | strict SemVer exact version | range resolution、update policy |\n| `PluginPackageDigest` | `sha256:<64 lowercase hex>` exact content identity | signature/trust |\n| `PluginPath` | portable ASCII、slash-separated package-relative path | host absolute path authority |\n| `PluginManifest::from_json` | strict JSON/schema/semantic validation | contribution content parsing |\n| `DeclarativeExtensionContribution` | 声明一个包含静态 `package.json` 的 package-relative 目录 | 解析 Editor contribution、执行代码 |\n| `EditorExtensionContribution` | 声明 exact executable program、Host RPC API、activation triggers 与 capability ceiling | 启动/监管进程、provider registry、RPC transport |\n| `EditorExtensionActivationEvent` | 区分 startup/command/language/on-demand 等 bounded activation trigger | 把 provider kind 当成隐式 activation |\n| `LocalPluginPackage::load` | 验证一个 exact root、digest 和所有 contribution path | copy/install/immutability |\n| `LocalPluginCatalog::discover` | 读取一个 package 或目录下的直接 package children | recursive marketplace search |\n| `PluginMarketplace::open` | 校验 host 注册 catalog、无链接 package path 与 exact package digest | 网络下载、publisher 签名、客户端宿主路径 |\n| `PluginMarketplace::from_verified_remote` | 接收产品分发层已验证的签名 manifest、digest、统计与延迟 materializer | TUF/HTTP、自动下载、grant |\n| `PluginMarketplaceMode` / `PluginMarketplaceTrust` | 分别表达读取方式与运营方信任，避免把官方远端源误标成第三方 | signature 验证、用户授权 |\n| `PluginMarketplacePackageMaterializer` / `MaterializedPluginMarketplacePackage` | 安装时把 exact ref materialize 为 canonical package，并把分发租约保持到 authority 复制完成 | 目录发现、enable、运行时执行 |\n| `PluginMarketplaceService` | Marketplace install、staged update、rollback、profile reconcile，并拒绝跨远端源的 publisher namespace owner ambiguity | 自动 grant、远端 catalog 同步、Workspace trust 决策 |\n| `PluginPackageStore::install_local` | stage、复制、复验 digest 并原子 promote immutable object | enablement、grant、activation |\n| `PluginPackageStore::read` | 按 exact installed ref 重新验证 object | authority lookup、版本选择 |\n| `PluginPackageStore::activate` | 把 exact installed refs 解析为一个 activation generation | installed/enable authority、live publish |\n| `PluginActivationSnapshot::resolve` | 拒绝重复 Plugin identity 并固定 immutable object handles | contribution runtime、profile resolution |\n| `PluginActivationAuthority` | installed/enable/disable/uninstall record、replay、generation 与 live publish | contribution parsing、runtime composition |\n| `PluginInvocationFence` / `PluginInvocationLease` | dispatch 前复核 exact digest/revision，disable/update commit 后 drain | Tool Registry replacement、MCP protocol |\n| `InstalledPluginPackage::resolve_file` / `read_utf8_file` | 在 exact object root 内解析 regular file，并提供有界 UTF-8 读取 | 任意目录扫描、MCP/Skill 内容解释 |\n\n`PluginManifest` 的 serde `Deserialize` 与 `from_json` 使用同一 semantic validation，不存在绕过\nschema version、duplicate ID、credential reference 或 permission invariant 的反序列化入口。\nprogrammatic mutation 后必须再次调用 `PluginManifest::validate`。\n\n## 清单 v1 契约\n\n当前 schema 固定：\n\n- `schemaVersion == 1`；\n- Plugin version 使用 SemVer；\n- Skill、MCP server、Connector、asset、声明式 Extension 和可执行 Editor Extension 都有稳定\n  manifest-local ID；\n- Connector 必须引用同一个 manifest 中已声明的 MCP server contribution；\n- permission 是 `process/workspace/network` tagged value；\n- network v1 只接受 exact lowercase DNS/IP host，不接受 scheme、port 或 wildcard；\n- credential slot 只能引用已声明的 `skill:<id>`、`mcp:<id>`、`connector:<id>`、\n  `asset:<id>` 或 `editorExtension:<id>`；\n- unknown field、duplicate contribution/slot/permission/host 和非 namespaced metadata 均失败；\n- Skill path 必须是包含 regular `SKILL.md` 的目录，MCP definition/process executable 必须是\n  regular file，asset 可以是 regular file 或目录；\n- `declarativeExtensions[]` path 必须是包含 regular `package.json` 的包内目录；它不请求 process\n  permission，由 `zeta-extensions` 在 exact effective Plugin snapshot 中二次验证并冻结资源；\n- `editorExtensions[]` 的 entrypoint 必须是包内 regular file，并有相同 `PluginPath` 的 exact\n  `process` permission；runtime API 仅接受数值 `1`，entrypoint 和 manifest-local ID 都必须唯一；\n- 每个 Editor Extension 的 `activationEvents` 与 capability ceiling 均为 non-empty、unique、bounded\n  typed set。command/language/debug/task/test activation selector 不能越过对应 capability ceiling。\n\n可执行声明示例：\n\n```json\n{\n  \"id\": \"review-runtime\",\n  \"entrypoint\": \"bin/review-extension-host\",\n  \"runtimeApiVersion\": 1,\n  \"activationEvents\": [\n    { \"type\": \"onCommand\", \"id\": \"acme.review.run\" },\n    { \"type\": \"onLanguage\", \"id\": \"rust\" },\n    { \"type\": \"onDemand\", \"capability\": \"testProfileProvider\" }\n  ],\n  \"capabilities\": [\"command\", \"languageProvider\", \"testProfileProvider\"]\n}\n```\n\n每个 entrypoint 都是包自己提供、可直接启动并实现 Zeta Host RPC v1 的程序；它不是交给通用\nNode/WASM loader 解释的脚本。`zeta-editor-extension-host` supervisor 负责逐扩展隔离启动和故障监管。\n`capabilities` 是 Extension Host 允许注册的最大 provider 集合，不代表这些 provider 已注册，也不代表\n某次有副作用的调用已获批准。activation event 只回答何时请求激活；`startup` 和\n`onDemand` 不会隐式增加 capability。`workspaceContains` 当前没有 Workspace-owned 的安全 scanner\nconsumer，因此 manifest v1 会按 unknown event 拒绝；不能静默忽略或由扩展进程自行遍历磁盘。\n\n这里的 exact `process` permission 是“允许 supervisor 尝试启动这一条 package-relative path”的\nactivation ceiling，不是每次 provider invocation 都直接执行 entrypoint，也不绕过 invocation lease、\nWorkspace trust 或 broker policy。package ingestion 只证明 entrypoint 是包内 regular file；它不验证\nUnix executable bit、Windows PE/扩展名、CPU ABI、代码签名或跨平台可运行性。当前 schema 也没有\nper-platform artifact selector。supervisor 必须在目标平台 fail closed 地检查 launchability，失败时报告\nactivation failure，不能把 regular-file validation 描述为可执行性保证。\n\n`PluginPath` 使用保守的跨平台子集 `[A-Za-z0-9._-]`。它拒绝 absolute path、反斜杠、空/`.`/`..`\nsegment、Windows device name、非 ASCII 名称、超过 32 层或 1024 bytes 的路径。这是 PL0\n有意选择的 portability/security contract；扩大字符集前必须先固定 Unicode normalization 与\ncase-collision 规则。\n\n## 包校验路径\n\n```text\nLocalPluginPackage::load\n├─ validate_root\n├─ digest::scan_and_digest\n│  ├─ walk_directory\n│  │  └─ reject link / hard link / special file / unsafe PluginPath / size limits\n│  └─ hash_file\n│     └─ domain + relative path length/path + file length/content\n├─ PluginManifest::from_json\n│  └─ PluginManifest::validate\n└─ validate_contribution_paths\n   ├─ require_entry\n   ├─ require_contained\n   └─ unique_location\n```\n\n安装存储路径是：\n\n```text\nPluginPackageStore::install_local\n├─ create unique staging root\n├─ copy_package_tree\n│  └─ reject changed link/special-file/file identity\n├─ LocalPluginPackage::load(staging)\n├─ compare exact id/version/digest with source snapshot\n├─ sync_directory_tree\n└─ rename staging → objects/<sha256>\n```\n\nobject 已存在时操作是幂等的，但仍重新加载并验证；source 在首次 validation 后被修改会失败，且不会\npromote partial object。这个 store 当前只接受 explicit local-development package，built-in release 和\nremote archive 由未来各自的 source/trust adapter 处理。\n\nDigest 与 source root 无关，对 normalized relative path 和每个 regular file 的 bytes 敏感。\n当前 ingestion limits：\n\n| Limit | Value |\n| --- | ---: |\n| manifest | 1 MiB |\n| single file | 16 MiB |\n| total package bytes | 256 MiB |\n| regular files | 10,000 |\n| path depth | 32 segments |\n\n`LocalPluginCatalog::discover` 若给定目录自身包含 `.zeta-plugin/`，只读取该 package；否则只检查\n直接 children。相同 `(PluginId, PluginVersion)` 出现两次会使整个 catalog 失败，不能按扫描顺序\n静默覆盖。\n\n## 内部所有权与漂移信号\n\n| Private symbol | Ownership | 修改时同步检查 |\n| --- | --- | --- |\n| `UncheckedPluginManifest` | serde shape 与 unknown-field rejection | manifest tests、schema example |\n| `PluginManifest::validate` | 跨字段 semantic invariants | credential/contribution fixtures |\n| `digest::walk_directory` | file count/type/link/path limits | package security tests、limits table |\n| `digest::hash_file` | bytes 与 file-identity stability | digest fixtures、TOCTOU assumptions |\n| `validate_contribution_paths` | contribution type、existence、containment | Skill/MCP/Editor Extension consumer contract |\n| `reject_duplicate_exact_versions` | local catalog exact-version uniqueness | future resolver semantics |\n| `PluginPackageStore::install_local` | staging/revalidation/atomic promotion boundary | authority commit、store recovery tests |\n| `PluginActivationSnapshot::resolve` | exact package set 与 generation 的不可变发布边界 | profile resolver、consumer projection tests |\n| `PluginActivationAuthority::apply` | CAS mutation、atomic authority persistence、publish-before-drain | App Server reconcile、MCP fence tests |\n| `FileAuthorityPersistence` | bounded strict JSON、0600 staging、fsync + atomic rename | recovery tests、schema migration |\n| `copy_package_tree` | copy-time entry and file-identity checks | archive ingestion、platform link semantics |\n\n出现以下变化表示 ownership 漂移：\n\n- crate 启动 process、解析 MCP protocol 或解释 `SKILL.md`；\n- local catalog 自动安装/启用 package；\n- signature 被当成 runtime approval；\n- canonical host path、secret 或完整 file content 进入 public diagnostic；\n- `zeta-config` 与 `zeta-plugins` 互相依赖。当前 `zeta-config` 的 Plugin request identity 仍是\n  config-local representation；进入 App Server contract 时应显式转换，或先将获准的共享纯\n  identity 下沉到 `zeta-protocol`。\n\n## 失败语义与集成义务\n\n`PluginErrorKind` 区分 source unavailable、unsafe package、invalid manifest、invalid\ncontribution 与 exact-version conflict。任何失败都不返回 partial package/catalog，也不改变\nfilesystem。error message 只包含稳定 identity、relative path 与 sanitized 原因。\n\n`LocalPluginPackage` 捕获的是已验证的 source snapshot identity，source directory\n仍可被外部修改。runtime consumer 不能把它当 immutable root；必须先通过 `PluginPackageStore`\n复制、重新验证并从 content-addressed object root 绑定 contribution。\n\n`PluginMarketplace::open` 的 root 只能由产品 host 注册。`PluginMarketplace::from_verified_remote`\n只接受产品分发层已经验证的 discovery metadata，并把 package bytes 延迟到安装时通过\n`PluginMarketplacePackageMaterializer` 获取；返回的 `LocalPluginPackage` 仍须与 signed manifest、ID、\nversion 和 digest 完全一致。Renderer 只能提交 Marketplace ID 与 exact package ref，不能提交宿主路径。\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-plugins\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-plugins --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/plugins:plugins-unit-tests\n```\n\n当前测试覆盖严格/重复模式、身份/SemVer、凭据引用、权限、声明式 Extension package path、Editor\nExtension v1 API/activation/capability/entrypoint、路径穿越/设备名/规范化、摘要确定性、缺失贡献、符号链接/硬链接、目录排序与\n读取、精确版本冲突，以及 Marketplace digest mismatch、staged update 和 rollback grant 边界。\n\n## 当前限制与扩展点\n\nPL1 的 content store、durable installed/enabled/granted/effective authority、exact snapshot、live\nactivation publish、Marketplace ingestion 和 user-profile reconcile 已实现。Workspace request 只做\nprofile authority 的只读可用性判断，不从 Workspace 自动安装或授予权限；authority v1→v2 migration\n会把旧 active package 保守迁移为 enabled + granted。当前 object directory 的只读性由\n“不暴露可写根路径 + digest revalidation”保证，\n尚未施加平台级 immutable flag，也没有 orphan staging startup recovery；失败 install commit 和 uninstall\n会精确回收无引用 object。受信 host 可注册 materialized Marketplace root；公网 catalog 下载、\npublisher signature/revocation、materialized package cache quota 与 GC 已由 `zeta-plugin-marketplace`\n实现；installed content store 仍只在 failed install/uninstall 时精确回收，没有独立的全局 orphan quota\nauthority。package-rooted MCP consumer\n已位于 `zeta-mcp-extension`，不能反向并入本 crate。这些能力应在新的 private\n`authority/resolution` modules 中接入，不扩大 loader/store 为隐式 enable manager。\n\n本 crate 让声明式和可执行 Editor Extension 都进入既有 Plugin package、digest 与 enable/grant\n控制面。声明式目录由 App Server 投影到 `zeta-extensions`，不获得任意执行权限；可执行声明才进入\ninvocation fence。它不包含 Extension Host executable、RPC、crash supervisor、workspace trust broker\n或 provider runtime。即使 manifest 与 exact process permission 均有效，安装也不会启动 entrypoint；Host\n仍须在 dispatch 时复核 active generation/invocation lease，并把 capability ceiling 投影为拒绝默认的\nbroker policy。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "清单-v1-契约",
        "title": "清单 v1 契约"
      },
      {
        "depth": 2,
        "id": "包校验路径",
        "title": "包校验路径"
      },
      {
        "depth": 2,
        "id": "内部所有权与漂移信号",
        "title": "内部所有权与漂移信号"
      },
      {
        "depth": 2,
        "id": "失败语义与集成义务",
        "title": "失败语义与集成义务"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 Plugin manifest、identity、local package validation 与 discovery 的当前实现 契约。跨 crate 的安装、activation、权限和运行时演进由 docs/plugins.md 维护；Connector account/lifecycle 由 docs/connectors.md 维护。 zeta-plugins 严格解析 declarative Plugin v1 package，验证 package-relative path 与本地文件树， 计算确定性 SHA-256 digest，并把 host 注册的 Marketplace catalog 解析为 exact、digest-pinned package。 Marketplace install/update 先复制、复验并原子提升到 content-addressed object store；durable authority 分别持久化 installed/enabled/granted/effective refs、typed command receipts 和单调 activation generation， 并发布 generation subscription 与 exact invocation lease。它不自行执行 Plugin，不保存 credential， 也不解析 SKILL.md 或 MCP JSON-RPC。 公共契约 Symbol 当前职责 不承担 --- --- --- PluginId 校验最长 128 bytes 的 publisher/name identity display name、registry lookup PluginVersion strict SemVer exact version range resolution、update policy PluginPackageDigest sha256:<64 lowercase hex exact content identity signature/trust PluginPath portable ASCII、slash-separated package-relative path host absolute path authority PluginManifest::fromjson strict JSON/schema/semantic validation contribution content parsing DeclarativeExtensionContribution 声明一个包含静态 package.json 的 package-relative 目录 解析 Editor contribution、执行代码 EditorExtensionContribution 声明 exact executable program、Host RPC API、activation triggers 与 capability ceiling 启动/监管进程、provider registry、RPC transport EditorExtensionActivationEvent 区分 startup/command/language/on-demand 等 bounded activation trigger 把 provider kind 当成隐式 activation LocalPluginPackage::load 验证一个 exact root、digest 和所有 contribution path copy/install/immutability LocalPluginCatalog::discover 读取一个 package 或目录下的直接 package children recursive marketplace search PluginMarketplace::open 校验 host 注册 catalog、无链接 package path 与 exact package digest 网络下载、publisher 签名、客户端宿主路径 PluginMarketplace::fromverifiedremote 接收产品分发层已验证的签名 manifest、digest、统计与延迟 materializer TUF/HTTP、自动下载、grant PluginMarketplaceMode / PluginMarketplaceTrust 分别表达读取方式与运营方信任，避免把官方远端源误标成第三方 signature 验证、用户授权 PluginMarketplacePackageMaterializer / MaterializedPluginMarketplacePackage 安装时把 exact ref materialize 为 canonical package，并把分发租约保持到 authority 复制完成 目录发现、enable、运行时执行 PluginMarketplaceService Marketplace install、staged update、rollback、profile reconcile，并拒绝跨远端源的 publisher namespace owner ambiguity 自动 grant、远端 catalog 同步、Workspace trust 决策 PluginPackageStore::installlocal stage、复制、复验 digest 并原子 promote immutable object enablement、grant、activation PluginPackageStore::read 按 exact installed ref 重新验证 object authority lookup、版本选择 PluginPackageStore::activate 把 exact installed refs 解析为一个 activation generation installed/enable authority、live publish PluginActivationSnapshot::resolve 拒绝重复 Plugin identity 并固定 immutable object handles contribution runtime、profile resolution PluginActivationAuthority installed/enable/disable/uninstall record、replay、generation 与 live publish contribution parsing、runtime composition PluginInvocationFence / PluginInvocationLease dispatch 前复核 exact digest/revision，disable/update commit 后 drain Tool Registry replacement、MCP protocol InstalledPluginPackage::resolvefile / readutf8file 在 exact object root 内解析 regular file，并提供有界 UTF-8 读取 任意目录扫描、MCP/Skill 内容解释 PluginManifest 的 serde Deserialize 与 fromjson 使用同一 semantic validation，不存在绕过 schema version、duplicate ID、credential reference 或 permission invariant 的反序列化入口。 programmatic mutation 后必须再次调用 PluginManifest::validate。 清单 v1 契约 当前 schema 固定： - schemaVersion == 1； - Plugin version 使用 SemVer； - Skill、MCP server、Connector、asset、声明式 Extension 和可执行 Editor Extension 都有稳定 manifest-local ID； - Connector 必须引用同一个 manifest 中已声明的 MCP server contribution； - permission 是 process/workspace/network tagged value； - network v1 只接受 exact lowercase DNS/IP host，不接受 scheme、port 或 wildcard； - credential slot 只能引用已声明的 skill:<id 、mcp:<id 、connector:<id 、 asset:<id 或 editorExtension:<id ； - unknown field、duplicate contribution/slot/permission/host 和非 namespaced metadata 均失败； - Skill path 必须是包含 regular SKILL.md 的目录，MCP definition/process executable 必须是 regular file，asset 可以是 regular file 或目录； - declarativeExtensions path 必须是包含 regular package.json 的包内目录；它不请求 process permission，由 zeta-extensions 在 exact effective Plugin snapshot 中二次验证并冻结资源； - editorExtensions 的 entrypoint 必须是包内 regular file，并有相同 PluginPath 的 exact process permission；runtime API 仅接受数值 1，entrypoint 和 manifest-local ID 都必须唯一； - 每个 Editor Extension 的 activationEvents 与 capability ceiling 均为 non-empty、unique、bounded typed set。command/language/debug/task/test activation selector 不能越过对应 capability ceiling。 可执行声明示例： ``json \"id\": \"review-runtime\", \"entrypoint\": \"bin/review-extension-host\", \"runtimeApiVersion\": 1, \"activationEvents\": \"type\": \"onCommand\", \"id\": \"acme.review.run\" , \"type\": \"onLanguage\", \"id\": \"rust\" , \"type\": \"onDemand\", \"capability\": \"testProfileProvider\" , \"capabilities\": \"command\", \"languageProvider\", \"testProfileProvider\" ` 每个 entrypoint 都是包自己提供、可直接启动并实现 Zeta Host RPC v1 的程序；它不是交给通用 Node/WASM loader 解释的脚本。zeta-editor-extension-host supervisor 负责逐扩展隔离启动和故障监管。 capabilities 是 Extension Host 允许注册的最大 provider 集合，不代表这些 provider 已注册，也不代表 某次有副作用的调用已获批准。activation event 只回答何时请求激活；startup 和 onDemand 不会隐式增加 capability。workspaceContains 当前没有 Workspace-owned 的安全 scanner consumer，因此 manifest v1 会按 unknown event 拒绝；不能静默忽略或由扩展进程自行遍历磁盘。 这里的 exact process permission 是“允许 supervisor 尝试启动这一条 package-relative path”的 activation ceiling，不是每次 provider invocation 都直接执行 entrypoint，也不绕过 invocation lease、 Workspace trust 或 broker policy。package ingestion 只证明 entrypoint 是包内 regular file；它不验证 Unix executable bit、Windows PE/扩展名、CPU ABI、代码签名或跨平台可运行性。当前 schema 也没有 per-platform artifact selector。supervisor 必须在目标平台 fail closed 地检查 launchability，失败时报告 activation failure，不能把 regular-file validation 描述为可执行性保证。 PluginPath 使用保守的跨平台子集 A-Za-z0-9.- 。它拒绝 absolute path、反斜杠、空/./.. segment、Windows device name、非 ASCII 名称、超过 32 层或 1024 bytes 的路径。这是 PL0 有意选择的 portability/security contract；扩大字符集前必须先固定 Unicode normalization 与 case-collision 规则。 包校验路径 `text LocalPluginPackage::load ├─ validateroot ├─ digest::scananddigest │ ├─ walkdirectory │ │ └─ reject link / hard link / special file / unsafe PluginPath / size limits │ └─ hashfile │ └─ domain + relative path length/path + file length/content ├─ PluginManifest::fromjson │ └─ PluginManifest::validate └─ validatecontributionpaths ├─ requireentry ├─ requirecontained └─ uniquelocation ` 安装存储路径是： `text PluginPackageStore::installlocal ├─ create unique staging root ├─ copypackagetree │ └─ reject changed link/special-file/file identity ├─ LocalPluginPackage::load staging ├─ compare exact id/version/digest with source snapshot ├─ syncdirectorytree └─ rename staging → objects/<sha256 ` object 已存在时操作是幂等的，但仍重新加载并验证；source 在首次 validation 后被修改会失败，且不会 promote partial object。这个 store 当前只接受 explicit local-development package，built-in release 和 remote archive 由未来各自的 source/trust adapter 处理。 Digest 与 source root 无关，对 normalized relative path 和每个 regular file 的 bytes 敏感。 当前 ingestion limits： Limit Value --- ---: manifest 1 MiB single file 16 MiB total package bytes 256 MiB regular files 10,000 path depth 32 segments LocalPluginCatalog::discover 若给定目录自身包含 .zeta-plugin/，只读取该 package；否则只检查 直接 children。相同 PluginId, PluginVersion 出现两次会使整个 catalog 失败，不能按扫描顺序 静默覆盖。 内部所有权与漂移信号 Private symbol Ownership 修改时同步检查 --- --- --- UncheckedPluginManifest serde shape 与 unknown-field rejection manifest tests、schema example PluginManifest::validate 跨字段 semantic invariants credential/contribution fixtures digest::walkdirectory file count/type/link/path limits package security tests、limits table digest::hashfile bytes 与 file-identity stability digest fixtures、TOCTOU assumptions validatecontributionpaths contribution type、existence、containment Skill/MCP/Editor Extension consumer contract rejectduplicateexactversions local catalog exact-version uniqueness future resolver semantics PluginPackageStore::installlocal staging/revalidation/atomic promotion boundary authority commit、store recovery tests PluginActivationSnapshot::resolve exact package set 与 generation 的不可变发布边界 profile resolver、consumer projection tests PluginActivationAuthority::apply CAS mutation、atomic authority persistence、publish-before-drain App Server reconcile、MCP fence tests FileAuthorityPersistence bounded strict JSON、0600 staging、fsync + atomic rename recovery tests、schema migration copypackagetree copy-time entry and file-identity checks archive ingestion、platform link semantics 出现以下变化表示 ownership 漂移： - crate 启动 process、解析 MCP protocol 或解释 SKILL.md； - local catalog 自动安装/启用 package； - signature 被当成 runtime approval； - canonical host path、secret 或完整 file content 进入 public diagnostic； - zeta-config 与 zeta-plugins 互相依赖。当前 zeta-config 的 Plugin request identity 仍是 config-local representation；进入 App Server contract 时应显式转换，或先将获准的共享纯 identity 下沉到 zeta-protocol。 失败语义与集成义务 PluginErrorKind 区分 source unavailable、unsafe package、invalid manifest、invalid contribution 与 exact-version conflict。任何失败都不返回 partial package/catalog，也不改变 filesystem。error message 只包含稳定 identity、relative path 与 sanitized 原因。 LocalPluginPackage 捕获的是已验证的 source snapshot identity，source directory 仍可被外部修改。runtime consumer 不能把它当 immutable root；必须先通过 PluginPackageStore 复制、重新验证并从 content-addressed object root 绑定 contribution。 PluginMarketplace::open 的 root 只能由产品 host 注册。PluginMarketplace::fromverifiedremote 只接受产品分发层已经验证的 discovery metadata，并把 package bytes 延迟到安装时通过 PluginMarketplacePackageMaterializer 获取；返回的 LocalPluginPackage 仍须与 signed manifest、ID、 version 和 digest 完全一致。Renderer 只能提交 Marketplace ID 与 exact package ref，不能提交宿主路径。 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-plugins cargo clippy --manifest-path Cargo.toml \\ -p zeta-plugins --all-targets --no-deps -- -D warnings bazel test //zeta-rs/plugins:plugins-unit-tests ` 当前测试覆盖严格/重复模式、身份/SemVer、凭据引用、权限、声明式 Extension package path、Editor Extension v1 API/activation/capability/entrypoint、路径穿越/设备名/规范化、摘要确定性、缺失贡献、符号链接/硬链接、目录排序与 读取、精确版本冲突，以及 Marketplace digest mismatch、staged update 和 rollback grant 边界。 当前限制与扩展点 PL1 的 content store、durable installed/enabled/granted/effective authority、exact snapshot、live activation publish、Marketplace ingestion 和 user-profile reconcile 已实现。Workspace request 只做 profile authority 的只读可用性判断，不从 Workspace 自动安装或授予权限；authority v1→v2 migration 会把旧 active package 保守迁移为 enabled + granted。当前 object directory 的只读性由 “不暴露可写根路径 + digest revalidation”保证， 尚未施加平台级 immutable flag，也没有 orphan staging startup recovery；失败 install commit 和 uninstall 会精确回收无引用 object。受信 host 可注册 materialized Marketplace root；公网 catalog 下载、 publisher signature/revocation、materialized package cache quota 与 GC 已由 zeta-plugin-marketplace 实现；installed content store 仍只在 failed install/uninstall 时精确回收，没有独立的全局 orphan quota authority。package-rooted MCP consumer 已位于 zeta-mcp-extension，不能反向并入本 crate。这些能力应在新的 private authority/resolution modules 中接入，不扩大 loader/store 为隐式 enable manager。 本 crate 让声明式和可执行 Editor Extension 都进入既有 Plugin package、digest 与 enable/grant 控制面。声明式目录由 App Server 投影到 zeta-extensions`，不获得任意执行权限；可执行声明才进入 invocation fence。它不包含 Extension Host executable、RPC、crash supervisor、workspace trust broker 或 provider runtime。即使 manifest 与 exact process permission 均有效，安装也不会启动 entrypoint；Host 仍须在 dispatch 时复核 active generation/invocation lease，并把 capability ceiling 投影为拒绝默认的 broker policy。"
  },
  {
    "slug": "crates/prompts",
    "sourcePath": "zeta-rs/prompts/README.md",
    "title": "zeta-prompts",
    "description": "zeta-prompts 只保存 Zeta 自带的模型可见提示词，并为每个资产提供稳定 ID 和 revision。它不决定 提示词何时注入；需要某类提示词的调用模块负责触发条件，并把资产交给 Core 的上下文流程。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 说明内置模型提示词的实现 ownership 和修改契约。上下文选择、instruction precedence、\n> budget 与 compaction 生命周期的跨 crate canonical 设计见 [`docs/core-context.md`](../../docs/core-context.md)。\n\n## 快速理解\n\n`zeta-prompts` 只保存 Zeta 自带的模型可见提示词，并为每个资产提供稳定 ID 和 revision。它不决定\n提示词何时注入；需要某类提示词的调用模块负责触发条件，并把资产交给 Core 的上下文流程。\n\n| 提示词类别 | 当前资产 | 注入时机 owner | 当前状态 |\n|---|---|---|---|\n| System | `SYSTEM_PROMPT` | Agent invocation/context caller | 已具备资产，尚未由 Core 自动注入 |\n| Compaction | `COMPACTION_PROMPT` | Core `ContextCompactionService` | ✅ 已接入 durable checkpoint 流程 |\n| Goals | `GOALS_PROMPT` + `render_goals_prompt` | Goal lifecycle caller | 已具备模板和类型化渲染，目标模型尚未完成 |\n| Review | `REVIEW_PROMPT` | Review caller | 已具备通用 review 资产 |\n\n`zeta-auto-review` 的专用审查 prompt 不属于这里：它必须和 response schema、`review-protocol-3`\nrevision 绑定，继续由 [`zeta-auto-review`](../auto-review/README.md) 拥有。\n\n## Ownership\n\n本 crate 拥有：\n\n- `templates/` 下四类 compile-time prompt body；\n- `PromptCategory`、`PromptArtifact` 及资产的稳定 ID/revision；\n- `GoalPromptContext`、`GoalBudget` 和带源 revision 的 `RenderedPrompt`；\n- 资产的空内容、唯一身份和末尾换行测试；\n- Cargo/Bazel 对嵌入资源的编译依赖。\n\n本 crate 不拥有：\n\n- `ThreadSnapshot`、ContextPlan、history 选择或 Tool Call/Result 配对；\n- instruction precedence、token budget、compaction checkpoint 或 durable history；\n- live Config、Skill/MCP filesystem discovery、credential、model/provider client；\n- provider-specific message role、wire JSON 或 API 请求；\n- UI 文案、审批弹窗和 shell prompt；\n- 外部 Skill、workspace instruction 与 MCP Prompt 正文的信任背书。\n\n依赖方向应保持为：\n\n```text\nprompt consumer / zeta-core Context pipeline\n                    │\n                    ▼\n              zeta-prompts\n                    │\n                    ▼\n        compile-time embedded templates\n```\n\n`zeta-prompts` 不得依赖 `zeta-core`、App Server 或 provider runtime。若未来动态模板需要上下文，\n调用方应先构造只包含必要字段的 prompt-specific projection；不得让本 crate 读取 live runtime\n状态或 `ThreadSnapshot`。\n\n## Implementation\n\n`src/artifact.rs` 的 `PromptArtifact` 是所有公共资产的共同载体：\n\n- `category()` 标识四个内置类别；\n- `id()` 是跨调用稳定的逻辑身份；\n- `revision()` 在提示词语义变化时必须 bump；\n- `body()` 返回 `include_str!` 嵌入的原始正文。\n\n`RenderedPrompt` 将动态正文和源 `PromptArtifact` 绑定，调用方可以从同一个返回值读取 body 与\nrevision，不需要手工维护两条并行数据。\n\n`src/system.rs`、`src/compact.rs` 和 `src/review.rs` 负责绑定对应模板、类别、ID 与 revision。\n`src/goals.rs` 另外负责将 prompt-specific 的目标和预算投影渲染为 `RenderedPrompt`。`src/lib.rs`\n保持模块私有并显式导出公共资产和渲染 API；不要把模板文件直接暴露成可变路径或让调用方自行\n拼接字符串。\n\n调用关系是：\n\n```text\nfeature/context caller\n  → zeta_prompts::<CATEGORY>_PROMPT or render_goals_prompt\n  → PromptArtifact / RenderedPrompt body + revision\n  → caller decides injection timing and semantic layer\n  → ContextPlan / provider-neutral ModelRequest\n```\n\n模板资源通过 `include_str!` 嵌入，因此缺失会在编译期失败。当前动态 renderer 只存在于 goals\n模块：它使用有名字的输入类型、明确的 XML text escaping 和 budget contract，并由 sibling test\nmodule 覆盖。新增 renderer 也必须遵守这一模式；不要提供 `render(String, Option<...>)` 这类无法\n表达调用意图的接口。\n\n## 修改影响\n\n修改提示词正文必须同时检查：\n\n1. 对应资产 revision；\n2. 调用方的注入条件、instruction layer、provenance 和 snapshot binding；\n3. 相关 prompt contract/snapshot/evaluation tests；\n4. [`docs/core-context.md`](../../docs/core-context.md) 中的当前状态或 ownership 描述。\n\n新增类别必须同步更新 `PromptCategory`、对应 private module、`lib.rs` export、资源 glob、唯一性测试\n和本 README。不要把外部 Skill、MCP Prompt 或专用 classifier protocol 为了复用文本迁移到本 crate。\n\n## Tests\n\n```text\ncargo test -p zeta-prompts\nbazel test //zeta-rs/prompts:prompts-unit-tests\n```\n\n`prompt_tests.rs` 验证所有内置资产具有非空正文、稳定且唯一的 ID/revision，并保持 authored trailing\nnewline；`goals_tests.rs` 验证空目标拒绝、文本 escaping、budget 渲染和 source revision binding。\n调用时机和上下文 precedence 的测试属于调用方 crate，不应复制到本 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "implementation",
        "title": "Implementation"
      },
      {
        "depth": 2,
        "id": "修改影响",
        "title": "修改影响"
      },
      {
        "depth": 2,
        "id": "tests",
        "title": "Tests"
      }
    ],
    "searchText": " 本 README 说明内置模型提示词的实现 ownership 和修改契约。上下文选择、instruction precedence、 budget 与 compaction 生命周期的跨 crate canonical 设计见 docs/core-context.md。 快速理解 zeta-prompts 只保存 Zeta 自带的模型可见提示词，并为每个资产提供稳定 ID 和 revision。它不决定 提示词何时注入；需要某类提示词的调用模块负责触发条件，并把资产交给 Core 的上下文流程。 提示词类别 当前资产 注入时机 owner 当前状态 --- --- --- --- System SYSTEMPROMPT Agent invocation/context caller 已具备资产，尚未由 Core 自动注入 Compaction COMPACTIONPROMPT Core ContextCompactionService ✅ 已接入 durable checkpoint 流程 Goals GOALSPROMPT + rendergoalsprompt Goal lifecycle caller 已具备模板和类型化渲染，目标模型尚未完成 Review REVIEWPROMPT Review caller 已具备通用 review 资产 zeta-auto-review 的专用审查 prompt 不属于这里：它必须和 response schema、review-protocol-3 revision 绑定，继续由 zeta-auto-review 拥有。 Ownership 本 crate 拥有： - templates/ 下四类 compile-time prompt body； - PromptCategory、PromptArtifact 及资产的稳定 ID/revision； - GoalPromptContext、GoalBudget 和带源 revision 的 RenderedPrompt； - 资产的空内容、唯一身份和末尾换行测试； - Cargo/Bazel 对嵌入资源的编译依赖。 本 crate 不拥有： - ThreadSnapshot、ContextPlan、history 选择或 Tool Call/Result 配对； - instruction precedence、token budget、compaction checkpoint 或 durable history； - live Config、Skill/MCP filesystem discovery、credential、model/provider client； - provider-specific message role、wire JSON 或 API 请求； - UI 文案、审批弹窗和 shell prompt； - 外部 Skill、workspace instruction 与 MCP Prompt 正文的信任背书。 依赖方向应保持为： ``text prompt consumer / zeta-core Context pipeline │ ▼ zeta-prompts │ ▼ compile-time embedded templates ` zeta-prompts 不得依赖 zeta-core、App Server 或 provider runtime。若未来动态模板需要上下文， 调用方应先构造只包含必要字段的 prompt-specific projection；不得让本 crate 读取 live runtime 状态或 ThreadSnapshot。 Implementation src/artifact.rs 的 PromptArtifact 是所有公共资产的共同载体： - category 标识四个内置类别； - id 是跨调用稳定的逻辑身份； - revision 在提示词语义变化时必须 bump； - body 返回 includestr! 嵌入的原始正文。 RenderedPrompt 将动态正文和源 PromptArtifact 绑定，调用方可以从同一个返回值读取 body 与 revision，不需要手工维护两条并行数据。 src/system.rs、src/compact.rs 和 src/review.rs 负责绑定对应模板、类别、ID 与 revision。 src/goals.rs 另外负责将 prompt-specific 的目标和预算投影渲染为 RenderedPrompt。src/lib.rs 保持模块私有并显式导出公共资产和渲染 API；不要把模板文件直接暴露成可变路径或让调用方自行 拼接字符串。 调用关系是： `text feature/context caller → zetaprompts::<CATEGORY PROMPT or rendergoalsprompt → PromptArtifact / RenderedPrompt body + revision → caller decides injection timing and semantic layer → ContextPlan / provider-neutral ModelRequest ` 模板资源通过 includestr! 嵌入，因此缺失会在编译期失败。当前动态 renderer 只存在于 goals 模块：它使用有名字的输入类型、明确的 XML text escaping 和 budget contract，并由 sibling test module 覆盖。新增 renderer 也必须遵守这一模式；不要提供 render String, Option<... 这类无法 表达调用意图的接口。 修改影响 修改提示词正文必须同时检查： 1. 对应资产 revision； 2. 调用方的注入条件、instruction layer、provenance 和 snapshot binding； 3. 相关 prompt contract/snapshot/evaluation tests； 4. docs/core-context.md 中的当前状态或 ownership 描述。 新增类别必须同步更新 PromptCategory、对应 private module、lib.rs export、资源 glob、唯一性测试 和本 README。不要把外部 Skill、MCP Prompt 或专用 classifier protocol 为了复用文本迁移到本 crate。 Tests `text cargo test -p zeta-prompts bazel test //zeta-rs/prompts:prompts-unit-tests ` prompttests.rs 验证所有内置资产具有非空正文、稳定且唯一的 ID/revision，并保持 authored trailing newline；goalstests.rs` 验证空目标拒绝、文本 escaping、budget 渲染和 source revision binding。 调用时机和上下文 precedence 的测试属于调用方 crate，不应复制到本 crate。"
  },
  {
    "slug": "crates/protocol",
    "sourcePath": "zeta-rs/protocol/README.md",
    "title": "zeta-protocol",
    "description": "zeta-protocol 是 Zeta 跨 crate、跨进程和跨客户端共享的 provider-independent 语义契约。 它定义 ID、产品模型、command、durable event、consumer update、interaction 与 model invocation values；它没有 I/O、reducer、store、actor、tra…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 canonical data contract 的代码组织、关键 symbol 与变更约束。跨 Core、store、\n> App Server 和客户端的语义方向见 [`docs/protocol.md`](../../docs/protocol.md)。\n\n`zeta-protocol` 是 Zeta 跨 crate、跨进程和跨客户端共享的 provider-independent 语义契约。\n它定义 ID、产品模型、command、durable event、consumer update、interaction 与 model invocation\nvalues；它没有 I/O、reducer、store、actor、transport、provider wire codec 或 effect policy。\n\n## 所有权规则\n\n一个 type 只有在至少两个独立组件需要以相同语义理解它时才适合进入本 crate。执行层 private\nmessage、store envelope、JSON-RPC params、provider payload 和 UI view state 不应为了“复用”进入\ncanonical protocol。\n\n```text\nserde / schemars / ts-rs\n          │\n          ▼\n   zeta-protocol\n      ▲    ▲    ▲\n      │    │    └─ app-server-protocol / clients\n      │    └────── zeta-api / model-provider\n      └─────────── core / session-store / thread-store\n```\n\n`lib.rs` 保持所有实现 module private，并显式 export public contract。新增 module 不应直接公开，\n避免调用方依赖内部文件布局。\n\n## 公共契约地图\n\n| Domain | 主要 symbols | 语义 |\n| --- | --- | --- |\n| Identity | `SessionId`, `ThreadId`, `TurnId`, `ItemId`, `CommandId`, `RequestId`, `ToolCallId` | non-empty typed string identity |\n| Skill identity | `SkillName`, `SkillSourceId`, `SkillId` | Agent Skills name + source-qualified cross-config/catalog identity |\n| Product model | `Session`, `SessionThread`, `Thread`, `Turn`, `ThreadItem` | `Session → Thread → Turn → Item` snapshot |\n| Intent | `SessionCommand`, `ThreadCommand` | 请求改变状态，不表示已发生 |\n| Durable fact | `SessionEvent`, `ThreadEvent`, `ToolExecutionAuthority` | reducer/store 接受的过去式事实 |\n| Tool execution | `ProcessExecutionOutput`, `SandboxDenialOutput`, `ToolExecutionOutput`, `ToolReplaySafety` | executor、Core 与 durable audit 共享的原始结果/重放语义 |\n| Consumer update | `SessionUpdateEnvelope`, `ThreadUpdateEnvelope`, `ThreadUpdate`, `ItemDelta` | durable committed 与 transient projection |\n| Interaction | `TurnInteraction`, `AgentRequest`, `AgentResponse`, `PendingInteraction` | Turn 等待/恢复的 typed request-response |\n| Approval | `ActionApprovalRequest`, `ActionApprovalResponse`, capability/decision enums | exact action/policy binding |\n| Model catalog | identity、`ModelInfo`、capabilities、availability/freshness/lifecycle/quality enums、`ModelPreset` | provider-neutral selection metadata；cache/snapshot orchestration 属于 `zeta-models-manager` |\n| Model invocation | `ModelRequest`, `InputItem`, `Message`, tools, `ModelResponse`, `ModelStreamEvent` | canonical model I/O |\n| Config values | `Patch<T>`, `ApprovalMode`, `SandboxMode`, `Personality`, `Theme`, `WebSearchMode` | shared pure values |\n| Stable failure | `StableTurnError`, `StableTurnErrorCode` | durable/client-safe Turn failure |\n\n除 model invocation 中需要浮点 temperature 和 arbitrary JSON tool schema 的 types 外，公共值尽量\nderive `Eq`、serde、`JsonSchema` 与 `TS`。是否生成 TypeScript/schema artifact 由\n`zeta-app-server-protocol` 负责，不在这里写文件。\n\n## 内部接口地图\n\n本 crate 大部分代码是 public data shape，private interface 数量刻意很少。下面这些 helper 承载\n关键 invariant：\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `identifier!` | private macro | 为 canonical IDs 生成 constructor、serde、schema、TS | 所有 ID deserialize 必须重用 constructor validation |\n| `validate_identifier` | crate-private | 拒绝 empty/whitespace identity | 不引入 storage/provider-specific syntax |\n| `SkillName::new` / `SkillSourceId::new` | public constructors | 校验 Agent Skills name 与 `<namespace>:skill-source:<local-id>` | 不接受 raw path，也不承担 source trust |\n| `model_identifier!` | private macro | 生成 `ProviderId`/`ModelId` contract | 保持与 canonical ID 相同的 non-empty invariant |\n| `ThreadEvent::kind` | public method | stable internal event-kind label | 与 enum variants exhaustive 同步 |\n| `ThreadEvent::thread_id` | public method | 提取 aggregate identity | 新 event 必须显式加入 match |\n| `SessionEvent::session_id` | public method | 提取 aggregate identity | 新 event 必须显式加入 match |\n| `ThreadItem::{item_id,turn_id}` | public methods | 提取 item ownership | 所有 item variants 必须有两种 typed ID |\n| `AgentRequest::kind` / `AgentResponse::kind` | public methods | request-response kind correlation | request/response family 必须 lockstep |\n| `TurnInteraction::pending_state` | public method | full durable request → redaction-safe snapshot metadata | broad snapshot 不得包含 request payload |\n| `ModelInfo::effective_auto_compact_token_limit` | public method | min(configured, 90% known context) | `Unknown` context 不伪造 limit |\n| `ModelResponse::{text,tool_calls}` | public methods | 读取 canonical output projection | 不丢弃 authoritative raw output vector |\n\n## 四类状态契约\n\n### Command\n\n`SessionCommand` 与 `ThreadCommand` 表达 typed intent：\n\n```text\nSessionCommand\n├─ Create / SetModel / Complete / Archive\n└─ CreateThread / ForkThread / RewindThread / ArchiveThread\n\nThreadCommand\n├─ StartTurn / InterruptTurn\n└─ ResolveApproval / ResolveUserInput / ResolveDynamicTool\n```\n\nCommand ID、expected sequence、receipt 和 idempotent replay 是 Core/store execution concern，不被\n塞入每个 command enum。\n\n### Event\n\n`SessionEvent`/`ThreadEvent` 只表达已经 durable 的领域事实：\n\n`ThreadEvent::HistoryImported` 是 Rewind 子 Thread 的一次性 provenance-bearing 初始化事实；它\n只能紧跟 `ThreadCreated`，只包含 source checkpoint 之前的 terminal Turns，不会截断或改写 source\nThread。\n\n```text\nCommand\n└─ Core validation + policy\n   └─ Event\n      ├─ store append envelope\n      ├─ reducer\n      └─ ThreadUpdate::Committed / SessionUpdate::Committed\n```\n\nEvent 不携带 sequence、timestamp、schema version、event ID 或 transport metadata。特别地，\n`ThreadEvent::ToolExecutionStarted` durable 地保存 action digest、policy revision 和\n`ToolExecutionAuthority`，但不负责判断 authority 是否有效。`SessionEvent::SessionModelChanged`\n保存 Session 当前模型，而 `ThreadEvent::TurnAccepted` 再保存该 Turn 的不可变模型与\n`ApprovalMode` 快照。`ApprovalMode` 的 canonical variants 是 `AskPermissions`、`AutoReview` 和\n`BypassPermissions`；旧数据缺字段时按 `AskPermissions` 读取。模式只给 policy safe point 使用，\n不能替代 action、capability、revision 或 Tool Call 的精确绑定。\n一次 sandbox denial 获得新的 exact\nauthority 后，`ThreadEvent::ToolExecutionEscalated` 在重试前保存完整\n`SandboxDenialOutput` 与新 authority；Core reducer 负责验证它只能引用 started、未完成且尚未\nescalate 的 Tool Call。\n\n### 更新\n\n`ThreadUpdate::Committed { event }` 是 durable；`ItemStarted`、`ItemDelta` 与 `PlanUpdated` 是可丢失\n的低延迟 projection。`ThreadUpdateEnvelope` 同时区分：\n\n- `durable_sequence`：aggregate 已提交事实的位置；\n- `stream_cursor`：某个 transient stream instance 内的位置。\n\n重启后 stream cursor 可以失效，durable sequence 不可以。客户端不能把 transient delta 当成最终\nThread history。\n\n### 交互\n\n`TurnInteraction` 保存完整 durable request、request ID、optional item ID 与 absolute deadline。\n`TurnInteraction::pending_state()` 只保留 request ID、kind、item ID 和 deadline，避免普通 snapshot\n成为敏感 request delivery channel。\n\n`ActionApprovalRequest::sandbox_denial` 只用于 sandbox 已明确拒绝且原 attempt\n`SafeToRetry` 的升级审批。它保存 bounded `SandboxDenialOutput`，使 Core 在审批、进程重启后仍能\n验证并审计同一个 exact Tool Call 的一次性非 sandbox authority；字段缺失表示执行前的普通审批。\n\n```text\nThreadEvent::InteractionRequested\n└─ Turn.pending_interaction = PendingInteraction\n   ├─ App Server 通过 AgentRequestEnvelope 定向投递 full interaction\n   └─ typed AgentResponse\n      └─ Resolve* ThreadCommand\n         └─ InteractionResolved / InteractionCancelled\n```\n\nConnection ownership、delivery retry、deadline timer 与 disconnect policy不属于 protocol。\n\n## 序列化契约\n\nDomain enums 通常使用 internally tagged camelCase JSON：\n\n```json\n{\n  \"type\": \"turnStarted\",\n  \"threadId\": \"thread_1\",\n  \"turnId\": \"turn_1\"\n}\n```\n\n这不是实现细节：App Server schema、TypeScript binding、fixtures、stored events 和 external\nclients 都可能依赖 field/variant spelling。Rename、retag、required/optional 改动都是 contract\nchange，不能只修 Rust compile error。\n\nTyped IDs 的 `new` 和 serde deserialize 都拒绝 empty/whitespace values。不要为外部 payload\nderive 一个绕过 constructor 的 deserialize path。\n\n## 模型契约\n\nModel types是 provider-neutral semantic IR：\n\n```text\nModelRequest\n├─ instructions\n├─ InputItem::{Message,ToolResult}\n├─ ToolDefinition / ToolChoice\n└─ reasoning / token / temperature settings\n\nModelResponse\n├─ ResponseItem::{Text,Refusal,Reasoning,ToolCall}\n├─ ModelUsage\n└─ StopReason\n```\n\n`ModelStreamEvent` 当前只有 text/reasoning delta；最终 `ModelResponse` 仍是 authoritative outcome。\nProvider endpoint、header、cache-control、JSON shape、SSE event name 和 retry hint 不进入本 crate。\n\n`ToolName` 拒绝 empty 与 provider-specific slash syntax；provider adapter 负责 wire name conversion，\n不应污染 canonical name。\n\n## 同步修改关系\n\n| 修改 | 必须同步检查 |\n| --- | --- |\n| ID type/invariant | `identifier!`、deserialize、schema/TS、store keys、contract tests |\n| Command variant | Core handler/reducer plan、App Server RPC params、idempotency tests |\n| Event variant/field | store validation、Core reducer/recovery、`kind`/ID extractor、updates、fixtures |\n| Tool execution output/replay field | executor capture、Tool adapter、Core retry gate、durable escalation、schema/TS、contract tests |\n| ThreadItem variant | item ID/turn ID extractors、Core projection、TUI/render、schema |\n| Interaction family | request/response kind、pending redaction、Core resolve command、App Server routing |\n| Model field/variant | `zeta-api` 三套 codecs、provider adapters、schema consumers |\n| serde tag/name/optionality | TypeScript、JSON Schema、golden fixtures、backward compatibility decision |\n| Stream cursor/update | broker replay/gap semantics、client subscription tests |\n\n## 方向偏差检查\n\n- Protocol type 包含 channel、lock、future、filesystem path reader 或 database handle：runtime/I/O 漂移；\n- Event 包含 sequence/schema/checksum：store envelope 漂移进 domain；\n- JSON-RPC method/request ID 进入 command：transport 漂移进 intent；\n- Provider endpoint/header/payload 进入 model types：wire protocol 漂移；\n- Reducer 或 approval decision method 进入 value types：policy/behavior 漂移；\n- `PendingInteraction` 包含 full request payload：snapshot 绕过定向 delivery；\n- Transient `ItemDelta` 被追加为 durable item：authoritative history 语义漂移；\n- Consumer 新建平行 Session/Thread/model struct：canonical source 已分叉。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-protocol\nbazel test //zeta-rs/protocol:protocol-unit-tests\n```\n\n`contract_tests.rs` 当前验证 JSON 结构、Session 谱系、持久化/临时游标分离、交互载荷脱敏、\n批准绑定、结构化沙箱升级、`UserInput` 分支、`ToolName`/ID 校验、工具身份和自动压缩限制。\n新增测试继续放在独立的相邻测试文件。\n\n当前 protocol 已覆盖 Session-first product contract 与 durable interaction 基础；multi-agent\ndelegation、shared session settings、完整 streaming tool-call delta 和更强 schema compatibility\n治理仍是潜在演进。是否进入本 crate应先证明它是共享语义，而不是为了提前占位。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权规则",
        "title": "所有权规则"
      },
      {
        "depth": 2,
        "id": "公共契约地图",
        "title": "公共契约地图"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "四类状态契约",
        "title": "四类状态契约"
      },
      {
        "depth": 3,
        "id": "command",
        "title": "Command"
      },
      {
        "depth": 3,
        "id": "event",
        "title": "Event"
      },
      {
        "depth": 3,
        "id": "更新",
        "title": "更新"
      },
      {
        "depth": 3,
        "id": "交互",
        "title": "交互"
      },
      {
        "depth": 2,
        "id": "序列化契约",
        "title": "序列化契约"
      },
      {
        "depth": 2,
        "id": "模型契约",
        "title": "模型契约"
      },
      {
        "depth": 2,
        "id": "同步修改关系",
        "title": "同步修改关系"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释 canonical data contract 的代码组织、关键 symbol 与变更约束。跨 Core、store、 App Server 和客户端的语义方向见 docs/protocol.md。 zeta-protocol 是 Zeta 跨 crate、跨进程和跨客户端共享的 provider-independent 语义契约。 它定义 ID、产品模型、command、durable event、consumer update、interaction 与 model invocation values；它没有 I/O、reducer、store、actor、transport、provider wire codec 或 effect policy。 所有权规则 一个 type 只有在至少两个独立组件需要以相同语义理解它时才适合进入本 crate。执行层 private message、store envelope、JSON-RPC params、provider payload 和 UI view state 不应为了“复用”进入 canonical protocol。 ``text serde / schemars / ts-rs │ ▼ zeta-protocol ▲ ▲ ▲ │ │ └─ app-server-protocol / clients │ └────── zeta-api / model-provider └─────────── core / session-store / thread-store ` lib.rs 保持所有实现 module private，并显式 export public contract。新增 module 不应直接公开， 避免调用方依赖内部文件布局。 公共契约地图 Domain 主要 symbols 语义 --- --- --- Identity SessionId, ThreadId, TurnId, ItemId, CommandId, RequestId, ToolCallId non-empty typed string identity Skill identity SkillName, SkillSourceId, SkillId Agent Skills name + source-qualified cross-config/catalog identity Product model Session, SessionThread, Thread, Turn, ThreadItem Session → Thread → Turn → Item snapshot Intent SessionCommand, ThreadCommand 请求改变状态，不表示已发生 Durable fact SessionEvent, ThreadEvent, ToolExecutionAuthority reducer/store 接受的过去式事实 Tool execution ProcessExecutionOutput, SandboxDenialOutput, ToolExecutionOutput, ToolReplaySafety executor、Core 与 durable audit 共享的原始结果/重放语义 Consumer update SessionUpdateEnvelope, ThreadUpdateEnvelope, ThreadUpdate, ItemDelta durable committed 与 transient projection Interaction TurnInteraction, AgentRequest, AgentResponse, PendingInteraction Turn 等待/恢复的 typed request-response Approval ActionApprovalRequest, ActionApprovalResponse, capability/decision enums exact action/policy binding Model catalog identity、ModelInfo、capabilities、availability/freshness/lifecycle/quality enums、ModelPreset provider-neutral selection metadata；cache/snapshot orchestration 属于 zeta-models-manager Model invocation ModelRequest, InputItem, Message, tools, ModelResponse, ModelStreamEvent canonical model I/O Config values Patch<T , ApprovalMode, SandboxMode, Personality, Theme, WebSearchMode shared pure values Stable failure StableTurnError, StableTurnErrorCode durable/client-safe Turn failure 除 model invocation 中需要浮点 temperature 和 arbitrary JSON tool schema 的 types 外，公共值尽量 derive Eq、serde、JsonSchema 与 TS。是否生成 TypeScript/schema artifact 由 zeta-app-server-protocol 负责，不在这里写文件。 内部接口地图 本 crate 大部分代码是 public data shape，private interface 数量刻意很少。下面这些 helper 承载 关键 invariant： Symbol 可见性 当前职责 方向约束 --- --- --- --- identifier! private macro 为 canonical IDs 生成 constructor、serde、schema、TS 所有 ID deserialize 必须重用 constructor validation validateidentifier crate-private 拒绝 empty/whitespace identity 不引入 storage/provider-specific syntax SkillName::new / SkillSourceId::new public constructors 校验 Agent Skills name 与 <namespace :skill-source:<local-id 不接受 raw path，也不承担 source trust modelidentifier! private macro 生成 ProviderId/ModelId contract 保持与 canonical ID 相同的 non-empty invariant ThreadEvent::kind public method stable internal event-kind label 与 enum variants exhaustive 同步 ThreadEvent::threadid public method 提取 aggregate identity 新 event 必须显式加入 match SessionEvent::sessionid public method 提取 aggregate identity 新 event 必须显式加入 match ThreadItem:: itemid,turnid public methods 提取 item ownership 所有 item variants 必须有两种 typed ID AgentRequest::kind / AgentResponse::kind public methods request-response kind correlation request/response family 必须 lockstep TurnInteraction::pendingstate public method full durable request → redaction-safe snapshot metadata broad snapshot 不得包含 request payload ModelInfo::effectiveautocompacttokenlimit public method min configured, 90% known context Unknown context 不伪造 limit ModelResponse:: text,toolcalls public methods 读取 canonical output projection 不丢弃 authoritative raw output vector 四类状态契约 Command SessionCommand 与 ThreadCommand 表达 typed intent： `text SessionCommand ├─ Create / SetModel / Complete / Archive └─ CreateThread / ForkThread / RewindThread / ArchiveThread ThreadCommand ├─ StartTurn / InterruptTurn └─ ResolveApproval / ResolveUserInput / ResolveDynamicTool ` Command ID、expected sequence、receipt 和 idempotent replay 是 Core/store execution concern，不被 塞入每个 command enum。 Event SessionEvent/ThreadEvent 只表达已经 durable 的领域事实： ThreadEvent::HistoryImported 是 Rewind 子 Thread 的一次性 provenance-bearing 初始化事实；它 只能紧跟 ThreadCreated，只包含 source checkpoint 之前的 terminal Turns，不会截断或改写 source Thread。 `text Command └─ Core validation + policy └─ Event ├─ store append envelope ├─ reducer └─ ThreadUpdate::Committed / SessionUpdate::Committed ` Event 不携带 sequence、timestamp、schema version、event ID 或 transport metadata。特别地， ThreadEvent::ToolExecutionStarted durable 地保存 action digest、policy revision 和 ToolExecutionAuthority，但不负责判断 authority 是否有效。SessionEvent::SessionModelChanged 保存 Session 当前模型，而 ThreadEvent::TurnAccepted 再保存该 Turn 的不可变模型与 ApprovalMode 快照。ApprovalMode 的 canonical variants 是 AskPermissions、AutoReview 和 BypassPermissions；旧数据缺字段时按 AskPermissions 读取。模式只给 policy safe point 使用， 不能替代 action、capability、revision 或 Tool Call 的精确绑定。 一次 sandbox denial 获得新的 exact authority 后，ThreadEvent::ToolExecutionEscalated 在重试前保存完整 SandboxDenialOutput 与新 authority；Core reducer 负责验证它只能引用 started、未完成且尚未 escalate 的 Tool Call。 更新 ThreadUpdate::Committed event 是 durable；ItemStarted、ItemDelta 与 PlanUpdated 是可丢失 的低延迟 projection。ThreadUpdateEnvelope 同时区分： - durablesequence：aggregate 已提交事实的位置； - streamcursor：某个 transient stream instance 内的位置。 重启后 stream cursor 可以失效，durable sequence 不可以。客户端不能把 transient delta 当成最终 Thread history。 交互 TurnInteraction 保存完整 durable request、request ID、optional item ID 与 absolute deadline。 TurnInteraction::pendingstate 只保留 request ID、kind、item ID 和 deadline，避免普通 snapshot 成为敏感 request delivery channel。 ActionApprovalRequest::sandboxdenial 只用于 sandbox 已明确拒绝且原 attempt SafeToRetry 的升级审批。它保存 bounded SandboxDenialOutput，使 Core 在审批、进程重启后仍能 验证并审计同一个 exact Tool Call 的一次性非 sandbox authority；字段缺失表示执行前的普通审批。 `text ThreadEvent::InteractionRequested └─ Turn.pendinginteraction = PendingInteraction ├─ App Server 通过 AgentRequestEnvelope 定向投递 full interaction └─ typed AgentResponse └─ Resolve ThreadCommand └─ InteractionResolved / InteractionCancelled ` Connection ownership、delivery retry、deadline timer 与 disconnect policy不属于 protocol。 序列化契约 Domain enums 通常使用 internally tagged camelCase JSON： `json \"type\": \"turnStarted\", \"threadId\": \"thread1\", \"turnId\": \"turn1\" ` 这不是实现细节：App Server schema、TypeScript binding、fixtures、stored events 和 external clients 都可能依赖 field/variant spelling。Rename、retag、required/optional 改动都是 contract change，不能只修 Rust compile error。 Typed IDs 的 new 和 serde deserialize 都拒绝 empty/whitespace values。不要为外部 payload derive 一个绕过 constructor 的 deserialize path。 模型契约 Model types是 provider-neutral semantic IR： `text ModelRequest ├─ instructions ├─ InputItem:: Message,ToolResult ├─ ToolDefinition / ToolChoice └─ reasoning / token / temperature settings ModelResponse ├─ ResponseItem:: Text,Refusal,Reasoning,ToolCall ├─ ModelUsage └─ StopReason ` ModelStreamEvent 当前只有 text/reasoning delta；最终 ModelResponse 仍是 authoritative outcome。 Provider endpoint、header、cache-control、JSON shape、SSE event name 和 retry hint 不进入本 crate。 ToolName 拒绝 empty 与 provider-specific slash syntax；provider adapter 负责 wire name conversion， 不应污染 canonical name。 同步修改关系 修改 必须同步检查 --- --- ID type/invariant identifier!、deserialize、schema/TS、store keys、contract tests Command variant Core handler/reducer plan、App Server RPC params、idempotency tests Event variant/field store validation、Core reducer/recovery、kind/ID extractor、updates、fixtures Tool execution output/replay field executor capture、Tool adapter、Core retry gate、durable escalation、schema/TS、contract tests ThreadItem variant item ID/turn ID extractors、Core projection、TUI/render、schema Interaction family request/response kind、pending redaction、Core resolve command、App Server routing Model field/variant zeta-api 三套 codecs、provider adapters、schema consumers serde tag/name/optionality TypeScript、JSON Schema、golden fixtures、backward compatibility decision Stream cursor/update broker replay/gap semantics、client subscription tests 方向偏差检查 - Protocol type 包含 channel、lock、future、filesystem path reader 或 database handle：runtime/I/O 漂移； - Event 包含 sequence/schema/checksum：store envelope 漂移进 domain； - JSON-RPC method/request ID 进入 command：transport 漂移进 intent； - Provider endpoint/header/payload 进入 model types：wire protocol 漂移； - Reducer 或 approval decision method 进入 value types：policy/behavior 漂移； - PendingInteraction 包含 full request payload：snapshot 绕过定向 delivery； - Transient ItemDelta 被追加为 durable item：authoritative history 语义漂移； - Consumer 新建平行 Session/Thread/model struct：canonical source 已分叉。 测试、限制与演进 `text cargo test -p zeta-protocol bazel test //zeta-rs/protocol:protocol-unit-tests ` contracttests.rs 当前验证 JSON 结构、Session 谱系、持久化/临时游标分离、交互载荷脱敏、 批准绑定、结构化沙箱升级、UserInput 分支、ToolName`/ID 校验、工具身份和自动压缩限制。 新增测试继续放在独立的相邻测试文件。 当前 protocol 已覆盖 Session-first product contract 与 durable interaction 基础；multi-agent delegation、shared session settings、完整 streaming tool-call delta 和更强 schema compatibility 治理仍是潜在演进。是否进入本 crate应先证明它是共享语义，而不是为了提前占位。"
  },
  {
    "slug": "crates/zeterm/renderer",
    "sourcePath": "zeterm/renderer/README.md",
    "title": "zeta-renderer",
    "description": "zeta-renderer 是 UiScene 与具体图形 API 之间的稳定边界。产品 host 保存 dyn Renderer； 组件、布局和场景构造不接触 device、queue、command encoder、render pass 或 surface。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责后端无关渲染契约的当前实现。跨 crate 的渲染所有权、替换路径与长期不变量见\n> [`docs/rendering-architecture.md`](../docs/rendering-architecture.md)。`wgpu` 后端细节见\n> [`zeta-wgpu`](../wgpu/README.md)，scene contract 见 [`zui`](../zui/README.md)。\n\n`zeta-renderer` 是 `UiScene` 与具体图形 API 之间的稳定边界。产品 host 保存 `dyn Renderer`；\n组件、布局和场景构造不接触 device、queue、command encoder、render pass 或 surface。\n\n## 1. 所有权\n\n| 能力 | 本 crate | 委托/不拥有 |\n| --- | --- | --- |\n| 后端无关的帧执行接口 | ✅ `Renderer` | |\n| Physical target size、present outcome、统一错误边界 | ✅ | |\n| Scene primitive、paint order 与 backend-neutral batch contract | ❌ | `zui` |\n| Surface、GPU resource、shader、atlas 与提交 | ❌ | `zeta-wgpu` 或其他 backend crate |\n| Window/event loop 与 redraw policy | ❌ | `zeta-winit` / product host |\n| 后端选择和初始化 | ❌ | product composition root |\n\n本 crate 禁止依赖 `wgpu`、Metal、Vulkan、DirectX、窗口系统或产品状态。出现具体图形 API 类型、\nshader 或 surface handle，意味着 ownership 已经漂移。\n\n## 2. 公共接口\n\n| Symbol | 职责 | 实现义务 |\n| --- | --- | --- |\n| `Renderer` | 执行空帧或 immutable `UiScene` | 自行拥有并验证 backend/surface lifecycle，不修改 scene |\n| `RenderTargetSize` | 明确传递 physical width/height | 零尺寸语义由 backend 处理 |\n| `RenderOutcome` | 区分 `Presented`、`Skipped`、`Retry` | host 只在 `Retry` 后调度重绘 |\n| `RendererError` | 擦除具体 backend error，同时保留 source chain | adapter 用 `RendererError::backend` 包装原始错误 |\n\n调用路径：\n\n```text\nproduct component → UiScene\nproduct host → Box<dyn Renderer>::render_scene\n             → selected backend implementation\n```\n\n`Renderer` 是 object-safe 的，后端替换不要求 product state 或 component 泛型化。初始化不是 trait\n职责，因为不同 backend 的 adapter、device selection 与 window handle 输入不同；composition root\n负责创建实现后，把它擦除为 `Box<dyn Renderer>`。\n\n## 3. 失败语义与测试\n\n后端用 `Skipped` 表示当前无需 present，用 `Retry` 表示 surface 已恢复或重新配置、host 应请求\n下一帧。不可恢复的 device、surface 或 scene prepare failure 进入 `RendererError`；本 crate 不决定\n退出、降级或 backend migration。\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-renderer\n```\n\n`renderer_tests::backend_can_be_replaced_without_changing_scene_production` 用 recording backend 验证\nscene producer 只依赖 trait。真实 GPU 与 surface 验证属于具体 backend crate。\n\n## 4. 修改影响与当前限制\n\n- 新增所有 backend 都需要的帧语义时，修改 `Renderer`、所有实现、本 README 与系统文档；\n- backend 私有的 instance packing、shader、pipeline merge 或 atlas 不得扩张本接口；跨 primitive\n  的语义绘制顺序由 `zui::SceneBatch` 统一表达；\n- 新 scene primitive 先进入 `zui`，每个 backend 再选择支持、降级或返回明确错误；\n- 当前没有 backend capability negotiation、热切换或 device-lost 后自动迁移。",
    "headings": [
      {
        "depth": 2,
        "id": "1-所有权",
        "title": "1. 所有权"
      },
      {
        "depth": 2,
        "id": "2-公共接口",
        "title": "2. 公共接口"
      },
      {
        "depth": 2,
        "id": "3-失败语义与测试",
        "title": "3. 失败语义与测试"
      },
      {
        "depth": 2,
        "id": "4-修改影响与当前限制",
        "title": "4. 修改影响与当前限制"
      }
    ],
    "searchText": " 本 README 负责后端无关渲染契约的当前实现。跨 crate 的渲染所有权、替换路径与长期不变量见 docs/rendering-architecture.md。wgpu 后端细节见 zeta-wgpu，scene contract 见 zui。 zeta-renderer 是 UiScene 与具体图形 API 之间的稳定边界。产品 host 保存 dyn Renderer； 组件、布局和场景构造不接触 device、queue、command encoder、render pass 或 surface。 1. 所有权 能力 本 crate 委托/不拥有 --- --- --- 后端无关的帧执行接口 ✅ Renderer Physical target size、present outcome、统一错误边界 ✅ Scene primitive、paint order 与 backend-neutral batch contract ❌ zui Surface、GPU resource、shader、atlas 与提交 ❌ zeta-wgpu 或其他 backend crate Window/event loop 与 redraw policy ❌ zeta-winit / product host 后端选择和初始化 ❌ product composition root 本 crate 禁止依赖 wgpu、Metal、Vulkan、DirectX、窗口系统或产品状态。出现具体图形 API 类型、 shader 或 surface handle，意味着 ownership 已经漂移。 2. 公共接口 Symbol 职责 实现义务 --- --- --- Renderer 执行空帧或 immutable UiScene 自行拥有并验证 backend/surface lifecycle，不修改 scene RenderTargetSize 明确传递 physical width/height 零尺寸语义由 backend 处理 RenderOutcome 区分 Presented、Skipped、Retry host 只在 Retry 后调度重绘 RendererError 擦除具体 backend error，同时保留 source chain adapter 用 RendererError::backend 包装原始错误 调用路径： ``text product component → UiScene product host → Box<dyn Renderer ::renderscene → selected backend implementation ` Renderer 是 object-safe 的，后端替换不要求 product state 或 component 泛型化。初始化不是 trait 职责，因为不同 backend 的 adapter、device selection 与 window handle 输入不同；composition root 负责创建实现后，把它擦除为 Box<dyn Renderer 。 3. 失败语义与测试 后端用 Skipped 表示当前无需 present，用 Retry 表示 surface 已恢复或重新配置、host 应请求 下一帧。不可恢复的 device、surface 或 scene prepare failure 进入 RendererError；本 crate 不决定 退出、降级或 backend migration。 `bash cargo test --manifest-path Cargo.toml -p zeta-renderer ` renderertests::backendcanbereplacedwithoutchangingsceneproduction 用 recording backend 验证 scene producer 只依赖 trait。真实 GPU 与 surface 验证属于具体 backend crate。 4. 修改影响与当前限制 - 新增所有 backend 都需要的帧语义时，修改 Renderer、所有实现、本 README 与系统文档； - backend 私有的 instance packing、shader、pipeline merge 或 atlas 不得扩张本接口；跨 primitive 的语义绘制顺序由 zui::SceneBatch 统一表达； - 新 scene primitive 先进入 zui`，每个 backend 再选择支持、降级或返回明确错误； - 当前没有 backend capability negotiation、热切换或 device-lost 后自动迁移。"
  },
  {
    "slug": "crates/rmcp-client",
    "sourcePath": "zeta-rs/rmcp-client/README.md",
    "title": "zeta-rmcp-客户端",
    "description": "本 crate 直接使用官方 rmcp SDK，为一个外部 MCP server 建立一个隔离 client session。它拥有 initialize、原始 tool discovery/call、server notification/elicitation 回调以及 stdio / Streamable HTTP transport convenience con…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是外部 MCP client 协议与 transport 实现的 canonical 文档。跨 crate 产品语义、\n> catalog/approval/durability 边界及 `zeta-mcp` 演进见\n> [`../../docs/mcp.md`](../../docs/mcp.md)。反方向的 Zeta MCP server 见\n> [`../../docs/mcp-server.md`](../../docs/mcp-server.md)。\n\n本 crate 直接使用官方 `rmcp` SDK，为一个外部 MCP server 建立一个隔离 client session。它拥有\ninitialize、原始 tool discovery/call、server notification/elicitation 回调以及 stdio /\nStreamable HTTP transport convenience connector；它不拥有 server 配置解析、credential\n持久化、tool alias/catalog、approval、Core tool loop 或 durable Thread result。\n\n## 文件与公共契约\n\n| 文件 / symbol | 职责 |\n| --- | --- |\n| `client.rs` / `RmcpClient` | 一个已经 initialize 的 session；并发发送原始 `tools/list` / `tools/call` |\n| `client.rs` / `RmcpClient::connect` | caller-provided RMCP transport 接入点，供 sandbox/remote launcher 或自定义 HTTP stack 使用 |\n| `client.rs` / `connect_stdio`、`connect_streamable_http` | direct local child 与 reqwest HTTP convenience connector |\n| `client.rs` / `RmcpClientOptions`、`RmcpTimeouts` | client identity、host callback 与 initialize/request/shutdown deadline |\n| `handler.rs` / `McpClientHost` | server notification 与 elicitation 的 host-owned delivery contract |\n| `handler.rs` / `McpClientEvent` | progress、server cancellation 与 catalog invalidation notification |\n| `transport.rs` | `StdioServerCommand`、`StreamableHttpServer`、redacted `BearerToken` |\n| `error.rs` / `RmcpClientError` | transport start、handshake、request deadline、service 与 shutdown failure |\n\n公共工具请求/结果直接重新导出 RMCP 模型。这是有意的低层边界；特定协议版本的数据结构\n不能从本 crate继续向 Core 或 App Server protocol 泄漏。当前 `zeta-mcp` 已把这些类型投影为\nprovider-neutral catalog、冻结 binding 与 result。\n\n## 内部接口与执行路径\n\n真实调用关系：\n\n```text\nRmcpClient::connect_stdio\n→ StdioServerCommand::into_command\n→ rmcp::transport::TokioChildProcess\n→ RmcpClient::connect\n\nRmcpClient::connect_streamable_http\n→ StreamableHttpClientTransportConfig\n→ rmcp::transport::StreamableHttpClientTransport\n→ RmcpClient::connect\n\nRmcpClient::connect\n→ ClientRuntimeHandler::new\n→ ClientServiceExt::serve_with_lifecycle(Initialize)\n→ RMCP initialize / notifications/initialized\n→ immutable server_info snapshot\n\nRmcpClient::{list_tools,list_tools_after,call_tool}\n→ RmcpClient::send_request[_with_cancellation]\n→ RunningService::send_request_with_option\n→ RmcpClient::await_request\n→ response / caller cancellation / deadline 三路竞争\n```\n\n`ClientRuntimeHandler` 是关键 private adapter：它把 RMCP progress、cancelled 和 list-changed\nnotification 转交 `McpClientHost`，把 elicitation response 送回 server。绕过它直接用\n`ClientInfo` 启动 service，会静默丢失 host interaction，属于架构漂移。\n\n`RmcpClient::await_request` 统一 caller-visible request deadline。\n`call_tool_with_cancellation` 的 caller future 或 deadline 先完成时，client 通过\n`RequestHandle::cancel` 发送 protocol cancellation；server 是否遵守取消、或是否已经产生外部\n副作用仍不能从取消/timeout 推断。上层 durable tool runtime 必须保留 unknown-outcome 语义。\n\n`shutdown(self)` 消费 session、触发 RMCP cancellation，并在 `shutdown` deadline 内等待 transport\ncleanup。stdio transport 的 cleanup 包含 child termination；HTTP transport 会尝试 session\ncleanup。只依赖 `Drop` 不保证调用方观察到 cleanup 完成。\n\n## Host 接入与可信边界\n\n- `McpClientHost::on_event` 是同步、非阻塞 callback；实现需要把 UI/RPC delivery 放入有界队列。\n- `handle_elicitation` 可以异步等待交互。`NoopMcpClientHost` 会明确 decline，不会自动批准。\n- `BearerToken` 由 auth/secret owner 在连接时注入，不持久化、不实现 `Clone`，`Debug` 永远脱敏。\n- `StdioServerCommand` convenience connector 继承宿主环境，再叠加显式 env。需要 allowlist、\n  sandbox 或 remote execution 的 host 应自行构造 transport，再调用 `RmcpClient::connect`。\n- Streamable HTTP 默认 transport 禁止 reqwest redirect（由 RMCP SDK 实施），但 OAuth、\n  credential refresh 与 credential store 不在本 crate。\n- server info、tool schema、notification 与 tool result 都是不可信远端输入；本 crate只保证\n  RMCP decode，不替代产品层的 size、policy、schema normalization 或 content safety。\n\n## 测试与修改影响\n\n```text\ncargo test -p zeta-rmcp-client\ncargo clippy -p zeta-rmcp-client --all-targets -- -D warnings\nbazel test //zeta-rs/rmcp-client:rmcp-client-unit-tests\n```\n\n`client_tests.rs` 用 duplex transport 启动真实 RMCP server/client service，覆盖 initialize、\ntools/list、tools/call、request timeout → protocol cancellation 与 shutdown；另覆盖 HTTP\nscheme、header injection 和 bearer token redaction。修改 RMCP feature、lifecycle mode、\ntimeout/cancellation、callback 或 transport 时需同步检查本 README 与\n[`../../docs/mcp.md`](../../docs/mcp.md)。\n\n## 当前限制与扩展点\n\n- Current：低层 client 已通过 `zeta-mcp` 接入 App Server/Core tools vertical slice。\n- Current：支持 tools list/call；resources、prompts、roots 与 custom request 尚未暴露。\n- Current：HTTP 支持 unauthenticated/bearer；OAuth discovery/refresh、custom header policy 与\n  credential persistence 尚未实现。\n- Current：没有 session reconnect/recovery；connection generation 与 immutable product catalog\n  由上层 `zeta-mcp` 管理。\n- Extension point：通过 `RmcpClient::connect` 注入 executor-owned stdio transport 或\n  Zeta-owned async HTTP adapter，不需要把 process/network authority移入本 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "文件与公共契约",
        "title": "文件与公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口与执行路径",
        "title": "内部接口与执行路径"
      },
      {
        "depth": 2,
        "id": "host-接入与可信边界",
        "title": "Host 接入与可信边界"
      },
      {
        "depth": 2,
        "id": "测试与修改影响",
        "title": "测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是外部 MCP client 协议与 transport 实现的 canonical 文档。跨 crate 产品语义、 catalog/approval/durability 边界及 zeta-mcp 演进见 ../../docs/mcp.md。反方向的 Zeta MCP server 见 ../../docs/mcp-server.md。 本 crate 直接使用官方 rmcp SDK，为一个外部 MCP server 建立一个隔离 client session。它拥有 initialize、原始 tool discovery/call、server notification/elicitation 回调以及 stdio / Streamable HTTP transport convenience connector；它不拥有 server 配置解析、credential 持久化、tool alias/catalog、approval、Core tool loop 或 durable Thread result。 文件与公共契约 文件 / symbol 职责 --- --- client.rs / RmcpClient 一个已经 initialize 的 session；并发发送原始 tools/list / tools/call client.rs / RmcpClient::connect caller-provided RMCP transport 接入点，供 sandbox/remote launcher 或自定义 HTTP stack 使用 client.rs / connectstdio、connectstreamablehttp direct local child 与 reqwest HTTP convenience connector client.rs / RmcpClientOptions、RmcpTimeouts client identity、host callback 与 initialize/request/shutdown deadline handler.rs / McpClientHost server notification 与 elicitation 的 host-owned delivery contract handler.rs / McpClientEvent progress、server cancellation 与 catalog invalidation notification transport.rs StdioServerCommand、StreamableHttpServer、redacted BearerToken error.rs / RmcpClientError transport start、handshake、request deadline、service 与 shutdown failure 公共工具请求/结果直接重新导出 RMCP 模型。这是有意的低层边界；特定协议版本的数据结构 不能从本 crate继续向 Core 或 App Server protocol 泄漏。当前 zeta-mcp 已把这些类型投影为 provider-neutral catalog、冻结 binding 与 result。 内部接口与执行路径 真实调用关系： ``text RmcpClient::connectstdio → StdioServerCommand::intocommand → rmcp::transport::TokioChildProcess → RmcpClient::connect RmcpClient::connectstreamablehttp → StreamableHttpClientTransportConfig → rmcp::transport::StreamableHttpClientTransport → RmcpClient::connect RmcpClient::connect → ClientRuntimeHandler::new → ClientServiceExt::servewithlifecycle Initialize → RMCP initialize / notifications/initialized → immutable serverinfo snapshot RmcpClient:: listtools,listtoolsafter,calltool → RmcpClient::sendrequest withcancellation → RunningService::sendrequestwithoption → RmcpClient::awaitrequest → response / caller cancellation / deadline 三路竞争 ` ClientRuntimeHandler 是关键 private adapter：它把 RMCP progress、cancelled 和 list-changed notification 转交 McpClientHost，把 elicitation response 送回 server。绕过它直接用 ClientInfo 启动 service，会静默丢失 host interaction，属于架构漂移。 RmcpClient::awaitrequest 统一 caller-visible request deadline。 calltoolwithcancellation 的 caller future 或 deadline 先完成时，client 通过 RequestHandle::cancel 发送 protocol cancellation；server 是否遵守取消、或是否已经产生外部 副作用仍不能从取消/timeout 推断。上层 durable tool runtime 必须保留 unknown-outcome 语义。 shutdown self 消费 session、触发 RMCP cancellation，并在 shutdown deadline 内等待 transport cleanup。stdio transport 的 cleanup 包含 child termination；HTTP transport 会尝试 session cleanup。只依赖 Drop 不保证调用方观察到 cleanup 完成。 Host 接入与可信边界 - McpClientHost::onevent 是同步、非阻塞 callback；实现需要把 UI/RPC delivery 放入有界队列。 - handleelicitation 可以异步等待交互。NoopMcpClientHost 会明确 decline，不会自动批准。 - BearerToken 由 auth/secret owner 在连接时注入，不持久化、不实现 Clone，Debug 永远脱敏。 - StdioServerCommand convenience connector 继承宿主环境，再叠加显式 env。需要 allowlist、 sandbox 或 remote execution 的 host 应自行构造 transport，再调用 RmcpClient::connect。 - Streamable HTTP 默认 transport 禁止 reqwest redirect（由 RMCP SDK 实施），但 OAuth、 credential refresh 与 credential store 不在本 crate。 - server info、tool schema、notification 与 tool result 都是不可信远端输入；本 crate只保证 RMCP decode，不替代产品层的 size、policy、schema normalization 或 content safety。 测试与修改影响 `text cargo test -p zeta-rmcp-client cargo clippy -p zeta-rmcp-client --all-targets -- -D warnings bazel test //zeta-rs/rmcp-client:rmcp-client-unit-tests ` clienttests.rs 用 duplex transport 启动真实 RMCP server/client service，覆盖 initialize、 tools/list、tools/call、request timeout → protocol cancellation 与 shutdown；另覆盖 HTTP scheme、header injection 和 bearer token redaction。修改 RMCP feature、lifecycle mode、 timeout/cancellation、callback 或 transport 时需同步检查本 README 与 ../../docs/mcp.md。 当前限制与扩展点 - Current：低层 client 已通过 zeta-mcp 接入 App Server/Core tools vertical slice。 - Current：支持 tools list/call；resources、prompts、roots 与 custom request 尚未暴露。 - Current：HTTP 支持 unauthenticated/bearer；OAuth discovery/refresh、custom header policy 与 credential persistence 尚未实现。 - Current：没有 session reconnect/recovery；connection generation 与 immutable product catalog 由上层 zeta-mcp 管理。 - Extension point：通过 RmcpClient::connect` 注入 executor-owned stdio transport 或 Zeta-owned async HTTP adapter，不需要把 process/network authority移入本 crate。"
  },
  {
    "slug": "crates/rollout",
    "sourcePath": "zeta-rs/rollout/README.md",
    "title": "zeta-rollout",
    "description": "zeta-rollout 是本地 state composition crate：在一个 profile root 下打开共享 state.sqlite3 的 typed Session/Thread stores 与 writer lease，并按 dependency order 恢复 SessionCoordinator。它不定义 event、SQLite sche…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释本地 durable repository 的组合与恢复顺序。Session/Thread store contract 分别见\n> [`zeta-session-store`](../session-store/README.md) 和\n> [`zeta-thread-store`](../thread-store/README.md)；Core ownership 见\n> [`docs/core.md`](../../docs/core.md)，workspace recovery 方向见\n> [`docs/zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md)。\n\n`zeta-rollout` 是本地 state composition crate：在一个 profile root 下打开共享\n`state.sqlite3` 的 typed Session/Thread stores 与 writer lease，并按 dependency order 恢复\n`SessionCoordinator`。它不定义 event、SQLite schema、reducer 或 trace format；crate 名是迁移\nSQLite 前的历史名称。\n\n## 公共与内部接口\n\n| Symbol | 可见性 | 职责 |\n| --- | --- | --- |\n| `LocalStateRepository` | public | 持有同一数据库的 `SqliteSessionStore`、`SqliteThreadStore` 与同一 profile 的 `LeaseDirectory` |\n| `LocalStateRepository::open` | public | 打开 `<profile_root>/state.sqlite3` 与 `<profile_root>/leases` |\n| `database_path` | public | 暴露同一 SQLite path，供 Config authority 加入同一 profile 数据库 |\n| `session_store` | public | 以 `Arc<dyn SessionStore>` 暴露 typed history port |\n| `thread_store` | public | 以 `Arc<dyn ThreadStore>` 暴露 typed history port |\n| `recover_coordinator` | public | 唯一受支持的 Core runtime recovery construction |\n| `LocalStateError` | public | 保留 Core、SessionStore、ThreadStore error domain |\n| repository fields | private | 防止调用方拆开 recovery generation 或替换其中一个 store |\n\n```text\nLocalStateRepository::open(profile_root)\n├─ SqliteSessionStore::open(profile_root/state.sqlite3)\n├─ SqliteThreadStore::open(profile_root/state.sqlite3)\n└─ LeaseDirectory::open(profile_root/leases)\n\nLocalStateRepository::recover_coordinator\n├─ ThreadController::with_store_and_lease\n├─ ThreadStore::list_thread_ids\n├─ ThreadController::recover_thread(each)\n├─ SessionCoordinator::with_store_and_lease\n├─ SessionStore::list_session_ids\n├─ SessionCoordinator::recover_session(each)\n└─ return Arc<SessionCoordinator>\n```\n\nThread 必须先恢复，因为 Session recovery 可能 reconcile 一个 durable\n`ThreadCreationPlanned` saga。交换这两个循环不是重构，而是 recovery semantic change。\n\n方向偏差：\n\n- App Server 自行打开 stores/leases：composition ownership 被复制；\n- Session 在 Thread 前恢复：create/fork saga 可能错误补偿；\n- `LocalStateRepository` 定义新 event envelope 或 SQLite table：侵入 store/protocol ownership；\n- trace/export write 回 repository：read-only diagnostics 变成第二 authority；\n- 一个 store 或 lease 使用不同 root generation：runtime 不再代表一致 repository。\n\n## 失败与生命周期\n\n`LocalStateError` 不扁平化来源：open/recovery caller 可以区分 Core recovery、Session store 与 Thread\nstore failure。`open` 只创建 storage handles；只有 `recover_coordinator` 构造并恢复 runtime。\n\nRepository 返回 cloned `Arc<dyn ...Store>`，它们属于同一 opened database generation。profile\n变化应创建新的 repository，不应原地替换内部 backend。普通 Config 变化不重开 repository；\n`ConfigStore` 使用该 repository 暴露的 `database_path` 打开自己的 authority tables。\n\n## 测试与修改路径\n\n```text\ncargo test -p zeta-rollout\nbazel test //zeta-rs/rollout:rollout-unit-tests\n```\n\n测试使用真实临时 root，验证创建 Session/Thread 后重新打开 repository 可以恢复 membership 与\nThread history。修改恢复顺序时必须增加 interrupted create/fork saga case，而不只测试 happy path。\n\n## 当前限制与演进\n\n当前只组合本地 SQLite `zeta-storage` backend，没有 remote repository、encryption policy、\nmulti-process coordination protocol、旧 JSONL import 或 lazy recovery。未来 backend 可以变化，\n但 typed store ports、shared lease generation 和 Thread-before-Session recovery 是稳定边界。",
    "headings": [
      {
        "depth": 2,
        "id": "公共与内部接口",
        "title": "公共与内部接口"
      },
      {
        "depth": 2,
        "id": "失败与生命周期",
        "title": "失败与生命周期"
      },
      {
        "depth": 2,
        "id": "测试与修改路径",
        "title": "测试与修改路径"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 解释本地 durable repository 的组合与恢复顺序。Session/Thread store contract 分别见 zeta-session-store 和 zeta-thread-store；Core ownership 见 docs/core.md，workspace recovery 方向见 docs/zeta-rs-architecture.md。 zeta-rollout 是本地 state composition crate：在一个 profile root 下打开共享 state.sqlite3 的 typed Session/Thread stores 与 writer lease，并按 dependency order 恢复 SessionCoordinator。它不定义 event、SQLite schema、reducer 或 trace format；crate 名是迁移 SQLite 前的历史名称。 公共与内部接口 Symbol 可见性 职责 --- --- --- LocalStateRepository public 持有同一数据库的 SqliteSessionStore、SqliteThreadStore 与同一 profile 的 LeaseDirectory LocalStateRepository::open public 打开 <profileroot /state.sqlite3 与 <profileroot /leases databasepath public 暴露同一 SQLite path，供 Config authority 加入同一 profile 数据库 sessionstore public 以 Arc<dyn SessionStore 暴露 typed history port threadstore public 以 Arc<dyn ThreadStore 暴露 typed history port recovercoordinator public 唯一受支持的 Core runtime recovery construction LocalStateError public 保留 Core、SessionStore、ThreadStore error domain repository fields private 防止调用方拆开 recovery generation 或替换其中一个 store ``text LocalStateRepository::open profileroot ├─ SqliteSessionStore::open profileroot/state.sqlite3 ├─ SqliteThreadStore::open profileroot/state.sqlite3 └─ LeaseDirectory::open profileroot/leases LocalStateRepository::recovercoordinator ├─ ThreadController::withstoreandlease ├─ ThreadStore::listthreadids ├─ ThreadController::recoverthread each ├─ SessionCoordinator::withstoreandlease ├─ SessionStore::listsessionids ├─ SessionCoordinator::recoversession each └─ return Arc<SessionCoordinator ` Thread 必须先恢复，因为 Session recovery 可能 reconcile 一个 durable ThreadCreationPlanned saga。交换这两个循环不是重构，而是 recovery semantic change。 方向偏差： - App Server 自行打开 stores/leases：composition ownership 被复制； - Session 在 Thread 前恢复：create/fork saga 可能错误补偿； - LocalStateRepository 定义新 event envelope 或 SQLite table：侵入 store/protocol ownership； - trace/export write 回 repository：read-only diagnostics 变成第二 authority； - 一个 store 或 lease 使用不同 root generation：runtime 不再代表一致 repository。 失败与生命周期 LocalStateError 不扁平化来源：open/recovery caller 可以区分 Core recovery、Session store 与 Thread store failure。open 只创建 storage handles；只有 recovercoordinator 构造并恢复 runtime。 Repository 返回 cloned Arc<dyn ...Store ，它们属于同一 opened database generation。profile 变化应创建新的 repository，不应原地替换内部 backend。普通 Config 变化不重开 repository； ConfigStore 使用该 repository 暴露的 databasepath 打开自己的 authority tables。 测试与修改路径 `text cargo test -p zeta-rollout bazel test //zeta-rs/rollout:rollout-unit-tests ` 测试使用真实临时 root，验证创建 Session/Thread 后重新打开 repository 可以恢复 membership 与 Thread history。修改恢复顺序时必须增加 interrupted create/fork saga case，而不只测试 happy path。 当前限制与演进 当前只组合本地 SQLite zeta-storage` backend，没有 remote repository、encryption policy、 multi-process coordination protocol、旧 JSONL import 或 lazy recovery。未来 backend 可以变化， 但 typed store ports、shared lease generation 和 Thread-before-Session recovery 是稳定边界。"
  },
  {
    "slug": "crates/rollout-trace",
    "sourcePath": "zeta-rs/rollout-trace/README.md",
    "title": "zeta-rollout-trace",
    "description": "zeta-rollout-trace 从一个 Session topology stream 和它计划创建的 Thread streams 生成可序列化 诊断 artifact。Trace 保留每个 aggregate 的原 sequence，不制造虚假的 global ordering，也不能 成为 runtime authority。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 read-only trace capture。Durable source contract 见\n> [`zeta-history`](../history/README.md)、[`zeta-session-store`](../session-store/README.md) 与\n> [`zeta-thread-store`](../thread-store/README.md)；跨系统 durability/privacy 方向见\n> [`docs/zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md)。\n\n`zeta-rollout-trace` 从一个 Session topology stream 和它计划创建的 Thread streams 生成可序列化\n诊断 artifact。Trace 保留每个 aggregate 的原 sequence，不制造虚假的 global ordering，也不能\n成为 runtime authority。\n\n## 接口地图\n\n| Symbol | 可见性 | 职责 |\n| --- | --- | --- |\n| `capture_session_trace` | public function | 读取 Session，再按 `ThreadCreationPlanned` 读取 child Thread |\n| `RolloutTrace` | public struct | format version、Session ID、Session events、Thread traces |\n| `ThreadRolloutTrace` | public struct | 一个 planned Thread 的 ID 与原始 `zeta_history::StoredEvent` |\n| `ROLLOUT_TRACE_FORMAT_VERSION` | public constant | self-contained trace artifact version，当前为 `1` |\n| `RolloutTraceError` | public enum | Session missing 或 source store failure |\n| `seen_thread_ids` | private local set | 保持首次计划顺序并去重重复 planned event |\n\n```text\ncapture_session_trace(session_store, thread_store, session_id)\n├─ SessionStore::load\n├─ empty → SessionNotFound\n├─ iterate stored Session events in source order\n├─ select SessionEvent::ThreadCreationPlanned\n├─ deduplicate ThreadId\n├─ ThreadStore::load(each)\n└─ RolloutTrace { independent sequences preserved }\n```\n\n一个 planned Thread 即使 events 为空也必须包含在 trace 中；它表示 saga 已 durable plan、但 child\nstream 尚未创建。静默丢弃会抹去最需要诊断的中断状态。\n\n方向偏差：\n\n- 按 timestamp 合并 Session/Thread events：伪造 total order；\n- 从 existing Thread directory 推断 membership：绕过 Session authority；\n- reducer 后只导出 projection：丢失原始 recovery evidence；\n- crate 提供默认文件上传：把含敏感内容的 artifact 变成隐式 exfiltration path；\n- trace 被输入 runtime decision：diagnostic artifact 成为第二 authority。\n\n## Privacy、错误与测试\n\nTrace 可能含用户输入、Tool arguments/results 和 external identifiers。本 crate 只返回内存 value，\n不写文件、不上传、不脱敏。调用方拥有 redaction、access control、retention 和 sharing policy。\n\n`ThreadStore` error 包含 exact `thread_id` context；Session 空 history 映射为\n`SessionNotFound`，不是一个合法空 trace。\n\n```text\ncargo test -p zeta-rollout-trace\nbazel test //zeta-rs/rollout-trace:rollout-trace-unit-tests\n```\n\n修改 trace field 时 bump format version 并审查 downstream readers。当前没有 streaming capture、\nfilter/redaction policy、partial range 或 cross-Session export；这些未来能力不得改变 source\nsequence 或 read-only 性质。",
    "headings": [
      {
        "depth": 2,
        "id": "接口地图",
        "title": "接口地图"
      },
      {
        "depth": 2,
        "id": "privacy错误与测试",
        "title": "Privacy、错误与测试"
      }
    ],
    "searchText": " 本 README 解释 read-only trace capture。Durable source contract 见 zeta-history、zeta-session-store 与 zeta-thread-store；跨系统 durability/privacy 方向见 docs/zeta-rs-architecture.md。 zeta-rollout-trace 从一个 Session topology stream 和它计划创建的 Thread streams 生成可序列化 诊断 artifact。Trace 保留每个 aggregate 的原 sequence，不制造虚假的 global ordering，也不能 成为 runtime authority。 接口地图 Symbol 可见性 职责 --- --- --- capturesessiontrace public function 读取 Session，再按 ThreadCreationPlanned 读取 child Thread RolloutTrace public struct format version、Session ID、Session events、Thread traces ThreadRolloutTrace public struct 一个 planned Thread 的 ID 与原始 zetahistory::StoredEvent ROLLOUTTRACEFORMATVERSION public constant self-contained trace artifact version，当前为 1 RolloutTraceError public enum Session missing 或 source store failure seenthreadids private local set 保持首次计划顺序并去重重复 planned event ``text capturesessiontrace sessionstore, threadstore, sessionid ├─ SessionStore::load ├─ empty → SessionNotFound ├─ iterate stored Session events in source order ├─ select SessionEvent::ThreadCreationPlanned ├─ deduplicate ThreadId ├─ ThreadStore::load each └─ RolloutTrace independent sequences preserved ` 一个 planned Thread 即使 events 为空也必须包含在 trace 中；它表示 saga 已 durable plan、但 child stream 尚未创建。静默丢弃会抹去最需要诊断的中断状态。 方向偏差： - 按 timestamp 合并 Session/Thread events：伪造 total order； - 从 existing Thread directory 推断 membership：绕过 Session authority； - reducer 后只导出 projection：丢失原始 recovery evidence； - crate 提供默认文件上传：把含敏感内容的 artifact 变成隐式 exfiltration path； - trace 被输入 runtime decision：diagnostic artifact 成为第二 authority。 Privacy、错误与测试 Trace 可能含用户输入、Tool arguments/results 和 external identifiers。本 crate 只返回内存 value， 不写文件、不上传、不脱敏。调用方拥有 redaction、access control、retention 和 sharing policy。 ThreadStore error 包含 exact threadid context；Session 空 history 映射为 SessionNotFound，不是一个合法空 trace。 `text cargo test -p zeta-rollout-trace bazel test //zeta-rs/rollout-trace:rollout-trace-unit-tests `` 修改 trace field 时 bump format version 并审查 downstream readers。当前没有 streaming capture、 filter/redaction policy、partial range 或 cross-Session export；这些未来能力不得改变 source sequence 或 read-only 性质。"
  },
  {
    "slug": "crates/search",
    "sourcePath": "zeta-rs/search/README.md",
    "title": "zeta-search",
    "description": "zeta-search 在 host 提供的已授权搜索作用域内检索文件内容；当前默认且唯一的作用域是主工作 目录。它支持文字、正则、大小写策略和文件 glob。 它不搜索路径名称：zeta-file-search 继续独占文件路径的遍历、ignore 处理和 fuzzy matching。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有跨文件内容搜索的实现契约：查询校验、冻结 `rg` 执行、结果解析、分页作业和\n> owner 隔离。产品语义与跨进程所有权由 [`docs/search.md`](../../docs/search.md) canonical\n> 维护；App Server 的 RPC DTO 转换见\n> [`zeta-rs/app-server/README.md`](../app-server/README.md)。\n\n`zeta-search` 在 host 提供的已授权搜索作用域内检索文件内容；当前默认且唯一的作用域是主工作\n目录。它支持文字、正则、大小写策略和文件 glob。\n它不搜索路径名称：`zeta-file-search` 继续独占文件路径的遍历、ignore 处理和 fuzzy matching。\n\n## 快速理解\n\n调用方传入由可信 host 建立的 `WorkspaceRoot`、冻结的 `RipgrepExecutable`、`SearchOwner` 和\n`SearchQuery`。`SearchService` 启动受限后台作业，调用方随后按游标读取 `SearchPage`，或\n取消作业。crate 不知道 JSON-RPC、Desktop、模型 Tool 或 connection 的含义。\n\n| 需求 | `zeta-search` | `zeta-file-search` |\n| --- | --- | --- |\n| 找到包含文字或正则的文件行 | `SearchService` | 不负责 |\n| 文件名 / 路径 fuzzy match | 不负责 | `PathSearchHandle` |\n| 读取候选文件内容 | 通过冻结 `rg` | 不读取内容 |\n| 结果形状 | path、行号、preview、UTF-16 ranges | path、score、匹配字符 indices |\n| UI、RPC、模型 Tool | 不负责 | 不负责 |\n\n## 所有权与边界\n\n当前 crate 负责：\n\n- 文字/正则查询和 include/exclude glob 的输入上限与 containment 校验；\n- 无 shell 的冻结 `rg` 进程启动、取消、JSON 输出解析和稳定错误收束；\n- UTF-8 byte offset 到 UTF-16 preview range 的转换；\n- 最多 32 个 owner-bound job、分页读取、5 分钟完成作业保留和全量取消。\n\n当前 crate 不负责：\n\n- 工作区信任决策、`rg` discovery 或任意 executable 选择；host 在构造\n  `SearchService` 前完成这些工作；\n- 文件路径 fuzzy 搜索、目录树、Editor buffer overlay 或 Renderer 的结果分组；\n- JSON-RPC DTO、connection ID、搜索表单、模型 Tool 和批准策略。\n\n如果这个 crate 开始依赖 `zeta-app-server-protocol`、`zeta-file-search`、Desktop 或模型 Tool，说明\n搜索执行边界已经漂移。协议适配属于 App Server；路径 fuzzy 属于 `zeta-file-search`。\n\n## 文件与职责\n\n```text\nsrc/\n├── lib.rs                # 只导出稳定的领域 API\n├── types.rs              # query、page、owner、match 和 error contract\n├── service.rs            # SearchService、rg lifecycle、validation 与 parsing\n└── service_tests.rs      # argv、glob、UTF-16 range 和结果上限测试\n```\n\n`service.rs` 只持有 `WorkspaceRoot`、冻结 `RipgrepExecutable` 和 job state。`types.rs` 不引用\nApp Server protocol，因此 CLI、TUI 或未来其他 host 可以自行将输入映射为 `SearchQuery`。\n\n## 公共契约\n\n| Symbol | 调用方做什么 | crate 保证什么 |\n| --- | --- | --- |\n| `SearchService::new` | 传入可信 root 与已冻结 `rg` | 不自行从 `PATH` discovery executable |\n| `switch_workspace` | 在 host 切换 root 后调用 | 仅影响之后启动的作业；已有作业保留开始时的 root |\n| `start` | 传入 `SearchOwner` 和 `SearchQuery` | 校验输入、分配 opaque ID、启动有界作业 |\n| `read` | 传入同一 owner、ID 和 cursor | 返回最多 200 条、不泄漏其他 owner 的作业 |\n| `cancel` | 传入同一 owner 和 ID | 停止并释放一个作业 |\n| `cancel_all` | workspace 退休、撤销信任或服务关闭时调用 | 取消并释放所有尚存作业 |\n\n`SearchOwner` 是 crate 不解释的 opaque `u64`。App Server 当前以 connection ID 建立它，但该映射\n不属于本 crate 的持久化或协议契约。`SearchPage.error` 只包含稳定、已脱敏的运行/解析说明；原始\nstderr 不会返回给调用方。\n\n## 执行路径与失败语义\n\n```text\ntrusted host root + frozen rg\n  → SearchService::start(owner, query)\n  → validate_query / validate_glob\n  → typed rg argv, shell-free spawn\n  → parse JSON matches into SearchMatch\n  → owner-bound SearchPage reads\n  → cancel / retention cleanup / cancel_all\n```\n\n- 无效查询、glob、游标或 batch size：返回 `SearchError::InvalidInput`；\n- job 不存在或已被释放：返回 `NotFound`；不同 owner：返回 `NotOwner`；服务容量或 poisoned state：\n  返回 `Busy`；\n- `rg` 无法启动、输出无法读取或 JSON 不合法：作业标记完成，并在 `SearchPage.error` 返回稳定说明；\n- 达到结果上限时，只有确实观察到额外匹配才设置 `limit_hit`；\n- 取消会终止子进程；已完成 job 最多保留五分钟，供调用方读取最后 batch。\n\n## 验证与修改影响\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-search\ncargo test --manifest-path Cargo.toml -p zeta-app-server workspace_search --lib\nbazel test //zeta-rs/search:search-unit-tests\n```\n\n修改 argv、输入校验、range 转换、分页、取消或 owner 规则时，必须同步检查 `service_tests.rs`、\nApp Server `search_operations.rs` 的 DTO 映射、[`docs/search.md`](../../docs/search.md) 和该 README。\n\n## 当前限制与潜在演进\n\n当前实现每次查询启动冻结的 `rg`，只搜索磁盘内容；未保存 Editor buffer、replace、multi-root、\n`add-dir` runtime、持久化索引和 watcher 驱动失效均尚未实现。未来本 crate 只消费 host 从\n`zeta-add-dir::DirectoryAccessScope` 冻结出的搜索作用域：主工作目录默认进入，所有具备\nfile-read grant 的附加目录都可以进入，但它们不会因此成为项目配置根。Agent Import 的一次性\n来源不能自行扩大搜索范围。\n\n引入多 root 前必须先扩展领域结果：`SearchMatch` 当前只有 relative `path`，不足以区分不同 root\n中的同名文件。目标 contract 需要 root-qualified match identity，并按 root 独立执行 glob、\nignore、containment 和错误隔离；不能把多个 absolute path flatten 成一个伪 Workspace。\n\n潜在方向是在本 crate 内部加入索引实现，但只有先定义 ignore、一致性、watcher、持久化和隐私\n语义后才可以进行。当前不预先引入 `Engine` 或 `Backend` trait；出现第二种真实执行实现时，再以\n实际调用方需要的最小接口抽象。",
    "headings": [
      {
        "depth": 2,
        "id": "快速理解",
        "title": "快速理解"
      },
      {
        "depth": 2,
        "id": "所有权与边界",
        "title": "所有权与边界"
      },
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "执行路径与失败语义",
        "title": "执行路径与失败语义"
      },
      {
        "depth": 2,
        "id": "验证与修改影响",
        "title": "验证与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与潜在演进",
        "title": "当前限制与潜在演进"
      }
    ],
    "searchText": " 本 README 拥有跨文件内容搜索的实现契约：查询校验、冻结 rg 执行、结果解析、分页作业和 owner 隔离。产品语义与跨进程所有权由 docs/search.md canonical 维护；App Server 的 RPC DTO 转换见 zeta-rs/app-server/README.md。 zeta-search 在 host 提供的已授权搜索作用域内检索文件内容；当前默认且唯一的作用域是主工作 目录。它支持文字、正则、大小写策略和文件 glob。 它不搜索路径名称：zeta-file-search 继续独占文件路径的遍历、ignore 处理和 fuzzy matching。 快速理解 调用方传入由可信 host 建立的 WorkspaceRoot、冻结的 RipgrepExecutable、SearchOwner 和 SearchQuery。SearchService 启动受限后台作业，调用方随后按游标读取 SearchPage，或 取消作业。crate 不知道 JSON-RPC、Desktop、模型 Tool 或 connection 的含义。 需求 zeta-search zeta-file-search --- --- --- 找到包含文字或正则的文件行 SearchService 不负责 文件名 / 路径 fuzzy match 不负责 PathSearchHandle 读取候选文件内容 通过冻结 rg 不读取内容 结果形状 path、行号、preview、UTF-16 ranges path、score、匹配字符 indices UI、RPC、模型 Tool 不负责 不负责 所有权与边界 当前 crate 负责： - 文字/正则查询和 include/exclude glob 的输入上限与 containment 校验； - 无 shell 的冻结 rg 进程启动、取消、JSON 输出解析和稳定错误收束； - UTF-8 byte offset 到 UTF-16 preview range 的转换； - 最多 32 个 owner-bound job、分页读取、5 分钟完成作业保留和全量取消。 当前 crate 不负责： - 工作区信任决策、rg discovery 或任意 executable 选择；host 在构造 SearchService 前完成这些工作； - 文件路径 fuzzy 搜索、目录树、Editor buffer overlay 或 Renderer 的结果分组； - JSON-RPC DTO、connection ID、搜索表单、模型 Tool 和批准策略。 如果这个 crate 开始依赖 zeta-app-server-protocol、zeta-file-search、Desktop 或模型 Tool，说明 搜索执行边界已经漂移。协议适配属于 App Server；路径 fuzzy 属于 zeta-file-search。 文件与职责 ``text src/ ├── lib.rs 只导出稳定的领域 API ├── types.rs query、page、owner、match 和 error contract ├── service.rs SearchService、rg lifecycle、validation 与 parsing └── servicetests.rs argv、glob、UTF-16 range 和结果上限测试 ` service.rs 只持有 WorkspaceRoot、冻结 RipgrepExecutable 和 job state。types.rs 不引用 App Server protocol，因此 CLI、TUI 或未来其他 host 可以自行将输入映射为 SearchQuery。 公共契约 Symbol 调用方做什么 crate 保证什么 --- --- --- SearchService::new 传入可信 root 与已冻结 rg 不自行从 PATH discovery executable switchworkspace 在 host 切换 root 后调用 仅影响之后启动的作业；已有作业保留开始时的 root start 传入 SearchOwner 和 SearchQuery 校验输入、分配 opaque ID、启动有界作业 read 传入同一 owner、ID 和 cursor 返回最多 200 条、不泄漏其他 owner 的作业 cancel 传入同一 owner 和 ID 停止并释放一个作业 cancelall workspace 退休、撤销信任或服务关闭时调用 取消并释放所有尚存作业 SearchOwner 是 crate 不解释的 opaque u64。App Server 当前以 connection ID 建立它，但该映射 不属于本 crate 的持久化或协议契约。SearchPage.error 只包含稳定、已脱敏的运行/解析说明；原始 stderr 不会返回给调用方。 执行路径与失败语义 `text trusted host root + frozen rg → SearchService::start owner, query → validatequery / validateglob → typed rg argv, shell-free spawn → parse JSON matches into SearchMatch → owner-bound SearchPage reads → cancel / retention cleanup / cancelall ` - 无效查询、glob、游标或 batch size：返回 SearchError::InvalidInput； - job 不存在或已被释放：返回 NotFound；不同 owner：返回 NotOwner；服务容量或 poisoned state： 返回 Busy； - rg 无法启动、输出无法读取或 JSON 不合法：作业标记完成，并在 SearchPage.error 返回稳定说明； - 达到结果上限时，只有确实观察到额外匹配才设置 limithit； - 取消会终止子进程；已完成 job 最多保留五分钟，供调用方读取最后 batch。 验证与修改影响 `bash cargo test --manifest-path Cargo.toml -p zeta-search cargo test --manifest-path Cargo.toml -p zeta-app-server workspacesearch --lib bazel test //zeta-rs/search:search-unit-tests ` 修改 argv、输入校验、range 转换、分页、取消或 owner 规则时，必须同步检查 servicetests.rs、 App Server searchoperations.rs 的 DTO 映射、docs/search.md 和该 README。 当前限制与潜在演进 当前实现每次查询启动冻结的 rg，只搜索磁盘内容；未保存 Editor buffer、replace、multi-root、 add-dir runtime、持久化索引和 watcher 驱动失效均尚未实现。未来本 crate 只消费 host 从 zeta-add-dir::DirectoryAccessScope 冻结出的搜索作用域：主工作目录默认进入，所有具备 file-read grant 的附加目录都可以进入，但它们不会因此成为项目配置根。Agent Import 的一次性 来源不能自行扩大搜索范围。 引入多 root 前必须先扩展领域结果：SearchMatch 当前只有 relative path，不足以区分不同 root 中的同名文件。目标 contract 需要 root-qualified match identity，并按 root 独立执行 glob、 ignore、containment 和错误隔离；不能把多个 absolute path flatten 成一个伪 Workspace。 潜在方向是在本 crate 内部加入索引实现，但只有先定义 ignore、一致性、watcher、持久化和隐私 语义后才可以进行。当前不预先引入 Engine 或 Backend` trait；出现第二种真实执行实现时，再以 实际调用方需要的最小接口抽象。"
  },
  {
    "slug": "crates/secrets",
    "sourcePath": "zeta-rs/secrets/README.md",
    "title": "zeta-secrets",
    "description": "zeta-secrets 只保存 opaque bytes。OAuth、token refresh、account metadata、credential scope、 request signing 和 Provider header materialization 均属于消费它的 domain runtime。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 opaque secret value、store port 与 backend obligation。跨系统 credential\n> ownership 和 backend policy 见 [`docs/secrets.md`](../../docs/secrets.md)。OS keyring 实现契约见\n> [`zeta-keyring-store`](../keyring-store/README.md)。\n\n`zeta-secrets` 只保存 opaque bytes。OAuth、token refresh、account metadata、credential scope、\nrequest signing 和 Provider header materialization 均属于消费它的 domain runtime。\n\n## 公共契约\n\n| Symbol | 职责 | 安全语义 |\n| --- | --- | --- |\n| `SecretKey` | 非 secret 的 stable lookup identity | 非空、最多 512 bytes、禁止 control character |\n| `SecretValue` | opaque secret bytes | 不实现 Clone/Display/Serialize；Debug 固定 redacted；Drop zeroize |\n| `SecretStore` | `load / store / delete` port | namespace isolation、sanitized error、完整三操作 |\n| `DeleteSecretOutcome` | exact delete result | 区分 `Deleted` 与 `NotFound` |\n| `MemorySecretStore` | process-local ephemeral backend | replacement、delete、drop 时 zeroize stored bytes |\n| `UnavailableSecretStore` | explicit fail-closed backend | 所有操作返回 `BackendUnavailable` |\n| `FileSecretStore` | explicit opt-in durable backend | hashed filenames、私有权限、有界读取、同步 staging 与 replace |\n| `SecretStoreError` | sanitized error | message 不能包含 secret/header/raw backend response |\n| `SecretStoreErrorKind` | stable caller classification | unavailable、access denied、backend failure |\n\n`SecretKey` 的内容会出现在 `Debug`，因此 key schema 不能包含 token、email 或其他敏感 identifier。\nKey schema 由调用 domain 拥有，本 crate 只验证基本安全 shape。\n\n## 内部接口与调用路径\n\n| Symbol | 可见性 | 当前职责 |\n| --- | --- | --- |\n| `SecretValue::expose` | public explicit borrow | 唯一取得 plaintext bytes 的路径；borrow 应尽量短 |\n| `MemorySecretStore::values` | private mutex map | 保存 owned byte copies，不保存 `SecretValue` clone |\n| `lock_error` | private function | poisoned mutex → sanitized `BackendFailure` |\n| `unavailable` | private function | 三个 unavailable operations 共用稳定错误 |\n| `Drop for SecretValue` | impl | zeroize caller-owned secret buffer |\n| `Drop for MemorySecretStore` | impl | zeroize map 中所有 surviving values |\n| `key_filename` | private function | domain-separated SHA-256 key → non-PII filename |\n| `promote_file` | private platform function | synced staging → destination replacement |\n| `cleanup_staging_files` | private function | open 时清理同 namespace 的 stale `.tmp-*` 文件 |\n\n```text\nstore(key, SecretValue)\n├─ SecretValue::expose (short borrow)\n├─ copy bytes into backend\n├─ replace old bytes\n└─ zeroize replaced buffer\n\nload(key)\n├─ backend copies opaque bytes\n└─ SecretValue::new → caller-owned zeroizing value\n\ndelete/drop\n└─ zeroize backend-owned bytes before release\n```\n\n方向偏差：\n\n- `SecretValue` 增加 `Clone`、`Display` 或 serde：secret 传播面失控；\n- backend error 包含 command/header/raw response：sanitization boundary 被破坏；\n- `MemorySecretStore` 被当作 production persistence：进程退出即丢失；\n- unavailable host 返回空值而非 error：credential absence 与 backend failure 混淆；\n- API/network layer 直接读 `SecretStore`：credential lifecycle ownership 下沉错误。\n\n## 后端实现清单\n\n生产后端必须实现完整 load/store/delete、隔离 Zeta namespace、定义 overwrite atomicity、\nsanitized errors 和 negative logging tests。只有 load 没有 delete 的 backend 不满足 trait 语义。\n\n内存 zeroize 降低残留风险，但不保证编译器、allocator、swap、core dump 或 backend copy 中不存在\n副本；系统级保护仍由 OS facility、process hardening 和 caller lifecycle 负责。\n\n## 测试与修改路径\n\n```text\ncargo test -p zeta-secrets\nbazel test //zeta-rs/secrets:secrets-unit-tests\n```\n\n当前测试覆盖 memory/file round-trip、replace/delete、Debug redaction、key validation、unavailable error、\nUnix 0700/0600 权限、stale staging cleanup、key filename 隐藏与 1 MiB 上限。\n新增 backend 时必须补 error/log/debug negative tests，并证明 replacement/delete/drop 的 secret\nbuffer 处理。\n\n## 当前限制与演进\n\n当前具有 memory、unavailable 与显式文件 backend。`FileSecretStore` 适合 host 明确选择的私有产品\n目录：Unix 使用 0700/0600 与原子 rename；未实现等价 ACL 的非 Unix host 在 `open` 时明确返回\n`BackendUnavailable`，不会静默写入普通文件。OS keyring 位于独立 `zeta-keyring-store` 并实现同一\n`SecretStore` contract；加密文件、跨进程锁、migration 与 rotation metadata 尚未实现。后续 backend\n不得扩大 `SecretValue` 的复制、序列化或日志接口。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口与调用路径",
        "title": "内部接口与调用路径"
      },
      {
        "depth": 2,
        "id": "后端实现清单",
        "title": "后端实现清单"
      },
      {
        "depth": 2,
        "id": "测试与修改路径",
        "title": "测试与修改路径"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 解释 opaque secret value、store port 与 backend obligation。跨系统 credential ownership 和 backend policy 见 docs/secrets.md。OS keyring 实现契约见 zeta-keyring-store。 zeta-secrets 只保存 opaque bytes。OAuth、token refresh、account metadata、credential scope、 request signing 和 Provider header materialization 均属于消费它的 domain runtime。 公共契约 Symbol 职责 安全语义 --- --- --- SecretKey 非 secret 的 stable lookup identity 非空、最多 512 bytes、禁止 control character SecretValue opaque secret bytes 不实现 Clone/Display/Serialize；Debug 固定 redacted；Drop zeroize SecretStore load / store / delete port namespace isolation、sanitized error、完整三操作 DeleteSecretOutcome exact delete result 区分 Deleted 与 NotFound MemorySecretStore process-local ephemeral backend replacement、delete、drop 时 zeroize stored bytes UnavailableSecretStore explicit fail-closed backend 所有操作返回 BackendUnavailable FileSecretStore explicit opt-in durable backend hashed filenames、私有权限、有界读取、同步 staging 与 replace SecretStoreError sanitized error message 不能包含 secret/header/raw backend response SecretStoreErrorKind stable caller classification unavailable、access denied、backend failure SecretKey 的内容会出现在 Debug，因此 key schema 不能包含 token、email 或其他敏感 identifier。 Key schema 由调用 domain 拥有，本 crate 只验证基本安全 shape。 内部接口与调用路径 Symbol 可见性 当前职责 --- --- --- SecretValue::expose public explicit borrow 唯一取得 plaintext bytes 的路径；borrow 应尽量短 MemorySecretStore::values private mutex map 保存 owned byte copies，不保存 SecretValue clone lockerror private function poisoned mutex → sanitized BackendFailure unavailable private function 三个 unavailable operations 共用稳定错误 Drop for SecretValue impl zeroize caller-owned secret buffer Drop for MemorySecretStore impl zeroize map 中所有 surviving values keyfilename private function domain-separated SHA-256 key → non-PII filename promotefile private platform function synced staging → destination replacement cleanupstagingfiles private function open 时清理同 namespace 的 stale .tmp- 文件 ``text store key, SecretValue ├─ SecretValue::expose short borrow ├─ copy bytes into backend ├─ replace old bytes └─ zeroize replaced buffer load key ├─ backend copies opaque bytes └─ SecretValue::new → caller-owned zeroizing value delete/drop └─ zeroize backend-owned bytes before release ` 方向偏差： - SecretValue 增加 Clone、Display 或 serde：secret 传播面失控； - backend error 包含 command/header/raw response：sanitization boundary 被破坏； - MemorySecretStore 被当作 production persistence：进程退出即丢失； - unavailable host 返回空值而非 error：credential absence 与 backend failure 混淆； - API/network layer 直接读 SecretStore：credential lifecycle ownership 下沉错误。 后端实现清单 生产后端必须实现完整 load/store/delete、隔离 Zeta namespace、定义 overwrite atomicity、 sanitized errors 和 negative logging tests。只有 load 没有 delete 的 backend 不满足 trait 语义。 内存 zeroize 降低残留风险，但不保证编译器、allocator、swap、core dump 或 backend copy 中不存在 副本；系统级保护仍由 OS facility、process hardening 和 caller lifecycle 负责。 测试与修改路径 `text cargo test -p zeta-secrets bazel test //zeta-rs/secrets:secrets-unit-tests ` 当前测试覆盖 memory/file round-trip、replace/delete、Debug redaction、key validation、unavailable error、 Unix 0700/0600 权限、stale staging cleanup、key filename 隐藏与 1 MiB 上限。 新增 backend 时必须补 error/log/debug negative tests，并证明 replacement/delete/drop 的 secret buffer 处理。 当前限制与演进 当前具有 memory、unavailable 与显式文件 backend。FileSecretStore 适合 host 明确选择的私有产品 目录：Unix 使用 0700/0600 与原子 rename；未实现等价 ACL 的非 Unix host 在 open 时明确返回 BackendUnavailable，不会静默写入普通文件。OS keyring 位于独立 zeta-keyring-store 并实现同一 SecretStore contract；加密文件、跨进程锁、migration 与 rotation metadata 尚未实现。后续 backend 不得扩大 SecretValue` 的复制、序列化或日志接口。"
  },
  {
    "slug": "crates/session-store",
    "sourcePath": "zeta-rs/session-store/README.md",
    "title": "zeta-session-store",
    "description": "zeta-session-store 定义 storage-neutral Session event-stream contract。它保存 SessionEvent 和 exact typed SessionCommandReceipt，但不实现文件格式、reducer、runtime mailbox、Thread transcript 或 RPC。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 Session durability port、stored envelope 和 batch validation。Canonical\n> Session domain 见 [`docs/protocol.md`](../../docs/protocol.md)，跨 Session/Thread 的恢复与\n> saga 见 [`docs/core.md`](../../docs/core.md)。\n\n`zeta-session-store` 定义 storage-neutral Session event-stream contract。它保存\n`SessionEvent` 和 exact typed `SessionCommandReceipt`，但不实现文件格式、reducer、runtime\nmailbox、Thread transcript 或 RPC。\n\n## 边界与公共契约\n\n| Symbol | 职责 | 关键约束 |\n| --- | --- | --- |\n| `SessionStore` | `list_session_ids / load / append_batch` port | 每个 Session 独立 sequence；batch 原子可见 |\n| `SessionEventBatch` | 一次 append intent | exact `session_id + expected_sequence + events` |\n| `AppendSessionBatchResult` | 已提交 batch 摘要 | 返回 committed sequence 和 event count |\n| `StoredSessionEvent` | storage-owned durable envelope | schema、event ID、sequence、timestamp、optional command receipt |\n| `SessionCommandReceipt` | accepted command 的 exact typed copy | 用于 idempotency/conflict recovery，不是日志文本 |\n| `SessionStoreError` | stable boundary error | 区分 invalid batch、sequence conflict 和 backend storage |\n\n本 crate 不拥有 `SessionEvent` 语义；它来自 `zeta-protocol`。Backend 实现必须复用这里的\nenvelope 与 validator，不能发明第二套 Session event schema。\n\n## 内部接口地图与调用路径\n\n核心函数只有一个，但它是所有 backend 的写入前置条件：\n\n| Symbol | 当前职责 | 不得迁移的语义 |\n| --- | --- | --- |\n| `validate_session_append_batch` | 对照 backend actual sequence 校验完整 batch | identity、sequence、schema version、batch/event ID 约束必须在 durable append 前成立 |\n| `CURRENT_SESSION_EVENT_SCHEMA_VERSION` | 新写入 event 的 schema version | migration reader 可以支持旧版本，新写入不能任意选择版本 |\n\n```text\nbackend append_batch\n├─ read current Session sequence\n├─ validate_session_append_batch(batch, actual_sequence)\n│  ├─ expected sequence exact match\n│  ├─ non-empty batch ID\n│  ├─ at least one event\n│  ├─ current schema version\n│  ├─ envelope/event/batch Session ID agreement\n│  ├─ contiguous sequence\n│  └─ unique event ID within batch\n├─ atomically persist complete batch\n└─ expose AppendSessionBatchResult\n```\n\n方向偏差：\n\n- backend 跳过 `validate_session_append_batch`：不同 backend 会产生不同 durability 语义；\n- partial event 可被 `load` 观察：batch atomicity 已破坏；\n- `ThreadEvent` 或 transcript 进入 `StoredSessionEvent`：aggregate ownership 已漂移；\n- command 只保存 ID、不保存 exact `SessionCommand`：replay conflict 无法可靠判断；\n- Session 与 Thread 共用逻辑 sequence：独立并发边界已被错误合并。\n\n## Validation 与错误\n\n`validate_session_append_batch` 按 deterministic 顺序返回：\n\n- `SequenceConflict { expected, actual }`：caller snapshot 已过期；\n- `InvalidBatch`：空 ID、空 events、schema、identity、sequence 或 duplicate event ID 错误；\n- `Storage` 只能由 backend 表达 I/O、corruption 或 durable facility failure。\n\nValidator 不检查 reducer-level 合法状态迁移。Core 负责产生合法 `SessionEvent`；store 只验证\nstorage envelope 与 append contract。\n\n## Backend 实现要求\n\n一个 `SessionStore` backend 必须：\n\n1. 对每个 Session 串行比较 actual sequence；\n2. 在同一原子边界内提交整个 batch；\n3. success 前完成所声明的 durability；\n4. `load` 不返回未提交 tail；\n5. `list_session_ids` 不虚构空 aggregate；\n6. 保留 exact event/command bytes 的语义；\n7. 将 backend detail 映射为 sanitized `SessionStoreError::Storage`。\n\n物理 JSONL framing、checksum、tail recovery 和 filesystem layout 当前由 `zeta-storage`\n实现，不应反向进入这个 port。\n\n## 测试与修改路径\n\n```text\ncargo test -p zeta-session-store\nbazel test //zeta-rs/session-store:session-store-unit-tests\n```\n\n修改 stored field 或 schema version 时，同时审查 serde compatibility、`zeta-storage`\nadapter、rollout recovery、trace export 和 fixtures。修改 batch validation 时，SessionStore 与\nThreadStore 的共同原则应保持一致，但不要抽象掉各自的 ID、event 和 sequence 类型。\n\n## 当前限制与演进\n\n当前新写入使用 schema version `2`，reader 仍接受 minimum version `1`；version 2 覆盖 Agent\nchild Thread plan/context seed contract。migration registry、compaction、snapshot/checkpoint 与\ncorruption diagnostics 尚不属于本 crate 的已实现能力。未来可以增加 versioned\nreader/migration policy，但不能削弱 append atomicity、exact command receipt 或 per-Session\nsequence。",
    "headings": [
      {
        "depth": 2,
        "id": "边界与公共契约",
        "title": "边界与公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图与调用路径",
        "title": "内部接口地图与调用路径"
      },
      {
        "depth": 2,
        "id": "validation-与错误",
        "title": "Validation 与错误"
      },
      {
        "depth": 2,
        "id": "backend-实现要求",
        "title": "Backend 实现要求"
      },
      {
        "depth": 2,
        "id": "测试与修改路径",
        "title": "测试与修改路径"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 解释 Session durability port、stored envelope 和 batch validation。Canonical Session domain 见 docs/protocol.md，跨 Session/Thread 的恢复与 saga 见 docs/core.md。 zeta-session-store 定义 storage-neutral Session event-stream contract。它保存 SessionEvent 和 exact typed SessionCommandReceipt，但不实现文件格式、reducer、runtime mailbox、Thread transcript 或 RPC。 边界与公共契约 Symbol 职责 关键约束 --- --- --- SessionStore listsessionids / load / appendbatch port 每个 Session 独立 sequence；batch 原子可见 SessionEventBatch 一次 append intent exact sessionid + expectedsequence + events AppendSessionBatchResult 已提交 batch 摘要 返回 committed sequence 和 event count StoredSessionEvent storage-owned durable envelope schema、event ID、sequence、timestamp、optional command receipt SessionCommandReceipt accepted command 的 exact typed copy 用于 idempotency/conflict recovery，不是日志文本 SessionStoreError stable boundary error 区分 invalid batch、sequence conflict 和 backend storage 本 crate 不拥有 SessionEvent 语义；它来自 zeta-protocol。Backend 实现必须复用这里的 envelope 与 validator，不能发明第二套 Session event schema。 内部接口地图与调用路径 核心函数只有一个，但它是所有 backend 的写入前置条件： Symbol 当前职责 不得迁移的语义 --- --- --- validatesessionappendbatch 对照 backend actual sequence 校验完整 batch identity、sequence、schema version、batch/event ID 约束必须在 durable append 前成立 CURRENTSESSIONEVENTSCHEMAVERSION 新写入 event 的 schema version migration reader 可以支持旧版本，新写入不能任意选择版本 ``text backend appendbatch ├─ read current Session sequence ├─ validatesessionappendbatch batch, actualsequence │ ├─ expected sequence exact match │ ├─ non-empty batch ID │ ├─ at least one event │ ├─ current schema version │ ├─ envelope/event/batch Session ID agreement │ ├─ contiguous sequence │ └─ unique event ID within batch ├─ atomically persist complete batch └─ expose AppendSessionBatchResult ` 方向偏差： - backend 跳过 validatesessionappendbatch：不同 backend 会产生不同 durability 语义； - partial event 可被 load 观察：batch atomicity 已破坏； - ThreadEvent 或 transcript 进入 StoredSessionEvent：aggregate ownership 已漂移； - command 只保存 ID、不保存 exact SessionCommand：replay conflict 无法可靠判断； - Session 与 Thread 共用逻辑 sequence：独立并发边界已被错误合并。 Validation 与错误 validatesessionappendbatch 按 deterministic 顺序返回： - SequenceConflict expected, actual ：caller snapshot 已过期； - InvalidBatch：空 ID、空 events、schema、identity、sequence 或 duplicate event ID 错误； - Storage 只能由 backend 表达 I/O、corruption 或 durable facility failure。 Validator 不检查 reducer-level 合法状态迁移。Core 负责产生合法 SessionEvent；store 只验证 storage envelope 与 append contract。 Backend 实现要求 一个 SessionStore backend 必须： 1. 对每个 Session 串行比较 actual sequence； 2. 在同一原子边界内提交整个 batch； 3. success 前完成所声明的 durability； 4. load 不返回未提交 tail； 5. listsessionids 不虚构空 aggregate； 6. 保留 exact event/command bytes 的语义； 7. 将 backend detail 映射为 sanitized SessionStoreError::Storage。 物理 JSONL framing、checksum、tail recovery 和 filesystem layout 当前由 zeta-storage 实现，不应反向进入这个 port。 测试与修改路径 `text cargo test -p zeta-session-store bazel test //zeta-rs/session-store:session-store-unit-tests ` 修改 stored field 或 schema version 时，同时审查 serde compatibility、zeta-storage adapter、rollout recovery、trace export 和 fixtures。修改 batch validation 时，SessionStore 与 ThreadStore 的共同原则应保持一致，但不要抽象掉各自的 ID、event 和 sequence 类型。 当前限制与演进 当前新写入使用 schema version 2，reader 仍接受 minimum version 1`；version 2 覆盖 Agent child Thread plan/context seed contract。migration registry、compaction、snapshot/checkpoint 与 corruption diagnostics 尚不属于本 crate 的已实现能力。未来可以增加 versioned reader/migration policy，但不能削弱 append atomicity、exact command receipt 或 per-Session sequence。"
  },
  {
    "slug": "crates/zeterm/settings",
    "sourcePath": "zeterm/settings/README.md",
    "title": "zeta-settings",
    "description": "zeta-settings owns the product Settings page shell: the left navigation, titlebar-height header with search and close controls, action bar, and the content slot for the active settings se…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-settings` owns the product Settings page shell: the left navigation,\ntitlebar-height header with search and close controls, action bar, and the\ncontent slot for the active settings section. Configuration persistence and\nnative window integration stay outside this crate.\n\n## Ownership\n\n| Concern | Owner | Boundary |\n| --- | --- | --- |\n| Settings page rail, titlebar-height header, action bar, and content geometry | `SettingsPageLayout` / `SettingsPage` | Owns page composition; the host supplies the header height and it does not know the native window or config transport. |\n| Search, close, navigation, and page action element identities | `zeta-settings` | Publishes stable interaction nodes and a modal focus boundary. |\n| Back item and semantic icon+label navigation | `SettingsPage` | Keeps Settings navigation presentation and activation identities together; the host maps Back to its surface transition. |\n| Active section content | Settings section host | Paints into `SettingsPage::content_bounds()`. |\n| Generic controls and layout primitives | `zeta-ui` / `zui` | Supplies reusable presentation and backend-neutral interaction contracts. |\n| Configuration load/save and platform events | Native/config host | Adapts DTOs, executes actions, and owns IME/window lifecycle. |\n\n## Execution path\n\n```text\nnative snapshot → SettingsPage shell + active section → UiIntent::Activate\n                → native/config adapter executes the action\n```\n\nThe first active section is Language Servers. Its existing native model and\ncontent view are mounted into the page content slot while the page shell is\nowned by this crate. The header close control and Back item both remain\npage-owned interactions; the host decides how they close or leave Settings.\nNew sections should add their own product state and content composition here\nrather than growing `native` with another page layout.\n\n## Verification\n\nRun `cargo test -p zeta-settings` and `cargo test --manifest-path Cargo.toml -p zeterm` after changing\nthe page contract or a host adapter.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "execution-path",
        "title": "Execution path"
      },
      {
        "depth": 2,
        "id": "verification",
        "title": "Verification"
      }
    ],
    "searchText": "zeta-settings owns the product Settings page shell: the left navigation, titlebar-height header with search and close controls, action bar, and the content slot for the active settings section. Configuration persistence and native window integration stay outside this crate. Ownership Concern Owner Boundary --- --- --- Settings page rail, titlebar-height header, action bar, and content geometry SettingsPageLayout / SettingsPage Owns page composition; the host supplies the header height and it does not know the native window or config transport. Search, close, navigation, and page action element identities zeta-settings Publishes stable interaction nodes and a modal focus boundary. Back item and semantic icon+label navigation SettingsPage Keeps Settings navigation presentation and activation identities together; the host maps Back to its surface transition. Active section content Settings section host Paints into SettingsPage::contentbounds . Generic controls and layout primitives zeta-ui / zui Supplies reusable presentation and backend-neutral interaction contracts. Configuration load/save and platform events Native/config host Adapts DTOs, executes actions, and owns IME/window lifecycle. Execution path ``text native snapshot → SettingsPage shell + active section → UiIntent::Activate → native/config adapter executes the action ` The first active section is Language Servers. Its existing native model and content view are mounted into the page content slot while the page shell is owned by this crate. The header close control and Back item both remain page-owned interactions; the host decides how they close or leave Settings. New sections should add their own product state and content composition here rather than growing native with another page layout. Verification Run cargo test -p zeta-settings and cargo test --manifest-path Cargo.toml -p zeterm` after changing the page contract or a host adapter."
  },
  {
    "slug": "crates/shell-command",
    "sourcePath": "zeta-rs/shell-command/README.md",
    "title": "zeta-shell-command",
    "description": "本 crate 拥有 shell-command host definition、结构化请求解析和冻结的 rg executable materialization。安装布局与候选位置由 zeta-install-context 提供；本 crate 不读取安装方式，不隐式启动 shell，不决定 Core approval，也不持有 Thread/Turn 状态。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是显式本地进程 Tool 与只读 ripgrep profile 的实现契约。跨 crate Tool ownership\n> 见 [`docs/tools.md`](../../docs/tools.md)，sandbox policy 见\n> [`docs/sandboxing.md`](../../docs/sandboxing.md)。\n\n本 crate 拥有 `shell-command` host definition、结构化请求解析和冻结的 `rg` executable\nmaterialization。安装布局与候选位置由 [`zeta-install-context`](../install-context/README.md)\n提供；本 crate 不读取安装方式，不隐式启动 shell，不决定 Core approval，也不持有 Thread/Turn\n状态。\n\n## 文件与职责\n\n| 文件 | 职责 |\n| --- | --- |\n| `src/lib.rs` | `ShellCommandTool`、`ShellCommandRequest`、definition 与 executor bridge |\n| `src/ripgrep.rs` | `RipgrepExecutable` candidate validation/identity freeze 与 `BuiltInRipgrepPolicy` 参数约束 |\n| `src/shell_command_tests.rs` | binding、authority、sandbox denial 与 validation |\n| `src/ripgrep_tests.rs` | executable discovery 和 unsafe flag rejection |\n\n## 公共契约与调用路径\n\n```text\nShellCommandTool::new\n└─ shell_command_definition + zeta_tool_executor::CommandExecutor\n\nToolExecutor::execute\n├─ validate binding / environment / cancellation\n├─ ShellCommandRequest::from_arguments\n└─ ShellCommandTool::execute_authorized\n   └─ CommandExecutor::execute\n\nApp Server local adapter\n├─ InstallContext::executable_candidates(Ripgrep)\n├─ RipgrepExecutable::from_override / discover_candidates\n├─ ShellCommandRequest::from_arguments\n├─ workspace argument validation\n├─ RipgrepExecutable::materialize\n└─ ShellCommandTool::execute_authorized\n```\n\n`ShellCommandRequest` 始终是显式 `program + arguments + relative working_directory`；没有 quoting、\nglobbing、environment expansion 或 shell parsing。`ShellCommandTool::execute_authorized` 只供已经\n完成 host materialization 和 Core policy decision 的 adapter 使用。\n\n## 只读 `rg` 配置档案\n\nApp Server 先从 `InstallContext` 获取以下候选：\n\n1. `ZETA_RG_PATH`；\n2. package layout 的 `zeta-path/rg[.exe]`；\n3. 当前 Zeta executable 同目录的 legacy `rg`/`rg.exe`；\n4. 启动时 `PATH`。\n\n显式 override 通过 `from_override` 单独验证，无效时直接失败；普通候选由\n`discover_candidates` 逐个验证。选中的 executable canonicalize 并冻结后，`materialize` 只接受\n模型别名 `rg`，替换成冻结的绝对 executable，并在 argv 前加入 `--no-config`。\n`BuiltInRipgrepPolicy` 拒绝\npreprocessor、hostname command、archive search、symlink follow、pattern file 与 ignore file\n参数，包括 `-f/path`、`-LH` 等紧凑短参数形式，因为它们会扩大进程或文件读取边界。该 policy\n只负责 built-in 参数安全，不替代下层 OS sandbox。\nApp Server adapter 还拒绝绝对路径和含 `..` component 的参数。\n已存在的相对 path argument 会在 review 前 canonicalize；通过 Workspace 内 symlink 指向外部的\n路径同样被拒绝。\n\ncanonical package builder 当前按 checksum-locked manifest 把 ripgrep 放到\n`zeta-path/rg[.exe]`，并在 `zeta-package.json` 记录版本和 digest；具体 staging contract 见\n[`scripts/zeta_package/README.md`](../../scripts/zeta_package/README.md)。本 crate 仍不信任\nmetadata，也尚未执行版本/capability probe。Packaging 不能改变上述 executable validation、\nidentity freeze 和 fail-closed 语义。Linux package 同时携带经过 source lock 构建的\n`zeta-resources/bwrap`；它的 discovery/probe contract 由\n[`zeta-linux-sandbox`](../linux-sandbox/README.md) 拥有。\n\n## 失败、取消与输出\n\n- JSON shape、空 program、binding 或 environment 不匹配：子进程启动前失败；\n- Core 选择的 sandbox backend 无法建立边界：返回 structured `SandboxDenied`；\n- cancellation 在 spawn 前返回 not-started；spawn 后终止 child 并返回 uncertain outcome；\n- timeout 同样终止 child，并按 started/uncertain 处理；\n- stdout/stderr 共用一个 byte budget，结果保留 bounded prefix，并分别标记是否截断。\n\n底层 checkpoint、kill 和 capture 语义由\n[`zeta-tool-executor`](../tool-executor/README.md) canonical 定义。\n\n## 修改与验证\n\n修改 model schema、危险 `rg` flags、discovery 顺序或 outcome mapping 时，必须同步检查 App Server\n`local_tools.rs`、本 README 和 `docs/tools.md`。\n\n```bash\ncargo test -p zeta-shell-command\ncargo clippy -p zeta-shell-command --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/shell-command:shell-command-unit-tests\n```",
    "headings": [
      {
        "depth": 2,
        "id": "文件与职责",
        "title": "文件与职责"
      },
      {
        "depth": 2,
        "id": "公共契约与调用路径",
        "title": "公共契约与调用路径"
      },
      {
        "depth": 2,
        "id": "只读-rg-配置档案",
        "title": "只读 rg 配置档案"
      },
      {
        "depth": 2,
        "id": "失败取消与输出",
        "title": "失败、取消与输出"
      },
      {
        "depth": 2,
        "id": "修改与验证",
        "title": "修改与验证"
      }
    ],
    "searchText": " 本 README 是显式本地进程 Tool 与只读 ripgrep profile 的实现契约。跨 crate Tool ownership 见 docs/tools.md，sandbox policy 见 docs/sandboxing.md。 本 crate 拥有 shell-command host definition、结构化请求解析和冻结的 rg executable materialization。安装布局与候选位置由 zeta-install-context 提供；本 crate 不读取安装方式，不隐式启动 shell，不决定 Core approval，也不持有 Thread/Turn 状态。 文件与职责 文件 职责 --- --- src/lib.rs ShellCommandTool、ShellCommandRequest、definition 与 executor bridge src/ripgrep.rs RipgrepExecutable candidate validation/identity freeze 与 BuiltInRipgrepPolicy 参数约束 src/shellcommandtests.rs binding、authority、sandbox denial 与 validation src/ripgreptests.rs executable discovery 和 unsafe flag rejection 公共契约与调用路径 ``text ShellCommandTool::new └─ shellcommanddefinition + zetatoolexecutor::CommandExecutor ToolExecutor::execute ├─ validate binding / environment / cancellation ├─ ShellCommandRequest::fromarguments └─ ShellCommandTool::executeauthorized └─ CommandExecutor::execute App Server local adapter ├─ InstallContext::executablecandidates Ripgrep ├─ RipgrepExecutable::fromoverride / discovercandidates ├─ ShellCommandRequest::fromarguments ├─ workspace argument validation ├─ RipgrepExecutable::materialize └─ ShellCommandTool::executeauthorized ` ShellCommandRequest 始终是显式 program + arguments + relative workingdirectory；没有 quoting、 globbing、environment expansion 或 shell parsing。ShellCommandTool::executeauthorized 只供已经 完成 host materialization 和 Core policy decision 的 adapter 使用。 只读 rg 配置档案 App Server 先从 InstallContext 获取以下候选： 1. ZETARGPATH； 2. package layout 的 zeta-path/rg .exe ； 3. 当前 Zeta executable 同目录的 legacy rg/rg.exe； 4. 启动时 PATH。 显式 override 通过 fromoverride 单独验证，无效时直接失败；普通候选由 discovercandidates 逐个验证。选中的 executable canonicalize 并冻结后，materialize 只接受 模型别名 rg，替换成冻结的绝对 executable，并在 argv 前加入 --no-config。 BuiltInRipgrepPolicy 拒绝 preprocessor、hostname command、archive search、symlink follow、pattern file 与 ignore file 参数，包括 -f/path、-LH 等紧凑短参数形式，因为它们会扩大进程或文件读取边界。该 policy 只负责 built-in 参数安全，不替代下层 OS sandbox。 App Server adapter 还拒绝绝对路径和含 .. component 的参数。 已存在的相对 path argument 会在 review 前 canonicalize；通过 Workspace 内 symlink 指向外部的 路径同样被拒绝。 canonical package builder 当前按 checksum-locked manifest 把 ripgrep 放到 zeta-path/rg .exe ，并在 zeta-package.json 记录版本和 digest；具体 staging contract 见 scripts/zetapackage/README.md。本 crate 仍不信任 metadata，也尚未执行版本/capability probe。Packaging 不能改变上述 executable validation、 identity freeze 和 fail-closed 语义。Linux package 同时携带经过 source lock 构建的 zeta-resources/bwrap；它的 discovery/probe contract 由 zeta-linux-sandbox 拥有。 失败、取消与输出 - JSON shape、空 program、binding 或 environment 不匹配：子进程启动前失败； - Core 选择的 sandbox backend 无法建立边界：返回 structured SandboxDenied； - cancellation 在 spawn 前返回 not-started；spawn 后终止 child 并返回 uncertain outcome； - timeout 同样终止 child，并按 started/uncertain 处理； - stdout/stderr 共用一个 byte budget，结果保留 bounded prefix，并分别标记是否截断。 底层 checkpoint、kill 和 capture 语义由 zeta-tool-executor canonical 定义。 修改与验证 修改 model schema、危险 rg flags、discovery 顺序或 outcome mapping 时，必须同步检查 App Server localtools.rs、本 README 和 docs/tools.md。 `bash cargo test -p zeta-shell-command cargo clippy -p zeta-shell-command --all-targets --no-deps -- -D warnings bazel test //zeta-rs/shell-command:shell-command-unit-tests ``"
  },
  {
    "slug": "crates/shell-completion",
    "sourcePath": "zeta-rs/shell-completion/README.md",
    "title": "zeta-shell-completion",
    "description": "本 crate 拥有后端无关的 Shell 结构知识。它解析一次输入，结合 command registry、PATH 快照、工作区 manifest、现有路径和宿主提供的 alias，为每个精确 token 生成可验证的描述，并从同一状态生成补全项。 它不执行命令、不读取交互 Shell 状态、不决定 Shell/Agent 路由，也不拥有任何 UI。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Shell parser、command signature、token evidence 与 completion candidate 的实现契约。\n> Shell/Agent 路由顺序见 [`zeta-input-classifier`](../input-classifier/README.md)；命令执行和授权见\n> [`zeta-shell-command`](../shell-command/README.md)。\n\n本 crate 拥有后端无关的 Shell 结构知识。它解析一次输入，结合 command registry、PATH 快照、工作区\nmanifest、现有路径和宿主提供的 alias，为每个精确 token 生成可验证的描述，并从同一状态生成补全项。\n它不执行命令、不读取交互 Shell 状态、不决定 Shell/Agent 路由，也不拥有任何 UI。\n\n## 公共契约\n\n| Symbol | 职责 | 调用约束 |\n| --- | --- | --- |\n| `ShellCompletionEngine` | 持有 registry、PATH、工作目录、工作区候选和 alias 快照 | 每个 Shell 环境持有一个实例；环境变化后显式更新 |\n| `ShellCommandRegistry` | 注册递归 `ShellCommandSpec` | 调用者显式注册的顶层命令视为权威事实；注册过程不能启动 generator 或授权执行 |\n| `ShellCommandSpec` | 描述一个 command/subcommand 的 option 和 argument grammar | 未知 option 和无法验证的 opaque value 不产生 token evidence |\n| `ShellTokenSnapshot` | 返回输入中每个 token 的位置和可选结构描述 | classifier 只把 `description.is_some()` 当作确定性证据 |\n| `ShellCompletion` | 返回 replacement、display、kind 和 byte replace range | 产品宿主负责展示和应用，不得越过 editor authority |\n| `ShellCompletionSnapshot` | 返回有界候选和当前 token 是否已有精确匹配 | inline UI 必须用精确匹配标记避免把 `git` 错续写成 `git-*`；不应自己重建 command 语义 |\n| `ShellAlias` | 表达宿主已解析的一个 alias | 本 crate 只做最多三层展开；不自行读取 dotfile 或 PTY |\n\n`ShellValueHint::Opaque` 只声明 argument slot，不会把任意字符串当作已识别 token。只有静态 choice、\n合法 integer、已注册 command、现有 path、精确 workspace target 等可验证值才产生描述。这一约束防止\n`git status 是做什么的` 中的自然语言因为“命令接受参数”而被错误计为 Shell evidence。\n\n## 执行路径与内部 owner\n\n```text\nShellCompletionEngine::analyze\n  → parser::parse_shell_commands\n  → engine::ShellCompletionEngine::describe_word\n      environment assignment / alias / wrapper\n      command registry / subcommand / exact option\n      option value / workspace candidate / existing path\n  → types::ShellTokenSnapshot\n\nShellCompletionEngine::complete\n  → 同一个 parser 和 CommandState\n  → engine::completions\n      command / alias / subcommand / option / value / path candidates\n```\n\n- `parser::ShellLexer` 拥有 quote、escape、comment、pipeline、command separator、redirection 和\n  `--flag=value` 边界；classifier 不得再维护第二套 Shell parser。\n- `engine::CommandState` 拥有递归 subcommand、pending option value、combined short option、wrapper\n  command 与 alias expansion 的状态迁移。\n- `catalog::default_registry` 是 Zeta 自建的内置 command grammar；没有复制 Warp completer 或其\n  AGPL command-signature 数据。Shell builtin 始终可用；Git、Cargo、kubectl 等外部默认规格只有对应\n  executable 出现在 PATH 快照时才参与顶层证据和补全。\n- `environment::ExecutableCatalog` 在明确的 PATH 快照中冻结 executable identity；PATH 更新必须通过\n  `set_path_entries` 重新建立快照。\n- `workspace::WorkspaceCatalog` 只读 package scripts、Just recipes 和 Make targets；工作区内容变化后\n  调用 `refresh_workspace`。\n- `engine::completions` 只投影候选，不拥有 ghost text/popup、selection、Tab 行为或 editor mutation。\n\n## 失败和安全边界\n\n- 无法读取 PATH 目录、manifest 或普通目录时跳过该来源，不启动子进程；PATH 快照最多读取 256 个\n  目录并保留 16,384 个 executable，单个 workspace source 最多读取 1 MiB、保留 4,096 个候选；\n- alias 名称非法或 replacement 为空时在进入 engine 前拒绝；循环或超过三层的 alias 不产生证据；\n- command registry 中没有的 option、无法验证的 value 和不存在的严格 file/directory 不产生描述；\n- parser 接受未完成输入并返回已有 token，不把解析失败升级为产品错误；\n- completion 最多返回 100 项，replace range 始终是原始 UTF-8 输入的 byte range；路径 replacement\n  会保留单/双引号上下文或对未引用的 Shell 特殊字符做转义。\n\n本 crate 不实现动态 generator。需要 namespace、Git branch、container 等运行时候选时，长期扩展方向是\n接收宿主已经授权并带 revision 的候选快照；不得为了补全在同步 parser 或 classifier 路径中执行任意命令。\n\n## 当前覆盖与限制\n\n当前内置 catalog 覆盖 Shell builtins、常见 POSIX 命令，以及 Git、Cargo、Docker Compose、kubectl、\nnpm/pnpm/yarn/bun、Python/pytest、ripgrep、find 和 curl 的常用结构。PATH 中未收录 signature 的 executable\n仍会被识别为顶层命令，但其参数没有结构描述。alias 必须由产品宿主提供；Zeterm 当前尚未从交互 PTY\n采集 alias 或动态 completion snapshot。\n\n## 验证\n\n```bash\ncargo test -p zeta-shell-completion\ncargo clippy -p zeta-shell-completion --all-targets -- -D warnings\n```\n\n测试覆盖 parser byte span、pipeline、redirection、递归 grammar、严格未知值、组合短参数、command wrapper、\n递归和循环 alias、PATH replacement、有界工作区 targets、光标中间/`--option=` replacement、Shell-aware\nseparator，以及 command/subcommand/option/value/quoted path completion。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "执行路径与内部-owner",
        "title": "执行路径与内部 owner"
      },
      {
        "depth": 2,
        "id": "失败和安全边界",
        "title": "失败和安全边界"
      },
      {
        "depth": 2,
        "id": "当前覆盖与限制",
        "title": "当前覆盖与限制"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 本 README 是 Shell parser、command signature、token evidence 与 completion candidate 的实现契约。 Shell/Agent 路由顺序见 zeta-input-classifier；命令执行和授权见 zeta-shell-command。 本 crate 拥有后端无关的 Shell 结构知识。它解析一次输入，结合 command registry、PATH 快照、工作区 manifest、现有路径和宿主提供的 alias，为每个精确 token 生成可验证的描述，并从同一状态生成补全项。 它不执行命令、不读取交互 Shell 状态、不决定 Shell/Agent 路由，也不拥有任何 UI。 公共契约 Symbol 职责 调用约束 --- --- --- ShellCompletionEngine 持有 registry、PATH、工作目录、工作区候选和 alias 快照 每个 Shell 环境持有一个实例；环境变化后显式更新 ShellCommandRegistry 注册递归 ShellCommandSpec 调用者显式注册的顶层命令视为权威事实；注册过程不能启动 generator 或授权执行 ShellCommandSpec 描述一个 command/subcommand 的 option 和 argument grammar 未知 option 和无法验证的 opaque value 不产生 token evidence ShellTokenSnapshot 返回输入中每个 token 的位置和可选结构描述 classifier 只把 description.issome 当作确定性证据 ShellCompletion 返回 replacement、display、kind 和 byte replace range 产品宿主负责展示和应用，不得越过 editor authority ShellCompletionSnapshot 返回有界候选和当前 token 是否已有精确匹配 inline UI 必须用精确匹配标记避免把 git 错续写成 git-；不应自己重建 command 语义 ShellAlias 表达宿主已解析的一个 alias 本 crate 只做最多三层展开；不自行读取 dotfile 或 PTY ShellValueHint::Opaque 只声明 argument slot，不会把任意字符串当作已识别 token。只有静态 choice、 合法 integer、已注册 command、现有 path、精确 workspace target 等可验证值才产生描述。这一约束防止 git status 是做什么的 中的自然语言因为“命令接受参数”而被错误计为 Shell evidence。 执行路径与内部 owner ``text ShellCompletionEngine::analyze → parser::parseshellcommands → engine::ShellCompletionEngine::describeword environment assignment / alias / wrapper command registry / subcommand / exact option option value / workspace candidate / existing path → types::ShellTokenSnapshot ShellCompletionEngine::complete → 同一个 parser 和 CommandState → engine::completions command / alias / subcommand / option / value / path candidates ` - parser::ShellLexer 拥有 quote、escape、comment、pipeline、command separator、redirection 和 --flag=value 边界；classifier 不得再维护第二套 Shell parser。 - engine::CommandState 拥有递归 subcommand、pending option value、combined short option、wrapper command 与 alias expansion 的状态迁移。 - catalog::defaultregistry 是 Zeta 自建的内置 command grammar；没有复制 Warp completer 或其 AGPL command-signature 数据。Shell builtin 始终可用；Git、Cargo、kubectl 等外部默认规格只有对应 executable 出现在 PATH 快照时才参与顶层证据和补全。 - environment::ExecutableCatalog 在明确的 PATH 快照中冻结 executable identity；PATH 更新必须通过 setpathentries 重新建立快照。 - workspace::WorkspaceCatalog 只读 package scripts、Just recipes 和 Make targets；工作区内容变化后 调用 refreshworkspace。 - engine::completions 只投影候选，不拥有 ghost text/popup、selection、Tab 行为或 editor mutation。 失败和安全边界 - 无法读取 PATH 目录、manifest 或普通目录时跳过该来源，不启动子进程；PATH 快照最多读取 256 个 目录并保留 16,384 个 executable，单个 workspace source 最多读取 1 MiB、保留 4,096 个候选； - alias 名称非法或 replacement 为空时在进入 engine 前拒绝；循环或超过三层的 alias 不产生证据； - command registry 中没有的 option、无法验证的 value 和不存在的严格 file/directory 不产生描述； - parser 接受未完成输入并返回已有 token，不把解析失败升级为产品错误； - completion 最多返回 100 项，replace range 始终是原始 UTF-8 输入的 byte range；路径 replacement 会保留单/双引号上下文或对未引用的 Shell 特殊字符做转义。 本 crate 不实现动态 generator。需要 namespace、Git branch、container 等运行时候选时，长期扩展方向是 接收宿主已经授权并带 revision 的候选快照；不得为了补全在同步 parser 或 classifier 路径中执行任意命令。 当前覆盖与限制 当前内置 catalog 覆盖 Shell builtins、常见 POSIX 命令，以及 Git、Cargo、Docker Compose、kubectl、 npm/pnpm/yarn/bun、Python/pytest、ripgrep、find 和 curl 的常用结构。PATH 中未收录 signature 的 executable 仍会被识别为顶层命令，但其参数没有结构描述。alias 必须由产品宿主提供；Zeterm 当前尚未从交互 PTY 采集 alias 或动态 completion snapshot。 验证 `bash cargo test -p zeta-shell-completion cargo clippy -p zeta-shell-completion --all-targets -- -D warnings ` 测试覆盖 parser byte span、pipeline、redirection、递归 grammar、严格未知值、组合短参数、command wrapper、 递归和循环 alias、PATH replacement、有界工作区 targets、光标中间/--option=` replacement、Shell-aware separator，以及 command/subcommand/option/value/quoted path completion。"
  },
  {
    "slug": "crates/skills",
    "sourcePath": "zeta-rs/skills/README.md",
    "title": "zeta-skills",
    "description": "zeta-skills 验证 built-in/user/Workspace source root，流式读取并严格校验 Agent Skills SKILL.md frontmatter，计算完整文件 SHA-256 digest，并发布 deterministic、immutable catalog snapshot。显式选择后，SkillCatalog::activ…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Skill S0 catalog、S1 exact activation 与 bounded package resource read 的当前实现契约。\n> 跨 crate 的选择语义、context、Plugin/MCP 组合与后续阶段由\n> [`docs/skills.md`](../../docs/skills.md) 维护。\n\n`zeta-skills` 验证 built-in/user/Workspace source root，流式读取并严格校验 Agent Skills\n`SKILL.md` frontmatter，计算完整文件 SHA-256 digest，并发布 deterministic、immutable catalog\nsnapshot。显式选择后，`SkillCatalog::activate` 还会通过同一个受控 root 完整读取 exact\n`SKILL.md`，重新绑定 file identity 与 digest，并返回 `ActivatedSkill`。`SkillCatalog::read_resource`\n还可以在 pinned Skill digest 下读取任意有界 package-relative regular file。返回值保留原始\nbytes，因此 reference、script、asset 共用一个定位与校验接口；本 crate 仍不执行命令、解释 MIME、\n把二进制注入模型，也不拥有 config、watcher、App Server 或 Core\nintegration。共享 runtime、watcher、enablement overlay、activation contributor 与\nmodel-safe-point reload 属于 [`zeta-skills-extension`](../ext/skills/README.md)；App Server 只装配\n扩展并投影协议 DTO。\n\nRepository-owned built-in content 位于 [`assets/`](assets)，打包后位于\n`zeta-resources/skills/`。package host 通过\n`InstallContext::bundled_resource_directory(\"skills\")` 取得 candidate，再用\n`SkillSourceRoot::built_in` 重新验证；打包 provenance 不替代本 crate 的内容校验。\n当前正式内置内容只有 `skill-creator`；通用 code review/debugging 工作流没有因测试便利而成为\n产品 built-in。\n\nCatalog scanner 通过 [`zeta-file-identity`](../file-identity/README.md) 从已打开文件句柄取得\n稳定 identity 与 hard-link count。`scan_skill_file` 在读取前绑定句柄 identity，读取后重新打开\n受控 path 并比较 identity；因此 Windows 不依赖不稳定的标准库 `MetadataExt` API，也不会为了\n跨平台编译而跳过 hard-link 或换文件检测。symlink、root containment、大小限制和 diagnostic\n语义仍由本 crate 拥有。\n\n## 公共契约\n\n| Symbol | 当前职责 | 不承担 |\n| --- | --- | --- |\n| `zeta_protocol::SkillName` | Agent Skills 的 lowercase ASCII、数字、单连字符、1–64 字符 identity | display alias、Unicode normalization |\n| `zeta_protocol::{SkillSourceId, SkillId}` | 跨 config/catalog/App Server 的 source-qualified stable identity | raw host path、版本选择 |\n| `SkillSourceRoot` | host 注入并验证的 built-in/user/Workspace real directory handle | config resolution、安装、immutability |\n| `SkillCatalog::discover` | 对受控 roots 做首次 bounded metadata scan | arbitrary path search、recursive source search |\n| `SkillCatalog::refresh` | 重扫并仅在 visible projection 改变时 bump generation | filesystem watching、safe-point scheduling |\n| `SkillCatalogSnapshot::list/read` | deterministic metadata-only read API | `SKILL.md` body/content API |\n| `SkillCatalog::activate` | 解析 `SkillRef`、校验 pinned digest 并从受控 root 读取 exact body | enablement、compatibility、Turn 持久化 |\n| `ActivatedSkill` | 将完整 body 与 `FrozenSkillActivation` provenance 绑定 | Core instruction precedence、工具授权 |\n| `SkillResourcePath` | 校验 Skill package root 下非空、无 traversal 的相对路径 | absolute path、authority、执行权限 |\n| `SkillResourceKind` | 按首层目录标识 instruction/reference/script/asset/agent metadata/other 用途 | 改变 resolver、授予执行或发布权限 |\n| `SkillCatalog::read_resource` | 在 pinned Skill digest 下读取单个有界 regular file | 递归展开、脚本执行、MIME 或外部 URL |\n| `SkillResource` | 返回 relative path、用途、内容摘要与原始 bytes | 模型文本编码、host private root |\n| `SkillDiagnostic` | 隔离单 entry/source discovery failure | secret/body/private root 回传 |\n\n`SkillSourceRoot` 的 canonical host path 是 private implementation state，其 `Debug` 输出也会隐藏\n路径。调用方必须从已解析的 config/内建 release authority 构造 handle，不能把客户端提交的 path\n直接转成 source。\n\n## 文件、限制与真实调用路径\n\n```text\nSkillCatalog::discover / refresh\n  → catalog::scanner::scan_sources\n    → scan_source                     # direct children、1024-entry cap\n      → scan_skill                    # containment/type/link checks\n        → scan_skill_file             # stream hash；只 capture bounded frontmatter\n        → format::parse_frontmatter   # YAML shape + Agent Skills field validation\n  → SkillCatalogSnapshot::new\n\nSkillCatalog::activate\n  → snapshot.read(exact SkillId)\n  → resolve validated SkillSourceRoot\n  → activation::load_exact_body\n    → reject directory/manifest symlink、hard link、special/oversized file\n    → compare opened file identity before/after read\n    → compare complete SHA-256 with selected/catalog digest\n  → ActivatedSkill { FrozenSkillActivation, body }\n\nSkillCatalog::read_resource\n  → snapshot.read(exact SkillId) + verify pinned SKILL.md digest\n  → resolve validated SkillSourceRoot / Skill directory\n  → reject traversal、symlink、hard link、special/oversized file\n  → compare opened file identity before/after read\n  → SkillResource { relative path, kind, content digest, bytes }\n```\n\n关键 limits：\n\n| 项目 | 当前值 |\n| --- | ---: |\n| source direct entries | 1,024 |\n| frontmatter | 16 KiB |\n| frontmatter lines | 256 |\n| one frontmatter line | 2 KiB |\n| complete `SKILL.md` | 1 MiB |\n| one package resource | 256 KiB |\n| resource relative path | 1 KiB |\n| metadata entries | 64 |\n\n`scan_skill_file` 对完整 `SKILL.md` bytes 流式 SHA-256，但最多只保留 16 KiB frontmatter；\nMarkdown body 不进入 catalog entry、diagnostic 或 snapshot debug projection。只有\n`SkillCatalog::activate` 会在请求时把不超过 1 MiB 的 exact body 返回给调用方。扫描和激活都在\n读取前后检查\nfile identity、length、regular/single-link type；directory、manifest symlink 和 hard-linked\nmanifest 被拒绝。Resource reader 只在明确请求时读取一个 package-relative file，不递归展开\n引用；读取 scripts 和 assets 也只返回 inert bytes，不会执行或产生副作用。\n\n`format::validate_yaml_resource_shape` 在 `serde_yaml` 前限制 bytes/lines/line length/indent/flow\ndepth，并拒绝 YAML anchor、alias 与 tag control token。frontmatter 只接受规范字段，name 必须和\nparent directory 一致；description、compatibility 和 metadata 另有 catalog memory bounds。\n`allowed-tools` 只通过 `SkillMetadata::allowed_tools_hint` 暴露作者意图，绝不代表 approval。\n\n## 失败、generation 与集成\n\n一个坏 Skill 产生排序、去重后的 `SkillDiagnostic`，不会丢弃同 source 的有效 entry。source 在\nhandle 创建后消失会让下一次 refresh 发布 `SourceUnavailable` diagnostic。重复\n`SkillSourceId` 是 host composition error，`SkillCatalog::discover` 返回 `DuplicateSource`。\n\nentries 按 exact `SkillId` 排序，同名不同 source 同时保留。refresh 重新扫描事实；若 entries、\ndigest、compatibility、availability 与 diagnostics 完全不变，会复用同一个 `Arc` snapshot 和\ngeneration。任何 consumer-visible 变化才发布下一 generation。\n\n当前 `zeta-skills-extension` 组合 built-in/user/active Workspace roots、订阅 watcher，并叠加\nper-Skill enablement；App Server 以 `skills/list`/`skills/changed` 投影其 snapshot。共享 identity\n位于 `zeta-protocol`，本 crate 只 re-export，不能重新定义一套。\n\n本 crate 已有显式 exact activation 与 rooted package resource reader，但没有 automatic selector、\nMIME/artifact adapter、script executor 或 safe-point scheduler。enablement/compatibility gate、显式选择汇合和 safe-point reload 由\n`zeta-skills-extension` 拥有；它通过通用 extension API 返回带 provenance 的 prompt fragment，\nCore 不解释 catalog 或选择入口。后续能力应继续扩展该 runtime；如果\nscanner 开始执行 script、预加载 optional resource、解释 Plugin grant，或 catalog 反向依赖\nCore/App Server，表示 crate ownership 已经漂移。\n\n## 验证\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-skills\ncargo clippy --manifest-path Cargo.toml \\\n  -p zeta-skills --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/skills:skills-unit-tests\n```\n\ntests 覆盖 identity/frontmatter contract、alias/depth/size bounds、metadata-only body exclusion、\nsource-qualified ordering/read、坏 entry 隔离、symlink/hardlink、digest refresh、no-op generation、\nfollow-latest freeze、pinned digest mismatch、resource kind/bytes/binary、资源路径\ntraversal/symlink/hard-link/size、文件替换 race 与 private root diagnostic boundary。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "文件限制与真实调用路径",
        "title": "文件、限制与真实调用路径"
      },
      {
        "depth": 2,
        "id": "失败generation-与集成",
        "title": "失败、generation 与集成"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 本 README 是 Skill S0 catalog、S1 exact activation 与 bounded package resource read 的当前实现契约。 跨 crate 的选择语义、context、Plugin/MCP 组合与后续阶段由 docs/skills.md 维护。 zeta-skills 验证 built-in/user/Workspace source root，流式读取并严格校验 Agent Skills SKILL.md frontmatter，计算完整文件 SHA-256 digest，并发布 deterministic、immutable catalog snapshot。显式选择后，SkillCatalog::activate 还会通过同一个受控 root 完整读取 exact SKILL.md，重新绑定 file identity 与 digest，并返回 ActivatedSkill。SkillCatalog::readresource 还可以在 pinned Skill digest 下读取任意有界 package-relative regular file。返回值保留原始 bytes，因此 reference、script、asset 共用一个定位与校验接口；本 crate 仍不执行命令、解释 MIME、 把二进制注入模型，也不拥有 config、watcher、App Server 或 Core integration。共享 runtime、watcher、enablement overlay、activation contributor 与 model-safe-point reload 属于 zeta-skills-extension；App Server 只装配 扩展并投影协议 DTO。 Repository-owned built-in content 位于 assets/，打包后位于 zeta-resources/skills/。package host 通过 InstallContext::bundledresourcedirectory \"skills\" 取得 candidate，再用 SkillSourceRoot::builtin 重新验证；打包 provenance 不替代本 crate 的内容校验。 当前正式内置内容只有 skill-creator；通用 code review/debugging 工作流没有因测试便利而成为 产品 built-in。 Catalog scanner 通过 zeta-file-identity 从已打开文件句柄取得 稳定 identity 与 hard-link count。scanskillfile 在读取前绑定句柄 identity，读取后重新打开 受控 path 并比较 identity；因此 Windows 不依赖不稳定的标准库 MetadataExt API，也不会为了 跨平台编译而跳过 hard-link 或换文件检测。symlink、root containment、大小限制和 diagnostic 语义仍由本 crate 拥有。 公共契约 Symbol 当前职责 不承担 --- --- --- zetaprotocol::SkillName Agent Skills 的 lowercase ASCII、数字、单连字符、1–64 字符 identity display alias、Unicode normalization zetaprotocol:: SkillSourceId, SkillId 跨 config/catalog/App Server 的 source-qualified stable identity raw host path、版本选择 SkillSourceRoot host 注入并验证的 built-in/user/Workspace real directory handle config resolution、安装、immutability SkillCatalog::discover 对受控 roots 做首次 bounded metadata scan arbitrary path search、recursive source search SkillCatalog::refresh 重扫并仅在 visible projection 改变时 bump generation filesystem watching、safe-point scheduling SkillCatalogSnapshot::list/read deterministic metadata-only read API SKILL.md body/content API SkillCatalog::activate 解析 SkillRef、校验 pinned digest 并从受控 root 读取 exact body enablement、compatibility、Turn 持久化 ActivatedSkill 将完整 body 与 FrozenSkillActivation provenance 绑定 Core instruction precedence、工具授权 SkillResourcePath 校验 Skill package root 下非空、无 traversal 的相对路径 absolute path、authority、执行权限 SkillResourceKind 按首层目录标识 instruction/reference/script/asset/agent metadata/other 用途 改变 resolver、授予执行或发布权限 SkillCatalog::readresource 在 pinned Skill digest 下读取单个有界 regular file 递归展开、脚本执行、MIME 或外部 URL SkillResource 返回 relative path、用途、内容摘要与原始 bytes 模型文本编码、host private root SkillDiagnostic 隔离单 entry/source discovery failure secret/body/private root 回传 SkillSourceRoot 的 canonical host path 是 private implementation state，其 Debug 输出也会隐藏 路径。调用方必须从已解析的 config/内建 release authority 构造 handle，不能把客户端提交的 path 直接转成 source。 文件、限制与真实调用路径 ``text SkillCatalog::discover / refresh → catalog::scanner::scansources → scansource direct children、1024-entry cap → scanskill containment/type/link checks → scanskillfile stream hash；只 capture bounded frontmatter → format::parsefrontmatter YAML shape + Agent Skills field validation → SkillCatalogSnapshot::new SkillCatalog::activate → snapshot.read exact SkillId → resolve validated SkillSourceRoot → activation::loadexactbody → reject directory/manifest symlink、hard link、special/oversized file → compare opened file identity before/after read → compare complete SHA-256 with selected/catalog digest → ActivatedSkill FrozenSkillActivation, body SkillCatalog::readresource → snapshot.read exact SkillId + verify pinned SKILL.md digest → resolve validated SkillSourceRoot / Skill directory → reject traversal、symlink、hard link、special/oversized file → compare opened file identity before/after read → SkillResource relative path, kind, content digest, bytes ` 关键 limits： 项目 当前值 --- ---: source direct entries 1,024 frontmatter 16 KiB frontmatter lines 256 one frontmatter line 2 KiB complete SKILL.md 1 MiB one package resource 256 KiB resource relative path 1 KiB metadata entries 64 scanskillfile 对完整 SKILL.md bytes 流式 SHA-256，但最多只保留 16 KiB frontmatter； Markdown body 不进入 catalog entry、diagnostic 或 snapshot debug projection。只有 SkillCatalog::activate 会在请求时把不超过 1 MiB 的 exact body 返回给调用方。扫描和激活都在 读取前后检查 file identity、length、regular/single-link type；directory、manifest symlink 和 hard-linked manifest 被拒绝。Resource reader 只在明确请求时读取一个 package-relative file，不递归展开 引用；读取 scripts 和 assets 也只返回 inert bytes，不会执行或产生副作用。 format::validateyamlresourceshape 在 serdeyaml 前限制 bytes/lines/line length/indent/flow depth，并拒绝 YAML anchor、alias 与 tag control token。frontmatter 只接受规范字段，name 必须和 parent directory 一致；description、compatibility 和 metadata 另有 catalog memory bounds。 allowed-tools 只通过 SkillMetadata::allowedtoolshint 暴露作者意图，绝不代表 approval。 失败、generation 与集成 一个坏 Skill 产生排序、去重后的 SkillDiagnostic，不会丢弃同 source 的有效 entry。source 在 handle 创建后消失会让下一次 refresh 发布 SourceUnavailable diagnostic。重复 SkillSourceId 是 host composition error，SkillCatalog::discover 返回 DuplicateSource。 entries 按 exact SkillId 排序，同名不同 source 同时保留。refresh 重新扫描事实；若 entries、 digest、compatibility、availability 与 diagnostics 完全不变，会复用同一个 Arc snapshot 和 generation。任何 consumer-visible 变化才发布下一 generation。 当前 zeta-skills-extension 组合 built-in/user/active Workspace roots、订阅 watcher，并叠加 per-Skill enablement；App Server 以 skills/list/skills/changed 投影其 snapshot。共享 identity 位于 zeta-protocol，本 crate 只 re-export，不能重新定义一套。 本 crate 已有显式 exact activation 与 rooted package resource reader，但没有 automatic selector、 MIME/artifact adapter、script executor 或 safe-point scheduler。enablement/compatibility gate、显式选择汇合和 safe-point reload 由 zeta-skills-extension 拥有；它通过通用 extension API 返回带 provenance 的 prompt fragment， Core 不解释 catalog 或选择入口。后续能力应继续扩展该 runtime；如果 scanner 开始执行 script、预加载 optional resource、解释 Plugin grant，或 catalog 反向依赖 Core/App Server，表示 crate ownership 已经漂移。 验证 `bash cargo test --manifest-path Cargo.toml -p zeta-skills cargo clippy --manifest-path Cargo.toml \\ -p zeta-skills --all-targets --no-deps -- -D warnings bazel test //zeta-rs/skills:skills-unit-tests `` tests 覆盖 identity/frontmatter contract、alias/depth/size bounds、metadata-only body exclusion、 source-qualified ordering/read、坏 entry 隔离、symlink/hardlink、digest refresh、no-op generation、 follow-latest freeze、pinned digest mismatch、resource kind/bytes/binary、资源路径 traversal/symlink/hard-link/size、文件替换 race 与 private root diagnostic boundary。"
  },
  {
    "slug": "crates/ext/skills",
    "sourcePath": "zeta-rs/ext/skills/README.md",
    "title": "zeta-skills-extension",
    "description": "跨 crate 的 Skill 语义和分阶段演进由 docs/skills.md 维护；底层目录、 文件和 catalog 契约由 zeta-skills 维护。",
    "group": "Crate 实现参考",
    "markdown": "跨 crate 的 Skill 语义和分阶段演进由 [`docs/skills.md`](../../../docs/skills.md) 维护；底层目录、\n文件和 catalog 契约由 [`zeta-skills`](../../skills/README.md) 维护。\n\n`zeta-skills-extension` 拥有 `zeta-skills` 之上的 Agent 运行时编排：组合 built-in、user 和当前\nWorkspace source，应用启用状态与兼容性策略，在 Turn 提交前冻结显式选择，在模型调用安全点重新\n加载 exact content，并通过 `zeta-extension-api` 贡献有界元数据目录、exact Skill instructions\n以及单个只读 `skills-read` 模型工具。\n\n## 所有权\n\n| Symbol | 当前职责 |\n| --- | --- |\n| `SkillRuntime` | catalog generation、source composition、exact activation 和 watcher lifecycle |\n| `SkillActivationContributor` 实现 | 把显式 `UserInput::Skill` 选择转换为 durable activation |\n| `TurnInputContributor` 实现 | 输出 metadata-only `catalog_prompt` 和 exact frozen activation fragment |\n| `SkillToolContributor` | 注册 `SkillReadTool`，不依赖 App Server |\n| `SkillReadTool` | 解析 exact `source + name`，通过 tagged target 读取完整说明或 digest-pinned package-relative text resource |\n| `SkillRuntimeEventSink` | public catalog generation 改变后通知安装它的 host |\n\n`zeta-skills` 仍是底层文件和 catalog authority。Core 只调用通用 extension lifecycle contract。\nApp Server 可以提供配置、事件 adapter 和 list DTO，但不得选择、激活、加载、缓存或渲染 Skill\ninstructions。\n\n真实调用路径：\n\n```text\ninstall\n  -> TurnInputContributor::contribute\n       -> catalog_prompt (metadata only, at most 8 KiB)\n  -> ReadOnlyToolContributor::contribute\n       -> SkillReadTool::execute\n            -> target = instructions\n                 -> SkillRuntime::activate_model_selected\n                      -> SkillCatalog::activate\n            -> target = resource\n                 -> SkillRuntime::read_model_resource\n                      -> SkillCatalog::read_resource\n```\n\n手动选择和模型选择共用 catalog validation 与 exact file loading，但 durable shape 不同。手动选择\n在 `TurnAccepted` 中冻结并作为 `PromptFragment` 贡献；模型选择发生在 Tool loop 中，成功的\n`skills-read` result 成为下一次 invocation 的 durable model context。后端不做关键词分类。\n\n当前模型侧 resource read 只接受 UTF-8 text、每次一个文件，上限 256 KiB。底层 resource contract\n保留 binary bytes，但 MIME/artifact materialization 尚未接通；script execution 仍是普通 Tool\noperation。catalog prompt 固定上限 8 KiB，Core 继续通过普通 BestEffort context budget 计算它。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      }
    ],
    "searchText": "跨 crate 的 Skill 语义和分阶段演进由 docs/skills.md 维护；底层目录、 文件和 catalog 契约由 zeta-skills 维护。 zeta-skills-extension 拥有 zeta-skills 之上的 Agent 运行时编排：组合 built-in、user 和当前 Workspace source，应用启用状态与兼容性策略，在 Turn 提交前冻结显式选择，在模型调用安全点重新 加载 exact content，并通过 zeta-extension-api 贡献有界元数据目录、exact Skill instructions 以及单个只读 skills-read 模型工具。 所有权 Symbol 当前职责 --- --- SkillRuntime catalog generation、source composition、exact activation 和 watcher lifecycle SkillActivationContributor 实现 把显式 UserInput::Skill 选择转换为 durable activation TurnInputContributor 实现 输出 metadata-only catalogprompt 和 exact frozen activation fragment SkillToolContributor 注册 SkillReadTool，不依赖 App Server SkillReadTool 解析 exact source + name，通过 tagged target 读取完整说明或 digest-pinned package-relative text resource SkillRuntimeEventSink public catalog generation 改变后通知安装它的 host zeta-skills 仍是底层文件和 catalog authority。Core 只调用通用 extension lifecycle contract。 App Server 可以提供配置、事件 adapter 和 list DTO，但不得选择、激活、加载、缓存或渲染 Skill instructions。 真实调用路径： ``text install - TurnInputContributor::contribute - catalogprompt metadata only, at most 8 KiB - ReadOnlyToolContributor::contribute - SkillReadTool::execute - target = instructions - SkillRuntime::activatemodelselected - SkillCatalog::activate - target = resource - SkillRuntime::readmodelresource - SkillCatalog::readresource ` 手动选择和模型选择共用 catalog validation 与 exact file loading，但 durable shape 不同。手动选择 在 TurnAccepted 中冻结并作为 PromptFragment 贡献；模型选择发生在 Tool loop 中，成功的 skills-read` result 成为下一次 invocation 的 durable model context。后端不做关键词分类。 当前模型侧 resource read 只接受 UTF-8 text、每次一个文件，上限 256 KiB。底层 resource contract 保留 binary bytes，但 MIME/artifact materialization 尚未接通；script execution 仍是普通 Tool operation。catalog prompt 固定上限 8 KiB，Core 继续通过普通 BestEffort context budget 计算它。"
  },
  {
    "slug": "crates/slash-commands",
    "sourcePath": "zeta-rs/slash-commands/README.md",
    "title": "zeta-slash-commands",
    "description": "zeta-slash-commands 为 App Server、TUI 和 zeta-ui/native 提供同一套 catalog 校验、输入语法、 匹配、选择、补全与提交解析。它不渲染 UI、不执行命令，也不拥有 App Server composition。 这里的选择状态只针对真实 Slash Command catalog；它不负责把 Skills、命令和其他…",
    "group": "Crate 实现参考",
    "markdown": "> 本文拥有无渲染 Slash Commands 核心的实现契约。跨客户端产品语义见\n> [`docs/slash-commands.md`](../../docs/slash-commands.md)；wire snapshot 见\n> [`docs/zeta-app-server-api.md`](../../docs/zeta-app-server-api.md)。TUI、native 与 Desktop\n> 分别拥有自己的渲染适配器。通用斜杠启动面板的多列表组合由\n> [`zeta-slash-launcher`](../slash-launcher/README.md) 拥有。\n\n`zeta-slash-commands` 为 App Server、TUI 和 zeta-ui/native 提供同一套 catalog 校验、输入语法、\n匹配、选择、补全与提交解析。它不渲染 UI、不执行命令，也不拥有 App Server composition。\n这里的选择状态只针对真实 Slash Command catalog；它不负责把 Skills、命令和其他产品列表组合成\n通用斜杠面板。\n\n## 1. 边界与依赖\n\n| 拥有 | 不拥有 |\n| --- | --- |\n| canonical `SlashCommandDefinition` 的名称、描述、冲突校验与稳定顺序 | 命令实际执行与授权 |\n| `SlashCommandInput` 的 `/` 查询、补全范围和参数解析 | composer 文本存储、键盘或鼠标事件 |\n| `SlashCommandsState` 的匹配、选择与 dismiss 状态 | Ratatui、WGPU、DOM 或 Alpha Editor 绘制与滚动几何 |\n| 与 model 分离的 server/local/Skill contribution kind | `SkillRef`、App Server 初始化、IPC 或 Renderer lifecycle |\n\n本 crate 依赖 `zeta-app-server-protocol` 以直接复用 canonical wire definition。App Server、TUI 和\nnative 可以依赖本 crate；本 crate 禁止反向依赖这些消费者。\n\n## 2. 公共契约\n\n- `SlashCommandCatalog::new` 构造 server-only snapshot；\n- `SlashCommandCatalog::with_local_and_server` 按 local、server 顺序合并并拒绝任何重名；\n- `SlashCommandCatalog::with_local_server_and_skills` 在同一 snapshot 末尾追加 Skill command definition，\n  并保留独立 `Skill` origin；调用方继续拥有 exact `SkillRef` binding；\n- `SlashCommandInput` 对同一 catalog 提供 query、completion、invocation 与 command element range；\n- `SlashCommandsState` 保存当前输入对应的匹配、选择与 dismiss 状态；viewport、可见范围与滚动由各 renderer 保存；\n- `SlashCommandsView` 是 renderer 只读 projection，不允许渲染过程改变状态。\n\n输入校验按 Rust UTF-8 byte range 工作。名称只允许 lowercase ASCII letters、digits 与 interior\nhyphen；空描述、非法名称和跨来源冲突都使整份 catalog 构造失败。\n\n## 3. 内部接口地图\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `catalog::append_commands` | private | 校验并按 origin 追加完整 snapshot | caller 绕过它直接修改 command vector |\n| `catalog::validate_command` | private | 固定 canonical name 与 description 规则 | 客户端重新实现另一套校验 |\n| `input::command_name_range` | private | 找出首个 `/name` token 的 byte range | popup 和 submission 使用不同 grammar |\n| `input::trimmed_range` | private | 保留参数在原输入中的 exact range | adapter 自行切割参数文本 |\n| `SlashCommandsState::refresh` | private | 从 input、cursor 与 catalog 原子重建 view | renderer 保存第二份匹配或 query authority |\n\n调用关系：\n\n```text\nSlashCommandCatalog::{new,with_local_and_server,with_local_server_and_skills}\n  → append_commands → validate_command\n\nSlashCommandsState::sync_input\n  → refresh → SlashCommandInput::{query,matching_commands}\n  → SlashCommandsView\n\nrenderer action\n  → SlashCommandsState::{select_next,select_previous,select,dismiss}\n  → selected_completion / invocation\n```\n\n## 4. 失败语义与接入义务\n\nCatalog validation 是全有或全无；失败时不发布部分 snapshot。`SlashCommandInput` 对不完整、未知、\n越界 cursor 或不允许参数的输入返回 `None`，不猜测命令。dismiss 绑定 exact input；文本变化后匹配\n可以重新显示。\n\nAdapter 必须把完整 catalog 一次性交给 `set_catalog`，不能逐项修改内部列表；提交时必须使用同一\nstate/catalog 解析，避免展示了一个命令却由另一套 parser 拒绝。\n\n## 5. 测试与修改影响\n\n运行：\n\n```bash\ncargo test -p zeta-slash-commands\ncargo clippy -p zeta-slash-commands --all-targets -- -D warnings\n```\n\n修改名称语法、匹配顺序、completion range、selection 或 dismiss 规则时，必须同步更新本 crate tests、\nApp Server initialization tests、TUI/native adapter tests 和 Desktop 的跨语言 fixture tests。\n\n## 6. 当前限制与扩展点\n\n- **Current**：Rust App Server、TUI 与 native 可直接复用同一实现。\n- **Current**：Desktop 直接消费 protocol-generated `SlashCommandDefinition`；它与 Rust surfaces\n  使用同一个 model，TypeScript adapter 只绑定 Workbench action 和 Alpha renderer。\n- **Conformance**：Rust core 与 Desktop adapter 共同执行同一 fixtures，保证不同语言宿主不会改变\n  model validation、matching 或 input semantics。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与依赖",
        "title": "1. 边界与依赖"
      },
      {
        "depth": 2,
        "id": "2-公共契约",
        "title": "2. 公共契约"
      },
      {
        "depth": 2,
        "id": "3-内部接口地图",
        "title": "3. 内部接口地图"
      },
      {
        "depth": 2,
        "id": "4-失败语义与接入义务",
        "title": "4. 失败语义与接入义务"
      },
      {
        "depth": 2,
        "id": "5-测试与修改影响",
        "title": "5. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "6-当前限制与扩展点",
        "title": "6. 当前限制与扩展点"
      }
    ],
    "searchText": " 本文拥有无渲染 Slash Commands 核心的实现契约。跨客户端产品语义见 docs/slash-commands.md；wire snapshot 见 docs/zeta-app-server-api.md。TUI、native 与 Desktop 分别拥有自己的渲染适配器。通用斜杠启动面板的多列表组合由 zeta-slash-launcher 拥有。 zeta-slash-commands 为 App Server、TUI 和 zeta-ui/native 提供同一套 catalog 校验、输入语法、 匹配、选择、补全与提交解析。它不渲染 UI、不执行命令，也不拥有 App Server composition。 这里的选择状态只针对真实 Slash Command catalog；它不负责把 Skills、命令和其他产品列表组合成 通用斜杠面板。 1. 边界与依赖 拥有 不拥有 --- --- canonical SlashCommandDefinition 的名称、描述、冲突校验与稳定顺序 命令实际执行与授权 SlashCommandInput 的 / 查询、补全范围和参数解析 composer 文本存储、键盘或鼠标事件 SlashCommandsState 的匹配、选择与 dismiss 状态 Ratatui、WGPU、DOM 或 Alpha Editor 绘制与滚动几何 与 model 分离的 server/local/Skill contribution kind SkillRef、App Server 初始化、IPC 或 Renderer lifecycle 本 crate 依赖 zeta-app-server-protocol 以直接复用 canonical wire definition。App Server、TUI 和 native 可以依赖本 crate；本 crate 禁止反向依赖这些消费者。 2. 公共契约 - SlashCommandCatalog::new 构造 server-only snapshot； - SlashCommandCatalog::withlocalandserver 按 local、server 顺序合并并拒绝任何重名； - SlashCommandCatalog::withlocalserverandskills 在同一 snapshot 末尾追加 Skill command definition， 并保留独立 Skill origin；调用方继续拥有 exact SkillRef binding； - SlashCommandInput 对同一 catalog 提供 query、completion、invocation 与 command element range； - SlashCommandsState 保存当前输入对应的匹配、选择与 dismiss 状态；viewport、可见范围与滚动由各 renderer 保存； - SlashCommandsView 是 renderer 只读 projection，不允许渲染过程改变状态。 输入校验按 Rust UTF-8 byte range 工作。名称只允许 lowercase ASCII letters、digits 与 interior hyphen；空描述、非法名称和跨来源冲突都使整份 catalog 构造失败。 3. 内部接口地图 Symbol 可见性 职责 漂移信号 --- --- --- --- catalog::appendcommands private 校验并按 origin 追加完整 snapshot caller 绕过它直接修改 command vector catalog::validatecommand private 固定 canonical name 与 description 规则 客户端重新实现另一套校验 input::commandnamerange private 找出首个 /name token 的 byte range popup 和 submission 使用不同 grammar input::trimmedrange private 保留参数在原输入中的 exact range adapter 自行切割参数文本 SlashCommandsState::refresh private 从 input、cursor 与 catalog 原子重建 view renderer 保存第二份匹配或 query authority 调用关系： ``text SlashCommandCatalog:: new,withlocalandserver,withlocalserverandskills → appendcommands → validatecommand SlashCommandsState::syncinput → refresh → SlashCommandInput:: query,matchingcommands → SlashCommandsView renderer action → SlashCommandsState:: selectnext,selectprevious,select,dismiss → selectedcompletion / invocation ` 4. 失败语义与接入义务 Catalog validation 是全有或全无；失败时不发布部分 snapshot。SlashCommandInput 对不完整、未知、 越界 cursor 或不允许参数的输入返回 None，不猜测命令。dismiss 绑定 exact input；文本变化后匹配 可以重新显示。 Adapter 必须把完整 catalog 一次性交给 setcatalog，不能逐项修改内部列表；提交时必须使用同一 state/catalog 解析，避免展示了一个命令却由另一套 parser 拒绝。 5. 测试与修改影响 运行： `bash cargo test -p zeta-slash-commands cargo clippy -p zeta-slash-commands --all-targets -- -D warnings ` 修改名称语法、匹配顺序、completion range、selection 或 dismiss 规则时，必须同步更新本 crate tests、 App Server initialization tests、TUI/native adapter tests 和 Desktop 的跨语言 fixture tests。 6. 当前限制与扩展点 - Current：Rust App Server、TUI 与 native 可直接复用同一实现。 - Current：Desktop 直接消费 protocol-generated SlashCommandDefinition`；它与 Rust surfaces 使用同一个 model，TypeScript adapter 只绑定 Workbench action 和 Alpha renderer。 - Conformance：Rust core 与 Desktop adapter 共同执行同一 fixtures，保证不同语言宿主不会改变 model validation、matching 或 input semantics。"
  },
  {
    "slug": "crates/slash-launcher",
    "sourcePath": "zeta-rs/slash-launcher/README.md",
    "title": "zeta-slash-launcher",
    "description": "zeta-slash-launcher 把产品选择的若干列表组合成一个可搜索、可选择的无渲染快照。它不认识 Slash Command、Skill 或产品动作，也不决定选择后的执行行为。",
    "group": "Crate 实现参考",
    "markdown": "> 本文拥有通用斜杠启动面板核心的实现契约。Slash Launcher 与真正 Slash Command 的跨产品边界见\n> [`docs/slash-commands.md`](../../docs/slash-commands.md)。命令 catalog、补全和提交解析继续由\n> [`zeta-slash-commands`](../slash-commands/README.md) 拥有。\n\n`zeta-slash-launcher` 把产品选择的若干列表组合成一个可搜索、可选择的无渲染快照。它不认识\nSlash Command、Skill 或产品动作，也不决定选择后的执行行为。\n\n## 1. 边界与依赖\n\n| 拥有 | 不拥有 |\n| --- | --- |\n| 多列表的稳定顺序、ID 校验与全量快照 | Slash Command 定义、参数语法或执行 |\n| 首个 `/query` token 的识别与替换范围 | Skill 发现、启用状态、正文或激活 |\n| 跨列表前缀匹配、选择、循环移动与 dismiss | TUI、WGPU、DOM、键鼠事件或 popup 几何 |\n| 选中项的 `list_id + item_id` 和展示快照 | target registry、handler、IPC、异步刷新或授权 |\n\n本 crate 没有运行时依赖。产品宿主和领域 adapter 可以依赖它；它不得反向依赖\n`zeta-slash-commands`、`zeta-skills`、App Server protocol 或 renderer。\n\n## 2. 公共契约\n\n- `SlashLauncherItem` 保存来源内稳定 ID、展示字段和可选搜索别名。`item_id` 必须能让产品解析到\n  同一份不可变业务绑定，不能只是会被刷新后重用的临时序号。\n- `SlashLauncherList` 是一个产品选择的列表。列表可以来自命令、Skills 或其他领域；同一列表内\n  item ID 必须唯一，展示名称允许重复。\n- `SlashLauncherSnapshot::compose` 按调用方顺序组合列表并拒绝重复 list ID。不同列表可以包含同名\n  项，因为选中键由 `(list_id, item_id)` 唯一确定。\n- `SlashLauncherInput` 只把输入开头、光标所在的首个 `/query` token 解释为启动面板查询；它不解析\n  Slash Command 参数。`SlashLauncherQuery::range` 覆盖完整首 token，便于产品替换已输入内容。\n- `SlashLauncherState` 原子地从快照、输入和光标生成 `SlashLauncherView`，并拥有选择与 dismiss。\n  `SlashLauncherSelection` 是 owned value，列表刷新后已取得的选择不会改变。\n\n本 crate 有意不提供 provider trait。发现、异步刷新、缓存和生命周期由产品决定；调用方在准备好\n完整列表后一次性构造新快照并调用 `set_snapshot`。\n\n## 3. 内部接口地图\n\n| Symbol | 可见性 | 职责 | 漂移信号 |\n| --- | --- | --- | --- |\n| `model::validate_id` | private | 固定 list/item dispatch key 的最小稳定性规则 | 领域名称语法或业务授权进入通用校验 |\n| `SlashLauncherItem::matches` | crate-private | 对 label 和 keywords 做大小写无关前缀匹配 | renderer 建立第二套匹配 authority |\n| `input::launcher_token_range` | private | 定位输入开头的完整斜杠 token | 把命令参数 grammar 搬入 Launcher |\n| `SlashLauncherState::refresh` | private | 从输入与快照一次重建 query、items 和 selection | adapter 逐项修改内部结果或保存另一份选择状态 |\n\n调用关系：\n\n```text\nproduct adapters\n  → SlashLauncherItem → SlashLauncherList\n  → SlashLauncherSnapshot::compose\n  → SlashLauncherState::{new,set_snapshot,sync_input}\n      → SlashLauncherInput::query\n      → SlashLauncherSnapshot::matching\n      → SlashLauncherView\n\nrenderer activation\n  → SlashLauncherSelection::{list_id,item_id}\n  → product-owned typed binding and handler\n```\n\n## 4. 失败语义与接入义务\n\n构造失败是全有或全无：空白 label/title、不稳定 ID、重复 list ID 或同一列表内重复 item ID 都不会\n产生部分快照；description 允许为空。跨列表同名不报错，由 renderer 用列表标题说明来源。无前导\n`/`、光标越界、光标不在首 token 或 UTF-8 byte 边界非法时，`SlashLauncherInput::query` 返回\n`None` 并关闭 view。\n\n产品 adapter 必须拥有 `item_id` 到 typed target 的绑定，并保证选中后按同一不可变身份解析。Launcher\n不会调用 handler，也不会在刷新后按 label 猜测 target。\n\n## 5. 测试与修改影响\n\n```bash\ncargo test -p zeta-slash-launcher\ncargo clippy -p zeta-slash-launcher --all-targets -- -D warnings\n```\n\n修改 ID、匹配、token range、顺序、选择或 dismiss 规则时，必须同步更新本 crate 的 sibling tests。\n接入具体产品后，还要更新该产品的列表 adapter、无窗口交互测试和 target binding 测试。\n\n## 6. 当前状态与扩展点\n\n- **Current**：通用数据模型、列表组合、输入查询和交互状态已实现并由 crate 单测覆盖。\n- **Current limitation**：TUI、Desktop 和 zeterm 尚未切换到本 crate；现有交互继续按各产品当前路径\n  工作。\n- **Current limitation**：当前匹配是稳定的大小写无关前缀匹配，没有 fuzzy ranking、分页或协议模型。\n- **Extension point**：出现真实跨语言消费者后，再为同一 value contract 增加 protocol/TypeScript\n  投影；不要提前把产品 handler 或领域 target 下沉到本 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与依赖",
        "title": "1. 边界与依赖"
      },
      {
        "depth": 2,
        "id": "2-公共契约",
        "title": "2. 公共契约"
      },
      {
        "depth": 2,
        "id": "3-内部接口地图",
        "title": "3. 内部接口地图"
      },
      {
        "depth": 2,
        "id": "4-失败语义与接入义务",
        "title": "4. 失败语义与接入义务"
      },
      {
        "depth": 2,
        "id": "5-测试与修改影响",
        "title": "5. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "6-当前状态与扩展点",
        "title": "6. 当前状态与扩展点"
      }
    ],
    "searchText": " 本文拥有通用斜杠启动面板核心的实现契约。Slash Launcher 与真正 Slash Command 的跨产品边界见 docs/slash-commands.md。命令 catalog、补全和提交解析继续由 zeta-slash-commands 拥有。 zeta-slash-launcher 把产品选择的若干列表组合成一个可搜索、可选择的无渲染快照。它不认识 Slash Command、Skill 或产品动作，也不决定选择后的执行行为。 1. 边界与依赖 拥有 不拥有 --- --- 多列表的稳定顺序、ID 校验与全量快照 Slash Command 定义、参数语法或执行 首个 /query token 的识别与替换范围 Skill 发现、启用状态、正文或激活 跨列表前缀匹配、选择、循环移动与 dismiss TUI、WGPU、DOM、键鼠事件或 popup 几何 选中项的 listid + itemid 和展示快照 target registry、handler、IPC、异步刷新或授权 本 crate 没有运行时依赖。产品宿主和领域 adapter 可以依赖它；它不得反向依赖 zeta-slash-commands、zeta-skills、App Server protocol 或 renderer。 2. 公共契约 - SlashLauncherItem 保存来源内稳定 ID、展示字段和可选搜索别名。itemid 必须能让产品解析到 同一份不可变业务绑定，不能只是会被刷新后重用的临时序号。 - SlashLauncherList 是一个产品选择的列表。列表可以来自命令、Skills 或其他领域；同一列表内 item ID 必须唯一，展示名称允许重复。 - SlashLauncherSnapshot::compose 按调用方顺序组合列表并拒绝重复 list ID。不同列表可以包含同名 项，因为选中键由 listid, itemid 唯一确定。 - SlashLauncherInput 只把输入开头、光标所在的首个 /query token 解释为启动面板查询；它不解析 Slash Command 参数。SlashLauncherQuery::range 覆盖完整首 token，便于产品替换已输入内容。 - SlashLauncherState 原子地从快照、输入和光标生成 SlashLauncherView，并拥有选择与 dismiss。 SlashLauncherSelection 是 owned value，列表刷新后已取得的选择不会改变。 本 crate 有意不提供 provider trait。发现、异步刷新、缓存和生命周期由产品决定；调用方在准备好 完整列表后一次性构造新快照并调用 setsnapshot。 3. 内部接口地图 Symbol 可见性 职责 漂移信号 --- --- --- --- model::validateid private 固定 list/item dispatch key 的最小稳定性规则 领域名称语法或业务授权进入通用校验 SlashLauncherItem::matches crate-private 对 label 和 keywords 做大小写无关前缀匹配 renderer 建立第二套匹配 authority input::launchertokenrange private 定位输入开头的完整斜杠 token 把命令参数 grammar 搬入 Launcher SlashLauncherState::refresh private 从输入与快照一次重建 query、items 和 selection adapter 逐项修改内部结果或保存另一份选择状态 调用关系： ``text product adapters → SlashLauncherItem → SlashLauncherList → SlashLauncherSnapshot::compose → SlashLauncherState:: new,setsnapshot,syncinput → SlashLauncherInput::query → SlashLauncherSnapshot::matching → SlashLauncherView renderer activation → SlashLauncherSelection:: listid,itemid → product-owned typed binding and handler ` 4. 失败语义与接入义务 构造失败是全有或全无：空白 label/title、不稳定 ID、重复 list ID 或同一列表内重复 item ID 都不会 产生部分快照；description 允许为空。跨列表同名不报错，由 renderer 用列表标题说明来源。无前导 /、光标越界、光标不在首 token 或 UTF-8 byte 边界非法时，SlashLauncherInput::query 返回 None 并关闭 view。 产品 adapter 必须拥有 itemid 到 typed target 的绑定，并保证选中后按同一不可变身份解析。Launcher 不会调用 handler，也不会在刷新后按 label 猜测 target。 5. 测试与修改影响 `bash cargo test -p zeta-slash-launcher cargo clippy -p zeta-slash-launcher --all-targets -- -D warnings `` 修改 ID、匹配、token range、顺序、选择或 dismiss 规则时，必须同步更新本 crate 的 sibling tests。 接入具体产品后，还要更新该产品的列表 adapter、无窗口交互测试和 target binding 测试。 6. 当前状态与扩展点 - Current：通用数据模型、列表组合、输入查询和交互状态已实现并由 crate 单测覆盖。 - Current limitation：TUI、Desktop 和 zeterm 尚未切换到本 crate；现有交互继续按各产品当前路径 工作。 - Current limitation：当前匹配是稳定的大小写无关前缀匹配，没有 fuzzy ranking、分页或协议模型。 - Extension point：出现真实跨语言消费者后，再为同一 value contract 增加 protocol/TypeScript 投影；不要提前把产品 handler 或领域 target 下沉到本 crate。"
  },
  {
    "slug": "crates/storage",
    "sourcePath": "zeta-rs/storage/README.md",
    "title": "zeta-storage",
    "description": "zeta-storage 是本地 Session/Thread authority 的 SQLite adapter。它不定义领域 event、 reducer、Config schema、trace/export 格式或 UI query model。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责 SQLite 物理存储契约。Session envelope 由\n> [`zeta-session-store`](../session-store/README.md) 拥有，Thread record 由\n> [`zeta-history`](../history/README.md) 拥有，batch validator 与 trait 见\n> [`zeta-thread-store`](../thread-store/README.md)；跨 crate 恢复顺序见\n> [`zeta-rollout`](../rollout/README.md)。\n\n`zeta-storage` 是本地 Session/Thread authority 的 SQLite adapter。它不定义领域 event、\nreducer、Config schema、trace/export 格式或 UI query model。\n\n## 当前接口\n\n| Symbol | 职责 |\n| --- | --- |\n| `SqliteSessionStore` | 实现 `SessionStore`，读写 `session_*` tables |\n| `SqliteThreadStore` | 实现 `ThreadStore`，读写 `thread_*` tables |\n| `LeaseDirectory` | 为 Core aggregate writer 提供 profile-scoped advisory lease |\n\n两个 store 必须打开同一 `<profile_root>/state.sqlite3`。每个 adapter 使用独立 connection；\nSQLite WAL 协调它们的并发事务。\n\n## Schema 与提交\n\n`sqlite::connection::open` 是物理 schema 的唯一安装入口：\n\n```text\nzeta_schema_migrations(component = \"event-store\")\nthread_streams / thread_batches / thread_events\nsession_streams / session_batches / session_events\n```\n\n每次 append 固定执行：\n\n```text\nBEGIN IMMEDIATE\n→ materialize aggregate stream row\n→ read current sequence\n→ run domain batch validator\n→ reject duplicate batch/event identity\n→ insert full typed envelopes\n→ compare-and-set current sequence\n→ COMMIT\n```\n\n任一步失败都会回滚整批，不暴露部分 event。完整 typed envelope 以 JSON 保存，同时将 aggregate\nidentity、sequence、event ID 和 schema version 放入稳定列，用于唯一性和顺序查询。加载后仍会\n调用领域 validator，数据库内容不能绕过 envelope contract。\n\n连接统一启用：\n\n- foreign keys；\n- WAL；\n- `synchronous=FULL`；\n- 5 秒 busy timeout；\n- component schema version fail-closed。\n\nSQLite 在这里是 authority，不是可删除 projection。旧 JSONL rollout、tail recovery、checksum\nframe 和双写路径已移除；当前开发期不自动导入旧格式。\n\n## 所有权与漂移信号\n\n- `sqlite::connection` 拥有物理表、PRAGMA 与 schema version；\n- `sqlite::session` 只做 `SessionStore` 映射；\n- `sqlite::thread` 只做 `ThreadStore` 映射；\n- batch legality 继续委托 `validate_session_append_batch` / `validate_append_batch`；\n- recovery 顺序继续由 `LocalStateRepository` 拥有。\n\n若本 crate 开始解释 Session/Thread lifecycle、生成 event、组合 Core，或让 Config 表依赖\nSession/Thread reducer，即表示所有权漂移。若 App Server 直接写这些表，同样是绕过 store port。\n\n## 验证\n\n```text\ncargo test -p zeta-storage\n```\n\n测试覆盖共享数据库重开、typed event recovery、sequence conflict、原子 batch 与 writer lease。\n新增 schema migration 时还必须增加旧 schema fixture、失败中断和重开测试。",
    "headings": [
      {
        "depth": 2,
        "id": "当前接口",
        "title": "当前接口"
      },
      {
        "depth": 2,
        "id": "schema-与提交",
        "title": "Schema 与提交"
      },
      {
        "depth": 2,
        "id": "所有权与漂移信号",
        "title": "所有权与漂移信号"
      },
      {
        "depth": 2,
        "id": "验证",
        "title": "验证"
      }
    ],
    "searchText": " 本 README 负责 SQLite 物理存储契约。Session envelope 由 zeta-session-store 拥有，Thread record 由 zeta-history 拥有，batch validator 与 trait 见 zeta-thread-store；跨 crate 恢复顺序见 zeta-rollout。 zeta-storage 是本地 Session/Thread authority 的 SQLite adapter。它不定义领域 event、 reducer、Config schema、trace/export 格式或 UI query model。 当前接口 Symbol 职责 --- --- SqliteSessionStore 实现 SessionStore，读写 session tables SqliteThreadStore 实现 ThreadStore，读写 thread tables LeaseDirectory 为 Core aggregate writer 提供 profile-scoped advisory lease 两个 store 必须打开同一 <profileroot /state.sqlite3。每个 adapter 使用独立 connection； SQLite WAL 协调它们的并发事务。 Schema 与提交 sqlite::connection::open 是物理 schema 的唯一安装入口： ``text zetaschemamigrations component = \"event-store\" threadstreams / threadbatches / threadevents sessionstreams / sessionbatches / sessionevents ` 每次 append 固定执行： `text BEGIN IMMEDIATE → materialize aggregate stream row → read current sequence → run domain batch validator → reject duplicate batch/event identity → insert full typed envelopes → compare-and-set current sequence → COMMIT ` 任一步失败都会回滚整批，不暴露部分 event。完整 typed envelope 以 JSON 保存，同时将 aggregate identity、sequence、event ID 和 schema version 放入稳定列，用于唯一性和顺序查询。加载后仍会 调用领域 validator，数据库内容不能绕过 envelope contract。 连接统一启用： - foreign keys； - WAL； - synchronous=FULL； - 5 秒 busy timeout； - component schema version fail-closed。 SQLite 在这里是 authority，不是可删除 projection。旧 JSONL rollout、tail recovery、checksum frame 和双写路径已移除；当前开发期不自动导入旧格式。 所有权与漂移信号 - sqlite::connection 拥有物理表、PRAGMA 与 schema version； - sqlite::session 只做 SessionStore 映射； - sqlite::thread 只做 ThreadStore 映射； - batch legality 继续委托 validatesessionappendbatch / validateappendbatch； - recovery 顺序继续由 LocalStateRepository 拥有。 若本 crate 开始解释 Session/Thread lifecycle、生成 event、组合 Core，或让 Config 表依赖 Session/Thread reducer，即表示所有权漂移。若 App Server 直接写这些表，同样是绕过 store port。 验证 `text cargo test -p zeta-storage `` 测试覆盖共享数据库重开、typed event recovery、sequence conflict、原子 batch 与 writer lease。 新增 schema migration 时还必须增加旧 schema fixture、失败中断和重开测试。"
  },
  {
    "slug": "crates/symbol-index",
    "sourcePath": "zeta-rs/symbol-index/README.md",
    "title": "zeta-symbol-index",
    "description": "zeta-symbol-index 把 zeta-code-index 已发布并复核的 Workspace sources 投影为 revision-bound 语法声明，在 SQLite 中复用未变化文件的结果，并用 Nucleo 提供本地 fuzzy symbol search。它不扫描 文件系统、不请求 Language Server，也不把语法声明提升为编译器语义。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README canonical 拥有 `zeta-symbol-index` 的当前实现契约。编辑器语法、Language Server、工作区\n> 检索与未来代码图的跨系统边界和阶段计划由\n> [`docs/code-intelligence.md`](../../docs/code-intelligence.md) 拥有；Workspace source/chunk authority\n> 见 [`zeta-code-index` README](../code-index/README.md)。\n\n`zeta-symbol-index` 把 `zeta-code-index` 已发布并复核的 Workspace sources 投影为 revision-bound\n语法声明，在 SQLite 中复用未变化文件的结果，并用 Nucleo 提供本地 fuzzy symbol search。它不扫描\n文件系统、不请求 Language Server，也不把语法声明提升为编译器语义。\n\n## 1. 所有权与依赖方向\n\n当前 crate 负责：\n\n- 比较 `CodeIndexManifest` 与已有 symbol projection；\n- 只通过 `CodeIndex::materialize_sources` 取得变化文件正文；\n- 使用 `zeta-syntax` 提取支持语言的 `DocumentSymbol`；\n- 按 source revision 复用未变化文件的 symbols；\n- SQLite transaction 发布完整 generation；\n- 从已发布 generation 构造只读 Nucleo candidate set；\n- exact/prefix boost、fuzzy Top K、matched indices 与 cooperative cancellation；\n- 从 CodeIndex canonical overlay 投影 dirty document symbols，并在查询中抑制同路径磁盘 rows；\n- query、per-source、total symbol 和 matcher worker limits。\n\n当前 crate 不负责：\n\n- filesystem scan、ignore、symlink、trust 或源码读取策略；\n- Editor snapshot 的采集、owner lifecycle 与 save 判定；\n- LSP workspace symbol、definition、reference 或 hierarchy；\n- stable semantic `SymbolId`、occurrence、edge、SCIP 或跨语言 resolver；\n- App Server watcher、RPC、profile placement 或 Desktop provider；\n- AI candidate fusion、content materialization 或 token budget。\n\n依赖方向固定为：\n\n```text\nzeta-symbol-index → zeta-code-index + zeta-syntax + zeta-async-utils\n```\n\n如果本 crate 开始使用 `ignore::WalkBuilder`、直接打开 Workspace source、依赖 LSP/App Server DTO，或\n决定跨来源最终排序，说明 ownership 已经漂移。\n\n## 2. 文件与关键接口\n\n| 文件 / symbol | 可见性 | 单一职责 | 不能承担 |\n| --- | --- | --- | --- |\n| `SymbolIndex` | public | 组合 CodeIndex、store、limits 与当前 matcher，暴露 reconcile/search/overlay projection | watcher、RPC、Editor state |\n| `SymbolIndex::reconcile` | public | 对齐当前 CodeIndex source generation，并原子替换 symbol projection | 自主扫描或相信 watcher event 内容 |\n| `SymbolIndex::search_with_cancellation` | public | 校验 query limit，在当前 matcher snapshot 查询 | LSP fallback 或 UI query generation |\n| `extractor::extract_source` | private | 将一个 `MaterializedSource` 投影为有界 syntax symbols | 文件读取、semantic reference resolution |\n| `IndexStore` | private | root/schema/syntax-facts binding 与 SQLite publication | source authority、matcher policy |\n| `IndexStore::load_projection` | private | 恢复 source rows 和 symbols，供 revision reuse 与 startup hydrate | 读取源码纠正 stale row |\n| `SymbolMatcher` | private | 持有 Nucleo worker/candidates，并生成 scored Top K | persistence、跨 provider fusion |\n| `SYNTAX_FACTS_VERSION` | `zeta-syntax` public constant | 使 grammar/tags 语义变化失效持久化 facts | 数据库 schema version |\n\n公共 module 保持 private，由 `lib.rs` named exports 暴露唯一 crate API。第一版 `SymbolReference` 明确\n绑定 source revision；它不是 rename/move 后仍稳定的 semantic identity。\n\n## 3. 当前调用路径\n\n```mermaid\nflowchart TD\n    Open[\"SymbolIndex::open\"] --> StoreOpen[\"IndexStore::open\"]\n    StoreOpen --> Load[\"IndexStore::load_projection\"]\n    Load --> Hydrate[\"SymbolMatcher::new\"]\n\n    Reconcile[\"SymbolIndex::reconcile\"] --> Manifest[\"CodeIndex::manifest\"]\n    Manifest --> Compare[\"比较 path + revision + language\"]\n    Compare --> Materialize[\"CodeIndex::materialize_sources\"]\n    Materialize --> Extract[\"extractor::extract_source\"]\n    Extract --> Publish[\"IndexStore::replace_projection\"]\n    Publish --> Swap[\"原子替换当前 SymbolMatcher\"]\n\n    Query[\"search_with_cancellation\"] --> Match[\"SymbolMatcher::search\"]\n    Overlay[\"project_overlay / close_overlay\"] --> OverlaySymbols[\"ephemeral symbols\"]\n    OverlaySymbols --> Match\n    Match --> Result[\"SymbolSearchHit\"]\n```\n\n`reconcile` 使用独立 operation lock 串行化 projection publication。查询先取得当前 matcher 的 `Arc`，\n所以 reconcile 可以在新 matcher 完整建立后一次替换；正在运行的查询继续读取旧的 immutable candidate\nset，不会观察半发布 generation。\n\n## 4. 数据与身份\n\n`SymbolReference` 保存：\n\n- `IndexRootId`；\n- Workspace-relative path；\n- `SourceRevision`；\n- `IndexedLanguage`；\n- source byte length；\n- 文件内 deterministic ordinal；\n- declaration 和 selection 的 UTF-8 byte/row/byte-column range。\n\nSQLite projection 使用三个表：\n\n```text\nsymbol_index_metadata\nsymbol_index_files\nsymbol_index_symbols\n```\n\nmetadata 同时绑定 root ID、schema version、`SYNTAX_FACTS_VERSION`、自身 generation、CodeIndex source\ngeneration 与 total-limit state。root 不匹配会失败；schema 或 syntax-facts identity 不兼容会删除并\n重建可再生 projection。\n\n持久化 reconcile 会读取已有 symbol rows，但只对 revision/language 不变且上一 generation 未触发\ntotal limit 的文件复用。上一 generation 命中 total limit 时，下一次 source generation 变化会重新提取\n全部 sources，避免删除早期文件后仍永久缺失后来被截断的 symbols。\n\n## 5. 查询与排序\n\nNucleo candidate set 只匹配 symbol name。当前排序依次使用：\n\n1. exact case-insensitive name boost；\n2. case-insensitive prefix boost；\n3. Nucleo fuzzy score；\n4. name、path、ordinal deterministic tie-break。\n\nmatcher 先取得 bounded candidate pool，再应用 boost 和最终 Top K。`matched_indices` 使用 Unicode\ncharacter indices，供 UI 高亮，不是 UTF-8 byte offset。空 query 返回 deterministic 的首批 symbols。\n\n当前没有 container/path proximity、recent-use 或跨 provider score normalization；这些属于 Desktop\nWorkspace Symbol aggregator 或后续有基准的 matcher 演进，不能静默改变为 AI final ranking。\n\n## 6. 失败、取消与资源限制\n\n| 条件 | 当前结果 |\n| --- | --- |\n| CodeIndex root 与 symbol store root 不同 | `StorageRootMismatch` / `SourceRootMismatch` |\n| source revision 在 materialization 前已变化 | 传播 `CodeIndexError`，不发布新 generation |\n| syntax grammar/query 无法加载 | 带相对 path 的 `Syntax` error，不替换 last-ready projection |\n| query 超过 byte limit | `QueryTooLarge` |\n| cancellation 已生效或在 matcher tick 间生效 | `Cancelled`，不返回 partial hits |\n| unsupported/plaintext language | 保存 source row但不产生 symbols |\n| per-source/total symbol limit | 截断结果并在 source/snapshot 显式标记 |\n| persistent path 非普通文件 | 明确 I/O failure |\n\nPersistent SQLite 在 Unix 上强制普通文件和 `0600`。一次 publication 失败时 transaction rollback，旧\ngeneration 和 matcher 继续可用。\n\n## 7. 集成义务\n\n- App Server 必须先让 CodeIndex 发布 canonical source generation，再调度 `reconcile`。\n- App Server 必须把 symbol store 放在 root-digest scoped profile path；crate 不选择 profile。\n- 查询调用方必须维护自己的 query generation；crate 的 cancellation 不替代 UI stale-result gate。\n- 打开搜索结果前，产品必须验证当前 source/selection，不能把旧 range 当作可持久 semantic anchor。\n- LSP provider 与本地 symbol provider 的并发、dedupe 和 fallback 属于 Desktop aggregator。\n- AI consumer 不能直接把 `SymbolSearchHit` 正文发给模型；必须通过 Workspace authority materialize。\n- App Server 当前在 CodeIndex generation 发布后调度 reconcile，并通过\n  `workspace/symbolIndex/status|search` 暴露能力；Desktop `ISymbolIndexService` 与 LSP provider 并发查询。\n- overlay snapshot 必须先由 CodeIndex 接受，再投影到 SymbolIndex；close 与 save handoff 也必须以\n  CodeIndex 的 canonical dirty-path 状态为准。\n\n## 8. 测试与修改影响\n\n```bash\ncargo test -p zeta-symbol-index\ncargo clippy -p zeta-symbol-index --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/symbol-index:symbol-index-unit-tests\n```\n\n测试位于 sibling `src/symbol_index_tests.rs`，当前覆盖 extraction/fuzzy、exact boost、add/change/delete、\npersistent reopen/no-op generation、total limit、cancellation、dirty override 和 overlay close。修改以下\n内容时必须同步检查：\n\n| 修改 | 同步检查 |\n| --- | --- |\n| symbol kind mapping | `zeta-syntax` tags、store serde ID、App Server DTO |\n| range coordinate | Desktop UTF-16 conversion、accept-time validation |\n| schema | `SCHEMA_VERSION`、reset/reopen tests |\n| syntax facts | `SYNTAX_FACTS_VERSION`、CodeIndex chunk boundary consumers |\n| matcher score | deterministic ranking fixtures 与未来 Desktop fusion |\n| reconcile reuse | stale source、limit recovery、watcher generation tests |\n\n## 9. 当前限制与下一步\n\n- Current：只索引 `zeta-syntax` 当前支持的 JavaScript/JSX、TypeScript/TSX、JSON/JSONC、Rust、Shell。\n- Current：只保存平面 document symbols；`container_name` 暂为空。\n- Current：只匹配 symbol name，不匹配 path/container/signature。\n- Current：磁盘 CodeIndex generation 是持久 source，CodeIndex canonical overlay 是唯一 ephemeral source；\n  overlay 不写 SQLite，查询时完全抑制同路径磁盘 rows。\n- Current：App Server watcher/reconcile、status/search RPC 与 Desktop provider 已接通；Desktop 将本地结果\n  与 LSP workspace symbols 分阶段、确定性合并并隔离 provider failure。\n- Current limitation：没有 occurrence/reference/edge，也没有跨 edit 稳定 semantic identity。\n- Potential：出现真实 compiler/indexer consumer 后增加 semantic symbol/occurrence/edge；不扩张当前\n  revision-bound `SymbolReference` 的承诺。",
    "headings": [
      {
        "depth": 2,
        "id": "1-所有权与依赖方向",
        "title": "1. 所有权与依赖方向"
      },
      {
        "depth": 2,
        "id": "2-文件与关键接口",
        "title": "2. 文件与关键接口"
      },
      {
        "depth": 2,
        "id": "3-当前调用路径",
        "title": "3. 当前调用路径"
      },
      {
        "depth": 2,
        "id": "4-数据与身份",
        "title": "4. 数据与身份"
      },
      {
        "depth": 2,
        "id": "5-查询与排序",
        "title": "5. 查询与排序"
      },
      {
        "depth": 2,
        "id": "6-失败取消与资源限制",
        "title": "6. 失败、取消与资源限制"
      },
      {
        "depth": 2,
        "id": "7-集成义务",
        "title": "7. 集成义务"
      },
      {
        "depth": 2,
        "id": "8-测试与修改影响",
        "title": "8. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "9-当前限制与下一步",
        "title": "9. 当前限制与下一步"
      }
    ],
    "searchText": " 本 README canonical 拥有 zeta-symbol-index 的当前实现契约。编辑器语法、Language Server、工作区 检索与未来代码图的跨系统边界和阶段计划由 docs/code-intelligence.md 拥有；Workspace source/chunk authority 见 zeta-code-index README。 zeta-symbol-index 把 zeta-code-index 已发布并复核的 Workspace sources 投影为 revision-bound 语法声明，在 SQLite 中复用未变化文件的结果，并用 Nucleo 提供本地 fuzzy symbol search。它不扫描 文件系统、不请求 Language Server，也不把语法声明提升为编译器语义。 1. 所有权与依赖方向 当前 crate 负责： - 比较 CodeIndexManifest 与已有 symbol projection； - 只通过 CodeIndex::materializesources 取得变化文件正文； - 使用 zeta-syntax 提取支持语言的 DocumentSymbol； - 按 source revision 复用未变化文件的 symbols； - SQLite transaction 发布完整 generation； - 从已发布 generation 构造只读 Nucleo candidate set； - exact/prefix boost、fuzzy Top K、matched indices 与 cooperative cancellation； - 从 CodeIndex canonical overlay 投影 dirty document symbols，并在查询中抑制同路径磁盘 rows； - query、per-source、total symbol 和 matcher worker limits。 当前 crate 不负责： - filesystem scan、ignore、symlink、trust 或源码读取策略； - Editor snapshot 的采集、owner lifecycle 与 save 判定； - LSP workspace symbol、definition、reference 或 hierarchy； - stable semantic SymbolId、occurrence、edge、SCIP 或跨语言 resolver； - App Server watcher、RPC、profile placement 或 Desktop provider； - AI candidate fusion、content materialization 或 token budget。 依赖方向固定为： ``text zeta-symbol-index → zeta-code-index + zeta-syntax + zeta-async-utils ` 如果本 crate 开始使用 ignore::WalkBuilder、直接打开 Workspace source、依赖 LSP/App Server DTO，或 决定跨来源最终排序，说明 ownership 已经漂移。 2. 文件与关键接口 文件 / symbol 可见性 单一职责 不能承担 --- --- --- --- SymbolIndex public 组合 CodeIndex、store、limits 与当前 matcher，暴露 reconcile/search/overlay projection watcher、RPC、Editor state SymbolIndex::reconcile public 对齐当前 CodeIndex source generation，并原子替换 symbol projection 自主扫描或相信 watcher event 内容 SymbolIndex::searchwithcancellation public 校验 query limit，在当前 matcher snapshot 查询 LSP fallback 或 UI query generation extractor::extractsource private 将一个 MaterializedSource 投影为有界 syntax symbols 文件读取、semantic reference resolution IndexStore private root/schema/syntax-facts binding 与 SQLite publication source authority、matcher policy IndexStore::loadprojection private 恢复 source rows 和 symbols，供 revision reuse 与 startup hydrate 读取源码纠正 stale row SymbolMatcher private 持有 Nucleo worker/candidates，并生成 scored Top K persistence、跨 provider fusion SYNTAXFACTSVERSION zeta-syntax public constant 使 grammar/tags 语义变化失效持久化 facts 数据库 schema version 公共 module 保持 private，由 lib.rs named exports 暴露唯一 crate API。第一版 SymbolReference 明确 绑定 source revision；它不是 rename/move 后仍稳定的 semantic identity。 3. 当前调用路径 `mermaid flowchart TD Open \"SymbolIndex::open\" -- StoreOpen \"IndexStore::open\" StoreOpen -- Load \"IndexStore::loadprojection\" Load -- Hydrate \"SymbolMatcher::new\" Reconcile \"SymbolIndex::reconcile\" -- Manifest \"CodeIndex::manifest\" Manifest -- Compare \"比较 path + revision + language\" Compare -- Materialize \"CodeIndex::materializesources\" Materialize -- Extract \"extractor::extractsource\" Extract -- Publish \"IndexStore::replaceprojection\" Publish -- Swap \"原子替换当前 SymbolMatcher\" Query \"searchwithcancellation\" -- Match \"SymbolMatcher::search\" Overlay \"projectoverlay / closeoverlay\" -- OverlaySymbols \"ephemeral symbols\" OverlaySymbols -- Match Match -- Result \"SymbolSearchHit\" ` reconcile 使用独立 operation lock 串行化 projection publication。查询先取得当前 matcher 的 Arc， 所以 reconcile 可以在新 matcher 完整建立后一次替换；正在运行的查询继续读取旧的 immutable candidate set，不会观察半发布 generation。 4. 数据与身份 SymbolReference 保存： - IndexRootId； - Workspace-relative path； - SourceRevision； - IndexedLanguage； - source byte length； - 文件内 deterministic ordinal； - declaration 和 selection 的 UTF-8 byte/row/byte-column range。 SQLite projection 使用三个表： `text symbolindexmetadata symbolindexfiles symbolindexsymbols ` metadata 同时绑定 root ID、schema version、SYNTAXFACTSVERSION、自身 generation、CodeIndex source generation 与 total-limit state。root 不匹配会失败；schema 或 syntax-facts identity 不兼容会删除并 重建可再生 projection。 持久化 reconcile 会读取已有 symbol rows，但只对 revision/language 不变且上一 generation 未触发 total limit 的文件复用。上一 generation 命中 total limit 时，下一次 source generation 变化会重新提取 全部 sources，避免删除早期文件后仍永久缺失后来被截断的 symbols。 5. 查询与排序 Nucleo candidate set 只匹配 symbol name。当前排序依次使用： 1. exact case-insensitive name boost； 2. case-insensitive prefix boost； 3. Nucleo fuzzy score； 4. name、path、ordinal deterministic tie-break。 matcher 先取得 bounded candidate pool，再应用 boost 和最终 Top K。matchedindices 使用 Unicode character indices，供 UI 高亮，不是 UTF-8 byte offset。空 query 返回 deterministic 的首批 symbols。 当前没有 container/path proximity、recent-use 或跨 provider score normalization；这些属于 Desktop Workspace Symbol aggregator 或后续有基准的 matcher 演进，不能静默改变为 AI final ranking。 6. 失败、取消与资源限制 条件 当前结果 --- --- CodeIndex root 与 symbol store root 不同 StorageRootMismatch / SourceRootMismatch source revision 在 materialization 前已变化 传播 CodeIndexError，不发布新 generation syntax grammar/query 无法加载 带相对 path 的 Syntax error，不替换 last-ready projection query 超过 byte limit QueryTooLarge cancellation 已生效或在 matcher tick 间生效 Cancelled，不返回 partial hits unsupported/plaintext language 保存 source row但不产生 symbols per-source/total symbol limit 截断结果并在 source/snapshot 显式标记 persistent path 非普通文件 明确 I/O failure Persistent SQLite 在 Unix 上强制普通文件和 0600。一次 publication 失败时 transaction rollback，旧 generation 和 matcher 继续可用。 7. 集成义务 - App Server 必须先让 CodeIndex 发布 canonical source generation，再调度 reconcile。 - App Server 必须把 symbol store 放在 root-digest scoped profile path；crate 不选择 profile。 - 查询调用方必须维护自己的 query generation；crate 的 cancellation 不替代 UI stale-result gate。 - 打开搜索结果前，产品必须验证当前 source/selection，不能把旧 range 当作可持久 semantic anchor。 - LSP provider 与本地 symbol provider 的并发、dedupe 和 fallback 属于 Desktop aggregator。 - AI consumer 不能直接把 SymbolSearchHit 正文发给模型；必须通过 Workspace authority materialize。 - App Server 当前在 CodeIndex generation 发布后调度 reconcile，并通过 workspace/symbolIndex/status search 暴露能力；Desktop ISymbolIndexService 与 LSP provider 并发查询。 - overlay snapshot 必须先由 CodeIndex 接受，再投影到 SymbolIndex；close 与 save handoff 也必须以 CodeIndex 的 canonical dirty-path 状态为准。 8. 测试与修改影响 `bash cargo test -p zeta-symbol-index cargo clippy -p zeta-symbol-index --all-targets --no-deps -- -D warnings bazel test //zeta-rs/symbol-index:symbol-index-unit-tests ` 测试位于 sibling src/symbolindextests.rs，当前覆盖 extraction/fuzzy、exact boost、add/change/delete、 persistent reopen/no-op generation、total limit、cancellation、dirty override 和 overlay close。修改以下 内容时必须同步检查： 修改 同步检查 --- --- symbol kind mapping zeta-syntax tags、store serde ID、App Server DTO range coordinate Desktop UTF-16 conversion、accept-time validation schema SCHEMAVERSION、reset/reopen tests syntax facts SYNTAXFACTSVERSION、CodeIndex chunk boundary consumers matcher score deterministic ranking fixtures 与未来 Desktop fusion reconcile reuse stale source、limit recovery、watcher generation tests 9. 当前限制与下一步 - Current：只索引 zeta-syntax 当前支持的 JavaScript/JSX、TypeScript/TSX、JSON/JSONC、Rust、Shell。 - Current：只保存平面 document symbols；containername 暂为空。 - Current：只匹配 symbol name，不匹配 path/container/signature。 - Current：磁盘 CodeIndex generation 是持久 source，CodeIndex canonical overlay 是唯一 ephemeral source； overlay 不写 SQLite，查询时完全抑制同路径磁盘 rows。 - Current：App Server watcher/reconcile、status/search RPC 与 Desktop provider 已接通；Desktop 将本地结果 与 LSP workspace symbols 分阶段、确定性合并并隔离 provider failure。 - Current limitation：没有 occurrence/reference/edge，也没有跨 edit 稳定 semantic identity。 - Potential：出现真实 compiler/indexer consumer 后增加 semantic symbol/occurrence/edge；不扩张当前 revision-bound SymbolReference` 的承诺。"
  },
  {
    "slug": "crates/syntax",
    "sourcePath": "zeta-rs/syntax/README.md",
    "title": "zeta-syntax",
    "description": "zeta-syntax 对宿主提供有界、增量、与 presentation 无关的源码结构分析。当前实现支持 JavaScript/JSX、TypeScript/TSX、Rust、JSON、JSONC 和 Shell，使用 tree-sitter 保存每个打开文档的 增量 parse tree，并为精确 host revision 派生 syntax tokens、fol…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 `zeta-syntax` 当前 public/private interface、执行路径、失败语义与修改影响。\n> 跨 Desktop、Native、EditorHost 和 LSP 的语言分析边界由\n> [`docs/syntax-analysis.md`](../../docs/syntax-analysis.md) 拥有。\n\n`zeta-syntax` 对宿主提供有界、增量、与 presentation 无关的源码结构分析。当前实现支持\nJavaScript/JSX、TypeScript/TSX、Rust、JSON、JSONC 和 Shell，使用 tree-sitter 保存每个打开文档的\n增量 parse tree，并为精确 host revision 派生 syntax tokens、folding ranges、document symbols、parse\ndiagnostics，以及按需的 structural selection scopes。它不读取文件、不监听 workspace、\n不持久化索引、不启动语言服务器，也不依赖 legacy editor runtime、`zeta-editor`、Native paint types 或 App\nServer DTO。\n\n## 所有权与公共接口\n\n| API / type | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `SyntaxDocument` | 保存一个打开文档的 text、line index、parser、tree、revision 与 limits | 文件加载、保存、dirty state、并发调度 |\n| `SyntaxLanguage` | 选择 crate 内已注册且经过测试的 grammar/query 组合 | 接受任意 native grammar pointer 或用户 query |\n| `DocumentRevision` | 绑定宿主权威文本和派生 snapshot；更新时必须单调增加 | 充当磁盘 revision、Git identity 或全局 sequence |\n| `SyntaxEdit` | 以当前文档的 UTF-8 byte range 表达 replace/insert/delete；`apply_edits` 原子接收同一旧 revision 上的非重叠 batch | 接受编辑器 UTF-16 position 或猜测编码 |\n| `SyntaxSnapshot` | 返回同一 revision 的 tokens、folds、symbols 与 diagnostics | 暴露 `tree_sitter::Tree`、`Node` 或跨语言统一 AST |\n| `SyntaxDocument::selection_ranges` | 按一个 exact UTF-8 byte range 返回有界、innermost-first named parser scopes | 把整棵 tree 放进普通 snapshot 或声称 semantic AST |\n| `AnalysisLimits` | 限制文档和派生 collection 的资源使用 | 限制宿主队列、IPC message 或 workspace 文件数 |\n| `SyntaxTokenKind` | 提供不含主题颜色的语言中立 highlight category | 决定主题、foreground 或 decoration layer |\n| `DocumentSymbol` | 表达 tree-sitter tags query 发现的语法声明 | 类型解析、跨文件 reference、rename 或 completion |\n\n`SyntaxPoint.column` 和 `SyntaxRange.bytes` 都使用 UTF-8 byte offset。Desktop adapter 必须显式在\nAlpha UTF-16 position 与该 contract 之间转换；Native host 可以直接使用 byte range，但仍必须\n拒绝不匹配当前 EditorHost revision 的 snapshot。\n\n## 内部接口与执行路径\n\n| private symbol | 精确职责 | 不能承担 |\n| --- | --- | --- |\n| `LanguageConfiguration::load` | 把 `SyntaxLanguage` 绑定到 grammar、highlights query 和 tags query | product language detection 或动态安装 grammar |\n| `LineIndex` | 以增量 line starts 把 byte offset 转换为 tree-sitter row/byte-column | Unicode display column 或 LSP position encoding |\n| `collect_tokens` | 运行 grammar highlight query、映射稳定 category 并应用结果上限 | 主题选择或编辑器 provider registration |\n| `collect_folding_ranges` | 从已解析 tree 的明确容器节点派生多行范围 | UI 折叠状态 |\n| `collect_selection_ranges` | 只沿 requested selection 的 named ancestor chain 收集 scopes | 全树遍历、selection history 或 edit application |\n| `collect_symbols` | 从 tags query 的 `definition.*` 与 `name` capture 构造声明 | workspace index lifecycle |\n| `collect_diagnostics` | 收集 error/missing node，并应用结果上限 | compiler diagnostics 或 quick fix |\n\n```text\nSyntaxDocument::open / open_with_limits\n  → validate_document_size\n  → LanguageConfiguration::load\n  → Parser::parse\n  → LineIndex::new\n\nSyntaxDocument::apply_edit / apply_edits\n  → revision / range / UTF-8 boundary / size validation\n  → LineIndex::point\n  → Tree::edit on a cheap tree clone\n  → String::replace_range\n  → Parser::parse with the edited previous tree\n  → commit tree + line index + revision\n  → SyntaxDocument::snapshot\n\nSyntaxDocument::snapshot\n  ├─ collect_tokens\n  ├─ collect_folding_ranges\n  ├─ collect_symbols\n  └─ collect_diagnostics\n\nSyntaxDocument::selection_ranges(exact byte range)\n  → range / UTF-8 boundary validation\n  → named_descendant_for_byte_range\n  → bounded ancestor walk（默认最多 64 层）\n```\n\n编辑校验发生在文本修改之前。解析使用已应用 edit 的旧 tree clone；如果 tree-sitter 取消解析，\n实现会恢复被替换文本，权威 revision/tree 保持不变。解析成功仍可包含可恢复的语法错误，这些\n错误作为 diagnostics 返回，不会导致整次操作失败。\n\ngrammar query 可能为父节点提供通用分类，并为子节点提供更具体分类，因此 highlight capture\n可以重叠。range 顺序是确定的；consumer 应让较晚、较窄的 capture 覆盖较早 capture。各结果\ncollection 达到对应 `AnalysisLimits` 后停止收集，不会把仍可使用的部分 snapshot 变成错误。\n即使 `max_diagnostics` 把 diagnostics 截断为零，`SyntaxSnapshot::has_errors` 仍保留准确事实。\n\n## 测试、修改影响与当前限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-syntax\ncargo clippy --manifest-path Cargo.toml -p zeta-syntax --all-targets -- -D warnings\nbazel test //zeta-rs/syntax:syntax-unit-tests\n```\n\n`src/syntax_tests.rs` 覆盖 Rust token/fold/symbol projection、JSON/JSONC token/fold/comment、Shell\ncommand/operator/comment token、ECMAScript grammar、单行和多行增量编辑、revision binding、UTF-8\nboundary、selection scope ordering、更新后的 line index、resource limits，以及失败不修改文档。\n\n新增 grammar 时必须同时增加 `SyntaxLanguage`、`LanguageConfiguration::load` 的 grammar/query\n绑定、capture mapping 测试和语言 fixture。修改 edit coordinate contract 时必须同步检查所有\nhost adapter；修改 symbol category 时必须同步检查未来 workspace index schema。若 crate 开始\n读取 workspace、保存数据库、选择主题或处理 LSP request，说明 ownership 已经漂移。\n\n当前限制：\n\n- Current：注册 JavaScript/JSX、TypeScript/TSX、Rust、Shell grammar，以及由独立私有语言模块选择的 JSON/JSONC grammar；JSON 与 JSONC\n  共用上游 comment-capable JSON parser，syntax snapshot 不代替 language server 的严格格式校验；\n- Current：单个 edit 和同一旧 revision 上的非重叠 batch 都使用 UTF-8 byte range；宿主负责事件 coalescing；\n- Current：文本保存在 `String`，中间插入需要移动后续 bytes；超大文档的 rope/chunked input 尚未实现；\n- Current：snapshot 派生为同步调用；异步 worker、debounce 与 cancellation 由产品宿主负责；\n- Current：App Server 使用 connection/model-owned open/change/close session 将 Rust、JSON 与 JSONC 的完整\n  analysis snapshot 投影为 UTF-16 wire DTO；Aster 消费 token、parse diagnostic、symbol 与 folding；\n- Current：App Server 的 `syntax/selectionRanges` 只为离散命令按当前 selection 返回有限 scopes；普通\n  `syntax/analyze` 不携带全树 selection nodes，Aster Smart Select 负责 revision/cancellation/stale gate；\n- Current：Native `zeta-editor` 已私有组合 incremental analysis 与 folding；Aster folding/document-symbol\n  provider 已接入，具体 outline presentation 仍由产品 UI 决定；\n- Current：`zeta-symbol-index` 已作为独立 consumer 持久化 verified declaration facts；磁盘/dirty\n  arbitration 由 `zeta-code-index` overlay 拥有，不进入本 crate；\n- Potential：继续增加按需 structural operations；mutation 仍由 Editor 生成 undo transaction 并验证\n  revision，本 crate 不拥有 editor history。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权与公共接口",
        "title": "所有权与公共接口"
      },
      {
        "depth": 2,
        "id": "内部接口与执行路径",
        "title": "内部接口与执行路径"
      },
      {
        "depth": 2,
        "id": "测试修改影响与当前限制",
        "title": "测试、修改影响与当前限制"
      }
    ],
    "searchText": " 本 README 拥有 zeta-syntax 当前 public/private interface、执行路径、失败语义与修改影响。 跨 Desktop、Native、EditorHost 和 LSP 的语言分析边界由 docs/syntax-analysis.md 拥有。 zeta-syntax 对宿主提供有界、增量、与 presentation 无关的源码结构分析。当前实现支持 JavaScript/JSX、TypeScript/TSX、Rust、JSON、JSONC 和 Shell，使用 tree-sitter 保存每个打开文档的 增量 parse tree，并为精确 host revision 派生 syntax tokens、folding ranges、document symbols、parse diagnostics，以及按需的 structural selection scopes。它不读取文件、不监听 workspace、 不持久化索引、不启动语言服务器，也不依赖 legacy editor runtime、zeta-editor、Native paint types 或 App Server DTO。 所有权与公共接口 API / type 当前职责 明确不做 --- --- --- SyntaxDocument 保存一个打开文档的 text、line index、parser、tree、revision 与 limits 文件加载、保存、dirty state、并发调度 SyntaxLanguage 选择 crate 内已注册且经过测试的 grammar/query 组合 接受任意 native grammar pointer 或用户 query DocumentRevision 绑定宿主权威文本和派生 snapshot；更新时必须单调增加 充当磁盘 revision、Git identity 或全局 sequence SyntaxEdit 以当前文档的 UTF-8 byte range 表达 replace/insert/delete；applyedits 原子接收同一旧 revision 上的非重叠 batch 接受编辑器 UTF-16 position 或猜测编码 SyntaxSnapshot 返回同一 revision 的 tokens、folds、symbols 与 diagnostics 暴露 treesitter::Tree、Node 或跨语言统一 AST SyntaxDocument::selectionranges 按一个 exact UTF-8 byte range 返回有界、innermost-first named parser scopes 把整棵 tree 放进普通 snapshot 或声称 semantic AST AnalysisLimits 限制文档和派生 collection 的资源使用 限制宿主队列、IPC message 或 workspace 文件数 SyntaxTokenKind 提供不含主题颜色的语言中立 highlight category 决定主题、foreground 或 decoration layer DocumentSymbol 表达 tree-sitter tags query 发现的语法声明 类型解析、跨文件 reference、rename 或 completion SyntaxPoint.column 和 SyntaxRange.bytes 都使用 UTF-8 byte offset。Desktop adapter 必须显式在 Alpha UTF-16 position 与该 contract 之间转换；Native host 可以直接使用 byte range，但仍必须 拒绝不匹配当前 EditorHost revision 的 snapshot。 内部接口与执行路径 private symbol 精确职责 不能承担 --- --- --- LanguageConfiguration::load 把 SyntaxLanguage 绑定到 grammar、highlights query 和 tags query product language detection 或动态安装 grammar LineIndex 以增量 line starts 把 byte offset 转换为 tree-sitter row/byte-column Unicode display column 或 LSP position encoding collecttokens 运行 grammar highlight query、映射稳定 category 并应用结果上限 主题选择或编辑器 provider registration collectfoldingranges 从已解析 tree 的明确容器节点派生多行范围 UI 折叠状态 collectselectionranges 只沿 requested selection 的 named ancestor chain 收集 scopes 全树遍历、selection history 或 edit application collectsymbols 从 tags query 的 definition. 与 name capture 构造声明 workspace index lifecycle collectdiagnostics 收集 error/missing node，并应用结果上限 compiler diagnostics 或 quick fix ``text SyntaxDocument::open / openwithlimits → validatedocumentsize → LanguageConfiguration::load → Parser::parse → LineIndex::new SyntaxDocument::applyedit / applyedits → revision / range / UTF-8 boundary / size validation → LineIndex::point → Tree::edit on a cheap tree clone → String::replacerange → Parser::parse with the edited previous tree → commit tree + line index + revision → SyntaxDocument::snapshot SyntaxDocument::snapshot ├─ collecttokens ├─ collectfoldingranges ├─ collectsymbols └─ collectdiagnostics SyntaxDocument::selectionranges exact byte range → range / UTF-8 boundary validation → nameddescendantforbyterange → bounded ancestor walk（默认最多 64 层） ` 编辑校验发生在文本修改之前。解析使用已应用 edit 的旧 tree clone；如果 tree-sitter 取消解析， 实现会恢复被替换文本，权威 revision/tree 保持不变。解析成功仍可包含可恢复的语法错误，这些 错误作为 diagnostics 返回，不会导致整次操作失败。 grammar query 可能为父节点提供通用分类，并为子节点提供更具体分类，因此 highlight capture 可以重叠。range 顺序是确定的；consumer 应让较晚、较窄的 capture 覆盖较早 capture。各结果 collection 达到对应 AnalysisLimits 后停止收集，不会把仍可使用的部分 snapshot 变成错误。 即使 maxdiagnostics 把 diagnostics 截断为零，SyntaxSnapshot::haserrors 仍保留准确事实。 测试、修改影响与当前限制 `bash cargo test --manifest-path Cargo.toml -p zeta-syntax cargo clippy --manifest-path Cargo.toml -p zeta-syntax --all-targets -- -D warnings bazel test //zeta-rs/syntax:syntax-unit-tests ` src/syntaxtests.rs 覆盖 Rust token/fold/symbol projection、JSON/JSONC token/fold/comment、Shell command/operator/comment token、ECMAScript grammar、单行和多行增量编辑、revision binding、UTF-8 boundary、selection scope ordering、更新后的 line index、resource limits，以及失败不修改文档。 新增 grammar 时必须同时增加 SyntaxLanguage、LanguageConfiguration::load 的 grammar/query 绑定、capture mapping 测试和语言 fixture。修改 edit coordinate contract 时必须同步检查所有 host adapter；修改 symbol category 时必须同步检查未来 workspace index schema。若 crate 开始 读取 workspace、保存数据库、选择主题或处理 LSP request，说明 ownership 已经漂移。 当前限制： - Current：注册 JavaScript/JSX、TypeScript/TSX、Rust、Shell grammar，以及由独立私有语言模块选择的 JSON/JSONC grammar；JSON 与 JSONC 共用上游 comment-capable JSON parser，syntax snapshot 不代替 language server 的严格格式校验； - Current：单个 edit 和同一旧 revision 上的非重叠 batch 都使用 UTF-8 byte range；宿主负责事件 coalescing； - Current：文本保存在 String，中间插入需要移动后续 bytes；超大文档的 rope/chunked input 尚未实现； - Current：snapshot 派生为同步调用；异步 worker、debounce 与 cancellation 由产品宿主负责； - Current：App Server 使用 connection/model-owned open/change/close session 将 Rust、JSON 与 JSONC 的完整 analysis snapshot 投影为 UTF-16 wire DTO；Aster 消费 token、parse diagnostic、symbol 与 folding； - Current：App Server 的 syntax/selectionRanges 只为离散命令按当前 selection 返回有限 scopes；普通 syntax/analyze 不携带全树 selection nodes，Aster Smart Select 负责 revision/cancellation/stale gate； - Current：Native zeta-editor 已私有组合 incremental analysis 与 folding；Aster folding/document-symbol provider 已接入，具体 outline presentation 仍由产品 UI 决定； - Current：zeta-symbol-index 已作为独立 consumer 持久化 verified declaration facts；磁盘/dirty arbitration 由 zeta-code-index` overlay 拥有，不进入本 crate； - Potential：继续增加按需 structural operations；mutation 仍由 Editor 生成 undo transaction 并验证 revision，本 crate 不拥有 editor history。"
  },
  {
    "slug": "crates/terminal",
    "sourcePath": "zeta-rs/terminal/README.md",
    "title": "zeta-terminal",
    "description": "zeta-terminal 把 PTY 输出字节解析为可绘制的终端 grid，并把用户提交的命令及其 printable output 组织为有界 BlockList。它不启动进程、不拥有窗口、GPU、输入组件、shell profile、Session 持久化或 Agent 状态。",
    "group": "Crate 实现参考",
    "markdown": "> Native 终端产品结构、用户语义和分阶段演进由\n> [`zeterm/docs/native-terminal-ui.md`](../../zeterm/docs/native-terminal-ui.md) 统一说明。本 README 只拥有\n> terminal model 的当前实现契约、内部接口和修改路径；PTY process plumbing 见\n> [`zeta-utils-pty`](../utils/pty/README.md)。\n\n`zeta-terminal` 把 PTY 输出字节解析为可绘制的终端 grid，并把用户提交的命令及其 printable\noutput 组织为有界 BlockList。它不启动进程、不拥有窗口、GPU、输入组件、shell profile、Session\n持久化或 Agent 状态。\n\n## 所有权\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| 跨 chunk ANSI/VT parser state | `emulator::TerminalCore` / `vte::Parser` | ✅ |\n| rows × columns、cursor、wrap、erase 和 scroll | `grid::TerminalGrid` | ✅ |\n| cell text、extended grapheme、wide-character continuation 和 SGR style | `grid::Cell` / `CellStyle` | ✅ |\n| command/output block 顺序、状态和 retention cap | `block_list::BlockList` | ✅ |\n| primary/alternate screen 与 DEC input/pointer mode state | `screen::TerminalScreen` | ✅ |\n| scroll region、origin mode 与 line insert/delete | `grid::scrolling` | ✅ |\n| mode-aware keyboard 与 bracketed-paste byte encoding | `input` + `TerminalCore` | ✅ |\n| 1000/1002/1003 tracking 与 legacy/1006 mouse encoding | `mouse` + `TerminalCore` | ✅ |\n| device attributes、status 与 cursor-position reply | `emulator::GridPerformer` / `TerminalCore` | ✅ |\n| 主屏 cell scrollback、视口切片与 10,000 行 retention cap | `grid::TerminalGrid` | ✅ |\n| 主屏 wrapped-line resize reflow 与 cursor remap | `grid::reflow` | ✅ |\n| OSC 0/2 会话标题 | `emulator::GridPerformer` / `TerminalCore::title` | ✅ |\n| PTY spawn、write、resize、exit 与 cleanup | `zeta-utils-pty` + product host | ❌ |\n| shell command boundary、PTY echo filtering 与 OSC 133 `D` completion | host submit + `block_list::PendingEcho` + `emulator::GridPerformer` | ✅ |\n| 完整 DEC private modes 与更多 query family | 尚无完整 owner | 尚未完成 |\n| window pointer event → terminal cell mapping / PTY write | product host | integration obligation |\n| 跨进程重启的 Block/scrollback 持久化 | 尚无 owner | 尚未完成 |\n\n依赖方向：\n\n```text\nzeterm/zeterm\n├─ zeta-terminal → vte\n└─ zeta-utils-pty\n```\n\n`zeta-terminal` 不得反向依赖 `zeterm/zeterm`、`zeta-ui`、`zeta-wgpu` 或 PTY backend。Renderer\n从 public projection 读取 state；不能把 UI `Color`、`Rect` 或 process handles 放进 model。\n\n## 公共契约\n\n| Symbol | 调用者看到什么 | 不变量 |\n| --- | --- | --- |\n| `GridSize` | 非零 rows/columns | `new` 把零规范化为一 |\n| `TerminalCore::process_output` | 增量消费任意 byte chunk | parser state 跨 chunk 保留 |\n| `TerminalCore::resize` | 调整 grid cell dimensions | cursor 和保存位置始终 clamp 在 grid 内 |\n| `TerminalCore::active_screen` | 当前 primary/alternate projection | Renderer 只读取 active grid |\n| `TerminalCore::title` | OSC 0/2 最近设置的有界标题 | control characters 被移除，最多 256 个字符 |\n| `TerminalCore::modes` | cursor key、cursor visibility、paste 与 mouse request state | mode state 来自 PTY output，不由 UI 推断 |\n| `TerminalCore::encode_key` | 把 logical key 与 modifier 编码为 PTY input | application cursor mode 改变 cursor-key sequence |\n| `TerminalCore::encode_paste` | 编码 literal 或 bracketed paste | 只在 `?2004h` 后添加 paste delimiters |\n| `TerminalCore::encode_mouse` | 根据 tracking/encoding mode 过滤并编码一个 cell-addressed mouse event | disabled/不匹配的 motion 返回空 bytes |\n| `TerminalCore::take_reply_bytes` | 取出 parser 为 terminal query 生成的有序 bytes | 取出后清空；host 必须把非空结果写回同一 PTY |\n| `TerminalCore::start_command` | 建立新的 running Block | 前一个 running Block 转为 completed |\n| `BlockList::complete_command` | 完成当前 running Block | 先无损 flush 尚未判定的 echo，再转为 completed |\n| `TerminalCore::mark_process_exited` | 记录 process exit | 回到 primary screen、重置 modes，active Block 转为 `Exited(code)` |\n| `TerminalGrid::lines` | 当前 viewport rows | 每行 cell 数等于 `GridSize::cols` |\n| `TerminalGrid::scrollback_lines` | 被主屏全屏滚动顶出的有界 cell rows | alternate screen 和局部 scroll region 不进入历史 |\n| `TerminalGrid::viewport_lines` | 以行偏移读取一屏 live/history projection | 偏移自动 clamp；零偏移始终返回 live grid |\n| `TerminalLine::is_wrapped` | 当前 physical row 是否延续到下一行 | reflow 只连接 soft-wrapped rows，不连接 hard line break |\n| `BlockList::preamble` | 首次 command 前的 printable output | retention cap 为 256 KiB |\n| `BlockList::blocks` | ordered command/output history | 每个 Block output cap 为 1 MiB |\n\n`TerminalCore::process_output` 同时推进 active screen grid 和 printable Block output。后者移除\nANSI control sequence，只保留 primary screen 上的 printable characters、tab 和 line feed；\nalternate-screen frame 不进入 Block retention。carriage-return progress display 的权威状态仍在\ngrid，不应从 Block text 反推。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 职责 | 修改影响 |\n| --- | --- | --- | --- |\n| `emulator::GridPerformer` | private | 把 `vte::Perform` callback 投影为 grid operation 与 printable text | 同步检查 parser chunk、CSI/ESC 和 Block tests |\n| `screen::TerminalScreen` | crate-private | 拥有 primary/alternate grid、active projection 与 DEC mode transition | 同步检查 `47/1047/1048/1049`、resize、exit 和 renderer projection |\n| `screen::TerminalModes` | public read-only projection | 暴露 input/pointer 所需的 mode state | 新 mode 必须区分“已解析”与“已执行” |\n| `input::encode_key` | crate-private | logical key + mode → PTY bytes | 同步检查 normal/application cursor 与 modifiers |\n| `mouse::encode_mouse` | crate-private | tracking filter + legacy/SGR protocol encoding | 坐标输入为零基 cell；wire coordinate 为一基 |\n| `grid::scrolling` | private child module | scroll margins、origin-relative addressing、line insert/delete 与主屏历史 retention | 同步检查 full-region/partial-region、alternate screen 和 resize |\n| `grid::reflow` | private child module | 把 soft-wrapped logical line 重排为新列宽，并映射 history/cursor/pending-wrap | 同步检查宽窄 resize、wide cell、alternate screen 与 style |\n| `grid::TerminalGrid::print` | crate-private | extended grapheme、Unicode width、wrap 和 continuation cell 写入 | 同步检查 CJK/Emoji/组合符、wide-character 与 overwrite |\n| `TerminalGrid::{index,reverse_index}` | crate-private | cursor movement 与 viewport scroll | 同步检查 newline、bottom-row 和 resize |\n| `TerminalGrid::set_graphics_rendition` | crate-private | SGR → `CellStyle` | 同步检查 indexed/truecolor projection |\n| `block_list::truncate_front` | private | 在 UTF-8 boundary 前删旧 output | 同步检查 retention 与 truncation marker |\n| `block_list::PendingEcho` | private | 跨 output chunk 精确过滤 host 已建立 Block 的 PTY command echo | 不匹配或退出时必须无损回放 buffered output |\n\n真实调用关系：\n\n```text\nTerminalCore::process_output(bytes)\n├─ vte::Parser::advance\n│  └─ GridPerformer\n│     ├─ print / execute\n│     ├─ csi_dispatch\n│     │  ├─ TerminalScreen mode/buffer transition\n│     │  ├─ TerminalGrid mutation\n│     │  └─ terminal query → ordered reply bytes\n│     └─ esc_dispatch → TerminalGrid/TerminalScreen mutation\n└─ BlockList::append_printable_output\n\nhost output event\n└─ TerminalCore::take_reply_bytes\n   └─ host writes non-empty reply to the same PTY\n\nhost key event\n└─ TerminalCore::encode_key\n   └─ input::encode_key → PTY bytes\n\nhost pointer event\n└─ viewport point → TerminalMousePosition\n   └─ TerminalCore::encode_mouse → optional PTY bytes\n\nhost primary-screen wheel\n└─ presentation-owned line offset\n   └─ TerminalGrid::viewport_lines → retained/live cell projection\n\nhost submit\n└─ TerminalCore::start_command\n   └─ BlockList::start_command → PendingEcho\n\nshell OSC 133 D\n└─ GridPerformer command-finished signal\n   └─ BlockList::complete_command\n\nhost resize\n└─ TerminalCore::resize\n   └─ grid::reflow → retained rows + live rows + cursor remap\n```\n\n绕过 `TerminalCore::process_output` 直接修改 grid、从渲染文本重建 terminal state、让 BlockList\n拥有 PTY handle，或让 UI layout 决定 parser semantics，都意味着 ownership 已经漂移。\n\n## 测试与限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-terminal\nbazel test //zeta-rs/terminal:terminal-unit-tests\n```\n\n当前测试覆盖：\n\n- printable text、wrap、scroll 与 cursor position；\n- CSI cursor addressing、erase line 与 SGR；\n- ANSI sequence 跨 output chunk；\n- 简中、日文、韩文的 cell width，以及组合音标、ZWJ Emoji 和 regional-indicator flag 的\n  extended-grapheme cell ownership；\n- wide-character continuation cell；\n- primary/alternate screen entry、exit、cursor restore 与 process-exit fallback；\n- DEC application cursor、cursor visibility、bracketed paste 与 mouse request state；\n- scrolling margins、origin-relative cursor addressing 与 line insert/delete；\n- device attributes、status、standard/private cursor-position replies 与 drain semantics；\n- cursor/function/control/Alt key 与 bracketed-paste encoding；\n- 1000/1002/1003 motion filtering、legacy/SGR press/release/wheel encoding 与 coordinate clamp；\n- 主屏 full-region scrollback、历史视口、局部 scroll region 排除与 `CSI 3 J` 清理；\n- 主屏 soft-wrap reflow、宽字符边界、cursor/pending-wrap 映射与 alternate-screen fixed resize；\n- OSC 0/2 title、长度/control-character filtering；\n- PTY command echo 的跨 chunk 去重与 mismatch 无损回放；\n- OSC 133 `D` 的 Block completion；\n- alternate-screen output 不污染 Block retention；\n- preamble、Block transition、exit status 和 printable output projection。\n\n当前 parser 是可运行的最小核心，不是完整 xterm 兼容层。备用屏幕和 mode-aware key encoding\n已经具备，滚动区域、常见 terminal query、主屏 reflow 和 OSC title 也形成了闭环；尚未实现完整\nDEC mode/query family、1005/1015 等其他鼠标编码、OSC 52 clipboard、OSC 133 `A/B/C`、\ncwd/exit-status retention、prompt boundary discovery、跨进程重启的历史持久化和 cell 级无障碍\n支持。当前回滚缓冲区只在活动\n`TerminalCore` 生命周期内保留最多 10,000 个 cell rows；BlockList 继续独立保留命令级 printable\noutput，不能用其中一方重建另一方。`TerminalCore::encode_mouse` 不拥有窗口坐标、pressed button\nlifecycle 或 PTY handle；host 必须只为真实 terminal viewport 构造事件并写回非空结果。扩展时\n优先按 escape-sequence family 增加独立测试，不在 renderer 中补偿 model 缺失。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "测试与限制",
        "title": "测试与限制"
      }
    ],
    "searchText": " Native 终端产品结构、用户语义和分阶段演进由 zeterm/docs/native-terminal-ui.md 统一说明。本 README 只拥有 terminal model 的当前实现契约、内部接口和修改路径；PTY process plumbing 见 zeta-utils-pty。 zeta-terminal 把 PTY 输出字节解析为可绘制的终端 grid，并把用户提交的命令及其 printable output 组织为有界 BlockList。它不启动进程、不拥有窗口、GPU、输入组件、shell profile、Session 持久化或 Agent 状态。 所有权 能力 当前 owner 状态 --- --- --- 跨 chunk ANSI/VT parser state emulator::TerminalCore / vte::Parser ✅ rows × columns、cursor、wrap、erase 和 scroll grid::TerminalGrid ✅ cell text、extended grapheme、wide-character continuation 和 SGR style grid::Cell / CellStyle ✅ command/output block 顺序、状态和 retention cap blocklist::BlockList ✅ primary/alternate screen 与 DEC input/pointer mode state screen::TerminalScreen ✅ scroll region、origin mode 与 line insert/delete grid::scrolling ✅ mode-aware keyboard 与 bracketed-paste byte encoding input + TerminalCore ✅ 1000/1002/1003 tracking 与 legacy/1006 mouse encoding mouse + TerminalCore ✅ device attributes、status 与 cursor-position reply emulator::GridPerformer / TerminalCore ✅ 主屏 cell scrollback、视口切片与 10,000 行 retention cap grid::TerminalGrid ✅ 主屏 wrapped-line resize reflow 与 cursor remap grid::reflow ✅ OSC 0/2 会话标题 emulator::GridPerformer / TerminalCore::title ✅ PTY spawn、write、resize、exit 与 cleanup zeta-utils-pty + product host ❌ shell command boundary、PTY echo filtering 与 OSC 133 D completion host submit + blocklist::PendingEcho + emulator::GridPerformer ✅ 完整 DEC private modes 与更多 query family 尚无完整 owner 尚未完成 window pointer event → terminal cell mapping / PTY write product host integration obligation 跨进程重启的 Block/scrollback 持久化 尚无 owner 尚未完成 依赖方向： ``text zeterm/zeterm ├─ zeta-terminal → vte └─ zeta-utils-pty ` zeta-terminal 不得反向依赖 zeterm/zeterm、zeta-ui、zeta-wgpu 或 PTY backend。Renderer 从 public projection 读取 state；不能把 UI Color、Rect 或 process handles 放进 model。 公共契约 Symbol 调用者看到什么 不变量 --- --- --- GridSize 非零 rows/columns new 把零规范化为一 TerminalCore::processoutput 增量消费任意 byte chunk parser state 跨 chunk 保留 TerminalCore::resize 调整 grid cell dimensions cursor 和保存位置始终 clamp 在 grid 内 TerminalCore::activescreen 当前 primary/alternate projection Renderer 只读取 active grid TerminalCore::title OSC 0/2 最近设置的有界标题 control characters 被移除，最多 256 个字符 TerminalCore::modes cursor key、cursor visibility、paste 与 mouse request state mode state 来自 PTY output，不由 UI 推断 TerminalCore::encodekey 把 logical key 与 modifier 编码为 PTY input application cursor mode 改变 cursor-key sequence TerminalCore::encodepaste 编码 literal 或 bracketed paste 只在 ?2004h 后添加 paste delimiters TerminalCore::encodemouse 根据 tracking/encoding mode 过滤并编码一个 cell-addressed mouse event disabled/不匹配的 motion 返回空 bytes TerminalCore::takereplybytes 取出 parser 为 terminal query 生成的有序 bytes 取出后清空；host 必须把非空结果写回同一 PTY TerminalCore::startcommand 建立新的 running Block 前一个 running Block 转为 completed BlockList::completecommand 完成当前 running Block 先无损 flush 尚未判定的 echo，再转为 completed TerminalCore::markprocessexited 记录 process exit 回到 primary screen、重置 modes，active Block 转为 Exited code TerminalGrid::lines 当前 viewport rows 每行 cell 数等于 GridSize::cols TerminalGrid::scrollbacklines 被主屏全屏滚动顶出的有界 cell rows alternate screen 和局部 scroll region 不进入历史 TerminalGrid::viewportlines 以行偏移读取一屏 live/history projection 偏移自动 clamp；零偏移始终返回 live grid TerminalLine::iswrapped 当前 physical row 是否延续到下一行 reflow 只连接 soft-wrapped rows，不连接 hard line break BlockList::preamble 首次 command 前的 printable output retention cap 为 256 KiB BlockList::blocks ordered command/output history 每个 Block output cap 为 1 MiB TerminalCore::processoutput 同时推进 active screen grid 和 printable Block output。后者移除 ANSI control sequence，只保留 primary screen 上的 printable characters、tab 和 line feed； alternate-screen frame 不进入 Block retention。carriage-return progress display 的权威状态仍在 grid，不应从 Block text 反推。 内部接口地图 Symbol 可见性 职责 修改影响 --- --- --- --- emulator::GridPerformer private 把 vte::Perform callback 投影为 grid operation 与 printable text 同步检查 parser chunk、CSI/ESC 和 Block tests screen::TerminalScreen crate-private 拥有 primary/alternate grid、active projection 与 DEC mode transition 同步检查 47/1047/1048/1049、resize、exit 和 renderer projection screen::TerminalModes public read-only projection 暴露 input/pointer 所需的 mode state 新 mode 必须区分“已解析”与“已执行” input::encodekey crate-private logical key + mode → PTY bytes 同步检查 normal/application cursor 与 modifiers mouse::encodemouse crate-private tracking filter + legacy/SGR protocol encoding 坐标输入为零基 cell；wire coordinate 为一基 grid::scrolling private child module scroll margins、origin-relative addressing、line insert/delete 与主屏历史 retention 同步检查 full-region/partial-region、alternate screen 和 resize grid::reflow private child module 把 soft-wrapped logical line 重排为新列宽，并映射 history/cursor/pending-wrap 同步检查宽窄 resize、wide cell、alternate screen 与 style grid::TerminalGrid::print crate-private extended grapheme、Unicode width、wrap 和 continuation cell 写入 同步检查 CJK/Emoji/组合符、wide-character 与 overwrite TerminalGrid:: index,reverseindex crate-private cursor movement 与 viewport scroll 同步检查 newline、bottom-row 和 resize TerminalGrid::setgraphicsrendition crate-private SGR → CellStyle 同步检查 indexed/truecolor projection blocklist::truncatefront private 在 UTF-8 boundary 前删旧 output 同步检查 retention 与 truncation marker blocklist::PendingEcho private 跨 output chunk 精确过滤 host 已建立 Block 的 PTY command echo 不匹配或退出时必须无损回放 buffered output 真实调用关系： `text TerminalCore::processoutput bytes ├─ vte::Parser::advance │ └─ GridPerformer │ ├─ print / execute │ ├─ csidispatch │ │ ├─ TerminalScreen mode/buffer transition │ │ ├─ TerminalGrid mutation │ │ └─ terminal query → ordered reply bytes │ └─ escdispatch → TerminalGrid/TerminalScreen mutation └─ BlockList::appendprintableoutput host output event └─ TerminalCore::takereplybytes └─ host writes non-empty reply to the same PTY host key event └─ TerminalCore::encodekey └─ input::encodekey → PTY bytes host pointer event └─ viewport point → TerminalMousePosition └─ TerminalCore::encodemouse → optional PTY bytes host primary-screen wheel └─ presentation-owned line offset └─ TerminalGrid::viewportlines → retained/live cell projection host submit └─ TerminalCore::startcommand └─ BlockList::startcommand → PendingEcho shell OSC 133 D └─ GridPerformer command-finished signal └─ BlockList::completecommand host resize └─ TerminalCore::resize └─ grid::reflow → retained rows + live rows + cursor remap ` 绕过 TerminalCore::processoutput 直接修改 grid、从渲染文本重建 terminal state、让 BlockList 拥有 PTY handle，或让 UI layout 决定 parser semantics，都意味着 ownership 已经漂移。 测试与限制 `bash cargo test --manifest-path Cargo.toml -p zeta-terminal bazel test //zeta-rs/terminal:terminal-unit-tests ` 当前测试覆盖： - printable text、wrap、scroll 与 cursor position； - CSI cursor addressing、erase line 与 SGR； - ANSI sequence 跨 output chunk； - 简中、日文、韩文的 cell width，以及组合音标、ZWJ Emoji 和 regional-indicator flag 的 extended-grapheme cell ownership； - wide-character continuation cell； - primary/alternate screen entry、exit、cursor restore 与 process-exit fallback； - DEC application cursor、cursor visibility、bracketed paste 与 mouse request state； - scrolling margins、origin-relative cursor addressing 与 line insert/delete； - device attributes、status、standard/private cursor-position replies 与 drain semantics； - cursor/function/control/Alt key 与 bracketed-paste encoding； - 1000/1002/1003 motion filtering、legacy/SGR press/release/wheel encoding 与 coordinate clamp； - 主屏 full-region scrollback、历史视口、局部 scroll region 排除与 CSI 3 J 清理； - 主屏 soft-wrap reflow、宽字符边界、cursor/pending-wrap 映射与 alternate-screen fixed resize； - OSC 0/2 title、长度/control-character filtering； - PTY command echo 的跨 chunk 去重与 mismatch 无损回放； - OSC 133 D 的 Block completion； - alternate-screen output 不污染 Block retention； - preamble、Block transition、exit status 和 printable output projection。 当前 parser 是可运行的最小核心，不是完整 xterm 兼容层。备用屏幕和 mode-aware key encoding 已经具备，滚动区域、常见 terminal query、主屏 reflow 和 OSC title 也形成了闭环；尚未实现完整 DEC mode/query family、1005/1015 等其他鼠标编码、OSC 52 clipboard、OSC 133 A/B/C、 cwd/exit-status retention、prompt boundary discovery、跨进程重启的历史持久化和 cell 级无障碍 支持。当前回滚缓冲区只在活动 TerminalCore 生命周期内保留最多 10,000 个 cell rows；BlockList 继续独立保留命令级 printable output，不能用其中一方重建另一方。TerminalCore::encodemouse` 不拥有窗口坐标、pressed button lifecycle 或 PTY handle；host 必须只为真实 terminal viewport 构造事件并写回非空结果。扩展时 优先按 escape-sequence family 增加独立测试，不在 renderer 中补偿 model 缺失。"
  },
  {
    "slug": "crates/terminal-detection",
    "sourcePath": "zeta-rs/terminal-detection/README.md",
    "title": "zeta-terminal-detection",
    "description": "This crate identifies the terminal program, active multiplexer and color fidelity from process environment. It also resolves an OSC-reported background or COLORFGBG fallback into a light/…",
    "group": "Crate 实现参考",
    "markdown": "This crate identifies the terminal program, active multiplexer and color fidelity from process\nenvironment. It also resolves an OSC-reported background or `COLORFGBG` fallback into a light/dark\nappearance with an explicit evidence source. It does not read terminal input, send OSC/CSI queries,\nemulate a child terminal, manage a PTY, or choose a Zeta theme.\n\n| Symbol | Responsibility |\n| --- | --- |\n| `detect_host_terminal` | Cached process-wide terminal, multiplexer and color-level detection |\n| `HostTerminal` | Structured program, version, TERM and multiplexer metadata |\n| `TerminalKind` | Stable known-terminal category used by product adapters |\n| `ColorLevel` | TrueColor, ANSI-256, ANSI-16 or monochrome fidelity |\n| `resolve_background` | OSC 11 RGB → `COLORFGBG` → conservative Dark resolution |\n\nThe TUI owns exclusive terminal-response probe windows because those reads must be coordinated with\nits crossterm event stream. `zeta-terminal` separately owns child-terminal emulation, while\n`zeta-utils-pty` owns process and PTY plumbing.\n\n```text\nzeta-tui\n├─ zeta-terminal-detection  # environment identity\n├─ terminal_probe           # OSC query while TUI exclusively owns stdin\n└─ zeta-theme               # scheme resolution and token snapshots\n```\n\nDetection favors `TERM_PROGRAM`, then terminal-specific variables, then `TERM`; multiplexer identity\nis retained independently. It never starts helper processes, so detection is deterministic over the\ncurrent process environment.\n\n```bash\ncargo test -p zeta-terminal-detection\nbazel test //zeta-rs/terminal-detection:terminal-detection-unit-tests\n```",
    "headings": [],
    "searchText": "This crate identifies the terminal program, active multiplexer and color fidelity from process environment. It also resolves an OSC-reported background or COLORFGBG fallback into a light/dark appearance with an explicit evidence source. It does not read terminal input, send OSC/CSI queries, emulate a child terminal, manage a PTY, or choose a Zeta theme. Symbol Responsibility --- --- detecthostterminal Cached process-wide terminal, multiplexer and color-level detection HostTerminal Structured program, version, TERM and multiplexer metadata TerminalKind Stable known-terminal category used by product adapters ColorLevel TrueColor, ANSI-256, ANSI-16 or monochrome fidelity resolvebackground OSC 11 RGB → COLORFGBG → conservative Dark resolution The TUI owns exclusive terminal-response probe windows because those reads must be coordinated with its crossterm event stream. zeta-terminal separately owns child-terminal emulation, while zeta-utils-pty owns process and PTY plumbing. ``text zeta-tui ├─ zeta-terminal-detection environment identity ├─ terminalprobe OSC query while TUI exclusively owns stdin └─ zeta-theme scheme resolution and token snapshots ` Detection favors TERMPROGRAM, then terminal-specific variables, then TERM; multiplexer identity is retained independently. It never starts helper processes, so detection is deterministic over the current process environment. `bash cargo test -p zeta-terminal-detection bazel test //zeta-rs/terminal-detection:terminal-detection-unit-tests ``"
  },
  {
    "slug": "crates/text-file",
    "sourcePath": "zeta-rs/text-file/README.md",
    "title": "zeta-text-file",
    "description": "zeta-text-file owns the editor-independent lifecycle of one UTF-8 workspace file. Cross-crate placement and product composition are documented in docs/zeta-rs-architecture.md; this README…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-text-file` owns the editor-independent lifecycle of one UTF-8 workspace file. Cross-crate\nplacement and product composition are documented in\n[`docs/zeta-rs-architecture.md`](../../docs/zeta-rs-architecture.md); this README is canonical for\nthe crate's implementation contract.\n\n## Ownership\n\n| Concern | Owner | Status |\n| --- | --- | --- |\n| Saved text baseline, disk version and read-only state | `TextFileLifecycle` | ✅ |\n| Dirty/reload/conflict classification | `TextFileLifecycle::status` | ✅ |\n| Optimistic save and explicit conflict-overwrite payloads | `TextFileLifecycle::save_request` / `overwrite_request` | ✅ |\n| Pending external snapshot reconciliation | `TextFileLifecycle::observe_external` | ✅ |\n| Mutable document, caret, selection, syntax and folding | Editor implementation | 委托 |\n| Filesystem reads, metadata validation and writes | Host filesystem adapter | 委托 |\n| Tabs, active document, close confirmation and rendering | Product host | 委托 |\n\nThe crate has no dependency on `zeta-editor`, Native, App Server, or a filesystem executor. Adding\none of those dependencies would be architectural drift.\n\n## Public contract and execution path\n\n`TextFileSnapshot` binds workspace-relative path, UTF-8 content and `TextFileDiskVersion` captured\nby a host read. `TextFileLifecycle::new` retains that content only as the saved baseline; the host\nkeeps authoritative mutable editor text and supplies it to lifecycle operations.\n\n```text\nfilesystem adapter → TextFileSnapshot\n                   → host creates editor document + TextFileLifecycle\neditor text        → status / save_request\nexternal read      → observe_external → synchronized or pending reload\nuser keeps editor  → overwrite_request using the observed external version\nsuccessful write   → mark_saved\nhost-approved load → take_pending_external\n```\n\n`TextFileSaveRequest` contains the expected disk version. Ordinary saves use the last saved version;\n`overwrite_request` is only available for dirty text with a pending external snapshot and uses that\nobserved external version. The adapter must compare the expected version with current\nmetadata before writing and report a mismatch. This is only a preflight unless the backend offers an\natomic compare-and-swap write, so adapters must not present it as a stronger guarantee. The crate\nperforms no I/O and therefore has no transport errors. A snapshot for another path returns\n`TextFileObserveResult::PathMismatch` without changing retained state.\n\n## Integration obligations\n\n- Construct a snapshot from content and metadata belonging to the same logical read.\n- Keep paths in the workspace-relative identity form selected by the host; the crate compares them\n  lexically and does not canonicalize or authorize them.\n- Do not mutate the editor document when merely observing an external snapshot. Reload is an explicit\n  host decision using `take_pending_external`.\n- Call `overwrite_request` only after an explicit user decision to keep editor text. It remains an\n  optimistic write and must fail if the file changed again.\n- Call `mark_saved` only after a successful write and pass the metadata returned for that write.\n\n## Tests and modification impact\n\nRun `cargo test -p zeta-text-file`. `lifecycle_tests.rs` covers dirty/save state, explicit optimistic\noverwrite, read-only behavior, reload/conflict classification, baseline synchronization and path\nmismatch rejection.\n\nChanges to disk-version equality require matching adapter preflight tests. Changes to status rules\nrequire host presentation and close-safety tests. Adding binary data, encoding detection, merge logic,\nautosave or durable recovery is future work and must not be documented as current behavior.",
    "headings": [
      {
        "depth": 2,
        "id": "ownership",
        "title": "Ownership"
      },
      {
        "depth": 2,
        "id": "public-contract-and-execution-path",
        "title": "Public contract and execution path"
      },
      {
        "depth": 2,
        "id": "integration-obligations",
        "title": "Integration obligations"
      },
      {
        "depth": 2,
        "id": "tests-and-modification-impact",
        "title": "Tests and modification impact"
      }
    ],
    "searchText": "zeta-text-file owns the editor-independent lifecycle of one UTF-8 workspace file. Cross-crate placement and product composition are documented in docs/zeta-rs-architecture.md; this README is canonical for the crate's implementation contract. Ownership Concern Owner Status --- --- --- Saved text baseline, disk version and read-only state TextFileLifecycle ✅ Dirty/reload/conflict classification TextFileLifecycle::status ✅ Optimistic save and explicit conflict-overwrite payloads TextFileLifecycle::saverequest / overwriterequest ✅ Pending external snapshot reconciliation TextFileLifecycle::observeexternal ✅ Mutable document, caret, selection, syntax and folding Editor implementation 委托 Filesystem reads, metadata validation and writes Host filesystem adapter 委托 Tabs, active document, close confirmation and rendering Product host 委托 The crate has no dependency on zeta-editor, Native, App Server, or a filesystem executor. Adding one of those dependencies would be architectural drift. Public contract and execution path TextFileSnapshot binds workspace-relative path, UTF-8 content and TextFileDiskVersion captured by a host read. TextFileLifecycle::new retains that content only as the saved baseline; the host keeps authoritative mutable editor text and supplies it to lifecycle operations. ``text filesystem adapter → TextFileSnapshot → host creates editor document + TextFileLifecycle editor text → status / saverequest external read → observeexternal → synchronized or pending reload user keeps editor → overwriterequest using the observed external version successful write → marksaved host-approved load → takependingexternal ` TextFileSaveRequest contains the expected disk version. Ordinary saves use the last saved version; overwriterequest is only available for dirty text with a pending external snapshot and uses that observed external version. The adapter must compare the expected version with current metadata before writing and report a mismatch. This is only a preflight unless the backend offers an atomic compare-and-swap write, so adapters must not present it as a stronger guarantee. The crate performs no I/O and therefore has no transport errors. A snapshot for another path returns TextFileObserveResult::PathMismatch without changing retained state. Integration obligations - Construct a snapshot from content and metadata belonging to the same logical read. - Keep paths in the workspace-relative identity form selected by the host; the crate compares them lexically and does not canonicalize or authorize them. - Do not mutate the editor document when merely observing an external snapshot. Reload is an explicit host decision using takependingexternal. - Call overwriterequest only after an explicit user decision to keep editor text. It remains an optimistic write and must fail if the file changed again. - Call marksaved only after a successful write and pass the metadata returned for that write. Tests and modification impact Run cargo test -p zeta-text-file. lifecycletests.rs` covers dirty/save state, explicit optimistic overwrite, read-only behavior, reload/conflict classification, baseline synchronization and path mismatch rejection. Changes to disk-version equality require matching adapter preflight tests. Changes to status rules require host presentation and close-safety tests. Adding binary data, encoding detection, merge logic, autosave or durable recovery is future work and must not be documented as current behavior."
  },
  {
    "slug": "crates/theme",
    "sourcePath": "zeta-rs/theme/README.md",
    "title": "zeta-theme",
    "description": "zeta-theme 是 Desktop、Native 与 TUI 共享主题 contract 的 Rust runtime。token catalog、JSON Schema 和模板由 Desktop registry 编译到 resources/design-tokens； 本 crate 嵌入同一 manifest，保留 alias/default graph，严格…",
    "group": "Crate 实现参考",
    "markdown": "`zeta-theme` 是 Desktop、Native 与 TUI 共享主题 contract 的 Rust runtime。token catalog、JSON\nSchema 和模板由 Desktop registry 编译到 [`resources/design-tokens`](../../resources/design-tokens)；\n本 crate 嵌入同一 manifest，保留 alias/default graph，严格解析用户主题，并产生平台中立、不可变的\n`ThemeSnapshot`。\n\n| Symbol | 职责 |\n| --- | --- |\n| `ThemeCatalog` | 读取版本化共享 manifest，解析默认值、alias、覆盖和颜色变换 |\n| `ThemeDocument` | 严格解析最多 1 MiB、512 个覆盖项的用户主题 JSON |\n| `ThemeSnapshot` | 保存完整 resolved RGBA token table；不包含 DOM、WGPU 或 Ratatui 类型 |\n| `ThemeLoader` | 有界读取主题入口、device configuration 与 `themes/*.json`，隔离单文件错误并选择主题 |\n| `ThemeLoader::choices` / `preview` / `select` | 枚举有效 built-in/user 主题；无副作用解析 preview；验证后原子保存 surface preference |\n| `ThemeLoadOptions::with_default_entry` | 由产品启动组合选择 `zeta`、`zeta-code` 或 `zeterm` 默认入口；组件和 token 不感知产品 |\n| `ThemeSurface` | 选择 graphical 或 terminal device preference；不把 UI preference 放进 `zeta-config` |\n\nDesktop 使用自己的 TypeScript resolver 和 CSS projection；Rust 与 TypeScript 通过同一 manifest、\nSchema 和 parity fixture 保持一致。Native/TUI adapter 只能把 snapshot 投影为自己的 component\nstyle，不能在本 crate 注册宿主状态或布局规则。TUI 可以只读取 token 子集并执行终端色彩能力降级。\n\n## 执行路径与内部所有者\n\n```text\nThemeLoader::embedded\n└─ ThemeCatalog::embedded\n   ├─ include_str!(resources/design-tokens/design-tokens.json)\n   └─ include_str!(resources/design-tokens/theme-entries.json)\n\nThemeLoader::load(options)\n├─ preference::read_preference(configuration.json)\n├─ system preference → ThemeCatalog::built_in_entry(default_entry)\n├─ read_theme_documents(themes/*.json)\n│  └─ ThemeDocument::parse → validate schema/version/id/label/value bounds\n├─ ThemeCatalog::resolve_document\n│  ├─ normalize_legacy_editor_tokens\n│  └─ Resolver::resolve_token / resolve_value\n└─ LoadedTheme { snapshot, diagnostics }\n\nThemeLoader::select(options, preference)\n├─ ThemeLoader::preview（先验证并解析 built-in/user theme，不写配置）\n└─ preference::write_preference\n   ├─ 保留其他 device-local values\n   └─ zeta_utils_path::write_text_atomically\n```\n\n`Resolver` 是 alias/default/override graph、cycle path、transform depth、factor 与 transparency\ncontract 的唯一内部 owner；宿主不能再解析 token 引用。`read_preference` 只解释 device-local\n`workbench.colorTheme` 与 `tui.colorTheme`；`preference::{read_preference,write_preference}` 是选择值\n读取、保留未知 device values 与原子替换的内部 owner。`read_theme_documents` 只枚举非递归 regular JSON，按路径\n排序并限制为 128 个。`read_bounded_text` 在分配完整文档前将配置和主题文件限制为 1 MiB。\n\n`theme-entries.json` 只为同一 token catalog 提供数据化默认覆盖。`zeta`、`zeta-code` 与 `zeterm`\n不创建产品 token 或 resolver 分支；宿主只在 `ThemeLoadOptions` 选择入口。配置为 `system` 时，入口\n跟随系统明暗方案；配置为 `<entry>-light`、`<entry>-dark` 或用户主题 ID 时固定到对应主题。\n\n## 失败语义与接入义务\n\n- embedded manifest 版本、重复 token 或解析失败会使 `ThemeLoader::embedded` 返回错误；这是构建\n  contract 破坏，宿主应使用自己的最小安全 fallback，而不是猜 token。\n- 未知宿主默认入口产生诊断并回退 `zeta`；未知 device preference 回退宿主所选的默认入口，而\n  不是绕过入口回到全局颜色。\n- 一个用户主题文件失败只产生带路径的 `ThemeDiagnostic`，不会阻断其他主题；选中的主题不可用时\n  回退调用方提供的 system scheme。\n- `ThemeSnapshot::required_color` 只用于宿主声明为必需的 token；缺失返回 `ThemeError`，adapter\n  必须原子地保留上一份完整 palette 或 fallback，不能部分应用。\n- Native 选择 `ThemeSurface::Graphical`，TUI 选择 `ThemeSurface::Terminal`；TUI preference 缺失时\n  loader 才回退 graphical preference。\n- 新增 token 只在 Desktop registry 声明并重新生成 manifest；不得在 `tokens.rs` 加默认 RGB。\n  `tokens.rs` 只是 Rust adapter 使用的稳定 ID 常量子集。\n\n## 修改影响、测试与当前限制\n\n修改 `document.rs` 必须同步用户主题 Schema 与 Desktop validator；修改 `catalog.rs` 的颜色数学或\nlegacy 映射必须同时更新共享 `theme-conformance.json` 并让 TS/Rust 两端通过。修改入口默认值需更新\n`resources/design-tokens/theme-entries.json`；修改 device path、文件上限或 preference 优先级必须同步\nDesktop loader、本文与 `docs/design-tokens.md`。\n\n当前 snapshot 只包含颜色；尺寸仍由 Desktop 消费。Native/TUI 目前不监听外部主题文件，应用新\nJSON 需要重启对应进程；Native 会在系统明暗事件到达时重新选择 system snapshot。高对比度已保留\n独立 scheme，但默认值当前继承相应明暗方案。\n\n```text\ncargo test --manifest-path Cargo.toml -p zeta-theme\nbazel test //zeta-rs/theme:theme-unit-tests\n```",
    "headings": [
      {
        "depth": 2,
        "id": "执行路径与内部所有者",
        "title": "执行路径与内部所有者"
      },
      {
        "depth": 2,
        "id": "失败语义与接入义务",
        "title": "失败语义与接入义务"
      },
      {
        "depth": 2,
        "id": "修改影响测试与当前限制",
        "title": "修改影响、测试与当前限制"
      }
    ],
    "searchText": "zeta-theme 是 Desktop、Native 与 TUI 共享主题 contract 的 Rust runtime。token catalog、JSON Schema 和模板由 Desktop registry 编译到 resources/design-tokens； 本 crate 嵌入同一 manifest，保留 alias/default graph，严格解析用户主题，并产生平台中立、不可变的 ThemeSnapshot。 Symbol 职责 --- --- ThemeCatalog 读取版本化共享 manifest，解析默认值、alias、覆盖和颜色变换 ThemeDocument 严格解析最多 1 MiB、512 个覆盖项的用户主题 JSON ThemeSnapshot 保存完整 resolved RGBA token table；不包含 DOM、WGPU 或 Ratatui 类型 ThemeLoader 有界读取主题入口、device configuration 与 themes/.json，隔离单文件错误并选择主题 ThemeLoader::choices / preview / select 枚举有效 built-in/user 主题；无副作用解析 preview；验证后原子保存 surface preference ThemeLoadOptions::withdefaultentry 由产品启动组合选择 zeta、zeta-code 或 zeterm 默认入口；组件和 token 不感知产品 ThemeSurface 选择 graphical 或 terminal device preference；不把 UI preference 放进 zeta-config Desktop 使用自己的 TypeScript resolver 和 CSS projection；Rust 与 TypeScript 通过同一 manifest、 Schema 和 parity fixture 保持一致。Native/TUI adapter 只能把 snapshot 投影为自己的 component style，不能在本 crate 注册宿主状态或布局规则。TUI 可以只读取 token 子集并执行终端色彩能力降级。 执行路径与内部所有者 ``text ThemeLoader::embedded └─ ThemeCatalog::embedded ├─ includestr! resources/design-tokens/design-tokens.json └─ includestr! resources/design-tokens/theme-entries.json ThemeLoader::load options ├─ preference::readpreference configuration.json ├─ system preference → ThemeCatalog::builtinentry defaultentry ├─ readthemedocuments themes/.json │ └─ ThemeDocument::parse → validate schema/version/id/label/value bounds ├─ ThemeCatalog::resolvedocument │ ├─ normalizelegacyeditortokens │ └─ Resolver::resolvetoken / resolvevalue └─ LoadedTheme snapshot, diagnostics ThemeLoader::select options, preference ├─ ThemeLoader::preview（先验证并解析 built-in/user theme，不写配置） └─ preference::writepreference ├─ 保留其他 device-local values └─ zetautilspath::writetextatomically ` Resolver 是 alias/default/override graph、cycle path、transform depth、factor 与 transparency contract 的唯一内部 owner；宿主不能再解析 token 引用。readpreference 只解释 device-local workbench.colorTheme 与 tui.colorTheme；preference:: readpreference,writepreference 是选择值 读取、保留未知 device values 与原子替换的内部 owner。readthemedocuments 只枚举非递归 regular JSON，按路径 排序并限制为 128 个。readboundedtext 在分配完整文档前将配置和主题文件限制为 1 MiB。 theme-entries.json 只为同一 token catalog 提供数据化默认覆盖。zeta、zeta-code 与 zeterm 不创建产品 token 或 resolver 分支；宿主只在 ThemeLoadOptions 选择入口。配置为 system 时，入口 跟随系统明暗方案；配置为 <entry -light、<entry -dark 或用户主题 ID 时固定到对应主题。 失败语义与接入义务 - embedded manifest 版本、重复 token 或解析失败会使 ThemeLoader::embedded 返回错误；这是构建 contract 破坏，宿主应使用自己的最小安全 fallback，而不是猜 token。 - 未知宿主默认入口产生诊断并回退 zeta；未知 device preference 回退宿主所选的默认入口，而 不是绕过入口回到全局颜色。 - 一个用户主题文件失败只产生带路径的 ThemeDiagnostic，不会阻断其他主题；选中的主题不可用时 回退调用方提供的 system scheme。 - ThemeSnapshot::requiredcolor 只用于宿主声明为必需的 token；缺失返回 ThemeError，adapter 必须原子地保留上一份完整 palette 或 fallback，不能部分应用。 - Native 选择 ThemeSurface::Graphical，TUI 选择 ThemeSurface::Terminal；TUI preference 缺失时 loader 才回退 graphical preference。 - 新增 token 只在 Desktop registry 声明并重新生成 manifest；不得在 tokens.rs 加默认 RGB。 tokens.rs 只是 Rust adapter 使用的稳定 ID 常量子集。 修改影响、测试与当前限制 修改 document.rs 必须同步用户主题 Schema 与 Desktop validator；修改 catalog.rs 的颜色数学或 legacy 映射必须同时更新共享 theme-conformance.json 并让 TS/Rust 两端通过。修改入口默认值需更新 resources/design-tokens/theme-entries.json；修改 device path、文件上限或 preference 优先级必须同步 Desktop loader、本文与 docs/design-tokens.md。 当前 snapshot 只包含颜色；尺寸仍由 Desktop 消费。Native/TUI 目前不监听外部主题文件，应用新 JSON 需要重启对应进程；Native 会在系统明暗事件到达时重新选择 system snapshot。高对比度已保留 独立 scheme，但默认值当前继承相应明暗方案。 `text cargo test --manifest-path Cargo.toml -p zeta-theme bazel test //zeta-rs/theme:theme-unit-tests ``"
  },
  {
    "slug": "crates/thread-store",
    "sourcePath": "zeta-rs/thread-store/README.md",
    "title": "zeta-thread-store",
    "description": "zeta-thread-store 定义 storage-neutral authoritative Thread history port。它接收 zetahistory::StoredEvent，负责完整读取、原子追加契约与错误；record 本身的 serde shape、 receipt 和 schema version 不再由本 crate 定义。ThreadU…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 Thread durability port、完整恢复和 batch validation。Persisted record 格式由\n> [`zeta-history`](../history/README.md) 拥有；Canonical Thread lifecycle 见\n> [`docs/protocol.md`](../../docs/protocol.md)，Core execution/recovery 见\n> [`docs/core.md`](../../docs/core.md)。\n\n`zeta-thread-store` 定义 storage-neutral authoritative Thread history port。它接收\n`zeta_history::StoredEvent`，负责完整读取、原子追加契约与错误；record 本身的 serde shape、\nreceipt 和 schema version 不再由本 crate 定义。`ThreadUpdate`、token delta、actor state、RPC\npayload 和 Session membership 不进入该 stream。\n\n## 公共契约\n\n| Symbol | 职责 | 关键约束 |\n| --- | --- | --- |\n| `ThreadStore` | `list_thread_ids / load / append_batch` port | per-Thread ordered recovery 与 atomic batch |\n| `ThreadEventBatch` | exact append intent | thread ID、expected sequence、ordered events |\n| `AppendBatchResult` | committed batch 摘要 | committed sequence 与 event count |\n| `zeta_history::StoredEvent` | Store 接收和返回的 Thread history record | 类型 owner 是 `zeta-history`，Store 不复制它 |\n| `ThreadStoreError` | stable store error | invalid batch、sequence conflict、storage failure |\n\nSession membership/lineage 由 `zeta-session-store` 独立保存：\n\n```text\nSessionStore  → membership / lineage / Session lifecycle\nThreadStore   → Turn / ThreadItem / interaction / Tool lifecycle\n```\n\n两者可以共享物理 storage engine，但不能共享 logical sequence 或 envelope。\n\n## 内部接口地图与调用图\n\n| Symbol | 当前职责 | 方向约束 |\n| --- | --- | --- |\n| `validate_append_batch` | backend-independent append validation | 所有 implementation 必须在 durable write 前调用等价逻辑 |\n| `zeta_history::CURRENT_STORED_EVENT_SCHEMA_VERSION` | append validator 接受的新记录版本 | reader migration 与 new-write version 必须区分 |\n\n```text\nThreadStore::append_batch\n├─ obtain actual Thread sequence\n├─ validate_append_batch\n│  ├─ expected sequence exact match\n│  ├─ non-empty batch ID/events\n│  ├─ current schema version\n│  ├─ batch/envelope/event Thread ID agreement\n│  ├─ contiguous event sequence\n│  └─ unique event IDs within batch\n├─ atomically persist all events\n└─ return AppendBatchResult\n```\n\n方向偏差：\n\n- 持久化 `ThreadUpdate`：把 transient delivery state 错当成 durable fact；\n- command receipt 丢失 exact `ThreadCommand`：无法检测 same ID/different payload；\n- start marker 后缺少 terminal result时自动重放：Core recovery 安全边界被绕过；\n- backend 自行接受 gap sequence：单写者与 reducer ordering 被破坏；\n- `SessionId` 在 Thread 创建后可变：Thread immutable ownership 已被破坏。\n\n## 错误与后端义务\n\n`SequenceConflict` 是正常 concurrency signal，不应被转换为 generic I/O failure。\n`InvalidBatch` 表示 caller/backend contract bug；`Storage` 表示 backend failure。\n\nBackend 必须 complete-or-none commit、success-before-durability 禁止、tail 不可见，并保留每条\nevent 的 exact sequence。Reducer legality 属于 Core，物理 framing/checksum/tail recovery 属于\n`zeta-storage`。\n\n## 测试与修改路径\n\n```text\ncargo test -p zeta-thread-store\nbazel test //zeta-rs/thread-store:thread-store-unit-tests\n```\n\n新增 `ThreadEvent` 通常只修改 `zeta-protocol` 与 Core；只有 durable envelope 变化才修改本 crate。\n修改 schema/version 时同步审查 storage adapter、rollout recovery、rollout trace 和 protocol\ncontract tests。\n\n## 当前限制与演进\n\n当前实现消费 version `3` history record，并提供完整恢复与 append validator；version 3 覆盖 Agent\ncontext seed、delegation、message/result facts 和自动 Skill activation command snapshot。snapshot、Turn\nprojection、UI history page、compaction 和 event migration registry 不属于本 crate。App Server\n当前从 Core projection 生成 bounded Turn window；若未来需要 bounded recovery，必须先建立可独立\n验证的 Turn projection/index，不能把任意 event 片段误当成可独立 reducer 输入。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部接口地图与调用图",
        "title": "内部接口地图与调用图"
      },
      {
        "depth": 2,
        "id": "错误与后端义务",
        "title": "错误与后端义务"
      },
      {
        "depth": 2,
        "id": "测试与修改路径",
        "title": "测试与修改路径"
      },
      {
        "depth": 2,
        "id": "当前限制与演进",
        "title": "当前限制与演进"
      }
    ],
    "searchText": " 本 README 解释 Thread durability port、完整恢复和 batch validation。Persisted record 格式由 zeta-history 拥有；Canonical Thread lifecycle 见 docs/protocol.md，Core execution/recovery 见 docs/core.md。 zeta-thread-store 定义 storage-neutral authoritative Thread history port。它接收 zetahistory::StoredEvent，负责完整读取、原子追加契约与错误；record 本身的 serde shape、 receipt 和 schema version 不再由本 crate 定义。ThreadUpdate、token delta、actor state、RPC payload 和 Session membership 不进入该 stream。 公共契约 Symbol 职责 关键约束 --- --- --- ThreadStore listthreadids / load / appendbatch port per-Thread ordered recovery 与 atomic batch ThreadEventBatch exact append intent thread ID、expected sequence、ordered events AppendBatchResult committed batch 摘要 committed sequence 与 event count zetahistory::StoredEvent Store 接收和返回的 Thread history record 类型 owner 是 zeta-history，Store 不复制它 ThreadStoreError stable store error invalid batch、sequence conflict、storage failure Session membership/lineage 由 zeta-session-store 独立保存： ``text SessionStore → membership / lineage / Session lifecycle ThreadStore → Turn / ThreadItem / interaction / Tool lifecycle ` 两者可以共享物理 storage engine，但不能共享 logical sequence 或 envelope。 内部接口地图与调用图 Symbol 当前职责 方向约束 --- --- --- validateappendbatch backend-independent append validation 所有 implementation 必须在 durable write 前调用等价逻辑 zetahistory::CURRENTSTOREDEVENTSCHEMAVERSION append validator 接受的新记录版本 reader migration 与 new-write version 必须区分 `text ThreadStore::appendbatch ├─ obtain actual Thread sequence ├─ validateappendbatch │ ├─ expected sequence exact match │ ├─ non-empty batch ID/events │ ├─ current schema version │ ├─ batch/envelope/event Thread ID agreement │ ├─ contiguous event sequence │ └─ unique event IDs within batch ├─ atomically persist all events └─ return AppendBatchResult ` 方向偏差： - 持久化 ThreadUpdate：把 transient delivery state 错当成 durable fact； - command receipt 丢失 exact ThreadCommand：无法检测 same ID/different payload； - start marker 后缺少 terminal result时自动重放：Core recovery 安全边界被绕过； - backend 自行接受 gap sequence：单写者与 reducer ordering 被破坏； - SessionId 在 Thread 创建后可变：Thread immutable ownership 已被破坏。 错误与后端义务 SequenceConflict 是正常 concurrency signal，不应被转换为 generic I/O failure。 InvalidBatch 表示 caller/backend contract bug；Storage 表示 backend failure。 Backend 必须 complete-or-none commit、success-before-durability 禁止、tail 不可见，并保留每条 event 的 exact sequence。Reducer legality 属于 Core，物理 framing/checksum/tail recovery 属于 zeta-storage。 测试与修改路径 `text cargo test -p zeta-thread-store bazel test //zeta-rs/thread-store:thread-store-unit-tests ` 新增 ThreadEvent 通常只修改 zeta-protocol 与 Core；只有 durable envelope 变化才修改本 crate。 修改 schema/version 时同步审查 storage adapter、rollout recovery、rollout trace 和 protocol contract tests。 当前限制与演进 当前实现消费 version 3` history record，并提供完整恢复与 append validator；version 3 覆盖 Agent context seed、delegation、message/result facts 和自动 Skill activation command snapshot。snapshot、Turn projection、UI history page、compaction 和 event migration registry 不属于本 crate。App Server 当前从 Core projection 生成 bounded Turn window；若未来需要 bounded recovery，必须先建立可独立 验证的 Turn projection/index，不能把任意 event 片段误当成可独立 reducer 输入。"
  },
  {
    "slug": "crates/tool-executor",
    "sourcePath": "zeta-rs/tool-executor/README.md",
    "title": "zeta-tool-executor",
    "description": "本 crate 在 approval、WorkspaceRoot、sandbox backend、timeout、cancellation 和 output budget 都已固定后启动一个显式进程。它不解析 shell，不决定 Core policy，也不把普通 non-zero exit 自动分类为 sandbox denial。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Zeta 单进程执行边界的实现契约。Tool ownership 见\n> [`docs/tools.md`](../../docs/tools.md)，平台 enforcement 见\n> [`docs/sandboxing.md`](../../docs/sandboxing.md)。完整 Agent 的无界面运行由\n> [`zeta-exec`](../exec/README.md) 拥有。\n\n本 crate 在 approval、WorkspaceRoot、sandbox backend、timeout、cancellation 和 output budget\n都已固定后启动一个显式进程。它不解析 shell，不决定 Core policy，也不把普通 non-zero exit\n自动分类为 sandbox denial。\n\n## 公共契约\n\n| Symbol | 职责 |\n| --- | --- |\n| `ApprovalPolicy` | 对 exact program/argv digest 给出 start gate |\n| `CommandExecutionAuthority` | 显式区分 sandboxed 与 unrestricted execution |\n| `ExecutionLimits` | 固定 wall-clock timeout 与 stdout/stderr 总 byte budget |\n| `CommandExecutor::execute` | prepare、spawn、监督、capture 与 backend denial classification |\n| `CommandExecutionOutcome` | completed output 或 structured sandbox denial |\n| `ExecutionError` | start 前拒绝、spawn failure、取消、timeout 或 sandbox setup failure |\n\n真实顺序不可交换：\n\n```text\ncancellation checkpoint\n→ ApprovalPolicy\n→ cancellation checkpoint\n→ SandboxManager::prepare\n→ cancellation checkpoint\n→ spawn prepared command\n→ poll child + cancellation + timeout\n→ join stdout/stderr readers\n→ bounded merge\n→ SandboxBackend::classify_denial\n```\n\n只有 backend 返回的 `PreparedCommand` 会被启动。requested command 不会绕过 backend 直接 spawn。\n`Sandboxed` authority 若无法建立 backend boundary，必须失败关闭；调用方不能静默改成\n`Unrestricted`。\n\n## 取消、timeout 与输出\n\nspawn 前观察到 cancellation 返回 `CancelledBeforeStart`。spawn 后 cancellation 或 timeout 会\n先 kill/wait child，再 join capture threads；前者返回 `CancelledAfterStart`，后者返回\n`TimedOut`。上层必须把两者视为已经跨过 side-effect boundary，不能自动 replay。\n\nstdout 与 stderr reader 各自最多读取总 budget，最终合并时 stdout 优先、stderr 使用剩余空间。\n`CommandOutput::{stdout_truncated,stderr_truncated}` 明确指出原 stream 是否超过保留范围。budget 是\nbyte 数；截断跨 UTF-8 code point 时使用 lossy decoding，调用方不能把 retained string 当作完整\n输出。\n\n## 内部接口与漂移检查\n\n`check_cancellation_before_start` 固定 pre-spawn semantics；`terminate` 统一 kill/wait/join；\n`drain_stream` 只负责 bounded capture。若 Tool schema、Core approval、自动 retry 或 Thread mutation\n进入本 crate，说明 ownership 已漂移。\n\n```bash\ncargo test -p zeta-tool-executor\ncargo clippy -p zeta-tool-executor --all-targets --no-deps -- -D warnings\nbazel test //zeta-rs/tool-executor:tool-executor-unit-tests\n```",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "取消timeout-与输出",
        "title": "取消、timeout 与输出"
      },
      {
        "depth": 2,
        "id": "内部接口与漂移检查",
        "title": "内部接口与漂移检查"
      }
    ],
    "searchText": " 本 README 是 Zeta 单进程执行边界的实现契约。Tool ownership 见 docs/tools.md，平台 enforcement 见 docs/sandboxing.md。完整 Agent 的无界面运行由 zeta-exec 拥有。 本 crate 在 approval、WorkspaceRoot、sandbox backend、timeout、cancellation 和 output budget 都已固定后启动一个显式进程。它不解析 shell，不决定 Core policy，也不把普通 non-zero exit 自动分类为 sandbox denial。 公共契约 Symbol 职责 --- --- ApprovalPolicy 对 exact program/argv digest 给出 start gate CommandExecutionAuthority 显式区分 sandboxed 与 unrestricted execution ExecutionLimits 固定 wall-clock timeout 与 stdout/stderr 总 byte budget CommandExecutor::execute prepare、spawn、监督、capture 与 backend denial classification CommandExecutionOutcome completed output 或 structured sandbox denial ExecutionError start 前拒绝、spawn failure、取消、timeout 或 sandbox setup failure 真实顺序不可交换： ``text cancellation checkpoint → ApprovalPolicy → cancellation checkpoint → SandboxManager::prepare → cancellation checkpoint → spawn prepared command → poll child + cancellation + timeout → join stdout/stderr readers → bounded merge → SandboxBackend::classifydenial ` 只有 backend 返回的 PreparedCommand 会被启动。requested command 不会绕过 backend 直接 spawn。 Sandboxed authority 若无法建立 backend boundary，必须失败关闭；调用方不能静默改成 Unrestricted。 取消、timeout 与输出 spawn 前观察到 cancellation 返回 CancelledBeforeStart。spawn 后 cancellation 或 timeout 会 先 kill/wait child，再 join capture threads；前者返回 CancelledAfterStart，后者返回 TimedOut。上层必须把两者视为已经跨过 side-effect boundary，不能自动 replay。 stdout 与 stderr reader 各自最多读取总 budget，最终合并时 stdout 优先、stderr 使用剩余空间。 CommandOutput:: stdouttruncated,stderrtruncated 明确指出原 stream 是否超过保留范围。budget 是 byte 数；截断跨 UTF-8 code point 时使用 lossy decoding，调用方不能把 retained string 当作完整 输出。 内部接口与漂移检查 checkcancellationbeforestart 固定 pre-spawn semantics；terminate 统一 kill/wait/join； drainstream 只负责 bounded capture。若 Tool schema、Core approval、自动 retry 或 Thread mutation 进入本 crate，说明 ownership 已漂移。 `bash cargo test -p zeta-tool-executor cargo clippy -p zeta-tool-executor --all-targets --no-deps -- -D warnings bazel test //zeta-rs/tool-executor:tool-executor-unit-tests ``"
  },
  {
    "slug": "crates/utils/typst",
    "sourcePath": "zeta-rs/utils/typst/README.md",
    "title": "zeta-typst",
    "description": "zeta-typst 把调用方提供的一段 Typst 源码编译为 PDF。它拥有 Typst World 实现，明确不 提供操作系统文件系统、包注册表、网络、环境变量或时钟访问。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责内存编译器边界的实现细节。跨进程所有权、产品语义和分阶段演进以\n> [`docs/typst.md`](../../../docs/typst.md) 为准。\n\n`zeta-typst` 把调用方提供的一段 Typst 源码编译为 PDF。它拥有 Typst `World` 实现，明确不\n提供操作系统文件系统、包注册表、网络、环境变量或时钟访问。\n\n## 边界与公共契约\n\n`TypstCompiler` 缓存 Typst 标准库、内置字体和字体目录。`TypstCompiler::compile` 借用 UTF-8\n源码并返回：\n\n- `TypstCompileOutcome::Success`：PDF 字节和非致命诊断；\n- `TypstCompileOutcome::Failed`：普通源码错误或 PDF 生成诊断；\n- `TypstCompileError::SourceTooLarge`：源码超过 `MAX_TYPST_SOURCE_BYTES`，即按 UTF-8 字节\n  计算的 1 MiB。\n\n源码错误属于编译结果而不是基础设施错误，因此 UI 无需解析内部错误字符串即可展示。诊断范围\n使用半开区间，表示提交源码中的 UTF-8 字节偏移。\n\n本 crate 不拥有 RPC 数据结构、PDF 资源保留、编辑器模型、预览 UI 或文档持久化。\n\n## 内部所有权与调用路径\n\n```text\nTypstCompiler::compile\n|- 大小校验\n|- InMemoryWorld::new(source)\n|- typst::compile\n|  `- World::{source,file,font,today}\n|- map_diagnostics\n`- typst_pdf::pdf\n```\n\n关键私有符号：\n\n- `InMemoryWorld` 把 `/main.typ` 绑定到提交的源码。`source` 和 `file` 对其他所有 `FileId`\n  返回 `FileError::AccessDenied`；修改该行为会改变信任边界，必须同步更新测试和\n  `docs/typst.md`。\n- `map_diagnostics` 移除 Typst 内部类型，并通过 `WorldExt::range` 转换范围。把该转换移到\n  Desktop 会让编译器依赖越过进程边界。\n- `TypstCompiler::{library,book,fonts}` 拥有可复用的不可变编译器状态。单份文档的可变状态\n  属于 `InMemoryWorld`。\n\n`today` 始终返回 `None`，使编译结果不依赖宿主时钟。Typst 文档请求当前日期时会得到源码诊断。\n\n## 字体与许可证\n\n`typst-assets` 的 `fonts` 功能提供当前固定字体集。`zeta-typst` 仍受仓库根目录 `LICENSE`\n约束；上游 Apache 许可证不会改变这一自有包装层的许可证。\n\n本集成拥有的上游许可证文本位于 `licenses/`：\n\n- `Typst.txt`：Typst 使用的 Apache-2.0 许可证；\n- `Typst-NOTICE.txt`：Typst 要求保留的第三方声明；\n- `Typst-Assets-NOTICE.txt`：内置字体和其他 `typst-assets` 材料的许可证与声明。\n\nDesktop 发布包还必须携带 `desktop/THIRD_PARTY_NOTICES.md` 以及 `desktop/licenses/` 下的\n对应文件。这些是面向发布的副本，必须与组件目录逐字节一致。改变 Typst 版本、资源功能或字体\n来源时，必须重新审查许可证、同步两个位置，并检查确定性输出。\n\n## 测试与修改影响\n\n```text\ncargo test -p zeta-typst\n```\n\n测试覆盖 PDF 输出、带范围的诊断、宿主文件访问拒绝和源码字节上限。修改公共结果类型时，还要\n重新生成协议样例，并运行 App Server 与 Desktop 测试。修改文件或包访问前必须更新威胁模型。\n\n## 当前限制与扩展点\n\n当前只接受 `/main.typ`，尚不支持图片、参考文献、多文件项目、Typst Universe 包、系统字体、\n取消、执行时间限制或增量编译。\n\n下一扩展点是由 App Server 提供不可变、有大小限制的内存项目文件映射，不能通过接受宿主根路径\n实现。启用不可信的长时间任务前，还必须先设计取消以及工作进程/进程隔离。",
    "headings": [
      {
        "depth": 2,
        "id": "边界与公共契约",
        "title": "边界与公共契约"
      },
      {
        "depth": 2,
        "id": "内部所有权与调用路径",
        "title": "内部所有权与调用路径"
      },
      {
        "depth": 2,
        "id": "字体与许可证",
        "title": "字体与许可证"
      },
      {
        "depth": 2,
        "id": "测试与修改影响",
        "title": "测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 负责内存编译器边界的实现细节。跨进程所有权、产品语义和分阶段演进以 docs/typst.md 为准。 zeta-typst 把调用方提供的一段 Typst 源码编译为 PDF。它拥有 Typst World 实现，明确不 提供操作系统文件系统、包注册表、网络、环境变量或时钟访问。 边界与公共契约 TypstCompiler 缓存 Typst 标准库、内置字体和字体目录。TypstCompiler::compile 借用 UTF-8 源码并返回： - TypstCompileOutcome::Success：PDF 字节和非致命诊断； - TypstCompileOutcome::Failed：普通源码错误或 PDF 生成诊断； - TypstCompileError::SourceTooLarge：源码超过 MAXTYPSTSOURCEBYTES，即按 UTF-8 字节 计算的 1 MiB。 源码错误属于编译结果而不是基础设施错误，因此 UI 无需解析内部错误字符串即可展示。诊断范围 使用半开区间，表示提交源码中的 UTF-8 字节偏移。 本 crate 不拥有 RPC 数据结构、PDF 资源保留、编辑器模型、预览 UI 或文档持久化。 内部所有权与调用路径 ``text TypstCompiler::compile - 大小校验 - InMemoryWorld::new source - typst::compile - World:: source,file,font,today - mapdiagnostics - typstpdf::pdf ` 关键私有符号： - InMemoryWorld 把 /main.typ 绑定到提交的源码。source 和 file 对其他所有 FileId 返回 FileError::AccessDenied；修改该行为会改变信任边界，必须同步更新测试和 docs/typst.md。 - mapdiagnostics 移除 Typst 内部类型，并通过 WorldExt::range 转换范围。把该转换移到 Desktop 会让编译器依赖越过进程边界。 - TypstCompiler:: library,book,fonts 拥有可复用的不可变编译器状态。单份文档的可变状态 属于 InMemoryWorld。 today 始终返回 None，使编译结果不依赖宿主时钟。Typst 文档请求当前日期时会得到源码诊断。 字体与许可证 typst-assets 的 fonts 功能提供当前固定字体集。zeta-typst 仍受仓库根目录 LICENSE 约束；上游 Apache 许可证不会改变这一自有包装层的许可证。 本集成拥有的上游许可证文本位于 licenses/： - Typst.txt：Typst 使用的 Apache-2.0 许可证； - Typst-NOTICE.txt：Typst 要求保留的第三方声明； - Typst-Assets-NOTICE.txt：内置字体和其他 typst-assets 材料的许可证与声明。 Desktop 发布包还必须携带 desktop/THIRDPARTYNOTICES.md 以及 desktop/licenses/ 下的 对应文件。这些是面向发布的副本，必须与组件目录逐字节一致。改变 Typst 版本、资源功能或字体 来源时，必须重新审查许可证、同步两个位置，并检查确定性输出。 测试与修改影响 `text cargo test -p zeta-typst ` 测试覆盖 PDF 输出、带范围的诊断、宿主文件访问拒绝和源码字节上限。修改公共结果类型时，还要 重新生成协议样例，并运行 App Server 与 Desktop 测试。修改文件或包访问前必须更新威胁模型。 当前限制与扩展点 当前只接受 /main.typ`，尚不支持图片、参考文献、多文件项目、Typst Universe 包、系统字体、 取消、执行时间限制或增量编译。 下一扩展点是由 App Server 提供不可变、有大小限制的内存项目文件映射，不能通过接受宿主根路径 实现。启用不可信的长时间任务前，还必须先设计取消以及工作进程/进程隔离。"
  },
  {
    "slug": "crates/zeterm/ui",
    "sourcePath": "zeterm/ui/README.md",
    "title": "zeta-ui",
    "description": "zeta-ui 基于 zui 提供 presentation-only 的 Button、Switch、ActionBar、ContextMenu、Dropdown、 TabList、Keycap、Sash、ContextView、ScrollView 和输入框等组合控件。它暂时 pub use zui::， 让现有产品代码可以渐进迁移 import；这只是兼容入口，不表…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是可复用 native UI 组件的当前实现说明。Element、scene、inspection、font 与基础\n> layout contract 由 [`zui`](../zui/README.md) 维护；跨 crate 渲染边界见\n> [`docs/rendering-architecture.md`](../docs/rendering-architecture.md)，\n> 后端接口与 wgpu 实现分别见 [`zeta-renderer`](../renderer/README.md) 和\n> [`zeta-wgpu`](../wgpu/README.md)；native 文本输入的跨 crate ownership 见\n> [`docs/native-text-input.md`](../docs/native-text-input.md)；product icon system 见\n> [`docs/icons.md`](../../docs/icons.md)。`Keycap` 的快捷键产品组合由\n> [`zeta-keybinding`](../keybinding/README.md) 拥有。\n\n`zeta-ui` 基于 `zui` 提供 presentation-only 的 Button、Switch、ActionBar、ContextMenu、Dropdown、\nTabList、Keycap、Sash、ContextView、ScrollView 和输入框等组合控件。它暂时 `pub use zui::*`，\n让现有产品代码可以渐进迁移 import；这只是兼容入口，不表示本 crate 拥有 framework contract。\nGPU pipeline、atlas、shader 和 surface 全部委托给 renderer backend。\n\n## 1. 边界与依赖方向\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| Presentation-only component contract、Element、scene 与 inspection | [`zui`](../zui/README.md) | 委托；本 crate 兼容 re-export |\n| Text、symbolic-icon 与 icon-only button 的状态、样式和内部布局 | `zeta-ui::Button` | ✅ |\n| Switch track、thumb、on/off 与交互状态 presentation | `zeta-ui::Switch` | ✅；值、输入路由和 accessibility 归 host |\n| Button/Separator action 排列、绘制和可查询命中几何 | `zeta-ui::ActionBar` | ✅ |\n| Tab surface 状态与横/纵 TabList 排列 | `zeta-ui::Tab` / `TabList` | ✅；product content 与 tabpanel 不在本 crate |\n| 单轴 Pane 与递归 Grid layout | `zui::{SplitViewLayout,GridLayout}` | 委托；产品 topology/state 归 host |\n| Sash 命中几何与 hover/active 反馈线 | `zeta-ui::Sash` | ✅；pointer capture、identity 与 resize transition 归 host |\n| 通用像素滚动状态、viewport 裁剪、内容坐标与滚动条交互 geometry | `zeta-ui::ScrollState` / `ScrollView` | ✅；包含 hover/active/fade presentation、thumb drag mapping 和 track paging；平台事件路由、pointer capture 与产品内容归 host |\n| 固定/可变高度列表测量、可见/overscan range、item bounds、hit-test 与虚拟化绘制 | `zeta-ui::VirtualListLayout` / `ListView` | ✅；固定高度直接计算，可变高度使用 prefix index 二分定位；identity、selection、键盘语义与产品数据归 host |\n| 虚拟 Tree 行、层级缩进、disclosure/content geometry 与命中 | `zeta-ui::TreeView` | ✅；复用固定高度 ListView；hierarchy、稳定节点 identity、展开状态和 child loading 归 host |\n| 锚点浮层布局、viewport 翻转/约束、通用外壳与浮层合成 | `zeta-ui::ContextView` / `zui::UiScene::with_overlay` | ✅；显示生命周期、关闭和输入路由归 host |\n| 柔和阴影、2px padding、4px radius、纵向 menu item geometry 与默认选择 | `zeta-ui::ContextMenu` | ✅；组合 ContextView/ActionBar，产品 identity、关闭与 command 归 host |\n| 无边框、无外层 padding 的锚定下拉项布局、可选 header 与默认选择 | `zeta-ui::Dropdown` | ✅；可滚动项复用 ListView 可见范围投影，选中 identity、header 内容、关闭与 command 归 host |\n| Icon+text label 的内部布局 | `zeta-ui::IconLabel` | ✅ |\n| 单个按键与多段快捷键的 keycap 几何和绘制 | `zeta-ui::Keycap` / `KeycapSequence` | ✅；按键语义与平台 label 归 caller |\n| Renderer-independent icon identity、SVG definition 与 rendering mode | `zeta-icon` | 委托 |\n| 非 component 单行编辑基座与 shaping | `zui::{TextInput,TextInputLayoutEngine}` | 委托 |\n| Input-box chrome、状态与 scene composition | `InputBox` | ✅ |\n| 带左侧语义图标的单行搜索框 composition | `SearchBox` | ✅；过滤策略与输入状态仍归 host |\n| Scene primitive、ordered batch、text layout 与 font catalog | `zui` | 委托；Markdown 语义归 `zeta-markdown` |\n| shaping 与 renderer-compatible text measurement | `zui` → `cosmic-text` | 委托 |\n| 后端无关 frame execution contract | `zeta-renderer::Renderer` | ❌ |\n| GPU pipeline、atlas、shader、surface 与 present | `zeta-wgpu::WgpuRenderer` | ❌ |\n| Focus、input routing 与 accessibility semantics | `zui` + product host | ❌；Button 只消费 host 投影的 focused presentation |\n\n依赖方向：\n\n```text\nproduct host → zeta-ui\nzeta-ui → zui\nproduct host → zeta-renderer → zui\nproduct host → selected backend → zeta-renderer + zui\n\nzeta-ui → zui → zeta-icon\nproduct catalog → zeta-icon\nzui(macOS font catalog) → coretext-rs → CoreText\n\nzeta-ui -X→ wgpu / Metal / Vulkan / zeta-winit\nzeta-ui -X→ App Server / workspace / product state\n```\n\n`zeta-icons` 是可选的产品语义目录；组件只接收 caller 提供的 `zeta-icon::Icon`，因此本 crate\n不需要依赖 zeterm 的产品 artwork。若本 crate 开始拥有 scene primitive、font adapter、GPU API、窗口、workspace 或产品 reducer，\n说明 ownership 已经漂移。基础 framework 的内部符号、验证与扩展点以 `zui/README.md` 为准。\n\n## 2. 文件与接口地图\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `zui::{Component,Element,ComputedElement,UiScene}` | compatibility re-export | Framework contract；canonical API 与私有 ownership 见 `zui/README.md` |\n| `components::button::{Button, ButtonState, ButtonSelection}` | public | 根据 host 投影的交互、disabled 与 selected 状态绘制 text、icon+text 或 icon-only button |\n| `components::switch::{Switch, SwitchState, SwitchSelection}` | public | 根据 host 投影的交互、on/off 状态和动画采样进度绘制 centered track 与 thumb；不拥有值、时钟或输入 |\n| `components::switch::{SwitchColors, SwitchStateColors, SwitchStyle}` | public | 定义 on/off、交互状态、track/thumb 几何、边框和圆角；动画规格属于 `zui` binding |\n| `components::action_bar::ActionBar` | public | 在 caller bounds 内排列和绘制 action representation，并公开同源 visual/interactive bounds 与 hit-test |\n| `components::action_bar::{ActionBarItem, ActionBarButton}` | public | 分别表达 Button/Separator representation 与单个 Button 的 presentation data；Button 可命名覆盖 main-axis extent |\n| `components::action_bar::{ActionBarStyle, ActionBarSeparatorStyle, ActionBarOrientation}` | public | 定义 item size、gap、separator metrics、共享 Button style 与排列轴 |\n| `components::tab_list::{Tab, TabState, TabSelection}` | public | 表达无产品 identity/content 的 Tab surface 交互与选中 presentation |\n| `components::tab_list::{TabList, TabListStyle, TabListOrientation}` | public | 横向或纵向排列 Tab surface，拥有 item size/gap，并公开同源 tab bounds |\n| `components::tab_list::{TabStyle, TabBackgrounds}` | public | 定义 border、corner radii 及普通/selected 的状态背景 |\n| `components::sash::{Sash, SashStyle, SashState}` | public | 从零面积 separator track 推导共享 drag target 与 feedback line，并绘制 host 投影的 hover/active 状态 |\n| `components::context_view::ContextView` | public | 计算锚点附近的浮层 bounds/content bounds，并把通用外壳与调用方内容画入独立浮层 |\n| `components::context_view::{ContextViewPlacement, ContextViewStyle}` | public | 分别定义锚定轴/方向/对齐/gap/viewport margin，以及 background/radius/padding；ContextView 天然无 border |\n| `components::context_view::ContextViewLayout` | public | 暴露实际 bounds、content bounds 及翻转后的方向/对齐，供 host 注册命中和组合内容 |\n| `components::context_view::{place_beside, align_with_anchor}` | private | 分别执行主轴侧边翻转/贴边与交叉轴对齐翻转/贴边；只计算 logical geometry，不读取窗口状态 |\n| `components::scroll_view::{ScrollState, ScrollMetrics, ScrollCommand}` | public | 保存 logical-pixel offset，根据 viewport/content metrics 执行按像素、首尾和 ensure-visible transition |\n| `components::scroll_view::{ScrollView, ScrollViewport}` | public | 约束有效 offset，裁剪调用方内容，并公开 translated content origin 与 visible content bounds |\n| `components::scroll_view::{ScrollbarLayout, ScrollbarHit, ScrollbarDrag}` | public | 以绘制所用的同一 track/thumb geometry 执行命中、轨道翻页和拖动到绝对 offset 的映射 |\n| `components::scroll_view::{ScrollbarController, ScrollbarPresentation, ScrollbarStyle}` | public | 计算 hover/active 与 fade-in/hold/fade-out deadline，选择语义颜色并绘制 overlay scrollbar；不安装 timer 或持有平台 pointer capture |\n| `components::list_view::{VirtualListLayout, ListView}` | public | 固定 extent 使用 O(1) geometry；可变 extent 保存 prefix index 并以 O(log n) 定位可见 range，组合 ScrollView 且只调用 projected item paint |\n| `components::list_view::ListContentPadding` | public | 显式表达列表 item sequence 前后的内容留白，不把 padding 混入首尾 item identity 或高度 |\n| `components::list_view::ListItemLayout` | public | 为一个 projected index 暴露 translated item bounds；不携带产品 identity 或内容 |\n| `components::tree_view::{TreeView, TreeViewStyle}` | public | 在 host-flattened visible node sequence 上组合 ListView，拥有 row extent、depth indentation、disclosure/content geometry 与虚拟化 |\n| `components::tree_view::{TreeItem, TreeItemExpansion, TreeItemLayout}` | public | 分别表达可见节点的 depth/Leaf/Collapsed/Expanded 结构状态，以及同源 row/disclosure/content bounds |\n| `components::context_menu::{ContextMenu, ContextMenuItem}` | public | 组合 ContextView 与纵向 ActionBar，绘制带柔和 BoxShadow 的无边框 menu surface，公开同源 item bounds/hit-test，并允许 host 在保留的 header row 中绘制搜索等产品内容 |\n| `components::context_menu::{ContextMenuSelection, ContextMenuStyle}` | public | 默认选择首个 enabled item；定义 surface color、item size/style、可选 header height 和锚点 placement，padding 固定为 2px、radius 固定为 4px |\n| `components::dropdown::{Dropdown, DropdownItem}` | public | 组合锚定浮层与纵向 label item；可滚动模式只为 visible/overscan range 构建 ActionBar，同时以 O(1) 固定高度几何公开全部 item/interactive bounds，供 host 保留键盘与 accessibility identity |\n| `components::dropdown::{DropdownSelection, DropdownStyle}` | public | 默认选择首个 enabled item，并定义 borderless surface、item size/style、可选 header height、圆角和锚点 placement |\n| `components::dropdown::DropdownScrollConfiguration` | public | 让 host 以 retained `ScrollState`、最大可见项数与 `ScrollViewStyle` 为 Dropdown 的 item region 启用独立滚动；header 保持固定 |\n| `components::icon_label::{IconLabel, IconLabelStyle}` | public | 对齐 semantic icon 与单行 text；不选择产品 icon |\n| `components::keycap::{Keycap, KeycapSequence, KeycapStyle}` | public | 绘制 caller 提供 label 的按键块，并区分同一 Chord 内按键间距与多段 Chord 间距；不解析快捷键或选择平台 label |\n| `components::input_box::InputBox` | public | 组合 base layout 与 input-box chrome/style，并实现 `Component` |\n| `components::search_box::{SearchBox, SearchBoxStyle}` | public | 复用 `InputBox` 的 chrome/text layout，在组件内拥有左侧 search icon 占位与几何 |\n\n`Color` 的 RGB channel 是 sRGB、alpha 为 straight alpha。`Point`、`Size`、font size 与 line\nheight 都使用 logical UI pixels；只有 renderer backend 可以执行 logical-to-physical 转换。\n\n## 3. 当前执行路径\n\n```text\nhost\n  → GridLayout::new\n      → each GridNode::Split → SplitViewLayout::new\n          → fit_sizes / distribute_delta\n          → pane bounds + SplitViewSashLayout\n      → recursive leaf bounds + GridSashLayout\n  → Sash::new\n      → interaction_bounds (host hit registration)\n      → Component::paint (hover/active feedback)\n  → TextInput::apply / apply_composition (editing only)\n  → CaretBlinkController::focus / activity / advance\n  → InputBox::new\n      → TextInputLayoutEngine::layout\n  → UiScene::new\n  → UiScene::draw_component\n      → Component::element → ComponentElement::compute\n      → ComputedElement → automatic InspectionNode\n      → Component::paint_element(same ComputedElement)\n          ├─ ContextView → anchored layout → overlay layer\n          │   ├─ floating shell rect\n          │   └─ caller content inside content-bounds clip\n          ├─ ContextMenu → ContextView + shadow/menu surface + vertical ActionBar\n          │   └─ soft BoxShadow + 2px padding + 4px radius + selected MenuItem presentation\n          ├─ Dropdown → ContextView + ListView projected range + vertical ActionBar\n          │   └─ visible/overscan selected item → Button selection presentation\n          ├─ ScrollView → viewport clip + translated content geometry + interactive scrollbar chrome\n          ├─ ListView → ScrollView + fixed/variable-extent visible/overscan item projection\n          ├─ TreeView → fixed ListView + depth/disclosure/content item projection\n          ├─ ActionBar → item bounds\n          │   ├─ ActionBarButton → Button → icon/text primitives\n          │   └─ Separator → rect primitive\n          ├─ TabList → Tab bounds → state/selection surface rect\n          ├─ Button state/style → IconLabel → icon/text primitives\n          ├─ Files row presentation → IconLabel → icon/text primitives\n          ├─ zui::AnimationBinding → Switch progress → track/thumb rect primitives\n          └─ InputBox → rect/text primitives\n  → UiScene::draw_rect / UiScene::draw_image / UiScene::draw_icon / UiScene::draw_text\n  → UiScene::batches (layer order + exact cross-kind paint order)\n  → zeta_renderer::Renderer::render_scene\n      → selected backend\n```\n\n`UiScene` 是本 crate 依赖的 `zui` 输出边界；组件只通过 `zui::Component` 写入它，不拥有 scene\n协议。当前 wgpu 如何消费这些数据由\n[`zeta-wgpu`](../wgpu/README.md) 说明；替换规则由\n[`docs/rendering-architecture.md`](../docs/rendering-architecture.md) 统一定义。\n\n## 4. 字体与 CoreText\n\n字体实现由 `zui` 拥有，以下是组件必须遵守的 delegated contract。`FontCatalog::system` 在 macOS\n调用 `coretext::FontCollection::available`，只返回 canonicalized\nfamily-name snapshot。其他平台从 cosmic-text 的 font database 枚举 family。\n\n当前文本测量路径在所有平台统一使用 cosmic-text；具体 backend 可以复用\n`renderer_support` 提供的 font policy：\n\n- cosmic-text 负责 shaping、line breaking、font matching 与 fallback；\n- `TextLayoutEngine` 负责向组件返回 logical geometry；\n- glyph raster、atlas 与 draw 归具体 renderer backend。\n\n`zui::font::system::new_font_system` 是 `TextInputLayoutEngine` 与 backend bridge 的私有共同构造入口。\n它加载系统字体、保留系统 locale，并在 macOS 排除 `GB18030 Bitmap`：该 face 能被 cosmic-text\n选为 CJK fallback，但 swash 不能把其 bitmap glyph 栅格化，若不排除会出现 cell/caret 已推进而\n字形透明。排除后 CJK 继续由可栅格化的系统 outline font fallback 承担。平台 filter 只处理\n已验证的 backend incompatibility，不承担产品字体偏好。\n\n因此“已经接入 CoreText”只表示 macOS 原生 font catalog 已接入，不能解读为 CTLine shaping、\nCoreGraphics raster 或原生 typographic metrics 已经成为绘制事实。\n\n## 5. 校验、失败与接入义务\n\n- `ImageData::from_rgba8` 拒绝零 dimensions、乘法溢出与不匹配的 RGBA byte length；\n- scene 保存 logical geometry 和显式 clip/layer，不执行 GPU capability 或 atlas 校验；\n- 不换行的 code/input row 必须选择 `TextBlockWrap::None`，不能依赖 renderer 默认断行；\n- primitive validation、raster、atlas、scale factor 与 surface failure 属于具体 renderer backend；\n- backend-neutral outcome 与 error wrapping 属于 `zeta-renderer`。\n\nHost 必须先根据当前 logical layout 构造完整 `UiScene`，再把同一帧的 physical extent 与 scale\nfactor 交给 renderer。不要预先把 text coordinates 乘 DPI，否则会发生二次缩放。\n`Component` implementation 只能消费 caller 已投影好的 presentation state 并发出 primitives；\ncomponent bounds、hit registration、event dispatch 和 authoritative state transition 仍由 host\n拥有。每个组件通过 `Component::element` 返回声明式 `ComponentElement`，并在\n`Component::paint_element` 中消费框架计算的同一个 `ComputedElement`；`UiScene::draw_component`\n只计算一次，再自动把 name、authored style、computed bounds、resolved padding、gap、gap regions、\nradius 与 Element 声明位置写入检查快照。Caller 必须使用 `UiScene::draw_component`，\n由它在当前 nested clip 内自动注册 inspection parent 并同步 paint；这不引入 retained component\ninstance、隐式 identity 或 lifecycle。`Button` 拥有 control 内部 padding 和 state-specific\nbackground selection，并把 icon/text placement 委托给 `IconLabel`；`Button::icon` 保留不参与\n绘制的 accessible label，供 host 的后续 accessibility adapter 使用。Files pane 同样把文件行\n的 icon/text 几何委托给 `IconLabel`，但由 product host 先选择具体 semantic file icon。Caller\n必须显式提供\n`ButtonState`、`ButtonStyle`、bounds 与具体 content constructor。`ButtonState::Focused`\n让 host 明确投影键盘 focus，不让组件自行监听键盘；selected presentation 通过\n`ButtonSelection` 独立投影。\n\n`SplitViewLayout` 是每帧重算的 immutable geometry，不是 retained widget。Host 保存每个\nPane 的 preferred size 与 visibility，传入 `SplitViewPane`；布局只计算当前 viewport 下的\neffective size，因此窗口临时缩小不能覆盖用户首选尺寸。`SplitViewSashLayout` 的\n`resize_snapshot` 固定一次拖动开始时相邻 Pane 的尺寸与约束，Host 必须始终用相对 drag-start\n的 delta 调用 `resize`，不能逐 pointer move 累加 delta。`Sash` 使用同一个 zero-area track\n推导 interaction bounds 和 feedback bounds；Host 用前者注册 identity、cursor、\naccessibility 与 pointer capture，再把 hover/active 投影为 `SashState`。若 Host 另行计算\n命中区域或直接在产品 scene 中画反馈线，说明 Sash geometry ownership 已漂移。\n\n`GridLayout` 在这层单轴能力上递归解析 caller-owned `GridNode`。每个 Split 的 identity、\norientation、children 与 preferred sizes 都来自 Host；布局只输出当前帧的 Leaf/Split bounds\n和带 owning split identity 的 `GridSashLayout`。产品层必须把 resize 结果写回对应 Split，\n并自行处理 add/remove/move、active Pane、Session binding 与序列化。若 `zeta-ui` 开始创建\nTerminal Session、决定 split command 或跨帧修改树，说明 Grid ownership 已漂移。\n\n`ScrollState` 是 logical-pixel offset primitive，不读取 `winit::MouseScrollDelta`。\nHost 把平台 wheel、键盘或 scrollbar drag 归一化为 `ScrollCommand`，再使用同一\n`ScrollMetrics` 更新 retained state。`ScrollView::draw` 把调用方内容裁剪到 viewport，并通过\n`ScrollViewport` 返回 translated content origin 和 content-coordinate visible bounds；\n`ScrollbarLayout` 与最终 track/thumb paint 使用同一 geometry。`ListView` 在这层基座上为固定\n或可变高度数据提供 content height、可见/overscan range、item bounds、point hit-test 和\nensure-visible command；固定高度不分配逐项 geometry，可变高度保存 extent prefix index，\n通过二分查找定位 viewport。它只向 caller 请求 projected index，不拥有 item。高度重新测量、\nscroll anchoring、focus reveal policy 和交互 identity 仍归 composed control 或产品 host。\n`TreeView` 再把 host 已按展开状态扁平化的 visible node sequence 映射为固定高度 ListView\nitems，计算 depth indentation 与 disclosure/content bounds；它不读取 children、不持有展开状态，\n也不生成产品节点 identity。\nTerminal 从底部计数和输出增长锚定不属于通用 `ScrollState`；Native 的\n`TerminalOutputScrollView` 只负责把该产品状态适配为 `ScrollView` 的顶部相对内容坐标。\n\n`ActionBar` 接收 caller-provided outer bounds，用 `Element::row/column` 声明 Button/Separator 的\n方向、间距和 item fixed size；zui element layout 生成的 `ComputedElement` 同时驱动 paint、\n`item_bounds`/hit-test 与自动 inspection。默认 item extent 来自共享 style；label 长度不同的正式 Toolbar 可以通过\n`ActionBarButton::with_main_axis_extent` 覆盖单项主轴尺寸。`ActionBar::item_bounds` 暴露 visual\nbounds；`ActionBar::interactive_item_bounds` 与\n`ActionBar::hit_test` 复用相同几何并排除 disabled Button 和 Separator。Host 必须把返回的 item\nindex 映射到自己的 action identity 和命令。ActionBar 不持有 callback、命令、hover/focus\nstate 或 product action registry。\n`TabList` 同样用 Element pipeline 消费 caller-provided bounds、排列轴、Tab presentation 和 style。\n`TabList::tab_bounds` 是 host 注册命中范围和组合 label/icon/status content 的唯一几何来源；\n`TabList` 不持有 tab identity、activation、focus、accessibility、关闭动作或对应 tabpanel。\nSession navigation 和后续 Editor tabs 可以复用同一 surface/排列 primitive，但各自保留内容\n布局与 active panel 生命周期。\n`ContextView::new` 接收同一 logical coordinate space 中的 viewport、anchor 和期望 content\nsize。它先把 padding 加入外壳尺寸，再按 `ContextViewPlacement` 尝试首选侧和对齐；\n首选位置不适合时先翻转，仍无法完整放入时贴紧 inset viewport 并约束外壳和内容尺寸。\n`ContextView::draw` 把外壳与调用方 closure 发出的任意 primitive 放入同一个新浮层；该层不继承\nhost component 的 clip，因此可以越过锚点所在控件的边界，调用方内容再单独裁剪到\n`content_bounds`。Host 必须使用同一 `ContextViewLayout`\n注册命中区域，并自行管理 open/close、outside click、Escape、focus restoration 和 anchored\ncontent 的领域交互；这些 retained lifecycle 不进入 scene component。当前\n`ContextViewStyle` 不暴露 border：浮层天然无边框；若某个具体浮层需要描边，应由其内容组件\n拥有并绘制，不能改变 ContextView 的定位几何。`ContextMenu` 在 ContextView 上建立通用菜单\n基座：`PaintRect::with_shadow` 让 renderer 在本体前生成扩展 shadow quad，fragment shader\n根据圆角矩形 signed distance 近似 Gaussian coverage，以 surface 边缘作为 50% coverage\n中点；ContextMenu 再组合低透明度的 ambient shadow 与向下偏移的 key shadow，避免单层黑色\n光晕。内部无边框 menu surface 固定使用 2px padding 与 4px radius，再由纵向 ActionBar\n排列 label item。Host 通过\n`ContextMenuSelection::Item` 投影唯一选中项，并使用 ContextMenu 返回的同源 bounds 注册交互。\n`ContextMenuStyle::with_header_height` 只保留 product-owned header geometry；\n`ContextMenu::paint_with_header` 保证 header 与菜单项绘制在同一 overlay。搜索状态、输入语义、\n过滤和焦点仍由 host 保存，ContextMenu 不依赖 `TextInput` 或业务数据。Native 的分支菜单通过\n这条 header contract 组合产品搜索。\n`Dropdown` 是另一层 ContextView 组合：它使用无外层 padding 的浮层外壳，\n用垂直 ActionBar 排列 label item，并默认选择第一个 enabled item。Host 可以用\n`DropdownSelection::Item` 投影 hover/focus/pressed 对应的唯一选择，用 Dropdown 返回的同源\nbounds 注册交互。`DropdownStyle::with_header_height` 与 `Dropdown::paint_with_header` 为\n选择器保留 product-owned header，但不拥有查询或输入状态；Native 的工作区目录 picker 通过\n这条契约组合 SearchBox。`Dropdown::new_scrollable` 进一步把 item region 组合进 ScrollView，\n并以 `DropdownScrollConfiguration` 限制可见行数；header 不随内容滚动。selected identity、\nopen/close、平台滚轮路由和 command 不进入组件。\n`TextInput` 拥有 local editing state 和 composition，但不拥有 focus、platform IME lifecycle、\ncomponent chrome 或产品 reducer。`InputBox::new` 使用 `TextInputLayoutEngine` 从 base state\n生成 immutable layout，再组合 background、border、placeholder、selection、caret 和 preedit\npresentation。`InputBoxState::Focused(CaretVisibility)` 显式投影 blink phase；组件不读取时钟。\nIME 候选框定位读取同一个 `InputBox::caret_bounds`，即使 blink phase 隐藏也不能按字符数量另行\n估算。\n\n## 6. 测试与修改路径\n\n```bash\ncargo test --manifest-path Cargo.toml -p zui -p zeta-ui\nbazel test //zeterm/ui:ui-unit-tests\n```\n\n`zui` 单元测试覆盖检查节点、Element、scene、font/text input 与 Split/Grid；`zeta-ui` 单元测试覆盖\n组件裁剪与浮层合成、Sash 命中/反馈几何与状态绘制，ScrollState 的 axis clamp、绝对 offset、首尾和\nensure-visible transition，ScrollView 的内容坐标、裁剪、visibility policy、比例 thumb geometry、\ntrack paging、thumb drag 映射、hover/active 颜色与 fade deadline，ListView 的固定/可变高度\nvisible/overscan range、prefix geometry、gap/padding、translated bounds、hit-test、ensure-visible\n与 projected-only paint，TreeView 的 depth/disclosure geometry、命中与 projected-only paint，\nContextView 的纵/横锚定、\n翻转、对齐、viewport 约束、外壳/内容裁剪，Dropdown 的默认/显式选择、无外层 inset 与命中，\nContextMenu 的柔和阴影、2px padding、4px radius、默认/显式选择与命中，ActionBar 排列与命中、\nTabList 横纵排列与\nsurface 状态、按钮、图标标签和输入框的状态/样式/布局。GPU conversion、shader、atlas 与 input\nvalidation 测试属于具体 backend crate。\n\n- 扩展 text style/span、path、rect/clip、font 或 scene：修改 `zui`、backend 与其 canonical README，\n  再检查本 crate 的组件和 `zeta-markdown` projection；\n- 更换 shaping backend：保持 `zui::UiScene` 平台无关，并更新字体语义与 backend bridge；\n- 修改 DPI 转换、shader、atlas 或 glyph raster：只修改具体 backend，不向组件暴露实现类型；\n- 新增拥有 box geometry 的组件：实现必需的 `Component::element` 与需要时的 `paint_element`，让\n  `ComputedElement` 同时驱动 paint/hit-test/inspection，并在 sibling test 中验证 computed geometry；\n  组合调用统一使用 `UiScene::draw_component`。非组件布局函数或拥有自定义 content closure 的\n  composition surface 使用 `UiScene::with_element`；产品代码不直接调用 `with_inspection_node`。\n\n## 7. 当前限制与扩展点\n\n当前限制：\n\n- framework scene、Element、text input 与 Split/Grid 的限制由 `zui/README.md` 维护；\n- 全部标准组件以及 `ContextView` content-closure/overlay 入口都使用 Element/ComputedElement 自动\n  检查路径。纯 layout projection 和 primitive helper 仍不制造节点，检查器也不会从裸 scene\n  primitives 反推 ownership；\n- `Button` 当前支持 resting、hovered、focused、pressed、disabled、selected、icon-only 与\n  leading icon，但尚无独立 focus ring、trailing content 或真实 accessibility adapter；\n- `ActionBar` 当前支持 horizontal/vertical Button 与 Separator、同源 item bounds 和 hit-test，\n  但尚无 roving focus、keyboard navigation、overflow 或 custom representation；\n- `Dropdown` 当前只支持单列 label item、固定 item size、单项 selection 与非虚拟化滚动；icon、\n  separator、submenu、typeahead、打开/关闭与 accessibility scope 尚无；\n- `BoxShadow` 当前支持单个 rounded-rect shadow 的 color、offset 与 blur radius；尚无 spread、\n  inset 或多重 shadow。ContextMenu 也暂不内建 icon、separator、submenu、typeahead 或超高菜单滚动；\n- `TabList` 当前只拥有固定 item size、gap 和 Tab surface paint；custom content、动态宽度、\n  overflow、close action、identity、interaction 与 tabpanel 均由 composed control/host 拥有；\n- `ContextView` 不拥有 shadow、arrow/callout，也不拥有 outside click、Escape、focus\n  restoration 或 accessibility scope；overflow shadow 由托管内容的 `PaintRect` 拥有，\n  lifecycle/interaction 由 host 与 `zui` 组合；\n- `ScrollView` 当前提供 overlay scrollbar 的同源 paint/hit/track-page/thumb-drag geometry，\n  `ScrollbarController` 提供 hover/active/fade presentation；平台事件接线、pointer capture、\n  滚动惯性、overscroll 和 accessibility adapter 仍由 host/dispatch 扩展；\n- `ListView` 支持固定和可变 item extent，但不测量 item 内容；caller 必须在高度改变时重建\n  layout。稳定 item identity、selection、键盘导航、滚动锚定、focus anchor 与 Tree\n  flatten/expand semantics 仍由 composed control 或后续专用组件拥有；\n- `TreeView` 只消费 host-flattened visible nodes；异步 child loading、展开状态持久化、稳定节点\n  identity、selection、重命名、拖放和文件打开仍属于产品 Tree model/host；\n- `Sash` 当前只拥有 presentation geometry，没有 pointer capture、keyboard resize 或\n  accessibility adapter；这些交互由 host 与 `zui` 组合；\n- `InputBox` 消费显式 blink phase，但没有 mouse caret hit testing、drag selection 或\n  disabled/read-only presentation；\n- native 当前使用 `CaretBlinkController::default` 的 530ms half-period，尚未读取系统 caret\n  blink preference 或 reduced-motion setting；\n- 没有 widget tree、retained Grid state、focus、input routing、IME lifecycle 或\n  accessibility；\n- 每次测量重建 cosmic-text buffer，富文本 span 会在同一个 paragraph buffer 中 shaping，但尚无\n  paragraph-level retained cache；\n- `FontWeight` 与 `FontStyle` 只有常用 semantic variants；\n- CoreText 只做 catalog，不做 shaping/raster，也没有 app font registration；\n\n扩展点：出现第二个需要 secondary action/overflow 的真实消费者后，可以在 `ActionBar` 上组合\n独立 `ToolBar`；出现 Dropdown 或 custom representation 后，再增加对应 `ActionBarItem`\nvariant，不为单一 Button 增加纯转发 wrapper。出现真实编辑器/终端消费者后，还可以分别增加\n可增长或可回收的图标图集、保留式段落缓存、平台字体注册、RGBA 图像/路径\nprimitives 与统一 display list。是否采用 CoreText shaping 应由跨平台 metrics/fallback\n一致性测试决定，不是当前 API 的既定承诺。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与依赖方向",
        "title": "1. 边界与依赖方向"
      },
      {
        "depth": 2,
        "id": "2-文件与接口地图",
        "title": "2. 文件与接口地图"
      },
      {
        "depth": 2,
        "id": "3-当前执行路径",
        "title": "3. 当前执行路径"
      },
      {
        "depth": 2,
        "id": "4-字体与-coretext",
        "title": "4. 字体与 CoreText"
      },
      {
        "depth": 2,
        "id": "5-校验失败与接入义务",
        "title": "5. 校验、失败与接入义务"
      },
      {
        "depth": 2,
        "id": "6-测试与修改路径",
        "title": "6. 测试与修改路径"
      },
      {
        "depth": 2,
        "id": "7-当前限制与扩展点",
        "title": "7. 当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是可复用 native UI 组件的当前实现说明。Element、scene、inspection、font 与基础 layout contract 由 zui 维护；跨 crate 渲染边界见 docs/rendering-architecture.md， 后端接口与 wgpu 实现分别见 zeta-renderer 和 zeta-wgpu；native 文本输入的跨 crate ownership 见 docs/native-text-input.md；product icon system 见 docs/icons.md。Keycap 的快捷键产品组合由 zeta-keybinding 拥有。 zeta-ui 基于 zui 提供 presentation-only 的 Button、Switch、ActionBar、ContextMenu、Dropdown、 TabList、Keycap、Sash、ContextView、ScrollView 和输入框等组合控件。它暂时 pub use zui::， 让现有产品代码可以渐进迁移 import；这只是兼容入口，不表示本 crate 拥有 framework contract。 GPU pipeline、atlas、shader 和 surface 全部委托给 renderer backend。 1. 边界与依赖方向 能力 当前 owner 状态 --- --- --- Presentation-only component contract、Element、scene 与 inspection zui 委托；本 crate 兼容 re-export Text、symbolic-icon 与 icon-only button 的状态、样式和内部布局 zeta-ui::Button ✅ Switch track、thumb、on/off 与交互状态 presentation zeta-ui::Switch ✅；值、输入路由和 accessibility 归 host Button/Separator action 排列、绘制和可查询命中几何 zeta-ui::ActionBar ✅ Tab surface 状态与横/纵 TabList 排列 zeta-ui::Tab / TabList ✅；product content 与 tabpanel 不在本 crate 单轴 Pane 与递归 Grid layout zui:: SplitViewLayout,GridLayout 委托；产品 topology/state 归 host Sash 命中几何与 hover/active 反馈线 zeta-ui::Sash ✅；pointer capture、identity 与 resize transition 归 host 通用像素滚动状态、viewport 裁剪、内容坐标与滚动条交互 geometry zeta-ui::ScrollState / ScrollView ✅；包含 hover/active/fade presentation、thumb drag mapping 和 track paging；平台事件路由、pointer capture 与产品内容归 host 固定/可变高度列表测量、可见/overscan range、item bounds、hit-test 与虚拟化绘制 zeta-ui::VirtualListLayout / ListView ✅；固定高度直接计算，可变高度使用 prefix index 二分定位；identity、selection、键盘语义与产品数据归 host 虚拟 Tree 行、层级缩进、disclosure/content geometry 与命中 zeta-ui::TreeView ✅；复用固定高度 ListView；hierarchy、稳定节点 identity、展开状态和 child loading 归 host 锚点浮层布局、viewport 翻转/约束、通用外壳与浮层合成 zeta-ui::ContextView / zui::UiScene::withoverlay ✅；显示生命周期、关闭和输入路由归 host 柔和阴影、2px padding、4px radius、纵向 menu item geometry 与默认选择 zeta-ui::ContextMenu ✅；组合 ContextView/ActionBar，产品 identity、关闭与 command 归 host 无边框、无外层 padding 的锚定下拉项布局、可选 header 与默认选择 zeta-ui::Dropdown ✅；可滚动项复用 ListView 可见范围投影，选中 identity、header 内容、关闭与 command 归 host Icon+text label 的内部布局 zeta-ui::IconLabel ✅ 单个按键与多段快捷键的 keycap 几何和绘制 zeta-ui::Keycap / KeycapSequence ✅；按键语义与平台 label 归 caller Renderer-independent icon identity、SVG definition 与 rendering mode zeta-icon 委托 非 component 单行编辑基座与 shaping zui:: TextInput,TextInputLayoutEngine 委托 Input-box chrome、状态与 scene composition InputBox ✅ 带左侧语义图标的单行搜索框 composition SearchBox ✅；过滤策略与输入状态仍归 host Scene primitive、ordered batch、text layout 与 font catalog zui 委托；Markdown 语义归 zeta-markdown shaping 与 renderer-compatible text measurement zui → cosmic-text 委托 后端无关 frame execution contract zeta-renderer::Renderer ❌ GPU pipeline、atlas、shader、surface 与 present zeta-wgpu::WgpuRenderer ❌ Focus、input routing 与 accessibility semantics zui + product host ❌；Button 只消费 host 投影的 focused presentation 依赖方向： ``text product host → zeta-ui zeta-ui → zui product host → zeta-renderer → zui product host → selected backend → zeta-renderer + zui zeta-ui → zui → zeta-icon product catalog → zeta-icon zui macOS font catalog → coretext-rs → CoreText zeta-ui -X→ wgpu / Metal / Vulkan / zeta-winit zeta-ui -X→ App Server / workspace / product state ` zeta-icons 是可选的产品语义目录；组件只接收 caller 提供的 zeta-icon::Icon，因此本 crate 不需要依赖 zeterm 的产品 artwork。若本 crate 开始拥有 scene primitive、font adapter、GPU API、窗口、workspace 或产品 reducer， 说明 ownership 已经漂移。基础 framework 的内部符号、验证与扩展点以 zui/README.md 为准。 2. 文件与接口地图 Symbol 可见性 精确职责 --- --- --- zui:: Component,Element,ComputedElement,UiScene compatibility re-export Framework contract；canonical API 与私有 ownership 见 zui/README.md components::button:: Button, ButtonState, ButtonSelection public 根据 host 投影的交互、disabled 与 selected 状态绘制 text、icon+text 或 icon-only button components::switch:: Switch, SwitchState, SwitchSelection public 根据 host 投影的交互、on/off 状态和动画采样进度绘制 centered track 与 thumb；不拥有值、时钟或输入 components::switch:: SwitchColors, SwitchStateColors, SwitchStyle public 定义 on/off、交互状态、track/thumb 几何、边框和圆角；动画规格属于 zui binding components::actionbar::ActionBar public 在 caller bounds 内排列和绘制 action representation，并公开同源 visual/interactive bounds 与 hit-test components::actionbar:: ActionBarItem, ActionBarButton public 分别表达 Button/Separator representation 与单个 Button 的 presentation data；Button 可命名覆盖 main-axis extent components::actionbar:: ActionBarStyle, ActionBarSeparatorStyle, ActionBarOrientation public 定义 item size、gap、separator metrics、共享 Button style 与排列轴 components::tablist:: Tab, TabState, TabSelection public 表达无产品 identity/content 的 Tab surface 交互与选中 presentation components::tablist:: TabList, TabListStyle, TabListOrientation public 横向或纵向排列 Tab surface，拥有 item size/gap，并公开同源 tab bounds components::tablist:: TabStyle, TabBackgrounds public 定义 border、corner radii 及普通/selected 的状态背景 components::sash:: Sash, SashStyle, SashState public 从零面积 separator track 推导共享 drag target 与 feedback line，并绘制 host 投影的 hover/active 状态 components::contextview::ContextView public 计算锚点附近的浮层 bounds/content bounds，并把通用外壳与调用方内容画入独立浮层 components::contextview:: ContextViewPlacement, ContextViewStyle public 分别定义锚定轴/方向/对齐/gap/viewport margin，以及 background/radius/padding；ContextView 天然无 border components::contextview::ContextViewLayout public 暴露实际 bounds、content bounds 及翻转后的方向/对齐，供 host 注册命中和组合内容 components::contextview:: placebeside, alignwithanchor private 分别执行主轴侧边翻转/贴边与交叉轴对齐翻转/贴边；只计算 logical geometry，不读取窗口状态 components::scrollview:: ScrollState, ScrollMetrics, ScrollCommand public 保存 logical-pixel offset，根据 viewport/content metrics 执行按像素、首尾和 ensure-visible transition components::scrollview:: ScrollView, ScrollViewport public 约束有效 offset，裁剪调用方内容，并公开 translated content origin 与 visible content bounds components::scrollview:: ScrollbarLayout, ScrollbarHit, ScrollbarDrag public 以绘制所用的同一 track/thumb geometry 执行命中、轨道翻页和拖动到绝对 offset 的映射 components::scrollview:: ScrollbarController, ScrollbarPresentation, ScrollbarStyle public 计算 hover/active 与 fade-in/hold/fade-out deadline，选择语义颜色并绘制 overlay scrollbar；不安装 timer 或持有平台 pointer capture components::listview:: VirtualListLayout, ListView public 固定 extent 使用 O 1 geometry；可变 extent 保存 prefix index 并以 O log n 定位可见 range，组合 ScrollView 且只调用 projected item paint components::listview::ListContentPadding public 显式表达列表 item sequence 前后的内容留白，不把 padding 混入首尾 item identity 或高度 components::listview::ListItemLayout public 为一个 projected index 暴露 translated item bounds；不携带产品 identity 或内容 components::treeview:: TreeView, TreeViewStyle public 在 host-flattened visible node sequence 上组合 ListView，拥有 row extent、depth indentation、disclosure/content geometry 与虚拟化 components::treeview:: TreeItem, TreeItemExpansion, TreeItemLayout public 分别表达可见节点的 depth/Leaf/Collapsed/Expanded 结构状态，以及同源 row/disclosure/content bounds components::contextmenu:: ContextMenu, ContextMenuItem public 组合 ContextView 与纵向 ActionBar，绘制带柔和 BoxShadow 的无边框 menu surface，公开同源 item bounds/hit-test，并允许 host 在保留的 header row 中绘制搜索等产品内容 components::contextmenu:: ContextMenuSelection, ContextMenuStyle public 默认选择首个 enabled item；定义 surface color、item size/style、可选 header height 和锚点 placement，padding 固定为 2px、radius 固定为 4px components::dropdown:: Dropdown, DropdownItem public 组合锚定浮层与纵向 label item；可滚动模式只为 visible/overscan range 构建 ActionBar，同时以 O 1 固定高度几何公开全部 item/interactive bounds，供 host 保留键盘与 accessibility identity components::dropdown:: DropdownSelection, DropdownStyle public 默认选择首个 enabled item，并定义 borderless surface、item size/style、可选 header height、圆角和锚点 placement components::dropdown::DropdownScrollConfiguration public 让 host 以 retained ScrollState、最大可见项数与 ScrollViewStyle 为 Dropdown 的 item region 启用独立滚动；header 保持固定 components::iconlabel:: IconLabel, IconLabelStyle public 对齐 semantic icon 与单行 text；不选择产品 icon components::keycap:: Keycap, KeycapSequence, KeycapStyle public 绘制 caller 提供 label 的按键块，并区分同一 Chord 内按键间距与多段 Chord 间距；不解析快捷键或选择平台 label components::inputbox::InputBox public 组合 base layout 与 input-box chrome/style，并实现 Component components::searchbox:: SearchBox, SearchBoxStyle public 复用 InputBox 的 chrome/text layout，在组件内拥有左侧 search icon 占位与几何 Color 的 RGB channel 是 sRGB、alpha 为 straight alpha。Point、Size、font size 与 line height 都使用 logical UI pixels；只有 renderer backend 可以执行 logical-to-physical 转换。 3. 当前执行路径 `text host → GridLayout::new → each GridNode::Split → SplitViewLayout::new → fitsizes / distributedelta → pane bounds + SplitViewSashLayout → recursive leaf bounds + GridSashLayout → Sash::new → interactionbounds host hit registration → Component::paint hover/active feedback → TextInput::apply / applycomposition editing only → CaretBlinkController::focus / activity / advance → InputBox::new → TextInputLayoutEngine::layout → UiScene::new → UiScene::drawcomponent → Component::element → ComponentElement::compute → ComputedElement → automatic InspectionNode → Component::paintelement same ComputedElement ├─ ContextView → anchored layout → overlay layer │ ├─ floating shell rect │ └─ caller content inside content-bounds clip ├─ ContextMenu → ContextView + shadow/menu surface + vertical ActionBar │ └─ soft BoxShadow + 2px padding + 4px radius + selected MenuItem presentation ├─ Dropdown → ContextView + ListView projected range + vertical ActionBar │ └─ visible/overscan selected item → Button selection presentation ├─ ScrollView → viewport clip + translated content geometry + interactive scrollbar chrome ├─ ListView → ScrollView + fixed/variable-extent visible/overscan item projection ├─ TreeView → fixed ListView + depth/disclosure/content item projection ├─ ActionBar → item bounds │ ├─ ActionBarButton → Button → icon/text primitives │ └─ Separator → rect primitive ├─ TabList → Tab bounds → state/selection surface rect ├─ Button state/style → IconLabel → icon/text primitives ├─ Files row presentation → IconLabel → icon/text primitives ├─ zui::AnimationBinding → Switch progress → track/thumb rect primitives └─ InputBox → rect/text primitives → UiScene::drawrect / UiScene::drawimage / UiScene::drawicon / UiScene::drawtext → UiScene::batches layer order + exact cross-kind paint order → zetarenderer::Renderer::renderscene → selected backend ` UiScene 是本 crate 依赖的 zui 输出边界；组件只通过 zui::Component 写入它，不拥有 scene 协议。当前 wgpu 如何消费这些数据由 zeta-wgpu 说明；替换规则由 docs/rendering-architecture.md 统一定义。 4. 字体与 CoreText 字体实现由 zui 拥有，以下是组件必须遵守的 delegated contract。FontCatalog::system 在 macOS 调用 coretext::FontCollection::available，只返回 canonicalized family-name snapshot。其他平台从 cosmic-text 的 font database 枚举 family。 当前文本测量路径在所有平台统一使用 cosmic-text；具体 backend 可以复用 renderersupport 提供的 font policy： - cosmic-text 负责 shaping、line breaking、font matching 与 fallback； - TextLayoutEngine 负责向组件返回 logical geometry； - glyph raster、atlas 与 draw 归具体 renderer backend。 zui::font::system::newfontsystem 是 TextInputLayoutEngine 与 backend bridge 的私有共同构造入口。 它加载系统字体、保留系统 locale，并在 macOS 排除 GB18030 Bitmap：该 face 能被 cosmic-text 选为 CJK fallback，但 swash 不能把其 bitmap glyph 栅格化，若不排除会出现 cell/caret 已推进而 字形透明。排除后 CJK 继续由可栅格化的系统 outline font fallback 承担。平台 filter 只处理 已验证的 backend incompatibility，不承担产品字体偏好。 因此“已经接入 CoreText”只表示 macOS 原生 font catalog 已接入，不能解读为 CTLine shaping、 CoreGraphics raster 或原生 typographic metrics 已经成为绘制事实。 5. 校验、失败与接入义务 - ImageData::fromrgba8 拒绝零 dimensions、乘法溢出与不匹配的 RGBA byte length； - scene 保存 logical geometry 和显式 clip/layer，不执行 GPU capability 或 atlas 校验； - 不换行的 code/input row 必须选择 TextBlockWrap::None，不能依赖 renderer 默认断行； - primitive validation、raster、atlas、scale factor 与 surface failure 属于具体 renderer backend； - backend-neutral outcome 与 error wrapping 属于 zeta-renderer。 Host 必须先根据当前 logical layout 构造完整 UiScene，再把同一帧的 physical extent 与 scale factor 交给 renderer。不要预先把 text coordinates 乘 DPI，否则会发生二次缩放。 Component implementation 只能消费 caller 已投影好的 presentation state 并发出 primitives； component bounds、hit registration、event dispatch 和 authoritative state transition 仍由 host 拥有。每个组件通过 Component::element 返回声明式 ComponentElement，并在 Component::paintelement 中消费框架计算的同一个 ComputedElement；UiScene::drawcomponent 只计算一次，再自动把 name、authored style、computed bounds、resolved padding、gap、gap regions、 radius 与 Element 声明位置写入检"
  },
  {
    "slug": "crates/utils/image",
    "sourcePath": "zeta-rs/utils/image/README.md",
    "title": "zeta-utils-image",
    "description": "zeta-utils-image 在不可信图片 bytes 对模型可见之前，负责供应商无关的校验和转换。跨 crate 的 安全边界与 Tool output policy 以 docs/tools.md 为准；本 README 是 crate 实现契约的权威说明。",
    "group": "Crate 实现参考",
    "markdown": "`zeta-utils-image` 在不可信图片 bytes 对模型可见之前，负责供应商无关的校验和转换。跨 crate 的\n安全边界与 Tool output policy 以 [`docs/tools.md`](../../../docs/tools.md) 为准；本 README 是\ncrate 实现契约的权威说明。\n\n## 所有权\n\n本 crate 拥有受支持格式识别、Base64 data URL 解析与生成、真实图片解码、编码与解码资源限制、\n尺寸/像素/frame 校验、缩放与 patch budget 计算、ICC/EXIF 处理、静态转码以及进程内有界缓存。\n它不读取文件、不抓取远程 URL、不选择模型 capability、不持久化附件、不决定产品 limits、不发送\ntelemetry，也不决定处理失败的图片如何出现在对话中。\n\n`zeta-core` 提供 `PromptImagePolicy`，在 durable user/tool 图片内容记录前调用处理，并把失败映射\n为产品语义。产品 host 可以使用 `detect_image_format` 和 `data_url_from_bytes` 投影本地附件，但\n这些检查不是权威校验。Provider adapter 只消费已经准备好的图片 URL 并编码 wire 差异。\n\n## 公共契约\n\n- `load_for_prompt_bytes` 在解码模型可见 frame 前校验编码大小、格式、尺寸、像素、解码 bytes 和\n  frame 数，然后应用 `PromptImageMode`、metadata 与 animation policy，返回 `EncodedImage`。\n- `load_data_url_for_prompt` 只接受受支持的 Base64 图片 data URL，核对声明 MIME 与实际文件签名，\n  再委托 `load_for_prompt_bytes`。\n- `PromptImagePolicy` 让安全、缩放、metadata 和 animation 选择在调用点保持显式。`Original` 只\n  关闭面向模型的主动缩放，不能绕过任何安全限制。\n- `EncodedImage` 同时报告源尺寸、准备后尺寸和源 frame 数；其 bytes 不可变且可共享，cache hit\n  不会复制大 buffer。\n- `detect_image_format` 只识别文件签名，不构成完整校验；只有 `load_for_prompt_bytes` 成功才能证明\n  图片可以在当前 policy 下安全解码。\n\n输入支持 PNG、JPEG、GIF 和 WebP。`ImageAnimationPolicy` 明确决定拒绝动画或只取第一帧；转换\n后的 GIF 输出为 PNG。只有不需要缩放、动画降帧或 metadata 清理时，PNG/JPEG/WebP 才按原 bytes\n透传。\n\n## 内部执行路径\n\n```text\nload_data_url_for_prompt\n  → Base64/MIME validation\n  → load_for_prompt_bytes\n      → validate_policy\n      → frame_count\n      → validate_decoded_shape\n      → DynamicImage::from_decoder\n      → output_dimensions\n      → encode_image (only when transformation is required)\n      → IMAGE_CACHE\n```\n\n`frame_count` 在接受输入前限制 animation 工作量；`validate_decoded_shape` 拥有尺寸、像素与解码\n内存不变量；`output_dimensions_for_limits` 拥有 32-pixel patch grid 计算；`encode_image` 和\n`apply_metadata` 是唯一转码路径。若这些 symbol 开始检查模型 capability、抓取远程资源、读取\nfilesystem 或保存 durable 数据，即表示实现所有权已经漂移。\n\nCache key 对源 bytes 和完整 `PromptImagePolicy` 计算 hash；cache 同时受 entry 数和编码 byte 数\n约束。它只是一项优化，不能改变校验结果或失败行为。\n\n## 失败与限制\n\n`ImageProcessingError` 区分 malformed data URL、MIME 不匹配、不支持格式、无效 policy、编码大小、\n尺寸、像素、解码 bytes、frame 数、animation policy、解码失败与编码失败。错误内容绝不包含图片\nbytes 或完整 data URL。\n\ncrate 的默认输入上限只是绝对 sanity guard，不是产品上传额度。调用者必须提供实际接受的 byte、\n尺寸、像素、解码内存和 frame limits。修改 patch size、支持格式、metadata 行为、cache identity\n或错误分类会影响 Core durability、provider request 和附件客户端，必须同步更新 integration test\n与文档。\n\n## 测试\n\n运行 `cargo test -p zeta-utils-image`。单元测试覆盖源 bytes 透传、缩放、patch budget、`Original`\n安全语义、data URL 校验、资源限制、animation policy、metadata 保留/清理和输出编码。Bazel target\n为 `//zeta-rs/utils/image:image-unit-tests`。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "内部执行路径",
        "title": "内部执行路径"
      },
      {
        "depth": 2,
        "id": "失败与限制",
        "title": "失败与限制"
      },
      {
        "depth": 2,
        "id": "测试",
        "title": "测试"
      }
    ],
    "searchText": "zeta-utils-image 在不可信图片 bytes 对模型可见之前，负责供应商无关的校验和转换。跨 crate 的 安全边界与 Tool output policy 以 docs/tools.md 为准；本 README 是 crate 实现契约的权威说明。 所有权 本 crate 拥有受支持格式识别、Base64 data URL 解析与生成、真实图片解码、编码与解码资源限制、 尺寸/像素/frame 校验、缩放与 patch budget 计算、ICC/EXIF 处理、静态转码以及进程内有界缓存。 它不读取文件、不抓取远程 URL、不选择模型 capability、不持久化附件、不决定产品 limits、不发送 telemetry，也不决定处理失败的图片如何出现在对话中。 zeta-core 提供 PromptImagePolicy，在 durable user/tool 图片内容记录前调用处理，并把失败映射 为产品语义。产品 host 可以使用 detectimageformat 和 dataurlfrombytes 投影本地附件，但 这些检查不是权威校验。Provider adapter 只消费已经准备好的图片 URL 并编码 wire 差异。 公共契约 - loadforpromptbytes 在解码模型可见 frame 前校验编码大小、格式、尺寸、像素、解码 bytes 和 frame 数，然后应用 PromptImageMode、metadata 与 animation policy，返回 EncodedImage。 - loaddataurlforprompt 只接受受支持的 Base64 图片 data URL，核对声明 MIME 与实际文件签名， 再委托 loadforpromptbytes。 - PromptImagePolicy 让安全、缩放、metadata 和 animation 选择在调用点保持显式。Original 只 关闭面向模型的主动缩放，不能绕过任何安全限制。 - EncodedImage 同时报告源尺寸、准备后尺寸和源 frame 数；其 bytes 不可变且可共享，cache hit 不会复制大 buffer。 - detectimageformat 只识别文件签名，不构成完整校验；只有 loadforpromptbytes 成功才能证明 图片可以在当前 policy 下安全解码。 输入支持 PNG、JPEG、GIF 和 WebP。ImageAnimationPolicy 明确决定拒绝动画或只取第一帧；转换 后的 GIF 输出为 PNG。只有不需要缩放、动画降帧或 metadata 清理时，PNG/JPEG/WebP 才按原 bytes 透传。 内部执行路径 ``text loaddataurlforprompt → Base64/MIME validation → loadforpromptbytes → validatepolicy → framecount → validatedecodedshape → DynamicImage::fromdecoder → outputdimensions → encodeimage only when transformation is required → IMAGECACHE ` framecount 在接受输入前限制 animation 工作量；validatedecodedshape 拥有尺寸、像素与解码 内存不变量；outputdimensionsforlimits 拥有 32-pixel patch grid 计算；encodeimage 和 applymetadata 是唯一转码路径。若这些 symbol 开始检查模型 capability、抓取远程资源、读取 filesystem 或保存 durable 数据，即表示实现所有权已经漂移。 Cache key 对源 bytes 和完整 PromptImagePolicy 计算 hash；cache 同时受 entry 数和编码 byte 数 约束。它只是一项优化，不能改变校验结果或失败行为。 失败与限制 ImageProcessingError 区分 malformed data URL、MIME 不匹配、不支持格式、无效 policy、编码大小、 尺寸、像素、解码 bytes、frame 数、animation policy、解码失败与编码失败。错误内容绝不包含图片 bytes 或完整 data URL。 crate 的默认输入上限只是绝对 sanity guard，不是产品上传额度。调用者必须提供实际接受的 byte、 尺寸、像素、解码内存和 frame limits。修改 patch size、支持格式、metadata 行为、cache identity 或错误分类会影响 Core durability、provider request 和附件客户端，必须同步更新 integration test 与文档。 测试 运行 cargo test -p zeta-utils-image。单元测试覆盖源 bytes 透传、缩放、patch budget、Original 安全语义、data URL 校验、资源限制、animation policy、metadata 保留/清理和输出编码。Bazel target 为 //zeta-rs/utils/image:image-unit-tests`。"
  },
  {
    "slug": "crates/utils/path-utils",
    "sourcePath": "zeta-rs/utils/path-utils/README.md",
    "title": "zeta-utils-path",
    "description": "zeta-utils-path 只处理当前 host 文件系统上的路径比较、canonical containment、symlink 写入目标解析和原子替换。 它不定义远程文件身份、workspace 授权边界或 Session 恢复策略。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是本 crate 当前实现契约的 canonical owner。跨进程文件位置 identity 的契约由\n> [`zeta-utils-path-uri`](../path-uri/README.md) 拥有；Session cwd 与文件系统系统语义仍由对应\n> `docs/*.md` 文档拥有。\n\n`zeta-utils-path` 只处理当前 host 文件系统上的路径比较、canonical containment、symlink\n写入目标解析和原子替换。\n它不定义远程文件身份、workspace 授权边界或 Session 恢复策略。\n\n## 边界与公共契约\n\n| API | 当前职责 | Failure semantics |\n| --- | --- | --- |\n| `normalize_for_path_comparison` | canonicalize existing path，并应用 WSL mount case 规则 | 路径不存在或无法 canonicalize 时返回 `io::Error` |\n| `paths_match_after_normalization` | 比较两个可能具有 symlink/host alias 的路径 | 任一路径无法规范化时退回原始 `Path` 相等 |\n| `CanonicalPathRoot::new` / `canonicalize_within` | 缓存 canonical root，并验证 existing candidate 的真实 host path containment | candidate 不可用与 canonical path 逃出 root 分别返回 `Unavailable` / `OutsideRoot` |\n| `normalize_for_native_workdir` | Windows 上移除 verbatim path 语法 | 不访问文件系统 |\n| `resolve_symlink_write_paths` | 跟随相对或绝对 symlink chain | cycle/metadata failure 返回原始 `write_path`，且 `read_path = None` |\n| `write_atomically` / `write_text_atomically` | 同目录临时文件写入、flush、rename、目录 sync | rename 前失败保留旧 destination；rename 后目录 sync 失败会在新内容已可见时返回错误 |\n\n`paths_match_after_normalization` 适合 Resume cwd 或本地 session filter，但本 crate 不决定何时\n恢复会话或提示用户。`write_atomically` 也不拥有上层配置 schema、revision 或 locking。\n\n## 文件、内部所有权与调用关系\n\n| 文件 / private symbol | Ownership |\n| --- | --- |\n| `comparison.rs::normalize_for_wsl_on` | WSL `/mnt/<drive>` ASCII case normalization |\n| `comparison.rs::is_wsl_case_insensitive_path` | 精确识别 Windows drive mount，不把普通 Linux path 当成 case-insensitive |\n| `canonical_root.rs::CanonicalPathRoot::comparison_path` | root 的 host-aware comparison identity，不改变返回给 caller 的 canonical path |\n| `environment.rs::is_wsl` | 环境变量与 `/proc/version` detection |\n| `persistence.rs::unresolved` | symlink resolution 的 conservative fallback |\n| `persistence.rs::sync_parent` | rename 后 durability checkpoint |\n\n```text\npaths_match_after_normalization\n  → normalize_for_path_comparison\n      → canonicalize\n      → normalize_for_wsl_on\n\nCanonicalPathRoot::canonicalize_within\n  → canonicalize(candidate)\n  → normalize canonical root + candidate for host comparison\n  → component-aware starts_with\n\nresolve_symlink_write_paths\n  → symlink_metadata/read_link loop\n  → resolved target | unresolved fallback\n\nwrite_atomically\n  → create parent\n  → NamedTempFile + write + sync\n  → persist(rename)\n  → sync_parent\n```\n\n如果这里开始保存 Session 状态、解析 URI、决定 workspace grant，或把 symlink 是否允许等领域\npolicy 固化进 containment primitive，表示 ownership 已经漂移。\n\n## 集成与测试\n\nconsumer 只应依赖本 crate 的公开 API，不应依赖 private module。需要跨 RPC 序列化的路径应使用\n`zeta-utils-path-uri` 或所属 protocol 的 root-relative path contract。\n\n```text\ncargo test -p zeta-utils-path\nbazel test //zeta-rs/utils/path-utils:path-utils-unit-tests\n```\n\n修改 WSL、containment、symlink 或 atomic-write failure semantics 时必须同步更新\n`path_utils_tests.rs` 和本 README。[`zeta-agent-import`](../../agent-import/README.md) 当前消费\ncanonical containment API，并在自己的测试中覆盖外部来源的 symlink 与 escape policy。\n\n## 当前限制与扩展点\n\n- Current：只比较当前 host 上可 canonicalize 的真实路径；不存在路径只做原始相等比较。\n- Current：canonical containment 跟随 symlink；是否允许 symlink 由 consumer 在调用前决定。\n- Current：atomic replace 不提供跨进程 locking，caller 必须自行拥有并发控制。\n- Current：Windows 不尝试 sync directory handle。\n- Extension point：Session cwd filter 可复用比较 API，但其产品语义不能进入本 crate。",
    "headings": [
      {
        "depth": 2,
        "id": "边界与公共契约",
        "title": "边界与公共契约"
      },
      {
        "depth": 2,
        "id": "文件内部所有权与调用关系",
        "title": "文件、内部所有权与调用关系"
      },
      {
        "depth": 2,
        "id": "集成与测试",
        "title": "集成与测试"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是本 crate 当前实现契约的 canonical owner。跨进程文件位置 identity 的契约由 zeta-utils-path-uri 拥有；Session cwd 与文件系统系统语义仍由对应 docs/.md 文档拥有。 zeta-utils-path 只处理当前 host 文件系统上的路径比较、canonical containment、symlink 写入目标解析和原子替换。 它不定义远程文件身份、workspace 授权边界或 Session 恢复策略。 边界与公共契约 API 当前职责 Failure semantics --- --- --- normalizeforpathcomparison canonicalize existing path，并应用 WSL mount case 规则 路径不存在或无法 canonicalize 时返回 io::Error pathsmatchafternormalization 比较两个可能具有 symlink/host alias 的路径 任一路径无法规范化时退回原始 Path 相等 CanonicalPathRoot::new / canonicalizewithin 缓存 canonical root，并验证 existing candidate 的真实 host path containment candidate 不可用与 canonical path 逃出 root 分别返回 Unavailable / OutsideRoot normalizefornativeworkdir Windows 上移除 verbatim path 语法 不访问文件系统 resolvesymlinkwritepaths 跟随相对或绝对 symlink chain cycle/metadata failure 返回原始 writepath，且 readpath = None writeatomically / writetextatomically 同目录临时文件写入、flush、rename、目录 sync rename 前失败保留旧 destination；rename 后目录 sync 失败会在新内容已可见时返回错误 pathsmatchafternormalization 适合 Resume cwd 或本地 session filter，但本 crate 不决定何时 恢复会话或提示用户。writeatomically 也不拥有上层配置 schema、revision 或 locking。 文件、内部所有权与调用关系 文件 / private symbol Ownership --- --- comparison.rs::normalizeforwslon WSL /mnt/<drive ASCII case normalization comparison.rs::iswslcaseinsensitivepath 精确识别 Windows drive mount，不把普通 Linux path 当成 case-insensitive canonicalroot.rs::CanonicalPathRoot::comparisonpath root 的 host-aware comparison identity，不改变返回给 caller 的 canonical path environment.rs::iswsl 环境变量与 /proc/version detection persistence.rs::unresolved symlink resolution 的 conservative fallback persistence.rs::syncparent rename 后 durability checkpoint ``text pathsmatchafternormalization → normalizeforpathcomparison → canonicalize → normalizeforwslon CanonicalPathRoot::canonicalizewithin → canonicalize candidate → normalize canonical root + candidate for host comparison → component-aware startswith resolvesymlinkwritepaths → symlinkmetadata/readlink loop → resolved target unresolved fallback writeatomically → create parent → NamedTempFile + write + sync → persist rename → syncparent ` 如果这里开始保存 Session 状态、解析 URI、决定 workspace grant，或把 symlink 是否允许等领域 policy 固化进 containment primitive，表示 ownership 已经漂移。 集成与测试 consumer 只应依赖本 crate 的公开 API，不应依赖 private module。需要跨 RPC 序列化的路径应使用 zeta-utils-path-uri 或所属 protocol 的 root-relative path contract。 `text cargo test -p zeta-utils-path bazel test //zeta-rs/utils/path-utils:path-utils-unit-tests ` 修改 WSL、containment、symlink 或 atomic-write failure semantics 时必须同步更新 pathutilstests.rs 和本 README。zeta-agent-import` 当前消费 canonical containment API，并在自己的测试中覆盖外部来源的 symlink 与 escape policy。 当前限制与扩展点 - Current：只比较当前 host 上可 canonicalize 的真实路径；不存在路径只做原始相等比较。 - Current：canonical containment 跟随 symlink；是否允许 symlink 由 consumer 在调用前决定。 - Current：atomic replace 不提供跨进程 locking，caller 必须自行拥有并发控制。 - Current：Windows 不尝试 sync directory handle。 - Extension point：Session cwd filter 可复用比较 API，但其产品语义不能进入本 crate。"
  },
  {
    "slug": "crates/utils/path-uri",
    "sourcePath": "zeta-rs/utils/path-uri/README.md",
    "title": "zeta-utils-path-uri",
    "description": "zeta-utils-path-uri 提供可序列化、跨 host 检查的绝对 file: URI。它用于需要在进程、RPC 或 remote execution boundary 上传递文件位置的 contract，不替代当前 App Server filesystem API 的 workspace-relative PathBuf，也不授予 URI 指向文件的访问权限。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 Rust `file:` URI 实现契约的 canonical owner。Desktop 通用资源 URI identity\n> 由 [`desktop/src/zeta/base/common/uri.ts`](../../../desktop/src/zeta/base/common/uri.ts) 与\n> [`resources.ts`](../../../desktop/src/zeta/base/common/resources.ts) 拥有；跨层 ownership 与\n> 当前状态见 [`docs/zeta-desktop-architecture.md`](../../../docs/zeta-desktop-architecture.md)；\n> 本机 filesystem normalization 由 [`zeta-utils-path`](../path-utils/README.md) 拥有。\n\n`zeta-utils-path-uri` 提供可序列化、跨 host 检查的绝对 `file:` URI。它用于需要在进程、RPC 或\nremote execution boundary 上传递文件位置的 contract，不替代当前 App Server filesystem API\n的 workspace-relative `PathBuf`，也不授予 URI 指向文件的访问权限。\n\n## 公共契约\n\n| API / type | 当前职责 | 明确不做 |\n| --- | --- | --- |\n| `PathUri::parse` | 校验并 canonicalize `file:` URI | 不接受其他 scheme、query、fragment、port 或 credentials |\n| `from_absolute_path` / `to_host_path` | 当前 host absolute path 的 lossless round trip | 不把 foreign Windows path 映射成 POSIX path，反之亦然 |\n| `from_native_path` | 按显式 `PathConvention` 解析 POSIX、drive 或 UNC path | 不访问文件系统 |\n| `basename` / `parent` / `ancestors` / `join` | 跨 host lexical path operations | 不解析 symlink、case alias 或 Unicode filesystem normalization |\n| `starts_with` / `relative_path_from` | 按 authority 与 URI segment 做 containment | encoded `/` 或 Windows `\\` fail closed |\n| `PathConvention` | 显式选择 POSIX/Windows grammar | 不代表一台具体机器或授权环境 |\n\nSerde 和 `TS` 将 `PathUri` 表示为 canonical URI string。Windows drive letter 统一为大写，\n`file://localhost/...` 统一为无 authority 的本地 URI。无法普通 URL 表示的 host-native path\n使用保留的 opaque URI；opaque URI 只能在相同 host convention 上恢复，且除自身相等外不参与\nlexical containment。\n\n## 文件与内部所有权\n\n| 文件 / private symbol | Ownership |\n| --- | --- |\n| `validation.rs::validated_file_url` | scheme、metadata、NUL、localhost 与 drive canonicalization |\n| `validation.rs::decode_opaque_path_uri` | opaque namespace 的 canonical base64 验证 |\n| `native.rs::parse_native_path` | 显式 POSIX/Windows absolute path parsing |\n| `native.rs::from_segments` | `.` / `..` lexical normalization 与 root anchor |\n| `native.rs::render_native_path` | foreign convention display，不依赖 current host |\n| `operations.rs::containment_segments` | encoded-separator fail-closed boundary |\n\n```text\nPathUri::parse\n  → Url::parse\n  → validated_file_url\n\nPathUri::from_native_path\n  → native::parse_native_path\n  → native::from_segments\n  → validated_file_url\n\nPathUri::starts_with / relative_path_from\n  → operations::containment_segments\n  → exact authority + segment comparison\n```\n\n如果实现开始 canonicalize 真实文件、跟随 symlink、应用 workspace grant，表示 ownership 已经\n漂移到 `zeta-utils-path`、`zeta-sandboxing` 或具体 filesystem domain。Rust `PathUri` 若开始\n接受任意 URI scheme，则必须先与 Desktop 通用 `URI` contract 统一，而不是私自扩张。\n\n## 集成\n\n- local-only API 可以继续使用 `Path`/`PathBuf`；\n- workspace-relative RPC 继续使用相对 path，由 Rust authority 执行 root confinement；\n- absolute file identity 跨 JSON/RPC、remote executor 或不同 OS 时使用 `PathUri`；\n- 收到 `PathUri` 不等于获得读写授权，consumer 必须另行验证 capability/workspace boundary；\n- domain 若需要忽略 fragment 等替代 identity，必须显式选择；`PathUri` 本身拒绝 fragment。\n\n## 测试与修改影响\n\n```text\ncargo test -p zeta-utils-path-uri\nbazel test //zeta-rs/utils/path-uri:path-uri-unit-tests\n```\n\n`path_uri_tests.rs` 覆盖 wire canonicalization、POSIX/Windows/UNC、serde、parent/join、\ncontainment、encoded separator、host round trip 与 non-UTF-8 fallback。修改 URI string\nspelling、normalization 或 serde 时，必须同步检查 Desktop `URI` fixtures 和未来 protocol\nschema；修改 containment 时还要检查 sandbox/file-system consumer 的领域测试。\n\n## 当前限制与扩展点\n\n- Current：crate 已实现但尚无 production consumer。\n- Current：只接受 `file:`，不表示 HTTP、MCP resource 或 editor fragment URI。\n- Current：lexical identity 大小写敏感；filesystem-specific case comparison 属于 consumer。\n- Current：opaque fallback 不支持 parent/join/relative traversal。\n- Extension point：remote filesystem 或 executor protocol 可以采用 `PathUri`，前提是同时定义\n  environment identity、授权、foreign-path failure 和 conversion boundary。",
    "headings": [
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 2,
        "id": "文件与内部所有权",
        "title": "文件与内部所有权"
      },
      {
        "depth": 2,
        "id": "集成",
        "title": "集成"
      },
      {
        "depth": 2,
        "id": "测试与修改影响",
        "title": "测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 Rust file: URI 实现契约的 canonical owner。Desktop 通用资源 URI identity 由 desktop/src/zeta/base/common/uri.ts 与 resources.ts 拥有；跨层 ownership 与 当前状态见 docs/zeta-desktop-architecture.md； 本机 filesystem normalization 由 zeta-utils-path 拥有。 zeta-utils-path-uri 提供可序列化、跨 host 检查的绝对 file: URI。它用于需要在进程、RPC 或 remote execution boundary 上传递文件位置的 contract，不替代当前 App Server filesystem API 的 workspace-relative PathBuf，也不授予 URI 指向文件的访问权限。 公共契约 API / type 当前职责 明确不做 --- --- --- PathUri::parse 校验并 canonicalize file: URI 不接受其他 scheme、query、fragment、port 或 credentials fromabsolutepath / tohostpath 当前 host absolute path 的 lossless round trip 不把 foreign Windows path 映射成 POSIX path，反之亦然 fromnativepath 按显式 PathConvention 解析 POSIX、drive 或 UNC path 不访问文件系统 basename / parent / ancestors / join 跨 host lexical path operations 不解析 symlink、case alias 或 Unicode filesystem normalization startswith / relativepathfrom 按 authority 与 URI segment 做 containment encoded / 或 Windows \\ fail closed PathConvention 显式选择 POSIX/Windows grammar 不代表一台具体机器或授权环境 Serde 和 TS 将 PathUri 表示为 canonical URI string。Windows drive letter 统一为大写， file://localhost/... 统一为无 authority 的本地 URI。无法普通 URL 表示的 host-native path 使用保留的 opaque URI；opaque URI 只能在相同 host convention 上恢复，且除自身相等外不参与 lexical containment。 文件与内部所有权 文件 / private symbol Ownership --- --- validation.rs::validatedfileurl scheme、metadata、NUL、localhost 与 drive canonicalization validation.rs::decodeopaquepathuri opaque namespace 的 canonical base64 验证 native.rs::parsenativepath 显式 POSIX/Windows absolute path parsing native.rs::fromsegments . / .. lexical normalization 与 root anchor native.rs::rendernativepath foreign convention display，不依赖 current host operations.rs::containmentsegments encoded-separator fail-closed boundary ``text PathUri::parse → Url::parse → validatedfileurl PathUri::fromnativepath → native::parsenativepath → native::fromsegments → validatedfileurl PathUri::startswith / relativepathfrom → operations::containmentsegments → exact authority + segment comparison ` 如果实现开始 canonicalize 真实文件、跟随 symlink、应用 workspace grant，表示 ownership 已经 漂移到 zeta-utils-path、zeta-sandboxing 或具体 filesystem domain。Rust PathUri 若开始 接受任意 URI scheme，则必须先与 Desktop 通用 URI contract 统一，而不是私自扩张。 集成 - local-only API 可以继续使用 Path/PathBuf； - workspace-relative RPC 继续使用相对 path，由 Rust authority 执行 root confinement； - absolute file identity 跨 JSON/RPC、remote executor 或不同 OS 时使用 PathUri； - 收到 PathUri 不等于获得读写授权，consumer 必须另行验证 capability/workspace boundary； - domain 若需要忽略 fragment 等替代 identity，必须显式选择；PathUri 本身拒绝 fragment。 测试与修改影响 `text cargo test -p zeta-utils-path-uri bazel test //zeta-rs/utils/path-uri:path-uri-unit-tests ` pathuritests.rs 覆盖 wire canonicalization、POSIX/Windows/UNC、serde、parent/join、 containment、encoded separator、host round trip 与 non-UTF-8 fallback。修改 URI string spelling、normalization 或 serde 时，必须同步检查 Desktop URI fixtures 和未来 protocol schema；修改 containment 时还要检查 sandbox/file-system consumer 的领域测试。 当前限制与扩展点 - Current：crate 已实现但尚无 production consumer。 - Current：只接受 file:，不表示 HTTP、MCP resource 或 editor fragment URI。 - Current：lexical identity 大小写敏感；filesystem-specific case comparison 属于 consumer。 - Current：opaque fallback 不支持 parent/join/relative traversal。 - Extension point：remote filesystem 或 executor protocol 可以采用 PathUri`，前提是同时定义 environment identity、授权、foreign-path failure 和 conversion boundary。"
  },
  {
    "slug": "crates/utils/pty",
    "sourcePath": "zeta-rs/utils/pty/README.md",
    "title": "zeta-utils-pty",
    "description": "zeta-utils-pty 为 Zeta 提供统一的 interactive PTY、non-interactive pipe 与 externally-driven process handle。它拥有 process spawn plumbing、stdin/output/exit channels、resize、interrupt 和 best-effort pr…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 解释 PTY/pipe process adapter、channel lifecycle 与跨平台 kill-tree 语义。\n> Source provenance 与第三方许可见 [`NOTICE`](NOTICE)。\n\n`zeta-utils-pty` 为 Zeta 提供统一的 interactive PTY、non-interactive pipe 与 externally-driven\nprocess handle。它拥有 process spawn plumbing、stdin/output/exit channels、resize、interrupt 和\nbest-effort process-tree cleanup。\n\n它不拥有 Tool authorization、sandbox policy、command allow-list、timeout、output persistence 或\nAgent execution lifecycle。\n\n## 来源边界\n\nPTY integration 基于 `NOTICE` 中固定的 OpenAI Codex revision 并在 Zeta 内适配，Apache-2.0。\n`src/win/` 的 ConPTY 部分来自 WezTerm，MIT license 位于\n`third_party/wezterm/LICENSE`。这是 source reuse，不是 Codex wire protocol/runtime dependency。\n\n同步上游实现时必须保留 provenance，并按 Zeta 当前 public contract、process-group tests 和 Windows\ntests 审查行为；不能用“来自上游”替代本地 correctness review。\n\n## 公共契约\n\n### Spawn 与统一结果\n\n| Symbol | 职责 | 当前语义 |\n| --- | --- | --- |\n| `spawn_pty_process` | interactive PTY spawn | stdout/stderr 是 terminal 合并流；支持 resize |\n| `spawn_pipe_process` | stdin/stdout/stderr pipes | stdout 与 stderr 分离 |\n| `spawn_pipe_process_no_stdin` | stdin 立即关闭的 pipe spawn | 适合不接收输入的 child |\n| `SpawnedProcess` | `session + stdout_rx + stderr_rx + exit_rx` | 三种 spawn path 的统一 result |\n| `ProcessHandle` | input、state、resize、signal、terminate | Drop 会 hard terminate + abort helper tasks |\n| `ProcessSignal::Interrupt` | cooperative interrupt request | Unix 发 SIGINT；unsupported backend 返回 error |\n| `TerminalSize` | rows/cols | 默认 24 × 80 |\n| `combine_output_receivers` | split mpsc → one broadcast stream | 不保证 stdout/stderr total order |\n\n`spawn_*` 参数包括 program、args、cwd、完整 env map、optional arg0 与 Unix inherited FDs；PTY 额外\n接收 `TerminalSize`。Spawn 前 `env_clear`，因此 child 只看到 caller 显式提供的 environment。\n\n### 外部驱动适配器\n\n| Symbol | 职责 | Implementation obligation |\n| --- | --- | --- |\n| `ProcessDriver` | 接入已有 stdin/output/exit backend | output sender 必须在 final bytes 后关闭 |\n| `spawn_from_driver` | 转成 standard `SpawnedProcess` | exit signal 后继续 drain 到 broadcast close |\n| `ProcessHandle::resize` | local PTY 或 driver resizer | pipe/no-resizer 返回 error |\n| `ProcessHandle::writer_sender` | clone raw-byte stdin sender | handle closed 后返回 disconnected sender |\n| `ProcessHandle::close_stdin` | drop owned stdin sender | 已 clone sender 仍可保持 channel alive |\n| `ProcessHandle::request_terminate` | kill child、保留 I/O tasks 以 drain EOF | killer 只消费一次 |\n| `ProcessHandle::terminate` | kill child并 abort reader/writer/wait tasks | 不保证剩余 output drain |\n| `ProcessHandle::{has_exited,exit_code}` | non-blocking observed exit state | `exit_rx` 是 authoritative completion notification |\n| `ProcessHandle::release_pty_handles_after_exit` | authoritative exit 后释放 parent-held PTY/ConPTY handles | 运行中调用是 no-op；允许 reader 在 final bytes 后观察 EOF |\n\n`ExecCommandSession` 与 `SpawnedPty` 是 backward-compatible type aliases。新增代码应优先使用\n`ProcessHandle` 与 `SpawnedProcess` 的真实名称。\n\n`DEFAULT_OUTPUT_BYTES_CAP` 当前是 public 1 MiB constant，但 crate 内没有读取或 enforce 它。Output\nchannels 以 chunk 数 bounded；总 captured bytes、truncation 与 persistence 必须由 consumer 实施。\n不要把这个常量写成现有 hard limit。\n\n## 内部接口地图\n\n| Symbol | 可见性 | 当前职责 | 方向约束 |\n| --- | --- | --- | --- |\n| `ChildTerminator` | crate-private trait | backend-specific interrupt/hard kill | 与 helper-task abortion 分离 |\n| `PipeChildTerminator` | private | Unix process group 或 Windows job/process kill | pipe backend 不泄漏 OS handle |\n| `PtyChildTerminator` / `RawPidTerminator` | private | PTY child/process-group control | interactive descendants 必须一起 cleanup |\n| `ProcessHandle::new` | crate-private | 组装 writer、killer、tasks、exit state、PTY handles | 所有 spawn paths 共享 lifecycle |\n| `PtyMasterHandle` | crate-private | portable resizable 或 raw-FD PTY master | raw handle 必须存活到 session drop |\n| `read_output_stream` | private async fn | 8 KiB pipe reads → bounded mpsc | stdout/stderr reader 独立 drain |\n| `spawn_process_with_stdin_mode` | private async fn | pipe command + containment + channel/tasks | public pipe variants 的唯一 spawn path |\n| `spawn_process_portable` | private async fn | portable-pty/ConPTY path | 无 inherited FDs 的常规 PTY |\n| `spawn_process_preserving_fds` | private async fn, Unix | raw openpty + setsid + controlling TTY | inherited FD contract 不走 portable path |\n| `close_inherited_fds_except` | crate-private, Unix | exec 前关闭非 stdio/non-preserved non-CLOEXEC FDs | 保留 stdio、explicit FDs 与 exec-error pipe |\n| `exit_code_from_status` | crate-private | exit code；Unix signal → `128 + signal`；unknown `-1` | 所有 wait paths使用一致编码 |\n| `ClosureTerminator` | private | driver closure → hard-kill adapter | driver signal 当前 unsupported |\n| `WindowsTtyInputNormalizer` | Windows-only public | LF→CR、CRLF collapse、backspace→DEL | state 跨 write chunk 保留 |\n| process-group helpers | public module | detach、pdeathsig、SIGINT/SIGTERM/SIGKILL group operations | non-Unix 多数为 no-op |\n\n## Pipe spawn 调用图\n\n```text\nspawn_pipe_process / spawn_pipe_process_no_stdin\n└─ spawn_process_with_stdin_mode\n   ├─ validate non-empty program\n   ├─ Command::new + cwd + env_clear + args\n   ├─ Unix pre_exec\n   │  ├─ detach_from_tty\n   │  ├─ set_parent_death_signal [Linux]\n   │  └─ close_inherited_fds_except\n   ├─ Windows JobObject assignment [best effort]\n   ├─ spawn child\n   ├─ stdin writer task\n   ├─ stdout/stderr read_output_stream tasks\n   ├─ wait task → exit code/state/oneshot\n   └─ ProcessHandle::new(PipeChildTerminator, tasks...)\n```\n\nUnix pipe child 进入独立 session/process group；terminate/interrupt 作用于 group。Linux\n`set_parent_death_signal` 在 `pre_exec` 设置 SIGTERM 并复查 parent PID，降低 fork/exec race。\n\nWindows pipe backend 在 process spawn 后分配 Job Object，因此 child 在 assignment 前创建 descendant\n时存在逃逸 race。Assignment 失败会 fallback 到只终止 root process。ConPTY path 的 containment\n与 pipe path 不同，相关保证必须按 platform test 而不是统一假设。\n\n## PTY spawn 调用图\n\n```text\nspawn_pty_process\n├─ validate non-empty program\n├─ inherited_fds empty / non-Unix\n│  └─ spawn_process_portable\n│     ├─ platform_native_pty_system\n│     ├─ openpty(size)\n│     ├─ CommandBuilder + env_clear\n│     ├─ spawn child\n│     ├─ blocking PTY reader + async writer\n│     ├─ blocking wait\n│     └─ ProcessHandle::new(PtyChildTerminator, PtyHandles...)\n└─ inherited_fds non-empty [Unix]\n   └─ spawn_process_preserving_fds\n      ├─ open_unix_pty + CLOEXEC\n      ├─ std::process::Command\n      ├─ pre_exec: reset signals, setsid, TIOCSCTTY, close FDs\n      ├─ raw master reader/writer + wait tasks\n      └─ ProcessHandle::new(RawPidTerminator, opaque raw master...)\n```\n\nPTY stdout 与 stderr 指向同一个 terminal slave，因此 result 的 `stderr_rx` 不承载独立 PTY stderr。\nCaller 如果需要 split stderr，应选择 pipe backend。\n\nWindows portable writer 在写入 ConPTY 前经过 `WindowsTtyInputNormalizer`。Normalizer 维护\n`previous_was_cr`，所以 CR 和下一 chunk 的 LF 也只生成一个 carriage return。\n\nWindows portable path 在 child 运行期间保留 slave/master pseudoconsole handles，避免过早关闭\n改变前台输入语义。消费 `exit_rx` 后，owner 应调用\n`ProcessHandle::release_pty_handles_after_exit`；该方法先检查 `has_exited`，再释放 parent-held\nhandles，使 reader 在排空 ConPTY tail 后收到 EOF。只观察 exit code 而不释放 handles，会让\n依赖 output-close 的上层 terminal lifecycle 无法收束。\n\n## 驱动与输出生命周期\n\n`spawn_from_driver` 把 broadcast output 转成 capacity-256 mpsc。它收到 exit code后只标记\n`has_exited/exit_code`，仍等待 driver 的 stdout/stderr broadcast sender关闭，避免丢失 exit signal\n之后到达的 tail output。\n\n因此 driver contract 是：\n\n```text\npublish final output\n→ drop/close output senders\n→ exit lifecycle may fully settle\n```\n\n若 sender 永不关闭，reader task 会一直等待，直到 `ProcessHandle::terminate`/Drop abort。若 broadcast\nreceiver发生 `Lagged`，当前 adapter 会跳过丢失的 chunks，没有 error 或 gap signal。需要 lossless\noutput 的 caller 不应让 producer 超过消费能力。\n\n`combine_output_receivers` 也使用 capacity-256 broadcast；select 顺序只反映运行时可用性，不构造\nstdout/stderr 的全局 byte order。\n\n## 终止语义\n\n```text\nrequest_terminate\n├─ take ChildTerminator once\n└─ hard kill child/tree\n   └─ keep read/write/wait tasks alive for drain\n\nterminate / Drop\n├─ request_terminate\n├─ abort primary reader\n├─ abort detached stdout/stderr readers\n├─ abort writer\n└─ abort wait task\n```\n\n`signal(Interrupt)` 是 cooperative signal；`terminate` 是 hard cleanup。Killer 已被\n`request_terminate` 消费后，后续 signal/terminate 是 best effort no-op。Caller 需要 graceful\ndeadline 时应先 signal，再等待 `exit_rx`，最后 terminate；本 crate 不提供 timer。\n\n## 方向偏差检查\n\n- Tool command/policy/sandbox 进入 spawn API：utility 获得 execution authority；\n- Pipe/PTY 各自实现一套 `ProcessHandle` lifecycle：cleanup semantics 分叉；\n- 只 kill root PID、不测试 descendants：process tree 泄漏；\n- 把 `DEFAULT_OUTPUT_BYTES_CAP` 当作已 enforce：内存/输出安全假设错误；\n- PTY caller 期待 split stderr：terminal stream contract 被误读；\n- Driver 在 final output 前关闭 sender：tail output 被丢弃；\n- Driver sender 永不关闭：exit 后 reader task 泄漏；\n- Public API 暴露 portable-pty/Tokio child/Windows raw handles（Windows interop exports除外）；\n- 修改 `src/win/` 不维护 WezTerm attribution；\n- Non-Unix no-op process-group helper 被上层当作 containment guarantee。\n\n## 测试、限制与演进\n\n```text\ncargo test -p zeta-utils-pty\nbazel test //zeta-rs/utils/pty:pty-unit-tests\n```\n\nCross-platform tests 覆盖 PTY Python REPL、pipe stdin、session detach、统一 result、split stderr、\ndriver resize/tail drain、terminate reader abort、Unix descendant kill、inherited FDs、spawn failure 与\nresize。Windows-only tests 覆盖 job/ConPTY descendant behavior、foreground input、Ctrl-C 与 input\nnormalization。\n\n部分 integration tests 依赖 Python、`setsid` 或特定 OS capability，会在环境不满足时 skip。当前\nUnix inherited-FD cases 还要求 child 能打开 `/dev/fd/<n>`；禁止该访问的 managed sandbox 会使\nchild 以非零状态退出，即使 FD preservation path 本身已经执行。\n\n当前\n没有 cancellation token、deadline、output byte cap enforcement、structured spawn error taxonomy、\nsandbox integration 或 lossless broadcast gap reporting。未来能力必须继续由上层拥有 policy，\n本 crate只负责可测试的 process/terminal mechanism。",
    "headings": [
      {
        "depth": 2,
        "id": "来源边界",
        "title": "来源边界"
      },
      {
        "depth": 2,
        "id": "公共契约",
        "title": "公共契约"
      },
      {
        "depth": 3,
        "id": "spawn-与统一结果",
        "title": "Spawn 与统一结果"
      },
      {
        "depth": 3,
        "id": "外部驱动适配器",
        "title": "外部驱动适配器"
      },
      {
        "depth": 2,
        "id": "内部接口地图",
        "title": "内部接口地图"
      },
      {
        "depth": 2,
        "id": "pipe-spawn-调用图",
        "title": "Pipe spawn 调用图"
      },
      {
        "depth": 2,
        "id": "pty-spawn-调用图",
        "title": "PTY spawn 调用图"
      },
      {
        "depth": 2,
        "id": "驱动与输出生命周期",
        "title": "驱动与输出生命周期"
      },
      {
        "depth": 2,
        "id": "终止语义",
        "title": "终止语义"
      },
      {
        "depth": 2,
        "id": "方向偏差检查",
        "title": "方向偏差检查"
      },
      {
        "depth": 2,
        "id": "测试限制与演进",
        "title": "测试、限制与演进"
      }
    ],
    "searchText": " 本 README 解释 PTY/pipe process adapter、channel lifecycle 与跨平台 kill-tree 语义。 Source provenance 与第三方许可见 NOTICE。 zeta-utils-pty 为 Zeta 提供统一的 interactive PTY、non-interactive pipe 与 externally-driven process handle。它拥有 process spawn plumbing、stdin/output/exit channels、resize、interrupt 和 best-effort process-tree cleanup。 它不拥有 Tool authorization、sandbox policy、command allow-list、timeout、output persistence 或 Agent execution lifecycle。 来源边界 PTY integration 基于 NOTICE 中固定的 OpenAI Codex revision 并在 Zeta 内适配，Apache-2.0。 src/win/ 的 ConPTY 部分来自 WezTerm，MIT license 位于 thirdparty/wezterm/LICENSE。这是 source reuse，不是 Codex wire protocol/runtime dependency。 同步上游实现时必须保留 provenance，并按 Zeta 当前 public contract、process-group tests 和 Windows tests 审查行为；不能用“来自上游”替代本地 correctness review。 公共契约 Spawn 与统一结果 Symbol 职责 当前语义 --- --- --- spawnptyprocess interactive PTY spawn stdout/stderr 是 terminal 合并流；支持 resize spawnpipeprocess stdin/stdout/stderr pipes stdout 与 stderr 分离 spawnpipeprocessnostdin stdin 立即关闭的 pipe spawn 适合不接收输入的 child SpawnedProcess session + stdoutrx + stderrrx + exitrx 三种 spawn path 的统一 result ProcessHandle input、state、resize、signal、terminate Drop 会 hard terminate + abort helper tasks ProcessSignal::Interrupt cooperative interrupt request Unix 发 SIGINT；unsupported backend 返回 error TerminalSize rows/cols 默认 24 × 80 combineoutputreceivers split mpsc → one broadcast stream 不保证 stdout/stderr total order spawn 参数包括 program、args、cwd、完整 env map、optional arg0 与 Unix inherited FDs；PTY 额外 接收 TerminalSize。Spawn 前 envclear，因此 child 只看到 caller 显式提供的 environment。 外部驱动适配器 Symbol 职责 Implementation obligation --- --- --- ProcessDriver 接入已有 stdin/output/exit backend output sender 必须在 final bytes 后关闭 spawnfromdriver 转成 standard SpawnedProcess exit signal 后继续 drain 到 broadcast close ProcessHandle::resize local PTY 或 driver resizer pipe/no-resizer 返回 error ProcessHandle::writersender clone raw-byte stdin sender handle closed 后返回 disconnected sender ProcessHandle::closestdin drop owned stdin sender 已 clone sender 仍可保持 channel alive ProcessHandle::requestterminate kill child、保留 I/O tasks 以 drain EOF killer 只消费一次 ProcessHandle::terminate kill child并 abort reader/writer/wait tasks 不保证剩余 output drain ProcessHandle:: hasexited,exitcode non-blocking observed exit state exitrx 是 authoritative completion notification ProcessHandle::releaseptyhandlesafterexit authoritative exit 后释放 parent-held PTY/ConPTY handles 运行中调用是 no-op；允许 reader 在 final bytes 后观察 EOF ExecCommandSession 与 SpawnedPty 是 backward-compatible type aliases。新增代码应优先使用 ProcessHandle 与 SpawnedProcess 的真实名称。 DEFAULTOUTPUTBYTESCAP 当前是 public 1 MiB constant，但 crate 内没有读取或 enforce 它。Output channels 以 chunk 数 bounded；总 captured bytes、truncation 与 persistence 必须由 consumer 实施。 不要把这个常量写成现有 hard limit。 内部接口地图 Symbol 可见性 当前职责 方向约束 --- --- --- --- ChildTerminator crate-private trait backend-specific interrupt/hard kill 与 helper-task abortion 分离 PipeChildTerminator private Unix process group 或 Windows job/process kill pipe backend 不泄漏 OS handle PtyChildTerminator / RawPidTerminator private PTY child/process-group control interactive descendants 必须一起 cleanup ProcessHandle::new crate-private 组装 writer、killer、tasks、exit state、PTY handles 所有 spawn paths 共享 lifecycle PtyMasterHandle crate-private portable resizable 或 raw-FD PTY master raw handle 必须存活到 session drop readoutputstream private async fn 8 KiB pipe reads → bounded mpsc stdout/stderr reader 独立 drain spawnprocesswithstdinmode private async fn pipe command + containment + channel/tasks public pipe variants 的唯一 spawn path spawnprocessportable private async fn portable-pty/ConPTY path 无 inherited FDs 的常规 PTY spawnprocesspreservingfds private async fn, Unix raw openpty + setsid + controlling TTY inherited FD contract 不走 portable path closeinheritedfdsexcept crate-private, Unix exec 前关闭非 stdio/non-preserved non-CLOEXEC FDs 保留 stdio、explicit FDs 与 exec-error pipe exitcodefromstatus crate-private exit code；Unix signal → 128 + signal；unknown -1 所有 wait paths使用一致编码 ClosureTerminator private driver closure → hard-kill adapter driver signal 当前 unsupported WindowsTtyInputNormalizer Windows-only public LF→CR、CRLF collapse、backspace→DEL state 跨 write chunk 保留 process-group helpers public module detach、pdeathsig、SIGINT/SIGTERM/SIGKILL group operations non-Unix 多数为 no-op Pipe spawn 调用图 ``text spawnpipeprocess / spawnpipeprocessnostdin └─ spawnprocesswithstdinmode ├─ validate non-empty program ├─ Command::new + cwd + envclear + args ├─ Unix preexec │ ├─ detachfromtty │ ├─ setparentdeathsignal Linux │ └─ closeinheritedfdsexcept ├─ Windows JobObject assignment best effort ├─ spawn child ├─ stdin writer task ├─ stdout/stderr readoutputstream tasks ├─ wait task → exit code/state/oneshot └─ ProcessHandle::new PipeChildTerminator, tasks... ` Unix pipe child 进入独立 session/process group；terminate/interrupt 作用于 group。Linux setparentdeathsignal 在 preexec 设置 SIGTERM 并复查 parent PID，降低 fork/exec race。 Windows pipe backend 在 process spawn 后分配 Job Object，因此 child 在 assignment 前创建 descendant 时存在逃逸 race。Assignment 失败会 fallback 到只终止 root process。ConPTY path 的 containment 与 pipe path 不同，相关保证必须按 platform test 而不是统一假设。 PTY spawn 调用图 `text spawnptyprocess ├─ validate non-empty program ├─ inheritedfds empty / non-Unix │ └─ spawnprocessportable │ ├─ platformnativeptysystem │ ├─ openpty size │ ├─ CommandBuilder + envclear │ ├─ spawn child │ ├─ blocking PTY reader + async writer │ ├─ blocking wait │ └─ ProcessHandle::new PtyChildTerminator, PtyHandles... └─ inheritedfds non-empty Unix └─ spawnprocesspreservingfds ├─ openunixpty + CLOEXEC ├─ std::process::Command ├─ preexec: reset signals, setsid, TIOCSCTTY, close FDs ├─ raw master reader/writer + wait tasks └─ ProcessHandle::new RawPidTerminator, opaque raw master... ` PTY stdout 与 stderr 指向同一个 terminal slave，因此 result 的 stderrrx 不承载独立 PTY stderr。 Caller 如果需要 split stderr，应选择 pipe backend。 Windows portable writer 在写入 ConPTY 前经过 WindowsTtyInputNormalizer。Normalizer 维护 previouswascr，所以 CR 和下一 chunk 的 LF 也只生成一个 carriage return。 Windows portable path 在 child 运行期间保留 slave/master pseudoconsole handles，避免过早关闭 改变前台输入语义。消费 exitrx 后，owner 应调用 ProcessHandle::releaseptyhandlesafterexit；该方法先检查 hasexited，再释放 parent-held handles，使 reader 在排空 ConPTY tail 后收到 EOF。只观察 exit code 而不释放 handles，会让 依赖 output-close 的上层 terminal lifecycle 无法收束。 驱动与输出生命周期 spawnfromdriver 把 broadcast output 转成 capacity-256 mpsc。它收到 exit code后只标记 hasexited/exitcode，仍等待 driver 的 stdout/stderr broadcast sender关闭，避免丢失 exit signal 之后到达的 tail output。 因此 driver contract 是： `text publish final output → drop/close output senders → exit lifecycle may fully settle ` 若 sender 永不关闭，reader task 会一直等待，直到 ProcessHandle::terminate/Drop abort。若 broadcast receiver发生 Lagged，当前 adapter 会跳过丢失的 chunks，没有 error 或 gap signal。需要 lossless output 的 caller 不应让 producer 超过消费能力。 combineoutputreceivers 也使用 capacity-256 broadcast；select 顺序只反映运行时可用性，不构造 stdout/stderr 的全局 byte order。 终止语义 `text requestterminate ├─ take ChildTerminator once └─ hard kill child/tree └─ keep read/write/wait tasks alive for drain terminate / Drop ├─ requestterminate ├─ abort primary reader ├─ abort detached stdout/stderr readers ├─ abort writer └─ abort wait task ` signal Interrupt 是 cooperative signal；terminate 是 hard cleanup。Killer 已被 requestterminate 消费后，后续 signal/terminate 是 best effort no-op。Caller 需要 graceful deadline 时应先 signal，再等待 exitrx，最后 terminate；本 crate 不提供 timer。 方向偏差检查 - Tool command/policy/sandbox 进入 spawn API：utility 获得 execution authority； - Pipe/PTY 各自实现一套 ProcessHandle lifecycle：cleanup semantics 分叉； - 只 kill root PID、不测试 descendants：process tree 泄漏； - 把 DEFAULTOUTPUTBYTESCAP 当作已 enforce：内存/输出安全假设错误； - PTY caller 期待 split stderr：terminal stream contract 被误读； - Driver 在 final output 前关闭 sender：tail output 被丢弃； - Driver sender 永不关闭：exit 后 reader task 泄漏； - Public API 暴露 portable-pty/Tokio child/Windows raw handles（Windows interop exports除外）； - 修改 src/win/ 不维护 WezTerm attribution； - Non-Unix no-op process-group helper 被上层当作 containment guarantee。 测试、限制与演进 `text cargo test -p zeta-utils-pty bazel test //zeta-rs/utils/pty:pty-unit-tests ` Cross-platform tests 覆盖 PTY Python REPL、pipe stdin、session detach、统一 result、split stderr、 driver resize/tail drain、terminate reader abort、Unix descendant kill、inherited FDs、spawn failure 与 resize。Windows-only tests 覆盖 job/ConPTY descendant behavior、foreground input、Ctrl-C 与 input normalization。 部分 integration tests 依赖 Python、setsid 或特定 OS capability，会在环境不满足时 skip。当前 Unix inherited-FD cases 还要求 child 能打开 /dev/fd/<n `；禁止该访问的 managed sandbox 会使 child 以非零状态退出，即使 FD preservation path 本身已经执行。 当前 没有 cancellation token、deadline、output byte cap enforcement、structured spawn error taxonomy、 sandbox integration 或 lossless broadcast gap reporting。未来能力必须继续由上层拥有 policy， 本 crate只负责可测试的 process/terminal mechanism。"
  },
  {
    "slug": "crates/zeterm/wgpu",
    "sourcePath": "zeterm/wgpu/README.md",
    "title": "zeta-wgpu",
    "description": "zeta-wgpu 拥有一个 native window 的 wgpu presentation 资源，并把可选的 immutable zui::UiScene 组合进同一 render pass。它不知道 App Server、Session、Thread、Agent、 Workbench 或其他产品状态。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责 wgpu backend 的当前实现与失败语义。跨 backend 契约见\n> [`zeta-renderer`](../renderer/README.md)，系统替换规则见\n> [`docs/rendering-architecture.md`](../docs/rendering-architecture.md)，scene 语义见\n> [`zui`](../zui/README.md)。\n\n`zeta-wgpu` 拥有一个 native window 的 `wgpu` presentation 资源，并把可选的 immutable\n`zui::UiScene` 组合进同一 render pass。它不知道 App Server、Session、Thread、Agent、\nWorkbench 或其他产品状态。\n\n## 1. 所有权\n\n| 能力 | 本 crate | 委托/不拥有 |\n| --- | --- | --- |\n| Instance、adapter、device、queue、surface、configuration | ✅ | |\n| Frame acquire、clear、submit、present | ✅ | |\n| Resize、零尺寸与 surface lost recovery | ✅ | |\n| UI scene/paint/font/text semantics | ❌ | `zui` |\n| Rect/image/icon/text GPU pipelines、atlas 与 glyph resources | ✅ | |\n| Backend-neutral frame contract | ❌ | `zeta-renderer::Renderer` |\n| Event loop、窗口策略 | ❌ | `zeta-winit` / product host |\n| Element/layout 与 text-input 基座 | ❌ | `zui` |\n| Widget、input routing、IME adapter、accessibility | ❌ | `zeta-ui` / product host / platform adapter |\n| App Server、workspace、durable state | ❌ | 上层产品 |\n\n依赖方向：\n\n```text\nnative product host\n  ├─→ zeta-winit → winit\n  ├─→ zeta-ui → zui\n  ├─→ zeta-renderer → zui\n  └─→ zeta-wgpu → zeta-renderer\n                  → zeta-winit\n                  → zui\n                  → wgpu\n```\n\n出现 App Server method、product command、workspace model、widget state 或 font-platform\nimplementation，意味着 crate ownership 已经漂移。\n\n## 2. 公共与内部接口\n\n| Symbol | 可见性 | 职责 | 不能承担 |\n| --- | --- | --- | --- |\n| `WgpuRenderer` | public | 一个 window 的 GPU/surface 与 UI GPU pipeline ownership，并实现 `Renderer` | event loop、产品状态 |\n| `WgpuRenderer::initialize` | public | `NativeWindow` → surface/adapter/device/config/UI pipeline | App Server startup |\n| `WgpuRenderer::resize` | public | 保存 physical extent 并在非零时 configure | layout 计算 |\n| `WgpuRenderer::set_scale_factor` | public | 保存平台 DPI 事实 | logical layout policy |\n| `WgpuRenderer::request_redraw` | public | 转发 owned native window 的 redraw 请求 | 动画或 invalidation policy |\n| `WgpuRenderer::render` | public | 无 scene 的稳定 clear/present 路径 | UI state mutation |\n| `WgpuRenderer::render_scene` | public | prepare UI、clear scene background、draw、present | 构造或布局 scene |\n| `zeta_renderer::RenderOutcome` | re-export | 告知 host presented/skipped/retry | 自行调度 event loop |\n| `WgpuRendererError` | public | surface/device/UI render failure | 产品级恢复策略 |\n| `ui_renderer::UiRenderer` | private | 协调 rect/image/icon/text prepare，并严格按 `UiScene::batches` 执行 | scene/layout/paint-order 语义 |\n| `ui_renderer::CachedTextBuffer` | private | 按 Scene 文本槽位复用 shape-affecting 内容未变化的 glyphon Buffer；位置、clip 与普通文本颜色变化只更新 TextArea | 跨组件 identity、无限历史缓存 |\n| `ui_renderer::{rect,image,icon}` | private | instance conversion、validation、atlas、shader 与 primitive-to-instance range mapping | component state |\n| `ui_renderer::UiRenderError` | public | 区分 invalid scene、atlas、glyph prepare/render failure | surface recovery |\n| `viewport::Viewport` | private | physical extent 与 scale-factor 状态 | window/GPU handle |\n| `WgpuRenderer::render_frame` | private | 统一 clear-only 与 scene frame 顺序 | 业务 invalidation |\n| `wgpu_color` | private | sRGB background → linear `wgpu::Color` | glyph color mapping |\n\n## 3. 执行路径与接口面语义\n\n```text\nproduct-owned ApplicationHandler\n  → NativeWindow::create\n  → WgpuRenderer::initialize\n  → Box<dyn zeta_renderer::Renderer>\n  → resize / set_scale_factor\n  → render 或 render_scene\n      → WgpuRenderer::render_frame\n          → Surface::get_current_texture\n          → UiRenderer::prepare (scene primitive → backend resource/range)\n              → refresh_text_buffer_cache (reuse unchanged shaping buffers)\n          → clear background\n          → UiRenderer::render (ordered SceneBatch execution)\n          → Queue::submit\n          → NativeWindow::pre_present_notify\n          → Queue::present\n          → UiRenderer::trim (scene only)\n```\n\n`Viewport::surface_extent()` 对任一零维返回 `None`，因此最小化期间不会提交零尺寸 surface\nconfiguration。UI prepare 在 frame acquire 后、command encoding 前发生；prepare 失败时 frame\n不会 submit/present。\n\n| `CurrentSurfaceTexture` | `zeta-wgpu` 行为 | Host 行为 |\n| --- | --- | --- |\n| `Success` | clear、可选 UI draw、submit、present | 无 |\n| `Timeout` / `Occluded` | 返回 `Skipped` | 等下一次 invalidation |\n| `Outdated` / `Suboptimal` | configure，返回 `Retry` | 请求一次 redraw |\n| `Lost` | 重建 surface，返回 `Retry` | 请求一次 redraw |\n| `Validation` | 返回 `SurfaceValidation` | 产品决定退出或恢复 |\n\nrenderer 从不主动 busy-loop。动画、timer、App Server update 与 UI invalidation 均由上层 host\n产生。\n\n## 4. 测试\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-wgpu\nbazel test //zeterm/wgpu:wgpu-unit-tests\n```\n\n单元测试覆盖 viewport 状态、sRGB conversion、scene primitive validation、instance conversion、\nicon raster 和 atlas allocation，不创建窗口或 GPU。普通 CI 编译\n不能证明 Metal、DX12 或 Vulkan presentation 可用；真实 surface 与 glyph output smoke 由产品\nbinary 在各平台执行。\n\n## 5. 修改路径与当前限制\n\n- surface API/恢复策略：同步检查 `gpu.rs`、`viewport_tests.rs` 和本 README；\n- scene/text contract：修改 `zui`，不要在 backend 中复制组件或产品语义；\n- 新 paint primitive：先在 `zui` 建立 scene contract，再在 `ui_renderer` 增加 backend 实现；\n- 通用 frame outcome 或 host 接口：修改 `zeta-renderer`，不要把 wgpu 类型加入 trait；\n- window/event-loop policy：属于 `zeta-winit` 和 product host；\n- App Server/product 功能：禁止在本 crate 接入。\n\n当前限制：\n\n- 一个 `WgpuRenderer` 只服务一个 window/surface；\n- 没有 frame telemetry、device-lost migration 或显式 pipeline warmup；\n- 没有 headless GPU test、golden image 或产品 host vertical；\n- multicolor icon 栅格当前把纯黑像素视为 symbolic coverage；需要固定黑色与 caller tint 并存时，\n  必须扩展 icon resource contract；\n- `render_scene` 当前绘制 `zui` 的背景、rect、RGBA image、icon 和 text primitive。",
    "headings": [
      {
        "depth": 2,
        "id": "1-所有权",
        "title": "1. 所有权"
      },
      {
        "depth": 2,
        "id": "2-公共与内部接口",
        "title": "2. 公共与内部接口"
      },
      {
        "depth": 2,
        "id": "3-执行路径与接口面语义",
        "title": "3. 执行路径与接口面语义"
      },
      {
        "depth": 2,
        "id": "4-测试",
        "title": "4. 测试"
      },
      {
        "depth": 2,
        "id": "5-修改路径与当前限制",
        "title": "5. 修改路径与当前限制"
      }
    ],
    "searchText": " 本 README 负责 wgpu backend 的当前实现与失败语义。跨 backend 契约见 zeta-renderer，系统替换规则见 docs/rendering-architecture.md，scene 语义见 zui。 zeta-wgpu 拥有一个 native window 的 wgpu presentation 资源，并把可选的 immutable zui::UiScene 组合进同一 render pass。它不知道 App Server、Session、Thread、Agent、 Workbench 或其他产品状态。 1. 所有权 能力 本 crate 委托/不拥有 --- --- --- Instance、adapter、device、queue、surface、configuration ✅ Frame acquire、clear、submit、present ✅ Resize、零尺寸与 surface lost recovery ✅ UI scene/paint/font/text semantics ❌ zui Rect/image/icon/text GPU pipelines、atlas 与 glyph resources ✅ Backend-neutral frame contract ❌ zeta-renderer::Renderer Event loop、窗口策略 ❌ zeta-winit / product host Element/layout 与 text-input 基座 ❌ zui Widget、input routing、IME adapter、accessibility ❌ zeta-ui / product host / platform adapter App Server、workspace、durable state ❌ 上层产品 依赖方向： ``text native product host ├─→ zeta-winit → winit ├─→ zeta-ui → zui ├─→ zeta-renderer → zui └─→ zeta-wgpu → zeta-renderer → zeta-winit → zui → wgpu ` 出现 App Server method、product command、workspace model、widget state 或 font-platform implementation，意味着 crate ownership 已经漂移。 2. 公共与内部接口 Symbol 可见性 职责 不能承担 --- --- --- --- WgpuRenderer public 一个 window 的 GPU/surface 与 UI GPU pipeline ownership，并实现 Renderer event loop、产品状态 WgpuRenderer::initialize public NativeWindow → surface/adapter/device/config/UI pipeline App Server startup WgpuRenderer::resize public 保存 physical extent 并在非零时 configure layout 计算 WgpuRenderer::setscalefactor public 保存平台 DPI 事实 logical layout policy WgpuRenderer::requestredraw public 转发 owned native window 的 redraw 请求 动画或 invalidation policy WgpuRenderer::render public 无 scene 的稳定 clear/present 路径 UI state mutation WgpuRenderer::renderscene public prepare UI、clear scene background、draw、present 构造或布局 scene zetarenderer::RenderOutcome re-export 告知 host presented/skipped/retry 自行调度 event loop WgpuRendererError public surface/device/UI render failure 产品级恢复策略 uirenderer::UiRenderer private 协调 rect/image/icon/text prepare，并严格按 UiScene::batches 执行 scene/layout/paint-order 语义 uirenderer::CachedTextBuffer private 按 Scene 文本槽位复用 shape-affecting 内容未变化的 glyphon Buffer；位置、clip 与普通文本颜色变化只更新 TextArea 跨组件 identity、无限历史缓存 uirenderer:: rect,image,icon private instance conversion、validation、atlas、shader 与 primitive-to-instance range mapping component state uirenderer::UiRenderError public 区分 invalid scene、atlas、glyph prepare/render failure surface recovery viewport::Viewport private physical extent 与 scale-factor 状态 window/GPU handle WgpuRenderer::renderframe private 统一 clear-only 与 scene frame 顺序 业务 invalidation wgpucolor private sRGB background → linear wgpu::Color glyph color mapping 3. 执行路径与接口面语义 `text product-owned ApplicationHandler → NativeWindow::create → WgpuRenderer::initialize → Box<dyn zetarenderer::Renderer → resize / setscalefactor → render 或 renderscene → WgpuRenderer::renderframe → Surface::getcurrenttexture → UiRenderer::prepare scene primitive → backend resource/range → refreshtextbuffercache reuse unchanged shaping buffers → clear background → UiRenderer::render ordered SceneBatch execution → Queue::submit → NativeWindow::prepresentnotify → Queue::present → UiRenderer::trim scene only ` Viewport::surfaceextent 对任一零维返回 None，因此最小化期间不会提交零尺寸 surface configuration。UI prepare 在 frame acquire 后、command encoding 前发生；prepare 失败时 frame 不会 submit/present。 CurrentSurfaceTexture zeta-wgpu 行为 Host 行为 --- --- --- Success clear、可选 UI draw、submit、present 无 Timeout / Occluded 返回 Skipped 等下一次 invalidation Outdated / Suboptimal configure，返回 Retry 请求一次 redraw Lost 重建 surface，返回 Retry 请求一次 redraw Validation 返回 SurfaceValidation 产品决定退出或恢复 renderer 从不主动 busy-loop。动画、timer、App Server update 与 UI invalidation 均由上层 host 产生。 4. 测试 `bash cargo test --manifest-path Cargo.toml -p zeta-wgpu bazel test //zeterm/wgpu:wgpu-unit-tests ` 单元测试覆盖 viewport 状态、sRGB conversion、scene primitive validation、instance conversion、 icon raster 和 atlas allocation，不创建窗口或 GPU。普通 CI 编译 不能证明 Metal、DX12 或 Vulkan presentation 可用；真实 surface 与 glyph output smoke 由产品 binary 在各平台执行。 5. 修改路径与当前限制 - surface API/恢复策略：同步检查 gpu.rs、viewporttests.rs 和本 README； - scene/text contract：修改 zui，不要在 backend 中复制组件或产品语义； - 新 paint primitive：先在 zui 建立 scene contract，再在 uirenderer 增加 backend 实现； - 通用 frame outcome 或 host 接口：修改 zeta-renderer，不要把 wgpu 类型加入 trait； - window/event-loop policy：属于 zeta-winit 和 product host； - App Server/product 功能：禁止在本 crate 接入。 当前限制： - 一个 WgpuRenderer 只服务一个 window/surface； - 没有 frame telemetry、device-lost migration 或显式 pipeline warmup； - 没有 headless GPU test、golden image 或产品 host vertical； - multicolor icon 栅格当前把纯黑像素视为 symbolic coverage；需要固定黑色与 caller tint 并存时， 必须扩展 icon resource contract； - renderscene 当前绘制 zui` 的背景、rect、RGBA image、icon 和 text primitive。"
  },
  {
    "slug": "crates/windows-sandbox-rs",
    "sourcePath": "zeta-rs/windows-sandbox-rs/README.md",
    "title": "zeta-windows-sandbox",
    "description": "当前 backend 支持 ReadOnly + NetworkDenied 和 WorkspaceWrite + NetworkDenied。其他受限 policy 返回 BackendUnavailable，不会降级为普通进程。FullAccess + NetworkAllowed 仍按共享 contract 直接执行，不进入 helper。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有 shared sandbox policy 到 Windows AppContainer enforcement 的实现契约；\n> package helper contract 见\n> [`scripts/zeta_package`](../../scripts/zeta_package/README.md)，跨平台决策见\n> [`docs/sandboxing.md`](../../docs/sandboxing.md)。\n> 真实 Windows 验收步骤与 golden expectations 见\n> [`Windows Sandbox 手工验收 Runbook`](../../docs/windows-sandbox-acceptance-runbook.md)。\n\n当前 backend 支持 `ReadOnly + NetworkDenied` 和 `WorkspaceWrite + NetworkDenied`。其他受限\npolicy 返回 `BackendUnavailable`，不会降级为普通进程。`FullAccess + NetworkAllowed` 仍按共享\ncontract 直接执行，不进入 helper。\n\n## 执行路径\n\n```text\nInstallContext\n├─ WindowsCommandRunner candidates\n└─ WindowsSandboxSetup candidates\n   ↓ canonicalize + --zeta-sandbox-probe\nWindowsSandbox::prepare\n   ↓\nzeta-command-runner.exe\n├─ zeta-windows-sandbox-setup.exe\n│  ├─ create/derive workspace-and-access-scoped profile SID\n│  └─ install workspace/program ACLs\n└─ CreateProcessW\n   ├─ PROC_THREAD_ATTRIBUTE_SECURITY_CAPABILITIES\n   ├─ zero capabilities (network denied)\n   ├─ restricted child-process policy\n   └─ Job Object: kill-on-close + one active process\n```\n\n`discovery::discover_helper` 验证两个 helper 是普通 executable，执行精确 protocol probe，\ncanonicalize 后冻结路径。显式环境 override\n`ZETA_WINDOWS_COMMAND_RUNNER_PATH` / `ZETA_WINDOWS_SANDBOX_SETUP_PATH` 无效时直接失败；普通\npackage/PATH candidate 可以逐个尝试。\n\nbackend 以 canonical Workspace path 和 `ro`/`rw` access mode 的 SHA-256 前缀派生 profile 名；\n不同 Workspace 不复用 AppContainer identity，写模式也不会向只读模式累积 authority。\n`setup::run` 创建或复用该 AppContainer profile，并把 profile SID 的 read/execute 权限授予\n对应 Workspace。runner 先把冻结的 inner program 复制到本次调用独有的用户 temp directory；\nsetup 只给该目录与 staged program 授予 read/execute，child 结束后 runner 清理它。因此安装在\n`Program Files` 时也不要求修改随包 `rg.exe` 的 DACL。WorkspaceWrite 额外授予 Workspace 写入权限，同时对\n`.git` 等 `PROTECTED_WORKSPACE_METADATA_NAMES` 显式递归安装 write/delete deny ACE；递归不跟随\nreparse point，任一 ACL 操作失败都会阻止 child 启动。ACL 是持久的 Windows filesystem metadata，\n不是进程退出后自动撤销的临时 mount。\n\n`runner::launch` 不向 AppContainer 提供 network capability，并用 child-process policy 与 Job\nObject 阻止 rg 建立额外进程树。setup、profile 创建、ACL、attribute list、Job assignment 或\nspawn 任一步失败，runner 都输出私有 diagnostic marker 并返回保留 exit code。inner process\n若恰好返回该 code，runner 会先重映射；backend 因此只信任不可由 child 透传的保留状态，把它\n分类为 start-before-process sandbox denial。普通 non-zero exit 或伪造 stderr marker 不会被误报。\n\n## 当前限制\n\n- 该实现没有复刻 Codex 的 dedicated local users、private desktop 或 WFP firewall backend；\n  Zeta v1 使用原生 AppContainer 的 package SID、capability 和 ACL 模型。\n- 目前只为 built-in、固定 executable 的无网络 local process（当前是 `rg`）接线；不支持任意\n  shell、PTY、网络代理或动态 capability。\n- AppContainer profiles 与 ACL 是持久状态；目前没有 installer/uninstaller cleanup，长期使用过\n  的 Workspace 会保留只授予其 scoped profile SID 的 ACE。\n- Windows API 已通过 MSVC target 的 Rust 交叉检查，但仍需要 Windows CI 的真实\n  AppContainer、ACL、网络和 cancellation/kill-tree integration tests，才能标记为\n  production-enforced。\n\n修改 profile 名、ACL mask/递归、process attributes、probe protocol、helper 名称或 denial\nmarker 时，必须同步本 crate tests、`zeta-install-context`、package builder、App Server\ncomposition 和系统文档。\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-windows-sandbox\ncargo check --manifest-path Cargo.toml \\\n  --target x86_64-pc-windows-msvc -p zeta-windows-sandbox --all-targets\n```",
    "headings": [
      {
        "depth": 2,
        "id": "执行路径",
        "title": "执行路径"
      },
      {
        "depth": 2,
        "id": "当前限制",
        "title": "当前限制"
      }
    ],
    "searchText": " 本 README 拥有 shared sandbox policy 到 Windows AppContainer enforcement 的实现契约； package helper contract 见 scripts/zetapackage，跨平台决策见 docs/sandboxing.md。 真实 Windows 验收步骤与 golden expectations 见 Windows Sandbox 手工验收 Runbook。 当前 backend 支持 ReadOnly + NetworkDenied 和 WorkspaceWrite + NetworkDenied。其他受限 policy 返回 BackendUnavailable，不会降级为普通进程。FullAccess + NetworkAllowed 仍按共享 contract 直接执行，不进入 helper。 执行路径 ``text InstallContext ├─ WindowsCommandRunner candidates └─ WindowsSandboxSetup candidates ↓ canonicalize + --zeta-sandbox-probe WindowsSandbox::prepare ↓ zeta-command-runner.exe ├─ zeta-windows-sandbox-setup.exe │ ├─ create/derive workspace-and-access-scoped profile SID │ └─ install workspace/program ACLs └─ CreateProcessW ├─ PROCTHREADATTRIBUTESECURITYCAPABILITIES ├─ zero capabilities network denied ├─ restricted child-process policy └─ Job Object: kill-on-close + one active process ` discovery::discoverhelper 验证两个 helper 是普通 executable，执行精确 protocol probe， canonicalize 后冻结路径。显式环境 override ZETAWINDOWSCOMMANDRUNNERPATH / ZETAWINDOWSSANDBOXSETUPPATH 无效时直接失败；普通 package/PATH candidate 可以逐个尝试。 backend 以 canonical Workspace path 和 ro/rw access mode 的 SHA-256 前缀派生 profile 名； 不同 Workspace 不复用 AppContainer identity，写模式也不会向只读模式累积 authority。 setup::run 创建或复用该 AppContainer profile，并把 profile SID 的 read/execute 权限授予 对应 Workspace。runner 先把冻结的 inner program 复制到本次调用独有的用户 temp directory； setup 只给该目录与 staged program 授予 read/execute，child 结束后 runner 清理它。因此安装在 Program Files 时也不要求修改随包 rg.exe 的 DACL。WorkspaceWrite 额外授予 Workspace 写入权限，同时对 .git 等 PROTECTEDWORKSPACEMETADATANAMES 显式递归安装 write/delete deny ACE；递归不跟随 reparse point，任一 ACL 操作失败都会阻止 child 启动。ACL 是持久的 Windows filesystem metadata， 不是进程退出后自动撤销的临时 mount。 runner::launch 不向 AppContainer 提供 network capability，并用 child-process policy 与 Job Object 阻止 rg 建立额外进程树。setup、profile 创建、ACL、attribute list、Job assignment 或 spawn 任一步失败，runner 都输出私有 diagnostic marker 并返回保留 exit code。inner process 若恰好返回该 code，runner 会先重映射；backend 因此只信任不可由 child 透传的保留状态，把它 分类为 start-before-process sandbox denial。普通 non-zero exit 或伪造 stderr marker 不会被误报。 当前限制 - 该实现没有复刻 Codex 的 dedicated local users、private desktop 或 WFP firewall backend； Zeta v1 使用原生 AppContainer 的 package SID、capability 和 ACL 模型。 - 目前只为 built-in、固定 executable 的无网络 local process（当前是 rg）接线；不支持任意 shell、PTY、网络代理或动态 capability。 - AppContainer profiles 与 ACL 是持久状态；目前没有 installer/uninstaller cleanup，长期使用过 的 Workspace 会保留只授予其 scoped profile SID 的 ACE。 - Windows API 已通过 MSVC target 的 Rust 交叉检查，但仍需要 Windows CI 的真实 AppContainer、ACL、网络和 cancellation/kill-tree integration tests，才能标记为 production-enforced。 修改 profile 名、ACL mask/递归、process attributes、probe protocol、helper 名称或 denial marker 时，必须同步本 crate tests、zeta-install-context、package builder、App Server composition 和系统文档。 `bash cargo test --manifest-path Cargo.toml -p zeta-windows-sandbox cargo check --manifest-path Cargo.toml \\ --target x8664-pc-windows-msvc -p zeta-windows-sandbox --all-targets ``"
  },
  {
    "slug": "crates/zeterm/winit",
    "sourcePath": "zeterm/winit/README.md",
    "title": "zeta-winit",
    "description": "zeta-winit 是架构分类中位于 App Server 下方的底层 native host adapter。它封装 winit event-loop bootstrap、window ownership 与 persistent display handle，但不拥有任何产品身份、 App Server connection、UI tree 或渲染状态。",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 负责 native event-loop/window crate 的当前实现、集成义务和修改路径。\n> GPU surface 与 presentation 由 [`zeta-wgpu`](../wgpu/README.md) 拥有。\n> Native 文本输入的跨 crate ownership 见\n> [`docs/native-text-input.md`](../docs/native-text-input.md)。\n> 产品 Titlebar 如何消费窗口控件占位由\n> [`docs/native-terminal-ui.md`](../docs/native-terminal-ui.md) 统一说明。\n\n`zeta-winit` 是架构分类中位于 App Server 下方的底层 native host adapter。它封装 `winit`\nevent-loop bootstrap、window ownership 与 persistent display handle，但不拥有任何产品身份、\nApp Server connection、UI tree 或渲染状态。\n\n## 1. 所有权\n\n| Symbol | 可见性 | 职责 | 不拥有 |\n| --- | --- | --- | --- |\n| `run_application` | public | 创建 event loop 并运行 product-owned handler | 产品 lifecycle 或错误策略 |\n| `run_application_with_user_events` | public | 创建 typed event loop，并把 `EventLoopProxy<T>` 交给产品构造器 | user-event 类型或后台任务 |\n| `NativeWindow::create` | public | 从 `ActiveEventLoop`、attributes 和显式 `WindowChrome` 创建窗口 | 产品标题、尺寸或窗口模式 |\n| `NativeWindow` | public | 安全持有 window 与 display handle | GPU surface、widget 或 workspace |\n| `PhysicalExtent` | public | 跨底层 crate 传递 physical pixel dimensions | logical layout |\n| `NativeWindow::request_inner_logical_size` | public | 把 host 决定的 logical inner size 请求转发给平台窗口 | 产品布局、面板宽度或 resize policy |\n| `WindowChrome` | public | 让产品选择 native chrome 与 full-size titlebar 的共享方式 | titlebar paint/layout |\n| `WindowControlInsets` / `NativeWindow::window_control_insets` | public | 把当前 chrome policy 投影为覆盖产品内容的左右逻辑占位 | ActionBar 间距或产品布局 |\n| `NativeWindow::start_window_drag` | public | 转发产品 titlebar 命中的平台窗口拖动 | hit testing |\n| `NativeWindow::set_cursor` | public | 应用产品 hit testing 选择的 cursor | hover state |\n| `NativeWindow::set_title` | public | 把 product/session title 转发给 platform window | title 来源或 OSC parsing |\n| `NativeWindow::enable_ime` / `disable_ime` | public | 转发产品 focus 选择的 IME activation | focus 或 composition |\n| `ImeCursorArea` / `set_ime_cursor_area` | public | 把 logical caret area 转发给平台候选框 | shaping 或 caret policy |\n\n`ApplicationHandler`、`ActiveEventLoop`、`ControlFlow`、`WindowEvent`、keyboard/IME/pointer\nevent values（包括 `MouseButton` 与 `MouseScrollDelta`）、`WindowAttributes`、`WindowId` 和\n`LogicalSize`/`PhysicalPosition` 由本 crate 重新导出，使上层 host 不需要绕过 adapter 建立另一套 `winit`\nintegration。\n\n真实调用关系：\n\n```text\nproduct-owned ApplicationHandler\n  → run_application\n  → NativeWindow::create\n       ├─ apply_window_chrome\n       ├─ ActiveEventLoop::create_window\n       └─ ActiveEventLoop::owned_display_handle\n  → NativeWindow::window_control_insets\n       └─ window_chrome::window_control_insets\n  → NativeWindow event/redraw methods\n  → ActiveEventLoop::set_control_flow (product-owned wakeup deadline)\n  → enable_ime / disable_ime / set_ime_cursor_area\n  → zeta-wgpu consumes surface_target + display_handle\n```\n\n## 2. 边界与失败\n\n- window 必须在 `ApplicationHandler::resumed` 后创建，以保留移动端 surface lifecycle；\n- `WindowAttributes` 由产品构造，`NativeWindow::create` 只按显式 `WindowChrome` 应用平台 chrome，\n  因此标题、尺寸和窗口模式不是本 crate policy；\n- `ContentUnderTitlebar` 在 macOS 保留 system window buttons，同时启用 transparent titlebar、\n  full-size content 和 first-mouse；其他平台当前保持 attributes 不变；\n- `WindowControlInsets` 使用 logical pixels，只描述 system controls 覆盖产品内容的宽度；macOS\n  `ContentUnderTitlebar` 当前返回左侧 `70px`，其他已实现组合返回零；Titlebar 自身必须另加组件\n  间距，不能把 `70px` 复制到产品组件；\n- `Theme` 只作为 `winit` 窗口外观策略透传；产品可以显式选择 light/dark native chrome，\n  `zeta-winit` 不据此决定产品 scene palette；\n- event-loop 与 window creation error 原样返回，产品决定诊断、恢复或退出；\n- `NativeWindow` 只提供 handle、identity、extent、scale factor、size request、redraw/present hooks\n  与原生窗口交互 forwarding；`request_inner_logical_size` 不保证平台接受请求，host 仍须以随后收到的\n  `WindowEvent::Resized` 作为实际 surface extent；\n- IME 默认保持关闭；product host 必须根据 editable focus 显式启停，并只在 active composition\n  contract 内更新 logical candidate area；\n- 出现 App Server method、workspace state、widget、paint scene 或 GPU resource 意味着 ownership\n  已漂移。\n\n## 3. 测试与限制\n\n```bash\ncargo test --manifest-path Cargo.toml -p zeta-winit\n```\n\n当前单元测试验证值语义、chrome attributes preservation 和平台占位 policy。CI 编译不能代替\nmacOS、Windows、Linux 的真实窗口、resume/suspend、DPI 与多窗口 smoke。\n\n当前没有产品 event handler、默认窗口策略、clipboard ownership、accessibility、file drag/drop 或\nplatform menu。IME 仅有 platform forwarding，不拥有 focus、composition 或文本编辑语义。\n`winit` 当前没有安全的 system button geometry API，而 workspace 禁止 `unsafe` AppKit pointer\n访问，因此 macOS 占位是集中在 `window_chrome` 的显式 policy，不是运行时测量；RTL 按钮换边和\n未来 Windows controls overlay 必须在该 adapter 增加平台实现，不能在产品 Titlebar 添加第二套\n常量。",
    "headings": [
      {
        "depth": 2,
        "id": "1-所有权",
        "title": "1. 所有权"
      },
      {
        "depth": 2,
        "id": "2-边界与失败",
        "title": "2. 边界与失败"
      },
      {
        "depth": 2,
        "id": "3-测试与限制",
        "title": "3. 测试与限制"
      }
    ],
    "searchText": " 本 README 负责 native event-loop/window crate 的当前实现、集成义务和修改路径。 GPU surface 与 presentation 由 zeta-wgpu 拥有。 Native 文本输入的跨 crate ownership 见 docs/native-text-input.md。 产品 Titlebar 如何消费窗口控件占位由 docs/native-terminal-ui.md 统一说明。 zeta-winit 是架构分类中位于 App Server 下方的底层 native host adapter。它封装 winit event-loop bootstrap、window ownership 与 persistent display handle，但不拥有任何产品身份、 App Server connection、UI tree 或渲染状态。 1. 所有权 Symbol 可见性 职责 不拥有 --- --- --- --- runapplication public 创建 event loop 并运行 product-owned handler 产品 lifecycle 或错误策略 runapplicationwithuserevents public 创建 typed event loop，并把 EventLoopProxy<T 交给产品构造器 user-event 类型或后台任务 NativeWindow::create public 从 ActiveEventLoop、attributes 和显式 WindowChrome 创建窗口 产品标题、尺寸或窗口模式 NativeWindow public 安全持有 window 与 display handle GPU surface、widget 或 workspace PhysicalExtent public 跨底层 crate 传递 physical pixel dimensions logical layout NativeWindow::requestinnerlogicalsize public 把 host 决定的 logical inner size 请求转发给平台窗口 产品布局、面板宽度或 resize policy WindowChrome public 让产品选择 native chrome 与 full-size titlebar 的共享方式 titlebar paint/layout WindowControlInsets / NativeWindow::windowcontrolinsets public 把当前 chrome policy 投影为覆盖产品内容的左右逻辑占位 ActionBar 间距或产品布局 NativeWindow::startwindowdrag public 转发产品 titlebar 命中的平台窗口拖动 hit testing NativeWindow::setcursor public 应用产品 hit testing 选择的 cursor hover state NativeWindow::settitle public 把 product/session title 转发给 platform window title 来源或 OSC parsing NativeWindow::enableime / disableime public 转发产品 focus 选择的 IME activation focus 或 composition ImeCursorArea / setimecursorarea public 把 logical caret area 转发给平台候选框 shaping 或 caret policy ApplicationHandler、ActiveEventLoop、ControlFlow、WindowEvent、keyboard/IME/pointer event values（包括 MouseButton 与 MouseScrollDelta）、WindowAttributes、WindowId 和 LogicalSize/PhysicalPosition 由本 crate 重新导出，使上层 host 不需要绕过 adapter 建立另一套 winit integration。 真实调用关系： ``text product-owned ApplicationHandler → runapplication → NativeWindow::create ├─ applywindowchrome ├─ ActiveEventLoop::createwindow └─ ActiveEventLoop::owneddisplayhandle → NativeWindow::windowcontrolinsets └─ windowchrome::windowcontrolinsets → NativeWindow event/redraw methods → ActiveEventLoop::setcontrolflow product-owned wakeup deadline → enableime / disableime / setimecursorarea → zeta-wgpu consumes surfacetarget + displayhandle ` 2. 边界与失败 - window 必须在 ApplicationHandler::resumed 后创建，以保留移动端 surface lifecycle； - WindowAttributes 由产品构造，NativeWindow::create 只按显式 WindowChrome 应用平台 chrome， 因此标题、尺寸和窗口模式不是本 crate policy； - ContentUnderTitlebar 在 macOS 保留 system window buttons，同时启用 transparent titlebar、 full-size content 和 first-mouse；其他平台当前保持 attributes 不变； - WindowControlInsets 使用 logical pixels，只描述 system controls 覆盖产品内容的宽度；macOS ContentUnderTitlebar 当前返回左侧 70px，其他已实现组合返回零；Titlebar 自身必须另加组件 间距，不能把 70px 复制到产品组件； - Theme 只作为 winit 窗口外观策略透传；产品可以显式选择 light/dark native chrome， zeta-winit 不据此决定产品 scene palette； - event-loop 与 window creation error 原样返回，产品决定诊断、恢复或退出； - NativeWindow 只提供 handle、identity、extent、scale factor、size request、redraw/present hooks 与原生窗口交互 forwarding；requestinnerlogicalsize 不保证平台接受请求，host 仍须以随后收到的 WindowEvent::Resized 作为实际 surface extent； - IME 默认保持关闭；product host 必须根据 editable focus 显式启停，并只在 active composition contract 内更新 logical candidate area； - 出现 App Server method、workspace state、widget、paint scene 或 GPU resource 意味着 ownership 已漂移。 3. 测试与限制 `bash cargo test --manifest-path Cargo.toml -p zeta-winit ` 当前单元测试验证值语义、chrome attributes preservation 和平台占位 policy。CI 编译不能代替 macOS、Windows、Linux 的真实窗口、resume/suspend、DPI 与多窗口 smoke。 当前没有产品 event handler、默认窗口策略、clipboard ownership、accessibility、file drag/drop 或 platform menu。IME 仅有 platform forwarding，不拥有 focus、composition 或文本编辑语义。 winit 当前没有安全的 system button geometry API，而 workspace 禁止 unsafe AppKit pointer 访问，因此 macOS 占位是集中在 windowchrome` 的显式 policy，不是运行时测量；RTL 按钮换边和 未来 Windows controls overlay 必须在该 adapter 增加平台实现，不能在产品 Titlebar 添加第二套 常量。"
  },
  {
    "slug": "crates/workspace",
    "sourcePath": "zeta-rs/workspace/README.md",
    "title": "zeta-workspace",
    "description": "zeta-workspace 是 Workspace root identity、containment、observer-path projection 和 root-bound trust token 的 canonical Rust owner。它不拥有 Workspace picker、Config persistence 或 App Server runtime…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 拥有单个本地 Workspace 文件系统边界及其 execution-trust capability 的实现契约。\n> 跨 crate 的产品语义与 staged evolution 由\n> [`docs/workspace-security.md`](../../docs/workspace-security.md) 维护。\n\n`zeta-workspace` 是 Workspace root identity、containment、observer-path projection 和 root-bound\ntrust token 的 canonical Rust owner。它不拥有 Workspace picker、Config persistence 或 App\nServer runtime orchestration。\n\n## 所有权\n\n| Symbol | 精确契约 | 明确不拥有 |\n| --- | --- | --- |\n| `WorkspaceRoot::open` | 冻结 requested 与 canonical directory namespace | Workspace discovery UI 或 persistence |\n| `WorkspaceRoot::resolve_existing` | 跟随 symlink，并证明 existing target 保持 contained | handle-relative I/O |\n| `WorkspaceRoot::resolve_for_write` | 通过最近 existing ancestor 校验 new target | 后续 write operation |\n| `WorkspaceRoot::project_observed_path` | 把 requested/canonical watcher path 投影到一个 relative namespace | watcher lifecycle 或 debounce |\n| `WorkspaceRoot::relative_to_existing_ancestor` | 为 nested Git worktree 提供 canonical projection | Git status semantics |\n| `WorkspaceRoot::trust_id` / `WorkspaceTrustId` | 生成 host persistence 使用的 opaque canonical-root key | User Config storage 或 trust policy |\n| `WorkspaceAuthorization::revoke` | 失效一个 host decision 签发的全部 capability token | runtime teardown orchestration |\n| `TrustedWorkspace::require` | 将 host trust decision 转成绑定 exact root 的 revocable token | trust UI、trust-store persistence、organization policy resolution |\n\nPrivate `root::WorkspaceRoot::candidate` 与 `root::WorkspaceRoot::ensure_contained` 承载 lexical 和\ncanonical containment。`trust::TrustedWorkspace::require` 是 executable capability token 的唯一\nconstructor：\n\n```text\nhost-selected path\n  → WorkspaceRoot::open\n  → host trust-store / policy decision\n  → TrustedWorkspace::require(capability)\n  → Terminal / Tool / extension / repository mutation owner\n```\n\n## 失败语义\n\n- Root 无法 absolute、无法 canonicalize 或不是 directory 时，`WorkspaceRoot::open` 失败。\n- Existing path 跟随 symlink 后离开 canonical root 时，resolution 失败即关闭。\n- Write target 拒绝 parent、root 与 platform prefix component，并校验最近 existing ancestor；\n  caller 仍须使用安全 I/O 并处理 race。\n- Observer projection 不 canonicalize changed path，因此已删除 entry 仍可投影；两个冻结\n  namespace 之外的 path 被忽略。\n- Restricted trust decision 不能生成 `TrustedWorkspace`；revocation 后，已经签发的 token 也会在\n  `TrustedWorkspace::ensure_active` 失败。\n\n## 集成义务\n\n安全的 read、browse、edit、Search 与 watch service 可以保留 `WorkspaceRoot`。Process launch、\nWorkspace-declared Tool、executable configuration、extension activation 与 repository mutation\n必须保留 `TrustedWorkspace`，或在 authoritative entry point 执行等价 capability check。Client\n提交的 path 或 Workspace config file 不能授予 trust。\n\n`WorkspaceRoot` 是 path boundary，不是完整 TOCTOU defense。需要抵御 hostile concurrent\nfilesystem mutation 的 caller 必须增加 platform handle-relative operation（例如 `openat`），\n不能假设先前的 path check 会让后续 open 变成 atomic。\n\n`WorkspaceTrustId` hash canonical path 的平台原生 bytes，因此 persistence 不存 cleartext path。\nWorkspace 移动后 identity 会变化；canonical alias 会得到同一 identity。当前它不能检测同一路径\n上的 filesystem object replacement。\n\n## 测试与修改影响\n\n`root_tests.rs` 覆盖 directory validation、lexical escape rejection、symlink escape rejection 与\ndual-namespace watcher projection。`trust_tests.rs` 覆盖 Restricted denial、exact-root binding 与\nrevocation。Identity 或 projection 变化会影响 Files、sandbox、watcher 和 Git consumer；trust\ncapability semantics 变化会影响全部 executable Workspace runtime。\n\n下列代码形态表示 architecture drift：\n\n- 把 Editor、Git 或 runtime orchestration 移进本 crate；\n- 用 raw `PathBuf` 直接构造 executable service；\n- 把 canonical containment 等同于 user trust；\n- 允许 Workspace config 自行生成 trust decision。\n\n## 当前限制与扩展点\n\n当前 crate 有意只建模一个 root，不拥有附加目录访问作用域。主工作目录与附加目录的角色、\ndirectory source lifetime、canonical deduplication 和 contribution policy 已由\n[`zeta-add-dir`](../add-dir/README.md) 独立拥有；这些规则不能回流到 `WorkspaceRoot` 或 trust\ntoken。\n\nApp Server 负责为 `zeta-add-dir` scope 中的每个 root 解析 capability，并只重建 authorized\nconsumer。`/cd` 是独立 authority-switch 路径，负责替换主工作目录并重新加载完整项目配置。\n\n`zeta-agent-import` 可以为 `zeta-add-dir` adapter 提供安全的 source-specific inspection，但\nImport workflow 与 directory\nauthorization 必须保持分离。一次性 user import root 不能隐式变成 persistent additional root；\n反过来，持久 file-access-only root 也不能因为可访问就触发 Agent import。",
    "headings": [
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "失败语义",
        "title": "失败语义"
      },
      {
        "depth": 2,
        "id": "集成义务",
        "title": "集成义务"
      },
      {
        "depth": 2,
        "id": "测试与修改影响",
        "title": "测试与修改影响"
      },
      {
        "depth": 2,
        "id": "当前限制与扩展点",
        "title": "当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 拥有单个本地 Workspace 文件系统边界及其 execution-trust capability 的实现契约。 跨 crate 的产品语义与 staged evolution 由 docs/workspace-security.md 维护。 zeta-workspace 是 Workspace root identity、containment、observer-path projection 和 root-bound trust token 的 canonical Rust owner。它不拥有 Workspace picker、Config persistence 或 App Server runtime orchestration。 所有权 Symbol 精确契约 明确不拥有 --- --- --- WorkspaceRoot::open 冻结 requested 与 canonical directory namespace Workspace discovery UI 或 persistence WorkspaceRoot::resolveexisting 跟随 symlink，并证明 existing target 保持 contained handle-relative I/O WorkspaceRoot::resolveforwrite 通过最近 existing ancestor 校验 new target 后续 write operation WorkspaceRoot::projectobservedpath 把 requested/canonical watcher path 投影到一个 relative namespace watcher lifecycle 或 debounce WorkspaceRoot::relativetoexistingancestor 为 nested Git worktree 提供 canonical projection Git status semantics WorkspaceRoot::trustid / WorkspaceTrustId 生成 host persistence 使用的 opaque canonical-root key User Config storage 或 trust policy WorkspaceAuthorization::revoke 失效一个 host decision 签发的全部 capability token runtime teardown orchestration TrustedWorkspace::require 将 host trust decision 转成绑定 exact root 的 revocable token trust UI、trust-store persistence、organization policy resolution Private root::WorkspaceRoot::candidate 与 root::WorkspaceRoot::ensurecontained 承载 lexical 和 canonical containment。trust::TrustedWorkspace::require 是 executable capability token 的唯一 constructor： ``text host-selected path → WorkspaceRoot::open → host trust-store / policy decision → TrustedWorkspace::require capability → Terminal / Tool / extension / repository mutation owner ` 失败语义 - Root 无法 absolute、无法 canonicalize 或不是 directory 时，WorkspaceRoot::open 失败。 - Existing path 跟随 symlink 后离开 canonical root 时，resolution 失败即关闭。 - Write target 拒绝 parent、root 与 platform prefix component，并校验最近 existing ancestor； caller 仍须使用安全 I/O 并处理 race。 - Observer projection 不 canonicalize changed path，因此已删除 entry 仍可投影；两个冻结 namespace 之外的 path 被忽略。 - Restricted trust decision 不能生成 TrustedWorkspace；revocation 后，已经签发的 token 也会在 TrustedWorkspace::ensureactive 失败。 集成义务 安全的 read、browse、edit、Search 与 watch service 可以保留 WorkspaceRoot。Process launch、 Workspace-declared Tool、executable configuration、extension activation 与 repository mutation 必须保留 TrustedWorkspace，或在 authoritative entry point 执行等价 capability check。Client 提交的 path 或 Workspace config file 不能授予 trust。 WorkspaceRoot 是 path boundary，不是完整 TOCTOU defense。需要抵御 hostile concurrent filesystem mutation 的 caller 必须增加 platform handle-relative operation（例如 openat）， 不能假设先前的 path check 会让后续 open 变成 atomic。 WorkspaceTrustId hash canonical path 的平台原生 bytes，因此 persistence 不存 cleartext path。 Workspace 移动后 identity 会变化；canonical alias 会得到同一 identity。当前它不能检测同一路径 上的 filesystem object replacement。 测试与修改影响 roottests.rs 覆盖 directory validation、lexical escape rejection、symlink escape rejection 与 dual-namespace watcher projection。trusttests.rs 覆盖 Restricted denial、exact-root binding 与 revocation。Identity 或 projection 变化会影响 Files、sandbox、watcher 和 Git consumer；trust capability semantics 变化会影响全部 executable Workspace runtime。 下列代码形态表示 architecture drift： - 把 Editor、Git 或 runtime orchestration 移进本 crate； - 用 raw PathBuf 直接构造 executable service； - 把 canonical containment 等同于 user trust； - 允许 Workspace config 自行生成 trust decision。 当前限制与扩展点 当前 crate 有意只建模一个 root，不拥有附加目录访问作用域。主工作目录与附加目录的角色、 directory source lifetime、canonical deduplication 和 contribution policy 已由 zeta-add-dir 独立拥有；这些规则不能回流到 WorkspaceRoot 或 trust token。 App Server 负责为 zeta-add-dir scope 中的每个 root 解析 capability，并只重建 authorized consumer。/cd 是独立 authority-switch 路径，负责替换主工作目录并重新加载完整项目配置。 zeta-agent-import 可以为 zeta-add-dir` adapter 提供安全的 source-specific inspection，但 Import workflow 与 directory authorization 必须保持分离。一次性 user import root 不能隐式变成 persistent additional root； 反过来，持久 file-access-only root 也不能因为可访问就触发 Agent import。"
  },
  {
    "slug": "crates/zeterm/",
    "sourcePath": "zeterm/README.md",
    "title": "zeterm",
    "description": "zeterm 是与 zeta Electron Desktop 和 zeta code TUI 同级的纯 Rust Desktop 产品入口， 在发布边界导出为 zeterm。三条产品线的宿主边界见 docs/product-lines.md。 当前窗口纵切由产品拥有 ApplicationHandler，组合 zeta-winit、zeta-renderer、当前",
    "group": "Crate 实现参考",
    "markdown": "> Native 文本输入的跨 crate canonical ownership 见\n> [`docs/native-text-input.md`](docs/native-text-input.md)；Agent-first 产品结构见\n> [`docs/native-agent-console.md`](docs/native-agent-console.md)。Terminal compatibility\n> 细节见 [`docs/native-terminal-ui.md`](docs/native-terminal-ui.md)；本 README 只拥有 native\n> host 当前源码路径和接入义务。Terminal grid 与 BlockList 的实现契约见\n> [`zeta-terminal` README](../zeta-rs/terminal/README.md)；CodeEditor/DiffEditor presentation 契约见\n> [`zeta-editor` README](editor/README.md)；文件保存基线与冲突状态契约见\n> [`zeta-text-file` README](../zeta-rs/text-file/README.md)。\n> UI 到 GPU 的 canonical 边界见\n> [`docs/rendering-architecture.md`](docs/rendering-architecture.md)。\n\n`zeterm` 是与 `zeta` Electron Desktop 和 `zeta code` TUI 同级的纯 Rust Desktop 产品入口，\n在发布边界导出为 `zeterm`。三条产品线的宿主边界见\n[`docs/product-lines.md`](../docs/product-lines.md)。\n当前窗口纵切由产品拥有 `ApplicationHandler`，组合 `zeta-winit`、`zeta-renderer`、当前\n`zeta-wgpu` backend、`zui` framework 与 `zeta-ui` components，\n并在单个原生窗口中绘制 Agent ThreadTimeline 与固定底部 Agent/Shell Composer。\n启动时 `zeta-theme` 从共享 design-token manifest、`zeterm` 默认主题入口与 device-local 用户主题生成不可变 snapshot；\n`shell_style` 将其适配为 Shell、CodeEditor、MultiDiffEditor、scrollbar 与 terminal ANSI palette。\n`zeta-layout` 拥有 Root/Inspector/Terminal Workspace 的 pane topology；`zeta-composer` 拥有 Composer\npanel 与 interaction list 的纯几何；`ShellLayout` 和 `composer_panel` 仍只拥有剩余 Shell/Composer\nproduct composition 的区域背景、边界、绘制与状态接线，组件内部视觉继续由组件 style contract 拥有。\n窗口级产品 pane topology 已抽到 [`zeta-layout`](layout/README.md)；通用框架可脱离产品运行的\n最小链路由 [`zui-demo`](zui-demo/README.md) 持续验证。\n\n## 构建与发布边界\n\n仓库根 `Cargo.toml` 是唯一 Cargo workspace；`zeterm/Cargo.toml` 是 `zeterm` 产品 package 和发布边界：\n\n```bash\ncargo check --manifest-path Cargo.toml -p zeterm\ncargo test --manifest-path Cargo.toml -p zeterm\ncargo run --manifest-path Cargo.toml -p zeterm\n```\n\n`zeterm` 通过根 workspace 的 path dependencies 使用 shared backend；`zeta-rs` 不反向依赖 zeterm 或\nNative UI。Bazel 从同一个根 Cargo graph 生成 `@crates`，并以 `//zeterm:zeterm`、\n`//zeterm:zeterm_sources`、`//zeterm:zeterm_release_inputs` 和\n`//zeterm:zeterm_ci` 拥有产品 target、输入、发布契约与 boundary check。zeterm 仍是独立产品目录和\n发布入口，但不再是第二个 workspace 根；\n当前完整 zeterm binary 的 macOS 验证仍受 apple-cf Swift bridge 在沙箱中启动 SwiftPM 的环境限制，\nCargo workspace check/test 是产品源码的首要验证入口。发布 staging 入口是：\n\n```bash\njust zeterm-package --package-dir /absolute/path/to/zeterm-package --zeterm-bin /path/to/zeterm\nbazel test //zeterm:zeterm_ci\n```\n\n平台 release job 使用 `ZETERM_PACKAGE_DIR`、`ZETERM_PLATFORM` 以及对应的 signing identity 环境\n调用 `scripts/release_zeterm_package.sh`；该入口完成签名和验签后才留下 `verified` artifact。\n\npackage staging 只生成 unsigned artifact 和 binary digest；签名、平台验证与发布顺序见\n[`zeterm` release graph](docs/zeterm-release-graph.md)。\n\n## 宿主边界与 frame ownership\n\n`zeterm` 是产品宿主，不是第二个 UI framework。它保留窗口、平台事件、产品状态映射、App Server\n适配、command 执行以及具体 Shell/Composer/Inspector Part 的组合；通用 layout、scene、inspection、\ninteraction、animation、deadline 和 retained lifecycle 由 `zui` canonical owner 提供。跨 crate 的\n阶段、弃用范围和删除条件见 [`docs/zeterm-app-migration-plan.md`](docs/zeterm-app-migration-plan.md)。\n\n| 边界 | Native 当前状态 | 维护规则 |\n| --- | --- | --- |\n| 产品状态、平台事件、App Server/文件/Git/Session adapter | ✅ 当前 owner | 可以在 Native 演进，不迁入 `zui` |\n| Shell/Composer/Inspector 的产品组合 | ✅ 当前 owner | 组合 `zui`/`zeta-ui`，不复制组件内部 geometry 或 hit-test |\n| `ShellPresentation` 的 scene/interaction/accessibility 聚合 | `UiFrame<InteractionFrame>` | frame 是唯一 owner；renderer、input 和 Inspector 通过窄 accessor 消费 |\n| 低层产品 composition | `UiFrame::with_context` / `ComponentContext` | 只能在 scoped context 中注册交互和绘制；可复用组件使用 `draw_component` |\n| 通用 frame、失效、动画和 retained lifecycle | `zui` 委托 | Native 只注册产品 fragment、推进 runtime 并投影 deadline |\n\n因此不能把 `zeterm` binary 标成 deprecated：它是当前产品宿主。旧 split scene/interaction\ncomposition API 已删除，后续不得在 Native 宿主重新引入平行输出字段。\n\n## 所有权\n\n| 能力 | 当前 owner | 状态 |\n| --- | --- | --- |\n| `zeterm` 发布名与用户可见显示 | `PRODUCT_DISPLAY_NAME` / Cargo `[[bin]]` | ✅ |\n| 产品窗口标题、初始尺寸与 event routing | `zeterm::NativeApp` | ✅ |\n| Event loop 与 native window adapter | `zeta-winit` | 委托 |\n| 后端无关 render、resize、scale 与 frame outcome | `zeta-renderer::Renderer` | 委托；`NativeApp` 只保存 trait object |\n| GPU surface、pipeline、atlas、shader、present 与 retry | `zeta-wgpu` | 委托；仅 `renderer_backend` 知道具体 backend 类型 |\n| Element、Rect/image/icon/text scene、检查快照与字体测量 | `zui` | 委托；不接触组件或 GPU API |\n| Shell product layout 与 scene composition | `zeta-layout` + `zeta-composer` + `shell_scene` / `composer_panel` | 部分抽取；`zeta-layout` 拥有 root/workspace pane geometry，`zeta-composer` 拥有 Composer 几何，Native 仍拥有状态与 scene composition |\n| Native frame assembly | `ShellPresentation::frame` | ✅；由单一 `zui::UiFrame<InteractionFrame>` owner 管理 |\n| 原生布局检查模式、pointer 拦截与 highlight overlay | `layout_inspector::LayoutInspector` / `NativeApp` | ✅；Inspector Panel 是根 Grid leaf，只有产品节点高亮进入 overlay |\n| Sessions/Main 单轴约束与 Sash presentation geometry | `zui::SplitViewLayout` / `zeta-ui::Sash` | 委托 |\n| Terminal Workspace 与 Agent Sidebar 递归 Pane geometry | `zui::GridLayout` | 委托；当前输入树包含活动终端与可选右栏 Leaf |\n| Terminal Pane Tree、Pane-to-Session binding 与 active Pane | 后续 native Terminal Workspace model | 尚未完成；Session Tab 到独立 Terminal PTY 的一对一 binding 已完成 |\n| Sessions preferred width、显隐与当前 drag snapshot | `session_sidebar::SessionSidebarState` | ✅ |\n| Agent Sidebar 显隐、preferred width 与 resize gesture | `agent_sidebar::AgentSidebarState` / `zeta-ui::Sash` | ✅；宽度限制为 240–560px，不拥有内部 Pane 内容 |\n| Agent Sidebar 导航与各功能 Pane 的布局 | [`zeta-agent-sidebar`](agent-sidebar/README.md) 的 `AgentSidebarNavigation` / `files::FilesLayout` / `scm::ScmLayout` | 委托；zeterm 只提供主题投影、workspace 快照与 shell slot |\n| Files 层级树与模糊搜索 | [`zeta-agent-sidebar`](agent-sidebar/README.md) 的 `files::FilesState` / `files::FilesPane` / `zeta-ui::TreeView` / `ListView` / `zeta-file-search` | ✅；目录懒加载、稳定 mounted-node ID、展开/收起和 24px 虚拟行已接入；zeterm 只适配 App Server 目录 DTO 并执行打开/加载动作 |\n| UTF-8 文件保存 baseline、磁盘版本与外部变化冲突 | `zeta-text-file::TextFileLifecycle` | 委托；Native 只提供当前 editor text 与 I/O adapter |\n| 语言服务 composition、持久化设置、文档/请求 freshness 与 presentation | `language_service_host::NativeLanguageService` / `language_server_settings_model::LanguageServerSettingsState` / `file_editor_language_features` / `zeta-language-service` | ✅；Rust/JSON/Shell 独立设置与 runtime state，diagnostics，latest-only pointer hover，Ctrl/Cmd+Space completion popup/安全 edit 接受和 F12 definition navigation 已接入；文件读取仍通过 App Server authority |\n| 文件 Tab、active document、关闭决策与 presentation | `file_editor_host::FileEditorHost` / `file_editor_pane::FileEditorPane` / `file_editor_input::FileEditorInputState` | ✅；Explorer activation 已通过 typed `fs/readFile`/metadata 打开 language-aware document，Cmd/Ctrl+S 使用版本 preflight 和 `fs/writeFile`，中心 Editor Surface 已接通 tabs、关闭确认、外部重载/显式乐观覆盖、find/replace、自动缩进、soft wrap、focus、keyboard、IME、pointer、clipboard 与 visual-row viewport |\n| Changed-file collection、整体滚动与每文件 diff 视口 | [`zeta-agent-sidebar`](agent-sidebar/README.md) 的 `ScmState` / `EditorPaneState` / `zeta-ui::ScrollState` | 委托；zeterm 只把 Git 投影映射为 `ScmDiff` 并执行刷新动作 |\n| Titlebar 背景、窗口拖拽区、语言服务器设置入口与左右侧栏开关 | `titlebar::Titlebar` | ✅；不绘制可见窗口标题 |\n| Top Bar 左右 sidebar toggle `ActionBar` | `titlebar::Titlebar` | ✅ |\n| 原生窗口控件占位 | `zeta-winit::WindowControlInsets` | 委托；Titlebar 只增加自身内容间距 |\n| 通用 Tab surface 与横/纵排列 | `zeta-ui::Tab` / `TabList` | 委托；不拥有 Session content 或 tabpanel |\n| 多行编辑、caret/selection、find/replace、自动缩进、undo/redo、IME 与 syntax projection | `zeta-editor::CodeEditorDocument` / `CodeEditor` | 委托；Composer 与中心文件 Editor 都只转交平台输入，editor 内部管理文本、搜索替换、缩进、parser/revision/token、fold 与 hit-test |\n| 文本差异计算、行映射与字符级范围 | `zeta-diff::DiffDocument` | 委托；Native 不复制 diff 算法 |\n| 单列/并排及多文件只读差异展示 | `zeta-editor::DiffEditorDocument` / `DiffEditor` / `MultiDiffEditor` | 委托；Native 只按文件扩展名选择 language，editor 内部维护两侧 parser/revision/token；Changes pane 使用窄栏 Unified presentation |\n| 可折叠 Session Sidebar、名称搜索与多 Session Tab | `session_sidebar_toolbar::SessionSidebarToolbar` / `session_search::SessionSearch` / `session_tab_list::SessionTabList` | ✅；Add 通过 App Server 创建 Session/Thread，tab 激活切换 Thread projection，并绑定独立 Terminal PTY |\n| 可折叠 Agent Sidebar 产品组合 | `zeta-agent-sidebar` 的 `AgentSidebarNavigation` / `files::FilesToolbar` / `FilesPane` / `scm::EditorPane` | 委托；Native 只负责外层显隐、主题映射和 action 执行 |\n| 锚点浮层定位与独立 layer 合成 | `zeta-ui::ContextView` | 委托；不拥有显示生命周期或输入路由 |\n| 无边框下拉 surface、可选 header、item geometry 与默认选择 | `zeta-ui::Dropdown` | 委托；不拥有产品查询、选择 identity、关闭或 command |\n| 柔和阴影、2px menu padding、4px radius、item geometry 与默认选择 | `zeta-ui::ContextMenu` | 委托；不拥有 Session identity、关闭或 command |\n| Session Tab 右键菜单、关闭策略与 action 映射 | `session_context_menu::SessionContextMenu` / `SessionContextMenuState` | ✅；四个 action 已进入产品边界，Pin/Close/Rename/Fork mutation 尚未执行 |\n| 共享主题加载与平台中立 snapshot | [`zeta-theme`](../zeta-rs/theme/README.md) | 委托；选择数据化 `zeterm` 默认入口，并与 Desktop/TUI 消费同一 manifest 和用户主题 JSON |\n| Native Shell/CodeEditor/Diff/Terminal 主题投影 | `shell_style::ShellPalette` 与命名 component palette | ✅；启动时加载一次，失败回退内置浅色 palette |\n| 稳定控件身份与 shell action 映射 | `shell_interaction` | ✅ |\n| 稳定 product command identity、request 与注册式执行入口 | `zeta-commands::{ZetermCommandId, CommandRequest, CommandRegistry}` / `NativeApp::dispatch_command` | ✅；pointer、menu 与 shortcut 汇合到宿主注册的 handler |\n| 平台按键转换、Chord 生命周期与当前 context | `keybindings::NativeKeybindings` | ✅；1.5 秒超时，失焦或 IME 事件取消 |\n| 用户快捷键资源、验证与热更新 | `zeta-keybindings-host::KeybindingsResource` | ✅；读取 `<ZETA_PROFILE_ROOT>/keybindings.json`，坏更新保留上一份有效规则 |\n| 快捷键模型、设置、录制与 Chord 提示 | [`zeta-keybinding`](keybinding/README.md) | 委托；zeterm 提供命令行、事件 adapter 与保存接线 |\n| 平台无关快捷键模型、规则顺序与冲突解析 | [`zeta-keybinding`](keybinding/README.md) | 委托 |\n| 命中、hover/press/capture、focus、键盘导航、cursor 与 accessibility semantics | `zui` | 委托 |\n| accessibility semantics → 平台屏幕阅读器 | 尚无 native adapter | 尚未完成 |\n| Transparent native chrome 与窗口拖动 adapter | `zeta-winit` | 委托 |\n| ANSI parser、terminal grid 与 BlockList | `zeta-terminal::TerminalCore` | 委托 |\n| 默认 shell PTY、output/exit event、write 与 resize | `terminal_session::TerminalSession` | ✅；PTY 创建在后台 worker 完成，UI 只接管 ready runtime |\n| Session Tab 到本地 PTY 的一对一 binding、活动/非活动 runtime 切换 | `terminal_workspace::TerminalWorkspace` | ✅；pending key、乱序 ready 和非活动 runtime 由 Native adapter 管理，不拥有 App Server Session/Thread authority |\n| Agent ThreadTimeline + fixed Agent/Shell Composer | `shell_scene` / `thread_timeline` / `composer_panel` / `agent_composer` | ✅ |\n| Composer 面板几何、信息栏与可展开交互 Pane 的位置 | [`zeta-composer`](composer/README.md) / `composer_panel` / `shell_scene` | ✅；`zeta-composer` 只解析 panel、固定行、interaction bounds 与 list scroll geometry，Native 负责状态、绘制和 scene 接线 |\n| Composer active View 与滚动状态 | `composer_interaction::ComposerInteractionModel` / `composer_interaction_pane::ComposerInteractionPaneState` / `zeta-ui::{ScrollView,ListView}` | ✅；Pane 只宿主 Slash 与 `/model` active View 并保留 viewport offset，通用 UI 基座负责裁剪、滚动与可见范围；Shell completion 不使用该 Pane |\n| App Server Session adapter 与 transient Thread projection / stream-gap recovery | `agent_session` / `thread_projection` | ✅；Thread 只作为 App Server 内部状态的 Native projection，不形成独立 crate |\n| durable direct Shell Turn | App Server `session/request::StartShellTurn` / Core `StartShellTurn` | ✅ |\n| 独立交互式 Terminal Surface | `workspace_surface` / `terminal_session` / `terminal_input` | ✅；`Cmd/Ctrl+J` 切换 |\n| Composer/File Editor/Session Search/File Search/Workspace Path Search/Git Branch Search IME target、事件转换、启停、composition lifecycle 与候选框同步 | `input_method` | ✅ |\n| Bottom Widget 底部的 Local/cwd/branch/Changes context toolbar | `input_context_toolbar::InputContextToolbar` / `workspace_path_picker` / `git_branch_context_menu` / `workspace_context::WorkspaceContext` | ✅；cwd 复用带 Search Box header 的 `Dropdown`，branch 复用带同类 header 的 `ContextMenu`，分别切换工作区投影和本地分支；Changes 显示 changed path 数与文本 `+addition -deletion`，点击后刷新并展开 Changes Pane |\n| shell bootstrap、host-owned command submit 与 zsh completion marker | `terminal_session::TerminalSession` | 部分具备 |\n| terminal query reply → PTY write | `TerminalCore::take_reply_bytes` / `TerminalSession::handle_event` | ✅ |\n| alternate-screen mouse cell mapping、button state 与 PTY report | `terminal_pointer::TerminalPointer` / `TerminalCore::encode_mouse` | ✅ |\n| 主屏滚轮浏览、输出增长锚定与 Block/grid 视口投影 | `terminal_scrollback::TerminalScroll` / `terminal_output_scroll_view::TerminalOutputScrollView` / `zeta-ui::ScrollView` | ✅；Terminal 保留底部相对行锚定，通用基座负责裁剪、内容坐标和滚动条绘制 |\n| 主屏拖拽选择、selection paint 与 cell-aware text extraction | `terminal_selection::TerminalSelection` | ✅ |\n| system clipboard copy/paste 与 bracketed-paste routing | `terminal_selection` / `terminal_input` | ✅ |\n| OSC title → Terminal Surface / native window title | `TerminalCore::title` / `NativeWindow::set_title` | ✅；后台 terminal title 不覆盖 Agent Session title |\n| 完整 TUI compatibility | 尚无完整 owner | 尚未完成 |\n| App Server session 与进程内 Thread projection | `agent_session` / `thread_projection` | ✅；Native 通过 `SessionStateMode::Ephemeral` 使用 application boundary，Session/Thread/用户消息只在当前 zeterm 进程内存在 |\n\n依赖方向：\n\n```text\nzeterm → zeta-winit\n            → zeta-renderer → zui\n            → zeta-wgpu → zeta-renderer\n                        → zeta-winit\n                        → zui\n            → zeta-ui → zui\n            → zeta-commands\n            → zeta-keybinding\n            → zeta-app-server-client\n            → zeta-protocol\n            → zeta-terminal\n            → zeta-text-file\n            → zeta-diff\n            → zeta-editor → zeta-ui → zui\n                          → zeta-diff\n                          → zeta-syntax\n            → zeta-theme\n            → zeta-file-search\n            → zeta-slash-commands → zeta-app-server-protocol\n            → zeta-utils-pty\n```\n\n`zeterm` 可以拥有可丢弃的 presentation state 和产品交互，但不能复制 Session、Thread、\nTurn 或 Tool 的权威状态机。当前通过 `zeta-app-server-client` 的 typed contract 订阅与提交。\n\n## 产品方向与当前边界\n\nNative App 是 Agent-first Console；Terminal 是显式切换的 compatibility Surface，不是 Thread\nauthority。下表把仍保留的历史 shell vocabulary 映射到当前产品语义：\n\n| 当前源码 | 当前能力 | 目标产品语义 | 状态 |\n| --- | --- | --- | --- |\n| `titlebar::Titlebar` | 窗口拖拽区和左右 sidebar toggle `ActionBar`；不绘制标题文案 | Top Bar | ✅ |\n| `zeta_layout::RootLayout` | 用 `GridLayout` 解析固定 Product leaf 与可选 Inspector leaf | Native window 根布局 | ✅；窗口扩展后 Inspector 获得独立 360px sibling leaf，Product bounds 不变 |\n| `ShellLayout` | 组合 titlebar、可选 Sessions sidebar，并把剩余区域交给 `TerminalWorkspaceLayout` | Top Bar 与 Workspace 外部布局 | ✅；Sessions 使用外层单轴 split |\n| `zeta_layout::TerminalWorkspaceLayout` | 用 `GridLayout` 投影活动终端与可选 Agent Sidebar Leaf bounds | Workspace Pane geometry adapter | ✅；尚无多 Terminal Pane Tree；Session/Thread 与 Session-to-PTY switching 由 Native/App Server adapter 负责 |\n| `zeta-commands` / `command_dispatch` / `keybindings` / `keybindings_resource` / `keyboard_shortcuts` | 把 pointer/menu 的 `ElementId` 与标准化键盘事件映射到同一 `CommandRequest`，再交给 `CommandRegistry` 的宿主 handler；向 `zeta-keybinding` 提供命令行、稳定 identity 和保存 adapter | Product command 与快捷键输入层 | ✅；支持完整 `when` 表达式、冲突/错误诊断、最多四段 Chord 与 keycap UI |\n| `shell_scene` / `thread_timeline` | Agent Surface 绘制 canonical Thread items；Terminal Surface 绘制活动 grid | Agent Workspace / Terminal compatibility | ✅ |\n| `layout_inspector` | `Cmd/Ctrl+Shift+I` 开关检查面板，面板 cursor action 显式开关选取，点击锁定最深检查节点，Escape 先停止选取再关闭 | Native UI layout inspection | ✅；原生窗口向右扩展独立层级面板，自动显示 ancestor、authored row/column/width/height、computed size/padding/gap/radius、layer 与源码位置 |\n| `composer_editor` / `agent_composer` / `composer_interaction` / `composer_interaction_pane` / `composer_panel` / `terminal_input` | Compact `CodeEditor` 共享 Agent/Shell 多行文档；`zeta-input-classifier` 统一提供路由和 Shell completion；Shell 候选收敛为光标后的单条 ghost text，Tab 接受、Escape 隐藏；active View model 只接管 Slash/模型选择；`zeta-composer` 与 zeta-ui list 基座拥有几何和滚动 | Agent Composer、Slash/模型选择、Shell 补全与 explicit Shell Turn | ✅ |\n| `input_context_toolbar` / `workspace_path_picker` / `git_branch_context_menu` | `ActionBar` 排列 mode、Local、cwd、branch 与 `Changes files • +additions -deletions`；cwd 组合 `Dropdown`，branch 组合 `ContextMenu`，两者均使用各自通用 header slot | Composer context toolbar | ✅；两个浮层第一行均默认聚焦 Search Box；目录或分支切换后替换 Files 根、文件搜索索引和 Git/Changes projection；Changes action 刷新 Git projection、展开右栏并选择 Changes Pane |\n| `session_tab_list` | 组合 `zeta-ui::TabList` 投影 App Server Session；自身拥有白色状态容器、会话名和工作区两行截断信息，以及纵向 TabList/selected Tab 语义 | 多会话导航 | 已支持动态创建和切换；每个 Session Tab 绑定独立 Terminal PTY |\n| `session_sidebar_toolbar` / `session_search` | 整行组合 `SearchBox` 与右侧 `ActionBar`；按 session name 执行大小写不敏感过滤，并把 Add 暴露为稳定 action | Session 搜索与新建入口 | 搜索、新建 Session/Thread 和新 tab projection 已接通 |\n| `session_context_menu` | 右键当前 Session Tab 后，用 `ContextMenu` 呈现 Pin、Close、Rename、Fork；拥有 outside click、Escape、键盘导航和焦点恢复 | Session action surface | 通用基座提供柔和阴影、2px padding 与 4px radius，打开默认选择 Pin；真实 mutation 仍未执行 |\n| `zeta-editor::CodeEditor` | 多行 Unicode 编辑、selection、history、IME/syntax projection、Document/Compact presentation、soft wrap 与可见行绘制 | CodeEditor | 委托；Composer 与文件 Editor 都已接入平台事件、focus、caret blink、IME、pointer caret/drag、clipboard 和垂直 viewport；文件 Editor 启用 soft wrap 与拖选越界自动滚动 |\n| `zeta-agent-sidebar::AgentSidebarNavigation` | 只负责 Changes/Files 的跨功能切换和导航语义 | Agent Sidebar navigation | ✅；不拥有 Files/SCM 功能布局 |\n| `zeta-agent-sidebar::files::FilesLayout` / `FilesToolbar` / `FilesState` / `FilesPane` | Files 自己拥有 36px 功能 toolbar、Refresh、ahead/behind、Search、文件树与搜索结果；Native 只通过 App Server 适配 `DirectoryEntry` | Files Pane | ✅；pointer/Enter/Space 产生 crate action，由宿主加载目录或打开文件 |\n| `zeta-agent-sidebar::scm::ScmLayout` / `ScmState` / `EditorPaneState` / `EditorPane` | SCM 自己拥有 Changes toolbar slot、changed-file snapshot、language-aware `DiffEditorDocument`、整体滚动位置、scrollbar pointer capture/animation、每文件 `DiffEditorState` 和 `MultiDiffEditorLayout` | Changes Pane | ✅；Native 只提供 `ScmDiff` 和主题投影 |\n| `zeta-editor::DiffEditor` / `MultiDiffEditor` | DiffEditor 提供 SideBySide/Unified presentation 与未修改区间折叠投影；MultiDiffEditor 再纵向组合多个文件 section、发布每文件 fold identity 并裁剪不可见项 | 多文件差异文档 | Changes 固定宽度栏显式选择 Unified；文件读取、diff 计算、持久状态与产品输入路由不属于 editor crate |\n| terminal grid / PTY / scrollback | grid、PTY 与会话内有界回滚已接通，跨重启持久化尚无 | 活动 Terminal Session runtime | 部分具备 |\n| multi-session projection / switching | App Server Session/Thread 的创建、动态 tab projection 和切换已接通；每个 Session Tab 绑定独立本地 PTY，切换时保留各自 TerminalCore 与 shell 进程 | 多会话入口 | 已实现；Session/Thread/用户消息不跨 zeterm 重启恢复，Session action mutation 与 Terminal Pane 分屏仍是当前限制 |\n\nCodeEditor/DiffEditor/MultiDiffEditor 的实现 ownership、`DiffSideRows` 投影、显示列 contract、\n测试和当前限制由 [`zeta-editor` README](editor/README.md) 维护。zeterm 负责 changed-file\ncollection、文件 identity、整体滚动位置和每文件 `DiffEditorState`；MultiDiffEditor 只借用这些\n快照完成多文件组合。Native 不能复制代码行或 diff decoration 绘制。当前 Git binding 跳过\nbinary、非 UTF-8 与单侧超过 2 MiB 的文件；index-only 对比仍是后续接线。\n\n当前已用 `zui::SplitViewLayout` 与 `zeta-ui::Sash` 支持 Sessions/Main 的单轴 resize，并用\n`zui::GridLayout` 作为 Terminal Workspace 的递归几何入口。当前 Grid 输入只有一个可见活动\n终端 Leaf，因此窗口 resize 仍从这一 Leaf 的 logical viewport 计算 rows/columns，再把同一\n尺寸发送给所有绑定的 terminal grid 和 PTY。多 Pane 还需要 native 拥有 Pane Tree、逐 Pane\n`TerminalSession`/scroll/selection/composer 状态以及 split command；这些不能由几何层伪造。\n\n## 当前执行路径\n\n```text\nmain\n  → zeta_winit::run_application\n  → NativeApp::resumed\n      → NativeWindow::create\n      → NativeWindow::window_control_insets\n      → build_shell_presentation\n      → WgpuRenderer::initialize\n      → request_redraw\n  → NativeApp::window_event\n      → resize / scale-factor update → rebuild scene\n          → TerminalWorkspace::resize_all → each TerminalSession::resize → TerminalCore + PTY\n      → AgentSessionEvent::Snapshot/Update → SessionTab/TerminalWorkspace binding + ThreadProjection\n          → unknown Session reserves a terminal key and starts background PTY creation\n          → TerminalReady → adopt the runtime; output/exit events route by terminal key\n          → ThreadTimeline\n          → committed update / stream gap → session/subscribe refresh\n          → transient Agent/Tool delta → rebuild scene\n      → TerminalSessionEvent::Output(key) → bound TerminalCore::process_output → rebuild scene when active\n          → terminal query → take_reply_bytes → TerminalSession::send_input → PTY\n      → TerminalSessionEvent::Exited → TerminalCore::mark_process_exited → rebuild scene\n      → cursor / primary mouse event → titlebar drag 或 terminal cell mapping\n      → Terminal Surface pointer → cell mapping / TerminalPointer / TerminalSelection → PTY\n      → Agent Surface wheel → ThreadTimelineScroll → redraw\n      → keyboard → NativeKeybindings → zeta-keybinding::KeybindingResolver\n          → PendingChord → 1.5s deadline；失焦、超时或 IME 清空\n          → CommandRequest → CommandRegistry handler / focused input / PTY\n          → NoMatch → focused control navigation/editing → Terminal Surface PTY fallback\n      → about_to_wait → KeybindingsResource 轮询 `<ZETA_PROFILE_ROOT>/keybindings.json`\n          → 完整验证成功 → 原子替换 Builtin + User 规则\n          → 读取或解析失败 → 保留上一份完整规则并输出诊断\n      → input_method → IME preedit/commit/cancel → AgentComposer\n          → Agent Enter → App Server session/request::StartTurn\n          → Shell Enter → App Server session/request::StartShellTurn\n          → composer caret bounds → native IME candidate area\n      → pointer → zui::UiDispatch\n          → InteractionFrame reverse-order hit-test\n          → hover / press / capture / focus → presentation rebuild\n          → UiIntent → window drag 或 product action\n      → Cmd/Ctrl+Shift+I → LayoutInspector\n          → InspectionFrame layer-aware reverse-order hit-test\n          → 原生窗口向右扩展 hierarchy panel；产品 content viewport 保持不变\n          → ancestor bounds + padding overlay；pointer 不再进入产品 UI\n      → current Session Tab secondary click\n          → SessionContextMenuState → ContextMenu → ContextView overlay\n          → pointer / Up / Down / Tab / Enter / Space\n          → Pin / Close / Rename / Fork product action boundary\n          → outside click / Escape → dismiss + focus restoration\n      → Sessions toolbar\n          → SearchBox keyboard / IME → SessionSearch → session-name filter\n          → Add icon → ADD_SESSION product action boundary\n      → Sessions Sash pointer\n          → SessionSidebarState drag snapshot\n          → SplitViewLayout resize constraints\n          → shell bounds + terminal grid/PTY resize\n      → Agent Sidebar toggle\n          → AgentSidebarState visibility\n          → shell bounds + terminal grid/PTY resize\n          → AgentSidebarNavigation → Changes / Files selection\n              → FilesLayout → FilesToolbar + 根目录树 / 模糊路径匹配结果\n              → ScmLayout → MultiDiffEditor → visible file sections → DiffEditor\n      → zeta_layout::TerminalWorkspaceLayout\n          → GridLayout → active terminal + optional Agent Sidebar leaf bounds\n          → terminal rows/columns → TerminalSession resize\n      → Tab / Shift+Tab / Arrow keys → unified focus navigation\n          → Enter / Space → focused action activation\n      → alternate keyboard / paste → TerminalCore encoding → PTY\n      → input_method → IME commit → TerminalCore encoding → PTY\n      → titlebar drag hit → NativeWindow::start_window_drag\n      → visible-after-occlusion → request redraw\n      → Box<dyn Renderer>::render_scene\n          → UiScene::batches（严格保留跨 primitive paint order）\n          → selected backend (`WgpuRenderer` currently)\n```\n\n运行：\n\n```bash\njust zeterm\n\n# Without just:\ncargo run --manifest-path Cargo.toml -p zeterm\n```\n\n`shell_scene::ShellLayout` 把 titlebar 下方 body 先交给 Sessions/Main 横向\n`SplitViewLayout`，再把剩余区域交给 `zeta_layout::TerminalWorkspaceLayout`；\n后者通过 `GridLayout` 同时投影活动 Terminal Leaf 和可选的右侧 Agent Sidebar Leaf。当前活动\nTerminal Leaf 再分成上方 output viewport 与固定底部 composer；alternate screen 临时使用完整\n活动 Terminal Leaf。\n`SessionSidebarState` 保存 visibility、preferred width 与当前 `SplitViewResizeSnapshot`，\nviewport 临时约束只改变 effective width，不覆盖 preferred width。`Sash` 从\n`SplitViewSashLayout::track_bounds` 同源计算 drag target 与 hover/active feedback；\n`AgentSidebarState` 保存显隐、preferred width 与当前 `SplitViewResizeSnapshot`，并向外层 Grid\n提供右侧 Sash 所需的 pane sizing；宽度限制为 240–560px，始终为 main Pane 保留至少 240px；\n`TerminalWorkspaceLayout` 解析右栏 Leaf bounds；`files::FilesLayout` 和 `scm::ScmLayout`\n分别把各自的功能 bounds 解析为 36px toolbar slot 与 active content pane；跨功能的\nChanges / Files 切换只由 `AgentSidebarNavigation` 负责。\n`zeta-agent-sidebar::AgentSidebar` 保存 Files / Changes 选择；`files::FilesState` 保存文件搜索、\n树与滚动，`scm::ScmState` 保存 changed-file snapshot 和 `EditorPaneState`。Native 的\n`AgentSidebarWorkspace` 只负责将 App Server/`WorkspaceContext` 快照适配为 crate 类型，并执行\nRefresh、文件打开和子目录加载动作。Refresh、Composer Changes action 和 shell command completion\n通过 App Server `git/textDiff` 重建上游领先/落后距离、HEAD/working-tree `DiffDocument` 与增删行统计；\nNative 按 path 选择 `CodeEditorLanguage` 后立即包装为 `DiffEditorDocument`，此后 parser/revision/token\n都留在 editor crate。\nFiles pane 的层级模式由 `files::FilesTree` 保存 arena、parent/children、稳定 mounted node ID\n和展开状态；根目录和首次展开的子目录都由 `AgentSession` 通过 App Server\n`fs/readDirectory` 读取 workspace-relative 直接子项，Tree model 不直接访问文件系统；收起/再次展开\n复用已加载 children。App Server `fs/changed` 通知会重新读取根目录，当前仍不恢复刷新前的展开状态。\n`zeta-ui::TreeView` 在 24px 固定行高 ListView 上投影 depth、disclosure 和 content geometry，\n只为 visible range 注册 `Tree`/`TreeItem` accessibility node，paint 使用两行 overscan。\nSearch 结果仍是扁平 List/ListItem。滚轮更新 workspace-owned `ScrollState`，查询、Refresh 或\nworkspace 替换会回到顶部。文件打开已接入中心 Editor Surface；重命名、拖放与刷新后的展开状态\n恢复尚未接入。\n`EditorPaneState` 保存整体 `zeta-ui::ScrollState` 和每文件 `DiffEditorState`；\n`MultiDiffEditor` 在一个纵向文档中连续绘制所有可见文件 section，每段再复用\n`DiffEditorPresentation::Unified` 将删除/新增行投影为单列内容。section 高度由\n`zeta-ui::VirtualListLayout` 建立 prefix index，paint 与 fold-control 查询通过二分只访问\nviewport 相交的文件；展开/收起改变高度时由 `EditorPaneState::remeasure` 重建该 snapshot。\n长未修改连续区间默认只保留变更前后各三行上下文；`DiffEditor` 发布 Show/Hide fold control，\n`EditorPane` 将其注册为支持 pointer、Tab focus 和 Activate 的 Button，并把激活结果写回对应文件\n的 `DiffEditorState`。普通 CodeEditor 不决定 diff 折叠规则。\n指针位于 MultiDiffEditor 时，wheel 会更新其有界整体纵向 offset，不落入 Terminal scrollback。\n高频 PixelDelta 和 scrollbar drag 只累计 retained offset，并把 presentation 重建合并到下一次\n`RedrawRequested`；同一事件循环批次不会为每个 delta 同步重建整个 Shell scene。\n通用的 `Render < Fragment < Rebuild` 失效等级、Scene checkpoint 和 fragment 原地替换契约由\n`zui` 拥有；Native 只把产品状态变化映射到失效等级，并定义 Shell 的 base/overlay retained boundary。\n`shell_scene` 只注册产品 `ElementId`、cursor 和 separator semantics。`NativeApp` 每次重建\npresentation 时从 `NativeWindow::window_control_insets` 读取 host chrome 占位，并通过\n`ShellPresentationModel` 交给 `titlebar::Titlebar`；Titlebar 在占位外再增加自己的 `8px`\n内容间距。窗口控件宽度和所在边不能进入 `ActionBar` 或 `zeta-ui`。\n`shell_interaction` 只声明产品稳定 `ElementId` 并把 context action 映射回产品命令；实际\nsidebar state 由 `session_sidebar` 定义并由 `NativeApp` 保存，它不保存 hover 或 focus。\n`command_dispatch::command_request_for_element` 和 `NativeKeybindings` 分别把 pointer/menu entry\npoint 与标准化键盘事件映射到 `CommandRequest`；`NativeApp` 启动时通过\n`command_dispatch::builtin_command_registry` 注册完整产品命令目录，`NativeApp::dispatch_command`\n从注册表取出 handler，是唯一产品执行入口。`KeybindingsResource` 每秒检查 `<ZETA_PROFILE_ROOT>/keybindings.json`，只在完整资源通过\n大小、字段、按键、条件和命令校验后替换 User 规则；内容无效时保留上一份有效规则并把诊断\n显示在快捷键设置页。`zeta-keybinding::KeyboardShortcutsState` 录制最多四段按键，暂停一秒后\n把 commit 交回 Native 资源层原子写入；设置浮层、深灰 keycap 与 Chord 提示也由同一快捷键\ncrate 拥有。Native 的 `keyboard_shortcuts` 只分配产品 `ElementId`、投影\n`ZetermCommandId` 行并连接保存结果。\n`zeta-keybinding` 解析平台无关按键和 `when` 表达式，不读取 focus、不执行命令，也不拥有\nChord timer。\n`session_context_menu` 用 `SessionContextMenuState` 保存当前目标、锚点和待恢复焦点，用\n`zeta-ui::ContextMenu` 组合 ContextView 定位、renderer BoxShadow、2px menu padding、4px\nradius 和纵向 ActionBar item geometry。打开时默认选择第一个 enabled item `Pin`；菜单子树会\n成为当前 interaction frame 的 modal scope，hover 同步 roving focus，移出后保留最后一项，\n下层控件不再接收 pointer、focus 或 activation。右键当前真实 Session Tab 打开菜单；菜单外\n左键、Escape 或窗口失焦关闭菜单，方向键、Tab、Enter 和 Space 复用统一 focus/activation 路径。\n`SessionContextMenuAction` 已将 Pin、\nClose、Rename、Fork 映射为产品 command identity；App Server Session/Thread 已支持创建和切换，\n每个 Session Tab 也已绑定独立 PTY，但这些 command 尚不执行 pinning、关闭、重命名或 fork，\n不得由 presentation state 伪造结果。\n`zui` 是跨 native 组件的通用交互运行时：\n`InteractionFrame` 按 scene 构建顺序注册有父子关系的 `UiNode`，反向命中最上层节点，并投影\naccessibility role、label、bounds 与 focused state；`UiDispatch` 跨 frame 保存\nhover path、press/capture 和 focused identity，最后只返回 `UiIntent`。\n\n标题栏、侧栏开关、会话 `SessionTabList`、Session 右键菜单、通用 `zeta-ui::TabList`、\nThreadTimeline、Terminal Surface、`ComposerPanel`、compact `CodeEditor`、`ActionBar` 和五个 Composer\n上下文 `Button` 都走这条路径。\nComposerPanel 使用默认指针，输入与终端文本使用文本指针，Button 使用 pointer 指针；绘制、\n输入上下文 toolbar 的命中与语义 bounds 共享 `ActionBar::interactive_item_bounds`，Session\n菜单则共享 `ContextMenu::interactive_item_bounds`。Agent Surface 默认聚焦 composer，Editor Surface\n打开文件时聚焦 active document；\nTab/Shift+Tab 遍历全部 tab stop，toolbar 内左右键移动相邻 Button，Enter/Space 激活焦点\naction，Escape 从 action 返回 composer。pointer press 同时更新 focus，release 按 capture identity\n决定是否激活。`input_method::InputMethodTarget` 合并窗口活动状态、Workspace Surface 与 focus；\n焦点离开当前文本输入或窗口失焦时取消对应 composition 并停止 caret blink，Terminal Surface\n仍保留直接输入所需的 IME。过小 viewport 使用有边界的 compact fallback。\n\n`ShellPresentation::accessibility_nodes` 已保存当前 frame 的语义快照，但 `zeta-winit` 尚未提供\nAccessKit 或各平台原生 accessibility adapter，因此当前不能声称 VoiceOver、Narrator 或\nOrca 已能读取这些节点。后续 adapter 只能发布现有语义树和 focus，不得另建第二套控件身份。\n`ShellPresentation` 由单一 `UiFrame<InteractionFrame>` 保存 scene 与 interaction，再保存\naccessibility projection；只有 `UiScene` 被传给 `Renderer`，因此 GPU backend 不承担 hit-test、focus、\ncommand dispatch 或 accessibility ownership。新增组件组合必须使用 `UiFrame::draw_component`，不能\n重新引入 `ShellPresentation` 的平行输出字段。\n\n`layout_inspector::LayoutInspector` 是独立的检查工具 presentation state。`Cmd/Ctrl+Shift+I` 开启后，\nNative 先保存当前产品 content width，再通过 `NativeWindow::request_inner_logical_size` 向右扩展\n`360px`；Shell、Terminal grid 与产品 hit testing 继续使用保存的 content viewport，因此检查面板不会\n挤压或重排被观察布局。`zeta_layout::RootLayout` 把保存的 Product viewport 和新增宽度交给\n`GridLayout` 解析为两个真实 sibling leaf；`InspectorPanel` 在 Inspector leaf 的 layer 0 内组合\n`InspectorToolbar` 与 `InspectorContent`，不再作为 scene overlay。Toolbar 与产品 Titlebar 同高，左侧\ncursor action 显式开关选取，右侧 close action 关闭面板并恢复原窗口宽度；Content 只拥有层级行、\n指标与节点切换。面板打开时默认不进入选取状态，产品 content 继续接收正常 pointer、keyboard 和\ncursor feedback。选取期间 Native 才截获产品输入，按当前\n`UiScene::inspection` 反向选择最深节点；左键释放后锁定当前链并自动退出选取，保留检查结果。Escape\n先停止选取，再次按下才关闭面板并请求恢复原窗口宽度。Inspector 用橙色显示 padding、青绿色显示组件\n上报的实际 gap 区域、蓝色显示当前\nbounds、紫色显示 ancestor bounds，并在右侧面板按真实 parent chain 显示每层的组件名、size、\npadding、gap、radius、scene layer 与源码位置。只有这些产品节点的 outline/padding highlight 使用 overlay。\n层级行可以直接重定向当前目标；面板保留完整 path，左侧只\n强调所选节点及其祖先，因此可在 parent 与原 descendant 之间反复切换。它不复用 `InteractionFrame`\n推断样式，因为交互树不覆盖纯视觉组件；也不修改组件 style 或产品 reducer。\n每个组件通过 `Component::element` 声明布局，由 `UiScene::draw_component` 解析一次\n`ComputedElement` 并自动建立 parent chain。`SessionSidebar`、`ComposerPanel` 这类非组件 composition\n函数以及 `ContextView::draw` 这类 content-closure surface 使用 `UiScene::with_element` 进入相同管线；\n产品代码不再手写检查元数据。\nNative 当前所有拥有稳定 bounds 的 Component 均已接入，包括 Titlebar、两个 Sidebar Toolbar、\nComposerPanel/ComposerInfoBar、ComposerEditor、ThreadTimeline、Explorer/Changes Pane、Session Tab、\n快捷键与两个选择浮层。选取期间的十字 cursor 只覆盖产品 content viewport；右侧 Inspector panel\n始终使用普通 cursor，只消费自身 action 与 panel pointer，不参与组件选中或锁定。\n`component_composition_tests::product_composition_uses_scene_draw_component` 扫描非测试产品源码，阻止\n新增代码绕过 canonical composition 入口而静默丢失 inspection ancestor。\n\n`composer_editor::ComposerEditor` 保存 `CodeEditorDocument` 与 retained viewport；\n`agent_composer::AgentComposer` 在其上拥有显式 Agent/Shell mode 和 Shell history。\nComposer Interaction Pane 是可展开、可收起的 presentation host，不定义或拥有一套通用\ninteraction model。`composer_panel` 只放置 active View，`ComposerInteractionPaneState` 只保留\nviewport offset，`zeta-ui::ScrollView` / `ListView` 统一处理 content geometry、裁剪、滚动条与可见\n范围；这些层都不解释挂载的是 Slash、Model、Plan 或其他 View。Slash catalog、输入 grammar、过滤、\n选择和 dismiss 委托给 `zeta-slash-commands`，滚动由 Native renderer 保留。当前输入 `/` 时由共享\n状态提供 active View 并按初始化快照过滤命令；选择 `/model` 后压入模型列表。Escape 从子 View\n返回上一层，在根 View 时使 Pane 收起。\n非 Slash 输入由 `AgentComposer` 从\n[`zeta-input-classifier`](../zeta-rs/input-classifier/README.md) 持有的同一 Shell context 请求候选；\ncommand、subcommand、option、工作区 target 与 path 候选不会打开 Pane。`AgentComposer` 在光标位于\n输入末尾时把唯一候选的剩余文本，或多个候选仍可确定的最长公共前缀，投影为 `CodeEditor` 光标后的\n灰色 ghost text；没有可确定的下一段时不显示。Tab 通过 `CodeEditorTextEdit` 接受该段并重新运行自动\n路由，Escape 在输入再次变化前隐藏建议。Shell parser、command registry、PATH/manifest 扫描与 alias 展开仍由\n[`zeta-shell-completion`](../zeta-rs/shell-completion/README.md) 拥有，Native 不复制判断规则；相关\nworkspace 文件变化时只触发该 context 的候选快照刷新。\n`zeta_composer::ComposerPanelLayout` 组合“交互区 → 信息栏 → editor → toolbar”；信息栏固定显示\n当前 mode 的提示，toolbar 固定在底部。交互区出现时只增加底部 Panel 高度并向上压缩\nThreadTimeline，信息栏、editor 与 toolbar 的位置保持不变。模型列表来自 typed\n`model/list`，选择结果通过 `session/request::SetModel` 写入当前 Session；UI 不复制模型目录或伪造\nSession model。\n`terminal_input` 把普通 key 和 paste 路由到 Composer；临时 View 可见时优先消费方向键、Enter、\nTab 和 Escape；否则 Enter 分别提交 `session/request::StartTurn` 或\n`session/request::StartShellTurn`，Shift+Enter 插入换行。Composer 从紧凑基线自动增长到八行，之后由\nviewport 跟随 caret。Terminal Surface 的输入经过\n`TerminalCore::encode_key/encode_paste` 直接写入 PTY。`input_method` 单独把 IME 路由到当前\n`InputMethodTarget`：Agent Surface candidate area 跟随 composer caret，Editor Surface 跟随 active\nCodeEditor caret，Terminal Surface 跟随 grid cursor。`file_editor_input` 只把标准化 keyboard、IME、\npointer、clipboard 与 wheel 操作转交给 active `CodeEditorDocument`/`CodeEditorViewport`。\n`FileEditorPane` 启用 editor-owned soft wrap，并把 `CodeEditor::navigation()`、visual row count 与\ncaret visual row 原样交回 host；`file_editor_auto_scroll` 只在 pointer 拖选进入上下 8px 边缘区或\n越过边界时以 35ms deadline 推进一个 visual row，pointer 回到内部、释放、失焦或到达文档边界后立即停止。\n关闭按钮先由 `FileEditorHost::request_close_active` 判断 dirty 状态；未修改 Tab 直接关闭，dirty\nTab 由 `FileEditorInputState` 打开 modal 决策条并提供 Save、Don't Save 与 Cancel。磁盘外部变化由\n`TextFileLifecycle::status` 投影为 reload/conflict 提示条：Reload from Disk 显式替换 editor text，\nOverwrite 使用待处理 snapshot 的磁盘版本再次执行 optimistic preflight，若文件再次变化仍拒绝写入。\n`workspace_context::WorkspaceContext` 在 Session 启动时捕获真实 cwd，并消费\n`zeta-app-server-client` 返回的 workspace-scoped `GitTextDiffResult`；\n`InputContextToolbar` 消费 Composer mode 与四项 context value，使用 `ActionBar` 统一排列并把每项语义交给\n`ActionBarButton::icon_and_label` / `Button`，以及 Files pane 的文件行。`IconLabel` 在 Button\n和 Files pane 内部完成 icon/text placement，不作为 Toolbar 的直接 action representation。\nToolResult durable commit 后刷新\nbranch 与 `Changes files • +additions -deletions`；五项 Button 已接入 hover、press、focus、\n键盘导航和 pointer feedback。cwd Button 使用 product-owned `WorkspacePathPickerState` 组合\nmodal `Dropdown`；第一行通过 header slot 承载默认聚焦的 `SearchBox`，输入按当前目录的直接\n子目录名称实时过滤；显式输入 `../path`、`~/path` 或绝对路径时，可直接选择已存在目录。Git\nprojection 的 repository-relative `workspacePath` 用于还原并固定展示 repository root 快捷项，\n不在协议中传递 host 绝对路径。进入子目录后清空查询，同时支持父目录、排序子目录、固定搜索\nheader 下的结果滚动、鼠标滚轮、键盘焦点自动滚入视野、clipboard、IME、Escape/外部点击关闭和\nroving keyboard focus；选择后同步替换 Files 根、文件\n搜索索引和 Git/Changes projection。branch Button 通过\n`GitBranchContextMenuState` 组合通用 `ContextMenu`，第一行通过其 header slot 承载默认聚焦的\n`SearchBox`；输入按 branch name 实时过滤，方向键/Tab 从搜索框进入结果，Enter 选择首个匹配项，\n并支持 clipboard 与 IME。当前分支置顶并标记，其他本地分支分页展示；候选和选择分别通过\n`git/branch/list` 与 `git/branch/switch`。Git 因工作树冲突拒绝时，菜单保持打开并显示失败\n状态，不丢弃用户改动。Changes Button 会刷新 Git projection、展开 Agent Sidebar 并选择 Changes\nPane；environment picker 尚未接入。\n在 shell integration 提供 cwd 事件前，目录标签表示用户选择的工作区，而不推断 PTY 内部 `cd`。\n`workspace_path_picker_path` 中的 `canonical_directory`、`resolve_directory_query` 和\n`read_child_directories` 在替换状态前验证目标、解析显式路径并读取子目录；无权限、\n已删除或非目录目标会保留原工作区与当前浮层状态并记录错误。`workspace_path_picker_input` 单独拥有\n模态 pointer/keyboard 路由、focus restoration 和 `WorkspacePathPickerActivation` 的产品状态转换，\n通用 `Dropdown` 不读取文件系统、搜索查询，也不拥有工作区切换。\n`SessionTabList` 的白色状态圆形保留独立可访问性 label，后续可在其中绘制状态 SVG；当前\nnative runtime 只投影通用 `Active`，尚不能声称已接入 Planning、Thinking、Editing 等 Agent\n执行阶段。这些状态必须在 App Server 提供权威 Session projection 后映射，不能由 UI 根据输出\n推断。\nPTY output 中的 device/status/cursor query 由 `TerminalCore` 生成 reply bytes；\n`TerminalSession::handle_event` 在同一次 output event 后取出并写回同一 PTY，renderer 不参与。\n`terminal_pointer::TerminalPointer` 只在 alternate screen 且应用启用 tracking mode 时接管 terminal\nviewport，维护 held button 与最后一个有效 cell。titlebar 和 terminal padding 外部仍走\n产品 hit testing；1000/1002/1003 filtering 与 legacy/1006 wire encoding 委托 `TerminalCore`。\n`terminal_scrollback::TerminalScroll` 只保存当前视口距底部的行偏移、触控板小数滚动量和\nscrollbar fade controller；主屏滚轮在 BlockList 已建立后浏览命令 transcript，否则浏览\n`TerminalGrid` 的 cell history。`TerminalOutputScrollView` 把这个底部相对行窗口转换为通用\n`zeta-ui::ScrollView` 的顶部相对内容坐标，由后者统一执行 viewport clip、内容平移和 scrollbar\ngeometry/paint。用户停在旧输出时，新输出增加相同偏移以保持内容锚定；这条 terminal 语义没有\n下沉到通用滚动组件。提交新命令会回到底部。alternate screen 请求 mouse report 时，滚轮优先\n写入 PTY，不改变产品回滚位置。\n`terminal_selection::TerminalSelection` 同样只拥有可丢弃的 viewport selection。主屏左键拖拽\n跨过至少一个 cell 后才生成选区，单击不会留下蓝色矩形；宽字符复制按 display width 截取。\nmacOS 使用 `Cmd+C/V`，其他平台保留未加 Shift 的 `Ctrl+C/V` 终端语义，并使用\n`Ctrl+Shift+C/V` 访问剪贴板。OSC 0/2 title 同时投影到 native window 和 Session Tab，不改变\n内部品牌名。\n\n`terminal_session::shell_bootstrap` 对支持的 POSIX shell 关闭 PTY echo 并隐藏原生 prompt，\n`BootstrapOutputFilter` 在 bootstrap marker 前不向产品暴露启动噪声。zsh 额外安装最小\n`precmd` hook，以 OSC 133 `D` 完成活动 Block。这个 hook 还不是可协商、可版本化的完整 shell\nintegration；其他支持 shell 目前不能可靠报告每条命令的完成状态、cwd 或 exit status。\n\n当前仍没有 Session action mutation、多行/历史/建议式 Block Editor、\n双击词/三击行选择、terminal selection auto-scroll、跨进程重启的回滚/Block 持久化、完整\nDEC/query/mouse family 或平台\naccessibility adapter。内部语义树与统一 focus 已具备。alternate screen 已具备基础 direct\nkey/IME commit/clipboard 和请求式 mouse input，但\n尚不能据此声明完整 TUI compatibility。这些后续纵切不应进入 `zeta-winit` 或 `zeta-wgpu`。\n布局检查模式当前覆盖所有 Component 和拥有独立 box ownership 的 composition surface；尚不是完整\nretained widget tree，也不支持运行时编辑样式、跨 frame 选择 identity、面板滚动或远程协议。\nSessions 搜索区域当前真实链路\n是 `SessionSidebar → SessionSidebarToolbar → SearchBox → InputBox`；不存在单独的 Header 组件。\n\n`zui` 的 crate-level 实现契约见\n[`zui/README.md`](zui/README.md)。后续 file tree、tabs、chat 和 editor\n首次接入时，应由各自 presentation owner 分配稳定\n`ElementId`、注册父子节点、role/label/bounds、focus policy 与 activation intent。动态行或 tab\n必须在仍表示同一对象时保持 identity；domain selection、document model、chat turn 或 filesystem\nstate 不进入 `zui`。\n\nmacOS 可能在新窗口激活完成前把首次 surface acquisition 报为 occluded；该 frame 会被跳过。\n`NativeApp` 在后续 `WindowEvent::Occluded(false)` 上重新请求 redraw，保证首个可见 frame 不会\n因为一次正常的 activation transition 永久丢失。",
    "headings": [
      {
        "depth": 2,
        "id": "构建与发布边界",
        "title": "构建与发布边界"
      },
      {
        "depth": 2,
        "id": "宿主边界与-frame-ownership",
        "title": "宿主边界与 frame ownership"
      },
      {
        "depth": 2,
        "id": "所有权",
        "title": "所有权"
      },
      {
        "depth": 2,
        "id": "产品方向与当前边界",
        "title": "产品方向与当前边界"
      },
      {
        "depth": 2,
        "id": "当前执行路径",
        "title": "当前执行路径"
      }
    ],
    "searchText": " Native 文本输入的跨 crate canonical ownership 见 docs/native-text-input.md；Agent-first 产品结构见 docs/native-agent-console.md。Terminal compatibility 细节见 docs/native-terminal-ui.md；本 README 只拥有 native host 当前源码路径和接入义务。Terminal grid 与 BlockList 的实现契约见 zeta-terminal README；CodeEditor/DiffEditor presentation 契约见 zeta-editor README；文件保存基线与冲突状态契约见 zeta-text-file README。 UI 到 GPU 的 canonical 边界见 docs/rendering-architecture.md。 zeterm 是与 zeta Electron Desktop 和 zeta code TUI 同级的纯 Rust Desktop 产品入口， 在发布边界导出为 zeterm。三条产品线的宿主边界见 docs/product-lines.md。 当前窗口纵切由产品拥有 ApplicationHandler，组合 zeta-winit、zeta-renderer、当前 zeta-wgpu backend、zui framework 与 zeta-ui components， 并在单个原生窗口中绘制 Agent ThreadTimeline 与固定底部 Agent/Shell Composer。 启动时 zeta-theme 从共享 design-token manifest、zeterm 默认主题入口与 device-local 用户主题生成不可变 snapshot； shellstyle 将其适配为 Shell、CodeEditor、MultiDiffEditor、scrollbar 与 terminal ANSI palette。 zeta-layout 拥有 Root/Inspector/Terminal Workspace 的 pane topology；zeta-composer 拥有 Composer panel 与 interaction list 的纯几何；ShellLayout 和 composerpanel 仍只拥有剩余 Shell/Composer product composition 的区域背景、边界、绘制与状态接线，组件内部视觉继续由组件 style contract 拥有。 窗口级产品 pane topology 已抽到 zeta-layout；通用框架可脱离产品运行的 最小链路由 zui-demo 持续验证。 构建与发布边界 仓库根 Cargo.toml 是唯一 Cargo workspace；zeterm/Cargo.toml 是 zeterm 产品 package 和发布边界： ``bash cargo check --manifest-path Cargo.toml -p zeterm cargo test --manifest-path Cargo.toml -p zeterm cargo run --manifest-path Cargo.toml -p zeterm ` zeterm 通过根 workspace 的 path dependencies 使用 shared backend；zeta-rs 不反向依赖 zeterm 或 Native UI。Bazel 从同一个根 Cargo graph 生成 @crates，并以 //zeterm:zeterm、 //zeterm:zetermsources、//zeterm:zetermreleaseinputs 和 //zeterm:zetermci 拥有产品 target、输入、发布契约与 boundary check。zeterm 仍是独立产品目录和 发布入口，但不再是第二个 workspace 根； 当前完整 zeterm binary 的 macOS 验证仍受 apple-cf Swift bridge 在沙箱中启动 SwiftPM 的环境限制， Cargo workspace check/test 是产品源码的首要验证入口。发布 staging 入口是： `bash just zeterm-package --package-dir /absolute/path/to/zeterm-package --zeterm-bin /path/to/zeterm bazel test //zeterm:zetermci ` 平台 release job 使用 ZETERMPACKAGEDIR、ZETERMPLATFORM 以及对应的 signing identity 环境 调用 scripts/releasezetermpackage.sh；该入口完成签名和验签后才留下 verified artifact。 package staging 只生成 unsigned artifact 和 binary digest；签名、平台验证与发布顺序见 zeterm release graph。 宿主边界与 frame ownership zeterm 是产品宿主，不是第二个 UI framework。它保留窗口、平台事件、产品状态映射、App Server 适配、command 执行以及具体 Shell/Composer/Inspector Part 的组合；通用 layout、scene、inspection、 interaction、animation、deadline 和 retained lifecycle 由 zui canonical owner 提供。跨 crate 的 阶段、弃用范围和删除条件见 docs/zeterm-app-migration-plan.md。 边界 Native 当前状态 维护规则 --- --- --- 产品状态、平台事件、App Server/文件/Git/Session adapter ✅ 当前 owner 可以在 Native 演进，不迁入 zui Shell/Composer/Inspector 的产品组合 ✅ 当前 owner 组合 zui/zeta-ui，不复制组件内部 geometry 或 hit-test ShellPresentation 的 scene/interaction/accessibility 聚合 UiFrame<InteractionFrame frame 是唯一 owner；renderer、input 和 Inspector 通过窄 accessor 消费 低层产品 composition UiFrame::withcontext / ComponentContext 只能在 scoped context 中注册交互和绘制；可复用组件使用 drawcomponent 通用 frame、失效、动画和 retained lifecycle zui 委托 Native 只注册产品 fragment、推进 runtime 并投影 deadline 因此不能把 zeterm binary 标成 deprecated：它是当前产品宿主。旧 split scene/interaction composition API 已删除，后续不得在 Native 宿主重新引入平行输出字段。 所有权 能力 当前 owner 状态 --- --- --- zeterm 发布名与用户可见显示 PRODUCTDISPLAYNAME / Cargo bin ✅ 产品窗口标题、初始尺寸与 event routing zeterm::NativeApp ✅ Event loop 与 native window adapter zeta-winit 委托 后端无关 render、resize、scale 与 frame outcome zeta-renderer::Renderer 委托；NativeApp 只保存 trait object GPU surface、pipeline、atlas、shader、present 与 retry zeta-wgpu 委托；仅 rendererbackend 知道具体 backend 类型 Element、Rect/image/icon/text scene、检查快照与字体测量 zui 委托；不接触组件或 GPU API Shell product layout 与 scene composition zeta-layout + zeta-composer + shellscene / composerpanel 部分抽取；zeta-layout 拥有 root/workspace pane geometry，zeta-composer 拥有 Composer 几何，Native 仍拥有状态与 scene composition Native frame assembly ShellPresentation::frame ✅；由单一 zui::UiFrame<InteractionFrame owner 管理 原生布局检查模式、pointer 拦截与 highlight overlay layoutinspector::LayoutInspector / NativeApp ✅；Inspector Panel 是根 Grid leaf，只有产品节点高亮进入 overlay Sessions/Main 单轴约束与 Sash presentation geometry zui::SplitViewLayout / zeta-ui::Sash 委托 Terminal Workspace 与 Agent Sidebar 递归 Pane geometry zui::GridLayout 委托；当前输入树包含活动终端与可选右栏 Leaf Terminal Pane Tree、Pane-to-Session binding 与 active Pane 后续 native Terminal Workspace model 尚未完成；Session Tab 到独立 Terminal PTY 的一对一 binding 已完成 Sessions preferred width、显隐与当前 drag snapshot sessionsidebar::SessionSidebarState ✅ Agent Sidebar 显隐、preferred width 与 resize gesture agentsidebar::AgentSidebarState / zeta-ui::Sash ✅；宽度限制为 240–560px，不拥有内部 Pane 内容 Agent Sidebar 导航与各功能 Pane 的布局 zeta-agent-sidebar 的 AgentSidebarNavigation / files::FilesLayout / scm::ScmLayout 委托；zeterm 只提供主题投影、workspace 快照与 shell slot Files 层级树与模糊搜索 zeta-agent-sidebar 的 files::FilesState / files::FilesPane / zeta-ui::TreeView / ListView / zeta-file-search ✅；目录懒加载、稳定 mounted-node ID、展开/收起和 24px 虚拟行已接入；zeterm 只适配 App Server 目录 DTO 并执行打开/加载动作 UTF-8 文件保存 baseline、磁盘版本与外部变化冲突 zeta-text-file::TextFileLifecycle 委托；Native 只提供当前 editor text 与 I/O adapter 语言服务 composition、持久化设置、文档/请求 freshness 与 presentation languageservicehost::NativeLanguageService / languageserversettingsmodel::LanguageServerSettingsState / fileeditorlanguagefeatures / zeta-language-service ✅；Rust/JSON/Shell 独立设置与 runtime state，diagnostics，latest-only pointer hover，Ctrl/Cmd+Space completion popup/安全 edit 接受和 F12 definition navigation 已接入；文件读取仍通过 App Server authority 文件 Tab、active document、关闭决策与 presentation fileeditorhost::FileEditorHost / fileeditorpane::FileEditorPane / fileeditorinput::FileEditorInputState ✅；Explorer activation 已通过 typed fs/readFile/metadata 打开 language-aware document，Cmd/Ctrl+S 使用版本 preflight 和 fs/writeFile，中心 Editor Surface 已接通 tabs、关闭确认、外部重载/显式乐观覆盖、find/replace、自动缩进、soft wrap、focus、keyboard、IME、pointer、clipboard 与 visual-row viewport Changed-file collection、整体滚动与每文件 diff 视口 zeta-agent-sidebar 的 ScmState / EditorPaneState / zeta-ui::ScrollState 委托；zeterm 只把 Git 投影映射为 ScmDiff 并执行刷新动作 Titlebar 背景、窗口拖拽区、语言服务器设置入口与左右侧栏开关 titlebar::Titlebar ✅；不绘制可见窗口标题 Top Bar 左右 sidebar toggle ActionBar titlebar::Titlebar ✅ 原生窗口控件占位 zeta-winit::WindowControlInsets 委托；Titlebar 只增加自身内容间距 通用 Tab surface 与横/纵排列 zeta-ui::Tab / TabList 委托；不拥有 Session content 或 tabpanel 多行编辑、caret/selection、find/replace、自动缩进、undo/redo、IME 与 syntax projection zeta-editor::CodeEditorDocument / CodeEditor 委托；Composer 与中心文件 Editor 都只转交平台输入，editor 内部管理文本、搜索替换、缩进、parser/revision/token、fold 与 hit-test 文本差异计算、行映射与字符级范围 zeta-diff::DiffDocument 委托；Native 不复制 diff 算法 单列/并排及多文件只读差异展示 zeta-editor::DiffEditorDocument / DiffEditor / MultiDiffEditor 委托；Native 只按文件扩展名选择 language，editor 内部维护两侧 parser/revision/token；Changes pane 使用窄栏 Unified presentation 可折叠 Session Sidebar、名称搜索与多 Session Tab sessionsidebartoolbar::SessionSidebarToolbar / sessionsearch::SessionSearch / sessiontablist::SessionTabList ✅；Add 通过 App Server 创建 Session/Thread，tab 激活切换 Thread projection，并绑定独立 Terminal PTY 可折叠 Agent Sidebar 产品组合 zeta-agent-sidebar 的 AgentSidebarNavigation / files::FilesToolbar / FilesPane / scm::EditorPane 委托；Native 只负责外层显隐、主题映射和 action 执行 锚点浮层定位与独立 layer 合成 zeta-ui::ContextView 委托；不拥有显示生命周期或输入路由 无边框下拉 surface、可选 header、item geometry 与默认选择 zeta-ui::Dropdown 委托；不拥有产品查询、选择 identity、关闭或 command 柔和阴影、2px menu padding、4px radius、item geometry 与默认选择 zeta-ui::ContextMenu 委托；不拥有 Session identity、关闭或 command Session Tab 右键菜单、关闭策略与 action 映射 sessioncontextmenu::SessionContextMenu / SessionContextMenuState ✅；四个 action 已进入产品边界，Pin/Close/Rename/Fork mutation 尚未执行 共享主题加载与平台中立 snapshot zeta-theme 委托；选择数据化 zeterm 默认入口，并与 Desktop/TUI 消费同一 manifest 和用户主题 JSON Native Shell/CodeEditor/Diff/Terminal 主题投影 shellstyle::ShellPalette 与命名 component palette ✅；启动时加载一次，失败回退内置浅色 palette 稳定控件身份与 shell action 映射 shellinteraction ✅ 稳定 product command identity、request 与注册式执行入口 zeta-commands:: ZetermCommandId, CommandRequest, CommandRegistry / NativeApp::dispatchcommand ✅；pointer、menu 与 shortcut 汇合到宿主注册的 handler 平台按键转换、Chord 生命周期与当前 context keybindings::NativeKeybindings ✅；1.5 秒超时，失焦或 IME 事件取消 用户快捷键资源、验证与热更新 zeta-keybindings-host::KeybindingsResource ✅；读取 <ZETAPROFILEROOT /keybindings.json，坏更新保留上一份有效规则 快捷键模型、设置、录制与 Chord 提示 zeta-keybinding 委托；zeterm 提供命令行、事件 adapter 与保存接线 平台无关快捷键模型、规则顺序与冲突解析 zeta-keybinding 委托 命中、hover/press/capture、focus、键盘导航、cursor 与 accessibility semantics zui 委托 accessibility semantics → 平台屏幕阅读器 尚无 native adapter 尚未完成 Transparent native chrome 与窗口拖动 adapter zeta-winit 委托 ANSI parser、terminal grid 与 BlockList zeta-terminal::TerminalCore 委托 默认 shell PTY、output/exit event、write 与 resize terminalsession::TerminalSession ✅；PTY 创建在后台 worker 完成，UI 只接管 ready runtime Session Tab 到本地 PTY 的一对一 binding、活动/非活动 runtime 切换 terminalworkspace::TerminalWorkspace ✅；pending key、乱序 ready 和非活动 runtime 由 Native adapter 管理，不拥有 App Server Session/Thread authority Agent ThreadTimeline + fixed Agent/Shell Composer shellscene / threadtimeline / composerpanel / agentcomposer ✅ Composer 面板几何、信息栏与可展开交互 Pane 的位置 zeta-composer / composerpanel / shellscene ✅；zeta-composer 只解析 panel、固定行、interaction bounds 与 list scroll geometry，Native 负责状态、绘制和 scene 接线 Composer active View 与滚动状态 composerinteraction::ComposerInteractionModel / composerinteractionpane::ComposerInteractionPaneState / zeta-ui:: ScrollView,ListView ✅；Pane 只宿主 Slash 与 /model active View 并保留 viewport offset，通用 UI 基座负责裁剪、滚动与可见范围；Shell completion 不使用该 Pane App Server Session adapter 与 transient Thread projection / stream-gap recovery agentsession / threadprojection ✅；Thread 只作为 App Server 内部状态的 Native projection，不形成独立 crate durable direct Shell Turn App Server session/request::StartShellTurn / Core StartShellTurn ✅ 独立交互式 Terminal Surface workspacesurface / terminalsession / terminalinput ✅；Cmd/Ctrl+J 切换 Composer/File Editor/Session Search/File Search/Workspace Path Search/Git Branch Search IME target、事件转换、启停、composition lifecycle 与候选框同步 inputmethod ✅ Bottom Widget 底部的 Local/cwd/branch/Changes context toolbar inputcontexttoolbar::InputContextToolbar / workspacepathpicker / gitbranchcontextmenu / workspacecontext::WorkspaceContext ✅；cwd 复用带 Search Box header 的 Dropdown，branch 复用带同类 header 的 ContextMenu，分别切换工作区投影和本地分支；Changes 显示 changed path 数与文本 +addition -deletion，点击后刷新并展开 Changes Pane shell bootstrap、host-owned command submit 与 zsh completion marker terminalsession::TerminalSession 部分具备 terminal query reply → PTY write TerminalCore::takereplybytes / TerminalSession::handleevent ✅ alternate-screen mouse cell mapping、button state 与 PTY report terminalpointer::TerminalPointer / TerminalCore::encodemouse ✅ 主屏滚轮浏览、输出增长锚定与 Block/grid 视口投影 terminalscrollback::TerminalScroll / terminaloutputscrollview::TerminalOutputScrollView / zeta-ui::ScrollView ✅；Terminal 保留底部相对行锚定，通用基座负责裁剪、内容坐标和滚动条绘制 主屏拖拽选择、selection paint 与 cell-aware text extraction terminalselection::TerminalSelection ✅ system clipboard copy/paste 与 bracketed-paste routing terminalselection / terminalinput ✅ OSC title → Terminal Surface / native window title TerminalCore::title / NativeWindow::settitle ✅；后台 terminal title 不覆盖 Agent Session title 完整 TUI compatibility 尚无完整 owner 尚未完成 App Server session 与进程内 Thread projection agentsession / threadprojection ✅；Native 通过 SessionStateMode::Ephemeral 使用 application boundary，Session/Thread/用户消息只在当前 zeterm 进程内存在 依赖方向： `text zeterm → zeta-winit → zeta-renderer → zui → zeta-wgpu → zeta-renderer → zeta-winit → zui → zeta-ui → zui → zeta-commands → zeta-keybinding → zeta-app-server-client → zeta-protocol → zeta-terminal → zeta-text-file → zeta-diff → zeta-editor → zeta-ui → zui → zeta-diff → zeta-syntax → zeta-theme → zeta-file-search → zeta-slash-commands → zeta-app-server-protocol → zeta-utils-pty ` zeterm 可以拥有可丢弃的 presentation state 和产品交互，但不能复制 Session、Thread、 Turn 或 Tool 的权威状态机。当前通过 zeta-app-server-client 的 typed contract 订阅与提交。 产品方向与当前边界 Native App 是 Agent-first Console；Terminal 是显式切换的 compatibility Surface，不是 Thread authority。下表把仍保留的历史 shell vocabulary 映射到当前产品语义： 当前源码 当前能力 目标产品语义 状态 --- --- --- --- titlebar::Titlebar 窗口拖拽区和左右 sidebar toggl"
  },
  {
    "slug": "crates/zeterm/zui",
    "sourcePath": "zeterm/zui/README.md",
    "title": "zui",
    "description": "zui 是后端无关的原生 UI 框架。它把声明式 Element 解析为一次 ComputedElement，用同一份 计算几何驱动组件绘制与检查快照，并把最终 UiScene 交给 renderer。它还提供后端无关的交互帧、 稳定控件身份和 pointer/focus 分发语义。它不提供 Button、ActionBar 等组件，也不调用窗口 API、 映射产品命令或…",
    "group": "Crate 实现参考",
    "markdown": "> 本 README 是 `zui` crate 的实现权威文档。跨 crate 的渲染所有权、后端替换边界和长期演进由\n> [`docs/rendering-architecture.md`](../docs/rendering-architecture.md) 维护；通用组件的实现由\n> [`zeta-ui`](../ui/README.md) 维护。Native split host 的渐进式弃用边界由\n> [`docs/native-deprecation-plan.md`](../docs/native-deprecation-plan.md) 维护。\n\n`zui` 是后端无关的原生 UI 框架。它把声明式 `Element` 解析为一次 `ComputedElement`，用同一份\n计算几何驱动组件绘制与检查快照，并把最终 `UiScene` 交给 renderer。它还提供后端无关的交互帧、\n稳定控件身份和 pointer/focus 分发语义。它不提供 Button、ActionBar 等组件，也不调用窗口 API、\n映射产品命令或保存产品状态。\n\n## 1. 边界与依赖方向\n\n| 能力 | Owner | 状态 |\n| --- | --- | --- |\n| Element row/column、fixed/fill、padding、gap 与 radius | `zui` | ✅ |\n| `ComputedElement` 与每帧 `InspectionFrame` | `zui` | ✅ |\n| geometry、paint/image/icon/text scene primitive | `zui` | ✅ |\n| Renderer-independent icon asset contract | `zeta-icon`（由 `zui` facade re-export） | 委托 |\n| Scene 稳定前缀 checkpoint 与 volatile fragment 原地替换 | `zui` | ✅；host 决定 retained boundary |\n| Retained fragment lifecycle、animation cleanup 与 deadline report | `zui::RetainedRuntime` / `zui::RetainedFragmentRegistry` | ✅；host 应用 scene/interaction cleanup |\n| Backend-neutral scalar animation、stable property key、deadline 与 advance contract | `zui::AnimationRegistry` / `zui::ScalarAnimation` | ✅；host 提供时间、调度唤醒和重绘 |\n| Split/Grid、text shaping 与单行 text input 基座 | `zui` | ✅ |\n| Button、ActionBar、TabList、ContextView 等组件 | `zeta-ui` | 委托 |\n| GPU execution 与 surface lifecycle | `zeta-renderer` / backend crate | 委托 |\n| 命中、pointer capture、focus、键盘导航、cursor 与 accessibility snapshot | `zui` | ✅ |\n| 平台事件转换、cursor/accessibility 发布与产品命令映射 | product host / platform adapter | 委托 |\n\n允许的依赖方向是 `zeta-ui → zui → zeta-icon`、`zeta-renderer → zui` 和\n`backend → zeta-renderer + zui`。`zui` 不得依赖这些上层 crate。出现 `wgpu::*`、产品 identity、\n组件样式或 host reducer，意味着 ownership 已经漂移。\n\n### 内部分层\n\n`lib.rs` 只是显式公共 facade，不承载实现。内部依赖固定为：\n\n| 层 | 物理目录 | 可以依赖 | 明确不拥有 |\n| --- | --- | --- | --- |\n| 基础值 | `foundation/` | 无 | layout、scene、interaction、平台类型 |\n| 几何算法 | `layout/` | `foundation` | Element、paint、产品 Pane 状态 |\n| 文本内核 | `text/` | `foundation`、自身子模块 | scene、组件 chrome、IME 平台 lifecycle |\n| 呈现组合 | `presentation/` | `foundation`、`text` | 跨帧 focus/capture、窗口事件、产品 reducer |\n| 框架运行时 | `runtime/` | `foundation` | scene、component、text、产品 command |\n| Renderer bridge | `renderer_support.rs` | `text` | scene mutation、GPU backend、平台 surface |\n\n`presentation` 与 `runtime` 是并列层：product host 负责把一帧 `UiScene`、`InteractionFrame` 和\n`FrameScheduler` 组合起来，任何一侧都不得反向获得另一侧。内部实现必须通过 canonical layer path\n引用依赖，不能经由 crate root re-export 绕过层次。生产模块上限为 500 行；新增职责应进入所属层的\n新私有模块。\n\n## 2. 文件与接口地图\n\n| Symbol | 可见性 | 精确职责 |\n| --- | --- | --- |\n| `Component` | public | 要求组件声明 `ComponentElement`，并允许绘制消费同一次 computed geometry |\n| `Element` / `ElementStyle` | public | 保存 authored direction、length、padding、gap、radius 和 child tree |\n| `ComponentElement::compute` | public | 在 host bounds 内解析 immutable `ComputedElement` |\n| `compute_element` / `resolved_padding` / `child_bounds` | private | 分配 fixed/fill 主轴空间、裁剪 box 并生成准确 gap regions |\n| `UiScene::draw_component` | public | compute 一次、自动注册检查节点，再调用 `paint_element`；适用于不需要交互 sink 的 scene-only 组合 |\n| `ComponentContext` / `UiFrame` | public | 在同一 frame 中携带 inspection parent、interaction sink 与显式时钟，组合子组件并声明 modal scope |\n| `UiFrame::draw_component` / `with_context` / `with_element` / `with_clip` | public | canonical frame composition；低层 host paint 也必须留在 frame 的 scoped closure 内 |\n| `ComponentContext::with_component` | public | 在一个组件根下交错自定义 paint 与子组件，同时保持 scene inspection parent、interaction parent 和 animation binding |\n| `UiFrame::with_animation_bindings` / `draw_component_with_animation_bindings` | public | 将 retained `AnimationBinding` scoped 到一帧组件组合；组件不直接依赖 runtime registry |\n| `presentation::inspection::node_for_element` | crate-private | 单向把 `ComputedElement` 投影为 inspection metadata；Element 层不反向依赖 inspection |\n| `presentation::component` 中的 `impl UiScene::draw_component` | public binding | 把 Component 接入 scene；scene 核心不依赖 Component trait |\n| `UiScene::with_element` | public | 让 content closure 与 overlay 进入相同 compute/inspection 管线 |\n| `UiScene::with_current_layer_element` / `with_inspection_node` | private | 绑定 scene layer、inspection parent 与声明源码位置 |\n| `UiScene::checkpoint` / `restore` | public | 保留 scene primitive/layer/inspection 稳定前缀，并复用分配原地替换后续 fragment |\n| `UiScene::with_fragment` / `replace_fragment` / `remove_fragment` | public | 以稳定 `ElementId` 管理 terminal retained fragment，并在移除时恢复 scene/inspection prefix |\n| `FrameScheduler::request` / `take` | public | 合并平台帧之间的失效请求，并由 host 在一次 redraw 中消费最高级别的工作 |\n| `FrameDeadlineSet::include` / `next_deadline` | public | 聚合组件、动画和异步轮询的最早单调 deadline；不接触平台 event loop |\n| `ScalarAnimation::transition_to` / `advance` | public | 插值一个后端无关的标量值，支持 retarget、easing、deadline 与 snap/cancel |\n| `AnimationRegistry::transition_to` / `advance` | public | 按 `ElementId + AnimationProperty` 保留跨帧 track，聚合 changed keys、fragment IDs、失效等级与 deadline |\n| `RetainedFragmentRegistry::mount` / `begin_exit` / `advance` | public | 保留稳定 fragment lifecycle，取消退出、按显式时钟生成到期 cleanup IDs，并投影局部失效 |\n| `RetainedRuntime::mount` / `begin_exit` / `advance` | public | 聚合 fragment lifecycle 与 animation tracks；fragment 到期或 unmount 时自动清理其 animation tracks |\n| `InteractionFrame::register` / `checkpoint` / `restore` | public | 记录一帧的绘制顺序、交互节点与 modal scope，并与 retained scene prefix 对齐 |\n| `InteractionSink::register` / `set_modal_root` | public | 为 presentation 组合提供后端无关的交互节点与 modal boundary；runtime frame 承担实际保留和分派 |\n| `UiDispatch` | public | 跨 frame 保存 hover、press/capture、focus 与窗口激活状态，只产生无效请求和 `UiIntent` |\n| `UiNode` / `ElementId` / `AccessibilityNode` | public | 分别表达当前帧控件语义（含交互失效等级）、跨帧稳定 identity 与不可变 accessibility snapshot；不分配产品 identity 或发布平台 API |\n| `InspectionFrame::register` | crate-private | 建立单帧 identity、parent、layer 和命中顺序 |\n| `scene::batching::batches` | private | 保留跨 primitive 的真实插入顺序并合并连续同类 range |\n| `font::new_font_system` / `font::mapping` | private | 固定 layout 与 renderer 共享的 locale/fallback/font mapping policy |\n| `renderer_support` | public backend bridge | 只向 renderer adapter 暴露共享 shaping policy |\n\n## 3. 执行路径与不变量\n\n```text\nUiScene::draw_component\n  → Component::element\n  → ComponentElement::compute\n  → compute_element\n  → presentation::inspection::node_for_element\n  → InspectionFrame::register\n  → Component::paint_element(same ComputedElement)\n  → UiScene primitives\n  → SceneBatch\n  → zeta-renderer::Renderer\n```\n\n新宿主的 canonical 入口是 `UiFrame::draw_component`：它让 scene、inspection、interaction 和 frame clock\n由一个 frame owner 进入同一次组合。需要自定义低层 paint 与子组件交错时，使用\n`ComponentContext::with_component`；需要无交互的低层 element closure 时才使用 `with_element`，让\n`ComponentContext` 继续拥有同一个 frame。\n旧的 `UiScene::draw_component_with_interaction`、`UiFrame::parts_mut` 和 `UiFrame::into_parts` 已删除，\n不得重新引入平行的 scene/interaction 输出接口。\n\n`Element` tree 每帧解析，不持有跨帧 identity。`InspectionNodeId` 只能在当前 `UiScene` 生命周期内使用；\n选中状态如果要跨帧保存，必须由 host 建立自己的稳定 identity。Padding 色块使用 clamp 后的 resolved\n值，Inspector 同时可读取原始 `ElementStyle`。Gap 由 layout 生成实际矩形，不允许 Inspector 猜测。\n`element` 不引用 `inspection`，`scene` 不引用 `Component`；这两个单向绑定分别由\n`presentation::inspection` 和 `presentation::component` 承担，禁止重新引入双向模块依赖。\n\n`FrameScheduler` 只合并 `Render < Fragment < Rebuild` 帧请求，不调用窗口 API，也不决定产品状态如何变化。\n`Fragment` 表示由 host 划定的任意 presentation fragment，不表示 Overlay、Picker 或其他产品拓扑。Host 在收到\n`FrameSchedule::RequestFrame` 时唤醒平台，在 redraw 开始时通过 `take` 消费工作；同步完成了等价重建时\n必须调用 `clear`，避免下一帧重复执行。\n\n`SceneCheckpoint` 是当前 retained presentation 的最小边界：它记录 primitive、composition layer 与\ninspection prefix，`restore` 后 host 可以只重建 volatile fragment，并复用 Scene Vec capacity。它不提供\n跨 scene identity，也不自动判断 dirty subtree；checkpoint 必须由创建它的同一 `UiScene` 消费。\n\n`RetainedRuntime` 是跨帧 retained state 的通用 owner；其内部的 `RetainedFragmentRegistry` 管理 fragment\nlifecycle，`AnimationRegistry` 管理属性 track。Host 在当前组合出现稳定\nidentity 时调用 `mount`；重复调用表示 update，不会重置 retained state。组件离开当前组合时调用\n`begin_exit`，退出期间不能重新注册 interaction 或 inspection 节点；`advance` 到达 deadline 后返回\ncleanup IDs，并自动移除这些 identity 的 animation tracks。Host 必须用这些 ID 调用\n`UiScene::remove_fragment`，并恢复配对的 `InteractionFrameCheckpoint`；如果 fragment 不是 terminal，则\n退回 `FrameInvalidation::Rebuild`，不能静默破坏后续 paint order。\n\nOverlay Element 会创建高于当前 layer 的 scene layer，并在闭包返回后恢复调用者的 layer 与 clip。\npanic recovery 不是当前 contract；组件 paint panic 会中断本帧构建。\n\n交互沿同一份 layout bounds 运行：host 在 scene 构建时注册 `UiNode`，平台事件进入 `UiDispatch`，\n其返回的 `UiIntent` 再由 host 映射为产品状态转换。`zui` 不接受 callback registry，也不执行\nwindow drag、菜单、Session 或 editor command。\n\n## 4. 集成义务\n\n- 组件调用者使用 `UiScene::draw_component`，不能直接调用 `Component::paint`。\n- 拥有自定义 paint 与子组件的 surface 使用 `ComponentContext::with_component`；无交互 scene-only\n  closure 才使用 `UiScene::with_element`。\n- Renderer 只消费 `UiScene`，不得获得 Component、interaction 或 accessibility frame。\n- 使用同一份绘制 bounds 注册 `UiNode`，不能另行估算命中区域；动态对象在仍表示同一对象时保持\n  `ElementId`。\n- retained fragment 的 mount/update/unmount 必须由 `RetainedRuntime` 记录；退出 fragment\n  不得继续出现在当前 interaction 或 inspector snapshot 中。\n- 处理 `RetainedRuntimeAdvanceReport::fragment().removed_ids` 时，scene 和 interaction checkpoint 必须\n  在同一 cleanup 路径收敛；动画 track 由 runtime 自动清理，不能只删 paint 而留下可交互或可检查的 ghost node。\n- product host 将 `UiIntent` 映射为 command，并由 platform adapter 发布 cursor 与 accessibility\n  snapshot；`zui` 不建立第二套业务 reducer 或平台 adapter。\n- `zeta-ui` 可以兼容 re-export `zui` API，但 renderer 必须直接依赖 `zui`，避免基础协议由组件 crate\n  反向拥有。\n\n## 5. 测试与修改影响\n\n运行 `cargo test -p zui` 验证 Element layout、inspection、scene batching、font/text layout、text input、\nSplit/Grid、interaction 与 primitive contract。修改以下边界时还需同步验证：\n\n- `architecture_tests` 验证物理分层、canonical dependency path、500 行模块上限、显式 root export，\n  以及平台、GPU、组件和产品依赖禁令；\n\n- scene primitive 或 batch ordering：`zeta-renderer` 与全部 backend；\n- font/text contract：`zeta-wgpu`、`zeta-ui`、`zeta-editor`、`zeta-markdown`；\n- Element/Component contract：`zeta-ui`、`zeta-keybinding`、`zeta-editor` 与 native 架构审计；\n- geometry/layout/interaction contract：native root/split/grid tests。\n\n## 6. 当前限制与扩展点\n\n当前具备帧级失效合并、host 划定的 Scene/interaction prefix checkpoint、terminal fragment 移除、\n`RetainedRuntime`、`RetainedFragmentRegistry`、`AnimationBinding` 和通用 pointer/focus 语义，但没有自动子树级\ndirty propagation、跨帧 layout cache、style cascade、运行时样式编辑、path primitive、disabled/live-region/\ntext-selection accessibility semantics 或平台 accessibility adapter。产品级 exit retention 仍需各 fragment\n明确保留内容、退出目标和 reduced-motion policy；GPU backend 变化不应改变 `zui` public contract。",
    "headings": [
      {
        "depth": 2,
        "id": "1-边界与依赖方向",
        "title": "1. 边界与依赖方向"
      },
      {
        "depth": 3,
        "id": "内部分层",
        "title": "内部分层"
      },
      {
        "depth": 2,
        "id": "2-文件与接口地图",
        "title": "2. 文件与接口地图"
      },
      {
        "depth": 2,
        "id": "3-执行路径与不变量",
        "title": "3. 执行路径与不变量"
      },
      {
        "depth": 2,
        "id": "4-集成义务",
        "title": "4. 集成义务"
      },
      {
        "depth": 2,
        "id": "5-测试与修改影响",
        "title": "5. 测试与修改影响"
      },
      {
        "depth": 2,
        "id": "6-当前限制与扩展点",
        "title": "6. 当前限制与扩展点"
      }
    ],
    "searchText": " 本 README 是 zui crate 的实现权威文档。跨 crate 的渲染所有权、后端替换边界和长期演进由 docs/rendering-architecture.md 维护；通用组件的实现由 zeta-ui 维护。Native split host 的渐进式弃用边界由 docs/native-deprecation-plan.md 维护。 zui 是后端无关的原生 UI 框架。它把声明式 Element 解析为一次 ComputedElement，用同一份 计算几何驱动组件绘制与检查快照，并把最终 UiScene 交给 renderer。它还提供后端无关的交互帧、 稳定控件身份和 pointer/focus 分发语义。它不提供 Button、ActionBar 等组件，也不调用窗口 API、 映射产品命令或保存产品状态。 1. 边界与依赖方向 能力 Owner 状态 --- --- --- Element row/column、fixed/fill、padding、gap 与 radius zui ✅ ComputedElement 与每帧 InspectionFrame zui ✅ geometry、paint/image/icon/text scene primitive zui ✅ Renderer-independent icon asset contract zeta-icon（由 zui facade re-export） 委托 Scene 稳定前缀 checkpoint 与 volatile fragment 原地替换 zui ✅；host 决定 retained boundary Retained fragment lifecycle、animation cleanup 与 deadline report zui::RetainedRuntime / zui::RetainedFragmentRegistry ✅；host 应用 scene/interaction cleanup Backend-neutral scalar animation、stable property key、deadline 与 advance contract zui::AnimationRegistry / zui::ScalarAnimation ✅；host 提供时间、调度唤醒和重绘 Split/Grid、text shaping 与单行 text input 基座 zui ✅ Button、ActionBar、TabList、ContextView 等组件 zeta-ui 委托 GPU execution 与 surface lifecycle zeta-renderer / backend crate 委托 命中、pointer capture、focus、键盘导航、cursor 与 accessibility snapshot zui ✅ 平台事件转换、cursor/accessibility 发布与产品命令映射 product host / platform adapter 委托 允许的依赖方向是 zeta-ui → zui → zeta-icon、zeta-renderer → zui 和 backend → zeta-renderer + zui。zui 不得依赖这些上层 crate。出现 wgpu::、产品 identity、 组件样式或 host reducer，意味着 ownership 已经漂移。 内部分层 lib.rs 只是显式公共 facade，不承载实现。内部依赖固定为： 层 物理目录 可以依赖 明确不拥有 --- --- --- --- 基础值 foundation/ 无 layout、scene、interaction、平台类型 几何算法 layout/ foundation Element、paint、产品 Pane 状态 文本内核 text/ foundation、自身子模块 scene、组件 chrome、IME 平台 lifecycle 呈现组合 presentation/ foundation、text 跨帧 focus/capture、窗口事件、产品 reducer 框架运行时 runtime/ foundation scene、component、text、产品 command Renderer bridge renderersupport.rs text scene mutation、GPU backend、平台 surface presentation 与 runtime 是并列层：product host 负责把一帧 UiScene、InteractionFrame 和 FrameScheduler 组合起来，任何一侧都不得反向获得另一侧。内部实现必须通过 canonical layer path 引用依赖，不能经由 crate root re-export 绕过层次。生产模块上限为 500 行；新增职责应进入所属层的 新私有模块。 2. 文件与接口地图 Symbol 可见性 精确职责 --- --- --- Component public 要求组件声明 ComponentElement，并允许绘制消费同一次 computed geometry Element / ElementStyle public 保存 authored direction、length、padding、gap、radius 和 child tree ComponentElement::compute public 在 host bounds 内解析 immutable ComputedElement computeelement / resolvedpadding / childbounds private 分配 fixed/fill 主轴空间、裁剪 box 并生成准确 gap regions UiScene::drawcomponent public compute 一次、自动注册检查节点，再调用 paintelement；适用于不需要交互 sink 的 scene-only 组合 ComponentContext / UiFrame public 在同一 frame 中携带 inspection parent、interaction sink 与显式时钟，组合子组件并声明 modal scope UiFrame::drawcomponent / withcontext / withelement / withclip public canonical frame composition；低层 host paint 也必须留在 frame 的 scoped closure 内 ComponentContext::withcomponent public 在一个组件根下交错自定义 paint 与子组件，同时保持 scene inspection parent、interaction parent 和 animation binding UiFrame::withanimationbindings / drawcomponentwithanimationbindings public 将 retained AnimationBinding scoped 到一帧组件组合；组件不直接依赖 runtime registry presentation::inspection::nodeforelement crate-private 单向把 ComputedElement 投影为 inspection metadata；Element 层不反向依赖 inspection presentation::component 中的 impl UiScene::drawcomponent public binding 把 Component 接入 scene；scene 核心不依赖 Component trait UiScene::withelement public 让 content closure 与 overlay 进入相同 compute/inspection 管线 UiScene::withcurrentlayerelement / withinspectionnode private 绑定 scene layer、inspection parent 与声明源码位置 UiScene::checkpoint / restore public 保留 scene primitive/layer/inspection 稳定前缀，并复用分配原地替换后续 fragment UiScene::withfragment / replacefragment / removefragment public 以稳定 ElementId 管理 terminal retained fragment，并在移除时恢复 scene/inspection prefix FrameScheduler::request / take public 合并平台帧之间的失效请求，并由 host 在一次 redraw 中消费最高级别的工作 FrameDeadlineSet::include / nextdeadline public 聚合组件、动画和异步轮询的最早单调 deadline；不接触平台 event loop ScalarAnimation::transitionto / advance public 插值一个后端无关的标量值，支持 retarget、easing、deadline 与 snap/cancel AnimationRegistry::transitionto / advance public 按 ElementId + AnimationProperty 保留跨帧 track，聚合 changed keys、fragment IDs、失效等级与 deadline RetainedFragmentRegistry::mount / beginexit / advance public 保留稳定 fragment lifecycle，取消退出、按显式时钟生成到期 cleanup IDs，并投影局部失效 RetainedRuntime::mount / beginexit / advance public 聚合 fragment lifecycle 与 animation tracks；fragment 到期或 unmount 时自动清理其 animation tracks InteractionFrame::register / checkpoint / restore public 记录一帧的绘制顺序、交互节点与 modal scope，并与 retained scene prefix 对齐 InteractionSink::register / setmodalroot public 为 presentation 组合提供后端无关的交互节点与 modal boundary；runtime frame 承担实际保留和分派 UiDispatch public 跨 frame 保存 hover、press/capture、focus 与窗口激活状态，只产生无效请求和 UiIntent UiNode / ElementId / AccessibilityNode public 分别表达当前帧控件语义（含交互失效等级）、跨帧稳定 identity 与不可变 accessibility snapshot；不分配产品 identity 或发布平台 API InspectionFrame::register crate-private 建立单帧 identity、parent、layer 和命中顺序 scene::batching::batches private 保留跨 primitive 的真实插入顺序并合并连续同类 range font::newfontsystem / font::mapping private 固定 layout 与 renderer 共享的 locale/fallback/font mapping policy renderersupport public backend bridge 只向 renderer adapter 暴露共享 shaping policy 3. 执行路径与不变量 ``text UiScene::drawcomponent → Component::element → ComponentElement::compute → computeelement → presentation::inspection::nodeforelement → InspectionFrame::register → Component::paintelement same ComputedElement → UiScene primitives → SceneBatch → zeta-renderer::Renderer ` 新宿主的 canonical 入口是 UiFrame::drawcomponent：它让 scene、inspection、interaction 和 frame clock 由一个 frame owner 进入同一次组合。需要自定义低层 paint 与子组件交错时，使用 ComponentContext::withcomponent；需要无交互的低层 element closure 时才使用 withelement，让 ComponentContext 继续拥有同一个 frame。 旧的 UiScene::drawcomponentwithinteraction、UiFrame::partsmut 和 UiFrame::intoparts 已删除， 不得重新引入平行的 scene/interaction 输出接口。 Element tree 每帧解析，不持有跨帧 identity。InspectionNodeId 只能在当前 UiScene 生命周期内使用； 选中状态如果要跨帧保存，必须由 host 建立自己的稳定 identity。Padding 色块使用 clamp 后的 resolved 值，Inspector 同时可读取原始 ElementStyle。Gap 由 layout 生成实际矩形，不允许 Inspector 猜测。 element 不引用 inspection，scene 不引用 Component；这两个单向绑定分别由 presentation::inspection 和 presentation::component 承担，禁止重新引入双向模块依赖。 FrameScheduler 只合并 Render < Fragment < Rebuild 帧请求，不调用窗口 API，也不决定产品状态如何变化。 Fragment 表示由 host 划定的任意 presentation fragment，不表示 Overlay、Picker 或其他产品拓扑。Host 在收到 FrameSchedule::RequestFrame 时唤醒平台，在 redraw 开始时通过 take 消费工作；同步完成了等价重建时 必须调用 clear，避免下一帧重复执行。 SceneCheckpoint 是当前 retained presentation 的最小边界：它记录 primitive、composition layer 与 inspection prefix，restore 后 host 可以只重建 volatile fragment，并复用 Scene Vec capacity。它不提供 跨 scene identity，也不自动判断 dirty subtree；checkpoint 必须由创建它的同一 UiScene 消费。 RetainedRuntime 是跨帧 retained state 的通用 owner；其内部的 RetainedFragmentRegistry 管理 fragment lifecycle，AnimationRegistry 管理属性 track。Host 在当前组合出现稳定 identity 时调用 mount；重复调用表示 update，不会重置 retained state。组件离开当前组合时调用 beginexit，退出期间不能重新注册 interaction 或 inspection 节点；advance 到达 deadline 后返回 cleanup IDs，并自动移除这些 identity 的 animation tracks。Host 必须用这些 ID 调用 UiScene::removefragment，并恢复配对的 InteractionFrameCheckpoint；如果 fragment 不是 terminal，则 退回 FrameInvalidation::Rebuild，不能静默破坏后续 paint order。 Overlay Element 会创建高于当前 layer 的 scene layer，并在闭包返回后恢复调用者的 layer 与 clip。 panic recovery 不是当前 contract；组件 paint panic 会中断本帧构建。 交互沿同一份 layout bounds 运行：host 在 scene 构建时注册 UiNode，平台事件进入 UiDispatch， 其返回的 UiIntent 再由 host 映射为产品状态转换。zui 不接受 callback registry，也不执行 window drag、菜单、Session 或 editor command。 4. 集成义务 - 组件调用者使用 UiScene::drawcomponent，不能直接调用 Component::paint。 - 拥有自定义 paint 与子组件的 surface 使用 ComponentContext::withcomponent；无交互 scene-only closure 才使用 UiScene::withelement。 - Renderer 只消费 UiScene，不得获得 Component、interaction 或 accessibility frame。 - 使用同一份绘制 bounds 注册 UiNode，不能另行估算命中区域；动态对象在仍表示同一对象时保持 ElementId。 - retained fragment 的 mount/update/unmount 必须由 RetainedRuntime 记录；退出 fragment 不得继续出现在当前 interaction 或 inspector snapshot 中。 - 处理 RetainedRuntimeAdvanceReport::fragment .removedids 时，scene 和 interaction checkpoint 必须 在同一 cleanup 路径收敛；动画 track 由 runtime 自动清理，不能只删 paint 而留下可交互或可检查的 ghost node。 - product host 将 UiIntent 映射为 command，并由 platform adapter 发布 cursor 与 accessibility snapshot；zui 不建立第二套业务 reducer 或平台 adapter。 - zeta-ui 可以兼容 re-export zui API，但 renderer 必须直接依赖 zui，避免基础协议由组件 crate 反向拥有。 5. 测试与修改影响 运行 cargo test -p zui 验证 Element layout、inspection、scene batching、font/text layout、text input、 Split/Grid、interaction 与 primitive contract。修改以下边界时还需同步验证： - architecturetests 验证物理分层、canonical dependency path、500 行模块上限、显式 root export， 以及平台、GPU、组件和产品依赖禁令； - scene primitive 或 batch ordering：zeta-renderer 与全部 backend； - font/text contract：zeta-wgpu、zeta-ui、zeta-editor、zeta-markdown； - Element/Component contract：zeta-ui、zeta-keybinding、zeta-editor 与 native 架构审计； - geometry/layout/interaction contract：native root/split/grid tests。 6. 当前限制与扩展点 当前具备帧级失效合并、host 划定的 Scene/interaction prefix checkpoint、terminal fragment 移除、 RetainedRuntime、RetainedFragmentRegistry、AnimationBinding 和通用 pointer/focus 语义，但没有自动子树级 dirty propagation、跨帧 layout cache、style cascade、运行时样式编辑、path primitive、disabled/live-region/ text-selection accessibility semantics 或平台 accessibility adapter。产品级 exit retention 仍需各 fragment 明确保留内容、退出目标和 reduced-motion policy；GPU backend 变化不应改变 zui` public contract。"
  },
  {
    "slug": "crates/zeterm/zui-demo",
    "sourcePath": "zeterm/zui-demo/README.md",
    "title": "zui-demo",
    "description": "zui-demo is a framework-only host smoke test. It composes a zeta-ui::ActionBar with a local zui::Icon, submits the resulting UiScene through the zeta-renderer::Renderer contract, and",
    "group": "Crate 实现参考",
    "markdown": "`zui-demo` is a framework-only host smoke test. It composes a `zeta-ui::ActionBar` with a local\n`zui::Icon`, submits the resulting `UiScene` through the `zeta-renderer::Renderer` contract, and\nuses an in-memory recording backend. It intentionally has no dependency on `zeterm`, commands,\nterminal sessions, App Server state, `zeta-icons`, `winit`, or `wgpu`.\n\nThe crate is not a production application. Its purpose is to keep the portability claim\nexecutable: if a reusable component starts requiring product state or a platform backend, this\nhost should fail to remain small and dependency-neutral.\n\nVerification:\n\n```text\ncargo test -p zui-demo\ncargo run -p zui-demo\n```",
    "headings": [],
    "searchText": "zui-demo is a framework-only host smoke test. It composes a zeta-ui::ActionBar with a local zui::Icon, submits the resulting UiScene through the zeta-renderer::Renderer contract, and uses an in-memory recording backend. It intentionally has no dependency on zeterm, commands, terminal sessions, App Server state, zeta-icons, winit, or wgpu. The crate is not a production application. Its purpose is to keep the portability claim executable: if a reusable component starts requiring product state or a platform backend, this host should fail to remain small and dependency-neutral. Verification: ``text cargo test -p zui-demo cargo run -p zui-demo ``"
  }
];
export const docGroups: DocGroup[] = [
  {
    "label": "开始了解",
    "slugs": [
      "README",
      "architecture",
      "product-editions",
      "product-lines",
      "zeta-rs-architecture",
      "zeta-desktop-architecture",
      "zeta-cli-architecture",
      "zeta-agent-runtime-architecture"
    ]
  },
  {
    "label": "界面与体验",
    "slugs": [
      "ui-styling-ownership",
      "workbench-pane-composite-design",
      "design-tokens",
      "theme-authoring-template",
      "menu-system",
      "icons",
      "search",
      "code-intelligence",
      "code-index",
      "syntax-analysis",
      "lsp",
      "editor-extensions",
      "editor-architecture",
      "editor-core",
      "pdf",
      "typst",
      "chat-session-inspector",
      "tui"
    ]
  },
  {
    "label": "zeterm 产品",
    "slugs": [
      "zeterm/README",
      "zeterm/native-agent-console",
      "zeterm/native-terminal-ui",
      "zeterm/native-text-input",
      "zeterm/rendering-architecture",
      "zeterm/ui-component-migration-plan",
      "zeterm/native-deprecation-plan",
      "zeterm/zeterm-app-migration-plan",
      "zeterm/zeterm-release-graph"
    ]
  },
  {
    "label": "Agent 与运行时",
    "slugs": [
      "core",
      "agent-harness-design",
      "agent-tools-spec",
      "agent-harness-implementation-plan",
      "core-context",
      "core-multi-agent",
      "agent-customizations",
      "exec",
      "tools",
      "skills",
      "plugins",
      "slash-commands",
      "mcp",
      "mcp-server"
    ]
  },
  {
    "label": "安全与权限",
    "slugs": [
      "permissions",
      "auto-review",
      "sandboxing",
      "workspace-security",
      "secrets",
      "windows-sandbox-acceptance-runbook"
    ]
  },
  {
    "label": "API 与协议",
    "slugs": [
      "zeta-api",
      "zeta-api-interface-requirements",
      "zeta-api-interface-template",
      "zeta-app-server-api",
      "app-server-client",
      "codex-app-server",
      "protocol",
      "zeta-client"
    ]
  },
  {
    "label": "模型与配置",
    "slugs": [
      "models-manager",
      "model-provider",
      "model-provider-config",
      "config",
      "login"
    ]
  },
  {
    "label": "平台与交付",
    "slugs": [
      "git",
      "documentation-guidelines"
    ]
  },
  {
    "label": "计划与迁移",
    "slugs": [
      "zeta-code-architecture-codex-style-v2"
    ]
  },
  {
    "label": "其他系统文档",
    "slugs": [
      "connectors",
      "debugging"
    ]
  },
  {
    "label": "Crate 实现参考",
    "slugs": [
      "crates/auto-review/evals",
      "crates/ext/connectors",
      "crates/ext/mcp",
      "crates/utils/typst/licenses",
      "crates/ext/web-search",
      "crates/action-policy",
      "crates/add-dir",
      "crates/agent-import",
      "crates/zeterm/agent-sidebar",
      "crates/agents",
      "crates/zeta-api",
      "crates/app-server",
      "crates/app-server-client",
      "crates/app-server-protocol",
      "crates/async-utils",
      "crates/attachments",
      "crates/auto-review",
      "crates/bwrap",
      "crates/code-index",
      "crates/code-index-cloud",
      "crates/code-index-semantic",
      "crates/code-retrieval",
      "crates/codex-app-server",
      "crates/collaboration",
      "crates/collaboration-server",
      "crates/zeterm/commands",
      "crates/zeterm/composer",
      "crates/config",
      "crates/connectors",
      "crates/context-engine",
      "crates/debug-adapter",
      "crates/diff",
      "crates/zeterm/editor",
      "crates/editor-core",
      "crates/editor-extension-host",
      "crates/exec",
      "crates/execpolicy",
      "crates/ext/extension-api",
      "crates/extensions",
      "crates/file-icons",
      "crates/file-identity",
      "crates/file-search",
      "crates/file-system",
      "crates/file-system-tool",
      "crates/file-watcher",
      "crates/git",
      "crates/history",
      "crates/http-client",
      "crates/zeterm/icon",
      "crates/zeterm/icons",
      "crates/input-classifier",
      "crates/install-context",
      "crates/instructions",
      "crates/zeterm/keybinding",
      "crates/zeterm/native-keybindings",
      "crates/keyring-store",
      "crates/language-marketplace",
      "crates/language-server-catalog",
      "crates/language-server-distribution",
      "crates/language-service",
      "crates/zeterm/layout",
      "crates/linux-sandbox",
      "crates/login",
      "crates/lsp",
      "crates/zeterm/markdown",
      "crates/mcp",
      "crates/mcp-server",
      "crates/model-provider",
      "crates/model-provider-config",
      "crates/model-tokenizer",
      "crates/models-manager",
      "crates/utils/pdf",
      "crates/plugin-marketplace",
      "crates/plugins",
      "crates/prompts",
      "crates/protocol",
      "crates/zeterm/renderer",
      "crates/rmcp-client",
      "crates/rollout",
      "crates/rollout-trace",
      "crates/search",
      "crates/secrets",
      "crates/session-store",
      "crates/zeterm/settings",
      "crates/shell-command",
      "crates/shell-completion",
      "crates/skills",
      "crates/ext/skills",
      "crates/slash-commands",
      "crates/slash-launcher",
      "crates/storage",
      "crates/symbol-index",
      "crates/syntax",
      "crates/terminal",
      "crates/terminal-detection",
      "crates/text-file",
      "crates/theme",
      "crates/thread-store",
      "crates/tool-executor",
      "crates/utils/typst",
      "crates/zeterm/ui",
      "crates/utils/image",
      "crates/utils/path-utils",
      "crates/utils/path-uri",
      "crates/utils/pty",
      "crates/zeterm/wgpu",
      "crates/windows-sandbox-rs",
      "crates/zeterm/winit",
      "crates/workspace",
      "crates/zeterm/",
      "crates/zeterm/zui",
      "crates/zeterm/zui-demo"
    ]
  }
];
